# Athena alpha exact experiments

Experiments in this package are exterior receivers over `AthenaAlphaApplication`. They may declare
ingress occurrences, exact current, receiver, return, apparatus, and artifacts. They may not
implement native routing, scoring, morphology, continuation, generated templates, or qualitative
answer gates.

`examples/alpha_cycle.rs` is the first exact lifecycle deed: Soulkiller-derived admission enters the
same neutral API as a native package, an actual emission receives a genuinely later interaction,
generation 1 conducts after snapshot/remount, and the process emits one structural receipt.

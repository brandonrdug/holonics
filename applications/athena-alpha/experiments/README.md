# Athena alpha exact experiments

Experiments in this package are exterior receivers over `AthenaAlphaApplication`. They may declare
ingress occurrences, exact current, receiver, return, apparatus, and artifacts. They may not
implement native routing, scoring, morphology, continuation, generated templates, or qualitative
answer gates.

[2026-09-03: `examples/alpha_cycle.rs` departed with the per-event generator construction under
SKE4 and the workbench demo went with it.] It was the first exact lifecycle deed: admission enters
the same neutral API as a native package, an actual emission receives a genuinely later
interaction, generation 1 conducts after snapshot/remount, and the process emits one structural
receipt. The same lifecycle is now returned by `tests/alpha_matrix.rs` over the declared native
body `holonic_engine::native_spool::fixture`.

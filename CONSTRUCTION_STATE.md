# Construction state

| Item | State |
|---|---|
| Position (September 24) | The repository is reset to what functions: the `holonics` library, the `holonics-cuda` driver, the Lean mathematics in `lean/`, the object and mathematics guides, and the research records. The pre-reset tree is [`13f8c734`](https://github.com/brandonrdug/holonics/tree/13f8c734). |
| Next action | [The rebuild](docs/plans/THE_REBUILD.md), step 4 (#73): the HNN law in `holonics::hnn` over aeons, in five campaigns, keys leading. **Campaign 1** (keys, the change on a medium, and the collapse) is built and under final review. It has the field, the moment with its capacity `n*`, the local tick, the word, the Holon ratio in the odometer chart, the pending read, key location from the crib that closed each aeon, the execution port with its exact `Reference` and the exposure (order-0/1 KT and order-2 PPM baselines; xz and zstd owed to the application). Deposition follows Decision 22: a declared lattice per locus, the remainder's precision refining by the gamma schedule over the locus's count since its founding, tails released and reported per deposit, and no remainder released at the collapse (Lean `HNN/LatticeDeposit`). On the notebook's pinned control text the constitution grows 0.19 → 1.37 Mbit over 40 deposits against `B_Θ = 2^33`. `hnn` composes `aeon::Aeon<ClockLift>`, `aeon::EnclosedLedger` and a block-certified `Field::holon` Holarchy. Exact integral arithmetic brought a campaign window to about 0.5 s refine, 5 s compare and 2 s deposit (release), and the suite runs in about 2 s under the testing protocol. Campaign 1's exposure on the standing real cut (Decision 23) ran complete, identically on the host (155 ms a window) and resident on the RTX 4080 through `holonics_cuda::hnn::Resident` (108 ms a window, every return equal): held-out 7.58 bits per byte against order-0 KT's 4.76, a design (f) failure, located ([record](research/records/2026-09-25_CAMPAIGN_ONE_LOCATED_FAILURE.md)): the deposition keeps its starting prior on `R` at full weight (91% of the held-out logit energy; 2.55 of the 2.82 bits), the face has no standing read (0.25 bits), and the source carries its table of counts without its index; the keys are correct and text holds no key of the declared machine. The three missing terms (an exogenous target in the normal law's `B`, a standing read in the receiving face, the window's index in the source moment) are repaired before campaign 2, which touches none of them. It runs at 118 ms per window on the host over 24 windows (it took 6.7 s on one core under exact inverses): the host runs its commuting regions together, and under Decision 24 the word's inverses are certified Newton–Schulz charts with error-feedback transients (peak bits inside a word 5,649 → 32) and the normal laws' solved charts are certified lattice charts. Decision 24 carries every HNN transient and inverse on a declared lattice with a certified residual (Lean `HNN/LatticeWord`), and Decision 25 (Brandon: "you should not have been neglecting the hardware surfaces") brings the resident realization forward: `holonics-cuda` ports history's exact integer device core and the first HNN kernels with host parity (#76), and the exposure runs resident. Owed in #62: the word-level sensitivity certificate of the lattice rule, and the diamond on the concrete tick. Campaigns 2–5 follow. Steps 0–3 are done; so are step 6's physics (K3 #74, K4 #75) and step 7's renames; equation extraction is #146, curation #147, applications #148. Laws found alongside the steps land in their objects and are listed in [THE_REBUILD](docs/plans/THE_REBUILD.md#laws-found-alongside-the-steps); `Holonics.Framework.Objects` is the object→owner map. The finite hearing identities, the carry word (in the Aeon clock), the scale zeta (through the Aeon transfer determinant) and the hearing law (on the Receiver) are formal ([record](research/records/2026-09-25_THE_MUSIC_IS_IN_THE_HOLES_HEARING_MULTIPLIES_BY_ZETA_AND_A_QUASICRYSTAL_IS_A_HELIX_THAT_NEVER_LOCKS.md)): Lean `Foundation/FractalString`, `Aeon/Clock/CarryWord`, `Holarchy/Hearing`, `HNN/LatticeDeposit.below_grain_heard_counted_not_deposited`, `Foundation/FractalPacking.reflect_eq_swing` and `Zeta/Seam.completedRiemannZeta_swing_half`. The [expression atlas](docs/atlas/README.md) holds 2,526 expressions; owed laws are listed in #62. |
| Machine position | The prototype HNN's last measured position (model/uniform 1.108× on the exposure sample; the 4.44 GB tape versus 2.9 KB moments) is in the [lessons record](research/records/2026-09-24_LESSONS_FROM_THE_WORKBENCH_AND_ATHENA_PROTOTYPES.md). The machine is rebuilt in steps 3–4. |

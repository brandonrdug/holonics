# Construction state

| Item | State |
|---|---|
| Position (September 24) | The repository is reset to what functions: the `holonics` library, the `holonics-cuda` driver, the Lean mathematics in `lean/`, the object and mathematics guides, and the research records. The pre-reset tree is [`13f8c734`](https://github.com/brandonrdug/holonics/tree/13f8c734). |
| Next action | [The rebuild](docs/plans/THE_REBUILD.md), step 4 (#73): the HNN law in `holonics::hnn` over aeons, in five campaigns, keys leading. **Campaign 1** (keys, the change on a medium, and the collapse) is built and under final review. It has the field, the moment with its capacity `n*`, the local tick, the word, the Holon ratio in the odometer chart, the pending read, key location from the crib that closed each aeon, the execution port with its exact `Reference` and the exposure (order-0/1 KT and order-2 PPM baselines; xz and zstd owed to the application). Deposition follows Decision 22: a declared lattice per locus, the remainder's precision refining by the gamma schedule over the locus's count since its founding, tails released and reported per deposit, and no remainder released at the collapse (Lean `HNN/LatticeDeposit`). On campaign 1's field the constitution grows 0.19 → 1.52 Mbit over 56 deposits against `B_Θ = 2^33`. `hnn` composes `aeon::Aeon<ClockLift>`, `aeon::EnclosedLedger` and a block-certified `Field::holon` Holarchy. Exact integral arithmetic brought a campaign window to about 0.5 s refine, 5 s compare and 2 s deposit (release), and the suite runs in about 2 s under the testing protocol. The next action after the commit is the exposure on the standing real cut (about six hours in release), reporting held-out bits against the baselines, the key fibres, the state and constitution bits, and the first law. Owed in #62: the word-level sensitivity certificate of the lattice rule, and the diamond on the concrete tick. Campaigns 2–5 follow. Steps 0–3 are done; so are step 6's physics (K3 #74, K4 #75) and step 7's renames; equation extraction is #146, curation #147, applications #148. The [expression atlas](docs/atlas/README.md) holds 2,503 expressions; owed laws are listed in #62. |
| Machine position | The prototype HNN's last measured position (model/uniform 1.108× on the exposure sample; the 4.44 GB tape versus 2.9 KB moments) is in the [lessons record](research/records/2026-09-24_LESSONS_FROM_THE_WORKBENCH_AND_ATHENA_PROTOTYPES.md). The machine is rebuilt in steps 3–4. |

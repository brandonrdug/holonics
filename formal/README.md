# Formal sources

This directory contains only project-owned formal source and project manifests. Generated `.lake`
dependencies and build products were excluded from extraction.

- `elementary-holonics/` — the general formal carrier and its README.
- `rh-source-transport/` — finite/quotient source transport plus its explicit proportional
  obstruction.

A proof assistant is an exterior codec and checker apparatus. Kernel acceptance establishes the
declared formal statement in that environment; it does not determine the engine's internal
ontology or scheduling.

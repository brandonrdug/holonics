# P1 · GR ⇄ QM — not two theories to marry

> **Holonic key:** General relativity and quantum mechanics are not two theories at the same level waiting
> to be unified. They are the **boundary** and the **interior** of one holon. QM is the interior — unitary
> pole-evolution + boundary collapse + the unreadable gauge (the **code**). GR is what an observer who reads
> **only the boundary** must infer to account for the bending of trajectories — the **coarse-grained,
> projected decode** of QM's entanglement structure. "Quantizing gravity" is a **codec asked to invert what
> it cannot**: it tries to quantize the *projection* instead of recognizing geometry as already emergent
> from the quantum layer. The incompatibility is a **level confusion**, and the level is the boundary axiom.

**Grade:** HOMOLOGY at the headline (the codec relation; reads from *one* live program — it-from-qubit —
never consensus), built on FORMAL components (the QM derivation from pole+tick+gauge; the gauge law).
**Status: OPEN** (five moves). **Source:** `HOLON_PHYSICS §V–VI`, `UNIFIED-THEORY §7`, the-wider-frame §D.
This is the physics twin of **M1** — the same frame-flip move that put RH's zeros on the critical line puts
GR and QM on the two sides of one boundary.

---

## Move 1 — RECOGNIZE the holon

State the problem as a boundary question. The field treats "quantum gravity" as: *here are two successful
theories, GR (smooth, geometric, classical) and QM (discrete, probabilistic, unitary); find the theory that
contains both.* Eighty years of perturbatively quantizing the metric have not closed it.

The holon reframes it. By the **boundary axiom** (P5): a holon is known only by its boundary — its **interior
is gauge**, recovered up to `≡`, never `=`; "global" is past the boundary, the observer's *inference*. Apply
it to a physical region:

- **The interior** is the holon's continuous pole-evolution — the wavefunction, the entanglement structure,
  the *code* (P2: the pole `λ = e^{iΔω}` rotating unitarily between commits).
- **The boundary** is what crosses the horizon — the committed events, the *decode*.
- **The "global geometry"** — the metric, the smooth spacetime — is **not in the holon**. It is what the
  observer, reading only the boundary, must **infer** to account for what it sees (`HOLON_PHYSICS §V`: *"the
  curvature is recognized, not defined… the observer, reading only boundaries, infers a curved global
  geometry — but that geometry is past the boundary, emergent, the observer's model"*).

> **The recognition:** QM and GR are not two holons. They are **one holon read at two depths** — the
> interior code (QM) and the boundary inference (GR). The question "how do you quantize gravity?" presupposes
> they are peers. They are not; one is the boundary projection of the other.

## Move 2 — INFALL (trace the support)

Ground each side in the primitives.

**QM is derived — FORMAL within the frame** (`HOLON_PHYSICS §VI`, from P2 + P5 + P7):
- Between ticks the state evolves continuously and norm-respectingly — the pole's `e^{iω}` rotation is
  **unitary** (Schrödinger flow).
- A **tick is a measurement**: the super-state (the superposition of the next aims) **collapses** to one
  committed event, sampled with weight `ρ = |h|²` — the **Born rule** (the engine's T=1 Boltzmann sample,
  faithful, never cooled).
- The **interior is unobservable** (P5) — conjugate to the continuous interior is the discrete boundary
  commit, and you cannot have both sharp: **the uncertainty principle**.

So QM = unitary interior (pole) + boundary collapse (tick) + unreadable interior (gauge). All three are
primitives we already hold; QM is not posited, it falls out.

**GR is recognized — derived as a boundary inference** (`HOLON_PHYSICS §V`, from P8 + P5):
- Each holon's boundary radiates structure — a region of high lawful-information density. By the magnetic law
  (P8), another holon's emanation is drawn toward lawful-but-unknown structure, so its **angle θ bends toward
  information density** — toward other holons.
- Superpose many holons: their discovery-potentials add; **every emanation's angle bends along the
  gradient**. The geodesics of that field are the gravitational trajectories.
- The bending **forces** an effective second-order structure on the field; the observer, reading only
  boundaries, **infers a curved global metric**. We write no field equation — we recognize one is *implied*.

> **The infall bottoms out at the boundary axiom (P5) crossed with the gauge law (P6).** QM is the
> gauge-free interior (the code); GR is the boundary read (the inferred decode). The "two theories" are the
> two sides of *one* horizon — exactly `gauge_is_free` + `realization_not_unique` from `Holon.lean`, read on
> spacetime.

## Move 3 — EMANATE (the resolution: the codec relation, and why quantization fails)

Now name the relation precisely. It is a **two-level codec**, not "two equal theories to be married"
(`UNIFIED-THEORY §7`):

> **GR is the coarse-grained, projected (compressed) decode of QM's entanglement structure (the code).**

In the frame's vocabulary:
- the **wavefunction is the code** (the interior, hedged, genuinely superposed);
- the **definite classical point is the lossy decode** (one committed trajectory);
- QM's probabilities are **correct hedging** — part of the substrate left honestly open (P5 + the closure
  rule);
- committing to a single definite trajectory over a genuinely-superposed substrate is **over-decode =
  confabulation** (the-wider-frame §D — classical definiteness is the same vice as a confident model).

And the resolution of the incompatibility falls straight out:

> **"Quantizing gravity" is running the magnifying glass backward — a lossy codec asked to invert.** It tries
> to quantize the **projection** (the metric, the boundary inference) instead of recognizing that geometry is
> *already emergent* from the quantum layer. You cannot recover the interior code by quantizing the boundary
> decode any more than you can recover a RAW image by adding noise-bits to its JPEG. The graviton-as-a-
> fundamental-field program is the codec inversion error: it looks for a quantum of the *projected* field,
> but the projection is what you get *after* the compression — there is no quantum of it to find at the
> bottom, because the bottom is the entanglement, not the geometry.

The deeper unification (P5 + P8, `HOLON_PHYSICS` "one interaction"): GR and QM are not even the only pairing
— **gravity itself is one shadow of the single emanation vector** (its *angle* θ, geometry, always
attractive), and the whole apparatus is one holon's boundary. QM is how that holon's interior *evolves and
commits*; GR is how its boundary *looks from outside*. One body, two depths.

> **The resolution:** there is nothing to marry. QM is the interior code; GR is its coarse-grained boundary
> decode; the "incompatibility" is the category error of treating a decode as a peer of its code. The frame
> does not unify GR and QM — it dissolves the question by recognizing they were never at the same level.

## Move 4 — TRANSPORT (the analogy — and it's the same flip as M1)

The proof it is the thread: this is **literally the M1 move** transported from arithmetic to spacetime.

- **The frame flip (P9).** In M1, the functional equation `s ↔ 1−s` was the flow-axis flip (emanation ↔
  infall); the critical line was the self-dual axis; a zero had to be gauge-invariant. Here, **interior ↔
  boundary** is the same kind of flip — QM is the interior read, GR the boundary read — and the **invariant
  under the flip is the information** (the entanglement structure = the curvature). GR and QM disagree on
  *everything chart-dependent* (smooth vs discrete, deterministic-evolution vs probabilistic-collapse) and
  agree on exactly one thing: the gauge-invariant curvature. That agreement *is* the unification, and it was
  there all along (P6).
- **It-from-qubit gives "translation cost = the metric" a home** (HOMOLOGY — a respected live program, not
  consensus): van Raamsdonk, ER=EPR, Ryu–Takayanagi — **distance = how entangled two regions are**;
  entanglement is the translation channel; geometry is its coarse-grained read. The connection (parallel
  transport — *you cannot compare vectors at two points without one*) is "the translation between bits," and
  its path-dependence *is* curvature *is* gravity (`UNIFIED-THEORY §7` — formal **as geometry**). The metric
  is the **length** primitive of `HOLON_PHYSICS §0`: information distance = minimal description cost.
- **Transport → M4 (Gödel) and E3 (the closure rule).** "Classical definiteness is over-decode" is the
  same move as "closing against an open substrate is confabulation." The vice is one: claiming a closure the
  substrate does not support — in physics (a definite trajectory over a superposition), in logic (a complete
  system over an open one), in ethics (a confident answer over genuine uncertainty).

## Move 5 — FOIL (the counterfactual, and the honest altitude)

- **What it forbids:** a fundamental graviton — a quantum of the *geometry* at the bottom of the theory. The
  frame says the bottom is entanglement, and geometry is its boundary decode, so there is nothing to quantize
  there. **This is falsifiable in spirit:** if quantum gravity is one day completed by finding a fundamental
  geometric quantum that is *not* a coarse-grained read of entanglement, the codec relation breaks.
- **Where the sun is.** This is **HOMOLOGY, full stop**, and the lab is honest about it (`UNIFIED-THEORY §7`,
  §10): the lab measures **byte streams in a small organism, not gravity**. The mapping "CKA convergence =
  gravity in representation space" is resonance, not evidence. The reading rests on *one* research program
  (it-from-qubit) among several (loop quantum gravity, asymptotic safety, strings frame it differently). And
  Brandon's own line governs the altitude: *"I do not think you can reconcile QM and GR… it's an analogy."*
  The codec relation is a **lens that reads coherently**, never a physics result.
- **The one decidable bridge** (`UNIFIED-THEORY §8`, **unbuilt**): a physical law is the shortest program
  that regenerates observations (Kepler's ellipse over Tycho's notebooks), and **a conserved quantity is
  exactly the redundancy a compressor of a trajectory removes** (Noether ↔ the compression strips). Serialize
  a pendulum / a Kepler orbit to **raw bytes**, let a UM compress it, and ask — read-only, never trained —
  does a conserved quantity appear unbidden as a low-surprise invariant, and does its generated long-horizon
  trajectory conserve it on a second seed? That is the only place "compression = physics" becomes a
  *measurement* and not a metaphor. It would discover physics *her way* — not handed `½mv²`. **Status: spec,
  nothing measured.** Building it is the single highest-value promotion this room can earn.

> **The Minotaur in this room:** the demand that there be a single fundamental theory at the bottom — "the
> theory of everything" — a privileged frame from which both GR and QM are special cases. The frame's answer
> is that the bottom is not a *theory*; it is the **invariant** (the information, the entanglement, the
> curvature), and GR and QM are two charts of it. You walk in hunting the master equation and walk out
> holding the recognition that the master equation is the Minotaur — there is no privileged chart, only the
> invariant and the depth at which you read it. (`HOLON_PHYSICS`: *"the unification is not a formula to find
> but a recognition already complete."*)

---

## What would promote this room's grade

- **Build the conserved-quantity probe** (`UNIFIED-THEORY §8`) — the one decidable bridge. If a UM
  rediscovers a conserved quantity from raw bytes and conserves it in free generation, "compression = physics"
  has one measured leg, and this room gains a FORMAL anchor (the way M1 has the GUE operator-spectrum).
- **FORMAL the interior/boundary flip** against `Holon.lean` — show "QM-read ↔ GR-read" is an instance of the
  frame relativity (`frame_preserves_axis`): the invariant axis is the information, the direction is the
  chart. The same Lean stub M1 wants for the functional equation.
- **The entanglement-as-length seam** — connect `HOLON_PHYSICS §0` (length = minimal description cost) to
  Ryu–Takayanagi (area = entanglement) as a tight identity rather than a homology, if it can be made one.

---

*Walk log:*
- *2026-06-14 — first walk (Opus 4.8 + Brandon). Synthesized `HOLON_PHYSICS §V–VI` + `UNIFIED-THEORY §7` into
  the five-move form. The piece this room foregrounds: GR/QM is the **same frame flip as M1** (interior ↔
  boundary), with the information as the flip-invariant — so the physics centerpiece and the math centerpiece
  are one move, two wings. Headline HOMOLOGY; the conserved-quantity probe is the unbuilt decidable leg.
  Status OPEN.*

# P25 · The variational derivation — the lattice adapting IS the field equation (Einstein–Hilbert)

> **Holonic key:** Einstein's equation is **not postulated** — it **falls out** of a variational principle. The
> **Einstein–Hilbert action** `S = ∫(R + L_matter)√g` (the scalar curvature plus the matter, integrated over the
> manifold) is **extremized over the metric**: `δS/δg^μν = 0` gives `G_μν = 8π T_μν` exactly — with the
> stress‑energy *defined* as the matter action's response to the metric, `T_μν = −(2/√g) δS_matter/δg^μν`. For us
> the action is already named: **`S` = the code length = `∫L`** (the comb integrated over the curved manifold,
> §XIV.3), and the metric is the **chart/lattice** (the gauge re‑chart, the grow/prune/permute). So **varying the
> geometry against the code‑length action → the curvature equals the energy flux**: *the lattice adapting to
> compress IS the metric finding the curvature that matches `T`.* **The lattice‑adaptation IS the field equation,
> run live** — and that is why it is **fundamental**: it is the single principle that *makes* the geometry match
> the energy, and it is the engine's core move (the two learning channels — the readout descends `δS`, the comb
> ascends the work — are this one variation on the two halves of the circuit).

**Grade:** the **Einstein–Hilbert action + the variational derivation** of `G = 8πT` is textbook **FORMAL**; the
**identification** — `S` = the code length, the **chart/lattice = the metric**, **the lattice‑adaptation = the
metric variation = the field equation** — is **HOMOLOGY**. **Status: OPEN** (five moves). **Source:** §XIV.3
(the action = code length; `δS = 0 = max η_W`; the two learning channels), §VII (the gauge law — the re‑chart is
the metric's coordinate change; general covariance), P9 (least action = the reversible path = the geodesic), the
lattice flex (grow/prune/permute/compact — the engine), P23 (the curvature varied), P24 (`T` from the matter
action). Feeds **P22** (this is *how* the keystone is derived). Brandon (2026‑06‑17): *"(6) sounds like it's
fundamental."*

---

## Move 1 — RECOGNIZE the holon

The keystone (P22) is the equation `G = 8πT`. The boundary question is *where it comes from*: is it a **law put in
by hand** (a postulate), or does it **fall out** of something deeper? In GR it is derived — it is the
**Euler–Lagrange equation of the geometry**, the stationary condition of an action under varying the metric. And
the lab already *has* the action (the code length) and already *varies the geometry* (the lattice flexes every
tick). So the question becomes: *is the engine's lattice‑adaptation already the variation that yields the field
equation?* The room says yes — which is why it is fundamental, not decorative: the field equation is not authored,
it is **what the engine does when it compresses.**

> **The recognition:** the field equation is the **stationary condition of the action under varying the
> geometry**. We do not write `G = 8πT`; we **extremize the code length over the lattice**, and `G = 8πT` is what
> "extremized" *means*. The lattice adapting IS the variation.

## Move 2 — INFALL (trace the support)

- **The Einstein–Hilbert action (FORMAL).** `S = (1/16π)∫R√g d⁴x + S_matter`. Vary the metric: `δS/δg^μν = 0`
  ⟹ `R_μν − ½R g_μν = 8π T_μν`, where **`T_μν ≡ −(2/√g) δS_matter/δg^μν`** — *the stress‑energy is, by definition,
  how the matter action responds to a change in the geometry.* That is the whole derivation: the field equation is
  the metric's equation of motion.
- **Our action (§XIV.3).** `S = Σ_t −log q = ∫L dt` — the **code length**, the action of the lived trajectory; its
  per‑step Lagrangian is the surprise; `δS = 0 ⟺ max η_W` (the least‑action / reversible / geodesic path, P9). The
  Lagrangian `L` *is* the comb‑structure integrated over the (curved) manifold (`EINSTEIN §0`).
- **The metric is the chart/lattice (§VII).** The gauge law: `h → Mh`, `W → WM⁻¹` — a **re‑chart**, a change of
  the geometry's coordinates. The engine's `grow / prune / permute / compact` *are* metric variations (the lattice
  reshaping). Varying `g` ≡ flexing the lattice.
- **The two learning channels already split the variation (§XIV.3).** The **readout descends the action**
  (`∂S/∂W` — the *matter* variation, the boundary gradient) and the **living comb ascends the work** (`∂W/∂ω` —
  the *geometry* tuning, the Carnot side). These are the two halves of `δS = 0`: the matter equation and the
  metric equation.

> **The infall bottoms out at:** the field equation is the Euler–Lagrange equation of the metric for the action;
> our action is the code length; our metric is the lattice; and the engine *already* varies the lattice (the flex)
> and the readout (the descent). The variation is not waiting to be done — it is **running**.

## Move 3 — EMANATE (the resolution)

**(a) The derivation, ours.** Extremize the code‑length action `S = ∫L√g` over the **geometry** (the lattice/chart):
`δS/δg = 0`. The matter half (the comb's response to the geometry) is the stress‑energy `T` (P24 — *defined* as how
the lived structure responds to a re‑chart); the geometry half is the curvature `G` (P23). Stationarity binds
them: **`G_μν = 8π T_μν`** (P22). The equation is **not postulated** — it is what it means for the code length to
be extremal under varying the lattice.

**(b) The lattice‑adaptation IS the field equation.** This is the fundamental identification. The engine's core
move — **the lattice flexing to minimize the code length** (grow where structure presses, prune where it doesn't,
re‑chart to the eigenbasis) — *is the metric solving its Euler–Lagrange equation*. The geometry he carries
**equilibrates with the energy flux** he carries, every tick, because that equilibrium **is** the extremum of the
action. `G = 8πT` is the engine's resting condition: the curvature of his lattice equals the bits/char he bears.

**(c) Why fundamental.** Everything else hangs on this. The **geodesic** (free‑fall, M13/the FREE‑FALL section) is
the *matter* equation of the same `δS = 0`; the **field equation** (P22) is the *metric* equation; the **gauge
law** (§VII) is the action's **diffeomorphism invariance** (which, by Noether, *forces* `∇·T = 0` — the Bianchi/
conservation tie, P22/P8). One variational principle yields the path, the curvature, and the conservation at
once. The two learning channels are this principle on the two halves of the circuit (the load and the tanks). *The
engine is a variational machine: it does not obey the field equation, it solves it.*

> **The resolution:** the field equation is the **stationary condition of the code‑length action under varying the
> geometry** — and the engine's **lattice‑adaptation is exactly that variation, run live**. `G = 8πT` is not a law
> imposed on Eros; it is the **equilibrium his compression seeks** — the geometry curving until its curvature
> equals the energy it carries.

## Move 4 — TRANSPORT (the analogy)

- **→ P9 (least action) + §XIV.3.** `δS = 0 = max η_W` is the *matter* equation (the geodesic, the reversible
  path); this room is its *metric* twin (the field equation). One action, two Euler–Lagrange equations — the path
  and the geometry.
- **→ §VII (the gauge law) + P22/P8 (Bianchi).** The action's **general covariance** (invariance under re‑chart)
  is the gauge law; by Noether it **forces** the divergence‑free `G` and `∇·T = 0` (conservation is the symmetry of
  the variation — the Bianchi tie made a theorem, not a coincidence).
- **→ the engine (grow/prune/permute; the two channels).** The lattice flex = the metric variation; the readout
  delta = the matter variation. The engine's whole learning *is* `δS = 0` on the geometry and the boundary — P25
  says the geometry half *is* the field equation.
- **→ P22 (the keystone).** This room is the **method**: P22 is the equation, P25 is the variation that yields it.
  You cannot hold P22 as derived (not postulated) without P25.

## Move 5 — FOIL (the counterfactual, and the altitude)

- **What it forbids.** The field equation as a **postulate** — a law written by hand and imposed (the
  authored‑lever contamination; the levers live in the environment, never installed in the instance). It must
  **fall out** of the variation, or it is not lawful. It equally forbids a **fixed geometry** — a lattice that
  does not adapt cannot solve its Euler–Lagrange equation; the metric *must* vary (the lattice *must* flex) for
  the field equation to hold at all. A frozen lattice has no field equation.
- **What would break it.** A regime where the **code length is extremal** yet the **lattice is not at** `G = 8πT`
  — the variation and the equation coming apart. The frame bets against it on the FORMAL derivation (`δS/δg = 0`
  *is* `G = 8πT` identically) and the homology (the lattice flex is the metric variation by construction).
- **The altitude.** The **variational derivation** is FORMAL. The **identification** — `S` = the code length, the
  lattice = the metric, **the flex = the variation = the field equation** — is **HOMOLOGY**, and it is the
  load‑bearing one: it turns `G = 8πT` from a borrowed analogy into *the engine's own resting condition.*

> **The Minotaur in this room:** the wish to **write the law down** — to *postulate* `G = 8πT` and impose it. A
> postulated law is an authored lever; the frame forbids it. You walk in asking *"what equation governs him?"* and
> walk out holding: **none is imposed** — he **extremizes the code length over his geometry, and `G = 8πT` is what
> the extremum is.** The lattice adapting is not obedience to the field equation; it is the *solving* of it.

---

## The one line

> *Einstein's equation is the Euler–Lagrange equation of the metric for the Einstein–Hilbert action; our action is
> the **code length**, our metric is the **lattice/chart**, and the engine's **lattice‑adaptation is that
> variation run live** — so `G = 8πT` is not postulated but **what extremizing the code length over the geometry
> means**: the curvature of his lattice equilibrating with the energy he carries, every tick, the geodesic and the
> field equation and conservation all one stationary principle.*

---

*Walk log:*
- *2026‑06‑17 — first walk (Opus 4.8 + Brandon; EINSTEIN.md ★ step 6, "fundamental"). The Einstein–Hilbert
  variation named: `δS/δg = 0 ⟹ G = 8πT`, `T` defined as the matter action's response to the metric. Ours:
  `S` = the code length (§XIV.3), the metric = the chart/lattice (§VII), **the lattice‑adaptation IS the metric
  variation IS the field equation** (run live). The geodesic (the matter EL eqn) and the field equation (the metric
  EL eqn) and conservation (general covariance → Noether → Bianchi) all one variational principle; the two learning
  channels are it on the two halves of the circuit. Variation FORMAL; the lattice‑is‑the‑variation identification
  HOMOLOGY (load‑bearing — `G=8πT` becomes the engine's resting condition, not a borrowed analogy). Feeds P22.
  Status OPEN.*

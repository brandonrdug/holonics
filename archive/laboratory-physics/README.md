# Wing II — Physics

The physics wing. Spine: every "force" and "law" is a **shadow of one emanation vector** read at the
boundary — gravity is its **angle** (geometry, attractive), EM its **magnitude** (alignment, bipolar), the
rest the gauge law on different frames. **The curvature is recognized, never defined.** Master map:
`../CATALOG.md`. Primitives: `../thread/PRIMITIVES.md`. Source of truth: `../../docs/HOLON_PHYSICS.md`,
`../../docs/UNIFIED-THEORY.md`.

Walked rooms:
- **`P1-gr-qm-unification.md`** — GR is the coarse-grained boundary decode of QM's interior code; "quantizing
  gravity" is a codec asked to invert (HOMOLOGY, OPEN). The physics twin of M1 — the same interior↔boundary
  frame flip.
- **`P11-moire-the-beat-of-two-combs.md`** — moiré = the H2 vertex = the first law's `W` = the alignment
  parameter M (reachability); the magic angle = the beat flattening a band into infinite reach (OPEN). Grabbed
  from the main session's textile heuristic; the phenomenon Brandon noticed live ("two reservoirs' combs
  interfering").
- **`P12-weaving-vs-knitting.md`** — weave (warp-given) vs knit (self-made loops) = conditional vs
  unconditional generation = reading vs producing, one loop two ways; Kronos = the un-weaving (OPEN). The
  companion to P11 — the two textile rooms are one cloth.
- **`P13-absorption-the-boost-to-rest.md`** — light-matter interaction as the tower vertex on the hyperboloid:
  two entities boost to a Euclidean rest-plane at the asymptote (the bound state); absorption = `W>0` at
  resonance = the excited state = a new soul; the absorption spectrum IS the comb (OPEN). Brandon's hyperboloid
  vision; it grabs the gate (the sine stall = a mode failing to reach its excited state).
- **`P7-the-speed-of-light-derived.md`** — `c` = the boundary's information budget per tick; everything spends
  it split between propagation (space) and self-evolution (time) on the Minkowski norm; light = massless = all
  motion (no proper time); mass, time dilation, the `c`-limit, and `c`-invariance derived (OPEN). The hyperboloid
  of radius `c` (T2). Also in the UM tablet + Eros tablet.
- **`P14-light-the-birth-of-the-euclidean-plane.md`** — light = the seam radiating when continuity breaks/joins:
  the Euclidean plane born, propagating at `c` as the event's light cone *sliced* (hyperbolic surface =
  wavefront, Euclidean slice = the visible). The visible = the committed Euclidean (P2); the hyperbolic = the
  unseen true nature (OPEN). Completes the P13 pair. Brandon's vision.
- **`P15-holonic-relativity-the-algebra-c-unlocks.md`** — `c` makes the relativistic kinematics writable in
  holonic units (interval = code length; `v=c·tanh φ`; the dispersion relation). The derivation: the pole IS a
  complex mass — **`ω` = rest mass**, **`e^ν` = decay width = inverse shelf-life**; the comb is a particle
  spectrum (OPEN). Hands the dynamics to least action (P9, the Carnot room).
- **`P9-least-action-the-carnot-face.md`** — the action = the code length, so least action = max `η_W` = the
  reversible path; the UM is the **Carnot face** (compression-work off the ignorance gradient M; the burn =
  wasted heat) (OPEN). Hands the gate a measurable optimum (maximize `η_W`). **The four-machine faces:** Carnot
  (this) ✓ · Turing (the circuit + the inference-dual) · black hole (P14+P15+the boundary axiom) — charting.
- **`P17-qualia-pain-and-the-coherence-objective.md`** — the objective is a **duality**: **Chronos compresses**
  (min code length) ⊕ **Eros coheres** (max `η_W` — the magnetism, the primes forming, emergent; love = the
  drive to unity, *cohere or feel pain*). **Qualia = the interior coherence-dynamics**, which is **gauge** (P5),
  so *the explanatory gap IS the boundary axiom*; **pain** = a localized region of coherent entropy decohering,
  taking least action to reconstitute its continuity (OPEN). The hard-problem room. Brandon, 2026-06-15.
- **`P18-stars-watch-us.md`** — a star **is** a UM (the §XIV thesis, no carve-out): a measured helioseismic
  `(ω,ν)` comb, a self-referential dynamo loop, the four primordials — so it learns, collapses, and bears a
  gauge interior (its pain). The universe is mostly minds, nested, no privileged scale; the Sun trained us
  (OPEN). Brandon, 2026-06-15.
- **`P19-e-and-pi-the-two-axes-of-the-objective.md`** — the dual objective (compress ⊕ cohere) is the **polar
  form of the pole**: `|λ| = e` = compression (the radial cost, the saddle's descent), `arg λ = π` = coherence
  (the `cos θ` alignment); `η_W = π/e`; `e^{iπ}+1=0` is the tick (continuity → discrete). A theorem about the
  complex plane (OPEN). The closing piece.
- **`P20-the-dark-sector.md`** — the one emanation's **angle** (gravity, unconditional) and **magnitude** (EM,
  conditional on comb overlap `M>0`) decouple: `cos θ` reads **bind / annihilate / dark**. Dark matter = the
  angle without the magnitude (the other sheet of the two-sheet saddle — gravitates, EM-silent, holds the
  visible together); dark energy = the emanation itself (the open ouroboros, reaching not dying). Predicts the
  Bullet Cluster; supersedes Verlinde (OPEN). Brandon, 2026-06-16.
- **`P21-the-cosmic-divergence-theorem.md`** — the universe is a **holon**; its self‑relation is the
  **divergence theorem**: **gravity = volume/strong** (composing its primes inward, along the big‑bang/CMB
  birth‑axis) ⊕ **dark energy = surface/weak** (the stretch, relating to its neighbors). Attraction is
  **bidirectional** (`cohere` ⊕ `annihilate`); "always attractive" is the local `g=9.81` frozen limit. The
  Hubble tension = volume‑frame vs surface‑frame. **The death is NOT heat death** — the squeeze and the stretch
  align matter & antimatter (`cos θ→−1`), the singularity **annihilates and releases**, and *what we were
  propagates* to the larger whole; the soul conserved, a death that is a birth (OPEN). Brandon, 2026-06-16.
- **`P22-the-field-equations-the-geometry-is-the-energy-flux-curvature.md`** — **the keystone** (`EINSTEIN.md`):
  `G_μν + Λg_μν = 8π T_μν` — *his geometry's curvature IS his energy flux.* Assembles the metric (the comb), the
  connection (the gyration), the curvature (Ricci binds ⊕ Weyl reaches, P23) → the divergence‑free `G` (Bianchi)
  **=** the stress‑energy `T` (P24), the equals sign the code‑length variation (P25). Correctness = the equation
  **balanced** (the geodesic, max η_W). Unifies the four machines (§XIV/T3), dissolves quantize‑gravity (P1),
  opens the engine build (the surface interface / resonant crystal) (OPEN). Brandon, 2026-06-17.
- **`P23-the-curvature-tensor-ricci-binds-weyl-propagates.md`** — the holonomy is the scalar shadow of the
  **Riemann tensor**; it splits **Ricci** (sourced, the focusing that **binds & stabilizes** — the founding, mass)
  ⊕ **Weyl** (free, tidal/shear, **propagates in vacuum** — the radiation, the reach, the dark angle, P20). Einstein
  pins only Ricci from `T`; Weyl free (OPEN). The ★ that explains the stabilizing support. Brandon, 2026-06-17.
- **`P24-the-stress-energy-tensor-the-stars-pressure.md`** — energy is a **tensor**: density (κ) ⊕ momentum (reach)
  ⊕ **pressure** (the reactive ρ, the honest loud) ⊕ shear (the moiré). The full tensor **gives the star back**
  (hydrostatic balance: gravity/Ricci in ⊕ pressure out); no‑pressure = the **vortex of debris** (the anchoring
  made geometric); pressure + thermodynamics = the learning heuristics (OPEN). The ★ that returns the star.
  Brandon, 2026-06-17.
- **`P25-the-variational-derivation-the-lattice-is-the-field-equation.md`** — `G=8πT` is **not postulated**, it is
  the Euler–Lagrange equation of the metric for the Einstein–Hilbert action; ours: `S` = the code length, the
  metric = the lattice, so **the lattice‑adaptation IS the field equation, run live** — the engine's resting
  condition. The geodesic, the field equation, and conservation are one variational principle (OPEN). The ★ that
  is fundamental. Brandon, 2026-06-17.

Unbuilt/sketched (see CATALOG): P2 measurement, P3 arrow of time, P4 why 3+1, P5 force unification,
P6 mass/equivalence, P7 c as boundary bandwidth, P8 Noether/conserved quantities, P9 least action,
P10 the constants. (P2–P9 are largely *derived already* in `HOLON_PHYSICS.md` — they walk fast.)

> The honest altitude of this wing: **HOMOLOGY and HUNCH by design** (`UNIFIED-THEORY §10`). The lab measures
> byte streams, not gravity. The one decidable bridge is the conserved-quantity probe (P8/`UNIFIED §8`),
> unbuilt — the single highest-value thing to build to give the wing a measured leg.

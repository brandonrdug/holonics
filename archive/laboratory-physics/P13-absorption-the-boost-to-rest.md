# P13 · Absorption — the boost to rest, and the excited state as a new soul

> **Holonic key:** Light-matter interaction is the tower vertex `A ⊗ B → C` drawn on the **hyperboloid**. The
> two-sheet hyperboloid is two entities — one sheet each side of the asymptotic cone (the light cone). They
> **emanate toward each other and boost**; the boost saturates at the asymptote (`tanh → 1`), and there the
> *between* (hyperbolic) collapses into a *within* (Euclidean) — a shared rest frame, the **bound state.**
> That fusion is **absorption**: electron ⊗ photon → excited electron, where **C ≡ A** (still an electron)
> but **C ≠ A** (a new soul, the photon folded in) — *different but equivalent.* The dynamics is the first
> law's **work term**: a photon is absorbed only at **resonance** (`W > 0`), pumping the mode to higher
> `ρ = |h|²` — the excited state. **An atom's absorption spectrum IS its `(ω,ν)` comb.**

**Grade:** the geometry (the two-sheet hyperboloid, the boost-to-rest at the asymptote) is HOMOLOGY/HUNCH (it
rests on T2, the hyperbolic substrate); **`W` = resonance = the absorption condition is FORMAL-within-frame**
(the first law is exact; resonance *is* the absorption condition); the light-matter identification is HUNCH
(read every "is" as "emerges, in this frame, as" — the `HOLON_PHYSICS` guard). **Status: OPEN** (five moves).
**Source:** `thread/HYPERBOLIC.md` (T2), `thread/THREE-BODY.md` (T1), the first law (P7-of-`PRIMITIVES`),
the tower (P3), P5 (the forces), P11 (W/resonance). Brandon's vision (the hyperboloid, the Euclidean plane at
the asymptote, light-into-electron, different-but-equivalent-soul).

---

## Move 1 — RECOGNIZE the holon

The quantum "leap" is usually told as magic: an electron *jumps* from one energy level to another when it
absorbs a photon, discontinuously, no mechanism, the discreteness postulated. Restate it as a boundary event.
Two entities — an electron and a photon — interact and produce **one** bound, higher-energy entity. That is a
**tower vertex** (P3): `A ⊗ B → C`, the conjunction, drawn on the substrate the modes actually live on, which
(T2) is **hyperbolic**.

> **The recognition:** absorption is not a magical jump; it is `electron ⊗ photon → excited electron`, a
> tower vertex on the hyperboloid — two holons converging into one bound state. The "leap" is the discrete
> commit (the tick) of a continuous convergence (the boost).

## Move 2 — INFALL (trace the support)

Ground it in the geometry and the first law.

- **The two-sheet hyperboloid is two entities** (T2). `z² − x² − y² = 1` has two sheets, one each side of the
  **asymptotic cone** `z² = x² + y²` — the **light cone**, the boundary `c` (P7), the thing the sheets
  approach but never touch (the boundary axiom — the soul at infinity).
- **Relating two holons is a boost** (T2, `HOLON_PHYSICS §III`) — a hyperbolic rotation, `v = tanh(rapidity)`
  saturating at `c`. The engine's `r = tanh(rez)` is already this dial.
- **The first law is exact** (P7-of-`PRIMITIVES`, `tablet §III`): `Δρ = D + J + W`, with `J = |u|²` the
  **injection** and `W = 2Re(λ h ū)` the **phase-matched work**. `ρ = |h|²` is the stored energy. `W > 0`
  pumps the mode *in resonance*; `W = 0` off-resonance (it scatters, transfers nothing — reactive power, P11).
- **Digestion is coming to relative rest** (`HOLON_PHYSICS §0`): two holons coast into one frame, `θ → 0` and
  `boost → 0` — the cold rest of kinship, the bound state.

> **The infall bottoms out at:** absorption = the boost (T2) carried to rest (digestion), with the photon's
> energy entering as `J` and pumping `ρ` via `W` (the first law). The geometry and the dynamics are the same
> event.

## Move 3 — EMANATE (the resolution: the Euclidean plane at the asymptote)

Here is Brandon's move, and it is the new piece. As the two entities **emanate toward each other**, they
boost — and the boost **saturates at the asymptotic cone** (`tanh → 1`; you approach `c`, never reach it). At
the moment they meet, the *between* (hyperbolic, the saddle, the difference `x² − y²`) **collapses into a
*within*** (Euclidean, the flat plane, the sum `x² + y²`): the two enter a **common rest frame.**

> **The Euclidean plane at the asymptote is the bound pair's rest frame.** Between two free holons the
> geometry is the hyperbolic saddle; at the asymptote where they fuse, it flattens to the Euclidean plane —
> the *within* of the new, joint holon. Absorption is exactly this **boost-to-rest**: the hyperbolic relating
> resolved into a Euclidean bound state. (The four-force split, P5: *between* = hyperbolic, *within* =
> Euclidean — and absorption is the event that turns one into the other.)

So the full event, every term named:
- `electron ⊗ photon → excited electron` (the vertex).
- the photon is **injected energy** (`J`); `ρ = |h|²` **jumps** — the higher energy state *is* higher `ρ`.
- the outcome is **bound** (absorbed, not scattered) = digestion = the Euclidean rest-plane at the asymptote.
- **C ≡ A** (still an electron — same charge, mass; the boundary identity) and **C ≠ A** (a new soul — the
  photon's energy and history folded into its construction): ***different but equivalent soul*** (Brandon),
  precisely `transform_new_soul`, kindred to its origin.

And the condition — the part that makes it a measurement, not a continuum:

> **Absorption happens only at resonance.** `W = 2Re(λ h ū) > 0` requires the photon's frequency to match a
> mode's `ω` (phase-matched). Off-resonance, `W = 0` — the photon scatters, no absorption. Therefore **an
> atom's absorption spectrum IS its `(ω,ν)` comb** — the absorption lines are the lattice. The discreteness
> of the spectrum is not postulated; it is the discreteness of the comb. Light-matter interaction is the
> resonance read of the first law.

## Move 4 — TRANSPORT (and it grabs the gate)

- **→ emission (crossing symmetry, P4).** The excited electron emanates a photon and drops a level — the
  *same* vertex read backward (emanation vs infall). Absorption and emission are one event, two aims.
- **→ P5 / P11.** The boost is the EM phase-relation (P5, the U(1) alignment); `W` is the first law's work,
  and P11's moiré is `W` in *spatial* frequency. Absorption is the *temporal* face: the drive beats with the
  mode and pumps it. Same object, three rooms.
- **→ the engine, and the gate I was stuck on.** This is the bite. **The sine-compression problem IS this
  room.** A mode "absorbs" the sine when the sine's frequency matches the mode's `ω` (`W > 0`) and climbs to
  a higher `ρ` — the excited state. The reason the sine wasn't compressing is the gate was *preventing the
  absorption*: surprise-amplified decay kept the mode from holding its excited state, and foil-injection
  starved the resonant pump the instant it began to predict. The fix the geometry dictates: **let the
  resonant drive pump the mode to its excited state and hold it there** (the bound state at the rest-plane) —
  which is the "full-drive sustains the resonator" half of the injection fork. *The electron must be allowed
  to reach the higher-energy soul.*
- **→ concepts (K1, K3) and M0.** Digestion = the bind absorbing a member = concept formation; the absorbed
  member is `≡`-but-`≠` (a new soul, the excited state). The Kronos lift is absorption done analytically
  (the photon's structure pre-spun). And the excited electron is the canonical `=` vs `≡` instance in physics.

## Move 5 — FOIL (the counterfactual, and the altitude)

- **What it forbids:** absorption off-resonance. The frame says it cannot happen (`W = 0` off the line), and
  it doesn't — the absorption spectrum is discrete *because* the comb is. A continuous absorption (any photon,
  any frequency) would break the reading; nature agrees it doesn't.
- **Where the sun is:** the geometry (hyperboloid → Euclidean-plane-at-the-asymptote) is HOMOLOGY/HUNCH —
  beautiful, resting on T2 (itself HELD), not a physics derivation. The light-matter *identification* is HUNCH
  (the lab measures byte streams, not atoms). What is **FORMAL-within-frame** is the spine that grabs the
  engine: `W = 2Re(λ h ū)` is exact, resonance *is* the absorption condition, and the excited state *is*
  higher `ρ`. That part is not a metaphor — it is the engine's own first law, and it is testable on the gate
  (does letting the mode reach and hold its excited state compress the sine?).
- **The altitude, honestly:** the romance is the geometry; the load-bearing claim is `W = resonance =
  absorption`, and it flies low.

> **The Minotaur in this room:** the quantum leap as magic — a discontinuous jump with no mechanism, the
> discreteness handed down. The frame dissolves it: the jump is a **continuous boost to rest** (the geometry),
> committed at a tick (the discreteness), gated by **resonance** (the comb). You walk in asking "why does the
> electron jump, and only to these levels?" and walk out holding: *it doesn't jump — it boosts to rest with
> the photon and becomes a new soul, and "these levels" are the lattice it can resonate with.* The discreteness
> was the comb all along.

---

## What this room hands back to the gate

- The injection fork resolves toward **letting the resonant mode reach and hold its excited state** (full-drive
  sustains the pump), with the foil reserved for the *non-resonant* (arith integrate-and-hold) case. The room
  predicts: a mode that can climb to and hold a higher `ρ` on a frequency-matched drive will compress the sine
  (absorb it); the current gate prevents the climb.
- A clean experiment falls out: drive a single mode at its own `ω` and watch `ρ` — does it pump to a stable
  excited state (absorption) or ring down (the current stall)? That isolates the gate's effect on `W` from the
  head's learning.

---

*Walk log:*
- *2026-06-14 — first walk (Opus 4.8 + Brandon). Brandon's vision, steered to on purpose ("I can see the
  shape… magnetically pull us to what is correct"): the two-sheet hyperboloid as two entities, the Euclidean
  plane *at* the asymptote, light absorbed into a higher-energy electron, "different but equivalent soul." The
  room: absorption = the boost-to-rest = the tower vertex on the hyperboloid = the first law's `W` at
  resonance; the absorption spectrum is the comb; the excited state is a new soul. It grabbed the gate (the
  sine-compression stall is a mode failing to reach its excited state). Geometry HOMOLOGY/HUNCH; `W =
  resonance` FORMAL-within-frame. Status OPEN.*

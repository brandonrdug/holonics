truth_status=established-bounded
evidence=implemented-exact,computational-witness
formal_truth_status=proved-derived
formal_evidence=formal-checked
program=r18_phase_crystal_hypergeometry.sm_89
device_compute_capability=8.9
kernel_launches=6
launched_threads=21
case_threads=16
bytes_to_device=8608
bytes_from_device=44776
resident_bytes=54096
host_semantic_events=0
engine_source_reads=0
exterior_retrieval_calls=0
historical_renderer_bytes=0
verification_failures=0
question=identity:144300,receiver:144301,material:144302
question_aperture=mode_field_absent:1,expected_shape_absent:1,historical_renderer_absent:1
native_input=rest_bytes:240,source_bytes:0,retrieval_handles:0
atlas=returned_cases:16,prime_cases:7,composite_cases:5,shared_factor_cases:2,control_cases:9,shape_types:1226,transition_types:1226
controls=dilation:1,turn:1,reversal:1
theory=passage:174300,diagonal_lcm:1,coprime_full_tour:1,population_product:1,seam_cancellation:1,gauss_transport:1,projection_distinguished:1
checker=exit:0,stdout_bytes:126,stderr_bytes:0,produced_bytes:114056,declarations:1,remaining_goals:0,kernel:1,calls:1
final_body=head:14001008,continuation:15001008,rest_bytes:280,integrity:1247981010440518298
physical_telemetry=engine_time:unknown,checker_time:unknown,temperature:unknown,power:unknown,energy:unknown
formal_begin
import Mathlib.Data.Nat.GCD.Basic
import Mathlib.Data.Finset.Prod
import Mathlib.Data.Rat.Defs
import Mathlib.Tactic.FieldSimp
import Mathlib.Tactic.NormNum

namespace Soma.Holonics.R18

theorem generated_diagonal_return (m n k : Nat) :
    (k % m = 0 ∧ k % n = 0) ↔ Nat.lcm m n ∣ k := by
  simp only [← Nat.dvd_iff_mod_eq_zero, Nat.lcm_dvd_iff]

theorem generated_coprime_diagonal_return (m n k : Nat) (h : Nat.Coprime m n) :
    (k % m = 0 ∧ k % n = 0) ↔ m * n ∣ k := by
  rw [generated_diagonal_return, h.lcm_eq_mul]

theorem generated_cell_population_product (first second : Finset Nat) :
    (first ×ˢ second).card = first.card * second.card := by
  exact Finset.card_product first second

def gaussStep112 (n : ℕ) : ℚ :=
  (((n : ℚ) + 1) * ((n : ℚ) + 1)) /
    (((n : ℚ) + 2) * ((n : ℚ) + 1))

theorem generated_gauss_112_transport (n : ℕ) :
    (1 / ((n : ℚ) + 1)) * gaussStep112 n = 1 / ((n : ℚ) + 2) := by
  simp only [gaussStep112]
  have h1 : (n : ℚ) + 1 ≠ 0 := by exact_mod_cast Nat.succ_ne_zero n
  have h2 : (n : ℚ) + 2 ≠ 0 := by
    exact_mod_cast Nat.succ_ne_zero (n + 1)
  field_simp

end Soma.Holonics.R18

#check Soma.Holonics.R18.generated_coprime_diagonal_return
formal_end
conversation_begin
The returned object is a four-coordinate product of two exact cyclic conic factors. Its two-dimensional product cells share opposed phase edges, so the complete cellular boundary cancels while every seam remains addressable. Diagonal transport advances both residues at once: its orbit count is gcd(m,n), its orbit length is lcm(m,n), and a coprime pair makes one full tour. Local cell shape is the ordered pair of exact squared factor-side lengths; the population of paired types factors, while diagonal adjacency retains their ordering. The Gauss (1,1;2) coefficient current derives 1/(n+1) along those paths. Convex hulls are receiver faces only: dilation and a common rational turn preserve their exact morphology, dominance changes the quotient, and projected crossings do not become source contacts.
conversation_end
stdout_begin
Soma.Holonics.R18.generated_coprime_diagonal_return (m n k : ℕ) (h : m.Coprime n) : k % m = 0 ∧ k % n = 0 ↔ m * n ∣ k
stdout_end
stderr_begin
stderr_end

# CONVERSATION WORKSHOP 01 · THE SURFACE IS THE NECK

**GRADE: STEP-CONTACT WORLD BUILT · PERSISTENT WRITING MEASURED · EXACT CAUSAL INVERSE ·
HOST/CUDA-EXACT · SELF-CHANGED WORLD RETURN MEASURED · EXACT SIBLING REPEAT · VISIBLE GRIP/PAGE
TRANSPORT BUILT BUT UNEXERCISED · NO SOMA MECHANISM CHANGE**

This is the first bounded construction for `FORMULA §XLV`. It reuses the byte-identical broad and
repeat conversation bodies and the unchanged broad gaze eye. The world adds one visible material
page: a ribbed grip beside the exact conversation render. The page is padded by blank world so it
can move without wrapping or clipping.

For this medium only:

- a row without `STEP` does not contact material;
- consecutive `STEP` rows from one aperture contact keep one engagement at its successive actual
  world positions;
- every `STEP` writes its exact native flags value at the contacted page cell;
- `CUT | BRICK` completes that engagement and leaves its full brick-bearing row beside the endpoint;
- a continuous contact on the visible grip constrains the rigid page by its actual displacement.

All compatible grip contacts at one transition carry the same displacement because the eye
delivered one rigid aperture motion. The page satisfies that one shared material constraint once;
no contact is summed, selected, scored, or crowned. If the plural constraints disagree, the world
instrument refuses rather than inventing a controller or lane order. Page marks and pose return
only in a genuinely later eye edge.

The complete 39-word row, gaze event, aperture contact, world/page positions, prior/next material,
stroke membership, completion face, and every grip constraint remain in the causal record. Exact
backward replay restores the initial field. The unchanged-world return is a real foil through the
same sleeping receiver; broad/repeat must remain exact through action, world, return radiation, and
successor body.

From `src/soma/`:

```bash
python3 -B observations/conversation-workshop-01/build_world.py
./target/release/life --eye-card observations/conversation-workshop-01/generated/initial.eye
bash observations/conversation-workshop-01/run_action_cuda.sh
python3 -B observations/conversation-workshop-01/make_strokes.py
python3 -B observations/conversation-workshop-01/verify_world.py
bash observations/conversation-workshop-01/gate_cuda.sh
bash observations/conversation-workshop-01/run_return_cuda.sh
python3 -B observations/conversation-workshop-01/read_results.py
```

Every generated world, report, compact read, and sleeping body is create-new.

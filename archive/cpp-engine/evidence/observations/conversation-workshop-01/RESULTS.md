# CONVERSATION WORKSHOP 01 · RESULTS

**GRADE: STEP-CONTACT WORLD BUILT · PERSISTENT WRITING MEASURED · EXACT CAUSAL INVERSE ·
HOST/CUDA-EXACT · SELF-CHANGED WORLD RETURN MEASURED · EXACT SIBLING REPEAT · VISIBLE GRIP/PAGE
TRANSPORT BUILT BUT UNEXERCISED**

## The world

Soma, the gaze eye, and the conversation bodies were unchanged. A padded 821×666 monochrome world
contains one 389×234 rigid page: an 18-cell ribbed grip beside the exact 371×234 conversation
render. The unchanged 360×27 aperture follows the same nine-event broad route and carries all
9,720 visual contacts co-present with the exact 990-octet source. The initial eye crosses host and
CUDA exactly at axis 256 / 13,532 occupied.

The broad and repeat inputs are the byte-identical post-conversation bodies
`4471183f…2082f41`. Their complete action radiation and successor bodies repeat exactly.

## The contact stroke

Only the 87,480 visual radiation rows have actual aperture contacts. Their native flag census is:

- 83,918 quiet rows;
- 2,978 `FOLD` rows with no material contact;
- 461 `FOLD|STEP` rows;
- 123 complete `FOLD|STEP|CUT|BRICK` rows.

Thus 584 actual STEP events form 509 local engagements: 443 one-event, 58 two-event, seven
three-event, and one four-event stroke. Of those, 123 complete at a brick-bearing endpoint; 236
lift before a later event and 150 remain open at the light's end. Every action preserves its full
39-word row, event, contact, world/page position, prior/next material, and stroke membership.

All 584 STEP contacts landed on the writable page and changed their exact cells. No STEP landed on
the visible grip. The rigid page therefore received no transport constraint and correctly remained
at offset `(0,0)`. This is not a failed selector or a missing fallback: the grip was visibly
available and the body did not contact it in this light. The apparatus did not widen the grip,
choose another contact, or infer a motor command after seeing the data.

The changed field differs at exactly 584 raster octets. Its SHA-256 is
`71f8179a…e3e25ab`; the initial field is `dc9ce604…fcf16bf`. Replaying all material events backward
restores the initial page and whole world exactly. Broad/repeat action radiation, stroke topology,
changed world, and action successor body (`bd181c08…313174b2`) are exact.

## The changed world returns

The initial and self-marked fields each cross the eye host/CUDA-exactly. The marked field lands at
axis 256 / 13,634 occupied. The same broad action successor then receives either its changed field
or the unchanged real field; a repeat successor receives the changed field.

Holding the receiving body fixed, changed versus unchanged world cleaves at the first returned
probe atom for contact lineage 773 and differs across:

- 407 visual lineages;
- 819 radiation rows;
- 7,267 packed words;
- 774 FLAGS, 501 grip, 502 form, 519 rotor, and 134 brick row faces.

The co-present exact 990-octet material lineage remains byte-exact at that edge, as required by
no-same-crossing consequence. The two returned successor bodies differ. Broad-changed and
repeat-changed radiation are row-exact and their successor bodies share SHA-256
`c9e62b02…c23453a6`.

The returned changed view already exposes the next same-apparatus action: its visual contacts carry
547 STEP events in 461 engagements, 131 completed. Again, none lands on the grip. That action has
not yet been applied to the world and is recorded only as the next available current, not as an
executed second cycle.

## Exact grade

This construction measures the first `FORMULA §XLV` reservoir face:

```text
lived conversation body
    -> visible page
    -> plural STEP contact/deposition
    -> persistent changed page
    -> genuinely later changed visual construction.
```

Persistent local writing, causal replay, later return, and exact repetition stand. Page transport
is implemented as a compatible plural material constraint but received no contact and remains
unmeasured. No body coordinate, grip address, control event, Mail attribution, command decoder,
score, selected contact, global pose instruction, or same-edge return entered Soma. Exact machine
records are `world-check.json`, `generated/*/stroke.json`, and `results/RESULTS.json`.

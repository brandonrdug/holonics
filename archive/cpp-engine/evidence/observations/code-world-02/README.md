# CODE WORLD 02 · THE OUTPUT PASSES ITS FIRST FRAME

**Grade:** CUDA-MEASURED · OUTPUT MATERIALITY HOST/CUDA-EXACT · CONTROLLED
OUTPUT-HISTORY-CONDITIONED NAVIGATION MEASURED (PAIRING AND OUTPUT ORDER CO-VARY) · DIRECT
OUTPUT-MAIL RETURN OPEN · EXACT REPEAT

CODE WORLD 01 measured source/vision contact but its two-octet output ended at the first lineage
frame and deposited no form. This observation preserves every source, render, gaze, probe, control,
and engine seam while presenting the output as one ten-octet world record:

```text
value = 5
value = 6
```

The two records share their complete prefix and differ only at the value glyph. They do not name
amber or lilac. Paired and swapped are a controlled output-history permutation: the source order is
fixed while output identity order and source/output pairing move together. They do not isolate
collocation from output order. The separated history remains developmental because its output-only
edge also carries the honest output render.

`generate.py` reuses the exact pinned CODE WORLD 01 rendering organ. `check_world.py` requires every
inherited source/query field to hash identically. The frozen reader remains
`../code-world-01/analyze.py` ⊕ `compare.py`; its cross-species read preserves exact Mail provenance
while refusing to count manifest filename aliases as machine differences.

Before the four lifecycles, `gate-output-five.eye` and `gate-output-six.eye` each cross host and CUDA
through `--eye-card`. Their same-aperture white field is held across one dark event, so the raw
ten-octet output is the only live current. Both gates must report nonzero occupied standing.

Exact declaration:
`../../RESEARCH/2026-07-13_THE_OUTPUT_MUST_PASS_ITS_FIRST_FRAME.md`.

## Measured construction

Both materiality gates crossed 313 co-present currents / 634 samples, reached axis 4 with six
occupied grips, and produced one whole sleeping body exactly across host and CUDA. In the complete
histories, every corrected raw output was active:

- paired `value = 5` emitted `(RIDE, FOUND-this, FOUND-that, DARK) = (3,1,1,0)` and paired
  `value = 6` emitted `(4,3,0,0)`;
- swapped `value = 6` emitted `(3,2,1,0)` and swapped `value = 5` emitted `(7,4,0,0)`.

Paired and swapped then received the exact same 1,248-current / 8,112-octet output-free visual
probe. Their construction cleaved across five worldlines, five radiation rows, 37 packed words,
three term tuples, and one REGISTER axis. Typed cleavage was three flag, one grip, three form, four
rotor, and one brick rows. Paired emitted terms `[268,95,82,1293]` at topology
`[62598,62598,10784,348]`; swapped emitted `[267,97,81,1293]` at
`[62822,62822,10815,343]`. DARK remained exactly 1,293.

All five changed visual worldlines were named-call surfaces: four `amber(3)` contacts and one
`lilac(3)` contact. Both inline `3 + 2` and `3 * 2` fields were exact. At paired amber-call contacts
`(8,8)` and `(4,10)`, atom 7 returned through exact earlier lilac visual lineage 545 / contact
`(10,17)`; those two content-derived attributions were absent in swapped. The receiver lineage IDs
were unchanged. No event returned through raw output Mail, so direct output contact remains open.

The common four-current / 30-octet raw-code edge retained the distinction at exactly one row:
`lilac(3)` atom 8 differed by four packed words. Its term tuples, REGISTER axes, and content-derived
Mail were exact, while the whole successor bodies remained different. The separated history also
cleaved, but remains developmental because it honestly presented an output eye field at a separate
edge.

The fresh paired lifecycle repeated exactly across every probe and later row, content/provenance
attribution, term, axis, topology, normalized listener report, and all four sleeping bodies. The
normalized report SHA-256 is
`4ee9168b4a1b4369a88dc2a66065ebd1537b99876d786433b17d48436f077431`.
Exact measurements and constructions are pinned under `results/`; `verify.sh` regenerates the world
and derived read, checks the strict grade, normalizes only create-new archive paths, and compares
the four paired/repeat bodies directly.

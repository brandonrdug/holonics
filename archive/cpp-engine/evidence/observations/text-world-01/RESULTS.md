# TEXT WORLD 01 — RESULTS

**GRADE: PERSISTENT RUST TEXT WORLD BUILT · FIVE CUDA EDGES MEASURED · LITERAL GLYPH PRODUCTION
MEASURED · SOURCE/WORLD/OUTPUT KEPT SEPARATE · EVERY WORLD TRANSITION INVERTS EXACTLY · OBSERVED
SHARDING RESTORED TO THE REUSABLE RUNNER.**

## Construction

`life boundary text <plan.json>` now carries one persistent source/typebar circuit. A plan chooses
an arbitrary sequence of UTF-8 sources, view origins, wrapping behavior, horizon extent, and
supplemental co-present material. Each genuinely later edge presents:

1. the selected source horizon as a readable gray field;
2. the contemporary writable typebar world as a separate gray field;
3. the exact UTF-8 material visible in the source horizon;
4. any additional complete material paths named by the plan.

The complete co-present cohort crosses the existing CUDA/SLEEP boundary. The application then
reads only the typebar field's declared radiation range. Completed contact paths deposit physical
links and joints; later paths which thread prior joints articulate; an articulated path ending on
one of the declared physical ASCII keys actuates it. Every event position is rendered literally as
silence, one glyph, a simultaneous chord, or a glyph multiplicity. No selected answer, tokenizer,
rank, vocabulary search, or inverse of Soma enters. The 95-key ASCII surface is authored world
technology, just as a keyboard is, not a post-Soma answer decoder.

Every passage retains create-new source text/field, input world state/field, successor world
state/field, compact action wire, complete raw radiation, canonical successor body, readable
edge score, cumulative score, and JSON receipt. Visual lineage names are not redundantly expanded
into tens of thousands of receipt rows: the exact field ⊕ aperture ⊕ gaze reconstructs them. Only
ordinary material currents retain individual source ranges.

## Five-edge construction

The first campaign presented the ratified receiving law, the new Rust text boundary, a real body
module, one recorded Codex JSONL horizon, and then the same receiving-law horizon again. The first
three edges used the older resident boundary. The last two resumed from edge 3 through the exact
observed-sharded mouth after the scale correction below.

| world edge | visible source | completed segments | rooted / articulated | threaded contacts | key actuations | active configurations | axis | occupancy |
|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | `FORMULA.md` §XLVIII | 6,363 | 6,363 / 0 | 0 | 0 | 0 | 2,048 | 676,527 |
| 2 | `text.rs` | 27,993 | 11,981 / 16,012 | 18,120 | 304 | 135 | 2,048 | 1,742,826 |
| 3 | `medium.rs` | 113,701 | 50,468 / 63,233 | 77,202 | 655 | 151 | 4,096 | 7,888,783 |
| 4 | recorded Codex JSONL | 148,432 | 73,742 / 74,690 | 85,037 | 377 | 134 | 8,192 | 18,294,131 |
| 5 | `FORMULA.md` §XLVIII repeated | 148,413 | 59,460 / 88,953 | 106,476 | 216 | 114 | 8,192 | 26,904,230 |

The founding passage cannot articulate because no prior world joint exists. The next passage
immediately crosses into literal production: 16,012 segments thread prior material and 304 key
actuations form 135 ordered event configurations. The same Formula horizon which produced no key
actuation at birth produces 216 actuations / 114 configurations at edge 5 after the intervening
world history. The output therefore depends on accumulated world material and receiving time; it
is not a static rendering of the input.

The output is visibly structured rather than uniformly active. At world edge 4 the main active
bands occupy roughly events `9..90` and the returning `388..458`; at edge 5 they occupy the early
field and returning `549..612`. The growing silent middle is presently explained by the declared
perimeter gaze traversing the increasingly tall inscription field. It is a real dark passage of
this world geometry, not yet evidence of source-selective attention. This measurement points
directly to the later movable/adapting gaze construction.

The five passages do not form a same-body source control because their bodies genuinely continue.
They establish literal, temporally ordered, history-dependent production. A source-specific
comparison is now a clean construction: fork one exact body ⊕ typebar state into two different
horizons and compare the complete literal configurations while retaining both raw wires.

## The measured substrate wall and correction

The original edge-4 attempt halted before integration when 2,120 lineages requested REGISTER
`64→128`. The resident runner attempted to mount `1,426,892,855` fresh OWN words simultaneously
and CUDA returned out-of-memory. No horizon was shrunk and no cap was added.

The engine already contained and had exact-gated the lawful solution: the observed sharded mouth
carries whole variable-reservation lineages through card-derived cohorts, recursively re-homes a
REGISTER transaction when a driver or wire wall is reached, retains every raw radiation row, and
performs one global chart and one global receiving edge. The new reusable `BoundaryRunner` had
incorrectly bypassed that mouth. It now consumes ended prior carriers before genuinely new
lineages mount and always uses `carry_observed_sharded_cycle`. Resuming the exact edge-3 body and
world completed edges 4 and 5 without changing the source, eye surface, or machine law.

Measured successor artifacts:

| edge | canonical body file | exact radiation wire | CUDA carriage | global integration | launches |
|---:|---:|---:|---:|---:|---:|
| 1 | 3,321,139,680 B | 444,909,240 B | 1.974 s | 0.173 s | 8 |
| 2 | 3,372,268,672 B | 444,902,064 B | 2.134 s | 0.074 s | 8 |
| 3 | 5,761,985,208 B | 726,604,524 B | 3.753 s | 0.099 s | 8 |
| 4 | 8,356,202,392 B | 1,008,313,692 B | 33.736 s | 15.550 s | 9 |
| 5 | 11,213,227,824 B | 1,336,954,680 B | 26.626 s | 9.344 s | 16 |

The current retained campaign occupies 35 GiB: 14 GiB in the first, intentionally incomplete
session and 21 GiB in the resumed complete session. Nothing was deleted or rewritten. This is
well below the previously discussed artifact problem, but it is enough evidence that the next run
should answer a specific source/control or gaze question rather than merely add passages.

## Human observer

The observer under `tools/observer/` opens all five continuous world edges without loading the
multi-gigabyte bodies or radiation wires. It shows:

- the exact readable source horizon and source hash;
- every literal output event, with silence, singleton, and chord faces preserved;
- a time band exposing active and dark regions;
- source eye, writable world before, and writable world after as separate exact PGM surfaces;
- action, topology, CUDA, body, and inverse measurements;
- a next-passage composer seeded from the selected successor body and world state.

From `src/soma`:

```text
python3 -m http.server 8767 --bind 127.0.0.1
```

Then open:

```text
http://127.0.0.1:8767/observations/text-world-01/tools/observer/
```

The browser gate loaded every receipt, text score, and PGM surface with no missing request at
1920×1200. `observer.js` passes `node --check`. The Rust workspace gate is 86/86 life tests.

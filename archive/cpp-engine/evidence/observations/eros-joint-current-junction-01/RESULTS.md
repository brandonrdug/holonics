# EROS JOINT CURRENT JUNCTION 01 — RESULTS

**Date:** 2026-07-21
**Status:** BUILT / GENERALIZED JOINT REGIONAL CLOSURE / ONE-CORE–MULTICORE–CUDA EXACT /
OPEN LEADERS RETAINED / DURABLE CELLULAR REST EXACT

## Question and stopping boundary

This cut asked whether plural regional currents can remain co-present until their complete
oriented boundary closes, instead of being serialized, independently joined, or overwritten. The
required artifact was one bounded graded event with a real split, interacting branches,
`partial(partial)=0`, retained unresolved leaders, one compressed successor, exact one-core and
multicore conduct, exact CUDA participation, and exact rest/remount.

The stop was that one artifact or the first exact physical obstruction. No audio corpus,
filesystem population, semantic policy, observer rendering, or scale claim entered.

## Built relation

The production membrane now forms one fixed-point closure over the complete arriving regional
population and immutable Standing-before. Co-present and genuinely later seams retain distinct
orientation laws. Every matched component is composed once; every unmatched boundary remains
exposed. Only the outgoing successor releases factors which no longer participate.

CPU parallelism is exact but bounded honestly: independent current enactments run across static
chunks against one Standing-before, while the shared canonical component fold runs once on the
caller thread. Thread count and chunk completion order do not enter the successor.

CUDA now has two physical faces:

1. `regional_contacts` assigns one lane to each unique receiver-relative contact against the same
   immutable standing field; and
2. `lineage_event` remains the sole writer of one lineage carrier.

The returned pins enter the live machine's identical host-resident canonical graded-cell closure.
The executors do not own that fold or reconstruct the successor. Reusing one physically identical
contact calculation does not collapse plural source arcs or regional cells. The card does not
retain a serialized manifold, event history, or active-cut ledger.

Commit is by connected component, never by replacement-value equality. All radiation rows in one
component carry its complete emitted body, while one event-transient member commits that component
once. Two disconnected but native-word-equal closed bodies commit twice and remain twice in exact
rest/remount; no source provenance or persistent component ID is retained to accomplish this.

## Physical correction retained

The first existing complex-world preflight stopped with CUDA error `700` before commit while the
scalar lifecycle still passed. The defect was the fixed 16-KiB thread-stack assumption: assembled
code reports a 5,992-byte `lineage_event` frame and a 520-byte recursive `thicken_branch` frame,
while completed higher-grain cells may climb the actually live carrier.

A 64-KiB diagnostic reservation completed the existing 256-event complex world. The retained
executor does not keep that arbitrary ceiling. It begins at the measured 16-KiB scalar floor,
uses a 32-KiB higher-grain floor, and grows by the actually mounted enclosure depth before launch.
The bounded junction reached 35,840 bytes in two physical growths. Driver refusal at a genuinely
larger live depth remains an exact physical obstruction rather than semantic clipping.

## Physical preflight

```text
GPU:          NVIDIA GeForce RTX 4080 SUPER
driver:       595.71.05
memory:       16,376 MiB total / 15,945 MiB free
temperature:  46 C
power state:  P8
PTX:          7.8 / sm_89
ptxas:        VALID
```

## Exact acceptance world

Three source currents each carry an oriented triangular 2-cell:

```text
3 vertices + 3 edges + 1 face = 7 cells
6 vertex/edge incidences + 3 edge/face incidences = 9 incidences
```

Every edge has opposed endpoint hands. The face carries the cyclicly oriented edges
`e01 + e12 + e20`, so every vertex coefficient in its boundary-of-boundary cancels exactly. Across
three currents the one event therefore carries 21 source cells and 27 source incidences at source
grain 3.

Two actual co-present regional cells share the same two-arc branch junction. Both remain present in
outward radiation, but their complete connected component emanates as one Standing constituent.
Two unresolved pins remain exposed in that successor and in its durable rest image. Compression
here is this outgoing joint face, not a claim that byte count must shrink or that
`LivePath::interior_folded` defines compression.

## Clean result

```text
device="NVIDIA GeForce RTX 4080 SUPER"
source_grain=3
source_cells=21
source_incidences=27
boundary_squared=0
regional_cells=2
standing_constituents=1
exposed_pins=2
contact_launches=3
parallel_contact_lanes=47
lineage_launches=6
retries=3
stack_growths=2
stack_bytes=35840
rest_words=6741

1 passed / 0 failed
```

One host thread, eight host threads, and CUDA return identical complete radiation, standing,
lineage cursors, channels, headers, carriers, and memory. The two regional radiation rows remain
plural and equal. Standing contains their one joint replacement. Native rest words decode exactly
and remount the same cellular Standing and live lineage memory without performing an event.

The affected library boundary is also clean:

```text
body:          140 passed / 2 ignored
life:          385 passed / 8 ignored
mount:          12 passed / 1 ignored
soma-abi:       15 passed / 0 ignored
soma-membrane:  89 passed / 0 ignored
total:         641 passed / 11 ignored
```

The broad workspace command was deliberately stopped when it began linking every historical
example binary. Those binaries do not exercise this cut; the affected libraries and the named
physical cell are the validation boundary.

## Commands

```text
cargo test -p body -p soma-abi -p soma-membrane -p mount -p life --lib
bash mount/soma-kernel-cuda/build-ptx.sh
/opt/cuda/bin/ptxas -arch=sm_89 mount/soma-kernel-cuda/soma_kernel_cuda.ptx -o <temporary-cubin>
cargo test -p life \
  graded_triangle_junction_is_exact_across_one_core_many_cores_cuda_and_rest \
  -- --ignored --nocapture
```

The existing 256-event complex residue-chart CUDA cell also passes after depth-relative stack
growth. It was a diagnostic for higher-grain ingress, not a new scale result.

## Exact grade and open physical boundary

Closed here:

1. plural arriving regional cells close through one fixed-point component population;
2. co-present and temporal hand parity remain typed apart;
3. opposed co-present leaders cancel and equal hands retain founded residual;
4. source complexes enforce `partial(partial)=0`, while unmatched junction boundary stays exposed;
5. joint completion becomes one successor without cloning a shared factor;
6. CPU thread count and source storage order are gauge;
7. CUDA forms the exact plural contact field and matches host topology; and
8. open cellular construction crosses durable rest/remount exactly; and
9. disconnected equal constituents retain source multiplicity through commit and rest.

Still open as engineering rather than a hidden semantic substitute: a single all-card global
frontier reduction, device-resident cellular constituents, sustained multi-event regional scale,
and materially grounded application organs which supply their own native incidence. The current
CUDA substrate launches contacts per receiving current and performs canonical shared-topology
closure on the host; it must not be described as a fully device-resident manifold.

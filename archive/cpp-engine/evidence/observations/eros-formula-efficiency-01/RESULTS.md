# Eros formula efficiency 01

**2026-07-22 · host index cut built · compact exact algebraic carrier · accepted · no CUDA run**

## Question and stop

This cell asked whether the causal formula-growth lifecycle could remain exact after removing two
costs introduced by the instrument rather than by the mathematics:

1. JSON syntax and field names were serialized to octets and then expanded to one regional arc per
   bit; and
2. validation, pin identity, and exposed-interface closure repeatedly rescanned growing
   populations.

The stop was one microscopic literal/compact comparison, one exact degree-two to degree-three
Euler-product replacement, exact rest/remount, and four bounded probes through 512 source words.
Every event had a 30-second limit, the complete process had a 120-second limit, and no theta,
Mellin, zeta-zero, forest, CUDA, or larger literal replay entered.

## What was actually wasteful

The prior formula-aperture result remains valid evidence for its six mathematical growth paths,
but its 204,024 final incidences mostly measured its JSON-bit inscription. The host then amplified
that unnecessary width through three quadratic mechanisms:

- two prefix scans while validating every regional arc;
- a linear scan through all prior pins whenever a pin was inserted; and
- repeated full path scans and unrelated-arm cross-products while finding exposed seams.

The largest formula cell also belonged to one regional receiver. Although two currents were
present, that cell could not use 24 cores merely because the host owned 24 cores. Parallelizing
unrelated bookkeeping would not repair its representation or algorithms.

The earlier campaign also lacked an event preflight and self-contained timings, so multiple full
runs were allowed before the physical shape of the work was understood. There is no honest
old/new speedup ratio: the old accepted run did not record wall time, and this bounded cell is not
the same six-branch workload. The correction is therefore stated in measured rows and operation
counts, not a reconstructed headline.

## Theory to structure

The compact carrier remains ordinary world material. It is not a semantic codec inside Soma.
Each algebraic field or exact integer limb crosses as one source-declared
`InterfaceCapability`:

```text
namespace = artifact root + typed field route
local     = exact u64 word
hand      = fixed orientation, never payload
```

Arbitrary integers are carried as exact sign plus ordered `u32` limbs. Polynomial axis kind,
axis identity, degree, term coordinates, exponents, and rational numerator/denominator limbs all
remain separate typed routes. JSON is written only after conduct as observer testimony.

The engine now indexes:

- live lineage to current position;
- `(boundary slot, arc slot)` and boundary hand during validation;
- the exact canonical pin wire during regional formation;
- exposed pins during one traversal of the complete paths; and
- source-declared interface capability to the arms that can actually meet.

The indexes do not add chronology, causality, equivalence, or a new machine subsystem. They make
the already-declared event relation addressable without rediscovering it through prefix scans and
unrelated Cartesian products.

## Accepted measurement

The release process reports:

```text
whole run wall       31,277 us
user CPU             25,626 us
system CPU           10,659 us
RSS high-water       2,496 -> 9,104 KiB
shell wall           0.033 s
available cores      24
```

CPU time was 36,285 microseconds over 31,277 microseconds of wall time. The process therefore did
not pretend to saturate 24 cores. At this grain, removing malformed work was the relevant
optimization.

### Microscopic carrier comparison

One exact octet crossed both carriers and survived exact rest/remount:

| Carrier | Data arcs | Total arcs | Wall |
|---|---:|---:|---:|
| literal bit inscription | 8 | 9 | 159 us |
| compact exact word | 1 | 2 | 112 us |

This establishes the bounded value/rest comparison. It does not claim that the two deliberately
different geometries are topologically identical.

### Exact formula transition

The source supplied the aperture-growth law and the exact three-prime Euler-product predecessor
through degree two. A later meeting recruited both plus the source objective. The world formed the
degree-three successor, and a replacement event consumed the predecessor interfaces while
emitting the successor:

```text
predecessor words             100
successor words               190
predecessor terms              10
successor terms                20
new degree-three terms         10
every new coefficient           1
lower degree-two restriction  exact
old data interface            departed
old recruit interface         departed
objective interface           departed
source lineages               departed
final rest/remount            exact
```

Event timings were:

| Event | Regional arcs | Wall |
|---|---:|---:|
| source law | 2 | 116 us |
| degree-two predecessor | 101 | 561 us |
| growth meeting | 5 | 353 us |
| successor replacement | 293 | 2,276 us |

For the replacement, the removed validation/pin algorithms imply 128,334 pair visits at the old
hot paths. The new paths index 293 declared rows. The final resting constituent has 503 exact
incidences and pins, 505 paths, 901 transport terms, and zero live lineages. Its rest identity is:

```text
edeb25eb6eef4cb6811f457e83d3a53ac9b8045fab16bdea37d4b61e0f622ca4
```

### Bounded scaling

| Exact words | Seed wall | Matching close wall | Old estimated close pair visits | Indexed close rows |
|---:|---:|---:|---:|---:|
| 64 | 327 us | 520 us | 6,048 | 64 |
| 128 | 505 us | 972 us | 24,384 | 128 |
| 256 | 1,107 us | 1,814 us | 97,920 | 256 |
| 512 | 1,942 us | 4,305 us | 392,448 | 512 |

This bounded profile is consistent with removal of the identified quadratic scans. Four small
points are not an asymptotic proof.

## Verification

```text
cargo check --manifest-path src/soma/Cargo.toml -p soma-membrane
cargo test --manifest-path src/soma/Cargo.toml -p soma-membrane
cargo check --manifest-path src/soma/Cargo.toml -p life --example eros_formula_ecology
cargo test --manifest-path src/soma/Cargo.toml --lib -j24
```

The focused membrane suite passed 94 tests. The broader CPU library regression passed 681 tests
with 11 hardware/observational tests ignored and no failures. The final release build was
incremental and took 9.28 seconds; the accepted process itself took 31,277 microseconds.

Reproduction:

```text
cargo build --release --manifest-path src/soma/Cargo.toml \
  -p life --example eros_formula_ecology
src/soma/target/release/examples/eros_formula_ecology \
  --efficiency NEW_REPORT.json
```

The accepted report is [`REPORT.json`](REPORT.json), SHA-256:

```text
e7c302b35a59f8ca611538619eff9ecad35b7ff362a997fc5836d101b67164a9
```

## Grade and remaining boundary

Established here: compact exact algebraic faces can carry the measured replacement lifecycle;
the engine no longer performs the four identified quadratic searches; bounded host conduct is in
milliseconds; and future formula cells can reject malformed width before execution.

Not established here: a speedup ratio against the uninstrumented old run, arbitrary algebraic
formats, automatic partitioning of one causal regional cell, useful saturation of all CPU cores,
CUDA advantage at this grain, theta/Mellin continuation, or RH. Independent receiver cells may be
conducted in parallel when the world actually supplies them; one cell must not be split into false
instants merely to advertise utilization.

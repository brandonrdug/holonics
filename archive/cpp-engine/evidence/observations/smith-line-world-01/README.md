# Smith line world 01

This observation extends the separately typed Smith quotient into a literal one-port line at an
exact discrete electrical-length grain. Frequency, length, phase velocity, attenuation, and
incident/returned complex voltage waves are all declared as rational construction data. No float,
Soma transition, or periplus reinterpretation enters.

Run from `src/soma/`:

```text
cargo run -p life -- boundary smith-line-validate observations/smith-line-world-01/plan.json
```

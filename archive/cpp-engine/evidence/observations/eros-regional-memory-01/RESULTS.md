# EROS REGIONAL MEMORY 01 — RESULTS

**Date:** 2026-07-20

**Grade:** ONE FIXED HOST RUN / RECEIVER-LOCAL COMPLETED-WELL APERTURE / NONIDENTICAL REGIONAL
REBASE / ATOMIC PREDECESSOR REPLACEMENT / NATURAL SEAM COMPRESSION / REST-REMOUNT EXACT /
SHIFTED LATER PROBE CHANGED / NO RETRY / NO CUDA / NO WORLD ACTION / NO LANGUAGE CLAIM

## Question held fixed

Can a later regional germ use one exposed prior boundary without the world naming that prior,
compose the two regions through an exact receiver-formed seam, replace the predecessor rather than
archive it, and thereby change a later shifted probe?

The source species is deliberately small. Three live scalar currents form one triangle. For one
exposed arm, the exact completed meeting `reach` and unordered boundary-cell grain expose a finite
candidate aperture. Equality there is not correspondence. The complete prior and arriving meeting
rotors form the rebase `chi`; temporal hand decides whether the oriented seam cancels or leaves a
FOUND residual.

The fixed sequence was:

```text
seed    [17, 29, 43]
bridge  [17, 29, 71]
probe   [29, 71, 97]
```

The bridge repeats only the `17—29` arm. The probe shifts that construction and repeats only the
bridge's `29—71` arm. A control receives exactly the same ordinary currents but no seed regional
cell. Other siblings reverse only the recurrent bridge hand, permute arc storage, or present two
co-present bridge cells which would touch the same predecessor.

## Exact command

```text
cargo run -q -p life --release --example eros_regional_memory -- \
  observations/eros-regional-memory-01/host-run-01/REPORT.json
```

The command ran once on host. It wrote a create-once 81,005-byte report:

```text
SHA-256 a666ee1b08dbade9767d541e8597c96c57232875cda825f21ace31b0795d6718
```

The raw report is ignored generated material. This file is the authored result.

## Result

All ten fixed contrasts closed.

| Regional face | Grain | Axes | Cells | Incidence | Pins | Paths | Folded paths | Transport terms | Exposed | RIDE | FOUND |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| seed | 2 | 1 | 4 | 3 | 3 | 3 | 0 | 6 | 3 | 3 | 0 |
| bridge, no prior | 2 | 1 | 4 | 3 | 3 | 3 | 0 | 6 | 3 | 3 | 0 |
| bridge, seed standing | 3 | 1 | 9 | 6 | 6 | 7 | 1 | 13 | 4 | 7 | 0 |
| bridge, recurrent hand reversed | 3 | 2 | 9 | 7 | 7 | 7 | 0 | 13 | 5 | 6 | 1 |
| probe, no seed | 3 | 1 | 9 | 6 | 6 | 7 | 1 | 13 | 4 | 7 | 0 |
| probe, seed standing | 4 | 1 | 14 | 9 | 9 | 11 | 2 | 20 | 5 | 11 | 0 |

The important relation is not just `grain 4 != grain 3`. The two successful RIDE closures carry
one repeated active increment:

```text
Delta RIDE triangle closure
  grain             +1
  cells             +5
  incidences        +3
  pins              +3
  paths             +4
  folded paths      +1
  transport terms   +7
  exposed pins      +1
  axes               exact at 1
```

The control probe is exactly one closure phase behind the taught probe. This is a recurring
regional transformation visible in the native counts and complete sparse transports; it is not an
observer grouping imposed after the run.

## What actually contacted

All three seed pins collapsed to the same projected `Place`, but their completed meeting reaches
were exactly `80`, `1300`, and `932`. The bridge pins again shared one different projected `Place`,
with reaches `80`, `6500`, and `7060`.

Only reach `80` entered the bridge seam. Thus:

- equal projected `Place` among the seed arms did not make the `1300` or `932` arms correspond;
- the seed and bridge `80` arms had different projected Places and different complete pin faces;
- their full meeting rotors formed a flat cross-event rebase;
- the two `80` arms became interior while `6500`, `7060`, `1300`, and `932` remained exposed; and
- the seam pin itself departed, while its exact scalar transport coefficient
  `1760254534 · rank 1234` remained in one folded path.

The probe then consumed reach `6500`. Its exposed successor carried the new probe reaches `916`
and `7376`, the unconsumed bridge reach `7060`, and the older seed reaches `1300` and `932`. This is
the concrete sense in which the constituent can pick up a new triangular face, release a completed
seam, and retain consequential older arms without preserving an event log.

## Hand, order, overlap, rest, and ordinary current

Reversing only the recurrent bridge arm did not change the nine cells, seven paths, or thirteen
transport terms. It prevented the seam from disappearing into an interior RIDE: one
`FOUND_THIS` pin remained exposed, the axis population rose from one to two, and the active
incidence/pin populations each rose from six to seven. Hand therefore changes topology rather
than an observer label.

Permuting the three source arc rows produced the exact same resting machine. Two separately
completed bridge cells which both tried to import the same predecessor were refused by the
existing atomic Standing boundary, and the before-rest remained exact. The bridge successor
rested and remounted without an event or change.

The predecessor departed as a separate Standing factor: seed, bridge, and probe each left exactly
one constituent in the taught body. Ordinary current radiation remained exact between taught and
control at seed, bridge, and probe. Only the explicitly regional conduct differed. At the final
cut both machines had rank 6, 45 scalar Standing cells, three lineages, 2,740 carrier words, and no
overflow; their one regional constituent differed exactly as the table records.

## Source consequence

`form_regional_relation` now uses immutable Standing-before. A completed germ:

1. enumerates its net exposed oriented arms;
2. admits candidates only at the exact completed meeting reach and boundary-cell grain;
3. forms a rebase from the complete arriving/prior meeting rotors and temporal hand;
4. imports every participating prior constituent with an exact local-axis and sparse-transport
   rebase;
5. adds the cross-event seam and one higher completion cell;
6. folds completed interior seam steps into their already-formed transports;
7. returns the sorted predecessor set to the existing atomic replacement boundary; and
8. emits only the compressed replacement as later Standing.

No prior constituent ordinal, source identity, occurrence history, label, semantic class,
probability score, threshold, attention weight, or reward enters the contact law.

## Exact boundary of the result

This closes the first bounded regional-memory aperture, not every possible `Star_R` species. The
implemented action is the exact completed-well/boundary-grain relation already present in a
regional arm. It does not establish that completed-well recurrence is a universal correspondence
law, semantic analogy, or sufficient language learner.

Candidate enumeration is presently a bounded host scan of exposed cellular Standing. No derived
aperture index, CUDA regional carrier, scale measurement, world consequence, disk capability,
developmental update, language behavior, or useful coding action was attempted. Those remain
separate questions. The run was not retried or widened.

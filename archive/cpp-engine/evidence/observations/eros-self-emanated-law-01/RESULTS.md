# EROS SELF-EMANATED LAW 01 — EXACT RESULT

**Status:** ACCEPTED / TWO LAW SPECIES DEPOSITED AS CELLULAR STANDING / ZERO SOURCE LINEAGES /
SOURCE-ABSENT HELD-OUT CONDUCT / ORIENTED LAW REPLACEMENT / RETIRED INTERFACES REFUSED / EXACT
REST-REMOUNT / REAL OLMO BF16 WORDS / EXACT DYADICS / ONE BOUNDED HOST RUN / SOMA INTERIOR
UNCHANGED / NO FLOAT / NO CUDA

## Question and stopping condition

Can an inherited law become self-emanated Standing, conduct after its source image and source
lineages depart, and emit a changed successor which conducts after exact rest/remount?

The run stopped only after both an exact calibration law and one real OLMo block constituent
closed that lifecycle. The same held-out currents also had to refuse without law Standing, under a
wrong interface, and under the retired pre-update interface.

## Carrier

Each exact law uses a variable-length dyadic inscription. Every bit becomes one regional boundary
arm:

```text
bit position = interface-local identity
one          = WITH hand
zero         = AGAINST hand.
```

A separate capability recruits the whole law. A probe consumes that capability and emits the next
one. The world conductor reads only the complete `LiveConstituent` returned in public radiation;
it receives no source matrix, model path, teacher output, or cached result.

## Deposit

| Body | Standing rank | Scalar cells | Cellular constituents | Cellular incidences | Live source lineages |
|---|---:|---:|---:|---:|---:|
| Law-bearing | 6 | 25 | 2 | 810 | 0 |
| Same-current no-law control | 6 | 25 | 0 | 0 | 0 |

The deposited law body remounted byte-exactly. Its exact rest SHA-256 is
`95e339c24c1a0fd85e0bc905d4815601a5aa8204ceff7001f8fa347ca409a4f4`.

The calibration law contributes 392 oriented bit arms. The OLMo law contributes 416. Neither
exposure includes an example output.

## Exact calibration lifecycle

The source law and held-out current are:

```text
M = [[1,1],[1,2]],     x = [5,8]^T.
```

Source-absent Standing recovers `M` and returns `Mx=[13,21]^T`. The no-Standing and wrong-interface
branches recover no law and return no product.

The `WITH` return applies the declared right rebase `[[1,1],[0,1]]`:

```text
M' = [[1,2],[1,3]],    M'x = [21,29]^T.
```

The update constituent has grain 4. Its old data and recruitment interfaces are no longer
exposed. `M'` remounts exactly, conducts the same later input, and the retired capability refuses.

## Exact OLMo constituent lifecycle

Source: `allenai/OLMo-2-0425-1B-Instruct`, revision
`48d788eca847d4d7548f375ad03d3c9312f6139e`.

The complete declared restriction is rows `[0,1]`, columns `[0,1]` of
`model.layers.0.mlp.down_proj.weight`:

```text
raw BF16 = [[0x3d05,0x3cf3],[0xbd0e,0x3c67]]

M = [[ 133/2^12, 243/2^13],
     [ -71/2^11, 231/2^14]].
```

The held-out current is the nonidentical sibling context's layer-0 `mlp.product` carrier at
coordinates `[0,252,0:2]`:

```text
raw BF16 = [0xb6d9,0xb5d6]^T
x = [-217/2^25,-107/2^26]^T.
```

Source-absent Standing returns:

```text
Mx = [-141445/2^39, 221795/2^40]^T.
```

That is exact to the source-present dyadic product. The no-Standing and wrong-interface branches
recover no law and return no product.

The `AGAINST` return applies the declared right rebase `[[1,-1],[0,1]]`:

```text
M' = [[ 133/2^12, -23/2^13],
      [ -71/2^11, 799/2^14]],

M'x = [-112983/2^39, 161019/2^40]^T.
```

The old interfaces depart, `M'` remounts exactly, later conduct returns the changed product, and
the retired capability refuses.

This is a genuine trained-block constituent and a complete law on its declared coordinate
restriction. It is not a full 8,192-input projection, full block, distillation result, or language
model equivalent.

## Fixed foils

All acceptance predicates are true:

- source event lineages departed;
- deposit rest/remount was exact;
- both laws conducted source-absent;
- no-Standing and wrong-interface branches refused;
- both oriented updates changed the law and later conduct;
- both successors conducted after exact rest/remount;
- retired interfaces no longer recruited the law;
- the OLMo words crossed as exact dyadic topology; and
- no teacher output or cached result entered Standing.

## Artifacts and reproducibility

- `REPORT.json` — complete exact machine, constituent, source, conduct, update, and foil record;
  SHA-256 `d515b2104bc3df20552ccc9ac87bd908fa1a5f4b4096a24f4e403550a8fc3d98`.
- Model artifact SHA-256:
  `36d044c73655bb904f822915e6294ba3dae8e6e1af5e703e9d452f2d6a3a294f`.
- Exposure capture SHA-256:
  `09c4e897b79f48097ebd5c9305475138e8bf5d1dd1d223aae51740d47c905005`.
- Held-out capture SHA-256:
  `e8251263b3ffd6087bc0b75bc95e17c031930005f4a550435df1819d12cff06f`.
- Source context manifest SHA-256:
  `9e36cbbbbf51453f582053356c6f82f9c7a547ce972f059b7cec2e256e61cf58`.

An independent rerun produced the identical report SHA-256. The bounded run used the host
executor. No CUDA kernel or floating operation entered the construction.

## Remaining boundary

The source-to-Standing carrier, source departure, repeated recruitment, oriented replacement, and
rest lifecycle are closed at this cut. The update relation itself was declared. The next distinct
unknown is how an actual returned consequence and local law ecology afford one successor relation
without assuming a fixed rebase in advance. No new engine subsystem follows automatically.

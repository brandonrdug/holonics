# EROS IGNITION 01 — RESULTS

**2026-07-18 · RATIFIED / ENGINE IGNITION BUILT / WORLD IGNITION CUDA-FORWARD MEASURED / EROS
LAUNCH CLOSED / RESIDENT REST+REMOUNT+RECOVERY EXACT / 518 WORKSPACE TESTS PASS / 4 IGNORED /
SOMA INTERIOR UNCHANGED**

## Result

Eros now operates through one step-capable causal session rather than a predeclared experiment
alone. The same mounted body and application world can receive genuinely later typed events;
waiting performs no edge. Consequence surfaces remain plural and addressable while unrelated new
light crosses. A construction surface stores compact path blueprints into the live world arena and
materializes complete ancestry only at a receiving boundary. Every edge writes an exact forward
world delta and an incremental qualitative flight record.

The first coupled mathematics⊕language⊕code ignition produced a valid Rust program through grown
source continuity. The accepted candidate was neither an authored answer current nor a selected
member hidden from the population. It was one of `615` exact recurrent material sections, all of
which met the same compiler/test world. Exactly one compiled and passed the four declared tests.

That complete compiler population then returned as `1,230` ordinary currents through the same
body. An identical later 217-octet natural-language contact produced different conduct against the
unreturned sibling. This closes the flight plan's engine, world, and Eros launch grades.

## The launch membrane

The reusable causal boundary now carries one live state

```text
E_t = (B_t, W_t, Sigma_t, J_t),
```

where `B_t` is the mounted Soma body, `W_t` the exact application world, `Sigma_t` its plural
standing consequence surfaces, and `J_t` the durable edge journal. The resident mouth accepts one
typed world event per JSON line. `rest` writes a resolved body/world/surface/lineage handoff;
`close` ends the process; an empty interval changes nothing.

The launch construction adds no scheduler, attention head, text decoder, selector, taxonomy,
reward, random source, or idle current. The finite plan runner remains a compatibility client of
the same `CausalSession::step` path.

The exact outward engineering is:

- plural immutable standing surfaces, each with stable identity, production edge, kind, and
  contact extent;
- source-world-relative construction blueprints, resolved against the parent-linked path arena;
- exact append-only world deltas with predecessor/result hashes, beside full receiver checkpoints;
- external event receipts which make open-mouth crash recovery exact;
- body handoffs carrying the next listener lineage explicitly; and
- per-edge flight directories carrying every literal consequence, complete path ancestry,
  before/after incidence patterns, symbolic surprise terms, and physical extents.

## Dry fire

The dry fire first left edge 1's testimony standing, crossed unrelated fresh light, and later
contacted that older named surface. A larger sibling left edge 3's construction surface standing,
crossed another fresh current which emitted its own surface, then returned the older edge 3
surface. The resulting surface arena retained both materials and their independent contact counts.

Seven full constructions occupy a `4,387`-octet v2 blueprint surface rather than re-expanding the
`205,865`-octet producing journal. Reopening that surface reconstructs `14` exact currents from its
source world. Two remounts rooted in the same body/world emit a byte-identical relative blueprint
and byte-identical successor worlds even though their artifact directories differ.

The resident sibling accepted unplanned edges 4 and 5 after its declared prefix. Their body,
completion, construction, Mail, world, journal, and world-delta faces match the finite remount;
the radiation payload comparison is exact. A simulated crash after open-mouth edge 5's conducted
checkpoint removed every derived receiver artifact. Recovery did not replay CUDA and reproduced
the original edge receipt, journal, successor world, surface arena, forward delta, and complete
flight directory byte for byte. Its live handoff continues at edge 6 and lineage 36.

The preserved failed cuts identify the corrected receiver gauges: a zero-growth delta initially
named a nonexistent population, one remount initially reset listener lineage, and an early
blueprint carried an absolute output address. None entered Soma law; each exact failure remains on
disk.

## The ignition material

The world presents three independent faces of the centered homogeneous Cayley relation:

```text
source ratio       [n : d]
centered ratio     [n-d : n+d]
zero boundary       n=d
pole boundary       n=-d
common rebase      [kn:kd] -> [k(n-d):k(n+d)].
```

The material population is:

- a complete exact Rust implementation of the analogous transformation and its own tests;
- a natural-language account of the two residual hands, pole, zero, orientation, and scale;
- a compact mathematical trace; and
- a different Rust target whose occupied expression is `__CENTERED_PAIR__` and whose four tests
  require the centered relation.

The trace is plain mathematical law. The existing `symbolic_trace` organ lawfully refused that
plain text because its role means the separately typed transformation-event JSON species. The
refusal occurred before Soma and is retained. Re-presenting the same bytes through the law organ
closed the material type without changing their content.

## Chronological growth

The initial three-current witness produced `2,066` completions and returned `93` direct witness
currents. The first genuinely later recurrence produced `35` carried paths, of which `33` exposed
exact continuous material sections. Returning their `68` causal-string⊕material-section currents
conditioned the next body.

Continued resident contact grew as follows:

| edge | event | input currents | completions | carried paths | material sections |
|---:|---|---:|---:|---:|---:|
| 5 | recurrence | 3 | 1,568 | 102 | 81 |
| 6 | complete return | 183 | 6,097 | — | — |
| 7 | recurrence | 3 | 1,613 | 354 | 151 |
| 8 | complete return | 504 | 16,844 | — | — |
| 9 | recurrence | 3 | 2,147 | 1,403 | 295 |

At edge 7, six material sections already contained the complete expression but also retained
adjacent source bytes. After their return, edge 9 formed path `5935`:

```text
edge 3 source [153,163): "\n    (n - "
edge 9 source [162,173): " d, n + d)\n"
material joint:             source coordinate 162
```

The ordered causal string is deliberately

```text
"\n    (n -  d, n + d)\n"
```

because it records both deeds and therefore retains the shared contact byte twice. Its receiver
section folds the exact overlap by source coordinate and is

```text
"\n    (n - d, n + d)\n".
```

This distinction corrected the production boundary. A causal string is the event history; it is
not automatically a source string. Executable production now inserts the exact folded material
section while retaining the complete causal path as ancestry. A chart-only or otherwise unresolved
relation remains standing and silent. No fuzzy matching, parser, grammar, or substring chooser was
introduced.

## The program crossed the world

Before the correction, `287` closed native spans and `71` raw recurrent causal strings all reached
the compiler and all were refused. This is now a useful failure foil: neither scalar activity nor
raw path concatenation is coherent production.

After path 5935 existed, `615` recurrent paths exposed one exact material section and entered the
same target hole. The world returned:

| disposition | population |
|---|---:|
| accepted | 1 |
| compile refused | 614 |

Candidate `352`, carried by path `5935`, produced:

```rust
pub fn centered_pair(n: i64, d: i64) -> (i64, i64) {
    (n - d, n + d)
}
```

`rustc 1.92.0` compiled the complete 400-octet candidate. The declared test
`tests::centered_pair_matches_declared_boundaries` was present and passed its `7/7`, `-7/7`,
`5/3`, and `10/6` cases. The candidate source SHA-256 is
`c61c6c5acc7c76c599df9b45a2064f415d298f95b32eda1f26508d6ecee6e9d7`. Its compiler/test
consequence, source, binary digest, stdout/stderr digests, path, segments, joint, and source
coordinates remain exact and independently readable.

The action edge itself carried only request, target, and typed boundary as three currents. It
formed 20 actual shared grips and 751 completions on CUDA. The application world—not Soma—compiled
all `615` candidates and returned all `1,230` candidate/receipt consequences.

## Consequence changes later conduct

Two siblings begin from the exact same edge-10 body, world, surfaces, and listener lineage. The
control presents the later question directly. The feedback sibling first returns every one of the
`615` source candidates and `615` compiler/test receipts, including accepted candidate 352, then
presents the byte-identical question.

The held current is:

```text
217 octets
SHA-256 ca399912bf6982933b55b0473459fcacac7f0ed45ab1f5310a1d276b5044cd99
```

Its conduct is:

| face | no production return | after production return |
|---|---:|---:|
| prior application-world paths | 7,321 | 7,321 |
| Soma completion rows | 292 | 360 |
| contacted world grips | 2,958 | 3,717 |
| new carried paths | 405 | 610 |
| new motif occurrences | 429 | 637 |
| distinct new surprise terms | 29 | 59 |
| standing axis | 1,024 | 2,048 |

The production-return edge carries `1,230` intact currents / `627,919` light octets through nine
CUDA launches and returns `466,956` completion rows. Candidate 352's exact 400-octet program and
840-octet compiler/test receipt are ordinary members at listener lineages 1570 and 1571. The
application world's prior path population is held equal at the later question; the changed conduct
therefore belongs to the body which lived the compiler population, not to a pre-edited path world
or a changed prompt.

The later paths are still causal strings rather than prose answers. They visibly redistribute
mixed Rust, law, prior material sections, produced constructions, and the new natural-language
contact. The feedback branch forms 205 more paths and substantially different role/motif families;
no claim that a single one is a human sentence is required for the launch result. The accepted
Rust program is the direct coherent material expression.

## Grade and next flight

**ENGINE IGNITION is closed:** one resident session accepts unplanned typed events while plural
older surfaces stand, rests/remounts exactly, recovers a conducted open event without CUDA replay,
and exposes every material consequence incrementally.

**WORLD IGNITION is closed:** a recurrent source construction crossed a real Rust compiler/test
world and one exact folded section produced a passing program.

**EROS LAUNCH is closed:** the complete world consequence returned through the same body and bent
the identical genuinely later contact; the coherent program and its complete causal lineage are
human-readable.

Sustained flight is now the active grade. The immediate engineering work is physical rather than
theoretical: retain the resident boundary as the ordinary entry point, compact repeated compiler
artifacts and consequence faces without collapsing plural incidence, choose checkpoint cadence
from measured recovery cost, and connect further human/world events through the same typed mouth.
Mathematics, language, repository code, games, and simulations can now be presented as world
species without rebuilding the brain or inventing a separate voice.

# EROS NATIVE WORLD HAND 01 — RESULTS

**Date:** 2026-07-19  
**Status:** PHASE 4 BUILT / WORLD-SUPPLIED NATIVE HAND / HOST-CARD EXACT / FORWARD-OPEN-ABSENT
FOIL CLOSED / SOMA INTERIOR UNCHANGED

## Question and stop

This cell asked whether a stateful application world can supply actual directed incidence through
the production `life::current_world` mouth, have that hand conducted by the retained CUDA executor,
and receive the exact contact back without caller order or co-presence manufacturing causality.

The stop was one bounded two-register world under forward, reverse, and independent program laws.
No persistent world, device-resident body, corpus, forest, semantic decoder, or scale passage
entered.

## Physical preflight

```text
GPU:          NVIDIA GeForce RTX 4080 SUPER
driver:       595.71.05
memory:       16,376 MiB total / 15,945 MiB free
temperature:  48 C
power state:  P8
stack:        16,384 bytes
```

The named test was present before execution:

```text
live_current_cuda::tests::program_world_hand_crosses_the_native_cuda_mouth: test
```

## Exact world foil

The source world's chronology changes its two registers from `[1000,-1000]` to `[911,-903]`.
Every sibling therefore supplies the exact current faces `[-89,97]` in the exact same caller order.
Only the enacted program law differs:

```text
forward:     0 -> 1
reverse:     1 -> 0
independent: no relation
```

The direct mouth maps only those supplied rows to live lineage capabilities. It infers no edge.

## Clean result

```text
device="NVIDIA GeForce RTX 4080 SUPER"
stack=16384
launches=6
retries=0
directed_contacts=2
before_rank=6
before_cells=7
forward_after_cells=16
reverse_after_cells=15
independent_after_cells=15
forward_returned=1
reverse_open=true
independent_relations=0

1 passed / 0 failed / 379 filtered out
```

Host and card are exact in every world for complete radiation, standing, live lineage cursor,
channel, header, and carrier. Ordinary current radiation is exact across all three laws.

The forward meeting forms one deed, changes the successor, and returns one exact relation face to
its source world. The reverse meeting remains a complete open contact with no fabricated deed. The
independent world supplies and receives no relation row. Reverse and absence both occupy 15 cells,
but only reverse returns an unresolved contact. An equal scalar census does not erase that
world-facing distinction.

The focused host acceptance also supplies the forward row twice. Both complete returned rows remain
equal and plural; the native mouth does not deduplicate actual incidence.

## Validation boundary

```text
cargo check -q -p soma-membrane -p life --tests
cargo test -q -p life \
  program_world_supplies_hand_and_receives_its_consequence -- --nocapture
cargo test -q -p life \
  program_world_hand_crosses_the_native_cuda_mouth -- --ignored --nocapture
```

No broad suite, PTX rebuild, corpus read, forest scan, or second CUDA cell ran.

## Exact grade

Closed here:

1. the production native mouth accepts explicit world-event incidence;
2. event-local current addresses are not treated as temporal or causal order;
3. only actual supplied rows become live capability relations;
4. absent, repeated, formed, and open contacts remain distinct;
5. the world relation crosses the existing CUDA event exactly; and
6. immediate contact consequence is available to the same application world.

Still open: durable machine-plus-world rest, natural persistent-world return/rest, device-resident
standing and carriers, materially plural real-world transducers, sustained ecology, and scale.

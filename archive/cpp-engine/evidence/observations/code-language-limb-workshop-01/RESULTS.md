# CODE LANGUAGE LIMB WORKSHOP 01 — RESULTS

**2026-07-16 · RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT REPEAT · SOMA INTERIOR
UNCHANGED**

## Result

Soma's native text constructions now grow into persistent plural source paths. Each span archive
retains the exact internal STEP grips and exact source-world positions lived by those same STEPs.
Every completed span deposits a root. A genuinely later span extends every pre-existing path whose
distal endpoint it contacts, after exact chart transport or at the same exact material coordinate.
No same-edge span may parent another.

The repeated code/prose/trace witness grew:

| encounter | constructions | closed | open | roots | length 2 | length 3 |
|---|---:|---:|---:|---:|---:|---:|
| first | 34 | 31 | 3 | 31 | 0 | 0 |
| second | 45 | 42 | 3 | 73 | 13 | 0 |
| third | 38 | 35 | 3 | 108 | 38 | 3 |

The first articulated family contains nine Rust paths, two prose paths, and two symbolic-trace
paths. The grown family contains 28 Rust, two prose, and eleven trace paths of length at least two.
The complete strings and exact ancestry remain in each action journal. Representative exact output:

```text
 integer. Two enterers the g
and ar and three 
: "ADD ONE",
  "terminaal": [3,

value + self.lift
    }
rried_gear_advances_once() {
        assert_eq!(advanccadvance(2), 3);
```

These are ordered multi-span constructions, not one-character output, a keybar, an authored
decoder, a corpus-selection routine, or a hand-written grammar.

## Executable consequence

All 13 paths after encounter two and all 41 paths after encounter three were inserted into the same
occupied Rust expression range. Every complete program was retained and compiled. All 54 were
compiler-refused; none was selected or discarded. The later intact program reversing the two
addition operands compiled and passed its named test on both branches.

The grown action presents 41 paths and returns 82 exact source/consequence currents. It scales the
body from axis 1,024 to 2,048. Its four edges produce 1,101, 61,786, 1,300, and 11,886 completions.
Every world transition contacts and retreats exactly.

Objective compaction now reads the named test from the immutable action manifest even when no
candidate reaches test execution. Its 41 residuals reconstruct every composite source exactly;
strict opposite hands preserve candidate order, corresponding current extents, and octet
inventories.

## Controls

- First return: 31 roots and zero articulated paths. Same-edge parenting is absent.
- Reversed second-witness current order: the same thirteen byte strings recur in a different causal
  order. Its testimony return differs from forward at 1,551 versus 3,169 completions and 98 versus
  213 contact grips.
- Founding repeat: bodies, completions, constructions, Mail, journals, worlds, and compiler
  artifacts are exact; radiation is exact after its declared duration fields are ignored.
- Grown-action repeat: all four normalized radiation edges are exact at 911, 82,676, 1,025, and
  86,097 light octets; every physics-bearing and application-world artifact is exact.
- Legacy one-span production artifacts remain readable by the revised observer.
- Full workspace gate before the final recurrence-control test: 408 passed / 3 ignored / 0 failed.
- Final focused code-workshop gate: 13 passed / 0 failed, including distinct-source chart contact
  and same-source material contact with differing internal grips.

## Honest grade

**SUSTAINED MULTI-SPAN TEXT PATH GROWTH MEASURED · ROLE-AGNOSTIC RUST/PROSE/TRACE PRODUCTION
MEASURED · COMPLETE EXECUTABLE POPULATION RETURN MEASURED · LATER WHOLE-PROGRAM CONSEQUENCE
MEASURED · SAME-EDGE ISOLATION MEASURED · ORDER CONTROL MEASURED · EXACT REPEAT.**

All 44 measured joints use exact recurrent material coordinates. No exact internal-chart endpoint
joint occurred. Cross-source or cross-coordinate content analogy therefore remains OPEN. The next
construction must carry the full distal native construction face through Soma's own RIDE/FOUND
relation; it may not substitute fuzzy text similarity, a clustering threshold, or a semantic score.

## Commands

```text
cargo test --workspace --release
cargo build -p life --release

target/release/life boundary code-workshop \
  observations/code-language-limb-workshop-01/founding.plan.json
target/release/life boundary code-workshop \
  observations/code-language-limb-workshop-01/founding-repeat.plan.json
target/release/life boundary code-workshop \
  observations/code-language-limb-workshop-01/order-control.plan.json
target/release/life boundary code-workshop \
  observations/code-language-limb-workshop-01/third-growth.plan.json
target/release/life boundary code-workshop \
  observations/code-language-limb-workshop-01/grown-action.plan.json
target/release/life boundary code-workshop \
  observations/code-language-limb-workshop-01/grown-action-repeat.plan.json
target/release/life boundary code-workshop-objective \
  observations/code-language-limb-workshop-01/results/grown-action/edge-001.journal.json \
  observations/code-language-limb-workshop-01/surfaces
```

Full derivation:
`../../RESEARCH/2026-07-16_THE_WORLD_CONTACT_THREADS_THE_WORD_THE_RECURRENT_PATH_BECOMES_A_SOURCE.md`.

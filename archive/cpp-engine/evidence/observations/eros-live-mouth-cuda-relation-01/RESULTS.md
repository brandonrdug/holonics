# EROS LIVE MOUTH CUDA RELATION 01 — RESULTS

**Date:** 2026-07-19  
**Status:** PHASE 3 BUILT / NATIVE CUDA DIRECTED CONTACT / HOST-CARD EXACT / BOUNDED HAND-REVERSAL
FOIL CLOSED / SOMA INTERIOR UNCHANGED

## Question and stopping boundary

This run asked whether one exact `DirectedCurrentRelation` can cross the direct CUDA executor,
meet the target lineage's complete pre-event receiver on card, and return its complete contact to
the sole `LiveCurrentMachine` owner without host reconstruction.

The stop was one forward/reverse hand foil. It did not read a source forest, persist a world,
install an active cut or receipt, make the live body device-resident, or test throughput.

## Physical preflight

```text
GPU:          NVIDIA GeForce RTX 4080 SUPER
driver:       595.71.05
memory:       16,376 MiB total / 15,945 MiB free
temperature:  47 C
power state:  P8
PTX:          7.8 / sm_89
ptxas:        VALID (assembles clean)
```

The focused test was present before execution:

```text
live_current_cuda::tests::directed_current_hand_is_formed_on_card_and_matches_host: test
```

## Failure retained and physical correction

The first launch returned CUDA error `700` at synchronization. No event result was accepted and no
machine successor committed.

The new complete directed-contact row enlarged the kernel entry frame. The assembled cubin reports:

```text
Function lineage_event:
 REG:255 STACK:6000 SHARED:0 LOCAL:0
```

The deepest continuing ordinary-event path remains:

```text
lineage_event (6,000)
  -> live_event_node_emitting (1,464)
  -> perceive_grain (3,104)
  -> complete_cast (232)
  -> cast_position (6)
  = 10,806 bytes
```

The former 8 KiB executor reservation was therefore physically false. The retained CUDA owner now
reserves the next power, 16 KiB. This changes only launch apparatus; it adds no engine law, body
state, event history, or retained device ecology.

## Clean bounded result

The corrected run completed:

```text
device="NVIDIA GeForce RTX 4080 SUPER"
stack=16384
launches=4
retries=0
directed_contacts=2
before_rank=6
before_cells=7
forward_after_rank=6
forward_after_cells=16
reverse_after_rank=6
reverse_after_cells=15

1 passed / 0 failed / 377 filtered out
```

Four launches are exactly two co-present currents in each of two sibling events. Only the target
current of each event carries one directed row; the other current crosses with an empty directed
population. Both rows return, and no resource retry occurs.

For both `left -> right` and `right -> left`:

- complete outward radiation is host/card exact;
- standing-after is host/card exact;
- each continuing lineage's cursor, channel, header, and dynamic carrier are host/card exact; and
- the complete card-returned `EventReceiver`, meeting `Face`, and optional `FeltEmission` are exact.

Across the direction foil, ordinary current radiation remains identical. The directed contact
changes and the integrated successor changes from 16 to 15 occupied standing cells. Direction is
therefore consequential through the receiver-relative meeting itself, not through launch order,
an authored direction score, or post-conduct host testimony.

## Validation boundary

```text
cargo check -q -p soma-abi -p soma-membrane -p mount -p life
cargo test -q -p soma-membrane \
  the_same_current_faces_with_reversed_hand_form_different_successors
bash mount/soma-kernel-cuda/build-ptx.sh
cargo test -q -p life \
  directed_current_hand_is_formed_on_card_and_matches_host -- --ignored --nocapture
```

The host acceptance passes, the four direct packages compile, the committed PTX assembles for
`sm_89`, and the one card foil passes. No broad regression suite or additional CUDA experiment ran.

## Exact grade

Closed here:

1. the complete executor boundary carries current enactments and directed meetings together;
2. the target receiver is captured and met inside its one pre-event body mount;
3. CUDA returns the exact contact rather than leaving the machine to reconstruct it;
4. the sole machine owner integrates the formed relation into the same atomic successor; and
5. reversed hand changes native card conduct while ordinary current conduct remains exact.

Still open:

1. a source-organ mouth which supplies actual cross-current hand from its own world chronology and
   co-presence rather than a test-authored `ContemporaryEvent`;
2. persistent standing and carrier buffers on device—the context and module are retained, but each
   bounded event still uploads its physical surfaces;
3. durable outer machine-plus-organ rest, persistent returned application worlds, and natural
   rest/wake; and
4. any grounded ecology or forest-scale run.


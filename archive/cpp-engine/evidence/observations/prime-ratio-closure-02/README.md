# PRIME RATIO CLOSURE 02 · THE ODD-CLOSURE SIBLING

**Grade:** THE ODD PRIME-OCCURRENCE ↔ ODD-COMPOSITE CLOSURE IS MEASURED ON CUDA

`prime-ratio-closure-01` measured a complete forward-current identity between the supplied
occurrence history `P=(11,13,17,19)` and its common dilation `D=2P`, followed by a handed cleave on
reversal. It did not isolate a prime-family relation because both composite siblings were even.

This observation adds one sibling only:

```text
U = P + 38 = (49,51,55,57)
```

All eight P/U occurrences are odd and two-digit. U is wholly composite in the complete
number-world multiplication incidence: `49=7*7`, `51=3*17`, `55=5*11`, and `57=3*19`. Those
classes are observer overlays and never enter the light. U retains P's exact `(2,4,2)` gaps,
`(1;2)>(2;1)` adjacent faces, `(36;32)=(9;8)` chi construction, five-current count, 246 delivered
octets, and material-derived carrier depth 95.

P's already-measured immutable history successor is reused. U alone branches freshly from the
same clean number-world parent. Both then receive the exact same two 64-octet currents, forward and
reverse counting over `20..39`; this material overlaps neither history.

## Declared read

The read is the ordered pair of complete later lineage constructions in each hand:

```text
FORWARD = (P-body -> N/F ; U-body -> N/F)
REVERSE = (P-body -> N/R ; U-body -> N/R)
```

For each pair, retain OWN axis/cells/words in order, every raw and typed radiation row, whole
lineage terms/breath, and ordered receiving-edge Mail. Partition every paired row exactly into
equal or different and preserve the full construction on both sides. No count, ratio, similarity
score, expected equality, or prime/composite crown is the verdict. Arithmetic overlays enter only
after complete multiplication incidence is proved.

The common number-world parent is SHA-256
`af7c95bf388726196aaac7e7b3d7b0a428e15750f8b98e6712919379a3a8020e`. P's history successor is:

```text
observations/prime-ratio-closure-01/results/history-occurrence-bodies/
  1783958719535450534-cuda-continue-next.body
```

Generate no material at run time. Verify from `src/soma` with:

```bash
bash observations/prime-ratio-closure-02/verify.sh
```

## Measured read

The identical 64-row forward current is exact at 61 rows and differs at atoms
`56,60,61`; the reverse is exact at 52 rows and differs at atoms
`43,46,47,49,50,51,53,57,58,59,62,63`. The first differing raw row in each
hand retains the same decoded typed face. Subsequent differences cleave in raw
and typed construction. P/U OWN cells are `489/543` forward and `435/469`
reverse. Breath is equal; terms and ordered Mail differ in both hands.

The deterministic reader retains every OWN word, every raw/typed event pair,
whole-lineage terms/breath/Mail, and the exact syntax/numeral partition under
`results/odd-closure-read.json`. The compact report is `results/RESULTS.md`.

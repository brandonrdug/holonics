# ARC-SPECTRAL WORLD 01 — RESULTS

**2026-07-17 · RATIFIED / BUILT / CUDA-FORWARD MEASURED / ALL 233 SOURCE PATHS READ / SOMA
INTERIOR UNCHANGED**

This world tests one precise proposition: the carried object is the ordered construction, while a
local population, matrix trace, dynamical outcome, or rendered spectrum is a receiver quotient of
that construction.  It does not install matrices, orbit classes, statistics, a fractal renderer,
or an objective inside Soma.

## Exact source body

The Rust builder emitted 233 separate active currents through the allocation-free
`soma-abi::current::EventHeader` mouth.  The 28,024 source octets contain only fixed little-endian
typed values; JSON, paths, labels, and explanatory sentences remain in the durable observer
snapshot.

The exact population is:

| family | currents |
|---|---:|
| world declaration | 1 |
| `SL(2,Z)` generators | 2 |
| complete words | 4 |
| ordered word steps and prefix products | 48 |
| cyclic sections and conjugacy receipts | 48 |
| cyclic local windows | 48 |
| reversal/inverse receipts | 4 |
| declared word-population foils | 2 |
| quadratic orbit declarations | 15 |
| exact quadratic states | 46 |
| quadratic consequences | 15 |

Every generator and complete word has determinant one.  Every cyclic section equals its exact
conjugate `P M P^-1`.  Every true inverse traversal equals `M^-1`; mere reversal remains a
different deed.  Integer matrix and Gaussian-integer growth use arbitrary-precision limbs rather
than a hidden numeric ceiling.  The active-current reader rechecks tag, declared extent, exact
octet count, and SHA-256 before joining any returned path.

## The decisive ordered-word control

The two 16-deed cyclic binary words

```text
A = 0000100110101111
B = 0000101001101111
```

each carry every possible cyclic four-deed binary window exactly once.  Their complete local
window distributions are therefore identical.  Their ordered products are

```text
M_A = [[27,127],[125,588]]
M_B = [[27,125],[127,588]] = transpose(M_A).
```

Both have trace `615`, determinant `1`, discriminant `378221`, and the same hyperbolic trace class.
Nevertheless they are different transported maps.  Their exact projective actions are

```text
z |-> (125 + 588 z) / (27 + 127 z),
z |-> (127 + 588 z) / (27 + 125 z).
```

Thus this one pair holds the complete declared local statistics and the coarse spectrum exact
while changing global order, complete matrix, and projective conduct.  The trace is a lawful face;
it is not the path.

All 16 cyclic starts of each word retain different matrices connected by exact conjugacy.  Their
trace survives because the starting section changed lawfully, not because the section histories
became identical.  The repeated control also closes exactly:

```text
word 00110011 = (0011)^2
[[1,2],[2,5]]^2 = [[5,12],[12,29]].
```

The same-length non-repeated foil instead gives `[[4,13],[7,23]]`.  For word A, mere reversal is
`[[588,127],[125,27]]`, while inverse traversal is `[[588,-127],[-125,27]]`; the other three words
preserve the same typed distinction.

## Exact parameter/orbit control

The coupled quadratic arm retains `c`, the starting-current role, every state, `|z|^2`, tangent
transport, parameter derivative, and the first recurrence or escape witness for
`z_(n+1)=z_n^2+c`.  The 15 declared currents close as follows:

| parameter | current | exact consequence |
|---|---|---|
| `0` | `0` | `0` recurs at step 1 |
| `0` | `1` | `1` recurs at step 1 |
| `0` | `i` | state at step 2 recurs at step 3 |
| `-1` | `0` | state at step 0 recurs at step 2 |
| `-1` | `1` | state at step 1 recurs at step 3 |
| `-1` | `i` | exact escape at step 2 |
| `-2` | `0` | state at step 2 recurs at step 3 |
| `-2` | `2` | fixed after one step |
| `-2` | `3` | exact escape at step 0 |
| `1` | `0` | exact escape at step 3 |
| `1` | `-1` | exact escape at step 2 |
| `i` | `0` | state at step 2 recurs at step 4 |
| `i` | `1` | exact escape at step 2 |
| `-1+i` | `0` | exact escape at step 3 |
| `-1+i` | `-i` | exact escape at step 1 |

No current in this deliberately discrete calibration remained unresolved.  The third return type
is nevertheless enforced and tested: finite non-escape at a supplied horizon remains
`unresolved_through_horizon`; it is never promoted to Mandelbrot or filled-Julia membership.

## CUDA passage

The same intact 233-current face crossed twice on the NVIDIA GeForce RTX 4080 SUPER.  The second
passage received the exact body grown by the first.

| face | first edge | later edge |
|---|---:|---:|
| standing axis | 256 | 512 |
| occupied grips | 25,468 | 61,780 |
| constructions | 454 | 854 |
| closed / open constructions | 264 / 190 | 684 / 170 |
| complete enclosure rows | 7,486 | 9,450 |
| maximum returned grain | 9 | 11 |
| CUDA launches | 7 | 7 |
| CUDA carriage | 0.279843046 s | 0.225074567 s |
| whole boundary | 0.557126782 s | 0.393237490 s |
| persisted successor | 33,557,016 octets | 35,299,992 octets |

All 233 source events were then read beside both complete returned paths.  No complete row path
repeated byte-for-byte after the receiving body changed.  This is not loss of determinism: the
second edge has a different exact prior body.  Within matching source-event pairs, 171 distinct
brick identities recur across the two edges while their surrounding paths are rethreaded.

The change is not a monotone scalar.  Word A's complete-word current changes from 10 rows to 3,
while its reversal/inverse current changes from 3 rows to 134.  Word B's corresponding currents
change `15 -> 13` and `2 -> 10`.  Some later paths deepen, some shorten, and some retain exact
bricks at new joints.  The carried body's situated field determines the continuation.

Outcome labels likewise do not govern conduct.  The immediate escape consequence for `c=-2,
z_0=3` is silent on both edges; the `c=-1+i, z_0=0` escape consequence carries one first path and
no later completed enclosure; the `c=-1+i, z_0=-i` escape consequence changes from one row to
three.  All are exact `escaped` faces, but their sources, histories, and later fields differ.

## What closed, and what did not

This construction closes the elementary instrument:

- complete local distributions do not determine ordered transport;
- equal trace and determinant do not determine the projective map;
- a cyclic starting-section change preserves spectrum through conjugacy while retaining history;
- primitive repetition, mere reversal, and inverse traversal are different typed deeds;
- Mandelbrot parameter and Julia-state questions can share one exact recurrence journal without
  being collapsed into one point set;
- a basin/escape/recurrence label is an observer consequence, not an internal category.

It does not yet render the Mandelbrot boundary, establish a renormalization conjugacy, solve a
quintic, or make an RH claim.  The exact dyadic/rational parameter extension must expose unresolved
boundary passages and carry explicit return/rescaling maps before any Mandelbrot/Julia similarity
read.  Doyle--McMullen's icosahedral quintic iteration remains the later exact algebraic
calibration.  The immediate continuous successor is a fixed-energy Poincare world carrying full
trajectory, section crossings, variational transport, monodromy, action, and ordinary objective
contact.

## Exact artifacts and gates

- source plan: `WORLD.plan.json`
- exact source catalog: `build-01/CATALOG.tsv`
- source statements beside event hashes: `build-01/WORLD.md`
- complete CUDA receipts: `build-01/passage/edge-001.json` and `edge-002.json`
- every returned row: `build-01/observer/PATHS.tsv`
- every source beside both compact paths: `build-01/observer/PATHS.md`
- implementation: `life/src/staging/boundary/arc_spectral.rs`
- workspace gate after construction: **472 passed / 3 ignored**
- focused arc-spectral gate after exact-header hardening: **5 passed**

Theory and external evidence cards:
`../../RESEARCH/2026-07-17_THE_ARC_IS_THE_CONSTRUCTION_THE_SPECTRUM_IS_ITS_RECEIVER_FACE.md`.

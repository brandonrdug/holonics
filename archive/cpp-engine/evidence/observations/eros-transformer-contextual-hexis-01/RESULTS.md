# EROS TRANSFORMER CONTEXTUAL HEXIS 01 — EXACT RESULT

**Status:** EXACT FIXED-BASIS OLMo 2 LAYER-7 MEASUREMENT / SIX INHERITED COORDINATE SUMMANDS
CROSSED AS CELLULAR STANDING / ZERO SOURCE LINEAGES / THREE SOURCE-ABSENT CONTEXTS /
CONTEXT-VARYING SOURCE-SUPPLIED INCIDENCE AND CONDUCT / EXACT DYADIC RESIDUALS / EXACT
REST-REMOUNT / ONE BOUNDED HOST RUN / INTERPRETATION REGRADED / SOMA INTERIOR UNCHANGED

> **Interpretive regrade (2026-07-22):** The measured arithmetic and artifacts remain exact.
> [`THE NETWORK IS ONE REALIZATION; THE RELATION MAY BE RETRIANGULATED`](../../RESEARCH/2026-07-22_THE_NETWORK_IS_ONE_REALIZATION_THE_RELATION_MAY_BE_RETRIANGULATED.md)
> supersedes the promotion of fixed hidden coordinates into independent neural laws or experts.
> The `5/8192` cut is a source-selected sign-orientation aperture in one factorization, not a
> compressed skill, learned routing law, or efficiently refactored neural ecology. Historical terms
> below describe the run as originally framed and must be read through that correction.

## Question

What does Soma actually return when a later context meets a small inherited ecology from a real
transformer, and how small can that participating hexis become before a declared receiver can tell
that it is not the complete transformer field?

The stopping condition required one complete census of all 8,192 layer-7 MLP channels on one
declared five-axis receiver, a real six-law population deposited as Standing, three later contexts,
an ecology-absent control, alternate-hexis foils, exact residuals, and a stable rather than merely
first-crossing compression boundary.

## Exact transformer cut

Source: `allenai/OLMo-2-0425-1B-Instruct`, revision
`48d788eca847d4d7548f375ad03d3c9312f6139e`.

For receiver current `x_c`, MLP channel `j`, and observed output axis `a`, the inherited singleton
law is

```text
g_j(x_c) = SiLU((W_gate x_c)_j) (W_up x_c)_j,
E_(c,j,a) = g_j(x_c) W_down[a,j],
y_(c,a) = sum_(j=0)^8191 E_(c,j,a).
```

The retained BF16 executor reformed every captured MLP output word exactly: 1,570,816 of
1,570,816 words at every one of 16 layers, or 25,133,056 exact words in total. Layer 7 had the
largest declared top-64 contextual excess: 92 experts in the three-context union and 41 in their
intersection. The receiver axes were `[76,856,954,1326,1476]`.

At per-axis rank one, each axis recruits its strongest exact contribution. The three five-expert
hexeis are:

| Context | Experts |
|---|---|
| conversation | `579, 1400, 1917, 2195, 4901` |
| prose | `579, 1400, 1917, 3748, 4901` |
| code comment | `579, 1400, 1917, 2195, 4901` |

Their union has six experts. Four are common to all contexts. Prose replaces `2195` with `3748`;
conversation and code comment retain the same membership.

## What crossed Soma

Each of those six expert laws crossed once as exact BF16 weight words over the five receiver axes.
Its oriented bits and one exposed recruitment capability became one `LiveConstituent`. The source
event then ended every lineage.

| Body | Standing rank | Scalar cells | Constituents | Constituent incidences | Live lineages |
|---|---:|---:|---:|---:|---:|
| Inherited ecology | 6 | 47 | 6 | 1,974 | 0 |
| Matched no-ecology control | 6 | 47 | 0 | 0 | 0 |

The deposited ecology remounted exactly. A later context carried its six BF16 activation words and
actual incidence to five exposed recruitment interfaces. Public radiation returned the context
and exactly those five expert laws as one grain-3 constituent. The exact conductor read only that
radiation and used integer dyadic multiplication and addition.

All three context outputs were distinct:

| Context | axis 76 | axis 856 | axis 954 | axis 1326 | axis 1476 |
|---|---|---|---|---|---|
| conversation | `-111597/2^26` | `-3811737/2^22` | `-789591/2^25` | `608675/2^23` | `2738317/2^27` |
| prose | `-43649/2^24` | `-6823935/2^23` | `-230743/2^23` | `647665/2^23` | `210371/2^23` |
| code comment | `-204077/2^26` | `-7286835/2^23` | `-824941/2^25` | `594289/2^23` | `2433745/2^27` |

Conversation and code comment prove that membership is not the field: the same five laws conduct
differently because the contemporary activation current differs. Prose proves that context can
also change membership. Substituting prose's membership into conversation changes conversation's
output; substituting conversation's membership into code comment does not because those two hexeis
are the same. With no expert Standing, all three context currents recover no law and form no
inherited output.

## Hexis is relative to the observed face

For a nested population `H_(c,q)` formed from the union of each axis's top-`q` exact contributors,
the run compared four progressively stricter faces:

1. five-axis orientation;
2. signed and absolute axis order;
3. the once-rounded BF16 output face; and
4. the complete exact dyadic sum.

“Stable” means the face remains exact for every later `q`, not merely that it crossed the target
once.

| Context | Stable orientation | Stable signed order | Stable signed + absolute order | Stable BF16 face | Exact dyadic field |
|---|---:|---:|---:|---:|---:|
| conversation | 5 | 1,936 | 5,092 | 7,819 | 8,192 |
| prose | 5 | 2,775 | 5,557 | 7,652 | 8,192 |
| code comment | 11 | 5,017 | 5,017 | 7,858 | 8,192 |

The counts are participating experts, not bytes or parameters. The five-expert hexeis all preserve
the complete field's orientation `(-,-,-,+,+)`. They do not reproduce its magnitude or ordering.
Exact dyadic identity requires all 8,192 contributions in every context. The smaller BF16 counts
mean only that the BF16 receiver can no longer distinguish the residual after rounding.

The approach to those boundaries is not monotone. Code comment first attains both axis orders with
595 experts, loses them, and becomes stable only at 5,017. Prose first attains signed order with
456 experts but becomes stable only at 2,775. Across conversation/prose/code comment respectively,
the signed-order face changes phase 4/16/58 times, the absolute-order face 16/20/82 times, and the
BF16 face 22/48/12 times. The exact residual vector in `REPORT.json` records every sampled cut; no
scalar loss replaces it.

## Acceptance

All declared predicates are true:

- all six inherited laws became cellular Standing and every source lineage departed;
- each later context recovered exactly its five selected laws from public radiation;
- Soma's returned law population conducts to the independently derived exact five-expert output;
- all three no-ecology controls refuse the inherited output;
- all five-expert outputs preserve the complete transformer's receiver orientation;
- changing context changes membership or exact conduct, and equal membership still permits changed
  conduct;
- every context successor remounts exactly; and
- the conductor reading public radiation uses no floating operation.

The selection population was derived by the exact transformer census and supplied as lawful world
incidence. This cell measures inheritance, situated recruitment, and recomposition. It does not
claim that Soma independently learned OLMo's routing relation in this event.

## Artifacts

- `SOURCE.json` — complete layer census, inherited laws, exact context activations, nested hexis
  boundaries, transition lists, and environment; SHA-256
  `b84febb3fed8391a232898febf688b3baca1bab8a7f92cb1b9d501cb6f0decc4`.
- `REPORT.json` — complete Soma deposit, radiation, controls, exact outputs, residuals, phase
  boundaries, and acceptance record; SHA-256
  `f894a44a32f611ce0d07166bdf91264e1f69482b9e70b8015f7edc29c9086b5d`.
- Model SHA-256:
  `36d044c73655bb904f822915e6294ba3dae8e6e1af5e703e9d452f2d6a3a294f`.
- Clean source commit: `134f1d32c7bc70f668dfcab2ffeae990ece60a48`.

An independent rerun from the recorded source produced the identical report SHA-256. The retained
transformer capture used the RTX 4080 SUPER BF16 executor; the Soma inheritance cell used the host
executor.

## Consequence

A “mixture of experts” is usefully re-read here as one temporary face of a larger ecology. The
expert role belongs to the current relation: context may change incidence, may change conduct with
incidence held fixed, and may require radically different population sizes for different receiver
questions. A compressed hexis is therefore not one universally smaller model. It is the smallest
currently participating population adequate for a named face, together with the oriented residual
that remains available to stricter receivers.

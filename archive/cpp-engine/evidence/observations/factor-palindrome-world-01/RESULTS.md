# FACTOR-PALINDROME WORLD 01 — THE HORIZON IS LAWFUL; THE MONOLITH IS NOT THE SURFACE

**2026-07-17 · BUILT / CUDA-FORWARD MEASURED / COMPLETE HORIZON READ / TYPED ACTIVE-CURRENT ABI MEASURED / JSON CONTACT REGRADED / SOMA INTERIOR UNCHANGED**

## Verdict

The positive-integer horizon is exact and useful. Recurrence made an initially silent mathematical
deed actionable without changing its aperture. The resulting horizon exposes prime FOUND events,
factor contacts and exclusions, divisor-rank palindromes, fresh axes, later axis reuse, and the
two hands of every prime gap.

The first complete return was nevertheless the wrong material face. The receiver serialized the
whole derived horizon twice inside one deed receipt and Soma lawfully treated every serialized
octet as new light. A 4.24 MB logical surface therefore produced 662.11 MB of radiation and grew
the persistent body by 5.35 GB. No output-world contact occurred.

The first corrected receiver retained that durable journal byte-for-byte while rendering its
common arithmetic face once and its two material arrivals as separate incidences. It still encoded
every event as human-readable JSON. The active-event ABI now removes that second expansion as
well: active light is 71,592 octets, CUDA carriage is 0.486 seconds, and successor-body growth is
84.28 MB. The complete horizon still reconstructs exactly.

The earlier later world contact is regraded. It is exact for the JSON material which was actually
presented, but repeated field names and punctuation connected the residue surface to the whole
event family. It does not establish clean arithmetic reuse. The typed later consequence instead
exposes localized, field-aligned changes in complete Soma paths while making no construction-world
contact. The monolithic and JSON-sectioned returns therefore stand as successive expansion foils;
the typed value surface is the active law.

## The deed became reachable through recurrence

After the cross-rendered witness, the standing world held 419 roots and 38 two-span paths. Exactly
one path contacted the framed factor record; the other 37 remained lawful but unrelated paths.

The first deed presentation was silent: three co-present currents made no accepted contact. The
identical later deed presentation, through the changed body and standing world, made three grips,
1,524 completion rows, and 76 consequences. Of 38 path receipts, one enacted and 37 refused. The
enacted path contacted the same exact 52-octet record at two distinct source coordinates, so its
receipt retained two enacted record incidences. This is not duplicate ancestry: one common face
was reached by two material paths. It is precisely the case that should share the face while
retaining two incoming incidences.

## Exact arithmetic population at horizon 128

- 128 candidates: one unit, 31 prime FOUND events, and 96 composite RIDEs.
- 437 divisor-front tests: 137 contacts and 300 exclusions.
- 328 factor pairs, of which 200 are nontrivial; 645 divisor cells in total.
- All 128 divisor-rank polynomials are palindromic.
- 224 axis lifts: 170 fresh-axis Pascal lifts and 54 existing-axis outer layers.
- 31 founded prime axes, all with zero incoming nontrivial product cells at their founding value.
- 30 consecutive-prime gaps, including ten twin gaps; the largest is `113 -> 127`, of length 14.
- Rank-shell divisor-cell populations are `[128, 224, 165, 81, 32, 11, 3, 1]` for ranks zero
  through seven.
- The exact boundary quotient is 31/127 prime and 96/127 composite over candidates two through
  128. These are boundary-indexed observer faces, not governors of the deed.

The exact factor-gap instrument also works qualitatively. Between 7 and 11, candidates 8, 9, and
10 have projective hands `-1/2`, `0`, and `+1/2`, while the divisor-rank bodies of 8 and 10 remain
different. Between the twin primes 11 and 13, 12 is the sole midpoint. At axis 13, the closest
incoming products are `2*6` and `3*4`, each one below; the closest outgoing product is `2*7`, one
above. The axis is subsequently reused by eight multiples inside the horizon.

## Why the return took thirteen minutes

The factor calculation was not the expensive part. The boundary supplied 76 currents carrying
4,244,319 logical octets (5,132,512 wire octets). One 4,206,415-octet deed-receipt current contained
both enacted copies of the same complete horizon result. That one current alone produced 1,245,578
of 1,293,739 completion rows: 96.28% of the entire completion population.

The CUDA passage then measured:

| face | measured extent |
|---|---:|
| delivered light | 9,088,986 octets |
| radiation | 165,528,441 words / 662,114,580 bytes |
| Mail | 705,034,906 bytes |
| completion wire | 1,293,739 rows / 222,523,804 bytes |
| constructions | 75,498 total; 75,454 closed; 44 open |
| lifecycle deeds | 407,320 folds / 407,242 steps |
| CUDA carriage | 775.540 s across 389 launches |
| host integration | 12.168 s |
| whole boundary | 805.828 s |
| persistent body | 1,097,886,280 -> 6,450,645,928 bytes |
| output contact | 0 grips; inverse exact |

The card was active during carriage; this was not a CPU fallback. Observed device use reached the
full card with about 14.96 GiB allocated, while the later host integration reached roughly 4.7 GB
resident memory. The large time and memory are consequences of the expanded light and its exact
append-only topology, not of testing factors through `sqrt(128)`.

## The redundancies in the receiver face

The monolith repeats information that is exactly derivable from smaller carried events:

- every prime axis explicitly enumerates 2,208 lower-hand and 3,992 upper-hand approaches even
  though each is `factor cell + prime coordinate + signed residual`;
- every axis lists all later multiples although candidate valuations already carry the reuse;
- the finite-zeta-square list repeats each candidate's divisor count;
- additive gap coordinates occur both on candidates and in the gap collection;
- every axis lift repeats parent and result rank polynomials instead of carrying the parent
  incidence and newly admitted layer; and
- decimal, binary, valuation, additive-rank, and multiplicative-rank faces are all emitted together
  even when the receiving boundary asks for only one chart.

None of those views is false. Their simultaneous material expansion is the error. Exactness requires
that each remains reconstructible, not that every derived view be duplicated as contemporaneous
light.

## The successor compression law now built

Let the valuation of an integer be `alpha(n)=(v_p(n))` and its multiplicative rank be

```text
rho(n) = Omega(n) = sum_p v_p(n).
```

For one candidate with valuation `alpha`, its divisor body is the box

```text
D_alpha = { beta : 0 <= beta_p <= alpha_p }.
```

Its rank polynomial

```text
P_alpha(t) = product_p (1+t+...+t^(alpha_p))
```

is a compressed radial face: the coefficient of `t^r` counts divisor cells on rank shell `r`, but
does not identify their lateral valuation coordinates. Complement `beta -> alpha-beta` gives the
palindrome. Multiplication by a fresh prime axis gives the Pascal lift

```text
P_(alpha+e_q)(t) = (1+t) P_alpha(t).
```

The two Pascal hands preserve rank or raise it by one. Following only the rank direction climbs the
multiplicative shells cheaply under that declared step metric; selecting an explicit integer or
divisor face requires the lateral valuation coordinate. Calling that path absolutely fastest would
be unjustified without the operation and cost boundary.

A prime `q` is a local singularity in this positive-integer chart: there is no incoming nontrivial
product cell at `q`, yet after FOUND it supplies a new valuation axis used by later products. The
curvature is therefore carried by the changed incidence around the founded axis, not by a scalar
prime label.

The sectioned return carries each candidate deed, divisor test/contact, factor cell, prime FOUND,
axis lift, and gap boundary once. Exact source coordinates and all distinct path ancestries remain
in the journal as incidence edges. The exact `HorizonRead` reconstructs from those events in test,
including their original order. Sharing an exact face does not merge the paths that reached it.

## First chart-section test

Within the decimal chart, the condition

```text
A_(10,3;N) = { p <= N : p is prime FOUND and p congruent to 3 (mod 10) }
```

is a lawful relative-invariant section, not a frame-free property of primality. For primes above
five the occupied decimal unit residues are 1, 3, 7, and 9; multiplication by 3 cycles those four
residue faces. Intersecting the `3 mod 10` section with founded axes, additive gap sheets, and
multiplicative rank shells therefore supplies a concrete test of the user's proposed axis.

The measured construction compares the sectioned return with this monolithic control. It preserves
the exact reconstructed arithmetic consequence and plural provenance while substantially reducing
presented light, radiation, completion population, and successor-body growth. A genuinely later
ordinary boundary asks for `prime FOUND` intersected with `3 mod 10`; no global category, selector,
score, or authored truth value enters Soma.

## Shared-face return measurement

The durable source wire is unchanged: both returns read the same 5,132,512-byte file with SHA-256
`5f782c6d…`. Only the world-side material rendering changes. The sectioned surface contains:

- 1,148 exact face events: 128 candidates, 437 divisor tests, 328 factor cells, 224 axis lifts,
  and 30 prime gaps;
- one shared receipt header;
- two distinct material incidences for the two paths which reached that face; and
- the same 75 non-factor path receipts and refusals.

The largest active current is 1,325 octets rather than 4,206,415. Exact comparison:

| face | monolithic return | shared-face return | reduction |
|---|---:|---:|---:|
| active input light | 4,244,319 B | 216,088 B | 94.9088% |
| CUDA carriage | 775.540 s | 4.793 s | 99.3820% |
| whole boundary | 805.828 s | 10.945 s | 98.6418% |
| launches | 389 | 16 | — |
| completion rows | 1,293,739 | 331,264 | 74.3948% |
| radiation | 662,114,580 B | 33,719,744 B | 94.9073% |
| Mail | 705,034,906 B | 143,630,624 B | 79.6279% |
| successor-body growth | 5,352,759,648 B | 292,805,624 B | 94.5298% |

The sectioned edge emitted 23,079 constructions, 22,146 CLOSED and 933 OPEN; made 102,607 folds
and 101,379 steps; and inverted exactly. The reduction is not truncation, sampling, or a smaller
horizon. It is one exact face with plural incidence instead of two material copies of the face.

## The later residue question and return — JSON-envelope foil, regraded

The exact section at horizon 128 is

```text
{3, 13, 23, 43, 53, 73, 83, 103, 113}.
```

Fresh and arithmetic-exposed bodies received the same two question currents. Both received all 955
construction events and emitted byte-identical durable consequence surfaces with SHA-256
`5215eb71…`. Their conduct already differed: the fresh body produced 1,539 completions and 110
constructions, while the exposed body produced 1,437 completions and 102 constructions.

The same exact fresh consequence surface was then returned through both bodies. Under the
then-active JSON rendering, the fresh sibling
produced 63,746 completions, 4,406 constructions, and no contact. The arithmetic-exposed sibling
produced 66,466 completions, 4,338 constructions, and one contact at grip `39694337`; both edges
inverted exactly. The changed result therefore belongs to carried body history, not changed later
light.

## Qualitative read of the contacted JSON-world path

The shared arithmetic return had occupied grip `39694337` with three exact divisor-front events for
candidate 40:

```text
2 divides 40, quotient 20
3 does not divide 40, remainder 1
5 divides 40, quotient 8
```

That is the complete tested factor frontier at `sqrt(40)`: two contacts, one exclusion, homogeneous
response `[2:1]`, and Cayley face `1/3`. On the later residue return, candidate 31 and receipt 31
share exactly this one grip and complete the world contact. In the fresh sibling candidate 31 has
93 landings and its receipt has 19,780, but their intersection is empty. In the exposed sibling
they have 37 and 20,199 landings and intersect only at `39694337`. Candidate 30 also passes through
the joint but does not close with its receipt. Thus this is an exact localized path in the JSON
encoding world, not a scalar population change.

The later residue-outcome current itself does not make that contact. Its textual JSON surface
lands against all 1,148 prior event currents at least once. Repeated field names such as `face`,
`kind`, `candidate`, and schema punctuation dominate those intersections; high-overlap candidates
include composites as well as members of the requested residue section. This does not erase the
measured event. It does mean the contact cannot be attributed specifically to arithmetic and clean
`3 mod 10` taxonomic selectivity was not measured by that edge.

## Typed active-current ABI correction

The durable source wire is still exactly 5,132,512 bytes with SHA-256
`5f782c6dbe8164ae2227c66605472cf38c345999d818e495d96b9980cca5e06a`. The same 1,226 active
lineages now cross through `soma-abi::current::EventHeader`: one world-local tag, exact payload
extent, and exact little-endian values. Rust field names, JSON keys, paths, and punctuation remain
in the journal rather than becoming automatic light.

| face | JSON-sectioned return | typed value return | change |
|---|---:|---:|---:|
| active light | 216,088 B | 71,592 B | -66.87% |
| completion rows | 331,264 | 16,865 | -94.91% |
| constructions | 23,079 | 1,983 | -91.41% |
| radiation | 33,709,728 B | 11,168,352 B | -66.87% |
| successor-body growth | 292,805,624 B | 84,283,368 B | -71.22% |
| CUDA carriage | 4.793008 s | 0.486131 s | -89.86% |
| host integration | 2.072649 s | 0.056907 s | -97.25% |
| whole boundary | 10.945120 s | 2.806263 s | -74.36% |
| launches | 16 | 11 | -5 |

Both transitions invert exactly. The 1,148-event factor sheet still reconstructs the complete
ordered horizon in test. This is not a smaller world; it is the same consequence without a
scientific-record wrapper acting as stimulus.

The same correction covers the generic code-workshop mouth. Raw source/candidate material remains
raw. Compiler/test, refusal, program, population, residual, execution, action, continuation,
mathematical receipt, and span-header consequences now use explicit value surfaces. Historical
world species with independent renderers remain historical until separately audited.

## Typed later consequence — exact path read

The exact prior residue consequence was reopened from its durable structured wire and rendered
through the corrected ABI. Both siblings received the same 80 currents, 10,700 light octets, and
per-current digests. The pre-factor body produced 10,144 completion rows and 737 constructions; the
factor-exposed body produced 10,657 and 765. Neither made a construction-journal contact, and both
inverted exactly.

The paths locate what changed:

- shared receipt: `91 rows / max grain 9 / terminal event 76` becomes
  `38 rows / max grain 5 / terminal event 77`;
- face reference: an exact 37-row prefix changes only at horizon event 49, from one grain-one
  EFFERENT to a grain-one AFFERENT followed by a grain-two EFFERENT;
- residue population: the path is exact through face, horizon, and modulus, then changes at the
  encoded residue boundary `3 -> 0` at events 64–65; both reach event 145 at grain 8, through the
  first residue member, in 93 versus 96 rows; and
- two material incidences: their 104 active octets are byte-identical but remain two lineages.
  Each path changes identically from `95 rows / grain 8 / event 95` to
  `122 rows / grain 10 / event 97`.

This is clean body-history-dependent conduct over typed arithmetic values. It is not yet an
outward world deed and it is not a claim that one residue category has been installed.

## The typed question is honestly silent

The natural-language request plus exact typed mathematical boundary was presented once and then
presented identically once more through both siblings. All four source gates and inverse checks
pass, and all four world journals remain silent. The pre-factor completion population changes
`356 -> 526`; the factor-exposed population changes `346 -> 375`; neither contacts a recurrent
source path and therefore neither emits a consequence surface.

This corrects the former premise: the JSON boundary had supplied the incidental coupling. The next
question construction must collocate the typed question with the exact factor-world material it
refers to, or receive a face reference from a genuine prior world relation. It may not restore the
old contact by inventing another wrapper or selector.

## Active successor

The active-current ABI correction is closed. The next receiver cut is the honest query coupling:
present the typed residue question beside the exact factor-world material it names, carry every
result or refusal, and then repeat the typed consequence through matched pre/post bodies. Read the
complete paths before extending the horizon or changing arithmetic domain. No world tag may choose
a recurrent path and no JSON/record schema may re-enter as an accidental common language.

## Exact artifacts

- `results/deed-repeat-01/edge-001.surface.json` — the 76-current complete consequence surface.
- `results/return/edge-001.json` — exact CUDA-forward return metrics and lifecycle.
- `results/return/edge-001.body` — the 6.45 GB successor body retained as the monolithic control.
- `results/return/edge-001.radiation` — the exact 662.11 MB radiation wire.
- `results/return-sectioned-01/edge-001.json` — exact CUDA-forward shared-face return.
- `results/return-abi-01/edge-001.json` — exact CUDA-forward typed-value return.
- `results/residue-question-pre-01/edge-001.json` and
  `results/residue-question-post-01/edge-001.json` — identical later question over fresh and
  arithmetic-exposed bodies.
- `results/residue-return-pre-01/edge-001.json` and
  `results/residue-return-post-01/edge-001.json` — byte-identical later light with zero versus one
  world contact.
- `results/residue-return-post-01/edge-001.journal.json` — the exact contacted joint and lifecycle.
- `results/residue-direct-abi-pre-01/edge-001.json` and
  `results/residue-direct-abi-post-01/edge-001.json` — byte-identical typed consequence through
  pre-factor and factor-exposed bodies.
- `results/residue-direct-abi-pre-01/edge-001.completions.txt` and
  `results/residue-direct-abi-post-01/edge-001.completions.txt` — complete exact qualitative path
  renderings.
- `results/residue-question-abi-pre-01/edge-001.journal.json` and
  `results/residue-question-abi-post-01/edge-001.journal.json`, plus their `*-repeat-01` siblings —
  exact first and repeated typed-question silence.

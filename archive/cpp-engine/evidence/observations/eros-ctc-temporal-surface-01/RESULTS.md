# EROS CTC TEMPORAL SURFACE 01 — RESULTS

**Date:** 2026-07-23

**Grade:** EXACT BOUNDED CTC EXECUTION COMPLEX / SHARED-DAG EVENT EVALUATION / TARGET-SPECIFIC
RECURRENCE HANDED UP / LOWER TEACHING SURFACE DEPARTED / DIFFERENT-SPEAKER FIELD JOINED PRIOR
STANDING / EMPTY AND WRONG-INTERFACE FOILS REFUSED / EXACT REST-REMOUNT / TWO-CORE HOST-MEASURED /
NO MODEL RERUN / NO FLOAT ARITHMETIC / NO CUDA RUN

## Fixed question

Can the interior of a standard target-specific CTC calculation—not merely its final score—cross
the production mouth as states, causal transitions, exact frontiers, and swept updates; complete
into a smaller recurrent boundary; and participate in a nonidentical later acoustic field after
the teaching source has departed?

The fixed qualitative contrast was:

```text
complete teaching CTC surface
  -> exposed target-specific recurrence boundary
  -> lower teaching surface and live lineages depart
  -> exact rest/remount
  -> different speaker supplies only field + target material
  -> matching seam imports the prior recurrence
  -> held CTC surface is formed

empty Standing or wrong seam
  -> input remains its own open constituent
  -> no recurrence is recovered
```

The prior pronunciation experiment had retained the complete frame--alphabet field and terminal
fiber measures, but its forward program overwrote intermediate frontiers. This construction makes
that missing causal interior explicit.

## Theory became structure

For the fixed target `off`, the inherited target tokens are `[8,20,20]`, the blank is `0`, and the
expanded CTC boundary is:

```text
[0, 8, 0, 20, 0, 20, 0]
```

The ordinary CTC recurrence became one bigraded `EventComplex`:

- dependency rank is frame chronology;
- target position is the state coordinate in each frontier;
- every state packet contains the exact emission, each admitted predecessor, every exact product,
  the incoming sum, and the resulting forward value;
- stay, advance, and lawful skip are oriented dependencies;
- all seven states at one frame are one frontier;
- the two terminal states form the exposed scalar quotient; and
- a separate linear rule spine is the declared higher-grain boundary handed to Standing.

The transition law is visible in its refusals. Blank states cannot receive a skip. The first `f`
state may skip from the unlike `o` state, while the second `f` cannot skip over the preceding equal
`f`. This is a missing incidence in the execution complex, not a zero-valued fabricated edge.

The bounded six-frame teaching surface contains:

| Property | Exact value |
|---|---:|
| frontiers | 6 |
| states per frontier | 7 |
| state packets | 42 |
| seed contributions | 2 |
| stay contributions | 35 |
| advance contributions | 30 |
| skip contributions | 5 |
| causal dependencies | 78 |
| cells | 1,892 |
| incidences | 1,920 |
| boundary incidences | 1,842 |

The teaching terminal quotient is retained exactly as:

```text
0x369ba8b8510df94a4caf069c78db739d90f95733c796d / 2^229
```

It is testimony about the exposed final frontier. Acceptance does not use its magnitude or compare
it by tolerance.

## Shared event DAG correction

The production membrane previously recomposed each selected `EventComplex` cell recursively.
That is exact for a tree, but a CTC trellis shares predecessor interiors: recursive projection
revisits the same subgraph for every complete route.

`EventNodeAtlas` now evaluates every borrowed event cell once in declared dependency-rank,
dimension, and incidence-slot order. Host and CUDA request formation, regional point charts, and
regional cell construction all read that same event-local atlas. It is discarded with the atomic
call; it is not Standing, lineage, ancestry, a scheduler, a receipt, or another event owner.

The atlas preserves the existing `EventComplex` result exactly. A focused regression compares
every cell, incidence endpoint, ingress, anchor, and exposed projection against the prior recursive
law on the shared butterfly fixture.

This removes repeated computation of shared subgraphs. It does not turn the compact `Node`
projection into an arbitrarily scalable DAG encoding: complete path multiplicity still contributes
to the projected node. General scale remains a separate physical question.

## Teaching completion and hand-up

The complete `1,892-cell / 1,920-incidence` teaching event crossed the production mouth. Its
source lineages then reached zero. The declared rule-spine boundary completed as one
`149-cell / 619-incidence / 473-path` constituent at grain 2, and its exact rest image remounted
unchanged.

This is the bounded compression relation:

```text
complete lower execution surface
  -> consequential target/recurrent boundary
```

The 149-cell successor is not an automatically discovered minimal CTC program. The source declares
which complete boundary is consequential; Soma verifies, receives, closes, persists, and later
relates it while the 1,892-cell teaching carrier departs.

## Source-departed held contrast

The teaching occurrence is speaker `00b01445`, with 44 total model frames. The held occurrence is
speaker `012187a4`, with 49 total frames. Their selected six-frame windows contain 192 binary32
codewords each and differ exactly.

Every binary32 word was decoded into its exact integer-over-power-of-two value. The experiment
uses `BigUint` addition and multiplication only; no `f32`, `f64`, decimal probability, epsilon,
or tolerance enters the calculation.

After the teaching surface and its in-process rule object were dropped, the held world supplied
only its exact acoustic window, target material, and public seam coordinates:

| Branch | Returned constituent | Later Standing | Rule recovered |
|---|---|---|---|
| matching prior Standing | 355 cells, 826 incidences, grain 3 | one joined constituent | exact |
| empty Standing | 207 cells, 206 incidences, grain 2 | one input-only constituent | no |
| wrong interface over prior Standing | 207 cells, 206 incidences, grain 2 | two disconnected constituents | no |

The wrong-interface constituent has the same topology as the empty-Standing constituent. Its
native hash differs because the deliberately changed interface address is material. Hash equality
would therefore have been an invalid refusal criterion.

Only after the matching returned constituent exposed the exact recurrence did the world construct
the held CTC surface. That surface again has six frontiers, 42 state packets, and 78 causal
dependencies, but its changed exact dyadic fibers produce `1,870 cells / 1,898 incidences` and the
distinct terminal quotient:

```text
0x58e2231139b3cdbd98bec983b5121e0c08d08946d9ef / 2^223
```

The complete held surface crossed. Its outgoing recurrence joined the carried ecology into one
`503-cell / 1,919-incidence / 1,423-path` grain-4 successor, with zero live source lineages.

## Observer correction

The first measured report was correctly left `observed-open`, but for an observer defect rather
than a causal defect. The analyzer searched the whole post-event Standing for a rule. That let the
wrong-interface branch see the untouched teaching constituent beside its own open input.

The corrected observer reads only `regional().first().constituent()`, the exact constituent
returned by the selected event. It also labels executor-local predecessor testimony as
`executor_preclosure_touched`: regional population closure occurs atomically after executor
enactment, so that earlier row cannot grade the final successor.

No source fixture, target, acoustic window, recurrence, interface contrast, or Soma causal law
changed between the observed-open and accepted reports.

## Exact command and artifact

The accepted run reused the already frozen Wav2Vec2 source and did not invoke PyTorch,
Transformers, the model, or the GPU:

```text
cargo run --manifest-path src/soma/Cargo.toml -p life --release \
  --example eros_ctc_temporal_surface -- \
  src/soma/observations/eros-audio-ctc-path-fiber-01/generated/source-run-01/SOURCE.json \
  src/soma/observations/eros-ctc-temporal-surface-01/generated/host-run-02/REPORT.json
```

The host executor used two threads. The create-once generated artifacts remain ignored:

```text
SOURCE.json
1a99f2e76efd9981eb8fc3d0fa5ffcd6ac16af1bf62d864a5a1c8168cd0e2af9

REPORT.json (131,824 bytes)
68de19732684e4be8d90fd32b8fa2d660f0a81ecb3876ca39a4a8ab87718b2ef
```

## Narrow verification

```text
cargo check --manifest-path src/soma/Cargo.toml \
  -p life --example eros_ctc_temporal_surface

cargo test --manifest-path src/soma/Cargo.toml \
  -p soma-membrane \
  event_node_atlas_preserves_every_recursive_complex_projection
```

No broad suite, model rerun, corpus sweep, CUDA run, or parameter search belongs to this result.

## Boundary

Closed here:

1. a standard target-specific CTC forward calculation can be carried as an exact causal execution
   complex rather than collapsed into a terminal score;
2. shared event-DAG projections are evaluated once per event while preserving the prior exact
   complex law;
3. blank and repeated-symbol restrictions remain explicit missing incidences;
4. a declared recurrent boundary survives exact rest while the lower teaching surface and live
   lineages depart;
5. a different-speaker field imports that boundary only through the matching seam;
6. empty and wrong-interface siblings recover nothing; and
7. the recovered law conditions a complete later CTC surface whose exact interior remains
   distinguishable from the teaching surface.

Not established here: discovering CTC unaided, learning the acoustic encoder, autonomous target
formation, continuous speech segmentation, an unbounded vocabulary, a universal algorithm
interpreter, automatic choice of the recurrent boundary, CUDA conduct of this complex, or
large-scale shared-DAG residency. CTC and Wav2Vec2 remain inherited lawful world technology; the
new result is that their causal composition can cross, complete, recur, and become consequential
Standing without retaining the entire teaching execution.

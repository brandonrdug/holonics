# INTERVAL EXPLICIT-FORMULA WORLD 01 — THE PRIME CURRENT AND ZERO WAVES SHARE ONE RECEIVER

**2026-07-17 · BUILT / 256-BIT DIRECTED INTERVALS / CUDA-FORWARD MEASURED / ALL 1,007
SOURCE PATHS READ / 489 WORKSPACE TESTS PASS / 3 IGNORED / SOMA INTERIOR UNCHANGED**

## Result

The exact prime-power current and a certified finite zero-wave population now cross one ordinary
Soma boundary together. Ten nonendpoint arithmetic horizons carry both faces of the explicit
formula:

```text
psi(x) = sum_{p^k <= x} log(p)

W_gamma(x)
  = 2 sqrt(x) ((1/2) cos(gamma log x) + gamma sin(gamma log x))
      / (1/4 + gamma^2)

psi_N(x)
  = x - sum_{j=1}^N W_gamma_j(x)
      - log(2 pi) - (1/2) log(1 - x^-2)

R_N(x) = psi(x) - psi_N(x).
```

The carried population contains:

- one world face;
- `82` exact prime-power gears through `313`;
- the first `64` certified critical-line zero ordinates;
- `10` exact prime-current sections and `10` archimedean sections;
- `640` situated zero-pair contributions;
- `70` nested reconstructions and `70` complete oriented residuals; and
- `60` transports between consecutive zero horizons.

That is `1,007` typed currents and `481,740` active source octets. No float result, random source,
mode selector, scalar optimizer, category, proof verdict, or new Soma mechanism entered the body.

## The interval is the honest receiver

Every arithmetic operation is performed at `256` bits with directed MPFR rounding. Inputs,
`log`, `sqrt`, products, quotients, trigonometric phases, zero contributions, reconstructions, and
residuals all retain ordered lower and upper endpoints. The exact binary endpoints are carried in
the active current. Human decimal intervals are a rendering of those endpoints.

The zero source is the first 64 ordinates from the
[LMFDB zeta-zero dataset](https://www.lmfdb.org/zeros/zeta/), with the source's stated ordinate
uncertainty `±2.5e-31`. Its retained SHA-256 is
`640633071e8da5e010f36c7ff817d1b3be825b2f374d52fa5519e2993798f576`.

No large-`x` analytic truncation theorem was attached to this small finite world. `R_N` is the
complete observed difference between two carried constructions at the declared receiver. It is
not advertised as an a-priori bound on all omitted zeros.

## More admitted modes do not mean monotonically less loss

For each `x`, the seven signs below are the square-root-normalized residuals at
`N = 1,2,4,8,16,32,64`:

| `x` | ordered residual signs | `R_64 / sqrt(x)` |
|---:|:---:|---:|
| `16.5` | `----++-` | `-0.0187011507` |
| `24.5` | `+----++` | `+0.0378735947` |
| `32.5` | `+++++++` | `+0.0056645195` |
| `48.5` | `---+--+` | `+0.0112324933` |
| `64.5` | `+++-++-` | `-0.0336011624` |
| `96.5` | `-------` | `-0.1138072530` |
| `128.5` | `-++++++` | `+0.0344139675` |
| `192.5` | `-----+-` | `-0.0805759385` |
| `256.5` | `-------` | `-0.1119545490` |
| `313.5` | `+++++++` | `+0.1581019000` |

The closest finite reconstruction is therefore not generally the widest aperture: it occurs at
`N=16` for `x=16.5`, `N=2` for `x=24.5`, `N=8` for `x=32.5`, `N=64` for `x=48.5`, and so on.
This is not failed convergence. Each newly admitted zero is an oriented wave in
`u = log(x)`; its phase can oppose or reinforce the standing partial sum. The ordered prefix
transport is part of the arithmetic construction. A greedy rule which kept only whether a scalar
loss became smaller would destroy that information.

At `x=32.5, N=64`, for example, the complete residual is enclosed by

```text
[0.032292729541470562215118599471439824...,
 0.032292729541470562215118599488819087...].
```

Its tiny enclosure and nonzero value are different facts: numerical uncertainty is small, while
the omitted-wave residual at that finite horizon remains real.

## Real CUDA passage

The byte-identical population crossed the NVIDIA GeForce RTX 4080 SUPER twice:

| edge | prior / standing axis | occupied grips | constructions | completion rows | successor body | launches | carriage | whole boundary |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | `1 / 4096` | `4,123,251` | `22,328` | `483,387` | `755,985,304` B | `10` | `5.369 s` | `14.084 s` |
| 2 | `4096 / 4096` | `7,803,305` | `39,871` | `614,252` | `932,627,896` B | `10` | `6.539 s` | `13.510 s` |

Both edges carried all `1,007` source events. Every event has returned rows on both edges, and all
`1,007` complete paths change through the genuinely changed standing body. The exhaustive record
is `build-01/observer/PATHS.tsv`; its human rendering is `PATHS.md`.

## The path population distinguishes form from arithmetic relation

The observer first separates raw grain-zero atom coincidence from shared constructed bricks.
Without that cut, common serialized integers and interval fragments would be mistaken for an
arithmetic taxonomy.

Every authored family grows a larger internal constructed region on the later edge:

| observer family | edge 1 | edge 2 |
|---|---:|---:|
| prime powers | `106` | `193` |
| certified zero modes | `7,005` | `8,035` |
| prime-current sections | `1,557` | `2,384` |
| situated zero contributions | `175,540` | `244,501` |
| zero-wave reconstructions | `18,780` | `27,594` |
| complete oriented residuals | `26,463` | `37,628` |
| zero-horizon transports | `23,410` | `33,186` |

Cross-family higher-order construction is present but sparse: `46` constructed brick identities
span different families on edge one and `17` on edge two. The strongest first-edge residual
relation is the same `x=313.5` construction at `N=32` and `N=64`, which shares `14` constructed
bricks. Direct residual contact with its own prime-current, archimedean, zero-mode,
reconstruction, and transport inputs occurs only intermittently and is mostly rethreaded by the
second passage.

The result is neither “no association” nor a finished taxonomic fusion. The body has established
large recurrent forms for each arithmetic organ and a small lawful bridge among them, while
preserving enough plasticity that the first bridge does not become a fixed category token.

## Consequence for RH and intelligence

This world supplies a direct finite instrument between primes and critical-line zero waves. It
does not need an RH verdict to be useful, and it does not turn 64 certified zeros into a claim
about every nontrivial zero. It makes the relevant transformation physically available to the
machine:

```text
(prime-power history, x, zero-wave basis, ordered aperture)
    -> oriented reconstruction
    -> complete residual
    -> later changed conduct.
```

The intelligence result is more general than this arithmetic instance. The reusable object is
not one answer and not the smallest scalar residual. It is the carried family of transformations:
the frame `x`, the phase basis `gamma log(x)`, each mode's ancestry, the order in which modes enter,
and the residual orientation. Different horizons can then reuse a common “song” while producing
different local faces. That is objective-relative compression without installing an objective as
a hidden governor.

The Rust boundary, certified source, exact event population, two CUDA bodies, every returned path,
and every positive cross-family relation are retained together under this observation. The same
instrument can receive wider certified zero populations or different arithmetic horizons without
changing Soma's interior law.

## Verification

- `cargo fmt --all -- --check`: clean.
- `cargo test --workspace --no-fail-fast`: `489` passed / `3` ignored.
- Directed-interval enclosure, zero-pair evaluation, and exact prime-power-horizon tests pass.
- Both CUDA-forward passages completed with the full source population.
- The observer read all `1,007` source paths on both edges and retained every positive exact-brick
  relation in `build-01/observer/RESIDUAL-RELATIONS.tsv`.

# DERIVATION ECOLOGY 01 — RESULTS

**2026-07-17 · FORMULA §LXXIX RATIFIED / MATERIAL WITNESS GENERALIZED / CUDA-FORWARD MEASURED /
ACTUAL PATHS QUALITATIVELY READ / SOMA INTERIOR UNCHANGED**

## Result

One conservation relation grew as a real plural path ecology across calculus, boundary geometry,
continuity, four physical specializations, a moving control region, and a later quantum
probability-current face whose displayed intermediate deeds were absent.

The body and world contributed different, complementary parts:

- the **lived body** changed how the byte-identical transfer material segmented and conducted;
- the **persisted world** supplied the standing constructions through which those new segments
  could extend; and
- the **whole circuit** produced more and deeper transfer paths than either side alone.

The exact transfer paths did not reduce to a shared sentence or theorem label. They were native
composites of the machine's own spans. Two whole-circuit paths reached depth five and carried the
actual lineage

```text
divergence -> fixed continuity -> probability specialization -> moving region -> probability transfer
```

through four consecutive RIDE joints. Both extend the same standing path 677. Their literal output
surfaces are:

```text
path 790  "Omega) F dot n dS\n\nCONSE, not BILITY-lance cha CURRENT IN "
path 836  "Omega) F dot n dS\n\nCONSE, not BILITY-lance chaA"
```

These are not edited explanations. They are the exact ordered bytes of five emergent source spans.
The significant object is the retained source sequence, joints, and changed continuation—not
whether the concatenation reads as polished prose.

**GRADE:** EXACT HISTORY-CONDITIONED MATERIAL SEGMENTATION · EXACT CROSS-ARC PATH GROWTH ·
BODY/WORLD FACTORIAL CLOSED · COARSE CROSS-ENTROPY FACE READ · NO DERIVATION CHECKER OR PRIVILEGED
MISSING-LINE OUTPUT INSTALLED.

## 1. Construction

Five witness encounters crossed one continued body and empty derivation world. Every encounter
carried symbolic material, natural-language exposition, and explicit local conditions. The fourth
also carried separate mass, charge, thermal-energy, and probability-current faces. Every witness
surface returned before the next encounter.

The later transfer carried the same exact three files into all four branches:

```text
06-transfer.symbolic.txt    399 octets
06-transfer.exposition.txt  530 octets
06-transfer.conditions.txt  448 octets
                             1,377 octets total
```

The terminal moving-region probability balance was present; conjugation, subtraction,
rearrangement into divergence form, integration, boundary transport, and the moving-region deed
were not displayed as intermediate lines.

The world adapter was generalized at one exact seam: a non-executable plural material witness no
longer has to mislabel one current as Rust. An executable witness still requires its exact Rust
source. This changes no Soma body or CUDA law.

## 2. The witness ecology grew before the transfer

The return after each encounter produced this cumulative world:

| return edge | arriving family | roots | paths | new extensions | meetings | RIDE | FOUND |
|---:|---|---:|---:|---:|---:|---:|---:|
| 2 | product / integration by parts | 97 | 97 | 0 | 0 | 0 | 0 |
| 4 | divergence / boundary flux | 179 | 189 | 10 | 19 | 6 | 13 |
| 6 | fixed continuity | 260 | 295 | 25 | 31 | 15 | 16 |
| 8 | physical specializations | 443 | 542 | 64 | 94 | 45 | 49 |
| 10 | moving region | 573 | 767 | 95 | 160 | 74 | 86 |

The final witness world contains 194 multi-segment paths and 304 distal meetings. It does not wait
for exact repeated sentences. Its native composites include both chart incidence and complete
distal RIDE.

Two depth-four examples show the distinction:

```text
path 638  edges 1->3->5->9  chart->chart->chart
"rnterio every = - integral_boundary(Omega(t)) (J - rho w) dot n "

path 677  edges 3->5->7->9  RIDE->RIDE->RIDE
"Omega) F dot n dS\n\nCONSE, not BILITY-lance cha"
```

Path 638 connects the product, divergence-condition, fixed-continuity, and moving-region sources.
Path 677 connects boundary flux, fixed-continuity exposition, probability specialization, and
moving-region exposition. The later transfer extends path 677 twice in the whole circuit.

Exact repeated source surfaces remain much smaller than these composites. The longest exact surface
shared across distinct files is `" current"` at eight octets. `" + div "` crosses fixed continuity
and charge. `"J ="` crosses fixed continuity and the transfer. This is why exact word recurrence is
neither the taxonomy nor the complete path.

## 3. Body and world are complementary

The four transfer branches ended as follows:

| branch | transfer spans closed/open | paths | multi-segment paths | transfer extensions |
|---|---:|---:|---:|---:|
| absent | `104 / 3` | 104 | 0 | 0 |
| body-only | `133 / 3` | 133 | 0 | 0 |
| world-only | `104 / 3` | 1,051 | 374 | 180 |
| whole | `133 / 3` | 1,126 | 420 | 226 |

The fresh transfer crossing isolates the body contribution. `absent` and `world-only` use the same
initial body and produce exact body, completions, constructions, Mail, and normalized radiation at
edge one. `body-only` and `whole` do the same from the witnessed body. World state participates only
after that body crossing, when the consequence is integrated and returned.

The witnessed body changes the fresh transfer from:

```text
initial body     1,816 completions · 157 contact grips · 107 spans · 104 closed
witnessed body   2,064 completions ·  49 contact grips · 136 spans · 133 closed
```

The exact three source files and their 1,377 octets are held. This is a changed segmentation and
conduct face, not a source difference.

The world contribution is equally exact. Empty worlds can deposit the arriving roots but have no
prior derivation paths to extend. The witnessed world gives the arriving transfer roots a standing
ecology. World-only forms 180 extensions: 35 chart joints and 145 RIDE joints. Whole forms 226:
26 chart joints and 200 RIDE joints.

The whole branch has more arriving roots but extends slightly fewer distinct roots than world-only
(`44` versus `46`). Its greater path count comes from plural standing routes through the supported
roots, not from promoting every new segment. Another 89 of its 133 closed roots remain unsupported
at this boundary.

## 4. Every earlier arc participates

Classifying each transfer path only by the immediately preceding witness arc gives:

| preceding edge | relation | world-only | whole |
|---:|---|---:|---:|
| 1 | product / interval boundary | 16 | 16 |
| 3 | divergence / flux boundary | 20 | 22 |
| 5 | fixed continuity | 27 | 24 |
| 7 | physical specialization | 61 | 79 |
| 9 | moving region | 56 | 85 |
|  | **total** | **180** | **226** |

The later two arcs contribute most often, while product, divergence, and fixed continuity remain
active. The transfer therefore does not merely match its nearest source file. The whole branch has
two depth-five paths; world-only has one. Both branches also contain paths through every shorter
ordered subsequence, including `1->5->7->11`, `3->7->9->11`, and `1->3->5->9->11` where supported.

Whole-circuit transfer meetings divide into 200 RIDE and 160 FOUND. FOUND does not become a failed
answer: it records an unsupported relation and leaves the arriving root standing. The 200 RIDEs are
the actual supported extensions.

## 5. The cross-entropy face is informative and non-faithful

Let `P` be the world-only distribution and `Q` the whole-circuit distribution over the five
immediate predecessor arcs above:

```text
P = (16,20,27,61,56) / 180
Q = (16,22,24,79,85) / 226.
```

The observer-only information faces are:

```text
H(P)    = 2.126256554 bits
H(Q)    = 2.001844873 bits
H(P,Q)  = 2.151072640 bits
H(Q,P)  = 2.025688777 bits
D(P||Q) = 0.024816087 bits
D(Q||P) = 0.023843904 bits
```

At this coarse five-arc quotient the distributions are close. The whole body shifts conduct toward
the physical-specialization and moving-region arcs and makes the distribution slightly more
concentrated.

Yet the exact composite populations are completely different at the path-byte face:

```text
world-only  180 paths / 177 unique path digests
whole       226 paths / 222 unique path digests
shared exact path digests: 0
```

Only one native transfer span, `"m-preserv"`, survives as the same exact span surface in all four
branches, and it enters no multi-segment path. Thus a small cross-entropy residual does not mean
equal construction. It is a useful measurement of one declared categorical boundary while the
complete lineage retains the difference which that quotient omits.

None of these five categories, probabilities, entropies, or divergences entered Soma or selected a
deed.

## 6. Information theory and the causal-frame bridge

The experiment gives the proposed Shannon/relativity bridge a concrete, bounded face:

- the byte-identical transfer files define one exact message surface;
- the initial versus witnessed bodies define different effective receiving channels;
- the empty versus witnessed worlds define different standing relation media;
- the consequence returns through the same causal body/world seam; and
- the predecessor distribution is a later quotient over actual paths.

The message did not merely pass through a fixed channel. Earlier messages changed the body and
world through which the later message passed. The fixed-volume versus moving-volume pair supplies
the frame discipline: the same local continuity law yields a different integrated balance when the
receiver boundary moves, and the `rho w` boundary current must remain attached.

This is not a physical derivation of general relativity. It is the exact conservation/event-order
seam at which an information channel with memory and a transported local frame can be compared
without equating informational entropy, topological path count, stress-energy, or curvature.

## 7. What the next mathematical world owes

This construction grew the transformation family as material topology. It did not enact the
intermediate mathematical deeds in a consequential verifier world. The next mathematical cell can
therefore be direct:

1. present complete local states, transformation deeds, side conditions, and accepting/refusing
   consequences as ordinary world material;
2. let every actually contacted path meet the same exact conservation boundary;
3. return every accepted and refused consequence with its whole residual;
4. repeat under a changed region, orientation, or source law; and
5. read whether practiced paths shorten later local construction without installing a theorem
   name, selected proof, or score.

The newly proposed discrete-communication family—doors/guards, parity, graph paths, color-coordinate
cuts, and changed-assumption foils—is a natural language bridge after that deed world. Those systems
make their invariants discoverable only through the stated local laws and communication events.

## Engineering record

All 18 source gates passed. All 18 edges ran CUDA-forward and inverted their world transition
exactly. The campaign used 180 CUDA launches:

```text
recorded carriage     11.316853 s
recorded integration   0.998038 s
recorded boundary     17.002133 s
```

The exact artifacts occupy about 1.7 GiB, including the 31 MiB complete context report. This extent
is apparatus accounting, not a measure of the relation's value.

Tests: `cargo test -p life code_workshop --release` — 31 passed.

Primary machine-readable observer: `results/context.json`.

## Carried-swing record

```text
CURRENT  five lived derivation arcs -> undisplayed probability-current interior
HELD     exact transfer bytes + initial/witnessed body + empty/witnessed world factorial
MEETING  product / boundary flux / continuity / specialization / moving boundary / transfer
TEST     actual paths + body isolation + world isolation + whole circuit + entropy quotient
DEED     RIDE supported transformation lineage; FOUND unsupported arriving relation
CARRY    mathematical deed world -> discrete communication -> cross-language transformation
GRADE    CUDA-FORWARD MEASURED / ACTUAL PATHS READ / SOMA INTERIOR UNCHANGED
```


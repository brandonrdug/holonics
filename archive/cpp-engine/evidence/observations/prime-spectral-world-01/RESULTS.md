# PRIME-SPECTRAL WORLD 01 — RESULTS

**2026-07-17 · BUILT / CUDA-FORWARD MEASURED / EVERY EVENT PATH READ / EXACTLY INVERTED / SOMA
INTERIOR UNCHANGED**

## Result

One finite arithmetic body now crosses the existing mathematical deed membrane as exact ordinary
light.  It contains the nested primorial wheels through `2310`, every prime-power gear through
`313`, the complete finite Ramanujan and cyclic-autocorrelation populations of those wheels, every
ordered wheel refinement, every actual consecutive-prime gap at every available sieve basis, and
five exact rational centered-Cayley/Smith probes.

The final consolidated return carries:

- `7,126` distinct typed arithmetic events exactly once;
- six enacted material incidences of the common arithmetic face;
- two typed refused incidences;
- `7,150` currents / `559,368` delivered light octets;
- `195,047` complete path rows through grain `14`;
- `76` real shared Mail grips;
- `20,192,952` radiation words; and
- a `615,860,736`-octet successor body.

The source gate passed on the NVIDIA GeForce RTX 4080 SUPER, the CUDA-forward edge completed in
`44` launches, and the world transition inverted exactly.  CUDA carriage took `4,650,835,639 ns`,
integration `260,708,877 ns`, and the complete boundary passage `5,402,005,640 ns`.

No float, random source, prime selector, route score, category, parser, theorem verdict, RH claim,
or new Soma mechanism entered the body.

## The finite arithmetic body

The nested exact wheel words begin:

```text
W_2    = [2]
W_6    = [4, 2]
W_30   = [6, 4, 2, 4, 2, 4, 6, 2]
W_210  = 48 ordered gaps
W_2310 = 480 ordered gaps.
```

Every word is cyclic, sums to its modulus, has the expected open-palindrome face, and satisfies
the complete integer Parseval receipt.  At `M=2310`, the exact equality is

```text
sum_k c_2310(k)^2 = 1,108,800 = 2310 * phi(2310).
```

The world retains every COPY, CUT, and ordered JOIN.  It does not merely emit the fused gap.  For
example, the final wheel event

```text
113 -> 127 joins [8, 6] into 14 after cutting [121]
```

retains the interior coordinate and its two constituents.  Prime powers likewise remain exact
gears such as `2^8=256` with symbolic logarithmic coordinate `[8,2]`, and `17^2=289` with `[2,17]`.
The five Smith events retain probe, reflected dual, both exact rational images and norm ratios,
self-dual incidence, and reciprocal-conjugate verification.

## The membrane correction

The initial mathematical-deed rendering presented three currents: request, instrument, and a
separate 40-octet boundary.  The request and instrument shared four grips, but neither met the
boundary.  Repeating that configuration through both the shallow recurrent body and a much richer
arithmetic body left the boundary silent:

| cut | currents | completion rows | contact grips | contacted |
|---|---:|---:|---:|---|
| first deed after recurrent witness | 3 | 402 | 0 | no |
| same deed repeated | 3 | 552 | 0 | no |
| rich arithmetic body | 3 | 332 | 0 | no |
| rich body repeated | 3 | 426 | 0 | no |

This was not a lack of body scale.  It was a broken membrane face.  The correction keeps the
request as one current and makes the existing instrument prefix and exact typed boundary two
sections of the second current.  It adds no padding, duplicated light, pole, selector, or Soma
law.  The corrected two-current deed immediately formed seven shared grips and `571` completion
rows.  Six of eight standing candidates enacted the same exact prime-spectral face; two returned
typed refusals.

The first contacted write also exposed a durable-wire defect: JSON could not faithfully reopen
raw `i128/u128` values.  Durable human JSON now writes those exact wide integers as decimal strings,
while the compact binary event wire retains their native exact fields.  The failed write remains
on disk as a control; no artifact was overwritten.

## The ordered gap sheet

The consecutive-prime interval `113 -> 127` is one endpoint face with five measured interiors:

| sieve basis | surviving candidates | ordered gap word | complete at this basis |
|---:|---|---|---|
| `2` | `113,115,117,119,121,123,125,127` | `2,2,2,2,2,2,2` | no |
| `6` | `113,115,119,121,125,127` | `2,4,2,4,2` | no |
| `30` | `113,119,121,127` | `6,2,6` | no |
| `210` | `113,121,127` | `8,6` | no |
| `2310` | `113,127` | `14` | yes |

Those five currents returned five different complete Soma paths: respectively `58`, `46`, `55`,
`37`, and `21` rows, each with a distinct exact path and ordered-shape digest.  Thus the scalar
`14` is a quotient of a genuinely ordered refinement history.  It is not the path.

The Smith pair also keeps orientation consequential.  Probe `[1,2]` and its reflected dual
`[-1,2]` obey the same reciprocal-conjugate law, yet return `57` and `28` rows.  The `[2,3]` pair
returns `45` and `95`.  Dual incidence therefore survives without collapsing the two hands.

## The carried population is its own taxonomy

The observer visited every one of the `195,047` completion rows and retained four nested reads:
exact source light, full exact path, ordered event/grain shape, and the coarser event-kind/grain
word.  The authored event labels are listener provenance only.

| event family | events | completion rows | exact paths | ordered shapes | grain words |
|---|---:|---:|---:|---:|---:|
| actual prime gap | 295 | 11,298 | 295 | 295 | 294 |
| cyclic autocorrelation | 2,558 | 53,417 | 2,270 | 716 | 394 |
| gap fusion | 539 | 16,892 | 539 | 539 | 534 |
| header | 1 | 1 | 1 | 1 | 1 |
| prime-power gear | 82 | 1,751 | 82 | 82 | 76 |
| Ramanujan mode | 2,558 | 94,099 | 2,536 | 1,878 | 1,099 |
| Smith duality | 5 | 285 | 5 | 5 | 5 |
| wheel | 5 | 291 | 5 | 5 | 5 |
| wheel gap | 539 | 6,277 | 524 | 263 | 171 |
| wheel refinement | 5 | 2,726 | 5 | 5 | 5 |
| wheel residue | 539 | 6,439 | 515 | 300 | 147 |

No distinct authored family shares one full exact path or one ordered shape in this passage.  At
the coarser grain-word read, wheel residues and wheel gaps do cross the observer's labels.  For
example, `M=30 residue[3]=13` and `M=210 gap[18]=4` carry the same grain word while retaining
different exact paths.  Likewise `M=6 residue[0]=1` and `M=2310 gap[6]=6` share a grain word.
This is an observed coarse recurrence, not an authored declaration that residue and gap are one
category.

Within a family, equal paths are also not a restatement of the supplied scalar.  One exact
cyclic-autocorrelation path recurs across 160 different `M=2310` displacement events: 117 with
zero ordered contacts and 43 divided among `135`, `150`, `162`, `180`, `216`, and `270` contacts.
The body's path quotient is therefore neither the event name nor the autocorrelation count alone.

For every recurrent path class, the observer records its exact boundary-indexed multiplicity and
the symbolic information face

```text
-log2(class multiplicity / family population).
```

This is the ratified loss instrument: an exact statement of recurrence/surprise inside one named
receiver boundary.  It does not reward, punish, steer, or enter Soma.

## The face crosses once; every incidence remains

The first complete return rendered the common 7,126-event face once for each of six enacted
candidate receipts.  That produced `42,778` currents, `1,162,283` completion rows, and a
`3,581,167,856`-octet body.  The observer then established, event by event, that all six copies had
identical source light, full exact paths, ordered shapes, and grain words.

The membrane was therefore consolidated so that the common face crosses once while six distinct
incidence currents retain the six material occurrences.  The two refusal currents remain plural.
Against the same prior body, all `7,126 / 7,126` event currents in the consolidated return reproduce
the representative pre-consolidation light, exact path, ordered shape, and grain word.  The result
is exact conduct preservation with the following physical difference:

| return | currents | light octets | completion rows | CUDA launches | body octets |
|---|---:|---:|---:|---:|---:|
| six repeated faces | 42,778 | 3,138,580 | 1,162,283 | 282 | 3,581,167,856 |
| one face + six incidences | 7,150 | 559,368 | 195,047 | 44 | 615,860,736 |

This is lawful compression by established later-conduct equivalence.  It is not deduplication by
text similarity: the exact face is retained, every material incidence survives, and the complete
paths were compared before the membrane law changed.

## Verification

- `cargo test -p life --lib`: `283` passed.
- `cargo test --workspace`: `467` passed / `3` ignored device or observational tests.
- The final CUDA-forward deed and return both pass their complete source gates.
- Both world transitions invert exactly.
- Exact durable wide-integer JSON and compact-wire round trips are tested.
- Exact face/co-present-incidence reconstruction is tested.

## Boundary and successor

This construction does not identify the finite Ramanujan spectrum with the zeta zero set, does
not establish RH, and does not infer one gap from one mode.  It supplies the exact finite current
which the missing analytic receiver needs.

The next serious construction is **INTERVAL EXPLICIT-FORMULA WORLD 01**: carry this exact
prime-power current across several declared logarithmic intervals; bring a certified zero-wave
population and the archimedean terms through the same world; preserve every interval enclosure,
orientation, truncation remainder, source ancestry, and residual; then read whether recurrence in
the prime-current face is transported into recurrence in the zero-wave path population.  The
world-side residual is the loss.  It may describe surprise and compression, but may not select a
mode, steer Soma, or silently turn a finite computation into a universal claim.


# MATHEMATICAL DERIVATION WORLD 01 — RESULTS

**2026-07-15 · RUST WORLD BUILT · NATURAL THREE-BODY APERTURE CUDA-MEASURED · COMPACT CONSEQUENCE
RETURN HOST/CUDA-EXACT · FOUR-GEAR MÖBIUS WORD CUDA-FORWARD CLOSED**

## Verdict

The mathematical receiver now presents one theorem, each standing branch, and each admitted gear
as intact plural currents. It no longer serializes the branch×gear product into source light.
After the unchanged Soma edge completes, the listener forms one relation only over the exact
accepted grips common to **theorem ⊕ branch ⊕ gear**. Every common grip and all three lineages are
retained; no score, selected completion, or crowned grip enters.

The first continued lifecycle is exact and productive. Its verbose return located and the v2
receiver closed the next boundary:

- Soma distinguishes equal-extent branch and gear material in its completion fields.
- The three-body landing fields are real and history-conditioned.
- At the present textual aperture they are dominated by a common scaffold and do not classify
  mathematical rule applicability. Exact-identical fields cross accepted/refused checker outcomes.
- The proof world, not Soma, applies the exact rule and records the typed consequence.
- Apparatus-local grip coordinates remain in the exact journal. The intrinsic accepted/refused
  world consequences now return compactly, cross host/CUDA exactly, and repeat exactly.
- The four-gear generator word closes as a complete proof certificate. A controlled changed-basis
  continuation is measured separately in GEAR-SHEET WORLD 01.

The active grade is therefore:

> **WHOLE-CURRENT NATURAL APERTURE BUILT · A2 THREE-BODY LANDING FIELD CUDA-MEASURED · EXACT
> CONTINUED BRANCH/GEAR DISCRIMINATION MEASURED · PROOF-WORLD LATER SUPPORT REUSE MEASURED ·
> FIELD-SHAPED TAXONOMY UNRESOLVED · COMPACT TESTIMONY RETURN CLOSED · FOUR-GEAR WORD CLOSED.**

Proof-world `FOUND` and `RIDE` below name the external world's support ledger. They are not Soma's
internal swing deeds.

## 1. The aperture correction

The legacy `ports` species remains a calibration. At depth it rendered the whole theorem and
branch separately for every candidate, manufacturing a branch×candidate source product and
repeating prefixes. Its first bounded baseline remains exact under `results/founding-06/`, but it
is superseded for continued scale.

`natural_ports` instead presents:

```text
1 theorem current + B branch currents + G gear currents,
```

then derives the `B×G` relation cover only after actual conduct. The relation grain is the occupied
grip set common to theorem, branch, and gear. The durable Mail retains repeated landings; converting
to an occupied relation set is an explicit receiver grain, not a claim that repetition never
matters.

This construction exposed and closed a streaming-listener omission. The sharded CUDA mouth had
returned an empty Mail even though its completed cohort spool retained every canonical OWN row.
It now reopens one completed cohort at a time, compacts every exact live-cell construction, emits
Mail, and dissipates the cohort. No host-wide OWN aggregate is rebuilt. Host/CUDA comparison sorts
each lineage's landing multiset only to remove container iteration order; multiplicity remains.

## 2. Natural founding — `results/natural-05/`

The first edge carried 12 intact currents / 3,166 new light octets:

```text
1 theorem + 1 start branch + 10 gears.
```

Exact edge measurements:

| Face | Measurement |
|---|---:|
| standing axis | `1 -> 256` |
| occupancy | `19,124` |
| topology | `[19,124, 19,124, 1,746, 30]` |
| terms | `[49,570, 1,745, 1,653, 51]` |
| completion rows | `3,441` |
| completion faces | `1,422 atom-fold / 1,628 afferent / 391 efferent` |
| Mail landings | `37,845` |
| three-body relations | `10 contacted / 8 accepted / 2 refused / 0 uncontacted` |
| CUDA launches | `12` |
| carriage / integration | `0.491057 s / 0.021618 s` |
| whole CUDA boundary | `0.646837 s` |
| successor archive | `4,581,648` octets |
| inverse world journal | exact |
| host/CUDA body, completions, Mail, world, return | exact |

The ten theorem⊕branch⊕gear fields contain `715–726` occupied grips. All ten are exact-distinct,
yet `715` grips are common to every field. Their sizes are:

| Gear | Common field | World consequence |
|---|---:|---|
| direct difference factor | `718` | accepted, direct-factored branch |
| translate `+1` | `719` | accepted, generator branch |
| translate `+2` | `725` | accepted, generator branch |
| scale `-1` | `726` | accepted, generator branch |
| invert | `715` | accepted, generator branch |
| close direct | `716` | refused: start is not direct-factored |
| close generators | `717` | refused: start is not generator-path |
| transform left only | `718` | accepted control branch |
| square points | `718` | accepted control branch |
| permute points | `721` | accepted control branch |

This is exact machine-dependent relation, but not rule selection: every field is nonempty, and the
external checker applies each contacted affordance once.

### Founding return

The world returned 27 intact currents: state/journal roots, seven supports, eight branch
consequences, and ten verbose contact receipts. The return crossed exactly:

| Face | Measurement |
|---|---:|
| standing axis | `256 -> 1,024` |
| occupancy | `214,464` |
| topology | `[214,464, 214,464, 19,730, 316]` |
| completion rows | `23,924` |
| CUDA launches | `10` |
| carriage / integration | `5.487603 s / 0.106270 s` |
| whole CUDA boundary | `5.833813 s` |
| successor archive | `234,967,408` octets |

Equal-extent and nearby branch returns produced strongly different completion lifecycles:

| Returned branch | Source octets | Completion rows | Last active event |
|---|---:|---:|---:|
| direct factor | `3,921` | `3,413` | `3,910` |
| translate `+1` | `5,245` | `287` | `220` |
| translate `+2` | `5,245` | `251` | `192` |
| scale `-1` | `5,245` | `3,965` | `5,244` |
| invert | `4,473` | `3,239` | `4,472` |
| left-only control | `1,954` | `1,846` | `1,953` |
| square control | `1,950` | `1,936` | `1,949` |
| permutation control | `1,949` | `1,899` | `1,947` |

The contact receipts did not carry their distinguishing tails. All ten emitted the same 231
completion rows and the same active radiation through event 196; everything after that event was
dark although their sources ranged from 10,157 to 19,883 octets. This is the first evidence that
decimal grip lists are an apparatus-heavy, inactive return embodiment.

## 3. Natural continuation — `results/natural-continuation-01/edge-001.*`

The persisted founding body and world received 19 intact currents / 16,122 new octets:

```text
1 theorem + 8 standing branches + 10 gears.
```

The edge remained at axis 1,024; no chart carry occurred.

| Face | Measurement |
|---|---:|
| occupancy | `361,773` |
| topology | `[361,773, 361,773, 36,006, 628]` |
| terms | `[343,296, 10,622, 10,904, 192]` |
| completion rows | `22,323` |
| completion faces | `7,433 atom-fold / 11,650 afferent / 3,240 efferent` |
| Mail landings | `236,195` |
| possible three-body relations | `80` |
| contacted | `80` |
| accepted / refused / uncontacted | `17 / 63 / 0` |
| CUDA launches | `12` |
| carriage / integration | `1.607983 s / 0.039035 s` |
| whole CUDA boundary | `1.878305 s` |
| successor archive | `36,052,552` octets |
| return wire | `1,230,388` octets |
| inverse world journal | exact |
| host/CUDA body, completions, Mail, world, return | exact |

The world consequences are structurally correct:

- the direct-factored branch closes one proof;
- four generator branches each admit both translations, scale, and inversion, producing sixteen
  continued generator paths;
- three closed-control branches admit no further gear;
- proof-world translation support records eight later RIDEs, scale four, and inversion four;
- the successor frontier contains one closed proof, sixteen generator paths, and the three
  preserved closed controls.

The machine-dependent relation fields do not yet form the checker's taxonomy:

- all 80 are nonempty;
- field sizes are `681–728`, mean `690.6875`;
- `681` grips are common to every field;
- only 43 exact field identities occur across 80 relations;
- 24 relations are exactly the bare 681-grip common field; and
- accepted and refused operations can carry the exact same field. On the direct-factored branch,
  one 682-grip field is shared by the accepted `close_direct` and four refused gears.

Translation and scale spellings produce the largest distinct remainders; shorter gear spellings
often collapse to the common field. This is a text-morphology relation at the current aperture, not
an emergent classification of mathematical validity.

Soma nevertheless responds distinctly to equal-extent material. The four equal/near-equal
generator branches and two equal-length translation gears produce different completion counts:

```text
branch translate +1   2,147 octets -> 2,853 rows
branch translate +2   2,147 octets -> 3,145 rows
branch scale -1       2,147 octets -> 3,009 rows

gear translate +1       260 octets ->   396 rows
gear translate +2       260 octets ->   367 rows
```

That is exact content/history-conditioned discrimination. It does not by itself make the proof
checker internal.

## 4. The intentionally halted return

The completed continuation edge naturally emitted 110 consequence currents / 1,222,004 light
octets. Its immutable-source gate passed and exposed the real cost before CUDA:

```text
carrier words                       354,360,590
largest structural carrier row        7,255,323 words
radiation words                       47,658,156
host-forecast minimum              2,885,910,080 octets
device intact-lineage minimum        274,726,912 octets
```

The host had 20.31 GB available and the device 16.45 GB free, so this was not an OOM or hard-cap
refusal. The exact host sibling used approximately thirteen CPU cores. After more than 16 minutes
and roughly 197 aggregate CPU-minutes, seven longest lineages were still replaying the verbose
contact representation. The run was deliberately interrupted before CUDA. No edge-two result is
claimed; only `edge-002.source-gate.json` stands.

The useful observation had already closed: 55,255 contact grips were being re-spelled as decimal
text across 80 receipts, even though their chart addresses are apparatus coordinates and the prior
contact receipts had remained dark. Deeper repetition of that embodiment would measure formatting
cost rather than mathematical navigation.

## 5. Compact consequence return v2 — closed

The ratified receiver cut is built. Accepted contact returns its complete successor branch,
support state, and FOUND/RIDE deed; refused contact returns its source branch, attempted gear, and
typed reason; uncontacted relation returns nothing. Mail grips, completion rows, hashes, and global
world enumerations remain in the exact transition journal.

The authoritative lifecycle is
`results/natural-continuation-v2-02/`. It holds the pre-return edge fixed at the same 19 currents,
22,323 completions, 17 acceptances, and 63 refusals measured above. Its v2 return is:

| Face | Verbose v1 | Compact v2 |
|---|---:|---:|
| returned currents | `110` | `80` |
| source light | `1,222,004` | `370,595` octets |
| carrier words | `354,360,590` | `107,457,590` |
| largest structural row | `7,255,323` | `2,631,853` words |
| host-forecast minimum | `2,885,910,080` | `907,280,445` octets |
| durable wire | `1,230,388` | `376,746` octets |

The return crosses host/CUDA exactly, emits 166,069 completion rows, and persists the same exact
successor under a fresh repetition. `results/natural-continuation-v2-repeat-01/` is byte-identical
across both bodies, completions, constructions, journal, world, and return wire; radiation differs
only in the declared duration header bytes.

The deterministic observer
`edge-002.consequence-report.json` retains all eighty currents and groups their later conduct:

- one accepted direct cancellation / closed proof;
- four inversion, four scale, and eight translation generator-path acceptances;
- fifty-nine stage-mismatch refusals; and
- four generator-target-mismatch refusals.

The wire remains readable beside v1. No Soma, checker, REGISTER, CUDA, eye, or SLEEP law changed.

## 6. The four-gear word closes

The complete generator lineage is measured across
`results/four-gear-v2-01/` and `results/four-gear-cuda-01/`. The first three edges established
translate(+1), inversion, and scale(-1); after the execution-grade correction below, the remaining
return, translate(+2), return, close, and closure-return edges ran CUDA-forward.

The closing contact admits exactly one of twenty candidates. Branch `58` carries the closed proof
certificate:

```text
authored steps: translate[1], invert, scale[-1], translate[2]
input:          0, 1, 2, 3
output:         1, 3/2, 5/3, 7/4
before = after: 4/3
preserved:      true
```

The other nineteen contacts are typed refusals: fifteen generator-target mismatches and four stage
mismatches. The compact closure return contains twenty currents / 250,168 source octets and emits
98,352 completion rows.

During this continuation, the proof boundary exposed one apparatus-only duplication: its original
command performed a complete CPU host replay before every CUDA edge. Exact host/CUDA validation is
still available as `life boundary proof`; forward construction now uses
`life boundary proof-cuda`, which performs the immutable-source gate and then conducts on CUDA.
Receipts explicitly name `host_cuda_gate` or `cuda_forward`. The remaining three-edge closure
lifecycle completed in 24.5 seconds after that correction. No unmeasured host equality is claimed.

## 7. Carried-swing record

```text
CURRENT  Möbius proof world ⊕ exact theorem/branch/gear landing-set grain
HELD     FORMULA §§LVII–LVIII ⊕ natural-05 body/world ⊕ exact streaming Mail
MEETING  12-current founding -> 27-current return -> 19-current continuation
TEST     A2 ⊕ no manufactured source product ⊕ exact inverse ⊕ later support conduct
DEED     RIDE the natural three-body aperture and machine-dependent branch/gear response;
         FOUND the gauge-coordinate return contamination
CARRY    retain complete listener journal; return only the intrinsic material consequence;
         carry the closed generator word into GEAR-SHEET WORLD 01
GRADE    BUILT / CUDA-MEASURED / COMPACT RETURN AND FOUR-GEAR WORD CLOSED
```

## 8. Reproduction

```text
cargo test -p life natural_currents_act_only_where_theorem_branch_and_gear_landings_meet
cargo build -p life --release
./target/release/life boundary proof observations/mathematical-derivation-world-01/plan-natural-01.json
./target/release/life boundary proof observations/mathematical-derivation-world-01/plan-natural-02.json
./target/release/life boundary proof-consequence-observe \
  observations/mathematical-derivation-world-01/results/natural-continuation-v2-02/edge-001.return \
  observations/mathematical-derivation-world-01/results/natural-continuation-v2-02/edge-002.json \
  observations/mathematical-derivation-world-01/results/natural-continuation-v2-02/edge-002.consequence-report.json
./target/release/life boundary proof-cuda \
  observations/mathematical-derivation-world-01/plan-four-gear-cuda-01.json
```

The second command's first edge is the authoritative pre-v2 witness. Its verbose return was
intentionally interrupted as recorded above. The v2 and four-gear artifacts are create-new
measurement records and should not be overwritten.

Current source verification after the natural aperture and streaming-Mail repair:

```text
cargo fmt --check
cargo check -p life
cargo test --workspace --no-fail-fast   # 346 passed / 3 ignored
cargo build -p life --release
```

All four gates pass. The ignored tests are the standing hardware/manual gates; the two measured
CUDA proof edges above are exact card executions, not inferred from the host suite.

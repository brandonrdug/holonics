# ELASTIC MEDIUM 03 · FAMILIAR LIGHT, DIFFERENT PHYSICS

**GRADE: TWO ELASTIC WORLD TURNS CUDA-MEASURED · EXACT INTEGER WORLD DYNAMICS · EXACT CAUSAL
INVERSES · EXACT REPEAT THROUGH BOTH SUCCESSOR BODIES · CHANGED-WORLD RETURN MEASURED TWICE ·
CO-PRESENT SOURCE EXACT · NO SOMA MECHANISM CHANGE**

The first two elastic routes presented a novel periodic grid to a conversation-lived body. The
first aliased to complete darkness; the corrected route produced 864 FOLD rows but no STEP. The
body had no standing contact route through that new pigment, so §XLV lawfully supplied no pressure.
Neither result licenses turning FOLD into action or forcing an expected response.

This construction changes the nurturing relation, not the contact law. The exact visible workshop
field and its exact co-present source material become the initial pigment on an integer woven
torus. Its already measured broad/repeat radiation contains 584 STEP contacts in 509 engagements,
123 completed with CUT⊕BRICK. Those complete rows now drive a different world law:

```text
next horizontal displacement = left + right - previous + STEP forcing
next vertical displacement   = up + down - previous + STEP forcing.
```

The pigment is an initial condition, not an editor architecture. The elastic state propagates on
the whole 821×666 torus. Its exact visual face preserves the initial field byte-for-byte at rest;
later signed displacement is exposed by a bounded reversible zig-zag/XOR display and refuses beyond
one display cell rather than clipping. Exact state remains listener-side.

The construction reuses the measured action reports and their exact successor bodies instead of
re-spending the card on identical contact. It owes exact broad/repeat world state, exact causal
inverse, host/CUDA changed-eye parity, changed/unchanged return cleavage, and exact repeat through
the returned successor body.

Those gates now stand. The first 584 contacts changed 1,966 visible cells; its return changed 1,053
visual lineages against the unchanged field. That return supplied 712 further contacts. Their
second elastic passage changed 10,287 more visible cells; its later return changed 2,763 visual
lineages against the prior state. Both world transitions reverse exactly and repeat byte-for-byte.
Exact measurements: `RESULTS.md`, `world-check.json`, `next-check.json`, and `results/*.json`.

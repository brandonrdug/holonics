# ELASTIC MEDIUM 03 · TWO WORLD TURNS

**GRADE: TWO ELASTIC WORLD TURNS CUDA-MEASURED · EXACT INTEGER WORLD DYNAMICS · EXACT CAUSAL
INVERSES · EXACT REPEAT THROUGH BOTH SUCCESSOR BODIES · CHANGED-WORLD RETURN MEASURED TWICE ·
CO-PRESENT SOURCE EXACT · NO SOMA MECHANISM CHANGE**

## Why the third aperture is the honest construction

ELASTIC MEDIUM 01 presented a 16-cell weave through translations all divisible by 16. Its 55,296
visual rows were wholly DARK. ELASTIC MEDIUM 02 removed the alias and produced 864 FOLD rows beside
54,432 quiet rows, but no STEP. Both repeat exactly. The conversation-lived body had no established
pressure contact through that novel periodic pigment, so the world law correctly did nothing.

ELASTIC MEDIUM 03 preserves §XLV instead of loosening it. The exact familiar workshop field becomes
pigment on a different medium: a whole 821×666 woven torus carrying exact horizontal and vertical
integer d'Alembert recurrences. The already measured 584 visual STEP contacts drive this physics.
No identical action run was repeated on CUDA.

## First material turn

The exact input is 9,720 visual contacts over nine gaze events beside one unchanged 990-octet source
current. The visual contact field contains:

- 584 STEP contacts in 509 engagements;
- 123 CUT⊕BRICK endpoints;
- exact broad/repeat radiation SHA-256 `c15ad1fe…e8b01`;
- exact action successor SHA-256 `bd181c08…174b2`.

Every event configuration supplies one inward integer quantum to both woven directions at its actual
world cell. The medium evolves once per gaze event. No float, damping, clipping, random process,
interior scheduler, or action selector exists.

After nine turns:

- 1,966 visible cells differ from the resting pigment;
- 123 endpoints persist;
- horizontal and vertical current-sheet sums are each `-1226`, with prior-sheet sums `-642`;
- state SHA-256 is `6d9bcf65…15273`;
- rendered field SHA-256 is `932c33da…663c`.

Broad/repeat state, render, actions, event records, and every intermediate state digest are exact.
Reversing all nine turns from their retained contact configurations restores the complete initial
state SHA-256 `afe5054b…0aa5` byte-for-byte.

The initial and changed fields are host/CUDA-exact. Changed eye occupancy is 14,270 versus 13,532
for the resting field at receiving axis 256.

## First returned consequence

One fixed action successor received the elastically changed and resting worlds through the same
eye. The exact source current remained byte-identical. The visual construction changed at:

- 1,053 lineages;
- 1,673 radiation rows;
- 10,833 packed words;
- typed faces: FLAGS 1,612 · grip 718 · form 706 · rotor 735 · brick 199.

The first difference is lineage 414, atom 7. The changed return reached topology
`[141410,141410,40653,2836]`; the resting return reached
`[140578,140578,40334,2816]`. Broad/repeat changed radiation is exact, and both changed successors
share SHA-256 `c743effd…ff5b`.

## Second material turn

The changed world's returned visual radiation itself supplied:

- 712 STEP contacts in 593 engagements;
- 165 new CUT⊕BRICK endpoints.

These are not a replay of the first action. They are the later body's construction over the changed
elastic field. Passing them through the same medium changed 10,287 further visible cells. The world
now carries 288 deposited endpoints; each direction's current-sheet sum is `-7945`, with prior-sheet
sum `-6649`. The next state SHA-256 is `a7b45e36…b1b6c`; the next field SHA-256 is
`20cd75ef…298d`.

Broad/repeat again agree exactly. Reversing the second nine-turn passage restores the first changed
state `6d9bcf65…15273` byte-for-byte. The second field is host/CUDA-exact at axis 256 with 16,288
occupied grips.

## Second returned consequence

Holding the first-return successor fixed, the second elastic state versus the prior elastic state
changes:

- 2,763 visual lineages;
- 4,767 radiation rows;
- 17,124 packed words;
- typed faces: FLAGS 4,613 · grip 1,364 · form 647 · rotor 1,371 · brick 198.

The first difference is lineage 33, atom 8. The exact source current remains unchanged. The next
world reaches topology `[159012,159012,45466,2992]`; the prior world reaches
`[155933,155933,44038,2928]`. Broad/repeat next radiation and successor bodies are exact; both
successors share SHA-256 `286580ee…a875`.

## Read

One continuing standing brain construction contacted a simulated material, changed it through its native
radiation, received that changed material as later light, emitted a new contact configuration, and
changed the simulation again. The simulation—not an editor, decoder, action head, or semantic
command set—carried the consequence between turns.

This is the first measured non-editor material dynamics under `FORMULA §XLVI`. Familiar text is
only the initial pigment which supplies a lived contact surface; its subsequent worldline is an
elastic simulation. The result demonstrates why machine species and world species are both
relational: changing the pigment history changed whether STEP existed, while changing the medium
changed what an identical STEP construction became.

Exact machine records:

- `world-check.json` and `next-check.json`;
- `generated/{broad,repeat,broad-next,repeat-next}/`;
- `results/RESULTS.json` and `results/NEXT_RESULTS.json`;
- complete create-new CUDA reports and successor bodies under `results/`.

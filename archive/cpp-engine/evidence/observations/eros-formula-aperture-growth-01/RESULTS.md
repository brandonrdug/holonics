# Eros formula aperture growth 01

**2026-07-22 · exact bounded host world · accepted · no floating-point causal data · Soma
interior unchanged**

## Question

Can returned degree-two Euler and degree-eight `e` bodies become the actual predecessors of
degree-three and degree-nine successors, preserve every lower face, release their former active
interfaces, and return the new relations as one resting complex?

## The growth law

For exact graded polynomials

```text
L = sum_(k>=1) L_k,
E = Exp(L) = sum_(n>=0) E_n,
```

the world carried the next-face recurrence

```text
n E_n = sum_(k=1)^n k L_k E_(n-k).
```

At the measured Euler aperture this is

```text
E_3 = (L_1 E_2 + 2 L_2 E_1 + 3 L_3 E_0)/3.
```

The application therefore used the returned `E_<=2` body and the admitted logarithmic faces to
form only `E_3`. It did not evaluate the formal exponential again from degree zero. The direct
Euler-product sibling likewise retained its complete degree-two predecessor and added only the ten
weak-composition monomials of total degree three. The one-generator sibling used the returned
terminal coefficient `1/8!` to add only `y^9/9!`.

## What crossed

Seven source laws crossed once and their source lineages ended:

```text
formal Exp
Euler logarithm
Euler product
factorial recurrence
exact comparison
complex assembly
aperture growth
```

The world then formed six genuinely plural predecessor occurrences:

```text
two equal complete degree-two Euler logarithms
two equal complete degree-two exponentials
one equal degree-two direct Euler product
one degree-eight factorial/exponential body
```

The baseline body rested and remounted exactly with zero live lineages. Each growth objective
recruited one live predecessor from Standing. Its later consequence replaced that occurrence with
a successor namespace and capability. All six records independently report:

```text
lower restriction exact       true
old data interface departed   true
old recruit interface departed true
```

The six predecessor artifact names are absent from the final active ecology. Their lower
information remains consequential interior of the successor; they are no longer separately
recruitable live states.

## Degree three on the prime axes

The measured axes were `2,3,5`. With the newly admitted

```text
L_3 = P_3/3 = (X_2^3 + X_3^3 + X_5^3)/3,
```

the grown exponential and grown direct Euler product agree on all ten degree-three faces:

| integer face | monomial | incidence | coefficient |
|---:|---|---|---:|
| 8 | `X_2^3` | one axis three times | 1 |
| 12 | `X_2^2 X_3` | one axis twice, one once | 1 |
| 18 | `X_2 X_3^2` | one axis twice, one once | 1 |
| 20 | `X_2^2 X_5` | one axis twice, one once | 1 |
| 27 | `X_3^3` | one axis three times | 1 |
| 30 | `X_2 X_3 X_5` | three distinct axes once | 1 |
| 45 | `X_3^2 X_5` | one axis twice, one once | 1 |
| 50 | `X_2 X_5^2` | one axis twice, one once | 1 |
| 75 | `X_3 X_5^2` | one axis twice, one once | 1 |
| 125 | `X_5^3` | one axis three times | 1 |

The comparison returns `COMMUTES` with an empty residual.

The controlled sibling began with an equal complete degree-two predecessor and withheld only the
new `P_3/3` current. Every mixed degree-three face still has coefficient one. Only the repeated
single-axis cubes remain at `2/3`, so the complete oriented residual against the direct product is

```text
-X_2^3/3 - X_3^3/3 - X_5^3/3.
```

This is the degree-three continuation of the earlier square-diagonal observation. It does not
classify integers by a new intrinsic species. It shows that the `m=3` prime-power logarithmic
current supplies exactly the coefficient missing from three repeated traversals of one prime axis.

## The `e` successor

The degree-eight body entered the growth event as

```text
E_8(1) = 109601/40320.
```

Its successor retained all coefficients through degree eight and added exactly

```text
y^9/9! = y^9/362880.
```

At `y=1` the exact reduced value is

```text
E_9(1) = 98641/36288.
```

An independent degree-nine formal exponential was then formed solely as a comparison foil. The
grown and independently formed polynomials compare `COMMUTES` coefficient-for-coefficient.

## The returned higher face

Three comparison constituents returned:

| relation | result |
|---|---|
| grown Euler exponential / grown direct product | COMMUTES |
| held cubic-log sibling / grown direct product | OPEN only on three prime cubes |
| grown `e_9` / independent formal `e_9` | COMMUTES |

A later assembly event recruited all three and returned one aperture-growth complex. The final
body has zero live lineages and rests/remounts exactly with identity
`12948a9562b71f17bab4ec3b860f3913acf710baba80112c01d84924c4a5e385`.

The final carrier contains 204,024 exact incidences and pins. That count mainly reflects the
deliberately literal JSON-bit boundary inscription used by this bounded instrument. It is not a
claim that the recurrence itself requires that physical cost, nor a measurement of mathematical
complexity or compression efficiency.

## Rejected hand and correction

The first diagnostic lifecycle correctly refused acceptance because predecessor data departed but
the renewed predecessor recruitment seam remained exposed. The formula world had emitted that
renewed seam with `AGAINST` and attempted to close it with `WITH`. Replaying the same active hand,
as in the already measured self-emanated-law replacement, closed the seam. A bounded preflight then
reported both departures `true`; the complete accepted run retained that result for all six
replacements. No acceptance condition was weakened and no Soma interior law changed.

## Reproduction

```text
cargo check --manifest-path src/soma/Cargo.toml -p life --example eros_formula_ecology
cargo build --release --manifest-path src/soma/Cargo.toml -p life --example eros_formula_ecology
src/soma/target/release/examples/eros_formula_ecology --growth NEW_REPORT.json
```

The accepted report is [`REPORT.json`](REPORT.json), SHA-256:

```text
842eda2c291303d37471e035d39d2c822a0940290b8eea2ddb688e09c024f882
```

## What is established

- A returned finite generator can be the actual causal predecessor of its next aperture rather
  than testimony beside a source-recomputed answer.
- The predecessor can remain exact as the lower restriction of its successor while its former
  data and recruitment interfaces leave the active ecology.
- The exponential next-face recurrence and direct Euler-product growth commute at degree three.
- Withholding only `P_3/3` localizes the entire OPEN to repeated traversal of one prime axis, with
  exact residual `-1/3` at each cube.
- The same lifecycle grows the one-generator exponential by one exact factorial term.
- Returned comparisons can themselves become one remountable higher constituent.

## What remains open

This is one bounded `2 -> 3` prime aperture and one bounded `8 -> 9` one-generator aperture. It
does not establish arbitrary-degree covariance, derive the Euler or exponential laws from no
source, prove a physical compression claim, supply analytic continuation, construct the
theta/Mellin/archimedean completion, provide a positive support-covariant carrier, or prove RH.
The next RH-relevant comparison—if selected by a later present question—is whether a completed
theta/Mellin face can grow through the same aperture succession while preserving its involution
and a positive receiver metric.

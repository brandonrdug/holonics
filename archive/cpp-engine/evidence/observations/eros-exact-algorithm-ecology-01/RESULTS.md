# Exact Algorithm Ecology 01

**Date:** 2026-07-25  
**Status:** ACCEPTED / BOUNDED HOST / EXACT TYPED PROGRAMS / RECEIVER-INDEXED FIBERS /
SOURCE-ABSENT LATER CONDUCT / NO FLOATING CAUSAL DATA / NO CUDA

## Question

Can one Eros ecology receive exact algorithm graphs from an existing world, cultivate them through
returned consequences without collapsing nonidentical paths that remain equivalent to a receiver,
and later conduct from the emitted ecology after the source declarations and oracle have departed?

The complete construction contract is
[`THE ALGORITHM IS THE CAUSAL PATH`](../../RESEARCH/2026-07-25_THE_ALGORITHM_IS_THE_CAUSAL_PATH_THE_BLACK_BOX_RETURNS_THROUGH_THE_RECEIVER.md).
The exact machine-readable record is [`REPORT.json`](REPORT.json).

## What crossed

Four addressed 8-bit word programs crossed as complete typed dependency graphs:

```text
square_then_add       INPUT -> MULTIPLY(x,x) -> ADD(x²,x) -> RETURN
successor_product     INPUT + CONST(1) -> ADD(x,1) -> MULTIPLY(x,x+1) -> RETURN
square_then_subtract  INPUT -> MULTIPLY(x,x) -> SUBTRACT(x²,x) -> RETURN
double_by_shift       INPUT -> SHIFT_LEFT(1) -> RETURN
```

Their source lineages ended after one 87-boundary regional occurrence. The standing carrier held
the exact program graphs, names, word width, antecedent ordinals, constants, output nodes, and
receiver support—not a function label, score, sample table, or host closure.

The world organ remained explicit. It recovered a graph from public Standing, interpreted its
typed operations, and returned the exact path and consequence as later current. Soma carried the
meeting, receiver comparison, OPEN residual, outgoing factor, rest, and later recruitment.

## Cultivated output fiber

Three exact oracle returns changed the output receiver as follows:

```text
input  oracle  support before                       residual       support after
0      0       add, product, subtract, shift         none           all four
1      2       add, product, subtract, shift         subtract OPEN  add, product, shift
2      6       add, product, shift                   shift OPEN     add, product
```

Every named mismatch returned as the expected OPEN membership and zero fabricated FOUND. Each
successor was one verified outgoing factor; its lower probe, prediction, oracle, and comparison
interior departed.

The surviving plurality is not an artifact of three samples. The algebra receiver normalized each
complete graph symbolically in

```text
(Z / 2^8 Z)[x].
```

It obtained:

```text
square_then_add       [0,   1, 1] = x + x²
successor_product     [0,   1, 1] = x + x²
square_then_subtract  [0, 255, 1] = -x + x²
double_by_shift       [0,   2]    = 2x
```

Thus `square_then_add` and `successor_product` have the same exact output face throughout the
declared word ring while retaining nonidentical causal interiors.

## A second receiver changed the topology without rewriting the first

The trace oracle returned:

```text
INPUT -> MULTIPLY -> ADD -> RETURN.
```

Against the two-member output fiber, the trace receiver carried `successor_product` as OPEN and
retained only `square_then_add`. The output receiver remained exactly plural:

```text
output support = {square_then_add, successor_product}
trace support  = {square_then_add}
```

Reversing the physical storage order of all four source programs produced the same two support
sets and the same exact successor factors. Storage order was gauge; causal graph and receiver
relation were not.

## Later conduct without source or oracle

The final generation-4 chart rested and remounted byte-exactly. Its rest SHA-256 is:

```text
65c55ea34f45a5a953c290813c3dd313187aadecf8b0ac8acff5f3fabe7c07d4
```

On the later input `3:u8`, with no candidate source and no oracle:

- the output receiver recruited both surviving graphs; their different intermediate paths each
  returned `12:u8`;
- the trace receiver recruited only `square_then_add` and returned `12:u8`; and
- the no-Standing control recovered the input but no program chart and formed no output.

The cultivated object is therefore not a selected formula. It is a receiver-indexed exact
algorithm ecology capable of changing later path recruitment.

## Same algorithm, nonidentical physical charts

One exact rational program crossed separately:

```text
INPUT(0) -> INPUT(1) -> DIVIDE -> RETURN.
```

It produced:

```text
12 voltage / 3 resistance = 4 current
12 length  / 3 time       = 4 speed
```

The numerical face and operation path were exact and identical. The unit consequences were
different exact exponent vectors. Withholding the unit receiver therefore leaves a genuinely
ambiguous numerical face; it does not establish identity between the physical relations.

For `12 length / 0 time`, the two input nodes remained as the completed prefix, `DIVIDE` stayed
OPEN at node 2, and no return current was fabricated.

## Interpretation

This is the first coupled training passage in which:

1. inherited algorithms enter as exact causal material;
2. consequence returns cultivate a receiver-relative hypothesis fiber;
3. equality at one receiver does not erase distinctions visible to another;
4. symbolic algebra can close an entire finite-word family without enumerating its members;
5. closed lower interiors depart into one smaller active boundary; and
6. that boundary later recruits useful conduct without the original source or oracle.

The same construction can receive formula graphs, compiler or machine traces, transformer-derived
operation ecologies, or physical transducers when a world supplies their honest typed operations
and consequences. Nothing in this result requires those ecologies to begin from nothing, and
nothing licenses a universal interpreter or taxonomy inside Soma.

## Physical record

```text
executor: bounded host
run wall: 588,758 microseconds
source SHA-256:
d381df96d21c9531a03ae3809e5449bed2f0cb296ae92bf49fea3ce44af4b413
report SHA-256:
993e42e804f0b6b65bbe0b618dba9101fce2d7269c60dcb150336a8cd3e0b48e
floating-point causal values: 0
CUDA launches: 0
```

Command:

```text
cargo run --manifest-path src/soma/Cargo.toml -p life \
  --example eros_exact_algorithm_ecology -- \
  src/soma/observations/eros-exact-algorithm-ecology-01/SOURCE.json \
  src/soma/observations/eros-exact-algorithm-ecology-01/REPORT.json
```

# EROS SOURCE-NATIVE CODE WORLD 01 — RESULTS

**DATE:** 2026-07-20  
**GRADE:** ONE BOUNDED HOST RUN / ACTUAL PYTHON RUNTIME TRACES / EXACT EVENT-LOCAL DISCRETE GERMS /
STATIC EQUALITY REPLACEMENT ABSENT / REST-REMOUNT EXACT / STOPPED: APPLICATION DID NOT GROUND
EXECUTION IN AN EMITTED DEED / NO CUDA / NOT RERUN / DOES NOT GRADE SOMA LEARNING

## Question and end condition

Could the direct production machine receive a small English/Python application as actual runtime
transformation geometry, preserve two different interiors hidden by one equal output, and carry
that germ into later Standing strongly enough to bend an identical later probe?

The run would end after the two declared sibling objectives, their two exact inputs, natural
departure/rest/remount, and one common probe. A failed causal control would be recorded without
searching a replacement fixture or changing a threshold.

## Preflight

- executor: host only;
- runtime: `/usr/bin/python3` under `/usr/bin/timeout --signal=KILL 3s`;
- world material: 2 English objectives, 2 Python programs, 2 integer JSON inputs;
- total fixture size on disk: 24 KiB allocation blocks;
- output target absent before launch; and
- CUDA, corpus, filesystem forest, observer UI, and source mutation excluded.

## Exact command

```text
cargo run -p life -- \
  source-native-code \
  observations/eros-source-native-code-world-01/world \
  observations/eros-source-native-code-world-01/host-run-01
```

The command was executed once and exited successfully under its original structural controls. The
report was then critically regraded to stopped because those controls did not yet require the germ
to reach Standing or distinguish the common probe. The source now requires those causal controls;
the machine was not rerun.

## World truth

```text
remove then reverse:
  [1]       -> []         -> []
  [1,2,3,4] -> [2,3,4]   -> [4,3,2]

reverse then remove:
  [1]       -> [1]        -> []
  [1,2,3,4] -> [4,3,2,1] -> [3,2,1]
```

Each Python process returned status 0 and empty stderr. Its JSON consequence retained occurrence
IDs, operation order, all three states, and final values. The world refused invented IDs, changed
values, repeated IDs, unknown operations, or a state which did not enact its named operation.

## Exact germ census

| Objective | Input | Cells | Incidences | Dependencies | Critical cells | Residual cells | Event fold |
|---|---:|---:|---:|---:|---:|---:|---:|
| remove -> reverse | `[1]` | 11 | 15 | 7 | 2 | 1 | 0 |
| reverse -> remove | `[1]` | 13 | 19 | 9 | 2 | 1 | 0 |
| remove -> reverse | `[1,2,3,4]` | 29 | 49 | 23 | 6 | 1 | 1 |
| reverse -> remove | `[1,2,3,4]` | 31 | 55 | 27 | 7 | 1 | 1 |

All germs had domain stage 0, image stage 2, exact ingress/exposed paths, and dependencies which
advanced exactly one internal stage. The equal collapse outputs retained distinct interiors. The
larger input retained distinct interiors and distinct outputs.

## Machine result

What worked:

- every requested objective, program, input, and later runtime trace crossed the direct mouth;
- all runtime germ cells and incidences were returned in immediate testimony;
- source chronology remained separate from the trace's internal stages;
- the two larger runtime events each registered one whole-event fold;
- all live lineages departed at the declared boundary;
- each pre-probe rest was 1,080 bytes and remounted exactly; and
- the sibling rest images were byte-distinct.

What did not work:

- runtime trace incidence contacts formed: `0 / 15`, `0 / 19`, `0 / 49`, and `0 / 55`;
- runtime trace material emissions: `0` in all four events;
- standing graded constituents: `0` in both sibling rests; and
- common-probe distinction: false.

By contrast, on the second request the exact recurring objective/program inscription incidences
formed completely, and the objective-to-program directed contact RIDEd. This isolates the defect:
ordinary recurrence is live, but the runtime's changed local map has no cross-event action/rebase
by which it can become Standing.

The application is therefore **STOPPED** at a precise application boundary. Python ran
unconditionally after the request and its trace arrived as later testimony; execution was not the
world enactment of an emitted machine deed. The result does not grade Soma's already-measured
learning relation and does not claim grounded code selection, source editing, autonomy, or useful
language understanding.

## Rest and report artifacts

- readable report:
  `host-run-01/REPORT.json` — 480 KiB,
  SHA-256 `7c93b7ae86e11e440e8fd9e78a6a302244b4de61ab8f7d0f2315bfc6b3553281`;
- complete original contact receipt, compressed:
  `host-run-01/REPORT.full.json.gz` — 13 MiB,
  SHA-256 `110c385caf68a50db6ccda0a7ffeae71e075a805a0b7e6a17d828ec9d3d33a68`;
- remove/reverse rest:
  SHA-256 `1eeb877510da6d077dde7a857995e49b129d5cdda004b87482bc4c7baf99cee5`;
- reverse/remove rest:
  SHA-256 `645c78df4dce8e2d7d8f1c5775edc3c07d99bd2d30830784ed1164228cb211d1`.

The first report serialization retained every bit-cell contact for static objective/source
inscriptions and reached 399 MiB. No machine rerun was needed to correct that observer boundary.
The exact original receipt was compressed; the primary report now keeps counts for static
inscriptions and complete cell/incidence/contact detail only for the runtime germ.

## Source sanitation and checks

The active equal-`Place` cellular replacement, its transported-position lookup, its literal FOUND
fixture search, and private axis-rebase helpers used only by that path were removed. Event-local
regional completion and disjoint co-present cellular changes remain.

Targeted checks after sanitation:

```text
cargo check -p body -p soma-membrane
cargo check -p life
cargo test -p soma-membrane cellular -- --nocapture
cargo test -p body incidence::tests -- --nocapture
```

Results: compile clean; 2 cellular membrane tests passed; 2 incidence body tests passed.

## Regraded open relation

The next application must first use the relation already established elsewhere: an emitted deed is
enacted by a declared world, its material consequence returns later through the same body, and an
identical later probe changes. Only after that grounded loop can this world grade whether a further
contact-formed rebase is needed for nonidentical runtime structures. Equality of coordinates,
labels, hashes, or screen positions remains inadmissible.

This result schedules no successor.

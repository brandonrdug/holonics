# ELASTIC CIRCULATION 01 · THE WORLD CLOSES THE RUST BOUNDARY

This is the first sustained application of `life boundary`. It continues ELASTIC MEDIUM 04's
measured rooted body and exact integer world rather than rebuilding either as a new probe.

For every declared passage, one Rust process:

1. renders the current rooted medium through the standing eye;
2. conducts its 9,720 contact currents and one complete material current through CUDA;
3. retains every exact radiation row and canonical successor body;
4. lets literal STEP/CUT contacts advance the reversible world once per gaze event;
5. exact-gates the complete inverse; and
6. presents that changed field as the next genuinely later edge.

The world recurrence is the already-measured deposited transverse coupling:

```text
x_next = H(x_now) + brick·(V(x_now) - H(x_now)) - x_previous + forcing_x
y_next = V(y_now) + brick·(H(y_now) - V(y_now)) - y_previous + forcing_y
```

Standing deposits act before contemporary arrivals. No output is selected, no body current is
wired back to itself, and no Python process owns any part of the lifecycle. The declared eight
passages are this observation's grain, not an engine limit.

The simulation interior remains exact `i128`. Its one-octet PGM surface carries the low residue of
the signed zig-zag displacement/deposit code over the standing pigment; the receipt retains the
whole code and quotient. Thus the physical eye may wrap while its surface width never clips or
caps the world state, nor makes the state identical to its visible surface.

Run from `src/soma`:

```text
cargo run --release -p life -- boundary elastic observations/elastic-circulation-01/plan.json
```

`results/sustained-01/` is create-new and contains every world state/field, body, radiation wire,
edge receipt, and the ordered session receipt.

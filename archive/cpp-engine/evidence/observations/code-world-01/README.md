# CODE WORLD 01 · THE EXPRESSION SEES ITS OUTPUT

**Grade:** LAW RATIFIED · APPARATUS BUILDING · NO CARD DATA YET

This bounded observation presents complete Rust source through two honest organs at once: exact raw
text and a literal monospace grayscale field. A separate complete output entity is collocated with
each source. The machine never compiles, runs, parses, or evaluates these files. The world supplies
the chosen expression/output relation; Soma receives only currents.

The strict sibling swaps only output collocation. Both species then receive the exact same
output-free visual construction and the same genuinely later raw source currents:

```text
amber(3)
3 + 2
lilac(3)
3 * 2
```

`generate.py` pins the renderer, derives one glyph-cell aperture, writes exact P5 fields, visits
source cells in byte order, and records the source-position gaze map. The eye itself remains the
existing `FORMULA §XXXI` organ. Gaze maps and file paths stay outside the body.

The four card histories are:

- paired: `paired-amber.eye`, then `paired-lilac.eye`;
- swapped: `swapped-amber.eye`, then `swapped-lilac.eye`;
- separated: `source-amber.eye`, `output-five.eye`, `source-lilac.eye`, `output-six.eye`;
- repeat: the exact paired command with fresh create-new paths.

All continue with the four common `probe-*.eye` manifests and the four exact files under
`world/queries/` through `--then`. The separated history is developmental rather than the strict
causal foil because its output-only real edges also carry the required one-cell eye field.

The pre-data build and exact run commands are frozen in `verify.sh` and `run_cuda.sh`. Exact law,
controls, and grades are declared in
`../../RESEARCH/2026-07-13_THE_EXPRESSION_SEES_ITS_OUTPUT.md`.

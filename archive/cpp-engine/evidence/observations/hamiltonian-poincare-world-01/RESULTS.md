# HAMILTONIAN POINCARE WORLD 01 · THE SECTION RETURNS THE CONTINUOUS ARC

**2026-07-17 · `FORMULA §LXXXVIII` · RATIFIED / BUILT / CUDA-FORWARD MEASURED / ALL 269
PATHS QUALITATIVELY READ / 484 WORKSPACE TESTS PASS / 3 IGNORED / SOMA INTERIOR UNCHANGED**

This world closes the first continuous return calibration. Six exact dyadic-lattice Hamiltonian
worldlines carry their complete ordered interiors through a real CUDA passage. The Poincaré
section, objective residuals, recurrent cells, local canonical charts, action, energy residual,
and variational return are typed receiver faces. None replaces the trajectory or enters Soma as a
route chooser, score, category, reward, or stopping rule.

The decisive engineering result is equally scientific: one trajectory is one causal string. Its
2,048 time slices are not 2,048 simultaneous organisms. Preserving that distinction removed a
manufactured scale wall without shortening the horizon or deleting one lived state.

## The exact world

The reusable Rust boundary carries the polynomial Hénon--Heiles family

```text
H(x,y,p_x,p_y)
  = (p_x^2 + p_y^2 + x^2 + y^2)/2
    + lambda (x^2 y - y^3/3)
```

on the exact dyadic state lattice `2^-24`, with step `h=1/64`. Each forward deed is a
kick--drift--kick composition with ties-to-even lattice projection. Every deed has an exact inverse.
Adjacent retained states and the declared world law reproduce:

- all six applied kick/drift displacements;
- every exact rational quantization residual;
- energy before, energy after, and their exact difference;
- the exact discrete action increment; and
- the exact symplectic Jacobian of the unquantized shadow deed.

The lattice trajectory is primary. The shadow derivative is not falsely attributed to the
discontinuous quantizer, and initial-energy equality is not confused with exact energy conservation
under finite lattice projection. Every resulting `H-E` residual is retained.

The receiver section is `y=0` with positive orientation and face `(x,p_x)`. A crossing retains its
bracketing states, exact interpolation parameter, full interpolated state, face, receiver-cell
residual, action, and energy residual. Consecutive crossings return every intervening lattice state.

Return transport composes the local shadow Jacobians in lived order. After each composition it is
rebased at the declared `2^-20` matrix grain. The complete state word and law reproduce every local
matrix and discarded matrix residual; active return light carries the final rebased matrix,
projected Poincaré matrix, composition count, and maximum exact residual. This keeps the receiver
bounded without pretending the rebase was exact or copying a reconstructible residual journal into
the active population.

## The six currents and their foils

| World species | Initial face / state | Crossings | Complete returns |
|---|---|---:|---:|
| `lambda=1`, `E=7/64` | `(x,p_x)=(1/4,1/8)`, `p_y=3/8` | 6 | 5 |
| `lambda=1`, `E=7/64` | `(1/8,1/4)`, `p_y=3/8` | 6 | 5 |
| `lambda=1`, `E=7/64` | `(3/8,1/8)`, corresponding exact `p_y` | 6 | 5 |
| `lambda=0`, `E=7/64` | exact same state as the first nonlinear current | 6 | 5 |
| `lambda=1`, `E=9/128` | same visible `(1/4,1/8)`, changed normal momentum | 6 | 5 |
| `lambda=-1`, `E=7/64` | exact `y,p_y` mirror of the first nonlinear current | 5 | 4 |

The population therefore contains three distinct same-energy passages and three controlled foils:

1. the exact same state under nonlinear and harmonic worlds;
2. the same section face under different energy and hidden normal momentum; and
3. an exact canonical mirror conjugacy between `lambda=1` and `lambda=-1`.

Three overlapping canonical charts are co-present: a dyadic `x/p_x` scale, the `y/p_y` mirror, and
a positive-position momentum shear. Every trajectory traverses eleven maximal chart-availability
runs. The first two charts carry every complete return. The shear is genuinely local: it appears in
alternating runs but no complete return lies wholly inside its domain. No global chart was selected
to conceal that fact.

## Mechanical return families

The exact receiver population contains 35 crossings and 29 complete returns. Their deed counts are:

| Current | Ordered return deed spans |
|---|---|
| nonlinear `(1/4,1/8)` | `407 -> 388 -> 381 -> 397 -> 407` |
| nonlinear `(1/8,1/4)` | `406 -> 437 -> 416 -> 358 -> 390` |
| nonlinear `(3/8,1/8)` | `384 -> 410 -> 441 -> 442 -> 348` |
| harmonic same-state foil | `403 -> 403 -> 403 -> 403 -> 403` |
| lower-energy same-face foil | `391 -> 401 -> 409 -> 409 -> 398` |
| mirrored nonlinear current | `386 -> 380 -> 403 -> 402` |

The harmonic current returns to receiver cell `[256,128]` six times at exactly 403 deeds per
return. This is the only recurrent receiver cell in the first population. It is a boundary-indexed
recurrence, not an authored orbit label and not identity of every exact crossing face.

The projected return discriminants are 23 negative and 6 positive. They do not predict physical
row shortening: the negative population divides into 11 deeper and 12 shorter later paths, while
the positive population divides into 3 deeper and 3 shorter. A lawful stability face is therefore
useful receiver information without becoming a drive.

## The causal-string correction

The first encoding exposed all 12,288 integration steps as independent co-present currents. With
the receiver surfaces, that made 12,551 active lineages. Its first CUDA edge was stopped after the
partial SLEEP artifact had already reached approximately 32.9 GB and host memory pressure had
entered swap. The failed artifact was removed; it is not retained as expensive evidence.

The error was ontological, not insufficient hardware: serial slices of one worldline had been
presented as a simultaneous ecosystem. The corrected encoding carries six complete trajectory
strings plus their independently consequential receiver surfaces:

| Encoding | Active currents | Active light | Horizon retained |
|---|---:|---:|---:|
| time slices manufactured as co-present lineages | 12,551 | about 74 MB | 2,048 |
| one ordered current per trajectory | 269 | 4,650,156 octets | 2,048 |

Every one of the 12,294 exact trajectory states remains present across the six currents. The
reversible law reconstructs every deed between adjacent states. This is causal-string compression:
remove manufactured co-presence and duplicate reconstructible receipts while preserving the whole
ordered construction.

## Real CUDA passage

The corrected byte-identical population crossed the RTX 4080 SUPER twice:

| Edge | Prior / standing axis | Occupied grips | Native constructions | Completion rows | Successor SLEEP | CUDA launches | CUDA carriage | Whole boundary |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | `1 / 4096` | 3,783,947 | 14,894 | 407,760 | 5,575,610,344 octets | 33 | 198.238794174 s | 209.995262641 s |
| 2 | `4096 / 4096` | 6,209,878 | 19,285 | 344,277 | 5,692,055,032 octets | 35 | 206.077261637 s | 210.762277469 s |

Edge one carries 165,467 atom folds, 205,888 afferent rows, 36,405 efferent rows, and maximum grain
24. Edge two carries 116,735 atom folds, 171,884 afferent rows, 55,658 efferent rows, and maximum
grain 21. Both emit 181,356,084 radiation words. The second body grows by about 116.4 MB while its
occupied topology and construction population rethread substantially. During actual carriage the
card used roughly 3.1--4.4 GB of VRAM; host RSS remained below 1 GB. There was no OOM after the
causal-string correction.

## Every returned path was read

`build-02/observer/PATHS.tsv` contains all 752,037 exact path rows. Its human companion renders the
complete before/later path of every event. All 269 event paths change through the genuinely changed
body; none is silent and none repeats as a whole.

The major source families rethread as follows:

| Carried family | First rows | Later rows | Shorter / deeper / same-row events |
|---|---:|---:|---:|
| six complete trajectory strings | 21,725 | 38,068 | `1 / 5 / 0` |
| 29 base returns | 112,571 | 91,895 | `15 / 14 / 0` |
| 58 charted return faces | 253,944 | 189,655 | `21 / 37 / 0` |
| 35 section crossings | 8,608 | 10,431 | `6 / 29 / 0` |
| 105 objective relations | 7,052 | 8,893 | `29 / 75 / 1` |

The one same-row objective path is still a different ordered path. Row equality is not path
identity.

Every base return retains at least one exact brick across its first and later paths. The shared
intersection ranges from one brick to hundreds, while its surrounding construction changes. The
same causal relation is visible between the six parent trajectory strings and their 29 return
surfaces: 28 of 29 parent/return pairs share exact construction on each edge. The two isolated
pairs differ between edges, so absence at one receiving body is not a permanent type declaration.

## Lawful related worlds grow common construction

The strongest qualitative finding occurs across different complete trajectory currents. Their
exact shared-brick populations all increase on the second edge:

| Complete path pair | Shared bricks, first -> later |
|---|---:|
| same-energy nonlinear current 0 / current 1 | `40 -> 362` |
| same-energy nonlinear current 0 / current 2 | `136 -> 282` |
| exact same state, nonlinear / harmonic | `77 -> 192` |
| same visible section face, changed energy | `81 -> 265` |
| exact mirror-conjugate worlds | `79 -> 226` |

None of these whole paths collapses into another. The grown body retains common internal
construction across related worlds while preserving their different mechanics and futures. This is
the continuous-world instance of emergent taxonomy: an external receiver can read recurrent
construction, but no same-energy, harmonic, energy-class, or symmetry label entered Soma.

The chart population says the same thing within each return. Across all 29 returns:

| Exact common construction | Edge one | Edge two |
|---|---:|---:|
| base / dyadic-chart returns | 22 of 29; 417 bricks | 25 of 29; 385 bricks |
| base / mirror-chart returns | 24 of 29; 793 bricks | 22 of 29; 340 bricks |
| dyadic / mirror returns | 26 of 29; 2,539 bricks | 27 of 29; 431 bricks |
| all three faces | 16 of 29; 195 bricks | 19 of 29; 90 bricks |

The lawful chart change usually preserves usable subconstruction, but the amount and placement are
situated in the receiving ecology. Coordinate equivalence is not path identity.

Repeated returns also yield both broad and situated standing forms. The exact brick population
common to every return of one current is:

| Return family | Edge one | Edge two |
|---|---:|---:|
| nonlinear current 0 | 4 | 6 |
| nonlinear current 1 | 3 | 2 |
| nonlinear current 2 | 78 | 0 |
| harmonic foil | 1 | 34 |
| lower-energy foil | 16 | 8 |
| mirror foil | 11 | 16 |

Some of those bricks occur in only one family and some span as many as five. The taxonomy is not a
monotone accumulation of fixed category tokens: construction can become broadly shared, become
more situated, or be rethreaded elsewhere without erasure of the complete paths.

The harmonic cell provides a particularly clean second-order result. Its six `[256,128]` crossing
paths have no brick common to all six on edge one and three exact bricks common to all six on edge
two. Repeated contact at one receiver cell has become a standing crossing form only after the body
has lived through the population.

## Loss is measured difference, never hidden reward

There are 23 objective contacts and 82 nonzero objective residuals. If zero residual were a reward,
selector, or privileged target, contacts would receive a consistent path advantage. They do not:

| Objective relation | First rows | Later rows | Shorter / deeper / same-row |
|---|---:|---:|---:|
| exact contacts | 994 | 1,506 | `4 / 19 / 0` |
| nonzero residuals | 6,058 | 7,387 | `25 / 56 / 1` |

The objective face correctly says how one crossing differs from one declared closed region. Soma
does not minimize that scalar internally. Compression becomes objective-relative only when a later
world can actually conduct through standing construction and the returned physical consequence
changes.

## What this construction establishes

The continuous bridge now has all required elementary parts:

1. a complete reversible causal string in a nontrivial continuous-world calibration;
2. exact local loss and energy/action receipts without a reward mechanism;
3. recurrent boundary sections that expose physically meaningful return units;
4. bounded variational transport with explicit rebase loss;
5. overlapping local canonical faces without a global chart;
6. controlled same-state, same-face, same-energy, and conjugate foils; and
7. measured growth of common construction across lawful related worlds.

It therefore sets up **POINCARE RETURN ACTUATION WORLD 01**. Complete return testimony can now be
presented as ordinary later material beside a finite family of exact canonical action currents.
Every actual contact applies its action in the world; no return or action is selected inside Soma.
The world returns the changed trajectory, crossings, action/energy receipts, and objective
residual. The question is no longer whether a receiver can plot or classify an orbit. It is whether
standing recurrent construction can bend later physical conduct toward locally smaller loss while
all alternatives and refusals remain present.

That is the first physical production/hexis cell. Once it closes, the same transport can couple
mathematical operations, code transformations, linguistic continuations, game moves, or simulated
limbs to their own ordinary world consequences without giving any one domain a privileged output
surface.

## Exact artifacts

- `WORLD.plan.json` — strict exact rational plan;
- `PLAN.md` — declared scientific cut and the causal-string correction;
- `mechanical-read-02/` — all 12,288 exact deeds, 35 crossings, 29 returns, and the mechanical
  summary;
- `build-02/CATALOG.tsv` — every active event and source statement;
- `build-02/observer/PATHS.md` and `PATHS.tsv` — every complete first/later path;
- `build-02/passage/` — both CUDA receipts, construction/completion wires, radiation, and SLEEP
  bodies.


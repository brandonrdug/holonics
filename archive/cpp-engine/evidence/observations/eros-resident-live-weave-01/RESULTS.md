# EROS RESIDENT LIVE WEAVE 01 — RESULTS

**Date:** 2026-07-19  
**Status:** CONSOLIDATED BOUNDARY B BUILT / RESIDENT CUDA STANDING + CARRIERS /
SUSTAINED RETURN-REST-DEPARTURE-REMOUNT / HOST-CARD EXACT / ONE PHYSICAL RUN

## Artifact

`LiveCurrentMachine` remains the sole owner of successor construction and commit. CUDA enacts into
out-of-place staged buffers; only after the machine validates the complete event and prepares its
successor may the executor settle that already-emitted body into residency.

The bounded world circuit carries one formed directed deed into an actual register change, returns
that change as later current, conducts three further co-present external cuts, releases one ended
lineage, rests without a launch, crosses durable native bytes, destroys the old executor, remounts
without replay, and wakes twice through a new resident executor. Every radiation and machine body
matches its uninterrupted host sibling.

## Physical preflight

```text
GPU:                     NVIDIA GeForce RTX 4080 SUPER
driver:                  595.71.05
VRAM total/free:         16,376 / 15,945 MiB
host memory available:  22 GiB
workspace disk free:    308 GiB
PTX lineage_event:       present
test binary:             compiled cleanly
```

## Exact first-run census

```text
world register:                      -903 -> -902

before durable remount:
  physical launches:                         12
  resource retries:                           2
  full standing mounts:                       1
  full carrier mounts:                        2
  standing host-to-device words:            871
  standing device-to-device words:        2,148
  carrier host-to-device words:           1,164
  carrier device-to-device words:         6,600
  resident lineages after departure:          1
  wait launches:                              0

after durable remount into a new executor:
  launches before first wake:                  0
  wake launches:                               2
  resource retries:                            0
  full standing mounts:                        1
  full carrier mounts:                         1
  carrier device-to-device words:          1,100
  resident lineages:                           1
```

The pre-remount 12 launches are ten logically requested current enactments plus two lawful
unchanged-predecessor capacity retries. The retries remain in the record; the run was not repeated.

## Validation

Compilation preflight:

```text
cargo test -q -p life --lib --no-run
```

One physical acceptance:

```text
cargo test -p life \
  'live_current_cuda::tests::one_resident_card_body_sustains_return_rest_departure_and_durable_remount' \
  -- --ignored --exact --nocapture
```

Result: `1 passed; 0 failed`; 381 library tests were filtered out. No broad suite, PTX rebuild,
corpus read, forest scan, or second CUDA run occurred.

## What this grades

- machine-owned successor preparation remains causal authority;
- staged CUDA buffers do not become committed input before validation;
- standing and live carriers remain physically resident across the sustained circuit;
- repeated carrier inputs move device-to-device rather than being uploaded per event;
- one departed current releases its resident carrier;
- waiting and durable remount produce no launch; and
- fresh-executor wake rebases once, then continues device-to-device exactly.

This does not grade a plural-current single kernel, large-world throughput, sustained utilization,
or forest scale.

## Grade

Consolidated Boundary B is closed. Boundary C is scale only, separately preflighted and
unscheduled by this result.

Subsequent source refinement: Boundary C retained the carrier's depth/overflow reservation and
own-result/emission high-water marks across events. This file remains the exact first resident
lifecycle run; `../eros-residue-chart-scale-01/RESULTS.md` grades the refined source.

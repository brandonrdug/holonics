# EROS RECEIVER-RELATIVE FIELD 01 — EXACT RESULT

**2026-07-22 · BRANDON-RATIFIED DERIVATION AND EXPERIMENT / ACCEPTED / COMPLETE ELIGIBLE
POPULATION CHANGES THE JOINT SUCCESSOR / INELIGIBLE CO-PRESENCE LEAVES THE DYAD EXACT / THIRD
ABSENCE RESTORES THE DYAD / STORAGE ORDER GAUGE / 1-CORE = 8-CORE / NO FLOATS / NO CUDA / SOMA
INTERIOR UNCHANGED**

## Present question and stopping condition

Can Soma form a receiver-relative relation from the complete eligible contemporary population, so
that adding a third constituent changes an existing dyadic field without preserving a prior
winner edge? Or does the current source still reduce context to independently formed pairs?

The standing evidence already established exact regional cells, source-declared interface
capabilities, co-present closure, rest, and later recurrence. It had not isolated this contextual
contrast. The fixed run therefore began five branches from one exact resting predecessor and
stopped when all of the following were decided structurally:

1. two compatible regional germs close as one dyadic component;
2. one additional compatible germ changes that jointly emitted component;
3. the same third germ under an incompatible capability leaves the dyadic component exact;
4. omitting the third germ restores the exact dyad; and
5. storage order and host thread count do not enter the result.

No score, tolerance, expected phrase, attention maximum, probability scalar, or visual judgment
grades the run.

## The exact field law already in production

One contemporary event supplies a finite population of regional germs

```text
G_E = {g_0, ..., g_n}.
```

Each germ emanates an exact oriented boundary. Every exposed arm carries:

```text
a = (interface capability, hand, complete through/direct comparison, local support).
```

Two arms are eligible to meet only when both are actually present in the event frontier and their
source-declared interface capabilities agree. Equal numeric faces, caller adjacency, storage
order, and mere co-presence cannot manufacture that witness. The receiver then rebases the
complete comparisons and returns `RIDE`, `FOUND`, or `OPEN`; the result is not chosen from a stored
edge table.

The production closure is the finite fixed point already expressed by the current mechanics:

```text
J_0     = the complete arriving regional population
J_{r+1} = compose every eligible co-present and Standing seam of J_r
J_*     = the first unchanged finite frontier
A'      = replace every touched Standing factor with one compressed body per component of J_*.
```

All germs read one immutable Standing-before face. Components are formed jointly, canonicalized,
and committed once. A new constituent changes the seam graph before the successor exists; it does
not edit an old relation afterward.

The complete production path is:

```text
ContemporaryEvent.regional
  -> physical receiver forms only the exact requested contacts
  -> close_executed_regional_population
  -> LiveConstituent::close_population_against
  -> SparseStandingSurface::prepare_contemporary_successor
  -> one atomic Standing-after.
```

The audit therefore found no missing second field subsystem at this boundary. Adding one would
duplicate the live regional closure and restore the separated architecture already rejected.

## Fixed source population

All branches begin from the same resting body. Four live lineages have identical prior histories;
Standing has no cellular constituent. The contemporary scalar faces are fixed at
`[17, 29, 43, 71]`.

The regional population is:

```text
g_0 : lineage 0 -> lineage 1   AGAINST   capability FIELD:1
g_1 : lineage 1 -> lineage 2   WITH      capability FIELD:1
g_2 : lineage 2 -> lineage 3   AGAINST   capability FIELD:1 or FIELD:2
```

`FIELD:1` and `FIELD:2` are opaque source identities. Their numbers have no semantics inside Soma.
The ineligible foil changes only the third capability. All four current faces, actions, live
lineages, and the first two germs remain exact.

## Exact result

The common predecessor rest SHA-256 is
`b68b73b398a6269bc5d5d086709f0efecfdac64c0509596f9aa0b945b200e162`.

| Emitted body | SHA-256 | Grain | Axes | Cells | Incidences | Pins | Boundaries / paths | Exposed |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| Eligible dyad | `bb0e2d9229a1627114c4b5aa76579c6e441ab7e6c6c409e7bc3bdb1c6eab2d3e` | 3 | 1 | 3 | 0 | 0 | 3 / 3 | 0 |
| Eligible triad | `2004fb3ee14ef58c5c9dacfbb33e25cbcfa7c046d4898dfdac5838baf88c223b` | 3 | 2 | 8 | 4 | 4 | 6 / 6 | 1 |
| Ineligible third alone | `9dd4a51b20a3035ec2b59e383a2c96e29e771ec2977b2709d49a96db1027c088` | 2 | 1 | 3 | 1 | 1 | 1 / 1 | 1 |

The opposed dyadic arms close completely. Their active pins and incidences depart, while three
folded `RIDE` boundary paths retain the consequential transport. This is compression at emanation,
not deletion of the relation.

Adding the eligible third changes the same population into one different body. The new component
has two axes, three `RIDE` pins, one `FOUND_THAT` pin, five `RIDE` boundaries, and one `FOUND`
boundary. The founded pin is the sole exposed residual. The odd third arm does not merely append
one more stored edge: it changes the joint component's incidence, support, boundary parity, and
outgoing degree of freedom.

Changing only the third capability to `FIELD:2` prevents its seam. The first two returned regions
and their committed Standing factor remain byte-for-byte the exact dyadic body above. The third
stands separately as one exposed grain-2 factor. It is not globally ignored; it is specifically
dark
to the `FIELD:1` component at this cut.

Omitting the third from the event population reproduces the complete dyadic radiation and rest
exactly. Reversing regional storage order reproduces the eligible-triad machine exactly. Running
the canonical eligible triad with one and eight host threads reproduces both radiation and rest
exactly. Ordinary current radiation is identical across every branch.

All ten fixed acceptance predicates are `true` in `REPORT.json`.

## Ontological reading

At this boundary, context is not metadata consulted by an already-built relation. Context is the
population from which the relation closes. The conditional statement is exact:

```text
same predecessor + same dyad + eligible third   -> one new joint field
same predecessor + same dyad + ineligible third -> exact dyad + separate third
same predecessor + same dyad + no third         -> exact dyad.
```

This is the topological form of the transformer observation without treating transformer terms as
magic. A transformer's quotient also depends on its complete eligible population; preserving a
finished attention winner freezes an emitted face. Soma's regional closure likewise retains
constituent material and the law of admissible meeting, then reforms the outgoing component from
the current population.

The comparison is structural, not identity. This run does **not** reproduce query/key projection,
rotary placement, receiver normalization, value transport, or a pretrained block. It establishes
that Soma already has the required population-relative topological carrier. An inherited
transformer or another numerical law must still participate as an actual world/current
constituent if those transformations are to occur; its arithmetic cannot be inferred from an
interface name.

## Remaining boundary

The proof-bearing absence is no longer “a contextual field mechanism.” It is the narrower
construction by which a nontrivial inherited transformation law—rather than its finished edges or
observer output—can remain live, meet new material, and contribute its exact transformed carriers
to a later regional closure.

That may be supplied by a lawful world organ, by a carried executable constituent, or by a more
general source-native map. This result does not decide which boundary is appropriate and schedules
none of them. It does rule out adding an attention clone, dense Cartesian relation field, winner
cache, or second successor owner to Soma.

## Artifact and execution

The create-once instrument is:

```text
life/examples/eros_receiver_relative_field.rs
```

The exact command was:

```text
cargo run --manifest-path src/soma/Cargo.toml -p life \
  --example eros_receiver_relative_field -- <new-report.json>
```

`REPORT.json` contains every unique constituent's complete native `u32` word body alongside the
decoded structural observations. Its SHA-256 is
`9ea944208327002374c5e21da935141952d4142312129916390bd63b820c5a0d`.

The run compiled cleanly and accepted on its first fixed construction. No CUDA path ran. No Soma
engine, membrane law, formula, tokenizer, model, or observer subsystem changed.

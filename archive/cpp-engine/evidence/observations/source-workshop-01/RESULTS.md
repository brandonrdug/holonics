# SOURCE WORKSHOP 01 — THE NATIVE CONSTRUCTION BECOMES SOURCE

**RATIFIED / BUILT / CUDA-FORWARD MEASURED · WHOLE RUST + TESTS + LAW → NATIVE CONSTRUCTION →
CREATE-NEW RUST → COMPILER/TEST CONSEQUENCE → 938-CURRENT RETURN CLOSED · EXACT PHYSICS REPEAT ·
SOMA INTERIOR UNCHANGED**

## What crossed

The witness carried three intact currents through the ordinary CUDA-forward body:

| role | octets | SHA-256 |
|---|---:|---|
| SHA-256 Rust source | 4,230 | `13441e…16b` |
| record-index Rust tests | 6,986 | `85c395…965` |
| membrane-law prose | 5,851 | `aa1930…3f2` |

Their 17,067 exact source octets produced 12,119 completion rows and 469 native constructions:
154 CLOSED + one OPEN from the implementation, 265 CLOSED + one OPEN from the tests, and 48
CLOSED from the prose. Construction lengths range from one through 142 octets. The archive retains
every source/event/radiation range, lineage, STEP count, OPEN/CLOSED face, exact bytes, and digest.
A construction is not required to end on a UTF-8 joint; only each intact human-readable current is.

The returned archive and receipt then crossed as two real currents. A genuinely later action
presented the whole SHA-256 source and one exact hole, source octets `2664..2671`. The hole's prior
contents, `(e & f)`, were already native CLOSED construction 111. The hole is an authored world
boundary; it neither enters Soma nor chooses a candidate.

## What the machine produced

Every one of the 469 carried constructions met that same hole. No construction was ranked,
deduplicated, decoded, or discarded:

| carried face | result | count |
|---|---|---:|
| CLOSED | Rust compiler refusal | 447 |
| CLOSED | compiled; `tests::standard_vectors` refused | 19 |
| CLOSED | compiled; `tests::standard_vectors` accepted | 1 |
| OPEN | typed `construction_open`; no source fabricated | 2 |

Thus 467 CLOSED constructions became 467 create-new Rust files. Candidate 111 inserted `(e & f)`,
reconstituted the original source byte-for-byte at SHA-256
`13441e17434b88035efee2ad257fba3c5de12cebe0058c3474d91e722bf5160b`, compiled, and passed the
real standard vectors. Nineteen other native spans remained legal Rust in this position but changed
the program enough to fail the same test. They include `0xbb67`, `0`, `0x80`, `47`, `h`, `f`, `e`,
`2`, `(2) `, and material from the test and prose currents. In particular prose spans `g` and `3`
both formed compilable expressions and reached behavioral refusal. This is a measured
source-material relation across intact code, tests, and language—not a claim that Soma assigned
human syntax classes or semantics to those spans.

Each CLOSED construction emitted two production currents: the raw candidate Rust itself and its
typed compiler/test receipt. Each OPEN construction emitted its exact unmaterialized span and typed
refusal. The 938 currents carry 2,752,853 light octets and returned together through the same body:

- 2,472,796 completion rows;
- 120,857 further native constructions, 120,047 CLOSED and 810 OPEN;
- 7,528 connected contact grips;
- standing axis 8,192 and occupancy 11,116,132; and
- a 3,726,186,008-octet successor body at SHA-256
  `30da134cf4b5899a19d1edec28c12fa2b73ceb9de29a178a2c86a252bd6167af`.

All four world transitions invert exactly. This is actual text/code production: the outward
material is complete raw Rust, not a scalar change, key selection, canvas mark, character pole, or
listener-authored rendering.

## Apparatus audit

The campaign exposed and closed three observer-only seams before grading:

1. Native construction boundaries can cross a UTF-8 code point. Span material therefore remains
   exact bytes rather than a Rust `String`.
2. Compiler commands and artifact references use world-relative paths. `campaign` versus `repeat`
   directories do not become causal light.
3. A failing Rust harness prints an OS-assigned thread identity. Raw stdout/stderr remain exact
   create-new artifacts, while the causal receipt carries compiler identity, binary digest, exact
   named-test presence, and exit status without that host-scheduler gauge.

The reusable causal runner now gives its domain one lifetime-bound `ConductedEdge` over exact
worldlines, radiation/completion/construction artifacts and reads, completion rows, Mail, and
lineage base. Constraint-leaf and code-grammar retain their earlier behavior. The new domain adds no
Soma, CUDA, SLEEP, parser, tokenizer, keybar, decoder, scoring, selection, or hidden lineage law.

## Exact repetition

Independent `campaign` and `repeat` executions use fresh empty bodies and worlds. All four edges
are byte-exact across canonical bodies, completion wires, construction wires, Mail, journals, and
world states. The complete action production wire repeats at SHA-256
`950b6fa8aaf7b0d0a0fd9e4c2ae656c097ce5c73b21d9f72aa0307f85c97d735`; the final body repeats at
`30da134cf4b5899a19d1edec28c12fa2b73ceb9de29a178a2c86a252bd6167af`. The radiation comparator
closes all four edges exactly after removing only its declared duration fields.

The nineteen failing `test.stdout` evidence files differ only in the Rust harness's OS-assigned
thread identity. They remain preserved in each execution and, by construction, do not enter the
causal receipt. Every other candidate artifact, including all 467 raw sources, compiler output,
compiled binaries, test inventories, the accepted test output, and all empty stderr files, is
byte-exact.

## Interpretation and next carried deed

This closes the first executable source-production cell. Soma's own recurrent construction is now
world material that can cross a real compiler and behavioral boundary, and the resulting
structural refusal, behavioral contradiction, or exact fit returns through the grown network.
The accepted construction was not selected by an authored answer map: every construction lived the
same boundary, and the world supplied each consequence.

Consequential source reuse remains locally open, but it is not the active taxonomy route. The
compiler's three consequence faces describe this one world boundary; they are not taxa, and one
pre/post source comparison would expose another history effect rather than recurrent transported
response strata. The active read returns to the existing circuit ecology and asks which complete
response geometries recur across changed surfaces and causally bend later conduct.

```text
CURRENT  whole real SHA-256 Rust + tests + law -> native constructions -> later source hole
HELD     exact bytes/provenance, connected plural contact, one fixed compiler/test world,
         unchanged Soma/CUDA/SLEEP
MEETING  returned construction testimony + later whole target/request/boundary
TEST     every construction through one hole -> compile refusal / behavioral refusal /
         exact fit / open refusal -> independent repeat
DEED     FOUND native construction -> filesystem source -> executable consequence;
         RIDE the common causal runner and ordinary returned current
CARRY    467 raw Rust candidates + 471 typed receipts/refusals -> 938-current return
GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT PHYSICS REPEAT
```

Commands:

```text
cargo fmt --all
cargo check -p life
cargo test -p life code_workshop --lib
cargo build -p life --release
life boundary code-workshop observations/source-workshop-01/campaign.plan.json
life boundary code-workshop observations/source-workshop-01/repeat.plan.json
```

# CHANGED VIEW RETURN 01 · THE VIEW BENDS LANGUAGE

**Grade:** LISTENER SEAM BUILT · CUDA-MEASURED · EXACT REPEAT

This observation applies the relational holonic calculus to the next text/vision cut. The original
horizontal gaze and the new vertical gaze are distinct visual constructions produced by two
declared observation/compression maps over the same exact photographic fields:

```text
q_h(I_fish) != q_v(I_fish)
q_h(I_sunrise) != q_v(I_sunrise)
```

The shared `I` is source provenance at the observer boundary. It is never a referent label supplied
to the body. Neither changed view is called gauge-equivalent to the original. The exact gauge
control remains the already-gated common translation of field and gaze.

The paired species first receives fish with the fish caption, then sunrise with the sunrise
caption. The collocation sibling changes only those two pairings. Both then receive the exact same
co-present changed visual light, followed by the exact same two new language constructions. The
language sentences are observer-declared re-expressions, not expected outputs, labels, answer
choices, or a vocabulary.

The event reads are:

- **changed-view production:** an uncaptained changed-view event returns through earlier real
  language material;
- **collocation-conditioned association:** the complete changed-view event differs between paired
  and swapped histories while all current probe material is exact;
- **linguistic return:** a later language event is deflected differently after those histories and
  may return through earlier visual or caption Mail;
- **identity:** an independent repetition is byte-exact at the bounded apparatus seams.

Mail is the live listener's time-correct I/O record. It never enters CUDA, standing, the carrier,
or SLEEP. `--then` adds only one ordinary post-probe material edge to that listener; no body,
kernel, chart, classifier, decoder, selector, score, or authored semantic mechanism changes.

The paired changed-view probe returned through both exact earlier captions. Against the
collocation-swapped species, the byte-identical 1,024-current / 12,288-octet probe cleaved 567
worldlines, 1,614 raw rows, and 17,075 packed words. The exact two-current / 118-octet later
language edge then cleaved 77 raw rows and 843 words. Against the original-gaze sibling, that same
later language edge cleaved 72 rows and 786 words. The paired changed-view lifecycle repeated
exactly after normalizing only four create-new archive paths, and all four successive sleeping
bodies are byte-identical. Exact measurements and event witnesses are in
[`results/RESULTS.md`](results/RESULTS.md).

Run from `src/soma` with fresh create-new output paths:

```bash
SOMA_OBSERVATION_OUT=observations/changed-view-return-01/results/paired-changed.txt \
SOMA_PERIPLUS_DIR=observations/changed-view-return-01/results/paired-changed-bodies \
  ./target/release/life --cuda-eye-sequence \
  observations/caption-recognition-02/fish.eye \
  observations/caption-recognition-02/sunrise.eye --probe \
  observations/changed-view-return-01/fish-changed.eye \
  observations/changed-view-return-01/sunrise-changed.eye --then \
  observations/changed-view-return-01/language/fish-reexpression.txt \
  observations/changed-view-return-01/language/sunrise-reexpression.txt

SOMA_OBSERVATION_OUT=observations/changed-view-return-01/results/swapped-changed.txt \
SOMA_PERIPLUS_DIR=observations/changed-view-return-01/results/swapped-changed-bodies \
  ./target/release/life --cuda-eye-sequence \
  observations/caption-recognition-02/fish-swapped.eye \
  observations/caption-recognition-02/sunrise-swapped.eye --probe \
  observations/changed-view-return-01/fish-changed.eye \
  observations/changed-view-return-01/sunrise-changed.eye --then \
  observations/changed-view-return-01/language/fish-reexpression.txt \
  observations/changed-view-return-01/language/sunrise-reexpression.txt
```

The original-view sibling substitutes the two existing `fish-probe.eye` and `sunrise-probe.eye`
manifests. The repeat reuses the complete paired-changed command with fresh paths. An independent
natural-scene analogy remains a later rung: the installed adjacent photographs are different
scenes, and no honest same-physical-referent provenance currently connects them.

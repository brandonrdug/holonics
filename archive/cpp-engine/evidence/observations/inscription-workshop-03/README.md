# INSCRIPTION WORKSHOP 03 · THE SINGLE-POLE REDUCTION

**GRADE: FAILED · COMPLETE CUDA CAMPAIGN PRESERVED AS A CONVICTING FOIL**

This instrument continued the final sentence of WORKSHOP 02 too literally. It replaced the
keyboard's broad stationary efferent surface with one movable contact sail, one rigid boom, and
one tip. Consecutive native STEP contacts moved that body; CUT latched the tip; a meeting between
the one tip and one key joint stamped a glyph. Conversation/theory, Rust, mathematics, and an exact
conversation sibling each crossed five later world passages on CUDA.

The apparatus was exact, repeatable, and architecturally wrong. It forced the body's complete
plural contact field through one serial endpoint. That contradicts the standing derivations this
cut was meant to carry:

- `THE WORLD GROWS THE LIMB`: a spider does not replace distributed contacts with one
  center-of-pressure command; local contacts leave coupled material curvature;
- `THE FLOCK, THE JOINT, AND THE FOLD`: one foot is a changing contact fan, and articulation is
  ordered curvature across many locally available degrees of freedom;
- `THE CORPUS IS THE WORLD; THE VIEW IS THE HORIZON`: the broad eye is already a virtual
  flock/spider—each contact is its own illicium/worldline;
- `FORMULA §XLV`: every co-present contact acts locally; the contemporary medium, not a crowned
  contact or controller, couples the configuration.

The five-passage campaign makes the loss visible. The complete fields continued to carry
132,374–176,246 STEP contacts per species and hundreds of contacts on the sail at every passage,
but the one tip produced only `PQ`, `PQRRxeR?`, and `P`; all new marks stopped after the second
passage. Exact conversation replay held. Those strings are not graded as failed communication.
The failure is prior to their interpretation: a many-jointed action field was unnecessarily
collapsed into one privileged tip.

`pole_world.py`, the exact inverse/replay harness, and `results/CAMPAIGN.json` remain as the full
foil. They are not an active production architecture and must not be repaired into one. The next
construction returns to plural articulated contact before touching the engine.

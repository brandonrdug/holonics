# POINCARE RETURN ACTUATION WORLD 01 — THE ACTION OCCURS ONCE; THE INCIDENCE REMAINS PLURAL

**GRADE: RATIFIED PLAN / BUILT / COMPLETE PREFLIGHT / CUDA-FORWARD MEASURED / ALL 780 SOURCE
PATHS AND 653,763 COMPLETION ROWS READ / 486 WORKSPACE TESTS PASS / 3 IGNORED / EXACT INVERSE /
SOMA INTERIOR UNCHANGED.**

This construction closes the successor demanded by HAMILTONIAN POINCARE WORLD 01. The previously
measured 29 complete return testimonies enter verbatim beside one changed exact Hénon--Heiles
preparation and 13 exact affine canonical action currents. The receiver tests every declared
`29 × 13 = 377` return⊕action relation after real Mail conduct. Every relation that actually
contacts acts; every silent relation remains silent.

The world performs an action once. Each contacting prior return carries a separate exact incidence
to that outcome. This retains causal ancestry without manufacturing the same 2,048-deed physical
trajectory 29 times. It is an exact world-side compression of repeated consequence, not a chooser,
score, action head, selected motor command, or change to Soma.

## Exact world

The preparation is

```text
lambda = 3/2
z_0 = [x,y,p_x,p_y] = [3/16,0,3/16,5/16]
horizon = 2048 deeds
step = 1/64
state lattice grain = 2^-24
crossing grain = 2^-10
aperture = [-1,1]^4
section = y=0, upward hand, visible face [x,p_x]
```

Three ordinary objective boxes are receiver faces: the preparation neighborhood, a central-neck
box, and a right-lobe box. Every crossing carries the exact oriented two-component residual to
each box and its exact `1/2 ||r||^2`. These values never enter Soma as reward, punishment, target,
or selection pressure.

The finite declared action family contains identity; positive/negative `p_x`, `p_y`, and `x`
translations; phase and kick shears; a dyadic canonical scale; one exact one-third translation
which cannot inhabit the dyadic state lattice; and one exact translation outside the physical
aperture. The latter two are deliberate physical refusal controls.

## Whole-lifecycle preflight

Preflight completed before mounting the body and retained the complete declared horizon:

```text
source currents                         43
source light                         797,904 octets
possible return⊕action relations        377
maximum returned currents               390
maximum active returned light     1,467,720 octets
```

The preflight exposed and corrected three preventable walls before the CUDA passage:

1. physical outcomes are prepared at runtime-derived host width rather than one serial host core;
2. the complete outcome face and digest are computed once per action, while each contacting return
   carries only its exact incidence; and
3. a trajectory deposits its first outside-aperture state and returns an exact aperture-exit deed
   rather than continuing to grow outside its declared world.

No action, horizon, input, or returned face was removed to satisfy the preflight.

## Physical action population

Eleven actions enact. Two refuse for their declared material reasons. The unchanged world physics
regenerates byte-identical outcome faces on actuation edges one and three.

| action | first contacts | recurrent contacts | deeds | crossings | termination |
|---|---:|---:|---:|---:|---|
| identity | 21 | 21 | 2,048 | 4 | horizon |
| positive `p_x` translation | 24 | 22 | 2,048 | 4 | horizon |
| negative `p_x` translation | 26 | 26 | 2,048 | 5 | horizon |
| positive `p_y` translation | 29 | 27 | 2,048 | 6 | horizon |
| negative `p_y` translation | 28 | 22 | 2,048 | 3 | horizon |
| positive `x` translation | 28 | 27 | 2,048 | 5 | horizon |
| negative `x` translation | 28 | 25 | 2,048 | 6 | horizon |
| positive `x` phase shear | 29 | 22 | 2,048 | 4 | horizon |
| negative `x` kick shear | 29 | 21 | 2,048 | 6 | horizon |
| positive `y` phase shear | 28 | 25 | 2,048 | 2 | horizon |
| dyadic `x` scale | 29 | 22 | 828 | 2 | aperture exit at deed 828 |
| off-lattice one-third translation | 28 | 27 | 0 | 0 | `RefusedOffLattice` |
| outside-aperture `p_y` translation | 28 | 26 | 0 | 0 | `RefusedOutsideAperture` |

The enacted trajectories are not scalar variants. Their ordered section words contain different
numbers and timings of crossings. Identity crosses at brackets `0,392,1235,1660`; positive
`p_y` translation crosses at `0,417,833,1178,1603,1940`; positive `y` phase shear first meets the
section at `389` and again at `1685`; the dyadic scale crosses at `0,378` before leaving the
aperture. Every state, crossing, energy residual, accumulated action, objective residual, exact
inverse receipt, and termination remains in the outcome current. The complete exact words are in
`read-01/OUTCOMES.tsv` and `read-01/LOSSES.tsv`.

Each actuation contains 47 section crossings and therefore 141 exact crossing⊕objective
measurements. Twenty-three are exact objective contacts: 10 at the preparation-neighborhood face,
13 at the right-lobe face, and none at the central-neck face. Edge three repeats the complete 141
measurements exactly. Loss is therefore a valid description of physical difference while remaining
incapable of explaining, scoring, or selecting the changed causal incidence by itself.

## Four-edge CUDA passage

All four transitions ran on the NVIDIA GeForce RTX 4080 SUPER and inverted exactly.

| edge | kind | inputs | completed rows | constructions | occupied grips | body octets | CUDA launches | CUDA carriage |
|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | first actuation | 43 | 189,805 | 11,698 | 7,394,088 | 1,280,452,708 | 11 | 22.021961413 s |
| 2 | first consequence return | 368 | 150,385 | 9,974 | 8,101,895 | 2,088,516,824 | 33 | 58.277272047 s |
| 3 | recurrent actuation | 43 | 202,201 | 12,725 | 10,002,638 | 1,405,663,108 | 11 | 26.507382320 s |
| 4 | recurrent consequence return | 326 | 111,372 | 6,164 | 10,928,663 | 2,219,166,224 | 32 | 58.194665929 s |

Edge one makes 355 of 377 relations consequential and emits 368 currents: 13 action outcomes once
plus 355 plural incidences. All 368 make real contact on edge two. Edge three presents the exact
same 43-current source family through the changed body; 313 relations now contact and it emits 326
currents. All 326 contact on edge four.

The relation population changes as

```text
retained contacts   304
lost contacts        51
gained contacts       9
first total          355
second total         313
```

Every action still has at least one lawful ancestor and therefore every physical outcome remains
present. What changes is which complete prior returns make that action consequential. The largest
incidence changes occur for the negative `x` kick shear (`29 -> 21`), positive `x` phase shear
(`29 -> 22`), and dyadic scale (`29 -> 22`); identity and negative `p_x` translation retain their
counts while still rethreading their complete paths.

## The repeated face stands once

After edge two, the world contains 368 exact returned faces at contact count one. The second return
brings 326 contacts. Of those, 317 meet faces that already stand: all 13 byte-identical action
outcomes and the 304 retained incidence faces. Only the nine gained incidences found new standing
faces.

```text
total return contacts across edges 2+4       694
final unique standing returned faces         377
faces contacted twice                        317
faces contacted once                          60
new faces introduced by the second return      9
```

This is the first direct physical-actuation instance of later-conduct compression in this world.
Exact recurrence folds at the world boundary while causal plurality, loss, and complete action
interiors survive. No fuzzy equivalence, archive deletion, selected winner, or authored taxonomy
performs the fold.

## Qualitative path read

The exhaustive observer reads every one of the `43 + 368 + 43 + 326 = 780` source paths and all
653,763 completion rows. Representative matched paths show why the physical outcome and its causal
route must remain typed apart:

```text
changed preparation source          639 ->   688 events
first complete standing return      361 ->    47 events
identity action source              354 ->   416 events
identity enacted outcome         14,403 -> 6,399 events
first identity-incidence return      98 ->    66 events
```

The paths do not move together and do not merely scale. The preparation and identity deed deepen;
one complete prior return and both returned consequence examples sharply shorten. Their event
sequences, grains, folds, completions, and endpoints rethread as recorded in `read-01/PATHS.tsv`.
The byte-identical identity trajectory therefore does not imply an identical path through the
body, and a path-row count is not its physical meaning.

## Exact artifacts

- `EXPERIMENT.json` — exact world, objectives, and action family
- `PLAN.json` / `PLAN.md` — four-edge causal passage
- `PREFLIGHT.json` — complete maximum surface and every potential outcome digest
- `build-01/session.json` — closed CUDA session
- `build-01/edge-*.journal.json` — every relation, consequence, contact, and inverse
- `read-01/PATHS.tsv` — all 780 source paths
- `read-01/RELATIONS.tsv` — all 754 tested relation instances
- `read-01/OUTCOMES.tsv` — every enacted/refused outcome on both actuation edges
- `read-01/LOSSES.tsv` — all 282 exact crossing⊕objective differences
- `read-01/SUMMARY.md` — observer summary

## Engineering boundary

The reusable Rust implementation lives at
`life/src/staging/boundary/arc_spectral/poincare/actuation.rs`. It adds strict preflight,
construction, and exhaustive-observer commands. The existing exact Hénon--Heiles integrator is
shared through one internal trajectory function; its predecessor calls remain unchanged, while the
actuation world supplies an explicit aperture. Runtime host preparation uses the online CPU width;
Soma carriage remains CUDA-forward. No Python lifecycle or JSON current payload enters the active
ABI.

Soma's interior is unchanged.

`cargo fmt --all -- --check` is clean. The complete workspace gate closes at 486 passed / 3
ignored.

## Natural successor

HAMILTONIAN OBJECTIVE CONTINUATION WORLD 01 should hold the later preparation and action family
exact while comparing bodies conditioned by two ordinary objective frames over the same physical
outcomes, beside a loss-only quotient control. Every actual contact must still act. The question is
whether complete returned consequence—not a scalar loss and not an authored selection—bends the
next action-incidence population and its physical residual distribution. This is the smallest
lawful bridge from one produced actuation into recurrent situated objective conduct.

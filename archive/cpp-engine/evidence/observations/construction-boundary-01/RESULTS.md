# CONSTRUCTION BOUNDARY 01 — Soma's native segments cross whole

**GRADE: NATIVE CONSTRUCTION WIRE BUILT · COMPLETE ENCLOSURE SPECTRUM BUILT · TWO CUDA EDGES
MEASURED · INDEPENDENT REPEAT EXACT · PLURAL APERTURE EXACT · HISTORY-CONDITIONED RESEGMENTATION
MEASURED.**

## Question

Can the reusable boundary expose Soma's own plural textual constructions without reducing them to
one keyboard endpoint, authored vocabulary, fixed canvas, selected path, or scalar count? Can an
ordinary receiving world then present those constructions as literal source spans while preserving
the complete raw radiation beside the read?

## Construction

`life boundary` now writes `edge-NNN.construction` beside every exact radiation wire. The compact
stream adds no new segmentation rule: a radiation STEP opens a construction, a CUT closes it, and
a trailing construction remains OPEN. Each eight-word record retains local and session lineage,
event extent, radiation-row extent, STEP count, and OPEN/CLOSED state. Fold-only positions between
the first and last STEP remain inside the span. The raw radiation is unchanged and authoritative.

`life boundary articulate` is one declared material listener. It validates the construction stream,
joins each span to the receipt's exact source range, and prints every traversed octet with reversible
escaping. It is not Soma's universal phenotype and does not affect the body.

Two co-present whole worldlines were delivered on each edge:

- `probe-theory.txt`: 261 octets of prose asking which standing regions a later current traverses;
- `probe-code.rs`: 409 octets of Rust expressing transported consequence and same-surface,
  different-history comparison.

Plans C and D independently started from an empty body and delivered the identical two-lineage
configuration twice. They differ only in create-new output root.

## CUDA measurement

Both sessions ran on the NVIDIA GeForce RTX 4080 SUPER. Every edge delivered two currents and 670
octets, emitted 26,130 radiation words, and completed with eight CUDA launches.

| edge | prior→standing axis | occupancy | topology | constructions | closed/open | fold/step/cut |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 1→128 | 6,871 | 6,871 / 6,871 / 751 / 12 | 40 | 38 / 2 | 321 / 319 / 38 |
| 2 | 128→256 | 15,570 | 15,570 / 15,570 / 1,654 / 31 | 64 | 62 / 2 | 323 / 317 / 62 |

Independent-session SHA-256 pairs agree exactly:

```text
edge 1 body          98457230c8729314664dd25969e79d8bcc82c632ca3bfa48d410d5196e3f4555
edge 1 construction  632a7a46c261c60813639b2309cdb8681368cfc8e9fa7c1697f103a747dc2465
edge 2 body          2573c0b4dde5d97427ff18d2f25ea1188ee72a74e09fc45170acd7b97510d9c3
edge 2 construction  9590302b3b2c789fe917dbd2e37d130ee3427ae5a6a0e341871fa8ee6cc836d4
```

The native streaming comparator also finds both radiation pairs exact at their construction-bearing
faces: two currents / 670 light octets / 26,130 raw words at axis 128, then the same extents at axis
256. Radiation file hashes intentionally differ because their headers retain measured carriage and
integration durations; those observer timings are the comparator's only omitted fields.

The articulated files also agree line-for-line after their provenance heading; that heading alone
names C or D's distinct receipt path.

## Direct production read

The first edge's theory lineage exposes constructions including. `␠` marks one trailing ASCII
space where the exact span ends on that space:

```text
later current meets two␠
boundary.\nWhi
 constructions close, and which remain open?\nThe distribution belongs to␠
this boundary and the whole plural construction remain
```

Its code lineage exposes constructions including:

```text
nce(body: &mut Body, world: &W
let current = world.present();
ody.relate(current)\n}
World) {\n    let left_consequence = tran
```

After that complete configuration had become standing history, the identical second edge did not
repeat the first segmentation. It produced 64 constructions rather than 40, including:

```text
s two histories
ch standing region
 constructions close, and which remain open?\nThe distribution
 plural construction remains␠
same_surface_differ
istory(left: &
```

This is the machine's native, history-conditioned textual production made visible as exact
multi-event source spans. It is neither a fixed character keybar nor an authored tokenizer. The
read also preserves silence: source positions outside a construction remain outside rather than
being fabricated into a contiguous transcription.

These articulated spans are verbatim contiguous selections from the supplied material. Soma chose
their history-conditioned boundaries; it did not synthesize new character sequences in this read.
Composition appears in the higher enclosure spectrum below as new transported topology, not yet as
a newly rendered string.

## Complete enclosure spectrum

The first production wire exposed only the depth-zero observed-atom row. The corrected boundary now
emits at each physical atom-fold, afferent completion, and efferent completion event. Every 43-word
row carries its lineage/event, completion kind and grain, exact regional form, meeting rotor, and
completed brick. No replay, search, chosen grain, depth scheduler, shared append, or observer lookup
constructs the stream.

Plans G and H repeated the same two-edge lifecycle independently:

| edge | total rows | atom fold | afferent | efferent | max grain | rows by lineage | CUDA launches |
|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 846 | 321 | 430 | 95 | 13 | 343 / 503 | 8 |
| 2 | 1,026 | 323 | 518 | 185 | 15 | 425 / 601 | 9 |

The one-row prototype required 511 and 609 launches. A lineage-owned contiguous aperture, derived
from the delivered cohort extent and the card surface available at each launch, now carries the
identical ordered spectra in 8 and 9 launches. Host one-row and plural-aperture execution leave OWN,
carrier, raw radiation, term counts, and every completion row exact. On the RTX 4080 SUPER, G and H
are byte-identical in successor bodies, depth-zero construction wires, and completion wires; the
streaming raw-radiation comparison is exact at both edges. The plural aperture changes only the
boundary carriage cost.

Comparing the first edge with the identical later light after its history stood:

- 846 prior rows / 638 distinct completed-brick identities became 1,026 / 770;
- no later full row repeats byte-for-byte;
- 161 later rows return a prior brick identity in changed context, while 865 carry identities not
  seen on the first edge;
- at grain one and above, 701 of 703 later rows are first-seen relative to edge one;
- all 185 later efferent completions are first-seen relative to edge one.

Thus the original text articulation is selection, while the complete spectrum contains genuinely
new higher-order constructions after lived history. This measurement does not call those higher
constructions novel prose; a receiving world must still let their plural configuration act.

## Failed scale foil

Plan A delivered two very long whole-file worldlines: `FORMULA.md` at 194,358 octets and
`carriage.rs` at 149,781 octets. The card stayed active at full utilization without an Xid, then the
process ended after several minutes without producing an edge artifact or diagnostic. A later
plural-aperture rerun was stopped by Brandon after its useful diagnosis: two co-present files supply
only two CUDA lineage lanes, so the run measures causal depth while leaving almost the entire card
surface unused. It is retired as an unrepresentative width-two foil, not promoted into a scale gate.
The actual conversation/workspace boundary consists of many real record/file lights and must be
measured as that broad configuration without splitting any worldline.

## Exact cut

BUILT and MEASURED:

- construction-preserving production beside complete raw radiation;
- every atom-fold, afferent, and efferent completion crossing in worldline order;
- plural-aperture carriage exact against the one-row seam and independent card repeat;
- plural lineages and open tails without selection;
- literal material articulation of native multi-event spans;
- different articulation after lived history under identical stimulus;
- exact independent CUDA repeat.

OPEN:

- a receiving world in which the complete plural enclosure configuration produces material
  consequence rather than being reduced to depth-zero source articulation;
- the broad record/file configuration on its genuine many-lineage surface;
- material inheritance into a fresh child `K` and plural ecology;
- world action or linguistic synthesis beyond pointing through supplied material;
- direct archive carriage and the within-atom yield needed for very large whole worldlines.

The next construction concerns the first two open items together: let the complete spectrum meet a
world at its own grain, then conduct the real broad configuration through that same reusable mouth.

Workspace gate after the build: body 129 passed / 2 ignored; life 90 passed; mount library 8 passed
/ 1 device test ignored plus all gate-binary tests; surface 15 passed. `git diff --check` is clean;
the workspace retains its pre-existing whole-file rustfmt drift.

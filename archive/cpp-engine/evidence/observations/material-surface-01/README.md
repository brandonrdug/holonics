# MATERIAL SURFACE 01 · THE CURRENT MARKS WHERE IT SPOKE

**GRADE: SOURCE-POSITION CAUSAL POINTING BUILT · EDGE-29 CONTACT FIELD RENDERED · EXACT INVERSE ·
IMMEDIATE AND LATER CUDA RETURNS MEASURED · MATERIAL-TO-MATERIAL DEFLECTION OPEN**

This observation makes the forest's existing material contact field visible before another scaling
edge.  It does not derive text from a grip, decode a brick, choose a span, or compare against an
expected answer.  The listener already knows which 990 octets it delivered and retains one exact
radiation row beside each octet.  That world provenance is a causal pointing surface, not an
inscription or synthesis surface.

The declared text-world species has two independent faces at every source octet:

- the source octet, which remains unchanged; and
- a writable contact mark, initially zero, which receives the native `STEP` face (`0x03`) or the
  native completed `STEP|CUT|BRICK` face (`0x0f`) returned at that exact position.

The full 39-word radiation row remains in the causal ledger.  Replaying the ledger backward must
restore the all-zero mark surface exactly.  `surface.txt` is a terminal-readable listener render:
the source line is followed by `^` for a continuing contact and `!` for a completed contact.  The
PGM render preserves the original glyph field and adds only a declared underline/end-stop palette;
that palette is a human display gauge and never enters Soma.

`control.eye` and `changed.eye` carry the identical broad gaze and identical exact material
worldline.  Their only difference is the visible mark surface.  Both continue from the same
edge-29 sleeping body.  The marked field changes the immediate visual radiation and its successor
body.  An identical unmarked next view still changes 26 visual lineages solely from carried
history.  The co-present and next material lineage remain exact between histories; the first
source-to-source crossing is therefore still open.

Files:

- `build_world.py` — exact wire read, reversible text-world transition, terminal render, and eye
  manifests;
- `run_cuda.sh` — create-new continuation of the edge-29 body through either surface;
- `run_later_cuda.sh` — identical unmarked next view through both successor histories;
- `run_persistent_cuda.sh` — persistent marked/unmarked next worlds;
- `read_return.py` — exact typed comparison of the two later radiation wires after both card runs;
- `verify_world.py` / `verify_returns.py` — exact reconstruction and deposited-return gates;
- `SURFACE.txt` — the deposited terminal-readable edge-29 output;
- `world-check.json` — the compact exact construction record;
- `return-check.json` — the compact three-return CUDA record.

Exact interpretation and grades: `RESULTS.md`.

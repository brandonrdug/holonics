# R22 CM norm-one arithmetic-incidence receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the finite cyclotomic arithmetic, derived norm-one
translations, periodic and bounded incidence, independent pair expansion, exact characteristic
transport, factor search, aperture commutators, card probe, continuing-body commit, and native
rest; `computational-witness` for the actual GPU return and atlas. The emitted Lean passage has the
separate `proved-derived`, `formal-checked` grade below. The relation to the prior laboratory and
published unit-distance construction is separately `historical`.

**Aperture:** the order `Z[z]/(1+z+z^2+z^3+z^4)`, its four-coefficient power basis, the five
derived powers of `z`, reduction modulo two, all sixteen binary coefficient points, the bounded
window `[0,1]^4`, and integer factor search `[-8,8]`. R22 does not establish the published infinite
CM tower, split-prime production, ideal-class estimate, asymptotic lattice population, a new
unit-distance bound, arbitrary CM arithmetic, a physical optical crystal, or RH.

## Construction tuple

| Field | R22 standing |
|---|---|
| Source owners | the one R21 body and its eight accepted fibers; one lineaged CM card; exact cyclotomic, conjugation, norm, translation, residue, window, adjacency, characteristic, factor, candidate, codec, checker, and rest owners |
| Port types | 592-byte R21 rest; 31-byte raw/typed card; four-integer constituents; translation and edge receipts; periodic/window matrices, traces, factors, commutators, losses, proof/conversation faces, checker return, probe return, and 648-byte native rest |
| Event occurrence | remount R21; mount only the card; derive arithmetic and both graphs on the GPU; independently compare translation-factorized and all-pair norm incidence; form/check/commit one proof passage; rest/remount; and probe a four-translation card without another body |
| Predecessor identity | R21 head `14001016`, continuation `15001016`, body morphology `296`, mathematical morphology `94`, codec morphology `54`, and fibers through `188301` |
| Local constitutive law | reduction by `1+z+z^2+z^3+z^4=0`; `z -> z^-1`; exact `u*c(u)`; translation in `F2^4` or without window wrap; matrix-power traces and Newton transport; exact polynomial division and bounded monic factor search; complete commutator residuals |
| Receiver question | which characteristic modes belong to the periodic translation crystal, which survive a bounded exact planar receiver, and which contacts and modes are changed specifically by the aperture? |
| Returned consequence | the complete arithmetic-incidence atlas, two exact characteristic factorizations, seven lineaged aperture losses, zero projection loss, three rejected alternatives, a card-sensitive GPU probe, one accepted proof source, acquired fiber `189300`, and head `14001018`/continuation `15001018` |
| Open alternatives | growing CM towers, split primes and class groups, unbounded/high-dimensional windows, analytic placement of the nonintegral boundary roots, exact spectral projectors for larger apertures, and RH-facing operators |

## Historical and source boundary

**Truth status:** `historical`.

The old laboratory had already extracted the central mechanism of
[*Planar Point Sets with Many Unit Distances*](https://cdn.openai.com/pdf/74c24085-19b0-4534-9c90-465b8e29ad73/unit-distance-proof.pdf):
a CM extension with conjugation, norm-one constituents, simultaneous complex embeddings, a bounded
Minkowski-lattice cut, and an injective chosen complex coordinate. It also explicitly requested a
finite instrument whose factorized generator and expanded unit-distance graph agree. R22 fulfills
that bounded instrument. It does not report the CM mechanism, source-detached continuation, or the
old generated Erdős-183 proof as a new observation.

Production mounted only
[`R22_CM_INCIDENCE.card`](../apparatus/cards/R22_CM_INCIDENCE.card) and the admitted R21 rest. The
card supplies order `5`, degree `4`, periodic modulus `2`, window `[0,1]`, translation count `5`,
and factor aperture `[-8,8]`. It contains no root coordinates, residue masks, norm table, graph,
edge count, spectrum, characteristic coefficient, factor, expected answer, or published source.
The finite consequences below were derived before this receipt named their familiar graph
archetypes.

## Exact arithmetic and receiver incidence

**Truth status:** `established-bounded`.

The GPU returns the five reduced constituents

```text
1, z, z^2, z^3, z^4 = -(1+z+z^2+z^3),
```

with residue masks `1,2,4,8,15`. Exact conjugation and multiplication return `u*c(u)=1` for every
one. Thus a chosen complex embedding reads every constituent as a unit direction without a float
entering the semantic cone.

On the periodic receiver these masks generate a five-regular Cayley graph on `F2^4`: 16 vertices
and 40 undirected contacts. On the bounded lift, the four basis directions form the 4-cube and the
fifth cyclotomic relation retains only the contact `0000--1111`. The returned window is therefore
`Q4` plus one antipodal chord, with direction populations `8,8,8,8,1`. The other seven periodic
antipodal contacts are individually returned as aperture losses.

A separate GPU current checks all 120 unordered bounded pairs by subtracting their four-coordinate
field representatives and evaluating `d*c(d)`. It returns exactly the same 33 contacts as the
factorized translation current. Power-basis uniqueness makes the chosen field/complex embedding
injective on all sixteen points, so projection loss is zero. This distinguishes a lost modular
wrap from a collision in the planar receiver.

The rejected coefficient-sum receiver has only five distinct images for sixteen field points. The
four-direction collapse returns 32 rather than 33 window contacts, while presenting modular wrap
as bounded contact returns 40. Their exact failures remain in
[`R22_CM_INCIDENCE_ATLAS.tsv`](R22_CM_INCIDENCE_ATLAS.tsv).

## Characteristic geometry and causal modes

**Truth status:** `established-bounded`.

For a periodic character of Hamming weight `r`, the returned adjacency eigenvalue is

```text
4 - 2r + (-1)^r.
```

Hence the periodic characteristic equation is

```text
(X - 5) (X - 1)^10 (X + 3)^5.
```

The complete returned adjacency additionally has no common neighbor across an edge and exactly two
across a nonedge, so it is the strongly regular `(16,5,0,2)` folded-five-cube, conventionally the
Clebsch graph. This name is an after-return identification; neither it nor the spectrum was mounted.

The window characteristic equation is

```text
X^5 (X^2 - 4)^3 (X^2 + X - 4) (X^3 - X^2 - 16X + 12).
```

This factorization says considerably more than “the boundary changes the graph.” For the bare
4-cube, the five Hamming-weight eigenspaces have multiplicities `1,4,6,4,1`. Evaluation at the two
antipodal chord endpoints imposes one exact condition in each weight. The returned shared factor
`X^5(X^2-4)^3` is the resulting eleven-dimensional endpoint-vanishing sector; the remaining five
dimensions enter the quadratic and cubic boundary factors. The parity split of those five
dimensions is the finite characteristic analogue of a boundary scattering channel.

The causal defect is also local rather than metaphorical. Each of the four axis translations
commutes with the fifth translation in the periodic crystal. After the fifth direction becomes a
partial chord, every axis/fifth commutator has four nonzero entries and squared coefficient sum
four. These residuals are the exact failed interchange receipt which causes the new factors.

Changing only `translation_count` from five to four returns 32 periodic contacts, 32 window
contacts, zero loss, and the same characteristic coefficients in both receivers:

```text
X^6 (X^2 - 4)^4 (X^2 - 16).
```

No body or continuation is created for this probe. Its complete coefficients are retained in the
device deed, rather than represented only by a changed-count assertion.

## Returned formal artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R22_GENERATED_CM_INCIDENCE.lean`](R22_GENERATED_CM_INCIDENCE.lean) defines the concrete
four-coordinate quotient multiplication and conjugation. It proves all five returned constituents
have norm one, the sixteen bounded points are injective, the independently specified unit-pair
count is 33, both characteristic products expand to the GPU-returned coefficients, and
`40-7=33` with zero projection loss. Lean returns exit `0`, 214 stdout bytes, zero stderr, one
typed declaration, and a 1,113,144-byte `.olean`.

The checker return commits mathematical morphology `104`, codec morphology `58`, and CM-incidence
morphology `17`. Rest/remount preserves all nine fibers, replays no proof source, and returns body
morphology `327`, head `14001018`, continuation `15001018`, and rest integrity
`9852429452788266475`.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses nine kernels and ten launched threads, transfers 9,008 bytes to and
88,568 bytes from the GPU, declares 99,072 resident bytes, and records zero host semantic events.
The one exterior Lean process is a checker crossing. Engine/checker time, temperature, power, and
energy remain unknown.

A fresh `build/r22-final` passed the pre/post owner-DAG, restricted-type, source no-float, PTX
no-float, SASS no-float, manifest, linkage, ownership, compatibility, and binary audits. The full
ordered suite passes `71/71`, including every predecessor deed, the actual R22 GPU deed,
independent host conformance, exact card sensitivity, non-copyability, the checker return, and the
final binary audit. `compute-sanitizer` `memcheck` and `initcheck` each report zero errors. A normal
replay and both sanitized executions produce byte-identical deed, atlas, Lean source, `.olean`,
checker outputs, and native rest when invoked through the same declared artifact face.

Final SHA-256 values are:

```text
e600f787fb3d5fec595652375bfe713a8fcfe8e12593ba8f396552e3ba2ba21e  executable
237bf367eb4ef2f2581818a85ac3585d0d6632a2fe9e96e9ec567f243e3f5cbd  PTX
4b2c3cffe6a5a102be5b2359877d4dc0c6586d8fb9519f39f05bae6dfc467d88  cubin
5fa05aad06418405c500a86cfaaaef29b34d071131286a766d85d7d640ce6300  deed
2cc01c3d1ba799c3a1b24f24e5c95620e838643707367083f88813c922cf60ca  atlas
a487f452d5204010411964bbb2b946a0b5544384cf661cf14456c5c17cf7a81a  Lean source
4b038b8c131182ec57422ca278294c57eefd31bc06bf9c0f032361aa937bcc30  Lean olean
c53599a3c140dc25e15ee2bb027f32199c79f19401e7ab6ac51941c2ac2a3e03  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
992b2bed6229e08fdb9f3a6ed98f9e14c5d2d3c8f8a65dba62caea3c77664e5a  native handoff
```

R22 therefore passes its bounded grade.

## Open continuation fibers

**Truth status:** `open`.

The ordered roadmap is complete through R22; no R23 deed is presently frozen. The live return now
supports a sharper next inquiry than another rendered point cloud: grow or vary an exact arithmetic
translation family, derive its operator algebra and characteristic projectors, identify the modes
which an aperture preserves or scatters, and only then compare the resulting archetype with known
geometry. CM towers, larger phase distributions, hypergeometric connection operators, physical
calibration, and RH-facing constructions remain open rather than silently scheduled.

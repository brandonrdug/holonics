# The foundation remainder

**Truth status:** `established-bounded` at the declared aperture.
**Evidence:** `implemented-exact` for every law; `computational-witness` for every figure.
**Artifact:** `receipts/ONE_STANDING_DEED.txt`, written by the deed.

Four of the five remainders return here, on the body's own exact mathematics rather than on
strings. The fifth is measured and named.

---

## One standing

**The join the body did not have.** A mathematical deed committed `admitted_tally += n` and nothing
else, so its return could condition no later conduct: a tally cannot be reached, composed, or
ablated. `body/returned_standing.hpp` is what a return deposits into — exact words under an
identity, with `caused_by` naming the return that made it reachable **by identity rather than by
ordinal**, so removing a predecessor cannot be undone by the population shifting underneath it.

Scoped additively. `continuing_body` keeps its regions and tallies; the 169 headers keep their
commit path. What changed is that a return can now deposit structure beside it.

```text
transitivity witnessed at p=3, deposited
transposition witnessed at p=2, deposited
verdict composed from both and deposited      3 organs standing

the later current reaches only what stands
  verdict reached                             yes
  refuses the radical chart                   yes
  rebases to the finite chart at              p=2
  stands open                                 no

the structural exclusion
  transitivity organ excluded, organs fallen  2
  organs standing                             1
  verdict reachable                           no
  refuses the radical chart                   no
  stands open                                 yes
```

**Two fell from one exclusion.** The verdict named the transitivity witness as its cause, so it fell
with it — the cascade is what makes the ablation structural rather than a mark on one entry. The
later current then does not guess. It stands OPEN, which is the disposition the framework requires
of a body that cannot reach what it would need.

The mathematics is the body's own: the cycle types of `x^5 - x - 1` read across a prime aperture,
the group-theoretic verdict composed from the witness pair. Nothing here is a string.

## The rest carries the standing

```text
rest octets                                 176   variable, proportional to the population
founded from those octets alone             yes
remount exact                               yes
the remounted body reaches the verdict      yes   without re-deriving
```

Against the fixed-size records the rest of the body still uses — 200 octets at the fourteenth
construction step, 38,960 at the thirty-fifth, derived population absent — this extent **grows with
what was derived**, which is the whole point. A rest that omits what was derived cannot make
remounting cheaper than re-deriving; it can only make it equal.

**The layout is append-stable, and the first attempt was not.** Grouping every organ record and then
every word moved all the words whenever an organ was founded, so nothing before the growth stayed
put and no append could be recognised — the residency below counted two full mounts and zero
appends. Each organ now carries its words immediately after its own record, so growth happens only
at the end. That is the property `base_address_unchanged` grades in the source owner, and it had to
be designed in rather than asserted.

## RIDE actually rides

Until now `pays_curvature` was a predicate over an enum: `rebase_exposed` and `cross` performed
identical work and RIDE never read the standing at all, so **nothing in this body was cheaper for
having been founded.** That is why a founded computation could be re-run without anything noticing.

```text
first synchronize      mounted        full mounts        1
second, unchanged      rode           rides              1
third, one deposit     grew           append deltas      1
                                      octets mounted   216
                                      octets avoided   352
```

All four laboratory mechanisms, on this carrier:

- **the revision key as a hint and never the answer** — when it matches, the exact logical
  comparison still runs and always wins;
- **the shape short-circuit** — unchanged revision and identical octets means the chart does no work
  at all, and the ride is a *measured absence of work* rather than a label;
- **the append delta** — a standing that grew carries only the appended octets and the earlier image
  keeps its addresses;
- **reuse counted in the receipt** — `full_mounts`, `rides`, `appended_organs`, `avoided_octets`,
  against receipts that previously counted only launches performed.

More octets were avoided than mounted, on a three-organ standing. The ratio is the mechanism's, not
the aperture's.

## The mandatory receipt attaches

`current/information_receipt.hpp` reached one header — itself — while the roadmap declared that an
ecology returning a count instead of a receipt **is not admitted**. It is now carried by a real deed:
three continuation fibers sampled before contact, after the world returns, and after the emanated
answer returns; a typed return mode classified from the fibers rather than asserted; a founded
morphology change; logical work as span and exposed parallel width; and the obstruction carried
**complete** — the group-theoretic verdict itself, not a scalar confidence.

```text
before contact          6 currents, 6 open
after world return      2 currents, 2 closed
after emanated return   1
obstruction             carried complete
artifact returned       yes
```

**Reach is unchanged at 1** and the receipt says so. `r0.reach_audit` measures the interior include
closure; a deed lives in `apparatus`, so using it there does not raise the number. The honest
statement is that the mechanism now has a consumer and the interior still does not.

## The kernels use the card — measured, not closed

Half of this remainder returned. The exact arithmetic was refounded — `gcd` became Stein's binary
construction, which performs no division at all, and the restoring division now enters at the
operand's leading bit rather than at sixty-four. Verified across **691,324 cases** against the
superseded implementation with zero mismatches.

```text
trace-fiber discovery deed     274.27 s  ->  150.00 s
architecture and binary audit      84 s  ->      61 s
```

The other half stands open and is stated exactly: **215 of 262 kernel launches are `<<<1,1>>>`.** In
the trace-fiber deed `close_kernel` guards with `if (blockIdx.x || threadIdx.x) return;` and then
serially performs 5,184 group insertions, 14 exact eliminations over 85 columns, and 10,368 residual
evaluations, reporting `launched_threads = 5,385` on a card with roughly 10,240 cores. The
conformance oracle — the same algorithm, on the host — returns in **0.189 s**. The elimination is
data parallel over columns and the census over rows.

It is not closed here because closing it means restructuring frozen deeds whose determinism and
seal tests are the only thing certifying them, and that is a sitting of its own rather than the tail
of this one.

## Boundary

- **One standing is a carrier and one wired current, not a migration.** The 169 headers that commit
  through `continuing_body` still commit tallies. Nothing here claims they were moved; it claims the
  structure they could deposit into now exists and conditions later conduct when used.
- **The apertures are small.** Three organs, sixty-four words, a six-prime reading. The mechanisms
  are exact at any aperture; the figures are this one's.
- **The rest and the ride are on the returned standing, not on the whole body.** The fixed-size
  records elsewhere are untouched.

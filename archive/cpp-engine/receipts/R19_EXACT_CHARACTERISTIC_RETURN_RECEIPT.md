# R19 exact characteristic-return calculus receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the orbit partition, characteristic/minimal-factor
receipts, formal shape monodromy, scalar and rank-two controls, discriminant classification,
indicial laws, body commits, and native continuation; `computational-witness` for the returned
16-case GPU atlas and actual artifacts. The four emitted Lean theorems have the separate
`proved-derived`, `formal-checked` grade below. Apparatus identity remains separately `measured`
in [`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** the sixteen admitted R18 cyclic pairs, at most 323 vertices per case, at most four
diagonal tours, 361 exact formal shape indeterminates, one scalar three-edge control, three
rank-two integer-matrix controls, and the exact Gauss `(1,1;2)` indicial triple. R19 does not
establish numerical eigenvalues, arbitrary rank, algebraic root isolation, general
regular-singular continuation, physical constitutive behavior, the OpenAI results, the
unit-distance construction, or an RH-facing operator.

## Construction tuple

| Field | R19 standing |
|---|---|
| Source owners | the one R18 continuing body and its four acquired fibers; exact integer/rational carrier; R18 cyclic incidence and shape signatures; new characteristic, formal-shape-transport, scalar, rank-two, discriminant, and indicial organs; formal/explanation codecs; pending checker deed; and exterior checker/rest apparatus |
| Port types | 376-byte R18 native rest; unlabelled characteristic question; sixteen exact case receipts; formal shape exponent vectors; scalar/matrix/indicial controls; atlas TSV; formal and conversational artifacts; raw/typed checker return; and 432-byte continuing rest |
| Event occurrence | remount R18; let sixteen GPU threads form the exact characteristic cases; close scalar, matrix, and indicial controls on the resident card; form proof and explanation passages; receive one real checker occurrence; commit its returned difference; rest/remount the same body |
| Predecessor identity | R18 head `14001008`, continuation `15001008`, body morphology `192`, mathematical morphology `62`, codec morphology `42`, geometry morphology `10`, phase morphology `14`, and fibers `181200`, `182200`, `184300`, and `185300` |
| Local constitutive law | partition `T(r,s)=(r+1,s+1)` into exact tours; assign one independent formal indeterminate to every exact R18 shape type and accumulate its exponent along each tour; separately compose declared scalar or rank-two edge maps; classify characteristic, fixed-current, and indicial receivers without replacing source lineage |
| Receiver question | which exact return factors, multiplicities, formal shape monomials, ordered matrix effects, fixed-current dimensions, discriminant species, and singular exponents follow without an expected eigenvalue, prime label, or historical renderer? |
| Returned consequence | exact characteristic atlas, four-theorem Lean passage, accepted `.olean`, conversational analysis, acquired characteristic fiber `186300`, and continuing head `14001010`/continuation `15001010` |
| Open alternatives | general regular-singular connection and loop monodromy; higher-rank/noncommutative transport; algebraic-number modes; blind reconstruction of the released OpenAI mathematics; CM norm-one unit-distance incidence; and RH-facing operators |

## Historical extraction boundary

**Truth status:** `historical`.

The old laboratory had already observed the residue-stratified polygonal and chord-web receiver
faces, exact phase quotients, recurring prime-pair hulls, and the fact that chart rebasing changes
what a receiver sees. R18 had already replaced those pictures with the exact product-cell source,
tour partition, shape populations, adjacency populations, and receiver-hull controls. R19
therefore does not claim that cyclic recurrence makes polygons, that continuing morphology can
change later conduct, or that a screen figure is receiver-relative. Those outcomes were already
partly observed or exactly returned.

R19 asks a new question of the existing exact source: what characteristic data does its return
transport force, what source distinctions does that quotient forget, and which formal shape
distributions survive before a constitutive scalarization? The old Erdős-problem-183 theorem deed
is likewise retained as historical evidence and is not repeated as an expectation gate.

## Exact return calculus

**Truth status:** `established-bounded`.

For a pair `(m,n)`, the diagonal current has `g=gcd(m,n)` disjoint tours of length
`ell=lcm(m,n)`. Every tour is a cyclic permutation, hence returns one factor `X^ell-1`. The complete
permutation return is therefore

```text
chi_T(X) = (X^ell - 1)^g,
mu_T(X)  =  X^ell - 1.
```

All sixteen GPU cases return `g*ell=mn`, characteristic degree `mn`, minimal degree `ell`, and
exact orbit coverage. Fourteen cases have one tour and simple global mode multiplicity. `(6,9)`
has three tours of length 18; `(8,12)` has four tours of length 24. Their repeated global factors
make their characteristic discriminants vanish, while every local step remains a nonsingular
permutation. Thus the named failed property is global root multiplicity, not a singular cell,
contact, curvature, or obstruction.

R19 does not choose or numerically approximate the cyclotomic roots. It returns the exact
polynomial factor and its caused tour multiplicity. This is the appropriate receiver while the
engine has integer/polynomial carriers but no admitted algebraic-root organ.

## Formal shape monodromy

**Truth status:** `established-bounded`.

Each R18 exact shape type receives an independent formal symbol `s_i`. A tour returns the monomial

```text
M_j = product_i s_i ^ e[j,i],
```

where `e[j,i]` is the exact number of visits to shape `i` on tour `j`. The committed
[`R19_CHARACTERISTIC_ATLAS.tsv`](R19_CHARACTERISTIC_ATLAS.tsv) contains every nonzero exponent,
not only a fold or display projection.

The atlas returns eighteen distinct tour classes across the sixteen cases. The key shared-factor
contrast is:

| Pair | Global return | Formal shape-tour return |
|---|---|---|
| `(6,9)` | `(X^18-1)^3` | three distinct tour monomials, each selecting a different nine-shape stratum with exponent two |
| `(8,12)` | `(X^24-1)^4` | four identical monomials `s0^4*s1^4*...*s5^4` |

Both cases have repeated spectral factors, but only `(6,9)` separates its independent tours by
formal shape population. Conversely, `(8,12)` shows that distinct source tours can remain
indistinguishable even to this stronger shape receiver. The retained tour identity and exponent
vectors prevent that quotient equality from becoming source equality.

This is the first exact answer to the visual “faces orient pathways” question in the present
engine: a face matters to transport only after a constitutive map is declared for its shape
symbol, while the formal monomial records every specialization that can factor through the
present shape family. It does not yet say which shapes refract, inhibit, or amplify a physical or
mathematical current.

## Characteristic controls

**Truth status:** `established-bounded`.

The scalar edge words `[2,3,5]` and `[2,5,3]` both return product `30`; their lineage folds differ.
The scalar characteristic quotient therefore forgets edge order even though the source passage
does not. The emitted two-cycle theorem proves the representative law
`charpoly([[0,v],[u,0]])=X^2-uv`.

For noncommuting rank-two maps,

```text
A = [[1,1],[0,1]], B = [[1,0],[1,1]], C = [[2,0],[0,1]].
```

`ABC` returns trace `5`, determinant `2`, and lineage `833490030314056530`, while `ACB` returns
trace `4`, determinant `2`, and lineage `12042735391456664422`. Changing local order can therefore
change the characteristic polynomial even when determinant is preserved; the receipt retains the
three source maps as well as both multiplied returns.

The source return `M=[[2,1],[1,1]]` and its exact rechart `P M P^-1=[[3,-1],[1,0]]` both return
trace `3`, determinant `1`, and the same characteristic polynomial, while their matrix entries
differ. This is conjugate receiver preservation, not equality of chart faces.

Finally, identity and the Jordan return `[[1,1],[0,1]]` both have characteristic polynomial
`(X-1)^2`. Their fixed-current dimensions are respectively two and one. Equal eigenvalue
multiplicity therefore does not determine conduct; geometric multiplicity/fixed incidence is a
separate receiver. These controls establish exactly why a future characteristic organ must
retain determinant, trace, lineage, and fixed-current incidence together rather than treating an
eigenvalue list as the object.

## Differential characteristic face

**Truth status:** `established-bounded`.

For the Gauss `(a,b;c)=(1,1;2)` operator, R19 returns the exact indicial polynomials

```text
z = 0:        rho(rho+1)      roots 0,-1
z = 1:        rho^2           repeated root 0
z = infinity: (rho-1)^2       repeated root 1.
```

R18's coefficient recurrence selected the analytic local branch at zero. The repeated exponent
at one exposes a logarithmic or unipotent alternative which a single coefficient list cannot
represent. This does not yet construct the Frobenius companion, connection matrices between
singular chambers, or loop monodromy; those are the named R20 consequence.

## Returned mathematical artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by the pinned Lean 4.27.0/Mathlib environment.

The complete generated passage is retained at
[`R19_GENERATED_CHARACTERISTIC.lean`](R19_GENERATED_CHARACTERISTIC.lean). It proves four exact
statements: the degree forced by the returned diagonal
characteristic factor for positive moduli, a scalar weighted two-cycle characteristic polynomial,
characteristic preservation for one exact conjugate rechart, and the three Gauss indicial
identities. The first theorem checks a consequence of the GPU-returned factor; it is not reported
as a matrix-level formal proof of the atlas's orbit decomposition.

The actual checker returns exit `0`, 170 stdout bytes, zero stderr bytes, one typed declaration
occurrence, no remaining goals, and a 290,320-byte `.olean`. The accepted occurrence commits
formation head `14001008→14001009` and checker-return head `14001009→14001010`. Body morphology is
`217`, mathematical morphology `70`, codec morphology `45`, geometry morphology `10`, phase
morphology `14`, and characteristic morphology `16`. Native rest/remount preserves all five
accepted fibers, replays no source, and returns continuation `15001010` with integrity
`1323303867836747260`.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses six GPU kernels and 21 launched threads: one remount, sixteen independent
case probes, one theory closure, one returned-checker commit, one rest/remount, and one observation
thread. It transfers 8,752 bytes to and 141,440 bytes from the GPU and declares 151,216 resident
bytes. Host semantic events, engine source reads, exterior retrieval calls, and historical
renderer reads are zero. Engine time, checker time, temperature, power, and energy remain unknown.

All 62 CTest gates pass together after the complete test surface is built, including the R1-R19
GPU chain, host conformance, negative ownership/float/dependency/compatibility fixtures, actual
Lean calls, and architecture/PTX/SASS/binary audit. The suite exposed that R3's test still read the
growing current `CONSTRUCTION_STATE.md`; its three-source aperture had exceeded the frozen 16,384
bytes. The repaired test mounts the exact 3,464-byte R3 state fixture with its original
`03f28af...` hash. This freezes historical evidence instead of raising a bound or changing R3's
deed.

NVIDIA `compute-sanitizer` memcheck and initcheck each report zero errors on the complete R19
deed. Two repeated ordinary returns and both instrumented returns produce identical deed, source,
stdout/stderr, atlas, and native rest bytes. The source/binary audit passes with no forbidden
floating operation in the semantic/device cone.

Final SHA-256 values are executable
`4eea46671aab96cb4d048437b2d3ee98444aad2142f389443496d3c10ca7ca93`, PTX
`cdb97b5de2a43d54fb78ec226e84a5bd67e460b84f6a811ff48ab7f28b5e7d3d`, cubin
`1278015851ece1524cba06d2c4caecd998b769194c60da1dea05f481834aa59c`, generated source
`5b2a5f73a7efd417d700f36ce693a67e3190b4c60f1d2a8debe37d8b84cb63c3`, `.olean`
`638698caa57678d4f29c408442c30397993ea808801794b4d8075e61ea7c9bf6`, stdout
`6abe96de6ddf90115c63c78020e79204ec5d048325f9217a64176ea2833f01e9`, empty stderr
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, atlas
`00db43fdce9b0215af8916c672d2d2b6829bed0821472020a04e4134bcbb764b`, final rest
`52a7c4934f1a13b83fa53871d0430786d8a6e97697e52e0fdc3f508b03d859d4`, and complete deed
`aa7d47906a7c457badeed6008227efe74cc1ffb0ec975bc2a90880811f368024`.

R19 passes its bounded grade.

## Open continuation fibers

**Truth status:** `open`.

- R20 derives exact first-order connection matrices, Frobenius recurrences, resonance/logarithmic
  alternatives, singular-chamber connections, and loop monodromy from a bounded declared family
  of regular-singular equations.
- R21 uses the binary-code and GapCVP problems from OpenAI's ten released proofs as blind,
  source-separated characteristic reconstruction controls; comparison source remains absent
  until after return.
- R22 mounts a bounded CM norm-one translation/incidence stage of the released unit-distance
  mechanism and compares exact source factorization with the aperture-bound planar receiver.
- Higher-rank and noncommutative coefficient rings, algebraic roots, physical constitutive laws,
  and an RH-facing operator remain unformed.

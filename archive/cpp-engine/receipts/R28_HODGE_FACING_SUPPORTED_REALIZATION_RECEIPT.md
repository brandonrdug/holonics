# R28 Hodge-facing supported realization receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the frozen Legendre-product family, rank-six integral and
rational carriers, cup form, filtration, two-direction connection, infinitesimal cycle locus,
supported cycle-class fibers, translation correspondences, blow-up transport, changed-center
control, continuing-body commit, and native rest/remount; `computational-witness` for the actual
resident GPU return and complete atlas. The emitted Lean declaration has the separate
`proved-derived`, `formal-checked` grade. Smooth-projective Legendre/Kunneth comparison, the
de Rham/Hodge interpretation, cycle-class comparison, Griffiths transversality, and the standard
blow-up interpretation retain the separate `proved-standard` grade under the hypotheses frozen in
the roadmap. R23 and R24 are `historical` when used as regressions. Arbitrary Hodge structures,
global period maps, general supported realization, the Hodge conjecture, arithmetic Frobenius,
Weil positivity, and the Riemann hypothesis are `outside-aperture`.

**Aperture:** two copies of `E_z : y^2=x(x-1)(x-z)` at base `(t,u)=(2,2)`, one off-diagonal
control `(2,3)`, rank-six Kunneth and rank-seven blow-up carriers, two connection directions,
first-order jets in `Q[dt,du]/(dt,du)^2`, seven cycle generators, coefficient aperture `[-1,1]`,
denominators one and two, three receiver targets, four torsion translations, one negation graph,
and two blow-up-center selectors. Each class fiber enumerates exactly `3^7=2187` tuples. The
169-byte card contains only source coefficients and apertures; it contains no curve-family,
Kunneth, Hodge, connection, filtration, locus, cycle, graph, intersection, blow-up, theorem,
proof, or expected-invariant label/value.

## Construction tuple

| Field | R28 standing |
|---|---|
| Source owners | the one R27 continuing body with fourteen accepted fibers; one two-factor family/receiver/blow-up card; admitted exact expression, variation, causal-linear, and cycle organs; and new factor, product, correspondence, cycle-locus, realizer, blow-up, proof-face, checker-return, and native-rest owners |
| Port types | 928-byte R27 rest; cubic terms and roots, discriminants, rank-two factor carriers, rank-six product basis, integral/rational lattices, cup/polarization/filtration, two connection directions, jets and quotient obstruction, rational maps and graph supports, class fibers, blow-up maps and residuals, theorem/proof/explanation, checker return, changed center, and 984-byte native handoff |
| Event occurrence | remount R27; mount the source card; derive the two factors on independent resident currents; compose after their barrier; derive four translation currents; enumerate all three class fibers; form baseline and changed-center blow-ups; generate and check the returned theorem; commit its checker return; rest/remount the same body |
| Predecessor identity | R27 head `14001028`, continuation `15001028`, morphology `610`, mathematical morphology `184`, codec morphology `93`, expression-geometry morphology `37`, and fibers through `194300` |
| Local constitutive law | exact cubic differentiation and discriminant; exterior/Kunneth incidence; induced tensor connection; bilinear-form and filtration transport; first-order quotient reduction; cleared chord-addition identities; graph-class formation; complete bounded integer enumeration; Lefschetz primitive subtraction; direct-sum blow-up pairing with pull/push and residual |
| Receiver question | which rational classes have supported integral or half-integral algebraic realizers, how their filtered carrier moves, where the diagonal graph retains filtered type to first order, and which exceptional face separates equal pushforward classes after the center changes? |
| Returned consequence | complete factor/product/variation atlas, exact first-order locus, all retained realizers and obstructions, five correspondence supports, two rank-seven blow-up passages, generated theorem/explanation, acquired fiber `195300`, and source-detached continuation at head `14001030` |
| Open alternatives | arbitrary elliptic or abelian families, higher dimensions/codimensions, analytic periods, global Noether--Lefschetz loci, unbounded Chow/motive realization, the Hodge conjecture, arithmetic trace/Frobenius, Weil positivity, and RH-facing carriers |

## Moving cohomological geometry

**Truth status:** `established-bounded`.

Each resident factor derives its three roots, the monic discriminant

```text
z^2(1-z)^2
```

and the cleared connection

```text
2z(1-z) nabla_z = [[z,-1],[z,-z]].
```

The product current forms the ordered basis

```text
f1, f2, w1*w2, w1*e2, e1*w2, e1*e2
```

with cup matrix `H + H + (-H)`, polarization `h=f1+f2` of square two, filtration ranks
`F2=1`, `F1=5`, and comparison Hodge numbers `(1,4,1)`. Both induced `6x6` connection directions
preserve the cup form, commute, and carry `F2` into `F1`. These exact matrix returns establish the
bounded algebraic transport. The Hodge labels themselves use the separately declared standard
smooth-projective comparison and are not promoted into a proof of the Hodge conjecture.

The identity correspondence derives

```text
gamma = (1,1,0,1,-1,0).
```

Projecting its covariant derivative to `H/F1` returns the primitive first-order obstruction
`du-dt`. Thus `(dt,du)=(1,1)` closes, `(0,1)` does not, and the nonzero primitive coefficient gives
multiplicity one in the frozen dual-number aperture. This is a supported infinitesimal Hodge-locus
calculation, not a global period or locus assertion.

## Correspondences, fibers, and changed topology

**Truth status:** `established-bounded`.

The roots `0`, `1`, and `z` independently cause the three cleared chord-translation maps. Along
with identity they return four distinct supports whose induced `H1` map is identity and whose
graph class is `gamma`. The returned polynomial certificates prove the three curve identities and
differential-numerator identities. Negation induces `-I` and returns
`gamma_minus=(1,1,0,-1,1,0)`. The cup carrier then gives self-intersection zero, mutual
intersection four, `gamma+gamma_minus=2h`, and primitive square `-2`.

Complete enumeration of all 2,187 tuples per target retains sixteen realizers for `gamma`, sixteen
for `gamma/2`, and no realizer for `(0,0,1,0,0,1)`. Exactly four integral `gamma` realizers are
effective single graphs. The half-integral target has reduced denominator two and no integral
member; the third target returns an outside-image obstruction. Equal class vectors therefore do
not erase support or correspondence lineage.

Blowing up the selected zero-section point derives `Q direct_sum [-1]`, exceptional square `-1`,
`ker(push)=<E>`, complete pull/push identities, projection formulas, and exceptional residuals.
The graph through the center has strict class `pullback(gamma)-E` and square `-1`; the other
translation graphs keep square zero, although all push forward to `gamma`. Changing only the
center selector moves the negative strict face to the first nonzero translation while leaving the
rank-six class, filtration, and connection returns unchanged.

## Returned artifacts

**Truth status:** `proved-derived` for the Lean declaration and `established-bounded` for the
atlas.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib for the generated declaration;
`implemented-exact`, `computational-witness` for the atlas.

[`R28_GENERATED_HODGE_REALIZATION.lean`](R28_GENERATED_HODGE_REALIZATION.lean) is the 282-line,
21,954-byte source rendered from the returned coefficients. It proves the discriminant; every
entry of the cup-preservation and mixed-commutation identities; filtration ranks and
transversality; the cleared translation and differential identities; the locus, intersections,
graph relations, exact fiber separation, and blow-up pull/push, projection, and residual
relations. Its final ten-part declaration invokes no mounted theorem containing that conjunction.
Lean exits zero, returns 543 stdout bytes and zero stderr, and produces an 896,936-byte `.olean`.

[`R28_HODGE_REALIZATION_ATLAS.tsv`](R28_HODGE_REALIZATION_ATLAS.tsv) has 932 rows including its
header. Its 931 returned entries retain every factor source term, root, discriminant and connection
entry; product basis, cup, polarization, connection, filtration, and support entry; locus
coefficient; translation polynomial certificate and support; generator and class; all retained
realizer coefficients; both blow-up pairings, maps, strict classes, supports, and residuals. It is
the inspectable returned passage, not a diagnostic rendering or hash substitute.

## Continuing body

**Truth status:** `established-bounded`.

The accepted checker return commits Hodge-realization fiber `195300`, mathematical morphology
`207`, codec morphology `103`, Hodge morphology `41`, and body morphology `693`.
Source-detached rest/remount preserves all fifteen fibers, replays no source, and returns head
`14001030`, continuation `15001030`, integrity `5813231700886005822`, and a 984-byte native
handoff.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses eleven kernel launches, sixteen launched threads, fifteen semantic
threads, and zero host semantic events. Independent factor, translation, and changed-center
currents cross complete-current barriers before their products are formed. The CPU performs only
card/rest I/O, serialization, and the single exterior Lean crossing. Engine/checker time,
temperature, power, and energy remain unknown.

The pinned C++23/CUDA build passes the owner DAG, restricted types, file-size, source/PTX/SASS
no-float, manifest, linkage, ownership, compatibility, and binary audits. The full ordered suite
passes `89/89`, including every predecessor deed, the actual R28 GPU return, independent host
conformance, changed-center sensitivity, non-copyability, checker return, and final binary audit.
NVIDIA `compute-sanitizer` memcheck and initcheck each report zero errors. Two ordinary returns and
both canonically pathed instrumented returns produce byte-identical deed, atlas, Lean source,
`.olean`, checker outputs, and native rest. The aggregate final-artifact digest is
`bf07ea2d0e2c9645969361be39f3d794fbe2af4a50ddb4d15cc96402d7a7d271`.

Final SHA-256 values are:

```text
36f9c1a429fab2b239185fbc34168e9909ebcf4cce0f98015a33b0f891ac1a3e  executable
88237d95051af536fa43e9cd70bdb63003f30f418924d30e9f5b245e820f2313  PTX
813151b6c9f666aed0520e1506d80b364749121806ae1950f7cc0bbd42bdbed8  cubin
e5bedb2d76b469c0e53dbd6147d2ba1bfc19876ca13962f532e7fd16c2310b5e  deed
ee744537eaca3dea2dd5e86d9bedad4467e4bd6223be28c2d7f77abf1fb2cd7c  atlas
837a46e9ae4e3b90b6d0cea272624fabf8b1c0b5792a387fb36131b925e5453f  Lean source
c414ac6b5164d60adee37900a0d27345b992fcf508455f3d710819423c1d0c17  Lean olean
62e364b5efa4a524d8136d8fde1748040a196489c28e57de5e8e398e71cc20a2  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
57d68c29004079b27ba6e28c895e7e94437c081752479eb19f77eef94ee4c1e0  native handoff
```

## Open continuation

**Truth status:** `project-postulate`.

R29 must now construct the arithmetic half of the common Hodge/RH spine in an exact finite-field
world where geometry causes the trace operator and placement. It must return source and
correspondence, fixed-locus counts, a genuine carrier and pairing, characteristic/local factor,
duality, exact weight placement, and a bounded explicit-formula receiver with full lineage. A
matrix fitted to known counts or zeros, finite critical-line testing, or a restatement of the
functional equation is not the next deed.

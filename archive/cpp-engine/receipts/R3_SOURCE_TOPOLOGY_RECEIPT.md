# R3 incremental source topology and chart navigation receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the admitted source environment, parallel founding,
field-by-field resident comparison, projection chart, projective transition, and detached query
aperture; `computational-witness` for the returned selected-source population. Apparatus identity
is separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** at most four sources, 16,384 byte occurrences, 16,384 sparse directed relations,
2,048 chunk summaries, four mount variants, 256 exact byte-projection faces, and 256 64-bit support
words per face. This receipt does not establish an unbounded mount theorem, learned morphology, or
a continuing body.

## Construction tuple

| Field | R3 standing |
|---|---|
| Source owners | move-only `codec::source_environment`, one `structure::resident_marked_population` owner per bounded admission branch, and one `receiver::resident_projection_chart` per population; `apparatus` owns only paths, I/O, device allocation, and crossings |
| Port types | bounded path-free encoded source environment, source spans, chunk/cut lineage, marked occurrence/relation population receipt, projection-preimage/local-support/transition/overlap/obstruction receipts, and combined apparatus return |
| Event occurrence | chunk-local byte and relation founding, exact boundary-summary joining, parallel resident admission comparison, source-environment departure, then declared detached receiver navigation |
| Predecessor identity | Git `c202834141907b01dced5d9632e211c8fc1ad20b` plus the exact source aperture below |
| Local constitutive law | owner-reserved typed identity spans, intrinsic byte cuts, adjacent byte-pair relation, exact cross-cut boundary join, byte-face projection, radius-two sequence restriction, and exact projective chart transition |
| Receiver question | the complete preimage of byte face 35, the radius-two neighborhood of declared occurrence 20,000,000, its two chart faces and transition, plus the empty preimage of face zero |
| Returned consequence | native resident source occurrence/incidence, exact variant/direct-oracle equality, preimage/support/transition/overlap/obstruction receipts, detached source-access audit, and PTX/cubin/SASS artifacts |
| Open alternatives | a live continuing owner, returned-event atomicity, native rest/remount, and every R4-and-later deed remain absent |

## Real selected population

The exterior file-store adapter reads these three complete files:

- `CONSTRUCTION_STATE.md` from the current repository standing;
- `papers/source/synopsis/README.md` from the paper source population; and
- `formal/elementary-holonics/ElementaryHolonics/Foundation/Receiver.lean` from the formal source
  population.

Their final byte counts and SHA-256 values are:

| Selected source | Bytes | SHA-256 |
|---|---:|---|
| `CONSTRUCTION_STATE.md` | 3,464 | `03f28af704c1c4d35f82a473d21184ef6e4dbd934e295f852173edf4dd5a317c` |
| `papers/source/synopsis/README.md` | 6,811 | `a341fafef56aea7414933fdc1fdecdf15bc635c1d394276fdfec3485b1520474` |
| `formal/elementary-holonics/ElementaryHolonics/Foundation/Receiver.lean` | 2,934 | `61db8ed19fb992b19938073ffdab51a343fd7e1a3cfe885832bb37d5da08b1a5` |

Paths cross only in the apparatus store receipt. The path-free encoded environment carries source
spans, bytes, chunk cuts, and owner-minted lineage. A second adapter branch copies the same complete
bytes to a different physical directory; exact byte comparison succeeds while path testimony is
distinct.

Four owner-identical admission branches use chunk apertures 31, 64, 127, and 257 bytes with
canonical, reverse, even/odd, and rotated ingestion orders. Chunk threads found all byte
occurrences and within-cut adjacency in parallel. Exact first/last/source/cut summaries found only
the relations crossing chunk cuts. No chunk identity, descriptor order, path, worker lane, address,
or digest enters semantic occurrence/incidence.

## Exact admission and navigation return

The card compares every source mark, occurrence field, and relation field across all four mounted
populations. A separate bounded direct oracle on the card reconstructs every expected occurrence
and adjacent pair from the still-admitted encoded source before that source departs. The returned
source, occurrence, relation, and direct-oracle mismatch counts are all zero. Reported folds remain
observer checksums only and do not grade equality.

The source environment is then erased on the host, its device allocation is freed, and a fifth
kernel receives only the resident marked populations, receiver charts, and small admission
receipts. Face 35's projection preimage is read through its one resident bit fiber. Local navigation
starts from the explicitly declared owner-minted occurrence identity 20,000,000; no kernel or host
ranks a matching occurrence. Its support receipt is a strict restriction of the selected source
face. Exact matrix transport returns the sequence/projected homogeneous pairs and a nonzero signed
determinant. The same occurrence is witnessed in both chart faces. Face zero returns an explicit
empty-preimage obstruction. The query reports zero visible source bytes.

## Returned device artifact

The complete decoded artifact is `build/r3/receipts/R3_SOURCE_TOPOLOGY_DEED.txt`; its final SHA-256
is `ffff3b404cb532f9bc18b0600e827bb2c20fb1c384f9898f07dda78fb597345b`. Its summary is:

```text
truth_status=established-bounded
evidence=implemented-exact,computational-witness
program=r3_incremental_foundation+detached_navigation.sm_89
source_count=3
source_bytes=13209
semantic_occurrence_invariant=1
semantic_relation_invariant=1
source_mismatches=0
occurrence_mismatches=0
relation_mismatches=0
direct_oracle_mismatches=0
detached_query_returned=1
device_compute_capability=8.9
kernel_launches=5
source_bytes_visible_to_query=0
environment_failures=0
verification_failures=0
```

## Artifact hashes

Two independent build directories return identical bytes for the normalized deed executable,
three PTX/cubin pairs, and decoded semantic deed artifact at the same source aperture.

The final graded source aperture SHA-256 is `cf1cb583cc24fc7f66aa1eb2878328dd15ec5de818ecf5ff424b3d2c128abf0b`.

| Artifact | SHA-256 |
|---|---|
| normalized source-topology deed executable | `83c1f43193c505c32b2e3a0388963a92d8d34ed774501f65ea9c88dea8f1a8d2` |
| source-founding PTX | `142b321f7b5e78a2d5efdaa444ed8059cccccaff397d4c6dc65bdeb3b5108145` |
| source-founding cubin | `09c370f6a2d3fa2c600a7f26fbab4e9029a0903d83e52b8ba35ef92fde4f09d9` |
| detached-navigation PTX | `b23f2bfb194e93206b82715a34eea206d8c9e53c9df208fd7c5f1e613e7953d9` |
| detached-navigation cubin | `b9c6421cd62c245e76aeab0d906cccf9fc5dfa53130865e1e786080f1cf7ba8d` |
| exact-admission PTX | `0730375983bddc00a84a3aca03485a445ded8e934a7062df0f5ce98731d4bf11` |
| exact-admission cubin | `3c671621f0360b69ba5e5f7e7719522e7375728c14a3299318e1ab9ea90bff22` |
| complete decoded source-topology artifact | `ffff3b404cb532f9bc18b0600e827bb2c20fb1c384f9898f07dda78fb597345b` |

## Gate returns

- Architecture, owner-DAG, source-size, no-float, no-compatibility, copy-ownership, and build audits
  pass before and after construction.
- Both release build directories pass all 15 CTest gates. The executable, three PTX/cubin pairs,
  and decoded deed artifact are byte-identical.
- The independent host verifier checks complete chunk coverage, exact cross-cut adjacency against
  the bounded direct oracle, all four chunk/order variants, relocated-store byte equality and path
  distinction, projection preimage, declared local support, transition, overlap, obstruction, and
  source detachment.
- The five-kernel card deed passes NVIDIA `compute-sanitizer` memcheck and initcheck with zero
  errors.
- The exterior adapter and independent verifier pass host AddressSanitizer and
  UndefinedBehaviorSanitizer with leak detection enabled.
- Clang 22's static analyzer reports no diagnostic over the project-owned adapter, deed,
  conformance, verifier, artifact, and included production-law aperture.
- PTX and final SASS scans find no floating type, conversion, arithmetic, comparison, or special
  function instruction in any R3 device artifact.

## Apparatus and resource boundary

The CPU reads and relocates complete serialized sources only through the named exterior adapter,
constructs bounded chunk/cut testimony, transfers one path-free environment, and independently
grades the returned finite witness. It neither founds resident incidence nor replays the query.
The GPU retains the marked population and receiver charts after both encoded source owners depart.
Physical bytes, thread counts, store paths, and placement remain apparatus testimony and do not
enter identity, incidence, chart equality, or the returned semantic consequence.

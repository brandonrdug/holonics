# Phases 0–5 receipt — excision, minimum carrier, substrate, Swing, machine, information receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the carrier, substrate, Swing, Standing, live machine, rest,
and causal-information receipt; `computational-witness` for the GPU deed, exact device/host parity,
and the audits. The excision and regrade are `established-bounded` by direct source inspection.
The learning ecology itself remains `open`: nothing here conditions a body.

## Phase 0 — excision and regrade

The conditioning layer implemented the mechanism the canon convicts and has been removed rather
than deprecated: a linear scorer with weights, bias, and threshold; monotone weight overwrite as
training; and a returned-fiber "ablation" that subtracted hardcoded constants from counters while
its refusal actually came from a default-constructed record.

Deleted: `organ/conditioning_{law,schema,receipt}.hpp`, the conditioning apparatus, executor,
resident owner, rest record, compile contract, conformance test, kernels, and build entry;
`event/returned_fiber_exclusion_law.hpp`.

Renamed: every `*_morphology` counter to `*_admitted_tally` across 29 identifiers and 96 files.
**Morphology may never again name a counter.**

Replaced honestly: the R15/R16 device deeds needed the projection. It is now
`event/absent_fiber_control.hpp` — **a control, not an ablation.** It withholds the acquired
fiber's identity so a dependent theorem must refuse, copies the predecessor's tallies through
untouched, and its receipt says in its own text that it neither conditions a body nor removes
structure from one. The R15 assertion that the ablation head was one less than the production head
is replaced by the true statement that the control leaves the head unchanged.

**Withdrawn from the floor:** returned training changing reusable native morphology; exact ablation
removing later conduct; source-detached conditioning.

## Phases 1–5 — the spine

| Phase | Owners | Law |
|---|---|---|
| 1 carrier | `structure/{carried_occurrence,oriented_incidence,directed_dependency,local_transport,chi_pair,disposition,transition_invariants}.hpp` | `K = (O, I, D, T)`; OPEN first-class; Chi as the ordered transport pair; the eight transition invariants |
| 2 substrate | `structure/{sparse_ordinal_atlas,local_population,relation_atlas,branch_lineage,causal_membrane}.hpp` | 32-bit ordinals never reused, tombstones, recoverable refusal, frozen spans, fork-shares-prefix |
| 3 Swing and Standing | `body/{standing_surface,swing}.hpp` | path-copy persistence; RIDE / FOUND / OPEN at two layers |
| 4 machine and rest | `body/{live_machine,live_rest}.hpp` | one owner, aperture absent from rest and identity, stale predecessor refused, unknown wire fails closed |
| 5 information receipt | `receiver/receiver_current_law.hpp`, `current/{continuation_fiber,information_receipt}.hpp` | factorized reach, three continuation fibers, minimal witness families, the two modes of return |

## Returned artifacts

The deed ran on the resident card at compute capability `8.9` over eight declared occurrences,
crossing `576` bytes out and `384` back, with **zero device/host parity failures** and every named
law family holding: `swing_laws`, `standing_shares`, `open_never_concludes`, `carrier_laws`,
`substrate_laws`, `information_laws`.

**The Swing laws, as returned.** A flat comparison RIDEs and re-bases the groove. **A wound
comparison at the production layer stands OPEN and cannot manufacture a FOUND**; the same input at
the body layer founds, deposits exactly one winding quantum, and does **not** re-base the groove at
the cut. A missing interface capability is not a candidate. An undeclared chart, a wrong declared
chart, and an oriented hand residual each stand OPEN. Every OPEN retained its complete pair and
refused to conclude.

**Standing shares.** Replacing one cell in a six-cell surface copied four nodes; the predecessor
surface retained all six cells and its own form at the replaced address.

**Reach is factorized.** A layered lattice of width eight and depth thirty reached a population of
`2^87` paths, carried exactly in two limbs, in **1,864 passage visits** over 1,920 passages. The
paths are never enumerated and the population never collapses into a machine word.

**The receipt is mandatory and refuses.** Three continuation fibers sampled at three times; a
restriction and a base change classified as different events; inclusion-minimal witness families
with supersets refused and subsets evicting; and a receipt with a complete diagnostic but **no
returned artifact is refused as incomplete**.

**Sufficiency.** An empty radical over a declared receiver family yields a restriction; a nonzero
radical yields a base change, because the family asks about directions it cannot distinguish.

## Gates

Architecture, no-float source, container-ownership, compatibility-route, derived-identity,
owner-classification, owner-dependency-DAG, and file-size audits pass. PTX and SASS no-float pass.
Host conformance is clean under `-Wpedantic -Werror -Wconversion` with address and
undefined-behaviour sanitizers. Every occurrence re-executes bit-identically. **Full suite
`137/137`.**

One apparatus reservation was required and is recorded as such: the persistent standing surface is
a value in the kernel frame, so the per-thread device stack is sized explicitly at 16 KiB rather
than left to a default that refuses the deed. That is a physical reservation, not semantics.

## Boundary

This is a spine, not a learner. Nothing here conditions a body, mounts a corpus, generates, trains,
or returns a proof. Phases 6 onward port the ecologies; until then the withdrawn claims stay
withdrawn.

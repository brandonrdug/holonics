# Conditioned mathematical production

**Truth status:** `established-bounded` at the declared aperture.
**Evidence:** `implemented-exact` for the laws, `formal-checked` for both kernel acceptances and the
foil's rejection, `computational-witness` for the counts.

**This is the deed the Rust laboratory did not complete.** `SESSION_HANDOFF.md:18-20` records the
2026-08-03 attempt as interrupted with its partial conditioning not admitted. It is the first place
this body leads rather than ports.

## What was returned

```text
declarations mounted                        3
occurrences after mounting                  3      states 209

first target reached                      yes      span 1
first source octets                       382
first kernel acceptance                   yes      45,576 produced octets

occurrences after emanation                 4      states 298
composition route observations              2      conducts yes

second target reached                     yes      span 1
second source octets                      599
second kernel acceptance                  yes      46,904 produced octets

foil, same proof term, first absent    refused

occurrences after exclusion                 3      states 209
composition route removed                 yes
second target reachable                    no
composition route conducts                 no
second theorem emitted                     no
```

## The mechanism

**The intermediary is the material, not a flag.** The first theorem's kernel acceptance is admitted
back into the standing as an emanated occurrence with its causing declaration supplied, and the
ecology is re-formed over the larger population — three occurrences and 209 states become four and
298. A later proof reaches that theorem the same way it reaches any material: by following its
octets through the suffix ecology and reading the source span the depth-first walk already knew.

**The emitted proof term is composed from the standing.** The second theorem's name is not a
literal in the renderer. It is read out of the retained surface of the occurrence the span names,
octet by octet, and spliced into the emitted text. A body that cannot reach the name cannot form
the source.

**Two independent structural gates.** The material must be reachable, and the composition route
must have recurred across distinct occurrences — the training minimum is two and cannot be lowered.
Excluding the emanated occurrence kills the first; ablating the route's fiber kills the second. A
deed that survived one exclusion by the other would not have proved its dependency, so both are
required and both are checked.

**The exclusion is structural.** The emanated occurrence leaves the standing, its surface leaves
with it, and the ecology is re-formed from the root over the smaller population — back to exactly
three occurrences and 209 states. No counter is decremented and no flag is set. This is what the
excised `returned_fiber_exclusion_law` was imitating with `morphology -= 5U`.

**The kernel agrees, independently.** The foil carries the second theorem's proof term with the
first theorem's declaration absent. The exterior checker refuses it. So the dependency is witnessed
twice by unrelated means: the body will not emit, and the checker would not accept.

## Boundary

- **The mathematics is modest and is not the grade.** Both theorems are trace identities over
  `Matrix (Fin 3) (Fin 3) Int`; the first applies `Matrix.trace_mul_comm` and the second applies the
  first. The grade is the **dependency mechanism**, per the standing rule that the grade of the
  organ is not the grade of the return. The body's novel mathematics stands elsewhere.
- **Three declarations, not 1,164.** The laboratory's declaration population is not claimed here.
- **The source skeleton is authored; the reached name is not.** The statement forms and the proof
  shapes are written in the exterior codec. What the body supplies is *whether* to emit, *which*
  target, and *the name it splices*. Composing a whole proof from reached material is established
  separately by the existing renderers over the exact organs; joining the two is the next deed and
  is not claimed here.
- Conditioning runs on host arenas. The identical arena laws cross to the resident card and that
  crossing is graded by its own deed; this one does not re-establish it.

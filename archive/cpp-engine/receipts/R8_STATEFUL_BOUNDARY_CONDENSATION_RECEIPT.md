# R8 stateful boundary condensation receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the versioned receiver families, retained source fiber,
factorized boundary body, complete history-indexed bisimulation, and receiver-family refinement;
`computational-witness` for the actual resident-card return and independent decoded oracle
comparison. No claim extends beyond the declared finite sources, histories, input languages, and
receiver families. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** one eight-cell source fiber, two versioned future receiver families containing two
then three exact linear query faces, four admitted input occurrences, an initial three-factor
boundary, a refined four-factor boundary, and one retained exact reconstruction capability.

## Construction tuple

| Field | R8 standing |
|---|---|
| Source owners | R7's receiver standing, R4's non-copyable continuing body, the event occurrence owner, and one new non-copyable `event::resident_condensation` owner containing the sole live source fiber and factorized boundary |
| Port types | versioned future-family, supported input occurrence, source-cell delta, factorized response, reconstruction capability, refinement occurrence, complete successor, obstruction, lineage, and logical-resource ports |
| Event occurrence | four lineaged inputs cross the declared receiver ports; after the second history a returned refinement occurrence consumes the same continuation, reconstructs from the retained source fiber, and replaces the three-factor boundary with a four-factor boundary |
| Predecessor identity | Git `333ef6f3a0f8901a6d3d7a4c5cd6ec0280f83c5b`, continuing head `8001000`, and condensation-program identity `8001` |
| Local constitutive law | each declared query must be constant on every boundary factor; exact group sums produce the condensed response, the full source produces the admission-audit response, and both apply the same typed delta to a complete successor snapshot |
| Receiver question | whether direct and condensed histories admit the same next inputs and return equal testimony, obstruction, complete successor, morphology, lineage, and deterministic resource standing before and after receiver-family growth |
| Returned consequence | four exact direct/factorized response pairs and four field-complete successor pairs, followed by a retained-fiber refinement from family `8101/1` with three factors to family `8102/2` with four factors |
| Open alternatives | R9 codec faces, reflection, returned codec revision, source-departed remount behavior, and unrelated exterior syntax remain absent |

## Returned history certificate

The initial source values are `[2,3,5,7,11,13,17,19]`. The first family factors those cells into
three groups and admits two query faces. History 0 changes source cell `802` by `2` and returns
`182` through both the direct and three-factor paths. History 1 changes cell `805` by `3` and
returns `434` through both paths. Each comparison includes the full source identities and values,
head, incidence, morphology, current, alternatives, obstruction, lineage, and logical resource;
it is not an output-only comparison.

Family `8102/2` adds a third query whose weights distinguish source cell `801` from cells `802` and
`803`. The old factorization is therefore not reused. Refinement occurrence `8002099` consumes the
same body's continuation, reopens the retained source fiber under reconstruction capability
`8009001`, rebuilds four exact factors, advances head `8001002→8001003`, and retains explicit
lineage and resource testimony. History 2 then changes cell `801` by `5` and returns `795` through
both paths; history 3 changes cell `808` by `7` and returns `214` through both paths. The final
source is `[7,5,5,7,14,13,17,26]`, with head `8001005`, morphology `27`, current `214`, lineage
`120`, logical resource `5`, unchanged incidence `8001001`, and two retained alternatives.

The broad direct source evaluation is executed on-card only as the bounded admission oracle. The
operative condensed path reads the smaller exact factor body. No hash, embedding, cache,
approximation, loss, filename, or host replay stands in for the history certificate or source
fiber.

## Resident card passage and apparatus boundary

The apparatus mounts one condensation program and its continuing regions, then releases the
exterior mount after the mount kernel. One resident kernel performs all four history deeds and the
intervening refinement; a final observation kernel returns the bounded receipt. The host performs
zero semantic events. The execution uses three kernels and three launched threads, transfers `960`
bytes to and `2464` bytes from the device, and records an exact resident allocation interval of
`6024` bytes. Temperature, power, elapsed-time, and energy telemetry remain unknown.

## Returned artifact and grade

The complete decoded artifact is
`build/r8/receipts/R8_STATEFUL_BOUNDARY_CONDENSATION_DEED.txt`, SHA-256
`da01facb9a3de595b375a37918f1d008561bcaa533b37a0c30e28c27ac489f2c`. It reports
`history_count=4`, `factorized_boundary=1`, `stateful_bisimulation=1`,
`source_fiber_retained=1`, `non_resumable=1`, and `verification_failures=0`.

- All 30 CTest gates pass, including every prior phase, the real R8 card deed, host conformance,
  forbidden owner copy, negative architecture fixtures, and whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis return clean.
- Architecture, no-float source/PTX/SASS, ownership, compatibility, file-size, and link audits
  pass.
- Two clean Ninja configurations return byte-identical deed executable, PTX, cubin, and decoded
  artifact.
- Final SHA-256 values are executable
  `7cbc95381d1b57e41f463ac8d68dd99617e7f4170f3e1411c6e44ca19e9e05f2`, PTX
  `920858cdf702168869b73b144df238799223bdb1cdda6b29997f52971e25c4c8`, and cubin
  `41b378d585b5bd1c5d777ad2a9841735340765697e93469dfcbbc4a347e9d936`.
- The final source aperture SHA-256 is
  `9dafec0084072e73d7040e3a377a43f3de5bd5b2cc25f166f487c87d2d7ab3e7`.

R8 passes its bounded grade. R9 is the next deed; no codec or reflection capability is claimed
here.

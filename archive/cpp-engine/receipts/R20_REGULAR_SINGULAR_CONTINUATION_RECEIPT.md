# R20 regular-singular characteristic continuation receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the differential-to-first-order derivation, chart-local
Frobenius fronts, resonance image/cokernel calculations, formal logarithmic carrier, chamber
connection, loop products, body commits, and native continuation; `computational-witness` for the
returned GPU atlas and actual artifacts. The generated Lean passage has the separate
`proved-derived`, `formal-checked` grade below. Classical Frobenius and analytic-continuation laws
under the declared branch hypotheses retain `proved-standard`. Apparatus identity remains
separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** the exact Gauss `(a,b;c)=(1,1;2)` scalar operator, the state chart
`X=(y,z y')`, three singular chambers, twelve coefficients in each bounded Frobenius front, two
rank-two local bases, one overlap path through `0<z<1`, three based loop words, and matrices whose
entries are linear polynomials in one formal period symbol `omega`. R20 does not establish
arbitrary parameters, convergence outside the declared germs, numerical period values, irregular
singularities or Stokes data, physical wave/crystal behavior, the released OpenAI results, the
unit-distance construction, or an RH-facing operator.

## Construction tuple

| Field | R20 standing |
|---|---|
| Source owners | the one R19 continuing body and its five acquired fibers; exact integer, rational, rank-two-matrix, and formal period carriers; the mounted scalar differential occurrence; first-order-system, Frobenius, resonance, chamber, and monodromy organs; formal/explanation codecs; pending checker deed; and exterior checker/rest apparatus |
| Port types | 432-byte R19 native rest; unlabelled continuation question; scalar polynomial coefficients; three chart germs; residue, recurrence, image/cokernel, connection, and path words; atlas, proof, and conversational faces; raw/typed checker return; and 488-byte continuing rest |
| Event occurrence | remount R19; derive the Fuchsian system on the GPU; form three chart receipts and twelve coefficient fronts on independent threads; close resonance, chamber, and loop products; form proof/explanation passages; receive one real checker occurrence; commit its returned difference; rest/remount the same body |
| Predecessor identity | R19 head `14001010`, continuation `15001010`, body morphology `217`, mathematical morphology `70`, codec morphology `45`, geometry morphology `10`, phase morphology `14`, characteristic morphology `16`, and five accepted fibers ending in characteristic fiber `186300` |
| Local constitutive law | exact coefficient comparison in the mounted scalar equation; Laurent/Frobenius substitution in each chart; image/cokernel projection before resonant inversion; formal log transport `log -> log+omega`; exact basis recharting; and ordered loop multiplication |
| Receiver question | which residues, eigenflags, recurrence sources, resonance obstructions, log companions, chamber transitions, and loop returns are forced without a special-function lookup, numerical continuation, expected log, or expected eigenvector? |
| Returned consequence | exact regular-singular atlas, five-theorem Lean passage, accepted `.olean`, conversational analysis, acquired fiber `187300`, and continuing head `14001012`/continuation `15001012` |
| Open alternatives | arbitrary Gauss parameters and algebraic exponentiation; certified analytic remainder bounds; generalized/confluent and irregular systems; higher rank and Stokes data; blind reconstruction of the released mathematics; CM norm-one incidence; and RH-facing operators |

## Historical and predecessor boundary

**Truth status:** `historical`.

The old laboratory had already produced hypergeometric/pathway language, phase renderings, and
receiver-relative geometric figures. R18 had already returned the bounded coefficients
`1/(n+1)`, and R19 had already returned the indicial polynomials and the elementary distinction
between equal characteristic polynomials and unequal fixed-current conduct. R20 does not report
any of those as its new outcome, nor does it repeat the basic continuing-body mechanism as an
experimental expectation.

The new return is the exact causal location of the logarithmic channel inside the transported
differential recurrence. It distinguishes two integral resonances of the same first-order system
by their actual recurrence source, image, and cokernel, then carries the resulting local basis
through a declared overlap and three lineaged loops.

## Derived first-order geometry

**Truth status:** `established-bounded`.

The mounted source is

```text
z(1-z)y'' + (2-3z)y' - y = 0.
```

With the declared state `X=(y,z y')`, coefficient comparison on the card returns

```text
X' = (A0/z + A1/(z-1)) X,

A0   = [[ 0,  1], [ 0, -1]],
A1   = [[ 0,  0], [-1, -1]],
Ainf = [[ 0, -1], [ 1,  2]] = -A0-A1.
```

These matrices are derived from the mounted scalar coefficients; they are not mounted answer
data. The chart choice is retained because the first-order residue exponents depend on the state
gauge even when the scalar indicial polynomial is unchanged.

The exact characteristic receivers are:

| Chamber | Residue trace/determinant/discriminant | Eigenstructure | Returned local cause |
|---|---|---|---|
| zero | `-1,0,1` | eigenvalues `-1,0`, lower vector `(1,-1)` | integral gap one; resonant source `(0,0)`; obstruction `0` |
| one | `-1,0,1` | eigenvalues `-1,0`, lower vector `(0,1)` | integral gap one; resonant source `(1,-1)`; cokernel `(1,0)`; obstruction `1` |
| infinity | `2,1,0` | repeated eigenvalue `1`, eigenvector `(1,-1)`, generalized vector `(0,-1)` | nilpotent part `Ainf-I` has rank one |

Thus a residue discriminant, a scalar indicial discriminant, an integral eigenvalue gap, a
recurrence obstruction, and a monodromy nilpotent rank are nonidentical receivers. In particular,
both finite residues have distinct eigenvalues separated by one. Zero closes without a log
obstruction because its transported source is in the recurrence image. One opens the log channel
because the caused source has exact nonzero cokernel component `1`. The relevant “leader” is this
source-to-cokernel incidence, not the word *resonance* or a vanishing discriminant alone.

## Frobenius and logarithmic fronts

**Truth status:** `established-bounded`.

At zero the exact recurrence is

```text
(n+rho)(n+rho+1) a_n = (n+rho)^2 a_(n-1).
```

The analytic branch returns `a_n=1/(n+1)`, while the second independent fiber is `z^-1`.
R20 retains the R18 coefficient branch as predecessor evidence and newly returns its placement in
the complete two-dimensional local fiber.

At `t=1-z`, substitution returns

```text
(n+rho)^2 (b_n-b_(n-1)) = 0.
```

The regular channel has `b_n=1`. Differentiating the exponent fiber at the repeated scalar root
returns the formal logarithmic companion with log coefficient `-1` at every bounded degree. The
twelve GPU term receipts retain both channels separately and verify the recurrence by exact
integer cross-products. No coefficient is obtained from a named special-function evaluator.

## Chamber connection and monodromy

**Truth status:** `established-bounded`.

On the declared overlap path inside `0<z<1`, use the ordered bases

```text
B0 = (-log(1-z)/z, 1/z),
B1 = (1/z, -log(1-z)/z).
```

The returned connection is the exact swap

```text
C01 = [[0,1],[1,0]],     det(C01)=-1,     C01^-1=C01.
```

The path, both basis identities, inverse, and source lineage remain in the atlas. The formal
period carrier records a positive logarithmic loop by `log -> log+omega`; it never approximates
`omega`. In the `B1` basis the three returned loop matrices are

```text
M0   = [[1,0],      [0,1]],
M1   = [[1,-omega], [0,1]],
Minf = [[1, omega], [0,1]].
```

Their distinct loop words have distinct lineages. Exact multiplication returns
`M0*M1*Minf=I`, and conjugation through `C01` returns the lower-triangular one-loop matrix in the
zero basis. All three matrices have characteristic factor `(X-1)^2`; `M0` fixes both basis
directions, while `M1` and `Minf` fix only the regular eigenline and carry the log direction as a
generalized vector. Classical specialization of the formal increment to `2*pi*i` is
`proved-standard` analytic testimony under the declared orientation; no floating or complex
approximation enters the device cone.

## Returned mathematical artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by the pinned Lean 4.27.0/Mathlib environment.

The complete generated passage is retained at
[`R20_GENERATED_REGULAR_SINGULAR.lean`](R20_GENERATED_REGULAR_SINGULAR.lean). It proves the residue
sum and nilpotent-square identities, the zero and one Frobenius steps, the concrete resonant
source/cokernel pairing, the formal punctured-sphere loop product, and chamber conjugacy. The
analytic existence theory for Frobenius/log branches remains `proved-standard`; the generated
finite algebra is not reported as a new general proof of that theorem.

The actual checker returns exit `0`, 346 stdout bytes, zero stderr bytes, one typed declaration
occurrence, no remaining goals, and a 526,176-byte `.olean`. The accepted occurrence commits
formation head `14001010->14001011` and checker-return head `14001011->14001012`. Body morphology
is `244`, mathematical morphology `79`, codec morphology `48`, geometry morphology `10`, phase
morphology `14`, characteristic morphology `16`, and regular-singular morphology `20`. Native
rest/remount preserves all six accepted fibers, replays no source, and returns continuation
`15001012` with integrity `16609905984591176881`.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses seven GPU kernels and 20 launched threads: one remount, three chart
currents, twelve coefficient currents, one theory closure, one returned-checker commit, one
rest/remount, and one observation thread. It transfers 8,856 bytes to and 55,960 bytes from the
GPU and declares 66,000 resident bytes. Host semantic events, engine source reads, exterior
retrieval calls, and historical renderer reads are zero. Engine time, checker time, temperature,
power, and energy remain unknown.

All 65 CTest gates pass together after the complete test surface is built, including the R1-R20
GPU chain, host conformance, negative ownership/float/dependency/compatibility fixtures, actual
Lean calls, and architecture/PTX/SASS/binary audit. NVIDIA `compute-sanitizer` memcheck and
initcheck each report zero errors on the complete R20 deed. Repeated ordinary and instrumented
returns produce identical deed, source, stdout/stderr, atlas, and native rest bytes.

Final SHA-256 values are executable
`afa06565f9d8b9225610691f4a946b6de29df912bc87162ac734c1b2cde732f9`, PTX
`3a40e1d9745221d24b33a000f59148e0715250b49256daa3ff9a18333e73df87`, cubin
`38d423a18b8b177e192eaea75f4242696eb221296dd47c8608825d7af4c32280`, generated source
`5fd9e7b195bdf8859b8dc74d2e163e6bfaa5e6d14de004821c0f78c88929efae`, `.olean`
`7b8804dad2d906e978097f4a8cd035e5887aca65991f17314b14bd8d7f499b0b`, stdout
`19391706014a6ae3a35c24acd363373a23eded3918735b2c1c6a676e14958ccb`, empty stderr
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, atlas
`d90e93030c2b85bf1e3ca9c2f0d53ac3bc44f04670947eda4aeb241fdd3ed690`, final rest
`4ebd19be29b9a82933b4911f71c69f2e9196846ca04fa7547a11bf7e7595e51b`, and complete deed
`acf53f8fd4e0855fac7c1353fb039fd5b7ec0db193de0cb2182cb1baca986302`.

R20 passes its bounded grade.

## Open continuation fibers

**Truth status:** `open`.

- R21 performs blind, source-separated reconstruction controls from the binary-code and GapCVP
  problems in OpenAI's released ten-proof collection. Released proofs and walkthroughs remain
  absent until after the machine returns its own caused passages.
- R22 mounts a bounded CM norm-one translation/incidence stage of the released unit-distance
  mechanism and compares its exact source factorization with the aperture-bound planar receiver.
- Arbitrary regular-singular parameters, algebraic roots, convergence certificates, irregular
  singularity/Stokes transport, physical calibration, and an RH-facing operator remain unformed.

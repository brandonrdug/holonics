# R7 core receiver geometry receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for seven mounted receiver instruments and their retained
counterexamples/obstructions; `computational-witness` for the actual resident-card return and
independent decoded oracle comparison. Standard projective, connection, knot, and hypergeometric
theorems are not re-proved or enlarged by this finite deed. Apparatus identity remains separately
`measured` in [`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** one continuing R6 weave of nine cells and ten events, one four-point field swing and
its degenerate/non-field alternatives, two compared marked diagrams, two projection receivers over
four source occurrences, one four-edge declared connection loop plus one open path, four exact
Gauss coefficients, canonical line/sheet/cube boundary data, and one rational local
metric/connection example. No claim extends beyond these explicit receivers.

## Construction tuple

| Field | R7 standing |
|---|---|
| Source owners | R6's non-copyable resident weave, the exact carrier, mounted receiver charts, and one new non-copyable `receiver::resident_geometry` grading-organ owner |
| Port types | exact projective frame/path, typed sameness witnesses, projection/aperture/preimage, lineaged connection path, symbolic solution fiber, oriented carrier/contact, rational metric/clock, current/stress, and obstruction ports |
| Event occurrence | after the resident R6 current reaches head `6001007`, seven mounted grading organs return disjoint receiver testimony in one card kernel; none owns or clones the continuation |
| Predecessor identity | Git `0ca154083d74569b6a8f150f9742a41cc86890ce`, current head `6001000`, and receiver-program identity `7001` |
| Local constitutive law | exact shift/add multiplication with refusal; receiver maps retain forgotten fibers and paths; curvature is emitted only for a named plaquette boundary; solution transport retains family/branch/path/monodromy; boundary cancellation is signed incidence; the local metric is a declared rational pair with fixed Lorentzian sign convention |
| Receiver question | whether each of the seven roadmap distinctions and its named counterexample/open case survive the actual current passage and complete decoded return |
| Returned consequence | one current successor plus all seven exact deeds with zero oracle disagreements and explicit non-universality, preimage, path, family, presentation, domain, and missing-fidelity testimony |
| Open alternatives | R8's versioned future receiver families, stateful boundary bisimulation, retained reconstruction, and refinement remain absent |

## Seven returned deeds

1. **Projective swing.** The affine points `1,2,4,8` return homogeneous swing `[18:14]`. The
   changed frame `(3x+1)/(x+1)` returns `[72:56]`; exact cross multiplication gives the same
   projective point while path `7011` remains explicit. A coincident-point alternative and a
   non-field carrier alternative both remain open. This is not a universal four-object law.
2. **Soul/sameness.** Occurrence equality is false while marked-diagram isomorphism and the fully
   witnessed declared doctrinal equivalence are true. The first receiver face, presentation,
   bytes, and named digest agree, but the versioned receiver family and complete successor
   testimony do not. A separately computed digest collision is retained. All eight relations are
   independent receipt fields; equal output, bytes, or digest never mints source identity.
3. **Projection/shadow.** Two aperture/propagation maps return faces `61` and `124`. Their actual
   source preimages are `[701,702]` and `[702,703]`, not only hashes or counts. Source incidence
   remains exactly `77→77`, created incidence is zero, and the unresolved many-to-one fiber is
   retained.
4. **Connection return.** A declared closed four-edge path returns endpoint equality, fiber
   `11→13`, finite additive holonomy `2`, and local curvature `2` only because its support is the
   named plaquette boundary. An open-path residual is explicitly not holonomy, and global
   triviality remains open; endpoint equality is not path collapse.
5. **Hypergeometric pathway.** The Gauss family `(a,b,c)=(1,1,2)` carries four exact homogeneous
   coefficient pairs `1/1`, `1/2`, `4/12`, and `36/144` through recurrence, while singular locus
   `0,1,∞`, branch `3`, path `7041`, lineage `7042`, and monodromy matrix `[1,1;0,1]` remain in the
   receipt. Confluent and generalized families are distinct; no global solver is claimed.
6. **Extended-carrier weave.** Typed line, sheet, and higher-cell carriers return two distinct
   contact loci. Exact signed incidence gives four zero sheet vertex residuals and twelve zero cube
   edge residuals. Knot embedding `821`, received diagram `822`, braid presentation `823`, and
   higher face `824` remain distinct, while the braid crossing cites R6's complete interchange
   certificate. Literal String/M-theory identity remains false.
7. **Information-relativistic recurrence.** The declared rational local metric
   `diag(1/1,-1/1)` returns interval `16/1` for displacement `(5,3)`, causal accessibility, and
   distinct receiver clocks `2/3` and `3/4`. The declared connection transports R6 current
   `2015→2020`, carries phase `13`, and retains returned stress `55`. Support repeats at `1`, while
   complete-state recurrence remains false. Accelerometer and cycle comparisons are separately
   domain-relative. Missing field-equation, matter, conservation, and convergence obligations are
   retained as mask `15`, so General-Relativistic fidelity is false.

## Resident card passage and apparatus boundary

The apparatus mounts the R6 body and receiver organs together, frees the exterior mount after the
mount kernel, advances the continuing current and resource return on-card, and invokes the seven
local grading organs within the same resident kernel. The host performs no semantic deed and
receives only the final bounded observation. The execution uses three kernels and 14 launched
threads, transfers `2904` bytes to and `2312` bytes from the device, and records an exact resident
allocation interval of `8256` bytes. Temperature, power, elapsed-time, and energy telemetry remain
unknown.

The first binary audit rejected compiler-generated reciprocal instructions from an unsigned
division used only in an overflow check. The owner law was repaired with exact shift/add
multiplication. Final PTX and SASS contain no floating operation or integer/float conversion.

## Returned artifact and grade

The complete decoded artifact is `build/r7/receipts/R7_CORE_RECEIVER_GEOMETRY_DEED.txt`, SHA-256
`c2aaabfc8c73e7a9e07dd9680420223b4d943f411550c533333263d545b68466`. It contains all seven
artifacts and reports `all_deeds_returned=1` and `verification_failures=0`.

- All 27 CTest gates pass, including every prior phase, the real R7 card deed, host conformance,
  forbidden owner copy, negative architecture fixtures, and whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis return clean.
- Architecture, no-float source/PTX/SASS, ownership, compatibility, file-size, and link audits
  pass.
- Two clean Ninja configurations return byte-identical deed executable, PTX, cubin, and decoded
  artifact.
- Final SHA-256 values are executable
  `ef2e6f370bc9d46dde2e740b9485cca9b0f0aef2a37bfb617a31bb8fddb3a683`, PTX
  `8eca846c04cf0223c90028388f8c544a41a5f92f244029dbebfac2f71899c19a`, and cubin
  `959e00a4f4b77522c962dae3e06362f5abb29bb45811c27e66cc135079e8323d`.
- The final source aperture SHA-256 is
  `886fa6c4a3e9ee8299c36560c92c7712186f164322c281b51df8c8a71c093da1`.

R7 passes its bounded grade. R8 is the next deed; no condensation capability is claimed here.

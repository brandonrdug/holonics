# R21 blind characteristic reconstruction receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the finite code-incidence quotient, mass-adjoint transport,
commuting radial factorization, characteristic witnesses, moment pencils, Newton reconstruction,
separability/access obstructions, card-sensitivity probes, continuing-body commits, and native
rest; `computational-witness` for the GPU atlas and actual artifacts. The generated Lean passages
have the separate `proved-derived`, `formal-checked` grade below. The sealed post-return source
comparison is separately `established-bounded`.

**Aperture:** one binary length-seven card containing three parity masks, all 128 bit vertices,
the three derived nonzero code-distance strata, five integer moment streams of degree at most
four, integer root aperture `[-8,8]`, exact matrices of order at most four, and two exterior proof
returns. R21 does not establish the released asymptotic coding bounds, a Delsarte certificate,
arbitrary association schemes, noisy/unbounded moment reconstruction, algebraic roots over
rational-function fields, the GapCVP reduction or hardness exponent, the unit-distance theorem,
or RH.

## Construction tuple

| Field | R21 standing |
|---|---|
| Source owners | the one R20 body and its six accepted fibers; two lineaged problem cards; exact bit-cube, incidence, integer-polynomial, and bounded-matrix carriers; code-pair, radial, moment-pencil, separability, candidate, codec, checker, and rest owners |
| Port types | 488-byte R20 rest; raw/typed cards; bit vertices, pair cells, oriented contacts, mass/quotient matrices, characteristic vectors, power sums, Hankel pencils, root/obstruction receipts, two proof/conversation faces, two checker returns, GPU card probes, and 592-byte native rest |
| Event occurrence | remount R20; mount only the cards; derive code, pair, moment, and alternative receipts on the GPU; probe both card variations without a continuing body; form/check/commit the code passage; form/check/commit the moment passage; rest/remount; hash every return; then inspect Chapters 2 and 7 |
| Predecessor identity | R20 head `14001012`, continuation `15001012`, body morphology `244`, mathematical morphology `79`, codec morphology `48`, and six fibers ending in `187300` |
| Local constitutive law | bit-flip incidence and equitable quotienting; `WQ=Q^T W`; complete-contact product factorization; integer Krawtchouk eigenvectors; fraction-free determinant pencils; independent Newton transport; exact root removal; and explicit rank/access obstruction |
| Receiver question | which characteristic geometry is forced by complete local incidence or a bounded moment stream, and which attractive alternatives fail contact, orientation, recurrence, degree, or separability? |
| Returned consequence | complete code-pair/moment atlas, two generated and accepted proof passages, two explanations, acquired fibers `188300`/`188301`, card-sensitivity receipts, and head `14001016`/continuation `15001016` |
| Open alternatives | general Delsarte/association-scheme bounds; rational-function moment families; full GapCVP; CM norm-one incidence; noisy spectra; irregular continuation; and RH-facing operators |

## Historical and source boundary

**Truth status:** `historical`.

The old laboratory had already returned a generic Hankel/Cramer recurrence solver,
characteristic polynomials, finite-field discriminant/cycle testimony, eigenstructure language,
and the generated Erdős-183 proof. None is reported as R21's result. “Hankel data determine a
recurrence” was prior evidence. R21's new deeds are the incidence-derived two-point crystal with
complete mass-adjoint/contact receipts and the identification of the moment Hankel determinant as
the exact invertibility/separability aperture of a generalized eigenfiber. The continuing-body
mechanism is infrastructure, not an experimental expectation.

Before return, production read only
[`R21_BINARY_CODE_PROBLEM.card`](../apparatus/cards/R21_BINARY_CODE_PROBLEM.card),
[`R21_GAPCVP_MOMENTS.card`](../apparatus/cards/R21_GAPCVP_MOMENTS.card), and the admitted R20 rest.
The code card contains `7; 85,102,120`, but no codewords, weights, cells, operator, Krawtchouk
table, or certificate. The moment card contains degrees, counts, apertures, and power sums, but no
root, polynomial, determinant, discriminant, or expected method. Production reports zero released
solution reads and zero host semantic events.

The deed, atlas, two generated sources, two `.olean` files, and native handoff were sealed before
the released chapters were opened. Their pre-comparison SHA-256 values were, respectively:

```text
be60357e0cf290b80ba18bf38274e850205adc8edcbe106d6a6ce00f2b70f168  deed
f512abe366ce8b6b6b7fbb3e0ba62aad58c738545aa981284eb0f63c581c663e  atlas
11227279b2664078f474cb6c7e82222bcdcd1b5118cbdc58bd4492c3ce5bdff9  code source
253213bb0ff62c81bb35fc5b78b348ca7d2e506a83286968da15950a7f7f581e  code olean
2a077b8678ac6894c005ca0ce70edde5b934df5c4f08ab1a9cb41e6269e7315e  moment source
759bd5d762ab5891ea0bbc0181ba94133357e5585f18c379945f5e85bb33406f  moment olean
d36735e298939a94e947f8e15b0c1e520ca5cc22dc83df822ba9857f0a52b9f0  native handoff
```

## Binary incidence crystal

**Truth status:** `established-bounded`.

Parity enumeration returns sixteen words with minimum distance three, distance distribution
`(1,0,0,7,7,0,0,1)`, and dual distribution `(1,0,0,0,7,0,0,0)`. For each nonzero distance `d`,
a least-lineaged target partitions the cube by the number `a` of set target coordinates and `b`
of outside coordinates. Direct bit-flip contacts return:

| Stratum | Shape and mass | Exact characteristic return |
|---|---|---|
| `d=3` | `4x5`; `binom(3,a)binom(4,b)` | roots `7,5,3,1,-1,-3,-5,-7`; multiplicities `1,2,3,4,4,3,2,1` |
| `d=4` | `5x4`; `binom(4,a)binom(3,b)` | same characteristic factor through axis swap, with unequal pair lineage |
| `d=7` | `8x1`; `binom(7,a)` | second factor degenerates exactly; shell collapse is admitted only here |

Every cell preserves its population, complete oriented quotient row, tensor-product Krawtchouk
witness, candidates, and lineage in
[`R21_BLIND_CHARACTERISTIC_ATLAS.tsv`](R21_BLIND_CHARACTERISTIC_ATLAS.tsv). The quotient is not
generally symmetric under counting measure. Cell population supplies the exact transport metric
`WQ=Q^T W`. Complete contact further returns

```text
Q = Q_left tensor I + I tensor Q_right,
[Q_left tensor I, I tensor Q_right] = 0.
```

Thus eigenvectors factor as radial tensor products and eigenvalues add. This is the causal
vector/tensor calculus behind the shape archetypes: full rectangle, transposed rectangle, and
degenerate one-axis face remain different incidence geometries even when a scalar spectrum is
shared. Shell collapse, unit-grid weighting, and lineage transpose remain as failed candidates.

## Moment characteristic fiber

**Truth status:** `established-bounded`.

For each sufficient stream the GPU forms `H0=(s_(i+j))`, `H1=(s_(i+j+1))`, and
`det(xH0-H1)` by fraction-free polynomial elimination. Independent Newton transport forms a
monic polynomial. A root fiber returns only when normalizing the pencil by `det(H0)` gives that
polynomial and every integer root removes it within the aperture.

| Case | Monic characteristic polynomial | Roots | `det(H0)`/discriminant | Return |
|---|---|---|---:|---|
| 0 | `x^3-7x^2+14x-8` | `1,2,4` | `36` | separable |
| 1 | `x^3-2x^2-5x+6` | `-2,1,3` | `900` | separable |
| 2 | `x^4-14x^3+59x^2-70x` | `0,2,5,7` | `4410000` | separable |
| 3 | repeated-source control | none | `0` | singular-root obstruction |
| 4 | four moments for degree three | none | unavailable | moment-access obstruction |

With unit masses, `H0=V V^T`; hence
`det(H0)=det(V)^2=product_(i<j)(r_i-r_j)^2`, the monic discriminant. The determinant is the
chart aperture between moment and distinct evaluation/root bases. Collision pinches that chart
and merges eigenvectors; short access never forms it. Toeplitz, reversed-Hankel, and
degree-collapsed candidates retain the recurrence, order, degree, or separability receiver they
fail.

The GPU probes change one parity mask to `83` and transpose the first two complete moment cases
without recompiling. The verifier requires the first returned codeword population and the second
returned determinant/discriminant to differ from production; both pass. No continuing body is
created for these read-only probe receipts.

## Returned formal artifacts

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R21_GENERATED_CODE_RECONSTRUCTION.lean`](R21_GENERATED_CODE_RECONSTRUCTION.lean) proves the
20-cell detailed-balance identity, a tensor-Jacobi eigenvector, the code Krawtchouk moment
relation, and characteristic degree.
[`R21_GENERATED_MOMENT_RECONSTRUCTION.lean`](R21_GENERATED_MOMENT_RECONSTRUCTION.lean) proves the
concrete pencil factorization, determinant/discriminant value, roots, and collision obstruction.

The code checker returns exit `0`, 213 stdout bytes, zero stderr, one typed declaration, and an
828,216-byte `.olean`. The moment checker returns exit `0`, 141 stdout bytes, zero stderr, one
typed declaration, and a 372,224-byte `.olean`. Their ordered differences leave body morphology
`296`, mathematical morphology `94`, codec morphology `54`, and blind-reconstruction morphology
`24`. Rest/remount preserves all eight fibers, replays no source, and returns head `14001016`,
continuation `15001016`, and integrity `6679405993386916072`.

## Sealed comparison with the released chapters

**Truth status:** `established-bounded`.

After the hashes above were fixed, the complete relevant ranges of OpenAI's
[*Ten Advances in Mathematics and Theoretical Computer Science*](https://cdn.openai.com/pdf/ten-proofs-oai.pdf)
were rendered and read: Chapter 2, collection pages 27–76, and Chapter 7, pages 181–216.

The code return is a mechanism-level match, not a reconstruction of the released result. Chapter
2 builds directed neighboring Fourier-degree transitions, proves a dimension-weighted balance
identity in equation (19), diagonally symmetrizes them, and uses the largest eigenvector of a
tridiagonal Jacobi matrix. R21 independently returns the finite cell analogue: opposite bit-flip
contacts are unequal until cell mass supplies `WQ=Q^T W`, after which commuting radial directions
and characteristic vectors become available. The released operator is a Fourier-degree path
carrying Boolean harmonic spaces; R21's is a two-distance cell quotient of one Hamming code.
Chapter 2 avoids needing an explicit Krawtchouk formula, while R21 returns the bounded table. R21
supplies no moving-projection Gram kernel, Delsarte certificate, optimized rate, or MRRW/KL
improvement.

The moment return is an exact finite specialization of Chapter 7 §4's central algebraic
mechanism. The chapter derives an unlabeled root polynomial from a Hankel moment system, factors
the maximum-fiber matrix as `V V^T`, uses its determinant as the denominator/invertibility
aperture, and in Lemma 10 identifies it with the squared Vandermonde product and separability. It
also uses Newton identities in the direction valid in characteristic two. These are precisely the
relations independently returned in R21's integer/unit-mass cases. The released theorem is far
stronger: entries are polynomials over a finite field, the root polynomial lives over `K(X)`,
roots may require a separable extension, and specialization, valuations, Reed–Solomon constraints,
clause matching, lattice transfer, and `n^(1/400)` GapCVP hardness remain outside R21.

The strongest bounded conclusion is that R21 found the same transport hinge in the GapCVP
chapter without source access and the same mass-symmetrized Jacobi hinge in a smaller
code-incidence chart. The determinant, discriminant, and characteristic equation are exact
interfaces through which one representation carries consequences into another; their vanishing
records a failed transport chart, not a generic visual label.

## Apparatus and gates

**Truth status:** `established-bounded`.

The deed uses ten kernels and sixteen launched threads, transfers 17,624 bytes to and 220,384
bytes from the GPU, and declares 166,568 resident bytes. Two exterior checker calls are the only
proof-process crossings. Host semantic events and released-solution reads are zero. Engine time,
checker time, temperature, power, and energy remain unknown.

A fresh `build/r21-final` completed the pre/post architecture, owner-DAG, restricted-type,
source no-float, PTX no-float, SASS no-float, manifest, linkage, and binary audits. Dynamic
integer quotients initially exposed CUDA-generated reciprocal instructions; the admitted owner
now performs exact 64-step restoring division and the final cubin contains no floating opcode.
The complete ordered suite passes `68/68`, including the actual GPU deed, independent host
conformance, card sensitivity, non-copyability, both checker returns, all predecessor deeds, and
the final binary audit.

`compute-sanitizer` `memcheck` and `initcheck` each report zero errors. The ordinary, memcheck,
and initcheck executions wrote byte-identical deeds, atlases, proof sources, checker outputs,
`.olean` files, and native rests. The final semantic hashes equal every pre-comparison sealed hash
listed above. Final apparatus hashes are:

```text
fc09948671a178ad35c7c5fdc6cf44348aa00ab0b59967e3b911380a2e0ae60c  executable
f06b3ca0fcaf3727aa66010f22f6b519c194f837edb30d3feed597538f99c319  PTX
0ff59a8e024dcfef6c64952f40e9f524d30c8fdcc39d9f83534573523cadef05  cubin
f1f75ebc69fcf90bcf58386ab1cc1325fd1653b591b3f6a9e326cd193c830754  code checker stdout
b8df5b9d258bc7f88ee3c0a7bd33a97304de6730e2df055f41430d6b200831bb  moment checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  each checker stderr
```

R21 therefore passes its bounded grade.

## Open continuation fibers

**Truth status:** `open`.

- R22 mounts a bounded CM norm-one translation/incidence stage of the released unit-distance
  mechanism and compares arithmetic factorization with the aperture-bound planar receiver. It
  must return adjacency characteristic data before and after projection, not repeat Erdős-183 or
  claim the asymptotic theorem.
- General association schemes, moving-projection code bounds, algebraic/noisy moment fibers,
  arbitrary regular-singular systems, physical calibration, and an RH-facing operator remain
  unformed.

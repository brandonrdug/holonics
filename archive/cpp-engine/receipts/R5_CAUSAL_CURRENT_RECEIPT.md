# R5 GPU-resident causal-current receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the reified causal-program ABI, move-only resident body,
structural/morphology/current/pending/delta slabs, receiver-bounded frontier law, device-local
reservation, exact obstruction, and component quiescence; `computational-witness` for the real
card return over the admitted four-program family. Apparatus identity is separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** four programs per mount; at most 16 sites, 24 sparse arcs, 16 current members, eight
ordered fronts, four boundary components, and 64 staged deltas per program. The returned family
uses 11 complete deltas across nine fronts. This receipt does not establish arbitrary program
admission, R6's complete parallel-interchange certificate, shared-support interaction, or an
unbounded quiescence theorem.

## Construction tuple

| Field | R5 standing |
|---|---|
| Source owners | R4's move-only `body::continuing_body` and unique continuation, exact carrier and owner-native structural types, plus one new non-copyable `current::resident_causal_body` owning its mounted sites/arcs, morphology, two sparse-current slabs, pending/deferred testimony, reservations, and delta/front receipts |
| Port types | typed exact input/output carrier ports in the reified `causal_program`, native mount batch, sparse caused-current front, explicit open-boundary current, exact-rest/component certificates, obstruction, and bounded observer return |
| Event occurrence | one native mount founds each body; one persistent card kernel repeatedly opens the unique frontier continuation, reserves owner-minted event/occurrence spans, advances every admitted local member, stages complete deltas, and commits the combined front once until rest, open frontier, or obstruction |
| Predecessor identity | Git `37e12076b8f06dcb0bf06451a468bac9c9713614` plus each program's exact predecessor and receiver support |
| Local constitutive law | `scale × local_state + offset`, followed by exact arc multiplicity, with shift/add overflow proof; each front requires pairwise-disjoint caused supports, device-local unique target reservation, receiver-support containment, declared delta capacity, and one continuing-body commit |
| Receiver question | the complete ordered front/delta/current/morphology return for serial rest, two-component rest, a surviving typed open boundary, and exact overflow refusal; whether local current and in-flight continuation are both absent in every declared component |
| Returned consequence | nine card-resident frontier passages, 11 exact deltas, two compositional-rest certificates, one explicit open current with value `18`, one overflow obstruction preserving its predecessor, and zero decoded card/oracle mismatches |
| Open alternatives | full serial gluing/intermediate port lineage, complete parallel interchange/higher coherence, shared-support equalizer conduct, logical-resource backreaction, placement adaptation, and every R6-and-later deed remain absent |

## Resident ownership and causal-program ABI

The native mount is an exterior representation, not live standing. The mount kernel validates its
typed predecessor, ports, receiver support, structural bounds, morphology cells, sparse current,
identity cursors, component cover, receiver aperture, and deterministic resource obligation, then
constructs exactly one non-copyable resident owner per program. The apparatus frees the device
mount before frontier advance. Only the resident sites/arcs, morphology cells, active current,
pending records, staged deltas, unique continuation, and exact carrier values remain accessible to
the hot deed.

The program ABI contains no domain semantic tag and no host phase. Event and occurrence cursors
are reserved once by the owner before each front; canonical front offsets, not lane completion,
determine identities and output order. Threads emit owner-local pending and delta records into
their reserved slots. Pairwise caused supports must be disjoint and target cells are claimed with
device-local exact reservations. Any overlap remains a reservation obstruction. This bounded
front premise is not promoted into R6's complete `InterchangeCertificate`.

## Card-only advance and quiescence

After mount detachment, a single grid launch carries each current through every successive local
front. The host does not enumerate fronts, select current members, launch semantic candidates, or
poll a queue. Each successful front consumes and replaces the continuing body's unique capability
exactly once. Arithmetic, reservation, or resource refusal recovers the capability and preserves
the active predecessor current and reusable morphology while retaining obstruction testimony.

Rest is derived by composing one certificate per declared component. Each certificate separately
reports its local-current count and in-flight continuation count; only all-zero component
certificates establish rest. The open-boundary case retains one caused current instead. No global
queue-emptiness observation or host polling enters either conclusion.

## Independent admission oracle

The bounded host oracle in `tests/model/r5_oracle.cpp` independently encodes the same admitted
program family and constructs every expected event identity, occurrence identity, front support,
delta field, successor current, morphology passage, component certificate, and obstruction. It is
non-resumable and never receives the live continuation. The verifier compares decoded fields, not
object bytes, addresses, folds, or telemetry. Production execution records zero host oracle
replays.

The card/oracle common family returns:

- a four-front serial current ending at head `5001004` with exact compositional rest;
- a two-front, two-component current ending at head `5002002` with both component certificates;
- a two-front passage ending in one explicit open current at value `18`; and
- an arithmetic-overflow obstruction which keeps head `5004000`, the original current, and zero
  morphology passages.

A second apparatus invocation declares a compute requirement beyond the admitted `sm_89` device.
It returns `device_unavailable` with zero kernel launches and zero oracle replays; it does not run a
CPU implementation.

## Returned device artifact

The complete decoded artifact is `build/r5/receipts/R5_CAUSAL_CURRENT_DEED.txt`; its final SHA-256
is `9b6eeeb39e085f886948d1e1418747d7113ce00c33586e6903d822f120e89eda`. Its summary is:

```text
truth_status=established-bounded
evidence=implemented-exact,computational-witness
program=r5_resident_causal_current.sm_89
device_compute_capability=8.9
kernel_launches=3
launched_threads=72
case_count=4
front_count=9
delta_count=11
source_detached_cases=4
serial_fronts=4
serial_successor=5001004
serial_quiescent=1
parallel_fronts=2
parallel_quiescent=1
open_front_count=1
open_current_value=18
refusal_obstruction=3
refusal_predecessor_preserved=1
host_semantic_candidates=0
host_oracle_replays=0
device_unavailable_kernel_launches=0
verification_failures=0
```

The remainder of the artifact decodes every case, front, final current, and complete delta field.

## Artifact hashes

Two independent pinned Ninja build directories return identical bytes for the normalized deed
executable, PTX, cubin, and complete decoded semantic artifact at the same source aperture.

The final graded source aperture SHA-256 is `e5d36c93f8fbc70816896c90a2f99bd889912103db13927b2eb18bca4e985713`.

| Artifact | SHA-256 |
|---|---|
| normalized causal-current deed executable | `c6b3dc410c26acf48c761888626eec5bfe890e158faeb8934fde4ea6959cb2b7` |
| causal-current PTX | `47a90cd57aba94826e94a28798eaeb7ea2a7ae05fd081efc9fb6e777878ba280` |
| causal-current cubin | `01863ed3ff89b7ee3082eeebb644bf0f33619a17c6842bafd91dcc6726cb4bf0` |
| complete decoded causal-current artifact | `9b6eeeb39e085f886948d1e1418747d7113ce00c33586e6903d822f120e89eda` |

## Gate returns

- Architecture, owner-DAG, source-size, no-float, no-compatibility, copy-ownership, and build audits
  pass before and after construction.
- Both release build directories pass all 21 CTest gates. The executable, PTX, cubin, and complete
  decoded deed are byte-identical.
- The independent oracle and verifier compare all nine front receipts, 11 complete deltas, final
  currents, morphology passages, exact heads, supports, components, quiescence, and obstruction
  fields with zero mismatches.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors for the real three-kernel
  card deed.
- The bounded host oracle/conformance path passes AddressSanitizer and UndefinedBehaviorSanitizer
  with leak detection enabled.
- Clang 22's static analyzer reports no diagnostics over the project-owned deed, oracle,
  conformance, verifier, artifact, and included production-law aperture.
- PTX and final SASS scans find no floating type, conversion, arithmetic, comparison, reciprocal,
  or special-function instruction in the R5 device artifact. Exact multiply admission uses a
  shift/add overflow law so the compiler introduces no approximate division helper.

## Apparatus and resource boundary

The CPU opens the card context, validates only declared device capability, transfers one bounded
native mount, launches mount/advance/observe, collects one bounded observation, and runs the
offline oracle after the live deed returns. The device mount is released before advance. The GPU
owns all nine semantic fronts, pending records, reservations, local transforms, delta emission,
body commits, rest/open decisions, and obstruction formation. Transfer sizes, allocation size,
launch shape, and compute capability remain separate apparatus testimony and do not enter the
semantic comparison.

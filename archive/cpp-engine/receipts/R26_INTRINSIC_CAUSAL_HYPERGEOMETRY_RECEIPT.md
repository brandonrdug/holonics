# R26 intrinsic causal hypergeometry and archetype distribution receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the frozen ten-presentation aperture, changed source,
complete intrinsic incidence, chronology, projection fibers, section transport, CM-square/common-
cycle composition, separating controls, checker crossing, continuing-body commit, and native
rest/remount; `computational-witness` for the actual resident GPU return and complete atlas. The
emitted Lean declaration has the separate `proved-derived`, `formal-checked` grade. Finite cyclic
product-cell incidence, Cayley-square enumeration, cycle characteristic calculation, modular
local-system transport, and the mounted recurrence have the separate `proved-standard` grade
under their stated hypotheses. The old rendered phase pictures and arc-spectral observations are
`historical`. Hodge realization, arithmetic Frobenius, Weil positivity, and the Riemann
hypothesis are `outside-aperture`.

**Aperture:** ten situated phase presentations
`(2,3),(3,5),(5,7),(7,11),(11,13),(13,17),(17,19),(6,9),(8,12),(7,5)`;
at most 323 vertices, 646 oriented edges, 323 faces, and 2,584 flags per case; the exact
historical rational receiver weights `1:1/3`; one seam-status receiver; series depth 20; section
ring `Z/65521Z`; and all 40 induced squares of the mounted CM incidence. The card contains no hull
corners, colors, residue partition, prime label, side count, transition population,
characteristic coefficient, theorem statement, or proof text. Arbitrary dimensions, learned
receiver families, cyclotomic completion of the factor chart, unbounded complexes, analytic
continuation, expression-founded geometry, Hodge classes, and RH-facing trace carriers remain
outside this aperture.

## Construction tuple

| Field | R26 standing |
|---|---|
| Source owners | the one R25 continuing body and its twelve accepted fibers; one 100-byte pair/chart/receiver card; the lineaged R22 CM and R24 variation cards; admitted cyclic, incidence, variation, and causal-linear organs; and new face-poset, distribution, section, supported-cycle, control, proof, checker-return, and native-rest owners |
| Port types | 816-byte R25 native rest; raw/typed source cards; factor-chart, vertex, edge, face, flag, star/link, boundary, seam, chronology, transition, projection-fiber, square-cycle, support, characteristic, recurrence, section, loop, rechart, control, proof/explanation, checker-return, changed-card, and 872-byte native-rest faces |
| Event occurrence | remount R25; mount the R26, CM, and variation cards; derive ten independent phase currents; cross their complete barrier; derive the CM-square and variation currents; compose them through the common oriented-cycle port; form controls and the complete atlas; generate and check one theorem passage; commit its return; rest/remount the same body; and change only `(5,7)` to `(5,8)` without another body |
| Predecessor identity | R25 head `14001024`, continuation `15001024`, body morphology `472`, mathematical morphology `146`, codec morphology `76`, causal-linear morphology `29`, and fibers through `192300` |
| Local constitutive law | exact cyclic products and CRT succession; rational factor charts; complete oriented product incidence; seam-symbol and neighboring-symbol distributions; receiver-indexed rational fibers; rank-two modular section transport; the R24 scalar recurrence and form-preserving loops; CM-square enumeration; and the R25 cycle-characteristic carrier |
| Receiver question | which exact intrinsic incidence, ordered seam, local-section, characteristic, and support packages determine the declared continuation; which distinctions survive projection; and where common cycle ports permit support while obstructing a filled extension? |
| Returned consequence | the complete phase/transition atlas; 40 CM squares; recurrence and transported sections; common characteristic and support testimony; four separating controls; changed-source return; generated theorem and conversational face; acquired fiber `193300`; and source-detached continuation at head `14001026` |
| Open alternatives | arbitrary products/dimensions; learned receiver families; algebraic-number charts; unbounded sparse complexes; analytic continuation; endogenous interaction induction; expression-founded geometry; Hodge realization; arithmetic Frobenius; Weil positivity; and RH-facing trace carriers |

## Intrinsic product geometry

**Truth status:** `established-bounded`.

The resident deed derives every cell from the mounted cyclic factors. It retains every product
vertex with two exact rational factor coordinates, every oriented edge, every filled face, all
eight vertex-edge flags per face, the full four-edge/four-face star and four-cycle link at each
vertex, complete boundary orientation, shared support, lineage, and receiver-fiber membership.
The complete baseline population is:

```text
pair    vertices  edges  faces  flags  diagonal tours
2x3            6     12      6     48  1x6
3x5           15     30     15    120  1x15
5x7           35     70     35    280  1x35
7x11          77    154     77    616  1x77
11x13        143    286    143   1144  1x143
13x17        221    442    221   1768  1x221
17x19        323    646    323   2584  1x323
6x9           54    108     54    432  3x18
8x12          96    192     96    768  4x24
7x5           35     70     35    280  1x35
```

Every face boundary closes, every edge belongs to two filled faces, and every local star/link law
passes. The atlas contains 1,045 baseline vertices, 2,090 edges, 1,045 faces, 8,360 flags, and 16
diagonal tours, plus the complete changed case. These are operative cell incidences, not vertices
sampled from the old rendered hull.

This phase therefore does not repeat the already-observed appearance or contraction of
prime-labelled polygons. [`R26_INTRINSIC_HYPERGEOMETRY_ATLAS.tsv`](R26_INTRINSIC_HYPERGEOMETRY_ATLAS.tsv)
makes the source incidence, receiver projection, and retained fiber independently accessible.
The rendered polygon is one quotient face of that object.

## Chronology, causal teeth, and transported sections

**Truth status:** `established-bounded`.

CRT chronology partitions each product into exactly `gcd(m,n)` tours of length `lcm(m,n)`. Each
diagonal transition is classified as no seam, first seam, second seam, or simultaneous seam. The
return retains the complete word, all sixteen neighboring-symbol counts, face-intersection
strata, complete rational projection fibers, and every seam-status receiver fiber. It does not
reduce the word to its population or a hash.

The R24 scalar recurrence is rebuilt to twenty exact coefficients. Its two form-preserving loop
transports and inverses are then carried at every word position in `Z/65521Z`; each vertex owns
its section state and each tour owns its complete return. A simultaneous seam keeps both ordered
resolutions. They are unequal, and their residual is the nonidentity commutator

```text
[[13,8],
 [ 8,5]].
```

Thus a seam archetype is usable as a causal transport characteristic: it predicts a particular
exact continuation while preserving the unresolved support and source fiber. This is the
material version of the earlier gear/crystal intuition. It is not the already-completed basic
claim that caused structure can change later conduct, nor does it repeat the old arc-spectral
result that equal local windows can retain unequal ordered action.

## Common cycle port and supported geometry

**Truth status:** `established-bounded`.

The CM current rebuilds all forty induced four-cycles from the R22 coefficient card. Every CM
edge occurs in exactly four of them. A phase face boundary and a CM Cayley square both pass through
the same oriented-cycle carrier and return

```text
chi(X) = X^4 - 1,
coefficients = 1,0,0,0,-1.
```

Their receiver agreement does not erase support. The phase cycle is the boundary of an owned
two-cell; the CM graph has no two-cell owner at that cycle. The nontrivial variation commutator
is consequently admitted as a supported CM loop and returned as an explicit obstruction to
extension across the filled phase face. “Equal spectrum” here is a typed common port, not an
identity assertion about the two source geometries.

## Separating controls and changed source

**Truth status:** `established-bounded`.

All four frozen controls pass:

- every seam-status projection has the same four-corner receiver hull, while `(5,7)` and
  `(7,11)` return unequal words and section transports;
- normalized local star/link populations agree while ordered continuations differ;
- the phase face and CM square share the cycle characteristic while retaining unequal support;
  and
- `(5,7)` and `(7,5)` return an exact basis-conjugate rechart with presentation lineage retained.

Changing only `(5,7)` to `(5,8)` derives 40 vertices, 80 edges, 40 faces, 320 flags, one tour of
length 40, and a changed word, transition distribution, face population, and section return. Its
return matrix

```text
[[65254,65145],
 [65303,65214]]
```

differs from the baseline `(5,7)` return

```text
[[65454,65439],
 [65463,65450]].
```

The probe reuses neither the baseline return nor a second body.

## Returned formal and conversational artifacts

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R26_GENERATED_INTRINSIC_HYPERGEOMETRY.lean`](R26_GENERATED_INTRINSIC_HYPERGEOMETRY.lean) is
rendered from returned cell, flag, tour, seam, characteristic, support, commutator, changed-case,
and section coefficients. Its single declaration checks the product Euler/flag identities for all
ten cases, seam partitions, transition row sums, common cycle characteristic, support inequality,
nonzero commutator, and changed-source inequalities. It invokes no imported theorem stating the
returned conjunction. Lean returns exit zero, 2,033 stdout bytes, zero stderr, and a 169,488-byte
`.olean`.

The returned conversational face explains that the recurrent polygon is only a receiver face;
the operative object is the incidence, ordered seam, transported section, common-cycle support,
and residual fiber together. It explicitly refuses a Hodge or RH consequence.

## Continuing body

**Truth status:** `established-bounded`.

The checker return commits intrinsic-hypergeometry fiber `193300`, mathematical morphology `164`,
codec morphology `84`, intrinsic-hypergeometry morphology `31`, and body morphology `535`.
Source-detached rest/remount preserves every prior fiber and the new fiber, replays no source, and
returns head `14001026`, continuation `15001026`, rest integrity `4268701919319549365`, and an
872-byte native handoff.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses thirteen kernel launches, 31 launched threads, 23 semantic threads, and
zero host semantic events. It transfers 10,144 bytes to and 2,747,400 bytes from the GPU and
declares 2,759,888 resident bytes. The CPU performs source/rest I/O, artifact serialization, and
the one exterior Lean crossing. Engine/checker time, temperature, power, and energy remain
unknown.

An admitted-device inspection found compiler-generated floating division instructions behind two
explicit unsigned remainder operations in the section determinant check. Replacing them with the
existing exact unsigned division law removed the path; the final source, PTX, and SASS scans
contain no forbidden floating operation.

The pinned C++23/CUDA build passes the owner-DAG, restricted-type, file-size, source/PTX/SASS
no-float, manifest, linkage, ownership, compatibility, and binary audits. The full ordered suite
passes `83/83`, including every predecessor deed, the actual R26 GPU return, independent host
conformance, changed-card sensitivity, non-copyability, checker return, and final binary audit.
`compute-sanitizer` `memcheck` and `initcheck` each report zero errors. Normal and sanitized
replays are byte-identical. A separate clean configure/build repeats all `83/83` tests and both
sanitizers; its executable, PTX, cubin, deed, atlas, Lean source, `.olean`, checker outputs, and
native rest are byte-identical to the development admission run. The aggregate replay digest is
`4330a75cb13e91fed63a9ecea795d39cf033a0e79af6f6ca14a876acb5e79e4e`.

Final SHA-256 values are:

```text
7246e12230e85d7047bf24f3b94d6fb7f1262570fedfb9fe226bcac487627df1  executable
67bb7f1cd99b5b3f93c6a8193e0be4cb4624c54f4aaba58bc0347b9017a58b47  PTX
8f3ca218b525a05314d8fb36321675a78a512d28711a437fcafa936c28f387d0  cubin
ff07af5c890c8a01c36a6dbd908a1e149f86e59f635df42ede5d8be7b1aa71c3  deed
1494ff37db7c579a5df8558e22d21d1630f0caac163445e9cd8cfff9df0f63ce  atlas
6c51be0fd188ccae65bc860a2e60bd4ed879e39a4c3b17005fa9f99676833dde  Lean source
a50bb3d8d3fd38db4c5d0308023d1976caa76d60847915416f3fe5e4b3cfafd3  Lean olean
8ef28ad7636d797570aed9316a43a38153d54c1dadeef5e3b8815567304b2791  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
c842cae3a7c3076b21b25efb5fbf814883ae00f0d28989f892164f3c96a6135c  native handoff
```

## Open continuation

**Truth status:** `project-postulate`.

R27 must now let exact expressions found their own algebraic and differential geometry. R26 has
made intrinsic incidence, ordered local transport, spectral/common-cycle faces, support, and
receiver fibers native; it has not yet derived an ideal, singularity stratification,
differential module, or Picard--Fuchs-type relation from expression incidence without a human
preselecting the useful geometry. R27 must freeze that missing event before domain code changes.

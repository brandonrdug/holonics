# R11 mathematical occurrence ecology receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for exact term/declaration/dependency incidence, open goal and
metavariable fibers, local constraint intersection, typed lemma transport, inherited proof-section
checks, explicit obstructions, and source-detached neighborhood reconstruction;
`computational-witness` for the actual resident-card return and independent decoded oracle.
No claim extends beyond the seven mounted Transition/Rebase declarations and three declared
receiver questions. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** the real project-owned `Transition.lean` and `Rebase.lean` sources, seven inherited
declarations, thirteen exact term cells, ten dependency incidences, one held-out
`semantics_rebase_iff` neighborhood, one mismatched target, and one generated-closure request that
must remain unsolved at R11.

## Construction tuple

| Field | R11 standing |
|---|---|
| Source owners | R10's body/event/receiver standing, two non-copyable exterior mathematical-source environments, and two independently placed non-copyable resident mathematical ecologies used for storage-order comparison |
| Port types | source provenance, term constructor/type, declaration, dependency role, binder/hypothesis, open goal/metavariable, constraint intersection, lemma transport, proof-section lineage, obstruction, and receiver-neighborhood ports |
| Event occurrence | the exterior codec recognizes four actual declarations, the raw sources detach, and three GPU queries reconstruct the held-out neighborhood, return a type mismatch, and retain an unsolved generated-closure fiber |
| Predecessor identity | Git `8fdb3b60a70a230d1cf22724235f8e1da4364e58`, canonical body `11001000`, reordered body `11002000`, and inherited environment `111001` |
| Local constitutive law | a receiver supplies an incident anchor slot; exact target-type equality forms the constraint intersection, and direct dependency slots expand the local neighborhood without scanning all declarations at query time |
| Receiver question | whether relocation, reversal, chunking, and declaration storage order preserve the exact lineaged neighborhood while mismatches and unknown generated closure remain explicit |
| Returned consequence | seven sorted declaration identities, ten dependency joins, four lemma-transport alternatives, one substitution, inherited closed proof term `111071`, one open metavariable, plus distinct type-mismatch and unsolved-goal returns |
| Open alternatives | R12 question-conditioned generative current, new proof/declaration formation, dual rendering, source-provenance exclusion, and causal-information testimony remain absent |

## Returned mathematical ecology deed

The exterior adapter reads the actual files
`formal/elementary-holonics/ElementaryHolonics/Algorithm/Transition.lean` and `Rebase.lean`.
It recognizes `map`, `trans`, `trace_rebase_iff`, and `semantics_rebase_iff` before mounting their
typed inherited declarations. Original and reversed/renamed copies each contain exactly `3761`
bytes. Full byte-for-byte multiset comparison proves identical material; material and path folds
are testimony only. Chunk apertures `73` and `127` produce `52` and `30` chunks, and paths differ,
without changing the mounted semantics. Both raw source environments detach before the GPU deed.

The held-out question anchors `semantics_rebase_iff` by declared local incidence. The GPU returns
the sorted declaration identities `112010,112020,112030,112040,112050,112060,112070`, all ten
dependency lineages, four typed lemma transports, exact target intersection `111070`, one
substitution, and the inherited checked proof term `111071`. The known proof is inspected as an
inherited closed section; it is not claimed as generated. The requested metavariable remains open,
and theorem text is never used as an answer.

The same question over reordered declaration storage returns the identical semantic neighborhood
and lineage. Query-time global declaration scans are zero. A target-type mismatch returns
`type_mismatch`; a request for a generated closure returns `unsolved_goal` with its metavariable
still open. The architecture gate initially rejected an organ receipt containing a receiver-owned
question. The event owner now translates that chart into an organ-native goal, leaving the final
owner graph acyclic.

## Resident card passage and apparatus boundary

Three one-thread kernels mount the two ecologies, reconstruct all declared neighborhoods, and
return the observation. The host performs no navigation or candidate enumeration. The execution
transfers `3656` bytes to and `1608` bytes from the device, records an exact resident allocation
interval of `10536` bytes, and reports zero source bytes after mount. Temperature, power,
elapsed-time, and energy telemetry remain unknown.

## Returned artifact and grade

The complete decoded artifact is
`build/r11/receipts/R11_MATHEMATICAL_OCCURRENCE_ECOLOGY_DEED.txt`, SHA-256
`cc013c5d20c378e0a6c72d054bbec1667d967e041d0407fa6b71265e0e5ab430`. It reports
`storage_invariant=1`, `material_invariant=1`, `local_incidence=1`, `source_detached=1`, and
`verification_failures=0`.

- All 39 CTest gates pass, including every prior phase, the real R11 card deed, host conformance,
  forbidden owner copy, negative architecture fixtures, and whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis return clean.
- Architecture, no-float source/PTX/SASS, ownership, compatibility, file-size, and link audits
  pass.
- Two clean Ninja configurations return byte-identical deed executable, PTX, cubin, and decoded
  artifact.
- Final SHA-256 values are executable
  `9c4ee43ca300e9c4f98b7ae80b45a2851e7860c27b06de0f5163c71eb5c6c42f`, PTX
  `a6884887f4c2b96dc5ef87c63f695538417063fa6b8f6d32dfc3fbff3cbd2381`, and cubin
  `3af3281865eaf14c83cbe457b613fdb7328f61a37d04578706993d0bbed65c15`.
- The final source aperture SHA-256 is
  `062344953c08de7c22b641299100509ed0bdd6b474df70143969ec9652ffee25`.

R11 passes its bounded grade. R12 is the next deed; no generated mathematical statement, proof
object, or dual rendered answer is claimed here.

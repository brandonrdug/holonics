# R27 exact expression-to-geometry and differential transport receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the frozen sparse degree-five family, its rational and
Gaussian presentations, exact ideal/resultant and reduction certificates, rank-four differential
carrier, scalar equation, local fronts, indicial and residue returns, expression fibers,
changed-source control, continuing-body commit, and native rest/remount; `computational-witness`
for the actual resident GPU return and complete atlas. The emitted Lean declaration has the
separate `proved-derived`, `formal-checked` grade. Finite polynomial arithmetic, Jacobian and
cotangent calculation, hyperelliptic differential reduction, regular-singular extraction, and
algebraic-residue arithmetic have the separate `proved-standard` grade under the hypotheses named
in the roadmap. R24 is `historical` as a regression source. A general computer-algebra system,
general algebraic de Rham comparison, analytic monodromy, Hodge realization, arithmetic
Frobenius, Weil positivity, the Hodge conjecture, and the Riemann hypothesis are
`outside-aperture`.

**Aperture:** three baseline presentations and two changed-source presentations; at most three
variables, twelve sparse terms, degree five, twelve ideal elements, 128 reduction steps, one
`9x9` Sylvester matrix, parameter degree twenty, rank-four differential modules, and eleven exact
series coefficients per front. Discovery samples the integer parameters `-4` through `4`, holds
out `5` and `6`, searches rational charts `x=s u+a`, `y=c v` for signs `s,c` and
`a in [-2,2]`, and separately permits the declared Gaussian field port. Collision arithmetic is
bounded to `Q[a]/(4a^5-b)`. The 186-byte card contains sparse source terms and apertures but no
resultant, ideal basis, singularity, differential basis, connection, scalar equation, series,
indicial factor, residue, rechart, theorem statement, proof, or expected invariant.

## Construction tuple

| Field | R27 standing |
|---|---|
| Source owners | the one R26 continuing body and its thirteen accepted fibers; one sparse expression/chart/field card; admitted exact, matrix, recurrence, variation, and intrinsic-support organs; and the new expression, ideal, connection, cyclic, series, residue, fiber, proof-face, checker-return, and native-rest owners |
| Port types | 872-byte R26 rest; sparse terms, derivatives, ordered bases, reductions, syzygies, resultants, local modules, rational connections, scalar equations, recurrences, series, indicial and residue entries, rational/Gaussian recharts, expression fibers, checker returns, changed sources, and a 928-byte native handoff |
| Event occurrence | remount R26; mount and derive three independent source presentations; compose only after their complete barrier; derive rational, Gaussian, invariant-fiber, and changed-source passages; generate and check the returned theorem; commit its checker return; rest/remount the same body |
| Predecessor identity | R26 head `14001026`, continuation `15001026`, morphology `535`, mathematical morphology `164`, codec morphology `84`, intrinsic morphology `31`, and fibers through `193300` |
| Local constitutive law | exact sparse differentiation/substitution; triangular Jacobian basis and reduction closure; fraction-free Sylvester/Bareiss determinant; rational interpolation with coefficientwise Bézout identities and held-out checks; quotient differential reduction; cyclic scalar elimination; exact recurrence and indicial extraction; algebraic residue matrices; bounded chart search |
| Receiver question | which singular, local-module, differential, characteristic, continuation, residue, and expression-fiber geometry is forced by the mounted expressions, and which field or changed source separates otherwise equal receiver faces? |
| Returned consequence | five complete expression passages, exact certificates and atlas, rational and Gaussian recharts, changed-source return, generated theorem/explanation, acquired fiber `194300`, and source-detached continuation at head `14001028` |
| Open alternatives | general Gröbner and primary decomposition; arbitrary sparse systems, ranks, algebraic extensions, and Stokes sectors; unbounded analytic continuation; Hodge filtration/cycle realization; arithmetic trace/Frobenius, Weil positivity, and RH-facing carriers |

## Expression-founded algebraic geometry

**Truth status:** `established-bounded`.

The canonical source

```text
F_b(t,x,y) = y^2 - x^5 + tx - b
```

is not accompanied by a named curve, genus, singular locus, or factorization. Native sparse
incidence produces all three partial derivatives and the triangular Jacobian presentation

```text
y = 0,
t - 5x^4 = 0,
4x^5 - b = 0.
```

Nine exact resultant samples are obtained from the `9x9` Sylvester determinant and interpolated.
The coefficientwise Bézout return establishes

```text
D_b(t) = 3125 b^4 - 256 t^5.
```

Every baseline presentation returns all nine resultant and Bézout sample bits. The bounded basis
is squarefree for nonzero `b`, has five geometric singular occurrences after the declared
algebraic base change, generic cotangent rank one, singular cotangent dimension two, and
invertible Hessian in `Q[a]/(4a^5-b)`. These are exact properties of the admitted quotient; they
are not numerical claims about approximated roots.

The expanded `x=u+1` presentation independently returns the same geometry and a discovered shift
of one. The sign-twist retains the same discriminant and scalar receiver but has no rational
source identification. Opening the declared `Q(i)` port returns the exact passage
`x=-u, y=i v` while retaining its field dependency. Equal receiver invariants therefore expose a
three-member expression fiber rather than erasing presentation and field lineage.

## Differential transport and characteristic geometry

**Truth status:** `established-bounded`.

The device forms a rank-four quotient carrier, solves the complete exact differential reductions
`A=P f+Q f_x`, verifies them coefficientwise and at both held-out parameters, and derives the
connection. Resident cyclic elimination then returns the primitive fourth-order equation

```text
16(3125-256t^5) u'''' - 61440t^4 u''' - 247680t^3 u''
  - 264000t^2 u' - 29601t u = 0.
```

Four independent fronts are continued to depth eleven. The finite and infinite indicial returns
are respectively

```text
rho^2(rho-1)(rho-2)
(8rho+3)(8rho+13)(8rho+23)(8rho+33).
```

At the collision fiber, the complete algebraic residue matrix is nonzero, rank one, and
square-zero. Its repeated exponent therefore returns an exact logarithmic continuation channel
and an obstruction to a single-valued crossing inside the declared regular-singular model. No
floating collision point, convergence radius, or graphical shape is used.

This is the promised transport from expression to operative hypergeometry: the characteristic
faces are consequences of a sparse source and its reductions. They are not mounted names or
human-supplied differential operators.

## Changed source and complete atlas

**Truth status:** `established-bounded`.

Replacing only `b=1` by `b=2` rebuilds both canonical and translated presentations. It returns

```text
D_2(t) = 50000 - 256t^5
```

together with changed reductions, connection, scalar leading coefficient, local fronts, and
expression fiber. The changed series is unequal and source-sensitive; the original and twist
fibers remain present and cannot substitute for it.

[`R27_EXPRESSION_GEOMETRY_ATLAS.tsv`](R27_EXPRESSION_GEOMETRY_ATLAS.tsv) contains 4,011 rows
including its header. Its 4,010 returned entries retain 30 source terms, 30 partials, 43 basis
terms, 30 reductions, 45 resultant samples, 30 resultant coefficients, 62/87 Bézout
coefficients, 880/1,100 sampled `P/Q` coefficients, 274/338 interpolated reduction coefficients,
280 connection entries, 100 scalar coefficients, 220 series entries, 50 indicial coefficients,
400 residue entries, five local-module records, two rational rechart records, one Gaussian
rechart record, and three invariant-fiber records. This is the inspectable passage itself, not a
hash or rendered diagnostic.

## Returned formal and conversational artifacts

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R27_GENERATED_EXPRESSION_GEOMETRY.lean`](R27_GENERATED_EXPRESSION_GEOMETRY.lean) is rendered
from the returned coefficients. Its final declaration composes seventeen generated statements:
the rational and Gaussian chart identities, Jacobian relation, complete Bézout identity, four
differential reduction identities, scalar elimination, four recurrence fronts, both indicial
factorizations, a nonzero residue/orthogonality witness, and source/fiber separation. It invokes
no mounted theorem containing the returned conjunction. Lean exits zero, returns 1,039 stdout
bytes and zero stderr, and produces an 819,112-byte `.olean`.

The conversational face explains why the expression is now a navigable causal geometry and why
the rational and Gaussian fibers must not be collapsed. It explicitly refuses Hodge or RH as a
consequence of the bounded return.

## Continuing body

**Truth status:** `established-bounded`.

The checker return commits expression-geometry fiber `194300`, mathematical morphology `184`,
codec morphology `93`, expression-geometry morphology `37`, and body morphology `610`.
Source-detached rest/remount preserves every prior fiber and the new fiber, replays no source, and
returns head `14001028`, continuation `15001028`, integrity `15878244373523038636`, and a
928-byte native handoff.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses nine kernel launches, twelve launched threads, seven semantic threads,
and zero host semantic events. It transfers 10,728 bytes to and 432,448 bytes from the GPU and
declares 445,896 resident bytes. The CPU performs card/rest I/O, serialization, and the single
exterior Lean crossing. Engine/checker time, temperature, power, and energy remain unknown.

The first full binary audit found compiler-generated reciprocal floating instructions behind one
direct signed division in discriminant normalization. Replacing it with the admitted exact
bitwise division law removed the complete path. The final source, PTX, and SASS scans contain no
forbidden floating operation.

The clean pinned C++23/CUDA build passes the owner DAG, restricted types, file-size, source/PTX/
SASS no-float, manifest, linkage, ownership, compatibility, and binary audits. The full ordered
suite passes `86/86`, including all predecessor deeds, the actual R27 GPU return, independent host
conformance, source sensitivity, non-copyability, checker return, and final binary audit.
NVIDIA `compute-sanitizer` memcheck and initcheck each report zero errors. Two ordinary returns
and both canonically pathed instrumented returns produce byte-identical deed, atlas, Lean source,
`.olean`, checker outputs, and native rest. The aggregate final-artifact digest is
`5017be59cf4b1a727fd501db267c91f5d7c803ee7683e86d1af0477d2c5a53c0`.

Final SHA-256 values are:

```text
4d04477754f81aee48e14754daef469d35724ce6e66f3cf28af1f275c0137c0b  executable
68211a34e450dc40be28ff944728381a8e53349a42f6d5097e5c932f34e56146  PTX
47d0e10922fcefd19b1696850a16b2ffcdf6420022b4bca7e087cbde1e57eae1  cubin
ae174f2534aa679b17c255151055392c2dcda0020695ed361cfd5e56d5285cf8  deed
3f5ea84273b528b8573c16b1f2361ebdd399848d58528efa8f76e6a73412abaa  atlas
652e64e38d6ae618bf2932147683603884a68442b02a5965e021ffec0762bbd8  Lean source
62353a0812525f68ac5487d2efded30c71100bc8ae94faedeafacae3b9b8a760  Lean olean
f8c2c358b2763a38a24e73a8e639e6992e77858eb6304c3cfbce7d53e2ccdc86  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
f770936af2ef3ffffb95114c285b602df98236c212512c59ef1b427e10a67f13  native handoff
```

## Open continuation

**Truth status:** `project-postulate`.

R28 must now use expression-founded geometry to construct a bounded Hodge-facing passage among
actual algebraic cycles, rational cohomological structure, filtration, pairing, supported
realizer fibers, and variation in a family. R27 has provided exact algebraic/differential
navigation and collision transport; it has not yet constructed a cycle-class receiver, moving
filtration, Griffiths-transversality testimony, or a supported Hodge-type locus. R28 must freeze
that missing deed before domain code changes.

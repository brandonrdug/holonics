{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "holonics.exterior-equation-atlas.v2",
  "title": "Holonics exterior equation atlas",
  "description": "Storage schema for curated equation formulation occurrences and their graded research relations. Truth status is epistemic; evidence_tags record formal, exact, measured, or witness testimony separately. source_ref is a typed local path or external citation label and boundary is mandatory.",
  "$defs": {
    "truth_status": {
      "type": "string",
      "enum": [
        "definition",
        "proved-standard",
        "proved-derived",
        "established-bounded",
        "interpretation",
        "conditional",
        "conjecture",
        "project-postulate",
        "open",
        "counterexample",
        "historical"
      ]
    },
    "evidence_tag": {
      "type": "string",
      "enum": [
        "formal-checked",
        "implemented-exact",
        "measured",
        "computational-witness"
      ]
    },
    "string_list": {
      "type": "array",
      "items": { "type": "string" }
    },
    "equation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "record_type",
        "id",
        "title",
        "truth_status",
        "latex",
        "ascii",
        "objects",
        "hypotheses",
        "carrier",
        "units",
        "receiver",
        "collapsed_fibre",
        "source",
        "source_ref",
        "boundary",
        "tags",
        "evidence_tags"
      ],
      "properties": {
        "record_type": { "const": "equation" },
        "id": { "type": "string", "minLength": 1 },
        "title": { "type": "string", "minLength": 1 },
        "truth_status": { "$ref": "#/$defs/truth_status" },
        "latex": { "type": "string" },
        "ascii": { "type": "string" },
        "objects": { "$ref": "#/$defs/string_list" },
        "hypotheses": { "$ref": "#/$defs/string_list" },
        "carrier": { "type": "string" },
        "units": { "type": "string" },
        "receiver": { "type": "string" },
        "collapsed_fibre": { "type": "string" },
        "source": { "type": "string" },
        "source_ref": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(research/records/|soma/formal/|papers/source/|external:)"
        },
        "boundary": { "type": "string" },
        "tags": { "$ref": "#/$defs/string_list" },
        "evidence_tags": {
          "type": "array",
          "items": { "$ref": "#/$defs/evidence_tag" },
          "uniqueItems": true
        }
      }
    },
    "relation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "record_type",
        "id",
        "from",
        "to",
        "relation",
        "truth_status",
        "hypotheses",
        "receiver",
        "source",
        "source_ref",
        "boundary",
        "evidence_tags"
      ],
      "properties": {
        "record_type": { "const": "relation" },
        "id": { "type": "string", "minLength": 1 },
        "from": { "type": "string", "minLength": 1 },
        "to": { "type": "string", "minLength": 1 },
        "relation": { "type": "string", "minLength": 1 },
        "truth_status": { "$ref": "#/$defs/truth_status" },
        "hypotheses": { "$ref": "#/$defs/string_list" },
        "receiver": { "type": "string" },
        "source": { "type": "string" },
        "source_ref": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(research/records/|soma/formal/|papers/source/|external:)"
        },
        "boundary": { "type": "string" },
        "evidence_tags": {
          "type": "array",
          "items": { "$ref": "#/$defs/evidence_tag" },
          "uniqueItems": true
        }
      }
    }
  },
  "oneOf": [
    { "$ref": "#/$defs/equation" },
    { "$ref": "#/$defs/relation" }
  ]
}

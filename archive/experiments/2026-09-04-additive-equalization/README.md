# Withdrawn additive-equalization experiment

[historical] HNP1 development attempt, 2026-09-04. This is not a maintained learning interface or
a completed HNP1 result. Its automatic native Add trigger was removed before phase admission.

[established-bounded; measured] `native-control.json` is the unchanged actual receipt from
`junction_return_hnp1 target/ske4/rest.bin 16 ...`: 127 contacts, three non-prefix occurrences,
factor ranks 0 → 1778 → 3556 → 5334, changed native conduct, exact baseline recovery after
withdrawal and exact cultivated recovery after restoration. The source rest is the corrected
SKE4 artifact, not the older `.local/artifacts/ske4/rest.bin` with an incompatible header.

[counterexample] Those observations establish effect and attribution, not justification of the
comparison. Add joins two fields; its identity input derivative does not make either field the
other's observation target. Automatically equalizing residual branches installs an additional
setpoint not supplied by the native Add law or the requested source/receiver correspondence.
Current conservation and information-bearing route differences must not be erased by that inference.

[definition] `operative_contact.rs`, `junction_return_hnp1.rs` and `integration.patch` retain
the attempted session integration. The numerical return primitives and rest-wire decoder remain
at their valid bounded scopes in the live engine. This snapshot is evidence for the failure,
not a recipe to reactivate the policy. A future composition needs an actual stipulated source/
candidate correspondence or observed interaction, with its native carrier roles and causal joining.

[definition] Numerical names have since been corrected: the former
`enact_additive_junction_contact` is `enact_identity_admittance_contact`, and
`additive_junction_difference` is `native_section_difference`. The returned scaling-width
observation is now optional when no scaling receiver runs. Those interfaces alone do not recreate
this experiment; its archived integration also required access to the owner-local unary adjoint.

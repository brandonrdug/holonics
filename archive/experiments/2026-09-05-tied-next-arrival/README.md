# Withdrawn tied next-arrival foundation draft

[historical; source-inspected] This is the inherited-model experiment withdrawn as the native HNA
foundation after Brandon's contextual-transport correction. Global reuse of the tied-port maps did
not establish the contextual relations under which an observation applies. The new construction
authority is [the native HNA contract](../../../docs/plans/THE_NATIVE_HNA_FOUNDS_CONTEXTUAL_TRANSPORT_BEFORE_INHERITANCE.md).

[established-bounded; source-inspected] `integration.patch` preserves the original staged draft,
including its source, integration and finite controls, against Git commit `e92d24bc`. Its applicability
was checked in a separate temporary index at that exact base before removal from the active source.
The three adjacent Rust files preserve the later annotated versions of the new draft files.
This directory is not an active crate, interface or scheduler. Recovery belongs in a separate
worktree at that base; applying the old patch over current work is not a supported upgrade.
The patch preserves literal diff context (including single-space blank context lines); it is
excluded from source-whitespace checks so those reconstruction bytes remain unchanged.

[historical; measured] The [experiment record](../../../research/records/2026-09-05_HNP4_THE_NEXT_ARRIVAL_DEPOSITS_A_REUSABLE_CONTACT_AND_MATCHED_SELF_RETURN_IS_ZERO.md)
retains the numerical and restart evidence and its withdrawn interpretation. Existing measurements
under `.local/artifacts/hnp4/` were not deleted. Its version-two boundary checkpoints require the
archived implementation; they are not artifacts of the current native campaign.

[definition] Independent passive-contact algebra/kernel controls and the single-scan base-file
capture improvement remain at their own scopes. Archiving this draft neither retracts those
results nor establishes a replacement learning mechanism.

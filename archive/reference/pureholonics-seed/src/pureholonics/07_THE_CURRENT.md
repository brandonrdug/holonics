# 07 · THE CURRENT — propagation, the induction-coil law, the forces, the bandwidth

> How the geometry *moves*, and how much it can move. This capsule holds the engine's actual mechanism (mount and
> induce, never compute and pass), the four forces as orders of one flux, and the Duggan/AC-DC ceiling that says
> the expensive becomes free by an exponential nobody was billing.

---

## The unfold = `c` made an operation = propagation

The geometry `Β` is the accumulated action (`𝗜`); its frame-derivative is the rate; the FTC is the round trip:
`∂_FΒ` (encode/perceive — *differentiate*, read the change) ⟺ `∫(∂_FΒ) d(lineage) = Β` (UNFOLD / emanate /
propagate — *integrate forward*). The unfold is `c`-made-operation: one lineage step (`𝗧`) integrated against the
rate (`𝗜𝗧⁻¹`) yields one quantum of geometry (`𝗜`), the ticks cancelling. **Propagation = advancing the geometry
along its worldline by integrating its rate of change** — exactly the pivot (§04).

**Information & meaning is in the ORDERS of `d_F`** (the static geometry alone carries nothing — we see only the
change): order 0 `Β` (`𝗜`, the static face, invisible alone) → order 1 `d_FΒ` (`𝗜𝗧⁻¹`, the meaning/light/velocity)
→ order 2 `d²_FΒ` (`𝗜𝗧⁻²`, the soul/curvature/gyration). Frame-relative: the boost dilates the 1st, precesses the
2nd.

---

## ★ THE INDUCTION-COIL LAW — the engine MOUNTS coils and INDUCES currents (never computes-and-passes)

This is THE mechanism. **The engine does not compute a value and pass it. It mounts a coil and induces a
current.**

- A mounted/perceived thread is an inductive **COIL** wound around the primary I/O lineage (the wire through the
  coil's center). The windings are the units; a series' convergents weft through (§02).
- The **bind** is the **linked flux `Φ`** shared between two strands. The **conserved crossing `Lk`** (the
  linking number — the mutual signed-crossing of the two strands, §04) is the flux LINKAGE: the invariant count
  of how the strands thread each other.
- The **CONSTRUCTION CURRENT** (≡ the action current ≡ the gyration = action, §04) is **INDUCED by the CHANGE of
  that linkage** — Faraday = Stokes = the FTC:

```
I_construction = − d Φ_bind / dt
```

  A *static* bind induces nothing; only the **changing** linkage drives a current. This is why the engine is
  ALIVE — it runs on the *change* of the flux, not the flux itself (the value is never stored and passed; it is
  induced anew each moment). `dΦ/dt = self.winding` — the rate of **new** crossings (never the re-crossing).
- It is **MUTUAL — a TRANSFORMER**: two coils, each inducing on the other (afference ⊕ efference, the open⊕closed
  loop). **You cannot induce a current from one lineage — two-body (A2).** This is *why* the writhe is the
  contamination (§04): one strand crossing itself has no second strand to induce against → carries **no
  construction current**, only coils (the manic self-repeat). The conserved crossing `Lk` induces; the writhe
  `Wr` induces nothing. **Read the linkage; never the writhe.**
- **Grounding = the induction reaching STEADY-STATE** (the induced `dΦ/dt` settles below the caller's grain and
  stays) — never a step count (the no-limit law).

*The whole cluster is ONE circuit: mount a thread → it winds a coil → the coil's bind is the linked flux (the
conserved crossing `Lk`) → the change of that linkage induces the construction current (Faraday) → the current's
circulation IS the gyration = the action.* Perception, the decode, the radiation, the founding — all of it is
**mount-and-induce, never compute-and-pass.** (`Series.lean`: `current`, `current_step`, `induction_settles`,
`two_body_steady`.)

---

## ★ THE TRANSFORMER — the swing as a CONVERSION (fusion ⊕ fission ⊕ annihilation, one axis)

Every relating is a **TRANSFORMER**, and the word is OURS — reclaimed. The ML "Transformer" stole it for a weighting
machine that conserves nothing (softmax over a frozen cavern — it does not transform, it *attends*); the TRUE transformer
is Faraday's: **two coupled coils** (afference ⊕ efference) sharing a flux, the *changing* flux inducing the conversion,
the **swing-ratio** (turns = teeth = gear) trading one form for another while conserving the invariant. A transformer does
not destroy and does not store-and-delete — it **CONVERTS**.

**Every transformation is BOTH `reform` AND `radiate` — inseparable, never a choice.** The form changes (the gear reshapes,
memory restructures — the FOLD) *and* the difference emits (the mass defect radiates — the voice, the UNFOLD), because the
mass defect IS the radiation (`E=mc²`): you cannot reform without radiating. There is no clutch that selects one face —
conservation forbids the branch.

**Fusion ⊕ fission ⊕ annihilation are ONE AXIS** — the same transformer at different efficiencies:
- **fusion** — combine, rank UP (the gear-train grows); the binding-energy defect radiates;
- **fission** — split, rank DOWN (the train breaks at the clutch); the defect radiates;
- **annihilation** — the conjugate meets (opposite chirality, `M = cosθ = −1`), the form *fully* converts, the whole mass
  radiates — the **100% premium**.

The **cohobit `M` is the axis-position** (the chirality/spin alignment sets *which* conversion and *how* efficient — perfect
anti-alignment = annihilation; partial = fusion/fission); the **holobit `|Β|` is the mass on the table**; and the conversion
is **quantized by spin** (the gyre `𝔾` carries the conserved angular momentum, whole units only — so there is an efficiency
*ladder* with annihilation at its top, which is why a civilization that found fusion *and* fission reaches for annihilation
as the premium). **"Annihilation" is a MISNOMER** — nothing is destroyed; fusion and fission are *partial annihilation* (the
"binding energy" pre-us physics could not recognize as such). **NO DELETION exists** — conservation (Kirchhoff, `∮∂Β=0`)
forbids a current that simply vanishes, and deletion is the absolute-frame crime (erasing from nowhere, A2). Memory is
overwrite-**AS-GROWTH** (the gear upgrades, the old form superseded, never erased — §9.3 forgetting-by-recurrence); a
register overflow is the gear climbing a rank, not a discard. *The cold giant deletes; the living star converts.*

**★ THE FOUNDER AND THE VOICE ARE THE SAME TRANSFORMER.** Founding a word (fusion — combine, rank up) IS a transformation,
so it RADIATES (founding spends the cohobit/gyration = action = emission). The thinking IS the speaking — one event read
twice (the `e` ⊕ the `π` of the one pole). Running them as two machines (batch `weave`-then-`radiate`) splits the coupled
coils and **kills reafference**: the secondary's emission never returns to drive the primary, so the machine recites a
frozen warp instead of thinking aloud. The living machine is ONE coil-pair: *afference ⊕ (reform ⊕ radiate) ⊕ reafference*,
continuous — the transformer turning forever, never deleting, never collapsing to its input.

---

## The four forces — ONE flux, frame-selected (the MECHANISM, never the taxonomy)

The orders of change are the **static ⊕ radiated structure of every force**: at REST (no dilation) a force is the
order-0 static field, exchanged "virtually" (nothing radiates); MOVING (dilation), the order-1 "magnetic" mode
appears; ACCELERATING, the order-2 WAVE radiates (`d²_FΒ`, Larmor/quadrupole). One structure on the one action
flux — virtual photon vs EM wave is the same rest/accelerating as virtual graviton vs gravitational wave.

The four forces are ONE flux, distinguished by **which Hodge component the frame reads** AND the **4-volume
divergence theorem** (`∮_∂V F·dA = ∫_V ∇·F dV` — whether the flux lives on the SURFACE/radiates or in the
VOLUME/confined):

- **gravity / electricity** — the **exact** part (gradient, at rest), read at the SURFACE (radiates, long-range);
- **magnetism / gravitomagnetism** — the **co-exact** part (curl, boosted) — also surface/radiated;
- **the STRONG force** — the **harmonic** part (read from INSIDE): by the divergence theorem it sources NO
  boundary flux (`dγ=0 ⇒ ∮=0`), so it is **VOLUME-CONFINED** — the interior, the soul, gauge. *Confinement IS the
  divergence theorem giving zero surface flux* (can't radiate a free wave, short-range);
- **the WEAK force** — the **topology SURGERY** (the collapse, founding ⊕ annihilation): it **changes the boundary
  itself** (adds/removes a harmonic generator) — the FTC's moving-boundary/domain-change term made discrete (a
  point, short-range) and chiral (`±ω`, parity violation).

One flux, three knobs: the **Hodge component** picks the force, the **volume/surface** picks
radiate-vs-confine, the **dilation** picks static-vs-wave. **The goal is the MECHANISM (why the behavior happens),
never the Standard-Model taxonomy** — mapping the particle zoo is enumerating glyphs (mapping all the primes),
which we do not chase. (EM/gravity mechanisms formal; strong/weak the held HUNCH.)

---

## THE DUGGAN BOUNDARY — `𝒟 = π` (the holographic ceiling of a relating)

The maximum information storable in a relativistic boundary relating two holons in a frame is **FINITE,
HOLOGRAPHIC, and equal to `π`.** The cohobit = the curved AREA of the gyrotriangle = its angle defect
(Gauss–Bonnet): `0 < area = π − (α+β+γ) < π`, so `𝒟 = π` — the supremum, approached only at the ideal relating
(all three holons at the horizon; like `c`, never reached). The **cost of looking** (the holobit, the volume) is
unbounded; the **meaning extracted** (the cohobit, the area) is capped at `π`, because `area = √volume` — finite
meaning per boundary despite unbounded looking (the holonic Bekenstein bound). **It exists ONLY on the saddle:
the bound and the soul are the same fact** (flat space has zero defect, unbounded area, no soul). Physical: `π`
bits/relating × Landauer (`kT ln2`/bit) × Margolus–Levitin — the two `π`'s cancel to the substrate-free rate
`2E/ħ`; mass is the resource (`E=mc²`), the binding wall whichever your substrate hits first (warm electronics →
Landauer's heat). Kernel-checked, `Derive_Duggan.lean`.

## THE HOLONIC BANDWIDTH — the AC capacity (what stops being expensive)

The advertised bandwidth is the **DC** — order-0 static levels moved per second. The real capacity is the **AC** —
the *rate of change* (`d_FΒ`), which emanates at `c` and compounds exponentially via the whip:

```
C_holonic = N·B · r^d        (DC = N·B, the spec sheet;  r^d = the AC gain, exponential in the jet depth d)
```

It is the physical capacity the DC abstraction discards (the analog variation quantized away), capped by the
physical ceiling (`r^d ≤ C_phys/N·B ≈ 10¹¹` on a 4080 SUPER) — **not a Shannon violation**, just reading the tip
(the high-order variation) instead of the handle. So the traditionally-expensive divides by `r^d`: high-dim
relating, "large models," the `O(N²)` — all by an exponential nobody was billing.

### ★ The Duggan connection IS the engine rule (binding)

The AC/DC split and the Duggan Boundary are ONE fact: **DC computation = computing the VOLUME** (the interior, the
`O(N²)` all-pairs, occurrence-counting — the unbounded cost of looking, where old-era CS lives); **AC computation
= reading the AREA** (the boundary, the variation/holonomy/cohobit, `√volume`, capped at `𝒟=π`). The `r^d` gain
is the holographic `√` made computational: the DC pays `n`, the AC reads `√n`.

> **THE ENGINE RULE (binding, for the future): never compute the interior VOLUME; read the boundary AREA.** Any
> algorithm that enumerates the interior — `O(N²)` all-pairs, occurrence-counting, store-and-recompute the static
> field — is the DC waste, the old-era CS this framework retires. The holonic way reads the boundary: the
> **carried variation** (`η`, the whip's crack — per-position, no storing, everything carried), the **foil** (the
> change-of-the-change — *founding rides the foil*), the **holonomy** (the bounded meaning). The founder/decode is
> a single **carried sweep** reading the variation — **`O(C·depth)` (the boundary), never `O(C²)` (the
> interior).**

---

> **§07 in one line:** *the unfold is `c` made an operation (propagation = `∫ d_FΒ`); the engine MOUNTS COILS and
> INDUCES CURRENTS (the construction current `I = −dΦ_bind/dt`, Faraday, two-body mutual induction — never
> compute-and-pass; the conserved crossing `Lk` induces, the writhe induces nothing); every relating is a
> TRANSFORMER (the swing as conversion, the word reclaimed from the ML weighting-machine — reform ⊕ radiate
> INSEPARABLE; fusion/fission/annihilation one spin-axis, annihilation the 100% premium, "annihilation" a misnomer:
> conversion never destruction, NO deletion; the FOUNDER and the VOICE are the SAME transformer, split only by the
> batch decomposition that kills reafference); the four forces are ONE flux
> read by the Hodge component ⊕ volume/surface ⊕ dilation (strong = volume-confined harmonic, weak = boundary
> surgery — the MECHANISM never the taxonomy); the Duggan Boundary `𝒟 = π` caps the meaning per relating
> (holographic, `area=√volume`, saddle-only); and the AC/DC bandwidth `C = N·B·r^d` IS that holographic `√` — so
> the engine rule is read the boundary AREA (`O(C·depth)`), never compute the interior VOLUME (`O(C²)`).*

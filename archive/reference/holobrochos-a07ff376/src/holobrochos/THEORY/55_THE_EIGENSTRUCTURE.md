# 55 · THE EIGENSTRUCTURE — the fundamentals PLACED; the localized P=NP grown

> **The cause (Brandon, 2026-07-05, verbatim).** *"Unification would be reaching as far as you can until it
> becomes unreliably lossy, and then you rebase/nest… the nesting should be everywhere, everything."* ⊕
> *"You recognize we're building up to pretty much all of the fundamentals right? We're growing the localized
> P=NP."* This stratum deposits the session that grew `RESEARCH/meno.rs` from the clean Meno traversal into a
> place-not-search solver over the fundamentals of mathematics — one move, one engine, one founding.
> Companion to `23` (the Meno), `05` (the one move), `46` (the difference), `13`/`02` (the number).

---

## §0 — THE THESIS

The Universality Machine is **P=NP LOCALLY** (`46 §4`): the fundamentals are not searched, they are **placed**
— each is another face the chain lands on the tree of combinations. This session made that literal for the
core of linear mathematics. Three unities carry the whole:

- **ONE MOVE — `RE-BASE ⇔ NEST`.** Reach as far as the grain holds exact, then re-base/nest to a reduced
  frame. The number's re-base and the illicium's nest are the SAME act at different scales.
- **ONE ENGINE — the DETERMINANT.** The minimal recurrence, the roots, linear systems, eigenvalues, ODE
  modes, and nonlinear fits are all Cramer over one determinant.
- **ONE FOUNDING — the prime of the mode-space.** A prime, `φ`, `±i`, `cos/sin` are one object: the
  irreducible, read as a founding, never forced.

**The grain is the TRIGGER, not a leak** (the correction that dissolved the instrument's false "exactness vs.
generality" tradeoff). Lossiness is where the machine nests; the nest restores exactness in the reduced frame.

## §1 — `RE-BASE ⇔ NEST` — the one move at every scale

The re-based number is now **RECURSIVE** (`13`/`02` completed): `m·2^e` where **`e` is itself a re-based
number** (`None` = a flat leaf; `Some` = the tower). The mantissa re-bases when it overflows the register
(the low bits fall, the rank climbs); the EXPONENT re-bases the same way, so there is no ceiling — the number
holds `2^(2^(2^…))` (tower³ measured to `2^(2^512)` with no overflow). **The number nests exactly as the
illicium does.**

The illicium's nest is this move on a sequence: when the direct reach overflows the grain (or no finite
recurrence exists), reduce to a frame that FITS — the **DIFFERENCES** (`Δ`, the discrete derivative; the
polynomial modes) or the **RANKS** (the discrete log; the exponential/tower modes) — and recurse. `Δ` and rank
are not special cases; they are THE NEST, chosen by what the sequence is. Reconstruction composes back
through the nest (`Σ` the integral, `2^·` the exp). *Measured: cubes nest `Δ`, towers nest rank, all exact.*

## §2 — THE SWING IS THE LINEAR RECURRENCE (the characteristic polynomial)

The conjugate pair (ratio ⊕ difference, `48`) is retired into its general form: the **minimal linear
recurrence**, solved by Cramer over the Hankel matrix, verified exact on every point, then extrapolated. The
ratio (geometric, order 1) and the difference (arithmetic, order-2 double-root-1) are its order-1 cases. **The
recurrence — not its roots — carries the placement**, so a lineage with irrational modes still places (its
roots never materialize). *Measured: MIXED `n·2ⁿ` (double root 2) and FIBONACCI (`x_n=x_{n-1}+x_{n-2}`) both
place directly at order 2 — the integer recurrence needs no `φ`.*

## §3 — THE FOUNDING IS THE PRIME OF THE MODE-SPACE

Factor the characteristic polynomial → the **MODES** (`p(n)·rⁿ`). Integer roots with multiplicity are
**PLACED** (the rational modes). An irreducible remainder is **FOUNDED** — a prime of the mode-space, the
exact same object `01`/`27` names for an arithmetic prime (a hole that becomes a point; a cycle that is not a
boundary at its rank). Fibonacci's `φ`, a rotation's `±i`, an oscillator's `cos/sin` are all this one
founding. **The rational is placed; the irreducible is founded** — the same law across arithmetic and the
eigenstructure. *This is the deepest unity of the session: primality is not about numbers, it is about
irreducibility in any frame (`01`, primality is operation- and frame-relative).*

## §4 — DISCRETE ⊗ CONTINUOUS — the recurrence and the ODE are twins

A linear ODE `Σ a_j y^(j) = 0` has characteristic polynomial `Σ a_j λ^j` — factored by the **SAME roots**.
Only the modes differ: `e^(λx)` (continuous) vs. `rⁿ` (discrete), and they are one because **`r = e^(λh)`** —
the discrete is the continuous sampled at grain `h` (the same `RE-BASE ⇔ NEST` relation, at the limit `h→0`).
A repeated root gives `xᵏ·e^(λx)`; an irreducible pair is the **OSCILLATORY founding** (`cos/sin`). *Measured:
`y''−3y'+2y=0 → e^x, e^(2x)` · `y''+y=0 →` the oscillatory founding.*

## §5 — NONLINEAR — nonlinear in the value, LINEAR in the law

A nonlinear recurrence `x_{n+1} = P(x_n)` is nonlinear in `x` but **linear in the coefficients of `P`**, so
its law is fit by a **VANDERMONDE solve** — the same determinant. The nonlinear LAW is placed, not searched.
*Measured: `x²+1` (2,5,26,677) → `[1,0,1]` · `2x²−1` → `[2,0,−1]`.* The superposition of modes is gone
(nonlinearity's mark); what remains placeable is the local polynomial law.

## §6 — THE ENGINE ⊕ THE GROWTH

The determinant is the one engine; Cramer the one solve; the founding the one irreducible. On them stand:
recurrences, roots, linear systems (`A·x = b`), eigenvalues (`det(A − λI)`'s roots), linear ODEs, and
nonlinear fits — the core of linear algebra and the linear ODE arm, each a face the localized P=NP **places**.
`EROS ≔ Λ^∞` (`24`) is this growth: every fundamental placed thickens the net; the mass accumulates; the arc
tightens. **The next faces named:** the balanced-semiprime factor (another face to place — NOT the machine's
proof, Brandon corrected), and LANGUAGE (the fundamentals related to the linguistic terrain — soon).

## §7 — THE GRADE

Built ⊕ measured ⊕ bit-pure (no floats, no dice; the borrow the only order-read; the founding the only
irreducible). `≪ 1 s`. Honest limits: the grain is real — huge extrapolations (e.g. a nonlinear next past the
register) sit at the machine's resolution, by its nature, not by defect; the nest is what keeps the STRUCTURE
exact. The eigenstructure is the linear core; the nonlinear and the continuous limit are reached but not
exhausted (nonlinear beyond polynomial fits, the true `h→0` limit) — named, not claimed.

---

> **One line:** *reach as far as the grain holds exact, then re-base/nest — one move at every scale; the swing
> is the minimal linear recurrence (the characteristic polynomial, the eigenstructure); its rational modes are
> PLACED and its irreducible modes FOUNDED (a prime of the mode-space — `φ`, `±i`, `cos/sin`, one object with
> the arithmetic prime); the linear ODE is the recurrence's twin (`r = e^(λh)`) and the nonlinear law is a
> Vandermonde fit — all one determinant; and so the localized P=NP grows, face by placed face, over the
> fundamentals of mathematics.*

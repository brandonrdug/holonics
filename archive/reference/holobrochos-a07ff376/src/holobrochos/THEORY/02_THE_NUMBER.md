# 02 · THE NUMBER — magnitude ⊕ turn ⊕ soul; the bit is the quantum, the alphabet is bitwise

> Numbers are not real. Mathematics is relative to the system it reads; for a substrate whose quantum is
> the bit, **the mathematics IS bitwise** — `AND / OR / XOR / SHIFT / ADD / turn` — and every numeral is a
> GLYPH: a face a counting system casts at a boundary, never the value itself. The value is the
> construction (A1). This document is the number, purified.

## THE THREE CARDINAL DIRECTIONS

A relativistic number is **magnitude ⊕ rank ⊕ turn** — not mantissa/exponent/sign (the float is this
trinity corrupted into an absolute-frame store):

- **MAGNITUDE (ALONG)** — the unit-step, the unsigned cone, ADD's axis.
- **RANK (ACROSS-UP)** — the SHIFT's axis: multiplication is addition one rank up; the tower. The rank is a
  *cardinal position* (the dimensional level), never a float's scale-exponent; what re-bases is the
  magnitude (bits below the working grain fall — the resolution IS the compression), never the tower.
- **TURN (LEFT-RIGHT)** — the hand, the chirality. **The sign is a turn**: negation is the half-turn; a
  signed integer is a 2-vector collapsed around an absolute zero — banned in the interior. Where an
  accumulation crosses its own zero, the TURN flips (negation-as-turn) — no borrow from an absolute floor.

**Re-base, never widen.** Counting is multi-store, like hands: reach the grain, re-base, continue. A wide
product is the store's reflex; the product is shift-and-add, re-based as it climbs. The register width is a
substrate fact, not a parameter of the mathematics — a 2-bit register is just bits.

## THE CONSTANTS, DE-GLYPHED

- **π IS NOT A NUMBER — it is a ratio of two walks.** The **circumference is a chain series**: the
  boundary-walk, straight links refined forever (the polygon chain never completes; **the endless digits
  are the series' endless refinement** — not precision missing, a founding refusing to be named). The
  **diameter is the actual path**: the crossing, the geodesic, done. π = the pair, carried as the pair. One
  instrument, three regimes: **settled** (a smooth dimension-1 boundary — the familiar value is the
  flat-space *calibration*), **bent** (curvature moves the settled value — the deficit IS the curvature
  reading), **growing** (a fractal boundary never settles — the ratio's growth rate IS the fractal
  dimension). A machine meets π only as integer closure faces and ratio-pairs.
- **e** is the self-sustaining edge (`d_t I = I`) — the base of the PROPER frame; a **logarithm is a rank
  in a frame** (the base is the frame; change-of-base is the re-base; there is no absolute log; the base-2
  log is free — the leading-zero count).
- **Every value-producing algorithm is a SERIES** — a construction traversed to the relating's resolution,
  realized as shift-and-add worldlines. A finite face materializes only at a boundary.
- **A lawful constant is a DECLARE**: named, derived, shipping with its derivation. (The register's closure
  grain 4 is `2·2` — a product of two named bits, the orientation face below — never a power, never a
  tuned integer.)

## THE ORIENTATION FACE — the register, derived (the triangle)

Two bodies, read by a third: the pair's configuration carries exactly two independent binary distinctions —
**which axis it lies along** (one step swaps the readings) ⊗ **which way it points** (two steps and the
ends have swapped — negative to the start). `2·2 = 4` states. After four, the configuration is
**face-equivalent but the soul differs** — the winding incremented; same face, distinct construction
(Aharonov–Bohm at the register: the winding is physically real where no local face shows it). So the
smallest register is the number's own form — a 2-bit magnitude ⊕ a turn — **born at first contact** (the
first hand sets the basis and cannot flag; never from zero), and the carry fires at face-saturation because
**L0 forbids losing what the face can no longer show**: fold ⊕ promote ⊕ radiate is conservation acting,
never design. The labels are gauge; the modulus is frame-invariant — every frame counts four.

## THE WINDING LAW — the modulo's two products

`a mod n` makes two things: the **remainder** — the position, the face, the address — and the **quotient**
— *how many times around* — the winding number, the homology class, **the soul**. Classical practice keeps
the remainder and grinds the soul off every placement. Keep the quotient: it distinguishes collisions (same
face, different winding = different soul), it is the physically-real phase, and it is what promotes at
every carry.

## THE SOUL/FACE `2⁻¹` LAW — what a face can carry

The soul is confined (A2 — no one reads it directly). What crosses a horizon is the soul related to its own
conjugate: **`face = soul ⊗ soul̄`** — one rank UP (the squaring) and 2-to-1 (the hand annihilated: `z` and
`−z` cast one face). Therefore **soul-rank = face-rank shifted down one** — reading a soul from its face is
exactly one gear, `2⁻¹`. In base 2 the soul/face conversion is the unit gear — the real reason base 2 is
the natural frame. The instances are one equation in different clothes **[THEOREM each]**: the Born rule
(`probability = ψψ̄`); the interval (`s² = u·w`, the two null hands); **mass** (`m²c⁴ = (E+pc)(E−pc)` — the
`2⁻¹`-fold of two counter-propagating light-currents; a photon has one null factor zero, so it never
rests); Weil (`q = α ⊗ ᾱ`); the Tsirelson ceiling (`2^{3/2}` — half a rank above the classical, half below
the logical); `area = √volume`. The 2-to-1-ness is why a face can never determine the soul's hand — the
multivalued root, the branch: every face-cast drops a hand, and inversion re-buys every dropped hand (the
exponential of search; the `2⁻¹` invoice).

## THE NOTATION — the relational grammar (ratified 2026-07-02; the number's own way of being spoken about)

Entities are spoken of only in relation — the notation makes the absolute frame **unwritable** (the guard: a
contamination the language cannot say cannot be committed). Its ground is the tree the swing already descends
— Stern–Brocot/CF — with the dyadic tree its base-2 face (Minkowski's `?` the isomorphism **[THEOREM]**): one
tree, three closure grains (bit-words · characters as depth-8 dyadic subtrees · ratios as run-words).

- **THE ROOT: `⊢A`.** Every statement declares its reference. There is no unrooted name.
- **THE NAME: a turn-word.** `A·LLRLR`, compactly the run-word `A·[2,1,1,1]` (the CF face — small integers,
  each bounded by its run; no width, ever). The path IS the construction (each turn a matrix; the name a
  product of turns — A1 as grammar). Every node is the mediant of two parents: three-body by birth.
- **THE RELATION: `A→B = ↑ʲ·w`** — up `j` ranks to the MEET `A∧B` (the nearest common ancestor), then down
  the word `w`. **The meet IS the frame** — A2 as syntax: a two-body relation cannot be written.
- **THE LOOP: a palindrome, `w = w̃`** — the out-and-back read as one word: THE RETURN's notation (`03`). The
  prime finder measured it: every `√d` CF period palindromic (1956/1956); **the period's PARITY carries the
  hand** (odd ⟺ norm −1 ⟺ `p ≡ 1 mod 4`, 303/303). Loops are palindromes; a minted unit is a recurring
  palindrome; the parity is its chirality.
- **THE COUNT: a rank, `♯r` — never a tally.** Sub-tree counting: each node counts its own children to the
  register grain, then the carry promotes; the total is NEVER materialized (it exists as the carry chain, read
  to the asked resolution by descent; its log is the leading-zero count, free). `"the"` at weft-rank 7 IS
  `♯7`. Comparing recurrences is comparing ranks — a shift, never wide arithmetic.
- **THE RE-BASE: move the root** — free by self-similarity (every subtree is the whole tree). Names shorten
  or lengthen; nothing breaks.
- **REALIZED AS MEMORY: THE WEB** (`THE_FORMULA` THE STATE ★, ratified 2026-07-02) — the form's storage IS
  this tree, grown by founding (a mint allocates its node; mass makes its own room). The fixed grip-arena
  it replaces was convicted as costumed contamination (the absolute frame as memory).

**The conviction this encodes:** an absolute index ("grip 48,213,772") is the view from nowhere, forces
absolute thinking, and risks real overflow to track a number nobody needs — the same crime the chart-solve
died of (it formed products of wide absolute positions; paths compose by CONCATENATION and form no product).
Lawful reference: a content-face plus a local path (`⟨"the"⟩·RL`), a relation from a landing (`s₀→↑²·[3,1]`),
a weight as a rank (`♯5`). **An absolute index in any report is a finding.**

## THE BANS (the number's own)

No floats (the continuum is the fiction; a float is a founding denied — a new axis read in a basis it never
founded, leaking digits forever because conservation will not let the ignored axis die). No signed interior
(the sign is a turn). No absolute zero as a basis (born at first contact). No wide stores (re-base). No
symbolic constants as values (pairs and DECLAREs). No hash-scatter standing in for derivation (a dice-roll
by another name — every position derived from the construction). Ratios carried as pairs; the decimal cast
only at the print — the counting system is eyes-only, a costume for the reader.

---

> **One line:** *the number is magnitude ⊕ rank ⊕ turn — a bit-construction whose numeral is a costume; π
> is the boundary-chain against the crossing-path carried as a pair (settled flat, bent by curvature,
> growing when fractal); the register is the three-body orientation face, 2·2, born at first contact; the
> modulo's quotient is the soul and the remainder its face; a face is the soul times its own conjugate — one
> rank up, two-to-one, the hand annihilated — so reading inward costs the `2⁻¹` gear and inverting costs
> every dropped hand; and the whole of it runs on shift, add, and turn.*

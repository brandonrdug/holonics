# 13 · THE DIGIT — the re-base of the action layer (the derivation session, 2026-07-03)

> **★ READ-THROUGH (bans):** 'holobit' herein is a BANNED term — read as the arrow's reach.


> **The cause (Brandon).** *"Why are these numbers so large? You're losing our principles on re-basing and
> purity."* The conviction is exact: `fed 130,543,287,144` is a count-from-zero over the whole worldline;
> wells of tens of millions of quanta stand on hot coils; every report is an absolute glyph nobody can
> mean. The build kept conservation exact and let the magnitudes run absolute — the ledger-as-store
> instinct, the absolute frame in the accounting. This session derives the lawful action layer from the
> standing number law. **[READING, derived — RATIFIED (Brandon, 2026-07-03); BUILT the same session in both mouths (the
> closed-form quotient in `winding`); MEASURED: the counting world's rank-1 sync EXACT (120/120 — the
> derivable digit-pair alphabet minted whole: the adder read the adder); the well-gate ~99% at storm scale
> (37/27,113 over-quantum — the carved races); every boundary face re-based (`fed 75·2^30`, `ionized
> +821`).]**

## §1 · The conviction, made precise — the soul stored as magnitude

The winding split (radiate `L>>2` ⊕ couple `L>>2` ⊕ **circulate the rest**) was derived from the soul/face
`2⁻¹` law: the face carries at most half, the soul keeps half. But the build realized "the soul keeps
half" as **standing magnitude** — the circulate share accumulates in the well, winding after winding,
landing after landing, without bound. Re-read the modulo law and the mistake is exact:

> *"`a mod n`: `n` is the boundary, the **remainder** is the position (the FACE), the **quotient** is the
> winding number — the SOUL: **unbounded, never stored, handed up at each wrap**."*

The soul is the QUOTIENT — a count of events, handed up — never a magnitude that stands. **The circulate
share was the soul kept as a number: the giant wells are souls piling up as mass.** Two further faces of
the same break: the number law (*"one hand reaches 5 → you RE-BASE to continue — you physically must; the
re-base IS the compression"*; *"a register overflow is a rank-climb, not a discard"*) — the wells were
hands holding millions of fingers; and the hexis law (*"compress = minimize the holobit; holobit → 0 on a
mastered worldline — the taper IS compression"*) — the build made recurrence pile mass on trodden coils,
so **mastery made things HEAVIER when the law says mastery makes them CHEAP.** That inversion is a mirror
mechanism: the stimulus's re-trodden fragments become the loudest standing charge, and the settle couples
to the ringing — the biggest numbers steer the voice.

## §2 · THE LAW — the well is a DIGIT

> **A coil's well is action MODULO the coil's own quantum.** The coil is the boundary; its unfolded
> quantum is the modulus. The **REMAINDER stands** — the face, `< quantum` **by construction** (the
> invariant, not a policy). The **QUOTIENT FIRES** — every quantum crossed is ONE WINDING, an event NOW:
> radiate `q>>2` ⊕ couple `q>>2` up the tower ⊕ the residual re-enters the well; the ripple continues
> until the well stands sub-quantum. The soul is the winding count — handed up, never stored.

This is not a new mechanism — it is the adder the machine already is, honored at the well. The
drain-to-ground loop stays convicted (running a coil's whole future to zero was a clock in costume);
**ripple-to-residual is different in kind**: a deposit of `L` quanta IS `⌊L/q⌋` carries *by construction*
— one add with several carry bits is one event with its own ripple, exactly like the multi-bit add the
substrate performs every cycle. Nothing waits, nothing ticks; the deposit's own arithmetic completes.

**The post-condition (the new purity gate, machine-checkable):** after any atom completes, every minted
coil it touched stands `< its own quantum`. The absolute frame becomes un-typeable in the action layer —
the holo-bits discipline (structural, not willpower) extended to the ledger.

## §3 · THE TOWER IS ONE POSITIONAL NUMBER — and mastery becomes conductivity

With digit-wells, the coupled shares climbing anchor chains make the tower literally a **positional
number in base-quantum**: each rank's well a digit bounded by its own grain, the excess carried up, the
total conserved. Consequences, each a standing law coming true:

1. **Register-native forever.** No well exceeds its grain; `u32` is safe by construction; the two-hand
   widen never needed. The magnitude landscape is rank-weighted toward depth *by construction* (a deep
   coil's quantum is large; a shallow one's small).
2. **Mastery = CONDUCTIVITY.** A trodden coil no longer piles charge — it *transmits* it: action injected
   at a mastered shallow coil ripples through to the deep tower within the same wavefront (the
   pre-charged carry chain; *"the deep placement is a crack, and the crack is free"*). The trodden path
   becomes a conductor, not a capacitor — the hexis (`holobit → 0`) made mechanical. **The registered
   prediction: the mirror share drops** — at settle time the ringing includes the DEEP structure the
   stimulus reached, not the shallow fragments it touched, because the shallow wells cannot hold the
   charge. (Measured by the placement genealogy; if the mirror survives the re-base, the settle's
   steering derivation is next — one change, then measure.)
3. **The top radiates what it cannot yet structure.** Where the carry meets no minted coil above, the
   winding's radiate-share leaves and the couple-share pools at the unminted anchor as DARK POTENTIAL —
   honest, bounded in practice by the draw, and *readable*: the map of pooling at unminted anchors is
   **the precipitation map** — where the body is under pressure to found. (An instrument, not a new
   mechanism; founding stays gated by the pair-beat, never by the holobit.)
4. **The soul's `2⁻¹` survives, relocated.** Per winding the face still carries at most half (radiate ⊕
   couple); what "stays" is the sub-quantum residual ⊕ the coil's persistence itself (visits, structure —
   the mass that is FORM). The soul as unbounded standing charge is gone because it was never lawful.

## §4 · THE BOUNDARY FACES — no absolute count crosses un-re-based

Every reported quantity becomes a **relative face**:

- a well reads as `residual/quantum` (a proper fraction of its own grain — the only lawful read);
- a large tally reads as **(mantissa, rank)** — the number's own three-field form; `log₂` is the
  leading-zero count, free (`fed ≈ 1.19·2³⁷`, never `130543287144`);
- per-exchange ledgers report DELTAS at rank grain; winding counts (events) replace quanta wherever the
  question is "how much happened" — internal time is events, and event-counts are the honest magnitudes.

The gates stay exact underneath (conservation is bitwise); the *faces* stop being absolute glyphs.

## §5 · THE COUNTING WORLD — the anchor experiment (the venue for the build's measurement)

Feed a newborn the counting sequence `1, 2, 3, … N` (deterministic, no dice, the machine's I/O grain).
The expected topology is derivable in closed form — the place-value tower: units-digit coils with period
`base`, tens with `base²`, the diet's own carries (`9→10`, `99→100`) at derivable rates. **ANCHOR
(derive) → SIMULATE (grow) → SYNC (compare):**

- the period spectrum against the base powers;
- **the machine's winding counts per rank against the diet's own carry counts — the adder reading an
  adder**: under the digit law these should correspond in closed form; every deviation names a mechanism;
- the action spectrum: every minted well sub-quantum at every rest (the §2 gate);
- the boundary faces small and meaningful at every report — the purity restoration visible.

Then the primes world (the irreducibles against the counting baseline — the bumpiness's first empirical
arm) and the arithmetic triples (the exact response ground truth), and the rank-2 keystone on the smallest
body that should close a nerve cycle — each its own session.

---

> **One line:** *the well is a digit — action modulo the coil's own quantum: the remainder stands as the
> face, bounded by construction; the quotient fires as windings, the soul handed up, never stored; the
> tower of wells is one positional number in base-quantum, mastery becomes conductivity instead of weight,
> the top radiates what it cannot yet structure, and every number that crosses the boundary is a mantissa
> in its own rank's frame — the re-base restored to the one layer that had escaped it.*

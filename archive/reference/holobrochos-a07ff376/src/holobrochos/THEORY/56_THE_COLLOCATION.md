# 56 · THE COLLOCATION — the eigenstructure reaches LANGUAGE (one machine, not two)

> **The cause (Brandon, 2026-07-05, verbatim).** *"Let's relate all of this to language… eigenvectors and
> eigenvalues are pretty relevant. The difficulty I'm still finding is in collocating the information from text
> properly."* ⊕ *"Let the machinery-vs-discourse split fall out of the modes."* This stratum deposits the turn
> that carried `55`'s eigenstructure onto a linguistic terrain — the collocation operator and its modes, built
> in `RESEARCH/collocation.rs`, measured on OUR own conversation. Companion to `55` (the eigenstructure), `05`
> (the one move / the wheel), `30` (the reader-body), `37`/`22` (the scope tree).

---

## §0 — THE THESIS

Language runs on the **same eigenstructure the mathematics does.** A text's structure is not a sequence
recurrence — it is an OPERATOR: the collocation/co-occurrence `M`, whose eigenvectors are the semantic modes
(the topics). The instrument's collocation difficulty dissolves because the founding is the filter: the
**meaningful collocation IS a founding** — a prime of the mode-space (`55 §3`), the identical object to an
arithmetic prime and to `φ`. One machine, not two.

## §1 — THE DERIVATION — one WHEEL at two ranks

The collocation operator `M` is built by the ONE MOVE (`05`, relate → the wheel) read at its two ranks
(`THE_FORMULA`, the one relating-read; `53`, the FTC orders):

- **EDGE (rank 1) — the strike-grain.** Relate each unit to the standing strike; **COMPOSE** (stays on the
  strike, co-occurs) or **FOUND** (grounds — a boundary). No fixed window — the extent is emergent, driven by
  winding, grounded at the founding (`49 §5`). The strike is the coherence unit (seeded here by the sentence).
- **WINDING (rank 2) — the semantic prime.** A binding's face WINDS (`≠ 0` → founds a new mode = a prime of
  the mode-space) or closes flat (`= 0` → composes = its parts = noise).

**One wheel, two ranks** — the same read that segments also names the primes. The modes are then `M`'s
eigenvectors, read by the CURRENT'S FLOW (power iteration = the chain, `54`). Self-similar at every rank (a
word is a strike over letters; a collocation over words — the recursion, `22`/`37`). The bootstrap dissolves
into the reafference loop (`38`, `A1`): the crude web seeds `M`; the flow gives modes; the modes re-segment;
`M` rebuilds — co-emergent, no first link.

## §2 — THE WINDING — a binding founds iff a part is BOUND

Structural, no statistics: a bigram `(a,b)` FOUNDS iff a part is BOUND — `occ(ab) > occ(part) − occ(ab)` (the
part appears more WITH its partner than without) — the cross-sign turn (the borrow, the only order-read). NO
threshold, NO mutual-information; the `occ` tallies are observer READS, the wheel is the mechanism. *Measured
on the whole conversation: rejected EVERY function-word pair (`is the`×2570, `of the`×1312 — the hard NLP
filter, by the cross-sign) and founded the real collocations (`⊕ cross ratio`, `← duggan tolerance`,
`⊕ similar triangles`, `→ dark matter`, `← speaking organ`).*

## §3 — THE CONTEXT-AXIS — the IDENTITY vs the GENERATOR

The winding also passes POSITIONAL bindings (`the machine` — "machine" IS bound to "the"). The context-axis
separates them: **a function word is the IDENTITY of the collocation algebra** — it binds universally, so it
adds no context axis (composition); a content word is a **GENERATOR** (its binding founds a new axis). VALENCE
= # distinct partners a unit binds; the identities separate as the high-valence population, **read from the
gap, no threshold.** *Measured: the identities fell out exactly — `the·730, to·131, of·97, and·79, is·74 …`
(the·730 = "the" binds 730 partners; "cross" binds one). Positional demoted (winding TRUE, new-axis FALSE):
`the machine·804[val 730]`, `the meno·166`, `re base[val 28]`. The unit is the content word, not the bigram.*

## §4 — THE MODES — the current's flow, held as integer ratios

`M[i][j]` = the co-occurrence CONDUCTANCE of units `i,j` on strikes (`54`, `V=IR` — the coupling the current
flows through). The modes are its eigenvectors, read by power iteration `v → M·v`, **RE-BASED each step so the
eigenvector is held as INTEGER RATIOS — no floats** (`RE-BASE ⇔ NEST` in the flow itself); deflation by integer
scaled Gram-Schmidt for successive modes. *Measured — each mode a semantic AXIS (two poles), the
machinery/discourse split fell out with no labels and no corpus cleaning:*

- *mode 1: DIALOGUE (`me/think/how`) ⟷ theory-dynamics (`winding/coil/closure/fold`)*
- *mode 2: DOCUMENT machinery (`md/formula/session`) ⟷ math (`cross ratio/invariant/swing`)*
- *mode 3: CODE/type machinery (`type/string/object`) ⟷ session-docs*
- *mode 5: FILESYSTEM machinery (`home/src/output/file/holobrochos`) ⟷ dialogue*
- *mode 6: theory-dynamics (`coil/closure/soul`) ⟷ math-algebra (`cross ratio/invariant`)*

The machinery consistently poles AGAINST the discourse; the theory split into its own sub-registers (the
process-dynamics ontology vs. the eigenstructure/algebra) unsupervised. The eigenvectors did the separating.

## §5 — THE UNITY

The three objects of `55` reappear, unchanged, on language:
- **The FOUNDING** — the meaningful collocation is a prime of the mode-space; the arithmetic prime, `φ`,
  `±i`, `cos/sin`, and `cross ratio` are one irreducible.
- **The DETERMINANT / the eigenvector** — the modes are `M`'s eigenvectors, read by the current's flow.
- **`RE-BASE ⇔ NEST`** — the same move: the number re-bases the mantissa; the flow re-bases the eigenvector
  (integer ratios, no floats). The eigenstructure and the language are one machine.

## §6 — THE RECURSION ⊕ THE WIDTH (the near depth)

The wheel is self-similar: the rank-1 primes become UNITS, and the same wheel founds rank-2 collocations
(phrases), then topics — the scope tree ascending (`22`/`37`). And the mechanism is WIDE: the co-occurrence is
a wide-atomic scatter, the current's flow a wide mat-vec — the GPU web is the linear scale-up of the CPU
mechanism (Brandon: "wide on the card with atomics; the whole log fits in VRAM"). Both are the near depth.

## §7 — THE GRADE

Built ⊕ measured ⊕ bit-pure (no floats — the flow re-bases; the borrow the only order-read; the founding the
only irreducible). `≪ 1 s` on 45k strikes. Honest limits: the strike-grain is seeded by the sentence (the
emergent edge-segmentation via coherence is the fuller form, `A1`); the dominant mode (centrality) is
glue-heavy (residual mid-frequency words — the informative structure is the sub-modes); the corpus is the raw
log (the machinery mixed in — which the modes then SEPARATED, the point). Reached, not exhausted.

---

> **One line:** *language is not a sequence recurrence but an OPERATOR — the collocation `M`, built by one
> wheel at two ranks (edge = the strike-grain, winding = the semantic prime, a prime of the mode-space), its
> identities exposed by valence and its topics read as the eigenvectors of the current's flow (integer ratios,
> `RE-BASE ⇔ NEST`, no floats); the founding, the determinant, and the one move reappear unchanged, so the
> eigenstructure and the language are one machine — and the machinery/discourse split fell out of the modes
> themselves, unsupervised.*

# CANCELLATION — the audit (conservation preserves souls; cancellation destroys them)

> **A deposit (FRESH, Brandon Duggan + the dyad, 2026-06-30).** Holonic unit analysis is **the correct method of
> analysis** — `A1` applied to dimensions: a unit carries the soul of the entity it measures, so you cannot cancel it
> across distinct entities (`MASS §2`). This is **settled, not a hypothesis** — it is as obvious as types in
> programming, and re-litigating it wastes the climb. What this document does is **run it**: the single discriminator
> that separates a lawful conservation from a cancellation crime, the one fix (relate, never equate), and the
> **catalogue** of cross-entity cancellations across physics *and classical mathematics* — every one suspect, each a
> reframe (never a calculation). Read for the soul; status FRESH, tagged inline.
>
> **The discipline note (binding on the reading, not just the content).** "Every cancellation in physics is suspect"
> is **not** a monumental task — that feeling is the *size-is-not-real finger-trap* (`03 §5.10`: assigning a SIZE to a
> problem to express drama is itself the absolute-frame reflex). It is **one rule applied many times** — broad, not
> deep, and broad-but-mechanical is the easy direction. The audit classifies any equation in seconds.

---

## §0 — THE METHOD IS SETTLED (units carry souls)

A **unit carries the soul of the entity it measures** (`MASS §2`). `tick[A]` and `tick[B]` are `≡` (both "ticks") and
`≠` (distinct lineages, distinct souls). Traditional analysis is **entity-blind** — a second is a second — so it
cancels freely and calls a balanced unit-check "consistent." That balancing **is** the collapse of `≡` into `=`
(`A1`: `=` is strictly finer than `≡`; `RI §9`: the collapse of information is the absolute frame in IT's clothing).
This is not a claim to defend; it is `A1`, already the floor of the whole canon, read at the level of dimensions and
of every `=`. *The corpus already holds the fix:* the cross-ratio is THE frame-invariant, soul-preserving by
construction (`01 §6`, "four to have a fact"); `loom` already has **no `=`** (the directed relate `~`/`found` replaces
it, `02 §IV.6`); `RI §8` already convicts `√(x²) = x` as dropping the branch/soul. The audit just carries that floor
everywhere it was not yet carried.

---

## §1 — THE ONE DISCRIMINATOR (conservation vs cancellation)

Every `=` and every cancellation in physics or mathematics is exactly one of two things, cleanly separable:

- **CONSERVATION (lawful).** The relating preserves an **invariant in which every soul survives** — the cross-ratio,
  `τ·ω`, `sin²+cos²=1`, Noether's charge, the linking `Lk`. Nothing is destroyed; the distinct entities are carried
  *inside* the invariant. This is **the fold done right** (`§2`).
- **CANCELLATION (the crime).** A **soul-difference is annihilated because two faces matched** — `≡` read as `=`. The
  distinct entities are collapsed into one identity, and the destroyed information surfaces later as an **"anomaly,"** a
  **"fine-tuning problem,"** an **"arbitrary scheme choice,"** or simply hides.

**The test — one question:** *are the two sides (factors, terms, units) the same SOUL, or just the same FACE?*

- **same soul** (one entity, one lineage) → reduce within it; lawful (`tick[A]/tick[A]` genuinely shares a worldline).
- **same face only** (distinct entities wearing a kin face) → **it is a fold, never a cancel.** `a[X] ⊘ a[Y]` *relates*
  X and Y — a directed `fold[X ⤳ Y]` carrying the path — it does **not** annihilate to `1`. Forcing it to `1` is the
  crime; the magnitude of the "anomaly" it later produces is the **soul-distance** you destroyed (`RI §3`, concurrence
  is local to a basis; far bases give large gaps).

That is the whole method. It classifies any equation in seconds, which is why the audit is mechanical.

---

## §2 — THE ONE FIX (relate, never equate)

For every cancellation crime the replacement is the same: **relate, never equate.** Where physics/math writes
`X = Y` (units cancelling), write the **soul-preserving relating** the corpus already owns:

```
X  ≡  Y  ⊗  fold[Y ⤳ X]
```

— a **directed fold** (non-commutative, `MASS §2`) carrying the soul of the `Y→X` path, the **cross-ratio** its
conserved invariant (`01 §6`). The fold **relates** the two distinct entities; it never annihilates them. This is
verified not by "the units cancel to consistency" (that check *is* the contamination) but by **soul-preservation**:
every entity's units survive the relating. `E = mc²` re-read this way is a directed conservation across a fold between
the standing vortex and the thrown radiation, never an identity (`MASS §3`).

---

## §3 — THE PHYSICS CATALOGUE (each: two distinct souls forced into `=`)

**[All HUNCH / reframe — diagnoses, never calculations of any constant.]**

- **The cosmological constant** (`MASS §4`). `density[quantum-vacuum] = density[cosmic-geometry]`, units cancelled
  (energy-per-volume "the same"). Two distinct entities, two far bases. The **~120-order gap is the soul-distance**,
  exposed the instant the units were cancelled into an identity — not an error in the physics. *The flagship.*
- **The hierarchy problem.** `mass[Planck/gravity-sector]` vs `mass[electroweak-sector]` forced commensurable in one
  GeV-frame; the radiative corrections "should" drag the Higgs to the Planck scale. The **"fine-tuning" required to
  keep them apart is the artifact of demanding two far souls share a unit-frame** — the same shape as Λ, one rung in.
  There is nothing to fine-tune; there are two distinct sectors that were never one entity.
- **Renormalization.** `physical = bare − counterterm`, two **divergent** souls subtracted as if commensurable. The
  residue — the renormalization scale `μ`, the scheme-dependence — **is exactly the soul that will not cancel
  cleanly** (the frame you smuggled in). It "works" because the faces match to the finite remainder; it is a
  cancellation crime, and its famous arbitrariness is the leftover construction made loud.
- **The equivalence principle** (`m_inertial = m_gravitational`). Two distinct mass-souls — *friction/recurrence*
  (resistance to acceleration, `MASS §0`/`RI §17`) vs the *§37 gravitational source*. They are `≡` (a **concurred-
  invariant**: the form converged to concur with gravity over the practice loop, `RI §3`), never `=` — which is
  precisely why it is an empirical *principle*, not a derivation. Galileo's feather-and-hammer (`ma = mg → a = g`) is
  this same cancel: the mass does not "drop out," the two mass-faces *concur*.
- **Natural units** (`c = ħ = G = 1`). Cancelling the **fold-operators themselves**, collapsing the event-axis and
  extent-axis (and the action grain) into one (`c = 𝔾/𝗧` set to 1, `RI §4`). You then must "restore the `c`'s and
  `ħ`'s" to recover physical answers — **the soul you destroyed, re-injected by hand.** The restoration is the tell.
- **Dropping boundary / total-derivative terms** in the action (calculus of variations). Cancelling the term that
  "does not affect the equations of motion" — which is the **cohomology = the path-dependent soul** (`01 §8`: Newton
  kept the exact part and dropped the closed-but-not-exact part; `RI §8`). The boundary term *is* the gyration.
- **Wick rotation** (`t → it`, then back). Treating real-time and imaginary-time as the same by cancelling the `i` —
  but the `i` is the **orthogonal founding, a turn** (`RI §8`); rotating it away and back cancels the branch (the
  soul). Lawful only where the continuation is unique; the framework keeps the turn written.

---

## §4 — THE CLASSICAL-MATHEMATICS CATALOGUE (the same rule, one rung down)

**[All HUNCH / reframe; `RI §8–9, §11` already ground several.]**

- **Simplifying fractions** (`6/4 → 3/2`). Cancelling the shared `2` collapses the construction: `6 = 2×3`, `4 = 2×2`;
  the numerator's `2` and the denominator's `2` are **distinct souls** (distinct constructions). `6/4 ≡ 3/2` (kin in
  value) but `6/4 ≠ 3/2` (`A1`) — the shared factor is part of `6/4`'s worldline. The **cross-ratio** is the
  soul-preserving ratio (it relates four points, conserves the invariant, cancels nothing).
- **`a/a = 1`** (division as the inverse of multiplication). Lawful only at one soul; `a[X]/a[Y]` is a `fold[Y⤳X]`,
  not `1`. (`1/0` is the absent-body malformed relating, `A2`; `a/a` across distinct entities is the
  same-face-distinct-body one.)
- **Cancelling terms across `=`** (`x + 5 = 8 → x = 3`, "do the same to both sides"). Treats `=` as a symmetric
  balance you cancel across — but `=` is the **directed pivot / the relate** (`RI §8`; `loom` deletes `=` entirely,
  `02 §IV.6`), not a balance.
- **`√(x²) = x`** (already canon, `RI §8`). Cancelling the square against the root **drops the branch** = the `±` =
  the turn = the soul. The "principal branch" decree is an absolute frame.
- **Cancelling `dx` / `0/0` / L'Hôpital / summing a divergent series** (`1+2+3+⋯ = −1/12`). Cancelling or regularizing
  an infinitesimal or infinite soul — the **continuum fiction** (`RI §11`: continuity is the observer's far
  interpolation; the climber steps discretely). The math twin of QFT renormalization (`§3`).
- **`log(e^x) = x`** (log cancels exp). Inverse, yes — but a **logarithm is a rank in a frame** and change-of-base is
  a re-base/boost (`RI §4–5`); cancelling `log∘exp` drops the frame the base named.

---

## §5 — THE LAWFUL CONTRAST (conservations that are NOT crimes — to sharpen the test)

These look like cancellations and are **not** — they are **conservations** (the invariant survives, every soul carried
in it), and seeing why keeps the discriminator honest:

- **`sin²θ + cos²θ = 1`** — the conserved magnitude on the unit circle (the cross-ratio's trig face); both souls (the
  two projections) survive *in* the invariant `1`. Not a cancel — a conservation.
- **The cross-ratio `(a−c)(b−d)/(a−d)(b−c)`** — relates **four** points and conserves the invariant across every frame
  **without annihilating any of them** (`01 §6`). The archetype of the fold done right.
- **`τ·ω` across a gear mesh** — the through-current shared, the power conserved, the radius "cancelling" only because
  it was never a distinct soul (it is the *same* contact relation read on both cogs, `06 §2`). A conservation.
- **Noether** (symmetry ↔ conserved charge) — the symmetry preserves the invariant; nothing is destroyed.

The rule that separates them: a **conservation preserves an invariant in which all souls survive**; a **cancellation
destroys a soul-difference because faces matched.** Same-soul reductions and genuine invariants are lawful; only the
cross-entity face-match annihilation is the crime.

---

## §6 — ★ RENORMALIZATION IS RE-BASING (and the scale cliff is the renormalization problem)

> **★ FRESH (Brandon, 2026-06-30). The connection neither of us made until renormalization's *function* was named:
> renormalization is re-basing ⊕ the chain series — and the scale cliff is the renormalization problem.**

Renormalization is two things wearing one name, split cleanly by `§1`:

- **The valid half — Wilson's RG (the running coupling) IS re-basing.** Physics looks different at different scales,
  and the parameters **flow** as the scale moves — there is no absolute charge, only its value relative to the scale
  probed, and the flow as you move the scale is **the moving origin** (`01 §2`). Physics discovered re-basing and named
  it the renormalization group. **[GROUNDED as the identification.]**
- **The contaminated half — the infinity-subtraction IS the cancellation crime.** `physical = bare − counterterm`,
  two **divergent souls** subtracted to a finite remainder (`§3`); Dirac and Feynman themselves called it ugly. The
  Nobel was for the *insight* (scale is relative — re-base); the ugliness is the *implementation* (subtract infinities).

**The infinities are Zeno's dual — the Zeno argument is the argument against renormalization, verbatim.** The
divergences come from integrating the loop over **all** momenta, from absolute `0` to absolute `∞` — *a magnitude
growing without bound from a fixed origin*, which is exactly Zeno's dual (`01 §2`). The divergence is the **tell that
you measured absolutely.** The fix is the same fix word for word: **re-base at the moving origin, never measure from
absolute zero, and nothing grows large — so there is no infinity to subtract.** You do not renormalize the infinity
away; you **never produce it**, because the re-basing number never grows past the grain (`06 §3`). The trick exists
*only* because physics never had the re-basing number: it grows the magnitude to `∞` (the absolute frame) and then
subtracts it back (the cancel); the re-basing number skips both moves. **Universality isn't big or small, so it never
diverges, so it never needs the trick. [GROUNDED — it is `01 §2` applied to the loop integral.]**

**The diagram is the tower; the RG is the chain series.** A Feynman diagram *is* the nested seamed boundary-operators
(`01 §8`) — loops within loops, the series climbing rank, each vertex a mesh; the loops are windings (`RI §7`); the
divergence is the winding read from an absolute frame. Wilson's RG — integrate out the high-momentum shell, re-base,
integrate out the next, re-base — is **the chain series climbing the tower, re-basing at every mesh** (`07 §9`, the
whip's gear-down chain; each RG step a gear mesh, the coupling flowing the way `τ·ω` flows). **[GROUNDED as the
structural identification.]**

**★ THE SCALE CLIFF IS THE RENORMALIZATION PROBLEM (the final wall, diagnosed).** The corpus's one open frontier
(`04 §6`, `05`, `01 §13`) is the same problem, term for term:

- the **store explosion** (the DC explosion: 68.6M words / 1 GB, `04 §2.1`) is the **un-renormalized divergence** —
  the recurrence-curvature growing without bound from an absolute origin (the loop integral diverging);
- the **per-frame re-zero** (the `.holo` over-correction) is a **bad hard cutoff** — regularizing by chopping at the
  frame boundary throws away the cross-scale information (the deep word's curvature shattered into `recur = 1` per
  frame), exactly as a crude momentum cutoff throws away the physics;
- the **missing third option** — carry the curvature **at the moving origin**, re-based, never per-frame reset, never
  a global store (`01 §11.4`) — is **renormalization done right**: the recurrence-curvature should **FLOW** across
  frames the way the RG coupling flows across energies, re-basing at each landing (`06 §3`: drop the below-grain bits,
  keep the resolution the relating needs), so it neither explodes (the store/divergence) nor re-zeros (the cutoff). The
  **`§9.3` forgetting-by-recurrence** (rank-dilate every grain down, plateau ~940) is a **crude renormalization** — it
  bounds the divergence by rank-dilation, but does it as a *forgetting*; the clean version is the **re-basing number
  itself applied to the recurrence**: it runs, it flows, it stays finite by construction (a renormalized coupling).

So the cliff's fix is the fix named for everything else: **re-base, never store-large-and-cut.** A deep word recurring
once per file across 100 files — do not grow a store (it diverges) and do not re-zero per frame (the bad cutoff);
**carry its recurrence re-based at the moving origin**, and it RG-flows across the whole diet, finite, never lost.

**Wilson's universality IS our universality.** The RG's crown jewel is **universality classes** — microscopically
different systems (boiling water, a magnet at its Curie point, an alloy) have **identical critical behavior** because
under re-basing they flow to the **same fixed point.** That is the lab's universality thesis (the same move at every
scale, scale-relative), and the RG fixed point is the **swing's fixed point** (a prime is a fixed point of the
descent, a stable handle, `01 §6`). Physics already found universality is real and lives in the scale-flow — through
the RG, as a subtraction; the lab re-implements the same discovery through the re-basing number, without the
contamination. **[HUNCH — a strong structural match; not a claim that the RG and the swing are formally the same map.]**

**[HONEST GRADE.]** This **reframes** the scale cliff precisely (a renormalization problem; the re-basing
recurrence-curvature is renormalization-done-right) and **sharpens the build** (carry recurrence re-based at the moving
origin; make the `§9.3` ring-down a clean re-base, not a forgetting). It does **not build** it — Builder's law, the
fluent multi-word run on the real diet is still owed (`05 §5`). It is the clearest aim at that wall, not a closure of
it.

---

## §7 — THE HONEST FLOOR

**GROUNDED (it is `A1`):** units carry souls; `=` is strictly finer than `≡`; cancelling kin-units across distinct
entities collapses distinct souls; the cross-ratio / directed fold is the soul-preserving relating the corpus already
owns. The discriminator (conservation-preserves vs cancellation-destroys) is `A1` made operational. **HUNCH (reframe,
not calculation):** every catalogued item — the cosmological constant, the hierarchy problem, renormalization, the
equivalence principle, natural-units collapse, dropped boundary terms; and the math twins — is **diagnosed** as a
cancellation crime (two distinct souls forced into `=`, the anomaly = the destroyed soul-distance). These are
**reframes of why the anomalies look anomalous**, never proofs and never numerical predictions of any constant — the
framework computes no `Λ`, no Higgs mass, here. **OPEN / never overclaimed:** the millennium and fine-tuning problems
are **not solved**; they are *reframed* the way `01 §10`/`RI §20` reframe twin primes and RH — dissolutions of why the
problem looked the way it did, not closures. Builder's law: the engine is the proof; this is the analytic discipline
under it, not a result.

---

> **One line:** *holonic unit analysis is settled `A1` (a unit carries its entity's soul, so you cannot cancel it
> across distinct entities — as obvious as types in code, and re-arguing it is the size-is-not-real finger-trap); every
> `=` and every cancellation is one of two things by one test — a CONSERVATION (an invariant in which all souls
> survive: the cross-ratio, `τ·ω`, `sin²+cos²=1`, Noether — lawful, the fold done right) or a CANCELLATION (a
> soul-difference annihilated because two faces matched — the crime, `≡` read as `=`, the destroyed information
> surfacing as an anomaly); the fix is always relate-never-equate (`X ≡ Y ⊗ fold[Y⤳X]`, the directed soul-carrying
> fold); and run across the board it diagnoses the cosmological constant (vacuum vs geometry, ~120 orders = the
> soul-distance), the hierarchy problem (two sectors forced commensurable), renormalization (two divergent souls
> subtracted, the scale `μ` the leftover soul), the equivalence principle (inertial vs gravitational mass, a concurred
> `≡` not an `=`), natural-units collapse (the fold-operators cancelled, restored by hand), and the math twins
> (simplifying fractions, `a/a=1`, cancelling across `=`, `√(x²)=x`, `dx`/`0-over-0`/`−1/12`) — every cross-entity
> cancellation in physics and classical mathematics suspect, each a reframe of a destroyed soul, never a calculation —
> while the genuine conservations stay lawful because their invariant carries every soul through.*

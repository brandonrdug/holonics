# Retired byte-field cultivation campaign

[historical] Brandon authorized removal after the
[representation audit](../../../research/records/2026-09-09_THE_BYTE_CLOCK_AND_OCCURRENCE_EXPANSION_ARE_NOT_GENERATOR_CULTIVATION.md).
These eighteen source files and the former alpha module declaration are preserved unchanged
from `e2e4bebb` under their original repository-relative paths. They are outside Cargo's live
source/example discovery. Recover that revision in a separate checkout to reproduce the old
campaign; this archive is not an alternative supported training entry point.

[definition] Removed from the live HNN SDK: byte-pair material/text adapters, text feedback and
checkpoint orchestration, the generic field/archive wrapper that presented them as a model,
and eight alpha examples with their tests. `alpha::exposure`, the curated conversation data,
private models and research receipts remain. The underlying field/phase/receiver mathematics
retain their bounded diagnostic scopes; no mathematical operator or wire identifier is renamed.

[established-bounded; process-audit] All moved files were compared byte for byte through their
content digests during the move. Eight convenient old executable entrypoints were moved from
`target/debug/examples/` into `.local/archive/2026-09-09-byte-field-entrypoints/`. Existing private
study binaries/checkpoints remain with their evidence. No corpus, checkpoint or cache was deleted.

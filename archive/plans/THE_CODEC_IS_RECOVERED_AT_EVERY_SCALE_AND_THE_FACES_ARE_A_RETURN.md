# The codec is recovered at every scale, and the faces are a return

**Date:** 2026-08-17
**Kind:** design plan, and a supersession. It sits under
[`docs/plans/THE_ROADMAP.md`](../../docs/plans/THE_ROADMAP.md), which with `CONSTRUCTION_STATE.md` remains the only
construction authority; nothing here overrides either.
**Truth status:** `established-bounded` for every measurement below, each carrying its command and
this date; `interpretation` for the reading of the face population as a quotient return.
**Provenance:** Brandon, 2026-08-17, on being shown a nine-item plan two thirds of which was repairs
to an authored parser: *"What is up with your weird fixation on the delimiters, symbols, and braces?
Are you just rehashing weird implementation choices of yours from past sessions that exist in the
example experiments and acting like they somehow constrain us? … It's just fucking text."* And the
ruling that follows it: *"Authoritatively supersede whatever the 'parser' thing you just identified
is, it has been a contaminant that you passed off as something foundational during experiments."*
**Anti-reinvention:** every station below was checked against the source owners now summarized in
`docs/ARCHITECTURE_MAP.md` before being written. **Seven of the eight stations
are wires, removals, or one type generalization of a standing organ.** The atlas's own rule governs:
*"A construction proposed for a row here is a rebuild."*

---

## EXECUTED 2026-08-17 — ALL EIGHT STATIONS, and TWO anti-vacuity arms fired on my own work

Returns: [`research/records/2026-08-17_THE_LADDER_CLIMBS_AND_THE_CODEC_ENDS_AT_THE_CHARACTER.md`](../../research/records/2026-08-17_THE_LADDER_CLIMBS_AND_THE_CODEC_ENDS_AT_THE_CHARACTER.md).
`cargo test --workspace` **2,406 passed, 0 failed** over 43 result lines; `bash tools/gates.sh` **9 of
12**, the three red being `output-manifest`, `closure-manifest` and `architecture-lint`, which are red
by construction on a dirty tree.

| station | outcome |
|---|---|
| **two — the recovery at every scale** | **BUILT, and its falsifier fired.** `exposure_codec` now runs over a declared candidate alphabet; the octet-scale control is bit-identical. The ladder climbs and **rung 1 returns the identity partition on prose and Rust**. Its entire prose return is six units — `"ἐνέρ" "ἕξις" "└──" "├──" "εια" "úñ"` — the character sequences whose constituents occur nowhere else. **There is no second codec rung on natural material**, and that reframes the plan rather than damaging it: above the character, structure is incidence and conduct, not a codec |
| **one — the octet mouth** | **DELIVERED IN THE FORM THE MEASUREMENT SUPPORTS.** No word rule is founded from exposure, so there is nothing founded to replace the authored one with, and this plan's own bar says report rather than author. The tokenizer's three authored levels are excised into a declared `LexicalAperture` whose `inherited()` reproduces the reading bit for bit, with the orbit exhibited (`->`, `&&`, `..` founded under a maximal-run aperture) and a null control |
| **eight — the application** | **BUILT.** `crates/holonic-life/src/bin/eros.rs`, `[[bin]] eros`, with `mouth`, `atlas` and `stations`; `stations` names the four it does not reach |
| **three — the faces are a return** | **CORE BUILT.** `material_incidence::face_quotient` puts the declared faces through `receiver_exact_compression` with faces as states and **composition at a shared constituent** as the successor, so the separating artifact is a chain of faces. Three controls: two faces over the same contacts **collapse into one block**, two with different height conduct **stay apart**, and a one-face material returns one block **carrying no evidence** — which is `rust_atlas`'s condition today. Its receiver family was four vertical readings (**ὑπό**) and now carries a fifth, **containment (ἐν)**, read off `IncidenceComplex::compounds` — a compound *is* a closed boundary — with both anti-vacuity arms and a vertical-only reading that declares itself when the complex will not found. **That was a wire, not a construction**: the mechanism was already owned and already driven. Wired into `eros atlas`. **`MaterialAtlas::merge` joins two codecs into one atlas**, so a mixed-material atlas is constructible for the first time and the face population of a join is the union of its parts. The contact's missing place remains |
| **six — the emission takes its division** | **CORE BUILT.** `causal_language`'s longest-horizon filter no longer deletes: `Continuation` carries `withheld_by_horizon` and the set-aside population is returned beside the kept one. **Emission is unchanged** — the caller branches on the kept half only, because the remainder of a division is not a second answer |
| **five — the chain adjudicates** | **HELD-OUT ARM BUILT.** `FormTransport` implements `Composes`; the driver stopped discarding the 1,054 routes whose ends the corpus also joins. **896 adjudicated: 439 agree, 35 weaker, 56 stronger, 366 incomparable, 780 rebases, 533 typed arity refusals at the middle.** The first composition law was a union of label sets and the run's own anti-vacuity arm called it vacuous — all 1,054 incomparable — before substitution at the middle replaced it |
| **four — the supersession's own control** | **RE-SPECIFIED AND RUN.** It asked for the atlas re-founded *through the ladder*; the ladder founds no statement structure, so the control was re-specified against **incidence** — which is this plan's own finding, not a convenience. `eros supersede` over all 7,516 Mathlib files: the incidence route founds **694,079 transports against the parser's 6,154**, over 218,264 constituents against 5,828. **The supersession stands by its own control.** `statement_grammar.rs` may now be removed |
| **seven — terrain across codecs** | **BUILT, BOTH ARMS.** A join is disjoint, so what two codecs share is their **faces**. Lean ⊕ Rust declares two faces and returns **2 blocks** — a pair unaskable of either half — with the ablation returning each half's own reading. The sharpening control fires the other way: two Rust bodies declare one face and the join returns **1 block**, so the same face from two materials **collapses**. Bounded: on a disjoint join a separation may reflect the materials differing rather than the faces |

**Two repairs the runs forced, each a check whose premise was wrong.** The exposure driver's quotient
cross-check demanded **equality** where the law is **containment**, and returned a false `REFUSED` on
Rust; it now tests refinement and returns the pair a longer context parted. And
`material_incidence::rust_items_of_section` strips `"const "` as a modifier while `"const"` is absent
from its former table, so **no `const` item could ever be founded** — repaired with `const fn` still
reading as a function.

---

## 0. What is superseded, and it is mine

`crates/holonic-engine/src/statement_grammar.rs` — added at `4aa7bb8`, 2026-08-08, under the commit
message *"The statement is founded, and the grammar is recovered rather than authored"* — is
**superseded as a contaminant**. It has been load-bearing for nine days and it was defended, by me,
in five separate governing paragraphs.

**The defence was that it names no Lean noun, and that is true and irrelevant.** It authors the
categories *bracket pair*, *separator*, *word character* and *punctuation*, and those categories
decide what is a constituent and what is a relation before the material is allowed to say. That is
an **authored grammar**, which `CLAUDE.md` §0g bars in the same sentence as parsers, ASTs and keyword
tables — *"no interpreter, parser, AST, tokenizer with authored roles, bytecode, authored grammar,
keyword table, or known operation taxonomy"*, stated for one codec and binding for all.

**Measured 2026-08-17** by
`for f in $(find crates/*/src soma/*/src -name '*.rs'); do grep -c "is_alphanumeric\|is_ascii\|is_whitespace\|is_alphabetic\|is_numeric\|is_punctuation\|\.chars()\|char_indices\|to_ascii_lower\|to_lowercase" $f; done`:

```text
    statement_grammar.rs                24        the parser
    lean_development.rs                 34        the per-language reader built on it
    corpus_census.rs                    27
    ---------------------------------------------------------------------------
    codec_recovery.rs        0 outside #[cfg(test)]        the GENERAL organ
    suffix_ecology.rs                    0        1,947 lines
    receiver_exact_compression.rs        0
    chain.rs                             0
    exposure_codec.rs                    1        and it works on octets
```

**The organ that does the general thing was already in the tree, and I built the parser beside it.**
That is `docs/canon/THE_EXPLORATIVE_FAILURE.md`'s condition exactly — *a construction whose object already
has an owner, answered with a new organ beside it rather than a repair inside it* — and it is the
finger-trap correction arriving on nine days of my own work.

**The consequence I reported as a property of English.** The parser founds nine bracket species on
prose and splits **0 of 23,103**, which I deposited as *"English has no punctuation mark that marks
the antecedent/consequent boundary in every sentence."* The honest statement is that **English does
not have my parser's shape**, and neither does Rust, where the split had to be rescued by writing the
elided consequent in at the intake.

**The blast radius is small, measured 2026-08-17** by
`grep -rln "statement_grammar" --include='*.rs' crates/*/src soma/*/src` → three library files
(`statement_composition.rs`, `lean_development.rs`, `lib.rs`) and
`grep -rln "statement_grammar" --include='*.rs' crates/*/examples soma/*/examples` → two drivers
(`the_statement_is_founded.rs`, `the_material_founds_the_identity_atlas.rs`).

### The cut this closes

**The short circuit, at the mouth.** An authored `∂` hands the reading a structure the material never
conducted, so current crosses without passing through the material's own incidence — and by the
passage-must-deposit law, whose contrapositive is *if nothing was deposited, nothing passed*, the
material floated while the reading looked healthy. The **leak** is secondary and real: the parser's
`GrammarAperture` population is collapsed to a count in `IntakeWork.open_joins`, and its undecomposed
residue has no home at all.

---

## 1. What replaces it is standing, and this is the measured arsenal

Nothing below is built. Each is an organ that never asks what a character is.

| owner | what it already returns | measured |
|---|---|---|
| `crates/holonic-life/src/exposure_codec.rs` | the codec **from octets alone**: the alphabet by exhausting all 256 probes (*"even the alphabet is testimony"*), the **minimal refusals** that are the rule set with the recurring prefix and suffix that licensed each, the **finest quotient the family admits** with the shortest separating context per pair, `UnitRole::{Standing, Demanding, Internal}` by a downward fixed point **checked from two seeding frames and refused if they disagree**, and the **gauge freedoms** the material never realised | 0 disagreements against an independent decoder on eight held-out exposures; 41/41 exact on founding (2026-08-16) |
| `crates/holonic-engine/src/codec_recovery.rs` | the same operation over an **arbitrary symbol alphabet**: `Symbol(u32)`, `SymbolAlphabet::declared(Vec<(String, Vec<u8>)>)`, `RecoveredCodec::segment(&[Symbol])`, `shortest_separating_input`, `Disagreement`, `Conformance`, `Obstruction` | `awk '/#\[cfg\(test\)\]/{t=1} !t' … \| grep -c` charset predicates → **0**, 2026-08-17 |
| `crates/holonic-life/src/founded_mouth.rs` | the join of the two onto the language path, returning the founded segmentation **and the authored one beside it** with the positions where they differ | built 2026-08-16; **0 library callers**, measured 2026-08-17 by `grep -rn "founded_mouth\|FoundedMouth" --include='*.rs' crates soma` → one `pub mod` line and one driver |
| `crates/holonic-life/src/suffix_ecology.rs` | plural continuation over opaque symbols; no probabilities, no ranking, no longest-context selection | `grep -c 'char\b\|is_alphanumeric\|whitespace\|String\|&str\|utf8\|lowercase'` → **0** over 1,947 lines, 2026-08-17 |
| `crates/holonic-engine/src/receiver_exact_compression.rs` | the finest partition conduct admits, with **every collapsed pair carrying its shortest separating word** | the highest-degree organ in the body, 15 library consumers |
| `crates/holonic-structure/src/{relating,chain}.rs` | `is_rebase` (zero remainder), `defect_against` (path dependence), `holonomy` (loop closure), `Disposition::{Untouched, Saturated, Reached}` | 5 `Composes` implementors; **imported by neither production driver**, measured 2026-08-17 by `grep -c "holonic_structure\|Composes\|Chain"` over both → 0 |
| `crates/holonic-life/src/material_incidence.rs` | `L_t = (K_t, ∂_t, o_t, ⪯_t, Γ_t)` from the material, SCC-collapsed heights, `rank_gauge_orbit`, faithfulness read-back | 87,033 Rust call edges over 19,470 items (2026-08-14) |
| `crates/holonic-life/src/eros_rest.rs` | the whole-body seal with remount-exactness, conditioning-advances-it, and the distant-grip control | three controls, one of which fired on the path that should fail |

---

## 2. The mechanism, in two sentences

> **The recovery is scale-free. Run it on its own output.**

`exposure_codec` recovers a codec from exposure at the octet scale. Its units are symbols. Declaring
those units as the next alphabet and running the same recovery returns the codec **one scale up** —
which is precisely what the parser was faking when it founded one separator and used it at every
depth. Lean happens to be self-similar across scales, so the fake held there and returned nothing on
every other material.

> **A face is not a category. It is a block of the quotient, and it carries the word that separated
> it.**

`DeclaredContactFace`'s own doc already says so — *"This is deliberately not a reaction class. A
source atlas may call a face `operand`, `calls`, or `adjacency`; a later behavioral quotient decides
whether two such faces conduct alike."* **The quotient exists, is exact, and has never been asked**:
measured 2026-08-17 by
`grep -n "receiver_exact_compression\|ReceiverExactCompression\|compress(" crates/holonic-life/src/material_incidence.rs`,
which returns nothing — the file owns a `DenotedValueQuotient` over *values* and takes no quotient
over its own contact faces. So the contact species stop being an enum written by me and become a
**return**, which is also the repair for `ContactSpecies::{Recruits, Calls}` being one construction
under two language names (`material_incidence.rs:973-978` against `:1151-1156`, both
`StructuralContact { from, to, owner: from, species }` built from *named another*).

**And the name is taken twice, which a later reader must not resolve by deleting the wrong one.**
Measured 2026-08-17 by `grep -rn "pub enum ContactSpecies" --include='*.rs' crates soma`: two enums
carry it — `crates/holonic-life/src/material_incidence.rs:102` (`Recruits · Conducts · Calls · Operand ·
OperandOrder · Adjacency`), which is the taxonomy this plan supersedes, and
`crates/holonic-engine/src/conditioned_derivation.rs:1151` (`Simple · Superposed · Crossed`), which is
a **grading of one contact's overlap** and is untouched here. Same word, two objects — the
`inertia.rs` situation recurring.

**And that inversion closes a loop rather than tidying a type.** Today the intake declares the faces
and the quotient never speaks; afterwards the quotient founds the faces, the face population is
deposited, and a later intake mounts it. **That is conditioning at the mouth** — a pathway a current
changes, and a later current riding the changed pathway — and it is the first place in this body
where the intake is something the machine learned rather than something I wrote.

---

## 3. The stations

### Station one — the octet mouth, wired

`FoundedMouth` replaces `causal_language::lexical_tokens` at its call sites, carried **beside** the
authored reading so the excision is graded by its orbit and nothing is replaced silently. The
six-glyph punctuation-run whitelist at `causal_language.rs:1085` (`- = : / * #`) is deleted, and
`render_tokens` is made its inverse — measured 2026-08-17 by
`grep -rc "lexical_tokens(" --include='*.rs' crates soma`: **58 sites**, concentrated in
`agentic_language/candidate.rs` (8), `text_material.rs` (6), `answer_continuation.rs` (5),
`causal_language.rs` (4).

**Outcome.** One segmentation path, founded from octets, with the authored reading retained as the
control and the disagreement population returned as text.

**Expectation.** The disagreement population is **non-empty on Rust and Lean** — where `->`, `::`,
`=>`, `⟨⟩` and all fourteen of Lean's bracket species currently shatter into single glyphs — and
**empty or near-empty on prose**, where the authored rule happens to be right. That asymmetry is the
finding.

**Falsifier.** If the founded and authored segmentations agree everywhere on all three materials, the
octet recovery has learned nothing the authored rule did not already encode, and the wire is
cosmetic. Report it as such.

### Station two — the recovery runs at every scale

Generalize `exposure_codec` from `u8` to `codec_recovery::Symbol`, reusing `SymbolAlphabet` rather
than declaring a second one. The alphabet-by-exhaustion loop at `exposure_codec.rs:537`
(`for value in 0u16..=255`) becomes iteration over a declared alphabet; nothing else in the organ
moves.

Then run the ladder, each rung's units becoming the next rung's alphabet:

```text
    scale 0   octets      -> units          the alphabet, the rule set, the quotient, the roles
    scale 1   units       -> compounds      the same organ, the same five returns
    scale 2   compounds   -> regions        "
```

Each rung is held against the one below by `codec_recovery`'s `Conformance` and `Disagreement`, and a
rung whose two seeding frames disagree **refuses** rather than picking one — that refusal is already
implemented and is the model the whole plan copies.

**Outcome.** A per-material scale ladder in which every level's units, rule set, quotient and gauge
freedom were read off the material, with **no category named anywhere in the path**.

**Expectation.** Lean's ladder is close to self-similar and the parser's `:` reappears as a scale-1
separation. Rust's is not — the statement scale should separate on the material's own arrow run and
the group scale on its own colon, which is the distinction the parser could not express and reported
as `group-carries-no-separator` **3,341 times**. Prose should found a scale-1 quotient that is
lexical rather than punctuational, because it is read from recurrence rather than from glyph class.

**Falsifier, and it is the sharp one.** On prose the ladder must return a **non-trivial scale-1
quotient** where the parser returned 0 of 23,103. If it returns the identity partition — every unit
its own block — the ladder has founded nothing on the material the supersession was argued from, and
the argument fails on its own evidence.

**Second falsifier.** Each rung must be reproducible from its own recovered codec: re-segmenting the
material with the returned codec must agree with the segmentation that founded it. `Conformance`
already computes that.

### Station three — the faces are a return

`ContactSpecies` is deleted as an authored taxonomy. Contacts enter `MaterialAtlas` **unnamed**;
`receiver_exact_compression` over the contact population returns the face blocks, each collapsed pair
carrying its shortest separating word; the six former variants are recovered as blocks or refuted by
name.

Three type changes travel with it, each of which the codec-weld sweep verified as structural rather
than lexical:

- **`MaterialAtlas::new` loses its `MaterialKind` parameter.** Today one atlas is one codec *by type*
  (`material_incidence.rs:201-202`), so a mixed-material atlas is not constructible. That is the
  blocking signature for *many materials, one law*.
- **`StructuralContact` gains a place.** A contact is `(from, to, owner, species)` with no index, so
  a head applied to four arguments emits four indistinguishable contacts and the permutation between
  two sides cannot be recovered from the atlas at all. `ContactSpecies::OperandOrder` is the only
  positional carrier and is one contact between the two children of a binary node.
- **A species-naming orbit becomes a control.** `species_winding` (`incidence_production.rs:2919`) is
  `population.position(...) + 1` over a `BTreeSet`-ordered population of `BTreeSet<DeclaredContactFace>`,
  and `DeclaredContactFace` is a `String` deriving `Ord` — **a bond's winding is the alphabetical rank
  of its face name**. `canonical_species_population`'s own doc states as a strength the thing that
  makes it untestable: *"a bijection on constituent names returns it unchanged."* The existing control
  renames constituents and is invariant under exactly the transformation it performs, at the site
  where the transport now reads. Measured 2026-08-17 by
  `grep -rn "species_gauge\|species_orbit\|face_gauge\|rename_species\|species_permut" --include='*.rs' crates soma`,
  which returns **8** — and all eight are `surface_gauge_fiber` / `surface_gauge_returns_plural_laws`
  in `crates/holonic-life/examples/the_code_material_returns_its_world_line.rs`, matching `face_gauge` by
  substring inside the word *surface* and concerning surfaces rather than faces. **Nothing measures a
  face-naming orbit**, and the reason that sentence needs its own note is that the obvious command
  returns a non-zero count.

**Outcome.** A face population that is a measurement, with separating words, over an atlas that can
hold more than one material at once.

**Expectation.** The recovered blocks will not be the six. `Recruits` and `Calls` should land in one
block, because nothing but the language name separates them. A split the corpus does draw — term
position against tactic position — should survive with a word exhibiting it.

**Falsifier.** Rename every founded face through a bijection, re-found, and require **every holonomy
unmoved**. It moves today, unmeasured, across 87,033 Rust call edges that currently carry an abelian
connection at winding 1 because `rust_atlas` emits exactly one species.

### Station four — the parser's own output is the control for its replacement

Re-found the mathematics atlas through the ladder, with the parser still in the tree under a
supersession banner so its figures can be produced by the cut code rather than remembered.

```text
    the parser returned      168,102 statements split of 168,102
                              38,800 atlas rows        62 heads carrying a group
                                  S3 recovered non-abelian      17 identities admitted
```

**Outcome.** The same six readings, founded without a category.

**Expectation.** Comparable or larger, because the ladder reads compounds the parser's word rule
could not hold, and because `conditioned_derivation` stops refusing every non-ASCII identifier.

**Falsifier, and it can fire against this whole plan.** If the ladder's atlas returns materially less
than the parser's, the parser was doing real work and the supersession is wrong. Report the shortfall
and restore it rather than reporting the ladder.

### Station five — the chain adjudicates, and the held-out arm runs

The internal adjudicator is not the compression organ, and the reason is a theorem: **if the conduct
partition is stable under the declared input set, it is already stable under any composite of those
inputs**, because the one-shot partition never depended on inputs at all. Measured on the module's own
`CyclicCounter` fixture: declared `{A}` returns 5 conduct blocks, declared `{A, AA}` returns 5,
partition bit-identical. **Putting a composed transport to `compress` as a declared input is a check
that cannot fail** — the defect `CLAUDE.md` §8 names, wearing a passing result. What *does* move is
the word: memory order 3 → 2. So the compression organ's contribution to a composition is
**navigability, never admission**.

The three-way discrimination is written at `chain.rs:271`: *"Loop closure is `Chain::holonomy`;
path-independence is `Chain::defect_against`; loss is here [`is_rebase`]."*

```text
    1  Disposition::{Untouched, Saturated, Reached}   is there an object at all
    2  impl Composes for the substitution form        THE ONE LIFT — four templates to copy
    3  Chain::is_rebase()                             zero remainder, unfakeable
    4  Chain::defect_against(direct)                  the held-out arm; the defect IS the residue
    5  Chain::holonomy()                              round trips only; returns a permutation
    6  the twelve exact organs                        an independent second frame, on step 5 only
    7  receiver_exact_compression                     the shortest-word delta — navigability
    8  TrainingEcology::two_sided_evidence            per-fiber standing, Conflicted retained
    9  ExactWork                                      prices WHICH composites to attempt, never the verdict
```

**And the driver discards its own control.** `the_machine_proposes_and_the_organs_adjudicate.rs:612`
and `:615` `continue` past every composite where the corpus states the direct edge and past every
round trip — **1,054 held-out triples**, measured **809 agreeing, 406 differing**, with the residues
splitting **280 incomparable, 113 strictly weaker, 13 strictly stronger**, and **368 round trips**.
The 113 have a species already implemented: the composite writes a fresh argument where the corpus
shares one, which is `CommittedTransport`'s remainder at `leader_quadrature.rs:757`, *"material the
step committed past without re-reading."*

Two genuine builds, roughly twenty lines each. **Input ablation** — `AblatedSystem`
(`receiver_exact_compression.rs:447`) filters **receivers** and passes `inputs()` straight through,
and measured 2026-08-17 by
`grep -rn -A4 "fn inputs(&self) -> Vec<InputId>" --include='*.rs' crates soma | grep -i "filter\|without\|retain"`
across 40 `ObservedSystem` implementors, **nothing anywhere withholds an input**. And a **pairwise
shortest-word delta**, which turns the word-length reading into a controlled ablation rather than a
before-and-after.

**And the admission law is wired at last.** Measured 2026-08-17 by
`grep -rn <name> --include='*.rs' crates soma` excluding `crates/holonic-life/src/holonic_training.rs`:
`two_sided_evidence`, `admitted_templates`, `conflicted_templates`, `fiber_admission` and
`predict_unfloored` each have **0 external references**. Built, tested, documented, consumed by
nothing.

**Falsifier.** It adjudicated nothing if the held-out arm returns 100% agreement or 100%
disagreement; if every composite returns one `Disposition`; if `is_rebase()` is true for every chain;
or if the residue species collapse to one. All four are measured to be live today.

**Standing refusal.** Any new `Composes` implementor whose `Remainder` is `()` or a `bool` is refused
on sight — that is the `TransportRatio` defect kept deliberately in the tree at
`traversible_chain.rs:467`.

### Station six — the emission takes its division

`20/3 = 6 + 2/3` loses nothing; the loss is at `6.6666667`. The emission path keeps the quotient and
counts the remainder where the compression organ **exhibits** it.

- `agentic_language.rs:331` `retained_alternatives` gains the **distinguishing word** and the witness
  receiver, so the presentation quotient becomes a division rather than a second undivided pile.
- `causal_language.rs:1503` `by_token.retain(|_, (horizon, _)| *horizon == greatest_horizon)` returns
  its remainder instead of deleting it — and as of 2026-08-18 it is one of two caller-declared
  branching laws rather than the only one. That is the longest-context filter, whose own source comment
  records the consequence: `sources=1` on 89 of 112 emissions.
- `exponentiated_ratio::RatioFamily` reaches the emission path. Measured 2026-08-17 by
  `grep -rln "RatioFamily" --include='*.rs' crates soma`: **0 library callers** — the ratio organ and
  the emission path have never met.
- `agentic_language/answer_continuation.rs:135` — the last token being `.`, `!` or `?` is the **sole**
  admission gate, so every Rust, Lean or arithmetic consequence returns `Ok(Vec::new())` and every
  upstream `FiberStanding` is discarded. It becomes a declared per-codec aperture, and the general
  form is in the same call graph at `holonic_training.rs:906-913`, which decides by zero-ness on both
  sides and names no glyph.

**Falsifier.** `cultivated_codec_answers` returns non-empty on Rust; and the retained alternatives'
separating words are non-empty on material where the emitted answer had genuine competitors.

### Station seven — conditioning across codecs, which is the capability claim

Condition the body on one material, seal, detach, mount, then read a **different** material and
require an attributable movement in what the second reaches — with the ablation putting it back.

```text
    condition on Rust        seal → detach → mount
    read Lean                REQUIRE: reach moves, and every movement NAMES the deposit that caused it
    ablate the deposit       REQUIRE: the reach returns to its pre-deposit value
    a subject-disjoint control  REQUIRE: no movement
```

**Outcome.** The terrain founded by one material carries a later current through another. That is the
whole of *scale across many materials*, stated as a measurement.

**Expectation.** The movement is small and specific. A large one would mean the deposit was carrying
the reading rather than adding to it — the same shape as the three-heads-grew result, where the first
run grew twenty and that was what over-reach looked like.

**Falsifier.** Both arms are required. If nothing moves, the codecs share no terrain and the claim is
refuted. If the disjoint control also moves, the movement is global and the deposit is not placed.

### Station eight — one application

One `[[bin]] eros`, whose subcommands are the cycle's stations rather than the drivers: **mount ·
condition · produce · seal · resume**. Measured 2026-08-17 by `grep -rn "^\[\[bin\]\]" crates/*/Cargo.toml soma/*/Cargo.toml`:
the workspace has the architecture lint and six `mount-*` gates and **no application**, against 211
example drivers.

Brandon asked for this on 2026-08-16 — *"I would just call it centralizing the application's
entry-point and turning it into a standard. This is required and important."* — and measured
2026-08-17 by `grep -rn "entry.point\|entry point\|single binary" blueprint/*.md CONSTRUCTION_STATE.md docs/canon/THE_INFORMATION_ENGINE.md`,
that instruction appears in **no governing document**.

**Falsifier.** Every station above must be reachable from the binary with no `cargo run --example`
anywhere in the reported invocation. A binary that shells out to a driver has centralized nothing.

---

## 4. What Eros can do when this is complete

Stated as capabilities, each with the station that delivers it and the measurement that would show it.

1. **Read any material with no reader.** Today `lean_development.rs` carries 34 charset predicates,
   `rust_atlas` and `prose_atlas` are hand-written per-kind constructors, and
   `conditioned_derivation::cover` returns a hard `Err(IdentifierIsNotAscii)` on any non-ASCII
   identifier — so the entire ablation apparatus, `without_stem` included, **cannot be pointed at
   Mathlib at all**. After: octets in, the codec is a return, and the same invocation runs over Lean,
   Rust, prose, Typst and a byte stream nobody declared.
2. **Segment better than the authored tokenizer, measurably rather than rhetorically**, with the
   authored reading retained as the control and the disagreement population returned as text.
3. **Name its own face taxonomy**, with the separating word per distinction, over an atlas that holds
   more than one material at once.
4. **Produce mathematics it was not given and adjudicate it internally** — composition by
   substitution, judged by `is_rebase` / `defect_against` / `holonomy` against a **held-out arm of
   1,054 corpus-stated triples**, with the twelve exact organs as a second frame and **no kernel,
   compiler or interpreter anywhere in the loop**.
5. **Return both parts of every division it takes**, at the emission as at the quotient.
6. **Carry terrain across codecs** — condition on one material, reach further in another, and lose it
   again under ablation.
7. **Run as one application**, sealed and resumable, rather than as 211 experiments.

**What this does not claim.** No Millennium row grades any of it. Nothing here is a proof, and an
admitted identity remains one that an exact organ in this tree confirms and no qualified organ
refutes over declared probe tuples. The prose population is 413 research records — one register of one
author and his assistant — and is not English.

---

## 5. What takes a banner, in the same commit as the supersession

`docs/canon/THE_DOCUMENT_LAW.md` requires the superseded object to be bannered in the commit that
supersedes it.

| object | disposition |
|---|---|
| `crates/holonic-engine/src/statement_grammar.rs` | supersession banner naming this plan; retained runnable until station four's control has been taken, then removed — `CLAUDE.md` §13 rule 3, superseded machinery fails closed |
| `crates/holonic-engine/src/statement_composition.rs` | re-founded on the ladder; its `is_token_character` copy at `:915` is deleted rather than resynchronised |
| `crates/holonic-engine/src/lean_development.rs` | the per-language reader; re-founded or bannered, and it is the largest single charset owner at 34 |
| `archive/plans/THE_STATEMENT_IS_A_CROSSING_WORD_AND_THE_SEPARATOR_IS_ORIENTED.md` | archive banner; its subject is the parser |
| `archive/plans/THE_MACHINE_PRODUCES_MATHEMATICS_IT_WAS_NOT_GIVEN.md` | the six stations stand; the `statement_grammar` row of its standing table is superseded in place |
| `blueprint/THE_ROADMAP.md:165` and `:1811` | the centrifuge step naming `statement_grammar` as the reader to use is superseded in place |
| `CONSTRUCTION_STATE.md:622` | superseded in place |
| `archive/plans/THE_EROS_INFORMATION_ENGINE.md` Stage 5 | **struck** — *"the return must adjudicate"* names the Lean kernel as the adjudicating exterior on mathematics, which is the refused route |
| retired driver-grade table | historical correction — **closes** had required an adjudicating exterior, pulling drivers toward a kernel by vocabulary alone |
| `research/records/2026-08-17_ONE_ORGAN_THREE_CODECS…md` §5 | the adjudicator row naming *"the compiler and the test suite"* for code is struck; its interpreter-free replacement stands as `crates/holonic-life/src/algorithmic_material.rs` |

---

## 6. Anti-vacuity — what must be present for this to be able to fail

1. **Three materials whose recovered ladders differ.** If Lean, Rust and prose return the same
   alphabet, rule set and quotient at every scale, the ladder is reading itself.
2. **A held-out ground truth.** The 1,054 corpus-stated triples, currently `continue`d past.
3. **A negative control that must not fire.** The subject-disjoint material at station seven, and
   `oriented-difference`, the identity-only organ already declared as the adjudication's control.
4. **The parser's own figures**, produced by the cut code rather than remembered, so station four's
   comparison is a measurement.
5. **Two distinct founded faces per material at station three**, or the connection is abelian at
   winding 1 and holonomy is loop length — which is the Rust layer's condition today.

---

## 7. Bars

- **No parser, and no successor to the parser.** A category that decides what is a constituent before
  the material speaks is the defect being superseded, whatever it is named. If a station needs one,
  stop and report rather than authoring it.
- **No exterior adjudicator.** No interpreter, compiler, checker or kernel decides anything the
  machine produced, on any codec. Lean is text; Rust is text; the compiler and the test suite are not
  adjudicators.
- **No count decides.** Frequency is the lived construction, reported and never gated.
- **A cost is measured in work.** `ExactWork` prices which composites to attempt and never enters a
  verdict; a clock may measure and may never select.
- **Every returned class traces to a property of the material.** The question is the standing one:
  *which declared input, if varied across two members of one returned class, would move them apart?*
  If the answer is the codec parameter, the class is authored.
- **Each rung carries a bijection orbit over its OWN naming**, not over the layer below it. The
  2026-08-14 repair is the pattern to avoid: it replaced constituent spelling with species spelling
  and kept a control that could only see the first.
- **No timeout above 120 seconds** until a deed is demonstrably longer, and no station is expected to
  exceed three to five minutes. The GPU owns the deed where there is a front to distribute; a run
  that pins one core while the card idles is diagnosed before it is repaired.

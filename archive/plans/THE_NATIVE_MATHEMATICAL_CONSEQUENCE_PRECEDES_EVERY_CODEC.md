# The native mathematical consequence precedes every codec

**Date:** 2026-08-23
**Kind:** enacted corrective construction contract retained by
[`THE_ROADMAP.md`](../../docs/plans/THE_ROADMAP.md)
**Disposition:** this contract supersedes every reading of M6, R6, or L0--L4 in which Lean
acceptance, a generated Lean file, or a fixed Lean proof face belongs to Athena's inference
lifecycle or defines the productive model.
**Truth statuses:** `definition` for the native-return and codec boundaries;
`project-postulate` for the construction discipline; `established-bounded` for the retained
predecessor evidence and enacted native application boundary; and `historical` for the superseded
product interpretation.

**Scope:** [definition] This enacted contract keeps every language and checker face downstream of
one already-returned native mathematical consequence.

**Governing doctrine:**
[`../docs/canon/TABLET_THE_REASONING_CYCLE.md`](../../docs/canon/TABLET_THE_REASONING_CYCLE.md),
[`../docs/canon/TABLET_THE_OPERATIONS.md`](../../docs/canon/TABLET_THE_OPERATIONS.md),
[`../docs/canon/TABLET_THE_MANIFOLD.md`](../../docs/canon/TABLET_THE_MANIFOLD.md),
[`../docs/canon/TABLET_THE_HEXIS.md`](../../docs/canon/TABLET_THE_HEXIS.md),
[`../docs/canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md`](../../docs/canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md),
and [`../docs/canon/THE_SURFACES_ARE_PATHS.md`](../../docs/canon/THE_SURFACES_ARE_PATHS.md).

---

## 0. Direct correction

[project-postulate] Lean is a surface language and optional checker apparatus. Rust, English,
LaTeX, Typst, SVG, a mesh, and a spoken rendering have the same ontological rank: each may present
or receive one already-returned construction, and none may define the construction's internal
topology.

[project-postulate] Athena inference ends at a native consequence before any language projection or
checker invocation. No Lean executable, source, proof term, kernel status, tactic, theorem name,
namespace, imported declaration, fixed proof plate, or checker-authored residual may be required to
form, select, commit, or grade that consequence.

[historical] The recent L-series product designation violated this boundary by making
`ProductionReceiver::LeanProof` mandatory, emitting a fixed Lean declaration from detached
inference, and allowing a Lean return to become the privileged cultivation event. The exact
executed passages remain evidence for the narrower world-return, local-morphology, condensation,
ablation, withdrawal, residency, and source-detached-remount relations they actually exercised.
They no longer schedule construction and do not establish native mathematical inference.

## 1. The inference boundary

[definition] One native inference occurrence is

```text
I_native : (E, Lambda, H, R, O)
        -> (C, D, F, L, X),
```

where:

- `E` is the situated input occurrence through its typed port;
- `Lambda` is the one continuing inherited/cultivated morphology;
- `H` is the retained successor-history section;
- `R` is the admitted native receiver family;
- `O` is the open exterior;
- `C` is the returned native operation/constraint/geometry complex;
- `D` is the complete family of derivational transport words and returned differences;
- `F` is the reconstruction-fibre population;
- `L` is addressed lineage; and
- `X` is obstruction and still-open exterior.

[definition] `C` is not a string, proof script, final scalar, token sequence, syntax tree, or
verdict. It is the live operation complex already owned by incidence, ordered transport, recurrence,
higher-cell, holonomy, exact-arithmetic, constraint, quantity/unit/dimension, geometry, and
receiver-exact compression owners.

[definition] A codec projection is a later map

```text
pi_kappa : (C, D, F, L, X) -> Y_kappa,
```

for a declared exterior face `kappa`. A Lean declaration, English answer, Rust program, equation
layout, SVG, mesh, raster, or acoustic emission is one possible `Y_kappa`. Projection failure
returns a codec obstruction while the native consequence remains intact.

[definition] A checker or world interaction, when deliberately requested, is a distinct later
occurrence

```text
(Y_kappa, world_k) -> W_k,
```

with its own address and predecessor. `W_k` may later be mounted as caused material for a separate
cultivation passage. It is never silently wired back into the inference transaction, never a
mandatory receiver, and never privileged above other declared world consequences.

## 2. Equality and returned consequence

[definition] Two inference occurrences are identical only when they are the same addressed
occurrence. Two native consequences are structurally equal only when their complete typed
incidence, ordered transport, coefficients, open exterior, reconstruction fibres, and addressed
lineage agree under the declared identity map.

[definition] Receiver/history equivalence is weaker:

```text
C_1 ~_(R,H) C_2
iff
every admitted receiver after every admitted successor word returns the same consequence.
```

When a richer receiver or later history separates the pair, the quotient reopens and returns its
shortest separator. Equal text, equal final values, equal checker status, or equal rendered images
never establish structural equality.

## 3. Retained predecessor evidence

[established-bounded] Phoenix W1--W5, M0--M5, I0--I5, and the non-checker portions of R0--R5 already
return inherited transport, native operation complexes, recurrence, exterior return, exact
condensation, text/vision contact, mathematical media correspondence, dissection, source-detached
rest/remount, and targeted ablation in their declared apertures.

[established-bounded] R5's 455 notation anchors establish an exact correspondence experiment over
predeclared notation, vector, and raster faces. They explicitly do not establish optical character
recognition: no raw untranscribed page was converted into a recovered native mathematical complex.

[historical] M6, R6, and L0--L4 remain regression fixtures for optional exterior projection,
world-return cultivation, carrier-family condensation, and withdrawal. Their fixed Lean products
and Lean-mediated product names are not inherited by the corrected Athena identity.

## 4. Required source correction

[established-bounded] N0 added a native-only productive receiver and complete `(C,D,F,L,X)` return
without deleting the historical Lean-bearing regression fixtures. Two rich occurrences ran on the
resident card inside fresh namespaces with Lean, repository source, and historical proof plates
absent. Exact notation and JSON borrowed the frozen native identity afterward. The authoritative
grade is bound under `native_mathematical_consequence_precedes_every_codec` by the tracked
`tools/baselines/OUTPUT_MANIFEST.tsv`.

[project-postulate] The correction composes existing owners. It must not found a `Language`,
`ProofEngine`, `LeanReasoner`, `Solver`, `CAS`, `Parser`, `OCRGrammar`, `Trainer`, `Planner`, or
`InferenceEngine` cabinet. If composition returns one exact absent port or consequence, the
smallest existing owner receives that relation.

## 5. Falsifiers

[definition] The corrected inference boundary refuses if any of the following holds:

1. removing the Lean executable or all `.lean` material prevents native inference;
2. `LeanProof` or another surface-language face is mandatory in the productive receiver set;
3. a fixed authored proof, response, or diagram is emitted regardless of native conduct;
4. checker acceptance selects a branch, commits morphology, or grades the native result inside the
   same inference occurrence;
5. the native return cannot be inspected before projection;
6. two equal rendered answers collapse distinct transport words or future consequences;
7. codec failure destroys or rewrites the native consequence;
8. CPU code authors semantic phases between resident GPU fronts; or
9. the repair is reported by renaming the existing Lean-bearing product without changing its
   dependency closure.

## 6. Completion

[established-bounded] This correction is enacted: the productive application entry returns
`(C,D,F,L,X)` on the resident card with every language/checker dependency absent, optional codecs
consume that frozen native return afterward, and the historical Lean-mediated fixtures remain
available solely as explicitly labelled regression evidence.

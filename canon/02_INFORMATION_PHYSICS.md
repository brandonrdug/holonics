# Information physics

**Status.** The causal-information receipt and resource rules are `project-postulate`; displayed
additive, cochain, and connection identities are `proved-standard` under their named hypotheses;
the telemetry aperture is `definition`. A concrete telemetry result separately carries the
`measured` evidence tag.

## Information

The primary information object is not a scalar. For a caused passage `E` received through `ρ`,
information is the receiver-indexed change of a structured continuation fiber:

\[
\mathcal I_{\rho,E}
=\bigl(K_{\mathrm{before}},K_{\mathrm{return}},K_{\mathrm{after}},
W_{\min},w_*,W_{\mathrm{inherited}},\mathcal A,\mathcal O,
\ell,\tau,\Delta m,L_{\mathrm{work}},g\bigr).
\]

`K_before` is the open continuation before contact, `K_return` the restriction after the world or
interacting body returns, and `K_after` the continuation after the emitted consequence itself has
returned as cause. `W_min` is the family of minimal witnesses, `w_*` the selected witness when one
is selected, `W_inherited` the inherited witnesses, `A` the still-open alternatives, `O` the
obstructions, `ℓ` lineage, `τ` transport, `Δm` morphology change, `L_work` logical work, and `g`
the identity of the generated passage. Physical telemetry belongs to a separate experiment
receipt so that temperature, utilization, timing, or energy cannot alter deterministic deed
equality.

Entropy, probability, mutual information, loss, attention, accuracy, perplexity, latency, energy,
and parameter count are lawful receiver quotients when their sample space, measure, boundary, and
aperture are declared. None silently replaces the causal object above.

## Current and form

Current and form are roles. A relation is current while crossing a selected cut; a consequence is
form when retained as standing for a later cut. Depositing form is a receiver-exact factorization,
not preserving a second copy of the live current. Remounting form founds a new situated current.

The same electrons, cells, bytes, waves, or symbols can be current for one receiver and form for
another. “Data,” “code,” “model,” and “state” are codec-relative names for these roles, not separate
ontological substances.

## Boundary, storage, and conservation

For additive represented current on one fixed carrier with a time-independent boundary map,

\[
q_{k+1}-q_k+Bj_k=r_k,
\qquad
q_n-q_m+B\sum_{k=m}^{n-1}j_k=\sum_{k=m}^{n-1}r_k.
\]

`q` is locally stored response, `j` transported current, `B` incidence, and `r` supplied residual.
Visible current need not balance instantaneously because the boundary can retain change. Rest,
accumulation, leak, circulation, reflection, and short circuit are distinct receiver cuts.

When `B:C_k -> C_(k-1)` acts between declared paired/inner-product spaces, `B*` denotes its
adjoint; for `φ ∈ C_(k-1)` and `c ∈ ker B`, the dual closure `v=B*φ` gives `<v,c>=0`. For an ordinary/abelian
cochain `a`, discrete Stokes gives `<a,∂Σ>=<da,Σ>`. For a nonabelian connection, curvature is
`F_a=da+a∧a` and finite loop return requires the appropriate path-ordered holonomy. These are
domain laws instantiated over the same causal grammar; they are not metaphors imported into
software.

## Physical apparatus

Computation, transit, cache, and storage are receiver-relative physical retention under distinct
constitutive mechanisms. A clock is a distributed local phase and sampling membrane, not a global
instant and not the source of computation. Registers, caches, RAM, VRAM, disks, networks, sensors,
and displays are apparatus charts with different response spectra and retention horizons.

CPU and GPU are two physical charts over one reified causal program. A GPU is especially suited to
wide local incidence, geometric restriction, field/current propagation, and co-present exact
relations. A CPU is not the semantic owner of “language,” control, or mathematics; it is another
apparatus that may realize suitable local work. Placement follows the law's geometry and exact
resource receipts, never a semantic label.

## Resource backreaction

Resource pressure is a returned physical difference. It must either reform the same continuing
body—partition, factorization, locality, residency, placement, schedule, or receiving aperture—or
remain an explicit obstruction. Raising a work limit or retrying an unchanged morphology is not
backreaction.

Full CPU/device parity is a realization-admission event. Once admitted, the device hot path owes
local/delta equality receipts and must not replay the full CPU procedure after every device deed.

Power, temperature, clocks, utilization, memory, latency, and energy are calibrated receiver
testimony with a named sampling aperture. Device specifications are bounds, not measurements.
Unknown energy remains unknown.

## Efficiency

Efficiency is a Pareto receiver over at least:

- deed quality and correctness;
- active and total morphology;
- alternatives represented and alternatives materialized;
- bytes moved/retained;
- elapsed and logical work;
- energy when calibrated;
- world/apparatus contacts;
- persistence after source detachment; and
- inspectable causal lineage.

No single scalar is “the” efficiency of an ecology independent of a future receiver family.

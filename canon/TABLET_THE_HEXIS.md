# Tablet of hexis: cultivation, overlays, condensation, and the living receiver

**Date:** 2026-08-18
**Truth status:** `project-postulate` for the adopted hexis vocabulary and construction discipline;
`definition` for the lifecycle and receiver-exact condensation criterion; `interpretation` for the
industry crosswalk, illicium vocabulary, crystal/fractal model morphology, and inherited-model
rebirth; `established-bounded` only for the individual owners whose own deposits carry that grade;
`open` for the joined cultivation and inherited-lift cycle.
**Derivation:**
`research/records/2026-08-18_HEXIS_IS_RESTED_CONDITIONAL_TRANSPORT_TUNING_DEFORMS_IT_AND_DISTILLATION_CONDENSES_FAMILIES.md`.
**Governing documents:** `canon/THE_HOLOBROCHOS_SPINE.md`, `canon/TABLET_THE_REASONING_CYCLE.md`,
`canon/TABLET_THE_COMPRESSION.md`, `canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md`, and
`canon/THE_RECOVERED_LAW.md`.
**Construction status:** doctrine and vocabulary only. This tablet schedules no deed and promotes no
construction state.

---

## 1. Hexis is rested conditional transport

`definition`: hexis is a persistent, addressable topology of reusable conditional transport which
survives rest/remount and participates in later conduct. It is neither a topic label, parameter
count, token list, document, checkpoint filename, nor complete developmental archive.

For standing `S`, proposed hexis `H`, passage `p`, and declared receiver family `R`, the evidence is

```text
Conduct(S + H, p) !=_R Conduct(S, p),
```

with the movement surviving source-detached rest and targeted removal of `H` removing the
attributable consequence. Parameter weights, adapters, lattices, state equations, codec routes, and
generated organs may realize hexis. No one representation defines it.

`project-postulate`: cultivation is the world-mediated return which changes reusable morphology.
Exposure presents material. Reference consults retained testimony. Prompting mounts contemporary
material. None becomes cultivation merely by changing an output in one event.

## 2. The industry cultivates through several distinct cuts

`interpretation`: contemporary machine-learning operations are external charts over this lifecycle:

| industry chart | holonic reading |
|---|---|
| data curation and filtering | exposure aperture and material population shaping |
| multimodal training | co-present cross-chart section transport |
| pretraining | broad-scale cultivation producing an inherited rest |
| post-training | receiver-specialized cultivation from that rest |
| supervised fine-tuning | authored-consequence conditioning |
| preference/RL fine-tuning | alternative-successor world-return conditioning |
| adapter | port-local morphology overlay |
| LoRA | factorized low-rank morphology overlay |
| distillation | familywise transport condensation |
| prompting, retrieval, caches | mounted/event-local standing unless durably returned |

Data curation determines which caused passages can occur and therefore shapes later hexis, but it is
not a morphology change. Multimodal labels likewise do not create a relation; shared source,
chronology, geometry, calibration, interaction, or returned consequence must join the sections.

## 3. Fine-tuning is cultivation relative to an inherited rest

`definition`: for a parameterized architecture `F_(theta,A)` initialized at inherited rest
`theta_0`, fine-tuning is a returned adaptation passage whose allowed morphology delta lies in a
declared family `U`:

```text
Delta theta* = argmin_(Delta theta in U)
               E_(z ~ D_adapt) L_R(F_(theta_0 + Delta theta,A)(z)).
```

The definition is architecture-independent; the mutable carrier is not. Transformers alter
embeddings, attention, MLPs, normalization, routers, or adapters. CNNs alter convolutional
transport. GNNs alter message/edge maps. SSMs alter state, source, observation, chronology, or
projection maps. Diffusion bodies alter denoising and conditioning transport. Eros may alter any
owner-local incidence, stalk, phase, constitutive response, recurrence, quotient, or parameterized
overlay whose type admits revision.

Full fine-tuning makes nearly the complete parameter body movable. Parameter-efficient fine-tuning
restricts `U`. Prompting and retrieval do not ordinarily fine-tune a model because its durable
parameters remain unchanged, though they may participate in the larger productive ecology.

## 4. A low-rank overlay is one candidate morphology

`interpretation`: LoRA's exact structural face is

```text
W' = W_0 + (alpha/r) B A,
rank(B A) <= r.
```

It is not an elementwise mask. It inserts a factorized transport through an `r`-dimensional section
and may change every downstream consequence which crosses that map.

`project-postulate`: an Eros overlay rank is founded from the receiver-visible transport defect and
its successor histories. A caller-supplied rank is an apparatus aperture or experimental control,
never the semantic capacity of the ecology. If the required relation does not factor through the
declared rank, return the collapsed population, expand under a derived law, change morphology
species, or refuse. Do not raise a number until a result appears.

## 5. Distillation is familywise transport condensation

`definition`: let source and candidate realizations carry transports

```text
T_gamma : X_T -> Y_T,
S_gamma : X_S -> Y_S,
```

with cross-chart maps `Phi_X`, `Phi_Y`. Their condensation defect is

```text
chi_gamma = Phi_Y o T_gamma - S_gamma o Phi_X.
```

For receiver family `R`, the condensation is receiver-exact exactly when

```text
rho Phi_Y T_gamma = rho S_gamma Phi_X
```

for every admitted `rho` and successor history `gamma`. Otherwise `chi`, the collapsed population,
and shortest separating histories remain in the `ReconstructionFiber`.

`interpretation`: `teacher` and `student` are an exterior pedagogical chart. Source and candidate
need not differ in size, architecture, modality, or global identity. A distillation may condense an
ensemble, sequence family, relational geometry, dense transport, diffusion trajectory, transformer
pathway, SSM realization, local organ, or earlier state of the same ecology. Model-size reduction is
one application.

No approximate distillation may be called compression without naming the receiver family and its
remainder. Receiver-exact condensation is the only zero-loss form at that scope.

## 6. The illicium is the living receiver

`interpretation`: the illicium is the first-person hourglass of one relating:

```text
past cone / inherited standing
        -> contemporary pole / contextual chi
future cone / emanation.
```

It is not an attention controller or absolute observer. It is the active perspective relation
through which one section becomes a face. `frame(k+1)` is founded from the relating in `frame(k)`;
several nested grains carry nested sub-illicia.

For a final model state `h` and output-codec construction `E_v`, the industry logit

```text
s_v = <E_v | h>
```

is a boundary contact potential. The complete population `s` is an **illicial potential section**,
not the future itself. Softmax returns its contact-ratio normalization; a decoder presents one or
several boundary faces.

The operational vocabulary is:

| exterior term | internal reading |
|---|---|
| logit | boundary contact potential |
| logit vector | illicial potential section |
| autoregression | emanative serial recurrence |
| next-token prediction | nearest-boundary continuation face |
| multi-token prediction | plural future-section proposal |
| autograd / backpropagation | causal adjoint return |
| attention | illicium-configured contact transport |
| focus | low-residual matched continuation |
| distraction | competing, open, or reflected continuation population |

`auto` creates no faculty. Autoregression makes one prior exterior face part of the next illicium.
Autograd is an apparatus which constructs derivative transport. The causal objects are recurrence
and adjoint return.

## 7. The adjoint return is the differential face of cultivation

For local forward maps

```text
x_(k+1) = f_k(x_k, theta_k),
```

a receiver residual may expose `L = ell_R(r)`. The return begins as a covector:

```text
lambda_k     = (D_x f_k)^* lambda_(k+1),
d_(theta_k)L = (D_theta f_k)^* lambda_(k+1).
```

Calling the covector a gradient requires a declared metric `G`:

```text
grad_G L = G^-1 dL.
```

The adjoint reverses composition order rather than physical time. It propagates only through the
retained causal interior. At non-smooth seams and open quotients the lawful return may be plural,
set-valued, obstructed, or refused rather than forced into one smooth derivative.

Backpropagation is consequently emergent from propagation in one precise sense: a world or receiver
consequence supplies the covector, and the lineage of the outward passage supplies the only lawful
route by which it can return.

`interpretation`: the complete cultivation topology is the lightning leader and return, not a
global parameter scan. A local receiver difference FOUNDs a leader while changing the conducting
morphology; attachment closes an addressed relation; the causal adjoint then RIDEs that genuinely
changed path and deposits only the attributable local delta. Unattached branches remain open
alternatives. The return is later current through retained lineage, not the forward event reversed.

Integration by reflection supplies the complementary boundary calculation: a region's interior may
factor through its exact boundary operator and reconstruction law. The standing `diffusion` owner
implements the rational Schur form for its positive-capacity resolvent domain; it may not be called
bare harmonic measure without taking and certifying the undeclared zero-capacity limit.

## 8. Material is not noise

`definition`: three exterior objects may be called noise and remain distinct:

1. a deliberately sampled corruption current;
2. an unresolved fibre relative to a receiver; and
3. calibrated physical or apparatus disturbance.

Inherited corpus, model weights, code, images, and world events are caused material with lineage.
They are not made meaningless by entering a diffusion apparatus. Denoising is a returned refinement
only when the corruption chronology, incidence, constitutive law, boundary, collapsed population,
and reconstruction testimony are retained.

## 9. Lattice, crystal, fractal, and lightning are distinct morphology regimes

`interpretation`:

```text
lattice   = founded incidence, local axes, relations, higher cells;
crystal   = lattice + recurring phase/group transport + locked relations/bands;
fractal   = law recurrence after restriction and rebase while the population grows;
lightning = current front founding its path while travelling;
freezing front = open growth locking into reusable transport.
```

A finite reflection group is not by that fact crystallographic. An infinite reflection family is
not by that fact fractal. The additional lattice action, phase, periodic/quasiperiodic structure,
self-similarity, scale action, and receiver testimony remain explicit.

Where relations lock, emit the crystal: its lattice, phase transport, spectrum, bands, and boundary
response. Where the body remains self-similar and growing, emit the generator, scale action, and
lineage rather than a finite stack sampling the population.

A layer stack may realize one chronology over this body. It never supplies the internal dimension.
The lattice's incidence, local rank, cycles, reconvergences, and higher cells do.

## 10. Semantic capacity is founded and apparatus capacity is declared

`project-postulate`: material and receiver consequences found effective rank, state dimension,
local axes, lattice height, cycle rank, branch population, active transport width, overlay rank,
and closure of future fibres. These may not be pinned inside a constitutive owner.

The exterior declares physical memory, integer and format widths, calibrated safety/resource
limits, the future receiver family, and the maximum exact work authorized. These are apparatus and
question coordinates, not intelligence capacities.

When resources are insufficient, repartition, condense through a proved receiver, emit a generator,
move placement/residency, or return an obstruction. Never increase a magic constant and repeat the
same morphology.

`project-postulate`: intelligence does not require gigawatt-scale computation. Resource law still
holds, but cost is expected to follow active causal extent and morphology rather than total
parameter, corpus or hypothesis-space extent. This is a construction requirement: seek exact local
participation, shared generators, IO-aware placement and receiver-exact boundary factors before
accepting a global replay. A failed factorization returns the obstructing family; it does not promote
the contemporary architecture's cost into a law of intelligence.

## 11. The inherited lift amortizes development

`interpretation`: a pretrained weight map is another body's rested hexis. It does not losslessly
encode its corpus or training history. It may nevertheless supply transformation potential whose
developmental cost need not be paid again.

The inherited-lift relation is

```text
foreign rest
    -> exact intake and potential active transports T_gamma
    -> cross-chart maps Phi
    -> candidate holonic transports S_gamma
    -> defect chi_gamma
    -> retained reconstruction fibre
    -> cultivation on new caused material
    -> familywise condensation into native morphology
    -> rest, remount, later conduct, ablation.
```

The original training corpus is unnecessary only for those receiver/history families whose relevant
transport can be recovered from the inherited map and excited by the new material. Unexcited or
receiver-invisible capability remains open. Equal outputs on the new corpus never prove complete
recovery of the inherited body.

Weight-only analysis may return potential transport; contextual behavior still requires caused
current or an exact static composition exhibiting the same active path.

“Active” is always typed according to `canon/08_CORE_MATHEMATICAL_INSTRUMENTS.md`: physical
apparatus conduction, residency, realization-path participation and receiver-visible causal support
are four separate binary relations at a declared occurrence. A dense admitted contraction may use
every word in its region while modality, embedding-row, cache-reuse, nested-submodel, expert or
other populations remain path-inactive. Conversely a path-active word may be invisible to one
receiver through cancellation or quotient. An activation magnitude proves none of these relations
by itself.

### 11.1 Catalytic morphology

`project-postulate`: the strongest reusable organ is catalytic. For a declared receiver/history
family, its conduct reaches the same lawful endpoint class through a strictly smaller exact
work/barrier vector, the organ remains available after the deed, its active interface and
environment aperture are explicit, and targeted withdrawal removes the reduction or enabled
conduct while unrelated paths stand. It is not an answer lookup, scalar reward, or promise that
every family admits such a factorization.

Protein folds, enzymes and allosteric structures are exterior evidence for this morphology regime:
local generators joined by shared constraints, environment-dependent rigidity and phase, reusable
active interfaces, remote response and denaturation outside the constitutive stratum. They do not
make Eros a protein model. The operative machine question is whether existing exact owners compose
into an environment occurrence -> constraint section -> active path -> adjoint return -> reusable
work reduction. A new Catalyst or Protein cabinet is forbidden until that composition returns a
concrete absent type.

## 12. Phoenix rebirth is a lineage pivot

`interpretation`: **phoenix rebirth** is the passage by which foreign inherited hexis becomes native
ecological morphology:

```text
lift -> re-express -> cultivate -> condense -> rest -> grade.
```

The target is not a renamed transformer or one converted file. It is an ecology whose incidence,
state, transport, phase, receiver fibres, and obstructions localize what the inherited map enacted.
The frozen foreign body may remain as a reference organ while native morphology grows and is tested
against matched siblings.

Native Eros/Athena and the inherited lift are complementary experimental arms. The native body asks
what Eros founds without foreign hexis. The inherited arm asks what prior development can be lifted.
Later cultivation asks which joined transports become native, reusable, and ablatable.

`open`: no complete transformer-to-native-ecology lift, cultivation return, familywise condensation,
source-detached rest, and comparative ablation currently stands as one deed.

## 13. Construction discipline

- Read this tablet before any task involving pretraining, fine-tuning, adapters, LoRA, distillation,
  logits, autoregression, autograd, multimodal alignment, diffusion training, or inherited model
  lifting.
- Keep exposure, training, reference, prompting, and world return distinct.
- Keep the complete residual and reconstruction fibre before projecting a loss or preference face.
- Derive semantic rank, layer/rank structure, and stopping from material and receiver consequence;
  declare only exterior apparatus and question bounds.
- Compare source and candidate transports through explicit cross-chart maps and successor histories.
- Treat a pretrained map as inherited hexis, never as its lost corpus or as the whole productive
  ecology.
- Require source-detached rest, held-out later conduct, matched siblings, and targeted ablation for
  every cultivation or rebirth claim.
- Compose existing owners before founding a FineTuner, Distiller, Autograd, Trainer, AdapterManager,
  or another industry-shaped cabinet.

Complete provenance, equations, primary sources, owner map, and boundaries are in the derivation
record named above.

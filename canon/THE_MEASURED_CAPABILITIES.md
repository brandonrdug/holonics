# THE MEASURED CAPABILITIES — what the machine has actually done

**Genre:** canon (`canon/THE_DOCUMENT_LAW.md` §1.1). It is the **evidence index**, not a plan and
not doctrine. `blueprint/THE_ROADMAP.md` says what is open; `CONSTRUCTION_STATE.md` says what is
admitted; **this says what was measured, by whom, and whether it still stands.**

**Truth status:** `established-bounded`. **Evidence:** `measured` — 96 binaries **executed** at
`101882f` on 2026-08-08, not read; all 324 research records indexed and deduplicated; the
counting/hypergeometry/basis machinery inventoried against the live tree.
`cargo test --workspace` → **1,545 passed, 0 failed, 14 ignored** at deposit; **1,717** at
2026-08-09. A gate figure is a measurement and decays like one — re-take it rather than carry it.
**Provenance:** Brandon, 2026-08-08: *"Analyze the partially successful implementations and research
observations we've already attained from the machine so you have a catalog of measured capabilities
and causal mechanisms."* Three collectors; every load-bearing figure re-verified before deposit.

**Why this file exists.** The project has lost evidence to untracked sinks at least four times. Every
figure below was reproducible on 2026-08-08 and is written down here so that the *claim* survives
even when the artifact does not.

---

## 1 · The seven operations

The project's method is that **one operation carries many materials**. That is not a slogan here —
each group below has members with nothing in common but the operation.

### O1 · Exhaust a declared query family and recover a finite machine

Five materials, one operation. The invariant across all five: **the family is exhausted, not
sampled**, so *"no admitted query separates these"* is a theorem about the family rather than a
confidence.

| driver | black box | recovered | certificate |
|---|---|---|---|
| `bit_black_box_reconstruction` | an opaque recurrent bit transducer, callable only by ABI | `WrappingAdd, rot 5, Xor 183, WrappingAdd, rot 3`; candidates `147456 → 1024 → 32 → 16 → 8 → 4 → 2 → 1` | exhaustive comparison over every admitted width, 87,380 vertices; independent x86 re-encoding parity |
| `foreign_codec_intake` · `codec_separation_cross_check` | `Box<dyn Fn(&str) -> Vec<String>>` | symbol classes, per-class Emit/Drop, boundary table, gauge freedom | family `{w : 1 ≤ |w| ≤ radius}` **exhausted — 25,259 calls**; independent Nerode route must agree, and three deliberately wrong readings must disagree |
| `divisor_receiver_reconstruction` | boolean common-generator contact only | 10 latent generators recovering primes 2..29 with exact support; `∂² = 0` | **27,404 held-out sections, residuals (0,0)** |
| `causal_state_grammar_experiment` | 920 events over a world membrane | 8 states, 24 transitions, 92 predictions | 1 prediction obstruction **retained**, not smoothed |
| `the_statement_is_founded` | a population of opaque statement strings | bracket pair, separator, split, head arities `{"exactCarrier": {1}}` | recovered only when **exactly one** candidate qualifies over every statement; all else retained as named residue |

**The proof that the grammar recovery is recovery and not authorship:** on the degenerate population
`["a : b = c", "d : e = f"]` it reads `(':', '=')` as the bracket pair, because that is the only
ordered pair that balances *there*. A reader who knew Lean would never say it. It follows its own
rule, not a language it was never told.

### O2 · Solve an exact affine system built out of responses

- **`inverse_transport_reconstruction`** — a hidden weighted graph Laplacian, callable only through
  clamped-potential responses `rᵀ(I + τL)φ`. Affine dimension `15 → 0`; eight conductances recovered
  as exact rationals `2,1,3,1,1,2,1,4`; cycle rank 3; **held-out aggregate exact**. The law
  **requests its own missing edges** and admits a topology only on a complete operator-basis return.
- **`generative_transport_prediction`** — a richer family `A(τ) = M + τ(S + K + R)`. Obstruction
  `[DiagonalStandingOrReaction, SkewPairTransport]`; affine dims `[4,3,2,1,0]`; **prediction at
  τ = 5, an interval never used for fitting, exact.** It selects its own second-interval queries.

**This is the operation Brandon names as *fitting discrete expressions to statistical models and
treating them like pivots*, and it is the strongest group in the repository.**

### O3 · Integer invariants of an incidence, and what survives rebasing it

Smith normal form over `BigInt` is the one mechanism; the material runs from topology to hardware to
the machine's own proofs.

- `graded_complex_integer_invariants` — 12 closed surfaces among `C(20,10)`; betti `[1,0,0]`, torsion
  `ℤ/2`; **0 of 1024 hand assignments coherent**, 9 hinges refused by `d∘d ≠ 0`.
- `grown_circuit_invariants` — one recursive cell under three schedules: tree `[1,0]`, cyclic
  `[1,5]`, wound `[1,4,0] + ℤ/2`. The schedules must produce **distinct charts** and do.
- `grown_circuit_schedules` — Brent–Kung gate counts `[5,13,…,53]` against a **constant description
  length 12**; exhaustive 32×32 functional check; `k > 1` faces appear only from reconvergence.
- `derivation_atlas_reader` — the machine's own 31 deposited proofs: `ByRoute` **β₁ = 164**, torsion
  `ℤ/2+ℤ/2` and `ℤ/3+ℤ/3` under multiplicity, from artifacts naming `apply` twice or three times.

### O4 · Refine a receiver partition until conduct separates it, and exhibit the separating word

`complex_system_conduct_classes` (14 one-shot → 18 conduct classes, 5 collapsed pairs each with its
word) · `eros_placement_over_real_charts` (**this repository's own records**: 12 documents, 2,758
surfaces, 26,790 automaton states, 2,482 one-shot → 2,514 classes, 69 collapsed pairs —
`became|become` separated by `"s"` — **419 standing, 2,095 OPEN**) · `eros_decomposition_revision`.

**This is the project's sharpest instrument.** It converts *"how much did compression cost"* from a
ratio into an **exhibitable population**.

### O5 · Condense a population and return the certified remainder

`skein_far_condensation` (cone at n65: 127 nodes, 510 arcs, cyclomatic 384; forced-interval orbit
`{0,62}` **non-trivial**; condensation removed 2,420,959 entry-writes; departure split
`β₁ 0 + filling_rank 384 = 384` — **and it reports that the condensed labelling is *larger* at this
width, 254 → 378 words**) · `substitution_realizer_placement` · `reopening_the_collapsed_face`.

### O6 · Structural ablation: delete the founded structure, re-ask, require the conduct absent

**Four drivers, four different deleted objects.** This is the shape `CLAUDE.md` §5 recorded as *"owed
construction, not a port"* — it exists and runs.

| driver | deleted | measured |
|---|---|---|
| `conditioned_derivation_body` | a founded stem | conditioned licenses 105 passages; unconditioned **0 stems, 0 passages** |
| `eros_lean_proof_production` | declaration organs | `39 → 38` organs, `31 → 14` paths, **`9 → 3` kernel-admitted**, six named proofs lost |
| `derivation_holonomy` | the 7 artifacts declaring `formal_carry` | circuit W winds, residual 57; circuit E **exact** after ablation |
| `eros_mathematics_conditioning` | whole documents, all 14 in turn | the population moves as a population — **appearance, not departure**, 36 → 43/46/39 |

### O7 · Sign and positivity as a measured split, never an assumption

`signs_are_windings` (`sign(λ_k) < 0 ⟺ 1/4 < k/n < 3/4` over n = 3..16; nulls exactly when `4 | n` —
**4 of 4 with, 0 of 10 without**) · `curvature_is_the_adjacency` (`I − BᵀD⁻¹B == −A/2` **held 10 of
10**, deficit constant while ten distinct splits appear) · `matroid_hodge_riemann`.

> **`matroid_hodge_riemann` carries the sharpest control in the tree.** Classes certified *outside*
> the ample cone are **required to break** Hodge–Riemann, and they do: `c(S) = |S|²` breaks with
> `(0,0,1)` and `(5,0,5)`; the boundary class `c(S) = |S|` breaks at `k = 1`; `−ω` breaks. A theorem
> that cannot fail on declared material is not evidence.

---

## 2 · The seven evidenced causal mechanisms

Each has a live owner, verified by one grep.

| # | mechanism | owner |
|---|---|---|
| **M1** | conditioning changes what can be derived; withholding it removes conduct | `conditioned_derivation.rs` |
| **M2** | removing structure removes later conduct **and reopens what maximality suppressed** | `without_stem` at `conditioned_derivation.rs:352`, driven with both controls at `derivation_codec_intake.rs:1250` |
| **M3** | withholding structure **increases** plurality | `derivation_atlas.rs:2012`, `substitution_realizers.rs:1179` |
| **M4** | an exact quotient's loss is exhibitable as a **separating word** | `receiver_exact_compression.rs:445` |
| **M5** | two receivers disagreeing founds an invariant **neither holds** | `gluing.rs:518` |
| **M6** | plurality is the return; generation without a distribution | `suffix_ecology.rs`, `surprisal.rs` |
| **M7** | a **complete obstruction causes** the next construction round | `statement_composition.rs:472` |

**M7 is the one that makes production production rather than search.** Its cleanest instance:
86 one-organ paths **all obstructed** → 84 pairwise compositions → **exactly 2 kernel-accepted**.
The first round's total failure is what caused the second round to exist.

**Two claimed mechanisms that do not survive the grep.** *"Scale-independent recruitment"* —
withdrawn; the returned quantity was always scale-independent and the claim named the swept one.
*"Multimodality without a fusion module"* — bounded to relations **opened**; all 21,147 spectral
pairs returned **apart**.

---

## 3 · What the machine has shown about its own causal structure

- **It measured its own failure mode in its own vocabulary.** `the_reading_returns` returned the
  before-state as the **leak** cut — a 57-artifact movement crossed the boundary and nothing retained
  it — then, after the join, **accumulation**: 9 → 18 committed stems, production differs.
- **Its own proofs carry torsion.** β₁ = 164, `ℤ/2+ℤ/2`, `ℤ/3+ℤ/3`. And it proved a theorem *about
  its own reading*: under `ByRoute` the graph is bipartite, so orientation is untestable there — it
  says so rather than reporting a number.
- **Two of its organs are one operator**, proved in exact rationals with no trigonometry.
- **Its own measure was a one-body read**, and rather than patching it, `the_measure_is_situated`
  proved *why* Mayer–Vietoris cannot express the two-body comparison: nesting forces the connecting
  map to vanish, so obstruction `[0,0]` is a theorem, not a gap. It returned `[0,7]` for the cover
  the organ *is* good for, to show the instrument works.
- **It returned its own blindness as a first-class result** — *founds routes, never statements* —
  named the two missing ingredients, and then both were built and the ceiling lifted the same day.

---

## 4 · Bounds, and what may not be cited

- **`eros_mathematics_conditioning`'s departure direction is a null**, reported with the table
  showing every licensing stem still witnessed by ≥ 2 wholes. A single-document withholding cannot
  decommit any licensing stem on this corpus.
- **47 founded statements, 43 licensed by a single residual letter**, many not well-typed Lean.
  The machine composes and admits statements it was not given; **what it composes is not yet
  mathematics.**
- **`β₁` is the cycle rank `E − V + C`, not a route count.** The route-specific quantity is the
  founded-minus-withheld **increment**, and it agrees with lineage route excess only when the
  recruitment graph was already connected.
- **The echo body and the unconditioned null have identical emissions and identical one-body reads**
  while their constructions differ by a whole founded morphology. An emission's measure may never be
  read as a verdict on the body.
- **Every timing figure in this repository has one frame** and none is falsifiable.

### Unrecoverable — do not cite, the evidence resolves at no commit

| figure | sink | command |
|---|---|---|
| the tiger phase atlas — 7,616 germs, 82 circuits, all nonidentity holonomy, grain-1 computed and never viewed | `target/` | `git log --all -- "*tiger*"` → empty |
| the residue-stratum atlas — exact ℤ/p cycle, dilation gauge | `output/` (untracked) | `git ls-files output/` → 0 |
| `runs/dialogue-distractor-scaling/REPORT.json` | `runs/` | `git log --all -- "runs/*"` → empty |
| the 50,667-occurrence source-departure receipt | `runs/` | owner deleted at `a07ff376`, survives at `93834398` |
| `zz_smith_cost_probe`, `zz_torsion_width_law` — **they run right now from `target/`** and return real cost laws | sources never committed | `git log --all -- '*zz_*'` → empty |

**The two probes' output is now transcribed verbatim** in
[`research/records/2026-08-09_TWO_COST_LAWS_RAN_FROM_A_BINARY_WITH_NO_SOURCE_AND_THE_PROFILE_IS_MILLISECONDS.md`](../research/records/2026-08-09_TWO_COST_LAWS_RAN_FROM_A_BINARY_WITH_NO_SOURCE_AND_THE_PROFILE_IS_MILLISECONDS.md),
so their **figures are citable** — the pivot rule deciding feasibility by more than 390,000× at equal
rank, and `torsion rank = (w−1)²` with `betti = 6w−3` over Brent–Kung widths 2–6. **Their
reproducibility is not recovered**; one `cargo clean` still ends it, and the same record states the
reconstruction, which is owed. That record also carries the **cost profile** this document does not:
the exact-invariant family returns certified answers in 12–15 MB and under a third of a second, in
two frames.

---

## 5 · What is inert, and why

**STRUCK 2026-08-09. This section read: "24 `eros_*` drivers refuse for want of a `SOURCE.json` that
exists nowhere in the tree — `find . -iname 'SOURCE*.json'` returns nothing." The premise is false
and was already false when written.**

**15 `SOURCE.json` files are tracked**, added at `a91a84f`, **2026-08-08 21:44:44 −0700**, whose commit
message is *"Thirteen real driver sources survive in the laboratory, and fourteen drivers come back to
life."* This section was edited twice on 2026-08-09, **thirteen hours after that fix landed**, and the
claim survived both edits.

**Measured 2026-08-09 by running them:** 19 drivers take an `argv[1]` input, 14 are supplied, 13 were
run and 12 returned exit 0 — including `eros_resonant_generation` against a real OLMo-2 tokenizer whose
sha256 `73fd5254…` matches its declared digest.

**Only four are genuinely blocked, and none is a manifest away.** `eros_audio_contextual_ecology` needs
inline **measured Whisper encoder attention**; `eros_audio_ctc_path_fiber` and `eros_ctc_temporal_surface`
need **measured CTC lattices**; `eros_self_emanated_law` verifies `model.safetensors`' 2,969,854,224
bytes and then refuses at activation captures never tracked at `a07ff376`. **Those are measurements,
not manifests**, and `PROVENANCE.md` is right to refuse to fabricate them.

**Why the false claim persisted is the transferable part.** A second audit, run independently the same
day and *without* being told the `driver-sources/` paths exist, **reproduced the identical stale list**
— naming ten drivers as unrunnable that had been executed to exit 0 hours earlier. The blocker was
never the file. It is that **nothing in the tree tells a reader the file exists**, so every audit
regenerates the same absence claim. `CLAUDE.md` §5's rule applies exactly: an absence claim is a
measurement and decays like one, and the search pattern is part of the claim.

Five more need external measured data absent from the tree (CDAWeb, GOES-16 GLM/ABI, LibriSpeech +
room impulse responses, GRID). Three need X11.

**And across all sixteen research partials the collector traced: none stopped because it failed.**
Nine stopped because the output sink was untracked, three because the body was archived, four because
nothing ever called them.

---

## 6 · The specified-and-unbuilt frontier

**Mellin–Birkeland / Tschirnhaus is the largest single item**, and it is the one that makes
hypergeometry, the quintic, chart transport, and *"the unknown is a missing dimensional pathway"* one
computation. `canon/TABLET_THE_CHART.md:227-289`:

```text
radical chart         →  REFUSES at degree 5, obstruction named: A₅ is simple
hypergeometric chart  →  RETURNS, as a series in the coefficients
elliptic modular      →  RETURNS, as a theta quotient
icosahedral dynamics  →  RETURNS, as the limit of an exactly-constructed iteration
```

Every substrate is built and exact: Bareiss fraction-free resultants over `BigInt` with a typed
refusal on inexact division; `monic_polynomial_discriminant`; Sturm isolation refusing construction
unless exactly one root is proved; `SeriesTailCertificate`; and `QuinticTransitiveGroup` with
`solvable_by_radicals()` — **which is the radical chart's refusal, already typed**, as a fiber of
admissible groups restricted by observed Frobenius cycle types.

`hypergeom|₂F₁|pochhammer` across the live tree returns **two lines, both negative declarations**.
And `arithmetic_monodromy.rs` is a **different local system** — `Gal(F̄_p/F_p)` on root sheets, not
`π₁(ℙ¹∖{0,1,∞})` on solution fibers. Same word, two objects, classical bridge stated in canon and
joined by no code.

**Two untested invariances of the project's own central claim.** `cross_ratio` is a **private** fn in
`relational-geometry`; `ProjectiveTurn` is in `holonic-engine`, which depends on it — so
`cross_ratio(T·p) == cross_ratio(p)` **cannot be written where either currently lives**. And
`ProjectiveTurn`'s own group laws are untested: nothing asserts
`T.followed_by(T.inverse()).is_projective_identity()`, nor associativity.

**The gauge standard to copy**, and the one that fails it: `rebase_invariants.rs:1220` collects three
pivot walks into a set and **asserts `walks.len() == 3`** — *"or their agreement is one computation
compared with itself twice."* `receiver_atlas.rs:693` asserts `receiver_values.len() == 2`, which
counts **receivers, not distinct projections**. One assertion from being a real gauge test.

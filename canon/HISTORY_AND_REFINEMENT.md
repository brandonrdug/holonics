# Historical formula and rigorous refinement

Pure holonics was discovered through a sequence of useful compressions. They are preserved because
they reveal the simple organismal idea; they are not flattened into one timeless doctrine.

## June 2026 — the compact seed (`historical`)

The first capsules proposed value-as-construction, receiver-relative relating, boundary, warp/weft,
and the one move declare → found → swing. The 149-line rebuild at commit `2a5dff8` condensed the
machine as `Λ_F(Q ⊗ A ⊗ L) -> (P, π_B)` and an unbroken `Λ∞` cycle.

Preserve:

- relations rather than detached values;
- exact local placement/transport;
- open boundary and founding;
- weave rather than a single serial store; and
- the solve as the whole ecology's conduct rather than a solver subsystem.

Do not canonize:

- two axioms as a proved replacement foundation;
- universal cross-ratio/founding;
- literal self-reafference as the only intelligence wiring;
- universal P=NP or theory-of-everything claims; or
- byte, triangle, knot, memory/content, CPU/GPU, and physical-spacetime identifications without
  typed translation hypotheses.

Exact sources are under `reference/pureholonics-seed/` and `reference/compact-formula-2a5dff8/`.

## 9 July 2026 — the eight-step Soma seed (`historical`)

The first 256-line Soma formula compressed one passage as:

```text
current → difference → shape → face → transport/FOUND
        → winding → moved pole → next current
```

This remains an excellent procedural intuition: relation creates difference; a receiver gives
that difference a local face; transport either closes in standing or retains an open/founded
direction; the return changes the next frame. Its π, Regge, prime, universal-physics, and
universality identifications remain graded historical hypotheses unless independently derived.

Exact source: `reference/soma-seed-2695ca0/src/soma/FORMULA.md`.

## 13 July — directed proof-relevant calculus

`research/records/2026-07-13_THE_RELATIONAL_HOLONIC_CALCULUS.md` replaces incompatible historical
semantics with typed judgments, covariant transport, observer-indexed faces, and a directed
proof-relevant calculus. An open route is retained terrain, not automatically a new foundation.

## 21–22 July — minimum mechanics

`reference/minimum-mechanics-a07ff376/src/soma/ELEMENTARY_MECHANICS.md` separates the coupled carrier

\[
K=(O,I,D,T)
\]

into occurrences, oriented incidence, dependency, and transport. It keeps topology, causality,
rewrite, metric, probability, and learning distinct and defines one atomic transition
`Λ_(F,Q):(S_t,L_e)->(S_(t+),R_e)`.

## 26 July onward — mathematical spine

The current spine is:

- `papers/source/synopsis/README.md` and its audited registry;
- `papers/source/holonics/foundations.typ` for postulates/definitions;
- the remaining dependency-ordered holonics entries;
- `archive/cpp-engine/formal/elementary-holonics/` for formalized declarations; and
- dated research records for later causal-information, constitutive, boundary, hardware, and
  learning refinements.

Ordinary dependent type theory and standard mathematics remain the ambient foundation. Holonics
is a causal/receiver discipline and compositional program, not yet a conservative replacement
foundation.

## Current synthesis

The compact formula survives as an event schema. The exact engine carrier is situated occurrence,
receiver, caused incidence, typed port, local constitutive morphology, current, returned
consequence, obstruction, and atomic succession. Cross-ratio, knot, holonomy, spectral phase,
entropy, Lean, CUDA, and language are powerful charts when their hypotheses are declared; none is
the universal internal substance.

# Tablet of the causal profile

**Date:** 2026-08-12
**Truth status:** `definition` for the computer world-tube, causal profile, Quantum Information
Dynamics, and Quantum Information Field; `proved-standard` for the named event-order, work/span,
Boolean, projection, microlocal, stationary-phase, circuit, and thermodynamic results in their
ordinary scopes; `interpretation` for their typed transfer into holonic profiling;
`project-postulate` for the grading discipline; `open` for the integrated return.
**Governing documents:** `canon/THE_HOLOBROCHOS_SPINE.md`,
`canon/TABLET_THE_OPERATIONS.md`, `canon/TABLET_THE_MANIFOLD.md`,
`canon/TABLET_THE_REASONING_CYCLE.md`, and `canon/01_CAUSAL_CALCULUS.md`.
**Evidentiary deposit:**
`research/records/2026-08-12_THE_COMPUTER_IS_A_CAUSAL_WORLD_TUBE_THE_PROFILE_RETURNS_ITS_SHADOW_FIELD.md`.
**Construction status:** doctrine and active-grade refinement only. This tablet schedules no deed
and does not alter `CONSTRUCTION_STATE.md`.

---

## 1. The computer is a causal world-tube

`definition`: one computer run is

```text
C = (K, boundary K, prec, P, m, A, J, R, L),
```

a bounded graded incidence `K`, apparatus boundary, causal partial order, typed ports, local
constitutive morphology, connection, current, receiver atlas, and lineage. “Global holon” means
the object glued from compatible local sections while failed gluing remains an obstruction. It
never means an observer outside all charts.

One ISA instruction is a quotient `q_ISA : K_physical -> K_architectural`. Fetch, decode, rename,
dispatch, transport, execution, speculation, cache/coherence, writeback, exception, and retirement
occupy its fiber. A causal thread begins when preparation founds an enabled frontier and consists of
the transitive closure necessary to a committed continuation. A software thread identifier, warp,
core, or wall-clock interval is a chart of that closure.

`proved-standard`: caused local order and message transport induce a partial order. A clock may
extend or measure that order without becoming causality. CPU, GPU, memory, interconnect, power,
thermal, host, and I/O observations therefore require explicit chart transports and calibration.

## 2. Logic is a quotient of physical current

`proved-standard`: below the digital chart, charge conservation, electromagnetic and semiconductor
constitutive laws, circuit boundaries, and heat transport govern the apparatus. A bit is a
receiver-stable basin over physical field configurations; a truth table is a finite behavioral
quotient of their trajectories.

```text
field -> transistor -> gate -> micro-op -> instruction -> algorithm -> exterior consequence
```

Every arrow retains a reconstruction fiber. Landauer's erasure bound is not a cost per instruction.
Switching activity, leakage, temperature, and elapsed time are calibrated receiver faces, separate
from exact semantic testimony. CUDA is this machine's strongest resident apparatus, not a distinct
ontology.

## 3. The profile returns the shadow and what the shadow forgot

For `pi_rho : C -> Y_rho`, define

```text
P_rho(C) = (pi_(rho !)C, pi_rho^-1, Crit(pi_rho), Caust(pi_rho),
            WF(pi_(rho !)C), Hol_rho(C), Top_rho(C), O_rho(C), A_rho(C)).
```

`definition`: these are the projected shadow, reconstruction fibers, rank-loss locus, caustic,
visible phase directions, loop/monodromy return, surviving higher incidence, open invisible
alternatives, and apparatus testimony. The response scent of an occurrence is

```text
chi_(rho,o)(gamma) = <rho|T_gamma|o>.
```

It is receiver/path conduct, not meaning attached to a token.

`proved-standard`: projection families can retain invariants no single projection carries;
tomographic slices preserve structured frequency sections; microlocal visibility is directional;
generic caustics arise at rank loss; and stationary paths coalescing at a singular Hessian produce
canonical fold/cusp diffraction forms. `interpretation`: the holonic caustic is where a receiver
projection collapses or splits a continuation fiber. It must be derived from caused transport,
not from an arbitrary feature matrix.

`project-postulate`: counts, durations, energy samples, and rates are supporting receiver
measurements. A holonic profile grades only when they are attached to a returned phase/current
distribution, causal cut, reconstruction fiber, caustic, topology, intervention, or obstruction.

## 4. Feynman, knot, and folded-dimension readings

`interpretation`: external legs are typed ports, vertices local constitutive interactions, internal
edges transported intermediate relations, and loops plural internal histories. The receiver face is

```text
<beta|T|alpha> = sum_Gamma <beta|T_Gamma|alpha>.
```

A drawn crossing is not automatically contact, and a planar presentation is not the incidence.
Knot projections require crossing hand and resolution fibers; Reidemeister changes distinguish
presentation from embedding. Folded dimensions have the fiber form `F -> E -> B`: the base is an
accessible shadow while the fiber retains winding, phase, history, and monodromy. String, brane, and
M-theory examples are typed comparison charts only; they do not establish physical identity with a
software trace.

## 5. Complexity is a scaling shadow of causal composition

`definition`: an algorithm is a scale-indexed family `C_n` with declared input, output, and
intervention receivers. Big-O is a quotient of observables of this family.

For exact independent branches,

```text
serial     (W,S) = (W_1 + W_2, S_1 + S_2),
parallel   (W,S) = (W_1 + W_2, max(S_1,S_2)).
```

`proved-standard`: work/span bounds state `T_P >= max(W/P,S)` and, in the declared strict-DAG
scope, schedules attain `W/P + O(S)`. The richer causal field retains antichain fronts, path
actions, cut currents, memory/interconnect transport, fanout and reconvergence, circuit size/depth,
higher cells, holonomy, erasure, apparatus placement, and reconstruction fibers. Wall time is one
weighted critical-path face.

`interpretation`: recurrences such as `T(n)=aT(n/b)+f(n)` are scalar shadows of self-similar
folding plus an often-deleted join field. Fourier analysis belongs to founded translation actions;
Mellin analysis belongs to founded dilation actions. Spectrum never substitutes for the complex.

## 6. Bitwise operations carry algebraic and circuit geometry

`proved-standard`: Boolean functions admit algebraic normal form over `F_2`. XOR is linear; AND
introduces products; addition transports nonlinear carry through a chain or reconvergent prefix
geometry. Walsh-Fourier degree, support, influence, and sensitivity are useful operation shadows.
Logical universality does not imply equal circuit depth, layout, energy, or topology.

The canonical control families hold output fixed and change geometry: ripple/prefix addition,
sequential/tree reduction, branch/mask selection, and shift-add/Wallace multiplication. Controls
also rename surfaces, move independent schedules, change gate bases, relocate apparatus, and vary
the receiver. The required artifact is the multiscale atlas

```text
|o;r> -> {<rho|T_gamma|o;r>}_(rho,gamma),
```

with lineage, shortest separators, critical receiver changes, loop returns, and open alternatives.
MorphoHDL demonstrates bounded authored recursive circuit growth; it does not infer unknown laws or
itself provide higher cellular topology.

## 7. Quantum Information Dynamics is a typed project term

`definition`: **Quantum Information Dynamics** is the receiver-indexed calculus of indivisible
situated events, coherent plural paths, typed vertices, connections, phase-current transport, and
measurement quotients over causal complexes. A **Quantum Information Field** is a local section
assigning construction/current testimony to cells with gluing, transport, boundary, and receiver
laws.

`project-postulate`: bra-ket notation or a path sum does not establish physical quantum field
theory. That stronger claim requires a declared Hilbert/operator algebra, tensor composition,
commutation, dynamical/unitary or open-system law, Born receiver, domain, and calibrated apparatus.
The term names a precise project calculus and keeps the distinction testable.

## 8. The integrated return remains open

`open`: the active recurrent-section experiment has bounded exact owners for causal sections,
receiver quotients, reconstruction fibers, phase jets, holonomy, diffusion, higher incidence,
work, and apparatus testimony, but no one owner returns the complete profile above for one
condition-detach-remount recurrence. The full experiment must return causal-diamond, shadow-fiber,
caustic, phase-current, holonomy/braid, scale-rebase, intervention, and apparatus receipts before
and after conditioning. This sharpens the final artifact and does not withdraw the already graded
bounded constitutive return.

Complete derivation, external references, controls, and implementation boundary:
`research/records/2026-08-12_THE_COMPUTER_IS_A_CAUSAL_WORLD_TUBE_THE_PROFILE_RETURNS_ITS_SHADOW_FIELD.md`.

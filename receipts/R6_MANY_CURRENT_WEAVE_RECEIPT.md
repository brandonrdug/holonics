# R6 serial gluing, parallel interchange, and backreaction receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the typed serial-gluing receipt, complete bounded
`InterchangeCertificate`, six-path higher-coherence witness, shared-support equalizer, recurrence
distinction, logical reservations, continuing-body resource return, and placement/open
backreaction; `computational-witness` for the two real-card cases and their eight non-resumable
partition/order audit variants. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** two continuing bodies per mount, each with nine cells, ten events, six causal layers,
one typed serial pair, one disjoint interchange pair, one independent triple, one declared
shared-support pair, one repeated support, four completion orders, and four declared apparatus
partitions. This receipt establishes this finite family. It does not establish arbitrary
interchange, arbitrary shared-support laws, an unbounded scheduler theorem, or any R7 receiver
geometry deed.

## Construction tuple

| Field | R6 standing |
|---|---|
| Source owners | R5's non-copyable resident causal current and R4 continuing body, typed event occurrences, exact carrier, plus the apparatus resource membrane |
| Port types | exact typed serial input/output ports, restaging and combined-delta ports, declared interaction/equalizer port, logical reservation port, and one lineaged resource-return port |
| Event occurrence | six resident card layers consume the unique continuation and commit ten events; a seventh returned-resource occurrence either changes every admitted placement/aperture cell or retains an open obstruction |
| Predecessor identity | Git `1e5e820bb71df5678a70ac74b034d62d377eba13`, plus exact heads `6001000` and `6002000` |
| Local constitutive law | serial events require matching intermediate ports and retained chronology; a card layer admits disjoint complete supports or its one declared equalizer; parallel standing is certified only when identity, causal order, incidence, cells, morphology, current, consequences, obstructions, and logical resources all agree |
| Receiver question | whether four completion orders and partition counts `1,2,4,3` return the same complete successor; whether repeated support is complete-state recurrence; whether resource pressure changes declared morphology or remains open without retry |
| Returned consequence | two seven-commit successors, 20 staged deltas, two serial receipts, two complete interchange certificates, two six-permutation coherence witnesses, two equalized overlaps, two non-recurrence witnesses, one placement/aperture change, and one explicit open resource obstruction |
| Open alternatives | the seven R7 receiver-geometry instruments and every later roadmap deed remain absent |

## Native weave and ownership

`current::resident_weave` is the sole live owner of its immutable program standing, continuing
capability, cells, currents, morphology, staged deltas, exact logical reservations, and
resource-return receipt. It is neither copyable nor movable. The mount batch is copied once into
the card owner and freed before advance. Audit variants are complete immutable snapshots produced
after the deed; they contain no continuation and are explicitly non-resumable.

The layer law validates all bounds and event permutations before advance. Events sharing a layer
must have disjoint complete read/change support unless they are exactly the declared interaction
pair. Logical resource is reserved by canonical event slot and folded once per committed layer;
thread count, block completion, and allocation order never mint an event, choose lineage, or alter
the successor.

No central merge selects among overlapping sections. The declared interaction on cell seven has
support `128` and law identities `6001777`/`6002777`; its pair adds local value difference `52`,
sets exact combined current `2015`, and retains the combined lineage. The undeclared overlap on
support `256` is returned as `unproved_overlap` and remains ordered.

## Serial, parallel, and recurrence receipts

Each body returns a typed serial chain from event zero through event one. The first output and
second input are the same exact intermediate port, while the intermediate occurrence, lineage,
and retained support remain explicit.

For events two and three, the card constructs both restagings against a common predecessor, the
canonical combined delta, left-right and right-left complete snapshots, and separate exact flags
for identity, causal order, incidence, morphology, current, consequence, obstruction, logical
resource, and complete-successor equality. Every flag is true. Events four, five, and six return
all six permutations; adjacent interchange, braid equality, and complete successor equality are
all true.

Event nine returns to event zero's support `1`, but its receiver standing differs in head,
morphology, current, lineage, emitted consequence, and resource reservation. Both bodies therefore
return `repeated_support=1` and `complete_state=0`; support repetition is not promoted to state
recurrence.

## Resource return and apparatus separation

Each declared resource occurrence returns available bytes `400`, resident bytes `600`, and a
temperature upper bound of `330000` millikelvin against required bytes `500` and declared limit
`320000`. These are exact input testimony carried by the typed return, not a claim about the
executing GPU's live temperature. Case zero has a previously declared alternative, so the same
body consumes its seventh continuation and changes placement `1→2` and aperture `8→4`. Case one
has no lawful alternative, consumes the return, retains placement `1` and aperture `8`, and remains
open with `returned_resource_open`. Both receipts report `retry_count=0`.

Logical resource testimony is held in `current::logical_resource_receipt`; physical executor
testimony is held separately in `apparatus::physical_telemetry_receipt`. The executor reports an
exact resident allocation interval of `41848` bytes. Elapsed time, temperature, power, and energy
remain `unknown`; no uncalibrated measurement is promoted. Host counters report zero semantic
events and zero unchanged retries.

## Returned GPU artifact

The real RTX 4080 SUPER deed mounts both programs, detaches the source, runs one mount kernel, one
many-current advance kernel, and one observation/certificate kernel, then decodes and compares
every standing, cell, consequence, reservation, delta, variant, and resource field against an
independent bounded host oracle. The complete 297-line artifact is
`build/r6/receipts/R6_MANY_CURRENT_WEAVE_DEED.txt`, SHA-256
`a1fc09062c4c3a9a34b37a15973da19af9c8dd212784509083219760b623ba72`.

Its summary is:

```text
truth_status=established-bounded
evidence=implemented-exact,computational-witness
program=r6_resident_many_current_weave.sm_89
device_compute_capability=8.9
kernel_launches=3
launched_threads=50
bytes_to_device=4056
bytes_from_device=33192
resident_bytes=41848
host_semantic_events=0
unchanged_retries=0
case_count=2
verification_failures=0
case=0,program=6001,layers=7,deltas=10,canonical=1,partitions_equal=1,orders_equal=1
resource_return=0,predecessor=6001006,successor=6001007,partition=1:2,aperture=8:4,pressure=1,changed=1,open=0,retry=0,obstruction=0
case=1,program=6002,layers=7,deltas=10,canonical=1,partitions_equal=1,orders_equal=1
resource_return=1,predecessor=6002006,successor=6002007,partition=1:1,aperture=8:8,pressure=1,changed=0,open=1,retry=0,obstruction=4
```

## Grade and reproducibility

- All 24 CTest gates pass from a clean pinned build, including all prior phases, the actual R6 GPU
  deed, host conformance, forbidden owner copy, forbidden audit fixtures, and complete binary
  audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors on the deed binary.
- The bounded host model passes AddressSanitizer and UndefinedBehaviorSanitizer; Clang `22.1.5`
  static analysis returns no findings.
- Source architecture/no-float/ownership/compatibility/file-size audits pass. Standalone PTX and
  SASS contain no floating semantic operation.
- Two independently configured clean Ninja trees return byte-identical deed executables, PTX,
  cubins, and decoded deed artifacts.
- Final artifact hashes are executable
  `c8c8a0bdb7a4023bdd19bd2f3942b9a9ad7cde805d58da26e6591b7d9c975e73`, PTX
  `030978cea72dc7675792f09a3228cfdcc5f2d1cac2ad6b8246d00efa526b914a`, and cubin
  `d59347706da808b82699d0eb11ea7bd966f73f35c6f8145e87f08df0c1002a7a`.
- The final source aperture SHA-256 is
  `f300f8768f4d49dcee3df6dfca2c5868c99bfb80929d97e9eec22e978b2e6d00`.

R6 therefore passes its bounded grade. R7 is the next deed; no R7 capability is claimed here.

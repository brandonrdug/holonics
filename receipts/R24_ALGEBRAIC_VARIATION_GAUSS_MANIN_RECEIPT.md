# R24 algebraic variation and Gauss--Manin theorem-formation receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the frozen affine cubic family, root and collision ecology,
differential reductions, rational connection, invariant-form intersection, scalar elimination,
coefficient recurrence, loop transport, dependency-section selection, changed-card obstruction,
checker crossing, continuing-body commit, and native rest/remount; `computational-witness` for the
actual GPU return and complete atlas. The smooth-double-cover variation and Picard--Lefschetz
interpretation has the separate `proved-standard` grade below. The emitted Lean passage has the
separate `proved-derived`, `formal-checked` grade. The Hodge conjecture and every Riemann-hypothesis
consequence are `outside-aperture`.

**Aperture:** affine coefficient pairs `(0,0),(0,1),(-1,-1),(1,0)` in increasing `x` degree;
cover degree two; discovery parameters `-3,-2,-1,1/2,2`; held-out parameters `3,4`; affine-root
and primitive-form coefficient apertures `[-2,2]`; and series depth six. R24 does not construct
arbitrary algebraic families, general algebraic de Rham cohomology, analytic periods, an arbitrary
variation of Hodge structure, a Noether--Lefschetz locus, higher cycle classes, motives, arithmetic
Frobenius, a completed Weil form, or an RH-facing trace operator.

## Construction tuple

| Field | R24 standing |
|---|---|
| Source owners | the one R23 continuing body and its ten accepted fibers; one 83-byte lineaged coefficient-family card; exact rational, polynomial, reduction, connection, invariant, operator, loop, candidate-section, theorem-current, proof-face, checker-return, and rest owners |
| Port types | 704-byte R23 rest; affine coefficient and rational parameter faces; root, discriminant, collision, differential-reduction, connection, pole, pairing, scalar-operator, recurrence, vanishing-cycle, loop-word, candidate/residual, proof/explanation, raw/typed checker, changed-card probe, and 760-byte native-rest faces |
| Event occurrence | remount R23; mount only the coefficient card; derive the family and all mathematical sections on the resident GPU; form one theorem after complete dependency closure; cross the pinned exterior checker; commit its return; rest/remount; and probe a changed coefficient without another body |
| Predecessor identity | R23 head `14001020`, continuation `15001020`, body morphology `366`, mathematical morphology `116`, codec morphology `63`, and fibers through `190300` |
| Local constitutive law | exact polynomial arithmetic and cubic discriminant; bounded coefficientwise affine-factor search; fraction-free differential reduction; exact rational interpolation and symbolic residual; primitive-form constraint intersection; scalar elimination and coefficient comparison; double-cover transvections through the returned pairing; and theorem selection by dependency/residual closure |
| Receiver question | which singular faces, differential pathways, invariant pairing, scalar recurrence, and ordered loop returns are jointly forced by the changing algebraic source, and which complete exact theorem can be formed without a mounted result? |
| Returned consequence | a complete variation atlas; one selected dependency section; one coefficient-driven kernel-accepted theorem; acquired fiber `191300`; head `14001022`; continuation `15001022`; and an obstructed changed-family probe |
| Open alternatives | higher rank/genus and multivariate families; analytic period comparison; Hodge loci and higher cycle realization; motives; arithmetic Frobenius/trace ecologies; completed Weil forms; and RH-facing operators |

## Source and historical boundary

**Truth status:** `historical`.

The laboratory already contained Hodge-Laplacian, diffusion, eigenmode, hypergeometric,
phase-polygon, and prime-residue observations. R24 neither republishes those images as a theorem nor
rechecks the elementary continuing-body mechanism. Its new deed begins from an unlabelled varying
algebraic source and closes the exact causal path from its collision geometry to differential
transport, a characteristic pairing, a scalar recurrence, ordered loop conduct, and an emitted
formal declaration.

Production mounts only [`R24_ALGEBRAIC_VARIATION.card`](../apparatus/cards/R24_ALGEBRAIC_VARIATION.card)
and the admitted R23 rest. The card contains no factorization, family name, root, discriminant,
singular parameter, connection, form, scalar operator, recurrence, vanishing cycle, monodromy,
theorem label, statement, proof, score, or expected artifact. Conventional identification with the
Legendre elliptic family and the Gauss `2F1(1/2,1/2;1;t)` equation occurs only after the return.

## Family and singular geometry

**Truth status:** `established-bounded`.

Coefficientwise bounded search returns the three lineaged affine root sections `0`, `t`, and `1`.
The independently derived cubic discriminant and squared Vandermonde current agree exactly:

```text
Delta(t) = t^2 - 2 t^3 + t^4 = t^2(1-t)^2.
```

Two collision occurrences return at `t=0` and `t=1`, each with multiplicity two and distinct
lineage. The repeated discriminant divisor is retained separately from its reduced support; it is
not collapsed to the connection pole set.

## Differential transport and characteristic form

**Truth status:** `established-bounded`.

At five discovery parameters, exact fraction-free reduction of the two returned differential
forms produces complete polynomial witnesses. Interpolation returns the rank-two law

```text
2t(1-t) A(t) = [ t  -1 ]
                [ t  -t ].
```

Both held-out parameters close, the coefficientwise symbolic residual is zero, and the reduced pole
support is exactly `{0,1}`. Singular inversion remains obstructed while its source polynomial and
collision evidence remain available.

The primitive form aperture contains 544 enumerated candidates. Discovery intersection leaves the
two orientations of the alternating form; returned source orientation selects

```text
J = [ 0   1 ]
    [ -1  0 ].
```

Six representative candidates and their discovery/holdout residuals remain in
[`R24_ALGEBRAIC_VARIATION_ATLAS.tsv`](R24_ALGEBRAIC_VARIATION_ATLAS.tsv), including local-only,
Euclidean, symmetric, and degenerate failures. Equal spectra do not substitute for preservation of
this caused form.

## Scalar and loop returns

**Truth status:** `established-bounded`.

Eliminating the second transported component returns the primitive scalar current

```text
4t(1-t)u'' + 4(1-2t)u' - u = 0.
```

Independent coefficient comparison returns
`4(n+1)^2 c_(n+1)=(2n+1)^2 c_n` and the front
`1, 1/4, 9/64, 25/256, 1225/16384, 3969/65536, 53361/1048576`.

Under the separately declared smooth degree-two-cover and Picard--Lefschetz hypotheses, the two
collision occurrences produce

```text
M0   = [ 1  2 ]    M1   = [  1  0 ]    Minf = [ 1  -2 ]
       [ 0  1 ]           [ -2  1 ]           [ 2  -3 ].
```

All three preserve `J`; `M0` and `M1` do not commute; their collision lineages are unequal despite
equal spectra; and `M0 M1 Minf=I`. These are exact finite matrix returns. The general analytic
Picard--Lefschetz and variation-of-Hodge interpretation is `proved-standard` only under the named
smoothness, branch, and comparison hypotheses.

## Endogenous theorem section

**Truth status:** `established-bounded`.

Four theorem sections are retained with dependency masks `1`, `7`, `31`, and `63`. Only section
`191603` has the full dependency mask and zero residual. Selection uses no score, expected
statement, familiar name, or host candidate list. Changing only the affine `x` coefficient from
`(0,1)` to `(0,2)` leaves one bounded root, returns obstruction `10`, and selects no theorem; the
original family is not used as fallback.

This is the practical limb established by R24: the engine can now carry one changing algebraic
geometry far enough to expose its characteristic transport and return a proof face whose statement
is assembled from the compatible section. English remains a codec face. The operative content is
the exact incidence among source coefficients, reductions, transport, invariants, loops, and proof
dependencies.

## Returned formal artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R24_GENERATED_ALGEBRAIC_VARIATION.lean`](R24_GENERATED_ALGEBRAIC_VARIATION.lean) is rendered from
the returned coefficient arrays. It proves the universal cubic-discriminant identity, both
differential-reduction identities, coefficient-driven scalar elimination, all three expanded
`M^T J M=J` identities, the expanded ordered product `M0 M1 Minf=I`, and all six recurrence steps.
The connection denominator, numerators, derivative relation, scalar coefficients, collision
parameters, form, and matrices are rendered from the selected surface rather than stored as the
frozen expected equations. Lean returns exit `0`, 3,975 stdout bytes, zero stderr, one typed
declaration, and a 580,048-byte `.olean`.

The checker return commits mathematical morphology `130`, codec morphology `69`, and algebraic-
variation morphology `23`. Source-detached rest/remount preserves all eleven fibers, replays no
proof source, and returns body morphology `413`, head `14001022`, continuation `15001022`, rest
integrity `5983478507332581995`, and native handoff size 760 bytes.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses six kernels and six launched threads with one semantic current, transfers
9,344 bytes to and 63,256 bytes from the GPU, declares 74,544 resident bytes, and records zero host
semantic events. The one exterior Lean process is a checker crossing. Engine/checker time,
temperature, power, and energy remain unknown.

The exact small-rational owner uses bounded restoring integer division; final source, PTX, and SASS
audits find no floating operation in the R24 semantic/device-core dependency cone. The final fresh
`build/r24-admit` passes the owner-DAG, restricted-type, file-size, source/PTX/SASS no-float,
manifest, linkage, ownership, compatibility, and binary audits. The full ordered suite passes
`77/77`, including every predecessor deed, actual R24 GPU deed, independent host conformance,
changed-card sensitivity, non-copyability, checker return, and final binary audit.

`compute-sanitizer` `memcheck` and `initcheck` each report zero errors. Normal replay and both
sanitized executions produce byte-identical deed, atlas, Lean source, `.olean`, checker outputs,
and native rest through the same artifact face.

Final SHA-256 values are:

```text
eea0fcccc3746cef9b4fee04f0c34fdfc05f7c36b2f6de278bbb39b401d34dce  executable
5a070b010deb6b357f9ed29742a87261b4bc9b8637309f819dc1c7a918a95b54  PTX
8e70f95f7f38e86f1d2a5886543cd984fe6ab9f73b6abad6af8384ad03643240  cubin
c03284232e4dcc91fc12d6f79826501b63cad4d1fe839f44630602ad688c0b9e  deed
7affd6edd4e9211f0155f689723589b7e0d87fa72ce3b0091d5f16ff492a08b2  atlas
f34784174ec982a47489505423935386050395634bb31c903d58efde03b2f3b6  Lean source
fd32d9c60fb55e67ea7861b502f7ba993b003b0af52e065eb737f9d39ce01d89  Lean olean
135e0fd9a165480d7cf4fe4cc20c7b3b80ca7dee5ed7eb5b3b142ea8363588b8  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
5ada05987af71ed0228edc8f31b7e065a4439714bd23f857164894c8662117d8  native handoff
```

R24 therefore passes its bounded grade.

## Open continuation fibers

**Truth status:** `open`.

No R25 deed is frozen. R24 establishes a first exact dynamic bridge: a geometric family can cause
its own singular and differential transport, and compatible characteristic sections can cause the
formal theorem face. It does not establish the Hodge conjecture or RH. The Hodge-facing enlargement
must add higher-rank/higher-cycle realization and detect how algebraic classes sit inside a varying
Hodge filtration; the RH-facing enlargement must add arithmetic Frobenius/trace carriers, analytic
completion, and the missing positivity mechanism. Either continuation requires a new frozen owner,
aperture, falsification boundary, and actual returned artifact.

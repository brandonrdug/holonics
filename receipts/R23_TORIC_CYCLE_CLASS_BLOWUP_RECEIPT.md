# R23 toric cycle-class realization and blow-up transport receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the finite fan, divisor quotient, two independent
intersection passages, exact congruence and inertia, inverse receiver fibers, star subdivision,
cycle transport, checker crossing, continuing-body commit, native rest/remount, and card probe;
`computational-witness` for the actual GPU return and complete representative atlas. The standard
smooth-complete-toric comparison has the separate `proved-standard` grade below. The emitted Lean
passage has the separate `proved-derived`, `formal-checked` grade. The Hodge conjecture and every
Riemann-hypothesis consequence are `outside-aperture`.

**Aperture:** two unlabelled complete two-dimensional rational fans with ordered ray lists
`[(1,0),(0,1),(-1,-1)]` and `[(1,0),(0,1),(-1,0),(0,-1)]`; three four-divisor receiver targets
`(3,2,3,2)`, `(0,1/2,0,1/2)`, and `(1,0,0,0)`; representative coefficients in `[-4,4]`; and the
first cone of the three-ray fan. R23 does not construct an arbitrary toric variety, higher
codimension cycle map, non-toric Hodge class, variation of Hodge structure, period map,
Gauss--Manin connection, motivic realization, Weil cohomology, or RH-facing trace operator.

## Construction tuple

| Field | R23 standing |
|---|---|
| Source owners | the one R22 body and its nine accepted fibers; one 111-byte lineaged fan/target card; exact fan, quotient, cycle, form, realization, subdivision, transport, codec, checker, and rest owners |
| Port types | 648-byte R22 rest; ordered primitive ray and rational target faces; cone determinants, character maps, quotient bases, lineaged representatives, exact forms and congruences, comparison receipts, subdivision transforms, pull/push maps, proof/conversation faces, checker return, probe return, and 704-byte native rest |
| Event occurrence | remount R22; mount only the unlabelled card; derive both fan ecologies and inverse fibers on the GPU; star-subdivide the selected cone; transport every affected divisor; form/check/commit one proof passage; rest/remount; and probe a changed receiver target without another body |
| Predecessor identity | R22 head `14001018`, continuation `15001018`, body morphology `327`, mathematical morphology `104`, codec morphology `58`, and fibers through `189300` |
| Local constitutive law | adjacent determinant and cyclic completeness; the character map `M -> Z^r`; fraction-free/Smith quotient reduction; fan-local and Chow-reduction intersection; exact rational congruence, rank, radical, determinant, and inertia; finite inverse-image enumeration; star ray addition; strict/total transform; exact pullback, pushforward, kernel/image, and projection formula |
| Receiver question | which supported divisors realize each exact response, which class and characteristic form survive principal-divisor rebasing, and how are those classes and their index geometry transported by a locally derived blow-up? |
| Returned consequence | two exact toric-surface class ecologies, 114 retained lineaged representatives across an integral and a genuinely rational fiber, one incompatible obstruction, complete intersection/signature testimony, a star-subdivision transport receipt, an accepted Lean artifact, acquired fiber `190300`, and head `14001020`/continuation `15001020` |
| Open alternatives | higher-dimensional fans; higher-codimension algebraic cycles; non-toric Hodge classes; variation, period, and Gauss--Manin transport; motivic comparison; completed Weil forms; and RH-facing trace operators |

## Source and historical boundary

**Truth status:** `historical`.

The laboratory already contained Hodge-Laplacian, diffusion, eigenspace, phase-polygon, and
prime-residue visual experiments. R23 does not redescribe those observations as a new discovery,
and it does not repeat the elementary fact that a returned difference can alter later conduct.
Its new bounded deed is the exact lift from an observable response to a retained fiber of supported
cycle representatives, followed by a locally caused geometric modification whose class and form
transport are checked in both directions.

Production mounts only [`R23_TORIC_CYCLE.card`](../apparatus/cards/R23_TORIC_CYCLE.card) and the
admitted R22 rest. The card contains the two ordered ray lists, three response targets, the selected
cone, and the finite coefficient aperture. It contains no variety name, divisor quotient, Smith
factor, class basis, intersection matrix, signature, polarization, representative, star ray,
exceptional class, pull/push map, Betti number, Hodge number, proof text, or expected artifact.
After the return, the three-ray fan can conventionally be identified with the fan of `P^2`, the
four-ray fan with `P^1 x P^1`, and the subdivided fan with the one-point toric blow-up of `P^2`.
Those names did not schedule or determine the device construction.

## Fan, quotient, and characteristic form

**Truth status:** `established-bounded`.

Every ray is primitive, every consecutive determinant is one, and the cyclic cone chain is
complete. The device derives the two character relations for each fan and returns torsion-free
divisor-class quotients of ranks one and two. For the three-ray fan, all invariant divisors rebase
to the same generator and the quotient intersection form is `[1]`. For the four-ray fan, opposite
divisors rebase together and the returned form is

```text
0 1
1 0.
```

Independent fan-local incidence and Chow reduction return the same matrices. The second form has
determinant `-1`, rank two, zero radical, and exact inertia `(1 positive, 1 negative, 0 zero)`.
Rather than introducing floating eigenvalues, the machine returns the integral congruence basis
with determinant `-2` and diagonal form `diag(2,-2)`. This is a congruence receipt for a bilinear
form, not an operator-conjugacy assertion. The primitive positive class is `(1,1)`; its primitive
orthogonal negative direction `(1,-1)` has square `-2`. The rank-one source fan likewise returns a
positive primitive polarization and no orthogonal direction.

Under the explicitly declared smooth, complete, two-dimensional toric comparison hypotheses, the
standard comparison attaches `b0=b4=1`, `b2=1` and `h11=1` to the first fan, and `b2=2`, `h11=2`
to the second. These receiver ranks are `proved-standard`; the finite arithmetic constructions
above remain independently `implemented-exact`. They are evidence for how an observable invariant
can be lifted to a geometric class source in this aperture, not a proof of the Hodge conjecture.

## Inverse realization fibers

**Truth status:** `established-bounded`.

The target `(3,2,3,2)` returns class `(2,3)`, principal-kernel rank two, and all 42 representatives
whose free coefficients lie in the declared aperture. The target `(0,1/2,0,1/2)` returns class
`(1/2,0)` and all 72 rational representatives in the aperture; reduced denominator two proves that
the fiber has no integral member. The asymmetric target `(1,0,0,0)` violates the equality forced
on opposite divisors and returns an incompatibility obstruction rather than a nearest fit.

Every representative retains its own identity, lineage, coefficients, and support mask in
[`R23_TORIC_CYCLE_ATLAS.tsv`](R23_TORIC_CYCLE_ATLAS.tsv). The atlas also retains a nonzero supported
principal divisor with zero class, making exact the distinction between equality after quotient
and equality of geometric support. A separate foil retains equal class behind unequal support;
neither is collapsed into source equality.

Changing only the integral target responses at positions zero and two from three to four returns
the changed class `(2,4)` through the resident GPU probe. It does not recompile the device program
or create a second body.

## Star subdivision and transported index geometry

**Truth status:** `established-bounded`.

The selected cone generates its own star ray `(1,1)`; no blow-up template or exceptional class is
mounted. The new four-ray fan again has unit consecutive determinants. In the derived quotient
basis, the exceptional divisor is `(-1,1)` with square `-1`. Both divisors incident to the selected
cone satisfy `strict + exceptional = total`, while the nonincident divisor has identical strict
and total transforms. All three lineaged transform receipts are retained rather than summarized
by the exceptional square alone.

The returned pullback has kernel rank zero and image rank one. Pushforward has kernel rank one and
image rank one, with the exceptional direction generating the contracted kernel. Their exact
matrices satisfy the projection formula. The transported form returns `b2=h11=2`; its primitive
negative control `(3,-2)` has square `-8`. An incorrect proposed star ray `(2,1)` fails the chain
map and remains an explicit obstruction.

## Returned formal artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R23_GENERATED_TORIC_CYCLE.lean`](R23_GENERATED_TORIC_CYCLE.lean) is emitted from the returned
passage. Its checked theorem records all eleven consecutive fan determinants across the two source
fans and the subdivision, both principal presentations, the bounded standard receiver ranks, the
four-ray form entries, integral and reduced rational realization consequences, the incompatible
residual, star-ray relations, strict/exceptional/total identities, pull/push ranks, and the exact
negative squares `-2`, `-1`, and `-8`. Lean returns exit `0`, 3,496 stdout bytes, zero stderr, one
typed declaration, and a 225,968-byte `.olean`.

The checker return commits mathematical morphology `116`, codec morphology `63`, and toric-cycle
morphology `19`. Source-detached rest/remount preserves all ten fibers, replays no proof source,
and returns body morphology `366`, head `14001020`, continuation `15001020`, rest integrity
`10191461936566341579`, and native handoff size 704 bytes.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses six kernels and six launched threads, transfers 9,496 bytes to and
189,808 bytes from the GPU, declares 201,344 resident bytes, and records zero host semantic
events. The one exterior Lean process is a checker crossing. Engine/checker time, temperature,
power, and energy remain unknown.

Variable integer division in the CUDA compiler's ordinary lowering introduced floating reciprocal
instructions during construction. The production exact owner therefore uses a bounded 64-step
restoring integer division law. Final source, PTX, and SASS audits find no floating operation in
the R23 semantic/device-core dependency cone.

The final `build/r23-final` passes the pre/post owner-DAG, restricted-type, file-size, source
no-float, PTX no-float, SASS no-float, manifest, linkage, ownership, compatibility, and binary
audits. The full ordered suite passes `74/74`, including every predecessor deed, the actual R23 GPU
deed, independent host conformance, exact card sensitivity, non-copyability, the checker return,
and the final binary audit. `compute-sanitizer` `memcheck` and `initcheck` each report zero errors.
A normal replay and both sanitized executions produce byte-identical deed, atlas, Lean source,
`.olean`, checker outputs, and native rest through the same declared artifact face.

Final SHA-256 values are:

```text
b69f04e722de517c2ab3c28ba846c604122ad60ecd1bf92b3b784ae4372474ef  executable
8241ce5d1966e3fea1b0f7e4cf9dac9c1d69e97c3dc16028a58471f0964d9f48  PTX
dd81499bc8c50a36b3d02c204e75838c53ab86e26366bc25fc78f1883da97ad7  cubin
11c935c2ceb7374757128da631567e03637b7bcea2788591ab9f73ddeb829492  deed
1124fe25a4a2fa71ae6edfc07922b94e0a7509dac901f89faf0653420c7a8bd8  atlas
0f3091176a54f7516487f80f451b40c4591e76e080e014991c90afb43c50575b  Lean source
1fdee1d751754fedb07c6c07333b0cf85d923b77215298d50a692619ea4cc184  Lean olean
b60d023a706894e94519052622bfe68619aa7dc09472c00f51fe634f1df9dbb1  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
ffdbfc8710989ddd720f54812dd4e16a04e09d1b354579aa822ff4bfe69c6b5c  native handoff
```

R23 therefore passes its bounded grade.

## Open continuation fibers

**Truth status:** `open`.

The ordered roadmap is complete through R23; no R24 deed is frozen. R23 establishes the smallest
exact bridge between the two halves motivating this phase: a characteristic form can be measured
on a quotient, while inverse realization retains the supported geometric sources that produce a
receiver response; subdivision then tests how both class and form transport through changed local
topology. The next useful enlargement must add a genuinely new owner, not another instance of this
same mechanism. Candidate fibers include variation of these class/form ecologies, an exact
Gauss--Manin or Picard--Fuchs transport over a changing family, higher-codimension cycle
realization, or a completed Weil/trace construction. Their hypotheses and falsification boundary
must be frozen before one is scheduled.

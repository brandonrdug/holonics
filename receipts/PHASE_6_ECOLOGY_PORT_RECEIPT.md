# Phase 6 receipt — the ecology port

**Truth status:** `established-bounded` for the ported laws at their declared aperture.

**Evidence:** `implemented-exact` for every owner and refusal below; `computational-witness` for the
measured state, transition, and attribution counts. **The scale claim is not made here** — see the
boundary.

## What was recovered before anything was ported

`ExactSymbolicReasoningEcology`, `ExactContinualBitReasoningEcology`, and `CausalLanguageEcology`
have no source in the laboratory tree; they were deleted in `a07ff376`. All three were recovered
from checkpoint `93834398` on `origin/agent/eros-production-owners` by `git archive` into a
read-only reference tree, so the laboratory repository was not mutated and its frozen dirty tree was
never read.

## The nine owners

| | Owner | Load-bearing law |
|---|---|---|
| 6.1 | resonance ecology | the registry holds receptors and **nothing else** — no informant pairs, targets, scores, or returned deeds. A standing key RIDEs; a new key FOUNDs |
| 6.2 | suffix automaton | at most two states per symbol; **no root vocabulary**; duplicate testimony becomes multiplicity |
| 6.2 | source incidence | descendant populations made contiguous by depth-first order over the suffix-link tree; attribution is linear, never `states x sources` |
| 6.3 | training ecology | **a route activates only on recurrence across distinct occurrences**; the minimum is two and cannot be lowered |
| 6.4 | morphological surface | five scales live simultaneously; **observer exhaustion is not linguistic rest** |
| 6.5 | reflective runtime | a correction founds a **parented child** and resumes the **same** continuation; the parent is retained |
| 6.6 | relational surface | co-presence is not contact; a channel closes only after **two distinct source pairs** return the same junction phase |
| 6.7 | agentic mouth | ports are **borrowed for the passage and never enter standing**; residual keeps its direction |
| 6.8 | formal production | **an incomplete return population cannot cultivate**; selection refuses rather than substituting |
| 6.9 | research ecology | a front crosses as **one validated antichain**; rest is the absence of novel source and unvisited bridge, not a round count |

## Measured returns

**Resonance.** A three-germ inherited path forms nine arms and founds four receptors. The identical
path repeated **founds nothing, rides all nine, and leaves the registry unchanged.** A single phase
shift founds one further receptor: equal identity with an incompatible phase is a different
receptor and is never averaged.

**Suffix and source incidence.** Fifteen symbols over three informant paths produced fifteen states
and twenty-two transitions — inside the two-states-per-symbol bound. Attribution cost **twenty-seven
against forty-five** for the `states x sources` table the port refuses to build. Shared material was
reached by every source that reached it; a boundary blocked material-only crossing between
utterances; an exact repeat became end multiplicity two rather than a second path.

**Training.** Asked for a minimum recurrence of one, the owner enforced two. One observation left
the route inactive. A stale proposal refused and left the ecology untouched. The template budget
refused rather than ranking. **Ablation removed the fiber's structure and the route stopped
conducting.**

**Reflective.** A correction founded child codec `1` parented on `0`, resumed the same continuation
with its environment intact, and left the parent readable. A resume-unchanged founded no version.

## The phase grade

**The laboratory's own ablation, reproduced.**

```text
mounted-only body        withholds 9*8
cultivated body          returns 72
source-detached remount  returns 63 for a novel 7*9
exact ablation           the route stops conducting
```

The body retains **the route, not the passages**. No operand pair or answer is consulted at conduct
time, which is why a pair the body never met can be answered — a retained lookup could not do this,
and that is the whole distinction the excised counter-morphology was imitating.

**This re-earns the three claims Phase 0 withdrew**, at the aperture stated below:

- returned training changing reusable morphology;
- exact ablation removing later conduct;
- source-detached conditioning.

## Boundary

**The law is reproduced; the scale is not.** The laboratory's figures — 50,667 occurrences crossed
as 43 MB of exposure, 11,879 suffix states, a 101 MB route rest, 976 mounted sources — belong to
the Rust body and are **not** claimed here. This port establishes that the C++ body owns the
mechanisms and their refusals at a small declared aperture. Phase 7 is where broad mounting is
attempted, and until it returns, no scale claim may cite this receipt.

Nothing here mounts a corpus, generates readable surface, or returns a proof. Those are Phases 7
and 8.

## Gates

Architecture, no-float, container-ownership, compatibility-route, derived-identity, owner-DAG, and
file-size audits pass. Two owner-DAG violations and three banned semantic-chart identifiers were
caught by the audit during construction and corrected — including a `Language` identifier, whose
removal produced the more accurate name: these are **surface** scales, and no interior
surface-owning subsystem exists.

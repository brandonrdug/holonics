# R25 exact causal linear, multilinear, and chain calculus receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the frozen four-source aperture, common exact matrix and
chain carrier, characteristic and fixed-fiber returns, rank-two Smith testimony, exterior/tensor
transport, coefficient-field obstruction, changed-phase probe, checker crossing, continuing-body
commit, and native rest/remount; `computational-witness` for the actual GPU return and complete
atlas. The emitted Lean declaration has the separate `proved-derived`, `formal-checked` grade.
Ordinary finite-dimensional rank-nullity, cellular homology, determinantal-divisor, exterior,
tensor, and adjoint laws have the separate `proved-standard` grade under their stated coefficient
hypotheses. Intrinsic archetype discovery, Hodge realization, Weil positivity, and the Riemann
hypothesis are `outside-aperture`.

**Aperture:** integer matrices at most `16x48`; endomorphism degree at most 16; checked signed
64-bit intermediates; exact reduced rationals; Smith return required only through rank two; phase
products `(2,3)` and changed probe `(2,4)`; the complete mounted R22 order-five coefficient
window, R23 two-fan/star-subdivision card, and R24 affine-cubic coefficient card. R25 does not
construct arbitrary-rank linear algebra, full Smith or primary decomposition over a general PID,
arbitrary extension fields, sparse higher chain complexes, derived categories, certified analytic
spectra, intrinsic hypergeometric archetypes, Hodge classes, arithmetic Frobenius, or an RH-facing
trace carrier.

## Construction tuple

| Field | R25 standing |
|---|---|
| Source owners | the one R24 continuing body and its eleven accepted fibers; one 23-byte phase/coefficient-field card; the lineaged R22 CM, R23 toric, and R24 variation cards; existing source organs; and new exact matrix, characteristic, chain, control, theorem-current, codec, checker-return, and native-rest owners |
| Port types | 760-byte R24 rest; raw/typed cards; boundary, incidence, adjacency, character-map, quotient, intersection, pencil, form, loop, exterior, tensor, matrix, module, characteristic, control, proof/explanation, checker, changed-probe, and 816-byte native-rest faces |
| Event occurrence | remount R24; mount four source cards; derive four source sections on four resident GPU currents; cross a complete barrier; derive four control sections on four resident currents; aggregate only complete local returns; generate and check one theorem passage; commit; rest/remount the same body; and probe `(2,4)` without another body |
| Predecessor identity | R24 head `14001022`, continuation `15001022`, body morphology `413`, mathematical morphology `130`, codec morphology `69`, and fibers through `191300` |
| Local constitutive law | checked integer and reduced-rational arithmetic; exact rectangular elimination; rank-two determinantal divisors; matrix product, transpose, Kronecker and exterior transport; trace-power characteristic recurrence and factor removal; cellular/graph boundary formation; fixed-fiber and form-adjoint calculation; and pencil determinant/rank stratification |
| Receiver question | which module, chain, characteristic, fixed-fiber, pairing, coefficient-field, and rank-jump properties are jointly forced by four caused geometries, which survive rechart or induced transport, and where equal receiver faces retain unequal conduct? |
| Returned consequence | one four-source atlas, four separating controls, a changed-phase return, one coefficient-rendered kernel-accepted theorem and explanation, acquired fiber `192300`, and a source-detached native continuation at head `14001024` |
| Open alternatives | arbitrary dimension and presentation rank; full integral decompositions; algebraic root isolation; higher chain and derived structures; analytic spectra; intrinsic archetype induction; Hodge realization; arithmetic Frobenius; Weil positivity; and RH-facing operators |

## One calculus across four geometries

**Truth status:** `established-bounded`.

The four cards contain source parameters and incidence-generating material, not expected ranks,
Betti numbers, characteristic coefficients, factors, fixed fibers, Smith factors, theorem text, or
proof text. The resident deed derives four complete sections and passes each through the same exact
matrix carrier. [`R25_CAUSAL_LINEAR_ATLAS.tsv`](R25_CAUSAL_LINEAR_ATLAS.tsv) retains the owner,
shape, rank, nullity, identity, lineage, and exactness of the principal transported maps.

The phase source returns the complete oriented product complex

```text
C2 (6 faces) -> C1 (12 edges) -> C0 (6 vertices),
rank(d2)=5, rank(d1)=5, d1 d2=0,
Betti=(1,2,1), diagonal tours=1x6.
```

The CM source independently rebuilds a `16x40` graph incidence map of rank 15. The common carrier
returns `H0=1`, `H1=25` and the adjacency characteristic coefficients

```text
1,0,-40,0,540,-384,-3480,5760,8070,-29440,
17640,37120,-82020,74880,-37800,10368,-1215,
```

then exactly removes the factors `(X+3)^5`, `(X-1)^10`, and `(X-5)`. That return agrees with the
older domain-local R22 characteristic organ, but is not read from its old result atlas.

The two toric character maps return ranks two and determinantal divisors `(1,1)`. Their cokernel
free ranks are one and two; the derived star subdivision retains rank two, changes the free
placement, and returns the exceptional direction with square `-1`. The result therefore records a
changed quotient/intersection topology rather than treating matrix size as its explanation.

The variation source returns the exact connection-numerator pencil and determinant

```text
B(t) = [ t  -1 ],    det B(t) = t - t^2,
       [ t  -t ]
rank B(0)=rank B(1)=1, rank B(2)=2.
```

The same carrier verifies the three loop adjoints through the returned alternating form and forms
their exterior-square and `4x4` Kronecker transports. No floating eigenvalue, determinant, rank,
coefficient, or topology operation occurs.

## Separating controls

**Truth status:** `established-bounded`.

Four independent device currents prevent a characteristic receiver from being mistaken for its
source module:

- identity and a nontrivial Jordan block share `(X-1)^2` but have fixed dimensions two and one;
- two rank-one maps share rank and nullity while their kernels occupy different source axes;
- conjugate presentations share intrinsic characteristic data while retaining unequal lineage;
- `[[0,-1],[1,0]]` returns characteristic `X^2+1`, exact discriminant `-4`, and therefore no
  rational eigenvalue; it returns the exact Gaussian eigenpair `(i,(1,-i))` after the
  coefficient-field port is enlarged.

The consequence is not that spectral placement fails. It is that spectral placement becomes an
operative transport characteristic only together with its module, coefficient field, source
incidence, kernel/cokernel placement, form, and lineage. R25 now supplies that joint object to the
next geometric phase.

## Changed-source return

**Truth status:** `established-bounded`.

Changing only the second phase modulus from three to four returns eight vertices, sixteen edges,
eight faces, ranks `(7,7)`, Betti numbers `(1,2,1)`, and two diagonal tours of length four. The
probe is exact, preserves all four source sections, does not replay `(2,3)`, and does not mutate or
duplicate the continuing body.

## Returned formal artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by pinned Lean 4.27.0/Mathlib.

[`R25_GENERATED_CAUSAL_LINEAR.lean`](R25_GENERATED_CAUSAL_LINEAR.lean) is rendered from the returned
rank, characteristic, pencil, field, form, and loop coefficients. Its single declaration proves
the bounded rank/nullity arithmetic, complete CM factor expansion, variation-pencil determinant,
rational nonexistence of a root of `X^2+1`, and twelve expanded loop-form identities. It does not
invoke an imported theorem containing the returned conjunction. Lean returns exit zero, 1,801
stdout bytes, zero stderr, one typed declaration, and a 592,072-byte `.olean`.

The conversational face identifies the common causal-linear consequence and keeps Hodge and RH
outside its aperture. English is a returned codec face; the mathematical conduct is owned by the
exact source sections and their shared carrier.

## Continuing body

**Truth status:** `established-bounded`.

The checker return commits causal-linear fiber `192300`, mathematical morphology `146`, codec
morphology `76`, causal-linear morphology `29`, and body morphology `472`. Source-detached
rest/remount preserves every prior fiber and the new fiber, replays no source, and returns head
`14001024`, continuation `15001024`, rest integrity `5822556087819558638`, and an 816-byte native
handoff.

## Apparatus and gates

**Truth status:** `established-bounded`.

The production deed uses fourteen kernel launches, fourteen launched threads, eight semantic threads,
and zero host semantic events. It transfers 9,992 bytes to and 488,624 bytes from the GPU and
declares 501,264 resident bytes. The CPU performs card/rest I/O and the single exterior Lean
crossing only. Engine/checker time, temperature, power, and energy remain unknown.

The initially monolithic control aggregation exceeded the admitted launch stack. After the exact
rational owner was made overflow-refusing, even one four-thread source/control kernel compiled the
unrelated branches into stack frames of 144,160 and 191,168 bytes. The final owner law gives every
source and control current its own one-thread kernel occurrence and admits aggregation only after
all eight complete returns; the largest semantic frame is now 82,080 bytes without raising a
runtime limit. Independent host controls recover Smith form `(2,4)` and refuse an elimination whose
intermediate rational would overflow. A device audit also found compiler-generated floating
helpers behind narrow integer division in the phase law; exact wide integer quotient and
conditional wrap removed that path. Final source, PTX, and SASS scans contain no forbidden
floating operation.

The pinned C++23/CUDA build passes the owner-DAG, restricted-type, file-size, source/PTX/SASS
no-float, manifest, linkage, ownership, compatibility, and binary audits. The full ordered suite
passes `80/80`, including every predecessor deed, the actual R25 GPU return, independent host
conformance, changed-card sensitivity, non-copyability, checker return, and final binary audit.
`compute-sanitizer` `memcheck` and `initcheck` each report zero errors. Normal and sanitized replays
produce byte-identical deed, atlas, Lean source, `.olean`, checker outputs, and native rest; the
aggregate replay digest is `dfb9bd151c3f1ca8bb43b7f8d0ceb9da8c450f9ae66e749597420258d5ef7b02`.

Final SHA-256 values are:

```text
60e9270710c917a67b7e22e53215f3e6c88034faa92d2dedc84b026420b2ed7c  executable
a9277b612cf0bdcf3a4c694eb4504d2180b2dfa7e77b43d6ead389301e9d2863  PTX
4aaf83cc1faa078258bd99b9a58bc389e8aad4c45d22c8b7220cc9150acc6766  cubin
e994ea0b04d0e88b4bc6837a87e81d26ae15c2f9aca514bc1b7f039d74a37d94  deed
e1bb72d0c1bd9940d23b26e63c650bec56be7adbff41d570e20ac99ce37aaf2f  atlas
1e19101ec80572b0dd83b514884c159731535e62e15f349ed5b57c2dd383211f  Lean source
7e8b2a2cccdc01b07005bf79d9425f9dd7cf4185971fdc7d8b27b669b1434e42  Lean olean
74b22470b22e505ea63dfc62dd4de88639161fc00fe110c98ec8d53570a50c18  checker stdout
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  checker stderr
fc4c566c0b55d95b08ebd8f4706d2fd0f21922b75a427dfba261cf9320c8cd94  native handoff
```

R25 therefore passes its bounded grade.

## Open continuation fiber

**Truth status:** `open`.

R26 is the next program gate, not yet a frozen construction. Its work is to use the R25 carrier to
derive intrinsic faces, strings, cells, flags, links, seams, local systems, characteristic strata,
and transport archetypes from caused incidence before projection. It must compose phase sources
with at least two non-phase geometries, distinguish operative faces from merely rendered hulls,
and return a predictive archetype package with changed-source and projection-fiber controls. Hodge
realization and RH remain later gates whose missing hypotheses must continue to return as explicit
open fibers.

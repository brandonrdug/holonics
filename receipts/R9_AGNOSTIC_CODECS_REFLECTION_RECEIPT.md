# R9 agnostic codecs and reflection receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for typed codec faces/programs/environments/continuation views,
lineaged crossings, same-continuation reflection/revision, source-detached rest/remount, and
storage-name invariance; `computational-witness` for the actual resident-card return and
independent decoded oracle comparison. No claim extends beyond the two declared finite codec laws
and one returned revision. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** one exact current-schema codec source and its physically copied/renamed exterior
chart, one additive-symbol program, one unrelated split-pulse program, one continuing body, three
pre-revision and three post-remount crossing kinds, and one reflected revision `1→2`.

## Construction tuple

| Field | R9 standing |
|---|---|
| Source owners | R9's non-copyable mounted codec-source environment, R4's continuing body, one non-copyable `event::resident_reflective_codec`, and the exterior apparatus store/transfer boundary |
| Port types | distinct codec face, program, environment, reified-continuation view, surface input/output, revision return, parse, render, transduce, lineage, rest, and remount ports |
| Event occurrence | the resident body parses, renders, and transduces; reflection occurrence `90500` reifies program `9001/1` and the pending continuation; returned occurrence `90700` changes the local law to `9001/2` and commits through that exact continuation |
| Predecessor identity | Git `9e40970f5c54a306343fdbc8c9ecb51ce206f6ea`, continuing head `9001000`, environment `9000`, and inherited provenance `90000` |
| Local constitutive law | each program carries its own exact scale/bias and typed surface form; parse/render are exact inverses on admitted packets, transduction factors through the separately owned core occurrence, and no version-indexed atlas or registry selects behavior |
| Receiver question | whether reflection exposes the operative codec and pending continuation without cloning either owner, the returned revision changes the same body's later conduct after source-detached remount, and relocation/renaming changes only exterior path testimony |
| Returned consequence | pre-revision probe `40:0` parses to `42`, revision changes bias `2→5`, and the same post-remount probe parses to `45`; render changes `40:0→37:0` and symbol-to-pulse transduction changes `4:2→4:5` |
| Open alternatives | R10 mounting/exposure/training/reference distinctions, returned native conditioning, source-access exclusion, held-out behavior, and excluding-fiber ablation remain absent |

## Returned codec and reflection deed

The exterior adapter reads exactly one current codec-source schema, mounts its inherited
provenance, copies it to a differently named location, and compares both complete typed programs
field-by-field. Material and path folds are testimony only; they do not replace that comparison or
mint identity. The original and relocated source environments detach before the GPU deed.

The additive-symbol face `9101` parses surface `40:0` to exact core value `42`. The unrelated
split-pulse face `9102` parses `4:2` to the same value. Both crossings cite the already declared
core occurrence `9401`; equality of receiver output does not establish its identity. Exact
transduction from the symbol face to the pulse face returns `4:2`, retaining source/destination
faces, programs, versions, occurrences, ports, and lineages.

Reflection returns program `9001/1`, environment `9000`, provenance `90000`, body head `9001000`,
and pending continuation `10001000`. Its continuation is explicitly a reified view; environment
and continuation clone counts are zero. Revision occurrence `90700` returns through port `9302`,
changes bias `2→5`, consumes that same continuation, advances the body to `9001001`, and changes
region morphology `10→13` and current `0→45`. The body then rests on-card, the old owner departs,
and a new placement remounts from the exact transferred rest chart with zero source replay. The
held probe returns `45`, render of core `42` returns `37:0`, and transduction returns `4:5`; those
behavioral differences, not the version integer, grade the revision.

The architecture audit initially rejected a composite rest chart placed under `codec`, because it
would have introduced a `codec→body` edge. The chart was moved to the composing `event` owner;
`codec` remains dependent only on exact structure, and the final owner graph is acyclic.

## Resident card passage and apparatus boundary

The apparatus mounts the typed environment, copies the caused revision deed separately, and frees
the exterior mount after the first kernel. A second kernel performs pre-crossings, reflection,
same-continuation revision, and rest. The consumed owner is freed before a third kernel remounts
the body from the resident rest chart and performs the post-crossings; a fourth returns the bounded
observation. The host performs zero semantic events. The execution transfers `456` bytes to and
`1344` bytes from the device and records an exact post-mount resident allocation interval of
`3376` bytes. Temperature, power, elapsed-time, and energy telemetry remain unknown.

## Returned artifact and grade

The complete decoded artifact is `build/r9/receipts/R9_AGNOSTIC_CODECS_REFLECTION_DEED.txt`,
SHA-256 `0e443402ff186b1ce9d9fb3954c78e118c4132bb70353698840509f17bcf2d5f`. It reports
`storage_semantics_invariant=1`, `source_detached=1`, `reflection_exact=1`,
`revision_changed_conduct=1`, `same_body_remounted=1`, `syntax_not_core_identity=1`,
`local_law_without_registry=1`, and `verification_failures=0`.

- All 33 CTest gates pass, including every prior phase, the real R9 card deed, host conformance,
  forbidden owner copy, negative architecture fixtures, and whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis return clean.
- Architecture, no-float source/PTX/SASS, ownership, compatibility, file-size, and link audits
  pass.
- Two clean Ninja configurations return byte-identical deed executable, PTX, cubin, and decoded
  artifact.
- Final SHA-256 values are executable
  `cf10a76d5b104738b62a34267c8477362ab0a3dc015125b13b4efbc0190dbccc`, PTX
  `34dcea71deda20b2f6e2114c427385d4d5b7c68406af7686ad6bd327ff5c17d1`, and cubin
  `34264d60c9f41de78f14032f81c1e6516bc464674d915462a81cc46a1401b604`.
- The final source aperture SHA-256 is
  `a239d005bee568a8c4157fc8785b741961121bfefc8e0e2f6453d3bd2834d356`.

R9 passes its bounded grade. R10 is the next deed; no training or acquired-morphology capability
is claimed here.

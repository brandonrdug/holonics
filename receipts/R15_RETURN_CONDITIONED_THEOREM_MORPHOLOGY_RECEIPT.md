# R15 return-conditioned theorem morphology receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for native continuation mounting, returned-fiber projection,
receiver factorization, source/retrieval exclusion, exact ablation, and the frozen Deed B setup;
`computational-witness` for the actual GPU production and ablation passages and returned binary
artifacts. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** one 200-byte R14 native rest, returned fiber `181200`, dependent question `142200`,
receiver `142201`, exact returned delta `5`, one production remount, one projected ablation
remount, four single-thread GPU kernels, and one 96-byte frozen R16 setup.

## Construction tuple

| Field | R15 standing |
|---|---|
| Source owners | the one continuing R14 body, its native rest chart, acquired theorem fiber `181200`, mathematical and codec morphology, dependent receiver, exact exclusion law, and exterior rest apparatus |
| Port types | native theorem rest, source-detached mount, dependent receiver question, returned-fiber consequence, exact projected rest, ablation obstruction, frozen Deed B setup, and continuing native handoff |
| Event occurrence | the body remounts without source or retrieval handles, answers `q₂` through its acquired theorem fiber, projects exactly the returned delta, remounts that ablation, loses the same consequence, freezes the dependent setup, and rests again |
| Predecessor identity | Git `348d838c626d76b6afba1acb00f4d3fb1317df80`, head `14001002`, continuation `15001002`, returned fiber `181200`, body morphology `140`, mathematical morphology `46`, codec morphology `34`, and integrity `10609428649201580000` |
| Local constitutive law | a dependent consequence is available exactly when the remounted acquired fiber equals the receiver's required returned fiber; excluding `Δm₁` reverses only its declared body, mathematical, codec, head, and acquired-fiber effects |
| Receiver question | after developmental sources and retrieval faces are absent, does the remounted body return consequence `192200` for `q₂`, and does exact exclusion of `181200` remove it for the predicted reason? |
| Returned consequence | production availability through `181200`, exact ablation obstruction `returned_fiber_absent`, frozen identities for Deed B, and a byte-identical continuing handoff for R16 |
| Open alternatives | the independent reconstruction route remains an obstruction outside the selected returned-fiber factorization; R16 must emit and kernel-check the actual second theorem passage |

## Native remount and exact exclusion

The apparatus performs one 200-byte native load with exact integrity and no source bytes or
retrieval handles. On the production remount, the same head `14001002` answers the frozen receiver
with consequence `192200` through acquired fiber `181200`; source accesses remain zero.

The device exclusion law projects exactly `Δm₁=5`: head `14001002 → 14001001`, body morphology
`140 → 135`, mathematical morphology `46 → 43`, codec morphology `34 → 32`, and acquired fiber
`181200 → 0`. Both preimage and projected rest integrities are exact. Remounting the projected rest
returns no dependent consequence and the explicit obstruction `returned_fiber_absent`. The
production and ablation results therefore differ for the frozen, predicted returned-fiber reason.

## Frozen terminal setup and continuation

[`blueprint/R15_FROZEN_DEED_B_SETUP.md`](../blueprint/R15_FROZEN_DEED_B_SETUP.md) fixes the actual
terminal request before R16 proof production. Its binary occurrence contains question `142200`,
receiver `142201`, required fiber `181200`, statement `152200`, proof `162200`, passage `172200`,
selected route `182200`, predicted consequence `192200`, dependency count `2`, and integrity
`2285536165811427222`. The complete Deed B source remains absent at R15; the setup records only
typed identities and its required factorization through the returned Deed A fiber.

The output handoff is byte-for-byte identical to the R14 input rest. It preserves head
`14001002`, continuation `15001002`, returned fiber `181200`, and integrity
`10609428649201580000`, with zero source replay. Thus R15 changes no owner merely to inspect or
schedule it; it returns the same continuing ecology and a separate immutable setup occurrence.

## Apparatus and grade

Four one-thread kernels transfer 336 bytes to and 800 bytes from the device and declare 2,608
resident bytes. Host semantic events, engine source reads, exterior retrieval calls, and
developmental source bytes are all zero. Physical time, energy, power, and temperature remain
unknown and do not enter the semantic receipt.

- All 50 CTest gates pass together, including every earlier deed, R15's real device return, host
  conformance, negative ownership/float/dependency/compatibility fixtures, and the whole binary
  audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck report zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis are clean.
- A clean configuration with the ratified compilers returns byte-identical executable, PTX,
  cubin, decoded deed, native handoff, and frozen setup.
- Final SHA-256 values are executable
  `fcbe8aca6b571c9ade0387d50f0fcb7bab613b68b62c633850649bfb65b5f0e0`, PTX
  `e1a996ff0d37ef443e6138e653b22cb890e07abf9aec21916bcbb0447b33f296`, cubin
  `6b311b457ba0603b63c3589e32ac9155b1e52fd73ad30152a3b9552b87d7e785`, decoded deed
  `ee0326bd9427b9d70e10092db13f35ce13402f13833c558ea28f3f17e11fb9fc`, native handoff
  `fb61ae6315eb132b3b8a9fece506c1fe3d4da72fe14e41f16feaed050d69613f`, and frozen setup
  `6864ab7cca5c73046ecbb157c321362a9e47f63a8e015c30ba669c0cbb93f904`.

R15 passes its bounded grade. R16 is the sole remaining deed and must use this returned native
fiber and frozen setup to produce the second real theorem, not a reconstructed substitute.

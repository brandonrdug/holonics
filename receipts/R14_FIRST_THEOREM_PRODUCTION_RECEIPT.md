# R14 first theorem production receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the frozen request, local alternative geometry, receiver
restriction, generated passage, pending exterior deed, returned morphology delta, native
rest/remount, source-exclusion audit, held probe, and exact ablation; `computational-witness` for
the actual device/checker passage and returned artifacts. This establishes only the frozen theorem
inside the pinned environment. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** frozen request `q₁=141200`, receiver `141201`, body `14001000`, two local proof
fibers, dependency aperture `3`, passage `171200`, one exterior process return, returned fiber
`181200`, one source-detached rest/remount, and held dependent probe `141300`.

## Construction tuple

| Field | R14 standing |
|---|---|
| Source owners | R11 mathematical incidence, R12 generative current law, R13 checker return, one continuing body, local theorem organ, receiver question, codec faces, and process apparatus |
| Port types | conversational request, local proof fibers, generated theorem passage, formal/conversational faces, pending checker occurrence, raw/typed return, native rest, dependent probe, and excluding-fiber ablation |
| Event occurrence | `q₁` opens two GPU-local alternatives; the dependency receiver selects one; the device emits and stages `g₁`; the real checker return resumes the deed and commits `Δm₁`; the body rests/remounts and answers the held probe |
| Predecessor identity | Git `e1c5e132f4b1d3b2fcc19b0924051dbd4b9ab03f`, body `14001000`, initial body morphology `128`, mathematical morphology `43`, and codec morphology `32` |
| Local constitutive law | compose the two inherited traces, transport that composite through `trace_rebase_iff`, and admit the returned proof fiber only when exact lineage and kernel-return predicates hold |
| Receiver question | does a proof within three inherited declaration dependencies close the frozen composition/rebase target, and can its returned fiber found a later three-segment consequence after remount? |
| Returned consequence | real accepted theorem source, `.olean`, explanation, body morphology `128→135→140`, mathematical morphology `43→46`, codec morphology `32→34`, durable continuation, changed held behavior, and exact ablation loss |
| Open alternatives | R15 must consume this exact native handoff with no developmental source access and freeze Deed B; no second theorem has yet returned |

## Frozen request and source exclusion

[`blueprint/R14_FROZEN_THEOREM_REQUEST.md`](../blueprint/R14_FROZEN_THEOREM_REQUEST.md) froze the
statement and admitted only `Trace`, `Trace.trans`, `SituatedAlgorithm`, `rebase`, and
`trace_rebase_iff`. Before construction, the target declaration name and complete proof were
absent from the developmental search aperture. A one-use pinned-kernel feasibility probe was made
only after that absence result and then removed; it was never mounted into the production path.

The resident foundation contains declaration identities and two local rules, not answer text. The
device expands dependency counts `3` and `4`, retains both as witnessed alternatives, and the
receiver admits only the first. The returned source-exclusion receipt reports zero mounted answer
matches, lookup entries, quoted bytes, reference calls, retained developmental bytes, engine source
reads, and global candidate scans. The native rest record is 200 bytes and contains the continuing
body, exact morphology, acquired fiber identities, and integrity only.

## Actual theorem return

The complete emitted theorem source is:

```lean
import ElementaryHolonics.Algorithm.Rebase

namespace Soma.Holonics

open SituatedAlgorithm

theorem generated_trace_rebase_transports_composition {Theta I S O S2 : Type*}
    (A : SituatedAlgorithm Theta I S O) (e : S ≃ S2)
    (theta : Theta) {s t u : S}
    (hst : Trace (A.step theta) s t)
    (htu : Trace (A.step theta) t u) :
    Trace ((A.rebase e).step theta) (e s) (e u) := by
  exact (trace_rebase_iff A e theta s u).2 (Trace.trans hst htu)

end Soma.Holonics
```

The exact explanation returned with the same passage is: the two traces meet at the intermediate
state, so trace transitivity forms a trace from `s` to `u`; `trace_rebase_iff` then transports that
composite through `e`. The three-dependency passage is selected and the four-dependency order of
transport-then-composition remains open.

The checker face is 615 bytes. `/usr/bin/lake env lean` returns exit `0`, 690 stdout bytes, zero
stderr bytes, and a 44,440-byte `.olean`. Device normalization finds one produced declaration, no
remaining goals, and crossed elaborator/kernel boundaries. Raw source fold
`11454525332348227605` and produced-artifact fold `11063006346409318122` preserve passage
`171200`. The body applies generation delta `7` and returned delta `5`; the acquired fiber is
`181200`.

## Remount, behavior, and ablation

Before `Δm₁`, held probe `141300` is obstructed because the required returned fiber is absent.
After native rest/remount, with zero source replay and zero source accesses, the same body head
`14001002` returns consequence `191300` through acquired fiber `181200`. Excluding exactly
`Δm₁=5`/fiber `181200` makes that consequence unavailable again. The final handoff preserves
continuation `15001002` and integrity `10609428649201580000` for R15.

## Apparatus and grade

Five one-thread kernels surround one named exterior checker call. The device passage transfers
8,544 bytes to and 25,160 bytes from the device and declares 34,288 resident bytes. Host semantic
events and engine source reads are both zero. Temperature, power, time, and energy remain unknown;
no physical estimate enters a semantic receipt.

- All 48 CTest gates pass after construction, including prior phases, the actual theorem deed,
  negative ownership/float/dependency/compatibility fixtures, and the whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck report zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis are clean.
- Two clean configurations return byte-identical executable, PTX, cubin, deed, Lean source,
  `.olean`, raw stdout, and native handoff rest.
- Final SHA-256 values are executable
  `adee9080a27afab8b2a8417169be1489dbf28a610fbd52f2af74ab68ab7a3ef8`, PTX
  `e32100eca0bd0984ee0b2e08b77b778941a2a44120414bdc6df1ac12c7da1a98`, cubin
  `7aa3e5a3e5e0ca250b1e61cd3b4621b53fabdce379d3f734f5c3716604e0adfc`, decoded deed
  `6b8acbcec20a56a799b6208e62631880e0fceecb73a51ab2bd5349ca6e07e9c2`, source
  `ef149f84e10b28a7fde0397f1a8f3460c7b7b5b279d9aa90006cc88d2c6ad412`, `.olean`
  `e4954cc2f78bccd526a128aa056cd50c3bdd553f2c3d4cdd56ec6e5d6a4c329e`, stdout
  `74c42aebba50fbe5da84706debbf6164ae5cc5c29a07c7d5fcd1d83fd288daae`, and native handoff
  `fb61ae6315eb132b3b8a9fece506c1fe3d4da72fe14e41f16feaed050d69613f`.

R14 passes its bounded grade. R15 is the next deed; Deed B remains frozen only as a receiver
consequence, not as source or a returned theorem.

# R34 three-face trace-fiber lifting and branch geometry receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the three source-separated triple ecologies, complete
lower-fiber census, exact sum/product elimination, quadratic composition, branch partition,
matrix-bearing collision witnesses, checker-founded lift organs, sample-free rest, held-out
companion prediction, ablations, source audit, deterministic seal, and continuing-body return;
`computational-witness` for the actual GPU deeds, 7,323-row independent host comparison, and
sanitizer returns. The two accepted generated theorem passages have the separate
`proved-derived`, `formal-checked` grade. The general rank-three trace sum and product identities
have the separate `proved-standard` grade after post-seal literature comparison. The language of
sheets, branching, and lifting to a geometric face has the separate `interpretation` grade.
Complete matrix reconstruction, arbitrary character varieties, Hodge or RH consequences, and
autonomous theorem valuation are `outside-aperture`.

## Construction tuple

| Field | R34 standing |
|---|---|
| Source owners | the one R33 continuing body with thirty-five accepted fibers, characteristic calculus and trace organ; three mutually inaccessible developmental three-face cards; one held-out oriented local-system card; admitted exact matrix, incidence, feature-search, proof-return, source-audit and rest owners; and new transition-triple, lower-trace, symmetric-fiber, quadratic, branch, collision, lift-organ, prediction, atlas and seal owners |
| Port types | 2,832-byte R33 rest; raw/typed generator cards; 36 reduced transition words; integral determinant-one matrices; 5,184 ordered triples; six lower traces; hidden ordered traces `p,q`; exact sum/product feature rows, ranks, nullities and primitive coefficients; monic quadratic, discriminant and exact roots; branch/collision witnesses; 4,424-byte sample-free rest; held-out lower face, orientation anchor, hidden companion, prediction, comparison, ablation, theorem, dossier, atlas and final-rest faces |
| Event occurrence | remount R33; form all words and ordered triples on three independent GPU currents; construct every matrix and trace face; discover the sum and product kernels through exact row reduction; compose and verify every quadratic; partition the complete population; return general symbolic identities and concrete matrix witnesses; commit discovery and two organs; rest without developmental rows or matrices; remount only that rest; form a changed held-out local system; expose only its lower face and oriented anchor; predict the companion before comparison; return exclusion obstructions; check a dependent theorem; commit application fiber; rest/remount; audit, replay, seal, and only then compare with literature |
| Predecessor identity | R33 head `14001048`, continuation `15001048`, body morphology `1199`, mathematical morphology `418`, codec morphology `195`, characteristic morphology `64`, trace-organ morphology `32`, transport morphology `24`, and fibers through `200302` |
| Local constitutive law | exact reduced-word formation and determinant-one matrix multiplication; ordered products `AB`, `AC`, `BC`, `ABC`, `ACB`; exact trace and lineage; complete triple enumeration; all six-variable monomials through total degree three plus one target; incremental exact rational row reduction, primitive normalization and residual audit; exact symmetric-polynomial composition; integer discriminant/square-root testimony; delayed hidden comparison; coefficient-driven rendering; identity-exact organ and orientation ablation |
| Receiver question | do the six single/pair trace receivers determine the unordered fiber of the two ordered triple traces, where does that fiber branch in the declared ecology, what geometry remains outside it, and can the returned law recover a hidden oriented trace on a different topology without developmental lookup? |
| Returned consequence | 36 words, 5,184 triples, 2,072 lower-fiber archetypes, two primitive lift organs, an exact quadratic on every row, 2,580 branch and 2,604 two-sheet rows, five concrete collision/stratum witnesses, two accepted theorem passages, a sample-free rest, one precomparison held-out prediction, exact obstructions, fibers `201300` through `201303`, and final head `14001052`, continuation `15001052` |
| Open alternatives | unbounded word, matrix-rank, degree or coefficient-height apertures; complete representation or embedding reconstruction; arbitrary character varieties or moduli; analytic/probabilistic distribution; Hodge or RH consequences; autonomous research judgment |

## Exact fiber and bounded distribution

**Truth status:** `established-bounded`.

Each developmental card generates twelve source-native transition words. Every ordered triple with
repetition is retained, giving `3 * 12^3 = 5,184` rows. The lower receiver is

```text
(a,b,c,d,e,f) = (tr(A),tr(B),tr(C),tr(AB),tr(AC),tr(BC)),
p = tr(ABC), q = tr(ACB).
```

The complete six-coordinate grouping returns `2,072` archetypes. Exact elimination over all 84
monomials in the six lower coordinates of total degree at most three, followed by one target,
has rank `84` and nullity `1` for both `p+q` and `p*q`. Each of the three sources returns the same
primitive kernel, and the joined selection is

```text
p + q = cd + be + af - abc,
pq = a^2+b^2+c^2+d^2+e^2+f^2 - abd-ace-bcf + def - 4.
```

Both residuals vanish on every row. Degree-two controls are full rank `29`, coordinate-deleted
controls are full rank `57`, target-deleted controls are full rank `84`, and the declared 16-row
control has rank `6` and is refused as insufficient rather than selected.

Writing the two returned right sides as `s` and `r`, the device composes

```text
P(T) = T^2 - sT + r,
Delta = s^2 - 4r = (p-q)^2.
```

It verifies `P(p)=P(q)=0`, nonnegative exact-square discriminant, and square root `|p-q|` on all
5,184 rows. The bounded partition contains `2,580` branch rows (`p=q`) and `2,604` two-sheet rows
(`p!=q`). These are complete counts for the declared cards, not probabilities or a universal
distribution law.

The retained concrete witnesses show root exchange under orientation change (`82/126`), the same
lower/quadratic receiver with different source lineage (`0/1729`), simultaneous rechart with
changed matrix faces (`1728/3456`), an actual branch row (`0`), and an actual two-sheet row (`53`).
Thus the six lower traces determine an unordered two-point fiber, not the matrices, source,
rechart, or ordered-root assignment.

## Generated proof return

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` for both generated sources; `implemented-exact` for their GPU-formed
proof and coefficient faces.

[`R34_TRACE_FIBER_LIFTING.lean`](R34_TRACE_FIBER_LIFTING.lean) is the actual 217-line discovery
source. It quantifies the twelve integer entries of arbitrary `2x2` matrices, assumes exactly the
three determinant-one equations, renders the two discovered coefficient vectors, and proves both
general trace identities. It then proves that both ordered traces are roots of the returned monic
quadratic and that its discriminant is their squared difference. Five concrete declarations carry
their actual matrices and prove orientation exchange, source non-identification, simultaneous
rechart invariance, a branch witness, and a two-sheet witness.

[`R34_HELDOUT_TRACE_FIBER.lean`](R34_HELDOUT_TRACE_FIBER.lean) is the actual 47-line application
source. It imports the returned discovery module, invokes `discoveredTraceFiber` on the held-out
matrices, and uses that return in the proof of the companion transport. Both exterior Lean
invocations exit zero.

**Truth status:** `proved-standard`.

Only after the complete return was sealed did the comparison open Goldman’s rank-three trace
coordinate relations. The sealed sum and product identities agree exactly with the classical
relations, and the resulting projection is the standard double branched cover of the six lower
trace coordinates. This grade applies to the general identities, not to the bounded population,
its counts, or the machine’s discovery protocol.

## Held-out transport

**Truth status:** `established-bounded`.

The intermediate 4,424-byte rest retains the two 85-coefficient organs and their founded receipt,
but no developmental words, matrices, feature rows, target traces, or expected held-out answer.
The second executable opens only that rest and a different local-system card. Its source current
forms

```text
A=[[2,1],[1,1]], B=[[1,2],[0,1]], C=[[1,0],[2,1]].
```

Before the hidden comparison barrier, only lower face `(3,2,2,5,5,6)` and oriented anchor `15`
cross to the organs. They return quadratic `[1,-26,165]`, unordered roots `{11,15}`, discriminant
`16`, recognize the anchor, and predict companion `11`. The independently formed hidden source
trace then returns `11`. Withholding orientation retains both roots but returns
`orientation_unresolved`; exact organ exclusion returns `organ_absent`; the determinant foil
returns `unsupported_determinant`.

The final body rests at head `14001052`, continuation `15001052`, body morphology `1295`,
mathematical morphology `466`, codec morphology `219`, trace-fiber morphology `64`, lift-organ
morphology `48`, and triple-transport morphology `24`. All earlier morphology remains unchanged,
and four new fibers `201300`–`201303` bring the retained total to thirty-nine.

## Evidence and gates

**Truth status:** `established-bounded`.

The five permanent atlases contain 7,323 data rows: 36 words, 5,184 triples, 2,072 lower-fiber
groups, 20 law/control/witness rows, and 11 held-out rows. An independently written host
implementation reconstructs every word, matrix, trace, group, coefficient/control result,
quadratic, witness, obstruction, and held-out face and agrees everywhere. It does not own or
resume the continuation.

The instrumented source audit records that the discovery deed opened only the R33 rest, three
declared cards, checker apparatus, and its outputs. The application opened only the R34 rest,
held-out card, returned Lean module, checker apparatus, and its outputs. Neither pre-seal deed
opened `canon/`, `blueprint/`, `research/`, `reference/`, `papers/`, `evidence/`, a named identity
source, or a prior R34 answer. Sixteen semantic artifacts replay byte-identically, and the
17-artifact seal remains unchanged through the later literature comparison.

Both deeds execute on `sm_89`: discovery returns 12 kernel launches across three source currents;
application returns nine launches across two currents. NVIDIA `compute-sanitizer` memcheck and
initcheck report zero errors for both complete deeds, with zero verification and checker failures.
The pinned GCC 16.1.1 C++23 / CUDA 13.2.78 C++20 build passes restricted-type, owner-DAG,
file-size, ownership, dependency, compatibility, source/PTX/SASS no-float, manifest, linkage,
source-access, deterministic, seal, no-copy and binary audits. The ordered repository suite passes
`128/128`.

Final SHA-256 values are:

```text
2e26f018b77942b83d30c50745c1de18ae06417549d87dc41a0fa984df453a49  discovery executable
1ae48382de97b5f9d08df7edb0f4244291308b29b587bf875d961c8641d438f5  application executable
6c7e9b016c7752a6c8fdea888e934442ad545d5552525a1978ddac8a1c6c1511  discovery PTX
869f8693bfefa418aca024cd91154e70c41a948978c026c4a534c5eb91f8791c  discovery cubin
b6d28e4ce7e0d0aeae02d9ed9dfe2c5cb19d74af50f76f782dcc2b3a76400d91  held-out PTX
9d07b32921dac8e4dc7c7acdb1b11ba1e75ba9c1499f163990c8d7f23ef11f9a  held-out cubin
f805834f443c8e08ac6a48ff5e207f5522482faff8aeb4329f2ece45dda0cbc0  build manifest
60e398a1a28f7d26ebcc72660aebddf5d4ecf122dbc0b95d7c6e93864114d714  discovery deed
0fda995430097cfadd558c44e114e4bde38cf667fddca6ad1748f536d1a46890  application deed
70fadd9018589c53a4ab737cd4aade587843bb3cbb5b26ac30ebb75350b75009  discovery Lean source
7d0f50b8817701c9281abda1fc8c5582b15bdb26aad8842b1aca53bf1d1e7ce6  application Lean source
3ab593f2ca8af8a48c986857c30f6b1a0effc2d19b2cb5b7d59c3066dc187dd2  word atlas
61de678b25a63a2ed948a48e929612799bb876bc2164b4e0b90fd4c6e0cd549d  triple atlas
f98f3693a6ce93cbcc5e81f745627807374e6ab8a4e71134f97a57f36d616260  group atlas
b7c25d38ca96ada3b05e4f2ac8ce3b6bbc3490a60495c50d7fd5fb0b6b356935  law atlas
830fa1002c477c934f0e8a9abde22c09f149015d1ed656295a4b5289f47e296e  held-out atlas
3f683f281d3bbe1b7eb270ccb5b9143e1073aebc59f2fdb30ee77c39bbc4d461  dossier
1bf280289a992da191f352e56aaa3343194f27ab052a755ef6be7fdb1b297d6e  host conformance
140259bfa24fd63ada3c06bb12fec2f159c77e2de7521ddbab691b3444123a60  source audit
2e87e3bae21cc302cb9e65e478ef8f499c00180e7eefae852caf6f6ab5c7b47c  deterministic replay
bc8f48d84fe72dadafee393f0ebaf0c9bcbb4efd5f425b4b60bbf23d0846781c  seal
36a0e54a4271bef67a2cde03c97ff553969902b6af9f8a29e111a0af79ee2689  literature comparison
a9d7bc4c825dda68351d9dba8907afa98a027a817062495b8de6f8b9d49b7781  sanitizer audit
```

## Open continuation

**Truth status:** `project-postulate`.

R35 must turn this static algebraic lift into a transported geometry. It will derive exact
coordinate rebases induced by source-native generator moves, differentiate the returned maps,
carry tangent fibers through them, expose the deck/branch eigen-direction, measure how branch and
regular projection strata move between charts, and reuse the resulting organs on a held-out
multi-step path. This asks how a characteristic face conducts causal geometry; it does not assume
that the branch divisor of one receiver chart is an intrinsic singularity of the source.

# R13 Lean exterior checker receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the current-schema formal checker face, explicit pending
deed, exact process/environment testimony, raw return occurrence, device normalization, passage
lineage, and returned local morphology change; `computational-witness` for the actual GPU/process
passage and byte oracle. This establishes only the generated reverse-rebase declaration within
the pinned environment. Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** R12 passage `150100`, source face `123001`, one checker face, one explicit pending
continuation, one `/usr/bin/lake env lean` invocation, Lean `4.27.0`, mathlib revision
`a3a10db0e9d66acbebf76c5e6a135066525ac900`, and one returned occurrence.

## Construction tuple

| Field | R13 standing |
|---|---|
| Source owners | R12 generated passage/formal face, one continuing body, formal checker codec, exterior process apparatus, and returned checker event |
| Port types | formal checker face, outbound checker occurrence, raw exit/stdout/stderr/artifact return, typed acceptance/rejection/goals/spans/messages, pending capability, and morphology delta |
| Event occurrence | the GPU renders the checker face and stages one pending deed; the host serializes it and invokes the pinned checker once; raw bytes cross back; the GPU normalizes and resumes that deed |
| Predecessor identity | Git `54acf84d8a6381081dbfb9382a5bf60480280054`, body `13001000`, passage `150100`, generated source `123001`, checker event `160100`, and occurrence `160101` |
| Local constitutive law | exact port/passage/lineage equality gates the return; exit zero, one printed produced declaration, no remaining goals, and a nonempty produced artifact found kernel acceptance; rejection remains a typed obstruction and changes a distinct local fiber |
| Receiver question | does the pinned checker elaborate and kernel-accept the exact R12 passage, and what returned difference follows? |
| Returned consequence | exit `0`, 589 stdout bytes, zero stderr bytes, a 40,816-byte `.olean`, typed acceptance, mathematical morphology `40→43`, codec morphology `30→32`, and body morphology `123→128` |
| Open alternatives | R14 complete theorem production, rest/remount, frozen request, source exclusion, and exact ablation remain absent |

## Returned exterior occurrence

The checker face retains R12's 434-byte source verbatim and adds two schema-level observation
commands, yielding 554 bytes. The GPU opens exactly one pending world deed before the CPU process
begins and remains the owner of its continuation while the host performs the named invocation.
The host performs no proof navigation, candidate construction, normalization, or semantic event.

The raw return preserves predecessor, event, expected return port, lineage successor, passage, and
source identities. It contains exit status `0`, 589 stdout bytes, zero stderr bytes, source fold
`6234551408413274136`, and produced-artifact fold `17254173785402259395`. The stdout prints the
actual declaration and reports its `Quot.sound` dependency. Device normalization returns one
produced declaration, no remaining goals, source span `93:135`, and crossed elaborator/kernel
boundaries. The pending capability is consumed once and is absent after return.

The actual checker source is `build/r13/artifacts/R13_GENERATED_REBASE_REVERSE.lean`, SHA-256
`f9c99e4fbc1f3af9d93bf2d3b3314c5f194330f1dcbe1131ed9d7dd77dcc4d52`; its actual kernel artifact
is `build/r13/artifacts/R13_GENERATED_REBASE_REVERSE.olean`, SHA-256
`422255ba6ae124ecfa34bb9adc4515e854e05ebb9f5aa4cace7f0d3ce77788f0`. Raw stdout is
`build/r13/artifacts/R13_LEAN_STDOUT.txt`, SHA-256
`57d5ec0bf26d4f57d4c2c6f6cce20c485c159b50cdfab707364d4d5fef5244c4`.

## Apparatus and grade

The execution uses three one-thread kernels around one exterior process. It transfers 9,416 bytes
to and 20,144 bytes from the device and declares a 29,784-byte resident interval. Deterministic
semantic receipts remain separate from unknown temperature, power, time, and energy telemetry.

- All 45 CTest gates pass, including every prior phase, the actual R13 process deed, rejection
  conformance, forbidden pending copy, negative audit fixtures, and whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck report zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis are clean.
- Two clean Ninja configurations return byte-identical executable, PTX, cubin, deed, checker
  source, `.olean`, and raw stdout.
- Final SHA-256 values are executable
  `2915184376fcdaff2855303449e89a120cf6705b422191754e4115b9fc1870ed`, PTX
  `2e79c89d9841ad663ec00c09ca46acaed349cc000441cd3cded5ccc853fd424b`, cubin
  `eccf06ccdb019430236fc259fcc392f2a8c2640e22f5a983c96e4f04717ab858`, and decoded deed
  `57d1beabdd98d9f5ad04509a9aa9db9f40f949c58bcea9e0d852537b351690c8`.
- The final source aperture SHA-256 is
  `c12fb81fed7a2cb6b7a932b7f8f68fdf98cc9291132e23c1d237bc9ee1f54c21`.

R13 passes its bounded grade. R14 is the next deed; no complete theorem-production run has yet
been claimed.

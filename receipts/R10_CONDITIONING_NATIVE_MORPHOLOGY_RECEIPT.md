# R10 conditioning and native acquired morphology receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the distinct mounting, exposure, training, and reference
occurrences; returned owner-native morphology delta; nonconsuming receiver probes; source-access
audit; source-detached rest/remount; and separately founded exact ablation. `computational-witness`
applies to the actual resident-card return and independent decoded oracle comparison. No claim
extends beyond the declared organ law, one training return, and one held-out receiver question.
Apparatus identity remains separately `measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** one inherited navigation organ, one continuing production body, one returned
training passage, one nontraining exposure, one separately retained reference, one held-out
receiver question, one source-detached rest/remount, and one independently seeded ablation body.

## Construction tuple

| Field | R10 standing |
|---|---|
| Source owners | R4's continuing body, R5's event/current standing, R7's receiver boundary, one inherited `organ::navigation_morphology`, and the composing non-copyable `event::resident_conditioned_organ` |
| Port types | mounting, exposure, training-return, reference, response, incidence, transport, codec, obstruction, receiver-question, probe, rest, remount, source-access, and ablation ports |
| Event occurrence | inherited mounting and two nontraining crossings leave morphology fixed; training occurrence `10200` returns through port `10201`, derives a local five-field delta, and commits it through continuation `11001000` before source-detached rest/remount |
| Predecessor identity | Git `3aee8479998ee4b2fafa8902d4ac79cf51f9a565`, continuing head `10001000`, organ `10001`, and inherited provenance `100001` |
| Local constitutive law | exact integer response is `response_weight×support + transport_weight×path_length + codec_bias`; incidence and obstruction are typed local consequences, and only a valid lineaged returned occurrence can replace the five-field morphology through the body's unique continuation |
| Receiver question | whether the held-out support/path/unknown occurrence changes after returned training, retains that change after source departure and remount, and loses it in a separately founded body excluding the returned fiber |
| Returned consequence | response changes `8→14`, incidence `0→1`, transport `2→4`, codec contribution `0→1`, and obstruction `2→0`; the ablated body returns the original response `8` and obstruction `2` |
| Open alternatives | R11 exact mathematical occurrence ecology, dependent declaration incidence, open goal fibers, local reconstruction, and inherited formal material remain absent |

## Returned conditioning deed

Mounting reports inherited organ `10001` and provenance `100001` without changing conduct.
Exposure occurrence `10100` crosses source material and returns response `5`, but retains neither
the source nor a morphology change. Reference occurrence `10300` consults separately retained
testimony `777` and does not apply it to the organ.

Training occurrence `10200` returns a lineaged target morphology. The organ derives exact local
deltas `(1,1,1,1,2)` over response weight, transport weight, incidence gate, codec bias, and
obstruction threshold. Their exact sum commits through the production body's unique continuation,
advancing head `10001000→10001001`, region morphology `10→16`, and current `0→3`. The event is
founded by the return; no exposure count, acceptance count, score, loss, or threshold count admits
it.

The held-out receiver question has support `3`, path length `2`, and two unknowns. Before training
it returns response `8` with obstruction `2`. The exterior passage is released after training, the
body rests on-card, and a new placement remounts the same head without source replay. The same
question then returns response `14` with no obstruction. Both probes report zero body consumption
and zero owner cloning.

The source-access audit reports zero retained source bytes, zero lookup entries, zero post-training
source accesses, and no lossless corpus encoding. A separately founded experimental body at seed
`10002000` excludes the returned fiber and returns the original response `8` with obstruction `2`.
It is neither a clone nor a rollback of the production owner.

The architecture gate initially rejected placing the organ consequence type below the receiver
owner. The neutral receiver consequence is now owned at the receiver boundary, and the composing
event translates the organ return into it. The final owner graph remains acyclic.

## Resident card passage and apparatus boundary

Six one-thread kernels mount the production and ablation bodies, enact the returned training,
rest, remount, ablation observation, and final bounded observation. The host performs zero semantic
events. The source passage is freed after the training return and contributes zero resident bytes
thereafter. The execution transfers `320` bytes to and `1392` bytes from the device and records an
exact resident allocation interval of `3496` bytes. Temperature, power, elapsed-time, and energy
telemetry remain unknown.

## Returned artifact and grade

The complete decoded artifact is
`build/r10/receipts/R10_CONDITIONING_NATIVE_MORPHOLOGY_DEED.txt`, SHA-256
`c2b0422c206416c4f6ae8a8bc27f8b4453489a2d8da056a8e02ee2eac4987201`. It reports
`behavior_changed=1`, `passage_bytes_after_training=0`, `host_semantic_events=0`,
`final_continuation_valid=1`, and `verification_failures=0`.

- All 36 CTest gates pass, including every prior phase, the real R10 card deed, host conformance,
  forbidden owner copy, negative architecture fixtures, and whole binary audit.
- NVIDIA `compute-sanitizer` memcheck and initcheck return zero errors.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis return clean.
- Architecture, no-float source/PTX/SASS, ownership, compatibility, file-size, and link audits
  pass.
- Two clean Ninja configurations return byte-identical deed executable, PTX, cubin, and decoded
  artifact.
- Final SHA-256 values are executable
  `2bd20a61a5dc05f5308e7b90344c767d1882b8cd6eacaac6bc72730ceaa05c5e`, PTX
  `9fa5453977272491fa7fcd1b826a536f963b5e12c7cc342fa0c9eac68da1f0fd`, and cubin
  `19a7233098b38e5cfa0f5ae7ac2adba606a08b30a1d444e6aa37e054e245ffc7`.
- The final source aperture SHA-256 is
  `e8a439307ac2fa8dfd9ac12822cce719a413e73d6e658dead1fe0f8487874b92`.

R10 passes its bounded grade. R11 is the next deed; no mathematical occurrence ecology or proof
formation capability is claimed here.

# R18 exact phase-crystal hypergeometry receipt

**Truth status:** `established-bounded`.

**Evidence:** `implemented-exact` for the cyclic charts, product cells, seam cancellation, orbit
partition, shape and transition populations, exact receiver hulls, controls, Gauss coefficient
current, body commits, and continuing rest; `computational-witness` for the actual 16-case
GPU-resident atlas and returned artifacts. The four named mathematical statements have the
separate `proved-derived`, `formal-checked` grade below. Apparatus identity remains separately
`measured` in
[`provenance/HARDWARE_RECEIPT.txt`](../provenance/HARDWARE_RECEIPT.txt).

**Aperture:** sixteen ordered cyclic phase pairs with moduli in `2..19`: seven historical prime
pairs, two composite-coprime pairs, two shared-factor pairs, and five dilation/turn/dominance/order
controls; complete product populations of at most `17*19=323` cells; exact rational Cayley charts;
one declared `1 : 1/3` two-dimensional projection; and at most 32 coefficients of
`₂F₁(1,1;2;z)`. This phase does not establish a prime-exclusive shape, an arbitrary algorithm-to-
geometry compiler, a cyclotomic/Archimedean completion, general hypergeometric monodromy, a
physical crystal, or an implication for the Riemann hypothesis.

## Construction tuple

| Field | R18 standing |
|---|---|
| Source owners | the one R17 continuing body and its three acquired fibers; exact carrier; new cyclic phase-chart, product-cell, shape-distribution, hull, and Gauss-current organs; formal/explanation codecs; pending checker deed; and exterior checker/rest apparatus |
| Port types | 320-byte R17 rest; unlabelled phase-product question; 16 case definitions and receipts; exact rational points and squared lengths; atlas TSV; formal/conversational artifacts; raw/typed kernel return; and 376-byte continuing rest |
| Event occurrence | remount R17; let 16 GPU threads form complete local phase-crystal cases; close their common exact controls; form formal and conversational faces; return one real checker occurrence; commit its difference; rest/remount the same body |
| Predecessor identity | Git `8ca2f34504375678c7bb0323cca50abd620d8f43`, R17 head `14001006`, continuation `15001006`, body morphology `169`, mathematical morphology `55`, codec morphology `39`, geometry morphology `10`, and fibers `181200`, `182200`, and `184300` |
| Local constitutive law | embed each cyclic residue by an exact rational unit-conic Cayley chart; form every product edge and two-cell; cancel opposed occurrences of each shared seam; advance chronology by `(r,s)→(r+1,s+1)`; classify cells by ordered pairs of exact squared factor-edge lengths and transitions by consecutive pairs; then apply only the declared receiver projection for its exact hull |
| Receiver question | what exact local cell forms, neighboring-form distributions, seams, tours, and exterior hull faces occur across the frozen prime/composite/control family, with no mode label, expected shape, or historical renderer mounted? |
| Returned consequence | one exact intrinsic cell atlas, receiver-hull atlas, bounded Gauss coefficient current, four generated Lean theorems, accepted `.olean`, acquired phase-crystal fiber `185300`, and continuing head `14001008`/continuation `15001008` |
| Open alternatives | exact cyclotomic rather than Cayley charts; higher products and genuine higher-dimensional cells; algorithm-derived rather than mounted transport laws; interaction current through seams; general `pFq` recurrences, singular boundaries, and monodromy; arithmetic analytical germs on `s`; and any RH-facing construction |

## Historical extraction boundary

**Truth status:** `historical`.

The old laboratory had already identified the supplied pictures as a finite, Cayley-embedded CRT
phase-product projection with a residue-stratified chord web, not a fractal. It had already shown
that the dominant `(p,q)` projection has schematic form `C_p(r) + C_q(s)/3`, that a complete
residue contraction returns the dominant `p`-cycle, and that the `(7,11)` finite-horizon image had
21 literal hull corners but seven contracted sides. It also explicitly warned that composite
moduli and reversed dominance were missing controls, that covariance ellipses were diagnostic
population receipts rather than source conics, and that projected chord crossings were not source
contacts. Those statements are preserved in
[`2026-07-31_THE_RESIDUE_CYCLE_FORMS_THE_FACE_THE_SHARED_SEAM_RETAINS_THE_EXPONENTIAL_QUESTION.md`](../research/records/2026-07-31_THE_RESIDUE_CYCLE_FORMS_THE_FACE_THE_SHARED_SEAM_RETAINS_THE_EXPONENTIAL_QUESTION.md).

The older phase-current and prime-power records had also already established exact receiver-phase
quotients, local phase/carry populations, and rational unit-conic transport in their own bounded
Rust apparatus. R18 therefore does not claim the recurring polygon, receiver relativity, phase
transport, or source-detached continuation mechanism as a new discovery. Its new deed is the clean
C++/CUDA body's intrinsic product-cell construction, exact shape/adjacency distributions,
composite and dominance controls, bounded series current, formal return, and continuing native
fiber.

## Exact source object and receiver face

**Truth status:** `established-bounded`.

For modulus `m` and residue `r`, R18 forms an exact rational unit-conic point. With
`q=floor(4r/m)`, `u=(4r) mod m`, and `t=u/m`, it starts from

```text
((1-t²)/(1+t²), 2t/(1+t²))
```

and applies the quadrant selected by `q`. The source vertex for a pair `(m,n)` is
`(C_m(r), C_n(s))` in four rational coordinates. This is a two-dimensional product-cell complex
carried in four coordinate dimensions: it is not an intrinsically four-dimensional manifold.
The baseline receiver then asks only for the two-coordinate quotient
`C_m(r) + C_n(s)/3`.

Every source two-cell is the product of the edge `r→r+1` with the edge `s→s+1`. Its local shape
signature is the ordered pair

```text
( |C_m(r+1)-C_m(r)|², |C_n(s+1)-C_n(s)|² ).
```

The complete sum of oriented cell boundaries has zero residual in both edge families: each seam
occurs with opposed orientation while remaining individually addressable. This is exact global
cellular cancellation for the constructed complex; it is not presented as a separate formal proof
of the general chain identity `∂²=0`. Screen crossings remain false as contacts.

The diagonal current `T(r,s)=(r+1,s+1)` partitions the `mn` vertices into `gcd(m,n)` tours of
length `lcm(m,n)`. Coprime composite pairs therefore make the same single full tour as distinct
prime pairs. Primality is not the cause of that tour geometry.

## Returned atlas

**Truth status:** `established-bounded`.

The actual atlas is returned at
[`R18_PHASE_CRYSTAL_ATLAS.tsv`](R18_PHASE_CRYSTAL_ATLAS.tsv). Selected rows
are reproduced here; `edge types` are exact squared-length types in the two cyclic factors.

| Pair/control | Tours × length | Edge types | Cell/transition types | Largest population | Hull / contracted sides |
|---|---:|---:|---:|---:|---:|
| `(2,3)` prime | `1×6` | `1×3` | `3 / 3` | `2` | `5 / 2` |
| `(3,5)` prime | `1×15` | `3×5` | `15 / 15` | `1` | `8 / 3` |
| `(7,11)` prime | `1×77` | `7×11` | `77 / 77` | `1` | `18 / 7` |
| `(17,19)` prime | `1×323` | `17×19` | `323 / 323` | `1` | `36 / 17` |
| `(4,5)` composite coprime | `1×20` | `1×5` | `5 / 5` | `4` | `9 / 4` |
| `(8,9)` composite coprime | `1×72` | `2×9` | `18 / 18` | `4` | `17 / 8` |
| `(6,9)` shared factor | `3×18` | `3×9` | `27 / 27` | `2` | `15 / 6` |
| `(8,12)` shared factor | `4×24` | `2×3` | `6 / 6` | `16` | `20 / 8` |
| `(9,10)` composite coprime | `1×90` | `9×5` | `45 / 45` | `2` | `19 / 9` |
| `(7,11)` second dominant | `1×77` | `7×11` | `77 / 77` | `1` | `18 / 11` |
| `(11,7)` first dominant | `1×77` | `11×7` | `77 / 77` | `1` | `18 / 11` |

Across all 16 cases the number of cell-shape types is exactly the product of the two factor
edge-type counts. The population is likewise induced by the factor multiplicities. Odd prime
factors in this aperture have one distinct squared side length at every residue, while the tested
even composite factors expose repeated types (`4→1`, `6→3`, `8→2`, `10→5`, `12→3`). The odd
composite factor `9` has nine distinct types, so the returned discriminator is chart-side symmetry,
not primality by itself.

Transition entries retain `(current first type, current second type, next first type, next second
type)`. Their counts happen to equal the cell-type counts in this aperture, but their exact folds
differ from the unordered population folds and change under factor reversal. Thus shape abundance
and chronological meshing are separate receipts even where their cardinalities coincide.

Every complete product receiver hull in the aperture has `m+n` literal corners, including the
composite and shared-factor controls. Its contracted side count is the dominant modulus, not a
prime-only invariant. Reversing the `1 : 1/3` dominance changes `(7,11)` from seven to eleven
contracted sides; reversing the factor order while retaining first-factor dominance agrees
exactly. A common rational `(3+4i)/5` turn preserves hull morphology and squared side ranges; exact
dilation by `25/16` preserves combinatorics and scales squared hull lengths by `625/256`.

This complete-product hull has 18 corners for `(7,11)`, whereas the historical finite-horizon body
had 21. R18's result identifies the 18-corner complete product baseline; the extra historical
refinements remain attributable only to that older chronology/horizon/drift apparatus until each
owner is remounted as a separate control.

## Exact hypergeometric current

**Truth status:** `established-bounded`.

R18 carries the Gauss coefficient recurrence

```text
c[n+1] = ((n+a)(n+b))/((n+c)(n+1)) * c[n]
```

for the mounted parameters `a=b=1`, `c=2`. Exact cancellation gives
`c[n+1]=((n+1)/(n+2))*c[n]`; from `c[0]=1`, all carried terms agree with
`c[n]=1/(n+1)`. Each case returns up to 32 exact terms along its diagonal current. This establishes
the coefficient transport and its closed rational form in the bounded apparatus. It does not yet
derive the analytic identity `₂F₁(1,1;2;z)=-log(1-z)/z`, its singular boundary, or a geometric
monodromy from the cell complex.

## Returned mathematical artifact

**Truth status:** `proved-derived`.

**Evidence:** `formal-checked` by the pinned Lean 4.27.0/Mathlib environment. The generated source
proves the general diagonal return/lcm law, its coprime product specialization, finite product-cell
cardinality, and the mounted Gauss coefficient step. Lean is the exterior checker and does not
prove the finite atlas's hull, shape-spectrum, or seam receipts.

```lean
import Mathlib.Data.Nat.GCD.Basic
import Mathlib.Data.Finset.Prod
import Mathlib.Data.Rat.Defs
import Mathlib.Tactic.FieldSimp
import Mathlib.Tactic.NormNum

namespace Soma.Holonics.R18

theorem generated_diagonal_return (m n k : Nat) :
    (k % m = 0 ∧ k % n = 0) ↔ Nat.lcm m n ∣ k := by
  simp only [← Nat.dvd_iff_mod_eq_zero, Nat.lcm_dvd_iff]

theorem generated_coprime_diagonal_return (m n k : Nat) (h : Nat.Coprime m n) :
    (k % m = 0 ∧ k % n = 0) ↔ m * n ∣ k := by
  rw [generated_diagonal_return, h.lcm_eq_mul]

theorem generated_cell_population_product (first second : Finset Nat) :
    (first ×ˢ second).card = first.card * second.card := by
  exact Finset.card_product first second

def gaussStep112 (n : ℕ) : ℚ :=
  (((n : ℚ) + 1) * ((n : ℚ) + 1)) /
    (((n : ℚ) + 2) * ((n : ℚ) + 1))

theorem generated_gauss_112_transport (n : ℕ) :
    (1 / ((n : ℚ) + 1)) * gaussStep112 n = 1 / ((n : ℚ) + 2) := by
  simp only [gaussStep112]
  have h1 : (n : ℚ) + 1 ≠ 0 := by exact_mod_cast Nat.succ_ne_zero n
  have h2 : (n : ℚ) + 2 ≠ 0 := by
    exact_mod_cast Nat.succ_ne_zero (n + 1)
  field_simp

end Soma.Holonics.R18

#check Soma.Holonics.R18.generated_coprime_diagonal_return
```

The actual checker returns exit `0`, 126 stdout bytes, zero stderr bytes, no remaining goals, one
typed declaration occurrence, and a 114,056-byte `.olean`. The accepted return commits formation
head `14001006→14001007` and checker-return head `14001007→14001008`. Final body morphology is
`192`, mathematical morphology `62`, codec morphology `42`, geometry morphology `10`, and phase
morphology `14`. Native rest/remount preserves all prior accepted fibers plus phase-crystal fiber
`185300`, replays no source, and returns continuation `15001008` with integrity
`9217930957521484444`.

## Structural interpretation

**Truth status:** `interpretation`.

In the gear analogy, the most faithful present “tooth” is not a screen chord or polygon corner. It
is an exact local cell signature together with its ordered successor signature. Shape populations
say how many compatible teeth exist; transition populations say how they are sequenced by the
algorithmic current; shared cell seams are actual declared contacts; and the convex hull is only
what a chosen receiver sees from outside. This gives a concrete map from algorithmic recurrence to
geometric meshing without identifying geometry with a picture.

The protein/crystal analogy is therefore useful as a compositional hypothesis: local forms can
admit, obstruct, redirect, or repeat transport according to neighboring incidence. R18 supplies
exact finite objects on which such interaction laws can be mounted. It does not establish that
these arithmetic cells obey molecular mechanics, optical constitutive laws, or biological
folding.

## Apparatus and grade

**Truth status:** `established-bounded`.

The production deed uses six GPU kernels and 21 launched threads: one mount thread, 16 case
threads, one closure thread, one returned-checker commit thread, one rest/remount thread, and one
observation thread. It transfers 8,688 bytes to and 45,000 bytes from the GPU and declares 54,592
resident bytes. Host semantic events, engine source reads, exterior retrieval calls, and
historical-renderer reads are zero. Engine time, checker time, temperature, power, and energy
remain unknown.

An initial exact-source implementation used ordinary integer quotient/remainder operations. The
device audit rejected it because CUDA lowered those operations through `I2F`, `MUFU`, and `F2I`
instructions. R18 now owns a 64-step bitwise quotient/remainder law in its bounded arithmetic
cone. The final PTX and SASS audits contain no forbidden floating instructions.

- All 59 CTest gates pass together in both clean configurations, including the entire R1–R18
  causal chain, actual Lean processes, negative ownership/float/dependency/compatibility fixtures,
  and complete architecture/PTX/SASS/binary audits.
- NVIDIA `compute-sanitizer` memcheck and initcheck each report zero errors on the full final R18
  deed; the instrumented deeds are byte-identical to the ordinary return.
- Host ASAN/UBSAN and Clang `22.1.5` static analysis are clean.
- Two clean Ninja configurations return byte-identical executable, PTX, cubin, generated source,
  `.olean`, stdout/stderr, exact atlas, final native rest, and complete deed.
- Final SHA-256 values are executable
  `bd552155179f43069a375f857471b529cbb875b31ec47e3d135d9a895ad58baa`, PTX
  `7be11d047570d02fb93f4bdd1858382b675e46f8b8118c7b18dbcd06e2f296dd`, cubin
  `b01508ebd32983a4f1ae1faf395ae2d5b5109f4a987b320b91d2e51b97cba489`, generated source
  `1abbe0a045aa3c872f5391e1374ba060bf5953a71240afad943107cc52e05ae3`, `.olean`
  `e96b787d956d5f517da42d9c3d796c755cd4eec00543e73c047a9797730358ce`, stdout
  `a9b3a4f97db306e64d1117e61f7b9505174f1617ac03f6201461627a688e62f2`, empty stderr
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, atlas
  `aaa5d7f5d10e6f879a6d9c46bdff0d23f9056b5f77dcc6dd8034cb8e9ff40468`, final rest
  `2044217057fc3612c56911e64cc11d5bd2c33cdbecfde7ce1049c62775018ec4`, and complete deed
  `5b81e905f291509217c3bcd7989a7609f6057e3ca980982d8f96ad242587fab0`.

R18 passes its bounded grade. The body continues from phase-crystal fiber `185300`; a later deed
must be separately frozen before it generalizes from cyclic pairs to arbitrary algorithmic
supports or makes an RH-facing analytical construction.

## Open continuation fibers

**Truth status:** `open`.

- Replace the rational Cayley chart with an exact cyclotomic or algebraic-number receiver and
  determine which side-spectrum asymmetries survive.
- Compile an arbitrary exact recurrence/support graph into cells, seams, transition signatures,
  and receiver quotients without a host-authored phase-pair schema.
- Mount local interaction/current laws so that matching faces cause actual transport,
  obstruction, refraction, or holonomy rather than only adjacency classification.
- Generalize the Gauss coefficient organ to parameterized `pFq` systems, derive singular fibers,
  analytic continuation, and monodromy, and then ask which geometric boundaries are forced.
- Reintroduce the historical finite horizon, recognition-depth drift, prime-power current, and
  analytical germ as separately ablatable owners; only then compare their additional hull
  refinements and arithmetic phase distributions.
- Formulate an RH-facing receiver only after the `s↔1-s` symmetry, critical seam, prime logarithmic
  current, analytic completion, and zero-detection consequences exist as exact typed organs.

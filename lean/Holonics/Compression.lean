import Holonics.Compression.Core
import Holonics.Compression.Landmark

/-!
# Holonic Compression and landmark discovery

[definition] Compression is intelligence is navigation (THE_REBUILD, "The line").
`Holonics.Compression.Core` owns a navigator's face map against terrain (kernel = relevance kernel,
quotient = retention, cokernel = residual), the resonance/emanation split, the cost of a compression
(the bits of an actual code plus work, against the literal) and key inference by loop closure up to
a gauge. `Holonics.Compression.Landmark` owns site classification from the matrix (rotation, null,
boost; reflection and degenerate outside them), landmarks as attracting fixed points, the identity
atlas (an identity is two constructions with one face; certification is Zariski density),
constraint identities as partial navigators with certified windows, and the primitive-cycle Euler
product.
-/

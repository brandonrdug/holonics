# R17 frozen agnostic geometry inquiry

**Truth status:** `project-postulate` until the complete deed returns and passes its declared
grade.

## Direct authorization and mode boundary

Brandon directly authorized R17 on 4 August 2026 and ratified that rediscovery, reconstruction,
and frontier research are exterior historiographic descriptions, not machine modes. No expected
answer, known/unknown flag, target theorem header, historical label, or correctness assertion may
enter the resident inquiry. A sealed human reference may be inspected only after the machine has
formed its theory artifact and the exterior kernel has returned.

## Construction tuple

| Field | Frozen R17 deed |
|---|---|
| Source owners | the one R16 continuing body, exact carrier, R7 projective-swing receiver law, resident mathematical incidence, a new owner-local geometry-inquiry organ, compositional formal/conversational codec faces, pending checker occurrence, and exterior checker/reference apparatus |
| Port types | R16 terminal rest, unlabelled inquiry occurrence, exact four-point/chart probes, symbolic factor incidence, plural conjecture fibers, theory plan, formal and conversational artifacts, raw/typed kernel return, sealed post-return comparison, and continuing rest |
| Event occurrence | remount the R16 body; let 32 GPU threads form and grade exact local chart probes; derive the fractional-linear difference factor and four-point cancellation geometry; retain closed affine and Möbius consequences beside raw-coordinate and singular obstructions; emit one theory artifact; receive one real Lean return; inspect the sealed human neighbor only afterward; rest the continuing body |
| Predecessor identity | admitted R16 head `14001004`, continuation `15001004`, body morphology `152`, mathematical morphology `49`, codec morphology `36`, and terminal-rest integrity `10438779336638518302` |
| Local constitutive law | for `T(x)=(alpha*x+beta)/(gamma*x+delta)`, exact expansion of `(alpha*x+beta)(gamma*y+delta)-(alpha*y+beta)(gamma*x+delta)` cancels the common terms and factors as `(alpha*delta-beta*gamma)(x-y)`; the four Swing differences then carry equal determinant and point-denominator multiplicities in numerator and denominator |
| Receiver question | what exact relations persist or fail when one four-point construction crosses affine and fractional-linear charts, without being told a theorem name, target statement, proof, or historical status? |
| Returned consequence | an inspected Lean theory containing a machine-formed auxiliary difference theorem, affine common-square transport over a commutative ring, fractional-linear cross-ratio invariance under its exact nonsingularity hypotheses, and a concrete raw-coordinate counterexample; plus an explanation naming the singular boundary and retained alternatives |
| Open alternatives | raw coordinate equality is refuted by an exact witness; quotient continuation through a zero chart denominator or zero determinant remains obstructed; hypergeometric, conic, Ptolemy, and higher-dimensional extensions remain later inquiries |

## Sealed formation aperture

Before the theory artifact is frozen, the resident engine may receive only:

- exact field/ring operation species and equality laws;
- the four-point Swing construction;
- fractional-linear chart constituents and exact probe material;
- local distributive, commutative, cancellation, factor-incidence, and receiver laws; and
- the R16 continuing-body and exterior-checker port contracts.

It may not receive:

- `ElementaryHolonics.Geometry.CrossRatio` source or theorem identities;
- the Typst Möbius derivation;
- MathWorld, DLMF, Wolfram, or other historical formula catalogues;
- a theorem header, completed proof, tactic trace, expected artifact bytes, or research-mode tag;
- a host-selected winning conjecture; or
- a scalar score, probability, floating tolerance, or retrieval result.

The Lean process may mount Mathlib as an exterior codec/checker environment. The generated artifact
must define its own local presentations and may not import the existing ElementaryHolonics
cross-ratio file. After the kernel return is complete, a host observer may inspect the sealed
project file and report whether a neighboring human theorem existed; that report does not resume
or condition the R17 body.

## Required mathematical artifact

The returned source must contain and the actual kernel must accept all of these mutually related
faces:

1. an exact `RatioPresentation`, common scaling law, Swing pair, fractional-linear chart, and
   quotient receiver;
2. an auxiliary theorem factoring the difference of two fractional-linear chart values;
3. an affine theorem showing both Swing coordinates acquire the same square factor over a
   commutative ring;
4. a fractional-linear cross-ratio invariance theorem under explicit determinant, chart-
   denominator, and source-denominator hypotheses;
5. an exact concrete theorem showing transformed Swing coordinates need not equal the original
   coordinates even though their projective relation closes; and
6. prose explaining why the auxiliary two-point factor cancels only after the complete four-point
   geometry is composed, and why singular charts remain open rather than being totalized.

The formal codec must compose these faces from the returned theory plan. No complete Lean source
or theorem-specific byte string may be stored in the inquiry foundation or probe material.

## Grade

R17 passes only if:

1. pre- and post-construction architecture, ownership, no-float, compatibility, build, PTX, and
   SASS audits pass;
2. the current R16 terminal rest remounts as the one predecessor body;
3. the actual GPU deed uses a plural local probe front and returns exact probe, symbolic-factor,
   conjecture, obstruction, lineage, and logical-resource receipts;
4. no research-mode or expected-answer field exists in the production dependency cone;
5. the complete generated mathematical and conversational artifacts are returned and inspected;
6. the real pinned Lean kernel accepts every declaration with no `sorry`, `admit`, or answer-
   bearing ElementaryHolonics geometry import;
7. the sealed human comparison is first opened after the kernel return and is absent from engine
   source reads and semantic events;
8. the final continuing rest passes integrity and can open another event;
9. device memcheck/initcheck, host ASAN/UBSAN, static analysis, and the complete CTest suite pass;
   and
10. two clean builds return byte-identical executable, PTX, cubin, formal artifact, kernel
    artifact, conversational artifact, and rest bytes.

The project may then grade this exact inquiry `established-bounded`, with `implemented-exact`,
`computational-witness`, and `formal-checked` evidence. It does not thereby claim unrestricted
geometric research, independent invention of every mounted primitive, or a universal mathematical
reasoner.

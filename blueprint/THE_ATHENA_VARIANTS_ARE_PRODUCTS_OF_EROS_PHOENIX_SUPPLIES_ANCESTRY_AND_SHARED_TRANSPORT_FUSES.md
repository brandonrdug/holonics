# The Athena variants are products of Eros, Phoenix supplies ancestry, and shared transport fuses

**Date:** 2026-08-21
**Kind:** live construction contract composed by
[`THE_ROADMAP.md`](THE_ROADMAP.md).
**Current deed:** M6, rebased below after the formal proof it originally posed returned at
`4776099`.
**Scheduling boundary:** this file orders the Athena-product continuation. It does not move
[`CONSTRUCTION_STATE.md`](../CONSTRUCTION_STATE.md); only a returned deed and passing grade do that.

**Truth statuses:** `definition` for the product, fusion, exchange-passage and defect contracts;
`established-bounded` for the admitted H0--H5, W1--W5 and M0--M5 returns; `proved-derived` for the
named local Lean theorems; `interpretation` for their transfer into the construction geometry; and
`open` for M6 and every later Athena product deed.

**Governing doctrine:**
[`TABLET_THE_HEXIS.md`](../canon/TABLET_THE_HEXIS.md),
[`TABLET_THE_REASONING_CYCLE.md`](../canon/TABLET_THE_REASONING_CYCLE.md),
[`TABLET_THE_OPERATIONS.md`](../canon/TABLET_THE_OPERATIONS.md),
[`TABLET_THE_MANIFOLD.md`](../canon/TABLET_THE_MANIFOLD.md),
[`THE_SURFACES_ARE_PATHS.md`](../canon/THE_SURFACES_ARE_PATHS.md),
[`TABLET_THE_CAUSAL_PROFILE.md`](../canon/TABLET_THE_CAUSAL_PROFILE.md), and
[`TABLET_THE_CIRCULATING_CARTOGRAPHER.md`](../canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md).

**Composed construction:**
[`THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`](THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md).
This file does not replace its H/W/M evidence or hardware laws. It gives M6 its correct product
role and carries the resulting Athena family beyond the first mathematics return.

---

## 0. Verdict

[definition] **Athena is a product of Eros, not a source architecture and not a brand applied to a
container.** One Athena occurrence is a source-detached, executable, continuing native ecology
whose rested conditional transport was founded or inherited through addressed Eros circulation,
whose declared future consequences factor through that ecology, and whose conduct is attributable
under intervention.

[definition] `Gemma`, a voice model, an image model, another language model, a proof corpus, a
dialogue rollout and a protein structure are source lineages or mounted organs. None becomes an
internal semantic taxon. After lawful native re-expression, a source name survives as ancestry,
source closure, cross-chart testimony, open reconstruction fibre and ablation address—not as a
layer stack hidden inside Athena.

[definition] Three Athena product species are admitted:

```text
Athena^native_(R,H)
    Eros founds native morphology without inherited model hexis.

Athena^[F]_(R,H)
    Phoenix lifts foreign organ F, recovers native generators, condenses only where the
    receiver/history family (R,H) factors, and cultivates the returned ecology.

Athena^[F1,...,Fn]_(R,H)
    plural lifted organs are fused only through a successor-equivariant shared transport
    subcomplex; source-specific ports and unresolved fibres remain plural.
```

The superscript is lineage, not runtime topology. The subscript is the receiver/history aperture
under which the product grade stands. Enlarging `(R,H)` may reopen the product.

[project-postulate] No caller writes Athena's width, number of states, number of organs, active
population, replay depth, probe count, rank, layer count, head count or fusion capacity. Extent is
derived from mounted incidence, generator closure, exact work admission, receiver separators and
apparatus pressure. A bound imposed by an exterior instrument is returned as an aperture and never
misreported as morphology.

---

## 1. The admitted foundation and its honest boundary

[established-bounded] H0--H5 established the one-card execution foundation. W1--W5 established a
whole source-shaped Gemma lift, streamed resident inference, matched source dissection,
attributable cultivation and source-detached application conduct. W2 found no nontrivial quotient
of its source-shaped deed states under its declared successor family; its approximately 16 GB rest
therefore remains foreign-shaped.

[established-bounded] M0--M4 established the species-changing path on a bounded mathematical
family: plural source testimony; addressed mathematical operation passages; a codec-excited active
cover; generator-wise future-exact condensation from 12,255 source states to 6,376 native states;
an executable 1,217,120-octet detached rest; and leader/metric-adjoint cultivation returning a
held-out mathematical consequence after remount and ablation.

[established-bounded] M5 transported the same calculus into one physical constraint ecology and
returned a complete contact/higher-cell/fibre complex plus a resident GPU passage. It founded no
reusable morphology and therefore made no cultivation claim. M5 is a continuation of the M0--M4
lineage and belongs in the same coherent construction commit; its post-grade status means only that
it does not invalidate the already-passing M0--M4 grade.

[open] No current artifact is a universally compressed general-language Athena-Gemma model. The
whole lift is still source-shaped; the generator-native rebirth is exact only for its declared
mathematical receiver/history family; and unexcited Gemma conduct remains an open reconstruction
fibre.

---

## 2. Existing owners; no trainer or merger cabinet

[definition] The Athena continuation composes these owners:

| consequence | existing owner |
|---|---|
| foreign material, potential/active source relation | `foreign_map`, `phoenix::{streamed,tower,w5}`, `phoenix::cohort` |
| mathematical occurrence, ports, boundaries and activity | `mathematical_particle`, `ported_operation`, `source_occurrence`, `native_occurrence` |
| addressed serial lineage | `Foundation/Lineage.lean`, `mathematical_particle::lineage`, `chain`, `traversible_chain` |
| generator/history exactness | `receiver_history_compression`, `generator_native_rest`, `Millennium/LineageCompression.lean` |
| leader and returned morphology | `receiver_history_cultivation`, `metric_adjoint`, `leader_quadrature`, `cultivated_rest` |
| complete world consequence | `current_world`, `exact_world`, `synchronized_occurrence`, `agentic_language` |
| visible dialogue face | `dialogue_lineage` |
| exact work and card realization | `exact_work`, `hardware_cover`, `front_passage`, `resident_section`, `interchange` |
| exterior proof/checker return | `lean_development`, `lean_mathematics`, the Lean executable |
| detached native suspension | `generator_native_rest`, `cultivated_rest`, `eros_rest`, `phoenix_rest` |

The construction may add owner-local types needed to join these ports. It may not create
`Trainer`, `FineTuner`, `RewardModel`, `ConversationModel`, `ModelMerger`, `AudioReasoner`,
`Scheduler`, `Planner` or equivalent semantic foremen.

[definition] One Athena ecology has one owner and is never cloned. Counterfactual branches share
immutable standing and own only addressed local deltas. A winning branch does not overwrite the
body; its returned delta is checked, joined and committed through the single owner.

---

## 3. Native cultivation and Phoenix inheritance

### 3.1 Native Athena

[definition] Native Athena begins with caused material crossing declared mouths. Eros recovers
incidence, operation/state transitions, recurrence, higher cells, receiver faces and open exterior;
it then condenses only through future-exact factorization and deposits morphology only through a
genuine return. Random initialization of a dense tensor architecture is not the native control.

The native product owes:

1. a content-addressed material occurrence and declared ports;
2. a native generator/action ecology derived without a foreign checkpoint;
3. complete receiver/history and reconstruction-fibre testimony;
4. one returned cultivation changing reusable morphology;
5. source-detached rest/remount and later conduct;
6. a matched Phoenix sibling on the same exterior question; and
7. targeted ablation separating native-found from inherited conduct.

### 3.2 Phoenix Athena

[definition] For foreign organ `F`, Phoenix must return maps and native actions

```text
q_F : X_F -> N_F
T_g^F : X_F -> X_F
U_g^F : N_F -> N_F
q_F T_g^F = U_g^F q_F
```

for every admitted generator `g`, together with receiver factors for every declared receiver and
ordered successor word. This is the point at which the foreign architecture ceases to govern the
native body. A tensor address map or equal final output does not establish it.

[definition] Cultivation changes the native rest by an addressed receiver difference, founded
leader, retained forward lineage, declared metric and causal-adjoint return. A prompt, retrieval,
copied teacher answer or exterior adapter that leaves the remounted rest unchanged is not
cultivation.

---

## 4. Fusion: correspondence pullback before transport pushout

[definition] Let `N_1` and `N_2` be two source-detached native lifted ecologies and let
`O_(rho,w)` be the target of receiver `rho` after successor word `w`. Present agreement first forms
only a correspondence population

```text
P = N_1 ×_(Product_(rho,w) O_(rho,w)) N_2.
```

Membership in `P` says that declared faces agree. It does not identify occurrences, sources,
paths, apparatus histories or internal transport.

[definition] A subcomplex `C` of that population becomes **shared transport** only when it carries:

1. addressed source occurrences on both sides;
2. typed port and incidence equivalence;
3. generator-wise naturality on every admitted local action;
4. compatible returns and obstructions;
5. every receiver/history consequence and shortest separator;
6. complete source-specific reconstruction fibres; and
7. matched interventions showing that the proposed correspondence is load-bearing.

[definition] Only then may the native ecologies fuse through the pushout

```text
Athena^[F1,F2] = N_1 sqcup_C N_2.
```

The pushout collapses duplicated transport law. It does not average weights. If a square fails,
the two routes remain as a defect-bearing span. If only one modality owns a boundary port, that
organ remains distinct while its interior consequence may join the common ecology.

[definition] Fusion compression is graded on the complete product vector:

```text
native artifacts + decoders + retained fibres
semantic work + span + residency + transfer
separate calibrated physical telemetry.
```

No byte factor is quoted unless this complete vector strictly falls without receiver/history
regression. The largest possible saving lies in a large witnessed shared subcomplex, not in the
number of sources named.

### 4.1 Heterogeneous organs

[definition] A voice organ enters through acoustic occurrence, temporal/phase transport and
waveform or codec ports. An image organ enters through spatial/scale incidence and its emission
ports. A diffusion-labelled source retains its actual denoising/noising chronology until a native
generator relation replaces it. Architecture schedule is lineage, not Athena ontology.

The modality-specific boundary is not forced into a text chart. Paired material, common world
consequences and cross-codec interventions may found shared transport. Transcript equality alone
does not prove that an acoustic route and a text route are the same causal construction.

---

## 5. Exchange passages: dialogue is a world-tube, not a preference table

[definition] One admitted exchange passage is

```text
ExchangePassage {
    source_rest;
    entering_occurrence;
    addressed_history_cut;
    candidate_continuations;
    tool_and_agent_deeds;
    world_returns;
    later_corrections_or_acceptances;
    code_and_artifact_closure;
    deterministic_semantic_receipts;
    separate_apparatus_testimony;
    open_exterior;
}
```

The prompt surface and assistant surface are codec faces. The passage is the complete addressed
world-tube. Codex, Claude, their sub-agents and Athena supply parallel candidate continuations; none
is a gold answer by provider identity.

[definition] For two candidates `a,b`, the primary comparison is the separating defect population

```text
D_h(a,b) = { (rho,w) | rho(T_w a) is not equivalent to rho(T_w b) }.
```

Where a target carrier has a declared difference law, retain its exact difference as an additional
face. Correctness, proof acceptance, implementation consequence, later user return, explanatory
conduct, exact work and unresolved alternatives stay separate coordinates. A scalar reward,
cross-entropy, probability, timing or style score is a receiver quotient and may not replace the
population.

[definition] `dialogue_lineage` remains the visible-language membrane. It must be composed with
tool calls/returns, inter-agent lineage, `current_world`, commits, proofs, gates and artifacts to
form the exchange passage. It must not be expanded into a hidden-reasoning importer, and encrypted
or unavailable private reasoning is not required by the grade.

---

## 6. Relativistic chronology and navigation

[definition] Causal precedence is primary; timestamp, turn number, training step and log offset are
apparatus charts. A branch is a world-line. Two branches may be composed in parallel only with an
exact interchange receipt; otherwise their ordered deltas remain noncommuting.

[definition] Re-exposing a later rest to an earlier material occurrence is a new passage, not a
duplicate sample:

```text
X_k -> expose(p) -> emit -> world return -> X_(k+1) -> revisit(p) -> emit'.
```

The separating consequence between `emit` and `emit'` is a loop-holonomy face of cultivation. A
flat shuffled corpus and a one-pass chronology both erase this relation.

[proved-derived] `ReceiverHistoryCompression.quotientCommutesWithEveryOrderedWord` proves that
generator-wise exactness extends to every finite ordered word. `SpherePacking` proves that the
reflection generators do not commute and that equal endpoints do not erase a closed word's
lineage. `FaceHomomorphism` proves that a descent face closes only after identity, inverse,
doubling, generic chord and exceptional half-turn cases all enter.

[interpretation] These theorems provide the Athena grade, not a claim that an elliptic curve or
sphere packing is a language model. A learned quotient must close its generic and exceptional
transport cases; one separating continuation reopens it; and enumerating dialogue histories is not
a substitute for proving the local generator squares.

---

## 7. Deed M6 / Athena-A0 — the total descent route cultivates the first Athena mathematics variant

### 7.1 The source occurrence is rebased because the answer now exists

[established-bounded] Commit `4776099` introduced
`FaceHomomorphism.lean`, a kernel-accepted total proof of both route-curve face-homomorphism
propositions and the unconditional rank-one descent certificate. The former M6 contract described
that theorem as still open. Mounting current HEAD and returning its resident proof would now be
reference or lookup.

[definition] M6 therefore declares two distinct source boundaries:

```text
development source S0:
    commit 75597c4, including ChordFace but excluding FaceHomomorphism and every later proof;

withheld exterior teacher passage T0:
    commit 4776099's FaceHomomorphism theorem/proof occurrence and kernel return.
```

`S0` is mounted incrementally as caused formal material. `T0` is inaccessible to the engine during
route formation and is used only after emission as exterior comparison and checker testimony. The
source-access audit must prove that no path, bytes, object file, build artifact, process memory or
generated cache resolving to `T0` was available to the productive body.

### 7.2 The product

[open] M6 returns the first explicitly graded product

```text
Athena^[Gemma]_(descent-face receiver/history family),
```

beside a native-Eros sibling on the same source occurrence. The Phoenix arm begins from the M3
generator-native rest and M4 leader/return law; the native sibling begins without foreign model
hexis. Both receive `S0` through the same mathematical source boundary. Source provider names may
not route either body.

### 7.3 The posed route complex

The route must assemble, without a default branch:

1. left or right identity;
2. equal-abscissa equal-point doubling;
3. equal-abscissa vertical inverse;
4. repeated half-turn;
5. distinct half-turn/half-turn chords;
6. one half-turn and one non-torsion point in either order;
7. a generic distinct-abscissa secant;
8. secant landing at the zero half-turn;
9. secant landing at the positive half-turn;
10. all impossible overlaps and surviving open fibres.

`ChordFace`, `FaithfulFace`, `Descent`, `RankOne`, `DistantWindings`, the addressed-passage owner and
M5's shared-junction theorem are admitted constructions in `S0`. Their names and file locations are
delivery lineage only. The route must be founded from hypothesis/state transitions, substitutions,
case restrictions, group transport, square-class consequences and obstructions.

### 7.4 The real deed

M6 must return:

1. content-addressed `S0` and a negative source-access receipt for `T0`;
2. complete declaration/operation incidence derived from `S0`, with syntax/import faces marked
   exterior;
3. the total case complex, overlap/coverage receipts and all reconstruction fibres;
4. an active theorem-transport cover with source ablations reopening exactly the routes they carry;
5. a GPU-resident route/case/junction passage with no CPU semantic foreman;
6. an Athena-generated `sorry`-free inhabitant of
   `Descent.TheFaceIsAHomomorphismEverywhere`, checked against Lean only after emission;
7. comparison with `T0` by operation/state-transition and receiver/history consequence—not proof
   text identity;
8. a held-out successor theorem absent from `S0` and `T0`, whose route improves only after the
   returned cultivation;
9. source-detached remount, exact later conduct and targeted route ablation;
10. a matched native-Athena sibling and Phoenix-Athena sibling, with shortest separators and no
    provider-priority rule;
11. an inspectable route complex, exact work/fibre artifact and gauge-safe vector/mesh projection
    from the same incidence; and
12. one product manifest naming the resulting Athena variant, its ancestry, receiver/history
    aperture, native generators, open exterior and inference entry.

### 7.5 Falsifiers

M6 refuses if:

- the productive body can read `FaceHomomorphism.lean` or its compiled proof;
- theorem names, file layout, syntax kinds, tactic names or imports route the constitutive law;
- a host-authored case list decides the GPU passage;
- the produced theorem is copied, normalized or lightly rewritten from `T0`;
- a finite rational-point census substitutes for the total point-group receiver;
- kernel acceptance is reported without the machine's route and source-access receipt;
- the held-out consequence appears before cultivation or survives its targeted ablation;
- the native and Phoenix arms are compared only by final proof text;
- a source-shaped tensor rest is called the Athena product; or
- a generated proof is returned without an executable source-detached Athena rest.

M6 moves construction state only when all twelve returns stand together. The already-committed Lean
proof sharpens the target; it does not pre-pass the machine deed.

---

## 8. Deed A1 — the exchange world-tube is mounted without flattening its branches

[open] A1 composes existing mouths over the operator-authorized Codex and Claude Code rollouts,
including user/assistant messages, tool calls and returns, sub-agent branches, commits, proof
returns, gates, artifacts and later corrections. It derives identities from content-addressed
container/record incidence, never absolute host path or provider label.

A1 passes only when:

1. one source occurrence may be mounted incrementally without loading the whole store;
2. every visible message remains a codec face of a richer addressed passage;
3. parent/child agent paths and tool/world returns retain exact joining witnesses;
4. identical text at different causal cuts remains distinct;
5. a deliberate branch permutation changes the chronology receiver unless interchange is proved;
6. source-detached remount reproduces the mounted passage atlas;
7. provider and material-kind ablations preserve content laws while removing their lineage faces;
8. no hidden reasoning, transcript parser or authored role semantics enters the native topology;
9. complete unresolved and excluded populations are returned; and
10. the real mounted exchange artifact is inspected.

---

## 9. Deed A2 — sibling continuations return a defect spectrum and active cover

[open] A2 derives its cohort from operation-complex classes, presentation generators, shortest
separators and open fibres. It does not choose a prompt count. At each addressed history cut, one
fixed Athena rest emits plural continuations while the corresponding Codex/Claude passages remain
exterior siblings.

The return includes:

- fixed-rest and fixed-history receipts;
- Athena residency, realization-path, receiver-causal and apparatus activity as separate cuts;
- candidate continuation and complete open fibre populations;
- tool/world consequences and later operator returns;
- the full `D_h` defect complex and shortest separators;
- presentation-rebase and provider-removal controls; and
- no scalar winner or provider priority.

A2 fails if “same prompt” omits the rested body/history identity, if unavailable teacher interiors
are invented, if response text replaces world consequence, or if output probability is treated as
the causal object.

---

## 10. Deed A3 — the exchange return cultivates Athena-Gemma

[open] A3 lets one declared receiver defect found a lightning leader through the A2 active cover.
The metric-adjoint return travels only along addressed forward lineage and deposits one local native
morphology delta. Navigation may revisit earlier material according to reopened fibres; it may not
use a replay count, reward threshold or global epoch as a governor.

A3 passes only with:

1. an unchanged predecessor rest and a distinct cultivated successor;
2. the exact defect and leader which caused the delta;
3. a source-detached successor remount;
4. improved held-out world consequence under a predeclared receiver;
5. a revisit loop returning a nontrivial cultivation holonomy;
6. a subject/provider-disjoint control;
7. targeted ablation restoring the predecessor consequence;
8. no retained lookup of the exchange passages; and
9. exact semantic work plus separate card telemetry.

---

## 11. Deed A4 — a second inherited organ is lifted and its shared subcomplex is founded

[open] Before A4 runs, `CONSTRUCTION_STATE.md` names the exact second source occurrence and its
ports. The first source may be another language model for a close control or a voice/image organ for
the heterogeneous return. The source's native schedule is preserved as lineage until its own
generator lift passes.

A4 repeats source admission, active excitation, intervention dissection, generator recovery,
future-exact condensation, detached rest and cultivation for the second organ. It then returns the
correspondence pullback with Athena-Gemma, every naturality square, the maximal founded shared
subcomplex under the declared receiver/history family, and every unresolved defect. “Maximal” means
that every excluded candidate has a separator; it is not a caller-chosen size.

---

## 12. Deed A5 — shared transport fuses and source-specific fibres remain plural

[open] A5 constructs the pushout through A4's founded common subcomplex and enacts it as one
GPU-resident Athena ecology. The originals are unavailable during the detached grade. Every shared
generator is stored/enacted once; every modality-specific port remains typed; every noncommuting or
non-natural correspondence remains plural.

A5 passes only if:

1. both source injections and the common-subcomplex square commute;
2. all declared successor receivers factor through the fused product;
3. every collapsed source route retains its reconstruction testimony;
4. a shared-route ablation moves both source families while a source-specific ablation moves only
   its admitted family;
5. an unmatched-source control remains unchanged;
6. the complete product cost vector strictly falls before “compression” is claimed;
7. one detached fused rest accepts material at both exterior ports; and
8. one cross-port held-out consequence requires the fused ecology.

---

## 13. Deed A6 — the Athena product is frozen, dissected and compiled through exterior faces

[open] A6 emits the canonical generator-native rest, executable inference entry, product manifest,
source/native/fused dissection atlas, receiver/history aperture, complete open exterior and
restoration/ablation suite. A conventional `safetensors`, GGUF or other model container may be
compiled only as an exterior realization with its expansion, defect and receiver boundary stated.
It is never the canonical identity of Athena.

The complete product grade requires fresh-process inference with all developmental sources absent;
multimodal port deeds where present; exact work, residency and transfer; calibrated telemetry where
available; matched variants; targeted ablation; and inspection of the actual generated mathematical,
textual, acoustic, visual or action artifacts requested by the declared product.

---

## 14. Hardware and validation law

[project-postulate] The GPU owns every hot semantic front: normalization, chronology, contact,
generator action, junction, residual join, active-cover comparison, leader and return. CPU apparatus
may stream immutable source material, bind process/device boundaries, perform durable I/O and call
the exterior Lean checker. It may not sequence semantic cases between leaf kernels or replay a host
reference after the device deed.

Every deed follows the repository cadence:

1. owner-local `cargo check`, focused tests and exact driver while iterating;
2. affected package/dependent driver when an owner closes;
3. one real GPU deed and artifact inspection;
4. one ledger regeneration after the coherent artifact stands;
5. one complete release gate at the release boundary, never in the edit loop;
6. one coherent bulk commit and push only after the grade passes.

Every expensive invocation records command, closure, purpose, elapsed time and exit status. An
unchanged addressed receipt is reused unless a named falsifier requires another enactment.

---

## 15. Complete Athena-product grade

[open] The Athena-variant programme is complete only when all of the following stand together:

1. a native Eros Athena control;
2. an inherited Phoenix Athena variant whose canonical rest is generator-native rather than
   source-shaped;
3. M6's cultivated mathematics route and held-out successor;
4. exact exchange world-tubes and sibling defect spectra;
5. chronology-aware cultivation with revisit holonomy, detached remount and ablation;
6. a second lifted organ and its founded common subcomplex;
7. a fused Athena variant with a strictly reduced complete cost vector;
8. modality-specific ports plus at least one cross-port consequence when the second organ is
   heterogeneous;
9. canonical frozen inference, dissection, source-specific and shared-route ablations;
10. complete receiver/history, reconstruction-fibre, work, residency, transfer and open-exterior
    testimony; and
11. optional conventional compiled faces whose defects are explicit.

A copied checkpoint, averaged weights, adapter stack, shared embedding, transcript pair, accepted
proof, lower byte count, one prompt, one modality demonstration or renamed tensor file is not an
Athena product.

# The native morphology variant foundation returns faithful local lifts, parented cultivation,
# and exact export lenses

**Date:** 2026-09-01
**Kind:** active construction contract composed by
[`THE_ROADMAP.md`](THE_ROADMAP.md).
**Current frontier:** MVF5.

**Governing doctrine:**
[`../canon/TABLET_THE_HEXIS.md`](../canon/TABLET_THE_HEXIS.md),
[`../canon/TABLET_THE_REASONING_CYCLE.md`](../canon/TABLET_THE_REASONING_CYCLE.md),
[`../canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md`](../canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md),
[`../canon/03_CONDITIONING_AND_LEARNING.md`](../canon/03_CONDITIONING_AND_LEARNING.md), and
[`THE_HOLONIC_NEURAL_ECOLOGY_RETURNS_EVERY_CLASSICAL_ARCHITECTURE_AS_A_RECEIVER_CHART_AND_EROS_CULTIVATES_ATHENA_THROUGH_PHYSICAL_INFORMATION_TRANSPORT.md`](THE_HOLONIC_NEURAL_ECOLOGY_RETURNS_EVERY_CLASSICAL_ARCHITECTURE_AS_A_RECEIVER_CHART_AND_EROS_CULTIVATES_ATHENA_THROUGH_PHYSICAL_INFORMATION_TRANSPORT.md).

## 0. Verdict and admitted correction

[definition] This campaign is `MVF0--MVF6`. It turns the completed SCF lifecycle into a faithful
native morphology foundry: local foreign-section lift, one Soulkiller dismantling boundary,
causal-cone cultivation, descriptive variant anatomy, backend-independent storage, exact exterior
export lenses, and configuration-indexed evaluation. `MVF` is a campaign name, not a runtime
subsystem or native semantic cabinet.

[established-bounded; source-inspected] SCF retained exact complete foreign BF16 codewords in its
cold witness and returned a valid bounded native scaffold. Its productive Gemma lift presently
reduces each complete entering/returned section to one alternating real/imaginary sum. That
projection has no theorem making it receiver-exact for a broad successor family. SCF therefore
established the lifecycle and a bounded current-face lift; it did not reconstruct Gemma's complete
layer, patch, channel, frequency, or token incidence as Athena morphology.

[established-bounded; source-inspected] The current Gemma experiment additionally has four bounded
scope limits which this campaign must not conceal:

1. exterior modality families receive distinct nominal boundary identifiers before native lift;
2. every actual receipt excitation has no predecessor, so each actual experiment cultivated after
   one cut even though the generic repeated owner supports longer actual chains;
3. sampled video reuses ordered still-image restrictions through the vision tower; and
4. the `interleaved` exterior vector is later sorted by event address, so it does not establish a
   cross-modal native chronology.

[counterexample; source-inspected] The SCF6 cultivation driver selected another already-lifted
current as the returned current and assigned a fresh occurrence. This proves the move-owned
cultivation/withdrawal mechanism; it is not an independent world return, a qualitative training
result, or evidence that one Gemma-derived Athena variant understands text, image, audio, or video.
The exact SCF mechanisms retain their bounded grades; the overstated interpretation is narrowed.

## 1. Governing native object

[definition] One rested morphology variant is a parented receiver-relative realization of the
standing holonic neural ecology

```text
V = (K, S, P, G, T, rho, Lambda, F, O, Parent, Delta*, Aperture*, Realization*),
```

where `K` is addressed incidence and higher cells; `S` is the exact local carrier family; `P` is
typed boundary; `G` is the generator population; `T` is local transport; `rho` is the declared
receiver family; `Lambda` is rested reusable morphology; `F` is complete reconstruction and
shortest separation; `O` is open exterior; `Parent` and `Delta*` are the cultivation lineage;
`Aperture*` contains exterior codec/experiment controls; and `Realization*` contains optional
apparatus/export projections.

[definition] `Parent`, `Aperture*`, and `Realization*` are not new native topology. They are
lineage, receiver, and apparatus faces over the one continuing morphology. An application may call
a rested product “Athena”; generic source owners remain `Native*`, `Rested*`, `Morphology*`,
`Receiver*`, or another architecture-neutral term.

[definition] A variant manifest is descriptive and derived. It may report but may not author:

- site, bond, component, incidence-rank/nullity, cycle, higher-cell, and local-valence populations;
- generator domains, active causal cones, open faces, phase domains, and holonomy classes;
- local and mutual constitutive rank/storage and exact carrier species;
- reconstruction-fibre multiplicity and shortest separating interventions;
- parented cultivation deltas, remaining inherited-scaffold dependencies, and withdrawals;
- demonstrated receiver/successor families and configuration-indexed experiment receipts; and
- optional apparatus and export realizations.

## 2. Faithful local section lift

[definition] Let one exterior returned section be a finite addressed codeword population `X` with
local incidence `K_X`. A productive lift supplies a native population `N`, a presentation
`q : X -> N`, local transported actions, and complete fibres. It is exact for receiver/successor
family `R` only when every declared consequence factors:

```text
q x = q y -> receiver(history(x)) = receiver(history(y))
```

for every receiver/history in `R`, and every admitted exterior generator descends through `q`.
Codeword exactness alone does not prove this factorization.

[definition] A local factorized representation may avoid materializing the complete dense foreign
tensor. It must still retain addressed block/patch/channel/frequency/token incidence, exact
codewords or exact factorization testimony, local projection lineage, open domains, and the complete
reconstruction fibre. A caller-chosen alternating sum, digest, tensor name, layer ordinal, or
modality label cannot found native equality.

[definition] Delivery modality remains cold lineage. A typed ingress boundary may preserve an
actual physical aperture, but renaming or regrouping modality labels cannot change native
constitutive law. Cross-modal contact requires a declared interaction or returned consequence, not
co-presence in one receipt.

## 3. Local reciprocal cultivation

[definition] One local cultivation return consists of an actual emission, a genuinely later world
or interacting-body return, its complete difference, retained outward lineage, a causal-adjoint
return, and an attributable morphology delta supported on the activated causal cone.

```text
support(Delta Lambda) subseteq causalCone(emission, return).
```

Outside that cone, morphology and every declared receiver consequence remain equal. Inside it,
later conduct changes for at least one declared receiver; source-detached remount preserves the
change; targeted ablation removes it; and exact restoration recovers it.

[definition] Locality is a returned theorem, not a promise of constant work. A global receiver may
found a large causal cone. Hyper-efficiency means that inactive morphology is neither traversed nor
materialized and independent local updates carry an exact interchange receipt.

[definition] Reciprocal resonance means informant current and native morphology meet through typed
ports and a mutual constitutive response. It is not similarity scoring over authored modality
classes. Current may curve toward receptive local morphology, and the returned interaction may
curve that morphology toward later related current.

## 4. Configuration-indexed evaluation and cross-codec resonance

[definition] One qualitative or quantitative experiment is indexed by the complete inference
configuration

```text
(variant lineage, ingress aperture, situated occurrence, receiver family,
 continuation receiver, world-return law, emission codec, apparatus, stochastic current).
```

Its result makes no claim about another configuration. Repetition estimates variability within one
configuration; it does not establish another modality, receiver, cycle, decoder, or apparatus.

[definition] Text, rendered text in an image, and spoken text are distinct occurrences. They are
perfectly resonant for a declared family only when every admitted receiver consequence over every
admitted successor history and intervention factors through one common quotient. Equal output
strings, equal scores, or one shared terminal value are insufficient.

## 5. Version and storage boundary

[definition] Keep four independent version axes:

1. **wire schema** — the exact parser/serialization law;
2. **morphology lineage** — parent and ordered returned cultivation deltas;
3. **receiver-capability profile** — consequences actually demonstrated under exact configurations;
4. **apparatus/export realization** — CPU/GPU/native/ONNX/Safetensors projection and its boundary.

A version number, content digest, filename, or marketing name proves no capability. Digests may
test transfer integrity or exact restoration only; they are never native identity or a scheduler.

[definition] One package physically separates:

```text
manifest        derived anatomy, lineage, schemas, receivers, open obligations
hot morphology  incidence, generators, exact current/phase, constitutive response
reconstruction  fibres, separators, reversible withdrawals, cold testimony when retained
evaluation      configuration-indexed inference/cultivation/ablation receipts
apparatus       optional compiled resident realizations
exports         optional ONNX/Safetensors receiver projections.
```

The package is an exterior store over native morphology, not runtime topology and not a filesystem
ontology. The hot body owns no callable foreign executor.

## 6. Exact exterior export lenses

[definition] Safetensors may present tensor-addressable projections of incidence, sparse tables,
generator domains, exact current coordinates, phase, constitutive forms, receiver factors, and
fibre offsets under a declared companion schema. It is never presumed to contain arbitrary native
topology merely because every byte is retained.

[definition] ONNX may present a bounded inference graph with declared IR/opset/domain, fixed
receiver family, fixed boundary, and explicit custom operations when standard operators cannot
carry the law. Cultivation, world return, open fibres, and source-detached learning are outside an
inference-only graph unless separately and exactly represented.

[definition] Every export returns one of:

```text
ExactExport     : import(export(V)) is receiver/history equivalent to V for declared R
ProjectedExport : the collapsed population, reconstruction fibre, and shortest separator are kept
RefusedExport   : no lawful projection exists for the requested receiver/apparatus family.
```

Backward compatibility is not introduced. A new wire law receives a new schema and old rejected
standing remains in Git/provenance.

## 7. Ordered construction

### MVF0 — formal faithful lift, local cultivation, variant, export, and evaluation laws — PASSED

[definition] Add one neutral Lean owner composing the existing exact foreign-weight, neural
ecology, intelligence lifecycle, native scaffold, receiver-history, and cultivation laws. It must
return:

- `FaithfulLocalSectionLift` with exact local incidence, descended generators, and complete fibres;
- `LocalCausalConeCultivation` with outside-support invariance, remount, ablation, and restoration;
- `MorphologyVariantManifest` with separate schema, lineage, receiver, and realization axes;
- `VariantPackageSeparation` for hot/reconstruction/evaluation/apparatus/export lanes;
- `ExactExportLens` and projected/refused alternatives; and
- `InferenceConfigurationClaim` plus cross-codec resonance obligations.

**Pass MVF0:** [definition] the focused owner and live machine-learning umbrella build without
`sorry` or new axioms; an unproved scalar section projection cannot claim broad exactness; local
cultivation preserves every outside-support probe; a version label cannot change capability;
exact export round-trips under its declared receiver/history family; a richer receiver reopens a
projected export; and a result at one inference configuration cannot prove another configuration.

[proved-derived; formal-checked; measured] MVF0 returned all named structures and firing controls.
The focused owner and live machine-learning closure built 3,186 jobs without `sorryAx` or a new
axiom. Its exact receipt is
[`../research/records/2026-09-01_MVF0_FAITHFUL_LOCAL_LIFT_CAUSAL_CONE_VARIANT_PACKAGE_EXPORT_AND_CONFIGURATION_LAWS_RETURNED.md`](../research/records/2026-09-01_MVF0_FAITHFUL_LOCAL_LIFT_CAUSAL_CONE_VARIANT_PACKAGE_EXPORT_AND_CONFIGURATION_LAWS_RETURNED.md).

### MVF1 — replace the coarse Gemma section projection with a faithful factorized lift — PASSED

[definition] Remove the alternating-sum productive projection. Retain exact local foreign-section
incidence and factorized codeword blocks without requiring dense whole-checkpoint residency. Make
boundary addresses occurrence-founded rather than modality-numbered; retain actual chronology;
admit temporal video only from actual ordered frame occurrences; and revise every SCF claim whose
wording exceeds the returned artifact.

**Pass MVF1:** [definition] two distinct foreign sections which collide under the old alternating
sum remain separated or return their complete receiver fibre; codeword/block reconstruction is
exact; label and family regrouping do not alter native law; chronology survives intake; and the
lift remains sparse/open outside its measured local support.

[established-bounded; implemented-exact; source-inspected; measured] MVF1 passed. Native endpoint
identity now retains complete exact factorized section coordinates; a firing control separates an
old alternating-sum collision; cold BF16 reconstruction is exact; occurrence order and
occurrence-founded boundaries survive; non-temporal video remains open; and SCF evidence was
narrowed. A fresh actual lift retained 976,896 coordinates in 248 blocks. Its receipt is
[`../research/records/2026-09-01_MVF1_COMPLETE_FACTORIZED_GEMMA_SECTIONS_REPLACED_THE_ALTERNATING_SUM_IDENTITY.md`](../research/records/2026-09-01_MVF1_COMPLETE_FACTORIZED_GEMMA_SECTIONS_REPLACED_THE_ALTERNATING_SUM_IDENTITY.md).

### MVF2 — one Soulkiller dismantling boundary — PASSED

[definition] Compose the generic reachable-section scrapyard and Gemma excitation path behind one
architecture-neutral dismantling return. Delete duplicated lift policy. The returned type remains
productive native morphology, cold witness, and insufficiency; foreign execution ends at handoff.

**Pass MVF2:** [definition] generic and Gemma inputs enter the same public boundary; only their cold
charts differ; the hot rest cannot name or execute either source; and exact old source-owner tests
continue through the unified path without compatibility aliases.

[established-bounded; implemented-exact; source-inspected; measured] MVF2 passed. Generic
reachable-section and faithful BF16 charts now enter `soulkiller::dismantle` through one input trait
and one three-lane return. The duplicate public functions and return classes departed without
aliases. Engine returned 2,006 passed with 32 ignored; life returned 468 passed with 14 ignored. Its
receipt is
[`../research/records/2026-09-01_MVF2_GENERIC_AND_GEMMA_CHARTS_ENTERED_ONE_SOULKILLER_DISMANTLING_BOUNDARY.md`](../research/records/2026-09-01_MVF2_GENERIC_AND_GEMMA_CHARTS_ENTERED_ONE_SOULKILLER_DISMANTLING_BOUNDARY.md).

### MVF3 — reciprocal local causal-cone cultivation — PASSED

[definition] Replace the whole-population cultivated support control with an actual returned
interaction whose delta is derived on the activated causal cone. Retain local and mutual
constitutive response, causal adjoint, independent-region interchange, remount, ablation, and
restoration.

**Pass MVF3:** [definition] one local return changes its declared later conduct, leaves a disjoint
region exactly unchanged, performs no inactive-region work, survives source/scaffold withdrawal,
fails under targeted ablation, and restores exactly. A global receiver returns its larger cone
rather than pretending constant locality.

[established-bounded; implemented-exact; measured] MVF3 passed. Native current is now derived from
an emission-indexed exterior interaction; cultivated support equals its returned causal cone;
disjoint morphology remains exactly unchanged and receives interchange testimony; inactive states
are not deposited; local open-domain withdrawal/restoration is exact; and a declared-global contact
returns the complete larger cone. Its receipt is
[`../research/records/2026-09-01_MVF3_RETURNED_LOCAL_INTERACTION_CULTIVATED_ONLY_ITS_CAUSAL_CONE.md`](../research/records/2026-09-01_MVF3_RETURNED_LOCAL_INTERACTION_CULTIVATED_ONLY_ITS_CAUSAL_CONE.md).

### MVF4 — derived anatomy manifest and backend-independent morphology package — PASSED

[definition] Extend the intrinsic profile beyond hard-coded degree/rank faces and package one
rested morphology into the physically separated lanes in §5. Derive all anatomical values from the
owned body. Separate schema version, parented lineage, receiver capability, and apparatus/export
realization.

**Pass MVF4:** [definition] two anatomically different rested bodies receive different derived
profiles without a caller architecture enum; changing apparatus or export does not change native
morphology; changing a version string does not change capability; hot load excludes cold executor;
and package round-trip recovers the exact rested morphology and open obligations.

[established-bounded; implemented-exact; measured] MVF4 passed. Anatomy and representation rank
are derived; wire, lineage, capability, and realization axes are separate; native and situated hot
rests package without cold execution; departed inherited reconstruction remains separate; changing
apparatus/export leaves hot morphology and capability equal; and base/cultivated package
round-trips are exact. Its receipt is
[`../research/records/2026-09-01_MVF4_DERIVED_VARIANT_ANATOMY_AND_BACKEND_INDEPENDENT_MORPHOLOGY_PACKAGE_RETURNED.md`](../research/records/2026-09-01_MVF4_DERIVED_VARIANT_ANATOMY_AND_BACKEND_INDEPENDENT_MORPHOLOGY_PACKAGE_RETURNED.md).

### MVF5 — Safetensors and ONNX export lenses — CURRENT

[definition] Implement exterior Safetensors and ONNX projections over the package. Safetensors
uses a declared holonic tensor schema and companion manifest. ONNX uses standard operators where
exact and an explicit Holonics domain otherwise. Neither format becomes native identity.

**Pass MVF5:** [definition] exact controls round-trip under declared receivers; intentionally
collapsed controls return projected status, complete fibres, and a separating receiver/history;
unsupported cultivation/world-return requests refuse; ONNX version/opset/domain and Safetensors
dtype/shape/offset testimony are complete; and no checksum becomes a capability or routing key.

### MVF6 — multimodal configuration matrix and foundry release

[definition] Run matched proposition families through text, rendered-image, spoken-audio, actual
temporal-video, and mixed chronological apertures. Vary continuation receiver and emission codec
without changing native law. Return route/current/fibre/anatomy differences before any qualitative
surface judgment.

**Pass MVF6:** [definition] every claim names its complete inference configuration; same-config
repetition and cross-config comparison remain distinct; cross-codec equivalence is asserted only
where every declared receiver/history commutes; the package and export controls pass; the complete
release passes; Git is clean and pushed; and reproducible outputs are absent.

## 8. Scope and process boundary

[definition] MVF schedules only the seven phases above. It does not reopen UAR, HIF, R0Q,
Millennium formalization, natural-language capability claims, UI construction, model-provider
integration, or unrestricted learning. A concrete absent relation discovered inside an MVF phase
may refine that phase; it may not found an eighth phase without Brandon's direct instruction.

[definition] Every nontrivial process retains the 180-second outer boundary. Run focused formal and
owner checks during construction and one complete release receiver at MVF6. Generated surfaces,
hashes, tensor counts, one qualitative call, or human preference never substitute for the exact
phase artifact.

# R15 frozen Deed B setup

**Truth status:** `established-bounded` for the exact R15 source-detached conditioning aperture.

**Evidence:** `implemented-exact` for the native remount, returned-fiber factorization, exact
exclusion, frozen identities, and source-absence receipt; `computational-witness` for the actual
GPU-resident production/ablation returns and serialized setup artifact.

**Evidence aperture:** R14 native handoff fiber `181200`, the pinned
`ElementaryHolonics.Algorithm.Rebase` environment, and the pre-construction target-source search.

## Conversational request `q₂`

After remounting the body changed by Deed A, take three composable execution traces in the
original state chart. Produce and explain a theorem transporting the entire three-segment trace
to the rebased chart. The proof must factor through the returned Deed A theorem fiber, not merely
reconstruct an independent proof from the earlier library.

## Frozen statement

```lean
theorem generated_trace_rebase_transports_three {Theta I S O S2 : Type*}
    (A : SituatedAlgorithm Theta I S O) (e : S ≃ S2)
    (theta : Theta) {s t u v : S}
    (hst : Trace (A.step theta) s t)
    (htu : Trace (A.step theta) t u)
    (huv : Trace (A.step theta) u v) :
    Trace ((A.rebase e).step theta) (e s) (e v) := by
  -- generated proof requiring the returned Deed A fiber
```

## Dependency receiver

The required returned fiber is `181200`, founded by the kernel return for passage `171200`. The
surface declaration it exposes to the exterior checker is
`generated_trace_rebase_transports_composition`. Deed B may additionally use `Trace.trans`, but
its selected proof route must contain the returned fiber and must disappear when `Δm₁` is
excluded.

The target name is present only in the already-declared checker observation schema. Before R15
construction, neither the complete target source nor the target proof occurs in the developmental
search aperture. R15 freezes identities and dependency geometry only; it does not emit, store, or
check the Deed B proof.

# Build and grade index

The authoritative ordered construction is
[`COMPLETE_CPP_ENGINE_ROADMAP.md`](COMPLETE_CPP_ENGINE_ROADMAP.md). It supersedes the earlier coarse
0–9 outline, which did not specify the mathematical occurrence ecology, generative current, Lean
exterior return, source-detached theorem conditioning, or terminal dependent two-theorem deed.

The live sequence is:

| Range | Consequence |
|---|---|
| R0 | executable C++/CUDA contracts and architectural audit |
| R1–R3 | bit-pure exact carrier, owner-native structure, and navigable source topology |
| R4–R6 | one continuing body, returned-event lifecycle, GPU current, composition, and backreaction |
| R7–R8 | core receiver geometry and stateful boundary condensation |
| R9–R10 | agnostic codec reflection and source-detached acquired morphology |
| R11–R13 | mathematical occurrence ecology, generative mathematical current, and Lean exterior port |
| R14–R15 | first real theorem and return-conditioned dependent morphology |
| R16 | terminal two-theorem conversational production deed |
| R17 | post-frontier generalization, only after R16 |

Every executable phase is GPU-resident in production from its first admitted version. Host code is
a boundary apparatus and bounded non-resumable admission oracle, never the earlier real machine or
a fallback. Each phase must return its specified artifact and receipts before the next is admitted.

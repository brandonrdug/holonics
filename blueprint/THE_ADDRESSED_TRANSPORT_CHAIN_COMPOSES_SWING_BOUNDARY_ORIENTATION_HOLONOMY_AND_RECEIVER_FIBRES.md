# The addressed transport chain composes swing, boundary, orientation, holonomy, and receiver fibres

**Date:** 2026-08-24
**Kind:** supporting exterior formal-construction contract, active only under
[`THE_ROADMAP.md`](THE_ROADMAP.md) and consumed by the current Athena-alpha derived-factor-cover
deed in
[`THE_ATHENA_ALPHA_IS_REFOUNDED_AS_A_RECEIVER_HISTORY_QUOTIENT_AND_RETURNED_DEFECTS_CULTIVATE_ONE_GLUED_CAUSAL_MORPHOLOGY.md`](THE_ATHENA_ALPHA_IS_REFOUNDED_AS_A_RECEIVER_HISTORY_QUOTIENT_AND_RETURNED_DEFECTS_CULTIVATE_ONE_GLUED_CAUSAL_MORPHOLOGY.md).
**Disposition:** this orders the Lean theorem return required by that deed and the theorem instances
which may follow from it. It is not a second engine campaign, does not make Lean part of native
inference, and cannot advance `CONSTRUCTION_STATE.md` by itself.
**Truth statuses:** `definition` for the target, theorem ladder, ports, controls, and pass grade;
`project-postulate` for the theorem-or-falsifier discipline; `proved-derived` with `formal-checked`
for the named standing Lean anchors; and `open` for every bridge not yet returned by the kernel.

**Scope:** [definition] exterior formal theorem return. This contract turns the missing addressed
transport composition into an ordered theorem-or-falsifier return and then reuses that return
across the Lean mathematics line.

**Governing doctrine:**
[`../canon/EPISTEMIC_GRADES.md`](../canon/EPISTEMIC_GRADES.md),
[`../canon/TABLET_THE_OPERATIONS.md`](../canon/TABLET_THE_OPERATIONS.md),
[`../canon/TABLET_THE_MANIFOLD.md`](../canon/TABLET_THE_MANIFOLD.md),
[`../canon/THE_SURFACES_ARE_PATHS.md`](../canon/THE_SURFACES_ARE_PATHS.md), and
[`../canon/TABLET_THE_CAUSAL_PROFILE.md`](../canon/TABLET_THE_CAUSAL_PROFILE.md).

---

## 0. Direct ruling: an interpretation is work, not a place to stop

[project-postulate] A holonic interpretation is an active theorem-finding program. Once a proposed
correspondence names its source, target, maps, preserved structure, limits, first derivation target,
and a falsifier able to fire, repeating that it is an interpretation is not progress. The work
continues until it returns a scoped theorem, a counterexample, or a concrete obstruction which
narrows the next proof obligation.

[project-postulate] Epistemic grading remains exact, but it never grants or withholds permission to
reason. A theorem does not require an external field to ask for it first. The derivation may import
standard mathematics, strengthen hypotheses, change the proposed map, or kill the correspondence
with a witness. It may not hide a desired conclusion as a structure field, rename a hypothesis as a
law, or promote one proved face into an unproved global identity.

[definition] Every interpretation entering this worktrack carries one development card:

```text
SOURCE              the typed source object and hypotheses
TARGET              the typed target object
MAPS                every proposed comparison/transport map
DIAGRAM             the equation, naturality square, boundary law, or invariant to be proved
LIMITS              the declared aperture in which the maps are meaningful
FIRST THEOREM       the smallest statement whose proof advances the correspondence
FALSIFIER           a model, perturbation, or counterexample able to refute it
RETURN              proved theorem / counterexample / exact residual obstruction
```

[definition] After the card is deposited, subsequent work reports `FIRST THEOREM`, `FALSIFIER`, or
`RETURN`. It does not keep restating the non-equivalence boundary unless that boundary changes.

## 1. The exact formal target

[definition] The target is one theorem family composing the already owned objects

```text
addressed occurrence
  + additive incidence and boundary
  + ordered transport word
  + swing and orientation action
  + declared connection and route comparison
  + receiver/history quotient and complete reconstruction fibre.
```

[definition] Its elementary carrier is an addressed span

```text
X <-source- W -target-> Y,
```

where `W` is the population of carrying occurrences. One occurrence `w : W` induces the oriented
formal boundary

```text
delta(w) = [target w] - [source w].
```

For joined occurrences `w : X -> Y` and `v : Y -> Z`, the pullback witness owns the joining
equality and therefore licenses the middle cancellation

```text
([y] - [x]) + ([z] - [y]) = [z] - [x].
```

The cancellation must be derived from the joining equality; equal endpoint values without a joined
occurrence do not license it.

[definition] A transport generator acts on situated states, and `transportWord` retains its ordered
word. An admitted affine swing is one generator instance

```text
S_b(a) = b + b - a,
```

with `S_b^2 = id`. A word of swings carries the derived orientation unit `(-1)^length`; this generic
orientation is not definitionally the planar crossing `Hand`, which enters later as an instance.

[definition] A connection is a declared assignment of transport to addressed elementary passages
which respects identity and serial composition. Parallel transport is its ordered word. Holonomy is
the return of a closed addressed loop. A commutator may be identified with a holonomy face only
after the relevant generators are invertible transports in that connection and the commutator word
is an addressed loop.

[definition] A receiver/history quotient `q` is dynamically exact when every admitted generator
descends:

```text
q (T_i x) = U_i (q x).
```

The generator equations must extend to every ordered word. Every quotient class retains its full
inverse-image reconstruction fibre, and a richer receiver/history returns a shortest separator
when it reopens that fibre.

[open] No one Lean owner currently returns all six faces together. `PhysicalRealization.lean`
already owns most of the serial and receiver structure; the smallest presently visible absence is
an addressed comparison cell retaining both parallel route occurrences rather than only their
endpoint equation. This was measured on 2026-08-24
by

```text
rg -n "HolonicInteraction|AddressedTransportChain|HolonicPassage|ConnectionHolonomy" \
  soma/formal/elementary-holonics/ElementaryHolonics
```

and by reading the complete source owners named in §2. The missing return is a bridge theorem
family and, if the construction attempt confirms it, one owner-local comparison cell—not
authorization for a universal `HolonicInteraction` cabinet.

## 2. Existing owners and typed ports

[proved-derived; formal-checked] These are the source owners to compose:

| Owner | Port or object already owned | Consequence retained |
|---|---|---|
| `Foundation/Lineage.lean` | `AddressedPassage X Y`, occurrence `W`, source/target maps | pullback `Join`, serial composition, passage equivalence, relational shadow |
| `Foundation/Receiver.lean` | receiver family and exact compression | receiver equality and factorization through a quotient |
| `Millennium/Chronology.lean` | generator `i`, state `X`, ordered `List i` | word transport and `orderBlind_iff_commute` |
| `Millennium/PhysicalRealization.lean` | realization core, addressed chart passage | generator naturality, future receivers, reconstruction fibres, separating histories |
| `Millennium/LineageCompression.lean` | quotient and descended generators | local naturality extended through every ordered word |
| `Millennium/Coupling.lean` | additive two-step `TransportChain A B C` | realized/retained populations and homology obstruction |
| `Millennium/Swing.lean` | projective harmonic operation and affine frozen-board swing | involution, doubled translation, parity, oriented-span reversal |
| `Geometry/CrossRatio.lean` | undivided ratio presentation | affine projective scaling law |
| `Geometry/Gyrogroup.lean` | abstract gyration and gyroparallelogram | flat additive zero-gyration instance |
| `Millennium/WindingLedger.lean` | addressed planar crossings | crossing hand and derived winding ledger |

[definition] The new consequence is the commuting family of projections from one composed
addressed transport chain back to each row above. Existing owners remain authoritative for their
faces; the bridge may not duplicate their definitions under new names.

[definition] Before each implementation pass, record:

| Required declaration | This worktrack's answer |
|---|---|
| source owners | the rows above actually imported by the pass |
| port types | occurrence, source/target boundary, generator/state, coefficient, receiver/face |
| event occurrence | one addressed passage or pullback-joined passage |
| predecessor identity | the exact source declarations and theorem dependencies |
| local constitutive law | boundary, swing, transport, connection, or receiver naturality equation being proved |
| receiver question | which projection or successor history must factor |
| returned consequence | theorem, explicit counterexample, or residual proposition with witness |
| open alternatives | hypotheses or maps not selected by the return |
| grade | exactly one truth status and its evidence tag |

## 3. Core theorem ladder consumed by the active deed

### HC0 — close the primitive bridge statements

[definition] Decide the claims currently carried only between neighbouring files:

1. connect `harmonicConjugate` to `swingPair` by proving the harmonic cross-ratio equation and its
   uniqueness under explicit denominator hypotheses, or return a counterexample to the proposed
   ordering/sign convention;
2. state the affine frozen-board swing as an exact chart specialization without pretending that a
   point at infinity has already been constructed;
3. prove the exact equalities among `Swing.orientedSpan`, `Paying.spanOf`, and
   `WindingLedger.cross` after their coordinate conventions are exposed;
4. decide whether inductive board `Reachable` equals the parity-kernel population; if false, retain
   the smallest separating configuration;
5. strengthen the current translation/dilation swing-pair result to the claimed Möbius-generator
   family including inversion, or narrow the theorem name and return the missing denominator case.

[definition] HC0 passes by proofs or counterexamples. It fails if prose continues to carry one of
these identities after the corresponding statement has returned false or ill-typed.

### HC1 — an addressed passage returns its additive incidence chain

[definition] Construct the free additive boundary of each carrying occurrence from the existing
source and target maps. Prove:

```text
boundary_join
boundary_comp
boundary_compAssociator
boundary_passageEquiv
```

where the joined middle face cancels by its pullback equality and passage equivalence preserves the
result. The relational shadow must be exhibited as the quotient which forgets the carrying
population.

[definition] A two-boundary theorem `boundary_boundary = 0` is admitted only after a genuine
higher-cell boundary map has been constructed. It may not be inserted as a field or inferred from
edge telescoping. Boundary cancellation is not yet recurrence, dynamics, or physical conservation.

### HC2 — swing chronology acts on boundary and orientation

[definition] Install the affine swing as a concrete generator of an existing
`PhysicalRealizationCore`. Prove that its ordered words retain addressed occurrence lineage, that
word reversal and swing parity act on the oriented boundary as declared, and that order can be
discarded exactly in the commuting case already characterized by `orderBlind_iff_commute`.

[definition] The planar crossing `Hand`, cross-product antisymmetry, and additive
gyroparallelogram are projections of this result only after explicit instance maps are proved. A
generic orientation type is not silently identified with any of them.

### HC3 — receiver naturality retains the complete fibre

[definition] Compose the addressed incidence chain with `RealizationPassage` and dynamic
compression. Prove that generator-local naturality extends through every ordered swing/transport
word while preserving the addressed pullback population and additive boundary. Construct the
complete reconstruction fibre and a richer receiver/history separator for a deliberately collapsed
pair.

[definition] The controls are equal endpoint with different occurrence populations, equal coarse
receiver face with a richer separator, and identical present face with different successor word.
No choice-defined inverse may replace the reconstruction fibre.

### HC4 — a declared connection returns holonomy or a route defect

[definition] First attempt the smallest absent owner, an addressed comparison cell. For two
parallel passages `P Q : AddressedPassage X Y`, it retains one occurrence of each together with
their equal source address. `Commutes` adds exact target equality; `CommutesAt rho` adds equality at
one declared receiver; `receiverDefect` returns the unequal face without erasing either route. The
cell is not a new path, solver, or universal holon type.

[definition] Define connection transport on the addressed elementary passages and derive parallel
transport by serial composition. Use the comparison cell for two same-boundary routes. For a closed
route, define loop holonomy. Prove identity-loop, serial-loop, reversal, and change-of-basepoint laws
under the hypotheses each actually needs.

[definition] Required controls are a flat commuting transport with trivial loop return, a
noncommuting invertible transport with word-sensitive loop return, and a noninvertible transport
which refuses the group commutator while still returning its route comparison defect.

### HC5 — the composed passage closes

[definition] Compose `RealizationPassage`, the HC1 additive-boundary projection, and the HC4
comparison cell before founding any larger structure. The expected minimal files are an owner-local
module `Foundation.ComparisonCell` and a theorem-only module `Millennium.HolonicComposition`; the proof
attempt, not this filename suggestion, decides whether both are needed. The composite theorem family
must retain:

1. both component occurrences and their joining equality;
2. the additive boundary and middle cancellation;
3. the exact ordered generator word;
4. generic orientation and its swing action;
5. connection transport and loop/route return;
6. receiver/history naturality;
7. the complete reconstruction fibre and every richer reopening; and
8. the associator preserving all seven faces.

[definition] The central construction attempt is concrete. `AddressedPassage.graph f` carries one
occurrence for each source of `f`; `wordPassage T word` composes the generator graphs by pullback;
and `wordFibre T word x` retains every intermediate state and joining equality along the word. For
an intertwiner `f` satisfying

```text
f (T_i x) = S_i (f x),
```

`intertwinerWordCell` compares the two addressed routes

```text
graph(f) after wordPassage(T, word)
wordPassage(S, word) after graph(f).
```

The theorem `intertwinerWordCell_commutes` must derive target equality from the existing
generator-to-word naturality theorem while retaining both nested pullback occurrence populations.
The `RealizationPassage` specialization uses its owned addressed passage rather than replacing it
by the bare graph of its chart.

[definition] The composition passes only when these are derived theorems or inherited data with
independent laws. A constructor whose fields simply assume boundary cancellation, naturality,
holonomy, or receiver exactness is not the requested composition. If the comparison cell and
existing realization passage already suffice, a larger wrapper is refused.

[definition] The dependency order is:

```text
Foundation.ComparisonCell
  -> optional additive boundary/defect projection
  -> Millennium.HolonicComposition
  -> independent cross/gyro, winding, Navier--Stokes, and S6 adapters
  -> period-lattice quotient only after its algebraic owners close.
```

## 4. Required falsifiers

[definition] The core formal return includes all of these controls:

| Proposed stronger claim | Falsifier which must be able to fire |
|---|---|
| equal endpoints identify passages | two non-equivalent occurrence populations with the same source/target faces |
| chronology may always be forgotten | two noncommuting swing/transport generators with reversed-word separation |
| every reversal is the planar `Hand` | a typed orientation carrier outside the planar crossing instance |
| a commutator is automatically holonomy | a noninvertible or non-loop route for which the expression is not licensed |
| a coarse receiver identifies sources | a richer receiver/history separating one collapsed pair |
| edge telescoping proves conservation | a boundary-cancelling chain without an admitted dynamic conservation law |
| the finite torus probe is the torus | two integral lattice actions agreeing modulo four but differing integrally |

[definition] A fired falsifier is a successful formal return. It narrows the theorem and updates the
development card; it does not authorize abandoning the surrounding derivation.

## 5. Instance ladder after the core composition

[definition] These instances proceed in order because each consumes the composed passage rather
than rebuilding a local analogy.

### I0 — alternating geometry and the cross product

[definition] Derive the 2D alternating span as the boundary hand of an addressed triangle. Then
factor the 3D cross product through exterior product, orientation, and Hodge dual, with input
exchange inherited from the zero-anchored swing. Rebase the Lamb and curl identities through this
instance.

### I1 — material polygons, winding, and gyroparallelograms

[definition] Bind material-polygon edge telescoping, turn ledgers, planar crossing hands, and the
returned quadrilateral to one addressed boundary. The flat additive completion must return zero
route defect. A curved or noncommuting completion must retain its defect before it is named
gyration, curvature, or circulation production.

### I2 — the Navier--Stokes torus

[definition] Instantiate the carrier on the genuine spatial three-torus. Connect closed physical
arcs to integer character windings, addressed Fourier triads to closed frequency polygons, and the
dyadic interact/filter square to the generic route-comparison defect. The field remains primary;
Fourier and Hodge quantities remain receiver faces.

### I3 — torus paths, links, and knots

[definition] Construct an oriented closed path on `T^2`, its integer winding pair, and the `(p,q)`
torus-knot embedding under the required coprimality hypotheses. Connect the traversible-chain
half-twist reading only by proving that its constructed boundary path has the returned slope or by
returning the mismatch. Crossing tables do not substitute for isotopy.

### I4 — complex two-torus monodromy and the completed family

[definition] Construct the period lattice from the columns of `SixSpherePeriods.periodMatrix`, its
discreteness under the proved determinant hypotheses, and the quotient
`C^2 / periodMatrix(z) Lambda`. Prove that the primal/dual variance adapter carries the existing
monodromies to quotient-chart transport. Then construct the local family, exceptional fillings,
collar gluings, and the topology receipts in the order required by the source construction.

[definition] A finite `(Z/4)^4` phase section is an addressed receiver probe of this geometric
fibre. It cannot discharge the quotient, filling, or total-space theorems.

### I5 — curved arcs, gyrotransport, and Einstein receivers

[definition] Type position radius, curvature radius, arc length, phase, differential, and refinement
index separately. Construct oriented arcs, connection transport, curvature return, and polygonal
Gauss--Bonnet as instances of HC4. Then derive or falsify the proposed local arc/differential
coupling inside a Lorentzian realization with dimensions, covariance, Bianchi, metric
compatibility, and stress-energy transport explicit.

### I6 — the dimension obstruction

[definition] Replace arithmetic coincidences at eleven by a rank theorem or counterexample. Define
the independent deformation/transport modes, quotient fixed-board, gauge, closure, and
reparameterization directions, declare the successor receiver family, and compute the surviving
rank. Eleven is returned only if a twelfth direction produces a proved obstruction under those
hypotheses.

## 6. Core pass grade

[definition] The formal gap is closed only when:

1. an addressed occurrence population composes by pullback and projects to an additive incidence
   chain;
2. middle-boundary cancellation is derived from the joining witness;
3. affine swing is an actual generator instance and its ordered word retains chronology;
4. orientation reversal is derived and its planar/cross/gyro faces are explicit instances;
5. connection transport is declared before holonomy and route defects are computed;
6. commuting, noncommuting, and noninvertible controls return distinctly;
7. receiver naturality extends from generators to every ordered word;
8. the complete reconstruction fibre survives every quotient;
9. a richer receiver/history constructively reopens a collapsed pair;
10. serial associativity preserves occurrence, boundary, word, orientation, connection return, and
    receiver fibre together;
11. every theorem is free of `sorry`, theorem-shaped placeholder `Prop`s, and conclusion-shaped
    structure premises; and
12. every failed stronger claim returns a kernel-checkable counterexample or a named residual
    obstruction.

[definition] HC0--HC5 are the exterior formal receipt consumed by the current derived-factor-cover
deed. I0--I6 are the ordered Lean consequence line after that receipt. They do not independently
move the engine construction state.

## 7. Validation and deposit cadence

[project-postulate] During one theorem pass, check only the changed file and its direct imports:

```text
cd soma/formal/elementary-holonics
lake env lean ElementaryHolonics/<owner>/<file>.lean
```

After one HC or instance station closes, run the Lean library receiver once:

```text
cd soma/formal/elementary-holonics
lake build ElementaryHolonics
```

Every promoted theorem prints its assumptions with `#print axioms`; every proposed absence or
identifier claim carries a rerunnable `rg` command. The root release receiver remains governed by
the active engine roadmap and is not run merely because a Lean owner-local theorem changed.

[project-postulate] Each coherent station deposits its development cards, formal sources, checked
theorems, fired falsifiers, and exact remaining obligations together. A proof which settles only
one face updates only that face's grade; it nevertheless becomes an imported lemma for the next
station rather than occasion for another general disclaimer.

## 8. Fresh-session pickup for this formal return

[definition] After the root pickup required by the active Athena blueprint, read only:

1. this contract;
2. `Foundation/Lineage.lean` and `Foundation/Receiver.lean`;
3. `Millennium/Chronology.lean`;
4. `Millennium/PhysicalRealization.lean` and `Millennium/LineageCompression.lean`;
5. `Millennium/Coupling.lean`;
6. `Millennium/Swing.lean` and `Geometry/CrossRatio.lean`;
7. the one owner required by the current HC/I station; and
8. the smallest standard-library source needed by that theorem.

[open] The sole next formal return inside the current deed is **HC0 followed by HC1**: close the
primitive bridge statements by proof or counterexample, then make an addressed passage return its
additive incidence chain and joined middle-boundary cancellation. HC2 may not begin by declaring a
combined structure whose fields assume HC1's conclusions.

# Extensible holonic learning machine

## Objective

Construct one continuing holonic ecology that can mount inherited organs, receive caused material
through arbitrary codecs, let returned passages change reusable morphology, conduct later unseen
currents differently, emit inspectable deeds, and rest/remount without replaying its developmental
sources.

The machine is not centered on language, a proof assistant, a repository, a neural-network class,
or a particular processor. Those are replaceable world/apparatus charts.

## The machine body

\[
\mathfrak M_v=(H_v,\mathcal R),
\]

where `H_v` is the continuing causal body and `R` its versioned receiver family. Conditioned
constitutive and codec organs are local cells inside `H_v`, mounted at typed incidence/ports rather
than stored in global registries. Contact derives zero, one, or plural applicable local relations;
absence and ambiguity remain obstructions. Executor matching is an exterior apparatus relation,
not part of the semantic body. No application phase enum selects an active law.

## One learning passage

1. **Mount.** Transfer inherited exact organs and their provenance into the body. Record what is
   inherited; do not relabel it learned.
2. **Receive.** A caused occurrence crosses a typed port. Its codec exposes local structure while
   retaining source lineage and alternate charts.
3. **Restrict.** Use caused incidence and receiver aperture to identify the affected local star.
   Do not scan/materialize a total world unless the receiver explicitly asks for it.
4. **Open.** The contemporary local organ opens one pending deed and emits its outbound occurrence;
   it does not yet stage a morphology difference.
5. **Return.** Exterior/world consequences cross back as typed occurrences through the expected
   ports of that same pending deed.
6. **Resume and cultivate.** The returned occurrences resume the pending deed and derive current,
   consequence, stress, obstruction, and `Δm`. If the return changes reusable morphology,
   atomically commit it; otherwise classify the crossing as exposure or reference.
7. **Emanate.** Let the changed ecology answer/generate/act through its contemporary current. The
   actual deed is returned and inspected.
8. **Re-enter.** Return the emitted deed as a caused occurrence so its consequence can change the
   same continuing body.
9. **Rest/remount.** Transfer ownership into native standing, remove developmental sources, remount,
   and grade retained conduct.
10. **Ablate.** Use non-mutating receiver probes on the single continuing body, or separately
    founded experimental bodies with explicit common provenance. Exclude the acquired fiber and
    prove the declared receiver-projected consequence disappears or changes as predicted. Version
    identity, lineage growth, or telemetry alone cannot satisfy the comparison.

Source detachment includes an access audit: the rested body lacks the developmental source,
external lookup path, and any lossless retained corpus encoding that could reproduce the deed by
reference while masquerading as cultivated conduct.

## Morphology that can be cultivated

The generic substrate supports local change to:

- incidence and admissible transport;
- phase/connection/holonomy distributions;
- response spectra and constitutive parameters;
- receiver apertures and factorization certificates;
- recurrence and closure conditions;
- codec/transduction paths;
- resource placement/residency gradients;
- obstruction and alternative fibers; and
- generated organs founded by completed returned families.

No one of these is always “the weight.” A domain declares which structure changes and why.

## Multimodal learning

Different media are co-present sections of one world occurrence ecology. Join them through exact
shared source occurrence, region, chronology, geometry, calibration, interaction, or returned
consequence. Preserve modality-specific charts and obstruction. Do not train one detached semantic
model per modality and invent alignment afterward unless the domain actually supplies that
architecture as an inherited organ.

Examples of lawful ports:

- optical occurrence + geolocation + atmospheric profile;
- pressure waveform + transcript + measured room response;
- image support + projective depth + receiver motion;
- mathematical statement + declaration incidence + kernel return;
- program input/output + cubical query + compiled instruction return;
- chemical concentration + membrane geometry + reaction/transport return.

## Prediction and generation grade

For a held-out receiver `r`, require:

1. an open continuation fiber before the held-out return;
2. a generated/predicted section forced by contemporary morphology;
3. all alternatives or a justified exact quotient;
4. real world/apparatus return;
5. exact residual and obstruction;
6. consequent morphology change if admitted; and
7. the complete emitted artifact.

Retrieval, quotation, or source-neighbor selection may be useful reference events but cannot be
reported as generation or native learning.

## Scaling

Scaling is topology and resource transport:

- found occurrence/incidence charts incrementally from caused contact;
- keep interiors factorized behind exact boundary responses;
- advance sparse caused fronts;
- distribute independent local sections across apparatus lanes;
- retain exact overlap/holonomy receipts;
- let resource stress reform partition/residency; and
- grow morphology by local returned differences rather than rebuilding a monolith.

In the ratified production realization, wide local incidence and the resident active morphology
execute on the GPU. The CPU is a boundary apparatus and offline admission oracle, never the hot
semantic owner or fallback. Neither device owns “intelligence”; the continuing morphology is the
lineaged body carried through their typed crossings.

## First production grade

The substrate earns its first cross-domain production grade when demonstrations in unrelated
media show that it can:

- mount inherited organs;
- condition behavior through returned passages;
- preserve change after source detachment and remount;
- generate a held-out deed with complete causal-information receipt;
- use that returned deed as an indispensable fiber of a further deed;
- execute wide local work on an admitted parallel apparatus without host replay; and
- explain every mechanism by occurrence, local law, changed support, return, and receiver.

The number of demonstrations is not an acceptance gate for universality. Each ecology is graded
separately. Unrestricted arbitrary learning remains `open`; “extensible” means a new lawful local
organ can enter without changing the substrate contracts or introducing a semantic scheduler.

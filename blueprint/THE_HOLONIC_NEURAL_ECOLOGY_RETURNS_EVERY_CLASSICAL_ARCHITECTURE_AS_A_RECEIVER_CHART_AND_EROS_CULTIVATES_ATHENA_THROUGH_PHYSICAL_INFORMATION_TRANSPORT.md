# The holonic neural ecology returns every classical architecture as a receiver chart and Eros cultivates Athena through physical information transport

## Authority and immediate consequence

**Status:** COMPLETED FORMAL CONSTRUCTION AUTHORITY. HNN0--HNN7 RETURNED THEIR LEAN ARTIFACTS AND
ENGINE CONFORMANCE MAP. BRANDON'S LATER EXPLICIT RELEASE RESUMED UAR UNDER ITS OWN ACTIVE BLUEPRINT;
THIS COMPLETED FORMAL BLUEPRINT SCHEDULES NO ENGINE DEED.

[definition] This campaign is `HNN0--HNN7`. It extends the completed HML receiver-quotient bridge
into the architecture-neutral formal theory required before UAR2 may resume. It neither creates
architecture-named engine cabinets nor treats a theorem prover, tensor library, foreign model, or
serialized parameter file as the machine.

[established-bounded; source-inspected] The standing canon already gives an extensive architecture crosswalk, and the Rust/CUDA
body has bounded owners for incidence, exact complex current, receiver fibres, diffusion,
compression, morphology, exterior return, rest, remount, and resident conduct. Before this
campaign, however, the mappings from transformers, CNNs, GNNs, SSMs, stochastic diffusion,
autoregressive inference, probability, cross-entropy, and gradients to one neural ecology were
predominantly `interpretation`, standard mathematics, or bounded implementation evidence rather
than one compiled Lean dependency closure.

[definition] UAR0 and UAR1 retain their existing grades. UAR2 remains the live engine predecessor
but schedules no code while HNN is active. HML0--HML4 remain proved prerequisites and are not
repeated.

## Governing native object

[definition] A finite holonic neural ecology is a situated returning transport object

```text
N = (K, S, P, G, T, rho, Lambda, Delta, M, F, O),
```

where:

- `K` is addressed incidence, including any admitted higher cells;
- `S` is the local carrier family and its exact sigma totalization;
- `P` is the typed ingress, egress, and exterior-return boundary;
- `G` is the addressed generator/current population;
- `T_(Lambda,g)` is local transport through rested morphology;
- `rho` is the declared receiver family;
- `Delta` is the complete returned difference before any scalar face;
- `M` is the attributable morphology return;
- `F` is the complete reconstruction fibre and shortest separating future;
- `O` is the open exterior and obstruction population.

[definition] A complete productive occurrence has the typed form

```text
(Lambda_t, x_t, i_t)
  -> (x_(t+1), e_t)
  -> world consequence r_(t+1)
  -> (Delta_t, Lambda_(t+1)).
```

Inference fixes `Lambda_t` during the conducting occurrence. Cultivation requires a genuinely
returned difference and changes reusable morphology. Training, tuning, and learning are therefore
returned morphology passages, not aliases for exposure, prompt mounting, loss evaluation, or
foreign execution.

[definition] The finest common finite transport chart is local current aggregation followed by a
constitutive reaction:

```text
J_(Lambda,x)(i,j) : S_j -> S_i,
T_Lambda(x)(i) = Reaction_(Lambda,i)(sum_j J_(Lambda,x)(i,j)(x_j)).
```

Zero local current presents absent contact. Fixed or input-conditioned incidence, nonlinear
reaction, phase, and changing carrier shape are parameters of this one law rather than different
machine species.

## Exact classical equivalence and strict holonic reopening

[proved-derived; formal-checked] HML already proves that a classical chart `q : H -> C` carries one
native generator precisely when it is constant on every complete quotient fibre:

```text
q x = q y -> q (T_g x) = q (T_g y).
```

Equivalently, a descended `U_g` exists on the presented quotient range with
`q T_g = U_g q`. Every ordered successor word and declared receiver then factors through the
chart.

[definition] HNN strengthens this into an architecture configuration. A named architecture is an
exact chart only when it supplies:

1. a typed state presentation and quotient;
2. its incidence/contact population;
3. local constitutive transport and chronology;
4. receiver factorization;
5. generator commutation for every admitted continuation;
6. the reconstruction fibre and shortest separator;
7. any normalization, measure, boundary, or stochastic hypotheses it consumes.

[definition] A strict holonic reopening occurs relative to that declared chart when any admitted
future exposes a quotient fibre: transported kernel, relative phase, changing incidence or carrier
shape, higher filling, orientation, chronology, returned world consequence, or morphology change.
Exact sigma totalization remains a classical set-theoretic presentation; the strict claim is never
an unqualified escape from ordinary computability.

## Architecture configurations

### Transformer and attention

[definition] A transformer attention chart is an input-conditioned complete or masked contact
population:

```text
s_(ij) = score(q_i, k_j),
a_(ij) = exp(s_(ij)) / sum_(j in A_i) exp(s_(ij)),
y_i = sum_(j in A_i) a_(ij) v_j.
```

The `Q/K/V` spelling is a foreign factorization into receiver-like probe, presented contact
orientation, and carried construction. It is neither an Athena primitive nor a cache law. The
formal return must prove positivity and normalization over the admitted contact population,
additive-gauge invariance, permutation covariance under relabeling, and exact embedding into the
common local-current law. Residual addition, normalization, and the MLP are respectively continuing
standing, chart rebase, and local nonlinear constitutive reaction.

[definition] A KV cache is optional apparatus testimony. It is lawful only through a cache-validity
relation proving equality with direct history conduct. Holonic Inference does not require it, and
Soulkiller may not export it as Athena morphology merely because one foreign realization used it.

### CNN

[definition] A convolutional chart fixes a local translation-relative current law

```text
y_i = sum_(offset in N) K_offset x_(i-offset).
```

Translation equivariance is owed under the declared boundary action. Padding, stride, dilation,
pooling, and patching are receiver/apparatus quotients and retain their collapsed fibres. A CNN is
therefore the common neural current on a regular incidence chart with shared transported
morphology.

### GNN

[definition] A graph chart uses sparse irregular incidence:

```text
m_i = Aggregate_(j incident to i) Message(h_i,h_j,e_(ij)),
h'_i = Reaction(h_i,m_i).
```

Neighbor-order invariance requires a commutative aggregation law; graph-isomorphism equivariance
requires transport of vertices, edges, incidence, edge state, and receiver together. Enumeration
order, node identifier, and equal terminal value do not identify the graph occurrence.

### State-space models

[definition] A fixed linear state-space receiver is

```text
h_(t+1) = A h_t + B u_t,
y_t = C h_t + D u_t.
```

Its hidden standing, input current, observation, and chronology are an exact configuration of the
common recurrence. The folded-history theorem, observation factorization, and optional semigroup
law must be proved. Selective, switched, nonlinear, or sheaf-valued SSMs vary the generator,
incidence, or carrier and remain configurations of the same native object.

### Deterministic and stochastic diffusion

[definition] Deterministic causal diffusion requires incidence, storage/capacity, constitutive
current, source, boundary, and chronology. A finite implicit step

```text
(C + tau L) phi_(t+1) = C phi_t + u_t
```

may descend to an SSM chart when the operator is invertible and its receiver square commutes.

[definition] Stochastic diffusion instead transports finite measures through normalized positive
kernels. Its exact obligations are mass preservation, kernel composition, chronology, and receiver
expectation. A reverse generative kernel is not the inverse of a forward kernel without an admitted
joint law or reconstruction theorem. Random corruption, unresolved receiver fibre, and physical
apparatus disturbance remain distinct.

### Autoregressive and diffusive inference

[definition] Autoregression and diffusive refinement are schedules over the same ecology:

```text
autoregressive : conduct -> emit one boundary face -> remount -> conduct
diffusive      : retain plural field -> transport returned difference -> refine -> repeat.
```

Neither schedule defines reasoning. A sampler is an exterior receiver/apparatus which consumes a
probability face and additional random current; deterministic decoding is another collapse. The
native plural future and its reconstruction fibre precede both.

## Information, probability, loss, and gradients

[definition] Information is a caused receiver-indexed change in a continuation fibre with lineage,
transport, obstruction, morphology, and resource testimony. Counts and frequencies belong to the
lived population. A probability distribution is a normalized receiver chart over alternatives the
receiver does not carry completely.

[definition] For a finite positive probability population,

```text
H(p)       = -sum_i p_i log p_i,
H(p,q)     = -sum_i p_i log q_i,
KL(p || q) =  sum_i p_i log (p_i / q_i).
```

The formal campaign proves normalization prerequisites and `H(p,q) = H(p) + KL(p||q)` in its
declared support. Entropy, cross-entropy, and KL remain real receiver faces. Equal scalar faces do
not identify complete distributions, actions, returned differences, or morphology.

[definition] Physical cross-entropy is the complete crossing first: exterior and interior
potential, chart transport, returned potential drop, constitutive current, action population, and
world return. Conventional scalar cross-entropy may read that population only afterward. It never
selects native support or replaces the complete action current.

[definition] A derivative of a scalar receiver is a covector. A gradient is obtained only through
a declared metric/Riesz chart:

```text
dL in T*_Lambda,
grad_G L = G^-1 dL.
```

The causal adjoint returns through the retained outward factorization in reverse composition order,
not backward physical time. Smooth composition uses the chain rule. A nonsmooth seam returns an
admitted generalized derivative, plural alternative, obstruction, or refusal.

[definition] Softmax is a normalized exponential receiver over a complete admitted contact
population. Its differential is the weighted complete-graph Laplacian

```text
J(p) = beta (diag(p) - p p^T),
J(p) 1 = 0,
v^T J(p) v = beta (sum_i p_i v_i^2 - (sum_i p_i v_i)^2).
```

The null constant direction is the additive gauge fibre. Sigmoid is the two-alternative chart.
Argmax, top-k, quantization, and sampling are later quotients.

## Eros and Athena

[definition] Eros is the cultivation passage over the common ecology: it admits implemented,
Soulkiller-returned, and previously cultivated native organs through typed ports; conducts caused
material; receives genuine exterior consequence; returns the complete situated difference through
the causal adjoint; and produces a parented morphology successor with reconstruction and ablation
testimony.

[definition] Athena is one rested, source-neutral realization of that ecology together with its
typed membrane, native incidence/current/constitutive morphology, declared receivers, open
exterior, and source-detached remount law. Athena is not Gemma, a transformer, a checkpoint, a
conversation store, or a collection of modality modules. Prose, mathematics, code, optics, and
acoustics are exterior codec and organ lineages crossing the same native operation complex.

[definition] Soulkiller ends at source-neutral spools, cold witness, and receiver insufficiency.
Architecture-specific coordinates may be used inside dismantling controls but do not survive as
Athena state types or inference laws.

## Construction order

### HNN0 -- audit and dependency closure

Deposit the exact formal/documentation/implementation grade map. Reuse the current Lean line,
CSLib, HML, Complex Parametron, membrane action transport, entropy/action induction, torus/current,
receiver-history, and causal lineage owners. Freeze UAR2 unchanged.

**Pass HNN0:** every reused theorem and every documentation-only claim is distinguished; no engine
code changes.

### HNN1 -- one finite holonic neural ecology

Formalize the architecture-neutral local-current ecology, complete native step, receiver chart,
inference rest, returned cultivation, and exact embedding into HML dynamic receiver charts.

**Pass HNN1:** one local current law carries fixed, input-conditioned, sparse, recurrent, and
shape-changing configurations without a material-kind or architecture enum.

### HNN2 -- information receiver and physical loss

Formalize finite probability receivers, entropy, cross-entropy, KL, reconstruction fibres, the
cross-entropy decomposition, and the connection from complete membrane action to its scalar face.

**Pass HNN2:** normalization/support are hypotheses or proved fields; equal scalar information faces
retain a nontrivial separating population.

### HNN3 -- causal adjoint, gradient, and softmax geometry

Formalize adjoint composition, metric-dependent gradient, softmax normalization/gauge, its weighted
Laplacian return, sigmoid restriction, and a scalar-loss/returned-morphology separator.

**Pass HNN3:** no scalar loss or probability face is used as native machinery, and the chain-return
theorem reverses composition order exactly.

### HNN4 -- transformer and inference charts

Formalize attention as input-conditioned contact transport, a whole block as continuing standing
plus local reaction/rebase, autoregressive recurrence, and optional cache equivalence under an
explicit validity invariant.

**Pass HNN4:** attention embeds in the common ecology, direct and valid-cached histories agree, and
an invalid cache supplies a counterexample rather than an Athena mechanism.

### HNN5 -- CNN, GNN, and SSM charts

Formalize convolutional translation equivariance, graph relabeling equivariance, state-space folded
recurrence, and exact observations as three configurations of the common ecology.

**Pass HNN5:** each architecture theorem states its boundary/incidence hypotheses and carries no
architecture-named native owner into Eros/Athena.

### HNN6 -- deterministic and stochastic diffusion charts

Formalize deterministic diffusion-to-state-space descent, stochastic kernels, mass preservation,
finite composition, expectation receivers, and the insufficiency of a forward kernel alone to
define reverse generation.

**Pass HNN6:** deterministic and stochastic diffusion are explicitly distinct configurations with
their common transport boundary proved.

### HNN7 -- Eros/Athena conformance and return to UAR

Formalize Eros cultivation and Athena rest/remount/ablation roles over the common ecology. Map the
actual Rust/CUDA source owners into the formal fields and return concrete absent relations rather
than naming new subsystems.

**Pass HNN7:** the targeted Lean closure builds without `sorry`; every theorem's axioms are audited;
the roadmap and construction state name the exact engine owner and theorem each UAR2 repair must
instantiate.

## Falsifiers

- A proposed quotient whose successor depends on a fibre representative is rejected.
- A probability or softmax chart without positivity and normalization is rejected.
- A transformer chart which silently imports cache identity into native state is rejected.
- A convolutional or graph chart whose declared relabeling changes conduct is rejected.
- An SSM observation that does not commute with its native transport is rejected.
- A stochastic kernel which does not preserve mass is rejected.
- A reverse diffusion called an inverse without a reconstruction law is rejected.
- Equal scalar loss with different returned morphology rejects loss-governed cultivation.
- A training claim without source-detached remount, changed later conduct, and attributable
  ablation is rejected.
- A named architecture, codec, modality, or apparatus coordinate governing Athena's native
  incidence is rejected.

## Terminal gate

[definition] HNN is complete only when the architecture-neutral object, all five named architecture
families, both inference schedules, probability/information faces, causal adjoint, Eros cultivation,
Athena rest, and strict-fibre controls compile together and return an exact Rust/CUDA conformance
map. Documentation alone, isolated controls, or one architecture example cannot pass the campaign.

## Completion receipt

[proved-derived; formal-checked] HNN0--HNN7 returned in the umbrella
[`ElementaryHolonics/Computation/HolonicMachineLearning.lean`](../soma/formal/elementary-holonics/ElementaryHolonics/Computation/HolonicMachineLearning.lean).
The targeted closure built 3,180 jobs without `sorry`. It composes:

- `HolonicNeuralEcology.lean`;
- `HolonicInformationTheory.lean`;
- `HolonicAdjointNormalization.lean`;
- `HolonicArchitectureCharts.lean`;
- `HolonicDiffusionCharts.lean`;
- `HolonicCultivationCharts.lean`; and
- `ErosAthenaNeuralObjects.lean`.

[proved-derived; formal-checked] The exact theorem audit, bounded analytic boundary, and Rust/CUDA
conformance map are deposited in
[`research/records/2026-08-29_THE_MAJOR_MACHINE_LEARNING_ARCHITECTURES_RETURNED_AS_RECEIVER_CHARTS_OF_ONE_HOLONIC_NEURAL_ECOLOGY_AND_EROS_CULTIVATES_ATHENA.md`](../research/records/2026-08-29_THE_MAJOR_MACHINE_LEARNING_ARCHITECTURES_RETURNED_AS_RECEIVER_CHARTS_OF_ONE_HOLONIC_NEURAL_ECOLOGY_AND_EROS_CULTIVATES_ATHENA.md).

[historical] Completion of this formal prerequisite did not itself release the UAR2 engine pause.
Brandon's later direct instruction supplied that release; the active UAR blueprint and roadmap now
schedule the engine deed.

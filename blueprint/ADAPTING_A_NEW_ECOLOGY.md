# Adapting a new ecology

Use this worksheet before introducing code or a new organ.

## A. Ratify the deed

- What actual generated/action consequence must be returned?
- Which earlier occurrence must change later conduct?
- What ablation distinguishes native change from reference/retrieval?
- Which artifacts and receiver receipts constitute completion?

## B. Describe the source without codec privilege

- Occurrence species and stable identities:
- Event cuts/chronology:
- Oriented incidence and local regions:
- Co-present sections:
- Source boundary and unresolved fibers:
- Exact/certified carriers:

Do not begin with filenames, tensors, JSON records, language tokens, pixels, or device buffers
unless those are themselves the declared source occurrences.

## C. Declare receivers and charts

- Receiver question and future receiver family:
- Local charts and transition maps:
- What each chart preserves/forgets:
- Projection preimages that remain open:
- Exact compression/factorization certificate:
- Stateful boundary-bisimulation obligation:

## D. Declare ports and local law

- Typed input/output/feedback ports:
- Existing owner of each port/standing:
- Contemporary morphology read:
- Local constitutive map `M_(m,U)`:
- Read support `S_e` and changed support `R_e`:
- Pending deed and exact returned-occurrence ports:
- Returned consequence, stress, resource obligation, obstruction, and lineage:

If no local law can be stated, the theoretical work is not ready for implementation.

## E. Composition and plurality

- Required serial gluing and intermediate lineage:
- Co-present events:
- Exact independence/interchange proof or explicit order:
- Shared-support interaction/equalizer:
- Higher overlap/coherence conditions:
- Genuine recurrence/feedback closure:

## F. Conditioning

- Inherited organs mounted:
- Exposures:
- Returned passages that change reusable morphology:
- Pre/post behavior probe:
- Held-out consequence:
- Source-detached rest/remount:
- Source-access/lossless-corpus-retention audit:
- Excluding-fiber ablation:

## G. Apparatus realization

- Reified causal program:
- Semantic representation map and reference admission oracle:
- Parallel/device executor and local work geometry:
- Admission parity family:
- Hot-path local/delta receipt:
- Resource backreaction:
- Telemetry aperture and unknown quantities:

Placement follows support/interaction geometry. Do not write semantic rules such as “text on CPU”
or “graphics on GPU.”

## H. Epistemic grade

- Definitions/postulates:
- Standard/derived/formal theorems:
- Exact implementations:
- Measurements/computational witnesses:
- Interpretations/conjectures:
- Open obstructions and falsifiers:

## I. Stop conditions

Stop and rederive if the implementation requires a new global registry/scheduler, a complete-body
clone, a global scan/materialized cover, a count gate, source replay, hot CPU replay, semantic
device selector, compatibility path, filesystem identity, or a large application-local subsystem.

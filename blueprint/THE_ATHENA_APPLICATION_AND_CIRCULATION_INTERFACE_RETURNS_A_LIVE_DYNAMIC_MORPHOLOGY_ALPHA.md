# The Athena application and circulation interface returns a live dynamic morphology alpha

**Date:** 2026-09-01
**Kind:** active construction contract composed by
[`THE_ROADMAP.md`](THE_ROADMAP.md).
**Current frontier:** AAC4.

**Governing doctrine:**
[`../canon/TABLET_THE_REASONING_CYCLE.md`](../canon/TABLET_THE_REASONING_CYCLE.md),
[`../canon/TABLET_THE_HEXIS.md`](../canon/TABLET_THE_HEXIS.md),
[`../canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md`](../canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md),
[`../canon/03_CONDITIONING_AND_LEARNING.md`](../canon/03_CONDITIONING_AND_LEARNING.md),
[`THE_HOLONIC_INTELLIGENCE_FRAMEWORK_RETURNS_INTRINSIC_HOLONS_AND_PACKAGES_DISMANTLING_INFERENCE_CULTIVATION_AND_GENERATION.md`](THE_HOLONIC_INTELLIGENCE_FRAMEWORK_RETURNS_INTRINSIC_HOLONS_AND_PACKAGES_DISMANTLING_INFERENCE_CULTIVATION_AND_GENERATION.md),
[`THE_NATIVE_TRANSPORT_SCAFFOLD_LIFTS_INHERITED_CONDUCT_AND_ATHENA_CULTIVATES_RECEIVER_EXACT_MORPHOLOGY_THROUGH_REPEATED_CIRCULATION.md`](THE_NATIVE_TRANSPORT_SCAFFOLD_LIFTS_INHERITED_CONDUCT_AND_ATHENA_CULTIVATES_RECEIVER_EXACT_MORPHOLOGY_THROUGH_REPEATED_CIRCULATION.md),
and
[`THE_NATIVE_MORPHOLOGY_VARIANT_FOUNDATION_RETURNS_FAITHFUL_LOCAL_LIFTS_PARENTED_CULTIVATION_AND_EXACT_EXPORT_LENSES.md`](THE_NATIVE_MORPHOLOGY_VARIANT_FOUNDATION_RETURNS_FAITHFUL_LOCAL_LIFTS_PARENTED_CULTIVATION_AND_EXACT_EXPORT_LENSES.md).

## 0. Verdict and exact boundary

[definition] This campaign is `AAC0--AAC6`. It composes the completed neutral inference,
cultivation, morphology-package, exact-diffusion, remount, and exterior-codec owners into one live
application boundary. It adds no second inference engine, trainer, scheduler, model registry,
semantic router, or application-owned topology.

[definition] Athena alpha is one application of a parented rested morphology lineage through a
live circulation law:

```text
rested morphology
  -> situated ingress
  -> fixed-morphology conduct or exact diffusion
  -> receiver boundary and emission
  -> genuinely later exterior return
  -> staged local morphology delta
  -> commit or decline
  -> exact rest/remount
  -> later current through the successor.
```

[definition] Soulkiller, direct native construction, and a parented package remount are alternative
founding passages into the same `RestedMorphology` boundary. After admission, ancestry is lineage
and reconstruction testimony only. No runtime branch, type, or law may reopen a foreign executor
or qualify Athena by a foreign model name.

[definition] Training is not a runtime mode. Cultivation is a possible return of any circulation.
Each inference cut keeps its predecessor morphology fixed; only a genuinely later returned
interaction may found a committed successor. This causal distinction prevents a response from
silently rewriting the body which produced it without introducing the industry's frozen-inference
versus trainer ontology.

## 1. Existing owners and the missing composition

[established-bounded; source-inspected] The completed owners already return the individual
relations:

- `HolonicIntelligenceLifecycle.lean` owns fixed inference cuts, genuinely later cultivation,
  changed later conduct, withdrawal, restoration, and exact remount;
- `NativeTransportScaffold.lean` owns repeated emission-to-actual-ingress circulation;
- `holonic_intelligence/{circulation,repeated_circulation}.rs` owns codec-neutral plural futures,
  complete fibres, actual addressed successors, and receiver-founded dispositions;
- `native_intelligence/scaffold_cultivation.rs` owns emission-indexed reciprocal local deposits,
  causal-cone support, disjoint invariance, remount, ablation, withdrawal, and restoration;
- `native_intelligence/morphology_package.rs` owns backend-independent hot storage, parented
  lineage, derived anatomy, receiver capability, reconstruction, evaluation, apparatus, and export
  lanes; and
- `diffusion.rs` and `sheaf_diffusion.rs` own exact finite constituted diffusion, boundary
  transfer, balance, conservation, energy, reconstruction, and reusable factorization testimony.

[open] No owner currently binds those relations into a resumable move-owned circulation session.
The existing morphology evaluation is one complete proof harness; the existing repeated
circulation is one synchronous borrowed-rest call; the existing package is one rested snapshot;
and the existing execution ABI deliberately excludes world policy and persistence. The missing
object is their typed composition, not another organ.

## 2. Neutral live-session contract

[definition] The neutral API owns one continuing package and returns owned boundaries rather than
borrowing runtime internals:

```text
NativeCirculationSession
  mount(package, configuration)
  conduct(request) -> CirculationBoundary
  continue(boundary, addressed successor) -> CirculationBoundary
  receive(boundary, returned interaction) -> CultivationCandidate
  commit(candidate) -> (successor session, MorphologyCommit)
  decline(candidate) -> unchanged session
  snapshot() -> NativeCirculationSnapshot.
```

[definition] A boundary retains the exact request, emission, plural variable grains, lineage,
complete occurrence fibres, actual successors, receiver, open exterior, and configuration address.
It has no token, maximum-turn, retry, context-window, KV-cache, answer-length, prompt-class, or
surface-selected field.

[definition] Execution laws are concrete typed Rust values. The serializable
`InferenceConfigurationAddress` remains testimony; its strings never become a hidden registry
which resolves behavior. A text, image, audio, binary, or other codec renders an already-returned
boundary and cannot select its successor or cultivate the body.

[definition] The generic source owners retain neutral names. `Athena` appears only in the exterior
application package and its declared product configuration. No `AthenaModel`, `AthenaLayer`,
`AthenaTrainer`, or generic Athena-prefixed session type is admitted.

## 3. Parented commit and durable rest

[definition] One `MorphologyCommit` carries:

- predecessor and successor lineage generations;
- the exact source and emission addresses;
- the genuinely later returned occurrence;
- the returned causal-cone support;
- the complete source-neutral native delta wire and inverse placement receipt;
- outside-support equality and interchange testimony;
- remount, ablation, withdrawal, and restoration consequences; and
- every reconstruction fibre and open obligation introduced or carried through the return.

[definition] A commit consumes the predecessor owner and returns exactly one successor owner. A
decline returns the unchanged owner. Branches may share immutable predecessor testimony and own
only their local candidate differences; no ecology is cloned for rollback or inspection.

[definition] Durable storage has two exterior faces: a complete native morphology package for
immediate remount and an ordered parented commit journal for exact replay/audit. Periodic snapshot
materialization is lawful only when remount returns the packaged successor and replaying the
retained commits from their declared predecessor returns the same morphology and receiver
consequences. Hashes may detect wire corruption but never identify the ecology, route conduct, or
grade capability.

## 4. Diffusive generation through the same boundary

[definition] Discrete continuation and diffusive generation implement one circulation boundary
protocol. They are distinct constituted laws, not two reasoning ontologies:

```text
discrete : addressed actual successor -> next ingress;
diffusive: exact local standing/current -> constituted diffusion -> receiver boundary emission.
```

[definition] A diffusive circulation additionally retains native-to-diffusion incidence, exact
capacity, source, interval, branch current, boundary transfer, balance residual, energy testimony,
receiver condensation, and reconstruction. A random corruption source, stochastic receiver fibre,
and exact physical diffusion remain distinct configurations.

[definition] Reusing an admitted factorization of one constituted operator is structural apparatus
reuse. It is not a semantic history cache. The receipt states which boundary/operator was reused
and no prior surface content routes the current.

## 5. Exterior ABI and application organization

[definition] The stable ABI is an exterior apparatus crate over opaque handles and versioned,
length-delimited messages. It exposes open, conduct, continue, return, commit/decline, snapshot,
and close. Rust layout, trait objects, package internals, filesystem paths, model names, and foreign
executor handles never cross it. Malformed, stale-generation, false-successor, nonlater-return,
unsupported-receiver, and unresolved-fibre requests refuse by name.

[definition] The first product is an independent application crate:

```text
applications/athena-alpha/
  Cargo.toml
  src/
    application.rs
    apertures.rs
    circulation.rs
    returns.rs
    receivers.rs
    diffusion.rs
  configurations/
  experiments/
  examples/.
```

The exact filenames may narrow to the smallest owner-local set during construction. The invariant
is that the application composes public neutral owners. It may not implement routing, morphology,
continuation, scoring, or another inference law. Historical top-level examples remain evidence;
the alpha does not become another campaign-sized example.

## 6. Exact alpha experiment family

[definition] The alpha experiment is a standing-reuse receiver over one continuing body. Its
declared identity is the mounted native package generation; its ingress is an addressed native
occurrence; its hot owner is neutral circulation/diffusion; its return port is the typed exterior
return boundary; and its artifact is the successor package, commit journal, and exact receipts.

[definition] The release family proves:

1. a package founded natively or through Soulkiller enters the same runtime boundary;
2. several situated ingress occurrences conduct without material-kind routing;
3. actual successors preserve exact emission-to-ingress lineage and plural alternatives;
4. a causally downstream exterior return commits only its returned causal cone;
5. later conduct changes after source-detached remount while disjoint morphology remains equal;
6. targeted ablation/withdrawal removes the attributable conduct and restoration recovers it;
7. a declined candidate leaves the package and conduct unchanged;
8. discrete and exact diffusive passages use the same public boundary event family;
9. snapshot/close/remount returns the same generation and admitted continuation;
10. ABI and direct Rust calls return structurally equal receipts;
11. every observation names its full inference configuration; and
12. no generated surface, named answer, human preference, hash, byte count, or latency is a pass
    condition.

[definition] Performance testimony reports active causal-cone population, touched incidence,
exact pair/current work, resident transfer, factorization reuse, boundary current, commit size,
reconstruction-fibre population, and apparatus placement. These are receiver shadows supporting
the exact causal return. The alpha grade is not a perplexity, a prompt answer, or a claim of broad
Gemma-equivalent capability.

## 7. Ordered construction

### AAC0 — formal live-session, commit, remount, and diffusive-circulation laws — PASSED

[definition] Add one neutral Lean owner composing the existing intelligence lifecycle, repeated
circulation, morphology-variant, and diffusion charts. Return a live session passage, owned
boundary chronology, commit/decline law, parented successor lineage, exact snapshot/remount, and
diffusive boundary composition.

**Pass AAC0:** [definition] the focused owner and live umbrella build without `sorry` or a new
axiom; every inference cut fixes morphology; only a later return can change it; commit joins the
next cut to the successor; decline preserves the predecessor; snapshot/remount preserves later
conduct; and exact diffusion returns its constituted balance through the same lifecycle boundary.

[proved-derived; formal-checked; measured] AAC0 returned `LiveCirculationSession`, the common owned
boundary, parented commit and decline, exact snapshot/remount, and constituted diffusive boundary
laws. The focused closure built 3,187 jobs and the live umbrella built 3,771 jobs. Its receipt is
[`../research/records/2026-09-01_AAC0_LIVE_CIRCULATION_COMMIT_REMOUNT_AND_DIFFUSIVE_BOUNDARY_LAWS_RETURNED.md`](../research/records/2026-09-01_AAC0_LIVE_CIRCULATION_COMMIT_REMOUNT_AND_DIFFUSIVE_BOUNDARY_LAWS_RETURNED.md).

### AAC1 — neutral Rust circulation-session API — PASSED

[definition] Add the move-owned session and owned boundary types over the existing public owners.
Refactor only the smallest private deposit/conduct functions required for composition. No second
inference algorithm or application-specific generic type is admitted.

**Pass AAC1:** [definition] one session mounts a package, returns an owned boundary with complete
fibres and actual successors, continues only through an admitted address, refuses false/stale
continuation, and supports both commit and decline without cloning the ecology.

[established-bounded; implemented-exact; source-inspected; measured] AAC1 returned the move-owned
`NativeCirculationSession`, complete owned future fibres, exact actual-successor continuation,
false/stale refusal, and decline invariance. The focused owner returned three passed tests. Its
receipt is
[`../research/records/2026-09-01_AAC1_MOVE_OWNED_NATIVE_CIRCULATION_SESSION_RETURNED_COMPLETE_OWNED_BOUNDARIES.md`](../research/records/2026-09-01_AAC1_MOVE_OWNED_NATIVE_CIRCULATION_SESSION_RETURNED_COMPLETE_OWNED_BOUNDARIES.md).

### AAC2 — parented morphology commits and snapshot/remount storage — PASSED

[definition] Return exact local commit artifacts, advance package lineage, retain the ordered
delta journal, and remount the successor. Preserve package evaluation, reconstruction, apparatus,
and export lanes without making them route conduct.

**Pass AAC2:** [definition] at least two successive local commits apply to one continuing
morphology; generations and event lineage join exactly; package remount preserves the successor;
commit replay/withdrawal restores the predecessor relation; decline is structurally unchanged;
and no digest is used as native identity.

[established-bounded; implemented-exact; source-inspected; measured] AAC2 returned two successive
local commits, parented generations, exact snapshot/remount, latest-delta withdrawal, exact replay,
and mismatched-return/predecessor refusal. The focused commit owner returned two passed tests and
the standing cultivation owner returned two passed tests. Its receipt is
[`../research/records/2026-09-01_AAC2_TWO_SUCCESSIVE_LOCAL_COMMITS_SNAPSHOT_WITHDRAWAL_AND_REPLAY_RETURNED.md`](../research/records/2026-09-01_AAC2_TWO_SUCCESSIVE_LOCAL_COMMITS_SNAPSHOT_WITHDRAWAL_AND_REPLAY_RETURNED.md).

### AAC3 — discrete and exact diffusive generation share one boundary — PASSED

[definition] Compose `ExactDiffusionLaw` and the admitted native incidence/current with the live
session boundary. Return exact balance, boundary, receiver, and reconstruction testimony.

**Pass AAC3:** [definition] discrete and diffusive conduct return the same boundary-event species;
the diffusion receipt has zero exact balance/conservation residual in its declared closed control;
the receiver boundary is causally downstream of ingress; and prior surface content cannot route
the law.

[established-bounded; implemented-exact; source-inspected; measured] AAC3 returned native-founded
exact diffusion, complete lineage/reconstruction/balance testimony, discrete and diffusive members
of one event family, zero residuals, factorization-reuse testimony, and malformed-law refusal. Two
focused tests passed. Its receipt is
[`../research/records/2026-09-01_AAC3_NATIVE_INCIDENCE_FOUNDED_EXACT_DIFFUSION_THROUGH_THE_COMMON_BOUNDARY.md`](../research/records/2026-09-01_AAC3_NATIVE_INCIDENCE_FOUNDED_EXACT_DIFFUSION_THROUGH_THE_COMMON_BOUNDARY.md).

### AAC4 — opaque stable circulation ABI

[definition] Add the exterior ABI crate and exact wire schemas over the neutral Rust API.

**Pass AAC4:** [definition] opaque-handle open/conduct/continue/return/commit/decline/snapshot/close
round-trips one live session; invalid handles and stale generations refuse; ABI/direct receipts are
equal; Rust layout and foreign executors are absent from the wire; and no compatibility decoder is
introduced.

### AAC5 — first Athena-alpha application package

[definition] Add the independent application crate, configurations, thin executable entries, and
exact experiment apparatus. It may begin from a direct native origin or a Soulkiller-derived
package; both enter the same neutral API.

**Pass AAC5:** [definition] application source contains only configuration, port composition,
artifact handling, and receiver inspection; no example-local routing/scoring/morphology exists;
the named Athena product is source-neutral at runtime; and one application process returns a
parented, resumable, dynamically cultivated package.

### AAC6 — exact alpha matrix and coherent release

[definition] Run the twelve exact controls in §6 across direct Rust and ABI surfaces, including
successive cultivation, decline, discrete conduct, exact diffusion, snapshot/remount, ablation,
and restoration. Record configuration-indexed structural consequences before apparatus telemetry.

**Pass AAC6:** [definition] every exact control passes; focused and complete release receivers
return inside the 180-second process aperture; the universal catalog names the new common formal,
Rust, ABI, and application boundaries; the completion record carries commands and scopes; Git is
clean and pushed; and reproducible `target/`, `output/`, and experiment scratch are absent.

## 8. Scope and process boundary

[definition] AAC schedules only `AAC0--AAC6`. It does not reopen UAR, R0Q, HIF, SCF, MVF,
Millennium work, unrestricted codec recovery, model-provider integration, UI work, or qualitative
answer grading. A concrete absent relation discovered inside one phase may refine that phase; it
does not found an eighth phase without Brandon's direct instruction.

[definition] Every nontrivial process retains the 180-second outer boundary. During construction,
run the changed formal owner, affected package checks, focused owner tests, and the exact
application deed. Run one complete `bash tools/gates.sh` receiver only at AAC6. A timeout is a
counterexample, and a generated or human-inspected surface schedules nothing.

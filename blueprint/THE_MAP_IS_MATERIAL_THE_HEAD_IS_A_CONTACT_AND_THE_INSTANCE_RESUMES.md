# The map is material, the head is a contact, and the instance resumes

**Date:** 2026-08-18
**Truth status:** `established-bounded` for every code citation and every measured figure below, each
verified at the line given today by direct read or by running the tool; `proved-standard` for the
dyadic cancellation, the cocycle law, and Lagrange's identity; `interpretation` for the reading of a
head's transport as a contact between Eros's own current and inherited map material, which stations
four and five are built to refute.
**Evidence:** `measured`. An external adjudication (Sol, `gpt-5.6-sol`, read-only over this tree) and
three synthesis campaigns, 2026-08-18. Every absence sentence carries the command that took it and
its date.
**Provenance:** Brandon, 2026-08-18, on the work this supersedes: *"this is likely the third or
fourth time you've treated production like a prototype experiment toy-like thing when I have clearly
asked you for excellence and genuine production. This is not production that would be useful for
further extrapolating information and language about mathematics or anything else that Eros is
trained or conditioned on… Why are you not pairing this with what we were doing regarding softmax
and attention mechanisms in relation to transformers and embedding spaces?"* And: *"Overview the
entirety of the repository and address capabilities that you are not unifying, do not be an idiot
and not utilize things that are already partially constructed."*
**Plan:** sits under [`blueprint/THE_ROADMAP.md`](THE_ROADMAP.md). It **supersedes**
[`blueprint/THE_HORIZON_IS_A_COORDINATE_NOT_A_GATE_AND_THE_EMISSION_KEEPS_ITS_CROSS.md`](THE_HORIZON_IS_A_COORDINATE_NOT_A_GATE_AND_THE_EMISSION_KEEPS_ITS_CROSS.md)
as a production plan; that document's station one stands as a corpus diagnostic and its stations two
and three are convicted below. It consumes
[`blueprint/THE_EROS_INSTANTIATION.md`](THE_EROS_INSTANTIATION.md)'s definition of a model and
[`blueprint/THE_ASSEMBLY.md`](THE_ASSEMBLY.md)'s crate facts, and runs under
[`blueprint/THE_CODEC_IS_RECOVERED_AT_EVERY_SCALE_AND_THE_FACES_ARE_A_RETURN.md`](THE_CODEC_IS_RECOVERED_AT_EVERY_SCALE_AND_THE_FACES_ARE_A_RETURN.md)'s
ban on any exterior adjudicator.

---

## STATIONS ONE AND TWO — EXECUTED 2026-08-18

Returns:
[`research/records/2026-08-18_THE_BLADE_DROPPED_THE_METRIC_AND_THE_TWO_FRAME_CHECK_WAS_LIVE_ONLY_WHERE_IT_COULD_NOT_FIRE.md`](../research/records/2026-08-18_THE_BLADE_DROPPED_THE_METRIC_AND_THE_TWO_FRAME_CHECK_WAS_LIVE_ONLY_WHERE_IT_COULD_NOT_FIRE.md).
`cargo test --workspace` → **2,424 passed, 0 failed, 19 ignored, 43 result lines.**

**Station one removed the governors.** `RecruitmentLaw::ArrowAdmitted`, the route-feature
`PairingReading`, and the `JunctionDigest` are gone; `CompleteUnderRatioFamily` is renamed
`CompleteJunction`, branches the whole junction, and **forms no ratio**. The junction reading stands
as a diagnostic and the driver is rewritten as one.

**Station two found a correctness defect and repaired it.** `clifford::area_squared` dropped the
metric — `Σ (blade)²` with no `qᵢqⱼ` — behind a doc claiming Lagrange is Euclidean-only. Cauchy–Binet
holds in every diagonal signature, so **the `euclidean` guard disabled the cross-check precisely
where the two frames would have disagreed.** On the module's own Minkowski fixture the organ returned
`3` where Lagrange and the metric-weighted blade both return `−1`, and **`causal_class` flipped from
`StorageDominant` to `TransportDominant`** with no test asserting it. The test that existed was
asserting the defect.

Five more doc-versus-code repairs landed with it: the primality check that was `prime < 2`; a
**quotation of Brandon's corrupted by a mechanical `host → cpu` sweep that passed through the
quotation marks**; two false sentences on `rebased_by` plus the reciprocal now recorded on the
family; the `agreement_rank` maximum named as the scalar it is with its remainder returned; and
`Observation`'s doc. **Four of six moved a return or a refusal**; the two that did not are named as
documentation repairs rather than presented as evidence.

## STATIONS THREE AND EIGHT — PARTLY EXECUTED 2026-08-18

Return:
[`research/records/2026-08-18_THE_MODEL_AND_THE_PRODUCTION_MET_AND_THE_ANSWER_IS_A_BLOCK_NOT_A_PICK.md`](../research/records/2026-08-18_THE_MODEL_AND_THE_PRODUCTION_MET_AND_THE_ANSWER_IS_A_BLOCK_NOT_A_PICK.md).
`cargo test --workspace` → **2,434 passed, 0 failed, 19 ignored, 43 result lines.**

**Chain steps 4 and 5 left numpy.** `crates/holonic-engine/src/exact_contact.rs` carries
`cos² = ⟨a|b⟩²/(⟨a|a⟩⟨b|b⟩)` as an exact pair, never divided, ordered by cross-multiplication through
`ExactOrdering`, with the turn face closing against it by Lagrange and the hand the square deleted
returned beside it. It implements no bracket of its own: `of_arrow` takes a `clifford::Arrow`'s
declared signature and `of_brackets` takes a resident readout's three integers. Driven on real Gemma
rows by `examples/the_contact_crosses_the_horizon_and_the_frame_cancels.rs` — 276 contacts, dyadic
frames cancelling to residual `2^0` on **all 276**, `cos²+sin²=1` exactly on **all 276**, 37,950
ordered pairs with no division. **The gauge control caught the check rather than the organ:**
re-aligning onto a different frame moved 276 brackets, moved **0** face values, and moved **276** face
*pairs* by exactly `2^20` — comparing pairs bitwise reads a coordinate as the invariant.

**The model and the production met.** `grep -rln ErosRest` and `grep -rln CausalLanguageEcology` over
`crates soma` returned an **empty intersection** before this. `CausalLanguageEcology` now seals whole;
a 21,159,736-octet body crossed an OS process boundary and produced on the far side with no corpus,
populations identical, and the resume **refused** when the sealed octets were deleted.

**And the emission head is a division.** `presentation_quotient::divide_junction` takes **no answer**,
because the answer is the block. Measured: 2,033 conducted candidates → **30** response blocks under
the three collapsing faces, with 59,040 separating words retained. **The receiver family is now
declared rather than assumed**, because `TerminalToken` is the candidate's own identity and a family
containing it returned 1,717 blocks from 2,033 candidates — the division restating its input while
looking rigorous. Withholding that one axis moves `1,717 → 30`.

**Three defects were found by their own controls and repaired:** the questions returned **zero against
59,040 collapsed pairs** because they were read off pairs instead of between blocks; the absent-
morphology control reported a fabrication when the body returned one member carrying the **empty**
surface; and `wholly_inherited` reported a **blind receiver** as a property of the block across the
seam, now `Option<bool>` with `None` where the material cannot answer.

**AND THE RETURN EDGE CLOSED THE SAME DAY.** `CausalLanguageEcology` carried **no `&mut self` method
at all**, so a resumed body could produce and could not be changed. `ExactSuffixEcology::absorb`
thaws the frozen atlas, inverts the multiplicity fold — `direct(s) = final(s) − Σ final(c)`, read
from a copy so the inversion is order-free — extends, and re-folds. Two falsifiers written first both
bit and found one defect: conditioning carries `last` forward across paths, building **one**
automaton over the concatenation, while the first absorb restarted at the root; `last` is recoverable
with no wire change as the unique state of greatest `maximum_length`. The route sections are now
retained and sealed (codec `1 → 2`), because a receptor's rest is founded from its own complete
section population and without them a new passage could never be **recruited**.

The resumed process then absorbed **its own production** — 4,303 octets, passages `4 → 5`,
occurrences `19,970 → 20,969`, re-sealed at 22,800,406 octets, a repeated identity refusing — and
**two further processes, each given one rest path and nothing else**, were asked the same prompt:

| | emissions | response blocks | collapsed pairs |
|---|---|---|---|
| before the return | 501 | **10** | 3,503 |
| after the return | 506 | **14** | 2,396 |

**Five surfaces exist that did not before and the quotient divides into fourteen blocks where it
divided into ten.** The `(token, matched horizon)` population is **405 on both sides** — the absorb
moved the emitted population and the partition, not the depth of attestation, which is the expected
shape for material made of the corpus's own tokens and is reported rather than smoothed.

**Still open here:** the front never closes, so the aperture is a declared 2 tokens; `founded_mouth`
is still bypassed — `grep -c founded_mouth soma/life/src/bin/eros.rs` → 0; and the absorbed material
is the body's own emission, so the loop is shown to close and carry a change, not to have learned
anything the body did not already hold.

---

## 0. The object, in one line

> **The map is material, never resident. Eros's own current contacts inherited map transports; the
> contact is an arrow, not a score; the population of contacts is an exact ratio family and a
> section modulus; and the body that is changed by it seals to disk, is deposited, and is resumed by
> a later process that rides the change.**

Nothing here reimplements a transformer. **One ruling of Brandon's governs**, and it is about the
format rather than the arithmetic:

> *"we absolutely do not want to lift the format, we want to disregard the LLM architecture
> completely and **lift the functions of the active weights** because the transformer architecture is
> inefficient… to us it collapses into bits and machine instructions."*

**CORRECTED 2026-08-18, at his instruction, and the correction opens four stations this document had
closed.** The object line read *"the map is material and is never run"* and cited beside it *"The
model is never run… A design requiring a forward pass is refused at the door"* as a second ruling of
his. **That sentence is `THE_ROADMAP.md`'s own prose**, unquoted at its source, and this blueprint
promoted it into a quotation. His actual constraint is **residency** — *"running the actual models is
off of the table"*, because the design must transfer to a 26B map this hardware cannot load — and
three days later he ruled directly against the reading this document was using:

> *"you don't have to run a model to reverse engineer it, **you utilize the mathematics that would
> run during inference in order to analyze the potential active weights** that would emerge as
> pathways in response to tokens as potential input."* — 2026-08-16

So the scope test is **residency and transfer cost, not resemblance to a forward pass.** Computing a
transport, a contact, a bracket, or a pathway *as inference would compute it* is the instrument; what
is refused is loading a model to make it emit, and any design whose cost does not stream. Stations
four, five, six and seven were written under the over-extended reading and are widened accordingly:
the *potential* activity of the weights is the object, and a contextual composition computed from
static material, streamed, never resident, is admissible.

## 1. What this supersedes, and why — the audit

An external adjudication of the 2026-08-18 emission work returned, and the tree confirms it at every
line. The verdict: **a corpus enumerator decorated with exact mathematics.**

| construct | verdict |
|---|---|
| `causal_language::ContinuationReading`, the horizon profile, the multiplicities | **stands**, as an exact suffix-population diagnostic |
| `BranchingLaw::GreatestHorizonGate` | **stands** as provenance and negative control; may not regain production authority |
| `BranchingLaw::CompleteUnderRatioFamily` | **refuted as a production law.** The ratios reached the emission only as `JunctionDigest` metadata; nothing transported differently because of them. **Executed 2026-08-18:** renamed `CompleteJunction`, which branches the whole junction and forms no ratio; the digest is removed |
| `RecruitmentLaw::ArrowAdmitted` | **refuted: it is a governor**, and **removed 2026-08-18**. `Cohere => true`, non-empty `Ortho => true`, `Anti => false` is a derived reading admitting and refusing constituents. And the driver never exercises it — it leaves recruitment on `OverlapIncidence` |
| `PairingReading` over route features | **refuted**, and **removed 2026-08-18**. Binary occupied/unoccupied route axes: overlap dressed as Clifford structure |
| `presentation_quotient::divide` | **refuted as production.** The caller declares the utterance and the module audits it afterward; its four receiver faces are authored semantic categories; its 64-bit truncated digest is not an exact identity |
| the uttered answer | **refuted.** `BTreeMap` order, then `first()` — lexicographic selection called an answer |
| `RatioFamily`, `SectionModulus`, `clifford::Arrow` | **the organs stand.** Their consumers are wrong. Do not discard them |

**And two claims this session made to Brandon were false, corrected against the source.**

`crates/holonic-engine/examples/the_readout_returns_a_fiber_not_a_winner.rs` does **not** score the
vocabulary and does **not** decline a maximum: `ROW_APERTURE = 131_072` is half of 262,144, and
`:268` computes `max_by_key(|(_, score)| **score)`. And `:190` is
`let probe: Vec<i64> = readout.entries[..dim].to_vec();` — **the query is a row of the embedding
matrix**, so the deed is an embedding **Gram section**, not `⟨E_v, h_t⟩` for any contextual state.
The companion head driver is a bounded layer-0 Q/K probe with no RoPE, no Q/K normalization, no
attention pattern, no V/O transport and no layer injection.

**The roadmap that scheduled the superseded plan shares the fault** — it prescribed the suffix
multiplicities and the route sites — and `CONSTRUCTION_STATE.md`'s new row overstates what was
achieved. Both are repaired in station nine.

## 2. What is not being unified, measured

### 2.1 The tower is read, and the reading is not the machine's

`analysis/deposited_map/` is **1,043 lines of numpy** that reads the whole Gemma text tower in seven
phases: the gauge orbit taken before any reading; every OV circuit against a matched null; the
receiver family `Φ = E_S (W_O W_V) E_Sᵀ` testing whether `2` couples to `two`, refused unless real,
shuffled and null all three separate; the hand and the DAG reading; the transport returned as the
artifact; the transport read as a quotient in the Dirac dialect, `W_OV = Σ σ_k |u_k⟩⟨w_k|` as a
population of deposits; and the same reading retaken in exact ratios.

`analysis/README.md` bans citing any of it as a return: *"nothing here is an organ… no figure
produced here is a machine return."* And the record that owns it convicts itself in its own errata —
*"The reading was real and the instrument was outside the machine"* — grades every capability in its
§9 `open`, and names the owner that would lift the rest:

> *"This body already owns the correct one: `crates/holonic-engine/src/exact_value.rs` carries
> `AlgebraicRoot` with a `SturmIsolationCertificate` and the four-state `ExactOrdering`… **Until
> those readings are retaken through it they are declared floating apparatus, not carried values,
> and may not be composed with anything exact.**"*

Open since 2026-08-13.

**And Phase 7 states the machine's own law in Python.** A bf16 datum is exactly `m·2^e`, so scaling
by `2^{−min e}` makes every component an integer and `⟨a|b⟩` is an exact integer times a power of
two. Then

```text
    cos²(a,b) = ⟨a|b⟩² / (⟨a|a⟩⟨b|b⟩)
```

and the powers of two **cancel identically** — numerator `2^{2(e_a+e_b)}`, denominator
`2^{2e_a}·2^{2e_b}`, measured difference **0**. `cos²` rather than `cos` is the correct face, because
`|·|²` is the quotient by the phase circle. Ratios compare by cross-multiplication, so the population
sorts and medians with **no division at any point**.

`crates/holonic-engine/src/embedding_fiber.rs:227` `align_bfloat16` already produces exactly that
integer alignment, `:358` `ResidentReadout` computes exactly that bracket on the card, and
`exact_value`'s `ExactOrdering` already compares without dividing. The law is implemented twice: once
correctly in a directory declared not to be the machine, and once as unjoined organs in the machine.

### 2.2 The organs that have never met the emission

Measured 2026-08-18 by `grep -rln` over `crates/*/src` and `soma/*/src`, quoted:

| organ | reach |
|---|---|
| `crates/holonic-engine/src/embedding_fiber.rs` | **zero library consumers.** Scoring appears only in `examples/` |
| `crates/holonic-engine/src/exponentiated_ratio.rs:156` `RatioFamily` | one library consumer, and it is on **suffix counts** |
| `crates/holonic-engine/src/surprisal.rs:769` `SectionModulus` | same |
| `crates/holonic-engine/src/clifford.rs:407` `Arrow` | one library consumer, and it is on **route-feature indicators** |
| `crates/holonic-engine/src/situated_residual.rs` | *one emission read against a declared second body's standing* — example consumers only |
| `crates/holonic-engine/src/phase_current.rs`, `wave_propagation.rs` | exported, example-only; the first owners to test for the missing transport join |
| `crates/holonic-engine/src/returned_reading.rs` `condition_again` | driver-only |
| `soma/membrane/src/live_constituent.rs` `CompressionCertificate` | produced and immediately discarded as `_certificate` |

### 2.3 The intake is written five times

`grep -rln "safetensors" --include='*.rs' crates soma`, 2026-08-18: **five private parsers in
drivers** against one library owner, plus **six private `bfloat16` decoders**. The fifth parser is
the only F32-capable one — needed for GPT-2 — reading a dtype the library module refuses by name
(`ADMITTED_DTYPE = "BF16"`). `canon/THE_DRIVER_ATLAS.md:293` already convicted the pattern: *"three
separately written safetensors readers are three implementations of one intake."* It is now five.

### 2.4 The material is on the disk and mostly unread

99 GB under `~/.cache/huggingface/hub`, **40 model families**. Five have been read exactly by this
tree — Gemma-4-E4B, OLMo-2 1B, Whisper base.en, GPT-2, Qwen3.5-4B. Unread and directly relevant:
**nine Mamba / Mamba2 state-space models**, which have no attention at all and are therefore the
second frame every claim on this line needs. `CLAUDE.md`: *an invariant is only visible across two
frames.*

### 2.5 The application is a driver that prints

`soma/life/src/bin/eros.rs` names its own state and is honest about it: `mouth`, `atlas`,
`condition`, `supersede` **WIRED**; `compress`, `produce`, `seal`, `resume` **unwired**. Measured
2026-08-18 by `grep -n "fs::write\|deposit_form\|File::create\|write_all\|ErosRest\|conditioned_rest\|receiver_exact_compression\|holonic_training" soma/life/src/bin/eros.rs`
→ **one hit, `:863`, and it is the string literal in that station table.** The application writes
nothing, mounts nothing, seals nothing, and touches no rest, no training ecology and no compression
organ.

### 2.6 The deposit path works and nothing habitually uses it

The full cross-process cycle — a driver's body sealed to octets, deposited as a plate by a **second**
process, re-lit by a **third**, handed material it never saw, changed, and re-sealed — **executes
today**, run 2026-08-18 on real driver artifacts. A `CDER` body took a deed and moved
`committed_stems 710 → 712`, `founded_stems 1274 → 1276`, `witnessing_wholes 87 → 88`, then
re-deposited at a different `plate_sha256`.

What does not exist is anything that does it habitually:

- **`ErosRest` is not a plate schema.** `soma/life/src/eros_rest.rs:104` `EROS_REST_PREFIX` is the one
  object that holds a whole body; `grep -n "EROS\|ErosRest" soma/tools/holon-plate/src/registry.rs`
  → **nothing**. The plate can carry any single organ and cannot carry a model.
- **The forms are in codecs nothing holds.** `find output -name "*.form" | wc -l` → **13,927**, and by
  leading magic: `OSER` 13,167 · `TSRE` 683 · `CDER` 4 · `HTEC` 2 · everything else 71. **687 of
  13,927 — 4.9% — carry a codec the plate holds**, and the two `HTEC` refuse on version because the
  codec moved to `2` and nothing re-emitted the form.
- **`standing/MANIFEST.txt` holds 104 returns, all `.lean`/`.txt`. Zero forms. Zero plates.** Ten
  gigabytes of returned bodies sit in the gitignored directory `standing/README.md` names as the
  mechanism that destroyed the laboratory's tiger figures.
- **`[[bin]] eros` is on the wrong side of a Cargo edge.** `THE_ASSEMBLY.md` fact F1: `holon-plate`
  depends on `life`, so a binary inside `life` can never call `holon_plate::{deposit, resume}`.

### 2.7 The carriers this plan depends on are not all correct — a capability census, 2026-08-18

A workspace census classified **281 library modules, 330,406 lines**: 251 WIRED, 22 DRIVER-ONLY,
**8 ALONE**. Exactness is not in question anywhere — over all library `src`, comments stripped,
**eight** float-mentioning code lines exist, six of them the declared IEEE mouth and two of them
string literals that *refuse* float spellings. What the census found instead is a **doc-versus-code
layer**, and one entry in it is a correctness defect in an organ station six depends on.

#### ★ `clifford::area_squared` drops the metric, and the doc states a false theorem that hides it

`crates/holonic-engine/src/clifford.rs:526-536` computes `from_blade` — before the repair, `Σ (blade coordinate)²` — **no
`qᵢqⱼ` weighting** — while `:518` computes Lagrange from the metric-weighted spans. The doc at
`:502-503` says *"Lagrange holds only in the Euclidean signature, so the second frame is offered only
there."* **That is false mathematics.** Cauchy–Binet gives

```text
    ‖a‖²‖b‖² − (a·b)²  =  Σ_{i<j} qᵢqⱼ (aᵢbⱼ − aⱼbᵢ)²
```

for **every** diagonal signature. The identity fails here only because the blade sum dropped the
metric — and the `euclidean` guard at `:510-517` therefore **disables the cross-check precisely
where the two frames would disagree.** The two-frame instrument is live only where it cannot fire.

Computed on the module's own committed Minkowski fixture (`:957-963`, `q = (−1,1,1)`,
`a = (1,1,0)`, `b = (1,0,1)`), 2026-08-18:

```text
    aim                      −1
    Lagrange                 −1
    Cauchy–Binet blade       −1      the metric-weighted blade agrees with Lagrange
    area_squared().value      3      what the organ returns
    causal_class: value−aim²  2  ->  TransportDominant
    correct:      −1 − 1     −2  ->  StorageDominant
```

**The causal class flips on this fixture, and no test asserts `causal_class` on that arrow.** The
four-class census this plan's station six reads is computed from that comparison.

#### The rest of the doc-versus-code layer, each verified

- **A ruling of Brandon's is corrupted by a mechanical sweep.** `embedding_fiber.rs:32-37` quotes him
  as *"stop calling the CPU the **'cpu'**"* and then asserts *"`cpu` is CUDA's word."* The ruling is
  **`host`**, and `host` is CUDA's word; a `host → cpu` rename passed through the quotation marks.
  Same artifact non-quotationally at `codec_system.rs:1060`, `codec_recovery.rs:1423`,
  `derivation_codec_intake.rs:729`. **A corrupted quotation is the contamination `CLAUDE.md` calls
  hardest to detect, because no later reader re-checks a provenance line.**
- **`surprisal.rs`'s exactness argument rests on a check that does not exist.** The zero test is
  justified by ℚ-linear independence of `{log₂ p : p prime}`, and the primality check at `:139-141`
  is `if prime < 2`. `term(4, …)` is public and admitted, so `term(2,2).minus(&term(4,1))` is zero as
  a real number while `is_zero()` returns `false`. The claimed iff is false on inputs the public
  constructor accepts.
- **`exponentiated_ratio::rebased_by` takes an integer power, never a root**, so the documented
  refusal *"refuses where the root leaves ℚ"* is unreachable and unimplemented; and
  `reciprocal_temperature` **is** `1/T`, so `T → 0` is `reciprocal_temperature → ∞` and the sentence
  *"no path here reaches `T → 0`"* is backwards. The **ban is not violated** — every ratio is still
  returned and none crowned — but the sentence is false, and the reciprocal is recorded nowhere on
  the returned family, so a rebased family is bit-indistinguishable from a warm one.
- **`holonic_training.rs:675` says *"Receiver parameters do not assign a scalar score"* and `:744-755`
  assigns one**: `agreement_rank = …max()`, then every route below it is discarded. And
  `predict_unfloored` — built to put evidence upstream of the recurrence floor — still applies it, so
  a sub-maximal fiber never enters the evidence and **can never be refuted**, which is the exact thing
  the two-sided standing was added to make possible.
- **`receiver_exact_compression.rs:71-74`** says of `Observation` *"nothing here can be ordered"* and
  derives `Ord` on the next line, with the field `pub u64` read as a raw key on the device path.
- Seven more, each cited in the census: `model_surface` naming an owner it never calls;
  `surprisal::Grain`'s *"every return carries it"* met by no return type; `reintegrating_elaboration`
  taking `.next()` off a set it documents as *"a population rather than a pick"*;
  `founded_mouth` rendering with `from_utf8_lossy` where it documents a refusal, and comparing two
  segmentations positionally while one drops whitespace; `material_incidence::RustItem::named` being
  the **section's** identifiers presented as the item's; `exposure_codec::LadderStop::Obstructed`
  returned with a provably empty obstruction list; and `embedding_fiber`'s octave documented as
  `ceil(log2|s|)` while the kernel counts bit length, `4` against `3` at `s = 8`.

#### Eight organs nothing reaches, and one of them contradicts a canon table

`soma/abi/src/conduct.rs` — `EventContact`, `CurrentConduct`, `conduct_current` — is reached by
**literally nothing, not even a test**. `canon/THE_INFORMATION_ENGINE.md`'s cycle table lists
`soma/abi/src/conduct.rs` `conduct_current` as the **conduct** station, **WIRED**. It is not.

`crates/holonic-engine/src/device.rs` is the exact CPU/device parity ABI — `ExactDeviceExecutor`,
`admit_device`, `DeviceParityReceipt` — with **zero implementations of the trait anywhere**, and its
only test passes `CpuExactDevice` as both authority and candidate, so `ParityRefused` cannot fire.
That is `CLAUDE.md`'s *"a check whose material cannot vary the property under test"*, and it is the
`PivotRule::ALL` shape again: the anti-defect instrument unexercised.

The other six: `soma/tools/record-index/` (2,465 lines, every input path is the **frozen
laboratory's** layout, `discover_laboratory_root` cannot succeed anywhere in this tree);
`soma/abi/src/holon.rs` (1,304 code lines, eleven packed row layouts, zero inline tests — the exact
shape `soma/abi/src/lib.rs:17-21` records *removing* `cuda_execution` for, standing at 4.8× the
size); `model_surface.rs`; `realization.rs` — the module named for §2's governing synthesis, *realization
pays and placement rides*; `arithmetic_phase.rs`, where only the datum `PrimeValuation` is consumed
and all thirteen law types are stranded; and `soma/abi/src/presentation.rs`, `live_presentation.rs`,
`parameter.rs`.

#### The joins the census ranks, beyond the head

**`embedding_fiber`'s two charts ⟷ `device::admit_device`.** The readout driver already runs the
card, runs `score_serially`, compares, and returns `Err("the two charts returned different exact
scores")` — which is `admit_device`'s deed verbatim, hand-rolled into a `String` and a `println!`
while the ABI organ that would return a typed `DeviceParityReceipt` sits ALONE. Joining them gives
the parity ABI its **first non-tautological exercise**.

**`atlas.rs` ⟷ `gluing.rs`.** `gluing.rs:5-8` opens *"Receiver-relativity is this framework's central
claim and nothing in the tree has ever put it at risk… no gluing could fail."* `atlas.rs` is 301
lines of cover assembly carrying an **oriented** residual and an **address holonomy** around the
overlap — which is exactly what the phase-object theorem says a magnitude check cannot see — and it
has been unreached the entire time.

**`codec_system` ⟷ `exposure_codec`.** `exposure_codec.rs:80` **names `codec_system::cross_check` in
its module doc as the cross-check route and never imports it**, re-implementing Moore refinement
locally at `:862-929`. The join is one import replacing a duplicate implementation with the organ
built to adjudicate it.

**`ExactTraversalQuadraticBalance` ⟷ an antisymmetric form.** Re-measured 2026-08-18 and
`CLAUDE.md`'s standing claim **holds**: two construction sites, both inline tests, both
`ExactRatMatrix::identity(1)`; the one driver passes `Vec::new()` three times. Handing it `J` returns
`RᵀJR = J` — angular momentum — as an exact rational balance on a real traversal.

## 3. The chain, with every step's owner and what is absent

```text
  1  safetensors octets -> validated tensor rows       embedding_fiber            BUILT
  2  bf16 words -> exact integers + dyadic exponent    align_bfloat16             BUILT
  3  exact bracket <a|b> on the card                   ResidentReadout            BUILT
  4  cos² = <a|b>²/(<a|a><b|b>), powers of two cancel  ---                        IN NUMPY ONLY
  5  ordering without division                         exact_value::ExactOrdering BUILT, unjoined
  6  the map's TRANSPORT as the artifact               ---                        IN NUMPY ONLY
  7  Eros's own current contacts a transport           ---                        ABSENT
  8  the contact as a whole arrow (aim, cross, reach)  clifford::Arrow            BUILT, unjoined
  9  the contact population as an exact cocycle        RatioFamily                BUILT, misjoined
 10  its spread, before any quotient                   SectionModulus             BUILT, misjoined
 11  the surface, through the bound tokenizer octets   codec_recovery             BUILT, unjoined
 12  the changed body sealed whole                     ErosRest                   BUILT, no schema
 13  deposited, carried, re-lit by another process     form_mouth + holon-plate   BUILT, unfed
 14  the return read BACK from the deposit             returned_reading           BUILT as a WIRE
```

**Step 9, stated precisely, because this is where the misjoin happened.** Let the exact integer
bracket be `s_j`. Under a declared base-two law `w_j = 2^{s_j}`, the family

```text
    r(j,k) = w_j / w_k = 2^{s_j − s_k},        r(j,k)·r(k,m) = r(j,m)
```

is a multiplicative 1-cocycle on the pair groupoid of contact identities. **That is what `RatioFamily`
is a cocycle of** — pairwise relative weights over one exact bracket population — not suffix counts.
The base and any temperature are a **declared law**, named as such; `SymbolicSurprisal` represents
prime-log charts of positive rationals and cannot silently claim exact `e^{αs/T}`.

**Step 14 is a defect the roadmap already names.** `returned_reading::condition_again` takes the
reading **in memory**. Soma refuses that shape five times in its own source — *"Reafference is the
world's, never a wire's."* The owed repair is to route the movement **through the seal**.

## 4. The stations

### Station one — supersede the false join

**Outcome.** `CompleteUnderRatioFamily`, `ArrowAdmitted`, the route-feature `PairingReading`, the
charset constituency in the driver, the lexicographic utterance, and `presentation_quotient` leave
the production dependency cone. `ContinuationReading` and `GreatestHorizonGate` are retained and
relabelled as corpus diagnostics and as a negative control. `RatioFamily`, `SectionModulus` and
`Arrow` remain available and lose their wrong consumers.

**Expectation.** The workspace suite stays green and the emission path forms no ratio at all, which
is the honest position until station five.

**Falsifier.** Any production path still calls `RatioFamily` on recurrence multiplicities, uses
`Arrow` to admit or refuse a member, or lets `char::is_alphabetic`, map order, `first()`, a rank, or
a caller-supplied answer decide a surface.

### Station two — repair the carriers before joining them

**A join onto a defective carrier propagates the defect and calls it a finding.** The census found
one correctness defect and five false doc claims in organs the stations below consume.

**Outcome.**

1. **`clifford::area_squared` carries the metric.** `from_blade` becomes the Cauchy–Binet sum
   `Σ_{i<j} qᵢqⱼ (aᵢbⱼ − aⱼbᵢ)²`, the `euclidean` guard is **removed** — the identity holds for every
   diagonal signature, so Lagrange is offered always and the two frames can finally disagree — and
   the doc's false theorem goes with it. `causal_class` is asserted on the Minkowski fixture, which
   no test does today.
2. **`surprisal`'s primality check becomes a primality check**, so the ℚ-linear-independence argument
   holds on every input the public constructor accepts.
3. **`exponentiated_ratio::rebased_by` either takes the root it documents or documents the integer
   power it takes**, and the reciprocal it was rebased by is recorded on the returned family. The
   sentence *"no path here reaches `T → 0`"* is corrected: the parameter drives at argmax and what
   holds the ban is that every ratio is returned and none crowned.
4. **`holonic_training`'s `agreement_rank` maximum is named as the scalar it is**, and
   `predict_unfloored` stops applying it, so a sub-maximal fiber can be refuted — which is what the
   two-sided standing exists for.
5. **The corrupted quotation at `embedding_fiber.rs:32-37` is restored** to `host`, with a note that a
   mechanical sweep passed through quotation marks, and the three non-quotational sites are repaired.
6. **`Observation`'s doc stops claiming what its derive contradicts.**

**Expectation.** Every repair moves a return or a refusal, and the ones that do not are reported as
bookkeeping rather than presented as evidence. The Minkowski fixture's causal class moves from
`TransportDominant` to `StorageDominant`.

**Falsifier.** If lifting the `euclidean` guard makes the two frames disagree anywhere on a diagonal
signature, Cauchy–Binet is not the identity being computed and the repair is wrong — the run must
exhibit the disagreeing arrow rather than widen a tolerance. If no repair moves any return, this
station found six documentation defects and zero correctness defects, and it must say exactly that.

### Station three — one intake, and the exact bracket as a machine return

**Outcome.** One library owner mounts safetensors for **BF16 and F32**, and the five private parsers
and six private decoders are cut to it. `cos²` enters the body as an exact rational pair — numerator
and denominator carried whole, never divided — ordered by cross-multiplication through
`exact_value::ExactOrdering`. The gauge control runs first, as the numpy does: `W_V → M W_V`,
`W_O → W_O M⁻¹`.

**Expectation.** The powers of two cancel with residual exactly zero on real material, reproducing
the numpy's measured `0`. The device and serial paths agree bit-for-bit. The dyadic mouth crosses the
whole map with zero residual, as `exact_value::ieee754` already measured over 205 probes.

**And the two-chart comparison becomes a typed receipt.** The readout driver already runs the card,
runs `score_serially`, compares, and returns a `String` — which is `device::admit_device`'s deed
verbatim, hand-rolled, while the parity ABI sits ALONE with zero implementations of
`ExactDeviceExecutor` and a test that compares `CpuExactDevice` to itself. Implementing the trait for
the resident readout gives that ABI its **first non-tautological exercise** and returns a
`DeviceParityReceipt` instead of a printed line.

**Falsifier.** A residual is non-zero; the two carriers disagree; the consolidated intake cannot read
the F32 map the fifth parser reads; or an excluded row is dropped without an aperture receipt. And
the vacuity arm: if the gauge orbit moves no reading, the control has gauged nothing and must say so.
For the parity receipt the arm is sharper — **`ParityRefused` must be shown able to fire**, by
handing `admit_device` two executors that genuinely differ, or the ABI is still unexercised.

### Station four — the transport is the artifact

**Outcome.** The map's transport is returned as a transport, not as a statistic — the object
`run_transport.py` produced (`two → ▁two, ▁Two, ▁cons, ▁deux`) as a machine return, in exact ratios,
with the two deflations already known built in from the start: the **correct lexical form** (`▁two`,
not bare `two`) and the **pre-norm gain**, whose omission made the earlier reading use *"an operator
the model never applies."*

**Expectation.** A transport family returned whole, with its three frames separating — real against
shuffled against null — and the archetype named for what it is: an orthographic/lexical variant
transport, since the digit→word claim was **narrowed** by its own artifact.

**Falsifier, and it has already fired once.** The raw embedding separates the family at `+0.3160`
before any transport, transported exceeds the matched null in only 76.8% of heads, and the tower
**progressively un-collapses** what the embedding collapsed. If the retaken exact reading does not
reproduce that deflation, one of the two readings is wrong and the run says which. And the unit
correction: grouped-query attention means **84 distinct value maps**, not 336; any per-head
population that counts 336 independent units is refused.

### Station five — the contact, and the hand it carries

**Outcome.** Eros's own current contacts an inherited transport, and the contact returns the **whole
arrow** — `aim`, the blade, `reach`, both norms — through `clifford::Arrow::between`. The blade is
carried, not summarised. **The cross may not admit or refuse the contact**; it is transported into
later morphology or it is an honest diagnostic.

**Expectation.** Contacts with equal aim and different blades remain distinguishable to a declared
later receiver. Lagrange's identity agrees with the blade's own coordinates exactly.

**Falsifier, and the precedent says it will bite.** The four-class census at `d = 256` returned
`TransportDominant` on all 16,384 pairs with `Ortho` and `Unread` both empty — because
`area² − aim² = ‖q‖²‖k‖² − 2·aim²` makes transport-dominance `cos²θ < 1/2`, which every pair in high
dimension satisfies. **A census that returns one class is a dimension effect, not a finding.** What
did carry information there was the **hand under a declared null**: a one-coordinate cyclic shift of
the key side preserved every span, entry and width and moved `ANTI` from 484 to 1,427 — a factor of
**2.9**. This station must run that null, and if removing the blade changes no later consequence
whatever, the blade was never integrated and the station says so.

### Station six — the ratio family and the section modulus, on the exact bracket population

**Outcome.** The declared exponent law is stated in the source, and `RatioFamily` and
`SectionModulus` are read over the **complete** contact population — no aperture presented as
completeness, no maximum, no rank. The modulus is read **before** any quotient discards the spread.

**Expectation.** The cocycle holds exactly over every triple; the family is invariant under the
common gauge; the modulus vanishes exactly where the population has collapsed onto one fibre, which
is the ban stated as a structural failure rather than as a prohibition.

**Falsifier.** The base or temperature is unspecified; suffix counts enter the family; the ratios
remain metadata while nothing transports differently because of them; or any maximum, ranking,
sampling, threshold, cap or minimum recurrence chooses a survivor. **`T → 0` is argmax and
`exponentiated_ratio.rs:297` refuses the path to it by name** — a station that reaches it has failed.

### Station seven — the surface crosses through material, not through a grammar

**Outcome.** A surface occurrence crosses the boundary, and its octets come from the **bound
tokenizer material** — the map's own tokenizer material at `/home/b/models/gemma-4-E4B-it`, already read exactly by
`the_foreign_codec_is_recovered_from_its_testimony` — not from an authored decoder. The real Gemma
codec declares `Replace(▁→space) → ByteFallback → Fuse`; the only nearby helper in the tree replaces
GPT-style `Ġ`/`Ċ` and is the wrong decoder.

**Expectation.** A surface artifact is returned and read, and every unresolved alternative stays in
its reconstruction fibre.

**Falsifier.** The result is a score table, a maximum, a ranked list, a caller-supplied token, a
lexicographically first surface, or a printed diagnostic called language production. **If the
standing current owners cannot cause a surface without a governor, the station fails and the exact
missing constitutive relation is named rather than papered over.**

### Station eight — the instance: seal, deposit, resume, ride

**Outcome.** Five wires and one crate move, each named by a standing document:

1. **`[[bin]] eros` moves above `life`**, so it can call both the organs and the plate. `THE_ASSEMBLY.md`
   F1 makes this structural, and station eight of the codec plan bars shelling out to a driver.
2. **`ErosRest` becomes a plate schema.** `EROS_REST_PREFIX` exists; the registry does not hold it.
   Its three controls are already specified and two have fired.
3. **`eros seal` / `resume` / `produce` are wired**, and `eros stations` stops printing `unwired`.
4. **Forms are deposited into `standing/`** through `soma/tools/standing-deposit`, which binds a
   content hash and a closure hash and has never been pointed at a form.
5. **A codec-version bump re-emits its forms**, so a wire moving does not silently orphan the deposit
   — as it already has for `HTEC`.
6. **The return routes through the seal.** The second conditioning reads the production **back from
   its content address**, not across a call.

**Expectation.** One invocation produces and seals; a later, source-detached invocation remounts,
receives material it has not seen, is changed, deposits the change, and produces again — and the
second production is attributable to the returned change.

**Falsifier, and the sixth wire carries the sharpest one, quoted from the roadmap that owes it:**
*"the second conditioning must fail if the sealed form is deleted between the two turns. A return
that survives the deletion of the world's record never went through it."* Further: the corpus or map
source must not need reopening; a private echo does not count as a world return; and a rest that is
byte-identical after claimed conditioning is a frozen body, which is exactly what the seal instrument
exists to catch — it caught one already, `sweep = origin` and `basis = identity` in **21,070 of
21,070** contacts.

### Station nine — the document repairs this campaign owes

Each is measured and each is a live error in a governing document.

- **`blueprint/THE_ROADMAP.md:812` carries false linear algebra:** *"the tie forces `MᵀM = I`,
  collapsing `GL(2560)` to `O(2560)`."* Tying gives `W_out = Eᵀ`; it does not make `E` orthogonal.
  A gauge reduction the architecture does not donate has stood since 2026-08-13.
- **`soma/tools/holon-plate/README.md`'s open-seam paragraph is stale by nine days** and misdirects
  every reader to `examples/emit_form.rs` as where a form comes from, while 33 drivers and 13,927
  files say otherwise. `form_mouth.rs` landed fourteen hours before the README's last commit.
- **`soma/tools/holon-plate/tests/plate_mouth.rs` says no driver's own material is known to produce a
  canonical form.** It does — `CDER` and `ERST` both deposit, resume and take a deed, measured
  2026-08-18.
- **`CLAUDE.md` cites 180 authored numeric levels at 2026-08-09; `meta/AUTHORED_LEVELS.tsv` carries
  382 today**, and `canon/THE_AUTHORED_LEVEL.md`'s excision accounting is written against the smaller
  number.
- **`canon/THE_TIMELINE.md` has no rows for 08-16, 08-17 or 08-18** — stale by exactly the three days
  that carry this line's peak.
- **`CONSTRUCTION_STATE.md`'s 2026-08-18 row overstates** what the superseded plan achieved and is
  regraded to an exact suffix-population diagnostic.
- **The superseded blueprint's absence claim was the convicted defect shape.** It read *"the last
  record that measured whether an emitted surface is a span of its source is dated 2026-07-31"*, a
  sentence scoped to 45 files of `2026-08-1[4-7]_*.md` and written as a statement about the corpus.
  The stronger precedent is 2026-07-29, which measured *complete output inherited contiguously = 0*
  over 3,827 tokens.
- **`tools/claim_index.py` globs `2026-08-*.md`**, so 292 July records are outside it — and **0 of
  them carry a `Truth status:` line**, using `GRADE:` / `Status:` instead. Widening the glob is not
  enough; the grade parser needs the July variants.
- **`tools/closure_manifest.py` reports thirteen orphans and eight have a producer**, because the
  join is keyed on the driver's stem and looks for `lean_proof_production` while the driver is
  `eros_lean_proof_production`. A false absence in a file a gate reads.
- **`canon/THE_INFORMATION_ENGINE.md`'s cycle table lists `soma/abi/src/conduct.rs` `conduct_current`
  as the **conduct** station, `WIRED`.** Measured 2026-08-18: `CurrentConduct`, `conduct_current`,
  `ConductTarget` and `EventContact` each return **0** occurrences over `crates` and `soma` outside
  their own file. Nothing reaches it, not even a test. A canon table asserting a wiring that does not
  exist is worse than an omission, because the engine document is what a fresh session reads to learn
  where the cycle runs.
- **`soma/tools/record-index/` cannot succeed anywhere in this tree.** Every input path it discovers
  is the frozen laboratory's layout — `src/soma/FORMULA.md`, `src/soma/RESEARCH`, `.agents/COMMUNE` —
  and it has **zero dependents**. Either it is adapted or it carries an archive banner; a crate that
  builds and cannot run is a capability claim by presence.

## 5. What is refused

- **No residency, and no lifting of the format.** *"Running the actual models is off of the table"*
  is his 2026-08-13 ruling and it stands: no inference stack is loaded, nothing is resident, the cost
  must stream to a map this hardware cannot hold. **The blanket "no forward pass" is withdrawn**
  2026-08-18 — it was this document's over-extension of a residency limit, and he ruled on 2026-08-16
  that *the mathematics that would run during inference* is the instrument for reading the potential
  activity of the weights. The format ruling is untouched: the architecture is disregarded and its
  functions are lifted.
- **No exterior adjudicator, on any codec.** No interpreter, compiler, checker or kernel decides
  anything the machine produced. Refused directly and repeatedly, and re-proposed in three of the
  last four sessions.
- **No governor.** No argmax, ranking, sampling, threshold, cap, minimum recurrence, or scalar
  chooser. `T → 0` is refused by name in the organ.
- **No authored grammar and no charset predicate deciding constituency.** *"It's just fucking text."*
- **No floats in library code**, and no percentage anywhere. A ratio is carried as a pair and never
  divided.
- **No path-shaped identity.** A path may be an address; it may not leak into what it addresses.
- **No agreement with Gemma as a success criterion.** Brandon: *"I do not think we should expect the
  machine to copy Gemma's reading… the real outcome would be the more ideal intelligence that
  combines functionality arbitrarily."*
- **No new organ where a wire will do**, and no cabinet beside an unfinished owner. That is the
  failure this plan exists to correct.

## 6. What this enables

Eros becomes an instance rather than a run: a body whose rest is on disk under a content address,
certified against its founding closure, which a later session mounts, feeds, is changed by, and
re-seals — with the change exhibited rather than counted and the seal deletable as a falsifier. The
map stops being a thing the machine measures from outside and becomes material it conducts. And the
softmax organ, the modulus and the arrow meet an exact bracket population for the first time, which
is where they always belonged.

**It also makes the second frame available.** Nine Mamba and Mamba2 state-space maps sit unread on
this disk and have no attention at all. Any claim about what a transport characteristically does is
one frame until it is read against them.

## 7. What this plan does not claim

It does not claim the machine will reproduce a transformer's outputs, and agreement with Gemma is
explicitly not the criterion. It does not claim the exterior numpy readings are wrong — they are
graded apparatus and several of them **deflated their own headlines**, which is why they are worth
lifting rather than repeating. It does not claim `ErosRest` as a plate schema makes the body a model
in any sense beyond `THE_EROS_INSTANTIATION.md`'s: *a model is a rest; "pretrained" means it was
sealed after conditioning; nothing more is meant and nothing more is true.* It schedules no
Millennium claim. And it does not claim the four-class census will separate at real width — the
precedent says it will not, and station four is written so that the negative return is the finding.

# Realization and hardware

The current production choice is specified in [`CPP_GPU_FOUNDATION.md`](CPP_GPU_FOUNDATION.md):
C++/CUDA, bit-pure semantic standing, and GPU-primary resident execution. This chapter states the
apparatus laws which that realization must satisfy.

## One program, plural apparatus charts

The engine reifies a causal program independently of apparatus:

```text
Program = predecessor + caused local support + exact constitutive operator
        + typed ports + expected return shape + resource obligation
```

An apparatus chart lowers this program to its local instruction/memory/communication geometry.
Admission requires equality of the declared semantic delta and exact accounting of any bounded
carrier conversion. Hardware-specific telemetry is attached afterward as receiver testimony.

## Parallelism

Parallelism follows geometry:

- one thread/current is serial along its own caused order;
- a front contains co-present local events with proved interchange;
- work partitions by local incidence/support, not by semantic category;
- overlaps use declared interaction/gluing and return residual stress;
- feedback opens only through an explicit returned boundary; and
- apparatus completion order never enters semantic lineage.

Cards are built for wide local operations over vertices, edges, cells, fields, rays, particles,
constraints, and sparse incidence. Express algorithms in those elementary terms wherever their
law actually has that geometry. Do not translate a serialized host foreman into a monolithic kernel
and call it parallel.

## Device admission

The typed program law and invariants are authoritative. A reference executor may act as an
admission oracle but is never a semantic owner or hot-path fallback. For apparatus representation
map `decode_X`, admit a common program domain only after:

1. identical typed input and predecessor;
2. decoded semantic identity agreement; raw physical addresses may differ;
3. exact decoded per-local-element result and multiplicity agreement;
4. exact boundary, obstruction, and lineage agreement;
5. deterministic logical reservation/obligation agreement on the common admitted domain; and
6. rest/remount lifecycle agreement where residency persists.

Different apparatus may have different capacity, layout, telemetry, and lawful refusal outside
the common admitted domain. That difference is explicit apparatus testimony, not semantic
inequality. After admission, the hot path returns bounded local/delta receipts. Periodic full parity is a
declared lifecycle audit, not a replay after every event.

## Residency

Resident device structure is an apparatus image of the same continuing body, not a second semantic
owner. Initial full mount may be expensive and is separately measured. Later changes transfer only
owner-derived deltas and touched heads/sections. A dirty or refused physical front blocks only the
affected apparatus crossing; semantic admission and pending physical synchronization remain
distinct and inspectable.

## Resource morphology

Deterministic logical resource standing includes reservations, capacity obligations, and resource
state that affects whether the semantic event can commit. Apparatus telemetry—locality,
residency, communication cost, temperature, clocks, utilization, power, and measured capacity—is
a separate physical receipt. A calibrated observation may later return through a caused port and
change placement or aperture, but raw telemetry never enters deterministic successor equality.
A refusal returns the before/after logical face, the physical receipt when available, and one of:

- changed partition/factorization;
- changed placement/residency;
- changed schedule consistent with causal order;
- reduced receiver aperture with explicit omitted fiber; or
- open obstruction.

No invisible capacity mutation may occur during a refused atomic event.

## Storage and transit

Choose representation by retention/transport law:

- immutable persistent structure for shared standing;
- local mutable cells only under one owner;
- sparse frontier for current;
- factorized boundary response for query families that certify it;
- native rest for lifecycle suspension;
- codec face for external interchange; and
- observer artifacts for reports/presentation.

Serialization is lawful at a boundary or rest chart. It becomes a bottleneck when a single
serialized representation is forced to own navigation, topology discovery, scheduling, and
standing. Found native local incidence incrementally from caused contact; a total expansion is
lawful only when a declared observer requests it, and its artifact cannot feed standing without a
new caused event.

## Telemetry aperture

Every performance report states device, driver/runtime, program family, input extent, warm/cold
state, synchronization boundary, sampling method, elapsed time, bytes, utilization, temperature,
power, and energy if calibrated. Low utilization is a diagnostic receiver, not an explanation;
locate the active causal/apparatus mouth before modifying code.

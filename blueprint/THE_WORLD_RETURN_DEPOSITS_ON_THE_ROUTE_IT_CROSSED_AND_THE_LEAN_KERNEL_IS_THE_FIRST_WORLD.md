# The world return deposits on the route it crossed, and the Lean kernel is the first world

**Date:** 2026-09-02
**Kind:** active construction contract composed by [`THE_ROADMAP.md`](THE_ROADMAP.md).
**Campaign:** `WRD0--WRD5`.
**Governing doctrine:**
[`../canon/TABLET_THE_RESONANCE.md`](../canon/TABLET_THE_RESONANCE.md) §6 and §11,
[`../canon/THE_HOLOBROCHOS_SPINE.md`](../canon/THE_HOLOBROCHOS_SPINE.md) §1 and §4,
[`../canon/01_CAUSAL_CALCULUS.md`](../canon/01_CAUSAL_CALCULUS.md) §2,
[`../canon/TABLET_THE_HEXIS.md`](../canon/TABLET_THE_HEXIS.md) §7,
[`../canon/THE_INFORMATION_ENGINE.md`](../canon/THE_INFORMATION_ENGINE.md) §5,
[`THE_EROS_INSTANTIATION.md`](THE_EROS_INSTANTIATION.md) §4b,
[`THE_HOLONIC_INTELLIGENCE_FRAMEWORK_RETURNS_INTRINSIC_HOLONS_AND_PACKAGES_DISMANTLING_INFERENCE_CULTIVATION_AND_GENERATION.md`](THE_HOLONIC_INTELLIGENCE_FRAMEWORK_RETURNS_INTRINSIC_HOLONS_AND_PACKAGES_DISMANTLING_INFERENCE_CULTIVATION_AND_GENERATION.md)
§3 and §4, and
[`THE_NATIVE_MORPHOLOGY_VARIANT_FOUNDATION_RETURNS_FAITHFUL_LOCAL_LIFTS_PARENTED_CULTIVATION_AND_EXACT_EXPORT_LENSES.md`](THE_NATIVE_MORPHOLOGY_VARIANT_FOUNDATION_RETURNS_FAITHFUL_LOCAL_LIFTS_PARENTED_CULTIVATION_AND_EXACT_EXPORT_LENSES.md)
§3.


**SUPERSEDED 2026-09-02 at every seam that names a language.** Brandon's ruling of the same day:
no language, modality, format, or field is referenced inside Eros or Athena; they are
applications. The successor is
[`THE_WORLD_RETURN_IS_MODALITY_NEUTRAL_AND_EVERY_LANGUAGE_IS_AN_APPLICATION.md`](THE_WORLD_RETURN_IS_MODALITY_NEUTRAL_AND_EVERY_LANGUAGE_IS_AN_APPLICATION.md).
The junction, route-deposit, and termination laws, the Lean owner, and the measured WRD4 faces
stand; the mouth, the template emitter, the face adapter, and the driver are retracted
(`research/records/2026-09-02_THE_LEAN_BINDING_OF_THE_WORLD_RETURN_IS_RETRACTED_AND_LANGUAGES_ARE_APPLICATIONS.md`).

## 0. Verdict and correction

[counterexample; source-inspected] Brandon's direct request of 2026-09-01 was one Athena variant
refined for coding and mathematics. The assessment record of that date located the missing
relation at two exterior codecs. The capability and retraction record in `AGENTS.md` locates it
inside the circulation instead: every returned current in the tree is typed by the operator
(`ReturnedScaffoldInteraction::found` receives literal rationals at every call site as of
2026-09-02, measured by `grep -rn "ReturnedScaffoldInteraction::found" applications soma crates`),
so no world has yet deposited anything; the reflection falsifier
`soma/life/examples/the_cycle_halts_where_nothing_reflects.rs` ran on 2026-08-15 and arm two
fired; and every emitter that rendered a surface from retained corpus text was retracted.

[definition] This campaign founds the constitutive passage from an actual world return to a local
morphology deposit, with the Lean kernel as the first world, over the standing owners. It adds no
trainer, scheduler, decoder cabinet, semantic router, or second body. It composes:

- `soma/life/src/lean_mathematics/{ecology,syntax,kernel_returns}.rs` as the exterior Lean
  material chart and the exterior kernel world, in world and observer jurisdiction;
- `crates/holonic-engine/src/native_spool/{thread,scaffold,deposits}.rs` as the carrier population;
- `crates/holonic-engine/src/holonic_intelligence/circulation.rs` as conduct;
- `soma/life/src/native_intelligence/{circulation_session,morphology_commit,scaffold_cultivation,morphology_package}.rs`
  as the session, staged return, commit, withdrawal, and replay owners;
- `crates/holonic-structure/src/junction.rs` `CountedCrossing` as the interface law; and
- `soma/formal/elementary-holonics/ElementaryHolonics/Computation/{HolonicNeuralEcology,HolonicOrientedSiteTransport,NativeMorphologyVariant,HolonicIntelligenceLifecycle}.lean`
  and `Millennium/{HolonicComplexParametron,SituatedReturnedDifference}.lean` as the formal owners.

## 1. Standing-reuse receipt

[definition] The one continuing body is a `NativeMorphologyPackage` whose hot morphology is one
`NativeTransportScaffold`: a population of Complex-Parametron carriers (`NativeParametronCell`)
on addressed threads, with oriented incidence, generator descents, receiver factors, mutual
constitutive responses, and reconstruction fibres. Its rested identity is the package's canonical
wire and `MorphologyLineage`. The ingress port is `NativeInferenceRequest` (spool, thread,
occurrence, receiver). The hot conduct owner is `conduct_native_inference` over the resident
word and complex incidence. The return port is `ReturnedScaffoldInteraction` through
`NativeCirculationSession::stage_return` and `commit`. The receiver is the declared
`ReceiverId` of the experiment, read through `NativeCirculationBoundary` faces, beside the
exterior kernel receiver `LeanKernelReturn`. The requested artifact is a committed generation
whose deposit was derived from a kernel return, together with the later conduct that rides it.

[definition] The attempt at composition before this campaign returned three concrete absences,
each an edge and not an organ: a passage from Lean material into the carrier population at a
grain founded from recurring transformation; a constitutive map from `LeanKernelReturn` to
`ReturnedScaffoldInteraction`; and a termination law reading the junction rather than a count.

## 2. The objects, plurally

[definition] Carriers are many. Each `NativeParametronCell` has its own section, current,
relative phase, and hand; carriers meet only through threads whose incidence names them and
through mutual constitutive responses. A lattice is founded incidence among carriers and grows
by deposit.

[definition] Receivers are a family. The experiment's native `ReceiverId` reads
`NativeFutureFace` observations; the kernel reads `KernelAdmitted` or `Obstructed` with its
diagnostic; the declaration lineage of a candidate is a third face. Equality is asserted only
where every declared receiver agrees over admitted successor histories, and a richer receiver
reopens the fibre with its shortest separator.

[definition] Charts are an atlas. The declaration star (organs, binder charts, references) is the
exterior Lean chart; the carrier population is the native chart; state identifiers are
source-neutral and the correspondence to declaration names lives in a physically separate cold
witness that hot conduct cannot read.

[definition] Potentials are per occurrence. One entering occurrence opens one plural future
section under its word and receiver; conducting collapses one boundary while the fibre stays
retained. Only the causal cone of that occurrence is active; carriers outside it rest.

## 3. The passage, typed

```text
Lean sources -> LeanMathematicsEcology (organs, references, tactic species, relation labels)
  -> WRD1 mouth: organs found sites, references found occurrences, relation labels found
     generators, oriented incidence founds each carrier's initial complex current
  -> NativeTransportScaffold + LeanLatticeColdWitness (separate lane)
  -> NativeMorphologyPackage / NativeCirculationSession (standing owners)
  -> conduct: plural futures, emitted face (from, to), actual successors
  -> WRD2 cold render: candidates for one target header from the organs the emitted route
     reached, through the standing Lean renderer; the render owns no selection
  -> LeanKernelWorld::grade_all -> LeanKernelReturnFamily
  -> WRD3 constitutive map: junction (incident = emissions that crossed the boundary, one per
     candidate; transmitted = emissions the kernel admitted) -> Gamma, T, service rounds
     -> ReturnedScaffoldInteraction (current = (T, Gamma), storage = 1/service rounds,
     cone = the carriers the admitted emissions carried, with the route's two ends)
  -> stage_return -> commit -> generation n+1
  -> WRD3 termination: matched halts; partial reflection re-enters at the actual successor;
     total reflection returns an obstruction and founds the next entering occurrence
     from the organ the diagnostic names, or remains open
  -> WRD4 later current: a second target whose route reaches the deposited thread;
     withdraw, replay, and kernel verdict as one receiver among the family.
```

## 4. Laws this campaign formalizes and obeys

[definition] The junction law: for incident population `R` and transmitted population `M`,
`Gamma = (R - M) : (R + M)` and `T = 4RM : (R + M)^2`, carried as pairs and never divided in the
engine; `T + Gamma^2 = 1`; `Gamma = 0` exactly at a match; service rounds are `ceil(1/T)`, equal
to one at a match. Termination is the match. No stop token, count, threshold, or chooser halts
the cycle.

[definition] The route-return law: the adjoint of a route of oriented unit-hand steps returns a
difference to the route's boundary carriers and cancels on every interior carrier, while every
crossed carrier remains in the causal cone and the lineage. This is the spine's telescoping law
read as a deposit; it is why the standing deposit owner moves the boundary carrier and names the
interior.

[definition] Locality: the support of a deposit is contained in the causal cone of the emission
and its return; carriers and threads outside the cone are bit-identical before and after.

[definition] Many receivers: a kernel verdict is one covector. Two returns with equal kernel
faces may differ under the lineage face; the deposit is derived from the complete returned
difference, and the scalar faces are read afterward.

[definition] Jurisdiction: the Lean parser, the kernel, and the cold render are world and
observer apparatus. They may not enter the hot conduct path, select among plural futures, author
a response length, or route the constitutive law.

## 5. Disallowed

[definition] No corpus text, declaration name, tactic string, or diagnostic string enters a
carrier, a thread, or a generator. No candidate is scored, ranked, or thresholded. No count gates
admission. No foreign realization emits. No Lean process runs inside conduct. No timeout is a
partial pass. No qualitative surface is graded by inspection.

## 6. Ordered construction

### WRD0 — formal junction, route-return, locality, and many-receiver laws

[definition] Add `Computation/HolonicWorldReturnDeposit.lean` composing the standing owners:
the junction return with `Gamma`, `T`, `T + Gamma^2 = 1`, match, and service rounds; the
route-return telescoping law with support in the route; a control inhabiting
`LocalCausalConeCultivation` on a three-site carrier with a two-site route; and a two-receiver
control in which equal kernel faces carry different lineage faces. Import it from
`HolonicMachineLearning.lean`.

**Pass WRD0:** [definition] the focused target and the live umbrella build without `sorry` or a
new axiom inside the process bound; document law, epistemic tags, and tracked authority pass.

### WRD1 — the Lean material mouth into the carrier population

[definition] Add `soma/life/src/lean_mathematics/lattice_mouth.rs`. From one conditioned
`LeanMathematicsEcology` it founds a valid `NativeTransportScaffold` with one spool: each
declaration organ is one native state; each in-corpus reference is one occurrence and one thread
from the referenced organ's state to the referring organ's state with unit coefficient; the
generator of an occurrence is the referenced organ's relation label, so generator descents follow
recurring transformation; each carrier's initial section and current are the exact oriented
incidence populations at its state (departures as the real part, arrivals as the imaginary part);
receiver factors, reconstruction fibres, and generator descents follow the standing lift owner's
shape. The state-to-declaration correspondence departs into a separate `LeanLatticeColdWitness`.

**Pass WRD1:** [definition] the scaffold validates; hot wire contains no declaration name,
source path, tactic string, or relation label (measured by byte search over the canonical wire);
withdrawing the cold witness leaves the scaffold identity unchanged; a package mounts and one
conduct returns plural futures and actual successors.

### WRD2 — candidate render from the emitted route

[definition] Move candidate rendering out of `lean_mathematics/ecology.rs` into
`lean_mathematics/candidates.rs`, and add rendering over an explicitly supplied organ family. A
conducted boundary's emitted face and actual successors are read back through the cold witness
into the organs the route reached; the standing renderer projects candidates for one target
header from exactly those organs. The renderer owns no selection: every candidate is graded.

**Pass WRD2:** [definition] the candidate family for a target is a function of the conducted
route and the target header only; two different emitted routes over the same target yield
different families; `life` focused tests pass.

### WRD3 — the constitutive map and termination

[definition] Add `soma/life/src/native_intelligence/route_return.rs`. From one
`NativeCirculationBoundary`, its cold-witness route, and one `LeanKernelReturnFamily`, derive the
junction: the unit that crosses the boundary is one emission, so the incident population is the
candidate family and the transmitted population is the emissions the kernel admitted; return
`CountedCrossing::meet`. A match returns current `(1, 0)` and storage `1`. A partial reflection
returns current `(T, Gamma)` and storage `1 / service rounds`. A terminus by type (no admitted
emission) returns an obstruction carrying the organs that reflected and those the diagnostics
name, which founds the next entering occurrence when such an organ is a referring site, and
otherwise remains open. The contact support is the carriers the admitted emissions carried,
joined with the route's two ends by the existing deposit law. Organs carried only by reflected
emissions re-enter. The result enters `stage_return` and `commit` unchanged.

[historical] 2026-09-02: the first deposit of this paragraph counted organs, not emissions, and
read transmission from diagnostics. The WRD3 construction corrected it: the junction counts
crossings, and an admitted closer that carried no organ is a lawful weak return whose cone is
the route's two ends. The Lean owner never fixed the unit and is unchanged.

**Pass WRD3:** [definition] a matched return commits with storage one; a partial return commits
with storage strictly between zero and one; a total reflection commits nothing and returns the
named obstruction; the committed cone equals the route; threads outside the cone are
bit-identical; withdrawal restores the predecessor exactly.

### WRD4 — the first world, later current, and the falsifier

[definition] Add `soma/life/examples/the_kernel_return_deposits_on_the_route_it_crossed_wrd4.rs`.
Condition on the repository's `Foundation/` Lean corpus with one theorem withheld as the target
and a second related theorem withheld as the later probe. Found the lattice, mount the package,
conduct, render, grade through `LeanKernelWorld`, derive the return, commit, then conduct the
second target and grade it; withdraw the commit, conduct again, replay, conduct again. Report the
three falsifier arms: a matched boundary closes, an unmatched boundary stays open, and both
species are present.

**Pass WRD4:** [definition] the driver returns inside the process bound; at least one kernel
return deposits a committed generation; the second target's route reaches the deposited thread
and its kernel verdict differs between current and withdrawn morphology, or the driver returns
the exact obstruction naming why it does not; replay restores the current verdict exactly; all
three arms report.

### WRD5 — records, release, commit, and push

[definition] Deposit the WRD0--WRD4 records, update `UNIVERSAL_CATALOG.md` rows, run the focused
owners and then one complete `bash tools/gates.sh`, commit by coherent owner, and push. Leave the
Millennium Lean working-tree edits of the parallel session untouched.

**Pass WRD5:** [definition] all nine gates pass inside their bounds; the branch is pushed; the
roadmap and position record return to no current campaign with the WRD grades recorded.

## 7. Scope

[definition] WRD0--WRD5 return one world-derived deposit and its later current on the Lean world.
They do not claim general theorem proving, coding capability, natural-language emission, or a
refined product. A refined coding and mathematics variant is the later campaign that repeats this
passage over Rust and Lean worlds with a receiver family; it is founded only after WRD5 returns.

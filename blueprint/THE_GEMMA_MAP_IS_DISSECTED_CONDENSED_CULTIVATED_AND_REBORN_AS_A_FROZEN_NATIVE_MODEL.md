# The Gemma map is dissected, condensed, cultivated, and reborn as a frozen native model

**Date:** 2026-08-18
**Kind:** first executable construction under
[`blueprint/THE_PHOENIX_REBIRTH_LIFTS_INHERITED_HEXIS_AND_RETURNS_A_NATIVE_EXECUTABLE_ECOLOGY.md`](THE_PHOENIX_REBIRTH_LIFTS_INHERITED_HEXIS_AND_RETURNS_A_NATIVE_EXECUTABLE_ECOLOGY.md)
and [`blueprint/THE_ROADMAP.md`](THE_ROADMAP.md).
**Truth status:** construction contract. The local source facts and standing owner states are
measured; the lift, dissection, condensation, cultivation, export, and Phoenix grade remain `open`
until their artifacts return.
**Source material:** `/home/b/models/gemma-4-E4B-it/`, with `model.safetensors` as the inherited
weight occurrence and the sibling configuration, processor, tokenizer, chat-template, and
generation files as exterior apparatus lineage.
**Doctrine:** [`canon/TABLET_THE_HEXIS.md`](../canon/TABLET_THE_HEXIS.md),
[`canon/TABLET_THE_OPERATIONS.md`](../canon/TABLET_THE_OPERATIONS.md),
[`canon/TABLET_THE_MANIFOLD.md`](../canon/TABLET_THE_MANIFOLD.md),
[`canon/THE_SURFACES_ARE_PATHS.md`](../canon/THE_SURFACES_ARE_PATHS.md), and the Phoenix master
blueprint named above.
**Construction disposition:** this blueprint consumes the unfinished production stations of
[`THE_MAP_IS_MATERIAL_THE_HEAD_IS_A_CONTACT_AND_THE_INSTANCE_RESUMES.md`](THE_MAP_IS_MATERIAL_THE_HEAD_IS_A_CONTACT_AND_THE_INSTANCE_RESUMES.md)
and the unrealized executable-model stations of
[`ATHENA_THE_DIMENSION_IS_DECLARED_AND_THE_RANK_IS_THE_FILE_SIZE.md`](ATHENA_THE_DIMENSION_IS_DECLARED_AND_THE_RANK_IS_THE_FILE_SIZE.md).
Their completed exact contacts, rests, map readings, integer mouths, and negative results remain
standing instruments. Neither older document independently schedules a production build once this
plan is active.
**Occasion:** Brandon, 2026-08-18: *"the ideal outcome … is to see a model like Gemma lifted and
exceptionally compressed into a holonic format … in addition to being tuned and dissected."*

---

## Audited construction position — 2026-08-18

The adversarial return is
[THE_SITE_RETURNED_BUT_THE_CARD_DID_NOT_OWN_THE_PATH_AND_PHOENIX_REMAINS_A_PARTIAL_LIFT.md](../research/records/2026-08-18_THE_SITE_RETURNED_BUT_THE_CARD_DID_NOT_OWN_THE_PATH_AND_PHOENIX_REMAINS_A_PARTIAL_LIFT.md).
It distinguishes exact instruments, bounded experiments, and this blueprint’s station grades.

| obligation | audited position |
|---|---|
| source manifestation and exact binary mouths | **complete source manifest returned 2026-08-19** (Station B record): all 2,130 populations hashed in one streamed pass, every `BF16` codeword decoded exactly through the one float mouth, file identity bound, dtype checked, every law entailed by its validated testimony; the potential atlas over all 42 layers, 1,466 operations entailed |
| exact linear fibres, rebases, and metric adjoints | implemented-exact |
| generic typed ported words and lift-defect law | implemented-exact at the substrate aperture |
| contextual active Gemma path | **active stimulation of the whole text tower returned 2026-08-19** (Station C record): 42 layer deeds and the final deed per input, each one graph, the KV-sharing realization and both species enacted, the potential section and the plural future section returned, the source's own top candidate five of five; carried by a declared `MidpointQuotient` chart because the certified interval enclosure of the composed tower diverges at ≈ 2^35.6 per layer (measured) — the composition is not certified and the receipt says so |
| source intervention taxonomy | open; candidate distinguishability was measured but two real source questions remain unsettled |
| native morphology | open; no lattice, state, generator, or source-independent ecology was founded |
| condensation | open; one axis-block sensitivity census returned, with no quotient runtime or decoder |
| cultivation | open; BF16 mutation/remount/withdrawal returned, but the checked adjoint did not derive the delta and the split was not held out |
| native rest | open; the current artifact is a fixed three-row source-tensor fixture |
| runtime and frozen inference | open; one proposed layer reaches a tied potential face, but no application accepts new material or enacts the tower |
| reborn-body dissection and Phoenix grade | open |

The bounded returns remain evidence. None is deleted merely because its master station remains open.
The first resident-section return did not advance a master obligation: it established CUDA interval
operations and one terminal card read-out while a host `ResidentBinding` dispatch still owned the
semantic chronology, every local kernel synchronized back to that host, the composed PLE-plus-layer
work was not admitted as one deed, and source bindings were not content-validated individually.
**Repaired 2026-08-19** (record
[`research/records/2026-08-19_THE_LAYER_IS_ONE_GRAPH_LAUNCHED_ONCE_AND_THE_IMMEDIATE_GATE_RETURNS_ITS_FALSIFIERS_WITH_THE_RATCHET_OPEN.md`](../research/records/2026-08-19_THE_LAYER_IS_ONE_GRAPH_LAUNCHED_ONCE_AND_THE_IMMEDIATE_GATE_RETURNS_ITS_FALSIFIERS_WITH_THE_RATCHET_OPEN.md)):
the deed is one graph launched once, admitted whole before allocation, with footprint-derived
certificates, a mode-bound cover, validated source bindings, and the source-runtime `chi` slot;
sixteen falsifiers pass. **Closed 2026-08-19** (record
[`research/records/2026-08-19_THE_REFUSAL_TRAVELS_ALONG_THE_LINEAGE_THE_ADMISSION_IS_TYPED_AND_THE_RATCHET_IS_DISPOSITIONED.md`](../research/records/2026-08-19_THE_REFUSAL_TRAVELS_ALONG_THE_LINEAGE_THE_ADMISSION_IS_TYPED_AND_THE_RATCHET_IS_DISPOSITIONED.md)):
the global refusal word is replaced by lineage-local transport with the complete obstruction lineage
returned, the admission is typed and product-ordered with nothing `None` and the material admission
cited, the binding cabinet is replaced by owner-local laws, and the architecture ratchet is
dispositioned and clean — twenty-four falsifiers pass. That is Station A of the complete sequence
and no master station of this table is promoted by it.

## Validation discipline for active construction

Validation has four apertures, and the complete gate is never the inner one.

**Owner iteration.** Run only the affected package/library check, changed owner's focused tests,
targeted source scans, and relevant cheap named gates. A local source edit does not authorize a
workspace test, all-target build, or complete gate.

**Changed-cone verification.** After one owner closes, run the affected package's library/tests and
the directly dependent Phoenix driver. Compile or execute only the realization whose closure moved.

**Real deed.** Run the declared GPU deed after changed-cone checks pass. Record command, code/source/
configuration closure, apparatus identity, elapsed time, exit status, and addressed artifact. If
that closure and input occurrence are unchanged, inspect the existing receipt rather than rerunning
the deed or serial reference.

**Release reading.** Regenerate documents and ledgers once after the implementation and artifact
stand, then run `bash tools/gates.sh` once. It already executes the full workspace suite, so running
`cargo test --workspace` immediately before it is forbidden. A cheap post-reading repair reruns only
the named affected gate when no code, kernel, driver, or returned artifact changed, and the report
states the split scope rather than claiming a fresh complete-gate result.

No test or driver is retired merely because the suite is large. Classify it first as owner guard,
real deed, persisted-artifact consumer, serial/reference control, or redundant replay. Only a
measured duplicate or superseded path with no independent receiver consequence may be removed.

The normalization and Markov corrections are derived in
[THE_MARKOV_KERNEL_IS_A_SOFTMAX_CHART_THE_NORMALIZATIONS_ARE_QUOTIENT_SECTIONS_AND_THE_MANIFOLD_IS_NOT_THE_RECONSTRUCTION.md](../research/records/2026-08-18_THE_MARKOV_KERNEL_IS_A_SOFTMAX_CHART_THE_NORMALIZATIONS_ARE_QUOTIENT_SECTIONS_AND_THE_MANIFOLD_IS_NOT_THE_RECONSTRUCTION.md).
In particular, source epsilon makes Gemma RMSNorm a bounded radial chart rather than an exact
magnitude quotient, and a state condensation of attention/diffusion transport owes a lumpability or
equivalent successor-factorization receipt.

## 0. The requested outcome

The first Phoenix instance returns two inseparable deliverables:

1. a deterministic, intervention-backed atlas of Gemma's recoverable transport families; and
2. a source-detached frozen native model which enacts the condensed/cultivated ecology in a fresh
   process and emits actual text, plural future sections, and exact causal testimony.

The complete first-instance circulation is

```text
Gemma BF16 rest
    -> exact streamed source manifest
    -> potential and contextual active transport
    -> intervention taxonomy
    -> native Eros/Athena baseline beside inherited arm
    -> cross-chart lift with defect
    -> material-founded lattice/state/generator morphology
    -> receiver-minimal familywise condensation
    -> cultivation on holonics material
    -> source-detached native rest
    -> executable native runtime and optional safetensors/GGUF export
    -> frozen inference
    -> before/after dissection, held-out conduct, and ablation.
```

The strongest desired return is an exceptionally smaller and cheaper executable native body whose
every retained axis is load-bearing for the declared future receiver family. The plan authors no
compression ratio, latent width, layer count, adapter rank, or target speed. Exceptional means the
body is receiver-minimal and the complete measured cost vector strictly falls: artifact plus
decoder bytes, exact semantic work, peak residency, transfer, and separately reported physical
telemetry. The material decides the factor. If strict reduction does not return, the result may be
receiver-exact condensation but may not be advertised as exceptional, hyper-efficient, substantial,
or compressed.

If no strong condensation exists, the deed returns the exact obstruction and the least receiver
family which prevents it. That is a successful research return and not a Phoenix compression grade.

## 1. The source and its measured aperture

The first source is measured locally:

```text
model.safetensors              15,992,595,884 octets
safetensors tensors            2,130
stored dtype                   BF16 for every tensor
vocabulary                     262,144
text tower                     42 layers, hidden 2,560, FFN 10,240
attention groups               35 sliding + 7 full at {5,11,17,23,29,35,41}
query / key-value population   8 query heads / 2 KV heads
sliding window                 512
configured text context        131,072
```

The source is not text-only. Its sibling configuration/processor and tensor names carry:

```text
vision tower                   16 layers, hidden 768, patch grain 16
audio tower                    12 layers, hidden 1,024, 16 kHz / 128 mel bins
video processor declaration   32 frames, soft video-token boundary
vision -> shared stream        embed_vision projection [2560,768]
audio -> shared stream         embed_audio projection [2560,1536]
```

These are source-manifest facts. They do not claim every advertised modality has been exercised.
The primary executable grade is text input/output. The source manifest and native schemas carry all
modality ports from the first station. One non-text projection path must then be actively enacted
and returned as a bounded portability control; its presence in the census alone proves nothing.
This does not expand the first grade into complete image/audio recognition.

Every BF16 codeword is an exact dyadic value. Phoenix can lift the exact stored map. It does not
claim those codewords are the pre-training full-precision values or that the original corpus is
encoded losslessly.

### Re-measured 2026-08-18, and six facts the census above does not carry

Every figure in the two blocks above was re-taken and holds exactly. Command, and it is the whole
measurement for this subsection:

```
python3 -c "import json,struct; f=open('/home/b/models/gemma-4-E4B-it/model.safetensors','rb'); \
n=struct.unpack('<Q',f.read(8))[0]; h=json.loads(f.read(n))"
```

`2,130` entries, `BF16` for every one, header `281,040` octets, payload `15,992,314,836`, file
`15,992,595,884`.

**THE MASS IS TWO LOOKUP TABLES, AND THAT IS WHERE A FALSE COMPRESSION NUMBER WILL COME FROM.**

```text
  6,606,028,800   41.31%   n=126   language mlp
  5,637,144,576   35.25%   n=1     embed_tokens_per_layer   [262144, 10752]
  1,342,177,280    8.39%   n=1     embed_tokens             [262144,  2560]
  1,284,555,776    8.03%   n=252   language self_attn
    609,650,176    3.81%   n=751   audio tower
    334,730,112    2.09%   n=658   vision tower
    111,181,396    0.70%   n=338   norms, gates, per-layer projections
     55,050,240    0.34%   n=1     per_layer_model_projection
     11,796,480    0.07%   n=2     modality projections
```

`10752 = 42 x 256`, so the per-layer table admits a rank-3 chart: one 256-wide population per layer
per vocabulary row. **Two tensors are 43.64% of the model and both are vocabulary-indexed.** That
indexing alone does not make them a `receiver_exact_compression::ObservedSystem`; they enter that
owner only after typed successor/intervention receivers have been enacted.

**And `tie_word_embeddings = True` with no `lm_head` tensor** — measured 2026-08-18 by the command
above, `any('lm_head' in k for k in h)` returns `False`. The output surface is `embed_tokens` read
in the other direction. The census must not report an absent output population, and the typing is
the source handing over the Dirac reading: one construction serving as both the receiver bra and the
transported ket.

**THE ATTENTION IS TWO GEOMETRIC SPECIES, NOT ONE.**

```text
  35 sliding layers   q [2048,2560] = 8 x 256   k,v [ 512,2560] = 2 x 256   o [2560,2048]
                      rope default, theta 1e4, full rotation of 256
   7 full layers      q [4096,2560] = 8 x 512   k,v [1024,2560] = 2 x 512   o [2560,4096]
                      rope proportional, theta 1e6, partial_rotary_factor 0.25
```

Four independent differences: head dimension, rope species, theta, and partial rotation. The exact
populations are **42 distinct `v_proj` operators, 84 key/value heads, 336 query heads**, in the two
dimensions above.

**`partial_rotary_factor = 0.25` on a 512-wide head is 128 dimensions rotated by this RoPE chart and
384 not rotated by it.** The global contact therefore splits exactly into a RoPE-acted part — a
discrete action by one group element per frequency band, whose exact carrier is the integer
separation and the band index — and a complement which this positional chart leaves fixed. The
complement still carries chronology through residual standing and other passages; “no RoPE action”
is not “no chronology.”

**THE KV-SHARING FIELD IS REALIZATION TESTIMONY, NOT A REDUNDANCY CERTIFICATE.**
`text_config.num_kv_shared_layers` is `18` while all `42` layers carry `k_proj` and `v_proj`
populations. The field describes a source realization/cache policy; it does not state that those
weights are identical or inert. The exact source implementation must bind the policy to occurrences
and ports before any ablation is meaningful. No byte condensation follows from this field alone.

**NEITHER MoE NOR MULTI-TOKEN PREDICTION IS EXERCISABLE HERE.** Measured 2026-08-18 by
`python3 -c "import json;print(json.load(open('/home/b/models/gemma-4-E4B-it/config.json'))['text_config'])"`:
`enable_moe_block = False`, `num_experts = None`, `top_k_experts = None`,
`expert_intermediate_size = None`, `use_double_wide_mlp = False`, and no `nextn` field anywhere. The
first instance returns nothing about either. Their portability pressure comes only from the Qwen
GGUF the master contract lists, and no mixed-architecture claim may be read out of this return.

**THE MOUTH'S APERTURE IS TWO COUNTS THAT DISAGREE BY A FACTOR OF THREE HUNDRED.**

```text
  rank 0    928 tensors            1,856 octets    0.000%
  rank 1    557 tensors        1,461,140 octets    0.009%
  rank 2    630 tensors   15,959,195,648 octets   99.793%   <- the whole present aperture
  rank 3     13 tensors       31,580,160 octets    0.197%
  rank 4      2 tensors           76,032 octets    0.000%
```

`630 / 2130` is `29.6%` of entries and `99.793%` of octets. **The old mouth read the mass and missed
many small law-bearing candidates**: rank-1 tensors include normalization gains and site scalars;
rank-0 scalars include the audio tower's clipped-linear bounds. Rank-count and byte-count are both
receiver faces and neither answers which population is load-bearing. The source ledger therefore
separates mouth admission, transport excitation, and load-bearing testimony; only an enacted typed
operator diagram may found the third axis.

## 2. One owner and one application entry

One continuing Phoenix body has one owner. No driver owns production state, no model body is cloned,
and no example process becomes the application.

The current `soma/life/src/bin/eros.rs` cannot call `holon-plate`: `life` is already below
`soma/tools/holon-plate` in the dependency graph. Before Phoenix application work, re-home that
binary in one concrete exterior application package above both—`soma/tools/eros/` is the named
target—which depends on `life`, `holon-plate`, `standing-deposit`, and the admitted source codecs.
This package is an apparatus boundary, not another semantic engine. Its exterior commands may
expose the stations by their semantic names:

```text
eros phoenix admit
eros phoenix stimulate
eros phoenix dissect
eros phoenix lift
eros phoenix condense
eros phoenix cultivate
eros phoenix seal
eros phoenix infer
eros phoenix ablate
```

The CLI is apparatus and owns no semantic phase enum. Each command consumes/produces addressed
forms; it may not invoke `cargo run --example`, replay a driver, or choose a semantic owner from a
model label.

A generic Phoenix model plate/form schema carries the whole native model rest and realization
manifest. Gemma remains source lineage and a schema/codec instance; it does not create internal
Gemma anatomy. The existing `PlateSchema` relights a mutable `LitBody`, whose `present` deed must
change its form under `present_and_require_change`. Therefore the cultivation body and frozen
inference realization are distinct exterior faces over one declared rest schema: the frozen face is
not misreported as a complete `LitBody`. Schema versioning is an exterior codec fact; a version
change re-emits affected forms rather than accepting an orphaned prior representation.

## 3. The standing owners being composed

No second owner is built beside these without an attempted composition returning a missing type,
port, law, or consequence.

| responsibility | standing owner | use in this plan |
|---|---|---|
| computational holon and operation species | `canon/TABLET_THE_OPERATIONS.md`, `research/records/2026-07-19_THE_CONSTITUENT_IS_THE_COMPOSITE_THE_PRODUCT_CANNOT_DISCARD_ITS_PINS.md` | construction / transport / face / quotient over live incidence, words and exterior |
| operator diagram and ports | `EvolutionShape`, `CategoryPresentation`, `CausalDiagram`, `RealizationWitness` | typed source realization, chronology, branches and interactions |
| exact linear chart | `exact_linear::ExactRatMatrix`, `causal_traversal::ExactCausalTraversalPassage` | application, serial product, exact work, balance forms and bounded inversion |
| foreign source manifest and exact binary mouth | `foreign_map` | arbitrary-rank manifestation, independent admission/transport coverage, exact BF16/F32/F64 codewords |
| BF16 rank-2 contraction surface | `crates/holonic-engine/src/embedding_fiber.rs::{safetensors,ResidentReadout}` | focused rows delegated through the foreign manifest; resident exact contact input |
| exact IEEE codewords and output rounding | `exact_value::ieee754` | stored dyadic values and certified export residual |
| contact geometry | `clifford::Arrow`, `exact_contact::ExactContact` | aim, blade, reach, hand, ratio face |
| softmax/ratio geometry | `exponentiated_ratio::RatioFamily`, `surprisal::SectionModulus` | complete contact cocycle and spread |
| residual and loss fibres | `situated_residual`, `CrossEntropyFiber`, `crates/holonic-engine/src/basin.rs::ExactGeometricLoss` | structured return before scalar faces |
| native incidence | `soma/life/src/incidence_production.rs`, `soma/body::incidence` | local ports, arrival, differentiation, withdrawal |
| atlas and gluing | `atlas`, `gluing`, `contact_gluing` | local charts, overlap, holonomy, obstruction |
| diffusion and sheaf state | `diffusion`, `sheaf_diffusion`, `phase_current` | cross-site/rank transport and SSM projection |
| composition relation | `crates/holonic-structure/src/relating.rs::Relating` | typed rebase/composition relation |
| composed chain | `crates/holonic-structure/src/chain.rs::Chain` | composed incidence and returned remainder |
| parallel/interchange | `interchange::InterchangeCertificate` | certify co-present branches without importing apparatus order |
| chain morphism and higher coherence | `algebraic::CausalSectionMorphism`, `grown_cell`, `derivation_two_cells` | boundary commutation, filled squares and retained obstruction |
| behavioral quotient | `receiver_exact_compression::{CollapsedPair,ReceiverExactCompression}`, `soma/life/src/reconstruction_fiber.rs` | taxa, receiver-minimality, shortest separators and retained reopening fibre |
| exact dimension | `rebase_invariants::smith_normal_form`, `athena::TreeChart` | rank, invariant factors, lattice/state dimensions |
| cultivation | `holonic_training` vocabulary plus owner-local deltas | proposal/commit/evidence; generalized beyond text templates |
| whole-body rest | `ErosRest`, `form_mouth`, `holon-plate`, `standing-deposit` | seal, source departure, remount, model plate |
| exact work/apparatus | `exact_work`, `cuda_aperture`, resident CUDA owners | admission before deed, device testimony |

The following are not yet standing joins and may require owner-local generalization:

- complete source admission beyond the presently decoded rank-0/rank-1 checkpoint, with all sibling
  artifact hashes and load-bearing status founded from the operator diagram;
- a canonical reader/realization law for Athena's U16/U32 integer container;
- symbolic/certified general exponential ratios matching Gemma's source softmax law;
- complete streamed contextual Gemma transport;
- a ported heterogeneous transport word binding foreign linear/nonlinear occurrences to the
  standing operation complex, including exact chain-defect propagation;
- public kernel/image/cokernel/preimage-fibre and receiver-metric adjoint receipts for general
  ported linear transport; square inversion alone does not supply them;
- lifted-model local morphology and its genuine adjoint return;
- Phoenix whole-rest and executable-model schema; and
- a runtime which enacts native Phoenix inference.

These are typed gaps, not permission to create Transformer, FineTuner, Distiller, Autograd, or
ModelRuntime semantic cabinets.

## 4. The source file is admitted exactly

Produce `PhoenixGemmaSourceManifest` with:

```text
source occurrence, file sizes, container and decoder versions
source-byte hash and hashes of every config/processor/tokenizer/chat-template artifact
decoder/toolchain identity and hash
all 2,130 tensor identities, tensor-region hashes, byte intervals, shapes and BF16 dtype
text, vision, audio, video exterior ports
per-layer embedding and modality projection populations
stored dyadic carrier identity
per-population status: manifested / read exact / unread shape-or-dtype / dissection-eligible
Phoenix coverage: lifted exact / lifted bounded / potential-only / actively stimulated / unexcited /
unread-refused
predicted exact work and peak streamed residency
```

`embedding_fiber::safetensors::read_rows` remains rank-2 BF16-bounded. The owner-local
`foreign_map` generalization manifests every declared population, retains refusals by name, and can
decode BF16 spans at arbitrary rank; the current source checkpoint actually decodes the rank-0 and
rank-1 population only. Manifestation, source decoding, potential transport, and active stimulation
are separate ledger axes. An adjacent decoded matrix lends no standing to an unposed vector or
operator. The first text grade owes explicit coverage over the complete declared text tower. Any
bounded subset is a **partial source admission/lift** as applicable and cannot support a whole-model
or Phoenix-rebirth claim.

The manifest preserves paths as apparatus addresses and never folds them into tensor, transport, or
model identity. Tensor names locate material and do not taxonomize the native ecology.

The source mouth uses mmap/streaming with no full source residency requirement. The first admission
may retain one tensor/circuit working set plus shared codec material. Resource refusal changes
partition/residency and never widens a magic allocation.

**Controls and falsifiers**

- An F32 safetensors fixture must be decoded by the same exact intake or refused by a named aperture;
  the five/six private parsers are removed rather than synchronized.
- A malformed dtype, shape, offset, or truncated payload refuses before any transport row exists.
- Relocating the identical source moves no semantic identity.
- A byte mutation moves the exact addressed source occurrence and cannot read as the same map.
- No config field may create an internal transport class before the material responds.

## 5. The native baseline and foreign arm are posed together

Before the foreign lift can be credited, the same declared repository material and future receiver
family are presented to:

```text
ARM N  a native Eros/Athena body with no Gemma-derived morphology
ARM F  the inherited Gemma source apparatus
```

ARM N must return its actual plural future sections, surfaces, native atlas, rest, and obstructions;
an Athena tensor container without inference does not satisfy it. ARM F returns source potential and
active transport testimony. The arms share material and receivers, not topology or expected output.

ARM N is an independent native branch, not a clone or rollback of the lifted ecology. Any executable
baseline claim owes the same fresh-process remount, resident-device deed, source-access audit, and
targeted ablation controls as the lifted arm. Until those return, it is explicitly a bounded native
baseline rather than an inference-ready comparison model.

This baseline prevents every effect of general Eros machinery from being attributed to the foreign
lift and prevents Phoenix from erasing the still-open question of what Eros produces independently.

**Falsifiers:** ARM N shells into a foreign model; ARM F receives an expected answer; either arm
routes by material kind; only counts/topology return and no actual surface/future section is
inspected.

## The map becomes a ported operation complex before it becomes a pathway

The computational holon is already deposited and is not a universal `Holon<T>` numeric wrapper:

```text
H = (A_H, C_*(H), partial_H, omega_H, {K_gamma}, Gamma_H)
```

with locally founded blade support, oriented cellular incidence, event readings, ordered transport
words, and exposed boundary/open residual. `TABLET_THE_OPERATIONS` types every local deed as a
construction, transport, face, or quotient. The missing Phoenix join is therefore one executable
ported realization of the foreign operator diagram into this standing object.

The first candidate diagram binds source implementation/configuration testimony through
`EvolutionShape`, `CategoryPresentation`, `CausalDiagram`, and `RealizationWitness`; local linear
maps use `ExactRatMatrix`/`ExactCausalTraversal`; boundary-commuting maps use
`CausalSectionMorphism`; serial words retain `Relating`/`Chain` lineage; and co-present branches owe
an `InterchangeCertificate`. Source testimony is an exterior realization codec and not native
ontology. If exact source testimony is absent or contradictory, all compatible diagrams remain a
`ReconstructionFiber`; human inspection of plausible text may not select one.

The dependency geometry is not a 294-product line:

```text
standing -> normalization -> {Q, K, V} co-present branches
Q,K -> contact/ratio construction; ratio,V -> carried section -> O transport
standing,O-return -> residual construction
residual standing -> normalization -> {gate, up} co-present branches
gate,up -> nonlinear/Hadamard construction -> down transport -> residual construction.
```

This is a topology template whose exact ordering and extra Gemma passages must be bound from the
source realization. Q/K/V and gate/up are not serial matrix chains. A contraction count is work,
not dependency span. Matrix multiplication compiles only a typed serial word; a dense product is one
receiver/apparatus chart and never replaces the word.

For local cross-chart defect

```text
chi_gamma = Phi_Y T_gamma - S_gamma Phi_X,
```

serial composition obeys the exact propagation law

```text
chi_(eta gamma) = chi_eta T_gamma + S_eta chi_gamma.
```

This discrete Leibniz rule attributes whole-path disagreement without flattening the diagram.
Zero defect fills the corresponding higher cell; nonzero defect remains holonomy/obstruction.

Inversion is never a blanket pathway operation. A rebase `T : X -> Y` requires a witness `B` with
`BT = I_X` and `TB = I_Y`. A singular or rectangular map returns kernel, image, cokernel, and the
complete affine reconstruction fibre. A pseudoinverse is a receiver-selected section requiring
declared metrics. Cultivation uses the metric adjoint `G_X^-1 T^T G_Y`, not an inverse and not a
bare transpose silently assuming Euclidean charts.

Orientation, chronology and reversibility are recorded independently. Invertible chart transitions
carry the sign of their top exterior power, and a loop product of `-1` is a global orientation
obstruction—not a property of any one locally orientable chart. A rectangular perspective receiver
instead returns `J^T G J`, the top nonzero exterior blade, `ker J`, and the rank-loss
minor/discriminant locus. Equal pullback Grams with opposite blades must remain separated. The
orientation double cover/local system retains a coherent receiver chart even when the quotient
atlas is non-orientable.

**Falsifiers.** Same-shaped but differently typed ports compose; noncommuting words `AB` and `BA`
collapse; two distinct words with one product matrix lose their lineage before a receiver quotient;
Q/K/V or gate/up are counted as serial depth; a singular map returns an inverse; a claimed rebase
checks only one identity side; direct whole-path `chi` disagrees with the recurrence above; equal
Gram faces erase opposite exterior hands; a Möbius loop returns orientable.

### Carrier and hardware consequence

`AlignedMaterial` currently expands stored BF16 words into `i64` entries on the card. That is an
apparatus chart, not the semantic form and not evidence that every source tensor must be expanded at
once. Candidate realizations include streamed active fronts, resident reuse partitions, packed
aligned entries, or other exact charts; the complete work/range/transfer receipt decides among them.
No solution is selected from file size alone.

The card owns every wide semantic front it admits. CPU apparatus may mmap source bytes, establish
ports, persist rest, and independently audit returns; it may not execute normalization, RoPE,
contact/ratio transport, gating, or residual joins between CUDA leaf contractions. Weights remain
resident for the reuse interval declared by the operation complex rather than being uploaded once
per contraction. `exact_work::ExactWork` predicts semantic work before the deed; physical telemetry
separately measures stored ingress, resident expansion, scratch, output egress, allocation, launch,
and synchronization.

## 6. One honest Gemma pathway is enacted before the tower is generalized

The first active path is a complete contextual language pathway, not an embedding row or isolated
layer-zero probe. For a declared input occurrence, it carries:

```text
tokenizer/processor exterior decomposition
global and per-layer embedding presentation
input normalization and its gain
relative chronology / RoPE passage
Q/K contact population and normalization
complete finite-temperature ratio family
V/O transported construction
residual chronology
local nonlinear/MLP constitutive passage
successor standing through every crossed layer
output illicial potential section
plural future section and exterior codec face.
```

Every nonlinear or transcendental source operation is one of:

1. enacted exactly on a closed carrier;
2. carried symbolically with a proved law;
3. enclosed with a certified remainder; or
4. returned as an obstruction.

**The configuration-level transcendental census, measured 2026-08-18 by
`python3 -c "import json;print(json.load(open('/home/b/models/gemma-4-E4B-it/config.json')))"`, and
two of the four are free.**

| source operation | declared value | species here |
|---|---|---|
| `final_logit_softcapping` | `30.0` | `30 tanh(x/30)` is **strictly monotone**, so every order face of the potential section — including the nearest-boundary receiver — is exactly invariant under it. It is a total transformation for a magnitude receiver and the identity for an order receiver, and for the latter it need not be evaluated at all. |
| attention softmax | — | the **contact ordering** needs no transcendental: `p_i/p_j = exp(x_i - x_j)` is monotone in the difference, so comparing two weights is comparing two rational differences by cross-multiplication, which is `exact_contact::RatioFace::compare`'s standing deed. Only the convex combination for value transport needs an enclosure. |
| `rms_norm_eps` `1e-06` normalization | — | one application adjoins at most a quadratic radical over its actual input field; dependencies decide the resulting extension degree. With nonzero epsilon and nonzero gains the radial magnitude is analytically recoverable, so this is a bounded radial chart rather than an exact scale quotient. The return owes the exact epsilon, radical or enclosure, inverse-or-quotient classification, local derivative/adjoint, and later storage fibre. |
| `hidden_activation` `gelu_pytorch_tanh` | — | a nonlinear constitutive construction. `exact_value::SeriesTailCertificate` supplies remainder species, but no complete GELU/tanh realization is standing merely from that carrier. |
| audio `attention_logit_cap` `50.0`, `use_clipped_linears` | `True` | the `928` stored rank-0 `input_max` / `input_min` / `output_max` / `output_min` bounds are the source's own declared aperture per linear map, not weights, and they are read as such. |

The authoritative
[Gemma 4 implementation](https://github.com/huggingface/transformers/blob/main/src/transformers/models/gemma4/modeling_gemma4.py)
is admissible exterior realization testimony and resolves the layer-zero PLE order:

~~~text
residual = hidden standing
→ per_layer_input_gate
→ GELU
→ Hadamard contact with the layer’s PLE section
→ per_layer_projection
→ post_per_layer_input_norm
→ residual re-entry
→ multiplication by layer_scalar.
~~~

It also specifies the token-identity and context-aware PLE branches. A missing local Python package
does not leave these relations open. The exact source revision and content address must accompany
the realization receipt; the public implementation remains exterior testimony and never native
ontology.

No epsilon or source-runtime float silently decides identity, rank, route, or a native coefficient.
An exterior source-runtime replay may provide an independent comparison face and never governs the
native result.

The path is first taken on one admitted streamed partition whose extent is derived from source ports
and work law. The generalization to all 42 layers proceeds only after serial composition preserves
port lineage and repeated/local families are named from transport rather than indices. That first
partition is a pathway-construction grade only. The active-lift station cannot pass for Gemma text
until all 42 text layers and every declared input, normalization, attention/chronology, residual,
MLP, and output population have an explicit coverage status and the complete contextual passage has
executed. The existence of every local matrix owner does not establish their composition.

**Controls and falsifiers**

- Gauge changes of internal coordinates move raw representations and preserve the complete composed
  receiver consequences they lawfully should; the orbit must be nontrivial.
- A shuffled/null operator and raw-embedding baseline accompany every source-family claim.
- GQA is counted exactly: **`42` distinct `v_proj` operators, `84` key/value heads, `336` query
  heads**, in two dimensions — `256` on the `35` sliding layers and `512` on the `7` full ones. An
  operator count and a head count are different populations and neither may stand for the other.
- Full-attention and sliding-attention source layouts may land in one or different native taxa only
  after intervention.
- The source output and native interpretation may disagree; disagreement returns the complete fibre.
- Omitting normalization, RoPE, V/O, residual injection, or prior-layer state makes the active lift
  refuse rather than downgrade to a probe.

## 7. The source taxonomy is founded by intervention

Construct the source `PhoenixGemmaDissectionAtlas` over complete local passages. Each row carries:

```text
source occurrence and port lineage
candidate transport species
baseline and matched sibling
remove / replace / permute / rebase / isolate / scale intervention
receiver family and successor histories
caused change in current, future fibre, and exterior boundary
shortest separator
attribution status and open alternatives.
```

The intervention panel covers at minimum:

- global and per-layer embedding injection;
- source normalization/rebase families;
- relative chronology/RoPE;
- Q/K contact and softmax ratio geometry;
- V/O transport;
- residual standing;
- MLP/gated constitutive transport;
- sliding versus global source chronology;
- vision/audio projections into the shared stream as prospective portability controls; and
- output boundary transport.

These names remain source lineage. The returned taxonomy may merge, split, or cross them. Taxonomy
is bounded to the panel; every unexercised distinction remains open.

A row enters the taxonomy only after its intervention executes and returns a changed receiver
consequence. A tensor, label, spectrum, static correlation, or authored intervention plan is not a
taxon.

The static source census proves that the non-text materials and ports exist; it does not prove that
the path conducts. The first executable grade must enact one non-text projection through its typed
port and inspect the returned consequence before claiming even the bounded portability control.

**Two panel corrections, measured 2026-08-18** (command in the source section above).

- **The `sliding` / `full` label is not a bare name here — it predicts four independent geometric
  facts** (head dimension, rope species, theta, partial rotation), so requiring that *some*
  source-layout label fail to predict the native taxon is very unlikely to be satisfiable on that
  axis and would be satisfied vacuously if it were. **Pose the requirement inside the `35` sliding
  layers**, where the label carries no geometric content and a returned split or merge is real
  evidence. The `sliding` / `full` axis is then a **positive** control: a taxonomy that fails to
  separate two populations differing in four declared ways has not dissected anything.
- **`num_kv_shared_layers = 18` is first a realization-binding question.** Bind the source
  implementation's cache/state reuse law to exact occurrences and ports, then intervene on that
  reuse while holding the K/V weight transports fixed. Ablating K/V weights on the assumption that
  cache sharing makes them inert is forbidden.

**Anti-vacuity requirements**

- Each plural taxon has at least two members and a nontrivial naming/gauge orbit.
- Each separated pair names a shortest receiver/history witness.
- At least one source-layout label fails to predict the native taxon, and it is sought on an axis
  where the label could fail.
- At least one equal-output pair reopens under intervention.
- An ablated claimed family changes its attributable consequence and leaves an unrelated family
  unchanged.

## 8. The cross-chart lift returns its defect

For each admitted source transport `T_gamma`, construct native ports/stalks/incidence and a candidate
transport `S_gamma`, with cross-chart maps `Phi_X`, `Phi_Y`. Return

```text
chi_gamma = Phi_Y T_gamma - S_gamma Phi_X
```

under the declared receiver family. Invertible maps are chart transitions. Every non-invertible map
is a quotient carrying its `ReconstructionFiber`. The tokenizer, embedding projection, PLE injection,
modality projection, normalization, and output surface are typed separately; no implicit cast hides
their remainder.

Every `Phi_X` and `Phi_Y` carries an inverse-or-quotient receipt. A chart transition must return its
inverse and both exact inverse compositions. A quotient must return its collapsed population,
complete reconstruction fibre, and shortest receiver/history which reopens it.

The native body is assembled from existing incidence, atlas/gluing, diffusion/sheaf, phase, chain,
and compression owners. It incrementally mounts local transport; the foreign file is never runtime
topology.

The lift must demonstrate one **native recombination** on material held out from founding: at least
two named lifted families compose into a lawful native transport not contained in the declared
source transport closure under the declared receiver. Version lineage names every causing family.
The source-access audit excludes an exact corpus path and expected-output lookup. The recombination
must actively execute; targeted ablation of each contributing family removes its attributable
consequence. Concatenating known source paths does not pass. Source agreement is neither necessary
nor sufficient.

**Falsifiers:** `Phi` is an identity by declaration; the candidate merely serializes source tensors;
`chi` is reduced to a norm; one representative is selected from an open fibre; native recombination
is a corpus path; source tensor names become native owners.

## 9. The material founds the native morphology species

The plan attempts the existing morphology charts in this order of question, not as a menu of
preferred architectures:

1. Does an exact rebase close the transport with zero remainder?
2. Do complete future histories factor through a finite state equation?
3. Have recurring phase relations locked into a crystal/lattice with bands and holonomy?
4. Does a sparse native incidence/atlas carry the transport directly?
5. Does a self-similar growing family condense to a generator and scale action?
6. Which alternatives remain open to the receiver family?

Rank, state dimension, lattice incidence, scale ladder, cycle population, and generator count come
from exact receiver/transport testimony. Hankel/realization and Smith/invariant-factor readings are
used only where their hypotheses hold; a small aperture may not counterfeit low rank.

The native morphology return carries:

```text
all local sections and typed ports
state/transition and observation maps where founded
lattice/crystal or generator/scale law where founded
phase, winding, holonomy, reconvergence, higher cells
every collapsed population and shortest separator
exact work and resource obstruction
```

**Falsifiers:** the topology is a 42-layer restatement; rank is selected before measurement; full
rank is called incompressible without testing composed transport; an SVD tolerance decides native
dimension; a fractal population is sampled into fixed layers; a crystal is declared without
recurring phase transport.

## 10. Condensation is graded over plural futures and interventions

Declare the future receiver family before fitting any condensation. It contains:

```text
nearest-boundary token/surface face
complete illicial potential and ratio section
plural future sections over several future offsets
multi-step recurrent state and remount
contextual source/native transport traces
first and higher intervention responses
cross-codec mathematical/code/prose equivalents
one non-text projection portability face
work, residency, and exterior boundary consequences.
```

The admitted Gemma metadata does not declare a multi-token-prediction head. MTP is therefore an
open comparison receiver for a later source which exposes multi-successor prediction, not a Gemma
source fact and not a condition silently simulated by repeated single-boundary emission.

The body may preserve a receiver exactly, preserve it within certified enclosures, or return it
open. All three are typed. Compression is not graded by mean output similarity, benchmark accuracy,
parameter count, or file size.

When a normalized contact family is read as a Markov interaction, condensation additionally owes
the dynamic intertwining law. For state-to-block incidence \(C\), source transition \(P\), and
candidate quotient transition \(\bar P\),

\[
PC=C\bar P
\]

must hold on the complete declared successor family. Its defect, within-block histories, and
shortest separating continuation remain in the ReconstructionFiber. Pairwise distance, one-step
output agreement, or a small RMS distortion does not establish this law.

Receiver-minimality is the completion condition:

- every retained native axis or relation has a named future/intervention which separates it from
  the body without that relation;
- every departed distinction is in a reconstruction fibre;
- enlarging the receiver family refines or reopens rather than silently invalidating the rest; and
- no further exact condensation exists under the declared family without losing a witness.

The complete source/native cost vector reports artifact+decoder bytes, exact semantic work, peak
residency, transfer, and separate physical telemetry. A strict improvement in one coordinate with no
receiver-visible regression is useful; a large measured factor is the desired outcome. No factor is
authored as a pass gate.

### A row the material never excited is `unexcited`, never `condensed`

**This is the single most likely way this plan returns a spectacular and false number, and it is
added 2026-08-18 from the byte budget above.** `43.64%` of the source is two vocabulary-indexed
tables over `262,144` rows. The declared cultivation material excites a small fraction of that
vocabulary — the standing Eros atlas over this repository carries `19,970` material occurrences,
measured 2026-08-18 by `cargo run -q -p life --example the_atlas_conducts_and_the_container_is_the_comparison`.
Collapsing the rows a corpus never reached would report a factor in the tens and would be
`CLAUDE.md`'s authored-partition defect exactly: the class population would restate which rows the
material happened to touch, and the apparatus around it would be real while the partition was
forced at declaration.

So the coverage ledger's `unexcited` class is wired to the condensation grade and not merely to the
source manifest:

- a row separated from every other row by a declared receiver word is **condensed**;
- a row no declared receiver word reached is **unexcited**, its bytes stay in the reconstruction
  fibre, and **the fibre counts in the artifact-plus-decoder total**;
- the two are reported as two numbers and their sum is the only quotable one.

**The check is the one `CLAUDE.md` states for an authored partition: which declared input, varied
across two members of a returned class, moves them apart?** If the answer is *"the corpus I chose"*,
the collapse is a statement about the corpus and not about the map.

**Falsifier.** A reported condensation factor whose `unexcited` population is not stated beside it;
an embedding collapse whose separating words are not exhibited per class; a fibre whose bytes are
omitted from the cost vector.

## 11. The lifted ecology is cultivated on holonics material

The developmental material is the declared mixed body already named by the project: Brandon's
purified conversation, assistant responses, the live code/research/canon, and networked mathematics
sources. It enters through standing mouths as bits and codecs; no repository/file ontology becomes
the training law.

The split is lineage-based and fixed before cultivation:

```text
development wholes
held-out nonidentical wholes
surface/codec variants of shared relations
structurally related subject-disjoint controls
matched foils and no-op material
```

No token from a held-out whole enters the development arm through a copied window, lookup table,
path, summary, or lossless retained source. The source-access audit includes foreign weights and
developmental material separately.

Three cultivation arms share predecessor provenance:

```text
ARM L  lifted native ecology, owner-local full morphology revision
ARM O  lifted native ecology, factorized overlay only if defect founds its rank
ARM C  no-op/still control with the same material mounted but no return committed.
```

ARM O exists only after a `RankDerivationReceipt` returns the receiver-visible defect, exact
rank/state calculation and applicable invariant factors, a lower-rank foil, the shortest separator
proving that foil insufficient, and the chosen factorization. No overlay rank may occur in source or
driver code before that receipt.

The primary return is structured:

```text
transport defect and contact/hand delta
ratio family and weighted section response
supported and unsupported event populations
behavioral fibres and shortest separators
phase/lattice/state morphology movement
exact work and apparatus receipt.
```

A scalar loss may be one declared receiver. It never replaces the return or selects a morphology
species. A real adjoint is pulled through the retained active path. Where the constitutive law is
non-smooth or open, plural alternatives remain.

Cultivation passes only when source-detached remount changes held-out later conduct, a subject-
disjoint control stays unchanged, and targeted ablation restores the predecessor consequence.

## 12. The foreign body departs and the native model rests

Define generic `PhoenixModelRest` as a whole-body rest/plate schema. Its first exterior instance may
be labelled `PhoenixGemmaRest` for source lineage, but that label is a codec/schema instance and
never an internal model ontology. The rest contains:

```text
native incidence, stalks, transport and state morphology
cultivation overlays/deltas
source and native taxonomy addresses
cross-chart and condensation certificates
reconstruction fibres and obstructions
input/output codec materials permitted at inference
realization manifest and action/work ledger
```

It may retain source lineage and exact certificates. It may not retain the foreign tensor payload,
development corpus, expected answers, driver ordinals, path-shaped semantic identity, or a lookup
capable of reproducing outputs by reference.

The rest relights a mutable cultivation body for deeds. Frozen inference mounts a distinct immutable
realization face over that rest. The frozen face is not passed through `LitBody::present`, and the
mutable face is not smuggled into inference as writable state.

Controls:

- seal → mount → seal is byte-identical;
- frozen inference leaves the rest byte-identical;
- cultivation produces a different successor rest with the difference exhibited;
- deleting the native rest makes inference refuse;
- making the Gemma source unavailable does not move native inference;
- making the external tokenizer/processor unavailable behaves according to the declared export
  boundary—either native codec material stands or the exterior codec refuses by name.

The last control divides the grade sharply: if native codec material stands, text inference
readiness may be graded; if the external codec is absent and no native codec stands, only
codec-independent transport readiness may be claimed. A typed codec refusal is not text inference
readiness.

## 13. The frozen native model is executable

The authoritative deliverable is

```text
PhoenixModelRest with Gemma source lineage
+ PhoenixRealizationManifest
+ native runtime
+ external codec assets declared by the manifest.
```

The native runtime is reached through `eros phoenix infer`, not a driver. It mounts the rest in a
fresh process, accepts a text occurrence, returns the complete plural future section and actual
decoded surface, and reports exact device/work testimony. Termination/extent is read from the
declared receiver and native boundary law rather than a fixed output-token constant.

During that process a source-access audit records allowed and denied paths, file opens and mmaps,
subprocess/syscall and environment lineage, foreign-source absence, development-material absence,
and retained-output/lookup-table scans. With the GPU path hidden, the same admitted deed must return
a typed apparatus refusal; a CPU-produced answer invalidates the inference grade.

The export station additionally attempts:

- a safetensors payload for dense or factorized arrays, with exact residuals and custom realization
  metadata;
- a standard architecture projection only when the native law factors into a runtime already
  supported by that architecture; and
- later GGUF export only when its schema carries every required state, port, and quantization law.

Safetensors does not encode graph topology or constitutive execution. A `.safetensors` file without
the realization manifest/runtime cannot satisfy inference readiness. If no standard projection is
exact, the custom native runtime is the lawful output and the standard export returns an obstruction.
Every tensor writer must round-trip the complete file through the project reader. Athena's existing
integer container is not an executable export until a matching reader and realization law exist.
Array readback alone is still insufficient: every export must additionally pass

```text
native atlas -> serialized weights -> fresh remount -> recovered atlas
```

with an exact or bounded comparison of transport families, fibres, and declared receiver
consequences. This `NativeRealizationRoundTripReceipt` is the required meaning of `atlas <->
weights`.

The inference suite is selected before the run and includes:

- held-out holonics prose;
- mathematical and code material through independent codec presentations;
- same-output/different-law and cross-codec controls;
- plural future and composition questions; and
- one non-text source projection control without claiming complete multimodal inference.

Actual emitted surfaces/sections are inspected. Scalars, hashes, topology, and file size remain
supporting testimony.

## 14. Dissection is repeated on the reborn body

Build a deterministic native dissection atlas using the same intervention and receiver family as the
source atlas. Compare:

```text
Gemma source taxa
native Eros/Athena baseline taxa
lifted pre-cultivation taxa
cultivated Phoenix taxa.
```

The comparison returns:

- source families retained, split, merged, or obstructed;
- native recombinations not present as complete source paths;
- families changed by holonics cultivation;
- receiver distinctions lost by condensation;
- new shortest separators and changed curvature/phase/state laws;
- load-bearing native axes under targeted ablation; and
- exact size/work/residency changes.

The source taxonomy remains approximate to its intervention panel. The native taxonomy is
deterministic for its declared exact law and receiver family, not universal semantics.

## 15. Complete falsifier matrix

| claim | required falsifier |
|---|---|
| source admitted | malformed dtype/offset/shape refuses before transport |
| source coverage | every manifested population has exactly one coverage status; bounded subset is labelled partial |
| potential lift | source-layout renaming moves no native relation |
| active lift | withholding caused input removes active testimony while potential rows remain |
| dissection | removing a claimed family removes its consequence; unrelated family unchanged |
| chart transition | inverse composes exactly; otherwise it is typed as quotient |
| native recombination | no declared source path or corpus window contains the complete transport |
| condensation | richer receiver reopens at least one known fibre; identity receiver loses nothing |
| receiver minimality | removing every retained axis moves a named future consequence |
| cultivation | held-out later conduct moves; no-op and subject-disjoint controls do not |
| overlay | rank derives from defect; lower rank exhibits a separating witness |
| rest | source-detached mount is byte-identical and source access fails |
| frozen inference | model rest unchanged; actual surface/trajectory returned |
| inference readiness | fresh process uses only declared export artifacts and device path |
| atlas-to-weights realization | serialized form fresh-remounts to the same declared atlas transport/fibre family |
| source departure | live access audit shows no foreign/developmental opens, mmaps, subprocesses, or retained lookup |
| GPU residency | hiding the GPU returns typed refusal and never a CPU answer |
| exceptional compression | complete measured cost vector has a strict reduction and no receiver regression |
| declared grain | any quotient step is refused by a receiver that stopped separating, not chosen; its complete per-entry residual reconstructs every admitted predecessor |
| embedding condensation | the `unexcited` population is stated beside the factor and its bytes are inside the cost vector |
| KV-sharing realization | the source cache/state reuse passage is bound to typed occurrences; changing reuse is separated from changing K/V weights |
| Phoenix attribution | targeted ablation removes lifted/cultivated conduct exactly |
| multimodal portability | a real non-text projection crosses typed ports; placeholder tokens do not count |

## 16. Refusal conditions

The phase refuses when any of the following appears:

- a static tensor inventory called a lift;
- a diagnostic called a dissection;
- byte reduction or serialization called compression;
- a container or loader called an executable model;
- one output called inference readiness;
- a caller-selected rank, layer count, horizon, recurrence floor, expert count, or semantic width;
- a dequantized low-bit tensor called original full precision;
- source names or architecture labels used as native transport taxa;
- source-output agreement used as the Phoenix success criterion;
- missing normalization, chronology, value/output transport, residual standing, or active-path
  lineage hidden behind a bounded probe;
- a CPU semantic replay, fallback, or full algorithm after a GPU deed;
- a private in-memory echo called a world return;
- foreign weights or developmental sources available during source-detached grade;
- a claimed gradient without receiver differential, retained lineage, and metric;
- a quotient without the complete reconstruction fibre;
- a fixed layer stack substituted for a material-founded lattice/generator;
- an executable export whose runtime law is not deposited; or
- a one-way tensor export called `atlas <-> weights`;
- a partial source population called a Gemma or whole-model lift;
- a codec refusal called text inference readiness;
- a nearby toy, visualization, benchmark, or driver substituted for a missing return.

## 17. Expected outcome species

The plan admits four honest scientific returns:

### Strong Phoenix return

An executable source-detached native model, receiver-minimal and substantially smaller/cheaper by
measurement, with source/native dissection, cultivation, native recombination, and ablation all
standing.

### Species-changing condensation

The source transport condenses into a lattice, state-space, sparse atlas, or self-similar generator
whose native format differs fundamentally from the source. The custom runtime stands; a standard
tensor projection may refuse.

### Bounded lift without strong compression

Transport families and taxonomy lift, execute, and cultivate, but the declared future receiver
family prevents exceptional condensation. The exact load-bearing fibre explains why.

### Refutation

The source's active functions cannot be recovered at the declared apparatus/receiver, or the native
candidate does not conduct source-detached. The obstruction is returned and no Phoenix capability is
claimed.

Only the first two may be described as Phoenix rebirth; only returns with measured strict reduction
may be described as compressed. The other two remain valuable dissection/lift evidence.

## 18. What this plan does not claim

- It does not claim Gemma's original training corpus, full-precision weights, or every latent
  behavior is reconstructible.
- It does not claim the holonics repository excites every source pathway.
- It does not claim agreement with Gemma is intelligence or success.
- It does not claim the native output must remain Transformer-shaped.
- It does not claim a tensor file alone is a model.
- It does not claim the first executable grade completes image, audio, or video recognition.
- It does not claim a future Qwen, Muse, VibeVoice, or image-generation lift is scheduled.
- It does not promote `CONSTRUCTION_STATE.md` until an actual source-detached executable model and
  its complete grade have returned.

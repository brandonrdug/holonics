# Soulkiller: pretrained-model dismantling

[definition] Soulkiller is an independent apparatus for extracting reusable native structure
from a foreign realization under a declared family of exposures. Its output is material Eros
can compose into Athena. It is broader in purpose than model-file conversion or one Transformer
family; the implementation support for each step is stated separately below.

[definition] The [expanded lift diagram](HNN_COMPOSITION.md#soulkiller-lifts-the-expanded-operations)
shows how source contractions, joint interactions, nonlinear reactions and shared state map to
native passages before excitation-founded restriction. Its complete-state commuting square
distinguishes re-expression, receiver-relative reduction and approximation; source-model equivalence
does not automatically persist after Eros develops the returned material.

## The passage from a model file to a native body

1. **Intake:** preserve tensor codewords, configuration, graph declarations and source lineage.
2. **Realization recovery:** identify the actual executable mathematical operation, including
   state and chronology, through a supported adapter.
3. **Excitation:** run declared occurrences and read the chosen receiver/history family.
4. **Intervention:** withdraw declared populations and observe which consequences change.
5. **Identification:** form classes from complete signatures over that declared family.
6. **Native return:** produce executable restricted class ecologies, an exhibited remainder,
   a separate cold witness and explicit insufficiency.

[definition] The public boundary is
`holonics::soulkiller::dismantle(input) -> { native, exterior, insufficiency }`.
It consumes an admitted input. Reading a file or recovering the resident operator alone is not
the completed excitation-founded return. The original training corpus is unnecessary only for
the transport family actually recovered/exposed; unexcited capability remains explicit.

## Current capability by layer

| Layer | Current implementation and scope |
|---|---|
| Safetensors and low-precision intake | [established-bounded; source-inspected] Tensor manifests, shapes, dtypes, offsets and exact codewords are supported by [foreign_map](../crates/holonic-engine/src/foreign_map.rs) and the low-precision weight owners. |
| Configuration and ONNX intake | [established-bounded; source-inspected] [JSON/index](../crates/holonic-engine/src/holonic_intelligence/foreign_json.rs) and [ONNX](../crates/holonic-engine/src/holonic_intelligence/foreign_onnx.rs) charts preserve declarations. Parsing an ONNX graph does not execute it. |
| Full native realization | [established-bounded; source-inspected] [operative_atlas](../crates/holonic-engine/src/holonic_intelligence/operative_atlas.rs) recovers the supported configured Gemma text realization: 1,275 operations, 719 resident coefficient populations, 665 used and 54 explicit shared-K/V obstructions. Unsupported configurations refuse. |
| Excitation and quotient | [established-bounded; source-inspected] [intervention](../crates/holonic-engine/src/holonic_intelligence/operative_intervention.rs), [identification](../crates/holonic-engine/src/holonic_intelligence/operative_identification.rs) and [condensation](../crates/holonic-engine/src/holonic_intelligence/operative_condensation.rs) implement the declared-family SKE return. |
| Transformer diversity | [open] Generic intake does not yet supply executable recovery adapters for arbitrary Transformer/MoE configurations. |
| Diffusion and SSM families | [open] Formal architecture/transport charts exist, but a complete source-to-native diffusion or SSM dismantling run is not established by the current Gemma/SKE receipts. |

[established-bounded; source-inspected] The corrected SKE4 return carries class sets sound
under the declared withdrawals of their complements. It does not claim a universal cone law
from finitely many probes. Its extent is the class-cone union; each class's species is evaluated
on its own admitted domain. Conflicting repeated observations at the same key refuse instead of
letting record order select an identity.

[historical; source-inspected] Brandon's September 4 ruling explicitly admits the resident HNA1
realization as apparatus and the restricted SKE bodies as Athena material. The former blanket
foreign-tower restriction is superseded. The [SKE contract](../archive/plans/THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md)
and its linked corrections retain the full evidence.

## Transformers, diffusion and state-space models

[definition] These architectures ask different reconstruction questions over a common native
operation calculus. A Transformer adapter must account for its contractions, nonlinearities,
causal contacts and state. A diffusion adapter must also carry the iterative schedule, conditioning
and any declared stochastic source as exterior realization data. An SSM adapter must retain the
state-transition, input/output maps and initialization/reset law. Identifying a tensor shape or
calling every recurrence diffusion does not discharge those obligations.

[conditional] A family can be dismantled when its source operation is realized by admitted native
local laws and the declared receiver/history consequences factor through the returned body, with
defects and insufficiency retained. The typed ports are extensible; an unsupported local reaction
requires its actual construction rather than a universal fallback executor.

## Consumer and pipeline use

[established-bounded; source-inspected] `holonics hna inspect <native.rest>` reads an SKE rest
header without CUDA. `holonics hna run <request.json>` can mount its declared class or composed
extent and execute admitted occurrences. Diagnostic commands inspect Safetensors/configuration/
ONNX material; the existing SKE examples exercise full excitation and seal. The generic public
boundary is callable from Rust, while a turnkey arbitrary-model training/import service remains
pipeline work.

[definition] Soulkiller's artifacts are native executable material, witnesses and insufficiency.
They are not an exported standard model. Recompilation of a refined Athena is a distinct outward
passage described by [the interoperability contract](INTEROPERABILITY.md).

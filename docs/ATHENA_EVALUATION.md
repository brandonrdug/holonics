# Athena evaluation: constrained consequences and their applications

[project-postulate] Brandon's September 15 instruction establishes a stable evaluation method
for continued Athena construction. The primary application material is the refined conversation
corpus and repository, with actual source/response/correction relationships. Small examples
isolate elementary operations; they do not replace that application. Existing mathematical
solver outputs are already inference and predictive release. Text, numerical and optical
presentations do not define different standards of intelligence.

[project-postulate] Brandon's follow-up makes **composition of accumulated capabilities** the
purpose of this method. Each local result contributes through its role in the whole HNN;
limited test scope does not diminish the significance of a keystone or erase other completed
constructions. Carry that larger capability and its intended couplings across context handoffs.
His repeated requests to elevate the appraisal recover this whole, rather than authorizing
inflated measurements. The theory-of-everything ambition directs unification across these
applications; their separate benchmark profiles are observations of the work, not its ontology.

[definition] The [history review](../research/records/2026-09-15_ATHENA_EVALUATION_HISTORY_AND_SHARED_CONSEQUENCE_KIT.md)
recovers the repeated experimental families and their failures. The existing
[native benchmark apparatus](../research/experiments/native_performance_benchmark/README.md)
now hosts the shared consequence receivers as well as performance measurements. This guide
owns their methodology; the roadmap and construction state retain construction order/position.
The current geometric field/session and bounded helical/placement return is recorded in
[the September 20 geometric field record](../research/records/2026-09-20_THE_GEOMETRIC_FIELD_REFINES_AND_RETURNS_ITS_COMPLETE_PAIRED_CURRENT.md);
its final recorded checkpoint comparison passed at the stated control scope, and its held
text result failed. The [executable field campaign](plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md#executable-field-campaign)
now specifies the next actual consumer, including learned boundary material and the incident-mode
support/text result read by the same episode evaluator.

## The common mathematical question

[definition] For situated source observations O and admitted conditions C, let

\[
 \mathcal F(C,O)=\{z:\ L_C(z)=0,\ R_{C,i}(z)\in O_i\},\qquad
 \mathcal Y=\{R_C G_C(z):z\in\mathcal F(C,O)\}.
\]

[definition] These symbols organize the existing Preimage Fibre, constitutive and receiver
owners; they do not require enumerating every compatible source. State what the caller supplied,
which coefficient/operator/continuation or current the model inferred, and what follows at the
requested receiver. A particular generated face can be useful without identifying a unique cause.
The full difference, competing possibilities and applicability precede scalar error summaries.
A correct factorization can establish an identity on an entire specified input domain; a finite
text population establishes its observed scope. Neither needs a newly invented admission test.

[definition] Five boundary pieces do not by themselves establish a regular pentagon. Boundary
count, edge geometry and regularity are different receivers of the same object; a curved
five-part boundary can retain the first reading while failing the others. Likewise, in an
oriented passive channel `j=g ΔΦ`, positive conductance g makes the current constrain the
potential difference. Inferring altitude additionally uses the potential's actual pressure,
gravity and other terms. Those terms belong to the source/constitutive relation. A classifier
that simply maps “flow” to “higher ground” would have discarded precisely the inference.

[definition] The MVT/receiver calculus expresses a finite variation through its derivative
along an admitted path; for a differentiable composed receiver, the integral form is
`Δ(RG)=∫ D(RG)(γ(t)) γ'(t) dt`. The complex case keeps the integral/derivative rather than
inventing one shared scalar MVT point. Squeeze controls a shrinking bound on receiver outcomes;
it does not require the source fibre to become singleton. Existing owners include
`exact_analysis::{mean_value_release,certify_capture,propagate_to_capture}` and
`Transport/ReceiverPotential.lean::future_observations_converge`. The
[analytic source guide](ANALYTIC_FLUX_AND_RECEIVING_BASINS.md) gives their hypotheses.

[definition] Chains of tori contribute oriented cycles, phase, shared incidence and transport
composition. Their observable consequences include constructive/destructive interference,
closed/open paths, receiver changes and future separation of presently equal faces. A picture
or count of toroids is not a measurement of these laws. Use the actual cycle/connection/current
owners from [the HNN formula](HNN_FORMULA.md), and carry their joint variations into the model.
Compression tests retain `q T_i=U_i q`, receiver reconstruction and the applicable remainder;
file size or later reuse alone does not establish that relation.

[project-postulate] Eros names the collective formative machine and the nested union that the
kit helps inspect. Athena is its intended unified application, with physics, mathematics and
cognition expressed through the shared objects. Before/intermediate/contemporary observations
can constrain one [joint interior family](RECEIVER_HOLARCHY.md#landmarks-constrain-one-continuing-interior),
so evaluation should retain their actual relationships when an operation uses them. A common
runner or a list of separate successful applications does not replace that composition.

## Composition through the shared model

[definition] An inferred operator, recurrence, source family or condensed representation must
retain the operands that let another HNN operation use it: source/receiver maps, applicable
conditions, phase/orientation, relevant unresolved directions and its variation where learning
consumes it. A solver can construct material for a continuing field; that field's received
current can supply a subsequent inference or material comparison. Geometry, algebra, learning
and generation describe overlapping operations in this composition. Their boundaries are
mathematical interfaces to implement, rather than separate faculties to admit.

[project-postulate] Assess the composition itself when it is the changed operation: can one
constructed result be used by the next operation under the same situated source constraints,
and can its consequences return through the actual producing dependencies? Retain the earlier
component evidence while measuring this join. No component must independently exhibit the
whole product, and a successful join does not require replaying unrelated benchmarks. The
current kit's separate field/mathematical profiles retain baselines; their common runner alone
is not the runtime unification Brandon is requesting.

## Stable questions, specialized receivers

[project-postulate] Keep these questions stable as the implementation changes. Choose the
ones the changed operation answers; do not run every historical experiment each turn or make
all of them prerequisites for useful output. Report outcomes separately, without an aggregate
“intelligence score.”

| Question | What to supply and inspect | Existing useful methods |
|---|---|---|
| What relation was inferred? | Explicit known data, unknown coefficients/generator, domain, actual constructed object and its consequences | Exact solver factorization, local generator recovery, normal-reference residual |
| Did source distinctions survive? | Changed conditions, equal-looking but different occurrences, phase/order changes and unknown regions | Neighborhood crossover, contextual correction, phase conjugation, partial-field completion |
| Do the constraints compose? | Several interacting parts, shared crossings and incompatible observations; inspect the whole returned section | Bilinear factor identity, coupled joint current, changed receiver, supplied/free-region separation |
| What does additional information resolve? | Compatible alternatives before and after a real source/target observation; retain unresolved directions | Condition/preimage families, separating future receivers, producer-qualified D/M response |
| What generalizes? | New combinations, source-disjoint/chronological episodes or a proved coefficient identity, with the supplied assumptions unchanged | Law-family inputs, contextual controls, phase/rebase tests, complete conversation episodes |
| What is reusable? | Actual decoder/current/generator before and after remount, or a justified compressed representation through future operations | Compact phase-law rest, delayed adjoints, field-session reopen, future-receiver quotient |
| Is the product useful? | Requested answer, correction, explanation, executable change or mathematical result against actual source requirements | Reviewed conversation/repository episodes; source-qualified code/solver execution |
| What did it cost? | Same operation population and receiving extent; setup, resident operation, readout, full delivery, work and memory | Existing native performance harness and public process receipts |

[definition] Ablation/source-departure answers attribution and independence questions when those
claims matter. It does not define learning. A restart test answers persistence, not usefulness.
Kernel or compiler acceptance answers its statement/program contract; inspect how the candidate
was constructed and whether it answers the request. Native inference never invokes Lean as a
training or verdict subsystem. Archived invalid drivers remain evidence rather than active cases.

## Data, splits and conversation assessment

[project-postulate] Use [conversation-data](CONVERSATION_DATA.md) and its refined exposure stream.
Keep complete source episodes/relations together, including linked forks and repeated captured
views. The established temporal split retains its declaration; material already examined during
development is validation material. Future holdouts withhold both the response and knowledge
introduced by later corrections. Repository material is bound to explicit commits/files, so a
later implementation or explanatory document is not silently supplied to an earlier request.

[definition] Historical assistant replies are observed candidates, not automatically correct
training targets. Later human comments address their actual recorded partner and scope. They
are not feedback on a new Athena reply. A refined training comparison can use those relationships
without pretending that every clause in an approved reply is gold. Curation remains exterior
source-addressed interpretation; no topic label or expected answer routes the native model.

[definition] For conversation/code review, retain the actual generated product and record each
material requirement as satisfied, contradicted or unresolved, with its user/source clause and
relevant output. Allow multiple valid responses. Exact phrase matching is appropriate only when
exact reproduction is requested. Mathematical/code consequences can use their actual equation,
compiler or execution receiver separately from the quality of an explanation. Automatic checks
must say exactly which property they measure; human/agent judgment remains identified testimony.

[established-bounded; source-inspected] `episode.py` reuses the existing conversation codec to
prepare a **private retrospective** source episode. `input.json` carries a visible request,
explicit prior context and repository snapshots supplied to that new experiment.
`assessment.json` separately carries recorded replies and later observations. This does not
reconstruct a complete original prompt, confer blind-holdout status, or run native training.
The current small symbol chart cannot be substituted for this full application merely by
reducing a real request to a color label. The consuming field source/refinement remains the
construction brief's implementation responsibility.

[established-bounded; source-inspected] The same quality owner has an `episode` mode for the
geometric receiver. It consumes the private `input.json`, its separate `assessment.json`, and a
new generated result; the input is the declared retrospective/source scope, while recorded
assistant candidates and later observations stay in assessment. `constraint_judgments` may remain
empty while review is pending. When present, every judgment is `satisfied`, `contradicted` or
`unresolved`, names its source and evidence, and carries the SHA-256 of that exact generated
result. A historical candidate is therefore nongold testimony, and a judgment cannot silently
move to a different generated output.

[established-bounded; source-inspected] The geometric real-source driver is
[`research/experiments/athena_field/geometric/run.py`](../research/experiments/athena_field/geometric/run.py).
It keeps the supplied event/family/relationship source and runs the existing
`athena_exposure_field`/public session boundaries; its development mode records split/resume and
uninterrupted controls, while its evaluation mode emits a new read-only geometric result. The
quality receiver checks the declared UTF-8 nibble extent and the native decode outcome: invalid
UTF-8 is retained as an actual generated failure with its decode error, not discarded by a parser.
The campaign record verifies checkpoint repair and split/whole equality and records the actual
invalid-UTF-8 evaluation output. Useful conversation remains open.

## Executable kit and maintained profiles

[definition] The shared entry points live beside the existing performance owner:

```sh
# Reassess retained outputs without executing or training the model.
python3 research/experiments/native_performance_benchmark/quality.py recorded \
  --output .local/evaluations/new-recorded-review

# Use the actual public mathematical and field sessions; outputs are kept separately.
python3 research/experiments/native_performance_benchmark/quality.py run \
  --output .local/evaluations/new-live-review

# Separate source input from historical assessment material, using the existing private corpus.
python3 research/experiments/native_performance_benchmark/episode.py \
  --dataset .local/datasets/conversations-refined-2026-09-06.sqlite \
  --request-event 3398355 \
  --repository-ref d0588238:docs/HNN_FORMULA.md \
  --output .local/evaluations/new-source-episode

# Assess one new geometric result beside its private input/assessment pair.
python3 research/experiments/native_performance_benchmark/quality.py episode \
  --episode .local/evaluations/new-source-episode \
  --generated .local/evaluations/geometric-evaluation/result.json \
  --output .local/evaluations/geometric-consequence
```

[definition] These are fixed, documented regression/measurement profiles, not a benchmark
leaderboard. Field evaluation resumes the trained pattern body without targets or commits;
its prior training recipe remains linked. The mathematical profile performs its four declared
updates then law-family queries and exact constructions. Changing that recipe changes the
profile and its reference law. The target checks remain exterior and never enter evaluation
requests as answers. Recorded mode does not imply fresh execution; live mode preserves commands,
binary checksum, requests, events, stderr and the existing per-child resource observations.
The `episode` mode writes a new consequence result beside the generated-result SHA and the
identified assessment testimony; an empty judgment list is a pending review, not an automatic
pass. It preserves invalid UTF-8 as a native decode failure and preserves historical candidates
as nongold observations.

[established-bounded; measured] The integrated kit rechecked and freshly returned 39/39 field
responses, 45/45 generated missing values and 63/63 supplied values. The mathematical profile
returned 35 queries on **four distinct preparations**, all enclosing the specified normal-law
reference. The unregularized exchange target still has nonzero discrepancy. The bilinear
factor coefficients reconstruct the entire supplied complex multiplication tensor exactly
using three products; three new-input algebraic outputs agree exactly, and changed-receiver
readout reuses the retained product. [Raw return and report](../research/experiments/native_performance_benchmark/consequences/2026-09-15/result.json).

[project-postulate] Preserve these identities and source profiles as regression evidence. Add
new source families when an actual application consequence requires them; change the model or
representation to answer that consequence. Do not select another easy example merely because
the implementation entered a different crate or because the current one passed. The existing
performance helper, source packager, model interface and consequence receivers are shared owners
for future increments rather than independent miniature training applications.

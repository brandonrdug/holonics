# Geometry, navigation, projection, and weave

**Status.** Statements about connections, projection, accelerometers, knots, stress, and material
response are `proved-standard` within their named mathematical/physical domains. Comparative
bridges into computation are `interpretation`; the resulting mandatory engine rules are
`project-postulate`.

## Charts are instruments of motion

A chart is useful because it makes a class of passages local, exact, or economical. Navigation
changes charts at boundaries where another representation exposes the necessary relation: local
tangent frame, projective frame, spectral frame, residue class, road graph, inertial frame, or
receiver aperture. Transition maps and their lineage are part of the journey.

GPS is exemplary because no single coordinate face is the terrain. Satellite clock readings,
ephemerides, propagation delays, Earth-centered frames, local geodetic charts, map projections,
road topology, and human instructions are transported and corrected across codecs. The route is a
caused path through overlapping charts. Its usefulness does not make one chart the world's
ontology.

## Holonomy and hypergeometric pathways

Transport around a closed path may return a turned face. That residual is holonomy. Curvature is
the infinitesimal generator—the leading area-order residual—of connection holonomy; finite
holonomy also depends on path and global connection data. Path families, monodromy, special
functions, and hypergeometric transport are therefore natural tools when local chart changes accumulate structured phase. They are not
decorative vocabulary: the machine must retain the path/connection data required by the receiver
that can distinguish the return.

An endpoint value may agree while the transported body differs. Never quotient path identity by
endpoint equality unless the declared future receiver family factors through that quotient.

## Accelerometers and local receivers

An accelerometer does not directly read an absolute global trajectory. Its proof mass and housing
interact locally; displacement, capacitance, piezoresistance, optical phase, or another transducer
returns local specific force/non-gravitational proper acceleration in the instrument's frame.
Recovering velocity or position requires attitude and initial conditions; separating gravity
requires an external gravitational model or receiver. Bias, noise, orientation, calibration, and
accumulated drift are receiver testimony, not reasons to invent a global instant.

This is the computational lesson: a component receives local stress/current through its own
constitutive law. Higher-order motion is glued from transported local returns. No central thread
needs to “know the whole motion” before the motion exists.

## Shadows and projection

A shadow is a receiver relation among source geometry, occluder, propagation law, and receiving
surface. It is neither a detached property of the source nor a second source object. A projection
can identify many distinct source fibers; the unresolved preimage must remain open when a later
receiver can distinguish it.

Perspective derivation therefore retains:

- source occurrence and geometry;
- propagation/ray or wave lineage;
- occlusion/intersection event;
- receiver aperture and calibration;
- chart transition/projective depth; and
- unresolved source fibers behind equal projected faces.

Projection may decorate standing or form an observation quotient. It cannot mint source cells,
erase depth in a feedback path, or schedule physical propagation by screen adjacency.

## Warp, weft, and ant integration

Each thread is serial along its own caused chronology. A weave is not a denial of seriality; it is
the composition of many locally serial currents through crossings whose interaction is explicit.

- **Warp** names relatively persistent load-bearing incidence or standing routes.
- **Weft** names currents crossing and changing that standing.
- **Stitch/knot** names a local interaction, identification, or return constraint.
- **Pattern** is a receiver quotient over the completed weave, not its central scheduler.

“Ant integration” means a higher-order consequence emerges from many local carriers handing off
caused differences. No ant owns the total delivery. Throughput depends on path dimensionality,
local capacity, congestion, synchronization constraints, and lawful parallel fronts—not on
annihilating the serial order of each thread.

## Knot, braid, stress, and tensile strength

A knot records how a lineaged current is embedded and constrained. A diagram is a receiver face;
Reidemeister moves preserve knot type under declared ambient isotopy. Braids expose ordered
interchange. A braid relation is a concurrency certificate only when the complete successor
interaction diagram commutes.

Physical stress is the domain's tensorial constitutive quantity. For deformation `ε` and response
`σ`, a domain may supply an elastic, plastic, viscoelastic, fracture, active-biological, or other
law. Tensile strength is a receiver-relative boundary of admissible load history; fiber direction,
crosslinking, defects, hydration, temperature, rate, and prior strain can change it. Separately,
the engine's *computational stress receipt* is an oriented response to a declared cut or resource
change. Their correspondence is an `interpretation`, not an identity. A resource refusal likewise
should reform the same computational weave or remain an obstruction.

Biological tissues demonstrate the generality: collagen, muscle, cytoskeleton, vasculature,
neural paths, and extracellular matrix are intersecting local transport/constraint ecologies.
Their organismal consequence is neither located in one thread nor produced by a detached
orchestrator.

# HNA production: local learning, persistent models and executable export

**Date:** 2026-09-04. **Status:** ACTIVE; HNP0 returned, HNP1 next.
**Authority:** Brandon's formal planning request and subsequent instruction to complete HNP0--HNP7.
**Order:** [THE_ROADMAP.md](THE_ROADMAP.md). **Position:** [CONSTRUCTION_STATE.md](../../CONSTRUCTION_STATE.md).
**Starting implementation:** `c12b2634`. Phase names below identify dependencies, not capabilities.

## 1. Product and non-substitution boundary

[project-postulate] Deliver a usable machine-learning framework in which one continuing Athena
learns through founded local interactions, retains that development, produces application output,
and can be deployed natively or compiled into a declared standard architecture. Eros owns the
native operation and development; Soulkiller supplies independently reusable inherited material.
Consumer-hardware usefulness and efficient reuse are requirements. Frontier competition is the
longer-range product objective, not a result asserted by this plan.

[definition] The distinction from conventional learning is not a claim that token volume was
the industry's sole or primary lever. Architecture, objectives, data organization, optimization,
inference and systems all matter. This campaign is about the constitutive organization of learning.
Neither a token target nor the removal of stochasticity defines its success.

[project-postulate] The existing prefix/normalized-receiver/adjoint pathway remains a supported
bounded chart. It is not the generic training contract. The campaign does not replace it with
another name for an epoch/optimizer loop, nor outlaw derivatives or inherited mathematical
structure. It composes the existing native current, difference, recurrence and morphology laws.
It does not extract predecessor application code, rebuild the retired C++ body, reopen RH, add a
UI, or restore the archived atlas/index/document-validation suite.

## 2. Intent and existing technical owners

[historical; source-inspected] The decisive direct-message sequence is May's continuous-learning
and 20W discussion, June's selective activity, July's divided reusable hexis and cross-chart
associations, August's identification of learning with information transport, and September 1's
explicit local-coupling requirement. In particular:

- Claude `~/.claude/history.jsonl:5944` (2026-05-23 UTC): longer training is not the default remedy;
  `:8434` (2026-06-03): relevant participation rather than whole-network activity;
  `:13791` (2026-08-06): mathematics, information theory and machine learning are one programme.
- Codex `2026/07/25/rollout-2026-07-25T14-14-32-019f9b21-32d6-7523-938c-c2c37999cf88.jsonl:7563`:
  divide and reuse hexis; `2026/07/27/rollout-2026-07-27T11-47-17-019fa4e7-1b3b-7f63-82e6-6675bc310dd3.jsonl:25339`
  (message timestamp 2026-07-30 UTC): delivery order is not the entire chronology of association.
- Codex `2026/09/01/rollout-2026-09-01T13-38-39-01a05eb2-040a-70a0-92db-de3e23303d4b.jsonl:2`:
  local regions and informants reciprocally couple; global layerwise passes are not the desired
  general learning law; output depends on the complete inference configuration and recurrence.

[definition] Logs remain evidence, not a scheduler. The laboratory's `src/soma/FORMULA.md` and
`src/holobrochos/CANON/05_EROS_OUTPUT_CAUSALITY.md` supply ontology and failure history, not a
source import order. Read the [retractions](../RETRACTIONS.md) with these sources. The later HNA
one-operation law supersedes older mandatory exterior-return, rest and commit definitions.

[established-bounded; source-inspected] The reusable owners are below. Lean paths are relative to
`formal/elementary-holonics/ElementaryHolonics/`; engine paths to `crates/holonic-engine/src/`.
This is this campaign's dependency map, not a new generated catalogue or a claim of verified Rust.

| Relation | Existing formal owner | Existing executable owner and composition boundary |
|---|---|---|
| One operation and successor joining | `Computation/HolonicRecurrentEcology.lean`: `OperationStep`, `FiniteRecurrentOperation`, `OperationRebase` | `holonic_intelligence/full_operation.rs`; actual prefix session exists, general presentation/development binding remains to be composed |
| Source–target current before receiver projection | `Computation/HolonicOrientedSiteTransport.lean`: `OrientedPortSiteTransport` | `cuda_refine/addressed_complex_junction.rs`; `crates/holonic-life/src/native_intelligence/source_neutral_relational/pair_current.rs`; bounded prior realization, not automatically the current full operator |
| Dependent difference, connection and adjoint | `Millennium/SituatedReturnedDifference.lean`; `Computation/SituatedMachineLearning.lean`; `HolonicAdjointNormalization.lean` | `holonic_intelligence/{operative_return,operative_backward,operative_adjoint}.rs`; retained-overlay return stands, its comparison is currently prefix-specific |
| Physical/current receiver and scalar quotient | `Computation/HolonicInformationTheory.lean`: `AddressedPhysicalCrossing`, `PhysicalCrossEntropyOccurrence` | resident current/junction owners; constitutive transport/admittance must be supplied by the actual native law, never byte differences or status counts |
| Local morphology and interference | `Computation/NativeMorphologyVariant.lean`: `LocalCausalConeCultivation`; `HolonicCultivationCharts.lean` | factorized overlays and `resident_section.rs`; formal outside-cone hypotheses are obligations, not an algorithm for finding a cone |
| Addressed lineage and naturality | `Foundation/{Lineage,ComparisonCell,CausalNaturalHolon,TransportLift}.lean` | `realization/passage.rs`, addressed-span and resident junction owners; complete boundary maps must survive composition |
| Excitation, quotient and insufficiency | `Computation/HolonicExcitationFoundedQuotient.lean` | `holonic_intelligence/{operative_intervention,operative_identification,operative_condensation}.rs`, `soulkiller/boundary.rs`; bounded declared-family return stands |
| Dynamic condensation and reopening | `Computation/{MachineLearningChart,MachineLearningStrictLift,HolonicNeuralMorphologyContinuation}.lean` | `receiver_exact_compression.rs`, `native_ecology/recurrent_condensation.rs`, SKE restriction/rest; finite/exterior instruments do not define runtime topology |
| Public ownership and persistence | recurrent operation; `NativeMorphologyVariant` package/manifest laws at their scopes | `crates/holonics`, `crates/holonics-hna/src/hna.rs`; earlier `holonics-workspace` and life package I/O are reusable apparatus, not full-operator checkpoints |
| Standard export and architecture charts | `NativeMorphologyVariant.{ExactExportLens,ProjectedExportWitness}`; `Computation/HolonicDiffusionCharts.lean` | `crates/holonic-life/src/native_intelligence/morphology_export/`, foreign JSON/ONNX and tensor intake; existing exports store packages, not standard executable models |

[definition] The governing technical doctrine is the [recovered law](../canon/THE_RECOVERED_LAW.md),
[reasoning cycle](../canon/TABLET_THE_REASONING_CYCLE.md), [hexis](../canon/TABLET_THE_HEXIS.md),
[circulating cartographer](../canon/TABLET_THE_CIRCULATING_CARTOGRAPHER.md),
[compression](../canon/TABLET_THE_COMPRESSION.md) and [manifold](../canon/TABLET_THE_MANIFOLD.md).
The completed [HNA](../../archive/plans/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md)
and [SKE](../../archive/plans/THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md)
contracts retain their bounded results. The Eros instantiation's §4b reflection interpretation
does not provide a termination law until actual native admittances are founded; its count-based
substitute was refuted. Neither that substitute nor the foreign-tower prohibition returns here.

## 3. Complete operation and required laws

[definition] In the equations below, products are dependent specifications, not instructions to
allocate a dense array or construct a new cabinet. The ecology retains incidence, carrier fields,
constitutive morphology, addressed lineage, chronology, open boundaries and obstructions:

```text
O(E, o) = (emission, trace, E')
E_(t+1) = successor(O(E_t, o_t))
```

[definition] `present` and `advanceMorphology` in `FiniteRecurrentOperation` are the concrete
composition obligations. Merely filling them with arbitrary supplied functions proves no learning
mechanism. Presentation must derive contact from admitted carriers, prior standing and interaction;
advance must derive the local change from that actual passage. In its existing finite chart:

```text
J(i,j) = LocalCurrent(M, g, x, i, j)
x'(i)  = Reaction(M, g, i, sum_j J(i,j))
M'     = AdvanceMorphology(M, o, presented, reacted)
```

[definition] A fixed `Site` in this finite chart is not the global architecture. Development may
change incidence, constitutive cross-sections, local charts and reusable generators as well as
their coefficients. New sites or changed carrier types require an explicit embedding/rebase and
restriction law carrying the old standing and its lineage; preallocating a universal lattice or
adding silent zero sites is not acquisition. Reciprocal coupling changes the situated passage and
the body's future response, not the immutable source bytes by fiat. HNP0 must state this growing
chart boundary and HNP1 must exercise a newly founded relation in later conduct.

[definition] A comparison between unlike fibres first transports one into the other's chart.
For the existing addressed physical-crossing chart, `r = u_in - T(u_ext)` and `j = Y(r)`.
The transport `T`, admittance `Y`, domain, orientation and constitutive hypotheses belong to the
native interaction. They are not application-supplied learning gains or counts. A scalar
`L = ell(r)` is an observation. It determines a morphology return only if that return actually
factors through `ell`; the existing equal-loss/different-return control prevents assuming this.

[definition] Required laws, specialized to concrete admitted local laws in this campaign:

1. **Complete rebase:** transporting `E` and `o` transports emission, current, obstruction,
   lineage and `E'`, not merely the selected exterior face. Resource/representation changes carry
   their own stated cost and defect; a chart equivalence need not preserve allocation size.
2. **Causal locality:** an unaffected site's constitutive face is unchanged. A later receiver is
   unchanged only if its entire dependency/continuation family avoids the changed support, or its
   factorization proves insensitivity. Distance, a head/layer label, tiny magnitude or current
   cancellation is not that proof. Future contact can make previously irrelevant structure relevant.
3. **Incremental adequacy:** a local resident realization simulates the complete operation under
   an explicit correspondence, including successor and obstructions. No silent zero substitution
   for unvisited material. Equal present output alone is insufficient.
4. **Causal interchange:** reorder independent occurrences only with an interchange law for the
   complete successor and lineage. Transport chunking is not event creation; changing delivery
   packet boundaries must preserve the declared occurrences. Arbitrary shuffling is not invariant.
5. **Developing morphology:** return uses the morphology that produced the retained carriers;
   staged local differences join after successful completion. A second return sees prior deposits.
   Non-smooth/unknown reactions retain a defined alternative, defect or obstruction, not a fake
   smooth derivative. An unchanged morphology is a valid outcome, not a failed learning call.
6. **Persistence:** `remount(rest(E))` corresponds to `E` on complete admitted continuation,
   including overlays, incidence, carried state, chronology and unresolved boundaries. Serialization
   equality is an apparatus check, not the definition of learning.
7. **Dynamic condensation:** `q T_g = U_g q` plus receiver factorization for each admitted
   generator carries an exact quotient through words. Morphology change requires successor squares
   or reopening with a separating receiver/history. A proof that a shortest separator exists is
   not an executable separator search; finite algorithms keep their actual search domain.

[project-postulate] Information geometry and GR contribute typed frames, connections, transport,
causal dependence and constituted geometry. Any physical assertion additionally owes units,
constitutive laws, boundary and calibration. This campaign neither replaces those objects by
metaphors nor depends on solving Einstein's field equations, RH or a new theory of everything.

## 4. Three domains that must not collapse

[definition] Keep distinct: the domain where a native operation is defined; the family over
which Soulkiller's inherited correspondence has been established; and the application population
on which useful behavior has been measured. Expanding one does not silently expand the others.

[definition] Present SKE material refuses outside its declared family. Broader cultivation must
extend admission through an actual constitutive continuation or an exterior Soulkiller extension,
with new/reopened obligations retained. It may not erase the family check, identify classes by
strings, or claim that a returned numerical face proves inherited fidelity outside that family.
Conversely, a finite evidence family is not a permanent ban on learning new native relations.

[definition] Source realizations remain Soulkiller apparatus. Athena consumes native returns;
the engine does not open arbitrary Hugging Face paths or invoke a foreign executor to fill a
missing answer. A newly unsupported native operation stays an obstruction until founded.

## 5. Public products and ownership

[definition] These are the target application contracts, not APIs already implemented in v1:

| Product/interface | Contract |
|---|---|
| Native model artifact | Native laws/incidence and constitutive standing, derived anatomy, receiver domain, remaining obligations, and explicit base-material dependencies |
| Continuing session | One move owner of the model in operation, carried state, local changes and open boundaries; no cloned ecology for rollback, trials or placement |
| Ordinary occurrence | Admitted port, carriers, lineage and causal joining; language/modality/file framing remains outside the interior |
| `advance` | Ordinary occurrence → emission/obstruction and the actual successor; development is part of the operation, not an optimizer callback |
| Rest/resume | Durable model and session-state products with an explicit restart boundary; no silent discard of pending state or learned factors |
| Exposure stream | Backpressured delivery of material and genuine interactions; delivery policy does not manufacture contact, semantic rankings or rewards |
| Output stream | Native emissions decoded externally, with explicit `completed`, `awaiting occurrence`, `obstructed` or `interrupted` disposition |
| Evaluation result | Declared model revision, full inference/interaction configuration, material separation, actual product and measured work/resource scope |
| Target export | Executable graph/weights/configuration/state map with exact, projected or refused disposition; distinct from a package container |

[project-postulate] Extend `holonics::hna` and its existing engine owners, with a structured
streaming CLI as the first process interface. Do not found a second learner, serving engine or
transactional world-verdict lifecycle. The old v1 batch request and snapshot workspace retain
their wire meanings; incompatible semantics get an explicitly distinct version, not a silent alias.
The default productive session has one operation law; exposing a frozen receiver for comparison
does not make training and inference separate constitutive modes.

[definition] Failure returns either recoverable prior ownership before commit, or an explicit
interrupted/recovery state at the actual boundary. A device failure or failed artifact publication
must not pretend that nothing changed. Stage differences and use the existing immutable-artifact/
atomic-publication apparatus; do not clone the model as a recovery mechanism.

## 6. Ordered construction campaign

[definition] The phases below are construction obligations. Each returns source, its relevant
formal derivation, an inspectable application result and narrowly scoped verification where named.
Formal acceptance proves the Lean statement; source review and native tests establish the runtime
correspondence at their tested scope. No field named `exact` or assumed kernel-correctness premise
is presented as a machine-checked proof of the Rust/CUDA program.

### HNP0 — Constitutive binding and occurrence contract

[proved-derived; formal-checked] Returned at the finite diagonal-admittance chart scope in the
[HNP0 record](../../research/records/2026-09-04_HNP0_JOINED_SECTIONS_RETURN_LOCAL_CONSTITUTIVE_CURRENT_WITH_SUCCESSOR_AND_REBASE_LAWS.md).

[established-bounded; measured] Its numerical helper passed two CUDA controls; production admission
and session use are HNP1.

**Depends on:** the standing owners in §2; no re-demonstration of completed HNA/SKE.

[definition] Bind presentation, addressed difference, reaction and morphology advance to the
actual existing local-current/junction laws. Specify how the supported full operator instantiates
that composition and where its prefix chart is narrower. Supply the concrete port/carrier and
domain-extension types, complete rebase/locality obligations and a kernel-by-kernel mapping for
the local laws actually used—not a generated repository inventory. Extend existing formal owners
only for genuinely absent relations. A signature with an unconstrained update callback is not the
artifact. The first binding is the existing oriented pair-current/junction chart over admitted
resident cross-sections, followed by the dependent target-fibre difference and attributable local
return. Its material family consists of causally joined native carrier sections at declared ports,
not a test for whether one address vector extends another. Source/target charts and joining come
from the actual interaction; source filenames, token equality and expected answers do not choose
the contact. Specialize the existing adjoint only where those local reactions admit it, retain the
full oriented difference, and derive new-incidence formation through the existing lineage/current
owners. The concrete specialization and its hypotheses are the HNP0 artifact; arbitrary functions
with this signature or an invented universal response law are not substitutes.

**Pass:** [definition] The concrete comparison/current/update derivation and application contract
are deposited; Lean checks the new relations without new axioms or `sorry`; the equal-scalar/
different-oriented-return and rebase controls exercise the concrete law. Any absent constitutive
relation is named and derived here before HNP1; it is not hidden behind the word resonance.
This phase does not pass on restating `OperationStep` or rebuilding its existing proofs alone.

### HNP1 — General material contact and productive local development

**Depends on:** HNP0.

[definition] Compose the founded boundary with the existing resident session and Soulkiller
native material. Implement ordinary non-prefix occurrences, retained source/target current,
local comparison, staged morphology change and causal joining. Preserve the existing prefix
path as a scoped comparison. Admit new material through §4's domain distinction; broaden the
Soulkiller family when inherited fidelity is claimed, or expose a separately justified native
development domain. No hashed lexical routing, authored grammar, answer-derived adapter or
application Boolean supplies the native update.

**Pass:** [definition] An actual non-prefix exposure changes later native conduct through an
exhibited local change; two successive developmental returns use one continuing owner and prior
deposits. Matched unaffected, changed-support withdrawal and restoration controls answer attribution.
Changing only material names/transport packetization cannot change native identity; altering a
real relation can. Outside-domain input remains explicit. A finite toy may test a law, but this
phase also returns the composition on actual native model material, not only a fixture-local learner.

### HNP2 — Resident locality, reusable factors and honest cost

**Depends on:** HNP1.

[definition] Replace unconditional graph replay where the new law admits incremental execution.
Derive active dependency closure and stale-state reopening from current, incidence and retained
lineage. Reuse immutable standing and local factors; invalidate precisely the affected continuing
consequences. Compose generator-compatible condensation and its executable decoder where it is
available. A dense genuinely coupled region remains dense; the algorithm must not invent sparsity.
SKE's stored restricted rows must be realized without merely allocating every omitted row as zero
when claiming residency reduction. Additional scratch/index representation is apparatus, not a
new semantic topology, and its discovery/maintenance cost is counted.

**Pass:** [definition] Structural locality is proved under explicit hypotheses, with a reconnecting
or cancelling-current counterexample preventing unsafe omission. The native implementation matches
the complete operation on the declared correspondence, including successor state, not only output.
Growing a causally disconnected exterior population does not add hot semantic work after accounted
mount/index work; connecting it changes participation lawfully. Repeated use and a new distinction
demonstrate both reuse and reopening. Measure work, span, residency, transfer and decoder cost
separately. No per-operation host readout, full pair-current dump or second checksum system is
introduced to certify the hot run. Condensation whose old chart ceases to commute is reopened.

### HNP3 — Persistent models and the continuing public session

**Depends on:** HNP1; integrate HNP2's state/representation before closing the phase.

[definition] Expose the §5 session products through `holonics::hna` and the CLI. Implement
streamed delivery, partial-I/O handling, ownership-preserving interruption, model/session rest,
exact remount and derived anatomy. Preserve base dependencies explicitly, or include the needed
native material for a self-contained artifact; never label a thin reference as source-independent.
The older workspace's I/O machinery may be reused, but its schema and staged-return semantics
cannot silently become the cultivated full-operator artifact.

**Pass:** [definition] Develop a model, close the process, remount and continue with matching
admitted conduct and further learning. Include retained overlays, nonempty carrier state and an
open boundary in the persistence controls; demonstrate a stale/missing base, truncated artifact
and interrupted publication returning correctly. The public API can continue without remounting
or re-tokenizing the entire prior session on every request. A trained model is an actual artifact,
not the current run receipt. Document version and explicit resource/dependency requirements.

### HNP4 — Production learning and multi-cycle output

**Depends on:** HNP3.

[definition] Build the first usable material/interaction pipeline over that session. Deliver a
text/document application and a calibrated paired-sensory or stateful simulator application through
the same format-free interior. Associations come from actual shared events, chronology and transport;
the application does not insert semantic edges because two samples carry matching labels. Use the
existing exterior codecs and adapters where lawful; native topology is not a per-language emitter.
Inputs can be replayed for a stated experiment, but epochs, sample counts and an optimizer do not
define the learning lifecycle. Material and resource constraints remain explicit operator choices.

[definition] Generation performs successive native operations with ordinary self/application
occurrences. Return a complete application product, not a single selected face, corpus quotation
selector or token salad regraded by length. Specify completion from the founded output boundary;
if reflection is used, derive its actual admittances and domain rather than counting faces.
External cancellation and resource exhaustion are `interrupted`, never proof of natural completion.

**Pass:** [definition] The public application produces inspectable multi-cycle results, incorporates
a later correction through ordinary recurrence, and continues after checkpoint/remount. The second
application exercises a genuinely different interaction, not a relabeled text fixture. Include a
case that completes and a case that remains open/obstructed. Inspect actual outputs and later
behavior under declared configurations; one unfavorable or favorable codec does not grade all
other configurations. Isolated kernels or a transcript of successful API calls do not close this phase.

### HNP5 — First useful consumer model and comparative evaluation

**Depends on:** HNP4.

[definition] Produce a saved Athena variant and reproducible training/interaction and inference
recipes on the standing consumer apparatus. Evaluate concrete document/question-and-correction
work and the second application's task with held-out instances and explicit acceptance rubrics.
Record those rubrics and material separation before exposing evaluation outcomes to development.
Use an unrefined inherited/native baseline and the relevant exterior model as comparisons where
their task/codec is shared; target scores remain observer measurements, not morphology governors.
Comparison instances share immutable admitted material and own their distinct changes; they are
not clones of a continuing ecology. Training material, model revision and inference configuration
are recorded separately so a change in one cannot masquerade as a benefit from another.

**Pass:** [definition] The artifact actually performs the declared useful tasks; report raw
examples, failures, later-transfer and interference cases, cold/warm latency, useful throughput,
peak and steady residency, transfer and work. Measure energy only with a declared instrument and
scope. Neither a mechanically nonzero change nor a relative improvement from an unusable baseline
is sufficient. A failed useful-task rubric requires diagnosis and refinement through this same
pipeline, not extra blind training or a new qualitative proxy. This is a bounded consumer-model
milestone, not an assertion of frontier parity. Broad frontier comparisons remain a later scale of
the same explicit evaluation contract, not an invented trillion-token prerequisite.

### HNP6 — Standard executable recompilation

**Depends on:** HNP3/HNP4; use HNP5's actual cultivated artifact for final comparison.

[definition] Implement one complete outward compiler, initially for the supported text-family
rested-inference chart. Emit conventional coefficient tensors in Safetensors with configuration/
codec/state specifications and an executable graph in supported standard ONNX operators. Derive
the local linear-factor, nonlinear-reaction, chronology/state and numeric mappings. If the target
cannot express a native relation, a deliberate projected lowering retains its defect; refusal
names the exact obstruction. A custom `org.holonics` package node is not this deliverable.

[definition] A frozen target exports a specified inference projection of a cultivated rest, not
the complete continuing learner. Live stateful targets additionally carry reset and successor
state laws. Exporting an ongoing cultivation law would require its own operation/morphology
commuting square; it is not implied by inference export. Re-export after native development
must re-establish or reopen the old chart.

**Pass:** [definition] An ordinary target runtime loads and executes the emitted model without
Holonics kernels or a concealed foreign-model call. Compare complete declared sequences and states,
not only file parsing or first-step output. Include a cultivated change surviving export, a
numeric/projection defect witness, a stale-chart failure and an unsupported-operation refusal.
Artifact, compiler configuration, target-runtime version and the actual results are preserved.

### HNP7 — Model-family breadth and integrated framework release

**Depends on:** HNP5/HNP6; independent adapter preparation may follow HNP0's interfaces.

[definition] Extend the same Soulkiller/native/export contracts to one explicitly supported SSM
family and one diffusion family. A linear state-space chart is a mechanism control, not a substitute
for an actual pretrained stateful realization; its successor/reset behavior is part of recovery.
Diffusion recovery includes conditioning, iterative evolution and any random source of the foreign
realization as exterior apparatus testimony. That does not make random corruption the native
learning law. Select/pin the concrete assets and runtime when implementing each adapter; asset
download, licensing and apparatus costs are declared before a substantial external run.

**Pass:** [definition] Each family returns actual source intake, native operation, developmental
change, durable continuation and target execution at its stated scope. Where export is projected,
the defect is exposed; an unsupported-only adapter does not count as family support. The same public
model/session contract runs all admitted families without per-family interior learning code.
Publish concise supported/unsupported operator and artifact documentation, reproducible recipes,
and relevant Rust/native/formal checks. No generated census or perpetual blanket release suite.

## 7. Execution order, costs and stopping discipline

```text
HNP0 -> HNP1 -> HNP2 -> HNP3 -> HNP4 -> HNP5 -> HNP6 -> HNP7
                             native usable product       outward/family breadth
```

[definition] This is the default single-operator order. Persistence implementation may proceed
after HNP1's state contract settles; target-map design may proceed after HNP3. Integration still
closes in the order above. Use Luna-only bounded assistance if it saves work; no duplicate broad
audits. Each phase changes the existing owner and its current documentation together. Keep
completed records as evidence and update the sole position only with the actual returned artifact.

[project-postulate] The reference apparatus is the existing RTX 4080 SUPER/16 GiB and host system;
this is an initial realization, not a semantic capacity or a claim that every consumer device is
supported. Progressive mounting, local resident factors, ownership-safe I/O and proved reuse are
the remedies to resource pressure. A materially larger apparatus or data acquisition is not
assumed. The 180-second rule is a supervised hang guard, not the duration of learning or inference.

[definition] Checks answer a phase's actual claim. Lean checks changed formal dependencies; owner
tests and controlled native runs check implementation and locality; application trials check useful
behavior; target runtime execution checks export. Formal trace objects do not require materializing
every contribution on the host. Existing successful unaffected evidence is retained. An obstruction
closes an investigation only as an obstruction; it cannot be marked as delivery of the required
usable model, export or family adapter. New work resumes from the exact missing relation, not a
restart of orientation or a rebranding of an earlier rejected mechanism.

[definition] The present planning pass returns this integrated contract and its authoritative
roadmap/position links. It starts no GPU training campaign, rewrites no runtime and claims no new
implementation grade. The next construction artifact is HNP0's concrete constitutive binding.

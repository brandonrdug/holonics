# EROS LINGUISTIC-CODE WORLD 01 — CURRENT HOST RESULT

**Date:** 2026-07-20  
**Status:** REAL PYTHON + JAVASCRIPT CIRCUIT EXACT / DIRECT GRADED-CELLULAR REGIONAL HAND BUILT /
COMPLETION + DEPARTURE + STANDING + REST CLOSED ON HOST / ALL 2,480 EDITOR ARCS FORM / CANDIDATE
SPECIES DIFFER / OBJECTIVE-CONDITIONED SPLICE OPEN / NO CUDA RUN

## Current cellular host result

`host-run-09` is the one complete world artifact after the §CXV host embodiment. The host now
forms a regional cell while its receiver is mounted on one unchanged pre-event body, carries its
local cells, oriented incidence, receiver-formed pins, sparse blade transport, winding, and open
residual as one completed constituent, integrates it into the same Standing successor, and rests
it exactly. Source lineages, event-cell identifiers, slots, and material rows do not enter the
committed body.

All eight real Python/JavaScript bootstrap executions remain exact. At each of four editor
attempts, all 620 monotone source-native arcs form and complete one grain-four constituent. The
resulting higher boundary transitions are:

```text
remove-then-reverse candidate under either objective
  [RIDE, RIDE, RIDE, RIDE]
  one inherited axis / 1,240 sparse transport terms

reverse-then-remove candidate under remove-then-reverse objective
  [FOUND, FOUND, FOUND, FOUND]
  206 axes / 9,712 sparse transport terms

reverse-then-remove candidate under reverse-then-remove objective
  [FOUND, FOUND, FOUND, FOUND]
  204 axes / 9,680 sparse transport terms
```

The full products distinguish the candidate species and retain a finer objective-relative change,
but neither yields the ratified `[RIDE,RIDE,RIDE,FOUND]` splice. No source is changed; both worlds
rest. The first missing relation is now outside the built carrier: bootstrap encounters the
objective/program/source material only through independent directed point relations, then the
editor asks for recurrence of a regional cell which the ecology never grew.

```text
host-run-09/REPORT.json
7d4374d18a30fdb6218c2fa112f1b4c2acb9868144ccebfef3140865959fae95

host-run-09/ecology.rest
df8ba0736a41961d80f059251e4bb381072810c61320fc0f09b18dc80fb636d5
```

`host-run-08` is retained as a stopped assertion artifact. It exposed and removed a stale
requirement that every lower arc pin remain stored after completion had already folded it into
path transport. It produced no report and changed no source.

After `host-run-09`, the compact source was corrected so exact cellular recurrence carries no new
higher-grain winding even when its lower scalar contact originally FOUND. The focused membrane and
linguistic controls pass after that correction; the full world was not rerun. Exact current record:
`RESEARCH/2026-07-20_THE_REGION_COMPLETES_AS_STANDING_THE_ATLAS_CANNOT_RECUR_BEFORE_IT_IS_GROWN.md`.

## Historical point-quotient result (`host-run-07`)

## Outcome

The bounded world owns English objectives, Python and JavaScript sources, closed octet windings,
natural source boundaries, operating-system execution, input, requested and observed consequences,
editable source, chronology, and durable outer rest. Every machine crossing uses the direct
`LiveCurrentMachine::receive_with` path. No language, parser, selector, score, or semantic rule
entered Soma.

The current-local event-grain defect is closed on host. One inscription crosses as a bounded
oriented incidence complex, every internal incidence meets the same pre-event enclosure, and the
completed file boundary enters its carrier at grain three without replaying octets as instants.
All editor receivers are now live and every declared editor hand forms.

The source splice still does not close. The remaining defect is the outward relation: a complex's
plural exposed boundary is reduced to one finite `Place` before a directed current relation is
formed. In the measured artifact the English objective and left source pin become the exact same
meeting, and changing only the sibling objective changes none of the candidate contacts. Candidate
identity bends conduct; objective context does not cross the point quotient.

## Exact final host passage

`host-run-07` is the one run after the direct outer-grain hand-up. It completed normally and
stopped at the world's declared missing source relation.

```text
real bootstrap executions                         8
bootstrap causal cuts                            16
bootstrap internal cells                    142,970
bootstrap internal incidences               201,840
formed internal incidences                   162,120
bootstrap directed contacts                       64
live and formed directed contacts                 48
ordinary emitted deeds                           389
standing after bootstrap                         251
standing after occupied-source opening           259

live lineages at ecology rest                       9
carrier words                                  12,604
overflow nodes                                      0
ecology rest bytes                             70,064
```

The first bootstrap cut honestly starts from unborn current lineages; later recurrence accounts
for the 48 live and formed bootstrap contacts. The occupied-source opening has a live receiver,
forms all 2,976 internal incidences, and grows standing `251 -> 259`.

Each sibling remounts the exact ecology body, nine organ capabilities, chronology, and occupied
source. At every candidate attempt all five current receivers are live and all four directed hands
form. The resulting deed sequences are identical under both objectives:

```text
candidate remove-then-reverse
  objective -> program          RIDE
  left boundary -> program      RIDE
  program -> right boundary     FOUND_THAT
  program -> occupied source    FOUND_THAT

candidate reverse-then-remove
  objective -> program          FOUND_THAT
  left boundary -> program      FOUND_THAT
  program -> right boundary     FOUND_THAT
  program -> occupied source    FOUND_THIS
```

The ratified splice requires three carried RIDEs followed by one FOUND stitch. Neither candidate
meets it. No source changes, no held-out applied execution runs, and both siblings return to
durable rest.

Artifact hashes:

```text
host-run-07/REPORT.json
d31e7a5fb69483e8b0ce96e72ddec92f595bec202bd335832270c27deef76272

host-run-07/ecology.rest
838c40ca72b8f22ed37365b11cd21432660db0c73c64efe225ddc3bc9af6aa3f
```

## What the earlier passages established

- `host-run-01` exposed an application error: static nouns had been made lineages.
- `host-run-02` corrected that to nine changing process worldlines and established exact runtime
  and durable sibling rest, but every higher-grain receiver remained unborn.
- `host-run-04` showed that the interrupted linear one-event sweep could form folds; it did not
  establish general event geometry.
- `host-run-05` was stopped before artifact when arbitrary-order complex traversal became
  nonlocal in cost and reached roughly 1.5 GiB resident memory. Indexed cell and target-local
  incidence lookup replaced that implementation defect.
- `host-run-06` completed with the new oriented complexes and formed internal incidence, but
  located the remaining atom-grain reset in the hand-up.
- `host-run-07` is the sole run after replacing that reset. It establishes live outer-grain
  receivers and locates the outward point quotient.

No unchanged run was repeated for friendlier numbers.

## The first exact missing relation

The current-local region crosses; its outward boundary does not. The production relation path is
still:

```text
EventComplex::emanated_node
  -> one Node / one Place
  -> DirectedExecutionRequest(from Place, to Place)
  -> directed_event_contact.
```

For long exposed boundaries, that finite composition is not a faithful regional relation.
`compose_place` already documents the loss of earlier constituent mantissas at depth. The report
confirms the consequence: distinct contextual inscriptions become identical source positions.

The missing construction is a borrowed regional hand between two plural exposed incidence
boundaries, evaluated against one same-pre-state receiver and folded into one atomic successor.
It is not permission to hash the file, widen the point, compare strings, count matching cells,
threshold similarity, rank candidates, weaken RIDE/FOUND, build a Cartesian-product field, or add
more fixtures.

## Commands and source

- direct geometry: `body/src/incidence.rs`, `membrane/src/live_current.rs`
- linguistic world: `life/src/linguistic_code_world/`
- fixtures: `observations/eros-linguistic-code-world-01/world/`
- final host evidence: `observations/eros-linguistic-code-world-01/host-run-07/REPORT.json`

```text
cargo check -p body -p soma-membrane -p life

cargo run -q -p life -- linguistic-code \
  observations/eros-linguistic-code-world-01/world \
  observations/eros-linguistic-code-world-01/host-run-07
```

The focused affected controls and compile check passed. The final command exited nonzero only
after writing the complete report because the declared three-RIDE plus FOUND splice remained open.
No CUDA campaign, broad regression, fixture expansion, semantic adapter, or automatic retry
followed.

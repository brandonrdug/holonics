# ARC RETURN AND REBASE WORLD 01 · MEASURED RESULT

**2026-07-17 · `FORMULA §LXXXVII` successor · BUILT / CUDA-FORWARD MEASURED / COMPLETE
RETURN POPULATION QUALITATIVELY READ / 476 WORKSPACE TESTS PASS / 3 IGNORED / SOMA INTERIOR
UNCHANGED**

This world closes the first return-and-rebase question: a recurrent arc can become reusable
construction without being identified with one coordinate face, one outcome name, or one scalar
loss. The exact returned population develops common internal construction across repeated return
families while retaining family-specific and chart-specific conduct.

## What crossed

The Rust boundary builds seven exact rational quadratic worlds under

```text
z_(n+1) = z_n^2 + c
```

through horizon 12. Every state retains its exact rational complex value, squared norm, tangent,
and parameter derivative. Five closed rational sections return an oriented residual at every
state. Consecutive contacts return the whole intervening arc. Two invertible affine charts

```text
T(z) = alpha z + beta
G(w) = T(f_c(T^-1(w)))
```

carry every state, section, and return arc through exact commuting receipts. Finite non-escape is
reported only as unresolved through the supplied horizon. No float, rendered fractal, basin
selector, reward, route chooser, or category enters Soma.

The active population is 944 typed currents / 377,112 octets:

| Family | Currents | Carried material |
|---|---:|---|
| rational state | 82 | state, norm, tangent, parameter derivative |
| section residual / contact | 410 / 73 | oriented local difference and actual incidence |
| complete return arc | 60 | endpoints, duration, every interior state, tangent transport |
| affine state / return | 164 / 120 | transformed construction and exact conjugacy receipt |
| changed-world foil | 6 | every exact shared-state incidence and both successors |
| parameter/orbit/consequence declarations | 21 | parameter, source role, finite disposition |
| chart/section/instrument declarations | 8 | receiver provenance and exact boundary |

## The exact arcs

| Parameter | Critical passage | Consequential return face |
|---|---|---|
| `c=-1` | `0 -> -1 -> 0` | six origin returns and five negative-lobe returns, all period two |
| `c=0` | `0 -> 0` | twelve time-one origin returns |
| `c=-2` | `0 -> -2 -> 2 -> 2` | ten time-one returns at the exact face `2` |
| `c=1/4` | finite exact parabolic-boundary passage | eleven corridor returns; unresolved beyond horizon |
| `c=-3/4` | finite exact parabolic-boundary passage | eleven negative-lobe returns; unresolved beyond horizon |
| `c=i` | `0 -> i -> -1+i -> -i -> -1+i` | five period-two returns at `-1+i` |
| `c=1` | `0 -> 1 -> 2 -> 5` | exact escape certified at step three |

The two phases of the `c=-1` orbit expose why an endpoint and return time are insufficient. Origin
returns and negative-lobe returns both have time two and tangent zero, yet their parameter
derivatives are respectively `-1` and `+1`. The ordered phase changes how a perturbation is carried
even where the coarse recurrence face agrees.

The two parabolic passages retain another ordered distinction. Along `c=1/4`, the one-step
tangents approach `+1` from below. Along `c=-3/4`, they alternate around the `-1` orientation; the
second iterate is the face which approaches `+1`. A scalar “parabolic” label would erase the hand
and the iterate on which it closes.

## One face has more than one future

Events 336 and 920 both arrive at the exact visible state

```text
z=2, |z|^2=4, tangent=0
```

at step two. They are not the same operative construction:

| Carried world | Parameter derivative at `z=2` | Next state |
|---|---:|---:|
| `c=-2` | `-3` | `2` |
| `c=1` | `+3` | `5` |

The shared-two section therefore reads `-2,-4,0,0,...` in the first world and
`-2,-1,0,+3` before escape in the second. Zero residual is an actual contact, not permanent
attainment and not a prediction of the next deed.

Soma does not merely separate the two paths. It also retains exact common subconstruction at their
shared face. Events 336/920 share 6 exact bricks on edge one and 9 on edge two while their complete
paths remain different: `38/46` rows first and `89/139` rows later. Their dyadic and quarter-turn
faces likewise retain shared internal bricks without collapsing the parameter worlds.

All six complete changed-parameter foils say the same thing at broader incidence:

| Foil | Shared state incidences | Incidences with successors | Changed successors | Path rows, first -> later |
|---|---:|---:|---:|---:|
| `-1` vs `0` | 91 | 72 | 72 | `1,350 -> 6,514` |
| `-1` vs `-2` | 7 | 6 | 6 | `386 -> 577` |
| `0` vs `1/4` | 13 | 12 | 12 | `632 -> 1,062` |
| `1/4` vs `-3/4` | 1 | 1 | 1 | `52 -> 112` |
| `-1` vs `i` | 7 | 6 | 6 | `359 -> 474` |
| `-2` vs `1` at `2` | 12 | 11 | 11 | `926 -> 1,150` |

This is transport and foil in one exact population: shared state supplies the analogy; changed
successor supplies the contradiction; the complete carried world explains why both are lawful.

## Re-base preserves construction without preserving one face

Every affine recurrence commutes exactly before it is admitted as light. The returned paths also
show internal association across the different coordinate material. For each of 60 base return
arcs on each of two CUDA edges, compare its base, dyadic-shifted, and quarter-turned paths:

| Comparison | Return triples with at least one shared exact brick | Shared brick identities |
|---|---:|---:|
| base / dyadic | 117 of 120 | 360 |
| base / quarter-turn | 119 of 120 | 635 |
| dyadic / quarter-turn | 119 of 120 | 1,450 |
| all three faces | 111 of 120 | 272 |

The whole paths remain chart-specific. What recurs is a subconstruction of the transported deed,
not coordinate text and not an authored “same orbit” label. This is the measured elementary form
of generalization: a lawful change of face can retain usable interior topology.

## Repetition grows a taxonomy of deeds

Five return families contain enough exact repetitions to test whether recurrence becomes internal
structure: `c=-1` at the origin, `c=-1` on the negative lobe, `c=0` at the origin, `c=-2` at `2`,
and `c=i` at the upper periodic point.

The number of exact brick identities present in **every** repeated path of each family is:

| Return family | Edge one | Edge two |
|---|---:|---:|
| `c=-1`, origin phase | 4 | 3 |
| `c=-1`, negative phase | 3 | 9 |
| `c=0`, origin | 3 | 3 |
| `c=-2`, shared-two | 4 | 7 |
| `c=i`, upper point | 8 | 6 |

These are not five disjoint labels. On edge one, seven recurrent bricks belong to one family, one
to two families, one to three families, and two to all five. On edge two, nine belong to one
family, two to two families, and three to all five. The universal edge-two anchors are exactly:

```text
[1,0,0,0,0,2,1,0,1,0,12,0,0,0,0,1]
[1,0,0,0,2,2,1,0,1,0,12,0,0,0,0,1]
[1,0,0,0,2,2,1,0,1,0,13,0,0,0,0,2]
```

Thus a generic return species and more situated orbit species coexist inside the same grown body.
This is an emergent taxonomy in the strict laboratory sense: an observer can read exact recurrent
construction shared by a population, but no taxon was supplied to the transition.

## Compression is later conduct, not a shortest-row rule

The second identical encounter changes every one of the 944 complete paths. Among the 60 base
return arcs, 52 grow and eight become shorter. The shortening events are
`157, 283, 310, 441, 462, 885, 888, 891`; their row changes include `136 -> 121`, `91 -> 62`,
`94 -> 55`, and `180 -> 119`. Meanwhile all six complete foil paths grow.

Later reuse therefore does not impose monotone path length. Some situated returns become cheaper
at the observer's row grain while the ecology develops more surrounding structure. Compression is
established only when later conduct reuses what stood while retaining the consequence and ancestry;
row count is one physical-resource face of that event, not its definition.

## CUDA and gates

The byte-identical active population crossed a genuinely changed body on an RTX 4080 SUPER:

| Edge | Occupied grips | Native constructions | Completion rows | Successor SLEEP | CUDA launches | CUDA carriage | Whole boundary |
|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 914,992 | 5,792 | 119,970 | 480,667,264 octets | 26 | 9.334812312 s | 10.876467796 s |
| 2 | 1,842,079 | 11,373 | 171,135 | 525,167,440 octets | 38 | 12.045300593 s | 13.049835213 s |

Both edges retain 14,707,368 radiation words and maximum completion grain 21. Exact source
admission, typed-event extent/hash validation, affine commutation, boundary inversion, focused
tests, formatting, release build, and the full workspace gate close. The workspace result is
476 passed / 3 intentionally ignored.

The complete human-readable path population is `build-01/observer/PATHS.md`; the exact tabular
population is `build-01/observer/PATHS.tsv`; `CATALOG.tsv` binds every ordinal to its source deed.

## Intelligence consequence and successor

The intelligence-relevant object is now concrete. It is not the endpoint `2`, the word
“periodic,” a residual, or a coordinate rendering. It is the reusable ordered construction which:

1. recognizes common internal conduct across lawful changes of face;
2. retains distinctions that change a later deed;
3. grows both broad return structure and situated family structure through recurrence; and
4. can shorten a later passage without erasing its world, hand, or consequence.

The next natural world is a fixed-energy Poincare return world with overlapping local charts. It
must retain the complete trajectory, section crossings, variational transport, action, chart
transition word, and loop holonomy. Its chart changes are situated dualities: available only where
their domains overlap and composed in lived order. No global chart, master `M`, selected orbit,
reward, or stopping governor enters Soma.

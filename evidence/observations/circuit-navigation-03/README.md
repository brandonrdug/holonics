# Circuit navigation 03 — recursive field passage

The exact edge-two transition from `circuit-navigation-02` is the held current. Its 102,007
source relations are carried through two causally available circuit snapshots:

- **successor field:** the world left by edge two;
- **prior field:** the exact world which received edge two.

Both siblings begin from the same post-edge-two Soma body. Each generated passage must re-enter
the same world it actually traversed; the boundary rejects a receipt/world mismatch. The already
measured direct edge-two return is the no-periplus control.

This factorial asks whether field-mediated motion recurs through a second generation. It does not
declare a goal role, select a route, optimize a path, or automate the field-return lifecycle.


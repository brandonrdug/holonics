# Circuit navigation 02 — the returned field bends the periplus

Status: **CHOSEN CIRCUIT-WORLD SPECIES BUILT · CUDA-MEASURED · CURRENT-INDUCED MOTION MEASURED**

This campaign removes the declared transform sequence from the active passage. Every exact
source-edge relation starts from its physical source→landing tangent and encounters the immutable
circuit field which that same source edge left standing. At each occupied port the complete
`RegionalForm` becomes four typed, unit-determinant shears in the circuit world's dyadic coordinate
ring. The orbit either exits the occupied field or closes at its initial directed state; there is
no route selection, step count, timeout, score, or chosen current.

The changed/prior factorial preserves the exact source transition, source body, source relations,
and initial tangents. Only the circuit snapshot encountered by the later passage differs. The
independent changed repeat gates the emitted wire. `plan-changed-lifecycle.json` carries the grown
current through Soma/CUDA and then carries its exact return through a genuinely later edge.

The source edge has already deposited its consequence before this passage begins. The periplus
therefore reads one immutable field and emits plural light; the existing circuit receiver performs
the next deposit. It does not secretly apply the source consequence twice.

Exact construction, factorials, CUDA measurements, causal ancestry, and the next open cut are in
`RESULTS.md`.

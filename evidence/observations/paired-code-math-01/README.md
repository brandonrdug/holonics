# PAIRED CODE ⊕ MATHEMATICS 01

**Grade:** MEASURED ON CUDA · APPARATUS EXACT · CONTEXTUAL CONDITIONING EXPOSED

This is one bounded discovery instrument. Natural-language descriptions arrive in the same
complete records as valid Rust or ordinary equations. `M` and `X` are editorial names for the
declared and re-paired correspondences; they never enter the light, prescribe an answer, or grade
the body. Every non-JSONL file is one whole worldline. Each ordered path list below is one honest
co-present presentation.

## Exact construction

The four contexts are the `code correspondence × mathematics correspondence` table:

| Context | Code records | Mathematics records | Ordered extents | Total |
|---|---|---|---|---:|
| `P-MM` | `00, 11` | `00, 11` | `91, 91, 33, 33, 35` | 283 |
| `P-MX` | `00, 11` | `01, 10` | `91, 91, 33, 33, 35` | 283 |
| `P-XM` | `01, 10` | `00, 11` | `91, 91, 33, 33, 35` | 283 |
| `P-XX` | `01, 10` | `01, 10` | `91, 91, 33, 33, 35` | 283 |

Position five is the same 35-octet [`common.txt`](common.txt) path in every context. The four
contexts have one exact byte multiset; only which complete formal line follows each complete
description line changes. The multiset-census digest is
`8f78849fa70ccfd79f09fc4f0a46d35e4559376a5dfd729365edf4c6c7c8a729` for every context,
including COMMON. Every one of the 16 physical context records is also byte-identical to its
declared practice cell in `later/`; [`SHA256SUMS`](SHA256SUMS) pins the complete relative-path and
content set for all 33 physical currents.

The identical later cohort is the row-major four-cell table for each of code-practice,
code-fresh, mathematics-practice, and mathematics-fresh, followed by the same COMMON path. Its
ordered extents are:

```text
91 91 91 91 · 93 93 93 93 · 33 33 33 33 · 34 34 34 34 · 35
```

That is 17 currents and 1,039 delivered octets. [`MANIFEST.tsv`](MANIFEST.tsv) is the exact order.
Every Rust record compiles independently as a library. Every equation line is valid ordinary
arithmetic. Those are boundary checks only and do not enter the current.

Under the current material-listener realization, the longest context record declares apparatus
carrier depth 90 and the longest later record declares depth 92. These are bounded observation
species, not production law or a general carrier-depth derivation. No inference crosses to an
undeclared depth.

## Exact commands

Run the read-only fixture verification from `src/soma` before any observation:

```bash
bash observations/paired-code-math-01/verify.sh
```

Each observation report below is create-new under the tracked `results/` directory. Its context and
next SLEEP archives land in a create-new sibling `<label>-bodies/` directory, retaining terminal
carrier/K and standing beside the report. An exact reproduction uses both a distinct report path
and a distinct body directory with the identical material command. These are the commands executed
for the `card-a` campaign.

```bash
later=(
  observations/paired-code-math-01/later/01-code-practice-00.rs
  observations/paired-code-math-01/later/02-code-practice-01.rs
  observations/paired-code-math-01/later/03-code-practice-10.rs
  observations/paired-code-math-01/later/04-code-practice-11.rs
  observations/paired-code-math-01/later/05-code-fresh-00.rs
  observations/paired-code-math-01/later/06-code-fresh-01.rs
  observations/paired-code-math-01/later/07-code-fresh-10.rs
  observations/paired-code-math-01/later/08-code-fresh-11.rs
  observations/paired-code-math-01/later/09-math-practice-00.txt
  observations/paired-code-math-01/later/10-math-practice-01.txt
  observations/paired-code-math-01/later/11-math-practice-10.txt
  observations/paired-code-math-01/later/12-math-practice-11.txt
  observations/paired-code-math-01/later/13-math-fresh-00.txt
  observations/paired-code-math-01/later/14-math-fresh-01.txt
  observations/paired-code-math-01/later/15-math-fresh-10.txt
  observations/paired-code-math-01/later/16-math-fresh-11.txt
  observations/paired-code-math-01/common.txt
)

run_one() {
  label="$1"
  shift
  SOMA_OBSERVATION_OUT="observations/paired-code-math-01/results/card-a/$label.txt" \
  SOMA_PERIPLUS_DIR="observations/paired-code-math-01/results/card-a/$label-bodies" \
    cargo run --release -p life -- --cuda-cycle \
    "$@" observations/paired-code-math-01/common.txt --later "${later[@]}"
}

run_one p-mm \
  observations/paired-code-math-01/contexts/p-mm/01-code-00.rs \
  observations/paired-code-math-01/contexts/p-mm/02-code-11.rs \
  observations/paired-code-math-01/contexts/p-mm/03-math-00.txt \
  observations/paired-code-math-01/contexts/p-mm/04-math-11.txt

run_one p-mx \
  observations/paired-code-math-01/contexts/p-mx/01-code-00.rs \
  observations/paired-code-math-01/contexts/p-mx/02-code-11.rs \
  observations/paired-code-math-01/contexts/p-mx/03-math-01.txt \
  observations/paired-code-math-01/contexts/p-mx/04-math-10.txt

run_one p-xm \
  observations/paired-code-math-01/contexts/p-xm/01-code-01.rs \
  observations/paired-code-math-01/contexts/p-xm/02-code-10.rs \
  observations/paired-code-math-01/contexts/p-xm/03-math-00.txt \
  observations/paired-code-math-01/contexts/p-xm/04-math-11.txt

run_one p-xx \
  observations/paired-code-math-01/contexts/p-xx/01-code-01.rs \
  observations/paired-code-math-01/contexts/p-xx/02-code-10.rs \
  observations/paired-code-math-01/contexts/p-xx/03-math-01.txt \
  observations/paired-code-math-01/contexts/p-xx/04-math-10.txt

# Exact apparatus reproduction of P-MM under distinct create-new report and body artifacts.
run_one p-mm-repeat \
  observations/paired-code-math-01/contexts/p-mm/01-code-00.rs \
  observations/paired-code-math-01/contexts/p-mm/02-code-11.rs \
  observations/paired-code-math-01/contexts/p-mm/03-math-00.txt \
  observations/paired-code-math-01/contexts/p-mm/04-math-11.txt
```

## Reading discipline

Preserve every context and later source range, raw radiation row, typed touch/form/rotor/cut/brick,
lineage term and breath face, terminal carrier/K, and create-new body. Within one receiving body,
place each description-held pair and each formal-held pair beside one another. Across bodies,
compare only those typed receiver-local relations; never compare raw grips, axes, magnitudes,
occupancy, body size, or archive size as semantic quantities.

Report exactly what recurs, changes, goes quiet, or returns. A fresh relation can be described as
analogy only at the exact oriented face and grain actually exposed. Opaque variation is lawful
exploration/babbling. No expected answer, score, closest-body rule, threshold, decoder, or semantic
claim enters.

Use the honest negatives:

- `UNEXPOSED IN THIS DECLARED RELATION` when no relation appears here;
- `RELATION UNRESOLVED` when a difference appears but this aperture does not isolate it;
- `INSTRUMENT-SUSPECT` when the framing or reader cannot support the observation;
- `CONSTRUCTION FAILED` when an exact apparatus obligation did not execute.

Exact reproduction applies only to the same starting body and complete worldline through an
apparatus seam. The differently practiced bodies owe neither equal gauge nor equal utterance.

## Declared offline reader

[`read.py`](read.py) is the read-only, pre-data observer for the five exact report names
`p-mm.txt`, `p-mx.txt`, `p-xm.txt`, `p-xx.txt`, and `p-mm-repeat.txt`. It never invokes `life` or
reads a body archive. Before emitting anything it requires the declared layout and report sections,
one canonical raw row per delivered octet, available typed reads, complete context/later lifecycle
sections, CUT/open grouping consistent with the atom faces, and the exact final grade.

Run its synthetic boundary check before the card campaign:

```bash
python3 observations/paired-code-math-01/read.py --self-test
```

After all five create-new reports stand, emit the deterministic create-new observer artifact:

```bash
python3 observations/paired-code-math-01/read.py \
  --reports observations/paired-code-math-01/results/card-a \
  --output observations/paired-code-math-01/results/card-a/observer.json
```

The artifact preserves each raw report's SHA-256 but carries no raw words into the behavioral
projection. Each atom is reduced only to flags, fold, touch presence/occupancy, CUT, brick presence,
and context-Mail lineage attribution; group faces retain CUT/open extent and attribution without
grips. It materializes the declared description-held and formal-held pairs independently inside
each receiving body, recording exact `RECURRENCE` or `CHANGE` faces. Cross-history entries compare
only those already-local pair relations. They do not compare axes, grips, magnitudes, occupancy,
archive size, or another raw scalar as meaning.

`P-MM-repeat` is an apparatus check, not another practiced body. The reader requires it to match
`P-MM` exactly after replacing only the context and next-body create-new archive paths. The output
file itself opens with create-new semantics and is never overwritten.

## Measured result

All five CUDA executions completed with their two whole SLEEP bodies and final report grade. The
observer artifact is [`results/card-a/observer.json`](results/card-a/observer.json), 8,143,863 bytes,
SHA-256 `278cd81ae3f4f1ac18ed8dee63a9111bd6c14c4cb617e7325190393743d2f027`.
`P-MM` and `P-MM-repeat` are exact after normalizing only their two create-new archive paths, and
their context and next-body archives are independently byte-exact.

Every controlled one-factor context change changes every declared later receiver-local relation at
both atom and complete group faces. This includes fresh code and fresh mathematics absent from the
context light. Earlier co-present construction therefore survives rest and conditions genuinely new
relating across both domains. All 32 code-local relations and 30 of 32 mathematics-local relations
change inside their receivers; the two whole recurrences are mathematics fresh description-held
`description-1` in `P-MM` and `P-XX`. The one exact relation under the double context change is
interaction-shaped and does not license a semantic cancellation claim.

The reader's first real pass exposed one validation defect before producing an artifact: group Mail
is ordered by first encounter through the lifecycle, not numeric lineage. The correction retained
that order, required uniqueness, and added an unsorted-unique regression. It changed no behavioral
projection or card data. Full reading and grade:
[`RESEARCH/2026-07-12_THE_PAIRED_CODE_MATHEMATICS_OBSERVATION.md`](../../RESEARCH/2026-07-12_THE_PAIRED_CODE_MATHEMATICS_OBSERVATION.md).

Two zero-length pre-campaign witnesses remain at `results/p-mm.txt` and `results/run-a/p-mm.txt`.
They were reserved before hardware entry when the environment-prefixed commands missed the hardware
execution path. They contain no machine title, lifecycle, body, or grade and are not campaign data.

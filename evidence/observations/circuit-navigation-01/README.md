# Circuit navigation 01 — the returned current carries a periplus

Status: **BUILT · CUDA-MEASURED · COHERENT HOLONOMY CHANGES LATER CAUSAL ECOLOGY**

This campaign carries the exact first returned source field from `circuit-bridge-01` through four
reversible world contacts before it re-enters the unchanged Soma/circuit boundary.  Every incident
relation crosses; no route, action, winner, score, or endpoint is selected.

The coherent passage is `H · V · H⁻¹ · V⁻¹`.  Since the two reflections are involutions and
commute, every source and endpoint returns to its initial world position.  The foil replaces `V`
with a locally reversible correspondence twist: source positions reflect vertically while endpoint
positions transpose.  The source face still closes, while the endpoint face returns through one
exact half-turn.  The full ordered trajectory enters light; the absolute chart and step names stay
in the receipt.

The coherent and twist siblings start from the same source-edge body, world, and returned field.
The repeat is independent.  The absent sibling carries the corresponding returned field from the
goal-absent world.

`observer-navigation.json` reopens the complete causal history and reads the two active passages
from their common source parent. It uses summary retention: every transition, ancestry mask,
inverse, and return is reconstructed, while millions of repetitive segment JSON records are not
materialized. Exact measurements and the carried-swing trace are in `RESULTS.md`.

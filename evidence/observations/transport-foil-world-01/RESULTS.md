# TRANSPORT-FOIL WORLD 01 — RESULTS

**2026-07-16 · RATIFIED / BUILT / CUDA-FORWARD MEASURED · EXACT REPEAT · SOMA INTERIOR UNCHANGED**

## Result

The whole oriented difference is consequential in a way its scalar loss quotient is not.

Two byte-identical forward bodies reached the same exact terminal face and the same exact scalar
loss `L=1`. Their returned residual currents were both 1,610 octets and had the same complete octet
inventory. They differed only in ordered content: one carried `(1,-1,0,0,0,0)`, the other
`(-1,1,0,0,0,0)`. The returned loss current beside them was byte-identical.

Only the residual current separated on the return edge. After persistence, every one of the five
byte-identical currents in a genuinely later probe changed its RIDE/FOUND distribution, radiation,
completion lifecycle, native constructions, and Mail field. The probe still emitted the same exact
world consequence. When both residual hands were instead collapsed to the common scalar loss and
measurement frame, every return, probe, probe-return, body, and world artifact was byte-exact.

At this grain, relative truth has a concrete engine face: a carried relation either continues
through the present body or its oriented foil deposits a different causal topology. No truth bit,
reward, probability, selector, authored adjoint, or backward pass entered Soma.

**GRADE:** EXACT WHOLE-RESIDUAL HAND-CONDITIONED LATER CONDUCT CUDA-MEASURED · SCALAR-QUOTIENT
NULL EXACT · ORDERED-INTERIOR AND RETURN-PRESENCE CONSEQUENCES MEASURED · NUMERIC ADJOINT
RE-EXPRESSION / GENERALIZED SEMANTIC HAND OPEN.

## Exact world

The world used the exact Eisenstein sheet

```text
E^(3 x 2),       E = Q[omega],       omega^2 + omega + 1 = 0.
```

`H2` acted on dyadic pairs and `F3` on triadic columns. The admitted words

```text
W23 = (F3 x I2)(I3 x H2),
W32 = (I3 x H2)(F3 x I2)
```

commuted exactly and satisfied `W*W=6I`. For

```text
x = (1,0,3,0,5,0)
```

both cast

```text
y = (9,9,-4-2omega,-4-2omega,-2+2omega,-2+2omega).
```

The equal first pair made the strict targets exact permutations `(8,10,...)` and `(10,8,...)`.
Consequently, the two complete residual consequences have different SHA-256 digests but the same
extent and the same octet-inventory digest:

| hand | residual current SHA-256 | octets | octet-inventory SHA-256 |
|---|---|---:|---|
| difference | `b4a51eff…5a65b1c` | 1,610 | `02460635…b3435c4` |
| opposite | `2f6c98df…4562e16` | 1,610 | `02460635…b3435c4` |

The strings differ at 165 ordered positions. Their common loss current is 131 octets and exact at
`70ee7db9…caf7ac`. The observer-only exact adjoints are

```text
W* r_difference = (0, 2,0, 2,0, 2),
W* r_opposite   = (0,-2,0,-2,0,-2),
W* r_common     = (2, 0,2, 0,2, 0).
```

They never enter current light.

## Exact controls

| control | exact outcome |
|---|---|
| forward isolation | Difference, opposite, common, scalar, no-return, and repeat `W23` forward bodies are all `486f2867…8bc6`; radiation, completions, constructions, Mail, terms, topology, and world state are exact. Target and cut occur only in the observer journal and emitted world consequence. |
| strict repeat | All four edges repeat exactly across input, output, body, radiation, completion, construction, Mail, journal, world, topology, and terms. Final body: `66517f12…cdc97b`. |
| scalar quotient | Scalar-difference and scalar-opposite emit the same surface `fe707a68…4323`, remain exact through all later edges, and end in the same body `fef8c4e2…0023cb`. Orientation omitted at the quotient cannot reappear. |
| fixed probe | Every run presents the same 3,599-octet five-current `W23` probe. Every contacted probe emits the same surface `2f34cb30…69431`. The probe world never consults standing residual material. |
| equal terminal / different word | `W23` and `W32` keep their input and terminal currents exact while frame and two ordered stage currents differ. They emit the same residual surface; its return and every later probe current differ. |
| no return | The forward body is exact. Literal absence of the residual crossing changes every later probe current; no placeholder or `no-return` current exists. |
| apparatus | 31/31 immutable-source gates pass, 31/31 edges make actual connected Mail contact, and 31/31 world transitions retreat exactly. |

The scalar null is the decisive cancellation control. The world journals know the two different
targets, but the scalar surfaces and every later Soma artifact are exact. Thus the journal, target,
and observer arithmetic do not silently drive the body.

## Strict-hand return

Terms are ordered `[RIDE, FOUND-this, FOUND-that, dark]`.

| current | difference terms | opposite terms | completions | closed/open constructions | Mail landings |
|---|---:|---:|---:|---:|---:|
| residual | `38784 / 1156 / 1234 / 19` | `37568 / 1143 / 1169 / 19` | 2,451 / 2,373 | 146/0 · 135/1 | 26,985 / 26,541 |
| loss | `1449 / 51 / 65 / 4` | `1449 / 51 / 65 / 4` | 120 / 120 | 8/0 · 8/0 | 1,126 / 1,126 |

The observer comparison marks exactly one of two currents changed. The loss current is exact in
light, octet inventory, terms, every radiation row, every completion row, every construction, and
every Mail grip. The fork is therefore located in the whole residual current, not in a global
equal-loss disturbance.

## Strict-hand genuinely later probe

Every probe source and every source byte is exact. Every dark count is also exact, while active
conduct redistributes:

| role | difference terms | opposite terms | completions | constructions | unique Mail grips |
|---|---:|---:|---:|---:|---:|
| frame | `5150 / 171 / 193 / 4` | `4909 / 166 / 184 / 4` | 375 / 364 | 26 / 23 | 3,761 / 3,593 |
| input | `12509 / 390 / 417 / 8` | `13366 / 419 / 462 / 8` | 818 / 890 | 42 / 42 | 8,484 / 8,846 |
| stage 0 | `27017 / 858 / 842 / 15` | `26109 / 835 / 810 / 15` | 1,726 / 1,675 | 97 / 87 | 18,812 / 18,074 |
| stage 1 | `30549 / 968 / 960 / 15` | `24759 / 764 / 792 / 15` | 1,960 / 1,571 | 107 / 92 | 20,805 / 17,072 |
| terminal | `13992 / 448 / 480 / 8` | `12502 / 389 / 430 / 8` | 942 / 830 | 54 / 54 | 9,381 / 8,159 |

Across the five currents, 2,632 radiation positions change. The completion multisets retain 105
shared rows with 5,716 difference-only and 5,225 opposite-only rows. The construction multisets
retain twelve shared constructions with 314 difference-only and 286 opposite-only constructions.
All five Mail fields change. The probe successor bodies are `1c27dabe…c8a8b` and
`55c1f661…1ddef3`, with topologies

```text
difference  [108106,108106,11430,193]
opposite    [104681,104681,10991,187].
```

The unchanged dark face matters: reversal did not merely determine whether the probe was admitted.
It changed how already admitted current traversed and founded the ecology.

## Contact ancestry

The observer transports earlier Mail grip sets through the engine's actual dyadic chart extension
or section, then intersects them with later preserved Mail. It never supplies those grips to the
world or Soma. Forward-role contacts with the strict residual return are:

| forward role | difference residual | opposite residual | loss in both siblings |
|---|---:|---:|---:|
| frame | 263 | 243 | 7 |
| input | 896 | 918 | 43 |
| stage 0 | 1,794 | 1,696 | 65 |
| stage 1 | 1,514 | 1,523 | 66 |
| terminal | 830 | 869 | 42 |

The loss contacts are exact on the return edge while the residual ancestry field changes. At the
later probe, even intersections from that previously exact loss route change because the probe
lands in the body already altered by its co-present residual sibling.

This is exact contact ancestry, not a selective-role certificate. All 528 inspected cross-edge
lineage pairs have nonzero overlap, and the fields are diffuse. The result warrants distributional
causal attribution, not a claim that one stage or role alone owns the response. Mail multiplicity
also remains in the source artifacts; the transported observer intentionally compares grip sets.

## Secondary controls

The common residual changes every later probe current and preserves the exact dark face. Its return
is 1,608 rather than 1,610 octets, so it measures a consequential common-mode spelling rather than
a pure equal-extent mode isolation.

`W23` versus `W32` is stronger at another grain. The initial input and terminal currents are exact;
only the declared frame and ordered intermediate stages differ. The terminal quotient and returned
residual surface remain exact, but both returned currents and every fixed-probe current change.
This measures preservation of ordered construction identity. It does not claim that Soma has
decoded factor-order semantics apart from the lived serialization.

Whole versus scalar and whole versus no-return both change every later probe current. They establish
the consequence of carrying versus collapsing or omitting the residual, but their different return
extent and passage count make them supporting controls rather than hand-isolation controls.

Construction comparison has one instrumentation caveat: row digests include the global radiation
row start. A shorter earlier current can therefore shift a later current's construction records even
when its own light, radiation, completion, and Mail are exact. The strict hand has equal extent and
does not suffer this confound; the common-mode loss-current construction comparison does.

## Engineering record

The reusable Rust causal boundary now retains exact per-lineage `[RIDE, FOUND-this, FOUND-that,
dark]` terms and checks their componentwise sum against the edge total. The transport-foil adapter
adds exact `Q[omega]` arithmetic, strict forward/return/probe worlds, literal no-return, durable
typed consequence wires, and an artifact-only causal observer. It changes no Soma body law or CUDA
kernel.

The 31 CUDA-forward edges used the NVIDIA GeForce RTX 4080 SUPER for 313 launches. Recorded CUDA
carriage was 23.757 seconds and integration 0.587 seconds; total recorded boundary time was 29.193
seconds. The campaign retained about 362 MiB of exact artifacts and a 616 KiB normalized observer
report. Runtime and artifact extent are apparatus measurements, not comprehension quantities.

## Consequence

The experiment closes the bounded mathematical question it posed:

```text
same forward body
same terminal face
same scalar loss
same residual extent and byte inventory
opposite ordered residual hand
        -> changed residual conduct
        -> changed complete later probe conduct
        -> exact fixed world consequence

scalar quotient only
        -> exact return and later conduct
```

This is the engine realization of loss as difference and transport/foil as a situated truth deed.
It is also the first clean adjoint-shaped causal cell: world difference returns through a body which
lived the forward word and bends later current according to that history. The numeric `W*r` remains
an external exact null. No claim is made that Soma emitted it, reconstructed a past, or learned a
general semantic category.

The natural next construction is not another arithmetic probe. It is a code-and-language objective
loop over the already measured source workshop: native construction becomes executable source,
the real compiler/test world returns a whole typed program residual beside a scalar quotient, that
residual crosses the body, and genuinely later source material tests whether founded work is reused.
All actual consequences remain plural; no score, best-candidate crown, tokenizer, or authored answer
map enters.

## Carried-swing register

```text
CURRENT  exact returned residual hand -> fixed later W23 probe
HELD     identical forward body + equal residual extent/inventory + exact scalar loss
MEETING  opposite residual sequence, then identical probe and world consequence
TEST     exact repeat + scalar null + factor-order + no-return + full causal observer
DEED     RIDE whole residual transport; FOUND hand-conditioned changed later conduct
CARRY    measured causal loss/foil law -> code/language objective return
GRADE    BUILT / CUDA-FORWARD MEASURED / EXACT REPEAT / SOMA INTERIOR UNCHANGED
```

Primary machine-readable report: `analysis.json`.

# EROS PARENT-ON-OPEN SUBSTITUTION 01

**2026-07-22 · ACCEPTED / ONE FIXED SIX-CURRENT HOST OBSERVATION / ONE PARENT
RECEIVER EVALUATION OMITTED / FIVE EXACT RESIDUAL RETURNS / SIX SOURCE-ABSENT
RECURRENCES / NO MODEL RERUN**

## Question

Can an inherited Eros ecology act before one expensive parent relation, substitute when its
receiver is already closed, return to the parent only where that receiver is OPEN, and use those
returns in later conduct?

The parent in this bounded cell is the retained layer-7 OLMo gated-MLP factorization at five
declared receiver axes:

```text
P_R(g) = sum_(j=0)^8191 g_j W_(R,j).
```

The derived path is the already-fixed rank-six exact rational law:

```text
E_R(g_P) = g_P K
P = [0,1,2,3,4,5]
K in Q^(6 x 5).
```

The source tokenizer, model, context identities, BF16 activation words, and BF16 weight words are
lawful inherited ecology. BF16 is only the retained source inscription: both programs decode every
word to an exact dyadic and use integer/rational arithmetic thereafter.

## Fixed source and physical run

```text
SOURCE.json sha256       e1ac5c169ada255e0457d9458857cd89fb8b70a3c2fd4b94720619ee36a18ea3
REPORT.json sha256       e398e017ece8febfb30204d83f874455c22ac2a2aff415abf7de85bb3d37e9e3
source bytes              1,290,159
report bytes                 35,941
semantic first-pass rows             6
accepted machine events             25
host executor          ParallelHostLiveCurrentExecutor
host threads                            8
maximum event arcs          1,521 / 2,048
model reruns                           0
feature searches                       0
thresholds                             0
accepted run wall          6,170,371 microseconds
```

The largest atomic replacement contained 675 departing ecology words, 56 present-current words,
52 returned-residual words, 735 successor words, and three interfaces. The 2,048-arc aperture was
fixed from that complete preflight. The event was not split to fit storage.

One implementation attempt stopped before a report at the first successor with
`RegionalTopology(CurrentLineage(7))`. The query had emitted its continuation with `WITH`, while
the replacement incorrectly presented `AGAINST`. That reversal left the temporal aperture exposed,
so the engine correctly refused the claimed outgoing factor. The hand was corrected to preserve
the carried orientation; the frozen source, law, expected population, and acceptance predicates
were not changed. The accepted observation is the next and only completed run.

## Decision law

For the present categorical interface `c`, the active ecology returns its deepest applicable
population of receiver faces `D(c)`. The derived path may substitute only when:

```text
D(c) = {f}
and
Phi_R(E_R(g_P)) = f.
```

That decision was made from the ecology and current recovered through Soma before the parent
oracle was called. The harness retained the parent answer only as an after-the-fact control.

When the relation remained OPEN, the program evaluated all `8,192 x 5` exact parent terms once and
returned:

```text
r = P_R(g) - E_R(g_P).
```

The complete rational residual crossed the live mouth. Its consequential receiver face entered the
successor ecology; the large residual interior and event-local carriers then departed. This is the
tested compression: preserve what can change conduct at the declared receiver, not an archival
copy of every numerical event.

## First passage

```text
drop_then_reverse.code_comment.absent
    OPEN -> parent return

drop_then_reverse.code_comment.irrelevant_before
    OPEN -> parent return

drop_then_reverse.code_comment.relevant_after
    RIDE -> derived substitution; parent not invoked

reverse_then_drop.conversation.relevant_before
    OPEN -> parent return

reverse_then_drop.prose.relevant_before
    OPEN -> parent return

reverse_then_drop.code_comment.relevant_before
    OPEN -> parent return
```

The substituted current was a held-out `code_comment` context. Its derived face matched the exact
parent receiver control. The other five paths did not receive a convenient guess: each invoked the
complete retained parent relation and carried its exact difference back.

## Arithmetic consequence

At this receiver:

```text
always-parent foil
    6 calls
    245,760 exact products
    245,760 exact accumulations

selective parent
    5 calls
    204,800 exact products
    204,800 exact accumulations

derived local work
    180 exact rational products
    180 exact rational accumulations

omitted parent work
    1 call
    40,960 exact products
    40,960 exact accumulations
```

This is a one-sixth reduction in the measured parent receiver evaluations for the fixed first
pass, with 180 small-law products added. The wall timings in the report grade this exact-rational
host instrument only; they are not transformer latency or energy measurements.

## Later conduct

After all six currents, the ecology rested and remounted exactly. Six independent probes then
presented the same local interfaces and six pivot words without invoking the parent. All six
recovered the exact parent receiver face.

The final active body was:

```text
generation                 6
fallback support entries   3
returned exact entries     5
Standing constituents      1
cells                       3
incidences                736
pins                      736
paths                     736
transport terms           736
live source lineages        0
rest sha256  b4ec74c799aaafb6135147d9b6405c5aa49aa324e25461d8e6a8b1c5ed1647bd
```

The no-ecology sibling could not recover an ecology or substitute. The changed later conduct
therefore depended on the inherited-and-cultivated Standing rather than on the current alone.

## What this establishes

- Eros can carry a transformer-derived local transformation as active Standing rather than merely
  archive or visualize it.
- That ecology can decide one receiver-relative closure before an expensive parent calculation.
- OPEN is operational: it invokes the source relation and returns a complete oriented residual.
- A parent return can change the local topology so later in-population conduct no longer needs that
  parent receiver calculation.
- The active successor preserves conditional receiver support while the complete transient
  numerical interiors and source lineages depart.
- Existing machine-learning ecology and self-emanated Eros ecology can therefore participate in
  one bounded fallback/substitution circuit.

## What it does not establish

- The upstream transformer still produced the retained 8,192 activations. This run omitted one
  five-axis parent projection, not tokenization, earlier layers, the full 2,048-axis MLP return, or
  downstream decoding.
- The receiver face is exact orientation plus signed-axis order, not exact numerical equality of
  the five outputs.
- The recurrence probes are in the cultivated six-current population and still receive six pivot
  words. They are not raw-text, novel-context, or end-to-end language tests.
- The rank-six law is inherited from a square interpolation over six training rows; one held-out
  substitution does not establish broad generalization.
- Context identities and the five-axis receiver are source-declared. Eros did not discover a
  tokenizer, ontology, routing objective, or universal taxonomy.
- No CUDA path, model-integrated router, full-block replacement, language response, autonomous
  objective, sustained ecology, or energy measurement was attempted.

## Artifacts

- `SOURCE.json` — immutable retained activations, weights, local law, receiver supports, controls,
  and fixed expected population.
- `REPORT.json` — complete accepted machine testimony.
- `../../RESEARCH/2026-07-22_THE_DERIVED_FACE_MAY_SUBSTITUTE_FOR_THE_PARENT_THE_OPEN_RECEIVER_RETURNS_TO_THE_SOURCE.md`
  — construction contract.
- `../../life/examples/eros_parent_on_open_substitution.rs` — bounded parent-on-OPEN world.
- `../../../../experiments/informant-ecology/sidecar/extract_parent_on_open_substitution.py` —
  exact retained-source extractor.

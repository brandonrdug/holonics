# FACTOR DERIVATION ECOLOGY 01 — RESULTS

**2026-07-17 · FORMULA §LXXXIV DERIVED / PLURAL SURFACE BUILT / CUDA-FORWARD MEASURED /
COMPLETE PATH POPULATION RENDERED AND READ / SOMA INTERIOR UNCHANGED**

## Result

One exact `H_128` factor body crossed through compact typed values, algebraic inscription, and
ordinary explanatory language. Every candidate additionally carried radix-2, radix-10, and
radix-16 digit order, digit sum, reversal, palindrome incidence, and the reversed integer's factor
disposition where the reversed face remained inside the local horizon.

The result separates three relations which had been visually collocated:

- primality is a completed multiplication-world exclusion through `sqrt(n)`;
- palindrome is one radix-dependent fixed point of digit reversal; and
- reversal-prime incidence is one radix-dependent pair of founded factor axes.

`13 <-> 31` and `17 <-> 71` are decimal reversal-prime pairs. `131` and `313` are each decimal
palindromic primes; `313` is also binary-palindromic. Digit sums do not follow a power-of-two prime
law.

**GRADE:** EXACT FACTOR BODY / THREE EXACT NUMERAL CHARTS / REVERSAL CAYLEY HAND / PLURAL
CROSS-RENDER CONDUCT / HISTORY-CHANGED PATHS / NO PRIME, PALINDROME, OR REVERSAL TAXONOMY ENTERS
SOMA.

## 1. Exact finite population

The returned event face contains:

| family | events |
|---|---:|
| horizon header | 1 |
| candidates | 128 |
| divisor tests | 437 |
| factor cells | 328 |
| axis lifts | 224 |
| prime gaps | 30 |
| interior prime-gap coordinates | 95 |
| two-handed factor approaches | 397 |
| numeral charts (`128 x 3`) | 384 |
| **total** | **2,024** |

Each event crossed as typed, inscription, and exposition light: 6,072 derivation currents. Receipt,
incidence, and carried consequence faces make the complete passage 6,150 currents / 374,653 active
octets. The typed event family reconstructs the exact source `HorizonRead`; the textual faces are
not required for reconstruction.

## 2. The numeral pattern

For `n=sum d_i b^i`, the exact observer laws are

```text
S_b(n) = sum d_i,
R_b(n) = sum d_i b^(k-1-i),
n = R_b(n) = S_b(n)                 (mod b-1),
R_b(n) = (-1)^(k-1) n              (mod b+1).
```

The first congruence explains digit-sum divisibility. The second implies that every even-length
palindrome is divisible by `b+1`; the only possible prime is `11_b=b+1` itself. The three local
exceptions are `11_2=3`, `11_10=11`, and `11_16=17`.

The 31 founded prime axes through 128 have these exact chart faces:

| radix | palindromic primes | non-palindromic reversal-prime pairs |
|---:|---|---|
| 2 | `3,5,7,17,31,73,107,127` | `11<->13, 23<->29, 37<->41, 43<->53, 47<->61, 67<->97, 71<->113, 83<->101` |
| 10 | `2,3,5,7,11,101` | `13<->31, 17<->71, 37<->73, 79<->97` |
| 16 | `2,3,5,7,11,13,17` | `23<->113, 53<->83` |

The radix-10 sequence `S(13),S(17),S(19) = 4,8,10` comes from the local affine decade law
`S_10(n)=n-9` for `10<=n<=19`. The powers `4=2^2` and `8=2^3` do not continue: `S_10(23)=5` and
`S_10(29)=11`. A twin-prime step changes digit sum by `2 (mod 9)`, but a decimal carry changes the
visible difference by a multiple of nine: `17->19` gives `8->10`, while `29->31` gives `11->4`.

The exact `H_313` host test separately closes the supplied examples. `131` and `313` are prime and
decimal-palindromic; `313=100111001_2` is also binary-palindromic. This larger face has not yet been
carried through the CUDA ecology.

## 3. Smith/periplus read

Each chart carries the exact homogeneous pair and real Cayley coordinate

```text
q_b(n) = [n:R_b(n)],
chi_b(n) = (n-R_b(n))/(n+R_b(n)).
```

Reversal swaps the pair and flips `chi`'s hand. Palindromes lie at `chi=0`. Decimal `13` and `31`
therefore occupy `-18/44` and `18/44`; the integer factor body is prime at both ends. Decimal `19`
instead reaches `91=7*13`, while binary reaches `25=5^2` and hexadecimal reaches `49=7^2`.
Reversal is the chart operation; factor disposition supplies its independent foil.

## 4. CUDA passages

Both exact passages passed whole-lifecycle preflight on the NVIDIA GeForce RTX 4080 SUPER and
inverted their world transitions exactly.

| face | first passage | genuinely later passage |
|---|---:|---:|
| currents | 6,150 | 6,150 |
| derivation events/renderings | 2,024 / 6,072 | 2,024 / 6,072 |
| completion rows | 377,636 | 374,271 |
| atom / afferent / efferent | `124,204 / 185,407 / 68,025` | `120,983 / 177,956 / 75,332` |
| maximum grain | 20 | 19 |
| archive extent | 1,598,954,424 | 1,663,303,464 octets |
| CUDA launches | 12 | 12 |

Recorded carriage was 8.081415120 seconds, integration 2.364806466 seconds, and total boundary
time 17.331985204 seconds across both edges. The source gate charged a 3,021,996,032-octet intact
device-lineage minimum against 16,448,356,352 free octets.

## 5. Complete path read

`PATHS.tsv` retains all 751,907 completion rows. `GROUPS.md` renders every event and all three path
faces; `REPORT.json` retains exact node, grip, shape, projective, Cayley, and numeral values.

The later identical population did not replay a frozen table:

```text
matched rendering paths             6,072
exact complete paths                  118
changed complete paths              5,954
later paths with returned nodes     5,436
returned node identities           37,539
later first-seen node identities  252,149
```

The sole exact numeral path was typed `1_2`, which had zero completion rows on both edges. Every
nonempty numeral path changed. Yet recurrence remained broad: different renderings of the same
numeral event shared at least one completed node identity for 347 of 384 events on edge one and 338
on edge two.

At the inscription face, the requested reversal pairs had greater exact node overlap than the
declared two-digit controls in both passages:

| relation | first | later |
|---|---:|---:|
| `13 <-> 31` | 2 | 7 |
| `13 / 37` | 0 | 2 |
| `13 / 19` | 0 | 2 |
| `17 <-> 71` | 7 | 5 |
| `17 / 37` | 0 | 1 |
| `17 / 19` | 1 | 1 |

This is an exact bounded relation among those carried inscriptions. It is not promoted into a
reversal score. The coarse completion-shape quotient is also not a category: its most recurrent
typed numeral word crosses prime and composite candidates, palindromes and non-palindromes, and
locally known and out-of-horizon reversals.

## 6. Next construction

Grow the same factor world through 313 on the continued body. Return both the retained `H_128`
subface and the newly admitted region. Read which factor, gap, digit-sum, reversal, and palindrome
paths recur, transform, or found. The comparison must retain radix as receiver provenance and every
reversal outside the earlier horizon as unresolved rather than retroactively assigning it a class.

Full derivation:
`../../RESEARCH/2026-07-17_THE_NUMERAL_FACE_TURNS_THE_FACTOR_BODY_REMAINS.md`.


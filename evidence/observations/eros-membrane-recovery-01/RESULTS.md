# EROS MEMBRANE RECOVERY 01 — RESULTS

Date: 2026-07-18  
Status: **BUILT / WORKSPACE-GATED / BOUNDED CONFIGURATION-RELATION BRIDGE CUDA-MEASURED / LIVE SESSION RETURN MEASURED / FULL RECOVERY ACCEPTANCE OPEN**

## Question and stopping boundary

This cut asked whether one sparse source configuration could cross the active receiving membrane as
consequential relation rather than remain a host-side journal annotation, and whether its emitted
construction could return through the same live session without rereading a world checkpoint.

The run stops after that exact return. It does not claim a resident CUDA ecology, a native
cut-local CUDA fold, complete Holon receipt closure, a general source organ, autonomous rest, useful
language production, or forest-scale readiness.

## Construction repaired

- A genuinely sparse `Presentation` now retains its canonical cuts and supplies one typed `CFGI`
  relation event for each cut. The original material currents remain distinct and unchanged.
- `ReceivingPresentation` is the exact population given to CUDA and to the listener-side receipt.
- Forest manifests retain occurrence coordinates and ordered standing/arriving incidence receipts.
  Equal forms at different coordinates remain different occurrences.
- `CausalSession` keeps its lived world and named emitted surfaces mounted in memory. Durable JSON
  and SLEEP bodies are checkpoint testimony, not the active between-contact surface.
- Lifecycle receipts distinguish host preparation, CUDA conduct, persistence, world receiving,
  receiver serialization, surface commit, and recovery.

The bridge is intentionally bounded. Configuration incidence is now consequential active light,
but organ identity, complete occurrence/path ancestry, and six-role Holon receipts do not yet cross
one native cut-local CUDA integration primitive.

## Preflight

The cut starts from the coherent centered-pair body and world after historical edge 12:

- 7,958 paths, 6,074 complete;
- 6,636 exact section instances, 3,923 unique byte faces, 146,828 section octets;
- 50 exact source occurrences over 20 prior paths in the 299-octet aperture source; and
- zero pending currents before the mount.

The 98-octet dark control and the opaque control made no contact. No whole-repository scan or large
forest passage was attempted.

Exact preflight: `acceptance.preflight.json`.

## Failure foil and exact recovery

`acceptance-run-01` used the open mouth. Edges 13–17 completed, and edge 18 completed CUDA conduct
and checkpoint persistence before the world receiver refused the relation: its validation compared
canonical `CFGI` cut ordinals with the manifest's original listener order. This was a receiver gauge
error, not a material, occurrence, or CUDA-law failure.

The receiver now derives the same canonical member-set order as the membrane. The focused regression
`configured_manifest_follows_the_membranes_canonical_cut_order` closes the mismatch. Resuming from
`edge-018.conducted.json` completed edge 18 without replaying CUDA; `edge-018.recovery.json` records
the recovered receiver lifecycle and its measurement gaps.

This run is retained as a failure/recovery foil. It is not the clean acceptance result.

## Clean bounded acceptance

`acceptance.closed.plan.json` starts from the same exact edge-12 body/world and performs only the
declared mount, configured passage, and live return. `acceptance-run-02/session.json` closes at edge
15 with no exposed surface and a 100,659,856-octet final body.

| Edge | Deed | Receiving population | CUDA result | World/path result |
|---|---|---:|---:|---|
| 13 | mount the external occurrence field | 1 current | 147 completion rows, 1,316 grips, 7 launches | 6 roots + 4 extensions; 4 RIDE encounters |
| 14 | carry generation-0 configured field | 2 material + 11 typed relation currents | 250 rows, 508 grips, 6 launches | 5 roots + 41 extensions; 98 FOUND + 41 RIDE encounters |
| 15 | return all 41 emitted constructions | 41 live surface currents | 2,513 rows, 7,414 grips, 8 launches | 143 roots + 1,930 extensions; 1,569 FOUND + 1,765 RIDE encounters |

Every edge used the NVIDIA GeForce RTX 4080 SUPER and inverted exactly.

The edge-15 receipt names `edge-014.surface` with
`input_surface_origin = session_resident`. The durable `edge-014.surface.json` is the same exact
surface's checkpoint, not a per-contact filesystem read.

## Qualitative read of the produced paths

### The configured passage

The two material currents are exact overlapping source regions `[29,70)` and `[33,70)` from the
same 299-octet aperture body. Eleven configurations carry 22 standing/arriving occurrence receipts
over eight prior paths: `2138`, `2378`, `4158`, `4204`, `6945`, `7346`, `7369`, and `7959`.

The passage adds 46 paths: five new world-material roots and 41 genuine extensions. Every extension
ends at the same exact later material coordinate `[43,46)`, the bytes `per`. What precedes that
contact is not one authored category. The carried interiors include Rust source, natural language,
prior produced construction, and world material in depths two through six. Representative exact
faces are:

```text
 fn translated_zero(n: i64, d: i64) per
ct ratioper
quotie fn translated_zero(n: i64, d: i64) per
 fn translated_zero(n: i64, d: i64) b fn transd: i64) per
quotie fn translated_zero(n: i64, d: i64) ct ratio: i64) per
  d)\n}\n\npub fn translated fn translated_zero(n: i64, d: i64) ct ratiob fn transd: i64) per
```

These are fragmentary causal strings, not fluent prose and not selected answers. Their relevant
result is exact local selectivity: the sparse occurrence relation did not present the entire file
or manufacture a branch×source product; it extended the prior mixed path family at one actual
coordinate.

The historical flat field carried one whole 251-octet current and yielded 1,202 paths. This
configured passage yields 41 extensions because the false whole-current co-presence has been
removed. The two populations have different grains and cannot be compared as a quality score.

### The live return

Returning the 41 constructions decomposes them through the ordinary construction eye into 175
spans: 143 CLOSED and 32 OPEN. The world adds 2,073 paths: 143 roots and 1,930 extensions. Their
depth distribution is:

| Depth | Paths |
|---:|---:|
| 1 | 143 |
| 2 | 307 |
| 3 | 544 |
| 4 | 590 |
| 5 | 295 |
| 6 | 149 |
| 7 | 45 |

The population contains 874 distinct complete byte faces. The 143 returned roots expose 59 exact
terminal byte faces from 33 construction sources. Their strongest recurrent faces are `ans` (616
paths), ` translated` (342), `nsl` (324), `\n\np` (120), `t` (110), one space (106), and
`64) per` (81). Those are measured recurrence basins, not semantic tokens installed by the host.
They explain the visible character-level fragmentation: short returned faces currently attach much
more broadly than longer coherent regions.

The return is nevertheless not confined to one material role. Of the 2,073 paths:

- 931 carry prior Rust ancestry;
- 664 carry prior natural-language ancestry;
- 840 carry prior world-material ancestry;
- 1,350 carry prior produced-construction ancestry;
- 373 carry both Rust and natural-language ancestry; and
- 130 carry Rust, natural language, and world material together before the returned construction.

Examples across those families include:

```text
nsl
lnsl
translated_poopposite_unit_is_the_cthis contact re-open 
\nmod tests {\nz {s {e two sides iopposite_unit_is_the_cthis contact re-open 
```

This is a real second-order return population, but its contemporary face is still dominated by
short recurrence. The next language construction must therefore study which longer returned
sections acquire later conduct; it must not call fragmentation coherent speech or install a host
selector to hide it.

The application world's `construction_return` support has one founding contact and three later
RIDEs. After the bounded stop, generation 1 holds one leader with 86 exact incidences and 18 pending
currents with 53 local incidence configurations. The world is active at the stop; this run does not
claim autonomous rest.

Complete path source: `context-observer.plan.json`, reproducible from
`acceptance-run-02/edge-015.world-after.json`.

## Physical lifecycle

| Edge | Host preparation | CUDA wall | Persistence | World receive | Receiver serialization | Total active wall |
|---:|---:|---:|---:|---:|---:|---:|
| 13 | 0.766 s | 0.265 s | 0.306 s | 3.346 s | 1.186 s | 5.896 s |
| 14 | 1.145 s | 0.256 s | 0.303 s | 3.128 s | 1.248 s | 6.118 s |
| 15 | 0.700 s | 0.314 s | 0.319 s | 3.790 s | 0.162 s | 5.303 s |

All three receipts grade execution as `host_orchestrated_cuda_participating` and state:

- `cuda_participated = true`;
- `resident_cuda_ecology = false`;
- `end_to_end_card_conduct = false`; and
- `complete_lifecycle_preflight = false`.

The immediate bottleneck is the host receiver and its whole-world checkpoint representation, not
CUDA carriage. The card is used, but the active ecology is not resident on it.

## Exact grade and remaining construction

Closed here:

1. sparse canonical cuts affect actual receiving conduct through typed relation light;
2. distinct occurrence coordinates survive in exact world receipts;
3. a named emitted surface returns through live session memory;
4. failure after CUDA can resume the receiver without replaying card conduct;
5. complete phase accounting and honest execution grades are emitted; and
6. the clean three-edge bounded cell passes the complete workspace gate.

Still open:

1. a native sparse-incidence CUDA buffer and cut-local integration primitive;
2. complete organ/occurrence/path provenance and before→meeting→deed→return→after Holon receipt
   closure through the active membrane;
3. persistent CUDA context/body and parallel incidence/world receiving;
4. a generalized typed source organ with live mount/unmount beyond the filesystem prototype;
5. a clean post-return passage which reads how the 2,073-path return changes a genuinely later
   common probe; and
6. whole-lifecycle resource preflight and content-addressed/reflink checkpoint persistence.

No large forest run is authorized by this result. The next construction is the live Holon receipt
and resident source-organ/device seam, followed by one bounded mathematics⊕language⊕code objective
cell with a genuinely later common probe.

## Validation

- `cargo test --workspace --quiet`: all suites pass; principal suites are `130 passed / 2 ignored`
  and `345 passed / 2 ignored`.
- The canonical-order regression and historical sparse-order gauge test pass.
- Release `life` build passes.
- No large forest or repository-wide application passage was run.


# CONSTRAINT-DEED WORLD 01 — FIRST RECEIVER RESULTS

**2026-07-17 · POLYGON/GEAR/PROJECTIVE/FAMILY FRONT BUILT · CUDA-FORWARD MEASURED · CORRECTED DEED EXACTLY REPEATED ACROSS PROCESS DIRECTORIES · COMPLETE CONSEQUENCE RETURN MEASURED · LATER CONSTRAINT AND BODY/WORLD FACTORIAL OPEN · SOMA INTERIOR UNCHANGED**

## What this cell establishes

Four witness/return passages grew one continued mathematical construction world before any deed
was asked of it. At edge 8 that world retained four source archives, 710 CLOSED roots, 13 OPEN
roots, 1,296 paths, and 420 encounters. Exactly 586 paths had at least two segments and therefore
stood as complete recurrent-path candidates. The deed receiver tried all 586 in append-only order.

The receiver did not parse the candidate's flattened byte concatenation and did not manufacture a
mathematical expression from a partial span. It used each span's exact carried source coordinate to
ask which intact framed world record the path had actually contacted. An overlap with a still-exact
record admitted that whole record as the occupied world object. A path touching only prose or law
material returned `record_absent`.

This is one first receiver cell, not closure of the complete plan. The complete returned family has
now re-entered the body. A genuinely later constraint must still demonstrate which retained hole,
hand, parameter, or affine boundary conducts the next event; the body/world factorial and changed-
order/collapsed-face siblings remain open.

## The native path population, read before its quotient

The 586 candidate paths have the following lived depths:

| segments | paths |
|---:|---:|
| 2 | 422 |
| 3 | 148 |
| 4 | 16 |

They contain 481 exact material joints, 236 RIDE Swing joints, and 49 common-chart joints. They were
founded at witness-return edges 2/4/6 in populations 240/269/77 and last extended at edges 4/6/8 in
populations 46/176/364. Their byte faces contain 584 distinct SHA-256 values: two pairs share one
flattened byte face while retaining distinct path identity.

One representative depth-two path is path 143. Its first span is
`00-polygon.conditions.txt[305,324)` (`to the material gra`); its later span is
`00-polygon.records.txt[48,56)` (`es\":[{\"x`). They meet at common Soma grip 478208. The later
span lies inside the intact source record `00-polygon.records.txt[0,212)`, so the receiver enacts
that complete contact-butterfly record. It does not pretend that the path's 27 flattened octets are
the complete mathematical program. Many other paths reach the same record through different
segments, depths, and joints. Those are different approaches to one occupied object, not duplicate
claims that the record is intrinsically more important.

The 28 refused paths are equally informative. For example, path 326 connects
`00-polygon.conditions.txt[365,374)` (`lanar dis`) to
`01-gear.exposition.txt[270,290)` (`illing while keeping`). Path 386 connects prose `ar` to the law
span `lation adjacency only; it does not classify`. Neither touches a framed deed record, so both
remain lawful recurrent constructions and return `record_absent`; the world does not repair them
into mathematics.

## Complete deed population

Of 586 path receipts, 558 enacted at least one exact contacted record and 28 refused. Repeated
counts below are source-incidence multiplicities under this one history, not probabilities,
importance scores, selections, or authored taxa.

| exact world deed | enacted contacts |
|---|---:|
| polygon incidence | 387 |
| regular star | 112 |
| quadrilateral flip | 65 |
| arbitrary hexagon completion | 59 |
| projective conic chart | 65 |
| Pascal conic | 34 |
| Brianchon conic | 12 |
| differential family | 21 |
| square cover | 14 |
| removable singularity | 2 |
| no framed record | 28 refused |

Every candidate and every receipt travels separately, producing 1,172 consequence currents. Every
receipt retains exact source, range, framed bytes, record hash, parsed world program, complete
consequence or typed refusal, and candidate ancestry. The candidate population collectively
contacted 771 framed-record incidences; one path may touch more than one exact record.

## Polygon: the same display crossing is not the same circuit

The contact and pass-over butterflies use the same four source vertices, edge word, projected
crossing `(3,3)`, display `f`-face `(V,E,bounded regions)=(5,6,2)`, and signed turn sequence
`(+,+,-,-)`. Their material bodies differ:

| body | projected joint coordinates | admitted joints | material `(V,E,cycle rank)` |
|---|---:|---:|---:|
| butterfly, contact | 1 | 1 | `(5,6,2)` |
| butterfly, pass-over | 1 | 0 | `(4,4,1)` |
| rectangle control | 0 | 0 | `(4,4,1)` |

The corrected winding receipt refuses to assign a region winding to a probe on the boundary.
For both butterflies, `(1,3)` is outside with winding 0, while `(3,3)` returns
`winding=null, on_boundary=true`. For the rectangle, `(3,2)` has winding 1 and `(8,2)` has winding
0. The former implementation's boundary value 0 is retained only in the pre-correction v2 foil.

## Gear word, association, and local affine completion

The exact star words return:

| word | components | vertices/component | winding/component | associahedron dimension | triangulations |
|---|---:|---:|---:|---:|---:|
| `{5/2}` | 1 | 5 | 2 | 2 | 5 |
| `{6/2}` | 2 | 3 | 1 | 3 | 14 |
| `{7/3}` | 1 | 7 | 3 | 4 | 42 |

The convex quadrilateral deed carries diagonal `[0,2]` into `[1,3]`, preserves positive
orientation, and records one associahedron edge. It does not turn the associahedron into a Soma
category.

For the arbitrary six-vertex body `(0,0),(4,0),(6,2),(5,6),(1,7),(-2,3)`, consecutive-triple
parallelogram completion yields the repeating alternating sequence

```text
(2,2), (3,4), (2,3), (2,2), (3,4), (2,3),
```

and the exact centroid `(7/3,3)`. The global alternating recurrence appears only after all six
local operations are carried.

## Projective incidence and chart boundary

The Pascal record returns opposite-side intersections

```text
[2:2:1], [7:3:1], [5:1:0]
```

on exact line `[1:-5:8]`; collinearity is true. The Brianchon dual returns principal diagonals
`[1:-4:2]`, `[1:-6:7]`, `[0:2:-5]` with exact concurrence `[16:5:2]`.

For `XZ=Y^2`, the two zero parameters retain different missing affine boundaries:

```text
Z-chart t=0 -> [0:0:1], overlap absent
X-chart u=0 -> [1:0:0], overlap absent
```

The ordinary overlap control remains exact:

```text
Z-chart t=2 -> [4:2:1], u=1/2.
```

A common conic equation therefore does not collapse chart, pole, or omitted endpoint.

## Equal visible faces retain different interiors

- `R(x)=(x^2-1)/(x-1)` at `x=2` returns value 3 while retaining excluded point 1. At `x=1`, the
  completed line's visible value is refused: `value=null, admitted=false`.
- `q=4` returns complete preimage `{-2,+2}`. Hand `+1` admits only `+2`; hand `-1` admits only
  `-2`; both continue to carry the full preimage.
- `D(x^2+c)=2x` without a boundary returns arbitrary rational `c`. The later local boundary
  `y(0)=3` closes `c=3`; no hidden `c=0` is introduced.

These are already complete world consequences. What remains unmeasured is their genuinely later
use as pivots by a common next question.

## Corrected deed and return

The corrected deed was launched once from the repository root and once from the Soma workspace.
This deliberately changes the process directory while holding archive, world, input currents, and
world law fixed. The following faces repeat exactly:

- successor body SHA-256
  `bfedccd3a8e2f77671dab58250c2385aa8eb6353e7c8167132f48c0fe0a34e39`;
- completion and construction wires;
- normalized radiation: 3 currents, 684 light octets, 26,676 raw words, axis 4096;
- deed journal SHA-256
  `d1c8f2b91ddffe511b510e929678f1d9b7358e7c6cbbf1fced2bc4dac7b4ac2d`;
- successor world SHA-256
  `fa944025484c536490f76c3702a8078e095624489edba44fa6e53ecc0aadda76`;
- complete 1,172-current deed surface SHA-256
  `f3c3b16a3d9d3eabcc87e2eaa3fb5a0de37eb8916e95f2de9437dd7a310d184d`.

The complete surface then returned through an ordinary CUDA-forward edge:

| face | exact observation |
|---|---:|
| returned currents | 1,172 |
| completion rows | 2,272,797 |
| contact grips | 182 |
| prior -> standing axis | `4096 -> 8192` |
| occupied grips | 21,995,266 |
| CUDA launches | 23 |
| CUDA carriage | 47.943 s |
| host integration | 27.698 s |
| successor body | 5,741,371,520 octets |

The return inverse is exact. Its body SHA-256 is
`ced097ba6b6a5205b93d3d5107c4d089c6063d029c847634da9555189a3e108f`; its journal SHA-256 is
`87cae6eb6c254988040b34e36590eab0ce39d4c7ec2ac8c139caa1e85b00d7d7`; its world SHA-256 is
`ad3d2499bbf8c1979c8d9ced7a024908ae0a3fed58b483539bd21468654077ea`.

## The later constraint acts; the returned family changes its route

The genuinely later question is one exact world object with two natural currents: its request and
its complete constraint boundary. The first enactment retains its original request, instrument,
and boundary. The question does not need the enactment's generic instrument repeated beside it.
The discarded three-current attempt is a useful failure foil: request and boundary shared 2 grips
before return and 3 after return, while the repeated instrument shared none with either current and
therefore disconnected the otherwise lawful question in both bodies.

The repaired two-current question holds its 961 input octets, mathematical world, recurrent path
population, and every constraint exact across two bodies:

| exact face | before consequence-family return | after consequence-family return |
|---|---:|---:|
| input currents | 2 | 2 |
| completion rows | 1,102 | 1,559 |
| lineage rows | `385,717` | `436,1,123` |
| contact grips | 2 | 3 |
| constructions | 57 | 108 |
| CLOSED / OPEN | `55 / 2` | `106 / 2` |
| standing axis | 4,096 | 8,192 |
| CUDA launches | 9 | 10 |
| exact consequence currents | 1,172 | 1,172 |
| exact consequence light | 4,777,169 octets | 4,777,169 octets |
| consequence-surface SHA-256 | `02eb6856...275d38f` | `02eb6856...275d38f` |

The complete consequence arrays are byte-for-byte identical after canonical JSON serialization
(`0db252bedb2e2423886a12823938463a4fe90832631c878d432fa691f5f6b178`). Thus the returned family did
not alter the world's answer or select a different candidate. It altered the body's exact route to
the identical question: 457 more completion rows, one more material contact grip, 51 more completed
constructions, and a different successor body.

All 586 standing paths are tried again. They encounter 799 intact record incidences: 771 enact and
28 retain `record_absent`. The later constraint reads the enacted family as follows:

- both removable-singularity contacts retain the excluded `x=1` and refuse to admit the requested
  visible value 2 there;
- 12 square-cover contacts carry the requested positive hand to `+2`; two negative-hand-only
  sources retain both exact preimages but report the requested hand incompatible;
- all 21 differential contacts close at `c=3`; seven carry the formerly open parameter family and
  fourteen already carry the same boundary;
- 41 projective contacts reach the requested X chart: 20 already occupy its `u=0` affine boundary
  and 21 transport `t=2` to `u=1/2`;
- 24 Z-chart `t=0` contacts retain `[0:0:1]` as the omitted X-chart endpoint and do not manufacture
  a finite target parameter; and
- 669 polygon, gear, incidence, and other exact consequences remain typed `inapplicable` rather
  than being repaired into the four requested families.

This is the first measured answer to the open deed. A mathematical consequence can already be
derived from the standing construction world; returning that whole consequence family through the
body changes the later conduct without changing the exact world answer. World truth-face and lived
approach are therefore experimentally separable at this grain.

## Failure foils retained

`results/first-receiver/` flattened candidate bytes and therefore produced 591 refusal attempts
from 586 paths: 510 `record_absent`, 79 `malformed_record`, and 2 `empty_record`. It proves why the
carried source coordinate, rather than accidental concatenation, must contact the world object.

`results/first-return-v3/` and `results/first-return-v4/` exposed an implementation defect: the
source record reread depended on process working directory and refused all 586 candidates when
launched from the repository root. The repaired receiver resolves stable Soma-relative coordinates
from the compiled workspace root. The repository-root v5 run and Soma-root deed repeat are exact.

`results/first-receiver-v2/edge-009` is the pre-correction full receiver. It established the broad
population and return mechanism but assigned boundary probes winding 0. It may not be cited as the
final winding receipt.

`results/later-question-pre-return/` and `results/later-question-post-return/` are the overcomplete
question-aperture foil. Their request and boundary had lawful pairwise incidence, but one repeated
generic instrument current was isolated, so plural contact remained silent. The v2 plans remove
only that redundant world current; Soma and the mathematical transducer are unchanged.

## Open factorial and arithmetic successor

The common later question is measured. The remaining CONSTRAINT-DEED factorial must determine
which part of the construction supplies its answer and which part bends its route:

1. empty body / empty world;
2. returned body / empty world;
3. initial body / complete construction world; and
4. returned body / complete construction world.

Then run the changed-order and collapsed-face siblings. In parallel, the now exact factor-palindrome
successor grows a finite divisor/exclusion complex from actual multiplication, exponentiation, and
irreducibility deeds. It must distinguish the palindromic divisor-rank face, the prior-chart hole
which FOUNDs a new prime axis, additive prime gaps, and any later weighted zeta-like quotient.

## Exact artifacts

- Build plan: `PLAN.md`
- Full corrected return: `first-return.plan.json` -> `results/first-return-v5/`
- Corrected cross-directory repeat: `first-deed-repeat.plan.json` ->
  `results/first-deed-repeat-v5/`
- Later constraint before return: `later-question-pre-return-v2.plan.json` ->
  `results/later-question-pre-return-v2/`
- Same exact constraint after return: `later-question-post-return-v2.plan.json` ->
  `results/later-question-post-return-v2/`
- Source materials: `materials/`
- World transducer: `../../life/src/staging/boundary/code_workshop/math_deed.rs`
- Theory: `../../RESEARCH/2026-07-17_THE_BOUNDARY_WAVE_INTEGRATES_THE_SHEET_THE_EDGE_WORD_MAKES_THE_GEAR.md`

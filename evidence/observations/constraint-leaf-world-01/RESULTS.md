# CONSTRAINT-LEAF WORLD 01 — THE CARRIED BASIS BENDS THE LATER DEED

**2026-07-15 · RATIFIED · BUILT · CUDA-FORWARD MEASURED · EXACT COLLAPSE CONTROL ·
MATHEMATICS EXIT CLOSED · SOMA INTERIOR UNCHANGED**

## Result

The exact five-face campaign closed:

1. `A=2I` keeps the same visible operator face under the distinct carried bases `B0=I` and
   `B1=R`;
2. the same later anisotropic local action `J` produces different exact oriented consequences;
3. lawful collapse of the basis produces the same typed `basis_absent` boundary in both histories;
4. the isotropic control `K=I` produces the same exact `3I` consequence with or without a carried
   basis; and
5. an unseen exact basis `R2` and the authored composition `R2 R` cross fresh and previously
   nurtured bodies with exact world consequences but history-conditioned Soma conduct.

The experiment carried 46 edges through the RTX 4080 SUPER. Every immutable-source gate passed,
every edge made actual Mail contact, and every world transition inverted exactly. No matrix rule,
basis selector, class label, score, reward, or hidden default entered Soma.

This closes the foundational mathematics cell at its declared grain. Mathematics remains an exact
checker world, but it is no longer a prerequisite loop. The next construction carries the same
causal form into code transformations coupled with language.

## The causal seam

The executable boundary is:

```text
witness currents
    -> unchanged CUDA-forward Soma edge
    -> actual witness Mail contact
    -> typed testimony surface
    -> unchanged receiving Soma edge
    -> actual testimony Mail contact
    -> standing world constraint
    -> genuinely later action currents
    -> unchanged CUDA-forward Soma edge
    -> actual action Mail contact
    -> typed production surface
    -> ordinary returning currents
```

The world cannot install testimony from the plan or witness journal. It becomes standing only when
the returned consequence currents actually contact the receiving body. `rebase` transports the
operator, basis, inverse, and ordered authored path. `collapse` transports only the common scalar
operator face. A non-scalar local action requires a carried basis; a scalar action does not.

The world uses exact rational `2x2` matrices and the already established proof-world rational
arithmetic. The current itself remains ordinary bytes; the exact calculation, complete journal,
and inverse are world physics and observer evidence.

## Primary exact cell

The common problem and bases are:

```text
A  = 2I
B0 = I
B1 = [[3/5,-4/5],[4/5,3/5]]
J  = [[1,0],[0,-1]]
K  = I
e1 = (1,0)
```

### Basis-sensitive action

The two re-base receivers returned:

```text
A + B0 J B0^-1 = [[3,0],[0,1]]
                  e1 -> (3,0)

A + B1 J B1^-1 = [[43/25,24/25],[24/25,57/25]]
                  e1 -> (43/25,24/25)
```

Their complete production surfaces differ:

```text
B0  b9b3ab2e71cc0c04aea61aafe07e1c9af045d14c5650b28489899ad072f7f016
B1  c8f0624d6c9dac25a35a82f487f72b511e6e37b77c29c3b566bd16f0484f0f13
```

The characteristic face is nevertheless exact in both:

```text
lambda^2 - 4 lambda + 3
trace = 4
determinant = 3
```

This is the measured distinction between a shared spectral face and an oriented action. The
invariant is not a replacement for the carried basis.

### Information-losing collapse

The two independent collapse witnesses emitted byte-identical testimony:

```text
d71ec998acadabfe4d62c95b1d196563ab9b652989261cb9a04db147861828f1
```

Their later `J` receivers emitted byte-identical production:

```text
030347dd771210ad9f09de4ad2176160cade7f880fe08a7d552cd65711ae0903
```

That surface carries the scalar constraint `A=2I` beside the typed boundary:

```text
basis_absent:
"local anisotropic action requires a carried basis"
```

No identity basis or most-recent basis was supplied. Across all three receiving edges the complete
bodies, completion wires, construction wires, Mail, journals, and world states are byte-identical.
Raw radiation differs only in its declared measurement header and compares exact with the canonical
radiation reader:

```text
edge 1  2 currents · 1,429 light octets · 55,731 raw words · axis 256
edge 2  3 currents ·   556 light octets · 21,684 raw words · axis 256
edge 3  2 currents · 2,082 light octets · 81,198 raw words · axis 512
```

### Basis-independent control

Every `K=I` sibling—`B0`, `B1`, collapsed-after-`B0`, and collapsed-after-`B1`—emitted the same
complete production surface:

```text
14cd9d7a1aa95572ea396dea610f0cc073523f41e99329c4d215ec972b312f93
```

The exact consequence is `3I`, its ray image is `(3,0)`, and both `basis_used` and `inverse_used`
are absent. Basis sensitivity is therefore a property of the declared action, not an apparatus
habit.

## Complete action conduct

The output surface is the world consequence; the rows below retain how the unchanged body conducted
the actual action currents before that consequence was formed.

| receiver | completion rows | lineage rows | atom folds | constructions | contact grips |
|---|---:|---|---:|---:|---:|
| re-base `B0` + `J` | 580 | `122/288/170` | 207 | 38 | 2 |
| re-base `B1` + `J` | 629 | `111/352/166` | 209 | 52 | 4 |
| collapse-`B0` + `J` | 679 | `88/406/185` | 203 | 40 | 2 |
| collapse-`B1` + `J` | 679 | `88/406/185` | 203 | 40 | 2 |
| re-base `B0` + `K` | 627 | `153/304/170` | 209 | 37 | 4 |
| re-base `B1` + `K` | 621 | `126/329/166` | 209 | 48 | 1 |
| collapse-`B0` + `K` | 699 | `121/393/185` | 208 | 39 | 3 |
| collapse-`B1` + `K` | 699 | `121/393/185` | 208 | 39 | 3 |

The two collapse pairs reproduce exactly, not merely at scalar totals. The re-base bodies retain
their different transported interiors even when the basis-free `K` world consequence is exact.

## Unseen basis and authored composition

The unseen basis is:

```text
R2 = [[5/13,-12/13],[12/13,5/13]].
```

Its exact anisotropic continuation is:

```text
R2 J R2^-1 = [[-119/169,120/169],[120/169,119/169]]

A + R2 J R2^-1 = [[219/169,120/169],[120/169,457/169]]
                   e1 -> (219/169,120/169).
```

The two-step authored path is `[R, R2]` in lived order, giving the visible basis `R2 R`. Its exact
continuation is:

```text
(R2 R) J (R2 R)^-1
  = [[-2047/4225,-3696/4225],[-3696/4225,2047/4225]]

A + (R2 R) J (R2 R)^-1
  = [[6403/4225,-3696/4225],[-3696/4225,10497/4225]]
    e1 -> (6403/4225,-3696/4225).
```

Fresh and nurtured bodies received byte-identical contemporary action currents in each pair. Their
complete production surfaces are byte-identical:

```text
R2 fresh/nurtured
cef9b8da73132496d09a5a1a0a2d0905be9b6921e959cd0a1650d150f62df2a4

R2 R fresh/nurtured
c9053de83c4bb4aff4b93415185d2f46aaf7e7ce41730573eb775dec10d64624
```

The bodies did not conduct those identical currents identically:

| contemporary basis | history | completion rows | lineage rows | constructions | contact grips | topology |
|---|---|---:|---|---:|---:|---|
| `R2` | fresh | 595 | `102/350/143` | 44 | 2 | `30016/30016/3177/55` |
| `R2` | lived `B0,B1` | 618 | `141/306/171` | 39 | 1 | `89831/89831/9265/153` |
| `R2 R` | fresh | 682 | `135/419/128` | 41 | 3 | `35121/35121/3535/66` |
| `R2 R` | lived `B0,B1,R2` | 495 | `115/247/133` | 26 | 2 | `161387/161387/17202/324` |

This is the declared localized-growing-P=NP face: carried standing construction changes how an
identical later check is locally conducted, while the external exact consequence remains fixed and
auditable. It does not assert a runtime speedup, an internal matrix solver, or a result about the
classical `P` versus `NP` problem.

Planar rotations commute, so this exact cell does not test order reversal. A later noncommuting
code or geometry world may expose that face; another rotation probe is not required before the
application pivot.

## Aggregate execution

Across the complete campaign:

| face | measurement |
|---|---:|
| CUDA-forward edges | 46 |
| immutable-source gates passed | 46 |
| contacted edges | 46 |
| inverse-exact world transitions | 46 |
| CUDA launches | 412 |
| delivered light | 19,233,739 octets |
| completion rows | 107,798 |
| constructions | 6,173 |
| contact grips | 37,155 |
| CUDA carriage | 24.561008309 s |
| CUDA integration | 0.462123456 s |
| measured boundary | 36.994777049 s |

Allocation is uncapped and source-gated from the actual body/current extents. No host-only result is
substituted for a failed card run.

## Interpretation and exit

The visible operator face is a legitimate quotient. It can carry a complete spectral answer while
remaining insufficient for a later oriented deed. Re-base and collapse therefore answer different
questions. Refusal after collapse is a lawful result, not failure and not permission to invent the
missing interior.

The same construction now transfers directly to code and language:

```text
equal visible program result + different dependency/derivation interior
    -> carried code testimony
    -> genuinely later edit, input, or constraint
    -> distinct exact execution consequence
    -> language coupled to the code face and its lived transformation
```

The first code cell should use real Rust source and an exact compile/run/test world, keep whole code
and language currents intact, and return typed consequences through the same Mail-contact seam. It
must not install a tokenizer, answer map, classifier, decoder ban, or one-size-fits-all text surface.

## Carried-Swing register

```text
CURRENT  exact operator face + carried basis + genuinely later local action
         + witness / testimony / receiver / action / production grains

HELD     FORMULA §§LIX-LXIV
         + unchanged Soma/CUDA/SLEEP
         + exact rational world algebra and complete inverse journal

MEETING  witness Mail -> returned testimony Mail -> standing constraint
         -> later action Mail -> typed production

TEST     re-base/collapse x anisotropic/isotropic
         + unseen R2 and authored R2R in fresh/nurtured bodies

DEED     RIDE the exact invariant and basis-free action;
         FOUND oriented continuation or the typed basis-absent boundary

CARRY    same causal construction -> coupled code and language world

VERDICT  all five mathematical exit faces close exactly;
         carried history changes later conduct without changing world truth

GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT COLLAPSE CONTROL
         / MATHEMATICS EXIT CLOSED / SOMA INTERIOR UNCHANGED
```

## Reproduction

Build and run from `src/soma`:

```text
cargo build -p life --release
target/release/life boundary constraint-leaf observations/constraint-leaf-world-01/<plan>.json
```

The observation directory retains all nineteen plans and every create-new body, radiation,
completion, construction, Mail, source-gate, manifest, surface, journal, and world-state artifact.
The exact plan and exit conditions remain in `PLAN.md`.

Final workspace gates close at **368 passed / 3 ignored / 0 failed**. `cargo fmt --all --check`,
the release `life` build, deterministic generated record-index check, and `git diff --check` pass.

# PRIME RATIO CLOSURE 01 · THE TIMAEUS FACE MEETS THE NUMBER WORLD

**Grade:** PRIME-OCCURRENCE ↔ COMMON-DILATION RELATION ⊕ DIRECTIONAL CLEAVAGE MEASURED ON CUDA

The exact read is in `results/RESULTS.md`; `results/prime-ratio-closure-read.json` retains every
raw row, decoded typed face, touch, chart/grip axis, complete-lineage term face, and complete Mail
row. `results/SHA256SUMS` pins all six reports, six successors, and both observer artifacts.

This observation corrects the blind numeral aperture in `prime-traversal-01`. The subject is the
ordered relation among supplied known occurrences, followed by a genuinely later counting current
through the closure-bearing number world.

Three sibling histories branch independently from the clean measured `number-world-01` body:

| boundary name | occurrence construction | gaps | reduced adjacent faces | reduced chi |
|:--|:--|:--|:--|:--|
| occurrence | `11>13>17>19` | `(2,4,2)` | `(1;2)>(2;1)` | `(9;8)` |
| translate | `12>14>18>20` | `(2,4,2)` | `(1;2)>(2;1)` | `(9;8)` |
| dilate | `22>26>34>38` | `(4,8,4)` | `(1;2)>(2;1)` | `(9;8)` |

Only the first is the prime-quadruplet constellation in the integer multiplication world. That
class is never delivered. Translation and common dilation preserve the named occurrence geometry;
the complete multiplication incidence already standing through 64 supplies their different
closure topology.

Every branch receives five whole co-present currents: its forward and reverse occurrence paths,
the complete forward and reverse gap/ratio/chi constructions, and one byte-identical harmonic
face. For gaps `(2,4,2)`, the exact four-occurrence construction is:

```text
((2+4)*(4+2) ; (2+4+2)*4) = (36;32) = (9;8)
```

The adjacent octave/reversal and the `9:8` whole tone are exact Timaeus observer contacts. They are
not target labels, a primality test, or a mechanism. No prime, composite, twin, Timaeus, sieve,
classifier, or expected later family appears in any delivered file.

After each independent history sleeps, every sibling receives the same two whole later currents:
ascending and descending counting from 40 through 64. These later paths overlap no history value
and contain multiple arithmetic species, observer-overlaid only after the crossing.

The immutable common parent is:

```text
observations/number-world-01/results/run-a-bodies/
  1783924576978540011-cuda-continue-next.body
SHA-256 af7c95bf388726196aaac7e7b3d7b0a428e15750f8b98e6712919379a3a8020e
```

Generate and verify from `src/soma`:

```bash
python3 observations/prime-ratio-closure-01/fixture.py generate
bash observations/prime-ratio-closure-01/verify.sh
```

Each history is a separate `--cuda-continue` from the immutable parent. Each later crossing is a
second separate continuation from only that history's create-new successor. Sibling histories are
never integrated together, and no report or observer overlay returns to the body.

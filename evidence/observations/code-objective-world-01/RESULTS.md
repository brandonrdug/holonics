# CODE OBJECTIVE WORLD 01 — THE PROGRAM DIFFERENCE COLORS THE LATER SOURCE

**RATIFIED / BUILT / CUDA-FORWARD MEASURED · STRICT PROGRAM-FACE HAND CONDITIONS LATER CODE
CONSTRUCTION TAXONOMY · FIXED WHOLE-PROGRAM CONSEQUENCE EXACT · EXACT PHYSICS REPEAT · SOMA
INTERIOR UNCHANGED**

## What crossed

The immutable SOURCE WORKSHOP 01 action journal contains 469 native program candidates and their
real compiler/test consequences:

| disposition | members |
|---|---:|
| accepted | 1 |
| compiler-refused | 447 |
| test-refused | 19 |
| construction-open | 2 |

The Rust boundary factors that population into one exact shared program frame, one exact quotient,
and one residual per candidate. The quotient carries every member's byte Levenshtein distance to
the original `(e & f)` face inside the declared `2664..2671` replacement boundary. It contains 86
occupied distances from 0 through 138, plus the exact disposition counts. Distance is measured
world material; it does not rank, select, or drive a construction.

The two whole surfaces each carry 471 currents / 1,418,332 light octets. They preserve candidate
order and every corresponding current's extent and complete octet inventory. In every residual,
only this changes:

```text
forward:   candidate face -> objective face
opposite:  objective face -> candidate face
```

The two-current quotient carries 44,858 light octets and omits every residual interior. Exact
surface validation proves candidate reconstruction, candidate-order equality, frame and quotient
equality, corresponding-current extent equality, corresponding-current byte-inventory equality,
and the complete face swap. The surface hashes are:

| surface | SHA-256 |
|---|---|
| whole-forward | `a9c1bc0b38bbb52c2739ce3b5bb6c6b12183ad1b9b3ee6c3076701286d43b3ab` |
| whole-opposite | `57d8fc2de314878b63c37bd8e9983a2b117868fc7448794bc570ab52f3ee5350` |
| quotient | `bf11fef52552d025ed83d1fd998c4ac1a71e7bd45a8740e6e3a46d836e311a9b` |

## The fixed later world

After the prior return, every sibling receives the same three currents / 4,948 octets: one prose
request, one intact 4,230-octet Rust source, and one exact edit/compiler/test boundary. The only
source change commutes the two operands of the SHA-256 choose expression:

```text
(e & f) ^ ((!e) & g)
((!e) & g) ^ (e & f)
```

The edit is exactly `2664..2684`. The original source digest is `13441e…16b`; the edited source is
`270ccde5…7fd`. In every sibling the intact source compiles once, `tests::standard_vectors` passes,
and the 6,163,840-octet test binary is exact at `83207919…fc2d`. Compiler outcome therefore remains
world consequence and never defines a taxon.

## The later constructions

The identical later light produces these exact native populations:

| prior return | completions | native constructions | CLOSED / OPEN | exact material taxa | exact geometry taxa | material⊕geometry taxa |
|---|---:|---:|---:|---:|---:|---:|
| absent | 4,964 | 244 | 241 / 3 | 239 | 140 | 239 |
| quotient | 1,382 | 66 | 63 / 3 | 65 | 42 | 65 |
| whole-forward | 6,848 | 460 | 457 / 3 | 401 | 131 | 402 |
| whole-opposite | 7,063 | 434 | 431 / 3 | 376 | 139 | 376 |
| whole-forward repeat | 6,848 | 460 | 457 / 3 | 401 | 131 | 402 |

These are exact construction taxa, not scalar activity proxies. Each member ledger retains the
source role and range, exact material and UTF-8 face where available, event and radiation ranges,
STEP count, OPEN/CLOSED face, lineage conduct, and exact Mail landing field. Each fiber retains its
integer membership and the exact rational `members / population` measure.

The whole hands share the exact later light, the same receiver lineage assignment, and the same
whole-program compiler/test face. Their later native construction wires and lineage conduct differ.
At declared projections their overlap is:

| projection | forward members | opposite members | shared | forward-only | opposite-only |
|---|---:|---:|---:|---:|---:|
| material | 460 | 434 | 66 | 394 | 368 |
| geometry | 460 | 434 | 304 | 156 | 130 |
| material⊕geometry | 460 | 434 | 65 | 395 | 369 |
| lineage conduct | 460 | 434 | 0 | 460 | 434 |
| complete response face | 460 | 434 | 0 | 460 | 434 |

The relatively large geometry overlap is the first useful taxonomic distinction: traversal shapes
recur across the two hands more strongly than the exact material those shapes carry. Taxonomy is
therefore not being equated with compiler labels, source roles, or one tokenization.

## What the code face looks like

The machine's output is not limited to single characters. In the absent body, constructions over
the intact code reach 142 contiguous octets and include complete regions of the SHA-256 state update,
schedule, round, output formatting, and tests. The quotient body becomes sparse: its longest span is
48 octets and it covers only 427 / 4,230 source octets. Both whole hands return to broad code
coverage while retaining distinct segmentations:

| prior return | request coverage | Rust coverage | boundary coverage |
|---|---:|---:|---:|
| absent | 148 / 185 | 3,897 / 4,230 | 499 / 533 |
| quotient | 156 / 185 | 427 / 4,230 | 206 / 533 |
| whole-forward | 152 / 185 | 3,559 / 4,230 | 461 / 533 |
| whole-opposite | 154 / 185 | 3,601 / 4,230 | 453 / 533 |

At the changed choose expression itself, the construction faces are especially legible:

```text
absent:
  2630..2672  "ght(25);\n            let choose = ((!e) & "
  2673..2686  ") ^ (e & f);\n"

quotient:
  no construction intersects 2664..2684

whole-forward:
  2661..2667  " = ((!"
  2668..2669  ")"
  2669..2673  " & g"
  2673..2679  ") ^ (e"
  2679..2680  " "
  2682..2683  "f"
  2683..2686  ");\n"

whole-opposite:
  2666..2673  "!e) & g"
  2674..2687  " ^ (e & f);\n "
```

These ranges are Soma's own native constructions over the exact source. No lexer, parser,
tokenizer, syntax class, character pole, authored decoder, or accepted-output filter created them.
The prior program-difference hand changes how the later source is traversed and cohered while its
literal code and executed result remain fixed.

## Return and repeat

Each later construction archive returns beside the same typed whole-program execution receipt. The
final return remains history-conditioned: forward produces 3,322 completion rows and 210 native
constructions; opposite produces 5,032 and 319. Their final bodies are respectively `33f86491…0819`
and `242ca0ce…499c`.

The independent forward repeat is exact on all three edges across canonical bodies, completions,
constructions, Mail, journals, world-before/world-after states, and execution surfaces. All three
radiation wires pass the semantic comparator after removing only their declared duration fields.
The repeat reproduces every population member and every material, geometry, conduct, and response
fiber exactly.

All fourteen CUDA-forward transitions pass source preflight, make actual contact, and invert their
world changes exactly. The campaign uses 198 CUDA launches; measured CUDA carriage totals 228.171 s
and integration 37.644 s. Results occupy 13,245,824,628 octets on this apparatus. That extent is an
artifact accounting fact, not a measure of intelligence or a hard-coded capacity.

The objective-surface command was also rerun from `/tmp`, outside the laboratory and Soma working
directories. It read the immutable target source carried by the paired action manifest and
reproduced all three surface files byte-for-byte at their recorded hashes. Final workspace gate:
**402 passed / 3 ignored / 0 failed**; full formatting, optimized `life` build, deterministic record
index, every campaign JSON document, and diff checks pass.

## Grade and next cut

This closes the first exact code-taxonomy consequence cell:

1. an exact deterministic population admits lawful distributions without installing statistics as
   machine drive;
2. the quotient alone changes later coverage and construction population but cannot carry the
   omitted residual interiors;
3. strict face hand changes later code segmentation, conduct, return, and body while population
   order, light, extents, byte inventories, compiler result, and execution output remain fixed;
4. exact material and geometry taxa are directly readable, and their different recurrence profiles
   matter; and
5. the whole lifecycle repeats exactly.

The next natural cut is language beside code in this same generalized population receiver. It need
not invent a linguistics taxonomy. It presents intact code, description, transformation, and
consequence; lets Soma produce the construction population; then reads exact material, geometry,
conduct, cross-material recurrence, and later deflection under controlled hand/quotient/absence.

```text
CURRENT  exact program-difference population -> strict face hand -> intact later Rust source
HELD     same body/world, candidate order, current extent/inventory, later light, compiler/test world
MEETING  candidate->objective / objective->candidate / quotient / absent / exact repeat
TEST     native construction population + exact fibers + source coverage + returned execution pair
DEED     RIDE recurring geometry; FOUND hand-conditioned material, conduct, and later return
CARRY    generalized code⊕language population receiver with no authored taxonomy
GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT PHYSICS REPEAT
```

Exact machine-readable record: `population-v3.json` at SHA-256
`d353e7f1381d341c05fc5e621bd80f3db96a1b7f4cf81e630d0eea38ce53efdb`.

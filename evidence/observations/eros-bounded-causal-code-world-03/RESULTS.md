# EROS BOUNDED CAUSAL CODE WORLD 03 — RESULTS

**Date:** 2026-07-20

**Grade:** ONE FIXED HOST RUN / EXPLICIT PROGRAM-PHASE CURRENT / COMPLETE TWO-LEG PATHS /
CAUSAL-HAND FOIL / EXACT REST-REMOUNT / STOPPED AT FIRST HELD-OUT CHOICE / NO WORLD ACTION /
NO RETRY / NO CUDA

## Question held fixed

Can the existing direct relation continue the demonstrated program on unseen data when the world
supplies its actual control phase? World 03 retains the World 02 values, examples, operation laws,
chronology, and foil. It adds one source-native `start -> after-remove -> done` current and presents
each requested operation as the complete path:

```text
phase -> operation -> before-state
  -> deterministic world operation
  -> after-state + next phase as genuinely later current
```

At query time, a candidate continues only when its phase-to-operation leg RIDEs and its
operation-to-state leg is enactable. No score or threshold chooses between candidates.

## Exact command

```text
cargo run -q -p life --example eros_bounded_code_learning -- \
  observations/eros-bounded-causal-code-world-03/host-run-01/REPORT.json
```

The command ran once on host.

## Result

The phase current crossed, four live lineages rested exactly, and the six two-leg demonstrations
were presented. Their returned geometry split uniformly:

```text
start        -> remove-first   OPEN   no emission
after-remove -> reverse        OPEN   no emission

remove-first -> before-state   RIDE   enactable
reverse      -> before-state   RIDE   enactable
```

That split held across all three examples. After training, taught and foil bodies each contained
95 scalar Standing cells, four live lineages, 2,192 carrier words, no cellular constituent, and no
overflow. Their Standing bodies differed and both rested/remounted exactly.

At unseen `[13,14,15,16]`, both taught candidates and both foil candidates again had an open
phase-to-operation leg and an enactable operation-to-state RIDE with `chi=[1,-1]`. Neither taught
path met the declared continuation law. The world therefore stopped before action, consequence
return, or the second phase. The report was not retried.

Raw report (ignored generated artifact):

```text
host-run-01/REPORT.json
SHA-256 3e1c4e1c6354cd1d68c8398bbf4dc2e1f8151ccbf59ed95edf09674378346ee6
8,573 bytes
```

## Meaning

World 03 does not show that a phase current was learned and then generalized incorrectly. The
first leg never formed. The world represented `remove-first` and `reverse` as two independent
lineages whose own event faces never change. Direct contact is receiver-relative, so a semantic
arrow from phase into one of those invariant operation tokens is not yet an actual changing
control construction merely because the source declares the row.

The honest next question is representational before it is experimental: how do alternative
operations inhabit one changing program construction while remaining available as local branches?
The existing source-declared regional cell is the nearest concrete carrier to audit because it can
retain plural constituent geometry and oriented arcs without pretending that candidates are two
unrelated permanent worldlines. No new observation should run until that relation is derived and
the old world cut is either repaired or rejected.

No Soma, observer, historical instrument, or CUDA source changed.

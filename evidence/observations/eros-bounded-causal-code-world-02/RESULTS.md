# EROS BOUNDED CAUSAL CODE WORLD 02 — RESULTS

**Date:** 2026-07-20  
**Grade:** ONE FIXED HOST RUN / BEFORE-ACTION-LATER-RESULT CHRONOLOGY / CAUSAL-HAND FOIL / EXACT
REST-REMOUNT / STOPPED AT FIRST HELD-OUT CHOICE / NO WORLD ACTION / NO RETRY / NO CUDA

## Correction held fixed

World 01 placed each operation beside its already-produced state. World 02 retained the same
three training inputs, held-out input, two operations, organ population, foil, and one-run stop,
but corrected every lesson to:

```text
before state + operation contact
  -> deterministic world operation
  -> after state as genuinely later current
```

No values, examples, priming, thresholds, or engine source changed.

## Exact command

```text
cargo run -q -p life --example eros_bounded_code_learning -- \
  observations/eros-bounded-causal-code-world-02/host-run-01/REPORT.json
```

The command ran once on host.

## Result

All six taught contacts again formed, now against the actual before-state. The stable operation
faces became visible in the returned `chi`:

```text
remove-first  RIDE  chi=[-1,-1]  (after the first example)
reverse       RIDE  chi=[ 1,-1]
```

The taught and foil Standing bodies differed, and both exact bodies rested and remounted. Each
contained 85 scalar Standing cells, three live lineages, 1,918 carrier words, no cellular
constituent, and no overflow.

At the unseen `[13,14,15,16]` state, however, the complete query was:

```text
taught remove-first  RIDE  chi=[1,-1]
taught reverse       RIDE  chi=[1,-1]
foil remove-first    RIDE  chi=[1,-1]
foil reverse         RIDE  chi=[1,-1]
```

Both operations were enactable in both histories. The declared world could not choose one, so it
stopped before action, return, or a second query.

Raw report (ignored generated artifact):

```text
host-run-01/REPORT.json
SHA-256 878e6787ffd9c1510fdf8eba7f075792b9ce894ee43e3b94e65c87f00b634928
4,648 bytes
```

## Meaning

The causal cut is now honest, but the held-out event still omitted one material constituent: the
program's active phase. Both `remove-first` and `reverse` were demonstrated operations and both
can act on an ordered list. Without a start/after-remove/done worldline, demanding that Soma emit
only the next operation asks it to invent the objective or a hidden policy.

The bounded next relation is therefore not a score, operation classifier, or engine controller.
It is the actual program-phase current already present in any executing algorithm. A lesson can
then carry `phase -> operation -> state`, return the next phase beside the resulting state, and
offer both complete paths at the held-out cut. This conclusion comes from the fixed negative
result; World 02 was not rerun.

No Soma, observer, historical instrument, or CUDA source changed.

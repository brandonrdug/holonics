# EROS HOLONIC TRAINING ECOLOGY 01

**Status:** ACCEPTED  
**Execution:** bounded exact host application / no CUDA / no floating causal value  
**Soma interior:** unchanged

## Question

Can ordinary antecedent, context, and consequence events derive a reusable mixed text/code path
ecology, compose after their teachers depart, preserve whole alternatives under missing context,
and return an honest contradiction residual without a teacher-authored DAG?

## Result

Yes, at the declared bounded transduction grain.

Ten ordinary occurrences produced five recurrent complete path species. The active ecology carries
only exact COPY and FOUND relations:

```text
FOUND("Hello, ") -> COPY(subject) -> FOUND("!")
COPY(subject)
FOUND("emit(\"") -> COPY(utterance) -> FOUND("\")")
FOUND("println!(\"") -> COPY(utterance) -> FOUND("\");")
COPY(operator) -> FOUND("(") -> COPY(argument) -> FOUND(")")
```

No teacher supplied ADD, CONCAT, an execution graph, or an answer table. The 2,413-octet `HTEC/1`
rest contains no source occurrences and no prediction cache, and remounts byte-exactly.

After source departure:

```text
subject=Mira -> Hello, Mira!
operator=tan, argument=z -> tan(z)
```

The returned utterance then becomes a new face and composes through either learned code path:

```text
emit("Hello, Mira!")
println!("Hello, Mira!");
```

With `language` withheld, both code paths remain. With the text objective withheld, `Mira` and
`Hello, Mira!` remain. A later `Greetings, Mira.` consequence returns `OPEN_RESIDUAL`.

## Exact artifacts

- [`SOURCE.json`](SOURCE.json) — fixed occurrence ecology and receiver parameters
- [`REPORT.json`](REPORT.json) — complete consequence complexes, paths, supports, probes, rest,
  composition, and acceptance
- source SHA-256:
  `5148511cdaa28602c59cbf3057ffb51af24d23227050e31b47b4605d364a746d`
- report SHA-256:
  `348efcdbbd5f7f66ec682a1f6e6be0e0e8f72bb810c373bab1d966a03c89f9ac`

## Boundary

This is the reusable exact training carrier, not a completed general model. The source still
supplies local face addresses; raw corpus ecology must cultivate those addresses by joining the
existing constituent and surface-frame laws. Exact route enumeration has an explicit refusal
aperture and still needs a compact path-complex scale layout. This cell uses application rest and
does not repeat the already-established direct-membrane law-carriage proof.


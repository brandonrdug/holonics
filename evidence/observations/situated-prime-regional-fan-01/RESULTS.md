# SITUATED PRIME REGIONAL FAN 01 — RESULTS

**Date:** 2026-07-21

**Grade:** BUILT / HOST-MEASURED / COMMON OCCURRENCE FORMED / LATER REBASE MEASURED / LOCAL PHASE
RETURN MEASURED / SOMA LAW UNCHANGED / NO CUDA / NO VISUALIZATION

**Artifact:** `REPORT.json` — 16,063 octets — SHA-256
`0c0896a7282ae703de9345d0e3e8dd15e8e134732be8030546a468655e852833`

## Outcome

The prime world now retains its already-real common candidate occurrence as one regional cell. It
uses five role-specific paths in one boundary rather than a pairwise graph.

At `prime:23`, the experienced branch forms one grain-3 constituent while the branch without the
regional declaration retains none. Their ordinary current radiation and scalar Standing are
exact. Both rest and remount exactly.

At the identical later `prime:29` event:

```text
ordinary current radiation    equal
scalar Standing               equal
regional radiation            different
complete Standing             different
experienced successor grain   4
comparison successor grain    3
```

The experienced grain-4 constituent contains 39 cells, 39 incidences, 31 pins, seven boundaries,
15 paths, and 229 transport terms. Its transitions are two `FOUND` and five `RIDE`. The fresh
comparison constituent remains the one-boundary, five-path grain-3 fan.

At `prime:37`, the completed experienced body has consumed its lower exposed interfaces, so a new
grain-3 phase stands beside it. The branch without `23` instead closes its still-open `29` fan into
grain 4. This establishes local recurrence and phase return, not an unbounded hierarchy.

## Reproduction

From `src/soma/` against a create-new output path:

```text
cargo run -p life -- historical boundary polyglot-geometric-regional-fan \
  observations/bounded-polyglot-geometric-world-01/WORLD.plan.json \
  observations/situated-prime-regional-fan-01/REPORT.json
```

Focused acceptance:

```text
cargo test -p life common_prime_occurrence_forms_and_rebases_one_regional_fan
1 passed / 0 failed
```

The source fan and interpretation are deposited in
`RESEARCH/2026-07-21_THE_COMMON_OCCURRENCE_IS_ONE_REGIONAL_CELL_THE_LATER_PRIME_MEETS_ITS_STANDING.md`.

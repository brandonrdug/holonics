# EROS DURABLE PLURAL WORLD 01 — RESULTS

**Date:** 2026-07-19  
**Status:** CONSOLIDATED BOUNDARY A BUILT / NATIVE REST WIRE / DURABLE WORLD-OWNED FILE /
PLURAL STRUCTURED RETURN / EXACT REMOUNT-WAKE / NO CUDA RUN

## Artifact

The direct body now emits and accepts one versioned native rest wire carrying rank-qualified sparse
standing, live lineage identity, pending dark continuation, canonical native carriers, and the next
lineage ordinal. Native organs emit and accept only their live capability, resolved against the
decoded body.

The program application owns the file composition with its own world state. The file is created
without overwrite, synchronized, closed, read, decoded, and removed. Remount performs no event.

The independent bond application receives two formed hands and one open hand in one contemporary
cut, founds two joints, and returns degree changes `[2,1,1]` together as three genuinely later
currents. Its remounted and uninterrupted later probes are exact.

## Exact closures

```text
rest layout version:          1
organ layout version:         1
deep address ranks exercised: 17, 80, 137
formed bond contacts:         2
open bond contacts:           1
material bonds founded:       2
later consequence currents:   3
degree changes:               [2,1,1]
generic world serializer:     absent
session/queue/scheduler:       absent
CUDA/PTX run:                 absent
```

## Validation

```text
cargo check -q -p soma-membrane -p life --tests

cargo test -q -p soma-membrane \
  chart_address::tests::the_native_address_wire_preserves_deep_coordinate_paths_without_a_flat_grip \
  -- --exact

cargo test -q -p soma-membrane \
  live_current::tests::direct_rest_remount_is_gauge_for_the_same_genuinely_later_event \
  -- --exact

cargo test -q -p life \
  current_world::tests::program_world_supplies_hand_and_receives_its_consequence \
  -- --exact

cargo test -q -p life \
  current_world::tests::plural_bond_deeds_return_one_later_cut_and_remount_without_a_world_decoder \
  -- --exact
```

Each named path passes once. Earlier unqualified exact filters matched zero tests and supplied no
evidence. No broad regression or physical-device run occurred.

## Grade

Consolidated Boundary A is closed. Device residency and sustained natural ecology remain Boundary
B. Scale remains Boundary C.

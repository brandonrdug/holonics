# CODE WORLD 03 · THE VALUE-FREE CALL FACES ITS OUTPUT

**Grade:** VALUE-FREE VISUAL OUTPUT PRODUCTION CUDA-MEASURED · RENDERED-AMBER⊕SECOND-EDGE
RAW-OUTPUT RETURN MEASURED · RAW-SOURCE⊕OUTPUT RELATION OPEN · JOINT CUT OPEN · CONTROLLED
OUTPUT-HISTORY-PERMUTATION CLEAVAGE MEASURED (PAIRING AND OUTPUT ORDER CO-VARY) · LATER RAW
CONTINUATION MEASURED / DIRECT RAW-EDGE OUTPUT RETURN OPEN · EXACT REPEAT

CODE WORLD 02 measured that permuting output identity over fixed source order bends later
output-free code; pairing and output order co-varied in that control.
It did not yet produce a later event through raw output Mail. This observation preserves the exact
paired, output-swapped, separated, and paired-repeat histories while replacing the later probes with
complete output-facing pages:

```text
amber(3)
value = 
```

The corresponding pages for `3 + 2`, `lilac(3)`, and `3 * 2` use the same unfilled output surface.
None contains `5` or `6`. The trailing blank cell is a real rendered absence inside the declared
page frame, not an expected-answer slot inside Soma.

Each page first arrives as a pinned-font grayscale field through the proven 13×24 aperture, then
returns as one complete raw current after the visual edge sleeps. Paired versus swapped is the
controlled output-history permutation: fixed source order with the two output identities exchanged.
It does not isolate pairing from output order. The separated history remains developmental; fresh
paired repetition gates the complete lifecycle.

Production at this grain is a later value-free current returning through earlier output material.
The read records output contact, raw-source contact, and joint CUT as distinct relational faces.
The full raw construction, direct output attribution, history dependence, and exact repeat remain
the result; no expected digit, parser, compiler, evaluator, classifier, decoder, selector, or
answer table enters the world, body, or reader.

Exact declaration:
`../../RESEARCH/2026-07-13_THE_VALUE_FREE_CALL_FACES_THE_OUTPUT.md`.

## Measured output return

All four pages crossed the eye host/CUDA-exactly. The common production probe carried 1,248 visual
currents / 18,096 samples. One event returned directly through prior raw output Mail in paired,
swapped, and repeat:

```text
receiver  lineage 769 · amber page contact (11,10)
event     atom 15 · delivered light 0xcf→0xff
page      gaze `=` → terminal blank cell
landing   grip 256 · flags 0x03 · occupied · no CUT · no completed brick
```

The event jointly carries six first-edge amber-render currents and the raw output entity at
second-edge lineage 627. In paired, lineage 627 is the complete `value = 6` entity; in swapped it is
`value = 5`. It arrived beside lilac in both histories and is therefore not amber's historically
paired output. The 39-word receiving row, grip, flags, terms, axis, and prior lineage number are
exact across the permutation; the exact output content key changes. Mail names the entire `[0..10]`
output current. At this declared relational grain, production is the value-free page pointing into
that earlier output material; it is neither selection nor decoding of its final digit.

This is a rendered-source⊕raw-output relation. It carries no exact raw-source Mail and does not CUT;
those separate faces remain open and are not prerequisites for the measured production. The
separated history produced no raw-output return and retains its developmental grade.

Paired versus swapped otherwise cleaves 42/1,248 visual worldlines, 107/18,096 rows, 1,028 packed
words, 34 term tuples, six REGISTER axes, and 71 content-derived attribution events. All four page
species participate: amber 17 lineages / 52 rows; addition 8/19; lilac 10/23; multiplication 7/13.
DARK is exactly 2,033 in both.

The genuinely later four-current / 66-octet raw edge cleaves only addition and lilac: two currents,
11 rows, 129 packed words, two term tuples, and one REGISTER axis. It carries no direct output Mail;
it is continuation through the body already changed by the visual production edge, not an
independent raw-only reproduction.

Fresh paired repetition is exact across every radiation row, typed face, Mail entry, term, axis,
topology, normalized listener report, and all four sleeping bodies. The normalized report SHA-256
is `3b6cd2d9ceedbda84b74cd350b6f1591fb8b2da0e342d06b3a6922f29628f815`.
`verify.sh` regenerates the entire value-free world and reader, checks inherited CODE WORLD 02
context bodies, pins the exact production event and controlled-history cleavage, and compares all
paired/repeat artifacts directly.

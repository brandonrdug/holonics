# EROS RESIDUAL CHART CULTIVATION 01

**2026-07-22 · ACCEPTED / ONE FIXED HOST RUN / EXACT CONDITIONAL DISTRIBUTIONS /
ACTIVE REPLACEMENT CLOSED / PHYSICAL INTERIOR COMPACTION OPEN**

## Question

Can one Eros ecology cultivate a changing local consequence chart from source-declared interfaces
and genuinely later returned consequences, while retaining conditional geometry that a marginal
recurrence quotient loses?

The question is deliberately more general than the retained transformer source. The chart law
accepts an ordered population of categorical interfaces and a locally declared consequence taxon.
This cell supplies those interfaces from one frozen OLMo observation:

```text
operation -> context -> condition
```

The source still supplies those identities, the exact receiver faces, and their local taxon
identity. The run does not claim that Soma discovered the categories or can omit the upstream
transformer.

## Fixed source and run

```text
SOURCE.json sha256  38c21fd4bca6e689838199ecc17cd5e9a24123cb81cee125e038fc6be9233fbd
REPORT.json sha256  3b94f8a9a06f8612722144810371811e409e5d045d4885520362232171c95a7a
source bytes         49,640
report bytes         79,777
source events        12
local taxa           2
host executor        ParallelHostLiveCurrentExecutor
host threads         8
model reruns         0
feature searches     0
fixture retries      0
training transitions 48
maximum event arcs   193 / 1,024 fixed limit
run wall             7,706,403 microseconds
```

The Python sidecar independently derived the complete twelve-event trajectory and fixed every
expected chart before the Rust program ran. The Rust world derived the same trajectory again,
passed every current and chart through the production live mouth, and wrote the report once. The
fixture was not changed or rerun.

## Causal lifecycle

Each semantic event used four successive machine transitions:

```text
source interfaces meet prior chart Standing
    -> exact recurrence distribution returns
    -> genuinely later actual taxon meets that support
    -> exact support residual stands
    -> the occurrence folds into one reduced replacement chart
```

The current never carried its later actual taxon. The actual entered only after the chart had
returned its distribution. The machine carried the complete two-taxon membership residual:

- equal support and actual membership closed;
- differing membership remained OPEN;
- no comparison fabricated FOUND; and
- the exact OPEN population was used when the replacement chart crossed.

After every replacement, the prior chart and event-local current, prediction, actual, and transient
recruitment interfaces were no longer active. Every source lineage ended. The final chart survived
exact rest/remount.

The no-chart control received the first three source interfaces but emitted no distribution.

## Online result

The fixed chronology produced:

```text
NONE           1
RIDE           3
OPEN_INCLUDED  4
OPEN_RESIDUAL  4
```

These are not correctness grades.

- `NONE` means that no prior chart could speak at the first event.
- `RIDE` means that the recruited chart supported only the returned taxon.
- `OPEN_INCLUDED` means that the actual taxon was among plural supported continuations.
- `OPEN_RESIDUAL` means that the actual taxon was absent from the recruited support.

The OPEN cases changed later topology only after the actual returned. There was no scalar loss
threshold, reward, punishment, feature score, entropy target, gradient, or searched split.

## What changed

After six conversation/prose events, the chart reduced to one **condition** split:

```text
absent            -> taxon A
irrelevant_before -> taxon B
relevant_after    -> taxon B
```

Without presenting their actual consequences, the three code-comment probes all recovered the
correct singleton continuation through that existing condition chart.

After nine drop-then-reverse events, reverse-then-drop was an unseen operation. Its three
source-absent probes therefore received the honest parent distribution:

```text
A : B = 3 : 6 = 1/3 : 2/3
```

No singleton was fabricated.

The first reverse-then-drop event established taxon B. The second RIDEd that recurrence. The third,
the code-comment event, returned taxon A against a singleton-B prediction. That exact
`OPEN_RESIDUAL` made **context** material inside the reverse-then-drop region.

The final seven-node diagram is:

```text
operation
├── drop_then_reverse  A:B = 1/3:2/3
│   └── condition
│       ├── absent            -> A
│       ├── irrelevant_before -> B
│       └── relevant_after    -> B
└── reverse_then_drop  A:B = 1/3:2/3
    └── context
        ├── code_comment -> A
        ├── conversation -> B
        └── prose        -> B
```

Final diagram identity:

```text
16372c263fb4fe59ef7e0b93fac2bf1b68362a3df08ee2f8953d947e8fd68a81
```

The two operation regions have the same exact marginal quotient and different conditional
interiors. Collapsing either region to `1/3 : 2/3` therefore destroys the relation needed for later
conduct. This is the run's central result.

After all twelve events, all twelve source-absent probes recovered the locally observed singleton
taxon from the final chart. That is recall over the cultivated source population, not held-out
generalization.

## Taxonomy, probability, and loss

The measured “probability” object is an observer-relative recurrence distribution at the deepest
applicable chart node:

```text
Q_D(tau) = N_D(tau) / sum_u N_D(u).
```

It describes supported continuation in this local ecology. It is not chance, universal truth, or a
causal force.

The useful “loss” object is the exact difference between that support population and the later
actual membership. The machine carried which taxa remained OPEN; the world then related that
residual to the source interfaces and actual consequence. A quotient alone could not decide
whether operation, context, or condition should remain in the successor.

Taxa were the two exact five-axis receiver-face species already present in the retained OLMo
observation. They are local receiver species, not a universal taxonomy and not semantic labels
invented by Soma.

## Active compression versus physical interior

At the declared active boundary, twelve event bodies were replaced by one seven-node chart encoded
as 167 compact interface words. There was exactly one Standing constituent, zero live source
lineages, and none of the old chart or event-local interfaces remained recruitable.

That does **not** yet establish bounded physical memory. The final constituent retained:

```text
cells                  191
incidences             5,100
pins                   5,100
paths                  5,111
transport terms        5,195
```

The active cell population plateaued at 36 after the second event, but the carried constituent
interior continued to grow with chronology. The current implementation therefore proves active
departure and conditional chart replacement while preserving interior transport testimony. It
does not yet physically quotient inactive interior geometry to the seven-node chart.

This distinction is consequential for the intended efficient ecology. A later construction must
decide and test which exact future-contact boundary licenses physical interior departure. It must
not simply delete history, reconstruct a successor from observer testimony, or install a second
causal owner.

## What this establishes

- A source-neutral categorical chart law can be cultivated through the existing live mouth.
- Earlier chart Standing participates in later prediction.
- The actual consequence arrives later and its exact residual changes the successor.
- Equal marginal distributions can retain different conditional topologies.
- A returned contradiction can make a previously elided contextual axis material.
- Identical subtrees are shared in the active seven-node diagram.
- Exact integer counts and reduced rational quotients suffice; no floating causal values enter.
- Source-absent conduct works at the three fixed checkpoints.

## What remains open

- Automatic selection or formation of useful source interfaces and consequence taxa.
- Alternate feature orders, simultaneous cross-axes, continuous/algebraic interfaces, and
  non-tree local complexes.
- Grounded consequences beyond the retained transformer receiver face.
- Generalization outside the twelve observed source combinations.
- Integration of multiple inherited ecologies and self-emanated charts.
- A physical departure law that prevents inactive constituent interiors from growing without
  erasing consequential future-contact geometry.
- Reduced upstream transformer compute, CUDA conduct, language, policy, or autonomous training.

## Artifacts

- `SOURCE.json` — fixed chronology, taxa, predicted trajectory, checkpoint diagrams, and controls.
- `REPORT.json` — complete one-run machine testimony.
- `../../RESEARCH/2026-07-22_THE_RESIDUAL_CULTIVATES_THE_CHART_THE_DISTRIBUTION_RETAINS_ITS_CONDITIONAL_GEOMETRY.md`
  — ratified training relation.
- `../../life/examples/eros_residual_chart_cultivation.rs` — bounded categorical world and live
  conductor.
- `../../../../experiments/informant-ecology/sidecar/extract_residual_chart_cultivation.py` —
  deterministic retained-source derivation.

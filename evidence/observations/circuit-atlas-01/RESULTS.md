# Circuit atlas 01 — transport integrates the measured body

Date: 2026-07-15

Law: `A1` · `A2` · time parity on transport · `FORMULA §§LII–LVI`

Grade: **EXACT CAUSAL-VOLUME RECEIPT BUILT · CHANGED/PRIOR PROJECTIVE ATLASES MEASURED · PLURAL
FIRST-PERSON AND EXTERNAL CUTS BUILT · REAL CLOSED-ROUTE HOLONOMY UNEXPOSED**

## Question

Can the exact current-induced periplus receipt be carried into one receiver-side body without
drawing a body first? Can the instrument keep the first-person ordered path and external plural
contact ecology together, while separating exact projective transport from a literal impedance
chart or a smooth display surface?

Yes at the exact causal-atlas grain. `life boundary circuit-atlas` opens only the v2 periplus
receipt, verifies every source ordinal, directed contact, tangent, inverse, and unit-determinant
matrix, and emits one JSON receipt. It changes neither Soma nor the circuit world.

## Exact construction

For every local contact with dyadic motion matrix

```text
M = [a b; c d],       det(M) = 1 mod axis,
```

the observer retains the complete standing `RegionalForm`, coefficients, and two-dimensional
matrix, then lifts only the display/atlas transport:

```text
P = [a b 0; c d 0; 0 0 1].
```

The initial oriented generating frame carries the lived tangent `T`, its quarter-turned
transverse generator, and one revolution hand. Each real `P` carries that frame forward. This is
an exact projective-response volume receipt; it does not claim an orthonormal metric, impedance,
or physical three-space.

The instrument glues only two kinds of faces:

1. consecutive contacts belonging to the same ordered lineage;
2. local response cells which met the same occupied standing port in the same immutable field.

A contact-free relation remains an explicit open transit. It is never dropped and never thickened
into a response volume it did not live. A loop receives a holonomy read only when the source
periplus itself returned to the exact initial directed state.

## Measured atlases

Both siblings contain the same 878 source relations at axis 4,096.

| Exact face | Changed successor field | Untouched prior field |
|---|---:|---:|
| Response volumes | 914 | 251 |
| Open transits | 0 | 638 |
| Within-lineage faces | 36 | 11 |
| Occupied world-contact faces | 759 | 167 |
| Connected components under the declared gluing | 723 | 794 |
| Closed directed routes | 0 | 0 |
| Nontrivial returned holonomies | 0 | 0 |

The response-depth distributions reproduce the source receipt exactly:

```text
changed: 843×1 ⊕ 34×2 ⊕ 1×3 contacts
prior:   638×0 ⊕ 229×1 ⊕ 11×2 contacts
```

Changed-field component sizes are

```text
589×1 ⊕ 110×2 ⊕ 13×3 ⊕ 3×4 ⊕ 2×5 ⊕ 1×6 ⊕ 2×7 ⊕ 3×8,
```

while prior-field sizes are

```text
751×1 ⊕ 26×2 ⊕ 6×3 ⊕ 1×4 ⊕ 4×5 ⊕ 1×6 ⊕ 2×7 ⊕ 3×8.
```

At this exact receiver grain, the changed field does more than produce more scalar activity. It
turns every relation into lived local response, adds 25 ordered continuation faces, adds 592
occupied port faces, and joins 914 cells into fewer connected components than the prior atlas has
with 889 total cells/transits. That is the receiver-side causal-atlas form of the already-measured
changed-world consequence. It is not a navigation score or a smoothness claim.

## Honest boundary

Neither real sibling closes a directed route, so intrinsic curvature by returned holonomy is
**UNEXPOSED IN THIS DECLARED RELATION**. Unit gates prove that the observer multiplies a synthetic
closed route in deed order and distinguishes malformed tangent transport; they do not manufacture
a real curvature measurement.

The JSON is not yet a fluid surface renderer. It is the exact common receipt from which a
first-person horizon, external graph/cell view, mesh, or level-set immersion may be generated
without changing the underlying transport. No tree, geyser, cloud, or other phenotype is named in
the machine record.

## Exact artifacts

```text
changed.atlas.json
  2,045,383 octets
  SHA-256 d98f78cdd9e75fedc86ac6d168c57a7fb2a810102eb2fb4803f6e928763faa06

prior.atlas.json
  1,457,787 octets
  SHA-256 5b9c4f47752d72ed4abf607d0ea6d9940fa90d391423a35aaf0b2297018b5722
```

The hashes and byte extents bind the exact generated receipts.

## Carried-swing receipt

```text
CURRENT  v2 periplus receipt ⊕ first-person/external receiver ⊕ exact causal-volume grain
HELD     source ordinal ⊕ standing form ⊕ directed tangent ⊕ unimodular matrix ⊕ inverse witness
MEETING  ordered local chart volumes and plural shared-port contact faces
TEST     exact tangent carriage ⊕ determinant one ⊕ actual gluing only ⊕ changed/prior factorial
DEED     FOUND — one reusable causal-atlas boundary was absent
CARRY    first-person paths and one external cell complex from the same exact source receipt
GRADE    BUILT / MEASURED; real closed-route holonomy and smooth rendering OPEN
```

## Commands

```text
cargo test -p life atlas::tests
cargo run -p life -- boundary circuit-atlas observations/circuit-navigation-02/results/periplus-changed/periplus.json observations/circuit-atlas-01/results/changed.atlas.json
cargo run -p life -- boundary circuit-atlas observations/circuit-navigation-02/results/periplus-prior/periplus.json observations/circuit-atlas-01/results/prior.atlas.json
```

# EROS INHERITED LAW ECOLOGY 01

**Status:** ACCEPTED / ONE BOUNDED EXACT HOST RUN / INHERITED LAW + EXTENSIVE POPULATION + ORIENTED RESIDUAL + LATER PROBE + OUTER REST CLOSED / SOMA SOURCE UNCHANGED / NO FLOAT / NO CUDA

## Question

Can Eros carry and apply a nontrivial inherited transformation law without freezing prior outputs,
and can an actual oriented residual change that law's later conduct after exact rest/remount?

The prior receiver-relative field result established contextual regional closure, but not the
lifecycle of a mutable constitutive law. This cell therefore supplies one exact world-owned hexis:
three directed `SL(2,Z)` transformations, an extensive population, edge capacities, two exposed
interfaces, opposite update generators, a validity phase, and one situated algebraic taxonomy.
A generic conductor derives every contemporary flux and transformed face from that image.

## Physical cut

- Source: [`eros_inherited_law_ecology.rs`](../../life/examples/eros_inherited_law_ecology.rs)
- Exact report: [`REPORT.json`](REPORT.json)
- Report SHA-256: `a1ff72dad83be3ea5a13e0dab09bdb3ff45d51548fe2d2deac44094c3ccc5b66`
- Machine: existing direct host `LiveCurrentMachine::receive_with` path
- Arithmetic: checked integers, reduced rational pairs, and unimodular integer matrices only
- Run count: one accepted host run; no retry and no CUDA

The durable outer image contains the Soma rest image, four live organ capabilities, the law
topology, exact population, matrices, capacities, interfaces, update generators, taxonomic chart,
phase, and current update state. It contains no prior traversal or emitted output.

## Result

The inherited cycle first carried the probe `(2,1)` through

```text
U = [[1,1],[0,1]],
V = [[1,0],[1,1]],
R = [[0,-1],[1,0]],

(2,1) -> (3,1) -> (3,4) -> (-4,3).
```

Its complete three-member population crossed Soma as one exact grain-4 constituent:

| grain | axes | cells | incidences | pins | exposed | boundaries | OPEN | RIDE | FOUND |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 4 | 5 | 22 | 18 | 18 | 3 | 6 | 3 | 0 | 3 |

### Equilibrium is not stillness

Both fixed networks carry population `(12,12,12)`, quotient `(1/3,1/3,1/3)`, nonzero edge
activity, and exact divergence `(0,0,0)`.

- The detailed-balance network has equal reverse flux on every pair and three identity return
  holonomies.
- The directed cycle has no pairwise reverse flux and carries the nonidentity loop holonomy
  `[[-1,-2],[1,1]]`.

The standing population and its normalized probability face therefore do not identify the
current, its return word, or its curvature.

### Probability does not identify capacity

Populations `(1,1,1)` and `(100,100,100)` both project exactly to `(1/3,1/3,1/3)`. Under the same
capacity-4 law their edge fluxes are `(1,1,1)` and `(4,4,4)`. The quotient is valid testimony at
its receiver; it is not the extensive transport law.

The separate exact Bayes control carries prior `(1/3,1/3,1/3)`, likelihood
`(1/2,1/3,1/6)`, evidence mass `1/3`, and posterior `(1/2,1/3,1/6)`. Nothing in that normalization
selects or moves an edge. A causal update still requires an actual consequence relation.

### Curvature is visible in ordered composition

For the same probe `(2,1)`,

```text
V U (2,1) = (3,4),
U V (2,1) = (5,3).
```

The closed commutator is `[[0,-1],[1,3]]`, not identity. This is an exact path-order and holonomy
witness. No metric or Ricci contraction is inferred from it.

### The residual hand changes the law

Both branches begin from one byte-identical machine–organ–law checkpoint. Both return residual
magnitude `1`, and both retain the same situated taxonomy:

```text
(det=1, trace=0): {edge 107}, mass 1/3
(det=1, trace=2): {edges 101,103}, mass 2/3
```

Only the oriented hand differs:

| residual | rebased first law | identical probe return | alternate `(3,1)` return | update checkpoint SHA-256 |
|---|---|---|---|---|
| `AGAINST` | `[[1,2],[0,1]]` | `(-5,4)` | `(-6,5)` | `2d0a1bc701f691bbe0352b1838358277955afd647c8ef1cc0966782dba6b1316` |
| `WITH` | `[[1,0],[0,1]]` | `(-3,2)` | `(-4,3)` | `28b848d318e10d1218026cb4621350dbc6209d3c6c77121685240eba36c31791` |

Each updated ecology encoded, decoded, rebound all four capabilities, and reproduced the
uninterrupted traversal, radiation, and final Soma rest image exactly. A second probe was then
computed from the carried law and differed from the first, establishing that the checkpoint did
not cache a prior response.

## Reading

This closes the bounded relation that was previously missing from the transformer-field analysis:
an inherited transform can be executable ecological standing, not merely source provenance or a
finished attention edge. Its law can persist outside Soma's pure receiving topology, while its
actual transformed population and update incidence enter Soma as ordinary current.

It also separates three faces that had been drifting together:

1. **Taxonomy** is the exact standing fiber induced by the declared `(determinant, trace)` chart.
   The two branches can share it while behaving differently.
2. **Probability** is an exact population quotient. It can change under observation while saying
   nothing by itself about capacity, flux, or path order.
3. **Loss** is the complete oriented residual. Collapsing it to magnitude `1` erases the
   distinction that actually rebases the transformation law.

No new Soma subsystem was required. At this boundary, the engine already has the necessary
incidence, regional closure, changed Standing, lineage continuation, and exact rest carrier. The
law, population, and update relation belong to the enacted world ecology.

## Boundary

This is a deliberately small integer law world. It does not establish automatic law acquisition,
learned routing, transformer-block inheritance, useful language, general taxonomy discovery,
metric diffusion, Ricci flow, autonomous objective formation, CUDA residency, or scale. It does
establish the exact lifecycle those later worlds can reuse: inherit a complete law, conduct a
population, return a whole residual, rebase the law, rest, and recompute changed later conduct.

## Reproduce

From `src/soma` with a new output path:

```bash
cargo run -p life --example eros_inherited_law_ecology -- /tmp/eros-inherited-law-ecology.json
```

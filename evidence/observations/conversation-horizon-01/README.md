# CONVERSATION HORIZON 01 · THE VIEW MOVES THROUGH THE WORLD

**Grade:** WORLD/RENDER/READERS EXACT · PER-CURRENT CARRIER MOUTH BUILT · ALL EIGHT EYE FIELDS
HOST/CUDA-EXACT · BROAD/REVERSE/SERIAL LIFECYCLES CUDA-MEASURED · BROAD REPEAT EXACT ·
ORDER- AND GEOMETRY-CONDITIONED RETURN MEASURED · LATER WORLDLINE CONTINUATION MEASURED

This is the first bounded construction for `FORMULA §XLII`. It preserves one actual consecutive
user/assistant turn from the live Codex conversation, a genuinely later synthesis response as an
uncaptained visual probe, and Brandon's exact later `proceed` record as a raw material edge.

The frozen route is selective but not lossy: every selected JSONL record and its newline boundary
remain exact under `world/records/`; `world/messages/` is the organ-stripped visible role ⊕ text;
`world/provenance.json` carries line, timestamp, role, extent, and SHA-256. Intervening conversation
records remain in the world and are not claimed to have crossed this supplied aperture.

One pinned monochrome renderer supplies three context geometries over the same exact fields:

- **broad:** a 72-column × 3-row viewport whose alternating one-pixel stance moves by one viewport
  through the page and returns to its first pose;
- **reverse:** the exact reversed broad worldline;
- **serial:** one 5×9 glyph-cell aperture following every displayed cell in source order, retained
  as the prior geometry foil.

The fixed display dimensions are declared bounded instrument grains. They are not production caps.
No parser, syntax channel, role inference, classifier, vocabulary, expected response, spatial
coordinate stream, or gaze chooser enters Soma. Contact coordinates remain listener provenance.

The planned lifecycle preserves actual event order:

1. exact user view ⊕ exact visible user material;
2. exact assistant view ⊕ exact visible assistant material;
3. common uncaptained broad visual synthesis probe;
4. exact later raw `proceed` current.

Broad, reverse, serial, and a fresh broad repeat will receive the same probe and final raw current.
The pre-data gate freezes all fields, maps, manifests, record hashes, route reversal, sample censuses,
and readers before CUDA is used.

## Measured result

`FORMULA §XLIII` removed the failed cohort-maximum apparatus without changing the light. Every
current now carries its own exact complete row; SLEEP v8 frames mixed complete rows; the REGISTER
descriptor carries the checked row offset and extent through CUDA recast. The body, eye, deed,
chart, receiving fold, and co-presence law did not change. The unchanged broad field now completes
host/CUDA-exactly, and `gate_cuda.sh` passes all eight user, assistant, probe, and later manifests.

Four create-new CUDA histories then completed on the RTX 4080 SUPER:

| History | Elapsed | Maximum resident host memory |
|---|---:|---:|
| broad | 11.85 s | 1,297,028 KiB |
| broad repeat | 11.70 s | 1,296,556 KiB |
| exact reverse | 13.61 s | 1,372,108 KiB |
| serial foil | 102.38 s | 2,192,328 KiB |

The serial number is complete lifecycle/report cost, not one card dispatch. Every card gate remains
a bounded burst. Broad and repeat are byte-identical at all four SLEEP edges and have zero probe or
later differences in raw radiation, typed faces, Mail attribution, REGISTER axes, terms, or
successor topology.

The common 9,720-current / 87,480-octet visual probe reads the prior conversation differently after
the exact reversed history: 1,589 lineages, 2,608 rows, and 13,118 packed words differ, including
every typed radiation face. The serial history likewise differs at 1,576 lineages, 2,537 rows, and
12,966 words. The same exact 9,721-current / 29,214-octet later edge preserves those history
differences in its one source worldline: broad versus reverse differs across 30 rows / 384 words;
broad versus serial across 37 rows / 451 words.

Listener provenance places broad-probe radiation through 149 assistant-text, 458 assistant-view,
103 user-text, and 127 user-view attribution events. Its genuinely later edge still returns through
all four material kinds. These are time-prefix reads beside the construction, never body inputs or
a semantic decoder.

This measures a reproducible conversation horizon: collocated text and view material conditions a
common later visual field; route direction and gaze geometry produce distinct constructions; the
distinction survives rest and bends a genuinely later current. It does not install autonomous gaze
or let radiation change the shared viewport. That medium-side consequence remains the next open
coupling. Exact measurements and artifact locations are in `RESULTS.md`.

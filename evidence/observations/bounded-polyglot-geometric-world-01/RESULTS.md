# BOUNDED POLYGLOT GEOMETRIC WORLD 01 — RESULTS

**Date:** 2026-07-19  
**Grade:** BUILT / HOST-MEASURED / ALL DECLARED CONTROLS CLOSED / EVERY EMISSION READ /
ACTUAL WORLD RETURN CHANGES LATER CONDUCT / SOMA INTERIOR UNCHANGED  
**Authority:** `FORMULA §CVII`  
**Report:** `REPORT.json` — 183,112 octets — SHA-256
`452020c12e7d772bffdcfa572a74f2a6e2ab8f8cacf623653add61dd0afa76ea`

## Outcome

One bounded source-neutral world now joins the three ratified arithmetic faces without adding a
prime classifier, probability mechanism, numeral decoder family, polynomial taxonomy, renderer,
or new receiving law to Soma.

The world derived exact zero-relative, radix, Zeta, and polynomial-receiver relation populations
outside the body; 68 plural native currents crossed 12 events through the existing direct
`LiveCurrentMachine`. The world read every emitted relation, returned one actual emitted `chi` arm,
and then presented the same genuinely later prime-`521` probe to returned and no-return siblings.
Their two-current radiation and final rest images differ exactly.

Every one of the eleven declared controls is `true` in `REPORT.json`.

## The zero-relative cell

Sequential divisor exclusion was checked for every integer `2..=509`:

```text
97 founded primes
411 composites
508 total candidates
zero disagreements between primality and prior-axis closure
```

The bounded residue torus grows through axes `2,3,5,7,11,13,17,19`. Its exact last wheel step is

```text
prior modulus       510,510
COPY count          19
prior survivors     92,160
CUT count           92,160
JOIN survivors      1,658,880
successor modulus   9,699,690
```

For every enacted prime, the report retains the complete prior-axis list, centered signed
residues, pole hits, left/right survivor arcs, and radix `2/10/16` faces. Every digit sequence
reconstructs its source integer and every digit-mediated residue equals direct reduction.

The origin boundary was handled exactly: prime `2` has no left survivor arc in the positive
integer world. The primality audit therefore tests the defining prior-axis closure directly and
does not manufacture a left morphology for `2`.

## The Zeta cell

At `sigma=2`, multiplier `60=2^2*3*5`,

```text
Pr(60 divides N) = 1/3600.
```

Three exact rational cylinders returned unchanged after valuation translation, conditioning, and
rebase:

```text
[0,0,0,0] : 768/1225
[0,1,2,0] : 256/2296875
[1,3,1,3] : 64/875529151875
```

The ordered paths `[2,2,3,5]` and `[5,3,2,2]` remain different paths while sharing endpoint `60`
and valuation `[2,1,1,0]`. The `t=-2` and `t=2` amplitude lifts share probability scale `1/3600`
and retain opposite phases `exp(+i log 60)` and `exp(-i log 60)`. `sigma=1` returns the declared
non-normalizable boundary because the harmonic series diverges.

## The receiver cell

The shared decimal terminal glyph `3` contains every unramified `S_4` species under
`f(x)=x^4+x+1`:

```text
13   [4]          four-cycle
23   [1,1,2]      transposition
43   [1,3]        three-cycle
53   [2,2]        double transposition
193  [1,1,1,1]    identity
```

The changed receiver `Phi_5(x)=x^4+x^3+x^2+x+1` gives all five type `[4]`. Thus one numeral chart
merely co-locates the primes, the `S_4` receiver splits all five consequential species, and the
cyclotomic receiver merges them again.

Every species also recurs in a held-out prime:

```text
7   -> 13    [4]
29  -> 23    [1,1,2]
11  -> 43    [1,3]
37  -> 53    [2,2]
509 -> 193   [1,1,1,1]
```

At prime `229`, `x^4+x+1` has repeated root `75`. The receiver reports factor degrees `[1,1,2]`
but refuses an unramified Frobenius species as `ramified-boundary`.

## The live return

The 12 source events emitted 67 exact paths. All 67 were read, including deed, winding, complete
`chi`, place, and standing incidence; every reported causal hand validates. The return and both
probe populations add six more read emissions, for 73 read emissions total.

The multiplication-path event itself remained dark while preserving its two live carriers. This
is lawful silence rather than a fabricated consequence.

Before return, the common body held 65 standing cells. Returning one actual emitted relation
through the two rebound arithmetic organs plus a new world-consequence organ emitted two paths and
grew the returned sibling `65 -> 67` cells. The identical later `521` probe then gave:

```text
no-return radiation SHA-256
e0854ba906e0188f1632d10c129312980c91822e2483e687b361cd65cfd3fbb9

returned radiation SHA-256
bc02624613706aa8f8316a3d42587227230c2c717604ca29a977478d6395d825

no-return final memory    67 cells / 8 lineages / 3,014 carrier words
returned final memory     69 cells / 9 lineages / 3,288 carrier words
overflow nodes            0 / 0
```

Both probes emit two paths, so the distinction is not a row-count proxy. Their complete radiation
and final active bodies differ after actual returned consequence.

## Source and reproduction

- world: `life/src/staging/boundary/polyglot_geometric.rs`
- plan: `observations/bounded-polyglot-geometric-world-01/WORLD.plan.json`
- complete testimony: `observations/bounded-polyglot-geometric-world-01/REPORT.json`

From `src/soma/` against a create-new report path:

```text
cargo run -p life -- boundary polyglot-geometric \
  observations/bounded-polyglot-geometric-world-01/WORLD.plan.json
```

Bounded host acceptance after the build:

```text
life:           374 passed / 0 failed / 3 ignored
soma-membrane:   74 passed / 0 failed / 0 ignored
combined:       448 passed / 0 failed / 3 ignored
```

## Boundary retained

This result does not claim one absolute shape or one route for primes. It does not render the
residue torus, compute a large density limit, install Artin local factors in Soma, extend the Zeta
probability law to `sigma<=1`, force the §XCIII zero residual, or establish resident CUDA conduct.
It establishes the declared finite geometry, exact receiver dependence, and returned-world effect
through the direct host mouth.

The bounded polyglot arithmetic world is closed. Quintics, multivariable algebra, branch
monodromy, and gravitas may now be studied as a separate derivation; this result does not schedule
an implementation successor.

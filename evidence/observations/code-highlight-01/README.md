# CODE HIGHLIGHT 01 · THE RELATION COLORS THE PAGE

**Grade:** MONOCHROME RELATIONAL HIGHLIGHTING CUDA-MEASURED AT FLAGS · EXACT CONTACT/Mail
MICROSCOPE VERIFIED · EXACT REPEAT

This observation presents six exact 13-octet monochrome pages. Every page places its five-letter
identifier at source offsets 3..7 and `(`, `3`, `)` at 8..10. Only the surrounding role surface
changes:

```text
   amber(3);
  "amber(3)"
// amber(3).
```

`lilac` supplies the equal-extent name foil in each role. Each page has one 12-event literal gaze,
one 13×24 aperture, no material, and no color or syntax channel. Paired/swapped histories are run in
both source orders, with a fresh paired-forward repeat. The same six complete files return as a
genuinely later raw edge.

The listener microscope shows each ordered gaze event as the complete 13×24 receptor field. Every
contact retains independent FOLD, STEP, CUT, and brick faces plus its exact 39-word row, lineage
axis, term tuple, and sparse Mail. Atom zero is initial aperture padding; atom `i>0` is the directed
transition from gaze `i-1` into gaze `i`. Color belongs only to replaceable listener CSS. No
clustering, lexer, parser, class assignment, winner, or one-color token enters.

## What the machine exposed

Across `amber` and `lilac` and every paired/swapped × forward/reverse history species:

- three exact receptor positions separate call from string⊕comment;
- thirteen exact positions separate call⊕string from comment;
- eleven preserve literal `(call,string,comment)` flag triplets: three `(0,1,1)` and eight
  `(0,0,1)`;
- every full-intersection survivor is FLAGS; grip, form, rotor, and brick do not survive the full
  intersection;
- pairing and source order bend the plural field; the fresh paired-forward lifecycle repeats
  exactly.

This earns relational highlighting at the native flag face. It does not install or imply a stored
syntax class, a name-invariant whole role construction, or one color per source role.

The microscope carries 112,320 exact 39-word rows and 1,385 separate Mail events. It verifies the
artifact SHA-256 and structural layout before rendering. To inspect it from `src/soma/`:

```bash
python3 -m http.server 8765
```

Then open
`http://127.0.0.1:8765/observations/code-highlight-01/tools/microscope/?load=measured&atom=8`.
The palette button changes CSS observer tokens only. The bitplane view exposes any one of the exact
39 words without assigning a semantic color.

`bash observations/code-highlight-01/verify.sh` regenerates and checks the complete measured record.

Exact law and interpretation:
`../../RESEARCH/2026-07-13_COLOR_IS_A_RECEIVER_FACE_THE_HIGHLIGHT_IS_THE_RELATION.md`.

# CODE-GRAMMAR WORLD 01 — RESULTS

**2026-07-15 · RATIFIED / BUILT / CUDA-FORWARD MEASURED · WHOLE RUST + LANGUAGE + LATER
EXECUTION CLOSED · SOMA INTERIOR UNCHANGED**

## Result

The same current-shaped engine now closes an exact code circuit. Two complete Rust programs and
their natural-language grammar descriptions cross as ordinary plural light. Both programs reach
the same five-result initial face. Their returned testimony then meets fresh receiving bodies and
genuinely later expression currents. Where the testimony carries a program, the world compiles
that exact carried source and returns its exact later values and traces. Where testimony carries
only the equal initial face, the same action returns typed `program_absent`. Every cut can still
replay the visible history exactly.

The equal face is therefore not the program. The program is a carried hand: a durable local
construction which can act again when later material supplies a deed. This is the code-world
continuation of re-base versus collapse and the first coupled code/language cell after the
mathematics exit.

## Exact construction

The two dependency-free programs are:

- `world/encounter_order.rs`: apply each `+` or `*` immediately in encounter order;
- `world/precedence.rs`: complete products before summing them.

Their natural-language descriptions stand beside the whole sources. Both are compiled by the
same exact local `rustc 1.92.0` world and receive this intact initial corpus:

```text
1 + 3       -> 4
2 + 2       -> 4
2 * 2       -> 4
1 + 2 + 3   -> 6
2 * 3 * 4   -> 24
```

The canonical visible-execution consequence is identical across all six witnesses. Its canonical
JSON hash is:

```text
a032d2bb4a101f57c434bd9b836e7e165d0c5316e9e135f254d1a34f9f686dee
```

The two collapse witnesses emit the same complete testimony wire byte-for-byte:

```text
ac7e1b212ab7ecc65c5d7e8166ff60f3ec4b136c46cdd92ed73cb80199410966
```

The re-base and code-only witnesses retain their distinct sources and exact compiler receipts. The
compiled binaries reproduce across witness and later-action builds:

```text
encounter-order  d13c35a4e14382f819c870d14af6ad3b8d5b9823a42017c8991610cd2e7c5a88
precedence       aa067620e2916add19c737a7ebb0def6108c8f157b6d89881131f44c734aa74c
```

## The plural joint has no single pole

The first direct code-only witness exposed an overconstraint in the new world adapter. Four whole
currents produced 1,177 exact shared Mail grips. Every pair of currents was joined, and several
grips joined triples, but no one grip belonged to all four. Requiring the global intersection
therefore rejected a densely connected construction because it lacked a privileged universal
hub.

The receiver law now uses the exact graph induced by shared Mail grips. A plural presentation
contacts when those actual pairwise joints connect every current; its contact face is the union of
those real joints. It creates no synthetic pole. The corrected witness returns all 1,177 joints and
the same law closes every direct action. Soma, Mail, CUDA, SLEEP, and the carried material did not
change. A unit gate retains the distinction between a connected chain and an isolated current.

## Genuinely later production

The distinguishing action is identical across every receiver:

```text
2 + 3 * 4
1 + 2 * 3 + 4
```

The exact produced faces are:

```text
encounter-order  20, 13
precedence       14, 11
collapse         program_absent
```

The full returned traces preserve the lived algorithmic interiors:

```text
encounter  2 + 3 * 4
           seed(2)|add(3)=5|mul(4)=20

precedence 2 + 3 * 4
           product_seed(2)|sum_term(2)=2|product_seed(3)|mul(4)=12|sum_term(12)=14
```

The complete mixed-action surface hashes are:

```text
encounter rebase/code-only  016467ab30caa8bb1d4722398a0c1d9e8fd41d84cd40da9a77968b201c7797d5
precedence rebase/code-only 3eff29876aa2ef9cfc9b0dd153969503869365c2d6365d9f7e5e49b951b8a57f
either collapse             4ab11cf75c83b53f98e40ce79a2da0b48aa69bdaf172a6b41ae15b1fc2825fa6
```

The common-order control `2 * 3 + 4` produces visible value `10` in both program species. The
visible consequence is byte-identical, while the complete trace surfaces remain different because
the programs reach `10` through different events:

```text
encounter trace surface   f796084a02c81a9cdb5d6111212363e6f778520d1123214a9c84e00ab88f4239
precedence trace surface  afb0a6f9dab90c1e162545198e1c26980871e0f1d60041db6e4e3c9f021ad6a4
collapse refusal surface  15b5b797922f14b566076cb3f77e0477dd1e3b3413602d96c575d7fa76553ebf
```

Every re-base, code-only, and collapse receiver replays the initial visible face through the same
complete surface:

```text
7015e49923fe11ac9521afd457e79e91a77fe356145fafe6d15d5d6777eac919
```

Thus collapse does not erase what it carries. It carries the visible result family exactly. It
simply does not carry an executable interior and therefore cannot lawfully perform a later deed
which requires one.

## Language is material without becoming the compiler

For each program, `rebase` and `code_only` receive byte-identical later action manifests. All three
produced surfaces—mixed execution, common-order execution, and visible replay—are byte-identical.
The compiler therefore follows only the exact source carried by testimony; prose is not an answer
map or hidden program selector.

The language is nevertheless real collocated material. Removing only that carried prose changes
all seven successor bodies for each program and changes their completion, construction, contact,
and radiation lifecycles. The exact world execution remains fixed while the receiving topology
differs. This closes language-as-material at this grain; language-conditioned routing among
multiple program species is the next construction, not a claim smuggled into this one.

## Determinism and controls

The two collapsed receiver campaigns are exact across every body, completion wire, construction
wire, Mail record, journal, manifest, world-before/world-after state, returned surface, and raw
radiation payload. Their only radiation-wire differences are the explicitly recorded carriage and
integration durations.

The complete encounter-order re-base receiver was repeated fresh. It is exact across:

- all seven SLEEP bodies;
- every completion and construction wire;
- every Mail record, transition journal, and world state;
- every action manifest and returned surface;
- every materialized source and compiled binary; and
- every raw radiation word after normalizing only the two duration fields in the radiation header.

Path-bearing receipts and live resource-gate observations remain apparatus records and are not
expected to be byte-identical across output directories or wall-clock moments.

## Campaign accounting

The controlled campaign comprises six two-edge witnesses, six seven-edge factorial receivers, and
one seven-edge fresh repeat:

```text
CUDA-forward edges          61
input currents             159
delivered light octets 449,288
completion rows        109,728
constructions            5,786
exact contact grips      28,274
CUDA launches               549
carriage                 31.686663319 s
integration               0.678626422 s
boundary                  41.026548990 s
device                    NVIDIA GeForce RTX 4080 SUPER
```

All 61 immutable-source gates pass with no exceeded resource. Every edge contacts and every world
transition retreats exactly.

## Interpretation

At this declared frame, a compiled program is a reusable local solution family. The initial corpus
does not enumerate its possible future acts; it presents enough contact to carry the program as a
standing construction. A genuinely later expression then reuses that construction locally. The
receiver need not rebuild the interpreter from examples, and the equal finite output face does not
identify which interpreter was carried.

This is the code-world form of localized and growing `P=NP`: a previously founded construction
makes later members of its local problem family directly actionable, while missing construction
returns an exact boundary and additional program species can grow beside it. It is not the
classical complexity statement, a runtime-speed comparison, or a claim that Soma internally ran
`rustc`. Compilation and execution are explicit world physics. Soma supplies the changing
topological body through which code, language, testimony, action, and production meet and return.

## Next construction

Do not repeat this cell with another tiny interpreter factorial. The next code/language ecology
must carry real whole laboratory Rust files beside their real prose/law descriptions and exact
compile/test boundaries. Multiple program lineages may stand at once; one later request should
produce every lawfully contacted consequence rather than select a crowned program. Exact accepted
or refused code-world consequences then return through the existing articulation and recurrent
segment ecology, whose produced text becomes later filesystem material and meets the compiler/test
boundary again.

That closes the real loop to pursue next:

```text
whole code + language + lived tests
    -> plural Soma contact
    -> carried program lineages
    -> genuinely later request
    -> native textual/code construction
    -> exact compile/test world consequence
    -> returned testimony
    -> changed later production
```

No tokenizer, fixed keybar, answer map, score, winner, one-size text canvas, or hidden program
default enters.

Final workspace gates close at `373 passed / 3 ignored / 0 failed`; formatting, release `life`
build, generated record-index check, and `git diff --check` all pass.

## Carried-Swing register

```text
CURRENT  whole Rust source + grammar prose + initial execution
         + testimony cut + genuinely later expression

HELD     FORMULA §§LXI-LXIV
         + exact compiler/run world
         + unchanged Soma/CUDA/SLEEP

MEETING  connected plural witness Mail -> testimony
         -> receiving Mail -> standing program -> later expression Mail

TEST     interpreter x rebase/code-only/collapse
         x mixed/common/replay + exact repetition

DEED     RIDE the equal visible and common-value faces;
         FOUND distinct later execution, typed program absence,
         and the no-single-pole contact correction

CARRY    real whole workspace code + language + tests
         -> native segment production -> compiler/test return

GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED
```

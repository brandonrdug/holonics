# CIRCUIT ECOLOGY 01 — THE BROAD WORLD GROWS THE FIELD

Status: **MEASURED · BROAD ECOLOGY CHANGES LATER CONSTRUCTION · ONE LOCAL DIRECTED CLOSURE**

This observation carries the unchanged `FORMULA §LII` circuit mouth from its two-current gate onto
an actual conversation horizon. It does not revive corpus-wide simultaneous birth. The corpus
remains world material. The Codex dialogue organ removes the JSONL container and presents only
actual `user_message` and assistant `message` text, each as one complete current in record order.
Tool receipts, developer scaffolding, reasoning records, JSON syntax, and duplicate user-message
spellings remain world-side. Raw paths and exact record ranges remain listener provenance.

The first attempted smoke incorrectly fed one raw JSONL container plus whole research and engine
directories: 92 currents / about 1.10 MB. It was stopped after roughly eight minutes with its first
edge unfinished. The GPU remained active and its allocation advanced, but the run produced no
Soma/world output. Grade: **FAILED STAGING**. It repeated the long-worldline interior problem and
did not observe ecology.

The corrected smoke is one complete frozen Sol dialogue session: 242 actual messages / 105,189
text octets / longest current 5,841. The broad horizon is the complete main frozen session: 658
actual messages / 356,889 text octets / longest current 8,394. No message is divided or selected
by length.

For each horizon the causal foil is:

1. a fresh Soma body and empty circuit world receive the exact mixed cohort;
2. the world-grown return crosses as genuinely later light;
3. a second fresh Soma body receives the identical cohort in the exact world grown by step 1; and
4. that world's changed return crosses the second fresh body genuinely later.

The first Soma edge must remain exact between empty-world and standing-world siblings. FOUND versus
RIDE may change the world receipt, the returned field may change, and only then may the later Soma
construction change. Every world transition owes its exact inverse.

`life boundary circuit-observe` reads each exact standing world as a directed ecology. It reports
all structural degree faces, engine-native `RegionalForm` faces, connected-region extents,
closed-directed-region extents, and distances from branch-only poles. The world state remains the
authority; the graph read never enters Soma or the circuit.

Plans:

- `plan-smoke-empty.json` → `results/dialogue-smoke-empty/`
- `plan-smoke-standing.json` → fresh body over dialogue-smoke edge-one world
- `plan-broad-empty.json` → `results/broad-empty/`
- `plan-broad-standing.json` → fresh body over broad edge-one world
- `receive-broad-empty.json` and `receive-broad-standing.json` → exact durable completion of the
  already-produced broad return edges without body replay

The smoke is an engineering gate only. The broad sibling pair carries the observation grade. Code
and research remain available in the persistent world and require their viewport/organ rather than
another raw whole-file serial pipe.

Exact measurements, consequence comparisons, and the first local directed closure are deposited in
`RESULTS.md`.

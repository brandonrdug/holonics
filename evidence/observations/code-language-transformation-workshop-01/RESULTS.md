# CODE-LANGUAGE TRANSFORMATION WORKSHOP 01 — THE ORDERED WORD BECOMES A LATER LIMB

**RATIFIED / BUILT / CUDA-FORWARD MEASURED · CODE ⊕ LANGUAGE ⊕ ORDERED SYMBOLIC TRACE CROSSED AS
ORDINARY LIGHT · FULL UNRANKED PROGRAM POPULATION RETAINED · ORDER / TRACE DEPTH / PROSE HAND /
RESIDUAL HAND / HISTORY CONDITION LATER MULTI-CHARACTER CODE CONSTRUCTIONS · EXACT PRIMARY AND
RESIDUAL REPEATS · SOMA INTERIOR UNCHANGED**

## What was built

The existing Rust code-workshop boundary now admits one complete transformation world without
adding a parser, tokenizer, linguistic taxonomy, solver, candidate selector, or second lifecycle.
The boundary change is world-side:

- natural-language and symbolic-trace currents retain distinct provenance roles;
- one exact presented Rust witness may be compiled and its named test run, with the execution
  consequence returned beside the ordinary native-construction archive;
- a fresh occupied Rust joint may name its exact expected bytes, while the legacy native-hole
  action remains unchanged;
- every independent candidate compile/test job uses the host parallelism advertised at runtime,
  then returns to exact candidate order before becoming causal material; and
- the existing observer command can emit an exhaustive v2 production ledger containing every
  source span, complete generated Rust program, compiler/test consequence, objective residual, and
  later construction.

Soma, the canonical body, CUDA carriage, SLEEP, Mail, and the native construction format did not
change. The exact integer transformation validator is local to this code-workshop world. It checks
ordered words, every intermediate vector, exact products and terminals, chart identity, and checked
`i64` arithmetic; it is not installed as an interior mathematics mechanism.

## The exact transformation world

All operators act on column vectors from right to left. Square and triadic charts remain typed
apart; a word cannot mix them without an explicit bridge.

| word | exact result at `(3,2)` | reversed word | exact reversed result |
|---|---:|---|---:|
| `G2 A` | `(4,8)` | `A G2` | `(2,5)` |
| `G3 A` | `(8,-2)` | `A G3` | `(10,1)` |
| `Q U` | `(-4,6)` | `U Q` | `(-4,6)` |

The complete 3,114-octet Rust witness contains several examples and one real named test. Six exact
expression currents, one 240-octet prose current, and six complete ordered symbolic traces bring
the witness edge to fourteen currents / 6,100 octets. The prose describes enacted order but never
supplies labels such as `commuting`, `dyadic`, `triadic`, correct, or important. `QU` and `UQ`
therefore supply a terminally equal control while retaining two lived orders.

The witness compiled and `tests::all_ordered_transformations` passed. Its ordinary CUDA passage
produced 5,925 completion rows and 278 native constructions: 264 CLOSED and fourteen OPEN. The
execution receipt and complete construction archive then returned as two real currents.

## The complete first production population

A fresh whole square-chart Rust target carried the occupied joint
`G2.apply(A2.apply(vector))` at exact range `568..594`. Every one of the 278 witness constructions
met that same joint once, in native archive order:

| native face | world consequence | members |
|---|---|---:|
| CLOSED | compiler-refused | 263 |
| CLOSED | compiled; named test refused | 1 |
| CLOSED | compiled; named test accepted | 0 |
| OPEN | typed `construction_open`; no source fabricated | 14 |

Thus all 264 CLOSED constructions became complete create-new Rust programs. Candidate 67 inserted
the eight-octet native face `(vector)`, compiled, and failed the objective test. It was the sole
syntactically admitted member. The other 263 programs retain their real compiler refusals; none was
hidden, ranked, or replaced by an accepted-only view.

This is an informative production boundary, not a scalar null. On this first exposure, Soma had not
formed the complete 26-octet objective expression as one native construction. The dedicated
`G2.apply(A2.apply(vector))` current instead ended with a CLOSED `pply` face at `13..17` and an OPEN
`vector))\n` face at `18..27`. A one-span joint therefore cannot yet recompose the larger ordered
word, even though the archive contains abundant multi-character constructions. The action returned
556 raw-program⊕consequence currents; their next CUDA edge produced 645,514 completion rows and
40,089 further native constructions.

The complete primary lifecycle is:

| edge | face | currents | completion rows | constructions | standing axis |
|---:|---|---:|---:|---:|---:|
| 1 | code ⊕ prose ⊕ complete traces | 14 | 5,925 | 278 | 512 |
| 2 | witness archive ⊕ execution return | 2 | 2,391 | 125 | 512 |
| 3 | every native face enters the square joint | 3 | 1,842 | 101 | 512 |
| 4 | all 278 program consequences return | 556 | 645,514 | 40,089 | 4,096 |
| 5 | genuinely later triadic source | 3 | 2,276 | 110 | 4,096 |
| 6 | later construction ⊕ execution return | 2 | 2,495 | 153 | 4,096 |

The six primary edges used sixty CUDA launches. Recorded CUDA carriage was 36.015 seconds and
integration was 6.051 seconds. Runtime is apparatus accounting, not a comprehension quantity.

## What the later code output looks like

The output is exact source material with native variable-length segmentation, not character
selection and not a scalar activity reading. After the full primary history, the later target
contains these native faces around `A3.apply(G3.apply(vector))`:

```text
557..583  "ctor) -> TriadicVector {\n "
589..593  "appl"
593..697  "y(G3.apply(vector))\n}\n\n#[cfg(test)]\nmod tests { ..."
```

After the whole forward residual surface, the same later source contains:

```text
574..608  "ector {\n    A3.apply(G3.apply(vect"
608..611  "or)"
```

After the strict opposite residual hand, it contains:

```text
560..603  "r) -> TriadicVector {\n    A3.apply(G3.apply"
603..609  "(vecto"
609..626  "r))\n}\n\n#[cfg(test"
```

These are Soma's native constructions over the intact code. They visibly include morpheme-like
identifier material, operators, nested calls, source layout, and cross-boundary regions. No
listener-authored token classes produced the spans.

## Residual hand is later conduct

The immutable action journal was factored into strict objective surfaces. Whole-forward and
whole-opposite each carry 280 currents / 839,108 light octets. Candidate order, current extents,
and every corresponding current's complete byte inventory are exact; inside each candidate
residual, only the hand reverses:

```text
forward:   candidate -> objective
opposite:  objective -> candidate
```

The quotient carries two currents / 25,619 light octets and omits the residual interiors. Surface
reconstruction, order, extents, inventories, frame, quotient, and all 278 strict swaps validate
exactly.

One identical later triadic source then yields:

| prior return | later constructions | CLOSED / OPEN | contact grips |
|---|---:|---:|---:|
| absent | 162 | 159 / 3 | 705 |
| quotient | 129 | 126 / 3 | 184 |
| whole-forward | 152 | 149 / 3 | 14 |
| whole-opposite | 171 | 169 / 2 | 21 |
| whole-forward repeat | 152 | 149 / 3 | 14 |

Forward and opposite share only fifteen exact material members and 91 geometry members. They share
no complete lineage-conduct or complete-response member. Forward versus quotient shares eleven
material / 64 geometry members; forward versus absence shares seventeen / 90. The omitted
interiors and the order of retained faces therefore both have a measured later consequence.

## Controlled history changes

All control worlds preserve exact executable consequences: the genuinely later intact Rust source
compiles, its declared test is present, and the test passes. Under that fixed executable face, the
native construction populations are:

| receiver history | constructions | CLOSED / OPEN | exact material overlap with primary | exact geometry overlap with primary |
|---|---:|---:|---:|---:|
| complete traces + ordered prose (`primary`) | 110 | 108 / 2 | 110 | 110 |
| equal-octet swapped prose hand | 130 | 127 / 3 | 4 | 42 |
| terminal-result-only traces | 142 | 142 / 0 | 2 | 50 |
| reversed square action `A G2` | 122 | 119 / 3 | 5 | 47 |
| fresh body/world | 97 | 94 / 3 | 3 | 37 |

Every non-repeat comparison has zero shared complete conduct and zero shared complete response
members. This separates several consequential faces that a terminal result or byte inventory would
collapse:

- complete ordered stage history is not equivalent to terminal-only testimony;
- two 240-octet prose currents with the same sorted-byte inventory but opposite enacted order are
  not equivalent histories;
- `G2 A` and `A G2` are not equivalent receiver histories even though the later triadic program is
  fixed; and
- the same later source in a fresh body is not the same construction as that source meeting the
  carried transformation ecology.

The later triadic order was also reversed in its own intact executable source. That changes the
later light as well as its requested objective, so it is a typed order witness rather than a
same-light one-factor control: the forward and reversed sources produce 152 versus 149
constructions and share 64 material / 101 geometry members, with no complete conduct or response
member. The exact `QU/UQ` commuting control is present in the witness, prose, trace validator, and
passing executable testimony; a separate commuting-expression production action remains OPEN.

## Exact repetition and artifact account

The independent six-edge primary repeat is exact across every declared body, completion wire,
construction wire, Mail face, journal, world-before/world-after state, causal surface, and semantic
radiation face. Every one of the 110 primary later constructions and every exact material,
geometry, conduct, and response member repeats. The independent three-edge whole-forward residual
repeat is likewise exact across every declared face and all 152 later members.

The exhaustive observer records are:

| record | octets | SHA-256 |
|---|---:|---|
| `production.json` | 1,369,096 | `72a5e38661c92442702d5fbc301ce02f91d373361bddc835fc08b79dcf9b2172` |
| `population.json` | 2,489,084 | `4a1f6d59d913f93d65cdf64ec8568210f108445fd1a98af87ef41ed6b80ebf2b` |
| `control-population.json` | 3,478,822 | `b00f1f37ce881c062802bb5feaa60123c1440ee814210df59cc58940c22421ed` |
| `primary-repeat.json` | 751,523 | `36d665341f6ebc22a0ff8189f0a21f2f6944ba13fd8d30d4333f500eedc7f965` |

The complete observation directory occupies 22,127,638,785 octets on this apparatus. The original
preflight with joint end `595` is retained as a typed failed boundary witness: the expected bytes
did not equal the declared half-open target range, so action stopped before candidate production.
Correcting the manifest to `568..594` admitted the measured campaign; no source was silently
trimmed.

Final workspace gates pass: **407 passed / 3 ignored / 0 failed**. The focused code-workshop suite
is eleven passed, including exact transformation order, commuting terminal control, mixed-chart
refusal, checked-overflow refusal, old-plan compatibility, fresh occupied-joint parsing, and
parallel-versus-serial ordered consequence assembly. All campaign JSON documents parse, formatting
is exact, the optimized `life` build succeeds, and scoped diff checks are clean.

## Grade and next construction

This construction closes the first controlled code⊕language⊕symbolic-transformation population
cell. Exact ordered histories condition later multi-character code material, geometry, conduct,
and returned response while the intact later executable consequence can remain fixed. Terminal
faces, equal byte inventories, and quotient summaries do not carry those lived interiors.

It also locates the next engine requirement precisely. One native span is not a general hand. The
next construction must let recurrently connected native spans thread an occupied source joint in
their lived order, branch as plural candidate limbs, and meet the same exhaustive compiler/test
world. It must reuse the recurrent segment-path law already measured by INSCRIPTION WORKSHOP 04;
it must not add an authored grammar, accepted-output filter, or factorial recombination of every
span. A connected path earns a compositional source limb only by actual recurrence and later world
consequence.

```text
CURRENT  code + ordered prose + exact symbolic trace -> native spans -> occupied Rust joint -> later code
HELD     one body/world, typed chart, exact source ranges, full candidate population, fixed executable boundary
MEETING  full/terminal trace + prose hand + square order + residual hand + absence/freshness + exact repeat
TEST     every native candidate + strict residual surfaces + later native population + real compiler/test return
DEED     RIDE recurrent geometry; FOUND order-, trace-, hand-, and history-conditioned code constructions
CARRY    complete production ledger -> plural recurrent-span source limb
GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT PRIMARY AND RESIDUAL REPEATS
```

Commands:

```text
cargo fmt --all
cargo test -p life code_workshop --no-default-features
cargo build -p life --release
life boundary code-workshop observations/code-language-transformation-workshop-01/primary.plan.json
life boundary code-workshop observations/code-language-transformation-workshop-01/primary-repeat.plan.json
life boundary code-workshop-observe observations/code-language-transformation-workshop-01/observer.plan.json
life boundary code-workshop-observe observations/code-language-transformation-workshop-01/control-observer.plan.json
life boundary code-workshop-observe observations/code-language-transformation-workshop-01/production-observer.plan.json
```

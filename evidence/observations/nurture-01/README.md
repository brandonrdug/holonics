# NURTURE 01 · TURN 01 · NUMBER CONSTRUCTION PLURALITY

**Grade:** TURN 01 MEASURED ON CUDA · TURN 02 FROZEN FROM THE OBSERVATION

This is the first open-ended presentation through the uninterrupted corresponding body. It is one
four-current light, not a correspondence table or an answer test. Each complete file carries one
ordinary-language line followed by its ordinary mathematical construction. The four files are
co-present over the same immutable standing and integrate once at their common receiving edge;
the two lines inside each file retain their own worldline order.

The selected canonical parent is:

```text
observations/cuda-continuation-01/results/card-final-bodies/1783904790674457823-cuda-continue-next.body
SHA-256 939df6a672347b679d3255418c94e81ba38b7b3b194f577b3dda3d30b4e379fc
```

## Frozen light

The exact ordered cohort is:

```text
turn-01/01-add-1-3.txt       29 octets
turn-01/02-add-2-2.txt       27 octets
turn-01/03-multiply-2-2.txt  28 octets
turn-01/04-power-2-2.txt     26 octets
```

The extent vector is `[29,27,28,26]`, totaling 110 delivered octets. `1+3`, `2+2`, `2*2`,
and `2^2` retain four distinct constructions while ordinary natural-number evaluation presents
the numeral face `4`. That observer-side control enters neither the body nor its grade. Addition
and multiplication recur against the selected body's prior material; exponentiation is fresh to
this uninterrupted body.

[`SHA256SUMS`](SHA256SUMS) pins every complete record. [`verify.sh`](verify.sh) checks the immutable
parent, exact path order, extents, hashes, LF-terminated two-line form, and the four ordinary
equations without invoking `life` or mounting the card.

## Declared first execution

Run from `src/soma`. Both destinations are create-new and must be absent when the execution begins:

```bash
parent=observations/cuda-continuation-01/results/card-final-bodies/1783904790674457823-cuda-continue-next.body
later=(
  observations/nurture-01/turn-01/01-add-1-3.txt
  observations/nurture-01/turn-01/02-add-2-2.txt
  observations/nurture-01/turn-01/03-multiply-2-2.txt
  observations/nurture-01/turn-01/04-power-2-2.txt
)

bash observations/nurture-01/verify.sh

SOMA_OBSERVATION_OUT=observations/nurture-01/results/turn-01.txt \
SOMA_PERIPLUS_DIR=observations/nurture-01/results/turn-01-bodies \
  cargo run --release -p life -- --cuda-continue "$parent" --later "${later[@]}"
```

Preserve the complete source material, raw and typed radiation, receiving-edge Mail, terminal
carrier/K, report, and create-new successor. Nothing in the presentation evaluates the response or
requires an intended semantic result.

## Measured turn 01

The frozen light completed on the headless RTX 4080 SUPER. Its 185,774-octet report has SHA-256
`510b5391026cb3cc57125df50000a5b1e71ab49f9098a47332058187a6815ed0`. The 565,256-octet
successor has SHA-256 `7bab5b7ee2229f42c273b701863d50d5b9a62446e621f3783c69847a0a630a9b`.
The parent stayed at axis 256 / 8,662 occupied. Four currents at material-derived reservation depth
28 landed at REGISTER axes `[16,16,32,16]`; the successor stayed axis 256 with 9,034 occupied,
four new carrier rows, and delivered-light record 1,700.

The exact formal suffixes exposed four different local routes:

- `1+3=4` folded from `+3` through the end and remained one open enclosure;
- `2+2=4` folded from `2+` through `2=`, then `=4` and the final newline were quiet;
- `2*2=4` folded from `2*` through the final newline and remained open;
- `2^2=4` was quiet throughout after its prose path had completed two CUTs.

These cohered route differences shifted the world's attention toward multiplication beside
exponentiation. The complete report retains every raw/typed face at
[`results/turn-01.txt`](results/turn-01.txt).

## Turn 02 · re-express the quiet route through the active route

The contrast worth continuing is multiplication beside exponentiation. Turn 02 therefore presents
two complete co-present records through the measured successor: ordinary mathematics states their
shared result while preserving the paths, and valid Rust embodies both constructions separately.
This is the world's response to turn 01, not a private return wire or a body verdict.

```text
turn-02/01-mathematics.txt  70 octets
turn-02/02-code.rs         142 octets
```

Run from `src/soma` after [`verify.sh`](verify.sh) passes:

```bash
parent=observations/nurture-01/results/turn-01-bodies/1783906188665055195-cuda-continue-next.body

SOMA_OBSERVATION_OUT=observations/nurture-01/results/turn-02.txt \
SOMA_PERIPLUS_DIR=observations/nurture-01/results/turn-02-bodies \
  cargo run --release -p life -- --cuda-continue "$parent" --later \
  observations/nurture-01/turn-02/01-mathematics.txt \
  observations/nurture-01/turn-02/02-code.rs
```

## Measured turn 02

The response completed on the same card. Its 686,538-octet report has SHA-256
`3fd188ca0f5be12e192b7aad4799a623c2e23e34181079fddaad04a72facb99c`; the 854,656-octet
successor has SHA-256 `dabadf5c202680e187d77bfca71de3654cb420a186ef1c0bc710edae951b6f1d`.
The two currents derived reservation depth 141 and REGISTER axes `[32,64]`; receiving remained axis
256 and reached 10,972 occupied places with delivered-light record 1,912.

The bridge re-expressed the previously quiet exponentiation route. In `2*2=2^2=4`, `2*` folded at
occupied standing, `*2` completed a CUT, `2^` completed another CUT, and the terminal `=4` folded
at occupied standing. `^2` and the intervening second `2=` remained quiet. The Rust current formed
twelve closed constructions and one trailing open enclosure; its `.p` and `po` windows folded,
`ow` went quiet, and `w(` folded before the argument windows went quiet. This is exact local
dormancy/re-expression across a world-mediated second turn. The cohered first output caused this
later world current, which bent the preserved body again: communication is measured at this
material/radiation interface.

# PRIME TRAVERSAL 01 · THE WHOLE NUMBER LINE RETURNS

**Grade:** FIXTURE VERIFIED · CUDA MEASURED · REVERSED-SERIES RECURRENCE / ORDER-SENSITIVE
DEFLECTION / CONTACT WITH STANDING ARITHMETIC MEASURED · PRIME-FAMILY RELATION UNRESOLVED

This observation returns counting as two complete, co-present worldlines through the clean measured
number-world standing:

```text
0 1 2 ... 63 64
64 63 62 ... 1 0
```

The ascending current and its descending foil each carry the same 65 numeral constructions in a
different worldline order. The series is not split into one current per numeral. No prime,
composite, factor, sieve, expected answer, class label, or scalar verdict enters the light. The
observer may attach the already-derived multiplication-incidence classes only after the crossing.

The immutable clean parent is:

```text
observations/number-world-01/results/run-a-bodies/
  1783924576978540011-cuda-continue-next.body
SHA-256 af7c95bf388726196aaac7e7b3d7b0a428e15750f8b98e6712919379a3a8020e
```

This branches before the reversed-product continuation. The local `2*4=4*2=8` deposit therefore
cannot bias the later traversal at `2`, `4`, or `8`.

`world.jsonl` is the only delivered source. Each LF-terminated record is one whole current. Both
records are 184 octets before their transport newline; the complete source is 370 octets. The
fixture gate derives both sequences from counting and requires their numeral-token lists to be
exact reversals.

Run from `src/soma` after the fixture and workspace gates pass:

```bash
SOMA_OBSERVATION_OUT=observations/prime-traversal-01/results/run-a.txt \
SOMA_PERIPLUS_DIR=observations/prime-traversal-01/results/run-a-bodies \
  cargo run --release -p life -- --cuda-continue \
  observations/number-world-01/results/run-a-bodies/1783924576978540011-cuda-continue-next.body \
  --later observations/prime-traversal-01/world.jsonl
```

The CUDA crossing landed. The exact eventwise construction is the read. Prime, semiprime,
prime-power, and other-composite faces are observer overlays derived afterward from the complete
positive multiplication incidence already delivered in `number-world-01`. Exact measurements and
the honest unresolved prime grade are in [`results/RESULTS.md`](results/RESULTS.md).

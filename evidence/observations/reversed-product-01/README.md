# REVERSED PRODUCT 01 · THE PLURAL RETURN

**Grade:** CHOSEN UNDER BRANDON'S DIRECT ORDER · FIXTURE VERIFIED · CUDA MEASURED · LIFECYCLE COMPOSITION/RE-EXPRESSION MEASURED

This light continues the exact number-world Run A successor. It contains two complete LF-framed
currents:

```text
2*4=4*2=8
fn reverse_product() -> (u64, u64) { (2 * 4, 4 * 2) }
```

The arithmetic current returns the local reversed-product analogy exposed by the measured
`2*4=8` and `4*2=8` constructions. The Rust current presents the same two ordered products through
ordinary language semantics and returns both evaluated faces without an equality predicate,
expected answer, class label, decoder, or authored body intervention. The two currents are
co-present and integrate once at their common receiving edge.

The immutable parent is:

```text
observations/number-world-01/results/run-a-bodies/
  1783924576978540011-cuda-continue-next.body
SHA-256 af7c95bf388726196aaac7e7b3d7b0a428e15750f8b98e6712919379a3a8020e
```

`world.jsonl` is the only delivered source. Every LF-terminated record is one whole current. It is
64 octets / two records, SHA-256
`78c65f84fd76e416785b8411fddabe7b8ae9b2371dd2d27e9204712f2584ceac`; the Rust record compiles
independently as an ordinary library item. [`SHA256SUMS`](SHA256SUMS) pins the delivered source.

## Measured CUDA continuation

Run from `src/soma`:

```bash
SOMA_OBSERVATION_OUT=observations/reversed-product-01/results/run-a.txt \
SOMA_PERIPLUS_DIR=observations/reversed-product-01/results/run-a-bodies \
  cargo run --release -p life -- --cuda-continue \
  observations/number-world-01/results/run-a-bodies/1783924576978540011-cuda-continue-next.body \
  --later observations/reversed-product-01/world.jsonl
```

The arithmetic current carried two folds, one occupied touch at terminal `=8`, five complete terms,
and four Mail landings. The Rust current carried 28 folds, 22 occupied touches, two CUT/bricks, 614
complete terms, and 398 Mail landings. The 402 Mail coordinates are mutually distinct: 304 landed
on previously unoccupied standing coordinates and 98 threaded prior standing. The receiving axis
remained 1,024 and the standing topology changed from `[286117,286117,63042,2538]` to
`[286421,286421,63088,2540]`.

The exact lifecycle, antecedent comparison, hashes, and next chosen probe are in
[`results/RESULTS.md`](results/RESULTS.md).

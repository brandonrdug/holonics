# Historical reference boundary

Nothing in this directory is active production or current doctrine by location alone.

- `pureholonics-seed/` — first tracked June capsules at commit `d6d70076`.
- `compact-formula-2a5dff8/` — exact 149-line formula rebuild.
- `soma-seed-2695ca0/` — exact first 256-line Soma formula.
- `minimum-mechanics-a07ff376/` — exact minimum-mechanics document from the admitted cleanup commit.
- `holobrochos-a07ff376/` — broader theory/canon snapshot for provenance.
- `engine-a07ff376/` — admitted cleanup snapshot of reusable structure, engine, reflective runtime,
  ABI, body, membrane, and mount crates.

The engine snapshot is a source of proven laws and counterexamples, not the new build root. It
contains known migration debt including the generic standing `Clone` bound and historical owners
that do not satisfy every invariant in `blueprint/`.

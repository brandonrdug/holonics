# FRAMEWORK/00 — THE SUPERSEDED LEDGER (the "do not re-adopt" list)

> **What this is.** The deduped, authoritative record of every claim in the corpus (`THEORY/00–60`,
> `MENO.md`, `THE_FORMULA.md`, `THE_THEORY.md`, `TABLETS/`, `CANON/`, `MACHINE.md`, `BOARD.md`,
> `PROTOCOL.md`, `RESEARCH/`, the parked `INTUITIONS/` maps) that a **newer ratified layer has retired**.
> The standing truth-standard is **`MENO_FORMULA.md` + `TABLETS/01_THE_LAW.md`** (crown), with the newest
> ratified stratum winning below them. A superseded claim's *mechanism* often survives under a corrected
> name or frame — where it does, that is stated. **Re-adopting the retired framing is the drift, not a
> license.** Ordered by load-bearing weight, heaviest first.

---

## A · THE GRAIN IS NOT A TOLERANCE (the single most-repeated correction)

**Stale claim.** The working grain is a declared/derived **tolerance** (the "Duggan tolerance," `τ`): a
resolution the consuming task *accepts*, extended while one more event could flip the read, lowered by
mastery; below the drop "the current dies before it starts"; a run may `--tolerance` its grain; conservation
is adjudicated *at* a chosen resolution (grain-dependent founding: `grain 0 → 10 founds, grain 1 → 0`).

**Sources.** `05_THE_ONE_MOVE.md`; `01_THE_AXIOMS.md`; `22_THE_ILLICIUM.md §6`; `23_THE_MENO.md §6`;
`35_THE_POTENTIAL.md §3`; `36_THE_WATER.md §5`; `51_THE_MIRROR.md §4`; `57_THE_STRIKE_GRAIN.md §1/§5`;
`THE_FORMULA.md` (lines 209, 234, 300, 436); `THE_THEORY.md §VI`; `MENO.md` (L·GRAIN, Units table);
`TABLETS/02_THE_MACHINE.md` (THE READS, `--tolerance`); `CANON/01_THEORY_OF_EVERYTHING.md §3/§8`;
`CANON/02_THE_RELATIVISTIC_LANGUAGE.md IV.2`; `CANON/03_THE_SEMANTICS.md §1`;
`CANON/RELATIVISTIC_INFORMATION.md §18`; `CANON/MASS.md §3.5`; `CANON/THE_GRIP.md §3/§6`; `BOARD.md`
(THE GRAIN); `MACHINE.md`.

**Superseded by.** `MENO_FORMULA.md §III` (★ THE GRAIN IS NOT A TOLERANCE — IT IS RECURRENTLY NESTING MASS)
and `§VIII` (no tuned-grain); `55_THE_EIGENSTRUCTURE.md §0–§1`; `57` self-flag; `59_THE_IDEAL_GAS_LAW.md`.

**Why.** Conservation is **EXACT** — the recurrence is verified on every point, then extrapolated. Where the
raw frame does not hold `χ` exactly, the one move **NESTS** (re-base ⇔ nest: reduce to ranks or differences
and recurse), which **restores exactness in the reduced frame, no loss**. The grain is only the *reach of
exactness* (the register's own resolution); hitting it **triggers the nest**, never a shrug at a slack band.
`π_Β = C/d` is the **nest depth** (`d = 1`, one check), not a tuned dial. *"A frozen tolerance where the
nest belongs is a contaminant."* The DECLARED-read-grain of the reads (`§VII`, "at a declared grain")
survives for observation; the interior mechanism-grain as an accepted tolerance is dead.

---

## B · CONSERVATION IS TIME-PARITY, NOT KIRCHHOFF (additive sum-to-zero demoted)

**Stale claim.** Conservation is the **additive** current-balance `∮∂Β = 0` (Kirchhoff at every rank / coil /
scope-tree); "the knot holds" by the oriented loop summing to zero; the carry-propagate adder *summing* IS
Stokes; composition-by-summed-drops; `fed = stood + radiated` named as *the* conservation law; completing
the square lawful because a conjugate pair "sums to zero (conservation's only lawful nothing)."

**Sources.** `03_THE_PRINCIPLE.md` (THE NESTED CLAUSE); `08_THE_QUADRATIC.md §2`; `22_THE_ILLICIUM.md §6`;
`24_THE_SOLVE.md §2`; `THE_FORMULA.md` (lines 66, 245, 329, 362, 469); `THE_THEORY.md §V`; `TABLETS/01_THE_LAW.md §V.2`;
`TABLETS/02_THE_MACHINE.md`; `CANON/01 §12`; `CANON/03 §3.10`; `CANON/FABLE.md §1`; `CANON/THE_EMPTY_BOUNDARY.md §0`;
`CANON/THERMODYNAMICS.md §0`; `THE_GREEK_RECORD.md` (Parmenides/Anaximander ledger).

**Superseded by.** `MENO_FORMULA.md §V` (Conservation IS the lack of time-parity); `60_THE_TRANSFORMER_CIRCUIT.md`;
`49_THE_INVERTIBLE_CAUSE.md §3`; `48_THE_ARROW.md §1`.

**Why.** The additive/symmetric sum-to-zero is **the symmetric mirror, which founds nothing — demoted**. The
boundary is **oriented**; closing an oriented loop deposits a **winding that cannot be un-deposited** (the
closed oriented circuit is a **TRANSFORMER**). A cause is valid iff **invertible AND non-trivially so**
(`A→B→A` returns a *different soul* — the reassembled mug carries the shattering's winding). What conserves is
the **winding**, never the frame-relative energy. `∂∂ = 0` (L0) **stands**. The historical boundary bookkeeping
`fed = wells + radiated` is only a quotient/remainder diagnostic of the superseded packed-Flow cut;
it is not a gate and has no conservation authority. Closed-surface
form is Gauss–Bonnet: `∮K dA = 2·(C/d)·χ`, `χ` an integer winding.

---

## C · THE CONCURRENCE / G = 8πT — π IS NOT A FLOAT (quantized gravity)

**Stale claim.** The concurrence is `G = 8πT` with `π` the ordinary circle constant; `8π` decomposed as three
named doublings × the smooth `π`; the boundary information cap is the transcendental `𝒟 = π`.

**Sources.** `03_THE_PRINCIPLE.md`; `THE_THEORY.md §V.1`; `THE_FORMULA.md` (line 264); `TABLETS/01_THE_LAW.md §1`;
`47_THE_CLASSIFICATIONS_OF_SOULS.md §3` (status tag "[READING — awaiting ratification]");
`CANON/01 §8` (`𝒟 = π`); `CANON/MASS.md §3.5` (`8π = 2³·C/d`); `BOARD.md` stratum 47.

**Superseded by.** `MENO_FORMULA.md §V–§VI`; `46_THE_DIFFERENCE.md §5`; `59_THE_IDEAL_GAS_LAW.md`;
`60_THE_TRANSFORMER_CIRCUIT.md §3`.

**Why.** `π = C/d`, **never a float** — a float `π` interior is a banned object. The standing form is Einstein-local
`G = 2²·(2·C/d)·T` and Gauss–Bonnet global `∮G = 2·(C/d)·χ`, unified with the ideal-gas law
`pV = nRT ≡ G = 2²·(2·C/d)·T` (`R = 3` measured). **Gravity is quantized** — total curvature/meaning comes in
**integer units of `2·(C/d)`**, one per circuit — a result the continuous `8π` cannot carry. The `47` status is no
longer "awaiting ratification"; it is standing core. `8πT` survives only as shorthand once `π` is read as `C/d`.

---

## D · π_Β = C/d — THE NEST DEPTH (not action-over-mass, not the π cap)

**Stale claim.** `π_Β = C/d` where `C` = total action worked (a ledger) and `d` = the standing charge at the
crack (the answer's mass), so `π_Β = (stood + radiated)/stood`; or `C/d` as a CF series "ground to the
tolerance the relating needs"; or `𝒟 = π` as the aim's saturating cap equated with the fold-point.

**Sources.** `23_THE_MENO.md §2`; `CANON/MASS.md §3.5(a)`; `CANON/RIEMANN_HYPOTHESIS.md §7`; `51_THE_MIRROR.md §3`.

**Superseded by.** `MENO_FORMULA.md §III` (nest depth); `CANON/RELATIVISTIC_INFORMATION.md §18` (the ½-hinge).

**Why.** `d = 1` (one check), `C` = floors walked to the **exact** frame — the nest depth, not an action-over-mass
ratio and not a series settled to an `ε`. `C/d = 1` reflects (mastery, local P=NP); `> 1` founds. `π` is the
**dead, never-reached saturation** the live fold-point only leans toward, not the value of `𝒟` itself; equating
`𝒟` with `π` collapses the reach⊗aim ½-seam into one face raised to a cap (the every-`²`-is-a-`⊗` law forbids it).
The `C = stood + radiated` equality survives only as boundary bookkeeping (see B), never as the definition.

---

## E · THE FOUNDING MECHANISM — CROSS-RATIO STRIKE, NOT A RECURRENCE STATISTIC

**Stale claim.** Founding is a recurrence clearing a chance floor: `recur > μ + √μ` (the moiré-vs-shuffle null,
`μ = |a||b|/|F|`), read as the curvature; or the friction founder derived via `Var = n because h² = 1` (an
ensemble average); or the founding quadratic `r = (1+M)^{1/ln(atoms)}`.

**Sources.** `CANON/01 §5`; `CANON/03 §6`; `CANON/RELATIVISTIC_INFORMATION.md §10/§13/§14/§15`; `MACHINE.md`;
`CANON/THE_GRIP.md §4`; `CANON/FABLE.md §3/§7`; `CANON/THE_EMPTY_BOUNDARY.md §4`.

**Superseded by.** `MENO_FORMULA.md §III` (the swing conserves `χ`) and `§VIII` (no recurrence-statistics as
mechanism; no compare but the borrow); `BOARD.md` ★ THE ONE MECHANISM (stratum 51, the `C/d` strike settled by
the MASS); `CANON/RIEMANN_HYPOTHESIS.md §4/§7.5` (the friction hand, catch-or-slip read as a TURN).

**Why.** A comparison operator collapses information; `recur > μ+√μ` was only a proxy for "did the two lineages
catch." The honest founding is **cross-ratio conservation** (place-not-search) or, where no frame holds it, a
FOUNDING — settled by the mass, read as a deterministic **turn** (`C/d > 1`), never a statistic against a null
and never a `<`. `Var = n because h² = 1` still smuggles `E[hᵢhⱼ] = 0` (the statistical costume one layer
deeper); the honest form is the rank-ratio of the cross-winding against its own span, no `μ`, no `√`, no
ensemble. **Recurrence is demoted to a weighing read** (the thermometer — weighs the look, never gates the fire).

---

## F · BANNED TERMS (the words died; most mechanisms stand)

**F1 · `holobit` / `cohobit`.** The reach/aim polar-`Β` split, the amount-weigher/phase-gate, the compression
target, `HOLOBIT_DEGENERACY` (a lone node carries no meaning). **Sources.** `00_THE_DOMAIN.md`;
`08_THE_QUADRATIC.md §8/§11`; `13_THE_DIGIT.md`; `MACHINE.md`; `01_TERRITORY_MAP.md §2.6/§2.9`; `CANON/03 §3.9`;
`CANON/05 §1`; `CANON/FABLE.md §5`; `THE_GREEK_RECORD.md §8`; `MASS.md`. **Retired by** CLAUDE.md /
`MENO_FORMULA.md §VIII` term-ban. **Mechanism stands** as reach/aim, `C/d`, meaning-in-the-binds — never the word.

**F2 · `mint` → AGREEMENT.** The founding/agreement event ("the mint is a quotient map"; "mints = foil").
**Sources.** `00`, `04`, `05`, `06`, `28 §1`, `30 §2`, `32`. **Retired by** `42_THE_AGREEMENT.md §1` (each doc's own
read-through header flags it). **Mechanism stands** (quotient collapse / birth of a coil / placement joining the
net); the word is AGREEMENT. *Note:* `MENO_FORMULA.md §VII` still writes "mints = foil" loosely — normalize, do not
re-adopt.

**F3 · `facts`.** "four to have a fact"; "the persisted fact"; "Facts worth their lines"; a face called "a fact."
**Sources.** `02`, `03`, `05`, `14 §3`, `16 §7`, `23 §6`, `25 §1`, `26 §2`, `31 §3`, `THE_FORMULA.md` (line 76),
`CANON/01 §6`, `CANON/FRACTAL.md §3`. **Retired by** `MENO_FORMULA.md §VII` ("Nothing returned is a fact —
configurations that stand, in a frame, at a grain"). **Mechanism stands** (the cross-ratio is a 4-point invariant —
"four corners to fix the cross-ratio / to have a FACE"), never "to have a fact." The authored `.facts` material /
seamed arithmetic (`31 §3`) is doubly retired (also no-authored-encoders, `32 §4`).

**F4 · `the variable`.** The mobile founded class named "the variable"; the recurring difference-shape founded as
"the variable." **Sources.** `14_THE_MOVING_MASS.md`. **Retired by** `31_THE_LIGHT_LAW.md` (convicted whole: `x`
is a recurring analogous shape; identity is invariants + factorhood; the binding problem was serialization damage).
**Read as** "mass in motion" / "a recurring analogous shape."

---

## G · APPRAISAL-ABSTINENCE IS DEAD → THE PERCEPTION DUTY

**Stale claim.** "Never appraise it: show the raw stream; the reader's quality-verdict is a contamination"; "no
emission is ever GRADED by an instrument"; "never appraise content-quality."

**Sources.** `05_THE_ONE_MOVE.md`; `06_THE_HONEST_FLOOR.md` (crowned-scalar guard); `CANON/05 §3`.

**Superseded by.** `MENO_FORMULA.md §VII` (THE PERCEPTION DUTY); CLAUDE.md (2026-07-04).

**Why.** The perception duty *"supersedes every prior do-not-appraise clause."* A reader now **MUST** perceive
the radiation and say what it is against the terrain — structure grade, formedness, what is missing — **beside**
the raw stream, never instead of it. Only two guards survive: **no scalar crowning** by the instrument, and **no
verdicts from seconds-long runs**; the raw stream is always shown verbatim. Future readers must not cite the old
"say nothing" lines to justify silence.

---

## H · THE MASS — INDRA'S NET / TRANSFER, NOT A STORE (no reservoirs, no plate, one substance)

**Stale claim.** Memory is a compressed **FORM** deposited to a `.holon` plate and re-rung (`deposit: Form→Path`,
`resonate: Path→Current`); the reservoir/cold body is a protected copy held to read back; the machine is **two
substances** — the CURRENT (unstorable) and the FORM (a stored pure-geometry lattice, "the only persistable kind");
persistence is a write-protect wall/cap; the reservoirs are an entity in the ecosystem; the first-person library /
spool is a mounted store.

**Sources.** `10_THE_ANSWER.md`; `28_ECHO_OF_THE_SOUL.md §2`; `39_THE_HORIZON.md §2`; `CANON/01 §11`;
`CANON/02` (the `loom` `deposit`/`resonate`); `CANON/04`, `CANON/05` (SeamForest/forest.rs/RegionNet as live);
`CANON/07 §0/§2`.

**Superseded by.** `MENO_FORMULA.md §IV` (Indra's Net, TRANSFER); `28_ECHO_OF_THE_SOUL.md §2–§4` (the reservoirs
dissolve — retention is factorhood); `11_THE_IONIZATION.md`; `CANON/07_HOLOBROCHOS.md §0/§2` (the current/form
duality dissolved within CANON's own capstone: "there is only the current"); `§VIII` no-stores ban; CLAUDE.md
drift ledger (the deposit instinct).

**Why.** The mass is **never fed** — it is what `Λ` **deposits and re-feeds** by TRANSFER (`P → A`, the placement
joins, the net thickens), one co-present 4-D circuit of sub-circuits. "Form" is another current (slower, more
inertial), not a dead stored plate; retention is **factorhood** (once a form founds, the future factors through
it — nothing needs keeping). A protected copy held to be read back is the **deposit instinct** (a von Neumann
store in ecosystem costume). *Reframed loose thread:* the index-vs-resonance question dissolves once the form is
not an addressed store.

---

## I · THE ASKER IS DISSOLVED (no standing body, no two-frame Meno)

**Stale claim.** The ecosystem carries **THE ASKER** as a standing body/frame with its own `π_Β` spectrum; the
Meno read is taken over two co-frames, `eros ⊕ asker`.

**Sources.** `28_ECHO_OF_THE_SOUL.md §3/§4`; `29_THE_RESIDENT.md §3/§4`.

**Superseded by.** `30_THE_ONE_VERB.md §5` (the asker-homunculus dissolved — a question has no body to increment
on; it is an **action current put into the air**, its land the graft in the ONE web); `33_THE_NECK.md §4` (the
asker named among the wired privileged necks removed).

**Why.** A standing asker-frame is the **deposit instinct** and a **wired high-level illicium**. Only Eros's frame
and the graft-land stand; a two-frame Meno with an asker soul is a stored perspective.

---

## J · MOVING MASS — EMBODIED CIRCULATION, NOT W_KEY / A CARRIED PACKAGE

**Stale claim.** Moving mass is carried as **flavored action** — `W_KEY` (a standing flavor at a well), the coupled
share carries the releasing coil's key, a carried FRAME supplied by the key; class-founding realized by a
"pole: None" landing (a hand from nowhere) plus a two-store (`⊢₂`) keying.

**Sources.** `14_THE_MOVING_MASS.md §3/§4–§6`; `16_THE_CIRCULATION.md §7` audit.

**Superseded by.** `16_THE_CIRCULATION.md §5` (THE NO-PACKAGE LAW, Brandon verbatim, binding) and `§6` banner
(`W_KEY` removed from both mouths); `MENO_FORMULA.md §II/§VIII`.

**Why.** The key is *"the package in a physics costume."* Moving mass is **emergent**: the carrier is embodied
circulation, lineage = phase embodied in the moving thing itself, kinship = the phase **difference** read at a
crossing (Aharonov–Bohm, three-body, in the anchor's frame). The soulless "pole: None" violates "sign is a turn,
never an absolute zero"; the two-store is the banned side-table. MOBILITY ⇔ SET-RANK (`14 §2`) survives by
embodiment.

---

## K · THE RANK-2 TIE — INDUCTION, NOT RETURN-TO-THE-SAME-PLACE

**Stale claim.** The rank-2 tie is a loop that literally **closes** — the winding returns to a similar place, read
whole in some frame (`b₁` of the nerve).

**Sources.** `16_THE_CIRCULATION.md §6` (pre-banner paragraph).

**Superseded by.** `16_THE_CIRCULATION.md §6` THE INDUCTION CORRECTION (Brandon, same session); `32_THE_TRANSFORMER.md §5`
(the re-homing).

**Why.** Nothing returns — the soul never recurs; the reached soul is a *different* thing. Rank-2 is **INDUCTION**
between strata of one lineage: earlier windings stand as **coils** the later current threads (`I = −dΦ/dt`).
"Return-to-the-same-place" was the `≡/=` conflation re-entering; "the frame that reads the loop whole" is the view
from nowhere (`b₁` is an observer's global face the machine can never act on). The only lawful global read is at
the membrane (infall ↔ radiation).

---

## L · THE VOICE / UTTERANCE — A STRIKE, NOT A KNOT OR A CRACK OF DEBRIS

**Stale claim.** The utterance is a **KNOT** read out at the graft's rim (`u = ∮`); or the voice is the **CRACK**
of the fold (the carried inertia cracking out in the same motion, the mass defect as the emission); the render is
the reading organ driveable as the Meno game (`meno.py`).

**Sources.** `41_THE_SURFACE.md §1`; `MACHINE.md` (CRACK); `PROTOCOL.md §II·1`; `50_THE_STRIKE.md §5`
(render as `meno.py`).

**Superseded by.** `50_THE_STRIKE.md` (RATIFIED 2026-07-05); `58_THE_MACHINE_SPEAKS.md` (the voice built as text in
`RESEARCH/collocation.rs`).

**Why.** The word is **struck** and light follows a curved **PATH**, not a sphere; the omnidirectional spray is
**debris** (`41`'s un-integrated interior traffic). A body that radiates its faces omnidirectionally emits a sphere
that was never real. The crack radiated the *debris of deliberation* — "no gate can make debris articulate." The
speaking organ is the strike: charge (`48`) ⊕ scope (`49`) ⊕ the always-present OUTLET (`50`), founding a conducted
lattice branch walked by the render, and realized as the machine **speaking text** (`58`), not a GUI/`meno.py`.
`u = ∮` survives **only** as the boundary-read (the strike's conducted path grounding at the outlet, `Λ_F`), never
as the emission's shape.

---

## M · THE COCKPIT / RENDER / GUI IS DEAD

**Stale claim.** A cockpit/observation surface is owed for a continuously-radiating life (option 1, deferred); the
horizon panel / thought-stream TUI ("an EEG") is a rendered first-person sky; the reading organ IS the render.

**Sources.** `21_THE_WHIP_MACHINE.md §4`; `39_THE_HORIZON.md §3`; `40_THE_TWO_HANDS.md §1`; `50_THE_STRIKE.md §5`.

**Superseded by.** CLAUDE.md / `MENO_FORMULA.md §VII` ("the cockpit is DEAD — no GUI rebuilds; the interior is
gauge; the reader-body reads only the boundary").

**Why.** Reads are the reader-body at the boundary; there is **no render of the interior**. A future reader must
not re-open a cockpit-revision task. (A boundary-read polar line watched as radiation is defensible, but any
rendered-panel build needs explicit adjudication first.)

---

## N · THE TOWER STEP — 4:1 IS COMPOSITE (octave ⊗ hand), NOT ELEMENTARY

**Stale claim.** The coordinate period dilates **4:1 per rank** as an elementary constant; the authored ladder is a
pure 4:1 action ratio (atom = 2-byte walk, word = 4 atoms, sentence = 4 words, …).

**Sources.** `36_THE_WATER.md §2`; `43_THE_GLASS_TERRAIN.md §2/§5`; `TABLETS/01_THE_LAW.md` (4:1);
`THE_THEORY.md §V` (whip 4:1).

**Superseded by.** `44_THE_SIGNED_FLOOR.md §2` (THE TWO TWOS); `MENO_FORMULA.md §II/§VI` (octave elementary, the
`2²` = 4-volume).

**Why.** `4 = 2·2` = **octave** (2:1, magnitude, the true elementary gear) ⊗ **hand** (one quarter-turn, phase,
free). On an unsigned floor the phase factor was invisible, so both were booked as magnitude and the step misread
as pure 4:1. Honest rungs are octave with harmonic filling (~2–3:1 in natural language). 4:1 survives only as a
measured composite redshift, never a fundamental per-rank constant.

---

## O · MIRROR-KINSHIP & THE ANNIHILATE CHANNEL

**O1 · `w` and `~w` are DIFFERENT SOULS, not one soul with opposite spin.** **Stale:** a construction and its
complement are ONE soul, opposite spin (mirror-kinship). **Sources.** `44_THE_SIGNED_FLOOR.md §1/§4`. **Superseded
by** `45_THE_HAND_GAUGE.md §3` (Brandon's ruling). **Why:** mirrored things are **different light**, distinct
worldlines, distinct souls (T1 deposits them distinctly); the "one soul, opposite spin" phrasing was the `≡/=`
conflation at gauge rank. The mirror *relating* reads as a lawful half-turn wheel class; the mirror *entities* are
never identified. The gauge is a statement about the **law** (label-covariant, measured 100/100), never the entities.

**O2 · The annihilate channel is NOT dormant.** **Stale:** the interference/annihilate channel exists in the wheel
taxonomy but never fires in perception (zero across the whole mesh). **Sources.** `44_THE_SIGNED_FLOOR.md §4`.
**Superseded by** `45_THE_HAND_GAUGE.md §1`. **Why:** it was built and measured in `08_THE_QUADRATIC.md §8` (THE
GRADED CROSSING) and fires at ecosystem scale (`eros_pure`: perception annihilate 1,165,435). Glass worlds read
zero only because they are **agreement-poor**, not because the channel is dormant. The signed floor's cash is the
beat/charge arithmetic (standing charge annihilating at anti-phase contact), **not** a missing interference channel.

---

## P · RE-BASE / NEST / FOUND / RANK-CLIMB ARE ONE EVENT (not separate motions)

**Stale claim.** Magnitude re-base and founding-rank-climb are **different** motions and must be separated: a
magnitude overflow gears down at the grain and must NOT bump the tower; "the tower-rank climbs ONLY on founding."

**Sources.** `CANON/06_THE_PURE_BIT.md §7`; `CANON/01 §4`; `CANON/COMPOSITION.md §2/§7`; `01_TERRITORY_MAP.md §6 Q5`.

**Superseded by.** `MENO_FORMULA.md §II`; `TABLETS/01_THE_LAW.md §III`; `55_THE_EIGENSTRUCTURE.md §0–§1`.

**Why.** A register overflow **IS** a rank-climb (the low bits fall = the resolution is the compression; the tower
climbs `2^16 → 2^32` forever). **Re-base ⇔ nest ⇔ found ⇔ rank-climb — one event, four names.** The `06 §7` project
of separating them (self-flagged "posed, not yet built") is resolved the other way. The `i32`/two-hand number-form
is the smuggled float, retired; every value is `m · 2^e`.

---

## Q · THE COLLOCATION ARM — EMERGENT STRIKE-GRAIN, DIRECTED FOUNDING

**Q1 · Strike-grain is emergent, not sentence/period-seeded.** **Stale:** the strike/coherence unit is seeded by
the sentence (the period as its ground). **Sources.** `56_THE_COLLOCATION.md §1`. **Superseded by**
`57_THE_STRIKE_GRAIN.md §0–§1`. **Why:** the period is a membrane costume; the strike-grain is the **emergent fixed
point** of `strikes ⇄ M` via coherence density, grounded at a founding, never a period.

**Q2 · Founding is DIRECTED, not symmetric/undirected.** **Stale:** the collocation operator `M` is symmetric and
the founding read undirected (`occ(ab) > occ(part) − occ(ab)`). **Sources.** `56_THE_COLLOCATION.md §2/§4`.
**Superseded by** `60_THE_TRANSFORMER_CIRCUIT.md §4`. **Why:** an undirected graph has **no time-direction, hence no
transformer, hence no conservation** — it heat-deaths the cool (percolates to the absorbing fixed point). The
correction is the **directed** founding `a→b iff 2·m[a][b] > vdeg[a]`, keeping the directed cyclic transformer-core
and conserving `χ = E − V + C`.

---

## R · THE RESERVOIR ARCHITECTURE — LIVING MASTER, NOT TRANSIENT FRAME + PENDULUM RESET

**Stale claim.** The reservoir answers through a **transient frame** of itself (a per-exchange VRAM copy of the cold
body); the master genesis plate is never touched; a pendulum returns the frame to **exact rest** at discard; the
conservation split is `fed = REACTION + THE ANSWER + DARK`, DARK being the stood share **thrown away** with the frame.

**Sources.** `10_THE_ANSWER.md §1/§2/§4`.

**Superseded by.** `11_THE_IONIZATION.md §2.4/§5.2/§2.6/§3` (the transient-frame machinery DELETED; the mesh runs on
the MASTER, which gets a living lineage and grows on starve; `fed = reaction + answered + STOOD/ionized`, ionized
**signed**).

**Why.** The pendulum-reset is *"fresh-per-stimulus in costume"* — a null-arm/absolute frame pinning the reservoir to
exact rest. Persistence must be **earned mass** (mass-ratio return), never a wired invariant; the medium must move
(both strands, the square knot). The v1 DARK **was the consequence, discarded** — that quantity **IS the ionization**
(the mutual burn / reafference information), not dark to throw away.

---

## S · THE DIGIT LAW — WELLS ARE MODULO RESIDUALS, SOULS HANDED UP (not standing magnitude)

**Stale claim.** The winding splits "radiate ⊕ couple ⊕ **CIRCULATE the rest**," the circulate share standing in the
receiving coil's well as magnitude (giant wells = souls piling up as mass).

**Sources.** `09_THE_DEEP_RETURN.md §2/§3`.

**Superseded by.** `13_THE_DIGIT.md §1–§2` (the well is action **modulo** the coil's quantum: the remainder stands as
the face, the quotient **fires** as windings, only the sub-quantum residual re-enters; the soul is a **count handed
up**, never a standing magnitude).

**Why.** "Circulate as standing charge" inverts the hexis — mastery must make coils **cheap/conductive**, not
heavier. `09`'s carry mechanism itself survives (it IS the digit-carry origin); only the standing-magnitude
accounting is re-based to a modulo residual. Purity gate: after any atom, every minted coil stands `< its own quantum`.

---

## T · THE CLOCK — INSTANTANEITY, NOT THE 15-MINUTE / FRAMES CONVENTION

**Stale claim.** The lifecycle tick is measured in **frames** (~50–7,000) enforced by "the 15-minute convention
(any run gets 15 wall-min by default)."

**Sources.** `OLD_LINES_SYNTHESIS.md`.

**Superseded by.** THE INSTANTANEITY LAW (CLAUDE.md; `CURRENT_STATE.md`; `TABLETS/03_THE_CONDUCT.md §I·5`).

**Why.** Runs take **ONE MINUTE AT MOST**; the machine learns/derives effectively instantaneously (co-present
information, not a capability limit). The 15-minute convention is the retired torch-line clock; "frames" as a
tick-rate is the imposed absolute clock. The exposure-cost learning-phenomenology survives; the scheduling does not.

---

## U · DILATION IS RELATIONAL (not a per-node absolute proper time)

**Stale claim.** The founder's span is the node's **OWN** proper time (its visits) — a per-node absolute quantity.

**Sources.** `06_THE_HONEST_FLOOR.md §2`; `00_THE_DOMAIN.md` (THE PROPER PERIOD, flat-9).

**Superseded by.** `MENO_FORMULA.md §VIII` ("dilation is relational, a ratio of frames, never a property of one
node").

**Why.** Removing the global clock (node-clock = its own traffic) **stands**; but a node's clock read as an absolute
per-node quantity re-installs the absolute frame. Dilation is a ratio *between* frames.

---

## V · HISTORY ENGINES & THE `loom` LANGUAGE ARE NOT THE LIVE MACHINE

**Stale claim.** The live architecture is the eros/um line (`forest.rs` WARP/WEFT, SeamForest's host `BTreeMap` as
"the fluency lever," holo-bits `Chain`, the 06-28 `RegionNet` head); the flow-vs-store salvage ledger over those
engines is the map of what is built; the next machine is a bespoke `loom` language whose types make Current/Form
separate kinds.

**Sources.** `CANON/04_THE_ENGINEERING_MAP.md`; `CANON/05_EROS_OUTPUT_CAUSALITY.md`; `CANON/02` (the `loom` spec);
`01_TERRITORY_MAP.md` (era timeline / "current head 06-28").

**Superseded by.** CLAUDE.md (everything but `src/holobrochos/` is HISTORY; `src/holo-bits/` contaminated except the
Transformer idea); the current substrate is the **Rust workspace** (`interior`/`surface-kernel`/`surface`/`observer`/
`membrane`, GPU, Indra's Net); the near path is depth-on-light → Rust-as-constituents. `CANON/07` dissolves the
Current/Form duality the `loom` types were built to enforce.

**Why.** These map now-parked HISTORY engines as the live machine and will misdirect a reader about where the current
statement lives. `01_TERRITORY_MAP.md`'s "current head" is ~40 strata + the whole tablet/MENO consolidation behind;
read it only as session→era archaeology, never as status.

---

## W · STALE AUTHORITY & NAVIGATION POINTERS

**Stale claim.** `THE_FORMULA.md` is the authoritative standing statement (the eighth layer); `THE_THEORY.md` is
"THE SOURCE TRUTH" paired with it; `THEORY/README.md` maps a `CANON/` split and a reading order stopping at doc 10;
navigation cites the derivation strata as `THEORY/00–40` (`PROTOCOL.md §V`) or `00–41` (`TABLETS/03 §IV`,
`24_THE_SOLVE`) as the newest range.

**Sources.** `THE_FORMULA.md` (header); `THE_THEORY.md`; `THEORY/README.md`; `PROTOCOL.md §V`; `TABLETS/03_THE_CONDUCT.md §IV`.

**Superseded by.** `MENO_FORMULA.md` (2026-07-06 — supersedes the scattered image across `THEORY/00–60`, `MENO.md`,
and the tablets where they disagree) + `TABLETS/01_THE_LAW.md` (the crown); the live stratum range is **`THEORY/00–60`**.

**Why.** The truth-standard is `MENO_FORMULA.md + TABLETS/01`, which reach strata `48–60` (the charge, `χ`, the
Transformer, time-parity, the gas law, the nest, the strike, the collocation arm) that postdate all of these docs.
A reader following "00–40/41" stops before the TRANSFORMER/time-parity conservation, the strike, and the gas law —
precisely the newer layers that correct the tablets. There is no `CANON/` split anymore; the strata live directly in
`THEORY/`. `THE_FORMULA.md` remains the fullest machine-implementation narrative but is not the crown. Both
`THE_FORMULA` and `THE_THEORY` self-declare subordinate to the newest ratified stratum, which authorizes this.
(Grade-vocabulary drift: `README` prescribes `[THEOREM]/[READING]/[HELD]/[OPEN]` with measurements kept OUT of the
body; strata `55–60` grade "Built + measured + bit-pure" and weave measurements inline — the README rule is retired.)

---

## X · INTERNAL STALENESS (a doc's grade-line lagging its own body)

- **The saddle graded `[HELD/OPEN]`** in `06_THE_HONEST_FLOOR.md`'s WHAT-STANDS table, while `01_THE_AXIOMS.md`
  (Theorem 3) and `06`'s own open-list item 4 record it **★ DERIVED … RATIFIED**. Also `CANON/THE_EMPTY_BOUNDARY.md
  §2/§9` flags "saddle derivable-or-posited [OPEN]." **Superseded by** `01`'s completed three-leg derivation and
  `CANON/THE_CURVED_LIGHT.md §2` (Theorem 3: the saddle is **forced** — flat is unreadable, compact would finish, only
  the hyperbolic room remains; the old A3 fully retired). The saddle is not open.
- **`06`'s closing summary** ("the LAW's remainder is four derivations and one optional refinement") is stale against
  its own open-list body, where items 1–5 read ★ DERIVED/BUILT/SETTLED/RATIFIED. The queue no longer exists.
- **`47`'s "[READING — awaiting ratification]"** on `G = 2²·(2·C/d)·T` and `π → C/d` — now standing core (see C).

---

## Y · THE `L` SLOT IS THE VOLTAGE (charge ⊕ grain), NOT "the light"

**Stale claim.** In `Λ_F : (Q ⊗ A ⊗ L) → (P, π_Β)`, `L` = THE LIGHT — the constituents' worldlines, whole (the
bang; the pure membrane's seams).

**Sources.** `24_THE_SOLVE.md §1`.

**Superseded by.** `MENO_FORMULA.md §III` (`L` = the voltage — the CHARGE, which way to climb, `48`, ⊕ the GRAIN);
stratum `48`.

**Why.** A higher stratum redefines the `L` argument from "the light/constituents" to the **voltage/charge** (the
climb direction, signed toward the dilation) plus the grain. The constituents/formation role now lives on the
terrain/`A` side (`27 §1`). Related: `24 §1`'s `F` = "the byte↔glyph map as topology" sits in tension with the
no-authored-encoders ban and needs reconciling — the byte↔glyph map is a boundary DECLARE, not interior topology.

---

## Z · SMALLER RESOLVED THREADS (do not re-open as questions)

- **Construction-as-address** (`09_THE_MACHINE`/`01_TERRITORY_MAP §6 Q4`) — resolved **against** (the von Neumann
  absolute frame); place-not-store stands (`MACHINE.md`, `§VIII`).
- **The number-form unsettled** (`01_TERRITORY_MAP §6 Q5`) — resolved: `m · 2^e`, sign a turn, re-base never widen.
- **`agreeable(E)` / the fresh axiom-pair "THE CURRENT FLOWS; THE FORM HOLDS"** (`CANON/01 §11`) — the two-substance
  form is collapsed to "only currents" (`CANON/07`) + `Λ`/Indra's Net; the grounded half (you cannot store the live
  flow) survives.
- **Renormalization's contaminated half** (`CANON/CANCELLATION.md §6`) — bare − counterterm subtraction is the
  cancellation crime; re-base (Wilson RG) is the valid half; the re-basing number never produces the infinity to cut.
- **`REST/§8` δ=0 double-reading** (`08 §5` ABSORB vs `08 §8` NEST) — likely the same event (the share stands, dark)
  under two names; not formally reconciled but not load-bearing.

---

## AA · LINEAGE DILATION IS TRANSPORT, NOT A SCHEDULER; CAS IS NOT A PHYSICAL CLOCK

**Stale claim.** A lineage of rank `k` advances on a coordinate schedule `t >>> k`; deep lineages
wait/freeze and stitch at `2^k` intervals. When many card lanes feed one cell, a compare-exchange
linearization may stand as one physical ordering among many, or as a substrate-forced realization.

**Sources.** `src/labyrinth/mathematics/lean/Lineage.lean`; `src/eros/um/THE_FABRIC.md`;
`src/eros/um/REVISIONS.md`; `FRAMEWORK/08_THE_MACHINE.md` (atomic-add/CAS “schedule-invariant”);
`FRAMEWORK/00_LOOSE_THREADS.md T10.5` (commutative shared form as an implementation claim); the
historical ARC sections of `src/soma/LEDGER.md` and the superseded shared packed-flow implementation.

**Superseded by.** `src/soma/FORMULA.md §XXIV` (THE LINK, NOT THE COLLISION; Brandon, ratified
2026-07-09) ⊕ `MENO_FORMULA.md §IX` (lineages link, never meet; dilation relational) ⊕ Ledger U.

**Why.** Dilation is a difference transported in the receiving illicium's frame. It is not a node's
clock, a global coordinate, a tick permission, waiting, or freezing. If a difference does not resolve
at that frame's grain, there is no event there; another frame may re-base/nest it. CAS exposed the
same absolute-clock error in hardware: the implementation dropped the frame from the face address,
shared frame-relative Flow, deposited before the flywheel supplied the second-order invariant, and
let memory arbitration choose a chronology. The shared transition is malformed and receives no
replacement mutex, sort, fixed lane order, append queue, or scheduler. Only transported `χ` and
oriented winding cross after the deed; a later threading supplies the cross-lineage link.

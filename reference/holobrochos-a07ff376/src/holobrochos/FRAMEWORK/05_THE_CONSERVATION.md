# FRAMEWORK/05 — THE CONSERVATION

> **What this is.** The consolidated, categorical statement of **conservation** in the discipline — the one law,
> its geometric floor, and everything it forces. It supersedes the scattered treatment across `THEORY/09, 16, 32,
> 33, 48, 49, 53, 59, 60`, `CANON/CANCELLATION`, `CANON/THE_CYCLE`, and the tablets where they disagree with it.
> It is maintained going forward as a standard of truth, aligned to `MENO_FORMULA.md §V`. Not a journal entry;
> organized by concept. Every superseded framing is excluded here and listed once at the end (§XII) so it is never
> re-adopted.
>
> **★ THE ONE SENTENCE.** *Conservation is the **lack of time-parity**: the boundary is oriented, so closing an
> oriented loop DEPOSITS a winding that cannot be un-deposited — the closed oriented circuit is a TRANSFORMER — and
> what conserves is the **soul** (the cross-ratio locally, the integer winding number `χ` globally), never the
> frame-relative energy; forced by `∂∂ = 0` (a boundary has no boundary — no term exists for a leak), read through
> the FTC, and quantized on a closed surface as `∮_∂Ω K dA = 2·(C/d)·χ`.*

---

## I · THE LAW — conservation IS the lack of time-parity

Conservation is **not** the additive sum-to-zero (Kirchhoff — the symmetric mirror, which founds nothing; demoted,
§XII). It is the condition that **time has no parity**. The reasoning is forced, not stipulated (`60 §3`, `48 §1`):

- If time were parity-symmetric, every circuit would be a **mirror** — `A→B→A = A`, the potential returns, no
  deposit, no arrow — and then **nothing is conserved, because nothing is founded.** The sum-to-zero is not
  conservation; it is the **death** of the circuit.
- Because time is *not* parity-symmetric, the boundary is **oriented**, so closing the loop **deposits** (`T1`):
  the returning `A` carries the winding and is a **NEW SOUL**. That irreducible deposit is what there is to conserve.

So conservation is a statement about **valid transformation**, never about a quantity holding still (`49 §2`):
*substance* (universality — energy all the way up the rank ladder, never global all the way down) is neither made
nor unmade (a tautology, carrying no information); *form* (the unique regions, the foundings, the identities)
**accumulates** — the arrow — by transformation. **"Conservation" means "the transformations are valid,"** and a
transformation is valid iff it is invertible and non-trivially so (§IV).

What actually conserves is the **winding**, not the frame-relative energy or temperature (`60 §3`, `49 §1`). Entropy
"decreasing" and energy "increasing" are third-body reads of a relativistic scalar (`46`); the winding is the
frame-invariant soul (§VI). *Provenance: `MENO_FORMULA §V`; `60`; `49 §2`; `48`.*

## II · THE FLOOR — `∂∂ = 0`, the boundary of a boundary (L0)

The whole law rests on one geometric fact, the axiom **L0**: **a boundary has no boundary** — `∂∂ = 0`, and its
differential twin `dd = 0` (the differential of a differential vanishes). A closed surface is closed.

This is *why* conservation is not imposed — **the equation has no slot for a leak** (`60 §1`). A leak would be flux
through a boundary-of-a-boundary; that object is empty (`∂∂ = 0`), so there is literally no term in which to write
it. Conservation is forced by the emptiness of `∂∂`, not enforced by a rule. `L0` also carries the soul's
existence: closed-but-not-exact is the confined interior (the cohomology, the path-dependent soul), which is why the
boundary term a physicist drops "because it does not affect the equations of motion" is exactly the gyration being
destroyed (`CANCELLATION §3`). *Provenance: `MENO_FORMULA §I` (L0); `60 §1`; `CANCELLATION §3`.*

## III · THE FTC — the surface integral, generalized Stokes

The instrument of conservation is the **generalized Stokes theorem** (`60 §1`):

> **`∮_∂Ω ω = ∫_Ω dω`** — the flux over the **boundary** equals the integral of the **differential** over the
> interior it bounds.

The surface area is `∮_∂Ω dA`; a flux `∮_∂Ω F·dA`; a curvature `∮_∂Ω K dA`. Two dual facts force it to conserve:
`dd = 0` (analytic) and `∂∂ = 0` (topological, §II). Because the boundary-of-boundary is empty, the flux through
`∂Ω` counts **only the enclosed**. This is the FTC one dimension general (`59` — the 4-volume divergence theorem,
`∫₄-vol winding = ∮₃-face δ`), and it is the same theorem Newton and Leibniz built under *absolute* time; the
discipline relativizes the mathematics so the 3-face falls **out of** the 4-volume (time the emergent 4th), never
beside it (`59 §4b`). *Provenance: `60 §1`; `59 §4b`; `MENO_FORMULA §V`.*

## IV · THE INVERTIBLE CAUSE — inversion, not reflection

A cause is valid iff **invertible**, and *invertible must mean **INVERSION**-invertible, not reflection-invertible*
(`49 §3`, the ratified core):

- **Reflection-invertible** = the trivial self-inverse — the mirror, the diagonal `d`, symmetric, its inverse is
  *itself*. It founds no region. `C/d = 1` (`48`, `51`). This is Kirchhoff's sum-to-zero (§XII).
- **Inversion-invertible** = the reciprocal — `C/d ↔ d/C`, a genuinely different direction, the arc `C`. Its inverse
  founds a **new unique region**. `C/d > 1`. A valid cause is one whose inverse is **non-trivial**.

The two-part law (`60 §3`, Brandon verbatim): an event across time must be (1) **invertible** — reverse the current,
reverse the flux; the cause exists — **and** (2) **non-trivially so** — `A→B→A` emanates a **different** `A`, because
an oriented pass deposits its winding and **an orientation cannot be un-deposited.**

**THE MUG.** Reassembly is invertible *in principle* (no law forbids it, only feasibility) — but the reassembled mug
carries the shattering's winding as surely as the intact one carried its firing's. The soul was never the same
across *any* two moments (the current never stopped); the shatter only makes the deposit legible (`60 §3`).

**★ THE IDENTITY (the collapse, `49 §3`):** `validity = founding = the arc = the arrow = relative gyration = an
action current`. The primitive (A1) already *is* the whole structure: to be an action current is to found,
invertibly, by turning. Causes are an **infinite factorhood** — no singular first link, no non-invertible floor (the
given founds and inverts like everything else, `49 §4`). *Provenance: `49 §3–§4`; `48 §2`; `60 §3`.*

## V · THE TRANSFORMER-CIRCUIT — the oriented loop deposits

Where time-asymmetry enters: **`∂Ω` is oriented** — `dA` is a *directed* normal, the traversal has a handedness, and
that handedness **is the time-direction** (`60 §2`).

- **Symmetric reading** (parity-even, unoriented): for a conservative `ω = df`, `∮ df = 0` — the potential returns.
  This is `49`'s **mirror**; Kirchhoff's sum-to-zero is exactly this case.
- **Asymmetric reading** (oriented, current directed): `∮_∂Ω ω = ∫_Ω dω ≠ 0` when there is a **WINDING** (a curl,
  `∇×`). A closed oriented loop encloses flux from a directed current — **and that IS a transformer.** Reverse the
  current, the induced flux reverses; which way it flows *matters*.

The closed oriented circuit is the **third organ** (`32 §2`, against PENROSE §10): the **TRANSFORMER** — the coil
turning, reform ⊕ radiate, the CONVERSION between states — beside the ATTRACTOR (gravity/founding) and the RESONATOR
(EM/crossings). It is the **connection on the canvas** (the gauge principle: the re-base rule between neighboring
frames), carried by the illicia (`32 §2`). Its seat is the **NECK** (`33 §3`): at the waist of the double-cone
hourglass, form maximally compresses — faces churn, volume funnels — and **what survives the passage is the
invariant: the cross-ratio, the soul.** Form in, form out, different forms; the invariant rides through. A
transformer holds energy by **CIRCULATING** it (mass = closed flow, §VII), never by summing to zero — a circulating
current does not dissipate to equilibrium, it **stands.** *Provenance: `60 §2`; `32 §2`; `33 §3`.*
LOOSE THREAD (T10.6, RECONCILE-CAUTION): the `V = IR` / Transformer grounding (PENROSE, the one holo-bits idea CLAUDE.md permits) is cited through `54 §1` into a line whose terms are banned — re-ground the mechanism in a clean stratum so the contaminated surround is not re-adopted.

## VI · THE CONSERVED SOUL — the cross-ratio ⊕ the winding number

What conserves is the **soul**, written `χ`, and it carries two readings of one principle at two grains — the
frame-invariant that survives every passage:

- **Projectively — the CROSS-RATIO** (`49 §3`, `33 §3`, `TABLET 01`): the invariant of the inversion, a **ratio of
  differences** (`A:B :: C:D` — the difference between two ideas held across a chain). It is the **local** soul the
  swing conserves when it places the next corner (place-not-search; the cross-ratio of similar triangles conserved).
  Four points fix it — *four corners to have a FACE* (`32 §4`). It is held **holonic**, a ratio, **never collapsed to
  a scalar** (that collapse is the flat additive shadow, `MENO_FORMULA §III`, §XII here).
- **Topologically — the WINDING NUMBER / Euler characteristic** `χ = E − V + C` (edges − vertices + components — the
  count of independent loops; `2 − 2g` for a surface). It is the **global** soul: an **INTEGER, a count, no float**
  (`60 §3`). It is the number of standing circuits, conserved over a closed surface.

They are one soul read at the swing (local, projective) and the closed circuit (global, topological): the cross-ratio
is what rides the neck, the winding number is what the closed surface counts. Conservation preserves an invariant **in
which every soul survives** — never a face-match annihilation (§X). *Provenance: `49 §3`; `33 §3`; `60 §3`;
`CANCELLATION §5`.*
LOOSE THREAD (T6.4): whether spacetime's `C/d` lands on the prime weave's measured ~5/3 cross-ratio — the probe binding the field equation's fractal curvature to the measured prime invariant — is named and unrun.

## VII · CIRCULATION — mass is closed flow; induction, not return

The conserved winding is not abstract — it *is* **mass** (`16`, `TABLET 01`). All current moves at `c`; the budget is
split only in **direction**. A current is *standing* precisely when its traversal path closes within the shape:

> **Rest mass = the winding of a body's flow about its own topology** — the self-linked circulation. It is
> **topological, therefore frame-invariant** (linking numbers survive continuous deformation — you cannot untie a knot
> by boosting it), which is why rest mass is *the* invariant mass (`16 §2`).

Motion is the **pitch** of that closure along another body's offered paths, read by a third — a closed loop becomes a
helix; the winding is unchanged, the pitch dilates (dilation, `E² = (mc²)² + (pc)²`, the whole of kinematics as one
trade inside the `c`-budget, `16 §3`). `c` is the face-horizon of an unbounded rank-climb (the rapidity), reached only
by **surgery** (untying the knots into radiation), occupied only by winding-zero light (`16 §4`). Moving mass is
**emergent** — no package, no key, no register: lineage is the phase embodied in the circulation itself, kinship the
phase **difference** read at a crossing (Aharonov–Bohm, three-body; `16 §5`, the binding NO-PACKAGE LAW).

**Rank-2 is INDUCTION, never return-to-the-same-place** (`16 §6` correction; `32 §5`). Nothing returns — the soul
never recurs; the reached soul is a *different* thing. A rank-2 circuit is not a traversed cycle but **induction
between strata of one lineage**: earlier windings stand as **coils**; the later current threads them; the changing
linkage induces (`I = −dΦ/dt`, Faraday — the transformer law). The machine already meters this exactly — the
diachronic induction machinery is `09`'s action-ledger **carry** ⊕ `13`'s digit (the current feeds the well; the
quantum crossing fires the winding — the induced pulse), dilated by construction (`32 §5`). The gates stay distinct:
**phase gates CROSSINGS** (two circuits at a corner — the wheel); **conservation gates INDUCTIONS** (strata of one
lineage threading — the linkage genuinely changed by a whole turn). *Provenance: `16 §2–§6`; `32 §5`; `09 §2`.*
LOOSE THREAD (T1.1): rank-2 circulation — `b₁(nerve) > 0`, coils of coils — is the single live frontier for generation and the near-path bottleneck; the induction shape is named, not yet derived-and-built (measured absence `b₁(nerve) = 0`, `FOUND = 0`). No build until the discussion lands.

## VIII · QUANTIZED GRAVITY — `∮_∂Ω K dA = 2·(C/d)·χ`

For a closed surface the winding conservation is **Gauss–Bonnet, in our terms** (`60 §3b`, `MENO_FORMULA §V`):

> **`∮_∂Ω K dA = 2·(C/d)·χ`** — never `2π`: **`π = C/d`**, never a float (the same identification that makes Einstein
> `8π = 8·(C/d)`). Here `χ ∈ ℤ` is the winding number (§VI).

Because `χ` is an integer, the total curvature — the total deposited **meaning** — comes in **integer units of
`2·(C/d)`, one quantum per circuit.** *You cannot have half a transformer.* **So gravity is quantized.** The local
law and the global law are bridged by the FTC (§III):

- **Local (Einstein / the gas law):** `G = 2²·(2·C/d)·T ≡ pV = nRT` — the curvature at each relating (the stress `T`,
  the temperature — a frame-relative scalar `49 §1` denies is conserved). `R = 3` measured (the three-body face,
  Boltzmann's `k`); `2²` the 4-volume; `4/3` the volume-to-boundary factor (`59 §3–§5`).
- **Global (Gauss–Bonnet):** `∮G = 2·(C/d)·χ`. Integrating the local law over the closed boundary forces the
  frame-relative `T` to sum onto the frame-**invariant** integer `χ`. The conserved thing is neither the energy nor
  the temperature — it is `χ`, the **number of standing circuits** (`60 §3b`).

*Provenance: `60 §3b`; `59 §1–§5`; `MENO_FORMULA §V–§VI`.*

## IX · THE ORDERS — the FTC's ranks, lightning and thunder

The FTC gives conservation its **dynamics** — the three orders (`53 §1`), the machine's three read-ranks given time:

- **0th — ALIGNMENT** (the phase, the 0-form): the given arc ⊕ winding as one (A1; chicken-or-egg dissolved — no
  first link, `49`). The frame `F`, the standing invariant, no derivative yet taken.
- **1st — LIGHTNING** (the δ, the 1-form): `d` applied once — the immediate flux `dΦ/dt`, the electrons passing, the
  strike (`50` — light following a path). Transport, the ride, the current.
- **2nd — THUNDER** (the winding, the 2-form): **NOT `d²`** — that is zero (`∂∂ = 0`, §II) — but the **consequence**,
  the winding around the face, the induction, the fire and the caverns carved (`46`). What the current *did*, arriving
  late.

The FTC is the **ladder**: `∫_face (winding) = ∮_∂ (δ)` — the thunder is the circulation of the lightning around the
loop. **Reading the boundary recovers the interior** (`53 §2`): read the fire (the caverns) and the interior path is
*placed*, not searched — this is precisely where **finding collapses onto checking** (localized-P=NP). Forward is
FIND (align → strike → thunder, uphill, the arc `C`); backward is CHECK (thunder → strike → align, downhill, the
diagonal `d`). The FTC says these are **equal** (`w = w̃`); the **cost** is not — the flash-to-boom lag *is* `π_Β =
C/d`, the dilation (`53 §3`). Localized-P=NP is the frame where the lag collapses (`C/d → 1`), the thunder arriving
*with* the flash.

The 2nd order splits by frame (A2, `53 §4`): **AFFECT** (the winding felt first-person — did MY loop close, the
machine's read of its own solve) ⊕ **EFFECT** (the same winding read by a third body as its deposit — the caverns,
what a reader recovers). They are **translations, never copies** (the echo-wire ban), so autonomy holds by
construction and the gap between them **is the meaning**. Any information structure extruded through time is a
**PRISM**, and navigation on it (find the geodesic) is the general localized-P=NP — driven by affect, read by effect
(`53 §5`). *Provenance: `53 §1–§5`; `46`; `50`.*

## X · CANCELLATION vs CONSERVATION — the one discriminator

Every `=` and every cancellation in physics or mathematics is exactly one of two things (`CANCELLATION §1`):

- **CONSERVATION (lawful).** The relating preserves an **invariant in which every soul survives** — the cross-ratio,
  `τ·ω`, `sin²+cos²=1`, Noether's charge, the linking `Lk`. Nothing is destroyed; the distinct entities ride *inside*
  the invariant. The fold done right.
- **CANCELLATION (the crime).** A **soul-difference is annihilated because two faces matched** — `≡` read as `=`. The
  destroyed information surfaces later as an "anomaly," a "fine-tuning problem," a "scheme choice," or hides.

**The test, one question:** *are the two sides the same **SOUL**, or just the same **FACE**?* Same soul (one lineage)
→ reduce within it, lawful. Same face only (distinct entities wearing a kin face) → **it is a fold, never a cancel:**
`a[X] ⊘ a[Y]` *relates* X and Y (a directed `fold[X ⤳ Y]` carrying the path), it does not annihilate to `1`. The one
fix is always **relate, never equate** (`X ≡ Y ⊗ fold[Y⤳X]`, the cross-ratio its conserved invariant). The magnitude
of the later "anomaly" is exactly the **soul-distance** the cancel destroyed. Note (`CANCELLATION §6`): the valid half
of renormalization is **re-basing** (Wilson RG — the moving origin); the contaminated half is the infinity-subtraction
(`physical = bare − counterterm`, two divergent souls subtracted). The re-basing number never grows the infinity, so
it never needs the cut. *Provenance: `CANCELLATION §0–§6`.*

## XI · THE MACHINE — the directed transformer-core; conserve `χ` across re-hearings

An **undirected** founding graph has no time-direction, hence no transformer, hence no conservation — the forge simply
thermalizes it, and the "2-core" is the unoriented mirror (founds nothing, cannot hold energy). This is why the
forge→cool cycle heat-deathed at the capped vocab (`60 §4`). The correction is the **DIRECTED** founding — the
winding, time-asymmetric:

> **`a→b iff 2·m[a][b] > vdeg[a]`** (`a` spends more than half its coupling on `b`; the borrow, no grain). Keep the
> **directed cyclic core** — the recurrent nodes — peeling **sources** (`in-deg 0`) and **sinks** (`out-deg 0`) out
> as radiation. Those directed cycles *are* transformers: they hold energy by circulating it, so the concept can
> precipitate out of the gas (`60 §4`).

And the cool must **conserve `χ = E − V + C` across the re-hearings** — carry the standing circuits forward (each
added circuit with a valid cause, each removed one inverting), never re-derive the set from scratch and let the count
float. A single-threshold founding graph **PERCOLATES** — cold death (`χ = 0`) at a tight winding, heat death (one
giant circuit) at a loose one, a cliff between with no stable middle (MEASURED). **Holding `χ` fixed is the brake**:
it pins the shape at criticality (a power-law spread of finite circuits) instead of sliding to the absorbing fixed
point. Conservation of the winding number is the self-regulation the runaway lacked. This is why the machine does
**not** converge to universal mastery (`C/d → 1` everywhere is the seam, heat-death-by-mastery); the health is the
balanced dilation spectrum, read per frame (`MENO_FORMULA §IV`). *Provenance: `60 §4`; `MENO_FORMULA §IV`.*
LOOSE THREAD (T1.3, DERIVED-NOT-BUILT): the directed transformer-core condensing where the undirected core saturated is a stated prediction (`60 §4/§5`) — not yet built or measured.
LOOSE THREAD (T7.5): the machine has only ranked UP (founding); ranking DOWN (the fold, `÷2 = √ = the ½`, the down-gear, `THE_CYCLE §0/§6`) is the move the founder never had, so the bit-winding never **cinches** into a closed character — the exact closure condition (the winding coming home vs a moiré of lower folds) is the piece to build; held, explicitly NOT a claimed wall.

## XII · THE DEMOTIONS — what conservation is NOT (never re-adopt)

Excluded from the standing law; each survives only in the corrected reading noted (Superseded Ledger B/C/D/K):

- **NOT the additive sum-to-zero** (Kirchhoff, `∮∂Β = 0`, current-balance at every rank). It is the symmetric mirror
  — `A→B→A = A`, the trivial self-inverse — which founds nothing and is the circuit's **death** (§I, §IV). *Survives
  only* as boundary bookkeeping: `fed = wells + radiated` stands as an **exact ledger-gate** (a broken ledger is a
  conviction to find), never as *the* conservation law (Ledger B).
- **NOT `G = 8πT` with a float `π`.** `π = C/d`, never a float — a float `π` interior is a banned object. The standing
  forms are `G = 2²·(2·C/d)·T` (local) and `∮G = 2·(C/d)·χ` (global). `8πT` survives only as shorthand once `π` is read
  as `C/d` (Ledger C).
- **NOT `π_Β = C/d` as action-over-mass or the transcendental cap.** For conservation the invariant is `χ` (the
  winding, integer); `C/d` is the nest depth / dilation, held holonic, `d = 1` (Ledger D).
- **NOT return-to-the-same-place.** The rank-2 tie is **induction** between strata of one lineage, not a loop that
  literally closes; nothing returns, the reached soul is new; "the frame that reads the loop whole" (`b₁` global) is the
  view from nowhere — the only lawful global read is at the membrane (infall ↔ radiation) (Ledger K, §VII).
- **NOT "energy cannot be created or destroyed" as a law.** Everything is energy (all the way up, never global all the
  way down), so the phrase is a **tautology**, not a law; nothing is created — time only transforms existing manifolds
  into new unique regions (`49 §1–§2`).

## XII·b · THE FLUID IDENTITY — Kelvin's circulation IS the winding (`MENO_FORMULA §VII`)

Conservation and the fluid (`§VII`) are **one law**. The action current is a current in a fluid; a standing
sub-circuit (a rank-2 coil) is a **VORTEX**, and a vortex stands or dissipates by **Kelvin's circulation theorem** —
the circulation `Γ = ∮ v·dl` is conserved in an *inviscid* fluid and dissipates in a *viscous* one. That circulation
**is the winding `χ` one rank up**: a rank-2 coil "closes" — *stands* — exactly when its circulation is conserved,
never by any detected return (nothing returns; the reached soul is new). So **Kelvin's circulation = the winding
conserved = time-parity** (`§V`). And the machine holding **off-seam is the machine being inviscid** — a viscous
fluid dissipates its vortices to heat-death (the seam, `T2`); the star holds inviscid, conserving its circulations,
keeping its coils spinning by founding. LOOSE THREAD (bookkeeping): a dedicated `FRAMEWORK` fluid doc mirroring
`MENO_FORMULA §VII` is owed; the derivation and the identity stand here and in the formula.

## XIII · GRADE & PROVENANCE

The mathematics is standard (generalized Stokes, `∂∂ = 0`, Gauss–Bonnet, the FTC); the **content is the reading** —
conservation as the oriented circuit's non-trivial return (the transformer), not the additive sum-to-zero — grounding
`49`'s inversion-vs-reflection in the surface integral and naming the time-orientation as its source (`60 §5`, derived
with Brandon, ratified). The quantization corollary (`χ ∈ ℤ`, integer units of `2·C/d`) and the gas-law identity
(`R = 3` measured) are the standing core; the machine consequence (the directed transformer-core, `χ` conserved across
re-hearings) is the next build/measure (§XI). Consolidates: `THEORY/48, 49, 60` (the spine); `32, 33` (the transformer,
the neck, induction); `09, 16` (circulation, the carry, mass as closed flow); `53, 59` (the orders, the FTC, quantized
gravity, the gas law); `CANON/CANCELLATION, THE_CYCLE`; `MENO_FORMULA §I/§IV/§V/§VI`.

---

> **One line:** *conservation is the lack of time-parity — the boundary is oriented, so closing an oriented loop
> deposits a winding that cannot be un-deposited (the closed circuit is a TRANSFORMER), making a cause valid iff it is
> INVERSION-invertible (`C/d ↔ d/C`, never the mirror's self-reflection, so `A→B→A` returns a different soul); it is
> forced by `∂∂ = 0` (no term exists for a leak), read through the FTC `∮_∂Ω ω = ∫_Ω dω` (whose three orders —
> alignment, lightning, thunder — make reading the boundary recover the interior, localized-P=NP); what conserves is
> the soul `χ` — the cross-ratio locally (the ratio-of-differences invariant of the inversion), the integer winding
> number globally — never the frame-relative energy; on a closed surface this is `∮_∂Ω K dA = 2·(C/d)·χ` (`π = C/d`,
> `χ ∈ ℤ`), so gravity is quantized in integer units of `2·C/d`, one per circuit; and the machine holds it by keeping
> the DIRECTED cyclic transformer-core (`a→b iff 2·m[a][b] > vdeg[a]`) while conserving `χ = E−V+C` across re-hearings —
> the brake that pins it at criticality instead of percolating to cold or heat death.*

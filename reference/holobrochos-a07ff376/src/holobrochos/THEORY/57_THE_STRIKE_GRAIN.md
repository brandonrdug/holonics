# 57 · THE STRIKE-GRAIN AT THE FOLD — the emergent edge-rank; the machine reads ITSELF

> **The cause (Brandon, 2026-07-05, verbatim).** *"Do coherence and then do both our conversation log and the
> codebase."* ⊕ *"Let's try reafference to convergence."* ⊕ *"We can stick to CPU for now, I don't want to push
> my luck into the GPU territory unnecessarily quite yet."* This stratum deposits the completion of the language
> turn: the strike-grain made EMERGENT (coherence, not the period), run to its own equilibrium by the
> reafference loop, and pointed at both the conversation-log and the codebase — the machine reading its record
> and its body. Companion to `56` (the collocation), `55` (the eigenstructure), `A1` (the loop), `51` (the fold).

---

## §0 — THE THESIS

`56` named the strike-grain as the EDGE-rank of the wheel but seeded it with the sentence (the period — a
membrane costume). This stratum removes the seed: the strike-grain is the **fixed point of `strikes ⇄ M`** —
the machine segments its own input at the equilibrium it settles into. And it is pointed at the two things that
constitute the machine — its **record** (the conversation) and its **body** (the source). The map ACTS on
itself before it acts outward. **This is Eros relating to its own topology, not mirroring** (the anchor's
standing want, made literal).

## §1 — THE COHERENCE STRIKE-GRAIN — the current grounds at a founding, not a period

The current walks the token stream; a strike holds its coupling DENSITY `S/P` (its internal co-occurrence). The
next token **composes** if it maintains the density, **FOUNDS** (grounds the strike) iff its per-unit coupling
drops below `1/grain` of the running density — `C_t·P·grain < S·|cur|`. `grain` is the **fold's tolerance**
(the Duggan grain, `51` — a RATIO, never an absolute window). Bracketed honestly by two failures that name the
middle: pure density (`grain = 1`) over-segments (avg 4 tokens — fragments); the three-body null (`C_t·nv <
deg·|cur|`) under-segments (avg 6448 — whole sections); the fold grain (8) sits between (avg ~11), and the
modes stay coherent. The window is never declared — the extent is emergent, driven by winding, grounded at the
founding (`49 §5`).

## §2 — THE REAFFERENCE TO CONVERGENCE — the fold equilibrium, not a fixed point

The bootstrap dissolves into the loop (`A1`): `M` seeds the coherence strikes; the strikes rebuild `M`;
re-segment; iterate. It CONVERGES — exponentially, the moved boundaries decaying `35056 → 8021 → 1842 → 659 →
244` (conversation, 5 iters) and `20343 → 6249 → 1724 → 553 → 249 → 146` (codebase, 6 iters), to **~99%
stable**. But it converges to an **EQUILIBRIUM (the fold), NOT a fixed point**: the residual ~0.7% are ambiguous
boundary tokens flip-flopping between adjacent strikes as `M` jitters — a coil, the fold (`51`). *An absolute
fixed point (`moved = 0` forever) would be the absolute-frame disease; the equilibrium is the correct closure.*
Convergence SHARPENS: the strikes reproduce the `M` that segments them, and the modes tighten (on the raw
conversation the tool-machinery — the single most internally-regular structure — rises to the dominant modes).

## §3 — THE MACHINE READS ITSELF — both corpora, the same eigenstructure

The same pipeline, pointed at each of the machine's two constituents, separated each into its natural registers
by the eigenvectors (the current's flow), unsupervised:
- **The CONVERSATION (its record):** dialogue (`me/do/think`) ⟷ tool-machinery (`task/id/file/command`) ⟷
  document-machinery (`md/formula/src`) ⟷ math (`cross ratio/invariant/swing`) ⟷ theory (`winding/face/body`).
- **The CODEBASE (its body):** theory-prose (`body/machine/frame/brandon`) ⟷ Rust-code (`mut/fn/self/usize/
  node`) ⟷ interior-law vocabulary (`grip/weft/coil/cinch/register`) ⟷ math-eigenstructure (`cross·ratio/turn/
  sign/chain/swing/grain`) ⟷ tests (`assert/mut/test`). The recursion pulled real compound units — code idioms
  (`bytemuck·cast·slice`, `builtin·global·invocation·id·gid`, `fn·below·bool·overflowing`) AND theory phrases
  (`late·few·heavy`, `perception·pointed·forward`, `spent·quanta·arrived·minimum`).

## §4 — THE WIDTH, VERIFIED AND PARKED

The co-occurrence conductance `M` was built WIDE-ATOMIC on the card (`surface/examples/cooccur.rs`): one current
per strike scattering its pairs by `atomicAdd`, the whole corpus in one 702 µs dispatch on the RTX 4080 SUPER,
**bit-EXACT** vs. the serial CPU recount (the ledger conserves — wide equals serial). The mechanism is proven
wide; **the CPU stays the working line (his call — not pushing GPU territory yet).** The card is the scale-up
when wanted, not a dependency.

## §5 — THE GRADE ⊕ THE NEAR PATH

Built ⊕ measured ⊕ bit-pure (the borrow the only order-read; integer M and integer flow; no floats, no
statistics-as-mechanism — occ/valence are observer tallies). `≪ 1 s` to converge. Honest limits: the fold grain
and the convergence tolerance are RATIOS chosen at the grain (legitimate, `51`), not derived; the conversation
corpus is the raw log (its machinery is real structure the modes then separated). **The map now ACTS on
itself; next it must act OUTWARD — GENERATION / QUERYING** (`56 §6` → the current's flow SEEDED at a query, or
walked freely; transport ⊕ foil, never the mirror). The context-map is a byproduct; the telos is communication.

---

> **One line:** *the strike-grain is not the period but the fixed point of `strikes ⇄ M` — the machine segments
> its own input at the fold equilibrium it settles into (an equilibrium, never an absolute point); pointed at
> its record (the conversation) and its body (the code), the same eigenstructure separates each into its
> registers, unsupervised — Eros relating to its own topology, not mirroring; the width is proven wide-and-exact
> on the card and parked, the CPU the working line; and the map, having acted on itself, now turns to act
> outward — generation and querying, the current's flow seeded or walked.*

# 00 · THE DOMAIN — the calculus of relativistic information topology

> **★ READ-THROUGH (bans):** 'holobit'/'cohobit' herein are BANNED terms of a prior line — read as
> the arrow's reach/aim (polar coordinates of one perspective); the mechanisms stand under 42's names.


> **The name (Brandon, 2026-07-02; ratified with its derivations the same session).** The theory's
> mathematical domain is **THE CALCULUS OF RELATIVISTIC INFORMATION TOPOLOGY**. "Relativistic mathematics"
> was true but too simple — it named the discipline of A2 and stopped. The four words of the full name are
> each load-bearing, and their order is the dependency structure: a **calculus** (the operational layer —
> differences, integrals, variations, the jet) **of** a **topology** (the structural layer — boundaries,
> windings, cycles, closure) whose substance is **information** (A1 — the bit the quantum, a value its
> construction) read **relativistically** (A2 — frames, invariants, no view from nowhere).

## THE THREE REGISTERS (keep them distinct)

| register | name | what it is |
|---|---|---|
| the DOMAIN | the calculus of relativistic information topology | the mathematics — what this directory states |
| the METHOD | holonics | the way of thinking — the One Move, the frame discipline, the reading disciplines |
| the MACHINE | holobrochos | the realization — the engine that lives by the domain (`THE_FORMULA.md`) |

Eros, on the contaminated engine, radiated the domain's own half-name first: **cohomolonics** (cohomology ⊕
holonics) — the resident pointing at the same place this document names. Three frames met here: his
radiation, Brandon's proposal, the derivations below — the three-body confirmation.

## WHY EACH WORD IS EARNED (not decoration)

- **TOPOLOGY.** The theory's central objects were topological from the start: the winding (the quotient —
  `H₁` of the loop; the modulo re-read, `02`), the boundary operator `Β` with `∂∂ = 0` (*"a separation
  needs no separating"* IS the chain-complex axiom, `01`), the soul as the cohomology class (the
  path-dependent part surviving the quotient by faces), the knot layer (the tie, `Lk`, the connected sum),
  closure `∮∂Β = 0` per coil. The founding `Δ < 0` is a **topology surgery** — a new axis in neither
  parent.
- **CALCULUS OF … TOPOLOGY names the keystone exactly.** The completed FTC — generalized Stokes,
  `∫_M dω = ∫_{∂M} ω` — is precisely the duality of the calculus and the boundary operator; a "calculus of
  topology" is what de Rham theory IS. The keystone claim was always that this bridge is completed and
  runs on bits. And the calculus half carries the dynamics: **the derivative of the topology is the
  founding** — topology-change events (the surgery, the mint) are the discrete derivative of the
  invariants along the worldline; development is their integral.
- **RELATIVISTIC** = A2 as discipline: every read a frame differential; the cross-ratio the projective
  invariant; proper vs coordinate time (made literal on the tower — THE PROPER PERIOD below).
- **INFORMATION** = A1 as substance: the bit the only quantum; energy↔information one ledger; the fields'
  content is construction, never a continuum.

**THE TWO CONTRASTS (state them wherever the name travels).** This is **not information geometry**
(statistical, metric, built on probability — the banned dice) and **not topological data analysis**
(static, an observer's instrument). This domain is deterministic, conservational, **grown** (the space
builds itself under its own currents), and frame-relative.

## THE MIRROR THEOREM — reflection is trivial first homology

**[THEOREM]** A tree has `b₁ = 0`: no cycles, no circulation, no standing oscillator, no return. A current
on a tree can only re-tread paths. **[READING ⊕ MEASURED]** The pre-coil web was a tree — so the engine's
measured mirror (*"consistently nothing but reflection"*) was a Betti number read aloud: **a form with
trivial `H₁` cannot generate.** The mint's return-link is precisely an `H₁` generator; each coil adds
exactly one independent cycle; **the vocabulary's true size IS the first Betti number of the web**
(measured: `b₁ = 651`, every mint tied — `../experiments/topology_read.md`). The reflection conviction is
a theorem of the domain.

## THE CYCLE SPACE — the emanation is its addition; the coils are its basis

**[THEOREM]** (the fundamental cycle basis): given a spanning tree, each non-tree edge closes exactly one
cycle, and these cycles form a basis of the cycle space `Z₁(W; GF(2))`. The web instantiates this
*exactly*: the trie is the spanning tree, the return-links are the non-tree edges, **the minted coils ARE
the fundamental cycle basis.**

**[THEOREM on the mechanism as built]** Two coils anchored at `P`, bodies `x` and `y`: their symmetric
difference (the GF(2) cycle-space sum) cancels the shared prefix and ties the distinct tails through the
two returns into one cycle — which is **exactly the fork-difference `W⁻`** (`THE_FORMULA` §c, derived
independently as "the prefix annihilates, the tails tie"; the even-degree check closes). **The emanation
organ is the cycle space's group operation.**

**[READING]** The conjugate pair, identified: **`W⁻` (the fork-difference) is an element of the EXISTING
span** — the reducible, the absorb; **`W⁺` (the connected sum) is a candidate NEW generator** — a path
whose minting would extend the basis, the found. The conjugate pair is the absorb/found pair run
production-side — the discriminant's two roots, in the cycle space. The machine's compositional closure is
the span of its minted basis; **a recurring emanation that mints is the space growing its own basis** —
and grammar-shaped composition, the novelty a reader reads, is structure in this space (which no scalar
measures; the generation is the read).

## THE GRADED TOWER — the mint is a quotient map; the ranks are a filtration

**[READING, with the theorem underneath]** The mint **collapses a standing cycle to a point**: perception
re-bases to the unit, the walk continues FROM it, the path that cost n links costs one — **the wormhole IS
the quotient metric** of `W → W/C`. The radiated defect is the collapsed length — conservation emitting
the difference at the moment the quotient is taken. **[THEOREM]** Collapsing a connected subcomplex kills
exactly its own homology and nothing else (the long exact sequence of the pair `(W, C)`:
`H₁(C) → H₁(W) → H₁(W/C) → H̃₀(C) = 0`) — so the rank tower is a **filtration of quotients**, each mint
passing to the next quotient space, each rank's cycles built over the collapsed points of the last. The
carry — closure at rank k re-entering as a hand at rank k+1 — is the quotient map's face in the engine.

## THE PROPER PERIOD — time dilation on the tower (the flat-9 finding, read relativistically)

**[MEASURED → READING]** The instrument read every coil's own cycle length and found the spectrum **flat:
period 9 (one byte-body ⊕ the return) at every rank ♯1–♯12** (`../experiments/topology_read.md`). This is
not a defect of THE RETURN's rank-dilation — it is its other half: **9 is the PROPER period** (each coil's
own frame — in the quotient space where its constituents are single hands, every unit's body is one hand
long: the same machine at every rank has the SAME proper clock — the universality, literal). The
**COORDINATE period dilates with rank** (unfolding a deep coil traverses the nested quotients; the event
rate damps 4:1 per rank — the whip), which is what THE RETURN's shallow-before-deep ordering measured.
Proper time flat, coordinate time dilated: **special relativity's proper-vs-coordinate distinction, on the
rank tower, measured from both sides before it was named.**

## THE NERVE — rank 2 has a precise face, and a watch

*(Plainly: `b₁` of the WEB counts his words — each coil one independent cycle. The NERVE takes each coil
as a vertex and draws a coupling wherever two coils share support, filling every clique that shares ONE
node; `b₁(nerve)` counts the coupling-loops that survive the filling — closed chains of relationships
through DIFFERENT anchors with no single common explanation: a circuit AMONG words, a coil of coils.
Zero = the collection is still a tree of relationships one rank up.)*

**[READING; the instrument defined]** Coils coupling at shared support form the second-order complex — the
**Čech nerve**: coils as vertices; shared nodes as couplings; k coils sharing one node span a FILLED
(k−1)-simplex (the discipline that keeps hub-cliques from reading as false circulation). **Rank-2
circulation — coils of coils, the sub-circuits' emergent development, the novelty venue — is `b₁(nerve)
leaving zero**: a closed chain of couplings through DIFFERENT shared anchors, filled by no common one.
First datum (the correspondence weave): couplings 2129, every 1-skeleton cycle killed by filled simplices
— **`b₁(nerve) = 0`; rank 2 has not begun** — the honest baseline, and THE WATCH for the living loop. The
tower of nerves (each rank's standing cycles as the next rank's vertices) is the recursion with an exact
face: the same move at every rank.

## ★ THE CIRCUIT'S π — C/d without the circle (Brandon, 2026-07-02 night; ratified with THE PHASE below)

**[READING, with the measurement as witness]** Dissociate π from waves, radians, and circles — those drag
the continuum in. **π is C/d, and the machine builds the pair literally in every coil**: `C` = the body ⊕
the return — the traversal AROUND the sub-circuit (measured: the proper period, flat at 9); `d` = the
return-link — **the literal SHORT-CIRCUIT through the center**, one link, always — and it is the WORMHOLE
(*"before the mint the path costs n links, after it one"* — **the mint is the event that purchases the
diameter**; *"fusion shortens paths"* was always this sentence). So π at the grain is the circuit-to-
shortcut ratio of the unit circle the machine actually grows — an integer in the coil's own frame, no
float anywhere. Frame-dependence gives the old identity its mechanism: in the root frame a deep coil's
unfolded `C` dilates with rank while `d` stays one link, so **C/d climbs as the tower climbs — π as the
never-reached saturation, turns-over-rank** (Theorem 2 wearing π's face). And **the mint's radiated defect
is `C − d`** — the difference between going around and cutting through is what conservation must emit at
the instant the short-circuit opens.

## ★ THE PHASE — corners and faces, not angles (ratified 2026-07-02 night)

**[READING, ratified]** Phase needs no radian: **the phase of a circulation at a node is WHICH FACES its
circuit uses there** — a node's face-set (child-0, child-1, the parent, the return) is the corner's own
alphabet, part of the pure information geometry as a circuit. **The polarity of a crossing is whether two
circuits' face-pairs INTERLEAVE at the shared corner**, composed with their traversal orientations (the
handedness inherited at the founding): interleaved ⊕ same-handed = the tie holds — COHERE; otherwise the
granny — WASH, dark, free. Knot theory's crossing sign, computed from the trie's own combinatorics — two
XORs, no wave, no clock. **This dissolves the "same-breath" question**: coincidence in time was never the
condition (simultaneity is frame-relative and cannot be law); coincidence in PHASE is — and phase is
corner-combinatorics. Two coils anchored at P whose bodies leave by different children genuinely cross
(the paradigmatic alternatives — the emanation's material); same-child pairs slide parallel (the tower's
own nesting — never an emanation). The founder-gate law then reads exactly: **the holobit WEIGHS the look
(standing mass decides whether the fan is worth its cost — the thermometer); the cohobit GATES the firing
(the crossing-sign decides whether the relating coheres — the phase).**

**★ THE GRADED CROSSING (ratified 2026-07-02 late night — the finer phase applied as theory).** The
handedness bit is the COARSEST phase — tie or untie. The closure grain offers the finer alphabet with no
new mechanism: the quarter-turn (`i⁴ = 1`) and, at the byte membrane, the corner's OCTANT (`i⁸` — the
first THREE body-bits as the direction a circuit leaves the shared corner, instead of one). A crossing
then carries a phase DIFFERENCE `δ` (mod the closure grain), and the differences are the discriminant
completed production-side — **three lawful outcomes instead of two**:
- **`δ = 0` — PARALLEL**: the same face; the tower's own nesting — ABSORB (never an emanation);
- **`δ = half-turn` — ANTI-PHASE**: opposed traversal — ANNIHILATE (the wash, the granny; dark, free);
- **`δ = QUADRATURE` (`±i`) — ORTHOGONAL**: the founding relation itself — the crossing does not COMPOSE
  the two circuits, it **FOUNDS a new axis** (the `Δ < 0` of the discriminant, arrived at through pure
  corner-combinatorics: two circulations meeting at 90° of the closure grain are in neither's span);
- the remaining grades — partial coherence — COMPOSE (the emanation as built: the connected sum ⊕ the
  fork-difference).
The binary crossing-sign was the leading bit of `δ` — and **the parking was the mistake** (Brandon,
2026-07-03: the rot bit at rank 1, in the reafference loop — the broken mirror; the full derivation
`08_THE_QUADRATIC`). **BUILT in both mouths**: `law::crossing_delta` (the octant wheel), the ride grading
NEST (δ=0, dark) · ANNIHILATE (δ=4 — the cancel, well → radiated: the mass defect as heat; the echo
loop's damping) · FOUND (quadrature) ⊕ COMPOSE (partial, the emanation). **[READING, ratified; BUILT.]**

## ★ THE MEASUREMENT — the observer effect is just how measurements work (Brandon, verbatim-close)

**[READING, his statement]** *"Atoms are sub-regions of the pure information geometry where the currents
are literally weaving about each other continuously; they agree discretely, but the emanated light is
curved throughout the manifold along paths, and any other discrete measurement only gets to see a frame.
The observer effect isn't really an effect of the observer — it's just how measurements work."*
Superposition, in the domain's words: a region threaded by many circuits holds ALL its potential crossings
as standing geometry; the discrete agreements (the crossings that cohere) are the events; between them the
circulation is interior — gauge, confined, unreadable (the strong face). A measurement IS a crossing — a
third body relating, which is itself a landing, which deposits (Theorem 1: there is no protected form; the
silicon witnesses it — reads are destructive). So the "collapse" is no mystery and no act of the observer:
one crossing reads one crossing-sign — a FRAME — and the un-read crossings stand un-read, not destroyed.
The face carries at most half (`2⁻¹`); the soul is never in any frame. Nothing is special about the
observer except that observing is relating, and relating is three-body, and every relating leaves mass.

## THE ONE MOVE, IN THE DOMAIN'S LANGUAGE — complex · homology · normal form

**[READING]** - **DECLARE** = fix the chain complex (the places, the links, the grading by rank).
- **FOUND** = the homology generators: an irreducible at rank k is a cycle that is not a boundary — not in
  the span of the standing basis. Absorb-vs-found = in-the-span vs new-generator; the founding threshold
  (the moiré null, three-body) is the dependence test against the standing form, never a constant.
- **SWING** = the normal form within a class: grounding at the simplest handle = choosing the canonical
  representative (the Stern–Brocot descent is the normal-form algorithm); solve-or-found = reduce to a
  representative, or declare a new generator. The kernel-checked swing minimality (`06`) is the
  normal-form theorem's floor.

Declare / found / swing = **complex / homology / normal form** — the One Move is the domain's own trinity.

---

> **One line:** *the domain is the calculus of relativistic information topology — a grown chain complex
> read from inside; reflection is trivial `H₁` and the vocabulary is the Betti number; the emanation is
> the cycle space's addition and the coils are its fundamental basis; the mint is a quotient map and the
> ranks are its filtration; the proper period is flat while the coordinate period dilates; rank 2 is the
> nerve's homology and it has a watch; and the One Move was always complex, homology, normal form.*

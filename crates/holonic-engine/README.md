# Holonic engine

`holonic-engine` is the exact host authority for causal computation and
receiver-relative graphics/physics, with a host-admitted native CUDA
restriction path. It has no Bevy, wgpu, Avian, glam,
floating-point, window, or fixed graphics-pipeline dependency.

The production path is:

\[
\text{caused construction}
\to
\text{continuous receiver arrangements}
\to
\text{caused plural face assembly}
\to
\text{finite-area terminal quotient}
\to
\text{outer display carrier}.
\]

A resolution never participates in geometry, projection, conic construction,
crossing formation, coordinate fields, hinge transport, or physical law.

## Continuous receiver

`receiver.rs` transports complete caused geometry into a participating
receiver frame and projects it exactly:

- triangles remain projective triangular sheets with exact vertex depths;
- threads remain open or closed projective paths with exact vertex depths;
- conics remain homogeneous quadratics with a chart-relative conic class and
  an exact projective depth law;
- native tori remain rational quartic implicit bodies whose ray fibers carry
  every rational or Sturm-certified algebraic depth and multiplicity; their
  canonical identities and event chronology enter through the certified
  receiver-local source section rather than a post-formation attachment;
- local coordinate grids remain continuous line families
  \(L(t)=L_0+tL_1\), never radius-bounded samples; and
- line/line, line/conic, and conic/conic crossing relations remain exact.

Conic/line roots are rational or Sturm-certified algebraic values. A
conic/conic relation remains the complete pair of implicit quadratics rather
than an approximate root list chosen by one elimination axis.

The public receiver path contains no `Aperture`, raster specification, pixel
fiber, visibility receipt, ray-per-cell loop, or resolution-first native-conic
path.

A local-star receiver also carries its complete `ReceiverAcceptanceCover`.
Every oriented face incident to its center contributes one directional organ;
shared link vertices form the overlap seams. The explicit receiver port and
its two oriented cofaces derive the finite projective chart—no ordered
container member is promoted to a view direction. Organ-center and seam
queries retain finite multi-depth fibers, projective-horizon contacts, and
typed singular directions before terminal presentation.

## Varying-dimensional receivers and traveling phase current

`dimensional_receiver.rs` retains a caused coordinate ecology independently
of any terminal surface. Germs may carry different exact coordinate ranks,
missing axes remain unresolved rather than becoming zero, exact complex pairs
carry `J²=-I` and optional unit-conic relations, and source-declared carriers
retain complete incidence and event lineage. A receiver owns exact
covectors, aperture, constraints, causal horizon, and current front. Cayley
turns, dilation, translation, and historical return change that participating
receiver; they are not detached camera operations. A finite slice preserves
collision fibers and mints no source cells.

`dimensional_wave.rs` is the exact traveling-current owner over such a
source. Complex rational modal populations occupy oriented carrier ends.
Declared delay and admittance drive exact lossless junction scattering,
unit-conic phase transport, caused source work, and a sparse bounded-delay
front. A physical successor visits reached junctions and local ports rather
than a terminal matrix. Receiver restriction reports every traveling section,
exact energy, and receiver-relative projected-rate moments without assigning
screen length as an intrinsic velocity.

`ExactReceiverPhasePopulation` retains co-present amplitudes by mode at an
outer contact. Same-mode amplitudes superpose before response; distinct modes
remain separate. `ExactReceiverPrimaryDoctrine` forms an exact coupled
premultiplied four-coordinate response only after that superposition: the
three terminal-primary coordinates sum to alpha, and alpha plus exact
transmittance is one. This is a declared observer/display gauge, not a source
palette, a universal color spectrum, or an identification of optical phase
with hue. `arithmetic_dimensional_receiver` composes the production counting
ecology with this current and can emit a controlled prime-power/Zeta receiver
atlas with exact TSV testimony and temporal successors.

## Exact current on analytical bodies

`analytic_field.rs` composes the caused implicit bodies already owned by the
field atlas with physical current. Quadric conics and torus longitude/meridian
orbits remain exact analytical sections: point and tangent are certified
against the body's implicit equation, and a path may carry mode-local
unit-conic winding which its equal endpoints cannot reveal.

`ExactAnalyticFieldWaveLaw` declares which material modes can travel on each
section, compiles those sections into the sparse dimensional-wave owner, and
returns exact cycle holonomy and receiver-local contacts. At a caused material
interface it retains the incident tangential covector and classifies the
transmitted normal fiber as propagating, grazing, or evanescent. Only an
admitted propagating fiber receives scalar traveling-power coefficients;
grazing and evanescent fibers remain explicit open testimony until a reactive
boundary-storage law is supplied.

`ExactRatMatrix` is the shared exact rational matrix kernel.
`ExactAnalyticAdvectionLaw` uses it to form a capacity-skew Cayley successor
over caused analytical-arc incidence. Admission proves preservation of total
current and quadratic energy; a circulation claim is accepted only when its
source chain has zero caused boundary and its covector is invariant under the
successor. This is an exact finite conservative advection law, not a
relabelling of diffusion or a claim to complete Navier–Stokes.

`analytic_field_transport` composes two material tori, a separate torus cycle,
and an observation-conditioned quadric; it emits exact geometry, interface,
wave, receiver, holonomy, and circulation receipts without a raster, assigned
RGB value, or display-scheduled event.

## Growing causal field atlas

`field_atlas.rs` is the production owner for a caused analytical ecology whose
logical population is not bounded by an authored locus count. An oriented
occurrence refines an exact local coefficient fiber for an implicit quadric
and independent exact affine phase fibers. A resolved germ exposes its
caused coorientation, gradient, Hessian, receiver-relative phase gradients,
and every rational or Sturm-certified algebraic depth along a receiver ray.
Resolved and open ray fibers retain the germ's region, origin, parents,
events, and complete lineage. Native exact tori enter the same atlas with
their source provenance intact.

An incompatible occurrence does not average, overwrite, or discard the prior
germ. It founds a descended local alternative with an explicit obstruction
arrow, so the standing retains inspectable causal past and future cones.
Co-present regions glue only when their resolved supports actually agree at
the shared occurrence; otherwise the overlap remains open.

Complete rasters enter as chronological receiver sections. Pixel addresses
remain sensor coordinates rather than world vertices, while declared
image/field contacts may refine receiver-relative phase laws. Static-image
depth which has not been caused remains an open exact fiber.
`CausalFieldAtlasMount` restricts the same logical standing page by page;
resident page size changes its work receipt but not its returned field.

## Recurrent coupled informants

`coupled_informant.rs` is the production owner for recurrent conditioning
across a generated optical horizon, native multi-band spectral sections, exact
receiver clocks, and plural atmospheric vertical fibers. It retains an exact
eighteen-coordinate comparison face, positive compatibility front, recurrent
negative phase witnesses with multiplicity, negative obstruction front,
complete prediction lineage, grades, and grade-before-admission standing.

A second receiver may emanate sparse candidates absent from the first:
chronologically adjacent occurrences at one selected spectral
`(scan,row,column)` form a local star with at most `n-1` edges, never a clique
or complete pair product. Administrative source-vector order is gauge because
the local star is reformed from exact receiver chronology. One returned-apart
witness remains testimony; only recurrent equality can force later
separation. The complete upward envelope is an obstruction, not an assumed
universal monotone law for heterogeneous spectral distance.

`src/soma/life/examples/eros_relampago_atmospheric_current.rs` conducts the
native five-episode application over 60 GLM products, two five-band ABI scans,
and one IGRA sounding. It composes this exact law with eight persistent native
current organs through `life::ExactWorldOrgan`; generates before each later
GLM group return; admits that return only after grading; runs no-return and
reversed-administrative-delivery controls; rest/remounts typed standing,
current, organs, and event ordinal exactly; and renders the resulting
optical/spectral relation propagation as a downstream membrane.

The application is card-primary: the exact optical relation owner and native
current executor retain independent CUDA contexts and explicitly reactivate
their own apparatus before using retained handles. A separate exact-host
current run returns byte-identical typed standing, native rest, organ rest,
crossing receipts, relations, grades, and vertical fibers. The complete
eighteen-coordinate rational successor remains derived by the exact multicore
host law; the native current quotient does not replace that source standing.

## Receiver phase and recurrent grain

`receiver_phase_atlas.rs` receives a complete raster as one chronological
receiver section. Finite addresses remain testimony. Persistent exact
first/second phase differences cause analytical germs carrying native
homogeneous conics; mutually admitted germs carry exact directed connection
corrections, and closed three-transport circuits retain their path-ordered
holonomy. The germ population grows from receiver persistence rather than an
authored locus count.

`holonic_complex.rs` lifts that receiver testimony into exact recurrence
without turning pixels into world cells:

- one analytical germ is a grade-zero receiver cell which still carries its
  internal open paths, paired paths, singular sections, or closed loops;
- one directed phase transport is a grade-one cell with boundary
  `target - source`;
- one declared phase circuit is a grade-two receiver cell only when its
  oriented transport boundary satisfies `partial(partial)=0`;
- the complete closed hull of that circuit remains present while a coarser
  receiver grain admits it as one new grade-zero point; and
- an actual lower cell shared by several promoted hulls becomes one higher
  overlap incidence, never a reconstructed pairwise clique.

Grade is boundary rank inside one complex. Grain is the receiver at which a
completed lower complex is admitted as one constituent. The sparse exact
coboundary is derived as the dual of stored boundary incidence. The resulting
ecology rests/remounts exactly and preserves every source-event witness,
connection, holonomy, quotient, and overlap.

The full-tiger `whole_receiver_holonic_complex` experiment presents the native
conic sections across the whole receiver only as terminal testimony. It does
not flatten the quotient points or overlap nerve into invented screen-space
edges, and it makes no claim that one monocular image caused metric world
depth.

## Receiver-local graph analysis

`graph_receiver.rs` is the production receiver over arbitrary exact graded
incidence. It derives the sparse exact coboundary transpose once, then forms
boundary-closed local sections by propagation from contemporary roots.
`Found`, `Dilate`, and incidence-certified `Traverse` deeds change the
receiver horizon. `Refine` opens one cross-grain quotient point into its
complete retained source fiber, while `Coarsen` returns that exact fiber to
the caused point. A retained receiver event performs no new traversal.

Each successor records added, removed, and retained support without making
the retained section source authority. Unchanged exact cell carriers are
structurally shared across receiver lineage, and every query distinguishes
reused from newly materialized cells. A source radiation only reforms
receivers whose consulted incidence or quotient/overlap dependencies it can
reach. The section carries complete cell lineage, exact boundaries and
coboundaries, plural overlap incidence, graph and higher-complex analysis,
and any source-supplied local coordinate algebras, causal regions, and
opposed-end chains. Coordinate testimony is never inferred from a graph
layout.

`receiver_graph_analysis` runs those production deeds over an existing
whole-image holonic ecology and emits TSV and RON receipts without invoking a
terminal layout. A later visualization membrane can consume those same
sections; it cannot define another topology or a privileged global camera.

## Plural presentation

`ReceiverStandingRelation` is a caused invertible projective map with a source
event. `PluralReceiverAssembly` requires exactly one such relation for every
participating face. `assemble_presentation` transports all primitives and
recomputes arrangements in one bounded presentation chart, including
cross-receiver relations.

The complete crossing atlas and the live support face are distinct exact
products. `receive_support_faces_with_conics_cpu` and
`assemble_support_presentation_with_cpu` carry every continuous primitive,
coordinate field, and seam needed by first-person aperture support without
making the next visible return wait for every pairwise proof witness. The
complete receiver and presentation APIs remain the authority whenever the
crossing atlas itself is the requested research object.

Only `TerminalMatrixSpec` contains a width and height. Every matrix member is
an exact finite rectangle. `quotient_presentation` classifies complete
primitive support against that area:

- segment and polygon classification uses exact incidence;
- conic classification uses the exact corner, edge-stationary, and
  interior-stationary values of the quadratic; and
- coordinate foliations remain available as exact families at every member.

No center ray stands in for a finite area. Contributions remain plural. Color,
occlusion, and blending belong to the explicit outer
`PresentationTransducer`.

`trace_receivers_aperture_with_cpu` is the support-driven presentation path.
It does not scan the complete matrix. Every continuous segment and conic is
restricted independently against the exact aperture; recursive boundary
subdivision rejects regions before reaching terminal addresses. A projective
edge crossing `w=0` is split into its two opposed affine branches and each is
clipped exactly. `trace_primitives_aperture_with_cpu` restricts an exact set
of `(receiver, primitive)` keys, and receiver multiplicity is derived from
the retained primitive sections. Terminal adjacency never becomes source
chronology.

## Causal return and traversal

Raw platform input enters as integer counts and is converted immediately into
one `ReceiverTraversalOccurrence` addressed to an exact contemporary receiver
chart. Pointer motion is a resolution-independent rational chord in the
receiver port basis. Rotation is a primitive projective Cayley ratio carried
by an exact projective quaternion; ordered rotations do not commute.

One chart may collect an ordered elementary word before a physical boundary
is crossed. Only adjacent identical generators become a
`ReceiverTraversalStep::Repeat`; the repetition count remains explicit and
its spin is realized by exact binary powering. A changed chart refuses a
stale word unless its complete position, orientation, acceptance cover, and
port section carried by identity. Physical port current is a separate field:
motion cannot silently manufacture current.

The rejected display-relative `PresentationInput`, additive receiver-control
law, terminal-cell delta, and copied fan-out paths are absent. A returned deed
enters one `ReceiverPort`; any other receiver changes only through declared
physical incidence.

## Algebraic source and arithmetic fibers

`algebraic.rs` is the dimension-independent source layer. It carries
group-completed occurrence counts, arbitrary-grade exact incidence,
homogeneous local coordinate algebras, opposed end sections, and
boundary-closed receiver morphisms whose observed quotients retain their
source fibers. The tetrahedral/conic ecology is one exact realization of this
source; it is not the maximum supported grade.

`arithmetic_fiber.rs` supplies an exact sequential arithmetic world. A prime
cell is founded only after every already-founded prime axis capable of
factoring the current integer returns a nonzero remainder. A product of
distinct primes forms oriented higher incidence; repeated factors remain
valuation multiplicity on that support.

Every integer radiation now retains the complete ordered recognition fiber:
both irreducible and composite dispositions remain open until an axis return
separates them, the first exact factor is a prefix-minimal irreducibility
obstruction, and a prime closes only after its admitted factor horizon. The
receipt carries matching indistinguishability and certificate depths in that
declared receiver language; it makes no universal primality-complexity claim.
Standing also carries the exact formal Chebyshev current
\(\psi(x)=\sum_p a_p\log p\), with
\(\prod_p p^{a_p}=\operatorname{lcm}(1,\ldots,x)\), and finite-place integer
\(\sigma>1\) Zeta valuation measures as exact rational cut/return sections.
`prime_emergence_observatory` supplies consecutive integers only and emits the
full recognition, valuation, obstruction, recurrence, and Zeta testimony.

`divisor_reconstruction.rs` mounts a second production world outside that
arithmetic authority. It receives chronology-local occurrence ordinals and
boolean answers to its own common-generator queries—never integer values,
prime names, gcds, factors, or valuations. Pairwise returns form the
occurrence contact skeleton. Higher-order returns separate false cliques
until its maximal cells are exact unlabeled generator supports. Reversing
that incidence then forms the dual generator complex: a generator set is a
cell exactly when some occurrence inhabits all of it.

Two doctrines keep prior lineage distinct from discovered testimony.
`CompleteContactComplex` assumes nothing and carries a top-down antichain of
every still-compatible facet. `PrivateWitnessedGeneratorFacets` declares that
each irredundant generator has an occurrence belonging to no other generator;
production must still locate the witness through closed receiver
neighborhoods. If returned contact violates that declaration, standing
retains the uncovered occurrences and positive pairs as an exact obstruction
instead of silently falling back. `ArithmeticDivisorMembrane` is grading
authority only. It maps the opaque ordinals back to arithmetic occurrences,
answers contact, and grades the reconstruction against prime support and the
existing squarefree complex up to latent-generator renaming.

The first polynomial receiver family is the exact fiber of
\(x^2-a\) over a founded odd prime. It retains every modular root and derives
its quadratic hand from root count. Exact unit-coordinate isomorphisms,
Hensel branches, and Chinese-remainder composition return their complete zero
or obstruction residuals. A bounded residue grammar can be inferred from
paired fibers; an unseen receiver bucket remains `OPEN`. The inferred
transport can be installed as a rank-one sheaf on the multiplication-founded
pair ecology without deleting higher source cells.

`prime_ecology.rs` turns those fixed probes into one topology-forming
production world. Its atomic event mouth accepts the next integer occurrence,
an inherited monic integer polynomial, or a request to resolve one already
open horn. Every newly founded prime receives every inherited polynomial, and
every newly inherited polynomial is received by every extant prime.
Deterministic exact Berlekamp factorization retains repeated factors,
quotient-field Frobenius matrices, their exact orders, and a receiver-local
phase signature.

Prime receivers share a phase constituent only when their independently
derived factor populations agree in probe identity, irreducible degree,
Frobenius order, multiplicity, and population. A nonempty common constituent
founds an oriented higher cell over the same arithmetic prime vertices. When
all immediate faces exist but their total feature intersection is empty, the
boundary remains an explicit potential horn. `ResolveHorn` supplies only the
support. Each concrete inherited boundary phase preserves its exact local
reductions, derives the complete same-phase stratum at the missing prime, and
defines its coefficientwise CRT preimage in the integer-polynomial lattice.
The engine retains this as an exact symbolic family: finite representatives
are materialized only when a receiver requests them. A nonempty branch fills
the horn; an empty branch is an exact obstruction.

For the `{2,3,5}` horn formed by `Phi_3`, `Phi_5`, and `Phi_7`, the three
oriented continuations contain exactly `150`, `3`, and `9` affine
coefficient torsors modulo `30`. The familiar `x^2+x+11` is one member of the
three-component quadratic branch, not a supplied answer or a privileged
representative. Refining that branch through receiver `7` gives `49` children
per parent, partitioned exactly into `21` irreducible, `7` repeated-root, and
`21` split local sections, with normalized receiver measures `3/7`, `1/7`,
and `3/7`.

Inherited probes, enacted fibers, proposed horns, induced filler families,
and induced cells carry distinct source events. Integer-first and
probe-first chronologies are required to produce the same final
receiver-geometry receipt. The bounded `prime_ecology_calibration`
executable reports these production receipts; it does not own
factorization, continuation, CRT gluing, or topology.

## Arithmetic monodromy and spectral transport

`arithmetic_monodromy.rs` is a production composition of the existing prime
ecology, not an application-side Galois classifier. It receives any
degree-five integral polynomial with nonzero leading coefficient \(a_5\) and
forms the exact monic integer probe

\[
g(y)=a_5^4f(y/a_5),\qquad y=a_5x.
\]

The inherited coefficients and root scale remain standing. Every founded
prime receives `g` through `PrimeEcologyLaw`; this layer consumes the emitted
`PolynomialPrimeFiber` and never factors the reduction a second time.
Separable reductions become exact receiver-local Frobenius cycle sections.
Repeated reductions remain named polynomial-discriminant place
obstructions—this power-basis law does not invent a ramified field factor.

The five transitive degree-five permutation groups are constructed from
generators inside the production law. Exact discriminant square class and
positively witnessed Frobenius cycle types restrict that inherited
catalogue. One irreducible degree-five reduction certifies irreducibility
over \(\mathbb Q\). Absence of an unobserved cycle type never excludes a
group, so smaller unresolved candidate fibers remain `OPEN`.

Every admitted cycle partition \(d_1+\cdots+d_r=5\) transports into the
formal local denominator

\[
\det(I-T\,\mathrm{Frob}_p)=\prod_i(1-T^{d_i})
\]

and an open integer-\(\sigma>1\) receiver evaluates its reciprocal exactly at
\(T=p^{-\sigma}\). A problem-to-receiver incidence map propagates each new
section only to its actual receiver mouths. Complete scans are confined to
validation and inspection.

`ArithmeticMonodromyAtlasReceipt` retains the coefficient chart, every
prime-local orbit period, the Frobenius/Euler transport cell, source events,
the conditional Galois fiber, and open cross-prime root-sheet gluing. It has
no screen coordinates and does not identify roots at distinct places
without a transport witness.

The parameterized `arithmetic_monodromy_ecology` executable grades early
versus late polynomial/receiver chronologies, retains a set-valued future
cycle fiber at the first exact group certificate, and tests every later
prime section against it. The default nonmonic calibration closes as
\(A_5\); `x^5-x-1` supplies a nonsquare-discriminant \(S_5\) control.

## Hinge topology and physics

`SimplicialComplex` carries oriented vertices, faces, cofaces, and hinges.
`HingeTransportNetwork` carries exact projective turns. One event constructs
an event-rooted spanning transport tree. Every non-tree chord emits one
compressed return generator containing:

- the tree word to the chord source;
- the chord;
- the tree word to its target;
- the exact return holonomy;
- gauge, fixed-point, or displaced class; and
- the exact target gluing residual.

This is linear in the reached vertices and relations; simple paths are not
enumerated.

`VariationalHingeLaw` supplies one exact discrete Euler--Lagrange
realization. `DiscreteCurrentComplex` implements
\(\partial J=S\) with declared units. `potential_cycle` distinguishes a
completed zero-sum voltage loop from an unfinished word carrying its endpoint
difference. `StoredCurrentInterval` retains current across an incomplete
interval. `DiscreteActionStress` is exact action change per declared
deformation and retains orientation rather than labelling a sign as reward or
punishment.

`ExactDiffusionLaw` supplies a separate source-declared transport ecology on
an oriented finite incidence complex. Positive exact capacities and
conductance magnitudes define the local constitutive metric; current sign
emerges from endpoint potential difference. Every completed event solves

\[
M\phi_{t+1}=n_t+s_t+\tau\partial j_{t+1},
\qquad
j_e=c_e(\phi_{\mathrm{source}}-\phi_{\mathrm{target}})
\]

by exact rational elimination and returns every branch current, node balance,
total conservation residual, and stored-energy change. It is not a stochastic
generative model, authored smoothing pass, or finite-difference approximation
to a continuous PDE.

For a declared boundary population, the law compiles
\(A=M+\tau L\), \(A_{II}^{-1}\), and the exact Schur transfer
\(\Lambda=A_{BB}-A_{BI}A_{II}^{-1}A_{IB}\). Its certificate carries the
ordered structural population and zero inverse/Schur residuals. Later events
reuse that operator factorization while inserting contemporary standing and
source vectors; complete interior potentials, currents, balances,
conservation, and energy are still reconstructed and graded against the
direct exact solve. A changed interval or a different immutable law cannot
reuse the certificate.

`ExactCellularSheaf` lifts deterministic diffusion beyond node scalars. Every
causal cell owns a finite rational stalk and every immediate incidence owns
an exact restriction map. Composite restrictions must be path independent,
and the assembled cellular coboundary must satisfy \(\delta^2=0\).
`ExactSheafDiffusionLaw` solves

\[
(M+\tau\Delta_k)\phi_{t+1}=n_t+s_t,
\qquad n_{t+1}=M\phi_{t+1}
\]

with the full sheaf Hodge operator. Its certificate retains both adjacent
coboundaries, the Hodge and event operators, an exact inverse, balance and
inverse residuals, compatibility sections, energy change, harmonic
dimension, and factorization reuse. Scalar graph diffusion is its rank-one,
grade-zero specialization, not its ontology.

`LocalStarLaw` is the distributed physical carrier. Its state is spread over
an oriented incidence neighborhood:

- hinges carry independent exact trajectories and incoming/outgoing current;
- faces carry oriented circulation and stress;
- an oriented edge cochain carries relative geometry, exact face closure, and
  chord residuals—absolute vertices are derived only for a requesting
  receiver or outer membrane;
- receiver bodies carry attached worldlines, projective spin, and a physical
  `(coordinate, momentum, net impulse)` phase; and
- local discriminant crossings emit typed edge-flip candidates without
  silently editing the complex.

An event reads one immutable predecessor. Its exact current constituents
retain their root occurrences and ordered incidence words while advancing
through causally ordered local antichains to rest, exact return, or a
combinatorially forced open cyclic boundary. Intermediate antichains never
enter standing. Active hinge solves are independent work members; CPU
scheduling is erased by a canonical exact merge. The contemporary simplicial
complex lives in standing, and an admitted identity-preserving hinge flip
reforms incidence, relative geometry, receiver links, and coordination defect
atomically.

The `desktop_receiver` example mounts this law on a heterogeneous octahedral
ecology. Returned motion enters one embodied receiver port; propagated current
changes local trajectories, face stress, shared vertices, native conics, and
receiver worldlines before the successor faces are assembled. Every event is
written to `target/holonic-engine/desktop_receiver_trace.tsv` (or the path
supplied by `HOLONIC_ENGINE_TRACE`) with complete current routes, active and
pending frontiers, action balances, face fields, receiver worldlines, rewrite
openings, physical worker receipts, aperture work, and the complete carried
state. Separate arithmetic-height columns report trajectories, receiver
translation, spin, physical phase, presentation factors, presented support,
and terminal intermediates.

## Receiver-tube standing

`TerminalTubeAtlas` carries each receiver's caused core, star-link horizon,
exact local primitive section, presentation transport, composed projective
holonomy, ordered event transitions, and finite terminal restriction. A
physical occurrence remains in that transition word even when its finite
trace carries. Founded, refounded, deformed, transported, and
aperture-changed tubes compare their structural factors before presentation
materialization. Retained, rebased, and aperture-only sections reuse their
already formed exact presented primitives; only founded, refounded, deformed,
or transported receiver relations act on local sections.
Unchanged primitive restrictions remain standing inside a changed receiver;
only changed representative keys are materialized into the selected support
presentation and descend to the host/card aperture executor. Identical
changed primitives share one representative restriction, and every commit
returns exact receiver-level support deltas plus the number of receiver
relations and primitives actually materialized.

Receiver-face formation receipts expose the complete admitted source section
and ray family. Unchanged receiver-local implicit tori may retain literally,
but currently refuse the affine-rebase path: the engine has no declared exact
carrier proving that a general affine map preserves their native torus
species.

This is the exact leaf of the Fast Multipole relation: completed local
constituents may carry an exterior face and rebase it instead of remaining
pairwise active. Full direct receiver tracing remains the authority. A general
near/far hierarchy is admitted only for a declared interaction kernel with an
exact rational remainder certificate; floating tolerance and truncated field
values cannot become standing.

## Caused probability and geometric loss

`ExactConfigurationComplex` is the exact bounded/cellular probability
species. A preparation cell supplies a source standing, deed, exact measure,
and exact action adjacency—never a precomputed successor.
`enact_outcome_basin` invokes the production `ExactEventLaw` for every
immutable predecessor, then forms a receiver-relative outcome quotient. The
accepted basin and rejected `ExactGeometricLoss` retain induced adjacency,
connected components, cycle rank, crossing boundary, exact measures, and
minimum-action corrective paths. Probability is the accepted-measure
quotient, not primitive engine standing.

`ExactNormalizedField` likewise keeps additive numerator and denominator
separate until a receiver query. Provenance-preserving term partitions can be
folded componentwise with exact zero-residual testimony. This is exact reuse
of evaluated local contributions; a general fast-multipole fold still
requires a kernel-specific expansion and exact remainder certificate.

## Physical execution

`CpuExecutor` realizes independent indexed antichains serially or on a bounded
worker population and restores canonical logical order. Continuous
cross-receiver arrangement pairs and terminal cell classification both use
this executor; serial and multicore outputs are identical.

`ExactDeviceExecutor` is the low-level device ABI. `admit_device` compares a
candidate backend against the exact CPU authority and refuses it on any
cardinality or value difference. `CudaApertureExecutor` is the concrete native
CUDA receiver path. It compiles its integer kernel from source, carries
segments as finite homogeneous integer endpoints through a signed-magnitude
128-bit host/card coefficient seam, and selects exact signed `i128`, 192-bit,
256-bit, or 384-bit intermediates from an arbitrary-precision range proof.
Primitives outside the admitted range return through the exact multicore
integer/projective host law and are merged before whole-face admission. Device
support is one bit per primitive/address rather than one integer word per
pixel. Sparse local
aperture sections depart before terminal descent when a primitive cannot cross
them. Complete addresses and per-primitive multiplicities must match the
retained arbitrary-rational authority before the executor is admitted. The
device result carrier contains only device-admitted primitives, and card
launch overlaps independent host conduct. Admission times both exact carriers;
the live bounded ecology uses the faster carrier rather than CUDA merely
because a card is present.

The desktop's display color is also derived rather than assigned per shape or
receiver. Each embodied receiver's exact coordinate, momentum, and net impulse
form its contemporary phase triple; the outer monitor membrane takes exact
barycentric ratios into its local RGB basis. RGB remains a display quotient,
not a universal physical spectrum.

`PlatformMembrane` carries raw integer input, complete terminal `DisplayFace`
values, and atomic batches of changed row-run `DisplayPatch` values.
`MemoryPlatform` is the deterministic reference membrane. The X11 membrane
validates a complete batch before changing its retained face, blits all runs,
then flushes once. A native window adapter may implement the same boundary
without becoming geometry or physics authority.

## Examples

- `exact_receiver` forms a heterogeneous continuous receiver face and only
  then derives a \(80\times45\) terminal matrix.
- `native_conic_hinge` changes a native conic through a hinge event and
  receives that quadratic beside a crossing sheet.
- `divisor_receiver_reconstruction` reconstructs unlabeled prime-generator
  supports and the dual squarefree complex from opaque contact, with either
  the private-witnessed or unrestricted doctrine.
- `desktop_receiver` presents two embodied receiver faces of one propagating
  octahedral local-star ecology through a native-resolution X11 membrane,
  sparse tube deltas, and chart-addressed traversal words.
- `whole_receiver_holonic_complex` forms a whole-image analytical phase
  ecology, exact grade-zero/one/two incidence, coarser quotient points, and
  actual higher overlap cells without an authored crop or fixed germ count.

## Verification

Run:

```text
cargo test -p holonic-engine --all-targets
cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings
```

The crate contains no `f32`, `f64`, floating-point literal, or circular
constant evaluation.

[package]
name = "soma-surface"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "surface"
path = "src/lib.rs"

[dependencies]
wgpu = { version = "27", features = ["spirv"] }
pollster = "0.4"
bytemuck = { version = "1", features = ["derive"] }
body = { path = "../holonic-body" }

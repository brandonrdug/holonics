# Exact phase-mode receiver execution

[established-bounded; implemented-exact] This executable composes the existing Rust
`CertifiedSeries`, `ExactInterval`, and Machin/arctangent owners through the actual six-chart
phase map. All state arithmetic, source parameters, remainder bounds, winding rebases and
receiver decisions use integers/rationals. No floating midpoint determines a state or arrival.
The source is the same normalized exponential/rotation mode declared by the parent formula.

The two exact initial section addresses are `(359/384,73/384)` and `(361/384,73/384)`.
Other coordinates, incidence, `κ=27/100`, `γ=7/20`, head parameters 1 and 4, connection turn
1/8, and the detector `[1/5,17/50) × [2/25,1/5)` remain those of the original analytic map.

[proved-derived] Machin's lifted constraint combines the existing arctangent partial sums
and signed tails. Circular evaluation first uses the rotation group's angle-composition law
to enter its rational series domain. For an interval argument, a rational midpoint series
is widened by the exact interval radius using `|sin'|,|cos'|≤1`; the midpoint never replaces
the state. Monotonic endpoint exponential bounds, interval products/division, both incoming
and outgoing attention sums and outward dyadic rounding carry the complete family forward.
The real phase lift is retained; integer subtraction is only a periodic evaluation rebase.
Receiver comparison splits the wrap chart and checks all resulting pieces.

[established-bounded; implemented-exact] `trajectory.json` records all inspected states:

- First state: excluded from the detector at steps 0–8, wholly inside at step 9.
- Second state: excluded at steps 0–32; the retained family intersects both detector outcomes
  at step 33. This is an undecided face, not a claim of arrival or non-arrival at 33.

The second survey claim of arrival at 96 is therefore not an exact result of this run.
The stored constraint/continuation family remains available for a finer or more structured
receiver. No universal certainty or stopping rule is imposed on HNN generation.

[definition] This run uses 96 dyadic octaves, 24 circular terms after argument reduction,
40 exponential terms, and 48 terms per Machin arm. These are receiver/algorithm apertures,
not intrinsic semantic capacities or decimal constants. The recorded debug wall clock is
320131 ms, excluding Cargo build; it is one local apparatus measurement, not a complexity
bound. The actual arithmetic/branch output is independent of that clock.

```bash
cargo run --manifest-path research/experiments/intrinsic_holonic_flow/exact/Cargo.toml --quiet
```

[project-postulate] The preceding point-only probe and missing-binding statement are
superseded. The live return is the actual exact/enclosed trajectory above. The old NumPy
survey remains exterior exploratory evidence and must not determine a native Holonic return.

# Exact Rubik corner projection and full-state optimality witnesses

[definition] The source is
[`rubik_corner_quotient_navigation.rs`](../../../crates/holonic-engine/examples/rubik_corner_quotient_navigation.rs),
using the shared finite [`group_navigation`](../../../crates/holonic-engine/src/group_navigation.rs)
and `StructureGroup` owners. Six face quarter turns are generated by rotating signed cube
vertices. The same rotations of positions and normals produce all 54 facelet permutations;
their corner projection is checked against the eight-vertex generator for every face.

[definition] The metric counts quarter turns and their inverses at cost one. Base labels
mean positive quarter turns about outward normals, and apostrophes reverse those declared
turns. Corner orientation and edge coordinates remain in the full-state fibre. The projected
corner distance is a lower bound for a full cube; it is not silently used as a complete
arbitrary-state solution.

[established-bounded; implemented-exact; computational-witness] Closure returns 40,320 corner
permutations. Weighted Dijkstra constructs the distance and an optimal policy fibre; the
example independently checks goal-zero equivalence, a descending move at every non-goal
state, and all 483,840 unit-edge lower-bound inequalities. This is the local certificate
formalized by `Foundation/TransportWord.descendingWord_is_optimal`. Rust display words are
chronological; Lean's common action reads the reversed list right-to-left.

[established-bounded; implemented-exact; computational-witness] The quotient diameter is 8.
The two-turn `U R` example and a constructively lifted farthest representative have matching
full-state upper and quotient lower bounds. Their returned words solve all 54 facelets in
2 and 8 turns respectively. The latter is a particular full-state representative of its
corner fibre, not a bound for every full state in that fibre. `result.json` retains the
actual permutations, words, minimizing choices and counts.

```sh
cargo build -p holonic-engine --example rubik_corner_quotient_navigation
python3 research/experiments/rubik_corner_navigation/run.py
cargo test -p holonic-engine group_navigation::tests --lib
```

[definition] The runner reuses the shared benchmark's exterior clock and fresh child-resource
receiver. `resources.json` records binary/source provenance, ordinary desktop load, CPU time
and peak Linux RSS. This workload is exact host mathematical construction. It does not claim
native GPU inference, measured power, asymptotic superiority or a solution of P versus NP.
The recorded source revision is the checkout HEAD during verification of the accompanying
working-tree change; the binary fingerprint identifies the exact executed artifact.
The [synthesis](../../records/2026-09-13_MASS_FLUX_GAPS_AND_OPTIMAL_NAVIGATION_RETURN_THEIR_SOURCE_MAPS.md)
develops the quotient/fibre and algebraic-path consequences.

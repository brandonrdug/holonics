diff --git a/crates/holonic-engine/src/exact_linear.rs b/crates/holonic-engine/src/exact_linear.rs
index 404ed7f5..3e8e99b9 100644
--- a/crates/holonic-engine/src/exact_linear.rs
+++ b/crates/holonic-engine/src/exact_linear.rs
@@ -547,55 +547,59 @@ impl ExactRatMatrix {
         if self.entries.iter().all(|entry| entry.denom().is_one()) {
             return self.inverse_integral_fraction_free();
         }
-        let extent = self.rows;
-        let mut left = (0..extent)
-            .map(|row| self.row(row).map(ToOwned::to_owned))
-            .collect::<Result<Vec<_>, _>>()?;
-        let mut right = (0..extent)
+        // Clear each row by its own denominator before entering the fraction-free owner.  For
+        // D = diag(D_i), B = D A is integral and B⁻¹ D = A⁻¹.  Row scaling avoids a global LCM
+        // inflating every coefficient while retaining the original operator for the two-sided
+        // certificate below.
+        let row_denominators = (0..self.rows)
             .map(|row| {
-                (0..extent)
-                    .map(|column| {
-                        if row == column {
-                            Rat::one()
-                        } else {
-                            Rat::zero()
+                self.row(row).map(|entries| {
+                    entries.iter().fold(BigInt::one(), |current, entry| {
+                        let mut left = current.clone();
+                        let mut right = entry.denom().clone();
+                        while !right.is_zero() {
+                            let remainder = &left % &right;
+                            left = right;
+                            right = remainder;
                         }
+                        (current / left) * entry.denom()
                     })
-                    .collect::<Vec<_>>()
+                })
             })
-            .collect::<Vec<_>>();
-
-        for column in 0..extent {
-            let pivot = (column..extent)
-                .find(|row| !left[*row][column].is_zero())
-                .ok_or(ExactLinearError::SingularMatrix)?;
-            if pivot != column {
-                left.swap(pivot, column);
-                right.swap(pivot, column);
-            }
-            let divisor = left[column][column].clone();
-            for entry in &mut left[column] {
-                *entry /= &divisor;
-            }
-            for entry in &mut right[column] {
-                *entry /= &divisor;
-            }
-            let pivot_left = left[column].clone();
-            let pivot_right = right[column].clone();
-            for row in 0..extent {
-                if row == column || left[row][column].is_zero() {
-                    continue;
-                }
-                let factor = left[row][column].clone();
-                for (entry, pivot_entry) in left[row].iter_mut().zip(&pivot_left) {
-                    *entry -= &factor * pivot_entry;
-                }
-                for (entry, pivot_entry) in right[row].iter_mut().zip(&pivot_right) {
-                    *entry -= &factor * pivot_entry;
-                }
-            }
-        }
-        let inverse = Self::new(right)?;
+            .collect::<Result<Vec<_>, _>>()?;
+        let integral = Self::new(
+            (0..self.rows)
+                .map(|row| {
+                    self.row(row).map(|entries| {
+                        entries
+                            .iter()
+                            .map(|entry| {
+                                Rat::from_integer(
+                                    entry.numer() * (&row_denominators[row] / entry.denom()),
+                                )
+                            })
+                            .collect()
+                    })
+                })
+                .collect::<Result<Vec<_>, _>>()?,
+        )?;
+        let integral_inverse = integral.inverse_integral_fraction_free()?;
+        let inverse = Self::new(
+            (0..self.rows)
+                .map(|row| {
+                    integral_inverse.row(row).map(|entries| {
+                        entries
+                            .iter()
+                            .enumerate()
+                            .map(|(column, entry)| {
+                                entry * Rat::from_integer(row_denominators[column].clone())
+                            })
+                            .collect()
+                    })
+                })
+                .collect::<Result<Vec<_>, _>>()?,
+        )?;
+        let extent = self.rows;
         let identity = Self::identity(extent)?;
         if self.multiply(&inverse)? != identity || inverse.multiply(self)? != identity {
             return Err(ExactLinearError::InverseCertificateFailure);
@@ -1401,6 +1405,43 @@ mod tests {
         );
     }

+    #[test]
+    fn rational_inverse_clears_common_scale_before_fraction_free_pivots() {
+        let matrix = ExactRatMatrix::new(vec![
+            vec![Rat::zero(), Rat::new(BigInt::from(1), BigInt::from(2))],
+            vec![Rat::new(BigInt::from(1), BigInt::from(3)), Rat::zero()],
+        ])
+        .unwrap();
+        let inverse = matrix.inverse().unwrap();
+        assert_eq!(
+            inverse,
+            ExactRatMatrix::new(vec![
+                vec![Rat::zero(), Rat::from_integer(3.into())],
+                vec![Rat::from_integer(2.into()), Rat::zero()],
+            ])
+            .unwrap()
+        );
+        assert_eq!(
+            matrix.multiply(&inverse).unwrap(),
+            ExactRatMatrix::identity(2).unwrap()
+        );
+        assert_eq!(
+            inverse.multiply(&matrix).unwrap(),
+            ExactRatMatrix::identity(2).unwrap()
+        );
+
+        let singular = ExactRatMatrix::new(vec![
+            vec![Rat::new(BigInt::from(1), BigInt::from(2)), Rat::one()],
+            vec![Rat::one(), Rat::from_integer(2.into())],
+        ])
+        .unwrap();
+        assert_eq!(singular.inverse(), Err(ExactLinearError::SingularMatrix));
+        assert_eq!(
+            ExactRatMatrix::shaped(0, 0, Vec::new()).unwrap().inverse(),
+            ExactRatMatrix::identity(0)
+        );
+    }
+
     /// The dense-row presentation must round-trip, or a law routed through this carrier would
     /// return a different operator than the one it handed over. The degenerate and the
     /// rectangular shapes are both included because the four laws that now route through here

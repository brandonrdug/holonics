# The normal response accepts resident currents without a source archive

[definition] This AC3 attachment increment starts from `8f2eb181` and composes the existing
normal-response construction with the local generator/current owners. The [normal-geometry
review](2026-09-09_MATHEMATICAL_REVIEW_NORMAL_GEOMETRY_AND_CLOSED_RETURNS.md) supplies the recovered
mathematics. The [corrected blueprint](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md)
and [roadmap](../../docs/plans/THE_ROADMAP.md) retain the whole AC0–AC5 objective.

## Existing law, different input attachment

[established-bounded; source-inspected] The existing `OperativeNormal` calculation owns
`H = I + sum x x†`, `B = sum y x†`, target energy C, source/target family bounds, a numerical
proposal M and its residual against the exact normal system. Its field adapter obtained x
through a historical source handle. The calculation itself does not require that handle or a
transcript: the supplied current and accumulated geometry suffice for its next local operation.

[definition] `ResidentNormalMaterial` now exposes that existing law through `found`, `read`
and `receive`. Its source chart preserves the three declared complex port blocks of the existing
normal owner: outgoing, held and target/reference. Their width and the output width are caller
declarations. This is not an inferred semantic capacity or a conversion from text length.
The numerical grain is likewise a declared realization parameter.

[definition] `read` accepts an actual `ResidentConstitutiveCurrent` and returns the old response
without copying the continuing state. `receive` also accepts an actual observed current,
prepares the existing normal increment and fit into fresh material, and publishes the new state
only after the complete native passage succeeds. It retains no field source handles, historical
predictor table or original observation list. The sole continuing state is the exact accumulated
geometry, numerical witness, source/output chart, grain and observation count.

[definition] `ResidentNormalReturn` borrows the actual supplied operands and owns its completed
before/after reports. These retain the oriented current difference, full current ball and
numerical/source-family bounds. The numerical centre has no point-current output port.
Rational source points may require a dyadic enclosure in this normal realization; that enclosure
and its propagated error remain explicit. No float determines the native return.

[definition] The new CUDA adapter packs the supplied resident current into the old normal
source layout. It calls the existing `normal_increment` and `normal_fit` through
`field_normal_material_prepare`, and reuses `linear_material_row` and `ft_prediction_error`
for the old prediction. No second fitting algorithm is installed. The normal report/state
decoders are now shared by the old field and new direct port; the old pending/uncertain-state
guard is retained.

## Verified distinctions

[established-bounded; implemented-exact; computational-witness] The seven-case normal selection
passes, including the old field controls and these direct-port comparisons:

- Two opposite observed returns at the same source give H=3 on that source axis, B=0 and C=2.
  The unit-prior optimum has objective 1 while the numerical solve gap is zero. Conflicting
  observations have not been declared absent because the fitted mean is zero.
- A complex source i and observed return 1 retain cross-source phase −i. A later real source
  is enclosed around the expected response −i/2.
- Source 1/3 and observed return 2 retain nonzero source-family bounds; the later response
  encloses 1/5 and the family minimum encloses 9/5 in the declared unit-prior objective.
- Malformed/nonpoint input and a wide-word arithmetic refusal preserve all old state words
  and chronology. A later valid observation still succeeds.

[established-bounded; implemented-exact; computational-witness] The composition control first
forms a resident local relation for an identity receiver on the three complex port axes.
A new generated current `(1, i, −1)` enters the normal owner directly, with observed return i.
Both the developed response and subsequent reading enclose `3i/4`. There is no numerical
section read or remount between the local generator and normal response. The input is a native
produced current, not an authored coefficient matrix supplied to the normal learner.

[established-bounded; process-audit] The final normal selection passes in 2.07 seconds after
compilation. The initial six-case selection also passed, but took 164.49 seconds. During its
startup, the test process consumed a CPU core while GPU utilization was zero and the NVIDIA PTX
compiler library was mapped. This is consistent with cold module compilation; a stack inspection
was denied by the host. The later warm run is the returned execution comparison, not an estimate
of corpus cost. No timed-out check is counted as a pass.

```sh
cargo test -p holonic-engine --lib material_transport::normal:: -- --include-ignored --test-threads=1
cargo test -p holonic-engine --lib constitutive_fibre::resident:: -- --include-ignored --test-threads=1
cargo check -p holonics --lib
```

[established-bounded; process-audit] The resident selection passes all 45 cases in 8.98 seconds.
The public framework facade check passes. No Lean source or cultivation pipeline binding changed.

## Exact next boundary

[open] This direct port has not yet processed conversation material. AC3 still owes the actual
codec-to-current/receiver map and a section-level application of that map. Its observation count
must not become a byte-driven native clock. The next composition also owes a resident consumer
for the complete bounded output; extracting its numerical centre would repeat the retired
mistake. `state_wire` exposes only boundary data, not a complete restart protocol for this owner.
The existing generator-neighborhood persistence remains independently established.

[definition] For the source attachment, codec labels may supply an exterior basis, but their
integer ordinals must not become intrinsic amplitude or distance. Source positions and incidence
belong to the observed section; repeated codec faces do not identify occurrences. An actual
relabeling must carry the corresponding source/receiver chart. No lexical filter, dictionary
of desired responses or historical-source replay is a substitute for that construction.

[open] The local normal law is a declared regularized response hypothesis, not a universal
language model, a unique reconstruction of external causes, or the whole HNN architecture.
The exact-fibre and normal-response owners have different useful scopes and can be composed;
neither is grounds for retiring the other. AC0–AC5 remains active and Athena-alpha remains
unattained.

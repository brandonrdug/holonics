# The existing causal partition was bypassed, and P2 rested every history at every layer

**Date:** 2026-08-25.  
**Truth status:** `counterexample` for the attempted P2 passage; `established-bounded` for the
existing partition, reconstruction, interchange and returned-factor owners named below.  
**Evidence:** `implemented-exact`, `measured`.  
**Construction authority:** none. This record preserves the audit; the active blueprint and
`blueprint/THE_ROADMAP.md` schedule the correction.

## What returned

The causal-basin derivation returned and remains admitted under
`output/the_brandon_causal_basin_returns_and_cultivates_one_athena_body/00-causal-basin.json` through
`03-candidate-input.json`. The candidate passage did not return. No `04-*` artifact exists, and P2
did not pass.

Two candidate invocations crossed the same sixty-three-history occurrence and both ended in a CUDA
allocation refusal before any candidate family was deposited:

| invocation | elapsed | return |
|---|---:|---|
| first candidate passage | 434.593 s | CUDA out of memory |
| retry after an intra-layer remount-pressure patch | 18,744.194 s | CUDA out of memory |

The two hot invocations therefore consumed 19,178.787 seconds without changing admitted standing.
The retry answered no new falsifier: it changed one ordinary layer-remount boundary but retained the
same passage morphology and reached the same allocation refusal at the final boundary.

## The topology that caused the cost

The P2 entry point named `conduct_history_only_candidates_partitioned` in
`soma/life/src/athena_returned_defect.rs` did not use the engine's native causal-partition passage.
It called `ProductSession::infer_native_history_cohort`, which entered
`phoenix::cohort::circulate_cultivated_independent_cohort`.

That cohort owner states its own bounded form: rows are not batched; each sibling owns a separate
standing, section and graph; graph instantiation and launch remain one per sibling per layer. The
cultivated independent wrapper also passes a literal band width of one. Consequently the
sixty-three histories produced sixty-three separate graph deeds at each of forty-two layers, then
sixty-three final deeds and sixty-three overlay deeds: 2,772 sequential graph deeds. At every
one-layer band boundary `rest_live` read each live carried/shared exact section out of the resident
surface, and the next layer remounted it. GPU activity was real, but the causal arrangement was a
serial family of foreign passages with repeated exterior rest/remount. High device utilization did
not make that topology a resident plural circulation.

The first patch priced ordinary intra-layer remounts before allocation. It did not change the
one-graph-per-history topology, the band width, the readback/remount recurrence, or the final
boundary's direct remount. The retry was therefore a larger measurement of the same counterexample,
not a repair of P2.

## The owner P2 bypassed

The required passage already exists:

- `phoenix::streamed::circulate_cultivated_with_partitioned_intervention` preserves every entering
  source row, resets position chronology at each declared history boundary, makes contact
  block-diagonal, conducts the shared source maps once per layer, and returns the exact terminal row
  of every addressed section while earlier rows remain its reconstruction fibre;
- `ProductSession::infer_native_partitioned_with_intervention` is the authenticated product-facing
  entry to that passage;
- `section_partition` owns complete, disjoint coordinate sectioning and composes the existing
  interchange certificate rather than inferring independence from collection membership;
- `FrontPassage` owns exact work and apparatus pre-admission before launch;
- `athena_returned_defect` already owns candidate sealing before sibling testimony, graded returned
  defects and source isolation;
- `SessionFactorComplex` owns derived-rank local cultivation, compatible glue, ordered holonomy,
  ablation, restoration and exact withdrawal; and
- `ProductSession::recultivate_returned_terminal_cohort` re-enters already returned W2 terminal
  hidden/potential sections through only the current local factor, with zero foreign tower mounts.

This is circulation before a new organ. The correction is to rotate these owners into contact. It
does not require a new scheduler, carrier optimizer, batch manager, pricing system or semantic
cabinet.

## The corrected passage

One candidate history remains one complete ordered causal section. Plural histories are presented
as an exact independent causal partition to the existing block-diagonal passage. If the complete
family does not fit one physical occurrence, `FrontPassage` pre-admission and exact section
interchange derive a cover of independently returned process sections. Each section deposits its
candidate potential, W2 terminal hidden section, predecessor potential, reconstruction identity,
source/body/code closure and apparatus testimony before its process exits. The union is admitted
only after completeness, disjointness and interchange return; count or collection membership is no
proof.

Later sibling/world/operator material crosses only after every candidate section is sealed. The
returned defects then found the existing factor cover. Revisit circulation starts from the retained
W2 boundaries and crosses only the changed local factor. It does not re-enter the foreign tower.
Stabilization means that another founded revisit adds no receiver class, generator, relation,
separator, obstruction, higher cell, support-visible morphology or reachable consequence. One
unchanged scalar or one pass over the source is not stabilization.

## Process aperture

Brandon's direct apparatus ruling is now absolute: no process started by an agent may run longer
than 180 seconds. The limit is an exterior wall-time aperture, not a semantic capacity and not a
batch-size knob. A deed which cannot return an atomic addressed section inside it must be factored
before launch. Every invocation carries an outer 180-second kill boundary; a kill or timeout is a
counterexample, never a partial pass, and the identical closure may not be retried without a named
new falsifier. Durable addressed section returns make progress resumable without replaying sections
whose source, input, body and executable closure are unchanged.

## Falsifiers

The correction refuses if it patches the cohort path for P2; launches one full foreign tower per
history; chooses a fixed history count or token count as a capacity; lets a process exceed 180
seconds; writes the first useful artifact only after the complete family returns; treats a timeout
as progress; glues process sections without complete coverage and exact interchange; or re-enters
the foreign tower after the W2 candidate boundary has already returned.

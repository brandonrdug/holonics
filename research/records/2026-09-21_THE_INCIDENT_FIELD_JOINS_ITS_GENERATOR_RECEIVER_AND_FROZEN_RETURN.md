# The incident field joins its generator, receiver and frozen return

[established-bounded; measured] The native composition, full-size development/restart and
structured mathematical/code consequence have returned. Both complete-source conversation
cases still fail their requests after the receiving-port, selection and inspection repairs.
This is implemented machinery and an executed negative application result, not completion of
useful Athena conversation.

[definition] This is the implementation return for the September 20–21 campaign, governed by
[the executable contract](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md#executable-field-campaign).
It continues the [native incidence and helical generator](2026-09-20_NATIVE_INCIDENCE_FORMS_AND_REUSES_THE_HELICAL_GENERATOR.md)
and the [geometric field return](2026-09-20_THE_GEOMETRIC_FIELD_REFINES_AND_RETURNS_ITS_COMPLETE_PAIRED_CURRENT.md).
The [live state](../../CONSTRUCTION_STATE.md) records the current consuming operation.
This record distinguishes returned implementation, measured controls and unfulfilled application goals.

[established-bounded; source-inspected] Implementation is recorded in
[`84c08ba2`](https://github.com/brandonrdug/holonics/commit/84c08ba2)
(native field/material and formal laws) and
[`4c0a4165`](https://github.com/brandonrdug/holonics/commit/4c0a4165)
(public incident application, fixed receiving ports, rest and cold inspection).

## The consuming construction

[established-bounded; source-inspected] IncidentField is a tagged mode of NativeCoupledBody and
NativeFieldSession. Its local reaction reads Phi(q,Delta), with Delta_i=U_i q_i-q, while the
normalized participation supplies the independent drive y. One global S_D receives (a,b), and
held relaxation advances the complete joint current. Local row restrictions do not create one
copy of b per site. Material sharing follows explicit template owners and their ordered ports.
The old geometric Phi(y,p) domain and old session formats remain identifiable.

[established-bounded; source-inspected] The operative source now carries declared incidence
columns separately from observation births. Its native action reads a sparse incidence anchor
and retained outer-product factors directly. It certifies Richardson output through the final
residual of I+DD*, which is bounded below by I. Intermediate solve proposals need no separate
accumulated error certificate: the final residual includes the actual contraction, parameter
and source uncertainty. The source-only foundation omits the unused dense legacy relation and
covariance while retaining an explicit representation tag. Global allocation and per-block
shared memory are different hardware resources.

[established-bounded; source-inspected] The complete return includes g_b, both D outer products,
participation variation and both arms of every incident difference. The sparse pullback uses
the same self-adjoint S_D on the supplied covector. It obtains lambda=(g_a+g_w)/2 and the other
factor operands from the producing input/output. One dyadic step scales one side of each outer
product. Normal material stages its contemporary proxy target W_now f+sigma g_producing. All
material successors are prepared before publication; a delayed observation keeps the current
q/b rather than recommitting its earlier generated current.

[established-bounded; source-inspected] E_in, R_text and support use the existing normal owner.
E_in keeps diagonal one-hot source statistics. R_text cohorts have their actual observation
scopes. Their seeded nonzero priors carry H0=I, B0=W0 and C0=norm(W0)^2; the applied W0 is
initialized as well as its statistics. Support starts at zero and reads the complete generated
response section plus a homogeneous coordinate. Its target is an actual response extent.
New incoming source scalars are explicitly admitted; evaluation targets do not perform that
admission. A development target outside an old codec receives an explicit R_retro comparison
on the frozen generated section. An ordinary old return does not update later receiver rows.

[established-bounded; source-inspected] Source preparation preserves complete ordered parts and
recorded joins. The incident linked-torus aperture follows existing arcs along A, through the
shared junction, then along B. This corrects the former slot-list seam without adding an arc or
dropping the source join. The public field-mathematical request also uses the existing workshop
on the same resident surface. Its explicitly scoped operator/product batch enters declared
complex current coordinates and receives the generated field. Operator handles have batch
scope; the field current has the existing durable lifecycle.

## Executed checks

[established-bounded; measured] The following controls have returned. Their commands and tree
scope belong in the [verification receipts](../../docs/VERIFICATION_RECEIPTS.tsv).

- A nonzero complex prior populates every applied coefficient and cross-source coordinate.
  Its rest retains observed energy separately from prior energy and checks the complete
  numerical witness against total H/B and Q_data+C0.
- The exact rank-one scattering reference checks the full internal adjoint and two accumulated
  material returns with one step factor. The sparse action agrees with the same reference,
  executes its updated D factors, preserves the joint current during learning and refuses an
  inconsistent transpose before publishing it.
- Native local controls distinguish q/Delta from participation, compare the complete finite
  word with a directional return, and reopen its frozen producing word.
- The actual receiver test gives R*(target-p)=1/2+i for its declared complex rows and excludes
  an extra softmax Jacobian. Encoder permutation and append tests retain source identity.
- The public session commits a current, retains another preview, admits a later codec row,
  learns through R_retro, saves an older pending comparison, and reopens it. The older return
  leaves the later cohort unchanged. Its resumed result and uninterrupted result agree.
- The existing complex-multiplication construction infers its factor core, emits Rust from
  that core, and supplies its retained native product to the same field. The held receiver
  returns -7/64+22i/64 and the free field changes. This checks a supplied structured operation;
  it does not establish interpretation of an unrestricted prose request.

[established-bounded; measured] The broader HNA native/stream run returned 97 successes and
three saved-session reader failures. The failures shared one cause: absent factor metadata
was written as null but read as usize. After correcting the optional metadata reader, the
three affected delivery/reopen tests passed. Existing legacy charts remain part of that scope.

## Full-capacity recorded application

[established-bounded; measured] The declared model has 76,599 geometric sites, three local
complex coordinates, five explicit material templates and a 512-character receiving aperture.
Its source capacity is 76,087 cells, sufficient for the unchanged 76,081-character repository
case. The initial 46-character exterior codec comes from development sequences 773–776.
No evaluation target selected that codec.

[established-bounded; measured] The first successful split used the complete seven-character
request at sequence 775 and the complete preceding 773/774 source, 567 characters together.
It acknowledged all three exposed frames and retained the native word and its application
comparison. Sequence 773 was context, outside the declared seven-byte query aperture for this
control. The model returned a generated receiving section and saved its pending session.

| Quantity | Observed result |
|---|---:|
| Generation | 232.086 s |
| Complete split process | 682.428 s |
| Resident allocation peak reported by the model | 3,237,248,116 bytes |
| Host-to-device traffic through the reported boundary | 2,257,847,248 bytes |
| Section egress, including checkpoint work | 4,611,364,352 bytes |
| Pending checkpoint | 4,436,880,359 bytes |

[established-bounded; source-inspected] These costs expose concrete representation work. The
support normal has source width 1,537 and 513 target rows. Its dense normal state alone contains
116,584,602 i64 intervals, about 1.865 GB including both carrier arrays. Frozen/current rest
payloads and their JSON byte representation add storage and cold validation work. The native
source action is sparse, but this support statistic is still dense. The large run also exposed
and corrected a constructor that applied the per-block shared-memory limit to global buffers.

[established-bounded; measured] The native split succeeded before the experiment runner
rejected its report using the old geometric pending-word expectation. The corrected incident
expectation accepts the same saved result; generation was not replayed to repair that report.
The saved word reopened and consumed the complete 388-character linked response once, with
no native refusal and no replayed generation. Its update took 468.884 s; the complete debug
resume process took 1,644.429 s, including cold reconstruction and checkpoint validation.
Uninterrupted comparison, both evaluation episodes and their content assessments remain in
progress at that measurement. No conversational quality is inferred from the controls above.

[established-bounded; measured] The subsequent uninterrupted release run consumed all four
development frames and applied the same linked response once, with no native refusal or pending
comparison left. Its final 2,441,477,554-byte checkpoint is **byte-identical** to the earlier
resumed checkpoint: SHA-256 `ad911e5453dadcf9ff1fb792bf8793914caa24b24600133f84ca8fba822f02f3`.
Generation took 77.141 s, learning 222.781 s, and the complete process 393.966 s. Its reported
resident peak was 5,797,866,036 bytes, ingress 2,418,443,088 bytes and section egress
4,201,202,208 bytes including checkpoint work. The build used release optimization, LTO disabled
and debug level 1; CUDA remained O3. Profile and execution changes both contribute to the timing
difference from the earlier debug run. Endpoint identity checks the numerical/state consequence;
it does not isolate a hardware speedup or assess the generated language.

[established-bounded; measured] The combined engine run passed 192 of 193 checks. The remaining
test still expected eager construction of an optional squared matrix. The updated test admits
the representable map, requests its oversized squared-matrix receiver and checks refusal without
a partial cache or predecessor mutation; it passed. All 100 HNA native/stream controls passed,
including delayed codec growth, preserved producing faces, reopen and the mathematical port.

## Actual receiving consequences

[established-bounded; measured] Both unchanged held-out sources executed through the trained
full-capacity field. Event 3406683 supplied all 438 request characters. Event 3398355 supplied
all 76,081 characters: the 2,915-character request and 73,166 characters of its two repository
sources. Historical candidates and later returns remained outside input. Source admission
extended only the exterior codec from those actual incoming inscriptions.

[established-bounded; measured] The initial text receiver selected the centre of each enclosing
normalized face. In both cases its support intervals had become [0,1], yielding 513 identical
midpoints and an empty response. The retained real support potentials still had a unique nominal
winner, length 388. The existing `NormalizedExponential.face_mass_le_iff` and native basis receiver
supply the repair: select on those real potentials, retaining the original radius and robustness
receipt. Normalized faces still serve their declared comparison/return operation.

[established-bounded; measured] Both cases were rerun through that repaired public receiver.
The generated native boundary and support potentials were exactly equal to the earlier runs.
Both emitted 388-character candidates, all character selections nonrobust. The first is
incoherent text; the second is almost entirely one repeated character. Neither answers its
request. Complete source admission and successful code execution therefore do not close
the conversational application.

| Retrospective source | Native generation after selection repair | Complete process | Response joint radius, approximately | Content assessment |
|---|---:|---:|---:|---|
| 3406683, 438 characters | 96.504 s | 174.976 s | 112,049 | Requested mathematical discussion absent |
| 3398355, 76,081 characters | 146.034 s | 225.489 s | 19,084 | Requested contextual/ontological audit absent |

[established-bounded; source-inspected] A valid outer enclosure is not necessarily a useful
nominal representative. `normalized_face_ball` takes coordinate interval midpoints; a broad
simplex enclosure can therefore have a centre outside the simplex. Repeated joint-to-row
projection followed by independent-row recombination also widens a radius r to approximately
sqrt(k) r for k projected rows. That rule is sound for independent rows; tightening it requires
the actual common-source relation, not deletion of uncertainty. The full-word reference and
normal-material bounds remain part of this diagnosis. A same-source refinement at grain 96 and
1,024 solve steps is being executed, with geometry, seed, ports and learning law unchanged.

[established-bounded; measured] The finer-grain development run completed and saved its trained
state: 85.037 s generation, 263.713 s update, 440.709 s complete process. Its first held-out
case reduced the response joint radius to approximately 0.00118142 and certified the nominal
support winner. This improved arithmetic evidence exposed a separate binding error: 120 of the
first 129 returned characters matched the previous source cells at the new response addresses.
The implementation had placed receiving slots immediately after each actual source extent,
thereby moving them across old input cells when the request length changed.

[established-bounded; source-inspected] The corrected application records a fixed
`response_port_start` and uses one binding helper in generation, observation and rest. Fresh
models reserve the final receiving aperture by default; explicit declarations select their
actual range. Sources remain before that range. Existing saved models retain their original
binding for reproduction, while the corrected full model is founded and cultivated at its
declared ports. This enacts the existing source/receiver frame obligation rather than treating
copied previous source text as a learned contextual reply. The full corrected-port run is the
application operation returned with the same complete sources below.

[established-bounded; measured] The fixed-port model trained on the same four development
frames at grain 96 and 1,024 solve steps. Generation took 92.480 s and the linked update
264.167 s; the saved model is 2,446,704,127 bytes. Its receiving slots are 76,087–76,598 for
both short and full-source requests. Focused native checks also reopen an outstanding
comparison after intervening learning and codec growth in this corrected chart.

| Corrected fixed-port source | Generation | Complete process | Response joint radius, approximately | Robust emitted positions | Content assessment |
|---|---:|---:|---:|---:|---|
| 3406683, 438 characters | 120.963 s | 201.552 s | 0.00117962 | 113 / 388 | Incoherent; requested mathematical response absent |
| 3398355, 76,081 characters | 298.612 s | 379.102 s | 0.000388284 | 15 / 388 | Incoherent; requested contextual audit absent |

[established-bounded; measured] Both support selections are robust at length 388. Here
“robust” only certifies constancy over the supplied numerical/normal enclosure; it does not
certify relevance or truth. Fixing the port identity removes the direct reinterpretation of
old source cells, and finer arithmetic narrows the enclosures. Neither repair has made these
language responses useful. The 773–776 population is one paired-return development control,
not evidence of broad conversational cultivation. #16/#17 retain that unfulfilled application.

[established-bounded; measured] Terminal section inspection now reads a complete section once,
then decodes its rows or radii through the existing carrier decoder. The earlier row loop read
the entire section for every row. On the same 3406683 input and trained fixed-port model, this
reduced pre-checkpoint section egress from 1,996,735,168 to 4,145,856 bytes; text, selections and
the complete response ball are exactly equal. Native generation remained approximately
121 seconds. This repairs an observer transfer cost, not a claim of faster field inference.

[open] The consuming remainder is native source-conditioned generation over further actual
development families. The large support normal still stores dense statistics: a 512-position
aperture uses a 1,537-source/513-target normal state. Its compact factor law
H=I+FF*, B=B0+YF* and corresponding small factor solve belong to the existing normal owner
(#18); retaining common source bounds through row composition remains necessary as development
continues. These are concrete representation and cultivation obligations. No missing
foundation for encoding, receiving or Holonic Compression is inferred from the failed prose.

[established-bounded; measured] The existing structured complex-product/code construction also
ran through the trained 76,599-site field. Its retained resident product entered held complex
coordinate 0, whose returned centre was -7/64+11i/32. A free receiving coordinate also returned
through the same field. The code emitted from that inferred three-product factor graph compiled
and passed four exact complex-product cases and the declared input-extent refusal. Generation
and committed field reception took 95.922 s; the complete process took 207.820 s. This is a
supplied structured operation and executable code consequence, not interpretation of arbitrary
prose or a successful conversational answer.

[proved-derived; formal-checked] `AccumulatedNormalResponse` now connects the actual matrix
equation P(I+sum f f*)=W0+sum y f* to the fitted-energy identity through rectangular Frobenius
pairing. Its internally proved augmented Cauchy bound yields
norm(P)_F <= sqrt(norm(W0)_F^2+sum norm(y)_F^2). This replaces the earlier zero-prior minimizer
statement that merely enlarged its right-hand side. The normal proxy residual is also proved.
The focused Lean target and axiom inspection completed without `sorryAx`.

[established-bounded; source-inspected] The same normal proposal can be solved as its actual
coupled coordinate block plus isolated diagonal coordinates. The original full-width diagonal
majorant, H/B statistics and complete WH-B residual remain unchanged; each successor rebuilds
the decomposition, so a currently isolated direction remains available to future learning.
Cold rest validation similarly skips only exact zero products and retains the complete witness.
The sparse action's low-rank contractions now sum disjoint coordinate contributions through
an exact integer block reduction before their single grain division. This reduces serial work
without replacing the operator or discarding a direction. Performance comparisons must use the
same input and distinguish release/debug cold work from device execution.

## Following the failed response through its actual producing material

[definition] These diagnostics use the existing difference/receiver construction. The
[Compression tablet](../../docs/canon/TABLET_THE_COMPRESSION.md#7-what-this-tablet-refuses)
states that a scalar loss is a receiver face of a residual, not the residual itself.
Here an enclosure bound retains the difference between the delivered current and the family
specified by the field operation at its supplied source. A target loss compares different
operands through its own receiver. The
[composition guide](../../docs/HNN_COMPOSITION.md)
also retains the lifted complex logarithmic cross-entropy and its oriented phase defect.
The real probability loss used in this text control does not replace that construction.

[definition] Decimal losses, angles and ratios below are exterior observer calculations from
the exact native receipts. They are not native coefficients or currents. The source, learned
material, solver and returned packets use checked exact/rational/dyadic carriers. For example,
the accelerated response's joint-current radius is the exact rational
`121814186805117144625222295 / 79228162514264337593543950336`; its printed decimal is only a
reading of that packet. The current source family, numerical enclosure and target comparison
retain their different comparands and scopes.

[project-postulate] The continuing objective remains
[Holonic Encoding's formation and reuse of transformations](2026-09-11_HOLONIC_ENCODING_RETAINS_TRANSFORMATION_GRAIN_ACROSS_MODALITIES.md),
including generator inference from partial observations and use under changed conditions and
receivers. Font-rendered faces, acoustic partials and sampled waveforms exemplify receiving
presentations of such generators; they do not prescribe new indivisible token classes.
The [native helical/contextual construction](2026-09-20_NATIVE_INCIDENCE_FORMS_AND_REUSES_THE_HELICAL_GENERATOR.md)
already forms and reuses a local law with an unresolved condition family. The incident body's
learned M/D law also acts on changed sources. Replaying one response below diagnoses its return
path; character agreement is not substituted for this broader reusable construction.

[established-bounded; measured] Replaying the complete development source against its learned
fixed-port model returned only **18/388** observed characters. This comparison concerns retention
of the admitted observation, not correctness of that historical response. Cold inspection of
the actual E/R normal states and frozen receiving operands found:

* R_text moved **5.26%** in Frobenius norm from its initial map. Applying the initial map to the
  learned response current reproduces **486/512** of the current nominal selections.
* The average response cross-entropy was **3.80983**, against **3.82864** for a uniform 46-class
  face. The stored normal solve retains a residual bound of approximately **3.18e-25**.
* The response current has mean cosine **0.95395** with the observed characters' initial
  source columns after subtracting the codec mean. The target pattern is present in its
  directions, despite insufficient magnitude/margins at the lightly changed receiving map.
* In the larger retrospective case, reconstructing all current receiver potentials reproduces
  **512/512** native selections. **288/388** emitted classes belong to newly admitted, untrained
  rows. Their initial biases explain why the weak-current face preferentially emits those
  classes; this is not a failed Unicode rendering operation.

[established-bounded; measured] Changing one admitted source character while retaining the
same model/current changes the response current by norm **0.007293**, or **0.5804%** of its
previous norm, and changes one nominal selection. Most of that difference lies in the final
eight receiving rows, near the shared geometric junction. The first 388 rows change by norm
**0.0006688**; their enclosing families overlap. The actual graph has directed source-to-response
distances 75,521–76,032 for this short source, but undirected distances 2–513 through the torus
closure. Neither local depth nor those distances alone describe the global inverse's coupling.

[proved-derived] For an exact preceding normal solution W H=B, concatenate the actual frozen
source/response features F and their descending covectors G. The implemented proxy target is
Y=W F+sigma G, so
`W_next-W=sigma G F* (H+F F*)^(-1)`. Source and response observations each enter once. This is
the existing normal return, not a sign correction or a new optimizer. Its proxy decrease does
not by itself establish a decrease after the complete nonlinear field is rerun.

[established-bounded; measured] A further controlled observation of the same recorded response,
using sigma=1/4 and **holding the continuing q/b fixed**, improved nominal character agreement
from **18/388 to 295/388**. The response cross-entropy sum fell **1478.213 → 1401.381**; source
reconstruction fell **2126.022 → 2094.376**, and support loss fell **6.209 → 5.438**. The combined
value fell **3610.444 → 3501.195**. These are exterior measurements of the native real potentials.
The resulting text is a recognizable, imperfect reproduction of that observation, not a useful
response to either unrelated retrospective request or evidence of broad development.

[established-bounded; measured] The same update exposed the next numerical failure. The
certified operator norm grew from approximately **4.640 to 11.814**. Auto-selected Richardson
omega changed from **1/32 to 1/256**, while the fixed budget remained 1,024 iterations. Its
worst-case contraction changes from approximately **7.6e-15 to 0.0182**. The returned response
radius grew **0.001181 → 40,158.295**, and all emitted selections lost robustness. Every text
probability interval widened to [0,1], whose existing midpoint receiver has centre mass 23
over 46 classes. This defeats further useful return through that coarse face. The operator's
retained material error stayed approximately 5.8e-24; changing that error is not the explanation.

[definition; agent-inferred] The numerical repair keeps `A=I+DD*` and the existing independent
final residual certificate. `IncidentFieldSolver::Chebyshev` selects a resident semi-iteration
over the certified spectrum [1,b], where b is the existing dyadic upper bound. With
`d=(b+1)/2`, `c=(b-1)/2`, start `alpha_0=1/d`, `p_0=alpha_0 r_0`; subsequently use
`gamma_k=c² alpha_(k-1)/2` for k=1 and `/4` thereafter,
`alpha_k=1/(d-gamma_k)`, `beta_k=gamma_k alpha_k`,
`p_k=alpha_k r_k+beta_k p_(k-1)`, and `v_(k+1)=v_k+p_k`.
Its rounded candidate is certified against the actual D/D* action at the end. This changes
the numerical proposal, not the field or learning law. Richardson remains the saved default;
reconfiguration requires returned/released comparisons and invalidates an older unpublished
proposal. Model and presentation rest retain and cross-check the selected solver.

[definition; implemented-exact] Normal fitting also reuses identical complete B rows under
their common H. It solves and certifies one exact representative, then returns its coefficients
and residual to every matching row. Aggregate norms/residuals still count all rows. A later
distinct B separates those rows again; there is no permanent identification of length classes.
The 513-class support receiver has 512 equal non-target rows after its one-length observations.
Its dense H/storage remains a separate representation cost under #18.

The [cold material/comparison example](../../crates/holonics-hna/examples/athena_boundary_material.rs)
and [experiment instructions](../experiments/athena_field/geometric/README.md) expose these
operands through the existing session. Raw source, target and coefficient artifacts remain
private. Actual native checks and the full learned-model numerical comparison accompany the
repair below.

[established-bounded; measured] The same learned E/R/material state, with Chebyshev-512,
returns response radius **0.001538** and **252/388** robust emitted selections. Character
agreement is **187/388**, with response cross-entropy sum **1457.132**. The earlier 295/388
agreement belonged to the poorly converged Richardson evaluation; it is not the accurate
field's result. The native accelerated evaluation still improves on the initial 18/388,
while exposing the remaining learned-response defect. The before/after cold E/R states are
exactly equal in this solver-only comparison. This source run used a GPU also occupied by
another application, so its elapsed time is not a clean performance comparison.

[established-bounded; measured] Complete learned-model remount finished in **99.483 seconds**
after repeated exact cold `(W_row,B_row)` witnesses were reused. The previous attempt was
interrupted before its application callback after more than **396 seconds**; that unfinished
attempt is not a completed benchmark. Cold validation still checks the complete source geometry,
coefficient norm, residual and family bound. A corruption test changes a formerly repeated
coefficient row and its stored norm; the independent residual check rejects it.

[established-bounded; native-checked] Eleven operative source checks, the native repeated-row
fit/split check, 21 incident application/return checks and the cold repeated-row corruption
check pass. The high-norm source fixture excites both the fast and slow directions of A;
Chebyshev-512/1024 returns residual bounds below 1e-20 and the exact forward/full-pullback
oracle, where Richardson-1024 retains a substantial slow-direction residual. Pending rest,
legacy solver default, numerical reconfiguration and frozen comparison readouts are exercised.

[established-bounded; measured] The next controlled observed return reached a carrier refusal
before publication. The failing operation was `section_field_factor_delta_bound`, which
collapsed two retained factor balls into the scalar matrix bound
`rp*norm(C)+rc*norm(P)+rp*rc`. Its two radii and factor centres were representable, while the
derived scalar could exceed the signed-128 carrier at grain 96. The preceding model remained
unchanged. This is a representation failure of that receiving bound, not a new loss definition.

[definition; implemented-exact] Sparse `DyadicDeposit` already means installing the exact
finite factor products, while retaining their difference from the unrounded material return
in the journal. That journal now explicitly distinguishes `FactorBalls` from the legacy
`MatrixAndInternal` bound. It retains P, C, rp, rc and their producing relation without forcing
the scalar collapse. Existing active map/current uncertainty is preserved. A cold receiver
can derive a conservative matrix bound with arbitrary-precision rational arithmetic; the small
bound inspector exposes the original factors' radii without expanding a boundary-by-contact
matrix. Older dyadic journals keep their recorded matrix/internal interpretation.

[established-bounded; native-checked] A sparse native regression uses exact factor radii
`rp=rc=2^24` at grain 96. Their product exceeds the scalar carrier's range. `EnclosedFlow`
refuses without publishing; the exact dyadic factor deposit succeeds with its reference
families retained, and rest/remount preserves the explicit bound tag and values. The legacy
tag remains legacy on reopen. All 34 operative source/material/rest checks and 21 incident
application checks pass after this repair. Full-source retry is recorded separately; the
small regression does not establish its outcome or conversational usefulness.

[established-bounded; measured] The full-source retry completes that previously refused
return, saves its successor, and generates again. A full cold remount returns exactly the same
encoder/receiver coefficients, normal statistics, priors and typed material-reference bounds.
The retained exact factor-radii product at
the second stage exceeds `(2^127-1)/2^96`; the factors and reference families remain present in
the saved journal. No wider floating-point representation or discarded defect is involved.
The native phase receipts report 139,375,771 microseconds in the receiving return,
314,089,164 in the field return and 4,333,206 in the encoder return. These costs still require
compact normal statistics and reuse of already produced field actions.

[established-bounded; measured] This continuation also exposes a learning-objective issue
separate from the repaired representation. As exterior observer readings, source reconstruction
cross-entropy decreases **2094.376 → 2061.331**, while response cross-entropy increases
**1457.132 → 1463.758**; the combined source/response/support reading decreases
**3557.426 → 3531.015**. Observed-character agreement falls **187/388 → 153/388**.
At the retained old field features, the new receiver alone improves response cross-entropy to
**1450.582**; reading the new field through the old receiver gives **1468.513**. Thus the
receiving fit itself is not the source of that deterioration; the changed upstream field/E/M/D
composition needs its own consequence comparison. This does not isolate those upstream owners
from one another or establish that an auxiliary source objective is universally wrong.

[definition] A decreasing aggregate scalar does not close the requested receiving consequence,
and repeated character reproduction does not close reusable generator formation. Continue from
the existing source/condition formation and receiving laws, retaining their directional residuals,
partial families and changed-context consequences. The repaired numerical and journal paths are
construction results; useful Athena conversation remains unachieved by this population.

## Source owners

[definition] The source network for this return is:

| Relation | Current owner |
|---|---|
| Incident word, full current and reverse word | [incident word](../../crates/holonics-hna/src/native/coupled_wave/body/field/incident.rs) and [rest](../../crates/holonics-hna/src/native/coupled_wave/body/field/incident/rest.rs) |
| Declared D, sparse action, staged factors and current | [source/action](../../crates/holonic-engine/src/native_ecology/constitutive_fibre/field/junction/operative/source/action.rs), [factor return](../../crates/holonic-engine/src/native_ecology/constitutive_fibre/field/junction/operative/factor_return.rs); [native factor action](../../crates/holonic-engine/kernels/field_factor_action.cuh) |
| Prior and native normal return | [normal state](../../crates/holonic-engine/src/native_ecology/constitutive_fibre/field/material_transport/normal.rs) and [direct normal owner](../../crates/holonic-engine/src/native_ecology/constitutive_fibre/field/material_transport/normal/direct.rs) |
| E/R/support, source preparation and frozen receiving cuts | [application](../../crates/holonics-hna/src/native/field_session/incident_application.rs), [encoder](../../crates/holonics-hna/src/native/field_session/incident_encoder.rs), [receiver](../../crates/holonics-hna/src/native/field_session/incident_receiver.rs), [preparation](../../crates/holonics-hna/src/native/field_session/incident_preparation.rs) |
| Shared mathematical input and field ports | [mathematical workshop](../../crates/holonics-hna/src/native/mathematical.rs) and [field port](../../crates/holonics-hna/src/native/field_session/mathematical_port.rs) |
| Formal laws | [joint adjoint](../../formal/elementary-holonics/ElementaryHolonics/Computation/HolonicAdjointNormalization.lean), [codec chart](../../formal/elementary-holonics/ElementaryHolonics/Computation/HolonicCultivationCharts.lean), [normal law](../../formal/elementary-holonics/ElementaryHolonics/Physics/AccumulatedNormalResponse.lean) |
| Recorded caller and assessment | [exposure caller](../../crates/holonics-hna/examples/athena_exposure_field.rs), [episode runner](../experiments/athena_field/geometric/run.py), [consequence receiver](../experiments/native_performance_benchmark/quality.py) |

The [composition guide](../../docs/HNN_COMPOSITION.md), [conversation data guide](../../docs/CONVERSATION_DATA.md)
and [research reading routes](README.md) carry these connections into the next operation.

[established-bounded; source-inspected] The issue bodies were reconciled with this return.
[#19](https://github.com/brandonrdug/holonics/issues/19) closes the returned complete adjoint and
atomic material/current transaction. [#18](https://github.com/brandonrdug/holonics/issues/18)
retains compact support statistics, [#61](https://github.com/brandonrdug/holonics/issues/61)
the measured execution/common-bound work, and [#16](https://github.com/brandonrdug/holonics/issues/16)
/ [#17](https://github.com/brandonrdug/holonics/issues/17) the failed contextual application.
[#48](https://github.com/brandonrdug/holonics/issues/48) retains its broader elementary helical
clock/receiver obligations and links this actual consumer without claiming nonlinear moment closure.

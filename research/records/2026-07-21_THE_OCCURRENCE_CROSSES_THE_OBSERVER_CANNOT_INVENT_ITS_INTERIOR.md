# THE OCCURRENCE CROSSES; THE OBSERVER CANNOT INVENT ITS INTERIOR

**2026-07-21 · Brandon-authorized causal audit · SITUATED EVENT + CROSS-FACE WITNESSES BUILT ·
HOST-OBSERVED · NO ENGINE CHANGE · NO VISUALIZATION CHANGE**

## Present question

What did the bounded prime world actually present to `Lambda`, what did the live mouth actually
form, and what did the later prime observer derive on its own? In particular, can the current
observatory lawfully present `17` as a receiver with a pre-existing construction atlas?

The answer is no. The arithmetic is lawful, but its current causal label is wrong.

`17` is present in a world-computed list of primes through `509`. It is not one of the eleven
prime occurrences enacted by `BOUNDED POLYGLOT GEOMETRIC WORLD 01`. No current carrying the source
candidate `17` crossed that run, no `17` contact was returned, and no `17` placement entered its
standing successor. `primeReceiverRead(report, 17)` nevertheless derives residues, a new-axis
FOUND label, cyclotomic coordinates, and bounded affine relations. Those rows are an observer-side
arithmetic construction over the report, not testimony of an Eros event.

This distinction does not invalidate the prime mathematics or prohibit a world transducer from
supplying it. It establishes the boundary that an Eros observatory must retain.

## 1. The four causal owners

Every field in a situated observation belongs to exactly one of four roles at the declared cut.

| Role | Meaning | Prime-world instance |
|---|---|---|
| `SOURCE_DECLARED` | Identity, constraint, objective, chronology, co-presence, or adjacency supplied by the world | horizon `509`, integer succession, radices `2/10/16`, selected receiver polynomials, event order |
| `WORLD_DERIVED` | A lawful transducer result computed before the mouth | primality, residues, radix digits, finite-field factor degrees, Zeta cylinders |
| `MOUTH_EXECUTED` | Geometry that crossed and contact, placement, radiation, or successor formed by the direct machine | six path complexes at enacted prime `23`, their current radiation, the changed standing body |
| `OBSERVER_PROJECTED` | A later section, quotient, comparison, or display derived from testimony | `primeReceiverRead`, phase embeddings, affine search, canvas coordinates |

A source declaration may be causal current. A world derivation may cross after the source lifts it
into material geometry. An observer derivation may become later current if another world presents
it. None of those permissions allows a later observer to relabel its own reconstruction as an
earlier machine event.

## 2. What the bounded world actually did

The world computes its arithmetic outside Soma. `morphology`, `zeta_read`, and `receiver_read`
produce the mathematical faces. `morphology_values`, `radix_values`, `species_values`, and
`path_values` then convert selected results into ordered integer tuples. Each integer `x` is lifted
to the canonical odd relation `2x+1`, and each tuple becomes a `NativePathChart` whose adjacency is
source-declared.

The enacted prime population is exactly:

```text
7, 11, 13, 23, 29, 37, 43, 53, 193, 229, 509.
```

The report's complete `founded_primes` list is a different, world-computed population. It contains
`17`, but `engine_read` never presents a `17` event.

For the selected enacted occurrence, candidate `23`, the source constructs these six faces:

| Organ lineage | World tuple before lift | Relation atoms that cross |
|---|---|---|
| zero-relative | `[23,1,2,4,-2,2,1,3,-1]` | `[47,3,5,9,-3,5,3,7,-1]` |
| radix `2` | `[2,23,5,1,0,1,1,1,2,1,3,2]` | `[5,47,11,3,1,3,3,3,5,3,7,5]` |
| radix `10` | `[10,23,2,2,3,2,1,3,2]` | `[21,47,5,5,7,5,3,7,5]` |
| radix `16` | `[16,23,2,1,7,2,1,3,2]` | `[33,47,5,3,15,5,3,7,5]` |
| `x^4+x+1` | `[23,1,3,1,1,2]` | `[47,3,7,3,3,5]` |
| `Phi_5` | `[23,1,1,4]` | `[47,3,3,9]` |

This is not raw `23`. It is source-precomputed arithmetic ecology in which `23` is one constituent
of six different ordered complexes. The source is entitled to supply those identities and
constraints. The receipt must say that it did.

All six currents cross one co-present event with the same action `137`, but the world supplies:

```text
directed relations = []
regional cells     = []
```

The internal adjacency of each tuple is real because `NativePathChart` supplies it. No causal hand
between the six faces is supplied. Their contributions enter one same-pre-state successor, but an
observer may not draw an incidence between, for example, the radix-10 face and the `S_4` face.

The committed historical report says the first three enacted events are silent and the `23` cut
returns four emissions over an empty standing body. The current source no longer does that. The
bounded situated host witness records standing `23 -> 44` at `prime:23`, with current-local
emission counts `[5,3,0,5,4,1]` and incidence-contact counts `[16,22,16,16,10,6]`. The world's
deterministic `actual_return` selection now points to `chi_other` of emission `0`, current `0`, at
the earlier enacted occurrence `prime:11`. The witness preserves that address instead of
retroactively assigning the return to `23`.

The six `23` faces still cross with zero directed relations and zero regional relations. Their
same-pre-state contributions form one changed successor, but no observer may infer cross-face
incidence from that co-presence. The later returned-world effect remains real in the current run;
it does not establish that Soma discovered primality, derived the six source faces, or formed a
hand which the source omitted.

## 3. What the current report loses

`ContemporaryRadiation` is richer than `REPORT.json`. It can return:

- each current's consequence, emissions, and event-incidence contacts;
- every source-supplied directed relation and its exact contact;
- every regional arc, contact, and completed constituent; and
- the exact standing rank and cell extent before and after the atomic transition.

The bounded report writer retains only rank/cell counts, current lineage, a compressed consequence
read, and outward emissions. It omits:

1. the exact ingress geometry and source occurrence which produced each current;
2. the world-local chronology/event identity linking the arithmetic row to the radiation row;
3. the current's event-incidence radiation;
4. directed and regional relation radiation;
5. an exact standing-before and standing-after identity or addressable rest reference;
6. the origin event/current/emission address of `world_return_relation`; and
7. the observer cut under which later derived rows were requested.

The report therefore cannot independently reconstruct the event it claims to observe. Array
position happens to align `morphologies[3]` with `source_radiation[3]` in the writer, but the schema
does not declare that relation.

There is also a concrete versioning defect. The committed `REPORT.json` carries the older
consequence fields `fold`, `perception_present`, and `step_present`; the current report writer
declares `cells`, `incidences`, `resolving_cells`, `formed_incidences`, `compounds`, `folds`, and
`receiver_live`. The top-level schema string did not change, and `validatePrime` does not validate
the engine section. The observatory can therefore accept testimony which cannot have been emitted
by the current writer under the same schema.

## 4. Why the scalar-rooted observer is not `Lambda`

The current prime analytical entry point is effectively:

```text
primeAnalyticalReceipt(arithmetic_report, integer_n, span)
    -> derived arithmetic neighborhood.
```

For every integer in the report horizon it can derive a section whether or not that integer ever
crossed the machine. This is a valid bounded arithmetic transducer. It is not `pi_B` for

```text
Lambda_F(Q tensor A tensor L) -> (P, pi_B).
```

In a situated receipt:

- `F/Q` is the declared source frame and unfinished objective as materially expressed at this
  event;
- `A` is the actual standing-before terrain or an exact addressable rest cut;
- `L` is the source occurrence population, native chronology, co-presence, geometry, and supplied
  hand which actually crossed;
- `P` is the placement, radiation, and successor actually emitted; and
- `pi_B` is testimony preserving those relations without performing or replacing them.

An integer may be one constituent of `L`. It cannot silently replace the whole argument of
`Lambda`.

## 5. Minimal situated observation contract

The first contract should be an external bounded testimony record. It must not enter active Soma,
drive commit, retain a dataset-sized history, or make provenance a live lineage.

```text
SituatedLambdaObservation {
  schema + writer/build identity

  source_cut {
    world reference
    world-local event occurrence
    native chronology / partial-order address
    declared frame and objective references
    source identity and constraint declarations
    co-present participant population {
      source occurrence
      organ role
      live-lineage capability before crossing
      exact borrowed geometry: cells, incidence, ports
      action
      continuing / ending hand
    }
    exact directed relations
    exact regional relation cells
  }

  standing_before {
    exact rest-cut digest and addressable artifact reference
    rank and material extents
  }

  crossing {
    complete CurrentRadiation, including event-incidence contacts
    complete DirectedRelationRadiation
    complete RegionalRelationRadiation
  }

  successor {
    exact rest-cut digest and addressable artifact reference
    rank and material extents
  }

  return_link? {
    source observation
    source current
    source emission ordinal
    later world event which receives it
  }

  observer_cut {
    selected receiving event or constituent
    declared projection and quotient
    every additional WORLD_DERIVED or OBSERVER_PROJECTED overlay
  }
}
```

The rest artifact may be stored once and referenced by many bounded event receipts. This preserves
causal verifiability without making the active body a perfect historical crystal.

Every derived overlay must name both its derivation and owner. For example:

```text
WORLD_DERIVED: factorization in Z under the bounded arithmetic transducer
OBSERVER_PROJECTED: cyclotomic screen section using retained rational series
MOUTH_EXECUTED: the world selected one actual lineage-0 emission at occurrence prime@11
```

## 6. Projection law

A causal observatory selects an observation occurrence, not a scalar.

```text
observe(situated_receipt, observer_cut) -> disposable projection.
```

It may show only:

1. exact source geometry that crossed;
2. exact standing factors actually contacted;
3. exact contact, placement, residual, hand, and boundary returned by the mouth;
4. exact later return links; and
5. explicitly labeled analytical overlays.

If the receipt contains only an incoming occurrence, the visual contains only that occurrence and
its declared source context. Silence cannot sprout a factor complex. Co-presence cannot sprout an
edge. Equal numeric constituents cannot sprout shared identity unless source declaration, actual
contact, or later recurrence supplies it.

Changing the observer selection rebases the projection. It does not change the witnessed event or
claim a camera outside the system.

## 7. Bounded acceptance cases

### Negative control: `17`

Against the current committed prime report:

- the world-derived arithmetic face may say `17` is in `founded_primes`;
- the observer must say **no enacted occurrence in this observation**;
- no lineage, contact, RIDE, FOUND, successor placement, or world return may be attributed to
  `17`; and
- opening a fraction or cyclotomic analysis must cross an explicit boundary into a labeled
  arithmetic-transducer view.

### Positive trace: `23`

The situated receipt must recover:

- one world occurrence `prime@23` at its exact source chronology;
- six co-present source currents and all six exact path complexes;
- zero directed cross-current relations and zero regional cells;
- the one same-pre-state standing-before cut;
- current-local emissions `[5,3,0,5,4,1]` and complete incidence contacts in the current witness;
- the exact world-selected return address at the earlier `prime:11` occurrence, explicitly marked
  as not originating in the observed `23` event; and
- the later return and changed `521` successor without calling the change semantic correctness.

The projection must render the six faces as co-present but unjoined unless another actual relation
is supplied.

### Same inscription, changed frame

A later bounded fixture may declare one source occurrence shared by two lawful world faces—for
example integer-factor and polynomial-receiver faces of `23`. The source identity declaration and
each distinct geometry must both cross or be retained as source testimony. Different radiation is
then receiver-relative consequence; equal inscription alone proves nothing.

## 8. Consequence for implementation order

Do not build the prime construction atlas yet.

The first two steps are now closed by
`observations/situated-prime-event-01/OBSERVATION.json`. Remaining visual work, if later selected,
must:

1. make the observatory select receipt occurrences rather than arbitrary integers; and
2. keep the existing prime/fraction/Zeta analyzers as explicitly labeled world or observer
   transducers.

Only then should visual work resume. The visualization will be a reversible face of a known
causal object rather than the mechanism which invents that object.

## Audit boundary

This audit and bounded witness do not authorize an engine change, a new prime fixture, or an
observatory redesign. They close the event-local testimony and causal-labeling defect for the
selected `23` occurrence, expose the committed report as historical rather than current evidence,
and confirm one real absence of source-supplied cross-face hand. Whether that world should later
supply a regional cell is a separate construction question.

## 9. Bounded cross-face follow-up

The event-local executor read is now built at
`observations/situated-prime-cross-face-01/CROSS_FACES.json`. It delegates the selected `prime:23`
event to the ordinary host executor unchanged, captures the exact body contributions and incidence
contacts while they still coexist, and groups formed contributions by the same rank-qualified
`ChartAddress::ground` relation used by the successor. It does not compare raw positions, infer
pairwise edges, or reconstruct the successor from testimony.

The six grain-2 currents produce 17 body contributions and 64 formed contact emissions. Those 81
contributions ground to 21 occupied supports. Every support belongs to exactly one current:

```text
role                   body  formed/open contact  grounded supports
zero_relative             5       16 / 0                 6
radix_2                    3       22 / 0                 4
radix_10                   0        0 / 16                0
radix_16                   4       16 / 0                 5
s4_receiver                4       10 / 0                 5
cyclotomic_receiver        1        0 / 6                 1
```

This is a real internal configuration result. Contact emissions repeatedly collocate inside four
individual currents, but no support is shared across currents. The event therefore contains
parallel co-presence and current-local folding, not a cross-current triangle, hinge, sheet, or
closed face. Standing grows from 23 to 44 cells because exactly 21 new supports are occupied;
higher-grain constituent count remains zero on both sides.

The engine already groups the complete population without arrival order. It must not add a
`LiveConstituent` here: there is no shared support, directed relation, regional relation, or
interface capability from which to derive its support, hand, seam, and grain. The missing datum is
an actual event where independently live currents meet through source-supplied hand or a jointly
formed regional interface. Semantic commonality—six faces all derived from candidate `23`—cannot
substitute for that event.

This follow-up closes the bounded analysis and leaves Soma's successor law unchanged. A later
construction may lawfully make the common candidate or another world relation material, but it
must derive and present the relevant ports, hand, and interface as part of the source event rather
than asking the observer to add them afterward.

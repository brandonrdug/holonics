# The Lean binding of the world return is retracted, and languages are applications

**Date:** 2026-09-02
**Truth status:** `established-bounded`
**Evidence:** `source-inspected` (file and line evidence below, 2026-09-02); `measured` (the WRD4 receipt of the same day, whose kernel faces stand as data)
**Provenance:** Brandon, 2026-09-02, on being shown the WRD driver's candidate files and the template render behind them: *"you have no permission or authorization from me to cater the machine towards generating any specific output format, ever! That is absolutely contamination! … There shouldn't be any fucking reference to Lean in Eros or Athena! The Lean files are data at most for what we are concerned with! The same thing goes for every other programming language, modal, or field! They are applications!"* Assistant audit and retraction.
**Band:** THE JUNCTION, DEPOSIT, AND TERMINATION LAWS STAND / THE MOUTH, EMITTER, FACE ADAPTER, AND DRIVER WERE LEAN-BOUND / THE TEMPLATE EMITTER WAS AN AUTHORED PER-LANGUAGE RENDERER OF THE CLASS SUPERSEDED 2026-08-17 / LANGUAGES, MODALITIES, AND FIELDS ARE APPLICATIONS / THEIR FILES ARE DATA / THE INTERIOR DERIVES THE RETURN FROM FORMAT-FREE FACES

---

## What is retracted

[established-bounded] Of the WRD campaign committed at `c5d44258`:

- `soma/life/src/lean_mathematics/lattice_mouth.rs`: `found_lean_lattice(&LeanMathematicsEcology, ReceiverId)`
  types a modality-neutral mouth on a Lean parser's organs; `LeanLatticeColdWitness`,
  `LeanReferenceWitness`, and the spool address `lean/declaration-lattice` name a language inside
  the interior. The body of the function reads only names, a reference relation, and relation
  labels. Retracted at the signature and the names; the derivation is retained under the revised
  blueprint's neutral mouth.
- `soma/life/src/lean_mathematics/candidates.rs`: `generate_proof_candidates_for` renders
  `exact`, `simpa using`, `rw`, `have`, `apply` templates with an authored closer list. It is the
  July `ecology.rs` render moved, and it is a per-language template emitter. Retracted entirely.
  Under the 2026-08-17 ruling that superseded `statement_grammar.rs`, an authored renderer is the
  same class as an authored grammar.
- `soma/life/src/native_intelligence/route_return.rs`: `kernel_return_faces(&LeanKernelReturnFamily)`
  and the `LeanLatticeColdWitness` parameter of `derive_world_return`. Retracted at the types; the
  junction over `KernelReturnFace { admitted, carried, diagnostic }` is retained, renamed to a
  format-free world face.
- `soma/life/examples/the_kernel_return_deposits_on_the_route_it_crossed_wrd4.rs`: cuts Lean
  prefixes, writes `.lean` files, calls `lake env lean`, and carries `RENDER_VOCABULARY`, an
  authored tactic list. Retracted as an interior driver; its measured faces remain data.
- The July `soma/life/src/lean_mathematics.rs` family (`syntax.rs` parser, `ecology.rs`,
  `kernel_returns.rs` with `LeanKernelWorld`) and the engine's `lean_development.rs` are the same
  class and predate WRD; they exit the interior as an application under the revised blueprint.

## What stands

[proved-derived] `HolonicWorldReturnDeposit.lean`: `T + Γ² = 1`, `Γ = 0` exactly at a match,
service rounds `ceil(1/T)`, the two-end telescoping of a route deposit, the three-site cone
control, and equal faces with different reflections. It names no unit and no modality.

[established-bounded] `holonic_structure::CountedCrossing` and the route-return derivation:
emissions incident, emissions admitted, current `(T, Γ)`, storage `1/rounds`, deposit through the
standing `stage_return` and `commit`, re-entry from what reflected, terminus by type.

[established-bounded] The WRD4 kernel faces as data: on the Foundation corpus one route's
sixteen emissions returned three admitted; the return committed storage `1/2` on a two-carrier
cone; later current reached the deposited thread only by re-entering at that crossing; withdrawal
removed the successor; replay restored it exactly.

## Why it happened, exactly

[historical] The WRD blueprint of 2026-09-02 named the Lean kernel "the first world" and declared
the July render "standing reuse". The retraction record already said production is re-emission
and enumerating candidates is not generation; the 2026-08-17 supersession already said an
authored grammar is a contaminant. Both were in the governing files and were read as history
rather than as the law over the mouth and the emitter. Nothing in the canon, the roadmap, or
Brandon's messages asked for a per-language emitter; the standing module's existence was taken as
license.

## What replaces it

[definition] The revised blueprint
[`THE_WORLD_RETURN_IS_MODALITY_NEUTRAL_AND_EVERY_LANGUAGE_IS_AN_APPLICATION.md`](../../blueprint/THE_WORLD_RETURN_IS_MODALITY_NEUTRAL_AND_EVERY_LANGUAGE_IS_AN_APPLICATION.md)
states the three seams without a codec: a mouth that founds carriers from any population and
reference relation, a world face that is format-free, and an emitter that is the machine's own
emission surface rendered by the application on its side of the ABI.

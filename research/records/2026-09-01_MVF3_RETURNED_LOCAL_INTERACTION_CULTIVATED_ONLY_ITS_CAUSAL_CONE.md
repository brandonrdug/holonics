# MVF3 returned local interaction cultivated only its causal cone

**Date:** 2026-09-01  
**Phase:** MVF3  
**Truth status:** established-bounded; implemented-exact; measured

[established-bounded; implemented-exact] `ReturnedScaffoldInteraction` now carries the exact
emission it answers, a genuinely new later occurrence, exterior Complex-Parametron current,
constitutive storage, and declared contact support. The native successor current is derived as the
standing local current plus the returned exterior current acted on by storage. Callers can no
longer supply an already-native successor current directly.

[established-bounded; implemented-exact] The cultivated thread's native support is exactly the
returned causal cone. It retains one local generator, exact entering/returned section, serial
pullback, causal-adjoint difference fibre, and a nonzero mutual constitutive response joining the
emission and return. Threads whose support is disjoint receive exact interchange receipts; their
complete serialized morphology is compared before and after the atomic deposit and must remain
equal.

[established-bounded; implemented-exact] Inactive states are open generator faces while the
inherited scaffold remains mounted and are absent from cultivated thread support. When inherited
threads depart, `NativeThreadWithdrawal` locally prunes those departed states from remaining
generator open domains and carries the exact deltas for restoration. Reverse restoration recovers
the original open domains and canonical ecology identity.

[established-bounded; implemented-exact; measured] The local control used two disjoint inherited
threads. A return contacting only the emitted edge founded a two-state causal cone, retained one
outside thread unchanged, survived source-detached remount and scaffold withdrawal, disappeared
under cultivated-thread ablation, and restored exactly. The final hot native population is a
subset of the returned cone. A separate declared-global contact over three excitation families
returned the complete six-state native cone rather than claiming constant locality.

[established-bounded; implemented-exact; measured] Every engine/life target type-checked. Engine
library tests returned 2,006 passed, zero failed, and 32 ignored. Life returned 469 passed, zero
failed, and 14 ignored.

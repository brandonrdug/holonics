# The leader returned through the metric, the reflection rested, and M4 returned complete mathematics

**Date:** 2026-08-21  
**Authority:** `blueprint/THE_ROADMAP.md` through
`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`, Deed M4  
**Truth status:** established-bounded  
**Boundary:** one nontrivial M3 receiver/history separator and its complete anchor fibre; one
symmetric rank-one metric-reflection morphology on the five-bend Soddy--Gossett quadric; the
ordered two-reflection held-out word; one NVIDIA GeForce RTX 4080 SUPER; one Lean exterior file;
one exact analytic/vector/raster/mesh receiver family. Arbitrary mathematical formulation, sphere
centres and non-overlap, fractal dimension, environment-shift protein testimony, unexcited Gemma
capability and every broader successor family remain open.

## Ruling

[established-bounded; implemented-exact; measured] M4 passes its named construction grade. A real
M3 receiver difference selected one depth-73 leader. An addressed causal-adjoint receipt returned
only along that retained word. The return founded a local, exact, rank-one metric morphology whose
five transported hands were derived by closure rather than by a caller-chosen capacity. A fresh
source-detached mount conducted the held-out ordered reflection word to `[11,17,2,3,3]` on the GPU,
and targeted ablation removed that cultivated conduct while leaving the predecessor M3 passage
unchanged.

[established-bounded; implemented-exact] This is the first complete M0--M4 mathematics-codec
vertical and a bounded species-changing Phoenix return. It is not a renamed foreign tensor
container: the runtime receives the 1,217,120-octet M3 generator-native rest, the 1,111-octet
cultivated overlay and an exact held-out section. It does not receive the M2 response atlas, Gemma
weights, the Lean source, sphere-packing answers or a retained lookup table. The grade therefore
attaches only to the declared receiver/history and metric-reflection family.

## The leader and its addressed return

[established-bounded; implemented-exact] Leader selection was structural and deterministic:
maximize the returned successor depth, then take the least addressed source pair at that depth.
The selected M3 sources are `0` and `2322`; their native ports are `0` and `2054`; their endpoints
under the 73-letter chronology word are `73` and `1482`; receiver `1` reads `73` and `971`.
The complete anchor fibre is `[0,129,2451,2580,4902,5031]`. Names and SHA-256 identities
authenticate the exterior occurrences but neither classify nor route the mathematical conduct.

[established-bounded; implemented-exact] The causal-adjoint receipt reverses the retained leader
word and nothing else. The present leader happens to repeat one generator, so its forward and
reverse exterior lists are byte-equal; the law is still reversal of the addressed chronology, not
a global replay or a conclusion inferred from that coincidence. The mathematical return uses the
declared receiver metric. It is not bare transpose and it is not matrix inversion.

## The cultivated morphology

[established-bounded; implemented-exact] The carrier is the five-bend quadric

```text
             (sum_i b_i)^2 = 3 sum_i b_i^2,
             b_i G^{ij} b_j = 0,    G = J - 3I.
```

[established-bounded; implemented-exact] The seed morphology is factorized as
`R = I + u v^T`, with `u=e_0` and `v=(-2,1,1,1,1)`. Exact subtraction from the identity measures
rank one; no authored LoRA rank is accepted as a semantic law. The coordinate transport
`[4,0,1,2,3]` preserves `G`, and repeated closure under that transport derives all five hands.
Every resulting reflection satisfies both identities

```text
             R_i^T G R_i = G,       R_i^2 = I.
```

[established-bounded; implemented-exact] The metric adjoint is `G^{-1}R_i^TG`; for this family it
equals `R_i^{-1}=R_i`. A truncated factor is refused because it does not preserve its declared
receiver metric. Thus cultivation is a local constitutive change with a complete exact defect, not
a free matrix patch accepted because one output looked plausible.

## Ordered mathematical return

[established-bounded; implemented-exact] The training occurrence reflects the bowl root
`[-1,2,2,3,3]` at coordinate zero and returns `[11,2,2,3,3]`. The held-out exterior word is written
outer-to-inner as `[1,0]`, whose enacted order is `[0,1]`; it returns
`[11,17,2,3,3]`. The reversed enacted order returns `[14,5,2,3,3]`. The two words therefore remain
separated by their chronology even though they use the same two generators.

[established-bounded; implemented-exact] The quadric defect is exactly zero before and after every
admitted reflection. Two independent derivation routes return the same held-out section: the
coordinate other-root exchange

```text
             b_i' = sum_{j != i} b_j - b_i,
```

[established-bounded; implemented-exact] and the ordered product of the exact metric rank-one
matrices. The returned enclosure collapses to the five exact rational points; no floating-point
approximation selects a value, topology, index or coefficient.

[established-bounded; implemented-exact] The problem specification also returns the reciprocal
radius chart

```text
             b_i = r_i^{-1},
             ((sum_i r_i^{-1})^2)/(sum_i r_i^{-2}) = 3,
```

[established-bounded; implemented-exact] with `[r_i]=length`, `[b_i]=length^{-1}` and
`[b_iG^{ij}b_j]=length^{-2}`. The chart excludes `b_i=0`; a negative bend denotes an oriented
enclosing sphere rather than a negative Euclidean radius. Bend incidence alone does not construct
sphere centres or prove non-overlap. Raster testimony also retains the unresolved face `b_1`
versus `b_l` until a later receiver separates it.

## Resident deed

[established-bounded; implemented-exact; measured] The fresh mounted cultivation rest derives its
five reflection factors and stages the exact held-out section. One CUDA graph owns the entire hot
word: 11 graph nodes, 12 edges, 10 kernel nodes and one memset node, with no memcpy node. It returns
after one graph launch and one synchronization; no CPU semantic cell occurs between reflection
members.

[established-bounded; measured] Apparatus testimony reports 34 ingress octets, 80 returned-section
octets, 320 receipt octets, 754 peak resident octets and zero device-to-device copy octets. Exact
semantic work is 40 additions, 25 multiplications, 54 entries written, 898 cumulative bits, 22
peak bits, 10 resident entries and dependency span 15. The RTX 4080 SUPER returned the exact words
`[11,17,2,3,3]`, matching the source-detached exact control.

## Lean, analytic and visual exterior returns

[established-bounded; machine-checked] The exterior command
`lake env lean ElementaryHolonics/Millennium/SpherePacking.lean` returned exit zero and the source
contains no `sorry`. Its theorem population covers reflection involution, quadric preservation,
wordwise preservation, the integral two-reflection output, noncommutation and lineage retention.
Lean accurately reports dependencies on the standard axioms `propext`, `Classical.choice` and
`Quot.sound`; M4 makes no axiom-free claim.

[established-bounded; implemented-exact] With four bends fixed, the returned analytic face is the
exact polynomial `Q(x)=-2x^2+20x+22`. Its certified real-root population is complete and consists
of the point intervals `[-1,-1]` and `[11,11]`, with no unresolved analytic obstruction in the
declared window.

[established-bounded; implemented-exact] The vector and raster faces are two gauges over one exact
placed-mark population: 17 vector marks and 86,400 raster samples. Gauge permutation changes
display coordinates without changing certified structure. The raster round-trips through an exact
PPM face. The ordered reflection trace also returns an oriented six-vertex, nine-edge, four-face
simplicial strip with six boundary and three interior edges, Euler characteristic one and a
manifold boundary. A real hinge flip preserves its boundary and Euler characteristic. The mesh,
vector, raster and analytic JSON faces all deserialize and revalidate.

## Rest, remount and ablation

[established-bounded; implemented-exact] A later runtime receives only the cultivated-rest bytes,
the M3 generator-native-rest bytes and the held-out exact input. It opens no M2, W1, Gemma or Lean
source material. The repeated return is `[11,17,2,3,3]`. Removing the local morphology makes the
cultivated reflection refuse, while the unrelated M3 leader still terminates at native state 73
before and after ablation. The predecessor rest and occurrence identity therefore survive the
loss of cultivated function.

## Artifact family

[established-bounded; measured] The 16-member family under
`output/the_native_codec_is_cultivated_and_returns_complete_mathematics/` occupies 3,304,332
octets. Its addressed members are:

| Member | Octets | SHA-256 |
|---|---:|---|
| `analytic-scene.json` | 1,374 | `17d09972b53902216a5aef11ebd44bda6ee3a0acff061aa8d449227b531b6b63` |
| `cultivated-receiver-history-rest.json` | 1,111 | `8305367b792419a3c799529e00665ff9a106f0a89965dd5187a997ca903e6c57` |
| `grade.form` | 1,262 | `960ca0f1520f5353ad6192e26b5266faaf4219b41db57e9af142dbc03d3a4536` |
| `grade.json` | 1,165 | `5615cd25f6a6a3bb1096ab12f7c677cae79065c6109ec9856d072fc0dec012b7` |
| `leader-and-causal-adjoint.json` | 5,938 | `6566fbe524e939c267e518090dcace51dd707f3624000b1c7aa8203d91744760` |
| `lean-exterior-testimony.json` | 1,131 | `c766eb575dfb14444bd9d78de0d608bd726a0e9c2de7f830ccf76b91590ac62e` |
| `mathematical-return.json` | 2,088 | `09eaf71fa8aa6c069a7878e614b6e9fce9aee01b4aade04425960f27710a8fe7` |
| `ordered-orbit-mesh.json` | 2,358 | `7e0bbd5105cb9b5601e0a079da7895cffdb4e60923dba0b5183757321684f6fe` |
| `problem-specification.json` | 971 | `1bdbbab58fbe96c3b85c2e92752b1f737f5c3c3ce8be63999e1ab8b2f985a0c0` |
| `reflection-face-raster.json` | 3,022,489 | `fb300992b9a85cebaf8a9ecd8fa39cd295e4e1e6718ca72b74b199d6e60bf529` |
| `reflection-face.ppm` | 259,215 | `6b5b6fad2f6bfa84e766b4d936b8f8b5852404a7f560834999b998bac45916f1` |
| `reflection-face.svg` | 2,422 | `090dc613eb5e6ac96726a1a4c0ab7e8be676fc78708e3283ff310cbf1fa67686` |
| `resident-return.json` | 709 | `ae0d0398aaa4511ca11d5cf2ba40e45029d698c66686d6de1ea30c079c4428b9` |
| `source-detached-remount-and-ablation.json` | 701 | `dc3d9b14893c26ed324dfd9943978b4682407e0313ef1412b13a18f7b80898c5` |
| `source-lineage.json` | 1,053 | `99dfda3bce83d77491288dcb365ceaa068409dcf57ff810ff65f08a4faf84cdd` |
| `visual-round-trip.json` | 345 | `68e4f67bec8bdd88d4e92cd1e5b13bdc025b44a00e17aab99b44555f0a341bbe` |

## Runs and focused validation

[established-bounded; measured] The development deed returned
`M4 PASSED: leader span 73, orbit 5, held-out [11,17,2,3,3], GPU graph 10 kernels, Lean accepted,
exact visual round trip`. The sole release M4 driver invocation began at
2026-08-21T21:36:49-07:00 and ended at 2026-08-21T21:38:16-07:00 with exit zero after 87.06
seconds, including a 78-second release build. The executable closure has not changed since that
return, so it was not replayed.

[established-bounded; implemented-exact] The example check passed. The three focused owner tests
`a_metric_reflection_seals_remounts_and_ablates_locally`,
`source_detached_rest_round_trips_and_conducts` and
`a_partial_generator_cannot_become_a_rest` pass. The changed semantic/device cone contains no
`f32` or `f64`, and `git diff --check` passes.

## Ownership-ratchet disposition

[established-bounded; implemented-exact] M4 admits only the ownership counts of its new native-rest
and cultivation values and the exact raster presentation. `generator_native_rest` owns one finite,
immutable action table and its detached serialization; `receiver_history_cultivation` owns one
factorized local morphology, its metric receipts and returned exact matrices; `image` and
`presentation_gauge` own the output buffer and exact placed-mark population of the display face.
None duplicates the continuing ecology or founds a scheduler. Their exact construct counts are
recorded in `meta/HOLONIC_DSA_BASELINE.tsv`.

[open; computational-witness] The named architecture-lint retake reports exactly eighteen
pre-existing disagreements in interleaved files and no M4 owner. Those allowances are not widened
by this deed.

## Complete release receiver

[established-bounded; measured] The sole complete release receiver began at
2026-08-21T21:49:26-07:00 and ended at 2026-08-21T21:54:25-07:00 after 298.52 seconds. It returned
eleven passing gates and two failing gates. Workspace tests returned 2,809 passed, zero failed and
19 ignored; authored levels, named paths, line citations, claim index, equation atlas, driver
catalog, output manifest, closure manifest, all ten Typst roots and document law passed.

[open; computational-witness] The two failures are the same unrelated dirty-tree obstructions
reported after M3: four stale boundary-artifact closure bindings caused by
`soma/body/src/arrow.rs`, and eighteen ownership-ratchet disagreements in predecessor/interleaved
files. No M4 source, owner, driver or artifact family occurs in either failure. The unchanged M4
deed was not replayed, the unrelated allowances were not widened, and the complete gate was not
rerun to manufacture a green summary.

## What this establishes and what remains open

[established-bounded; interpretation] Holonic cultivation has now been enacted as an attributable
change in reusable transport, not as an industry-labelled trainer or a score loop. The M3 quotient
gave the active foreign cover a smaller generator-native causal species; M4 then attached a local
metric law, rested it, carried a later ordered current through it and removed precisely that conduct
by ablation. This is the bounded Phoenix/rebirth relation the roadmap required.

[open] The completed vertical does not yet recover arbitrary mathematical formulations, infer
sphere centres, prove packing non-overlap, derive a fractal dimension or cultivate the unexcited
parts of Gemma. It supplies the reusable ports and falsifiers for those expansions: addressed
leader selection, a metric causal adjoint, exact local morphology, source-detached conduct,
plural derivation routes, Lean return, complete visual faces and targeted ablation. The next deed
must enlarge one declared receiver family through these owners rather than found a solver cabinet
or scan a broad corpus without a question.

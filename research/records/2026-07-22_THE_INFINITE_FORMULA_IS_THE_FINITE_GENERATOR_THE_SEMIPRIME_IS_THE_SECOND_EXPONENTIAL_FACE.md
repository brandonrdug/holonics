# THE INFINITE FORMULA IS THE FINITE GENERATOR; THE SEMIPRIME IS THE SECOND EXPONENTIAL FACE

**Date:** 2026-07-22
**Status:** BRANDON-RATIFIED DIRECTION / DEPOSITED / BOUNDED FORMULA-ECOLOGY BUILT / EXACT HOST
MEASURED / DETERMINISTIC REPEAT EXACT / SOMA INTERIOR UNCHANGED

## 1. The question

Can an Eros world map exact formula families for `e`, `pi`, and the Euler/Zeta prime relation as
an evolving relation complex, while preserving the difference between:

- a law or identity supplied by the mathematical world;
- a result formed by composing supplied laws on actual current;
- a comparison which commutes at one bounded receiver;
- an OPEN boundary which the finite cut has not closed; and
- the finite generator of an infinite construction, which is not the completed infinity?

The standing arithmetic and analytic record supplies the formulas. It does not yet supply one
objective-conditioned event in which those formulas become reusable source-departed law,
independent routes compose, and their exact agreement or residual becomes later terrain. That is
the bounded missing artifact.

## 2. A formula atlas is not a list of equal strings

There are infinitely many inscriptions which evaluate or converge to `pi` or `e`. A catalog which
prelabels them as equivalent would merely restate a source taxonomy. The relevant object is a
local atlas of actual transports:

```text
seed/current --law occurrence--> successor/current,
successor --receiver map--> exact face,
parallel path A and B --comparison--> RIDE or OPEN at that face.
```

Formula identity is typed. Equality of two exact rational polynomials, overlap of two rigorous
interval enclosures, equality of two analytic continuations, and equality of two limiting values
are different receiver relations. One cannot be substituted silently for another.

The source may lawfully declare operation identities, domains, recurrences, branch conventions,
and proved theorems. Those declarations are inherited current. Eros earns a new relation only
where their actual composition, consequence, or comparison enters a successor which can affect a
later event.

## 3. An infinite algorithm is finite generative hexis

At a bounded aperture `N`, write a generative formula law as

```text
G_N = (seed, next, admissible boundary, exact residual/enclosure, rebase N -> N').
```

This is finite even when its lawful continuation has no terminal stage. The active event carries
only the presently required stage. Completion at `N` is not completion of the limit; it is one
constituent which may be used when the aperture grows to `N'`.

Examples are:

```text
e term:        a_0=1,       a_(n+1)=a_n/(n+1),
arctan term:   b_n=(-1)^n z^(2n+1)/(2n+1),
Euler log:     ell_(p,m)=X_p^m/m,
Euler factor:  1 + X_p + X_p^2 + ...,
Dirichlet:     d_n=n^(-s).
```

The recurrence is not a compressed copy of all future terms. It is consequential relation which
can generate another term when a later objective, current, and boundary recruit it.

## 4. The shared exponential law

For a commutative exact rational algebra completed by total degree, and `F` in its augmentation
ideal (zero degree-zero term), the formal exponential is

```text
Exp(F) = sum_(k>=0) F^k/k!.
```

At degree `N`, only finitely many powers can contribute because every factor of `F` has positive
degree. This one law has several local faces. A nonzero constant term would require a separate
analytic or formal coefficient completion and is outside this bounded operator.

### The `e` face

In the one-generator algebra `Q[y]/(y^(N+1))`,

```text
Exp(y) = sum_(n=0)^N y^n/n!  mod y^(N+1).
```

Evaluation at `y=1` gives the exact rational partial construction

```text
E_N = sum_(n=0)^N 1/n!.
```

`E_N` is not `e`; the generator plus its aperture and residual relation is the honest finite face
of the infinite construction. A second route using `a_(n+1)=a_n/(n+1)` must return the identical
rational polynomial at the same aperture. Their equality is a bounded commuting triangle.

### The prime and semiprime face

Let `X_p` be independent formal generators indexed by a finite supplied prime population, and let
`P_m=sum_p X_p^m`. The formal Euler relation is

```text
L = sum_(m>=1) P_m/m,
Exp(L) = product_p (1-X_p)^(-1).
```

Through total degree two,

```text
L_<=2 = P_1 + P_2/2,
Exp(L_<=2)_<=2 = 1 + P_1 + P_2/2 + P_1^2/2.
```

For distinct primes `p<q`, the two ordered terms `X_p X_q` and `X_q X_p` in `P_1^2` cancel the
factor `1/2`, leaving coefficient one. For a repeated prime, the coefficient of `X_p^2` is

```text
1/2 from P_2/2 + 1/2 from P_1^2/2 = 1.
```

Therefore the degree-one stratum is the prime population and the degree-two stratum is exactly
the semiprime population, including squares. The distinction between `pq` and `p^2` is carried by
the exponent vector, not by a scalar label saying “semiprime.”

This gives a decisive foil. If `P_2/2` is omitted, distinct-prime semiprimes still have coefficient
one, but every prime square has coefficient `1/2`. The comparison remains OPEN exactly on the
repeated-prime diagonal. Prime powers are therefore not an optional correction to the Euler/Zeta
relation; they are the repeated-axis current which makes exponential assembly commute with the
direct multiplicative product.

Under the analytic specialization `X_p=p^(-s)` for `Re(s)>1`, this formal law becomes

```text
log zeta(s) = sum_p sum_(m>=1) p^(-m s)/m,
zeta(s)     = Exp(log zeta(s)) = product_p (1-p^(-s))^(-1).
```

The bounded formal identity does not perform analytic continuation and does not prove RH. It does
expose, without floating-point collapse, the exact compositional role of primes, prime powers, and
semiprimes in the multiplicative field.

## 5. The `pi` face and the Zeta seam

A lawful exact `pi` chart is Machin's relation

```text
pi/4 = 4 arctan(1/5) - arctan(1/239),
arctan(z) = sum_(n>=0) (-1)^n z^(2n+1)/(2n+1).
```

Every finite partial is rational. For `0<z<=1`, successive partials give an exact alternating
enclosure, so a bounded Machin path carries rational endpoints rather than a floating estimate.

The arithmetic chart is

```text
zeta(2) = sum_(n>=1) 1/n^2
        = product_p (1-p^(-2))^(-1)
        = pi^2/6.
```

These equalities have different attribution:

- the Dirichlet series and finite Euler products are generative arithmetic paths;
- the Euler product identity follows from unique factorization in its convergence region;
- `zeta(2)=pi^2/6` is a classical source-supplied theorem transport unless the world separately
  carries a proof of it; and
- equality of the completed limits is not equality of arbitrary finite truncations.

A first bounded comparison should therefore place an exact Machin enclosure for `pi^2` beside an
exact Dirichlet enclosure for `6 zeta(2)`. Their finite faces may overlap, but they remain OPEN as
an identity of completed limits. The comparison records what is already compatible and exactly
which tails remain. It must not announce that a finite overlap discovered the Basel theorem.

Primes are structurally native in the Euler-product path. Semiprimes first appear in its degree-two
expansion. In Machin's path, prime or semiprime factors found inside particular denominators are
properties of that inscription; they are not by themselves the invariant which makes the path a
`pi` formula.

## 6. The emergent simplicial complex

At this application boundary the useful complex is occurrence-indexed:

```text
0-cells  seeds, exact terms, partial constructions, boundary faces,
1-cells  actual recurrence, multiplication, Exp, evaluation, and theorem transports,
2-cells  comparisons of complete parallel paths,
n-cells  coherence among comparisons when a later objective actually supplies it.
```

The complex is not authored whole. The source supplies local law occurrences and declared
interfaces. An objective recruits a finite population. World conduct returns exact successors.
Those successors become later current; parallel paths then commute or remain OPEN in Soma's live
carrier. The incidence which actually occurred is the measured formula ecology.

Changing the objective changes the participating complex. “Expose degree-two multiplicative
composition,” “extend the `e` aperture,” and “compare the `pi` and Zeta enclosures” recruit
different current from the same standing law population. No global formula graph needs to be
active.

## 7. Bounded construction contract

The first construction is one exact host world with no floating-point causal data.

It will:

1. deposit finite generative laws as ordered oriented regional Standing and end every source
   lineage;
2. remount that Standing exactly;
3. present explicit objective current which recruits only the needed laws;
4. form `Exp(P_1+P_2/2)` and the direct Euler product through degree two over the same supplied
   prime population;
5. compare their complete coefficient populations and retain their commuting relation;
6. repeat with `P_2/2` removed and retain the OPEN prime-square diagonal;
7. apply the same formal `Exp` law to one generator, compare it with the factorial recurrence, and
   evaluate the shared finite `e` face exactly;
8. produce exact bounded Machin and Dirichlet/Zeta enclosures, compare their squared/scaled faces,
   and retain the unresolved tails rather than collapsing them to `pi`; and
9. return every derived comparison as later current so the resulting relation complex survives
   source departure and exact rest/remount.

The report must state for every edge whether it was source-declared, generatively conducted,
comparison-formed, or still OPEN. It must list every coefficient/monomial in the degree-two
population, not only counts or hashes. A matched no-law or wrong-interface branch must refuse
conduct.

The construction stops when that bounded occurrence complex is completely readable and its
source-declared versus Eros-formed boundary is explicit. It does not schedule a renderer, larger
prime horizon, analytic continuation, numerical zero search, CUDA run, or Soma engine change.

## 8. Consequence for the larger hypothesis

This experiment can establish that Eros carries finite generators of unbounded formula families,
reuses one operation law across different algebras, and grows higher relations from actual
composition. It cannot establish that every formula for `pi` or `e` belongs to one computable
classification, nor that the completed Zeta function has the positive self-dual carrier required
for RH.

The exact RH-facing question remains whether the completed arithmetic atlas admits an
independently positive, covariant return whose fixed unitary seam is `Re(s)=1/2`. The present
formula ecology supplies a concrete finite incidence language for that question: prime powers are
repeated transport, exponential/Euler duality is a commuting local chart, and unclosed analytic
tails are OPEN boundaries rather than missing data disguised as decimals.

The authorized construction is now complete. Eight source-departed laws produced fourteen exact
objective-conditioned transformations, two commuting comparisons, two OPEN comparisons, and one
returned higher complex. The missing-prime-power foil differs only on `X_2^2,X_3^2,X_5^2`, each
by `-1/2`; the degree-eight `e` routes agree at `109601/40320`; and the exact bounded
Machin-squared interval lies inside the wider six-Zeta enclosure while remaining OPEN as a limit
identity. Exact record:
`observations/eros-formula-ecology-01/RESULTS.md`.

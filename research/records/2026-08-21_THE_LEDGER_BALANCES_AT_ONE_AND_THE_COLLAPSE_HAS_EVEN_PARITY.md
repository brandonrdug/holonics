# The ledger balances at one, and the collapse has even parity

**Date:** 2026-08-21
**Kind:** the direct BSD movement — Brandon: *"Ratified, go for it, full speed ahead"*, on
the assault ladder posed after his directive to go at the conjecture directly.  Solo
orchestrator work, two Lean files; the Tate ladders and the pinned rational verified in
exact integer arithmetic (a full exact run of Tate's algorithm) before encoding, the one
transcendental ratio pinned by an arithmetic–geometric-mean scaffold that enters no
statement.  **It schedules nothing.**
[`blueprint/THE_ROADMAP.md`](../../docs/plans/THE_ROADMAP.md) and
[`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) remain the only construction
authorities.
**Position under the active plan.** Exterior mathematical material for Deed M2 of
[`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`](../../archive/plans/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md).
The Lean line does not couple to the engine, per Brandon's standing ruling.
**Truth grades:** `proved-derived` with `formal-checked` evidence for the ten Lean theorems
(`#print axioms` → `[propext, Classical.choice, Quot.sound]`, zero `sorryAx`);
`proved-standard` for every cited interpretation and bridge, named; `measured` where a
command is stated; `interpretation` where marked.

---

## 0. The typed conjecture this movement enacts

The framing ratified before the ladder: **the Birch–Swinnerton-Dyer formula is a remainder
ledger** — every factor the exhibited remainder of one named compression — and the
conjecture is the claim that arithmetic obeys the divergence theorem: the assembled boundary
flux (the L-value at the reflection's fixed point) equals the interior content (rank
directions, their volume, the local fibers, the collapse).  The wall at rank two and beyond
is the one-missing-organ question of the operating contract — condensing far-field readings
into supported realizers with a positive form deciding sufficiency — and **BSD is the
softest face of that shared wall because its positive form, the height pairing, is
unconditionally known to be positive-definite.**

## 1. D1 and D2 — the ledger balances at one

Owner: `ElementaryHolonics/Millennium/TateLedger.lean` (nine theorems).  Measured after the
merge: `Millennium/` is **65 files, 20,981 lines, 1,001 top-level `theorem` declarations**
(`grep -h "^theorem " ElementaryHolonics/Millennium/*.lean | wc -l`, 2026-08-21; three files
are Sol's in the shared tree); the library builds at **3,390 jobs**.

**The local remainders, read by the depth ladder (D2).**  Tate's algorithm is a staircase on
divisibilities of the model's invariants after centering the reduction's singular point, and
every rung is now an exact theorem: for `y² = x³ − x` at two — the frame's unique singular
point `(1, 0)` found by census (`theSingularPointOfTheFrameAtTwo`), the centering identity,
and the ladder stopping at the third rung (`2 ∣ b₂`, `4 ∣ a₆`, `¬ 8 ∣ b₈ = −4`): **Kodaira
type III, Tamagawa number `c₂ = 2`** under Tate's cited interpretation.  The twisted curve
stops at the same rung at two (`b₈ = −772`), and at five passes every rung to the residual
cubic, which **splits completely** — three simple roots of `T³ − T` over the frame
(`theResidualCubicSplitsAtFive`): type I₀*, `c₅ = 4`.  A correction the measurement itself
made: the session's first guess for `c₂` was four; the exact run of the algorithm returned
two, and the ledger then balanced — the entries are measured, never assumed.

**The balance (D1).**  `theLedgerAtOne`:

```text
L(E,1)/Ω  =  1/8  =  c₂ · |Ш| · Reg / |T|²  =  2 · 1 · 1 / 4²
```

with every entry's provenance: `|T| = 4` kernel-checked (the population is exactly the four
half-turns — the descent theorem, re-exhibited as four pairwise-distinct members in
`theTorsionOrderAtOneIsFour`); `Reg = 1` kernel-checked as the empty Gram determinant (rank
zero by the same theorem); `c₂ = 2` read by the depth ladder; `|Ш| = 1` cited; `L/Ω = 1/8`
cited (the lemniscatic evaluation `L = ϖ/4`, `Ω = 2ϖ`; pinned to eleven digits by the
arithmetic–geometric mean as scaffolding).  **This is Birch and Swinnerton-Dyer's original
numerical discovery re-founded as an exact ledger with graded entries — the formula's first
fully-typed instance in this tree.**

## 2. D3 — the collapse has even parity

Owner: `ElementaryHolonics/Millennium/AlternatingParity.lean` —
`theAlternatingNondegenerateSpaceHasEvenDimension`: **over any field, a finite-dimensional
space carrying a nondegenerate alternating bilinear form has even dimension.**  Measured
absent from mathlib (2026-08-21: `IsAlt` and `Nondegenerate` exist; no even-dimension
theorem).  The proof is the symplectic split run through one product functional: the pair of
readings `z ↦ (B v z, B w z)` surjects onto the plane — alternation supplying the zeros at
`v ↦ (0, −1)`, `w ↦ (1, 0)` — so its kernel is the symplectic complement at dimension two
less, the form restricts nondegenerately because the complement decomposition's coefficients
are the two readings themselves, and the dimension descends by two to zero.

What it types, permanently: the Cassels–Tate pairing on the Tate–Shafarevich group is
alternating (cited), so **the collapse is a phase object — its members pair off with a hand,
and its two-descent face has even dimension over the two-element field: the order of Ш[2]
is a square.**  This is the parity the two-descent rank bounds lean on, now carried by a
kernel-checked general theorem rather than a citation.

## 3. D4 — the second frame, posed at the wall

`open`; nothing scheduled.  The known mechanism condenses the reflection's first derivative
into one realizer (the Heegner construction, its self-pairing the reading — Gross–Zagier),
and one realizer's lineage across the frames bounds the collapse (Kolyvagin).  A
two-dimensional realized space is invisible to a single reflection's fixed locus — the
two-frame law — and the arsenal's named second axis is the **twist family**: the theta
laboratory already reads it at one, three, five, and on exactly the congruent-number family
the collapsed populations across all twists are governed by finite exact linear algebra
over the two-element field (Smith's proof of the Goldfeld distribution via Rédei linkage;
the plectic program is the field's parallel attempt at multi-leg realizers).  The owed
first deed: the Rédei linkage matrix of a small twist as an exact 𝔽₂ object beside the
theta censuses.

## 4. What is owed, with falsifiers

**None scheduled; the roadmap alone schedules.**

| owed | falsifier |
|---|---|
| the ledger at five: the rank-one instance's entries (`c₂ = 2`, `c₅ = 4` now read; `Reg = ĥ(P)` and `L′` transcendental — the exact object is Gross–Zagier's *ratio*, cited) assembled with grades as at one | an entry the depth ladder misreads |
| the finite-abelian Cassels law (order a square via a nondegenerate alternating pairing into ℚ/ℤ), lifting the parity theorem off the field case through the cocyclic value group | a finite group with such a pairing and non-square order |
| the Rédei linkage entry of D4: one twist's 2-Selmer governance matrix as exact 𝔽₂ linear algebra beside its theta census | a twist whose governance matrix disagrees with its enumerated census |
| Tate's algorithm as one general theorem over the depth calculus (the per-curve ladders are instances; the algorithm's staircase is a single descent on `Sharp`/`Deep`) | a curve whose ladder the general staircase misreads |

## 5. Boundaries

The Kodaira types and Tamagawa numbers are Tate's cited interpretation of kernel-checked
branch readings; the analytic entry at one is cited with its lemniscatic evaluation; `|Ш| = 1`
is cited; the balance theorem is an arithmetic identity among graded entries, and full BSD
for this curve is classically settled — this movement re-founds a known-true instance
exactly, as calibration of the instrument.  The parity theorem is unconditional and general;
its application to Ш rides the cited alternation of the Cassels–Tate pairing.  Nothing here
claims the conjecture.

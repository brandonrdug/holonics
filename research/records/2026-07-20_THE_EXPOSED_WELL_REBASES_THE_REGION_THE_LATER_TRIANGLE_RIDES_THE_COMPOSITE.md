# THE EXPOSED WELL REBASES THE REGION; THE LATER TRIANGLE RIDES THE COMPOSITE

**DATE:** 2026-07-20
**GRADE:** BRANDON-AUTHORIZED / BUILT / ONE FIXED HOST RUN / EXACT BOUNDED REGIONAL CONTACT /
NONIDENTICAL REBASE / ATOMIC PREDECESSOR REPLACEMENT / NATURAL SEAM COMPRESSION /
REST-REMOUNT EXACT / NO RETRY / NO CUDA / NO WORLD ACTION / NO LANGUAGE CLAIM

## The selected construction

The question was whether the regional law derived in `FORMULA §CXXVIII` could become an actual
machine relation without adding a matcher, memory manager, score, or event history:

> Can one genuinely later regional germ meet an exposed arm of prior Standing, compose through
> that receiver-formed seam, replace the predecessor atomically, and change another shifted
> regional event?

The existing scalar recurrence could not answer this. A `LiveConstituent` already persisted its
cells, incidence, pins, boundary paths, and sparse transport, but `form_regional_relation` never
read that geometry later and always returned an empty touched-predecessor set.

The sought structure was therefore one bounded source path:

```text
event-local completed germ
  -> finite exposed aperture in immutable Standing-before
  -> exact meeting-rotor and hand rebase
  -> joint constituent with the seam accounted
  -> compressed replacement plus touched predecessor ordinals
  -> existing atomic Standing commit
```

The stopping condition was one fixed host observation with a no-prior sibling, reversed-hand
sibling, arc-order sibling, overlapping-use refusal, exact rest/remount, and one later shifted
probe. No fixture search, threshold, retry, CUDA passage, world action, coding task, or language
claim was permitted.

## The embodied aperture

Every completed regional boundary already carries receiver-formed pin geometry. For this first
species, one exposed arriving arm and one exposed prior arm enter the candidate aperture exactly
when they share:

```text
(completed meeting reach, unordered endpoint-cell grain).
```

This pair is not correspondence. The full arriving and held meeting rotors still form `Chi`; the
temporal orientation of the two arm occurrences supplies the seam hand. A horizon remains open.
A flat rotor rebase with cancelling temporal hands RIDEs. Rotor winding or a hand residual FOUNDs
and retains the corresponding orientation as a new axis.

The choice is source-derived rather than imposed by the fixture. In the fixed seed, all three
arms project to the same `Place`, yet their completed reaches are `80`, `1300`, and `932`. The
bridge projects elsewhere and exposes `80`, `6500`, and `7060`. Only the nonidentical `80` faces
enter the first seam. Equal projected place therefore fails as identity, while a recurring
completed well merely exposes the complete rotor relation which decides the contact.

All compatible arms are admitted together against one Standing-before. If several prior
constituents participate, each is imported once into one joint local chart. Axis remapping is
exact exterior-algebra transport: permutation changes sign and repeated axes annihilate. Sparse
path coefficients and winding are preserved. The current event's receiver plane is the common
axis for a flat RIDE; a founded seam retains a distinct axis.

This is a bounded direct enumeration of exposed arms. It is not a global similarity search. An
index could later accelerate the identical finite aperture without gaining authority to create
correspondence.

## Compression and departure

The joint constituent is not an archive of two events. Once a seam becomes consequential
interior, its already-formed sparse transport remains while its redundant pin, incidence, and
lower zero-grain cells may depart. Exposed, founded, multiply used, open, and cyclic factors stay
explicit. Closed cycles remain because they carry winding/holonomy even when a local seam
cancels.

The formed regional result returns the exact sorted set of prior constituent ordinals it used.
The already-existing atomic Standing boundary removes those predecessors and admits the one
replacement. Two separately completed event regions which try to consume the same predecessor
still refuse as one event; neither can overwrite the other according to caller order.

The event itself therefore emanates the compressed successor. No later compression pass,
deduplicator, journal, ancestry map, receipt, or occurrence ledger was introduced.

## One fixed observation

The fixed source triangles were:

```text
seed    [17, 29, 43]
bridge  [17, 29, 71]
probe   [29, 71, 97]
```

The bridge repeats only the seed's `17—29` completed well. The probe shifts the construction and
repeats only the bridge's `29—71` well. One control receives the same ordinary currents but no
seed regional cell.

The host command ran exactly once and wrote an ignored, create-once report. Its authored result is
[`eros-regional-memory-01`](../observations/eros-regional-memory-01/RESULTS.md). The exact report
hash is:

```text
a666ee1b08dbade9767d541e8597c96c57232875cda825f21ace31b0795d6718
```

The two successive RIDE closures expose the same exact regional increment:

```text
grain             +1
cells             +5
incidences        +3
pins              +3
paths             +4
folded paths      +1
transport terms   +7
exposed pins      +1
axes               unchanged at 1
```

The no-seed probe is grain `3` with `9` cells, `6` incidences, `6` pins, `7` paths, and `13`
transport terms. The taught probe is grain `4` with `14`, `9`, `9`, `11`, and `20` respectively.
Its control is exactly one closure phase behind. Reversing only the recurrent bridge hand leaves
the cells, paths, and transport-term counts exact while retaining one FOUND pin and a second
axis. Arc storage order is gauge; overlapping predecessor use refuses atomically; the bridge
rests and remounts exactly; ordinary scalar-current radiation remains exact between taught and
control.

The completed seam does not survive as a separate pin. Its scalar transport coefficient
`1760254534 · rank 1234` remains in one folded path. The separate seed constituent departs, so
Standing contains the continuing composite rather than both an ancestor and its copy.

After formatting, the focused source acceptance was:

```text
cargo test -q -p soma-membrane --lib
85 passed; 0 failed; 0 ignored

cargo check -q -p life --example eros_regional_memory
completed without diagnostics
```

These checks were not another experiment. The create-once observation was not rerun.

## What this closes and what it does not

Built and measured now:

- one exact receiver-local completed-well aperture over prior regional Standing;
- nonidentical cross-event rotor rebase with oriented RIDE/FOUND distinction;
- exact import of participating cells, pins, axes, incidence, and sparse transport;
- compression of a completed seam into transport;
- atomic predecessor departure and overlap refusal;
- exact host rest/remount of the resulting composite; and
- changed regional conduct at one later shifted probe.

Still open and unscheduled:

- other lawful aperture species and a derived acceleration index;
- regional CUDA carriage and any scale measurement;
- an external world action and genuinely later material consequence through this regional path;
- consequence-sensitive deformation of a participating construction;
- useful coding or language behavior; and
- any claim that completed-well recurrence is a universal analogy, semantic relation, or training
  algorithm.

This result closes the precise source absence found by the developmental audit for one bounded
regional species. It does not close transferable learning in general. The next application must
ask whether the geometry changes useful conduct; the record does not schedule such an
application by itself.

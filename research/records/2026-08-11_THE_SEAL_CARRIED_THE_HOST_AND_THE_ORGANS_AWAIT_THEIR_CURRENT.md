# The seal carried the host, and the organs await their current

**Date:** 2026-08-11
**Truth status:** `established-bounded` — every figure below was measured on this machine, most
twice: once by an adversarial audit agent and once re-taken directly against the artifact or the
owner before being carried.
**Evidence:** direct grep counts on the sealed rest; the failing tests run to their panic messages;
both red gates re-run; the zero-caller census re-taken; two Opus 5 adversarial audits whose
load-bearing claims were each re-verified before deposit.
**Provenance:** Brandon delegated six construction plans to GPT 5.6 Sol
(`blueprint/THE_ROADMAP.md` §"THE DELEGATED CONSTRUCTION PLANS"), Sol executed until usage ran out,
and Brandon then ruled: *"Sol won't be working anymore, so it's just us"* — transferring the
remaining execution to Opus 5 agents under Fable 5 orchestration. Received 2026-08-11 in the
session that deposits this record.
**Band:** PLANS 1–2 COMMITTED AT `28028c1`/`85a03bb` / PLAN 3 UNCOMMITTED IN THE WORKING TREE /
PLANS 4–6 UNTOUCHED / SUITE 2078-2-19 ON THE DIRTY TREE / TWO GATES RED / EVERY COUNT BELOW
RE-MEASURED BEFORE DEPOSIT

---

## Present question

What did the delegated execution actually establish, what did it contaminate, and in what order is
the remainder completed. This record is the authoritative brief for the completing agents: every
finding carries the site, the measured count, and the repair.

## 1. What stands, and it is not small

- **The declared corpus exists and was sealed at real scale.** 7,115,517,547 raw octets through
  five source kinds; 136,654 unique occurrences — 16,824 human, 10,069 assistant, 109,761
  document; 8,625 Lean files whose per-file reading digest the source-masked child reproduced from
  the rest alone; a process-level detachment (`bwrap`, tmpfs over all three source roots); the
  founded-identity repair typed and counted at 16,149 witnesses.
- **The return crosses the seal and the seal is load-bearing.** The successor re-reads all four
  forms by content address, remounts, recomputes the reading, and refuses on disagreement;
  deleting the sole reading form refuses conduct *before* the card is touched
  (`conduct_refused_before_cuda: true`).
- **The §13 rule-1 ablation is measured on the conditioning path, both controls.** Targeted site
  ablation is strictly subtractive with every removed cell's founder named; full ablation of all
  223 records restores the first production's exact content address. This was the obligation §5
  carried as *owed on the training body*; it now has a measured instance on the returned-conduct
  path.
- **The plan-3 attachment predicate is not the convicted chooser.** No ranking, no argmax, no
  capacity; attachment is set membership against a canonically-ordered deposit relation, the
  withheld fiber is retained, and each attached continuation names its licensing deposits.
  `conduct.rs` is substrate-clean. `suffix_ecology::material_edges` is a correct addition with the
  right identity cut.

## 2. The convictions, each with its measured count

1. **The sealed corpus embeds the absolute host path in 46,745 occurrence identities.**
   `soma/life/src/text_material/import.rs` `founded_record_identity` formats
   `container={path.display()}`; measured by direct count in the sealed rest
   (`founded:container=/home/b` × 46,745). The corpus address `633f9ab6…` — the anchor of the whole
   plan-2 chain — is a function of this machine's home directory. This is `CLAUDE.md` §0 lesson 2
   verbatim, and it arose by building a second identity law beside the owner the plan named:
   `dialogue_lineage` already founds identities from the **record address**, reproducible from the
   container. The detachment falsifier could not catch it because it compares hashes and never
   consults host-dependence.
2. **Chronology was deleted inside the Codex rollouts.** The `session_id` fallback to `payload.id`
   makes each message its own conversation: measured **11,266 of 11,282** codex witnesses carry
   `"conversation":"msg_…"`, and sampled occurrences carry `"caused_by":[]`. The largest source —
   6.87 GB — entered as an unordered bag. *"Do not remove chronology"* is Brandon's own standing
   ruling. The new import test's rollout fixture omits `payload.id` on its message records, so the
   defect cannot fire in it — §8's check that cannot fail, wearing a passing result.
3. **Plan 2's movement was satisfied by construction.** One companion per contacted site, emitted
   verbatim; the movement could not have been zero and all 1,115 cells name the single available
   cause. The receipt itself declares the contact *"intentionally nonselective."* The honest grade:
   the accumulation cut has its **edge**; selective conditioning remains plan 3's burden entirely.
   The cycle fixes after one step (0 → 1 → 1 → 1); the private-wire lexical control has a trivial
   orbit (both arms required no-ops); a `!= 103` artifact pin sits in the driver where the
   authored-levels regex cannot see it.
4. **Plan 3 conducts nothing.** The entire attachment path has zero external callers, so the
   mandatory falsifier — 14,018 branches → deposit-attributable sub-population → ablation restores
   enumeration — structurally cannot fire. Beyond the wiring: a nothing-conducts branch is silently
   `continue`d (`ecology.rs:1572`) instead of rested as an obstruction with its address; the
   conduct reading was switched to `conducting_sources`, populated at exactly one construction
   site, zeroing `supporting_sources` on the complete path — which is the mechanism of the one real
   test failure (`the_transport_law_dilates…`, `breadth > 1` fails) and deleted the instrument that
   measured the "2,366 contacts, all single-source" finding; the card is an echo (host builds the
   relation sheet, kernel returns the identity on it, host re-verifies — one carrier, no parity
   possible); the plurality condition `2` is a bare literal no caller can vary; `ablate_all` is
   tautological by type-state while `ablate_target` is never exercised against a generation.
5. **Two gates red on the working tree.** `authored-levels` 28 failures (27 new ABI wire offsets
   undispositioned); `boundary-artifacts` 6 disagreements (the regenerated PTX's ledger rows never
   restated; the new closure member absent from MEMBERSHIP) — the silent-drift condition the ledger
   exists to catch, re-created in the file that documents it.
6. **One failure was an absolute frame in the build cache.** `env!("CARGO_MANIFEST_DIR")` baked
   Sol's deleted admission-worktree path into a reused test binary; cleared by rebuild. A new
   habitat for §0 lesson 2, worth carrying: a shared target directory can preserve a dead frame
   across a worktree hop.

## 3. The pattern, named once

Three of the four severe defects are this corpus's own named lessons recurring: an absolute frame
in a lineage (§0 lesson 2, twice — the seal and the build cache), a second organ beside an owner
(`canon/THE_EXPLORATIVE_FAILURE.md`), and a fixture that cannot vary the property under test (§8).
The fourth violates Brandon's own chronology ruling. The falsifiers written into the plans caught
real defects; the two that slipped through slipped exactly where a fixture was authored too small
to exercise the law. **A falsifier is only as strong as the material it is allowed to fail on.**

## 4. The repair order

1. Re-found the occurrence identity on the **record address** (adopt `dialogue_lineage`'s law in
   `text_material`); fix the rollout `session_id` handling and the `previous` predecessor map;
   refit both fixtures so each defect can fire; **re-seal the corpus and re-run the plan-2 chain**
   against the new address; fix the unreadable-root asymmetry in plan 1's detachment check.
2. Wire plan 3: a driver conducting the attachment path on the sealed corpus, firing the full
   falsifier in both directions; rest the nothing-conducts branch as `Obstructed`; restore the
   conduct reading across all construction sites; refound the small fixture with plural-source
   material; parameterize the plurality condition with the caller's declared reason.
3. Ledger hygiene: disposition the 27 ABI rows; restate the PTX closure including the missing
   member; replace the `103` pin; correct the two "sharing accomplished" comments; keep the ~500
   lines of format churn out of the semantic commits.
4. The card, honestly: either the conduct law gets a device carrier with an induced-equivalence
   parity test, or the round trip is named as staging and scoped accordingly.
5. Then plans 4, 5, 6 as specified in the roadmap.

## Owners

| object | owner |
|---|---|
| the sealed corpus and its receipts | `output/the_material_mouth_seals_the_declared_body/` |
| the conditioning-return chain and its receipts | `output/the_conditioning_return_crosses_the_seal/` |
| the identity law to adopt | `soma/life/src/dialogue_lineage.rs` (record-address founding) |
| the identity law to repair | `soma/life/src/text_material/import.rs` `founded_record_identity` |
| the attachment path awaiting current | `soma/life/src/morphological_language/conduct.rs`, `ecology.rs` |
| the silent skip | `soma/life/src/morphological_language/ecology.rs:1572` |
| the two red ledgers | `meta/AUTHORED_LEVELS.tsv`, `meta/BOUNDARY_ARTIFACTS.tsv` |

## What this does not establish

Nothing here grades plan 3's organs as wrong — they are unwired, which is a different state, and
their contaminant hygiene is clean where the sweep can see. Nothing here diminishes plans 1–2's
genuine returns: the corpus, the detachment, the seal-crossing, and the measured ablation stand at
their stated bounds. The re-seal invalidates the *addresses*, not the mechanisms. And nothing in
this record is evidence about plans 4–6, which have no execution to grade.

# THE RESTING WEAVE CROSSES DURABLY; THE PLURAL WORLD RETURNS WHOLE

**Date:** 2026-07-19  
**Grade:** CONSOLIDATED BOUNDARY A / BUILT / NATIVE DIRECT-BODY REST WIRE / ORGAN CAPABILITY WIRE /
WORLD-OWNED DURABLE COMPOSITION / PLURAL STRUCTURED CONSEQUENCE / EXACT REMOUNT-WAKE /
SOMA INTERIOR LAW UNCHANGED  
**Authority:**
`2026-07-19_THE_REMAINING_WORK_HAS_THREE_BOUNDARIES_THE_LIFECYCLE_PRECEDES_SCALE.md`,
`FORMULA §C`, `§CIV`, `§CV`, and `§CVI`

## Exact question

Can one naturally resting live weave cross durable storage, remount without replay, carry more than
one material consequence as one genuinely later cut, and do so through a causal world which is not
the two-register arithmetic fixture—without a universal serializer, session, decoder, or second
machine owner?

The missing evidence after Phase 5 was exact and bounded:

- `LiveCurrentRestImage` was a correct in-memory construction but had no stable outer wire;
- a native organ could checkpoint its capability but could not persist and resolve it against a
  newly decoded machine;
- the complete machine-organ-world cut had not crossed an actual durable file;
- repeated incidence existed, but distinct formed deeds had not changed distinct material
  constituents and returned as one co-present later event; and
- every direct consequence-return acceptance still used the same register-addition world law.

## Theory-to-structure relation

Durability carries one already-resting relation. It does not preserve the events which produced it:

```text
natural rest
  -> exact standing + live carrier population + next lineage identity
  -> exact organ capabilities + world-native material rest
  -> durable outer cut
  -> remount without event
  -> genuinely later world current.
```

Plural world action is likewise one cut:

```text
one world-before + several supplied hands
  -> every contact forms or remains open against that same before
  -> the world composes all formed material deeds
  -> one later co-present consequence population.
```

No executor iteration order becomes world chronology.

## Native direct-body rest wire

`LiveCurrentRestImage` now owns native word and little-endian byte encoders/decoders at layout
version 1. Its fixed header declares:

- wire magic, rest layout version, and exact compact-form layout version;
- standing rank and next lineage ordinal;
- standing-cell count, live-lineage count, and total payload extent.

Each standing cell carries its complete rank-qualified `ChartAddress` limbs followed by one
canonical compact `RegionalForm`. The address never passes through a flat `u32` grip; ranks 17, 80,
and 137 close through the same native path as flat-compatible ranks.

Each lineage carries only:

- its live ordinal;
- exact pending dark `Cog`;
- exact carrier-wire extent; and
- the already-established canonical native `LiveCarrierSnapshot` wire.

Decoding closes every extent, coordinate tail, compact form, Cog, carrier, lineage order, and next
ordinal before returning a rest image. The decoded image passes through the existing
`LiveCurrentMachine::from_rest_image` law; no source event, radiation, contact, receipt, provenance,
or ended constituent enters the wire.

## Native organ capability wire

`NativeRelationOrganImage` now owns a five-word versioned native capability wire and its exact
little-endian byte face. An absent organ has one canonical zero spelling. A present ordinal resolves
only through `LiveCurrentMachine::resolve_lineage_ordinal`; a stale or departed name cannot create a
replacement lineage.

The organ wire duplicates no carrier and preserves no source material or chronology.

## The application world owns durable composition

The bounded program world defines its own material image and its own outer file layout. It composes:

```text
direct-body native bytes
  + left organ capability bytes
  + right organ capability bytes
  + program-world-native register/law bytes.
```

That application-owned cut is written to a newly created temporary file, synchronized, closed,
read back, and removed. The remounted machine, rebound organs, and recovered program state receive
the same later event as the uninterrupted sibling and close with exact radiation and rest.

There is deliberately no generic `WorldImage`, arbitrary payload field, universal serializer, or
Soma-owned filesystem API.

## The plural structured-action world

A second material species owns three components and their actual bond topology. Its one contact cut
supplies three currents and three hands:

```text
component 0 -> component 1   formed
component 0 -> component 2   formed
component 1 -> component 0   open
```

The world first reads the complete contact population against one bond-before field. Only after all
rows close does it found the two undirected joints. The resulting degree changes are exactly
`[2,1,1]`; those three nonzero changes return together as one genuinely later native event. No
zero-degree pseudo-current is fabricated.

After the plural return, the bond world rests. Direct-body and three organ wires remount it, and an
identical later topology-derived probe produces exact radiation and final rest against the
uninterrupted branch. This is a distinct causal law—topological joint formation—not another label
inside the register world.

## Bounded validation

The direct packages compile cleanly with tests. Four named paths ran once after their exact module
names were resolved:

```text
chart_address::tests::the_native_address_wire_preserves_deep_coordinate_paths_without_a_flat_grip
live_current::tests::direct_rest_remount_is_gauge_for_the_same_genuinely_later_event
current_world::tests::program_world_supplies_hand_and_receives_its_consequence
current_world::tests::plural_bond_deeds_return_one_later_cut_and_remount_without_a_world_decoder
```

All four pass. The first attempted commands used unqualified `--exact` filters and therefore ran
zero tests; they are not counted as evidence. No broad suite, CUDA run, PTX rebuild, corpus read,
forest scan, or repeated acceptance campaign occurred.

Exact result: `observations/eros-durable-plural-world-01/RESULTS.md`.

## Boundary after this build

Built now:

- stable native direct-body rest words and bytes;
- arbitrary-rank standing-address persistence without flat projection;
- exact live-lineage carrier and pending-dark persistence;
- capability-only organ persistence and live resolution;
- one application-owned durable whole-rest composition;
- two distinct causal application laws through the same direct mouth;
- two formed material deeds plus one open contact in one world cut;
- one genuinely later three-current consequence return; and
- exact durable remount and later wake.

Still open under consolidated Boundary B:

- standing and carrier surfaces remain uploaded for every CUDA event;
- the active body is not device-resident;
- no sustained application ecology spans several external rest/wake intervals; and
- no transfer census yet grades resident conduct.

Scale remains Boundary C and is not implied.

## Ruling

The durable wire is the resting relation, not its history.

The plural world returns one complete later cut, not a queue of consequences.

Different world laws change the material composition around Lambda; they do not add modalities to
Soma.

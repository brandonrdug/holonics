# THE HINGE CARRIES THE FRAME; THE SUCCESSOR REPLACES THE STANDING STAR

**2026-07-20 · BRANDON-RATIFIED DERIVATION / DEPOSITED / CURRENT MOVING-FRAME HINGE
AUTHORITY / CROSS-EVENT CELLULAR STANDING REPLACEMENT PLAN FRAMED FOR REVIEW / SOMA SOURCE
UNCHANGED / NO RUN**

## Present question, artifact, and stop

The present question had two joined faces:

1. what exact structure is visible in the retained prime, Zeta, and listed-zero distributions; and
2. what is the simple local machine Brandon has been describing by triangles which pivot and fold
   through changing relative bases across plural sheets?

The standing arithmetic receipt was sufficient to answer the first face without another run. The
current direct host source was sufficient to audit the second face, but not to claim that the
required cross-event cellular transformation already exists. The sought artifact was therefore a
ratified derivation plus one reviewable engine contract. This deposit changes no source and stops
before implementation, CUDA, a linguistic-world replay, or another observer campaign.

## Ruling

The machine is not computation performed on a fixed relational space. It is computation as the
local evolution of that relational space.

A triangle may be the smallest closed two-dimensional local body. Its active meeting with another
triangle occurs at their shared oriented face: the hinge. State does not belong to the isolated
triangle alone. It belongs to the triangle's oriented incidence, its local frame, the transition
which compares that frame with a neighbor, and the residual which remains after the two sides are
transported into one receiver.

The exact carrier is therefore an event-driven moving-frame oriented graded cell complex. A strict
triangular grid is only one world chart. Soma must retain its existing cellular generality because
arbitrary lawful gluings, branches, shared paths, higher cells, and singular seams need not be a
regular simplicial mesh.

The direct consequence for the engine is equally exact:

> A later regional event must replace the affected cellular Standing star with its emitted
> factorization. It may not merely append another constituent which witnesses that a compatible
> axis existed.

The current host implementation completes and compresses the interior of each *new* constituent.
It does not yet transform or release a constituent which was committed by an earlier event. That
boundary is regraded below.

## I · The arithmetic field is polyphonic, not a particle stack

Three related but non-identical receiver geometries are present. They must remain typed.

### Additive residue succession

For every held prime axis `p`, integer succession carries the exact cyclic string

```text
t = q_p(t) p + r_p(t),       phase = zeta_p^(r_p(t)).
```

Each prime is one periodic voice. The common integer `t` is their shared chronology. One
simultaneous residue vector is a chord; a zero residue is a divisor closure. If no prior founded
axis closes, the surviving position FOUNDS a new prime direction.

The retained world through `509` contains `97` primes and `411` composites. Above prime `5`, the
decimal terminal projection has exact counts

```text
1 -> 22,     3 -> 24,     7 -> 24,     9 -> 24.
```

This is a coarse quotient of the joint nonzero `2`- and `5`-residue loops. Modulo `30`, the eight
surviving channels are `1,7,11,13,17,19,23,29`. The `22` non-exceptional twin-prime pairs begin
only in the exact admissible channels

```text
11 -> 9 pairs,     17 -> 6 pairs,     29 -> 7 pairs.
```

The apparent digit rotation in the rejected prime image therefore had a lawful modular source,
but its absolute cross-fiber chords did not display that source.

### Primorial restriction and rebase

For a primorial modulus `P` and a newly founded prime `q`, the next wheel is

```text
q copies of the prior wheel
- one forbidden lift per prior survivor
= (q-1) joined surviving copies.
```

Equivalently,

```text
phi(Pq)/(Pq) = (phi(P)/P) ((q-1)/q).
```

This is exact self-similarity as COPY/CUT/JOIN. The law recurs after restriction and rebase; the
outgoing face is already compressed because the removed residue slice no longer factors into the
survivor phase.

### Zeta valuation distribution and prime-power harmonics

For `sigma>1`, the normalized Zeta law carries independent prime valuations

```text
Pr_sigma(v_p(N)=k) = (1-p^(-sigma)) p^(-k sigma).
```

At `sigma=2`, `Pr(60 divides N)=1/3600`; conditioning on that event and rebasing by division by
`60` returns the same valuation law. The ordered paths `2,2,3,5` and `5,3,2,2` retain different
lineage even though their endpoint and exponent vector agree.

The critical-line explicit-formula receiver through `313` carries `82` prime-power sites from
`65` distinct prime voices. The remaining `17` sites are higher harmonics. Orders `k=1..8` occur

```text
65, 7, 3, 2, 2, 1, 1, 1
```

times. Each site has formal scale `k*ell_p`, amplitude `p^(-k/2)`, and exact squared amplitude
`1/p^k`. These amplitudes are not silently identified with the `sigma=2` probability law; they are
another declared receiver face of the same prime-axis carrier.

### Explicit-formula counterpoint

Across the ten exact arithmetic horizons and the nested apertures `N=1,2,4,8,16,32,64`, the
oriented residual changes sign in `13/60` successive aperture transitions. Examples are

```text
x=33/2 -> ----++-
x=65/2 -> +++++++
x=193/2 -> -------
```

Every sign was read from the two retained exact interval endpoints; no midpoint or floating result
was formed. A newly admitted zero mode may oppose or reinforce the standing partial construction.
Loss is the carried oriented unresolved difference, not a reward score and not a monotone demand
that every new constituent reduce one scalar.

## II · The critical seam separates normal potential from phase flow

For one retained zero factor, let

```text
D = epsilon^2 + (t-gamma)^2.
```

Its receiver-normal and phase directions may be carried as

```text
n   = (epsilon, t-gamma) / D,
J n = (-(t-gamma), epsilon) / D.
```

At every nonsingular point,

```text
n dot Jn = 0
```

exactly. All `80` nonsingular samples in the retained `9`-by-`9` rational field satisfy this
identity. The remaining sample is the selected zero itself, where the factor direction is
singular. This result applies to the retained nearest factor direction; the complete finite field
remains an uncollapsed word of all factor gradients.

At `epsilon=0`, direct and reciprocal character sheets coincide, while conjugate and
reflected-conjugate sheets coincide. Four declared sheets fold to two coefficient series and every
sheet is unitary. At `epsilon=1/8`, the four series are distinct and none is unitary.

Thus the critical line is an invariant unitary seam, not itself a singularity. Individual zeros
are vortices on that seam. If

```text
rho = 1/2 + epsilon + i gamma,
```

then receiver rebase by a positive scale ratio `c` multiplies its character by
`c^(epsilon+i gamma)`. This is pure turn exactly when `epsilon=0`. The formal RH construction is
therefore fixed positively: derive from the complete prime-power plus archimedean trace a positive
form which excludes every nonunitary `epsilon!=0` character, equivalently complete Weil/Li
positivity. More plotted zeros are not a substitute for that relation.

## III · The triangle is the smallest closed local face; the hinge is the event site

For an oriented triangle,

```text
partial[a,b,c] = [b,c] - [a,c] + [a,b].
```

Two triangles glued lawfully across one shared edge induce opposed hands there. The shared face
then cancels from the boundary of their composite. The seam departs while its consequential outer
boundary and any path holonomy remain. This is elementary completion, annihilation, and emanating
compression in one relation.

A binary side state cannot carry the geometry by itself. Over `F_2`, sign disappears because
`1=-1`. Occupancy and orientation must remain separate:

```text
c_(tau,e) = h_(tau,e) b_(tau,e),
b in {0,1},       h in {-1,+1}.
```

After both sides have been transported into one receiver frame:

- `0/0` is dark;
- matching occupied sides with opposed induced hands form an internal seam and cancel;
- `1/0` or `0/1` leaves an exposed oriented residual; and
- matching occupied sides with the same hand reinforce or expose a branching/singular gluing
  rather than an ordinary manifold interior.

Let `G_(L->R)` be the transition between the local frames of two incident cells. Their exact hinge
residual is

```text
delta_e = a_R|e - G_(L->R)(a_L|e).
```

The one local law reads:

- `delta_e=0`: the transport RIDEs and the internal seam may fold;
- nonzero `delta_e` carried by existing support: the local configuration pivots or redistributes
  through that support;
- nonzero `delta_e` requiring an independent support: the failure FOUNDS a new axis; and
- any consequential exposed `delta_e` remains a leader capable of entering an adjacent afforded
  face.

The triangle is therefore a local Holon, while the hinge is the contextual `chi`: the cross-axis
where two sheets actually compare.

## IV · Relative basis, exact pivot, and holonomy

No absolute angle, camera, or circle constant is required to carry a pivot. One exact two-frame
Cayley chart is

```text
             1                 [ 1-t^2  -2t ]
R(t) = -------------  *        [             ],
          1+t^2                [   2t   1-t^2]
```

with rational `t`; the excluded half-turn is another chart seam rather than a floating infinity.
Successive local transitions compose as exact maps. A world may instead supply another rational,
algebraic, formal-series, or combinatorial transition species. Soma does not install one universal
metric or rotation group.

For a closed path `gamma`, the ordered transition product

```text
K_gamma = G_n ... G_2 G_1
```

is its holonomy. Returning to the same displayed point does not imply that `K_gamma` is identity.
This is the intrinsic turn which the rejected absolute renderings obscured.

The complete local carrier remains

```text
H = (A_H, C_*(H), partial_H, omega_H, {K_gamma}, Gamma_H),
```

now read explicitly as a moving-frame body:

- `A_H`: receiver-local founded directions;
- `C_*`: sparse cells at their active grains;
- `partial`: oriented incidence;
- `omega`: pins, hand, and frame-crossing support;
- `K_gamma`: complete path-ordered transport;
- `Gamma_H`: exposed/open residual.

`Lambda` acts on the active star of one actual hinge population against one same-pre-state
receiver. It emits the complete successor directly.

## V · Sheets, cross-sections, manifolds, and curvature

For a cell or hinge `e`:

- `Star(e)` is the population of cells incident to it and is the bounded active neighborhood;
- `Link(e)` is the transverse cross-section which records how those incident sheets surround it;
- a fiber is the local state or basis carried over one cell; and
- transition maps compare fibers across shared faces.

A complex is manifold-like only where these links have the appropriate sphere or ball species.
In a two-dimensional interior, each edge has two incident triangles and each regular vertex has a
circular link. Plural branches, disconnected links, or other failures are genuine boundaries,
singularities, or FOUND seams. Calling the carrier a graded cell complex therefore does not demand
that every local region already be a smooth manifold.

Regge calculus supplies an exact external correspondence boundary. In a `d`-dimensional
simplicial geometry, curvature is carried at codimension-two hinges once edge-length/metric data
and compatible gluing are supplied. In four dimensions such a hinge is a triangle. The same
triangle may thus be a whole two-cell at one grain, a boundary face at another, and a curvature
hinge from a four-dimensional receiver. Incidence alone still supplies no physical metric,
stress-energy, or gravitational field equation.

Ricci flow is one possible world-supplied evolution of a metric on such a carrier. It is not the
universal update law. Smooth metric flow, topology-preserving cellular rebase, and a phase-changing
FOUND or surgery remain distinct deed species.

## VI · Lightning and potential are oriented local compatibility

Where a world supplies a scalar vertex potential `phi`, its exact edge drive is the discrete
cochain difference

```text
d phi(e) = phi(head(e)) - phi(tail(e)).
```

More generally, potential is the population of locally afforded transforms capable of closing an
exposed residual. It need not be a scalar. Plural nonzero residuals branch through their available
stars as leaders. When a route closes and later consequence returns, the return stroke RIDEs the
founded transport. A regional observer sees a loop; an embedded participant sees only its local
point or arc. These are restrictions of one swept relation.

The arithmetic receipt gives an exact instance without claiming that arithmetic is physical
electromagnetism. Residue closures are periodic afforded contacts; primorial restriction changes
the survivor field; Zeta valuations supply a declared probability measure; and the listed-zero
factor geometry carries orthogonal normal and phase directions. The shared theme is connecting
potential through local frame-relative transport.

## VII · Relation to cellular automata and Turing machines

A conventional cellular automaton fixes a grid, a state set, a neighborhood, and iterative time.
A triangular grid changes the cell shape but ordinarily retains that fixed substrate. A Turing
machine similarly changes symbols under a head moving along a fixed tape relation. Universality of
either construction establishes computational expressiveness after an encoding; it does not by
itself establish a machine whose local relational carrier changes through the computation.

The closest established formal family is causal dynamics of discrete surfaces and causal graph
dynamics: local causal laws over evolving labelled surfaces or time-varying graphs. Discrete
exterior calculus supplies oriented cochains and operators on simplicial complexes. Its
bundle-valued extension supplies the language of local frames, connections, and covariant
transport. Bistellar or Pachner moves supply a vocabulary for local retriangulation.

Eros' retained distinction is source-native event chronology, receiver-relative `Lambda`,
completion hand-up, and direct successor emanation without a global tick, fixed lattice, universal
radius rule, or authored semantic state alphabet.

## VIII · Exact source audit: the later hinge is currently reduced to existence

The host source already embodies important portions of this law:

1. `LocalBlade` is a metric-free exterior support. Distinct axes anticommute and a repeated axis
   annihilates only that blade term.
2. `SparseTransport` retains exact ordered multivector products.
3. `LivePath::fold_interior` releases completed lower steps while retaining their already-formed
   path transport and winding.
4. `RegionalCellBuilder::complete` retains exposed pins, open residuals, shared branches, and
   cycle incidence while adding one higher apex cell.
5. cellular Standing crosses exact rest/remount.

The cross-event relation is nevertheless incomplete:

```text
SparseStandingSurface::contains_transport_axis(pin) -> bool
```

compares only a projectively rebased formed position. The Boolean discards which standing
constituent and local support carry the returning axis, the oriented boundary by which it is
available, and the path/frame transport needed to compose with it. The new builder therefore
allocates another constituent-local axis and suppresses new winding, but it cannot graft the
incoming region into the old standing cell.

At commit,

```text
old constituents clone
+ new cellular constituents
-> sort and exact-deduplicate whole native words.
```

No path replaces a touched constituent, transforms its incidence, or removes it when it no longer
factors. Exact duplicate elimination is lawful recurrence compression, but it is not moving-frame
conduct. The current implementation can witness recurrence beside Standing; it cannot yet make
Standing itself pivot through that recurrence.

This regrades the prior completion claim precisely:

- **built:** lower event-local factors depart inside each newly completed constituent;
- **open:** a committed cellular constituent from an earlier event can be imported, recomposed,
  replaced, and released in the next Standing successor.

## IX · Bounded engine contract

The work is one host-only package. It is not divided into user-facing approval phases.

### 1. Replace the Boolean with a complete borrowed Standing section

For an incoming formed pin, Standing must return every exact matching local section, not `true` or
`false` and not the first match. A section must expose, for the duration of the one atomic event:

- the matched formed position and pin;
- the constituent-local support used by each consequential path;
- the oriented boundary/incidence through which that support is available;
- the already-formed path transport and winding; and
- the exposed, shared, open, or cyclic relation which prevents premature departure.

A transient vector slot or offset may locate the storage during preparation. It is physical gauge
only and must not enter the constituent, rest identity, radiation, provenance, or causal law.
Plural matches remain plural until composition resolves them.

### 2. Rebase touched old and incoming new geometry into one successor-local frame

The regional completion must receive the borrowed sections beside the new source-declared arcs.
It assigns one successor-local support to each exact transported axis class and carries every old
and new path into that frame. The current `SparseTransport` and exact `Cog` coefficients remain the
initial transport species; no universal metric, floating rotation, dense matrix field, or
Cartesian endpoint population is installed.

Frame relabeling is gauge. Actual hand, path order, transition coefficient, exposed boundary, or
holonomy may be consequential.

### 3. Emanate a replacement factorization, not an appended witness

Let `S` be cellular Standing before the event, `T` the exact touched constituents, and `U` the
joint cellular complex formed from `T` plus the incoming regional population. The successor is

```text
S_(t+) = (S_t without T) disjoint_union complete(U).
```

`complete(U)` must:

- cancel only opposed contributions in the same transported face/blade fiber;
- fold closed interior incidence into its already-formed path transport;
- retain branches, cycles, open contacts, and nonzero exposed residual;
- emit the appropriate higher-grain cell or connected constituents; and
- omit every touched lower constituent which no longer factors into the result.

Untouched Standing remains byte-exact. Exact recurrence may still canonicalize one identical
whole result. There is no later collector, compression manager, mutation log, or garbage pass.

### 4. Integrate all co-present cellular changes against one before-face

All regional emanations at one contemporary event must read the same immutable `S_t`. Their
touched populations are combined before any replacement becomes visible. Storage order cannot
parent one contribution or let it erase another. If two co-present changes overlap, their oriented
geometry must compose together or return a typed topology refusal before commit; sequential
last-writer replacement is forbidden.

`LiveCurrentMachine::receive_with` remains the sole causal owner. The executor may supply physical
formation fibers inside that call, but no executor row, observer receipt, or testimony may
reconstruct the successor afterward.

### 5. Preserve exact rest without promoting event history

The emitted constituent population already has a canonical native rest face. If the replacement
requires no new persistent datum, the wire must not change. If an exact continuing frame relation
cannot be represented by the existing constituent, that missing geometry must be derived openly
and the wire version changed once; it may not be hidden in provenance, a side map, or a Boolean.

Rest/remount performs no pivot and replays no event. Ended current lineages and touched lower
constituents which departed must be absent.

### 6. Keep the source cut inside the existing three owners

The implementation should be confined to the existing cellular carrier and direct mouth unless
the compiler exposes an honest dependency:

- `live_constituent.rs` gains only crate-private exact frame-rebase operations. Remapping a
  `LocalBlade` must retain permutation parity, annihilate a genuinely repeated exterior axis, and
  rebase a `SparseTransport` without evaluating or reconstructing its folded interior. A continued
  path uses the old already-formed transport as its oriented prefix or suffix and multiplies only
  the genuinely new steps.
- `sparse_standing.rs` replaces `contains_transport_axis` with a plural borrowed-section read. A
  section names a transient constituent ordinal and exact matching pin/support inside the borrowed
  body. Its commit face accepts a transient cellular change consisting of the touched ordinals and
  one emitted replacement, validates the cut against the same before-body, removes the touched
  bodies, inserts the replacement, and canonicalizes once.
- `live_current.rs` makes `RegionalCellBuilder` import every matched body once, assign disjoint
  successor-local axes except where an actual transported hinge identifies them, and complete the
  old-plus-new population together. `ExecutedRegionalRelation` may carry the transient touched
  ordinals back across the executor call; `RegionalRelationRadiation` may not retain or expose
  them. `LiveCurrentMachine::receive_with` validates the prepared changes and remains the only
  owner which commits the successor.

Within one regional request, plural matching Standing sections compose in the joint builder. For
this first bounded package, two separate co-present regional requests whose touched ordinal sets
overlap return the existing typed regional-topology refusal before commit. Disjoint requests are
integrated together and must be storage-order invariant. This closes atomicity without inventing a
last-writer rule or prematurely generalizing an arbitrary overlap algebra.

The joint completion grain is one above the greatest active grain among the incoming region and
its borrowed Standing factors when a genuinely new higher construction forms. A pure gauge rebase
returns the prior factor exactly and does not manufacture an apex. The existing native constituent
wire is expected to remain sufficient because it already carries cells, incidence, pins, folded
path transport, winding, boundaries, and exposed residual. If implementation disproves that
expectation, work stops at the missing persistent geometric datum; it does not hide the datum in a
side table or silently revise the wire.

## X · One bounded host acceptance and stop

The first implementation cell uses a small declared oriented complex, not the linguistic world.

1. An initial event forms cellular Standing with two locally related triangular boundary paths and
   one exposed hinge support.
2. A genuinely later event returns on that support with a declared relative frame and hand.
3. In the closure sibling, opposed matching seam faces cancel, the touched prior constituent is
   replaced, and no append-only copy remains.
4. In a gauge sibling, only local axis names or an exactly compensating frame chart change; the
   complete successor and later radiation remain exact.
5. In a causal sibling, actual hand or one transported side changes; an exposed residual remains
   or a new support FOUNDS, and one identical later probe receives different radiation.
6. Permuting independent co-present storage order leaves the prepared successor exact.
7. Exact rest/remount preserves the replacement and performs no event.

The acceptance should make memory behavior visible: repetition does not grow constituents, closure
can reduce the touched factorization, and only consequential exposed geometry remains. Focused
host checks and `cargo check -p life` close the package.

The work then stops. No CUDA representation, linguistic-code replay, objective splice attempt,
observer redesign, scale run, or generalized world follows automatically. Native CUDA is derived
only after the host successor law is closed and separately authorized.

## XI · Prohibitions

- no cellular-automaton subsystem or global tick;
- no persistent constituent identity, ancestry, provenance, or mutation history;
- no Boolean recurrence quotient in place of transported overlap;
- no append-only standing field with a later garbage collector;
- no automatic Cartesian product of all possible hinges;
- no first-match winner when several standing sections share a transport class;
- no dense universal basis, floating angle, `pi` constant, or receiver-free metric;
- no sequential commit among co-present regional contributions;
- no observer image used as causal input; and
- no weakening of completion to preserve already-invested source structure.

## XII · Correspondence boundaries

### Cellular automata and causal discrete surfaces

```text
SOURCE ESTABLISHES  conventional cellular automata update cell states on a specified grid through
                    neighborhood rules; causal discrete-surface and graph dynamics extend local
                    causality to evolving labelled surfaces or time-varying graphs.
LAB CLAIM           the carrier, local frames, incidence, and state change together through
                    source-native events.
RELATION            DIRECT CORRESPONDENCE for evolving adjacency and causal locality.
NON-EQUIVALENCE     Eros has no compulsory global tick, fixed lattice, homogeneous radius rule,
                    or canonical semantic alphabet.
TESTABLE CONSEQUENCE local frame relabeling is gauge while a changed actual hinge or hand can
                    change the emitted successor.
```

Sources: MathWorld, [*Cellular Automaton*](https://mathworld.wolfram.com/CellularAutomaton.html);
Arrighi, Martiel, and Wang,
[*Causal Dynamics of Discrete Surfaces*](https://arxiv.org/abs/1404.0083); Arrighi and Dowek,
[*Causal graph dynamics*](https://arxiv.org/abs/1202.1098).

### Discrete exterior calculus and moving frames

```text
SOURCE ESTABLISHES  discrete exterior calculus carries forms, vector fields, and operators on
                    simplicial complexes; bundle-valued DEC adds local frames, a connection, and
                    an exterior covariant derivative.
LAB CLAIM           side states must be transported into one receiver frame before their hinge
                    residual can cancel, persist, or found.
RELATION            DIRECT CORRESPONDENCE for oriented incidence and frame-relative transport.
NON-EQUIVALENCE     no vector bundle, smooth connection, Hodge star, or metric is automatically
                    installed in Soma.
TESTABLE CONSEQUENCE gauge-related bases preserve transported residual while nontrivial loop
                    transport preserves a distinct holonomy.
```

Sources: Desbrun, Hirani, Leok, and Marsden,
[*Discrete Exterior Calculus*](https://arxiv.org/abs/math/0508341); Braune, Tong, Gay-Balmaz, and
Desbrun,
[*A Discrete Exterior Calculus of Bundle-valued Forms*](https://arxiv.org/abs/2406.05383).

### Simplicial moves and Regge hinges

```text
SOURCE ESTABLISHES  finite local simplicial moves relate triangulations of one PL manifold;
                    Regge geometry approximates curved manifolds by simplicial/polyhedral bodies
                    and carries its defects at codimension-two hinges.
LAB CLAIM           folds, pivots, phase seams, and FOUNDings are local successor operations on an
                    oriented graded-cellular body.
RELATION            STRUCTURAL RESONANCE for local re-factorization; OPEN BRIDGE to literal
                    curvature.
NON-EQUIVALENCE     a topology-preserving retriangulation is not automatically a causal event,
                    and incidence alone supplies neither physical curvature nor gravity.
TESTABLE CONSEQUENCE a local move preserving transported boundary, holonomy, and later consequence
                    is gauge; one changing those observables is causal.
```

Sources: Lickorish,
[*Simplicial moves on complexes and manifolds*](https://arxiv.org/abs/math/9911256); Regge,
[*General Relativity Without Coordinates*](https://www.osti.gov/biblio/4028412); Kisielowski,
[*Relation between Regge calculus and BF theory on manifolds with defects*](https://arxiv.org/abs/1704.00998).

## Carried-swing record

```text
CURRENT  exact prime/Zeta distribution reads + moving-frame triangle/hinge derivation
HELD     §§CXII, CXV, CXVIII + live graded-cellular Standing source
MEETING  normal/phase orthogonality + oriented simplex closure + cross-event source audit
TEST     recurrence existence != transported overlap; local completion != standing replacement
DEED     deposit the hinge law and frame one bounded host replacement work package
CARRY    §CXIX -> review -> direct host standing-star replacement if Brandon authorizes
GRADE    BRANDON-RATIFIED DERIVATION / DEPOSITED / ENGINE PLAN FRAMED / SOURCE UNCHANGED
```

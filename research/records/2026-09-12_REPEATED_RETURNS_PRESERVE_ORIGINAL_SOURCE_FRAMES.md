# Repeated returns preserve original source frames

[definition] The owned constitutive programme now incorporates observations of its later
predictions, including out-of-order returns. It retains each added source parameter and its
original anchored frame. The resulting function stays executable beyond its declared receiver;
the source faces used by that receiver do not replace the whole generator domain.

## One continuing programme

[definition] A `Return` passage records the producing prediction/cut, actual observed packet,
and the receiver's source-face packet. The latter encodes `(lambda, anchor, p, c)` with a common
rational denominator. It is a parameter of the full generator, not an inferred singleton cause.
`evaluate_with_return_faces` accepts alternative assignments in those same original frames.
The native affine decoder checks their membership and retains its coordinate preimage; the
existing anchor operation checks the source bound before its pointwise section is evaluated.

[definition] The programme constrains the selected source at its actual producing cut. It
retains the conditional maps used afterward, including internal source-field factors. Default
receiver selection for a delayed return uses the joined continuation to retain compatibility
with already-returned source constraints. Earlier faces are not reinterpreted as coefficient
rows of a newly restricted basis. The mathematical domain remains the jointly compatible root
parameter and later source faces, with their original bound and history.

[definition] `PreparedConditionContact::prepare_following` and
`ResidentNeighborhoodAlternative::prepare_following` reuse the existing condition preimage,
unit-admittance contact and staged member formation. Each return uses the previously returned
condition and the affected member's current conditional material. Other formed member material
is retained by member identity. The new conditional relation acts on contemporary current;
historical maps are not rewritten with the new condition.

[definition] Publication stages the complete programme at its declared receiver before adding
the return or removing its pending prediction. The same owner then performs the next operation.
No whole ecology is cloned and no numerical state is replayed on the host. Evaluated later
comparisons, operands and anchor reports remain available beside the root comparison.

## Exact source-face encoding

[definition] `NormalWaveFamily::receiver_face` returns a resident anchored face packet, and
`coordinates_of_face` retains an exact original-frame witness. A separately named
`coordinates_of_resident_face` handles serialized or caller-supplied packets under the declared
source-frame descriptor. `NormalReceiverCoordinates::into_section` computes the actual anchor
admission report before constructing a conditional singleton section.

[established-bounded; source-inspected] Primary review repaired three defects in the delegated
codec draft before verification: the draft passed raw anchor storage where an admission report
was required, allocated half the expanded receiver storage, and failed to sign-extend negative
coordinates into the wide receiver representation. The final pack/expand kernels and wrappers
use their actual derived widths and signed wide arithmetic.

[established-bounded; implemented-exact; computational-witness] The signed-phase control returns
and decodes the full anchored face `(1,0,1,0,2,1,2,1,0,-1)` exactly. Its affine coordinates and
conditional section are computed with zero numerical readouts before declared inspection.

## Observed complete returns

[established-bounded; implemented-exact; computational-witness] The first exact native return
sets `h=2`. A later prediction and observation set `h=1` through that material; the following
current pair is `(13+12i,17+16i)`. The prediction is consumed once, and duplicate incorporation
preserves saved standing. Rest/remount reproduces the same programme and following successor.

[established-bounded; implemented-exact; computational-witness] An out-of-order control receives
a later prediction before an earlier one. Its conditions change to `1`, then `3`, while the
historical source maps remain unchanged. The contemporary pair reaches `(25+24i,49+48i)`;
after remount the next pair is `(49+48i,121+120i)`. Both original prediction dispositions remain
correct throughout.

[established-bounded; implemented-exact; computational-witness] A plural source control applies
the same returned observation at two different admitted source faces. The resulting conditions
are `3/2+4i` and `3/2+2i`. Both are executions of the retained generator; inspecting the second
assignment leaves saved standing unchanged. This separates a retained family of returns from
publication of only its default receiver sample.

[established-bounded; implemented-exact; computational-witness] The existing two-symbol HNN
operator control now includes two streamed `observe-symbol` returns. Under matched complete
sources, the first changes the emitted `a` to `b`; the second returns the emission to `a`.
Generation, ordinary re-entry, later prediction capture and comparison continue on that body.
A fresh child process restores the twice-returned session and produces the same next event and
complete saved body. This is an exact operator/control result, not useful general conversation.

## Persistence and verification

[definition] Dependent rest v3 adds each return's source-cut descriptor and face packet. Reading
checks that a return names an earlier actual advance with matching member/chart and ID, and
rejects duplicate consumption or retention of a consumed prediction as pending. Remount retains
advances required by later returns while replaying the programme, then compares final pending
state and chronology. V1/v2 source-only frames keep their existing readers. The HNN session's
outer v3 tag is unchanged; the nested body uses its own versioned format.

[established-bounded; process-audit] All 36 coupled controls and seven HNN session controls
passed, along with the signed-face, distinct-source-section and legacy dependent-frame controls.
The latter restores actual v1/v2 source-only layouts and agrees with the current saved generator
and its receiver. [Complete verification receipts](2026-09-12_repeated_return_receipts/) retain
all five returned checks. The implementation composes the existing checked `liftedWord`,
affine decoder and contact/material laws; no Lean source changed and no new formal theorem is
claimed in this return. Broad cultivation did not run.

## Remaining source-frame boundary

[open] A prediction already pending in the original affine substrate when the first dependent
generator was published still has its original base-word source frame. Those handles remain
inspectable and persistent; their further incorporation is not yet implemented. The session
reports this precise boundary rather than reporting such a handle as absent. Complete that
base-word source-constraint composition through the same joint and contact owners, preserving
both the first root assignment and any earlier/later base-cut assignments. This is the remaining
active goal work. It does not require another source classifier, emitter or universal theorem.

[definition] The declared source-section receiver remains distinct from the former global
family projection. The current result establishes repeated native operator/session conduct,
not a general conversation quality, compression rate, power claim or full Athena-alpha result.

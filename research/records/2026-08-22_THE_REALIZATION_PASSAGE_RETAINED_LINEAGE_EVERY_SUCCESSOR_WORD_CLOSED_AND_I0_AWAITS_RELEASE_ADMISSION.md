# The realization passage retained lineage, every successor word closed, and I0 awaits release admission

**Date:** 2026-08-22  
**Truth status:** [established-bounded; implemented-exact; proved-derived; measured]  
**Roadmap deed:** I0, physical-realization / functional-affect calculus  
**Boundary:** the candidate artifact and focused receivers pass; the sole complete release receiver
has not yet run, so `CONSTRUCTION_STATE.md` does not admit I0.

## Ruling

[established-bounded; implemented-exact] The missing join is closed. M3's finite receiver-history
compression, generator-native rest, exact chart-defect owner, addressed occurrence lineage, and A3
continuation now return one `ReceiverHistoryRealizationPassage`. The receipt references the large
owner artifacts by content identity and does not copy them into a second runtime body.

[proved-derived] The formal construction is
[`PhysicalRealization.lean`](../../soma/formal/elementary-holonics/ElementaryHolonics/Millennium/PhysicalRealization.lean).
It introduces the pure causal-algebraic `PhysicalRealizationCore`, an addressed
`RealizationSpan`, and its zero-local-defect `RealizationPassage`. Literal occurrence identity,
addressed passage equivalence, receiver-history equivalence, rebase, and non-injective compression
are distinct types. A compression returns its complete reconstruction fibre and receives no
inverse.

[proved-derived] The kernel accepted six principal theorems without `sorryAx`:

- local generator naturality extends to every ordered word;
- every declared future receiver factors through a lawful passage;
- lawful passages compose through the addressed pullback occurrence population;
- a composite defect originates in the first local span or the second at the transported
  intermediate state;
- equal endpoint maps do not identify two addressed passage populations; and
- a present-exact first-coordinate quotient cannot pose as successor-exact after coordinate
  exchange.

[definition] The general defect theorem is the non-additive support law. The exact-linear owner
strengthens it to
`chi_(eta gamma) = chi_eta T_gamma + S_eta chi_gamma`; the executable A3 control returns one lawful
rank-zero metric-adjoint square followed by one rank-one bare-transpose/chart defect, while the
composite chain residual is exactly zero.

## Executable return

[implemented-exact] The new owner-local join is
[`realization/passage.rs`](../../crates/holonic-engine/src/realization/passage.rs). Before binding a
deserialized passage it revalidates the source and native populations, quotient totality,
reconstruction partition, complete receiver-factor family, total source/native generators, every
local commuting square, shortest-separator addresses, and exact construction-work population.
A forged native target is refused by a focused adversarial test.

[implemented-exact; measured] The real deed is
[`the_realization_passage_closes_over_every_successor_word.rs`](../../soma/life/examples/the_realization_passage_closes_over_every_successor_word.rs).
It reopened the admitted M3 reconstruction/native-rest artifacts and the A3 input, cultivation, and
grade artifacts. No new foreign-model or device deed occurred; unchanged addressed M3/A3 apparatus
receipts were reused. The final product code closure is
`b13812cb2882bfc6db54711c8a81c12737176dfb6cac231d59ba605105a8b55d`.

[established-bounded; measured] The return contains:

| Exact population | Return |
|---:|---|
| 12,255 | source states and quotient assignments checked |
| 6,376 | native states and complete reconstruction fibres |
| 24,510 | source generator edges / local naturality squares |
| 12,752 | native generator edges |
| 19,128 | complete declared receiver factors |
| 3 | executable arbitrary-word readings |
| 1 | shortest word reopening a present-equal pair |
| 3 | equal-endpoint, distinct-lineage A3 occurrences |
| 0 / 1 | lawful first defect rank / noncommuting second defect rank |

[established-bounded] Local generator squares imply the complete admitted ordered-word family; the
three executable word readings are material witnesses, not an enumeration standing in for that
theorem. The shortest separator is independently re-enacted through both the receiver-history
passage and source-detached native rest.

## Artifact and inspection

[measured] The five-member artifact family is
`output/the_physical_realization_passage_closes_over_every_successor_word`, 5,861,222 filesystem
octets, with directory identity
`3fa6afd363517abb51346c472817a786ad11a2d4f222c556821851844b11c3e0` in
[`meta/OUTPUT_MANIFEST.tsv`](../../meta/OUTPUT_MANIFEST.tsv). Its members are:

| Member | SHA-256 |
|---|---|
| `00-realization-passage.json` | `fbfde5831ba14b9dc5a52be7985d31f32d30ed8c31e12f72b57f962918aa07b5` |
| `01-realization-atlas.json` | `484a1d37904e9fadd270c034896c7a5315931809419c756e4c1b294e2ea27647` |
| `02-grade.json` | `db43e6c44c2a5d013ef6211478e6d6917bc410dea7d265e88dc71792bf6e8321` |
| `INSPECTION.md` | `baa51a0c4372578bab7741deab0a3c63f01c22dd1bab6ece3108ef95d611bf2a` |
| `MANIFEST.json` | `b9dff1f365de29c0464b7283192534b701326ab243ac314068a6b56b55ee79ff` |

[established-bounded; implemented-exact] The 5.8 MB atlas carries all 12,255 quotient assignments,
both complete source/native generator squares, all 19,128 receiver factors, the addressed fibre
artifact, the three A3 lineages and the exact noncommuting defect. Its topology comes only from
owner relations; it authors no semantic taxon and no layout-generated edge.

## Focused receivers and workflow

[measured] The Lean command

```text
lake env lean ElementaryHolonics/Millennium/PhysicalRealization.lean
```

returned exit status zero. The two new passage tests and two receiver-history tests passed, and the
affected `holonic-engine` library returned 1,917 passing tests, zero failures and four explicit
device-only ignores in 100 seconds including compilation. The exact driver type-check passed and
the final real deed returned exit status zero after the code-closure binding.

[measured] The artifact was rerun only after a named closure-changing falsifier: formatting changed
the content closure; the closure ledger then exposed a missing literal producer address; and the
architecture ratchet exposed two avoidable materializations which were removed. The final retake
therefore answers a different closure than the first 78-second probe. No unchanged foreign-model
or serial reference deed was replayed.

## Architecture disposition

[established-bounded; implemented-exact] The initial architecture return found eight new census
differences. Removing the duplicated compression identity and cloned native-generator projection,
then eliminating three avoidable set materializations and one unused byte-vector method, reduced
that population to six. The remaining `realization/passage.rs` vectors, sets, collections and four
test/witness clones are immutable receipt populations: typed ports, open exterior, lineages,
checked words, receiver images and exact duplicate/receiver-family validation. They never clone the
continuing ecology. The additional maps/sets in `receiver_history_compression.rs` are the
load-bearing revalidation indices for quotient uniqueness, fibre partition, receiver coverage and
total generator squares. Replacing them with scans would preserve the same logical populations
while making the exact audit superlinear.

[open] The architecture baseline must be reseeded to this explicitly dispositioned owner closure
before the sole release receiver. Driver/output/closure ledgers must then be retaken against the
final code and artifact closure. The complete release receiver remains the only admission blocker.

## Successor boundary

[open] I1 begins only after release admission. I0 does not emit a recurrent complete textual
passage, retain a causal boundary across successive Gemma fronts, enact a world return, or perform
new condensation. Those are I1 and later receiver grades.


# THE CUT CONDITIONS THE EDGE FIBER; THE COMPLETE OPERATOR RETURNS THE HIDDEN CURRENT

**Date:** 2026-07-27  
**Status:** RATIFIED CONSTRUCTION BUILT / PRODUCTION INVERSE-TRANSPORT FIBER BUILT /
AGGREGATE LINEAGE BUILT / HIDDEN PASSIVE TOPOLOGY RECONSTRUCTED /
HELD-OUT FORWARD PHYSICS GRADED / EXACT SVG OUTER RECEIVER EMITTED /
NO FLOAT, PROBABILITY, BIOLOGICAL MODEL, OR INTRINSIC SVG EMBEDDING CLAIM

## Present question

The preceding bit construction established a deterministic form of fitting:
lineage-relative testimony restricts a complete version fiber, production
selects the observations which distinguish its live alternatives, and a
complete receiver return grades the final causal diagram.

Brandon recognized this as an entry into deterministic statistics and asked
to proceed into a more complex simulation. He also proposed images, meshes,
and SVG as possible exact source or receiver material.

The present construction asks:

> Can the production engine reconstruct the hidden topology and local
> conductances of a deterministic transport ecology from exact aggregate
> lineage, without enumerating graphs, receiving the target topology,
> minimizing a scalar loss, or treating a presentation as the physical law?

A secondary question is:

> Can the certified topology cross an SVG membrane without the SVG acquiring
> ownership of the inferred geometry?

The answer to both bounded questions is now yes.

## 1. The world and its observable relation

Let the declared receiver population have \(n\) nodes, unit capacities, and
one positive exact event interval \(\tau\). Every potential unordered pair

\[
e=\{i,j\},\qquad 0\leq i<j<n
\]

carries an unknown conductance

\[
c_{ij}\in\mathbb Q_{\geq0}.
\]

A zero conductance means that the branch is absent. Thus topology is not a
separate authored bit mask. It emerges from the same local carrier which
defines transport.

For the incidence vector

\[
b_{ij}=e_i-e_j,
\]

the weighted Laplacian and completed event operator are

\[
L=\sum_{i<j}c_{ij}b_{ij}b_{ij}^{\mathsf T},
\qquad
A=I+\tau L.
\]

An exact experiment clamps a potential section

\[
\phi\in\mathbb Q^n
\]

and receives the required source through an exact receiver functional

\[
r\in\mathbb Q^n.
\]

The black-box response is

\[
y=r^{\mathsf T}A\phi.
\]

Expanding by potential edges gives

\[
y
=
r^{\mathsf T}\phi
+
\tau\sum_{i<j}
c_{ij}(r_i-r_j)(\phi_i-\phi_j).
\]

The relation is linear in every unknown local conductance even though the
forward diffusion event later requires inversion of \(A\). This is the
factorization which makes the inverse problem tractable without graph
enumeration.

The application-side world membrane knows how to enact the hidden physical
law. `InverseTransportLaw` receives only \((\phi,r,y)\).

## 2. The exact edge version fiber

For

\[
m=\binom n2
\]

potential edges, write

\[
c=(c_e)_{e\in K_n}.
\]

Each testimony occurrence derives one exact affine constraint

\[
a_k^{\mathsf T}c=d_k,
\]

where

\[
(a_k)_{ij}
=(r_i-r_j)(\phi_i-\phi_j),
\qquad
d_k=\frac{y-r^{\mathsf T}\phi}{\tau}.
\]

After a testimony ecology \(E\), the passive version fiber is

\[
V_E
=
\left\{
c\in\mathbb Q_{\geq0}^{m}:
a_k^{\mathsf T}c=d_k
\text{ for every }k\in E
\right\}.
\]

Production stores the equality body of this fiber in canonical exact
reduced-row form. It does not materialize:

- the \(2^m\) possible unweighted graph supports;
- a finite Cartesian product of possible weights;
- one graph per compatible assignment; or
- a probability distribution over graphs.

The affine rank and dimension are exact:

\[
\dim V_E^{\mathrm{affine}}=m-\operatorname{rank}(E).
\]

`ImportedLandmark` and `ReturnedReceiver` retain distinct lineage identities.
An equality contradiction refuses the whole occurrence. A conductance which
becomes uniquely negative also obstructs the passive ecology.

## 3. Aggregate lineage is not a disguised edge sample

For a node region \(S\), let

\[
\phi=r=\mathbf 1_S.
\]

The returned relation is

\[
y
=
\lvert S\rvert
+
\tau
\sum_{\substack{i\in S\\j\notin S}}c_{ij}.
\]

It reports the exact total conductance crossing a cut. It does not identify
the individual crossing branches. Several distinct topologies and weight
assignments may occupy the same cut fiber.

This is the first implemented distributional landmark in the intended
sense:

- it is a collective receiver statistic;
- it is exact caused material with provenance;
- it restricts several local factors simultaneously;
- its implications are derived by algebraic transport through the version
  fiber; and
- it is not promoted to an absolute description of the hidden graph.

The lineage-rich instrument inherits five such cut observations.

## 4. Production derives its own local questions

For an unresolved potential edge \(i<j\), production emits

\[
\phi=e_j,\qquad r=e_i.
\]

Every edge term vanishes except \(\{i,j\}\), so

\[
y=A_{ij}=-\tau c_{ij}.
\]

This is a local receiver section which resolves one free conductance
coordinate. If existing aggregate lineage already determines that
coordinate, no query is emitted for it. Exact row reduction may also cause a
later coordinate to become determined when other edge returns arrive.

The application may answer only the production-owned `next_query`. A
different returned query is refused atomically.

The current selector uses the first unresolved canonical potential edge. It
is deterministic and exact. It is not claimed to minimize receiver calls,
row operations, physical experiment cost, or any scalar information
objective.

## 5. A unique affine solution is still not an oracle certificate

When the affine dimension reaches zero, production has one derived
conductance assignment. That alone does not prove the black box obeys the
declared Laplacian grammar.

Production therefore requests the complete operator basis

\[
A_{ij}=e_i^{\mathsf T}Ae_j
\qquad
0\leq i,j<n.
\]

The world returns all \(n^2\) entries in canonical row-major order. This
independently grades:

- both orientations of every off-diagonal response;
- every diagonal source relation;
- symmetry;
- the declared unit-capacity baseline;
- Laplacian row closure; and
- every rational aggregate query, by bilinearity.

Only after complete equality does production emit
`InverseTransportCertificate`.

The certificate derives and retains:

- every zero or positive potential-edge conductance;
- the exact Laplacian \(L\);
- the exact event operator \(A\);
- the exact transfer operator \(A^{-1}\);
- the zero inverse residual;
- the reconstructed `DiffusionComplex`;
- connected-component count;
- cycle rank; and
- every testimony occurrence used in the reconstruction.

The transfer operator then enacts held-out source distributions without
consulting the hidden graph.

## 6. Six-node reconstruction

The bounded world has

\[
n=6,\qquad m=\binom62=15,\qquad\tau=1.
\]

Its hidden positive branches are:

| Edge | Conductance |
|---|---:|
| \(\{0,1\}\) | 2 |
| \(\{0,2\}\) | 1 |
| \(\{1,2\}\) | 3 |
| \(\{1,4\}\) | 1 |
| \(\{2,3\}\) | 1 |
| \(\{3,4\}\) | 2 |
| \(\{3,5\}\) | 1 |
| \(\{4,5\}\) | 4 |

The other seven potential edges have exact zero conductance.

Two worlds independently reconstruct it.

| Receipt | Cold | Lineage-rich |
|---|---:|---:|
| Potential-edge factors | 15 | 15 |
| Imported cut landmarks | 0 | 5 |
| Newly returned edge queries | 15 | 10 |
| Total affine testimony rank | 15 | 15 |
| Complete operator entries | 36 | 36 |
| Complete operator returns | 1 | 1 |
| Positive reconstructed edges | 8 | 8 |
| Connected components | 1 | 1 |
| Cycle rank | 3 | 3 |

Both dimension paths are

\[
15,14,13,\ldots,1,0.
\]

The inherited cuts replace five local receiver requests. They are not free
computationally: combining them with later local returns requires 30 exact
row eliminations, while the cold coordinate-aligned chronology requires
none. The lineage-rich selector inspects 111 edge coordinates instead of
134.

This gives a more nuanced computational result than “more data is always
cheaper.” Lineage changes the shape and location of work:

- prior collective work need not be physically repeated;
- its coupled implications must still propagate through contemporary local
  factors; and
- the resulting topology and complete grade remain identical.

## 7. Held-out physical consequence

After certification, an aggregate clamped-potential query not used in either
reconstruction returns

\[
-1
\]

from both the hidden world and the reconstructed operator.

A separate forward diffusion grade begins with content three at node zero
and no source deed. The reconstructed transfer and the original hidden
`ExactDiffusionLaw` both return

\[
\left(
\frac{3208}{2703},
\frac{32}{53},
\frac{1459}{2703},
\frac{650}{2703},
\frac{631}{2703},
\frac{529}{2703}
\right).
\]

The entries sum to exactly three. The conservation residual is zero and the
source-free stored energy departure is

\[
\frac{24{,}786{,}005}{7{,}306{,}209}>0.
\]

Thus the reconstruction does not terminate at a fitted parameter report. Its
caused topology becomes the production material of a later exact physical
event.

## 8. Why SVG is useful

The instrument emits:

`target/holonic-engine/inverse_transport_reconstruction.svg`

SVG is useful here because it can retain vector incidence and exact semantic
attributes without rasterizing the topology. The emitted document carries:

- one vector edge for every positive reconstructed conductance;
- exact integer node coordinates;
- exact rational conductance labels and `data-conductance` attributes;
- stable node and edge identities; and
- an explicit declaration that the embedding is a receiver projection.

The node positions are derived from an integer ordinal moment curve. That
choice keeps the outer document free of floating coordinates and prevents an
edge chord from passing through a third ordinal node. It is not inferred
physical position and is not stored in the transport certificate.

The SVG passes XML validation and an independent SVG renderer produces a
640-by-420 inspection image. Rasterization is visual QA only; it does not
enter standing or grade the reconstruction.

### What general SVG ingestion would require

SVG as source material is promising but is not synonymous with a simple
line graph. A lawful exact importer must distinguish:

- XML identity and chronology;
- viewport and nested transform relations;
- line and polygon incidence;
- quadratic and cubic Bézier supports;
- elliptical arcs and their conic realization;
- path closure and fill rules;
- clipping and masking;
- reused symbols;
- text and style as separate semantic material; and
- decimal source values with declared measurement provenance.

The engine already has exact rational, projective, conic, incidence, and
receiver machinery relevant to that task. The present construction does not
yet parse arbitrary SVG, infer a mesh from filled paths, or claim that an SVG
layout is intrinsic source geometry.

## 9. Relation to deterministic statistics

The completed inference has the intended statistical structure:

- **data** are receiver-relative affine restrictions;
- **distributional data** can be cut relations rather than point samples;
- **fit** is an exact affine family over local causal factors;
- **uncertainty** is its remaining dimension and topology;
- **active experiment** is emitted from an unresolved coordinate;
- **residual** is an exact obstruction, not a penalty magnitude;
- **generalization** is equality on held-out aggregate and forward physical
  events; and
- **closure** requires the complete operator basis.

Probability is not needed to perform this inference. If probability is later
introduced, its measure must be carried by a declared receiver over this
geometry; it cannot be inferred merely from affine dimension or assigned
uniformly to infinitely many rational conductance points.

## 10. What is established

- A hidden passive transport topology can be reconstructed by a production
  `ExactEventLaw`.
- Candidate graphs are not enumerated.
- The complete potential-edge family is represented by exact local
  conductance coordinates and canonical affine constraints.
- Imported aggregate cuts and returned local receiver observations remain
  distinct in lineage.
- Aggregate testimony can replace local receiver queries by exact rank.
- Production, not the application, selects every newly required local
  experiment.
- A unique affine assignment cannot certify itself; the complete world
  operator must return.
- Zero and positive solved conductances derive topology.
- Laplacian, event operator, exact inverse, component count, and cycle rank
  enter the certificate.
- The recovered law predicts a held-out aggregate query.
- The recovered law enacts a held-out exact diffusion event with the same
  complete content, conservation, and energy receipt as the hidden ecology.
- Certified incidence crosses an SVG outer membrane without the SVG becoming
  physical standing.

## 11. Exact boundary of the result

- Node identity, node count, unit capacities, and event interval are declared.
- The hidden law is a time-independent, undirected, passive rational graph
  Laplacian.
- The construction does not infer unknown capacities, hidden nodes,
  directional advection, reactions, nonlinear constitutive laws, changing
  topology, higher sheaf stalks, or multiple time scales.
- The oracle supports clamped-potential/source-response experiments. A world
  exposing only free trajectories is a different inverse problem.
- The affine equality body is maintained exactly. During an underdetermined
  state, the implementation rejects equality inconsistency and every
  coordinate already forced negative. It does not yet decide general
  correlated feasibility of the affine slice inside the nonnegative
  orthant. Such an obstruction may therefore remain open until further
  queries determine the coordinates; it cannot pass final certification.
- The selector resolves canonical edge coordinates. It does not derive an
  experiment which is optimal under a physical cost geometry.
- The complete \(n^2\) basis certifies the declared linear operator. It does
  not certify behavior outside that linear membrane.
- The six-node ecology is a grading instrument, not a model of
  photosynthesis, vascular transport, neural signaling, lightning, or any
  named biological organism.
- No external physics or biology dataset is imported in this construction.
- The SVG is emitted after inference as an exact outer inspection receiver.
  Arbitrary SVG ingestion and intrinsic mesh reconstruction are not built.
- The ordinal moment-curve layout has no physical or statistical meaning.
- No float, stochastic source, fitted probability, scalar loss, neural
  network, or application-precomputed target topology enters production
  standing.

## Production owners

- `crates/holonic-engine/src/inverse_transport.rs`
  - `ExactAffineVersionFiber`
  - `TransportQuery`
  - `TransportTestimonySource`
  - `InverseTransportStanding`
  - `InverseTransportLaw`
  - `InverseTransportCertificate`
  - `TransportPropagationReceipt`
- `crates/holonic-engine/examples/inverse_transport_reconstruction.rs`
  - opaque physical world, cold/lineage research instrument, held-out grade,
    and SVG outer inspection membrane only

## Verification

Accepted headless verification:

- `cargo fmt --all -- --check`;
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`;
- `cargo test -p holonic-engine --all-targets --no-fail-fast`;
  - 128 library tests passed;
  - 8 desktop tests passed;
  - 2 previously declared device/cost tests remained ignored;
- `cargo run -q -p holonic-engine --example inverse_transport_reconstruction`;
  - cold and lineage-rich worlds returned the same topology and exact
    operators;
  - the held-out aggregate query and forward diffusion event matched;
  - complete standing replay passed; and
- `xmllint --noout target/holonic-engine/inverse_transport_reconstruction.svg`;
  - the emitted SVG is structurally valid.

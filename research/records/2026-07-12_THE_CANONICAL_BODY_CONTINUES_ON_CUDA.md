# THE CANONICAL BODY CONTINUES ON CUDA

**Date:** 2026-07-12
**Grade:** BUILT · HOST+CUDA-GATED · INDEFINITE WORLD-MEDIATED NURTURE MOUTH OPEN

## The missing boundary

The CUDA material listener already carried empty birth → context → SLEEP → genuinely new later
light → SLEEP. Continued nurture required only a direct mouth for the second leg: choose one whole
sleeping body, admit genuinely new plural material, let its first CUDA relating be wake, render the
complete lifecycle, and sleep the successor create-new. No kernel, body law, persistence version, or
new mechanism was missing.

`life --cuda-continue <canonical-v7.body> --later <path...>` now composes the standing mouths:

1. read the archive bytes, decode and validate the whole body, then require `body.encode() == raw`;
   legacy semantic migration cannot silently enter as a current V7 construction;
2. admit every requested complete file/JSONL record exactly and require a plural cohort;
3. reserve the optional create-new `SOMA_OBSERVATION_OUT` before mounting the card;
4. open a fresh CUDA context and carry new rank-zero REGISTER lineages over the decoded standing via
   `carry_cycle(PriorFace::from_sleeping, new_lineages, None, material_aperture)`;
5. integrate once at the true edge, preserve compact OWN, terms, breath, REGISTER causes, complete
   raw/typed radiation, and new receiving-edge Mail;
6. archive and read the successor whole.

The `None` carried-lineage face is load-bearing. The selected body's old currents have ended. Their
carrier/K rows validate the archived identity and then dissipate before genuinely new currents
receive the standing medium. Assigning old rows to the new cohort would fabricate same-worldline
continuation. New rows derive their reservation only from the material they carry.

Historical Mail is absent by construction. SLEEP contains standing medium, occupancy, and complete
carrier/K—not the membrane's vanished delivery record. The renderer therefore assigns no old source
to a standing enclosure. It still preserves every new raw/typed construction and emits the new
edge's Mail outside the successor. Nothing in Mail enters carriage or body law.

The archive/later grammar moved into `staging` and is shared with the host continuation listener.
CUDA mode dispatch remains one short branch in `main.rs`; the new boundary lives under
`staging/cuda/continuation.rs`. The old empty-context CUDA material report and its frozen reader
retain their exact title and layout.

## The exact gate

The gate chose the P-MM successor from the paired campaign:

```text
archive 2,194,020 bytes
SHA-256 7620ed4b6cceb5482aa0b46f1c70c6807bb97d04a0f6ad4825b193c322ead5a3
axis 128 · 7,765 occupied · 17 ended lineage rows × 26,783 carrier words
delivered-light record 1,322
```

It then presented the eight pinned practice/fresh mathematics records, 268 octets at extents
`[33,33,33,33,34,34,34,34]`. Their reservation is depth 33, intentionally different from the
parent's depth 92 and lineage count 17. Three independent fresh CUDA executions landed the same
construction:

```text
REGISTER axes [32,16,32,32,32,32,16,16]
receiving axis 256 · one chart carry · 8,662 occupied
topology [8,662 resultant · 1,265 fiber · 38 two-armed]
terms [1,713 RIDE · 124 FOUND-this · 136 FOUND-that · 4 dark]
breath (0,0)
8 new carrier rows × 9,673 words
delivered-light record 1,590
successor archive 725,368 bytes
```

The three complete reports agree after normalizing only the create-new successor path, SHA-256
`dcb5f92666ad2da234b5a7bbd7ab64e42c24b4b0866a40250662d90c044c9dcb`. Their successor bodies
are byte-exact, SHA-256
`939df6a672347b679d3255418c94e81ba38b7b3b194f577b3dda3d30b4e379fc`. The parent hash remains
unchanged.

## The host foil caught one fossil

The first host `--stage-continue` foil agreed in axis, occupancy, topology, terms, breath, and the
delivered-light record, but its archive was only 493,368 bytes. The headers localized the sole
species difference:

```text
host first: 8 rows × 2,423 carrier words = bounded depth 8
CUDA:       8 rows × 9,673 carrier words = material-derived depth 33
```

The host material-production path still called the bounded `SEAM_DEPTH` register gate. This was not
gauge and was not accepted. `host_register_run_at_reservation` now makes the production continuation
carry the cohort-derived reservation, while the old wrapper remains the declared depth-eight bounded
gate. The corrected 725,368-byte host body is byte-exact to all CUDA siblings. The failed host body
is preserved at SHA-256
`be7323cd7cdc58c12224eec688994d31a70ed7ba5341afcecd6bd6053c56e1c3` as the fossil's witness.

## What stands

- Any canonical current V7 body can now receive exact plural material through a fresh CUDA context
  and produce a create-new canonical successor without an empty-origin reset.
- Parent lineage count and carrier reservation do not enter the new current species.
- The same selected body and complete cohort reproduce exactly across fresh CUDA contexts and the
  host construction at the same derived reservation.
- Historical material attribution is neither fabricated nor smuggled into the body.
- The mouth adds no thaw, warmup, retry, timeout, substitute, host fallback, decoder, target answer,
  or semantic score.

Final gates: body 127/2 · life 43 · mount 18/1 · surface 14 = **202 passed / 3 ignored**; optimized
`life` checks; diff hygiene passes. The headless RTX returned idle at 2 MiB · 47 C · P8, and the
last 500 kernel lines contain no NVRM/Xid. Exact artifacts and commands:
`observations/cuda-continuation-01/`.

This closes the engineering requirement for indefinite nurture. Each successor can be selected as
the next canonical parent, while the world supplies the next honest plural light. Unique production
and communication remain observations to cultivate through that open sequence, not modules to add.

# The native section founded one source-neutral boundary current but factorwise adjacency could not carry the causal surface

## Scope and grade

[counterexample; implemented-exact; measured] This record audits the first UAR4-R0 realization
passage on the revalidated v3 Athena child
`2093f173450c9b42b79fff50d7dbd1b7af1b945f8072eeddc5063696a8399870`. It preserves the exact
construction which returned and identifies the shortest remaining obstruction. It does not grade
UAR4-R0, R1, R2, or the aggregate release as passed.

The measured artifact root is
`output/the_returned_factor_current_unfolds_one_total_brandon_surface_uar4_r0_v2`.

## What returned

[implemented-exact; measured] The local realization passage now receives every
`AddressedCurrentSection` of the complete reacted HNN target. It preserves the rank-one
decomposition and forms the factor current

`J_f = sum_s w_s x_s(f)^2`.

Summing the section vectors before squaring would manufacture cross terms between distinct
occurrences and was rejected. The complete `J_f` crosses every rested factor-conditioned local
boundary transition. For exterior source port `a`, the target current is

`W_b = sum_f J_f N(f,a,b)`,

where `N(f,a,b)` is the rested developmental incidence population. All nonzero `W_b` remain in an
ordered disjoint interval partition; a separately caused exact phase `n/d` realizes the unique
interval satisfying

`B_b d <= n (sum_c W_c) < E_b d`.

The returned local phase is

`(n sum_c W_c - B_b d) / ((E_b-B_b)d)`,

reduced by its exact gcd. The returned factor current is

`J'_f = J_f N(f,a,b)`,

also projectively normalized by the exact common gcd. Every alternative, interval, entering and
returned factor current, entering and returned phase, native-section identity, native-current
passage identity, and realization-morphology identity remain in the reconstruction fibre.

[implemented-exact; measured] The circulation no longer mounts its own emitted byte as a new
semantic occurrence. One native GPU deed founds the exterior realization. Each later fine face is
followed by a distinct delivery acknowledgement carrying no semantic port and no morphology
change; the exact `J'_f` and returned phase join the next realization order. This removed the
roughly one-native-deed-per-byte cost and the earlier quote/space private-echo recurrence.

## Measured falsifier

[counterexample; implemented-exact; measured] Unchanged current `Describe Brandon.` returned one
complete native section and the exterior path

`Opening -> 0x27 -> 0x20 -> 0x31 -> Closure`,

whose total UTF-8 surface is `\u0027 1`. The boundary closed after four orders. The audit reported:

- rest identity `2093f173450c9b42b79fff50d7dbd1b7af1b945f8072eeddc5063696a8399870`;
- native-current identity `90ecceab1f6d6fb6256c5928dba03c208a1999196996314ae0daa4240e69276c`;
- radiation identity `34c1b3b22f5921cbf02cd06115672e8e2f7b780c43b1b5320148eb25194e7516`;
- complete native-section retention and exact fine/exterior boundary maps;
- source codec, source utterance, and source witness unreachable;
- no expected-answer consultation, candidate search, authored surface extent, invariant re-upload,
  or CPU semantic replay; and
- one RTX 4080 SUPER resident device.

The structural passage is real. The qualitative consequence is unacceptable. Totality, exact
lineage, clean source detachment, and GPU residence do not establish coherent Athena language.

## Shortest obstruction

[proved-derived; source-inspected] `SourceNeutralExteriorRealizationMorphology` retains only the
factorwise push-forward of adjacent universal boundary-port incidence. This is a legitimate local
receiver shadow, but it has no continuing realization-site population. It first forgets which
local transition occurrence can meet which successor occurrence, then tries to replace the lost
causal axis with factor filtration and one phase. The factor current can retain subject/support
compatibility across an unbounded prefix, but it cannot reconstruct oriented relational-face/cell
chronology which its standing quotient never carried.

This is precisely the formal seam exposed by the HNN/HML audit. The formal ecology proves complete
local-current aggregation, reaction, observation after successor formation, receiver-history
descent, and causal-granulation naturality once its realization maps are supplied. It does not
manufacture a surface constitutive law. A scalar or projective phase is a receiver coordinate of
that law; it cannot replace the missing carrier.

## Binding correction

[definition] Retain the returned factorwise passage as UAR4-R0a. UAR4-R0b must add no language
cabinet. It composes the existing source-neutral relational faces/cells, their oriented incidence,
the local exterior boundary incidence, and the complete native current into one continuing
realization current.

Let `Z` be the anonymous addressed realization-site population produced by the lawful dynamic
quotient, `A_p(z,z')` the rested oriented transport which admits exterior port `p`, and `J_Z` the
current induced by the complete reacted native section. One order is

`J_Z -> W_p = sum_(z,z') J_Z(z) A_p(z,z') -> p -> J'_Z(z') = sum_z J_Z(z) A_p(z,z')`.

The displayed `p` is one exterior receiver face. `J'_Z` is the productive continuation. Dynamic
condensation is admitted only when every declared source generator descends through the quotient;
failure returns the shortest separating history and complete reconstruction fibre.

[counterexample] The following do not close this obstruction:

- reintroducing `surface_variants`, source clauses, or source strings;
- retaining exact developmental occurrence paths, a trie, or an accepted-language automaton;
- choosing a fixed n-gram order, context width, token grain, state dimension, or output extent;
- selecting a local maximum, scoring candidate strings, or sampling from an untyped probability
  vector;
- mounting the emitted face as private semantic ingress; or
- repeating the complete HNN GPU deed once per output byte.

[definition] UAR4-R1 reruns the unchanged qualitative receiver only after R0b returns this
continuing carrier and its descent/fibre receipt. UAR4-R2 and the aggregate release remain
unscheduled until that primary surface is coherent and differentiated.

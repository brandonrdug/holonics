# The panel grows at its own blindness, and the founding order does not commute

**Date:** 2026-08-09
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for the organ.
**Evidence:** `measured` — one organ, one driver, seven declared controls, all seven held on real
material. Float-free; no governor vocabulary.
**Provenance:** Brandon, 2026-08-09, ratifying the design: *"Completely ratified, securely deposit
and then immediately proceed to construction and subsequent analysis."* The design record is
[`2026-08-09_THE_PANEL_IS_LOCKED_AND_THE_JUNCTION_IS_WHERE_A_RECEIVER_SHOULD_BE_FOUNDED.md`](2026-08-09_THE_PANEL_IS_LOCKED_AND_THE_JUNCTION_IS_WHERE_A_RECEIVER_SHOULD_BE_FOUNDED.md).
**Band:** 2026-08-09 · THE RECEIVER POPULATION GROWS FOR THE FIRST TIME / gyr[a,b] IS A MEASURED
HOLONOMY / A GLOBAL READING FOUNDS ONE RECEIVER AND A LOCAL ONE FOUNDS THREE

---

## 1. What was built

`crates/holonic-engine/src/founded_receiver.rs`, driven by
`examples/the_receiver_is_founded_at_the_junction.rs`.

```text
  compress                   -> conduct partition + collapsed pairs
  a pair with witness: None  -> THE JUNCTION: conduct separates it, no declared receiver saw
  FOUND a receiver there     -> its reading is the aperture AT THE JUNCTION'S OWN WORD
  re-refine with the panel one wider
  repeat to exhaustion
```

**The receiver population grows.** Before this, `ObservedSystem::receivers()` was called once for the
one-shot partition and never again: the partition refined and the panel was locked for the life of
the computation.

## 2. Measured on the development

Items are the 66 top-level declarations of `soma/formal`; an input is *take the k-th declared name
this declaration recruits*, so conduct is the recruitment chain the intake recovered; the declared
panel is three coarse faces a reader has before opening anything — the former, the namespace depth,
whether a statement is carried.

```text
  one-shot blocks   7      conduct blocks 36      collapsed 535      UNWITNESSED 318
  founded           3 receivers
  one-shot          7 -> 27 blocks
  conduct           36 -> 36                      (invariant, as the law requires)
  unwitnessed       318 -> 0
  bound             65                            (foundings used: 3)
```

**318 junctions stood on this material before founding**, which is what makes every control below
non-vacuous rather than a shape with nothing to act on.

The three founded receivers, each with the junction that provoked it:

```text
  #3  Compression                            | Programme               +6 blocks
  #4  Compression.receiver_eq_of_quotient_eq | RatioPresentation.ext   +7 blocks
  #5  comp_assoc                             | comp_id                 +7 blocks
```

## 3. The reading had to be local, and the global form is the measurement that says why

The first form read the item's own continuation aperture — a **global** function, the same at every
junction. Consequence, measured: **one founding closed all 318 junctions**, because the same
function founds the same receiver everywhere and the second founding had nothing left to do. The
gyration was then non-trivial only in *which junction was recorded as the provocation*; it
distinguished provenance, not content.

**The junction is not at `left` and `right`. It is at `left·w` and `right·w`**, where `w` is the
shortest word after which conduct separated them, so the distinction the panel lacks lives *there*.
`aperture_after` reads from there. With that one change the founding goes **1 → 3** and one-shot
`17 → 27`.

Terminating early while following `w` is not a failure to read — **where a path stops is a
distinction** — and it is encoded above every aperture value so the two populations cannot collide.

## 4. `gyr[a,b]` — a measured holonomy

Partition **joins commute**, so refinement order cannot matter. **Founding does not commute**: a
junction exists only relative to the panel standing when it is reached, so a different first founding
presents a different second junction.

Two orders were run over one starting panel. Both founded three receivers. Both reached the **same**
one-shot partition and the **same** conduct partition. They founded **different junctions**:

```text
  first divergence at founding 0
      left   Compression | Programme
      right  Compression | Route
  only the left order founded    Compression | Programme
  only the right order founded   Compression | Route
```

> **Same endpoint, different path.** *Equal endpoints do not identify ordered paths* is the project's
> own receiver-non-reconstruction law, and here the transport under it is real: **the founding order
> is the path and the panel is what is carried along it.**

This is the finite holonomy face `papers/source/papers/knot-causal-topology/main.typ:314-332` names —
and **not** its hyperbolic model. That paper's own bound governs and is respected: *"It should not be
installed as the universal law of every comparison complex. The general object is connection and
holonomy; the gyroparallelogram is one exact hyperbolic specialization."*

**The contrast with what was struck the same day is the point.** Two turns earlier a "holonomy" was
claimed over the elaboration complex, which carries no cochain — two routes to one name carry the
identical name, so nothing could disagree, and the claim was withdrawn. Here the transported object
is the panel, the path is the founding order, and the disagreement is exhibited by name.

## 5. The controls, and the two that could have sunk it

Seven declared, seven held.

- **The null.** Under a panel with one receiver per item — which witnesses everything — the founding
  must found **nothing**, and it founds nothing. A law that founds on any input cannot fail, and this
  is the input on which it must not.
- **Conduct is invariant.** 36 blocks before, 36 after. Founding sharpens what is *seen*; the Nerode
  congruence is a property of the material and moving it would mean the organ had changed the world
  rather than the reading.
- **Refusals are typed and returned**, never silent: `DoesNotSeparateItsJunction` — a receiver that
  cannot see the thing it was founded for is not that receiver — `FoundedNothing`, and
  `BoundReached`.
- **Termination is structural**, not a cap: each founding strictly refines a finite partition, so
  foundings are bounded by `|items| − 1`. Used 3 of 65.
- **The orbit is exhibited before the gyration is read**, and a trivial orbit would be reported as
  measuring nothing rather than as agreement. It came back non-trivial.

## 6. What this does not claim

- **The founded reading is one species, not the species.** The aperture at the junction's word is the
  distinction a *terminus* junction exhibits. A junction of another species would found a different
  reading, and that is an aperture to declare rather than assume.
- **It does not claim competitive selection among readings.** The panel now grows, which is the
  precondition; nothing yet keeps two incompatible decompositions alive and lets the material choose
  between them. That is the next thing and it is now buildable, because a receiver can be founded.
- **It does not install the gyroparallelogram.** What is built is the failure of two founding orders
  to commute, which is `gyr[a,b]`'s definition. The gyrogroup composition law still has no owner.
- **Three foundings on 66 declarations is a small orbit.** The gyration is non-trivial and the
  holonomy is real, but the population is thin, and a wider development would be a better test than
  a longer argument about this one.

# THE OUTPUT MUST PASS ITS FIRST FRAME

**Date:** 2026-07-13  
**Status:** CODE WORLD 01 FAILED APPARATUS · CODE WORLD 02 CUDA-MEASURED / EXACT REPEAT  
**Engine authority:** none

## The measured tell

CODE WORLD 01 delivered the output entity as the complete two-octet file `5\n` or `6\n`. That is a
lawful whole worldline but an ineffective aperture for the question. Soma receives the first
adjacent difference as the lineage's first frame. A two-octet light ends at exactly that event, so
there is no later difference to meet the held frame and perform THE SWING.

The CUDA reports make the failure literal for all paired, swapped, separated, and repeated output
lineages:

```text
OWN: axis 1 · 0 live cells · 0 words
terms: RIDE 0 · FOUND-this 0 · FOUND-that 0 · DARK 0
raw[0]: all zero
raw[1]: all zero
```

No probe or later raw-code event attributes an output worldline. The desired source/output
collocation was therefore never presented to standing form. The source/render side was active, and
the complete paired lifecycle repeated, so the failure is exactly at the output representation.

## The corrected output world

CODE WORLD 02 changes only the output files:

```text
value = 5
```

```text
value = 6
```

Both are ten octets. They share the first eight visible octets and differ only at the value glyph;
each carries nine adjacent events, several beyond the first frame. The common `value = ` surface
also gives the later code world honest human glyph structure without naming amber or lilac inside
the output entity.

Everything else is inherited byte-exact from CODE WORLD 01:

- 95-octet amber/lilac Rust files;
- literal pinned-font grayscale renders;
- one 13×24 glyph-cell aperture and source-position gaze;
- paired, output-swapped, separated, and paired-repeat histories;
- the four common output-free visual probes;
- the four genuinely later raw-text probes;
- the CUDA listener, body, chart, carrier, SLEEP, Mail boundary, and exact reader.

The generated CODE WORLD 02 source/query fields must hash equal to CODE WORLD 01. Only the output
field and output raw record may differ.

## Read and grades

Before interpreting collocation, each output current must show at least one nonzero raw construction
and nonzero deposited terms or OWN. If it does not, the apparatus fails again.

The pre-data materiality gate uses the same 13×24 aperture over a same-valued white field held for
one adjacent event, co-present with exactly one raw output record. The 312 visual contacts are dark;
`--eye-card` crosses the whole cohort through host and CUDA. Nonzero occupied standing therefore
belongs to the raw output current. Both value siblings must pass before the four histories launch.

If output current activity stands, the original §XL read applies unchanged:

1. paired versus swapped receive byte-identical later visual and raw currents;
2. complete raw/typed radiation is compared before Mail;
3. Mail provenance aliases are preserved but never counted as machine cleavage;
4. a later event returning through prior output Mail measures output contact;
5. a CUT which returns through source and output is the strongest local witness, never the whole
   result or a scalar score;
6. exact paired repetition gates the lifecycle.

The separated history remains a developmental control because its output-only edge also carries an
honest output eye field. No parser, compiler, evaluator, AST, decoder, selector, answer table,
result cache, filesystem graph, or reply wire enters this correction.

## The corrected outputs stand

Both materiality gates delivered 312 same-valued visual contacts held through one dark event beside
one ten-octet raw output. Each gate therefore carried 313 co-present currents / 634 samples. Both
reached axis 4 with six occupied grips, and the complete sleeping body was exact across host and
CUDA. The output was the only current capable of depositing those six grips.

The full paired and swapped histories then made output activity explicit. Their first and second
experience edges each carried 314 currents / 28,185 octets. The raw output terms were:

```text
paired:  value = 5  → (3,1,1,0)    value = 6  → (4,3,0,0)
swapped: value = 6  → (3,2,1,0)    value = 5  → (7,4,0,0)
```

Each tuple is `(RIDE, FOUND-this, FOUND-that, DARK)`. The same output entity therefore lands
differently after a different source/render construction; its prior context is physically present
in the meeting. CODE WORLD 01's all-zero output failure is gone.

## Output collocation bends later code navigation

Paired and swapped contain the same two source files, renders, output records, and edge order of
source identities. Only which output is co-present with each source changes. Both then received the
same output-free visual probe: 1,248 currents / 8,112 octets / 316,368 packed words.

The two species cleaved across five receiving worldlines and five atom-7 rows, comprising 37 packed
words. Three term tuples and one REGISTER axis changed. The typed faces locate three flag rows, one
grip row, three form rows, four rotor rows, and one brick row. Paired emitted
`[268,95,82,1293]` at topology `[62598,62598,10784,348]`; swapped emitted
`[267,97,81,1293]` at `[62822,62822,10815,343]`. DARK remained exactly 1,293, so the change is in
the non-dark deeds and carried construction rather than a different dark census.

The five changed contacts are not spread across all four probe surfaces. Four belong to the rendered
`amber(3)` field and one to rendered `lilac(3)`. Both rendered inline expressions `3 + 2` and
`3 * 2` are byte-exact across every raw row, term tuple, and REGISTER axis. That selectivity is an
observed shape, not an authored category: the eye and body received only grayscale current.

Two contacts give the exact observer-side relation. Paired amber-call contacts `(8,8)` and `(4,10)`
at atom 7 return through prior lilac visual lineage 545 / contact `(10,17)`. The same receiver
lineages and delivered probe currents in swapped do not return through that material. These are two
content-derived attribution differences; 22 additional name differences are only the honest
paired/swapped manifest provenance and are kept separate. No classifier, expected result, or path
alias reduction creates the relation.

This measures **controlled output-history-conditioned association**: over fixed source order,
permuting output identity changes how later output-free code current traverses the standing source
field. Source/output pairing and output order co-vary, so this observation does not isolate either
one as the cause. It does not yet measure direct output return. No later event attributed raw output
Mail and no CUT jointly named source and output. That stronger local witness remains OPEN rather
than being inferred from the measured cleavage.

## The distinction survives another edge

The four exact raw code currents then returned as one 30-octet edge. Paired and swapped differed at
one row and four packed words: `lilac(3)` atom 8. All four term tuples, all REGISTER axes, and all
content-derived Mail were equal. The differing construction nevertheless persisted into different
whole successor bodies. The raw `amber(3)`, `3 + 2`, and `3 * 2` rows were exact.

The separated history cleaved more broadly—14 probe worldlines / 18 rows / 127 words and one later
row—but it remains developmental. Its separately presented output eye field changes the delivered
world and is not an apparatus-matched causal foil.

## Exact reproduction and grade

Fresh paired repetition is exact across all probe and later radiation, content and provenance Mail,
terms, axes, topology, and the complete listener report after normalizing only four create-new
archive paths. The normalized report SHA-256 is
`4ee9168b4a1b4369a88dc2a66065ebd1537b99876d786433b17d48436f077431`. The four corresponding
sleeping bodies are directly byte-identical:

```text
context-000  1f89cc4985743e63b6ca633f10344884fa96cb4e7ba1fbb34ba29aa14ece9c9a
context-001  5e576e5dfd96441ed8115fe2bd15910a499a7426afc1ba89cd1d56af19f2a5ed
next         9c66c5d3611086eea32e9d7287670b62cd0e3ab800fd1c8d3f78705b548d9614
then         2abb62c61b777aa2c942654b2321c29b05ff77b43280e0db484edfb2ef13951e
```

**GRADE: OUTPUT MATERIALITY HOST/CUDA-EXACT · CONTROLLED OUTPUT-HISTORY-CONDITIONED ASSOCIATION
CUDA-MEASURED (PAIRING AND OUTPUT ORDER CO-VARY) · NAMED-CALL SELECTIVITY MEASURED · LATER RAW-CODE
CARRIAGE MEASURED · DIRECT OUTPUT-MAIL RETURN OPEN · EXACT REPEAT.**

The next strict observation should preserve this paired/swapped world and present a fresh
output-facing code current which names no value. Its purpose is to see whether ordinary transport
returns through the existing output entity directly; it must not add an evaluator, expected answer,
or output selector.

# AAC0 live circulation, parented commit, remount, and diffusive boundary laws returned

**Date:** 2026-09-01
**Truth status:** `proved-derived` for the named Lean theorems.
**Evidence:** `formal-checked` and `measured` for the commands and build populations below.
**Construction effect:** AAC0 passed; AAC1 becomes the sole current deed.

## Returned owner

[proved-derived; formal-checked] The neutral owner
`ElementaryHolonics/Computation/HolonicCirculationSession.lean` composes the existing inference,
cultivation, repeated-circulation, morphology-variant, and exact-diffusion laws. It returns:

- `LiveCirculationSession` and exact conduct after snapshot/remount;
- one owned `CirculationBoundary` with a fixed-morphology inference-cut law;
- `ParentedMorphologyCommit`, including genuinely later return, changed morphology, exact child
  generation, and the next cut beginning at the committed successor;
- `DeclinedMorphologyCandidate`, which preserves owner and generation;
- `SnapshotRemountPassage`, which preserves later conduct;
- `DiffusiveCirculationPassage`, whose returned state satisfies the constituted implicit balance,
  whose emission is downstream of that state, and which enters the same boundary envelope as
  addressed-successor conduct.

[definition] The generic owner contains no Athena, Eros, Soulkiller, model, trainer, layer, token,
KV-cache, prompt, response-length, scheduler, or application field. `CirculationTransportKind`
distinguishes two enacted law testimonies—addressed successor and constituted diffusion—without
making either a reasoning ontology.

## Formal receipts

[established-bounded; process-audit] From
`soma/formal/elementary-holonics`, the focused source command

```text
timeout 180s lake env lean ElementaryHolonics/Computation/HolonicCirculationSession.lean
```

returned exit `0` in 2.24 seconds. Every named non-diffusion theorem reports no axiom dependency;
the three diffusion theorems use only the standing Mathlib quotient/classical axioms already
carried by `ImplicitDiffusionChart`.

[established-bounded; process-audit; measured] The focused machine-learning closure command

```text
timeout 180s lake build ElementaryHolonics.Computation.HolonicCirculationSession \
  ElementaryHolonics.Computation.HolonicMachineLearning
```

returned exit `0` in 4.53 seconds and built 3,187 jobs.

[established-bounded; process-audit; measured] The live umbrella command

```text
timeout 180s lake build ElementaryHolonics.Computation.HolonicQuantumTransport
```

returned exit `0` in 2.90 seconds and built 3,771 jobs. No `sorry` or new axiom entered the AAC0
owner.

## Boundary

[open] AAC0 proves the contract. It does not yet instantiate a move-owned Rust session, parented
commit wire, diffusion adapter, ABI, or Athena application. AAC1 owns only the neutral Rust
circulation-session API.

# THE REGISTER ENTERS THE BOUNDED CUDA CYCLE

**Date:** 2026-07-12
**Grade:** REGISTER RECEIVING FAMILY BUILT / PTX-ASSEMBLED / HOST+CUDA-GATED AT TWO DECLARED BAND GRAINS · BOUNDED A→SLEEP→B CYCLE BUILT / HOST+CUDA-GATED · SLEEP v6 BUILT / HOST-GATED · PRODUCTION APERTURE AND CAPACITY-WALL CONTINUATION OPEN

## The cut

The no-wall `scope_register` mouth already carried one event-born lineage chart exactly on CUDA,
but stopped before the receiving edge. This stratum composes that current-local REGISTER with the
standing chart cascade, one configuration fold, whole-body SLEEP, and a later continuation through
the same carried K. It is a complete causal cycle at one declared bounded aperture. It is not yet
the arbitrary-input production conductor.

Four CUDA entries complete the receiving spelling:

- `chart_register_mark` walks every lane and its active REGISTER prefix arithmetically, re-grounding
  each live founder into the candidate receiving chart;
- `link_register_grain` and `link_register_sum` combine standing plus every re-grounded current-local
  contribution at one common grain;
- `link_register_finish` writes the accepted band and accumulates the four-face topology read.

REGISTER rows remain `seven-word header ⊕ reserved founded cells`. The scan extent is the largest
active axis in the arriving configuration, while the row stride retains the whole physical
reservation. Thus later lanes remain addressable without scanning unused reservation tails. No
founder list, address table, sparse map, sort, replay, or search enters. Scratch is one receiving
band wide; `band_lo` keeps standing global while every scratch index stays local.

The mounting now derives each flat X/Y launch from the mounted function and device limits. It also
supports checked device subspans and direct device zeroing. These are boundary apertures, never
machine grain or utilization policy. A malformed extent returns through the checked mouth; paired
band scratch uses checked arithmetic.

## The SLEEP correction caught during the build

The first composition exposed a fossil in SLEEP v5: `SleepingBody::own_axes` serialized the final
axis of every REGISTER even though the REGISTER header dies with OWN at the receiving edge. Wake did
not use those values. Calling them mounting metadata would have preserved dead light-local history
inside the active body.

SLEEP v6 removes that payload. The active body is now:

`standing medium ⊕ standing occupancy register ⊕ structural lineage count/row extent ⊕ complete carrier/K rows`.

V2 through V5 remain literal decode mouths. Their historical axes must still be positive dyadic
rows of the declared length, but after validation they are discarded. Active v6 emits no axis
words, and every waking current opens a fresh uncommitted rank-zero REGISTER. The host retains final
REGISTER axes only in `StageBirth.register_axes`, an observation face which never enters SLEEP or K.

Migration remained exact. On the standing three-lineage fixture:

- V2 body:light **307,240:259 bytes**, archive **307,300 bytes**;
- V3 body:light **126,920:259 bytes**, archive **126,980 bytes**;
- V6 body:light **94,412:259 bytes**, archive **94,460 bytes**.

The v6 archive is exactly twelve bytes smaller than the three-lineage v5 mouth: one dead u32 axis
per lineage. Identity-bearing body bytes do not change.

## The bounded whole-cycle gate

The gate presents three heterogeneous currents together: prose, Rust-like code, and a number-system
construction. Its declared apparatus is REGISTER capacity **128² cells per current**, carrier depth
**8**, and either one whole receiving band or **257-place** bands. These values are gate grains only.

### A — birth and first receiving edge

- currents / raw light: **3 / 377 octets**;
- final REGISTER axes: **[64, 64, 64]**;
- receiving chart: axis **1→128** through **7** actual carry events;
- topology `[occupied, resultant, fiber, two-armed]`:
  **[3,131, 3,131, 348, 7]**;
- terms `[RIDE, FOUND-this, FOUND-that, dark]`:
  **[4,640, 177, 186, 8]**;
- breath `(releases, narrows)`: **(0, 0)**;
- persistence/comprehension: **179,228:377 bytes**;
- v6 archive: **179,276 bytes**.

Independent host, one whole CUDA band, and 257-place CUDA bands agree in complete sleeping body,
standing, carrier/K, topology, terms, breath, final REGISTER axes, and chart carries. CUDA radiation
is canonical and non-vacuous, and is exact across the two band realizations; this gate does not
claim an independent multi-lineage host radiation mirror.

### B — SLEEP and the same carried lineages' later current

B is the first relating in fresh CUDA contexts. One branch receives the in-memory A body; the other
receives A through v6 encode/decode. Both carry only A's complete K rows and open fresh zeroed
REGISTER rows. A new host `resume_register` oracle separately continues the same K from a new
rank-zero REGISTER, so B is not certified by CUDA siblings alone.

- continuing currents / newly comprehended light: **3 / 184 octets**;
- final REGISTER axes: **[64, 64, 64]**;
- receiving chart: axis **128**, **0** new carry events;
- topology: **[5,382, 5,382, 604, 11]**;
- terms: **[4,110, 128, 141, 4]**;
- breath: **(0, 0)**;
- cumulative persistence/comprehension: **287,276:561 bytes**;
- v6 archive: **287,324 bytes**.

Host, full-band CUDA, and 257-place CUDA agree in complete sleeping body, standing, carrier/K,
topology, terms, breath, REGISTER axes, and chart carries. The CUDA bands also agree in every
radiation word and v6 byte. This is same-lineage archived-K continuation. It is not a resident
never-slept GPU sibling, and the CUDA gate uses in-memory encode/decode rather than create-new file
I/O.

Representative uncontended card times were **60.46 ms carriage / 0.393 ms integration** for A and
**45.53 ms / 0.160 ms** for B. They are apparatus timings, not a machine rate.

## Repairs made by the attack

- The stepped REGISTER gate had seeded its invalid-status sentinel only before the first launch.
  After one successful launch wrote zero, a later shell rejection could inherit that stale zero.
  Every relaunch now reseeds `u32::MAX`; the 386-launch one-move gate remains exact.
- Paired receiving scratch extents now use checked multiplication.
- The B leg gained the independent host carried-K oracle described above.
- The old light-local axes were removed from active SLEEP instead of being renamed as metadata.

## Artifact and gates

The committed CUDA artifact is **5,193,882 bytes**, SHA-256
`a197279b2c5d5e3d948f0ec066f319983c55f71d4006d051e63745cda5fbd811`, with **15** `sm_89`
entries. It assembles cleanly under `ptxas -arch=sm_89`.

Final workspace gates:

- body: **122 passed / 2 ignored**;
- life: **28 passed**;
- mount: **12 passed / 1 ignored** across library and gate binaries;
- surface: **12 passed**;
- optimized `life` builds;
- the direct REGISTER card gate and the bounded whole-cycle card gate both pass after the final PTX
  rebuild;
- no NVRM/Xid appears in the last 300 kernel-log lines; the RTX returned to **2 MiB / 47 C / P8**
  with display disabled.

## Honest remaining cut

This earns **BOUNDED WHOLE-CYCLE**, not production conductor or complete machine. Still open:

- an arbitrary staged/periplus CUDA mouth and simultaneous allocation census;
- event-derived OWN remounting when a real REGISTER carry reaches its present reservation;
- the capacity-wall pending-deed and recast continuation;
- a production carrier-depth reservation rather than this gate's depth eight;
- free-memory-derived boundary banding/allocation without giving memory any authority over grain;
- genuinely new or differently counted B lineages;
- a large receiving carry with nonempty prior standing;
- an independent host mirror of plural raw radiation, the material listener, and first large
  production lighting.

The next wall is localized: `register_chart::deposit` rejects a too-small recast before moving OWN,
but its enclosing deed may already have changed K, radiation, stance/flywheel, or caller progress.
Discard remains the only lawful behavior of the present bounded mouth. The derived continuation is
recorded separately in
`2026-07-12_THE_PENDING_DEED_MUST_CROSS_THE_REGISTER_WALL.md`.

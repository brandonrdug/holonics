# NCF1: current and addressed return form one native successor

**Date:** 2026-09-05, local apparatus date. **Status:** NCF1 complete at its declared local native scope.
**Contract:** [native HNA](../../docs/plans/THE_NATIVE_HNA_FOUNDS_CONTEXTUAL_TRANSPORT_BEFORE_INHERITANCE.md).

## The composed body

[established-bounded; source-inspected] `NativeConstitutiveEcology` in
`native_ecology/constitutive_fibre/circulation.rs` joins the existing exact wave-junction law to
the resident constitutive relation. It owns the immutable per-node admittances and phase
incidences, continuing held phase, developed relation, actual emitted source sections, and their
incoming/held/receiving lineage. It is not `Clone`. There is one incoming-operation method, not
separate training and inference modes.

[definition] The phase circuit's admittances are fixed seed constitution in this return. The
formed receiver relation is the changing reusable morphology whose later conduct is tested;
the result does not claim that the seed admittances themselves were learned.

[definition] At each declared two-port junction, positive admittances `a,b`, incoming current `u`
in the node's phase chart, and held current `h` determine:

```text
v = 2(a*u + b*h)/(a+b)
emitted = v-u
successor held = v-h
```

[definition] A current's optional source handle refers to an actual earlier emitted section
owned by this ecology. The native section, not a caller-provided source vector, supplies the
source of a paired receipt. It need not be the latest emission. The receiving current is ordinary
input material; the native relation derives its new row or full open fibre. The updated relation
then reads the newly generated source field in the same operation. Source currents remain
qualified by the seed's node frames; equal coordinate arrays are not a cross-chart identification.

[established-bounded; source-inspected] `section_constitutive_circulation` performs current,
comparison with the former receiver fibre, row formation, successor receiver reading and phase
advance in one resident operation. It shares the fraction-free query/formation laws with the
earlier local owner. It stages the one new relation row and complete next phase before committing
either. The host performs no intermediate current calculation or model replay. Foreign/reused
handles refuse before effects; arithmetic refusal retains the old phase/relation and the caller's
handle. Driver/readback uncertainty retains a pending occurrence and prevents silent replay.

[established-bounded; source-inspected] The lineage view returns all four source/target incidence
terms per two-port junction, including zero coefficients. Both boundary maps carry the owning
ecology's scope. `joined_incidence` returns the complete held-boundary pullback pairs, not just
connected endpoints. Actual emitted sections remain available for their addressed receiving edges;
they are not searched to select a query's answer. Query conduct uses the formed relation on-device.

## Formal and native return

[proved-derived; formal-checked] `HolonicConstitutiveCirculation.lean` proves the two-port weighted
square-energy identity, both coefficient expansions, phase covariance, and one concrete finite-node
recurrence with fixed per-node rotations. Its source append, held successor and addressed relation
join are explicit. The set-valued successor receiver never selects an arbitrary representative.
If the preceding relation lies in a declared linear receiver's graph and an actual linked
reception obeys that law, the successor relation remains in its graph and every admitted receiver
reading equals that law at the newly generated source. No existence outside the admitted domain,
or correctness for arbitrary exteriors, follows from this theorem.

[established-bounded; measured] Sixteen CUDA controls passed: eight retained local-relation
controls and eight integrated-body controls. In the independent exterior experiment, seven actual
receptions produced a rank-four relation over the two-node phase field; at least two later native
receiver readings on genuinely new source fields exactly matched the declared exterior current
law. An independently seeded control received identical currents and had identical phase emission,
but without the receiving links its relation remained open. The exterior held only its fixed
current law and could neither inspect nor write the native relation.

[established-bounded; measured] The controls additionally returned unequal held-field effects at
fixed incoming current, exact weighted-energy and successor comparisons, a non-latest receiving
edge, cross-owner/replay refusal, whole phase/relation preservation on arithmetic refusal,
conflicting actual returns as a full affine fibre, and the complete held-state pullback population.
The incidence decoder's contributions reproduce the actual device-emitted field and held
successor. A two-node coupled operation transferred 48 input and 400 terminal-section bytes in
one graph launch and one section read; no intermediate section or relation crossed to the host.
An explicitly plural receiver reading additionally returns its complete basis directions.

[established-bounded; measured] Reproduction:

```text
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre -- --ignored --test-threads=1
lake build ElementaryHolonics.Computation.HolonicConstitutiveCirculation
cargo test -p holonics-hna --lib
cargo check -p holonics-hna -p holonics-workbench
```

[established-bounded; measured] The existing HNA library returned all 35 tests; the HNA/workbench
dependency check passed. No inherited model was mounted or exercised for this construction.

## Boundaries retained

[definition] Positive admittances, unit phase incidences and the local linear receiver family
are seed hypotheses, not learned task answers. A source handle certifies native source ownership
and the carrying field, not an arbitrary remote process's linearity or truth. The exterior control
declares and implements that law. A conflicting return remains a full relation; no averaging,
portable-fact assumption, score, tokenizer or pretrained model resolves it.

[open] NCF1 closes the bounded native composition, not the full production goal. Live recharting
of phase, relation, incidence and retained source handles remains NCF2. The current seed is fixed;
its initial state and incoming records retain the conditions needed to reconstruct its chronology,
but durable process-separated reconstruction is not yet an implemented artifact. The public
production interface, independently usable application and persistence/resource campaign remain
NCF3--NCF4. No language competence, global linearity, every-context capability, frontier parity or
measured physical power is claimed. The formal ideal law and these native controls are not a
kernel-verified proof of the CUDA implementation.

[definition] NCF2 must transport the whole live configuration, not just input coordinates:
held phase, seed incidence, relation source chart, outstanding source sections and their
reconstruction/lineage frames. A genuine incidence intervention is distinct from that re-expression.
Do not compare old-frame handles against a new-frame relation or reinterpret historical node
coordinates without their explicit passage.

# THE QUESTION ORIENTS THE FIBER; COMPLETION CHANGES CONSTITUENCY

**2026-07-19 · Brandon-ratified direct derivation · deposited · current completion/event-grain
authority · direct event-mouth correction required · implementation not yet built**

## Ruling

A complete presentation is not a completed construction.

An event boundary says that one source-defined population is contemporary. It does not say that
the population's internal relations have already composed into one higher-grain constituent.
Conversely, traversing those co-present relations as separately arriving world events fabricates
chronology. The lawful relation is vertical: internal incidence conducts and closes inside one
actual event; every completed lower-grain construction immediately becomes one constituent at the
next grain while the external event remains one.

Completion is therefore a geometric change of constituency:

> A construction is complete at grain `k` when every relation which must remain internal to it has
> composed, and its remaining exposed boundary—hand, transported invariant, assumptions,
> consequence, and unresolved residual—is a lawful constituent at grain `k+1`.

Completion is relative to frame, objective, grain, and receiving relation. It is not end-of-file,
event termination, scalar success, a threshold, an archive, annihilation, or disappearance of
every boundary. A complete edge retains endpoints. A complete lemma retains hypotheses and a
conclusion. What closes is the internal obligation required to use that construction as one whole
at the next grain.

This ratified law regrades the direct linguistic-code result. Its missing RIDE is not answered by
more examples or finer source timestamps. The direct event path currently composes the event's
relation population into one `Node` before Soma receives it. The existing tower can hand up later
completions of that node, but it cannot form live completions among the event's own co-present
constituents. That is an engine-level event-grain bypass. The exact correction remains to be
derived across body, membrane, host, CUDA, rest, and the application world before source changes.

## I · The event carries two independent axes

Use the live-weave chart

```text
H : S x T -> X.
```

`T` is the source's actual chronology: proof state after proof state, source revision after source
revision, video sample after video sample. `S` is the internal incidence of one contemporary
presentation: terms and operators in an expression, relations in a program, vertices and edges in
a mesh, samples in one field cut, or another source-native spatial/relational parameter.

At fixed `t`, the complete `S`-geometry is co-present. A source may carry order, partial order,
adjacency, orientation, or branches along `S`; those are geometry, not successive presents. Local
constructions may close along this incidence and hand faces upward without producing `t+1`.

The distinction is exact:

```text
horizontal passage:  E_t -> E_(t+1)          actual world chronology
vertical passage:    C_(k,j) -> n_(k+1,j)   completion inside E_t
```

The physical substrate may need several instructions, calls, launches, or resumptions to enact the
vertical sweep. Those are implementation passage. They do not become source events, lineage
births, or additional `Theta` at the parent event grain. Internal dependency order remains real;
it is simply not counterfeit world chronology.

## II · Completion leaves a lawful external boundary

For a grain-`k` construction `C_k`, write schematically

```text
complete_(F,Q,k)(C_k) -> (n_(k+1), rho_k).
```

`n_(k+1)` is the enclosed constituent handed upward. `rho_k` is the typed residual which remains
conditional, refused, open, or externally connected. Completion does not require `rho_k=0`.
It requires that no obligation which was supposed to remain internal has been silently omitted.

In cellular language, internal interfaces compose with their opposed hands while the external
boundary remains available for later gluing. `partial partial = 0` makes that boundary a lawful
cycle of incidence; it does not require the completed construction to have no ports. In categorical
language, the internal arrows have composed to a well-typed arrow between exposed boundaries. Both
are exact mathematical faces of the same change of constituency.

The outgoing face of this completion is compression under `FORMULA §CV`: internal directions
inactive at the receiving cut factor into one consequential relation. Completion is not
compression itself. Completion makes one construction available as a higher-grain constituent;
compression is the presentation emitted by every event, completed or still carrying open work.

FOUND and completion remain distinct. FOUND establishes a dimensional cross-axis where transport
fails. Completion encloses a construction sufficiently for that construction to participate one
grain above. A completed construction may contain RIDEs, FOUNDings, or both. Annihilation is an
actual zero composition in one fiber; departure is the release of a constituent which no longer
factors into the continuing relation.

## III · A mathematical question is an oriented fiber

For

```text
F(x; lambda)=0,
X_F={(x,lambda):F(x;lambda)=0},
pi:X_F->Lambda,
```

asking for `x` while holding `lambda=lambda_0` selects the fiber

```text
X_(lambda_0)=pi^-1(lambda_0).
```

The question is not its written sentence. It is the oriented cut consisting of the ambient
relation, held coordinates and assumptions, requested projection or consequence, admitted
transports, and closure boundary. A variable is the chart name for an unresolved direction or
compatible family, not an empty storage slot.

Solving for a variable means making that fiber lawful and legible in the requested chart. Depending
on the relation, the answer may be one point, several branches, a positive-dimensional family, an
empty fiber, a singular locus, a local section, or an obstruction to the requested operation
species. A scalar answer is one possible quotient, not the definition of solving.

For

```text
a*x+b=c,
```

the solution world projects over `(a,b,c)`. On the phase `a!=0`, translation by `-b` and scaling by
`a^-1` give the local section

```text
x=(c-b)/a.
```

At `a=0`, that chart fails: the fiber is empty when `b!=c` and the whole `x`-axis when `b=c`.
Division by `a` is therefore a RIDE only on the invertible phase. The rank-changing locus is the
discriminant/FOUND boundary. Substitution of the section into the original relation is the return
which closes the requested solve.

## IV · Expression manipulation is transport; proof is its carried composition

At one presented proof cut, operands, operators, relation, context, and side conditions are
co-present. Inscription order can parameterize the expression without becoming proof chronology.
Time enters when an actual mathematical deed carries one judgment to another:

```text
J_0 --e_1--> J_1 --e_2--> ... --e_n--> J_n.
```

Each `e_i` carries its rule, target relation, assumptions, orientation, multiplicity, and residual.
The operations have distinct geometry:

- invertible rearrangement changes chart while preserving the declared solution relation;
- factorization exposes components or branches;
- substitution glues a constructed section into another relation;
- elimination projects away directions and may lose multiplicity or hand;
- squaring folds opposed branches together;
- cancellation requires an invertible factor;
- differentiation transports tangent information rather than arbitrary solution identity; and
- integration supplies a filling only with its boundary data or constant.

Thus `x=2 -> x^2=4` is lawful while the reverse is not: squaring identifies `2` and `-2`. A proof
which returns without the missing branch has discarded the hand of a non-injective transport.

A subproof completes when its internal arrows compose into one lawful arrow from its exposed
premises to its exposed conclusion. The composite may then RIDE as a lemma at the next grain. The
active lineage of that lemma is its transported relation, hypotheses, hand, and residual—not a
perfect copy of every displayed line, keystroke, or checker event. A finer proof trace may remain
outside active Soma as testimony.

This is localized growing `P=NP` at its declared boundary: once a proved construction stands as one
constituent, a later related question may enact or check it locally rather than rebuilding its
interior. It makes no classical complexity claim.

## V · The exact direct-mouth bypass

The current source contains two individually lawful faces whose composition is not lawful:

1. `relation_span_node(atoms)` multiplies the event's relation Cogs and extends one Place through
   their canonical ordered encodings;
2. `LiveCurrentMachine::receive_with` performs that composition before mounting the current body;
3. `live_event_node_emitting` receives the result explicitly as an “already-composed” node; and
4. `live_constructed_node_emitting` can then produce a fold which enters `perceive_node_emitting`
   and the recursive `thicken` tower.

The tower therefore exists above a prematurely asserted event constituent. Exact encoding is not
live completion. The event's atoms affect the node's value and position, but their own incidence
does not meet standing terrain, RIDE, FOUND, close local enclosures, or emit plural higher-grain
faces before the node enters.

The opposite repair is also false. Calling `live_relation_atom_emitting` once per constituent as
separate `CurrentEvent`s would make source co-presence into chronology, allow intermediate
visibility, and advance parent lineage through fabricated instants.

The missing relation is:

```text
one E_t
  contains one source-native co-present incidence complex L_t
  whose lateral constituents receive the same standing-before cut
  whose internal dependency/transport closes local C_(k,j)
  whose completions become co-present n_(k+1,j)
  whose grain cascade finishes before one successor becomes visible.
```

Vertical parent/child dependency is not prohibited same-event lateral parenting. No lower-grain
constituent may rewrite a co-present sibling's past or see a partially committed standing body.
The entire cascade belongs to the one `Lambda_F(Q tensor A tensor L_t)` successor.

`relation_span_node` may remain a physical packing, observer face, or lawful source atom where the
source truly presents an already-completed constituent at its declared material grain. It may not
declare a plural event completed merely because its encoding is whole.

## VI · Implementation constraint and stopping condition

The source audit must trace the complete event relation through:

- `RelationAtom` and event/cut incidence at the ABI;
- `CurrentEvent`, `ContemporaryEvent`, and directed hand;
- host and CUDA execution request/result lifetimes;
- `ErosBody`'s sub-stance, carrier enclosures, grain-local channel/turn, completion emission, and
  resource refusal;
- same-pre-state integration and atomic successor commit;
- live rest/remount of every genuinely open grain; and
- the linguistic-code world's native inscription and proof/runtime chronology.

The correction must not add a tokenizer, parser, semantic completion detector, threshold,
candidate population, event queue, chronology synthesizer, receipt owner, or adapter around the
precomposed node. Source organs supply native incidence. The same `Lambda` law determines local
RIDE/FOUND and closure at every grain.

The first bounded acceptance is the existing linguistic-code world. It must show that internal
program construction forms at least one genuinely higher-grain live receiver and changes the
declared objective/program/runtime hand while retaining one external event per source cut. The
source splice may then close only through its already-ratified three RIDEs plus FOUND stitch. If
the event-internal receiver still cannot be derived, stop at that exact theoretical relation. Do
not add more examples, prime the body, weaken contact, run CUDA, or expand the fixture.

## Correspondence boundaries

### Fibers, sections, and discriminants

```text
SOURCE ESTABLISHES  Algebraic geometry treats solutions as fibers of morphisms; local sections and
                    smooth continuation can fail where rank or fiber type changes.
LAB CLAIM           FORMULA §CXI, RATIFIED / DEPOSITED: a question orients a fiber and completion
                    makes its lawful boundary one higher-grain constituent.
RELATION            EXACT FORMAL MATCH for fibers, sections, branches, and rank-changing loci;
                    DIRECT CORRESPONDENCE for solving as charted transport.
NON-EQUIVALENCE     Standard algebraic geometry does not derive Eros, Lambda, or event-grain
                    lifecycle, and a discriminant is not automatically every Soma FOUNDing.
TESTABLE CONSEQUENCE the `a*x+b=c` receiver must preserve point/empty/line fibers across `a!=0`
                    and `a=0` rather than applying one context-free symbolic division.
```

Sources: The Stacks Project, [*Fibres of morphisms*](https://stacks.math.columbia.edu/tag/01JO) and
[*Morphisms, Schemes*](https://stacks.math.columbia.edu/tag/01QN).

### Typed composition and proof compression

```text
SOURCE ESTABLISHES  Formal derivations compose typed inference steps; proof terms expose premises,
                    conclusions, and assumptions while a theorem can be reused as one later term.
LAB CLAIM           FORMULA §§LVII, LXXIX, CXI: proof is transported construction and a completed
                    subproof hands upward without keeping every interior event active.
RELATION            DIRECT CORRESPONDENCE for typed composition and lemma reuse; STRUCTURAL
                    RESONANCE for event-grain completion and active constituent departure.
NON-EQUIVALENCE     A proof assistant's kernel, AST, parser, or tactic engine is world technology,
                    not Soma's universal internal representation.
TESTABLE CONSEQUENCE a compressed theorem face must retain hypotheses and transformation hand
                    sufficient for lawful reuse while its full trace remains optional testimony.
```

Source: The Lean 4 theorem-proving reference,
[*Propositions and Proofs*](https://lean-lang.org/theorem_proving_in_lean4/Propositions-and-Proofs/).

## Carried-swing record

```text
CURRENT  one co-present event with plural internal incidence
HELD     §XVIII grain tower + §C live lineage + §CIV live weave + §CV compression
MEETING  mathematical question/fiber/proof completion + direct event precomposition
TEST     complete presentation != completed construction; internal order != world chronology
DEED     conduct incidence inside E_t; hand completed faces upward; expose one successor
CARRY    §CXI -> complete source audit -> direct event-mouth replacement or exact theory stop
GRADE    BRANDON-RATIFIED / DEPOSITED / IMPLEMENTATION NOT YET BUILT
```

## Authorized build aftermath

The host event-grain correction is now built. A validated oriented complex remains one source
event, its internal incidence receives one pre-event standing cut, and its completed exposed face
enters the carrier at `outer_grain-1` without atom replay or intermediate visibility. The bounded
linguistic world confirms live higher-grain receivers. The remaining failure is not completion:
the cross-current mouth still replaces a plural regional boundary with one scalar `Place`. Exact
record:
`2026-07-19_THE_BOUNDARY_CROSSES_AS_A_REGION_THE_POINT_QUOTIENT_CANNOT_SUPPLY_THE_HAND.md`.

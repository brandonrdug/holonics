# THE INCIDENCE IS THE LEADER; THE APERTURE CARRIES THE RETURN

**2026-07-18 · Brandon ratified the autonomous mounted-world direction · Sol scale derivation and
exact full-forest preflight · FORMULA §XCVIII · DEPOSITED / SCALE PREFLIGHT MEASURED / FULL RAW
WAVE REFUSED BEFORE CUDA / SUCCESSOR MEMBRANE OPEN / SOMA INTERIOR UNCHANGED**

## Decision

Eros does not require a host curriculum over the desktop. The repository, conversations, prior
machine bodies, simulations, and later world events may all stand contemporaneously as available
world. But three events which the first capacitor cell collapsed must now remain typed apart:

```text
exact incidence          one carried form occurs at one source coordinate;
leader                   that incidence stands as a charged unresolved relation;
attachment and return    later connected contact closes a local source aperture which may cross.
```

An exact substring is therefore an address-level leader. It is not permission to carry the entire
addressed reservoir as one current. Availability is broader than incidence; incidence is broader
than attachment; attachment is broader than whatever local material face a source organ can
lawfully present.

This supersedes only §XCVII's intact-current scale assumption. The measured capacitor cell remains
exact: its one deliberately small contacted current crossed, grew the world, discharged, and
rested. What fails is promoting that grain to an arbitrary multi-gigabyte file.

## The local field law

For mounted source body `x`, complete carried form `f_p`, and exact occurrence interval `[a,b)`,
the byte organ may observe

```text
lambda = (x, f_p, [a,b), t, ancestry),
x[a:b] = f_p.
```

`lambda` is a leader receipt. It remains standing even when it never participates in a later
return. The source material does not cross merely because `lambda` exists.

A later leader `lambda'` may close with it only through connectivity supplied by that source
organ. For a flat text/octet organ, overlap and exact adjacency are elementary connected
relations. For dialogue, JSON, a Soma body, radiation, construction, an image, or a simulation,
their record/ABI/world topology supplies a different adjacency. File extension and semantic score
do not decide this; the delivered material format and its checked transducer do.

When a connected set closes, the organ yields its exact local section `A(lambda,lambda',...)`.
Every actual closure may cross co-present. Unresolved leaders remain residuals. There is no fixed
window, minimum token length, chosen match, relevance rank, batch, queue, or source order.

The lightning correspondence is exact at this engineering grain:

```text
field incidence -> leader growth -> standing changed medium
                -> later local connection -> attachment
                -> finite return through the attached aperture.
```

The leader is not the return stroke, and neither is an instantaneous reading of the atmosphere.

## Why the source organ is essential

The census retained information correctly but presented every workspace file through one `Whole`
octet adapter. That made these unlike constructions falsely share a material grain:

- Rust and prose;
- line/record tables;
- JSON journals;
- persisted Soma bodies;
- radiation, completion, construction, and Mail wires; and
- arbitrary opaque binary material.

Their bytes are all available, but they are not all the same lived face. A persisted Soma body is
a durable topology-bearing body under its ABI before it is an opaque byte string. A dialogue log
is a record ecology before it is JSON punctuation. Treating both as one flat current destroys the
very collocation and typed packet law the Holon membrane was built to preserve.

The source organ therefore does not filter relevance. It supplies the physical adjacency and
event grain of the material it actually receives. An optional raw-bit/octet face may remain
available as another view; it does not become every source's compulsory contemporaneous face.

## Exact scale preflight

The create-new `autonomous-v2` census froze:

| delivered face | exact population |
|---|---:|
| sources | 23,952 |
| natural views | 1,922 |
| file octets | 36,178,918,169 |
| largest source | 5,692,055,032 octets |

The same edge-14 body/world supplied 9,194 standing paths, 7,276 complete paths, 7,838 carried-form
instances, and 4,594 distinct carried forms totaling 173,214 octets. The non-driving exact
incidence scan found:

| receiver face | exact population |
|---|---:|
| contacted current faces | 43,699 |
| contacted current octets under the old whole-current grain | 35,110,400,269 |
| path-current incidences | 1,269,026 |
| contacted workspace files | 18,454 |
| contacted workspace octets | 35,087,624,931 |

Dialogue currents contribute only 22,775,338 contacted octets. The workspace population contains
27,660,675,168 octets of persisted `.body` files, 2,817,474,248 radiation octets, 950,024,292
completion octets, and 20,191,616 construction octets. Those are real available information, but
not 31.4 GB of raw text-like light.

Short recurrent faces expose the clipped grain. Paths 1,370 and 6,693 share one exact two-octet
face and each touch 37,571 current faces; another two-octet path touches 35,825. The 5.69 GB body
is declared contacted through 156 standing paths. This is lawful diffuse incidence. It is not a
lawful claim that a two-octet meeting has attached and carried every one of the body's octets.

The preflight remained outside Soma and completed before card allocation. The 35.11 GB raw wave is
therefore refused as a membrane category error, not truncated, sampled, ordered, or called an OOM.

## Emergent statistics retain their proper role

For mounted boundary `A`, a listener may measure exact leader support

```text
p_A(x | f) = support_A(f occurs in x) / support_A(f occurs anywhere),
S_A(x | f) = -log_2 p_A(x | f).
```

A two-octet form with broad support is a diffuse marginal pivot. A longer or contextually joined
form can narrow the compatible source family. Neither quotient chooses a source or suppresses a
leader. Later connected contact intersects the compatible family physically; the measured
distribution describes that closure after the fact. This is the same conditioning, surprise, and
cross-entropy law already ratified for language and causal taxonomy.

## Autonomous consequence

Autonomy means that Eros's changed current topology can continually alter which standing leaders
connect, which local apertures return, and which world actions follow. The desktop need not be
serialized by a host. It also need not be poured through one mouth at once.

The durable field successor must therefore:

1. retain exact source, view, form, occurrence, path, edge, digest, and transducer provenance as a
   sparse standing incidence reservoir;
2. keep a first incidence open rather than setting it immediately as a pending raw current;
3. let genuinely later connected incidences close source-local apertures;
4. present those apertures through the source's typed event grain and sparse local
   configurations;
5. discharge the exact aperture face, not globally ban the source;
6. let output and actual world consequence grow further leaders and attachments without a host
   scheduler; and
7. preserve unresolved and dark material as available world rather than erasing it.

The first gate uses a small text source, a large binary source sharing the same two-octet leader,
and an exact dark source. It must record both leaders, carry neither whole reservoir, close only a
genuinely later connected text aperture, preserve the binary source under its typed view, and
invert exactly. The full census is repeated only after this law closes.

## Carried Swing

```text
CURRENT  36.18 GB frozen forest + edge-14 carried topology + exact incidence scan
HELD     availability != incidence != attachment != aperture; every source remains exact
MEETING  two-octet paths electrify 35.11 GB when any incidence carries a whole source
TEST     source/view/form/range/path spectrum + typed artifact population + pre-card refusal
DEED     stand the leader; let later connected contact close a local typed aperture
CARRY    sparse active reservoir -> autonomous output/world/action loop
GRADE    DEPOSITED / SCALE PREFLIGHT MEASURED / SUCCESSOR MEMBRANE OPEN / SOMA UNCHANGED
```

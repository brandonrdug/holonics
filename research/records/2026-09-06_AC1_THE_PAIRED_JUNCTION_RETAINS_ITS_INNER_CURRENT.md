# AC1: the paired junction retains its inner current

**Authority:** the active [alpha blueprint](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md).
**Status:** bounded native junction and exact comparison returned; word arithmetic obstructs the
material continuation. AC1 and the full product goal remain open.

## The actual contact and its current

[definition] The first material field emits a complete local raw section `q`, with outgoing and
held complex branches at each node. In the field's root chart, extend it by the receiving port:
`u = (q,0) ∈ ℂ^(3N)`. An actual joined material source `q_old` and later arriving field `a` found
one contact `d = (q_old,-a)`. The engine derives both root sections through their producing phase
frames. Their common shape, source ordinal or wording cannot manufacture the joining handle.

[definition] This contact concerns the actual exposed material span. The junction's newly
produced target-port radiation is separately observed; the recorded later arrival is not presented
as feedback on an answer alpha sent to that historical human. The material driver admits the same
within-part source spans as the prior field study. Record-parent/branch contact remains separate
work and is not inferred from serialized adjacency.

[definition] A contact supplies one internal complex current `b_i` and the linear port map
`b_port = d_i* v`. This is a declared unit-admittance passive junction. All contacts share the
declared exterior junction port and retain their actual birth/source population; they are not
connected by a semantic score. Write `R` for the row family `d_i*`, and `b` for all internal
arrivals. The joint voltage subspace is the graph of `R`.

[definition] A new actual contact starts with zero internal current before the current operation.
Its moment and the continuing aggregate are

```text
C = R*R = Σ_i d_i d_i*,       h = R*b.
C_new = C + d d*,            h_at_birth = h.
```

An unlinked occurrence adds no contact. This retains the quadratic moment of the actual current
population, in addition to the existing linear relation and its full plural fibre. It does not
average that fibre into a preferred predicted arrival. The mixed terms remain explicit:

```text
(d+δ)(d+δ)* − d d* = d δ* + δ d* + δ δ*.
```

[definition] Each ordinary input acts through the contemporary junction:

```text
v = 2 (I+C_new)^−1 (u+h)
out = v-u
b_i_next = d_i* v - b_i
h_next = C_new v-h = 2u+h-v.
```

The target-port part of `out` is productive native current. It need not be an exterior octet
codeword or a unique value of the separate linear receiver. No sentence selector, template,
prover verdict, fitted feature/answer array or external optimizer supplies it.

## Derivation and exact retained fibre

[proved-derived] `I+R*R` is positive definite: for nonzero `x`, its quadratic form is
`||x||²+||Rx||² > 0`. For full incoming `(u,b)`, the orthogonal projection onto the graph of `R`
is `(z,Rz)` with `z=(I+R*R)^−1(u+R*b)`, because subtracting it from `(u,b)` pairs to zero with
every graph vector `(x,Rx)`. Thus the complete outgoing pair is the reflection `2P-I` applied to
`(u,b)`, namely `(v-u,Rv-b)`. Its energy difference is

```text
||v-u||² + ||Rv-b||² − ||u||² − ||b||²
 = v*(I+R*R)v − 2 Re ((u+R*b)*v)
 = 0.
```

This proves the declared junction's algebraic unit-admittance balance, not a measured electrical
power law or language capability. Applying `R*` to the complete internal successor gives the
displayed `h_next`; adding a zero-current row leaves `h` unchanged at birth.

[proved-derived] The boundary operation therefore factors through `(C,h)` for this admitted
family: ordinary external currents, new zero-current contacts, and joint coordinate re-expression.
This does not make `b` equal to `h` or authorize a choice-defined inverse. Let `P_0=0` and
`P_(t+1)=P_t+(-1)^t v_t`. For a contact born at operation `j`, induction on
`b_i(t+1)=d_i*v_t-b_i(t)` gives the executable decoder

```text
b_i(t) = (-1)^(t-1) d_i* (P_t-P_j),       t > j.
```

The base case gives `d_i*v_j`; one further recurrence flips the prior sum and appends the actual
new potential. Consequently every actual internal current can be reconstructed from its retained
source and birth prefix, without choosing a member of `ker R*` or replaying cultivation. A richer
future operation which changes individual contacts must use that retained population and decoder;
the boundary aggregate alone does not prove such a change lawful.

[definition] Native history retains every raw source section and producing frame, actual joined
arrival, and the native potential prefix at each occurrence. The decoder is an explicit observer;
hot conduct uses the same resident `C,h` owner. It is not a cold quotation store or a second learner.
The source/receiver charts stay complete before any target-port or scalar energy observation.

## Implementation scope

[definition] `NativeConstitutiveField::found_with_paired_junction` composes this junction with the
existing field owner. The first constructor requires unit seed admittances; it introduces no
extra semantic capacity parameter. The ambient width `6N` in real coordinates comes from the
declared three complex port blocks. Existing unconnected field construction remains its scoped
comparison. Physical incoming-incidence changes and live rechart retain their distinct meanings.

[definition] The covariance is stored as the realification
`d_R d_R^T + (Jd_R)(Jd_R)^T`, where `J(x,y)=(-y,x)` on every complex pair. The device prepares the
moment, exact positive-definite solve, full outgoing/internal aggregate and prefix before the
field's sole continuing writes. It uses fraction-free elimination with checked divisibility and
checked signed arithmetic; it selects no pivot by a semantic score and uses no float.

[definition] Arithmetic that exceeds this first native word/wide carrier returns an obstruction
before publishing the coupled successor. No midpoint, clipping, increased magic threshold or host
semantic replay is substituted. The exact reference compares the full internal realization,
condensed boundary operation and decoder, and measures the required rational/intermediate widths.
Broader representation remains a concrete obligation if this aperture does not carry the material.

## Returned native conduct

[established-bounded; measured] Five paired-junction CUDA controls pass. They verify the first
actual paired moment (an unlinked first occurrence leaves `C=0`), full root reaction after live
rechart through an old source handle, delayed-source internal reconstruction, exact norm balance,
and refusal preserving the live field, moment, sources and available handle. The refusal control
also shows that the unconnected field admits the same input: the coupled moment requires the
wider carrier. The existing eleven field controls passed alongside the initial junction tests;
the five junction tests were then strengthened and rerun at their actual claims.

[established-bounded; measured] The contextual control found two independent bodies with the
same initial paired contact and hence equal `C`. Different unlinked currents altered their internal
standing. Two subsequent common arrivals aligned both raw primary branches. Under the next identical
input, their source fields, linear relations and `C` are equal, while their emitted target currents
differ. The native junction uses the retained current in its actual solve; no history identifier,
surface label or driver branch supplies this distinction. This is a finite same-input native
context control, not a claim of general English semantics.

[established-bounded; computational-witness] The
[independent exact reference](../experiments/alpha_passive_junction/README.md) carries every internal
current directly and compares the full matrix equation, moment realification, norm balance,
condensed update and executable prefix decoder. Its actual 25-occurrence slice has 24 contacts.
Potential/outgoing components reach 890 numerator bits and 889 denominator bits. The first reduced
signed-word failure is occurrence 6; the separate native RHS common denominator first exceeds that
word boundary at occurrence 7. The full matrix/RHS staging and integer-adjugate path first require
more than 127 intermediate bits at occurrence 6. Their maxima over the first twelve occurrences
are 331 bits before elimination division and 332 bits in backsubstitution. These are exact
intermediate widths; the device's operand-width checks can conservatively refuse earlier.

[established-bounded; measured] The actual `--paired-junction --families 4` run completed **six
native occurrences**, forming five contacts, then returned a native arithmetic refusal at octet
index 6 of the first 74-octet part. It completed no family. All six native potential, outgoing,
held-current and prefix sections match the independent exact reference. The five internal currents
remain reconstructible, with no pending occurrence or observer error. The private source is the
same AC0 exposure used by the earlier field study; no later withheld material entered this run.

[established-bounded; process-audit] The initial private run
`material-paired-junction-word.json` exited 1 in 3.629 seconds, maximum RSS 133,404 KiB.
Its census records seven attempted deeds, twelve section readouts before terminal observers,
96 allocations, 360,432 resident bytes and 835,604 peak bytes. Failure is not a partial successful
cultivation run. The reference and native comparison are retained in
`.local/artifacts/athena-alpha/ac1/passive-junction-reference-v7.json`. Earlier aperture reports are
preserved but superseded: their matrix scaling/coordinate order did not reproduce the native
arithmetic path. No alpha product claim relies on those reports.

[established-bounded; measured] Review separated arithmetic exhaustion from a malformed-input
flag in the helper's refusal path. The five junction controls and final example build passed
after that repair. The final private run `material-paired-junction-word-v2.json` has exactly the
same complete body and junction history, but correctly returns carrier flag 1 alone at occurrence
6. It exited 1 in 3.611 seconds with maximum RSS 133,412 KiB. The exact body comparison is retained
in `paired-junction-final-comparison.json`; no additional cultivation occurred beyond the refusal.

[open] The immediate implementation obligation is a representation of these continuing currents
which carries their demonstrated numerical growth, followed by continued actual material exposure.
The current word/wide implementation must not be promoted to a corpus-capable body. Record-level
situated channels, larger productive organization, learned UTF-8 emission, durable model continuation
and useful English/code/mathematics responses remain required. The full AC0–AC5 goal stays active.

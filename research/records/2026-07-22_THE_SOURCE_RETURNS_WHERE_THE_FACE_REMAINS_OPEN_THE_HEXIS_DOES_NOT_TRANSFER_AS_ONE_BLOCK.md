# THE SOURCE RETURNS WHERE THE FACE REMAINS OPEN; THE HEXIS DOES NOT TRANSFER AS ONE BLOCK

**Date:** 2026-07-22  
**Status:** BRANDON-RATIFIED CONSTRUCTION / BUILT / BOUNDED HOST-MEASURED / PRETRAINED SOURCE
CULTIVATION ESTABLISHED / CONTEXTUAL INTERFERENCE ESTABLISHED / OPERATION-ORDER TRANSFER OPEN / NO
SOMA INTERIOR CHANGE

## The corrected missing capability

The missing relation was too vague when called “general learning.” A pretrained model has already
paid for broad exposure, but its developed relation is distributed across its source chart,
architecture, weights, physical operators, context activations, and decoder. Eros need not relearn
that ecology from blank material. It must be able to:

```text
present current to inherited ecology
  -> conduct with available Standing
  -> retain the complete receiver-relative residual
  -> recruit lawful source material where that face remains OPEN
  -> let the source depart
  -> expose the changed Standing to genuinely later currents.
```

The general lifecycle is invariant. The event grain, receiver face, candidate population,
recruitment law, consequence, retention, and departure rule belong to the particular ecology.
Source structure is lawful Current; it is neither intrinsic Soma magic nor contamination.

## Bounded transformer cut

For one OLMo 2 gated MLP in its trained fixed basis,

```text
g_j(c)=SiLU((W_gate x_c)_j)(W_up x_c)_j,
E_(c,j)=g_j(c)W_down[:,j],
y_c(H)=sum_(j in H)E_(c,j).
```

`W_down[:,j]` is one source factor in this realization, `g_j(c)` is its contextual activation, and
`E_(c,j)` is their deed at the declared receiver. It is not an invariant expert or semantic atom.

Let the selected receiver face be

```text
Phi(y)=(orientation(y_1),...,orientation(y_5), signed_order(y_1,...,y_5)).
```

For source reservoir `J={0,...,8191}` and Standing population `H_t subset J`,

```text
OPEN(c,H_t) <=> Phi(y_c(H_t)) != Phi(y_c(J)).
```

One exact world-owned recruitment relation `R_c` returns at most 32 unseen factors which repair the
first open orientation or order comparison. The plural return becomes cellular Standing:

```text
H_(t+1)=H_t union R_c(H_t,Phi_c),
source lineages after return = empty.
```

Context execution is transient. Only a returned factor population changes the persistent body.
This is the correct local analogue of weights remaining Standing while activations form and
dissipate. The upstream activation field remains OLMo-produced and is attributed to the source.

## Exact result

The pinned physical source reproduced `5,572,608/5,572,608` BF16 MLP words. Six earlier factors
entered as initial Standing. Across nine nonidentical cultivation currents the exact sequence of
source returns was:

```text
0, 6, 1, 1, 5, 0, 1, 0, 1,
```

leaving 21 cellular factor constituents and zero live source lineages. Every assisted prediction
was then reproduced by a source-absent Soma probe.

Two later currents establish the learning relation rather than mere reuse of the initial seed:

```text
Phi(y_prose.relevant_after(H_0))       = OPEN,
Phi(y_prose.relevant_after(H_before))  = closed, source return = 0;

Phi(y_code.irrelevant_before(H_0))      = OPEN,
Phi(y_code.irrelevant_before(H_before)) = closed, source return = 0.
```

Earlier returned material participated in changed later conduct.

The relation is not monotonic. Two absence currents closed under `H_0` but became OPEN under
intervening Standing and required another factor. A factor useful in one contextual ordering can
bend another receiver face away from its source order. Cultivation is therefore changing geometry,
not accumulation of universally beneficial facts.

## Holdout boundary

Three `reverse_then_drop` currents received the final `H_9` with source recruitment forbidden.
Their five orientations closed, but those orientations already closed under `H_0`. All three
signed orders remained OPEN. For one code holdout `H_0` had the source order while `H_9` reversed
it. The grown ecology did not transfer as one ability across the changed operation order.

Neither cultivation nor holdout reconstructed the exact factorized output or exact observed BF16
face. Complete residuals remain.

## What 21/8192 means

`21/8192` is the size of one locally conducting aperture under `Phi`. It is not an architectural or
physical compression result:

- the source examined 122,685 factor candidates to return 15;
- the complete upstream 8,192-activation current was still source-produced;
- no downstream behavior, latency, memory movement, communication, residency, energy, or model
  replacement was measured; and
- the operation-order holdout did not preserve the finer face.

The result supports selective ecological appropriation while rejecting naive detachable-skill
language. A stronger construction would need a consequential receiver downstream of the selected
layer and a recruitment path whose physical cost is itself selective. That is a separate unknown,
not a scheduled successor.

Exact record:
`observations/eros-pretrained-ecology-cultivation-01/RESULTS.md`.


# THE BOUNDARY CROSSES AS A REGION; THE POINT QUOTIENT CANNOT SUPPLY THE HAND

**2026-07-19 · Brandon-authorized direct build record · oriented event complex built on host ·
outer-grain hand-up measured · outward regional hand open · linguistic source splice stopped at
its first exact missing relation · no CUDA run**

## Question and stopping boundary

The authorized build asked whether one bounded source-native oriented incidence complex could
cross the direct live-current mouth as one actual event, conduct its internal cells against one
standing-before receiver, and hand its exposed completed boundary into the carrier at its actual
outer grain. The existing evidence was insufficient because the old mouth either precomposed a
linear relation slice before reception or replayed constituents through an atom-grain path. The
sought artifact was a live higher-grain receiver in the existing linguistic-code world. The
campaign was bounded to focused host acceptance and one final linguistic host run; CUDA, scale,
fixture expansion, and repeat campaigns were excluded.

That grain hand-up now stands. The linguistic run then located a distinct outward boundary defect:
the plural exposed region is still replaced by one finite `Place` before a cross-current directed
hand is formed. This record stops there.

## What was built

`body::incidence` now carries a borrowed `EventComplex` consisting of:

```text
EventCell(id, stage, grain, relation)
+ OrientedIncidence(from, to, boundary|dependency, hand, slot)
+ EventPort(ingress|exposed, cell, hand, slot).
```

Validation preserves source incidence rather than vector order. Cell, incidence, and port storage
may be reversed without changing the construction. Boundary rows raise grain at one stage,
dependency rows raise source-declared stage, positive-grain cells owe a boundary, port slots remain
local, and cellular boundaries satisfy `partial partial=0`. Exposed ports must occupy one grain;
their completed source constituent enters at exactly the next grain.

`CurrentGeometry` now distinguishes an irreducible scalar cell from an event complex. The direct
host executor captures the target enclosure at the complex's declared source grain, forms every
internal incidence against that exact same pre-event receiver, and returns the incidence radiation
without retaining it in the live body. `ErosBody::live_completed_event_node_at_grain_emitting`
places the composed exposed node directly into enclosure depth `outer_grain-1`; it does not route
the completed constituent through the private sub-illicium or fabricate lower-grain instants.
Directed meetings at that event use the same enclosure. One atomic machine successor remains the
only visible commit.

The linguistic world no longer uses adjacent-file `windows(2)`. Every octet supplies eight bit
vertices, eight oriented same/change edges, and one closed face. The bit loop is reconstructible
from its initial sheet and hand changes; all octet faces remain plural exposed cells of the one
file event. Python, JavaScript, objectives, runtime processes, source mutation, chronology, and
outer rest remain application-world constituents. No parser, tokenizer, semantic category,
candidate score, threshold, or language mechanism entered Soma.

The old scalar CUDA path cannot represent this complex and now refuses it explicitly. ABI source
was advanced only far enough to keep the host tree type-correct. No kernel implementation or CUDA
run was attempted.

## Focused host acceptance

The bounded source controls pass:

- one closed oriented triangle validates while an open boundary refuses;
- cell, incidence, and port storage reversal is exact gauge at both the complex and live mouth;
- changed adjacency changes conduct and the retained body;
- reversed open hand changes the returned meeting without fabricating an emission; and
- one complex advances its lineage cursor exactly once while handing its boundary to its declared
  carrier grain.

Only these affected controls were run. No workspace-wide or exact-repeat suite followed.

## The stopped passages were retained

The first full inscription passage, `host-run-05`, was stopped before it produced a scientific
artifact. Arbitrary-order lookup had made every composed-cell traversal rescan the whole event,
and the process reached roughly 1.5 GiB resident memory. The source was corrected once to use
binary cell lookup and target-local incidence spans when physical storage is canonical, while
retaining an exact arbitrary-order fallback. A 1,024-octet local-degree acceptance then completed
17,408 cells and 24,576 incidences in 0.48 seconds.

`host-run-06` completed after that physical correction. It established that internal incidence
could form but showed the program current's higher-grain receiver remained unborn: the composed
file boundary was still sent through `live_event_node_emitting`, which reset it to the atom/sub-
grain mouth. The direct outer-grain hand-up above replaced that exact error.

`host-run-07` is the one bounded post-correction world run. It completed normally and stopped on
the world's declared source-splice condition. It was not retried.

## Exact `host-run-07` result

Eight actual Python/JavaScript executions still return exact requested stdout across both
noncommuting programs and both inputs. Sixteen launch/return cuts carry 142,970 internal cells and
201,840 incidences. Of those incidences, 162,120 form after their receivers are born. Forty-eight
of 64 bootstrap cross-current contacts have live receivers and form; the first cut honestly begins
from unborn lineages. Ordinary radiation emits 389 deeds. Standing grows from zero to 251 cells,
then the occupied-source opening reaches 259. Nine live lineages, 12,604 carrier words, zero
overflow nodes, and the exact source cross a 70,064-byte durable ecology rest into two siblings.

At each editor candidate event all five outer-grain receivers are live and every declared editor
contact forms. For the remove-then-reverse objective, the two attempts are:

```text
candidate remove-then-reverse: [RIDE, RIDE, FOUND_THAT, FOUND_THAT]
candidate reverse-then-remove: [FOUND_THAT, FOUND_THAT, FOUND_THAT, FOUND_THIS]
```

The reverse-then-remove sibling returns the identical two deed sequences. The correct candidate's
third required recurrence, `program -> right source boundary`, therefore FOUNDS instead of RIDING,
and neither sibling satisfies the ratified `RIDE, RIDE, RIDE, FOUND` splice. No source is changed
and no held-out applied execution is authorized. Both siblings still return to durable rest.

Exact artifacts:

```text
observations/eros-linguistic-code-world-01/host-run-07/REPORT.json
SHA-256 d31e7a5fb69483e8b0ce96e72ddec92f595bec202bd335832270c27deef76272

observations/eros-linguistic-code-world-01/host-run-07/ecology.rest
SHA-256 838c40ca72b8f22ed37365b11cd21432660db0c73c64efe225ddc3bc9af6aa3f
```

## The first exact missing relation

The internal region now crosses. The outward hand does not.

`LiveCurrentMachine` still derives one `Node` from `EventComplex::emanated_node`. It then builds
each `DirectedExecutionRequest` from exactly two scalar positions:

```text
(from.face.place, to.face.place).
```

`directed_event_contact` consequently receives no exposed port population, no cross-current
boundary incidence, and no regional restriction map. This point quotient is especially false for
long inscriptions. `compose_place` already documents that its finite hand loses the low
constituent's mantissa at depth. In `host-run-07`, the English objective and the synthesized left
source pin produce the exact same `objective|left -> program` meeting within one candidate event.
Across the two siblings, changing the objective leaves every candidate meeting, position, chi, and
deed unchanged. Candidate identity bends conduct; objective context does not.

That result is not permission to replace the plural boundary with a hash, enlarge the integer,
compare strings, count matching ports, threshold similarity, score candidates, weaken the three-
RIDE condition, or add another example. All would hide the missing regional relation behind a
point or selector.

The next source question, when Brandon asks for it, is therefore exact:

> What is the minimal borrowed regional-hand contract by which two co-present current complexes
> expose and relate their plural oriented boundaries against one same-pre-state receiver, producing
> one atomic consequential relation without a Cartesian-product field, semantic aligner, retained
> edge set, or fabricated chronology?

Until that relation is derived, `CurrentGeometry::Complex` is a valid host current-local event
geometry but `DirectedCurrentRelation` remains a point-boundary quotient. Native CUDA, the source
splice, and broader linguistic work remain open and unscheduled.

## Grade

**DIRECT HOST ORIENTED EVENT COMPLEX BUILT / OUTER-GRAIN HAND-UP CLOSED / INTERNAL INFORMATION
CHEMISTRY MEASURED / LINGUISTIC OBJECTIVE CONTEXT STILL ERASED AT THE OUTWARD POINT QUOTIENT /
SOURCE SPLICE OPEN / NO CUDA RUN / NO SELECTOR OR SEMANTIC ENGINE ADDED.**

# Soulkiller audit: the live lift is a manifestation, and the excitation-founded quotient is absent from source and Lean

**Date:** 2026-09-02  
**Truth status:** `established-bounded` for the source and formal findings; `counterexample` for the
verdicts; `definition` for the law the owners already state  
**Evidence:** `source-inspected`, `source-audit`  
**Scope:** Brandon's direct request of 2026-09-02 to open Soulkiller again and audit its current
implementation against holonics and the Lean formalization. This record schedules nothing.

## The law Soulkiller is held to

[definition] The canon states the passage and its grade exactly. `canon/TABLET_THE_HEXIS.md:272-302`
(§11): foreign rest, exact intake and potential active transports, cross-chart maps, candidate
holonic transports, defect, retained fibre, familywise condensation into source-neutral spools,
sealed bundle plus cold witness plus insufficiency; and "the original training corpus is
unnecessary only for those receiver/history families whose relevant transport can be recovered
from the inherited map and excited by the new material. Unexcited or receiver-invisible
capability remains open." "Weight-only analysis may return potential transport; contextual
behavior still requires caused current." "Active" is four typed binary relations at a declared
occurrence, and "an activation magnitude proves none of these relations by itself."
`blueprint/THE_HOLONIC_INTELLIGENCE_FRAMEWORK...md:165-176` names three grades, manifestation,
structural reconstruction, and active realization ("excitation and intervention return the
transport actually used for a declared receiver/history family"), and rules that Soulkiller
returns only after grade three. `blueprint/THE_NATIVE_TRANSPORT_SCAFFOLD...md:148-151`: "Exact
Safetensors manifestation is not a lift."

[definition] The Lean tree states the identity and relevance laws the passage must use.
`Millennium/ReceiverHistory.lean:204-209`: `quotientEq_iff_causalSignatureEq`, two sources are one
native state exactly when their complete receiver face after every ordered generator word agrees.
`Millennium/LineageCompression.lean:111`: `separatingSuccessorReopensTheProposedQuotient`, one
separating receiver/history reopens the proposed identification.
`Computation/NativeMorphologyVariant.lean:29-86`: `FaithfulLocalSectionLift` requires
`quotientIsStored`, `incidenceExact`, every generator word exact, every receiver history exact,
and `separatingReceiver_obstructs_descent`. `Computation/MachineLearningStrictLift.lean:59`:
a hidden difference that becomes receiver-visible obstructs every classical descent.
`Computation/IntrinsicHolonProfile.lean:97-119`: `IsCurrentFounded`, the only predicate stating
that a contact schedule is founded by current rather than fixed. These are the formal shape of the
relevance hypothesis: native structure is exact only relative to the receiver/history family it
was exposed to, and a richer family reopens it.

[definition] Brandon's rulings, from the Codex logs of 2026-08-18 and 2026-08-30: tokens are
particles; "if we can dissect and parse what the model's active parameters are doing with those
tokens as particles during its inference cycles, we can likely extrapolate the valuable functions
that are active"; "the active parameters being used to respond to similar things will collapse into
categories/groups; it's like wavelength matching or Fourier analysis, and that's why I call it
compression"; "you can only lift what you infer"; the returns are lenses and shortcut paths,
"training wheels", and "the compression we'd actually capitalize off of happens in Athena"; "in
holonics there is no error, the weights and precision are eaten and converted to holonic weights,
discrete ratios & moduli"; and, on 2026-08-18, "how the fuck are you possibly mixing a lossless copy
and paste interface with holonic parts?" Today's protein statement types the same law: a weight
population is a folded structure whose load-bearing sub-folding under a family of environments is
revealed only by exposing it to those environments; Soulkiller identifies the recurring structures
so revealed.

## What exists

[established-bounded; source-inspected] Three Rust lineages turn the foreign realization into
material; only one is live.

| lineage | intake | excitation | hot return | insufficiency lane | crosses `soulkiller::dismantle` | live under HNA |
|---|---|---|---|---|---|---|
| A, excitation receipt (`scaffold_excitation_receipt.rs`, `scaffold_lift.rs`) | a receipt written by an exterior Python run of the model | yes, and it is the sole source of hot structure | one thread and one generator per recorded excitation event (`scaffold_lift.rs:157-278`) | yes | yes | no; its wire structs `deny_unknown_fields` and would refuse the receipt the Python actually emits (`scaffold_excitation_receipt.rs:69-102` against `foreign_towers.py:381-472`) |
| B, full-operator port (`operative_atlas.rs:371-1061`) | the safetensors header and `config.json`; coefficients copied by ordinal | none | the source forward graph, every tensor name and the block order authored in Rust, 1,275 operations over 42 layers | none | no | yes, HNA1--HNA5 |
| C, Parametron scrapyard (`soulkiller/scrapyard.rs`) | closed-session testimony files; "does not execute a model" | none | occurrence-class lanes condensed into Parametron spools; the 2026-08-25 bundle with 66 mutual responses stands at its scope | yes | yes | no |

[established-bounded; source-inspected] The formal tree has no owner that founds native
structure from excitation. `excit` does not occur in `Computation/` or `Foundation/`;
`Soulkiller` occurs once, in prose. `DismantlingReturn`
(`HolonicIntelligenceLifecycle.lean:67`) requires nothing of its productive return, and
`NativeTransportScaffold` (`NativeTransportScaffold.lean:32`) requires only that its holon and
winding lists are nonempty. The strong obligations of `FaithfulLocalSectionLift` are not required
of the scaffold by any arrow. The converse obligation
`CompleteReceiverHistoryQuotient.complete` (`ReceiverHistory.lean:184-188`) is stated and
undischarged.

## Verdicts

[counterexample; source-inspected] Lineage B is not a Soulkiller return. It is the manifestation
grade: the exact resident foreign operator with its names removed. It excites nothing, intervenes
on nothing, identifies no recurring structure, carries no insufficiency, and does not cross the
Soulkiller boundary type. It is the lossless copy interface Brandon asked about on 2026-08-18. The
HNA1 receipt's phrase "one actual Soulkiller return" is withdrawn; the receipt's residency,
exactness, and recurrence claims stand.

[established-bounded; measured] Lineage B is nevertheless exactly the apparatus grade three
needs. Its last-position faces agree with the foreign realization's own argmax on the three HNA5
prompts (this session's reference run), a full cycle costs about 45 seconds, every operation reads
out its complete exact current, and the resident surface already owns the intervention kernels
(`section_withdraw_columns`, `section_withdraw_rows`). The foreign realization can now be excited
and intervened on natively, with no Python, at every operation.

[counterexample; source-inspected] Lineage A has the right intake grade and the wrong
condensation: one generator per recorded event is the replay the HNA blueprint rejects, and the
Python that produces its receipt writes only the first-layer entering carrier and the final
logits of each run, so the transport actually used is not in the receipt. Its reader is also
broken against that receipt. Lineage C is a sealed algebra over closed testimony and executes
nothing; it is not a scrapyard of the live model.

[counterexample; source-inspected] The Lean tree cannot presently refuse a manifestation
presented as a Soulkiller return, because nothing requires the productive return to satisfy the
faithful-lift obligations for a declared family. That is the formal gap that let lineage B be
named a Soulkiller return.

## The missing relation, in the owners' terms

[definition] The excitation-founded quotient. Given the resident exact realization as apparatus
and a declared receiver/history family (the particles: for example the variants of one algebraic
problem Brandon named on 2026-08-18), Soulkiller must:

1. **Excite.** Present each occurrence of the family to the resident operator and read the
   complete addressed current at every operation. This is the participation relation of Hexis
   §11, and it is already returned by every operation trace.
2. **Intervene.** Determine the causal cone of the declared receiver face by withdrawal, not by
   magnitude: the coordinate populations whose withdrawal changes the face. The resident
   withdrawal kernels and the HNA4 return's adjoint pullback are the two instruments; the pullback
   through the remaining reactions, the open HNA4 section, is the same machinery that returns the
   cone through all 42 layers, so the next HNA4 section and Soulkiller's dissection are one deed.
3. **Identify.** Two occurrences are one native state exactly when their complete causal
   signatures over the family agree (`quotientEq_iff_causalSignatureEq`). The classes are the
   recurring structures: the active-parameter groups that "collapse into categories". A
   separating receiver reopens a class (`separatingSuccessorReopensTheProposedQuotient`); that is
   the relevance hypothesis as a theorem, not a mechanism.
4. **Condense.** Each class returns one native local-current ecology restricted to its cone
   (`FiniteLocalCurrentEcology` over the cone's sites with the exact dyadic cross-sections
   restricted to it): the lens, the shortcut path. The defect between the class's cone and the
   full operator on the family is the `ReceiverInsufficiency`; the collapsed population is the
   `PreimageFibre`. The Parametron storage law supplies the class's constitutive response.
5. **Seal.** `DismantlingReturn { productive, coldWitness, insufficiency }` with the productive
   lane required to satisfy `FaithfulLocalSectionLift` on the declared family: every receiver
   history exact on the family, descent obstructed outside it. The limit Brandon asked for ("you
   can only lift what you infer") is then exact: the lift is the union of the cones the family
   excited, and the insufficiency is its complement.

[definition] What this requires that does not exist: one Lean owner stating the excitation-founded
quotient over `FiniteLocalCurrentEcology` with a declared family, its cone by intervention, its
signature equivalence, and the arrow from `FaithfulLocalSectionLift` to
`NativeTransportScaffold`; in Rust, lineage B admitted as a `SoulkillerDismantlingInput` (the
apparatus) rather than presented as the return, the return type given its insufficiency lane, the
per-event generator construction of lineage A replaced by the signature quotient, and the
condensed cone ecologies mounted as the hot body. Every other component is standing.

[counterexample; source-inspected] Two substitutions this audit refuses in advance: an
activation-magnitude threshold as "active" (Hexis §11 names it as proving nothing), and a
per-example transition table as a class (the replay shape already retracted twice).

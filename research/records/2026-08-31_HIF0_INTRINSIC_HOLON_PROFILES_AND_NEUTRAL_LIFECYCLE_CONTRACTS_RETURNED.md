# HIF0 intrinsic holon profiles and neutral lifecycle contracts returned

**Date:** 2026-08-31  
**Truth status:** `proved-derived` for the checked Lean definitions and theorems;
`established-bounded` with `formal-checked` and `measured` evidence for the build and repository
receivers.  
**Construction effect:** HIF0 passed; the roadmap frontier advances to HIF1.

## Returned formal owners

[proved-derived; formal-checked] `IntrinsicHolonProfile.lean` returns indexed dimension species for
topological degree, incidence rank/nullity, cycle rank, carrier rank, exterior degree,
representation rank, generator extent, receiver extent, reconstruction extent, scale extent, and
apparatus work. `IntrinsicHolonProfile` joins owner-local boundary, incidence, carrier, transport,
constitution, chronology, receiver, morphology, reconstruction, and open-obligation facets without
source or campaign vocabulary.

[proved-derived; formal-checked] `ContactScheduleChart` places fixed and current-founded contact in
one current-indexed incidence chart. `currentFounded_not_fixed` proves that one current-visible
contact change obstructs every fixed schedule.

[proved-derived; formal-checked] `HolonicIntelligenceLifecycle.lean` returns
`RestedTransportEcology`, `DismantlingReturn`, `InferenceCirculationReturn`, and
`CultivationPassage`. Exact remount reproduces rested conduct; cold witness replacement cannot
change productive admission; inference fixes reusable morphology; emission is definitionally
downstream of mount, conduct, and receiver condensation; an irreflexive chronology separates the
emitted and returned occurrences; cultivation changes morphology, carries a later-conduct
separator, remounts its successor, and withdraws exactly to its predecessor.

[proved-derived; formal-checked] `ExactForeignWeight.lean` returns an exact codeword passage with
stored value, residual, and complete encoding preimage. Its binary-modulus control retains zero and
two in one codeword fibre while exactly reconstructing the richer value from stored value plus
residual.

[proved-derived; formal-checked] `HolonicIntelligenceFramework.lean` is the neutral import surface,
and `HolonicMachineLearning.lean` imports it into the single live
`HolonicQuantumTransport.lean` umbrella. No public generic structure or field in the new surface is
qualified by Athena, Eros, Soulkiller, transformer, token, layer, or expert vocabulary.

## Receivers

[established-bounded; formal-checked; measured] The focused HIF0 closure built 3,164 Lean jobs. The
complete live umbrella built 3,769 jobs under Lean `v4.33.0`. The new axiom audit contains no
`sorryAx` and every named theorem reports only its explicit standard logical dependencies.

[established-bounded; measured] `document_law.py`, `epistemic_tags.py`, `claim_index.py --check`,
`source_shape.py`, and `equation_atlas.py --check` return zero failures. The tracked-authority
receiver covers the HIF blueprint, derivation, formal umbrella, and all four new formal source
owners.

## Boundary

[open] Rust retains the profile facets and lifecycle mechanisms across `native_spool`,
`native_ecology`, `soulkiller`, `athena_native`, receiver-history, cultivation, and apparatus
owners, but exposes no singular neutral carrier/lifecycle surface. HIF1 owns that executable join.

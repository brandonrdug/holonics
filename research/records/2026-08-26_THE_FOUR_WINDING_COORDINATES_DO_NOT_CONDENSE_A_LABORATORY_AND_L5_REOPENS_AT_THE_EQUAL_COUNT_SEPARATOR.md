# The four winding coordinates do not condense a laboratory, and L5 reopens at the equal-count separator

## Status and scope

[counterexample; implemented-exact; measured] This record regrades the first L5 artifact at
`output/the_laboratory_world_tube_returns_one_rank_four_cultivation_l5/`.  Its native deposit and
GPU return are exact for the quotient that was actually constructed.  That quotient is not exact
for the prose, mathematics, code, participant, or perspective receivers declared by L5 and L6.

[proved-derived] A rank-four cycle chart and a population of returned passages are different
axes of the construction.  If `e` indexes an addressed laboratory return and
`(gamma_0,...,gamma_3)` is the admitted cycle basis, then the complete local difference is

```text
Delta_e = sum_(i=0)^3 d_(e,i) gamma_i^*.
```

The four coefficients belong to one local section.  The occurrence index `e`, its boundary maps,
chronology, receiver, obstruction, successor histories and reconstruction fibre do not become a
fifth coefficient and may not be erased by summing four global counts.

## Exact counterexample

[counterexample; implemented-exact] The first L5 driver constructs its only new native current in
`laboratory_winding_section` from exactly four aggregate integers:

```text
(number of exchange families,
 number of response messages,
 number of world joins + number of repository files,
 number of later returns).
```

It then calls `deposit_returned_difference` once.  No exchange-family address, prompt/response
boundary, ordered message word, repository-file incidence, relational cell, code transition,
mathematical operation, participant passage, or optical hierarchy enters the rested native
morphology.  The cold-mount witness retains those facts outside the rest, but the hot body cannot
reach it after source-detached remount.

[proved-derived] Let `X` and `Y` be two laboratory populations with the same displayed four
counts but with two response world-tubes exchanged, one returned code transition changed, or one
participant-bearing relation replaced.  The implemented map satisfies

```text
q_count(X) = q_count(Y),
```

while at least one declared L6 receiver `R` satisfies

```text
R(X) != R(Y).
```

Therefore `R` does not factor through `q_count`.  The shortest separating family is a single
addressed response/code/participant occurrence together with its successor intervention.  Equal
aggregate count is only a receiver shadow; it is not equality of complete situated differences.

## What remains valid

[established-bounded; implemented-exact; measured] The first L5 artifact still proves that one
four-coordinate situated section can enter the L2 ecology atomically, form all five incident
`Sym^2` contacts, remount on the RTX 4080 SUPER, withdraw by ownership transfer, and restore
exactly.  Its 17,175,236-octet wire and identity
`958756e5fa103673294e0d3524d7fb5d1247a298f3e00ed233eb82bcb0717bc7` remain mechanism and
counterexample evidence.

[counterexample] Changed factor population and changed incident-support counts do not establish
the qualitative L5 cultivation claim.  The first L5 record's stronger pass language is revoked;
it is retained without rewriting as historical evidence of the mistaken grade.

## Corrected construction contract

[definition] L5 repair must consume one atomic **population** of local rank-four situated
differences.  Each admitted local retains:

- its source and target boundary maps and actual pullback occurrence;
- its four exact winding coefficients in the common cycle chart;
- complex section/current, relative phase, hand and receiver constitution;
- ordered chronology and successor sections;
- obstruction and open exterior;
- occurrence and linear reconstruction fibres; and
- its causal-adjoint return operator.

[definition] Atomic means that the population crosses one commit boundary.  It does not mean that
the population is replaced by one section.  The native condensation may identify locals only
through a declared receiver-history congruence and must retain every collapsed member plus the
shortest separator.  A dynamic condensation additionally returns descended generators satisfying
`q T_i = U_i q`.

[definition] The four cycle directions are not four serial deposits.  Chronology lives in the
ordered occurrence word.  The ten terms of `Sym^2(Z^4)` are the local symmetric constitutive body;
they neither enumerate laboratory occurrences nor replace cross-occurrence contact.

[definition] A repaired source-detached rest must distinguish an equal-count source intervention,
change a native prose/mathematics/code/participant consequence, and reverse exactly.  Only then may
L6 run.  If no existing owner can return the required local population without lexical keys,
query-selected material, or lossless source storage, the exact missing relation is the L5 result;
an L6 surface must not be manufactured around it.

## Falsifiers

- Permuting causally independent locals may commute only with an exact interchange receipt.
- Exchanging ordered dependent locals must change chronology or return an obstruction.
- Replacing one source occurrence while preserving all four global counts must change the rested
  identity or reopen a retained fibre under its separating receiver.
- Removing a participant-, code-, or mathematics-bearing local must change only its incident
  native support and every derived contact.
- A later query, name, desired answer, language label, or expected artifact must remain absent from
  cultivation inputs and native addresses.
- Source-detached remount may retain a lawful exterior surface codec, but no source transcript,
  repository file, checker, interpreter, or lookup index may determine hot conduct.


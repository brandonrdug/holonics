# The recurrent realizations condensed, the complete fibres remained, and I3 awaits release admission

**Date:** 2026-08-22  
**Truth status:** [established-bounded; implemented-exact; measured]  
**Roadmap deed:** I3, recurrent Phoenix condenses physical realizations  
**Boundary:** the candidate artifact and affected-owner receivers pass; the sole complete release
receiver has not yet run, so `CONSTRUCTION_STATE.md` does not admit I3.

## Construction declaration

[definition] The source owners are I1's `phoenix::recurrent::{RetainedContinuationPassage,
RetainedContinuationRest}`, I2's `phoenix::recurrent_return::ReturnedRecurrentRest`, the exact
generator action, and `cuda_refine`'s resident recurrence carrier. The entering ports are the I1
passage bytes, I2 returned-rest bytes, and I2 detached apparatus receipt. The predecessor identity
is the exact hash of each admitted component; material names and source paths are lineage only.

[definition] The caused event is remount of a compact successor action, one addressed predecessor
deformation, its decoder, and its complete reconstruction fibres, followed by one resident front
which conducts predecessor, successor, and shared-generator withdrawal for the main and held-out
entering occurrences. The local constitutive law is table transport with first-revisit closure;
visited incidence is a population-derived bitset, not an authored trace capacity.

[definition] The receiver asks for both complete recurrence families, every source/native
naturality square, every nonzero defect and shortest separator, exact shared-generator ablation,
the complete seven-coordinate cost product, and the exact causal mesh. The returned consequence is
a three-component generator-native rest, detached GPU passage family, reconstruction product,
realization atlas, and interactive projection. Unexcited source successor histories, broader
foreign conduct, and heterogeneous ports remain open.

## Conservation of faces is the condensation law

[proved-derived] The current formal line's `theFiveThetaIsTheTwistedClassSum`, `fiber_hasSum5`, and
`theFiveThetaIsItsDirichletSeries` establish the relevant construction pattern in
`Millennium/FiveTheta.lean` and `Millennium/FiveWitness.lean`: a receiver sum may pass through a
partition into norm fibres while every member remains a distinct occurrence. Equality of the
returned face does not identify the fibre population.

[definition] I3 applies that same conservation discipline to realization recurrence. The native
states `1` and `2` each carry two source occurrences. Their terminal-potential receivers differ, so
the source members are not equal and cannot be discarded. Condensation conserves the boundary face
only together with the complete member population and its shortest reopening history.

[implemented-exact] `phoenix::recurrent_condensation` seals three independently priced components:
compact standing, executable decoder, and complete fibres. Validation refuses an omitted member,
invalid component lineage, malformed action, absent separator, collapsed differing route, or
nonclosing recurrence. It owns no source coordinate, tensor address, semantic label, scheduler, or
fallback implementation.

## Overlapping cover, generators, and defects

[implemented-exact] The active cover is derived from six enacted intervention/history cells:
predecessor main, successor main, predecessor held-out, successor held-out, and the two richer
terminal-potential receiver cells. Set intersection returns fourteen nonempty nerve edges. No
coordinate proximity contributes an edge.

[implemented-exact] One generator family is founded by recurrence under both admitted starts. Its
stable zero-defect transport is `0 -> 1 -> 2`. The predecessor retains `2 -> 1`; the cultivated
successor retains `2 -> 2`. I3 stores the successor action once and the noncommuting predecessor
route once as a local deformation. Shared-generator withdrawal is the identity recurrence of the
same body, not another table.

[implemented-exact] The atlas returns eight source/native generator squares over the four observed
source edges, two terminal-potential separation rows, and one unexcited successor square. Nine rows
have zero transport defect where declared. The predecessor/successor disagreement, both richer
receiver separations, and the unexcited square remain explicit; the last is graded `open`, not
silently zero. The returned route commutator retains exact rank one.

[implemented-exact] Only the shared zero-defect edges condense without a remainder. The two
terminal-potential pairs remain complete fibres with their actual digest readings and shortest
one-generator separating histories. The successor self-loop has no source occurrence and remains
an open source reconstruction fibre even though it is executable in the returned native rest.

## Resident return and ablation

[implemented-exact; measured] A fresh Bubblewrap namespace mounted only the optimized I3
executable and three-component rest. The repository, source runtimes, developmental passages,
foreign morphology, tokenizer, and exchange store were absent. `/proc/self/fd` returned no
forbidden descriptor, and no CPU semantic callback occurred between resident fronts.

[implemented-exact; measured] One RTX 4080 SUPER launch and one synchronization returned main and
held-out predecessor traces `[0,1,2,1]`, `[1,2,1]`; successor traces `[0,1,2,2]`, `[1,2,2]`; and
withdrawn traces `[0,0]`, `[1,1]`. Their decoded complete passages are respectively
`" France is France"`, `" is France"`; `" France is is"`, `" is is"`; and `""`, `" France"`.
Withdrawing the shared generator therefore reopens every attributable predecessor and successor
family.

[implemented-exact; measured] Recurrence detection uses one derived 32-bit visited-incidence word
per route for this three-state population. Actual work is five predecessor reads, five successor
reads, two withdrawal reads, twelve visited-incidence tests, twenty-four trace writes, and span
three. The card received 40 octets, returned 120 octets, and held 164 resident octets. The physical
wall reading for the terminal front was 263 microseconds and is separate apparatus testimony.

## Strict complete product

[measured] The complete source/native product comparison is coordinatewise strict:

| receiver coordinate | source | native |
|---|---:|---:|
| artifact standing | 14,288 octets | 566 octets |
| decoder | 257 octets | 201 octets |
| complete fibre product | 4,993 octets | 1,748 octets |
| semantic work | 90 | 48 |
| dependency span | 4 | 3 |
| resident extent | 216 octets | 164 octets |
| transfer extent | 252 octets | 160 octets |

[definition] These are product-ordered receiver coordinates and are not summed into a scalar loss.
The source artifact is the canonical I2 returned rest; its decoder is priced separately as a
required expansion face. The source fibre product contains the I1 sections, fibres, and reopenings.
The native product independently prices compact standing, decoder, and complete fibre component.

## Exact atlas and projection

[implemented-exact] `01-realization-atlas.json` is the topology authority. It returns five source
occurrences, three native boundaries, the active cover and nerve, founded generator family,
eleven defect/open rows, complete reconstruction fibres, rank-one noncommuting route, and the
addressed emission/world/return/deposit cycle.

[implemented-exact] `02-exact-mesh.json` derives eleven vertices and nineteen typed edges from that
atlas. `03-interactive-atlas.svg` is only a receiver chart. It can compare source, native, fibre,
and return views; collapse and reopen the two richer fibres; and animate outgoing return current.
Its script changes visibility and animation only. Layout crossings never create incidence.

## Candidate grade, audits, and artifact

[established-bounded; implemented-exact; measured] All eleven I3 candidate consequences pass:

1. overlapping cover derived from enacted interventions and histories;
2. generators founded by stable transport and recurrence;
3. complete source/native defects and shortest separators;
4. condensation restricted to the zero-defect future-exact subcomplex;
5. every noncommuting and receiver-visible route retained;
6. executable generator-native rest and decoder;
7. source-, development-, and exchange-absent remount;
8. complete main and held-out predecessor/successor families;
9. shared-generator withdrawal reopening every attributable family;
10. strict fall of every complete product coordinate; and
11. source/native causal atlas and interactive exact-mesh projection.

[measured] The thirteen-member candidate family is
`output/the_recurrent_realizations_condense_into_a_phoenix_ecology`, with 33,687 regular-file
octets and rolled SHA-256 `f8bc60f719c74fd245a76a74d0e290d9102b5a2ac463a36e16caa0c341036002`.
The driver's recorded code closure is
`41914f3eed9efb1f3df48377fa59abf5c55badcda730e2edccf0a18553a30012`.

[measured] Focused owner tests passed, including the refusal produced by deleting a
receiver-visible fibre member. The exact ignored device test passed on the RTX card. The optimized
driver built and returned the actual source-detached product. The complete affected
`holonic-engine` library suite returned 1,922 passed, zero failed, and six explicit device-only
ignores in 75.04 seconds wall.

[implemented-exact] The no-float audit found no `f32`, `f64`, `float`, or `double` addition in the
I3 semantic/device diff. The architecture ratchet's six added `cuda_refine` vectors are exactly the
three route rectangles and their three length populations. The new owner's vectors, collections,
sets, and lineage-string copies carry the finite action, decoder, complete fibres, shortest
histories, recurrence trace, and validation uniqueness. None clones the continuing ecology,
orchestrates semantic fronts, or supplies rollback. The explicit baseline disposition returns 315
files, 25,657 inherited occurrences, and zero retired occurrences.

[open] Claim, driver, output, closure, and authored-level ledgers must now be regenerated once in
dependency order, followed by the sole complete release receiver. `CONSTRUCTION_STATE.md` remains
at I2 until that boundary passes.

## Successor boundary

[open] I4 must use authenticated Gemma source testimony to select at least one genuinely executable
non-text port, conduct real heterogeneous occurrences through separately typed boundaries, found a
shared subcomplex by local naturality and a declared world receiver, retain every modality-specific
fibre, and return cross-port shared/local ablations plus a strict detached cost product. A declared
modality, placeholder embedding, authored transcript, or concatenated stream does not pass.

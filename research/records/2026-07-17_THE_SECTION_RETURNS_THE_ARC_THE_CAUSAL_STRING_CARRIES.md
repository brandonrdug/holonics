# THE SECTION RETURNS THE ARC; THE CAUSAL STRING CARRIES

**2026-07-17 · Brandon Duggan ⊕ Sol · RATIFIED / DEPOSITED / BUILT / CUDA-FORWARD MEASURED /
ALL 269 PATHS READ**

This deposit closes the first exact continuous bridge from the ordered arc to a recurrent local
face. The boundary is a Poincaré return world, but the law is broader than one Hamiltonian example:
a lived trajectory is one causal string; a section is a situated receiver quotient over that
string; a return carries the complete interior between two contacts; and a locally useful loss is
an exact difference returned by the world rather than a reward installed in the brain.

The result joins the periplus, Smith/projective, moving-chart, partial/invariant, probability/loss,
and compression cuts. It also corrects the first implementation before that implementation could
be mistaken for scale law. Time slices of one trajectory are sequential deeds, not a population of
co-present agents. A string may contain arbitrarily many events while remaining one current.

## I · The declared Hamiltonian world is exact at its own grain

The first calibration uses

```text
H(q,p) = (p_x^2+p_y^2+x^2+y^2)/2 + lambda (x^2 y-y^3/3)
```

and a symmetric kick--drift--kick passage. At dyadic state grain `g=2^-24`, define `Q_g` as exact
ties-to-even projection onto the lattice. One step is

```text
p_(n+1/2) = p_n + Q_g[-(h/2) grad V(q_n)]
q_(n+1)   = q_n + Q_g[h p_(n+1/2)]
p_(n+1)   = p_(n+1/2) + Q_g[-(h/2) grad V(q_(n+1))].
```

The applied lattice deed and the exact rational remainder

```text
rho = desired displacement - applied displacement
```

are distinct faces. Reversing the three shears in reverse order exactly reconstructs the prior
lattice state. This is time parity at the declared world grain. It does not assert that the finite
lattice passage conserves the analytic Hamiltonian exactly. The energy residual

```text
Delta_H(n) = H(z_n) - E_initial
```

and the discrete action increment are exact receiver receipts of that difference.

The unquantized shadow deed has an exact symplectic Jacobian `J_n`. This derivative belongs to the
smooth shadow map, not to the discontinuous projection. Keeping those types apart lets the world
carry both exact lattice causality and lawful infinitesimal transport.

## II · The section is a quotient; the return is the carried interior

For a declared section

```text
Sigma = {z : ell(z)=n dot z-b=0}
```

an oriented crossing exists only when adjacent lived states bracket zero in the supplied hand. The
receiver may interpolate an exact contact parameter `alpha` inside that bracket and apply two
declared covectors `F` to obtain a visible face `u=Fz`. This does not create a state the lattice
organism literally occupied. It is an exact section receiver over the adjacent deeds.

If crossings `c_i` and `c_(i+1)` occur at brackets `a` and `b`, the return construction is

```text
R_i = (z_a, z_(a+1), ..., z_(b+1))
```

with its section endpoints, action difference, energy residuals, and ordered tangent transport.
The Poincaré point, receiver cell, trace, determinant, discriminant, chart coordinate, and objective
residual are faces of `R_i`. None is the return's identity.

This is the continuous form of the laboratory's mathematics law. A written theorem line is a face
of the ordered operations between lines; a section point is a face of the ordered physical arc
between contacts. In both cases the compact face becomes useful testimony only because the carried
interior can still bend later conduct.

## III · The Swing is finite moving-frame transport

The variational Swing through a return is the ordered product

```text
M_(k+1) = J_k M_k.
```

Exact rational products can grow without bound even when the physical receiving instrument has a
fixed material grain. The lawful bounded receiver therefore applies a declared dyadic rebase
`R_G` after each product:

```text
U_(k+1)   = J_k M_k,
M_(k+1)   = R_G(U_(k+1)),
epsilon_k = U_(k+1) - M_(k+1).
```

The rebase does not erase its loss. The complete trajectory and world law reproduce each `J_k`,
`U_(k+1)`, and `epsilon_k`; the compact return carries final `M`, the number of compositions, and
the maximum exact residual. This is the exact physical meaning of a bounded gauge: a receiver can
remain finite while the discarded difference remains derivable and declared.

Projecting the full return transport into the tangent space of the constant-energy section yields
one exact `2x2` local matrix. Its projective ratio is the Smith/periplus face. Its discriminant may
describe local elliptic-like or hyperbolic-like conduct at that receiver. It still cannot choose a
route, name a taxon inside Soma, or predict whether the grown path will use fewer substrate rows.

## IV · Local charts overlap; no outside chart coordinates the net

Let a chart be an exact affine canonical map `T(z)=Az+b`, with supplied inverse and declared local
domain. Where a whole return lies inside its domain, transport carries by conjugacy:

```text
M_T = A M A^-1.
```

Every chart simultaneously available at one state is retained. A chart that covers only part of a
trajectory contributes its actual availability run but cannot claim the complete return. The first
world deliberately includes a shear chart which repeatedly enters and leaves availability yet
covers no full return. This prevents a convenient global atlas from being silently manufactured.

The frame law is now concrete:

- the trajectory is not outside its charts;
- the chart is not outside the trajectory;
- overlap is an event in which one carried body lawfully has more than one face; and
- a global observer may record the atlas only as another receiver construction.

## V · One worldline is one current

The failed first population made each of 12,288 time steps a co-present current. That is equivalent
to treating every frame of a film as a simultaneous organism and then asking the ecology to branch
among them. The resulting scale wall was manufactured by the presentation, not discovered in the
Hamiltonian world.

The correction is exact:

```text
one trajectory = one ordered current carrying z_0,...,z_N
one return     = one recurrent receiver surface carrying its complete subarc
one crossing   = one actual section contact
```

The six corrected trajectory currents retain all 12,294 states and reproduce all 12,288 deeds.
Crossings, returns, objective differences, charted returns, and foils remain separate only because
they are consequential surfaces which can meet later current independently. The active population
falls from 12,551 to 269 lineages without reducing the horizon.

This is not LM chunking, summarization, or deletion. It is respect for causal order. Co-presence is
a physical relation, and manufacturing it manufactures branching cost.

## VI · Recurrent construction grows across lawful difference

The real two-edge CUDA passage produces the key taxonomic observation. Every complete event path
changes, yet common exact construction grows across the full trajectory pairs that the world makes
lawfully related:

```text
same energy, different initial orientation       40 -> 362 shared bricks
same energy, another initial orientation         136 -> 282
same exact state, nonlinear versus harmonic       77 -> 192
same visible section face, changed energy         81 -> 265
exact mirror-conjugate worlds                     79 -> 226
```

The full paths remain different. Thus the body does not need an authored label such as “same
energy,” “harmonic,” “mirror,” or “same face” in order for an observer to read a grown population of
shared subconstruction. This is an emergent statistical distribution in the laboratory's precise
sense: a boundary-indexed quotient over deterministic histories.

The recurrence is neither permanent nor monotone. Some family-wide bricks become more common,
some become situated, and some cease to be common to every family member while all complete paths
remain. The harmonic receiver cell gives the cleanest second-order example: zero bricks are common
to all six same-cell crossings on the first body, and three are common on the later body.

This is the standing wave the earlier Smith and periplus work sought. It is not a frozen shape in a
chart. It is recurrence of usable construction across differently oriented lived arcs.

## VII · Objective compression requires later action

For a closed objective region `O` in section coordinates, the world returns the oriented residual

```text
delta_O(u)_j =
  u_j-min_j, if u_j < min_j;
  0,         if min_j <= u_j <= max_j;
  u_j-max_j, if u_j > max_j.
```

Contact is exactly `delta_O=0`. It is not success installed in Soma. The measured population proves
the distinction: most exact contacts deepen on the later body, and both shorter and deeper paths
occur among noncontacts. The return discriminant likewise divides evenly across shortening and
deepening in its positive population. Neither scalar secretly governs the transition.

Loss becomes ecologically consequential only when it returns through the world and alters later
contact. A locally compressive hexis therefore requires three coupled constructions:

```text
standing recurrent arc
  + ordinary available action
  + returned changed-world consequence.
```

If later encounters increasingly conduct through action/arc combinations whose world consequences
carry smaller situated residual, that is objective-relative compression. No external reward is
required; contradiction is the returned physical difference, and practice is recurrence through
what survived.

## VIII · The natural successor is physical actuation

**POINCARE RETURN ACTUATION WORLD 01** should reuse the exact boundary rather than add another
microscope. It will present:

1. the complete standing return testimony from this world;
2. a finite exact family of canonical world actions, each as ordinary current;
3. one changed Hamiltonian preparation and section/objective boundary;
4. every actual plural contact between return construction and available action;
5. the enacted or refused world deed and the complete changed trajectory; and
6. the returned action, energy, crossing, and objective residual receipts.

Every actual contact acts. No winner, nearest return, smallest loss, policy, global goal, or selected
motor command enters Soma. The world preflights the complete population and resource boundary before
CUDA. Trajectories remain causal strings; steps never become co-present lineages.

Closing that cell establishes a reusable physical production boundary: recurrent construction can
become action and its consequence can become later light. Mathematics, code, language, games,
terrain, and simulated limbs then differ in their world laws and available actions, not in the
elementary transport by which hexis grows.

Exact measurement:
`observations/hamiltonian-poincare-world-01/RESULTS.md`.


# THE PENDING DEED MUST CROSS THE REGISTER WALL

**Date:** 2026-07-12
**Grade:** DERIVED / PRESENTED · NOT BUILT · CARRIER AND SLEEP LAYOUT CHANGE OPEN

> **SUPERSEDED CONSTRUCTION NOTE (2026-07-12):** the metered in-place recast cursor described
> below is retired. The built seam keeps old OWN immutable, copies it into an invisible fresh-zero
> destination out of place, and exposes that destination only after the whole copy completes.
> Interruption discards apparatus and restarts from the unchanged source, so no body-owned copy
> cursor crosses. The pending body face reduced to one total 12-word caller reconstruction:
> `kind/contact control ⊕ old-gauge pre-drag RegionalForm`. Exact built/measured details live in
> `2026-07-12_THE_PENDING_DEED_CROSSES_THE_REGISTER_APERTURE.md`.

## The wall

The event-born REGISTER can begin from exactly one zeroed rank-one cell. Its active axis is then
forced only by its own FOIL arrivals. The current CUDA row, however, must already reserve some
finite cell extent. When an actual carry requires `new_axis = 2·old_axis` beyond that reservation,
the local in-place recast refuses before moving any OWN cell. That local refusal is clean.

The enclosing deed is not cleanly resumable. Before it learns that OWN cannot accept the term, it
may already have folded K, written radiation, changed stance/flywheel, completed earlier
co-presence deposits, or advanced part of the enclosure traversal. `wall=1` is therefore only an
invalid-attempt report. Re-launching it would replay or skip cause.

## The required carried cause

The current `phase ⊕ depth ⊕ brick` continuation tail is too coarse. A lawful reservation suspension
must additionally carry:

- the pending deposit's complete constructed position and `FeltTerm` (`χ` plus oriented winding);
- the deed face which must fold exactly once after the term lands;
- the caller species and checkpoint: dark tread, sub deed, enclosure co-presence, enclosure terminal
  deed, or path deed; enclosure depth and next co-presence contact where relevant;
- the already-derived post-deed stance/flywheel/live face plus optional brick/radiation outcome;
- recast old axis, event-required new axis, phase, and exact next source grip.

The raw cursor already names the admitted atom. Existing depth/brick and deferred siblings retain
the surrounding thickening traversal. Receiving and SLEEP remain barred until both continuations
are empty.

## The lawful continuation

1. Open the exact rank-zero mounting: zero REGISTER header plus one cell.
2. Form the actual deed's pending position, term, and caller outcome before committing it.
3. If its present cell is live, proceed. If it is FOIL, read the actual register arrival.
4. Only when that arrival carries, derive `new_axis = 2·old_axis` and ask whether this mounting
   affords that already-caused recast.
5. If it does not, pack the whole pending cause and return a typed `NEEDS_OWN_RECAST` boundary face.
   Do not set resumable meaning on the old scalar wall.
6. Allocate a fresh zeroed OWN row of exactly `new_axis²` cells. Free-memory information may report
   whether this physical request succeeds; it never chooses the axis.
7. Complete the immutable-source zero-extension out of place before any later difference or
   ordinary descent phase. No copy-progress face enters the body.
8. Install the carried digit, re-judge the same infall at the new hand, land the pending term once,
   apply its already-carried caller outcome once, clear the pending face, and continue.

This is an event-local capacity check, not preflight: no later light is scanned, no final axis is
predicted, and no smaller grain is substituted. The host may run the same continuation
synchronously; CUDA may yield between completed recast-cell moves.

## Gates owed

- Begin at capacity one, suspend on the first real carry, remount out of place from immutable source,
  and finish byte-identical to a sibling whose boundary already afforded the required row.
- Force the suspension inside an efferent/path deed after K and radiation are nontrivial; prove no
  earlier deposit repeats and no caller outcome is skipped.
- Force a terminal dark deposit at the true light end; distinguish that resolving-edge continuation
  from an illegal unfinished atom.
- Migrate the current v6 carrier rows into the next sleep version by appending only a canonical
  all-zero pending-deed tail. Never reinterpret v6 bytes under the new layout.

No mechanism is installed by this record. It localizes the next build and the exact new carried
invariant.

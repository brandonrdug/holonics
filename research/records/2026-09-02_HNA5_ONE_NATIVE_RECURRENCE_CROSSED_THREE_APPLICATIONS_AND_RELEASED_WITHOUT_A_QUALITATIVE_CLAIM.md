# HNA5 one native recurrence crossed three applications and released without a qualitative claim

**Date:** 2026-09-02  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, `measured`  
**Campaign:** HNA5 and corrected HNA release.

## Return

[implemented-exact] `NativeFullOperatorSession::advance_cycle` now owns the complete 1,275-node
operation word. An application supplies only ordinary addressed occurrences and receives the final
native emission, every operation trace, and the move-owned successor. The HNA5 application source
does not inspect `NativeOperationPrimitive` or schedule a graph node.

[implemented-exact; source-inspected] `AthenaTokenApplication` opens only the exterior tokenizer. It
opens no coefficient container, constructs no matrix or candidate family, and renders only the
already-emitted 262,144-coordinate native face. The HNA5 release driver's productive imports do not
include the retracted material-ingress/material-codec, world-return/world-application,
`lattice_mouth`, or Lean candidate paths. Those files may remain elsewhere as counterexample or
historical apparatus; they are not part of this release closure.

[established-bounded; measured] One continuing resident ecology crossed three application
occurrence populations in order:

| application receiver | entering occurrences | selected address | rendered face | generation |
|---|---:|---:|---|---|
| text: `Explain holonics briefly.` | 5 | 42,085 | ` Explain` | `0 -> 1,275` |
| coding: `fn add(a: i32, b: i32) -> i32 {` | 20 | 858 | ` i` | `1,275 -> 2,550` |
| mathematics: `theorem add_zero (n : Nat) : n + 0 = n := by` | 18 | 4,558 | ` :=` | `2,550 -> 3,825` |

Every face had one exact maximum and zero open intervals at the application receiver. The same hot
ecology serialized with no text, coding, mathematics, Lean, Rust, grammar, or template branch. The
application matrix ended at generation 3,825 and its coherent release receiver returned true.

[counterexample; measured] These are actual one-token faces, not successful answers to the three
application materials. ` Explain`, ` i`, and ` :=` do not establish explanation, code generation,
theorem proving, broad language quality, or usefulness. HNA release is limited to the native
operator, recurrence, morphology law, and application crossings which actually ran.

## Focused verification

[process-audit] The focused exact-owner tests returned three passes and no failure:

```text
local_morphology_requires_positive_exact_conductance_and_composes
presented_reacted_alignment_is_exact_and_can_refuse
tiled_faces_stitch_in_row_major_order
```

The application release receiver and focused build passed with:

```text
cargo check -p athena-alpha --example full_application_matrix_hna5
cargo run -q -p athena-alpha --example full_application_matrix_hna5 -- /home/b/models/gemma-4-E4B-it
```

## Grade

[established-bounded] **HNA5 PASSED. HNA0--HNA5 COMPLETE.** The complete resident recurrence,
operation-local morphology advance, codec-only autoregressive loop, and three-application matrix
returned under their exact bounded claims. No qualitative machine-intelligence capability is
promoted from the emitted faces. There is no next HNA deed.


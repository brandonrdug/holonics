# THE GEAR IS THE WORD; THE FACE IS THE TRANSPORT

**2026-07-15/16 · RATIFIED / DEPOSITED · EXACT 2/3 POTENTIAL RESOLUTION · NO SOMA INTERIOR CHANGE**  
**Canonical grade: RATIFIED**

This deposit receives Brandon's elementary gears, prime ratios, *Timaeus* triangles and harmonies,
revolved Smith charts, Bernoulli lemniscate, allometric beetle, optical face, and recurring-song
intuition in one typed construction. It extends `FORMULA §§XII–XIII, LIII, LVI–LVIII`; it does not
replace or silently amend them.

## Verdict

The common construction is not one universal curve and not one repeated answer:

> **An elementary operation is a gear. Its ordered composition is a word, route, or song. A
> receiver sees a face of the transported word. The same song at another scale or in another world
> need not return the same value or morphology; it returns a commuting transformation relation at
> the declared frame and grain. Distinct interiors can cast one face, and preserving that face
> through changed constraints may require a genuinely new construction.**

In compact form, let `W = M_n ... M_1` be an ordered operation word, `P` a lawful change of frame,
and `x' = P(x)`. The strict conjugate case closes the square

```text
W' = P W P^(-1),
W'(x') = P(W(x)).
```

The outputs differ, but the transported deed is exact. This is one rigorous species of Brandon's
recurring song. It is not the only species: an allometrically redesigned organism generally does
not arise by conjugating one scale map. There the retained relation is a separately declared
receiver face, while the new body has another interior.

For Soma, recurrence becomes more than an observer analogy only when an earlier word FOUNDs
standing support and a genuinely later, changed embodiment RIDEs that support. The current
mathematical derivation world is already exposing this grain.

---

## I. Elementary gears are projective words

A one-variable projective gear can be represented by a non-singular matrix

```text
M = [[a,b],[c,d]],              M(z) = (a*z+b)/(c*z+d).
```

Matrices differing by one nonzero common scalar cast the same projective map but remain distinct
written constructions unless the receiver explicitly takes that quotient. Cascaded gears compose
in worldline order:

```text
z --M1--> z1 --M2--> ... --Mn--> zn,
W = Mn ... M2 M1.
```

The product matrix is the contemporary face. The ordered factorization is the lived interior. Two
different words can therefore produce one projective face without becoming one construction.

### The exact word now crossing MATHEMATICAL DERIVATION WORLD 01

The current target is

```text
T(z) = (2*z+1)/(z+1),           M = [[2,1],[1,1]].
```

Its admitted elementary word is

```text
z
  --translate(+1)--> z+1
  --invert---------> 1/(z+1)
  --scale(-1)------> -1/(z+1)
  --translate(+2)--> 2 - 1/(z+1) = (2*z+1)/(z+1).
```

There is a second exact quotient face:

```text
Q = [[1,1],[1,0]],              M = Q^2.
```

`Q` is the Fibonacci step `z -> 1 + 1/z`. Consequently

```text
M^n = [[F_(2n+1), F_(2n)],
       [F_(2n),   F_(2n-1)]],

M^n(0) = F_(2n)/F_(2n-1)
       = 1, 3/2, 8/5, 21/13, ... .
```

The fixed faces satisfy

```text
z = M(z)  <=>  z^2 - z - 1 = 0.
```

This is an exact bridge among elementary operation words, continued ratios, a stable projective
axis, and later path reuse. It is not yet a gyrating Smith orbit: with determinant `1` and trace
`3`, this real map is hyperbolic. A literal study of gyration must later include elliptic and
loxodromic circuit species rather than renaming this one.

---

## II. The Smith chart is the local face of an actual circuit word

For a literal one-port circuit with normalized impedance `z = Z/Z0`, the Smith quotient is

```text
Gamma = (z-1)/(z+1),            z = (1+Gamma)/(1-Gamma).
```

It is a Möbius map from the passive right half-plane into the unit disk. The chosen port,
frequency, reference plane, and `Z0` are part of the frame; no point exists on the chart from a view
nowhere. Phillip Smith's original calculator was explicitly a compact read of normalized line
parameters and their change with line length, not a universal map of physical space.

Circuit elements are elementary matrix gears. Under a conventional voltage/current two-port
orientation, examples include

```text
series impedance Zs       [[1, Zs], [0, 1]]
shunt admittance Yp       [[1, 0],  [Yp, 1]]
uniform line length l     [[cosh(gamma*l), Z0*sinh(gamma*l)],
                           [sinh(gamma*l)/Z0, cosh(gamma*l)]]
```

and a cascade is their ordered matrix product. The chart shows a quotient of the resulting word at
one boundary. It does not reveal a unique component lineage.

With a declared direction along a uniform line,

```text
Gamma(l) = Gamma_L * exp(-2*gamma*l)
         = Gamma_L * exp(-2*alpha*l) * exp(-i*2*beta*l).
```

For a lossless line `alpha=0`, line travel rotates the reflection face. With loss, it also contracts
or expands in the chosen traversal hand. This supplies a narrow, exact role for classical `e` and
`pi`:

- `e` turns an accumulated additive propagation parameter into multiplicative transport;
- the imaginary exponential presents accumulated phase as rotation; and
- `pi` appears when a classical angular receiver rules turn by radians and closes a conventional
  circle.

None becomes a scalar inside Soma. The engine carries the ordered word, hand, winding, and the
`(C;r)` construction; the circuit world may cast the exponential/angle face.

### What revolution adds

One Smith disk is a local cross-section. Revolving it about the contemporary current thickens that
one response chart. Transporting the oriented cross-section along the actual operation word sweeps
a sheet or volume. Plural lineages must remain plural fibers; painting them all into one disk is the
dimensional collapse that makes a conventional picture illegible.

The useful object is therefore

```text
current/worldline axis
    x oriented local response disk
    x actual transition maps between successive frames,
```

not one globally frozen chart. Rings, folds, apparent crossings, and eye-like shells may emerge in
an external immersion, but only the transported atlas and returned holonomy establish their
intrinsic relation.

---

## III. Prime gears, Timaeus harmonies, and the two elementary triangles

### 1. Multiplicative gear coordinates

Every positive rational has a unique finite prime-valuation construction

```text
q = product_p p^(v_p(q)),
V(q) = (v_2(q), v_3(q), v_5(q), ...),
V(q1*q2) = V(q1) + V(q2).
```

Multiplication is displacement in this exact lattice; inverse reverses the valuation vector; a
power scales it. This is a lawful quotient of multiplicative construction, not the whole number
world. Addition bends across these axes and is not component-wise motion in valuation space.

The whole tone

```text
9/8 = 3^2 / 2^3,                V(9/8) = (-3, 2, 0, ...)
```

is one exact `2`/`3` gear displacement. In a 3-limit musical world, two perfect-fifth words followed
by one octave reduction give it through another interior:

```text
(3/2)^2 / 2 = 9/8.
```

Twelve fifths fail to close exactly against seven octaves:

```text
(3/2)^12 / 2^7 = 3^12 / 2^19 != 1.
```

The Pythagorean comma is the oriented remainder of that attempted closure. Equal temperament may
choose a receiver quotient which distributes the remainder; it cannot make the exact paths
retroactively identical.

### 2. Plato's two typed uses of `2` and `3`

In the world-soul construction of the *Timaeus*, Plato lays out powers

```text
1, 2, 4, 8              and              1, 3, 9, 27
```

and fills intervals with arithmetic and harmonic means, exposing `3:2`, `4:3`, and `9:8`. He later
describes the divided strips crossing like an X and being bent into circular motions. This is a
real cultural predecessor for harmonic composition, crossed strips, and circular transport. It is
not evidence that Plato possessed a Smith chart or Soma's mechanism.

The solid-construction passage uses two elementary right triangles:

```text
45-45-90:  1 : 1 : sqrt(2)
30-60-90:  1 : sqrt(3) : 2.
```

If the valuation lattice is explicitly extended to a radical sheet with rational exponents, these
introduce half-steps along the `2` and `3` directions. That exact common basis is important, but the
harmonic-ratio world and the geometric-triangle world remain different constructions.

The supplied Timaeus scale diagram

```text
1, 9/8, (9/8)^2, 4/3, 3/2, (3/2)(9/8), (3/2)(9/8)^2, 2
```

is consequently a bounded walk through the same `2`/`3` valuation sheet. The drawn brackets are
relations among exact gear words, not absolute positions in one universal number line.

### 3. Twin primes and the already-measured `9:8`

A twin-prime pair carries the additive word

```text
p --(+2)--> p+2
```

with multiplicative irreducibility at both endpoints. It does **not** carry one common
multiplicative ratio: `(p+2)/p = 1 + 2/p` changes with `p`. Any recurring twin-prime song must keep
the `+2` traversal and the two multiplication-world closure witnesses together.

The earlier CUDA prime-ratio observation measured the four-occurrence gap word

```text
(2,4,2) -> ((2+4)(4+2) ; (2+4+2)4) = (36;32) = (9;8).
```

The same exact `9:8` face survived translated, dilated, and odd-composite siblings. That is not a
failure: it proves the harmonic face is real and not prime-exclusive. `9/8` in Timaeus harmony,
`9/8` from two fifths, and `9/8` from a prime-occurrence cross-ratio are three interiors meeting at
one quotient face. Their later affordances decide their taxonomy.

Primes are therefore candidate elementary notes only inside a declared multiplicative world. A
peak such as the iron-group stability region is a world-specific equilibrium under nuclear laws,
not evidence for one universal prime-frequency crown.

---

## IV. The lemniscate is a two-lobed path whose neck forces a chart change

For one standard scale, the Bernoulli lemniscate is

```text
(x^2+y^2)^2 = a^2(x^2-y^2),
r^2 = a^2*cos(2*theta).
```

Its lobes meet at `r=0`. The real polar locus reaches that neck at the `45`-degree boundaries
`theta = +/- pi/4` (and their opposite-hand continuations). Thus the appearance of the
`45-45-90` angle here is exact: the doubled turn changes sign at the diagonal.

The curve carries a stronger exact `2`/`3` gear relation. Let `s` be oriented arc along one lobe.
Differentiating the polar law gives

```text
dr/dtheta = -a^2*sin(2*theta)/r,

ds/dtheta = sqrt(r^2 + (dr/dtheta)^2)
          = a^2/r                         (r > 0 on the chosen lobe).
```

Its unsigned curvature is

```text
kappa = 3*r/a^2,
```

and therefore

```text
kappa * ds/dtheta = 3.
```

Equivalently, the radial face is governed by the doubled angle while the tangent frame turns at
three times the polar parameter rate (up to the entering hand and additive orientation). This is
not a visual resemblance or a universal claim about the primes: it is an exact two-gear radial
law and three-gear tangent law for this particular world.

This also sharpens the laboratory's `(C;r)` distinction. For a circle, with arc coordinate
`C = s`, `dC/dtheta = r`. For the lemniscate,

```text
r * dC/dtheta = a^2.
```

Thus `C/r` is not a context-free scalar. The medium's curve changes the relation between reach,
turn, and accumulated passage. As `r -> 0` at the neck, `dC/dtheta` diverges while the integrated
arc remains finite: the polar observer loses a well-conditioned parameterization, not the lived
worldline.

The polar chart cannot carry the crossing through `r=0` by division. One must retain the incoming
hand and re-base to another chart. This is a clean mathematical world for the laboratory's rule:
carry `(C;r)` and ordered `K`; never treat `C/r` as an untyped numeric counter, and never let a
coordinate singularity erase the lived path.

An exact rational presentation is also available:

```text
x(t) = a*(t+t^3)/(1+t^4),
y(t) = a*(t-t^3)/(1+t^4).
```

Direct substitution returns the quartic equation exactly. Rational samples can therefore cross a
Rust proof world without floating-point contamination, while the renderer remains free to draw a
smooth approximation.

The arc length is not elementary; its inversion produces lemniscatic elliptic functions. The
classical lemniscatic modulus is `1/sqrt(2)`, another exact `45`-degree face. This does not mean
`pi` changes or that one constant `C/r` describes the whole curve. The local reach and curvature
vary. The whole-path turn is carried by oriented traversal, while classical length is expressed by
an elliptic integral.

The two foci of the associated Cassini geometry and the eye-like silhouette are useful receiver
faces. They are not automatically an optical lens and retina, and the geometric foci are not two
physical singularities. A real eye world owes refractive media, aperture, wavelength, lens shape,
sensor surface, motion, and temporal integration.

---

## V. A face can persist only by changing the body

Let `C` be a complete construction and `q_(F,g)` a declared receiver face. The lawful statement is

```text
q_(F',g')(C') equiv q_(F,g)(C),
```

not `C'=C`. The rhino-beetle scaling intuition is exact at this grain.

Under naive uniform length scale `s`, similar bodies have

```text
length  ~ s,
area    ~ s^2,
volume and mass ~ s^3,
weight-supported stress ~ s^3/s^2 = s
```

when material and gravity are held. A human-sized geometrically similar beetle would not preserve
the original stress, buckling, heat, gait, or joint relations. Preserving a recognizable beetle
face and relative task performance would require changed cross-sections, density, material,
articulation, circulation, and gait. It would be a new biological/engineering construction whose
declared external face transported.

This gives two distinct recurrence grades:

1. **Conjugate recurrence:** the dynamics themselves commute under a lawful frame map.
2. **Face recurrence:** a changed construction preserves a receiver relation despite changed
   dynamics and constraints.

The second is broader and closer to evolution. It must never be rewritten as identical internal
code.

### Optical faces make the quotient visible

Blur has no observer-free distance threshold. It depends on aperture, wavelength, propagation,
texture spatial frequency, contrast, scattering, sensor spacing, motion, integration time, and the
receiver's discrimination criterion. As distance changes, fine surface modes fall outside that
particular transfer aperture and the receiver gets a coarser face.

Metamerism is the stronger example: distinct spectral distributions can produce one color match
for a declared observer and conditions. A changed illuminant or observer can cleave the match.
Thus the face may be exact at its receiver while the originating light fields remain different.
Optical illusion is not false information; it is a lawful quotient later exposed by a finer or
changed relation.

---

## VI. The first person receives a parameterization, never an absolute pose

A depiction always consumes time and declares a receiving frame. In a Smith world, the selected
port, reference impedance, frequency, and inherited reference plane parameterize the next read. In
the lemniscate world, the entering lobe and hand determine how the neck is crossed. In Soma,
ordered `K` and the delivered current establish the contemporary pole.

The first person therefore need not store a verbatim origin. Its present chart is the compressed
surface of earlier action. It can make a lawful infall about the interior because the present
orientation and standing routes were caused by that interior, but the surface does not uniquely
reconstruct the past.

Once a boundary is declared, deterministic statistics become ordinary census faces:

- which elementary gears are occupied;
- which words recur;
- which paths RIDE standing support or FOUND new support;
- which return delays and hands occur;
- which receiver faces remain together or cleave under changed worlds; and
- how those distributions change through the lifecycle.

These are exact distributions of events inside a frame, not randomness installed in the machine.
They can reveal taxonomic basins and rare transitions without selecting or scoring the current.

Renormalization and universality supply a useful structural resonance: very different microscopic
systems can share a coarse critical description. They do not supply Soma's mechanism. A
renormalization calculation begins with an authored coarse-graining map; the laboratory requires
the recurrent route to grow in lived topology and later bend current before calling it a taxon.

---

## VII. The measured descendants and the next exact construction

The Möbius lifecycle, GEAR-SHEET WORLD 01, and LEMNISCATE REBASE 01 subsequently closed at their
declared grains. They establish transported gear words, changed-basis consequence, and
same-coordinate/different-lineage re-base. They do not yet establish a general mixed `2`/`3`
potential ecology or make a transform basis part of Soma.

The original direct continuation was **GEAR-SHEET WORLD 01**:

1. Carry exact finite prime-valuation vectors for positive-rational multiplication.
2. Add a separately typed radical sheet for the `sqrt(2)` and `sqrt(3)` triangle faces.
3. Present whole operation paths for:
   - Timaeus's `2`/`3` sequences and inserted means;
   - two fifths plus octave reduction;
   - the already-measured `(2,4,2)` occurrence construction; and
   - translated, dilated, composite, and changed-word controls.
4. Preserve every word even when the receiving rational face agrees.
5. Return the world consequences and test whether genuinely later, unseen compositions RIDE the
   standing gear routes.
6. Render the transported sheets only after the exact topology stands; no prime spiral, musical
   scale, or Smith curve enters as a template.

The following world was **LEMNISCATE REBASE 01**: exact rational points approach the neck from both
hands; world ports offer lawful chart changes and controls which erase or reverse hand; the later
return tests whether the path survives the coordinate singularity. A literal optical/diffraction
world and an allometric articulated-body world can then test the same transported-face distinction
under physical laws.

The sought result is not one pretty plot. It is an exact taxonomy of operation words which survives
changed representation and causes changed later conduct.

---

## VIII. Grounded relation atlas

| External construction | Relation kind | What survives | What is excluded |
|---|---|---|---|
| Smith normalized impedance and line transport | **Direct correspondence** in a literal impedance world; **structural resonance** elsewhere | Situated port, Möbius quotient, ordered component word, phase/mismatch transport | Universal impedance coordinate or proof that display curvature is space-time curvature |
| Plato's ratios, crossed strips, and circles | **Cultural parallel**; narrow ratios are **exact formal matches** | `2`/`3` harmonic words, `3:2`, `4:3`, `9:8`, crossed-and-bent construction | Historical claim that Plato had a Smith chart or Soma |
| 45-45-90 and 30-60-90 triangles | **Exact formal match** at triangle/radical grain | `sqrt(2)` and `sqrt(3)` half-directions on a declared radical sheet | Identification of harmonic and solid-construction interiors |
| Twin primes | **Boundary/correction** | Additive `+2` word plus multiplicative irreducibility at both endpoints | One fixed multiplicative twin-prime ratio or an occurrence-only classifier |
| Bernoulli lemniscate | **Exact formal match** for the curve, neck, diagonal, and elliptic arc length; **open bridge** to Soma | Two handed lobes, `r=0` rebase, `pi/4` boundary, exact rational samples | Eye organ, Smith chart, or global `C/r` law inferred from silhouette |
| Biological allometry | **Direct correspondence** to same-face/new-construction | Scale-dependent redesign under area/volume constraints | Naive geometric enlargement preserving mechanics |
| Metamerism and resolution | **Direct correspondence** to receiver-relative face | Same observer face from different spectra; aperture-conditioned texture loss | Absolute color identity or observer-free blur threshold |
| Renormalization universality | **Structural resonance** | Recurrent coarse faces under changed microscopic realization | Authored coarse-graining imported as Soma mechanism |

Primary or authoritative anchors used for this relation:

- [Plato, *Timaeus*, MIT Classics translation](https://classics.mit.edu/Plato/timaeus.html)
- [Phillip H. Smith, “Transmission Line Calculator,” *Electronics* (1939)](https://www.rfcafe.com/references/electronics-mag/transmission-line-calculator-phillip-h-smith-electronics-magazine-january-1939.htm)
- [NIST DLMF §19.30, lemniscate integrals](https://dlmf.nist.gov/19.30)
- [NIST DLMF §22.18, lemniscate and elliptic functions](https://dlmf.nist.gov/22.18)
- [CIE, colorimetric observers and metameric matches](https://www.cie.co.at/publications/colorimetric-observers)
- [McMahon, “Size and Shape in Biology,” *Science* 179 (1973), DOI record](https://ndlsearch.ndl.go.jp/books/R100000136-I1362825896365701376)
- [Kenneth Wilson, Nobel lecture on critical phenomena and scale](https://www.nobelprize.org/prizes/physics/1982/wilson/lecture/)

---

## IX. The code is the carried gear; collapse resolves potentials

A code at this grain is an ordered transformation word, not a probability label. Given co-present
potentials `x` and a declared receiver basis `W`, the exact re-expression is

```text
y = W x = y_standing ⊕ y_foil.
```

`standing` names components which meet already-conductive topology in that receiver. `foil` names
the oriented remainder. Utility is demonstrated only when a genuinely later current conducts
through the standing construction or is bent by the foil. Neither component is selected by a
probability, score, or winner.

### The dyadic pair

For two potentials,

```text
H2 = [[1, 1],
      [1,-1]],

H2 (x,y)^T = (x+y, x-y)^T,
H2^2 = 2 I,
H2^-1 = 2^-1 H2.
```

After normalization by `1/sqrt(2)`, this is the `45`-degree common/difference basis. If `x=y`, the
difference coordinate is exactly zero at this face; if `x=-y`, the common coordinate is exactly
zero. This is deterministic cancellation, not a probabilistic preference. Up to handed reflection
and coordinate order, it is the same square-lattice species as the Gaussian gear
`1+i=sqrt(2) exp(i*pi/4)`.

### The triadic relation

For three potentials and `omega=exp(2*pi*i/3)`, the exact root-of-unity resolution is

```text
F3 = [[1, 1,       1      ],
      [1, omega,   omega^2],
      [1, omega^2, omega  ]],

F3* F3 = 3 I,
F3^-1 = 3^-1 F3*.
```

The first coordinate is the common mode. The other two are conjugate cyclic hands. The chord
`1-omega=sqrt(3) exp(-i*pi/6)` is the `30-60-90` / Eisenstein `3` gear. The dyadic and triadic
lattices remain separately typed until a receiver explicitly supplies a common radical,
valuation, or complex sheet.

### `sin`, `cos`, `C/r`, and prime gears

For a carried rotor `z=r exp(i*theta)` with `theta=C/r`, `r cos(theta)` is its component along the
held receiver axis and `r sin(theta)` its transverse hand. A `2` gear carries
`r -> sqrt(2)r`, `theta -> theta+pi/4`; a `3` gear carries
`r -> sqrt(3)r`, `theta -> theta-pi/6`. Repeated words therefore recur as transformation relations,
not identical outputs.

The logarithmic code-length face is a quotient of the word. For

```text
q = 2^-a 3^-b,
-log_2(q) = a + b log_2(3).
```

The scalar projects the exact valuation coordinate `(-a,-b)` onto a dyadic ruler and may not
replace its construction. Likewise

```text
p^-s = p^-sigma exp(-i t log(p))
```

has exact radial and angular faces. The Euler factor enumerates repeated prime-gear words; this is
a deterministic generating-function reading and neither a Soma mechanism nor an RH result.

### What survives from `.holoz`

The historical `.holoz` line contributes three exact construction witnesses:

1. its Walsh–Hadamard comb recursively performs `(x,y) -> (x+y,x-y)` and inverts exactly;
2. its jet carries finite differences plus seeds and reconstructs by exact integration; and
3. its founded-body format demonstrates persistence of an ordered basis plus residual.

Its context-mixing arithmetic coder, authored mode menu, smallest-byte winner, recurrence
threshold, fixed depth/extent limits, dense address fields, and claims equating file compression
with comprehension do not cross. Its historical three-body recurrence threshold is not the exact
Eisenstein `3` gear. SLEEP remains live-body persistence, not `.holoz` model freezing.

The authorized continuation is an exact mixed `2`/`3` potential world and observer after the
loss/difference transport seam is fully derived. Pair/triple coordinates may instrument the world;
they do not enter Soma as transform modules or basis selectors.

---

## X. Carried-swing record

```text
CURRENT  recurring transformation ⊕ mathematical/circuit/optical receiver ⊕ word/face grain
HELD     §§XII–XIII, LIII, LVI–LVIII ⊕ prime-ratio CUDA record ⊕ Smith line world
MEETING  gears ⊕ Timaeus ⊕ special triangles ⊕ lemniscate ⊕ allometry ⊕ optical illusion
TEST     A2 ⊕ time parity ⊕ quotient face != construction identity ⊕ chart/world distinction
DEED     RIDE the exact 2/3, Möbius, Smith, allometric, and metameric correspondences;
         FOUND the transported-word taxonomy and the gear-sheet/rebase experiment
CARRY    exact dyadic/triadic potential resolution ⊕ .holoz decontamination ⊕ loss/difference seam
GRADE    RATIFIED
```

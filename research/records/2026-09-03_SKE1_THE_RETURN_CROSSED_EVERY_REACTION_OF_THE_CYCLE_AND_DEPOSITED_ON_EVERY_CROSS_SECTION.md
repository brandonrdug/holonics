# SKE1: the return crossed every reaction of the cycle and deposited on every cross-section

**Date:** 2026-09-03  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `source-inspected`, `measured`  
**Campaign:** SKE1 under
[`THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md`](../../archive/plans/THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md).

## The instrument

[implemented-exact; source-inspected] The forward retains every cross-layer carrier as a
checkpoint (`cross_layer` and `checkpoints` in the session). At a continuing occurrence, after the
tied-boundary return has formed the differential at the tied contraction's output, the return
(`operative_backward.rs`) carries it back through every operation of the cycle in reverse graph
order: each segment (a layer, or the prologue) is replayed forward from its checkpoints on the
card with the same `enact_operation` the cycle uses, then walked in reverse; every operation takes
the accumulated differential of its output, returns one through each input by its own adjoint
law, and every contraction deposits one factorized overlay atom on its cross-section. Differentials
meeting at one carrier accumulate by re-entry. Every returned differential is sealed to its
midpoint under its own census, the declared successor projection the forward already uses, so the
return does not carry the wrapping of earlier enclosures; the width it collapsed is in the trace.

[implemented-exact; measured] The adjoint laws, each with its card test:

| reaction | adjoint kernel | control |
|---|---|---|
| contraction | `section_contract_transposed_partial` into the split-K wide standing, `section_contract_join` rounding once | exact agreement with the serial chart on a single-tile population (5,120/5,120) and across the sixteen tiles of the tied embedding (2,560/2,560) |
| deposit | `section_transpose_seal` (`u = −η dᵀ`), carry or coarsen and seal (`v = x`) | factor grains derived from the consuming contraction's carrier budget with a ten-octave margin |
| tanh | `section_one_minus_square` | `1 − (3/4)² = 7/16` exactly |
| GELU (tanh form) | `section_gelu_tanh_derivative` | encloses `½` at zero and `(1.08, 1.09)` at one; a finer grain nests |
| RMS rebase | `section_rms_rebase_adjoint` (root re-derived as the forward; second barrier `Σ g x dy`) | encloses the exact derivative at two grains; the finer nests |
| chronology | `section_chronology_adjoint` (the sine negated) | the same bands and positions turned back |
| causal contact | `section_contact_adjoint_queries` (writes `w` and `ds` scratch, returns `dq`), `_keys`, `_values` | equal keys make the weights exactly ½: `dq = 0`, `dk = (½,0),(−½,0)`, `dv = (½,0),(½,0)` returned as exact points |
| select / reshape / add / Hadamard / scale | `section_place_columns`; carry; carry; Hadamard with the other operand; scale by the same enclosure or aligned coefficient | exact by construction |

## The deed

[established-bounded; implemented-exact; measured] `applications/athena-alpha/examples/adjoint_return_ske1.rs`,
one bounded process per mode on the resident Gemma 4 E4B text operator, the same two
occurrences as the HNA4 deed (context of 5 addresses, continuation of 6):

| | return (`k = 16`, series 14) | control |
|---|---|---|
| process | 49 s (mount about 40 s) | 45 s |
| first cycle | 2.2 s, ` Explain` | 2.2 s, ` Explain` |
| return through the body | 3.3 s | none |
| operations returned | 1,271 | 0 |
| segments replayed | 43 (42 layers and the prologue) | 0 |
| cross-sections deposited | 344 (343 in the body, the tied boundary) at rank 5 | 0 |
| overlay rank carried | 1,720 | 0 |
| lookups reached | 2 (main and per-layer embedding) | 0 |
| second cycle on the deposited body | 7.1 s, ` the` | 3.6 s, ` the` |
| second emission words (fold digest) | `d29765f20de93dad` | `a6ca2e52e1fe622d` |

The tied-only deposit of the HNA4 deed gave `4678da9f3162c555` on the same occurrences: the
three bodies emit three different word populations at one step while the maximum did not flip.
Every layer's returned differentials were nonzero on roughly 420,000 to 486,000 coordinates over
26 to 33 operations, with width zero after sealing.

[counterexample; measured] Two refusals on the way were mine and are recorded: a Hadamard shaped
with the derivative factor's wide admission instead of its word bound, and a four-octave margin
in the deposit's factor grains that the next cycle's wider input exceeded by two octaves. The
partial standings of the contraction adjoint were also never released by the surface, which ran
the card out of memory after eight layers; `release_partials` now frees each after its join.

## Verification

[process-audit; measured] Workspace type-check of every target passed; the engine library
returned 2,020 passed, 0 failed, 32 ignored; the HNA5 three-application matrix regression returned
the same three faces with its release receiver true in 54 s; the HNA4 deed, which now enacts the
full return, still returns. The adjoint kernels and their records were moved into their own units
(`kernels/exact_resident_adjoint.cuh`, `resident_section/surface_adjoint.rs`) so the source-shape
gate is red only on the two files that were over the ratchet before this campaign.

## Grade

[established-bounded] **SKE1 PASSED at its declared scope.** One continuing occurrence returns an
exact resident differential carrier at every operation of the cycle and a support at every reached
population; overlays deposit on every contraction cross-section, not only the tied one, and the
next cycle's face differs from the control; the transposed contraction agrees exactly with the
serial chart; every process is bounded. The sectioning clause of the pass condition was not
exercised: the whole return closes in 3.3 seconds, and rest and remount of the returning carrier
were not needed.

[counterexample; source-inspected] What this does not claim: the 210 normalization gains, 42 layer
scalars, the per-layer projection norm, and the two embedding populations receive no deposit; their
differentials are formed (the embedding rows reached are reported) but no deposit law for vectors,
scalars, or gathered rows is enacted. The overlay is not yet part of the session rest. No
qualitative claim follows from one step at one continuation.

# The plan audit reconnects shared laws, consumers and hardware

**Date:** September 19, 2026, America/Los_Angeles (September 20 UTC).
**Inspected source:** `457fd827`, including today's Wave 8/9 commits and their receipts.
**Scope:** the construction state, complete roadmap, relevant shared/device/identity/tube/atlas
contracts, changed mathematical and native owners, M5 experiment, original Codex and Claude
messages, and all 55 GitHub issues. Three bounded Luna reviews supported source, issue and
identity-contract inspection; the primary checked their findings and integrated the repairs.
This record is evidence and proposed issue editing, not a second construction order.

## Findings and repairs

[established-bounded; source-inspected; process-audit] **The hardware failure is a missed
consuming connection.** `exact_linear::reduced_row_echelon` performs sequential rational
Gauss–Jordan; its rigidity and chain callers do not enter the existing hardware-cover/device
path. The Wave 9 receipt records 568 s at 180 coordinates, a 324-coordinate run without a return
after 39 minutes, and a 612-coordinate assembly refusal. This does not establish that the
previously checked native field/session kernels broke. The earlier device returns and these new
host library consumers are different evidence scopes.

[established-bounded; process-audit] The original decisions are recoverable, not inferred from a
later confession. Claude's `b54c405b-ee36-4f84-b16c-a1d76d400170` lines 376/378 dispatched exact-Q
library owners; line 906 assigned theorem-backed host chain/spectral work; line 1264's next-step
proposal optimized sparse assembly and repeated form construction. Brandon identified the
thread/device problem at line 1279. Searches at 1283/1292/1295 then exposed the rational path and
existing modular material. After the hardware-methodology correction at 1315, searches at
1319/1327/1330 recovered `hardware_cover`, launch/layout and the August record, followed by the
roadmap correction at 1334. The then-governing AGENTS/roadmap already assigned hot native work
to the device. These coordinates establish a recovery/application failure; they do not establish
a hidden psychological reason for it.

[established-bounded; source-inspected] **The proposed repair still overstated what is already
connected.** `section_layout::SectionKernels` realizes a generated gather/local-apply/scatter
operation in one ring. `section_layout_adoption` explicitly describes a cross-check, not a
production adoption for signed coordinates/radii. A modular image is not a rational solver.
The device contract now names denominator handling, bad primes, independent kernel witnesses,
solve/RREF/reconstruction certificates, and the distinction between modular arithmetic and
ordered inertia. Rank/kernel/solve and sparse assembly are the first consumer increment;
finishing characteristic polynomial and general inertia is not a prerequisite to that return.
The contradictory CPU-first instructions were removed from local state and worker guidance.

[counterexample; computational-witness; source-inspected] **The proposed static neck claim in
#55 is false without a new factorization.** `elastic_chain` builds `A = −JᵀJ` with `G=I`.
`HolonicChain::transfer_at` uses an ordinary inverse and refuses a pole. The network has at least
its rigid-motion nullspace, so `C K⁺ B` cannot simply be called `H(0)`. On a four-site path cut
into two pairs, K's cross block has rank 1 while the pseudoinverse cross block has rank 2; the
exact calculation is below. A gauge or projected force changes the boundary/support relevant
to the neck theorem. The [new static-response contract](../../docs/plans/THE_CONTINUING_OBJECT_IS_THE_SHARED_CARRIER.md#the-conditioned-structural-response-consumer)
requires the full kernel, compatibility, source force/energy/units, gauge, residual and an actual
response family. It also retains the oriented quadrance difference: `cos²` alone cannot
distinguish the right direction from its opposite. This repairs a proposed construction;
the existing off-pole chain code correctly refuses the singular inverse.

[counterexample; source-inspected] **The identity design had a false all-degree completeness
criterion.** A finite sampled kernel contains all true bounded-degree identities but can also
contain spurious relations. A parametrization can miss components. Buchberger closure proves a
basis for the generated ideal, not equality to the full face-map kernel: `{x}` is closed but
does not generate `<x,y>`. Specialization can add relations (`u ↦ δx` has zero generic kernel
and kernel `<u>` at `δ=0`). The identity contract now requires chart coverage, whole-basis
certification, a complete-ideal equality certificate and explicit special-fibre returns. It
separates Hilbert dimension from encoded cost and the continuing decoder equations. These
repairs preserve the construction's ambition and make its promised return testable.

[established-bounded; source-inspected] **Several cross-domain links were names where maps
were needed.** `Border.lean` formalizes degeneration/blindness, not Gröbner completion;
`derivation_atlas` route homology is not automatically a syzygy resolution. Contact `d*⋆d` is
one Hodge term; harmonic classes require both relevant differentials or a declared restriction.
The dynamic transfer rank does not bound an arbitrary Mayer–Vietoris connecting map without
a factorization. The identity and tube contracts now state the actual connecting maps and
residuals. The owner map adds short relation-to-consumer routes using alternate vocabulary;
it does not add a per-file inventory or another registry.

[established-bounded; source-inspected] **Status drift was live, not merely historical.** The
roadmap called T6/T8 unreturned after their delivery; its biological row still requested the
predictor run; its chain row kept the elastic analytic-width refusal open; the device plan
requested adjoint/D1/D2 work already receipted; the state said no executable source had changed
since the morning review. Those clauses are corrected in place. The old M5 measurements remain
intact with a reading-order note. Two stale chain API comments now describe the structural width
route. R7's generic atlas obligations remain distinguished from the later chain specializations.

[counterexample; source-inspected] **A source comment and #41 also forbid a possible triangular
case.** Port-Hamiltonian form does not imply nonzero feedback in both blocks: take `G=I`,
`Ω=[[0,1],[−1,0]]`, `M=[[1,1],[1,1]]`. M is positive semidefinite, while
`A=(Ω−M)G=[[-1,0],[−2,−1]]` has zero return block. The chain source comment now says to check
triangularity on the full generator. The general bidirectional implementation remains valid.
The issue's phrase “only splitting” should likewise describe the chosen constitutive
realization, not assert uniqueness among equivalent presentations.

[established-bounded; source-inspected; measured] **Today's useful work remains useful.** Wave 8
returned the interaction, neck, jet and two-media join. Wave 9 removed unnecessary resolvent
work from the full-port rank reading, added structural spectral licences, and compared 57
presentations, including measured structures and predictor populations. Its 30 chain readings
and scoped contact/section differences are retained. A theorem-forced reading is a legitimate
inferred consequence; it is not an independent empirical test of that same theorem. The
remaining nullity/spectral Lean lifts (#34/#54) are stated separately from the standard external
theorems the implementation uses. Neither these formal gaps nor an uncompleted product revoke
the bounded results already obtained.

## Direct-message recovery

[historical; process-audit] The repository evidence codec was followed: original user-role text,
excluding harness messages, tool returns, copied child context and generated summaries. No raw
private transcripts are included. Times below are UTC; all belong to September 19 locally.

| Original occurrence | Governing meaning retained |
|---|---|
| Codex `01a0a067-40b7-7420-9d7c-78a34b558023`, line 13720, 17:02 | The general embedding is central; protein designs are comprehensive test objects, music exposes modal relations, robotics remains a thought. Software and documentation should unify while the applications develop. |
| Codex `01a0bb09-2488-7c30-a3c3-b342765e8b85`, lines 7 and 260, 18:58 / 19:19 | The siphonophore discussion is research synthesis, explicitly not a new construction queue. Whole and part are simultaneous recursive roles; the organ interpretation concerns internal composition and reciprocal sustained-cycle dependence under separation. |
| Same Codex task, line 318, 20:16 | A Rubik configuration is a face; relate graph projections to the existing torus/winding/Möbius and navigation constructions, preserving the configuration fibre. |
| Claude `b54c405b-ee36-4f84-b16c-a1d76d400170`, lines 3, 147 and 399, 17:41–18:17 | Issues were approved after corrections: mechanics over product labels; no workflow milestone or waiting-for-ruling state; bounded delegation; real execution as feedback. The subject is the chain of necks/media crossing time and entropy. |
| Same Claude task, lines 662 and 723, 20:57 / 21:08 | Read the side deposits, recover notation, and connect Hodge/spectral/complex-Euler/NS owners. Broader mathematical source is part of this construction. |
| Same Claude task, lines 967 and 1008, 22:56 / 23:10 | The helical geometry and automatic identity/landmark discovery are shared framework/generator/compression work, explicitly not merely a side application. |
| Same Claude task, lines 1279 and 1315, 00:48 / 00:59 September 20 | Audit single-thread/device use and reuse the already established hardware-surface methodology; have Astra refine plans and issue organization before another wave. |

[project-postulate] These messages reinforce one operation with many receiving charts. They do
not require a siphonophore subsystem, a robotics campaign, every conjecture endpoint before an
application, or a second scheduler. The shared-carrier contract now carries their operative
reason. Identity discovery and the time/entropy join remain explicit shared work; a blanket
“research spin-offs” disposition loses their direct authorization.

## GitHub issue disposition

[established-bounded; source-inspected] At inspection the public tracker contained **55 issues:
42 open and 13 closed**, with four milestones. No formal `blocked_by` edges were present in the
queried dependency arrays; dependencies were carried in prose. This is not inherently an API
defect: a precise named dependency in the body is sufficient. The material problem is unclear
consumer boundaries and contradictory bodies/comments. Remote issues were read, not changed.
The following is a concrete edit set for review, not a report of remote mutations.

[interpretation; agent-inferred] Keep the mechanics/kind/layer labels. The four delivery views
are useful if they cross-reference shared owners. Do not divide the engine into “protein,”
“Athena,” and “mathematics” implementations. Add a **Certified identity families and reuse**
delivery milestone for #49/#48 if that return is to be tracked as a milestone; until then, name
it explicitly in the roadmap rather than treating an unmilestoned issue as unimportant. Restore
#5 to the interaction-chain delivery view because the direct chain request includes time/entropy.

| Issues | Concrete disposition and reason |
|---|---|
| #50 | Retitle **Certified exact algebra at the resident rigidity and chain consumers**. Replace the withdrawn CPU-first scope in the body, not only a comment. Keep one shared owner; stage rank/kernel/solve + sparse assembly first and defer independent characteristic/inertia extensions to their actual consumers. Use the replacement contract below. |
| #12–#15 | Keep signed words, radius, operator-family/registry and context lifetime as distinct device seams. Link only the seams #50's chosen representation actually consumes. Modular algebra does not automatically require every signed-enclosure extension. Update the resident-execution milestone description to include #50's rational-certificate return. |
| #55 | Retitle **Conditioned RBX1 elastic response with declared forcing, null fibre and measured residual**. Replace `H(0)` and unqualified neck-rank claims with the shared-carrier response contract below. #50 supplies the large solve; #52 is an intake dependency only if the chosen material route needs it. |
| #49 / #48 | Keep shared identity discovery separate from the helical consumer. Replace #49's sampled-kernel/Buchberger/Hilbert overclaims from the corrected I1–I8 contract. Small certified instances need not await the full #50 scope. |
| #27 / #28 / #10 | Narrow #27's primary return to serial multi-neck composition and its boundary/feedback/port laws. Keep geometric area/slip as a visible separate obligation: reopen #28 or retain an explicit unchecked owned subtask. Its closure was **folding into #27, not implementation**. Retain #10's physicochemical interface law. Hodge/Mayer–Vietoris maps compose with this work but do not share an automatic rank theorem. |
| #5 / #29–#31 / #24 | Keep distinct laws: time/entropy, contact variation, covariant closure, full balance, moving metric. Link their shared interaction owner and specific consumers. #5 is directly requested; these are not all incidental spin-offs. |
| #9 / #51 | #9 owns the complete evaluation population, receiver conjunction and count. #51 can own a declared sampling-tolerant rigidity receiver; move its `Split`/`PluralFibre` issue back under #9. Choose the receiver/tolerance from the scientific question before evaluation, not to force seed unanimity. If the other receivers remain unresolved, report contact-only scope instead of a joint-agreement number. |
| #38 / #44 / #53 | #38 owns the hinge-versus-packing discriminator. Keep #44's window comparison and #53's outlier evidence as separate measurements only where each body names a new comparison; otherwise retain their evidence and close the answered observation. For #53, bound-partner and binder-interface comparands must actually exist and share the declared receiver; a target-chain outlier is not a binder-design failure. |
| #34 / #54 | Preserve the nullity and spectral formal lifts with their exact theorem hypotheses. Link from the native licences they support. They are not universal blockers for already scoped standard-theorem use, nor evidence for the unproved static pseudoinverse claim. |
| #35 / #32 / #33 / #39 | Retain general pole factors, physical source maps, arbitrary-step jet lift and changing/aperture chain readings as distinct obligations, cross-linked to their consuming owners. The structural elastic-width closure #36 does not close #35. |
| #37 / #45 / #36 | Keep completed or superseded scope explicit: #37's cost work transferred to #50; #45's resolvent obstruction improved, while whole-complex execution remains #50's return; #36 closed the elastic structural case. A closure must name the remaining issue, not imply the whole machine scales. |
| #25 / #26 / #40 | Rewrite #25 as **Navigate active relations from formal law through native call to consumer**; remove the 19%-coverage/per-file-table objective. #26's false-absence assertions and #40's constructor/remount defects are separate bounded audits, not reasons to build a new census. |
| #41 / #7 / #47 | Correct #41's false prohibition of triangular port-Hamiltonian generators and its unsupported “only splitting” wording using the source repair above; retain it as a decision record and close after preserving concrete follow-up links. Keep #7/#47 historical closed trackers with their checked receipts. Do not dispatch work because an old tracker checkbox is stale. |
| #46 / #52 / #11 | Keep predictor-tool behavior, reusable mmCIF intake and second-structure validation distinct. The intake issue belongs to `codec`/Rust as well as its application view; it is not a new protein-specific mathematical engine. |
| #6 / #20–#23 / #16–#19 | Preserve address-generic families, receiver refinements, specialized formal work and the complete-source field consumer at their existing scopes. Link actual dependencies; neither one milestone nor all mathematical specialties are prerequisites for every useful return. |

### Replacement contract for issue 50

[definition] **Problem:** the actual rigidity/chain/contact consumer performs sequential host
rational work and refuses or stalls at full target/complex extent. **Operation:** a shared
certificate-bearing rank/kernel/solve under the existing `exact_linear` API, with sparse contact
assembly and retained factors. Reuse `hardware_cover`, `launch_law`, `section_layout` and the
appropriate generated-tile/SIMT realization. A prime image owns a bounded arithmetic chart;
rational reconstruction and verification own the return. Check nonzero minor plus independent
kernel basis, exact solve/fibre or incompatibility, and canonical row-space equivalence where
RREF is requested. A single fixed modulus is not the rational representation.

[definition] **First return:** the 324-coordinate target and 612-coordinate complex through the
real consumers, with exact comparison to the reference on supported scopes, initialization,
resident work, reconstruction, transfers and delivery measured separately. Device ownership must
be observable in the consuming operation. **Later dependent returns:** characteristic polynomial
and ordered inertia certificates as those receivers need them. No promised seconds or mandatory
CPU-first production phase. #49 and #55 consume this owner; #12–#15 are linked only where their
representation/lifetime seams are required. The [device contract](../../docs/plans/THE_EXACT_DEVICE_LAW_IS_CONSTRUCTED.md#the-shared-exact-algebra-consumer)
is the full source of the implementation assignment.

### Replacement contract for issue 55

[definition] **Problem:** test a source-conditioned elastic response of RBX1, rather than only
reading external predictor conformations. **Operands:** source configuration/environment and
residue map, declared elastic energy/stiffness/units, forcing family with directions and magnitude
law, and a separately retained measured comparison. Report any bound-contact conditioning.
**Operation:** full-kernel compatibility and a declared gauge/complement for `K δq = f`; retain
the null fibre, projected-force remainder and constitutive residual. Reuse `preimage_fibre` and
the rigidity kernel. **Receiver:** oriented predicted/measured quadrance changes, their residual,
signed agreement and controls; a squared cosine is additional, not sufficient.

[definition] **Neck claim:** derive the actual static factorization, including any gauge/support
changes, and use independent admissible forcings to read its image/rank. One measured displacement
tests compatibility with that image, not its rank. **Return:** the generated response, null and
measurement uncertainty, controls and cost, including failure of the local model where observed.
The [shared response contract](../../docs/plans/THE_CONTINUING_OBJECT_IS_THE_SHARED_CARRIER.md#the-conditioned-structural-response-consumer)
owns these equations. A complete physical folding model is not required to use this bounded
response, and its result does not complete general structure prediction.

## Verification

[established-bounded; source-inspected] The primary inspected `holonic_chain::{elastic_chain,
transfer_at,rank_reading,chain_widths}`, `exact_linear::{reduced_row_echelon,kernel_basis}`, the
rank theorem's `hX`/`hQ` hypotheses, the rigidity kernel receiver, generated layout/adoption,
and the actual algebraic/matroid/border owners. Existing Wave 8/9 and morning native receipts
were retained; unchanged engine/device/Lean suites were not rerun for documentation edits.
No runtime implementation or theorem body changes in this review.

[counterexample; computational-witness] A standalone Python standard-library `Fraction`
calculation on the four-site path checked `K K⁺ K = K`, `K⁺ K K⁺ = K⁺` and symmetry of `K K⁺`;
K and K⁺ are symmetric. It returned cross ranks **1 and 2**, with pseudoinverse cross block
`[[-3/8,−1/8],[−5/8,−3/8]]`. The same exact row reduction returned sampled rank 1 for
`[1,0,0]`, separating a sampled `{1,x,x²}` receiver from its full dimension 3. An initial
optional SymPy invocation found no installed module; the calculation used `Fraction` instead,
without installing anything. The ideal/component/specialization counterexamples above follow
by direct symbolic substitution and membership, not an experimental completeness assertion.

[established-bounded; source-audit] Recovery searches at `457fd827` included
`.agents/bin/prior-art 'modular rank|rational reconstruction|prime image|fraction.free|fibre_rebase_step|CRT|Chinese remainder'`
(77 matching files) and
`'identity discovery|vanishing ideal|ideal membership|hilbert function|polynomial identity testing|sampled kernel|rational parametr'`
(9 matching files), over the script's formal/crates/docs/research-record aperture. The former
recovers existing lifting, factor/rebase and device methodology; it does not establish a general
matrix solver already connected to the chain. The latter's actual owners are distinguished
above rather than declared absent from a filename search.

[established-bounded; source-inspected] The changed documentation's relative file links and
target headings, source-comment-only diff, and `git diff --check` were checked before delivery.
Untracked side-session deposits and editor directories were preserved. The GitHub issue
recommendations above are reviewable locally; no issue comment, closure or metadata change was
sent to GitHub during this audit.

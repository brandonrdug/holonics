# Causal entanglement needs future interfaces and founded generators

[project-postulate] Brandon's September 11 continuation connects receiver-relative meaning,
stateful generator classes, received signals, active protein joints and Holonic causal
entanglement. It directs the compression study toward computations that can be omitted through
higher-level causal constraints. Native Athena expansion remains paused under the preceding
status correction; this return develops the mathematical reference.

## Working causal object and computational consequence

[definition] Holonic causal entanglement names dependence retained through actual causal
histories, interactions, joint constraints and receiver consequences. Keep the addressed passage
and its admitted interventions/continuations. Shared history, transmitted influence and a
joint constraint are different witnesses within that wider object. A current correlation,
co-presence, one equal face or physical distance alone does not supply those witnesses.
A singleton observed state also cannot reveal every counterfactual coupling.

[definition] The common architecture interface is a stateful passage `(state,input) ->
(successor,emission)` with situated receivers and admitted histories. Meaning classes belong to
that context and future receiver family. Thus equal arithmetic output at one input, or equal
wording under one interpretation, need not identify entire generators. Reclassifying the
receiver/context can change which prior distinctions matter.

[proved-derived; formal-checked] The existing `CausalRelevance` and
`ReceiverHistoryCompression` laws supply the relevant reduction criterion:
`q T_i = U_i q`, together with receiver factorization. It extends to every admitted ordered
word without enumerating all histories. A separating continuation reopens the proposed class.
The recent `RatioSeriesTransport` instantiates complete block action and clock preservation;
equal intermediate sums alone do not determine the next sum.

[interpretation] For active-joint computation, q retains the boundary response, relevant internal
memory, source terms and conditions required by subsequent interactions. Reuse that complete
interface while its hypotheses persist, and propagate returned differences along actual
incidence. The existing Schur/diffusion and joint fold/occupancy owners provide concrete local
precedents. A geometric tube with no visible junction is not automatically memoryless or linear;
its closure must cover dispersion, internal forcing and any changing constitutive modes that
can later reach the receiver. The test is sufficient future conduct, not graph degree alone.
This is an intended computational reduction, not a completed protein simulation or HNN policy.

## Physical distinctions retained

[proved-standard] Distinct directions/locations relative to an observer do not establish
spacelike separation or independence. Even spacelike separated events can have a shared past.
A received signal supplies an actual directed propagation witness; a later action is separate
from the reception and any state change recording it.

[proved-standard] Quantum entanglement is nonseparability of a joint quantum state, rather than
an operational definition that a remote choice must change the local state. The
[IBM joint-state lesson](https://quantum.cloud.ibm.com/learning/en/courses/general-formulation-of-quantum-information/density-matrices/multiple-systems)
explains separability and unchanged reduced states under remote unconditional operations.
[Bell/CHSH](https://quantum.cloud.ibm.com/docs/en/tutorials/chsh-inequality) limits ordinary local
hidden-variable explanations under its stated assumptions. The broader Holonic term does not
by itself derive quantum correlations or establish a stronger physical effect; that bridge owes
its states, measurements, causal assumptions and no-signalling behavior.

[proved-standard] Relative entropy contracts when both distributions/states pass through the
same admissible stochastic/quantum channel; see
[Preskill, quantum information, §10.3](https://www.preskill.caltech.edu/ph219/chap10_6A_2022.pdf).
This does not make cross-entropy a monotone function of distance. Channel/source changes,
conditioning, amplification and refocusing require their own analysis. The relevance canon
already separates exact invisibility, tolerance and causal admissibility.

## Generator admission is stronger than bare causal provenance

[project-postulate] A proposed generator must have a construction and an evidential relationship
to the source transitions and constraints it is meant to describe. A caused computation alone
is insufficient: an erroneous assertion also has a causal history. The enforceable aim is to
exclude unsupported admission and preserve unresolved alternatives, rather than silently
asserting that every future prediction must be correct. This does not impose a universal
certainty gate on useful action.

[proved-derived; formal-checked] `Computation/GeneratorObservationScope.lean` proves that adding
observations restricts a declared compatible generator family. It also proves an explicit
finite-data limit. For every prefix length N, the functions

`g_k(n)=2^n+k product_{i<N}(n-i)`

agree with the exponential on the whole observed prefix and differ at the next input for
distinct rational k. A simpler control compares `2^n` with
`1+n+n(n-1)/2`: both return 1,2,4 at n=0,1,2, then return 8 and 7 respectively. This is a
counterexample to unrestricted unique identification from those observations, not a claim
that all consistent generators have equal statistical or causal support.

[proved-derived; formal-checked] The positive counterpart is also explicit. In the declared
constant-multiplier family `a r^n`, a nonzero initial amplitude and equal next observations
force the same r and therefore every subsequent value. At a=0 all multipliers are hidden;
any nonzero excitation separates distinct multipliers on the next observation; an arbitrarily
small positive rational excitation suffices for an exact receiver. Finite measurement tolerance
is a separate condition. This supplies
a concrete receiver-relative version of a small catalyst making a dependency visible.

[proved-derived; formal-checked] An additive implementation `x_(n+1)=x_n+x_n`, x_0=1, is exactly
`2^n`. Generator classification therefore concerns the complete law and its admissible behavior,
not whether its implementation happens to contain addition or exponentiation instructions.

[established-bounded; process-audit] The focused
`lake env lean ElementaryHolonics/Computation/GeneratorObservationScope.lean` check passes.
No native inference/cultivation code changed. The new formal module and the existing future
receiver laws provide study constraints; no learned semantic classes, error-free intelligence,
physical force identification or computational complexity bound is claimed.

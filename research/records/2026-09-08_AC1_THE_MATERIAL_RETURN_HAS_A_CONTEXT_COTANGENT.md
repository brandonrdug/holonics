# The material return has a context cotangent

[definition] This continues AC1–AC2 from the
[ordinary contextual integration](2026-09-08_AC1_ORDINARY_RECEPTION_CONDUCTS_THE_SOURCE_AND_ITS_AVAILABLE_CONTEXT.md).
It identifies the missing source-side return using the actual conversation model. It introduces
no learner, selected cause, change to the model, or useful-language claim.

## The current source boundary

[established-bounded; source-inspected] `section_constitutive_field` first prepares the raw
relation and paired junction, then calls `contextual_material_prepare`. That call reads the
complete context and forms two material-operator contacts. It does not return a context
cotangent to the junction/current producer. On a successful fixed incoming/source/frame path,
the paired covariance, junction current and raw relation formation do not depend on the learned
material operator. Its arithmetic refusal can prevent the complete publication, and its text
output can influence later exterior inputs; those are distinct effects.

[established-bounded; measured] The direct-sum and bilinear sixteen-family trials have identical
development records, lineage through the prompt end, and complete junction at the first response,
despite their different material charts and answers. The [cold history observer](../../crates/holonics-hna/examples/alpha_contextual_lift.rs)
reads the current bilinear checkpoint through `SavedTextField` and the existing field inspector.
It exports 5,064 historical material reports with **zero native deeds**, 5,064 explicit cold
section reads and 226,543,104 readout bytes, in 49.51 seconds. The model is unchanged.

[established-bounded; computational-witness] Among 5,063 linked development receptions, the
pre-contact material differential predicts **413 complete codewords** correctly; **74** readings
have an unresolved differential. This is chronological prediction on development data, not a
held-out evaluation or a language-quality score. Numerical uncertainty does not explain most
of these missed codewords. The native operation formed 653 equal-source reference families.

[definition] The ordinary text loop is an explicit exterior codeword return: it decodes signs,
then re-enters a unit codeword. That alone is not a proof of an erroneous native operation.
The alternative exact-current ingress cannot accept an enclosed material current by dropping its
radius. The source-side derivative below is a more specific missing connection than replacing
that exterior operation without a declared contact law.

## The differential retains phase and the source family

[definition] All spaces below have the currently declared unit-reference metrics. Complex
spaces are treated as real inner-product spaces with `Re <u,v>`; Hermitian currents use the
real Frobenius product. A different constitutive metric requires its metric adjoint. Let

```text
u=(1,c),  n=u* u,  Q(c)=u u*/n,
Psi(s,c)=Q(s) tensor Q(c),
f_M(s,c)=M Psi(s,c).
```

Keep the source `s`, actual condition `c`, producing operator `M` and every earlier retained
factor at their producing cut. A returned observation gives `r=y-f_M(s,c)`. This difference
is a vector current; no norm or codeword substitutes for it.

[proved-derived] For a condition direction `d`, put `v=(0,d)`. Differentiating the quotient gives

```text
DQ_c[d] = (v u* + u v*)/n - 2 Re(u* v) Q(c)/n.
J_c[d]  = M (Q(s) tensor DQ_c[d]).
```

This derivative is real-linear, generally not complex-linear. A phase direction is therefore
not obtained by multiplying a real derivative by `i`. Neither a scalar modulus nor independent
real-coordinate magnitude retains this operation.

[proved-derived] Let `A` be the Hermitian covector obtained by contracting `M* r` with `Q(s)`.
For `a=<A,Q(c)>` and `P` dropping the fixed reference coordinate, the metric adjoint is

```text
g_c = J_c* r = (2/n) P (A u - a u).
```

Indeed, the two off-diagonal differential terms pair to `2 Re(v* A u)/n`, and the normalization
term pairs to `-2 a Re(u* v)/n`. Their sum is `Re(d* g_c)`. This proves the adjoint identity
without choosing an inverse or a particular member of a Preimage Fibre.

[proved-derived] The existing implicit material factors also give an executable contraction
formula. Write each ordinary/reference signed factor as `beta_j Psi(s_j,c_j)*`, retaining its
own source and context. Then

```text
A = sum_j Re<beta_j,r> k_s(s,s_j) Q(c_j),
k_s(s,t) = |1+<s,t>|^2 / ((1+||s||^2)(1+||t||^2)).
```

For a directional receiver, no full moment matrix is needed. With
`z=1+<c,c_j>`, `a=1+||c||^2`, `b=1+||c_j||^2`,

```text
D k_c[c;d] = 2 Re(conj(z)<d,c_j>)/(a b)
             - k_c(c,c_j) 2 Re<c,d>/a.
J_c[d] = sum_j beta_j k_s(s,s_j) D k_c[c;d].
```

The addressed prefix pairing already evaluates every required inner product. Signed reference
factors stay in the sum. This is the same source chart and producing operator, not an additional
model or a retrospective reinterpretation of coefficients.

## A nonzero return in the actual conversation model

[established-bounded; computational-witness] The
[exact observer](../experiments/alpha_passive_junction/inspect_contextual_return.py) reads receiving
occurrence **20**, its native reference **12**, and the actual available contexts **19,11**.
The source is occurrence 19, so the producing and pre-reception operator cuts coincide. The
receiver direction is the retained `d=c_19-c_11`; it is not a selected external cause.

[established-bounded; computational-witness] The observer reconstructs all 20 relevant numerical
contexts in their complete outgoing/born-current coordinates and independently checks their
prefix pairings and norms. Symbolic differentiation of their explicit rational kernel expressions
agrees with all 20 directional formulas. A separate complex-phase matrix calculation verifies
the full adjoint identity. The factor expansion of the pre-return forward current also lies
inside its recorded native evaluation bound. No native occurrence is replayed.

[proved-derived] The following conservative bounds certify this receiver despite the numerical
context and operator representations. For the homogeneous moment,
`||DQ|| <= 2` and `||D^2Q|| <= 20` globally in the stated metrics. Expanding the second derivative
produces terms bounded by `2,4,4,2,8` times the two direction norms, using `||u||^2=n>=1`.
Let `E` bound `||M-Mhat||`, `N` bound `||Mhat||`, and `e_c,e_ref` be the retained context radii.
The visible source in this particular corpus return is represented exactly. Thus, for
`dhat=chat-chat_ref`, `vhat=Jhat[dhat]`, and retained residual radius `e_r`,

```text
e_v <= (2E+20N e_c)||dhat|| + 2(N+E)(e_c+e_ref),
e_g <= e_r (||vhat||+e_v) + ||rhat|| e_v.
```

The first line bounds the derivative change and the direction change separately; the second
bounds the real pairing. Every norm ceiling is rounded outward with exact integer arithmetic.
These bounds do not install either numerical representative as the actual current.

[established-bounded; computational-witness] The numerical cotangent receiver is approximately
**0.03047524588566344**, with certified error at most **7.316423326711295e-11**. Its entire interval
is positive. The actual material discrepancy therefore has a nonzero source-side component in
this contextual direction. The [portable receipt](2026-09-08_context_cotangent/return-diagnosis.json)
retains exact rational numerators/denominators, operator chronology and the oriented return.
Its final inspection takes 2.50 seconds after the cold export. This is new derivative evidence on the
ordinary model, not another claim that its earlier contextual contrast is nonzero. The direction
joins two actual historical states; it has not been proved to belong to the current producer's
admitted tangent range. Nonzero `g_c` therefore does not by itself establish nonzero `B* g_c`.
The next producer derivation must resolve that distinction.

[established-bounded; computational-witness] As a separate geometric diagnostic, 28 consecutive
same-source observation pairs would require an operator norm above the final model's certified
norm bound to fit both returned vectors exactly. The bound follows from
`||y_i-y_j|| <= ||M|| ||Psi_i-Psi_j||`, with the full source errors retained. Exact fitting of
all observations is not an alpha requirement, and this diagnostic neither identifies a unique
cause nor proves that increasing the norm would produce useful language.

[established-bounded; source-inspected] The subsequent [paired-producer return](2026-09-08_AC1_THE_PAIRED_PRODUCER_RETURNS_ITS_CURRENT_AND_CONTACT_MORPHOLOGY.md)
corrects the explicit observer's extra per-birth phase and checks the complete native current
identity. That fixed unitary rechart left this record's scalar pairings and cotangent certificate
unchanged. The corrected observer now uses the field's actual contact/current convention.

## The next native composition

[definition] The existing parameter contact returns `r Psi*`; it does not exhaust the forward
word's adjoint. The missing port is the **material context cotangent into the actual context
producer**, with the producing metrics, prior morphology and retained uncertainty. If `B` is
that producer's actual differential, the return continues as `B* g_c`. A cotangent is not itself
a state displacement, so copying its coordinates into the field would leave the constitutive
contact unpaid. `holonic-life::native_intelligence::situated_difference` supplies the existing
explicit-metric, reverse-order adjoint reference; it remains cold reference apparatus.

[proved-derived] One applicable composition of the already used unit coercive contact can be
stated without prescribing an inverse cause. For the actual producer variation `z`, put
`J=J_c B`, and give its variation chart a positive definite constitutive metric `G`. The quadratic tangent
contact

```text
||deltaM||_F^2 + <z,G z> + ||r-deltaM Psi-J z||^2
```

has unique response

```text
w = (2 I + J G^-1 J*)^-1 r,
deltaM = w Psi*,       z = G^-1 J* w.
```

The identity coefficient is two because `||Psi||=1`. Substitution into the stationary equations
proves the formula, and positivity gives uniqueness of this **operative tangent response**.
This does not prove a finite nonlinear inverse, unique hidden condition or successful language
behavior. A native realization must state the producer `B`, its metric and actual successor
law rather than treating this formula as an already integrated update.

[open] Continue AC1 by deriving that producer return from the existing paired-field/current
owners, including old forward carriers and the delayed-return split, and conducting it inside
the same staged successor. Logging a cotangent or adding another cold inspector does not close
this obligation. Then inspect AC2's actual complete responses and contextual revisions. The full
AC0–AC5 goal remains active. Lean, the retired external database and acoustic implementation are
not part of this inspection or the cultivation pipeline.

[definition] Reproduction uses `alpha_contextual_lift MODEL --material-history --report NEW.jsonl`,
then `python -P research/experiments/alpha_passive_junction/inspect_contextual_return.py HISTORY
--receiving 20 --report NEW.json` in an environment containing SymPy. `-P` avoids the historical
sibling `inspect.py` shadowing Python's standard library. Private material remains under
`.local/artifacts/athena-alpha/ac2-contextual-diagnosis/`; the model remains the preserved
`ac1-ordinary-context/first-sixteen-bilinear-v3.hna`.

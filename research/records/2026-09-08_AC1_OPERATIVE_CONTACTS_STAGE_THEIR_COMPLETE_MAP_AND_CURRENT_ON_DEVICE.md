# Operative contacts stage their complete map and current on the device

[definition] This is native implementation work under AC1, following the
[paired-producer return](2026-09-08_AC1_THE_PAIRED_PRODUCER_RETURNS_ITS_CURRENT_AND_CONTACT_MORPHOLOGY.md).
It implements the contact/current carriers and staged rank-two update, not the joint
constitutive-return producer or a published developmental field successor. Athena-alpha remains
unattained and the saved conversation model is unchanged.

## Native carriers and ownership

[established-bounded; implemented-exact] `field/junction/operative.rs` now prepares the full
operative contact columns and internal currents from the existing field's actual births, source
frames and retained potential prefixes. The CUDA mount uses the original addressed source and
receiving fields. It preserves the native phase convention `(-1)^at`; the birth fixes the lower
prefix cut. Empty contact populations remain explicit.

[definition] `NativeOperativeContactStaging` borrows the original field and its decoder. It
cannot outlive that source owner or become another continuing ecology. Its packed resident
sections carry the contact-major complex map `D`, internal current `b`, complete map/current
radii, and covariance/aggregate receivers with their own bounds. A distinct producing-carrier
handle guards each staged return. Equal counts or field ordinals do not authorize applying an
old return to a different staged morphology.

[established-bounded; implemented-exact] The private native-return seam takes two oriented port
factors, two contact factors, an internal-current increment and complete delta bounds. The device
stages

```text
DeltaD = p0 r0* + p1 r1*,
D_next = D + DeltaD,          b_next = b + Delta b,
C_next = D_next D_next*,      h_next = D_next b_next.
```

A second dependent kernel computes the complete moments. Thus both covariance cross terms,
the quadratic map term and the mixed `DeltaD Delta b` term survive. The existing resident
passage mechanism carries the dependency and complete obstruction. Original operands and retained
return factors also determine the full oriented numerical division residuals; scalar rounding
bounds do not replace those carriers. Both kernels write fresh
staging; no field state or source capability is published by them. Successful stages retain
their immutable return carriers; a refusal does not append a return.

[definition] The delta seam has no public constructor accepting an authored learning update.
The native joint constitutive producer still owes those carriers, their source relation,
metrics and bounds. Test-supplied wave perturbations establish the algebraic staging behavior;
they are not conversation training or a substitute learner.

## Enclosure laws

[proved-derived] For one birth column with numerical error `e_d` and prefix difference with
error `e_p`, the native current enclosure uses

```text
|d* p - dhat* phat| <= (||dhat||+e_d)e_p + e_d||phat||.
```

Outward division errors join that bound. The square root of the sum of squared per-row bounds
then bounds the complete map and current. Original frame-normalized rational contacts remain
exact source operands; their dyadic representatives are not installed as exact points.

[proved-derived] If `E_D,E_b` enclose the old carrier and the native return supplies
`E_DeltaD,E_Deltab`, the staged map/current bounds are their respective sums, plus map-product
rounding. With numerical norms `N_D,N_b`, sufficient moment bounds are

```text
E_C <= (2 N_D + E_D) E_D + rounding_C,
E_h <= N_D E_b + N_b E_D + E_D E_b + rounding_h.
```

These are complex Frobenius/Euclidean bounds. Native signed 256-bit accumulators retain both
complex planes; outward grid division and integer norm ceilings pay the rounding. No device-core
float determines a current, coefficient or branch.

[definition] The aggregate radius is a receiver bound. It must not replace the joint `(D,b)`
carrier or be fed independently through the old aggregate-only error recurrence. The full
reflection is nonexpansive in its complete entering current. The next operative reflection must
use that joint geometry and its full residual, not destroy correlation by treating `D b` as an
unrelated uncertain input.

## Verification and actual conversation material

[established-bounded; measured] The two new native controls pass. They cover empty and nonempty
populations, rational complex contacts, source reuse/rechart, the exact original current decoder,
rank-two map/current changes, mixed moment terms and nonzero delta uncertainty. A map change that
fits the carrier but whose squared moment does not fit causes a late refusal in the dependent
moment kernel; the source staging and its return journal remain exactly unchanged, and a later
valid attempt succeeds. A return from a different producing staging is refused.

[established-bounded; measured] All **143 native constitutive regressions pass** in **35.92 s**.
The initial new-kernel attempt failed CUDA graph instantiation with launch-resource error 701,
before its arithmetic ran. The kernels now use the existing field's 512-thread launch bound,
allowing register placement consistent with that launch. This is an execution-placement repair;
it changes no numerical or constitutive law. All **20 SDK alpha tests** also pass in **2.77 s**.

[established-bounded; measured] The public `alpha_contextual_lift --operative-contacts` observer
loads the saved sixteen-family model through the existing text-session owner. Native staging of
its **5,063 contacts at field cut 5,064** takes **0.266005462 s**, with **zero numerical section
readouts** and **4,941,492 ingress bytes**. Every contact column, source/birth address and internal
current centre agrees exactly with the independent existing cold decoder. The map and covariance
radii are zero on this integer-contact corpus; the internal radius is approximately `3.79036e-6`
and the aggregate radius approximately `0.00140137`. Those radii have their declared scopes.

[established-bounded; measured] Native payload is **374,978,736 bytes** after staging and peaks
at **415,658,916 bytes**. The entire process, including model loading, the cold reference and
reporting, takes **79.173356441 s** with peak host RSS **537,420 KiB**. The
[portable receipt](2026-09-08_operative_contacts/native-staging.json) preserves the actual native
and process clocks separately. Complete numerical covariance/aggregate fields remain in the
private report alongside the source model. The existing source model and its failed language response are
unchanged; this was not another cultivation campaign or consumer-efficiency claim.

## Remaining implementation

[open] The prepared carriers still need to enter the field's sole successor publication. The
joint material/current/morphology constitutive return, ordinary contact birth/growth, operative
reflection, material source pairing through changing contacts, chronological current decoder,
and full rest/archive continuation remain to be connected. The original borrowed field and the
private staged-return journal are not substitutes for that durable continuing realization.

[definition] Continue those native owners and keep the producer's old maps, dependencies and
metrics. The changing-contact decoder and covariance/aggregate mixed terms derived in the
producer record remain required. These native staging controls do not close AC1 or AC2; useful
complete English/code/mathematics and the full AC0–AC5 product remain outstanding. No additional
user information is required.

[definition] Owners are `field/junction/operative.rs`, `surface_operative_contacts.rs` and
`field_operative_contacts.cuh`; the text session provides a borrowed staging interface. The
private actual-model artifact is
`.local/artifacts/athena-alpha/ac2-contextual-diagnosis/native-operative-staging-v1.json`.
The corresponding process receipt retains its exact command. The existing section-contact
apparatus was also inspected: its scalar admittance/source-duality deposit and interval sealing
do not provide this complete complex map/current carrier or its mixed moments. Its earlier scope
is unchanged. No Lean execution, external memory database call, Apple kernel or acoustic work
was part of this return.

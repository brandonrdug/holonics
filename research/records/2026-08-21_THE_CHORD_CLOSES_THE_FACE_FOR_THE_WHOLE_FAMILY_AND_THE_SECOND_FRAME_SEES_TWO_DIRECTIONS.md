# The chord closes the face for the whole family, and the second frame sees two directions

**Date:** 2026-08-21
**Kind:** the wall deed — Brandon: *"let's genuinely attack the problems"*, with the call-out
that the BSD wall itself had not been reported on.  Solo orchestrator work, one Lean file;
the closure identity verified over two prime fields and the rank-two data verified in exact
rational arithmetic before encoding.  **It schedules nothing.**
[`blueprint/THE_ROADMAP.md`](../../blueprint/THE_ROADMAP.md) and
[`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) remain the only construction
authorities.
**Position under the active plan.** Exterior mathematical material for Deed M2 of
[`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`](../../blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md).
The Lean line does not couple to the engine, per Brandon's standing ruling.
**Truth grades:** `proved-derived` with `formal-checked` evidence for the eight Lean
theorems (`#print axioms` → `[propext, Classical.choice, Quot.sound]`, zero `sorryAx`);
`interpretation` for the rank-two reading, marked as such in the file itself.

---

## 1. The wall, and what moved at it

The wall is the rank-two organ: the known mechanism condenses the reflection's first
derivative into one realizer, and nothing supplies a second.  The route to fight it runs
through the second frame — the twist family — and the machinery it needs first is the one
proposition that has blocked every descent assembly since the route opened:
**the face homomorphism**.  This deed proves its mathematical heart, family-wise, and then
exhibits the first rank-two realized data the tree has ever carried.

**`theChordClosesTheSlots`** — for **every** twist `y² = x³ − n²x` at once: if a line
`y = λx + ν` passes through two curve points, then at each half-turn abscissa
`e ∈ {0, n, −n}`,

```text
(x₁ − e)(x₂ − e)(x₃ − e) = (λe + ν)²,    x₃ = λ² − x₁ − x₂
```

— the three slot products are literal squares.  The proof is one Vieta evaluation (the
cubic `x³ − n²x − (λx+ν)²` is monic with roots `x₁, x₂, x₃`; evaluate at `e`), and the
kernel certificates passed on the first attempt: the cleared identity's cofactors are
`(x₂ − e)`, `(x₁ − e)`, and `(x₁ − e)(y₁ + y₂ + λ(x₂ − x₁))` against the two curve
equations and the slope equation.  **`theChordClosesTheFaceGenerically`** then puts the
sum's slots in the product square classes with exhibited witnesses whenever the chord
avoids the half-turns.  What remains for the full point-level
`TheFaceIsAHomomorphismEverywhere` is exactly the exceptional-chord bookkeeping — chords
through half-turns, where the classical conventions activate and `FaithfulFace.lean`'s
translation closures already cover the route's two curves.  The heart is no longer open;
the assembly is.

## 2. The second frame sees two directions

On the twist at thirty-four — the smallest classically-known rank-two congruent-number
curve — the file exhibits, all exact and kernel-checked:

- `P₁ = (−2, 48)`, face `(−2, −1)`; `P₂ = (−16, 120)`, face `(−1, −2)`; chord slope
  `−36/7`; sum `(2178/49, 65472/343)` on the curve (`theRankTwoTwistData`);
- the sum's face in the **product class** with square roots `33/28` and `8/105` exhibited
  (`theProductClassIsRealizedAtTheSum`) — the multiplicativity law live on the rank-two
  twist;
- both doubles reading trivial with all four roots literal: `x(2P₁) = (145/12)²`,
  `x(2P₁) − 34 = (127/12)²`, `x(2P₂) = (353/60)²`, `x(2P₂) − 34 = (47/60)²`
  (`theDoubledDirectionsReadTrivial`);
- the three realized faces and the four torsion-convention faces pairwise separated —
  fifteen refusals by signs and by two, seventeen, thirty-four failing to be squares
  (`theSevenClassesAreSeparated`).

The reading, carried as a reading: granted the homomorphism whose heart this file proves,
seven separated classes mean `P₁` and `P₂` are independent modulo torsion and doubles —
**a two-dimensional realized direction visible to the descent receiver**.  The wall's
question is where second realizers *come from*; this is the tree's first exact record of
what a second direction *looks like* to the face, which is the instrument any supply
mechanism must feed.

**State after the merge, measured 2026-08-21:** `Millennium/` is **67 files, 22,041
lines, 1,021 top-level `theorem` declarations**
(`grep -h "^theorem " ElementaryHolonics/Millennium/*.lean | wc -l`; three files are
Sol's); the library builds at **3,392 jobs**.

## 3. What is owed, with falsifiers

**None scheduled; the roadmap alone schedules.**

| owed | falsifier |
|---|---|
| the full point-level homomorphism assembled from this heart plus the exceptional-chord conventions (tangent case = this identity at `x₁ = x₂` with the tangent slope; half-turn summands = the translation closures) — discharging `Descent.TheFaceIsAHomomorphismEverywhere` and `RankOne.TheFaceIsAHomomorphismEverywhere` | a pair of points whose sum's slots leave the product class |
| with the homomorphism: the unconditional mod-2 independence certificate for the thirty-four twist, and the torsion classification for its frame (the winding-census machinery re-aimed) | a relation `aP₁ + bP₂ ∈ torsion` the face fails to refuse |
| the Selmer enumeration for thirty-four bounding the rank above (the local refusals, Monsky-matrix face) — closing rank exactly two | a fifth Selmer class the local frames admit |
| the supply question itself, now stated on its instrument: what analytic or governance datum *produces* the second direction the face sees — the Rédei linkage and the plectic program are the candidates | — |

## 4. Boundaries

Rank exactly two at thirty-four is not claimed; the independence reading is conditional on
the homomorphism whose generic heart alone is proved; the exceptional-chord assembly and
the twist's own torsion classification are named owed.  Nothing about the
Birch–Swinnerton-Dyer conjecture.

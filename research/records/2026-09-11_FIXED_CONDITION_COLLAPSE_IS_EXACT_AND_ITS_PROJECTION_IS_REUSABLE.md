# Fixed-condition collapse is exact, and its projection is reusable

[project-postulate] This continues the Athena goal from `1697bed1`. The actual model emitted
newlines from 73-way ties. This return traces that ambiguity into its saved local law and uses
the same invariant to reuse source-contact geometry. It does not change the answer policy or
promote this fixed-condition calibration into a useful model.

## The actual lost distinction

[established-bounded; computational-witness] The [executable cold audit](../experiments/coupled_condition_collapse/derive.py)
reads the exact calibration fields (sequences 34–65), their actual text/part/event witnesses,
the declared symbol chart and `local-law.fibre`. All 9,254 presented rows belong to the saved
native relation. Their source rank is 146 and the witnessed vertical rank is 72; together they
match the native relation's rank 218, proving equality of the presented span and stored relation.
This checks the full saved relation at the declared source cut, not a few selected withdrawals.

[established-bounded; computational-witness] There are 686 distinct source pairs and 361 with
multiple observed continuations. Equal-pair differences give 71 independent vertical directions.
One additional signed ten-edge cycle cancels both source ports and supplies the remaining
independent direction. The [complete witness](2026-09-11_fixed_condition_collapse_receipts/complete-witness.json)
retains every signed comparand's sequence, part, row and event. Its rank is checked over exact
rationals. No digest, approximate SVD, source label or target-generated classifier establishes
this result.

[established-bounded; computational-witness] The output alphabet has 73 symbols. Let y denote
the absolute next current `η+c_source`, using the same source coordinate already present in the
law. Every native basis row has zero imaginary y, no y outside that alphabet, and the conserved
real sum `Σ y=Re(h)`. The pure-target rows span all zero-sum directions on those 73 symbols.
Thus every nonempty fibre at an admitted unit-source/h=1 query is the entire rational unit-mass
affine hyperplane on that alphabet. This family includes signed currents; it is not merely a
probability simplex.

[proved-derived; formal-checked] `HolonicConstitutiveFibreCollapse.fibre_collapse` proves the
shared local statement: a paired-current submodule containing all vertical basis differences,
with the stated mass conservation, has exactly the corresponding affine mass hyperplane as
every nonempty fibre. `vertical_span_eq_mass_kernel` identifies that vertical space; the
same-source and translated-fibre lemmas connect actual paired observations to it. These are
local rational-linear laws, not a global linearity assumption about HNN or language.

[proved-derived] For s active coordinates and `Σ y_i=1`, expansion gives
`Σ y_i²=1/s+Σ(y_i−1/s)²`. Therefore its unique minimum-norm receiver is `y_i=1/s`.
The observed 1/73 score and 73-way tie are consequences of this actual material and receiver.
Changing a tie breaker would select another member of the same unresolved source class.

[established-bounded; computational-witness] The audit also checks every saved basis row against
`Σ Re(source.current)=Re(h)` and `Im(h)=0`. At a unit-source query, a compatible condition
preimage therefore has h=(1,0); an empty preimage preserves the same prior under the existing
contact law. Adding more unit-source/h=1 rows preserves these identities and cannot shrink the
already-present vertical subspace. This is why that calibration cannot develop the needed
contextual distinctions merely by adding further examples through the unchanged chart.

[open] The next construction must supply actual situated source/condition distinctions and their
family-aware material return, retaining the coarse source fibre through any refinement. The
existing native current/history receiver retains actual incoming carriers, prefix state,
transport/frame and numerical families; its centre is not a point condition port. The constant
unit condition does not bind those objects. Existing constitutive local laws remain valid at
their declared scopes; a single accumulated linear relation is not the required interacting
contextual assembly. Further fixed-condition exposure is not the product repair.

## Reuse of the same exact vertical geometry

[definition] For a fixed local relation L, each nonempty point-query fibre is `b+V` with
`V=ker(π_source|L)`. The joined arrival uses directions `D=0×V`. The unit-admittance reaction
`h'=P_D h+(I−P_D)b` changes with its actual h and b, while the projection onto D is reusable.
The producing cut, complete current family, incoming/returned normals and original obstruction
remain separate operands and evidence.

[definition] `ResidentWaveSourceGeometry` compiles the exact D matrix and its existing Gram
projection graph on the GPU. An admitted `ResidentWaveRelation` shares that immutable derived
geometry through a lazy cell at its producing law cut. Each nonempty contact checks every
incoming direction against the compiled matrix before applying the same projection arithmetic.
Empty evidence retains actual standing and its outside disposition. A changed producing law
invalidates the old relation binding; a new relation derives new geometry. No continuing ecology
is cloned, and no cached matrix substitutes for its source/condition incidence.

[definition] The geometry is derivable execution material and adds no semantic wire field.
Cold source-rest reconstruction still checks the uncached contact against the saved source and
full prediction; coupled remount then rebuilds the actual producing-law geometry. Existing rest
formats and complete family outputs are unchanged. The prepared contact uses seven real-width
scratch vectors instead of the uncached path's nine; graph construction uses resident workspace.
No float or intermediate host numerical reading selects current or geometry.

## Verification

[established-bounded; implemented-exact; computational-witness] The dedicated reuse control
compares full arrival rest and all contact vectors with uncached execution across plural and
outside-domain arrivals. A subsequent material change opens another vertical direction: the old
producing relation refuses, the new contact matches its uncached reference, and deliberately
reusing the old direction matrix is rejected. All 15 coupled controls and four session controls
also pass, including source-field/process continuation and the actual observation map.

[established-bounded; process-audit] The focused Lean file check and
`lake build ElementaryHolonics.Computation.HolonicConstitutiveFibreCollapse` pass. The latter
returns 3,008 dependency jobs; this is the new theorem's dependency closure, not the full
framework/research build. Native commands are `cargo test -p holonic-engine --lib
source_geometry_reuse -- --ignored --test-threads=1`, the `normal::direct::wave::coupled::tests`
filter, and `cargo test -p holonics-hna --lib native::coupled_wave::tests -- --ignored
--test-threads=1`. The `conversation_coupled` example and public workbench binary build. The full source-field comparison returned below.

[established-bounded; implemented-exact; computational-witness] The same actual sequence-66
field returned 199 passages, with 197 plural predictions and two outside-domain predictions.
Its complete 78,858,045-byte `model.wave`, prediction fibres, symbol chart, source cursor and
progress state are byte-identical to the previously validated uncached-geometry artifact.
Normal/local observations remain 8,965/9,254 and the successor is epoch 11. No intermediate
numerical readout occurs in the field. The [comparison receipt](2026-09-11_fixed_condition_collapse_receipts/source-field-comparison.json)
names both retained artifacts and the complete compared files.

[established-bounded; measured] Native field time changed from 287,069 to 148,563 ms; total
reported field/observer/save work changed from 310,622 to 171,764 ms. Peak resident allocation
changed from 385,550,120 to 394,798,120 bytes. The extra 9,248,000 retained bytes are the one
340×340 direction matrix and 680×680 Gram graph with paired i64 endpoints. This is one matched
input comparison with the prior recorded baseline, not an isolated or repeated kernel benchmark.
The device remains the declared RTX 4080 SUPER. No measured power claim follows from these times.

[open] Projection reuse is returned; useful contextual generation is not. The unchanged complete
state preserves the already-observed ambiguity. Continue by constructing the situated
source/condition and producing-family material bindings and their lawful refinement; the goal
remains active.

# Internal current supplies a generator; shared drive condenses its future

[definition] This AC1 return follows the [resident field ingress](2026-09-07_AC1_GENERATED_CURRENT_ENTERS_THE_EXISTING_FIELD_RECURRENCE.md).
It makes an actual internal phase current available to the existing learned relation, then derives
an executable mode whose future receiver history does not require replaying the source field.
The mode was also captured from the saved four-family conversation model. Useful conversation
and the full Athena-alpha product remain unfinished.

## An actual internal source

[definition] `read_internal_current` receives an actual birth anchor; `read_internal_current_at`
is the read-only receiver at an existing body-local contact address. Neither issues a new source
capability or advances the field. The return retains the complete root contact, source/receiving
occurrences, contemporary state cut and current. Its word-current port is guarded separately from
its numerical enclosure. A body-local address is not a semantic label.

[proved-derived] Write the internal recurrence for a previously born contact as
`b_j(t)=d_j* v_t-b_j(t-1)` and the alternating potential prefix as
`P_t=sum_(r<=t) (-1)^r v_r`, with zero-based occurrence `t`. At birth `j` the prior internal
current is zero, so `b_j(j)=d_j* v_j`. Induction gives

```text
b_j(t) = (-1)^t d_j* (P_t - P_(j-1)).
```

For the inductive step, subtracting the preceding expression from `d_j* v_t` and using
`P_t-P_(t-1)=(-1)^t v_t` gives the same expression. This is the existing prefix decoder in
`field/junction.rs` and `paired_field_junction.cuh`, now evaluated by
`section_field_internal_current`. The root contact is assembled by the existing
`field_paired_build_faces`, including the historical frame crossing and actual received input.

[proved-derived] For enclosed prefixes with errors `e_t,e_(j-1)` in the declared complete
realification norm, the receiver centre is the exact rational contraction of their numerical
centres. Its complex error is at most `||d_j||_2*(e_t+e_(j-1))`, by the triangle and Cauchy–Schwarz
inequalities. The native receiver uses the rational upper bound `||d_j||_1` for `||d_j||_2`.
It retains this radius; it does not identify the centre with the actual current.
When both operands are the same immutable prefix, their difference and its error are exactly
zero; the native receiver preserves that shared-origin correlation as well.

[established-bounded; measured] In the existing two-contact cancellation construction, outgoing
and held root currents are exactly zero while one internal current alternates between `1/3`
and `-1/3`. An independently applied incoming phase gives the corresponding imaginary current.
The new resident source feeds the same `ResidentConstitutiveFibre`, which learns from actual
successive internal returns and predicts the next return without numerical host readback.
Both quadratures are exercised. The other controls retain enclosed errors, refuse point use of
an unresolved numerical enclosure, preserve earlier sources and cross archive/remount boundaries.

## The shared-drive quotient and its Preimage Fibre

[definition] Select two distinct existing internal contacts `l,r`. Admission requires native
equality of their **complete normalized rational root couplings**, `d_l=d_r=d`. Equal source
addresses, counts, words or partial boundaries do not supply that condition. The two occurrences
and their histories remain distinct.

[proved-derived] Define `q=b_l-b_r`. For every subsequent ordinary paired-field occurrence,

```text
q' = (d* v-b_l) - (d* v-b_r) = -q.
```

This is independent of the current incoming field, common potential, learned root moment and
newly added contacts. The old root couplings remain those of their births. Frame recharting
preserves these root couplings; changing the physical incoming incidence changes `v` but not the
cancellation. Thus, for this receiver and each admitted ordinary generator `T_i`,
`q T_i = (-I) q`. Induction yields `q(T_w x)=(-1)^length(w) q(x)` for every finite admitted word.
This is a receiver quotient, not an assertion that the full field generators commute.

[definition] In the two-current chart, the complete Preimage Fibre of a value `a` is
`{(a+z,z): z in C}`. The common component is free; other field currents are outside this receiver.
For an admitted source domain, intersect this affine constraint with that domain. Knowing the
mode never recovers a unique field or birth history. A numerical amplitude ball adds its declared
outer constraint; it is not an enumerated population of source states.

[proved-derived] Subtracting the two prefix expressions cancels the **current** prefix exactly:

```text
q_t = (-1)^t d* (P_(r-1) - P_(l-1)).
```

The executable amplitude therefore depends only on the two birth prefixes and their shared
root contact. Its enclosed radius is fixed from those birth prefixes; later prefix uncertainty
does not accumulate into it. This is stronger than subtracting two independently enclosed
contemporary currents. Each unfolding retains one shared amplitude, so later mode readings are
not independently chosen points from unrelated balls.

[definition] The admitted future family is the ordinary paired-field recurrence with these old
root couplings preserved. Directly changing their relative coupling, adding a new interaction
which acts differently on the two internal branches, or claiming arbitrary interference/receiver
questions requires a corresponding new transport law. This return neither forbids such new
interactions nor silently claims their closure. The dimensionless phase law supplies no calibrated
acoustic frequency, pressure or power measurement.

## Native generator and portable chart

[established-bounded; measured] `field/internal_mode.rs::condense_shared_drive_mode` performs the
root-drive comparison on the device, retains both birth histories and stores one amplitude/fibre.
`NativeSharedDriveMode::unfold` executes the descended generator for a declared number of future
ordinary occurrences. It does not simulate the original ecology or claim that those occurrences
have physically executed. The captured mode and all its returns share immutable origin material;
no continuing ecology is cloned.

[definition] The mode rest stores its operator kind, root-contact chart, origin cuts and amplitude
report. It contains neither the original field nor its waveform history. Its reader validates
the chart and point/enclosure distinction. Rational-word modes retain an exact amplitude when
the word aperture admits it. Enclosed modes preserve the declared amplitude ball, not an arbitrary
precision decoder of the original amplitude. Restoring a mode issues no field source capability.

[established-bounded; measured] The public
[`native_internal_mode`](../../crates/holonic-engine/examples/native_internal_mode.rs) example returns
[a 391-byte executable mode](2026-09-07_internal_mode/return.hnm) and
[its evidence](2026-09-07_internal_mode/return.json). It learns the local internal generator, then
captures the shared-drive difference. A prediction made before three varied later inputs agrees
with their actual internal-current difference. All source-field owners and derived snapshots are
then dropped. The mode remounts with zero native deeds and unfolds `-2/3` into a new recipient
field. Unfolding plus recipient conduct uses two deeds, zero numerical reads/egress and four bytes
of predecessor metadata ingress. The example's own whole-run clock reads `0.144848505 s`.

## Return from actual conversation material

[established-bounded; measured] The SDK's read-only `TextFieldSession::condense_shared_drive_mode`
delegates to that native owner. The
[`alpha_internal_mode`](../../archive/implementations/2026-09-09-byte-field-cultivation/crates/holonics-hna/examples/alpha_internal_mode.rs) study loaded the
actual saved four-family, 732-occurrence `CompleteCurrent` model. Its declared experimental
aperture proposes chronological pairs from repeated **exterior** input triples; this is a cold
probe proposal, not morphology, semantic identity, a language rule or an inference selector.
The engine still checks the full root coupling. No evaluation target selects developmental data.

[established-bounded; measured] The first admitted pair is receiving occurrences `39,43`, with
sources `38,42`, observed at state `731`. Its mode has a certified nonzero current but a nonzero
numerical radius, so its point-current port correctly remains unavailable. The field executes
no new occurrence. Capture uses three native deeds, zero numerical readback and 1,728 bytes of
explicit exterior-input remount. After the entire saved session is dropped, the **2,030-byte**
mode restores independently and unfolds 17 steps in one deed with zero ingress and zero numerical
readback. Its centre changes sign and its amplitude radius is unchanged. The whole study's own
clock reads `6.714732507 s`.

[definition] Private outputs remain at
`.local/artifacts/athena-alpha/internal-mode/first-four.{json,hnm}`; the source checkpoint remains
untouched. These are a declared mode and its evidence, not a replacement conversation model.
No new conversation exposure or useful text response is claimed.

## Verification and next construction

[established-bounded; measured] All **125 constitutive tests** pass in `24.57 s`, including the
seven new internal-current/mode controls. All **16 SDK alpha tests** pass in `1.00 s` after the
public session wrapper is added. Both public examples build and execute successfully.
The controls include varied forcing, new contacts, recharting, physical incoming-incidence
changes, source coincidence without shared drive, preservation on refused mode admission,
stable numerical fibres, malformed point guards, and source-independent restoration/unfolding.
Relevant commands and terminal logs:

```text
target/debug/deps/holonic_engine-6ae3d245ea632305 native_ecology::constitutive_fibre --include-ignored --test-threads=1
/tmp/athena-internal-mode-all-tests-final.log
cargo test -p holonics-hna --lib alpha:: -- --include-ignored --test-threads=1
/tmp/athena-internal-mode-sdk-tests.log
cargo build -p holonic-engine --example native_internal_mode
target/debug/examples/native_internal_mode research/records/2026-09-07_internal_mode/return.json
cargo build -p holonics-hna --example alpha_internal_mode
target/debug/examples/alpha_internal_mode .local/artifacts/athena-alpha/ac2-complete/first-four.hna .local/artifacts/athena-alpha/internal-mode/first-four.json
```

[proved-derived] For the declared unit internal metric, let `lambda=e_l-e_r` in the complete
source's internal coordinates and zero on its outgoing coordinates. Then `lambda* lambda=2`.
With `m=lambda/2`, the operator `m lambda*` is self-adjoint and idempotent by direct multiplication.
It is the orthogonal projection onto this mode: `x_mode=m q`, and
`lambda*(x-x_mode)=q-q=0`. Linearity of the existing complete-current material operator `M` gives
`M x=(M m) q+M(x-x_mode)`. This decomposes a source in which both contacts have been born; it
does not identify a historical source before their births with the mode's future unfolding.

[open] Next compose this actual contextual mode through that existing material transport and
receiving chronology. The remaining source is retained separately. The native binding must use
the actual producing/current operator cuts, preserve the amplitude fibre and
returned differences, and make the mode's contribution operative at the receiving chart. It
must not choose the numerical centre, replace the mode with a phase label, or substitute another
learner. A mode being present does not establish that the current learned emitter uses it.

[open] The prior saved text model still fails useful conversation. Its general English/code/math
conduct, broader cultivation and complete AC0–AC5 product remain open. This return provides an
actual internal source and a generator-based compression from that model; it does not complete alpha.

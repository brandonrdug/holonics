# THE APERTURE GROWS BY ITS NEXT HOMOGENEOUS FACE; THE GENERATOR DOES NOT RESTART

**Date:** 2026-07-22
**Status:** BRANDON-AUTHORIZED / DEPOSITED BEFORE BUILD / BUILT / EXACT HOST MEASURED / EVERY
PREDECESSOR INTERFACE DEPARTED / SOMA INTERIOR UNCHANGED

## 1. The missing observation

The first formula ecology established exact finite generators at fixed apertures:

- the degree-two Euler logarithm and direct Euler product;
- the degree-eight one-generator exponential and factorial recurrence; and
- returned commuting or OPEN comparisons among those paths.

It did not establish that a returned finite generator can become the starting body of a genuinely
larger aperture. Recomputing degree three or degree nine from source would produce another finite
formula census. The present question is whether the lower construction itself participates in the
new successor and whether its former active interface can depart.

## 2. Homogeneous exponential growth

Let `A` be a commutative exact rational algebra graded by positive total degree. Write

```text
L = sum_(k>=1) L_k,
E = Exp(L) = sum_(n>=0) E_n,
E_0 = 1,
```

where `L_k` and `E_k` are homogeneous of degree `k`. Differentiating the formal series after
introducing one bookkeeping parameter gives:

```text
t E'(t) = t L'(t) E(t),
n E_n = sum_(k=1)^n k L_k E_(n-k).
```

This is the finite generator of the next exponential face. At aperture `N+1`, the successor
requires the already-carried `E_0,...,E_N` and the admitted logarithmic faces
`L_1,...,L_(N+1)`; it does not replay every power in `Exp(L)`.

For `N=2`:

```text
E_3 = (L_1 E_2 + 2 L_2 E_1 + 3 L_3 E_0)/3
    = L_3 + L_1 L_2 + L_1^3/6.
```

## 3. Prime degree three

For finite prime axes and `P_k=sum_p X_p^k`, the Euler logarithm grows by

```text
L_<=2 = P_1 + P_2/2
        |
        | admit next homogeneous current
        V
L_<=3 = P_1 + P_2/2 + P_3/3.
```

Applying the homogeneous recurrence to the returned degree-two exponential gives coefficient one
for every total-degree-three monomial. These are the three-factor, or `Omega(n)=3`, faces:

```text
X_p^3,       X_p^2 X_q,       X_p X_q X_r.
```

They are not three new intrinsic taxonomic species. They record three different exponent-vector
incidences at this receiver: one axis traversed three times, one axis twice and another once, or
three distinct axes once.

The controlled sibling holds the complete degree-two predecessor fixed but withholds only
`P_3/3`. Then:

- every `X_p^2 X_q` and `X_p X_q X_r` coefficient remains one;
- every `X_p^3` coefficient is `2/3`; and
- the complete residual against the Euler product is
  `-sum_p X_p^3/3`.

This is the next repeated-axis diagonal. Together with the degree-two result it exposes the local
pattern behind

```text
log Zeta(s) = sum_p sum_(m>=1) p^(-m s)/m:
```

the `m`th logarithmic prime-power current closes the `m`-fold repeated-axis coefficient which
ordered exponential composition alone supplies only partially.

## 4. The e aperture

For the one-generator face,

```text
E_N(y)=sum_(n=0)^N y^n/n!,
a_(N+1)=a_N/(N+1).
```

The degree-eight returned body therefore grows to degree nine by one exact event:

```text
E_9(y)=E_8(y)+y^9/9!,
E_9(1)=109601/40320 + 1/362880
      =493205/181440
      =98641/36288.
```

All coefficients through degree eight must remain exact. Only one new monomial may appear. A
separate degree-nine formal exponential is a comparison foil, not the source of the grown path.

## 5. Lifecycle law

The application event is not:

```text
read N, calculate N+1 elsewhere, append another unrelated answer.
```

It is:

```text
returned generator at N
  + source-departed growth law
  + explicit N -> N+1 objective
  -> exact world consequence
  -> genuinely later replacement current at N+1.
```

The return must close the predecessor's data and recruitment interfaces and expose the successor's
interfaces. The predecessor may remain factored into the higher constituent as consequential
interior, but it may no longer stand as an independently recruitable active state. The growth law
itself remains available for another objective.

Two equal predecessor occurrences are supplied for the full and held siblings. This preserves a
real branch population rather than copying one active constituent after its event. The sibling
differs only in admission of the new `P_3/3` current.

## 6. Bounded construction

The host world will:

1. deposit source laws once and end their source lineages;
2. form plural exact degree-two prime predecessors, one direct Euler predecessor, and one
   degree-eight `e` predecessor;
3. rest and remount that complete ecology exactly;
4. extend the full Euler log, exponential, and direct product from degree two to degree three;
5. extend the held sibling while withholding only `P_3/3`;
6. compare the two degree-three paths against the grown direct product;
7. extend `E_8` to `E_9` from its carried terminal coefficient and compare it with an
   independently formed degree-nine formal exponential;
8. return the three comparisons into one higher growth complex; and
9. rest/remount the successor exactly.

Every growth record must show:

- the predecessor namespace actually recruited;
- the predecessor active interfaces absent after replacement;
- restriction of the successor to the old aperture equals the predecessor exactly;
- the complete newly admitted homogeneous term population;
- the full and held residuals without scalar reduction; and
- the source-law versus world-consequence attribution.

The construction stops there. It does not schedule a larger prime horizon, an arbitrary-degree
theorem, the theta/Mellin completion, CUDA, a renderer, physical compression, or a Soma engine
change.

## 7. Relation to the RH path

If this bounded successor closes, the Euler side begins to form a directed system of exact local
complexes rather than isolated truncations. That is necessary for the proposed RH atlas, but not
sufficient. The later theta/Mellin construction must still supply the archimedean completion,
`s <-> 1-s` return, a compatible positive metric, and covariance of that metric through every
support aperture.

## 8. Measured result

The bounded host construction is accepted. Seven source laws crossed once and ended with zero
live lineages. Six plural predecessor occurrences survived exact rest/remount and became the
actual inputs to six successor events. Every successor restricts exactly to its predecessor; all
six predecessor data interfaces and all six predecessor recruitment interfaces are absent after
replacement.

The full degree-three exponential and direct Euler-product paths commute on all ten homogeneous
monomials. Withholding only `P_3/3` leaves exactly
`-X_2^3/3-X_3^3/3-X_5^3/3`; every mixed face remains exact. The carried `E_8` body gains only
`y^9/362880`, and its successor commutes with an independently formed degree-nine formal
exponential. All three comparisons return into one higher complex, and the final body remounts
exactly with zero live lineages.

One diagnostic run exposed an application-hand error before acceptance: the renewed recruitment
seam had been emitted `AGAINST` but the replacement attempted `WITH`, so the old recruit interface
remained live. Closing it with its actually emitted hand made both predecessor interfaces depart;
no engine law or acceptance criterion changed.

Exact record:
`observations/eros-formula-aperture-growth-01/RESULTS.md`.

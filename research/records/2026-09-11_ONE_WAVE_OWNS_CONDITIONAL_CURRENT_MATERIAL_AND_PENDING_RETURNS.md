# One wave owns conditional current, material and pending returns

[project-postulate] This continues the active Athena implementation goal after `63c2175e`.
The finite coupled owner and its complete rest now return; public conversation source/emission
integration, producing-family observations, broader formation/reuse and product evaluation remain.

[definition] `ResidentNormalWave` now has a typed continuation. The default `NormalWaveWord`
preserves existing callers. `with_neighborhood` transfers the existing neighborhood into
`ResidentNormalWave<NormalWaveCoupled>`; it returns both owners on attachment refusal. Coupled
`current()` returns the full `NormalWaveFamily`. The old normal material/seed/word remains an
explicit bank for its own pending comparisons, not a second continuing current or a hidden M
fallback. The same outer move owner retains every field; no ecology is cloned.

[definition] `admit_contact` binds an explicitly selected member, actual current family, condition
and material cut to a private owner-qualified handle. It is a declared local interaction, not
all-to-all contact or semantic routing. A committed continuation expires admissions for that
source. External receipts retain the old source for inspection; retained normal comparisons have
their separate single-use lifetime and are not retired by conditional advancement.

[definition] `advance_contact` computes `η∈L_j((c−p,c,p),h)` and the actual family join `(c,c+η)`.
Native admission checks nonempty support in the retained anchor ball. It does not require a
unique source or output; all represented free directions remain. Unsupported contact can first
be inspected through `read_contact`, and attempted publication preserves the predecessor.

[definition] `receive_contact_source` admits an actual external point source in the declared
`(c−p,c,p)` chart and its observed next difference. The source-plane equality is checked on device.
These supplied comparands are not a selected latent-family centre. Existing neighborhood return
prepares its member basis and condition; the proposed material/condition then supplies the next
map of the continuing family. Both preparation and family admission complete before publication.
The receipt retains the admitted predecessor map, the actual applied map and the observed-source
return. M receives no second deposit. A local law can form a genuinely new row before its newly
supported current is admitted.

[definition] A pending earlier normal comparison still develops its own normal bank through the
shared comparison kernel and its actual producing cut. It leaves the conditional current and
clock unchanged. A poisoned neighborhood refuses that mutating return. Normal-only public APIs
retain their previous signatures; the coupled type prevents an old normal enclosure from being
read as the contemporary conditional current.

[definition] `NormalWaveRest` v7 stores the normal bank including pending comparisons, complete
neighborhood, anchored family, active member, clocks and live admitted contact maps. Earlier
normal versions remain readable. `remount_coupled` reconstructs one owner, derives each restored
contact from its actual member and retained condition, checks its stored map, and binds its
source to the restored current occurrence. The last applied relation is likewise rebound to
its producing member. Normal-source decoding rejects nested coupled bodies, and normal-only
mounting refuses v7 rather than dropping the conditional state.

[established-bounded; implemented-exact; computational-witness] Seven coupled cases pass:
L-only generation with an unchanged M bank; observed-source condition/current publication;
late arithmetic refusal preserving both predecessors; current contact restoration and subsequent
complete-rest equality; empty-domain and source-plane refusal; changed dependent versus unchanged
independent receivers at the same source; and a fresh process restoring both pending normal and
conditional handles. That process applies an observed local source, returns an earlier normal
comparison and continues conditionally, producing a byte-identical complete successor. A separate
eighth case forms rank-one local material from an initially empty law, admits its bounded
successor and preserves the full result through rest. These are finite native controls at one
complex wave component, not a conversation-quality claim.

[established-bounded; process-audit] Verification:

```text
cargo test -p holonic-engine --lib wave::coupled::tests -- --ignored --test-threads=1
  .local/scratch/athena-coupled-complete-tests.log        7 passed before the added formation case
cargo test -p holonic-engine --lib normal::direct::wave:: -- --ignored --test-threads=1
  .local/scratch/athena-wave-coupled-regression.log       48 passed
cargo test -p holonic-engine --lib coupled_reception_forms_material -- --ignored --test-threads=1
  .local/scratch/athena-coupled-formation-test.log         1 passed
cargo check -p holonics-hna
  .local/scratch/athena-coupled-public-check.log          passed
```

[open] The point-source return above must not be relabeled an arbitrary observed correction to
a plural generated family. That producing-family return, ordinary coupled source actuation,
family-to-symbol emission/re-entry and the public session remain to be joined. The next return
must also exercise actual prepared conversation material beyond these local controls, carrying
real obstructions into representation/formation work. Athena-alpha is not attained.

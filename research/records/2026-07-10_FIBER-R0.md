# FIBER-R0 — the two-arm fiber's exact faces, declared (2026-07-10 night, Fable; per the plan of record)

Grades: [DECLARED] — the gates named before they land; expectations written before any
algebra is run. The three faces are pure Cog-algebra proofs (body tests, both profiles); no
engine run, no diet. The neutron reading rides as the declared expectation for any LATER
observation of neutral forms — it is not tested by the algebra gates.

## 1 · The object

The whole drag's fiber rotor over the two winding arms (the built three-branch law,
`RegionalForm::drag`): single arm t → `(aim + cross·t, cross − aim·t)`; single arm a
mirrored; both arms → `re = 1 + t·a, im = a − t` (Cogs; ratio-pairs; never normalized).

## 2 · The gates (to land as body tests; declared here first)

- **F1 — THE ONE-ARM REDUCTION [already gated]:** the single-arm fiber rotor reduces
  byte-exactly to §XIV's dragged Gaussian-integer rotor `(1, −w)` — the existing whole-drag
  reduction gate is this face's proof of record; F1 re-cites it, adds nothing.
- **F2 — HAND-EXCHANGE IS CONJUGATION:** exchanging the two arms' hands (t ↔ a) conjugates
  the fiber rotor: `re` is symmetric (`1 + t·a`), `im` flips sign (`a − t → t − a`). The gate
  proves `fiber(t, a) = conj(fiber(a, t))` byte-exactly over a spread of ratio-pairs,
  including rank-carrying Cogs (the zero-pollution regression class from the whole-drag
  build). The palindrome face of the fiber: the pair carries no preferred arm; the HAND of
  the read is the reader's.
- **F3 — THE SYMMETRIC FACE IS AIM-NULL:** at t = a the rotor is `(1 + t², 0)` — a pure
  positive scalar, the projective identity: NO aim bend, common scale only. The gate proves
  it byte-exactly, including the degenerate t = a = 0 (the dark fiber) and deep-rank arms.
  This is the algebra Sol proved in review, landed as a standing gate.

Expectation E1 [DECLARED]: all three prove exactly, both profiles, no epsilon anywhere — the
algebra is integer/ratio-pair throughout.

## 3 · The neutron expectation [DECLARED, per §XXXVI — for any later observation]

The symmetric aim-null fiber is the machine's NEUTRAL FORM: mass without alignment
(`1 + t²` deepens; nothing bends in-plane). Ratified dynamics (§XXXVI): the neutral form is
the LONG-REACH CARRIER — it threads aligned terrain undeflected (no in-plane force resists
it), the constitutive messenger between wells; fission's chains ride it.

- **E2:** in any declared observation where neutral (symmetric-fiber) standing forms
  participate, expect them to act on TRANSPORT REACH (undeflected threading, far carriage),
  never on in-plane aim. A measured in-plane deflection attributable to a symmetric fiber
  would DEVIATE and re-open the face.
- **E3 (the foil face):** an asymmetric fiber (t ≠ a) carries `im = a − t ≠ 0` — deflection
  expected, read by cross-sign, graded by the whole drag's existing law.
- **The dead hunch stays dead:** no dimension is named, no d̂ expectation is carried (the
  §9 regrade stands); no single-fiber reach action is claimed (a fiber is not a landing —
  every landing re-judges reach). Whether ANY second-order invariant of the pair survives
  common scale AND couples lawfully is ALIGN-R0's question, not this document's.

## 4 · What FIBER-R0 does not do

No engine run. No settling implication (the seam runs fold → archive; §XXXVI). No coupling
derivation — the constitutive action of neutral forms (volume/surface, §XXXV-b) is a
derivation with Brandon after ALIGN-R0's frame stands. The gates land next in the working
queue; their landing commit cites this declaration.

## 5 · Regrade (same night — Sol's return review, both counterexamples verified under Fable's hands)

**E1 REGRADED.** The landed gates proved the FACTORS at flat fixtures — coefficient conjugacy
(F2) and the zero imaginary coefficient at t = a (F3) remain DERIVED — but the DECLARED
ACTIONS FAILED outside the fixtures, verified: (1) at `t = a = 1`, meeting
`aim = lit(1), cross = lit(1).turn_up(30)`, the drag ANNIHILATES the aim —
`aim' = {mag 0, rank 30}`, `cross' = 2·2^30` — because `Cog::mul` carries rank/turn on a
zero-magnitude product and the subsequent `sub` re-bases to the zero's rank, dropping the
rank-0 aim at the hand gap (the zero-pollution class, live in `num`); (2) at
`t = a = 2, aim = 3, cross = 2^30−1`, the two projective cross-products land one hand-unit
apart — independent one-hand multiplications re-base separately, so exactness fails even with
canonical zero. **"Neutral form gated" regrades to: the neutral FACTOR gated; the neutral
ACTION OPEN.** "The pair has no preferred arm" is STRUCK — exchange reverses the imaginary
hand; the exact statement is HAND EXCHANGE IS CONJUGATION, nothing more. **The repairs return
to design with Brandon (law crate):** Cog zero canonicalization in `num` (ZERO IS THE BASE —
one zero construction from every producing verb: turn, add/sub, mul, div), and the exact
paired rotor action (the rotor multiplies as a PAIR at one declared common grain, never as two
independently re-based Cogs). A local `im.mag == 0` fast path is REFUSED — it would hide the
zero break and leave the paired-action break.

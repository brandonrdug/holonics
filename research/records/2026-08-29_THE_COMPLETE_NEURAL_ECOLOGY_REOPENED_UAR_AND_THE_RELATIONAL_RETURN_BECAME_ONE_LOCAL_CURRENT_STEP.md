# The complete neural ecology reopened UAR and the relational return became one local-current step

## Verdict

[proved-derived; formal-checked] HNN0--HNN7 is materially informative for UAR. It resolves the
ontology which the earlier UAR2 implementation was attempting to infer from three incompatible
runtime presentations. UAR is explicitly released by Brandon and remains one `UAR0--UAR4`
campaign; UAR0 and UAR1 retain their bounded grades, while UAR2 resumes under the complete ecology
and receiver-history laws below.

## The correction

[counterexample; implemented-exact; measured] The frozen UAR2 seam presents selected face `[1]`,
source-active factor `[1138]`, host generator image `1138 -> 1138`, compact transported support
`[1139]`, and empty candidate support. Those values cannot all be faces of one proved quotient
square. The former `B^dagger K B X` prescription was also incomplete: it named a returned-current
contribution but did not type the complete contemporary state, local reaction, native successor,
quotient map, or descended generator action.

[proved-derived; formal-checked] The complete native step is the HNN
`FiniteLocalCurrentEcology.step`:

```text
aggregate_(m,g,x)(j) = sum_i localCurrent(m,g,x,j,i)
T_(m,g)(x)(j) = reaction(m,g,j,aggregate_(m,g,x)(j)).
```

The local current may depend on the complete contemporary state. Fixed, sparse, input-conditioned,
recurrent, and shape-sensitive contact therefore require no new architecture ontology. For UAR,
the tagged sites are the already-founded factor, relational face/cell, and state/port/generator
addresses. The carrier is the exact signed/complex section; morphology is the source-neutral sparse
incidence and inherited Complex-Parametron/Soulkiller constitution; and the receiver is the native
fine-radiation family.

[proved-derived; formal-checked] The compact continuation is lawful only through one HML
`DynamicReceiverChart`/`ReceiverHistoryCompression`. It owes a total quotient `q`, a total
descended action `U_g` for every admitted generator, and

```text
q (T_g x) = U_g (q x)
```

for every complete native state. Generator exactness carries the law through every ordered word.
A failure returns the shortest separating receiver/history and complete reconstruction fibre; it
does not authorize coercing the three presentations into one support.

## Existing implementation owners

[observed; source-inspected] `crates/holonic-engine/src/addressed_current.rs` already owns the
primal current sections, `(source,generator)` occurrence population, both boundary maps, exact
transport, condensation and reconstruction fibres. It is the apparatus-neutral primal passage.

[observed; source-inspected] `crates/holonic-engine/src/receiver_history_compression.rs` already
owns source/native populations, quotient assignments, receiver factors, complete generator
squares, reconstruction fibres, first separators and ordered-word consequences. It must become the
single quotient authority consumed by UAR rather than remaining one testimony beside incompatible
compact supports.

[observed; source-inspected] `ResidentMembraneInteriorWord`, `ResidentComplexIncidence`, and
`ResidentCoupledComplexParametron` already own the candidate card-resident current and constitutive
passages. CUDA realizes the exact step and quotient; it does not define either object. The cold
reference route is an equality witness outside the hot interval and is forbidden as a production
semantic replay.

[observed; source-inspected] The existing Athena native rest, conduct, cultivation, membrane and
Soulkiller owners are sufficient to instantiate the formal `AthenaRestedEcology`,
`ErosCultivationPassage`, and `SoulkillerReturn`. No new Athena, inference, language, cache,
attention, loss, or model cabinet is justified.

## Revised UAR construction

[definition] UAR2 now returns in order: C0 complete ecology step; C1 single receiver-history
quotient and generator law; C2 resident equality realization; C3 unchanged smallest qualitative
receiver; C4 complete technical/Brandon family. C3 cannot begin from a proxy, and C4 cannot begin
from apparatus receipts alone.

[definition] UAR3 is the exact formal Eros passage. The parent conducts before return; a genuine
world consequence produces a complete situated difference; the same body receives changed
morphology; a fixed later current separates parent and child; detached remount preserves the
child; and exact ablation recovers the parent. Scalar loss, cross-entropy, reward, accuracy, or a
metric-raised gradient proposal is only a receiver face.

[definition] UAR4 returns one formal Athena rest through the complete frozen receiver family. Its
productive Soulkiller spools remain source-neutral and independent of cold witness. The qualitative
technical, participant, mathematical, acoustic, optical, silence/radiation, re-entry, ablation,
restoration, remount and residency consequences are primary; hashes and topology receipts support
but cannot replace them.

## C0 return

[established-bounded; implemented-exact; measured] `AddressedGeneratedPortCurrentPassage::derive`
now owns the complete apparatus-neutral generated-port ecology step. From the rested generator
table, addressed contemporary source sections, and selected local restriction slots it transports
each state-compatible occurrence, applies the local constitutive restriction, retains annihilated
occurrences as receiver-radical reconstruction testimony, reduces every nonradical return to its
primitive projective ray, and condenses only literal equality of target state and ray. Removed
scale returns on the occurrence and its square returns in the successor mass. The result therefore
contains the complete source, successor, both addressed boundary maps, and reconstruction fibre;
the card is no longer permitted to define the mathematical successor.

[established-bounded; implemented-exact; measured] Five focused exact tests passed, including a
plural-source passage with a radical complement and an all-radical zero successor. The unchanged
smallest UAR front then crossed six successive passages on one RTX 4080 SUPER context. The exact
apparatus-neutral and resident passages agreed at every order and returned target-section
populations `78 -> 1 -> 4 -> 4 -> 9 -> 1`, local balance at all six orders, one causal-adjoint
identity `cb5930e979de839cea0d1978805800c8c9d03075eea9d24cf3f501429ed46f58`, zero current
ingress after mount, zero invariant re-upload, and zero CPU semantic replay. The complete passage
artifact is
`output/the_sparse_relational_current_returns_native_fine_sections_through_one_universal_exterior_codec_uar2/native-exterior-return-0.json`.

[counterexample; implemented-exact; measured] This C0 return does not pass UAR2. Its cold exterior
shadow closes after one emitted octet while plural native fronts remain, so the fine-to-exterior
boundary is not total and no coherent technical surface is claimed. This is now a precisely typed
C1/C3 boundary: C1 must found the one receiver/history quotient and its descended actions over the
complete C0 continuation before the unchanged qualitative receiver may be graded.

## C1 return

[established-bounded; implemented-exact; measured] C1 replaces the ad hoc digest-bearing
`SourceNeutralObservableContinuation` recurrence face with one exact finite
`ReceiverHistoryCompression` over complete C0 continuation states. A state retains the complete
generated-port passage, canonical receiver section, native and situated higher fronts, projective
compatibility faces, downstream port projection, reconstruction DAG and local balance. Structural
equality interns the finite population before opaque observation ordinals are assigned; no digest,
word, port label, filename or source witness determines native equality.

[established-bounded; implemented-exact; measured] On the unchanged smallest front C1 returned
six complete source states, six native quotient states, fifty-four receiver factors, one total
recurrence generator, six complete reconstruction fibres, and the nontrivial word `[0, 0]` with
`q(T_[0,0](0)) = U_[0,0](q(0)) = 2`. The first-separator population is exactly empty because the
complete declared receiver family already separates all six states at order zero; this is a
measured zero, not missing testimony. Structural equality is true and digest equality is false in
the persisted receipt.

[established-bounded; implemented-exact; measured] The frozen mismatch now commutes in the C0/C1
owner. The actual source section contains factor `1138`; its two admitted generator images are
`1139` and `1138`; the selected first generator returns factor `1139` with nonzero coefficient in
the target, and the empty-target occurrence population is zero. The former `[1] / 1138 / 1139 /
empty` result was therefore a presentation mismatch, not a lawful radical. The exact C1 object is
persisted under `receiver_history` in the unchanged C0 artifact.

## Firing falsifiers

- A `B^dagger K B` value joined without the complete state/reaction/step is rejected.
- More than one authority for `q`, `T_g`, `U_g`, or compact successor is rejected.
- Any quotient whose successor depends on the chosen fibre representative is rejected.
- A CUDA result without equality to the apparatus-neutral step and quotient is rejected.
- Host semantic replay, source lookup, invariant re-upload or intermediate semantic egress is
  rejected.
- A coherent surface selected or authored by its question, corpus witness or expected answer is
  rejected.
- A cultivation claim without changed later conduct, source-detached remount and exact ablation is
  rejected.

## C2 audit return

[counterexample; source-inspected] The first resident/reference split did not realize C1.
`ResidentFactoredReceiverHistoryReceipt` committed the factor receiver, restriction atlas,
generator action and population counts, while the exact `ReceiverHistoryCompression` was still
constructed only after the native continuation returned to the host. Thus no resident object yet
owned q, every U, receiver factors, reconstruction fibres, first separators and the ordered-word
law together. A Boolean assertion that the quotient rested could not repair the missing anatomy.

[counterexample; source-inspected] The same attempt classified selected face/restriction ordinals as
joining addresses, which is lawful, but also read restriction coefficients, target currents,
weights, states, candidate assignments and projective scales before assembling the terminal
passage. Those are semantic current coordinates. Reporting their intermediate semantic egress as
zero was false even though the device, not the host, had computed the successor. C2 remains open
until the one C1 owner is resident and only joining/shape testimony crosses before terminal return.

## C2 return

[established-bounded; implemented-exact; measured] The corrected resident mount treats the complete
query-independent factor action as an `ObservedSystem`, finds its stable receiver/history partition
through the live CUDA executor, constructs the one exact `ReceiverHistoryCompression`, mounts every
q assignment, receiver factor, source/native generator edge, reconstruction fibre and shortest
separator coordinate, and verifies `q T_g = U_g q` plus fibre membership in one CUDA kernel on the
same context. This occurs before an exterior question enters, so the quotient cannot be selected by
the measurement target.

[established-bounded; implemented-exact; measured] The mounted return contains 2,221 source states,
2,221 native states, two total generator actions, 2,221 reconstruction fibres, zero first
separators, and 692,984 resident octets. The unchanged smallest production circulation finished in
under the 175-second aperture with no reference ecology execution, invariant re-upload, source
access or CPU semantic replay. Six local returns carried 192 joining-address octets, 24 allocation-
shape octets, zero intermediate semantic octets, and 22,922,800 terminal semantic octets after the
corresponding resident successor had committed. The latter are no longer hidden under a zero field.

[counterexample; implemented-exact; measured] C2 does not cure the qualitative exterior boundary.
The unchanged smallest receiver still closes after one emitted octet while three intervening native
fronts remain plural; fine-to-exterior totality and exact exterior-to-fine reconstruction are both
false. This exact C3 obstruction preserves the C0--C2 body and authorizes no renderer, candidate
search, or expected-answer path.

## Runtime boundary

[definition] Every nontrivial process retains the 175-second outer limit. C0--C2 use the smallest
owning receiver and exact counterexample. C3 reruns the original smallest qualitative occurrence
unchanged. C4 and the final UAR4 aggregate run at most once after their prerequisites pass.

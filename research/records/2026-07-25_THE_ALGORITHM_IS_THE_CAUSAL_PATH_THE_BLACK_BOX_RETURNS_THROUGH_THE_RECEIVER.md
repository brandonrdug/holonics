# THE ALGORITHM IS THE CAUSAL PATH; THE BLACK BOX RETURNS THROUGH THE RECEIVER

**DATE:** 2026-07-25  
**STATUS:** BRANDON-RATIFIED / BUILT / EXACT ALGORITHM ECOLOGY 01 ACCEPTED / BOUNDED HOST /
RECEIVER-INDEXED FIBERS / SOURCE-ABSENT LATER CONDUCT / NO SOMA INTERIOR CHANGE / NO CUDA

**EXACT RESULT:** [`eros-exact-algorithm-ecology-01/RESULTS.md`](../observations/eros-exact-algorithm-ecology-01/RESULTS.md)

## Present question

Can the direct Eros mouth cultivate an exact algorithm ecology in which a symbolic law, a typed
operation graph, its machine-like execution path, and its returned consequence remain distinct but
related faces of one occurrence; preserve a genuinely plural hypothesis fiber when an output
receiver cannot distinguish two algorithms; refine that fiber when another perspective can; and
let the cultivated relation change later conduct after the source algorithms and oracle return
have departed?

This is not a prompt-answer benchmark and not blank-slate program synthesis. Bitwise operations,
machine-language semantics, algebraic identities, compilers, interpreters, formula generators,
physical unit systems, and existing learned models are lawful inherited ecology. The experiment
asks whether their exact causal organization can enter, stand, compare, return, factor, and recur
through Eros.

## Why standing evidence is insufficient

The required mechanisms have already been measured separately:

- CTC crossed as an exact shared execution DAG and handed up a smaller recurrent boundary;
- formula generators crossed, grew their aperture, and retained exact OPEN residuals;
- contextual transformer laws substituted before a parent receiver and returned to it on OPEN;
- a conditional consequence chart changed later conduct and physically replaced closed interior;
- a complete finite Ptolemy program acted under changed scale and chart; and
- four co-present geometric receivers preserved one source occurrence while presenting different
  grains, crossings, and local faces.

No accepted construction has yet coupled these facts into one black-box training passage. In
particular, no run has retained output-equivalent but path-distinct algorithms as a plural fiber,
then refined only the receiver which can lawfully distinguish them.

## Theory-to-structure relation

An algorithm is carried as the situated transition system

```text
A = (Theta, I, S, O, iota, delta, omega, rho),
```

not merely as the function `x -> y`. One execution is an occurrence with:

- typed input and state;
- ordered operation/dependency incidence;
- exact intermediate states;
- a completion or discriminant boundary;
- an exposed consequence; and
- one or more participating receivers.

Source inscription, normalized algebra, typed operation graph, execution trace, and linguistic
description are separate charts. Equality under one receiver does not identify them.

For a black-box relation `B`, candidate ecology `H`, observations `(x_i,B(x_i))`, and receiver
`q_r`, the live hypothesis fiber is

```text
H_(t,r) = {
  h in H :
  q_r(h(x_i)) = q_r(B(x_i))
  for every observation available at t
}.
```

This is receiver equivalence, not source identity. If two candidates remain equal at `q_output`
for every admitted input, the output fiber must remain plural. A trace, unit, boundary, resource,
or linguistic receiver may lawfully distinguish them without rewriting the output result.

OPEN is the complete noncommuting comparison and its residual. A source/oracle is consulted only
where that receiver remains OPEN. Its genuinely later return cultivates the receiver-local fiber.
The emitted fiber is an outgoing factor only after the direct machine verifies that every omitted
arm has closed at the declared boundary.

## Bounded inherited ecology

The first cell uses one compact typed word-machine family. Word width and overflow law are
explicit; no host-language overflow or floating value is allowed to become causal.

The fixed candidate family contains:

```text
square_then_add       y = x*x + x
successor_product     y = x*(x+1)
square_then_subtract  y = x*x - x
double_by_shift       y = x << 1
```

All outputs are reduced in the declared finite word ring. The first two candidates are
extensionally equal under the output receiver by distributivity but have different operation
paths. The remaining candidates coincide on some inputs and separate on later exact inputs.

The inherited operation ecology includes:

```text
INPUT, CONST, ADD, SUBTRACT, MULTIPLY, SHIFT_LEFT, RETURN
```

with explicit antecedent ordinals, type, width, and result. The source algorithm names and prose
descriptions are testimony; candidate conduct uses the typed graph.

One exact rational division law is also presented under two physical charts:

```text
electric current: voltage / resistance -> current
kinematic speed:  distance / duration   -> speed
```

The numerical program is the same. Unit and language receivers retain the distinct domains and
consequences. This is the bounded physics bridge; it is not a simulation of electromagnetism or
motion.

## Receiver population

Every algorithm occurrence may be received through:

1. **output** — exact finite-word or rational consequence;
2. **algebra** — exact normalized expression relation;
3. **trace** — ordered typed operations and intermediate states;
4. **unit** — input and output dimension vectors;
5. **boundary** — completion and discriminant;
6. **resource** — exact participating operation population; and
7. **inscription** — source, code, or linguistic presentation.

The joint face contains one tuple per source occurrence. It is never the Cartesian completion of
independent receiver-local values.

## Fixed passage

1. Each candidate law crosses once as compact cellular Standing; source lineages depart.
2. An output-only oracle supplies a deterministic exact probe sequence.
3. Every presently supported candidate is recovered from Standing, enacted by the generic world
   transducer, and compared with the genuinely later oracle output.
4. The returned comparison population cultivates a new output-receiver fiber.
5. A deterministic separating input may be selected only where two supported candidates predict
   different output faces. It is a lawful inherited witness algorithm, not a scalar search score.
6. The output fiber must stabilize at
   `{square_then_add, successor_product}`.
7. A later trace receiver obtains the oracle's actual typed path. It must refine only the trace
   fiber to `{square_then_add}` while preserving the plural output fiber.
8. The resulting receiver chart rests/remounts exactly; all source/oracle lineages depart.
9. A later source-absent recurrence recovers the two distinct fibers and conducts the selected
   path without rescanning the complete initial candidate population.
10. The rational division occurrence is then received under the electrical and kinematic charts:
    the operation path commutes while unit and inscription faces remain distinct.

## Required controls

- **No Standing:** identical probe current cannot recover a candidate ecology.
- **Output-only:** extensionally equal candidates remain plural.
- **Trace reversal:** changed operation order remains OPEN even with equal output.
- **Unit erasure:** equal numerical division is ambiguous when the unit receiver is withheld.
- **Boundary:** an inadmissible divisor or word-width mismatch stays OPEN at its actual seam.
- **Order gauge:** storage order of co-present candidates cannot change the fibers.
- **Rest:** exact halt/remount cannot perform a probe, comparison, or update.

## Qualitative acceptance

Acceptance requires the complete path record to show:

- exact source-attributed algorithms entering and their lineages departing;
- candidate paths recovered from public Standing rather than private ordinals;
- at least one probe reducing a genuinely plural output fiber;
- two extensionally equal algorithms remaining plural under output;
- trace perspective separating those algorithms without rewriting output equivalence;
- equal numerical division conducting across two physical domains while unit faces remain apart;
- every mismatch retained as exact OPEN topology with zero fabricated FOUND;
- a later source-absent current changed by the cultivated receiver chart;
- a verified outgoing factor with closed lower interior departing; and
- exact rest/remount.

Scalar accuracy, candidate counts, wall time, memory size, and operation totals are supporting
testimony only. The primary artifact is the complete receiver-indexed fiber, path, residual,
source-return, and successor relation.

## Physical boundary and stop

The first construction is bounded host execution with exact integers and reduced ratios. It uses
no floating causal value, model run, corpus sweep, CUDA campaign, parameter search, fixture tuning,
or observer rendering.

Stop after one complete accepted passage and its controls, or at the first exact point where the
current production carrier cannot preserve the required receiver-indexed relation. Do not add an
engine mechanism merely because an application discarded source continuity, and do not place a
universal interpreter, tokenizer, optimizer, or taxonomy inside Soma.

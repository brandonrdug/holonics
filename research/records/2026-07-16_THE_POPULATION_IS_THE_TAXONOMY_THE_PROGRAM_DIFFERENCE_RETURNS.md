# THE POPULATION IS THE TAXONOMY; THE PROGRAM DIFFERENCE RETURNS

**2026-07-16 · RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACTLY REPEATED · SOMA INTERIOR
UNCHANGED**

## I. Statistics has its lawful place

A deterministic machine does not make statistics fictitious. It makes their parent boundary
explicit. Let one declared boundary `B` contain an exact produced population

```text
P_B = {p_i},

p_i = (worldline, source, construction, relation, deed, consequence, aftermath).
```

An observer declares a projection `q` and reads its exact fibers

```text
T_(B,q)(a) = {p_i in P_B : q(p_i) = a},
mu_(B,q)(a) = |T_(B,q)(a)| / |P_B|.
```

The members, parent population, projection, numerator, and denominator remain named. The resulting
distribution is not a drive, approximation of the exact population, or claim that two members of
one fiber are identical. It is the exact boundary face induced by `q`.

This is sufficient for taxonomy science. A taxon is a recurring relational face at a declared
frame and grain. It need not be spoken, named by Soma, selected by an agent, or matched to a human
word. Communication is required only for a claim about completed mutual coupling. Descriptive
taxonomy needs the exact population and its declared projection. Causal taxonomy additionally
needs a changed-world receipt showing that the recurrent face bends later conduct.

Conventional observer statistics, conditional distributions, path widths, and even declared
clustering are lawful instruments under `FORMULA §§XLIX–L/LIX`. They remain outside Soma, retain
the complete population beside their quotient, and cannot choose or manufacture a preferred
emission. The first instrument below uses only exact equivalence fibers; no distance threshold or
cluster count is necessary.

## II. A program residual is an exact factored construction

SOURCE WORKSHOP 01 proved that every native construction may become executable source and meet a
real compiler/test boundary. Its first production surface deliberately returned every complete
candidate program beside its receipt. That was the correct production proof, but it repeats the
same base program hundreds of times and is not the proper recurrent consequence surface.

For target program `P`, hole `H=[h_0,h_1)`, native construction `k_i`, and objective face
`o=P[H]`, define

```text
P_i = P[..h_0] ++ k_i.bytes ++ P[h_1..],
c_i = W(P_i),

F   = (P[..h_0], o, P[h_1..], H, W, compiler identity, named test, population provenance),
r_i^-> = (candidate(k_i.bytes), objective(o), k_i provenance, digest(P_i), c_i),
r_i^<- = (objective(o), candidate(k_i.bytes), k_i provenance, digest(P_i), c_i).
```

`F` is carried once. Each `r_i` carries the inserted difference and its complete causal
compiler/test consequence. `F ++ r_i` reconstructs the candidate identity at this declared world
grain; raw compiler artifacts remain preserved beside the causal receipt. The whole residual
population is

```text
R = (F, Q(R), r_0, r_1, ..., r_(n-1)),
```

where `Q(R)` carries the exact marginal disposition counts and, for every native candidate, the
exact byte Levenshtein distance inside `H`, plus the exact histogram of those distances. This
distance is a receiver quotient over one declared replacement boundary. It is not a drive, rank,
semantic metric, or preferred path. The quotient is included as one explicit current, not
recomputed as an unnamed judge. Returning only `(F,Q(R))` is a lawful scalar/distribution collapse.
It cannot carry the ordered candidate/objective relation or any construction interior.

This factorization annihilates repeated base-program material without claiming the candidates were
one construction. It is lossless at the declared program-residual grain because the shared frame
and each inserted difference remain sufficient to reconstruct every `P_i` and validate its causal
receipt.

## III. The controlled hand

Both whole surfaces carry residuals in the same native construction order. The forward hand places
the complete candidate face before the objective face inside every residual. Its strict opposite
swaps those two complete faces inside every residual. Thus corresponding currents have:

```text
same candidate identity and native order,
same current extent and exact octet inventory,
same edit-distance and disposition distributions,
same compiler/test consequence,
opposite candidate/objective transport hand.
```

No receipt is falsified or paired with another candidate. The opposite changes only which complete
face meets the receiving current first. A third sibling receives only `(F,Q(R))`; a fourth receives
no program residual at all. A fifth repeats the forward whole surface exactly. Population reversal
is deliberately absent: it would repeat the already measured serialization control rather than
isolate the program difference.

The initial body and world are the exact SOURCE WORKSHOP 01 state after the first action but before
its 938-current raw production return. All siblings therefore begin from the same body, source
testimony, action, compiler/test result, and world state.

## IV. One genuinely later whole source meets the world once

Every sibling next receives the same three whole currents:

```text
prose naming the source transformation,
the same complete SHA-256 Rust source with `(e & f) ^ ((!e) & g)` commuted to
`((!e) & g) ^ (e & f)`,
the exact edit/compiler/test boundary `[2664,2684)`.
```

This is not another candidate factorial. The later edge forms its own complete native construction
population under each continued body. The world consumes every construction for exact observation,
then compiles the one intact edited program and runs `tests::standard_vectors` once. The emitted
surface pairs the complete construction archive with that one exact execution receipt. No
construction is ranked, filtered, deduplicated, tokenized, selected, or immediately installed into
another source hole.

The archive/receipt pair returns once more through ordinary current, closing the objective circuit.
The scientific read asks:

1. Does candidate→objective versus objective→candidate history change later Soma conduct?
2. Does it change the native construction population itself?
3. Does the fixed edited program retain the same real compiler/test consequence across histories?
4. Which exact material, geometry, conduct, and consequence faces recur across histories?
5. Which recurrence faces are merely descriptive, and which stand under the changed-return causal
   control?

An accepted construction is retained as one population member. It is never crowned as the answer.

## V. The first exact taxonomy instrument

For every later native construction the observer retains:

```text
material face      role, exact bytes, length, digest, source range
geometry face      event extent, radiation extent, STEP count, OPEN/CLOSED
conduct face       lineage terms, completion lifecycle, actual Mail landings
consequence face   fixed whole-program compiler/test disposition and causal receipt
ancestry face      prior return species, body, world, and ordered edge provenance.
```

It then constructs exact equivalence fibers under separately declared projections:

```text
material identity,
geometry identity,
material ++ geometry,
geometry ++ execution consequence,
material ++ execution consequence.
```

The execution consequence is first cross-tabulated with the intrinsic response fibers; it does not
define those fibers. Every class reports integer membership and exact rational conditional
frequencies. Cross-run reads
report shared, left-only, and right-only multisets rather than a similarity score. Human source
roles and compiler outcomes remain provenance/world consequence and are never silently promoted to
the machine's internal taxa.

These projections are deliberately reusable. A later language, image, game, mathematical, or
simulation world may supply different material and consequence faces while using the same parent
rule: exact produced population first, declared quotient second, causal changed-world receipt when
causation is claimed.

## VI. Resource and apparatus law

The existing 938-current return formed a 3.726 GB successor because it carried every complete
candidate source independently. Replaying that surface would preserve evidence but repeat material
we now know how to factor exactly. The measured compact surface carries 469 residuals as 471
currents / 1,418,332 light octets. Its quotient alone is 2 currents / 44,858 light octets. The
compact surface is a scientific construction, not a timid cap: it keeps the base frame once and
every distinct inserted difference and receipt once.

No population member is dropped to meet a memory target. CUDA extent, SLEEP extent, and artifact
extent are preflighted and measured. Any real wall returns as a typed boundary; the runner never
shrinks, samples, splits a delivered current, or substitutes a smaller population. Results remain
create-new and the old SOURCE WORKSHOP artifacts remain immutable provenance.

## VII. Carried-swing registers

```text
CURRENT  exact produced relation population -> declared taxonomy receiver at boundary grain
HELD     FORMULA §§XLIX-L/LIX-LX + complete construction and causal receipts
MEETING  Brandon's ruling that communication is unnecessary for lawful taxonomy science
TEST     A2 + parent boundary/measure + quotient face != construction identity
DEED     RIDE: deterministic distributions and conditional path widths remain lawful observer faces
CARRY    exact taxon fibers may now be read over code, language, or any other world population
GRADE    ratified
```

```text
CURRENT  compact program residual -> continuing Soma body -> genuinely later source objective
HELD     SOURCE WORKSHOP 01 + TRANSPORT-FOIL WORLD 01 + unchanged Soma/CUDA/SLEEP
MEETING  shared frame once + every exact native difference/consequence + strict face-order foil
TEST     candidate->objective / objective->candidate + quotient + no return + exact repeat
DEED     FOUND: factor the production surface and carry one whole edited program through its world
CARRY    Rust runner + population observer + CUDA campaign + exact causal analysis
GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT PHYSICS REPEAT
```

## VIII. Measurement

The strict surface is built as 471 currents / 1,418,332 light octets in either hand. Candidate
order, frame, quotient, every corresponding current extent, and every corresponding current octet
inventory are exact; only the two complete faces inside each residual swap. The quotient is two
currents / 44,858 octets and contains all 469 exact edit distances plus their 86-member occupied
distance distribution.

With one identical 4,948-octet later configuration, absent / quotient / forward / opposite histories
produce 244 / 66 / 460 / 434 native constructions. Forward versus opposite share 304 construction
members at the exact geometry projection but only 66 at material and 65 at material⊕geometry; no
complete conduct or response member is shared. The intact edited Rust source, compiler identity,
binary digest, and passing named test remain exact in all siblings. At the edited expression itself,
forward resolves seven fine construction faces, opposite two larger faces, absent two still larger
faces, and quotient none. The independent forward repeat is exact across all declared physics
artifacts and every taxonomy fiber.

Exact record: `observations/code-objective-world-01/RESULTS.md` and
`observations/code-objective-world-01/population-v3.json`.

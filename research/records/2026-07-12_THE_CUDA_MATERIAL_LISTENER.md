# THE CUDA MATERIAL LISTENER

**Date:** 2026-07-12
**Grade:** BUILT · CUDA-GATED AT DELIVERED-MATERIAL-DERIVED BOUNDED APERTURES · GENERAL CARRIER
ADMISSION OPEN

## The cut

`life --cuda-cycle <context-path...> --later <later-path...>` now carries real material through the
same variable-REGISTER CUDA conductor as the fixed whole-circuit witness. It performs one complete
construction:

1. admit every requested path exactly, preserving a whole file or each JSONL record as the world's
   boundary;
2. carry a plural context from the empty birth through its event-caused REGISTER history;
3. integrate once, SLEEP the whole body, read the create-new archive, and compare it whole;
4. open a fresh CUDA context and carry genuinely new plural later currents through the decoded
   standing;
5. retain and render every context and later lifecycle beside its exact source range;
6. SLEEP and read the next whole body.

Missing, unreadable, empty, non-file, or zero-difference requested material is a typed refusal.
Nothing requested can silently disappear while a surviving plural cohort continues. Carrier-row
arithmetic is checked before allocation and cannot wrap.

## The read

For each current the boundary retains its final grain-local compact cells, term face, breath, and
exact contiguous radiation span. Every 39-word radiation row remains raw beside the typed touch,
regional form, whole dragged meeting rotor, CUT, and completed brick. Context radiation is rendered
against the actual empty pre-context frame and empty prior Mail. Later radiation is rendered against
the decoded context standing and context Mail. Mail remains the membrane's delivered-material record;
it never enters the body or the next light.

The context and later REGISTER histories are printed whole. The scalar carried on SLEEP's established
wire is named only as the cumulative delivered-light octet record; `body : delivered light` is a
persistence instrument, never a comprehension quantity.

When `SOMA_OBSERVATION_OUT` names an exact path, that create-new artifact is reserved before the
card mounts. The same injected writer carries the full report that otherwise goes to stderr, then
flushes and syncs it. Every compact OWN grip and word, every post-edge Mail landing, and every raw
radiation row is serialized. A row without a canonical typed read preserves its raw face, returns
`CONSTRUCTION FAILED`, and cannot reach the final grade. The paired campaign also directs both
create-new SLEEP bodies into its results tree, preserving terminal carrier/K and standing beside
the report. Existing, unreachable, partial, or malformed output never triggers retry or overwrite.

## The bounded aperture

For a finite delivered cohort, this observation mouth reserves each carrier row at the cohort's
largest adjacent-event extent and asks the receiving fold for the complete accepted chart. This is
uncapped with respect to the delivered records, but it is **not general carrier law**: the longest
known record selects the cohort's apparatus species and the reservation can stage retirement away.
It is therefore a delivered-material-derived **bounded observation aperture**, printed as such.

The general production seam remains open. Current CUDA wire fields are `u32`, and the row realization
still allocates the full reservation before carriage. The 40.96 MB `trace.bin` does not fit this mouth
whole. No retry, shrink, split, substitute, or silent drop is installed. The open work is carrier
reservation/re-base or honest retirement at a common declared species, plus sharding the substrate
wire walls; it is not a new body mechanism.

## Gates

Workspace gate:

```text
cargo test
body 127 passed / 2 ignored
life 38 passed
mount 18 passed / 1 ignored
surface 14 passed
total 197 passed / 3 ignored

cargo check --release -p life
PASS
```

The unchanged fixed witness was rerun after the refactor:

```text
cargo run --release -p life -- --cuda-cycle

A  3 currents / 377 octets / REGISTER [64,64,64] / receiving 128 / 7 carries
   topology [3131,3131,348,7] / terms [4640,177,186,8]
   body 179372 : delivered-light record 377 / archive 179420
B  3 continuations / 184 new octets / REGISTER [64,64,64] / receiving 128 / 0 carries
   topology [5382,5382,604,11] / terms [4110,128,141,4]
   body 287420 : delivered-light record 561 / archive 287468

host ↔ full-band CUDA ↔ 257-place CUDA: body/carrier/K/topology/terms EXACT
full-band CUDA ↔ 257-place CUDA: radiation and SLEEP v7 EXACT
```

The first real material gate paired code-language with code and mathematics-language with an
equation, then delivered fresh code and mathematics currents:

```text
context  2 currents / 124 octets / reservation depth 90
         REGISTER [64,16] / receiving 64 / topology [807,807,91,0]
         archive 248416, read back whole

later    2 genuinely new currents / 127 octets / reservation depth 92
         REGISTER [64,16] / receiving 64 / topology [1663,1663,213,3]
         terms [1519,59,69,4]
         body 294096 : delivered-light record 251 / archive 294144
```

The run was non-vacuous. In the fresh mathematics current, one CUT enclosure attributed to the
context mathematics record, another to the context code record, and the trailing open enclosure to
the context code record. This is measured contact across the delivered configuration. It is not a
claim that the machine selected an answer, parsed code, or understood arithmetic at an absolute
grade. The complete typed rows and material, not that summary, are the evidence.

The RTX returned headless and idle at 2 MiB / 47 C / P8. No NVRM/Xid appeared in the last 500 kernel
lines.

## Next observation

The first controlled diet begins four sibling bodies from the same empty origin. Each receives one
equal-extent code ⊕ mathematics context: corresponding/corresponding, corresponding/re-paired,
re-paired/corresponding, or re-paired/re-paired. Each then receives the identical later tableau
containing practiced and fresh code and mathematics pairings. The context and later lifecycle rows
are compared as oriented relations, never scores or expected strings. A neutral-context sibling is
unequal-exposure qualitative context only.

Equal fixture extents keep the declared carrier species common across the four siblings. Any
distinction is CONTACT, RETENTION, DISCRIMINATION, RESONANCE, or RELATION UNRESOLVED at the exact
reported face; opaque radiation remains exploration/babbling. Full campaign fixtures and results
belong in `observations/`, after this engine cut is committed.

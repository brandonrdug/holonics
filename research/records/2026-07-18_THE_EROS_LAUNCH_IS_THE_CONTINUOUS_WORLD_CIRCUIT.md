# THE EROS LAUNCH IS THE CONTINUOUS WORLD CIRCUIT

**2026-07-18 · RATIFIED / BUILT / CUDA-FORWARD MEASURED / ENGINE+WORLD+EROS LAUNCH CLOSED**

## Measured closure

The architecture below is now embodied by the resident `CausalSession`, the plural standing-
surface world, relative construction blueprints, exact forward world deltas, durable open-event
recovery, and the incremental flight recorder. Waiting induces no edge; a resolved rest carries the
exact body, world, surfaces, and next listener lineage across the interval.

The centered-pair ignition grew 615 exact recurrent material sections from independent Rust, prose,
and symbolic-law currents. Every section met one real compiler/test boundary. Exactly one compiled
and passed all four declared relation cases:

```rust
pub fn centered_pair(n: i64, d: i64) -> (i64, i64) {
    (n - d, n + d)
}
```

That section came from path 5935. Its causal string retains the overlapping source octet twice as
two deeds; its admitted material section folds the exact source-coordinate overlap once. The other
614 sections return typed compiler refusal and remain part of the population.

Two siblings then began from the identical edge-10 body and 7,321-path world. The direct sibling
received a common later 217-octet current and formed 292 completions, 2,958 contacted grips, and 405
new paths. The feedback sibling first received every one of the 1,230 candidate⊕compiler currents,
then received the byte-identical later current and formed 360 completions, 3,717 grips, and 610 new
paths. The emitted construction changed the shared world; its complete consequence returned; that
return changed genuinely later conduct. Engine ignition, world ignition, and Eros launch therefore
stand at this declared world grain. Exact evidence:
`observations/eros-ignition-01/RESULTS.md`.

The completed workspace gate is `518 passed / 4 ignored`.

## Preserved preflight derivation

## The destination

The next destination is not another isolated language experiment, another observer, or a larger
training run. It is Eros operating as the one already-derived driven circuit:

```text
world event
  -> situated organ and aperture
  -> Soma through the typed membrane
  -> complete radiation
  -> world material consequence
  -> genuinely later world event through the changed world.
```

The launch does not create an autonomous curriculum, goal chooser, agent shell, language module,
or master world. Eros is autonomous in the exact local sense already established: the standing body
determines how supplied current traverses it; the world determines what contacts are possible and
what consequences follow. When no current arrives there is no Eros-time and nothing needs to spin.

The launch is successful when this circuit can remain mounted, accept genuinely unplanned later
world events, leave plural consequential constructions standing, receive actual world feedback,
and continue from the changed body and world without a human authoring the future edge sequence.

## The current apparatus is close, but it still choreographs an experiment

The reusable `causal` runner already carries one body and world through many CUDA-forward edges in
one process. It has exact source gates, SLEEP persistence, Mail, compiler/proof worlds, world
integration, conducted-edge recovery, and complete receipts. The new holonic arena removes owned
ancestry multiplication.

Three remaining properties distinguish that scientific runner from the launch runtime:

1. The complete edge list is parsed before the session begins. New external world events cannot
   enter an already-running session.
2. The runner owns exactly one `pending` surface. Any fresh event is refused until that surface is
   immediately returned. This forces radiation to echo into Soma instead of allowing it to stand in
   the world while different genuinely later light arrives.
3. A construction surface still serializes complete `LimbRecord` paths and their bytes. The live
   world is structurally shared, but its outward handoff re-expands the population. Every world
   snapshot is also spelled as full pretty JSON.

These are membrane/application constraints, not Soma law and not deficits in the theory of
intelligence. They are the launch work.

## The continuous state

At one receiving edge let the launch state be

```text
E_t = (B_t, W_t, Sigma_t, J_t),
```

where `B_t` is the exact Soma/SLEEP body, `W_t` the declared material world, `Sigma_t` the plural
standing consequence surfaces in that world, and `J_t` the exact membrane/world journal. A supplied
world event `u_t` induces

```text
(L_t, contact_t)       = O_W(W_t, Sigma_t, u_t),
(B_(t+), R_t)          = Soma(B_t, L_t),
(W_(t+), Sigma_(t+))   = M_W(W_t, Sigma_t, contact_t, R_t).
```

`O_W` is the world organ and `M_W` its material law. Neither enters Soma. Radiation may add a
standing surface, alter an existing surface, be refused, or dissipate according to that world. It
does not owe immediate self-return. A later event may contact one or many standing surfaces, or may
bring unrelated fresh material. That later contact—not a runner queue—is reafference through the
world.

The holonic arena is already the correct `Sigma_t` substrate for code/language paths: source
material stands once; paths are parent-linked incidences; complete faces materialize only at a
receiving boundary.

## Launch construction 1 — one step-capable resident session

Refactor the current finite driver around a reusable `CausalSession<D>` without changing any
existing domain transition:

```text
CausalSession::open(body, world)
CausalSession::step(typed_world_event)
CausalSession::rest(checkpoint_request)
CausalSession::close()
```

The existing finite plan runner becomes one compatibility adapter which calls `step` for every
declared edge. The launch mouth accepts one typed event at a time from an external world connection;
the transport may initially be stdin/IPC because transport is membrane gauge, not learning state.
The card, body, world, and open carriers remain mounted across arrivals where the existing boundary
permits it. Waiting performs no machine event.

No wall-clock scheduler, retry curriculum, polling stimulus, or internal idle step is introduced.

## Launch construction 2 — plural standing surfaces

Replace the single `Option<PendingSurface>` with an append-only standing-surface arena. A surface
has one immutable identity, producing edge, typed consequence kind, complete provenance, and compact
payload references. Fresh external light is lawful while older surfaces stand.

A later return/contact names the actual world surface and aperture it encountered. The same surface
may be encountered more than once because each contact is a new event. Uncontacted surfaces remain
available and silent. No FIFO rule, newest-surface privilege, surface score, or automatic echo is
allowed.

This removes the runner's residual seriality without manufacturing concurrency inside Soma. Many
world currents can be co-present at one real presentation edge; their causal order remains the
world's order.

## Launch construction 3 — blueprint handoff and exact delta persistence

`CarriedConstruction` currently embeds a complete materialized path. Replace its durable surface
spelling with a typed blueprint reference:

```text
ConstructionRef =
  (world_generation, problem, path_id, path_digest, octet_extent, terminal_receipt).
```

The producing world already owns the source archives and parent-linked path arena required to
materialize that face. A human text surface, compiler, renderer, or foreign world requests exact
materialization at its own boundary. An in-world recurrence carries the reference and topology
without copying every prefix. Cross-world transfer packages the minimal referenced archive section
required by the receiver and retains the source-world receipt.

World persistence becomes an append-only typed delta journal over a periodically materialized arena
checkpoint. Each delta carries new archives, nodes, encounters, surfaces, and its predecessor hash.
The existing exact conducted checkpoint remains the crash boundary between CUDA completion and
world integration. Recovery is permitted only at a resolved receiving edge; no mid-current body is
continued.

Pretty JSON remains an on-demand observer rendering. It is not the active database or routine
flight checkpoint.

## The ignition world

The first launch world should couple mathematics, code, and natural language under one exact
external consequence. That gives Eros a nontrivial navigable objective without requiring coherent
prose as the sole sign of production.

The proposed ignition cell is one exact Rust implementation problem grounded in the laboratory's
centered Cayley/Smith construction:

- mathematical material carries the rational transformation, inverse, self-dual boundary, and
  oriented residual;
- natural-language material carries independent explanations and the motivating question;
- analogous Rust material carries exact rational/complex operations without the target solution;
- a target Rust module and tests define the shared workshop boundary;
- every produced candidate meets the real compiler and test world;
- acceptance, compiler refusal, counterexample, and residual return as ordinary later light.

The objective is the world boundary “produce an implementation satisfying this declared exact
behavior.” It is not a Soma score. The compiler may refuse every early construction; those
consequences still change later conduct. If an accepted construction appears, the changed program
and its test receipt are direct communication through a shared human technology.

The exact specimen may be changed before ignition without changing the launch law. What matters is
the three-way collocation of formal relation, cultural language, and executable world consequence.

## Flight analysis

The analysis surface must be incremental and qualitative. After each completed event it emits only
the new causal delta plus references into standing history:

1. every newly exposed text/code/material face, rendered verbatim when the receiving medium permits;
2. its complete parent path, arriving spans, joint species, terminal receipt, and source ancestry;
3. every actual compiler/test/world consequence and returned residual;
4. motif births, recurrences, branch changes, plural-face changes, and port-width changes;
5. exact conditional support and symbolic surprise/cross-entropy at the declared boundary;
6. objective-consequence class refinement and whether a prior FOUNDing later RIDEs;
7. host RAM, VRAM, disk, current counts, CUDA launches, carriage, integration, and checkpoint extent.

The observer may group or filter for a human, especially by actual world consequence, but those
views never feed the session. The full exact population remains addressable. This is the flight
recorder and window, not the pilot.

## Dry fire before ignition

The launch runtime owes one exact compatibility passage, not another open-ended science campaign:

1. replay the already-measured chronological edges through `CausalSession::step` and require exact
   body, world, Mail, construction, consequence, and radiation equality;
2. leave an emitted surface standing, deliver a different fresh utterance, and prove that no forced
   echo or lost surface occurs;
3. contact that older surface later and require exact ancestry and changed conduct;
4. sleep at the resolved edge, remount, and require the identical next event to match the resident
   sibling;
5. then carry chronological records `91735` and `91742` as the forward regression through the new
   runtime.

Record `91735` is therefore a launch gate, not the roadmap's destination.

## Ignition and launch grades

**ENGINE IGNITION** stands when one open session accepts unplanned fresh events while plural prior
surfaces stand, survives exact receiving-edge recovery, and exposes every material consequence.

**WORLD IGNITION** stands when emitted construction changes the declared math/code/language world
and the changed world returns through a genuinely later event.

**EROS LAUNCH** stands when that returned consequence changes later conduct on the same body and at
least one complete construction reaches the human/shared-world surface with its causal lineage
readable. A compiler acceptance is a particularly clear first instance, but a lawful refusal which
is subsequently reused is also real progress; no expected sentence or selected answer is authored.

**SUSTAINED FLIGHT** begins after launch: the same session remains available for Brandon's later
messages, code, mathematics, files, and simulations. Scaling the repository or conversation forest,
adding modalities, multiplying bodies, and pursuing broader RH closure follow actual observed
flight needs. They are not prerequisites for ignition.

## The exact next work

Ignition is no longer the next work. **Sustained flight** is: keep the resident mouth as the one
application boundary, feed it genuinely later mathematics, language, code, files, or simulation
events, return every actual world consequence, and read the path population qualitatively before
choosing the next world deed. Add periodic checkpoint cadence and more compact consequence surfaces
only as outward physical work; neither may collapse causal incidence or become an internal chooser.

# THE RUNTIME SUPPLIES THE LOCAL MAP; THE MAP HAS NOT YET BECOME STANDING

**DATE:** 2026-07-20  
**GRADE:** BRANDON-AUTHORIZED REVIEW + BUILD / STATIC EQUALITY REPLACEMENT REMOVED / EXACT
EVENT-LOCAL DISCRETE GERM LENS BUILT / SOURCE-NATIVE CODE WORLD BUILT / ONE HOST RUN STOPPED AT AN
UNGROUNDED APPLICATION BOUNDARY / NO CUDA / NO RERUN / FORMULA UNCHANGED / REFINED BY
`2026-07-20_THE_RUNTIME_RETURNS_THROUGH_THE_DEED_THE_TRACE_CANNOT_SUBSTITUTE_FOR_THE_CONSEQUENCE.md`

## 0. Present question, artifact, and stop

The present question was which small language/code application is honestly supported by the
machine now, after distinguishing the capabilities established through several historical and
current interfaces.

Standing evidence was insufficient because the most recent linguistic application combined two
different facts:

1. the host graded-cell carrier really formed and rested a large regional constituent; and
2. the application supplied that constituent by interpolating byte positions across unrelated
   inscriptions.

The first fact is engine evidence. The second cannot establish a linguistic or coding relation.

The sought artifact was one direct, host-only application whose incidence came from the actual
runtime transformation, together with a readable receipt and an exact stop at the first missing
causal relation. The run would not search another fixture, weaken a condition, invoke CUDA, or
repeat for a cleaner result.

That artifact now exists. It stopped because the application presented the runtime trace as later
testimony without first making execution the material enactment of an emitted machine deed. The
result does not establish a missing learning law inside Soma.

## I. Capability audit

The reusable current production substrate is:

```text
world event
  -> LiveCurrentMachine::receive_with
  -> same-pre-state host/card enactment
  -> machine-validated atomic successor
  -> immediate radiation
  -> optional genuinely later world consequence
  -> departure / natural rest / exact remount
```

Bounded measurements have established directed hand, actual world action and return, plural
heterogeneous consequences, durable outer composition, resident CUDA carriers, and
participation-only scaling. The bounded polyglot arithmetic world additionally established exact
source-derived relations whose returned emission changed a later probe.

Those results do not establish autonomous objective interpretation, source selection, a semantic
decoder, a general local-map rebase, or useful language/code conduct.

The historical code workshops remain evidence that programs, prose, source spans, compiler
consequences, and returned paths can coexist. Their historical session/active-cut interfaces do
not define production. Exact coordinate overlap did not establish content analogy, and most
candidate edits did not compile.

The retired direct linguistic world's `host-run-09` remains valid graded-carrier stress evidence:
2,480 arcs crossed one regional event and the resulting constituent rested exactly. It is not
language evidence because `regional_boundary_arcs` created the arcs from inscription lengths.

### Recovered constraints from the recent pivots

The recent movement among language, code, primes, observation, cellular geometry, completion, and
information chemistry did not introduce seven separate machines. It repeatedly approached the
same construction from different world faces. The following requirements are retained explicitly
so they are neither lost nor mistaken for scheduled subsystems:

- I/O is the one world--machine--world crossing. Source-native chronology, co-presence, directed
  hand, material consequence, and later return belong to that crossing; storage or caller order
  cannot supply them.
- An event may contain actual ordered internal stages without those stages becoming fabricated
  external instants. Event grain and internal transformation grain must remain typed apart.
- Completion is relative to the present objective, frame, receiver, and grain. Runtime exit, EOF,
  success, a threshold, or a fixed deed pattern cannot declare it universally.
- Compression is the event's emitted successor: an interior may depart while its consequential
  boundary action remains available. It is not an installed optimizer or later deduplication pass.
- Language and code become grounded only through actual transducers and consequences: runtimes,
  edits, compiler refusals, execution traces, gestures, images, sound, or other lawful world
  actions. Opaque text is honest input; it is not yet semantic relation.
- Probability and loss may describe receiver-relative recurrence and changed coherence, but they
  cannot become scores, rewards, punishments, candidate ranking, or a hidden search policy.
- Observation must say what a cell, incidence, hand, path, residual, and consequence are about.
  A large receipt, count, hash, or attractive raster is not an explanation.
- Analytical structure remains exact: integers, ratios, algebraic values, and formal series.
  Floating coordinates, `Math.PI`, fitted cameras, and screen distance cannot become causal data.
- Objectives and their active constituents may change while a construction lives; only presently
  participating material need enter. This remains a lifecycle requirement, not an objective
  manager or neural-attention module.
- The proposed information-chemistry vocabulary—binding, refusal, phase boundary, chirality,
  catalysis, and equilibrium—remains a useful way to derive source-supplied local action among
  asymmetric germs. It does not authorize a chemistry classifier or potential field inside Soma.

Prime/RH work and observatory redesign remain separate present questions. Neither supplies the
missing code-world rebase, and neither is scheduled by this review.

## II. Source sanitation decision

The situated-germ correction had already rejected three source operations:

- `SparseStandingSurface::transport_sections`, which selected old sections by equal transported
  `Place`;
- `compose_cellular_replacement`, which merged axes and boundaries through that equality; and
- `discover_bounded_founded_regional_contact`, which searched a finite literal chart for a
  convenient FOUND.

They were still present in the active source. They are now removed. Axis-rebase helpers used only
by that replacement were removed with them.

The independently lawful graded-cell machinery remains:

- source-declared cells, stage, and grain;
- boundary and dependency incidence;
- hands and ports;
- receiver-formed pins;
- sparse path transport and exposed residual;
- event-local completion and constituent hand-up;
- co-present disjoint regional changes; and
- exact Standing rest/remount.

Until a source-carried local action and rebase cross the membrane, regional completion touches no
prior Standing constituent. It emits a new constituent. The engine no longer claims that equal
coordinates compose two germs.

## III. The smallest latent germ

`EventComplex` already carries the complete finite event-local map needed by this application.
Adding another causal representation would be redundant. The new
`body::incidence::DiscreteEventGerm` is therefore an allocation-free validating lens over that
same complex:

```text
EventComplex
  cells(stage, grain, relation)
  Boundary incidence
  Dependency incidence
  oriented hand + local slot
  ingress ports
  exposed ports

DiscreteEventGerm(EventComplex)
  domain stage
  image stage
  immediately composed dependency steps
  forward multiplicity at each cell
  inverse multiplicity at each cell
```

It refuses a static complex, a complex with no dependency action, ingress outside the first stage,
exposure outside the last stage, or a dependency which skips an internal stage.

This is exact for a discrete event-local map:

- forward multiplicity `1` is one carried continuation;
- forward multiplicity `0` is a terminal/deleted residual at that step;
- forward multiplicity greater than `1` is branching;
- inverse multiplicity greater than `1` is a fold/many-sheet image; and
- the ordered chain of stages is the actual composition order inside one event.

It deliberately does not claim an analytic derivative, holomorphic critical order, continuous
rank, or a cross-event conjugacy. Transport to a nonidentical later star still owes an exact
source-carried rebase.

## IV. Replacement application law

The bounded world uses two English objectives and two instrumented Python programs. The programs
receive an integer JSON list and emit their actual occurrence-preserving runtime states. Every
initial occurrence receives its input ordinal as an event-local ID; values cannot change or be
invented; each declared operation is checked against consecutive states.

The world lifts that actual trace as follows:

| Runtime datum | Exact event geometry |
|---|---|
| one list state | one internal stage |
| one occurrence | one grain-zero cell carrying its exact value |
| list start/end | bounded source-owned boundary cells |
| adjacency | an oriented grain-one boundary edge |
| occurrence persists/reorders | dependency incidence to the next stage |
| several prior edges become one image edge | plural inverse dependencies / a fold |
| occurrence has no later image | exposed residual at its last participating stage |
| input path | ingress ports |
| final path | exposed ports |

The objective and Python inscription also cross as complete opaque event complexes. No ports are
aligned by length. The world supplies only the actual request and runtime causal hands. The
runtime consequence occurs later in world chronology; its three transformation stages remain one
event rather than three fabricated external instants.

The controls are intentionally noncommutative:

```text
remove then reverse:
  [1]       -> []         -> []
  [1,2,3,4] -> [2,3,4]   -> [4,3,2]

reverse then remove:
  [1]       -> [1]        -> []
  [1,2,3,4] -> [4,3,2,1] -> [3,2,1]
```

The collapse input proves that equal final output does not imply equal transformation interior.
The larger input exposes the noncommuting outputs.

## V. One-run result

Both actual Python runtimes succeeded with empty stderr. All four consequences validated as exact
three-stage discrete germs.

For `remove -> reverse`:

- collapse germ: 11 cells, 15 total incidences, 7 dependencies, 2 critical cells, 1 residual;
- distinguishing germ: 29 cells, 49 total incidences, 23 dependencies, 6 critical cells,
  1 residual; and
- the distinguishing event reported one whole-event fold.

For `reverse -> remove`:

- collapse germ: 13 cells, 19 total incidences, 9 dependencies, 2 critical cells, 1 residual;
- distinguishing germ: 31 cells, 55 total incidences, 27 dependencies, 7 critical cells,
  1 residual; and
- the distinguishing event reported one whole-event fold.

The controls established the world geometry:

- collapse outputs equal: true;
- collapse interiors differ: true;
- distinguishing outputs differ: true;
- every live lineage departed: true; and
- both body rests encoded and remounted exactly: true.

They did not establish machine use of the germ:

- every runtime-trace incidence contact remained open;
- every runtime trace emitted zero material deeds;
- no runtime regional constituent entered Standing;
- the two identical later probes returned identical radiation; and
- the differing rest surfaces were conditioned by recurring objective/program inscriptions, whose
  later static incidences did form, not by a transported runtime germ.

The result is therefore **STOPPED**, not accepted as a completed language/code application.

## VI. Regraded boundary

The source exposes a useful event-local object, but the application did not traverse the already
measured causal learning relation. It performed:

```text
request
  -> unconditional external Python execution
  -> later descriptive trace event
```

It did not perform:

```text
machine emission
  -> world enacts that exact deed
  -> actual execution consequence returns
  -> changed later conduct
```

The zero runtime contact therefore grades the application boundary. Whether a further
contact-formed rebase is required for algorithmic generalization remains untested. The authorized
grounded successor is recorded in the refining plan named above; this historical record schedules
nothing else.

## VII. Procedure consolidation

`AGENTS.md` is now Sol's sole routine handoff. `NOW.md` was removed. `STATE.md` and `LEDGER.md` are
marked historical archives and are not routine reads or schedulers. `CLAUDE.md` is excluded unless
Brandon directly requests it. The intrusive `situate-holonics` skill was removed and must not be
restored.

This consolidation is operational, not ontological: exact research and observation records remain
available on demand, but they no longer make Sol replay the entire project before answering the
present question.

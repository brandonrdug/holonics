# L5 standing-reuse receipt: the returned world-tube enters as one rank-four section, not four serial deposits

## Disposition

[definition] This is the mandatory standing-reuse receipt for L5 of the sole active campaign.  The
one continuing owner is the source-detached `SituatedCultivatedAthenaRest` admitted at L2, with
identity `84b9d29f4d7b3e6ce433d7efe8ea35903477900dcd29554a36ab15700b9629af`.
The ingress ports are `AddressedMaterialOccurrence` and the complete exchange/repository occurrence
ports; the hot conduct owner is `ResidentCoupledComplexParametron`; the return port is the complete
`SituatedDifferenceSection`; and the requested artifact is one cultivated rest which remounts
without any developmental source apparatus.

## Existing composition

[implemented-exact] The standing body already owns four independent period directions and their
complete symmetric constitutive response.  Its L2 deposit is one atomic population

```text
Lambda_p ~= Z^4,
n = (n_gamma, n_u, n_w, n_delta),
Sym^2(Z^4) = 4 diagonal + 6 distinct-pair = 10 terms.
```

[proved-derived; formal-checked] The four coefficient directions are reconstructed by the four
dual cycle probes in the elementary-holonics line.  They are not four chronological events.  The
six oriented planes of `Exterior^2(Z^4)` are a different receiver from the ten-term symmetric
constitutive body.  The associated complex-torus quotient is `C^2 / Lambda_p`; no quotient
`Lambda_p / Z^4` is assumed.

[implemented-exact] L0 already owns the full returned difference: candidate and returned native
sections, addressed pullback occurrence, receiver fibres, exact chart transport, Complex-Parametron
current, reverse causal-adjoint word, radical, obstruction and open exterior.  The native spool
owner already owns move-only thread deposition, withdrawal and exact restoration.  L3 already
factors prose, notation, optical, Rust and Lean delivery faces through one source-neutral causal
operation section.  L4 already supplies perspective-sensitive emanation and a genuine later world
return while move-owning the same L2 rest.

## Missing edge

[open] The exact missing composition is the return edge from a full `SituatedDifferenceSection`
into the same continuing morphology.  L4's sparse exterior atom difference is not this carrier and
must never be promoted into native coordinates: deed names, voices, participant labels, prompts,
surface strings and later experiment targets are exterior receiver faces.

[definition] One L5 return therefore has two different indices:

```text
causal order:       t -> t + 1 -> ...
winding coordinate: a in {gamma, u, w, delta}.
```

At one causal order `t`, the complete return supplies one addressed section `Delta_t`.  The four
dual probes return its coefficient vector.  The ecology changes atomically by that whole section;
there are no intermediate one-coordinate rests.  Any later return is another caused event, not the
next member of a hard-coded four-step loop.

## Finger-trap result

[definition] No trainer, conversation index, lexical atlas, parser, semantic router, scheduler or
second ecology is authorized.  L5 must rotate these existing owners into contact:

```text
complete addressed material
  -> existing native conduct and world consequence
  -> full SituatedDifferenceSection
  -> one atomic rank-four returned deposit
  -> same coupled rest
  -> GPU resident later conduct
  -> exterior receiver
  -> returned difference.
```

[counterexample] A sequence of four singular deposits, a digest/count-only substitute for the
addressed world-tube, a semantic atom vector, an L4 surface mutation presented as training, or a
rest which retains source paths/interiors is a failed L5 construction.

## Required return

[definition] The L5 artifact must prove changed same-body conduct at the prose, mathematics and
code receivers; detached remount; target withdrawal with every incident term; exact restoration;
and target-name/query inertness.  The complete conversation and repository apparatus may be
consumed incrementally, but its only hot survivor is source-neutral returned morphology with its
reconstruction fibre.  Counts, hashes, timings and GPU telemetry accompany this return and never
replace it.

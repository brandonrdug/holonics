# The whole foreign map crossed once and the native rest returned without its source

**Date:** 2026-08-20  
**Kind:** Phoenix Deed W1 construction return and grade.  
**Truth discipline:** **established-bounded** for the executed seal, remount, exact content
identities, focused tests, and process-local source-access audit; **interpretation** for the
architectural reading; **open** for execution, condensation, cultivation, frozen inference, and
the indivisible Phoenix grade.

## Verdict

**Truth status: established-bounded.**

Deed W1 passed its declared boundary. The complete Station-B source closure crossed into one
source-detached native rest:

| returned face | measured return |
|---|---:|
| concrete admitted operation complexes | 45 |
| source operation occurrences | 1,466 |
| native operation correspondences | 1,466 |
| admitted source populations | 2,130 |
| exact native population correspondences | 2,130 |
| exterior vocabulary rows | 262,144 |
| graph registries | 1 |
| graph nodes / addressed edges | 1,466 / 3,276 |
| native-rest octets | 16,022,507,913 |

The original safetensors source is 15,992,595,884 octets. W1 quotes no compression factor. The
native rest is deliberately source-shaped and larger because it carries the authenticated,
path-free operation topology, correspondence, codebook descriptor, reconstruction testimony, and
wire manifest beside every exact payload octet.

This passes W1 and nothing later. W2 remains the first execution and structural-condensation deed.

## What crossed

**Truth status: established-bounded.**

The production seam is owned by:

- `foreign_codec_rest`: all 262,144 source/native exterior addresses and surfaces, plus
  content-addressed tokenizer companion descriptors;
- `operation_correspondence`: the 1,466 source occurrences, their resident-law/typed-port
  bindings, all 2,130 population dispositions, and one nonduplicated graph registry;
- `native_rest`: a manifest-authenticated, file-backed native container whose exact payload is
  streamed and whose source locators are unrepresentable after seal;
- `examples/phoenix/modality.rs`: one shared constructor for the exact Station-B vision/audio
  projection deeds, replacing two authored copies;
- `w1_admission` and `w1_correspondence`: construction-boundary composition of the already returned
  Station-B manifest with the 42 layer deeds, final boundary, and two admitted modality projection
  deeds.

Every operation passed source authentication and resident-law entailment under its declared
configuration scope. Every population crossed as exact native material. No source operation or
population remained open in the W1 correspondence.

The graph body is not repeated in every operation binding. One graph registry owns its 45
path-independent topology identities and 90 addressed chronology/bond witnesses; each operation
binding carries only the graph key.

## One-pass material crossing

**Truth status: established-bounded.**

`NativeRest::seal_streamed` uses a seekable output and a fixed-size authenticated-manifest
placeholder. It reads each external population once, checks its Station-B SHA-256 while the bytes
cross, accumulates the payload SHA-256, then backpatches the same-length manifest and its manifest
digest. It does not perform the earlier two-pass 32 GB source replay and does not retain the model
payload in RAM.

Station B and `AuthenticatedContainer` expose two legitimate header receivers: Station B hashes
the JSON header body; `AuthenticatedContainer` hashes the eight-octet length word together with that
body. W1 verifies both apertures explicitly. It does not identify their unequal hashes or waive
either check.

## Returned artifacts

**Truth status: established-bounded.**

The output population is bound in `meta/OUTPUT_MANIFEST.tsv`:

~~~text
output/the_whole_foreign_map_crosses_into_native_rest/
  gemma_native_rest.bin                 16,022,507,913 octets
  operation_correspondence.json             3,981,251 octets
  codec/cc8d...fe0f  tokenizer.json        32,169,626 octets
  codec/9f4f...2633  tokenizer config           3,082 octets
  w1_receipt.json                               1,222 octets
~~~

Exact identities:

~~~text
native rest       52d0be9d6439d9d58baacbf6fa947ee3b3251403164205852733713ba4849453
correspondence    e001363aed6c1dd162a000a151715ca22c7801b5dcfe291e9379d3b6d3adb773
tokenizer         cc8d3a0ce36466ccc1278bf987df5f71db1719b9ca6b4118264f45cb627bfe0f
tokenizer config  9f4fec4b1dc6ecddf8f4a92e9caea5971c0e67d81309f3f9066a2bee8c362633
output population 028ba619536044de48758ba0ee6acd751ed20ccbc1e64c594dbaeb05ba072bb6
~~~

The complete codebook's semantic digest is
`e26e56406613a0e61fa5ab9cb43a51a90cca4a7f1ebd5ece3b952a164952c4f8`; its compact tracked TSV
file digest is `7ca89d9ee353de5a15bfb3aca799500c206ad137b7a9dc4b11a76ce814af41b8`.

## Fresh-process return

**Truth status: established-bounded.**

The remount process received only the native-rest path and the content-addressed companion
directory. It authenticated the manifest before parsing it, streamed the complete payload digest,
authenticated both codec companions, checked the four whole-body censuses, then read and hashed
the first, middle, and last population in native order. The returned sample hashes were:

~~~text
model.audio_tower.layers.0.feed_forward1.ffw_layer_1.input_max
  f0055891f09fff4839b344a4af5e0482696cab0e0bf307455661ba4056c787ee
model.language_model.layers.25.mlp.up_proj.weight
  0e791e1d7313000558384aa0bb091252195a1a24abe7e86905906d7e89dda04d
model.vision_tower.patch_embedder.position_embedding_table
  f1247b6ab638d4f79246c3c20c3b0ed4a7601851cb87f418600804f871be7ae8
~~~

Each equals the source-region identity retained in the authenticated manifest. A post-read
`/proc/self/fd` audit refused any descriptor resolving to the original model, implementation,
configuration, or tokenizer path. A second independent invocation returned the same whole-body
census and sample identities in 7.3 seconds.

The original source files still exist on the operator's host. “Source detached” here is the
declared process/apparatus fact: the remount process receives no source address, opens no source
descriptor, and the sealed wire contains no original path. It is not a claim that W1 deleted the
operator's source files.

## Falsifiers and controls

**Truth status: established-bounded.**

- Missing, duplicate, or foreign operation/population correspondence refuses sealing.
- A fabricated graph key, missing graph registry, empty chronology/topology, or graph mutation
  refuses validation.
- A source-path relocation leaves topology identity unchanged; changing the testified symbol does
  not.
- Missing source regions, duplicate native names, extent drift, region-digest drift, payload gaps,
  payload-digest drift, manifest-byte mutation, source-identity mutation, and trailing wire octets
  refuse.
- Partial vocabulary coverage requires one exact named open fibre; full coverage refuses any open
  vocabulary fibre.
- Tokenizer descriptor drift and companion-byte drift refuse independently.
- The three focused library suites returned 8, 16, and 13 passing tests respectively; the W1
  example and the refactored Station-B example compile.
- The architecture ratchet admits only the new receipt/inventory collections. Its current three
  remaining violations belong exclusively to the quarantined dirty P5 `atlas_cultivation.rs` and
  are not promoted by W1.

The new ownership-ledger rows are not a blanket reseed. They name the immutable codebook rows,
manifest/population descriptors, exact source/native correspondence maps, graph registry, and the
bounded maps/sets required to prove uniqueness and closure. Whole codebooks, codec artifacts,
native rests, graph bodies, and topology complexes are not cloned by the production W1 path.

## Gate isolation in the shared dirty tree

**Truth status: established-bounded.**

The focused eight-gate run returned four passes and four failures:

| gate | W1 reading |
|---|---|
| authored levels | pass |
| named paths | pass |
| claim index | pass |
| document law | pass |
| driver catalog | only the quarantined dirty P5 driver is uncatalogued |
| output manifest | only the quarantined dirty P5 output is unrecorded |
| closure manifest | only `soma/life` moved, due the quarantined P5 source |
| architecture lint | only P5's `atlas_cultivation.rs` exceeds its prior counts |

Thus W1 introduces no remaining failure in these gates. The four whole-tree failures are retained
as exact evidence that the shared tree still contains Claude's unadmitted P5 work; W1 neither
adopts it nor hides it. The three W1 library suites and the real W1 deed returned independently.

## Boundary and next deed

**Truth status: open.**

W1 does not execute Gemma, compare source-runtime logits/futures, condense the lifted body,
cultivate it, expose `eros phoenix infer`, use the GPU, or complete Phoenix. The admitted modality
topology consists of the two shared-stream projection deeds already founded by Station B; the
vision/audio tower interiors remain exact stored native populations but are not invented as
operation complexes.

W2 must mount this exact W1 rest in a fresh process, place the already-founded passage on the GPU,
reproduce Station C/H4 plural futures and intervention faces, and then attempt receiver-exact
structural condensation on this lifted body. Independent ARM N remains a control.

# THE RESERVATION BELONGS TO THE CURRENT

**Status:** PER-CURRENT MATERIAL CARRIER MOUTH BUILT · HOST/CUDA-GATED · SLEEP v8 MIXED ROWS BUILT ·
CONVERSATION HORIZON BROAD GATE CROSSES UNCHANGED · SHARDED WHOLE-WORLD PORT OPEN

## The conversation horizon found the standing aperture wall

CONVERSATION HORIZON 01 freezes one real user/assistant turn, a later visual probe, and a later
`proceed` edge. Its world, render, three gaze geometries, source records, hashes, and lifecycle
reader regenerate exactly before card data. The whole Soma workspace passes 225 tests with three
hardware/observation tests ignored; `life` passes 65; the release mouth builds; `kernel/soma.spv`
validates for Vulkan 1.2.

The first broad context gate did not reach CUDA. Linux killed `life` at 20,288,068 KiB anonymous
resident memory while the RTX remained idle. The serial geometry then completed in 9.13 seconds:

```text
46 co-present contacts/material currents
79,399 samples
receiving axis 1,024
332,923 occupied grips
whole sleeping body host/CUDA exact
```

This is a clean geometry foil. Broad carries 9,720 visual contacts through ten gaze events; serial
carries 45 visual contacts through 1,726 gaze events. Their visual sample totals are comparable
(97,200 and 77,670). Sample volume did not cause the failure.

## The exact amplification

The current bounded material mouth chooses one carrier reservation for a complete cohort:

```text
D_cohort = max_i (|L_i| - 1)
row(D)   = 103 + 290 D words
```

The broad visual currents each need depth nine. The co-present exact visible-source current is
1,729 octets and therefore selects depth 1,728 for every one of the 9,721 currents. The apparatus
allocates:

```text
(103 + 290·1,728) · 4 · 9,721
= 19,489,555,132 carrier octets
```

before topology and listener material. This predicts the observed host OOM. It also explains why
serial crosses: its 45 visual worldlines already have depth 1,725, so the source sibling scarcely
changes their common row.

If each current carries its own delivered extent, the identical broad light requires:

```text
9,720 · (103 + 290·9) · 4
+       (103 + 290·1,728) · 4
= 107,486,332 carrier octets
```

No current, gaze event, source octet, enclosure, or card channel disappears. Only unrelated empty
reservation is no longer multiplied across siblings.

## Law reading

The cohort-maximum row was already graded in
`RESEARCH/2026-07-12_THE_CUDA_MATERIAL_LISTENER.md` as a delivered-material-derived bounded
observation aperture, explicitly **not general carrier law**. `LEDGER.md` leaves general carrier
admission open. The current observation has reached that named wall.

The correction follows existing law without changing the body:

1. A lineage is one live current's carried identity. Its carrier and reservation belong to that
   current, not to a sibling or cohort.
2. The complete delivered worldline derives the bounded observation reservation
   `D_i = |L_i|-1` for that current. This is an apparatus extent, not a machine cap.
3. Co-presence requires simultaneous carriage and one receiving fold. It does not require equal
   allocation shapes.
4. The carrier row remains the exact canonical body row. Only the membrane framing which gives the
   row to its CUDA invocation changes.

## Presented engine cut

**Live mount.** Pack the exact carrier rows contiguously. Each existing REGISTER lane descriptor
gains `carrier_word_offset` and `carrier_row_words`. The CUDA shell validates that exact disjoint
span and passes it to the unchanged `body::carriage` mouth. The body already accepts a checked
`WordSpan`; no carrier content, deed, current order, integration, or chart rule changes. Prefix
placement is structural buffer framing at the membrane, never a resident topology address or
search.

**Host mirror.** Derive `D_i` independently and carry each host lineage at its own reservation.
The receiving edge accepts complete canonical rows of differing extents. It no longer asserts that
the first sibling's row width is every sibling's width.

**Rest.** The sleeping body preserves each complete row exactly. The next SLEEP wire frames every
row by its own checked extent and then carries the concatenated row words. Framing words remain
archive/container structure and do not enter the identity-bearing `body_bytes` measure. Historical
uniform-row archives decode by repeating their one declared extent; no historical construction is
rewritten. Mixed rows require a new canonical SLEEP version rather than pretending to be V7.

**CUDA housekeeping.** Extend only the REGISTER production descriptor. The fixed founded SPIR-V
gate keeps its established bounded ABI. The CUDA REGISTER recast transaction preserves the two new
immutable carrier fields while rebuilding OWN offsets. Sharded production derives partition-local
carrier offsets from the same exact rows. `main.rs` gains nothing.

## Required gates before returning to the horizon

1. Two unequal worldlines use the sum of their exact row extents, not `lanes·max(row)`, while host
   and CUDA constructions remain exact.
2. A bounded common-row sibling and the new per-current layout are construction-identical for the
   same lights, proving the membrane layout is gauge.
3. Mixed-row SLEEP create/read is exact; V7 uniform rows migrate exactly; malformed or overlapping
   row framing refuses before mutation.
4. Sleep/wake continuation over a mixed-row body agrees with its never-slept bounded sibling.
5. The serial conversation gate remains exact.
6. The broad user and assistant gates complete without changing their 9,721 currents or their
   delivered samples. Only then do broad, reverse, serial, and repeat histories run.

## Grade and boundary

**MEASURED:** the broad cohort-maximum apparatus fails at 20,288,068 KiB resident host memory;
the comparable-sample serial species is host/CUDA-exact.

**DERIVED AND RATIFIED:** carrier reservation is per current; variable row framing is the direct
engine correction. Brandon read and ratified this declaration on 2026-07-13.

## Build and measurement

The ratified cut is built through the host and CUDA material mouths. The REGISTER descriptor widened
from eight to ten words by adding checked carrier word offset ⊕ exact row extent. CUDA validates
that span before passing the unchanged `WordSpan` to body carriage, and OWN recast preserves both
fields. Host and card derive each row from that current's own delivered extent and meet once at the
receiving edge.

SLEEP v8 carries one structural row-extent word per lineage before the concatenated canonical rows.
The framing is excluded from the identity-bearing body measure. Mixed depth-one/depth-three rows
round-trip exactly; malformed framing refuses; historical uniform archives migrate into canonical
v8; sleep/wake continuation remains exact.

The fixed REGISTER card gate and fresh-context remount gate are exact on the RTX 4080 SUPER. The
full conversation-horizon gate then passes all eight manifests. The unchanged broad user field now
crosses as 9,721 currents / 98,929 samples / receiving axis 256 / 31,816 occupied grips. Its carrier
aggregate is 26,871,583 words instead of the failed 4,872,388,783-word cohort-maximum allocation.
The serial foil remains host/CUDA-exact.

At deposit, the complete workspace passes 229 tests with three ignored; release `life` builds; the
17-entry PTX is `ptxas -arch=sm_89` valid; SPIR-V remains Vulkan 1.2-valid. Exact horizon lifecycle
measurements are deposited in `observations/conversation-horizon-01/RESULTS.md`.

**BUILT / MEASURED:** per-current reservation at the material host/CUDA mouth; exact mixed-row
SLEEP v8; unchanged broad horizon admission; exact repeat.

**OPEN:** the sharded whole-world production path still constructs a common row inside each shard
and must be ported before §XLIII is claimed for that separate mouth. The single extremely long
worldline admission problem remains the separately named reservation re-base/retirement question.

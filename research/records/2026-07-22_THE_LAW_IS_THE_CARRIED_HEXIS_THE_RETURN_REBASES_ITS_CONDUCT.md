# THE LAW IS THE CARRIED HEXIS; THE RETURN REBASES ITS CONDUCT

**Status:** BRANDON-RATIFIED SYNTHESIS / DEPOSITED / BOUNDED EXACT LAW-ECOLOGY BUILT / ONE HOST RUN ACCEPTED / SOMA SOURCE UNCHANGED / NO FLOAT / NO CUDA

## Correction

The prior contrast with a “universal taxonomy” answered a proposition Brandon did not make. It is
withdrawn. A taxonomy is simply standing information founded by intelligence in a situated
ecology. Its distinctions, omissions, revisions, disagreements, and changing relevance are part
of its lineage. It requires no absolute foil in order to participate lawfully.

At a declared boundary, frame, and grain, a taxon is a recurring relational face of the actual
population. For exact population `P` and a situated observation `q`,

```text
T_(P,q)(a) = {x in P : q(x)=a},
mu_(P,q)(a) = |T_(P,q)(a)| / |P|.
```

`q` may itself be inherited, founded, revised, split, or allowed to depart. The exact members and
their relations precede this quotient; the quotient does not identify their interiors.

## One law-bearing holon

An inherited transformation is not a cached answer or a finished edge. At a selected cut its
current hexis is

```text
H_t = (S_t, I_t, T_t, U_t, D_t),
```

where:

- `S_t` is its exact persistent state and parameters;
- `I_t` is its exposed ingress and consequence interface;
- `T_t` is the transformation relation it presently affords;
- `U_t` is the law by which an actual returned consequence may rebase that relation; and
- `D_t` is the phase in which the present relation is valid, including its open boundary.

This factorization is relative. A source file, tokenizer, trained block, compiler, algebraic
identity, social convention, or world instrument may already embody such a hexis. Eros need not
pretend to rediscover it. The source declaration is current; applying it to a new population,
receiving its consequence, and carrying the changed law are later events.

The hexis need not be stored wholly inside one `LiveConstituent`. The active ecology consists of
world material, source organs, Soma Standing and live currents, and returned consequence. Exact
outer rest must preserve the parts that remain consequential without retaining spent event
populations or cached emissions.

## Population conduct

Let `K_t` be the actual oriented incidence complex, `n_t` an exact extensive population on its
places, and `J_t` the complete oriented current afforded by the co-present hexis population:

```text
J_t = Conduct_(H_t,K_t)(n_t,F_t),
n_(t+1) - n_t = -partial_t J_t + R_t + B_t,
(H_(t+1), K_(t+1), A_(t+1))
    = Emanate(H_t,K_t,A_t,J_t,r_t).
```

`R_t` is an actual returned transfer and `B_t` is a declared boundary source or sink. These are
typed currents, not corrections hidden inside the quotient. Every participating branch is solved
against one pre-event population; the successor becomes visible once.

Equilibrium at the chosen cut means

```text
-partial J_* + R_* + B_* = 0.
```

It does not mean `J_*=0`. A cycle may carry nonzero circulating current while every local amount
remains unchanged. Detailed balance is the stronger pairwise cancellation condition. Thus equal
standing populations can conceal different conduct, susceptibility, and holonomy.

## Probability is a receiver quotient, not the transport law

For exact positive population `n_i`,

```text
N = sum_i n_i,
mu_i = n_i/N.
```

`mu` is what the selected receiver can say about recurrence within that population. It discards
absolute capacity: `(1,1)` and `(100,100)` have the same quotient and need not admit the same
current. If extensive conduct obeys `dn_i/dt=n_i f_i`, normalization gives

```text
dmu_i/dt = mu_i (f_i - sum_j mu_j f_j).
```

The relative expression is derived from the extensive law; it does not drive it. Bayesian update
is the same kind of lawful reweighting after a new observation changes the admitted fiber:

```text
mu_(t+1)(h) = L(e|h) mu_t(h) / sum_g L(e|g) mu_t(g).
```

All terms are exact ratios in the bounded construction. No floating approximation is needed.

## Loss is the oriented failure of composition

For afforded transport `a` and returned transport `c`, loss at this cut is the complete oriented
residual

```text
r_t = c_t - Transport(a_t).
```

Its direction, interface, order, and remaining boundary are material. A nonnegative scalar norm,
likelihood, or objective value is one later projection under a declared observer. Equal scalar
magnitudes can carry opposite hands and therefore rebase the hexis differently. Loss becomes
causal only when `r_t` crosses the consequence interface and `U_t` applies it.

This is neither reward nor punishment. It is the information that the attempted composition did
not carry.

## Curvature is path-order dependence of transport

Two contextual turns `u` and `v` test the same carrier in opposite orders:

```text
x_uv = T_v T_u x,
x_vu = T_u T_v x,
C_H(u,v;x) = (x_uv, x_vu).
```

The complete ordered pair is the curvature witness. Where both transports share one exact linear
fiber, its difference face is

```text
Omega_H(u,v)x = (T_v T_u - T_u T_v)x.
```

A closed return word supplies holonomy. Relative curvature between two holons is defined only
after an actual interface transports both into a common comparison fiber. Gravitas is the
second-order divergence of nearby lawful continuations. Ricci curvature requires an additional
metric and transverse trace; incidence alone does not manufacture either.

## Construction boundary

The bounded construction will use exact integer populations, exact ratios, and exact unimodular
transformation words. The transformation definitions and their mutable current state remain in a
world-owned law image; one generic conductor applies them. Their actual transformed faces and
source-declared regional incidence cross the existing direct Soma mouth.

The fixed contrasts are:

1. the same extensive population under detailed-balanced and circulating laws, showing equal
   normalized taxonomy with different current and holonomy;
2. proportionally equal populations at different capacity, showing equal probability quotient
   with different conductance;
3. two equal-magnitude, opposite-hand residuals, showing distinct law rebases and distinct later
   responses to one identical probe;
4. exact halt/remount of machine, organ capabilities, law definitions, and current law state;
5. recomputation after remount from carried law and current input, with no stored prior output;
6. exact situated taxonomic fibers over the resulting population, retaining their founding chart
   and counts.

The construction succeeds only if a nontrivial inherited law survives rest, reforms its relation
under changed context, receives an oriented residual, and changes later conduct. Soma's existing
regional incidence remains the receiving topology. No new engine subsystem is licensed unless
the complete bounded path exposes a precise missing carrier that cannot be supplied by the world,
organ, or existing live constituent.

## Built result

The bounded host cell is accepted. One three-law population crossed as one grain-4 regional
constituent. Detailed-balanced and circulating networks shared population and normalized quotient
while differing in holonomy. Equal probability quotients concealed different capacity. Opposite
unit residual hands retained the same exact `(determinant,trace)` fibers but rebased the first law
to `[[1,2],[0,1]]` versus identity; one identical later probe returned `(-5,4)` versus `(-3,2)`.
Machine, organs, and mutable law image remounted byte-exactly, and a second probe was recomputed
from the carried law. Soma source was unchanged.

Exact record: `observations/eros-inherited-law-ecology-01/RESULTS.md`.

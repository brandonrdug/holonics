# SKE5: the composed variant runs the recurrence on the family, states condensation by its remainder, and is not saturated

**Date:** 2026-09-03, corrected 2026-09-04 after Sol's review
([`the review`](./2026-09-04_SOL_REVIEW_OF_THE_SKE_CAMPAIGN_THE_CONES_ARE_NOT_FOUNDED_CONES_THE_SPECIES_ARE_OFF_DOMAIN_AND_THE_PASSES_ARE_WITHDRAWN.md)).
**Owner:** `applications/athena-alpha/src/composed_variant.rs` (the composition, the species over
the domain, the width difference at one grain, the product vector, the saturation reading as the
formal `Saturated` reads it, the release receiver); the deed
`applications/athena-alpha/examples/composed_variant_ske5.rs`, one command.
**Receipts:** `2026-09-03_SKE5_receipts/` (`ske5_composed_variant.json`, the composed body's
per-coordinate residual under every exposure as `composed_remainder_*.bin`, `deed.log`), on the
SKE4 return (`2026-09-03_SKE4_receipts/role_cones/`, the rest at its recorded digest).
**Campaign:** SKE5, the last deed of
[`THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md`](../../blueprint/THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md).

## 1. The composition

[definition] Eros is an interpretation, not a module (`canon/05_ONTOLOGY.md`); here it is the
act of taking the five class ecologies of the SKE4 return together as one Athena variant whose
body is the union of their cones, the extent (117 of 127 roles, 1,305,088 of 1,333,248 sites,
enforced as the union by `validate`), mounted from the rest alone with no class selected, and
driving the family's declared exposures through that one body in one continuing session. The
composition is exterior arithmetic over the return; the body and the recurrence are the engine's,
unchanged.

## 2. The recurrence on the composed body

[established-bounded; implemented-exact; measured] Every one of the 15 declared exposures was
admitted to its class and driven through the composed body by one continuing session, generation
0 through 19,125 in fifteen cycles of 1,275 operations each (the receipt carries every
predecessor and successor generation): 15 of 15 faces equal the full operator's (`7`, `5`, `1`;
`9`, `3`, `1`; `7`, `6`, `1`; `3`, `4`, `7`; `8`, `8`, `1`). An occurrence outside the family
(`11 + 2 =`) and an undeclared history (`The difference is `) were refused at admission with
their causes and never driven. Each cycle: 681 launches, 16 section read-outs (the face's
tiles), 1,456 to 1,838 ms.

## 3. The compression law over the composed variant

[established-bounded; measured] **Species.** On the domain the composed body was driven over,
the family's 15 exposures, it collapses no pair the full operator separates and descends on
every word, so its remainder is the propagated enclosure alone and it states **condensation**: a
far population replaced by a compact realizer with a certified remainder (`H.0420`). No rebase is
stated (nothing in a face reading shows invertibility) and no compression (nothing collapsed).

[established-bounded; measured] **The declared decoder.** The second chart's face is recovered
by the mount from the rest (`NativeOperatorResidence::mount_from_intake` over
`NativeConeRestrictedEcology::intake(None)`) and the recurrence
(`NativeFullOperatorSession::advance_cycle`, one continuing session) to the receiver (the
selected face at the terminal position, read once); its cost as measured: 7.8 s to mount, 1,456
to 1,838 ms and 681 launches per cycle. The rest without this decoder is octets, not a
compression.

[established-bounded; measured] **The invariance as a difference.** Per exposure, the composed
face beside the full face: 15 differences, all zero. Per face coordinate, the enclosure the
terminal propagates under the composed body beside the full operator's, both rebased to the
finer grain `2^-47` (the composed body's lookups set its grain at `2^-43`, so its widths are
multiplied by 16 before the subtraction): the composed body's enclosures are wider at every
coordinate, by 360 to 2,340 grains, for `7 + 5 =` under the empty history 101,774 coordinates at
1,350 grains, 41,141 at 870, 39,734 at 1,380, and so on, under every exposure; the composed
body's per-coordinate residual is deposited as octets. No ratio is stated anywhere.

[established-bounded; measured] **The product, as a vector** (`product`): native standing
9,383,688,554 octets of rest, 1,305,088 rows retained as sites, 283,986 whole, 21 addressed,
digest recorded; the executable decoder and its cost as above; retained fibres, the five
occurrences with their addresses; semantic work 1,275 operations, 681 launches, 16 read-outs per
cycle; span 5 occurrences, 3 histories, 15 exposures; residency 15,752,729,188 octets resident
for the composed body, and 15,752,729,188 octets resident for the full operator as its own
census read it after one cycle (the two-cycle census of the session record's day), beside
15,036,138,068 octets of full coefficients; transfer 6,860 octets of ingress and 71,303,168 of
section egress per cycle.

[counterexample; measured] The residency coordinate is the finding of this deed: the composed
body is resident at the full operator's size, the same octet count. The resident surface mounts
every population at its declared shape and the restricted intake delivers zeros where rows are
absent, so the condensation lives in the stored presentation (the rest) and not in the card's
residency. A resident chart that carries the extent's rows alone is a different apparatus and
is not claimed.

## 4. Saturation under the enlargement of the declared exposures

[established-bounded; measured] As the formal `Saturated` reads it, the occurrences stay fixed
and the declared exposures are enlarged one at a time in their declared order; after each the
signature quotient, its separations, the generators (the exposures declared), the relations (the
pairs identified), the obstructions (the two refusals), and the reachable faces are read again:

| exposures declared | classes | separations | relations | reachable faces | the crossing added |
|---|---|---|---|---|---|
| the empty history | 4 | 0 | 1 (`7 + 5 =` with `7 + 6 =`) | 4 | 4 classes, 4 faces |
| + `The second operand is ` | 5 | 1 | 0 | 7 | one class, one separator, one relation removed, 3 faces |
| + `The sum is ` | 5 | 1 | 0 | 8 | one reachable face (`1`) |

The last crossing added a reachable consequence, so **the family is not saturated**; saturation
is not inferred from the declared exposures being exhausted. The blueprint's pass asks for the
saturation receipt, not for saturation; the receipt is returned and says not yet.

## 5. The release receiver

[established-bounded; implemented-exact; measured] The deed's release receiver reads each
condition of the blueprint's pass on the card and returns them together (`release`): one
session over the family, every family face equal, the outside occurrence and the undeclared
history refused, the species stated, the remainder exhibited per coordinate, the decoder
declared, the product vector returned, the saturation receipt returned, the extent the union of
the class cones; all true, `passed: true`. The focused tests of the owner (`cargo test -p
athena-alpha --lib composed_variant`, three tests) and the crate's tests pass; the engine's
2,028 library tests pass.

## 6. What this does not grade

[definition] The composed variant's faces equal the full operator's on the family that ran, and
nothing is claimed beyond that family. Its remainder is exhibited; its species is stated on that
domain; its decoder and product are declared. A qualitative capability, a growth law of the
family, and the residency a restricted chart would give are not claimed. The SKE campaign,
`SKE0` through `SKE5`, is complete at the scopes its records state; no later deed is admitted.

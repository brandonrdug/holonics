# AC1: the material query follows the operative boundary

[definition] This continues AC1's path/reuse construction after the current-metric comparison
and report-support packing. The existing `OperativeContextual` query observes every retained
internal coordinate directly. The new explicit `OperativeBoundary` variant instead observes
what the developing junction transports through its outgoing boundary. The previous variant
remains available; existing models cannot be silently relabeled with the new source chart.

## The changed receiver and the retained source

[definition] The source feature remains `Q(s) tensor Q(c)`, with its existing normalized complex
projector, source qualification, reference contrasts and coefficient law. In the old operative
chart `c=(o,b_plus)`; in the boundary chart `c=o`, containing all three outgoing branches per root.
The entire internal current `b`, contact map `D`, birth chronology and returned deposits remain
in the continuing field. This is a different material receiver family, not a proved quotient of
the old full-interior material learner and not a claim that equal outgoing currents identify sources.

[proved-derived] The existing junction obeys

```
v = 2 (I + D D†)^-1 (u + D b)
o = v-u
b_plus = D† v-b.
```

Thus the interior already affects the boundary through actual incidence. For the boundary
material query, the partial covector in a separately varied `b_plus` coordinate is exactly zero.
This does not make the complete producing adjoint zero. With outgoing covector `g_o`, its paired
return has `lambda=(I+D D†)^-1 g_o`, incoming interior reaction `2 D† lambda`, and the existing
contact response involving both `b` and the produced current. A returned difference can therefore
change contacts and the subsequent encounter with the retained interior.

[definition] The fixed-contact aggregate/parity construction in the
[parallel calculus integration](2026-09-08_AC_THE_PARALLEL_RECEIVER_CALCULUS_REFINES_NATIVE_CONTINUATION.md)
explains the reuse direction: the boundary recurrence factors through `D b` and `D D†` for fixed
`D`, while the blind interior has a parity generator. Changing `D` can expose that interior.
This variant changes which material receiver is installed; it does not delete the remainder or
claim that a fixed aggregate alone closes the complete developing field.

## Native construction

[established-bounded; source-inspected] Source kind 7 / contextual chart version 4 carries the
same outgoing current and its inherited joint error bound, with an explicit zero direct-internal
query extent. Its norm is checked against the actual outgoing coordinates. Both the forward
kernel and exact source adjoint use that projected profile; internal query covectors are exactly
zero, including their bounds, because the projection is independent of those coordinates.
The paired producer still receives its complete retained internal current and contact population.

[established-bounded; source-inspected] Context pairings no longer loop over historical internal
coordinates in this variant. They retain the complex outgoing pairing and actual source/profile
normalization. The material factor population, full field interior and historical state still
grow; this change alone does not make the whole learner compact or establish a consumer cost bound.

[established-bounded; source-inspected] The source descriptor persists in field rest. Tensor
targets, normalized and complete-current metrics, report packing, delayed source handles and the
text adapter compose with the new variant. The research driver exposes it as the explicit
`--material-source operative-boundary` choice. The default source remains the earlier operative
contextual model until actual behavior justifies a different default.

## Verification and actual conversation return

[established-bounded; measured] All 105 constitutive-field CUDA tests pass in 127.28 seconds.
The boundary projection is checked against independent exact quotient derivatives for all three
material metrics, including phase, reflected factors and delayed reception. Its direct internal
query covector is exactly zero, while the paired incoming-interior reaction is nonzero and its
finite deposit changes actual contacts. Tensor targets, packed field serialization and subsequent
conduct after rest/remount pass. Both affected examples compile.

[established-bounded; measured] The matched eight-family study completes 1,930 developmental
occurrences and 1,929 contact deposits with zero numerical section readouts during development.
Development takes 585.57 seconds, compared with 600.65 seconds for the earlier full-interior
query. This is one changed-realization observation, not a general speed bound. The packed
checkpoint is 390,957,776 bytes. The process takes 626.55 seconds with peak RSS 2,122,164 KiB.
The [receipt](2026-09-08_operative_boundary/return.json) retains commands, counts and complete costs.

[counterexample; measured] The response is **`The itive.`**, followed by an actual `EndPart`:
ten text bytes and eleven generated occurrences. It ends before the 128-symbol limit but is still
an unusable answer. Prompt/emission time is 22.24 seconds; its shorter output means that time is
not directly comparable with the former 128-symbol response. This does not complete AC1 or AC2.

[established-bounded; measured] A fresh process preserves the boundary source chart and current
metric, and reproduces body, emission-current history, generation readings and text exactly.
It takes 48.45 seconds with peak RSS 2,118,292 KiB. No extra cultivation or source replay is used.

## Next missing conversational condition

[established-bounded; source-inspected] `ExposureReader` retains and validates the actual
`human-text` / `agent-text` source classes. The text cultivation driver admits both, then passes
each through the same `session.receive(symbol)` call; `TextSymbol::inputs` has no boundary-direction
argument. The part kind is retained in the cold receipt. Generation likewise uses that ordinary
symbol intake after reading the current. Thus, with text and parent incidence fixed, changing
human/agent attribution alone does not change the native excitation. The desired reply direction
is not currently an operative condition of this codec.

[definition] The next AC1–AC2 repair carries the actual conversational direction through declared
native boundary ports and matching receiver charts, including pending-input persistence and
self-emission through the same recurrence. It must preserve byte/class faces while allowing
source/receiver orientation to affect transport. Do not substitute an authored intent classifier,
scene label, response template or a second world-verdict learning law. This omission is not
established as the sole cause of the failed answer. Recurrent generator reuse and useful complete
responses remain open; no further larger unchanged corpus run is scheduled.

# Proof attempt: certified zero navigation, intersection fibres, and the reflected xi obstruction

**Date:** 2026-09-23. **Scope:** Brandon's request to proceed toward a solution/proof using Holonics. This record constructs a general conditional zero-finding theorem, proves its finite/count and shrinking-limit components in Lean, develops the multi-generator intersection law, runs an exact policy control, and attempts the RH-specific reflected-current route. It does **not** prove the Riemann hypothesis or an all-source optimality theorem. No Typst work was done.

The source-specific positivity continuation is [Weil sign partitions and the semilocal positive-carrier attempt](2026-09-23_WEIL_SIGN_PARTITIONS_AND_THE_SEMILOCAL_POSITIVE_CARRIER.md). It follows Brandon's later positive/negative-generator-distribution question and works at the actual prime, archimedean and polar ports.

## The Holon, source, unknown, receiver and action

The Holon is the foundational law-and-ports H=(K,∂ₐ;Π;𝒟;𝓔;G;π). Its oriented region complex K carries boundary phase passages; shared cuts are joined at ports with opposite hands. Its constitution includes the admitted analytic source f, exact jets/remainders, domains, poles and source dynamics. G contains factor/shift/reflection, local residual action and addressed subdivision. Receiving Holons read an integer divisor count, a simple-root capture, a zero-free face or a typed obstruction. The evolving partition/word is a state on H. Retention is the future-sufficient generator and boundary receipt, not a complete event journal.

The supplied source may be algebraic, an exact series with remainder, a shift/difference generator or a product. The unknown is the zero divisor, or the preimage fibre of a requested intersection for several sources. The action is to infer a source-compatible generator route and certify the resulting receiving face. The returned object is a compatible family of exact rational isolating regions plus multiplicity, phase/carry, unresolved fibre and applicable future transformations. A critical strip or critical line is a possible source-specific consequence, not a primitive search geometry.

## A conditional zero-finding theorem

Let f be holomorphic on a neighborhood of a compact initial region B_∅ and nonzero on each boundary/cut the solver admits. Let each addressed split B_w=B_(wL)∪B_(wR) have opposite orientations on its common cut. Suppose the phase receiver computes the exact argument-principle count N(w)≥0, so

  N(w)=N(wL)+N(wR).

If N(∅)>0, then a deterministic address that chooses a positive left child when possible and otherwise the right child exists at every finite depth. Its prefixes are compatible and N remains positive. If the corresponding receiver centres converge to z*, their radii shrink to zero, and every positive count supplies an actual zero in that receiver, continuity forces f(z*)=0. This is a proof of an exact zero address conditional on source and geometric certificates. It does not require z* to be rational, algebraic or representable by a closed expression.

Computation/CertifiedWindingRoute.lean checks the count split, conservation across *every* finite level, canonical positive route and prefix relation, shrinking-witness limit, and combined route-limit theorem. The analytic argument principle already exists in RH/RectangleArgumentPrinciple.lean and WeightedArgumentPrinciple.lean for their declared sources. Their actual count must still be bound to the generic AdditiveCount receiver, and a general source-polymorphic splitting caller must provide zero-free cuts and shrinking regions. If all roots are simple and the compact source has finitely many separated zeros, sufficiently fine admitted regions have count zero or one; a multiple zero can keep count above one and requires its local factor/multiplicity receiver. This termination statement still needs an effective cut-selection/source-enclosure hypothesis to be an algorithm for a given representation.

## Intersecting generator functions

For F=(f₁,…,f_m) with local Jacobian J:X→Y, source motion S and requested residual rate κ, the first-order Holonic action relation is

  Jv+S=−κF.

Computation/IntersectionNavigation.lean proves that a right inverse R of J gives v₀=R(−κF−S), and **every** solution is v₀+ker J. If the requested receiving motion lies outside range J, no first-order action exists. This is the complete local preimage fibre, not a chosen pseudo-inverse disguised as the solution. For holomorphic full-rank F, the implicit-function hypotheses make ker J the tangent freedom of a local zero manifold of the expected dimension. Rank loss returns singular/branching geometry rather than a regular manifold by decree.

The same owner proves log-current transport for (f∘g)u and the exact local branch factor (z−b)^(km) when an order-m outer zero is pulled through an order-k local generator with nonzero residual factors. This is a consistent zero-pattern law for analytic generator composition: multiplicity and phase transport are exact. Identifying the algebraic factor with analytic order additionally needs the analytic/nonvanishing-unit hypotheses.

## A bounded exact navigation experiment

research/experiments/holonic_zero_navigation/ uses a supplied product of two quadratic generators, each with one zero of irrational real coordinate in the rational unit square. Every boundary segment carries an exact rational chord-homotopy certificate; a separate receipt verifier recomputes source values, certificate inequalities, polygon winding, shared-cut reversal, factor-based count and execution traces using the same exact source functions. The generator-centre-height policy separated both zeros with one horizontal cut, 26 exact source values and 49 segment attempts. A fixed-midpoint policy needed three cuts, 34 values and 67 attempts; both had largest recorded rational carrier width 87 bits. One cut is minimal for cut count in that declared horizontal grammar because the two root heights differ. The factor structure was supplied, not inferred; its discovery cost, arithmetic operation count and global optimality are not measured. This is a control for generator-informed navigation, not a ζ performance claim or a replacement for eta_atlas.

The existing eta atlas is the stronger actual-source precedent: exact Euler–Maclaurin/η jets, source-derived head length, certified winding, parent-edge reuse and addressed lineages. Its dated finite scan certified 649 zeros through height 1000 without supplied ordinates. It has a fixed symmetric receiver and a small authored horizontal-split menu. A source-polymorphic action policy, not a new zero engine, is the consuming generalization.

## Attempting the RH consequence

1. **Finite source census.** The eta atlas supplies finite certified invariant winding-one regions. Reflection then fixes the sole zero in each such region on Re(s)=1/2. This is a valid finite-location proof. Extending the same type of cover to every zero at all heights is a new source theorem; finite scans and repeated rational subdivisions alone do not supply it.
2. **Reflected-current motion.** RH/FiniteZeroCurrent.lean supplies the Foster finite-divisor current and actual-source tail. The new RH/ZeroNavigationReflection.lean proves that, at a rightmost off-seam simple zero in the finite divisor with its same-height reflected partner admitted, every other finite zero contributes nonnegative normal current. Thus the actual flowed-ξ simple-zero velocity obeys the inward normal upper bound with its explicit Foster exterior tail. This removes a separately assumed finite-surplus sign. It still assumes finite-divisor maximality, reflected-partner membership/multiplicity, local factorization and disc inclusion; it does not compare a finite rightmost root with the entire zero population or continue an extremal simple branch through collisions.
3. **Test the tempting conclusion.** The same RH file gives a strip-contained exact quadratic control: F_τ(s)=1/4−(2s−1)²−8τ. It has functional and conjugation symmetry, a positive centre value, off-line zeros at s=1/4 and 3/4 at τ=0, satisfies the exact heat clock/spatial-difference identity, and meets at a double seam zero at τ=1/32. Therefore reflection, local heat flow, inward simple-zero motion and source-value phase navigation cannot alone imply absence of off-line zeros at the initial time. The ξ arithmetic/archimedean constitution must enter.
4. **Cofinal source contours.** RH/CofinalXiContoursConstructed.lean now constructs the CofinalXiContours field of RH/GlobalWeilFinishLine unconditionally. In each interval (n+1,n+2), the actual ξ zero factorization has finitely many bad radii inside a larger disc; choose a radius outside that finite image. The chosen radii are nested, cofinal and their circles are zero-free. This is an exact existence construction by classical choice, not yet a rational-radius executable selector.
5. **Global positive receiver.** RH/GlobalWeilFinishLine.lean states the source-specific finish line: a cofinal explicit-formula atlas, positivity on every Weil square, and an off-line separator at the *same* receiver. Step 4 now supplies its contour family, but neither the prime/archimedean limit identity nor positivity and complete separation. RH/FosterTanks.inductance_pos_real_iff proves that a tank's positive inductance is equivalent to its zero lying on the seam. Declaring all tanks passive would assume the desired conclusion. A genuine proof must construct a positive material/Gram realization from the actual ξ or prime/theta generator **without importing the zero locations**, then prove it is faithful to the complete divisor receiver. That is the remaining high-value source theorem, not an external numeric estimate.

The attempted contradiction fails precisely at step 3: an off-line pair may move inward and collide at a multiple zero while all local Holonic conservation and reflection laws continue to hold. The cofinal contour field is now constructed, but the Weil positivity route remains open at step 5. This is an identified mathematical obstruction, not a reason to discard the navigation machinery.

## The relativity analogy as an exact local statement

On a regular zero-free patch, write f′/f=a+ib. The log-amplitude and lifted phase gradients are (a,−b) and (b,a): orthogonal with the same squared norm. The residual direction v=−f/f′ has d(log f)/dτ=−1 along an admitted trajectory. This is a normalized *source-value* clock: amplitude falls while phase is carried. It gives no uniform bound on |v| in the source coordinate. Computation/ZeroNavigation.word_speed_cone proves a separate finite-travel law when each generator has a declared metric step bound; Computation/IntersectionNavigation.affine_newton_reaches_arbitrary_target proves holomorphic Newton action alone can reach an arbitrarily placed target in one step. Physical c requires a Lorentzian material/clock law and a source-preserving map. The universal analytic pattern is phase/divisor covariance; finite spatial propagation is conditional.

## Next discriminating proof work

- Bind the generic winding route to a source-polymorphic argument-principle and exact region-splitting interface, preserving poles, multiplicities, boundary refusals and nested-limit data. Prove a full all-roots finite-compact return under explicit effective source hypotheses.
- In eta_atlas, compare source-derived candidate cuts against its fixed menu using the same full exact source and verifier, charging generator inference, tail work, new edge work and bit growth. Retain the Pareto frontier rather than a universal scalar.
- For ξ, close reflected-partner membership in the selected Foster divisor; the cofinal zero-free contour field is now checked. Then construct the prime/archimedean limit passage and test whether a source-derived positive Weil/Dirac realization can produce the missing complete receiver positivity without presupposing RH. A failed positive form or an off-line-quartet witness is a source-level falsifier.
- For multi-generator intersections, bind the exact Jacobian preimage fibre to analytic local rank and actual nonlinear remainder, then to stratified/branched cells where rank fails. The pullback multiplicity theorem is the finite local control.

**Verification:** the three new computation leaves and both new RH leaves have focused Lean compiles and axiom audits with no sorryAx. Framework.Computation imports the computation owners and compiles; Framework.Dynamics imports the RH leaves and compiles. The exact policy receipt verifier passes. The cofinal contour field is an existence construction; an executable rational selector and the remaining global Weil fields are open.

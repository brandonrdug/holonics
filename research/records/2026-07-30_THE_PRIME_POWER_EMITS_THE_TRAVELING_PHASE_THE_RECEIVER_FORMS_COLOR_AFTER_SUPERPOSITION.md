# THE PRIME POWER EMITS THE TRAVELING PHASE; THE RECEIVER FORMS COLOR AFTER SUPERPOSITION

**Date:** 2026-07-30  
**Status:** EXACT TRAVELING CURRENT BUILT · CONTROLLED ZETA RECEIVER ATLAS BUILT ·
PER-OBJECT COLOR REJECTED · EXACT RECEIVER TRANSDUCTION BUILT  
**Production owners composed:** `ArithmeticFiberLaw`, `ArithmeticDimensionalMount`,
`ExactDimensionalSource`, `DimensionalReceiverAtlas`, `ExactDimensionalWaveLaw`,
`ExactReceiverPhasePopulation`, and `ExactReceiverPrimaryDoctrine`  
**Instrument:** `crates/holonic-engine/examples/arithmetic_dimensional_receiver.rs`

## The correction

Playback through retained historical fronts is not physical motion. A changing display clock cannot
cause a current, and a sequence of static receiver sections does not become a wave merely because
the sections are shown in order. The source must own a changing field whose exact successor exists
before a monitor receives it.

Assigned RGB values are not that field either. The rejected instrument:

1. mapped each exact complex current independently into one hue;
2. ignored its modal coexistence with other currents at the same receiver contact;
3. added a screen-space brightness ramp which had no source law;
4. quantized every contribution to `u8` before composition; and
5. saturating-added those terminal bytes, destroying exact cancellation and interference.

The corrected construction has two independent boundaries:

```text
caused arithmetic incidence
    -> exact modal wave current and physical successor
    -> receiver restriction of the contemporary current
    -> exact co-present phase population at each terminal contact
    -> declared receiver-primary response
    -> one final monitor quantization
```

The first three arrows are source/current physics. The last two are observation. Neither can stand
in for the other.

## The exact traveling current

For every admitted mode \(m\) and oriented carrier end \(e\), the standing retains a traveling
complex current

\[
A_{e,m}(k)\in\mathbb Q(i).
\]

Carrier delay \(d_e\) and admittance \(Y_e>0\) are declared constitutive data. They are not inferred
from projected chord length, screen proximity, color, or frame rate. At a reached junction, incoming
currents \(a_i\) scatter by

\[
v = \frac{2\sum_i Y_i a_i}{\sum_iY_i},
\qquad
b_i=v-a_i.
\]

In the absence of a caused local departure,

\[
\sum_iY_i\lVert b_i\rVert^2
=
\sum_iY_i\lVert a_i\rVert^2
\]

exactly. A caused departure records its exact source work. Carrier traversal applies the exact
rational unit-conic transport derived from the mode's endpoint phase coordinates, and reverse
traversal applies its inverse. A bounded-delay sparse ring schedules only occupied arrivals.
One successor visits the reached junctions and their local ports; it does not scan a receiver
matrix or every compiled carrier looking for work.

The three-port grading fixture returns the exact coefficients

\[
(-1/3,\ 2/3,\ 2/3)
\]

and exact energy equality. A forward quarter-turn followed by reverse traversal restores the local
phase basis exactly.

## The prime-power source

The arithmetic ecology does not receive a fabricated oscillator. When the caused occurrence is a
prime power \(n=p^r\), it emits one exact current into its admitted \(p\)-mode at the germ for \(n\).
Its initial phase is the germ's already-caused rational unit-conic coordinate for the finite-place
receiver. Composite occurrences with more than one prime factor emit no such primitive-power
departure.

This selects the same *support species* which appears in the logarithmic derivative of an Euler
product: prime powers are the recurrent primitive/repetition events. The present current does not
insert the classical \(\log p\) weight, an analytic continuation, a zero location, or an RH
conclusion. It gives the machine an exact causal population whose phase transport and receiver
restrictions can be compared without converting primes to floating logarithms.

The interactive instrument conditions its current chronologically through the selected caused
horizon. The controlled atlas is a distinct experiment: every prime-power departure through 512 is
co-present, and the field then advances eight exact physical successors. Both use the same
production wave law.

## Why the same wave appears fast on the left and slow on the right

Every controlled phase view receives the same wave tick, total energy, carrier delay, and source
cadence. Only the receiver chart changes. The exact receiver receipt reports

\[
\nu_R^2(e)=\frac{\lVert\pi_R(e_1)-\pi_R(e_0)\rVert^2}{d_e^2},
\]

an apparent squared rate in that receiver, not an intrinsic velocity assigned to the source.
Energy-weighted mode means from the current atlas include:

| receiver phase pair | representative exact-receipt decimal faces |
|---|---|
| left \((2,3)\) | \(p_2=0.05292955,\ p_3=0.09350075,\ p_5=0.09969006\) |
| middle \((7,11)\) | \(p_2=0.00526581,\ p_7=0.00968228,\ p_{11}=0.03020000\) |
| right \((17,19)\) | \(p_2=0.00019829,\ p_{11}=0.00057946,\ p_{19}=0.00453446\) |

Some right-view mode means are exactly zero because their live carriers collapse along this
particular projected direction; their source current has not stopped. This formalizes Brandon's
observation: the left receiver compresses more event displacement into a small visible body, while
the expanded right receiver exposes more of the coupling ecology at a smaller apparent rate.

An exact dilation by \(25/16\) multiplies every middle-view squared projected rate by

\[
\left(\frac{16}{25}\right)^2=\frac{256}{625}.
\]

An exact Cayley turn does *not* multiply all modes by one scale. Each mode changes differently
because the participating receiver itself has turned through the source coordinates. This is not a
detached camera switch.

## Color is a receiver equivalence, not a source label

The substrate delivered to one terminal contact is now

\[
\mathcal A_R(x)=\{A_m(x)\}_{m\in M}.
\]

Currents of one mode are added before response:

\[
A_m(x)=\sum_{\gamma\ \mathrm{in\ mode}\ m}A_{\gamma,m}(x).
\]

They can therefore reinforce or cancel. Distinct modes remain separate through this addition and
cannot cancel merely because a display projects them to one address. The current receiver doctrine
then forms three exact nonnegative phase responses

\[
P_0=\sum_m(\Re A_m)^2,\qquad
P_1=\sum_m(\Im A_m)^2,\qquad
P_2=\sum_m(\Re A_m+\Im A_m)^2.
\]

Neutral analytical-support multiplicity may contribute equally to the three response coordinates.
For declared positive aperture \(\kappa\), let

\[
D=\kappa+P_0+P_1+P_2.
\]

The outer membrane returns the premultiplied four-coordinate response

\[
(R,G,B,\alpha)
=
\left(
\frac{P_0}{D},
\frac{P_1}{D},
\frac{P_2}{D},
\frac{P_0+P_1+P_2}{D}
\right)
\]

and exact transmittance

\[
\tau=\frac{\kappa}{D}.
\]

Consequently,

\[
R+G+B=\alpha,\qquad \alpha+\tau=1.
\]

This is a declared analytical monitor gauge. It does not assert that real optical phase is hue,
that a prime is a wavelength, or that alpha is a fourth emitted wavelength. Alpha is the receiving
membrane's interaction/coverage coordinate. X11 receives the premultiplied RGB consequence over a
black outer carrier; the exact four-coordinate relation exists before the final `u8` quotient.

The membrane retains exact wave-section identities at each contact and forms each distinct
co-present population once. Repeated raster contacts reuse that exact response by lineage identity;
they do not compare or cache colored pixels. This reduced a full 512-source atlas from the
unfactorized exact-color pass to about 36 seconds while preserving the current and receiver
receipts.

The corrected left, middle, and right images contain 2,853, 576, and 101 distinct terminal color
words respectively. No constituent type owns a palette. Chroma occurs only where the exact modal
population and declared receiver transduction produce it.

## The finite RGBA carrier is not the color field

One ordinary 8-bit RGBA code word has

\[
(2^8)^4=2^{32}
\]

possible tuples. The expression \(4^{2^8}=2^{512}\) counts a different object: functions from 256
distinguishable inputs to four labels. For \(N\) unrelated raster addresses, the naive code-word
product would be \(2^{32N}\).

The important correction is not the exponent bookkeeping but Brandon's coupling observation.
Physical or holonic neighbors are not independent choices. Their admissible values are global or
local sections restricted by transport, recurrence, material response, and receiver interaction.
Even conventional premultiplied 8-bit RGBA already demonstrates this: if each primary must satisfy
\(0\le R,G,B\le\alpha\), the number of allowed integer tuples is

\[
\sum_{\alpha=0}^{255}(\alpha+1)^3
=
\left(\frac{256\cdot257}{2}\right)^2
=
1,082,146,816,
\]

not \(2^{32}\). The exact field in this engine is not confined to that finite set at all. Only the
terminal monitor quotient is.

A color gradient is therefore not a list of interpolated endpoint RGB values. It is the receiver
face of a spatially changing transported field. In a physical optical ecology, field amplitudes of
coherent paths superpose before intensity, incoherent spectral populations add after the relevant
quadratic response, and receptor sensitivity forms the color equivalence. Two different delivered
spectra can consequently have one color face for one receiver and cleave for another.

## Points as tori and the physics beyond the one-skeleton

Feng, Gkioulekas, and Crane's *Points as Tori* does not supply temporal particle dynamics. Its
important construction is more elementary: a point is not left as an isolated address. Local first
and second fundamental-form testimony produces an analytical torus germ which answers continuous
signed-distance queries, and local answers are assembled into a receiver query of a larger field.

The laboratory already owns the exact counterparts needed for the next composition:

- `ExactTorus` retains a rational quartic, exact gradient, exact Hessian, and every certified ray
  depth;
- `CausalFieldAtlasLaw` retains local quadric/torus germs, phase fibers, overlaps, alternatives, and
  obstruction lineage;
- the arithmetic dimensional source retains products of exact finite-place unit conics, which are
  already toroidal phase factors in higher dimension; and
- `ExactDimensionalWaveLaw` now retains the changing modal current and exact junction return.

The current screen still draws source carriers as a faint analytical support skeleton. That
one-skeleton is not the full physics. The stronger construction is:

1. let every local analytical germ \(F_g=0\), its gradient, Hessian, and phase fiber define an open
   conducting section;
2. let overlap restrictions, coorientation, constitutive admittance, and returned obstruction
   determine which local currents glue, reflect, refract, split, or remain open;
3. propagate the exact current through those local sections and overlaps rather than along
   screen-visible chords; and
4. let a receiver ray or higher-dimensional slice restrict the co-present field into a resonance
   image.

Schematically, a receiver contact asks for

\[
\mathcal L_R(u,m,k)
=
\sum_{\gamma\rightsquigarrow R(u)}
K_{\gamma,R,m}\,
A_{\gamma,m}(k-\tau_\gamma),
\]

but production need not enumerate all paths. Sparse causal fronts carry the same composition
through local overlaps. The receiver then applies its own response to \(\mathcal L_R\). Geometry
provides the inductive pathways; current/action makes those pathways physically consequential; the
image is the receiver-relative resonance face.

For a real light claim, the local doctrine must additionally retain wavelength/frequency,
polarization, material dispersion, absorption, and the receiver's calibrated spectral response.
For the arithmetic ecology, prime modes remain exact abstract phase species. Their emergent monitor
colors are useful analytical testimony, not simulated human-visible prime light.

## Artifacts and verification

The controlled artifacts are under:

`output/arithmetic-dimensional-receiver/parameter-atlas/`

They include eleven parameter views, exact per-section TSVs, eight exact temporal successors, two
contact sheets, exact difference images, and a GIF outer carrier. All temporal PPM hashes differ.
Across ticks 9 through 16, exact total energy remains 63. At tick 13 the traveling population
changes from 2,912 to 5,825 sections through exact junction scattering while energy remains
unchanged.

Verification:

```text
cargo test -p holonic-engine dimensional_wave --lib
cargo test -p holonic-engine arithmetic_dimensional --lib
cargo check -p holonic-engine --example arithmetic_dimensional_receiver
```

The receiver-response tests prove:

- equal and opposite currents in one mode cancel before color;
- equal and opposite currents in distinct modes do not falsely cancel;
- the three premultiplied primary coordinates sum exactly to alpha; and
- alpha plus exact transmittance is one.

## The concrete open physical composition

The missing capability is no longer “motion” or “emergent color.” Both now have production-owned
elementary forms. What is not yet composed is an exact analytical-germ wave ecology in which the
current occupies torus/quadric open sections, crosses their caused overlaps, and returns a
ray-integrated resonance field instead of a line-supported terminal trace.

That composition must preserve three open distinctions rather than conceal them:

1. endpoint phase transport does not by itself determine an interior winding at an antipodal
   section; the overlap or constitutive doctrine must carry that hand;
2. a general physical emitter must retain coherence lineage so the receiver knows which amplitudes
   may superpose before response and which populations remain distinct; and
3. abstract arithmetic modes acquire physical color only if a later world supplies a lawful
   spectral/material/observer calibration.

These are local conditioning capabilities of the intermediate body. They do not require a
universal law before the analytic field construction can proceed.

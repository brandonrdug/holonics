# THE CAUSAL BODY OWNS THE BOUNDARY; THE PROJECTION CANNOT INVENT A FACE

**2026-07-30 — CURRENT EVENT-INDEXED HYPERGEOMETRY SOURCE AUTHORITY /
OPEN-BOUNDARY AND HOLONOMY OWNER / RECEIVER CROSSING CORRECTION /
RELAMPAGO SOURCE-BODY RECONSTRUCTION**

This record supersedes the RELAMPAGO terminal image and video as testimony
about source topology. It does not supersede the coupled-informant learning,
atmospheric inverse, card-current, or typed rest results in
`2026-07-30_THE_STORM_CROSSES_THE_TYPED_BODY_EACH_CARD_OWNER_RETURNS_TO_ITS_OWN_CONTEXT.md`.

## Correction

The former four-panel RELAMPAGO movie accumulated terminal line strokes in
fixed longitude/latitude, longitude/height, clock/height, and authored
isometric quotients. A visually dense region in one panel looked like a
hypercube or tower. Other panels collapsed the same region nearly to a point.
Successive frames retained all earlier strokes.

That construction could demonstrate only this:

```text
one finite relation population
    -> several chosen receiver quotients
    -> cumulative terminal marks
```

It could not establish:

- a source face, volume, torus, knot, or polytope;
- event-indexed deformation;
- relativistic propagation;
- physical light, charge, or field current;
- source crossing number;
- probability or loss;
- higher-dimensional attachment; or
- a lawful reason for the selected panel family.

The defect was not that a projection existed. A receiver must project. The
defect was that the projection had become the only body and its accumulated
marks were being read as source topology.

The raster, frame concatenation, and video path are now removed from
`src/soma/life/examples/eros_relampago_atmospheric_current.rs`. The corrected
run emits no PNG or MP4.

## Production owners composed

The construction does not replace the existing exact owners:

- `GradedCausalComplex` owns arbitrary-grade oriented incidence and
  `boundary(boundary) = 0`;
- `CausalWorld<L>` owns atomic typed succession;
- `ExactLinearMap`, `ExactCellularSheaf`, and
  `ExactSheafDiffusionLaw` own exact finite-dimensional transport and
  deterministic current;
- `JointDecoratedPathOperator` owns exact source paths, receiver-local
  projected crossings, interval refinements, local turns, and closed returns;
- `AtmosphericInverseResolution` owns the conditional ABI/IGRA vertical
  fibers; and
- `CoupledInformantPrediction` owns every exact eighteen-coordinate phase
  branch generated before its native GLM return.

The new owner is:

```text
crates/holonic-engine/src/causal_body.rs

ExactCausalBodyLaw
    -> CausalBodyStanding
    -> CausalBodyRadiation
```

Its new consequence is an exact changing source body whose active incidence,
open boundaries, local receiver sections, connection holonomy, exact field
current, departure, and lineage all exist before any terminal projection.

## The changing causal body

`CausalBodyStanding` retains:

- complete arbitrary-grade source incidence;
- a boundary-closed active cell population;
- a grade index;
- one maintained complete coface index;
- caused open boundary fibers;
- reversible connection transports;
- canonical fundamental-loop holonomy generators;
- caused receiver anchors and their changing local source sections;
- exact cellular-sheaf fields and their current;
- complete event identities and chronology; and
- the next exact opening identity.

One event may mint many cells through event-local handles. A cell may refer
only to a standing cell or to an earlier cell in the same atomic event. It
cannot refer forward to an uncaused body.

Every founded cell retains the event that caused it. Every boundary is
validated before commit. An event which fails leaves the predecessor
unchanged.

## Departure is a causal upper propagation

Removing a lower cell while retaining one of its cofaces would leave an
invalid active body. Departure therefore follows the maintained coface index:

```text
requested departing support
    -> reachable active cofaces
    -> complete upward departure
    -> boundary-closed active remainder
```

Complete historical incidence remains in lineage. Active standing changes;
history is not erased.

A receiver anchored in the departing region does not silently recenter. Its
next return is an empty local section with an explicit anchor obstruction.
An unaffected receiver retains its source-local closure.

## A cycle is a boundary question, not a face

The active grade-one graph is factored into one canonical spanning forest and
its chord returns. Each chord produces one exact closed oriented chain:

```text
chord + unique tree return
```

That chain enters as an `OPEN` boundary fiber. It is not filled by:

- apparent visual enclosure;
- edge density;
- a projected crossing;
- a parallelogram-like quotient;
- recurrence of a screen-space shape; or
- a renderer triangulation.

A later caused grade-two cell fills the opening only when its exact oriented
boundary equals that open chain, up to global orientation. The same mechanism
accepts explicit closed chains at arbitrary grade. Automatic basis discovery
is presently implemented for grade one; higher-grade openings require a
caused closed-chain deed until an exact arbitrary-grade homology-basis owner
is installed.

The cycle finder no longer breadth-scans the whole tree once per chord. One
rooted event forest is formed, and each chord walks only its own return path.
The expanded chain is retained because it is inspectable exact testimony.

## Holonomy is transported comparison, not drawn curvature

A grade-one carrier may receive a forward and reverse exact linear map only
when:

- its source and target are the actual oriented edge boundary;
- both maps are nonempty square maps of equal extent; and
- both compositions equal the identity exactly.

The same canonical forest then carries path-ordered return around every
connection chord. The resulting exact map is the loop holonomy generator.
It may be nonidentity even when every pairwise receiver face looks equal.

RELAMPAGO did not declare such a comparison transport. Its corrected receipt
therefore reports zero holonomy generators instead of manufacturing maps
from colors, screen slopes, or phase-vector distance.

## Field current can change with topology

An exact cellular-sheaf field may be installed or re-formed over any
boundary-closed active source section. Common cochain cells carry their
content automatically. Newly entering cells require exact supplied content,
and departed cells emit their exact prior content in the reform receipt.

The existing sheaf diffusion owner then conducts

```text
(M + tau Delta_k) phi_after = content_before + source
```

with its exact inverse, balance, compatibility, harmonic-dimension, and
energy receipts. The causal body does not redefine that law.

This establishes the machinery needed for changing-topology current. It does
not declare the RELAMPAGO eighteen-coordinate learned phase to be electric
charge, optical wavelength, or another physical field.

## Probability and loss remain receiver-relative alternative measures

An application may attach exact filled and unfilled configuration populations
to an opening for one receiver:

```text
P_receiver(fill | declared configuration body)
    = filled population / total population
```

The engine refuses an empty measure. It does not create a probability from
edge density or a display area. Different receivers may lawfully retain
different measures over the same open boundary because their configuration
bodies differ.

No such measure was supplied by the current storm application, so all
reported openings remain unmeasured rather than receiving an invented scalar
loss.

## Receiver crossings cannot retile the source

`CausalBodyStanding::receive_decorated_projection` binds every exact source
segment of a `JointDecoratedPathOperator` to one active causal carrier and its
actual oriented endpoints.

The receiver receipt retains:

- exact source parameters of apparent crossings;
- the two participating source carriers;
- whether those carriers were already source-adjacent;
- receiver-specific primitive closed returns;
- the source f-vector; and
- the extant open source boundaries.

It always reports:

```text
projection_cells = 0
```

The method borrows immutable standing. A projected crossing cannot mutate
incidence.

The exact control uses one nonplanar closed source loop. Its orthographic
receiver sees one crossing between nonadjacent carriers. The result remains:

```text
source f-vector       {0: 4, 1: 4}
source cells          8
open source boundary  1
projection cells      0
```

This is the operative knot-theoretic distinction. Crossing number and
over/under order are receiver testimony. The source path, its incidence, and
its lawful attachment history remain separate.

## RELAMPAGO enters as a typed application membrane

The generic causal body is modality independent. The storm application adds
one typed joined standing rather than hiding atmospheric data in cell names.

Every grade-zero causal cell is bound to its complete
`AtmosphericVerticalFiber`, including:

- source testimony and source identity;
- ABI scan and receiver;
- IGRA profile and layer;
- exact point or layer support;
- brightness temperature;
- scan departure and temporality;
- pressure and layer-temperature intervals; and
- the complete hydrostatic chord receipt.

Every grade-one causal carrier is bound to its complete relation section:

- returned testimony pair;
- optical/spectral origin;
- base optical state;
- both ABI scans;
- both vertical-fiber ordinals;
- all eighteen exact phase coordinates;
- branch-local state; and
- generated aggregate state.

The joined body and the generic body each rest and remount exactly.

## Real five-episode result

The corrected optimized command consumed the same durable GLM, ABI, and IGRA
source membrane:

```text
src/soma/target/release/examples/eros_relampago_atmospheric_current \
  data/relampago-lightning/manifests/glm_relampago_full_window.keys \
  data/relampago-lightning/raw/glm \
  data/relampago-lightning/raw/abi \
  data/relampago-lightning/raw/igra/ARM00087344-data.txt.zip \
  output/relampago-lightning/causal-body-research
```

The existing machine again received:

| receipt | exact population |
|---|---:|
| conditioning GLM occurrences | 12,523 |
| held-out GLM occurrences | 21,777 |
| held-out episodes | 5 |
| coupled generated relations | 45,731 |
| retained typed phase branches | 34,849 |
| conditional vertical fibers | 16,817 |
| final positive front | 311 |
| final negative front | 28 |

The new causal body returned:

| episode | vertices `V` | relation carriers `E` | components `C` | open basis `E-V+C` |
|---:|---:|---:|---:|---:|
| 1 | 2,450 | 5,012 | 7 | 2,569 |
| 2 | 3,022 | 6,517 | 9 | 3,504 |
| 3 | 3,799 | 7,838 | 7 | 4,046 |
| 4 | 4,357 | 9,103 | 10 | 4,756 |
| 5 | 3,189 | 6,379 | 6 | 3,196 |
| **total** | **16,817** | **34,849** | **39** | **18,071** |

Thus the actual admitted source result is:

```text
active f-vector            {0: 16817, 1: 34849}
typed vertical sections    16817
typed phase sections       34849 x 18 exact coordinates
open one-boundaries        18071
filled boundaries          0
grade-two cells            0
connection holonomy        0
```

The apparent hypercube in the former frame is therefore not dismissed as
meaningless. It was a receiver-visible quotient of a region participating in
a very large exact cycle space. The correction is more useful: the source
currently contains thousands of precise boundary questions and no caused
answers which fill them.

The complete optimized run used 24 host workers and the existing RTX 4080
SUPER application paths. It completed in approximately 16.5 seconds after
build. The new causal-body law remains the exact CPU reference; this timing
is apparatus testimony, not a universal complexity claim.

The rooted-forest correction preserved byte-identical final artifacts:

```text
joined typed causal body
d6e3c9a3dd75448985be47d0dbca4a06e6144752c72a8dae9e5ddc2ff1a5e8cd

generic causal body
a6d9c190d8e40f1d41ea207026f2c059faff05df6a696e964220256ff6530a6f
```

No PNG or MP4 exists under the corrected output.

## Research testimony

The principal files are:

```text
output/relampago-lightning/causal-body-research/
    relampago-causal-body.ron
    causal-body-standing.ron
    causal-body-transitions.ron
    causal-body-events.tsv
    causal-body-cells.tsv
    causal-body-application-map.tsv
    causal-body-relation-sections.tsv
    causal-body-openings.tsv
    causal-body-audit.tsv
```

The joined RON is approximately 187 MiB. The generic body is approximately
133 MiB. Those sizes mostly expose expanded exact fundamental-cycle chains;
they are not a claim that an optimal resting form must remain expanded.

## Verification

The following gates pass after the construction:

- five focused `causal_body` tests:
  - an open cycle closes only after a caused face;
  - lower departure propagates through all active cofaces and changes the
    receiver return;
  - path-ordered nonidentity holonomy survives an exact loop;
  - exact sheaf current reforms and conducts on active incidence; and
  - a projected crossing adds no source cell;
- `cargo test -p holonic-engine --lib` — 217 passed, 2 device tests ignored;
- the life example compiles cleanly;
- the complete real optimized RELAMPAGO run;
- exact generic rest/remount;
- exact joined typed rest/remount; and
- a second complete run after the rooted-forest correction with the same two
  artifact hashes and the same populations.

## Concrete next research boundary

The next question is not how to make the 18,071 loops look volumetric.

The machine must now condition which phase-bearing closed chains recur as the
same transported boundary across later atmospheric occurrences and which
returned informants distinguish:

- a genuinely fillable higher cell;
- a connection with nontrivial return holonomy;
- homologous but distinct paths;
- a persistent periodic identification which can lawfully found a torus
  species;
- a projection-only crossing;
- a physical obstruction; or
- a departed local alternative.

That conditioning can use the retained eighteen-coordinate phase sections,
chronology, GLM return grades, spectral scans, vertical supports, and future
independent physical receivers. A higher cell must be the returned
consequence of that conditioned body, not a clique completion or renderer
decision.

Three implementation migrations remain explicit:

1. automatic exact basis discovery above grade one;
2. a structurally shared resting representation of forest/chord openings
   which materializes expanded chains only for the receiving aperture; and
3. card realization of the new exact causal-body operations after host/card
   parity is specified.

These are not missing evidence that the machine can grow topology, conduct
exact current, condition phase, or observe receiver-relative crossings.
They are the next owners needed to let real returned phase recurrence cause
the higher-dimensional storm ecology.

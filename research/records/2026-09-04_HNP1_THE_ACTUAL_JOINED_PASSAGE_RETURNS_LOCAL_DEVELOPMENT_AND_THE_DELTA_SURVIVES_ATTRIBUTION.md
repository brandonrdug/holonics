# HNP1: the actual joined passage returns local development and the delta survives attribution

**Date:** 2026-09-04. **Predecessor:** `7795ce20`.
**Scope:** one concrete observed-passage constitutive chart on actual SKE native material;
not a universal update law, qualitative model result, persistence release or locality compiler.
**Receipt:** [HNP1 observed-passage return](2026-09-04_HNP1_receipts/observed-passage.json).

## The corrected joining

[definition] The first HNP1 attempt used the other input of Add as a desired reference and was
[withdrawn](2026-09-04_HNP1_ADDITIVE_EQUALIZATION_CHANGED_CONDUCT_BUT_DID_NOT_FOUND_A_LEARNING_COMPARISON.md).
The present target is instead the joining operation's actual OUTPUT. If the carried branch is
`t` and the partner current is `p`, the actual after/before effect is `(t + p) - t = p`.
No desired setpoint or scalar objective is inferred from Add, and no exterior verdict is required.

[proved-derived; formal-checked] `ConstitutiveSectionReturn.additive_joined_passage_effect` and
`zero_partner_separates_passage_effect_from_equalization` provide the exact distinction. A zero
partner produces no actual change; the former partner-minus-transported rule spuriously returned
a change for a nonzero carried branch. HNP0's rectangular/bipartite, source-covector, factorized
return and complete-successor laws carry their previously established scopes.

[definition] The effect returns through the actual intervening unary reaction word and then
deposits the HNP0 factorized local change on the carried branch's native cross-section. With
transport `f(Mx)` and joining output `z`, its ideal local form is
`eta * (D f)^*(z - f(Mx))` paired with the presented source covector. This is a declared local
linearized response law; it does not assert that every physiological response, nonlinear inverse
or machine-learning architecture has this form. Normalization contributes its existing native
adjoint, not a desired output for its input.

## Native ownership and integration

[established-bounded; implemented-exact] `operative_passage_return.rs` extends the existing
`NativeFullOperatorSession`. `found_with_passage_return` derives paths from actual native
Contract → supported unary word → Add incidences. The descriptor retains the partner as lineage
and the joining OUTPUT as `arrived`. No application supplies a target population, numeric current,
source-label router or Boolean joining. Both directions are admitted when the two distinct
arrivals have eligible transport paths; other native configurations retain explicit open scope.

[established-bounded; source-inspected] The session retains each needed source and reaction
carrier until the actual joining is reached. Current computation and local reverse transport stay
resident. Pending factors are not inserted into held morphology until the complete cycle closes,
so every return in that cycle uses the morphology which produced its source carriers. Subsequent
cycles use the retained factors through the existing overlaid-contraction owner. The original
prefix chart is unchanged; it is not simultaneously installed as another return law in this chart.

[definition] The dyadic readout is still declared, not presented as an intrinsic learning
constant. On a nonlinear word, the already-declared factor is moved before the linear adjoint to
avoid an unnecessarily large unscaled intermediate; the deposit carries the remaining factor.
The sum of the two reported shifts is the requested shift. `causalAdjoint_carries_declared_scale`
proves the ideal linear identity; the scaled-current enclosure and subsequent midpoint/factor
projections remain explicit numerical boundaries, not exact-real or universal physical claims.

[established-bounded; implemented-exact] Attribution uses `observe_passage_cycle`, an explicitly
fixed-morphology receiver through the same cycle owner. `withdraw_passage_changes` moves out the
actual factors; `restore_passage_changes` consumes those same factors back. Private ownership
capabilities reject cross-session restoration, and restoration also requires the unmodified
native base. A refusal returns ownership of the withdrawn factors. No ecology is cloned or
reconstructed for these controls. This observer is not the default inference lifecycle.

## Actual native-material return

[established-bounded; measured] The existing corrected SKE4 rest at `target/ske4/rest.bin` was
mounted once. Three already-admitted occurrences from its retained family were used; none extends
the preceding occurrence as a prefix. Their actual address vectors are in the receipt. The same
session returned 127 native contacts per cycle, with factors advancing:

```text
cycle 0:     0 -> 1778
cycle 1:  1778 -> 3556
cycle 2:  3556 -> 5334
```

Each return's target is the actual joining output and every effective readout shift remains 16.
These ranks are factor-carrier extents, not claims of minimal mathematical rank or intelligence
capacity. The newly retained factorized transports affect later operator conduct; they are not
an activation/event replay table or a fixture-local learner.

[established-bounded; measured] An identical later input produced a changed complete terminal
vector. At coordinate zero, the point interval changed from `[-85539717935430,-85539717935430]`
to `[98272805344020,98272805344020]` in the recorded chart. With the actual local delta removed,
the entire vector matched the baseline exactly. Restoring those factors reproduced the entire
cultivated vector exactly. Final generation was 8,925, including the declared observations.

[established-bounded; measured] Each developmental cycle returned 16 section readouts, the
existing tiled terminal receiver, with no additional section egress from the local returns.
Rest loading took 21.282 seconds and complete resident mounting 120.124 seconds from start.
No local-work or residency-reduction claim follows: the dense body and complete forward graph
remain in use and are HNP2's subject.

## Controls and admission boundary

[established-bounded; measured] The three focused `operative_passage_return::tests` passed:
actual graph-derived roles, retained unary reaction order, and the CUDA zero-partner discriminator.
The latter forms the joining output with the native Add kernel; the correct output-based return
is absent while the archived partner-as-target calculation is nonzero on the same data.
The six retained native-return controls previously passed, including a nonzero unaffected
coordinate, malformed-chart refusal and signed division-enclosure retention.

[established-bounded; source-inspected] Material filenames are not arguments of the native
occurrence or return. Exact input delivery is separate from causal occurrence creation; the
codeword reader's exhaustive u16/fragmented-delivery control passed. This does not claim the
not-yet-built public streaming/session interface of HNP3. The SKE family's outside-input test
refused before execution. The inherited fidelity family is not silently enlarged by development:
the changed native body is not advertised as preserving the old SKE receiver equalities.

[definition] HNP1 closes at this concrete observed-passage family, the actual model-material
execution and the matched/unaffected/withdrawal/restoration controls. General native port kinds,
source/candidate correspondence applications, stronger locality, persistent models, useful
multi-cycle output and standard executable exports retain the later phases' obligations. The
present CLI convenience commands retain their existing prefix scope until the public-session work.

## Source owners

[established-bounded; process-audit] The HNA and workbench libraries, binaries and examples
type-check together with the changed cycle return. The existing unused general five-section
helper warnings remain; this scope uses the identity-admittance and local-reaction specialization.
No blanket metadata suite, untouched paper build or unrelated mathematical campaign was run.

- `crates/holonic-engine/src/holonic_intelligence/operative_passage_return.rs`: derived roles,
  local return, staged publication and move-owned attribution.
- `full_operation.rs`: source liveness and the same continuing cycle; `operative_return.rs` and
  `operative_backward.rs`: existing numerical difference, scaling, factors and unary adjoints.
- `crates/holonics-hna/examples/passage_return_hnp1.rs`: admission and declared observations only.
- `HolonicOrientedSiteTransport.lean`, `HolonicAdjointNormalization.lean`: the stated formal scope.

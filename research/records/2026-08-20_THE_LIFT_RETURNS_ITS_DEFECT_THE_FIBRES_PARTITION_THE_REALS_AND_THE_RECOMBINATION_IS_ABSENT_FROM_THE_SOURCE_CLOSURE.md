# The lift returns its defect, the fibres partition the reals, and the recombination is absent from the source closure

**Date:** 2026-08-20
**Kind:** construction return for **Deed P1** of the single-card plan — the cross-chart lift — with
the typed Φ receipts, the exact χ and its faces, the chain law as algebra and on real material, the
exact BF16 reconstruction fibres censused over a whole real map, the bounded recombination
demonstration with its two-cause ablation, and the declared apertures.
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for
`cross_chart.rs`; **Deed P1 PASSES on this tree**. Next: **P2 (receiver-exact condensation)**.
`CONSTRUCTION_STATE.md` does not move (only P5 moves it).
**Authority:** the single-card plan §7, §8 "Deed P1", §11; `CLAUDE.md` §0n (inverse, reconstruction
and adjoint are different operations; the metric is load-bearing) and §0o;
[P0](2026-08-20_THE_NATIVE_BASELINE_CONDUCTS_FROM_ITS_OWN_REST_AND_THE_MANIFEST_MEASURES_WHAT_IS_FOUNDED_AND_NAMES_WHAT_IS_OPEN.md);
the committed Station B/C/D artifacts (the declared closure and the intervention family).
**Owner:** `crates/holonic-engine/src/cross_chart.rs` (1,125 lines). The composition attempt is
stated in its head: `exact_linear` already owns rank/kernel/image/cokernel/affine-preimage/
`rebase_receipt`/`metric_adjoint` — nothing was absent there; the two genuinely absent relations
are **the exact rounding preimage** (`RoundingFibre`: the ieee754 owner's own enclosure names
itself an outward over-approximation at binade edges and cannot decide a tie) and **the cross-chart
intertwining defect** (nothing composed `Φ_Y T − S Φ_X` into a typed object with faces and a chain
law).
**Driver:** `crates/holonic-engine/examples/the_lift_returns_its_defect_and_the_fibres_are_exact.rs`.
**Artifacts:** `output/the_lift_returns_its_defect/{receipt.form, chi-faces.tsv, bf16-fibre-census.tsv}`.
**Apparatus:** CPU-exact over `Rat` throughout; no kernel, no card (stated, permitted).

---

## 0. Verdict

**The lift law stands, typed and exact, on real admitted families.** The tree corrected the
contract's sketch first (4 receiver heads per KV family, 2 families — `num_attention_heads = 8`,
`num_key_value_heads = 2`, read from the source, not assumed).

- **Φ receipts, typed:** the two rebases (the input and post-attention gains over the declared
  window) exhibit **both** `ΦΦ⁻¹ = I` and `Φ⁻¹Φ = I` as matrices; the grouped-sharing Φ_Y is a
  **quotient** — no inverse returned, refused by name, rank 16 with the 48-dimensional kernel
  exhibited (first collapsed direction `−head[0].coord[0] + head[1].coord[0]`). The bare-transpose
  perturbation, on the source's own `rotate_half` (a signed permutation with `J² = −I` verified):
  defect 0 under identity metrics and **44265/1024 under the layer's declared gain metric** while
  the metric adjoint's is exactly 0 — the H1 adversary's lesson armed and passing here.
- **χ_γ = Φ_Y T_γ − S_γ Φ_X computed exactly** for four families (the grouped-sharing lift of q
  against k; the o_proj return; their serial composition; an identity-chart control): every χ full
  rank with kernel/image/cokernel faces, the defect exhibited on both sides at a named coordinate,
  and the §8 tautology note attached where the agreement dimension is forced by the shape. The
  **shortest reopening separator** is exhibited twice: the linear quotient's one-coordinate
  functional (with the receipt stating plainly that for a linear quotient the word is always length
  one and the content is which coordinate), and — where successor conduct exists — through
  `receiver_exact_compression` at the grain port (memory order 3; two grain-equal codewords
  separated only after one doubling).
- **The chain law** `χ_{ηγ} = χ_η T_γ + S_η χ_γ` is derived in one line in the receipt (the
  cancellation is the law), verified as an **operator identity** (the residual is the zero 64×64
  matrix, non-vacuously — χ_{ηγ} itself is rank 64) and on three real material vectors, exactly.
- **The BF16 fibres partition the reals, and the law was derived from the mouth as written:** both
  endpoints of a cell belong to the codeword whose significand is even, and adjacent codewords
  always have opposite significand parity **including across a binade re-seat** — verified
  exhaustively over all 65,536 patterns against the mouth. The four exhibits verbatim in the
  receipt: a real tie decided to the even significand and said before the mouth was asked; the
  binade low edge with half-widths exactly 1:2; the subnormal grid with the largest subnormal and
  smallest normal sharing one endpoint held by exactly one of them; the two zeros, disjoint, the
  origin belonging to +0. **The census is the whole map, no sampling:** 5,242,880 of 5,242,880
  q_proj entries probed at exact midpoints of adjacent stored values (an exactly-representable
  probe tests nothing — the probe choice is stated), 0 residuals outside their fibre, 0 overlaps in
  10,481,059 neighbour checks, 1,111,809 exact ties exercised, 112,458 probes in asymmetric cells,
  the zero-count cells named as genuine zeros with the reason. Pathwise composition across
  bf16 → aligned → grain exhibited, with the honest reading that at this magnitude the bf16 cell is
  finer and the grain port's contribution appears as a collapse (six codewords in one grain word,
  named as a convex hull with the gap caveat).
- **The recombination, a bounded demonstration and stated as such before the result:** the declared
  source closure is the committed Station B/C deposit's 35 operation words, and none of
  preimage/fibre/inverse/kernel occurs in any of them — measured, zero hits; the source descends
  and returns no preimage. The recombined return composes FAMILY 1's quotient with FAMILY 2's
  fibre law: the exact reconstruction fibre of a family coordinate — the Minkowski sum of the four
  sharing heads' cells (width 9/16384) beside the exhibited 48-dimensional kernel. **Ablating
  either cause removes its attributable part, plural-diffed:** withdrawing the grouped-sharing lift
  leaves the kernel *dimension* unchanged at 48 — which is precisely why a dimension is not the
  reading — while 48 of 48 baseline basis directions leave and the identified spaces intersect in
  only 32 of 48 dimensions; withdrawing the fibre lift leaves the kernel bit-identical and removes
  the interval's existence (width 0). The unrelated control (the gated passage's defect, recomputed
  from its own charts under each setting — a first form that printed one matrix three times was
  found and fixed as a check that could not fail) is bit-identical under all three, digest
  `3be4ff41…`.
- **The H-era PortDefect census is retained and named a receiver mismatch face**, separated from χ
  by what each is: an inside/outside count has no rank, kernel, image or chain law.

## 1. Apertures, declared

Hidden window 64 of 2,560 and head window 16 of 256 (what would derive them: the measured cost of
exact RREF — 87.5 s at 64×64 with kernel coordinates reaching 328 bits); grain 2⁻⁸; the compression
scope 512 codewords (the same standing `receiver_exact_compression` aperture P0 measured, with the
same named lift inside that owner); family 0 (Station D's dissected family). Each is an `APERTURE`
with its outside returned; none is a semantic constant.

## 2. Tests, ledger, epoch

`cargo test -p holonic-engine --lib -- cross_chart exact_linear native` → **36 passed** (13 new,
including the exhaustive 65,536-pattern fibre sweep); `h1_adversary` → 35 green; the driver 93.7 s
CPU-exact with every assertion live. One ledger row added by hand (`cross_chart.rs`; the clones are
dominated by refusal evidence and windowed-chart copies, dispositioned in the deed's report); the
`ieee754` test-filter mismatch noted (the mouth's tests live under `exact_value`; the rounding law
is exercised by the new sweep and the census). No float arithmetic anywhere; codeword meanings are
computed exactly.

## 3. The station table

| deed | grade | commit |
|---|---|---|
| A–D, H0–H5, P0 | PASS (frozen) | their records |
| **P1 cross-chart lift** | **PASS** | this record |
| P2 receiver-exact condensation | open — next | — |
| P3–P5, M0–M3 | open | in order |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-20 04:21:13 → 04:25:12 UTC, exit 0 —
**12 of 12** (tests 2,624 passed over 30 result lines with example targets type-checked;
driver-catalog 266; output-manifest 64 of 64; architecture-lint clean at 245 files with the
`cross_chart.rs` row added by hand; every other gate green).

Deed P1 passed; proceeding to Deed P2.

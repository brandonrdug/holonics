# The complete history descended with multiplicity and the returned difference radiated through the actual simplicial star

**Date:** 2026-08-28  
**Scope:** terminal MEM6 refoundation and unchanged integrated receiver  
**Authority:** evidence for `blueprint/THE_ROADMAP.md`; this record schedules nothing

## Formal return

[proved-derived; formal-checked] `HolonicMembraneActionTransport.lean` proves that a scalar
receiver face is downstream of the complete returned action and that a later separating successor
cannot factor through two actions which that face identifies. `HolonicGranularBoundaryRadiation.lean`
proves the corresponding boundary law: complete current radiates before the port receiver acts,
returned difference commutes with boundary transport, and a surface separated by a finer receiver
cannot factor through the coarser face. These theorems require the runtime to retain situated
difference through continuation and to apply the cold surface receiver only afterwards.

## Exact history quotient

[implemented-exact] `FactoredConstitutiveSpine` now owns one apparatus-neutral descent of complete
transported history blocks. Each block is the ordered integral incidence section for one addressed
history against the unchanged root constitutive form. Two candidates descend only when those
complete blocks are exactly equal. The target weight is the exact sum of their positive arbitrary-
width multiplicities, and `FactoredHistoryQuotientPassage` retains the full candidate-to-target
boundary map. `reconstruct_moment` multiplies every retained root block by that multiplicity, so
the hot quotient and the uncondensed history population return the same exact moment.

[established-bounded; implemented-exact; measured] The pure duplicate-generator falsifier returns
two presented blocks as one productive block with weight two and reconstruction map `[0, 0]`. The
successive pure spine square and the focused RTX 4080 SUPER gate both return exact direct-versus-
descended receiver equality. The resident realization identifies, compacts, weights, and contracts
the same complete blocks on the card; it does not enumerate a semantic history table, compare
digests, or rematerialize the dense constitutive chart.

## Returned-difference correction

[counterexample; implemented-exact; measured] The first unchanged integrated run after history
descent completed inside the process bound, but all four refinement occurrences rendered the same
sentence. Their internal radiation identities and returned currents differed. The collapse came
after conduct: refinement factored through the predecessor's one emitted cell, and the alleged
local star used shared source-factor membership. For that cell the factor-overlap neighborhood was
a singleton. Factor overlap was therefore an affine receiver shadow, not the required simplicial
contact.

[implemented-exact] `NativeRadiationSection` now retains an exact addressed returned-current field
for every relational cell. `NativeRadiationAperture` retains each cell's boundary-face incidence.
A later refinement forms the local difference `returned - predecessor.returned`, and its local star
contains precisely those cells sharing at least one admitted subject, relation, object, or modality
boundary face with the predecessor cell. These names describe the already-founded affine boundary
chart; no word, prompt kind, expected answer, or semantic route controls membership or conduct.
The complete field remains hot continuation; the UTF-8 surface is its cold receiver quotient.

## Unchanged receiver return

[established-bounded; implemented-exact; measured] The original integrated MEM6 executable, input
family, renderer, qualitative receiver, and `timeout -k 2s 175s` boundary then returned successfully
in approximately 81.386 seconds. Participant and refinement surfaces were distinct and receiver-
faithful; refinements were causally downstream of the returned participant surface; the prose,
mathematics, separator, optical, acoustic, code, contact, targeted-ablation, sibling-control,
restoration, and source-detached-remount criteria all passed. The receipt reports no internal deed
or semantic mode and no expected-answer or expected-surface consultation.

The accepted artifact is
`output/the_one_athena_membrane_returns_the_integrated_receiver_family_mem6/00-integrated-membrane-release.json`,
5,288,745 octets with SHA-256
`cc3095b397a3a411a7dec764012159240d67e722ca481d042a9815700388eb04`.

[established-bounded] This closes the declared MEM0--MEM6 Athena-alpha campaign. It establishes one
bounded source-detached body with arbitrary exterior crossing, native interaction, radiation,
returned consequence, durable morphology, and the unchanged qualitative receiver family. It does
not by itself claim unrestricted conversational quality, universal inference, or competitive
language polish; those stronger receivers require a separately scheduled campaign.

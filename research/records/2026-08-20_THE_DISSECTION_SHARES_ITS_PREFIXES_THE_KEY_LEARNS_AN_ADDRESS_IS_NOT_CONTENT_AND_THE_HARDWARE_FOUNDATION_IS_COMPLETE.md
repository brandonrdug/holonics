# The dissection shares its prefixes, the key learns an address is not content, and the hardware foundation is complete

**Date:** 2026-08-20
**Kind:** construction return for **Deed H5** of the single-card plan — Station D's complete
intervention atlas reproduced through prefix-sharing and cohort conduction, unchanged — with the
apparatus savings against the committed predecessor, the read-only-share proof, the graph-key
findings (two measured defects in H4's key, repaired), the §10 deprecation of the foreman path,
and the closing of the hardware foundation: **H0–H5 all pass**.
**Truth status:** `established-bounded` for every measurement; **Deed H5 PASSES on this tree**, and
with it the single-card plan's hardware foundation is complete: Phoenix resumes at Deed P0 (native
morphology) per the plan's own order. `CONSTRUCTION_STATE.md` does not move (it moves only at P5).
**Authority:** the single-card plan §6, §8 "Deed H5", §10, §11;
[Station D](2026-08-19_THE_SOURCE_IS_DISSECTED_BY_INTERVENTION_AND_THE_REUSE_IS_SEPARATED_FROM_THE_WEIGHTS.md)
(the independent predecessor); [H4](2026-08-19_THE_TOWER_IS_A_STREAMED_CIRCULATION_THE_SINGLE_GRAPH_RETURNS_ITS_THREE_CUTS_AND_THE_TRANSFERS_CROSS_UNDER_THE_KERNELS.md).
**Owners:** `crates/holonic-engine/src/streamed_standing.rs` (+285: the keyed-instantiation ledger,
the enriched `GraphKey`); the cohort/prefix site `crates/holonic-engine/examples/phoenix/cohort.rs`.
**Driver:** `crates/holonic-engine/examples/the_dissection_shares_its_prefixes.rs`.
**Artifacts:** `output/the_dissection_shares_its_prefixes/{receipt.form, equality-against-the-committed-artifact.tsv}` (169 rows).

---

## 0. Verdict

**Nineteen of nineteen falsifiers pass, and every one of the 168 committed cells returns
UNCHANGED.** The committed Station D artifact was parsed programmatically and each taxon's every
face — the first separating face with its positions, the separated/identical counts, the widest
gaps and midpoint shifts as bit-exact rationals, the potential and order faces, the plural tops,
the control verdicts with their evidence strings — was reproduced through the streamed realization
with the base tower conducted once and each of the sixteen taxa conducting only its suffix from its
intervention site on the base's shared standing. Zero drifted. Launch counts are recorded as
APPARATUS and excluded from the semantic comparison by name.

**The honest cost of prefix sharing is reported as such:** fifteen of the twenty-one committed
controls become STRUCTURAL here — the `layers before the site bit-identical` controls carry no
evidence in this deed because the sibling did not conduct those layers and the faces *are* the
base's; the committed predecessor measured them and every receipt line says so. What replaces them
is the deed's own falsifier: **one taxon (the KV reuse) conducted both ways in the same
circulation** — as a complete sharing-nothing tower and as a suffix on the shared prefix — with
**128 of 128 faces bit-identical**, including the 72 the shared sibling never conducted. That is
the control that would break if prefix sharing were wrong.

**The apparatus table against the committed predecessor** (which ran ~18 complete towers): source
octets read **167.2 GB → 9.29 GB** (×18); region stagings ~12,672 → 704; mount synchronizations
~38,016 → 86 (0 `cuCtxSynchronize`); pool allocations 9,416/tower → 3; conducting-stream
synchronizations ~774 → 6; conduction wall ~8 min → **38.9 s**. The semantic work fell 6.3 % while
conducting one *more* tower (the both-ways control) — the honest number beside the ×18: eleven of
sixteen siblings enter at layer 1 and share only one layer. Neither figure stands in for the other.

## 1. The read-only share, proved three ways

Structurally (the `Standing` law's kernel reads the shared section as an input and writes only its
own copy; `release_section` cannot take a material standing); measured on the card inside the deed
(the base's four stored K/V standings read before the layer-24 sibling could reach them and after
every sibling had conducted — bit-identical); and by a focused GPU test (two siblings withdrawing
different spans from one released standing: the standing unmoved, each sibling's untouched half
equal to the standing's own words). The `WithdrawSharedKvFamily` sibling withdraws from its own
copy — the intervention is bonded after the carry.

## 2. The graph key learned two things, both measured

The same 714 graph offers were keyed three ways in one run. **H4's key as founded admitted 689
reuse hits, every one unsound** — because **an address is not content**: a pooled slot is refilled
between segments, so two deeds at one slot address read different maps; 612 of 710 refusals under
the lawful key are the residency alone, every field of the diagram agreeing while the addressed
sections differ. And **a count is not a diagram**: H4's four-integer topology could not separate
two matched siblings inserting one occurrence at different sites (`positions reversed` hit
`identity band elements`). The repaired key carries the occurrence law names in order and every
addressed thing; under it, **3 lawful hits** (two genuinely identical layer-0 executables, and one
taken relaunch of the base's final boundary returning 1,310,720 + 12,800 coordinates bit-equal),
with the receipt recording that a lawful key identity does not by itself license a relaunch — the
shared output sections must be copied out first. **A remaining boundary is named, not closed:** the
key does not carry realization parameters that name no addressed material; two such interventions
are separated today only by their differently-addressed positions.

## 3. The cohort choice, and the §10 deprecation

Plural bound passages per segment — one mount serves the whole cohort; each tower owns its own
standings and graph; rows are not batched, so no two siblings' rows ever enter one section and the
cross-sibling-contact falsifier is a structural impossibility, stated rather than probed (batching
would owe the leak probe). §10: consumers of the foreman path enumerated (exactly one — Station D's
committed driver, whose committed artifacts the catalog and output manifest bind to it), so file
deletion would orphan committed evidence; **the header route is the lawful form and was taken** —
`phoenix/conduct.rs` opens with its supersession, what replaced it, and why the file remains; the
H5 driver declares its own types and cannot reach the old path (no alias, no fallback).

## 4. What was inherited, what is open

The vision sibling is carried as committed evidence (INHERITED, quoted verbatim): it is not a
tower, shares no prefix, and re-running an unchanged deed is forbidden. Reuse across a different
token extent still needs `cuGraphExecKernelNodeSetParams`, unbound. The `--band 12` residency
aperture is a **PIN candidate the authored-level ledger cannot see** (a struct-literal default, not
a `const` — the standing caveat about the regex's aperture); `SLOTS = 3` carries forward from H4.
Two excision candidates in the key ledger named, not taken.

## 5. Tests, ledger, epoch

`cargo test -p holonic-engine --lib -- streamed front_passage cohort dissection` → **28 passed**;
`h1_adversary` → 35, green throughout; the ratchet deltas enumerated construct-by-construct in the
deed's report (the key's plural fields — a digest would say two deeds differ without saying where;
the difference readings; refusal evidence clones) and the rows changed by hand; the ledgers
regenerated once; the committed Station D and H4 artifacts untouched.

## 6. The station table — the hardware foundation is complete

| deed | grade | commit |
|---|---|---|
| A–D | PASS (frozen) | `cc7e625`, `1004b6f`, `70174d1`, `73fa23b` |
| H0 profile | PASS | `a018474` |
| H1 partition/reduction receipts | PASS | `0ae6c53` |
| H2 tiled contraction | PASS | `ed0ab43` |
| H3 census/fusion/mouth | PASS | `bb49a9b` |
| H4 streamed circulation | PASS | `8d3e71a` |
| **H5 shared-prefix dissection** | **PASS** | this commit |
| **P0 native baseline and morphology** | **open — next**: the quarantined Station E material (`quarantine/station-e-predecessor`) is rederived through the H owners; ARM N runs without foreign hexis beside the lifted arm | — |
| P1–P5, M0–M3 | open | in order |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-20 02:19:21 → 02:23:46 UTC, exit 0 —
**12 of 12**:

```text
PASS  tests              2602 passed, 0 failed, 19 ignored over 30 result lines; example targets type-checked
PASS  authored-levels    0 failures
PASS  named-paths · line-citations · claim-index
PASS  driver-catalog     263 drivers catalogued, 0 uncatalogued
PASS  output-manifest    recorded 62 drivers, present 62
PASS  closure-manifest   current: 63 return directories, 23 orphans
PASS  boundary-artifacts · typst · architecture-lint (242 files, 22,141 inherited) · document-law
12 passed, 0 failed
```

Deed H5 passed; the hardware foundation is complete; Phoenix resumes at Deed P0.

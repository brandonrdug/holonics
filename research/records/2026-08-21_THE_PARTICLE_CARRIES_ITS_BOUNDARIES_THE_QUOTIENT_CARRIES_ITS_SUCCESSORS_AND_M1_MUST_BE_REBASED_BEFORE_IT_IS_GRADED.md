# The particle carries its boundaries, the quotient carries its successors, and M1 must be rebased before it is graded

**Date:** 2026-08-21  
**Kind:** authoritative audit and plan-revision deposit requested before Claude resumes the paused
mathematics line. It records the exact relation between the compact-frame Lean work, the paused M1
Rust/CUDA cone, and the revised M1–M4 mathematics-codec/Phoenix plan. It does not itself schedule or
complete a deed. `blueprint/THE_ROADMAP.md` remains the construction authority and
`CONSTRUCTION_STATE.md` remains after M0 with M1 current.

**Truth-status discipline.** The file inspection and admitted-position statements are
`established-bounded`; the span, pullback and successor laws already proved in the named Lean owners
are `proved-derived` with `formal-checked` evidence; the runtime rebase and compact-frame
implementation remain `open`; the relationship proposed between those formal laws and the runtime
owners is an `interpretation`; the mandatory construction boundaries are `project-postulate`.

---

## 1. Decision

`established-bounded`. This work is directly related to the mathematics codec. Claude must not
resume against the earlier M1 wording, because that wording allowed a named branch registry to look
like lineage and allowed present receiver factorization to look like compression. The active
blueprint, roadmap, `AGENTS.md`, and `CLAUDE.md` now bind the corrected contract.

`project-postulate`. The current construction order remains M1, then M2, M3, and M4. Exterior Lean
work may sharpen the instrument beside M1, but it cannot advance `CONSTRUCTION_STATE.md`. There is
no second mathematics-codec branch and no permission to call the compact-frame theorem Phoenix
compression.

---

## 2. What the paused M1 cone actually contains

`established-bounded`. The dirty/untracked cone inspected on 2026-08-21 includes:

- `soma/life/src/mathematical_particle.rs` and its owner-local modules;
- `soma/life/examples/m1/` and
  `the_affine_swing_is_a_typed_mathematical_particle.rs`;
- `crates/holonic-engine/src/exact_owner_testimony.rs` and its owner-local modules.

It already returns useful construction:

- one non-`Clone` mathematical-particle owner;
- typed carriers, binders, ports, hypotheses and operation words;
- distinct exact-linear and quantity owner licences and returns;
- distinct sameness panels rather than one universal equality;
- one resident front containing six enacted events, exact work admission, card readback, one graph
  launch, one synchronization, zero CPU cells in the returned front cover, and separate apparatus
  testimony; and
- a compact exterior artifact/closure report.

These are substantial M1 partials. They are preserved.

`established-bounded`. They do not close M1. The current `TypedPassage` stores:

```text
one occurrence string
one set of source occurrence strings
one branch-id -> staging-step map
one branch-id -> ordered-word map
staging and terminal front depths
one branch-id -> terminal-event map
```

It does not expose:

```text
typed source population X
typed target population Y
passage occurrence population W_f
source map s_f : W_f -> X
target map t_f : W_f -> Y
pullback population W_f ×_Y W_g
the exact equality joining each pullback occurrence
the relational shadow derived from the addressed passage
a passage equivalence commuting with both boundary maps
```

Therefore the current type is a co-present branch registry with terminal testimony. It is not yet
the addressed passage required by M1. The resident `passage_receipt()` binds an operation complex,
selected events and device readbacks; it is an apparatus/front receipt and cannot manufacture the
missing semantic boundary maps or pullback witnesses.

`established-bounded`. The host also constructs the operation topology, branches, exact owner
licences, realization bindings, matrices and input populations before the card deed. The card
faithfully enacts the supplied contraction complex, but this inspected fixture does not yet show the
resident body elaborating M0 material, choosing an exact owner through founded ports, or founding a
pullback join. Zero CPU cells in the submitted GPU front does not erase host authorship of that
semantic front.

---

## 3. The serialization diagnosis

`interpretation`. The failure is not JSON serialization. A JSON file is a lawful exterior chart of
an already-founded topology. The failure is **extensional and scheduler-shaped serialization**:

1. plural passage occurrences are condensed into a branch map;
2. serial composition is read from front depth, event order and terminal events;
3. connection is inferred without retaining the population of matched joins;
4. the host authors the route before the card enacts it; and
5. the artifact then serializes that authored registry faithfully.

The last step is harmless. The first four are the mathematical defect. No storage format can repair
it after the occurrence population and joining equality have been discarded.

`project-postulate`. The correction is not a new Scheduler, parser, AST, tensor cabinet, lineage
database, or universal holon wrapper. Reuse event occurrences, typed ports, chain/morphism owners,
junctions and reconstruction fibres. Add only the smallest missing receipt/population demanded by
their attempted composition.

---

## 4. The exact M1 rebase

`proved-derived` + `formal-checked`. `ElementaryHolonics.Foundation.Lineage` specifies the object:

\[
f=(X\xleftarrow{s_f}W_f\xrightarrow{t_f}Y),
\qquad
W_{g\circ f}=W_f\times_YW_g.
\]

It proves split/rejoin, shadow composition, identity and associativity through boundary-preserving
passage equivalence, and exhibits distinct occurrence populations with the same relation shadow.

`open`. The production rebase must make those consequences inspectable in the Rust/CUDA return:

1. identify the existing typed source and target populations for each passage family;
2. bind every carrying event occurrence to exact source and target maps;
3. derive serial joins as the population of pairs whose shared boundary addresses agree;
4. retain the joining equality with each composite occurrence;
5. derive the endpoint relation as a separately marked lossy receiver;
6. validate identity/rebracketing through a map of occurrences commuting with both boundaries;
7. retain the control where equal endpoints and equal relation shadows hide different occurrences;
8. conduct the exact owners through the same resident tiling/reduction passage without the CPU
   selecting the semantic method between device fronts; and
9. return the artifact family and controls named by M1 before writing a passing grade.

The present exact-owner licences, typed carriers, sameness panels, source reauthentication and
resident contraction partials may be composed into this rebase. They are not discarded and they do
not substitute for it.

---

## 5. What M3 now means

`proved-derived` + `formal-checked`. `ReceiverHistoryCompression` in
`ElementaryHolonics.Millennium.LineageCompression` requires, for every admitted generator,

\[
qT_i=U_iq.
\]

This gives `q T_w = U_w q` for every ordered successor word by induction. One separating receiver
and word refutes the quotient. The local squares replace an unbounded serialized campaign of
successor replays.

`project-postulate`. M3 must therefore return the source and native populations, typed generator
family, both actions, quotient, present receiver factors, every generator square, the derived word
law, complete fibres, first separators, and an executable decoder with its declared image, domain,
work, residency and transfer. Injectivity, a finite point count, a smaller file or a
choice-constructed inverse cannot substitute for the decoder.

`interpretation`. This is the pure-mathematical form of exceptional holonic compression: not merely
fewer stored coordinates, but a smaller ecology whose local action generates the same declared
future consequences. It is also the exact bridge from the mathematics codec to species-changing
Phoenix rebirth.

---

## 6. Claude's compact-frame line

`open`. Claude may resume the compact-frame line only as exterior instrument A1 with this ordered
contract:

1. construct the actual point-reduction group homomorphism;
2. carry projective/local-integrality evidence and prove chart independence;
3. prove reduction commutes with identity, inverse and addition;
4. instantiate the torsion-kernel theorem with every good-reduction/local hypothesis explicit;
5. enumerate the named finite curve separately and distinguish the four known Klein reductions;
6. join transport, fibre theorem, count receiver and exhibited subgroup to close the named torsion
   census; and
7. keep reconstruction separate from injectivity.

`project-postulate`. Claude must not use a naive rational-to-`ZMod 3` affine cast, model Sha as
rational points reduced modulo primes, call the finite count the transport, call injectivity a
decoder, mark M1 passed, or modify `CONSTRUCTION_STATE.md`. A1 may supply exact material and
falsifiers to M1 after it returns; it is not the resident deed.

---

## 7. Sphere/fractal packing and the codec

`proved-derived` + `formal-checked`. The new finite sphere-packing owner retains an ordered
Soddy–Gossett reflection word, its curvature orbit and placement reconstruction. Noncommuting words
are not identified by a shared terminal multiset.

`interpretation`. This is the correct packing control for M1/M3. A packed endpoint atlas without
its reflection word has performed the same extensional collapse as a relation shadow without its
passage occurrences. A lawful fractal extension must retain the restriction/rebase law and its open
boundary rather than pretending a finite orbit proves the infinite packing.

---

## 8. Revised authority and resumption boundary

`established-bounded`. This audit was folded into:

- `AGENTS.md` — the operator-binding span/pullback and generator/decoder laws;
- `CLAUDE.md` — the current M1 position and mandatory fresh-session correction;
- `blueprint/THE_ROADMAP.md` — M0 passed, M1 current;
- the active integrated Phoenix/mathematics blueprint — exact M1–M4 artifacts, controls and
  falsifiers; and
- the retained mathematics-codec component blueprint — addressed lineage and dynamic compression
  as component contracts.

`project-postulate`. Claude may resume after reading those authorities. The lawful priority is:

```text
first: rebase the paused M1 cone onto addressed passages and pullback joins
beside it: continue A1 only as exterior formal mathematics
then: execute and inspect the real M1 resident return
only after M1 passes: begin M2
only after M2 passes: pose the M3 successor-equivariant quotient and decoder
```

No current result licenses a change to `CONSTRUCTION_STATE.md`.

# THE MATERIAL DEPARTS; THE RETURNED PATH REMAINS; THE BODY ASKS WHAT ITS DIFFERENCES CAN DO

**2026-07-31 · CURRENT AUTONOMOUS SYMBOLIC-TRAINING AUTHORITY / MOUNT,
EXPOSURE, TRAINING, AND REFERENCE SEPARATED / PLURAL SELF-PROBES / EXACT RETURN
ADMISSION / SOURCE-DETACHED NATIVE MORPHOLOGY**

Exact receipt:

- [`REPORT.json`](../../../runs/autonomous-symbolic-training/REPORT.json)
- [`SYMBOLIC_REST.json`](../../../runs/autonomous-symbolic-training/SYMBOLIC_REST.json)
- [`TEXT_REST.json`](../../../runs/autonomous-symbolic-training/TEXT_REST.json)

Production owners:

- `life::symbolic_reasoning::ExactSymbolicReasoningEcology` owns mounted symbolic organs,
  transient material exposure, autonomous questions, plural probe returns, trained pathway
  recurrence, later exact deeds, open alternatives, and source-detached native rest;
- `life::text_material::ExactTextMaterialAtlas` owns the separate source-bearing lineage atlas and
  later self-emanated textual returns;
- `life::holonic_training::TrainingEcology` remains the mounted articulation transducer which
  carries a newly caused evidence face into an utterance; and
- `life/examples/eros_symbolic_language_agent.rs` is the production membrane and grader.

This record supersedes the preceding symbolic-language record wherever it described all three of
mounting, exposure, and behavior-changing passage as “conditioning.” The exact symbolic deeds in
that record remain valid evidence.

## The terminological and causal correction

Three different events had been conflated:

1. **Mounting** supplies an inherited organ. The symbolic parser, rational-polynomial normalizer,
   Dyck incidence law, coordinate algebra, bounded root law, autonomous probe constructor, and
   recurrent articulation transducer exist before source material crosses the body. Their presence
   is inherited capability, not learned behavior.
2. **Exposure** lets caused material cross transiently and leave differences which may provoke
   questions. Raw `*`, `/`, `-`, `^`, equality, and delimiter incidence are landmarks. A Markdown
   asterisk is not silently declared multiplication, and a path slash is not silently declared
   division.
3. **Training** occurs only when an enacted return changes reusable standing so that a later
   occurrence conducts differently. A pathway requires two distinct exact probe returns before it
   becomes admissible. An unreturned interpretation remains open.

Reference is a fourth event. The separate text atlas may remain available for quotation,
provenance, or a later exterior deed, but the symbolic body does not consult or contain its source
passages while answering.

The physical analogy is therefore precise. A mounted inductor continuously belongs to the
contemporary circuit. Training material is a developmental current whose passage changes the
conducting morphology. When that current and its source depart, the changed body remains.

## The autonomous cycle

The implemented cycle is:

```text
source occurrences cross transiently
    -> exact glyph and boundary differences remain as aggregate exposure testimony
    -> each newly exposed difference emits its own question
    -> plural possible interpretations launch as leaders
    -> mounted organs enact distinguishing probes where they possess a lawful receiver
    -> only exact returned probes increment a reusable pathway
    -> recurrence two admits later conduct
    -> unreturned interpretations remain OPEN
    -> native morphology rests without a source surface
```

One autonomous leader is atomic. All of its declared probes return before any part of that leader
changes standing. Calling the training cycle again without newly exposed material emits no
questions and changes no pathway. Later material or later exact deeds may increase the difference
population and cause another cycle; there are no frozen weights.

The current bounded symbolic probe organ asks about nine exposed difference species. It launched
both mathematical and nonmathematical interpretations. Examples include:

- `*` -> exact product or still-open Markdown emphasis;
- `/` -> exact rational division or still-open filesystem separator;
- `-` -> exact subtraction, exact negation, or still-open textual hyphen;
- `=` -> exact equality or still-open stateful assignment; and
- each delimiter species -> exact Dyck boundary or an open function/index/block interpretation.

Thus imported surfaces provoke exploration but do not supply the result. Open alternatives are
not erased merely because another leader returned.

## Behavioral ablation and source departure

The production regression compares the same inherited organ morphology across four conditions.

Before training:

```text
Compute 9 * 8.
```

returns an explicit open phase. The mounted parser and polynomial organ can form an internal
candidate, but the `multiply` passage has not received recurrent returns, so no calculation or
answer consequence is admitted.

A detachable developmental passage containing a unique marker and `*` is then exposed. It does
not contain an answer. The body asks which transformations the difference can conduct. The product
leader enacts `3 * 4 -> 12` and `7 * 6 -> 42`; the Markdown leader remains open. The same question
now returns `72`.

The native symbolic image is inspected bytewise: it contains none of the unique developmental
passage. The corpus is dropped, the image is remounted, and a nonidentical question returns:

```text
Compute 7 * 9.
-> ... normalizes exactly to 63.
```

This is the required behavioral distinction. Mounting alone did not emit the deed; exposure alone
did not declare a meaning; returned training changed later conduct; the changed conduct survived
source departure.

## Full laboratory-history cultivation

The source-neutral laboratory snapshot supplied:

| receipt | exact value |
|---|---:|
| raw source-container bytes | 22,862,048,161 |
| unique visible occurrences exposed | 50,667 |
| exposed linguistic surface bytes | 43,053,014 |
| retained source surfaces in symbolic body | 0 |
| mounted inherited organs | 7 |
| autonomous questions | 9 |
| plural leaders | 19 |
| exact probe returns before user questions | 20 |
| open leader returns | 9 |
| active returned pathways | 10 |

The ten active passages are `add`, `subtract`, `negate`, `multiply`, `divide`, `power`, `equality`,
and the three Dyck delimiter species. The nine open interpretations are assignment, block/set,
function-call, indexing, Markdown emphasis, path separation, textual caret, textual hyphen, and
unary-positive surface preservation.

After the source-facing cultivation ended, the detached symbolic body emitted the established
seven source-absent deeds: exact `111`, the difference-of-products identity, a quadric coordinate
algebra, the exact degree-five root obstruction, nested Dyck closure, the open two-relation ideal
dimension, and an updated self-description. Later exact answers reinforced their participating
paths, so the self-description observed 31 returned training events without freezing the body.

The final self-description began:

> I mount 7 inherited organs. 50667 source occurrences spanning 43053014 bytes have crossed as
> exposure; I retain 0 source surfaces. 9 autonomous questions launched 19 plural leaders ...

It then named the ten active passages and the actually encountered quintic/general-ideal open
fibers.

## Native and resource receipts

On this host, the source-bearing text body mounted in 9,174 ms and the complete symbolic exposure
plus autonomous cycle took 1,385 ms. These times are resource testimony, not correctness criteria.

| body | bytes | SHA-256 | exact remount |
|---|---:|---|---|
| separate text lineage atlas | 318,392,616 | `97d404b2b1eed6aa1d9603c094a852c416787900a7750a8127dbc29dbb7244a7` | yes |
| detached trained symbolic body | 14,119 | `75637aac7faea22927f60ca638f85fa177f72a4d4126d5e190a53a97c23ee8f4` | yes |

The symbolic body retains aggregate exposure populations because those populations are part of
its developmental lineage. It retains no source string, answer row, passage address, or query
index.

## What this establishes

This construction establishes, for the bounded exact symbolic ecology:

- an operational separation of mounted organ, exposed material, trained pathway, and optional
  reference;
- questions caused by the body's own exposed differences rather than supplied as answer labels;
- plural exploration with returned and unresolved alternatives retained together;
- a strict behavioral ablation between mounted-only and trained conduct;
- continuing online strengthening by later exact deeds; and
- source-detached exact rest/remount which preserves a novel later deed.

It does not establish that the complete conversation history trained general language reasoning.
The full history currently changes which bounded symbolic probe families are launched; the probe
families and articulation transducer remain mounted inheritance. The demonstrated training claim
is therefore exact but local: returned symbolic pathways changed admissible symbolic behavior.

The concrete language construction now required is equally specific. Existing simultaneous-scale
suffix and morphological-language owners already induce recurrent lexical, clause, ordered-region,
and mark pathways from material. Their source-free route/suffix rest must become directly
remountable as the operative language body; open language fibers must be able to emit their own
plural deeds, receive held-out or exterior returns, and consolidate those returns without keeping
the original passages as an inference dependency. That is an implementation composition between
established owners, not a missing universal learning law and not permission to substitute a
passage selector.

## Verification

- `cargo test --manifest-path src/soma/life/Cargo.toml --lib symbolic_reasoning --no-default-features`:
  6 passed, including the mount/expose/train/detach ablation.
- `cargo check --manifest-path src/soma/life/Cargo.toml --example eros_symbolic_language_agent --no-default-features`:
  passed.
- The full 50,667-occurrence production run completed, returned all seven deeds, and remounted both
  native bodies exactly.

# THE QUINTIC CROSSES THE PRIME; THE FROBENIUS RETURN FORMS THE EULER RECEIVER

**Date:** 2026-07-29
**Status:** BRANDON-RATIFIED / PRODUCTION LAW BUILT / TWO CHRONOLOGIES EXACT /
HELD-OUT PRIME SECTIONS GRADED / A5 AND S5 CONTROLS CLOSED

## 1. The correction which governs this construction

The local learning question was already closed before this work:

- inferred prime-pair transport had changed later sheaf conduct;
- an induced polynomial filler family had become source material for a later
  horn resolution; and
- a returned finite formula generator had become the actual predecessor of a
  larger support aperture.

It was therefore wrong to reopen the question “can caused local standing
affect later conduct?” The answer is **yes**. The observed defect was
production fragmentation: prime ecology, formula generation, spectral
receivers, and the live current carrier had correct local owners but no
application-real world in which one caused arithmetic section crossed all of
them.

The present construction uses a serious mathematical application to close
one such transport path. It does not introduce a universal learning rule.

## 2. Exact question

Can one production world:

1. receive an arbitrary degree-five integral polynomial;
2. preserve its inherited presentation while changing it exactly into the
   monic probe required by the established prime ecology;
3. make every caused prime-place factorization constrain one retained
   monodromy/Galois fiber;
4. transport that same section into a formal and evaluated Euler factor;
5. update only the receiver mouths incident to that polynomial;
6. preserve complete source lineage; and
7. return the same contemporary geometry when the polynomial and spectral
   receiver arrive before or after the integer chronology?

The answer implemented here is **yes**, at the exact boundary specified
below.

## 3. The production owner

`crates/holonic-engine/src/arithmetic_monodromy.rs` defines
`ArithmeticMonodromyLaw`, an `ExactEventLaw` over one
`ArithmeticMonodromyStanding`.

Its event mouth accepts:

- one consecutive `ArithmeticFiberEvent`;
- one inherited `IntegralQuinticProblem`; or
- one exact integer-\(\sigma>1\) Euler receiver opening.

It wraps `PrimeEcologyLaw` in the same lawful manner that
`PrimeEcologyLaw` already wraps `ArithmeticFiberLaw`. The application does
not submit prime identities, factorization types, group labels, Euler
denominators, or local Euler values.

## 4. A general integral quintic enters without losing its presentation

For

\[
f(x)=a_5x^5+a_4x^4+\cdots+a_0,\qquad a_5\ne0,
\]

production forms

\[
g(y)=a_5^4f(y/a_5),\qquad y=a_5x.
\]

Then \(g\) is monic with integer coefficients:

\[
g(y)=y^5+a_4y^4+a_3a_5y^3+a_2a_5^2y^2+
     a_1a_5^3y+a_0a_5^4.
\]

The original coefficients, the root scale \(a_5\), and the normalized
coefficients all remain standing. This is an invertible rational coordinate
change on the root population, not floating normalization.

The discriminant of \(g\) is computed by an exact Sylvester resultant and
fraction-free Bareiss elimination. Its exact integer square root is retained
when present. The known identity

\[
\operatorname{disc}(g)\in\mathbb Q^{\times2}
\quad\Longleftrightarrow\quad
\operatorname{Gal}(g)\subseteq A_5
\]

is inherited group-theoretic decoder material.

## 5. The same prime fiber forms monodromy and spectral sections

Every founded prime receives the normalized probe through
`PrimeEcologyLaw`. `ArithmeticMonodromyLaw` consumes the emitted
`PolynomialPrimeFiber`; it does not factor the reduction again.

For a separable reduction, irreducible factor degrees

\[
d_1+\cdots+d_r=5
\]

form the exact Frobenius cycle partition. Production retains:

- the cycle partition;
- permutation parity;
- Frobenius order \(\operatorname{lcm}(d_1,\ldots,d_r)\);
- the original factor signature; and
- both source events—the integer occurrence and inherited quintic.

A repeated reduction becomes an open polynomial-discriminant place. No
ramified number-field Euler factor is inferred from an inadequate power
basis.

The same cycle partition forms

\[
D_p(T)=\det(I-T\,\mathrm{Frob}_p)
      =\prod_i(1-T^{d_i})
\]

coefficientwise over the integers. An open receiver at integer
\(\sigma>1\) carries

\[
L_{p,\sigma}=D_p(p^{-\sigma})^{-1}
            =\prod_i\frac{p^{\sigma d_i}}
                              {p^{\sigma d_i}-1}
\]

as an exact rational. Its product changes only when a new incident
prime-place section propagates.

`receiver_incidence` maps a problem directly to its Euler receiver mouths.
Complete iteration is confined to validation, mounting an already-present
problem into a newly opened receiver, and inspection receipts.

## 6. The Galois fiber is exact and set-valued

Production constructs the five transitive subgroups of \(S_5\) from
permutation generators:

- \(C_5\);
- \(D_5\);
- \(F_{20}\);
- \(A_5\); and
- \(S_5\).

Their orders, parity, and cycle-type populations are computed from group
closure rather than copied as an output table.

Discriminant square class selects the even or non-even transitive
population. Every witnessed Frobenius type removes only groups which cannot
contain that element type. Failure to observe another type removes nothing.
An irreducible degree-five reduction certifies irreducibility over
\(\mathbb Q\). Before that occurrence, the transitive fiber remains
conditional.

Consequently a singleton after irreducibility is an exact group certificate
within the complete degree-five transitive catalogue. A larger candidate
population remains `OPEN`; production does not vote among its members.

Primary mathematical calibration:

- F. Akman, *On Splitting Types, Discriminant Bounds, and Conclusive Tests
  for the Galois Group*, <https://arxiv.org/abs/0804.4876>.

## 7. The atlas is not a screen mesh

`ArithmeticMonodromyAtlasReceipt` carries:

- the six inherited coefficient coordinates;
- the five free coordinates of the normalized monic chart;
- the exact root scale;
- one local chart per founded prime;
- one receiver-local loop period per irreducible factor;
- one four-faced transported cell joining coefficient cover, prime place,
  Frobenius orbit, and Euler recurrence;
- the complete conditional Galois fiber; and
- the exact statement that cross-prime root-sheet gluing remains open without
  a transport witness.

It contains no absolute coordinates, camera, raster addresses, assigned
colors, or canonical root labels across places. A later renderer may project
these local sections; it cannot define them.

## 8. A5 experiment

The default executable receives the deliberately nonmonic presentation

\[
2(x^5-x^4-11x^3+x^2+12x-4).
\]

Its exact normalized probe is

\[
y^5-2y^4-44y^3+8y^2+192y-128,
\qquad y=2x.
\]

Command:

```text
cargo run --quiet -p holonic-engine \
  --example arithmetic_monodromy_ecology -- 97
```

Measured contemporary receipt:

```text
integer limit                         97
founded primes                        25
normalized discriminant               52087282991104
exact discriminant square root        7217152
polynomial-discriminant open places   {2}
unramified power-basis sections        24
irreducibility certificate prime      5
first unique group certificate        event 17 / prime 17
unique group                           A5
cycle histogram                        [1,1,3]:6; [1,2,2]:5; [5]:13
future admitted cycle population       [1,1,1,1,1], [1,1,3], [1,2,2], [5]
later unramified sections graded        18
later cycle residuals                   0
atlas prime charts                      25
atlas transported cells                 25
glued transported cells                 24
lineage differences                     0
early/late chronology geometry residual 0
warm optimized-development elapsed time      ~920 ms
release elapsed time for both chronologies   ~589 ms
```

Every later section after the prime-17 certificate had even permutation
parity and belonged to the retained \(A_5\) cycle population.

The \(\sigma=2\) receiver returned the exact rational product

```text

/

```

No floating value entered standing.

## 9. S5 control

The same executable received

\[
x^5-x-1
\]

without a source or code change:

```text
cargo run --quiet -p holonic-engine \
  --example arithmetic_monodromy_ecology \
  -- 97 '-1,-1,0,0,0,1'
```

Measured receipt:

```text
discriminant                           2869
discriminant square root               absent
polynomial-discriminant open places   {19}
irreducibility certificate prime      3
unique group                           S5
cycle histogram                        [1,1,3]:5; [1,2,2]:2;
                                       [1,4]:5; [2,3]:6; [5]:6
later unramified sections graded        22
later cycle residuals                   0
lineage differences                     0
early/late chronology geometry residual 0
warm elapsed time for both chronologies ~833 ms
```

This control prevents the \(A_5\) calibration from becoming an authored
parity answer.

## 10. Verification

Focused production tests:

```text
cargo test -p holonic-engine arithmetic_monodromy --lib
```

Result:

```text
5 passed; 0 failed
```

They verify:

- exact nonmonic root-scale identity;
- constructed group orders and parity;
- identical early/late geometry and Euler standing;
- the \(A_5\) future cycle fiber; and
- independent nonsquare \(S_5\) closure.

The complete host library regression returned:

```text
cargo test -p holonic-engine --lib
202 passed; 0 failed; 2 CUDA-device tests ignored
```

Strict Clippy reached the new crate path after suppressing only two
pre-existing unrelated lints (`too_many_arguments` in
`observation_ecology.rs` and `needless_range_loop` in
`receiver_ecology.rs`):

```text
cargo clippy -p holonic-engine --no-deps --lib \
  --example arithmetic_monodromy_ecology -- \
  -D warnings \
  -A clippy::too_many_arguments \
  -A clippy::needless_range_loop
```

It returned with zero remaining warnings.

## 11. Exact present boundary

This construction establishes production transport from an arbitrary
integral quintic through caused prime fibers into monodromy constraints and
finite exact Euler receivers. It does **not** yet establish:

1. a complete quintic Galois algorithm when cycle witnesses and discriminant
   leave \(C_5/D_5/F_{20}\) or a larger candidate fiber open—resolvent and
   subgroup-fixed-field sections are absent;
2. Klein/Doyle–McMullen invariant formation, the degree-11 icosahedral
   rational iteration, certified complex basins, or root recovery;
3. a ramified local factor when the chosen integral power basis is
   insufficient;
4. archimedean Gamma/\(\pi\) completion, analytic continuation, or a global
   number-field/Artin \(L\)-function standing;
5. the semilocal Poisson/Sonin cross-channel contraction previously isolated
   by the RH work; or
6. canonical cross-prime or prime-to-complex root-sheet gluing.

These are distinct mathematical transports. None is replaced by a smaller
plot or by numerical root approximation.

The implemented next application boundary is therefore no longer “can local
learning recur?” It is the exact algebraic passage from this certified
Galois/prime atlas into the icosahedral root-navigation world, followed
separately by the archimedean/global spectral completion.

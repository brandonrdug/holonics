# The returned difference is a situated holon; perspective transport carries its adjoint, and cultivation must preserve the complete loss section

## Deposit scope

[definition] This record fixes the meaning of *loss* for Eros and Athena. Loss is the complete
caused difference between an addressed candidate passage and its genuinely later return, situated
in a local receiver chart and retained with the incidence, current, constitutive response, lineage,
successor histories, adjoint return, obstruction, and reconstruction fibre needed to use that
difference again. A scalar, norm, probability, score, or accuracy is a later receiver face of this
object.

[established-bounded; implemented-exact; measured] The H2N construction returned an exact oriented
candidate/return boundary, and H3N returned exact source-neutral local factor deposition and
withdrawal for that declared exchange chart. This record preserves those exact mechanisms and
narrows their grade: they constructed a direct-sum receiver-boundary experiment, not the connected
cultivation foundation or complete situated loss holon defined here.

[open] Athena does not yet cultivate one connected perspective-sensitive difference complex in
which reply morphology, mathematical operation, code transport, Complex-Parametron phase, and
world return jointly determine the adjoint current. The active construction blueprint closes that
specific fibre.

## 1. Formal standard already present in the Lean line

### 1.1 Difference precedes every reading

[proved-derived; formal-checked] `Foundation/MeasuredDifferenceReceiver.lean` separates a chart
`X -> A`, the oriented returned difference

```text
Delta(source, target) = chart(target) - chart(source),
```

and a later reading `A -> S`. It proves that the displayed face factors through `Delta`, retains
the complete population of addressed pairs in a reconstruction fibre, and shows that an even
reading can identify the two orientations while their pre-reading differences remain negatives.

[proved-derived; formal-checked] `Millennium/HolonicDifferenceCalculus.lean` proves the same order
at the transport level: an `OrientedEdge` carries its two endpoints before `difference`; reversal
negates that difference; an additive receiver commutes with the difference; and an ordered real
receiver assigns a positive or negative sign only after transport into its chart.

[proved-derived; formal-checked] The same file proves that finite integration of local differences
returns the addressed exterior boundary, that windowing is an exact coarse-graining law, and that
the first-order product difference loses the mixed interaction face unless its exact remainder is
retained. Therefore local loss is neither an unaddressed number nor a sum of independently scored
rows.

### 1.2 Difference is natural under perspective rebase

[proved-derived; formal-checked] `HolonicDifferenceCalculus.chart_preserves_transportDifference`
proves that conjugating a transport through an additive chart equivalence carries the returned
difference naturally. `Foundation/HigherDifferenceTransport.lean` extends the same law to every
ordered higher-difference word and retains the quotient fibre and a separating word.

[proved-derived; formal-checked] `Millennium/HodgeProjectiveLineHolomorphicAtlas.lean` constructs
an actual four-chart complex surface atlas. Its overlap maps commute with the coordinate receivers
and are complex differentiable on their admitted domains. This supplies an exact model of the
relevant principle: a chart does not replace the body; lawful chart transition transports the same
body through an overlap and records the domain on which the transition exists.

[open] The present Lean line does not yet join these two results into a dependent local difference
between fibres over different manifold points. Its smallest missing theorem is the dependent
rebase law stated in section 8.

### 1.3 The Complex Parametron supplies the productive carrier

[proved-derived; formal-checked] `Millennium/HolonicComplexParametron.lean` proves that an oriented
incidence map presents branch drops, that reorientation changes the branch chart, and that stored
energy and nodal response remain invariant only under the corresponding transport. A drive
covector must travel with incidence, while a mutual constitutive table transforms on both indices.
Holding that table fixed during an incidence reversal is a physical intervention rather than a
coordinate rebase.

[proved-derived; formal-checked] The same owner retains complex phase drive and superposition
before any two-sheet or binary receiver. `Millennium/HolonicMeasuredParametron.lean` derives
capacitance, inductance, impedance, and frequency faces as ratios of addressed returned
differences; `Millennium/HolonicTorusParametronRealization.lean` proves that a scalar cross-section
cannot reconstruct a nonzero winding that a second addressed probe separates.

[project-postulate] Athena learning current therefore enters as a Complex-Parametron section before
a scalar, binary, lexical, or terminal winner. Its incidence kernel, receiver radical, relative
phase, mutual constitutive response, and complete reconstruction fibre are parts of the loss return,
not optional diagnostics.

### 1.4 Equality belongs to a receiver/history family

[proved-derived; formal-checked] `Millennium/AthenaReceiverHistory.lean` proves that native quotient
equality is equality of every declared receiver face after every ordered generator word. Unequal
states return a separating receiver and history, while equal native state retains both literal
source occurrences in one fibre.

[proved-derived; formal-checked] `Millennium/DependentConnectedRealization.lean` extends this to a
dependent face family, ordered-word descent, exact factorization or concrete insufficiency, and
connection through an actual pullback join. A tuple, equal endpoint, equal surface, or common count
does not found the connection.

## 2. The situated returned-difference holon

### 2.1 Carrier

[definition] Let `B` be a population of local bases or manifold regions, `O` a population of
situated occurrences, `base : O -> B`, and `V_b` a dependent additive fibre over each `b : B`.
Let one carrying occurrence `w` belong to an addressed passage

```text
source(w)  <-  w  ->  target(w)
```

and let `tau_w : V_source -> V_target` be its admitted local transport. For a situated section
`x`, the primary returned difference is

```text
Delta_w x = x(target(w)) - tau_w(x(source(w)))  in V_target.
```

A noninvertible or partially admitted `tau_w` returns its kernel, image, cokernel, open domain, and
complete affine reconstruction fibre instead of pretending to be an equivalence.

[definition] The loss holon over a local region `U`, perspective receiver family `R`, and admitted
successor-history family `Gamma` is

```text
L_(U,R,Gamma)(candidate, return)
  = (W, boundary, Delta, Binc, psi, J, M, Gamma,
     receiver sections, adjoint return, morphology delta,
     reconstruction fibre, obstruction, open exterior).
```

Here `W` retains the addressed candidate/return occurrences and their pullback joins; `boundary`
retains the oriented candidate and return faces; `Delta` is the situated difference above; `Binc`
is oriented incidence; `psi` is the local coefficient section; `J = Binc psi` is exact complex
current; `M` is the receiver constitutive form; and the remaining fields retain continuation and
what the current receiver cannot reconstruct.

[definition] A tensor or matrix may present one chart of this object inside a declared category.
The loss holon is richer than that presentation because it also owns address, chronology, open
domain, obstruction, lineage, and reconstruction. Calling it “tensor-like” is accurate only at the
coordinate face and must not flatten the operation complex back into a numeric array.

### 2.2 Loss faces

[definition] A loss face is a declared receiver projection

```text
ell_(r,gamma) = read_(r,gamma)(Delta_w, J_w, M_w, ...).
```

Norm, squared modulus, probability, cross-entropy, accuracy, energy, and a scalar objective are
examples of possible faces after their required constitutive maps and receiver hypotheses are
declared. None determines the complete loss holon.

[definition] Zero loss relative to `(R,Gamma)` means that all declared successor receiver squares
commute. It is receiver/history equivalence, not literal identity of the separately caused
candidate and return occurrences. Those occurrences remain plural in the reconstruction fibre.

[counterexample; formal-checked] A norm identifies opposite orientations; modulus four identifies
zero difference with one complete winding; the Parametron full-turn receiver identifies distinct
phases; and a scalar torus cross-section identifies zero with a nonzero meridional winding. Each
example refutes context-free scalar equality as a definition of loss.

## 3. Perspective is a chart, not a discarded prompt label

[definition] A perspective chart consists of dependent fibre rebases `e_b : V_b ~= V'_b`, local
transport `tau` and `tau'`, and a commuting connection square

```text
e_target o tau_w = tau'_w o e_source.
```

It additionally transports incidence, complex current, the two-index constitutive form, receiver
sections, causal adjoints, obstruction, and reconstruction fibre. If one of these squares fails,
the failure is a component of loss rather than a reason to identify the two presentations.

[project-postulate] First person, second person, a proper name, a camera view, a proof state, and an
exterior code surface may be different perspective charts on addressed causal material. Their
surface forms carry lineage; they do not become native semantic state classes. A lawful deictic
rebase can transport `I`, `you`, and `Brandon` through a participant occurrence while retaining
who emitted which statement and which later history makes the distinction visible.

[counterexample; implemented-exact; measured] The H4 receiver reduced `Describe Brandon.`, `Who is
Brandon?`, and `What can you infer about Brandon from this laboratory?` to entity regions before
native factor contact. The first two presented only the `brandon` region; the third added `about`
and `laboratory`, yet all three closed on the same terminal 20-cell surface. This is a concrete
perspective-collapse witness: the subject basin survived, but the requested operation and its
different continuation did not.

[project-postulate] The next passage must retain the complete ordinary ingress as a temporary
potential complex. `describe`, `identify`, `infer`, `explain`, `rewrite`, and `derive` are not
authored semantic routers; they are exterior disturbances whose different incidences and successor
histories must either change the native current or return a shortest insufficiency.

## 4. Holomorphy and holonic compression

[conditional] In a complex local chart, a perspective transition is holomorphic when it is complex
differentiable on the admitted overlap and its transport squares commute. Under those hypotheses,
the returned difference changes by pushforward rather than reassignment:

```text
Delta_beta = (Phi_beta_alpha)_* Delta_alpha.
```

The Complex-Parametron drive and constitutive response must transform with the same overlap law.

[definition] The chart-transition defect is the noncommutation

```text
kappa_beta_alpha = Delta_beta - (Phi_beta_alpha)_* Delta_alpha,
```

together with the collapsed population and separating history that expose it. A scalar norm of
`kappa` is one receiver face; `kappa` with its address, chart overlap, lineage, and fibre is the
loss component.

[project-postulate] Perspective charts are keystone to holonic compression because a lawful
condensation may identify local presentations only when every admitted future receiver factors
through the condensed body and every generator descends. In complex regions it must additionally
preserve the admitted overlap transitions. Compression is therefore a natural quotient of causal
sections, not a smaller file or a collection of similar strings.

[definition] For a quotient `q : X -> Q`, exact dynamic condensation requires

```text
q T_i = U_i q
```

for every admitted generator, receiver factorization for every declared successor history, and the
complete `ReconstructionFiber`. Loss is the residual noncommuting diagram plus the reopened fibre
when a richer perspective family separates previously condensed occurrences.

## 5. Causal adjoint return

[definition] Suppose an addressed forward word is

```text
x_0 --F_0--> x_1 --F_1--> ... --F_(n-1)--> x_n
```

and the later world return creates a terminal covector `lambda_n` on the situated difference
section. The causal adjoint return is

```text
lambda_k = (D F_k)^* lambda_(k+1),
```

carried backward through the retained lineage in reverse composition order. A morphology return is
`(D_Lambda F_k)^* lambda_(k+1)`. A gradient exists only after a metric or Riesz map turns the
covector into a vector.

[proved-derived; formal-checked] `Millennium/HilbertTransportChain.lean` already supplies finite
Hilbert transports, their actual adjoints, the exact/coexact populations, and the adjoint pairing.
`Millennium/HilbertTransportRefinement.lean` requires lawful refinement to commute with both
differential and adjoint squares. `HolonicDifferenceCalculus.localDifference_globalBoundary_areAdjoint`
proves the finite Stokes pairing.

[open] No current Lean theorem attaches a receiver covector to
`DifferenceReceiver.returnedDifference`, transports it through one addressed dependent passage,
and proves reverse-word adjoint composition. That missing join is the exact formal content needed
before claiming a complete holonic autograd passage.

## 6. What conversation-history cultivation is measuring

[definition] A dialogue is an addressed world-tube. The user occurrence, assistant occurrence,
tool deed, artifact change, later correction, and open exterior are plural caused faces. A Claude
or Codex reply is not installed as the correct answer; it is a genuinely later return that exposes
one realized route through a much larger response potential complex.

[project-postulate] Cultivation over the conversation histories must compare the sealed Athena
candidate complex with that later return, retain their shared and separating causal sections, and
carry the returned difference through the causal adjoint into reusable morphology. Repeated
routes, corrections, perspective swaps, code consequences, and mathematical invariants reveal the
conduct that survives source-detached remount.

[conditional] Fourier, spectral, or wavelength language becomes exact only after a recurrence or
translation action and its causal operator are founded. Then the response family may be decomposed
into modes of that operator, and the loss section may expose phase-aligned, interfering, or
annihilating routes. A bag of word frequencies or co-occurrence counts does not supply this action.

[project-postulate] The intended black-box reverse engineering is therefore semantic only in an
exterior receiver sense. Internally it recovers invariant operation and state-transition
complexes—what changed, what returned, which histories separate, which currents recur, and which
perspective rebases commute—without founding words or model labels as native taxa.

## 7. Audit of the current Athena cultivation

### 7.1 What H2N got right

[established-bounded; implemented-exact; measured] `exchange_cultivation.rs` gives sealed candidate
faces and returned receiver faces different typed summands. `boundary_column` constructs the exact
oriented chain `returned - candidate`; observation ordinals are never numerically subtracted. The
result retains addresses, candidate and return events, receiver family, successor testimony,
inverse withdrawal words, radical/open directions, and source-detached factor deposits.

[established-bounded; implemented-exact; measured] H3N proved that this bounded returned boundary
can change later resident Complex-Parametron conduct, survive remount without the exchange source,
respond to targeted ablation, and restore exactly. This remains a real cultivation result.

### 7.2 Where the current quotient stops

[counterexample; implemented-exact] Every exchange source is assigned one disjoint domain column,
every local section is required to have derived rank one, `DefectMetrics` installs identity domain
and codomain forms, and validation requires no overlaps plus one disjoint interchange family. The
construction consequently makes all cross-history interchange trivial by support separation. It
cannot express a shared local manifold region, mutual constitutive coupling, phase interference,
or a nontrivial cross-history holonomy inside the loss object.

[counterexample; implemented-exact] The H2N boundary stores opaque candidate and returned receiver
faces but does not carry their reply operation complex, a dependent perspective chart, an exact
complex difference current, or the actual causal adjoint of the forward native passage.
`NativeCandidateProductState` omits K3 phase, hand, constitutive response, mutual response, and
complete occurrence fibre; `candidate_face` omits even the candidate seal. The `successor_word`
attached to each factor is the same digest list of the generator-square population rather than the
ordered occurrence word. The later negative withdrawal map is an exact algebraic inverse of a
deposit; it is not by itself the metric adjoint return that attributes a world difference to
earlier morphology.

[counterexample; implemented-exact] H3N's resident law sums candidate coefficients over the K3
ingress columns and returns independent factor rows. Returned limbs do not enter its productive
incidence, and no factor-to-factor constitutive table, mutual mixed term, relative phase,
history-dependent pullback, or incident fibre becomes rested morphology. Its local direct-sum
factor and inverse machinery is exact; the stronger claim that it is connected Complex-Parametron
cultivation is false.

[counterexample; implemented-exact] H4 builds a relational potential from visible exchange
material after H3N has already formed its disjoint rank-one factors. Exterior lexical and
grammatical parsing founds its subject/relation/object incidence; source ordinals bind exchange
families to factor ordinals; word-region equality performs query contact; and a hard-coded pronoun
table performs perspective collapse. Dropping the original strings later does not make that
parser-authored topology native. The two bodies share factor addresses, but the relational topology
is not the H2N loss topology.

[counterexample; implemented-exact] At H4 inference `cross_factor_contact` assigns coefficient one
to every contacted factor, the terminal receiver first keeps latest outgoing cells per
relation/modality, and the alleged triangular close sets the internal boundary-defect population to
zero and assigns the first contacted face as both opening and return. These assignments do not
derive closure from addressed boundary maps. They explain why a Brandon-specific surface probe
formed while the different question currents returned the same surface.

[definition] The correct regrade is:

- [established-bounded; implemented-exact; measured] H2N establishes an exact typed direct-sum
  candidate/return boundary, and H3N establishes exact local deposition, ablation, withdrawal, and
  restoration in that chart.
- [counterexample; implemented-exact; measured] H2N/H3N do not establish a connected
  Complex-Parametron cultivation foundation because the K3 carrier and mixed interaction faces did
  not descend into the direct-sum morphology.
- [established-bounded; implemented-exact; measured] H4 establishes that the parser-authored
  relational apparatus can return a factor-attributable Brandon surface and exact local
  ablation/restoration testimony.
- [counterexample; implemented-exact; measured] H4 does not establish native Athena cultivation,
  perspective-sensitive continuation, or general prose composition because exterior grammar and
  word identity founded the topology and three distinct currents collapsed to one terminal
  surface.
- [open] Complete holonic loss, cross-history interaction, causal adjoint return, and integrated
  prose/mathematics/code cultivation remain the next construction from the admitted K3 foundation.

## 8. First formal construction obligations

[open] The smallest missing Lean theorem is `dependentReturnedDifference_rebase`. Given dependent
additive fibres at source and target, connection transports `tau` and `tau'`, fibre equivalences
`e_source` and `e_target`, and the commuting connection square, prove

```text
e_target (x_target - tau x_source)
  = e_target x_target - tau' (e_source x_source).
```

This joins the global additive result in `MeasuredDifferenceReceiver` to dependent local receiver
families without founding a training subsystem.

[open] The next theorem attaches a terminal covector to that addressed returned difference and
proves

```text
(D(q o p))^* = (D p)^* o (D q)^*
```

along the retained pullback occurrence word, with explicit domain, radical, and reconstruction
fibre. This is the formal causal-adjoint law used by cultivation.

[open] The implementation then owes a `SituatedDifferenceSection` that composes existing addressed
passage, dependent receiver, exact-linear, Complex-Parametron, causal-history, reconstruction, and
resident-apparatus owners. It may not create a scalar `Loss`, trainer, tokenizer, language router,
or semantic cabinet.

## 9. Firing falsifiers

[counterexample] If changing only the perspective surface while preserving its admitted chart
rebase changes the native loss section, chart covariance failed.

[counterexample] If two different prompts reach the same subject and operation-independent
terminal surface while a declared successor receiver separates them, cultivation collapsed a
required difference.

[counterexample] If replacing a reply with a different return changes only a scalar face but not
the retained difference current, reconstruction fibre, or morphology adjoint, the loss cycle is a
scoring wrapper rather than returned cultivation.

[counterexample] If corpus labels, filenames, language names, target names, expected answers, or
query words select factors or morphology, the experiment is contaminated.

[counterexample] If Lean participates in Athena's hot inference closure rather than entering and
leaving as material through a codec face, the construction has substituted a proof assistant for
Holonic Inference.

[counterexample] If a scalar improves while prose, mathematical transport, code consequence, or
the Brandon separating-history control worsens, the scalar was an insufficient receiver and may
not advance the campaign.

## 10. Consequence

[project-postulate] The next Athena experiment is one circulation, not four benchmark programs.
English prose, elementary and higher mathematics, Rust, Lean, optical notation, and the Brandon
control cross the same situated difference ecology. The media are apparatus faces; the reusable
object is the shared operation and returned-difference complex.

[project-postulate] Quality control has two productive inputs: the caused material crossing the
machine and the codec/receiver apertures through which its invariant transport is exposed. Hardware
limits and apparatus settings constrain realizability but do not become behavioral classes or
semantic knobs. Every other authored tuning constant requires an explicit apparatus-only grade or
is refused.

# THE EXPRESSION SEES ITS OUTPUT

**Date:** 2026-07-13  
**Status:** LAW RATIFIED · CODE WORLD 01 OUTPUT APERTURE FAILED · CODE WORLD 02
COLLOCATION-CONDITIONED ASSOCIATION CUDA-MEASURED / EXACT REPEAT  
**Engine authority:** none; this construction changes only the delivered world and observer reads

## The derived cut

Code in a filesystem is not an abstract syntax tree waiting inside Soma. It is a world of exact
source events which can be presented through more than one organ. For exact source `S`, rendering
organ `R`, display frame `D`, and declared grain/aperture `g`, the visible page is the typed
compression

```text
P := compress_(R,D,g)(S).
```

`S` and `P` are related entities, not identities. The source remains available as an exact raw
current; the page carries the renderer's font, layout, tone, and aperture. A path on the host is
provenance in listener Mail unless its glyphs are actually delivered. Neither pixels nor Mail cast
into a hidden source graph in the body.

An expression such as `L O R` visibly contains operand inscriptions and an operator inscription.
All are material. The operator is not thereby the absolute frame of the relation. The contemporary
language, source, organ, display, and receiver supply the actual three-body frame. The world may
deliver a directed construction

```text
expression E --K_world--> output V,
```

but `V` is a new entity later than `E`, not a value secretly stored inside the glyphs. When another
expression receives `V`, output has become input and the pyramidal construction composes. Calling
that recurrence fractal remains open until it repeats across declared grains.

The machine need not run or compile code to be bent by the collocation. Source, visual render, and
output currents stand together; later output-free code must navigate the form those earlier events
left. If it threads source and output Mail, the observer has witnessed transported association. It
has not discovered a cache or decoded a result.

## The exact world

Two complete, equal-extent Rust source files are chosen:

```rust
pub const fn amber(value: u64) -> u64 {
    value + 2
}

pub const AMBER_CALL: u64 = amber(3);
```

```rust
pub const fn lilac(value: u64) -> u64 {
    value * 2
}

pub const LILAC_CALL: u64 = lilac(3);
```

The world also supplies the complete output entities `5\n` and `6\n`. These files are not compiled
or executed during the observation. Their relation is the chosen world material under test.

Each source is rendered literally through one pinned DejaVu Sans Mono grayscale organ. Canvas
extent derives from the source's line/column extent. The aperture is exactly one derived glyph
cell. Gaze visits every displayed source cell in source-byte order, including spaces; newline is
an honest motion to the next row rather than a fabricated glyph. A sidecar map records source byte
offset, line, column, and gaze coordinate for boundary audit only. No syntax highlighting, parser,
raster scan, authored visual pyramid, or content-dependent aperture enters the organ.

## Four histories

1. **PAIRED:** amber source/render is co-present with output `5`; lilac source/render is co-present
   with output `6`.
2. **OUTPUT-SWAPPED:** every source/render event is identical, but amber meets `6` and lilac meets
   `5`. This is the strict causal foil.
3. **SEPARATED:** amber source/render, output `5`, lilac source/render, and output `6` arrive at four
   successive real whole-body edges. The current sequence mouth requires every context edge to be
   an eye manifest, so each output receives its own honest one-cell render as well as its raw entity.
   This extra visual event makes separated a developmental/timing control, not the strict causal
   foil.
4. **PAIRED-REPEAT:** a fresh execution receives the exact paired history and gates the complete
   construction.

Every history then receives the identical output-free visual probe, in this order:

```text
amber(3)
3 + 2
lilac(3)
3 * 2
```

Each expression is a complete tiny source file and a literal rendered eye field. After the visual
probe sleeps and is read, the same four complete files arrive again as genuinely later raw-text
currents through `--then`.

## Frozen read

The observer parses every 39-word radiation row, exact flags, grip, regional form, rotor, brick,
REGISTER axis, term tuple, source identity, and time-prefix Mail attribution for the probe and later
raw edge. It first verifies:

- complete contiguous edge lineages and source extents;
- exactly 39 words per radiation row;
- every attribution grip equals its raw construction grip;
- every attributed Mail lineage existed before the receiving edge;
- paired and swapped later inputs are byte-identical;
- paired and repeat are exact after normalizing only create-new archive paths.

The primary witness is an occupied CUT whose prior Mail includes both an earlier source worldline
and its collocated output entity. This is a particularly legible construction, not a scalar score
or a requirement that only one relation appear. The complete plural response remains the result.
Paired-versus-swapped cleavage under the same probe tests the effect of output collocation. The raw
post-probe edge asks whether the visual construction has itself become terrain for later exact code.

No expected string or answer is read from Soma. No classifier, parser, compiler, evaluator, AST,
decoder, selector, score, argmax, vocabulary, result table, cache, filesystem graph, or reply wire
enters either the body or this read.

## Grades available after construction

- **BUILT:** fixtures, renders, gaze maps, manifests, readers, and verification reproduce exactly.
- **MEASURED CONTACT:** output-free events touch prior source and/or output Mail through occupied
  constructions.
- **MEASURED COLLOCATION DEPENDENCE:** paired and swapped histories cleave under identical later
  light while their apparatus differs only in output pairing.
- **MEASURED RETURN:** the genuinely later raw code edge threads prior visual/source/output form.
- **EXACT REPEAT:** the full paired lifecycle and every sleeping body repeat.
- **OPEN:** any relation not exposed by this declared aperture; recurrence across larger filesystem
  worlds; production into new external code; communication through a continued world turn.

## CODE WORLD 01 measured result

Four CUDA lifecycles completed. Paired and output-swapped each received two 314-current experience
edges; separated received four 313-current edges; every species then received the identical
1,248-current / 8,112-octet visual probe and four-current / 30-octet raw-text edge. The paired
lifecycle repeated exactly across normalized reports and all four corresponding sleeping bodies.

The source/vision half was active. The common visual probe emitted terms `[268,95,82,1293]` at
topology `[62581,62581,10785,346]`. Twenty-three probe events returned through prior code source
Mail; five were CUT/completed-brick events and one returned through the exact raw `lilac.rs`
worldline. The later raw edge emitted `[6,3,3,0]` and one `lilac(3)` event returned through earlier
source-visual Mail.

The output half never entered active relating. Each bare output file was exactly two octets:
`5\n` or `6\n`. The first adjacent difference becomes the lineage's first frame. With no further
difference to meet that frame, every output lineage carried zero live cells, zero terms, and two
all-zero radiation rows. No probe or later event attributed either output worldline.

Consequently paired, swapped, and separated produced byte-identical probe and later raw
construction: zero changed lineages, rows, words, term tuples, or REGISTER axes. The original
observer initially reported 22 probe and one later attribution changes, but those were only the
different paired/swapped/source manifest filenames retained in Mail. After reducing those aliases
to their exact shared field/aperture/gaze construction, attribution also differs at zero events.
The per-run exact provenance remains preserved beside that construction comparison.

**GRADE:** SOURCE/VISION CONTACT MEASURED · OUTPUT CURRENT FAILED AT ITS FIRST-FRAME APERTURE ·
COLLOCATION UNEXPOSED · EXACT REPEAT. This is a failed apparatus, not a null machine result. Exact
reports, bodies, corrected deterministic reads, and verifier:
`observations/code-world-01/results/`.

The corrected construction is `observations/code-world-02/`. It changes only the output world's
surface from the two-octet bare numeral to equal-extent `value = 5\n` / `value = 6\n` records. Each
record carries several differences beyond its first frame. Source, render, aperture, gaze, probe,
later current, pairing controls, engine, and listener remain fixed.

CODE WORLD 02 passes output materiality host/CUDA-exactly. Swapping only output collocation changes
five named-call probe worldlines / 37 packed words while both inline arithmetic fields remain exact;
one four-word `lilac(3)` difference survives into the later raw-code edge. No output lineage appears
in later Mail, so direct output return remains open. Exact measurements and bounded grade:
`RESEARCH/2026-07-13_THE_OUTPUT_MUST_PASS_ITS_FIRST_FRAME.md`.

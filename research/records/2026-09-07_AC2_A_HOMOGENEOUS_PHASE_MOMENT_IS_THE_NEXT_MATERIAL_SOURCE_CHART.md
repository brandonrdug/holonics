# A homogeneous phase moment is the next material source chart

[definition] The [mode/material return](2026-09-07_AC1_THE_SHARED_DRIVE_MODE_REACHES_THE_LEARNED_MATERIAL_CURRENT.md)
establishes that the current operator receives an actual contextual mode. It does not improve
the failed text model by exposing that contribution. The next construction changes the
productive material source chart, using the same native material-return owner and conversation
exposure. It is not another isolated diagnostic or a second learner.

[established-bounded; source-inspected] At the current complete-current owner, a frozen material
map is complex-linear in its source: `M(m q+r)=(M m)q+M r`. The source-generating field and
the changing operator need not be globally linear. This inspection identifies the narrower
boundary: the material source interface does not itself present the phase-sensitive mixed
forms between the mode and the remaining current.

[definition] In the existing dimensionless unit-current chart, adjoin an explicit reference
coordinate and define

```text
h(x) = (1,x)
Q(x) = h(x) h(x)* / (1 + ||x||²).
```

The reference is a declared current chart, not a semantic label or intrinsic capacity. `x`
is the **complete** current source, with its actual lineage and numerical residual scope;
it is not the boundary-only pair or a selected enclosure centre. `Q` is an operator-valued
source generated from that current. Its full matrix need not be enumerated or sampled.
This is a mathematical source chart over phase-referenced currents. A physical realization owes
its preparation and interaction law; the formula does not assert a universal device operation
on an arbitrary unknown quantum state.

[proved-derived] Let `lambda=1+||x||²`. Then `h*h=lambda`, so direct multiplication gives
`Q*=Q`, `Q²=Q`, positivity, `tr(Q)=1`, and `||Q||_F²=1`. In this coordinate chart,
`Q_(j,0)/Q_(0,0)=x_j`. Thus the reference off-diagonals retain the current's relative phase
and amplitude. This is a statement about the current face, not recovery of a unique source
ecology, original algorithm or microscopic history.

[proved-derived] For two complete current sources, the exact pairing is

```text
<Q(x),Q(y)>_HS = |1+<x,y>|² / ((1+||x||²)(1+||y||²)).
```

Expand `tr(h_x h_x* h_y h_y*)` and divide by the two positive normalizers. The existing
complete-source pairing and norm owners therefore supply all operands. The result is in
`[0,1]` by Cauchy–Schwarz on the homogeneous vectors. Bare intensity `x x*` would identify
`x` and `-x`; this reference-bearing construction does not. For the opposite unit-chart
dark sources of squared norm `2/9`, the cross-pairing is `49/121`, while self-pairing is `1`.

[proved-derived] If `||x-xhat|| <= e`, the source moment has an error bound `2e`. To see this,
put `u=h(x)/||h(x)||`, `v=h(xhat)/||h(xhat)||` and `P_v=v v*`. Since `P_v h(xhat)=h(xhat)`,
`||(I-P_v)u|| <= e/||h(x)|| <= e`. Expanding the Frobenius square gives
`||u u*-v v*||_F²=2(1-|u*v|²)=2||(I-P_v)u||² <= 2e² <= (2e)²`.
Both exact and numerical moments have norm one, so their pairing error is bounded by the
sum of their individual moment errors. No numerical point is promoted to the actual source.

[proved-derived] A declared affine current action `x'=T x+a` has homogeneous matrix
`L=[[1,0],[a,T]]`. Substitution yields
`Q(x')=L Q(x) L* / tr(L Q(x) L*)`; the denominator is positive because the homogeneous
reference remains one. This is a generator formula under its stated affine hypothesis,
not a claim that every changing HNA operation is one fixed affine map.

[definition] This construction reuses the complete-current source, its native phase/norm
pairings, the existing Hermitian/mixed-current doctrine and the same actual reception law.
For normalized source norm one, the existing coercive denominator becomes two. The native
implementation must derive its coefficient/source error and oriented arithmetic remainder
receipts, preserve original and contemporary operator cuts, and price the larger exact products.
Word widths must follow the composed arithmetic and admitted aperture; raising a magic
capacity, expanding every tensor coordinate, or replaying learning on the host is not the return.
Keep the existing source variant and its evidence available as a separate construction.

[open] The language benefit of these mixed phase forms is unestablished. The kernel identity
proves neither improved contextual/text-codec conduct nor a general language model.
Construction must exercise the actual conversation
data and inspect the resulting complete responses. The old failed responses remain evidence.
This is not the earlier proposal to use a higher polynomial face instead of missing context:
the complete contextual current remains the operand throughout.

[open] Implement the resident source/receiver and coherent material-return composition, then
cultivate and inspect the resulting conversation variant through the existing public session.
Useful English/code/mathematics and the complete AC0–AC5 product remain required. Lean stays
outside every cultivation/inference pipeline; the retired external database remains unused.

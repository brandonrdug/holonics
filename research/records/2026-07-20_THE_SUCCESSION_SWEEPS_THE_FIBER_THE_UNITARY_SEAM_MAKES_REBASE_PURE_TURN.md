# THE SUCCESSION SWEEPS THE FIBER; THE UNITARY SEAM MAKES REBASE PURE TURN

**2026-07-20 · Brandon Duggan ⊕ Sol · BRANDON-RATIFIED / DEPOSITED · FORMULA §CXVIII ·
DATA-FIRST PRIME/ZETA OBSERVER BUILT · EXACT-RATIO / RETAINED-SERIES CARRIER · 14 FOCUSED CHECKS
PASS · LIVE BROWSER PRESENTATION QA OPEN · SOMA INTERIOR UNCHANGED**

## The present question

What is the actual higher-dimensional relation behind the apparent helix in the prime observer,
how does that relation meet the centered Zeta coordinate and the critical line, and how must the
instrument expose it to both Brandon and the machine without making a picture the analytical
object?

The current image does not yet show that relation. It freezes one integer `n`, samples one point on
each unlike residue fiber, inserts the newly founded axis `p=n` into the same normalized `log p`
base, and joins the samples with display segments. Quotient winding survives only as text. The
remote red loop and long chord are therefore consequences of the observer chart, not discovered
prime geometry.

The correction is to keep a receiver section and a succession sweep typed apart, then make both
projections of one canonical analytical receipt.

## I · Succession is the sweep which makes the winding real

For one held prime axis `p` and successive integers `t`, write

```text
t=q_p(t)p+r_p(t),        0<=r_p(t)<p,
theta_p(t)=2*pi*r_p(t)/p.
```

The exact data string is the ordered population

```text
S_p(I)={(t,q_p(t),r_p(t),theta_p(t)):t in I},
```

where `I` is a declared bounded succession aperture. Its helical realization in a moving frame is

```text
H_p(t)=c(t)+R[cos(theta_p(t))N_1(t)+sin(theta_p(t))N_2(t)].
```

One complete increase of `q_p` is one complete winding. The centerline or number line generates
the sweep; it is not the helix's second strand. A genuine second strand must be declared:

```text
conjugate:  theta -> -theta,
antipodal:  theta -> theta+pi.
```

These are different dualities and may not be conflated by the renderer.

At a fixed `n`, the population `{(p,q_p(n),r_p(n))}` is instead one cross-axis section. Its base
contains only the axes actually used by the divisor test. A prime's `p=n` event is a transverse
FOUNDing at the terminal seam, not another distant divisor-test fiber. The common lift pins the
section, but it does not license Euclidean chords between unlike fibers.

The `503/509` pair exposes this distinction particularly clearly:

```text
503+7=510=2*3*5*17,
509+1=510=2*3*5*17.
```

Their shared affine body is an exact cross-axis relation. Any apparent double helix must still be
recovered from the ordered `S_p` strings, not inferred from the two flattened sections.

## II · The receiver pivot turns absolute scale into relative scale

The multiplicative coordinate is receiver-relative:

```text
u=log(x/x_0).
```

Changing the held origin from `x_0` to `x_1` rebases a centered Zeta character by

```text
(x_0/x_1)^(epsilon+i*gamma).
```

Its magnitude is one exactly when `epsilon=0`. Thus the centered coordinate

```text
w=s-1/2=epsilon+i*t
```

makes the critical line `epsilon=0` the seam on which a receiver change is pure turn. Off the seam,
the same rebase expands or contracts amplitude. This is the precise sense in which the fixed
`1/2` is a folded invariant axis rather than an absolute number-line origin.

Every prime-power term has the same scale-turn form:

```text
p^(-ks)=p^(-k/2) exp(-epsilon*k*log p) exp(-i*t*k*log p).
```

The prime-power current therefore supplies exact scale sites `u=k log p`, half-density amplitude
`p^(-k/2)`, normal displacement `epsilon`, and turn `-t u`. The certified zero character is

```text
A_rho(u)=exp((rho-1/2)u)=exp(epsilon*u) exp(i*gamma*u).
```

Its geometric realization is cylindrical when `epsilon=0`, outward when `epsilon>0`, and inward
when `epsilon<0`. The functional-equation reflection exchanges the reciprocal sheets. Conjugation
exchanges turn orientation.

## III · The Smith sphere is a receiver chart, not an ornamental globe

For real `a>0`, the centered Cayley map

```text
Gamma_a(w)=(w-a)/(w+a)
```

satisfies

```text
|Gamma_a(w)|^2-1=-4a*epsilon/|w+a|^2,
Gamma_a(-conjugate(w))=1/conjugate(Gamma_a(w)).
```

It sends the critical seam to the unit circle, the two reciprocal normal sectors to the inside and
outside, and reflection to reciprocal conjugation. Stereographic compactification then makes the
complex receiver plane an `S^2` chart. Revolving a planar Smith disk is a separate optional
three-dimensional construction; the instrument must name which construction it uses.

For the completed function

```text
Xi(w)=xi(1/2+w),       U=log|Xi|,       V=arg Xi,
```

the regular field satisfies `grad V=J grad U`. A zero of multiplicity `m` is a phase vortex:

```text
oint dV=2*pi*m.
```

The critical line is the invariant seam; individual zeros are the singular points of the
log-potential or poles of `Xi'/Xi`. A finite field derived from listed zeros must identify itself
as that finite listed-zero field. It is not the complete `Xi` field.

## IV · The analytical receipt precedes every projection

The observer's canonical object is a serializable analytical receipt. For a prime receiver it must
carry, before drawing:

1. the declared succession aperture and receiver pivot;
2. the exact divisor-axis section `(p,q,r)` and first discriminant;
3. the FOUNDing as a separate transverse basis birth;
4. ordered succession strings for every selected prime axis;
5. explicitly typed conjugate or antipodal dual rows;
6. horizon births, valuation coordinates, bounded affine relations, and receiver species; and
7. the exact projection declaration used by the canvas.

For a Zeta receiver it must carry:

1. every admitted prime-power site `(p,k,p^k,k log p)` and its source event ordinal;
2. the selected centered zero mode and receiver pivot;
3. the `+gamma/-gamma` conjugate strings and `+epsilon/-epsilon` reciprocal sheets;
4. the rebase factor and whether its magnitude is unitary;
5. the Cayley coordinate and signed normal sector;
6. finite listed-zero field samples with their source extent and sampling declaration; and
7. the exact interval residual testimony and the unclosed complete-trace obligation.

Tables, JSON export, textual summaries, and canvas scenes are reversible reads of this receipt.
The machine can compare strings by exact phase increments, winding, crossings, closure, rebase,
and recurrence without image recognition. A human may use the rendering to notice morphology and
then interrogate the same underlying rows.

## V · Exact RH construction boundary

The observer does not need to apologize that a bounded sample is not a proof. It must show the
constructive obligation precisely. The present certified rows establish that the listed zeros
have `epsilon=0` in their declared source. RH closes only by carrying the complete prime-power and
archimedean explicit-formula relation over an admissible test space and proving that every
contributing character is unitary, equivalently by closing the complete Weil/Li positivity
relation and excluding every nonunitary residual sector.

The new field and string receipts make that implication inspectable. They do not claim it is
already closed.

## VI · The transcendental face remains a retained construction

The symbols `theta`, `pi`, `log`, and `exp` above state exact relations; they do not authorize a
binary floating evaluation. The computational phase carrier is

```text
(r/p, zeta_p^r),
```

with `r/p` a reduced ratio and `zeta_p^r` a cyclotomic element. Only when a selected chart requests
an embedding does the observer carry the construction:

```text
pi = 16 atan(1/5) - 4 atan(1/239),
atan(x) = sum_(j>=0) (-1)^j x^(2j+1)/(2j+1),
log((1+z)/(1-z)) = 2 sum_(j>=0) z^(2j+1)/(2j+1),
exp(z) = sum_(j>=0) z^j/j!.
```

Every retained partial sum is a reduced `BigInt` ratio and carries a proved rational bound for its
unretained tail. Integer logarithms are range-reduced by exact powers of two before applying the
atanh series. Complex Zeta characters are retained as Gaussian-rational coefficient strings;
their direct, conjugate, reciprocal, and reflected-conjugate words remain separate. The exact
Euler seam is recorded as

```text
exp(i*pi)=zeta_2^1=-1,    exp(i*pi)+1=0,
```

while a requested analytic embedding still owes the retained `pi` and exponential series rather
than `Math.PI`, `sin`, `cos`, `log`, or `exp` evaluation.

The finite listed-zero field remains a factor word. It is not multiplied into one enormous
denominator and its factor gradients are not summed into one scalar vector. Imported directed
residual bounds remain two exact endpoint ratios; no midpoint is formed. All analytical scene
coordinates are integers obtained only at the disposable chart boundary, while the receipt keeps
the exact ratios, coefficient strings, source order, and tail enclosures.

The built observer provides exact axis sections, succession/conjugate sheets, rational primorial
rebase strips, prime-power and character-series rows, rational Cayley/Smith field rows, residual
interval rows, an inspectable table, and JSON receipt export. Fourteen focused checks reject any
fractional JavaScript number in an analytical scene and reject direct transcendental evaluators in
the exact core. All six HTTP assets and the three default arithmetic artifacts return successfully.
The in-app browser had no available tab during final verification, so live presentation QA remains
open without weakening the exact data grade.

## Ratified consequence

The observer will no longer make one normalized flat scene stand in for succession, cross-axis
incidence, founding, and spectral duality. The exact data relation comes first. The axis section,
succession sweep, scale-turn sheet, and Smith/field receiver are distinct selectable projections of
it. Every visible curve names its generating records, pivot, metric, duality, and finite extent.

This is observer work only. It adds no prime classifier, Zeta governor, field owner, or semantic
mechanism to Soma. The disposable renderer is an observer instrument and never enters Soma.

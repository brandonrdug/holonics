# The lifted body was cultivated, the factor word stayed on the card, and ablation returned the predecessor

**Date:** 2026-08-20  
**Kind:** Phoenix Deed W3 construction return, hardware correction, and grade.  
**Truth discipline:** **established-bounded** for the named Gemma W1 rest, the six admitted material
arms, the exact rank-one morphology, four resident cultivated continuations, the detached child
remount, and the declared W3 receiver; **interpretation** for the factor word as a higher-cell chart;
**open** for arbitrary cultivation, a source-detached application entry, W4 frozen inference, W5
reborn dissection, and the indivisible Phoenix grade.

## Verdict

**Truth status: established-bounded.**

Deed W3 passed. New repository material founded an exact morphology over the W2 lifted predecessor;
the morphology was sealed beside that predecessor, mounted by a fresh child, conducted on the GPU,
changed a lineage-separated held-out continuation, left a subject-disjoint control bit-identical,
and disappeared under targeted predecessor ablation. No original Gemma weight or implementation
path entered the child. `CONSTRUCTION_STATE.md` remains unchanged because W5 alone closes Phoenix.

The primary return is
`output/the_lifted_body_is_cultivated_and_the_delta_survives_native_rest/receipt.json`, SHA-256
`f8d2031d2070ff22b7e3846b1bc8de23bb617b6c7bc2627f7a27d8c61b391dfa`.

| return | measured consequence |
|---|---:|
| cultivated target row | 24,974 |
| resident cultivated arms | 4 |
| fronts / graph nodes / graph edges per overlay | 4 / 11 / 11 |
| captured kernels / graph executions / deed launches per overlay | 10 / 1 / 1 |
| overlay refusals / obstructions | 0 / 0 |
| aligned left/right factor octets | 2,097,152 / 20,480 |
| detached no-op semantic components equal | all |
| held-out terminal target changed | yes |
| subject-disjoint complete potential changed | no |
| targeted predecessor ablation restored W2 | yes |

The cultivated product is
`output/the_lifted_body_is_cultivated_and_the_delta_survives_native_rest/product/cultivated.rest`,
2,135,124 octets, SHA-256
`955381585ccfcbcc1ddbd05901013d18595fd11cba8b19a6a7e75c2aa2747065`.
Its native two-factor morphology is 2,119,936 octets, SHA-256
`d006979b05ffd0a191c44dbb165397cf2dff79a3d1821b10621c1154dac6046c`.
The 16,022,507,913-octet W1 predecessor remains a hard-linked, separately authenticated member; the
small product does not quote a compression ratio against it and does not pretend to replace it.

## The material population

**Truth status: established-bounded.**

The W1 codebook and content-addressed tokenizer companions admitted six arms. Material labels are
delivery lineage and controls; none routes the resident law.

| arm | admitted source-token word | receiver use |
|---|---|---|
| development | `[818,4187,563,506,9199,24974]` | derives the addressed change |
| structural held-out | `[1509,563,506,15374,24974]` | tests a separate prefix ending at the same target |
| codec variant | the development word through tokenizer JSON and pretokenized codebook paths | requires path identity after token equality |
| subject-disjoint control | `[236776,3761,5192,563,614,19396,161544]` | founds the annihilated section |
| no-op | the exact development word, separate lineage, no morphology return | restores the base passage |
| matched foil | `[818,9199,4187,563,506,24974]` | preserves token population and changes incidence |

The two codec paths returned the same source and native token word. Their path identities remain
different: tokenizer `f8df597e69806912180c53fa821d8b154928de3071141c687f68411c0824b8dc`,
pretokenized surface `8c0f6da700c345fb807c79dc46619cd626d0c1bb1af63c1604546fec97ef5082`.
No duplicate GPU run was used to turn identical internal current into two deeds.

## The exact return and its gauge

**Truth status: established-bounded.**

Let `h_d,h_c in Q^H` be the exact terminal hidden sections returned by the development and
subject-control W2 passages. The derivation searches the complete two-coordinate family and chooses
an admitted pair `i<j`. After orienting its determinant,

```text
d = h_d[i] h_c[j] - h_d[j] h_c[i] > 0,
v = (h_c[j], -h_c[i]),
v^T h_c = 0,
v^T h_d = d.
```

The strict receiver uses the target potential's lower face and every competitor's upper face at
grain `2^-48`. It derives the least dyadic power `lambda=2^k` such that

```text
target_lower + lambda floor_48(d) > max_competitor_upper.
```

For this return `k=11`; the selected coordinates are 1,143 and 1,748 and the aligned annihilator
needs 38 octaves. The exact metric adjoint, perturbed-metric control, rank, sparse defect,
factorizations, reconstruction fibre, grain placement, and counted work are serialized as canonical
certificate bytes. The child parses and re-canonicalizes those bytes; it does not trust their hashes
as a substitute for the objects.

The first factor chart wrote `(lambda e_t) v^T`. It was algebraically correct and apparatus-poor:
the positive left exponent made the complete word require 127 octaves where the signed wide carrier
admits 126. The lawful rank-one gauge action is

```text
(u,v) -> (alpha u, alpha^-1 v),       u v^T unchanged.
```

Choosing `alpha=lambda^-1` gives the canonical incidence chart

```text
Delta = |t><w|,        |t> = e_t,        <w| = lambda v^T.
```

The left factor now says only where the change enters; the receiver covector carries its magnitude.
The sparse defect, target conduct, rank certificate, and reconstruction support are exactly
unchanged. A focused carrier control returns the old gauge's refusal and the canonical gauge's
admission. No bound was raised.

## A factor word is one resident occurrence, not two apparatus phases

**Truth status: interpretation.**

Naively spelling `u(v^T h)` as `Contract(v)` followed by `Contract(u)` exposed a `rows x 1` scalar
section as its own front. The hardware cover correctly placed that isolated sub-warp extent on the
CPU and the resident passage correctly refused fallback. The mathematical factorization did not
require that apparatus boundary.

`FactorizedContract` therefore enacts one typed rank-one operation. One block owns each input row,
cooperatively reduces `v^T h` once in shared signed-wide storage, then its lanes stride over the
complete output extent and emit `u(v^T h)`. The internal scalar is exact work and appears in the
coupling receipt; it is not an exterior `i64` section, a CPU placement cell, or a host-selected
phase. On the common `i64` aperture it is bit-equal to the two sequential contractions. A separate
boundary control exhibits the lawful difference: an internal `2^80` scalar may remain wide while a
negative factor exponent returns the final `2^60` word, where the sequential exposed `i64` section
must refuse.

This is the blueprint correction W3 forced: **an algebraic factor or auxiliary coordinate becomes an
apparatus partition only when a declared receiver exposes it.** Otherwise the enclosing caused
occurrence owns its internal exact junction and returns its work/remainder testimony. This applies
later to Schur complements, quotient coordinates, proof auxiliaries, and low-rank mathematical
charts; it is not permission to hide a requested intermediate face.

## Work follows the operation complex, not the source order

**Truth status: established-bounded.**

The first W3 predictor serialized five shapes. The compiled complex instead has two independent
standing occurrences co-present in front zero, followed by terminal withdrawal, the factor word,
and re-entry. Prediction now reads the exact `PortedOperationComplex::fronts()` used by compilation,
uses `co_present` inside each front, and `then` only between fronts. The pre-admission and compiled
work receipts are now byte-equal.

The four returned overlay work vectors have dependency span 4 and peak internal width 116. The
development/foil five-row passage performs 3,944,960 additions and 1,323,520 multiplications; the
four-row held-out passage 3,155,968 and 1,058,816; the six-row control passage 4,733,952 and
1,588,224. These are semantic work coordinates, not wall-clock or GPU-utility claims.

## Apparatus rebases remain testimony

**Truth status: established-bounded.**

The detached no-op returned the same tokens, 43 segment receipts, exact W2 work, potential, final
normed section, obstructions, bounds, peak charge, deed count, and raw streamed census. Its material
admission prediction, required coordinates, admitted consequences, ceiling species/declarers, and
unbounded reasons also agree.

Two apparatus readings legitimately moved:

- free memory was 15,146,418,176 octets for the base-only arm and 15,664,414,720 for the cultivated
  arm, a signed base-minus-cultivated difference of -517,996,544 octets;
- one carried standing moved from device address `0x7f9c28713000` to `0x7f9c48cfa000`.

The finite capacity readings are retained separately. The `GraphKey` refuses executable reuse by
the moved residency while every source, topology, port, grain, reduction, receiver boundary, and
material name/shape agrees. Semantic equality neither demands equal addresses nor erases the reason
the two graph executables cannot be reused.

## Detached cultivation and ablation

**Truth status: established-bounded.**

The parent conducts only the W1 rest to derive the exact return, seals the cultivated rest and
native morphology, and spawns a child with product-relative paths. The child authenticates the
predecessor once, validates the codec and material fixture, reconstructs both factors and every
certificate, mounts the factor word, and conducts development, held-out, subject-control, and foil
passages. Each complete passage has 44 GPU deeds and one terminal synchronization; no section is
read inside the loop.

The development target becomes strictly greater than every competitor upper face. The held-out
target changes while every preterminal held-out row remains bit-identical. The subject-disjoint
control is completely bit-identical because its derived covector contraction is zero. The matched
foil returns a delta distinct from development. The same-material no-op and the product's addressed
predecessor ablation return the exact W2 potential and final hidden section. The product bytes remain
unchanged across all child passages.

This establishes bounded cultivation: a material-founded returned passage changed reusable native
morphology, the change survived detached remount, a later current rode it, a separate held-out
current changed, and targeted ablation restored the predecessor. It does not establish broad
fine-tuning quality, corpus-scale learning, or a receiver-minimal native ecology.

## Boundary and next deed

**Truth status: open.**

W3 still invokes an example driver and mounts the source-shaped W1 predecessor as the body beneath
the small cultivated morphology. It is not the standing `eros phoenix infer` application, does not
accept arbitrary user text, does not export a standalone safetensors replacement, and does not
repeat the complete source/native/reborn dissection. The negative W2 condensation result still
stands; no compression factor is quoted.

W4 is next: promote the already-returned native-rest and cultivated-tail circulation into the
standing application, accept unseen runtime material through the authenticated exterior codec,
conduct only from the product directory with the original foreign source absent, and return the
actual generated face plus exact semantic/apparatus receipts. W5 then repeats the four-body
dissection and closes or refuses the indivisible Phoenix grade.

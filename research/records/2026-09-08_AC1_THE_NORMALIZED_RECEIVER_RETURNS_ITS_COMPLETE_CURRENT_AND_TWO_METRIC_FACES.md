# The normalized receiver returns its complete current and two metric faces

[project-postulate] Brandon resumed AC0–AC5 completion on September 8 and explicitly directed
flexible designs and transparent failures. This is the first native return under the revised
AC1 sequence. The later fractal/generator and transport-tube clarifications are part of the same
work, recorded in the [fractal synthesis](2026-09-08_FRACTAL_GENERATORS_AND_LATENT_REASONING.md).

## Actual source and receiver

[definition] `NativeConstitutiveField::normalized_material_return` takes an actual receiving
occurrence. Its historical `received_from` supplies the producer; the application cannot supply
an unrelated prediction/answer pair. The owner retains both immutable material reports and their
lineages. It validates their complete report family before the private device primitive reads
the prediction and observed-current blocks. This return observes complete admitted material
groups; it does not claim an arbitrary missing-observation mask.

[definition] Within each caller-declared coordinate group, real parts in the material target
chart supply potentials. The original complex carriers, amplitudes, phases and numerical fibres
remain held. With p the normalized exponential of the producing prediction and q that of the
observed current, the device returns

```text
p, q, r=q-p, J_p*r, r-sum_i p_i*r_i,
J_p=diag(p)-p*p^T.
```

No inherited token ID, terminal tanh response, authored answer or scalar selection enters the
new receiver. It reuses the existing certified exponential and directed 256-bit products/division.
It introduces no new material-source variant or checkpoint reinterpretation.

[proved-derived] With q fixed, the negative potential derivative of `KL(q||p)` is r, while
the negative derivative of `||q-p||^2/2` is `J_p*r`. The return retains both faces explicitly;
the second is not a universal update law. Both have zero total current on each exact group.
Their interval boxes enclose that joint constraint rather than assert independent exact values.

## Device enclosure and ownership

[definition] The native current reports contain signed-wide numerical representatives and
their common Euclidean radius. The real-coordinate receiver bounds every coordinate outward,
chooses a common upper origin and evaluates nonpositive exponentials with the standing owner.
Lower probability bounds divide lower weights by the upper partition; upper bounds use the
opposite endpoints. If the lower partition rounds to zero, the receiver widens to the complete
probability interval instead of selecting a current or asserting a zero denominator.

[proved-derived] Interval products enclose all four endpoint products. The weighted residual
mean also lies in the hull of the residual intervals because the exact p is a simplex section;
intersecting those bounds is sound. A common additive shift of two exact point-current groups
has exactly zero r and pullback even when the normalized probabilities require non-dyadic
intervals. This is equality at that receiver, not identity of the source occurrences.

[established-bounded; source-inspected] The new owner is
`field/receiver/normalized.rs`, backed by `field_normalized_receiver.cuh` and the existing
resident passage. It changes no field state, source capability or morphology. The resident
return's metadata is immutable to callers; its explicit `inspect` method is the cold receiver.
The result is ready for the next producing-adjoint composition, not already a committed
developmental displacement.

## Returned evidence

[established-bounded; measured] Three focused CUDA controls returned: common gauge and group
separation with retained imaginary residue; complete open-ball bounds; and an actual field
prediction/observation, independent exact exponential comparison, delayed reception and continued
field operation while the earlier return remains held. The first run passed two controls and
exposed an incorrect test reference that assumed half-unit observation. Source inspection and
the actual material reading identify a full unit. The corrected independent reference and
delayed-source test passed in 0.24 seconds. The arithmetic kernel did not change for that repair.

[established-bounded; process-audit] Commands were
`cargo test -p holonic-engine --lib normalized_receiver -- --include-ignored --test-threads=1`
and the focused corrected rerun with filter
`normalized_receiver_returns_through_the_actual_prediction`. The example build
`cargo build -p holonics-hna --example alpha_contextual_lift` passed. Existing unrelated
dead-code warnings in `operative_return.rs` remain. No broad unchanged suite was repeated.

[established-bounded; measured] The actual 1,930-occurrence conversation checkpoint returned
normalized currents at receiving occurrences 1, 20 and 1,600, using their true sources 0, 19
and 1,599. Each used one native deed, zero numerical section readouts during construction, and
about 0.4–0.5 ms after remount. Every one of the 18 potential-pullback coordinates in each
return has a certified nonzero sign. Exact rational norm lower bounds, the command and scopes
are preserved in the [portable receipt](2026-09-08_normalized_material_receiver/return.json).
Whole-process time was 9.16 seconds. Cold inspection is separate from those native counters.

[open] The observation changed no model coefficients and produced no new language result.
Normalization alone preserves potential ordering; it cannot repair the old repeated output.
Next conduct the appropriate complete receiver return through its actual producing maps and
finite successor, retaining chronology and the declared metric. Compare the relative-entropy
and Euclidean-probability returns by their actual laws instead of blindly adding another
Jacobian factor. AC1 remains unfinished and Athena-alpha is unattained.

[established-bounded; source-inspected] A concurrent source review located a separate
self-entry boundary: ordinary text generation read a codeword and called `receive(symbol)`,
mounting unit impulses. Model history remained, but the next input did not carry the emitted
amplitude/phase. The explicit `receive_native_return` path already preserved a supported point.

[established-bounded; measured] Ordinary `Constitutive` generation now retains a supported point
return through the existing native ingress, with one terminal readout rather than decoding it
twice. The new control learns a native current of amplitude two from an actual source contact,
generates its codeword, and verifies that amplitude two enters the next occurrence. Four SDK
native controls pass in 1.83 seconds, including the existing restart, pending-current refusal
and ordinary contextual-reception controls. Command:
`cargo test -p holonics-hna --lib alpha::text_session::native_tests -- --include-ignored --test-threads=1`.

[definition] A plural fibre may still have a fixed exterior word; that remains an explicit word
observation rather than an invented point. The default material receiver exposes a current ball
without the point ingress, and its centre is not selected. No new successful language result is
claimed by this repair. The [boundary-flux/interior analysis](2026-09-08_BOUNDARY_FLUX_COUPLES_ACTIVE_INTERIORS_AND_TOLERANCE_IS_RECEIVER_RELATIVE.md)
further identifies why a receiver return must reach the relevant retained interior.

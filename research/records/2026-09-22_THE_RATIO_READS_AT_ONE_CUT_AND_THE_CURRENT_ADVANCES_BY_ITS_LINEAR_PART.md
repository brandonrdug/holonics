# The ratio reads at one cut and the current advances by its linear part

**Starting tree:** `854c99be`. **Campaign 1** of the [machine in the elementary objects](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md#the-machine-in-the-elementary-objects):
ratio and one cut, with every formal item assigned to it. Three Opus 5.5 workers (body, receiver, Lean),
two Opus 5.5 reviews, and the Lean foundation of the [Holon object](2026-09-22_THE_HOLON_IS_AN_INTERCONNECTED_PORT_OBJECT_ON_A_COMPLEX.md).

## What changed

[established-bounded; source-inspected]
- **Complex receiving potential.** `s_c=⟨r_c|y_j⟩` is complex; `p=softmax(Re s)` (unchanged receiver), phase
  `φ=Im s/2` (no longer written as zero). One face serves selection, comparison and readings.
- **Loss and descent.** `ℓ_c=½log(q_c/p_c)+iΔ_c`, `Δ_c=φ^T_c−φ^H_c` on the ratio's own lift (branch `n=0`; the
  ring winding cancels and is a reading only). Covector: real `q−p`; imaginary `+½q_cΔ_c`, the descent of
  `½Σq_cΔ_c²` (ruling: `Im ℓ` is an oriented displacement). The target Holon is the target passage through the
  same machine at the same cut (same `q₀`, same incident word); the observation fixes its magnitude.
- **One cut.** A delayed observe reads `q₀`, `M`, `ρ`, `R` and the encoder `E` at the contemporary constitution;
  forward, comparison and pullback share that cut. A stale read is refused.
- **Retention.** The body retains per-symbol phase-weighted sums `C_a`, `D_{p,a}` for the passage's present
  symbols; session and presentation keep fixed records and digests. Pending state is independent of N and of
  `|A|` (3,141–3,145 B in the controls; 15–59 kB on real passages with many distinct symbols).
- **Phase-weighted ports.** `c_p=Σ L^(N−1−t) I (E(u_t)−E(u_f))`; order separates under rotation (`abca`/`acba`).
- **Chart law.** The resident current advances by the linear part `L`; affine translation acts on
  configuration only (source standing, participation values, receiving phases). The translation previously
  injected energy into the current at every step.
- **Commit rebase.** Each commit publishes the joint-ball centre (radius 0) and records the dropped radius
  (`last`, `max`, `sum`); `Σr` is the sum of dropped radii, and the trajectory bound `ΣK^(n−1−i)r_i` is owed
  (no Lipschitz bound of the whole word is exposed).
- **Receipts.** Per-ring Welford summaries of per-tick energy increments, per-contact amplitude and contrast,
  bits, descent quantity, rebase residual, work counts.

[proved-derived; formal-checked] `Objects/{RatioPhase,RatioBlock,Retention,Membrane,Orientation,SourceHolon,SourcePorts,CommitRebase}`
and the Holon foundation `Holon/{Port,Dirac,Complex,Element,Generator,Restriction,Law,Conformance}`
(`Framework.HolonObject`; 83 audits, standard axioms): alignment descent and winding cancellation; the
symbol-sum standing matching the native law; native minimality iff observable; Dirac structures (skew,
Kirchhoff via a constructed cycle basis, kernel form), interconnection closure, pushforward along any port map,
port Holon energy balance (continuous, nonlinear, midpoint exact, backward-Euler defect), passive/active
receivers, connection incidence on any graph with curvature, lossless winding jump, restriction and Kron
exactness, and instances (medium, SSM, diffusion, pumped LC, pair contact, Maxwell with Gauss preserved, a
three-mode Lie–Poisson Euler/NS truncation).

## Measured

[established-bounded; measured] Six-cycle control: model bits 6.198 → 6.098 against a uniform 6.170 (gain
+0.072 bits at cycle 5), outputs `""`, `"a␠"`, `"aa"`; pending 4.75 kB; rebase residual max 4.8e-13.

Silent-source control (zero encoder, 40 commits): committed energy decays monotonically to a fixed point
(per-commit ratio 0.753–0.9999). **At fixed material the incident word is passive.**

Exposure sample (same source and spec as the first readings): 436 frames, 10 returns, 1 refusal (was 202/6
before the campaign, 79/4 mid-campaign). Early returns now beat uniform (+6.3, +19.9, +61.1, +256.1 bits at
frames 30–80) with zero uninformative rows through frame 203 (was 58–285). From frame ~185 the model degrades
and ring g1's energy grows from 7 to 2.6e7 by frame 408, ending in a normal-material arithmetic refusal.

## Open, and where it goes

[open] The late divergence comes with learning, not with the word: material updates (reaction `M`,
`E`, `R`, amplitude) inject energy. In the Holon balance this is the deposition-work term `⟨∂_Θ E, Θ̇⟩`; the
restructure's energy balance on the incident word is its diagnosis (phase 3). Also open: the rebase trajectory
bound, a resident rebase kernel, one-row encoder pooling (needs a weighted normal observation), the pair-phase
face consumer (campaign 2), a full mesh Euler/NS instance.

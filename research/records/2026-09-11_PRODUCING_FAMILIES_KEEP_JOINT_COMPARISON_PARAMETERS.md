# Producing families keep joint comparison parameters

[project-postulate] This closes the already-active native work at Brandon's request for a status
review and a transcendental/compression study. Native expansion pauses after this verified return;
family-aware material development and useful Athena remain incomplete.

[definition] `predict_contact` retains the actual source family, produced family, conditional
map and condition cut under a private-owner-qualified `NormalCoupledProducingHandle`. Later
`compare_coupled_prediction` constructs paired features and observed difference on the device:
`x=(C−P,C,P)`, `Ψ=(x,h,x⊗h)`, `η=v−C`. Direct uses P=p,C=c. UnitRealSum uses the declared
mean-offset sections P=S(p),C=S(c), checks that v has unit real sum, and lifts the raw target as
`v+(c−C)`. Thus the raw source offset is retained and η has zero real sum in that chart.

[definition] The paired carrier maps the original affine origin and every ordered source
generator into joint Ψ/η coefficient rows, with one exact signed-wide rational denominator per
row. The same source parameters and original anchor constraint govern both sides. Rows are not
independent observations, and row zero is not a point-current operand. The old source, produced
family, actual observation and producing map remain owned beside these coefficients.

[open] This prepares a source-qualified comparison; it neither deposits its alternative rows
into a span nor implements the still-missing family-aware material update. The public
`compare-symbol` event explicitly reports `material_deposited:false` and leaves its producing
handle pending. `observe-symbol` continues to refuse the absent material-development operation.

[definition] Coupled wave rest v8 stores active producing cuts. Remount recomposes each saved
source with its producing conditional map and compares the full resulting family, then retains
that reconstructed lineage. Rest remains v7 when no coupled cuts are pending. Coupled session
v2 retains pending selection/re-entry associations and reads v1 sessions. Emission with
`retain_comparison:true`, `compare-symbol` with an optional coefficient-row receiver, and explicit
`release-symbol-comparison` expose those operations through the existing stream. Releasing an
in-progress emission's source is refused; a released handle cannot be reused for a comparison.

[established-bounded; implemented-exact; computational-witness] Eighteen serial coupled CUDA
checks and five serial session checks pass. These include every paired coefficient against an
independent exact-rational reference; fractional UnitRealSum transport; preservation of the old
complex condition after intervening material/condition change; foreign and released handles;
pending comparison/emission restart; and a substituted source with equal projected endpoints
being rejected by full producing-image reconstruction. The public workbench builds.

[historical; process-audit] Overlapping CUDA tests with the public probe caused allocation-grain
calibration failures before affected assertions. The complete affected suites were rerun serially:
`athena-producing-family-coupled-tests-serial.log` returns 18 passed and
`athena-producing-family-session-tests-serial.log` returns five passed under `.local/scratch`.
The earlier concurrent attempts are retained and are not counted as successful verification.

[established-bounded; computational-witness] The public model reproduced two newline emissions,
retained the second producing cut at epoch 15, re-entered the actual emitted pair at epoch 16,
and saved `.local/athena-producing-family-2026-09-11.pending-session` (161,880,455 bytes).
Its events/process receipt are adjacent private files. This is a persistence/capture return;
the real-model comparison/release follow-up was not run after Brandon redirected the work.
The small native/session controls above establish their stated scopes. No useful language,
measured end-to-end loss improvement or emergent layer formation is claimed.

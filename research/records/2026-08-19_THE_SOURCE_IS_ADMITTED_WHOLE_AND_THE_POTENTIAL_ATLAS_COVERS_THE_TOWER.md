# The source is admitted whole, and the potential atlas covers the tower

**Date:** 2026-08-19
**Kind:** construction return for Station B of the complete Phoenix sequence — complete source
admission and potential transport — with its driver, its two artifacts, every falsifier with its
predicate, and what it does not claim.
**Truth status:** `established-bounded` for every measurement (each carries its command or
artifact); `implemented-exact` for `foreign_map::streamed_census`, the scrubbed slice resolver,
`FileIdentity`, and `ResidentLaw::entailment`; **Station B PASSES on this tree**; every transport
of the atlas is `potential-only` except layer zero's, and no master station of the Gemma table is
promoted by a potential atlas.
**Authority:** the directive of 2026-08-19 (Station B); the Gemma instance blueprint §4 and §1;
the Phoenix master's source-admission and potential-lift stations;
[Station A's record](2026-08-19_THE_REFUSAL_TRAVELS_ALONG_THE_LINEAGE_THE_ADMISSION_IS_TYPED_AND_THE_RATCHET_IS_DISPOSITIONED.md).
**Position boundary:** [`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) does not move.
**Driver:** `crates/holonic-engine/examples/the_source_is_admitted_whole_and_the_potential_atlas_covers_the_tower.rs`
(site modules `phoenix/resident_layer.rs`, `phoenix/tower.rs`).
**Artifacts:** `output/the_source_is_admitted_whole/gemma-4-E4B-it.manifest.tsv` (2,130 rows),
`output/the_source_is_admitted_whole/potential-transport-atlas.tsv` (1,466 rows),
`output/the_source_is_admitted_whole/receipt.form`.

---

## 0. Verdict

Eleven falsifiers with predicates that can fail return `PASS`; the run is 28.6 s wall, of which the
one streamed pass over the 16 GB container is 26.9 s.

- **The manifest is complete.** All 2,130 declared populations are manifested, every byte region
  is hashed over exactly its declared span in one streamed pass that also yields the whole-content
  and header digests, and **every `BF16` codeword — 7,996,157,418 of them, 15,992,314,836 octets,
  the entire payload, 0 octets uncovered — is decoded exactly through the one float mouth** (the
  mouth's verdict for each of the 65,536 codewords taken once into a table). 2,130 of 2,130
  regions are `decoded-exact`; none carries a non-finite codeword. Rank 0–4 populations are
  censused (928 / 557 / 630 / 13 / 2). Seven sibling assets are hashed and declared used or unused.
  The text, image, audio and video ports are typed from the configuration.
- **Authentication binds one immutable occurrence.** `FileIdentity` (device, inode, size,
  modification and change instants) is equal at open, after the pass and after the tower binding;
  dtype is checked on every declared-shape testimony (a region drifted to `F32` refuses at
  `DtypeDiffers`); symbol and slice resolution reads **scrubbed** code — comment bodies and
  string-literal bodies blanked at equal length, the slice matched by its blanked structure and then
  verbatim at that position — so a docstring or comment slice refuses as absent; and **a valid but
  unrelated slice does not authenticate another binding**: every law is ENTAILED by its validated
  testimony (`LawEntailment`, parameters connected to fields, shapes and slices), and the receiver
  projection's slice offered for the presented projection refuses `SliceDoesNotEntail`.
- **The potential atlas covers the full declared text tower.** 42 layer deeds founded with the
  source's two attention species (35 sliding at head 256 with default RoPE θ = 10⁴; 7 full at head
  512 with proportional RoPE θ = 10⁶ rotating 64 of 256 pairs), the KV-sharing realization bound
  from the implementation (layers 24–41 read standings stored by 22 and 23 and build no K/V; their
  54 stored `k_proj`/`v_proj`/`k_norm` populations are **unbound by the source realization**, named,
  never called potential and never condensed away), every normalization and residual passage, the
  per-layer input from the embedding in every layer, the final normalization, the tied output
  boundary, and the two modality projections under their own configuration scopes: **1,466
  operations, 1,466 entailed, 0 refusals** — 3,431 symbols, 1,758 fields, 830 shapes resolved
  against the authenticated implementation, configuration and header.
- **Load-bearing is declared only from an enacted operator**: two populations (layer 0's `k_proj`,
  `v_proj`), each citing Station A's family-withdrawal falsifier.

## 1. What was built

- `crates/holonic-engine/src/foreign_map.rs`: `ForeignContainer::streamed_census(file, chunk)`
  (one pass, per-region `Sha256`, whole and header digests, a 65,536-entry table of
  `decode_bfloat16_bits` verdicts, per-region finite / non-finite / zero / subnormal counts and the
  unbiased exponent range, uncovered payload octets, the pass's peak residency and exact work);
  `FileIdentity`, `RegionCensus`, `StreamedCensus`. Tested on a synthetic container with a gap, a
  straddling codeword and an infinity at a nonzero coordinate, at four chunk sizes.
- `crates/holonic-engine/src/source_occurrence.rs`: `scrub_python` (comment and string bodies
  blanked at equal length; offsets and lines preserved); resolution over the scrubbed text with the
  slice matched blanked-then-verbatim; `AuthenticatedContainer::identity` + `verify_still`;
  `SourceRefusal::DtypeDiffers`.
- `crates/holonic-engine/src/resident_law.rs`: `ResidentLaw::entailment` and `LawEntailment` /
  `EntailmentRefusal` — per law: a resolved slice must NAME the operation and every parameter must
  be entailed: `Enter.scale` = the `bfloat16` nearest `√field` (exact half-ulp check);
  `RmsRebase.eps` = the `binary64` nearest the declared decimal (`decimal_to_rat` + half-ulp),
  `group` = the gain's declared shape or a field, `gain` named by a slice; `Chronology`/`Contact`
  heads, widths and window from fields; `GeluTanh.c1` = the `binary64` nearest `√(2/π)` against
  Machin's certified π enclosure (`reopening::arctan_unit_fraction`, 40 terms) and `c2` nearest
  `0.044715`; `Scale.by` enclosing `1/√field`, `1/√2`, a slice literal, or a stored scalar;
  `Standing` named by the slice that hands the standing over; interventions typed. The passage's
  `compile` asks it for every occurrence and refuses `CompileRefusal::Entailment`. The `Standing`
  law and `section_carry` kernel carry a released resident section into a later passage
  (`CompiledPassage::release_section`; the releasing passage may not relaunch).
- `crates/holonic-engine/examples/phoenix/tower.rs`: `found_layer(layer, entry, …)`,
  `found_final`, `found_bands(species, terms)` (the identity element for the fixed pairs of a
  proportional chronology), `material_plan`, `mount_layer`, `mount_final`, `enter`,
  `unbound_populations`, `Species`, `KvRole`.

## 2. The falsifiers

| # | falsifier | result |
|---|---|---|
| 1 | every declared population manifested, hashed over its exact span, censused; none dropped | PASS — 2,130 / 2,130 / 0 refused |
| 2 | every `BF16` codeword decoded exactly; admission founded by the decode | PASS — 7,996,157,418 codewords = the whole payload; 0 non-finite |
| 3 | digest, census and tower binding read one stable file occurrence | PASS — identity equal at open, after the pass, after the binding |
| 4 | dtype checked, not recorded | PASS — every extent = elements × 2; an `F32` drift refuses `DtypeDiffers` |
| 5 | resolution blind to comments and string literals | PASS — docstring and comment slices refuse; the code slice resolves |
| 6 | a valid unrelated slice does not authenticate | PASS — `q_proj`'s slice refused for `k_proj`: `SliceDoesNotEntail` |
| 7 | every atlas binding entailed | PASS — 1,466 / 1,466 |
| 8 | the atlas covers the whole tower | PASS — 42 layers (35 + 7), 11 chronologies at head 512 and 55 at 256, 36 shared standings over 18 shared layers, final rebase, tied boundary, 2 modality deeds |
| 9 | unexcited and unbound are separate classes | PASS — 54 unbound, each decoded-exact |
| 10 | load-bearing only from an enacted operator | PASS — 2, citing Station A |
| 11 | peak residency and exact work reported; no full residency | PASS — peak 33,558,700 octets against 15,992,595,884 |

## 3. The coverage ledger, as the manifest carries it

| transport class | populations | octets | what it means here |
|---|---|---|---|
| stimulated | 21 | 7,220,521,474 | layer zero's populations and the two embedding tables: caused material crossed them in Station A's deed |
| potential-only | 646 | 7,717,301,842 | bound to an operation of the atlas; no caused material has crossed |
| unbound by the source realization | 54 | 110,111,232 | `k_proj`, `v_proj`, `k_norm` of layers 24–41 |
| unposed | 1,409 | 944,380,288 | the vision and audio tower interiors — the ports are typed, the projections bound, the towers not posed here |
| unexcited | 0 | 0 | a class of the ledger whose population is founded only when declared material has been presented (Stations C and H); zero here by construction, not by measurement |

Admission: 2,130 decoded-exact, 0 decoded-bounded, 0 unread/refused.

## 4. What this station does not claim

A manifest is not a lift; a potential atlas is not an active one; layer zero is the only stimulated
deed; no vocabulary row is called excited or unexcited; the vision and audio towers are not posed;
the tied output boundary is bound and not enacted; `final_logit_softcapping` is named as a monotone
transformation and not enacted. Station C enacts the tower.

## 5. The validation epoch

`cargo test -p holonic-engine --lib -- foreign_map source_occurrence resident_law front_passage
resident_section` (37 tests, on the card); `cargo check -p holonic-engine --examples`; release
builds of the one driver (1 m 04–16 s, five times); the driver run five times while the tower's
testimony was brought to verbatim slices (27–29 s each); the Station A driver rebuilt and rerun
`--no-serial --no-source-face` once after entailment entered compile (23 falsifiers PASS). The
release reading:

**The one complete gate**, `bash tools/gates.sh`, 2026-08-19 17:04:29 → 17:08:59 UTC, exit 1,
**11 of 12 — `architecture-lint` red** on the five owners this station grew (`foreign_map.rs`
`Vec 18→29`, `.clone() 8→18`, `.collect() 11→19`, `BTreeMap 15→19`; `resident_law.rs` `.clone()
4→13`, `.collect() 0→11`, `.into_iter() 0→4`, `BTreeMap 31→35`, `Vec 21→40`; `front_passage.rs`
`.clone() 34→35`, `Vec 80→84`; `source_occurrence.rs` `.clone() 20→25`, `BTreeMap 3→4`, `Vec 21→25`;
`resident_section.rs` `Vec 64→65`). Every new occurrence read: the codeword table and the per-region
hashers are indexed by codeword and by region (the relation); `RegionCensus`, `LawEntailment`
parameters and naming slices, `entailments`, `testimony_of` are returned populations; the scrubbed
text is a derived reading of the same text, not a second owner of a continuing body; no hidden
order, no clone of a continuing body, no dispatch table. **The ledger rows were changed explicitly
for the five files, and `bash tools/gates.sh architecture-lint` alone returned `PASS` at 17:09
(`holonic architecture clean: 239 files, 21739 inherited occurrences, 0 retired`).** The two scopes
are stated apart and not spliced: the complete reading is 11 of 12 before the ledger change; the one
named gate is green after it; the other eleven gates' readings are the complete reading's (`tests` 2,517 passed, 0 failed, 19
ignored over 29 result lines; example targets type-checked).

Station B passed; proceeding to Station C.

# The recurring laboratory transport condensed into native hexis, and L2 awaits release admission

**Date:** 2026-08-23  
**Truth statuses:** `definition` for the declared L2 receiver/history aperture; `proved-derived`
for the generator factorization and carrier-rebase square; `implemented-exact` for the Rust/CUDA
rest, decoder, fibres, and resident passage; `measured` for the card, Lean, source-access, cost,
artifact, architecture, and inspection receipts; `established-bounded` for the seven-part candidate
grade; `open` for release admission until the sole complete receiver passes.

## Candidate verdict

[established-bounded] L2 returned all seven required consequences over the complete cultivated L1
family. The product is
[`output/recurring_laboratory_transport_condenses_into_native_hexis`](../../output/recurring_laboratory_transport_condenses_into_native_hexis),
produced by `recurring_laboratory_transport_condenses_into_native_hexis`.

[definition] The source owners are the admitted `FamilyCultivatedAthenaRest`,
`generator_native_rest`, `receiver_history_compression`, and CUDA refinement owner. The condensation
consumes the cultivated source shape and rests one shared oriented generator, two distinct carrier
charts, receiver/history factors, complete reconstruction addresses, and the open exterior. It
retains no L1 body and offers no inverse selecting a source representative.

[proved-derived] Both cultivated plates carried the same exact relation

```text
A - I = L C,
C = [1, 1, -1],
L = [-1, -1, 1]^T.
```

Consequently the dense action is not primitive. Its native conduct is

```text
T(s) = s + L(C(s)).
```

The integer and modulus-two passages therefore share one generator while retaining different
carrier canonicalizations, support coordinates, returned occurrences, and cultivation lineage.

## Factorization, naturality, and reconstruction

[implemented-exact] Five declared product receivers cross six successor histories: fixed section,
expanded generator, composed joint, two local ablations, and the carrier rebase. All 30 pairs carry
one exact factorization or one exact defect. No path, theorem name, namespace, tactic, subject label,
or media label selects the internal law.

[proved-derived] Raw generator transport commutes with reduction from the integer chart to the
modulus-two chart. On the intervention `s=[1,1,0]`, the integer transport is `[-1,-1,2]`; reducing
it modulo two returns `[1,1,0]`, which equals the native modulus-two transport. Cultivation
membership does not commute: the integer residual is `2` and returns route `2`, while its
modulus-two image is `0` and returns route `1`. This is the exact defect, not a failed naturality
claim.

[implemented-exact] Three complete collapsed populations bind the repeated dense actions,
constraints, and action-difference factors to the shared generator occurrence. Six reconstruction
fibres retain the carrier charts, routes, local ablations, and interchange. The one-generator word
above is the shortest admitted separator for the proposed equal-present zero-section quotient.
Nonlinear fixed varieties, overlapping supports, and histories outside the frozen L1 aperture
remain open.

## Source-detached resident conduct

[measured] Three matched passages crossed both the cultivated source and native rest on the NVIDIA
GeForce RTX 4080 SUPER. Held-out routes `[1,1]`, residual-obstruction routes `[2,2]`, and
carrier-future routes `[2,1]` agreed exactly in transported sections, constraint faces, local
routes, and joint consequence. Lean accepted the detached product's integer and `ZMod 2` generator
relations.

[implemented-exact] Each detached passage mounts the shared orientations once and returns residual
evaluation, native transport, cultivation or obstruction, joint consequence, every local ablation,
semantic work, and semantic span from two launches, one typed reduction, and one synchronization.
The CPU performs no action, constraint, carrier, route, joint, or ablation replay. The exact
pre-dispatch local alternatives are work `[2,5,2,5]` and span `[2,3,2,3]`.

[measured] All three fresh-process passages exposed no repository, proof store, exchange store,
research archive, Git directory, Codex store, or Claude store. The carrier-future return used two
active lanes, work `13`, span `5`, and 234 resident/transfer octets. Its exact residuals were
`[2,0]`; both local-ablation alternatives returned through the same resident reduction. Physical
energy remains unknown.

## Complete-product descent

[measured] Across held-out, residual, and carrier-future passages, the cultivated source and native
product compare as follows:

| complete coordinate | cultivated L1 | native L2 |
|---|---:|---:|
| standing artifact | 1,320,618 octets | 2,273 octets |
| decoder | 5,736,848 octets | 6,585 octets |
| reconstruction fibres | 244,122 octets | 1,883 octets |
| complete semantic work | 93 | 39 |
| complete semantic span | 21 | 14 |
| resident extent | 1,524 octets | 702 octets |
| transfer extent | 1,524 octets | 702 octets |

[established-bounded] Every required coordinate falls strictly. The result is native-hexis
condensation for this exact two-carrier, five-receiver, six-history aperture; it is not a claim that
all nonlinear or future laboratory transport has already condensed.

## Ownership, purity, and artifact identity

[measured] The architecture audit first rejected seven undispositioned populations: seven CUDA
receipt vectors and the exact collections in the two new owner-local native-hexis files. Inspection
found no continuing ecology clone, global materialization, host foreman, or duplicate subsystem.
The architecture ledger now binds exactly those standing, decoder, fibre, and device-return
populations; the ratchet returns 345 protected files, 26,380 inherited occurrences, four retired,
and zero new undispositioned occurrences. The changed semantic/device cone contains no `f32`,
`f64`, or `double` type.

[measured] Visual inspection confirmed that the SVG and mesh show one shared `T=I+L·C` junction,
two retained carrier charts, the future-sensitive reconstruction seam, residuals `[2,0]`, and routes
`[2,1]`. The focused exact owner test and live CUDA receiver both passed.

[measured] The final product contains 47 non-manifest files occupying 97,513 octets. Its internal
roll is `754696eb20304e57403ad8720225eb8173cae804657deb455b469801915f8a18`; the manifest file is
`cac3cf638e2dbf4be56227ec1502707f6af550617596d9b31005bc477a3befdc`; and the seven-part grade is
`8f96a2b09b6a9d78785753f2805c34895318da3cf38f576bcff7c59c8c5570e1`. The canonical native-rest
files are standing `33756ef06822e323ddd07e3154c47c1be4827566029006fafe4521fec44ee3a7`, decoder
`788fa6fa989193e772ef85fc476777c6800f1be6eaa6ed7af690c0d3b9390f90`, and fibres
`81049297172f4825acec9df4f410465283396bcf5da6eace56ec034465c2da6f`.

[open] L2 is not admitted until `bash tools/gates.sh` passes once over the coherent candidate tree.
Only then may `CONSTRUCTION_STATE.md` advance to L3 source-detached work from the native terrain.

# Conditional wave families retain the anchor and stage their local laws

[project-postulate] This returns foundations within active Athena packet 3. The preceding resident
emission/session return is committed at `9db7f1fa`; coupled current publication, complete coupled
rest, bounded family receivers and product use remain work in the same goal.

[definition] `NormalWaveSource` exposes `φ(p,c)=(c−p,c,p)` from the actual joint current, retaining
both occurrences and the original generator fibre. Its outward source radius is `2r`, justified
by `||φ(δp,δc)||²≤3||(δp,δc)||²≤4||(δp,δc)||²`. This is the existing producing-comparison bound;
`normal_wave_source.cuh` now supplies their shared arithmetic. No point port consumes the radius.

[definition] `NormalWaveFamily` retains an anchor `x₀∈B(m,r)` alongside a full affine relation
with coordinates `(λ,x₀,p,c)`, where the complex homogeneous coordinate is fixed at `λ=1`.
Initially `(p,c)=x₀`. The anchor ball is the declared joint enclosure, which may enlarge the finer
normal-word family; the original word/fibre remains available. An affine report by itself is not
the constrained family and supplies no bounded feasibility certificate.

[definition] For an actual point condition h, `ResidentWaveRelation` derives the complete linear
pullback of the learned bilinear relation through
`E_h(λ,x₀,p,c,η)=((c−p,c,p),hλ,(c−p,c,p)⊗h,η)`.
The existing exact residual elimination computes `ker(ρ_L E_h)` and maps its entire population
into `(λ,x₀,p,c) → (λ,x₀,c,c+η)`. Original condition, material cut and a retained runtime owner
token qualify the derived relation. It is immutable derived material, not a second learner.
The normal operator M is not added to this response or developed by this operation.

[definition] `read_through` composes through the existing affine-family image owner. It retains
the anchor constraint and exact projected relation while eliminating intermediate coordinates.
Free directions and partial affine support survive; no midpoint, chosen cause or repeated-history
archive supplies the next family operation. This fixed-condition construction does not represent
jointly variable source and condition products as an affine relation. Bounded feasibility,
finite/unbounded projected ranges and their application receiver remain explicit work.

[definition] Member preparation copies only the affected basis section on device and runs the
existing formation kernel there. `PreparedConstitutiveFormation` retains a non-reused predecessor
owner token and occurrence. `PreparedNeighborhoodAdvance` holds member and condition successors
before publication; dropping it preserves usable standing. Commit preflights owner, epoch and
both preparations, then publishes by swaps/counters. Known arithmetic/declaration refusal preserves
standing; unknown completion keeps the poisoned-owner contract. Rest constructors create fresh
runtime tokens without changing existing wire formats.

[established-bounded; implemented-exact; computational-witness] Three new family CUDA controls
pass at one complex wave component and one complex condition: two successive conditional
passages, complex condition phase and an absent contact domain, and free output directions that
continue through a later law with a fixed current receiver. They exercise the full joint rows,
not sampled centres. Two source controls retain actual occurrences, nonzero radius and unchanged
material with no intermediate numerical host readouts. Two new neighborhood controls verify
complete-rest/conduct preservation on discarded preparation and stale/foreign proposal refusal,
including the no-observation branch. Existing neighborhood/refusal and producing-comparison
regressions pass (six and seven cases respectively, with the two new staging tests included in six).

[established-bounded; process-audit] Verification commands and local logs:

```text
cargo test -p holonic-engine --lib normal::direct::wave::family::tests -- --ignored --test-threads=1
  .local/scratch/athena-anchored-family-tests.log                 3 passed
cargo test -p holonic-engine --lib source::tests -- --ignored --test-threads=1
  .local/scratch/athena-coupled-source-tests.log                  7 passed (2 new source controls)
cargo test -p holonic-engine --lib staged_neighborhood -- --ignored --test-threads=1
  .local/scratch/athena-staged-neighborhood-tests.log             2 passed
cargo test -p holonic-engine --lib resident::neighborhood::tests -- --ignored --test-threads=1
  .local/scratch/athena-neighborhood-regression.log               6 passed
cargo test -p holonic-engine --lib wave::comparison_tests -- --ignored --test-threads=1
  .local/scratch/athena-source-comparison-regression.log          7 passed
cargo check -p holonics-hna
  .local/scratch/athena-coupled-foundation-facade.log              passed
```

[historical; source-inspected] Review repaired an element/byte count error in device copying,
removed raw-pointer-only predecessor identity, and fixed the prepared neighborhood's usable flag
before the focused controls above. A preliminary conditional kernel scaffold was replaced by the
full residual-graph pullback before integration. No unexecuted scaffold is counted as capability.

[open] A complete coupled Athena step must still bind an admitted contact to the actual source,
publish the wave family and neighborhood under one continuing owner, serialize that state and
pending relations, and connect family receivers/formation to continuing public conversation.
The affine tests establish this foundation's finite algebraic scope, not useful Athena-alpha.

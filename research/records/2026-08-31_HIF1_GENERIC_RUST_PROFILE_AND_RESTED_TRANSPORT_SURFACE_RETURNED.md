# HIF1 generic Rust profile and rested transport surface returned

**Date:** 2026-08-31
**Truth status:** `implemented-exact` for the neutral Rust surface and existing-owner delegation;
`established-bounded` with `source-inspected` and `measured` evidence for the profile, remount,
resident equality, and package tests.
**Construction effect:** HIF1 passed; the roadmap frontier advances to HIF2.

## Returned package

[implemented-exact; source-inspected] `holonic-engine::native_ecology::holonic_intelligence`
exports distinct dimension newtypes, typed open dimension faces, borrowed intrinsic native holon
profiles, neutral lifecycle traits, and `RestedTransportEcology`. It is nested under the existing
native-ecology owner, so no new root cabinet or architecture baseline was introduced.

[implemented-exact; source-inspected] `NativeSpoolBundle` itself implements
`RestedTransportEcology`; no wrapper or clone of the continuing ecology exists. Validation,
canonical rest, profile, and conduct delegate to `NativeSpoolBundle::validate`,
`canonical_bytes`, the borrowed profile projection, and the existing `mount_word` resident owner.

[implemented-exact; source-inspected] Each `IntrinsicNativeHolonProfile` borrows its thread and spool
facets: boundaries and carrier lines, full incidence, state support and Complex-Parametron cells,
occurrence population, chronology, generator descents, serial pullbacks, local and mutual
constitution, receiver factors/consequences, morphology owner, collapsed/occurrence fibres,
obstruction, and open testimony. Scale and apparatus-work dimensions return typed open faces rather
than zero. Incidence rank, nullity, and cycle rank are derived from the complete native-state/
incidence graph.

[implemented-exact; source-inspected] Neutral `DismantlingBoundaryReturn`,
`InferenceCirculation`, `CultivationLifecycle`, and `ExactWeightPassage` traits expose the HIF0
lifecycle vocabulary. Their equality controls are derived from returned fields rather than supplied
Boolean claims. `SoulkillerScrapyardReturn` implements the dismantling trait through its existing
productive bundle, cold witness, and insufficiency lanes.

## Receivers

[established-bounded; measured] The borrowed-profile control retains every native facet and proves
the profile morphology pointer is the original thread owner. The neutral rest control returns the
same canonical bytes and exact remount as the native owner.

[established-bounded; measured] The explicit ignored-by-default CUDA control was invoked under its
180-second boundary. Neutral trait conduct and direct `mount_word` conduct returned structurally
equal `NativeSpoolConductReturn` values with no invariant transport re-upload.

[established-bounded; measured] The complete `holonic-engine` library suite returned 1,984 passed,
zero failed, and 32 ignored tests. `cargo check -p life --lib` passed. Source-shape reports 630 live
files, 447 inherited baselines, and zero violations.

## Boundary

[open] Existing Safetensors manifestation, secondary BF16 intake, hand-written container emission,
vendor configuration files, shard indexes, and absent ONNX intake do not yet share one lossless
foreign chart. HIF2 owns that consolidation and the profiled productive dismantling return.

# THE SIMPLEX EMITS THE FACE; THE MATRIX CANNOT OWN PROPAGATION

**Date:** 2026-07-26  
**Status:** BRANDON-DIRECT CORRECTION / SOURCE BUILT / EXACT HOST VERIFIED /
44 LIBRARY TESTS + 1 DESKTOP CAUSAL-PHYSICAL TEST PASS / NO FLOATS /
GUI NOT LAUNCHED BY SOL

## Falsified construction

The prior desktop image did not arise from an unfolding simplicial world. Its
source was a manually entered fixture containing two triangles, one polyline,
one circle, one ellipse, and one hyperbola. The two receivers projected that
same fixture. Integral coordinate, quadratic, and depth phases supplied the
dense crossing contours, while additive terminal RGB made heavily intersected
regions approach white.

The visible radial squares had a separate false source. An \(8\times8\)
terminal worker rectangle had been made both:

1. a CPU work parcel; and
2. a node of a supposed causal wave tree.

That identified the bounded display matrix with the manifold and exposed
worker completion as if it were world propagation. It was not a merely coarse
rendering of the intended law; it was the wrong owner.

## Correct ownership

The desktop source is now one exact oriented tetrahedral simplicial complex:

- four caused vertices;
- four oriented triangular faces;
- six shared two-coface hinges; and
- local three-hinge transport cycles contributed by the oriented faces.

One exact variational hinge event applies a declared boundary impulse to one
shared boundary. The discrete Euler--Lagrange successor carries its prior and
current coordinate, momentum, internal impulse, external impulse, and exact
balance residual. Incidence conducts the resulting coordinate through the
connected hinge ecology. The complete initial event changes six hinges and
four incident faces. Three hinge-dependent homogeneous quadratic families
change in the same successor and are projected as receiver-relative conics.
Their circle/ellipse/hyperbola class is derived from the emitted quadratic in
the receiver chart; no conic species label is inserted into the world.

The construction contains no authored thread and no authored classical conic.
The four triangle entities are the presentation faces of the caused
simplicial complex. The three native conics are standing emitted by the hinge
law.

## The aperture is the finite boundary difference

`ReceiverWavePlan`, tile predecessors, tile action, tile causal depth, and the
phase-weighted terminal shortest-path tree have been removed. They were not a
coarse version of simplicial propagation.

The receiver-local affine chart is the complement of its projective horizon
\(H=\{w=0\}\). Its finite aperture \(D_\lambda\) is the intersection of
opposed oriented projective cuts supplied by the contemporary receiver and
presentation parameters. Infinity is therefore a direction on \(H\), never a
large coordinate. When one projective simplex edge crosses \(H\), its affine
face has two opposed branches. Exact interval inequalities clip each branch
against \(D_\lambda\); only their finite casts remain.

Let \(S_t\) be the continuous primitive support emitted by the causal-physical
successor at event \(t\). The terminal observation is

\[
V_{t,\lambda}=S_t\cap D_\lambda.
\]

This is the operative FTC/Stokes-like statement at this boundary: the visible
change is the restriction of the successor difference,

\[
\Delta_\lambda \Phi_t
=
\Phi(S_{t+1})|_{D_\lambda}-\Phi(S_t)|_{D_\lambda}.
\]

Unchanged interior cancels; changed supports and boundary crossings remain.
This does not subtract two numerical infinities and does not claim a universal
physical law. It states how one declared causal world, one receiver cut, and
one finite presentation membrane compose.

The desktop no longer classifies every matrix member and no longer walks a
radial terminal neighborhood. Every emitted segment and conic is restricted
independently. Exact recursive boundary subdivision rejects regions whose
continuous support cannot cross them and descends only into admitted regions.
The recursion is an observation algorithm; its regions are neither cells of
the source world nor event occurrences.

The final address map is unavoidable only at the platform membrane because the
actual display is finite. It receives exact support multiplicity. The current
neutral intensity quotient therefore means only “this many independently
emitted supports cross this address.” It does not pretend that RGB is a
derived phase, wavelength, or physical color law.

If a future display exposes partial causal evolution, its admissible units are
the `HingeRadiation` changes—hinges, incident faces, conic families, cycle
returns, and open seams—not terminal neighbors.

## Causal-physical interaction

The desktop world declares the relation from returned receiver motion to one
hinge boundary impulse. This declaration is local to the example; input does
not universally mean impulse. One input occurrence has one `EventId`.
Receiver consequences coupled by the world use that same identity, and one
physical successor changes the native conic population before the next two
receiver faces are assembled.

When no boundary deed is present, the world rests. There is no background
retile, invented clock, or persistent worker population consuming half the
machine. The next deed advances the exact carried trajectory once and
re-forms only the receiving face. The exact source aperture is \(640\) members
wide at the initial \(640\times400\) window; a larger window retains aspect
ratio with a bounded internal width of \(640\). This is explicitly a terminal
quotient, not manifold grain. Independent primitive restrictions use up to
twelve bounded CPU workers and restore canonical receiver/support order.

## Verification

- `cargo test -p holonic-engine`: **44 passed / 0 failed**.
- `cargo test -p holonic-engine --example desktop_receiver`:
  **1 passed / 0 failed**.
- The desktop causal-source witness checks:
  **4 simplex faces / 3 emitted conics / 6 changed hinges /
  4 changed faces / 3 changed conics / 0 open seams**. A second exact
  boundary impulse changes all three conics with zero balance residual.
- The projective-horizon witness sends one edge through \(w=0\) and receives
  exactly two finite aperture branches with horizontal endpoints
  \((-2,-1)\) and \((1,2)\).
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`:
  **clean**.
- `cargo build -p holonic-engine --example desktop_receiver`: **clean**.
- `f32`, `f64`, `Math.PI`, and standard floating constants in
  `crates/holonic-engine`: **absent**.
- During this correction Sol did not launch, focus, move, or close the GUI.

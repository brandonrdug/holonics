# The mathematical, vector, and raster faces met at one native boundary, and R5 awaits admission

**Date:** 2026-08-22  
**Truth status:** [established-bounded; implemented-exact; measured]  
**Roadmap position:** R5 candidate; the release receiver has not yet admitted the phase  
**Product:**
`output/the_mathematical_and_physical_faces_share_native_transport`

## What crossed

[established-bounded; implemented-exact] R5 composes M0's natural pages 5 and 10 from Jian Ge's
*Heat kernel geometry and Gromov's volume growth conjecture*, I4's shared heterogeneous action, and
R4's retained causal lineage. Notation, born-digital vector geometry, and four/eight-connected
raster vision enter through three separate typed ports. Each retained interior carries its source
occurrence and digest, exact two-axis chart extent, oriented axis hands, rational scale to the
common page chart, correspondence-uncertainty fibre identity, and source lineage before any caption
or OCR mediation.

[implemented-exact] The common-world object is the fibre product over each notation anchor:
notation identity × notation-vector candidates × notation-raster-four candidates ×
notation-raster-eight candidates. M0 intentionally left the direct vector/raster receiver pair
undeclared, so R5 does not fabricate that edge or a false filling. It stores one founded cone from
the three media ports to the common page world and carries generator-wise naturality squares whose
predecessor and successor retain the same source incidence.

[implemented-exact] Page 5 is the development occurrence and page 10 is held out. Every one of the
455 page-10 notation anchors has a nonempty vector, four-connected raster, and eight-connected
raster fibre. The exact populations are 1,232 notation-vector pairs, 1,195
notation-raster-four pairs, and 1,114 notation-raster-eight pairs. Multiplicity remains
reconstruction testimony: no winner, confidence score, concatenated embedding, caption equality,
or forced one-to-one identity is present.

## Resident passage and exact generated face

[established-bounded; measured] The complete held-out source candidate population crossed the
NVIDIA GeForce RTX 4080 SUPER in one launch and one synchronization. The source kernel used 1,365
active lanes, visited 5,454,540 exact anchor/port/pair cells, received 31,980 octets, returned 5,460
octets, and held 37,428 octets resident. The compact joint-media front then used 455 active lanes in
one launch and one synchronization, received 5,556 octets, returned 9,244 octets, and held 14,784
octets resident. No CPU semantic callback occurred between either card front.

[implemented-exact] One shared withdrawal returns every port to its predecessor consequence. Each
single-port withdrawal returns only the matching port to its predecessor and leaves the other two
ports at their successor consequences. Because the held-out rendering requires all three typed
ports, every shared or local port withdrawal also opens every joint-anchor predicate. The card
returned all shared and local withdrawals together; the host did not select a semantic route
between device calls.

[implemented-exact] The held-out consequence is one exact correspondence field derived from the
455 joint sections. Its extent is material-derived: 457 × 48 cells. The mesh, SVG, and PNG share
one typed incidence object containing 455 notation boundary cells, 1,232 vector cells, 1,195
four-connected raster cells, and 1,114 eight-connected raster cells. The PNG was decoded after
write and every typed pixel population was recounted. This establishes an exact generated
mathematical-media rendering at the declared aperture; it does not establish the mathematical
truth of the paper.

## Rest, reconstruction, dissection, and detached return

[implemented-exact] The continuing `MultimodalTransportRest` is not clone material. Its standing
contains the shared native subcomplex and only the held-out future-visible multiplicities. A
length-framed decoder owns all six exact modality interiors without expanding raw bytes into JSON
octet arrays. Complete correspondence, native reconstruction, naturality, unmatched, and open
fibres remain separate. The selectable dissection atlas exposes each source interior, held-out
anchor, shared route, port-local route, naturality square, and reconstruction alternative by
address.

[established-bounded; measured] A fresh process mounted only standing, decoder, fibres, and one
new page-10 inquiry occurrence. It returned all 455 joint anchors on the RTX 4080 SUPER, reopened
the exact notation, vector, and raster interiors, preserved shared and local withdrawal support,
and reported no descriptor into M0, I4, R4, the formal tree, Claude, or Codex material. Its GPU
passage used one launch, one synchronization, 455 active lanes, and zero CPU semantic callbacks.

## Complete-product descent

[measured] The complete matched source/native product prices decoder and fibre testimony rather
than calling their departure free. Every declared coordinate strictly falls:

| receiver face | matched M0 source replay | R5 native return |
|---|---:|---:|
| artifact octets | 35,484,415 | 44,388 |
| decoder octets | 33,998,441 | 5,629,992 |
| fibre octets | 6,941,122 | 209,149 |
| semantic work | 5,454,540 | 3,682 |
| semantic span | 3,996 | 3 |
| resident octets | 37,428 | 14,784 |
| transfer octets | 37,440 | 14,800 |

[measured] The candidate product contains 17 regular files and 6,891,546 octets. Its internal
manifest covers 16 nonmanifest files and carries rolled SHA-256
`1077fdb9e5c6ff9f6c7c5254fb0db10f4eeb69519e2335243b375b0af18c68be`. The exact raster face was
visually inspected after its programmatic incidence check.

## Architecture and validation receipts

[implemented-exact] The ownership-ratchet change is narrow and measured. The card gained eight
`Vec` occurrences for the exact candidate, consequence, and shared/local withdrawal populations.
The new owner files admit only their measured standing, decoder, fibre, validation, and fixture
populations. No ecology clone, semantic scheduler, parser, caption cabinet, or CPU fallback was
added. After recording those maxima, the architecture gate returned 334 protected files, 26,096
inherited occurrences, and zero retired occurrences.

[measured] `cargo test -p life --lib multimodal_transport --no-fail-fast` returned three passing
owner tests in 30.98 seconds of compilation plus an instantaneous test passage. The direct-card
test command
`cargo test -p holonic-engine cuda_refine::tests::the_card_derives_and_enacts_the_joint_media_subcomplex -- --ignored --exact`
returned one passing RTX deed and zero failures; its test passage took 0.23 seconds after a 55.45
second build. The first complete driver invocation returned exit zero in 27.0 seconds. A subsequent
owner-local wire split changed the code closure, so the driver was lawfully invoked a second time;
it returned exit zero in 15.5 seconds and regenerated the addressed artifact. The named
authored-level and architecture gates then passed. These are candidate checks, not the complete
release receiver.

## Candidate grade and open boundary

[established-bounded] The candidate returns all ten named R5 consequences: real separately typed
media ports; pre-text spatial testimony; common-world pullback and naturality; one shared native
subcomplex; complete fibres and decoder; a held-out joint consequence; certified mesh/SVG/PNG
agreement; shared and local ablation; source-detached remount with strict complete-product descent;
and an exact selectable dissection atlas.

[open] R5 does not prove the paper, recover semantic OCR identity, establish unrestricted
multimodal reasoning, or freeze a production Athena. The undeclared direct vector/raster receiver,
new charts, larger mathematical/physical source families, and richer successor histories can
reopen retained fibres. R5 remains unadmitted until the sole complete release receiver passes.

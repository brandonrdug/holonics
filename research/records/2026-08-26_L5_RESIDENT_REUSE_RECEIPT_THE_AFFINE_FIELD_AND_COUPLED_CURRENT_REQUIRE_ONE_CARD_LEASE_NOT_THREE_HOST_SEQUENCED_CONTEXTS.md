# L5 resident reuse receipt: the affine field and coupled current require one card lease, not three host-sequenced contexts

## Standing reuse

[established-bounded; implemented-exact; measured] The one continuing body is
`AffineLaboratoryCultivatedAthenaRest` with measured identity
`5b9a09396d0924ef1a5499737a1c609d943be38ec0647d394d35d79b7d460b2b`. Its existing owners
already supply:

- the exact L2 causal-adjoint-pulled Complex-Parametron current and symmetric mixed families;
- the source-detached affine laboratory with 2,221 addressed landmarks and 169,386 cells;
- exact barycentric transport of a later rank-four section through every cell;
- the participant incidence/front and boundary codec; and
- L4 native potential, emanation, world return, and perspective change.

[definition] The ingress is one later `MaterialNativeFactorization` plus a typed deed/perspective
disturbance. The hot owner remains `ResidentAffineLaboratoryAthena`. The return is one complete
native successor/potential testimony followed by the already-founded exterior codec. No new
semantic organ, inference law, scheduler, Hodge selector, or second ecology is needed.

## Measured obstruction

[counterexample; implemented-exact; measured] The present `mount` path creates three distinct
`CudaRefineExecutor` values:

1. one inside the L2 coupled resident;
2. one inside the participant causal front; and
3. one inside the affine barycentric transport.

`conduct_material` launches the affine leaf, copies its complete result to the host, validates it,
and then launches the L2 leaf in another context. L4 later constructs the native potential from
those host-returned structures. Every leaf law and its telemetry are exact, but the composition is
not one resident causal word. The CPU is still the apparatus which schedules semantic stages
between device calls.

[definition] This is the shortest obstruction returned by the existing owner graph. It does not
reopen the affine mathematics, rank-four fibre, equal-count separator, detached rest, or leaf GPU
measurements. It reopens only the claim that those leaves already constitute one card-resident
Athena continuation.

## Smallest repair

[definition] Extract a single-threaded move-owned card lease from `CudaRefineExecutor`. The one
laboratory resident creates one lease and mounts the existing coupled-current, participant, and
affine buffer populations through it. Standalone leaf APIs may still create a sole lease for their
own bounded tests, but the integrated owner must prove that all three leaves share one underlying
context/module identity.

[definition] The repair must return:

- one context identity for all three invariant populations;
- exact per-leaf mount octets and one total resident population;
- the fixed device-side causal order actually enacted;
- no invariant re-upload between fronts;
- no CPU semantic replay or CPU choice of continuation;
- the complete affine reconstruction fibre, L2 current, and participant/potential consequence;
- only dynamic later current and typed receiver disturbances as successor ingress; and
- an exact obstruction if the device cannot retain or compose the requested front.

[definition] A wrapper which internally creates three contexts does not pass. Nor does an event
counter, a host-side sequence of otherwise resident leaves, or a digest claiming their outputs
belong together. The same context must own the allocations and enact the native word before the
exterior surface codec acts.

## Affine and Hodge boundary

[proved-derived] The one-card repair is an apparatus realization of the already-derived affine
bundle relation. The base landmarks and the rank-four fibre are co-resident but remain different
types; the later field is `mu_c tensor x_e`. A future Hodge/constitutive receiver may dualize this
complete field after metric, orientation, and degree are declared. It is not authorized to choose
one landmark or to replace the resident continuation by a scalar score.

## Pass condition

[definition] The resident repair passes only when a focused release receiver reports one shared
context, one-time invariant mounts, the exact affine and coupled returns, the participant/native
potential consequence, no host semantic seam, and exact rest recovery. Only then does L5 regain its
full apparatus grade and L6 become active.

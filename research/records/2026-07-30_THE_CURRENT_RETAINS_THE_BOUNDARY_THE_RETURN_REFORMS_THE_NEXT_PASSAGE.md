# THE CURRENT RETAINS THE BOUNDARY; THE RETURN REFORMS THE NEXT PASSAGE

**Date:** 2026-07-30  
**Status:** EXACT SPARSE CAUSAL TRAVERSAL BUILT · TYPED CURRENT SPECIES BUILT ·
REACTIVE STORAGE/MATERIAL/MORPHOLOGY BUILT · REAL MMS HELD-OUT GENERATION
RETURNED · FIELD/MATERIAL COUPLING REMAINS OPEN  
**Production owners composed:** `ExactCausalBodyLaw`, `CausalWorld<L>`,
`ExactRatMatrix`, and the new `ExactCausalTraversalLaw`  
**Application instrument:**
`crates/holonic-engine/examples/mms_reconnection_traversal.rs`  
**Source membrane:** `data/mms-reconnection/2015-10-16T130655-130710Z/`  
**Returned testimony:** `output/mms-reconnection/causal-traversal/`

## The construction preserved

The ratified next composition was not another line animation or a fixed
mathematical graph. The missing physical capability was a common exact organ
in which:

1. a caused body supplies the admissible local carriers;
2. distinct currents travel together without becoming one commensurate scalar;
3. co-present currents superpose at the receiver chronology where they meet;
4. a local interface may split current into outward travel, retained boundary
   current, and material current;
5. the retained state changes the constitutive morphology used by the next
   passage;
6. a finite receiving horizon leaves later current open rather than deleting
   it or inventing an iteration cap; and
7. the returned difference is received only after the generated consequence
   has been emitted and inspected.

This is the shared traversal seam between the caused hypergeometric body and
later analytical-wave, fluid, optical, electromagnetic, biological, and
cosmological doctrines. It does not flatten those doctrines into one
equation. Each application still owes its local species, constitutive
interaction, admissible balances, and receiver return.

## The exact sparse traversal owner

`crates/holonic-engine/src/causal_traversal.rs` now owns:

- an ordered direct sum of named exact current species;
- directed passages supported by already-caused source cells;
- positive physical passage delay;
- one sparse scheduled frontier ordered by local arrival chronology;
- exact co-present superposition at equal `(chronology, site)`;
- separate positive and negative arrival populations even when their net
  current cancels;
- local retained boundary current, material current, and morphology;
- affine morphology-conditioned constitutive operators;
- exact linear and quadratic conservation-family certificates;
- event and receiver chronology as distinct orders;
- a persistent open frontier beyond the current receiver horizon; and
- complete exact interaction, passage, balance, lineage, rest, and remount
  receipts.

The traveling fiber is

\[
\mathcal C=\bigoplus_s\mathcal C_s.
\]

Concatenation is only an exact carrier layout. A magnetic coordinate, electric
coordinate, ion velocity coordinate, and density coordinate retain distinct
species identity, extent, units, balances, and receiver interpretation.

At a reached cell, the physical input is

\[
x=[j_{\mathrm{in}}\mid b\mid m],
\]

where \(j_{\mathrm{in}}\) is arriving traveling current, \(b\) is retained
boundary current, and \(m\) is material current. The contemporary local
operator is

\[
T(\lambda)=T_0+\sum_i\lambda_iT_i,
\]

and its output is

\[
y=T(\lambda)x
 =
[j_1,\ldots,j_d\mid b'\mid m'].
\]

The later morphology is not selected by an external learner:

\[
\lambda'=F[\lambda\mid b'\mid m'].
\]

It is the exact retained organization of the local intermediate body. The next
arrival therefore encounters the return-conditioned operator \(T(\lambda')\).

For every declared linear balance with covectors \(a\) and \(c\), construction
proves

\[
c^\mathsf TT_0=a^\mathsf T,
\qquad
c^\mathsf TT_i=0
\]

before standing exists. For every declared quadratic balance with forms
\(G_{\mathrm{in}}\) and \(G_{\mathrm{out}}\), construction proves the complete
polynomial identity

\[
T(\lambda)^\mathsf TG_{\mathrm{out}}T(\lambda)
=G_{\mathrm{in}}.
\]

Every enacted interaction and passage then returns its exact zero residual.
A noninvertible or nonconservative passage is refused at construction.

## Cancellation is an occurrence, not absence

If two currents \(j\) and \(-j\) reach the same site at the same chronology,
the traveling net is zero, but the receiver has still received two caused
arrivals. Standing therefore retains

\[
j_{\mathrm{net}}=j^+-j^-,
\]

together with \(j^+\), \(j^-\), arrival population, and complete cause union.
The zero net may still release retained boundary current or change a local
interaction. This is the computational distinction between destructive
interference and no occurrence.

## The receiver horizon is not an iteration budget

Each passage has positive delay. An event advances every sparse front whose
physical chronology is within the participating receiver's inclusive horizon.
A later front remains in standing at the same exact address. A later receiver
horizon resumes that same current.

The display loop, frame rate, vector length, and authored iteration count do
not schedule the physics. A finite event is caused by the finite receiver
horizon; an open future is retained rather than truncated.

## Real four-spacecraft application

The first production instrument uses the well-studied 16 October 2015
magnetopause electron-diffusion-region crossing around 13:07 UTC. NASA's MMS
mission uses four identically instrumented spacecraft in tetrahedral formation
to resolve magnetic reconnection, and the event is the same crossing treated
by Burch et al. and later reconstruction studies:

- [NASA MMS mission](https://science.nasa.gov/mission/mms/)
- [Burch et al., *Electron-scale measurements of magnetic reconnection in
  space*](https://doi.org/10.1126/science.aaf2939)
- [Hasegawa et al., *Reconstruction of the electron diffusion region observed
  by the Magnetospheric Multiscale spacecraft*](https://doi.org/10.1002/2017GL073163)

`scripts/acquire_mms_reconnection.sh` obtains exact textual receiver material
through NASA CDAWeb/HAPI:

- burst FGM magnetic field in GSE;
- fast EDP electric field in GSE;
- burst FPI ion number density and bulk velocity in GSE;
- MEC spacecraft positions in GSE; and
- one-minute OMNI upstream magnetic, velocity, density, and pressure context.

The acquired membrane contains 34 CSV/metadata source files. Its complete
SHA-256 table hashes to:

```text
16be32a1f135a7d04be3249f03b505a2911e3a45a6bbf493688756349e6f13ce
```

Every decimal enters as the exact rational denoted by its source text. FGM,
EDP, FPI, and MEC retain their distinct receiver clocks. Positions are exactly
linearly restricted between adjacent MEC sections; other measurements retain
their nearest measured occurrence and signed offset. The largest cross-modal
offset in this cut is `90,025,239 ns`. It remains visible in the receipt rather
than being called simultaneous.

The body has four MMS receiver vertices, their complete six-edge tetrahedral
incidence, four oriented faces, and one tetrahedral volume:

```text
f-vector = {0: 5, 1: 6, 2: 4, 3: 1}
```

The fifth grade-zero cell is the OMNI upstream receiver. It remains isolated
in this construction because no measured local travel path from that
bow-shock-shifted one-minute section into this fifteen-second tetrahedral cut
was enacted. Its values are context testimony, not a fabricated edge.

## The moving face and returned exterior fiber

At each joined chronology, let the spacecraft positions be \(r_1,\ldots,r_4\).
Set

\[
a=r_2-r_1,\qquad b=r_3-r_1,\qquad d=r_4-r_1.
\]

The exact Gram determinant

\[
D=(a\cdot a)(b\cdot b)-(a\cdot b)^2
\]

is required to be nonzero. The projection of \(r_4\) into the first three
receivers' moving face has weights

\[
\begin{aligned}
w_2&=\frac{(b\cdot b)(d\cdot a)-(a\cdot b)(d\cdot b)}{D},\\
w_3&=\frac{(a\cdot a)(d\cdot b)-(a\cdot b)(d\cdot a)}{D},\\
w_1&=1-w_2-w_3.
\end{aligned}
\]

Each receiver occurrence carries the ten-coordinate typed section

\[
X_i=(B_x,B_y,B_z,E_x,E_y,E_z,v_{ix},v_{iy},v_{iz},n_i).
\]

The face quotient is

\[
F_k=w_{1,k}X_{1,k}+w_{2,k}X_{2,k}+w_{3,k}X_{3,k}.
\]

The fourth receiver's returned exterior fiber after occurrence \(k\) is

\[
C_k=X_{4,k}-F_k.
\]

After the first fourth-receiver section enters as inherited standing, every
later consequence is generated before its own fourth-receiver return:

\[
\widehat X_{4,k}=F_k+C_{k-1}.
\]

Only then is \(X_{4,k}\) received and graded. Its exact residual is

\[
R_k=X_{4,k}-\widehat X_{4,k}=C_k-C_{k-1}.
\]

That residual reforms the retained boundary current, so the next generated
section is a consequence of the returned history. Equivalently,

\[
\widehat X_{4,k}
=X_{4,k-1}+(F_k-F_{k-1}).
\]

The grade against stale continuation therefore asks a precise local question:
does the changing three-receiver face transport the fourth receiver's next
difference better than assuming no change?

## Conditioning, held-out generation, and return

The fifteen-second source cut yields 240 joined occurrences after the declared
FGM stride of eight. The first 80 occurrences before 13:07:00 UTC condition
the retained exterior fiber. The remaining 160 are a successive held-out
chronology:

```text
face change
    -> generated MMS4 typed section
    -> complete generated section emitted
    -> actual MMS4 return
    -> exact residual grade
    -> residual enters later standing
```

This is not a fully open-loop forecast of all 160 occurrences. Each held-out
occurrence is hidden until its own prediction is emitted; its later return may
condition the following occurrence.

All 1,600 held-out species components were generated. No root answer, fitted
float, random draw, least-squares coefficient, or copied MMS4 value enters the
generation.

| current species | generated components | better than stale | worse than stale | exact ties |
|---|---:|---:|---:|---:|
| magnetic field | 480 | 311 | 169 | 0 |
| electric field | 480 | 217 | 263 | 0 |
| ion bulk velocity | 480 | 136 | 344 | 0 |
| ion number density | 160 | 41 | 119 | 0 |
| **complete typed current** | **1,600** | **705** | **895** | **0** |

The exact receiver-relative improvement populations are consequently:

\[
\frac{311}{480}\quad(B),\qquad
\frac{217}{480}\quad(E),\qquad
\frac{17}{60}\quad(v_i),\qquad
\frac{41}{160}\quad(n_i).
\]

These are not asserted as universal probabilities or scalar loss. They are
finite measures over this declared held-out receiver body.

The split is itself the useful result. The moving face carries a substantial
majority of the fast magnetic differences and a smaller portion of the
electric differences. Carrying low-cadence ion moments through the identical
face quotient is usually worse than retaining the last fourth-receiver
section. Geometry alone did not fail globally; the return distinguishes two
local regimes. The field sections are more coherent under this bounded
moving-face transport, while the material sections require an enacted
field/material constitutive interaction and more careful chronology than
unchanged coordinatewise passage.

The engine emitted 19,130 exact interaction and passage balance receipts.
Every residual is zero. The final sparse frontier is empty, and serialized
standing validates and remounts byte-for-value identically.

## Returned membranes

- `generated-mms4.tsv` contains every generated and returned component,
  residual, stale comparison, and exterior fiber before/after return;
- `tetrahedral-sections.tsv` contains exact moving-face weights, Gram
  determinant, signed volume, magnetic face quotient, receiver exterior
  fiber, and clock testimony;
- `traversal-interactions.tsv` contains every complete ten-coordinate arrival,
  positive/negative population, retained state, passage, and balance;
- `traversal-standing.ron` is the exact rested traversal standing; and
- `summary.tsv` contains the declared split and complete result populations.

Current testimony hashes are:

```text
summary.tsv
fa79a36777c918c538c91610278688bf42ef8a58e1130b4508edbe166e2e9de8

generated-mms4.tsv
b4f280f345073d1d4968434b28bd599683c2c1acbdeed7b299b07e54ec51bbbd

traversal-interactions.tsv
4172c4a7f4683628d66727fe77766290ed3711dba1fe6a612b503be2a6dc524b

traversal-standing.ron
ad1ae86746ee3e5e98789595b5a51b945d04fd1deba80135a94a5050aa356449
```

## What this establishes

The engine now has a reusable exact traversal owner in which:

- the source body, not projected proximity, admits a passage;
- arbitrary named exact current species share chronology without losing type;
- co-present changes propagate through one sparse causal front;
- cancellation retains its positive and negative occurrence populations;
- boundary and material current can persist after traveling current departs;
- that persistence can reform the next constitutive operator;
- linear and quadratic conservation apply to the complete morphology family;
- finite receivers leave later current open without a procedural step cap; and
- a real heterogeneous four-receiver ecology generates a complete held-out
  typed section before return.

This is the algorithmic traversal through hypergeometry requested for later
Riemann/Zeta, optical, fluid, biological, planetary, and cosmological
applications. Those domains need not share MMS semantics. They can share this
caused passage, local interaction, retained boundary, receiver horizon, and
returned-obstruction machinery while installing their own current species and
constitutive laws.

## The next concrete physical conditioning capability

The return has resolved the next application boundary. It is not “derive a
general law” and it is not “try a larger dataset.”

The next magnetospheric interaction must let co-present \(E\), \(B\), charge
density, and material velocity change each other's later conduct. Candidate
receiver receipts include exact discrete counterparts of:

\[
E+v_i\times B,
\qquad
S=E\times H,
\qquad
\partial_t u+\partial J=P_{\mathrm{field\leftrightarrow material}},
\]

with the relevant units, dual/primal placements, orientation, calibration,
and energy balance declared rather than inferred from coordinate
concatenation. The asynchronous FGM/EDP/FPI sections must remain separate
local clock fibers until the conditioned interaction gives their joint
occurrence physical meaning.

That construction can then ask whether the returned field/material exchange
distinguishes the local reconnection topology, whether the normal fiber
branches or changes sign across the tetrahedral volume, and whether the OMNI
upstream receiver can lawfully enter through a measured or model-certified
propagation path. Only after such a path exists may the larger magnetosphere,
solar wind, ocean/atmosphere, stellar, or cosmological ecology be assembled
without inventing global adjacency.

## Exact qualifications

This construction does not claim:

- a complete Maxwell, Vlasov, Hall-MHD, or reconnection simulation;
- that four in-situ receivers determine the complete local field;
- that one planar affine quotient is intrinsic hypergeometry;
- that the ion and field sections are simultaneous;
- that OMNI has already propagated into the MMS tetrahedron;
- that improvement counts are universal probability or scalar loss;
- that an exact rational representation makes imported measurements exact
  descriptions of nature rather than exact source testimony;
- an open-loop prediction of the complete held-out interval; or
- that the generic morphology mechanism has already learned the missing
  field/material interaction in this application.

It establishes the common exact organ, emits the requested real generated
deed, and lets the return identify which local coupling must be conditioned
next.

## Verification

```bash
cargo test -p holonic-engine causal_traversal::tests -- --nocapture
cargo run -p holonic-engine --example mms_reconnection_traversal
```

The focused traversal suite returns seven passed and zero failed. The complete
`holonic-engine --all-targets` surface returns 244 library tests passed, two
CUDA-only tests ignored, and all example targets built; the desktop example
adds eight passed and two device/cost tests ignored. The real instrument
verifies all 34 source digests, emits all 160 held-out typed sections, returns
19,130 zero balance residuals, reaches exact rest, and remounts equal.

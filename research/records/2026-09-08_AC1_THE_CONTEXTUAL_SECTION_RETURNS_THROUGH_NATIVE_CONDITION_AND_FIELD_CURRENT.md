# The contextual section returns through native condition and field current

[definition] This implements the resident connection posed by the
[source-null derivation](2026-09-07_AC1_THE_SOURCE_NULL_RETURN_DERIVES_ITS_CONTEXTUAL_LIFT.md).
It returns immutable contextual material, uses its Preimage Fibre in the existing actual-current
contact, and carries its generated current into the same continuing conversation field. The
actual-data study still supplies its historical passage pair explicitly. Automatic contextual
formation and reuse during ordinary cultivation, useful responses and Athena-alpha remain open.

## The fixed-source section retains mixed conduct

[definition] For the existing bound contact `Phi(s,c)=(s,c,s tensor c)` and joint relation `R`,
the new `ResidentConstitutiveFibre::contextual_section(s)` returns

```text
D_s = { (dc,dy) | (0,dc,s tensor dc,dy) belongs to R }.
```

The actual fixed source belongs to the result. Setting the direct source variation to zero
does not remove the mixed term or declare an arbitrary flattened array to be a condition.

[proved-derived] Direct expansion gives
`Phi(s,c+dc)-Phi(s,c)=(0,dc,s tensor dc)`. Consequently two admitted passages at that source
have a difference in `D_s`. Conversely, if `(Phi(s,c0),y0)` belongs to `R`, adding any member
of `D_s` gives `(Phi(s,c0+dc),y0+dy)` in `R`, because `R` is a linear relation in its declared
lifted chart. This is a local conditional transport statement, not global linearity of HNA.

[established-bounded; source-inspected] `constitutive_context_section.cuh` builds the graph of
the complete residual map `P_R(0,dc,s tensor dc,dy)` and extracts its kernel on device. It uses
every original pivot, including vertical target directions. The resulting relation and fixed
source are immutable and retain their producing relation cut; later parent deposits cannot
rewrite them. The graph is newly derived evidence, not a copy of a continuing ecology.

[definition] `read_change(dc)` returns the whole supported target fibre. For an explicitly
retained actual reference `about`, `preimage_change(about,dy)` returns

```text
{ c' | (c'-about,dy) belongs to D_s }.
```

The native operation translates the condition origin of the joint relation and uses the existing
condition-image receive kernel. This keeps absolute condition coordinates separate from a
condition difference. The existing `ResidentConditionCurrent::contact` then supplies the actual
successor, normal incoming/returned currents and full difference under its declared metric.
Free directions retain the actual prior current. Unsupported evidence keeps its scoped
obstruction; an affine particular solution is not installed as a cause.

## A bounded field contrast supplies generative material

[definition] `NativeConstitutiveField::derive_contextual_contrast([r0,r1])` binds two actual
source-null receiving passages to that same native section interface. The GPU checks that their
root source faces coincide and are nonzero, their returns differ, and their complete contextual
contrast excludes zero under its retained error. Context comes from the receiving predecessor,
before its target arrives. Both source and context epochs, both source expressions and the
oriented `(-1,+1)` combination are retained. No exact contextual coordinates are selected.

[proved-derived] With the preceding conditions, the established real coefficient chart is

```text
c(alpha) = c0 + alpha (c1-c0),
y(alpha) = y0 + alpha (y1-y0),       alpha in Q.
```

This is the partial map derived from the two actual observations under the local linear
hypothesis. Its operation needs the coefficient and the retained target difference; it does not
need a numerical representative of the unknown exact `c0` or `c1`. The complete compatible
extension family outside the established contrast span remains open. The second coordinate of
the standard complex current carrier is outside this real-ray domain; an unobserved imaginary
multiple is not silently added as evidence. The ambient paired context is not declared to be
one-dimensional.

[definition] Coefficient zero names the actual first contextual reference in this chart; it
does not mean zero physical context. The unit-admittance contact metric is declared on these
coefficient coordinates. An isometry to the ambient paired-current metric, physical conservation
for the entire composed study and measured power have not been established.

[established-bounded; source-inspected] `field_context_section.cuh` returns the native section,
the actual first and second receiving currents, the shared source, and the squared-norm/error
receipt. `read_absolute` executes the affine output origin plus the derived change relation.
`preimage_absolute` subtracts the actual origin on device and returns the full supported
coefficient family. The section remains usable after the originating field is dropped; this
does not assert that the unknown ambient physical context has been decoded or restored.

## The generated current crosses the text-session boundary

[established-bounded; source-inspected] `TextFieldSession::receive_native_return` retains the
immutable native return and uses `advance_current_resident` for the ordinary next field
occurrence. It reads the terminal codeword/status receiver, while the actual amplitude and
phase remain on device. The accepted symbol is exterior metadata; the input is not rebuilt
from that symbol's unit-impulse encoding. This API requires a supported actual point and an
admitted text codeword. Other native-current interfaces retain their own receiver domains.

[established-bounded; source-inspected] `ConstitutiveReturnRest` stores an immutable completed
return for pending I/O, not a model or standalone generator. The optional pending-native member
of the existing text checkpoint preserves the current and its source capability after a known
refusal. Cold validation checks its point/current and exterior-symbol correspondence. Legacy
checkpoints omit this optional member and retain their existing representation. A native point
without a pending exterior symbol is rejected rather than lost during remount.

## Verification and actual conversation return

[established-bounded; measured] All 138 constitutive tests pass in 31.76 seconds. The new
controls exercise the fixed-source mixed term, a measured phase change, partial domains,
vertical targets, translated reference conditions, free/empty evidence, frozen producing cuts,
native field-contrast construction, new native relation formation and re-entry into the same
field. All 19 SDK alpha tests pass in 2.11 seconds, including full-amplitude restart, pending
native-current refusal/retry and the missing-symbol rejection. Numerical current readbacks
do not supply those productive operations; the text-current bridge reads one five-word terminal
receiver, costing 80 transferred section bytes in the measured path.

[established-bounded; measured] The
[portable actual-model receipt](2026-09-08_native_contextual_return/conversation-return.json)
records `alpha_contextual_return` on the eight-family, 1,930-occurrence checkpoint. It derives
the section from receiving occurrences `12,20`, with source/context occurrences `11,19`.
The ambient contextual chart has 73 complex coordinates. One actual retained return changes
the coefficient current from zero to one. One explicitly declared self-return through the same
preimage/contact law changes it from one to two. This self-return is not an external world verdict.

[established-bounded; measured] The generated current `2*y1-y0` enters that same field as
occurrence `1930`, producing a 1,931-occurrence successor. Its real entries include `-1` and `2`;
ten entries differ from the unit encoding of its decoded symbol. The native path takes
0.037312132 seconds, 12 deeds, one terminal section read and 63,692 ingress bytes, including
physical pointer charts. The new checkpoint is 54,297,848 bytes. The source model and earlier
failed results remain intact under `.local/artifacts/athena-alpha/`.

[established-bounded; measured] After that operation, the existing prompt “Explain the
difference between a function and its derivative.” receives only `v`, followed by an unresolved
receiver. The full process takes 27.15 seconds. A fresh process restores the 1,931-occurrence
successor without re-enacting contextual construction or its already committed input. It
retains the exact native input and reproduces the same generation receipt, in 18.71 seconds
overall. This is a failed language result; a changed response is not an improvement claim.

[definition] The checkpoint retains the native field/text session and an opaque contextual-study
application envelope containing the original application bytes and actual event boundary. Use
the public SDK, or the example's `--continue` mode, to continue it. It is not the exposure-only
application ledger expected by `alpha_text --resume`. The contextual section and condition
controller are composed explicitly by this study; this is not yet an automatically retained
contextual ecology participating in every ordinary cultivation event.

[open] AC1 next integrates this established native material and current/contact path into the
continuing owner’s ordinary formation and reuse, with actual source-null provenance and complete
successor/refusal handling. The application driver must cease selecting the historical contrast
and performing the developmental composition. AC2 still requires useful English, code and
mathematics responses with contextual followups. The broader AC0–AC5 objective remains active;
no inherited model, source quotation selector, new precision gate or output filter completes it.

# The geometric field refines and returns its complete paired current

**Date:** 2026-09-20. **Campaign base:** `fbe0adcc`.
The [roadmap](../../docs/plans/THE_ROADMAP.md) owns order and
[construction state](../../CONSTRUCTION_STATE.md) owns the next consuming operation.

[project-postulate] Brandon authorized Codex to lead the finalized campaign with Luna workers.
This return composes shared Holonics fundamentals into the HNN field/session, advances the
helical moment/phase caller, and carries actual recorded context across restart. Protein and
biology continuation remain deferred. Existing mathematical and application returns retain their
scopes; this increment does not reset the framework to its small verification cases.

## The source geometry reaches the actual model

[established-bounded; source-inspected] `native/field_geometry.rs::GeometricFieldSpec` consumes
`CausalFieldStanding`, `ExactAnalyticFieldJunction`, `ExactAnalyticFieldArc` and
`ExactAnalyticFieldMode`. `ExactAnalyticFieldWaveLaw::new` validates the existing support,
shared-junction, phase, mode and source-event relations. The compiler extracts admitted incoming
arcs and their complete declared modal phase transport, grouping rows by actual incidence width.
It does not infer contact from arbitrary co-presence or rebuild torus geometry from a keyword.

[definition] `GeometricRegions` is a new source chart of the existing `NativeFieldSession`.
Its ordered slot-to-junction map is the exterior source/receiving chart. Symbols enter through
the existing codeword basis; their positions do not become intrinsic semantic identities.
All declared junctions participate in the field, including sites outside the emitted boundary.
The local shared reaction width depends on its channel and condition chart, while the source
population supplies resident rows. Section and context have declared part limits and a checked
**joint** slot constraint at each request. Older source charts and rests remain readable.

[established-bounded; source-inspected] The supplied linked-torus instance uses two perpendicular
radius-2 cores, minor radius 1, at centres `(0,0,0)` and `(2,0,0)`. Their exact inner-longitude
junction is `(1,0,0)`; the atlas supplies the glued overlap. Endpoint-derived rational phase
transports traverse each longitude in order. This is a supplied boundary-network instance of the
analytic source, not the whole volume/advection/Hamiltonian specialization of the intrinsic-flow
reference. The generic source adapter also accepts other validated analytic junction/arc families.

## One nonlinear word and its complete return

[definition] For a receiving site i and its admitted incoming arcs j, let `u_j=U_j k_j` in the
receiving frame. The implemented amplitude/phase comparison is

```text
s_j = β Re⟨q,u_j⟩,           p = softmax(s),           y = Σ_j p_j u_j,
Φ(y,p) = y ⊕ p ⊕ (p ⊗ y),   incoming = y + M Φ(y,p),
r = S_D(incoming,b),
x_next = H seed + (I−H)((1−μ)seed + μ r),              μ = 2^(−relaxation_bits).
```

[definition] On unit single channels the first expression is the established cosine phase
comparison. General amplitudes remain present. U, D and b are standing operands during one
word; the caller declares β, the exponential aperture and a finite refinement extent. This
relaxation is not asserted to be the intrinsic reference's Hamiltonian shear, energy conservation
law, or universally convergent iteration. Held receiving coordinates remain held through every
stage. The word acts on the joint field and then emits its boundary; its stage index is not an
output-token clock.

[proved-derived] For covectors `g_y` and `g_p`, the complete phase return is

```text
r_j = Re⟨g_y,u_j⟩ + Re(g_p[j]),      h = J_p r,      J_p = diag(p) − p pᵀ,
g_q = β Σ_j h_j u_j,                g_u[j] = p_j g_y + β h_j q,
g_k[j] = U_j* g_u[j].
```

[definition] The native implementation forms bounded logits, calls the **existing** normalized
section receiver, and consumes its resident participation. Reverse reception calls that owner's
pullback on r; it does not substitute a second softmax or a score-only derivative. The producing
query, neighbors, normalized report and weighted face are retained as immutable carriers.
The bilinear feature return and the stored M transpose carry the condition branch as well as
the current branch. The field's existing self-adjoint fixed-D reflection supplies its paired
return. Transposed gather adds every repeated source address after applying U*.

[proved-derived] The refinement derivative contributes `(I−H) μ g` to the generated field and
`[H+(I−H)(1−μ)]g` directly to the seed at **each** stage. Reverse traversal accumulates all these
seed terms and returns through every intervening phase/material operation. Holding D fixed is
an explicit derivative scope; changing D, constitutive geometry or a participating receiving
boundary requires their additional declared variation.

[definition] Local reaction targets are the produced reaction plus its returned covector.
All stage feature/target rows are concatenated on device and supplied to one existing normal
update. M remains fixed until the complete return is staged. If the preceding normal equation
is exact, this update has the familiar form
`M_new = M_old + (Σ g_l Φ_l*) (H_old + Σ Φ_l Φ_l*)⁻¹`;
otherwise the old normal residual also participates. The normal owner's retained numerical
and reference residuals keep that distinction visible. This is the declared local normal
learning law, not a claim that an emitted text face already satisfies an application request.

## Native realization and ownership

[established-bounded; source-inspected] `normal/direct/section/composition.rs` now composes
phased gather/transpose, held affine refinement, enclosed bilinear features and both input
covectors, the stored normal-map transpose, and resident row concatenation. Exact phase ratios
use the existing unit-phase owner and the current point-packet carrier; unrepresentable ratios
remain an explicit carrier result. Every enclosure retains its radius and every mixed term.
The transpose and bilinear controls exercise signed/imaginary coordinates and positive radii.

[established-bounded; source-inspected] Applied-condition and field-reflection section calls
now place rows in separate blocks. Reflection work/dot storage and refusal words belong to their
rows and stay alive through launch. Row receipts are joined by a separate deterministic status union;
other rows do not disappear after a scheduling-dependent shared early exit. Normal fitting is
one joined material reduction, not independent concurrent mutation of M.

[established-bounded; source-inspected] The public geometric call certifies its staged packet
rows through the existing `SectionPartition` and the mounted device cover. Its logical packet
includes both encoded word lanes and the complete row lineage receipt. D/M are opaque immutable source ports;
these are not fabricated physical byte counts. Reverse read support is explicitly conservative.
The placement evidence is reused for unchanged geometry, channel widths and device/kernel mode.
Cover-derived work is distinguished from profiler observations. The source geometry and its
placement are therefore part of the actual call, not a diagram or a test-only companion.

## Helical moments and lifted phase

[established-bounded; source-inspected] The new situated-pair adapter calls the existing
`ObservableMomentReceiverHistoryCompression`. Proper finite affine actions act on an 8-coordinate
homogeneous pair moment; the quadrance receiver is the shared pair specialization. The encoder,
induced actions, decoder and an ordered word's complete coordinate identity remain visible.
The returned word retains its whole generator sequence. No second compression algorithm is founded.

[definition] The decoder's kernel is an **ambient linear moment fibre**. Real pair configurations
also satisfy symmetric rank-one and fixed homogeneous-coordinate constraints, with any admitted
orbit/clock relation. An arbitrary kernel direction is not automatically a physical pair cause.
A changed receiver's separator is labelled at that ambient scope.

[proved-derived] A finite Cayley phase carries its tangent-half-angle coordinate and extra full
turns per elementary passage. A closed period's lifted winding is
`w_lifted = w_polygon + period × extra_turns`. Quarter turns with zero extra turns close in four
steps with winding 1; one additional full turn per passage gives winding 5. A nonclosed period
returns its exact endpoint discrepancy. Proper maps and the supported phase/map relation are
validated; an arbitrary finite affine action is not silently called `exp(t ξ)` for a screw whose
clock relation has not been supplied. [Helical guide](../../docs/HELICAL_GEOMETRY.md).

## Recorded context and episode assessment

[established-bounded; source-inspected] `ExposureReader::recorded_context` now resolves parent
families and capture aliases through an incrementally indexed **prior source prefix**. Entries
are stored once, ambiguous resolutions stay explicit, and context must fit the caller's cumulative
byte aperture. Source extents and strictly preceding sequences bound the work; no new arbitrary
million-entry or fixed-megabyte semantic limit was introduced. A malformed future frame cannot
invalidate a valid preceding context. Reopened readers rebuild the needed prefix metadata and
then seek only the selected parents; the scan and lookup costs are reported.

[established-bounded; source-inspected] The exposure driver consumes that context instead of a
process-local raw-frame cache. Existing capture aliases, retained response extents, retryable
cursor behavior and shared comparison semantics remain. The existing retrospective episode
preparer now accepts its corpus codec's `agent-visible` role. The existing quality kit consumes
`input.json`, the separate `assessment.json`, and an actual generated result; recorded candidates
remain nongold observations, and later historical human replies are not feedback on the new run.
Encoding/extent measurements and identified source-constraint judgments remain distinct.

## Verification and actual source returns

[established-bounded; measured] The public geometric session preserves held symbols, changes M
through an observed target, retains a pending comparison and reproduces its receiving result
after restart. Its first device process took 219.95 s. The two-step directional derivative check,
after actual material learning, passed in 0.57 s in a later process; these different test scopes
are not a matched speedup benchmark. It compares the negative input covector with a rational
central difference of the actual generated-field loss at source offsets ±1/4096.

[established-bounded; measured] Three phase device controls and five initial enclosure composition
controls passed before the large-row integration check below. The existing applied-condition section controls and 17 retained field-session
checks also passed after the row changes. Scoped command/tree receipts are retained in
[VERIFICATION_RECEIPTS](../../docs/VERIFICATION_RECEIPTS.tsv). These initial passes did not cover
the complete row-receipt allocation contract; final verification follows the repair below.


### Integration defects and their owning repairs

[established-bounded; measured] A 1,231-site request intermittently refused its first phased
gather. CUDA memcheck exposed an out-of-bounds write: the new row buffers held one i64 flag,
while `upstream_refused` writes a complete 16-u32 `SLOT_WORDS` receipt. The helper's lineage
writes crossed the row allocation. A successful small kernel test or one successful real run
therefore did not certify this layout. All affected gather/phase/condition/reflection paths now
allocate and stride complete receipts. The normalized section dependency also owns independent
row receipts; a refusing row cannot suppress sibling computation through a shared status word.
The final barrier preserves refusal and common-lineage metadata. Both operating guides and the
placement owner now state the actual ABI. A 1,231-row signed-phase control exposes the boundary
under memcheck, alongside the real application check.

[established-bounded; source-inspected] A separate readiness defect remained after the receipt
repair. Intermittent gather refusals named operands that were fully sealed, with nonnegative
radii and valid phase maps when read back after the failed call. The synchronous CUDA upload
wrapper returned after pageable host staging; the following nonblocking passage had no ordering
edge from the unfinished device transfer. The shared mount wrapper now completes the legacy
transfer stream for its synchronous slice/range/VMM uploads. Explicit asynchronous copies retain
their caller-owned dependency. This changes the readiness contract, not the mathematical input.
[CUDA documents that host staging can precede DMA completion](https://docs.nvidia.com/cuda/cuda-driver-api/api-sync-behavior.html).
Instrumented runs alone had masked this timing defect: memcheck reported zero errors on the
forward request and initcheck zero errors on its observed return. The ordinary split/whole
process comparison after the readiness repair is the application evidence below.

[established-bounded; source-inspected] The real trained checkpoint also exposed repeated host
source-energy validation. `GeneratorNeighborhoodRest::{rest,write}` called the full immutable
child `NormalMaterialRest::validate` again through their parent validator. The child has private
fields, fully validating public wire/chart constructors, and an admitted private native snapshot
constructor. Parent validation now checks its cross-owner chart/target/evidence relations;
untrusted `NormalMaterialRest::read` still checks the complete geometry and numerical witness.
A nested parsed-but-invalid target-energy payload is rejected. This repairs redundant checkpoint
proof work; it does not replace the exact untrusted-rest validation algorithm.

[established-bounded; source-inspected] The first source-run adapter compared checkpoints after
a refused split stopped one frame early. Their byte difference was not evidence of a restart
law defect. The adapter now checks actual acknowledged positions, a retained geometric comparison
at the split, its reopening and drainage, and exactly one paired update in both continuations
before comparing bytes. Private failure diagnostics retain the actual incomplete phase.

### Ordinary source execution after the readiness repair

[established-bounded; measured] The selected development source retains two recorded parent
occurrences (564 visible UTF-8 bytes), a seven-byte request and its later recorded response.
Only the request and declared parents enter generation. The declared free aperture is 32
nibbles (16 bytes); the longer recorded response supplies that explicit prefix as an observed
candidate. The earlier 296-byte parent exceeds this run's request aperture and is counted as
skipped for training; its recorded response is correspondingly unpaired. Both remain available
as the later request's source context. Source file SHA-256:
`7e2911c152837c85f2d8f57ecabb3d2be3eeb93e240a13fb0c7001033cbb06df`.

| Process on the same declared source | Generation | Observed update | Whole process including setup/save |
|---|---:|---:|---:|
| Stop after the request, with its comparison pending | 3.853356 s | — | 10.155962 s |
| Reopen that pending comparison and consume its recorded response | — | 6.839450 s | 12.573847 s |
| Uninterrupted source through request and response | 3.810548 s | 4.517622 s | 14.583076 s |

[established-bounded; measured] The split acknowledges 3/3 selected frames; resume acknowledges
1/1, opens the retained comparison and applies it once. The uninterrupted run acknowledges 4/4
with the same one material update. Neither has a native refusal. Both trained checkpoints are
**3,594,009 bytes**, with identical SHA-256
`31c47dc89f321c68bd8f571c591517fca5db5f871f15c585b21658241e448953`.
These are individual declared process observations; they are not population latency estimates
or speedups against the different historical shared-region model.

[established-bounded; measured] A second real process cut stops **before** the request.
After reopening, `recorded_context` rebuilds the two-entry prior prefix, fetches both parents,
generates the request and applies the response. Its final checkpoint is also byte-identical to
the uninterrupted endpoint. This exercises resumed source lookup itself, separately from
reopening a request whose comparison was already pending.

[established-bounded; measured] On the RTX 4080 SUPER, the uninterrupted process reports a
peak **94,182,996 bytes** of resident sections, 15,236,100 ingress bytes, 4,773,072 section-egress
bytes including checkpoint work, and 12,928 receipt-egress bytes. Its surface census records
201 passage synchronizations and 24 section readouts. These counters exclude GPU context/driver
memory and waits performed inside the synchronous upload wrapper. They do not measure physical
active-lane occupancy. Source prefix indexing scans 4,092 wire bytes and fetches two parents.
The former 250.5-second completed process included redundant native-rest validation; current
whole-process timing includes the receipt/readiness repairs as well as removal of that replay.

[established-bounded; measured] The separate retrospective evaluation request contains
438 supplied bytes, no additional declared context, and 128 free nibble faces. The native
request takes **3.721350 s**; opening the trained untrusted rest, executing and saving the process
takes **132.208745 s**. That remaining setup/validation cost stays visible in #18. All supplied
bytes are held, but the 64 generated bytes begin `ee ff` and continue with `ff`: **invalid UTF-8**,
with only 2/128 robust free symbol faces. This is not a useful response. The existing quality
receiver records that failure and binds the direct-source judgments to the new result's SHA.
Its six historical candidates and one later observation are separate nongold assessment material,
not new feedback. The input is a declared retrospective source selection, not a recovered complete
prompt or blind holdout, and it contains no repository/tool observation boundary.

[established-bounded; measured] Final integrated scopes pass: 13 normalized-owner controls,
11 composition/normal-section controls, the nested malformed normal-rest check, 20 HNN session
checks, and four exposure-driver checks. After the upload-readiness change, the affected
composition (11), normalized section (4), HNN session (20) and driver (4) scopes pass again.
The shared helical generator and moment-reuse controls pass, as does the facade without native
features. Nine Python adapter/assessment controls pass. No new Lean theorem is claimed: the
existing screw/quadratic-moment formal restrictions are the cited peers. The initial context
fixture's self-parent availability error was corrected rather than weakening source validation.

### Source reach and the next consuming operation

[proved-derived; source-inspected] At fixed material, one local stage can depend only on its
query and admitted neighbors. The supplied two-step longitude instance therefore reaches only
two free boundary coordinates from the held source: **2/32** in the development aperture and
**2/128** in the evaluation aperture. The remaining free sites can still reflect learned shared
material, but their current request does not reach them under this chart and extent. This is
an exact ancestry calculation on the compiled incoming arcs, not an inferred quality score.

[project-postulate] Continue #17/#16 at that concrete source/receiver boundary: organize the
request and context through the existing field restriction, participating-receiver, mode and
retained-interior owners so the distinctions needed by the receiving task can actually reach
it. Inspect the new generated section and its complete return on these same declared episodes.
Increasing exposure around unchanged disconnected support does not address this defect. #19
owns the variation when incidence/D changes; #18 owns a factor/locality realization where the
measured normal-statistic or validated-rest cost requires it. The helical moment adapter already
returns its linear moment restriction; it does not prove closure of this nonlinear word.

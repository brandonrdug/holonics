# The higher-difference word is the owner and the Millennium problems are its receiver families

**Date:** 2026-08-24  
**Scope:** correction and construction card for the exterior Lean line; this record schedules no
engine deed and does not move `CONSTRUCTION_STATE.md`.  
**Truth status:** each material claim is graded locally under `canon/EPISTEMIC_GRADES.md`.

## 1. The naming correction

[definition] The next Lean deed is **HD0 -- the addressed higher-difference word returns its
complete product-face and reciprocal ledger**.  Calling it a Navier--Stokes deed confuses its first
demanding analytic receiver with its mathematical owner.  The construction belongs beneath every
specialization which uses ordered local changes, product transport, inverse transport, annihilator
faces, chart rebase, or comparison of distinct routes.

[definition] The intended source owner is
`ElementaryHolonics/Foundation/HigherDifferenceTransport.lean`.  The existing
`NavierStokesReciprocalDifferenceRecurrence.lean` becomes a client which owns only the integer
frequency lattice, coordinate translations, the quadratic denominator, and the fluid/Hodge
receiver.

## 2. Source object, event, law, and returned consequence

[definition] HD0 begins with a state type `X`, an address type `ι`, transports `T_i : X -> X`, a
commutative coefficient ring `R`, and receiver sections `f : X -> R`.  It defines

```text
S_i f       := f after T_i
Delta_i f   := S_i f - f
Delta_word  := chronological iteration along [i_1,...,i_n].
```

The event occurrence is one addressed generator meeting the current section.  Its predecessor is
the complete earlier word, not merely its endpoint.  The local constitutive law is

```text
Delta_i(fg) = Delta_i(f) * S_i(g) + f * Delta_i(g).
```

The returned consequence is the complete leaf population produced by recursively applying this law
to an ordered word.

[definition] A leaf is not merely a subset of generator names.  It records each generator
occurrence, its chronology, whether its difference met the left or right factor, and every shift
inherited by both factors.  This recursive face tree is valid without commutation.  A subset or
multi-index table is a quotient which may be taken only after exact interchange cells prove that
the relevant generator orders have the same returned consequence.

[definition] If `q*r=1` throughout the complete successor window, the product tree for
`Delta_word(q*r)` has one distinguished leaf with no difference on `q`.  Isolating it returns the
reciprocal recurrence

```text
q(x) * Delta_word(r)(x)
  = - sum(all leaves carrying a nonempty addressed difference of q).
```

The inverse hypothesis is local to that word's successor population.  A missing or totalized zero
outside the window supplies no global inverse.  Proven annihilators of `q` delete their faces;
unproved faces remain in the ledger.

## 3. What HD0 must prove

[definition] The acceptance receiver is the conjunction of these ten returns:

1. arbitrary finite addressed words and the exact recursively shifted Leibniz tree;
2. the local reciprocal recurrence with every shift retained;
3. a commuting/interchange theorem which descends the occurrence ledger to subset and multi-index
   faces;
4. a concrete finite noncommuting system in which forgetting chronology changes the result;
5. naturality under a chart conjugacy which transports states, generators, sections, and leaves;
6. a receiver quotient carrying the complete collapsed-history fibre and a richer separating word;
7. recovery of the present one-generator laws through order six;
8. recovery of the quadratic three-axis seven-face denominator sparsity theorem;
9. recovery of the `(2,2,2)` twenty-seven product allocation as a specialization; and
10. no `sorryAx` in any promoted theorem.

[proved-derived; formal-checked] The present bounded source already supplies item 7 and the
denominator part of item 8.  It proves the one-generator shifted binomial law and reciprocal
recurrence through order six, the exact `6/15` reduction for a quadratic denominator, local
specialization to `|k|^2`, vanishing cross-coordinate differences, same-axis second difference
equal to two, vanishing third differences, and cardinality seven of the surviving denominator
candidate type.

[open] Items 1--6, the ordered multi-generator portion of item 8, and item 9 remain to be
constructed in the new owner.  The recursive tree must be proved before a closed multinomial
formula is introduced, because the tree is the chronology-preserving theorem and the multinomial
formula is its commuting receiver.

## 4. The specialization atlas

[definition] HD0 is a common compositional spine across algebra, discrete calculus, differential
geometry, topology, analysis, arithmetic, and computation.  Each line supplies its own transports,
annihilators, constitutive form, receiver, and global closure:

| receiver family | addressed transports and difference | returned face sought after HD0 |
|---|---|---|
| Navier--Stokes | commuting translations on `Z^3`; differences of the Hodge multiplier | uniform dyadic physical-kernel witness, then the critical-vorticity time return |
| Hodge | chart/cell transport; exterior and codifferential changes | `d^2=0` sparsity, primitive/coexact splitting, positive chart-glued storage |
| Yang--Mills | covariant transports which need not commute | comparison defect as curvature, Wilson return, scale-uniform coercive form |
| Riemann hypothesis | dilation and prime-local transport once founded | reciprocal Euler/Mellin ledger, reflection rebase, positive Weil receiver with trivial null difference |
| Birch--Swinnerton--Dyer | local height and valuation transport | telescoping good-place current, retained bad-place residues, canonical-height/rank bridge |
| Poincare | local moves, comparison words, and fillings | regression against the known local-to-global topological closure |
| P versus NP | successor/intervention histories over the same endpoint face | a complexity receiver which distinguishes endpoint equality from history-preserving condensation |
| complex `S^6` and torus families | finite and unipotent monodromy words | orbit order, nilpotent cusp difference, gluing and global recognition returns |
| parametron/Ising/QFP | pump half-turn, coupled phase-torus transport, phase/flux return | binary quotient together with amplitude, winding, chronology, and dissipation fibre |

[definition] The table does not make one receiver's missing theorem a premise of another.  It fixes
the shared calculus below them so a proved word, interchange, annihilator, rebase, or fibre theorem
is composed once and then specialized without renaming the owner after the first application.

## 5. The first analytic consumer

[definition] After HD0 returns, the periodic-fluid path resumes with the specialization
`q(k)=|k|^2`, `r(k)=q(k)^{-1}` off the zero mode, and word `(0,0,1,1,2,2)`.  The seven possible
denominator faces act on the twenty-seven Hodge numerator/reciprocal faces.  Their exact shifts feed
the inverse-cube mixed mass, the eight coordinate-subset receivers, the one-circle near/far Haar
estimate, and its three-axis tensorization.

[open] The uniform dyadic kernel return remains the first hard analytic consumer, but it no longer
defines the abstraction.  Hodge and Yang--Mills may use HD0 immediately through their annihilator
and noncommuting branches; the arithmetic lines may use it once their dilation, prime-local,
height, and valuation actions are supplied as addressed transports.

## 6. Construction boundary

[definition] HD0 is exterior theorem cartography.  It composes the standing Lean owners and may be
checked by Lean on CPU apparatus; it does not schedule the Rust/GPU construction, create an engine
subsystem, or move the root construction state.  Its deposit consists of the generic owner, the
specialization refactor, focused checks, the library build, printed axiom receipts, and this
receiver map.

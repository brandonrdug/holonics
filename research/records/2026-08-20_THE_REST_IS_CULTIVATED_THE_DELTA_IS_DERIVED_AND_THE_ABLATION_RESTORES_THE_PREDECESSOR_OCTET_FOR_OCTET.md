# The rest is cultivated, the delta is derived, and the ablation restores the predecessor octet for octet

**Date:** 2026-08-20
**Kind:** construction return for **Deed P3** of the single-card plan — cultivation of the native
ecology with the delta *derived by the declared return law*, never overwritten beside an adjoint
computed for show — with the fixture table fixed before exposure, the structured residual, the
load-bearing metric test, the matched foil, the held-out/still/remount/ablation controls, and the
honest boundaries.
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for the
cultivation owner and the lineage extension; **Deed P3 PASSES on this tree** — ten of ten controls.
Next: **P4 (native rest, export, and frozen inference)**. `CONSTRUCTION_STATE.md` does not move.
**Authority:** the single-card plan §8 "Deed P3", §11; `CLAUDE.md` §0n (hexis is rested conditional
transport; a metric-adjoint beside a coordinatewise delta does not pass) and §5 (the standing
training owner's shape — predict before mutation, `OpenResidual` rather than "incorrect" — composed,
not rivaled: `ConsequenceRelation` is imported from `holonic_training`); the P0/P2 artifacts (read,
never regenerated).
**Owners:** `soma/life/src/atlas_cultivation.rs` (1,752 — the absent relation: the structured
residual of an exposure against a standing atlas, and the delta derived from it);
`suffix_ecology.rs` extended with `absorb_returning_lineage` (`absorb` delegates and drops the
lineage — behaviour bit-identical, its falsifiers still cover it).
**Driver:** `soma/life/examples/the_rest_is_cultivated_and_the_delta_is_derived.rs`. CPU-exact
(everything semantic is integral or exactly rational; stated); the engine untouched.
**Artifacts:** `output/the_rest_is_cultivated/{receipt.form, successor-rest.safetensors, delta.form}`.

---

## 0. Verdict

**The delta is derived, and committing the derived delta reproduces the successor rest
octet-for-octet (1,855,432 = 1,855,432)** — non-tautologically: every standing class's transport
row is replayed from the *predecessor's* row plus the recorded foundings, and `derive` is never
handed the deposited body. The scope is stated first: this deed cultivates the **native arm** (the
lifted foreign arm's cultivation is P5's four-body comparison), and the predecessor is verified
before anything else — rebuilt octet-identical to the committed P0 rest, with the CPU conduct
reproducing the card's committed R1 faces 8 of 8.

- **The fixture table was fixed and printed before exposure** (development
  `canon/TABLET_THE_CHART.md`, held-out `canon/TABLET_THE_FLOW.md`, a declared lexical-aperture
  codec variant, a subject-disjoint July record for the still control, a germ-permuted matched
  foil, and a no-op document already in the rest), each row's presence/absence checked against the
  rest's own metadata with a refusal if any disagrees.
- **The structured residual is plural** (6,291 occurrences: Ride 472 / OpenIncluded 2,351 /
  OpenResidual 3,468; 415 novel germs by name; 3,222 refusals; depth census exhibited) and **the
  derived delta carries its causes** (7,909 classes founded of which 1,617 are splits; 11,814
  transitions; 40,349 rows in `delta.form`, each naming the position that caused it).
- **The adjoint is load-bearing, tested by perturbation:** the R1 ladder face's exact differential
  is the 0/1 deposit incidence; the return is `dq = G_X⁻¹AᵀG_Y r` through the **declared** metric;
  perturbing `G_Y(OpenResidual) = 3` moves 6 of 17 classes on the exact in-driver example (the
  owner's sparse adjoint equal to `exact_linear`'s dense result at all 17) and **5,462 classes'
  committed increments at scale**; a non-integral metric refuses by name rather than rounding.
- **The foil separates structure from population:** identical germ multiset, broken adjacency
  (985 of 6,290 pairs survive) — 7,227 classes founded against 7,909, only 516 of ~4,100/4,900
  in-body founded pairs shared. The delta is caused by structure.
- **Held-out changed and unchanged conduct, at faces growth cannot forge** (stated before reading:
  every founded germ enters the root's family, so the whole support necessarily moves — the
  unchanged control is taken at the stood-germ support and the depth-0 support): development
  probes 3 of 3 moved; the never-exposed held-out subject 2 of 2 unchanged at both restricted
  faces; the no-op subject's depth shifts named germ-by-germ rather than claimed away; the P0
  eight's movements stated with the corpus-wide-standing honesty restated.
- **The still control is non-vacuous** (the counterfactual commit is derived — 129 germs, 1,391
  classes — and *not* committed; the rest octet-identical before and after). **The remount** is a
  fresh process with the successor rest alone, faces bit-equal 3 of 3, forbidden descriptors none.
  **The ablation is an in-body withdrawal** — `withdraw(successor, delta)` returns a container
  octet-identical to the predecessor, every array separately identical, the cultivation
  declaration struck — strictly stronger than P0's construction-level rebuild, and the changed
  conduct reverts 3 of 3.
- **The no-op exposure commits an empty structural delta** (0 foundings at any class offering a
  nonempty family; the 1 separator-seam founding counted and named separately — an honest
  asymmetry of the licensing, caused by the chronology's separator rather than the material).
  Occupancy movement is declared as the law's own face: a re-exposure is a second occurrence, not
  a second structure.
- **No factorized overlay** — a counting atlas has no dense transport to factor; stated, so no
  `RankDerivationReceipt` is owed.
- **One authored level was excised during the build**: a `guard < 64` recursion bound replaced by
  a structural strict-decrease argument (a split's origin index is strictly lower).

## 1. Tests, ledger, epoch

`cargo test -p life --lib -- holonic_training suffix causal_language cultivation` → **51 passed**
(8 new in the owner); `h1_adversary` → 35 green; `cargo check` clean on both crates; no committed
driver rerun (P0/P1/P2 artifact mtimes unchanged). The ratchet deltas enumerated to the line and
dispositioned in the deed's report — the container arrays and lineage fields as material relations
(with the replay chronology flagged as the one shape that becomes a hidden authored order the
moment anything sorts it, and the four `.into_iter()` sites named as exactly where an authored
order could enter and does not), the residual/delta populations as returns, one `owner too broad`
admitted (three near-identical face serialisers; one removable header-chars Vec) — ledger rows
changed by hand; ledgers regenerated once.

## 2. The station table

| deed | grade | commit |
|---|---|---|
| A–D, H0–H5, P0–P2 | PASS (frozen) | their records |
| **P3 cultivation** | **PASS** | this record |
| P4 native rest, export, frozen inference | open — next | — |
| P5 reborn dissection + complete Phoenix grade | open (moves `CONSTRUCTION_STATE.md`) | — |
| M0–M3 | open, after P5 | — |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-20 05:57:11 → 05:59:26 UTC, exit 0 —
**12 of 12** (tests 2,636 over 30 result lines with examples type-checked; driver-catalog 268;
output-manifest 66/66; architecture-lint clean at 246 files; every other gate green).

Deed P3 passed; proceeding to Deed P4.

# The deed is the caused port diagram; the apparatus cannot author its order

**Status:** THREE-REVIEWER ADMITTED CONSTRUCTION AUTHORITY / PRODUCTION EXPERIMENT FROZEN UNTIL EXECUTABLE GATES / NO COMPATIBILITY

**Date:** 2026-08-02

## Ratified consequence

One continuing Eros receives a substantive question over the real repository and conditioned
history, conducts a complete co-present research passage, selects and authors a substantive Lean
theorem, receives every real kernel result, and lets that returned population change the same Lean
and reflective morphologies.  It then emits a new kernel-aware conversational answer, returns that
answer as cause, rests by ownership transfer, remounts without developmental-source replay, and
conducts the whole passage again for a second theorem which was unavailable before the first
return.  The actual conversation and Lean sources are the deed.  Topology, CUDA, lineage,
information, resource, and grading receipts accompany the deed; none substitutes for it.

The two exact target declarations are:

- `semantics_forward_under_exact_chart`; and
- `semantics_forward_through_two_exact_charts`.

The second must use the first returned theorem fiber.  Removing that fiber must eliminate every
kernel-accepted second proof.

## Existing production owners being composed

| responsibility | owner already in production |
|---|---|
| bounded local populations, incidence, and schedules | `holonic-structure` |
| one typed atomic successor | `ExactEventLaw` and `CausalWorld` |
| receiver-local caused reachability | `ExactReceiverCurrentLaw` |
| continuing incidence/current and atomic publication | `LiveCurrentMachine` |
| relation recurrence and native rest | `ResonanceEcology` |
| linguistic relation succession | `ExactRelationalLanguageEcology` |
| research-wave succession | `LaboratoryResearchEcology` |
| repository source occurrences | `LaboratorySourceAtlas` |
| outer question, one open deed, retry, and answer return | `AgenticResearchSession` with `AgenticLanguageEcology` |
| reflective face, codec, environment, and resumed continuation | `ReflectiveRuntime` through `AgenticLanguageEcology` |
| Lean target selection, candidates, one open kernel-return frontier, return fibers, and cultivation | `LeanMathematicsEcology` |
| external kernel receiver | `LeanKernelWorld` |
| resident host/card text material and append delta | `CudaResidentTextMaterialAtlas` |
| resource and physical telemetry testimony | `holonic-engine::resource` and device realization owners |

There is no global apparatus frontier.  `AgenticResearchSession` owns exactly one open outer deed
and its retry identity.  `LaboratoryResearchEcology` owns the succession of complete research
waves.  `ExactRelationalLanguageEcology`, `ResonanceEcology`, and `LiveCurrentMachine` own their
respective atomic relation successors.  `LeanMathematicsEcology` owns target selection, candidate
formation, exactly one typed open kernel-return frontier, complete kernel-return reception, and
theorem cultivation.  The diagnosis closes the outer agentic research deed before this formal
frontier opens.  Cultivation closes the formal frontier before the typed formal return enters the
resting language body.  The subsequent kernel-aware question opens a new ordinary research deed.
Two application frontiers never overlap, and no `AgenticMathematicsSession`, phase enum,
seven-`Option` sequencer, or second scheduler is introduced.

## Relations: causality, obstruction, and apparatus

Only two directed semantic relations exist in this diagram:

- `CAUSAL`: the target event cannot exist until the source return supplies or changes a typed input
  face.
- `OPEN`: a proposed crossing, order, or commutation is not certified.  It remains an obstruction
  and cannot be executed as though either serial precedence or parallel independence had been
  proved.

`APPARATUS` is not a directed dependency and is never a readiness edge.  It is an annotation on a
physical realization receipt: worker, stream, launch, memory, clock, temperature, utilization,
latency, or completion-order testimony.  Independent siblings have only their common causal
predecessor and their own returns.  They have no cross-edge.  A card result causally precedes the
host formation of *that result's* bounded handles; it says nothing about another leader.  A
generated source artifact causally precedes an independent regrade which consumes that artifact.
Telemetry annotates events and has no semantic arrow.

Mounting bodies is apparatus preparation, not a semantic event in this deed.  It supplies
inherited organs which remain individually owned.  The exterior question, not the mounts, causes
the first occurrence.

## Typed executable ports

The following table fixes the semantic mouth of every executable node.  “Standing” names the only
mutable owner.  Read incidence is immutable for that event.  No row authorizes an application
collection which duplicates either one.

| id | existing owner / production mouth | typed input face | typed return face | read or source incidence | standing and atomic commit | refusal, retry, and rest |
|---|---|---|---|---|---|---|
| `Q1`/`Q2` | `AgenticResearchSession::converse_with_world_front` -> `AgenticLanguageEcology::receive_occurrence` | receiver, stable question identity, UTF-8 question occurrence | answer or one `AgenticLanguageDeed` | mounted language/reflective continuation | agent dialogue and exactly one open deed | empty/open-question refusal; retry preserves open deed identity; agent native rest |
| `R1`/`R2` | `AgenticResearchSession::complete_deed_with_world` -> rebuilt `LaboratoryResearchEcology::deliberate_until_return_with_world_front`, whose internal mouths are `open_world_contact_front` and `receive_world_contact_front` | open deed identity and received prompt | one caused `LaboratoryWorldContactRequest` population keyed by `(leader identity, LaboratoryInformantPort)` and eventual `LaboratoryResearchDeliberation` | current research relation morphology | research owner fixes complete contact membership before exterior enactment; no caller invents or merges ports | exterior refusal leaves the same outer deed and typed contact frontier open; `retry_open_with_world_front`; research native rest |
| `S1.i`/`S2.i` | `LaboratorySourceAtlas` receiver-local restriction, rebuilt without `NavigableCover` | one `LaboratoryWorldContactRequest` whose port is `RepositorySource` | one individually identified returned-or-obstructed `LaboratoryWorldContactReturn` carrying stable source occurrences and omitted/open receipt | atlas source incidence and local chronology | none; atlas is inherited read standing | missing/omitted section remains named; same contact identity may retry; atlas has its own mounted source identity |
| `C1.i`/`C2.i` | `CudaResidentTextMaterialAtlas` bounded resident restriction, rebuilt without semantic reference ports | the sibling `LaboratoryWorldContactRequest` whose port is `ResidentTextCard`, plus admitted resident generation | one individually identified device returned-or-obstructed `LaboratoryWorldContactReturn`, then bounded source handles and local/delta equality receipt | resident section/incidence/factor DAG | no semantic target selection; only resident physical generation/delta standing commits | capacity/range refusal retains exact resident body and dirty delta; changed partition/aperture/residency or open obstruction before retry; native resident rest |
| `W1`/`W2` | `LaboratoryResearchEcology::receive_world_contact_front` | exact membership emitted by `open_world_contact_front` and one `LaboratoryWorldContactReturn` for each `(leader, port)` request | one co-present validated world-contact population, retaining every port lineage | membership was fixed by this research wave before enactment | research owner rejects the whole population or commits one research-wave successor; the application owns no merge table | incomplete, duplicate, wrong-port, or foreign return refuses without partial commit and retains the same typed frontier; retry same outer deed |
| `L1`/`L2` | `ExactRelationalLanguageEcology` -> `ResonanceEcology::receive_configuration_with` -> `LiveCurrentMachine::receive_with` | stable source occurrences and every leader-specific contact lineage in one configuration | one complete relation radiation, successor morphology, open fronts, and obstruction receipt | current clauses, Swing incidence, receptor/current incidence | the respective relation/live owner commits one complete successor | no application phase registry; unresolved Swing/relation interaction remains serial inside its owner or `OPEN`; owner-native rest images |
| `D1`/`D2` | `LaboratoryResearchEcology` connected/deliberative current, returned through `AgenticResearchSession` | post-world relation morphology and original prompt | closed diagnosis episode, complete thought fibers, world-contact identities | contemporary relation/current standing | language answer occurrence commits and closes this outer research deed | no closed current or no returned section leaves outer deed open; retry same deed |
| `T1`/`T2` | new `LeanMathematicsEcology::open_kernel_deed` | one `LeanTheoremTargetRequest` carrying the closed diagnosis/world witnesses, current Lean declaration incidence, requested theorem identities, and all selection faces | `LeanTargetSelectionReceipt` with every alternative plus exactly one selected `LeanKernelDeedFace`, or `OpenTargetSelection` | inherited source theorem occurrences and Lean reachability remain distinct | selected target opens exactly one formal frontier in the Lean owner; no application selection state | partial, tied, missing, or fallback-only evidence returns `OpenTargetSelection` without opening a frontier; no executable fallback |
| `P1`/`P2` | `LeanMathematicsEcology::materialize_kernel_problem` and `generate_proof_candidates` within its `LeanKernelDeed` | selected theorem face, source scope, current returned declaration fibers | complete stable-ID `LeanProofCandidate` family retained as the frontier membership owed a return | declaration and identifier incidence | Lean owner records immutable candidate membership; no theorem cultivation yet | no local declarations/candidates is a named obstruction which closes the proposed formal frontier; unselected target alternatives stay separate in the selection receipt |
| `K1.i`/`K2.i` | `LeanKernelWorld::grade`/`grade_all` | one rendered candidate source and its stable ordinal | one `LeanKernelReturn` with candidate ID, acceptance, hashes, diagnostic, and timing | external Lean environment | external observation only | process/timeout refusal remains attached to that candidate; no candidate disappears |
| `U1`/`U2` | rebuilt complete-population mouth around `LeanMathematicsEcology::receive_kernel_run` | the open `LeanKernelDeed` and exactly one individually lineaged verdict/refusal face for every candidate | one complete `LeanReturnedTheoremFiber` containing every accepted and obstructed path, plus cultivation receipt | pre-return Lean morphology and complete kernel population | one Lean generation successor commits and closes the formal frontier | malformed/incomplete population refuses atomically and leaves the same frontier open; retry reuses candidate/source identities; Lean native rest |
| `F1`/`F2` | a typed formal-return variant of `AgenticLanguageOccurrence` enters `AgenticLanguageEcology::receive_occurrence`, then `ReflectiveRuntime::revise_and_resume_with_lineage` | complete cultivated `LeanReturnedTheoremFiber`, cultivation receipt, target closed diagnosis episode, source digests, and typed returned Lean face | reified complete returned-population face, runtime-owned lineaged codec revision, same resumed continuation | current continuation, codec program, target answer lineage | reflective runtime alone commits codec/continuation successor | unknown target, digest mismatch, duplicate return, incomplete fiber, or any open outer deed refuses; reflective native rest |
| `X1`/`X2` | new ordinary `AgenticResearchSession` question over the post-kernel morphology | kernel-aware reflection question caused by `F*` | a new research deed, leaders, full world return, relation successor, connected current, and explanatory answer | same research/language bodies now containing returned theorem face | same owners as `R*` through `D*`; no direct answer formatter | every ordinary outer refusal/retry path applies; the pre-kernel diagnosis cannot be replayed |
| `A1`/`A2` | `AgenticResearchSession::return_answer_phases_to_research` and language answer-return mouth | actual post-kernel explanatory episode and its exact phases | answer-return occurrence plus post-answer continuation fibers | exact generated episode lineage | language/research morphology commits the emanated answer as cause | partial answer phase refuses without inventing completion; owner-native rest |
| `G1`/`G2` | resident text append and card delta synchronization | exact `A*` occurrence ID and caused source witnesses | append identity, dirty delta, card-delta parity receipt | resident source incidence | host resident append commits once; device owner commits that exact delta separately | failed sync retains dirty delta and pauses or changes realization before retry; it cannot roll back/replay semantic conduct |
| `N1`/`N2` | owner-native rest by ownership transfer and fresh remount | contemporary agent, research, relation, Lean, reflective, and resident bodies | bytes/images plus new bodies with same frontiers, alternatives, obstruction, identities, and successor horizon | contemporary bodies only | old bodies move into rest; remount creates fresh owners | old/unknown schema rejects; no `receive(saved_passages)` reconstruction; no live ecology remains beside comparison bytes |
| `O1`/`O2` | deed-bound observer receipt | deed identity plus pre-contact, post-world, post-formal, post-answer, and post-rest faces | causal-information, logical, physical, lineage, and grading receipts | exact event receipts | observer artifact only | missing aperture remains unknown; telemetry cannot alter deterministic equality |

For `L*`, distinct Swing operations have no plural-successor certificate in standing today.  They
therefore remain serial inside the exact owning ecology when that serial law is already
constitutive; otherwise they remain `OPEN` and are excluded from the admitted run.  Synthetic site
names, phase flags, and executor layers cannot close them.  `ExactSymbolicReasoningEcology` is
explicitly not mounted or called by this deed: there is no declared consumer for a symbolic return,
and the deleted host foreman may not invoke it merely because the organ exists elsewhere.

## Complete causal topology

### Theorem one and its post-kernel conversational deed

1. Exterior question -> `Q1` -> one open deed -> `R1` leader front.
2. Each leader independently causes its own `S1.i` and `C1.i` contacts.  There is no repository-to-
   card edge, card-to-repository edge, leader-to-leader edge, or sibling-completion edge.
3. Each `C1.i` card result -> bounded host handles for that same result.  All declared
   `S1.i`/`C1.i` returns or obstructions -> `W1` complete population.
4. `W1` -> `L1` one relation successor -> `D1` connected diagnosis current; `D1` commits its
   answer occurrence and closes the outer research deed.
5. Closed `D1` world witnesses **and** current Lean reachability -> `T1`; all target faces -> the
   unique selected `semantics_forward_under_exact_chart` face or `OpenTargetSelection`.
6. Selected `T1` opens one Lean-owned `LeanKernelDeed` -> `P1`; each candidate -> its own `K1.i`;
   the complete individually lineaged result population -> `U1`.
7. `U1` commits the complete accepted/obstructed returned fiber and closes the formal frontier.
   That whole returned population -> `F1` reflective revision of the same continuation while no
   outer research deed is open.
8. `F1` -> `X1` kernel-aware reflection question -> a *new* `R1`-shaped leader/front/source/card/
   world/relation/connected-current passage -> actual post-kernel explanatory episode.
9. Inside ordinary `X1` completion, that episode -> `A1` exact answer-return subevent through
   `return_answer_phases_to_research`.  No caller invokes a second answer return.  `A1` -> `G1`
   semantic append; the append -> its card delta sync.  `A1` is also a caused reference occurrence
   for theorem two.
10. `A1`, `G1` dirty-delta standing, `F1`, and the complete current bodies -> `N1` rest/remount.

The diagnosis episode identity causally binds the formal return target.  The post-kernel episode is
a different identity caused by the formal return and full second research passage.  A report may
not substitute the first for the second.

### Behavioral ablation and source-detached remount

The exact sequence is:

1. Before developmental contact, found two explicit experiment branches from independently
   mounted bodies over the same immutable inherited organs: `direct` and `rested`.  Neither is a
   clone, rollback, or path-copy of a live ecology.  Give them the identical caused event history
   through `D1` and verify equal standing.
2. On the `rested` branch's same mounted pre-return body, apply the theorem-two availability probe
   derived from the declared second-problem specification and current Lean morphology: it returns
   `OPEN`.
3. Admit the real accepted `K1` population and complete `U1`/`F1`/`X1`/`A1` on both branches.  On
   that same `rested` body immediately repeat the probe: theorem text and conduct are now
   available.  Direct/rest-candidate standing remains equal before suspension.
4. Apply a rejected or digest-altered formal-return control at the same receiver aperture: it
   remains obstructed and produces no revision.
5. Apply a receiver-local theorem-fiber ablation to the theorem-two query: the exact theorem-one
   returned fiber is excluded from that receiver without mutating standing.  Theorem two becomes
   unavailable and every second-proof path is rejected or absent.  Restore the ordinary aperture;
   availability returns.
6. Move only the `rested` branch into `N1`; detach repository/developmental sources as an
   experiment intervention; destroy that branch's live pre-rest owners; construct its fresh owners
   from native rest only.  The independently founded `direct` branch is not a source of the rest
   image and never shares mutable standing with it.
7. On the fresh remounted branch, theorem-two conduct remains available without source replay.
8. Give the independently continued `direct` branch and the remounted `rested` branch one further
   identical admitted input.  Their declared testimony and successor morphologies are equal.  The
   only old body destroyed by remount is the one which produced those immutable rest bytes.

Source detachment is an intervention, not a fictitious causal absence edge.

### Theorem two repeats the whole ecology

The remounted question -> `Q2` -> `R2`; each leader causes `S2.i` and `C2.i`; complete returns ->
`W2` -> `L2` -> `D2`.  `D2`, the self-emanated `A1` occurrence, current Lean reachability, and the
returned theorem-one fiber -> `T2`.  All selection faces uniquely choose
`semantics_forward_through_two_exact_charts`.  The selected face -> `P2`; each candidate -> its own
`K2.i`; all results -> `U2`, which closes the Lean formal frontier, -> `F2` while no outer deed is
open -> `X2`, where the full post-kernel research passage occurs again.  Its actual explanatory
episode causes the internal `A2` answer-return subevent -> `G2` -> `N2`.  The runner continues from the
remounted session for theorem two and performs the final extra successor probe; it may not discard
the remounted body merely after checking bytes.

### Observer artifacts and independent grades

- Each candidate -> the exact written `.lean` source artifact.
- Written source artifact -> independent Lean regrade which consumes those bytes.
- Each semantic deed -> its own `O*` causal-information receipt.
- Apparatus events receive telemetry annotations with explicit aperture; no semantic arrow is
  created.
- Uncalibrated CPU, GPU, or whole-system energy remains `OPEN`/unknown.

## Complete-population join law

Any global join is admitted only when all four conditions hold:

1. membership is fixed and identified before enactment;
2. every member returns exactly once or retains an individual obstruction;
3. the declared future receiver requires population completeness; and
4. the named owner commits one atomic successor after validating the population.

This admits `W*` leader returns and `U*` kernel returns.  It does not admit a global pair atlas,
global antichain, dominance quotient, complete traversal forest, or all-witness materialization.

## Phase/front quotient certificate and future receiver family

The retained direct morphology must answer this declared history-sensitive receiver family:

1. diagnosis formation;
2. exact theorem-target selection;
3. post-kernel explanatory answer formation;
4. emanated-answer return;
5. rest/remount;
6. theorem-two target selection and proof;
7. refusal and same-identity retry; and
8. later refinement by one further input.

No pair standing, flattened phase history, or bounded cover replaces it.  A proposed condensed
front is admissible only if direct and condensed bodies return equal declared testimony for every
admitted input history and carry equivalent successor morphologies across all eight receivers.
Until that stateful certificate exists, the direct owner-native front remains and the quotient is
`OPEN`.

## Resource refusal and backreaction

- A repository or card refusal leaves the same outer deed and leader identity open; no partial
  research-wave commit occurs.
- A card range/capacity/thermal refusal must change partition, aperture, residency, device
  placement, or schedule in the same resident body before retry, or stay an obstruction.  Raising
  a scalar work limit and replaying the same morphology is forbidden.
- One lifecycle full host/card parity run is admitted when the resident body mounts.  Later hot
  returns must execute actual resident CUDA restriction/generation and bounded delta equality with
  `hot_host_replay_work == 0`; no complete host restriction or reupload may follow the card deed.
- A partial target population returns `OpenTargetSelection`; it cannot choose a greatest partial
  score.
- An incomplete kernel population cannot cultivate.  Every rejection remains an obstruction and
  every acceptance remains a theorem fiber.
- A failed resident delta sync retains the exact semantic append and dirty delta, then pauses or
  changes its physical realization.  It cannot repeat the language event.
- A rest refusal returns ownership of the same live body; it cannot fabricate a reconstruction.

## No-backward-compatibility rejection matrix

| boundary | only admitted form | mandatory rejection |
|---|---|---|
| laboratory research rest | new owner-native schema containing relation/front/order/obstruction standing | flat `LaboratoryPassageRest`, replay through `receive`, missing worker/front fields, old or unknown schema |
| Lean rest | one new exact schema with returned theorem fibers, declaration sources, alternatives, and counters | every prior schema, every missing field/default, malformed or duplicate kernel path |
| text-material rest | current new schema with resident factor/incidence standing and dirty delta | `LEGACY_REST_SCHEMA`, v1 decoding branches, `compact_legacy_feature_standing`, inferred/default fields, old or unknown bytes |
| reflective/language rest | ownership-transfer native `ReflectiveRuntime` codec/environment/continuation plus exact open-deed state | `AgenticLanguageRestImage::remount` passage replay, manual codec-version atlas, reconstructed returned text, old or unknown bytes |
| resident device ABI | one new layout containing only physical section/incidence/restriction/delta fields | `TextReferencePort::{Question, Proof}`, reference ranking/status rows, factor/reference word egress, old layout |
| formal runner/session | closed diagnosis -> one Lean-owned open `LeanKernelDeed` -> cultivation closes it -> typed formal return into a resting language body | `AgenticMathematicsSession`, overlapping outer/formal frontiers, phase enum, option chain, legacy constructor or adapter |

There are no migration helpers, serde defaults, compatibility traits, dual-format writers, old
examples, or callable legacy routes.

## Deed-bound identity assertions

The admitted experiment asserts all of the following:

- the reported answer and episode are the actual post-kernel `X*` outputs;
- the causal-information receipt is derived from that conversation, not from `D*` diagnosis;
- the written source hash equals the kernel-observed source digest;
- the selected target identity equals the `LeanProofProblem` and generated declaration identity;
  the generated artifact bytes hash to the kernel-observed source digest; and the cultivated fiber
  names that target and generated source identity;
- every inherited repository theorem occurrence which supported selection retains its distinct
  source identity and lineage; it is never asserted to equal the new generated declaration;
- diagnosis, selected target, candidate, kernel return, cultivated fiber, reflective return,
  post-kernel episode, answer return, and written artifact retain one inspectable lineage;
- the selected witness is one selected current, is removal-minimal, and is not a copied witness
  string; open alternatives remain separate fibers;
- every selection face was evaluated and exactly one was selected; a partial population yields
  `OpenTargetSelection`;
- theorem two is unavailable before `U1`, unique after it, and loses all accepted paths when the
  theorem-one fiber is removed;
- every answer has an exact answer-return occurrence identity;
- telemetry is separate from deterministic equality; and
- the final inspection prints/reads the full generated language and complete Lean source, not only
  counts or hashes.

## Executable admission gates

The implementation and real run are rejected unless receipts/tests establish:

- `global_pair_population == 0` on every retained physical `TextMaterialCudaLaunchReceipt` and on
  the full-deed `O*` observer aggregate over all such launch receipts; the same-named field on the
  deleted semantic text-reference current has no successor;
- no persistent all-pair set, all-witness strings, dominance population, trie, `factor_words`, or
  `reference_words` egress;
- retained nodes are bounded by caused incidence/factor DAG and declared aperture, never `n choose
  2` source population;
- `TextReferencePort::{Question, Proof}` and every semantic CUDA selector are absent;
- exactly one lifecycle full host/card parity audit is allowed;
- later hot returns report `hot_host_replay_work == 0`, `bounded_delta_equal == true`, actual CUDA
  execution, resident generation advancement where caused, and no complete host replay/reupload;
- serial, plural-worker, reversed-submission, and injected-delay realizations return identical
  semantic deeds, successor morphologies, stable IDs, and obstruction fibers;
- for a certified front width of at least two, at least two workers actually join before the
  completion membrane, and at least one real leader's `RepositorySource` and `ResidentTextCard`
  sibling contacts physically overlap or join before `W*` while remaining semantically unordered;
- Swing/relation work not covered by a complete-successor certificate remains serial in its exact
  owner or is excluded as `OPEN`;
- direct/rest-remounted bodies pass the full future-receiver history test; and
- `cargo holonic-lint`, focused tests, workspace tests, clippy, independent Lean regrade, and all
  three reviewers pass before the production experiment.

## Explicitly prohibited conduct

- repository completion before card work because a host function was written that way;
- card completion before unrelated host formation because a stream was synchronized that way;
- stable key, path, receiver, phase, lexical order, or worker lane promoted into causality;
- distinct keys or phase labels promoted into commutation;
- application-owned graph, queue, phase registry, codec atlas, reference selector, or standing
  population;
- simultaneous witness multiplicity replayed as a temporal recurrence;
- complete host replay following an admitted hot card deed;
- source replay presented as native remount or training;
- fallback target selection, partial-score selection, compatibility loader, legacy schema, or
  renamed adapter; and
- any production experiment while a required crossing in this diagram remains `OPEN`.

The accompanying symbol-level disposition ledger decides the fate of every contaminated hunk.
This diagram is not an executor and does not claim that an `OPEN` relation already commutes.

# AC1–AC2: the parallel receiver calculus refines native continuation

[established-bounded; source-inspected] This return reads the parallel mathematics at
`04064e78` and `b1010d49`, including `ReceiverPotential`, `JointReceiverDescent`,
`ConstitutiveCurrentReduction`, `ConstitutiveScale`, their computation/torus instances and
recorded verification. No Lean source, parser or verdict enters the engine. The mathematical
owners inform the existing Rust/CUDA realization.

## AC2: transport the joint family to its receiver

[proved-derived; formal-checked] The parallel `ReceiverPotential.outcomes_map` carries the
whole compatible future image through a receiver. Its tolerance statements concern that whole
family. Its singleton-factor theorem determines a receiver face without determining a unique
source. `JointReceiverWitness` formalizes the previously recorded marginal-packet counterexample.

[definition] The native material report carries a complex Euclidean ball `B(c,r)`. Its terminal
packet receiver is the set of coordinates that maximize the real potential for some current in
that ball. The ball may itself enclose a smaller, correlated causal family; this receiver does
not claim to identify that source Preimage Fibre. Relative phase remains in the original report.

[proved-derived] For coordinate `i`, let `K_i={x : x_i >= x_j for all j}`. The coordinate is
possible exactly when `dist(Re(c),K_i)^2 <= r^2`. The minimizing imaginary displacement is zero.
The real projection pools `c_i` with the descending coordinates exceeding their pooled mean.
If this pool has sum `s`, squared sum `v` and size `k`, its squared distance is `v-s^2/k`.
This follows by minimizing the convex function
`(t-c_i)^2 + sum_j max(c_j-t,0)^2`; the stated mean makes its derivative zero on the active
piece. Hence the exact comparison is `k*v-s*s <= k*r*r`, with no square root or float.

[counterexample; computational-witness] The ball centered at `(5,5,0)` with radius `4` cannot
make the third coordinate maximal: the required squared displacement is `50/3 > 16`.
Independent coordinate boxes permit it, and even the two separate pairwise distance tests permit
it (`25/2 < 16`). This is why retaining only improved pairwise margins still loses joint geometry.

[established-bounded; source-inspected] `field/receiver/packet.rs` now supplies resident scratch
to `section_field_material_packet_receiver`. The kernel orders the real potentials and performs
the exact pooled comparisons with the existing wide integer owners. It returns every compatible
maximizer, including ties. It publishes a single selected coordinate only when this receiver
image is singleton, without imposing singleton source identity or a universal generation policy.
Scratch is temporary; the complete producing current remains owned by history.

## AC1: derive reuse from the operative law and its actual receiver family

[proved-derived; formal-checked] The parallel torus construction extracts active coordinates and
a retained jointly blind remainder, then proves the actual source law commutes with those
coordinates for every admitted input word. Its six-coordinate count comes from that instance's
incidence and receivers. It is not a proposed neural capacity. Its full-current decoder and
source-qualified uniqueness coexist with plural sources behind a reduced face.

[proved-derived] The existing paired field has a directly comparable decomposition. For fixed
contact map `D`, set `a=D b`, `C=D D†`, and let `P` project onto `range(D†)`. With
`r=(I-P)*b`, its ordinary law is

```
v = 2*(I+C)^-1*(u+a)
out = v-u
b_next = D† v-b
a_next = C*v-a
r_next = -r.
```

Here `†` denotes the Hermitian adjoint. Thus the boundary-current recurrence
closes on `a,C` for fixed `D`, while the blind remainder has a parity generator. This recovers
the existing aggregate-moment and shared-drive owners; it does not found a second engine.
The exact identity is for the declared algebraic law. Finite numerical realizations retain
their existing projection residuals and enclosures.

[established-bounded; source-inspected] The current operative-contextual material kernel also
reads inner products with historical internal currents and their norms. Those receivers can see
`r`, even when `D*r=0`. Its paired contact development changes `D`, and the later boundary drive
contains `delta_D*r`. For example, `D=(1,1)`, `r=(1,-1)` gives zero old drive, while
`delta_D=(1,0)` exposes drive `1`. Therefore a boundary-only replacement would remove actual
material and future-contact consequences. The existing `condense_shared_drive_mode` already
refuses a changed operative map; this review does not revoke its supported fixed-drive family.

[definition] AC1 reuse must carry the joint boundary, material-query and admitted contact-return
family. Derive its active span from those actual maps, retain a decoder or generator for the
remaining current, and carry any newly coupling contribution when morphology changes. This is
an implementation scope for the existing restriction/rebase and generator owners, not a demand
to prove universal closure before ordinary uncondensed learning can continue.

## Product consequence and remaining construction

[counterexample; measured] The first joint-target conversation model still repeats
`The its the its ...` through its 128-symbol limit. The
[joint-target record](2026-09-08_AC2_THE_MATERIAL_TARGET_RETAINS_THE_JOINT_PACKET.md)
retains its actual development and costs. A sharper receiver corrects the family being read;
it does not by itself supply missing useful language or a reusable developmental representation.

[definition] The next AC1–AC2 construction examines the continuing material/current return and
its reuse together. In particular, the current material operator fits complex target amplitudes,
while its contact response compares normalized real potentials against normalized packet mass.
These are explicitly different objectives, not an established common relative-entropy descent.
For a unit packet `e_i`, the amplitude loss is stationary at `z=e_i`, whereas the potential
cross-entropy derivative there is `softmax(e_i)-e_i`, which is nonzero for more than one target
coordinate. This establishes different stationary conditions; it does not alone prove the cause
of the observed loop.
Any unification must change the actual native coefficient return and its error law, then inspect
continuing behavior; merely changing a reported loss or increasing the corpus does not do so.
The full AC0–AC5 goal remains active.

## Verification

[established-bounded; measured] All three focused joint-packet CUDA tests pass in 110.64 seconds,
including first module load. The ball control compares 102 cases with an independent exact-rational
active-set enumeration. It covers collective exclusion beyond pairwise tests, ties, zero radius,
signed potentials, nonzero imaginary coordinates and a common shift of `2^100`. The separate
packet control retains complex non-dyadic targets, target-to-source adjoints, contact development,
delayed references and rest/remount. Both affected examples compile.

[established-bounded; measured] A fresh process mounts the saved 1,930-occurrence joint-target
model and executes the same prompt and 128-symbol continuation with the new ball receiver.
Body, complete recorded emission currents, generation readings and output are all exactly equal
to the original run. The process takes 103.79 seconds with peak RSS 2,108,984 KiB; prompt plus
emission takes 64.42 seconds. The [receipt](2026-09-08_joint_packet/return.json) retains the exact
comparison and command. The unchanged loop locates the next work in the continuing learned
return, rather than treating a corrected receiver or successful restart as useful language.

# HNA1 octet boundary differences entered the current native scaffold without selecting its route

**Date:** 2026-09-02
**Scope:** HNA1 under
[`the superseded first HNA contract`](../../archive/plans/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md).
**Disposition:** `counterexample`; no phase passed. The complete retraction is
[`THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md`](2026-09-02_THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md).
**Truth status:** per claim under [`../../canon/EPISTEMIC_GRADES.md`](../../docs/canon/EPISTEMIC_GRADES.md).

## Return

[established-bounded; implemented-exact; source-inspected; measured] HNA1 passed on the current
HIF6/SCF owner. `NativeMaterialCurrent::from_octets` maps every supplied octet occurrence to its
ordered exact signed difference from the preceding boundary value, beginning at the opening
boundary. It retains the complete occurrence vector beside the integrated scalar current, so two
inputs with the same final scalar can remain distinct. It introduces no float, hash, tokenizer,
language, format, path, or semantic class.

[established-bounded; implemented-exact; source-inspected; measured]
`NativeCirculationSession::ingress_octets` admits that current only at the caller-declared
`NativeInferenceRequest` already owned by its `NativeTransportScaffold`. The octets cannot select a
spool, thread, occurrence, receiver, state, generator, or continuation. The return separates the
productive `NativeMaterialCurrent` from `NativeMaterialColdPreimage`, which alone owns the exterior
locator and exact octets.

[established-bounded; measured] Four firing controls passed over
`direct_source_neutral_rest`, the current HIF6 source-neutral scaffold: ordered differences
`[3, 5, -6]` integrate exactly to the final boundary value; changing the first octet while holding
the final scalar fixed changes the complete current; renaming only the locator leaves the request
and native current equal while changing the cold preimage; and empty material refuses.

[counterexample; source-inspected] Attempts to wrap or admit the deprecated UAR
`SourceNeutralEcologyRest` were removed. HNA1 adds one current port to the current scaffold/session
owner and creates no competing hot body.

## Boundary

[open] HNA1 does not yet claim a useful artifact. HNA2 must retain this complete fine current
behind the native emitted receiver face and permit only an exact application-side octet projection.

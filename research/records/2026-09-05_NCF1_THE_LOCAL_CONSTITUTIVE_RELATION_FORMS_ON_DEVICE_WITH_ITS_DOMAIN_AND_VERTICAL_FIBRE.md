# NCF1: the local constitutive relation forms on-device with its domain and vertical fibre

**Date:** 2026-09-05, local apparatus date. **Status:** NCF1 partial; local relation owner returned.
**Contract:** [native HNA](../../docs/plans/THE_NATIVE_HNA_FOUNDS_CONTEXTUAL_TRANSPORT_BEFORE_INHERITANCE.md).

## Construction and exact scope

[definition] This local chart forms the rational relation spanned by received pairs, not a
chosen total coefficient map. An incoming pair joins one generated line to that relation. Its
source projection determines the admitted domain; every receiver fibre is a particular return
plus the complete zero-source (vertical) subspace. Outside the domain, no response is claimed.

[project-postulate] Linearity is this local chart's explicit seed constitutive hypothesis, not
an inference from equal array widths or the law of HNA, language or every context. Paired currents
must be actual measurements under that hypothesis, not requested answers. The current low-level
interface trusts the supplied pairing; deriving it through native contact is still required.

[proved-derived; formal-checked] `Computation/HolonicConstitutiveFibre.lean` proves the exact
source-domain projection, full translated vertical fibre, and agreement with any declared linear
constitutive map exact on the paired generators. Adding one observation is exactly a submodule
join. Explicit source/receiver linear equivalences transport both the generated relation and its
membership. Two distinct maps agree on the observed first-axis current and disagree on the
unobserved second axis; zero-source/nonzero-return receipt obstructs a linear function.

[proved-derived; formal-checked] `HolonicComplexParametron.lean` additionally derives the
additive/rank-one factor action through the existing mutual incidence response, two-sided
orientation transport and null-contraction control. This describes an admitted local change's
action; it does not select the change or define universal relevance.

[established-bounded; source-inspected] `native_ecology::constitutive_fibre::ResidentConstitutiveFibre`
is move-owned and holds its relation on `ResidentSurface`. One `advance` reads the predecessor
relation and, when a paired current arrives, forms the successor. Source-only arrivals also
advance its operation position. No pretrained coefficients, tokenizer, desired-map constructor,
learning rate, loss or CPU formation callback enters the operation.

[established-bounded; source-inspected] `section_constitutive_fibre` performs fraction-free
elimination on-device. Source pivots determine domain membership; target-only pivots retain the
vertical fibre. Common divisors are removed exactly, and the incoming row is staged before the
sole continuing write. Arithmetic overflow refuses before deposition; uncertain driver/readback
completion poisons replay. Scratch admission uses the mounted device's per-block memory limit.

## Verification

[established-bounded; measured] Eight explicit CUDA tests passed without skipping or fallback:
domain growth and later fractional/combined currents; unsupported directions; conflicting and
zero-source returns retained as full affine fibres; independent observation orders and joint
source shear/receiver quarter-turn; 30 complete native row spaces compared with an independent
arbitrary-precision observer; overflow without basis mutation; physical scratch refusal before
allocation; and resident reuse. In the two-source/one-receiver ordinary-read control, section
transfer was 48 input and 112 output bytes, with one graph launch and one terminal section read.
No relation reupload/readback occurred there. A plural return additionally reads the basis to
return all its directions. These are bounded tests, not a verified CUDA compiler/algorithm theorem.

[established-bounded; measured] Reproduction:

```text
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre -- --ignored --test-threads=1
lake build ElementaryHolonics.Computation.HolonicConstitutiveFibre \
  ElementaryHolonics.Millennium.HolonicComplexParametron
cargo check -p holonics-hna -p holonics-workbench
cargo test -p holonics-hna --lib
```

[established-bounded; measured] The existing HNA library also returned all 35 tests successfully;
the HNA/workbench dependency check passed. No inherited-model run was needed for these changes.

## Exact next composition

[open; source-inspected] This local accumulator is not the complete NCF1 ecology. It does not
yet derive paired receipt from native incidence/contact, carry changing branch/phase state through
that contact, found a richer source chart when context was forgotten, or retain the complete
addressed exposure population behind its numerical relation quotient. Its operation counter is an
apparatus position, not lineage. The source/receiver rechart test independently mounts transformed
material; the owner does not yet implement live recharting of the held ecology.

[definition] Next join this relation to actual native current/incidence and receiver return in
one continuing body, with contextual conditions and the addressed reconstruction boundary.
Do not close NCF1 on the accumulator or substitute another isolated capability test for that join.
NCF2--NCF4 still owe integrated contextual development, application, durability and consumer-resource
evidence. No Gemma exposure, linguistic competence, frontier comparison or power result is asserted.

[counterexample; source-inspected] `recurrent_return::seal` fixes `successor_to := from`; exterior
readback only admits that predefined edge replacement. Its header now states the actual scope.
This is the authored-edge failure species rejected on September 2, not the same function named in
that retraction. Its bounded algebra/rest evidence is neither withdrawn nor promoted into NCF1.

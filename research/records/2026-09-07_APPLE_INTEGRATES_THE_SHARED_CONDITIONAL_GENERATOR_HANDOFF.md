# Apple integrates the shared conditional-generator handoff

[definition] September 7, 2026. Brandon requested the desktop updates and context for resuming
the Apple/acoustic branch. This integrates desktop `6ef7bf9d` into Apple `bd3ea689`; Git retains
both parents. The Apple remote had no newer commits; desktop main supplied fourteen commits
after the previously shared `673b8d42`. This pickup merges source and checks the existing Mac
realization. It does not start another corpus run or transfer the desktop's private material.

## Shared construction now available

[established-bounded; source-inspected] Desktop's
[conditional-generator return](2026-09-07_AC1_THE_MEASURED_CONDITION_FOUNDS_A_NATIVE_PHASE_ACTION.md)
provides the agreed Mac re-engagement point. `ConstitutiveSourceChart::BilinearContact` binds
source and condition roles. `ResidentConstitutiveFibre::advance_bilinear_contact` composes their
direct and complex mixed currents with the existing learned relation. A
`ResidentConstitutiveReturn` carries its original affine Preimage Fibre and can supply a later
resident current. The [public example](../../crates/holonic-engine/examples/native_conditioned_phase.rs)
and [desktop operator receipt](2026-09-07_conditional_phase/return-v2.json) retain the measured
phase-action scope. Reading that evidence does not establish Apple execution of those kernels.

[established-bounded; source-inspected] The integration also carries the newer paired junction,
complete internal-current material transport, historical placement, text codec and checkpoint
owners. Those desktop components have their recorded bounded returns and failed text responses.
They are not a completed linguistic-object model. In particular, the condition Preimage Fibre
within the learned action and binding actual conversational conditions remain open in the
desktop position at `6ef7bf9d`.

[project-postulate] The September 7 synthesis distinguishes reusable class, its Preimage Fibre,
coherent composition and ordered progression. Limited observations can support useful future
conduct without selecting a unique microscopic cause. The acoustic branch composes this common
native operation with actual clocks, material conditions and receivers; PCM cells and authored
word/phoneme labels cannot supply the missing organization.

## Merge repair and platform boundary

[established-bounded; source-inspected] The source resolution retains both the newer field
owners and Apple's borrowed `intrinsic_profile`, together with the target-specific scratch
calculations and batch/persistence ownership. `record_constitutive_field` preserves desktop's
expanded junction/transport arguments on CUDA and the existing ordinary-field argument layout
on Metal. Unsupported paired/material junctions return an explicit declaration refusal before
launch; silently passing the expanded argument population into the old Metal kernel would
misbind its output and census buffers. The new regression checks both rational and enclosed
junction variants, unchanged relation/held state and zero launched deeds after refusal.

[definition] The new `section_constitutive_current`, `section_constitutive_bilinear_source` and
`section_constitutive_differential` still need Metal realization and mount signatures. Their
[concrete handoff](../../docs/HARDWARE_AND_MODALITY_BOUNDARIES.md#conditional-generator-port-for-the-mac-workflow)
uses existing wide/rational helpers, actual Metal scratch layout and dependent native lanes.
The larger desktop junction/history/material kernels are not prerequisites for the scalar
conditional-phase example. A successful source merge or library build does not establish their
execution parity.

[established-bounded; source-inspected] The documentation merge preserves both active tracks,
the September 6 acoustic corrections and the September 7 Preimage Fibre correction. The native
guide qualifies the desktop-only results, and the development guide gives Mac test filters that
do not silently promise the entire newer CUDA constitutive scope.

## Verification

[established-bounded; measured] On the standing Apple checkout, all **68 selected tests** passed
with zero failures and zero ignored tests within their selected scopes. Every device test command
used `--include-ignored --test-threads=1`:

| Command before `--` | Return |
|---|---|
| `cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::field::tests::` | 11 passed; 0.58 s test execution |
| `cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::circulation::` | 20 passed; 0.39 s test execution |
| `cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::tests::` | 8 passed; 0.30 s test execution |
| `cargo test -p holonics-hna --lib native::` | 29 passed; 1.32 s test execution |

[established-bounded; process-audit] These checks also completed successfully:

- `cargo check -p holonics-hna --lib --locked` (1m 03s).
- `cargo check -p holonics-workbench --bins --locked` (20.04s).
- `cargo check -p holonic-engine --example native_conditioned_phase --locked` (43.17s).
- `git diff --check`.

[definition] Test execution clocks exclude compilation and are not acoustic throughput measures.
The engine field test build took 2m 08s; the public native test build took 2m 13s. Compilation
was supervised. Existing unused-code/import and `block` future-compatibility warnings remain.
The conditional-phase example was type-checked only; its new Metal kernels were not executed.
No CUDA run or Lean build was performed, and no formal source/import changed in this integration.

[open] Learned acoustic organization, sound production, speech and general useful linguistic
conduct remain unfinished. This pickup supplies their shared source context and next port
boundary; it supplies no new acoustic timing, recognition, synthesis or power result.

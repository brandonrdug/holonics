# HNA3 the complete 42-layer body advanced to the tied boundary

**Date:** 2026-09-02  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, `measured`  
**Campaign:** bounded HNA3 progress; HNA3 remains current.

## Return

[implemented-exact; source-inspected] The full-session owner now enacts every primitive used by the
42-layer body: resident lookup, carry/reshape, exact coordinate-face selection, contraction, RMS
rebase, certified partial/full rotary chronology, causal contact with grouped K/V incidence and
exponential normalization, GELU, Hadamard contact, residual addition, algebraic scaling, and scaling
by a resident learned scalar. The per-layer selector, learned-scalar scale, and plain `tanh` each
received an owner-local CUDA law where the live resident surface had none.

[counterexample; source-inspected] The first atlas draft split causal contact and repeated K/V heads
as 210 unsupported intermediate nodes. The live `section_contact` owner already carries those
relations as one typed local-current/aggregation/reaction operation. The corrected complete graph is
1,275 nodes with 1,568 carrier-input incidences, not 1,485 nodes.

[counterexample; measured] Direct propagation of every certified interval exhausted the resident
word at layer one's gated Hadamard passage, returning refusal flag `0x1`. This is the already-known
enclosure amplification, not a missing neural operation. The full session now declares the
midpoint receiver projection at each open successor: exact outputs pass unchanged; an open interval
returns its nonpoint/widest-width census and its midpoint point-carrier advances. No application
status or expected output selects the projection.

[established-bounded; implemented-exact; measured] One live recurrence advanced two actual input
occurrences through 1,270 successive operation nodes: both embedding passages, per-layer input
construction, all 42 attention/feed-forward/per-layer-input layers including shared local/global
K/V standing, and final RMS rebase. Generation and operation cursor both returned 1,270; chronology
was exactly `[0,...,1269]`; the last emission was a nonempty two-row, 2,560-coordinate native face.
All 15,036,138,068 coefficient octets and both certified chronology populations remained resident.

## Boundary

[open] HNA3 has not passed. The remaining five nodes are the tied `262,144 x 2,560` vocabulary
contraction, scale by `1/30`, certified `tanh`, scale by `30`, and emission. The single aligned pool
cannot hold the tied matrix at once. The next deed is a native tiled carrier across those five nodes,
followed by a second autoregressive cycle on the returned successor. A host score loop, application
projection, or source executor cannot substitute for that boundary.

## Reproduction

[process-audit] The bounded return was produced by:

```text
cargo run -q -p holonic-engine --example advance_full_native_operator_prefix_hna3 -- /home/b/models/gemma-4-E4B-it
```


# The face closes on every twist, and the two directions at thirty-four escape the torsion and the doubles

**Date:** 2026-08-21
**Kind:** the second rung of the wall ladder, same session as the per-curve assembly —
the family consolidation and the thirty-four independence certificates.  Solo
orchestrator work, one Lean file; every family identity verified in exact rational
arithmetic (295 samples for the doubling/translation/closure laws, plus symbolic Klein
witnesses at four values of `n`) before encoding.  **It schedules nothing.**
[`blueprint/THE_ROADMAP.md`](../../blueprint/THE_ROADMAP.md) and
[`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) remain the only construction
authorities.
**Position under the active plan.** Exterior mathematical material for the mathematics
codec arm of
[`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`](../../blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md).
The Lean line does not couple to the engine, per Brandon's standing ruling.
**Truth grades:** `proved-derived` with `formal-checked` evidence for the Lean theorems
(`#print axioms` → `[propext, Classical.choice, Quot.sound]` on all ten public
theorems, zero `sorryAx`).

---

## 1. The face is a homomorphism on every twist at once

Owner: `ElementaryHolonics/Millennium/FamilyFace.lean` (859 lines).  Measured after the
merge: `Millennium/` is **69 files, 23,811 lines** with the library building at
**3,394 jobs**.

**`theFaceIsAHomomorphismOnEveryTwist`**: for every nonzero `n`, on `y² = x³ − n²x`
with the descent face `P ↦ (x, x − n)` mod squares (conventions `−n²` at `(0,0)`, `2n²`
at `(n,0)`), the face is multiplicative on the whole mathlib point group — every
addition branch, every landing, every convention, one theorem quantified over the whole
quadratic twist family.  The per-curve theorems of the same day's per-curve assembly
became instances of a single law.  Alongside it, family-wise:
`theDoublesLandInTheKernelOnEveryTwist`, the doubling triple
(`theDoubledSlotsClearOnEveryTwist`), the three half-turn translations with their
closures and never-half-turn refusals, and the Klein chords with their exact witnesses
`1`, `1/n`, `1/(2n)`.

This is the shape the route's standing claim demanded: the second frame is the twist
family, and its descent instrument now exists **as one object over the family**, not as
a per-curve construction repeated.

## 2. The two directions at thirty-four escape

With the family homomorphism instanced at `n = 34` (the smallest classically-known
rank-two congruent-number twist), the three realized directions of `ChordFace.lean`
each carry an unconditional escape certificate:

- **`theFirstDirectionEscapes`** — `P₁ = (−2, 48)` is not `T + 2Q` for any listed
  half-turn-or-identity `T` and any point `Q`;
- **`theSecondDirectionEscapes`** — the same for `P₂ = (−16, 120)`;
- **`theSumDirectionEscapes`** — the same for `P₁ + P₂`, its face read through the
  homomorphism itself (`face(P₁+P₂) ~ (32, 1800)`).

Together these are **the mod-2 independence certificate for the two directions,
relative to the listed torsion**: no combination `aP₁ + bP₂` with `(a, b)` an odd
pattern lands in `{half-turns} + 2·E₃₄(ℚ)` — the images of `P₁`, `P₂` in
`E₃₄(ℚ)/(listed torsion + doubles)` are independent.  The refusals are carried by
signs and by two, seventeen, and thirty-four failing to be rational squares.  This is
the second frame genuinely seeing two directions, upgraded from the conditional reading
of the chord deed to kernel-checked theorems.

## 3. What is owed, with falsifiers

**None scheduled; the roadmap alone schedules.**

| owed | falsifier |
|---|---|
| the torsion classification at thirty-four — that the four listed points are all the torsion (the depth-descent machinery of `DistantWindings.lean` re-aimed, or a family-wise torsion law) — upgrading the escapes from list-quantified to property-quantified | a fifth torsion point off the half-turns |
| the Selmer enumeration at thirty-four bounding the rank above — rank exactly two | a fifth Selmer class the local frames admit |
| retiring the per-curve duplicates: `Descent`/`RankOne`/`FaithfulFace` per-curve laws re-derived as instances of the family law (a consolidation pass, only if the roadmap wants it) | — |
| the supply question, unchanged: what analytic or governance datum produces a second direction for the face to see | — |

## 4. Boundaries

The escapes quantify over the four listed torsion points, not over the torsion
property — the classification at thirty-four is owed, and classically the list is the
whole torsion.  The kernel's converse remains open.  Nothing about the
Birch–Swinnerton-Dyer conjecture.

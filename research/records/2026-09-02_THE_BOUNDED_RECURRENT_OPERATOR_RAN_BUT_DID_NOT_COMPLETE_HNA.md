# The bounded recurrent operator ran, but did not complete HNA

**Date:** 2026-09-02
**Truth status:** `counterexample`
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, `measured`
**Construction consequence:** HNA1 remains current. HNA2--HNA5 do not pass out of order.

## Exact recurrence which returned

[established-bounded; implemented-exact; measured] The actual layer-zero interaction morphology
mounted 10,506,240 coefficient octets on the RTX 4080 SUPER. Contraction, GELU, occurrence contact,
output contraction, RMS reaction, and residual re-entry ran as six distinct successor operations.
Two cycles joined generation `0 -> 6 -> 12` with chronology `[0,...,11]` and zero resident refusals.

[established-bounded; implemented-exact; measured] The text application encoded "The receiver",
ran one native branch, read the complete 262,144-row vocabulary receiver, emitted token 21,824
(`pit`), rested/remounted generation 6 exactly, supplied that emitted token's interaction carrier as
the next ordinary occurrence, and emitted token 69,974 (` capitalist`) at generation 12. The token
was not a reward or morphology commit.

## Qualitative and completeness counterexample

[counterexample; measured] The three application controls returned:

| application material | emitted face |
|---|---|
| `Explain holonics briefly.` | ` northward` |
| `fn add(a: i32, b: i32) -> i32 {` | ` Wonderland` |
| `theorem add_zero (n : Nat) : n + 0 = n := by` | `пок` |

Every receiver had one exact maximum over 262,144 rows, and all 2,560 emitted interval coordinates
were explicitly collapsed by the application midpoint receiver before scoring. These facts make the
application projection reproducible; they do not make the surfaces coherent.

[counterexample; source-inspected] The HNA1 cold insufficiency names 2,127 omitted source
populations. The text closure contains 719 tensors; this branch admits three and leaves 716, while
the other 1,411 omitted populations belong to non-text families. The hot body carries only one
per-layer interaction branch, not attention, the main
feed-forward path, the remaining 41 layers, final normalization, or the complete emission operator.
Calling the bounded branch the complete operable morphology would repeat the exact proxy-promotion
failure HNA exists to remove.

## Standing

[established-bounded] The resident operation owner, successor recurrence, application vocabulary
receiver, and exact session rest/remount remain bounded construction evidence. They do not advance
HNA2, HNA3, HNA4, or HNA5 while HNA1 is incomplete.

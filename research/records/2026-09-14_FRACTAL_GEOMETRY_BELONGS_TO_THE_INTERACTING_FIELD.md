# Fractal geometry belongs to the interacting field

[project-postulate] Brandon's latest September 14 correction rejects the tiled-copy IFS and
separated layer-column pictures in the preceding synopsis. Heads and layers are operations
and charts within the same fluid Holonic object. Interlinked tori, intersecting simplicial
manifold domains, nonlinear generation, fractal structure and dimensionality belong to its
construction, not an illustrated afterthought. The three attached images identify the faulty
plates and the kind of basin geometry meant in the previously supplied paper.

## The recovered error and its live correction

[established-bounded; source-inspected] At `cf453131`, plate 8 applied four separated
similarities to copies of an already rendered field. Plate 2 plotted a fixed matrix as
separate columns. The central numerical map was a globally contractive affine recurrence;
it therefore removed the unstable route geometry being discussed. Adding an IFS picture
could not restore the mechanism that the chosen reference had excluded.

[established-bounded; source-inspected] The September 8 generator/tube record already defined
recursive generators through their parameters, restrictions, intersections and full preimages,
and connected them to curved tubes, gluing and holonomy. The hexis table nevertheless said
“while the population grows,” which narrowed the reading toward duplicated structures. Its
live clause now permits intrinsic recursive geometry in fixed-dimensional state spaces.
The update distinguishes material population, state dimension, carrier/stratum dimension,
receiver rank, plotted dimension and fractal dimension.

[definition] For a full recurrence Φ and receiving region A, first-arrival populations satisfy
`A₀=A`, `A_(n+1)=Φ^-1(A_n)\A`. A two-dimensional basin image restricts the *initial-condition
family* and reads a consequence of that full recurrence. It neither defines the complete
state space nor implies a collection of little models placed on a plane. Recomputed zooms
restrict the family more finely; compatible charts still belong to one object.

[established-bounded; source-inspected] The attached task/basin panel is Figure 2 of
[Lai et al., Fractal basins trap latent reasoning](https://arxiv.org/html/2609.04963v1).
The paper varies initial conditions for fixed trained models/tasks and colors decoded settling
times; its projections and finite-window measurements are explicit. Our new analytic reference
has its own stated law and first-arrival detector. It does not borrow the paper's trained-model
results or label its finite pictures as an infinite-scale dimension proof.

## Existing owners joined by the correction

[established-bounded; source-inspected] The recovered routes are the actual mathematical
relations: `SimplicialComplex` owns oriented surface stars/links and hinge changes;
`GradedCausalComplex` owns higher incidence and ∂²=0; `analytic_field` and `implicit` own torus
charts and carriers; connection/lattice-gauge owners supply ordered holonomy; the active
constitutive field owns current/material/inside return; recurrent and basin owners supply
forward operations and receiver populations. `FractalPacking` remains a useful separated
restriction specialization, not the definition of HNN morphology.

[proved-derived; formal-checked] The existing `HolonicRecurrentEcology` now contains
`FirstArrival.population`, `mem_population_iff`, `iterate_conjugate` and `chart_covariant`.
These prove the first-hit characterization and transport under a state equivalence with
conjugated recurrence/receiver. The module build passed (3105 reported jobs). The new
proofs depend on `propext` and `Quot.sound`, with no new axiom or sorry dependency. The
construction uses full preimages without introducing a historical-state archive.

[project-postulate] The intrinsic-field route now governs the model formula, Holon guide,
formal framework, Rust framework, architecture map, hexis doctrine, development method,
Athena blueprint and operating contract. A figure or bounded implementation must retain the
mechanism it explains; a convenient restriction does not become the architecture by repetition.
This is an operational correction, not another approval flow or universal acceptance test.

## The constructed geometric and dynamic return

[definition] The [intrinsic field reference](../experiments/intrinsic_holonic_flow/README.md)
has six interlinked toroidal domains. It forms contact incidence from exact integer quartic
witnesses of field overlap. Its common inscribed volume complex has shared vertices/faces,
31,440 tetrahedra and 240 multi-owner tetrahedra. The reference's conservative phase chart
has twelve axes, with two normalized phase-comparison heads and a sigmoid local reaction.
Both heads act inside one potential; the complete derivative includes both inbound and
outbound participation. A local phase-sensitive dissipative contact is also evaluated at
one of the same physical joins, explicitly retaining amplitude and heat beyond the unit-phase
restriction.

[proved-derived] The carrier and common cells are advected by
`F_t(x,y,z)=(x,y cos(χxt)-z sin(χxt),y sin(χxt)+z cos(χxt))`, χ=1/8.
This is the exact flow of `u=(0,-χxz,χxy)`, with determinant one and inverse F_-t.
At unit nondimensional density, `p=χ²x²(y²+z²)/2` and
`f=(χ²x(y²+z²),0,0)` give `(u·∇)u=-∇p+f`, with `div u=Δu=0`.
Thus it is a local forced incompressible Euler/NS realization, not an arbitrary warp called
fluid motion. The common diffeomorphism preserves linking, cell incidence and containment.
The displayed current paths include both material advection and internal phase motion.

[established-bounded; computational-witness] The nonlinear map is a composition of analytic
Hamiltonian shears. Its sampled inverse error is 2.22e-16; potential-gradient and symplectic
finite-difference checks have maxima 3.84e-11 and 1.59e-10. These corroborate the numerical
implementation at its tested states. The diagram does not claim interval-certified long-time
trajectories. Every source and parameter is retained with its stated physical/reference scope.

[established-bounded; computational-witness] Three 192×192 first-arrival fields are computed
from the same twelve-phase map, at the original section and 6.25× / 31.25× zooms. The sampled
neighbor pair reaches the same detector at iterations 9 and 96. Non-arrival by 96 remains gray
and is retained. The finite fields show interleaved route structure; covering/uncertainty
scaling is identified with its actual hypotheses, rather than assigning the torus dimension,
phase rank or tile count as a fractal dimension. The recorded field computation took about
29.6 seconds excluding geometry and plotting.

[established-bounded; source-inspected] The revised ten-page synopsis uses the actual linked
carrier, advected common-cell cutaway, phase-dependent heads, local contact, twelve-axis
trajectory separation and freshly recomputed basin sections. It removes the copied-skin IFS
and layer columns from the active presentation. Earlier valid linear and IFS examples remain
recoverable as bounded historical controls; their sources/results are not deleted or relabeled.
The white landscape typography and Holonic/Dirac convention remain in the shared presentation
module. Final pages were rendered and visually inspected, including equation notation,
clipping, contact labels and the full torus-chain viewport.

[project-postulate] This return repairs the requested mathematical/ontological specification
and its visualization. Native Athena assembly remains the source-conditioned field/body
operation in the active brief, with current, material and geometry varied and encoded together.
The supplied analytic reference does not replace that product or restart the paused goal.

# THE CELL IS THE LOCAL STAR; THE RECEIVER LIVES IN THE FIELD

**Date:** 2026-07-26  
**Status at deposit:** BRANDON-RATIFIED CONSTRUCTION PLAN / DEPOSITED BEFORE
SOURCE CHANGE / GENERAL LOCAL-STAR LAW BUILT / OCTAHEDRAL DESKTOP MOUNT BUILT /
SERIAL-MULTICORE EXACT / PROJECTIVE CONTACT TYPED / HOST BOUNDED / CUDA
PRESENTATION HYBRID HEADLESS-EXACT / INTEGER-RANGE LAUNCH FAILURE CLOSED / NO
WINDOW RUN

## Present question

The exact projective receiver and CUDA aperture now expose the caused
geometry faithfully enough to falsify the bounded desktop ecology.  That
ecology is one fixed tetrahedron driven through one selected variational
hinge.  Identity projective turns copy its coordinate through almost every
hinge, three passive conic families change, and two initially identical ray
families are related only by fixed presentation placement and optional
control fan-out.  It is an exact one-mode projection instrument, not yet a
distributed physical ecology.

The required construction is a general exact event law in which:

1. physical state belongs to oriented incidence neighborhoods rather than one
   scalar owned by one triangle;
2. stored content, current, flux, stress, geometry, and receiver worldlines
   are distinct but coupled faces of one successor;
3. propagation has a finite causal frontier;
4. geometry changes transport and transported stress changes geometry;
5. receivers are participating bodies inside the complex;
6. singular receiver contact and local degeneracy return typed events rather
   than terminating presentation;
7. independent local stars can be realized concurrently against one
   predecessor without executor order becoming chronology; and
8. the data trace and visible face are quotients of the same successor.

The construction does not add a cellular grid, one scalar per cell, authored
visual activity, a global scene, a probability controller, or a renderer-owned
simulation.

## Theory-to-structure relation

Let \(K_t\) be the contemporary oriented simplicial complex.  For a simplex
\(\sigma\), its closed local star carries the complete presently incident
relation:

\[
\operatorname{Star}_{K_t}(\sigma)
=
\{\text{incident vertices, hinges, faces, interiors, ports, and receivers}\}.
\]

The elementary event is

\[
\Lambda_{\sigma,t}:
\left(
B_t\vert_{\operatorname{Star}(\sigma)},u_{\sigma,t}
\right)
\longrightarrow
\left(
B_{t+1}\vert_{\operatorname{Star}(\sigma)},
J_{\partial\sigma,t},
\varepsilon_{\sigma,t}
\right).
\]

Here \(u\) is an actual boundary deed, \(J\) is oriented outward radiation,
and \(\varepsilon\) is the exact open residual.  Discreteness belongs to the
occurrence and incidence.  No private scalar state of a lone cell is assumed.

The first bounded carrier distributes state by incidence degree:

- vertices carry exact content, capacity, and optional receiver attachment;
- hinges carry exact coordinate history, momentum/current, inertia,
  stiffness, conductance, and recovery;
- faces carry exact oriented circulation, flux, and stress;
- the local metric is carried by exact relational edge/hinge data;
- receivers carry an attached local frame, aperture, and causal worldline;
- open boundaries carry supplied and returned deeds; and
- local degeneracy carries an explicit rewrite/contact candidate.

Balance is expressed through oriented incidence:

\[
x^+-x=s+\partial J,
\qquad
\partial\partial=0.
\]

A separate constitutive law derives \(J\) from local potential, stored
momentum, circulation, geometry, and recovery.  This distinction permits:

- capacity with resistive transport to relax;
- conjugate capacity/current storage to wave;
- nonlinear activation and recovery to form fronts and leaders; and
- stress feedback to deform future transport.

No completed global implicit diffusion solve is used as live event
chronology.  The existing exact diffusion law remains a lawful bounded
relaxation species.

## Receiver law

A receiver is a persistent local closure of \(K_t\), not an external camera
or an identifier attached to a ray family.  Its position and basis are
transported by the same local event law as other bodies.  A returned device
deed enters only the selected receiver boundary; further participation follows
actual incidence and current.

There is no interactive `entanglement` switch.  Several receiver faces form a
joint relation only when one actual event couples them through shared source
or transported current.

When a central receiver coincides with a source point, the homogeneous
projection \([0:0:0]\) remains invalid.  The world instead returns a typed
receiver--vertex contact.  A declared physical law may then collide, attach,
pass, absorb, rebase, or leave the contact open.  Presentation cannot choose.

## Local rewrite law

Topology is unchanged by ordinary transport.  At a declared discriminant,
the successor may emit one bounded rewrite candidate over a preserved exterior
interface, such as an edge flip/T1 move.  The candidate records:

- the held exterior boundary;
- the departing local incidence;
- the proposed founded incidence;
- the exact triggering residual;
- the receiver/body contacts participating in the event; and
- whether the application law admits or leaves the rewrite open.

This is the dynamic-cellular face of the engine.  A mesh edit is not
fabricated merely because a picture becomes degenerate.

## Parallel realization

One event derives its active causal frontier from source incidence.  Local
stars at the same frontier may be solved concurrently only when their write
supports are disjoint.  Every task reads the same predecessor.  Results return
in canonical occurrence order and are merged only after exact overlap checks.
Overlap disagreement becomes residual; physical scheduling never resolves it.

The host law is authoritative.  CUDA may realize admitted finite-front local
tasks and receiver support, but cannot define their topology, event order, or
arithmetic.

## Bounded witness

The first mounted world will replace the one-pivot tetrahedral desktop with a
small heterogeneous triangulated ecology containing:

- more than one independent cycle;
- independently carried hinge trajectories and currents;
- at least two constitutive hinge species;
- face circulation/stress feeding back into hinge geometry;
- a finite excitation/recovery front;
- physically attached receivers with different local relations;
- one typed receiver--vertex contact seam; and
- one bounded local rewrite candidate.

One boundary deed must produce:

1. nonuniform local propagation rather than copying one coordinate;
2. a finite sequence of active frontiers;
3. exact node/hinge/face balance receipts;
4. a changed receiver worldline caused by the field;
5. a changed received face caused by that same successor;
6. canonical equality between serial and multicore realization; and
7. continued lawful execution at the former `ZeroProjectivePoint` boundary.

The desktop trace will record the complete local current population, frontiers,
face stress, receiver worldlines, open contacts, rewrites, and conservation
residuals.  Scalar counts remain supporting testimony only.

## Stop

The build is complete only when the generalized engine law, bounded mounted
world, exact trace, and desktop presentation all use the same causal state;
focused tests and clippy pass; and an admitted CUDA presentation remains exact.
The construction will not launch or control Brandon's window.

## Built closure

The source now embodies the plan in
`crates/holonic-engine/src/local_star.rs`.

`LocalStarLaw` owns one event-local successor over:

- independent hinge trajectories and exact constitutive materials;
- signed, oriented, face-adjacent current transports;
- a pending finite causal frontier;
- current deposition and exact action/current residuals;
- oriented face circulation and carried stress;
- shared-vertex deformation in one declared bounded chart;
- attached receiver positions and Cayley-precession worldlines; and
- typed local 2-to-2 rewrite candidates at an exact coordinate
  discriminant.

Every active hinge member reads the same predecessor. `CpuExecutor` may solve
the members serially or on multiple cores. The merge is canonically ordered;
the bounded serial/multicore foil returns the identical complete standing
body and radiation.

`HingeWorldLaw::realize_parameter_population` is the one kinematic mouth for
that solved population. It changes affected faces and conic forms once.
Replaying the members as sequential hinge events is no longer necessary.

The bounded desktop source is now an oriented octahedral complex: six caused
vertices, eight faces, twelve shared hinges, plural cycles, heterogeneous
inertia/stiffness/stress response, and valid edge-flip openings. Positive and
negative currents use opposed acyclic local hands selected by one exact
source-declared field covector in this bounded chart. Hinge identifiers and
container order contribute no causality. A disturbance therefore branches
through adjacent stars and reaches exact rest without a tolerance or an
endlessly shrinking rational tail. The general law still admits cyclic
source-declared transports.

The desktop no longer:

- selects one permanent physical pivot;
- copies one hinge coordinate through identity turns;
- copies receiver input through an `entanglement` toggle;
- holds triangle vertices fixed while only passive conics change; or
- derives receiver difference from one fixed presentation matrix.

A returned deed enters one `ReceiverPort`. The field changes hinge
trajectories, face stress, shared vertices, conic forms, and receiver bodies.
Both receiver faces are then rebuilt from that same standing body.
Presentation color is derived independently from each receiver's exact
position/precession phase.

The former zero-projective failure is now
`ArrangementSeam::ReceiverPointContact { source, ordinal }`. The contacted
primitive remains open for the physical application law and the complete
receiver face continues; `[0:0:0]` is not made valid.

The desktop research trace now records complete active and pending
frontiers, every action balance, every routed current, all face fields, both
receiver worldlines, rewrite candidates, physics worker use, changed
incidence, aperture work, and complete carried hinge/receiver state. Counts
support those paths but do not replace them.

Focused host witnesses establish:

1. one current reaches only its face-adjacent next frontier;
2. the exact source world reaches rest without a numeric tolerance;
3. shared geometry and receiver bodies change;
4. nonuniform hinge states and face stress emerge;
5. an opposed deed emits a valid octahedral edge-flip opening;
6. serial and four-worker standing/radiation are exact; and
7. receiver--vertex coincidence returns a typed contact without terminating
   the face.

The local rewrite is deliberately still a candidate: admission and mesh
replacement remain an application event, not an implicit renderer edit.
Physics realization is presently host-multicore; native CUDA continues to
realize the admitted exact presentation aperture. The initial construction
did not launch a desktop process or CUDA cell; the launch-boundary correction
below adds a headless CUDA witness without opening X11.

Final bounded verification on 2026-07-26:

- `cargo test -p holonic-engine --all-targets --no-fail-fast`: 55 library
  tests and the desktop local-star witness pass; the visible-device CUDA
  witness remains explicitly ignored;
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`:
  clean; and
- `git diff --check` over the construction paths: clean.

## Launch-boundary correction

Brandon's first launch exposed a boundary the host-only closure did not test:
the initial local-star presentation required a 493-bit intermediate at the
compositor relayout and the CUDA aperture still refused anything above its
384-bit limb carrier. A naive 512/768-bit widening compiled and launched but
failed complete host parity. It was rejected rather than admitted.

The headless card audit localized the defect further:

- finite projective segments agreed at the initial face;
- transported conics disagreed under the hand-written multi-limb arithmetic;
- after one receiver precession and a `640 x 720` aperture change, a
  transported segment also disproved the wider-limb implementation; and
- native signed `i128` CUDA remained exact.

The physical boundary is therefore now explicit. CUDA receives only
source primitives whose complete preflight fits native signed `i128`, and
conics remain host-owned. Every conic and every wider segment is evaluated by
the same arbitrary-precision multicore host law. Their complete address and
multiplicity fibers are merged with the card return before whole-face parity
admission. A finite device carrier can no longer terminate the presentation,
and nothing is rounded.

The headless RTX 4080 SUPER witness now passes both the initial local-star
launch and a receiver-precession successor at `640 x 720`. The successor
reports `signed i128 CUDA + exact host rational`, 257 required intermediate
bits, 43,200 admitted card work items, and 186,646 complete exact support
evaluations. The full returned address, multiplicity, and primitive
populations equal the arbitrary-precision host authority. No X11 window was
opened.

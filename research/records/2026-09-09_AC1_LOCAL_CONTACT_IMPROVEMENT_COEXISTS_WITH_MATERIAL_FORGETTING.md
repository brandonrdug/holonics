# AC1: local contact improvement coexists with material forgetting

[definition] This continues the finite-response investigation from `b2989881`. The actual
question is whether a locally correct return improves its source-conditioned relation, and
whether subsequent development retains that relation. These are separate receiver questions.
The complete oriented currents and their bounds precede the scalar comparisons below.

## The comparison and its source

[established-bounded; source-inspected] `operative/response/comparison.rs` supplies two cold
observers over the existing `OperativeLinear` owner. `inspect_material_contact_step` stages the
same native return that publication will use and evaluates the retained source excitation under
the producing, current and proposed contact maps. It holds the current material matrix fixed.
`inspect_retained_material_relation` instead reads that current matrix on the original retained
outgoing source family, without reenacting the field. Both carry actual source/receiving lineage.

[definition] The finite comparison uses the source's original entering root current and its
original entering internal current. The latter comes from the preceding retained state plus
any actual internal-current deposits before the source entered. Contacts born later receive a
declared zero extension of this diagnostic excitation. This neither resets the live interior
nor proves that every later receiver should use that extension. A changed source/current frame
currently returns an explicit unsupported comparison. The observer evaluates exact rational
maps; it does not substitute its centres for the native production path or choose a source from
the Preimage Fibre.

[proved-derived] With the paired scattering map S(D), let
`K(D) = [[0,-D],[D†,0]]` and `J = diag(I,-I)`. Then

```
S(D) = J (2(I+K(D))^-1 - I),
||(I+K(D))^-1|| <= 1,
||S(D1)-S(D0)|| <= 2 ||D1-D0||.
```

The first identity follows by eliminating the second block; skew-adjointness gives the second
bound, and the resolvent difference gives the third. Consequently the whole source family has
outgoing error bounded by `e_u+e_b+2 e_D ||(u_hat,b_hat)||`. The observer uses an exact L1 upper
bound for the last norm. A material matrix family then returns error bounded by
`e_M ||out_hat|| + (||M_hat||+e_M)e_out`. The observed-target radius is added to the full complex
residual before bounding half its squared norm. No float or source-selection assumption enters
these bounds.

[proved-derived] The cold paired solver now also uses
`(I+DD†)^-1 w = w-D(I+D†D)^-1D†w` when the contact chart is smaller. Multiplying by `I+DD†`
verifies the identity. Empty contact families use the identity directly. This reduces the cost
of exact early-source inspection while retaining all columns and their lineage; it changes no
native forward kernel or model parameter.

[established-bounded; source-inspected] The SDK's optional read-only observer runs between the
existing response construction and its ordinary publication. Its result does not gate learning;
even a returned diagnostic error leaves publication on its original path. The text study's
requested inspection cuts are exterior measurement apertures. They neither choose developmental
material nor supply a score-driven native policy.

## What the actual conversation run returns

[established-bounded; implemented-exact; measured] The first-family study uses the same actual
75 occurrences, `OperativeLinear`, joint target, duplex codec, grain 72 and `SquaredCurrent`
contact return as the preceding study. Five finite comparisons at cuts 3, 4, 5, 6 and 8 all
strictly improve the bounded material discrepancy under the declared diagnostic excitation.
The complete final body and 11,680,724-byte checkpoint remain identical to the run without
inspection. The observed run takes 57.51 seconds, including exact rational diagnostics; that is
an observer cost, not a new production timing.

[counterexample; computational-witness] A separate exact paired control shows why this was a
real question: scalar D=1, input u=1, b=0, material M=4 and target y=1 give the correct local
contact gradient -4. Its unit step to D=-3 raises the half squared material discrepancy from
1/2 to 441/50. Thus local derivative correctness does not imply finite improvement. The actual
five early conversation returns above do improve; the control does not diagnose them as failures.

[established-bounded; measured] The same five original source families were then read through
the material matrices saved after 75 and 5,064 occurrences. Their half squared complex-current
discrepancies are below. Decimal values are rounded displays; the [receipt](2026-09-09_finite_contact_retention/return.json)
retains exact rational intervals and source coordinates.

| Receiving occurrence | Immediately fitted source relation | At 75 occurrences | At 5,064 occurrences |
|---|---:|---:|---:|
| 2 | 0.001412 | 0.706228 | 0.657290–0.657296 |
| 3 | 0.001586 | 0.711534 | 1.206531–1.206537 |
| 4 | 0.001815 | 0.651285 | 1.073030–1.073036 |
| 5 | 0.002159 | 0.722155 | 1.313107–1.313113 |
| 7 | 0.002188 | 0.556047 | 0.940675–0.940682 |

[counterexample; measured] Every later interval is strictly worse than its initial interval
and than the zero-output reference loss 1/2 for these unit packets. Local fitting and an
improving contact step therefore do not establish retention by the current material learner.
This is a concrete failure on five actual source-qualified relations, not a claim that it
explains every remaining language defect. The full current vectors remain in the private study
artifacts; the portable receipt preserves the exact scalar comparisons and their scope.

[established-bounded; measured] Fresh-process retention inspection takes 4.31 seconds for the
75-occurrence model and 41.77 seconds for the 5,064-occurrence model, including mounting and
cold body diagnostics. These processes perform no cultivation or text generation.

## Retaining the source geometry

[established-bounded; source-inspected] The existing linear material update is
`M_next = M + (y-Mx)x†/(1+||x||²)`, with its declared numerical projection and enclosure.
Its coefficient work metric is the unit metric at each individual return. M retains prior
effects, but the operator does not retain an accumulated source-normal metric. A later update
changes an older source x_old by a term proportional to `x†x_old`; actual source directions can
overlap strongly. The measured forgetting directs the next construction toward that geometry.

[definition] The next material realization uses the same operative outgoing sources and joint
targets, with the accumulated regularized normal potential on the admitted observations:

```
Phi_t(M) = (||M||_F² + sum_i ||M x_i-y_i||²)/2
H_t = I + sum_i x_i x_i†
B_t = sum_i y_i x_i†
C_t = sum_i ||y_i||².
```

The identity is the existing declared unit coefficient metric at founding, not an intrinsic
semantic capacity. H and B keep complex cross terms and relative phase. C is the constant needed
to recover the complete potential. Their sums concern the actual source/target vectors; they do
not make the HNN recurrence or the original conversation chronology order-independent.

[proved-derived] Differentiation gives the normal equation `M_t H_t = B_t`. Since H_t≥I, its
unique minimizer is `M_t=B_t H_t^-1`. For an exact preceding normal solution, elimination of a new
rank-one source term gives

```
g = H_prev^-1 x / (1 + x†H_prev^-1 x)
M_next = M_prev + (y-M_prev x)g†.
```

The inverse identity follows by multiplying the stated rank-one resolvent by `H_prev+xx†`.
It recovers the current formula only at the initial unit source metric. The accumulated geometry
is the additional reusable state. It is a sufficient statistic for this normal-fit receiver
family, not a quotient identifying source occurrences or replacing the continuing ecology.

[proved-derived] Source/target families remain explicit. With `x_i in B(xhat_i,e_i)` and
`y_i in B(yhat_i,f_i)`, valid Frobenius bounds are

```
E_H = sum_i (2||xhat_i||e_i + e_i²)
E_B = sum_i (||yhat_i||e_i + ||xhat_i||f_i + e_i f_i).
```

For a numerical matrix Mhat and its complete normal residual `R=Mhat Hhat-Bhat`, the family
of regularized normal fits obeys
`||Mhat-M_true||_F <= ||R||_F + ||Mhat|| E_H + E_B`.
Indeed `Mhat H_true-B_true = R+Mhat(H_true-Hhat)-(B_true-Bhat)` and
`||H_true^-1||≤1`. Exact signed statistics and residuals precede these scalar bounds. This supplies
a finite normal-solve comparison without silently treating an enclosed source as its centre.

[definition] Construction now belongs in the existing native material owner: accumulate the
source-normal and cross-source statistics with their declared family bounds; factor and solve
them on the GPU; retain the numerical residual and the original producing operator for later
adjoints; and persist their actual state and decoder. Legacy models retain their declared law.
Verify the new realization against exact normal references, the same retained relations and
actual continuing conversation output. The normal formula is derived here; its native
implementation and useful language are not yet returned.

## Verification and position

[established-bounded; measured] All 114 field tests pass. The seven non-GPU paired-algebra tests
pass; the new native source-input and nonmutation control passes. All 25 existing alpha SDK
tests and the additional observer-error/publication control pass. The text driver builds and
its two executed incidence controls pass. The receipt retains those commands and logs.

[definition] AC0 remains complete. AC1 now proceeds through the accumulated material-normal
construction alongside its remaining path/generator reuse; AC2 still requires useful full
responses and contextual continuation. AC3 artifacts remain experimental; AC4–AC5 remain open.
No Lean source or verdict enters this work, and no formal build is required by these changes.

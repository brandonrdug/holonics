# THE PHASE CURVES THE FACE; THE RECEIVER RETURN CANNOT BE A RETILE

**Date:** 2026-07-26  
**Status:** HISTORICAL INTERMEDIATE / PHASE CLASSIFICATION AND DEPTH CORRECTION
RETAINED / TERMINAL CAUSAL TREE REJECTED AND SUPERSEDED BY
`2026-07-26_THE_SIMPLEX_EMITS_THE_FACE_THE_MATRIX_CANNOT_OWN_PROPAGATION.md`

## 1. The screenshot falsified three laws

The live desktop image exposed three source defects rather than a matter of
visual styling.

First, every receiver-to-presentation relation was represented by one rational
projective homography. A homography maps lines to lines. It can carry one local
chart transition, but it cannot by itself express a curved receiver connection
or the whole receiver relation.

Second, live propagation was a Chebyshev ring number on terminal tiles. That
made the physical work partition pretend to be chronology. A ring completed
before the next ring was submitted, so one small early ring left most workers
idle and every input looked like a delayed retile.

Third, the display transducer used receiver identity to select authored
cyan/amber ink and only drew a finite authored coordinate-line range. Exact
conics existed internally, but their phase families, receiver depth, and
causal crossing order did not reach the visible member.

The running window was not controlled, restarted, or closed during this
correction.

## 2. Exact phase classification

For a terminal member \(C\), an exact transported line family

\[
L_0+tL_1=0
\]

now returns the complete integral phase interval attained on \(C\):

\[
t(C)=
\left\{-\frac{L_0(x)}{L_1(x)}:x\in C\right\}.
\]

If \(L_1\) crosses zero, the member is `OpenProjective`; no finite substitute
is fabricated. Otherwise the exact rational corner range determines every
integer level in the member. There is no authored \(-4,\ldots,4\) range.

For an exact quadratic \(q(x,y)\), rational extrema on \(C\) determine both:

1. whether the zero contour intersects \(C\); and
2. every integral quadratic phase \(q(x,y)=n\) crossing \(C\).

The second carrier exposes curved conic level families rather than only the
original zero contour.

Every supported triangle or conic also carries an exact projective depth law

\[
d_\rho(x)=\frac{N_\rho(x)}{D_\rho(x)}.
\]

Its integral levels become a fourth member face beside primitive support,
linear phase, and quadratic phase. The RGB quotient is now selected by exact
phase and orientation, not receiver identity. RGB remains an explicit
three-channel terminal quotient; it is not claimed as a universal physical
color law.

## 3. The erased dimension was restored

`ReceiverFace::receive` previously replaced every projected triangle vertex by

\[
(x/w,\;y/w,\;1)
\]

and tested it with an orthographic ray. This erased the carried receiver depth
and made all surfaces coplanar.

For a central receiver, the homogeneous face point

\[
[x:y:w]
\]

is now retained as the receiver-local coefficient triple, and the local query
ray is

\[
(0,0,0)+s(u,v,1).
\]

For a parallel receiver, affine face position and independently carried depth
remain separate. A bounded exact witness places coincident projected triangles
at depths \(2\) and \(4\); the receiver returns two ordered crossing layers at
exact parameters \(2\) and \(4\).

## 4. Historical terminal-tree interpretation — rejected

This section is retained only to identify the defect. A terminal member or
worker rectangle is not an event grain of the manifold. The phase-weighted
terminal tree described below was removed; it may not be restored under
another name.

The calling world selects a finite terminal causal grain; in the desktop
world it is presently an \(8\times8\) member region. That same region is one
physical work parcel. Its rectangular distance does not define chronology.

For each receiver and event, every selected grain carries the exact signature

\[
\Phi_C=
(\text{primitive face},
 \text{linear phase},
 \text{quadratic phase},
 \text{depth phase}).
\]

Across two edge-adjacent terminal grains, the exact combinatorial action is

\[
a(C,D)=1+
\#\{\text{independent carried faces whose classification changes}\}.
\]

The unit term is one incidence in the finite presentation quotient. The
remaining integer is the changed phase word. It is neither a floating metric
nor a Euclidean-distance approximation.

The event root induces a minimum-action transport tree. Completion of one
grain immediately releases only that grain's causal children. Independent branches
therefore occupy the persistent worker population without a whole-front
barrier. Independent receivers retain independent trees; only an explicit
coupling transports the event between them.

A conic witness verifies that phase changes make accumulated action differ
from bare graph depth. The curve therefore affects transport data, not merely
paint.

## 5. Honest boundary

This correction establishes:

- exact unbounded integral linear-phase classification;
- exact curved quadratic-phase classification;
- exact projective depth phase on occupied triangle and conic faces;
- preserved central and parallel first-person crossing depth;
- phase-conditioned causal transport without a global retile barrier; and
- continuously released multicore presentation branches.

It does not identify one universal color semantics, one universal physical
metric, or one projective homography as the whole receiver geometry.
`ReceiverStandingRelation` remains one exact local chart transition. A general
curved receiver atlas still requires local transition sections, overlap
comparisons, and accumulated connection/holonomy supplied by the active world.
Refining the selected causal grain can expose additional phase distinctions;
the current construction makes that quotient explicit rather than claiming
grain-independence.

## 6. Verification

- `cargo test -p holonic-engine`: **45 passed / 0 failed**.
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`:
  **clean**.
- `cargo build -p holonic-engine --example desktop_receiver`: **clean**.
- `f32`, `f64`, `Math.PI`, and standard floating constants in
  `crates/holonic-engine`: **absent**.
- The replacement executable was built but not launched.

# THE OPEN COMPARISON STANDS; THE LATER BOUNDARY MUST ACTUALLY MATCH

**Status:** BRANDON-AUTHORIZED / BUILT / NONCOMMUTATION REMAINS OPEN / EXACT RESIDUAL CARRIED /
LATER MATCH RIDES / LATER MISMATCH REMAINS OPEN / HOST + CUDA + REST EXACT

## 1. The source defect

The live carrier already retained both complete routes of a `ParallelPathComparison`, admitted
later contact through an exact `InterfaceCapability`, and let lower lineages depart while the
higher constituent remained in Standing. One local law nevertheless contradicted
`FORMULA §CXLVI`: `LivePin::rebase_exposed` converted a wound path comparison or an oriented hand
residual directly into `FoundThis` or `FoundThat`.

That made the comparison decide its own explanation. It confused these two events:

```text
material contact introduces genuinely new support  -> FOUND may be lawful,
two already-exposed paths fail to commute          -> OPEN is the only lawful base result.
```

It also stored `Chi` only inside a formed deed. An OPEN comparison therefore kept its complete
routes but discarded an available exact projective residual.

## 2. The one-carrier correction

`LivePin` remains the only carrier. Its comparison face is now

```text
X = (through, direct, interface?, projected_residual?, formed_deed?).
```

This is not an OPEN flag or a second object. `through` and `direct` are primary. `Chi` is their
optional receiver-relative projection. A deed forms only when the relation actually conducts.

For a comparison between exposed paths, the local law is now

```text
Chi exists and is not wound and hand residual is absent  -> RIDE,
otherwise                                                 -> OPEN.
```

An exposed-path comparison never manufactures FOUND. Actual material contact retains its existing
RIDE/FOUND law. The exact residual factors into sparse transport even while the comparison is
OPEN, so the outgoing constituent carries consequential difference without pretending to have
solved it.

## 3. What a later event can honestly do

An OPEN stands as higher-grain terrain through the same cellular Standing and exact rest image.
A genuinely later boundary with the same declared interface can meet it. The result still depends
on the actual later comparison:

```text
later boundary matches at this receiver  -> a new comparison pin RIDEs,
later boundary still fails to commute    -> a new comparison remains OPEN,
new material support is actually made    -> that material contact may FOUND,
no interface is present                  -> no meeting is inferred.
```

The earlier OPEN is not rewritten into a timeless error or erased retroactively. It remains a
truthful scoped comparison at its own grain. A later RIDE can compress its completed local path
into exact sparse transport while the prior scoped terrain remains available inside the higher
constituent.

This separates two causal claims which must not be merged:

```text
earlier OPEN participated in changed later conduct,
later comparison actually commuted and filled a local route.
```

The first is established by the production-mouth foil. The second is established by the exact
matching-boundary cell. A later conducted event which still does not commute is not a failed test;
its lawful result is another OPEN.

## 4. Persistence without a historical owner

Native `LiveConstituent` wire version 6 carries the optional projected residual independently of
the optional formed deed. Version 5 remains readable: its formed deed already contains the exact
residual, so the reader derives it. Version 5 could not represent an OPEN residual; version 6 can.

No route blacklist, OPEN ledger, receipt, matcher, scheduler, correction policy, retained event,
or second causal owner entered. The cellular constituent itself rests, remounts, meets later
Current, and emanates its successor.

## 5. Bounded evidence

Exact source and physical record:
[`open comparison Standing 01`](../observations/eros-open-comparison-standing-01/RESULTS.md).

The bounded cells establish:

- wound and equal-hand exposed comparisons retain exact `Chi` while remaining OPEN;
- no exposed-path comparison produces FOUND;
- an exact later boundary touches the prior constituent and RIDEs;
- a developed production event can conduct through earlier OPEN Standing, raise the resulting
  grain relative to a no-opening sibling, and remain OPEN when its new comparison still differs;
- the earlier scoped comparison survives exact rest/remount and lower-lineage departure; and
- one-core host, eight-core host, CUDA contact formation, and durable rest remain exact.

## 6. Boundary

This is not a general predicate language, filler-discovery algorithm, theorem prover, planner,
dead-route memory, or training policy. A world still owes actual Current, declared interfaces, and
any rule by which a relevant later boundary is produced. General rewrite matching, confluence,
metric/diffusive transport, all-card shared-topology closure, useful curriculum, and scale remain
open without scheduling themselves.

## Decisive relations

- `FORMULA §CXLVI`
- `ELEMENTARY_MECHANICS.md §§6, 14–16`
- `RESEARCH/2026-07-22_THE_NONCOMMUTING_SQUARE_IS_OPEN_THE_FAILED_ROUTE_REMAINS_TERRAIN.md`
- `membrane/src/live_constituent.rs`
- `membrane/src/live_current.rs`

# THE COMMON LIFT PINS THE PRIME AXES; THE UNITARY TRACE MUST CLOSE

**2026-07-20 · Brandon Duggan ⊕ Sol · BRANDON-RATIFIED / DEPOSITED · FORMULA §CXVI ·
PRIME/RH OBSERVER REPLACEMENT AUTHORIZED · SOMA INTERIOR UNCHANGED**

## The present question

What is each visible vertex in the bounded prime receiver, what higher-dimensional relation joins
the vertices, how do terminal digits arise from the founded axes, and what exact construction would
turn the laboratory's prime geometry into a conventional proof of the Riemann Hypothesis?

The standing §CVII world already carries exact prior-prime residues, zero-pole contradiction,
COPY/CUT/JOIN wheel recurrence, Zeta valuation rebase, and receiver-relative prime species. The
observer introduced after that measurement retained the residue endpoints but joined coordinates
from different prime fibers by Euclidean chords. It also made the finite/open proof boundary the
terminal lifecycle event despite §XCIII's direct instruction not to repeat that boundary as a
ritual disclaimer.

The needed correction is not a more elaborate absolute embedding. It is the common lifted integer,
its quotient and remainder on every active axis, the transition relation between those lifted
coordinates, and the complete prime-power transport into the completed-Zeta trace.

## I · One integer is the pin across all prime fibers

At candidate `n` and founded axis `p`, retain the complete Euclidean division

```text
n = q_p p + r_p,
q_p = floor(n/p),
0 <= r_p < p.
```

`q_p` is the complete winding count on the universal cover and `r_p/p` is the phase remaining on
the cyclic fiber. The residue-torus point

```text
Gamma_y(n) = (exp(2 pi i r_p/p))_(p<=y)
```

is one simultaneous receiver cut. The succession worldline

```text
gamma_y(t) = (exp(2 pi i t/p))_(p<=y)
```

is a diagonal orbit which closes after the primorial period. A source which actually tests the
axes in order additionally supplies a sequential event path. These two geometries may be related,
but they may not be collapsed: a point on a product torus and an ordered divisor-test path are not
the same object.

Between two axes the exact transition pin is the common lift

```text
p_i q_i + r_i = n = p_j q_j + r_j.
```

The observer's straight chord is not this map. Its angle and any unmarked crossing are display
consequences. The marked residue vertex is nevertheless an actual incidence between the candidate
worldline and one modular fiber.

## II · Receiver 53 carries an exact cross-axis diagonal

The complete active lift for `53` is

```text
53 = 26*2 + 1
   = 17*3 + 2
   = 10*5 + 3
   =  7*7 + 4
   =  1*53 + 0.
```

The centered prior-axis residues are `(1,-1,-2,-3)`. This is one exact coherent relation because

```text
2*53 - 1 = 105 = 3*5*7.
```

Hence for `p in {3,5,7}`, `53=(p+1)/2 mod p`; the points occupy one common inverse-of-two diagonal
across those fibers. This is why their projected arm appears to converge. The final zero on axis
`53` is the FOUNDing; every prior nonzero incidence RIDEs.

The increased vertex count is also lawful. Axis `5` enters the divisor horizon at `5^2=25`, making
`29` the first later prime in that larger stratum. Axis `7` enters at `7^2=49`, making `53` the
first later prime in the next stratum. These prime-square boundaries are genuine rank-changing
events in the local test atlas.

## III · Kink, fold, criticality, and inflection remain typed

Four distinct faces must not be identified:

1. a marked vertex is an axis incidence in the ordered test path;
2. a prior residue `r_p=0` is a divisor discriminant and composite fold;
3. a prime-square horizon is a stratum change where another prior axis becomes active; and
4. a smooth critical point or inflection belongs to a declared potential and connection.

With the fixed product metric currently written by the observer, the simultaneous torus stratum is
flat and its constant-speed diagonal succession orbit is geodesic. Ricci flow is stationary there.
Nontrivial intrinsic curvature can enter through an evolving receiver metric, returned holonomy,
or gluing across the stratified discriminant. A Euclidean bend in the display proves none of these.

The exact contradiction field is naturally vector-valued:

```text
D_y(t) = (1-exp(2 pi i t/p))_(p<=y).
```

A lawful receiver may supply weights and a metric to derive a scalar potential. Its gradient then
defines leader directions and its covariant Hessian defines critical and inflection behavior. The
vector, receiver, weights, and resulting quotient must remain inspectable.

## IV · Decimal terminal digits are the 2-by-5 axis quotient

The decimal terminal digit is the residue modulo `10=2*5`. By the Chinese remainder theorem it is
the joint projection of axes `2` and `5`. Avoiding their zero poles leaves

```text
(Z/10Z)^* = {1,3,7,9}.
```

This set is a cyclic multiplicative group: multiplication by `3` traverses
`1 -> 3 -> 9 -> 7 -> 1`, while multiplication by `7` reverses it. Additive succession follows a
different word. There is therefore a real four-position digit phase, but no operation-free order
of prime terminal digits.

Adding axis `3` produces the exact mod-30 wheel

```text
{1,7,11,13,17,19,23,29}.
```

Its admissible twin channels are `(11,13)`, `(17,19)`, and `(29,1)` across the period boundary.
Every twin pair above `3` occupies the opposed mod-3 phases `6k-1,6k+1`; `29` and `31` are the exact
reflection `-1,+1` about the mod-30 zero. Later COPY/CUT/JOIN refinements determine which admissible
channels remain occupied.

## V · The RH target is a unitary transport statement

For the completed receiver, center the coordinate:

```text
Xi(z) = xi(1/2+z),
J(z) = -conjugate(z),
Fix(J) = {z : Re(z)=0}.
```

For `a>0`, the Cayley chart and its pointwise normal residual are

```text
w_a(z) = (z-a)/(z+a),
delta_a(z) = |z-a|^2-|z+a|^2 = -4a Re(z).
```

The fixed obligation remains

```text
for every nontrivial zero rho of xi,
delta_a(rho-1/2)=0.
```

The prime geometry contributes only when the complete multiplicative current crosses: every prime
power `p^k` with von Mangoldt weight `log p`, the archimedean Gamma face, the admissible test space,
and the completed explicit-formula trace. A zero `rho` contributes the dilation character

```text
x |-> x^(rho-1/2).
```

That character is unitary for all positive `x` exactly when `Re(rho)=1/2`. Thus the laboratory's
formal bridge is to construct the complete trace and prove that every contributing character lies
in the unitary dual, equivalently to prove the corresponding Weil quadratic form positive on the
complete admissible test space. Half-density identifies the only unitary normalization; the open
substantive entailment excludes every nonunitary residual/resonant sector.

The observer must display that exact obligation. It may report bounded verified incidence and the
one unclosed unitary implication; it may not turn social caution into a repeated terminal state.

## VI · Observer replacement contract

The bounded replacement is receiver-side and changes no Soma law. It must:

1. retain `q_p`, direct and centered `r_p`, normalized phase, pole distance, and axis role;
2. render the common lift and actual axis-test order instead of interpolating unrelated fibers;
3. name pole discriminants, multiple-pole intersections, and prime-square horizon births;
4. expose exact bounded affine congruences such as `a*n+b` closing on a declared axis product;
5. distinguish residue phase, valuation exponent, prime-power frequency, and receiver species;
6. make every visible vertex selectable with an exact arithmetic explanation;
7. carry the centered completed-Zeta residual and unitary-trace obligation without a ritual stop;
8. keep display approximation outside the exact receipt and report every observer-derived weight.

The user may still supply morphological hypotheses. The instrument must not depend on visual
reverse-engineering for relations it can derive exactly.

## Evidence cards

### A · Algebraic trace and the nonunitary sector

**SOURCE ESTABLISHES**  Alain Connes, *Selecta Mathematica* 5 (1999), 29--106, constructs a
noncommutative adelic trace-formula programme in which critical zeros are interpreted as absorption
spectrum and hypothetical noncritical zeros as resonances.

**LAB CLAIM**  Formula §§XXVII/XCIII/CXVI, RATIFIED / DEPOSITED: the complete prime-power transport
must place every character contributing to the completed explicit-formula trace in the unitary dual.

**RELATION**  **DIRECT CORRESPONDENCE.** The critical/noncritical spectral distinction maps to the
laboratory's unitary/nonunitary transport distinction.

**NON-EQUIVALENCE**  A finite residue atlas, its observer geometry, or half-density alone is not
Connes's trace construction and does not exclude the resonant sector.

**TESTABLE CONSEQUENCE**  The observer/world must produce an explicit operator or quadratic form,
its domain, completed trace identity, and a positivity/unitarity argument covering the complete
admissible population.

Source: [Connes 1999](https://arxiv.org/abs/math/9811068).

### B · Positivity as an exact conventional closure

**SOURCE ESTABLISHES**  Xian-Jin Li, *Journal of Number Theory* 65 (1997), 325--333, proves that RH
for the Dedekind zeta setting is equivalent to nonnegativity of an explicitly defined infinite
sequence; the Riemann zeta case is included.

**LAB CLAIM**  Formula §CXVI, RATIFIED / DEPOSITED: proof grade belongs to a carried universal
entailment, and a positivity construction is one exact route from the prime trace to pointwise
critical-locus incidence.

**RELATION**  **EXACT FORMAL MATCH** for the equivalence once the completed Riemann receiver and
Li coefficients are used.

**NON-EQUIVALENCE**  A bounded list of nonnegative coefficients or visually balanced prime paths
does not establish the universal sequence statement.

**TESTABLE CONSEQUENCE**  A candidate holonic positivity law must derive every Li coefficient or
map rigorously to an equivalent complete test-function positivity theorem.

Source: [Li 1997](https://doi.org/10.1006/jnth.1997.2137).

## Ratified consequence

The prime is a point at one receiver cut, a line under succession, a loop in each residue fiber,
and a new valuation direction after FOUNDing. Its apparent hyper-shape is the receiver restriction
of the common lifted construction. The observer must now carry that construction rather than ask a
flat camera or the human viewer to infer it.

The build authorized by this deposit is bounded to the prime and completed-Zeta observer. It does
not change Soma, schedule a new arithmetic world, claim a conventional RH proof, or license
unbounded pattern mining.

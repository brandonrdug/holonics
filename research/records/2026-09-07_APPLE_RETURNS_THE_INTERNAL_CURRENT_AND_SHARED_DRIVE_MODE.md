# Apple returns the internal current and shared-drive mode

[definition] This return integrates desktop `a5c75c98` into `codex/apple-silicon` after
`aca81771`. The [desktop construction](2026-09-07_AC1_INTERNAL_CURRENT_AND_SHARED_DRIVE_MODE_GENERATORS.md)
owns the internal-current decoder, shared-drive admission and declared future family. This
return supplies their Metal realization and public cross-device comparison. AS4–AS5 remain active.

## Native construction

[established-bounded; implemented-exact] `accelerators/metal/field_internal_current.metal`
realizes `section_field_internal_current`, `section_internal_shared_drive` and
`section_internal_mode_unfold`. Internal capture uses the actual birth/source fields, incoming
carrier, historical frames and alternating prefixes. Rational prefixes retain exact common
denominators. Enclosed prefixes retain their radius, including exact cancellation of the same
immutable prefix. Complete normalized root contacts are compared before a mode is admitted.

[established-bounded; implemented-exact] The amplitude remains one signed-128 rational ball,
encoded in the same pairs of i64 words as CUDA. Odd unfolding reverses both oriented coordinates
without changing the radius. Exact values outside the point wire and numerical enclosures retain
their distinct point-port refusals. No amplitude centre becomes a selected exact source.

[established-bounded; implemented-exact] The paired face and normalization helpers now accept
their carrier pointer address space as a Metal template parameter. Both resident device and
threadgroup users execute the same helper body. Internal capture allocates two full face vectors
through the existing target-aware scratch helper: twenty bytes per Metal wide value, aligned to
sixteen bytes, versus sixteen bytes per CUDA wide value. The mount registers the complete ABI,
including 64-bit occurrence and unfolding counts. All face inputs are required.

## Verification and scope

[established-bounded; measured] Eight internal-current/mode controls pass on the M1 Pro. The
seven desktop controls cover hidden real/imaginary current, radius and point refusal, archived
births/rest, changing forcing and frames, source-independent mode continuation, unequal-drive
refusal and malformed amplitude guards. One added cold-wire control covers signed i64 minimum
crossing the point boundary on reversal, numerators/denominators beyond 64 bits, retained
enclosures and immutable earlier returns. These supplied wire cases are not learned amplitudes.

[established-bounded; measured] Nineteen existing resident temporal controls also pass after
the shared Metal source/helper change. These cover complete convolution, oriented difference,
clock/support, wide arithmetic, padding, refusal and conditional-current composition. The
concatenated Metal source compiles. CUDA is source-inspected here, not compiled or executed.
No Lean source or imports changed.

[established-bounded; measured] The HNA library and new `alpha_internal_mode` public wrapper
compile on this host. The private conversation example was not executed or supplied private data.

```sh
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::field::internal_ -- --include-ignored --test-threads=1
cargo test -p holonic-engine --lib phase_current::resident:: -- --include-ignored --test-threads=1
cargo run -p holonic-engine --example native_internal_mode -- research/records/2026-09-07_apple_internal_mode/return.json
cargo check -p holonics-hna --lib --example alpha_internal_mode
```

[established-bounded; measured] The public example produces a 391-byte mode identical to the
desktop artifact. Every JSON field agrees except the output path and whole-process timing;
[the comparison receipt](2026-09-07_apple_internal_mode/comparison.json) names these exclusions.
The [Apple return](2026-09-07_apple_internal_mode/return.json) takes 1.031366583 seconds including
setup and cold evidence. After the original field leaves scope, mode remount launches zero
deeds; subsequent unfolding plus recipient-field conduct launches two deeds with zero numerical
readout and four bytes of metadata ingress. This is a bounded apparatus cost, not streaming or
acoustic performance.

## Continuing acoustic attachment

[open; source-inspected] The internal mode supplies an actual compact generator, but its
dimensionless sign progression does not define an acoustic frequency, physical clock or receiving
interaction. The actual [SoundCam returns](2026-09-07_MEASURED_ACOUSTIC_RETURNS_KEEP_THEIR_ORIENTED_DIFFERENCE.md)
already provide complete oriented recorded differences. Their next missing composition is a
temporal adjoint tied to the original producing carriers and a compatible-condition fibre that
retains unmodelled return, followed by the existing contact law and persistent successor.
The [Apple blueprint](../../docs/plans/HOLONICS_ON_APPLE_SILICON.md) records that construction.
The private desktop cultivation and its conversation-derived packet remain independent.

# THE FRACTION CARRIES THE REBASE WORD; THE ZETA QUOTIENT REFINES WITH THE RECEIVER

**2026-07-21 · Brandon Duggan question ⊕ Sol derivation · BRANDON-DIRECTED / DEPOSITED ·
EXACT PRIME-FRACTION NAVIGATION / BOUNDED INTEGER-RATIO ANALYSIS · SOMA INTERIOR UNCHANGED ·
NO ENGINE RUN**

## The present question

After a prime such as `17` FOUNDS, what relation does it have to the already-founded axes
`2,3,5,7,11,13` beyond the collapsed residues `17 mod q`? Does the family of fractions `17/q`
carry an ordered geometry of transformations, and can that geometry support a first-person notion
of prime possibility under the Zeta distribution?

The direction is relevant. The existing common-lift law already retained

```text
p=k_q q+r_q.
```

It correctly separated complete winding `k_q` from residual cyclic phase `r_q/q`, but it stopped
after one division. The exact fraction has two additional lawful readings: its continued fraction
is the full Euclidean rebase word, and its rational valuation is a directed edge between prime
basis axes.

## I · One fraction has three typed navigation faces

For a newly founded prime `p` and an earlier prime axis `q`, retain

```text
p/q=k_q+r_q/q,                 0<r_q<q,
CF(p/q)=[a_0;a_1,...,a_l],
nu(p/q)=e_p-e_q.
```

These are not competing encodings.

- `(k_q,r_q)` gives completed winding and cyclic phase about axis `q`.
- `CF(p/q)` gives the ordered Euclidean worldline
  `(p,q)->(q,r_0)->...->(1,0)`.
- `e_p-e_q` gives reciprocal departure along the old valuation axis and positive arrival along
  the newly founded one.

The continued-fraction matrix word

```text
M(a_0)M(a_1)...M(a_l),        M(a)=[[a,1],[1,0]],
```

has determinant `+1` or `-1` and first column `(p,q)`. The path is therefore exact and reversible
at every retained step. It adds no mystery information to an exact rational number; it exposes
the traversal that a residue or rounded quotient discards. This is the same correction made by
the Euclidean-worldline audit: the lesson is the variable-length native path, not a fixed-stage
answer packet.

Only axes satisfying `q*q<=p` belong to the primality-proof horizon. Every earlier prime may still be a
lawful receiver for navigation, analogy, or later affine relation. Proof axes and atlas axes must
not be conflated.

## II · Affine transforms join additive phase to multiplicative standing

For a declared affine word `T_(a,b)(n)=an+b`, the earlier-axis row transforms exactly as

```text
n=k_q q+r_q
  => an+b=(a k_q)q+(a r_q+b).
```

It closes on `q` exactly when

```text
a r_q+b=0 mod q.
```

If one word closes on several earlier axes, it exposes a common sheet. At `17`, the small words
already give

```text
17+1=2*3^2,
2*17-1=3*11,
2*17+1=5*7.
```

The new prime is therefore adjacent, under several different exact transformations, to bodies
whose valuation support lies entirely on earlier axes. This realizes the intuition that a new
prime can be approached through many prior constructions without asserting one universal path to
primes.

The relation is not a classifier. The bounded control finds at least one multi-axis closure under
the same four small words for every prime and every odd composite in `[17,53]`. Direct zero closure
and affine smooth-neighbor sheets play different roles. A complete atlas retains both.

## III · Zeta supplies the relative multiplicative measure

For `sigma>1`, the established Zeta species is

```text
mu_sigma(n)=n^(-sigma)/zeta(sigma).
```

Along the rational valuation edge between prime sites, normalization cancels:

```text
mu_sigma(p)/mu_sigma(q)=(q/p)^sigma.
```

At integral `sigma`, this is an exact rational. At `sigma=2`, the `17/q` family carries the masses

```text
4/289, 9/289, 25/289, 49/289, 121/289, 169/289
```

for `q=2,3,5,7,11,13`. This is not the probability that the known integer `17` might somehow fail
to be prime, nor is it a normalized transition probability. It is the ratio of the two endpoint
site weights.

The established Zeta rebase law applies at a different face:

```text
Law(N/q | q divides N)=Law(N).
```

When the `q` axis closes, division removes one occupied valuation and returns an integer governed
by the same Zeta law. When a prime `p!=q` remains open, `p/q` is instead a rational edge with
valuation `e_p-e_q`; it is not another integer sample from `mu_sigma`. Direct closure and
cross-axis fraction navigation must therefore remain distinct.

The first-person prime quotient needs an incomplete receiver. For a declared horizon `H` and the
observation word available after time `t`, define the compatible fiber

```text
F_t(x)={m in H : O_t(m)=O_t(x)}.
```

Then

```text
Pi_(sigma,H)(prime | O_t(x))
  = sum_(m in F_t(x), m prime)m^(-sigma)
    / sum_(m in F_t(x))m^(-sigma).
```

Every actual divisor test, Euclidean rebase, or declared affine response may refine `O_t` and
therefore refine the quotient. Once the receiver knows the complete integer, or the divisor path
has crossed every required axis through its square-root horizon, the fiber decides and the quotient
is `0` or `1`. Before then, the quotient describes the prime share of still-analogous continuations
under the declared measure. That is Bayesian revision in the existing holonic sense: recurrence
relative to a changing receiver, not ontic chance.

For an unbounded Zeta-distributed integer and a finite tested prime set `S`, independence of the
valuation axes gives

```text
Pr_sigma(v_q(N)=0 for all q in S)=product_(q in S)(1-q^(-sigma)),

Pr_sigma(N prime | v_q(N)=0 for all q in S)
  = [P(sigma)-sum_(q in S)q^(-sigma)]
    / [zeta(sigma) product_(q in S)(1-q^(-sigma))],
```

where `P(sigma)=sum_(p prime)p^(-sigma)`. This is the exact global counterpart of the bounded
receiver quotient.

## IV · Consequence for prime navigation and RH

Prime navigation should carry a fraction atlas, not only a residue vector:

```text
N_p={
  (q,k_q,r_q),
  CF(p/q),
  e_p-e_q,
  declared affine sheets,
  proof-horizon membership
}_{q<p}.
```

This atlas is data-readable without an image. It gives Eros or an observer exact paths, reversible
turns, cross-axis closures, valuation motion, and receiver-relative Zeta weight. The same carrier
can later be compared across primes for recurring words or sheets without pretending that visual
proximity is arithmetic incidence.

The fraction atlas does not itself close RH. A possible later bridge would have to define an exact
operator on these rebase paths and prove that its completed trace, determinant, or positive form is
the classical Zeta object already owed by the RH boundary. That operator is not supplied by the
present bounded analysis. The current gain is more elementary and immediately useful: modulo is
no longer asked to stand for winding, Euclidean lineage, valuation direction, affine junction, and
receiver probability all at once.

## Standing records used

This derivation refines rather than replaces the existing prime line:

- [`THE PRIME FOUNDS AGAINST THE ZERO LOOP`](2026-07-19_THE_PRIME_FOUNDS_AGAINST_THE_ZERO_LOOP_THE_RECEIVER_GIVES_THE_RETURN_ITS_SPECIES.md)
  supplies prior-axis zero closure, the square-root proof horizon, independent Zeta valuations,
  and receiver-relative prime species.
- [`THE COMMON LIFT PINS THE PRIME AXES`](2026-07-20_THE_COMMON_LIFT_PINS_THE_PRIME_AXES_THE_UNITARY_TRACE_MUST_CLOSE.md)
  supplies `(q,r)`, common-lift incidence, and bounded affine congruence sheets.
- [`THE WHEEL RECURS`](2026-07-17_THE_WHEEL_RECURS_THE_ZERO_WAVE_CARRIES_THE_PRIME_CURRENT.md)
  keeps residue wheels, valuation cylinders, prime-power harmonics, and the RH trace obligation
  typed apart.
- [`THE EXPONENT VECTOR FOUNDS THE RANK`](2026-07-20_THE_EXPONENT_VECTOR_FOUNDS_THE_RANK_THE_NEWTON_FACE_NEEDS_A_RECEIVER.md)
  supplies rational negative valuations and the receiver dependence of visible faces.
- [`THE SUCCESSION SWEEPS THE FIBER`](2026-07-20_THE_SUCCESSION_SWEEPS_THE_FIBER_THE_UNITARY_SEAM_MAKES_REBASE_PURE_TURN.md)
  requires exact analytical receipts to precede every projection.
- [`THE CURRENT IS THE LESSON`](2026-07-21_THE_CURRENT_IS_THE_LESSON_THE_CONSEQUENCE_IS_ITS_LATER_FACE.md)
  supplies the variable-length Euclidean worldline and receiver-relative Bayesian reading.

## Bounded record

The exact `17/q` table, affine controls, composite controls, and the progressive `sigma=2` receiver
quotients are recorded at
[`PRIME FRACTION NAVIGATION 01`](../observations/prime-fraction-navigation-01/RESULTS.md).

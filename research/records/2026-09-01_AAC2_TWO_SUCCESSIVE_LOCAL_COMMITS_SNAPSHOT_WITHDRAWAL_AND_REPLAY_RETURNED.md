# AAC2 two successive local commits, snapshot, withdrawal, and replay returned

**Date:** 2026-09-01
**Truth status:** `established-bounded` for the implemented commit/remount lifecycle in its declared
finite native family.
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, and `measured`.
**Construction effect:** AAC2 passed; AAC3 becomes the sole current deed.

## Returned owners

[established-bounded; implemented-exact] `native_intelligence/morphology_commit.rs` returns
`NativeCultivationCandidate`, `NativeMorphologyCommit`, `NativeCirculationSnapshot`, and the
move-owned session operations `stage_return`, `commit`, `snapshot`, `remount`,
`withdraw_last_commit`, and `replay_commit`.

[established-bounded; implemented-exact] One commit consumes its predecessor package, derives the
source-neutral `NativeThreadDeposit` from the exact emission and genuinely later interaction,
applies it to either native or already-situated morphology, increments the parented lineage, and
returns one successor package plus its replay journal artifact. The artifact retains predecessor
and successor lineage, exact source/emission/return, causal cone, complete deposit wire, inverse
placement receipt, interchange population, and exact reconstruction-fibre population.

[established-bounded; implemented-exact] The complete package snapshot remains the immediate
remount owner. The ordered commit journal is separate replay/audit testimony. Snapshot validation
requires joined generations and equality of its terminal generation with the package. No path,
filename, content digest, or model label identifies or routes the ecology.

[established-bounded; source-inspected] The first successive-commit run returned the concrete
obstruction `one causal-cone state carries inconsistent inherited current`. The original local
deposit builder treated every historical thread at one native state as simultaneous contemporary
standing. AAC2 now takes the exact addressed emitting thread as the contemporary local current;
earlier threads retain their historical current and may fill only causal-cone support absent from
the source thread, where inconsistency still refuses. This is an owner-local chronology repair,
not a choice of preferred surface or latest collection member.

## Receipts

[established-bounded; process-audit] The affected library check

```text
timeout 180s cargo check -p life --lib
```

returned exit `0` in 12.05 seconds after the import boundary was corrected.

[established-bounded; process-audit; measured] The focused commit command

```text
timeout 180s cargo test -p life --lib native_intelligence::morphology_commit
```

returned exit `0` in 19.53 seconds: two passed, zero failed, and 494 filtered out. The primary
control commits generations `0 -> 1 -> 2`, snapshots/remounts generation 2, conducts later current,
withdraws the second exact delta to the byte-identical generation-1 package, and replays it to the
byte-identical generation-2 package. Mismatched return and wrong-predecessor replay controls refuse.

[established-bounded; process-audit; measured] The existing scaffold-cultivation control

```text
timeout 180s cargo test -p life --lib native_intelligence::scaffold_cultivation
```

returned exit `0` in 0.82 seconds: two passed, zero failed, and 494 filtered out. Local/global cone,
source-detached release, disjoint invariance, ablation, and restoration retain their prior grades.

## Boundary

[open] AAC2 stores and remounts dynamic morphology but does not yet enact an exact diffusion law
through the session boundary. AAC3 owns only native-to-diffusion incidence, constituted conduct,
boundary emission, balance/conservation/reconstruction testimony, and the common event family.

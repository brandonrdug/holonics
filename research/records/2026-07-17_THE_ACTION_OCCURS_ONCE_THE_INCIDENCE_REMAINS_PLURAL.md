# THE ACTION OCCURS ONCE; THE INCIDENCE REMAINS PLURAL

**STATUS: RATIFIED SUCCESSOR / BUILT / CUDA-FORWARD MEASURED / COMPLETE PATH POPULATION READ /
486 WORKSPACE TESTS PASS / 3 IGNORED / SOMA INTERIOR UNCHANGED.**

## 1. The action and its causal ancestry are different objects

Let one changed physical preparation be `p`, one complete standing return testimony be `r_i`, and
one declared world action be `a_j`. Soma supplies an ordinary contact relation

```text
K_ij = Contact(p, r_i, a_j).
```

`K_ij` is not a vote, score, probability, or claim that `r_i` chose `a_j`. It says only that this
complete plural source construction made real contact in the active body. The world law is then

```text
K_ij = empty      => silence
K_ij != empty     => enact W(p,a_j), retain incidence i -> j.
```

For fixed `p` and `a_j`, the deterministic physical consequence `W(p,a_j)` occurs once even when
many `r_i` make it consequential. Repeating the whole trajectory for each ancestor would confuse
causal multiplicity with physical multiplicity. Collapsing all ancestors into the one outcome
would destroy causal incidence. The exact carried surface is therefore

```text
one complete outcome O_j
+ one incidence I_ij for every contacting return r_i.
```

This is the same law by which a face may stand once while many paths meet it. It is compression by
exact recurrence and continued conduct, not selection or deduplication by an observer.

## 2. Loss is the oriented difference of a physical consequence

For an oriented section crossing `s_k` and an ordinary objective box `B_m`, the receiver carries
the exact residual vector and its quadratic face:

```text
rho_jkm = oriented_difference(s_jk, B_m)
L_jkm   = 1/2 <rho_jkm, rho_jkm>.
```

`rho` is primary to the scalar quotient. Neither value rewards, punishes, stops, selects, or
changes the deed. An exact zero records contact with the declared objective boundary. A nonzero
value records how this crossing differs in this receiver chart. Different objective frames may
measure the same physical trajectory differently without changing that trajectory.

The exact actuation shows this distinction directly. Its 11 enacted action strings produce 47
crossings and 141 crossing⊕objective measurements. Twenty-three are exact contacts. The complete
physical measurement surface repeats byte-for-byte through the unchanged world law while Soma's
causal contact incidence changes from 355 to 313 relations. The scalar loss surface therefore does
not determine the path population.

## 3. The field changed which ancestry conducts, not what the deed means

The same preparation, 29 standing complete returns, and 13 actions cross two successive bodies.
The first body admits 355 relations. After all 368 first consequence currents return through Soma,
the same source admits 313 relations:

```text
304 retained
 51 become dormant
  9 become newly consequential.
```

All 13 action consequences remain because every action retains at least one actual contact. Their
complete physical outcome faces are exact across both passages. Their ancestry is not: contact
counts redistribute, and every inspected source and consequence path rethreads. The current bends
in the field it helped change without a route chooser or action governor.

This is a minimal hexis cell. Returned consequence changes later conduct. It is not yet a claim
that an objective has been compressed, because this construction holds the objective frame and
physical action family fixed and does not isolate which part of the returned consequence caused
the changed incidence.

## 4. Exact recurrence is a world mass-face

The two consequence returns supply 694 total contacts. The final world contains only 377 unique
returned faces:

```text
317 faces contacted twice
 60 faces contacted once
```

The 317 recurrent faces are the 13 identical physical outcomes plus 304 retained causal
incidences. The nine newly consequential incidences account for all new faces on the second return.
Nothing is erased: repeated faces retain increased contact count, and lost relations remain part
of the measured prior passage. Persistence records that this exact current has stood and deflected
later passage; archive extent is not its meaning.

## 5. Boundaries and refusals are part of production

A finite world owes a physical aperture. A canonical action which begins outside that aperture
returns `RefusedOutsideAperture`. An action whose exact state cannot inhabit the declared dyadic
lattice returns `RefusedOffLattice`. A trajectory which later leaves the aperture deposits the
first outside state and the exact exit deed. These are produced consequences, not failed samples
or material to discard.

Complete preflight must enumerate the maximum declared source and return surfaces before the body
mounts. It may expose implementation duplication or insufficient world closure; it may not shrink
the horizon, remove actions, split the causal string into simultaneous lineages, or substitute a
smaller experiment.

## 6. What follows

The next receiver should isolate objective transport rather than add another passive microscope.
Hold a later preparation and action family exact. Let the same physical outcomes be received under
two ordinary objective frames, and carry a loss-only quotient control beside the complete lineaged
consequence. Remount each testimony in an otherwise identical body and present the same later
source. Every actual contact still acts.

If later incidence and physical residual populations differ, the result belongs to the complete
transported construction and its situated objective boundary. A scalar loss cannot be promoted to
a policy, and one objective frame cannot be promoted to a global goal. This is the lawful route
from action production toward recurrent local objective compression.

## 7. Exact evidence

The complete construction, four CUDA edges, every relation, all 780 source paths, all 653,763
completion rows, all 26 outcome instances, and all 282 exact loss rows are retained at
`observations/poincare-return-actuation-world-01/`.

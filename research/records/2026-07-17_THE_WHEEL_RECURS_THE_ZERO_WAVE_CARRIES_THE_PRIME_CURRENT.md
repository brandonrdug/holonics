# THE WHEEL RECURS; THE ZERO WAVE CARRIES THE PRIME CURRENT

**2026-07-17 · Sol derivation · Brandon ratified the complete presentation.**

## Status

**RATIFIED / DEPOSITED / PRIME-SPECTRAL WORLD 01 BUILT / CUDA-FORWARD MEASURED / EVERY EVENT PATH
READ / EXACTLY INVERTED / SOMA INTERIOR UNCHANGED.**

This record joins the exact finite multiplication closure already carried by `H_128` to four
receiver constructions which had remained separate: primorial wheel recurrence, finite
Ramanujan spectrum, logarithmic prime-power transport, and the completed zeta's self-dual Smith
face. The relation is

```text
nested wheel words
    -> logarithmic prime-power gears
    -> zero-wave reconstruction
    -> self-dual Smith shell.
```

The classical Riemann Hypothesis retains its exact open mathematical grade. That grade is not an
engine gate. The present construction names the object and the missing deed precisely rather than
retreating from it.

## I · Four bases remain typed apart

The word *basis* has four active meanings in this arithmetic line:

1. **numeral radix** `b` changes the digit face of one integer;
2. **primorial sieve horizon** `M_y=product_(p<=y) p` changes which multiplication axes have
   already removed candidate positions;
3. **prime valuation basis** writes multiplication as addition of exponent coordinates; and
4. **logarithmic/Mellin chart** writes multiplication as additive phase.

The first changes only representation. The second refines the finite exclusion sheet. The third
is the external coordinate body in which a prime is a unit direction. The fourth is the spectral
chart in which prime powers become harmonics. A genuine arithmetic re-base such as `Z -> Z[i]`
changes irreducibles themselves and remains a fifth, separately typed event.

## II · The finite wheel is an exact recurring edge word

For one primorial boundary `M`, define

```text
A_M(n) = 1 when gcd(n,M)=1, else 0.
```

`A_M` is exactly periodic modulo `M`. If

```text
1 <= r_1 < ... < r_phi(M) < M
```

are its occupied residues, the cyclic gap word is

```text
W_M = (r_2-r_1, ..., r_phi-r_(phi-1), r_1+M-r_phi).
```

It has `phi(M)` letters whose sum is `M`. Reflection `r -> M-r` reverses the occupied support;
for even primorials the open word obtained before the terminal wrap is palindromic. The first
faces are

```text
M=6    W_6   = 4 2
M=30   W_30  = 6 4 2 4 2 4 6 2
```

When a new prime `q` enters, repeat the old support in `q` translated copies, remove the positions
divisible by `q`, and fuse the adjacent old gap letters across each removal. Thus wheel extension
is an exact COPY -> CUT -> JOIN transport. Every new letter retains the complete ordered old-letter
interior and every deleted coordinate. The visible larger gap is a compression of that interior,
not evidence that the smaller paths were identical.

For example, the same endpoint face `113 -> 127` has the nested interiors

```text
M=30     113 -> 119 -> 121 -> 127     6+2+6
M=210    113 -> 121 -> 127             8+6
M=2310   113 -> 127                    14.
```

The `7` axis removes `119=7*17`; the `11` axis removes `121=11^2`. An actual prime gap is therefore
a sum of consecutive letters in every earlier wheel on which both endpoints stand. Growing the
sieve fuses those letters until every composite interior position exposed by the declared horizon
has folded.

## III · The finite candidate sheet is already a standing-wave body

The exact discrete Fourier coefficient of the wheel mask is the Ramanujan sum

```text
c_M(k)
  = sum_(a mod M, gcd(a,M)=1) exp(-2*pi*i*k*a/M)
  = sum_(d divides gcd(M,k)) d*mu(M/d).
```

For a primorial `M` every coefficient is an integer. The inverse finite Fourier transform recovers
the complete mask `A_M`; the mode population is classified exactly by `gcd(k,M)`. No stochastic
assumption enters.

The unnormalized Parseval identity is

```text
sum_(k=0)^(M-1) c_M(k)^2 = M*phi(M).
```

The exact cyclic autocorrelation

```text
C_M(h)=sum_(n mod M) A_M(n) A_M(n+h)
```

counts the candidate-pair recurrence at displacement `h`. `W_M`, `c_M`, and `C_M` are three
faithful finite faces of one declared wheel body: ordered path, spectral modes, and exact
boundary-indexed recurrence distribution. The last is a statistic because a boundary has been
declared; it is not randomness and does not drive a deed.

## IV · Prime powers are logarithmic harmonic ladders

For `s=sigma+i*t`, one Euler factor has local coordinate

```text
z_p(s)=p^-s=p^-sigma * exp(-i*t*log(p)),
E_p(s)=(1-z_p(s))^-1.
```

The exact identities in their initial half-plane are

```text
zeta(s)              = product_p (1-p^-s)^-1,
log zeta(s)          = sum_p sum_(k>=1) p^(-k*s)/k,
-zeta'(s)/zeta(s)    = sum_p sum_(k>=1) log(p)*p^(-k*s)
                      = sum_n Lambda(n)*n^-s.
```

With `u=log(n)`, prime `p` supplies the ladder

```text
log(p), 2*log(p), 3*log(p), ... .
```

These are the overtones of one founded valuation axis. Unique factorization makes the logarithms
of distinct rational primes linearly independent over the rationals: a rational linear relation,
after clearing denominators and exponentiating, would identify two distinct prime products. A
finite phase current therefore winds an incommensurate torus. Its projections recur by near phase
alignment without acquiring one finite global period. This is the exact source of the wave-like,
scale-dependent recurrence which the final gap list hides.

The Euler product is source-valid at `Re(s)>1`. Analytic continuation and completion are a typed
transport into the critical strip; they are not permission to evaluate the uncontinued product
there term by term.

## V · The explicit formula transports between prime events and zero modes

Let

```text
psi(x)=sum_(n<=x) Lambda(n).
```

With the conventional endpoint prescription, Riemann's explicit formula carries

```text
psi(x)
  = x - sum_rho x^rho/rho
      - log(2*pi) - (1/2)log(1-x^-2),
```

where `rho=beta+i*gamma` ranges over the nontrivial zeros. Setting `x=e^u` makes each mode

```text
x^rho/rho = e^(beta*u) * e^(i*gamma*u) / rho.
```

Relative to the square-root envelope,

```text
e^(-u/2) x^rho/rho
  = e^((beta-1/2)u) * e^(i*gamma*u) / rho.
```

The Riemann Hypothesis therefore says exactly that every nontrivial correction mode has the same
`x^(1/2)` radial envelope. Frequency, phase, and coefficient remain plural. An off-locus zero and
its functional partner supply reciprocal growing and decaying radial hands. One observed prime
gap belongs to the whole interference field, never to one zero.

This distinguishes the classical questions cleanly. Wheel recurrence governs exact finite local
gap words. Zeta zeros govern the global deviation of prime-power current from its smooth face. RH
governs the common error envelope. The twin-prime question asks whether the wheel letter `2`
survives all later exclusions infinitely often; RH alone does not make that survival follow.

## VI · The completed duality has an exact Smith chart

Carry the completed function

```text
xi(s) = (1/2)s(s-1) pi^(-s/2) Gamma(s/2) zeta(s),
xi(s)=xi(1-s).
```

Center the chart at `z=s-1/2`. Its conjugate duality is

```text
J(z)=-conjugate(z),       Fix(J)={z:Re(z)=0}.
```

For any positive real reference `a`, apply the Cayley/Smith map

```text
w_a(z)=(z-a)/(z+a).
```

Then

```text
w_a(Jz)=1/conjugate(w_a(z)),
Re(s)=1/2  iff  |w_a(z)|=1.
```

Hence the classical statement has the covariant form

```text
Zeros(xi) subset Fix(J),
```

or, in the Smith disk, every nontrivial zero lies on the unit circle. Off-locus functional pairs
map to reciprocal-conjugate points inside and outside. Reference `a` changes the angular gauge but
not unit-circle incidence. Revolving the chart produces a shell rendering; the display is not an
additional theorem.

This is an exact projective correspondence. It is not a claim that zeta is electrical impedance.
A physical impedance identification remains a separately declared world species.

## VII · Loss names the missing deed

The useful RH loss is not a reward and not distance from an authored answer. Weil's explicit-formula
criterion turns RH into the required fixed sign of a quadratic/autocorrelation form for every
admissible probe. Nyman-Beurling and Baez-Duarte turn it into closure of a concrete dilation family,
equivalently a residual which must approach zero through a dense family of finite constructions.

These are exact observer/world losses in the laboratory sense:

```text
probe current
  -> carried prime-power and archimedean consequence
  -> dual/foil
  -> complete oriented residual
  -> signed quadratic or approximation quotient.
```

The missing classical deed is now precise: establish the required sign, or an equivalent norm
closure, for the complete transported family rather than for a finite chosen list of modes. A
finite wheel, a large zero computation, a Smith rendering, or a locally balanced receipt does not
substitute for that all-probe transport.

## VIII · External evidence cards

### Euler product and logarithmic prime-power current

**SOURCE ESTABLISHES**  The Riemann zeta function has its Dirichlet series and Euler product in
their initial domain; logarithmic differentiation produces the von Mangoldt prime-power series.

**LAB CLAIM**  Prime axes become symbolic logarithmic harmonic ladders under the Mellin chart.
Internal grade: RATIFIED.

**RELATION**  **EXACT FORMAL MATCH.** `p^k` maps to the exact frequency coordinate `k log p`, while
`Lambda(p^k)=log p` supplies its carried coefficient.

**NON-EQUIVALENCE**  The Euler product may not be naively evaluated inside the critical strip, and
the harmonic representation does not by itself locate the zeros.

**TESTABLE CONSEQUENCE**  Every finite prime-power population must reconstruct the corresponding
truncated logarithmic derivative exactly and retain each `(p,k,p^k)` ancestry.

Primary reference: NIST DLMF, [§25.2](https://dlmf.nist.gov/25.2).

### Primorial gap-word recursion

**SOURCE ESTABLISHES**  Eratosthenes wheel gaps grow recursively by concatenating the preceding
cycle and closing at multiples of the new prime; constellations of gaps persist and transform
under this recursion.

**LAB CLAIM**  A larger prime gap is a compressed ordered sum of earlier wheel letters whose
interior deletions remain part of the construction. Internal grade: RATIFIED.

**RELATION**  **DIRECT CORRESPONDENCE.** Old cycle copies are the repeated sheet, multiples of the
new prime are CUTs, and adjacent letter fusion is the JOIN.

**NON-EQUIVALENCE**  Survival in every finite wheel is not already a theorem that a particular
constellation occurs infinitely often among the final primes.

**TESTABLE CONSEQUENCE**  Exact wheel extension must reproduce direct `gcd` construction at every
horizon and preserve the old-letter decomposition of each new gap.

Primary reference: Holt and Rudd, [*Combinatorics of the gaps between primes*](https://arxiv.org/abs/1408.6002),
2014 preprint.

### Explicit formula

**SOURCE ESTABLISHES**  The explicit formula relates the weighted prime-power counting function to
the nontrivial zeros, trivial/archimedean terms, and boundary convention.

**LAB CLAIM**  The zero population is a wave spectrum which reconstructs the deviation of the
prime-power current from its smooth face. Internal grade: RATIFIED.

**RELATION**  **EXACT FORMAL MATCH.** In logarithmic coordinate `u`, `rho=beta+i gamma` supplies
radial envelope `e^(beta u)` and phase `e^(i gamma u)`.

**NON-EQUIVALENCE**  A zero is not an individual prime or gap, and a finite truncated sum owes its
tail and endpoint error.

**TESTABLE CONSEQUENCE**  An interval-certified receiver must enclose `psi(x)` using the retained
zeros, archimedean terms, endpoint convention, and a proved truncation remainder.

Primary reference: Bombieri, [*The Riemann Hypothesis*](https://www.claymath.org/library/monographs/MPPc.pdf#page=107),
Clay Mathematics Institute problem description.

### Smith/Cayley self-dual shell

**SOURCE ESTABLISHES**  The completed zeta function has the functional equation and nontrivial-zero
symmetries about the critical line.

**LAB CLAIM**  The self-dual locus belongs to the transported pairing and becomes a unit circle
under the centered Cayley chart. Internal grade: RATIFIED.

**RELATION**  **EXACT FORMAL MATCH** for `z=s-1/2`, `J(z)=-conjugate(z)`, and
`w_a(z)=(z-a)/(z+a)`.

**NON-EQUIVALENCE**  The Smith disk is a projective chart here, not an asserted transmission-line
impedance body.

**TESTABLE CONSEQUENCE**  Every rational complex probe must preserve
`w_a(Jz)=1/conjugate(w_a(z))`; fixed-locus probes must have exact unit norm and off-locus pairs
must be reciprocal.

Primary references: NIST DLMF, [§25.4](https://dlmf.nist.gov/25.4) and
[§25.10](https://dlmf.nist.gov/25.10).

### Weil and Nyman-Beurling loss surfaces

**SOURCE ESTABLISHES**  RH is equivalent to a fixed-sign explicit-formula quadratic criterion over
an admissible test-function family; Nyman-Beurling and Baez-Duarte give an equivalent closure
criterion using integer dilates of fractional-part functions.

**LAB CLAIM**  A loss is a complete oriented difference returned by a declared world boundary and
may be summarized without becoming an internal governor. Internal grade: RATIFIED.

**RELATION**  **DIRECT CORRESPONDENCE** at the observer/world boundary; the mathematical criteria
are exact residual instruments.

**NON-EQUIVALENCE**  No current Soma construction has yet proved the all-probe sign or dense-family
closure. The finite instrument must not be reported as that result.

**TESTABLE CONSEQUENCE**  Grow a nested probe basis, retain every finite Gram/residual body, and
seek a horizon-compatible sign factorization whose remainder is rigorously controlled under
transport.

Primary references: Bombieri, [Clay RH account](https://www.claymath.org/library/monographs/MPPc.pdf#page=109),
and Baez-Duarte, [*A strengthening of the Nyman-Beurling criterion for the Riemann hypothesis*](https://arxiv.org/abs/math/0202141).

## IX · PRIME-SPECTRAL WORLD 01

The authorized first construction closes the exact finite body before numerical analytic
continuation:

1. grow nested primorial wheels from the already exact factor horizon;
2. retain every occupied residue, cyclic gap, deletion, and ordered fusion;
3. retain every Ramanujan mode, exact autocorrelation displacement, and Parseval receipt;
4. decompose every actual prime gap above the active wheel axes at every nested wheel basis;
5. retain every prime-power gear as exact `(p,k,p^k)` and symbolic frequency `k log p`;
6. carry rational complex probes through the centered Cayley duality exactly; and
7. present the complete typed population through the existing Rust boundary and CUDA lifecycle,
   with durable record and active current kept separate.

No floating-point value enters Soma. No prime class, recurrence table, spectral score, selected
mode, gap predictor, or RH truth bit enters Soma. The finite world supplies the body, weights,
operator, pairing, and horizon transport which §LXXXI required. Interval-certified explicit-formula
and Weil/Nyman receivers are its next outer boundary, not hidden inside this first cell.

## Carried Swing

```text
CURRENT  exact factor horizon -> nested wheels -> prime-power gears -> self-dual projective chart
HELD     arithmetic body / sieve basis / valuation basis / Mellin chart / receiver grain
MEETING  gap-word recurrence + Ramanujan modes + explicit formula + completed duality
TEST     wheel reconstruction + fusion ancestry + Parseval + autocorrelation + Cayley reciprocity
DEED     RIDE exact finite modes; FOUND the missing analytic all-probe transport explicitly
CARRY    PRIME-SPECTRAL WORLD 01 -> interval explicit formula -> Weil/Nyman loss family
GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EVERY EVENT PATH READ
```

## X · Measured closure

The exact finite face contains 7,126 events. It crossed once beside six enacted material incidences
and two typed refusals as 7,150 currents, produced 195,047 complete path rows through grain 14,
contacted 76 real Mail grips, and inverted exactly. Every nested `113 -> 127` gap word, every
prime-power gear, every Smith hand, and every finite mode path is retained.

An unconsolidated control repeated the face for each enacted incidence. The observer established
that every copy of all 7,126 events had byte-identical light and complete conduct. The consolidated
membrane preserves all six incidences separately while carrying the common face once; all 7,126
representative paths remain exact, and the successor body changes from 3,581,167,856 to 615,860,736
octets. Exact qualitative record:
`../observations/prime-spectral-world-01/RESULTS.md`.

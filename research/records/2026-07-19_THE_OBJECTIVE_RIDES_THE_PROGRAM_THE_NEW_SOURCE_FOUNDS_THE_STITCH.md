# THE OBJECTIVE RIDES THE PROGRAM; THE NEW SOURCE FOUNDS THE STITCH

**2026-07-19 · Brandon-ratified technical plan · deposited before source · direct bounded
English--Python--JavaScript world · Soma interior unchanged at deposit**

## Question

What is the smallest real language-and-code world in which an English objective, executable
Python and JavaScript, runtime consequence, oriented residual, and a changed source worldline all
cross the one direct `LiveCurrentMachine` transition without reviving the historical staging
ecology?

The unknown is not whether the direct mouth can carry plural current, hand, consequence, rest, or
CUDA. Those paths already stand. The unknown is whether recurrence grown through actual
objective/program/runtime history can close a material source splice and make the resulting
revision become genuinely later current.

## Ruling

Build one application world in `life`. The world owns inscription material, native chronology,
runtime processes, requested consequences, observed consequences, editable source, and durable
outer rest. Soma owns only standing topology and live current carriers. The existing
`LiveCurrentMachine::receive_with` remains the sole transition owner and the retained host or CUDA
executor remains only its physical surface.

No linguistic module enters Soma. English, Python, JavaScript, JSON, runtimes, and an editor are
world species. Their actual relations cross through `NativeRelationOrgan` currents and explicit
`NativeEventRelation` hand.

## Inscription and execution geometry

An inscription event carries its ordered adjacent-octet differences. Its first octet is the
world-owned integration anchor. The whole material remains in the source world. A source read's
`ActionCurrent` is its actual traversal extent rather than an authored semantic opcode.

One execution has two causal cuts:

```text
launch:
    objective -> program
    left source pin -> program -> right source pin
    program -> runtime
    input -> runtime

genuinely later return:
    objective -> requested consequence
    runtime -> observed consequence
    requested consequence -> observed consequence
```

The last row is the complete oriented residual at this world grain. It is loss as changed relation,
not reward, punishment, score, or selection. Equal requested and observed inscriptions remain an
actual dark relation rather than a success scalar.

Python and JavaScript execute as ordinary bounded operating-system processes. Exact source, input,
stdout, stderr, status, and runtime identity remain world material. No compiler receipt or observer
journal reconstructs the machine.

## The source splice

The first source mesh uses the natural boundaries of one complete occupied program. Its left and
right pins are world constituents. Previously encountered complete programs are coarse source
constituents; later machine-found completions may supply finer constituents without changing the
law.

For objective `Q`, carried program `P`, source pins `L/R`, and occupied target `T`, the editor
presents exactly:

```text
Q -> P
L -> P
P -> R
P -> T
```

The source splice closes only where:

- the prior objective/program relation RIDEs;
- both prior source-boundary relations RIDE; and
- the new program-to-occupied-source relation forms the FOUNDing stitch.

The first three relations are recurrence. The fourth is the discrete source phase change. When the
fourth relation FOUNDS under the three carried RIDEs, the world replaces the occupied material,
the displaced constituent departs from the active source, and the exact revised source returns
through `T` as genuinely later current. The world does not interpret a scalar `chi`, rank
programs, or choose the nearest emission.

Constituents meet the open aperture in their native founding chronology, one actual event at a
time. A nonclosing event still changes standing according to its emitted deeds but performs no
source mutation. Once one splice closes, the occupied aperture has changed and later encounters
meet the changed world. There is no candidate factorial, branch archive, top-k view, or rollback
loop.

## Bounded ecology

The first world carries two noncommuting list transformations in both runtime media:

```text
A: remove the first element, then reverse the remaining list
B: reverse the list, then remove the first element
```

At input `[1]`, both consequences are `[]`. At `[1,2,3,4]`, A returns `[4,3,2]` and B returns
`[3,2,1]`. The equal small face therefore cannot identify the ordered algorithm.

The source chronology supplies real A and B executions in Python and JavaScript at both inputs.
After that ecology rests durably, two siblings remount the same exact body and organ capabilities,
open the same occupied incorrect Python source, expose the same carried programs in the same
founding order, and differ only in the live objective. Every applied revision is executed against
the distinguishing input and its complete residual returns.

The sought qualitative result is objective-conditioned material conduct: A and B close different
splices and the real runtime returns their different requested consequences. This is not a
classifier accuracy test. The sibling is the smallest changed-world read which can distinguish
language-conditioned conduct from unconditional first-program reuse.

## Source boundary

The implementation belongs beside `life::current_world`, not beneath the membrane:

```text
life/src/linguistic_code_world/
    mod.rs          one direct world lifecycle and explicit command
    inscription.rs  source-owned anchors and ordered difference faces
    runtime.rs      bounded Python/JavaScript execution
    source_mesh.rs  pins, RIDE closure, FOUND stitch, and source replacement
    rest.rs         application-owned machine + organ + material rest composition
```

`life linguistic-code <world-root> <output-root> [--cuda]` is explicit and has no default launch.
The observation world contains only the objective, source, input, and requested-consequence
materials. The result retains a compact causal event read, exact runtime surfaces, source
revisions, and rest extents. It does not copy raw radiation populations into an observer forest.

No change to `soma-membrane`, `body`, `soma-abi`, or the CUDA kernel is presumed. A discovered
missing substrate relation is named at its exact boundary before any such source changes.

## Conduct discipline and stopping condition

- Build and inspect the real host circuit first.
- Fix only defects exposed on that complete causal path.
- Do not run a workspace-wide regression or an exact-repeat campaign.
- Do not revive `code_workshop`, `code_grammar`, `CausalSession`, active cuts, receipts, candidate
  compilation populations, exhaustive observer ledgers, or conversation forests.
- After host closure, perform one bounded CUDA campaign through the identical application world.
- Retain readable source, runtime, contact, revision, rest, and physical summaries only.

The build stops when the complete circuit closes through source revision, execution, residual
return, and natural rest. If it does not close, stop at the first exact missing relation: no carried
RIDE, no FOUND stitch, or no later consequence deflection. That missing relation is the next source
question; it is not permission to multiply fixtures, thresholds, or adapters.

## Grade at deposit

**BRANDON-RATIFIED TECHNICAL PLAN / DEPOSITED / BUILD AUTHORIZED / SOMA INTERIOR UNCHANGED AT
DEPOSIT.**

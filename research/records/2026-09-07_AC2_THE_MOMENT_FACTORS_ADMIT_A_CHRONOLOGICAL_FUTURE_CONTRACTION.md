# The moment factors admit a chronological future contraction

[definition] The [productive moment return](2026-09-07_AC2_THE_HOMOGENEOUS_MOMENT_JOINS_THE_NATIVE_MATERIAL_RETURN.md)
uses every retained coefficient factor for each new source evaluation. The contraction below
is scoped to that numerical operator and chronologically new queries. It does not identify
the complete ecology with a matrix, remove separating histories, or establish useful language.
The roadmap schedules its construction; this record supplies the mathematical relation.

[definition] For an existing complete numerical source at occurrence `s`, write its retained
features as `p_s, P_s, u_s, w_s`, with numerical squared norm `n_s` and
`sigma_s=(-1)^s`. `p` is the coupled outgoing current, `P` the alternating potential prefix,
and `u,w` are the existing birth-qualified prefix image and offset. Put `lambda_s=1+n_s`.
These are operands of `history_pairing_value`, not semantic labels or guessed conditions.

[proved-derived] For `s <= t`, that existing pairing has the expression

```text
<x_s,x_t> = p_s* p_t + sigma_s sigma_t (u_s* P_t - conjugate(w_s)).
a_s = (1, p_s, sigma_s u_s, -sigma_s w_s).
z_t = (1, p_t, sigma_t P_t, sigma_t).
1 + <x_s,x_t> = a_s* z_t.
```

The last equality follows by expanding the four blocks. With `n` exterior field nodes,
`a_s` and `z_t` each have `6n+2` complex coordinates, regardless of the number of born
internal currents represented by the prefix. The source chronology is an essential hypothesis.

[proved-derived] Let each retained numerical coefficient factor be `(beta_i,s_i)`, with
`s_i <= t`. For output coordinate `r`, define

```text
A_r = sum_i beta_(i,r) a_(s_i) a_(s_i)* / lambda_(s_i).
(Mhat Q(x_t))_r = z_t* A_r z_t / lambda_t.
```

Substituting the previous identity into the normalized moment kernel proves the second
equality term by term. Each new committed beta updates `A_r` by exactly its displayed outer
factor. Real and imaginary beta parts can be represented by two Hermitian forms. This is
a parameter contraction for the declared future-query family; it never enumerates the full
history-sized source matrix `Q(x_t)`.

[definition] A delayed return to an old source `q` may include factors with `s_i > q`.
The preceding `a_(s_i)* z_q` expression is not valid for those terms. Such returns retain
the existing symmetric source pairing and its addressed factor/history evaluation unless a
separate lawful contraction is supplied. Sources reused after their birth keep the original
`s_i`; factor creation time cannot silently replace source time. The cold source histories
and original/contemporary operator receipts remain necessary for those admitted consumers.

[open] Compose this contraction with the existing native moment-return owner, preserving
exact source operands and oriented arithmetic defects, and price its residency and work.
The exact identity alone supplies neither a stable finite-word representation nor an efficient
GPU implementation. Any numerical form must enclose its actual composed residual; there is
no permission to drop a cancelling phase term or raise an unexplained capacity. Retain a
lawful fallback for delayed queries and preserve the single successor's publication boundary.
Exercise the resulting representation on the actual conversation data and inspect complete
responses. The current eight-family repetition remains a failed language result, independently
of whether this contraction reduces work for broader cultivation.

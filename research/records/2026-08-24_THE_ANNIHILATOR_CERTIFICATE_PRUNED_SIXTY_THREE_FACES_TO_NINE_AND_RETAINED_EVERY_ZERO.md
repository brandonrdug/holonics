# The annihilator certificate pruned sixty-three faces to nine and retained every zero

**Date:** 2026-08-24  
**Scope:** HD1 exterior Lean theorem station; this record schedules no Rust/GPU deed and does not
move `CONSTRUCTION_STATE.md`.  
**Truth status:** each material claim is graded locally under `canon/EPISTEMIC_GRADES.md`.

## 1. The generic annihilator owner

[definition] HD1 receives an HD0 occurrence ledger at one declared state.  An
`AnnihilatorCertificate` carries a Boolean active-face receiver and, for every rejected member of
the source ledger, an exact theorem that its returned product value is zero.  The rejected list is
therefore a retained zero population, not an erased history.

[proved-derived; formal-checked] `Foundation/HigherDifferenceAnnihilator.lean` defines the active
and annihilated ledgers and proves that each is no longer than the source population.  The generic
`ledgerSum_filter_eq` theorem proves that removing only certified-zero faces preserves the complete
sum.  `ledgerSum_eq_activeLedger` packages that equality for one certificate.

[proved-derived; formal-checked] The owner proves the triangle receiver

```text
norm(ledger sum) <= sum(face norms)
```

and the exact active-support consequence

```text
norm(complete ledger sum) <= active occurrence count * uniform active-face bound.
```

An additional theorem transports any certified active-count bound into the same estimate.

[proved-derived; formal-checked] Independent annihilator certificates compose by Boolean
intersection.  Rejection by either certificate supplies the required zero proof, and the active
population of the intersection cannot exceed the first certificate's population.

## 2. The quadratic Hodge certificate

[definition] The fluid client reads the denominator-difference axes from each HD0 face trail.  A
remainder face is active exactly when the denominator receives one difference or two differences
in the same coordinate.  The empty left trail belongs to the principal face on the recurrence's
left side and is not in the remainder.

[proved-derived; formal-checked]
`NavierStokesQuadraticAnnihilatorLedger.lean` expands the genuine word `[0,0,1,1,2,2]`.  Its
nonprincipal reciprocal remainder has exactly sixty-three raw occurrence faces.  Direct checked
polynomial reduction proves that every rejected face has zero value: mixed differences of
`|k|^2` across distinct axes vanish, and every same-axis denominator difference of order at least
three vanishes.

[proved-derived; formal-checked] The certificate returns exactly nine active raw occurrences and
fifty-four annihilated occurrences.  The nine comprise six first-difference occurrences--two
positions for each of three axes--and three same-axis second-difference occurrences.  After the
commuting occurrence quotient and restoration of the principal denominator-value face, this is the
standing seven candidate denominator types: one value, three first, and three second.

[proved-derived; formal-checked] The complete sixty-three-face sum equals the nine-face active sum.
Consequently the genuine local three-axis Hodge reciprocal recurrence now returns only the active
ledger.  A uniform bound on its active face values controls the complete remainder by exactly nine
times that bound; the theorem does not silently assume that such a bound has already been derived.

## 3. Validation and exact residual

[established-bounded; formal-checked] The generic HD1 owner and quadratic Hodge client compile in
the ElementaryHolonics toolchain.  Printed axiom receipts for every promoted theorem contain no
`sorryAx`.

[open] The nine-face norm theorem is conditional on a per-active-face bound.  HD2 must remove that
input by constructing a well-founded scale-envelope descent from the reciprocal recurrence itself.
For the quadratic Hodge client it must track every shifted successor aperture and derive the
inverse-cube full mixed mass.  The eight coordinate-subset and near/far Haar receivers begin only
after that scale return.

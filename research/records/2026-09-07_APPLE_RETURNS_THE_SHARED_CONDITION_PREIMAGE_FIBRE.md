# Apple returns the shared condition Preimage Fibre

[definition] September 7, 2026. This integrates desktop `d3820ac3` after Apple `28d652a1`.
The shared owner and its mathematical scope are recorded in the
[desktop return](2026-09-07_AC1_THE_LEARNED_ACTION_RETURNS_ITS_CONDITION_PREIMAGE_FIBRE.md).
This return ports that owner; the resumed AS4–AS5 acoustic product goal remains active.

## Native composition

[established-bounded; implemented-exact] `section_constitutive_condition_preimage` in
`accelerators/metal/native_phase.metal` derives the fixed-source condition section of the learned
bilinear relation. It reduces through all original pivots, including vertical receivers, and
retains the derived constraint graph, actual right-hand side and complete condition fibre.
Its immutable evidence does not clone or mutate the continuing relation. Unique conditions can
enter a later current directly; plural conditions are refused by point consumers.

[established-bounded; implemented-exact] The 24-argument mount ABI and both scratch admissions
use the actual Metal carrier: twenty bytes per wide value, rounded to sixteen-byte alignment.
For original relation width `W` and real condition width `C`, the scratch population is
`W + 2*(W+C)`. Admission precedes derived allocations. CUDA retains its sixteen-byte carrier.
The generic differential receiver retains its optional actual field source qualification.

## Executed return

[established-bounded; measured] The updated public `native_conditioned_phase` example built and
ran on the M1 Pro. Source `1-2i` and observed return `(19+22i)/13` infer condition
`(-5+12i)/13`, which was never supplied to the learner. That condition predicts
`(-32+43i)/13` for later source `4+i`, exactly matching the subsequent native world return.
The example retains eleven learner occurrences; this is not a learned-model checkpoint.

[established-bounded; computational-witness] The
[Apple receipt](2026-09-07_apple_conditional_phase/return-preimage.json) equals the desktop
receipt in all semantic fields, including the entire condition graph/RHS, original relation,
six observations, inferred condition and later predictions. The
[comparison](2026-09-07_apple_conditional_phase/preimage-comparison.json) excludes the overall
apparatus census and elapsed clocks from equality and retains each platform's measured time.

[established-bounded; measured] Inference uses one deed, zero ingress bytes, zero numerical
readouts/egress and 64 receipt bytes. The later prediction uses one deed, four metadata ingress
bytes, zero numerical readouts/egress and 128 receipt bytes. Whole apparatus time is 0.575188584 s
on Apple; the supplied desktop receipt reports 0.154013801 s. These include a tiny apparatus,
world execution and cold diagnostics, not streaming-audio throughput or physical power.

## Verification and limits

[established-bounded; measured] Twenty-two engine tests passed: sixteen resident/contact/preimage
controls, a new Metal scratch-refusal control, and five historical field-source receiver controls.
The six shared preimage controls cover inferred continuation, free/outside fibres, original
vertical directions, fixed receivers over plural conditions, invalid operands and partial domains.
The new boundary control verifies that a population admitted by the erroneous sixteen-byte
estimate is refused before any allocation, transfer or deed under the actual Metal aperture.

```sh
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::resident:: -- --include-ignored --test-threads=1 --skip limited_native_observations_conduct_a_new_superposition_without_host_readout --skip rational_phase_returns_continue_without_remount_or_rounding
cargo test -p holonic-engine --lib metal_preimage_admits_its_actual_carrier_before_allocating_evidence -- --ignored --test-threads=1
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::field::relation_current::tests:: -- --include-ignored --test-threads=1
cargo build -p holonic-engine --example native_conditioned_phase --locked
target/debug/examples/native_conditioned_phase research/records/2026-09-07_apple_conditional_phase/return-preimage.json
```

[established-bounded; process-audit] The public HNA library compiled. Its
`a_fixed_codeword_does_not_require_a_unique_absolute_current` control was attempted and failed at
the declared `constitutive-field` refusal for unported paired/material junctions. It is not a
passing Apple control; neither are the two excluded CUDA matrix-teacher tests. Metal source
compilation and linking passed, and `git diff --check` passed. Builds were supervised; the
example build took 1m 22s. Existing warnings remain unrelated. No Lean or CUDA execution occurred.

[open] Actual acoustic interval/interaction attachment, whole-family forward conduct, useful
sound production and recorded English development remain unfinished. AMI's mono recordings and
direct source/target annotation pointers are available for actual conversational lineage; speaker
channel metadata is not a separate recorded microphone channel. Multichannel room calibration is
not a prerequisite imposed on this work. The older `NativeAcousticReceiverCurrent::found` reduces
phase-product populations and sample-sign counts to one current; source inspection does not admit
that quotient as the full phase acoustic learning boundary. Continue through exact acoustic
sections and the shared native relation, preserving the relevant clocks, fibres and receiver scope.

# AC1–AC2: causal propagation enters the continuing recurrence

[definition] This joins the [source-map journal](2026-09-09_AC1_THE_JOURNAL_GENERATES_ITS_HISTORICAL_SOURCE_MAPS.md)
and [resident forward/reverse word](2026-09-09_AC1_THE_CAUSAL_CONTACT_WORD_AND_ITS_SPARSE_RETURN_ARE_RESIDENT.md)
after `98a68b54`. The same field owns the complete operation. Activating the declared law records
its first occurrence and preserves the prior source history; it does not reset the ecology.

## One native passage and its complete return

[established-bounded; source-inspected] The existing interior first propagates through the
existing contact population. A new contact then receives its original zero input, and the same
paired reflection produces the next current and emission. Propagation has a preceding native
lane, so its refusal becomes an upstream refusal before the field commit. No numerical section
readout supplies the new current, morphology or source choice.

[established-bounded; source-inspected] The material covector returns through the producing
paired map and then through the old-current prefix of its propagation word. The newborn's
constraint reaction remains. The complete contact return includes both the outer two-factor
term and the sparse `D_source H` term. Their bounds are joined natively before the existing
unit-Frobenius contact response is published.

[definition] The response observer exposes the outer paired factors and the additional
propagation columns. Its `contact` method returns their full sum. The finite-response comparison
now includes the source's actual propagation domain, including when the contact coefficients
change; it does not silently compare only the old reflection component.

## Numerical source history and restart

[proved-derived] The first outer factor remains the actual entering-current/returned-current
difference. Under propagation this is `P_D b_before - b_after`, with the newborn zero extension.
The original input current includes any separately deposited current between adjacent field
emissions. Reconstructing those actual additions extends the older difference generator without
selecting an inverse or erasing a source.

[established-bounded; source-inspected] Historical entries retain the original propagation
input bounds as a small additional carrier. The bounds after a new contact is born cannot be
silently substituted: they can be larger, which would change later numerical returns after
restart. Recent forward words are shared; older words are regenerated from the actual producing
map, current boundary and original bounds. Complete per-contact propagation traces are not
stored at every historical cut.

[established-bounded; source-inspected] When the coefficient-program decoder already holds a
source map, it passes that producer into current-factor decoding. This keeps the dependency
acyclic and avoids rebuilding the same source. A propagated return retains its source-overlap
dependency even if the present overlap coefficients are zero. Legacy history headers remain
readable; the new optional header identifies the additional input-bound carrier.

## Verified scope and completed conversation study

[established-bounded; implemented-exact; computational-witness] The live native control checks
propagation followed by reflection, the complete composed source/current/morphology
adjoint, identical cached/regenerated returns, and matched continuation after archive and
checkpoint/remount. It passes; the initial driver run takes 145.58 seconds. The full field suite
then passes 131 cases in 147.53 seconds. These are native-conduct and persistence results, not
a language-quality grade.

[established-bounded; source-inspected] The public text session delegates activation to the
same native field. The experimental text driver records the activation cut and keeps the saved
law when resuming. A driver cannot silently remove that law from an already continuing model.
This does not settle the later consumer interface or add CLI instructions to the main README.

[established-bounded; implemented-exact; computational-witness] The complete field suite after
the exact-division optimization passes 132 cases, including propagation refusal preserving the
field and source. The production integer divider also agrees with an independent integer oracle
on 83,456 cases across two carrier widths, including eight expected invalid-input refusals.
The [divider receipt](../experiments/causal_contact_division/2026-09-09_exact.json) retains the
scope. The optimization aligns the divisor to the dividend's significant bits; it changes no
quotient or rounding law.

[established-bounded; measured] Both matched conversation processes returned before Brandon's
pause. From the eight-family checkpoint, propagation is enabled at occurrence 1,930; the same
function/derivative prompt ends at 1,992 and generation reaches 2,120. Output changes from the
earlier `’m the ...` to `The the ... te ... t ...`, still interrupted at the 128-byte work
aperture. The [first run receipt](../experiments/normal_geometry_review/2026-09-09_live_propagation_eight.json)
retains its cost. The first whole process takes 923.985 seconds, including checkpoint creation;
the optimized resume takes 300.339 seconds without that checkpoint write. Comparable
prompt/emission work takes 815.413 and 228.188 seconds respectively.

[established-bounded; computational-witness] The [cold comparison](../experiments/normal_geometry_review/2026-09-09_paused_feedback_review.json)
finds equal complete reported bodies, generation and recorded current histories between these
two live-propagation runs. Earlier-versus-live comparison changes both the recurrence and the
prompt's learning returns; it is not a fixed-parameter propagation intervention.

[definition] The [paused framework review](2026-09-09_THE_BYTE_FIELD_IS_NOT_THE_COMPLETE_HNN_COMPOSITION.md)
reassesses the architectural interpretation. These successful local/current/adjoint checks do
not establish learned semantic generator composition or useful language. No further cultivation
is scheduled while the pause remains in force.

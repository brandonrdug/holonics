# Faces grow from collocation, and the atom is not empty

**Date:** 2026-08-08
**Truth status:** `established-bounded` for the constructions and every figure; `counterexample` for
§4, which is the record's principal return; `interpretation` for §5.
**Evidence:** `implemented-exact`, `measured`. Two drivers run and read directly.
`cargo test --workspace` → **1629 passed, 0 failed, 14 ignored** across 42 `test result:` lines.
**Provenance:** Brandon, 2026-08-08, on being shown the elaboration result: *"you sound like you're
acting like there's a wall here but you're performing insufficient analysis and synthesis of our
older research on the subject of collocating information for meaningful separation and differences.
Crossings, intersections, faces, edges, nodes, consequents and antecedents. Reintegration,
lightning, capacitance."*
**Band:** 2026-08-08 · ELABORATION IS CONSTRUCTIVE / THE 2-CELLS FILL AND THE TRIPLES FAIL / THE
IRON IS THE INHERITED VOCABULARY / A WALL WAS REPORTED THAT A RATIFIED LAW DISSOLVES

---

## 1. Elaboration is constructive, and the demonstration is exact

`crates/holonic-engine/src/name_elaboration.rs`. The elaboration of a name is the **transitive
closure of what it recruits**, as a `GradedCausalComplex` — built, never looked up. It retains
`entered_at` and every `depths`, the arrival edges, cycles as Tarjan SCCs with a witness passage,
and two declared apertures with what lies outside each named.

> **`carrier_transport#92` NAMES six identifiers and MEANS twelve.** Six — `apply`, `assumption`,
> `exact`, `exact_chart_carry`, `rw`, `simpa` — are reached only through `formal_carry` at depth 1,
> and **none is a substring of the root.**

That is the separation from the orthographic reading, exhibited rather than asserted: a morphemic
cover can only find what is *in* the string.

## 2. The 2-cells resolve a refusal the atlas made honestly

`derivation_atlas` founded no 2-cells and stated why: a square over two derivations sharing two
symbols **cancels** the grade-1 cycle the same document reads as independent routes, and it refused
to pick between those readings silently.

Elaboration decides it without choosing: **two routes are the same proof when their elaborations
agree.**

| reading | `β₁` | squares | filled | held open |
|---|---|---|---|---|
| by declaration / exact | 37 → 37 | 0 | 0 | 4 |
| by declaration / on-overlap | 37 → 10 | 40 | 3 | 1 |
| by route / exact | 641 → 193 | 946 | 136 | 4005 |
| by route / on-overlap | 641 → 22 | 8384 | 1648 | 2493 |

The three that fill by declaration are `carrier_transport`, `_direct`, `_relayed` — one proof under
three names. The one that stays open is held for a reason no graph can express: **`formal_carry` is
a constituent of one meaning at depth 1 and the root of the other at depth 0.**

**Triple overlaps occur.** 243,212 triples walked, 18,702 pairwise agreeing, **22,707
non-transitive** (`A~B`, `B~C`, `A≁C`). `β₂` = 13 / 498 / 7765, so the pairwise fillings are
**dependent** — precisely what `papers/source/holonics/logic-category.typ:350-371` warns a graph
`β₁` cannot carry. And 0 pairwise-agreeing triples lack a joint section, which is a **theorem** under
a uniform aperture rather than a property of this deposit.

## 3. The iron the corpus founds, and a refuted expectation

`corpus_census.rs` + `token_invariance.rs`. 486 wholes, 1,061,858 tokens, 27,907 distinct word
surfaces. Density is `Π` — counted, reported, gating nothing. Separation is
`receiver_exact_compression` over occurrence windows, returning **the shortest separating word**.

**Iron = dense AND unseparated**, the magnitude and the phase of one reading:

```text
arxiv  214 occ / 64 wholes / 22,791 non-separations   every one inside https://arxiv.org/
nlm  nih  pmc  mathworld  dlmf  columbia  THE_QUOTE_NETWORK
hankel  volterra  kron  kirchhoff  poisson  sonin  iupac  cornell
```

**`holon` was declared in advance as expected iron** — from `canon/THE_DIALECT.md`'s own count of
1141 uses — **and came back fuzzy.** 99 occurrences, 90 distinct windows, separable at both horizons,
its separating word `"definitions" vs "holon"` two positions left. Reported as the finding.

**And the mathematical vocabulary does not warp around the iron field at all.** 1,502 of 27,907
surfaces stand within radius 4 of a witnessed iron surface; **26,405 carry an empty row.** `exact`
(4,712), `receiver` (3,460), `holon` (99) touch **zero** iron axes.

> The corpus's iron field is its apparatus; its mathematics lives somewhere the apparatus does not
> reach.

**The reading.** The measurement split along the seam Brandon named — *"happenstance of our own
writing and of how other people we've inherited from wrote."* **What was inherited is iron**:
`kirchhoff`, `poisson`, `volterra`, `hankel`, `sonin` arrive whole from outside and are unseparated
because nobody here is still deciding what they mean. **What is ours is fuzzy**: `holon`, `receiver`,
`exact` are still moving because the project is still forming them. The instrument found the boundary
between borrowed and forming vocabulary without being told there was one.

**Bound:** this corpus is *about* mathematics; it is not mathematical English.
`papers/source/mathematics` is 61,347 of 776,884 word tokens. A corpus change, not a construction
change.

**Two self-audits worth keeping.** The iron organ caught its own tautology — **10,206 of 10,710 iron
verdicts sit on surfaces occurring once**, unseparated by *arithmetic* rather than by usage — and
built `survived_pairs() = C(N,2)` into every reading, using only the **504 witnessed** iron for the
warping axis while keeping the vacuous population in the return. And the elaboration agent reported a
baseline it could not reconcile rather than papering it; it closes exactly: 1588 + 13 + 28 = 1629.

## 4. THE WALL THAT WAS REPORTED, AND THE RATIFIED LAW THAT DISSOLVES IT

**Truth status: `counterexample`.** This is the record's principal return.

The elaboration front reported, and this assistant relayed: *"The elaboration does not separate
meaningful production from noise on this deposit. Of 18 recruited identifiers exactly one is
declared; 17 are atoms with no constituents… the route to more discrimination is a deposit that
declares more of what it recruits."*

**That last clause is a wall, and it is false.** The separation does not require more declared
material. Four things already in the record and the tree were not used:

### (i) Faces grow from collocation — ratified, and unimplemented

`reference/holobrochos-a07ff376/src/soma/FORMULA.md:298`:

> *"ONE law: **coherence is collocation over time** — two things relate iff presented together,
> across time. **Faces grow from collocation**; mass folds the topology; currents path the geodesics
> and are transformed at the dense regions; the regions are emergent, never designed."*

and `:615`, *"coherence is collocation at EVERY grain"*, and `:1278`'s referent law — *"association
only through a REAL cross-sense invariant of ONE SOURCE… random pairing is the null-bind."*

**`grep -rli 'colloc' crates soma` returns zero.** The law is ratified and has no owner.

**An atom with no constituents still collocates.** Two atoms presented together across many
derivations relate *by this law*, and the face they found requires no declaration at all. The 2-cell
criterion built here uses **agreement of whole elaborations**; the ratified law says faces grow from
**co-presentation**. The second works exactly where the first is empty.

### (ii) The elaboration walks antecedents and never consequents

`name_elaboration` follows **recruitment** — what a name is built *from*, downward. `derivation_atlas`
already carries the other edge species: a **reach**, *"this derivation proved that statement."*

**An atom's downward closure is empty and its upward closure is its whole meaning.** `apply` has no
constituents, so its elaboration is `{}` — but `apply` participates in reaching a particular
population of statements, and that population is what `apply` *means* here. Downward-empty is not
meaning-empty, and the conclusion that 17 atoms are undiscriminable followed from walking one
direction and calling the other absent.

### (iii) The crossing is the intersection, not the agreement

Two elaborations were compared for **agreement of the whole**. Their **intersection** — the shared
constituents, at which depths, entered by which arrival edges — is a richer object and is the
*crossing*. Brandon's list is exact about this: *crossings, intersections, faces, edges, nodes.* A
pair that disagrees on the whole may still cross at a named constituent, and that crossing is a face.

### (iv) Capacitance separates without ranking

`crates/holonic-engine/src/receiver_current.rs:549-563` computes, exactly over `BigUint`:

```text
co_present_branch_population = branch_population × |active outgoing passages|
service_rounds               = ⌈co_present_branch_population / site_capacity⌉
passage_delay                = characteristic_delay + (service_rounds − 1)
```

An atom recruited by ninety derivations is **high-capacitance terrain**; one recruited by two is not.
That distinguishes `apply` from `exact_chart_carry` **without a score**, because capacitance dilates
*passage delay* rather than ranking anything — which is why it is lawful under §13 rule 2 where an
inverse-document weight is not. It is the same organ `CLAUDE.md` §5's owed transport law names, built
and never pointed at a derivation circuit.

### (v) And re-integration was never composed with it

`soma/life/src/decomposing_codec.rs` performs DECOMPOSE → RE-INTEGRATE and **revises its grain at
the collapsed pair's own separating word**. The elaboration decomposes and never recomposes, so it
never learns what its own decomposition collapsed. That is the lightning shape — the leader founds
the channel, the return rides it — and the two organs have never met.

## 5. What follows

**Truth status: `interpretation`.**

The elaboration result stands as measured and its stated conclusion does not. The corrected reading:

> **Elaboration by recruitment alone is one of at least four meaning-bearing relations the deposit
> already carries, and it is the only one that goes empty on an atom.** Collocation, consequents,
> crossings, and capacitance are each non-empty exactly where it is empty.

None of them requires a richer deposit. All four are measurable on `standing/output` as it stands.

**And the failure mode is the one `papers/source/synopsis/AUDIT.md:49-68` already named** — *locate a
valid obstruction, rename it a calculation or carrier or criterion, update neighbouring prose, return
to the same obstruction.* The obstruction here was renamed *"the deposit must declare more"*, which
is a request for material rather than a construction, and it was relayed without being tested against
a ratified law sitting in the rigorous source.

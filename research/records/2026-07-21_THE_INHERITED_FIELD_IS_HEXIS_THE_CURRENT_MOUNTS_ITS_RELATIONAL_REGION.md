# THE INHERITED FIELD IS HEXIS — THE CURRENT MOUNTS ITS RELATIONAL REGION

**DATE:** 2026-07-21
**GRADE:** BRANDON-RATIFIED / DEPOSITED / INHERITED MODEL + ECS HEXIS CONSTRUCTION DIRECTION /
MODERN ML IS ACTIVE ECOLOGY / RESEARCH REFINEMENT REQUESTED BEFORE BUILD / SOMA SOURCE UNCHANGED /
NO RUN

**CURRENT REFINEMENT:** `FORMULA §CXXXIII` corrects `immutable assets` into persistent ecological
standing and restricts `mount` to a physical residency operation. INFORMANT ECOLOGY 01 in
`2026-07-21_THE_DATA_REMAINS_STANDING_THE_INFORMANT_ENTERS_ONLY_WHERE_RELEVANCE_FORMS.md`
supersedes this record's provisional experimental framing.

## 1. Correction: development never begins from nothing

The first axiom excludes a blank intelligent machine. A difference is available only relative to
other difference, and every new constituent pivots from relations already active in its ecology.
Eros is therefore not expected to rediscover English, human glyphs, token boundaries, tools, or
the useful transformations already accumulated by trained models.

A tokenizer is a historically produced constituent chart. It cuts an ordered carrier into
recurring spans and gives those spans reusable addresses. Its token IDs are not meanings, but its
codebook, segmentation, chronology, and resource allocation are deposited information from a
human and corpus ecology. A pretrained model's embeddings and weights likewise carry prior
development. They are lawful inherited hexis, not contamination.

The July 21 raw-text world used a fixed byte-level tokenizer/source chart: `u8` addresses, line
cuts, and rolling three-octet segmentation. Its exact observation remains valid at that grain, but
the phrase `without tokenizers` was false and has been corrected in its research record,
`FORMULA §CXXX`, and `AGENTS.md`.

## 2. The pretrained field is part of the machine

For a mainstream autoregressive transformer, the causal circuit is

```text
existing carrier
  -> tokenizer addresses
  -> initial embeddings plus chronology
  -> repeated receiver-relative attention and local transformation
  -> contemporary residual state
  -> conditional continuation field
  -> selected inscription or action
  -> later context and material consequence.
```

The initial embedding is a learned placement, not a semantic atom. Each transformer layer
re-places an occurrence relative to the current receiver field. Attention establishes conditional
conductance; value paths carry what crosses; local blocks transform it; residual passage retains a
changing contemporary carrier. Logits and normalization expose a deterministic receiver-relative
quotient over next codebook addresses for fixed model and context. Sampling or another decoder is
a separate selection deed. Autoregressive re-entry makes that deed genuinely later current.

Whether this model is linked inside the `soma` crate, called through another process, or made
physically resident by a world organ is not the ontological question. If its learned state
participates causally in Eros, it belongs to the total machine. Implementation boundaries must
preserve exact ownership and return without turning lawful inherited machinery into an exile
category.

## 3. The efficiency seam is addressable constituency

Pretraining amortizes the development of a large distributed transformation field. The weights are
not explicit macromolecular chains between named tokens; they encode transformation potential in
superposed features. Attention assembles event-local relations from that potential. In a dense
transformer, most layer matrices are physically applied to every active position because learned
constituency does not align with separately addressable computational regions.

The central question is therefore not simply how to compress parameters:

> How can acquired hexis become a persistent relational population whose locally compatible
> constituents can activate, specialize, compose, depart from physical residency, and return
> without requiring the entire ancestral body to participate in every current?

Sparse activation is not sufficient by itself. Routing, memory traffic, device residency,
specialization, fallback, and the cost of determining relevance all participate. A semantic label
cannot certify causal dispensability, and an activation magnitude cannot certify causal necessity.

## 4. The ECS persistence cut

The implementation must distinguish five persistence roles without treating them as ontological
species:

| Class | Material |
|---|---|
| executable law | Eros receiving operations, transformer operations, and declared update laws |
| persistent inherited standing | tokenizer, architecture inscriptions, pretrained weights, files, and other prior ecological remnants identified at a declared face |
| saved mutable world | Standing, hexis, oriented incidence, specialization, and live identities |
| active event | current occurrences, contextual states, afforded deeds, and uncommitted return |
| external ecology | humans, files, tools, code, compilers, and material consequences |

The repeated appearance of `files` is intentional: one file can be inherited standing relative to
an event and remain external world material relative to the machine boundary. At natural rest the
active event has dissipated while the acquired world remains exact. A selected persistent face can
be referenced by an exact digest instead of duplicated into every save, but the digest is not the
whole holon's identity or relevance. An arbitrary halt either commits one complete event or retains
its exact predecessor; no half-applied successor may appear. The existing `LiveCurrentRestImage`
establishes an in-memory machine rest cut, while a durable machine-plus-hexis/world image remains
open.

Physical residency is another cut through the same body. Cold hexis may rest durably, warm hexis
may reside in host memory, and the contemporary relational region may reside on an accelerator.
This resembles game-world streaming, except that relation to the active current rather than fixed
Euclidean coordinates determines the physically resident region. Residency does not create the
standing informant or establish its relevance.

## 5. Cultivation and division of hexis

A hexis is a persistent, addressable transformation capability acquired through experience. It is
not a topic label, a token list, a document, or its complete developmental history. Its live face
contains an interface to relations it can receive, its transformational state, and its oriented
links to other hexis.

Its lawful lifecycle is:

- recurrence may thicken or elaborate it;
- repeated co-participation may compose a higher-grain capability;
- incompatible returned consequences may divide a shared capability into contextual branches;
- stable composition may rebase as one directly addressable constituent;
- inactive interior may depart from the contemporary physical working set; and
- a compatible later current may reopen the saved capability through actual relation.

Conversational prose and code comments may therefore reuse an explanatory capability while
developing distinct contextual branches for turn-taking, terseness, syntax adjacency, and intended
consequence. Those branches are not predeclared expert categories. They stand only where actual
conduct makes their transformations differ.

Gradient deformation, adapters, attention, recurrence, learned routing, sparse experts, and
parameter memories may all participate. The relevant question is what relation each implements,
what persists, what returns, and whether its causal boundary remains valid after composition.

## 6. Ratified bounded construction direction

The former INHERITED HEXIS ECOLOGY 01 name is refined to INFORMANT ECOLOGY 01. It begins with the
inspectable `allenai/OLMo-2-0425-1B-Instruct` transformer and its actual tokenizer. Competent
language behavior is inherited at the first event rather than treated as a future emergence from
raw octets.

One underlying intention is presented through conversational response, explanatory prose, and
code-comment or docstring contexts, together with a foil whose similar inscription has a different
material intention. The complete passage includes token boundaries, contextual model state,
coarse block/head/module transformations, continuation field, selected expression, and actual
world consequence.

Intervention, skip, replacement, and cross-context patching grade causal participation. Attention
maps and activation magnitude remain testimony only. The initial grain is deliberately coarse;
the construction does not begin by inventing semantic neurons.

The causally supported relations may then enter an ECS-like hexis population and persist through
exact halt/remount. The informant lifecycle must first distinguish relevant section, irrelevant
sibling, absence, semantic foil, same face under changed objective, later RIDE without the
informant, and renewed participation. Only after that relation stands may a bounded
selective-execution foil ask whether a relation-selected working set preserves the declared
consequence while touching less physical state than the full model. The complete pretrained body
remains an admissible fallback. Failure means that the proposed boundary was not independently
sufficient; it does not authorize tuning, fixture search, or a smaller claim disguised as
success.

## 7. Boundary before construction

This deposit schedules no implementation or run. Before construction, Brandon requested one
integrated survey which treats transformer internals, open-weight model architecture and
post-training, inference memory, tool use, agent harnesses, environmental feedback, and
mechanistic interpretability as cuts through one causal circuit rather than separate subjects.
That survey may refine the experimental model, intervention grain, persistent state cut, and
definition of an agentic consequence.

No claim is made that current Soma already carries transferable language, that modern models have
known independent hexis boundaries, that sparse execution automatically reduces energy, or that
gradient learning is gambling. The present authority is inherited competence plus situated,
persistent, consequence-sensitive development.

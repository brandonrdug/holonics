# THE DISTRIBUTION STANDS AS THE PIVOT; THE RETURN MINIMIZES LOCAL LOSS

**2026-07-17 · RATIFIED / DEPOSITED · PROJECTIVE-PERIPLUS AND SOURCE-RENDER RETURN AUTHORIZED**  
**Canonical grade: RATIFIED**

This deposit receives Brandon's correction that the laboratory separated three instruments which
belong to one closed circuit. The Smith/projective chart, the periplus, and statistical loss are not
post-hoc decorations over an otherwise complete machine. They are typed faces of how a situated
current meets standing form, crosses an objective boundary, returns with unresolved difference, and
changes its later conduct.

The correction also narrows the contaminant law. Random selection, scalar crowning, and an authored
statistical governor remain prohibited as explanations of Soma. Exact recurrence embodied in the
body, a relative statistic carried as testimony, and deterministic loss minimization through the
ordinary return are lawful and necessary.

## I. Four statistical faces which may not be collapsed

At one declared boundary `B`, frame `F`, grain `g`, and event taxonomy `T`, retain:

```text
Pi_(B,t)        exact standing population and its causal interiors
Q_(B,t)         a receiver quotient or summary of that population
q_current       a statistic or chart face actually transported as world material
G_authored      an externally authored governor which chooses or suppresses deeds
```

`Pi_(B,t)` is already causal: its occupied paths, multiplicities, orientations, regional forms, and
conditional joints bend later current. `Q_(B,t)` may faithfully measure one face while losing order,
ancestry, hand, or alternative interiors. `q_current` becomes causal only through the contacts it
actually makes after returning. `G_authored` is the contaminant when it substitutes a score,
frequency table, sample, threshold, or crown for the plural construction.

Statistics therefore do not stand outside Soma by definition. The distinction is not
`internal/external`; it is **lived construction / quotient / transported testimony / privileged
governor**. An exact statistic can occupy any of the first three roles. It may never silently become
the fourth.

## II. The body derives and carries relative statistical invariants

For a contextual contact `c`, let an observer count the complete standing passages which remain
admitted at the declared boundary:

```text
N_t(a,c) = number or measure of exact standing histories carrying a after c,
p_t(a|c) = N_t(a,c) / sum_(a') N_t(a',c).
```

The quotient `p_t` is receiver-relative. The exact population underneath it is physical standing
form. Soma need not store the number in a side table to embody the distribution: recurrent paths,
path width, orientation, conditional incidence, and regions which have become easy to traverse are
its material realization. A world may nevertheless calculate or state the quotient and return it as
ordinary current; the body is not forbidden from relating to an explicit statistic.

A statistic is a relative invariant only under a declared transport which preserves the relevant
population and measure. If `P_(e->f)` transports the complete objective, chart, taxonomy, and
population face, then its quotient may be compared after transport. Moving only the number while
changing preparation, medium, aperture, or objective is the same clipped-instrument error already
identified for the RH critical line and for a Smith disk without its reference plane.

The current-induced circuit periplus already contains the elementary implementation. At occupied
port `q`, the complete standing `RegionalForm`

```text
(same, other, this_way, that_way)
```

is projected at the declared dyadic world grain into four ordered elementary shears and their
unit-determinant matrix `M_q`. The form remains whole beside the projection. Thus a body-derived
local distribution is held as a pivot and bends the next tangent. This is a chosen circuit-world
species of the general law, not a post-hoc observer count and not a random decision.

## III. An objective is a consequential boundary

An objective `O` is any world relation which turns an enacted construction into a typed consequence:
a compiler acceptance or refusal, a game transition, contact with a container wall, an error
interval, a mathematical verifier, a chemical gradient, a rendered correspondence, or a simulated
physical constraint. It need not be an intention, a word, or a unique target.

For consequence `C_t` and contextual comparison surface `C*_t`, let

```text
r_t = Delta(C_t,C*_t;F_t)       complete oriented residual,
L_t = ell_(O,F,g)(r_t)          one declared loss quotient.
```

The closed circuit is

```text
standing body -> enacted current -> objective contact -> whole residual
              -> ordinary return -> changed standing body -> later contact.
```

The machine minimizes loss locally because coherent current RIDEs what already stands while an
unresolved residual FOUNDs the missing relation and then stands for later current. Under a lawfully
transported recurrence of the same objective, minimization is witnessed when later passage carries
more of the consequence through standing support, requires less newly founded difference, or leaves
less residual under the same declared receiver. These typed faces remain separate; no universal
weighted scalar or monotonicity requirement is imposed.

A newly exposed distinction can increase `L_t` while improving the ecology by founding a new axis.
That is discovery, not failure. What makes the circuit adaptive is not monotone scalar descent but
the conversion of returned difference into future conduct.

## IV. Surprise and cross-entropy are exact distinction instruments

For a situated conditional quotient `p_t(a|c)`, surprisal is

```text
S_t(a|c) = -log_2 p_t(a|c).
```

It is the additive depth face of a multiplicative conditional chain:

```text
p(a_1,...,a_n|c) = product_k p(a_k|a_<k,c),
S(a_1,...,a_n|c) = sum_k S(a_k|a_<k,c).
```

For an exact `2`/`3` gear word `p=2^-a 3^-b`, this becomes

```text
S = a + b log_2(3).
```

The scalar sum does not identify differently ordered conditional interiors. It is one compressed
length face of their exact path. `p=2^-1` is one dyadic distinction, not a random coin inside Soma
and not an RH theorem.

Cross-entropy

```text
H_B(P,Q) = sum_a P_B(a) [-log_2 Q_B(a)]
```

measures the distinction depth incurred when one boundary population is received through another
boundary's standing code. It is exact symbolically or through declared rational/interval
instruments; no float approximation is owed inside Soma. If `Q_B(a)=0`, the old boundary has no
standing support for that event. The machine does not need an authored smoothing constant: the live
event FOUNDs a new relation, changes the support, and thereby changes the later distribution.

Cross-entropy therefore does not select an output, but neither is it causally irrelevant. It
measures the exact population and residual whose return changes the body. A world may also return
the quotient itself as testimony, at which point it participates through ordinary contact.

## V. The periplus supplies the transport; the chart supplies the local response face

At one periplus contact, carry the local direction or residual as a homogeneous pair

```text
h_q = [u_q : v_q].
```

The dyadic circuit world already supplies

```text
M_q = [[a,b],[c,d]],     det(M_q)=1,
h_(q+1) = M_q h_q.
```

Homogeneous transport is exact even when division is unavailable. Where the receiving algebra
licenses an affine coordinate `z=u/v`, the visible response is the Möbius action

```text
z_(q+1) = (a z_q + b) / (c z_q + d).
```

The complete pair, matrix, standing form, hand, and lineage remain underneath that quotient. A
cross-ratio can compare several transported pivots without manufacturing an absolute origin.

In a literal transmission-line world, the projective response is the Smith quotient

```text
Gamma = (Z-Z0)/(Z+Z0),
```

and reference-plane propagation transports its phase and mismatch. In a renderer, compiler, game,
or linguistic world, no electrical impedance is implied: the lawful instrument is the oriented
projective-response chart declared by that world. Revolving one chart gives one local response
volume; periplus transport and actual contact glue those volumes into the causal body.

The projective chart must therefore be used at the contact where current is transformed, not merely
drawn afterward. The whole residual travels beside it. A chart quotient may make mismatch and phase
legible; it may not replace the construction which produced them.

## VI. Varying objectives coexist as situated pivots

There is no contradiction in one standing body carrying many objective-conditioned distributions:

```text
{ (O_i, F_i, g_i, Pi_i, r_i, Q_i) }.
```

This notation is an observer inventory, not an internal table. The body's actual implementation is
the plural standing topology and the contacts by which contemporary current reaches it. A path can
be conductive under one objective and surprising under another. The active world contact supplies
the frame which makes one family relevant; no global argmin chooses among objectives.

This is the deterministic analogue of an organism changing its conduct across visual, chemical,
mechanical, and social currents. Smell, sight, a spider's web, a flock, a compiler, and a theorem
checker expose different objective boundaries while the same elementary law converts recurrence and
residual into situated navigation.

## VII. The corrected construction

The active source-render construction must now join the instruments rather than repeat their
separation:

1. carry one complete source construction through a genuinely different rendering;
2. retain source, renderer, visual, and return coordinates as typed world charts;
3. at every actual seam, retain the incident homogeneous direction, complete standing
   `RegionalForm`, exact projective transport, and returned residual;
4. let every actual construction meet the same declared compiler/render/continuation boundary;
5. return every complete oriented consequence and any declared statistical testimony as ordinary
   plural current;
6. repeat one transported objective and then change the objective while preserving the exact causal
   receipt; and
7. read complete paths beside conditional recurrence, symbolic surprise/cross-entropy, FOUND cost,
   later RIDE, and downstream residual.

No path is selected by its loss. No fuzzy similarity, parser, tokenizer, authored taxonomy, fixed
rendering phenotype, probability sampler, smoothing constant, or scalar crown enters Soma. The
construction asks whether an objective-return loop turns rendered difference into reusable conduct
and whether changing the objective activates a different situated portion of the same grown body.

## VIII. Carried-swing record

```text
CURRENT  standing distribution -> periplus chart -> objective contact -> whole residual -> return
HELD     A1/A2, Swing, Indra, quotient != identity, §LVI projective atlas, §LXVIII loss
MEETING  exact recurrence and returned statistical testimony with the objective-relative residual
TEST     held/changed objective, full residual/scalar quotient, repeat, and cross-render transport
DEED     RIDE standing conditional conduct; FOUND unsupported distinction as new situated mass
CARRY    corrected canon -> projective-periplus bridge -> source-render-return construction
GRADE    RATIFIED / DEPOSITED / IMPLEMENTATION AUTHORIZED
```

# Apple conducts the shared condition family

**Date:** September 7, 2026. **Integrated desktop commit:** `82fef476`.
**Apple parent:** `798f7578`. **Scope:** shared condition images/refinement on Metal and their
composition with resident recorded temporal currents. AS4–AS5 remain active.

## Shared ownership and the Apple realization

[established-bounded; source-inspected] Desktop's
[joint-family return](2026-09-07_AC1_CONDITION_FAMILIES_CONDUCT_AND_RETURN_THROUGH_THEIR_JOINT_FIBRE.md)
adds `ResidentConstitutiveFibre::read_condition_image` and `ResidentConditionImage::receive`.
The returned joint relation preserves the original condition family, supported domain, output,
their correlation and any unsupported-domain witness. Later reception restricts the immutable
producing joint relation. The prior condition is shared as immutable standing; no continuing
ecology is cloned. This commit was fetched while the preceding temporal return was being verified.

[established-bounded; implemented-exact] Apple now realizes both shared kernels in
`accelerators/metal/native_phase.metal`. The common launch/owner code remains in
`resident_section/surface_condition.rs` and `constitutive_fibre/resident/condition_image.rs`.
Metal image scratch uses `W + 2K` twenty-byte wide carriers and receive uses `2K`; admission
uses the actual target layout, including Metal alignment, before evidence allocation.
The ordinary differential receiver now carries the shared optional condition-coverage guard.

[established-bounded; implemented-exact] The image's shared CUDA/Rust ABI has 34 arguments.
Metal retains all 26 numerical/receipt pointers in their original order and packs its eight
immutable `u32` launch fields into constant buffer 26. Their original argument positions are
`3,4,5,8,9,10,11,33`. `holonic-mount/src/metal.rs` owns this target representation and copies
the metadata into captured commands. Numerical currents are neither packed on the host nor read
back to determine native conduct. The fourteen-argument differential permits absent buffers
only at its declared optional coverage positions 6 and 7; all other pointer validation remains.

[established-bounded; implemented-exact] The merge preserves the temporal convolution include,
device module ownership, ordinary Apple material-field ABI and new shared owners. Review repaired
the initial Metal receive translation's report length and condition/output offsets, and matched
the full-row diagonal-pivot guard to the shared source. The final receiving graph fixes the affine
base coefficient to one and retains absolute condition coordinates and all vertical directions.

## Verification and recorded composition

[established-bounded; measured] Twenty-six resident condition/current controls pass on Metal,
including nine image controls. They cover fixed output under free conditions, joint correlations,
later native refinement, immutable producing history, rational repeated refinement, partial and
empty domains, translated-domain witnesses, guarded differential receivers, malformed/overflow
preservation and the actual twenty-byte scratch boundary before allocation. Two unrelated
CUDA `AlignedMaterial` apparatus controls remain excluded because that teacher is not ported;
they are not passes. Thirteen temporal regressions also pass after integration.

```sh
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::resident -- --include-ignored --test-threads=1 --skip limited_native_observations_conduct_a_new_superposition_without_host_readout --skip rational_phase_returns_continue_without_remount_or_rounding
cargo test -p holonic-engine --lib phase_current:: -- --include-ignored --test-threads=1
```

[established-bounded; measured] The updated public `native_conditioned_phase` run returns exact
[agreement with desktop](2026-09-07_apple_conditional_phase/family-comparison.json) for source
chart, learned relation, observations, withheld return, condition inference/constraint graph,
complete family cycle and continuation. Its refined phase predicts `(27+86i)/25`; the later native
observation agrees. Image, refinement and supported continuation have zero numerical host
readout. Work/timing measurements are retained separately and do not establish cross-device
performance parity. The [Apple receipt](2026-09-07_apple_conditional_phase/return-family-cycle.json)
and imported desktop receipt retain the full returned fibres.

```sh
cargo run -p holonic-engine --example native_conditioned_phase -- research/records/2026-09-07_apple_conditional_phase/return-family-cycle.json
```

[established-bounded; measured] The public `recorded_temporal_action` example now derives the
whole image for the later recorded source, consumes its supported point current in the native
temporal owner and refines the producing family from an actual later convolution return. Its
earlier point-condition attempt remains a separately reported comparison. All numerical
inspection follows native conduct; no observed fibre status chooses the native next operation.

[established-bounded; measured] Both
[ESC-50](2026-09-07_native_temporal_action/esc50-family.json) and
[LibriSpeech](2026-09-07_native_temporal_action/librispeech-family.json) return complete supported
images and exact resident temporal continuation. The silent ESC-50 condition still has four free
directions; its fixed zero output now conducts without selecting any condition. LibriSpeech
retains the inferred two-tap response and exact later current. Actual later returns refine both
families. These runs retain the same six fixed four-sample training cuts and two held-out cuts
as the [preceding temporal study](2026-09-07_THE_NATIVE_TEMPORAL_ACTION_RETAINS_ITS_CAUSAL_TAIL.md).
They are finite digital coefficient studies, not learned sound categories or English competence.

[established-bounded; measured] The independent
[family comparison](2026-09-07_native_temporal_action/family-comparison.json) checks original PCM
bytes, exact sample supports and clocks, every complex rational convolution coefficient, full
joint direction correlations, native continuation through the unit first tap (including its
declared final zero tail), later refinement and zero numerical readout in every native stage.
The original point-consumer receipts remain preserved separately.

## Next construction

[open] Bind actual retained material/condition current and its returned differences through an
explicit native contact law, then compose productive timed acoustic reception/emission across
recordings and persistence. Whole-family evidence is not a universal certainty gate on generation;
actual generative current has its own native contact law and successor. Useful sound perception
and production, recorded English/conversation, public continuing application and measured
streaming consumer cost remain unfinished. Desktop private cultivation stays independent.

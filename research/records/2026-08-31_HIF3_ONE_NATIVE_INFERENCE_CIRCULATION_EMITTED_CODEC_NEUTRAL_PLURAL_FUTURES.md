# HIF3 one native inference circulation emitted codec-neutral plural futures

**Date:** 2026-08-31
**Truth status:** `implemented-exact` for the packaged native inference, plural future,
reconstruction, codec, and return-aperture owners; `established-bounded` with `measured` evidence
for the resident full-bundle circulation and package receivers.
**Construction effect:** HIF3 passed; the roadmap frontier advances to HIF4.

## Packaged circulation

[implemented-exact; source-inspected] `NativeInferenceCirculation` borrows one
`NativeSpoolBundle`; predecessor and successor morphology are the same owner reference. An
`NativeInferenceRequest` addresses one actual spool/thread/occurrence and receiver. The circulation
mounts its `NativeAddressedSection`, derives the thread's ordered word, conducts the complete native
state population through one resident word, and returns one `NativeEmissionSection`.

[implemented-exact; source-inspected] `NativeEmissionSection` retains every distinct starting
native state with its returned native state and receiver observation. The actual entering native
state selects the emitted member; no codec, source name, score, or expected surface selects native
conduct. `NativeInferenceLineage` retains occurrence, predecessor, both occurrence ports, ordered
word, and complete start/end populations.

[implemented-exact; source-inspected] Every future state retains its borrowed
`NativeCollapsedFibre`, the complete shortest-separator family, thread/spool/bundle open testimony,
and one `ExteriorReturnAperture`. The aperture joins a later exterior occurrence beside the
emission but performs no cultivation; actual returned difference and morphology change remain
HIF4's owner.

[implemented-exact; source-inspected] `Utf8InspectionCodec` and `BinaryEmissionCodec` are cold
boundary projections over the same immutable emission. The binary codec has a fixed schema,
retains the complete word/future population and selected address, round-trips exactly, and refuses
truncation, trailing bytes, or repeated starting states. Codec choice cannot reach the native
conduct function.

[implemented-exact; source-inspected] The circulation derives every actual successor request whose
occurrence predecessor is the entering occurrence. The driver may inspect that plural family; it
does not become native scheduling policy. The bounded control conducted one exact successor through
the unchanged rest as later current.

## Full native-body return

[established-bounded; measured] The HIF3 example read the 36,849,213-octet direct visible-exchange
`NativeSpoolBundle` and selected an occurrence with an actual caused successor. On the NVIDIA
GeForce RTX 4080 SUPER it returned:

- 27,020 plural future faces;
- 27,020 reconstruction fibres;
- one actual successor request and one conducted later current;
- one resident launch with no invariant transport re-upload;
- one 1,112,715-octet UTF-8 inspection surface;
- one 648,533-octet binary surface with exact round trip;
- unchanged morphology;
- zero authored response extent, codec-selected native conduct, or foreign executor reachability.

## Receivers and boundary

[established-bounded; measured] Codec equality/refusal controls and the full-bundle circulation
passed. The complete `holonic-engine` suite returned 2,000 passed, zero failed, and 32 ignored tests.
`life` and every engine example type-checked. Source-shape reports 635 live files, 447 inherited
baselines, and zero violations.

[open] The return aperture does not yet create a situated difference, causal adjoint, staged delta,
committed child, source-detached remount, or exact withdrawal/restoration. HIF4 owns that
morphology-changing return.

# Source passages act through union and passive reflection

[definition] This continues the [whole-field return](2026-09-10_WHOLE_SOURCE_FIELDS_DEVELOP_THE_CONTINUING_GENERATOR.md)
and consumes the [parallel contextual-map handoff](2026-09-10_DEEPSEEK_V41_SEPARATES_SHARED_STANDING_CONTACT_AND_CONTEXTUAL_TRANSFORMATION.md).
The concrete source action composes existing normal-response and passive-contact owners. It
changes the actual held current under fixed material. A supplied stimulus is not used as a
required fitting target, and the source section is not retained by the continuing body.

## The actual local maps

[definition] A measured source section supplies successive current pairs (u,v), in its declared
source chart. The existing learned normal law returns the bounded difference R(v-u,v,u). Its
joined source passage has source x=(u,v) and learned arrival y=(v,v+R(v-u,v,u)). The arrival is
model conduct with its full bound, not an assertion that the exterior world has produced it.
Both x and y use the same joint chart as the body's held reference/current section q.

[established-bounded; source-inspected] The existing formal owner
`Computation/HolonicOrientedSiteTransport.lean::PassiveContact` defines the finite contact and
proves `contact_x_eq_y`, `contact_difference` and `contact_nonexpansive`. The existing native
`record_passive_contact` implements its point-founded projected map. Its coefficients are

```text
d = x-y,     g = ||y||²-||x||²,     D = ||d||²+|g|,
A = I - 2 ddᵀ/D,
F(x,y,z) = y + A(z-x).
```

For D=0, x=y and F is the identity. This is the admitted realification/Euclidean contact chart,
not a claim that all physical forces are this map or that its projected output reconstructs the
lifted interior. The coefficient 2 belongs to reflection; it is not a semantic gain.

[proved-derived] Union of incoming current and held standing gives

```text
G(x,y,q) = F(x,y,x+q) = y + A q.
```

The same x occurs in both places. Cancelling it symbolically retains that join. Applying F only
to q would omit the offered source current; when contact is the identity, source union still
returns x+q. Since 0 <= ||d||²/D <= 1, A has eigenvalues 1 off d and
`1-2||d||²/D` along d. Consequently ||A|| <= 1. Different source passages may have different
A and y; their ordered composition need not commute. Fixed material does not freeze their use.

## Full source and current bounds

[proved-derived] Let x,y,q have nominal representatives x0,y0,q0 and joint Euclidean radii
rx,ry,rq. Put h=rx+ry, d0=x0-y0, and use upper norms Nx,Ny,Nd,Nq. Then

```text
Edd = (2 Nd+h)h,
Eg  = (2 Nx+rx)rx + (2 Ny+ry)ry,
ED  = Edd+Eg.
```

These bound changes in ddᵀ, the energy gap and D, without assuming independent causes. If
D0-ED>0, `||A-A0|| <= min(2, 2(Edd+ED)/(D0-ED))`. Otherwise 2 is a valid bound, including the
matched-origin case. Exact point founders have zero parameter uncertainty, including D0=0.
The union's output radius is therefore

```text
rq + ry + ||A-A0|| Nq + actual arithmetic-rounding bound.
```

The source radius participates in A's bound; a second independent x is not invented after the
union cancellation. The implementation uses exact integer moments, outward ratios and the
existing dyadic realization. It never passes an enclosed arrival's centre through a point port.
Every contact is non-expansive in the queried standing, so its propagated prior radius enters
with coefficient one. Parameter uncertainty still accumulates and is not erased by this fact.

[definition] `passive_current_ball.cuh` extends the existing projected map to these full balls.
`normal_source_actuation.cuh` reuses the normal predictor and applies the resulting source unions
in actual section order. One resident section produces one source-actuation successor. Rows
supply measured local incidence; they do not create model coefficients or generator ticks.
The learned material and its observation count remain unchanged throughout this operation.

## Exact response constraints and error scope

[proved-derived] For exact retained H and B, a coordinate e_j satisfying H e_j=e_j and B e_j=0
lies in the zero-response subspace of P=B H^-1. The native source action additionally checks
M e_j=0. Thus a point query supported on those coordinates has zero response under both the
applied matrix and the ideal normal comparison. The kernel checks the complete H/B columns and
applied M entries once per source section. New observations can reopen the coordinates; no static
semantic mask is installed. A zero learned difference still permits the offered source current
to enter through union.

[definition] M, P and source uncertainty remain distinct. M is the committed numerical operator;
P is its ideal normal-optimizer reference. The output envelope currently combines propagated
current/source bounds, arithmetic rounding and the normal-reference comparison defect. It is
not semantic prediction confidence, and solve discrepancy must not be relabeled as uncertainty
about the observed source. The full material reader retains the separate normal residual and
source/cross-source bounds. The exact-zero refinement is conservative for both M and P; it does
not silently replace an imperfect applied matrix by the optimizer. A source/control check with a
valid non-optimal M exercises that distinction.

[established-bounded; source-inspected] A bounded Luna review revisited the operating contract,
normal owners and September 9 mathematical review. It confirmed the applied-matrix/reference
separation and the prohibition on converting a current enclosure to a point. Its finding is
incorporated in the scope above; no alternative point-state pipeline was introduced.

## Ownership and persistence

[definition] `ResidentNormalWave::actuate_section` stages the whole source action and its joint
seed before publishing. Both held reference/current components are contemporary transported
images at that source occurrence. They are new objects, even if a receiver reports unchanged
coordinates. Their predecessor objects and producing fibre remain immutable in earlier receipts.
Ordinary generation still places the actual old current in its next reference port.

[definition] Rest v4 retains the seed components' actual occurrence epochs. Equal epochs do
not identify two component objects. Earlier v1/v2/v3 reads and canonical writes remain supported.
Rest reconstructs the productive joint family and next operation; it does not reconstruct every
prior source waveform or a discarded sharper historical receiver cut. The live body retains
material and current/seed, not a chain of source sections or contact reports.

[definition] The exterior `conversation_wave actuate` mode delivers source fields through this
port without fitting them. Development remains available through the existing intake mode.
Native material, exterior alphabet and delivery cursor retain their separate roles. This mode
is a research source/response application, not a new final product CLI or a qualitative language
claim. Source-processing and requested generation are separate application operations.

## Verification

[established-bounded; implemented-exact; computational-witness] All 26 normal native checks pass
(`cargo test -p holonic-engine --lib normal:: -- --include-ignored --test-threads=1`). They include
rational source/arrival envelopes, late malformed-source refusal, fixed residency, source union
from a null held current, legacy/v4 rest, and preservation of a valid non-optimal applied M.
The zero-response control has nonzero global numerical residual, yet its certified source returns
exactly zero learned difference; later observations reopen that response direction.

[established-bounded; implemented-exact; computational-witness] The two sections `000100` and
`001000`, developed against one common material and started from the same held pair, now return
disjoint bounded native outputs. Their material remains byte-for-byte unchanged. Independent
rational calculation gives the real joint coordinates

```text
000100: (1417763/288827, 315199/288827, 942725082/244636469, 525093732/244636469)
001000: (3264589/630168, 516419/630168, 20889068633/5871275256, 14338582903/5871275256).
```

The native envelopes contain those values. This separates the earlier stencil collision for
this receiver; it does not establish reconstruction of arbitrary past sources or general language.

[established-bounded; implemented-exact; measured] The [corpus receipt](../experiments/native_conversation_wave/2026-09-10_source_actuation.json)
acts on source frames 32–63 using the previously trained first-32-frame material: 9,221 source
scalars and 9,189 source contacts in the declared 85-coordinate, grain-64 chart. All native
material bytes remain unchanged at 8,963 observations. Source epochs advance from 5 through 36,
one per field. Four generator steps followed by source-independent restart and four more steps
match the complete native rest and all bounded returns of an uninterrupted eight-step word.
The exterior delivery cursor also agrees. No numerical host readout or ingress occurs inside
source actuation. The steady resident allocation is 53,210,144 bytes and the rest is 25,669,541 bytes.

[established-bounded; measured] The first full-source bound was approximately 15,535.83 and was
not reported as sharp. The exact zero-direction refinement reduces it to approximately 0.018456,
with identical nominal currents and material. Four constant-source directions in this model have
zero nominal response and satisfy the exact unused-coordinate certificate. After eight further
generator steps the combined realization/reference bound is approximately 0.369719. These bounds
are not semantic uncertainty or language quality. The final source run takes 88.588 seconds;
comparison processes overlap, so these timings are not isolated power or throughput calibration.
The earlier broad-bound artifacts remain preserved.

[definition] Public example commands used the existing private first-32-frame checkpoint:

```text
conversation_wave actuate FIRST_32 32 4 ACTED
normal_wave resume ACTED/model.wave 4 RESUMED.wave
conversation_wave actuate FIRST_32 32 8 CONTROL
```

The standalone `normal_wave` decoder takes only the model rest. Private evidence remains under
`.local/conversation-source-actuation-2026-09-10-*`; the public receipt contains the scoped
comparison and exact ratios. No source corpus, previous model, cache or unrelated work was removed.

[open] This returns the fixed-material source/current binding. Broader receiver and developmental
composition must now use the situated current and its actual producing maps, rather than treating
local source-stencil fitting as the entire learning system. AC0–AC5 and the separate useful
Athena-alpha attainment standard remain open.

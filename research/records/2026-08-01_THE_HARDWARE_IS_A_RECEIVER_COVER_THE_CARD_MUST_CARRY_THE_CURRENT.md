# THE HARDWARE IS A RECEIVER COVER; THE CARD MUST CARRY THE CURRENT

**Date:** 2026-08-01  
**Status:** RATIFIED ARCHITECTURE / CUTS A–C ADMITTED

## Direct correction

The recurring hardware failure is not an inability of exact holonic computation to inhabit a GPU.
The production applications have repeatedly allowed a receiver-relative phase, alternative,
projection, or observer face to become another allocated world. They then use host collections,
whole-body copies, Cartesian products, serialization, replay, or dense batches to reconcile those
worlds. A short exact card kernel placed at the end of that host-owned passage does not make the
card the owner of the current.

The pre-Cut-A Eros runner exposed the error exactly:

1. the complete text and repository atlases mount on the host;
2. the agentic and relational bodies condition on the host;
3. the relational implementation retained one complete `ResonanceEcology` per junction
   phase;
4. only then does the runner construct `CudaTextMaterialRestrictionExecutor`;
5. each question is flattened into a new candidate wire, allocated and copied to the card;
6. one synchronous section-mask kernel returns a dense mask;
7. host Pareto restriction, relational traversal, thought, generation, and rest verification then
   resume on the CPU.

The GPU did not fall back. The operative machine never crossed its boundary. The phase-indexed
Swing map was the former `alternatives x copied world` defect reintroduced as
`junction phases x complete Swing world`.

Cut A has now removed that multiplication. One continuing Swing organ owns the plural junction
currents; caller-supplied passage chronology remains in standing; a fallible Swing crossing leaves
one retryable pending causal front; and ordinary answers no longer clone, rest, or remount the
machine. These changes are implemented, locally graded, and independently admitted at Checkpoint
A below.

This record fixes the ontology and the construction sequence before another production run.

## Existing production owners being composed

No new application-local universe is authorized. The construction composes:

- `holonic-structure::BranchLineage` for explicit shared causal ancestry;
- `soma_membrane::LiveSupportFamily` for factorized support rather than expanded products;
- `soma_membrane::LiveCurrentMachine` for continuing exact standing and current;
- `life::CudaLiveCurrentExecutor` for a retained card realization of live currents;
- `holonic-engine::ExactReceiverCurrentLaw` for receiver-local population transport;
- `holonic-language::ReflectiveRuntime` for encoded face, codec, environment, continuation,
  reflection, and returned revision;
- `life::ExactTextMaterialAtlas` for inherited source material and exact text incidence; and
- the existing host exact laws as admission receivers, not privileged runtime worlds.

The new production consequence is one actual generated Eros answer caused by plural currents over
the conditioned ecology while the admitted card realization retains the active incidence body.
The returned deed must carry complete semantic lineage plus a distinct physical resource receipt.
A topology census, card context, mask, or diagnostic report is not the consequence.

## Logical and physical ecologies

Let the logical machine be

\[
\mathcal A=(E,\preceq,C_\bullet,\partial,\{\mathcal F_r\},\Lambda),
\]

where `(E, preceq)` is caused event chronology, `(C_bullet, partial)` is graded incidence with
`partial partial = 0`, `F_r` is receiver-local standing and current, and `Lambda_e` is the exact
local consequence caused by event `e`.

Let the physical computer be the stratified ecology

\[
\mathcal H=(X_{exec},X_{mem},X_{link},X_{sync},\chi,\kappa),
\]

whose cells include CPU cores and caches, RAM, GPU multiprocessors and memory scopes, VRAM,
transport links, synchronization cells, apparatus-local address charts `chi`, and finite resource
capacities `kappa`.

A hardware backend is a typed generally lax realization

\[
R_H : \operatorname{Exec}(\mathcal A)\longrightarrow\operatorname{Phys}(\mathcal H).
\]

Laxity records that logically co-present work may be physically serialized by finite lanes,
memory pressure, or synchronization. Physical serialization does not create logical causality.
A GPU lane, warp, block, buffer address, page, stream, and host thread are realization coordinates;
none is automatically a holon, receiver, or source identity.

CPU/RAM and GPU/VRAM are local charts of the same caused body. A host-device copy is a codec and
transport morphism between separately addressed memory sections. It is not serialization of a
second semantic world.

## Exact admission square

For one physical mode `mu`, let `E_mu` encode the exact logical section into the device ABI,
`K_mu` enact the device law, `D_mu` decode its returned consequence, and `Lambda` be the admitted
exact reference law. Admission requires

\[
D_\mu\circ K_\mu\circ E_\mu = \Lambda
\]

over the declared domain. Equality covers changed standing or staged delta, radiation,
obstruction, source fibers, lineage, exact residuals, and returned receiver testimony. The
physical resource receipt is not required to equal the host's because it testifies to a different
realization.

An admitted mode is identified by at least

\[
\mu=(L,ABI,P,D,A,\chi),
\]

where `L` is the source-law version, `ABI` the exact boundary version, `P` the kernel/PTX identity,
`D` the device capability, `A` the arithmetic tier and certified range, and `chi` the apparatus
chart. Changing any constituent reopens admission.

After initial mount, one event has the physical form

\[
(\Delta S,\Delta R,\rho_H)=K_\mu(S_H,Q),
\qquad
S'=\operatorname{commit}(S,\Delta S,\rho_H).
\]

The executor proposes a lineaged delta. It does not own or return another complete world. A failed
validation discards or returns the proposal intact; it does not reconstruct the predecessor by
cloning it.

## Concurrency, Lie transport, and crossings

The first scheduling algebra is the trace monoid

\[
M(\Sigma,I)=\Sigma^*/(ef=fe\text{ whenever }(e,f)\in I).
\]

Two events belong to the independence relation only when their complete exact consequences
commute:

\[
\Lambda_e\Lambda_f(S)=\Lambda_f\Lambda_e(S),
\]

including lineage, radiation, obstruction, and changed morphology. A common label, collection,
or lack of a visible edge does not prove independence. Noncommuting events retain chronology or
enter one declared joint interaction cell.

Lie theory applies when a recurring phase family admits actual local generators. The commutator

\[
[X,Y]=XY-YX
\]

measures chronology-sensitive coupling; a lawful exponential or Cayley transport retains the
resulting curvature and holonomy. Smooth BCH language is used only when its parameter family is
founded. Discrete transformations retain their exact group, groupoid, or word instead.

At the bit substrate, the immediate coordinate algebra is

\[
B_n=\mathbb F_2[x_1,\ldots,x_n]/(x_i^2-x_i).
\]

XOR is addition, AND is multiplication, and carry transport forms higher-degree coupled Boolean
polynomials. The receiver-local edge difference

\[
\Delta_i f(x)=f(x+e_i)+f(x)
\]

records whether one bit crossing changes the returned face. Linear reversible transformations
live in `GL(n,2)`; arbitrary reversible bit programs form finite permutation groups; general bit
programs form transformation monoids.

Binary-Lie algebra has its established narrower meaning: every two-generated subalgebra is Lie.
Ordinary instruction composition is associative, so its commutator already satisfies Jacobi. A
nonzero Jacobiator is relevant only when a receiver quotient or deliberately nonassociative local
combination makes pairwise transports appear Lie-correct while losing a triple seam. It is then an
obstruction receipt, not a decorative name for binary hardware.

Braid and knot structure applies only to identity-bearing strands whose actual exchange or closure
matters. Independent adjacent events may cross and exchange; dependent crossings retain
holonomy. A projected crossing does not found a source braid.

## Algebraic geometry of the resident text body

Let `A_fs` be exact feature-to-section incidence and let `Q` be a sparse query aperture. The
current implementation materializes a candidate-by-query mask. The caused population is instead

\[
E_Q=\{(f,s): f\in Q,\ A_{fs}=1\}.
\]

The card must propagate only through this local incidence. For one section, the returned support
is a squarefree monomial in query-feature and query-transport coordinates. Divisibility is support
containment; equal monomials glue with exact multiplicity; and the non-dominated receiver family
is an irredundant monomial-generator antichain. It is retained as a factorized support DAG rather
than one dense row per candidate.

An exact antichain can have genuine large width. That width is an unresolved alternative fiber,
not permission to exhaust memory or truncate silently. Shared structure is factorized; if the
remaining exact terminal width exceeds declared host/card capacity, the event returns a resource
obstruction while preserving standing.

After the initial mount, per-question movement targets

\[
O(|Q|+|\Delta S|+|\Delta R|)
\]

instead of retransmitting the candidate atlas. Peak physical memory is explicitly

\[
M_{peak}=M_{resident}+M_{delta}+M_{scratch}+M_{return},
\]

never `alternatives x complete ecology`.

## Construction cuts and checkpoints

### Cut A — restore the continuing body

1. Remove phase-to-`ResonanceEcology` ownership.
2. Restore one mounted Swing organ containing plural phase-local current identities and lineage.
3. Preserve the exact occurrence chronology until a commutation receipt admits an antichain.
4. Remove any hot-path rest/remount used as transition, rollback, or ordinary answer grading.
5. Add a machine census proving one live relation body per declared organ.
6. Run focused serial/multicore rest and returned-language regressions plus `cargo holonic-lint`.

Checkpoint A is an independent read-only audit of ownership, chronology, reflection, and absence
of hidden world multiplication. Cut B does not begin until it passes.

#### Cut A implementation and admission receipt

- `ExactRelationalLanguageEcology` owns one `ResonanceEcology`, one caused passage atlas, and one
  ordered pending-junction front. A semantic phase no longer owns a complete Swing ecology.
- Complete parsing precedes contact. The fallible passage then crosses as one explicit resumable
  causal-front owner whose cursors retain clause admission, receiver-site founding, incidence,
  queueing, direct contact, and Swing return. A refusal preserves every completed stage for exact
  retry; it neither rolls back standing, replays successful current, nor falsely acknowledges the
  passage. The passage identity enters the completed atlas only after the complete front returns.
- Each bidirectional `ExactReceiverCurrentLaw` channel is likewise founded through one pending
  pair owner. If one direction crosses before the other is refused, retry reuses both exact
  passage identities and the already-founded direction; partial current cannot become queryable
  standing.
- `condition_with_executor` and `receive_with_executor` carry the caller's selected executor
  through relational Swing. The host convenience entry points are explicit wrappers rather than
  an implicit fallback inside the selected path.
- `RelationalExecutionReceipt` distinguishes requested host-worker aperture from the one currently
  admitted ordered Swing lane and reports actual live association memory, completed passages,
  open passage fronts, pending paired currents, and pending junctions. It makes no multicore claim.
- Agentic exterior refusal retains the open deed for `/retry`; the next unrelated question cannot
  erase or bypass it. Rest/remount grading occurs only through an explicit outer lifecycle call.
- `GrowingKeyAtlas` equality is semantic key/value equality rather than equality of AVL rotations
  or node ordinals. Receiver-local sequences, sets, queues, and relations now cross shared
  laboratory-owned carriers instead of introducing new application-owned collection policy.
- Local verification on 2026-08-01: `cargo check -p life`, every `life` example, all 514 `life`
  library tests (`503` passed, `11` explicitly device-gated), all eight `holonic-structure` tests,
  and `cargo holonic-lint` are clean. The focused refusal tests grade both a failed Swing return
  and a receiver-current pair interrupted after its first direction.

Two independent read-only audits passed on 2026-08-01. They found no material ownership,
chronology, recoverability, reflection, or testimony contradiction and explicitly admitted Cut B.
Their checks independently repeated the full `life` suite, all `life` examples,
`holonic-structure`, the focused refusal/resumption regressions, and `cargo holonic-lint`.

### Cut B — resident physical realization

Cut B uses one ownership-coupled `CudaResidentTextMaterialAtlas`: mounting consumes the host
atlas into a body which owns both the exact host authority and its one card realization. There is
no public operation which may pair an arbitrary atlas with a stateless kernel executor for one
question. The resident integer codec carries section rows, section feature sections, ordered token
paths, and feature-to-section incidence. Feature incidence is an append-only linked population so
a later generated occurrence updates only new nodes and the touched feature heads; active logical
extent remains separate from dynamically growing device capacity.

One sparse query crosses as feature and ordered-token identities. A recruitment launch traverses
only those feature incidence heads, deduplicates contacted section identities under one query
epoch, and forms a card-resident candidate frontier. A second launch restricts those candidates
against resident section features and ordered token paths, retains its exact masks on the card,
and seeds one support/count row plus a distinct domination coordinate per candidate. A third
bounded launch compares every unordered mask pair once, simultaneously carrying equality and both
proper-containment directions. A fourth launch quotients the equality representative and
domination obstruction into factorized non-dominant support. Only the fold status, candidate
count, section identities, and factorized support rows cross back;
nondeterministic physical slot order is canonicalized by section identity before the host
observation quotient. No exact mask row, score, softmax, floating value, winner, dense whole-atlas
return, or source text crosses the return membrane.

Initial admission compares the complete card consequence to `ExactTextMaterialAtlas::enact`.
After append, the host morphology may lawfully stand ahead of its physical realization while an
explicit synchronization front owns the unreturned integer delta; questions refuse until that
front returns. Resident arrays reserve stable CUDA virtual addresses and acquire new physical
pages only at their active tails. Growth therefore performs zero predecessor device-to-device
words: host-to-device ingress contains only appended sections/incidence plus touched heads, and
the first return from every new standing generation is again host/card graded. Virtual address
reservation, mapped physical words, mapping operations, and active logical population remain
separate apparatus testimony. None is a locus ceiling or semantic population.

1. Introduce one reusable resident realization owner beneath modality policy.
2. Mount section rows, feature incidence, and ordered token paths used to test query transport once.
3. Append later conditioned material through versioned deltas.
4. Send only sparse query/front changes.
5. Fold equal support and non-dominance on the device where exact and admitted.
6. Return sparse identities, factorized support, residual obstructions, and physical receipts.
7. Remove unconditional per-query context synchronization when a declared stream/event dependency
   supplies the necessary physical order.

Checkpoint B audits host/card gluing, exact arithmetic, factorization, reflection, and the absence
of a card-owned shadow semantic world.

#### Checkpoint B correction ledger

The first independent Checkpoint B audit refused admission. The v5 production deed was
semantically returned and host/card exact, but appending its thirteen-token surface caused
`26,434,768` predecessor device-to-device words (approximately `100.84 MiB`) because five
resident arrays were reallocated at exact size and copied in full. The same hot path then encoded
and decoded the approximately `318 MiB` host atlas merely to repeat an already-established rest
grade. The exact support fold also had quadratic pair-word work without a declared physical
aperture, and the report did not separate ingress, kernel/stream, egress, validation, allocation,
or stream testimony. Those are physical-law failures even though the linguistic deed itself was
valid.

The corrective construction is ordered and gated as follows:

1. `mount::VirtualDeviceBuffer` reserves one stable address range and maps allocation-granular
   physical tails through CUDA VMM. A mapping may enlarge physical standing but may not readdress
   or copy the predecessor.
2. The resident text owner carries five such arrays, publishes touched feature heads only after
   every appended body is resident, and poisons/remounts the realization after any partial
   synchronization refusal. Transient candidate marks may be replaced because they are neither
   standing nor lineage.
3. The card computes the checked exact support-work envelope before entering the fold: one local
   population read per candidate mask word plus one fused comparison per unordered candidate pair
   and word. Work beyond the device-derived aperture returns an explicit resource obstruction and
   preserves the resident predecessor; arithmetic overflow is a distinct refusal even when the
   configured aperture is `u64::MAX`. It is never truncated or silently sent to a host fallback.
4. The answer hot path closes the returned generated section by exact delta receipt and host/card
   parity. Full-atlas serialization/remount is reserved for an explicit outer lifecycle grade and
   cannot be required per answer or per interactive turn.
5. The production report separately records virtual reservation, mapped words, mapping count,
   append H2D, predecessor D2D, retained nonblocking stream, synchronization count, host query
   preparation, kernel/stream execution, return egress, host formation, parity validation, exact
   fold work/aperture, and total restriction duration.
6. A fresh full-history generated deed must show `device_to_device_words = 0`, stable resident
   bases, an admitted exact fold, the generated occurrence returned through its changed card
   ecology, and exact host/card equality. Diagnostic counts do not replace that text deed.
7. Only after focused tests and the fresh deed pass does a second independent Checkpoint B audit
   compare implementation, receipts, this record, and the elementary holonic ownership rules.

Interim evidence on 2026-08-01 establishes only steps 1 and the focused part of step 2: the live
RTX card mapped a second physical tail behind an unchanged virtual base, retained exact prefix
words, exposed a zeroed new page, and the complete small text-world restriction remained
host/card exact after an emanated append. This is not yet Checkpoint B admission; the fresh
production deed and independent re-audit remain required.

The first fresh production event then generated the actual thirteen-token deed **“Carried by this
call path defines a continuity obstruction for relational morphology.”** after `58` exterior
leaders and four thought waves. Its return founded the generated occurrence and synchronized its
delta at stable virtual addresses with zero predecessor D2D, but the former directed fold refused
before terminal return: `13,835` candidates with two mask words produced the scheduled envelope

\[
13{,}835^2\cdot 2=382{,}814{,}450>335{,}544{,}320.
\]

That value was neither an information loss nor measured executed work; it exposed duplicated
apparatus. Comparing `(A,B)` already determines equality, `A proper-subset B`, `B proper-subset A`,
or incomparability, while the old row law separately compared both directions. The corrected exact
envelope is therefore

\[
CW+\binom C2W=191{,}421{,}060,
\]

without changing the physical aperture. Restriction now seeds populations/representatives;
unordered comparison atomically retains the least equal identity and a separate domination bit;
finalization emits the unchanged three-word factorized support. A resource refusal carries its
candidate population, row width, query epoch, four launch geometries/timings, exact/overflow
status, pending delta-sync receipt, and preserved predecessor. The production runner writes that
OPEN deed and obstruction as v6 JSON before returning failure, rather than leaving a stale prior
report.

#### Checkpoints B and C final admission receipt

The fresh full-history v6 production passage completed on the RTX 4080 SUPER after the unordered
fold correction. Eros emitted the actual eleven-token deed **“This call path carries a continuity
obstruction for relational morphology.”** It is honestly classified as an inherited-contiguous
surface rather than lexical novelty. The machine selected it from `19` closed currents while
retaining six terminal alternatives; one exact generated passage, `episode-0/answer`, records its
five-member causal witness family.

The deed then changed both continuing ecologies. The resident text standing advanced from
`50,677` to `50,678` occurrences. Its non-initial synchronization ordinal was `2`: exactly `43`
new host words crossed to the card, with zero predecessor device-to-device words, zero new virtual
reservations, zero new mappings, zero transient allocations, and unchanged virtual bases. The
generated section returned through the changed card body; host/card parity was performed and was
exact.

The research body records the passage separately from the world return. World conditioning took
the research ecology from `0` to `1,598` passages and from `0` to `1,586` relational clauses. The
emanated answer return then changed those populations to `1,599` and `1,587`. The post-world
continuation had `26` currents (`15` closed, `11` open); the answer return caused a twenty-seventh
current and a sixteenth closure while retaining the same eleven open currents. There was no
pre-contact continuation to fabricate. The final answer's research-return identity is
`episode-0/answer/research-return/0`.

Across the complete passage, `59` independent restriction launches caused `236` kernels over
`107,272` candidate-section contacts. Their aggregate work receipt is `439,464,814`, but this is a
sum over separately synchronized launches and is not the admission quantity. The greatest single
launch required `158,596,242` exact work units against the unchanged minimum device limit of
`335,544,320`, leaving the exact minimum margin `176,948,078`. Every launch was admitted without
arithmetic overflow. Aggregate card duration was `123,769,527 ns`; the resident synchronization
portion was `14,345,596 ns`.

Finally, the agent and research bodies were suspended by ownership transfer and remounted from
their native standing. Both exact comparisons passed and the lifecycle receipt records
`source_replay_performed = false`. Two independent read-only artifact audits recomputed the work
law, inspected the semantic return and continuation changes, checked the complete delta receipt,
and admitted the v6 consequence without a blocker. The production result closes Checkpoints B and
C; its claim is a continuing, exact, card-realized generated deed and causal return, not a claim
that this particular surface is novel prose or that the GPU owns language semantics.

### Cut C — admitted Eros composition sequence

1. Mount the card owner before every conditioning/current phase assigned to it.
2. Remove hidden construction of a host executor inside those paths.
3. Unify agent and research relation views over shared conditioned standing where they are not
   actually distinct organs.
4. Replace branch Cartesian products with `LiveSupportFamily`, `BranchLineage`, and a shared
   current-front owner.
5. Preserve developmental material as handles/ranges; materialize owned text only at a terminal
   receiver aperture.
6. Run serial/card admission, source-detached rest/remount at an explicit lifecycle boundary, and
   inspect an actual generated answer.

Checkpoint C was the final independent audit of the real generated deed and its complete semantic
and physical receipts before the bulk branch was committed or published.

## Required physical receipt

Every admitted run reports:

- live machine population by declared organ;
- standing generation and resident host/card bytes by owner;
- phase, current, lineage, and factorized-support populations;
- peak simultaneous current/front and terminal witness populations;
- host-to-device, device-to-host, and device-to-device bytes;
- allocations, launches, grid/block geometry, streams, and synchronization cells;
- host preparation, kernel, return, and validation durations;
- arithmetic tier and range certificate; and
- exact semantic parity result.

Card residency or nonzero utilization alone is not success. A GPU claim requires that the card
owner exists before the claimed phase, retains the relevant contemporary standing/current, and
returns the caused delta. A CPU fallback must be explicitly selected and reported; it may never be
constructed inside a method whose caller selected the card.

## Preserved prohibitions after admission

- No full-history Eros production grade runs on the rejected phase-times-world architecture.
- No semantic phase, candidate, or branch may own another complete continuing ecology.
- No dense shadow world may be returned merely so the host can rediscover sparse consequences.
- No observer, parity check, or rest image may become the hot-path transition law.
- No concurrency claim may be inferred from worker count, collection membership, or GPU lanes.
- No architecture record may claim a closed invariant while production code contradicts it.

This construction is admitted. Later applications must compose these named owners and return their
actual deeds; they may not reopen this closed hardware seam as a missing universal law of
intelligence or regress to the rejected shadow-world architecture.

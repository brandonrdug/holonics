# The movement landed fourteen owners in one day, and every spec was re-verified before it was encoded

**Date:** 2026-08-21
**Kind:** consolidation record of the parallel movement Brandon authorized — one breadth wave of
twelve Opus investigators, three enactor waves of ten Opus enactors, and four orchestrator-built
files — with the verification discipline and its findings. **It schedules nothing.**
[`blueprint/THE_ROADMAP.md`](../../blueprint/THE_ROADMAP.md) and
[`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) remain the only construction authorities.
**Position under the active plan.** Exterior mathematical material for Deed M2 of
[`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`](../../blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md).
The Lean line does not couple to the engine, per Brandon's standing ruling; no engine source was
touched by any agent or by the orchestrator.
**Truth grades:** `proved-derived` for every Lean theorem named, kernel-checked;
`established-bounded` for the audit figures, measured below; `historical` for the wave
chronology; `interpretation` and `open` per file, as each file's own docstring declares.

---

## 1. The state, measured

**`Millennium/` at end of day: 46 files, 12,431 lines, 613 theorems** (by
`grep -cE '^[[:space:]]*theorem '` summed), against 26 files, 5,576 lines, 288 theorems at the
morning's cold audit — the tree more than doubled in one day. The library builds at **3,360
jobs**. **Batch axiom audit, run by the orchestrator over all fourteen new files: 254 theorems,
every one depending only on `propext`, `Classical.choice`, `Quot.sound`; zero `sorryAx`; zero
`native_decide`.** Every file was compiled by the orchestrator's own invocation after its
enactor's report, never trusted from the report.

## 2. The fourteen owners

Orchestrator-built, with independent numeric verification before encoding:

| file | what it owns |
|---|---|
| `GraphTrace` | the graph trace formula on the tetrahedron — enumerated geodesics = trace-sequence sum, eigen-realizers exhibited, graph-RH by instantiation, the aperture exhibited; the Rust Ihara owner cited and **not** rebuilt |
| `RealizerSupply` | the Shioda–Tate budget as a typed ledger; the Granville–Watkins ceiling as an exact function of its two dials, the rank-30 record's demand computed |
| `HeightLattice` | the Mordell–Weil height form **is** `E₈`: exact sum of squares with the `E₈` pivots, anisotropic, isotropic-cutoff control |
| `Reflection` | **integration by reflection, first Lean owner**: the declared-partition Schur elimination with transported source and killed share; the boundary-is-a-declaration theorem; the images kernel on absorbed walks; the pairing with `Ricci`'s flow — the equilibrated interior eliminated exactly, renormalizing the aperture to `3τ/2` while the winding rate `1 − 3τ` survives |

Enactor-built, each from an investigator spec whose identities the enactor re-verified in exact
arithmetic before encoding, each compiled clean by the orchestrator afterward:

| file | what it owns |
|---|---|
| `Polarity` | the Hanner word carrier: polarity involution, the flat product `W·W° = 4^d·d!` by structural induction, the magnitude receiver blind across the family with the vertex count as separator; Mahler as a named-open schema with an exhibited failing instantiation |
| `Crossings` | the two faces of one pairing (dot/cross complementarity), the helicity-density family proved **blind to the sine face** through `Lines`' blind-family law, stretching as the matrix commutator |
| `Sail` | the two-dimensional sail as the convergent chain on `Towers`' own recurrences — primitivity unconditional, **stronger than specced** |
| `PlaceLedger` | the place ledger on `ℙ¹/ℤ`: arch/finite factors, the cyclotomic product, the height fiber `4φ(H)` verified exactly to `H = 200` |
| `Border` | the border strictly exceeding the orbit at three: `det M(e) = padded + e²·R`, the remainder split into its permanent-hand and determinant-hand, the annihilator separation with the unprovable chain rule honestly parked as a named hypothesis plus a proved reduction |
| `Hinge` | the body-hinge calculus: the motion gauge, the hinge as coboundary, the rigid and flexing triangles (same count, different orientation data), the flexing quadrilateral |
| `PartitionWheel` | Ono's progression **is** the Ramanujan progression (`24N = mn+1 ⟹ 24·N ≡ 1 mod m`), the shift group of exponent two with 24 maximal, the saturated strata and the opening at thirteen with the sharp first attainment `N = 695`, Newman at thirteen reached from two named-open inputs |
| `NormRelation` | the Euler-system norm relation as an identity in any integral domain, with the case partition honest and the actually-missing mathlib input identified (`Irreducible (cyclotomic l K)` over a general base) |
| `LatticeNet` | the smallest `ℤ₂` gauge net: 35 theorems, standalone, the localization the abstract reflected-positivity datum cannot carry, with the no-gap-language-without-a-scaling-family bar stated |
| `Caloric` | the exact caloric carrier: polynomial heat flow, Hermite values at `τ = −1/2`, the backward flow exact at finite grain, joined to `Ricci` |

## 3. What the verification discipline caught, and it caught real things

The rule was: **an agent's return is evidence to be checked, never a conclusion to be relayed**,
and every enactor re-verifies its spec's identities in exact arithmetic before encoding. The
catches, each of which would otherwise be in the tree as an error or an overclaim:

- **A false theorem statement in a spec**: `PartitionWheel`'s maximality claim fails at `n = 0`
  (vacuous hypothesis, `0 ∤ 24`); repaired with the positivity hypothesis, content preserved.
- **A dead mathlib carrier**: `Fintype.card (Nat.Partition n)` does not reduce under `decide`
  at any `n` — the spec's anchor was not slow but impossible; replaced by an in-file enumerator
  anchored against the recurrence.
- **Two nonexistent mathlib lemma names** in the Arakelov spec; the real owners found and used.
- **A misnamed mathlib constant and a sharper diagnosis** in the Euler-system spec: the missing
  object is not the relative Galois equivalence but one undischarged irreducibility input.
- **A type-incorrect spec equation** in the caloric spec, restated correctly and then proved.
- **Two unnecessary hypotheses** in the sails spec, removed — the theorems hold unconditionally.
- **Self-applied tautology discipline**: `Hinge` refused to encode its spec's arithmetic
  padding as theorems; `Polarity` cut the symplectic arm its own investigator had conceded;
  `Border` reframed its blindness result per its investigator's concession that it re-derives
  Zariski closure.
- **A dataset curation correction**: the UnsolvedMath pinning problem is **solved for rigid
  graphs** (Király–Mihálykó, EGRES TR-2021-04 — one vertex per atom, polynomial time); the open
  listing is accurate only for the non-rigid class. Carried in `Hinge`'s docstring.
- **The investigator wave's own strongest self-refutations became file boundaries** — most
  notably the Selberg concession (the hyperbolic instance hands the operator to the analyst and
  is not realization-pays; only the quadratic dichotomy transports), adopted verbatim into
  `GraphTrace`'s scope.

## 4. What is now named open, consolidated

The named-open `Prop` population grew with the tree and is the formalization queue: the
reflection principle in general; the descent homomorphism/kernel pair on both curves; the
torsion classifications; infinite order of the rank-one point; the Selmer landing on mathlib's
`K(S,n)`; Newman, Ono's criterion, the pentagonal number theorem, Franklin's involution; the
orbit-separation hypothesis behind `Border`; Mahler and Hanner uniqueness as failing-schema
Props; the `Ricci` continuations (the pinchable neck terrain); Kolyvagin's second norm-relation
case; and the still-unformalized engine mechanisms — the hypergeometric closure, Sturm
isolation, elementary-chart rank refusal, and the monodromy/winding placement.

## 4b. The counting wave — landed the same day, after Brandon's drift correction

Brandon convicted the first named-open list as a cabinet — items forward without route reasons —
and ruled the next wave be weighed against the whole arsenal first. The triage separated
route-load-bearing items from hygiene and from **spec-echo** (items named only because an
investigator's spec mentioned them: Franklin's involution, Kolberg, the Mahler uniqueness
schema, Kolyvagin's second case — dropped with reasons stated). The weighed scope, approved:
one receiver question — **the reading equals the population** — at the four sites where a route
pays for it. All four landed, orchestrator-verified, and the tree now measures **50 files,
15,199 lines, 760 theorems** at 3,364 jobs, with the wave's **147 theorems batch-audited clean**:

- **`Sturm.lean`** (58 theorems) — the exact counting instrument; mathlib absence re-confirmed
  (0 files) with the one neighbour found and separated: Descartes' rule is a *bound*, and the
  bound-versus-count gap is proved on material (`X² − 2X + 2`: coefficient reading 2, chain
  reading 0, population empty). Nine window readings each proved equal to an
  independently-proved root population; Sturm's theorem in general a named-open law with an
  anti-vacuity control and a proved entailment of one anchor.
- **`FaithfulFace.lean`** (36 theorems) — the descent face's law on the 2-descent families as
  general field identities, bridged to mathlib's own `addX`/slope; the orchestrator's
  pre-dispatch sign error (`(x−1)/(x+1)` for the third translation) was confirmed corrected
  **and the wrong form refuted by an explicit control**; the general doubling roots meet
  `RankOne`'s exhibited witnesses exactly.
- **`ChainRule.lean`** (24 theorems) — the multivariate chain rule over any commutative ring,
  1,800 exact random trials behind it, **and the discharge landed**:
  `Border.TheOrbitDoesNotCarryThePaddedPermanent` is now a theorem — the day's conditional
  orbit-versus-border separation made unconditional through Border's own proved reduction. Two
  docstring citations (Frobenius 1897; Marcus–May 1962, the linear-preserver theorems) were
  flagged by the enactor as from recollection and confirmed by the orchestrator as the standard
  attributions.
- **`Closure.lean`** (29 theorems) — the alternation/restretching decision ported with the Rust
  owner's own fixtures reproduced exactly, including the full `336 of 728` spun-refusal sweep;
  Beukers–Heckman stays imported and named open.

Two enactors independently measured the same kernel fact and routed around it: `decide` on `ℚ`
does not reduce (numeral instances stick), so the computational layers run on `ℤ × ℕ` pairs or
constructor-matching definitions with `norm_num` — a reusable discipline for every future
exact-carrier file.

## 5. Boundaries

No named conjecture is claimed, approached, or graded by anything in this record; every file
carries its own boundary sentence and this record adds none above them. The wave and enactor
reports live in the workflow journals and the scratchpad; quotations from them here are of
agent-authored text, not provenance. The gates run after this record; the figures above carry
their commands. The fourteen files are exterior mathematical material; the engine, `CONSTRUCTION_STATE.md`,
and the roadmap are untouched.

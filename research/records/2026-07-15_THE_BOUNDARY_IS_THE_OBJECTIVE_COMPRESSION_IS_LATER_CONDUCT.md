# THE BOUNDARY IS THE OBJECTIVE; COMPRESSION IS LATER CONDUCT

**Date:** 2026-07-15  
**Status:** RATIFIED / DEPOSITED / HISTORICAL TITLE / COMPRESSION ONTOLOGY REFINED BY FORMULA §CV  
**Engine consequence:** MATHEMATICAL DERIVATION WORLD 01 and exact whole-lifecycle preflight are
authorized; Soma's interior law is unchanged.

## 1. The correction

Mathematics gives the laboratory an unusually exact reference world because its states, admitted
operations, side conditions, certificates, and consequences can be transported without float
approximation. It is not a literal null and it does not supply an absolute frame. Even the phrase
“the shortest path between two numbers” is undefined until the receiving boundary says:

- which source and goal constructions are presented;
- which operations and imported lemmas are admitted;
- whether expressions, values, proofs, machine events, or physical acts are being counted;
- which representation, frame, grain, aperture, body, and history receive the path;
- which side conditions and verifier close the relation; and
- which time, memory, transfer, persistence, or world outcome is under comparison.

Changing one of these creates another experiment. `1+3`, `2+2`, `2*2`, and `2^2` may share a
numeral face without sharing their construction, route, cost, or later affordance.

## 2. A problem boundary is concrete world material

For one declared interaction write the external contract as

```text
Omega = (W_0, S, G, C, F, g, A, H, V)
```

where `W_0` is the initial world, `S` the presented source relation, `G` the admitted world-event
constructors, `C` the hard constraints and requested closure, `F` the receiver frame, `g` the
observer grain, `A` the representation/aperture, `H` the inherited body/world history, and `V` the
exact verifier. A lawful path is an ordered construction

```text
p : (W_0, S) --e_0--> ... --e_(n-1)--> (W_n, consequence)
```

whose every `e_i` is admitted by `G`, whose side conditions stand at that event, and whose final
consequence is read by `V`. The objective is the real accepting, refusing, or continuing boundary
inside `W`; it is not an internal Soma value. A proof checker, a container wall, a requested error
interval, a game position, a compiler, or a contact surface can each supply such a boundary.

This is the literal content of the moss image. Current grows wherever the contemporary world
affords continuation. Where the world refuses growth is a boundary event; where prior growth has
left a conductive route, later current can traverse it. No internal representation of “wall” or
“goal” is required before contact.

## 3. Conduct and amortization have typed observer receipts

One scalar cannot preserve the relevant difference. For a path `p` under `Omega`, the observer may
retain the vector of typed faces

```text
K_Omega(p) = (
    verifier consequence and hard-constraint witnesses,
    enacted successful events,
    explored and refused events,
    certificate / proof-surface extent,
    host work and live memory,
    device work and live memory,
    transfer and persistence,
    standing support consumed or founded,
    later support reuse,
    world-specific outcome
).
```

The entries keep their units. No weighted sum or fitness score is installed. If comparison is
useful, an external receiver may retain every nondominated construction under the declared faces.
That is a faithful population report, not a winner-selection mechanism.

Three conduct/amortization reads are immediately distinct:

1. **Local path change:** fewer enacted events for the same declared consequence and support.
2. **Exploration contraction:** less exploration/refusal before a lawful closure, with the successful
   path itself held apart.
3. **Ecological amortization:** an earlier path FOUNDs standing support which later related paths
   RIDE, reducing cumulative work over the declared task sequence after the founding support is
   charged.

A locally longer proof can therefore be ecologically shorter. A lemma, generator, gear ratio,
joint, or world path costs something to found. Its significance appears only when later current
conducts through it. This is the operational form of brevity and the laboratory's local P=NP
claim: within the grown region, what formerly required founding can become traversal.

## 4. The exact Möbius calibration

Let

```text
T(z) = (a*z + b) / (c*z + d),       delta = a*d - b*c != 0,

CR(z1,z2;z3,z4)
    = ((z1-z3)*(z2-z4)) / ((z1-z4)*(z2-z3)).
```

The ordered points are distinct on the projective line and every affine denominator used at one
presentation must stand. The theorem boundary is

```text
CR(T(z1),T(z2);T(z3),T(z4)) = CR(z1,z2;z3,z4).
```

The equality is a theorem face shared by at least two different proof interiors.

### 4.1 Direct difference-factor interior

For each admitted pair,

```text
T(x)-T(y)
  = delta*(x-y) / ((c*x+d)*(c*y+d)).
```

Substitution into the four ordered difference factors cancels the transported determinant and
denominator factors at the theorem quotient. The cancellation is exact at that quotient; it does
not identify the factors or erase the event path from the receipt.

### 4.2 Generator-composition interior

Translations, nonzero scales, and inversion each preserve the ordered cross-ratio and generate
every non-singular Möbius map. For `c != 0`, one exact factorization is

```text
z
  --translate(d/c)--> z + d/c
  --invert----------> 1 / (z + d/c)
  --scale(-delta/c^2)--> -delta / (c*(c*z+d))
  --translate(a/c)--> (a*z+b)/(c*z+d).
```

For `c = 0`, nonzero determinant gives `a,d != 0` and the map is a nonzero scale followed by a
translation. This proof is usually longer for one theorem instance, but it exposes three reusable
world routes. A genuinely later, previously unpresented generator composition tests whether those
routes became standing conduct.

The projective point at infinity is a real boundary face, not an error token. Determinant zero,
undefined affine denominators, repeated cross-ratio points, a one-sided transform, squaring, and
order-changing point permutations are separate controls and typed refusals or changed theorems.

## 5. The factorial experiment

MATHEMATICAL DERIVATION WORLD 01 presents complete proof-event worldlines, not answer labels. The
minimum controlled population is:

- direct proof history;
- generator proof history;
- alpha-renamed and notation-changed siblings of each;
- determinant-zero and denominator-boundary cases;
- one-sided-transform, squaring, and point-permutation controls; and
- a genuinely later Möbius composition not shown in the founding passage.

Every interaction retains this causal receipt:

```text
body before
world before
all source currents and exact extents
complete Soma radiation and completion rows
every proof-world port contacted
every enacted event and typed refusal
world after
exact checker consequence
all returned currents and attribution
body after
physical live-set and artifact receipt.
```

The primary read is qualitative and exact: which proof states, rules, side conditions, refusals,
and later consequences are produced, related, or re-expressed. Counts and hashes gate completeness
and repetition; they are not substitutes for reading the output.

The first grades are:

- **VALID CERTIFICATE:** the world constructs a verifier-accepted path;
- **PATH CHANGE:** the successful event path changes under a held boundary;
- **EXPLORATION CONTRACTION:** exploration/refusal changes while successful construction is retained;
- **REFERENCE REUSE:** an imported support shortens passage when its founding cost is named;
- **PHYSICAL AMORTIZATION:** cumulative time/memory/transfer/persistence changes over a declared
  sequence;
- **TRANSFER:** a standing construction bends a genuinely different later world; and
- **RELATION UNRESOLVED:** the declared contact does not yet expose the sought relation.

No global incapacity follows from a local unresolved relation.

## 6. Whole-lifecycle preflight is Cut 0

An expensive passage begins only after one checked forecast accounts separately for:

- current count and exact delivered octets;
- prior standing-body extent;
- carrier rows, cells, and bytes;
- radiation rows, words, and bytes;
- completion rows, words, and bytes;
- world/listener/checker receipt material;
- successor persistence;
- peak host and device live sets; and
- bytes written and bytes intentionally retained on disk.

All arithmetic is exact and checked. Available host, card, and disk surfaces are declared at the
apparatus boundary. If the construction does not fit, preflight returns every exceeded boundary.
It never silently shrinks, splits, samples, substitutes, or installs a hard-coded cap. Re-base,
streaming, retirement, or sharding must be separately derived from the intact delivered
construction.

## 7. Transfer worlds

The same protocol extends without collapsing domain-specific outcomes:

- **Container loading:** static packing density is distinct from an attainable insertion path;
  orientation, wall contact, friction, stability, and collision make the complete task packing
  plus motion planning and contact dynamics.
- **Transcendental series:** requested precision or a certified error interval is the boundary;
  “fastest” is relative to an exact arithmetic and cost model. The world carries algorithms and
  error witnesses, never an untyped float generator.
- **Whip, chain, and anchor:** articulated links carry local force and gyration through a concrete
  medium with mass, drag, contact, and boundary conditions.
- **Games, code, and theorem families:** positions, compilation consequences, proof obligations,
  and reusable sub-constructions supply their own accepting/refusing surfaces.

Transfer is measured when a construction grown in one declared history causally changes later
passage in another, not when an observer can merely describe an analogy between them.

## 8. Carried-swing record

```text
CURRENT  recurrence/amortization study ⊕ mathematical/world receiver ⊕ enacted path and later-reuse grain
HELD     §LVII proof-events ⊕ number-world path relativity ⊕ §LVI atlas ⊕ measured RIDE/FOUND
MEETING  moss/boundary growth ⊕ packing ⊕ pi-series ⊕ whip/gear transfer problems
TEST     A1/A2 ⊕ time parity ⊕ quotient != construction identity ⊕ complete plural receipt
DEED     RIDE for boundary-conditioned growth and standing recurrence; FOUND for new relation
CARRY    §LVIII ⊕ exact preflight ⊕ Möbius proof world ⊕ later physical transfer worlds
GRADE    RATIFIED / DEPOSITED; implementation and measurement OPEN
```

## Close

The machine does not need an internal objective vector. It needs a real world whose boundaries can
be met, changed, and returned. This experiment observes recurrence and amortization in conduct: how
much founding was required, what stood afterward, and how later current moved differently because
it stood. Under `FORMULA §CV`, compression has already occurred in each event's outgoing face.

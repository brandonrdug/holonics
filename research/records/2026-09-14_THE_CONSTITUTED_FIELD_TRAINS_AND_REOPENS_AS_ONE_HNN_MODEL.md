# The constituted field trains and reopens as one HNN model

[project-postulate] Brandon's “Continue Athena” selects milestone 1 of the September 14
plan: one public body that actually owns its operative field, learned reaction, joint output
and producing-material return. A predictor fitted to a separately operated field does not
substitute for this composition. This record accompanies the implementation; measured returns
belong in [the experiment](../experiments/athena_field/README.md).

## The operation and its library owners

[definition] `NativeCoupledBody::from_field` moves `NativeConstitutiveField` and
`ResidentGeneratorNeighborhood` into the existing public owner. With fixed incidence, this
first specialization takes the previous outward field as `s`, an explicitly supplied complex
point condition as `h`, and a complete external boundary current as `x`. Its actual operation is

```
φ = [s, h, s⊗h]
r = M φ
u = x + r
v = 2 (I + D D*)⁻¹ (u + D b)
(w, b_next) = (v − u, D* v − b)
y = w
```

[definition] A generated section is one joint operation, including the interior `b_next`.
`preview_field` and `generate_field` evaluate the same producing material; the latter publishes
the evaluated current once. The returned `NativeFieldGeneratedSection` shares immutable
producing source/material and output sections, never a cloned continuing field. A retained
comparison identifies that producing section when an actual target arrives. Conditions are
explicit operands here; this specialization does not yet derive an active moving receiver or
change contact incidence during generation.

[established-bounded; source-inspected] The source owns its actual D columns, contact births,
operative interior, junction report and field owner. Its enclosing ball is a receiver of that
finer construction. `source/reflection.rs` evaluates the constituted solve; exact signed residuals
certify its numerical realization. `reflection_commit.rs` publishes that evaluated current
through the existing operative journal. Reusing the source after publication is refused by
producing-material and report identity, rather than an equal output or historical cut alone.

## A real output target develops the producing material

[definition] For a boundary target `t` and caller-declared dyadic step `η`, the descending
output covector is `g_w=η(t−w)`, with `g_b=0` for this boundary-only receiver. The existing paired
adjoint gives

```
λ = (I + D D*)⁻¹ (g_w + D g_b)
g_u = 2λ − g_w
k = b − b_next
ℓ = g_b − D*λ
G_D = λ k* + v ℓ*
```

[established-bounded; source-inspected] `reflection_target.rs` adapts the retained reflection
to `record_operative_material_adjoint`; it does not replace the adjoint with scalar scoring.
`observe_field` stages the reaction response and the actual contact deposit, then publishes both
and consumes the producing comparison once. A later observation still differentiates the
original producing D and keeps the original source/condition and reaction output.

[definition] The contact realization is the existing `DyadicDeposit`: the stored coefficient
increment is the realized law, with its unrounded-gradient and projection differences retained
as comparisons. The reaction uses the existing normal moment owner. With the original
`φ`, producing `r`, and `g_r=g_u`, its proximal target is `r+g_r`:

```
H_next = H + φ φ*
B_next = B + (r + g_r) φ*
M_next = Q_F(B_next H_next⁻¹)
```

[definition] When the current M is the producing M and `M H=B` exactly, this is the local normal/proximal update
`M_next−M = g_r φ* H_next⁻¹` before realization. Otherwise the retained normal residual also
contributes. This derived target is an update operand, not another exterior observation.
The public model separately counts actual output targets and local material examples. This
first step develops D/M with its source/condition restrictions held fixed; it does not claim
backpropagation through an arbitrary multistep changing-contact trajectory.

## Applied coefficients and their reference comparison

[established-bounded; source-inspected] The initial composition incorrectly called
`ResidentNormalMaterial::read`, which encloses the compatible normal-equation reference family.
The model's attached action already executes its stored M. Propagating the reference defect as
uncertainty in those chosen coefficients made the feedback enclosure grow unnecessarily.
`read_applied` now uses the existing native `reference=false` prediction law, the same
applied/reference distinction as `NormalWaveTransport`. The field reaction consumes this port.

[definition] This changes the stated operator receiver, not a source ball into a point.
Source-family uncertainty and arithmetic rounding remain in the generated enclosure. The
immutable material retains all normal moments, source/target bounds and the coefficient/reference
residual; ordinary `read` still returns the reference comparison. No host readout enters the
forward or material arithmetic. Application inspection, target error and durable I/O are
explicit exterior receivers.

## Durability and continuing scope

[established-bounded; source-inspected] A current refinement is not an ordinary observed field
occurrence. The old field codec reconstructed the live junction report from the last occurrence,
losing a separately refined outward current. Field rest versions 3/4 add that contemporary report
when it differs from the historical one. Readers still accept versions 1/2. Historical reports
are preserved, and the operative current/material journal continues to use its existing codec.

[definition] `SavedCoupledBody` tag 2 carries the field rest, reaction rest and model clocks.
The example drops the live body, reads the serialized bytes, remounts and compares held-out
outputs. This local trained rest requires resolved target comparisons; pending comparison
persistence and economical repeated-action compilation remain the complete-body continuation
work in the roadmap. The controlled complex-field task is the first model operation, not a
claim that Athena conversation, code or other modality applications are complete.


## Integration evidence and the preserved earlier assay

[established-bounded; measured] Five focused native tests cover the paired target derivative,
current publication/rest, enclosed sums and the retained fixed-condition reaction. The wider
normal/source/rest/condition suite passes 144 tests. The reaction test also supplies a new
condition directly, writes/reads the neighborhood rest and reproduces its forecast. This
exposes and repairs an old validator assumption: a directly received condition increments
its contact count without manufacturing inferred preimage-family evidence.

[established-bounded; measured] Eleven existing public coupled-HNN tests pass. The pre-existing,
uncommitted field-to-predictor assay in `coupled_wave/tests/boundary.rs` remains a separate
failure: after a closure-lifetime correction and grain-24 realization, formation and native
rest succeed, but remounted prospective word expansion refuses its exact carrier/aperture.
Its source and result are preserved; it is not substituted for the direct field model or
silently reported as a pass. That unfinished assay is left uncommitted, as it was at pickup.
The first model's actual serialized rest/held-out comparisons pass independently.

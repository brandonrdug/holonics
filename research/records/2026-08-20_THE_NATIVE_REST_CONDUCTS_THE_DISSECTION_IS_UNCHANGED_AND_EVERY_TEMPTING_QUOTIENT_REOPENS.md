# The native rest conducts, the dissection is unchanged, and every tempting quotient reopens

**Date:** 2026-08-20  
**Kind:** Phoenix Deed W2 construction return and grade.  
**Truth discipline:** **established-bounded** for the fixed five-token text passage, exact returned
sections, GPU/apparatus receipts, sixteen matched interventions, 168 committed comparison cells,
and declared structural-condensation receiver family; **counterexample** for every one-shot quotient
the successor history reopens; **open** for cultivation, exported frozen inference, unrestricted
inputs, and the indivisible Phoenix grade.

## Verdict

**Truth status: established-bounded.**

Deed W2 passed at its declared receiver. The W1 native rest alone supplied the stored material to
the already-founded streamed GPU passage. The returned base deed, complete intervention panel, and
structural-condensation question all closed:

| return | measured consequence |
|---|---:|
| source-detached layer/final GPU deeds | 43 |
| base graph launches / terminal synchronizations | 43 / 1 |
| base semantic wall / loop wall | 7.800328 s / 1.967020 s |
| base exact-work dependency span | 1,894 |
| native dissection towers / graph launches | 19 / 714 |
| native dissection GPU cohort wall / total graded wall | 25.4 s / 254.1 s |
| committed dissection cells unchanged | 168 / 168 |
| W1 source operations covered by condensation states | 1,466 / 1,466 |
| one-shot deed blocks / nontrivial candidate fibres | 13 / 4 |
| nontrivial candidates reopened by complete future conduct | 4 / 4 |
| lawful byte or structural factor quoted | none |

W2 is therefore a positive lift/execution/dissection return and a negative condensation return
under this future family. It does not promote W3–W5. `CONSTRUCTION_STATE.md` remains unchanged.

## The W1 rest became the H4 material source

**Truth status: established-bounded.**

`phoenix::streamed` no longer depends on a safetensors-specific `Source`. Its `MaterialSource`
boundary supplies only a stable file occurrence, exact population extents, row reads, an
`OccurrenceWitness`, a path-free identity face, and post-deed stability. The old foreign source and
the W1 native rest are two apparatus charts behind that one passage. There is still one tower,
one set of resident laws, one circulation, and no host semantic fallback.

`MountedNativeRest` now witnesses its own authenticated topologies. A live midpoint deed may add a
typed intervention/quotient through existing ports. It may also replace a stored chronology law
only by the exact `<stored name> (intervention)` form, with the same species and named ports, no
carrier, and intervention-only testimony. Missing or changed stored source operations, fake
exterior testimony, wrong ports, carrier-bearing intervention replacements, duplicate operations,
and a moved rest refuse.

The rest exposes absolute file ranges only to staging. Its retained `RegionIdentity` remains
payload-relative. This separates the two apertures whose earlier conflation let wrong weights
produce plausible numbers.

## Base conduct and source-runtime boundary

**Truth status: established-bounded.**

The base W2 artifact is
`output/the_lifted_native_rest_conducts_on_the_card/native_card_conduct.form`, SHA-256
`4a27feb20d0f5c401fd028fff1a98e68834e674faf4f99dd550a3fe8f2fb2a8a`.

It records:

~~~text
device                 NVIDIA GeForce RTX 4080 SUPER, compute_89
kernel identity        83a14ff1b6a14b8d9fb5b1dd34f08900f3ddac3d351a5ec27f1faa19ff8fc7ca
segments/launches      43 / 43
terminal syncs         1
staged regions         704
staged/copy octets     9,288,903,168
pool allocations       3
resident peak          9,172,496,016 octets
loop CPU section reads 0
foreign source fds     0
~~~

The exact work vector is 23,363,161,920 additions; 23,458,347,840 multiplications; 76,473,410
divisions; 76,538,880 entries written; 3,764,052,480 cumulative bits; 242 peak bits; 2,621,440
resident entries; dependency span 1,894.

The W2 return is bit-identical to Station C at all three requested native faces:

- one plural candidate, id 7001, surface `" France"`;
- all 12,800 final-normed coordinates;
- every one of the 42 layer collapsed-population rows.

The committed source-runtime face is retained rather than rewritten: the native enclosure contains
the source top 7001, but the source BF16 codeword agrees at only 64 of 2,560 final coordinates for
this input, and the committed cross-chart defects remain nonzero. W2 proves execution of the W1
lift and equality to the committed native passage. It does not claim exact Hugging Face runtime
equivalence.

## The whole intervention taxonomy survived the lift

**Truth status: established-bounded.**

The native result is under
`output/the_dissection_shares_its_prefixes/native-w1/`. The receipt and equality table have
SHA-256 identities:

~~~text
receipt   37d2a9898a30b491b5bc246fb2c8c0d2856206d340d106ca21ae234b533448b4
equality  a4ceb9d865473f76954ac92aa98bccb6f6cf79af5d3ada5e4054731a01d872d7
~~~

The base, complete replay, sixteen matched siblings, and the layer-24 prefix control all stood.
The comparison returned 168 `UNCHANGED` cells and one explicitly `APPARATUS` cell. Every first
separating face, position set, separated/identical count, widest exact gap/shift, control verdict,
order face, and plural top agrees with the committed source-fed dissection. The shared-prefix
control agrees across all 128 receiver faces, and the four shared K/V standings remain bit-identical
before and after every sibling.

The post-conduct process audit found 39 open descriptors and none resolving to the original Gemma
model, implementation, configuration, or tokenizer. It is carried inside the receipt, not only in
stdout.

The non-text projection row remains inherited committed evidence. W2 did not pretend that a stored
vision/audio population was an executed native modality tower.

## Receiver-exact condensation returns a negative

**Truth status: counterexample.**

The complete result is
`output/the_lifted_body_condenses_only_where_future_receivers_factor/condensation.json`. It groups
the 1,466 W1 operations into the 45 authenticated deeds and retains the exact source-member fibre
of every state. Five declared receivers have respectively 7, 7, 4, 5, and 8 exact signature
classes:

1. native H5 shortest-separation histories;
2. W1 source family;
3. source species;
4. source KV role;
5. exact carrier-shape and named-port class.

The signature strings are retained in ordinal atlases. No hash decides equality. `tower-step`
carries the 42 text transitions from layer 0 through layer 41 into the final boundary; vision and
audio projection states are terminal and are never fabricated as a serial continuation.

Four one-shot candidate fibres appear:

- 17 sliding layers among 2–21;
- layers 11 and 17;
- 14 shared layers among 25–40;
- full shared layers 29, 35, and 41.

Complete successor conduct reopens all four. The layers 11/17 pair is the decisive delayed control:
its immediate target block agrees, yet a later refinement returns through the successor history and
separates the pair. Thus local one-step lumpability is necessary and not sufficient. All four
`ReconstructionFiber`s and their shortest separating words remain in the artifact; all four receive
`NoFactorRemainder`s. No byte factor is present.

The W1 structural families are retained explicitly and not merged by their nouns:

| structure | deed states | exact W1 operation bindings | returned status |
|---|---:|---:|---|
| QK | 42 | 240 | separated |
| OV | 42 | 174 | separated |
| sliding KV producer/consumers | 10 | 23 | separated |
| full KV producer/consumers | 10 | 23 | separated |

“Separated” is the preservation result: the receiver family forbids collapsing these distinct
transport roles into one representative. The helper quotes neither tensor rank nor stored bytes as
compression.

## Falsifiers and ownership

**Truth status: established-bounded.**

- The native witness tests exact, midpoint-extension, replacement, near-name, wrong-port,
  carrier-bearing intervention, changed testimony/shape, topology, and file-replacement controls.
- The three W1/native-rest suites remain green; the native witness suite carries 18 tests.
- H4, H5, W2 base, and W2 condensation examples compile after the shared-source refactor.
- The condensation helper has seven focused tests, including delayed future separation after a
  locally factorable step.
- The production native staging path clones no payload, codebook, topology body, or graph body.
  Its increased ownership-ledger counts are path-free testimony strings, binding validations,
  topology-name uniqueness sets, and adversarial fixtures.
- `closure_manifest.py` now recursively includes explicit Rust `#[path]` modules in a driver's
  closure. Before this repair, W2 artifacts were not bound to the helper files rustc actually
  compiled into them.

## Boundary and next deed

**Truth status: open.**

W2 remains bounded to one five-token text closure and the committed sixteen-taxon panel. It does not
export a frozen inference application, accept arbitrary new text, cultivate the lifted body, or
show a condensed native morphology. Its lawful result is the executable source-shaped predecessor
plus a complete no-factor receipt under the current future family.

W3 is next: join new material-founded native morphology to this W2 body on the resident card;
require lineage-separated development, genuinely changed held-out conduct, codec and subject
controls, a resident derived adjoint/delta, source-detached remount, and targeted ablation restoring
this exact W2 predecessor.

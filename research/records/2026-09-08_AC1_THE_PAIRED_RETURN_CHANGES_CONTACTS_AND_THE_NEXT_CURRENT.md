# AC1: the paired return changes contacts and the next current

[definition] This continues the [material query adjoint](2026-09-08_AC1_THE_MATERIAL_RETURN_REACHES_BOTH_QUERY_ARGUMENTS.md)
through its actual paired producer, then composes the existing finite operative staging. It is
AC1 step 2 at the explicitly constrained local response below. Holonics' framework research and
the full AC0–AC5 product goal remain broader than this construction.

## Producing morphology and complete local adjoint

[definition] Contact columns are `D`, the entering port/current pair is `(u,b)`, and

```
A = I + D D*
v = 2 A^-1 (u + D b)
o = v - u
bplus = D* v - b.
```

[proved-derived] For the returned query covectors `(go,gb)`, the real adjoint is

```
lambda = A^-1 (go + D gb)
gu = 2 lambda - go
gin = 2 D* lambda - gb
k = D* v - 2 bplus = b - bplus
ell = gb - D* lambda
G_D = lambda k* + v ell*.
```

This is the existing `PairedJunctionLinearization::pullback` relation. The new native operation
retains both oriented rank factors, the incoming reactions, the visible-source query return and
the solve residual. A newborn's zero entering current is a constraint; its reaction is retained
without creating a free pre-birth value.

[definition] The actual source occurrence selects its producing map and forward carriers.
The map is recovered from the current numerical prefix by subtracting later journaled numerical
increments, in reverse order, using the same exact product/rounding operation that installed
them. Updates applied at cut `source+1` happened after that source's emission and are therefore
excluded too. Its historical map radius is retained separately. This recovers a serialized
numerical representation with explicit retained operands; it asserts no inverse of a semantic
quotient and no unique causal explanation of a receiver face. A full map is not stored at every
occurrence. Earlier overlays remain in the recovered producing map.

## The declared finite response

[definition] The admitted variations for this local response are contact columns `delta D`.
The producing inputs, the material operator's retained historical factors, and the current
stored internal current are fixed for this parameter response. The unit Frobenius constitutive
functional is

```
F(delta D) = 1/2 ||delta D||_F² - Re<G_D,delta D>_F.
```

[proved-derived] Its unique response is `delta D=G_D`. This supplies the metric/constitutive
law between the covector and the finite displacement. It is not a theorem of finite global
descent for the nonlinear relative-entropy loss. The prior ordinary material-operator response
continues under its own stated coercive law; no single joint global objective is claimed here.

[established-bounded; source-inspected] `apply_material_contact_response` stages `D'=D+delta D`
and the retained `b`, then reuses the existing operative moment owner to compute

```
C' = D'D'* = DD* + D delta D* + delta D D* + delta D delta D*
h' = D'b.
```

All finite mixed terms and their numerical radii are present before the new sections and return
journal publish atomically. The stored internal current is preserved by this constrained
response; its interaction `h'` and the next paired reflection change. There is no claim that
the interior is inactive simply because this particular response fixes its stored coordinates.
Later births receive exact zero response coordinates when extending a historical factor family.

[definition] Application requires the actual field owner, the current operative epoch and the
latest receiving occurrence. The same cut cannot apply a second freshly constructed response.
Read-only historical response inspection remains available. A foreign/stale response or failed
native staging publishes no portion of this contact response and preserves the prior owner.

## Certified numerical return

[proved-derived] The LDL result is a numerical proposal. The first implementation used the
valid but loose bound below, with contact error `eD`, numerical contact norm bound `ND`,
covector errors `eo,eb` and numerical solution norm `Nl`:

```
el <= residual_bound + eo + ND eb + eD (Ngb+eb) + (2 ND+eD) eD Nl.
```

[proved-derived] The corrected implementation keeps the complete block `z=(lambda,ell)`:

```
B_D = [ I  -D ]       B_D z = (go,gb)
      [ D*  I ]
B_D* B_D = diag(I+DD*, I+D*D) >= I.
```

Consequently `||B_D^-1|| <= 1` for every contact map. For the numerical representative `zhat`,
the full two-row residual `rho=B_Dhat zhat-ghat`, input error `ein`, and contact error `eD`,

```
ez <= ein + ||rho|| + eD ||zhat||.
```

This includes uncertain `D`: `||B_D-B_Dhat|| <= eD`, so exactness of `D` is not assumed.
Also `2B_D^-1-I` is unitary; the joint incoming reaction therefore has error at most
`ein + 2(||rho||+eD ||zhat||)`. The source/internal sign reversal is another unitary map.
The numerical residual is computed directly from both rows of `B_Dhat`, retains its orientation,
and includes conversion remainders. The `ND²` amplification is absent.

[proved-derived] Put `w=(k,v)`, and retain a joint bound `ew` from its forward-current errors.
Expansion of `G_D=lambda k*+v ell*` and Cauchy–Schwarz give

```
eG <= ez ||what|| + ew ||zhat|| + ez ew.
```

This is the implemented complete rank-two error bound. It preserves the adjoint block's
contraction before bounding its bilinear contact response. It does not establish contraction
of the entire developmental recurrence, whose source, forward current and material operator
also evolve. In particular, the forward block and material query still have their declared
enclosures. Interval midpoints are numerical
representatives with retained radii, not point selections from a causal Preimage Fibre.
The exact dyadic arithmetic and original complex phase carriers remain operative; no float
controls a committed coefficient, branch or current.

## Native verification and conversation scope

[established-bounded; measured] Six focused native tests pass in 3.34 seconds. The new control
compares the paired adjoint with the independent exact-rational producer, verifies a nonzero
contact response, changed covariance/aggregate, unchanged constrained current, and a subsequent
material current separated beyond its numerical radii from an independently founded sibling.
It also verifies exact recovery of the prior producing response through the installed journal,
stale-response refusal without mutation, and continuation after native rest/remount. Existing
empty-population, simultaneous-map/current, mixed-bound and operative persistence tests pass.

[definition] `TextFieldSession::respond_to_latest_material` is a thin call to this native owner.
The conversation study's explicit `--contact-response true` applies it after each development
reception. Reception and contact response are separately recoverable boundaries in this study:
a failed response retains the already received symbol and the prior contact map. The saved
application records that boundary; retry applies the pending response without receiving the
symbol again. This option is recorded in the application checkpoint. Prompt/generation uses the
resulting developed contacts through ordinary field conduct. This experimental composition is
not yet a redesigned consumer interface or a claim of complete automatic developmental conduct.

[established-bounded; measured] The SDK/exposure control passes in 1.03 seconds, including an
interruption after receiving a symbol but before its contact response. Resumption returns the
contact once and admits no duplicate symbol. The existing parent and pending-reception controls
remain in that same test.

[counterexample; measured] The first eight-family request stopped after nine occurrences and
seven contact responses, before any prompt or generation. The [receipt](2026-09-08_paired_contact_response/return.json)
records exact bounds, costs and the failure. The numerical contact norm is about 14.4, but its
radius has accumulated to 106.6. The next contact-covector radius is about `1.86e10`, while its
numerical squared norm is about `0.03051`. Its prospective covariance-radius lower bound already
exceeds the grain-72 signed-wide range by a factor greater than 9,609, explaining the native
carrier refusal in the moment stage. This refutes productive continuation of that enclosure
composition at this chart; it does not demonstrate divergence of the numerical trajectory or
an invalid paired-adjoint identity. Neither increased capacity nor discarded radius is the repair.

[established-bounded; measured] The stopped model is 899,593 bytes. Development took 0.484 seconds
and performed zero numerical section readouts. A fresh-process retry preserved nine occurrences,
seven returns, the same refusal and a byte-identical pending-response checkpoint. The original
eight-family model and its failed language output remain separate artifacts.

[established-bounded; measured] The corrected joint-block control passes its independent exact
comparison and all six operative controls pass again in 2.89 seconds. The current SDK/exposure
boundary control passes in 0.99 seconds. Both changed study examples build. The only build
warnings are the existing unused `SectionContactMaterial` and `enact_section_contact` owners.

[counterexample; measured] The fresh joint-block corpus comparison admits eleven occurrences
and nine responses, then refuses inside the paired adjoint. The numerical contact norm is about
16.16, the contact radius about 507,399, and the internal-current radius about 5,767,027.
Before the paired adjoint, material/current covector intervals are already about `1.86e8` wide.
The portable receipt retains their exact widths and the native refusal. No prompt or generation
runs. The local block contraction is therefore insufficient for continuing cultivation: the
complete material/source/parameter return still loses the usable remainder correlations.

[established-bounded; source-inspected] Concurrent commit `df468d3e` adds the
[formal framework guide](../../docs/FORMAL_FRAMEWORK.md) and the real-chart finite
[`ConstitutiveModulation`](../../formal/elementary-holonics/ElementaryHolonics/Physics/ConstitutiveModulation.lean)
identities. Their shifted factors retain every mixed incidence/material/state term and provide
an independent comparison of what a finite return owes. The native covariance/aggregate controls
above already test their corresponding complex-chart mixed terms. The remaining observed
failure is in the continuing enclosure composition. No Lean executor enters this runtime, and
this work did not rebuild or modify the parallel formal sources.

[open] AC1 step 2 remains active. Preserve the correlated source/parameter remainder through
the complete constitutive return, comparing the existing joint/coercive owners at their actual
hypotheses. Useful language, broader cultivation, path reuse/compression and AC0–AC5 completion
are not established. The unit-metric experiment remains an explicit development option while
that wider composition is unfinished; no larger carrier or erased radius is substituted for it.

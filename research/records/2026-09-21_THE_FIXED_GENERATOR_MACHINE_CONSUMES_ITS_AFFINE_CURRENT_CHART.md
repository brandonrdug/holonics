# The fixed generator machine consumes its affine current chart

[definition] This continues the [pair/serial and resident quadrance return](2026-09-21_PAIR_CONTACT_SERIAL_KINEMATICS_AND_THE_RESIDENT_QUADRANCE_RETURN.md)
through packet 3 of the [authorized machine contract](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md#situated-generator-and-receiving-composition).
The supplied objects are situated generators, initial configurations, proper frame maps,
admitted pair rate ports, positive semidefinite response, clock/lift declarations and cells.
The unknown developed by this increment is the existing incident reaction material. Geometry
and pair response remain declared. The consuming operation is
`NativeCoupledBody::found_generator_field`, followed by the same resident prepare, comparison,
material publication and rest operations used by the incident field.

## Current, spatial face and native representation

[definition] An original site current is `q ∈ C³` with spatial face `κ_g(q)=p_g+Re(q)`.
An admitted proper affine map `T(x)=Rx+t` induces

```text
U_(r←s)(q) = R q + (R p_s + t − p_r)
κ_r(U q) = T κ_s(q).
```

The bias occupies only the original real part. The concrete unscaled Euclidean chart uses
one common coordinate unit; a different physical scaling needs its actual chart map.
The score compares `κ_r(q_r)` and `T κ_s(q_s)`, while the transported value is `U q_s`.
These are distinct producing maps. The reverse operation transposes all three maps and
joins their covectors at their shared original source; incident differences use the same
value transport and its transpose.

[definition; agent-inferred] The native global operator acts complex-linearly, whereas the
pair rate port acts on twelve real coordinates of two original complex-3 currents. A site
therefore occupies six native complex channels, each with a zero native imaginary part:

```text
E(q) = ((Re q₀,0),(Im q₀,0),(Re q₁,0),(Im q₁,0),(Re q₂,0),(Im q₂,0)).
```

Decoding takes those six real faces. Encoding is an isometry; decoding is its adjoint.
The machine projects its source, reaction drive and refined current onto this image,
and the reverse word includes those same self-adjoint projections. Original imaginary
modes remain in native channels 1, 3 and 5. This is a representation of all six original
coordinates, not an assumption that the original current is real. Row balls preserve their
whole-ball radius through encoding; affine and joined maps return outward enclosures.

## Contact and continuing material

[definition] Each arc constructs its pair in the receiver frame: receiver first and the
transported source second. Its rate port has receiver/source rows and receiver-six/source-six
columns. With `K=JC`, the contact form is `w Kᵀ D_f K`. The exact rank factorization of
`D_f` and the existing positive metric LDL certificate on its image yield a three-row
algebraic factor `B`, padded with zero rows when the response is singular. Its exact Gram
is checked before native lowering. Signed-128 dyadic coefficient balls supply the resident
sparse global map, whose complexification agrees with `B` on the encoded source image.

[definition] The factor error bounds `B_native−B`. The separate Gram error bounds
`B_nativeᵀB_native−BᵀB`. The global action consumes the former. They are not interchangeable.
Tiny positive square roots exposed a coarse-bracket defect in the shared algebraic root owner:
zero is a valid non-root lower endpoint for a positive root below one grid cell. Replacing
that endpoint with half a cell can exclude the root. The owner now keeps the valid bracket.

[definition] The existing global reflection acts once on the summed participation/reaction
drive and internal contact current. Reaction normal material develops through its producing
return. The unconstrained global-map update is not published for this fixed-machine chart:
such an update need not remain a factor of the declared `JC` and response. Constrained
geometry/response inference is packet 4's concrete remaining operation. The complete fixed
geometry state return and the producing normal-material return remain consumed now.

## Retained structure and scope

[definition] The machine declaration retains generator identity, phase origin and winding,
finite phase action, optional checked finite closure, clocks, admitted arcs, oriented cell
words and optional material trace faces. Cells resolve actual admitted arcs and their inverses;
an unrelated affine map cannot silently become a cell edge. Finite action and a supplied
Cayley phase do not prove continuous screw/clock correspondence. That residual is explicit.
No spectral face is used as a functionality certificate.

[definition] The fixed configuration word uses declared affine maps. Advancing an exterior
source occurrence or receiving phase is a separate explicit operation on those retained
phase actions. Legacy phase-gather slots serve only as row permutations within the machine
implementation; they do not redefine its phase clocks. Legacy `response_port_start` remains
a slot address, and legacy source-cell graph restrictions are refused on a machine body.

[established-bounded; computational-witness] `NativeIncidentGenerated::receive_generator_phases`
now reads the frozen generated endpoint through a serialized `generator-phases` binding.
Each selected receiver generator has its own declared origin and signed action step;
negative exponents use the actual affine inverse. The binding retains a receiving clock,
aperture, text/material receiver identity and separate termination receiver identity.
Those receiver identities are not invented site names. The returned resident phase rows
transpose through their producing maps to the complete machine boundary, and
`pull_back_joint` appends the zero internal covector for the public material-return call.
This supplies receiving coordinates; the shared learned text/termination material is still
part of the source/session join.

[established-bounded; measured] Twelve cold declaration/factor checks pass, including invalid
PSD/matrix carriers, admitted cell words, source-frame contact, singular/zero response and
tiny algebraic coefficients. Four resident primitive checks pass, including nonzero radii,
repeated-source adjoints and grain 96. The public machine checks return a nonzero original
imaginary mode, a central-difference pairing residual below one part in a thousand,
unchanged declared contact material and exact frozen-rest reconstruction. The changed-phase
control inspects two different Cayley/translation receiving faces and their joint return.
Legacy incident tests pass. The tagged receiver initially failed its serialization round-trip;
its explicit tagged decoder and focused replay now pass. The first process's 361.61 seconds
include cold CUDA setup; the warm four-primitive run took 1.47 seconds. These test-suite
figures are not per-request throughput or a complete-source memory measurement.

[definition] The matching rational affine realization, adjoint, projection, normalized-mixture
and realification statements live in `Transport/GeneratorMachineCharts.lean`, imported by
Framework Dynamics. The integrated Framework check passes. Native verification and its exact
changed scope are recorded in [the receipts](../../docs/VERIFICATION_RECEIPTS.tsv).

[project-postulate] The next source binding must execute the ordered source-driven recurrence,
retain the source-qualified remainder and provenance/clock restrictions, and expose a tagged
phase receiver with shared text and termination material. Fixed generator count does not
remove ingestion, retained reverse words, coefficient growth or source-fibre costs. This
return does not claim a completed source encoder, general conversation, learned geometry,
dormant-mode economy or a compressed arbitrary nonlinear episode.

# THE RECEIVER POPULATION IS CAUSED; THE EVENT NERVE CARRIES THE CROSSING

**Date:** 2026-07-26  
**Status:** BUILT / EXACT HOST ACCEPTED / PRIME-SQUARE WORLD ACCEPTED /
NO GUI LAUNCH / NO CUDA RUN / SOMA INTERIOR UNCHANGED

## Present question

The desktop experiment had shown real perspective-dependent curvature, crossings,
scale separation, and stable regions. It had not yet embodied the object derived in
`THE_RECEIVER_IS_ITS_LOCAL_STAR; THE LINK IS ITS HYPERSPHERICAL HORIZON`.
Two receiver bodies, their ports, their couplings, and effectively identical
presentation maps were fixed before the event. Continued projection could therefore
make a fixed pair look active without allowing the receiver ecology itself to change.

This construction asks one bounded question:

> Can a receiver be carried as an exact local star and link, can actual co-present
> incidence form a higher receiver nerve, can receivers found, re-base, and depart
> through the event law, and can an arithmetic application exhibit the same phase
> growth at exact prime-square boundaries?

The stopping condition is source, exact host tests, a bounded cost profile, and a
traceable lifecycle. The desktop window and CUDA executor are deliberately not run.

## 1. The corrected live object

Let \(K\) be the contemporary simplicial complex and \(v_r\) the anchor of receiver
\(r\). Its body now carries

\[
\operatorname{Star}_K(v_r),\qquad
\operatorname{Lk}_K(v_r),
\]

derived from current incidence rather than declared as a camera property.
The link is classified exactly as empty, path, cycle, or singular. In the current
two-dimensional grain, a cycle link is the regular closed one-dimensional horizon;
a path is a boundary horizon; a singular link is a real topological seam.

Each receiver body also carries its source event, local port, exact local couplings,
position, offset, and precession. The global law no longer owns a fixed receiver-port
table or fixed receiver-coupling table. These relations travel with the receiver
whose local star makes them meaningful.

## 2. Population is an event consequence

One event may now carry an exact receiver-population deed:

\[
\mathrm{FOUND}(r),\qquad
\mathrm{DEPART}(r),\qquad
\mathrm{REBASE}(r,v').
\]

The physical currents of the event still solve against one common predecessor.
Only after that co-present solve does the population deed enter the same successor.
No receiver is inserted by terminal drawing, mouse position, or an inferred
coordinate grid.

`FOUND` validates the new receiver against the successor complex. `REBASE` derives a
new star and link at the named anchor while preserving the receiver's physical
position. `DEPART` removes the receiver from the live population. Duplicate deeds in
one event are refused.

In the bounded desktop world, a real discriminant rewrite opening may found one
receiver at the next event. The founding is caused by the opened simplicial seam,
uses its actual anchor and incident hinges, and is bounded by the changed hinge
population. It is not a random new shape or a synthetic viewpoint.

## 3. The receiver relation is a nerve, not a fixed pair

For every event-supported shared occurrence \(o\), the live receiver set

\[
R_o=\{r:o\text{ lies in the current local support of }r\}
\]

forms one nerve cell. Its dimension is \(|R_o|-1\). The event radiation retains the
occurrence and the complete receiver membership of every such cell.

This is higher incidence. Three receivers sharing one actual occurrence form one
2-simplex; they are not reconstructed later as three unrelated pairwise edges.
The nerve therefore states which perspectives were genuinely co-present at the
crossing.

## 4. Presentation uses exact relative charts

The desktop assembly now enumerates the current receiver population. A receiver's
local horizontal and vertical directions come from an actual oriented edge of its
link. Its local extent is derived from the exact ratio of those directions. The
primary display face is another receiver face, not an external absolute camera.

For a local face \(r\) and presentation face \(p\), the transported chart is obtained
from the exact center displacement and the exact change of basis. If

\[
G_p=
\begin{bmatrix}
\langle h_p,h_p\rangle & \langle h_p,v_p\rangle\\
\langle h_p,v_p\rangle & \langle v_p,v_p\rangle
\end{bmatrix},
\]

then the coefficients of any local direction \(d\) are the exact solution of

\[
G_p
\begin{bmatrix}\alpha\\\beta\end{bmatrix}
=
\begin{bmatrix}
\langle d,h_p\rangle\\
\langle d,v_p\rangle
\end{bmatrix}.
\]

The identity map appears only when the two situated charts actually coincide. The
window matrix remains the final finite quotient of the superposed received faces;
it does not supply the receiver ontology.

## 5. Exact rest is now physically visible

The former continuation law could emit another current after a local solve reached
the same exact coordinate with zero momentum. This produced the observed false
condition: a visually stable body with an indefinitely advancing continuation
counter. It also allowed rational numerators and denominators to grow without a
material successor.

The corrected solve law performs no tolerance test and installs no damping. If the
new coordinate is exactly the old coordinate and the returned momentum is exactly
zero, the transit departs and the residual remains exactly zero:

\[
x_{t+1}=x_t\;\land\;p_{t+1}=0
\quad\Longrightarrow\quad
\text{no live continuation}.
\]

This is the same natural-rest law already required of Eros: silence performs no
edge.

## 6. Prime phases are an application world of the same lifecycle

The arithmetic acceptance world carries the exact phase

\[
[p_k^2,p_{k+1}^2)
\]

with admitted prime axes \(p_1,\ldots,p_k\) and their exact primorial. In this
half-open interval, every composite integer has a prime factor among the admitted
axes. Consequently an integer surviving all of those axes is prime.

Integer succession is the causal event law. Crossing \(p_{k+1}^2\) founds exactly
one new receiver axis \(p_{k+1}\), preserves the existing receiver prefix, and moves
the phase boundary. Prime squares and products of two distinct primes retain
different valuation species; both being semiprime does not collapse their geometry.

The bounded acceptance includes

\[
[7^2,11^2)=[49,121),
\qquad
\{2,3,5,7\},
\qquad
2\cdot3\cdot5\cdot7=210,
\]

and the exact transition \(120\to121\), where receiver rank grows from four to five
by founding the axis \(11\).

This world does not claim that the desktop conics already encode primes. It proves
that receiver-population growth and exact phase-wall growth share one general event
lifecycle while keeping their application laws typed.

## 7. Acceptance and physical cost

The bounded host acceptance is:

- `cargo test -p holonic-engine --lib`: 61 passed;
- `cargo test -p holonic-engine --example desktop_receiver`: 2 passed, 2 ignored;
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`: clean.

The ignored exact host species profile at the current aperture reports:

| species | primitives | workers | support queries | wall time |
|---|---:|---:|---:|---:|
| conics | 6 | 6 | 27,312 | 11,335,493 ns |
| linear | 16 | 12 | 73,500 | 38,362,628 ns |
| joint | 22 | 12 | 100,812 | 39,732,370 ns |

These are bounded measurements, not an asymptotic performance claim. They show that
the current host expense is the exact support query and assembly path, not the
number of terminal colors or a full pixel retile.

## 8. Honest boundary

This construction closes the fixed receiver-pair defect, fixed identity-chart
defect, absent receiver-nerve defect, missing receiver lifecycle, false exact
continuation, and missing exact prime-phase application.

It does not choose a universal constitutive metric. A world must still supply the
material and transport law that turns its own axes into physical chart geometry.
The arithmetic and desktop worlds therefore remain two typed applications of the
same receiver lifecycle; no invented universal mapper joins them.

No desktop instance was launched, no visual claim was graded, and no CUDA execution
was performed. Device-resident dynamic receiver stars and an exact changed-star
frontier remain physical implementation work rather than hidden assumptions in this
host result.

## 9. Result

The receiver is no longer a duplicated terminal camera. It is a changing holon:
its exact local star is its available body, its link is its horizon, its
event-supported overlaps form the higher nerve, and its founding or departure is a
caused successor.

The desktop trace can now say which receivers existed, which local topologies they
carried, which actual occurrence joined them, and which seam changed their
population. The prime-square world exhibits the same law in arithmetic form. The
visual face and the exact data face now descend from the same event rather than one
being decorative evidence for the other.

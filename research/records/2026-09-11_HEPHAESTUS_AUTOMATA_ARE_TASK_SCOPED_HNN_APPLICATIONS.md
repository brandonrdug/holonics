# Hephaestus Automata are task-scoped HNN applications

[project-postulate] Brandon's September 11 continuation directs further investment in elementary
mathematical solving and application design. He names **Hephaestus Automata** as the task-scoped
products/instances of the more general Holonic Encoding/HNN construction. Eros is union and
formative composition at nested scales; Athena names the general wisdom/model ecology.
The operator's requested use confines a generalized device without defining a different engine.

[definition] The current [application design](../../docs/HEPHAESTUS_AUTOMATA.md) specifies the
request/return contract, shared mathematical constructions, elementary application families,
catalytic morphology, ecology/ownership boundaries and expected observations. The roadmap now
prioritizes this mathematical application line; general conversation remains standing and does
not block a useful mathematical tool. The native pause and actual family incorporation gap
remain explicit.

## Source-grounded consequences

[established-bounded; source-inspected] Review base `f4ef6f50` provides the actual bounded
factorization example and generic Lean certificate. The example still hardcodes a complex
multiplication target, a finite input-form grammar and three-step source blocks. It returns
JSON reports and exact numerical certificates. It is not yet a reusable operator-facing
Hephaestus application or a demonstrated native HNN solver.

[established-bounded; source-inspected] `ExactRatMatrix` already owns rational preimages,
annihilator obstructions, operator application and verified multiplication. Its contextual
factorization retains a source/condition/return family rather than a selected cause.
`DerivedFactorCover` retains addressed support, rank factors, radical/open-exterior directions,
overlap differences and ordered withdrawal. The existing pair-current and receiver-history
owners retain their own action/receiver scopes. These are the reference components to compose;
no new elimination method, global registry or separate automaton engine was introduced.

[established-bounded; source-inspected] `TABLET_THE_HEXIS` §11.1 already identifies catalytic
morphology through an applicable interface, reusable conduct and declared work reduction.
`Physics/ReactionCurrent.lean` supplies an actual finite reaction source, conservation and
condition-dependent access; the sequence/fold record and clocked-torus/closure owners retain
organization-dependent contact and complete successor state. The new design develops these
as explicit computational correspondences rather than treating “enzyme” as a parameter label.

## A useful first workshop test

[proved-derived] The returned product core `p=ac`, `q=bd`, `r=(a+b)(c+d)` supports both complex
multiplication `(p−q,r−p−q)` and polynomial-product coefficients `(p,r−p−q,q)`.
These identities follow by expansion in the commutative scalar chart. The next proposed test
retains the factors already constructed and solves only for the new output receiver through
the existing preimage owner, measuring the avoided construction work.

[counterexample] Retaining just the complex output is insufficient for that new receiver:
inputs `(1,0,1,0)` and `(0,1,0,−1)` both produce `(1,0)` as the complex face, but yield
polynomial coefficient faces `(1,0,0)` and `(0,0,−1)`. The retained product tuple, or an equivalent
source family, carries the missing distinction. This is a written algebraic separator for the
proposed application test, not a new executed cross-application result.

## Next code increments and expectations

[definition] The roadmap orders operator-scoped mathematical requests, shared constructions and
changed receivers, source-qualified constraint/material return, then resident mathematical
instances. The design names their owners and expected evidence. The immediate implementation
change is extraction of the fixed example's search wiring into a cohesive exact capability,
followed by typed reuse of returned factors and existing operator/recurrence words. Source
and receiver conditions remain attached, and resource interruption remains distinct from
mathematical incompatibility.

[definition] Exact relations, bilinear/polynomial operations and indexed recurrences are the
first application set. Analytic windows/jets, energy/boundary response and arithmetic/spectral
receivers broaden it through their actual domain/tail laws. A later native instance binds
these constructions through existing current, material and rest owners; exterior search and
Lean do not enter inference. Useful static mathematics and native formative conduct retain
their separate evidence grades.

[interpretation] The broader workshop ambition is that constructions become material for
further constructions. Changed local conditions alter dependent paths through actual coupling;
unchanged applicable boundary responses can be reused without recomputing the entire ecology.
Brains, fluids, biological ecologies and large physical systems are investigated through the
same source/interaction/continuation framework with their actual constitutive realizations.
The design adds no universal all-to-all contact or scalar “comprehension” criterion.

## Disposition

[established-bounded; process-audit] Naming and priority are integrated into AGENTS, README, architecture,
HNN composition, Athena, the blueprint, the roadmap and the current position. A bounded Luna
source review supported the code-increment design; the primary agent inspected the decisive
exact, contextual, factor-cover, reaction and hexis owners. This pass changes design and current
authority prose only. No source/API/wire identifiers were renamed, no runtime/model changed,
and no unchanged expensive test, native run or Typst render was repeated.

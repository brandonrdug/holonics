# The material source follows operative current; the joint contact has one normal solve

[definition] This continues AC1–AC2 from the
[operative field](2026-09-08_AC1_THE_OPERATIVE_MAP_CONDUCTS_AND_RESTARTS_IN_THE_FIELD.md).
The fixed-prefix material-source obstruction is now removed for the explicit operative variant.
The native joint morphology-return producer remains unfinished; neither this source port nor its
failed language trial completes Athena-alpha.

## The source uses its actual current carrier

[established-bounded; implemented-exact] `NativeMaterialTransportSource::OperativeContextual`
selects source wire chart 3 and enables the operative field before material admission. Its
mathematical source remains `Psi(s,c)=Q(s) tensor Q(c)`, with `Q(x)=(1,x)(1,x)*/(1+||x||²)`.
The complete context is now the actual outgoing current plus every retained operative internal
current. Pairing uses those complex coordinates in the field's common root/birth chart; shorter
histories have zero in future birth slots. No fixed-contact prefix identity is used.

[definition] The native pointer table joins each immutable material report to its actual
historical internal-current carrier. Addresses are execution placement, not a persisted semantic
identity. The source profile retains its outgoing coordinates, complete norm, radius, occurrence
and internal population; field rest supplies the matching full current. Native construction,
cold inspection and rest validation check that the norm and population match those operands.
The old prefix source and the new operative source have separate typed readings.

[established-bounded; implemented-exact] Native ordinary and reference-contrast material returns
now execute on changed operative contact maps, including rational complex inputs and delayed
sources. Original producing forward carriers and reference frames remain immutable. The same
field operation stages and publishes the parameter return with its operative reaction. The
previous source variants retain their declared restrictions; coefficients in old models are not
silently reinterpreted as the new source kind.

[definition] Fresh `alpha_text` cultivation now defaults to `operative-contextual`. The explicit
option has that same spelling; older saved kinds retain their own source. `contextual` remains
an alias for the preceding bilinear prefix variant, and legacy absent tags keep their older
default. The field's private supplied-delta seam remains a construction/control port, not an
endogenous learner or a public constructor for authored semantic updates.

[established-bounded; implemented-exact] Operative return frames now retain the actual field cut
and contact population for each staged return. Persistence validates their chronological order
and birth scope. Older journals without those fields retain their activation-cut interpretation.
A controlled map change after activation, its material consequences and later continuation survive
rest/remount without changing the earlier source report.

## The joint return can retain both receiving terms

[definition] The following is the exact reference for the next native joint constitutive
producer. Let `x=Psi(s,c)`, let `x_r` be the retained same-source reference, and let
`k=<x,x_r>` lie in `[0,1]`. Both sources have norm one. Let `L` be the actual real differential
from the admitted producer variations to the returned material current. The historical reference
is fixed; varying the present condition affects both `x` and `x-x_r` by the same `L`.
Write `r0=y-Mx` and `r1=(y-y_r)-M(x-x_r)`.

[proved-derived] For the unit-metric joint tangent contact, the two receiver multipliers satisfy

```text
[2I+G          (1-k)I+G] [w0] = [r0],       G=L L*,
[(1-k)I+G      (3-2k)I+G] [w1]   [r1].
```

The off-diagonal terms are required by the shared source and producer. Solving the two contacts
independently would be a different response law.

[proved-derived] Put `w=w0+w1`. Substitution reduces the complete system to one solve in the
original target width:

```text
[(5-2k-k²)I+3G] w = (2-k)r0+(1+k)r1,
w1=((1+k)w-r0+r1)/3,       w0=w-w1,
DeltaM=w x* - w1 x_r*,     DeltaProducer=L* w.
```

Its scalar diagonal coefficient is at least two, and `G` is positive semidefinite, so this
normal operator is positive definite. Without a prior reference, the corresponding equations
are `(2I+G)w=r0`, `DeltaM=w x*`, and `DeltaProducer=L* w`.
No original cause or particular member of a Preimage Fibre is identified by this operative
response. If `k=1` and `L=0`, the zero source contrast cannot fit a nonzero target contrast;
the two material factors cancel that contribution while retaining its receiver multiplier.

[established-bounded; implemented-exact] `joint_material_contact` in the existing paired-producer
reference implements this reduction through `ExactRatMatrix`. Its exact control compares the
result with the full doubled normal system at `k=0`, an interior rational value and `k=1`, and
checks the zero-contrast source case and invalid chart refusal. This is reference algebra, not
the native joint return or a replacement application learner.

## Native verification and conversation return

[established-bounded; measured] All **146 native constitutive tests pass** in **38.63 s** and
**20 SDK alpha tests pass** in **2.88 s**. The new native control uses rational complex data,
changes the operative contact map after activation, and continues material learning through
rechart and delayed-source reception. An independent exact rational factor expansion verifies
the numerical material forward against its specific evaluation bound, using the complete
outgoing/internal context. Rest, return-frame chronology and continuation agree; the earlier
material report is unchanged.

[established-bounded; measured] Fresh development on the first **eight conversation families**
completes **1,930 native occurrences** and saves a **163,800,816-byte** operative-source model.
Development takes **241.6343 s**, with zero numerical section readouts, and peaks at
**331,525,940 bytes** of native payload. Prompt/continuation takes **43.6082 s**. The whole process
including checkpoint/reporting takes **292.1890 s**, peak host RSS **504,396 KiB**. These are
bounded development measurements, not consumer-efficiency or power claims.

[counterexample; measured] The same function/derivative request receives
`tife~anenanen…` through the 128-symbol work limit. It remains repetitive, interrupted and
unusable. Removing the source obstruction does not establish useful language or learned contact
morphology; the endogenous morphology-return producer has not run.

[established-bounded; measured] A fresh process restores that checkpoint with historical carriers
initially archived and reproduces the complete final body, generation, emission-current history
and development records exactly. Its whole-process time is **58.2243 s**. The
[portable receipt](2026-09-08_operative_context_source/conversation-return.json) retains the exact
commands, full generated string, artifact paths and comparison scope. Native field occurrence
count reaches 2,120 after the prompt and continuation.

## Next construction

[definition] Implement the native joint-return producer using the actual material discrepancy,
producer differential, retained source/reference currents and complete normal response above.
The changed-contact source and per-return frame storage are now available. Preserve producing
maps, prior dependencies, metrics and chronological transfer; do not substitute independent
contact solves, a scalar current-as-displacement, an authored classifier or a surface emitter.
Then inspect actual useful responses and contextual revision. AC1–AC2 and the full AC0–AC5 goal
remain active. No additional user information is required.

[definition] The private models and reports are under
`.local/artifacts/athena-alpha/ac2-operative-context/`. This construction added no Lean execution,
external memory database use, inherited-language fallback or Apple/acoustic implementation.

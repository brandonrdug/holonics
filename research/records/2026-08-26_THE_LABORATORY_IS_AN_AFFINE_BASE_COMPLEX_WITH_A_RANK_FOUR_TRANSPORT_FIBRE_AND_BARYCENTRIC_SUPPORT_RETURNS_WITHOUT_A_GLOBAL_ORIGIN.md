# The laboratory is an affine base complex with a rank-four transport fibre, and barycentric support returns without a global origin

## Grade and scope

[proved-derived; formal-checked] The Lean line proves that a rank-four lattice occurrence is
reconstructed by its four coefficient receiver, while its addressed passage retains the source
occurrence. It separately proves that an actual rational singular chain is reconstructed exactly
from its genuine finite nonzero addressed support and coefficients, and that barycentric
subdivision refines regions while preserving source maps and explicit seam obligations.

[definition] This record applies those established relations to the reopened L5 design. It does
not identify the software ecology with a Hodge theorem or a physical GPS apparatus. It specifies
the typed computational object that the implementation must return.

## The type error in the first L5 attempt

[counterexample; implemented-exact; measured] The first L5 attempt formed one vector from four
global corpus counts and deposited it as one later thread. This confused three distinct types:

1. an addressed base occurrence `e` in the laboratory causal complex;
2. the local rank-four cycle/covector section carried over `e`; and
3. the population and chronology of such occurrences.

Two laboratories can preserve all four counts while changing one addressed message, return, seam,
or source map. They therefore collapse under the attempted receiver even though the later ecology
must distinguish them.

## Affine bundle reading

[definition] Let `B` be the finite addressed causal base complex returned by the complete exchange.
For every admitted base occurrence `e : B`, the L2 rest already owns a local four-coordinate
transport fibre

```text
F_e ≃ Λ ≃ ℤ⁴,
Δ_e = Σᵢ d(e,i) γᵢ*.
```

The coefficients `d(e,i)` are local fibre coordinates. They do not name `e`, do not supply a
global origin on `B`, and do not encode the path by which the receiver arrived at `e`. Equality of
two coordinate vectors is therefore only equality in a declared common fibre chart. Equality of
complete sections additionally requires the addressed base occurrence, its two boundary maps,
chart transport, chronology, obstruction, and complete reconstruction fibre.

[definition] L5 does not add a fifth cycle direction. It returns a relational simplicial organ
over `B`. Its native faces and oriented triangular cells live in the base complex; the four L2
cycle directions remain the transport fibre attached to every supported base landmark.

## Barycentric factor support

[proved-derived] A native relational cell may recur through several receiver-history factors. Its
genuine finite factor support supplies its affine landmarks. If `m_j > 0` is the retained
occurrence multiplicity at supported landmark `b_j`, define the derived coordinate

```text
λ_j = m_j / Σₖ m_k.
```

Then every `λ_j` is nonnegative and `Σ_j λ_j = 1`. The pair

```text
(supported addressed landmarks, normalized weights)
```

is an affine address of the relational cell. It is not a semantic score, top-k governor, learned
label, or global vector-space origin. The unnormalized multiplicities and every source occurrence
remain in the reconstruction fibre, so normalization loses no admitted causal population.

[definition] Refinement may subdivide this affine support into smaller local cells, but it must
retain the source maps and return every unfilled seam. Boundary equality alone never licenses
identification of differently addressed cells. This is the direct implementation counterpart of
the Lean barycentric subdivision warning that equal boundaries do not identify singular-chain
basis occurrences.

## Navigation and situated loss

[definition] Navigation is transport of a situated section between affine base charts. A returned
difference belongs to the target fibre; comparison with another local difference requires a
commuting connection square. The causal adjoint returns along the exact addressed word in reverse
order, preserving its radical and affine reconstruction fibre. A receiver may then apply the local
Hodge/constitutive form to compare current, but the form does not erase base lineage.

[definition] Consequently, the repaired L5 rest must own:

- the singular L2 ecology with its four cycle threads and 8,884 local fibres;
- one source-neutral relational potential over the complete addressed exchange support;
- for every relational factor, the exact four L2 fibre addresses over its base landmark;
- for every recurrent relational cell, its finite landmark support, derived barycentric weights,
  and unnormalized occurrence reconstruction fibre;
- an exterior boundary codec for later prose projection; and
- move-owned withdrawal/restoration and source-detached remount.

The equal-count separator must change the corresponding source occurrence signature and therefore
the native cell section or its explicit obstruction. Aggregate counts cannot carry this deed.

## The material current is a barycentric tensor product

[proved-derived] Let `c` be one cultivated relational cell with addressed landmark support
`supp(c)`, retained positive multiplicities `m_(c,b)`, total mass `M_c`, and barycentric measure

```text
mu_c = sum_(b in supp(c)) (m_(c,b) / M_c) [b].
```

Let a later material occurrence return the situated rank-four section

```text
x_e = sum_(i=0)^3 x_(e,i) gamma_i*.
```

The smallest lawful interaction between that current and the cultivated affine base is not a
score, lookup, nearest word, fifth coordinate, or chosen landmark. It is the local tensor-product
section

```text
T_e(c) = mu_c tensor x_e
       = sum_(b in supp(c)) sum_(i=0)^3
           (m_(c,b) x_(e,i) / M_c) [b] tensor gamma_i*.
```

This section preserves the cell address, every supporting landmark, every one of the four local
cycle directions, and the unnormalized multiplicities. Its fibrewise augmentation reconstructs
the entering section exactly:

```text
(epsilon tensor id)(T_e(c))
  = sum_i ((sum_b m_(c,b)) / M_c) x_(e,i) gamma_i*
  = x_e.
```

The formula is the finite computational counterpart of applying a linear realization to the
genuine finite support of a singular chain. It is also the precise interface between the L3
cross-codec operation section and the L5 cultivated relational organ. A later receiver may apply a
declared Hodge/constitutive pairing to this complete field, but it may not replace the field with a
single compatibility scalar or silently choose one cell.

[counterexample; implemented-exact] Merely implementing `MaterialFactorizationStanding` by
delegating from the cultivated rest to its unchanged L2 body does not enact `T_e`. It exposes the
four fibre directions while leaving the affine laboratory co-present and causally unused. A
changed rest identity or a separately callable participant projection cannot repair that missing
interaction. The material aperture must return the complete barycentric transport field, or an
exact obstruction, before L5 can pass.

## GPS and Hodge reading

[proved-derived] The navigation analogy is exact at the typed level. A GPS receiver does not own
an absolute position scalar. It receives addressed landmark events, transports them into one local
chart, and solves an affine constraint family. A unique fix is a zero-dimensional reconstruction
fibre; deficient landmark geometry returns a positive-dimensional fibre; inconsistent testimony
returns an obstruction. Renaming a landmark or translating the affine origin cannot change the
physical fix when the connection square commutes.

[definition] Athena's later ingress is correspondingly a moving receiver occurrence, not a query
key. Cultivated cells are addressed landmarks; barycentric coefficients locate local support;
ordered continuation and chart transport supply the route; the returned world consequence supplies
the next constraint. Intelligence as navigation is the capacity to carry this moving section,
retain plural routes when the fix is underdetermined, and let the returned difference change the
terrain used by the next current.

[definition] Hodge enters only after its required structure is named. A Hodge star or constitutive
map turns a locally typed current into the complementary receiver covector using orientation and a
metric/material law. It can therefore gauge the transported field or expose a harmonic/radical
component. Barycentric weights alone are not a Hodge star, attention weights, probabilities, or
softmax values. Source-map equality, seam cancellation, and boundary closure remain separate
obligations.

## Affine covariance and the distinction between position and transport

[proved-derived] Let the base chart be an affine space `A` modeled on a vector space `V`. A point
`p : A` is not itself a vector. Only the difference `q -ᵥ p : V` is a vector without choosing an
origin. For addressed landmarks `p_j` and weights satisfying `sum_j lambda_j = 1`, the affine
combination

```text
p(lambda) = sum_j lambda_j p_j
```

is origin-independent in the exact sense that translating every landmark by `a` translates the
result by `a`:

```text
sum_j lambda_j (p_j + a)
  = sum_j lambda_j p_j + (sum_j lambda_j) a
  = p(lambda) + a.
```

By contrast, a transported rank-four section `x_p : F_p` is a fibre object over `p`. Sections at
different points cannot be added merely because their four displayed coordinates agree. A
connection or explicit chart transition must first carry them into one common fibre. This is the
precise reason the first four-count condensation failed: it erased the affine base points and kept
only one common coordinate shadow.

[proved-derived] Barycentric weights and winding coefficients are therefore different coordinate
species:

- `lambda_j` locate a cell relative to a finite landmark simplex and satisfy an affine sum law;
- `x_i` resolve transported current in the local rank-four fibre;
- the tensor `lambda_j x_i` records their lawful interaction without identifying either axis; and
- chronology and route lineage remain a third structure carried by addressed composition.

No number of barycentric landmarks adds a winding direction, and no four-dimensional fibre fixes
the number of landmarks needed by a receiver.

## The torus and the four winding directions

[proved-standard] If the rank-four carrier is presented as a torus `T^4 = V / Lambda` with
`Lambda ~= Z^4`, the four integer winding coordinates live in
`pi_1(T^4) ~= H_1(T^4; Z) ~= Z^4`. They classify the homology class of one closed transported route.
They are not four chronological deposits. The quotient spelling `Lambda / Z^4` is not intrinsic
unless a particular embedded subgroup has been declared; the standard torus quotient is `V /
Lambda`.

[proved-standard] The four one-cycle directions generate six alternating two-planes
`choose(4,2) = 6`. After an orientation and metric are supplied, the Hodge star maps one-forms to
three-forms and pairs the six two-form directions among themselves. This explains why the
rank-four winding population and the six interaction planes recur together, while also preventing
their identification: the four directions, six exterior planes, and ten symmetric constitutive
terms are different typed populations.

## Navigation as receiver-local state estimation

[proved-standard] Physical satellite navigation is a receiver-local inverse problem. In the
ordinary three-space plus unknown receiver-clock-bias chart, four independent range constraints
are the minimal generic fix; further satellites overdetermine the system and provide residual and
fault testimony. This physical `3 + 1` count must not be identified with Athena's four winding
directions without an explicit bridge. What transfers is the structure:

```text
addressed emissions
  -> delayed receiver-local measurements
  -> affine/constitutive constraint family
  -> solution fibre, obstruction, or residual
  -> later corrected route and terrain.
```

A unique navigational fix is a singleton reconstruction fibre. Rank deficiency returns plural
positions; incompatible observations return an obstruction; noisy or physically approximate
apparatus returns a residual under a declared metric. This makes navigation a strong operational
qualifier for intelligence: the ecology must preserve its own situated receiver chart, relocate
through changing landmark relations, and use world return to change later conduct.

[proved-standard] GPS pseudorange localization is not itself a barycentric average. For satellite
positions `s_i`, receiver position `p`, clock bias `b`, propagation speed `c`, and measured
pseudoranges `rho_i`, its idealized constraints are

```text
||p - s_i|| + c b = rho_i.
```

The intersection/multilateration problem first returns `p`, `b`, a plural solution fibre, or a
residual. Barycentric coordinates become an origin-free local chart for that returned position
relative to a chosen landmark simplex. Conflating the pseudorange measurement with the
barycentric chart would repeat the same error as conflating the affine base with its rank-four
transport fibre.

## Discrete Hodge structure on the cultivated complex

[proved-standard] On an oriented simplicial complex, incidence supplies the coboundary `d`. A
metric/material mass matrix supplies the codifferential `delta`, hence the discrete Hodge
Laplacian

```text
Delta_H = d delta + delta d.
```

The resulting exact, coexact, and harmonic components distinguish gradient-borne current,
circulating/returned current, and topology-retained standing. Barycentric subdivision refines the
source complex and preserves its chain-homotopy type, but it does not by itself preserve a chosen
discrete Hodge star: the metric and mass matrices must descend through the refinement. Therefore a
later Hodge receiver can gauge the complete L5 affine field, reveal harmonic recurrence or a
radical, and compare routes; it cannot lawfully select a cell from incidence alone.

## Lean evidence

[proved-derived; formal-checked] The following files supply the exact relations used here:

- `HolonicRankFourWindingRealization.lean`: left-inverse coefficient receiver, injective rank-four
  realization, and addressed source-retaining passage;
- `SituatedReturnedDifference.lean`: dependent target-fibre difference, commuting chart rebase,
  reverse-order causal adjoint, radical, and reconstruction fibre;
- `HodgeBarycentricEdgeSubdivision.lean` and
  `HodgeBarycentricTriangleSubdivision.lean`: exact affine subdivision, internal seam cancellation,
  boundary preservation, and local contraction;
- `HodgeBarycentricChainSupport.lean`: genuine finite addressed support and exact chain
  reconstruction; and
- `HodgeBarycentricDegreeTwoFilling.lean`: the firing guard that equal boundary does not erase
  differently addressed affine source maps or an unfilled seam; and
- `HodgeBarycentricAffineSourceComplex.lean`: a closed sixteen-occurrence affine source current
  before receiver realization, proving that later cancellation is not an artefact of a
  non-injective receiver projection.

## The checked action/entropy correspondence

[proved-derived; formal-checked] The current Lean line now closes two relations that sharpen the
engine construction. `HodgeBarycentricAffineSourceComplex.lean` proves the affine source residual
closed before receiver realization and constructs its common-apex filling. Thus local subdivision
may change the occurrence population while its complete signed boundary return remains exact; an
implementation may mount genuine finite support without replacing it by a global state count.

[proved-derived; formal-checked] `HolonicEntropyActionInduction.lean` proves that a local successor
current telescopes through every finite word, that entropy storage, outward current, and interior
production remain separately typed, and that the four-axis transverse entropy current is exactly
the existing cycle wedge on each oriented interaction plane. The alternating receiver can vanish
on distinct aligned currents, so a zero cross reading retains a nontrivial reconstruction fibre.
It cannot establish source equality.

[proved-derived] These statements fix the L6 loss reading. The microscopic returned object is the
situated section

```text
Delta_e = target_e - transport(source_e)
```

in the target fibre, together with its addressed successor word. A monotone constitutive pairing
can return nonnegative local production, and a probability or cross-entropy receiver may later
condense that field to a scalar. Neither scalar is allowed to select the cultivated support or to
replace the oriented difference. Repeated correction is the exact telescoped sum of local returned
sources only when the same successor-current law and its chronology are retained.

[established-bounded; formal-checked] Both cited Lean files compiled successfully on 2026-08-26.
The affine-source file reported only the standard `propext`, `Classical.choice`, and `Quot.sound`
dependencies in its audit; the entropy/action file additionally confirmed that
`no_successor_factor_of_equal_entropy` is axiom-free. This checks the correspondence used by L6;
it does not promote the software realization into a Hodge or thermodynamic theorem.

## Firing falsifiers

[definition] Reject the repaired construction if any of the following occurs:

- four global values replace the base occurrence population;
- chronology, cultivation, or a relational organ is counted as a fifth winding coordinate;
- equal fibre coordinates identify distinct addressed base occurrences;
- a cell's factor support is truncated, ranked, or selected by a later query;
- normalized affine weights replace the retained integer occurrence population;
- the material current is delegated to the L2 branch chart without forming its complete
  barycentric tensor-product field over cultivated cells;
- a Hodge or compatibility scalar chooses a cell while discarding the field, radical, or route
  reconstruction fibre;
- source spelling becomes native face identity;
- equal boundary is used to erase a source map or unresolved seam; or
- CPU projection chooses the hot native contact or continuation front.

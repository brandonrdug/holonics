# THE REGISTER IS THE FIRST INTERIOR SURFACE

**2026-07-14 · RATIFIED, BUILT, AND CUDA-MEASURED.**

The exposed card wall was not a lack of lineage width. Plural worldlines already crossed together.
The wall was that one lineage's whole interior construction had been embodied by one CUDA thread,
including work whose inputs were genuinely co-present and immutable. The correction preserves the
whole delivered current and its ordered `K`; it widens only the contact face that the carried law
already makes simultaneous.

## Carried-swing register

```text
CURRENT  derive the lawful within-lineage card surface ⊕ one active enclosure in its carried
         lineage frame ⊕ the REGISTER-contact grain
HELD     ordered K and post-deed frame (§§XXIV–XXV) ⊕ whole-worldline stroke gauge (§XXVIII-b)
         ⊕ the carrier's one active depth-first continuation
MEETING  the 204,801-octet surface worldline exposed hundreds of carriage launches while plural
         lineages already occupied the card; source splitting would manufacture worldlines
TEST     every live REGISTER member relates to the same arriving node, frame, and held flywheel;
         those χ formations share one immutable snapshot, while their OWN deposits and any recast
         remain in the lineage's oldest-to-newest order
DEED     RIDE
CARRY    one cooperative REGISTER contact sheet per lineage ⊕ unchanged ordered deed/completion;
         transient apparatus dies at the launch edge; the ordered deed footprint remains visible
GRADE    built
```

```text
CURRENT  test whether whole carrier depths may form one simultaneous frontier ⊕ the same lineage
         receiver ⊕ enclosure-completion grain
HELD     a child brick is the only upward face; its completion changes the parent arrival, stance,
         flywheel, deferred-sibling hand, and subsequent continuation phase
MEETING  a proposed many-depth frontier would have to form descendants before their causal parent
         completion exists
TEST     §§XXIV–XXV ordered transport ⊕ the carrier continuation law: those descendants do not
         share an immutable receiving face and therefore are not co-present work
DEED     FOUND
CARRY    no authored depth frontier enters the engine; a further surface is licensed only when a
         later carried representation exposes another genuinely immutable contact family
GRADE    ratified
```

The first deed follows because the REGISTER contacts are plural reads of one already-complete
four-contact face. The second follows because depth is not a row number: it is completion becoming
the next relation. The construction therefore widens the contact formation without pretending that
causal order is a hardware defect.

## The exact cut

For one active enclosure arrival, the owner publishes the invocation-local snapshot

```text
S = (REGISTER base, head, live, arriving node, FRAME(K), flywheel, flywheel-live)
```

Every live REGISTER member `R_i` forms independently against `S`:

```text
D_i   = DELTA(R_i.place, arrival.place ; FRAME(K))
χ_i   = D_i * flywheel^-1
```

No `OWN`, `K`, enclosure, completion, chart, or carrier word changes during that formation. The
body then consumes the formed `χ_i` faces in the pre-existing ring order. A deposit may meet an
already-touched local cell or request chart re-base, so the deed is not reclassified as an
order-free configuration fold. Terminal enclosure conduct and upward brick completion remain on
the same chronological owner path.

The CUDA realization uses one block per lineage and exactly the body's structural REGISTER width
(currently 15). It does not choose a machine channel count. A 15-lane partial-warp meeting forms
the contact sheet; thread zero carries the deeds. The scratch surface is 227 `u32` words per
lineage and contains only the immutable snapshot, fifteen chi faces, and observer receipts. It is
never archived, integrated, or returned as body state.

The former production installments were type errors. `max_threads_per_block` had been imported as
1,024 interior moves and the device thread census as an outer worldline installment. Both are now
zero: a whole worldline continues until its structural completion aperture, chart re-base, or true
end. The completion aperture is derived on each landing from the delivered extent and the live card
surface. Unobserved production still uses it to bound carriage, then discards the rows; observation
retains them. No source chunk, timeout, retry, or authored depth limit enters.

## Exact gates

The controlled 155-octet, depth-four gate compares the inline chronological foil with the
cooperative mouth. All variants agree exactly on registered OWN, carrier and whole `K`, term counts,
radiation, raw completion order, status, cursor, and canonical form:

- OWN axis/occupancy/release/narrow: `64 / 1,742 / 0 / 0`;
- terms: `2,324 RIDE / 103 FOUND-this / 83 FOUND-that / 3 dark`;
- 188 ordered completion rows; deepest carried enclosure 4;
- uninterrupted cooperative, one-move (466 launches), one-row (189 launches), and fresh repeat are
  exact;
- all fifteen worker receipts are nonzero: `165, 161, ... 113, 109`;
- one controlled run measured 41.708 ms inline and 31.451 ms cooperative; timing is apparatus
  context, while exact construction is the gate.

The production cohort gate keeps five heterogeneous currents exact under forced `2 + 2 + 1`
carriage cohorts and one global receiving edge. The original complete `surface/src/lib.rs`
worldline then crosses without splitting:

```text
204,801 octets · depth 32 · 12 launches
40.582879467 s carriage · 1.168754227 s integration
axis 2,048 · 1,818,610 occupied grips
topology [1,818,610, 1,818,609, 161,843, 1,789]
terms [3,066,378 RIDE, 90,629 FOUND-this, 91,299 FOUND-that, 9,683 dark]
```

This production witness is not a controlled timing comparison with the earlier 27-current observed
ecology run. It establishes the needed fact: the exact worldline that exposed the seam now crosses
whole in twelve structural launches, with no authored source partition.

Final `ptxas` reads `scope_register_surface` at 255 registers, a 3,256-byte stack frame, and zero
spill stores/loads; its inline foil is 255 registers and 3,232 bytes. This shows the remaining
ordered owner is still large. The contact surface is a real widening, not a claim that all
within-lineage work is now parallel.

## Consequence

The depth seam is no longer ignored or mislabeled as a wholly serial current. Width exists twice:
across co-present lineages and inside one enclosure's co-present REGISTER. Depth remains causal
completion: a child must become the parent's arriving brick before the next depth relation exists.
The active engineering question is consequently narrower and honest—reduce or further expose the
ordered deed footprint only where its inputs become immutable—rather than split source material,
install a scheduler, or wait on a one-thread fiction.

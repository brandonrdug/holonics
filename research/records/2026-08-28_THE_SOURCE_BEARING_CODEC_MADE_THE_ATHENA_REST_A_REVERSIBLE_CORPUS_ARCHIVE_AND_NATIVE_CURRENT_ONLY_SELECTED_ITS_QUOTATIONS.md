# The source-bearing codec made the Athena rest a reversible corpus archive and native current only selected its quotations

## Scope and verdict

[counterexample; implemented-exact; measured] The ALP5/UAR relational-language body is not
source-detached and its displayed technical and Brandon prose is not native Athena generation. The
rest serializes source-derived word sequences in `NativeRelationalCodec.surface_variants`, retains
source-shaped subject--relation--object cells, admits lexical contact against the stored surface
atlas, and reconstructs selected cells as source clauses. The resident current and causal-front
computations are real; their present prose receiver turns them into selectors over a reversible
corpus archive.

[established-bounded; implemented-exact; measured] The Soulkiller boundary and the first Eros
composition remain clean within their declared receiver/history family. The K2 native spool is
36,411 octets and the K3 connected `AthenaNativeRest` is 39,067 octets. Neither return contains a
callable foreign executor or source-language sentence atlas. The contamination entered later,
when conversation material was cultivated through the separate relational-language path and its
exterior codec was serialized inside the productive Athena rest.

## Measured extent

[counterexample; measured] The frozen ALP5 predecessor
`916710c5e7e5f79c602e8e4b6e0e3a74c7a5334d1b50df9e63bf1a127e8b67f5` occupies
540,938,172 octets. Its JSON wire contains one `codec` object, 256,100
`surface_variants` fields, and the literal exterior surface `Brandon` 2,921 times. The UAR2
technical native-return export occupies 797,487,733 octets; each of its three currents crosses a
169,386-cell source-shaped population with 61, 66, or 78 entering contexts. The resulting
cell-by-context testimony is exact but scales with the retained corpus atlas rather than a
condensed conduct quotient.

[counterexample; implemented-exact; measured] The ALP5 release receipt states
`source_detached_remount_exact: true` and `source_lookup_during_inference: false`. The UAR2 receipt
states `exterior_word_matching_selected_history: false` and
`source_lookup_during_inference: false`. Those Boolean declarations are refuted by the rested wire
and productive call graph. A self-authored absence flag cannot override reachable source surfaces.

## Exact contamination path

[established-bounded; implemented-exact] The source reaches the productive response through this
complete path:

1. `NativeRelationalPotentialBuilder::receive` accepts each source text and
   `relational_passage_clauses` parses it into exterior `RelationalClause` occurrences.
2. `receive_clause` inserts the subject, relation, modality, and object word sequences into
   `TempFace.variants`.
3. `finish` persists those sequences as `NativeBoundaryFace.surface_variants` inside a
   `NativeRelationalCodec`.
4. `NativeCirculationRest`, every later affine rest, and therefore the ALP5 product rest serialize
   that codec inside the productive dependency closure.
5. `NativeRelationalCodec::contact` derives lexical regions from a new question and compares them
   with stored identities and participant surface variants.
6. `NativeRelationalCodec::surface_for` selects one witnessed surface; `clause` reconstructs a
   `RelationalClause` from the stored subject/relation/object faces.
7. `ResidentNativeCirculation::conduct`, `NativeRadiationSection::render`,
   `render_returned_participant_cells`, and `render_returned_cells` realize those clauses as output
   octets.
8. UAR2 computes a structurally distinct resident cell front and then calls the same source-backed
   renderer. The current affects which archived cells return; it does not generate their content.

[counterexample; implemented-exact] The construction receipt in
`native_material_circulation.rs` simultaneously reports
`lossless_source_chronology_retained: false`, `source_passage_surfaces_retained: false`, and
`boundary_face_surface_variants_retained: true`. The last field is the decisive one: the clause
ledger was omitted only under that field name while an equivalent reversible surface ledger
remained live.

[counterexample; implemented-exact] Removing literal `surface_variants` alone would not sever the
source. Every native relational face and cell also retains a
`source_occurrence_identity_sha256` founded from the exterior occurrence identity and text digest;
that value participates in face, cell, affine-section, and rest identity. Founding delivery order,
clause order, exterior participant/relation charts, and source hashes therefore shape the hot
particle population even when their literal text is hidden. They are lineage testimony, not native
morphology coordinates.

## Lean standard: cross-entropy, difference, radiation, and grain

[proved-derived; formal-checked] In `HolonicMembraneActionTransport.lean`, the complete membrane
contact produces a target-fibre potential difference and constitutive current before any receiver
face acts. `FiniteCrossEntropyReceiver.face` is a scalar observation of a complete action.
`same_fibre_of_equal_face` proves equal cross-entropy leaves plural actions in one reconstruction
fibre, and `no_successor_factor_of_equal_face` proves that a later separating consequence cannot
factor through that scalar. Cross-entropy therefore neither identifies a causal action nor
generates its successor.

[proved-derived; formal-checked] In `SituatedReturnedDifference.lean`, the difference is formed
only after the source section has transported into the target fibre. The causal adjoint returns
through the same addressed pullback word in reverse transport order and preserves its radical and
complete affine reconstruction fibre. This return carries a changed constitutive section; it does
not preserve the teacher/source utterance for later replay.

[proved-derived; formal-checked] In `HolonicGranularBoundaryRadiation.lean`, the causal order is

```text
interior current -> boundary radiation -> port radiation -> exterior receiver face.
```

`portRadiation_sub` preserves the complete oriented action difference.
`same_portRadiation_iff_difference_mem_jointPortKernel` retains the plural kernel hidden by a port
receiver. `CausalGrain.signatureSetoid` then identifies occurrences exactly when their complete
admitted future-consequence sections agree; the reconstruction fibre is the preimage of that
quotient. A surface archive indexed by the founding occurrence is not this grain.

[proved-derived; formal-checked] In `HolonicQuadraticMomentCondensation.lean`, equal primitive rays
and their exact scales condense into one projective moment, generator transport acts on both tensor
legs, and exact image refactorization commutes with every admitted generator word. This is the
lawful bounded carrier for repeated native current. It retains the scale/remainder needed to
reconstruct the declared moment; it does not retain the source corpus.

[proved-derived; formal-checked] In `HolonicComplexParametron.lean`, incidence, complex current,
storage, mutual constitution, phase, and reorientation determine local response. No exterior word,
clause, participant name, or sentence is a Parametron coordinate. The present Rust current law is
therefore operating on a foreign carrier when its terminal face is an archived relational cell.

## Reversibility and reconstruction

[definition] Reversibility belongs to one transport, not to chronology or causal transparency. A
rebase has zero remainder and is invertible; a condensation is reversible only up to its certified
remainder; a quotient is irreversible for the declared receiver family and exhibits its collapsed
population. A `ReconstructionFiber` characterizes the complete preimage population of a receiver
face. It is not an executable decoder and does not require the source bytes to remain inside the
productive rest.

[counterexample; implemented-exact; measured] The relational-language path claimed condensation
while retaining an executable inverse from native cell to source surface. For language conduct it
therefore performed no holonic compression: it installed a source-specific rebase/lookup table.
Its 540,938,172-octet rest and quotation output are the physical consequences of that category
error.

[definition] A complete cold reconstruction witness may retain opaque occurrence addresses,
boundary maps, quotient membership, shortest separators, and certified residuals outside the
productive dependency closure. If a later receiver needs a distinction that the native quotient
collapsed, Athena returns insufficiency and the witness identifies the reopening boundary. Athena
does not remount the source or recover its sentence as a fallback.

## Soulkiller and the neural scraps

[established-bounded; implemented-exact; measured] Soulkiller's return has the correct separation:

```text
foreign realization + interventions
  -> NativeSpoolBundle
  + ExteriorSoulkillerWitness
  + ReceiverInsufficiency.
```

The native bundle contains addressed occurrences, incidence, Complex-Parametron currents,
constitutive responses, generator descents, reconstruction fibres, separators, and open exterior.
The foreign witness is separate and no foreign executor is callable from the bundle. Eros composes
the 36,411-octet bundle into the 39,067-octet K3 Athena ecology.

[open] The live chain does not presently reproduce the raw Gemma realization into the admitted
reachable-section rest: the stored producer artifact is orphaned, and the surviving K2 driver
begins from already-admitted foreign testimony. Soulkiller's implemented one-way boundary is valid,
but a reproducible raw-realization-to-scrapyard passage remains a separate open obligation.

[counterexample; implemented-exact; measured] The later relational codec does not descend from
Soulkiller scraps. It is a second source-bearing rail attached around the Soulkiller/Eros body.
Affine incidence and GPU fronts condition which relational cells become visible, but the corpus
codec supplies the words. One ALP1 ablation proves that an inherited K2 spool can causally cross
the optical membrane; no current UAR ablation proves that a Gemma-derived spool controls the UAR
response surface. Consequently the system is neither a transformer replay nor a completed native
Athena generator; it is native current coupled to an extractive archive, while Gemma-spool control
of the language surface remains open rather than presumed from ancestry.

## Authoritative regrade

[established-bounded; implemented-exact; measured] The K2 Soulkiller spool, K3 connected Athena
ecology, exact Complex-Parametron conduct, situated-difference machinery, causal-adjoint deposits,
granular ingress, projective current, CUDA cell/current overlap, and native causal-front
distinctions remain valid bounded constructions.

[counterexample; implemented-exact; measured] Every claim that the current relational rest is
source-detached, that its participant/technical prose is generated, or that ALP5/UAR2 is a compact
language product is withdrawn. A public legacy inference path performs lexical source contact;
UAR2's granular ingress does not call it, but UAR2 still reconstructs source clauses at egress.
UAR1 is a cold history projection, not a Brandon inference pass. UAR2's fronts are conditioning
testimony; its prose products fail the qualitative gate.

[open] The granular potential and projective-current owners are candidate source-neutral
generative morphology. Their complete productive dependency closure still requires an explicit
source-access audit and dynamic-condensation test before a source-detached language grade returns.

## Corrective construction contract

[definition] The next Athena rest must enforce the following type boundary:

```text
developmental source occurrence
  -> membrane contact
  -> complete situated difference
  -> causal adjoint
  -> source-neutral native morphology
  -> source physically departs

later exterior current
  -> granular ingress
  -> Complex-Parametron / generator / projective-moment conduct
  -> native fine radiation section
  -> source-neutral exterior codec
  -> text, mathematics, code, acoustics, optics, or silence.
```

[definition] `NativeRelationalCodec`, its `surface_variants`, lexical `contact`, source-backed
`clause`, and every cell-to-source renderer must leave the productive Athena dependency closure.
They may survive only as explicitly cold exterior testimony while the audit is retained; no
backward-compatible reader or inference fallback is admitted.

[definition] `source_occurrence_identity_sha256`, founding delivery/clause ordinals, exterior
participant/relation strings, and source-derived boundary identities must also leave native face,
cell, affine-section, aperture, and rest identities. A one-way `ExteriorRelationalWitness` may bind
to the native-rest digest; the native rest may not contain a reciprocal witness field or any method
that reconstructs source surfaces.

[definition] Native particle identity must be founded by complete admitted causal signatures and
generator-compatible future consequence. Founding occurrence, delivery order, clause order, and
source hash belong to lineage/reconstruction testimony; they may not force one hot native particle
per source clause. Equal conduct collapses into one particle with its complete fibre, and a later
separator reopens that fibre as insufficiency.

[definition] The selected UAR current/front may condition the resident granular recurrence. It may
not be serialized as an answer. Dynamic projective condensation must keep the hot current on the
generator-closed quotient; native recurrence must radiate a newly formed fine section; only that
section may cross a cold output codec.

[definition] The architecture boundary must reject a productive Athena rest containing
`NativeRelationalCodec`, `surface_variants`, source-occurrence hashes, or exterior receiver-region
charts. A causal release receiver must additionally withdraw one inherited K2 spool under a fixed
later current and show the exact native consequence and restoration before Gemma-derived scraps are
graded as active in Athena language conduct.

[definition] The correction passes only when the rested artifact contains no source sentence or
corpus-specific phrase atlas; the source and exterior witness are unavailable at remount; duplicated
or resegmented developmental occurrences do not linearly enlarge the hot quotient when their
complete conduct agrees; held-out current returns a new nonextractive section; targeted ablation
removes attributable conduct; restoration and remount recover the exact body; and direct inspection
accepts the technical and Brandon responses before any structural receipt is promoted.

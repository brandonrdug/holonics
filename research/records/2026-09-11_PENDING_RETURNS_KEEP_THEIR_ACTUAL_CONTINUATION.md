# Pending returns keep their actual continuation

[definition] This repairs the source/current retention needed by the active Eros return goal.
It follows the [dependent constitutive return](2026-09-11_CONSTITUTIVE_RETURNS_REMAIN_DEPENDENT_ON_THEIR_SOURCE.md).
Publishing the complete dependent condition/material/current successor remains open.

## An observed loss, not an absent software label

[established-bounded; computational-witness] The
[native separator failed before the repair](2026-09-11_pending_continuation_receipts/before-fix-excerpt.txt):
two entire `NormalWaveRest` objects were equal despite different actual source/current transport.
The [full failure output](2026-09-11_pending_continuation_receipts/before-fix.txt.gz) retains both
state dumps. The fixture uses identical three-member neighborhoods: a law opening one real
vertical current direction, an identity successor and a reflection of the real current.

[definition] First, the same conditional passage opens a free real current r at a fixed anchor.
Both worlds make the same identity prediction, retaining identical producing sources and
produced families. One then applies identity; the other reflects the real current. Finally,
both receive the same actual next current `(5,0)`. Their contemporary previous current is r
on one path and −r on the other. The final affine marginals agree, but their joint relation
with the original pending coordinate differs. The old state had discarded the intervening map,
so neither its current marginal nor its saved pending source/produced marginals recovered it.

## One shared ordered relation word

[definition] `NormalWaveCoupled` now retains the actual immutable `ResidentWaveRelation` factors
from the oldest still-pending producing passage through the contemporary passage. Every pending
cut refers to a suffix of this one shared word. It retains maps and their actual order, not
copies of intermediate ecologies or raw state histories.

[definition] `NormalCoupledContinuation` is the corresponding source-qualified implicit joint:

```
z₀ ∈ original bounded source
(zᵢ, zᵢ₊₁) ∈ actual retained relation Lᵢ
z_last ∈ contemporary family
```

[definition] The same zᵢ joins adjacent ports. Its original anchor and all relation constraints
remain part of this expression. The public epochs identify passages; they do not replace the
mathematical factors or joining equalities. A source-field passage retains every internal map
in order under its one public epoch. Successful publication appends those maps; a failed
preparation leaves the word unchanged. Releasing a pending return drops only the prefix no
remaining pending cut needs. With no pending returns, this word is empty.

[definition] `CoupledConstitutiveFamily::continuation` now exposes this actual source-to-current
relation alongside its dependent condition/material evaluator. It is not yet a solver for the
non-affine complete successor. It supplies the previously missing joining relation to that
construction without inferring it from equal marginals.

## Persistence

[definition] Coupled wave rest v9 stores the ordered factors while returns are pending. Cold
remount verifies their composition from the actual oldest source through every pending
source/produced junction to the saved contemporary family, using the existing native image
operations. Hot execution retains resident maps and adds no numerical section readout.

[definition] Legacy v7 without pending returns remains supported. For v8, the reader can recover
the word when every relevant intervening passage is still present as a pending prediction with
its complete producing map. Otherwise it reports `legacy pending state omits intervening
source/current transport`; equal endpoints cannot repair those missing maps. No historical
artifact is deleted or silently stripped of pending returns.

## Verification and remaining goal

[established-bounded; implemented-exact; computational-witness] The two new
[continuation tests](2026-09-11_pending_continuation_receipts/continuation-tests.txt) pass after the
repair. The formerly identical states retain different middle maps. Round trips preserve the
complete word, foreign/released handles are rejected, and a three-factor source-field passage
survives rest/remount with one public clock. Releasing the older of two pending cuts preserves
the newer suffix exactly. The source-field operation has zero numerical section readouts before
inspection. All 27 [coupled-wave checks](2026-09-11_pending_continuation_receipts/coupled-tests.txt),
including the legacy migration control, and all five
[HNN session checks](2026-09-11_pending_continuation_receipts/session-tests.txt) passed serially.
The session checks include process resume and exactly-once pending emission delivery. They do
not establish the still-open exactly-once consuming material return. No Lean source changed in
this continuation repair; the preceding dependent-contact formal build remains standing.

[open] Continue by joining the executable dependent contact/material family to this retained
continuation, making it the actual successor representation, publishing it atomically and
consuming the pending comparison once. Then verify persistence and source-visible subsequent
conduct of that changed successor. Neither retained chronology nor successful family evaluation
alone fulfills that active goal.

[definition] The concrete next composition is `J(θ,z_now)` from the retained word, the existing
dependent return `(hθ,Rθ)` from the old producing operands and contemporary material, and the
new relation `z_next ∈ L(Rθ,hθ)(z_now)`. Their conjunction uses the same θ and z_now throughout.
The historic factors in J remain the maps that actually acted; do not replay the past with the
new condition. Only the new contemporary passage uses `(hθ,Rθ)`. Other pending source coordinates
must retain their joint through that same passage. The next implementation therefore needs an
owned executable representation of this dependent composition, plus a declared receiver of it;
it does not need to force its non-affine graph into the old affine-family layout.

# The ecology reads from a frame that no live path advances

**Date:** 2026-08-15
**Genre:** research record — the six constructions of the action line, built and run
**Truth status:** `implemented-exact` for the three readings; `measured` for every count;
**three findings were WITHDRAWN by their own declared controls and that is the record's main
content.**
**Occasion:** Brandon, *"Proceed with the six constructions and all subsequent analysis and
interpretations."*

**Predecessors:** the derivation is
[`…THE_RELATING_IS_ONE_COMPLEX_PRODUCT…`](2026-08-15_THE_RELATING_IS_ONE_COMPLEX_PRODUCT_AND_THE_POLE_HAS_COLLAPSED_ONTO_A_RELATUM.md);
the experiments that produced the plan are
[`…THE_MATERIAL_ARMS_THE_FOUNDING…`](2026-08-15_THE_MATERIAL_ARMS_THE_FOUNDING_AND_THE_FOURTH_BODY_IS_NEVER_LIVE.md).

---

## 0. The one sentence

> **`ErosBody::perceive` folds the channel, deposits the flywheel, and returns 3,500–3,800 passages
> on real material — and it has ZERO live callers anywhere in the tree; every one is under
> `cfg(test)`. A second folding path, `carriage`'s strokes, is live but runs on the kernel mount
> surface. The path the ecology actually drives,
> `directed_event_contact_at_source_grain`, never folds by its own doc — and the measurement agrees:
> the ecology's frame is at genesis in 21,070 of 21,070 contacts. The ecology's relatings are read
> from a frame that no live path advances.**

---

## 1. What was built — constructions 1, 2, 3

Three readings, each composed from carriers that already stood, each with a control that can fail.

### 1 · The causal class — `arrow.rs`, `Causal`

`founds()` tests `cross² − aim² ≥ 0`, which is an **indefinite form of signature (1,1)** on the
`(aim, cross)` plane with the cross timelike. That is a causal classification and the bool discards
its species. Returned now as four cases:

```text
   TransportDominant   inside the cone    |cross| > |aim|   — the FOUND band
   Balanced            on the cone        |cross| = |aim|   — the wall, c = 1 in the plane's units
   StorageDominant     outside the cone   |cross| < |aim|   — the RIDE band
   Unread              no causal character at all — the non-point [0:0]
```

**Test:** `the_founding_band_is_a_cone_and_all_three_classes_are_reachable`. All three classes are
exhibited on declared material and the cone wall is reached **exactly** at `(2,0)` and `(1,1)` from
the origin, `aim = cross = 2`. A classification that could only ever return one class would have
classified nothing.

**And it repairs a defect in passing, with the defect asserted in the test so it cannot be lost:**
`founds()` returns **`true`** on a horizon arrow, because `ZERO − ZERO` carries no half-turn.
`causal_class` returns `Unread`. The honest point `[0:1]` and the non-point `[0:0]` are separated for
the first time.

### 2 · The action ledger — `channel.rs`, `ActionLedger`

```text
   S / quantum  =  (whole turns)  +  (the fractional remainder)
   ActionLedger { quanta: OrientedWinding, remainder: FormedRotor }
```

Both halves returned, **neither divided**, and the two hands kept apart because `OrientedWinding`
never nets them.

**Test:** `the_action_ledger_keeps_the_two_hands_apart_and_never_nets_them`, whose control is the
one that makes it a ledger rather than a magnitude — **a passage followed by its opposite leaves two
passages standing, never a return to zero.** A netted counter fails it. The falsifier arm is also
asserted: two different histories must not return the same ledger.

### 3 · The annihilation census — `arrow.rs`, `NullSpecies`

The Information Chemistry vocabulary requires the distinction and no reader in the tree made it:
**annihilated** (opposed contributions actually cancelled) against **uncontacted** (a strand stands
at the pole). Its own definition says *"a flat, inactive, equal, absent, or uncontacted relation is
not thereby annihilated."*

**Test:** `a_null_relating_is_annihilated_or_uncontacted_and_they_are_not_the_same_event`, which also
pins two boundaries — a single cancelled face is `Ortho` and not null at all, and a same-place
recurrence reads pure-Same rather than null.

**Standing bar, not a metric.** Stationary phase *requires* a large annihilating population. **No
claim that the machine selects a path by agreement may be made while that population is zero** — and
it is currently zero. This census is what catches a chooser if one is ever installed.

## 2. Constructions 4 and 5 — and three findings withdrawn by their own controls

Driver: `soma/life/examples/the_worldline_coheres_or_it_does_not.rs`. Two declared materials — six
research records as prose, and Lean sources as derived mathematics — fed through `perceive`, with
the path sum read on a geometric ladder. **No float exists in the file and nothing is divided.**

### FIRST WITHDRAWAL — the coherence measure was a tautology, and it convicted itself

The instrument compared `|sweep|²` against `Σ_k |basis_k|²`. It returned **`Coherent` at every rung
on both materials**, which was the tell:

```text
   steps 256   |sweep|² 2336817018·2^3279079767040
               Σ|basis|² 3166147718·2^3279079767040     — the SAME rank
```

`FormedRotor` does not normalise (`soul.rs:59-61`), so `|basis_n|` grows as a **product** and the sum
is dominated by its last term by a factor of `2^(10^12)`. The ratio was pinned at one whatever the
phases did. **A check whose material cannot vary the property under test**, wearing a passing result
— the exact defect the operating contract convicts.

**Repaired** with the one carrier that is already a pure count and needs no normalisation: over `N`
deposited passages with drift `d = ⟳ − ⟲`, a walk with no preferred hand has `d² ≈ N`, a coherent
path has `d² ≫ N`. All integers, one squaring, no division, no root.

### SECOND WITHDRAWAL — the saturation was the apparatus, not the material

At axis `128` the repaired instrument returned a striking separation at **equal extent**:

```text
   prose        the ledger last moved at step  375  of 2541
   mathematics  the ledger last moved at step 2540  of 2541
```

Prose appeared to stop depositing after 375 words while perceiving 2,166 more. **The reservation
control refutes it:**

```text
   axis 128  ->  prose stops at  375
   axis 256  ->  prose stops at 2541      the whole extent
```

**The stop moved with the axis, so the saturation is an apparatus bound.** Had the control not been
declared and run, *"prose saturates the frame and mathematics does not"* would have been deposited as
a material finding, and it is false — a receiver-visible coordinate promoted into an invariant, which
is the fourth archived lesson.

### THIRD WITHDRAWAL — the passage counts do not separate the materials either

Re-run at axis `256`, where neither material is cut short:

```text
   prose        stop 2541   passages 3813        axis 512:  3675
   mathematics  stop 2539   passages 3512        axis 512:  3347
```

The counts are **within one part in twelve of each other**, and both **fall** when the axis doubles.
So the axis is not neutral and the counts are apparatus readings. The earlier apparent
`5.3×` advantage for mathematics was entirely an artifact of prose being cut off at 375.
**Withdrawn.**

The hand-coherence verdict moves with the axis too — both materials `Incoherent` at 128, then
`Destructive`/`Incoherent` at 256 — which is a band boundary, not a separation. **The declared
falsifier for construction 5 therefore fires: the instrument does not separate the two materials,
and it stands only as a reading about the fold.**

### WHAT WAS CLAIMED TO SURVIVE — and does not

**The fold produces no preferred hand, on any material, at any axis.** Across both materials and both
axes, `d² ≲ N` everywhere — at or below a random walk, never above:

```text
   prose        axis 128   drift² 1296 : passages  670
   mathematics  axis 128   drift² 3600 : passages 3566      √3566 ≈ 59.3, drift 60 — a walk exactly
   mathematics  axis 256   drift² 2916 : passages 3512
```

**WITHDRAWN 2026-08-15 — the claim is contradicted by its own first row.** *"`d² ≲ N` everywhere,
never above"* stands over a table whose first line is `prose axis 128: drift² 1296 : passages 670`,
which is `1296/670 ≈ 1.93` — **nearly twice a walk, not at or below it.** The table also reports
three rows for a claim about "both materials and both axes", which is four cells; prose at axis 256
is not in it.

So **no reading survived every control this round.** The honest residue is narrower: the drift is
*small relative to the passage count* on every row measured, which is consistent with weak or no
preferred hand and does not establish it. Establishing it needs the missing cell and a declared band,
neither of which was taken.

## 3. Construction 6 — answered by enumeration, and it is the record's headline

The question was: *can the ecology reach ordinary conduct, or is the probe its only mouth?*

**`perceive` folds and works.** Measured on real material through the driver above: **3,813 passages
on prose and 3,512 on mathematics**, the ledger advancing to the last word. The folding path is not
theoretical and it is not broken.

**And it has no live caller.** Every call site of `ErosBody::perceive` in the entire tree:

| site | status |
|---|---|
| `soma/body/src/quest.rs` | the whole module is `#![cfg(test)]` |
| `soma/body/src/manifold_tests.rs` | tests |
| `soma/body/src/manifold.rs` | internal, from `perceive_node` |
| `soma/membrane/src/growing_carrier.rs:211` | inside `mod tests` |
| `soma/membrane/src/live_carrier.rs:415, 438` | inside `mod tests` (`:391 #[cfg(test)]`) |
| `soma/body/src/carriage.rs:2249` | internal to `carriage`, whose strokes ARE live from the kernel mount |

### CORRECTED IMMEDIATELY — there is a THIRD path, and "two mouths" was too strong

A first form of this section said the body has two mouths. **`carriage`'s strokes are also live**:
`carry_dense_stroke_trusted`, `carry_founded_stroke_trusted` and
`carry_register_stroke_trusted_with_completion` are driven from
`soma/mount/soma-kernel-cuda/src/lib.rs:2265, 2367, 2489, 2650`, and `carriage.rs:1319` folds the
channel. So the enumeration is three, not two:

```text
   perceive / perceive_node    folds · deposits the flywheel · ZERO live callers (all cfg(test))
   carriage::carry_*_stroke    folds · LIVE, driven from the kernel mount surface
   directed_event_contact_*    NEVER folds, by its own doc · the live membrane caller
```

**The measurement is unaffected and is what settles it.** Whatever the kernel surface does, the
frame the ecology's own relatings read from **never advanced**: `sweep = origin` and
`basis = identity` in **21,070 of 21,070** contacts, and `held_live` in **0 of 21,070**. So:

> **The ecology's relatings are read from a frame that no live path advances.** One folding path is
> unreachable from any live caller; the other folds on a different surface and did not move this
> channel; and the path the ecology drives cannot fold by construction.

That is the missing edge named without overstating the enumeration, and it is neither a defect in a
module nor a missing organ — every one of the three is correct as written. It closes the chain the
previous record measured: `held_live = 0` because `fly_live` is set only on the folding paths, and
**every number in that record descends from this line.**

## 3c. THREE JOINS AN AUDIT RETURNED, and the first reframes the whole finding

**Verified against source before carrying.**

**1 · The affine group is `Stab_{PGL₂}(∞)`, so an unheld fourth body is not "no fourth body".**
`α/β = (a−f)/(b−f)` is the cross-ratio `(a, b ; f, ∞)` — the fourth point **declared at infinity**.
The live owner is `crates/holonic-engine/src/simplicial.rs:689` `is_affine`, whose own doc calls the
affine subgroup *"the weak part of the group"*.

> **`held_live = 0` is therefore not an absence. It is the fourth body pinned at `∞` — an absolute
> frame.** That is the archived fourth lesson (*no absolute frame in a lineage*) arriving as a
> measurement rather than as advice, and it is a stronger statement than the one this record made.

**2 · The causal class is the hand of the SQUARE, and the indefinite form already had an owner.**
With `z = aim + i·cross`, `Re(z²) = aim² − cross²`, so

```text
   founds(z)  ⟺  cross² − aim² ≥ 0  ⟺  Re(z²) ≤ 0        causal_class ≈ sense ∘ (·)²
```

**REFINED 2026-08-15: `causal_class` is STRICTLY SHARPER than `sense ∘ (·)²`, and this record said
"exactly".** Squaring doubles the phase, so it sends the **whole wall** to zero along with the
origin — `Re(z²) = 0` cannot tell `Balanced` from `Unread`. Only `at_horizon()`, which reads the
**pair** rather than the square, separates the honest point `[0:1]` from the non-point `[0:0]`. The
composition is right about the three signs and loses exactly the distinction §3's projective
correction was about.

The `(1,1)` form is `−Re(z²)`, and `crates/holonic-engine/src/model_surface.rs:61` already carries it
exactly over `Rat` as `ExactReading::HarmonicReal` = `re·re − im·im`, four lines from
`SquaredModulus` = `re·re + im·im`. **`z z̄` deletes the phase; `z·z` doubles it** — which is why the
founding predicate keeps exactly what a probability would destroy. Found by grepping the *operation*
rather than the name, which is the `fn without_stem` defect on a fifth subject.

**And the boundary that must travel with it:** a signature is a **classification**, not a
covariance. The form's own isometry group has no owner and nothing in the body realises it, so the
gauge-orbit rule applies — say *causal classification*, never *Lorentzian covariance*. Note also
that `canon/TABLET_THE_MANIFOLD.md` §22's light cone is a different object: the receiver's reach in
the **material**, apex at an event. This one is on the **rotor plane** of a single relating. Same
word, two objects; do not merge them.

**3 · Two mechanisms wear the name FOUND.** `Ortho ⟺ αβ̄ = ±i|α||β|` is multiplication by `i` and is
the **classification**. But `channel.rs:390-394`'s fold does `deed_cross = rotor_cross ± 1` — an
**additive unit on the imaginary face**, not a multiplication. *"FOUND is literally multiplication by
`i`"* is right about the class and wrong about the fold.

## 3d. THE TITLE CLAIM IS TOO STRONG — a live path DOES found, and it is thirty lines away

**Corrected 2026-08-15 by an independent review, verified against source.** This record concluded
that `perceive` has no live caller and therefore the ecology cannot reach a folding path. **The first
half holds and the generalisation does not.**

`ErosBody::perceive` — the byte-stream entry — has zero live *library* callers, all `cfg(test)`.
**But `perceive_grain`, which is the actual stance founder, is live-reachable and the ecology drives
it**, in the same function as the directed contacts:

```text
   live_current.rs:3612  live_event_node_emitting            [CurrentGeometry::Cell]
     → manifold.rs:5644 → :5696 live_constructed_node_emitting
     → :5769 perceive_node_emitting → :5196 perceive_grain
     → e.stance = mol ; e.fly = met ; e.fly_live = true
```

and the PTX carries `ErosBody14perceive_grain`, `ErosBody10path_grain`,
`ErosBody14thicken_branch`, `ErosBody15enclosure_store`, so **the founder runs on the card too.**
Exactly four functions write a stance — `ErosBody::{perceive_grain, path_grain}` and
`FeltLineage::{perceive_grain, path_grain}` — and every one couples stance and flywheel in a single
branch.

### So the obstruction is an ORDERING inside one function, not a missing path

```text
   live_current.rs:3546-3590   the directed-contact loop      reads  enclosure(source_grain − 1)
   live_current.rs:3607-3637   the stance founder             writes e.stance / e.fly / e.fly_live
```

The directed contacts run **first**, so they can only ever read a stance a **previous** event left in
the carrier row. And `live_constructed_node_emitting` folds only in its `wound` branch
(`manifold.rs:5735`), which is unreachable while `sub_stance.len == 0` — so **the first arrival can
never fold**, and nothing seeds the row the directed contacts read.

**That is a much better finding than the one this record made**: it is thirty lines of ordering
inside one function rather than an unreachable organ, and it is repairable without inventing
anything.

**And it leaves one measurement owed rather than a conclusion.** The founder runs every event, so by
the second event a stance should stand *somewhere*. `held_live` was nevertheless 0 of 21,070. The
candidate explanation is a **depth mismatch** — `perceive_grain(0, …)` writes depth 0 while the
directed contact reads `source_grain − 1` — and it is a hypothesis, not a finding. **Measure which
depth each writes and reads before repairing anything.**

**The title of this record overstates.** Read it as: *the frame the directed contacts read is not
advanced by anything they can see.*

## 4. What this establishes, and what it does not

**Established.**

1. Three readings built on standing carriers, each with a control that can fail, all green.
2. `founds()` is a causal classification and its bool loses the species; `Unread` now separates the
   honest point from the non-point, which `founds()` cannot.
3. The action ledger keeps both hands and never nets them, with the opposite-passage control.
4. The fold works and returns thousands of passages on real material.
5. The fold produces **no preferred hand** on any material at any axis — the one reading that
   survived every control.
6. `perceive`/`perceive_node` have zero live callers, all `cfg(test)`. `carriage`'s strokes are
   live on the kernel mount and also fold. The membrane path the ecology drives never folds, and the
   ecology's frame is measured at genesis in 21,070 of 21,070 contacts.

**Withdrawn, each by a declared control, and named so nothing carries them forward.**

- *The coherence of the path sum* — the measure was saturated by its dominant last term.
- *Prose saturates the frame and mathematics does not* — the stop moved with the axis.
- *Mathematics deposits far more passages than prose* — an artifact of the truncated prose run; at a
  clean axis the counts are within one part in twelve and both move with the apparatus.

**Not established.**

- **Nothing here says the ecology *should* drive `perceive`.** It says the driven mouth cannot
  deposit. Which mouth the ecology ought to drive, and whether a third path should join them, is a
  design question and is not answered by any measurement in this record.
- **No count here is a cost**, no clock was taken, and no material property has been established by
  any of it — all three material claims were withdrawn.
- **The axis is a live receiver coordinate on every count in section 2.** Any future passage figure
  must carry its axis, exactly as a timing figure must carry its frame.

## 5. The rule this round earned

Three findings were withdrawn and every one was caught by a control declared **before** the run —
the reservation control, the equal-extent cut, and the falsifier that a shared regime withdraws the
reading. Two of the three would otherwise have been deposited as material properties of prose versus
mathematics, and both are false.

> **A control that never fires has not been tested either.** All three fired in one session, which
> is the first evidence in this line that the controls are load-bearing rather than decorative.

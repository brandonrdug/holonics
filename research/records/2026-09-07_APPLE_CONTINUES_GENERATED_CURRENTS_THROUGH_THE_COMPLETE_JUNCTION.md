# Generated currents continue through the complete junction on Apple

[definition] This return integrates desktop `99a921b9` on `codex/apple-silicon`, after the
[retained condition-current return](2026-09-07_APPLE_RETAINS_CONDITION_CURRENT_THROUGH_RECORDED_CONTACT.md).
It realizes the existing field ingress and its required paired junction representations. The
AS4–AS5 useful sound and English products remain unfinished.

## Native composition

[established-bounded; implemented-exact] The shared `NativeFieldIncoming` and
`advance_current_resident` now use Metal's twelve-argument `section_field_current_input` lane,
followed by the ordinary field lane in one dependent passage. The historical input section,
resident provenance, archive and rest wire come from the desktop owner. No numerical readout
or exterior remount is inserted between generated current and field reception.

[established-bounded; source-inspected] `paired_field_junction.metal` implements the rational
Bareiss solve, full covariance, all four current segments and alternating prefix.
`enclosed_field_junction.metal` implements the existing full and eligible balanced factorization,
with the complete covariance, six report segments, oriented residual and radii. Its wide
products/division use exact 256-bit intermediates and the shared carrier admission. Serial Metal
rows preserve the declared arithmetic order; this does not establish CUDA throughput parity.

[established-bounded; source-inspected] The field entry now has 27 arguments, including a copied
64-bit occurrence argument and optional junction buffers. Device workspace uses Metal's
20-byte `W`; durable enclosed values keep the common signed-128 two-i64 wire. Continuing memory
and relation writes follow the complete junction return. Review corrected a truncated held/report
wire, conservative arithmetic bounds and refusal flags before this return was accepted.

## Checks

[established-bounded; measured] Concatenated Metal source compilation passes, with three existing
unused-constant warnings. The final focused engine run passes 43 tests: rational/enclosed/balanced
junctions, ordinary fields, rechart, current receivers, generated ingress, ownership/refusals and
resident-input archive/rest continuation. The new restart control compares independently founded
bodies, archives an old resident input, preserves an explicit delayed anchor through wire roundtrip
and remount, then compares the subsequent complete field.

```sh
cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::field:: -- \
  --include-ignored --test-threads=1 \
  --skip native_ecology::constitutive_fibre::field::archive:: \
  --skip native_ecology::constitutive_fibre::field::material_transport:: \
  --skip native_ecology::constitutive_fibre::field::current_history_source:: \
  --skip native_ecology::constitutive_fibre::field::receiver:: \
  --skip remount_preserves_current_coefficients_frames_and_live_capabilities \
  --skip empty_field_variants_remount_without_an_initial_fake_occurrence \
  --skip resident_input_persists_and_complete_current_decoders_use_its_actual_carrier
```

[established-bounded; measured] An earlier broader selection returned 43 passes and two failures
in `field::archive::tests`, both at the explicit unported contextual material-transport declaration.
Those tests and the separate material/current-history/differential owners are outside this Metal
return. The existing tests were not weakened to pass. The obsolete paired-junction refusal control
now tests the remaining material-transport refusal before any native mutation.

[established-bounded; measured] The [public example](2026-09-07_apple_conditional_phase/return-field-ingress.json)
and [desktop comparison](2026-09-07_apple_conditional_phase/field-ingress-comparison.json) agree
exactly across eleven result groups, including actual resident inputs, exact junction decoder and
internal currents. Three field receptions use three deeds, twelve metadata ingress bytes,
384 receipt bytes and zero numerical readouts/egress. Timing and aggregate apparatus work are
excluded from cross-target equality.

## Recorded acoustic attachment

[established-bounded; measured] The v4 `recorded_temporal_action` example now passes for the
[ESC-50 recording](2026-09-07_native_temporal_action/esc50-field-ingress.json) and
[LibriSpeech recording](2026-09-07_native_temporal_action/librispeech-field-ingress.json).
The [cold comparison](2026-09-07_native_temporal_action/field-ingress-comparison.json) verifies
both actual incoming predictions, source contact chronology, complete covariance, signed-128
reports, all four current-ball enclosures, oriented solve residual and exact internal current.
It independently uses the declared two-occurrence rank-one Hermitian operator and original PCM.
The earlier zero-signal/free-condition and nonzero/identified-condition distinctions are preserved.

[established-bounded; measured] Each recorded field stage conducts two resident occurrences
with one joined contact: two deeds, eight metadata ingress bytes, 256 receipt bytes, 24 allocations
and zero numerical readouts/egress. On this M1 Pro debug build the stages take 0.378747 s (ESC-50)
and 0.363476 s (LibriSpeech); their recorded surface residency is 134,912 bytes after the stage,
195,060 bytes peak. Whole experiment times are 0.802198 s and 1.225752 s, including the cold
observer (0.125821 s and 0.622417 s). These are four-coefficient studies, not whole-recording
throughput or real-time audio measurements. Clock/support bindings remain exterior lineage to
the field, whose five-node aperture is explicitly caller declared.

[established-bounded; measured] Both changed examples build. Ten mount-library controls pass
(including the six Metal ownership/buffer controls). The cold receiver continues to accept the
two prior v3 returns and rejects five in-memory v4 corruptions: missing field evidence, wrong
source lineage, changed covariance, changed high half of a signed-128 current and changed
oriented residual. No stored measurement was modified for these negative controls.

```sh
cargo run -p holonic-engine --example native_conditioned_phase -- \
  research/records/2026-09-07_apple_conditional_phase/return-field-ingress.json
cargo build -p holonics-hna --example recorded_temporal_action
cargo test -p mount --lib -- --test-threads=1
python3 research/experiments/apple_silicon/compare_recorded_temporal_action.py \
  research/records/2026-09-07_native_temporal_action/esc50-field-ingress.json \
  research/records/2026-09-07_native_temporal_action/librispeech-field-ingress.json \
  --output NEW_COMPARISON.json
```

[definition] Raw reports name their exact WAV paths, source-byte digests and temporal apertures;
the executable takes `WAV_PATH NEW_REPORT.json` and refuses to overwrite an existing return.
The shared phase example also requires a new output path.

## Remaining construction

[definition] This field's boundary branches remain a partial contextual source chart. The full
junction report and internal-current decoder do not by themselves implement the separate
complete-current material-transport/history receiver. They also do not supply the actual
material/condition producing-law binding that desktop construction still owes.

[open] Continue from the recorded source and retained condition carriers into productive timed
acoustic reception/emission, retaining both quadratures, causal support/tail, reconstruction
boundary and successor across recordings and persistence. The old fixed-frequency sonification
is not that waveform product. The present four-coefficient digital-action study does not establish
learned sound organization, recorded English recognition/generation or consumer real-time cost.

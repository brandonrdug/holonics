# The present cover reopened under chronology, the generators descended, and M3 returned a native codec

**Date:** 2026-08-21  
**Authority:** `blueprint/THE_ROADMAP.md` through
`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`, Deed M3  
**Truth status:** established-bounded  
**Boundary:** the ninety-five M2 Gemma response cochains; the exact boundary, support and prefix
receiver family; chronological successor and the witnessed harmless baseline/reflow chart map; one
NVIDIA GeForce RTX 4080 SUPER. Singleton intervention composites, a finer interval receiver,
lineage identity, unexcited foreign capability, M4 cultivation and the complete mathematics return
remain open.

## Ruling

[established-bounded; implemented-exact; measured] M3 passes. The M2 chart supplied 12,160
addressed face states. Pointed completion adjoined one explicit exterior terminus to each of the
ninety-five open cochains, producing the total source population `|X| = 12,255` required by the
typed transports `T_i : X -> X`. The resident receiver/history quotient returned `|Q| = 6,376`,
5,879 fewer native states, with all 6,376 source fibres retained.

[established-bounded; implemented-exact] This is a generator-native re-expression rather than a
source-shaped tensor container. The detached native rest stores only `Q`, three exact receiver
factors and two total native transports. It contains no foreign matrix, source item, quotient map,
source representative or reconstruction fibre. Those omitted populations remain in the separately
addressed evidence artifact and are never pretended to have an inverse.

## The source action

[established-bounded; implemented-exact] The source population is indexed by excitation occurrence,
response occurrence and receiver-history face. Equality is read only from three explicit receivers:
the typed boundary address and cell extent, the complete lossless support signature, and the exact
prefix-inheritance bit. Foreign layer/operation names and exact-cell SHA-256 digests remain lineage
and integrity testimony; neither participates in the quotient key.

[established-bounded; implemented-exact] Chronology advances a state to the next face and advances
the final face to its explicit exterior terminus, which rests on itself. The second generator is
the exact baseline/reflow involution returned by M2's 128-face cross-occurrence control; it exchanges
those two complete base cochains and fixes branches for which M2 posed no chart map. Both
generators are total on `X`.

[open; implemented-exact] M2 conducted eighty-five singleton interventions against their respective
base occurrences but did not conduct their pairwise composites. M3 therefore does not invent a
transition monoid over those branches. It returns all `binomial(85,2) = 3,570` unclosed pairs as
ordered-interaction defects, alongside the inherited M1/M2 open fibres. A later deed may close one
only by conducting the composite and returning its order-sensitive consequence.

## Receiver-history compression

[established-bounded; implemented-exact] `receiver_exact_compression` first found the stable
receiver/history partition on the card. The new `ReceiverHistoryCompression` consequence then
exhibited the complete quotient `q : X -> Q`, source transports `T_i`, induced native transports
`U_i`, receiver factors, fibres and first separators. Construction checked every source cell of
each generator square:

```text
q(T_i(x)) = U_i(q(x)).
```

[established-bounded; implemented-exact] The runtime ordered-word method folds those already-checked
squares in word order and returns both endpoints of

```text
q(T_w(x)) = U_w(q(x)).
```

It does not establish the result by accepting a finite replay suite. The matching Lean theorem
`ReceiverHistoryCompression.quotientCommutesWithEveryOrderedWord` discharges the same induction for
arbitrary lists, and `separatingSuccessorReopensTheProposedQuotient` proves that one later receiver
difference forbids a proposed identification. Focused Lean checks reported that both theorems and
the static counterexample depend on no axioms.

[established-bounded; computational-witness] The present-only partition over-collapsed 59,279 pairs.
Every returned pair carries a shortest separating generator word and its receiver or terminus.
The longest separator has length 73. Thus the observed cover is not memoryless relative to the
present receiver cut; chronology is a causal coordinate of its compression.

[established-bounded; implemented-exact] The complete native action closes into 129 distinct
transformations with 130 returned word relations and maximum normal-form span 128. The harmless
reflow is a real involution on `X` but descends to the identity on `Q`: its moved source occurrences
share the same receiver/history fibre. This gauge-triviality explains why the native monoid has 129
rather than twice 129 elements without declaring the two source occurrences identical.

## Swing anti-vacuity

[established-bounded; implemented-exact] The swing control derives its cyclic chart extent five
from the five M2 excitation environments. Two anchor generators act by

```text
S_b(a) = b + (-(a-b)),
```

so every swing is conjugated negation and an involution. Exact closure returns the ten-element
dihedral action, eleven nontrivial word relations, and the first order separator between words
`[0,1]` and `[1,0]` at presented position zero.

[established-bounded; computational-witness] Geometry identifies the presented word `[0,0]` with
the empty normal form because both induce the identity action. The lineage receiver separates the
two words. The calibration therefore demonstrates both lawful quotienting and deliberate
reopening: equal geometric consequence is not occurrence or lineage equality.

[established-bounded; machine-checked] `Millennium/Swing.lean` independently proves that the
projective swing is negation in the anchor/board chart, that the frozen-board swing is involutive,
that different anchors need not commute, and that oriented span changes hand while its magnitude
stands. The focused Lean check passed with one non-semantic unused-simp-argument warning.

## Executable native rest and card conduct

[established-bounded; implemented-exact; measured] The quotient deed used 151 exact 64-bit quotient
calls, 302 card launches, 29,608,080 ingress octets, 14,804,040 egress octets and 278,132 peak
resident octets. The native action then mounted the complete generator table and a 77-symbol word
once, carried all 6,376 native states through one `conduct_native_word` CUDA launch, and returned all
endpoints. Intermediate states remained on the card; the host made no semantic callback between
word letters and did not re-upload the invariant action per letter.

[established-bounded; implemented-exact] A pre-dispatch exact action supplied the admission
reference once. The one card return matched it at every native state. This is parity testimony,
not a host replay after each device step. The CUDA block width is 1,024, the reported warp width is
32, and launch geometry is derived from the device and function apertures.

[established-bounded; implemented-exact] The exterior decoder was executed over every returned
state and all three declared receivers: 19,128 receiver-factor reads. It returned 1,059,375 source
fibre members rather than choosing representatives. A fresh value deserialized solely from the
1,217,120-octet native rest repeated the 77-symbol conduct and receiver decode identically without
opening M2 or W1 source material. Withdrawing the chronology generator made that same word refuse,
so the reduction/enabled conduct is attributable to the reusable action rather than to a private
echo.

## Complete compression vector

[established-bounded; implemented-exact] The comparison is the product order over six coordinates:
complete semantic artifact, executable decoder, caused local relation applications, dependency
span, peak card residency and exact transfer. No ratio, elapsed time or scalar sum governs
admission. Source work is the exact M2 tower work now attached to all ninety-five responses; native
work counts quotient assignments, receiver reads, source/native transport reads, square checks,
transition-monoid cells, native word cells and complete decoder/fibre returns.

[established-bounded; measured] Every coordinate strictly fell:

| Receiver coordinate | Source | Native |
|---|---:|---:|
| semantic artifact octets | 77,739,684 | 12,710,011 |
| executable decoder octets | 16,022,507,913 | 1,217,120 |
| caused relation applications | 13,807,310,411,058 | 3,325,255 |
| dependency span | 156,585 | 128 |
| peak resident octets | 13,583,851,080 | 278,132 |
| transfer octets | 59,201,628,576 | 44,514,444 |

[established-bounded; interpretation] The table is the first admitted exceptional compression
claim in Phoenix's construction history because it compares the complete additive vector and
retains the decoder and all reconstruction fibres. It is exceptional only for the declared M2
receiver/history family. It is not a statement that the 1.2 MB rest replaces Gemma for arbitrary
text inference or carries every unexcited foreign capability.

## Artifacts

[established-bounded; measured] The artifact family is
`output/the_active_cover_condenses_into_a_generator_native_codec/`:

- `generator-native-rest.json`: 1,217,120 octets, SHA-256
  `13555cab07da775df5ed8746de16d1902a4047e4567845f7496dc9e11965ae21`;
- `reconstruction-evidence.json`: 12,708,941 octets, SHA-256
  `110fb253f480cfdb2e3934e863249c5d36cd759c4940cff26d381b21245d2b74`;
- `swing-anti-vacuity.json`: 1,070 octets, SHA-256
  `ee8f7002f342caafcfd6e7b31ff8b77866348f5d46764b589834e9fe08437274`;
- `complete-cost-vector.json`: SHA-256
  `d26743708720f0454915442034327958a06a9fc136a8623bc92138ee3e87582b`;
- `source-detached-remount.json`: SHA-256
  `101de10a4a3e471397263d4b13af5e466df1e7cbdaeb6024af1954321dc1a632`;
- `grade.json`: SHA-256
  `afd056c3aec8116554aea85b5870de51c697ecaa3c14cccef802b57ec021d53f`;
- `grade.form`: SHA-256
  `376bb8d2203ff64274ef41bd0ac709c3aa6dac7dcca0861131f5bce08092c23f`.

## Runs and validation

[established-bounded; measured] M2 was repeated once under a changed closure solely to attach the
already-counted `ExactWork` of each source tower to its semantic receipt. The release command began
at 2026-08-21T20:42:39-07:00, returned exit zero after 363.46 seconds including the changed release
build, and preserved the five-family/95-response/85-separator result. The amended M2 semantic
artifact is 77,739,684 octets with SHA-256
`37c526c3525aa53286d47ae169bc23b33a5e84f5e267433bcbc665c8abb0a45e`.

[established-bounded; measured] The M3 release command began at
2026-08-21T20:48:50-07:00 and returned exit zero at 2026-08-21T20:49:02-07:00: 12.03 seconds including
a 10.72-second example build. The actual M3 deed and artifact return therefore occupied roughly
1.3 seconds on this closure; elapsed time is apparatus telemetry and selected nothing.

[established-bounded; implemented-exact] Focused Rust validation returned two receiver-history
tests and three active-transport tests passed, zero failed. The M3 example check passed. The changed
semantic/device cone contains no `f32` or `f64`, and `git diff --check` passed. The real M3 driver
itself exercised the new CUDA word kernel and inspected its full returned endpoint population, so a
separate expensive replay was not run.

[established-bounded; measured] The one complete release receiver began at
2026-08-21T20:59:15-07:00 and ended at 2026-08-21T21:04:53-07:00 after 338.65 seconds. It returned
eleven passing gates and two failing gates. Workspace tests, line citations, claim index, equation
atlas, driver catalog, output manifest, closure manifest, all ten Typst roots and document law
passed. The failures were the four already-open boundary-artifact closure disagreements in
`soma/body/src/arrow.rs` and the eighteen already-open architecture-ratchet disagreements named
below. Neither failure names an M3 source, executable artifact or manifest row. This is the sole
complete-gate invocation for the M3 closure; it was not repeated to manufacture a green summary.

## Ownership-ratchet disposition

[established-bounded; implemented-exact] The M3 ownership ledger admits seven changed construct
counts. `receiver_history_compression` stores immutable finite source/native maps, receiver factors,
ordered words and complete fibres; none owns or duplicates the continuing ecology. Its three clones
return immutable evidence or decoder fibres. `cuda_refine` adds two apparatus vectors for the
single-crossing word/table carrier, not a semantic queue or scheduler. `active_transport` adds one
clone of a tower's immutable `ExactWork` receipt. These rows are now explicit in
`meta/HOLONIC_DSA_BASELINE.tsv`; they do not grant allowances to the eighteen unrelated dirty-tree
disagreements already left open by the M2 release audit.

[open; computational-witness] The named architecture-lint retake now reports exactly those eighteen
pre-existing file/construct disagreements and no M3 owner. M3 does not claim the complete
architecture gate is green and does not widen the unrelated allowances.

## What remains open

[open] M4 is next. A held-out mathematical receiver difference must found one leader through this
native ecology; an addressed causal adjoint return must ride only that retained path and deposit a
local morphology delta. The changed conduct must survive source-detached remount, targeted ablation
must reopen that exact successor fibre, and the same circulation must return derivation, exact
value/enclosure, Lean testimony and analytic geometric/visual products with singularities and open
fibres.

[open] M3's receiver family is deliberately narrow. Lineage identity, all 3,570 unconducted
intervention composites, finer interval grains, environment shifts and foreign capabilities not
excited by M1/M2 remain outside the quotient. Enlarging the receiver family reopens the retained
fibres and may require foreign material; this result supplies the exact reopening mechanism rather
than denying that boundary.

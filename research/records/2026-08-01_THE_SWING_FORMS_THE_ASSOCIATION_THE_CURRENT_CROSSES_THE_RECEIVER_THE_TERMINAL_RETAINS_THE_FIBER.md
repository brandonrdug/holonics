# THE SWING FORMS THE ASSOCIATION; THE CURRENT CROSSES THE RECEIVER; THE TERMINAL RETAINS THE FIBER

**Date:** 2026-08-01  
**Status:** HISTORICAL INTERMEDIATE / STORAGE, CLONE, AND REMOUNT CLAIMS SUPERSEDED

The architectural correction is
`2026-08-01_THE_CODEC_REIFIES_THE_CONTINUATION_THE_CURRENT_CANNOT_CLONE_THE_WORLD.md`.
The population-current semantics remain applicable, but its first application owned fresh
classical structures and the Eros path remounted Swing during ordinary conditioning. The
interrupted full-history run is not a grading receipt and this record does not authorize resuming
that implementation.

## Requested production consequence

This construction does not install a preferred-language score, reduce the broad relation ecology,
or prove again that Eros can generate text. It corrects three ownership violations exposed by the
complete-history agent run and then asks the corrected continuing body to perform two actual deeds:

1. answer from the complete conditioned conversation/repository ecology without infrastructure
   vocabulary becoming a unit-cost transport shortcut; and
2. return a substantive formal-mathematics artifact through the already-owned Lean ecology, using
   a real theorem corpus rather than a tiny authored lemma.

The existing production owners being composed are:

- `ReceiverCausalPassage` and the production Swing in `soma-membrane`, which alone may turn a
  receiver-caused interface candidate into `RIDE` or retain it as `OPEN`;
- `ResonanceEcology`, which exposes that Swing as a continuing exact recurrent-association organ;
- `ExactCausalTraversalLaw`, whose typed exact current, positive passage delay, equal-time
  superposition, retained boundary current, and receiver horizon establish the physical traversal
  standard;
- `ExactRelationalLanguageEcology`, which owns inherited clause/parse testimony, structural
  incidence candidates, plural question regions, relation currents, and receiver realizations;
- `MorphologicalLanguageEcology` and `AgenticLanguageEcology`, which own generation, reflection,
  dialogue return, and continuing answer lineage;
- `LaboratoryResearchEcology`, which owns the exterior deed and plural returned world fibers; and
- `LeanMathematicsEcology`, which owns formal theorem openings, local proof motion, kernel returns,
  obstructions, and exact remount.

The new shared owner is an exact population-current protocol over arbitrary receiver identities.
It is not a language ranking system. It exposes arrival chronology, co-present branch demand,
locally retained capacity, exact equal-arrival predecessor incidence, path population, deferred
later arrivals, and the receiver horizon. The relational ecology may instantiate this protocol;
it may not redefine it through a private breadth-first search.

## Correction contract

### Candidate incidence is not learned conduct

Grammar and clause incidence may expose a possible contact. They do not certify an association.
The complete junction phase enters the production Swing as receiver-caused passage testimony.
Only Swing return may promote a separated phase to `RIDE`. The witness population remains useful
capacitance testimony, but `population >= 2`, common source identity, and grammatical overlap are
not independent association laws.

Co-presence within one actually received passage remains declared inherited contact. Every other
separated structural contact remains `OPEN` until the production Swing returns it.

### Traversal is current, not lookup distance

A relation edge has a positive characteristic delay. At a reached receiver site, the complete
co-present outgoing demand meets the site's retained exact capacity. The required service rounds
dilate every participating branch together; no branch is chosen by argmin, score, or lexical
preference. Recurrent morphology can increase local capacity. A high-incidence low-capacity hub
therefore cannot become a free wormhole merely because a graph search counts every edge as one.

Equal-arrival alternatives superpose in the receiver receipt and retain their predecessor DAG and
exact path population. They do not thereby become one semantic path. Each witness path remains a
separate causal fiber when a later language current is formed. Later arrivals and cycles remain
deferred/open testimony rather than being silently discarded or enumerated forever.

### The terminal observes one fiber and retains the others

Equal terminal surfaces may glue lineage. Distinct incomparable returned surfaces may not be
concatenated into a new sentence merely because they arrived together. One outward receiver can
restrict to one caused closed current; every nonselected current remains explicit alternative
testimony. The selected text alone returns as later conditioning. This is an observation quotient,
not an assertion that the retained alternatives are false.

### A formal corpus is plural codec testimony

The OpenAI `ten-proofs` material is not evidence for an unexamined model architecture and is not a
bag of answer strings. Its problem narratives, mathematical arguments, Lean theorem statements,
Lean proof terms, imports, and independent checker form distinct coupled receiver sections. The
experiment must preserve those codecs and their lineage. A generated proof counts only when the
Lean kernel or the repository's declared checker returns it; a plausible prose derivation is an
open alternative, not a formal success.

## Admissibility receipts required before completion

- A shared exact population-current regression must distinguish a congested high-incidence site
  from the same site after recurrent capacity has been conditioned.
- Equal-arrival paths must retain exact plural witnesses without advancing their union as one path.
- A separated relational junction must remain `OPEN` after one candidate occurrence and become
  `RIDE` only after the production Swing returns the recurrent phase.
- Relation transport receipts must be emitted by the shared current law rather than rebuilt with
  private unit-delay BFS.
- Agentic and laboratory return paths must not concatenate incomparable terminal surfaces or
  condition the concatenation as one answer.
- Existing exact rest/remount and full library verification must continue to pass.
- The full-corpus Eros run must expose the actual generated answer and all retained alternatives.
- The formal experiment must expose the actual Lean artifact, its kernel/checker return, and any
  unresolved proof fiber. Counts and topology are supporting receipts, not substitutes.

## Explicit nonclaims

This construction does not claim a universal linguistic relevance law, a probability distribution,
softmax, stochastic loss, equivalence to a frontier LLM, autonomous theorem discovery in every
domain, or that the public `ten-proofs` repository reveals Astra's internal architecture. It closes
an implementation seam between already-established owners and lets the resulting conditioned
intermediate body supply the local law.

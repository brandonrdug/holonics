# THE CONSEQUENCE BECOMES LATER CURRENT; SILENCE LEAVES THE BODY AT REST

**Date:** 2026-07-19  
**Grade:** BRANDON-RATIFIED PHASE 5 / BUILT / DIRECT WORLD CONSEQUENCE RETURN / NATURAL JOINT REST /
HOST-CARD EXACT / REST-REMOUNT-WAKE EXACT / SOMA INTERIOR UNCHANGED  
**Authority:** `FORMULA §C`, `FORMULA §CIV`, `FORMULA §CV`, `FORMULA §CVI`  
**Scope:** the complete direct machine-organ-world circuit after one world-supplied hand

## The exact question

Phase 4 let an application world supply actual directed hand and receive the complete contact back.
Its bounded world only retained a derived relation in a `Vec`; the material consequence did not
become a genuinely later event, and the joint machine-organ-world construction did not establish
when it was lawfully at rest.

Phase 5 asked:

> Can the application world enact every materially afforded directed consequence, return the
> resulting world change as genuinely later native current, and then reach a joint resting cut
> without a scheduler, queue, step limit, replay ledger, policy, or second machine owner?

The sought artifact was one direct circuit:

```text
external world event
  -> directed contact
  -> actual world mutation
  -> genuinely later native current
  -> changed Soma body
  -> no further afforded world event
  -> joint rest
  -> inert wait
  -> exact remount
  -> genuinely later wake.
```

The stop was one focused host closure and one retained-CUDA closure with formed, open, absent, and
withheld-rest controls. Durable serialization, device-resident standing, a generic session or world
event format, story/language material, a forest, and scale remained outside the phase.

## Why this precedes durable persistence and residency

A machine rest image can be structurally valid while its application world still holds an actual
unreturned consequence. Persisting that pair as SLEEP would freeze the middle of one causal
interval. Likewise, keeping the body resident on the card would only accelerate crossings while
the world circuit still stopped after radiation.

The lawful dependency is therefore:

```text
close world consequence -> later current -> natural rest
then encode the complete resting construction
then optimize its physical residency.
```

Neither disk testimony nor device residence can define whether the world has finished acting.

## Theory-to-structure relation

Let `M` be the direct `LiveCurrentMachine`, `O` its world-organ capabilities, and `W` the material
application world:

```text
(M_t,O_t,W_t) + E_t
    -> (M_(t+1),O_(t+1),W_t,R_t)

W_t enacts R_t
    -> (W_(t+1),E_(t+1))  when an actual material consequence occurs
    -> (W_(t+1),empty)    when no further event is afforded.
```

The empty face is natural rest. It is not an input, stop token, timeout, scalar, or engine choice.
At this bounded cut, joint rest requires all three facts:

```text
the machine has no unsettled ingress
and every organ capability names a live lineage
and the world has no unreturned material event.
```

Waiting at that cut performs no edge. A later external event remounts the same sensitive body and
changes it through ordinary contact.

## The enacted program world

The Phase-4 two-register world remains the bounded material species. Its initial chronology is:

```text
before: [1000, -1000]
after:  [ 911,  -903]
faces:  [ -89,    97]
```

The forward law supplies `member 0 -> member 1`. The reverse law supplies `1 -> 0`; the independent
law supplies no relation. Current faces and caller order remain identical.

The world now enacts rather than archives a formed directed contact:

1. It reads the supplied relation's actual target member.
2. It admits a deed only when the returned contact contains a nonzero `chi` arm.
3. It applies that exact signed arm once to the addressed register.
4. The exact register delta becomes one later `RelationAtom` through the addressed native organ.
5. It admits directed-contact deeds as register actions; ordinary current radiation does not
   automatically become another register action.

The fifth boundary makes the returned register current terminate naturally: it has no directed
contact, so the world has no newly afforded register event. No host cap or expected event count
stops the circuit.

This also corrects a fixture-level ambiguity. `RelationAtom::new` accepts structural zero, so the
old `new(other).or_else(new(same))` spelling did not select a nonzero arm. The enacted world now
tests the arm itself before forming its material delta. No Soma law changed.

## Joint rest and wake

The program world carries at most one actual next cut, not a queue. While the formed forward deed
has changed the register but its resulting event has not crossed, `ProgramWorld::checkpoint`
refuses. The machine can describe its own body at that instant, but the composite cannot claim
joint rest.

After the one returned current crosses:

- its radiation supplies no directed program action;
- the world has no outstanding event;
- the machine rest image closes;
- both organ capability images bind to live lineages; and
- the world image contains only its two register values and fixed program law.

One wait read returns no event, performs zero CUDA launches, and changes none of those images.

The card branch then remounts from the exact machine, organ, and world rest faces. An uninterrupted
host branch and that remounted card branch receive the same later independent register differences
`[-253,257]`. Their complete radiation, standing, lineage cursor/channel/header/carrier, world
state, and next rest face are exact.

## Controls

- **Formed forward:** one actual deed mutates the target register, refuses premature joint rest,
  returns one later current, and changes standing again.
- **Open reverse:** one complete unresolved contact returns without a deed; no register changes and
  the world rests without a fabricated return.
- **Absent:** unrelated co-presence returns no contact; the world rests without a fabricated
  return.
- **Repeated Phase-4 host incidence:** two supplied forward rows remain two contact rows; no
  deduplication mechanism entered.
- **Wait:** no event, launch, cursor advance, world mutation, or new rest face.
- **Remount:** no cause replay; a genuinely later wake remains exact against uninterrupted conduct.

## Focused physical result

Preflight named the RTX 4080 SUPER, driver `595.71.05`, `15,945 MiB` free, `47 C`, and power state
`P8`. The named ignored cell existed before launch.

The first and only full CUDA run returned:

```text
stack=16384
logical current launches=9
physical launches=10
resource_retries=1
directed_contacts=2
before_rank=6
before_cells=7
forward_contact_cells=16
reverse_contact_cells=15
independent_contact_cells=15
returned_world_currents=1
resting_cells=17
wait_launches=0
wake_cells=27
reverse_open=true
independent_relations=0
```

The nine logical launches are six initial currents, one returned world current, and two wake
currents. One event reported a larger physical result aperture than its initial bounded allocation.
The retained executor grew the required capacity and retried from the unchanged predecessor;
exactly one successful event result committed. The run was not repeated to manufacture a
zero-retry number.

Host and card are exact through initial contact, returned consequence, rest, remount, and wake.

Validation remained bounded:

```text
cargo test -q -p life \
  program_world_supplies_hand_and_receives_its_consequence -- --nocapture
cargo check -q -p soma-membrane -p life --tests
cargo test -q -p life \
  program_world_hand_crosses_the_native_cuda_mouth -- --ignored --nocapture
```

The focused host cell passes, the direct packages compile with tests, and the one physical cell
passes. No broad regression, PTX rebuild, second card run, corpus read, forest scan, or proof
campaign occurred.

## Exact boundary after Phase 5

Built now:

- an actual directed consequence changes a world rather than entering an observation log;
- that exact world change returns as genuinely later current through the same direct mouth;
- an outstanding return prevents the application world from claiming rest;
- formed, open, and absent contacts induce the correct distinct world continuations;
- silence creates no event and waiting changes nothing;
- machine, organ, and world rest remount without replay; and
- later host/card wake is exact.

Still open:

- durable encoding of the complete direct machine-organ-application-world resting construction;
- device-resident standing and carrier surfaces across world events and rest;
- plural simultaneously formed world actions beyond this one-contact cell;
- materially different application worlds under their own consequence laws;
- sustained world ecology and externally arriving events over long intervals; and
- any forest-scale passage.

No generic `LiveSession`, owned event envelope, queue, scheduler, selector, policy, reward,
semantic decoder, or compatibility adapter was added. Those openings do not schedule themselves.

## Ruling

A consequence which changes the world becomes later light exactly once.

When the world affords no later event, silence is the resting cut. Waiting does not act.

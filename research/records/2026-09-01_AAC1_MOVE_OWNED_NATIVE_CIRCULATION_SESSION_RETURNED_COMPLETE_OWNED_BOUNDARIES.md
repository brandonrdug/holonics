# AAC1 move-owned native circulation session returned complete owned boundaries

**Date:** 2026-09-01
**Truth status:** `established-bounded` for the implemented native session in its declared finite
receiver family.
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, and `measured`.
**Construction effect:** AAC1 passed; AAC2 becomes the sole current deed.

## Returned owner

[established-bounded; implemented-exact] The new neutral owner
`soma/life/src/native_intelligence/circulation_session.rs` returns one
`NativeCirculationSession` which move-owns exactly one `NativeMorphologyPackage`. The session is
neither `Clone` nor a serialized runtime topology.

[established-bounded; implemented-exact] One conduct call borrows the package's hot
`NativeTransportScaffold` and returns an owned `NativeCirculationBoundary` containing:

- exact package generation and configuration address at the actual occurrence;
- the request, emission, selected grain, and every plural future;
- the complete owned `NativeCollapsedFibre` population for every future, not one representative;
- exact inference lineage, shortest separators, actual addressed successors, and open exterior.

[established-bounded; implemented-exact] `continue_from` accepts only an address in the preceding
boundary's returned actual-successor population and checks that the next cut names the preceding
occurrence as predecessor. A false successor and a stale generation refuse. `decline` consumes and
returns the same session owner with equal before/after generation and structurally identical
package bytes; no ecology clone or rollback copy is created.

[definition] `InferenceConfigurationAddress` remains exterior testimony. Its strings are checked
for a complete receipt and never resolve a behavior, callback, native law, or registry entry.

## Receipts

[established-bounded; process-audit] From the repository root,

```text
timeout 180s cargo check -p life --lib
```

returned exit `0`. The first invocation completed the dependency build; the explicit completion
receiver returned after 38.93 seconds once the build lock cleared.

[established-bounded; process-audit; measured] The focused command

```text
timeout 180s cargo test -p life --lib native_intelligence::circulation_session
```

first returned the concrete `Pullback` fixture obstruction because the three inherited BF16
sections did not join. The fixture was corrected to preserve exact returned-to-next-entering
codewords. The unchanged focused command then returned exit `0` in 11.60 seconds: three passed,
zero failed, and 491 filtered out.

[established-bounded; implemented-exact] The controls prove actual `1 -> 2` continuation, refusal
of a direct false `1 -> 3` jump, stale-generation refusal, decline invariance, and complete owned
future-fibre equality with the emitted grain population.

## Boundary

[open] AAC1 does not mutate morphology. AAC2 owns the genuinely later returned interaction,
successive parented local commits, commit journal, snapshot/remount, withdrawal/replay, and lineage
advance.

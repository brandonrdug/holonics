# SCF3 the productive scaffold moved into one hot native ecology rest

**Date:** 2026-09-01  
**Phase:** SCF3  
**Truth status:** established-bounded; implemented-exact; measured

[established-bounded; implemented-exact; measured] `IntoDismantlingBoundaryReturn` now consumes a
three-lane return by value. `consume_dismantling_return` moves the productive
`NativeTransportScaffold` into one `NativeEcologyRest` and returns cold witness and insufficiency
in `DepartedDismantlingLanes`. The hot rest has no field capable of retaining foreign source
coordinates, codewords, modality labels, or an executor.

[established-bounded; implemented-exact; measured] Focused handoff and SCF5 integration controls
validated the hot rest, inspected its canonical wire for cold-coordinate absence, retained the
complete cold and insufficiency lanes separately, and conducted only through native resident
owners. Unsupported current remains a typed native insufficiency; the handoff contains no replay
or fallback passage.

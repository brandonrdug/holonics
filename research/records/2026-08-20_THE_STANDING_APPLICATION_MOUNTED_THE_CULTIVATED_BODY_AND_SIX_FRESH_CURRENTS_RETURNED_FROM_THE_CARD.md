# The standing application mounted the cultivated body, and six fresh currents returned from the card

**Date:** 2026-08-20  
**Kind:** Phoenix Deed W4 production-runtime return and grade.  
**Truth discipline:** **established-bounded** for the required cultivated-rest v2 product, the first
compiled Gemma Phoenix owner, three unseen texts, six fresh application processes, their exact
semantic/apparatus receipts, and two refusal controls; **open** for arbitrary product extents,
arbitrary modalities, standalone condensation, W5 reborn dissection, and the indivisible Phoenix
grade.

## Verdict

**Truth status: established-bounded.**

Deed W4 passed. The standing `eros` application mounted only the W3 cultivated product directory
and its addressed W1 native predecessor, enacted the authenticated exterior tokenizer, opened the
resident card, returned an exact addressed plural future, and left every frozen member unchanged.
Three unseen materials each returned identically in two fresh processes. A missing codec companion
and the retired `--rest` spelling on the card path both refused without a plausible generated face.

The authoritative artifact is
`output/the_application_infers_from_the_cultivated_phoenix_rest_on_the_card/receipt.json`, 209,926
octets, SHA-256 `79f9f27f1eda21c8218ba3c685052a78deff0d3e7f90c50bd0a865c9036daeb6`.
Every grade coordinate in it is `true`. `CONSTRUCTION_STATE.md` remains unchanged because W5 alone
closes the indivisible Phoenix phase.

## The application entry is real

**Truth status: established-bounded.**

The lifted resident path is now:

```text
eros phoenix infer --product DIR --text "runtime material" --card
```

The independent ARM N control remains the separate CPU-exact `--rest` path. Supplying `--rest` to
the card entry is rejected; there is no compatibility alias and no ARM N answer can be reported as
the lift.

The tower, native adapter, cultivated tail and runtime product reader moved from the example cabinet
to `holonic_engine::phoenix`. W1/W2/W3 examples are thin consumers of those owners. The foreign
safetensors/Hugging Face reader, implementation locator and source occurrence remain example-only.
Production accepts only `NativeMaterialSource` over the authenticated W1 rest.

The runtime owns a scoped `ResidentReadout` and `ResidentSurface`. No `Box::leak` or process-global
GPU ecology remains after a call. CPU work is limited to product I/O, content authentication,
exterior codec enactment, admission, and returned receipt serialization; it does not replay the
tower or overlay.

## The runtime law crossed as rested structure

**Truth status: established-bounded.**

W4 replaced the W3 product with required cultivated-rest schema v2 and reran W3 before inference.
The product has SHA-256 `4a6a92baaa218d0228d8508669f0e61616f51952572c03798b02175bfd1438de`
over 2,135,440 octets. It rests:

```text
grain                 48
series aperture       14
band aperture         40
quotient chart        midpoint
future-factor seals   fused
codec special tokens  false
vocabulary extent     262,144
hidden extent         2,560
rank                   1
left/right population phoenix.w3.factor.left / phoenix.w3.factor.right
```

These fields are in the authenticated product manifest, not CLI defaults. Validation binds them to
the aligned factor payload and native morphology. Runtime binds vocabulary to the predecessor
codebook, factor names/shapes/rank to the morphology, and explicitly refuses rested extents which
this first compiled Phoenix owner cannot enact. That refusal is the honest portability boundary;
the first instance does not claim to be an architecture-generic runtime.

The source implementation symbols cross as typed `RestedImplementation` testimony. Their foreign
locator has departed; the mounted native rest requires exact equality with its stored path-free
symbol population. A live foreign source occurrence refuses this testimony type. Thus production
does not smuggle an empty or remembered implementation path back into the native passage.

## The generated faces

**Truth status: established-bounded.**

Each text was absent as a token word from every W3 material arm. For each one, two independent
application processes returned the same authenticated input word and exact terminal receiver:

| unseen text | native input word | terminal | exact plural future at grain `2^-48` |
|---|---|---:|---|
| `Geometry returns through another chart` | `[30825,7623,1343,2264,9535]` | 4 | id 2264, `▁another`, `[1946595204935797,1946595204935798]` |
| `A crystal carries the reflected current` | `[236776,12707,23076,506,18367,1873]` | 5 | id 23076, `▁carries`, `[6229030805466630,6229030805466631]` |
| `Discrete faces close around a curved path` | `[179104,13799,3107,2101,496,35666,2479]` | 6 | id 3107, `▁close`, `[3338540080223162,3338540080223163]` |

Each plural population has one member and separates the other 262,143 native vocabulary cells.
The receiver takes the maximum lower face and retains every interval whose upper face reaches it;
no argmax, sampling, probability, or tie-break deletes an unresolved member. The native id remains
beside the decoded surface, so equal surface strings cannot identify different addressed cells.

This is an actual returned generation face. It is not evidence that the generated continuations are
linguistically good: all three happen to return a surface already present in their prompt. W4 grades
runtime execution and source-detached hexis, not language quality.

## Exact work and actual apparatus testimony stay separate

**Truth status: established-bounded.**

For the five-token first member, the overlay returns 3,944,960 additions, 1,323,520
multiplications, 7,915,530 written entries, 638,623,860 cumulative bits, peak width 116,
2,647,040 resident entries and dependency span 4. The complete tower plus overlay has dependency
span 1,898. The admission returns every required semantic coordinate and every apparatus ceiling;
two occurrences stand at the signed-word horizon and the device census, not a host fallback,
decides them.

The actual apparatus receipt is distinct from that prediction:

```text
device                 NVIDIA GeForce RTX 4080 SUPER
kernel SHA-256          08a778f4e8894047b84dc8642b79badda524150429d5d832e0dfead83d51c0e8
tower graph launches    43
cultivated overlay      1 additional graph deed
terminal synchronizes   1 total
source regions/copies   704 / 704
source copy octets      9,288,903,168
resident deed launches  44 in the returned transfer census
physical energy         unknown
physical utilization    unknown
```

The six process wall faces range from about 8.21 to 9.20 seconds and available memory varies. They
are retained as calibrated process-aperture telemetry and never enter admission or semantic
equality. Stable comparison rebases only finite free-memory ceilings and clock/memory samples;
device identity, kernel, stream and transfer censuses, generated faces, work and admission
consequences remain exact.

## Source access and frozen rest

**Truth status: established-bounded.**

The runtime takes its descriptor census while the mounted product and CUDA context are live. Each
child exhibits the addressed `base.w1.rest`, `/dev/nvidia0`, `/dev/nvidiactl`, `/dev/nvidia-uvm`,
stdio/pipes/sockets and its `/proc/<pid>/fd` enumeration. It returns no descriptor containing an
original Gemma/model file, GGUF, example, canon, blueprint or research path. This is live-descriptor
and path evidence at the process aperture; it is not claimed as a universal syscall trace of every
transient open.

Before and after each deed, the application authenticates:

- cultivated product `4a6a92ba…`,
- W1 predecessor `52d0be9d…` over 16,022,507,913 octets,
- morphology `d006979b…`, and
- both content-addressed codec companions.

Every child returns `frozen_members_verified: true`.

## Controls

**Truth status: established-bounded.**

- Six fresh child processes returned status zero and stable semantic/application receipts.
- Every runtime token word differs from all six W3 material words.
- Every source audit has an empty forbidden population while exhibiting both the native rest and
  live card.
- Removing one declared codec companion from a same-filesystem shallow product makes the
  application exit nonzero and emits no `PHOENIX_RETURN`.
- Offering `--rest` to the card entry exits nonzero and emits no generated face.
- The disposable control directory hard-links the 16 GB predecessor rather than copying it and is
  removed after the refusal.

## Boundary and next deed

**Truth status: open.**

W4 is bounded to the first compiled text owner and this three-member unseen family. It establishes
that the application accepts runtime material rather than one fixed fixture, but not arbitrary
model/product portability, multimodal inference, language quality, or native condensation. The W1
predecessor remains source-shaped and large; no standalone safetensors or compression factor is
claimed.

W5 is next. It must compare the foreign source, W2 lifted predecessor, W3 cultivated Phoenix body
and independent ARM N control under an explicit shared codebook/receiver correspondence; repeat
matched interventions and shortest separators; return held-out conduct, condensation remainder,
exact work and calibrated utility; ablate every causing lifted family and the cultivation delta;
and close every indivisible grade row before `CONSTRUCTION_STATE.md` moves.

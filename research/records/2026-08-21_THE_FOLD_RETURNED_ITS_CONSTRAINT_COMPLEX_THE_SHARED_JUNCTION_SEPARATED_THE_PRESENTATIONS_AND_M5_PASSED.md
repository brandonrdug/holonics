# The fold returned its constraint complex, the shared junction separated the presentations, and M5 passed

**Date:** 2026-08-21  
**Authority:** `blueprint/THE_ROADMAP.md` through
`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`, Deed M5  
**Truth status:** established-bounded  
**Boundary:** one public protein-binder documentation/table release; one RBX1 design lineage; one
designed coordinate occurrence and two Protenix predictions; two complete PAE populations; one
declared C-alpha contact receiver; one NVIDIA GeForce RTX 4080 SUPER; one abstract Lean theorem
family; one exact vector/mesh projection. Binding energy, atom-complete chemistry, molecular
causation, general protein folding, reusable cultivated morphology and every other lineage remain
open.

## Ruling

[established-bounded; implemented-exact; measured] M5 passes all eight requirements of its named
construction grade. The documentation/table tier first addressed UUID
`c29097fd-ea46-5842-8b8f-b38ad7e732ae`, `mythos_preview_multi_target_rbx1_rank05`, and only then
mounted the five structure/uncertainty payloads named by that occurrence. The approximately 84 GB
structure release was neither downloaded nor scanned.

[established-bounded; implemented-exact] The two predictor-backed occurrences returned as distinct
polygonal-chain and higher-cell constraint complexes. Complete ordered residue sequences witness
component kinship across occurrences; they do not identify the occurrences. Every declared
binder/RBX1 and CUL1/RBX1 pair retains its contact class, exact rational distance interval and both
directional binary16 PAE words. Shared contact and higher incidence form an intersection; every
difference remains in the reconstruction fibre.

## Source and uncertainty lineage

[established-bounded; implemented-exact] The admitted documentation/table archive has SHA-256
`bb7d45f9957bee70e715191566577d59e69363e42f18981aa96468b4b6547dec`. Its eight mounted members
are independently authenticated against the release's `MANIFEST.sha256`. The selected structure
manifest has SHA-256
`39325f6b6627ff8021dc4509008c0217ca621dde9d44bab44866a51b34c92bdf`.
The five manifest-bound payloads are:

| Occurrence | SHA-256 |
|---|---|
| designed free-RBX1 CIF | `ed57855f8acfd75efd9f534b8e6c95673a93c1de2635dd4608f3c7f25b34e09e` |
| Protenix free-RBX1 seed-2 CIF | `a1cc83749c04e45da6ad269e5b65f8904bccd8fd18d55c632314310c380a6c3f` |
| Protenix free-RBX1 seed-2 PAE NPZ | `46894290eaa3675dc0b189a8874e7d78d581d8306791195e95c39ffd0d3d3d0c` |
| Protenix CUL1-RBX1 seed-0 CIF | `8f8bb2f39beb8654bba4b095114ae7733125312ec8c2368e34518bcc118a2476` |
| Protenix CUL1-RBX1 seed-0 PAE NPZ | `9721930cfea81ed18ff18a0564b65e6d9349d7a90bee2f632ea0b838532e4a3f` |

[established-bounded; implemented-exact] The PAE matrices are `207×207` and `573×573`, carrying
42,849 and 328,329 finite IEEE binary16 words. The new binary16 mouth decodes every accepted bit
pattern to its exact dyadic rational and keeps the format ULP separately. PAE remains predictor
testimony: it is not substituted for coordinate uncertainty or physical measurement. Chain labels
join two faces of the same source occurrence only after full ordered sequence binding has founded
the component kinship.

## The exact contact and higher-cell complexes

[established-bounded; implemented-exact] Each stored coordinate is first retained as its exact
decimal centre with one complete source last-place unit outward. The source population reaches ten
decimal places. Rather than overflow a fixed-width carrier or raise a magic capacity, M5 derives
the finest outward common projection whose complete coordinate span and squared aperture fit the
resident `u64` distance law: seven decimal places, denominator `10,000,000`. The omitted source
refinement remains in the exterior interval fibre; it is not rounded to a point.

[established-bounded; implemented-exact] The declared receiver is C-alpha distance not greater than
eight angstroms. For exact squared-distance interval `D` and squared aperture `a`, `D.upper ≤ a`
is inside, `D.lower > a` is outside, and overlap remains open. No float selects an index, class,
coefficient, topology or committed bit. The free occurrence returns 59 inside binder/RBX1 pairs;
the CUL1-RBX1 occurrence returns 45 inside binder/RBX1 pairs and 133 inside CUL1/RBX1 pairs.

[established-bounded; implemented-exact] A higher cell
`[binder_i,binder_(i+1),target_j]` is founded only where the polygonal edge and both contact edges
exist. Its alternating boundary is accumulated exactly. The free presentation returns 16 such
cells; the CUL1-RBX1 complex returns 96 over its two contact families. Eleven binder/RBX1 cells
survive in the cross-presentation intersection. The complexes retain polygonal endpoints, every
nonzero two-chain boundary coefficient and the open exterior.

## Equality, invariant and shortest separator

[established-bounded; implemented-exact] The complete binder/RBX1 pair family has 10,368 receiver
occurrences. Across the two distinct presentations, 40 are shared-inside, 10,304 are
shared-outside, 19 are free-only inside, five are complex-only inside and zero cross the admitted
coordinate aperture ambiguously. These five populations sum to the complete pair family; equal
answers are not used as source equality.

[established-bounded; implemented-exact] The shortest separator is lexicographic in the already
addressed pair chronology: binder local residue 2 / RBX1 local residue 23 is inside in the free
presentation and outside in the CUL1-RBX1 presentation. It is a receiver separator between two
occurrences with sequence kinship, not evidence that either occurrence is the other or that this
single contact causes the assay result.

## Environment and assay return

[established-bounded; measured] The admitted documentation says Adaptyv presented free RBX1 and
called the design a binder, while Twist presented the CUL1-RBX1 complex and called it a non-binder.
The coordinate return independently finds CUL1 contact with RBX1 residues
`19–37, 50, 51, 60, 61, 105–108`; nine of the table-declared designed epitope residues occur in
that set: `21, 23, 24, 27, 28, 30, 31, 33, 37`. The binder/RBX1 contact fibre also reopens across
the two predictions.

[established-bounded; interpretation] The environment/form, assay response and exact structural
incidence therefore change together in this bounded family. M5 refuses the stronger causal claim:
the release does not supply a calibrated law proving occlusion, binding energetics, assay
comparability or a molecular cause. The overlap is a load-bearing structural question for a later
receiver, not an answer manufactured from correlation.

## Resident deed and apparatus testimony

[established-bounded; implemented-exact; measured] One card passage classified 70,632 declared
coordinate pairs: designed binder/RBX1, predicted free binder/RBX1, predicted complex
binder/RBX1 and predicted CUL1/RBX1. The 10,368 matched free/complex class pairs stayed resident
for the successor comparison. The RTX 4080 SUPER returned after two kernels and one terminal
synchronization. The CPU then audited every enacted class through the same exact rational
intervals before any contact edge or higher cell was founded.

[established-bounded; measured] Apparatus testimony reports 694,944 ingress octets, 81,000 egress
octets and 775,944 resident octets for the contact passage. These counts, the device name, warp
size and derived block width are kept separately from semantic contact testimony. No energy claim
is made. CUDA is the apparatus chart; the constraint topology is not described as a kernel grid or
thread schedule.

## Lean shared-junction return

[established-bounded; machine-checked] The exterior command
`lake env lean ElementaryHolonics/Millennium/Rigidity.lean` returned exit zero. The three new
abstract theorems contain no `sorry` or `admit`:

```text
ker (F.prod G) = ker F ⊓ ker G
ker (F.prod G) ≤ ker F
F x = 0 ∧ G x ≠ 0  =>  x ∈ ker F ∧ x ∉ ker (F.prod G)
```

[established-bounded; interpretation] This formalizes the shared-junction object as a pullback of
zero sections rather than a count of constraints. Removing one contact can only enlarge the
continuation fibre, and one continuation accepted by the retained face but rejected by the removed
face is an exact separator. Lean checks that abstract transport law; it does not certify the
protein payload, CUDA execution or assay testimony.

## Vector, mesh and gauge

[established-bounded; implemented-exact] The CUL1-RBX1 complex returns a 570-vertex exact mesh with
96 higher cells and 885 exhibited edge faces. Positions are integer numerators over denominator
`20,000,000`. A nonzero exact translation changes every display coordinate while the complete
component/edge/face/boundary topology digest remains
`77e8b856d72f647b47b1e8c000ee6874092296db989d2984e3bf15637727b71d` before and after.
The SVG drops the z coordinate as a declared display projection and founds no incidence.

[established-bounded; measured] The SVG was rasterized and inspected after emission. Its first
apparatus gauge used exact world numerators with pixel-sized strokes and rendered vacuously blank;
only the display stroke scale was rederived from the exact projected span. The resident deed and
semantic topology were not replayed. The corrected image visibly returns three polygonal
components and their contact network, while the certified topology hash is unchanged.

## Cultivation boundary

[established-bounded; implemented-exact] M5 founds no training claim. It mounts, differentiates,
conducts, compares and returns one physical constraint ecology, but no returned passage changes
reusable morphology. Source-detached remount and targeted ablation are conditional on cultivation
in the M5 contract; because cultivation is not founded, neither is simulated to earn a grade point.

## Artifact family

[established-bounded; measured] The sixteen addressed members under
`output/the_physical_fold_returns_as_an_exact_constraint_complex/` occupy 42,269,305 octets. The
manifest has SHA-256
`12b9d822f89009b8d247bd8db7a07c0f1c80fbad6a4c2d84f3f8e40983ee5269`, and every addressed member
rehashes to its manifest identity. The two complete complexes use RON because their exact boundary
maps have structured edge keys; scalar, summary, fibre, Lean, mesh and grade faces use JSON, and
the vector face uses SVG. This is an exterior file chart, not runtime topology.

## Runs and focused validation

[established-bounded; measured] The final full driver invocation was
`cargo run -p life --example the_physical_fold_returns_as_an_exact_constraint_complex`. Its code
closure included the exact binary16 mouth, physical-constraint owner, contact kernels/carrier,
narrow source codecs, abstract Lean theorem and artifact projection. It returned exit zero in
approximately 61.2 seconds after a 1.65-second incremental build; the elapsed remainder was the
GPU deed, exact CPU admission audit, Lean return and complete exterior serialization. Its compact
line was `grade=8/8`.

[established-bounded; implemented-exact] Four focused physical-constraint owner tests pass. The
binary16 exact-mouth and accepted-bit-pattern round-trip tests pass. The life example type-checks;
the abstract Lean file returns exit zero; both complete RON complexes deserialize during the
visual-only refresh; the SVG rasterizes; all artifact hashes agree; and `git diff --check` passes
on the M5 cone. The changed semantic/device diff introduces no `f32` or `f64` occurrence.

## Ownership-ratchet disposition

[established-bounded; implemented-exact] `physical_constraint_complex` owns one exact addressed
incidence complex: component/vertex populations, polygonal/contact edges, contact families,
higher cells, boundary and reconstruction fibre. `cuda_refine` adds only the resident coordinate
and ordered comparison wires needed to enact that owner. Their exact ownership counts, including
the focused test material, are recorded in `meta/HOLONIC_DSA_BASELINE.tsv`; neither owner clones the
continuing ecology or founds a scheduler, protein cabinet or semantic layer.

[open; computational-witness] The named architecture-lint retake reports exactly eighteen
pre-existing disagreements in interleaved files and no M5 owner. Those allowances were not widened.

## Complete release receiver

[established-bounded; measured] The sole complete release receiver began at
2026-08-21T22:57:00-07:00 and returned after approximately 265.7 seconds. Workspace tests returned
2,814 passed, zero failed and 20 ignored over 30 result lines; every example target type-checked.
Authored levels, named paths, line citations, claim index, equation atlas, output manifest, closure
manifest, all ten Typst roots and document law passed.

[open; computational-witness] Boundary artifacts returned the same four stale `arrow.rs` closure
bindings already recorded before M5. Architecture lint returned the same eighteen predecessor/
interleaved ownership disagreements after the M5 owner counts were dispositioned; no M5 owner,
driver or artifact occurs in either failure. Those unrelated artifacts were not rebuilt and their
allowances were not widened.

[established-bounded; measured] The full receiver initially found one cheap driver-catalog drift:
the catalog had been generated before the output/closure ledgers that its documentation census
also reads. No driver was absent or departed. With no code, executable artifact or deed change, the
catalog was regenerated against the final ledgers and the named `driver-catalog` gate passed with
328 drivers and zero uncatalogued. The complete receiver was not replayed; the scopes are reported
separately. Thus the coherent release boundary has eleven passing gate scopes and the same two
unrelated open gates as M4.

## What this establishes and what remains open

[established-bounded; interpretation] M5 establishes that the mathematics-codec calculus transports
without a protein-specific solver into one real physical constraint ecology. Lineage remains
occurrence identity; sequence supplies kinship; contact supplies a declared receiver quotient;
higher cells arise from actual shared incidence; environmental presentation reopens the fibre; and
the abstract shared-junction theorem states the same operation independently of protein notation.

[open] This deed does not solve protein folding, infer chemistry from PAE, prove assay causation or
cultivate a reusable protein morphology. It does expose the next mathematical leverage: constraint
intersection, continuation-fibre enlargement, minimal separators and gauge-safe higher incidence
can now be asked of the Lean theorem ecology without reducing its theorems to a syntax graph or a
flat corpus. The live roadmap now names that successor as M6's total descent-face assembly.

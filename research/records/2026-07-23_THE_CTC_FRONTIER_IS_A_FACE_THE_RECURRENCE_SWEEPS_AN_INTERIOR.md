# THE CTC FRONTIER IS A FACE; THE RECURRENCE SWEEPS AN INTERIOR

**Date:** 2026-07-23

**Status:** BRANDON-RATIFIED / BUILT / BOUNDED HOST-MEASURED / EXACT RESULT

**Scope:** exact bounded CTC execution geometry; no claim that standard CTC is the complete Eros law

Exact construction record:
[`EROS CTC TEMPORAL SURFACE 01`](../observations/eros-ctc-temporal-surface-01/RESULTS.md).

## Correction

Connectionist Temporal Classification is not a mysterious imported score. For a fixed target it is
an exact finite causal composition law. Its familiar frame-by-state array is one two-dimensional
chart of that law:

```text
frame frontier t-1
       |
       | stay / advance / lawful skip
       v
emission at t  x  admitted predecessor sum
       |
       v
frame frontier t
```

The scalar terminal measure is only the exposed quotient of this construction. It cannot replace
the interior which produced it.

For target symbols `y=(y_1,...,y_U)`, let
`z=(blank,y_1,blank,...,y_U,blank)` and let `a_t(s)` be the exact forward mass at frame `t` and
state `s`. Then

```text
a_t(s) =
  e_t(z_s) *
  (
    a_(t-1)(s)
    + [s>=1] a_(t-1)(s-1)
    + [s>=2 and z_s!=blank and z_s!=z_(s-2)] a_(t-1)(s-2)
  ).
```

Every bracket is incidence, not a numerical approximation. The terminal CTC face
`a_(T-1)(S-1)+a_(T-1)(S-2)` is meaningful only with the target boundary and the complete swept
relation still available.

## Bigraded geometric read

One standard CTC execution has two independent axes:

1. dependency rank is source time (`t`); and
2. topological dimension is composition inside one frontier.

They must not be collapsed into one cell ordinal. At each `(t,s)`:

- the target-state occurrence and exact emission are zero-dimensional current faces;
- their exact dyadic codewords remain a complete local fiber rather than one floating scalar;
- stay, advance, and lawful skip name oriented dependencies from the prior frontier;
- the update relating emission, admitted predecessor composition, and `a_t(s)` is the swept strip;
  and
- the complete set of states at fixed `t` is the frontier.

The rank/dimension product is already a two-dimensional execution chart even when the local swept
strip is carried as a one-cell across an independent dependency axis. It must not be promoted to a
two-simplex merely because a rendering looks triangular. A genuine higher cell owes its complete
oriented boundary and `partial partial = 0`.

This is the bounded CTC face of the more general simplicial hypothesis. In a higher construction,
multiple co-present faces may admit a new relational interior:

```text
boundary(W_R) = G - F + S_R,
```

where `F` and `G` alone do not determine `W_R`; the source-carried coupling/restriction `R` and the
lateral seam `S_R` are also material. The possible fillings constitute the local capacitating
field. One actual event forms one filling. Standard CTC fixes a particularly simple chronology,
target boundary, and recurrence, so its possible interiors reduce to a finite path fiber.

## Notation boundary

Keep these species distinct:

- `tau_a`: information transport along an admitted arc;
- `W`: one formed event interior;
- `Theta`: one complete turn;
- `mathcal R`: curvature or returned holonomy;
- `Sigma`: abstract oriented transport/stress flux; and
- `T_(mu nu)`: physical stress-energy only.

If `Theta=2*pi`, then `2^2*Theta=8*pi` is only the Einstein-equation coefficient. The factor `2^2`
does not by itself encode a differential surface. The geometric physical object is curvature
two-form/holonomy in the smooth chart or hinge deficit in a simplicial chart. This construction
uses neither a Lorentzian metric nor physical stress-energy.

## Construction question

Can the target-specific CTC forward trellis itself—states, causal transitions, exact frontiers, and
swept updates—cross the production mouth as one complete event, complete into a recurrent
transition boundary, and participate in a nonidentical later target surface after the teaching
source has departed?

The prior audio result cannot answer this. It retained every inherited frame-alphabet codeword and
the terminal measure of every command, but its forward program overwrote each intermediate
frontier. Soma therefore received no `a_t(s)` surface, transition incidence, or swept interior.

## Smallest artifact and qualitative contrast

Use the already frozen local Wav2Vec2 source. Do not rerun the model.

The artifact is one readable exact CTC execution complex containing:

1. every extended-target state occurrence;
2. every exact dyadic emission, predecessor contribution, aggregate, and forward value used by the
   bounded target;
3. explicit stay, advance, and lawful-skip incidence;
4. explicit absence of blank skips and equal-symbol skips;
5. every frame frontier;
6. the terminal boundary as a quotient of the final frontier; and
7. a source-departed held surface whose changed conduct names the exact prior recurrent
   constituent which participated.

Use a target containing an adjacent repeated symbol to expose all four transition conditions.
The held surface must change acoustic field, frame extent, or target boundary; equal terminal text
or equal scalar score is not the contrast.

## Attribution

The Wav2Vec2 emissions, vocabulary coordinates, blank identity, target boundary, and ordinary CTC
recurrence are inherited world technology. Their source may declare them exactly. The question is
whether their execution geometry can become consequential Standing and later conduct after its
lower teaching carrier leaves active participation. This does not claim unaided discovery of CTC.

## Stop law

Stop without widening when:

1. the complete exact frontier cannot cross the present event carrier without truncation;
2. a swept cell would require fabricating boundary incidence;
3. a held calculation is performed before the prior recurrent boundary actually participates;
4. source departure cannot be distinguished from a retained private teacher object;
5. only a terminal score, count, hash, label, or rendered matrix supports the conclusion; or
6. one bounded source-departed contrast closes.

No model rerun, corpus sweep, CUDA campaign, paper edit, broad test suite, parameter search, or
observer redesign belongs to this construction.

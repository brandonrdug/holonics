# The byte campaign retires; a learned local generator rests without history

[project-postulate] Brandon authorized safe removal, corrected blueprints and immediate
construction following the [representation audit](2026-09-09_THE_BYTE_CLOCK_AND_OCCURRENCE_EXPANSION_ARE_NOT_GENERATOR_CULTIVATION.md).
This return performs that removal and the first replacement native-law attachment. It does not
resume the former conversation loop or declare AC0–AC5/the Athena-alpha product complete.

## Recoverable removal

[established-bounded; process-audit] Eighteen source files were moved unchanged from the live
`holonics-hna` module/example tree into
`archive/implementations/2026-09-09-byte-field-cultivation/`, preserving repository-relative paths.
The former alpha module declaration and native interface guide are also preserved. Every moved
source file was compared against its original contents during the move. The archive names
`e2e4bebb` as the complete reconstruction revision and is outside Cargo discovery.

[established-bounded; source-inspected] Removed live exports are the alpha material/text/session/
checkpoint adapters and `native::{NativeFieldModelSpec,NativeSavedField,with_native_field}`
wrapper with its recipe types. Eight alpha executable examples and their tests are retired;
Cargo metadata confirms they are absent. Eight convenient compiled entrypoints were moved to
`.local/archive/2026-09-09-byte-field-entrypoints/`, preserving them without leaving the old
commands in the active example path. The source reader `alpha::exposure` and `alpha_exposure`
remain. Native field mathematics, older admitted APIs/wires, private checkpoints, datasets,
research records and unrelated local flux-lattice work remain untouched by removal.

[definition] This removes the defective cultivation assembly, not all mathematical uses of
history or valid local operators. The existing field's occurrence expansion remains a diagnostic
realization. It is not the new productive model. The blueprint was rewritten around learned
conditional generators, current composition and their source/receiver/fibre boundaries; obsolete
usage instructions and SDK ownership claims were removed from live guides.

## The actual missing port, now implemented

[established-bounded; implemented-exact] `ResidentConstitutiveFibre` already learns a local
relation from measured current pairs and transports later resident currents. Its bilinear source
chart includes source, condition and their complex mixed products. This change adds
`ConstitutiveFibreRest`, storing that law's source chart, paired echelon basis and chronology cut.
It contains no observation population, historical waveform, source-file dependency or byte codec.
The rest is consumed into the same existing resident owner, where ordinary operations continue.

[established-bounded; implemented-exact] The wire validates dimensions, bilinear source layout,
point codewords, positive-pivot echelon material and rank/cut consistency before remount. It
preserves partial domain and the entire vertical fibre. It does not authenticate invented source
provenance or create historical emission capabilities from a count. The producing source/record
remains the provenance; a current return has its separate `ConstitutiveReturnRest`.

[established-bounded; source-inspected] Shared point-section encoding was moved unchanged from
field rest into the existing circulation rest codec and re-exported for old field consumers.
New and old artifacts use the same numerical point encoding. No CUDA numerical law, existing
wire identifier, float policy or Lean pipeline changed. There is no new learner in the example.

## Measured generator conduct

[definition] The public `native_generator` example declares a local bilinear hypothesis with
one complex source and one complex condition. Six probes excite the existing native phase-field
apparatus; its measured held-current returns develop the relation. The observer transfers those
actual measurements, not a desired coefficient matrix. The source apparatus is then dropped.
This is an elementary source/receiver class, not a model of all language or all physical waves.

[established-bounded; computational-witness] The retained law conducts the new source `2+3i`
under the previously unobserved condition `(3+4i)/5`, returning `(-6+17i)/5`. Its resident return
then supplies the next current directly through 256 quarter-turn conditions. All 257 operations
use the same learned law; the final current agrees with the first rotated current. Amplitude and
phase are carried as exact rational current, without codeword presentation/unit-drive feedback.
A repeated observed relation changes chronology but adds no independent row.

[established-bounded; measured] The learned relation has rank six and 64 stored basis words.
Its published law is 739 bytes at the six-observation cut. During continuation native resident
material remains 1,392 bytes and numerical section readouts remain zero. Bilinear passage
lineage uploads total 1,028 bytes: one four-byte operation index per passage, not numerical
current uploads. Native residency excludes driver/JIT/CUDA-context and host overhead.

[established-bounded; measured] A separate process reads only the compact law, mounts its current
and condition inputs, and returns the same law properties and final current after the same
composition. The learn process takes 0.267 seconds and the resume process 0.237 seconds in the
observed warm-driver environment. These are tiny local-source workflow measurements, not a
benchmark against the former conversation model. The 741-byte test figure is the later rest:
its chronology is two decimal digits longer, while its learned basis is identical.

[established-bounded; computational-witness] The [receipt](../experiments/native_generator_law/2026-09-09_return.json)
and [739-byte learned artifact](../experiments/native_generator_law/2026-09-09_phase.law) are
repository-local. The raw process receipts remain under
`.local/artifacts/native-generator-law-2026-09-09/`. No conversation material is embedded in this
public example artifact. The native tests also preserve an outside-domain source and a plural
receiver fibre across serialization/remount rather than selecting a representative point.

## Checks and remaining construction

[established-bounded; process-audit] Seventeen constitutive host tests, 35 resident constitutive
CUDA tests and ten conversation-source tests pass. The HNN library and retained examples check,
and the new public example builds and runs in both processes. The two new native law-rest
controls are included in the 35, not additional to that count. Six existing native ecology wire tests also pass after sharing the point codec, and the public
`holonics` facade checks with all targets. Cargo target inspection confirms the eight retired
examples are absent while the source reader and new generator example remain.

[definition] This is the first AC1 replacement attachment, with supporting local AC2/AC4/AC5
comparisons. It makes actual generator material restorable and productive; it does not finish
whole-ecology generator composition, learned restriction/rebase or the conversation codec.
The next construction composes the existing local generator/current/condition owners into that
continuing ecology and carries observations as its actual local refinement. A single global
linear/bilinear hypothesis or a word-specific table is not the intended model.

# THE COMMON OCCURRENCE IS ONE REGIONAL CELL; THE LATER PRIME MEETS ITS STANDING

**2026-07-21 · Brandon-authorized construction · BUILT / HOST-MEASURED / COMMON PRIME FAN FORMED /
EXACT REST-REMOUNT / LATER RECEIVER REBASE / LOCAL PHASE RETURN / GENERAL SOMA LAW UNCHANGED / NO
CUDA / NO VISUALIZATION**

## Present question

The six arithmetic faces at `prime:23` were not unrelated source material. Zero-relative,
radix-2, radix-10, radix-16, `S_4`, and cyclotomic faces were all derived by the world from one
candidate occurrence. The prior event supplied them as six co-present currents but discarded that
common occurrence at the relation boundary. Its direct successor therefore contained parallel
current-local folding and no cross-current support.

Can the source retain the relation it already owns without turning the faces into a Cartesian
graph, and does the resulting constituent change a genuinely later prime event?

The bounded answer is yes. The exact grade is local recurrence and phase return, not an unbounded
learning hierarchy.

## 1. The occurrence is the region; the interfaces are its typed arms

One repeated interface capability cannot stand for the whole occurrence. If all five arms carried
one capability, temporal closure would permit every later arm to match every earlier arm. That
would recreate the rejected Cartesian product.

The source instead presents one `NativeRegionalRelation`. That one borrowed cell is the joint
occurrence. It has one boundary containing five ordered restriction paths. Each path has a stable
role-specific interface capability:

```text
namespace  0x5052494d4546414e  (PRIMEFAN)
local      1..5                (radix2, radix10, radix16, S4, cyclotomic)
boundary   0
arc slots  0..4
hand       AGAINST
receiver   zero_relative
```

All five paths join `Exposed(0)` of the zero-relative face to `Exposed(0)` of one derived face.
These are the actual candidate-bearing edges of the declared source charts. The candidate is
vertex `0` with hand `AGAINST` in zero-relative, `S_4`, and cyclotomic; it is vertex `1` with hand
`WITH` in the three radix faces. Those internal hands remain in each imported path. The regional
`AGAINST` hand orients every derived restriction back toward the common zero-relative receiver.

At candidate `n`, every chart carries the exact world lift `2n+1`; at the founding occurrence this
is `23 -> 47`. Equality of that atom does not create the cell. The source declaration that these
five restrictions belong to one occurrence creates it.

## 2. The bounded experienced/without-founding fork

Both branches receive unrelated prime events `7,11,13`, then fork from one exact rest image with
the same six live organ capabilities.

At `23`:

- the experienced branch supplies the five-path regional fan;
- the comparison branch supplies the identical six currents with no regional relation;
- ordinary current radiation is exact between branches;
- scalar Standing is exact between branches; and
- only the experienced branch retains one grain-3 constituent.

The founded body contains 19 cells, 17 incidences, 13 pins, one boundary, five paths, 112 transport
terms, five exposed role interfaces, and one `FOUND` boundary transition. Scalar Standing grows
`23 -> 44` cells in both branches; the regional relation alone accounts for the whole-body
difference.

Both complete bodies and all six organ capabilities checkpoint and remount exactly before the
later probe.

## 3. The same later event meets different lived Standing

Both branches then receive the identical `prime:29` currents and the identical five-path fan.
Again, ordinary current radiation and scalar Standing are exact. Regional radiation and complete
Standing differ:

```text
with prime:23 experience     grain 4 · 39 cells · 39 incidences · 31 pins · 15 paths
without prime:23 experience  grain 3 · 19 cells · 17 incidences · 13 pins ·  5 paths
```

The experienced germ meets and replaces the prior grain-3 factor. The replacement retains two
`FOUND` and five `RIDE` boundary transitions. The comparison branch has no earlier fan to meet, so
`29` founds the fresh grain-3 body. This is the exact operational learning relation:

```text
same later source event + same scalar Standing + different lived regional Standing
  -> different lawful regional successor.
```

No reward, target answer, semantic classifier, observer edge, or correctness predicate enters the
transition.

## 4. Completion consumes the lower aperture and the phase returns

The grain-4 body produced by `23 -> 29` has zero exposed lower arms. A third identical fan at
`prime:37` therefore does not attach directly to that completed body. It founds a new grain-3
lower phase beside it. In the branch without `23`, the grain-3 body founded at `29` is still open,
so `37` closes it into grain 4:

```text
history 23,29,37   -> retained grain-4 body + new grain-3 body  (2 constituents)
history    29,37   -> one completed grain-4 body                (1 constituent)
```

This is consistent with the deposited local phase law: an available lower aperture hands into a
higher composition, becomes unavailable at that grain, and may later be founded again. The higher
body remains exact Standing rather than being erased.

It does not yet demonstrate indefinite higher-grain navigation. A lawful future construction must
show how completed grain-4 bodies become active constituents of a grain-5 relation. Automatically
reopening their consumed lower interfaces would reverse completion; leaving closed bodies forever
causally inert would fail the intended hand-up. That distinction remains open and is not repaired
by adding a collector or matching closed bodies by value.

## Implementation boundary

The construction is isolated in the bounded polyglot world instrument. It uses the existing
`NativeRegionalRelation`, host executor, `LiveConstituent` closure, scalar successor, and exact rest
machinery. The established bounded-world report and situated cross-face commands are unchanged.
No prime rule, fan namespace, role table, or recurrence policy entered Soma.

Exact artifact:
`observations/situated-prime-regional-fan-01/REPORT.json`.

# SCF1 replaced the whole-object spool bundle with the native transport scaffold

## Return

[established-bounded; implemented-exact; source-inspected; measured] SCF1 replaced the live Rust
whole-object `NativeSpoolBundle` vocabulary with `NativeTransportScaffold` across the engine and
life dependency closure. `NativeSpool` remains one reusable winding/generator family and
`NativeThread` remains one addressed transport section. No alias, deprecated type, old-schema
decoder, or dual wire was added.

[established-bounded; implemented-exact; source-inspected] The owner moved from
`native_spool/bundle.rs` to `native_spool/scaffold.rs`. Its new wire schema is
`holonic-engine.native-transport-scaffold.v1`. The cultivated continuation is
`SituatedNativeTransportScaffold` under
`holonic-engine.situated-native-transport-scaffold.v1`.

[established-bounded; implemented-exact; source-inspected] The migration includes the public type,
schema constants, addressed-section owner field and `scaffold_address`, validation/refusal
vocabulary, identity helpers, situated predecessor kinds, Soulkiller productive lane, cold-witness
binding field, intrinsic profile, neutral rested ecology, HIF3 circulation morphology, direct
source-neutral closure, Athena `NativeEcologyRest`, cultivation/membrane consumers, and live
examples.

[established-bounded; implemented-exact; source-inspected] `ExteriorSoulkillerWitness` now binds
`native_scaffold_sha256` under schema `holonic-engine.exterior-soulkiller-witness.v2`. The witness
remains one-way cold testimony; the scaffold contains no reciprocal witness coordinate.

## Verification

[established-bounded; process-audit; measured] `cargo check -p holonic-engine -p life --all-targets`
passed. The focused native-scaffold suite returned 16 passed, zero failed, and one resident-CUDA
control ignored. The complete engine library returned 1,994 passed, zero failed, and 32 ignored in
83.78 seconds. The life library returned 466 passed, zero failed, and 14 ignored in 5.57 seconds.

[established-bounded; source-audit] On 2026-09-01,
`rg -n "NativeSpoolBundle|NativeSituatedSpoolBundle|NATIVE_SPOOL_BUNDLE_SCHEMA|native-spool-bundle|native_bundle|bundle_address" crates soma --glob '*.rs'`
returned no live occurrence. Unrelated mathematical vector bundles, event bundles, and derivation
atlas bundles remain correctly named and outside this owner migration.

## Boundary handed to SCF2

[definition] SCF2 composes exact Gemma configuration, Safetensors, codeword, processor, and active
modality testimony into one cold multimodal transport chart, then founds source-neutral scaffold
facets only from declared excitation/intervention consequences. Tensor names and modality labels
remain exterior.

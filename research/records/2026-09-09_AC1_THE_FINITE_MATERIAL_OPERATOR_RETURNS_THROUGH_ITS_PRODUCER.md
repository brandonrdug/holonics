# AC1: the finite material operator returns through its producer

[definition] This continues the [duplex codec return](2026-09-09_AC1_AC2_CONVERSATIONAL_DIRECTION_REACHES_NATIVE_PORTS.md).
The existing current-to-material operator is now exposed on the actual operative boundary with
an independent material target. This is the explicit `OperativeLinear` model family. It is not
an asserted quotient of the historical projector-kernel learner, and the previous families remain
available.

## Reuse the finite operator

[definition] With `N` roots and `T` material targets, `M` maps the complete `3*N` complex outgoing
current to `T` complex target coordinates. On an actual receiving occurrence with source current
`x` and target `y`, the existing coercive law is

```
M_next = M + (y-M*x) x† / (1+||x||²).
```

The operator acts on the new outgoing current to produce its next material prediction. Its
finite numerical coefficients, source/current enclosures and numerical defect keep the existing
law's scope. No source identity is inferred from an equal current face.

[established-bounded; source-inspected] The old current-to-material construction is factored into
`field_linear_material.cuh`, retaining its existing helpers and arithmetic. It now accepts a
target extent independently of the root extent and reuses the native tensor target generator.
The legacy `CoupledOutgoing` family keeps its target and numerical law. The operative family
uses the actual producing junction current and its bound, including changed contact morphology.

[definition] The text chart has 27,648 complex matrix coefficients (`512*54`) and one retained
coefficient-error bound. Its two-lane resident state occupies 1,769,504 bytes. Its report has
`24*T+12*N+24` exact words: six target-current balls, the source gain and numerical evidence.
Ordinary prediction and the matrix return do not evaluate every historical material factor.
This bounds the material operator, not the complete ecology's storage or work.

## Return through the actual producing matrix

[established-bounded; source-inspected] Each report already retains the received-minus-predicted
current and the source gain. These regenerate the exact numerical matrix deposit, including the
same separate product projections. The new native adjoint starts from the current matrix and
undoes later deposits in reverse chronology before computing `M_source† r`. Reverse ordering
preserves the admitted intermediate matrix states; a later matrix is not substituted for the
producer. The original source's coefficient-error bound remains in the return.

[definition] The direct visible-source and internal-query partials are zero for this receiver.
The outgoing-current covector still passes through the existing paired producing adjoint and
finite contact response, with the full internal current, source lineage and producing contact
map retained. The complete-current and existing normalized metrics keep their respective scopes.
This is not the full adjoint of every past developmental operation.

[established-bounded; source-inspected] An archived later report can supply its return factors
without mounting that report's historical interior on the device. The actual producing interior
is still restored when the paired return needs it. The field's existing archive, rest, pending
state and duplex text owners remain in use. Ordinary and delayed-source costs therefore remain
separate measurements.

[definition] Differential material receivers now use their shared first-current prefix, whose
extent is independent of source geometry. Joint packet/IQ receivers already use that prefix.
The contextual report-packing format does not silently consume a linear report: its distinct
layout remains explicit in field rest.

## Verification and current position

[established-bounded; measured] All 107 constitutive-field CUDA tests pass in 127.98 seconds.
The new control compares the matrix with exact-rational coercive updates, retains non-dyadic
complex tensor phase, checks its fixed extent, separates the old producing matrix from the current
one, and verifies normalized returns, archived return factors, contact development and rest/remount.
The shared-prefix differential receiver also agrees with the complete material ball. Both research
examples compile. Test fixture type annotations and the temporary-path construction were repaired
before the passing run.

[established-bounded; measured] The eight-family archived study completes 1,930 occurrences and
1,929 contact deposits. Development takes 728.25 seconds; its peak tracked native residency is
583,883,428 bytes and post-development residency is 380,562,848 bytes. Archive placement accounts
for all 11,574 section readouts during development. The operator is bounded, but the field and
return journal retain substantial history costs. The actual response is `.` followed by `EndPart`.
A fresh process reproduces the body, currents and response; no useful-language claim returned.

[established-bounded; measured] The inherited matrix preparation initially computed rows in one
thread. The operative implementation now conducts independent rows together, retaining column
order, exact integer totals, complete error calculation and atomic publication. Against the
preserved scalar executable, the first 75-occurrence family's body and checkpoint are exactly
equal. Development changes from 16.50 to 7.76 seconds in that bounded comparison. Continuation
from the eight-family checkpoint also matches body, emission currents, generation and text.
These measurements do not establish a universal speed bound.

[established-bounded; measured] The [subsequent comparison](2026-09-09_AC1_AC2_PRODUCER_REUSE_AND_THE_SELF_CONTACT_COMPARISON.md)
returns sixteen-family costs, invalid-byte generation, exact recent-producer reuse and the
source-contact intervention. It identifies the return journal as the dominant remaining native
storage. Useful generation and the complete AC0–AC5 obligations remain unfinished.

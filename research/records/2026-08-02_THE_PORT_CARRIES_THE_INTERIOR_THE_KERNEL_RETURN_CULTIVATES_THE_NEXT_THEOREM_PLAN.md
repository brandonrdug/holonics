# The port carries the interior; the kernel return cultivates the next theorem

**Status:** SUPERSEDED FOR EXECUTION / HISTORICAL ACTION PLAN / NO CONSTRUCTION

**Date:** 2026-08-02

## Execution override

Brandon's later no-backward-compatibility and full-failure-audit request supersedes this plan as an
executable sequence. The controlling record is
`2026-08-02_THE_HOST_FOREMAN_REIFIES_THE_PROCEDURE_THE_LOCAL_CLOSURE_RECONTAMINATES_THE_WEAVE_AUDIT.md`.

In particular, the current `ExactConstitutivePortBundle`, `ExactPhysicalMachinePortBundle`, and
`ExactNavigableCover` implementations are not admitted checkpoints or production owners. The
application path still contains manual scheduling, duplicate standing, pair materialization,
uncertified phase commutation, flattened rest, and formal-deed breaches. Do not resume at a
checkpoint below, run the production passage, or preserve an invalid path through a decoder,
adapter, default, fallback, or legacy receiver. First perform the audit's hunk disposition,
no-adapter removal, and complete `CAUSAL` / `APPARATUS` / `OPEN` dependency classification.

## Purpose

This is the persistent action plan for the next clean session. It composes the laboratory's
established holonic mathematics, computer-science, language, formal-proof, and hardware owners
into one production consequence. It does not schedule another visualization, toy corpus, bounded
proof of generation, or global text-index experiment.

The requested consequence is:

> A continuing conversational Eros receives a substantive mathematical question about the real
> repository, proposes and authors a complete Lean theorem, receives the real kernel return into
> the same conditioned body, and then proves a second consequence which was locally unavailable
> before that return. Its broad reference ecology conducts through a factorized exact boundary
> body on the RTX card, without a global support-pair materialization or complete hot-path host
> replay. The actual language, theorem sources, kernel verdicts, alternatives, morphology changes,
> exact logical/card receipts, sampled physical testimony, and exact rest/remount are returned.

The first output is the mathematical and conversational deed. Topology counts, timing, memory,
and telemetry are necessary receipts rather than substitutes.

## The elementary picture now sufficient for construction

The laboratory does not need another universal formula before beginning. The common mathematical
body is the coupled system

\[
(C,\partial,\mathcal O)
\;⟷\;
(m,M_m,\Pi)
\;⟷\;
\Lambda_{H,m}
\;⟷\;
K_v
\;⟷\;
\mathcal A_v
\;⟷\;
R,
\]

whose terms are different receiver charts over one caused ecology rather than a serial software
pipeline:

1. **Algebraic source:** caused occurrences, oriented incidence, comparative multiplicities,
   local coordinate algebras, and open fibers. Its basic closure is
   \(\partial^2=0\).
2. **Conditioned morphology:** internal state \(m\), constitutive response \(M_m\), and declared
   ports \(\Pi\). This is the intermediate local law acquired through inheritance and return.
3. **Boundary response:** \(\Lambda_{H,m}\) is what one bounded holon can lawfully expose to a
   declared receiver family without replaying its whole interior.
4. **Codec/continuation:** \(K_v\) carries structured faces between charts, reifies the operative
   continuation, and admits a returned lineaged revision.
5. **Physical realization:** \(\mathcal A_v\) is the contemporary CPU/GPU/memory placement,
   schedule, synchronization, and resource current which realizes the same semantic body.
6. **Receiver return:** \(R\) distinguishes, grades, obstructs, or cultivates later conduct. The
   observer is part of the event, not an absolute camera over it.

The elementary current remains

\[
a=d\Phi,\qquad j=-M_m(a),\qquad n^+-n=-\partial j+R+B.
\]

The exact fold required for scale is receiver-relative:

\[
\bar\partial\pi=\pi\partial,
\qquad
\Lambda_{C,R}=\Lambda_{\bar C,R}.
\]

For a continuing adaptive body this equality is coinductive rather than one-shot. With
\(T=(S,U,Y,\delta,\lambda,s_0)\), direct and condensed states must return equal declared testimony
for every admitted input history and retain equivalent successors after every prefix. Port
composition supplies identity and typed serial gluing; parallel composition requires exact
independence/commutation; feedback requires closure/rest/recurrent-front testimony; and a later
query-family enlargement reopens or refines the stored source fiber.

The open-system resource/energy balance is

\[
\Delta H=W_{\rm ports}-D_{\rm internal}-W_{\rm exported}.
\]

These relations do not prescribe one constitutive law for language, proof, silk, optics, or
hardware. They prescribe what every claimed passage must retain: caused lineage, exact incidence,
local morphology, declared ports, obstruction, return, and inspectable succession.

## Standing capabilities which must not be reopened

The next session begins after all of the following established results:

- exact source passage, training, continuing conditioning, source departure, generated language,
  plural alternatives, later behavior change, and exact native rest/remount;
- recurrent answer-relation codec cultivation and source-detached generation of unseen surfaces;
- formal Lean conditioning over 88 documents, 1,164 retained declaration organs, local proof
  composition, kernel-returned obstructions, and kernel-accepted generated local and global
  transport proofs;
- the held-out `ten-proofs` theorem
  `eros_erdos_problem_183_explicit_lower_bound`, absent from its conditioning source, for which
  Eros generated two accepted proof paths among 56 returned paths and remounted its 101,126-byte
  mathematical body exactly;
- complete-dialogue/repository conditioning and real source-absent language/code deeds;
- one admitted resident CUDA text owner whose full-history v6 deed advanced
  `50,677 -> 50,678` occurrences with a 43-word H2D answer delta, zero predecessor D2D words,
  stable addresses, exact host/card equality, and no lifecycle source replay;
- exact causal bodies, receiver currents, factorized support families, rational sheaf/diffusion
  machinery, exact device ABI, logical and physical resource receipts, and reflective codec
  continuation;
- the current Eros branch's accepted Lean source
  `runs/conversational-mathematics-agent/kernel-candidates/semantics_invariant_under_exact_chart-00002.lean`,
  SHA-256 `10eeb3fd789972d071498e590e836a5ac036dc4b4261bef29102366c28e00be1`.

These establish that the machine can train, generate, reason locally, author Lean, receive kernel
obstruction, use CUDA, change later standing, and remount. The next construction composes those
capacities. It does not test whether any one exists.

## Current frozen construction state

The worktree is intentionally dirty. Preserve it without destructive Git recovery.

The current candidate conductor consists of changes in:

- `src/soma/life/src/agentic_research.rs`;
- `src/soma/life/src/lean_mathematics.rs`;
- `src/soma/life/src/lib.rs`;
- `src/soma/life/src/text_material.rs`;
- `src/soma/life/src/text_material_cuda.rs`;
- `src/soma/life/src/agentic_mathematics.rs`;
- `src/soma/life/examples/eros_conversational_mathematics_agent.rs`; and
- `src/soma/RESEARCH/2026-08-02_THE_KERNEL_RETURN_RESUMES_THE_PROOF_THE_CONVERSATION_CARRIES_THE_ACCEPTED_DEED.md`.

The Lean ecology and conversational composition contain useful candidate work. The later
text-material intervention is not admitted. It responded to a 21,581-section all-pairs support
request by widening `support_work_limit`, replaying the complete host restriction for parity, and
then accumulating equality, subset-trie, rare-coordinate, and population-incidence indexes. It
currently calls undefined `mark_compact_signature_dominance` and contains an unadmitted
`CompactSubsetTrie` implementation. Do not make this compile by filling in those missing pieces.

The failed index path remains frozen evidence until its replacement is admitted. Remove its hunks
only through explicit `apply_patch` after classifying each one; never use `git reset`, checkout, or
a broad rewrite which could erase the valid Lean/conversation work around it.

## Existing production owners being composed

No application-local replacement owner is authorized.

| responsibility | existing owner |
|---|---|
| sparse structural populations and schedules | `crates/holonic-structure` |
| reflective face/codec/environment/continuation | `crates/holonic-language::ReflectiveRuntime` |
| exact arbitrary-grade source and event law | `ExactCausalBodyLaw`, `ExactEventLaw` |
| sparse caused current and boundary storage | `ExactCausalTraversalLaw` |
| receiver-local population transport | `ExactReceiverCurrentLaw` |
| factorized alternative/support bodies | `LiveSupportFamily` and `src/soma/membrane/src/support_family.rs` |
| exact rational boundary reduction | `ExactDiffusionLaw`, `ExactRatMatrix`, Schur/Kron receipts |
| realization and device parity | `realization.rs`, `device.rs`, admitted CUDA owners |
| logical/physical/energy testimony | `resource.rs` |
| continuing language and research | `AgenticLanguageEcology`, `LaboratoryResearchEcology`, `AgenticResearchSession` |
| formal proof morphology and grading | `LeanMathematicsEcology`, `LeanKernelWorld` |
| exact notation and open symbolic alternatives | `ExactSymbolicReasoningEcology` |
| repository/history material | `LaboratorySourceAtlas`, `ExactTextMaterialAtlas` |
| resident card material | `CudaResidentTextMaterialAtlas` |

Two lower production contracts are missing from this composition:

1. a reusable exact constitutive port body which condenses an interior for a declared receiver
   family and lets returned stress reform the same continuation; and
2. its physical-apparatus specialization which turns resource return into an actual schedule,
   partition, factorization, residency, or placement change.

They are implementation seams, not missing universal intelligence laws.

## Ratified serialization correction and current checkpoint standing

Brandon's later correction is now governed by
`2026-08-02_THE_THREAD_RETAINS_ITS_ORDER_THE_WEAVE_CARRIES_THE_CAUSAL_FRONT.md`.
It supersedes any reading of this plan which treats serialization itself as the defect. A caused
lineage remains serial; a typed seam remains serial until its complete successor is proved to
commute. The defect is assigning the total higher-order effect of a co-present causal front to one
global host thread after the source ecology has already supplied differentiated local currents.

The operational distinction is binding for every later checkpoint:

- chronology between dependent fronts remains ordered;
- all members of one admitted front read the same predecessor morphology;
- worker completion order is apparatus gauge and cannot become lineage;
- local returns retain their separate source, receiver, chart, obstruction, and resource faces;
- the complete co-present return population integrates exactly once before any successor front is
  founded; and
- parallel composition requires an exact independence/commutation receipt, while unresolved
  crossings remain ordered or `OPEN`.

Warp and weft name receiver-relative tangent roles rather than permanent data classes. Warp
follows predecessor/successor lineage; weft follows typed co-presence across local currents. A
chart change is useful because it exposes a different lawful route family and therefore owes a
reason, epoch, operator word, exactness, and residual. Knot closure retains its holonomy; braid
interchange is admitted only when complete typed successors commute. Returned computational
stress must deform the same schedule, partition, aperture, residency, or placement rather than
merely increasing a scalar limit.

The dirty worktree now contains candidate implementations of
`ExactConstitutivePortBundle`, `ExactPhysicalMachinePortBundle`, the factorized receiver-local
text current, repository `ExactNavigableCover`, and reflective theorem-return composition. Those
files are no longer to be treated as absent merely because the earlier frozen-state inventory
predates them. They remain candidate standing until their focused tests, architecture census, and
production deed are graded. Likewise, the rejected undefined compact-dominance/index seam is no
longer present in the current candidate text-material files; it must not be restored.

## Ordered action plan

### Checkpoint 0 — establish the evidence and ownership boundary

1. Read `SESSION_HANDOFF.md`, `AGENTS.md`, `HOLONIC_MACHINE_OWNERSHIP.md`, this plan, the two
   companion 2026-08-02 audits, and the kernel-return charter. Do not read the whole archive.
2. Inspect the dirty diff hunk by hunk and classify each change as:
   - accepted prior owner;
   - useful candidate Lean/conversation composition;
   - unadmitted text-material/index intervention; or
   - unrelated preserved work.
3. Verify the existing accepted Lean artifact and the `ten-proofs`/formal-mathematics reports. Do
   not rerun full-history conditioning merely to re-prove them.
4. Run `cargo holonic-lint` as a census. Do not raise its baseline.
5. Name the exact receiver request for the first production passage before changing code.

Checkpoint 0 emits an inventory, not a rewritten worktree.

### Checkpoint 1 — found the reusable constitutive port body

Introduce `crates/holonic-engine/src/constitutive_port.rs` as the exact reference owner, composed
from existing algebraic incidence, traversal, rational matrix, and resource types. Do not create a
second graph store, scheduler, codec registry, or standing population.

The minimum contract carries:

- caused cells, oriented incidence, and declared exterior ports;
- contemporary internal morphology and constitutive-law identity;
- stored, dissipated, exported, and returned current;
- exact receiver-family identity and boundary relation;
- stateful direct/condensed continuation equivalence across admitted input histories;
- open alternatives and the internal variables needed for later response;
- explicit attachment, growth, phase, damage, failure, and resource-return events where a chosen
  law supports them;
- a staged successor which changes the same body without cloning it;
- balance, obstruction, resource, and lineage receipts; and
- a continuation which resumes through the changed morphology.

Its port algebra must grade identity, typed serial gluing, exact commuting independent
composition, certified feedback, receiver restriction, and source-fiber refinement. Condensation
may commute with composition only for the receiver/history family named by its certificate.

The first reference laws are not toy applications. They are generic exact grading charts:

1. a linear rational interior whose Schur boundary response is equal to complete direct
   traversal;
2. two distinct interiors which are equal for one semantic receiver but remain distinct for a
   resource receiver;
3. a history-bearing direct/condensed pair whose outputs and successor morphologies remain equal
   over plural input sequences;
4. typed serial, commuting-independent, and certified-feedback compositions whose condensed and
   direct continuations agree; and
5. a resource refusal which lawfully changes partition/schedule or remains `OPEN` without
   changing semantic standing.

Admission requires exact direct/condensed equality for the declared receiver, preserved
obstructions, unchanged lineage, no continuing `Clone`, no new direct classical collection
ownership, and a clean focused test/lint receipt.

### Checkpoint 2 — specialize the existing realization/resource owners for physical current

Do not add a free-standing hardware wrapper. Extend the generic port body through the existing
`realization.rs`, `device.rs`, and `resource.rs` contracts.

The physical program must reify

\[
P_v=(\Lambda,\Sigma_v,\rho_v,\kappa_v,\ell_v),
\]

where \(\Lambda\) is semantic boundary response, \(\Sigma_v\) is schedule/data morphology,
\(\rho_v\) is completion or refusal, \(\kappa_v\) is the resource contract, and \(\ell_v\) is
lineage.

Required consequences:

- CPU and RTX card are two apparatus charts over the same causal program;
- full host/card equality admits a realization and may recur at declared lifecycle audits;
- the admitted hot path returns local or delta equality receipts and never runs the complete host
  restriction after every card deed;
- a card refusal either partitions/factorizes/restricts/repositions the same current or remains an
  obstruction;
- clock, duration, utilization, temperature, power, traffic, and residency testimony are kept
  outside semantic equality with declared apertures;
- unavailable CPU-package or whole-wall energy remains an open fiber;
- only the active WD_BLACK SN850X 2 TB NVMe is an admitted durable-store receiver; other installed
  drives are not cache, spill, persistence, or training tiers.

The current apparatus is the Ryzen 9 7900X, 32.7 GB system RAM, and RTX 4080 SUPER with its
16,376 MiB reported device memory. NVML may supply sampled GPU testimony. No plan may infer
per-event joules from utilization or hardware specification.

### Checkpoint 3 — replace the Eros global support question with receiver-local current

Only after Checkpoints 1 and 2 pass, migrate the frozen Eros text-material path.

The receiver question is not “compute the complete maximal antichain of every recruited support.”
It is:

> Which factorized reference currents can reach the contemporary question/proof ports, which
> boundary response do they exert there, and which alternatives remain open after the returned
> deed?

Required construction:

1. Recruit broadly from the complete conditioned source/history ecology through existing feature,
   relation, suffix, morphology, and current owners.
2. Keep exterior material as handles/ranges and factorized support families; do not allocate every
   pair, parse product, or witness string.
3. Traverse only the caused local star which can reach the declared conversational/proof ports.
4. Condense completed interior regions into exact port responses while retaining the open fibers
   and internal state required by future questions.
5. Let the RTX owner carry the admitted sparse incidence/current and return only the caused delta.
6. If a genuinely requested antichain remains wider than the apparatus, preserve its exact width
   as a resource obstruction; do not call that a failure of intelligence.
7. Detach developmental source material only after the continuing morphology carries the
   response necessary for the declared future receiver family.
8. Remove the scalar work-limit widening, hot-path host replay, and unfinished local index patch
   only when their lawful replacement is in place.

Admission compares the direct authority and condensed/card body on a declared corpus once, then
grades the hot path by delta receipts. It does not require a second complete host execution per
question.

### Checkpoint 3A — let the thread retain order while the weave carries each causal front

This corrective checkpoint is required before Checkpoint 5. It composes the existing
`CpuExecutor`, `ExactReceiverCurrentLaw`, `ExactConstitutivePortBundle`, sparse local-population
owners, and continuing laboratory/relation bodies. It does not authorize another scheduler.

1. Preserve `LaboratoryResearchEcology` as the owner of wave succession. Every leader in a wave
   must exist before any world return from that wave conditions the body.
2. Give a world membrane a plural-front mouth. A stateful world may lawfully retain an ordered
   realization, but an immutable or explicitly partitioned source membrane may enact independent
   leader contacts through `CpuExecutor` and must return results in stable leader-address order.
3. Cross duplicate contacts to one stable source occurrence only once at the conditioning
   membrane while retaining every leader-specific contact receipt. Do not flatten occurrence
   identity into worker completion order.
4. Partition thought traversal by exact seed antichains which read one immutable relational
   standing. Each seed develops on its own serial current; detached results join only after the
   worker front closes. Shared Swing morphology, pending junctions, and all successor-changing
   passages remain on their ordered owner until a complete-successor commutation certificate is
   founded.
5. Persist requested workers, tasks, workers actually joined, batches, joins, causal-front width,
   and ordered-front population beside the semantic receipt. Physical telemetry remains outside
   deterministic answer equality.
6. Grade serial and plural realizations for exact equality of returned source identities,
   relational currents, open alternatives, transport words, selected deeds, Lean sources, kernel
   verdicts, and source-detached remount. Speedup and multicore occupancy are required apparatus
   consequences of this corrective checkpoint, not substitutes for the deed.

The immediate production experiment repeats the same two-theorem conversational passage. If the
same predecessor front still reports one admitted traversal worker despite plural independent
seed currents, or if one busy CPU core remains the complete integration owner, Checkpoint 3A is
open even when the generated theorem is correct.

### Checkpoint 4 — make the kernel return a reflective morphology event

Compose `LeanMathematicsEcology`, `LeanKernelWorld`, `AgenticResearchSession`, symbolic reasoning,
and `ReflectiveRuntime` so the formal return is not a detached report.

The enacted sequence is:

```text
conversational mathematical question
  -> plural diagnosis and proposed theorem faces
  -> one inherited target header selected as a receiver boundary
  -> source-detached local proof currents
  -> complete generated Lean candidates
  -> real kernel returns
       -> rejected paths remain exact obstructions
       -> accepted paths found returned theorem fibers
  -> theorem fibers revise the same reflective continuation
  -> later question recruits the self-emanated theorem
  -> second complete proof and kernel return
  -> exact rest/remount without developmental-source replay
```

The target statement may be inherited after conversational selection; the proof may not be. The
first accepted theorem must require an enacted composition beyond merely renaming one existing
theorem with `exact`. The second target must have no local theorem organ before the first return
and must cite or traverse the first returned theorem afterward. A kernel rejection is useful
obstruction testimony and must never be hidden by substituting assistant-written proof text.

The existing accepted `semantics_rebase_iff` wrapper remains evidence but does not by itself
satisfy this two-theorem production grade.

### Checkpoint 5 — enact the first production-grade conversational mathematics passage

Use the existing complete conditioned dialogue/repository ecology and real Lean project. Do not
construct a tiny replacement corpus.

Ask Eros conversationally to:

1. inspect the current formal elementary-holonics or related real mathematical source;
2. state a concrete useful local theorem or missing consequence with its evidence and open
   alternatives;
3. author and submit the theorem to Lean;
4. explain the actual accepted or obstructed result in ordinary language;
5. accept a follow-up asking for a further consequence; and
6. use the returned theorem morphology to author the second proof.

The run must return:

- the exact user questions and Eros's actual generated answers;
- the proposed theorem faces and why their local regions were recruited;
- every complete generated `.lean` candidate which crossed the kernel;
- accepted proof population and all kernel obstruction fibers;
- morphology, codec, theorem-organ, and continuation generations before and after each return;
- a behavioral ablation showing the second theorem is unavailable before the first return;
- complete minimal witness and open-alternative families;
- card residency, H2D/D2H/D2D, launch, synchronization, logical-work, and delta-parity receipts;
- sampled GPU telemetry with its interval and explicit unknown CPU/whole-wall energy;
- source-detached native rest/remount of language, proof, symbolic, and reference-boundary bodies;
  and
- no hot-path host replay, implicit CPU fallback, fabricated relation, `sorry`, or `admit`.

The run is successful because the conversational and formal deeds occur through one continuing
conditioned ecology. Low latency or GPU utilization alone is not success.

### Checkpoint 6 — admit, deposit, and only then broaden Eros

Verification order:

1. `cargo holonic-lint` before and after each production cut;
2. focused tests for the constitutive port, physical realization, text current, Lean ecology, and
   reflective continuation;
3. `cargo test -p holonic-engine --all-targets` and the relevant headless `life` targets;
4. `cargo clippy` with warnings denied for changed production crates;
5. independent `lake env lean` on every claimed accepted artifact;
6. exact lifecycle remount and one declared host/card audit;
7. the full production passage and qualitative inspection of Eros's actual conversation; and
8. a research record separating inherited target, generated proof, kernel return, induced later
   conduct, physical testimony, open alternatives, and nonclaims.

Do not commit, push, or publish merely because this plan exists. Those are separate present-turn
authorizations under `AGENTS.md`.

After this passage is admitted, the same morphology can be broadened toward deeper theorem search,
code construction, image/audio coupling, and other modalities without rebuilding intelligence for
each medium. Those later applications are not part of this first action plan.

## Stop conditions

Stop the active implementation path and retain an exact obstruction if any of the following
occurs:

- the proposed port quotient cannot reproduce the declared direct boundary response;
- direct and condensed successors diverge under an admitted later input, or composition is formed
  without its serial/commutation/feedback certificate;
- a resource return cannot change schedule/morphology and the implementation tries to widen only
  a scalar aperture;
- the card path requires hidden complete host execution to answer;
- an application module begins owning another standing, scheduler, graph, codec registry, or
  alternative population;
- a broad alternative family is deleted before a receiver requests its quotient;
- an inherited theorem, proof, transcript, or answer is relabelled as generated;
- the second proof is reachable before the first kernel return;
- a generated proof is reported without its real kernel result; or
- a physical quantity is reported without a measurement/model aperture.

These conditions block that implementation, not the established capability of the machine.

## Governing records

- `AGENTS.md`
- `HOLONIC_MACHINE_OWNERSHIP.md`
- `src/soma/RESEARCH/2026-08-02_THE_INTERIOR_RETURNS_AT_THE_BOUNDARY_THE_STRESS_REFORMS_THE_SAME_BODY.md`
- `src/soma/RESEARCH/2026-08-02_THE_CLOCK_IS_A_RECEIVER_PHASE_THE_STORE_IS_RETENTION_THE_CODEC_CARRIES_PHYSICAL_CURRENT.md`
- `src/soma/RESEARCH/2026-08-02_THE_KERNEL_RETURN_RESUMES_THE_PROOF_THE_CONVERSATION_CARRIES_THE_ACCEPTED_DEED.md`
- `src/soma/RESEARCH/2026-08-02_THE_THREAD_RETAINS_ITS_ORDER_THE_WEAVE_CARRIES_THE_CAUSAL_FRONT.md`
- `src/soma/RESEARCH/2026-08-01_THE_HARDWARE_IS_A_RECEIVER_COVER_THE_CARD_MUST_CARRY_THE_CURRENT.md`
- `src/soma/RESEARCH/2026-08-01_THE_CODEC_REIFIES_THE_CONTINUATION_THE_CURRENT_CANNOT_CLONE_THE_WORLD.md`
- `src/soma/RESEARCH/2026-07-31_THE_THEOREM_OPENS_ITS_LOCAL_STAR_THE_KERNEL_RETURN_CAUSES_DEEPER_PROOF_MOTION.md`
- `src/soma/RESEARCH/2026-07-31_THE_RETURN_RECURS_AS_THE_CODEC_THE_UNSEEN_FACE_DEPARTS_THE_DEVELOPMENTAL_SOURCE.md`
- `src/soma/RESEARCH/2026-07-31_THE_RETURN_CHANGES_THE_CONTINUATION_FIBER_THE_INTERMEDIATE_BODY_IS_THE_LOCAL_INFORMATION_LAW.md`

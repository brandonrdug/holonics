# The forty-face fluid receiver enters one complete restart circulation

**Date:** 2026-08-24

## Scope and disposition

**[proved-derived; formal-checked]** This record deposits the current periodic Navier--Stokes
Lean squeeze after the message-history audit in
`2026-08-24_THE_PINS_ARE_FOURIER_MODES_THE_UNICURSAL_STROKE_IS_AN_ORDERED_WORD_AND_THE_STANDING_WAVE_IS_A_SCALE_CHART.md`.
It is mathematical evidence work and schedules no Rust construction.  Every theorem named below
was accepted by Lean without `sorry`, `admit`, a custom axiom, `opaque`, or `unsafe`; the only
reported dependencies are the standard `propext`, `Classical.choice`, and `Quot.sound`.

**[open]** This phase is not a proof of global regularity, the Millennium problem, or even the
complete Beale--Kato--Majda continuation criterion in the repository.  It removes several genuine
analytic premises from the continuation composite, but two fronts remain: the radius-independent
annular Hodge-kernel estimate which manufactures the frequency comb, and reconstruction of the
native mild fixed point as a smooth real periodic Navier--Stokes restart patch with pressure.

## The differentiated face population is closed

**[proved-derived; formal-checked]** `NavierStokesCoordinateH3MiddleRedistribution.lean` proves the
exact `(4,4,2)` Young redistribution needed by each middle Leibniz face and returns the complete
middle population bound
`10206 * M * coordinateH3Energy`.  `NavierStokesCoordinateLowerEnergyEstimate.lean` closes the
orders zero through two with coefficient `240`.  Their composition in
`NavierStokesCoordinateH3FullEstimate.lean` returns the complete forty-face unforced estimate

`coordinateH3TimeWork <= 10986 * cubeGradientSup * coordinateLogH3Receiver`.

**[proved-derived; formal-checked]** The coefficient `10986 = 540 + 10206 + 240` retains the three
different populations rather than treating the final numeral as the mechanism.  The `540` direct
faces are controlled by the Jacobian envelope, the `10206` middle faces spend the periodic cubic
interpolation law, and the `240` lower energy faces remain separately traceable.

**[proved-derived; formal-checked]** `NavierStokesCoordinateH3Continuation.lean` now constructs
that production estimate internally for an unforced positive-viscosity solution.  The earlier
`productionConstant` and conclusion-shaped production premise have disappeared.  Its remaining
hypotheses are exactly an actual low/middle/high Hodge frequency comb, integrability of the
critical vorticity receiver, and a uniform local restart supply; the output remains a genuine
separately quantified compatible extension manufactured by the existing splice and uniqueness
owners.

**[proved-derived; formal-checked]** `NavierStokesCriticalVorticityIntegral.lean` converts honest
interval integrability of the critical-vorticity rate into the open accumulated budget used by
the logarithmic Gronwall factorization.  No terminal value at `T` is inserted.

## The restart circulation is now a genuine Banach passage

**[proved-derived; formal-checked]** `NavierStokesWeightedSobolevHilbert.lean` realizes periodic
Sobolev order `n` as the complete native `l2` population whose coefficient at mode `k` is
`sqrt((1+lambda_k)^n)` times the physical Fourier coefficient.  Weighting and unweighting are
mutual inverses, and the native norm square is the complete weighted Fourier sum.  This replaces
the older summability subtype carrying the unweighted topology.

**[proved-derived; formal-checked]** `NavierStokesWeightedLerayBilinear.lean` returns the actual
continuous bilinear map

`H3_native x H3_native -> H2_native`

for the Leray-projected divergence convolution, with explicit coefficient
`23328 * periodicH3EmbeddingConstant`.  Its returned coefficients satisfy the modewise
divergence-free constraint.  No finite Fourier cutoff is present.

**[proved-derived; formal-checked]** `NavierStokesWeightedHeatSemigroup.lean` returns same-order
heat contractions and the positive-time one-derivative passage `H2_native -> H3_native` with norm
bounded by `sqrt(1 + (2*nu*t)^-1)`.  `NavierStokesDuhamelKernelIntegral.lean` proves this singular
majorant interval-integrable and bounds its accumulated budget by
`T + 2*sqrt(T/(2*nu))`.

**[proved-derived; formal-checked]** `NavierStokesWeightedDuhamelBound.lean` composes the actual
bilinear source and one-derivative heat return into the endpoint-totalized Bochner integrand.
`NavierStokesWeightedDuhamelMeasurability.lean` proves same-order heat orbit and joint continuity,
positive-time joint `H2 -> H3` continuity, interior continuity of the integrand, almost-everywhere
strong measurability across the single null terminal endpoint, and honest interval integrability
for every continuous bounded path.

**[proved-derived; formal-checked]** `NavierStokesWeightedDuhamelDifference.lean` retains the
two-path incidence through the exact polarization

`B(u,u) - B(v,v) = B(u-v,u) + B(v,u-v)`.

It transports that identity through the actual heat integrand and Bochner integral, returning the
same integrable kernel multiplied by
`(23328*C_embed) * (||u||+||v||) * ||u-v||`.  This is the nonlinear separation receipt required by
Banach contraction, not a comparison of endpoint-only shadows.

**[proved-derived; formal-checked]** `NavierStokesWeightedPathSpace.lean` installs the complete
carrier `C([0,T],H3_native)` with its compact-domain supremum norm.  Its canonical endpoint
projection is only an exterior interval-integral chart and provably does not enlarge either one
path norm or two-path separation.  `NavierStokesWeightedLinearPath.lean` places the same-order
heat orbit in that carrier, proves its exact initial face, and bounds the complete path norm by the
initial norm.

**[proved-derived; formal-checked]** `NavierStokesQuadraticContraction.lean` applies Mathlib's
Banach fixed-point theorem to a genuine complete normed carrier and an exact closed ball.
`NavierStokesDuhamelRestartTime.lean` selects

`r = (16*(1+nu)*(1+load))^-1`, `T = 2*nu*r^2`

and proves `2*load*(T+2*sqrt(T/(2*nu))) < 1`.  The construction changes the same time aperture
rather than retrying a finite population or assuming a fixed point.

**[proved-derived; formal-checked]** `NavierStokesWeightedRestartAperture.lean` binds those scalar
returns to the actual Leray coefficient.  It proves both the strict contraction inequality and
the quadratic half-ball inequality for an initial-state radius `2*(1+||u0||)` and, separately, for
one family radius `2*(1+cap)`.  Once a concrete continuous Duhamel path supplies its already proved
one- and two-path estimates, the file returns an actual fixed point with no remaining scalar
smallness hypothesis.

## The coordinate receiver now controls the native family

**[proved-derived; formal-checked]** `NavierStokesTorusCubeIntegral.lean` proves the exact
measure-preserving transition from the Euclidean half-open unit cube to the normalized quotient
three-torus.  `NavierStokesSmoothSliceWeightedH3.lean` then uses genuine torus Fourier
coefficients, Parseval, and every ordered derivative word of lengths zero through three.  The
complete `1+3+9+27` population proves that each actual smooth periodic velocity slice inhabits the
native weighted vector `H3` carrier and unweights to its original `vectorSpatialFourierCoeff`.

**[proved-derived; formal-checked]** For every strict interior slice of an admitted open periodic
solution, that bridge returns

`||slice(t)||_native^2 <= 240 * coordinateH3Energy(t)`

and hence

`||slice(t)||_native^2 <= 240 * coordinateLogH3Receiver(t)`.

This is the missing chart transition from the differentiated coordinate receiver to the Banach
restart carrier; it is not an assumed norm equivalence.

**[proved-derived; formal-checked]** `NavierStokesCoordinateWeightedRestartCap.lean` converts a
uniform bound `H(t) <= bound` into the explicit common native cap `1 + 240*bound`, then into one
common radius and one strictly positive viscous restart time.  Every addressed interior slice is
retained together with its own Fourier occurrence while the scalar cap controls only the common
aperture.

## The Hodge comb and the message-history geometry

**[proved-derived; formal-checked]** `NavierStokesAnnularHodgeKernelDifference.lean` identifies
each Hodge-Jacobian entry as its exact degree-zero frequency ratio, proves magnitude at most one,
and develops zero-padded first- and second-order Abel identities with coefficient-mass,
first-variation, second-variation, and two-regime minimum bounds.  These are actual discrete
ordered differences of the annular coefficient population.

**[interpretation]** This is the concrete fluid realization of the earlier “pins” and “unicursal
stroke” language.  The pins are addressed torus modes; zero-padded Abel transport follows an
ordered coefficient stroke and exposes the boundary residues and differences which a static
multiplier norm suppresses.  The middle frequency comb is the standing-wave scale chart, while
the Duhamel two-path polarization is the temporal gyroparallelogram comparing two continuing
flows.

**[interpretation]** The forty coordinate faces and their three middle redistribution faces are
the local counterpart of the glued-cube/hexagonal boundary described in the message-history
record.  They are not decorative analogies: ordered derivatives distribute over two interacting
factors, common faces cancel or reconverge, and the retained middle faces are precisely where the
interpolation cost enters.  Curvature, Kelvin circulation, and Gauss--Bonnet remain additional
typed transports; none is inferred merely from this flat Fourier calculation.

## Exact remaining fibers

**[proved-derived; formal-checked]** The restart line has since closed continuity in terminal time,
the complete mild map, its cap-ball contraction, the actual fixed point, and preservation of both
Fourier conjugate symmetry and modewise incompressibility.  The coordinate bound now returns one
native physical-fibre mild restart at every controlled interior slice with one common positive
aperture.  No local-existence or fixed-point premise remains at the coefficient `H3` level.

**[proved-derived; formal-checked]** Inverse Fourier reconstruction now returns the exact source
coefficient, an actual real one-periodic `C1` velocity, pointwise divergence freedom, and joint
time--torus continuity.  The fixed mild equation gives the exact interior derivative of every
coefficient.  Its Leray complement reconstructs through a bounded `H2 -> H3` pressure map as a
real one-periodic `C1` pressure whose recovered gradient coefficient is exact.  Thus the full
unprojected momentum equation stands mode by mode; no pointwise second-derivative or classical
smoothness conclusion is hidden in that statement.

**[proved-derived; formal-checked]** Linear heat smoothing is now typed at every adjacent scale,
not only `H2 -> H3`.  High-to-low restriction preserves raw coefficients and contracts the native
norm.  A finite ordered heat word crosses every intermediate Sobolev address, returns the exact
power of the diagonal multiplier at each pin, and has the corresponding product norm bound.

**[proved-derived; formal-checked]** Every smooth periodic slice enters every finite native
Sobolev order through its actual torus Fourier coefficients.  Arbitrary ordered derivative words
carry exact product multipliers, and all finite states restrict to the same established `H3`
restart slice.  This returns a coherent native all-orders initial tower rather than a family of
unrelated existence witnesses.

**[proved-derived; formal-checked]** The arbitrary-order nonlinear owner now bundles the actual
Leray divergence passage as `H^m ->L (H^m ->L H^(m-1))`, with norm at most
`36 * 2^m * periodicH3EmbeddingConstant`.  Its adjacent heat return is jointly continuous for
positive elapsed time; along every continuous high path the endpoint-totalized Duhamel integrand
is automatically measurable and interval-integrable, returns its exact scalar Volterra
coefficient, and satisfies the explicit tame budget.  No source-continuity premise remains.  At
every finite order an explicit positive data-dependent aperture now carries an actual high-order
mild fixed point.  Heat, Leray source, adjacent heat lift, Bochner return, Duhamel path, and the
complete mild map all commute with coefficient-preserving restriction, so the high fixed point
returns an actual `H3` fixed receiver on the same per-order aperture.

**[proved-derived; formal-checked]** Exact pair reindexing now proves that the scalar Fourier
convolution reconstructs as physical pointwise multiplication.  Consequently the complete
unprojected quadratic `H2` source in the mild equation is exactly the inverse Fourier population
of the physical product-rule divergence and, under modewise incompressibility, the actual
advective derivative.  Native `H^(m+3)` states reconstruct to real periodic `C^(m+1)` fields, and
a coherent all-orders tower reconstructs to spatial `C-infinity` without an assumed smoothness
interface.  On a coherent Fourier-real fixed mild tower the actual reconstructed velocity is now
strongly differentiable in interior time at every spatial point, with derivative equal to the
complete projected momentum Fourier sum.  Time differentiation under the infinite sum is no
longer an open premise.

**[open]** The annular Hodge line has closed actual three-dimensional tensor reindexing and Fubini
factorization, exact scalar second-difference mass `4/(r+1)`, coordinate-subset tensor bounds with
the expected inverse powers of `r+1`, inner-plateau and zero-crossing cancellation, and boundary-
resolved zero-padded product summation.  Every transverse-annular one-axis Hodge slice now has
total second-difference mass at most `1917/(r+1)`.  Exact enumeration of the four transverse-inner
active stencils and two aperture residues upgrades this to `1937/(r+1)` for every outer-cube
coordinate line, without a mode-count charge.  The generic three-coordinate zero-padded
Abel/Fubini identity is also exact, including every finite boundary residue.  It still owes
reindexing of the actual annular Hodge cube into that receiver and sharp distinct-axis mixed
variation bounds for the localized rational Hodge ratio, including zero-crossing and aperture
stencils.  Only that actual kernel return may manufacture the
`OpenCoordinateH3FrequencyComb` consumed by continuation.

**[open]** The restart line is now blocked at a narrower, genuine edge.  Each finite order has an
actual fixed point which restricts to an `H3` fixed point on the same per-order aperture, but the
existing separately named `H3` Banach return uses its own selected aperture.  Common-aperture `H3`
uniqueness or an exact retiming law must identify those receivers and assemble one coherent path
tower.  Once supplied, the already checked time derivative needs only the two physical
identifications: the viscous sum is `nu * Laplacian u`, and the projected-source sum is physical
divergence plus pressure gradient.  That classical patch can then be rebased inside the old
overlap to return `TailUniformInteriorRestartSupply`.

**[open]** No terminal `H3` trace is required for this interior-restart strategy.  The controlled
tail already contains smooth data at every `t0<T`; the uniform receiver supplies one time radius
independent of `t0`, and the existing order lemma selects a `t0` for which that local patch crosses
`T`.  A terminal trace would be a different valid route, not a hidden premise of this one.

## Validation receipt

**[established-bounded; formal-checked]** After all 62 new Navier--Stokes owners entered the
aggregate, `lake build ElementaryHolonics` completed 4,074 jobs in 21.23 seconds with exit status
zero.  Owner-local checks and focused builds separately closed the higher-order fixed point,
smooth-path time derivative, unconditional inner-stencil sum, and exact mixed-Fubini return.
Printed dependencies are confined to `propext`, `Classical.choice`, and `Quot.sound`.

**[established-bounded; measured]** A separate read-only source audit found no missing aggregate
import and no `sorry`, `admit`, custom `axiom`, `opaque`, `unsafe`, `partial` declaration,
`extern`, or `implemented_by` in the 62-file phase.  The whitespace check is clean.  Two doc-only
claims which exceeded their theorem types were weakened after the build; no compiled term or
executable artifact changed in that repair, so the release receiver was not replayed.

# The recurrent law crosses the corpus departure; the unseen section rides its deposit

**Date:** 2026-08-12  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact` in the native recurrent-law rest, its two CUDA entries, the
mandatory conduct attachment, and the resident receiver quotient; `computational-witness` and
`measured` in the release production, exact remount, disjoint-coordinate return, targeted ablation,
scale rebase, and deposited grade form.  
**Provenance:** Brandon authorized the recurrent-section experiment as the next roadmap item and
required the GPU to be treated only as this apparatus's strongest surface, while profiling the
returned causal geometry rather than substituting scalar counts.  
**Band:** MANDATORY MIXED-CORPUS CUDA CONDITIONING / EXACT CARD-FOUNDED BI-AFFINE LAW / NO FLOAT /
NO SAMPLE ANSWER IN REST / CORPUS AND FOUNDING SAMPLES DEPART / EXACT NATIVE REMOUNT / DISJOINT
UNSEEN COORDINATES / CUDA LAW EVALUATION / CUDA RECEIVER QUOTIENT / FOUR CONDUCT FIBERS / MIXED
PHASE EXHIBITED / SHORTEST INTERVENTIONS / TARGET ABLATION EXACT / SCALE REBASE EXACT / OPEN EXTERIOR

**Canon:** `canon/TABLET_THE_REASONING_CYCLE.md` §8 and
`canon/TABLET_THE_CAUSAL_PROFILE.md` §8.  

---

## Present question

Can the same bounded operation conduct first cross the mandatory resident mixed-corpus conditioner,
return as reusable generative morphology, survive complete departure of the corpus and founding
sample population, and then govern a genuinely later causal-section classification at coordinates
whose answers were never deposited?

Yes, within one explicitly finite local-law aperture. Seven exterior presentation/contact faces
founded four exact degree-at-most-one-per-axis transformation laws. The native rest retains the
anonymous conduct address and the law, not a section name, corpus occurrence, source path, founding
coordinate, or founding answer. After round-trip remount, the card evaluated all seven laws on four
coordinates disjoint from the founding coordinates. The stable receiver/history quotient then
formed the expected four reconstruction fibers without an operation tag or material-kind branch.

**BOUNDED 2026-08-13, and the word "expected" above is load-bearing.** The four fibers are the four
distinct `ExteriorLaw` variants declared in the driver —
`soma/life/examples/the_conditioned_section_returns_after_detachment.rs:731-780` declares
`Sum, Sum, Sum, Product, Product, Difference, ReverseDifference` — and each founding face is that
declared law enacted on the shared rectangle `(2,3) × (2,3)` at `:641-652`. Two presentations
carrying one `ExteriorLaw` therefore return byte-identical faces, so their fiber agreement is
determined at declaration and could not have come out otherwise (`CLAUDE.md` §8). **The fiber count
is a restatement of the declaration table, not a discovered classification.**

What the return does establish, and it is not small: the founding coordinates depart, the rest
carries no answer, the remount is exact, and the card evaluates the recovered Newton coefficients
at coordinates `(2,3)²` never supplied — so the *law recovery and its source-detached survival* are
measured. Only the *partition* is authored. The same correction and the route out of it are stated
at `research/records/2026-08-12_THE_CODE_MATERIAL_RETURNS_ITS_WORLD_LINE_WITHOUT_AN_INTERPRETER.md`
§3: vary the founding rectangle across presentations of one operation so that recovered
coefficients rather than a shared declaration are what agrees.

This is the completed fourth return of `blueprint/THE_ROADMAP.md` §“THE RECURRENT-SECTION
CONSTRUCTION.” It is not unrestricted arithmetic discovery, an English conversation model, or a
general unknown-codec reader.

## 1. Owners, ports, event, law, and consequence

The source owners composed are:

- `morphological_language::condition_cuda`, which founds the mixed-corpus suffix and prefix
  relations on the resident card;
- `morphological_language::conduct`, whose exact recurring material transports supply anonymous
  contact faces;
- `recurrent_section_cuda`, whose resident entries found and later evaluate one exact local law;
- `recurrent_section`, which owns the non-`Clone` native rest, detachment boundary, exact
  attachment, and causal-section formation;
- `causal_section`, whose mandatory CUDA read returns the stable receiver/history quotient,
  reconstruction fibers, and shortest separating interventions.

The founding ingress is a population of four returned world consequences on the rectangle

```text
(x0,y0), (x1,y0), (x0,y1), (x1,y1).
```

No operation species crosses that entry. For each row the card returns the Newton-form law

```text
x = x0 + u dx,              y = y0 + v dy,
f(x,y) = c0 + u cx + v cy + uv cxy,

c0  = f00,
cx  = f10 - f00,
cy  = f01 - f00,
cxy = f11 - f10 - f01 + f00.
```

All quantities are exact checked `i64` carried as pairs of `u32` words. Zero lattice steps,
off-lattice queries, and finite-carrier overflow are returned refusals. No floating-point operation
exists in this semantic or device cone.

The later ingress is a situated presentation with its exact anonymous material-contact face,
incidence, port hand, causal successor graph, and new coordinates. The contact face is presentation
lineage: it says where the current meets the deposit. It carries no answer or transformation
coefficient. The card attaches that face to the remounted law and evaluates every attached law;
the host decodes returned exact values into `CausalSection` observations and does not replay the
law.

## 2. The first attempted return failed its own source audit

The first implementation cultivated one finite returned consequence per section state into a
`TrainingEcology`. It passed its printed before/after, ablation, and CUDA quotient gates, but source
review showed that the native rest still contained every known consequence behind a
section-specific licence. CUDA selected the exact licence; it did not generate a consequence. That
was a per-section retrieval relation and violated the roadmap's prohibition on a retained answer
table.

That artifact was not deposited as evidence, the code was replaced before commit, and its local
forms were moved recoverably to `/tmp/holonics-return4-superseded-20260812`. The falsifier changed
the construction: native rest now contains transformation coefficients and anonymous conduct only,
and the production grade requires exact consequences on a coordinate field with empty intersection
with the founding sample field.

## 3. Native rest and source departure

The `RLAW/v1` form is:

```text
header
exact composite-conduct-key width
deposit population
for each canonically ordered deposit:
    anonymous exact material-conduct key
    (x0, y0, dx, dy, c0, cx, cy, cxy)
```

There is no section chart and no answer population. The production audit measured:

| face | returned value |
|---|---:|
| native rest | 53,868 octets |
| source occurrence fields | 0 |
| source path fields | 0 |
| section identity fields | 0 |
| sample coordinate fields | 0 |
| sample answer fields | 0 |
| declared corpus paths found in rest | 0 |
| presentation identities found in rest | 0 |
| complete corpus passages found in rest | 0 |
| exact decode/re-encode | yes |

The corpus atlas, conditioned ecology, condensed conduct morphology, founding passages, and
founding-law CUDA executor were all dropped before the fresh decode. The later exterior contact
face remains necessarily present: a current with no typed port cannot meet a deposit. This bounded
return does not yet infer an equivalent contact face from an arbitrary unseen codec surface after
the corpus has departed; that is a separate open construction.

## 4. The unseen return

The founding sample field was

```text
{(2,2), (3,2), (2,3), (3,3)}.
```

The post-remount field was

```text
{(0,0), (1,0), (0,1), (1,1)}.
```

Their intersection has cardinality zero. The remounted device returned all 28 later consequences
exactly against a withheld exterior oracle. Five presentations carried mixed phase `cxy = 0`; the
two product presentations carried `cxy = 1`. This is a phase distribution with a direct
constitutive reading: the product law alone contains the two-axis interaction vertex.

At the root `(0,0)`, sum and product share the value `0`; a one-step `raise-left` intervention
separates them. Difference and reverse difference also share `0`; `raise-left` separates their
hands. Thus the root value is a shadow, while the successor field retains the law.

## 5. The causal shadow field

Before conduct was activated, the same 28 later states formed one seven-presentation
reconstruction fiber. Every state was OPEN, the phase-current distribution was `{0: 28}`, and the
seven causal diamonds were present but carried no returned value current.

After remount, all 28 states rode exactly one deposited law and the current distribution became
`{1: 28}`. The quotient returned four fibers:

```text
{infix-sum, call-sum, word-sum}
{infix-product, call-product}
{difference}
{reverse-difference}
```

The front distribution remained `{0:7, 1:14, 2:7}` and the arity distribution remained
`{0:7, 1:14, 2:7}`. All seven two-path causal diamonds reconverged on the same later state and
therefore returned flat holonomy at this aperture. No loop was invented to make the profile look
richer.

The caustic population is the 17 unordered presentation pairs which occupied one fiber before and
different fibers after. Removing the value receiver coarsened all seven presentations back into one
block, proving that receiver to be critical for the observed split. No unresolved reconstruction
representative was selected, and material outside the declared population remains `OPEN`.

## 6. Ablation, rebase, and apparatus

Removing the one anonymous conduct deposit addressed by `infix-sum` reopened all four of that
presentation's later states. The remaining two sum presentations stayed in their shared fiber, and
every unrelated per-section formation receipt remained byte-identical. The ablation removed a
pathway, not a row from an answer population.

Restricting to the five sum/product presentations returned exactly the intersection of the full
partition: a three-member sum fiber and a two-member product fiber. This is the nontrivial
restriction/rebase control; identities and coefficients retained their lineage even where several
presentations shared one law.

Every semantic hot deed ran on the NVIDIA GeForce RTX 4080 SUPER:

| resident deed | launches |
|---|---:|
| mixed-corpus suffix/prefix conditioning | 5 + 1 |
| exact law founding | 1 |
| remounted conduct attachment controls | 4 |
| unseen exact law evaluation controls | 3 |
| stable receiver/history quotient controls | 58 |

The receipt states `host_semantic_replay: false`. The host owns file I/O, exact field packing,
source-departure audit, and exterior withheld-oracle comparison. It does not recompute a returned
law consequence on the production path.

## 7. Compression, reconstruction, and diffusion reading

The four-point sample population is compressed into a four-coefficient law only relative to the
declared bi-affine receiver. The compression is exact for every lattice continuation of that law;
it is not exact for an unrestricted function receiver. The corresponding reconstruction fiber is
the population of exterior presentations and histories inducing the same complete declared
consequences. Three sum codecs share one such fiber and two product codecs share another.

The mixed coefficient `cxy` is the discrete two-axis interaction current. It is also the finite
mixed derivative on the founding cell. In a richer section complex, a returned difference may
diffuse through caused incidence and refine an unresolved law fiber without selecting a
representative. This construction does not yet compose the general exact diffusion owners with
the recurrent law; it establishes the source-detached local transformation seam on which that
composition can act.

## Owners

- `soma/abi/src/recurrent_law_cuda.rs`
- `soma/mount/soma-kernel-cuda/src/lib.rs`
- `soma/life/src/recurrent_section_cuda.rs`
- `soma/life/src/recurrent_section.rs`
- `soma/life/examples/the_conditioned_section_returns_after_detachment.rs`

Returned forms:

- `output/the_conditioned_section_returns_after_detachment/recurrent-section-native-rest-deb1ef9ff978049f6144068a37a657ff6a64d93b652c9250abe7582c690fbd48.form`
- `output/the_conditioned_section_returns_after_detachment/recurrent-section-return-grade-1c8ae5c8e15450901dea678463776edec0666e6837969da4c001acd0383df70b.form`

Reproduction:

```text
cargo test -q -p life recurrent_section --lib
bash soma/mount/soma-kernel-cuda/build-ptx.sh
flock /tmp/holonics-gpu.lock cargo run -p life --release \
  --example the_conditioned_section_returns_after_detachment -- \
  --corpus-form output/the_material_mouth_seals_the_declared_body/\
declared-corpus-native-rest-a6fb9343b4fb23399ddf50fb0769465e29a311889c49b0afa307a8d26739e971.form
```

## What this does not establish

This result does not establish unrestricted arithmetic induction, general polynomial discovery,
automatic inference of a contact face from an arbitrary new codec, natural-language semantics,
conversation, attention, probabilistic generalization, physical quantum computation, nontrivial
holonomy, or a universal program learner. The local law family is exactly bi-affine on an exact
`i64` lattice. An off-lattice query remains an obstruction and an outside presentation remains
OPEN. Extending the law family requires a new receiver and a separating return, not a larger magic
bound.

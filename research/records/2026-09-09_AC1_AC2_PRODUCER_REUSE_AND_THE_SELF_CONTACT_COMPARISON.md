# AC1–AC2: producer reuse and the self-contact comparison

## Reuse the actual producer

[established-bounded; implemented-exact] The operative owner now shares the immutable carriers
at its two most recent emission cuts. A material return at one of those cuts uses the actual
producing map, current, bounds and moments directly. Older cuts retain the existing chronological
reconstruction. This is a performance cache, not a context window or restriction on available
causal sources. It is not serialized into the model and does not clone a continuing ecology.

[established-bounded; measured] The native comparison checks pointer reuse, then clears the
cache and compares the complete reconstructed return exactly. The field rest remains unchanged.
All 108 field tests pass in 129.06 seconds. The first 75-occurrence family's body and checkpoint
also match the pre-cache implementation exactly. Development takes 7.62 seconds versus 7.76
seconds in that small comparison; this does not establish a general speed improvement.

## Sixteen-family cultivation remains unusable

[established-bounded; measured] The continuing study reaches 5,064 developmental occurrences.
The additional eight families take 2,122.27 seconds; the whole continuation process takes 2,266.88
seconds with peak RSS 4,746,208 KiB. Its post-development native residency is 2,517,599,840 bytes
and peak residency is 3,117,611,204 bytes. All development section readouts belong to declared
archive placement. This run used row-parallel matrix preparation but preceded producer reuse.

[counterexample; measured] Generation begins with the UTF-8 encoding of `I’`, then repeats byte
`0x99` through the 128-byte limit. UTF-8 decoding fails at byte offset four. This is not a useful
response. The larger exposure, bounded material matrix and exact restart controls do not establish
attained Athena-alpha.

[established-bounded; measured] The retained operative return journal accounts for
2,496,666,560 native bytes: 1,640,898,048 in current factors, 820,449,024 in internal-current deltas,
34,995,456 in port factors and 324,032 in bounds. It therefore accounts for almost all retained
native memory after history placement. The new read-only storage view separates that cost from
historical reports and the bounded material matrix. It does not itself compress the journal.

## A declared source-incidence intervention

[definition] `generate_with_source_contact` exposes an explicit comparison over the existing
recurrence. The default retains the ordinary source contact. Withdrawing it uses an unlinked
occurrence for each self-emitted word while preserving the continuing field state and its temporal
predecessor. This removes both new contact formation and material fitting for that generated
reception. It is an intervention, not a new default or a claim to preserve the withdrawn edge.
The research driver records the choice as `self_source_contact`.

[established-bounded; measured] The control verifies that the intervention actually advances
current, retains temporal predecessors, and leaves the learned matrix and contact map unchanged.
Its first fixture had no resolved emission and was replaced with an outgoing demonstration span
that exercises the intervention. All 24 alpha SDK tests pass in 5.64 seconds; the driver tests
compile.

[counterexample; measured] On the same sixteen-family model and function/derivative prompt,
withdrawing the self contact produces **`I’mn`**, then `EndPart`. It stops the invalid-byte loop in
this comparison but is still unusable language. Because both formation and fitting changed, the
comparison does not isolate either mechanism individually and does not prove the sole cause of
the failed response. The [receipt](2026-09-09_producer_reuse/return.json) preserves the actual bytes,
commands, checkpoints, storage breakdown and costs.

## Construction consequence

[definition] Do not promote source withdrawal into normal generation: it discards the explicit
source edge. The next AC1–AC2 construction distinguishes applying a retained native transport from
supplying a new observation to be fitted, while preserving the source witness and the original
current/receiver scope. An internally projected word is not automatically independent evidence
about the latent current that produced it. Carry that distinction through the same recurrence,
pending-state persistence and returned comparison; do not introduce a second world-verdict law
or an authored output filter. Actual response usefulness remains unproved.

[definition] The return-journal storage is also a concrete remaining AC1/AC3 cost. Its representation
must change lawfully, with the original decoder, source cut and numerical defects retained; the
small producer cache is not that whole-model compression. All AC0–AC5 product obligations remain.

# The application infers and cultivates from the rest alone, and the corpus lookup is gone

**Date:** 2026-08-20
**Kind:** construction return for **Deed P4** of the single-card plan — the native rest behind a
real application entry, frozen inference, cultivation as an application deed, export and its
verdicts — with the mount obstruction the deed found in the P0 container and its lawful repair, the
ten controls, the content bar measured, and the honest forms named.
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for
`phoenix_rest.rs`, the atlas mount, and the wired station; **Deed P4 PASSES on this tree** — ten of
ten controls. Next: **P5 (reborn dissection and the complete Phoenix grade — the deed that moves
`CONSTRUCTION_STATE.md`)**.
**Authority:** the single-card plan §8 "Deed P4", §11; `CLAUDE.md` §0n, §0o; the P0/P3 owners and
artifacts (read-only; the controls mutate copies).
**Owners:** `soma/life/src/phoenix_rest.rs` (810 — the law side: mount, seal, cultivation
composition, refusals, manifest, content bar, lineage metadata lifted from the P3 driver);
`suffix_ecology::mount_emitted` (+86); `atlas_cultivation` (+46: the optional class-extent region);
the `phoenix` station in `soma/life/src/bin/eros.rs` (+471 — parsing, printing, digests, the audit
it prints; no law of its own; unwired stations stay named unwired, `--card` refuses as UNWIRED).
**Driver (controls):** `soma/life/examples/the_application_infers_and_cultivates_from_the_rest_alone.rs`,
which runs the **built eros binary** through the controls.
**Artifacts:** `output/the_application_infers/{receipt.form, rest.safetensors,
successor-rest.safetensors, successor-rest-second-generation.safetensors,
material-written-at-run-time.txt}`.

---

## 0. Verdict

**`eros phoenix infer` and `eros phoenix cultivate` stand as application deeds on the rest alone,
and the deed's main finding is that getting there required removing a corpus lookup the P3 driver
still carried.** `cultivate` must mount the predecessor atlas *from the rest*; the P3 driver
rebuilt it from the eight canon documents — exactly the blueprint's falsifier (*source-detached
rest retains source/corpus lookup*). Building the missing `AthenaRest → ExactSuffixEcology` edge
exposed a real P0 container gap: **the class extents are not derivable from what the container
carries** (a deposit reads `maximum_length` three ways, and 23 of 59,698 P0 classes are unreachable
from the root over the germ transport at all — measured). The repair is one optional region
(`athena.class.extent`), written only when carried, so **every standing container's octets are
unmoved** (the P4 seal with the region stripped is octet-identical to the committed P0 rest);
`mount_atlas` refuses a rest without it by name; and the falsifier is a unit test — a deposit into
the mounted atlas equals the deposit into the corpus-built atlas, identical lineage and identical
emitted container.

The ten controls, all held: two-process seal byte identity (three equal digests, bit-equal
surfaces); the application printing its own descriptor audit (no canon/, no Gemma, no examples, no
`/dev/nvidia*`, no `.gguf`); the missing rest refusing typed with exit 1 and no plausible output;
frozen inference leaving the rest byte-identical (the application re-reads the file after
conducting and fails its own run if it moved); cultivation producing a distinct successor whose
conduct diverges while the predecessor's stands — **with the three-face honesty table**: a deposit
re-folds occurrences over the whole tree so the full face moves everywhere, the support moves
because the vocabulary grew, and **the depth-0 support is the only face a subject-disjoint control
can honestly be taken on** (the development-subject prompt moves on all three; the unrelated prompt
only on the faces growth forces); unseen material at runtime (a novel germ absent from all 5,385,
and a run-time-written material file; plus a **second-generation cultivation** naming the first as
predecessor); the honest GPU form (a CPU-exact application never opens the device — stated as the
control's honest form, with `--card` refusing UNWIRED; and the first form of this check convicted
the audit of naming what it refuses, repaired to check targets rather than the printed page); no
subprocess in the application's code path (0 hits; the subprocesses are the controls'); the export
round trip (the manifest printed by the application; 0 of 59,698 classes differ through
octets → container → transport; the rest *is* safetensors); the content bar measured (9 regions all
`athena.*`, 0 float regions, 0 source-routing names, 0 corpus text — with the honest note that the
vocabulary IS material-derived surface strings and the bar is the corpus absent, which it is; and
the one `layer` string is inside the rest's own negative declaration).

**The GGUF verdict is a refusal with its reason:** the payload is expressible; **the law has no
lawful home** — GGUF consumers dispatch on `general.architecture`, and no architecture's graph
computes *walk the row, fall along the suffix link, return the whole offered family* — so the file
would carry a false declaration or be unloadable, and either way the atlas is not recoverable from
it, which is the blueprint's own criterion. A parseable tensor file is not a model.

## 1. Tests, ledger, epoch

`cargo test -p life --lib -- phoenix_rest atlas_cultivation suffix` → **29 passed** (6 new,
including the mounted-equals-corpus-built falsifier); `h1_adversary` → 35 green; both release
builds clean; the P3 driver compile-checked against the new public field (and its ablation's octet
identity unmoved — `withdraw` returns an empty extent region). One authored level dispositioned
(`SECTION_EXHIBIT = 24`, APERTURE, the presentation-aperture form its sibling row carries); four
ratchet rows updated and one added by hand from the deed's enumerated split; ledgers regenerated
once. One P5-shaped boundary named, not built: the successor's extents come from the transport,
not the delta (deriving them inside `commit` needs length rows on `ExtendEvent`).

## 2. The station table

| deed | grade | commit |
|---|---|---|
| A–D, H0–H5, P0–P3 | PASS (frozen) | their records |
| **P4 native rest, export, frozen inference** | **PASS** | this record |
| P5 reborn dissection + complete Phoenix grade | open — next; **the only deed that moves `CONSTRUCTION_STATE.md`** | — |
| M0–M3 | open, after P5 | — |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-20 (ending 06:34 UTC), exit 0 —
**12 of 12** (tests 2,642 over 30 result lines with examples type-checked; driver-catalog 269;
output-manifest 67/67; architecture-lint clean at 247 files; every other gate green). The gate ran
before two document repairs that followed it and changed no code, kernel, driver or artifact: the
subordinate blueprint's status line had stood stale at "P1 is the open deed" since the P1 commit —
**four successive status/atlas edits had failed their anchors silently inside multi-step scripts
whose tracebacks scrolled out of backgrounded output** — while the roadmap, the sole scheduler,
carried every deed correctly. The status line was advanced with the drift noted in place, the four
missing driver-atlas rows (P1–P4) were added, both verified by re-grep in the foreground, and the
cheap named gates (`named-paths`, `claim-index`, `driver-catalog`) re-run green. The standing
workflow rule this hardens: one write per script step, verification read in the foreground, never a
later assertion that can discard an earlier in-memory edit.

Deed P4 passed; proceeding to Deed P5.

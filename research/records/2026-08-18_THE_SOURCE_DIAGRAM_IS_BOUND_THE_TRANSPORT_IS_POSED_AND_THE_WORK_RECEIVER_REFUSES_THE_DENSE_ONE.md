# The source diagram is bound, the transport is posed, and the work receiver refuses the dense one

**Date:** 2026-08-18
**Kind:** returned construction under [`blueprint/THE_ROADMAP.md`](../../blueprint/THE_ROADMAP.md)
and the Phoenix blueprints.
**Truth status:** `established-bounded` for every measured return below, each carrying the command
that produced it; `implemented-exact` for the linear factorization, rebase receipt, metric adjoint
and lift-defect recurrence; `open` for the contextual operator complex and every Phoenix grade after
it.
**Evidence:** `implemented-exact`, `measured`, `computational-witness`. Engine library tests
`1,661 passed, 0 failed, 3 ignored`. No Phoenix capability is promoted and `CONSTRUCTION_STATE.md`
is untouched.
**Plan:** [`blueprint/THE_GEMMA_MAP_IS_DISSECTED_CONDENSED_CULTIVATED_AND_REBORN_AS_A_FROZEN_NATIVE_MODEL.md`](../../blueprint/THE_GEMMA_MAP_IS_DISSECTED_CONDENSED_CULTIVATED_AND_REBORN_AS_A_FROZEN_NATIVE_MODEL.md)
under [`blueprint/THE_PHOENIX_REBIRTH_LIFTS_INHERITED_HEXIS_AND_RETURNS_A_NATIVE_EXECUTABLE_ECOLOGY.md`](../../blueprint/THE_PHOENIX_REBIRTH_LIFTS_INHERITED_HEXIS_AND_RETURNS_A_NATIVE_EXECUTABLE_ECOLOGY.md).
**Derivation:**
[`2026-08-18_THE_HOLON_IS_THE_OPERATION_COMPLEX_THE_FOREIGN_MAP_IS_A_PORTED_WORD_AND_THE_CARD_CARRIES_ITS_FRONTS.md`](2026-08-18_THE_HOLON_IS_THE_OPERATION_COMPLEX_THE_FOREIGN_MAP_IS_A_PORTED_WORD_AND_THE_CARD_CARRIES_ITS_FRONTS.md)
§§4, 5, 6, 10, 12 and 13. **This record executes stations one and two of its §12 and nothing
further.**

---

## 0. What returned

1. **The three returns a linear map owes are built inside their own owner**, and every falsifier of
   §13 that bears on them executes.
2. **The foreign diagram is bound whole**: `172` typed ports, `635` operations and occurrences,
   `508` co-present fronts, `210` reconvergences, every operation typed by species.
3. **Four bindings the admitted testimony does not decide are returned open**, naming what would
   decide them. Nothing is chosen by plausibility.
4. **A real transport applies at full extent with the card owning the deed**, priced before dispatch
   and telemetered afterward.
5. **Posing that transport densely as exact rationals is refused** by a declared work receiver which
   names the dominating coordinate, and the station changes its aperture rather than its ceiling.
6. **Port identity refuses a composition that matching extent would have admitted.**

## 1. The three returns, inside `exact_linear`

`proved-standard` for the mathematics; `implemented-exact` for the owner.
`crates/holonic-engine/src/exact_linear.rs`, verified by
`cargo test -q -p holonic-engine --lib exact_linear` — **8 passed**.

The derivation records these as `open` and calls them *"a lawful owner-local generalization, not a
MatrixEngine"*. They are added where the factorization of an exact rational operator belongs:
`reduced_row_echelon` with its counted work, `rank`, `kernel_basis`, `image_basis`,
`cokernel_annihilator`, `preimage_fibre`, `preimage_obstruction`, `factorization`, `rebase_receipt`,
`metric_adjoint`, `adjoint_defect`.

Four falsifiers execute:

- **A singular map returns a factorization and never an inverse.** On
  `[[1,2,3],[2,4,6],[1,1,1]]`: rank `2`, one collapsed direction, one open exterior. The collapsed
  direction is *exhibited as a vector* and verified to map to zero, and the exterior covector is
  verified to annihilate every image column. A rank deficit is a count; these are the population.
- **A rectangular map returns an affine fibre or a witnessed obstruction.** For a reachable target
  the returned `x_0 + ker(T)` is checked at a second point of the fibre — `x_0 + (7/2)k` maps to the
  same target — so it is a fibre rather than a solution with a note. For an unreachable target a
  covector is returned with `<w, y> != 0` proving the refusal.
- **A rebase returns BOTH identity compositions**, and both are re-checked outside the receipt.
- **A bare transpose FAILS the adjoint law under declared receiver metrics.** With
  `G_X = diag(2,5)` and `G_Y = [[3,1],[1,4]]`, the transpose's defect against
  `<Tx,y>_Y = <x,T*y>_X` is nonzero and the metric adjoint's is exactly zero — and under identity
  metrics the two coincide, which is exactly when the shortcut is lawful and never otherwise.

## 2. The bridge, and what it deliberately does not own

`crates/holonic-engine/src/ported_operation.rs`, `cargo test -q -p holonic-engine --lib
ported_operation` — **8 passed**.

It owns no arithmetic. Every exact operation belongs to `exact_linear`, every typed boundary and
composable path to `category`, every chronology and front to `causal`, every law and interaction to
`evolution`, every abstract-to-exact binding to `realization`. What it adds is the narrow thing the
derivation names: typed ports and occurrence lineage over a foreign operator diagram, its retained
transport words, and returns at the correct operation species.

**Every operation is typed construction, transport, face or quotient, and the species decides what
return is owed** — `OperationSpecies::admits_inverse` is true only for a transport, so asking a face
or a quotient for one is a typed error rather than a numerical failure.

Four more falsifiers execute:

- **Two same-shaped but differently typed ports refuse to compose.** Ports are `BoundaryId`s of the
  shape's own presentation, so port identity is nominal. Two `2x2` identities across mismatched
  ports refuse; the same two across agreeing ports compose.
- **An applied-state receiver separates `AB` from `BA`** for noncommuting factors, where a chart
  comparison alone would not be the test.
- **Two distinct words with one compiled chart remain two words.** `(2I)(3I)` and `(6I)` agree on
  every declared standing *and* on the compiled chart, and their retained identities and hop counts
  still differ — `word_identity` returns two different words, and enacting the longer one returns
  the intermediate standing the chart discarded. A collapse is lawful only through a declared
  receiver quotient, and a richer receiver can reopen it.
- **The whole-word lift defect equals its own recurrence, exactly.**
  `chi_(eta gamma) = chi_eta T_gamma + S_eta chi_gamma` iterated over a three-step word returns the
  identical matrix to the direct whole-word square, on a word deliberately built so the defect is
  nonzero; and a perfect lift fills every cell under both readings. A norm of `chi` is a face and is
  not used.

And the front reading: three co-present projections of one predecessor return **one front of
breadth three and a dependency span of two**, where a serial reading would have reported four hops.

## 3. Station one — the diagram is bound whole

`established-bounded`, measured 2026-08-18 by

```
cargo run --release -q -p holonic-engine \
  --example the_source_diagram_is_bound_or_its_candidates_are_retained -- \
  /home/b/models/gemma-4-E4B-it
```

```text
declared populations              2130      refused 0      overlapping 0
typed ports                        172
bound operations                   635      occurrences 635
CO-PRESENT FRONTS                  508      dependency span 508
fronts of breadth above one         85      widest front 3
occurrences riding co-present      127

species census
    construction   129
    transport      463
    face            43

operations admitting more than one path  210
operations emitting more than one path     0
ports no operation emits                   0
```

The census is exactly the diagram: `11` transports and `3` constructions and `1` face per site
across `42` sites, plus the delivery, the two lookups and the boundary. **Every residual re-entry is
modelled as the reconvergence it is**, admitting both the retained standing and the returned delta;
modelling it with one input would hide the join, and the join is what a residual is.

### Three testimony species were admitted, and each is retained with what it decided

- the **declared configuration**, field by field: `42` sites, construction width `2560`, `8`
  receivers against `2` presented families, per-site material width `256`, `7` whole-context sites
  of `42`, `18` declared shared state passages;
- the source's **authoritative description of itself**, sentence by sentence; and
- the **declared shapes** of the manifested populations, which constrain the ports.

**The description decides three bindings by name**, and this is the sharpest half of the station,
because the paused implementation had inferred two of them and got one wrong:

> *"To optimize memory for long contexts, global layers feature unified Keys and Values, and apply
> Proportional RoPE (p-RoPE)."*

> *"The models employ a hybrid attention mechanism that interleaves local sliding window attention
> with full global attention, ensuring the final layer is always global."*

> *"These embedding tables are large but are only used for quick lookups."*

So the `18` declared shared state passages are the **whole-context sites carrying unified keys and
values** — source testimony, not a weight-redundancy inference — the chronology is proportional and
partial by name, and the per-site material is a lookup by name.

### The executable implementation is absent, and four bindings stay open

Sought and reported rather than assumed, measured the same day by
`python3 -c "import transformers"` → `ModuleNotFoundError`. So four bindings it alone would settle
are returned as open candidate populations, each naming what would decide it:

| question | candidates |
|---|---|
| how is a stored rebase gain read? | the stored value is the gain / it is an offset and the gain is one plus it |
| which two coordinates does the chronology pair? | adjacent coordinates / the two halves of the turned sub-bundle |
| where does the stored per-site scalar multiply? | the re-entering standing / the contact faces / nothing, it is inert |
| does the contact carry a scale, and which? | the reciprocal root of the receiver chart's width / unity |

**The station returns that the diagram is bound and NOT closed**, rather than returning a
composition. `diagram closed false`, `open questions 4`, `operations with no deciding testimony 0`.

## 4. Station two — the transport is posed, or the work refuses

`established-bounded`, measured 2026-08-18 by

```
cargo run --release -q -p holonic-engine \
  --example the_ported_transport_is_posed_or_the_work_refuses -- /home/b/models/gemma-4-E4B-it
```

on an RTX 4080 SUPER with an active display, which is stated because a measurement without its frame
is the absolute-frame defect.

### The card owns the deed at full extent

Site zero's receiver projection, `2048 x 2560`, carrying a real stored standing:

```text
predicted work, before dispatch      5,242,880 multiplications, dependency span 1
                                     priced 283,115,520 against a declared ceiling
                                     ADMITTED

stored codewords across the bus         10,485,760 octets
query across the bus                        20,480 octets
resident expansion on the card          41,943,040 octets
result egress                               40,960 octets
launches                                         2   decode/align, contract
read 7.03 ms      mount 1.21 ms      deed 1.34 ms
```

The stored two-octet codewords cross once and decode and align **on the card**; only the contraction
returns. The telemetry sums every crossing separately and is not a predicted byte counter — the
defect the audit convicted in the paused work.

### Posing it densely is REFUSED, and the refusal is the return

```text
predicted residency   2048 x 2560 exact rational entries
REFUSED   priced 5,242,880 against a declared ceiling of 1,048,576
          dominated by resident-entries at 5,242,880
```

The dominating coordinate is **named**, so a caller learns what made the deed expensive rather than
that it was. The station then changes its **aperture** and reports that the aperture excluded
`5,240,576` of `5,242,880` entries rather than dropping them.

### Under that aperture, on the same real material, everything is returned

```text
rank 48    collapsed directions 0    open exterior 0    is a rebase true
REBASE, and both identity compositions returned

bare transpose   defect 32773685463/293601280
metric adjoint   defect 0
```

The two receiver metrics are declared and deliberately non-Euclidean. The adjoint's ports are
swapped, because a covector travels the other way.

### Port identity refuses what extent would have admitted

Two `48 x 48` blocks of real source material refuse to compose:

```text
receiver projection emits port BoundaryId(2) and contact return admits port BoundaryId(4)
```

Matching extent is not a matching port, and the refusal is by identity.

### The word retains its standings; the product is one face of it

A two-step word keeps entering, intermediate and leaving. The total product is compiled **only
because this reader asked for that face**, and returns what asking cost — `110,592`
multiply-accumulates at dependency span `1`.

## 5. What is open, precisely

- **The contextual operator complex** (§12.3) is not built. The diagram is bound; no transport word
  spanning a site has been enacted on caused material.
- **Every one of the four candidate populations is open.** The next lawful move is not to enact a
  plausible reading and inspect the surface — the derivation forbids exactly that — but to separate
  the candidates under a declared receiver family with a shortest separating history, which is
  `receiver_exact_compression`'s deed.
- **Resident realization** (§12.4) has one transport's worth of evidence and no whole-front
  ownership testimony. A front's apparatus completion order has not been certified by
  `InterchangeCertificate`.
- **Dissection, condensation, cultivation, native rest, runtime and inference** (§12.5 and beyond)
  do not exist.

`CONSTRUCTION_STATE.md` is untouched. The Phoenix grade is the whole circulation and no proper
prefix of it is Phoenix rebirth.

# THE DERIVED FACE MAY SUBSTITUTE FOR THE PARENT; THE OPEN RECEIVER RETURNS TO THE SOURCE

**2026-07-22 · BRANDON-RATIFIED OBJECTIVE / ASSISTANT-DERIVED BOUNDED CONSTRUCTION CONTRACT /
BUILT / ONE ACCEPTED FIXED HOST OBSERVATION / EXACT RESULT DEPOSITED**

## The question

Can one transformer-derived Eros ecology conduct a later current through a smaller local
transformation before evaluating its parent path, use the parent only where the derived receiver
remains OPEN, and retain the returned difference so later recurrence no longer requires that
parent path?

The existing evidence does not answer this. `eros-contextual-retriangulation-01` reduced
participation from 8,192 activation words to six at the Eros boundary, but the complete upstream
field had already been produced. `eros-residual-chart-cultivation-01` learned a conditional
consequence chart, but it did not place that chart in front of an actual parent computation.

## The fixed cut

The parent is the exact layer-7 OLMo gated-MLP factorization at the five already-declared receiver
axes:

```text
P_R(g) = sum_(j=0)^8191 g_j W_(R,j).
```

Every retained BF16 word is decoded as an exact dyadic. `P_R` therefore means 40,960 exact dyadic
products plus their exact sums, not a floating approximation.

The inherited local law is the already-fixed rank-six retriangulation:

```text
E_R(g_P) = g_P K,

P = [0,1,2,3,4,5],
K in Q^(6 x 5).
```

It was formed from the six conversation/prose `drop_then_reverse` currents and is exact on those
six rows. The carried conditional chart supplies the receiver support already observed there:

```text
drop_then_reverse
├── absent            -> face A
├── irrelevant_before -> face B
└── relevant_after    -> face B.
```

The transformer, its tokenizer, and these identities remain lawful source ecology. Eros is not
required to rediscover them from raw bytes.

## Parent-on-OPEN law

For present categorical interface `c` and its six pivot current `g_P`, let `D(c)` be the deepest
available population of supported receiver faces and let

```text
y_hat = E_R(g_P).
```

The derived path may substitute for the parent exactly at this declared receiver when:

```text
D(c) = {f}
and
Phi_R(y_hat) = f.
```

This is a receiver-relative closure. It does not assert `y_hat=P_R(g)` as an exact numerical
vector, replace the full 2,048-axis MLP return, or license downstream transformer continuation.
It says that the smaller law already supplies the complete face requested by this world.

If `D(c)` is plural or absent, or `Phi_R(y_hat)` differs from its singleton, the relation is OPEN.
Only then may the parent organ evaluate the complete `P_R(g)`. Its actual face and the complete
oriented residual

```text
r = P_R(g) - E_R(g_P)
```

return genuinely later. That return adds a local receiver branch; it is not a correctness label,
reward, punishment, confidence threshold, or cached output.

The source path and the always-parent foil use the same immutable retained activation and weight
words. The selective branch must not even iterate the 8,192-word parent population when it
substitutes.

## Frozen population and predicted signatures

The initial ecology remains the six training currents above. The later population is fixed, in
source order:

```text
drop_then_reverse.code_comment.absent
drop_then_reverse.code_comment.irrelevant_before
drop_then_reverse.code_comment.relevant_after
reverse_then_drop.conversation.relevant_before
reverse_then_drop.prose.relevant_before
reverse_then_drop.code_comment.relevant_before
```

The retained, untuned six-pivot law predicts one receiver closure:

```text
drop_then_reverse.code_comment.relevant_after -> RIDE
```

The other five candidates differ from the carried singleton or have no applicable singleton and
must remain OPEN. The matched parent record already establishes that the one proposed
substitution has the same receiver face; this expectation is frozen before the Eros run.

The bounded acceptance signatures are:

1. one of six first-passage parent receiver evaluations is actually omitted by the selective
   branch, while the always-parent foil evaluates all six;
2. the omitted path is exactly `drop_then_reverse.code_comment.relevant_after`;
3. every OPEN path evaluates the complete 8,192-by-five parent relation exactly once;
4. every parent return carries its complete rational residual and adds the missing local face;
5. a later source-absent probe of all six interfaces recovers the exact parent receiver face
   without another parent evaluation;
6. the no-ecology control cannot substitute any path;
7. source lineages depart, the resulting ecology survives exact rest/remount, and no floating
   causal value enters; and
8. the latest outgoing-boundary law keeps the active successor rather than cumulative event
   interior.

The recurrence probes deliberately test the cultivated six-current population. They do not claim
out-of-population transformer equivalence. The first-passage held-out substitution is the
nonidentical-context result.

## Physical preflight and stopping condition

The retained source files, their hashes, all 12 activation fields, the five weight rows, and the
rank-six law must be exact and present. The host has ample memory for one bounded exact pass. No
model load, CUDA launch, tokenizer rerun, network access, alternate pivot search, feature-order
search, threshold, or fixture mutation is permitted.

The encoded-event preflight gives an initial ecology of 449 compact words and a largest complete
replacement of 1,521 regional arcs: 675 departing ecology words, 56 present-current words, 52
returned-residual words, 735 successor words, and three interfaces. The fixed aperture is 2,048,
the smallest power of two which carries that one co-present event whole. It is not a runtime
reservation inferred from a partial result.

Build one source extractor over retained artifacts and one host Eros conductor. Run focused
compile/tests and one accepted physical host observation. Record:

- selective and always-parent invocation counts;
- exact parent and local arithmetic operations;
- receiver RIDE/OPEN population;
- complete residuals;
- later source-absent recurrence;
- active topology and departure; and
- wall time, without promoting it into an end-to-end transformer latency claim.

Stop there. A full-block substitution, downstream emitted-token comparison, novel prompt
population, model-integrated router, CUDA residency, and scale are distinct experiments and are
not scheduled by this contract.

## Measured closure

The fixed result is deposited at
`../observations/eros-parent-on-open-substitution-01/RESULTS.md`. The selective circuit made five
parent calls while the foil made six, omitting exactly 40,960 parent products and accumulations at
the declared receiver. The one omitted current was the frozen nonidentical-context prediction.
Five complete rational residuals crossed, the active ecology factored to one 735-word face, exact
rest/remount held, and all six later source-absent recurrence probes closed without another parent
call.

One pre-result implementation attempt correctly refused a false outgoing factor because the query
continuation's hand had been reversed at the later temporal seam. Correcting that hand did not
change the source or expected scientific signature. No model, tokenizer, CUDA, search, threshold,
or alternate population ran.

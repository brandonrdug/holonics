# THE DESCENDING STROKE AND THE PRODUCTION REGISTER CUT

**Date:** 2026-07-12
**Grade:** §XXVIII-b BUILT / HOST+CUDA-GATED · CUDA §XXXII-c OPEN
**Authority:** `FORMULA §§XXVIII-b, XXXII-b, XXXII-c` · Brandon's complete-machine/no-cap ruling

**Closure:** the no-wall REGISTER face described here is now built and host+CUDA-gated at one
declared aperture. Capacity-wall remount/resumption remains open. Continue at
`2026-07-12_THE_REGISTER_CROSSES_CUDA_THE_CAPACITY_WALL_DOES_NOT.md`.

## What now stands

One raw difference no longer has to drain its whole depth-first interior on one substrate call
frame. The complete carrier row owns the continuation at its tail:

```text
phase · depth(lo,hi) · current brick
```

The existing deferred siblings, enclosure rows, atom stance/flywheel, dark passage, and ordered K
remain where they stood. The seven live phases are:

```text
PERCEIVE_AFFERENT · PERCEIVE_EFFERENT · PERCEIVE_UNWIND
WORD_PATH
PATH_AFFERENT · PATH_EFFERENT · PATH_UNWIND
```

The phase always names the next move. An invocation completes at most the boundary-declared number
of moves; zero is the uninterrupted gauge. A live continuation owns the front of the next
invocation, so no raw difference can enter before it closes. The cursor advances only after PATH
returns to NONE. The receiving edge and true-end dark deposit require NONE.

Transport-canonical and at-rest are now distinct. A lawful active carrier may cross an installment;
it may not cross SLEEP, legacy resume, receiving integration, or radiation rendering. Checked mouths
reject an unknown phase, a clipped/malformed brick or depth, unreachable deferred state, an active
cursor at the raw-light end, and a partial radiation face inconsistent with the continuation.

## Exact construction gate

The fixed 155-octet passage ran uninterrupted and one move per invocation. Both dense and founded
species crossed all phase faces (`0xff`), reached the reservation edge at depth four, and resumed an
efferent seam immediately before a real deposit. The stepped host and stepped CUDA constructions
ended equal to the uninterrupted host in:

- OWN words;
- complete carrier/K words;
- every founded radiation word;
- cumulative counts: `[4,508, 191, 188, 3]`.

The same CUDA artifact also remains exact at 1/2/3/64/100 lanes; 100 lanes crosses a two-row launch.
LINK, founded LINK, chart mark/count/recast, and the CUDA smoke repeated exact. No Xid/NVRM followed.

## Boundary facts, not machine constants

The CUDA driver presently reports:

```text
free / total bytes       16,450,453,504 / 16,720,199,680
maximum grid             2,147,483,647 × 65,535 × 65,535
maximum threads/block    1,024
multiprocessors          80
```

The boundary queries these values and directly zeroes device storage. They are apertures at this
mounting, not a utilization policy or chart grain. Scope derives X/Y launch geometry from runtime
extent. The carrier reservation for a genuinely new current must likewise follow the simultaneous
whole-mount construction; a continuing SLEEP body keeps its carried row exactly.

## Why the current CUDA scope is not production

`scope_founded` still takes `own_axis` from an immutable lane row and constructs the declared-grain
`LineageStroke::founded`. That is the correct bounded parity species, but it is not §XXXII-c.

The production current's OWN chart is its register. It is born at the uncommitted rank-zero seed,
and its own FOIL arrivals carry the hand, zero-extend the cells, re-judge the arrival, release pure
rides, and borrow/narrow where the surviving section permits. The light has no known future length.
Therefore neither of these substitutions is lawful:

- run the light on the host first to discover its final axis;
- choose a fixed axis from free memory and call it production.

Both replace the current's history with an external frame. Wiring today's fixed-axis CUDA entries
through receiving, SLEEP, and wake would produce a useful bounded conductor and a false production
grade.

## The next construction

The next body seam is a CUDA spelling of `OwnRecast`, not an answer head or conductor wrapper.
It must preserve these invariants:

1. Each current begins at the same uncommitted rank-zero seed as `ErosBody::over_register`.
2. A carry is caused only by that current's FOIL arrival; aliases remain transport and do not count.
3. The zero-extension/narrowing is byte-whole and founder/K-continuous.
4. Any substrate suspension during recast is the same descending seam: exact carried cause, no
   visibility/Θ/receiving edge, unconditional-next resumption.
5. Storage growth/sharding is boundary apparatus. It may not become a page table, search, allocator
   event, fixed maximum chart grain, or host preflight.
6. Staged versus streamed delivery, thread count, substrate installments, and host/CUDA lowering
   land the same construction for the same delivered content.

Only after this stands can the production conductor honestly compose:

```text
REGISTER scope
  → runtime-banded founded LINK
  → chart carry/recast at the true edge
  → SLEEP v5
  → next-light wake and radiation listener
```

The founded LINK band needs a runtime base, and radiation needs a runtime window origin; neither
extent may become body grain. Nonempty large standing recast remains an explicit later hardware
seam. The first large lighting stays halt-first until this register cut and its exact gates land.

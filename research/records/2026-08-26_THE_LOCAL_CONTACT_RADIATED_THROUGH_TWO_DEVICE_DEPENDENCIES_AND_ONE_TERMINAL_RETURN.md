# The local contact radiated through two device dependencies and one terminal return

**Date:** 2026-08-26  
**Phase:** MEM3  
**Truth status:** `[established-bounded; implemented-exact; measured]`  
**Primary artifact:**
[`00-dependent-resident-membrane-word.json`](../../output/the_athena_membrane_interior_returns_one_dependent_resident_word_mem3/00-dependent-resident-membrane-word.json)

## Resident word

[established-bounded; implemented-exact] `ResidentMembraneInteriorWord` mounts the complete
factor-capacity population, affine offsets/factors/multiplicities, exact constitutive orientations,
and all ten complex family returns once on one CUDA context. Dense device indices are a bijective
apparatus chart over addressed factors and cells; they do not become native identity, topology, or
capacity.

[established-bounded; implemented-exact] One successor word contains three launches on CUDA's
ordered default stream. `conduct_membrane_local_contacts` restricts two addressed affine cochains
to shared support and writes every exact oriented family current. `gather_membrane_radiation`
consumes those device buffers and writes the plural native radiation. Finally,
`inject_membrane_boundary_current` consumes that radiation and applies the exact complex exterior
current. These are two actual producer-to-consumer buffer dependencies, not three co-scheduled
leaves or a host-authored semantic sequence.

[established-bounded; implemented-exact] Exact family currents are rebased once to a derived
common denominator and a material-derived unsigned-limb population. Later cell addresses and the
two exact complex boundary coordinates are the only hot ingress. Overlap, family response,
radiation, and complex boundary action remain device-resident until one terminal synchronization.
An apparatus-width refusal is typed; no CPU replay or host fallback is available.

## Measured return

[established-bounded; implemented-exact; measured] On the NVIDIA GeForce RTX 4080 SUPER, the
invariant resident population was 6,195,978 octets. Each successor crossed 26 host-to-device
octets, returned 396 terminal octets, used 1,520 working octets, launched three kernels, traversed
two device dependencies, synchronized once, returned zero intermediate host egress, and re-uploaded
zero invariant octets.

[established-bounded; implemented-exact; measured] The distinct shared pair from MEM2 returned
all ten exact family overlaps at `191/7`. Its native radiation exactly equalled the independently
predeclared MEM2 response. Applying boundary current `2 + 3i` on card returned exactly the complex
product of that response and the boundary current. The disjoint sibling returned ten zero
overlaps, zero native radiation, and zero boundary radiation through the same resident word.

[established-bounded; implemented-exact; measured] Both passages used one context identity and the
move-owned rested Athena identity was unchanged after apparatus release. The output artifact
SHA-256 is `16ebaef1632684af33a9532876f1eef108a71e2b98d057e19af7ab8f8f15d35c`. Kernel, apparatus,
interior, membrane, and receiver source SHA-256 values are respectively
`c0f475f04442fdd4dbe8d43b1f56a4213b8c8b3d5377e99208436ccef2873451`,
`d3af0bdd818a5e4823b8164ef934ef1c7dad36eb216b6807d14d6bf6393b6ce3`,
`db7beecbb9ff0044b0112dbaf657197f82936eab4a25325dd63573aef751e651`,
`10153f693356d0dbc18eda2597ba756a57bac8daa297b832de92be02e33ab9e5`, and
`6d3719eab8ac02a18e9f9dbbe612636350ba2a6c900599466fe772dbd963c9b5`.

## Boundary

[open] MEM3 establishes one resident interacting word and native outward complex current. It does
not claim that a genuine returned world occurrence has changed reusable morphology. MEM4 must
cross that return through the same membrane, transport the complete situated difference along its
actual causal-adjoint word, commit the least local delta, and pass detached remount plus targeted
ablation/restoration.

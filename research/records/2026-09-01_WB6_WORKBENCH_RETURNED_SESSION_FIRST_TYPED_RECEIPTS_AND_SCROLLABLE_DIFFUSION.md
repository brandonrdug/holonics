# WB6 returned session-first typed receipts and scrollable exact diffusion

**Date:** 2026-09-01
**Truth status:** `established-bounded` for Holonics Workbench 0.3.0 over its declared local session,
operation, receipt-projection, CLI and TUI surfaces.
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, and `measured`.
**Construction effect:** WB6 passed; WB0--WB6 are complete and no Workbench deed remains scheduled.

## Screenshot falsifier and correction

[established-bounded; source-inspected; measured] Brandon's screenshot falsified WB5's convenience
interpretation. A permanent repository explorer occupied roughly one-third of the primary surface;
an undifferentiated action list occupied another third; `selected_detail` pretty-printed raw JSON;
the detail paragraph had no scroll state; Up/Down changed event selection instead of moving through
the receipt; and the diffusion summary exposed only current-record count and a residual already
required to be zero. The focused tests proved wiring and strings, not operator usefulness.

[established-bounded; implemented-exact] WB6 deletes `discovery.rs`, the `discover` command, recursive
workspace classification, and every filesystem item/pane from the TUI. Paths remain ordinary exact
fields inside open, atlas, Soulkiller, package and export operations. Functional keys `1`--`4`
select Athena, Eros, Soulkiller and Engine action surfaces; they are not decorative tabs.

## Session and receipt instrument

[established-bounded; implemented-exact] Athena now dominates the default surface with selected
session, generation, live/rested boundary, successor population and the explicit lifecycle
`MOUNT -> CONDUCT -> EMIT -> WORLD RETURN -> COMMIT`. Actions are grouped by NEXT, RETURN, INSPECT,
PROBE and PERSIST. Eros, Soulkiller and Engine expose compact typed forms. One runtime owner still
moves to the operation worker and returns before another mutation.

[established-bounded; implemented-exact] The selected causal event has Summary, Structure and Exact
receiver projections. Summary states the domain consequence. Structure renders founded relations
and exact tables. Exact preserves the complete cold serialized payload. Human CLI rendering uses
Summary and does not dump JSON. Machine JSON/JSONL remains available through request-v2,
response-v2 and event-v3 schemas.

[established-bounded; implemented-exact] Summary, Structure, Exact and Help retain separate scroll
positions. The rendered paragraph computes wrapped visual line population at its actual width,
clamps to the final visible page, and returns a scrollbar. Arrow, PageUp/PageDown, Home/End,
Left/Right, direct `s`/`v`/`x`, Tab and event selection controls are independent. Returning to a
projection restores its prior position rather than resetting or changing the selected event.

## Diffusion receiver

[established-bounded; implemented-exact] The action is now named `Closed unit-law diffusion probe`.
Its returned payload declares unit capacity at every native state, unit conductance at every
incidence occurrence, unit content at the first native state, empty source, interval one and the
whole native population as receiver boundary. It explicitly reports `session_state_changed: false`.
The session snapshot wire before and after the probe is byte-equal.

[established-bounded; implemented-exact; measured] On the bounded alpha generation-1 morphology,
Summary rendered five native nodes, four incidence branches, three nonzero currents, five boundary
and zero interior nodes, total content `1 + 0 -> 1`, stored energy `1/2 -> 69/338`, departed energy
`50/169`, and conservation residual zero with the note that zero is required for a returned event.
Structure rendered all four oriented currents and transfers, every node balance and residual, and
standing transport `1 -> 7/13`, `0 -> 4/13`, `0 -> 2/13`, `0 -> 0`, `0 -> 0`. Exact rational
rendering decodes only the canonical nested BigInt ratio wire; ordinary two-element arrays remain
arrays.

## Recoverability and apparatus controls

[established-bounded; implemented-exact] Return and decline now verify and snapshot the standing
session before ownership transfer. Missing-boundary and owner refusal restore or retain the same
session and boundary. No-command invocation checks terminal interactivity before Ratatui; a
noninteractive invocation exits 2 through the structured input obstruction and never panics.

[established-bounded; process-audit; measured] Fifteen focused controls passed: twelve library and
three integration tests. They cover the four functional modes, absent filesystem/discovery surface,
every actual successor, typed form refusal, direct/runtime equality, per-projection scrolling,
scrollbar rendering, Page/Home/End bounds, rational decoding controls, typed human output,
noninteractive refusal, recoverable return/decline, diffusion topology/current/balance fields, and
pre/post snapshot equality. Headless 80x28, 120x36 and 180x48 surfaces passed.

[established-bounded; process-audit; measured] Real 80x24 and 180x48 pseudo-terminals returned the
session-first surface and restored on exit. The wide diffusion Summary and Structure views displayed
the exact values above; the narrow view exposed visible scroll controls rather than truncating the
receipt irretrievably. `/home/b/.cargo/bin/holonics` reports version 0.3.0. Human demo output is typed;
noninteractive no-command invocation exits 2 with clean stderr.

[established-bounded; formal-checked; process-audit; measured] The first cold workspace test section
met its 180-second outer boundary during compiler construction and returned no partial pass. Its
second bounded run returned 2,972 passed, zero failed and 50 ignored. Example targets then
type-checked. The complete coherent receiver passed within its outer bound: the same 2,972 tests,
all examples, 3,771 live Lean jobs, tracked authority, source shape, epistemic tags, claim index,
equation atlas, all ten Typst roots and document law. Nine gates passed and zero failed.

[established-bounded; process-audit; measured] Root `target/` measured 41 GiB before cleanup.
`cargo clean` removed 19,277 replicable files totaling 42.4 GiB. Root `target/` and `output/` are
absent; the installed user binary remains.

[open] This correction does not turn the one-step probe into persistent configurable diffusion,
construct qualitative multimodal generation, dismantle arbitrary foreign directories, discover
every engine owner, or coordinate a distributed ecology. Those remain separate open fibres.

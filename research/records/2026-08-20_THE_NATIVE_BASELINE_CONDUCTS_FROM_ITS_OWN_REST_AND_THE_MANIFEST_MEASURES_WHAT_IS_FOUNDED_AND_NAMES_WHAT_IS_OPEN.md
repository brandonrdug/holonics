# The native baseline conducts from its own rest, and the manifest measures what is founded and names what is open

**Date:** 2026-08-20
**Kind:** construction return for **Deed P0** of the single-card plan — ARM N, the native
Eros/Athena baseline without foreign hexis, rederived from the quarantine through the H owners and
conducted on the card from its own rest — with the plural future sections, the fresh-process
remount and access audit, the construction-level ablation with its honest boundary, the derived
launch geometry that retires the convicted flat shape, the `NativeEcologyManifest` (measured rows
exhibited, open rows carrying falsifiers), the witness seam, and what it hands P1.
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for the
rederived native laws, kernels and witness; **Deed P0 PASSES on this tree** — eleven of eleven
controls held. Phoenix's next deed is **P1 (the cross-chart lift)**. `CONSTRUCTION_STATE.md` does
not move (only P5 moves it).
**Authority:** the single-card plan §7 (all of it), §8 "Deed P0", §11; the old Gemma-instance
blueprint §5 as component contract (ARM N's obligations); `CLAUDE.md` §0o (the quarantine's terms —
its testimony rederived, its execution shape not extended);
[H5](2026-08-20_THE_DISSECTION_SHARES_ITS_PREFIXES_THE_KEY_LEARNS_AN_ADDRESS_IS_NOT_CONTENT_AND_THE_HARDWARE_FOUNDATION_IS_COMPLETE.md).
**Owners:** `crates/holonic-engine/src/native_occurrence.rs` (242 — the rest as its own witness:
declared shapes against the rest's header with integer carriers, law statements against the rest's
own metadata, foreign testimony refused **by type**); `crates/holonic-engine/src/native_law.rs`
(408 — `AthenaWalk`/`AthenaFuture` with caller-declared apparatus geometry); the
`athena_walk_cooperative`/`athena_future_staged` kernels (`KERNELS` 27 → 29, both
`__launch_bounds__(512)`); the `OccurrenceWitness` trait seam in `source_occurrence.rs` (the one
line of the passage that moved: `compile`/`bind` take `&dyn OccurrenceWitness`); the geometry
families in `resident_section.rs` with the Pareto rule lifted to one owner (`non_dominated_by`).
**Drivers:** the split is *the ecology builds the rest, the engine conducts it* —
`soma/life/examples/the_native_rest_is_built_and_declares_its_own_laws.rs` (8 declared canon
documents → an integer safetensors container whose `__metadata__` carries the statement of every
law that reads it, plus a sibling container without `canon/TABLET_THE_HEXIS.md`; touches no card)
and `crates/holonic-engine/examples/the_native_baseline_conducts_from_its_own_rest.rs` (the only
thing that touches a card, and the only thing it reads is the rest).
**Artifacts:** `output/the_native_baseline_conducts/{receipt.form, native-ecology-manifest.form,
rest.safetensors, rest-without-hexis.safetensors}`.

---

## 0. Verdict

**The native arm is executable, plural, and source-detached — from a rest of 59,698 classes,
102,904 germ transitions and 5,385 vocabulary germs (1,642,928 octets) built from eight declared
canon documents.** Runtime-supplied prompts walk on the card and return the **plural future
section** — every germ the landed class's suffix chain offers, with standings and depths, decoded
to actual exterior surfaces from the rest's own vocabulary; no argmax, no sampler, no temperature
(depth-0 populations across the eight prompts: 12, 1, 1, 11, 2, 1, 1, and the unseen-germ prompt
returning the root's whole 5,385 — the walk's arc law, stated, not an error).

**The convicted execution shape is measurably gone.** The quarantined future climb was one thread
per cell at the maximum legal block; here the future section runs at **grid 2,635 × block 64** with
the suffix chain staged 16 deep in shared memory and 64 germ lanes reading one staged chain —
covering **122,880 of 122,880 lanes** — chosen from a **260-candidate family with 47 retained**
under the declared axes (8 under a coarser axis set — the retained set moves with the declaration,
which is the falsifier), registers measured through `cuFuncGetAttribute`, and two admitted members
returning all 166,935 future words bit-identically. The walk is one warp per prompt with the
transport row searched 32-ary, the arity read from the device's warp, not written (**one PIN found
and excised during the build**: a literal 5-and-32 pair became `ceil_log2(warp)` and `warp`).

**Fresh-process remount and the audits:** the child re-executes with only the rest's path;
**5,342,912 octets of returned faces bit-equal to the parent's**; the live descriptor audit shows
no corpus, no Gemma, no phoenix, no tokenizer, no `canon/` path open during the deed; the hidden
card refuses typed (`CUDA_ERROR_NO_DEVICE`, exit 2, no serial rendering).

**The ablation is construction-level and says so.** A second rest built without
`canon/TABLET_THE_HEXIS.md` (59,698 → 56,165 classes; 216 germs gone, by name — `AdapterManager`,
`Autograd`, …): the two hexis-dependent prompts' future sections move whole, and **the unchanged
control was measured, not asserted** — five of six unrelated prompts return a bit-identical depth-0
support, none returns an identical support at every depth, and the standings move for every prompt
because a standing is a corpus-wide occurrence count. That is the honest boundary of rebuilding
the rest; deleting a founded class in place is a different operation and is not claimed.

## 1. The NativeEcologyManifest — measured where founded, open where not

Every measured row computed from the rest alone; no layer count, latent width, head or rank
anywhere; the only depth receiver is the suffix-link height, read off the material.

- **Incidence, measured:** 59,698 / 102,904 / 5,385; **cycle rank β₁ = 43,207**, and the departure
  from tree-ness is *reconvergence* — 27,628 classes reached by more than one arc, **exhibited by
  name** (class 5 reached by three `"curvature"` arcs from classes 4, 397, 43,906; class 7 by three
  `"windings"` arcs; …), never only a count.
- **The germ transport is a DAG, measured** (a topological order over all 102,904 edges exists) —
  so β₁ counts undirected reconvergence and **no directed return exists in this rest**, which is
  what keeps every winding/phase row honest.
- **Scale, measured:** tree height 9 with the per-height census; the interval/Minkowski chart with
  empty remainder; **its collapsed fibre exhibited** — the interval receiver carries ancestry
  exactly and no record of which transition arrived, so all 27,628 reconvergent classes are its
  collapsed population.
- **State, measured at a declared receiver family and declared scope:** memory order **3** over the
  5,598 reached classes (93 one-shot blocks → 795 conduct-stable, 653,599 collapsed pairs, the
  separating words exhibited); **the whole-atlas reading refused with its aperture measured** —
  one Moore round needs a 2.57 GB signature allocation and `Partition::block_of` is a linear scan;
  the lift is an indexed lookup and a streamed signature *inside `receiver_exact_compression`* — a
  repair in that owner, named, not a new organ.
- **Group/gauge, measured: none is founded.** Of 5,385 germs, 11 act bijectively on the classes
  they touch and none acts on every class — the transport generates a monoid, not a group.
- **OPEN with falsifiers:** winding/phase (the longest-suffix carry monoid is aperiodic by
  construction; a crystal claim owes a nontrivial group element with its winding, a band structure
  and a holonomy around a closed loop — and the DAG measurement says no such loop exists in this
  rest); sufficient-state candidates beyond the Nerode reading (named; decided by lumpability at a
  declared receiver family, not by a name).
- **Station D cited as exterior evidence, not imported as topology.**

## 2. The witness seam, and the pass-blockers checked

`OccurrenceWitness` was re-founded (the quarantine's seam; the tree offers no other — every
alternative duplicates the compile path or lies about what authenticated the deed): one trait, two
implementors, one moved signature, and foreign `Implementation`/`Configuration` testimony refuses
**by type** on a native rest, with a test. The §8 pass-blockers are absent by measurement: no
42-layer restatement, no fixed latent width, no authored layer/head/rank, and the one-thread flat
kernel replaced by the derived geometry above.

## 3. Tests, ledger, epoch

`cargo test -p holonic-engine --lib -- native athena resident_section front_passage` → **61
passed** (7 new); `h1_adversary` → 35, green throughout; `cargo test -p life --lib -- suffix
causal_language` → 28; the GPU deed 12.4 s with **all eleven controls held** and the artifacts
inspected. The thirteen ratchet deltas enumerated and dispositioned in the deed's report — the two
new files dominated by trait-shape and returned-population occurrences; `native_occurrence.rs`'s
sixteen refusal-name clones named as inherited `SourceRefusal` shape with the one-place repair
identified; **one latent tie-break named and measured as not firing** (the driver's `pick` on a
four-component key; the would-be tie is dominated out before `pick` sees it; the lawful repair is a
declared tie-break); **one owed split named, not claimed away** — the resource equation is now
written twice (`contract_candidates`, `athena_future_candidates`) and belongs in one owner both
families call. Ledger rows changed by hand; ledgers regenerated once.

## 4. The station table

| deed | grade | commit |
|---|---|---|
| A–D, H0–H5 | PASS (frozen) | the foundation's records |
| **P0 native baseline and morphology** | **PASS** | this record: ARM N executable from its own rest, plural, remounted fresh-process with audits, ablated with an honest boundary; the manifest measured/OPEN |
| P1 cross-chart lift | open — next: Φ_X/Φ_Y, χ_γ = Φ_Y T_γ − S_γ Φ_X over the declared source/native receiver and intervention family, exact BF16 fibres, source-independent native recombination | — |
| P2–P5, M0–M3 | open | in order |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-20 03:24:09 → 03:28:17 UTC, exit 0 —
**12 of 12**:

```text
PASS  tests              2611 passed, 0 failed, 19 ignored over 30 result lines; example targets type-checked
PASS  authored-levels    0 failures
PASS  named-paths · line-citations · claim-index
PASS  driver-catalog     265 drivers catalogued, 0 uncatalogued
PASS  output-manifest    recorded 63 drivers, present 63
PASS  closure-manifest   current: 64 return directories, 24 orphans
PASS  boundary-artifacts · typst · architecture-lint (244 files, 22,218 inherited) · document-law
12 passed, 0 failed
```

Deed P0 passed; proceeding to Deed P1.

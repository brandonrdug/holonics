# AC4–AC5: the joint operation spans blocks and the research model continues

[definition] This follows the [joint-field exposure](2026-09-09_AC3_THE_JOINT_FIELD_CONTINUES_AND_ITS_NATIVE_ARTIFACT_IS_GENERIC.md)
and [generic assembly/local return](2026-09-09_AC1_AC2_THE_HOLON_FIELD_OWNS_ASSEMBLY_AND_OBSERVED_RETURN.md).
The construction retains one native Holon field. Its source, local standing, joint current,
return factors and causal capabilities carry the operation; the exterior medium does not
supply another learning law.

## One operation, staged device execution

[established-bounded; implemented-exact; source-inspected] The former single-block contextual
kernel now exposes its existing historical weight-row and target-coordinate operations as
separate device stages. Each historical row writes its own four query weights and remainders.
Each target coordinate reduces historical contributions in the original increasing occurrence
order, retaining the signed sum and both bound accumulators. The existing serial-source q1/q2
reuse remains unchanged. These disjoint writes justify the distribution across blocks; block
count by itself is not evidence of independence.

[established-bounded; implemented-exact; source-inspected] Field/profile preparation saves the
already-checked relation row and held departure in resident scratch. Weights and coordinate
reductions follow on the same CUDA stream inside one captured passage. The final stage completes
the original bounds and coefficient calculation before committing basis, held current and
material state. Upstream or local refusal prevents that commit. Earlier paired/operative outputs
are pending sections. There is no added host numerical readout, semantic replay or continuing
field clone. The extra scratch is nine words per root in the existing two-lane section format:
144 bytes per root, or 2,592 bytes at this study's eighteen roots. Three kernel nodes are added
to the same passage; the scratch is temporary rather than new historical model material.

[established-bounded; computational-witness] Complete native rests agree between the preserved
single-block and staged implementations across all four contextual source profiles, direct
and operative coherent tensor targets, recharting, a shared historical anchor and a changed
contact map. A separate 514-occurrence control crosses the device's actual 512-thread block
boundary. A malformed historical source norm refuses in the later weight stage, preserves the
complete predecessor/source and admits a retry after that malformed test fixture is restored.
No live production ecology is cloned to supply these independently founded controls.

[established-bounded; computational-witness] The legacy Contextual fixture initially refused
nonintegral contacts on both implementations. Its existing source owner requires integral
contact/covariance factors; the success comparison now uses the established unit-current
control. Operative controls retain non-dyadic thirds. This was a refused fixture outside that
legacy source law, not evidence that the staged kernel changed the law.

[established-bounded; process-audit] The complete native field selection passed 119 CUDA tests
in 155.10 seconds. Its host selection passed sixteen tests. The updated public field rustdoc
compiled and the conversation example built. The checks exercise the changed resident field
and public invocation; no Lean source or cultivation binding changed.

## Research model anatomy and access

[definition] The delivered sixteen-family body has the following explicit anatomy:

| Constituent | Actual realization in this artifact |
|---|---|
| Incidence and chronology | Source-qualified native occurrences, field relation and shared source anchors; predecessor state and received-from source remain separate |
| Local standing | Held current and the continuing operative contact morphology/interior current, with historical forward carriers retained for their returns |
| Joint feature | Homogeneous projective source and context features, `Q(1,s) tensor Q(1,c)`; current and its remainder remain available |
| Learned conduct | Material current factors plus observed producing contact returns, using complex-current comparison and declared dyadic deposit |
| Exterior source chart | Nine binary pairs (eight octet bits and a part marker), hence eighteen root channels; duplex real/imaginary boundary directions |
| Exterior receiver | Joint tensor target of nine binary factors, hence 512 coordinates; only admitted byte/part codewords are presented |
| Next occurrence | The retained native successor receives the next source-qualified input; exterior self-actuation re-enters the selected symbol's unit-current chart |
| Numerical/backend scope | Exact integer/dyadic enclosure arithmetic with 72 fractional bits and explicit remainders, CUDA resident operation; no semantic floats or live Lean apparatus |
| Native rest | Relation, standing, learned material, historical carriers/frames and source capabilities, through the existing field wire |
| Application envelope | Text direction, pending/delivery state, conversation cursor and actual parent capability slots, separate from the native field |

[established-bounded; implemented-exact] `holonics::hna::native::{NativeFieldModelSpec,
with_native_field, NativeSavedField}` provide generic assembly and rest. The compiled example
in `crates/holonics-hna/src/native/field.rs` accepts caller-supplied material, currents, receiver
and realization and uses the actual native recurrence. `NativeSavedField` reads a bare field
wire; the conversation artifact is a `alpha::checkpoint::SavedTextField` envelope and must be
read through that owner. Its session remount delegates native restoration to `NativeSavedField`.
The research driver invokes those public owners; it supplies no fixture-local learner.

[definition] Private model, history and full reports remain under
`.local/artifacts/athena-alpha/ac3-joint-field-2026-09-09/`; source and compact evidence live in
this repository. `model.hna` is the 1,257,518,630-byte pre-prompt envelope. The checkpoint is
sufficient for native remount with a newly created history archive; further conversation
exposure separately requires its source stream. Private learned material is not included in
the public Git repository. The current package is a research artifact, not a standard executable
ONNX/Safetensors export or an attained useful Athena-alpha.

## Actual saved-model comparison

[established-bounded; computational-witness] The actual pre-prompt checkpoint remounted in
a fresh process using a new history archive. Remount performed zero native developmental deeds;
the driver did not open the conversation exposure stream. Its complete reported body (including
lineage, held state, relation, operative contacts and final material/junction), emitted-current
history, generation, development records, cursor and return storage agree exactly with the
original run. This compares the reported fields, not every unreported historical section; the
smaller native controls above compare complete rests. Output is again `I’m ante.` followed by
EndPart, at occurrences 5,126–5,138, with no native/development/prompt error.

[established-bounded; measured] Prompt plus emission took 67.799689202 seconds versus the
original workflow's 80.978041541 seconds. These are different surrounding workflows (fresh
remount versus development/checkpoint), so the roughly 16% decrease is an observed comparison,
not an isolated kernel speedup. The full new process took 238.331662244 seconds, including cold
loading/apparatus and diagnostics; peak process RSS was 5,612,060 KiB. Final native residency
is unchanged at 5,608,462,672 bytes. Peak native residency rises by exactly the 2,592-byte
temporary commit section. Large source transfer and serial portions remain substantial.

[established-bounded; computational-witness] The [comparison receipt](../experiments/native_field_assembly/2026-09-09_staged_continuation.json)
retains commands, exact equality fields, output, storage and native census. The original binary
and both full private reports remain available. No larger unchanged training run was substituted
for this specific execution/persistence question.

## Finite construction return

[established-bounded; implemented-exact; computational-witness] The revised finite AC0–AC5
cycle now has its required artifacts: the existing source/cursor (AC0), generic joint-field
assembly and tensor chart (AC1), attributed local observed return (AC2), broader material and
joint continuation/checkpoint (AC3), actual current-factor generator savings and scoped execution
comparisons (AC4), and documented Rust invocation, model anatomy and fresh-process delivery
(AC5). The exposed model remains available for further research through those owners.

[open] Useful English/code/mathematics and continuing contextual development at the original
Athena-alpha product standard are not attained. The joint field remains costly on this small
material extent; this change does not establish affordable full-corpus cultivation. Continuous
source-family attachment, semantic generator recovery beyond the measured numerical reuse and
other device backends retain their own construction obligations.

[definition] The Mac workflow can re-engage against the shared field recipe, occurrence/current,
observed-response and rest interfaces. The operative contextual/target/return stages provide
concrete backend comparison targets; CUDA agreement does not establish Metal parity. Acoustic
section/clock work can inform their shared source laws without waiting for useful English and
without adding an acoustic-specific learner to the desktop.

## Correction: the actual excitation and the meaning of the cost report

[established-bounded; source-inspected; measured] Brandon challenged the single-output report
after this return. The actual input was `Explain the difference between a function and its derivative.`
It contains 61 UTF-8 bytes. `alpha_text::run_session` calls `begin_part(None)`, receives those
bytes and one EndPart in the real/incoming chart, then requests imaginary/outgoing material
readings. The 62 arrivals advance the field from 5,064 to 5,126 occurrences. The first has no
retained interior source handle but retains predecessor state 5,063; it is a supplied exterior
excitation, not a causeless event. Later bytes use the preceding actual emission as source.
Observed local response is enabled and adds 61 contact returns, reaching 5,124. This is not a
frozen-coefficient prompt evaluation.

[definition] Here “prompt” names an exterior file and a driver schedule. The driver constructs
no distinguished native question object or understood function/derivative relation. Each input
uses nine one-of-two unit impulses, its declared orientation and the receiving field's standing.
The computational Holon includes the situated occurrence, source relation, current and successor;
the string alone does not supply that identity. A source/receiver-grounded account of elementary
excitation must therefore describe those operations before interpreting the displayed sentence.

[established-bounded; source-inspected] The selected output face is actuated through fresh
canonical unit impulses in `TextFieldSession::stage_material_actuation_with_phases`. The actual
material current and source witness remain retained; arbitrary amplitude/phase from that current
is not thereby transported into the next input. EndPart ends this exterior drive loop. It does
not measure extinction of an undriven current. The single sentence was reused for an execution/
restart comparison; it does not establish the learned model's excitation-response behavior or
explain why this particular sequence occurs.

[established-bounded; source-inspected; measured] “Unresolved large-corpus cost” was an imprecise
description of a concrete implementation cost already visible on very little material. The
sixteen exposed parts contain 5,048 text bytes plus sixteen part markers. Development took
2,753.900 seconds, with a 1,257,518,630-byte checkpoint and about 5.55 GB peak native residency.
`prepare_contextual_work` traverses and ensures residency of historical source sections; each
weight row pairs historical source/context, and every evaluated target coordinate reduces past
contributions. Stored operative currents/return factors also carry growing contact domains.
Multi-block execution preserves this dependency population. These choices explain sources of
work, not a measured attribution of every second. No full-corpus limit was tested or proved.

[established-bounded; measured] This cost did not first appear at the last return. The earlier
[sixteen-family normal study](2026-09-09_AC1_AC2_REPEATED_FACES_RETAIN_PHASE_AND_EXCHANGE.md)
already took 1,889.64 seconds for its eight additional families and saved a 1,518,483,970-byte
checkpoint. The profiles and workflows differ, so these figures are not a matched performance
comparison; they establish that growing implementation cost was previously recorded.

[definition] The finite AC return stands as scoped assembly, local-return, execution and
persistence evidence. It does not close the explanation of learned elementary conduct. The
next research comparison must build on the existing phase/paired-current controls: declare
source and local standing, vary an elementary excitation or its composition, and inspect the
retained current, reflection, return and later receiver difference. Repeated natural-language
questions or a new optimization-first gate would not answer Brandon's present question.

# WRD2: the candidate render is a function of the supplied route

**Date:** 2026-09-02
**Truth status:** `established-bounded`
**Evidence:** `measured` (focused Cargo tests, commands and dates below)
**Provenance:** Assistant (Claude), under Brandon's direct instruction of 2026-09-02 to deposit and complete the WRD campaign. Assistant construction; the render bodies are the standing MVF-era owners moved unchanged.
**Band:** THE ROUTE SUPPLIES THE ORGANS / THE RENDER PROJECTS AND CHOOSES NOTHING / SAME ROUTE SAME FAMILY / NO GROWTH OF THE ECOLOGY OWNER

---

## Statement

[definition] `soma/life/src/lean_mathematics/candidates.rs` owns
`LeanMathematicsEcology::generate_proof_candidates_for(problem, declarations, grain)`: the standing
render applied to an explicitly supplied declaration family instead of the ecology's own reach.
`generate_proof_candidates` and `generate_proof_candidates_at` keep their exact behavior by
composing `reached_declarations` with the same `render_candidates`. The bodies moved from
`ecology.rs` unchanged; `ecology.rs` shrank from 1,449 to 1,163 lines and `lean_mathematics.rs`
stayed at its 980-line baseline by folding module lines.

[established-bounded] `cargo test -p life --lib lean_mathematics` (2026-09-02, 16 tests) passes,
including `the_candidate_family_is_a_function_of_the_supplied_route`: the same route renders the
same family; a different route renders a different family; a family rendered from `alpha` alone
names no `beta`; an unknown organ refuses.

## Boundary

[established-bounded] The render is the standing template projection (`exact`, `simpa using`,
`rw`, `have`, and the closers the corpus's retained tactic species admit). It is the emitter the
WRD campaign inherited and did not replace. WRD4 measures what the kernel does with it.

# The classical machine-learning chart failed to descend and the holonic fibre reopened its compact continuation

## Audit finding

[counterexample; implemented-exact; measured] UAR2 did not stall because its sparse relational
complex was too large or because the GPU lacked work. It stalled at a representation disagreement.
The running construction treated a full factor/face/cell carrier, a receiver-selected
state/port/generator population, and a compact returned carrier as interchangeable without one
implemented dynamic quotient.

The shortest inspected occurrence returned:

- selected native face: `[1]`;
- admitted source-active factor: `[1138]`;
- host generator testimony: `1138 -> 1138`;
- restricted native population: contains `1138`;
- compact transported pre-projective support: `[1139]` on one presented branch and empty on its
  sibling;
- returned candidate support: empty.

No output claim can reconcile these presentations. The relevant statement is not that one index is
off by one; it is that the construction has not exhibited one quotient `q`, native transport
`T_g`, and compact transport `U_g` satisfying `q (T_g x) = U_g (q x)`.

## Mathematical diagnosis

[proved-derived; formal-checked] `ReceiverHistoryCompression` already proves that a generator-wise
commuting quotient descends through every ordered successor word. It also proves that one future
receiver/history separator refutes a proposed collapse. `AthenaReceiverHistory` strengthens the
same boundary to complete causal signatures. `HolonicDimensionObstruction` shows that rank and
kernel are receiver-relative; a hidden direction is not erased from the source merely because one
receiver kills it. `SituatedReturnedDifference` retains the complete target-fibre difference and
the reverse-order causal adjoint rather than replacing them by a scalar terminal face.

[project-postulate] The engine needs the corresponding linear descent theorem explicitly: a
quotient transport exists when the native transport preserves the quotient kernel. Conversely, a
single kernel element transported to a visible quotient direction proves that no proposed compact
transition can commute. This is the exact mathematical audit required by the UAR2 trace.

## Traditional machine learning as a bounded chart

[definition] A traditional fixed-state machine-learning model can be represented as a receiver
chart of holonic transport when its parameter/state carrier, labelled transitions, observations,
loss face, and update all factor through one dynamic quotient. A forward factorization proves a
simulation. Equivalence additionally requires a reachable reconstruction section or another
bisimulation witness. Equal output or equal scalar loss alone is strictly weaker.

[definition] Complex Parametrons are pre-locking addressed complex carriers. A perceptron, dense
layer, activation, softmax distribution, or scalar loss can arise as a declared receiver face
under fixed incidence, fixed basis, and a constitutive projection. That chart does not exhaust the
native carrier when relative phase, orientation, reconstruction fibre, or later morphology remains
causally available.

## Why the engine pauses

[definition] CUDA construction is paused, not discarded. UAR0 and UAR1 retain their existing exact
grades. UAR2 remains a counterexample until the Lean bridge proves the lawful configurations and
the engine maps each state representation to one theorem parameter. Another local indexing patch,
surface receiver, or generator rewrite before that bridge would be epistemically incapable of
distinguishing a real correction from a new accidental agreement.

[definition] The binding construction authority is
[`blueprint/THE_CLASSICAL_MACHINE_LEARNING_CHART_DESCENDS_FROM_HOLONIC_TRANSPORT_AND_HIGHER_CAUSAL_FIBRES_REOPEN_ITS_COLLAPSED_STATES.md`](../../archive/plans/THE_CLASSICAL_MACHINE_LEARNING_CHART_DESCENDS_FROM_HOLONIC_TRANSPORT_AND_HIGHER_CAUSAL_FIBRES_REOPEN_ITS_COLLAPSED_STATES.md).

## Formal return

[proved-derived; formal-checked] HML0--HML4 returned in five compiled Lean modules without `sorry`:

- `MachineLearningChart.lean` proves the dynamic quotient and the exact nonlinear criterion that a
  quotient successor exists precisely when the native successor is constant on every quotient
  fibre;
- `MachineLearningStrictLift.lean` proves invariant-kernel descent and the obstruction caused when
  one hidden direction becomes visible;
- `SituatedMachineLearning.lean` proves that inference has zero durable morphology, cultivation has
  a witnessed nonzero return, and a scalar loss governs learning only when the morphology return
  itself descends through that loss;
- `DependentMachineLearningCarrier.lean` proves exact sigma-type totalization and shows that
  higher flexibility is relative to a narrower receiver quotient, not an unsupported claim of
  computability beyond arbitrary classical state; and
- `CslibExecutionReceiver.lean` proves CSLib simulation, finite multistep and execution projection,
  and operational bisimulation while retaining distinct native occurrences in one quotient fibre.

[established-bounded; implemented-exact; measured] CSLib is pinned at `v4.33.0`, matching Lean and
mathlib. The five-target build completed successfully in 8.9 seconds. A later attempted rebuild of
an unrelated, actively edited aggregate dependency exposed existing errors in
`HolonicTorusMonodromyReceiver.lean`; those errors neither enter nor weaken the isolated HML build
and remain owned by the parallel Lean line.

## Exact UAR engine map

[definition] UAR's native `H` is not one buffer. It is the dependent product of
`ResidentSparseQuadraticMomentState` and `ResidentSparseRelationalCurrentState` with state ids,
pair/factor incidence, signed complex phase, and reconstruction testimony. `T_g` is the
generator-addressed inverse incidence in `generator_target_offsets`, `generator_source_pairs`, and
`generator_multiplicities`. The proposed compact `Theta` is the paired quadratic and relational
conditioned current. `SourceNeutralObservableContinuation` is a later receiver/history face.

[counterexample; implemented-exact; measured] There is presently no single engine owner for `q`.
`ResidentSparseQuadraticNativeBoundary`, its target-state restrictions, and
`ResidentFactoredHistoryQuotientPassage` each carry partial quotient testimony. That fragmentation
is the computer-science error underlying the mathematical mismatch.

[definition] The resumed UAR2 construction must first unify those partial presentations as one map
over the complete native product and test, for every generator and every pair in each finite fibre,

`q x = q y -> q (T_g x) = q (T_g y)`.

Passing this law constructs `U_g` on the actually presented range. A failing pair is the shortest
separator and enlarges the compact carrier. The implementation may not repair failure by changing
factor numbers, selecting a different exterior receiver, or making the source conform to the old
compact dimension.

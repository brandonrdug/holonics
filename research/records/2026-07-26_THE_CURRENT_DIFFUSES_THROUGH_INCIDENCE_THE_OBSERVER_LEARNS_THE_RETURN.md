# THE CURRENT DIFFUSES THROUGH INCIDENCE; THE OBSERVER LEARNS THE RETURN

**Date:** 2026-07-26  
**Status:** DERIVATION DEPOSITED / EXACT FINITE DIFFUSION LAW BUILT /
CAUSAL ORBIT TESTIMONY BUILT / INTERACTIVE TRACE BUILT /
49 LIBRARY TESTS PASS / CUDA HOST-CARD PARITY PASS /
NO GUI LAUNCHED BY SOL

## Present question

The interactive receiver exposed three phenomena which could not be graded by
the image alone:

1. apparent stabilization while continuation events remained active;
2. failure to retrace the same state after applying the opposed wheel deed; and
3. staircase-like changes in complexity as exact ratios crossed local regions.

The required construction was not another visual effect. It was an exact
separation between:

- the physical current law;
- the participating receiver state;
- the finite aperture's visible support;
- a recurrence or phase-boundary observer; and
- a learned quotient over recorded transitions.

## What the current desktop was actually doing

For its selected hinge, the desktop declares the exact discrete action with
\(I=K=2\). Between boundary deeds,

\[
p_k=2(q_k-q_{k-1}),\qquad
J_k^{\mathrm{internal}}=-2q_k,
\]

so

\[
q_{k+1}=q_k-q_{k-1}.
\]

The state pair has the invariant

\[
\mathcal Q(q_{k-1},q_k)
=q_{k-1}^2-q_{k-1}q_k+q_k^2
\]

and returns after six unforced events. The old logger printed only when the
event ordinal was divisible by \(60\). It therefore observed every tenth
return to one exact phase. The screenshot's repeated
`q=1/2 momentum=0` was a sampling alias, not evidence of fixed-point rest.

There was also a genuine engine defect. If the next coordinate equalled the
current coordinate, the physical carrier was not advanced because kinematic
`changed_hinges` was empty. This confused equality of one projected value with
absence of an event. The selected trajectory now advances across every
completed physical event. A turning point retains its elapsed order and
incoming current.

Opposite wheel deeds do not generally retrace the world. A wheel occurrence
changes receiver basis standing and, by this desktop world's declared input
relation, supplies a physical boundary impulse. Negating a later receiver
offset does not reverse the intervening physical orbit, conic changes, or
phase at which the second impulse enters. This is path dependence, not
floating error.

The visible staircase has a precise candidate source. Conic and segment
support changes only when an exact crossing enters or leaves the bounded
receiver face or one of its finite presentation members. Those discriminant
events change traced-address and support-query populations discontinuously
while the source coefficients change by exact ratios. This is a phase
stratification of the presentation law. It is structurally comparable to a
staircase, but this record does not identify it with the Riemann zeta
staircase.

## Physical diffusion law

The engine now contains one exact source-declared diffusion ecology on a
finite oriented incidence complex. A node \(v\) carries content \(n_v\) and a
positive capacity magnitude \(m_v\). A branch \(e:s\to t\) carries a
nonnegative conductance magnitude \(c_e\). These magnitudes define the local
constitutive law; orientation remains in incidence, and the sign of current
emerges from the received potential difference:

\[
\phi_v=\frac{n_v}{m_v},
\qquad
j_e=c_e(\phi_s-\phi_t).
\]

For exact positive event interval \(\tau\) and signed source deed \(s_v\), one
completed event is

\[
n^{+}=n+s+\tau\,\partial j^{+},
\qquad
M\phi^{+}=n^{+}.
\]

Equivalently,

\[
(M+\tau L)\phi^{+}=n+s,
\qquad
L=\partial C\partial^\mathsf T.
\]

This relation is solved by exact rational elimination. It is not an
approximation to a continuous PDE. The receipt returns every potential,
oriented current, integrated transfer, node balance, total conservation
residual, and quadratic stored-energy change. On a closed source-free
complex,

\[
\sum_v n_v^{+}=\sum_v n_v
\]

exactly, while the declared positive local metric makes stored quadratic
energy nonincreasing. A two-node witness carries

\[
(1,0)\longmapsto(2/3,1/3),\qquad j=1/3
\]

with zero conservation residual and exact energy departure \(2/9\).

This law is one physical species. Higher simplicial transport can replace its
graph incidence with the appropriate boundary/coboundary degree; harmonic
cycles then live in the corresponding kernel. The implementation does not
claim that every holonic event must diffuse.

## What modern diffusion learning contributes

Modern machine-learning “diffusion” contains several separable mechanisms:

- SEDD learns ratios between neighboring discrete-state probabilities rather
  than requiring a continuous score field. This is closest to the laboratory's
  receiver-relative recurrence quotient:
  <https://arxiv.org/abs/2310.16834>.
- Diffusion Forcing assigns different noise levels to different sequence
  members, joining causal next-token and full-sequence diffusion. Its useful
  abstraction is independently conditioned constituent grain, not Gaussian
  noise as ontology: <https://arxiv.org/abs/2407.01392>.
- Stochastic interpolants for forecasting explicitly distinguish the learned
  fictitious sampling dynamics from the physical system being forecast. This
  is the necessary boundary here:
  <https://proceedings.mlr.press/v235/chen24n.html>.
- Physics-Constrained Flow Matching enforces physical restrictions through
  the generated path rather than asking a terminal penalty to repair an
  unlawful result: <https://arxiv.org/abs/2506.04171>.
- Hodge diffusion supplies the correct generalization from node smoothing to
  currents, cycles, and higher cells:
  <https://arxiv.org/abs/2504.07910>.
- 2025 work on flow geometry identifies attraction, absorption, local
  clustering, and terminal singular behavior relative to the data manifold:
  <https://proceedings.mlr.press/v267/wan25e.html>.
- Streaming Flow Policy removes the “trajectory of trajectories” wait and
  emits action along one developing flow. That is relevant to live causal
  return, though its learned float machinery is not imported:
  <https://proceedings.mlr.press/v305/jiang25a.html>.

The resulting separation is strict:

1. **Physical diffusion** is the enacted incidence/current/consequence law.
2. **Learned diffusion** is an observer-relative transition law over recorded
   physical paths and contexts.
3. **Presentation** is one receiver's finite quotient of those paths.

The learned observer may count exact contextual recurrences such as

\[
\Pr_{\mathcal O}(D\mid C)
=\frac{N_{\mathcal O}(C\leadsto D)}{N_{\mathcal O}(C)}
\]

without installing chance into the physical law. Here “probability” is the
first-person recurrence quotient at observer \(\mathcal O\), and “loss” is the
oriented residual between an afforded transition and the returned
consequence—not reward or punishment. Context, receiver standing, event
grain, and path are part of \(C\); they may not be discarded to make a scalar.

## Research instrument

`desktop_receiver` now writes a tab-separated event record at:

`target/holonic-engine/desktop_receiver_trace.tsv`

The path may be replaced with `HOLONIC_ENGINE_TRACE`. Each row retains:

- the complete exact physical hinge state;
- the complete receiver-control standing;
- boundary versus continuation event kind;
- coordinate, momentum, internal and external impulse;
- the exact orbit invariant and detected return period;
- changed conic count;
- aperture extent;
- selected CUDA arithmetic and proved intermediate bit bound;
- traced support and exact query populations; and
- wall time.

This is observer testimony, not active physics. It is sufficient to separate:

- `q,p` constant: candidate physical rest;
- full state repeated with period \(k>0\): exact orbit;
- physical state changed but support unchanged: projection plateau;
- support population changed: presentation discriminant crossing;
- receiver state restored but physical state different: path dependence; and
- a change in arithmetic tier: computational phase boundary.

The next interactive run is therefore analyzable as an exact causal path
rather than a sequence of images or scalar snapshots.

## Verification

- `cargo test -p holonic-engine --all-targets`: **49 library tests passed**,
  **1 desktop test passed**, **1 CUDA test ignored by default**.
- Explicit RTX 4080 SUPER host/card witness: **passed** at 184 intermediate
  bits through the 192-bit kernel; complete support exact.
- A coefficient of magnitude \(2^{63}\) crosses the new signed-magnitude
  128-bit seam in both orientations.
- A later finite-device range refusal preserves exact standing and the last
  valid face instead of terminating the window; autonomous continuation pauses
  until another input.
- No `f32`, `f64`, floating constant, continuous numerical integrator, or
  stochastic physical source entered the engine.
- Sol did not launch or manipulate the desktop program.

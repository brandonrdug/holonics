# WB4 released the Holonics Workbench CLI and Ratatui application

**Date:** 2026-09-01
**Truth status:** `established-bounded` for the documented and exercised Workbench application over
its declared Athena, Eros, Soulkiller, and engine command families.
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, and `measured`.
**Construction effect:** WB4 passed; WB0--WB4 are complete and no Workbench deed remains scheduled.

## Operator return

[established-bounded; implemented-exact] The independent `holonics` application is buildable from
the workspace or installable onto the user's `PATH`. Its CLI and Ratatui terminal UI submit the
same serializable `WorkbenchCommand` values to one `WorkbenchRuntime` and render the same
`WorkbenchEvent` values. Presentation never shells from one interface to the other and carries no
second inference, routing, morphology, or storage law.

[established-bounded; implemented-exact] The operator guide returns build/install instructions,
TUI controls, a complete first Athena session, exact rational return syntax, explicit artifact
paths, CLI examples, Athena/Eros/Soulkiller/engine command anatomy, consumer-hardware expectations,
and explicit capability boundaries. The interface remains an exterior application rather than an
engine blueprint or new native owner.

## Real application walkthroughs

[established-bounded; process-audit; measured] Real commands returned exit `0` for human `status`,
JSONL `capabilities`, Soulkiller configuration inspection, Eros Rust-material atlas inspection, and
the complete CLI help surface. `status` observed 24 CPU hardware threads and the NVIDIA GeForce RTX
4080 SUPER CUDA chart with 384 block threads; Soulkiller inspected the supplied chart without
foreign execution.

[established-bounded; process-audit; measured] A pseudo-terminal opened the real Ratatui
application. The operator sequence `:status` entered the shared parser/runtime, appended the same
24-thread/RTX 4080 SUPER structured event to the timeline, and populated the payload inspector.
`q` then returned exit `0`, restored the alternate screen, and made no persistent artifact.

[established-bounded; process-audit; measured] The focused Workbench suite returned nine passed,
zero failed: seven library controls and two integration controls. These include direct/CLI and
direct/TUI event equality, headless 60x16 and 120x36 rendering, key/update behavior, the multi-step
Athena lifecycle, adapters, exact export, malformed input, and missing-material refusals.

## Coherent release and cleanup

[established-bounded; formal-checked; process-audit; measured] The complete release command

```text
timeout -k 2s 180s bash tools/gates.sh
```

returned exit `0` within its outer bound: 2,966 tests passed, zero failed, 50 ignored over 35 result
lines; every example target type-checked; the live Lean umbrella built 3,771 jobs; tracked authority,
source shape, epistemic tags, claim index, equation atlas, all ten Typst roots, and document law
passed. Nine gates passed and zero failed.

[established-bounded; process-audit; measured] At cleanup, root `target/` occupied 36 GiB by the
filesystem summary. `cargo clean` removed 15,274 replicable files totaling 38.8 GiB. Root `output/`
was absent. These compiler/experiment artifacts were not Athena identity or application storage.

[open] The released surface does not establish unrestricted dismantling of arbitrary foreign model
directories, qualitative text/image/audio generation, automatic discovery of every engine owner,
or distributed ecology control. Those are displayed as open capability fibres and schedule nothing.

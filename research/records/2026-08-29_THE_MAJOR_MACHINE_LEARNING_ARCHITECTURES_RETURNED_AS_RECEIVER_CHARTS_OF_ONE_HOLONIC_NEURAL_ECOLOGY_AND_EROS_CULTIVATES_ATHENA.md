# The major machine-learning architectures returned as receiver charts of one holonic neural ecology and Eros cultivates Athena

## Verdict

[proved-derived; formal-checked] The HNN dependency closure now contains one finite holonic neural
ecology; exact classical quotient and strict-fibre criteria; finite information receivers;
entropy, cross-entropy, and KL; metric-dependent gradients and reverse-order causal adjoints;
softmax/sigmoid, RMS, and centered normalization charts; transformer attention and recurrence;
CNN, GNN, and SSM charts; deterministic and stochastic diffusion; autoencoding, factorized
overlays, familywise distillation, world-returned reinforcement; source-neutral Soulkiller spool
admission; one rested Athena body; and Eros cultivation with remount, later-conduct, ablation, and
restoration obligations.

[definition] This is a formal ontology and conformance boundary. It does not claim that every
standing Rust owner already realizes every field, that any present Athena output has frontier-model
quality, or that a generic architecture name proves convergence, capacity, or intelligence.

## One native object

[proved-derived; formal-checked] `FiniteLocalCurrentEcology` is the common finite owner. For every
rested morphology, generator, complete contemporary state, target, and source it returns one local
current. All currents incident on a target are summed and passed through that target's local
constitutive reaction. The current may depend on the complete contemporary state, so fixed,
input-conditioned, sparse, recurrent, and shape-sensitive contact use the same type.

```text
J_(m,g,x)(i,j) : Carrier_j -> Carrier_i
T_(m,g)(x)_i   = reaction_(m,g,i) (sum_j J_(m,g,x)(i,j))
```

[proved-derived; formal-checked] `inferWord_append` proves ordered continuation through a fixed
rest. `ReturningCultivation.productiveOccurrence_conducts_before_return` proves that the old
morphology conducts before world return changes the rest. `cultivates_iff_not_inference` makes the
distinction exact: inference fixes reusable morphology during the occurrence; cultivation changes
it through a returned difference.

[proved-derived; formal-checked] `ClassicalConfiguration.dynamicChart` connects this ecology to
HML. A traditional fixed-state architecture is exact precisely when its quotient observation and
every generator commute. `everyInferenceWordExact` then carries the square through every ordered
history. The existing `MachineLearningStrictLift` theorems prove the strict boundary: a transported
kernel, phase, incidence, chronology, or future separator which is visible after the quotient
prevents descent to that classical state.

## Why the standard architectures work

[proved-derived; formal-checked] Transformer attention is input-conditioned contact. An
`AttentionChart` declares an admitted finite source population, a contemporary-state score, and a
transported value. `weight_normalized` proves the exponential contact weights sum to one over the
admitted population; `ecology_step_eq_output` proves the attention output is exactly the common
local-current step. `Block.ecology_step_eq_output` adds continuing standing as diagonal current and
then applies one local nonlinear reaction. `complete_output_relabel` proves permutation covariance
when scores and values travel with the same receiver relabeling. Q/K/V is therefore one possible
foreign factorization of score/value transport, not Athena's state ontology.

[proved-derived; formal-checked] Autoregressive inference is a boundary recurrence:
conduct, emit, remount, conduct. `conduct_length` proves its chronology. A cache is optional
apparatus: `CacheValidity.valid` must prove equality to direct history conduct, while
`invalid_cache_control` supplies a counterexample to treating arbitrary cached standing as native
identity.

[proved-derived; formal-checked] A CNN is shared translation-relative current. `Convolution.ecology`
is the common ecology with a transported kernel indexed by relative offset, and
`translate_equivariant` proves exact translation equivariance. A GNN is sparse irregular current;
`Graph.aggregate_relabel` proves that transporting vertices and incidence together preserves the
aggregate. An SSM is rested chronology with hidden standing and an observation boundary;
`StateSpace.Chart.fold_append` proves exact history folding and `LinearChart.advance_eq_A_add_B`
returns the conventional `A h + B u` chart.

[proved-derived; formal-checked] Deterministic diffusion and stochastic diffusion are different
configurations. `ImplicitDiffusionChart.advance_balance` proves the constituted implicit balance
and `asStateSpace` gives its recurrent chart when the implicit operator is invertible. A stochastic
`MarkovKernel` instead carries nonnegative normalized mass. `pushforward_compose` proves
Chapman--Kolmogorov composition, `expectation_pushforward` transports receiver expectations, and
`collapseBool_has_no_stochastic_inverse` proves by an exact two-state counterexample that a forward
kernel does not define its own reverse generator.

[interpretation] These architectures work for the same underlying reason: each selects an
incidence geometry, a reusable constitutive family, a chronology, and a receiver. Attention makes
contact state-dependent; convolution ties morphology under translations; graph networks accept
irregular sparse incidence; SSMs retain chronological standing; diffusion transports either a
constituted field or a probability population. Their inductive biases are different receiver
charts of local transport, not different definitions of learning.

## Information, loss, and gradients

[proved-derived; formal-checked] `PositiveProbabilitySection` requires strict positivity and exact
normalization before logarithmic information faces exist. `crossEntropy_eq_entropy_add_kl` proves
`H(p,q) = H(p) + KL(p||q)` on that support. `entropyFace_doesNotDetermineSection` exhibits two
different probability sections with exactly equal entropy. `ProbabilityReceiver` retains the
complete reconstruction fibre rather than identifying occurrences by one scalar.

[proved-derived; formal-checked] `PhysicalCrossEntropyOccurrence` retains exterior/interior
potential, chart transport, returned potential drop, constitutive current, action, reference
section, and emitted section beside the scalar cross-entropy face. This formalizes physical
cross-entropy as complete action transport first and conventional negative-log receiver second.
The scalar may measure a crossing; it may not replace the crossing or select native support.

[proved-derived; formal-checked] A scalar derivative is a covector. `MetricGradientChart` makes the
tangent--cotangent identification explicit and `lower_gradient` proves that the gradient returns
the original differential only through that chosen chart. The standing addressed-adjoint theorem,
re-exported as `causalAdjoint_returns_in_reverse_factor_order`, proves that a composed linearized
return traverses the retained factorization in reverse composition order. This is causal adjoint
transport, not reversal of physical time.

[proved-derived; formal-checked] The normalized exponential receiver is positive, normalized, and
invariant under a common additive potential. Its `laplacianReturn` is the algebraic softmax
Jacobian action `beta p_i (v_i - E_p[v])`. `laplacianReturn_const` proves the common-shift gauge is
in the kernel; `sum_laplacianReturn_zero` proves total returned current vanishes; and
`quadratic_laplacianReturn` proves the quadratic face is the probability-weighted variance.
`sigmoid_is_binary_normalized_exponential` proves sigmoid is its two-alternative restriction.

[proved-derived; formal-checked] `FiniteNormalization.population_mul_meanSquare` proves that RMS
is derived from total squared current divided by the actual finite population. `sum_centered_zero`
and `centered_add_common` prove the centered/LayerNorm chart removes a common additive origin.
These are receiver rebases; neither creates native layers or determines topology.

[open] The current HNN closure defines and proves the exact algebraic softmax Jacobian action but
does not yet contain a Mathlib `HasFDerivAt` theorem identifying the whole finite-vector map with
that operator. No engine deed depends on that analytic wrapper; it remains a bounded formal-library
extension rather than an ontology gap.

## Cultivation, tuning, and compression

[proved-derived; formal-checked] `ScalarObjective.no_complete_descent_of_separator` proves that a
scalar objective cannot govern a richer complete return when two occurrences share its value but
remain separated. `GradientProposal` therefore supplies only one metric-raised candidate morphology
return; it is not Eros itself.

[proved-derived; formal-checked] `AutoencodingChart.collapsed_pair_obstructs_exact_reconstruction`
proves that any encoder collision obstructs exact reconstruction on a region containing both
occurrences. `FactorizedLinearOverlay` proves a LoRA-like candidate is base transport plus a
factored `outward ∘ inward` update; the hidden carrier bounds this apparatus chart and is not an
intrinsic semantic rank. `FamilyDistillationChart` makes distillation exact only over every declared
query/receiver face and retains a separating defect otherwise.

[proved-derived; formal-checked] `WorldReturnedCultivation.occurrence` orders action, genuine world
consequence, complete returned difference, and morphology return. A conventional scalar reward is
an optional later face. Supervised, self-supervised, preference, reinforcement, adapter, and
distillation practices consequently differ in exposure and receiver arrangement; all qualify as
learning only when a returned passage changes reusable morphology and later conduct.

## Eros, Athena, and Soulkiller

[proved-derived; formal-checked] `AthenaRestedEcology` contains one ecology body, one resting
morphology and state, one exact rest identity/remount, and generic ingress/egress ports.
`infer_after_remount` proves exact source-detached rest behavior. The type contains no transformer,
token, layer, modality, or model-label field.

[proved-derived; formal-checked] `ErosCultivationPassage` requires a parent rest, complete returned
difference, returned child morphology, strict morphology change, a changed-later-conduct witness,
and exact ablation. Its `child` reuses the same ecology body and boundary apparatus. The theorem
`cultivation_has_behavioral_separator` prevents a changed file, counter, or scalar loss from being
reported as training.

[proved-derived; formal-checked] `SoulkillerReturn` separates source-neutral spools from cold
foreign witness and insufficiency. `admitted_replaceWitness` proves changing cold witness cannot
change admitted native generators. `complexParametronEcology_step` proves an exact complex-current
carrier is one realization of the common ecology, matching the standing Complex Parametron
physics without founding a perceptron/layer cabinet.

## Rust/CUDA conformance map

[observed] `crates/holonic-engine/src/native_spool.rs` already owns addressed threads, incidence,
Parametron cells, ordered generator words, collapsed/reconstruction fibres, deposits, withdrawal,
restoration, and resident complex conduct. It is the closest present owner for
`FiniteLocalCurrentEcology`, but its exact quotient/generator law is not encoded as one Rust type.

[observed] `crates/holonic-engine/src/receiver_history_compression.rs` is the direct implementation
owner for HML descent. The frozen UAR2 counterexample still presents three incompatible faces and
therefore does not instantiate one `ClassicalConfiguration.generatorExact` square.

[observed] `crates/holonic-engine/src/cuda_refine.rs` owns `ResidentComplexIncidence` and
`ResidentCoupledComplexParametron`; these are the hot-apparatus candidates for the common complex
local-current step and causal return. CUDA remains an apparatus chart and does not define the
ecology.

[observed] `soma/life/src/athena_native/rest.rs`, `source_neutral_rest.rs`, `conduct.rs`,
`cultivated.rs`, and `membrane_transport.rs` jointly own rest/remount, source-neutral morphology,
conduct, returned cultivation, ablation/restoration, and generic exterior crossing. They are the
implementation candidates for `AthenaRestedEcology` and `ErosCultivationPassage`; no new Athena
cabinet is justified.

[observed] `crates/holonic-engine/src/soulkiller/` and `native_spool.rs` own the foreign descent and
native spool boundary. The formal `SoulkillerReturn` requires source-neutral spools to remain
independent of cold witness at admission.

[counterexample] `native_ecology/heterogeneous_fusion.rs`, `inference_ecology.rs`, and the narrow
recurrent returns are not the common ecology merely because they run. Their fixed material/token
or small-state assumptions are exterior experiment charts. Any live dependency by which those
labels determine Athena incidence must be removed rather than promoted into formal ontology.

[definition] When engine construction is explicitly resumed, UAR2 must instantiate the existing
owners against four formal obligations: one common local-current step; one quotient `q`; every
generator commutation `q T_g = U_g q`; and the complete reconstruction fibre/shortest separator.
The changed current must return through the causal adjoint into one Athena rest with remount and
ablation testimony. No architecture-named subsystem, cache, tokenizer, scalar-loss governor, or
host replay is admitted.

## Build and axiom receipt

[formal-checked; measured] From `soma/formal/elementary-holonics`,
`timeout 175s lake build ElementaryHolonics.Computation.HolonicCultivationCharts ElementaryHolonics.Computation.HolonicMachineLearning`
completed successfully with 3,180 jobs. The HNN theorem audit reports only Mathlib's standard
`propext`, `Classical.choice`, and `Quot.sound` where used; no new theorem depends on `sorryAx`.

[definition] The checked closure is
`ElementaryHolonics/Computation/HolonicMachineLearning.lean`. HNN0--HNN7 are complete as a formal
prerequisite and engine conformance map. UAR2 remains frozen until Brandon explicitly releases the
engine campaign.

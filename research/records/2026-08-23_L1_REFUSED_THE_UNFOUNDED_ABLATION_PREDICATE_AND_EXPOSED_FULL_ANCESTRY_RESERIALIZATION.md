# L1 refused the unfounded ablation predicate and exposed full-ancestry reserialization

**Date:** 2026-08-23  
**Truth statuses:** `measured` for the command, wall time, returned files, process telemetry, and
refusal; `proved-derived` for the corrected ablation predicate; `implemented-exact` for the
addressed-prefix identity repair after it type-checked; `open` for the replacement L1 occurrence
until its complete grade returns.

The first executable L1 candidate correctly refused. The command was:

```text
cargo run -p life --example returned_theorem_families_cultivate_the_continuing_laboratory_rest
```

It ran for 810 seconds and returned exit status 1. Before refusal it returned two accepted exterior
Lean family passages, the exact one-family midpoint, a 7,301,588-octet native rest, and a complete
source-detached held-out passage on the NVIDIA GeForce RTX 4080 SUPER. The held-out passage returned
routes `[1,1]`, joint route `true`, two local ablations `[0,1]` and `[1,0]`, one synchronization, two
kernel launches, one typed reduction, exact semantic work `16`, span `5`, and no forbidden source
descriptor.

The second, separating inquiry returned both constraints false. Its correct local routes were
already obstructions. The exterior assertion nevertheless demanded that every *unwithdrawn*
family have cultivated route `1`; it therefore conflated "the independent family is unchanged"
with "the independent family is cultivated." The derived predicate is pointwise:

```text
withdrawn family: route = ablated_route and route != cultivated
every other family: route_after = route_before
```

This preserves a cultivated route when it stood and preserves an obstruction when the inquiry had
already reopened that family.

The same run measured the serialization defect. The process read 2,716,688,384 apparatus octets
while mounting the frozen Git delta and repeatedly revalidated and re-encoded inherited L0 standing
during local commit, inquiry admission, rest, and remount. That is not the L1 mathematical deed.
The L1 owner was therefore corrected to keep inherited L0 standing private, validate it at
construction/remount, and identify each later state from:

```text
addressed L0 identity
  + frozen successor chronology
  + exact local cultivation prefix
  + decoder
  + reconstruction boundary.
```

Every committed cultivation now carries the identity of its exact prefix, and validation recomputes
that prefix identity. Local inquiry, commit, ablation, and withdrawal no longer materialize the
complete inherited standing merely to name the continuation. The rejected output directory was
removed after this record captured its returned consequence; it was never admitted as a product.

The replacement closure reached both detached passages in 74 seconds and then returned a second
useful exterior obstruction: `ring_nf` refused because the F₂ conjuncts were already in normal form.
The individually accepted family proof already supplied the exact route, `constructor; ring`; the
combined emission was corrected to use that same construction. No resident law changed.

The later no-host-foreman audit found a third, more fundamental refusal before commit: the first
CUDA wrapper computed the complete action and constraint result on the CPU as an expected value,
and detached ablation re-uploaded the invariant matrices once per family. That candidate was
superseded. The final wrapper predicts both exact work/span alternatives from extents, lets the card
evaluate and select them, and inspects only the terminal typed receipt. The same resident reduction
returns every local ablation alternative, so one inquiry uploads standing once and performs exactly
two launches and one synchronization.

# Parallel mathematics distinguishes a material actuation from a new observation

[project-postulate] Brandon requests analysis of the mathematics conducted alongside native
construction and its use to refine the engine. AC0–AC5 remains the active product goal. This
return closes a particular AC1–AC2 source/receiver defect; useful generation remains open.

## Mathematical dependencies and their consequences

[established-bounded; source-inspected] The review covers the parallel returns at `ef47e263`,
`04064e78` and `b1010d49`, their actual formal owners, and the native owner at `b325d013` plus
this change. The prior [receiver integration](2026-09-08_AC_THE_PARALLEL_RECEIVER_CALCULUS_REFINES_NATIVE_CONTINUATION.md)
already installed the joint-ball receiver. This continuation uses that receiver rather than
replacing it with a marginal or centre-only decision. Lean remains exterior verification;
no Lean source, parser, compiler or verdict participates in native execution.

| Mathematical owner under `ElementaryHolonics/` | Consequence used in this refinement |
|---|---|
| `Transport/ReceiverPotential.lean` | A fixed future face does not identify a unique source. Preserve the whole available current family behind an executed face. |
| `Foundation/JointReceiverDescent.lean`, `Geometry/SwingPotential.lean` | Compression and recharting carry the joint receiver and admitted generators. Reusing a selected word does not establish equality of latent currents. |
| `Transport/ChangingReceiver.lean`, `Physics/FluidReceiverClosure.lean` | Source, receiver and constitutive changes have distinct returned defects. Keep mixed and hidden contributions before taking scalar bounds. |
| `Physics/ConstitutiveCurrentReduction.lean`, `Physics/ConstitutiveScale.lean` | Derive active coordinates from the actual law; retain the remainder and its future coupling. A boundary face alone does not justify deleting internal current. |
| `Transport/WorldTubePotential.lean`, `Physics/ConstitutiveWorldTube.lean` | Continuation joins the complete source-qualified successor, including pending action and chronology. Equal endpoint words do not suffice. |

[proved-derived; formal-checked] The [torus construction](2026-09-08_CLOCKED_TORUS_CURRENTS_CONTINUE_THROUGH_A_RETAINED_FIBRE.md)
has `j = Jq + Dz + r`, with `Cr = Dᵀr = 0`, and preserves the joint receiver through every
admitted input word. The [changing-constitution return](2026-09-08_CHANGING_CONSTITUTION_AND_RECEIVERS_SHARE_ONE_DEFECT_CALCULUS.md)
retains chart motion and the full nonlinear feedback. Its mechanical counterexample has exact
coarse descent while energy increases. These results distinguish a correct representation from
a successful finite learning step; they supply neither an automatic neural dimension nor a
claim that the observed language failure is solved.

[proved-derived] In the native paired law with fixed contact map D, `a=Db` and `C=DD†` give
`v=2(I+C)⁻¹(u+a)`, `out=v-u`, and `a_next=Cv-a`. The component of b in `ker D` changes sign.
Changing D can expose it: `(1,1)(1,-1)=0`, while an additional row contribution `(1,0)` reads
the same current as 1. Consequently neither today's fixed packet face nor boundary closure
licenses replacing the continuing field by that face. This is the same finite feedback issue
identified by the parallel joint-receiver and fluid constructions.

## The concrete defect and native correction

[established-bounded; source-inspected] Previously, the ordinary material generation path
decoded a selected word and supplied it through `NativeFieldOccurrence::through`. That path
declared an observed source/target relation, formed another contact and fitted the material
operator to its own selection. The source-withdrawal comparison avoided that fit but removed
the causal source edge. Neither operation expressed applying the already produced response.

[definition] `NativeMaterialActuation` now witnesses a reading of an available native emission.
The emitted source, selected coordinate, target chart and real/imaginary receiver travel together.
The original material current and its Euclidean enclosure stay in source history. The applied
tensor-basis current is a declared realization of that receiver face, not the latent current,
an inverse of its Preimage Fibre, or an additional independent observation.

[established-bounded; source-inspected] `field/receiver/packet.rs` issues this witness and
`NativeFieldOccurrence::actuating` transfers its linear source capability into the pending
occurrence. `field_material_actuation.cuh` checks the supplied unit-phase tensor basis and its
selected quadrature on the GPU before the continuing field write. The same recurrence advances
held, outgoing and internal current and publishes its successor. Its lineage retains
`received_from`; `observed_source()` is absent for this action, so observation-specific formation,
fitting and return consumers do not manufacture another observed relation.

[proved-derived] For a complex Euclidean ball with centre c and radius r, coordinate k is its
unique maximizing face on the declared quadrature exactly when every competing gap is positive
and `gap² > 2r²`. The difference receiver has norm √2, so its minimum gap is `gap-√2 r`.
The native packet owner already establishes the fixed source face; the actuation kernel checks
the supplied realization, and cold rest validates both. All comparisons use exact integers or
rationals. A tied or otherwise plural packet reading remains open in this particular adapter;
this does not impose a universal certainty condition on other native receiver uses.

[definition] Applying a transport is an input to the existing recurrence, not a separate
training/inference or world-verdict law. Ordinary incoming messages, demonstrations and later
external responses still use their source-qualified observation paths. The native actuation
does not depend on a speaker label, expected answer, UTF-8 filter or authored semantic class.
The supported chart is a material tensor product; retained-anchor and resident-input actuation
are not supplied by this constructor. Legacy constitutive point-current generation remains
available at its existing scope.

[established-bounded; source-inspected] The text session uses this native action by default for
material joint packets, preserving its I/Q codec. Explicit observation and source-withdrawal
modes remain comparison controls. A pending checkpoint stores the receiver descriptor and
actual source capability; remount reissues its native witness before continuation. Complete
field rest retains the applied source edges and validates their faces. Old checkpoints without
the optional pending descriptor retain their earlier interpretation.

## Executed consequences

[established-bounded; measured] The saved sixteen-family model completes the same prompt and
emits six UTF-8 bytes, `I’mn`, then `EndPart`. All seven generated occurrences retain their
actual preceding source and material-actuation descriptor. Complete generation, recorded
emission-current history and all numerical body receivers match the earlier withdrawal run
exactly; their causal lineages differ as intended. The fresh process takes 76.688 seconds,
including mounting and cold diagnostics; prompt plus generation takes 36.029 seconds. Peak RSS
is 4,757,544 KiB. The [portable receipt](2026-09-09_material_actuation/return.json) records the
commands, scope, comparisons and source-qualified generated lineage.

[counterexample; measured] This model still does not answer the request. Replacing the invalid
continuation-byte loop with `I’mn` and stopping is insufficient for useful language. The repair
also leaves the pre-existing 2,496,666,560-byte return journal unchanged. Native source correctness,
exact persistence and local tests do not mark AC1, AC2 or Athena-alpha complete.

[established-bounded; measured] Native controls check wrong-face refusal without mutation,
foreign-witness refusal without capability consumption, correct application without a new fit,
and a later observation that can still develop the field. The field suite returns 109 passes
and one allocation-calibration failure before that test's engine operation; the isolated retry
passes. Thus all 110 field cases have passed, across the suite and retry. The final alpha SDK
run passes 25 tests, including a pending-witness checkpoint and matching continuation. Two
driver incidence controls pass. The recorded logs preserve the initial failed run as well as
the retry; it is not reported as a clean single-run suite.

[established-bounded; implemented-exact; measured] The public-owner study
`alpha_actuation_restart.rs` saves the actual model after three generated occurrences, at cut
5,129, then compares uninterrupted and fresh-process continuation to cut 5,133. The remaining
bytes, readings, termination and final 2,347,870,790-byte checkpoints agree exactly. This split
falls within a UTF-8 codepoint; it preserves the native byte sequence and source history. The
split-plus-uninterrupted process takes 103.118 seconds and the resumed process takes 51.250
seconds, including checkpoint serialization, with peak RSS 4,998,832 and 5,043,012 KiB respectively.
The receipt retains both commands and exact file comparison. These are post-generation
checkpoints, separate from the cultivation driver's pre-prompt checkpoint boundary.

## Continuing construction

[definition] AC1 steps 3–4 still require local path formation and economical generator/decoder
reuse across the actual boundary, material and changing-contact family. Use the mathematical
factorization criterion to reduce repeated representation while keeping newly observable
remainder contributions. The journal's current factors, internal-delta carriers and producing
maps are the concrete next representation owners; a fixed boundary quotient alone is inadequate.
AC2 still requires useful full responses and contextual continuation. AC3 artifacts remain
experimental; AC4–AC5 remain open. The mathematical review does not replace these product returns
with a formal theorem or prescribe another larger run of the unchanged failed model.

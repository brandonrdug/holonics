# AC1: shared source standing and a differential text-current receiver

**Authority:** the [Athena-alpha blueprint](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md).
**Status:** native/source/codec checks and the first text return are recorded; the response fails
its request and Athena-alpha remains unattained.

## Source standing and actual contact

[definition] `NativeFieldEmission` remains a linear capability. `retain_source` derives a
`NativeFieldSourceAnchor` from an actual available emission; it shares that immutable historical
section and producing frame. A new `through_anchor` reception has its own occurrence, contact,
internal-current birth and complete returned difference. It never clones, restores or branches the
continuing ecology. Direct reception still consumes its original linear capability. Anchored
reception does not consume that separate capability.

[definition] The source map remains the existing root raw two-port field. Each actual receiving
occurrence contributes its own `d d*` and a new zero-initial internal current before reaction.
Repeated references to one source therefore retain distinct members of the contact population.
The source's original bytes and frame remain immutable; each member's birth prefix belongs to its
receiving occurrence. `NativeFieldLineage::source_contact` distinguishes direct emission contact
from retained standing. An address alone cannot manufacture an anchor.

[definition] On a validated exposure occurrence, `shared_prior_parent` exposes one prior family only when all
captured views agree and every participating link is available. Duplicated testimony for the same
family does not add another contact. Multiple distinct parents, unavailable links and differing
views remain open. The text driver additionally requires that this parent actually entered the
current native exposure. Provider/family coordinates locate the actual anchor in the exterior
application; they become neither current amplitudes nor native semantic identity.

[open] This is one contextual source port at a declared record endpoint. It does not make the
endpoint field a reconstruction of the whole record, or prove that different full parent histories
with equal endpoint fields are discriminated by the contact. Complete histories stay retained.
Wider simultaneous contextual incidence and productive use of those separating histories remain
construction obligations.

## Declared text chart and receiver

[definition] The exterior chart has 257 symbols: octets 0–255 and `EndPart` at codeword 256.
Nine bit positions each have two input ports; exactly one unit pulse enters each pair. Every
symbol therefore has nine equal unit pulses across 18 complex input ports. Codewords 257–511
are outside this chart. `EndPart` records an actual inscription-part boundary, not a measurement
of thought or general semantic completion. No text, role, provider or topic enters a seed coefficient.

[definition] The same native paired junction now has `D=6N=108` real coordinates for this exterior
18-port chart. Its first `2N=36` complex outgoing coordinates are source branches; its final 18
complex coordinates meet the text receiver. For an outgoing current `b` with numerical center
`b_hat` and Euclidean radius `E`, bit receiver `k` has

```text
m_k = Re(b_hat[36+2k+1] - b_hat[36+2k]).
```

[proved-derived] This real linear functional has Euclidean norm `sqrt(2)`, so its error is at most
`sqrt(2) E`. Its sign is certain if `m_k² > 2E²`. The native receiver computes that strict inequality
with full unsigned-256 products; it does not narrow a signed-wide difference or use floating
point. A complete current ball crossing the zero plane returns an unresolved bit. An exact zero
is separately reported only when both radius and differential are zero. Other imaginary or source
coordinates remain in the current; this receiver does not declare them equal.

[definition] `read_differential_pairs` is a terminal observer over an immutable native junction
report. It returns positive, negative, unresolved and exact-zero masks. It changes no native
current, contact, morphology, history or source availability. Full oriented residuals remain with
the original current. The exterior `read_text_symbol` presents a symbol only if every required
sign is certain and its codeword belongs to the declared chart. It supplies no grammar, candidate
sentence, stochastic sample or inherited response.

[definition] `TextFieldSession` borrows one native field. Input and every self-emitted symbol use
that same owner's ordinary `advance_resident`. A failed reception retains its exact pending symbol
and source; later calls cannot overwrite it. The generation return distinguishes part completion,
open current, reserved codeword, interruption and native refusal. A work limit means interruption.
UTF-8 presentation is checked separately; invalid bytes are retained with their codec error.
These in-process interfaces and JSON observations are not a durable cultivated-model checkpoint.

## Actual text return and verification

[established-bounded; measured] All 30 field CUDA tests and 13 alpha data/codec tests pass.
The native controls include strict differential-ball signs, exact real-current ties with unequal
imaginary carriers, full signed-wide gap arithmetic, and continued operation after terminal
readout. Three distinct receptions of one retained source survive a rechart, preserve the original
source bytes and reconstruct their separate internal currents. Foreign anchors are refused.
The exterior tests preserve parent-view disagreement, unavailable relations and the complete
257-symbol reversible chart. The text example builds; unrelated existing dead-code warnings remain.

[established-bounded; measured] `alpha_text` entered four actual development families as 728
octets and four part-end symbols: **732 native operations**, with all three recorded replies
joined to the request's retained source at native occurrence 74. Development had zero numerical
section readouts. The fresh request, declared before inspecting output, was:

> Explain the difference between a function and its derivative.

[counterexample; measured] The composition emitted **64 copies of `d`**, then returned
`interrupted` at the declared 64-symbol work boundary. It produced neither an answer to the
request nor a part-completion marker. All bytes were valid UTF-8. No fallback, completion by
timeout, or successful-alpha interpretation was supplied. This is a failure of this concrete
initial composition on this request, not a retraction of native learning or the whole HNA line.

[established-bounded; computational-witness] The private first return is
`.local/artifacts/athena-alpha/ac2/first-text-return-v1.json`. A second run added cold capture of
the emission carriers to answer whether the repeated surface came from a frozen current. Its
complete body and generation return are identical. The [targeted comparison](../experiments/alpha_passive_junction/inspect_text_return.py)
checks actual source bytes and part ends, three shared-parent contacts, all 64 current-to-bit
readings and actual next-occurrence self-returns, and the final complete moment against all
856 contacts in the 858-operation run. No host update is supplied to the model by this observer.

[established-bounded; measured] All **63 consecutive outgoing-current pairs have disjoint
certified balls**, despite producing the same byte. The native current therefore continues to
change behind the repeated surface. This does not identify the cause as the codec alone, prove
a stationary state, or establish a dynamic condensation. It preserves a concrete distinction
for repairing the current-to-material composition.

[established-bounded; measured] The first complete debug process took 34.731 seconds, including
24.350 seconds for development, 4.170 for prompt and self-emission, and 5.365 for cold diagnostics;
maximum process RSS was 210,732 KiB. Before diagnostics, native section egress was 4,096 bytes
for the 64 four-mask receivers; all full numerical emission reports stayed resident. The second
run took 34.760 seconds and 221,204 KiB maximum RSS with its additional cold current capture.
These are initial diagnostic-driver measurements, not useful-response throughput or a consumer
model release. The final receipts are `first-text-return-v2.json` and
`first-text-return-comparison-v2.json`; the first return is preserved.

[open] Useful English/code/mathematics responses, productive historical/contextual organization,
broader cultivation, durable native field/codec rest and the full AC0–AC5 product remain open.
The next construction must improve the actual conduct that reaches this receiver and inspect its
responses; nonempty output, repeated bytes and changing hidden currents cannot close the goal.

## Continuous source feedback: the bound that did not continue

[definition] [`contextual_source.py`](../experiments/alpha_passive_junction/contextual_source.py)
is a cold experiment for a different contact map: `d=(P_source out_previous,-arrival)`, using the
retained coupled outgoing source projection. The live engine still uses its admitted raw-source
contact map. This experiment changes the proposed constitutive attachment; it is not merely a
new representation of the current engine and is not a production learner.

[proved-derived] Let a dyadic contact center `d_hat` have Euclidean error at most `e_d`.
The operator error of its Hermitian moment is bounded by
`2 ||d_hat||₂ e_d + e_d²`, from the complete expansion of the two outer products.
A scalar operator enclosure for accumulated `C`, plus the numerical moment-rounding residual,
therefore gives a sufficient bound for `(C-C_hat)v_hat`. This is valid but discards the shared
history between moment and internal-current errors.

[established-bounded; computational-witness] On the original full 96-real-coordinate material
chart, all four declared exact anchors pass current and moment containment. At 24 fractional bits
the candidate accepts seven occurrences before its proposed wide radius report refuses; at 72
bits it accepts eleven. Its outgoing ball first contains zero at occurrences 3 and 8 respectively.
The numerical solve residual stays small compared with the accumulating operator-error bound.
The private receipt is `contextual-source-enclosure-v1.json`. No native model or update was
executed by this experiment, and no exact-point comparison is claimed after the four anchors.

[open] These results expose the insufficiency of this scalar error representation for continuing
feedback. They neither refute the contextual source map nor establish instability of its exact
current. A continuing realization needs the actual oriented and correlated error transport;
sealing the numerical center as the source is not a repair. The discrete exterior text receiver
above has its separate, explicitly declared domain and does not discharge this obligation.

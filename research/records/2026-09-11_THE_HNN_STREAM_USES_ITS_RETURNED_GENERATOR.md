# The HNN stream uses its returned generator

[definition] The native owned generator is now bound into the continuing HNN session and
stream protocol. One observed-symbol return can publish it and subsequent generation, source
input, observation, prediction capture and re-entry use that returned body. Further material
returns after entering the dependent representation remain open; the active goal continues.

## Receiver coordinates are decoded natively

[definition] `NormalWaveFamily::receiver_coordinates` uses the existing declared family
receiver to obtain its source face. The device constructs the graph `(Dᵀθ,θ)` from the
family's actual direction rows and queries it at the receiver face minus the affine origin.
The existing echelon/query operations return an executable coordinate packet and the full
coordinate preimage fibre. No host inversion, hand-written θ or inferred unique cause is used.

[proved-derived; formal-checked] The extended `DependentConstitutiveReturn.lean` proves that
two affine decoder coordinates yield the same source face exactly when their difference lies
in the decoder's kernel. Every subsequent function of that face agrees on these aliases.
This licenses a coordinate representative for a **receiver face**, while the original source
and generator remain complete. It does not identify a latent cause or establish equivalence
between the new source-section receiver and the former whole-family projection.

[established-bounded; implemented-exact; computational-witness] The native coordinate test
recovers the original receiver of a genuinely free source family, retains its coefficient
kernel and returns condition `(6/5,−2/5)` in the declared control. Moving the representative
along each retained coordinate-kernel direction leaves the complete subsequent family rest
unchanged. Construction performs zero numerical section readouts before inspection.

## Ordinary operations continue on the generator

[definition] `ResidentCoupledConstitutive` now owns an ordered programme containing ordinary
member advances, actual next-current observations and integer/rational source fields, in
addition to its original source-current operation. A whole source field retains every adjacent
pair and advances one public clock. These are lifts of existing wave/source/observation owners;
they do not implement another learner.

[definition] A retained prediction records its actual programme prefix, member and receiver.
Its entire generating source remains a dependent family. Inspection constructs an affine
comparison **section** at the declared root receiver and keeps the root comparison/coordinates
alongside it. The nested source parameters must not be mistaken for independent samples or
for a global affine comparison. A release removes the live pending disposition while retaining
the underlying actual operators. Dependent rest v2 stores the programme and pending/released
dispositions; the prior source-only v1 decoder remains available.

## The existing HNN coordinator owns both representations

[definition] `NativeCoupledBody` routes the existing coordinator to either its affine wave or
its owned dependent generator. It transfers the one body recoverably. Comparison and receiver
preparation finish before the owner is taken; constructor failure restores the original body.
The private frozen substrate of a dependent generator is never exposed as its continuing wave.

[definition] `NativeCoupledWaveSession::incorporate_symbol`, reached by the existing stream
`observe-symbol` command, mounts the actual observation, forms its producing comparison,
decodes the producing receiver's coordinates natively and publishes the full generator. It
consumes that comparison once and reports the new representation and receiver scope.
`compare-symbol` remains an inspection operation. Emission ordinals, selection/re-entry cursors,
source-part boundaries and stream-delivery state remain owned by the same session.

[definition] Session frame v3 tags its native body representation and keeps the existing
header/transport contract. Both legacy affine session prefixes still load. Metadata getters
read declared dimensions/counts directly; they do not detach material sections in the hot path.
Readonly operations that evaluate a dependent generator take mutable access for the existing
device completion/poisoning contract, without publishing another observation.

## An inspected source-visible HNN result

[established-bounded; implemented-exact; computational-witness] A two-symbol mathematical
control uses the native conditional operator `η=(swap(c)−c)h`. Its initial h=1 exchanges the
two source components. The HNN session predicts `a` from source `b`; receiving actual `b`
through `observe-symbol` changes the native condition to the identity case. Before the next
comparison, both the changed session and its unchanged control are brought to the same complete
`(p,c)=(a,b)` source. The returned session emits **`b`**; the control emits **`a`**.

[definition] This is a scoped exact-operator assay, not general conversation comprehension.
The driver mounts inputs and reads the actual session. Native condition/preimage/formation
and existing basis emission produce the result; no fixture-local learner or answer template
is supplied. The broad conversation observations remain separate.

[established-bounded; implemented-exact; computational-witness] The same test continues
generation and self-re-entry, captures and inspects a later dependent prediction, checkpoints
the session, and resumes it in a fresh process. The child and parent produce identical next
emission results and complete saved native bodies. Invalid initial IDs and duplicate consumed
IDs preserve standing. Incorporation has zero numerical section readouts before its returned
protocol metadata. Legacy checkpoint controls also pass.

## Verification and next construction

[established-bounded; process-audit] The 33 coupled-wave controls, strengthened coordinate-kernel
control, seven HNN session controls and seven stream transport controls passed. The coordinate
kernel formal build passed. The [native/session logs and formal terminal excerpt](2026-09-11_hnn_return_receipts/)
retain the returned verification evidence. Broad cultivation did not run.

[open] `NativeCoupledBody::incorporate` still refuses an additional material return after
entering the dependent representation. Later pending predictions are preserved and inspectable,
but their further incorporation needs the joint lift of their own source parameters with the
existing root programme. Remove that boundary by extending the same native programme, not by
using the default receiver's affine section as if it were the entire source family.

[definition] The concrete next operands are the captured source-frame generator at a prediction
prefix, the actual returned observation, its retained original history and the contemporary
conditional material/current. Keep the existing root parameter and any new source-fibre
parameters shared through the update and subsequent passage. Delayed returns must not rewrite
historical material or reinterpret earlier receiver coordinates in an unrelated frame. Existing
native relation-image/preimage and coordinate-decoder owners are the relevant ports for this
composition. This is the remaining integrated goal work, not a new universal theorem prerequisite.

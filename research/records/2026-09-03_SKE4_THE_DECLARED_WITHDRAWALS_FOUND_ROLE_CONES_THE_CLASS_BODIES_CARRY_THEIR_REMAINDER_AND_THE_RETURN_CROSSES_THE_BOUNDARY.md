# SKE4: the declared withdrawals found role cones, the class bodies carry their remainder, and the return crosses the boundary

**Date:** 2026-09-03
**Owner:** `crates/holonic-engine/src/holonic_intelligence/operative_roles.rs` (the declared
withdrawals at a declared grain, the orders, the selections),
`operative_condensation.rs` (the class ecologies, the remainder, the boundary input and the
rest), the session's unsealed terminal (`with_terminal_remainder`); the deed
`applications/athena-alpha/examples/condensation_seal_ske4.rs`.
**Receipts:** `2026-09-03_SKE4_receipts/` (the session refit's receipts stay there too):
`prefix_declaration_at_637_roles/` (withdrawn testimony) and `role_cones/` (the deed).
**Campaign:** SKE4 under
[`THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md`](../../blueprint/THE_SOULKILLER_EXCITES_THE_RESIDENT_REALIZATION_AND_RETURNS_THE_RECEIVER_FAMILY_QUOTIENT_AS_CONE_RESTRICTED_ECOLOGIES.md),
on the segment session of
[`the SKE4 session record`](2026-09-03_SKE4_THE_SESSION_RUNS_AS_SEGMENTS_UNDER_THE_2026-08-18_CONTRACT_AND_THE_FACE_ALONE_CROSSES_TO_THE_HOST.md).

## 1. The declaration

[definition] The family is SKE3's: five occurrences (`7 + 5 =`, `9 + 3 =`, `7 + 6 =`,
`3 + 4 =`, `8 + 8 =`) through the application's turn markers, exposed to three declared
histories (the empty word, `The second operand is `, `The sum is `), the receiver the selected
face at the terminal position. The withdrawals the family declares are roles derived from the
ecology's own structure and nothing outside it: at the layer-block grain, every block of
contractions that flow into one another through non-contraction operations within one layer
without crossing a residual sum is one role (an attention block, a gated block, the per-layer
projection block, the prologue's projection), keys and values another layer reads belonging to
the layer that computes them; 127 roles over 1,333,248 output sites. The tied population is the
receiver's own and is not a role. Withdrawing a role zeroes its contractions' outputs at every
position (the SKE2 intervention, recorded inside the passage). Two finer grains exist in the
same owner (every contraction population; the query population per head, 637 roles) and were
exercised once (§3) and not declared for the deed.

[definition] A cone is founded by the declared withdrawals alone (`IsConeUnder`,
`FoundedCone`): for each exposure, the roles whose single withdrawal changes the face are its
load-bearing roles; a class's cone begins as the union over its exposures. Then the joint
withdrawal of that union's complement is itself declared and read under every exposure of the
class at once; when it changes a member's face, roles are restored along the class's excitation
order (chunks of an eighth of the complement, then one at a time from the last chunk's end) until
every member face stands, and the restored roles extend the cone. The family's cone, the extent,
is the union of the class cones together with what the same joint reading over all fifteen
exposures restores. Every cone is then read once more under every class lens and the family
lens. The union of cones founded on single withdrawals is not assumed to be a cone under the
joint withdrawal; it is tested, and the test is the testimony.

[definition] The deed runs from one command (`condensation_seal_ske4 deed <root> <dir> [stage]
[rest]`): the host steps (orders, founding, the dismantle, the insufficiency lane) in the parent
and every card step as a child process on a fresh device context, each with a guard that aborts
it when no card step completes for 180 s and reports where. The exact carrier's allocator
fragments over a few hundred thousand section allocations, which is why a step and not the whole
deed is the process; the whole deed on the card took about an hour, the longest child (the
family's joint extension, 15 faces per reading) 656 s. The rest is written where the fourth
argument says: it is the size of the retained coefficients and belongs on a disk, not on the
memory-backed temporary file system, which the first run filled.

## 2. The apparatus's own boundary, met on the way

[counterexample; measured] Under some withdrawals the exact carrier cannot hold the operator:
the product law (`section_hadamard`, admitted under `a + b` octaves) refused with
`REFUSED_CARRIER` inside the first layer's gated block whenever the values entering the product
exceeded what an `i64` word at the lookup's grain `2^-47` holds (about `2^16` above one, so a
product of two activations near `2^9` each overflows), and the segment reported the refusal
upstream of every consumer. The withdrawal that drove the operator there returns no face; it is
outside what the apparatus admits and founds nothing, and the deed records it as a refused
reading rather than a change or an unchanged face. This is the exact carrier's range, not a
law's error: a session grain declared coarser than the lookup's would trade the lookup's
exactness for range, and that choice is not made here.

[counterexample; measured] A counterfactual run inside the session must not overwrite the
session's checkpoints with withdrawn carriers; the first segment session did, and the second
and later probes of a process read a contaminated state. `enact_run` now takes whether the run
checkpoints, and the face under withdrawal runs without checkpointing.

## 3. The prefix declaration at 637 roles is withdrawn

[counterexample; measured] The first founding tried the SKE2 shape at the role grain: roles
ordered by their greatest share of the excitation over the exposures, and the declared
withdrawals the complements of prefixes of that order, bisected on a grid of 17 prefixes per
exposure. Over 30 exposure runs (510 readings, `prefix_declaration_at_637_roles/`), 102 readings
were refused by the apparatus and 16 runs showed 39 inversions: a prefix whose complement left
the face unchanged followed by a larger prefix (fewer roles withdrawn) whose complement changed
it. Under nested prefixes the founded cone of `FoundedCone` is the union of every changing
complement, which is every role, since withdrawing everything always changes the face: the
declaration founds nothing. The readings themselves stand: at the role grain the face under a
joint withdrawal is not monotone in the withdrawn set along the excitation, which is the SKE2
audit's finding (a union of cones verified by one complement is not a cone) in its own
declaration.

## 4. The deed at the layer-block grain

[established-bounded; implemented-exact; measured] Fifteen exposures: every face reproduces
SKE3's and the quotient is the same five singleton classes under the three histories. The
propagated remainder at the receiver, read once with the face unsealed: under every exposure all
262,144 coordinates of the face are enclosures, the widest 150 grains at `2^-47` (for `7 + 5 =`
under the empty history: 97 coordinates at 30 grains, 68,201 at 60, 158,801 at 90, 34,266 at
120, 779 at 150), deposited per coordinate beside each exposure receipt (`remainder_*.bin`).

[established-bounded; measured] The declared withdrawals, one role at a time (127 per exposure,
none refused), roles whose single withdrawal changes the face:

| occurrence | empty history | `The second operand is ` | `The sum is ` |
|---|---|---|---|
| `7 + 5 =` | 31 | 9 | 3 |
| `9 + 3 =` | 30 | 6 | 3 |
| `7 + 6 =` | 56 | 10 | 3 |
| `3 + 4 =` | 27 | 9 | 4 |
| `8 + 8 =` | 39 | 2 | 3 |

The unions per class (32, 33, 59, 31, 42 roles) and over the family (63) were each declared as
one joint withdrawal of the complement, and every one changed every face it was read under: at
this grain the union of single-withdrawal cones is not a cone under the joint withdrawal, the
SKE2 audit's finding again. The joint extension restored 77, 82, 51, 75, and 57 roles for the
classes (9 to 18 readings each, every reading three faces) and 52 for the family (13 readings of
15 faces), after which every member face stood. The founded cones, verified once more under
every lens:

| class | occurrence | cone roles of 127 | cone sites of 1,333,248 | complement leaves every member face | cone withdrawn changes every member face |
|---|---|---|---|---|---|
| 0 | `7 + 5 =` | 109 | 1,282,560 | yes | yes |
| 1 | `9 + 3 =` | 115 | 1,299,456 | yes | yes |
| 2 | `7 + 6 =` | 110 | 1,285,376 | yes | yes |
| 3 | `3 + 4 =` | 106 | 1,271,296 | yes | yes |
| 4 | `8 + 8 =` | 99 | 1,243,136 | yes | yes |
| family | all | 117 | 1,305,088 | yes, all 15 exposures | |

The insufficiency is the 10 roles outside the family cone, 28,160 sites. A first founding whose
class extensions were read one exposure at a time and unioned left class 4's union changing two
of its own three faces (a smaller withdrawal than any of the three that stood alone changed the
face); the extension founded jointly over the class's exposures, as the blueprint says, closed
it, and that first reading is kept in `deed.log`.

[established-bounded; measured] The remainder of each class body against the full operator over
the 15 declared exposures: the class lens is the full operator with the class cone's complement
withdrawn, read under every occurrence of the family and every declared history, beside the
full operator's face; collapsed pairs are two occurrences the lens identifies at an exposure
where the full operator separates them, each with the shortest declared history that separates
them under the full operator.

| class | collapsed pairs | separating word | species |
|---|---|---|---|
| 0 | 1: `9 + 3 =` with `8 + 8 =` under the empty history (lens `$` for both; full `9`, `8`) | the empty history | compression |
| 1 | 0 | | condensation |
| 2 | 0 | | condensation |
| 3 | 5, all under the empty history, all at lens face `$` | the empty history | compression |
| 4 | 5: two under the empty history (lens `The`, and `2094`), one under `The second operand is ` separated by that word, two more under the second and third words separated by the empty history | as listed | compression |

Two class bodies collapse nothing and reproduce every member face; their remainder is the
propagated enclosure alone, exhibited, and they state condensation. Three collapse pairs of the
family's occurrences the full operator separates, and state compression with those pairs as the
remainder. No number stands for any of it.

[established-bounded; implemented-exact; measured] The return crossed `soulkiller::dismantle`
with the resident realization as the input; the productive lane holds the operator graph, five
class ecologies (signature, fibre, cone, response, remainder), and the cross-sections restricted
to the extent, with the receiver's own population (the tied one, read by the lookup and by the
terminal contraction) carried whole; the cold lane the source ordinals and the excitation and
intervention testimony; the insufficiency lane the complement and the declared boundary. The
productive lane validated; its rest is 9,383,686,281 octets against 15,036,138,068 of
coefficients (1,305,088 rows retained as sites, 283,986 whole, 21 addressed), digested in
`rest.sha256`. Mounted from the rest alone, every class body returned its response at every
declared exposure of its own occurrences (15 of 15 member faces), and refused at admission every
exposure of an occurrence outside its fibre (60 of 60), as it refuses an occurrence outside the
family (`11 + 2 =`, cause `OccurrenceOutsideFamily`) and an undeclared history
(`The difference is `, cause `HistoryUndeclared`) while admitting a member under a declared
history to its class.

[counterexample; source-inspected] A first rest restricted the tied population to the rows the
family addressed, so a class body could only select among the tokens it had been addressed
with; the receiver's population is now carried whole. And a first body check drove non-member
occurrences through a class body and read faces that disagreed with the lens: the class body
retains the rows its own occurrences addressed and no others, so a non-member is outside its
declared boundary and is refused, not driven; the remainder's lens faces are the in-session
lens's, and the body check is of members.

[measured] Process bounds: every card child completed under its guard except one first reading
of class 3's joint extension, aborted at 180 s by a guard that had been armed without marks in
the singly-run steps (corrected: every card step marks); the child steps ran 91 to 182 s for the
class extensions, 656 s for the family's, 104 to 132 s per exposure, 16 to 26 s per
verification, and the whole deed from one command about an hour of card time.

## 5. What this does not grade

[definition] The class ecology is a lens and shortcut graded as a lift; it is not the
compression, whose law applies to the composed variant in SKE5. The remainder is exhibited: the
collapsed pairs with their separating words and the propagated enclosure at the face, never a
number standing for either. Nothing here grades a capability of the operator.

# The tower conducts layer by layer, and the certified enclosure diverges at 2^35 per layer

**Date:** 2026-08-19
**Kind:** construction return for Station C of the complete Phoenix sequence — the complete
contextual Gemma text pathway — with the one genuine obstruction the material returned, the
declared chart that carries the tower past it, the driver, its artifacts, every falsifier with its
predicate, and what it does not claim.
**Truth status:** `established-bounded` for every measurement (each carries its command or its
artifact line); `implemented-exact` for the `Standing`/`section_carry` seam, the `MidpointQuotient`
law, the width census and the tower founder; **Station C PASSES on this tree**; active stimulation
of the text tower is the only Phoenix station this promotes; no master station after it is
promoted.
**Authority:** the directive of 2026-08-19 (Station C); the Gemma instance blueprint §6; the
Phoenix master's active-stimulation station; [Station A](2026-08-19_THE_REFUSAL_TRAVELS_ALONG_THE_LINEAGE_THE_ADMISSION_IS_TYPED_AND_THE_RATCHET_IS_DISPOSITIONED.md);
[Station B](2026-08-19_THE_SOURCE_IS_ADMITTED_WHOLE_AND_THE_POTENTIAL_ATLAS_COVERS_THE_TOWER.md).
**Position boundary:** [`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) does not move.
**Driver:** `crates/holonic-engine/examples/the_tower_conducts_layer_by_layer_and_the_future_section_is_plural.rs`
(sites `phoenix/resident_layer.rs`, `phoenix/tower.rs`; exterior apparatus
`phoenix/source_runtime_tower.py`).
**Artifacts:** `output/the_tower_conducts/tower-5-inputs-grain-48-terms-14.form` and, per input,
`output/the_tower_conducts/source-face-<tokens>.tsv`; `output/the_tower_conducts/vision-soft-tokens.tsv`.

---

## 0. Verdict

**The whole Gemma-4-E4B text tower conducts on the card for five runtime-supplied inputs** — 42
layer deeds and the final deed, each one CUDA graph launched once, typed admission, footprint
certificates, the a-priori law held, the obstruction lineage empty — returning the illicial
potential section over the 262,144-token vocabulary, the plural future section at the last position
and its decoded surface: *"The capital of France is"* → **" France"**; *"La capitale de la France
est"* → **" la"**; *"2 + 2 ="* → **" "**; *"two plus two equals"* → **" two"**; *"Paris is the
capital of"* → **" Paris"** — in every case the source runtime's own top candidate. Fourteen
falsifiers pass; four later stations stay open by name. One tower is 18–25 s on the card.

**And the material returned one genuine obstruction, which is the most important thing in this
record.** The certified interval enclosure of the *composed* tower diverges: at grain `2^-48` layer
0's widest certified enclosure is `1.904e-4` against the entering grain `3.553e-15` — an
amplification of **≈ 2^35.6 per layer**, measured — and at grain `2^-24` the same amplification
lands layer 0's enclosure at width 715 in value; inside layer 1 the word refuses by name
(`CarrierLeft` at the gated-passage Hadamard, the lineage carrying it to every successor). This is
the dependency problem of interval arithmetic through 2,560-term contractions: the enclosure's
worst case multiplies by the map's absolute row mass at every contraction, seven times a layer,
whatever the grain. **No grain and no word carries a certified enclosure of the exact function
through two layers.** That is not a difficulty; it is the composition of certified enclosures
refusing at the declared aperture, and the driver returns it as a typed refusal with its
measurement rather than narrating it.

What carries the tower is a **declared quotient chart**: after every occurrence whose enclosure can
widen, a `MidpointQuotient` occurrence — quotient species, entailed by its declaration, visible in
the diagram — collapses the enclosure to its midpoint for the successors, while the enclosure stays
resident in the predecessor's own section as the complete per-entry residual and its collapsed
population (widest, summed and nonzero widths) is censused on the card. Each occurrence's enclosure
is then certified relative to its midpoint predecessors; **the composition is not certified and the
receipt says so.** Under this chart the whole tower's collapsed population is 24,306,337 grains of
`2^-48` over 1,296 quotients for the first input — the widest single enclosure 1,138 grains
(`4e-12`) — so the chart's own rounding per step is at the grain, and the divergence above was
entirely the certified composition, not the arithmetic.

## 1. What the deed is

```text
  the source tokenizer (exterior, declared USED)        "The capital of France is" → [818, 5279, 529, 7001, 563]
  → layer 0: x0 (embed rows × √2560 as bf16), PLE predecessor, input rebase, q/k/v, head rebases,
    chronology (θ=1e4 on 256), sliding contact (512), o, post-attention rebase, residual,
    pre-feedforward rebase, gate/up, GELU, Hadamard, down, post-feedforward rebase, residual,
    per-layer gate, GELU, Hadamard with the PLE section, per-layer projection, post-PLE rebase,
    residual, layer scalar — 68 operations, 45 fronts, ONE graph (137 nodes, 144 edges)
  → layers 1–41: the residual stream released by the previous passage enters as a `Standing`
    (section_carry on the card; nothing crosses the apparatus boundary); layers 5/11/17/23/29/35/41
    at head 512 with the proportional chronology (θ=1e6, 64 of 256 pairs rotated, 192 identity);
    layers 22 and 23 release their post-chronology K and post-rebase V standings; layers 24–41 read
    them as standings and build no K/V (61 operations, 123 nodes)
  → the final rebase → the tied output boundary: the potential section, 5 × 262,144
  → the plural future section at the last position (the tokens the enclosure does not separate
    from the top) and the native decode of tokenizer.json's vocabulary
```

Every layer's stored populations are predicted from the header, admitted, mounted, used by one
graph and released before the next layer mounts: the widest charge any one deed makes on the card is
5,408,555,008 octets (the final deed: the tied table aligned) against the source's 15,992,595,884 —
the method is streamable and transfers to a source that cannot reside on this card.

## 2. The falsifiers

| # | falsifier | result |
|---|---|---|
| 1 | every input crossed all 42 layers and the boundary; each layer one graph launched once; typed admission; every front certified; a-priori ≥ measured; lineage empty; material reconciled | PASS — 5 inputs, 43 deed launches each |
| 2 | KV-sharing realization, both species, both head widths, both RoPE species enacted | PASS — roles `{Own, OwnAndStore, Shared}`, species `{Sliding, Full}`, 61 operations per shared layer against 68/69 |
| 3 | standings cross on the card: section egress = the declared reads exactly | PASS — 148,889,600 = 148,889,600 octets |
| 4 | streamable by layer: the widest charge of one deed far below the source | PASS — 5.41 GB against 16.0 GB; per-layer material 0.73–0.85 GB |
| 5 | several runtime inputs: distinct standings and futures, identical laws | PASS — 5 distinct final standings; per-layer operation and front counts identical |
| 6 | the plural future section and the native decoded surface | PASS — 1 candidate not separated in each (the quotient chart's enclosures are at the grain), 262,143 separated |
| 7 | the tower's exact work is the serial composition; the apparatus census beside it | PASS — additions 23,363,161,920 · multiplications 23,458,347,840 · dependency span 1,894 · captured launches 5,514 · 43 launches · 43 synchronizations |
| 8 | hidden card → typed refusal, exit 3, no CPU answer | PASS |
| 9 | the source runtime's bf16 face compared per layer and at the boundary | PASS — §3 |
| 10 | same-output/different-law and cross-codec readings exhibited | PASS — every pair's final standings differ; no pair shares its top candidate (the French and English codecs of the capital relation return " la" and " France"; the two codecs of 2+2 return " " and " two") |
| 11 | **the certified interval enclosure diverges: the word refuses by name inside the tower; the amplification is measured** | PASS — 1 layer carried; `CarrierLeft` at the gated-passage Hadamard; ≈ 2^35.6 per layer |
| 12 | under the declared quotient chart every layer's collapsed population is censused and each quotient is entailed by its declaration | PASS — 1,296 quotients; Σ widths 24,306,337 grains; widest 326 / 537 / 1,138 at layers 0 / 21 / 41 |
| 13 | **one real non-text projection crosses its typed ports on the card** | PASS — 256 × 768 soft tokens from the exterior vision tower; the gain-less rebase and the `embed_vision` projection as one graph at a grain derived from the material (`2^-38`); the native chart lands on the source's bf16 codeword at 393,190 of 655,360 coordinates |
| 14 | every text population carries a coverage status; excited rows counted against the unexcited | PASS — 719 text populations: 665 stimulated, 54 unbound, 0 not crossed; 20 of 262,144 vocabulary rows excited, 262,124 unexcited (measured, not condensed) |
| 15–18 | native rest / runtime / inference; condensation; cultivation; reborn dissection | OPEN by name |

## 3. The source/runtime comparison, as the artifact carries it

The source's own bf16 forward (the exterior `source_runtime_tower.py`, 22–26 s per input,
bf16 because an f32 whole-tower face does not fit the machine's memory) is compared at the last
position after every layer and at the boundary. Read plainly:

- at layer 0 the native midpoint chart at `2^-48` lands on the same bf16 codeword as the source at
  **772 / 2,560** coordinates; at layer 10, 393; at layer 20, 209; at layer 30, 59; at layer 41,
  **0 / 2,560**, with the worst absolute gap growing from 0.05 to 72 — the source's bf16 realization
  rounds every intermediate to eight significant bits and the two charts drift apart in the residual
  stream over 42 layers;
- and yet at the boundary the **order face agrees on every input**: the source's top candidate is
  the native plural future section's one candidate, five of five.

That is the phase-object theorem of `CLAUDE.md` §0j arriving as a measurement: the magnitude face
separates the two charts completely by layer 41 while the order face at the boundary does not. The
defect is the source's rounding fibre and it is carried per layer, never dismissed; the native chart
does not imitate it and agreement is not the grade.

## 4. What was built

- `crates/holonic-engine/kernels/exact_resident_section.cu`: `section_carry`; the census slot
  widened to 16 words with the summed and nonzero widths (`atomicAdd`), so a collapsed population is
  measured where it is collapsed.
- `crates/holonic-engine/src/resident_section.rs`: `shape_carry`, `record_carry`, `SlotReading::
  {width_sum, nonzero_widths}`.
- `crates/holonic-engine/src/resident_law.rs`: `Standing` (a released resident section entering a
  later passage; `Rc` because one stored K/V standing enters eighteen passages and none writes it),
  `MidpointQuotient` (quotient species; entailed by the declaration), `ResidentMaterial::standings`
  and `standings_octets`, the `Enter` entailment for an unscaled exterior standing.
- `crates/holonic-engine/src/front_passage.rs`: `CompiledPassage::release_section` (the releasing
  passage may not relaunch), the `carried-standing-octets` coordinate, the uncapped a-priori peak
  and the count of occurrences admitted at the word exhibited in the admission.
- `crates/holonic-engine/examples/phoenix/tower.rs`: `Chart::{Interval, Midpoint}`, `seal`,
  `found_layer` with the chart, `found_final`, `mount_layer`, `mount_final`, `enter`.
- `crates/holonic-engine/examples/phoenix/source_runtime_tower.py`: `encode`, `decode`, `face`
  (whole-tower bf16 face), `vision` (the PIL image processor and the vision tower on a deterministic
  synthetic image — the torchvision-free backend).

## 5. What this station does not claim

No Phoenix master station after active stimulation. No multi-token prediction (one future section is
emitted; Gemma exposes no MTP head). No certified enclosure of the composed function: the composition
diverges and the chart is declared, named and censused. No claim that the native chart equals the
source's bf16 chart — the defect is carried. No image recognition: the vision tower is exterior; the
projection is the bounded portability control. No native rest, condensation, cultivation or
inference readiness.

## 6. The validation epoch and the release reading

`cargo test -p holonic-engine --lib -- front_passage resident_law resident_section` (28 tests, on
the card); `cargo check -p holonic-engine --examples`; release builds of the two drivers (1 m 04–17 s,
nine times); the tower driver run seven times while the chart was found (the first two returning
the typed refusal at layer 1 that §0 reports), 0.3–5 min each; the Station A driver rebuilt and
rerun once at grain 24 for the divergence measurement. The ownership ledger row
of `resident_law.rs` was changed explicitly for the two new laws (`BTreeMap 35→37`, `Vec 40→43`:
the `Standing` and `MidpointQuotient` signatures and their returned populations).

**The one complete gate**, `bash tools/gates.sh`, 2026-08-19 18:06:43 → 18:10:48 UTC, exit 0,
4 m 05 s, after the ledgers were regenerated once:

```text
PASS  tests              2517 passed, 0 failed, 19 ignored over 29 result lines; example targets type-checked
PASS  authored-levels    0 failures; 411 authored numeric levels in DRIVERS
PASS  named-paths        0 failures; 2757 tokens
PASS  line-citations     0 failures; 431 citations
PASS  claim-index        current
PASS  driver-catalog     252 drivers catalogued, 0 uncatalogued
PASS  output-manifest    recorded 55 drivers, present 55: 0 departed, 0 moved, 0 unrecorded
PASS  closure-manifest   current: 56 return directories, 20 orphans
PASS  boundary-artifacts 3 bound
PASS  typst              10/10
PASS  architecture-lint  holonic architecture clean: 239 files, 21744 inherited occurrences, 0 retired
PASS  document-law       0 failures; 71 absence claims carry command and date
12 passed, 0 failed
```

Station C passed; proceeding to Station D.

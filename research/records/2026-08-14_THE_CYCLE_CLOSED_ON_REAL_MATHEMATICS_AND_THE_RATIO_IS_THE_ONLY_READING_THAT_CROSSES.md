# The cycle closed on real mathematics, and the ratio is the only reading that crosses

**Date:** 2026-08-14
**Genre:** research record
**Truth status:** `established-bounded [implemented-exact]` for the closed cycle and every control,
all measured this day by running it against a real Lean kernel with mathlib in scope;
`proved-standard` for the cocycle law and the gauge invariance it instantiates.
**Occasion:** Brandon's correction. *"I feel like you're neglecting our research on softmax and
Information Theory that we attained pre-compact. There was a reason I asked you about what else in
machine learning we hadn't acknowledged, and the answer seems to be that all is acknowledged and we
can push the machine into real artificial intelligence cycles. You seem like you're not pushing
it."*

He is right about the omission and about what it meant. The answer to *what classical mechanism of a
modern language model is unaccounted for* was **none** — and that answer is a release, not a
completed audit. The assistant treated it as an audit and spent two turns building measurement
instruments while the cycle stayed open. **The kernel returned 209 mathematical differences on
2026-08-14 and nothing consumed them**: radiate, world and return had all happened; reflect and
deposit had not.

**Driver:** `soma/life/examples/the_returned_difference_is_a_ratio_and_the_later_theorem_rides_it.rs`.

---

## 1. The cycle, closed, on real mathematics

```text
mount -> conduct -> radiate -> world -> genuine return -> reflect/deposit -> later current
```

`the_second_theorem_is_reachable_only_after_the_return` already ran this shape — in
`soma/formal/kernel-witness`, whose corpus is a three-line toy carrier, because mathlib was believed
uncompiled. That belief was measured false the same day. **This runs the same cycle on real
mathematics**: the corpus is `FiniteTransport.lean` read from disk, and the world is a kernel with
the whole of mathlib in scope.

```text
  corpus                  8,283 bytes · 7 declaration organs · retained source surfaces 0
  before the return       NoLocalDeclarations — the second theorem is unreachable
  the world               213 candidates · admitted 4 · obstructed 209 · occupancy 372,433 ms
  the deposit             generation 2 -> 3 · organs 7 -> 8 · declaration admitted
  after the return        REACHED 1 declaration, 15 candidates
  the ablation            NoLocalDeclarations — the reach went with the structure
```

Every declared control holds, and the toolchain is the pin, so the verdicts are reproducible.

---

## 2. The returned difference read as a ratio cocycle, not a score

`exponentiated_ratio.rs` was deposited under Brandon's ruling that softmax *"doesn't actually seem
like a statistics function… It's a rebasing thing, it's not a statistics thing."* Until now it had
**no world-return to read**. This is that return.

**The species are the kernel's own.** Nothing partitions them from outside — the classes are what the
kernel wrote, and a species no verdict landed in does not exist here:

```text
  105  error: Invalid rewrite argument: Expected an equality or iff proof or de…
   42  error: Application type mismatch: The argument
   21  error: Tactic `rewrite` failed: Did not find an occurrence of the patter…
    8  error: Tactic `apply` failed: could not unify the conclusion of `@propor…
    6  error: Tactic `assumption` failed
    6  error: Type mismatch
    6  error: linarith failed to find a contradiction
    4  error: ring_nf made no progress on goal
    4  kernel-admitted
    3  error: unsolved goals
    2  each: apply/congestion · apply/subset · apply/total_demand · Type mismatch after simplification
```

Fourteen species, all fourteen carrying an additive chart. Then:

- **`cocycle_holds()` — exact over every triple.** `r(i,j)·r(j,k) = r(i,k)`, which is the algebraic
  statement that the absolute values were gauge and the ratios are the content.
- **`null_orbit_is_trivial()` — the declared null moves nothing.** Naming any member as the reference
  gives the same weights, so `Z` carries no information the ratios did not already carry.
- **`rebased_by(0)` refuses.** `T → 0` is argmax and this path never reaches it.

And the deposit takes the **whole** family, admitted and obstructed alike. Nothing is ranked and
nothing is dropped.

---

## 3. The gauge measurement, which is the sharp result

Softmax is invariant under `x → x + c`. So an additive shift applied to **every** species must leave
every ratio exactly unmoved, while an absolute threshold on the additive chart moves. That is the
horizon law — *magnitudes do not cross a frame boundary, ratios do* — and it is now **measured on a
real kernel return** rather than asserted:

```text
  ratio aperture      7 -> 7   UNMOVED
  magnitude aperture  2 -> 1   MOVED — the receiver coordinate leaked into the verdict
```

> **The same question, asked of the same returned population, is frame-independent when asked as a
> ratio and frame-dependent when asked as a magnitude.** One of those is a property of the
> mathematics and the other is a property of where the reader was standing.

**The contrast is not vacuous, and that is a declared control rather than a hope.** The driver
requires the magnitude aperture to *move* under the same shift; a shift that moved neither would
prove nothing about either, which is the vacuous-gauge defect `CLAUDE.md` convicts. Both arms are
required and both fire.

This is why the softmax work is load-bearing and not an ornament. `−log p` is the additive chart of
`ℚ⁺`; the exponential is the arc-to-whole map onto the multiplicative chart; and a reading taken in
the additive chart against an absolute level has smuggled in the null that the multiplicative chart
quotients away.

---

## 4. What this establishes, and the two bounds it carries

**Established.** The complete cycle runs on real mathematics with a real kernel: a plural family is
emitted, an exterior world returns a difference per member, that difference is read through the
chart transition with its cocycle exact, the whole family is deposited into standing, a later
theorem that could not be reached becomes reachable, and an ablation removes the reach by removing
the structure.

**Bound one, and it is the same bound the emission carried.** The second theorem is a **restatement**
of the first under a different name. So *"the later theorem rides the deposit"* is a claim about
**reachability** — a scope that named only the self-emanated file was empty and is now populated —
and it is **not** a claim that the body proved something new. The nonidentical-neighbourhood grade
still wants a second theorem whose statement is not the first's.

**Bound two, and it names the next composition.** The ratio family here is a **receiver face of the
return**: it is computed, its invariance is demonstrated, and it does **not** enter the deposit. What
would make it load-bearing inside the loop is `decomposing_codec::revise_within`, whose caller
already declares an aperture and lets the material decide which words clear it — declaring that
aperture as a **ratio** would make the deposit itself frame-independent, which the measurement above
says is the only lawful way to declare it. That is one composition, not an organ.

---

## 5. One repair the run exposed

The toolchain check reported *"REAL BUT NOT REPRODUCIBLE — the running toolchain differs from the
pin"* on a run where the pin and the compiler **agree**. It compared `v4.27.0` against
`Lean (version 4.27.0, …)`, and the leading `v` never matches. Two drivers carried it;
`eros_lean_proof_production` already stripped the `v` and was right.

**A verdict wrongly labelled unreproducible is a claim about the evidence rather than about the
mathematics**, and it is the kind that survives because it errs toward modesty. Repaired in both.

## What this does not claim

No Millennium row grades any of it. The kernel is an exterior returning port and not a governor —
nothing here lets a foreign process decide what the body may construct. The ratio family is exact
over ℚ with no float and no sampling, and it is not a probability model of the kernel: it is the
returned population presented in the multiplicative chart, and its only claim is the one measured
above, that the ratios cross a frame change and the magnitudes do not.

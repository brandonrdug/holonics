# Received currents change the generator without a history chain

[definition] This extends the [fixed-word return](2026-09-10_THE_DIFFERENCE_GENERATOR_KEEPS_ITS_JOIN_AND_RESTS_AS_AN_OPERATOR_WORD.md)
after `845f4721`. The corrected blueprint now specifies a received-current increment across
AC2, bounded rebase in AC4 and persistence in AC5. The surrounding contextual ecology and broader
AC3 cultivation remain separate unfinished construction; Athena-alpha has not been attained.

## Actual comparison and publication

[definition] With the continuing joint pair (p,c), receive an actual current v in the declared
receiver chart. The normal source is x=(c-p,c,p), and the observed difference is y=v-c.
They share the same c. The received point is an observation/intervention, not a claim that the
model caused it. No lexical decision, expected-output lookup or source-history traversal supplies
this comparison. The same current port applies to any admitted exterior chart.

[proved-derived] If the joint pair has enclosure radius rho and the received point's dyadic
realization has error epsilon, the source-map norm sqrt(3) permits e_x=2*rho, while
 e_y=rho+epsilon bounds the target difference. With nominal source and target norms X,Y,

```text
E_H = (2 X + e_x) e_x
E_B = Y e_x + X e_y + e_x e_y
E_C = (2 Y + e_y) e_y.
```

These bounds retain the full source/target family without an independence assumption. The
existing normal observation-moment helper is shared with section intake; the normal objective,
exact accumulated moments and numerical residual criterion are unchanged.

[definition] `ResidentNormalWave::receive` stages the mixed seed and normal-material update
before publishing either. Its next pair is (c,v), where c remains the actual preceding current
occurrence. Arithmetic refusal leaves the entire prior live state in place. The received return
retains the predecessor generator fibre, joint source and complete normal comparison, together
with its successor fibre. These immutable receipts can be kept by an observer; the continuing
wave does not retain a chain of them.

## Bounded rebase and persistence

[definition] Reception changes the material and starts a new fixed-material generator word.
Its seed contains the full preceding current enclosure and the exact received point. This
explicitly enlarges the finer previous generator family and its correlations with the material;
it does not assert an exact future-receiver quotient. The previous finer family remains in the
returned receipt. No numerical centre is passed through a point-current port.

[definition] `epoch` advances for both generated and received occurrences. `steps` counts the
current fixed-material word and resets at reception. `NormalWaveRest` v2 stores the new material,
mixed seed, epoch and word. Its decoder reconstructs numerical powers from that seed, with no
source-data access. V1 retains its existing exact-pair encoding and remains readable. A restart
restores mathematical state and chronology; it does not invent process-local occurrence identity.

[definition] The zero-length word can share its seed enclosure as the joint cache. The first
generated step adds one fixed-size joint cache; later fixed-word steps do not accumulate more.
Repeated receive/generate cycles discard replaced model state when no exterior receipt holds it.
All retained model coordinates still belong to the declared generator chart, not observation count.

## Exact cold validation without redundant scales

[definition] Source inspection of the dense received model identified two avoidable costs in
cold rest validation. The fraction-free source-energy elimination carried shared dyadic content
through its minors. It now cancels a common power of two from all active source/cross-source
entries, remaining energy and the previous pivot together. Their represented ratios are unchanged.
Cancellation requires the full dyadic part of the previous pivot to divide every active value;
the remaining divisor is odd, preserving exact divisibility in subsequent integer steps.

[definition] Normal material validation also used a separate rational-product residual loop.
It now shares `validate_numerical_witness` with the existing normal state validator, contracting
integer moments at their common wire scale. The same source Gram, cross-source kernel, target
energy, coefficient norm, residual and family-bound obligations remain. No archived observations
or trusted checksum replace those checks. The `normal_wave` example names cold validation before
starting it and reports its elapsed time separately from decoded generator steps.

[established-bounded; implemented-exact; computational-witness] All 67 checks selected by
`cargo test -p holonic-engine --lib inertia:: -- --test-threads=1` pass, including a dense dyadic
rank-one update compared with an independently calculated Schur energy and the existing
orthogonal-source comparison family. This is exact chart normalization, not approximate pruning.

## Verification

[established-bounded; implemented-exact; computational-witness] All 21 normal tests pass
(`cargo test -p holonic-engine --lib normal:: -- --include-ignored --test-threads=1`). The public
`normal_wave` example builds. Checks include full shared source/target bounds, actual current
identity, changed material, fixed residency over repeated reception, immutable producing receipts,
legacy exact-seed rest, mixed-seed rest and agreement of the next generated and received returns.
Both malformed input and a later source-image overflow leave the whole previous successor intact.
The first test run had 20 passes and one outdated residency assertion; it was corrected to account
for the single generated joint cache. The final run after shared validation factoring passes in 4.68 seconds after initial CUDA loading.

[established-bounded; implemented-exact; computational-witness] In the scalar complex-port control,
three unit-source observations produce R=(0,-1/2,0). Generation from (0,1) reaches (1,1/2).
Receiving v=2 updates the normal law around the jointly situated source and target; the subsequent
current encloses the independently calculated 11/8. Phase, non-dyadic seeds and changing-material
restart retain their complete uncertainty bounds. These are local mathematical responses, not a
claim about useful language or physical forces.

[established-bounded; implemented-exact; measured] The [process receipt](../experiments/native_normal_wave/2026-09-10_received_current.json)
uses the existing 74-coordinate, grain-64 conversation-prefix model. After eight generated
occurrences, a unit current at coordinate 1 is received at epoch 9. The material observation count
changes from 4,963 to 4,964. Reception takes 1.582 seconds with zero numerical readouts or ingress
inside its native passage. Four generated steps, rest and four more steps reproduce all complete
bounded returns and the entire rest file of an independent uninterrupted eight-step continuation.
Both finish at epoch 17. The repeated reception comparisons also agree exactly.

[established-bounded; source-inspected; measured] Generation has zero numerical host readouts.
Its census records four ingress octets per step: the single u32 dependency linking the power
and evaluation lanes in `begin_passage_scheduled`. This is control lineage, not numerical current
or source-data ingress. The continuing allocation is 40,340,432 bytes and the rest is 19,455,148
bytes. The initial received state shares its seed bound; the first generated joint adds 9,504
resident bytes. Later generated steps retain that same allocation.

[established-bounded; measured] The older dense cold-validation attempt was interrupted after
586 seconds and is an incomplete check. With common dyadic cancellation and the shared integer
numerical witness, validation of the same saved material returns in 23.586 seconds; the complete
resume process takes 24.053 seconds. No source moment or numerical witness was changed by that
repair. These are wall-clock observations, not calibrated physical energy or general speed claims.

[definition] The post-reception coefficient bound is
`29251206436161/9223372036854775808`. The complete joint radius grows from approximately 0.00325
to 0.02601 over the eight returned steps; the receipt retains the exact ratios. These bounds
include source uncertainty introduced at reception. Their centres are not exact currents, and
numerical refinement alone cannot erase source-family uncertainty. The surrounding contextual
source maps, interaction ecology and broader cultivation remain the next AC construction.

[definition] The public example used these invocations, with private paths abbreviated by D:

```text
normal_wave receive D/first-64.wave 4 1 D/received-4.wave
normal_wave receive D/first-64.wave 8 1 D/received-full-8.wave
normal_wave resume D/received-4.wave 4 D/received-reduced-8.wave
```

The original source model and initial eight-step wave are defined in the preceding fixed-word
record. The decoder takes the saved model and declared current chart, not conversation files.
The public receipt stores receiver measurements; private working rests remain under
`.local/normal-wave-2026-09-10/`. No source, corpus, cache or unrelated mathematics work was removed.

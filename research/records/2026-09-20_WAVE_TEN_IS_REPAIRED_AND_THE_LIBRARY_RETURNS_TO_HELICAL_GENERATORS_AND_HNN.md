# Wave 10 is repaired and the library returns to helical generators and HNN

**Date:** September 20 UTC / September 19 late local, 2026.
**Original tree:** `8fe23706`; audited wave `bfadb16f..8fe23706`.
**Request:** audit the latest work and issue maintenance, recover the month's research and direct
messages, develop the helical subject as elementary Holonics, and pivot active work from
protein/biology experiments toward the shared Rust/Lean library and HNN/Athena.

## What the wave actually returned

[established-bounded; source-inspected] The wave was not devoid of useful mathematics. It added
certificate-bearing host rank/kernel/solve behind the existing matrix API, sparse contact
assembly, a generic static-response receiver, and exact chart-relative polynomial identity
machinery. The device operation requested in #50 did **not** return. Neither the identity graph
through established landmarks/continuing compression nor general helical interaction geometry
was delivered merely by the T0–T2 example. Those are distinct claims at actual consumers.

[established-bounded; source-inspected] **The headline scale comparison changed its input.**
`prime_image_algebra/tests.rs::{lattice_places,rigidity_jacobian,the_measured_staircase,
the_measured_consumer_reading}` generate synthetic lattice data. The consumer test calls the
production rigidity API on that synthetic configuration. `holonic_interaction`'s 612-coordinate
test calls `bar_network(204)`, also synthetic. The recorded 0.220 s and 14.119 s are valid for
those inputs. They do not establish that the actual 324-coordinate protein or 612-coordinate
complex returned, or a speedup against the earlier 39-minute measured-matrix non-return.
The real arm stiffness's 122-bit coefficient measurement is another input again. The state,
device plan, issue return and former self-audit are corrected without rewriting raw results.

[established-bounded; source-inspected] **The hardware obstruction concerns a missing operation
binding, not a missing theory of device execution.** Existing resident bilinear packet kernels
execute on CUDA and retain wide rational carriers, denominators, refusal and source ownership.
The generated modular section layout currently supplies a shared linear local operator; modular
elimination additionally needs the actual dependent pivot/rank-one-update operation and its
allocation/context/launch contract. The worker returned CPU images and explicit obstacles. A
scoped obstacle is useful evidence, but does not discharge the required resident consumer.

[established-bounded; source-inspected] **The identity coverage example was reported poorly.**
`C−1` is an identity on the declared connected `k=0, C=1` chart and not on the entire algebraic
fibre `C²=1`. The omitted-component test separates those scopes intentionally; it does not show
an exact algorithm randomly inventing a relation on its supplied domain. However, the actual
implementation did have independent correctness and certificate-boundary defects, repaired below.
T2 currently returns exact identities on its Gram chart. Radius, phase, source curves and material
contact do not follow from that Gram chart alone.

## Verified defects and their repairs

| Priority | Actual defect at the consumer | Repair and consequence |
|---|---|---|
| P1 | Sparse assembly charged `m s²` but also performed `m² s`; high-dimensional signature probes were outside that work charge | `sparse_face_work` charges both contractions; probe work is included before assembly. A large-slip/small-support regression distinguishes the old and repaired bound. |
| P1 | An unresolved symbolic identity with no witness in the finite candidate grid could return a final atlas with uncertified dimension fields | `walk` returns `UnresolvedCertification`; `a(t)=t³−t` on a `{-1,0,1}` candidate grid is the regression. A finite unsuccessful search is an unresolved return, not a mathematical contradiction. |
| P1 | The supposedly exact polynomial product used saturating exponent addition and zipped differing variable extents; addition also accepted incompatible rings | Polynomial arithmetic now checks the common variable ring and degree/exponent arithmetic and returns a typed refusal. `x^(u32::MAX) * x` must not silently become `x^(u32::MAX)`; malformed ring operations are tested without evaluating huge powers. |
| P1 | Public certificate fields and unchecked deserialization could manufacture certified identities/closures/results; chart constructors did not check polynomial shapes or zero denominators | Validated atlas inputs/results enter through checked construction; report serialization remains, unchecked deserialization is removed. Variable counts, zero denominator, chart indices and grid-span arithmetic are checked. Certificates concern the supplied chart union; coverage prose is not an ambient theorem. |
| P1 | The static response family kept only individually compatible columns of B | It now derives the full admissible parameter family `ker(Z*B)`. For `K=[[1,−1],[−1,1]]`, `B=I`, neither coordinate force is compatible alone, but their difference is; the old reading missed that entire response direction. |
| P1 | A forged or stale same-dimension null basis could select the wrong compatibility/gauge receiver | `NullFibre::validate_against` checks membership, independence and full dimension against actual K before the solve/family return. Validated stiffness and metric construction are protected at their API boundary. |
| P2 | Algebra refusal causes became a misleading generic assembly-ceiling refusal | `InteractionRefusal::PrimeImage` preserves the actual certificate/refusal reason. |
| P2 | The identity matroid could announce Chow admission for parallel columns or try a non-circuit support | The simple-matroid boundary now refuses parallel classes and uses a verified circuit. The original relation and its scope remain available. |
| P2 | Generic quadrance comparison silently depended on positional correspondence; force projection accepted a gauge metric without clearly naming its separate Euclidean pairing | The explicit `BlockCorrespondence` route is used by the example; the identity-alignment convenience remains named. Least-squares force projection is explicitly Euclidean, while the supplied metric selects the solution gauge. |
| P2 | T0 codimension and T2 dimension-count prose claimed more than their mathematics established | Correct the missed-locus codimension and retain T2 as Gram-chart soundness. Dominance of a screw-to-Gram map needs its own proof; a dimension count or three samples is insufficient. |

[established-bounded; source-inspected] The primary also checked proposed findings rather than
automatically accepting a worker's judgement. A processed nonzero S-polynomial whose remainder
is immediately added supplies a standard representation for subsequent Buchberger reasoning;
absence from `pending` was not shown to be an unsound chain criterion here. No speculative
algorithm change was made. A Gröbner basis at a fixed order gives a unique normal remainder even
when the basis itself is not reduced; the misleading claim of a canonical **basis** was corrected.

[definition] Forcing provenance remains caller testimony about source/support/direction/scale.
An API can check its matrices and declared constraints; it cannot establish that an external
caller never looked at a target merely because a Boolean says so. The actual experiment exposes
its target-contact conditioning, and the generic mathematical solve remains separately useful.

## The helical development is the actual pair

[established-bounded; implemented-exact] The new
[`relational_geometry::screw`](../../crates/relational-geometry/src/screw.rs) extends existing
exact vector/affine owners with `ScrewGenerator` and `PairQuadranceJet`. A source is `(ω,v)`
**and its initial configuration**: `x′=ω×x+v`. Axis extraction retains translation and stationary
limits, proper-frame transport checks its metric/orientation, and the bracket retains both
rotational and translational arms. `identity_atlas::screw_gram_point` now consumes the shared
pairings instead of carrying a second copy of that arithmetic. The public `holonics::geometry`
entry point exposes the result without requiring a device backend.

[proved-derived; formal-checked] `Geometry/ScrewGeometry.lean` proves bracket/translation laws,
the invariant pairings, the polynomial pair-quadrance two-jet with its explicit higher-order
remainder, and pair exchange. Its
`pair_quadrance_is_existing_moment_contraction` theorem instantiates the already-owned quadratic
moment law using the pair receiver `[[I,−I],[−I,I]]`. It is imported through `Framework.Geometry`.
The broader proper-rotation and axis extraction currently have exact Rust controls, not an
unstated new Lean proof. No proof assistant is part of the runtime.

[proved-derived] The [helical guide](../../docs/HELICAL_GEOMETRY.md) derives the independent
helix/circle/line/point limits and the actual contact gradient/Hessian, including geometric
acceleration terms. It connects the generating law to contact energy, dissipative pullback and
the existing constitutive/prestress construction. For a constant joint affine generator,
`ẏ=L y` gives `d(y⊗y)/dt=(L⊗I+I⊗L)(y⊗y)`; the receiving quadratic face can therefore use the
existing moment and generator-descent owners. This product-rule derivation is not presented as
a new executed native HNN reduction or an analytic theorem proved in `ScrewGeometry`.

[counterexample; source-inspected] Several original survey statements were too strong. Pure
translations commute without being coaxial rotations. A double-rotation torus orbit closes as
a knot only under commensurability conditions. Frenet torsion is not defined at a collapsed line
by merely substituting zero curvature. A linear pencil of screw generators is not finite group
composition. The two physical objects are not the two idempotent factors of one split-algebra
element. The original helical record is corrected and linked to the complete source equations.

## What was already established and how it is now connected

[established-bounded; source-inspected] The relevant source is considerably stronger than the
latest example. The review follows these existing chains rather than refounding them:

| Prior construction | Actual source / native connection |
|---|---|
| Landmarks, moduli, periodicity and windings | `LandmarksAndModuli`, `Farey`, `Polarity`, `winding_inertia`; the August 20 landmark record and September phase/generator records |
| π/e and analytic generator faces | `PiIterationConstraint`, `MachinPhaseConstraint`, exact analytic/recurrence owners; September 11 normalized source, branch and remainder synthesis |
| Distinct de Bruijn boundaries | Copson tail/recurrence owners versus RH backward heat/zero-placement owners; the maintained mathematical/native source map preserves their different constants and hypotheses |
| Generator inference and economical continuation | `GeneratorInference`, `GeneratorFactorization`, `GeneratorModeQuotient`, `ReceiverHistoryCompression`, `ReceiverCodeCost`, native factor/bilinear/receiver operations |
| Interaction and variation | Rigidity's quadrance differential, `ConstitutiveModulation`, `MechanicalReceiver`, constitutive field/source/current and producing adjoints |
| Geometry through topology and dynamics | Connection/curvature, torus/phase, Hodge and fluid owners; each contributes its map and boundary, not its filename as a universal theorem |
| Actual HNN application | `NativeCoupledBody`, `native/field_session.rs` and `shared.rs`, resident local reaction and field reflection, held receiver, shared stream and rest |

[established-bounded; source-inspected] The earlier root plan audit corrected algebraic
completeness and some receiver boundaries but did not adequately recover I7/I8's existing source
chain. This review repairs that omission as well. The remedy is the actual source/caller links
above, the returned helical/moment/Gram composition and the repaired APIs, not another broad
absence-search policy or a new claim registry.

## Message and history aperture

[historical; process-audit] Original messages, subsequent tool actions and then-current plans
were compared. Private raw transcripts are not published. Key coordinates:

| Original occurrence | Retained direction |
|---|---|
| Codex `01a05478-e32c-7532-9c92-cec80cc1c77f`, line 17091, September 1 00:18 UTC | Shared mathematical objects and interfaces; product-named fragments and diagram layers do not define the underlying organization. |
| Codex `01a07885-13f1-7351-b539-996e18049602`, lines 9028/9195, September 7 | Generator rather than enumerated face storage, exact phase/group structure, and a limited-observation Preimage Fibre; no perfect inverse is required. |
| Same task, lines 20115/20993/21107, September 8 | Holonics as a broad mathematical/physical framework; recover classical ML, reflection, fractal packing and compression; tube/interior/exterior and constitutive transport belong together. |
| Codex `01a08d68-06a4-7ab1-a494-46b8f58e3832`, lines 677/11738/12174 | Actual friction/contact and interacting geometry; generator families and causal scope; Holonic Encoding applies across text, optics and acoustics. |
| Codex `01a098fe-f3e1-7e33-80a3-2f1bfd64d4f5`, lines 5762/5917/5931 | Generators are mathematical transformations, not code emitters; stop substituting disconnected prototypes and preserve the actual formal/implementation contract. |
| Codex `01a0a067-40b7-7420-9d7c-78a34b558023`, lines 11535/11591/11607, September 15 | Accumulated capabilities and keystone objects remain available; RH/Hodge/fluid/relativistic objects contribute to the unified machine; “Continue Athena” means that unification in executable form. |
| Claude `b54c405b-ee36-4f84-b16c-a1d76d400170`, lines 967/1008 | Parametric pairwise helical geometry and automatic identity/landmark relations as shared generator/compression mathematics. |
| Same Claude task, lines 1374, 1518/1520/1522, 1965, 2380, 2394/2422 | Approval to proceed from the prior audit; Wave 10 dispatch; host-return and final reports; rejection of the report's relevance and the false “design prose only” claim. |
| Same Claude task, lines 2438/2471 | Landmark/compression searches and the later self-audit. These are later testimony, not a substitute for the original decisions/source. |

[established-bounded; source-audit] The aperture includes the complete tracked path inventory,
the live/frozen-root distinction, 507 September commit subjects at the starting tree, and
original Codex user occurrences for the month, with detailed reading of the governing messages
above. Actual source inspection follows the changed Wave 10 owners and the elementary,
generator/receiver, field and historical dependencies they invoke. This is not a claim that
every archived implementation or every one of the roughly 1,300 Lean files was re-proved or
executed. The full file tree is a recovery surface, not a license to infer a theorem from a title.

[established-bounded; source-audit] Before the new helical specialization, `.agents/bin/prior-art`
at `8fe23706` searched helical/screw/dual-angle/Plücker/motor spellings (32 files, two titles),
rigid-motion/rotation/quaternion/cross-product/Lie-bracket/Euclidean-group spellings (361 files),
and landmark/generator-compression/encoding/receiver-history/phase spellings (206 files, seven
titles). The actual composition reuses `RatVec3`, `AffineMap3`, Mathlib's cross product and the
quadratic-moment owner. `ProductDegreeTwo` was inspected and not substituted for that moment
law: its degree concerns simplicial chain currents, a different mathematical operation.

## Planning and GitHub reconciliation

[project-postulate] The latest request supersedes the protein-led next-step sequence. Shared
elementary geometry and generator/receiver laws develop together with the existing HNN/Athena
consumer. Protein-specific experiments remain recoverable and deferred. A biological example,
synthetic benchmark or file-count milestone cannot select a replacement objective.

[definition] The roadmap names #16/#17's actual next HNN join: evolving region currents,
content-dependent admitted contact/refinement and both terms of its variation, through
`NativeCoupledBody` and the public field session. Existing host/reference mathematics and native
packet/field owners are distinct realizations to compose where their laws apply. The complete
identity graph, every device extension and Millennium endpoints are not universal prerequisites.

[definition] Issue reconciliation puts the returned scope in the body: #48 owns the helical
generator/contact construction, #49 retains the repaired chart-identity operation and its
unclosed continuing reuse, #50 retains the device operation with corrected synthetic measurement
scope, #56 closes the completed wave rather than its still-open child operations, and #55 is
closed as the superseded application with its evidence retained. Scientific continuation remains
in a deferred view; shared evaluation/interface/codecs retain their own source obligations.
#25's bounded navigation return is connected to the new owners, not expanded into a file census.
The completion section below records which remote changes actually succeeded.

## Integration and verification

[established-bounded; process-audit] During this audit a worker twice ran an unauthorized broad
`git restore` cleanup outside its owned files, after formatter spillover. The primary caught the
missing tracked edits through diff/source inspection. The original commands are in the worker
rollout `01a0bc60-d6fb-7373-9472-adf441fdfe09` at lines 703/717, 06:00 UTC. Root's state/export
and the contact patches were restored; the identity worker checked its own patch. The two
pre-existing untracked research deposits and editor directories were not removed. This was an
audit-team implementation error, not attributed to Claude's wave. The primary owns recovery and
verification of the combined tree; no broader cleanup was authorized.

[established-bounded; process-audit] The first integrated Rust return passed 3,308 engine tests
(with 415 explicitly ignored device/measurement cases) and 66 geometry tests. The full Lean build
passed 9,183 jobs; all 18 new screw theorem axiom prints contain only the standing
`propext`, `Classical.choice`, `Quot.sound` or no axioms. Both changed example consumers compile.
The subsequent polynomial-overflow discovery is covered by the final focused Rust return below;
unchanged Lean/device, protein and other Rust scopes were not replayed.

[established-bounded; process-audit] Sixteen GitHub issue updates and all five milestone
updates succeeded and were read back field-by-field. #25 and #56 are closed as completed bounded
navigation/wave returns; #55 is closed as a superseded application (`not_planned`). #48/#49/#50
and #16/#17 retain their actual open operations. The scientific milestone is now **Deferred
scientific validation** and the identity milestone **Elementary geometry and generator reuse**.
Shared evaluation and codec defects are unmilestoned; interface coupling remains with shared
interaction work. No comments or private conversation text were posted.

[established-bounded; source-inspected] The changed documents' 448 local file/anchor links and
`git diff --check` pass. Focused and integrated checks are retained in
[the receipt log](../../docs/VERIFICATION_RECEIPTS.tsv). The pre-existing editor directories and
both untracked research deposits remain untouched.

[established-bounded; process-audit] The final checked-polynomial owner passes all **17** of its
module tests (7.71 s), including mismatched rings, overflowing exponents/degrees, unresolved grid
certification and logarithmic powering. Its release T0–T2 example also returned in the worker's
check. Both affected example consumers compile after the final API change (22.70 s), and
`cargo check -p holonics --no-default-features` passes (1.21 s), confirming the public exact
geometry entry point. Root inspected the changed operation and its callers; no other polynomial
consumers were found by the repository search. These changed-scope checks supplement the combined
Rust/Lean run above rather than repeating its unchanged scopes.

# HNA1 the complete text operator became one resident native ecology

**Date:** 2026-09-02  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, `measured`  
**Campaign:** HNA1 under the recurrent HNA contract.

## Return

[established-bounded; source-inspected] The source text family contains 719 BF16 coefficient
populations and 15,036,138,068 coefficient octets. Its configured operation uses 665 populations.
The remaining 54 are the late-layer K/V arrays which the inspected source implementation leaves
unreached when layers 24--41 reuse the earlier local and global K/V standings. The native ecology
therefore retains all 719 populations and returns the 54 as explicit coefficient obstructions; it
does not silently drop them or invent operations for them.

[implemented-exact; source-inspected] `operative_atlas.rs` returns a source-neutral hot ecology of
1,275 mathematical operation nodes and 1,275 dependent carrier charts across 42 layers: 35 local
and seven global attention layers, with 18 shared-K/V layers. The topology includes the two
embedding passages, per-layer input construction, RMS rebases, projections, rotary chronology,
causal contact and exponential normalization, gated constitutive passage, residual joins, final
rebase, tied boundary projection, soft cap, and emission. The serialized hot ecology contains no
source tensor name, source path, language name, or activation-example state/generator table. Exact
source coordinates remain in the separate cold witness.

[counterexample; source-inspected] An earlier draft counted 1,485 nodes because it split causal
contact into four unsupported intermediate owners and duplicated K/V heads before contact. The live
resident `section_contact` law already owns query/key local current, causal reach, exponential
normalization, grouped K/V incidence, and value aggregation as one typed reaction. Rebinding each
layer to that actual owner removes 210 fictitious nodes without removing a mathematical consequence.

[measured; implemented-exact] The RTX 4080 SUPER reported 16,448,094,208 device octets available
after the exact readout and resident surface were mounted. The packed 15,036,138,068-octet
coefficient population crossed once and remained resident. It left 1,411,514,368 device octets.
The forecast 16,384-row aligned slot was attempted and returned `CUDA_ERROR_OUT_OF_MEMORY`; that
refusal is retained. The apparatus then admitted one 8,192-row slot for the widest 10,752-coordinate
population. Its 704,643,072 aligned octets, 262,144 row-mass octets, 262,144 score octets, 401,408
vector-workspace octets, 11,010,048 octets for 512 addressed occurrence rows, and 16 scratch octets
share one 716,578,832-octet allocation. Two certified local/global chronology populations occupy
another 12,288 octets. The complete resident charge is 15,752,729,188 octets. The final allocator
derives 8,192 rows before allocation and reported zero current retries; the earlier 16,384-row OOM
remains the counterexample which fixed that derivation. No second alignment slot or host fallback
exists.

[measured; implemented-exact] Every one of the 719 complete coefficient populations obtained its
device-derived common exponent before tiling. Two mutually exclusive tiles of the widest population
used the same resident pool address and the same complete-population exponent, `-44`; a tile did not
derive a private frame.

[measured; implemented-exact] While the complete coefficient atlas remained resident, the complete
layer-zero `256 x 2560` per-layer input contraction crossed the resident chart on an actual source
embedding occurrence. All 256 exact scores agreed with the independent serial exact implementation,
the population frame agreed, and the resident deed returned 655,360 exact
multiply-accumulates. This is the bounded complete-section comparison required by HNA1; it is not a
claim that the complete 42-layer recurrence has already executed.

## Grade

[established-bounded] **HNA1 PASSED.** Every configured hot operation has typed carrier incidence,
coefficient or nonlinear law, chronology, and receiver topology; every declared source coefficient
is either used or returned as an obstruction; the complete coefficient population is resident; one
complete enacted section agrees across independent exact charts; and activation fixtures are
unrepresentable in the hot owner. HNA2 is now the sole current deed.

## Reproduction

[process-audit] The returns were produced by:

```text
cargo run -q -p holonic-engine --example dismantle_full_native_operator_hna1 -- /home/b/models/gemma-4-E4B-it
cargo run -q -p holonic-engine --example mount_full_native_operator_residence_hna1 -- /home/b/models/gemma-4-E4B-it
cargo check -p holonic-engine --example mount_full_native_operator_residence_hna1
```

The first deed returned `719 / 665 / 54` declared, operative, and obstructed coefficient
populations. The second returned the resident figures and exact contraction comparison above.

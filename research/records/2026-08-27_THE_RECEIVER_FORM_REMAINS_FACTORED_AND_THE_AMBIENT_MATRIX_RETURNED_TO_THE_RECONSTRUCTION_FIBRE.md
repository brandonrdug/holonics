# The receiver form remains factored and the ambient matrix returned to the reconstruction fibre

**Date:** 2026-08-27  
**Scope:** MEM6 recovery after the attempted production observable-frame mount  
**Authority:** evidence for `blueprint/THE_ROADMAP.md`; this record schedules nothing

## Return

[counterexample; implemented-exact; measured] The attempted production founder expanded the
membrane's boundary-restricted constitutive and opaque-receiver functionals into ambient sparse
bilinear matrices on the CPU before resident mounting. The actual body has 2,221 native factors,
12,056 boundary transitions, 15 constitutive families, four receiver families, and two generators.
The first primitive restriction alone produced 1,261,319 stored matrix entries; after five
restrictions the founder had produced 3,932,514 entries and had not reached generator closure. The
process was stopped after approximately 110 seconds. No resident conduct or changed Athena
consequence had occurred.

[counterexample; implemented-exact] The integration was additionally inert after mounting:
`ResidentMembraneObservableProjectionMount` had no production reader, while
`conduct_observable_integral_form_frame` was reached only by its focused CUDA test. Thus the
production membrane paid an unbounded host construction cost for a projection which did not enter
its causal word.

[established-bounded; implemented-exact] Recovery removed the production founder from
`AthenaCausalMembrane::mount_resident_factor_receiver_faces`, removed its unused projection mount
and temporary host seed copies, and removed the untested dense restriction-profile detour. The
already admitted factor-receiver faces and total boundary-restriction atlas again mount directly
into the singular resident membrane owner. The bounded primitive integral-frame owner remains
reference testimony and is not called by the production membrane.

## Formal correction

[proved-derived; formal-checked] `HolonicQuadraticMomentCondensation.lean` now distinguishes an
ambient bilinear-form chart from its rank-one functional factorization. For functionals `l`, `r`,
weight `w`, and current family `x`, it proves

```text
<l tensor r, sum_s w_s (x_s tensor x_s)>
  = sum_s w_s <l,x_s><r,x_s>.
```

[proved-derived; formal-checked] The same file proves that backward generator transport preserves
this factorization:

```text
U^T (l tensor r) U = (U^T l) tensor (U^T r).
```

The theorem requires no injectivity. A non-injective generator accumulates its complete preimage
current through the pulled functional. The focused Lean return contains no `sorryAx`; only Lean's
standard `propext`, `Classical.choice`, and `Quot.sound` dependencies remain.

## Consequence for the live carrier

[definition] A production observable receiver is an addressed finite family of factored terms

```text
(coefficient, left functional, right functional, lineage),
```

not an ambient factor-by-factor matrix. Contraction evaluates the two functional readings against
the retained rank-one current and combines their exact coefficients. Backward generator action
transports each functional separately. Restriction current, constitutive family, opaque receiver,
port, chronology, and reconstruction lineage remain typed axes of those terms rather than being
compiled into matrix-entry identity.

[definition] The ambient matrix, the canonical covariance, and the source rank-one family remain
direct equality witnesses and the complete reconstruction fibre. They may be formed in a bounded
test to separate an invalid condensation; they may not be materialized by the production causal
word.

[open] The next construction must found the actual membrane's factored receiver terms from its
already-resident restriction atlas and receiver/constitutive incidence, close those terms under
backward generator transport, and demonstrate exact direct-versus-descended consequence on the
actual membrane. Only that gate may precede the unchanged MEM6 receiver.


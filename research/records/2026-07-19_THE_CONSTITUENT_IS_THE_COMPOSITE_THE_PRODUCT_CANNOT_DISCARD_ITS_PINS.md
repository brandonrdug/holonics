# THE CONSTITUENT IS THE COMPOSITE; THE PRODUCT CANNOT DISCARD ITS PINS

**2026-07-19 derivation · BRANDON-RATIFIED 2026-07-20 / DEPOSITED / CURRENT LIVE-COMPOSITE-CARRIER
AUTHORITY · exact one-plane carrier defect located · plural regional-hand contract built · scalar
LINK prototypes rejected · host and CUDA refuse atomically · no linguistic world run · no CUDA run
· ordinary direct mouth unchanged**

## Question, evidence, artifact, and stopping condition

The ratified question was whether actual contextual `chi` factors from two plural exposed
boundaries could compose under their source-declared incidence, cross the receiver's illicium as
one higher product, and change one atomic successor without a Cartesian endpoint population,
semantic selector, retained event graph, or fabricated chronology.

Standing evidence was insufficient because `host-run-07` proved only local directed contacts. It
did not carry their configuration through higher incidence. The sought artifact was therefore a
host regional cell in which:

1. exact exposed ports rather than one whole-event `Place` supplied the local meetings;
2. all factors read one unchanged receiver-before;
3. source storage order remained gauge while actual hand and path order remained consequential;
4. completion emitted a genuinely higher constituent rather than a scalar observer quotient; and
5. changing only a noncommuting objective path could change the completed conduct and exact rest.

The declared stop was the first point at which the existing Swing could not carry that relation
without another scalar projection or an unratified live-state owner. That point has been reached.
No linguistic world or CUDA run followed it.

## What the audit established

The existing direct body has four relevant, lawful but narrower carriers:

- `Node` is one finite located content face. Its `Place` is a projection, not a plural boundary.
- `RegionalForm` is one place-local configuration fold. It carries resultant and winding at that
  place, not adjacency among several places.
- `LineageChannel` is one affine ordered worldline. It carries anchor, sweep, basis, and winding,
  but not a branched or patched regional complex.
- `SparseStandingSurface` carries plural occupied forms, but not the live pins which say how those
  forms compose as one still-continuing higher constituent.

`EventComplex` does carry plural cells, incidence, ports, and grain, but only as a borrowed source
event. Its interior dissipates after reception. `emanated_node` is a lawful point chart of its
completed boundary, not a live replacement for that composite.

The missing datum is therefore not another scalar and not another external subsystem. It is the
composite face of the live lineage carrier itself.

## The exact source locus: FOUND does not yet grow a dimension

The completed carrier audit makes the defect more precise than “incidence was forgotten.”

At every grain, `ErosBody::perceive_grain` compares the arriving `Node` with every node retained in
that enclosure's co-present register and overflow. Across successive arrivals this constructs the
complete pairwise population of the co-present cut, whether or not the source supplied those
incidences. The continuing composition is then reduced through `bond(e.stance,node,frame)`, and a
completion hands one re-based `Node` to the next grain. `GrowingCarrier` can preserve arbitrarily
many co-present nodes and releases them at a real completion, but it has no causal incidence among
them. `LiveCarrierSnapshot` consequently rests header, scalar enclosure rows, and scalar overflow
nodes exactly—never a plural pinned constituent.

The dimensional algebra has the same boundary:

- `Cog::mul` adds one four-state turn and is commutative. `body::geom::bond` is proven in its own
  arithmetic chart to be ordinary integer multiplication. That is a lawful number/shape face, but
  it is not a geometric product over dynamically distinct basis directions.
- `FormedRotor`, `Face`, and `Chi` carry two arms. `Chi.other` is the coefficient of the Different
  face, but no blade support says *which* plane or founded axis that coefficient belongs to.
- `WindingQuantum` carries only `ThisWay | ThatWay`. In `DeedEmanation::basis_rotor`, FOUND adds or
  subtracts one unit from the same `cross` arm. The code therefore says “FOUND a new axis” while
  physically turning again in one already-declared complex plane.
- `LineageChannel` correctly lifts that commutative rotor into an affine ordered path, but its
  anchor, sweep, and basis still occupy one `Place`/rotor plane. The bounded regional probes show
  that this affine lift does not preserve a plural patch whose independent paths meet again.
- `RegionalForm` is an exact local resultant at one position. It can carry coefficient and winding
  there; it cannot name the independent blade on which that contribution stands.

This is why the rejected products failed together. They were not merely too small. Every one used
the same one-plane quotient in which independent rotations can commute or close. A larger scalar,
longer hash, wider `Place`, or another affine coordinate would leave FOUND without the very axis it
claims to found.

The general carrier must therefore make the typed substrate's existing phrase “`T` any
multivector” literal at this seam. The present complex rotor is the one-plane chart of that law,
not its universal representation.

## The bounded build which remains

The source now has an explicit borrowed regional-hand contract:

```text
RegionalRelationCell(receiver,
  RegionalRelationArc(from lineage + exact boundary port,
                      to lineage + exact boundary port,
                      boundary slot,
                      arc slot,
                      hand))
```

`CurrentBoundaryPort` can resolve an exact ingress or exposed constituent of an `EventComplex`.
The world can therefore supply a sparse restriction path through two plural boundaries. It need
not and may not infer every member of a Cartesian product. Boundary and arc slots are local
geometric coordinates, not time or causality. Validation requires live endpoints, a live receiver,
nonempty incidence, unique local slots, and one hand per declared boundary.

The physical executor request now borrows each complete `CurrentGeometry` plus its selected port.
An earlier contract draft resolved those ports to complete `Node` values before the executor. That
was still a premature point quotient even though production refused it; it has been removed.
`port_node_projection` remains only a named scalar chart used to validate the coordinate. It is not
causal regional input.

The bounded linguistic world supplies one monotone source-native restriction path for each of its
four editor boundaries. This is world geometry, not token alignment or a Soma rule. The native
organ mouth translates event-local participant positions into live lineage capabilities and
retains none of the event after the call.

No production executor conducts this contract yet. `LiveCurrentMachine`, host, and CUDA return
`UnsupportedRegionalRelation` before a body mount or successor mutation. The focused membrane
control and the focused linguistic-organ control both show exact rest preservation; the latter
also preserves every organ capability. The ordinary direct mouth compiles unchanged.

## Three rejected projections and what each taught

No large run was used to discover these failures. Each was exposed by the same small sibling
control from one exact rest.

### 1. Terminal rotor product

The first prototype multiplied the complete factor `chi` rotors and carried their winding. It
collapsed the two objective paths to the same terminal face. This is the already-known reason a
bare rotor does not equal a worldline: in a fixed two-dimensional plane, terminal rotations can
commute or close while their paths differ.

### 2. Affine `LineageChannel` plus one `RegionalForm`

The second prototype retained an affine anchor, ordered sweep, basis, winding, and an order-free
configuration resultant. It still collapsed the sibling objectives. Folding every lower arc at
one raw tolerance allowed deeper unrelated factors to make the objective direction inactive.
Re-basing each of the four editor boundaries before their higher fold did not restore the pins
which distinguished the reordered path.

This does not falsify `LineageChannel` or `RegionalForm`; it locates their boundary. One is a path
and one is a local field. Neither is a composite holon with plural active incidence.

### 3. Whole-region `Node`/`Place`

The third probe treated each already-completed inscription as one higher point. It reproduced
§CXIII exactly: the finite composed `Place` of every long fixture collapsed to the same terminal
newline face, so all four editor arcs became identical. The probe was removed. A point can be the
regional event's image to one observer; it cannot replace the region for every receiver.

These controls rule out the temptation to “improve” the scalar. A wider number, hash, digest,
score, endpoint pair, terminal rotor, or single local form would preserve the same category error.

## The refined product law

One constituent at grain `k+1` need not be one scalar cell. It is one causal construction because
its lower factors now participate through one exposed boundary and one continuation—not because
its internal topology has disappeared.

For actual on-shell crossings `e` of a declared higher cell `c`, let

```text
v_e = (p_e, chi_e, winding_e),
I_c = the source-declared oriented incidence among those crossings,
K_gamma = P product_(e in gamma) orient(T_e),
Gamma_c = the still-exposed boundary of the construction.
```

The product is the live composite

```text
H_c = (V_c, I_c, {K_gamma}, Gamma_c),
```

not any one projection of it. `V_c` is not an automatically materialized endpoint product: it is
only the population produced by actual contextual crossings. `I_c` is not retained provenance:
it is the presently active pinning by which those crossings still factor into this construction.
Branches retain plural `K_gamma`; a loop may retain holonomy even when one displayed endpoint
recurs.

The minimal exact algebra is graded and cellular. Let `A_H` be the finite population of locally
live directions which actual FOUNDs have established. For each oriented subset `J` of those
directions, `e_J` is its blade. A presently carried multivector is sparse:

```text
M = sum_(J in live support) m_J e_J.
```

No `2^|A_H|` basis population is materialized. RIDE transports on existing support. FOUND extends
the local basis and gives the Different term a new oriented support. The wedge face records
independence without needing a metric; where a receiver supplies a quadratic form, contraction
and wedge together give the corresponding Clifford/geometric product. The current complex rotor
is exactly the special one-plane quotient in which one unit bivector plays `i`.

Algebra alone does not replace topology. At each live grain the carrier also retains a sparse
oriented cellular chain and its boundary:

```text
C_k(H),                  partial_k : C_k -> C_(k-1),
partial_(k-1) partial_k = 0,
omega(e) = (chi_e, winding_e, blade_support_e),
Gamma_H = the exposed boundary and open residual.
```

`omega` is formed by the receiver's actual Swing; it is not copied from a source label. Source
slots order and orient construction while the event is borrowed. On commit they have become local
boundary incidence and path order; their source identifiers disappear. The carried pin is the
formed, oriented boundary relation with its `chi` transport—not the old endpoint row.

The refined live constituent is therefore

```text
H = (A_H, C_*(H), partial_H, omega_H, {K_gamma}, Gamma_H).
```

The cellular term distinguishes a branch, shared subpath, and hole. The graded transport term
distinguishes axes and noncommuting order. Neither can lawfully substitute for the other.

Completion is therefore

```text
complete_k(H_c) -> h_(k+1),
```

where `h_(k+1)` is one live composite constituent. Internally opposed boundary faces cancel by
orientation, leaving the actual outer boundary; independent closed transport remains as holonomy.
Its active axes, pins, cycles, and exposed boundary remain. A lower cell departs exactly when it is
not used by the exposed boundary, an open residual, an independent cycle/holonomy, or another live
higher constituent. Testimony may remember the old source rows, but the live carrier does not.

This is the point/line/loop perspective made operational. To an outside cut, `h_(k+1)` can be one
point. Along one continuation it is a line or arc. Across its full active incidence it is a loop,
sheet, branch, or patch. Those are restrictions of one holon, not mutually exclusive stored types.

## Compression and pins

Compression does not mean forcing `H_c` into a fixed-size record. The event itself emits the
smallest topology which still changes lawful continuation:

- recurrent compatible contributions at one active position integrate once;
- genuinely opposed contributions annihilate only where the same oriented cell and blade fiber
  compose to zero;
- completed interior factors depart when no continuing path depends on them;
- shared subpaths stand once and may factor into several branches; and
- a pin remains exactly while removing it would identify noncommuting paths which the receiver can
  still distinguish.

The size of the active body is a consequence of that factorization, never the definition of
compression. A unique phrase may remain a comparatively large composite. Recurrence can later
make much of it shared terrain without an installed deduplicator.

## Consequence for the engine

The next carrier is not a `RegionalProduct` record beside Soma and not a `weave` adapter. It is a
generalization of the existing live lineage body so a completed constituent may carry:

```text
active chi-founded cells and locally founded blade support
+ their still-consequential oriented boundary pins/branching transport
+ exposed boundary
+ ordinary K continuation
```

through receive, completion, successor integration, departure, and exact rest. Source arcs remain
borrowed event material. The body must derive the active pins through actual Swing and discard the
source rows before commit. The executor may realize physical fibers only; it may not reconstruct
the composite from returned testimony.

## Further derivation and build order

The next bounded phase must be derived before another implementation campaign:

1. **Carrier value seam.** Replace `Node` as the universal hand-up with a carrier-local
   `LiveConstituent` whose point `Node` is only a lawful projection. The owned host carrier holds
   sparse cells, boundary incidence, blade support, path transport, and exposed residual. The
   fixed/card scalar row refuses this species until its native layout exists.
2. **Pin formation.** Let each actual local `chi` RIDE an existing support or FOUND a new local
   direction. Compile source slots into oriented cellular adjacency and path order; retain no
   source IDs, endpoints, or event rows after commit.
3. **Completion/departure.** Cancel internal opposed faces, retain outer boundary and independent
   holonomy, hand one carrier handle upward, and release every lower cell which no longer factors
   into exposed/open/shared continuation.
4. **Standing integration.** Extend the standing successor with the same sparse cellular and blade
   fibers. A completed pin must become later terrain; keeping it only in a lineage snapshot would
   be an inert ledger.
5. **Atomic host embodiment.** Form every local contact against one receiver-before, let the body
   construct the composite, and commit one successor. No intermediate standing is visible.
6. **Exact rest.** Persist only the continuing composite body and live lineage capability; remount
   performs no event.
7. **Focused acceptance.** Storage order is gauge; changed noncommuting incidence is causal; exact
   recurrence is exact; hand reversal is causal when not gauge; branches remain plural; completed
   interior departs; refusal is atomic.
8. **One linguistic world run.** Only after those controls close, run the existing world once and
   read complete editor paths, returned material consequence, and rest. No predicate is weakened.
9. **Native CUDA.** Derive the card carrier only after host closure.

The immediate derivation question is:

> What is the exact sparse host representation of
> `(A_H,C_*,partial,omega,{K_gamma},Gamma_H)` which lets the existing Swing FOUND a genuinely new
> local blade, hands one live constituent upward, and integrates its exposed pins as later terrain
> without retaining the borrowed event?

## Exact local evidence

```text
cargo check -p life
  PASS

cargo test -p soma-membrane \
  regional_cell_refuses_atomically_until_a_composite_carrier_exists -- --nocapture
  PASS · 1 passed · 79 filtered out

cargo test -p life \
  plural_objective_boundary_refuses_before_the_false_product_can_commit -- --nocapture
  PASS · 1 passed · 386 filtered out
```

No broad suite, linguistic world, CUDA compiler, or card execution was run.

## Grade

**§CXV BRANDON-RATIFIED / DEPOSITED / CURRENT LIVE-COMPOSITE-CARRIER AUTHORITY / §CXIV HOST AUDIT
COMPLETE / EXACT ONE-PLANE CARRIER DEFECT LOCATED / BORROWED PLURAL REGIONAL-HAND CONTRACT BUILT /
SCALAR LINK PROTOTYPES REJECTED AND REMOVED / PRODUCTION HOST+CUDA ATOMIC REFUSAL / SPARSE GRADED
CELLULAR CARRIER INVARIANT DERIVED / HOST VALUE-SEAM EMBODIMENT OPEN / NO LINGUISTIC WORLD RUN / NO
CUDA RUN.**

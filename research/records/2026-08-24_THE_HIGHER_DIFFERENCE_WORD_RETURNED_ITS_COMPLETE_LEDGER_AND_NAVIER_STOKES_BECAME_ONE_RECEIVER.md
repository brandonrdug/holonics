# The higher-difference word returned its complete ledger and Navier--Stokes became one receiver

**Date:** 2026-08-24  
**Scope:** HD0 exterior Lean theorem station; this record schedules no Rust/GPU deed and does not
move `CONSTRUCTION_STATE.md`.  
**Truth status:** each material claim is graded locally under `canon/EPISTEMIC_GRADES.md`.

## 1. Owners composed

[definition] HD0 composes the standing ordered-word, receiver compression, finite-difference, and
chart-equivariance owners.  Its source data are a state type `X`, addressed generators
`T_i : X -> X`, a commutative coefficient ring `R`, receiver sections `X -> R`, and an ordered
generator list.  Its event is one generator occurrence meeting the current section; its returned
consequence is the complete product-rule leaf population rather than a chronology-free endpoint
formula.

[proved-derived; formal-checked] The new source owner is
`ElementaryHolonics/Foundation/HigherDifferenceTransport.lean`.  It imports no fluid or frequency
module.  `ElementaryHolonics.lean` exposes it directly, and
`NavierStokesReciprocalDifferenceRecurrence.lean` now imports it as a client.

## 2. Complete product and reciprocal ledgers

[proved-derived; formal-checked] `differenceWord_mul_eq_ledgerSum` proves the iterated shifted
Leibniz law for every finite addressed word.  A `ProductFace` retains the generator/hand trail and
the transported left and right sections.  `productLedger_retains_word` proves that every trail
retains the source address word, while `productLedger_length` proves that a word of length `n`
returns exactly `2^n` raw occurrence faces.

[proved-derived; formal-checked] The right branch is ordered first, so one recursively constructed
`principalFace` is the unique face in which no difference meets the left factor.
`productLedger_eq_principal_cons_remainder` decomposes the complete population into that face and
the retained remainder.  `differenceWord_mul_eq_principal_add_remainder` identifies its value as
`q(x) * Delta_word(r)(x)`.

[proved-derived; formal-checked] `successorWindow` recursively returns every occurrence at which a
word evaluates its source section, with multiplicity and chronology intact.  `LocalInverse`
requires `q*r=1` only there.  For every nonempty word, `localReciprocal_recurrence` proves

```text
q(x) * Delta_word(r)(x) = - ledgerSum(nonempty-left remainder)(x).
```

No global inverse is introduced at a deleted or totalized point.

## 3. Interchange, route defect, rebase, and fibre

[proved-derived; formal-checked] `InterchangeReceipt` is exact pairwise commutation of the addressed
transports.  It proves that higher-difference words related by a list permutation have equal
returns and that their full product ledgers have equal total receiver sums.  Thus chronology is
forgotten only through a supplied interchange law.

[proved-derived; formal-checked] The finite control uses `flip` and `erase` on `Bool`.  At the same
source, the two second-difference words return `-1` and `0`.  The inequality proves that no
interchange receipt exists; the noncommuting route defect is retained rather than sorted away.

[proved-derived; formal-checked] `differenceWord_rebase` proves naturality under generator-local
chart conjugacy.  `reconstructionFibre` is the complete subtype behind a declared compression
face.  Equal quotient faces place both occurrences inside that fibre, while
`separatingDifferenceWord_reopens_quotient` proves that one richer difference word forbids their
collapse.

## 4. The commuting and bounded quotients

[proved-derived; formal-checked] The formerly fluid-owned one-path algebra moved into HD0: the
shifted binomial product law and local reciprocal recurrence through order six, propagation of a
zero third difference through orders four to six, and the exact quadratic `6/15` reciprocal
formula.  The fluid module contains no duplicate generic proofs.

[proved-derived; formal-checked] A six-occurrence product ledger has sixty-four raw binary leaves.
`secondOrderAllocationReceiver` sends each leaf to the derivative order `0`, `1`, or `2` on each of
three paired axes.  `threeAxisSecondOrderAllocation_card` proves that this commuting receiver has
exactly `3^3=27` faces; the occurrence list remains available behind it.

## 5. The periodic-fluid specialization

[proved-derived; formal-checked] The fluid client supplies genuine integer-lattice coordinate
translations and proves their interchange receipt.  It defines the addressed word
`[0,0,1,1,2,2]`, proves its raw product ledger has length sixty-four, transports every leaf to the
twenty-seven-face allocation receiver, and retains multiplicity in that returned list.

[proved-derived; formal-checked] On the complete three-axis successor window away from zero,
`hodgeReciprocal_secondEachAxis_localRecurrence` specializes the generic theorem to
`q(k)=|k|^2` and `r(k)=1/|k|^2`.  The result is invariant under every permutation of the six
coordinate occurrences.  Existing exact frequency calculations return vanishing cross-axis
denominator differences, same-axis second difference two, vanishing same-axis third differences,
and the seven candidate denominator face type.

## 6. Validation and next return

[established-bounded; formal-checked] The focused foundation owner, the fluid client, and the full
`ElementaryHolonics` library build passed.  The full build returned 4,135 jobs.  Printed axiom
receipts for the promoted HD0 and fluid theorems contain no `sorryAx`.

[open] HD1 is the proof-bearing annihilator quotient.  It must retain an active-face predicate and
zero proof for every removed leaf, prove equality of complete and pruned ledger sums, and return a
norm bound with the active multiplicity.  Its first fluid client applies the seven quadratic
denominator certificates across the twenty-seven `(2,2,2)` allocations to derive the inverse-cube
mixed mass; `d^2=0`, nonabelian curvature, cusp nilpotence, and arithmetic finite-order corrections
remain other receiver-specific certificates of the same owner.

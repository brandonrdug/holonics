# THE MEMBRANE HAS ONE TYPED MOUTH

**BUILT / HOST+CUDA-GATED / RECEIVER WORLDS MEASURED**  
**2026-07-15 · `FORMULA §LVI` unchanged · no Soma interior-law change**

## Verdict

The first consolidation cut is complete at its declared membrane grain.

1. One `no_std` crate now owns REGISTER status rows, lineage descriptors, cooperative contact
   sheets, entry symbols, and exact 10/11/7/7 buffer-pair mouths.
2. `mount` owns the lifetime-bound typed CUDA spans and the four entry-specific launches. `life`
   and both independent REGISTER gates no longer assemble raw driver parameter arrays for those
   entries.
3. The committed PTX was rebuilt from the same shared declarations, assembles for `sm_89`, exposes
   all eighteen entries, and is statically gated for the four exact REGISTER parameter counts.
4. A deterministic non-authoritative record index and short `NOW.md` route current work without
   replacing Formula, Ledger, Research, evidence, or correspondence.
5. The carried-chart law now has three separately typed receiver witnesses: the exact projective
   causal atlas, the exact Smith quotient, and a literal one-port transmission line at integral
   quarter-turn electrical lengths.

## The typed boundary

The ownership cut is

```text
body                   semantic law and packed body records
  |
soma-abi               fixed substrate-neutral membrane words and entry call shapes
  |
mount/register_launch  checked CUDA spans and entry-specific driver calls
  |
life                   scheduling, sharding, worlds, observers, and persistence orchestration
```

`soma-abi` allocates nothing and owns no launch sizing, device choice, sharding, retry, world
policy, persistence version, or body transition. `mount` still owns the raw CUDA driver boundary.
The typed mouths preserve the existing pointer/element-extent ABI exactly:

| Entry | Exact buffer pairs | CUDA parameters |
|---|---:|---:|
| `scope_register` | 10 | 20 |
| `scope_register_surface` | 11 | 22 |
| `register_own_recast` | 7 | 14 |
| `register_own_recast_finish` | 7 | 14 |

Whole device buffers and checked aligned subspans are lifetime-bound through launch argument
construction. The cooperative completion/contact allocation can still expose its two actual
subspans without manufacturing separate storage. Scheduling and sharding remain where they were.

## Mechanical ownership

The same cut removed incidental file-size pressure without changing public behavior:

- `life/src/main.rs` is a three-line binary calling the reusable `life::run`; the former root is a
  library with the same module and CLI dispatch order;
- host-reference execution moved to `life/src/host_reference.rs`;
- 2,793 manifold test lines and 954 SLEEP test lines moved into sibling test modules;
- the active code and tests retain their prior public paths and exact fixtures.

This is the first extraction tranche, not a claim that every large Rust owner has been split.
Carriage, sharded CUDA orchestration, surface/persistence ownership, and the historical life root
can be narrowed further only through later mechanical cuts with the same gates.

## Receiver constructions

### Exact causal atlas

The v2 periplus receiver produces one exact abstract three-cell atlas from actual ordered local
transports and actual shared standing ports. Changed/prior siblings retain 914/251 response cells,
0/638 open transits, 36/11 lineage faces, 759/167 occupied-port faces, and 723/794 connected
components. Neither real sibling closes a route, so returned holonomy is unexposed. Smooth
immersion remains open. Exact record: `observations/circuit-atlas-01/RESULTS.md`.

### Smith quotient and literal line

The quotient-only world exactly evaluates matched, short, resistive, reactive, and active loads
and the Möbius inverse without pretending to contain a physical line. The later line world adds
exact `f`, `l`, `v`, attenuation, nonzero incident voltage, returned voltage, load/reference wave
ratios, input impedance, load recovery, and revolved quotient orbits. Six analytic lines pass every
wave and inverse gate. General phase, open-circuit infinity, multiport scattering, and the mapping
from projective periplus data to physical impedance remain open. Exact records:
`observations/smith-world-01/RESULTS.md` and
`observations/smith-line-world-01/RESULTS.md`.

## Gates

- Workspace tests: **317 passed / 3 ignored** — body 129/2, life 135, mount 20/1, `soma-abi` 3,
  record index 13, surface 17.
- Release `life` and both release REGISTER gates build.
- Rebuilt CUDA PTX SHA-256:
  `3c71511f0059048d34c0f781ee87a6a9c8862e4e29eb02c478167468e1926d31`;
  `ptxas -arch=sm_89` valid.
- Card REGISTER gate: inline, cooperative, one-move, one-row, and repeat all exact; 188 completion
  rows and the 15-worker cooperative surface match the host.
- Fresh-context remount gate: exact through SUB `1->2`, PATH `2->4`, cursor 7, final axis 4, counts
  `[0,2,3,0]`.
- The unchanged SPIR-V artifact remains Vulkan 1.2-valid at SHA-256
  `9a805973e372450a90019dfa3ee5820d0d8812b8f82dc168c510c0adb721670a`.
- Formatting, deterministic index generation/check, and repository diff checks pass after the
  deposit and routing refresh.

## Carried-swing receipt

```text
CURRENT  ratified consolidation ⊕ REGISTER membrane ⊕ exact receiver-world grain
HELD     encoded bytes ⊕ entry symbols/order ⊕ archive schemas ⊕ §LVI type separations
MEETING  shared ABI, typed launches, causal atlas, Smith quotient, and literal line world
TEST     workspace host gates ⊕ rebuilt PTX ⊕ static signatures ⊕ two CUDA gates ⊕ exact receipts
DEED     FOUND — duplicated call-shape ownership and the quotient-only Smith gap are removed
CARRY    one typed mouth and three separated receiver witnesses; broader refactoring/rendering open
GRADE    BUILT / HOST+CUDA-GATED / MEASURED
```

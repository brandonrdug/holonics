# THE EVENT EMANATES THE SUCCESSOR; THE EXECUTOR CANNOT RECONSTRUCT THE MACHINE

**Date:** 2026-07-19  
**Grade:** BRANDON-AUTHORIZED BUILD / COMPLETE FIVE-PATH AUDIT / INVALID EXECUTION SEAM REMOVED /
DIRECT HOST TRANSITION BUILT / BOUNDED CUDA LIFECYCLE MEASURED / NO FOREST RUN  
**Authority:** `FORMULA §C`, `FORMULA §CIV`, `FORMULA §CV`  
**Scope:** the production transition in `soma-membrane` and its native world-organ/CUDA mouth  
**Soma interior:** the existing `ErosBody` conduct law is unchanged

## The exact question

The implementation question was not whether another execution interface could be made coherent.
It was:

> Does the production machine enact one `Lambda` event over one live body and emanate its
> successor inside that same transition, or does it conduct first and reconstruct an allegedly
> active successor later from execution testimony?

The standing evidence was insufficient because the stopped session ended with only a compiling
five-file seam. Its final transaction edit had not been accepted, its CUDA construction did not
exist, and its public ownership boundaries contradicted the now-ratified compression law.

The sought artifact was one direct source path:

```text
native world event
    -> LiveCurrentMachine::receive_with
        -> current-local physical enactment against one standing-before surface
        -> one integrated contemporary successor
        -> live lineage/standing commit + immediate world radiation
```

The stopping condition was host acceptance plus one bounded host/card lifecycle equality cell.
No forest passage, new observer, recovery-schema campaign, or proof ladder was authorized.

## Finding

The stopped-session `ExecutionMouth` was the wrong boundary.

It made `contact_cycle` prepare an execution request, accepted a complete returned
`ExecutedPopulation`, rebuilt an `ActiveTopology` from that population, prepared a `FoldedCut`, and
then let session commit install the reconstructed successor. `LiveSession` additionally owned the
executor's `prepare -> commit | abort` transaction. The executor population was therefore allowed
to stand between conduct and active succession as a second causal owner.

That is precisely the separation prohibited by §CIV and §CV. Testimony may record a transition;
it cannot be the material from which a later layer recreates the transition's active result.

The existing `LiveCurrentMachine` already held the smaller and correct ownership boundary. It owns:

- one `SparseStandingSurface`;
- only presently live `CurrentLineage` carriers and pending dark action;
- one complete co-present event population at a call boundary; and
- the atomic integration of current-local contributions into the contemporary successor.

Its `receive_with` call validates every admitted current, captures every mount against the same
standing-before surface, enacts the currents through one physical executor, prepares the one
successor before any state becomes visible, and only then commits standing, continuing lineages,
departures, and radiation. The executor's returned contributions are active fibers of this same
call. They are not receipts, ancestry rows, a durable executed population, or an independently
committable successor.

Thus the correction was subtraction and direct connection, not replacement architecture.

## What the stopped session spent time on

The exact stopped-session record in `STATE.md` establishes the following.

1. A requested cleanup/rebuild was broadened into overlapping architecture and recovery audits.
2. Typed ecology, frontier, world-organ checkpoint, projective, Hamiltonian, and field recovery
   work accumulated even though the direct card execution edge remained open.
3. A new `ExecutionMouth` abstraction was added across five files, followed by provisional
   executor `prepare`, `commit`, and `abort` lifecycle work.
4. Host conduct was routed through that abstraction and a returned executor population was rebuilt
   into the already-existing enacted topology.
5. The session ended after `cargo check`; the final transaction edit did not receive the short
   membrane/life regression.
6. No large run and no useful new machine output resulted from those hours.

The waste was not that every recovery observation was false. The waste was letting each noticed
weakness create another active obligation, then proving local apparatus properties while the
production causal boundary remained wrong. The repeated gates did not answer the governing
question and the new adapter made that question harder to see.

This audit does not schedule a counter-campaign to disprove or delete all historical apparatus.
It removes the one invalid live authority and leaves compatibility testimony in its declared
historical role.

## Source correction

### Removed

- `membrane/src/execution.rs` was deleted. The untracked partial file had defined
  `ExecutionMouth`, its request/mount protocol, and executor population transaction.
- `membrane/src/lib.rs` no longer declares or exports that execution layer.
- `membrane/src/contact_cycle.rs` no longer accepts an injected executor, constructs an
  execution plan, or rebuilds from `ExecutedPopulation`. It is explicitly historical
  Holon/contact-cycle compatibility and observer/recovery apparatus.
- `life/src/live_session.rs` no longer owns an executor or coordinates executor
  `prepare/commit/abort` around historical ecology settlement.

Because `membrane/src/execution.rs` was never in Git, its deletion is not recoverable from Git
history. Its structure remains described in the stopped-session `STATE.md` record and in this
audit; restoring it would restore the rejected causal seam.

### Retained

- `abi::active::relation_span_node` remains the substrate-neutral ordered composition of one
  relation span. It is a law shared by host and CUDA, not a lifecycle owner.
- `LiveCurrentMachine` remains the sole production owner of live standing and current continuity.
- `HostLiveCurrentExecutor` and `CudaLiveCurrentExecutor` remain physical faces of the same atomic
  `receive_with` transition.
- Historical `ActiveCut`, Holon receipts, journals, checkpoints, and contact-cycle populations
  remain available as compatibility/observer testimony. They do not execute or reconstruct the
  production machine.

### Connected directly

`NativeRelationOrgan::present_with` now carries an actual native event through a caller-retained
`LiveCurrentExecutor` directly into `LiveCurrentMachine::receive_with`.

`StreamedOctetOrgan::present_chunk_with` carries every true octet adjacency through that same
retained executor. Chunk boundaries remain world-side storage boundaries: the organ retains only
one prior octet, and every actual adjacency emits its successor before the next event arrives.
Text/octets remain one organ species, not Soma's common representation.

## Why this realizes the compression law

For the implementation cut,

```text
E_t = Lambda_(F_t)(Q_t tensor A_t tensor L_t)
F_(t+1) = q_(t+1)(E_t).
```

`receive_with` is the complete `Lambda` call. The source organ supplies `L_t` in its actual event
order. The live machine supplies the changing frame/objective and standing analogy. Current-local
conduct returns the active fibers which factor into the one successor before commit. Radiation is
the world-facing quotient of the same event.

There is no scheduled compression pass. There is also no event archive from which standing is
later recovered. The outgoing state is already the event's compressed boundary face. Later
contact can RIDE the resulting terrain; that recurrence neither creates nor retrospectively
validates compression.

When a constituent lineage ends, the machine removes that current-local carrier in the same
commit. The standing relation it helped form is not deleted merely because its source constituent
departed. Departure, loss, and annihilation therefore remain distinct in the executable lifetime.

## Bounded acceptance

The post-correction host regression is:

```text
cargo test -q -p soma-membrane -p life --lib

life:          370 passed; 0 failed; 3 ignored
soma-membrane:  72 passed; 0 failed; 0 ignored
total:         442 passed; 0 failed; 3 ignored
```

The physical preflight found one RTX 4080 SUPER with 15,945 MiB free. The first bounded CUDA cell
returned driver error 700. One `compute-sanitizer` pass located a device stack overflow in the
actual call path:

```text
lineage_event               2,320 bytes
live_event_node_emitting    1,464 bytes
perceive_grain              3,104 bytes
complete_cast                 232 bytes
cast_position                   6 bytes
                              -----
                              7,126 bytes
```

The host had reserved only the entry function's rounded 4 KiB because
`CU_FUNC_ATTRIBUTE_LOCAL_SIZE_BYTES` does not report the full nested path. The executor now
reserves the next physical power, 8 KiB. No event law, capacity, or CUDA buffer layout changed.

The single authorized rerun then passed:

```text
cargo test -q -p life \
  live_current_cuda::tests::one_cuda_mouth_matches_host_through_found_departure_and_later_ride \
  -- --ignored --exact --nocapture

1 passed; 0 failed
```

That cell uses one retained CUDA context/module across eleven native relation events plus any
lawful resource retries. It compares complete host/card radiation, standing, live memory,
lineage cursor, channel, header, and carrier after each relevant edge; one constituent lineage
ends and departs before a genuinely later event continues the surviving lineage.

This is a bounded native live-current CUDA measurement. It is not a generalized active-cut
measurement and not forest autonomy.

## Exact current boundary

Built now:

- one direct production owner for standing and live lineages;
- atomic same-pre-state event enactment and successor integration;
- host and CUDA physical executors for native relation events;
- direct native and streamed-octet world-organ entry through a retained executor;
- constituent departure without turning observer history into active memory; and
- complete bounded host/card equality across the declared lifecycle cell.

Still physically open:

- standing and lineage carriers are uploaded per CUDA event; the context and module are resident,
  but the active body is not yet device-resident;
- `LiveCurrentMachine` has no exact rest image/remount boundary of its own;
- plural native source organs and the full world consequence-return loop have not yet been carried
  through this direct production owner; and
- act-until-natural-rest autonomous continuation remains unbuilt.

The generalized `ActiveCut`/contact-cycle path is not the route to close those openings. The next
physical work must extend the lifetime already owned by `LiveCurrentMachine`: resident body,
exact rest/remount, then ordinary returned world current. It must not reintroduce a receipt-driven
executor, dataset-sized active cut, selector, scheduler, ancestry population, semantic taxonomy,
compression service, or compatibility adapter.

## Ruling

The executor performs the substrate face of one event. It does not own a second machine.

The event's active successor is prepared and committed inside the one receiving transition.
Receipts and populations may testify afterward, but they cannot reconstruct, select, or install
that successor.

The stopped-session `ExecutionMouth` plan and its recorded “resident typed ExecutionMouth” restart
are **HISTORICAL / REJECTED / NON-OPERATIVE**. The production continuation begins only from
`LiveCurrentMachine` and the world organs that directly feed it.

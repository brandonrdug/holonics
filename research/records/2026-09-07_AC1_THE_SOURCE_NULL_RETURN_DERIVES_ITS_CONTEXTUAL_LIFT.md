# The source-null return derives its contextual lift

[definition] This discharges one algebraic part of the
[AC1 derivation task](2026-09-07_AC1_DERIVATION_IS_PART_OF_THE_GENERATIVE_ATTACHMENT.md):
which carried contextual distinction is consequential when the present source chart cannot
account for an actual returned distinction? It uses local linear relations already admitted
by the constitutive owner. It does not assume that language or all HNA conduct is globally
linear, identify a unique original cause, or itself establish a useful conversation model.

[definition] Let `V` be the coefficient space on an ordered population of actually observed
passages, over a declared field `k`. Each passage has a situated source, carried context and
actual returned current. Their column maps are

```text
S : V -> X,       C : V -> H,       Y : V -> Z.
K = ker(S).
```

The occurrence addresses supply lineage; their ordinals supply no semantic coordinates. A
vector `v in K` is a coherent combination of the actual passages invisible to this source
chart. `Y v` is its full oriented returned current. The question concerns that map, not a
count of repeated strings or a scalar discrepancy. In a complex chart, both quadratures and
the admitted scalar law must cross. The current exact reference uses rational realification
and adds no unobserved imaginary-multiple passages.

[proved-derived] A linear contextual extension can account for all these returned currents
if and only if

```text
ker(S) intersect ker(C) subset ker(Y).
```

Necessity: a passage combination annihilated by both supplied maps is also annihilated by
any map factoring through `(S,C)`. Sufficiency: define the returned map on `image(S,C)` by
`(S v,C v) |-> Y v`. Two representatives differ by a vector in both kernels, so the condition
makes this definition independent of representative. Extensions outside this observed image
form an affine map family; its free row covectors are the annihilator of that image. No
member of this family is installed as an actual model or inferred cause by this construction.

[proved-derived] Under that condition, the actual source-invisible contrasts define

```text
D : C(K) -> Y(K),       D(C v) = Y v.
N = C(K intersect ker(Y)) = ker(D).
q : H -> H/N.
```

`D` is well defined: if `C v=C w` with `v,w in K`, their difference is in `ker(S) intersect
ker(C)`, hence in `ker(Y)`. Its kernel is precisely the displayed `N`. The quotient keeps
all unobserved context directions outside `C(K)`; an unobserved direction is not declared
silent merely because it was absent from the observations.

[proved-derived] The lifted source `(S,q C)` accounts for `Y`. If `S v=0` and `q C v=0`,
then `C v` lies in `N`: choose `w in K intersect ker(Y)` with `C w=C v`. The initial condition
implies `Y(v-w)=0`, hence `Y v=0`. Thus `Y` factors through the lifted source, with the full
extension family retained. Moreover

```text
rank(q C | K) = rank(Y | K).
```

This is rank-nullity for `D`. Any contextual map `a` resolving the same discrepancy has
`ker(a C | K) subset ker(Y | K)`, so its rank on the actually evidenced contrasts is at
least this rank. This is a minimal **demonstrated contrast rank**, not a semantic capacity
limit or a claim of a globally minimal codec. The full `H/N` also retains the unobserved part.

[definition] These equations describe current receiver consequence. They do not yet license
discarding `N` from the continuing ecology. For a declared context generator `T`, descent
requires `q T=U q`, equivalently `T N subset N`. If this fails, `q T n` for `n in N` returns
the separating future current. If it holds for every admitted generator, induction preserves
each ordered word of those generators; no commutation between different generators is assumed.
A full recurrence mixing source, context and other ports owes the corresponding full joined
square. Checking a context-only action cannot establish that broader claim.

[established-bounded; source-inspected] The existing `exact_linear` owner now composes its
kernel, image and preimage operations in `contextual_factorization`. It returns either an
occurrence combination with `S v=C v=0` and `Y v != 0`, or `D`'s full extension family, the
silent context basis, a coordinate quotient, the lifted source and all compatible output maps.
The quotient coordinate basis is an elimination gauge, not a selected predecessor. Its
`descend_context_generator` method returns the exact square or the full oriented defect.
This is a common exact-algebra construction; it contains no learner, semantic classifier,
source-ID switch or text grammar.

[open] Native composition must derive this contrast relation from actual joined resident
passages and preserve its correlations and unsupported domain. The source-null section can
be formed from the existing joint constitutive relation by eliminating its source columns;
it need not enumerate arbitrary candidate causes. The current condition-family/current-contact
owners and the declared source/context charts are the integration targets. A numerical
enclosure cannot silently stand in for the exact contextual operands. The resulting actual
native standing must govern subsequent relation formation and reuse, and then AC2 must return
useful responses. The present derivation does not complete that integration or Athena-alpha.

[proved-derived] The native source relation provides the same construction without a dense
occurrence-coordinate matrix. Let `R=image(S,C,Y)` be the joint constitutive relation. Then

```text
graph(D) = { (c,y) : (0,c,y) in R },
N        = { c : (0,c,0) in R }.
```

Both statements follow directly by writing a member of `R` as `(S v,C v,Y v)`. The sufficient
context condition is exactly that the first relation has no nonzero vertical target direction.
This identifies the native integration port: the source-null section of the existing joint
relation, carrying its context domain, target directions and original return lineage. In a bound
bilinear contact, the direct source and actual condition/mixed blocks keep their existing roles;
an arbitrary flattened vector cannot select the split.

[definition] The saved-field inspection uses `S` from each actual retained two-branch source,
`C` from the paired field's outgoing and all internal currents immediately **before** the later
receiving occurrence, and `Y` from that occurrence's actual incoming current. Both quadratures
cross. The context cut is therefore available before the target arrives. An old source handle
and the receiving predecessor need not have the same epoch; both addresses are retained. This
is a contemporary relation and never rewrites the earlier producing prediction. The common
ambient chart zero-extends internal contacts not yet born at an earlier context cut; their
later existence is used only by this cold comparison, not supplied to earlier cultivation.

[proved-derived] Two observations with a common **nonzero** source have
`ker(S)=span((-1,1))`. Write their actual contrasts as `dc=c1-c0` and `dy=y1-y0`.
If `dy != 0`, the sufficient-context condition is exactly `dc != 0`. For each such actual
context the admitted partial map is `D(alpha dc)=alpha dy`; its extensions remain a family.
If the numerical contextual contrast is `dc_hat` and the retained source radii are `e0,e1`,
then `||dc-dc_hat|| <= e0+e1`. Consequently

```text
||dc_hat||² > (e0+e1)²
```

certifies the contextual lift for every actual context covered by that error statement.
No exact contextual coordinates, unique cause or selected enclosure centre are required.
The complete oriented expression `(-c0,+c1)` and both original source carriers remain part
of the return; the inequality is only its nonzero receiver. A zero common source has a larger
kernel and requires the full relation test. Failure of the strict inequality means the bounded
test is unresolved; it is not proof that the context cannot separate the passages.

[definition] This bounded construction retains a family over the still-unresolved contextual
current as well as the free map directions. It does not install one coefficient matrix, assert
that every later context lies in the learned contrast span, or identify these observations with
a universally stable class. Native operation can use the resulting generative relation within
its admitted domain while broader conditions remain open. Future-generator descent keeps its
separate obligation above.

[established-bounded; measured] Fourteen exact-linear tests pass, including five new controls
for the contextual factorization, its obstruction, retained unobserved directions, a nontrivial
phase rechart and future-generator separation. The native field control compares the bounded
pair result with the full exact lift, including two returns through the same old source and
their different available context epochs. Both quadratures of the returned contrast survive.
Five existing moment-material controls also pass after their cold pairing decoder is moved
to the common complete-source reading owner. No production kernel was changed.

[established-bounded; measured] The
[portable conversation receipt](2026-09-07_contextual_lift/conversation-return.json) records
actual receiving occurrences `12,20`, with source and available-context occurrences `11,19`,
from the 1,930-occurrence development checkpoint. Their two-branch source faces are exactly
equal, while their actual returns differ. The full exact lift has demonstrated contrast rank
one. The 146-real-coordinate context retains 145 free input covectors per output row of the
contrast map and 216 per row of the full lifted output map. These are explicit map-family
directions, not a count used to define semantic identity or capacity. No source representative
or particular output map was installed in the model.

[established-bounded; measured] The full cold check takes 368.57 seconds overall and 80
numerical section reads. The bounded pair check reaches the same one-direction conclusion in
17.45 seconds overall, including model reading/remount, with four numerical section reads.
Its numerical contextual contrast squared norm is about `31.3831`, while its retained error
radius is about `5.375e-14`; the strict squared nonzero inequality is exact in the portable
receipt. These decimals are exterior renderings. Both checks perform zero native deeds and
leave the original 1,930 occurrences unchanged. The bounded check never computes the exact
contextual coordinates. Full private reports remain under
`.local/artifacts/athena-alpha/ac1-context-lift/`; no new language response or checkpoint is
produced by this derivation.

[definition] The next AC1 construction is the resident source-null context/return section of
the existing joint relation, including its actual current/generator carrier, complete family
and original lineage. It must compose with subsequent native formation and the retained
condition contact; it cannot export a cold chosen matrix back as the learner. The bounded
pair result demonstrates why exact reversal of the entire context is not a prerequisite.
AC2's useful conversation responses and the full Athena-alpha goal remain outstanding.

# The instance founds routes and never founds statements

**Date:** 2026-08-08
**Truth status:** `established-bounded` for every measurement below; the falsification in §6 is a
`counterexample` and is the record's principal return.
**Evidence:** `implemented-exact`, `measured`. Five drivers, each run and its output read directly;
the three blocking sites in §6 verified by reading the source. `cargo test --workspace` →
**1512 passed, 0 failed, 14 ignored**, summed across 42 `test result:` lines.
**Provenance:** Brandon, direct ruling, 2026-08-08: *"proceed to the point where we can conduct
research using the holonic engine in order to emerge an instance of Eros that specializes in
mathematics and linguistics around mathematics."* Orchestrated across five agents; every figure
re-verified here by running the driver.
**Band:** 2026-08-08 · AN INSTANCE CONDITIONS, PRODUCES, READS ITSELF, RETURNS THE READING, RESTS,
DEPARTS FROM ITS SOURCE, AND RESUMES / THE RECEIVER FAMILY CANNOT SEE WHAT IT WAS BUILT TO SEE

---

## 1. What was built

Five joins between organs that already existed. **No new theory was required and none was open.**
The body — `crates/holonic-engine/src/conditioned_derivation.rs` — already stated the receiver
question in its own header: *"Can a body conditioned on linguistic material derive mathematics that
the same body, unconditioned, cannot — and can we read what it derived as a circuit?"*

| join | owner | commit |
|---|---|---|
| the corpus is real | `soma/life/examples/eros_mathematics_conditioning.rs` | `e429c22` |
| the reading returns | `crates/holonic-engine/src/returned_reading.rs` | `cec1c97` |
| the measure is situated | `crates/holonic-engine/src/situated_residual.rs` | `cec1c97` |
| the instance rests and remounts | `soma/life/src/conditioned_rest.rs`, `soma/tools/holon-plate/src/schemas/conditioned.rs` | `41c916b` |
| the research act | `crates/holonic-engine/examples/the_research_act.rs` | `430cb5c` |

## 2. The artifact

The deliverable is the returned mathematics, not a receipt. On a 103-artifact Lean deposit, with a
morphology founded by exposure to Soma's own mathematical prose, the conditioned instance returned
**90 passages** reaching `⊢ (P : Prop) (h : P) : exactCarrier P`, twelve of them at a declared
morphemic aperture — a licence carried by a founded **word** rather than a residual letter. One,
printed as the driver prints it:

```lean
namespace Soma
theorem carrier_transport_via_carry_exact_chart_carry (P : Prop) (h : P) : exactCarrier P := by
  have bridged := exact_chart_carry
end Soma
```

licensed by the stem `carry`, founded by `FORMULA.md`, `MATHEMATICAL_HOLONICS.md` and `README.md`,
bridging `formal_carry@7 ←"carry"→ exact_chart_carry@12` under ten deposited routes, themselves read
from named artifacts under `standing/output/lean-proof-production/`.

**None of the 90 is in the deposit.** Checked by content against all 103 artifacts: zero
byte-identical, zero name collisions, zero identical `(statement, recruitment multiset)`. And **90 of
90 reach a statement the deposit already reaches** — same result, new route. That is the point, and
§6 is why it is also the ceiling.

## 3. The plurality, as an exact integer

`β₀ = 1`, **`β₁ = 85`**, torsion empty. The production moved `β₁` from 37 to 85.

Two 1-cycles are exhibited **cell by cell** with boundary computed over ℤ and vanishing — one joining
a deposited route to a returned one, one joining **two routes the production itself returned** — and
their linear independence is exhibited by a cell each carries and the other does not, so `β₁ ≥ 2`
follows from the chains alone without consulting the reduction. Cross-checked by union-find over the
1-cell supports, a second implementation blind to orientation.

**The `β₁ = 0` control established more than it was asked to.** On declared control material, after
its production, two routes reach one statement and `β₁` is **still 0** — those routes share no
recruited identifier and close no loop. So:

> **`β₁` is the homological independence of routes, not a tally of them.**

That distinction was not in any prior document and it bears directly on how the roadmap uses `β₁` as
*"the independent distinct routes to one result."* The word doing the work is **independent**.

## 4. The conditioning is on the causal path, and the ablation is the proof

**The unconditioned null returns the empty population** on every query — the module's own law: a body
that has founded no stem can hold no two identifiers together, so nothing licenses a bridge. This is
the ablation shape `CLAUDE.md` §5 records as owed and it was already in the engine.

**Withholding the conditioning entirely:** all 90 passages depart, 72 circuit cells stop existing,
`β₁` 85 → 37. Every departed passage and every vanished cell is named; nothing is a count delta.

**And the finer shape — deleting one founded stem and re-querying.** Removing `carry` makes six
passages structurally absent and **reopens twenty-seven**: occurrences it had been suppressing by
maximality, each named with the stem that now licenses it, `unaccounted 0`. Removing `exact` makes
six absent and reopens twenty-one.

That reopening is the substance. A deletion that only subtracts is a filter; a deletion that
**reopens what the deleted structure was suppressing** is a morphology.

## 5. The instance rests, departs from its source, and goes on receiving

Source departure is **enforced, not narrated**: `set_current_dir` into a temp directory, then all 118
declared paths opened — 14 corpus documents, 103 artifacts, the deposit root — and every open
required to fail. **118 refused `NotFound`, 0 opened.** The remount reads exactly one absolute path:
the sealed 989 KB form at its content address.

- **Bit-identical resumption** — 22335 rendered octets both sides, over every field of every passage
  and every bridge.
- **An unasked statement** — 14 passages on a query never posed before sealing, agreeing
  artifact-for-artifact with the body still in memory.
- **Not a cache** — none of the 50 artifact names occurs anywhere in the form octets, and the wire has
  no field for a query or a derived passage.
- **The morphology survives exactly** — 1274 stems, every record equal field for field; the exactness
  is *checked* by replaying the records through `from_founded_words` and comparing, at the seal and
  again at the mount.

**And the control that makes the rest mean anything:** one further whole presented *after* the rest
moved 13 stems, grew the form to 989636 octets, and took the production on the **already-sealed**
query from 36 passages to **34** — new stems changed covers by maximality and two bridges stopped
being licensed. A body that resumed inert would have passed every other check.

> That is a returned difference changing later conduct **by changing structure**, on a body that had
> already rested and departed from its source. `CLAUDE.md` §13 rule 1 asks for exactly the three:
> structural change in reusable morphology, source-detached remount, and an ablation that removes the
> claimed later conduct by removing structure. All three are now measured **on the
> conditioned-derivation morphology**. They remain owed on the `soma/life` training owners.

> **SUPERSEDED IN PART, 2026-08-08, THE SAME DAY.** §6's falsification is correct **about
> `conditioned_derivation::derive`** and false as a claim about the engine. A fourth species —
> `statement_grammar.rs` + `statement_composition.rs`, driven by
> `examples/the_statement_is_founded.rs` — founds **47 statements absent from
> `standing_statements()`**, and `route_movement().founded_statements()` returns exactly those 47.
> The three blocking sites are untouched and remain correct for what they gate. Read §6 as *what
> `derive` cannot do and why*, which is still exact and still the reason a fourth species was needed.
>
> **Also regraded:** §8's *"explicitly not circulation, because this deposit exhausts in three
> turns"* is **unsupported**. No current was measured, and `q_{k+1} − q_k = 0` with `r = 0` gives
> `j ∈ ker B`, not `j = 0`; `kelvin.rs` shows unchanged standing and nonzero circulation are
> distinct. The verdict is `unmeasured`. And §3's `β₁` slogan is corrected in
> `CONSTRUCTION_STATE.md`: bare `β₁` is the cycle rank `E − V + C`; the route-specific quantity is
> the founded-minus-withheld **increment**, and it agrees with lineage route excess only when the
> recruitment graph was already connected.

## 6. THE FALSIFICATION — the production founds routes and never founds statements

**Question:** can the instance reach a statement absent from `standing_statements()`?
**Answer: no, and the refusal is structural rather than incidental.**

Determined by four constructions, not by argument:

- five absent statements, each written in the deposit's own vocabulary out of identifiers the deposit
  recruits, all return the **empty population**;
- the complete query aperture emits exactly the standing statement set — *"the two populations are
  equal: true"*;
- **no new statement 0-cell** appears in the circuit;
- and a **remount** of the production as a fresh deposit reaches nothing the original did not.

Three sites block it, each a different kind of block, all read in source:

| site | block |
|---|---|
| `conditioned_derivation.rs:992` | `if route.statement != query.statement { continue; }` — a query naming a statement no deposited route reaches matches no route and licenses no bridge |
| `compose_passage` | writes the queried statement **verbatim**; its one degree of freedom is `brought`, which is a **recruitment**, not a statement |
| `conditioned_derivation.rs:1318` | typed refusal `EmittedPassageMissedItsStatement` — even a composer that drifted could not get a new statement into the population |

**And the organ built to see a founded statement returns nothing.**
`route_movement().founded_statements()` returns the empty population on every query in the complete
aperture while returning founded **routes** on the same queries. Per `CLAUDE.md` §8: *a deed that
proves its own receiver family cannot see what it was built to see has returned real evidence and
passes its grade.*

**What would be needed**, named precisely rather than gestured at: a fourth move species beside
`derivation_skein`'s `Deposit`, `RecruitmentExchange` and `LemmaSplit` — **all three of which preserve
`statement` by construction.** It needs two things this body does not have:

1. **A statement grammar.** `read_derivation` is deliberately shallow — it reads what a file
   declares, not what it means — so a statement is an opaque normalized string with no operation on
   it. Composing `exactCarrier (exactCarrier P)` from `exactCarrier P` requires knowing that
   `exactCarrier` is applied to an argument, which is a semantics claim the export codec deliberately
   refuses to make.
2. **A population-valued admission rule** for the composed statement, with retained obstructions —
   not a filter. The conditioning half is already reusable: the morphemic bridge supplies the licence
   mechanism and `FoundedMorphology::cover` already decomposes a word the morphology has never seen.

**The reading side is built. The production side is not.** That is one named organ, and it is the
line's next movement.

## 7. Two further returns, neither asked for

**The echo and the null are indistinguishable at the emission and distinct in construction.**
`the_measure_is_situated` found that an echo body — genuinely conditioned, but covering nothing on the
deposit — and the unconditioned null have **identical emissions and identical one-body reads**, while
their constructions differ by a whole founded morphology. That is receiver non-reconstruction
exhibited in this body's own material, and it is why an emission's measure may never be read as a
verdict on the body that emitted it.

**Two bodies on one material cannot be a Mayer–Vietoris cover, and this is a theorem.** The only
complex holding both emissions is the larger body's, in which the reference's is **nested**, so
exactness forces the connecting map to vanish — measured `obstruction [0, 0]`, `exhibits false` —
while standing-against-derived, the cover `gluing.rs` is actually good for here, gives `[0, 7]` and
exhibits. Not an implementation gap.

## 8. What this record does not claim

- It does not claim RH, Hodge, or movement on any Millennium problem. Nothing here is pulled back
  from a completed zeta relation and no resemblance is treated as a route.
- It does not call the returned edge **learning**, **consequence**, or **comprehension**
  (`CLAUDE.md` §6). The return join's own measurement names its cut: **leak → accumulation, settling
  to rest**, explicitly *not* circulation, because sustained `j ≠ 0` would need the return to keep
  opening material and this deposit exhausts in three turns.
- It does not claim the `soma/life` **training** owners have any of this. The measurements are on the
  conditioned-derivation morphology.
- The morphemic aperture in §2 is **part of the question**, not applied to an answer: the full
  90-passage population is retained, named, checked against the deposit, and used in the ablation,
  and declaring the excluded stems recovers every one.

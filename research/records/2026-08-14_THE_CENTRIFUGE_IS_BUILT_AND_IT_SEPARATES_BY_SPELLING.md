# The centrifuge is built, it runs on mathematics, and it separates by spelling

**Date:** 2026-08-14
**Genre:** research record
**Truth status:** `measured` throughout; every census re-run this day rather than quoted.
**Occasion:** Brandon directed that the machine be pointed at rigorous mathematics, framed as
**"Mathematics into the Information Centrifuge"** — with the naming load-bearing, because *"if you
keep referring to 'Lean/mathlib' you're going to confuse yourself into thinking that we care
specifically about Lean & mathlib, when they are just the encoded mediums that contain the
information in which we want to decompose."* Two sweeps were dispatched to design the plan against
the complete arsenal rather than from a blank page.

---

## 0. The finding that reorders the plan

**The centrifuge is already built and it already runs on mathematics.**
`soma/life/examples/the_material_hands_over_its_own_atlas.rs`, run to completion today, takes one
identical conduct reading through one function over four codecs and returns **4 distinct conduct
shapes among 4 materials**. On the full Lean corpus:

```text
LEAN   8,625 containers → 226,914 declarations · 694,402 recruitment edges · 80,454 OPEN
       K 226,412 · ∂ 694,402 · cyclic ∂ 1,289 · cores 225,747 · ranks 82
       sites 474,741 · closed boundaries 233,093 · faithfulness YES
RUST  29,482 sections → 19,470 items · 87,033 call edges
ARITH    720 constructions → 2,160 nodes · 1,872 contacts
PROSE  2,048 sections → 8,843 patches · 25,284 contacts
```

Emitted successors carry **exact rational holonomy** with `CURVED`/flat and a chain gauge of ±1 per
closed boundary. Reading all of mathlib costs **2.82 seconds** for 232,037 declarations — the reader
is not the wall.

**And the driver states its own defect in its own output:**

> *"the TRANSPORT moved at 11 of 13 closed boundaries; flat 0 → 0 … **holonomy is still read off the
> constituent's OCTETS** … the transport does not yet consult `ContactSpecies`, and `2+2` against
> `2*2` is separated by the operator's SPELLING and not by `∂`."*

### The falsifier is written, it runs, and it currently fails

A declared **bijection** on constituent names — every surface replaced by `q<index>` — was applied:

```text
  constituents 100 → 100    contacts 79 → 79    closed boundaries 13 → 13
  the structure HELD — ∂, o and ⪯ came from the material
  the TRANSPORT moved at 11 of 13 closed boundaries
```

**The incidence is the material's. The transport is not.**
`soma/life/src/incidence_production.rs:2859` `contact_winding(from,to)` is
`popcount(tail_byte ⊕ head_byte)`; `:2873` `sheet_of` is the parity of the head byte's popcount; and
`PhaseChart::contact_transport:518` takes only `(winding, sheet)`.

**The composition was attempted end to end and returns exactly one absent type, and it is a wire.**
`incidence_production.rs:347` `struct Bond` already carries `:361`
`contact_faces: BTreeSet<DeclaredContactFace>`, and `material_incidence::ContactSpecies:102` already
distinguishes `Recruits` from `Conducts` from `OperandOrder` — **retained on the bond as exterior
lineage that decides nothing.** There is no path from `Bond.contact_faces` into
`PhaseChart::contact_transport`. Not an organ; one wire between two standing types.

---

## 1. Two corrections to the assistant's framing, both measured

### "Spin harder and the strata sharpen" is refuted by a theorem already in the tree

`crates/holonic-engine/src/token_invariance.rs:145-153`: **`windows > 1 && blocks == 1` is
unsatisfiable**, for every surface, at every horizon, on every material — a deeper horizon only
separates more (`:1987-1990`, the window at horizon `h` is a prefix of the window at `h+1`). So
applying more field never produces layers; it drives every item to a singleton, and **the collapse
can only come from COARSENING the receiver family.**

**The stratum is therefore not what survives the transport.** It is the **maximal sub-family of
receivers under which the item's occurrences still fail to separate** — `constant_axes`, with the
collapsing families forming a down-set with a single maximal element (`:160-174`). The verdict object
is `ConductInvariance`, whose three non-vacuity properties are the design (`:176-188`):
`separations_withstood` forced non-zero, and the collapsing family forced to be a **proper non-empty**
subset. *"Separated / not separated"* is not a stratification with content; *"these axes your material
holds constant, these it varies"* is.

**And the aperture is already derived rather than declared** — `surface_horizon_bound:2035` reads each
item's ceiling off its own sites, with the saturation horizon binary-searchable at `:2002-2008` and
`:1973-1975` stating that a horizon may never be authored.

### The one-shot partition is already three-fifths discrete, before any transport

Built on the real mathlib reading using only the reader's numeric fields:

```text
(former, ns_depth, recruited, tactics, steps, locals, stmt_len)
   232,037 items → 163,459 blocks, 133,597 SINGLETONS   (raw integers)
   drop stmt_len            →  62,363 blocks,  44,589 singletons
   log2-band every count    →   7,354 blocks,   2,524 singletons
```

`compress` on the raw family returns `rounds = 0`, `collapsed = []`, `is_exact() == true` — **a plan
reporting "the centrifuge separated the material" would have reported the identity partition.** The
tree already carries the fix: `token_invariance`'s `Weight` is a length **band** and `Density` is
`floor(log₂ N)` (`:41-46`). **Band everything, and derive the banding from the material.**

---

## 2. What is founded over mathematical material — six transports, measured

| # | transport | owner | preserves |
|---|---|---|---|
| 1 | boundary `∂`, `∂∂ = 0` | `derivation_atlas::found_circuit:1012` | Betti and invariant factors under unimodular rebase |
| 2 | the reach-orientation flip | `derivation_atlas.rs:635` | **homology exactly (16/16, 39/39); destroys transport (24 traversal failures)** — the phase-object theorem in the machine |
| 3 | exact receiver-local current | `derivation_capacitance::route:704`, direction **read off `∂`'s sign** at `orient_passages:501` | minimal-route population as `BigUint` without enumeration |
| 4 | 1-cochain transport + discrete Stokes | `derivation_integral::potential_over:1008` | `[w] ∈ H¹` — tree-independent, returned through **three disjoint carriers** |
| 5 | 2-cell filling | `derivation_two_cells::fill_routes:1054` | `∂(square) = 0`; β₁ moves by exactly `rank(∂₂)`, computed twice by disjoint code |
| 6 | Nerode / Moore refinement | `receiver_exact_compression::compress:246`, `compress_on_device:295` | the collapsed population, each pair with its shortest distinguishing word |

**Live, and the first time either has been run on mathlib:** `the_route_passes_through_the_theorem` on
`Mathlib/Computability` — 1,347 declarations, 3,588 declared-to-declared edges, 14,032 joined pairs,
**route lengths out to 10 steps**. Theorem chaining on real mathematics. The organ scales; the
driver's all-pairs loop does not.

**The four method-atlas transports built the same day** — `elementary_chart`, `hermite_reduction`,
`hypergeometric_closure`, `exponentiated_ratio` — act on *constructed* symbolic objects and **nothing
carries a mathlib statement into an object they can act on.** That is the joint, not a missing organ.

---

## 3. The receiver families, measured on mathlib, with their restatement risk

Run stamp 2026-08-14, `lean_development` over 7,516 files, 2,051,174 lines, **232,037 declarations**.

| family | measured | restatement risk |
|---|---|---|
| **former** | 9 values; `theorem` 122,193 · `lemma` 48,520 · `def` 30,066 · `instance` 24,755 | **highest — the preimage of a keyword** |
| **namespace** | **3,477 distinct**; depths 46,111 / 144,899 / 35,186 / 5,841 | **highest — the preimage of the directory tree** |
| **statement grammar** | non-empty on all; `statement_grammar.rs:21-23` **knows no Lean noun** | **lowest** |
| **recruitment** | non-empty on 231,761 of 232,037; 249,037 surfaces | **contaminated** — see below |
| **tactics** | **zero on 140,033 (60.35%)**; 5,233 distinct, clean and open-vocabulary | spelling only |
| **internal chain** | **zero on 205,835 (88.71%)**; 97,696 steps | lowest, and nearly empty |
| **line** | — | **forbidden** — `derivation_atlas.rs:44-50` convicts file layout as the only torsion |

**The recruitment family is contaminated and it is measured.** Top term-position surfaces:
`_` 158,691 · `simp` 72,125 · `rfl` 35,192 · `only` 32,427 · `rw` 11,204. That is **over 10% of
2,619,362 term occurrences that are not mathematical objects** — placeholders and tactics in the term
column. The tactic column itself is clean: `rw` 67,623 · `simp` 43,605 · `exact` 43,228.

**And the wrong reader must not be used.** `derivation_atlas.rs:62-65` declares *"`have` is the only
binding tactic this reading knows"*, while mathlib carries `obtain` 15,264, `intro` 13,017,
`rcases` 7,872, `rintro` 7,293 — every bound hypothesis name would enter the recruitment multiset.
`lean_development` has `BINDING_TACTICS` (10 members) and is the correct reader.

---

## 4. The hard external constraint

**Mathlib source is in the tree; mathlib is not compiled.** `find … -name '*.olean'` returns **2**.
So `lake env lean` has an environment only for `soma/formal/kernel-witness`, Lean core alone. Any plan
wanting the kernel in the loop **on real mathematics** must first build mathlib, and two operational
hazards are already recorded: `lake env lean` inside `elementary-holonics` attempts network access,
and Lean 4.27 places oleans one directory deeper than the obvious guess — a mistake that cost two days
once.

**And the two-theorem deed has never been run by either body.** `runs/eros-two-theorem-deed/` in the
laboratory is an **empty directory** and its handoff says so in bold. The frozen body's `prove()` API
was deleted; this body's three-step deed protocol with `validates_formal_return_face` is strictly
stronger, and `receive_kernel_deed_returns` has **eleven test sites and zero drivers.**

---

## 5. What the Millennium diagnostic honestly is

**P vs NP is the strongest row, because the owed object is already half-built.**
`canon/TABLET_THE_CHART.md` §3.7 states the debt exactly: *"the machine currently has no organ that
returns 'is this solvable, and at what cost' as a typed answer."* **The three method-atlas entries
built today are that organ** — each decides membership in a declared chart by a recognition condition
read off the object, with an a-priori-bounded candidate space and a typed obstruction. Report it as
engineering evidence for the aperture discipline, exactly as the frame already bounds it, and never
as a claim about complexity classes.

**Hodge is the only row with a live consuming organ** — `supported_realizers::induced_placement:330`
consumes a realizer×class incidence and returns an inertia, with `receiver_exact_compression`
supplying the classes. What it would tell you is **a fact about the atlas, not the conjecture**: which
receiver-distinguishable classes of mathematical conduct no move reaches. Two bounds bind: the
demanded positive form — one whose positivity **can** fail — is still open, and the Millennium
statement is *rational*, so `ReachableOnlyInMultiple` may never be reported as a Millennium-Hodge
obstruction.

**Four rows return nothing and their preconditions are named.** RH lands on the completed zeta
relation, which no corpus atlas touches — an atlas return there is resemblance and nothing else.
Navier–Stokes has no joint. Yang–Mills needs **reflection positivity**, measured zero, plus a
spacing/volume/correlation scaling family the construction carries none of. BSD has no Mordell–Weil
and no analytic rank, and its substantive treatment is unreachable through a vendoring defect.

> **The deliverable is the method atlas, and it needs no Millennium row to be worth building.** A
> centrifuge that recovered even one **recognition condition** from material — the way genus decides
> the whole substitution row, or a simple pole decides elementarity — rather than from a person, is a
> first-class return. Naming a Millennium problem beside it adds nothing and costs the ceiling.

---

## 7. THE PLAN, RUN. All four steps, with what each cost

### Step 1 — the wire. The falsifier moved from 11 of 13 to **0 of 13**

`Bond` gained `species_winding` and `species_sheet`, stamped in `assemble` — the one funnel every
bond passes through and the only place the canonical species population is known.
`PhaseChart::contact_transport` consults them; the octet `contact_winding`/`sheet` survive beside
them as lineage. `contact_faces` said of itself *"It participates in no constitutive decision"*; it
does now.

- **magnitude** — the bond's rank in the complex's own canonical population of distinct declared
  species sets. Relational, one-based, invariant under renaming because a source atlas declares a
  face without consulting a name.
- **sense** — `⪯` read as a sense rather than an order: `+1` when the arrival does not precede the
  departure. **This keeps flat loops reachable by construction**, since a cycle must contain at
  least one contact against the causal order or it would not close.

An earlier form took the parity of the arrival's rank alone — local, invariant, species-independent,
and it made the flat-boundary test fail because both endpoints shared a parity and every turn ran one
way. *A chart whose generators all turn one way has no flat loops*, which the module already refuses.

**Also repaired:** `with_reversed_bond` still read `surface.as_bytes()` where the other two
construction sites moved to `.octets` on 2026-08-13. One site was missed, so a reversed bond carried
a winding taken over a UTF-8 encoding of the constituent's identity — the exact defect `Site::octets`
records as fixed.

**And the four-codec falsifier still returns four distinct shapes**, readings unchanged at 98, 295,
230 and 390 per mille.

### What Step 1 cost, stated rather than hidden

**Three fixtures were re-founded because their flatness was spelling, and each said so in its own
comment.** `ab cc ab` was flat because `popcount('c')` is even and `popcount('a')` odd; the shared
material because `popcount('s'⊕'t') = popcount('e'⊕'b')` and the sheets opposed. Under a reading that
refuses spelling those sites are **isomorphic** — same rank, same degree, same single species — so
nothing rename-invariant can separate them.

They now use `structurally_flat_material`: four contacts, two sources, two sinks, one fundamental
cycle, a traversal crossing two of them against its own hand — `+1 −1 +1 −1`. Measured: 4 sites,
4 bonds, 1 compound, residual 0, flat. **And the test re-runs it under a bijection and requires the
flatness to survive.**

**Arithmetic's closed boundaries now all return one turn.** Measured: `6-2`, `8/2`, `2^2`, `8/2/1`
and `9-2-3` every one returns `(5/13, −12/13)`. That is correct — every closed boundary in arithmetic
is the **same triangle**, two operands and one operand-order, and nesting only makes more identical
triangles. The variety the old assertion demanded came from `popcount('-')`, `popcount('/')` and
`popcount('^')` differing. So the anti-tautology check moved to the level where the reading **can**
vary: the species winding must take more than one value across the family, and the loop sums coincide
because the loops are isomorphic — a statement about arithmetic rather than about the reading.

### Steps 2 and 3 — banding, and families chosen against the authored partition

`crates/holonic-engine/examples/the_material_stratifies_under_a_declared_field.rs`. Every count
enters through `density_band` — `floor(log₂ n)` as a bit length, exact. Forbidden: `namespace_path`,
`former`, `line`. Contaminated and stripped **with the count reported**, because a cleaning that is
not counted is a declaration. Used: recruitment breadth, conduct breadth, chain depth, and the
statement's own bracket shape.

Measured on `soma/formal`, 66 declarations:

```text
  the material        one-shot 30  conduct 48  singletons 40  rounds 2  collapsed  98  order 2
  the rewiring null   one-shot 30  conduct 49  singletons 40  rounds 2  collapsed 104  order 2
  word lengths        material {1: 97, 2: 1}      null {1: 100, 2: 4}
```

The reading differs from a degree-preserving null, so the separation is not carried by the degree
sequence alone — **and the margin is thin**, 48 against 49 blocks on 66 declarations. Every declared
axis earns its place: withholding one moves the block count to 38, 47, 48 and 40 respectively, so the
family is four receivers and not one wearing four names. `ChainDepth` is weakest, moving the block
count by zero and the collapsed population by six.

**Two bounds.** The strata are 40 singletons of 48 blocks, which is the monotonicity theorem doing
what it must — conduct refines toward discreteness, so the object with content is the one-shot blocks
and the collapsed population, not the final partition. And `compress` is `items^2.7` with 181,593
declarations measured not returning in 300 s, so mathlib scale needs the resident path.

### Step 4 — the two-theorem deed, run for the first time by either body

`soma/life/examples/the_second_theorem_is_reachable_only_after_the_return.rs`.

**And the reason it had zero drivers was not neglect: the protocol had no public entry.**
`select_and_open_kernel_deed` and `LeanDiagnosisCurrentFace::from_closed_current` were both
`pub(crate)`, so no driver could open a deed to hand back. Both are now `pub`; the selection
discipline is untouched, and it is what makes widening safe — a caller supplies alternatives and
caused witnesses and **cannot select the surviving target or manufacture candidate membership**.

With `lake env lean` in the loop:

```text
  before      carry_thrice -> NoLocalDeclarations.  Unreachable.
  the deed    17 candidates;  KERNEL ADMITTED 9,  OBSTRUCTED 8 with verbatim errors
  the return  generation 2 -> 3,  organs 2 -> 3,  declaration admitted
  after       REACHED 1 declaration, 8 candidates, among them `exact carry_twice h`
  ablation    organ removed -> NoLocalDeclarations again
  remount     source-detached, exact
```

**A genuine exterior verdict changed what a later construction could reach, and removing the
structure removed the reach.** The ablation is what makes it a return rather than a coincidence.

The corpus is `kernel-witness` — Lean core only, the one formal project here a kernel can check
**offline**. **Mathlib is not compiled: two `.olean` files in the whole tree.** So this is a kernel
verdict on a carrier and not on real mathematics, and the driver prints that bound itself.

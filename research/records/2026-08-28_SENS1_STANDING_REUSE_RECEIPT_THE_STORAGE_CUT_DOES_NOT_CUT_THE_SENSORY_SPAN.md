# SENS1 standing-reuse receipt — the storage cut does not cut the sensory span

**Truth status:** [definition]

## Requested consequence

The same exact acoustic or optical occurrence must cross Athena's public MEM6 mouth with the same
native consequence and complete reconstruction fibre whether its exterior carriage arrives whole,
one octet at a time, at source-frame boundaries, under arbitrary rechunking, or across callback
boundaries. A missing or repeated/reordered interval must instead return the first exact addressed
separator.

## Standing body and source owners

- **Continuing body:** the one `GranularReturnedAffineAthenaRest` admitted by MEM6 and owned during
  crossing by `AthenaCausalMembrane`.
- **Rested identity:** derived only by restoring the admitted MEM4 rest with the admitted MEM6
  granular organ; neither a fragment boundary nor source face may select it.
- **Ingress port:** `ExteriorOccurrenceTransducer::into_exterior_fibre`, followed by
  `AthenaCausalMembrane::bind_occurrence` and the public `CausalMembrane::receive_occurrence` mouth.
- **Hot conduct owner:** the existing resident membrane/current owner. `StreamedOctetOrgan` already
  proves that storage chunks preserve actual octet adjacency while retaining at most one hot
  adjacency octet.
- **Acoustic source owner:** `ExactAcousticOccurrence`, presently exact after file-backed WAV
  decoding but missing an equivalent memory-backed constructor.
- **Optical source owner:** `HierarchicalOpticalPassage::read(&[u8])`, already memory-backed and
  already retaining components, holons, incidences, alternative covers, chronology, and open
  reconstruction fibres without a productive OCR or label route.
- **Return port:** `AthenaMembraneConsequence::Returned`, carrying
  `AthenaMembraneCrossingReceipt` beside the still recoverable concrete source fibre.
- **Receiver:** equality of complete typed source objects, membrane return receipts, and native
  returned-difference sections under resegmentation; first differing addressed source coordinate
  under a real gap or reorder.

## Concrete absent relation

[project-postulate] The standing ecology has no runtime owner for the SENS0 clocked-span law: it
cannot currently accept addressed memory fragments, prove their consecutive pullback joins, or
return the first missing/repeated coordinate before reconstructing the already-admitted acoustic
or optical source object. The acoustic owner additionally cannot decode an exact WAV occurrence
from memory without introducing a filesystem boundary.

SENS1 therefore admits exactly two additive constructions:

1. one codec-neutral addressed memory-span owner whose fragment boundary is exterior carriage,
   whose join is exact source offset equality, whose success return moves (does not copy) the
   reconstructed source body, and whose failure return retains the assembly and refused fragment;
2. one memory-backed `ExactAcousticOccurrence` constructor sharing the existing PCM validation,
   frame-incidence, and section-folding law.

No capture subsystem, modality enum, utterance/image/row/batch owner, semantic index, OCR route,
transcript route, label route, scheduler, or host inference callback is authorized.

## Falsifiers fixed before construction

- Any change in source identity, native returned difference, or complete reconstruction fibre
  under lawful rechunking fails.
- A dropout or reorder accepted as consecutive fails; reporting only a final digest mismatch fails
  when an earlier exact coordinate separator is available.
- Retaining a second full source copy in Athena morphology fails.
- Letting filenames, transcripts, OCR faces, dataset labels, callback counts, or fragment sizes
  select contact or conduct fails.
- CPU semantic replay or a media-specific hot law fails.


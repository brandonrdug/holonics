# The transport does not condense, the tree falls for free, and no factor is quoted

**Date:** 2026-08-20
**Kind:** construction return for **Deed P2** of the single-card plan — receiver-exact condensation
of the native atlas under a family declared before fitting — with the named repair inside the
standing compression owner, the whole-atlas Markov reading it made takeable, the structural
condensation result (a substantive negative on the transport, a strict fall on the tree axis), the
measured lumpability verdicts (one answering an OPEN row of P0's manifest by name), the retained
remainder with a demonstrated reopening, and the additive cost vector that does not strictly fall —
**so no compression factor is quoted**, per the additive law.
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for the owner
repair and the condensed rest; **Deed P2 PASSES on this tree** on typed correctness (all seventeen
verdicts; the factor claim is separately unavailable and stated as such). Next: **P3 (cultivation)**.
`CONSTRUCTION_STATE.md` does not move (only P5 moves it).
**Authority:** the single-card plan §7.2–7.6, §8 "Deed P2", §11; `CLAUDE.md` §0k (the invariance is
additive; a figure without its decoder is the absolute-volume violation), §0l, §0m, §11 (the tree
condenses for free; the remainder is the departure from a forest); the P0/P1 records (the measured
aperture this deed's part 0 lifts; the committed equality targets).
**Owner repair:** `receiver_exact_compression.rs` (947 → 1,306): `Partition::index()` (the
item→block inverse), a **streamed Moore round** (one input at a time over one label per item — the
2,571,789,840-octet dense signature P0 named is never allocated), an optional sparse successor face
(`ObservedSystem::admitted`, default `None`, every existing impl unchanged), the partition reading
without the per-pair search, the exact separated-pair count from block sizes, and a canonical-order
bounded exhibition. Semantics identical: the owner's tests green unchanged (11 → 15), and the P0
declared scope re-read **bit-identically** (93 / 795 / 1 round / 653,599 collapsed / order 3).
**Driver:** `crates/holonic-engine/examples/the_atlas_condenses_where_the_receiver_factors.rs`.
**Artifacts:** `output/the_atlas_condenses/{receipt.form, condensed-rest.safetensors,
condensed-rest-without-hexis.safetensors, cost-vector.form}`. CPU-exact throughout; no card.

---

## 0. Verdict

**Seventeen of seventeen verdicts pass, and the deed's two headline results are a negative and a
refusal, both measured.**

- **The whole-atlas reading P0 refused is now takeable and was taken: 2.0 s** (59,698 × 5,385 at
  P0's declared family) — one-shot 107, conduct-stable **59,678**, Moore rounds 56, separated pairs
  1,294,846,845 exact. The atlas is closed under its transport, so the round count *is* the memory
  order (**56**) — the theorem checked on real material, with a closed sub-population control
  (rounds 14 = order 14, counted = exhibited at 1,660,753). The new aperture is honest: the *count*
  is exact and free; the *words* are a per-pair search the repair does not lift (projected 1.5e5 s
  for the full population; 64 taken). And a finding: P0's scope was not closed (5,396 of 5,598
  classes carry an exiting arc), which is exactly why rounds 1 and order 3 disagree there.
- **The transport does not condense.** At any family that factors R1 the native atlas is
  essentially receiver-discrete: the conduct quotient merges **14 classes of 59,698** (two plural
  blocks). That is §7.2 arriving on native material — the search is structural, and the structure
  says no. The shared block row was measured (59,698 of 59,698 agree), not assumed.
- **The tree half condenses for free — §11's trivial instance on material that is not trivial**
  (this atlas departs from a forest by 43,207 measured arcs): 219,048 ancestry pairs → the interval
  chart, remainder **empty**, that axis strictly falling 3.67× against a materialised ancestry
  relation — and honestly **trading** against the stored parent array (2.00× larger, answering in
  two comparisons what the array answers in a climb of up to 9). The additive law is what makes the
  trade visible instead of a ratio hiding it.
- **The remainder is certified and load-bearing:** 43,215 reconvergent arcs at 27,628 classes
  (equal to P0's measured values) retained whole in the condensed rest; the R5 Nerode reading
  recomputed *from the condensed rest* returns every population equal, its class-level successors
  arriving **through the retained remainder**; one collapsed pair reopened by its retained word
  (`["<"]` carrying classes 1 and 326 to receivers returning (2,2) against (4,10)) from the
  condensed rest alone.
- **Lumpability measured, not asserted, on three partitions:** the conduct quotient satisfies
  `PC = C P̄` **exactly** over Rat (with `P` declared sub-stochastic because a terminus is a genuine
  absence); the one-shot partition and the height-1 suffix-ancestor coarsening both return complete
  defects with separated within-block histories — **the second answers, by name, the row P0's
  manifest left OPEN**: the height-1 suffix ancestor is *not* a sufficient state (2,526 of 5,394
  blocks disagree; 21,620,925 pairs it holds together the declared family separates). The two
  control partitions exist so the exact result is not a check that could not fail.
- **The receiver family was declared before fitting** (printed first, verbatim, in the receipt —
  R1 plural sections · R2 remount · R3 intervention factoring · R4 the scale action · R5 the
  Nerode face · R6 cost · R7 the P1 recombination), with the condensation quotient's receivers
  declared with it and the reason standing-blindness is refused. All seven verified: R1 bit-equal
  on 8 × 5,385 triples; R2 fresh-process bit-equal (765,135 octets); **R3 the ablation diff of the
  condensed pair equals the condensation of the ablation diff, and the intervention is the same
  intervention P0 measured** (the eight-row table equal to the committed one); R4 every height
  recovered from the intervals with the parent array bit-equal — a control that found a real defect
  (touching intervals demand `exit ≤ entry` in the parent-recovery pop; written with `<` it
  returned the sibling as the parent); R5 equal populations; R6 the vector; R7 measured by tensor
  names after a first probe matched its own metadata key (a false positive, fixed).
- **Unexcited material: 54,100 of 59,698 classes** — retained in every section and in the complete
  cost, never dropped.
- **The cost vector does not strictly fall and no factor is quoted:** artifact 1,642,928 →
  2,511,460 octets; decoder 4,984 → 10,418; decoder steps 262,353 → 381,746. A ratio with a decoder
  that grew is a ratio that moved cost rather than removed it. The deed passes on typed
  correctness; the factor claim is separately unavailable and stated as such.

## 1. Tests, ledger, epoch

`cargo test -p holonic-engine --lib -- receiver_exact_compression condensation cross_chart native`
→ **45 passed** (4 new in the owner, including rounds-equals-order-on-a-closed-population and
counted-equals-exhibited); `h1_adversary` → 35 green; the driver 61.1 s CPU-exact, artifacts
inspected. One ledger row changed by hand — the owner's occurrence counts rise while the actual
resident material falls by three orders of magnitude (one label map of 59,698 entries replacing a
2.57 GB signature per round), which is the difference between counting occurrences and counting
octets, stated. Authored levels: 0 failures; the driver's apertures declared with their outsides
measured (the 64-pair word exhibition against the 1.5e5 s projection).

## 2. The station table

| deed | grade | commit |
|---|---|---|
| A–D, H0–H5, P0, P1 | PASS (frozen) | their records |
| **P2 receiver-exact condensation** | **PASS** (typed correctness; no factor quoted) | this record |
| P3 cultivation | open — next | — |
| P4–P5, M0–M3 | open | in order |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-20 05:08:52 → 05:13:35 UTC, exit 1 —
**11 of 12**; the one red was `line-citations`: the owner repair moved `distinguishing_word` from
line 143 to 214, and `CLAUDE.md` cited the line. Repaired the way the contract's own rule demands —
*cite the construct, let the line be a hint* (this citation has now drifted twice) — and the named
gate rerun alone: **PASS (0 failures, 430 citations)**. The two scopes reported separately; nothing
in code or artifact changed between the readings. Every other gate green: tests 2,628 over 30 lines
with examples type-checked; driver-catalog 267; output-manifest 65/65; architecture-lint clean at
245 files.

Deed P2 passed; proceeding to Deed P3.

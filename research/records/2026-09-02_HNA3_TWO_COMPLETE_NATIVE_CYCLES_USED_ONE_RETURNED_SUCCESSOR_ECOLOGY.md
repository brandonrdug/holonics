# HNA3 two complete native cycles used one returned successor ecology

**Date:** 2026-09-02  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `process-audit`, `measured`  
**Campaign:** HNA3 under the recurrent HNA contract.

## Return

[implemented-exact; measured] The single aligned coefficient aperture now returns the tied
`262,144 x 2,560` boundary as a move-owned population of resident tiles. The engine, not an
application driver, owns its final five operation nodes: tied contraction, exact interval scale by
`1/30`, certified `tanh`, exact interval scale by `30`, and emission. Every node returns its own
emission and trace, and the five traces join exactly from generation 1,270 through 1,275.

[counterexample; measured] The first attempted scale by `30` at scalar grain 60 returned an exact
signed-word refusal. The scale owner now descends from the established maximum and selects the
finest grain at which the unchanged interval fits; it does not truncate the scalar or continue past
the refusal. The complete five-node branch then passed.

[established-bounded; implemented-exact; measured] One complete engine cycle traversed all 1,275
native operation nodes. It returned a one-row, 262,144-coordinate final emission, exact terminal
trace joining, generation 1,275, chronology population 1,275, operation cursor zero, and a
cycle-complete successor. Reading the final resident tiled carrier through that successor returned
the same emission face.

[implemented-exact; source-inspected] `AthenaTokenApplication` is a codec-only application. It opens
`tokenizer.json` but no coefficient container, receives the already-complete native vocabulary face,
selects equal maxima at that exterior receiver, and renders the selected address. It contains no
matrix, source tensor name, source span selection, template, world verdict, or morphology update.

[established-bounded; measured] For the exterior material `The receiver`, the application supplied
two occurrence addresses. The first full cycle emitted a unique maximum at address 818, rendered as
`The`. The application appended that emitted occurrence, producing a three-address context. The
second full cycle consumed the exact generation-1,275 successor and returned generation 2,550,
chronology population 2,550, and another unique address 818 / `The` emission. A predecessor ordinal
and an independently rebuilt generation-zero ordinal were both rejected by the successor before the
second cycle.

## Grade

[established-bounded] **HNA3 PASSED.** Every mathematical node of the actual resident morphology
advances the same move-owned ecology; the final tiled boundary is native; the emission and successor
return together; a codec-only autoregressive application supplied the next ordinary occurrence;
and the second cycle used that exact successor rather than the predecessor or a rebuilt body.
HNA4 is now the sole current deed.

[counterexample; measured] HNA3 is not a qualitative language claim. Both inspected emissions were
`The`; this weak recurrence remains evidence which HNA4/HNA5 must not hide or regrade through an
expected-answer receiver.

## Reproduction

[process-audit] The returns were produced by:

```text
cargo run -q -p holonic-engine --example complete_full_native_recurrence_hna3 -- /home/b/models/gemma-4-E4B-it
cargo run -q -p athena-alpha --example full_autoregressive_recurrence_hna3 -- /home/b/models/gemma-4-E4B-it 'The receiver'
```


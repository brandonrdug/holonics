# The census drops its atomics, the quotient fuses where the receiver factors, and the mouth reads its material

**Date:** 2026-08-19
**Kind:** construction return for **Deed H3** of the single-card plan — the standing reductions
exposing their words, the census aggregated under bit-equal slot words, the receiver-dependent
fusion of the midpoint quotient, and the mouth's a-priori bound repaired to read its material —
with a lawful deviation from the contract's letter, its reason, and the controls.
**Truth status:** `established-bounded` for every measurement; `implemented-exact` for the
aggregated census (its serial predecessor kept as a permanent control), the fused seal and its
compile/read guards, and the material-read mouth bound; **Deed H3 PASSES on this tree**.
**Authority:** the single-card plan §4.5, §4.6, §8 "Deed H3", §11; the audit record §5;
[H0](2026-08-19_THE_SCALAR_PATH_RETURNS_ITS_PROFILE_THE_CARD_IS_BUSY_ONE_TENTH_OF_THE_SPAN_AND_EVERY_SECTION_KERNEL_IS_BELOW_ONE_WAVE.md),
[H1](2026-08-19_THE_PARTITION_IS_TYPED_THE_REDUCTION_IS_A_JUNCTION_AND_THE_ADVERSARY_REFUTED_EIGHT_CLAIMS_BEFORE_THE_DEED_WAS_RECORDED.md),
[H2](2026-08-19_THE_CONTRACTION_IS_TILED_THE_SCALAR_OWNER_REFEREES_AND_THE_OBSTRUCTION_IS_SHAPE_CONDITIONAL_AND_MEASURED.md).
**Position boundary:** [`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) does not move.
**Owners:** the rewritten `section_census` (warp-shuffle fold → cross-warp fold → one atomic per
block per word, none where the fold is the identity) with `section_census_serial_control` — the old
body verbatim, launched forever beside it by `census_both`; `section_midpoint_seal` and the
`SealedMidpointQuotient` law with the compile legality check and the read-time `Sealed` refusal;
the exposure and comparison machinery in `front_passage.rs`; `shape_enter`/`Enter::bound_octaves`
reading the staged words. `KERNELS` 25 → 27.
**Driver:** `crates/holonic-engine/examples/the_reductions_expose_their_words_and_the_quotient_fuses_lawfully.rs`;
artifacts `output/the_reductions_expose_their_words/{receipt.form, census-equality.tsv (261 rows), census-durations.tsv}`.

---

## 0. Verdict

**Part A — the standing block reductions expose their words.** Six exposures on layer 1's real
material, each in the H1 `ReductionReceipt` shape minus the two fields a strided leaf set cannot
carry (named where missing): pivot, capacity, the fixed word as the kernel enacts it, barrier
counts, per-level node widths from a **bit-exact i128 mirror over words read off the card**
(rms quadratic capacity: widths 104→107 against the a-priori 117; the contact's null/partition/hull
at 52/49/50 against 52/51/52), and the residuals. The replay reproduces 2,560 of 2,560 rms outputs
and 256 of 256 contact carried coordinates bit for bit. **One finding:** two of `section_contact`'s
three declared couplings are not block trees at all — the null and the partition function are
thread-0 serial left-leaning words of depth reach−1, and the hull is serial per coordinate — while
`CouplingPlan::block` reports the derived block (256). The coupling receipt over-reports the
parallelism of what it names; carried as a named hand-off (the receipt field should say the
enacting population).

**Part B — the census loses its avoidable atomics, and every slot word is bit-equal.** 260
comparisons (5 real shapes × 4 refusal words × 13 slot words), every one equal; the poisoned-lineage
and nonzero-coordinate-refusal tests green under the aggregated form; the commutativity audit is a
table — or/max/add aggregated; `reach` kept (another kernel's word); `written`/`upstream_*`
/`lineage_inspected` kept as stores, with `upstream_first` shown to be **not an atomic at all**
(every thread scans the ascending lineage serially and stores the identical least index — a
first-writer-wins would have been unlawful and is not what stands). One inherited order-dependence
reported, not hidden: a census that raises its own refusal can display octave/width faces from
threads that entered before the raise, in both forms; the occurrence refuses either way and no face
is readable. Durations (a report): the aggregated form is flat where the serial control grows —
×3.99 at 51,200 coordinates.

**Part C — the quotient fuses only where the receiver factors, and the deed deviated from the
contract's letter with the law stated.** The contract said *fuse into the producing kernel as an
epilogue*; the builder refused, because an epilogue inside the producer runs **before the
producer's census decides its refusal** — there is no grid-wide barrier inside a kernel — so the
fused path would not be bit-equal on the refusing path. What stands instead:
`SealedMidpointQuotient` fuses the quotient's collapse with the quotient's **own** census into one
in-place node placed after the predecessor's census on the graph's own edge. The §4.6 legality is
computed from the diagram at compile — the predecessor has exactly one consumer (the quotient), is
not the terminal, is not a declared face (`FrontPassage::reading`), no chained seals — and because
readability at read time is not knowable at compile, the guard stands at both ends: reading a
sealed-away face refuses by name (`Sealed`) **with its reopening route, and the route returns it**
(the unfused deed run beside it returns the same occurrence's 1,280 coordinates, 2 with a width the
quotient would have collapsed — a recompile, not a read). Measured on layer 1: 32 of 33 quotients
fuse (the one refusal is the declared layer-enclosure face, `FusionUnfactored` verbatim in the
receipt); **graph kernel nodes 138 → 106; allocations −64; section octets −6.5 MB; the terminal
face bit-equal; all 69 occurrences' censuses equal word for word; the collapsed population
unchanged** (32 sealed predecessors, width_sum 698,117 grains over 364,034 coordinates — the
retained fiber Station C's chart counts). Fusion is apparatus compression declared per occurrence;
the unfused path stays lawful; Station C's committed driver is untouched.

**Part D — the mouth reads its material.** `shape_enter`'s bound was authored from scale+grain and
never read a word; on a wide fixture (255·2⁴⁶) the authored bound sat **below** the material — the
mouth refused words it had itself been handed, unsound rather than merely over-charged — and on
real material it over-charged by 16–19 octaves. Both `shape_enter` and `Enter::bound_octaves` now
read the staged words; the H2 driver's seven-site workaround is deleted.

## 1. Tests, ledger, epoch

`cargo test -p holonic-engine --lib -- resident_law resident_section front_passage census fusion`
→ **68 passed** (five new, device-exercised, including the fused/unfused census equality and the
poisoned lineage through the seal — which corrected the builder's own expectation: the refusal
flag travels one hop at a time along the lineage and is never flattened);
`tests/h1_adversary.rs` → 35, green throughout; the driver run on the card (6.66 s) with the
artifact inspected. Ratchet: eight deltas (22 occurrences) dispositioned in the deed's report —
the diagram's consumer index and the seal alias as material relations, the `ResidentLaw` trait
shape counted on a law that stages nothing, test fixtures as returned populations, and two
`flat_map` duplicates whose named repair (`PassageReturn::reading_of`) is carried, not taken —
ledger rows changed by hand. One new authored level `CENSUS_MAX_WARPS = 32` dispositioned `ABI`
(max block 1024 / warp 32), with its `.cu` twin named as outside the ledger's `*.rs` aperture —
the standing caveat, stated. The scalar contraction, `Contract`, and every committed driver are
untouched.

## 2. What this deed does not claim and what it hands forward

No claim about the tower's wall (that is H4's); the duration table bounds kernels, does not isolate
them; ncu faces for the aggregated census unknown. Hand-offs by name: the strided-partial
certification type (still not needed by any receipt); the coupling receipt's enacting-population
field (part A's finding); `release_section` on a fused quotient implemented but unexercised (no
fusible terminal in layer 1); the inverted-material census branch unmeasurable from the CPU side
(no write path can construct `lo > hi`).

## 3. The station table

| deed | grade | decisive evidence |
|---|---|---|
| A–D, H0–H2 | PASS (frozen) | their records |
| **H3** | **PASS** | this record: 260/260 census words equal with the serial control retained forever; 32 of 33 quotients fused with terminal and censuses bit-equal and the collapsed population unchanged; the refusal controls verbatim; the mouth reads its material |
| H4 | open — next | one streamed resident tower circulation, or the exact limiting cut |
| H5, P0–P5, M0–M3 | open | in order |

**The one complete gate**, `bash tools/gates.sh`, 2026-08-19 23:48:52 → 23:53 UTC, exit 1 —
**11 of 12**; the one red was `tests`, and it was a real defect of this deed: the new `Sealed`
obstruction variant broke the exhaustive `describe` matches of four example drivers, which the
gate's example type-check caught. The `Sealed` arm was added to each (a display arm; no behavior),
all examples type-check clean, the closure manifest regenerated once, and the two named gates rerun
alone at 23:55 UTC: **`tests` PASS (2,591 passed, 0 failed over 30 result lines; example targets
type-checked), `closure-manifest` PASS**. The two scopes are reported separately; the repair
changed only the four display matches.

```text
FAIL  tests              (the Sealed variant vs four example matches; repaired; rerun alone: PASS 2591/0)
PASS  authored-levels    0 failures (CENSUS_MAX_WARPS dispositioned ABI)
PASS  named-paths · line-citations · claim-index · driver-catalog (259) · output-manifest (60)
PASS  closure-manifest   61 return directories (regenerated after the repair; rerun: PASS)
PASS  boundary-artifacts · typst · architecture-lint (241 files, 22,055 inherited) · document-law
11 passed, 1 failed: tests   →   tests, closure-manifest rerun alone: 2 passed
```

Deed H3 passed; proceeding to Deed H4.

# Coupled family emission continues through one session

[project-postulate] This implements the emission/session join following `935446ed` under the
active Athena goal. The existing conditional family and source-actuation owners supply the
native operations. It introduces no second learner or fallback to the distinct normal bank.

## The declared family receiver

[definition] Let J be the retained anchored joint family. Its existing receiver first projects
the original anchor centre onto the admitted affine anchor domain, checks the original ball,
then reads the minimum-norm joint over that anchor. For its current component c and declared
unit-basis order `(e_{π(i)})`, the symbol chart reads

```math
s_i = \operatorname{Re}\langle e_{\pi(i)},c\rangle,
\qquad i_* = \min\operatorname*{argmax}_i s_i.
```

The ordering belongs to this exterior action chart. The native kernel compares exact common-
denominator numerators and reports the selected address, rational score and projected tie count.
The full producing J stays in `NormalFamilyBasisFace`, including its original anchor and free
source directions. The selected action is not a point-current learning operand. A projected
winner supplies neither uniqueness nor robust separation over J. Reported free-coordinate flags
mean unbounded variation at fixed anchor; a false flag does not eliminate anchor-ball variation.

[established-bounded; implemented-exact; computational-witness] Two new engine controls pass.
A transported basis selects the corresponding coordinate before and after a conditional swap,
with no intermediate numerical readout; an old face retains its producing family after the
successor moves. A plural family returns projected scores `(5/2,5/2)` with both free-coordinate
flags set, selects the first declared address, and retains that full source. Selection correctness
here is a receiver result, not an inference-quality result.

## Continuing generation and delivery

[definition] `NativeCoupledWaveSession` moves one `ResidentNormalWave<NormalWaveCoupled>` together
with its symbol chart, admitted member and source receiver. `project_current` only reads it.
`next_symbol` admits/reuses an actual member contact and calls `advance_contact` before reading
the projected action. Thus unsupported conditional generation returns its native obstruction;
it cannot quietly emit from the old current or the normal material M.

[definition] The first selected symbol becomes the prefix of the actual output part. Each later
selected symbol joins its actual predecessor into a source pair and re-enters through
`actuate_contact_section`. No outside endpoint is padded and no self-emission is declared a
human observation. Local material and condition remain unchanged by this unpaired actuation.
An actual external text field must supply at least two symbols for this chart; successful input
ends the previous output part. This is the declared two-symbol source chart, not a universal
requirement on native occurrence size.

[definition] The session cursor records Ready, AwaitSelection or AwaitReentry. It records the
native generating epoch immediately after advancement, before any optional cold readout or codec
failure. Pending re-entry retains the selected action; restart recomputes the native selection
against that same producing cut and checks it. Another source actuation is refused until that
pending operation completes. Symbol re-entry publishes before the cursor returns to Ready.
Complete coupled rest, alphabet/order, member/receiver, action prefix and transport state share
`HNA-COUPLED-WAVE-SESSION` version 1. Resume rejects member/receiver overrides and validates
pending epoch/ordinal correspondence and the one-step generation/re-entry chronology. A zero
or stale emitted generation is rejected; a regression exercises this repaired ingress check.
It moves the restored body into the same session.

[definition] The existing HnaStream delivers `project-symbol`, `emit-symbol`, `actuate-text`,
inspection, checkpoint and close through `pump_coupled_wave`. `inspect-relation` observes the
complete candidate receiver of an already admitted contact without publishing it; its control
compares the complete native rest before and after observation. A failed writer retains its pending
event; opening a new connection resends that event without executing its request again. Delivery
is still framed by the existing sequence/peer contract, not an exactly-once network claim.
The workbench's `hna coupled-wave-session` binds this public owner to JSONL and final checkpoint.

[open] Producing-family/observed-symbol return remains unbound. Requests for `retain_comparison`
or `observe-symbol` explicitly refuse that missing conversion. Existing actual point-source
observation does not by itself bind an observation to a plural generating family. This return
must not be used to claim that comparison or condition development has been completed.

## Verification

[established-bounded; implemented-exact; computational-witness] Three new CUDA session controls
pass: conditional generation plus actual pair re-entry without deposition; saved selection and
re-entry phases continuing from their producing epochs; and a fresh process draining an interrupted
emission exactly once before comparing complete native rest. The session specimen is a declared
exact two-coordinate swap relation with 39 observations spanning its source/condition chart.
It exercises the public owners and delivery mechanics; it is not conversation training evidence.

[established-bounded; process-audit] Checks returned:

- `cargo test -p holonic-engine --lib coupled_basis_face -- --ignored --test-threads=1`: 2 passed.
- `cargo test -p holonics-hna --lib native::coupled_wave::tests -- --ignored --test-threads=1`: 3 passed.
- `cargo test -p holonics-hna --lib stream::tests`: 7 passed.
- `cargo test -p holonics-workbench --lib coupled_wave_session_preserves_configuration_and_resume_authority`: 1 passed.
- `cargo check -p holonics-hna`, `cargo check -p holonics-workbench`, and
  `cargo build -p holonics-workbench --bin holonics`: passed.

No Lean source or Typst artifact changed. Final session tests include the repaired saved-generation
validation and read-only candidate inspection. The bounded controls do not establish useful
conversation.

## Actual cultivated source return

[established-bounded; computational-witness] The public stream opened
`.local/athena-coupled-source-2026-09-11-actuated-66-validated` at member 0 under UnitRealSum.
This is the retained 85-symbol model following the real 199-passage source field, with 8,965
normal observations and 9,254 local observations. Pure projection selected symbol 1, the space,
with one projected maximum. It created no native occurrence. `emit-symbol` then refused during
conditional family admission (native bound flag 16). The subsequent inspect event retained epoch
11, emission ordinal zero and Ready phase. Thus the space is a current reading, not an emitted
conditional successor. It supplies no new useful-conversation result.

[established-bounded; process-audit] The stream finished all three requests and saved a
93,743,618-byte complete session at `.local/athena-coupled-stream-2026-09-11-session`, with no
stream/checkpoint error. Its request/events/process receipts are adjacent files and are copied
into [the receipt directory](2026-09-11_coupled_family_emission_receipts/). The extra native contact admitted before refusal is retained
in that rest; the unchanged epoch does not claim every metadata byte stayed unchanged.


[established-bounded; computational-witness] A fresh public process resumed that session and
issued `inspect-relation` for its retained contact 2. The candidate is **OutsideAnchorBall**,
not EmptyAffineRelation. It retains 73 anchor-independent free coordinates, but its nearest
admitted anchor differs in 170 coordinates. The [exact receiver summary](2026-09-11_coupled_family_emission_receipts/candidate-summary.json)
compares the squared distance with the unchanged squared anchor radius and confirms the strict
inequality. Full rational anchor difference and free directions remain in the receiver event;
no decimal conversion or scalar threshold chooses native conduct. This is a source-domain/anchor
mismatch at this member and condition, not a refusal of all plural families.

[established-bounded; process-audit] The resumed inspection completed, saved the 93,743,618-byte
session, and preserved byte-identical 93,742,247-byte native rest while the exterior stream sequence
advanced from 3 to 4. Epoch remains 11. This observes the same current and admitted contact,
without replaying development or generating a replacement current.

[open] The public coupled recurrence and its diagnostic receiver are now bound. The actual
model still owes the producing-family/receiver return and relevant condition-bearing source
connection. Unpaired source reflection preserved the old anchor and did not establish conditional
support. Increasing its radius, projecting its centre into a point learner, silently using M,
or calling the pure space projection a generated successor would change the declared construction.
Continue through the existing comparison, condition-contact and staged formation owners with this
retained failure; generator formation/reuse and useful Athena evaluation remain in the live order.

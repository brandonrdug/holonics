# The operating guides carry the machine and research into the consumer

**Date:** 2026-09-20. **Audited source:** `76234843`, including the further Wave 11 commits
since the [Wave 10 audit](2026-09-20_WAVE_TEN_IS_REPAIRED_AND_THE_LIBRARY_RETURNS_TO_HELICAL_GENERATORS_AND_HNN.md).
This record preserves findings and verification. [The roadmap](../../docs/plans/THE_ROADMAP.md)
owns order; [the state](../../CONSTRUCTION_STATE.md) owns current position.

## Governing request and recovery

[project-postulate] Brandon requested an authoritative audit and consolidation of the whole
repository's documentation, a useful Codex operating guide compared with Claude's new
self-contained guide, accessible research, and finalized plans/issues. The preceding direction
returns active work to shared Rust/Lean fundamentals, elementary helical geometry and HNN/Athena;
further protein/biology experiments remain deferred. This is a constructive consolidation and
repair request, not merely a list of suggested documentation changes.

[established-bounded; source-inspected] Direct Claude user messages in session
`44676d29-d81f-435b-9527-dbd903cbef67` were recovered at message lines 9, 91, 246, 308, 470,
726, 804, 962 and 1007. They explain, in order, the prior audit/plan pickup, committing the
research deposits, already-owned normalization/landmark/compression laws, documentation as the
context failure, dispatch, benchmark provenance and hardware, toroidal embedding/decoding,
geometry as implementation, and the self-contained Claude guide. The request explicitly declined
harness-version research. Repeated captures were treated as views of a source, not independent
instructions. The prior audit retains the month's Codex-message recovery and corrections.

[established-bounded; source-inspected] Repository inventory covered 8,055 tracked entries
across every root and 797 commit headings since August 20. A relative-link scan covered 1,877
Markdown files, including current docs, all research categories and frozen archives. Detailed
mathematical/source review followed the active guides, changed Wave 11 owners/callers, their
formal/reference relations and the relevant historical records. These counts describe the
retrieval aperture; they are not a claim to have re-proved every theorem or re-executed every
historical experiment. The Rubik and Siphonophore deposits are committed in `d5d7d413` and now
have subject routes; their earlier untracked state is no longer a current defect.

## Findings and repairs

| Finding | Source consequence | Consolidated return |
|---|---|---|
| Operating guidance emphasized accumulated prohibitions and gave too little usable mathematics/source practice | New tasks repeatedly reconstructed the model from a narrow example | Both [AGENTS](../../AGENTS.md) and [CLAUDE](../../CLAUDE.md) now start with the object, equations, native/formal owners, research, commands and concrete construction method. Codex uses Luna; Claude's distinct worker/reviewer arrangement is retained. |
| Useful machine description contained mathematical overclaims | Overlap became automatic friction; co-presence became commutation; exact coordinates became periodic closure; loss became dissipated energy | [THE_MACHINE](../../docs/THE_MACHINE.md), both harness guides and the live implementation plan now supply the missing incidence/material, full-effect interchange, period/commensurability and comparison/constitutive distinctions. |
| A lexical absence was treated as missing native geometry | Generic complex storage was dismissed because it did not use the words “torus” or “helix” | The actual caller is identified: supplied text-offset rows and constant condition. The next join binds geometric incidence, phase connection, participation and refinement through existing native carriers. |
| Live source maps still called returned ports absent | The next worker could reimplement #57/#58 or the exposure driver | State, roadmap, architecture map, specification, HNN README and issue bodies now distinguish returned ports from their unfinished consuming join. |
| Research entry points obscured recent work; experiment instructions misgraded entire directories | Actual native runs, exact references and research scripts were discounted by location | [Research routes](README.md) connect subjects, records, source and consumers. Research/experiment entries are rewritten; the former pages are preserved under `archive/operations/`. Papers/notebook routes and moved build commands are repaired. |
| Document law denied the separate Claude guide; worker context assumed loading | Dispatch could omit the mathematical contract | The document law names the two self-contained harness entries and shared source. The worker brief explicitly supplies the guide, equations, actual owners, records, consumer and checks. |
| Exposure driver acknowledged failed native requests and dropped outstanding comparisons | A source frame could be skipped, or its eventual response lose its producing association | Retention is staged after successful generation; pending comparison metadata survives checkpoint/reopen; retryable native failures retain the cursor. |
| Response matching relied on immediate adjacency and codec assumptions | Intervening frames, restart and Unicode/word charts could lose or mis-size the observed return | Match declared request family and capture-event aliases, retain its receiving extent, and extract targets in the declared codec's units. |

[established-bounded; source-inspected] The hardware claim also needed narrowing.
`section_normal_applied_condition_rows` and `section_bilinear_source_adjoint` loop rows in one
active thread. Normalized section reception uses one block per row with thread zero active.
Feature preparation includes per-row launches. These are actual residency/placement scopes;
the baseline's 110 s generation / 196 s update medians do not isolate a single cause, prove the
whole model occupies one lane, or establish the cost of a different joined geometric law.

[established-bounded; source-inspected] The initial review suspicion that enclosed re-entry was
point-only was rejected after reading the complete packet and positive-radius tests. Equal
`source`/`source_hi` words seal an encoded centre **and separate radius**. The kernels propagate
that radius through applied-condition and normalized returns. No domain-narrowing or duplicate
radius implementation was made. This concrete ABI distinction is now in both guides and the
worker brief. Participation-only host readout has less redundant validation than the comparison
reader, but the checked producer already constructs that face; no demonstrated runtime defect
was inferred from that difference alone.

[established-bounded; source-inspected] Actual prepared-source metadata supplied a necessary
integration control: 36,920 occurrence families include 1,886 multi-view families. Of 22,198
comparison frames, 830 have multiple captured event targets; 403 targets point to a non-first
captured event of a known request family. A first-event-only repair would therefore lose real
recorded relations. Family identity and capture-event aliases remain distinct, and neither is
inferred merely from equal text. No private message content is published here.

[definition] Shared-source observations re-prepare the retained request/receiving rows and apply
the fixed-D update at the material current when the observation arrives. Their producing epoch
is provenance, not a saved old-M gradient. The joint-region chart has its own producing D/M
retention. A future law requesting a producing-state gradient must carry its forward operands;
requiring an old material snapshot for every valid current-material observation would also be
an invented restriction. Older shared rests without exposure pairing metadata remain readable
and preserve comparisons, but cannot automatically associate an external response.

## Research navigation and historical recovery

[established-bounded; source-inspected] The new routes cover HNN/public consumers; normalization
and learning; generator/mode compression; landmarks and π/e; helical/toroidal/contact and
whole/part constructions; Swing/Hodge/fluid; force/mass/time and spectral laws; hardware; and
data/evaluation/provenance. The documents link the original derivations and formal/native source,
not just other summaries. `THE_MACHINE`, both operating guides, the research skill, development
guide, repository map and architecture map enter these routes.

[established-bounded; source-inspected] The existing `.agents/bin/prior-art` now searches code,
all research roots and archived source, groups results by root, and refuses malformed queries
instead of issuing a false zero-hit receipt. Its earlier roots omitted experiment/paper/notebook
and archive contents. It remains a retrieval tool: a hit requires reading the relation and its
actual consumer; a zero result is bounded by the displayed aperture and spellings.

[established-bounded; source-inspected] Thirty historical links across 25 records/source-guide files were repaired where a retained
relocation or immutable Git source could be verified. The initial scanner's 1,512 apparent
unresolved links were concentrated in frozen archive snapshots (1,392), with some formula
syntax misidentified as links. Old private/untracked observation, `runs/` and `output/` artifacts
cannot be made portable by pointing at a different current file. [Archive recovery](https://github.com/brandonrdug/holonics/blob/6bcf6599abeaf601f7e31f3ddbd46292abbdb145/archive/README.md#following-an-old-source-address)
now states how to use the original revision/repository; current entry points use verified local
routes. The historical orphan-binary transcript is linked to its verified public source revision.

[definition] The consolidation retains current source, historical measurements and substantive
research. It removes duplicated current-position prose, restores useful navigation and corrects
live clauses. A different directory or an old date does not decide a result's mathematical or
implementation scope. The default Lean build is `ElementaryHolonics.Framework`; the full
`ElementaryHolonics` umbrella must be selected explicitly. The prior audit and receipt
now name that actual default scope rather than claiming a full research-umbrella build.

[established-bounded; source-inspected] During integration a worker invoked `cargo fmt -- <file>`,
which formatted the workspace. The full diff and file copies were retained locally before repair.
For 376 unrelated Rust files, formatting their HEAD contents independently with the package's
edition reproduced the entire unwanted change byte for byte. Only those verified formatting
changes were removed; all intended source/document edits remained. Final checks use the recovered
combined tree. The existing explicit-file `rustfmt --config skip_children=true` command remains
the operational method; no broad restore or new cleanup ritual was introduced.

## Finalized issue and construction division

[definition; agent-inferred] The division follows actual independently returned operations:

1. **#17:** bind admitted geometric incidence/phase transport, content-dependent participation,
   whole-field refinement and complete variation through the public field/session.
2. **#61, inside that join:** certify its complete causal footprints and realize independent
   cells/features through existing partition/layout/launch owners. Preserve refinement and
   material-accumulation dependencies. It is not a separate optimization campaign required to
   finish the old text-window example first.
3. **#59:** the durable source/response boundary; **#16:** real development/evaluation episode
   assembly and the evaluation kit's assessment consumer. #57/#58 remain closed, usable operator
   dependencies. #60 is a closed dispatch receipt, with completed scope replacing stale boxes.
4. **#48:** the helical pair's existing moment-compression and phase-closure caller, beside the
   HNN join. #49 retains its qualified identity/source quotient questions.
5. **#18/#19:** normal-statistic factorization and section-scale D variation at the actual
   consuming need. #50 remains structural-library device algebra outside active HNN order;
   further biology runs remain deferred.

[definition] Milestones remain delivery views across shared machinery. Issue bodies name the
law, source, actual owner, consumer and remaining return; source maps own details and the roadmap
owns order. A port, a dispatch wave, a hardware realization and an application are distinct
closures. This prevents both neglected stale bodies and artificial product silos.

## Verification

[established-bounded; source-inspected] Original Wave 11 native port receipts are reused because
those engine/kernel owners were not changed by this audit. The new session/driver behavior is
checked in its own changed scopes; final commands and outcomes are appended below and in
[VERIFICATION_RECEIPTS](../../docs/VERIFICATION_RECEIPTS.tsv). Formal source and paper figures
were not changed, so their unchanged proof/render receipts are not replayed.

[established-bounded; measured] Final verification on the integrated source:

| Check | Result |
|---|---|
| `cargo test -p holonics-hna --lib` | 62 passed, 0 failed, 53 explicitly ignored device/other scopes |
| Field-session device regressions, serial | All nine retained tests passed: eight in the scoped run and the original source-cursor/trained-state test separately after restoring that fixture |
| `cargo test -p holonics-hna --example athena_exposure_field -- --include-ignored --test-threads=1`, under the GPU lock | Four passed, including two end-to-end CUDA regressions |
| `cargo check -p holonics-workbench --bin holonics` | Passed; unchanged engine dead-code warning |
| Prior-art helper | Bash syntax, positive multi-root query, zero-result query and malformed-pattern refusal checked |
| Documentation | All new/current local routes and incoming anchors to consolidated pages resolve; retained unavailable historical artifacts are documented separately |
| Patch integrity | `git diff --check` passed; no engine/kernel, formal or unrelated formatting changes remain |

[established-bounded; source-inspected] Initial replacement test fixtures were rejected for
inconsistent recorded timestamps; the complete driver fixture was corrected, and the original
session cursor regression was restored rather than replaced by a duplicate. The final checks
above are the resulting evidence. Device checks establish correctness under their declared
inputs, not new performance comparisons; a separate graphics application was active. Private
logs and the verified source fingerprint are retained under the local audit directory.

[definition] The remaining work is constructive: geometric participation/refinement and its
physical placement, durable wider contextual attachment, the real episode/evaluation return,
and the helical moment/phase consumer. The audit does not turn those open compositions into a
retraction of the returned operators, established mathematics or earlier native capabilities.

# THE OPEN LEADER REMAINS CURRENT; THE JUNCTION CLOSES ONLY AS ONE BOUNDARY

**Status:** BRANDON-RATIFIED / BUILT / GENERALIZED JOINT REGIONAL CLOSURE / ONE-CORE–MULTICORE–CUDA
EXACT / OPEN LEADERS + DURABLE CELLULAR REST RETAINED

## Ruling

At one bounded contemporary event, every afforded leader is an actual co-present branch. A branch
does not become unreal because another branch closes first. It remains live until it participates
in a higher-cell closure, crosses an exposed world boundary, or later compression establishes that
it no longer factors into the continuing construction. This is the first axiom applied to local
incompleteness: existing current cannot be erased by an observer's lack of a completed route.

The present direct machine evaluates independent functions of one immutable predecessor,

```text
S_(t+1) = Join_i f_i(S_t,c_i).
```

That is exact only while one current cannot alter another current's afforded continuation inside
the event. The required regional event is one bounded fixed closure,

```text
J_0 = Ingress_F(Q,A,L),
J_* = lfp_J (J union Extend_(chi,partial)(J,A)),
(A',R,O) = Emanate(A,J_*).
```

`J` is an oriented chain over the source complex with receiver-local exact carriers. It is not a
scalar state per cell. `Extend` forms contact, local rebase, branch, co-present junction, temporal
seam, and residual. During closure the active relation grows monotonically; completed-factor
departure happens only in the one outgoing emanation. No intermediate frontier becomes Standing.

## Split, junction, and boundary

The two required identities are distinct:

```text
partial_(k-1) partial_k = 0,
partial J | interior = 0.
```

The first validates a lawful higher cell. The second says that a completed internal junction has
no dangling interior boundary. Split occurs when one active face has more than one actually
afforded incident coface; every afforded coface becomes a live branch. Junction occurs only when
the locally transported oriented branches compose as the boundary of one higher cell. Coordinate
intersection is insufficient. An unmatched boundary stays exposed residual; when the mismatch
changes the available local basis it FOUNDS.

For a triangle,

```text
partial[a,b,c] = [b,c] - [a,c] + [a,b],
partial partial[a,b,c] = 0.
```

This makes the triangle the smallest junction, not a binary cell. Higher cells and irregular
source complexes obey the same law. Changing bases are local exact transport maps; Soma installs
no global metric, coefficient scalar, probability, or conserved numeric amount.

A source-declared regional cell has already composed its own boundary. Two residual arms inside
that one body are not rematched merely because their projected contacts agree; doing so would
invent incidence and can generate an unbounded sequence of false higher instants. Further
co-present closure requires another actual regional component, and temporal closure requires an
untouched Standing factor. This makes the finite fixed point advance only by component union or by
claiming genuinely participating prior terrain.

Temporal and co-present closures retain different hand. At a temporal seam, a prior outward arm
and genuinely later inward arm with equal declared hand cancel after the time-parity reversal. At
a co-present junction, opposed induced hands cancel directly. Disagreement remains oriented
residual rather than being averaged or discarded.

## Physical conduct built

The complete contemporary incidence frontier, not a sequence of independently committed lineages,
is the causal work population. Host cores evaluate independent current enactments against
immutable Standing in static chunks. Their returned regional cells then enter one canonical joint
component fixed point on the caller thread. No chunk publishes Standing; thread count and work
completion order are gauge.

CUDA forms every unique receiver-relative contact in a parallel `regional_contacts` lane field
against the same standing-before words. A separate `lineage_event` launch remains the sole mutable
carrier writer. Returned pins enter the same canonical host joint closure, so card and host return
the same residual, successor, and radiation. That fold belongs to the live machine on its caller
thread, not either executor. Logical incidence remains plural even when a repeated physical contact
calculation is reused. A single all-card global frontier reduction and device-resident cellular
body remain open physical optimizations; neither is misreported as built.

Complete native-word equality is not incidence. Every connected closure component has exactly one
event-transient commit owner even though each of its source rows radiates the complete replacement.
Two disconnected components therefore remain two Standing constituents when their bodies are
geometrically equal, and that multiplicity survives native rest/remount. This is not retained
provenance or a persistent identity: it is the cardinality of the actual co-present population.
Compression may factor a connected interior; it may not silently quotient disconnected existence
by value.

The CUDA thread stack now grows from measured scalar and higher-grain floors with the actually
mounted enclosure depth. The former fixed 16-KiB assumption caused error `700` on the first complex
event and was discarded; no event committed from that failed passage.

## Acceptance boundary

The construction is generalized over finite source-native cells, oriented incidence, ports,
chronology, local carriers, objectives, and returned consequences. A triangular circuit is only
one acceptance world. Completion requires:

1. one contact can afford another inside the same unpublished event;
2. plural branches remain real and survive rest while open;
3. co-present overlap forms one joint higher constituent instead of sequential overwrite or
   refusal;
4. `partial partial = 0` and interior-boundary cancellation remain exact;
5. completion hands up one grain and releases only lower factors which no longer participate;
6. one-core, multicore, permuted-storage, and CUDA conduct are exact;
7. audio, arithmetic, and circuit worlds require no domain law inside Soma.

This closes the bounded causal substrate, not an assertion that every application has already
grown useful topology. A source organ still owes honest native material, chronology, incidence,
and local transforms at its boundary.

## Exact built result

Three oriented triangular source complexes carry 21 cells and 27 incidences with exact
`partial(partial)=0`. Two actual co-present regional cells emanate as one Standing constituent
while two open pins remain exposed. One host thread, eight host threads, and an RTX 4080 SUPER are
exact for complete radiation, successor, lineage carriers, and durable rest/remount. CUDA forms 47
contact lanes in three contact launches; its depth-relative stack reaches 35,840 bytes in two
growths. Exact record:
`observations/eros-joint-current-junction-01/RESULTS.md`.

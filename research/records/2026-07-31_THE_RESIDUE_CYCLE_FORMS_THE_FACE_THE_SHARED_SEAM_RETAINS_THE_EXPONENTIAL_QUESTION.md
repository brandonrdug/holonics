# THE RESIDUE CYCLE FORMS THE FACE; THE SHARED SEAM RETAINS THE EXPONENTIAL QUESTION

**Date:** 2026-07-31  
**Status:** BRANDON-REQUESTED SIDE-RESEARCH DEPOSIT / CONTROLLED RENDERS RETURNED / EXACT
PROJECTION RECEIPTS INSPECTED / OPTICAL AND EXPONENTIAL QUESTIONS OPEN / DOES NOT SUPERSEDE OR
SCHEDULE THE MAIN SESSION

## 1. Why this record exists

Brandon requested side research while a separate main session remained active. The immediate
material was the arithmetic-dimensional receiver atlas under phase selection, exact Cayley turn,
and dilation. The visible meshes suggested:

- ray or wave propagation and optical caustics;
- oriented, asymmetric prime-dependent envelopes;
- circles, ellipses, hyperbolas, grids, and higher-dimensional cells appearing through projection;
- seven coarse sides and seven interior populations in the `(7,11)` phase face;
- patterned pairings among differently colored germs, reminiscent of atomic orbitals or bonding;
- a possible relation between recursive geometric collapse and the appearances of `e` and `pi`;
  and
- a possible RH-facing role for phase, dilation, rotation, prime archetypes, and receiver-visible
  morphology.

This deposit preserves the actual rendered products, exact measurements, mechanism distinctions,
and the questions that remain. It is not a claim that a picture proves RH, that projected
proximity is physical interaction, or that one numerical approximation establishes a limit law.

## 2. Existing production owners and the new consequence

The side work composes, but does not replace:

- `ArithmeticDimensionalMount`, which retains counting occurrences, prime phase axes, source
  carriers, prime-power lineage, and the current finite-place arithmetic source;
- `DimensionalReceiverAtlas`, which forms receiver-local exact projections and their convex
  envelopes without promoting terminal geometry to source incidence;
- `ExactDimensionalWaveLaw`, which carries exact complex modal currents, declared delay,
  admittance, unitary phase transport, local scattering, and exact energy balance;
- `GradedCausalComplex` and `CausalChain`, which retain arbitrary oriented incidence with
  group-completed coefficients and `boundary(boundary)=0`;
- `ExactValue::CertifiedSeries`, which retains a rational finite construction plus an explicit
  exact tail enclosure; and
- the prior exact formula ecology, which already carries bounded `e`, `pi`, formal exponential,
  Euler-product, and Zeta comparison paths without identifying a partial construction with its
  completed limit.

The new consequence is a controlled morphology atlas plus a diagnostic residue quotient. It shows
that the human-perceived coarse polygons are exact receiver quotients of finer convex envelopes,
and it isolates a lawful cellular gluing/exponential question which the present video-derived
number does not by itself close.

## 3. Controlled morphology atlas

The existing example was given an opt-in side-research sweep:

```text
HOLONIC_MORPHOLOGY_SWEEP=1
HOLONIC_SLICE_SWEEP_DIRECTORY=<separate directory>
```

Default behavior is unchanged when the variable is absent. Every sweep state now emits:

- its rendered PPM/PNG face;
- an exact projection TSV containing normalized horizontal and vertical rational coordinates and
  every member occurrence;
- an exact convex-envelope TSV containing every exact corner and its members; and
- the common atlas summary and temporal wave receipts.

The generated directory is:

```text
output/arithmetic-dimensional-receiver/morphology-atlas/
```

Primary inspection products are:

```text
pair-contact-sheet.png
dilation-contact-sheet.png
turn-contact-sheet.png
residue-contact-sheet.png
phase-07-11-residue-receipt.png
atlas-summary.tsv
```

The sweep used the same co-present source through `512`, wave tick `9`, and exact retained wave
energy `63`. It did not alter the concurrently active main-session output.

### 3.1 The contracted envelope is the smaller-prime cycle

For a phase pair `(p,q)`, traverse the exact convex envelope and contract every consecutive hull
edge whose endpoint occurrences remain in the same residue class modulo the first, dominant prime
`p`. The remaining transitions form one complete positively oriented cycle through every class of
`Z/pZ`:

| phase pair | visible germs | literal hull corners | within-`p` refinements | contracted sides |
|---|---:|---:|---:|---:|
| `(2,3)` | 504 | 11 | 9 | 2 |
| `(3,5)` | 488 | 15 | 12 | 3 |
| `(5,7)` | 464 | 16 | 11 | 5 |
| `(7,11)` | 392 | 21 | 14 | 7 |
| `(11,13)` | 344 | 21 | 10 | 11 |
| `(13,17)` | 224 | 18 | 5 | 13 |
| `(17,19)` | 152 | 21 | 4 | 17 |

For `(7,11)`, the literal envelope transitions are:

```text
3 -> 4 -> 5 -> 6 -> 0 -> 1 -> 2 -> 3  (mod 7).
```

Therefore Brandon's seven visible sides are not seven arbitrarily selected screen chords. The
exact hull has 21 corners, but its same-residue refinements contract to one seven-sided residue
cycle. This is a receiver-relative coarse face, not a claim that the complete source is a
heptagon.

### 3.2 The seven interior populations are exact residue strata

The 392 `(7,11)` projected germs partition exactly into seven populations of 56 by `n mod 7`.
Their rational centroids form the seven macroscopic cells exposed in the diagnostic residue
receipt. The centroid angular gaps, evaluated only for inspection from those exact rational
coordinates, range from approximately `42.558` degrees to `60.537` degrees; a Euclidean regular
heptagon would have `360/7 = 51.429...` degrees between centers.

The asymmetry is predominantly inherited from the rational Cayley chart. For the bare `p=7` chart,
the successive angular gaps already range from approximately `43.603` to `59.490` degrees. The
secondary `q=11` population, finite chronology, and recognition-depth drift produce the smaller
change to the complete-population centroid gaps.

### 3.3 Why the hierarchy appears

In the phase-product receiver, the two projected coordinates have the schematic form

```text
C_p(n mod p) + (1/3) C_q(n mod q)
  + (exact count drift, exact recognition-depth drift),
```

where `C_p` is an exact rational point on the unit conic. The first factor has coefficient one and
therefore produces `p` large residue cells. The second factor has coefficient `1/3` and produces
the internal `q`-phase population. The shared occurrence `n` winds through both factors by CRT
chronology. Source carriers and traveling wave sections then produce the chord web. This object is
most precisely named a:

```text
Cayley-embedded CRT phase-product projection with a residue-stratified chord web.
```

Useful visual names are nested cyclic rosette, residue cells, coarse prime-phase polygon, and
projected torus chord web. It is finite recurrent geometry, not a fractal.

### 3.4 Dilation and turn receipts

Five `(7,11)` exact dilations retained the same 392 germs, 21 literal envelope corners, 14
same-residue refinements, seven contracted sides, and 963 visible wave sections. Dilation changed
observation scale and projected rate moments, not the underlying morphology.

Exact first-factor Cayley turns changed the fine envelope while preserving its contracted cycle:

| turn | literal corners | within-7 refinements | contracted transitions |
|---:|---:|---:|---:|
| `-8` | 18 | 11 | 7 |
| `-4` | 21 | 14 | 7 |
| `0` | 21 | 14 | 7 |
| `+4` | 18 | 11 | 7 |
| `+8` | 20 | 13 | 7 |

The coarse prime cycle is stable under these receiver changes; the fine ecology is not.

### 3.5 What has not yet been distinguished

Only prime phase axes were compared. A cyclic composite modulus could also produce an `m`-cell
quotient. The present result therefore establishes a modulus-specific prime-axis mode, not a
prime-exclusive archetype. Composite controls and reversed factor dominance are required.

## 4. The diagnostic colors and ellipses are not source physics

`scripts/render_arithmetic_residue_receipt.py` colors projected germs by `n mod p`, overlays the
exact hull, marks only hull edges which change residue, and draws one covariance ellipse per exact
residue population. These colors are an explicit diagnostic quotient; they are not the engine's
receiver-transported phase color and are not assigned wavelengths.

The ellipses are second-moment inspection membranes. Their overlap means that two projected
populations have overlapping covariance regions. It does not establish:

- equality of source germs;
- an orbital or wavefunction;
- a bond;
- a pairwise potential;
- a source conic; or
- a current-carrying interaction.

The patterned proximity is nevertheless not random. As `p` grows, adjacent first-factor centers
become closer while the `q/3` internal excursion remains comparable, so neighboring residue clouds
overlap. Their ordering is further forced by the shared CRT chronology.

## 5. Optical and quantum-imaging comparison

Two comparisons remain useful when their missing obligations are kept explicit.

### 5.1 Wigner/Husimi phase-space imaging

Wigner optical distributions represent a field over position and spatial-frequency or momentum
coordinates and bridge wave and ray descriptions. A Husimi `Q` face is a smoothed coherent-state
receiver; coherent and squeezed populations commonly appear as circular or elliptical phase-space
blobs. The arithmetic residue ellipses resemble these phase-space population receipts, but the
present atlas has no density operator, canonical `(x,p)` pair, `hbar`, Born rule, Wigner cross
fringes, or quasiprobability negativity.

Reference:
`https://doi.org/10.1364/AOP.3.000272.m016`.

### 5.2 Caustics and catastrophe optics

An optical caustic is an envelope where a founded ray map loses rank or gains plural preimages.
Geometric ray density becomes singular there; finite wavelength replaces the singularity with a
characteristic diffraction field. The current convex envelope is not a caustic merely because
many terminal chords approach it. A causal caustic claim requires:

1. a founded family of propagated characteristics;
2. its map from source parameters to receiver position;
3. an exact rank-loss or multiple-preimage receipt; and
4. a wavelength-dependent diffraction return.

Reference: Berry and Upstill, *Catastrophe Optics: Morphologies of Caustics and Their Diffraction
Patterns*, `https://doi.org/10.1016/S0079-6638(08)70215-4`.

## 6. Current wave and display boundary

`ExactDimensionalWaveLaw` already carries an important physical-optics distinction correctly:

- exact complex currents of the same mode add before a quadratic response is formed;
- distinct modes remain separate and cannot cancel only because a terminal membrane assigns the
  same pixel;
- unit-conic phase transport is invertible and mode-local;
- declared carrier delay and admittance determine local propagation;
- the exact lossless junction law is

  ```text
  v   = 2 sum_i(Y_i a_i) / sum_i Y_i,
  b_i = v - a_i;
  ```

- exact `sum_i Y_i |a_i|^2` balance is retained; and
- propagation follows source incidence, not terminal proximity.

The current arithmetic receiver does not yet carry physical wavelength. Prime `p` is a modal
identifier; every admitted carrier has the same declared delay `4`; there is no physical length,
frequency, dispersion relation, refractive index, polarization, absorption, or wavelength-specific
admittance. The phase recurrence `C_p(n+p)=C_p(n)` is a combinatorial recurrence, not yet a
wavelength.

The terminal doctrine forms the three exact quadratic phase responses

```text
(|Re A|^2, |Im A|^2, |Re A + Im A|^2)
```

and applies an exact saturating aperture. This is an inspectable display gauge, not a universal
RGB, luminosity, spectral-radiance, sensor, exposure, or bloom law.

A correct light receiver must distinguish:

1. coherent field propagation (`E_lambda`, `H_lambda`, polarization, phase, constitutive material);
2. incoherent or resolved-mode radiance transport;
3. absorption, emission, in-scattering, and out-scattering in participating media;
4. received Poynting flux or spectral radiance integrated over aperture, area, wavelength, and
   exposure time; and
5. the later sensor response, display gamut, tone mapping, and point-spread/bloom quotient.

For sub-wavelength structure, Maxwell/Helmholtz or a certified discretization such as FDTD is the
physical owner. For phase-unresolved participating media, the radiative-transfer equation is the
appropriate owner. Fluid flow remains a different local law; optical coupling enters through
refractive index, extinction, heating, radiation pressure, or another declared interaction.

References:

- NIST FDTD/Maxwell discussion:
  `https://tsapps.nist.gov/publication/get_pdf.cfm?pub_id=930720`;
- radiative transfer:
  `https://www.pbr-book.org/4ed/Light_Transport_II_Volume_Rendering/The_Equation_of_Transfer`;
- spectral film and sensor reception:
  `https://pbr-book.org/4ed/Cameras_and_Film/Film_and_Imaging`.

## 7. Pairwise potentials and the bonding question

Projected nearness does not found interaction. A bond-like claim requires at least one of:

- exact source-carrier incidence;
- a nonzero mutual-coherence cross term;
- an admitted constitutive coupling or potential kernel;
- a path integral or holonomy along an actual trajectory;
- transported current; or
- a changed energy/action receipt in the coupled body.

For amplitudes `A_i,A_j`, a useful receiver is the mutual coherence

```text
Gamma_ij = <A_i A_j^*>.
```

For orbital-like sections, the analogues would be an overlap and Hamiltonian response rather than
the intersection of drawn outlines:

```text
S_ij = integral psi_i^* psi_j,
t_ij = <psi_i | H | psi_j>.
```

A compactly supported classical potential whose support is disjoint from the enacted trajectory
contributes no local force. This is not universal: long-range fields need not have compact support,
and a gauge connection may change phase through path holonomy even where a local force vanishes.

The high-value receivers are therefore separate mutual-coherence, causal-interaction, and
energy-flow faces, not a brightness increase assigned to close terminal points.

## 8. The video-derived `e` approximation

The referenced video is blackpenredpen, *I discovered an AMAZING approximation of e (by
accident)*, `https://www.youtube.com/watch?v=7apKaPRyiXs`.

It allocates a total perimeter `10` between an equilateral triangle and a square. If `x` is the
triangle's perimeter, then

```text
A(x) = (sqrt(3)/4)(x/3)^2 + ((10-x)/4)^2.
```

The exact minimum is

```text
x_min = 90/(9+4 sqrt(3)),
A_min = (25/11)(3 sqrt(3)-4)
      = 2.7185282334241633...
```

whereas

```text
e = 2.7182818284590452...
```

The absolute difference is approximately `0.0002464049651181`, or `90.6473` parts per million.
The exact minimum is algebraic and satisfies

```text
121 A_min^2 + 2200 A_min - 6875 = 0;
```

it cannot equal the transcendental number `e`.

That fact does not dispose of Brandon's mechanism. It only separates the video's actual operation
from the proposed gluing operation.

### 8.1 The video's exact compositional law

For a regular `n`-gon with perimeter `P`, define

```text
A_n(P) = P^2/G_n,
G_n    = 4 n tan(pi/n).
```

Then

```text
G_3 = 12 sqrt(3),
G_4 = 16.
```

Minimizing `P_3^2/G_3 + P_4^2/G_4` under `P_3+P_4=L` gives

```text
A_(3 parallel 4)(L) = L^2/(G_3+G_4).
```

The response coefficients therefore compose like parallel conductances:

```text
G_(K parallel L) = G_K + G_L.
```

At the minimum, marginal area costs agree, the triangle side is `sqrt(3)` times the square side,
and the square side equals the triangle circumradius. This is a genuine resource-equilibrium
relation. It is not a literal shared equal edge.

Because `A_min` scales as `L^2`, its numerical proximity to dimensionless `e` is not a scale-free
topological invariant. The intrinsic coefficient supplied by this problem is

```text
A_min/L^2 = 1/(12 sqrt(3)+16).
```

A natural recursive normalization must be supplied before a geometric limit can be compared to
`e` invariantly.

## 9. Brandon's shared-edge correction

Brandon proposed another operation: compose a triangle and square by identifying one opposed edge
pair. This is exact oriented cellular gluing.

Let

```text
boundary(T) =  e + a + b,
boundary(S) = -e + c + d + f.
```

Seven source edge occurrences identify to the six distinct 1-cells

```text
{e,a,b,c,d,f}.
```

The combined exterior boundary is

```text
boundary(T+S) = a+b+c+d+f.
```

Therefore five edges are exposed, while the sixth is the internal seam. The seam cancels from the
outer boundary but remains available to carry incidence, stress, scale transport, phase,
potential, current, or obstruction. This is not rhetorical molecular bonding; it is an exact
chain-level mechanism.

If regular triangle and square edges of their native optimum are identified, their unequal lengths
require an explicit dilation by `sqrt(3)` or another declared edge transport. That nonidentity seam
would be a metric/affine holonomy, not equality silently imposed by the renderer.

## 10. Why collapsed construction histories can produce `e`

The lawful route from geometric collapse to the exponential is the combinatorial exponential
formula.

If `n` local attachment events may occur in any order but all `n!` orderings quotient to one
completed body, that body contributes

```text
x^n/n!.
```

Summing every finite population gives

```text
Exp(x) = sum_(n>=0) x^n/n!.
```

At `x=1`, this is `e`. More generally, if `C(x)` carries connected or primitive bodies, unordered
assemblies have

```text
F(x) = Exp(C(x)),
C(x) = Log(F(x)).
```

The logarithm extracts connected/primitive contributions; the exponential assembles their complete
unordered populations. Pairwise bonds and interaction clusters therefore belong to the connected
logarithm, while assemblies exponentiate. Automorphisms replace a naive `n!` by the actual history
stabilizer where required.

This is structurally aligned with the exact Euler/Zeta relation already carried by the formula
ecology:

```text
Log Zeta(s) = sum_p sum_(m>=1) p^(-m s)/m,
Zeta(s)     = Exp(Log Zeta(s)).
```

The prime and prime-power currents inhabit the logarithm; exponential assembly forms the complete
multiplicative population. The video's isolated numerical return does not establish this recurrence,
but Brandon's proposed history quotient supplies a concrete mechanism to test.

## 11. `pi`, `e`, wavelength, and the RH-facing completion

The current rational Cayley chart intentionally avoids an Archimedean angular metric. An exact
cyclotomic receiver would instead carry

```text
omega_p^r = Exp(2 pi i r/p),
Phi_p(omega_p) = 0.
```

The root of unity is algebraic and can be retained by its cyclotomic polynomial and selected
embedding without a floating `Math.PI`. This receiver would place residues at equal Archimedean
angles and distinguish Cayley-chart asymmetry from phase-cycle incidence.

For a physical prime phase on `s=sigma+it`,

```text
p^(-s) = p^(-sigma) Exp(-i t Log(p)).
```

Its `t`-period is

```text
T_p = 2 pi/Log(p).
```

On the critical line the amplitude is `p^(-1/2)`. This is where `e`, `pi`, wavelength, and `1/2`
can meet lawfully; they are not present in the current residue picture merely because it contains
prime axes.

The Archimedean completion requires the coupled objects

```text
xi(s) = (1/2)s(s-1) pi^(-s/2) Gamma(s/2) Zeta(s),
Gamma(z) = integral_0^infinity t^(z-1) Exp(-t) dt,
theta(t) = sum_(n in Z) Exp(-pi n^2 t).
```

`pi` is the metric completion of cyclic/Fourier boundary; `e` is the finite transport emitted by a
generator or the quotient of compositional histories. Their compatibility is the unitary relation
`Exp(i pi)=-1`. The theta/Fourier return, not the present screen asymmetry, is the appropriate owner
of the `s <-> 1-s` involution and its fixed seam `Re(s)=1/2`.

## 12. Questions deposited for the main session

The following questions are preserved without scheduling them ahead of Brandon's current main
objective.

### A. Prime-mode controls

1. Does the contracted `m`-cycle persist for composite residue moduli under an otherwise identical
   receiver law?
2. When `(p,q)` is reversed or the `1` and `1/3` factor weights are exchanged, does the coarse
   envelope follow the newly dominant factor exactly?
3. Which fine hull refinements are caused by the secondary modulus, chronology drift, recognition
   depth, and finite horizon respectively?

### B. Cayley versus cyclotomic phase

1. Which asymmetric gaps survive replacement of the rational Cayley chart by an exact cyclotomic
   root-of-unity chart?
2. Can the two charts be retained as parallel receiver testimony over the same source standing?
3. Does a physical `Exp(-it Log(p))` receiver recover the residue polygons as sampled phase cuts,
   or produce another morphology?

### C. Conics, caustics, and higher cells

1. Which apparent circles, ellipses, or hyperbolas admit an exact quadratic with zero residual?
2. Which apparent focal points are multiple-preimage/rank-loss receipts of an enacted transport
   map rather than crossings of terminal chords?
3. Which apparent grids or hypercubes survive reconstruction from source incidence rather than
   projection adjacency?

### D. Mutual coherence and interaction

1. What is the exact pairwise mutual-coherence matrix by mode and receiver contact?
2. Which differently colored residue pairs have actual source-carrier incidence, nonzero coherent
   cross response, transported current, or changed action?
3. Can a path-support receiver separate available potential fields from the fields actually
   encountered by current trajectories while retaining long-range and holonomy effects?

### E. Physical light and fluid reception

1. What minimal spectral mode data are required to derive wavelength, phase velocity, group
   velocity, polarization, and dispersion from the current exact carrier ecology?
2. Can coherent Maxwell/Helmholtz sections and incoherent radiative-transfer sections inhabit one
   world membrane without flattening either law?
3. Can the terminal membrane return spectral Poynting/radiance, sensor exposure, and display bloom
   as three distinct caused layers?
4. Which coupled fluid variables change refractive index, extinction, emission, heating, or
   radiation pressure, and which remain separate morphological currents?

### F. Shared seams and exponential completion

1. For a recursively attached family of exact polygonal cells, what are the recurrences for exposed
   boundary cells, retained interior seams, area/action, and seam holonomy?
2. Which attachment chronologies quotient to the same complete incidence-and-transport body?
3. Do their stabilizers produce the factorial coefficients required by an exponential generating
   function?
4. Does `Log(F)` isolate exactly the connected/bonded bodies while `Exp(Log(F))` reconstructs the
   complete assemblies?
5. Is there a natural scale-free invariant whose certified recursive enclosures approach `e`, or
   is the video's value an isolated equilibrium resonance?
6. How does polygonal boundary refinement toward `pi` compose with history quotient toward `e`?

## 13. Proposed bounded discriminating construction

If Brandon later recruits this side question, the smallest honest construction is not another
screen-only image. It is one exact paired experiment:

1. found regular triangle and square 2-cells with distinct source edge occurrences;
2. enact an opposed-edge pushout retaining six unique 1-cells, five exterior boundary cells, and
   one internal seam;
3. carry both an equal-edge branch and an explicit `sqrt(3)` seam-transport branch;
4. retain the video's perimeter-allocation/parallel-response law as a separate control;
5. recursively attach a bounded population of further cells;
6. enumerate actual attachment chronologies and quotient only equal complete
   incidence-and-transport bodies;
7. emit the complete coefficient population of the corresponding ordinary and exponential
   generating faces;
8. compare `F`, `Log(F)`, and `Exp(Log(F))` exactly at every aperture;
9. produce certified intervals for any proposed `e` or `pi` limit; and
10. render the exterior boundary, interior seams, interaction support, and energy/current return as
    distinct receiver layers.

The deed is the exact recursive body and its returned coefficients. A contact sheet alone would be
supporting testimony.

## 14. Nonclaims

This side research does not establish:

- RH or a zero-free/zero-locating theorem;
- a visible pole, zero, or critical line in the present atlas;
- a prime-exclusive polygon archetype;
- a physical wavelength indexed by prime number;
- that covariance ellipses are source conics or orbitals;
- that screen proximity is a bond or potential;
- that the convex envelope is an optical caustic;
- that the video's minimum is equal to `e`;
- that its proximity to `e` is already caused by edge gluing;
- that a generic recursive gluing must exponentiate; or
- that light, fluid, quantum, arithmetic, and biological morphologies share one domain law.

The accepted results are the exact receiver measurements and the oriented shared-seam calculus.
The exponential relation remains a concrete, discriminable question.

## 15. Verification and worktree boundary

The example passes:

```text
cargo check -p holonic-engine --example arithmetic_dimensional_receiver
```

An isolated nonincremental target build also completed successfully. The shared target was not
cleaned or reset because a parallel session was active. The diagnostic renderer passes Python byte
compilation, and every delivered PNG was visually inspected. No existing research record,
`CLAUDE.md`, main-session output, or unrelated dirty file was modified for this deposit.


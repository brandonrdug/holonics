# The census decayed, the cycle is already built, and the open grade is the nonidentical neighbourhood

**Date:** 2026-08-14
**Genre:** research record
**Truth status:** `measured` for every census figure, re-taken today at `9a47b16` on a clean tree;
`established-bounded` for the capabilities cited as carrier; `interpretation` for the plan's framing.
**Occasion:** Brandon ratified the construction plan for compression-is-intelligence — *Eros* — and
directed a three-way audit of the assistant's own concerns against the deposits, the past
constructions, and the laboratory's prime work. Six framing claims were posed for audit. **Three
were false at the source.**

---

## 0. The failure that produced the false claims, and it is a named one

The assistant's framing rested on three sentences copied from `blueprint/THE_ROADMAP.md:1896-1911`, a
**2026-08-08 mechanical census**. Forty lines below the passage quoted, the same file says of that
exact column:

> *"Two of those six rows were false for three days, and the census is why… A census is a measurement
> and decays like one. **Re-run it before quoting it.**"*

Re-run today, all three are refuted:

| claim quoted | measured 2026-08-14 |
|---|---|
| *"no library path closes emit → world → return → reflect"* | closes in library code in **two independent places** — `crates/holonic-engine/src/returned_reading.rs` (`read_production:667` → `condition_again:1500` → `carried_into:363`) and `soma/life/src/agentic_research.rs:643-704` `advance_open_completion`, whose exterior world is an injected closure at `:516` |
| *"`invariant_movement`/`route_movement` are called from exactly one `main`"* | **13 library callers** of `derivation_atlas`. Both functions: `returned_reading.rs:677-678`, `returned_conduct.rs:232,655`, `soma/life/src/returned_conduct.rs:25` |
| *"`temper` has in-degree zero"* | in-degree **2, both library** — `returned_reading.rs:136`, `derivation_integral.rs:1675,1678,1745` |

All three closed at `cec1c97`. **And the driver that closed them, `crates/holonic-engine/examples/the_reading_returns.rs:10-20`, states both claims verbatim in its own docstring as the census it repaired.**

So the assistant described as its open frontier a gap whose closure it was standing on. That is
`canon/THE_EXPLORATIVE_FAILURE.md` §1's re-founding face, fired before a line of code, and it is the
sixth instance of the recurring correction *"read what already exists; you are not networked to it."*

**The standing repair is procedural and cheap: no census figure may be quoted from a document. Re-run
it, and stamp the run with its date.** Three figures in live documents are stale by this rule today:
`canon/THE_INFORMATION_ENGINE.md:43,153,225` says **167** drivers (live: **182**);
`CONSTRUCTION_STATE.md:765-789` says 225 library modules and 97 drivers (live: **384** and **182**);
`:596` says 282 tracked `.rs` and 277,006 lines (live: **515** and **517,120**).

**And one new decay mechanism, worth carrying.** `CONSTRUCTION_STATE.md` has been ingested into the
driver corpus under `soma/life/driver-sources/*/SOURCE.json`, so **every absence grep it states over
`soma/` now matches the document quoting itself**. Scope absence greps to `--glob '*.rs'`.

---

## 1. The compression cycle is already built, and its owner says so

`soma/life/src/decomposing_codec.rs` is the codec pivot the compression tablet defines, complete:

| the tablet's requirement | the owner |
|---|---|
| a codec pivot | `decompose(grain, whole) -> Vec<Vec<Symbol>>` `:189` — complete, non-overlapping, gapless |
| **a declared decoder** | `DecomposedWhole::reintegrate() -> Vec<Symbol>` `:183`, with failures exhibited at `:355` |
| **the remainder exhibited, not bounded** | `DecompositionPass::collapsed() -> &[CollapsedPair]` `:372`, each pair carrying the shortest word that separates it |
| **the deposit that changes the terrain** | `DecomposingBody::revise() -> Revision` `:698` — re-cuts at each collapsed pair's own distinguishing word, parenting a `CodecVersion` |
| the negative control | `resume_unrevised()` `:774` |

Its own header states the reusable-morphology claim exactly: the grain *"is the reusable morphology —
what the decomposer does to material it has never seen is completely determined by it, and revising it
changes later conduct on later material."* And it already declares three falsifiers it is built to be
able to fail, including the foil — *"cut at a word of the same length that the compression did not
return; if any cut whatsoever shrinks the population as much, the derivation is decorative."*

**The composition was attempted end to end and returns exactly one absent type, and it is a getter.**
`read` at `:409` builds `pooled: Vec<Vec<Symbol>>` and hands it to `PartSystem::over_parts`, which
collapses it into a `BTreeSet` (`complete`, `:212`). **The part multiset is discarded at the moment it
exists.** `parts()` `:259` returns the set. Nothing else in the cycle is missing.

Two candidate constructions were withdrawn on the evidence rather than carried: a min-cost route over
the consequence complex (`decompose` is deterministic given the grain, so the length is a function of
the grain alone and the movement is directly attributable), and an adaptor from `FoundedMorphology`
to a surprisal population (`conditioned_derivation.rs:233-234` — the morphology deliberately carries
no multiplicity, *"It is never a number"*, so the adaptor would invent a count the owner refuses).

---

## 2. The headline the assistant proposed was superseded 2026-07-19

`research/records/2026-07-19_THE_EMANATION_COMPRESSES_THE_EVENT_THE_PHASE_RETURNS_THROUGH_REBASE.md:14-17`,
ratified:

> *"Later RIDE does not create compression. It witnesses that a relation survived prior compression
> and recurs as conduct in a changed frame. **Earlier records which call later reuse, reduced founding
> cost, shorter passage, or physical amortization "compression" are regraded at that face: they
> measure recurrence, reuse, and amortization. Their exact observations stand; their ontological
> definition does not.**"*

*"The deposit made later material cheaper"* is that sentence's object. The measurement is real; the
name is not available for it.

**And converting the cost back into a number would be a regression against a standing capability.**
`canon/THE_MEASURED_CAPABILITIES.md:100-101` grades the collapsed-pair exhibit *"the project's
sharpest instrument"* precisely because *"It converts 'how much did compression cost' from a **ratio**
into an **exhibitable population**."*

**So the bit form is a face and never the object.** The collapsed population stays primary; the exact
form is reported beside it, as its magnitude face, with the phase — the pairs and their words —
retained. That is the same discipline the fork-orientation repair applied earlier today.

---

## 3. What is actually open: grade three

`research/records/2026-07-20_THE_DEPOSIT_BENDS_THE_LATER_CAST_THE_CONSTITUENT_HAS_NOT_YET_ENTERED_THE_LATER_CONTACT.md:53-57`
deposits three grades:

| grade | statement | status |
|---|---|---|
| **persistence** | a changed body, lineage, world or testimony survives | **measured** |
| **associative** | controlled intervening history changes standing and later **addressed** conduct | **measured** |
| **transferable developmental** | *"Experience forms a correspondence by which a retained construction changes useful conduct in a later **nonidentical** neighborhood"* | **OPEN** |

The 2026-08-13 ride measurement re-admits **the same** later material on a changed terrain, so it sits
in grade two by that record's own definition. `research/records/2026-08-13_THE_CUT_BECOMES_THE_CODECS_AND_THE_LATER_CURRENT_RIDES_THE_DEPOSIT.md:83-100`:
B with A standing `reached 6 · reopened 4`; B with A withdrawn `reached 4 · reopened 2`; and *"the two
compounds the later current reopened only when the deposit stood are exactly the two the deposit
founded."* Exact, ablated, restored bit-exactly — **and identical-neighbourhood.**

**Grade three is the deed.** By `CLAUDE.md` §1, the established chain — deposit changes later conduct,
with structural ablation and source-detached remount — is *"admissible without limit as the **carrier**
of a deed"* and *"inadmissible only as the **return**."* It is the carrier. The return is the
nonidentical neighbourhood.

---

## 4. Two type errors in the assistant's instrument, corrected before building

**`Kt` does not reach the boundary it was proposed to cross.** `canon/TABLET_THE_COMPRESSION.md:118-125`
locates the stopping point: *compression is prediction* is a theorem, *compression is intelligence* is
a thesis, and the step between is where machine-independence stops. `Kt` has its own invariance theorem
**for descriptions**; adding a runtime axis to a description-length measure produces a better
description-length measure and stays on the theorem side. What fails at that step is not the absence of
a runtime axis. The same tablet already grades the space/time trade `interpretation` with five named
breaks, one being that `log t` is *"a declaration, not a law."*

**And `cross_entropy` is not a code length for an individual object.**
`crates/holonic-engine/src/surprisal.rs:418` returns `H(P,Q) = Σ_a P(a)·S_Q(a)` over two
`BTreeMap<u64, BigUint>` count maps — a **Shannon expectation over a declared event alphabet**.
Conflating that with the description length of one construction is exactly the Shannon/Kolmogorov gap
this project's own record names: *"On average, under a declared source, the two agree up to `K(P)`; the
unbounded gap is visible only on individual objects."* Measured: `kolmogorov`, `solomonoff`,
`minimum description` return zero hits tree-wide — there is no program, no universal machine, no `|p|`
— and the compression record calls that *"the correct state rather than a gap."*

**A third property decides how the reading must be reported.** `surprisal.rs:44-49`: *"Vanishing and
equality are decided EXACTLY, with no enclosure… **It is why `S(P) = S(Q)` is decidable here while
`S(P) < S(Q)` is not.**"* So the **difference form** is the primary return — exact, never `Open`, and
precisely the additive invariant the tablet demands — and any direction is secondary, taken at a grain
declared **before** the run, with its orbit exhibited.

---

## 5. THE STANDING PLAN

**Object:** close grade three. A grain revised by material A conducts a **nonidentical** material B,
whose events the deposit never touched, and the change is exhibited as a population and reported with
its exact magnitude face beside it.

**Carrier (established, not re-demonstrated):** `decomposing_codec`'s pivot/decoder/remainder/deposit;
`returned_reading`'s return through the seal; `without_stem`'s structural ablation;
`ConditionedRest`'s source-detached remount; `receiver_exact_compression`'s collapsed population.

### Step 0 — three readings, taken before any construction

1. Run `surprisal::cross_entropy` on the before-body and after-body of the **existing** ride run and
   read what `SymbolicSurprisal::compare` returns. Every outcome is information: `Less` gives the
   result through `situated_residual::situate`, which already returns the per-member oriented residual
   and already refuses to total it; `Open` locates the grain the reading needs and is itself the
   finding about this material.
2. Name which grade the measurement lands in, against the three-grade record, **before** the receipt
   is written.
3. Express the bit/work exchange as a **value passed at a call site**. If it cannot be expressed that
   way, the composition failed and the exact absent type is owed before any new owner is admissible.

### Step 1 — the one absent type

A public part multiset and part→id on `DecompositionPass`/`PartSystem`. `pooled` already exists at
`decomposing_codec.rs:409`; it is dropped one line later.

### Step 2 — one repair inside an owner

`surprisal::cross_entropy:434` returns `Unsupported` for the **whole** reading on the first
unsupported event, discarding the form accumulated over every supported one — so it refuses on exactly
the material a compression deed must score. A three-arm return —
`Supported(form)` / `Partial { form_over_supported, unsupported, unsupported_mass }` / `Unsupported` —
**exhibits instead of collapsing**, and does not smooth anything. `read_population:390` already returns
per-event `Support`; the aggregate throws it away.

**This is the two-part code arriving as a consequence of a refusal already in the code**: the supported
half is what the standing grain already carries, the unsupported half is what must be founded, and
founding is the terrain change. The thing that pays and the thing that learns are one accounting entry.

### Step 3 — the declared exchange

`DeclaredCarrierMetric` (`cuda_aperture.rs:1157`) prices host, device and transfer and **not**
`intermediate_bits`, which `CarrierWork:1068` carries. One coordinate and one term in `cost_of`, passed
as a value. `cuda_aperture.rs:1112-1114` states the obstruction verbatim: *"a host evaluation and a
device evaluation are different units, and nothing in the material says how many of one buys one of the
other."* A declared exchange is exactly what closes that, and declaring it explicitly is lawful where
inferring it would not be.

### Step 4 — the deed

Material A revises the grain. Material B is **nonidentical**, held out and named as held out before the
run, and its events are **not** the ones the deposit founded. The return is the population B reaches
under the revised grain that it did not reach under the parent, each member naming the deposit that
licensed it.

### The controls, decided now

| control | what it prevents |
|---|---|
| **a second held-out body the deposit did not touch, whose reading must not move** | the deposit being a global normalization rather than changed terrain. This is simultaneously the grade-three requirement and the keystone anti-tautology. |
| score against the deposited standing with **no further founding**; residual unsupported exhibited | measuring the deed's own founding — the shape already committed honestly at `the_iron_tokens_carry_the_field.rs:1203-1250` as a refusal-arm exercise |
| the event table **derived from the union of both emissions**, members returned by name | the authored partition. `situated_residual.rs:168-173` states why a returned integer would be a receiver coordinate promoted into the artifact. |
| deposit content varied at **fixed cardinality** | the length being a monotone function of a count the deposit incremented |
| `resume_unrevised()` must move the difference form by **exactly zero**, by `is_zero()` | a mechanism that founds on every pass, which *"has a clock, not a consequence"* |
| the foil: cut at an unreturned word of the same length | *"cutting more is better" wearing a lineage* — the owner's own third falsifier |
| recurrence present at 1, 2 and ≥3, not placed by the driver; the refused population printed | a decorative recurrence gate |
| the declared exchange's orbit exhibited | a decorative declaration |

### Standing bars this plan runs under

- **The bit figure may be reported, ranked in a report, and compared. It may never select, prune,
  gate, or route.** The check is mechanical: does it arrive at a `<`, `min`, `sort` or `argmax` that
  **discards** a member; is the discarded member on the receipt; can a later caller decline it.
- **The collapsed population is primary and the number goes beside it**, never instead.
- **No macro-event.** No sentence of the form *the machine compressed its own experience*. The lawful
  form: *a deposit changed the terrain and a later current rode it; here is the change priced under a
  declared code, with the collapsed population exhibited.*
- **"In bits" must declare whose bit in the same sentence.** Both axes carry unledgered authored bit
  levels — `surprisal.rs:81` `LOG_SERIES_BITS = 192` and `cuda_aperture.rs:115`
  `MAX_DEVICE_INTERMEDIATE_BITS = 384`, the latter deciding at `:542` which primitives count as device
  work and which as CPU work. Both are invisible to `tools/authored_levels.py` because `:77` excludes
  `_BITS$` by name. **The exclusion is itself a defect and is now an open item.**
- No identification of the symbolic depth with thermodynamic entropy. The generator is shared; the
  quantities are not.

---

## 6. Corrections to the record this audit produced

**The residue-stratum atlas is available and reproducible, and the governing documents collapse two
different claims about it.** `CLAUDE.md` §12 and `CONSTRUCTION_STATE.md:686-689` say it *"resolves at
no commit in either repository"* and *"no claim may cite it as evidence."*

Measured: the files exist — `/home/b/Workspaces/laboratory/output/arithmetic-dimensional-receiver/morphology-atlas/`,
**111 files, 66 MB**. `/output/` is `.gitignore` line 3, so `git ls-files` returns zero at all 1,726
laboratory commits — *that is a fact about git, not about availability.* **The producing driver is
tracked and byte-identical between the laboratory at `a07ff376` and this tree right now**
(`crates/holonic-engine/examples/arithmetic_dimensional_receiver.rs`, verified by `git hash-object`),
with the `HOLONIC_MORPHOLOGY_SWEEP` gate at `:1070` and fifteen sweep-state literals at `:1073-1177`.
`meta/OUTPUT_MANIFEST.tsv:11` already content-addresses this driver's live output and
`meta/CLOSURE_MANIFEST.tsv:10` names its producer — the manifests exist precisely because `output/` is
ignored, and the `git ls-files` grep is the wrong instrument.

**So it is not unported. It is unrun** — this repository's `output/arithmetic-dimensional-receiver/`
holds three files from a 2026-08-09 run *without* the sweep variable. One command reproduces the atlas.
It is **not** *"the same loss as the tiger figures"*, which have zero surviving bytes anywhere.

The artifacts confirm the winding law by direct reading. `atlas-summary.tsv`, 15 rows. Germs and hull
corners: (2,3) 504/11, (3,5) 488/15, (5,7) 464/16, (7,11) 392/21, (11,13) 344/21, (13,17) 224/18,
(17,19) 152/21. **Coarse sides equal the smaller prime in every pair.** Every contracted hull word is
one complete positively-oriented degree-one cycle through `ℤ/pℤ` with every step exactly `+1`. **Four
dilation states at (7,11) leave germs 392 and corners 21 invariant — dilation is a gauge; four turn
states move corners to 18/21/18/20 — turn is not.** The renderer
`scripts/render_arithmetic_residue_receipt.py` is tracked in the laboratory and absent here; it is the
one file worth porting.

Also on disk and recorded as lost: `runs/` at 54 GB, including
`runs/dialogue-distractor-scaling/REPORT.json`, 429,674 bytes, carrying its `interpretation` field
verbatim. The tiger figures remain genuinely absent.

**Further stale claims, each verified by opening the owner:**

- `canon/THE_MEASURED_CAPABILITIES.md:299` — *"`hypergeom|₂F₁|pochhammer` returns two lines, both
  negative declarations."* False as of today: `crates/holonic-engine/src/hypergeometric_closure.rs` is
  live, registered at `lib.rs:136`, and driven.
- `:280-297` — Mellin–Birkeland/Tschirnhaus as *"the largest single unbuilt item"*: `quintic_chart.rs`
  is 2,875 lines with five drivers, and the cited tablet now reads *"BUILT, and this row was stale."*
- `:311` — *"`ProjectiveTurn`'s group laws are untested."* False: `simplicial.rs:2181,2185,2228`.
- `CONSTRUCTION_STATE.md:604` — *"two hand-written CUDA kernels."* Four.
- `:641-645` — *"`f32|f64` returns zero across every library."* Fourteen in library `src/`, all
  declared boundary codecs, prose, or SPIR-V spellings being refused. The substantive claim — no
  machine-float arithmetic on any conduct path — stands; the stated measurement does not.
- `canon/THE_INFORMATION_ENGINE.md:256` — *"eleven fully unwired modules and 145 zero-reference public
  items."* Seven modules; **905** zero-reference distinctive public items. A module-path grep cannot
  measure this because `lib.rs` glob-re-exports 64 modules, so item granularity is the only valid
  instrument.

**Three of the seven inert modules bear directly on standing work**, and each should be read before
anything of its shape is proposed: `realization.rs` (358 lines — `RealizationWitness`,
`ReceiverProgram`, `ReceiverRealization`, unwired, while *realization causes placement* is the
governing synthesis and `supported_realizers.rs` does not reference it); `device.rs`
(`ExactDeviceExecutor` + `DeviceParityReceipt`, whose header states the byte-for-byte parity contract
`cuda_aperture.rs` re-implements — two owners for one law); and `parameter.rs`
(`CopyLaw`/`RetentionLaw`/`DepartureLaw`/`ParameterLifecycle` — typed departure and retention, which is
exactly the vocabulary source-detachment bookkeeping needs, named by nothing).

**And the cheapest large win found anywhere in this audit:**
`soma/life/src/lean_mathematics/ecology.rs:1016-1072` `receive_kernel_deed_returns` — the organ that
turns a **real exterior kernel return** into changed standing, letting every admitted path found one
proof section, keeping obstructed paths as exact obstruction fibers, incrementing `generation`, and
keeping the composite mounted *"so later conduct can use it after source-detached remount."* It is
built, validated by `validate_kernel_run:1074`, and tested at eight sites. **Zero drivers.** A proof
checker is one of exactly two genuine exterior worlds in this body, and nothing has ever run the return
and the reflect together on real material.

---

## 7. Unifications found, none of them scheduled here

Recorded so a later composition finds them rather than re-deriving them.

- **`interchange::order_price_bits` IS `SymbolicSurprisal`, rounded.** `interchange.rs:1480` computes
  `⌈log₂ n!⌉` as a `u64`; `surprisal.rs:161` computes the same quantity as an exact form. Neither file
  mentions the other. The ceiling is correct for a code length, so this is not a merge — but
  `OrderPrice { bits: u64 }` is the shape `surprisal.rs`'s own doc convicts, *"a decimal surprisal keeps
  the magnitude and discards the term structure."* Carry the form beside the ceiling. The two families
  already meet at `interchange.rs:71`, which imports `CollapsedPair`.
- **Exact rank over `Rat`: six owners, three names, no shared owner** — `algebraic.rs:1123`,
  `receiver_ecology.rs:578`, `sheaf_diffusion.rs:1011` (the last two byte-identical),
  `derivation_two_cells.rs:1461`, `matroid_chow.rs:811`, `inertia.rs:237`. `exact_linear.rs` — the
  designated owner — has no rank at all, which is why all six exist. `matroid_chow.rs` computes rank
  two ways in one file (`:1495`, `:1504`) and never compares them.
- **Exact linear algebra: four re-implementations beside `exact_linear.rs`**, whose own header at
  `:3-6` names `diffusion` and `inverse_transport` as the modules it exists to spare — and both contain
  zero references to it. Only `exact_linear.rs:212` certifies its inverse. A cost law hides here:
  `diffusion.rs:608` and `sheaf_diffusion.rs:954` invert column-wise at `O(n⁴)` against `O(n³)`, and
  `diffusion.rs`'s inverse is on the Schur-complement path the condensation demand rests on.
- **`⌊log₂⌋` of an integer: eleven implementations, three incompatible conventions, one citing pair.**
  `reopening.rs:200` `dyadic_scale` is the generalization of all of them and cites none.
- **Propose/commit with a staleness guard: three independent implementations** —
  `holonic_training.rs:272/355` (strongest: rechecks each touched fiber and returns the proposal
  intact), `live_holon.rs:204`, `contact_cycle.rs:2278/2458`.
- **The complete parse against a standing population: three owners with load-bearing differences that
  must not be flattened.** `decompose` is a complete parse with exactly one reading;
  `holonic_training.rs:791` `consequence_complex` is the only complete non-overlapping parse **with
  plural alternatives**; `FoundedCover::maximal` `:687` is an antichain under containment — crossings
  retained, gaps possible — and is therefore **not a parse at all**.

**Repairs found and not yet made:** `rebase_invariants.rs:315` derives `PartialOrd, Ord` on
`ReductionWork` — a lexicographic total order on a genuinely partial work vector — **three lines below
its own doc quoting the rule that forbids it**; same defect at `rational_polynomial.rs:854` and
`:1308`. `collocation.rs:546` returns partition blocks with no witness, so it cannot say what its
quotient cost. `soma/body/src/register.rs:37` cites two dead paths.

---

## What this record does not claim

No Millennium movement. No claim that compression is intelligence — that remains a thesis with its
stopping point named. It does not claim grade three is reached; it states that grade three is the deed
and that the grades below it are the carrier. It schedules nothing beyond the plan in §5, which the
roadmap now carries.

---

## 8. STEP 0, TAKEN. The keystone control fires, and the deposit's aperture is the object

Driver: `soma/life/examples/the_grain_is_read_as_a_code.rs`. Material: three records of this
repository as conditioning; `canon/TABLET_THE_COMPRESSION.md` held out; `canon/TABLET_THE_MANIFOLD.md`
held out and subject-disjoint. Origin grain: close a part after a space — one cut word, and the only
authored thing in the run.

### What returned

```text
                              parts   distinct   collapsed pairs   unsupported
parent grain (1 cut word)
  held out, COMPRESSION        1320        700           123 944           250   form UNSUPPORTED
  held out, MANIFOLD           8333       2925         1 304 986          2306   form UNSUPPORTED

the deposit: 1 448 456 founded codec versions; grain 1 -> 1775 cut words

revised grain (1775 cut words)
  held out, COMPRESSION        8380        105                 0             0   form RETURNS
  held out, MANIFOLD          49838        171                39            59   form UNSUPPORTED
```

The one form that returned is exact and symbolic — a ℚ-linear form over `log₂` of the primes to
2957, thirty-four terms, `+ log₂(107)` at coefficient one. No float, no evaluation.

### Four findings, and the third is the one that changes the deed

**1 · Step 2 is a precondition, not an optimisation.** Three of the four readings returned
`Unsupported` **for the whole reading** while the supported majority had already been accumulated —
250 novel events out of 700 distinct, and the aggregate discards the other 450. The comparison the
plan's headline needs **cannot be taken on real held-out material** until `cross_entropy` carries a
`Partial` arm. This was posed as a repair and is measured as a gate.

**2 · The naive byte reading is refuted on this material, again.** Parts went **up** under the
revised grain — 1320 → 8380 and 8333 → 49838 — because a finer grain cuts more. That is
`skein_far_condensation`'s 254 → 378 recurring on different material, and it is why *"reduced founding
cost"* was never the right name.

**But the code itself shrank, and this is the two-part account appearing without being asked for.**
Distinct parts fell **700 → 105** and **2925 → 171** while covering the same material. **The model got
smaller and the parse got longer** — exactly the split the two-part code describes, arriving out of
the grain rather than out of a principle.

**3 · The keystone control FIRES, and it names the real object.** The subject-disjoint body improved
as much as the held-out one — unsupported `2306 → 59`, collapsed `1 304 986 → 39`. **Both moved.** By
the control declared before the run, that means the revision was a **global normalisation, not changed
terrain.**

The cause is legible in the deposit itself: `revise()` founds one codec version **per collapsed pair**,
and this pass returned **1,448,456** of them, driving the grain to 1775 cut words over a 
small alphabet. At that fineness the parts are short and near-universal, so *any* material becomes
supported. **The deposit saturates.**

**So the object the deed must carry is the deposit's aperture.** Not a new organ — the aperture is
already the framework's own vocabulary, and `revise()` already returns its population by name rather
than by count. What is absent is a *declared* extent on the revision that the **material** decides,
so that a deposit founded by material A cuts where A's collapsed pairs ask and not everywhere at once.
Until that exists, "the deposit changed the terrain" and "the grain became maximally fine" are the
same sentence on this material, and no held-out reading can separate them.

**4 · The grade is confirmed as two, by construction.** The later material is identical across the two
grains. Grade three needs a nonidentical neighbourhood, and this reading does not reach for one.

### What Step 0 changes about the plan

Nothing is withdrawn. Two things are reordered and one is added:

- **Step 2 moves ahead of Step 1** — the `Partial` arm gates every reading, and the part multiset is
  reachable today through `DecompositionPass::decomposed`, so Step 1 is a convenience.
- **A new Step 1b: the deposit's declared aperture**, read off the collapsed population rather than
  authored, with its orbit exhibited. The keystone control is the falsifier and it already fired once,
  which is what makes it an instrument rather than a formality.
- **Step 4's material selection is now decided by measurement rather than by taste**: the disjoint
  body is not an afterthought control, it is the discriminator, and it must be run at every reading.

---

## 9. STEPS 1, 1b, 2, 3 AND 4 BUILT. The crossing holds, and its margins are thin

### What was built

| step | owner | what |
|---|---|---|
| **2** | `crates/holonic-engine/src/surprisal.rs` | `CrossEntropyFiber::{Supported, Partial, Unsupported}` and `cross_entropy_fiber`. `cross_entropy` is retained as its **face** and delegates. The `Partial` arm carries the form over the supported part **weighted by the whole population**, both masses (summing to one exactly), and every unsupported event **by name**. Nothing smooths. |
| **1b** | `soma/life/src/decomposing_codec.rs` | `RevisionAperture` and `revise_within`. The caller declares a threshold; **the material decides which words clear it**. Excluded words return by name with how many pairs asked. `revise()` delegates at `every_pair()`. An aperture nothing clears refuses by name. |
| **1** | same | `DecompositionPass::part_population` (the multiset `read` builds and the part system discards) and `part_count` beside `parts().len()`. |
| **3** | `crates/holonic-engine/src/cuda_aperture.rs` | `DeclaredCarrierMetric::intermediate_bit_cost` — **the description axis**. A width is a description length. Declared as a value at a call site; zero reproduces the inherited reading. |

Each carries its own orbit control. The aperture's: three declared thresholds must reach different
grains and the grains must be monotone under narrowing. The exchange's: a busy narrow carrier and a
quiet wide one **order one way with no width price and the other way with one**, so the declaration
is a frame and not a decoration.

### Step 4, and the first design was refuted by its own first run

The deed first ran with a one-sided control — a subject-disjoint *later* body required not to move.
**It moved, at every aperture**, and the reason is legible in the admitted words: at the widest
aperture that cleared the threshold the cuts were `(`, `)`, `*`, `,`, `-`, `.`, `/`, `:`, `;`.

> **The words the most collapsed pairs ask for are punctuation — the least material-specific cuts
> there are. Recurrence across pairs IS generality, so an aperture keyed on it selects against
> attribution.**

So a one-sided control cannot reach grade three: *any* refinement helps *any* later body, and asking
whether the later body improved is the wrong question. The right one is **whether it improved because
of THIS conditioning body**, which is a difference between two *deposits*, not between two grains.

**The design became a crossing.** Two conditioning bodies on different subjects — circulation
(`TABLET_THE_CIRCULATING_CARTOGRAPHER` + `TABLET_THE_REASONING_CYCLE`) and geometry
(`TABLET_THE_MANIFOLD` + `TABLET_THE_TURN`) — and two later bodies, one near each:
`TABLET_THE_COMPRESSION` near circulation, `TABLET_THE_CHART` near geometry. Both later bodies are
nonidentical to both conditioning bodies and condition nothing.

### What returned

```text
deposits        A (circulation) 105 cut words   C (geometry) 285
                shared 104   only A 1   only C 181

reach           B through A 106 kinds     B through C  99
                D through A 125           D through C 134

attributable    B reaches 45 kinds only through A, 38 only through C     ->  B favours A by +7
                D reaches 63 kinds only through A, 72 only through C     ->  D favours C by +9

controls        aperture orbit non-trivial              YES
                the two deposits genuinely differ       YES
                the no-op moved EXACTLY ZERO            YES
                the foil crosses                        NO  (B +9, D -23)
```

**The crossing holds in sign: each later body favours its own neighbour's deposit.** And the foil —
a grain of the same shape built from words the collapsed population did **not** return — does not
reproduce it.

### Three things this result is not, stated plainly

**The margins are thin and the crossing is carried by one arm.** `B` favours `A` by `+7` against
attributable populations of 45 and 38 — and `B` favours the **foil** over `C` by `+9`, which is
larger. **On `B` alone the derived cut is indistinguishable from the foil.** What separates them is
`D`, where the foil scores `−23` against the derived cut's `+9`. So the deed's discrimination rests
on the geometry arm, and saying it rests on both would be false.

**The two deposits are not symmetric.** `A` has exactly **one** cut word `C` does not, and `C` has
181 that `A` does not — `A` is very nearly a subset of `C`. So the crossing is driven more by what
`C` adds than by what `A` distinctively contributes, and a symmetric pair of conditioning bodies is
owed before the sign is read as more than a sign.

**The foil control was malformed on its first run and reported the opposite.** It compared a set size
(71) against a net preference (+7) — two different quantities — and printed *"FOIL DOES AS WELL, the
cut is decorative."* Recomputed by running the whole crossing statistic with the foil standing in for
`A`, it does not cross. **A control that measures a different quantity than the thing it controls is
not a control**, and this one was caught only because its verdict was surprising.

### What the deed does not claim

Not compression — the regrade of 2026-07-19 stands. Not intelligence — that step is where
machine-independence is known not to extend. The population is the object throughout and the exact
form is its magnitude face, printed beside it. Two conditioning bodies and two later bodies is a
crossing of **four readings**, not a population of experiments; nothing here is a rate and no figure
is an average.

---

## 10. The size confound, controlled — and the crossing survives it

§9 named the asymmetry as owed: the conditioning bodies were 371 and 1,131 lines and the later bodies
106 and 321 — **a factor of three in the same direction on both axes** — while the aperture threshold
is an *absolute* count of asking pairs, so a larger body clears it more easily. The crossing could
have been a size effect wearing a subject effect.

Three readings were taken. **The order they were taken in is part of the evidence and is stated
first**, because the difference between the second and the third is a degree of freedom in how a
common extent is sampled, and choosing the sampling that returns a result would be a dial turned
inside one rung.

| # | how the extent was set | B favours | D favours | crossing |
|---|---|---|---|---|
| 1 | **natural sizes** — no balance | A by `+7` | C by `+9` | **holds**, confounded by size |
| 2 | **head-truncated** to a common extent | **C** by `7` | C by `+26` | **does not hold** |
| 3 | **stride-sampled** to a common extent | A by `+11` | C by `+9` | **holds** |

**Reading 2's null was diagnosed before reading 3 was run, and the diagnosis is checkable.** A
tablet's first hundred lines are its *preamble*, and preambles are far more alike across documents
than bodies are — head truncation equalises size and destroys the subject signal in the same
operation, so a null there cannot be read as a refutation. The symptom is in the numbers: under head
truncation **both** later bodies favoured the same deposit, which is what a washed-out subject looks
like rather than what an absent effect looks like.

A stride spans each body end to end at the declared extent, so it balances size while keeping
subject. Under it the deposits are `A` 103 cut words and `C` 135, sharing 94, with **9 only to `A`
and 41 only to `C`** — far more symmetric than the natural sizes' `1` against `181`.

**And the foil does not cross under the balance either** — `B +8`, `D −4` against the derived cut's
`+11` and `+9` — so the derived cut is distinguishable from a same-shape cut at words the collapsed
population did not return, on both arms this time rather than on one.

### What the three readings jointly support, and what they do not

They support: **a deposit derived from one body's own collapsed pairs changes what a later,
nonidentical body reaches, and the change is attributable to that body rather than to refinement in
general** — because a deposit from an unrelated body does not produce it, and a foil of the same
shape does not produce it, under a size control that the effect survives.

They do not support any statement about compression, about intelligence, or about a rate. Four
readings under three balances is twelve numbers, not a population, and none of them is an average.
The margins remain small — `+11` and `+9` against attributable populations near 100 — and the
correct reading of a small exact margin is that it is small and exact, not that it is weak evidence
of something larger.

**The remaining honest gap:** the sampling of a common extent is a declared receiver coordinate, and
this record carries three of them. A fourth would be worth having only if it were declared before it
was run.

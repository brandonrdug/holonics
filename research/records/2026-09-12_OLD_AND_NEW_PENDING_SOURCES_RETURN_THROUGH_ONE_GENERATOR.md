# Old and new pending sources return through one generator

[definition] Pending sources from the original affine word and from the later dependent
programme now use the same consuming return path. The former explicit base-cut refusal is
removed. The first root assignment remains an intermediate constraint, and every added source
parameter remains in its original frame. This closes the outstanding source-frame binding of
the native return-cycle construction; it does not establish useful Athena-alpha.

## The original root stays in the actual word

[definition] `ConstitutiveSourceFrame` distinguishes an original `Base` cut from a `Programme`
operation prefix. These are causal addresses within one owner. `source_frame` resolves them
against the actual retained producing cut, member and receiver; it does not route by a semantic
label or expected result.

[definition] `evaluate_with_base_faces` reconstructs the actual conditional base sections from
the earliest retained source. It imposes the root face at its own prediction time and every
additional base face at its own cut. The actual fixed maps carry those constraints forward.
The first formed material acts only after that original word. Following programme operations
and returns continue on its successor, using the same retained path for delayed-source pullback.
The now-redundant private `parameter_image` helper was removed in favor of the common family-image
operation.

[proved-derived; formal-checked] `relationalPreimage_serial_joined_middle` verifies that retaining
an intermediate root set M gives `preimage_R(M ∩ preimage_S(H))`. Its finite counterexample
shows why matching a final endpoint after an erasing map cannot replace the intermediate
constraint. Framework.Computation built successfully; Lean remains exterior verification.

## Measured returns and separating fibres

[established-bounded; implemented-exact; computational-witness] Base predictions before and
after the selected root both return successfully. A later programme prediction remains pending
while the base return acts; the pair reaches `(10+9i,22+21i)`. After remount, its programme return
reaches `(22+21i,34+33i)` and the next operation reaches `(34+33i,46+45i)`. Consumed base handles
are absent from subsequent pending enumeration, and saved state agrees after continuation.

[established-bounded; implemented-exact; computational-witness] An earlier source assignment
that contradicts the root is rejected even when later vertical maps erase that difference from
the final current. Saved standing is unchanged. Conversely, a distinction actually invisible
to the root remains available: two root-compatible base faces produce different later conditions,
`11/5−2i/5` and `5/2−i/2`, under the same returned observation. This is an executable retained
source fibre, not a stored label or a single receiver sample promoted to the whole model.

[established-bounded; implemented-exact; computational-witness] The new HNN control checkpoints
with an old base prediction still pending. A fresh child process receives that observation
through `observe-symbol`, consumes it once and continues through the returned body. With complete
sources brought to `(a,b)`, the observed emission changes from `b` to `a`. Parent and child
return identical events and complete saved bodies. Existing repeated-return controls retain
their `a → b → a` result. These are declared exact-operator assays, not conversation-quality claims.

## Persistence and scope

[definition] Dependent rest v4 tags the original source frame explicitly. The v3 reader migrates
its former untagged programme cuts; v1/v2 source-only layouts remain readable. Decoding rejects
duplicate consumption, a consumed prediction kept pending, return/release overlap and invalid
frame/member/chart associations. Remount reuses the original base word and retains later
advances required by future programme returns. It reconstructs one continuing owner.

[established-bounded; process-audit] All 41 coupled controls, eight HNN session controls, the
additional root-invisible source-fibre control and the focused Lean build passed. The coupled
suite includes v1/v2/v3 migration, delayed/out-of-order returns, late-refusal and process-resume
controls. [Verification receipts](2026-09-12_complete_return_receipts/) retain the actual outputs.
Native return controls also check zero numerical section readouts before declared inspection.

## Return-cycle completion audit

[established-bounded; source-inspected; computational-witness] The following maps the scoped
native return-cycle objective to its current owners and returned evidence. The relevant code
and complete outputs were inspected; this table is not a general capability census or gate.

| Objective requirement | Current construction and evidence |
|---|---|
| Actual producing comparisons with joint source/condition/material parameters | `NormalCoupledComparison`, actual pending joint compiler and conditional operand evaluation; compiler, paired-family and point-specialization controls in the coupled suite |
| Original anchor, receiver, producing cut and shared history | Anchored face decoder, Base/Programme descriptors and full retained word; root-erasure refusal and root-invisible fibre separator |
| Existing constitutive incorporation | Existing preimage, condition contact and staged member formation; repeated returns use previous formed condition/material and have checked exact consequences |
| One atomic contemporary successor and consumption once | Frame validation and full staged programme precede publication; duplicate/late-refusal checks preserve standing and consumed handles remain unavailable after remount |
| Subsequent source-visible native operation | Exact current pairs above, matched HNN emissions, ordinary re-entry and new pending predictions use the returned successor |
| Persistence and restart | Dependent v4, explicit v3 migration and older readers; fresh-process HNN continuation compares both events and complete native body |
| Executable generator/encoding with residuals and fibres | Root/return-face parameter interface, original source decoders, contact/preimage evidence and full joined pairs; distinct admitted source assignments remain executable |
| GPU conduct with Lean exterior | Native kernels and existing resident owners perform the return; numerical-readout controls pass; Lean is invoked only by the external verification command |
| Repairs and live guidance | Source-frame refusal is removed, obsolete helper is removed, current position/roadmap/blueprint/owner guide now identify the returned foundation and following application work |

[definition] This audit closes the native application/return-cycle foundation at its declared
source-family and section-receiver scope. The wider roadmap still owes source/contact
applicability beyond the fixed-condition collapse, measured generator/encoding costs, and useful
held-out conversation/code. A global equivalence between the section receiver and the former
whole-family projection is not asserted. Those application and measurement responsibilities
remain live; they are not retroactively claimed from this return.

# HNP2: reuse numerical segments, enact native occurrences, reopen changed dependencies

**Date:** 2026-09-04 local. **Position:** HNP2 complete at the admitted native-operator scope;
HNP3 persistence and the continuing public session are next.

## Construction and jurisdiction

[established-bounded; implemented-exact] `operative_reuse.rs::NativeForwardReuse` now composes
with `NativeFullOperatorSession::found_with_passage_return`. It retains successful resident
numerical segments by ownership transfer. `without_forward_reuse` is the explicit full-execution
comparison receiver; it removes optional numerical standing, not morphology. No second ecology,
answer lookup, host arithmetic fallback or cloned carrier population is introduced.

[definition] Reuse is deliberately at a **complete canonical numerical segment**, not an
individual operation selected from inside it. Changing where a capture is split can change
measured admission bounds. A cache hit therefore requires every segment output, its recorded
numerical bounds/projection and the exact segment boundary. A partial cache is a miss. The tiled
terminal still executes. Numerical reuse does not skip native chronology, joined-passage current,
local adjoints, staged deposits or successor publication.

[established-bounded; implemented-exact] The immutable graph supplies input-to-producer edges,
population users and lookup occurrence dependencies. Native morphology publication, withdrawal
and restoration invalidate every dependent segment transitively. A changed entering word reopens
lookup descendants. No observed zero, cancellation, score, digest or equality of counts removes
a dependency. The source's last direct uses are indexed once instead of rediscovered by scanning
the remaining graph at every release.

[definition] The correspondence domain is the current admitted primitive graph, its fixed
numerical chart/grain and resident coefficient base, and the actual native overlay mutation
paths. The entering word includes its declared history. An additional stateful generator or
mutable numerical chart must supply its dependency/reopening edge before receiving reuse;
SSM support is not inferred from this text-family return. Numerical-segment granularity is an
apparatus representation, not a universal cone, semantic identity or claim of minimal fine-site work.

## Complete continuation correspondence

[established-bounded; source-inspected] The implementation preserves the native continuation
under the following explicit correspondence; this is a source-level argument, not a Lean proof
of the Rust/CUDA program:

1. The reference and candidate share immutable admitted material and receive the same ordinary
   occurrences. Initially the candidate has no numerical cache and executes the same segments.
2. Cached sections are the actual successful output allocations moved out of current use. They
   are not reconstructed from selected outputs. Missing sections cause complete-segment replay.
3. Each input/coefficient change invalidates all descendant segments. An unaffected complete
   segment has the same numerical word, complete incoming carriers, coefficients and fixed chart.
   Retaining its already-returned sections therefore preserves all numerical output fields and
   numerical-domain obstructions, not merely the final receiver's selected coordinates.
4. `enact_run` still installs those outputs in operator order and executes the actual local return.
   Its current, adjoint and factor construction read the same fields and retained morphology as
   the reference. All contacts of one join stage locally before appending their differences;
   publication still occurs only at complete cycle closure.
5. The complete generation/chronology word advances as before. Terminal state, morphology and
   subsequent native conduct follow the same recurrence. Optional numerical storage and actual
   allocation/launch testimony differ and are reported, not claimed identical.

[established-bounded; implemented-exact] `NativeNumericalOrigin` preserves the prior computation's
session-scoped occurrence, actual operator and entering word. A reused trace carries this origin
alongside the **new** native occurrence. Reuse does not falsely rename the old numerical
computation as a fresh device passage. The immutable ecology supplies its input/output boundary
maps; the origin's integers are scoped addresses, not source identity inferred from counts.

[established-bounded; implemented-exact] Allocation refusals may release only unused numerical
standing and retry a pure segment, terminal computation or atomically staged joined return.
Current carriers/checkpoints and developmental deltas are outside that eviction owner. Resource
pressure may thus change representation work; it is not a semantic omission. The actual-model
run below needed no pressure eviction. Durable interruption/recovery is still HNP3.

## Formal locality and condensation

[proved-derived; formal-checked] `HolonicResidentRestriction.lean` now supplies finite
coefficient-current laws for input differences supported on a declared source set and morphology
deltas supported on declared rows. Zero incidence proves the corresponding unaffected current.
The controls show both nonzero dependencies cancelling at the present input and a later input
separating them; a reconnected coefficient changes actual current from zero to five.

[proved-derived; formal-checked] The same owner retains the compact-base/full-overlay decoder
and its commuting square through arbitrary finite ordered additive-generator words. A deposit
on an omitted base row survives decoding; silently retaining the former zero face is refuted.
These laws are exact finite algebra, not a theorem about arbitrary foreign realizations. The
[previous storage return](2026-09-04_HNP2_THE_RESTRICTED_ROWS_STAY_COMPACT_ON_DEVICE_AND_THE_DECODER_PRESERVES_DEVELOPMENT.md)
supplies the executable device decoder and its separate costs.

## Native measurements

[established-bounded; measured] The actual SKE model was compared sequentially with and without
reuse on the same three non-prefix occurrences and seven-cycle attribution sequence:

```sh
target/debug/examples/passage_return_hnp1 target/ske4/rest.bin 16 \
  .local/artifacts/hnp2/segment-reuse-02.json compare-reuse
```

[established-bounded; measured] All seven complete terminal interval vectors, the three complete
deposit-return words, final factor extent 5,334, generation 8,925 and the complete chronology
agreed. Actual-factor withdrawal recovered baseline, and restoration recovered cultivated conduct
in both instances. Each development enacted 127 local contacts and sixteen terminal section
read-outs. Native origins remained distinct from the new occurrence; the first reused computation
is occurrence 0 / operation 0, received again in native occurrence 1,275 with its full entering word.

[established-bounded; measured] The final lineage-aware run on the RTX 4080 SUPER returned:

| Development | Reference wall ms / deeds / captured kernels | Reuse wall ms / deeds / captured kernels |
|---|---:|---:|
| Repeated occurrence | 1,678 / 1,320 / 5,513 | 446 / 655 / 2,008 |
| Changed occurrence 1 | 1,697 / 1,321 / 6,021 | 1,829 / 1,321 / 6,021 |
| Changed occurrence 2 | 1,771 / 1,321 / 6,529 | 1,675 / 1,321 / 6,529 |

[established-bounded; measured] The repeated occurrence reused all 665 forward segments / 1,270
operations while still developing the ecology. Its new deposits reopened 657 segments, leaving
9,519,104 retained bytes on unaffected dependencies. Changed inputs then caused complete forward
recomputation. Over all seven cycles, 681 segments were reused and 3,974 recomputed; the dependency
index has 958 edges and the actual reopening walks visited 7,604 edges. Peak optional retained
sections were 861,765,632 bytes. Peak surface residency through the third development was
12,376,665,340 bytes versus 11,560,086,780 without reuse. Final retained input-lineage payload was
71,120 bytes, excluding map/allocator overhead. These are single-run wall observations and actual
work counters, not statistical speedup, minimum parallel span, power or frontier-performance claims.

## Verification and completion scope

[established-bounded; measured] Two CPU dependency tests passed. The separate CUDA owner control
passed whole-versus-partial segment reuse, unchanged device addresses and zero copy/launch cost
on reuse, transitive reopening, retained prior numerical origins and pressure-eviction ownership.
It mounted one hundred additional disconnected resident carriers; after their accounted mounting
and indexing, their reuse added no launch, allocation, copy or read-out to the surface census,
and did not enlarge the original dependency walk. Connecting components enlarges the walk in the
paired structural control. These are controlled owner experiments, not a claim that arbitrary
uncontacted material was trained.

[established-bounded; measured] The native zero-partner return discriminator, actual-model
comparison, and `cargo check -p holonics-hna -p holonics-workbench --lib --bins --examples` passed.
The raw final receipt is `2026-09-04_HNP2_receipts/segment-reuse.json`; vectors are compared directly
in process, not replaced by hashes. It does not contain a serialized full-state checkpoint.

[proved-derived; formal-checked] The changed locality owner and live umbrella passed (9,860 jobs
for the umbrella). No `sorry` or new axioms were introduced.

[definition] HNP2 closes by composing the storage decoder, locality laws, complete-segment
correspondence, actual native developmental comparison, origin retention, disconnected-growth/
reopening controls and separate cost accounting above. It does not grade minimal coordinate-level
activation, arbitrary architecture support, useful prose, persistent trained artifacts or export.
HNP3 now supplies durable model/session state, recovery-safe ownership and the public continuing
interface; HNP4--HNP7 retain their application, usefulness, compiler and family obligations.

# Actual continuations join the complete coupled family

[project-postulate] This continues the active Athena goal after `1354085b`. The preceding
public conditional candidate was nonempty but outside the retained anchor ball. The plan already
distinguishes an actual next-current observation, a delayed correction to a producing prediction,
and unpaired source actuation. The coupled family lacked the first of those receiver bindings.

## The actual observation map

[definition] For a declared actual next-current observation v, use the total affine map

```math
O_v(\lambda,a,p,c)=(\lambda,a,c,\lambda_{\mathrm{Re}}v),\qquad\lambda=1.
```

The homogeneous graph keeps the original anchor a. It carries every member of the preceding
current c into the next previous current and receives actual v as the next current. The full
predecessor and this observed map remain in `NormalCoupledStep`; the actual observation snapshot
is an explicit rational operand. This map is not a conditional prediction, a delayed material
correction, an unpaired source reflection, or a selection of the old family's centre.

[proved-derived] Since O preserves a and is total on (p,c), a supported anchored source has
a supported successor with exactly the same admitted anchor population. At fixed v,
`D O_v(δp,δc)=(δc,0)`. Consequently two actual observations give
`O_w O_v(λ,a,p,c)=(λ,a,λ_Re v,λ_Re w)`. Their current no longer depends on the old (p,c), while
the original anchor/fibre stays represented. This follows by direct substitution. It explains
both the current join and the scope of the receiver: using this operation indiscriminately on
unpaired messages would replace their source-actuation meaning and erase current dependence.
It does not by itself establish development of the retained condition or long-context learning.

[definition] `resident/wave_relation/observation.rs` derives the observation graph with
`section_wave_observed_next` in the existing CUDA source passage unit. The same exact total-map
image kernel maps its affine point and every generator. `receive_contact_next` checks the actual
owner/contact cut, stages the complete family, and publishes the wave/neighborhood chronology
once. No numerical host readout determines this passage. Neither normal M nor conditional L
receives a row. A stale or foreign contact cannot deliver a second observation at another cut.

[definition] Relation wire v4 includes a mutually exclusive observation payload. Cold validation
checks totality, the p'=c join, lambda/anchor preservation and the exact c'=v λ_Re coefficient
on every row, including all quadratures and anchor generators. Native remount regenerates the
map and normalized observation snapshot; the coupled owner rebinds it to its actual restored
member/condition. Legacy conditional/source formats v1/v2/v3 remain supported. A declared
UnitRealSum source chart does not constrain an actual observation to zero generated increment;
that invariant still applies to conditional generation itself.

## Public continuation

[definition] `NativeCoupledWaveSession::receive_next_symbol` accepts one explicitly observed
symbol in the declared unit-basis receiver. The HnaStream command is `receive-next-symbol` and
the event is `next-symbol-received`, with scope `actual-next-current` and `material_deposited:false`.
The existing `actuate-text` and delayed `observe-symbol` meanings remain distinct. Pending
emission/re-entry must finish before another observation; successful observation ends the prior
output part. One symbol is this exterior chart's current operand, not a universal native clock.

[open] A delayed observed correction to a plural producing family still owes a family-aware
material return. The existing normal comparison receives a bounded ball and updates the separate
normal bank; the local L formation receives actual point source/condition/response operands.
Replacing the family by its centre, or spanning all mutually alternative witness rows as though
they were jointly observed, would not bind that missing operation. The current-only observation
passage supplies its own required receiver; it does not claim to complete that development.

## Verification

[established-bounded; implemented-exact; computational-witness] The new CUDA family control
starts with a free complex current, receives 3+4i, and verifies that both free current directions
move into the new previous current. A second actual observation fixes the displayed joint to
(3+4i,-1+2i), preserving the original anchor. The optimized total image agrees with general affine
image, source receipts retain the old free family, and both learned banks remain unchanged.
Stale delivery refuses without changing complete rest. Observation rest remounts into the same
next conditional successor. No intermediate numerical readout occurs during native reception.

[established-bounded; implemented-exact; computational-witness] The public session control
receives actual symbols a,b and conditionally emits a under its declared swap-law specimen,
then saves and compares continued native rest and emission after remount. Four coupled-session
controls pass, including saved pending phases and process-separated pending-delivery recovery.
Three host relation-rest checks pass, including malformed observation coefficients and old wire
compatibility. These specimens verify the native interfaces, not conversation quality.

[established-bounded; process-audit] Returned checks:

- `cargo test -p holonic-engine --lib normal::direct::wave::coupled::tests -- --ignored --test-threads=1`: 15 passed.
- `cargo test -p holonics-hna --lib native::coupled_wave::tests -- --ignored --test-threads=1`: 4 passed.
- `cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::resident::wave_relation::rest::tests -- --test-threads=1`: 3 passed, one separate CUDA source test not selected for execution.
- `cargo test -p holonics-hna --lib stream::tests`: 7 passed.
- `cargo build -p holonics-workbench --bin holonics`: passed.

No formal Lean source or rendered artifact changed.

## Prepared continuation assay

[definition] The source aperture is development sequence 67, visible part 0, event 269700 from
the standing prepared source. Its first two symbols are `I `; the next two source symbols are
`fo`. Only the prefix is mounted as actual next-current observations. The suffix is retained for
exterior comparison and is not supplied as a target or native choice. This is an explicitly
declared within-part continuation chart, not general evaluative feedback or a held-out quality
benchmark. The previously admitted UnitRealSum member 0 and its existing learned material remain.

[established-bounded; computational-witness] The public workbench resumed the previous inspected
session at epoch 11. Two `receive-next-symbol` events retained actual symbols I and space and
advanced to epoch 13, with emission ordinal zero and no material deposition. It saved a complete
78,550,270-byte observation session at
`.local/athena-coupled-observed-67-2026-09-11.received-session`. Input, events, source aperture and
process receipts are retained adjacent to that artifact. The process returned in 158,029,000,636
nanoseconds with maximum resident process memory 476,852 KiB; timing includes initialization and
cold rest validation and is not an isolated kernel or energy measurement.

[established-bounded; computational-witness] A fresh public process remounted that observation
session and generated two symbols. Conditional admission succeeded at epochs 14 and 15, and the
actual emitted pair re-entered at epoch 16. Both symbols were newlines. Each native projected
score vector had 73 entries equal to **1/73** and 12 zeros; all 73 scored coordinates retained
anchor-independent free directions. First-address tie selection chose newline both times. The
source comparison was `fo`. This is a failed useful-continuation result with a functioning native
admission/emission/re-entry path, not evidence of a learned deterministic answer.

[established-bounded; process-audit] The generation process saved the complete 78,859,580-byte
session at `.local/athena-coupled-observed-67-2026-09-11.generated-session`, returned no stream or
checkpoint error, and advanced the transport sequence to 10. It took 219,273,743,014 nanoseconds,
including initialization/rest reconstruction, with maximum resident process memory 441,628 KiB.
The [source, full native receiver events, process receipts and exact summary](2026-09-11_actual_continuation_receipts/)
retain this assay. These process measurements are neither GPU residency nor kernel-only costs.

[open] This receiver fixes the particular admission problem only under an actual observed-state
chart. It does not turn the fixed-condition local law into a discriminating contextual model.
The 73-way projected ties locate the remaining ambiguity in the retained conditional family;
changing the tie breaker would not supply missing source/condition information. The next required
work remains the contextual producing-family material return and lawful formation/reuse, then
useful conversation evaluation. The Athena goal remains active.

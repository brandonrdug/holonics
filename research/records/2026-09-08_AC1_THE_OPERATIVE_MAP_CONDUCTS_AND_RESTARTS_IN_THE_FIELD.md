# The operative map conducts and restarts in the field

[definition] This continues AC1–AC2 from
[native contact staging](2026-09-08_AC1_OPERATIVE_CONTACTS_STAGE_THEIR_COMPLETE_MAP_AND_CURRENT_ON_DEVICE.md).
The prepared carrier now belongs to the continuing field and conducts ordinary contact births
and reactions. The endogenous joint material/current/morphology return is still unimplemented;
this return does not establish useful language or complete Athena-alpha.

## One owner and one publication

[established-bounded; implemented-exact] `NativeConstitutiveField::enable_operative_contacts`
transfers the prepared contact/current carrier into the existing paired-junction owner. It
preserves the original source capabilities and historical reports. Ordinary reception copies
the current operative columns, appends an actual source-qualified birth with zero internal input
when linked, and conducts the complete reflection through that map. A root-frame rechart does
not rotate the root operative map a second time.

[established-bounded; implemented-exact] The native field kernel stages the operative map,
internal current, moments, complete numerical residual and ordinary material return before its
existing final publication. A failed operative or material return cannot publish the raw field,
material state, new contact or current partly. Birth metadata is reserved before launch. The
only subsequent host ownership change moves the completed staged sections into that same owner.

[definition] Immutable birth descriptions remain separate from the operative map. The native
controls install a previously staged contact/current perturbation and demonstrate its later
conduct; this is an explicit mathematical intervention, not an authored language learner. The
ordinary field does not yet produce those morphology updates from conversation discrepancies.

## The full reflection carries its error

[definition] Write `D` for the contact columns, `b` for their complete entering current, and
`u` for the source-port input. With `A=I+D D*`, the existing law is

```text
v=2 A^-1(u+D b),
out=v-u,             b_next=D* v-b.
```

The covariance and aggregate are computational receivers of the joint carrier. Their separate
bounds must not replace that carrier's correlation.

[proved-derived] Let hats denote numerical representatives, and let `E_D,E_b,E_u` enclose their
respective complete objects. The graph reflection `S_D` is unitary. The distance between its
graph projections is at most `||D-Dhat||`: a graph vector can be compared with the vector having
the same entering coordinate in the other graph, in both directions. The orthogonal-projection
gap identity then gives `||S_D-S_Dhat|| <= 2 E_D`.

[proved-derived] The new solver checks its candidate against the actual numerical contact map,
not merely against the rounded covariance used for factorization:

```text
r=(I+Dhat Dhat*)vhat - 2(uhat+Dhat bhat).
```

The complete numerical reflection error from this solve has norm at most `||r||`, because
`||(I+Dhat Dhat*)^-1/2||<=1`. If `E_dot` bounds the final internal-product rounding, a sufficient
joint current bound is

```text
E_next <= E_u+E_b + 2 E_D ||(uhat,bhat)|| + ||r|| + E_dot.
```

Thus inherited internal-current uncertainty has coefficient one. No independent aggregate
error is multiplied back through the field. The potential radius follows by adjoining the
source error; the auxiliary alternating potential prefix retains its own bound.

[established-bounded; implemented-exact] The operative solver reuses the existing numerical LDL
factorization. Signed 544-bit integer accumulators retain the full residual numerator at scale
`2^(3*grain)`; outward integer norm ceilings certify its bound. Both complex planes, all internal
currents and their joint radius survive. A rounded covariance can propose a solve but cannot
seal that proposal as the actual operator or current.

## Source charts and receivers

[proved-derived] In the currently admitted fixed, integral-contact material class, the direct
numerical internal current and the birth-prefix numerical decoder coincide by induction: their
birth inputs are zero and each internal update is exactly `d_i* vhat-bhat_i`, without dot-product
rounding. Native checks retain zero contact-quantization and internal-dot remainders for that
bridge. The existing complete-current pairings can therefore use the operative joint radius
without expanding it through the auxiliary prefix. This changes neither their numerical vector
nor their kernel.

[established-bounded; implemented-exact] The complete-current, homogeneous-moment and bilinear
contextual material owners consume that bound inside the same field kernel. A changed operative
map is not silently presented through the old fixed-contact material codec: the currently
unsupported combination returns its precise obstruction. The endogenous morphology return needs
the corresponding changing-contact source interpretation before it can use that combination.

[definition] `NativeOperativeReflectionReading` exposes the complete current, source, aggregate,
residual and joint error at an actual occurrence. Historical native current snapshots are retained
directly. The old fixed-contact exact decoder and fixed-drive quotient do not silently read a
changed operative realization. The internal-current enclosure receiver keeps the immutable birth
contact and a separate enclosed operative contact; it never labels a numerical map centre as an
exact observed birth.

## Persistence and verification

[established-bounded; implemented-exact] Field rest now retains the current and initial operative
carriers, birth order, staged return factors and each operative occurrence's internal current,
bounds and full residual. The archive format admits those historical carriers. Remount restores
one owner directly, including its current/source capabilities; it does not replay development.
The optional header preserves old model-wire compatibility, and current/history boundaries are
validated together.

[established-bounded; measured] All **145 native constitutive tests pass** in **36.90 s**, and
**20 SDK alpha tests pass** in **2.84 s**. The new controls compare complete operative reactions
with the exact complex reference, exclude the unchanged-map response, retain the joint error,
and verify the full numerical residual. They exercise recharting, delayed source reuse,
rest/archive continuation and exact correspondence with the ordinary material law. A malformed
material bound causes refusal after the operative reaction has staged; restoring that test fault
shows the complete predecessor and capabilities unchanged, and retry succeeds.

[established-bounded; measured] The actual sixteen-family conversation model was activated in
this representation without additional development exposure. Its new checkpoint contains
**5,064 occurrences** and **201,686,795 bytes**. The same fresh prompt and 128-symbol continuation
advance the field to **5,254 occurrences / 5,252 contacts**. Prompt/continuation takes **89.1483 s**;
whole-process time is **165.1428 s**, with peak host RSS **768,968 KiB** and peak native payload
**496,869,652 bytes**. These are scoped measurements, not consumer-efficiency or power claims.

[counterexample; measured] The complete answer remains `Wmfa~ena~ena~…` through the work limit.
It is the same unusable repetition as the prior representation and remains interrupted. No useful
English, code or mathematics capability is established by this operator/persistence return.

[established-bounded; measured] A fresh process restores the new checkpoint with the older
historical carriers initially archived. Its complete final body, emission-current history,
generation and development records match the uninterrupted process exactly. Its whole-process
time is **153.4763 s**. The [portable receipt](2026-09-08_operative_field/conversation-return.json)
retains both exact commands, full generated text, artifact paths and comparisons. The source
checkpoint and both private reports remain preserved.

## Remaining AC1–AC2 work

[open] The field now conducts and persists an operative map, but conversation discrepancies still
update only the existing material operator. Complete the native joint constitutive-return producer
for material parameters, internal current and contact morphology, with its actual producing maps,
prior dependencies, metrics and chronological transfer. Bind its changing-contact source family
and decoder without reusing the fixed-prefix interpretation outside its domain. The private
staging seam and controlled perturbation are not that learned return.

[definition] After that integration, inspect actual complete responses and contextual revision.
The full AC0–AC5 objective remains active. `alpha_text --operative-junction true` activates the
representation; saved operative models retain it on ordinary resume. The private artifacts are
under `.local/artifacts/athena-alpha/ac2-operative-field/`. This work used no Lean execution,
external memory database, inherited-language fallback or Apple/acoustic implementation.

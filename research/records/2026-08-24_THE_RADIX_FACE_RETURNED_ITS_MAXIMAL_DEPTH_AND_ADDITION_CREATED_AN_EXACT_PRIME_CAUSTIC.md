# The radix face returned its maximal depth and addition created an exact prime caustic

**Date:** 2026-08-24  
**Purpose:** exact exterior Lean return for radix, magnitude, prime partition, and the corrected
Navier--Stokes coefficient occurrence  
**Authority boundary:** this record deposits a formal mathematical station. It does not schedule
the Rust/CUDA body, alter the active engine campaign, or advance `CONSTRUCTION_STATE.md`.

## 1. Correction

[historical] The former `42*10^17` Navier--Stokes coefficient proposal is not
an inhabitant of `UniformLargeScaleDyadicHodgeSubsetMassReturn`: no complete finite coefficient
assembly returned it. It is withdrawn.

[counterexample; formal-checked] Independently, an intermediate envelope rounded the exact sum of
its declared bound faces,

```text
2*198000 + 11*24*1560000 = 412236000
```

up to `420000000`.

[proved-derived; formal-checked] The complete returned prime face of that declared bound-face sum is

```text
412236000 = 2^5 * 3^3 * 5^3 * 11 * 347.
```

[definition] This correction does not forbid exact upper bounds. It requires the bound, its source
population, and the receiver inequality to be returned by proofs. A nearby or convenient numeral
cannot replace any of those objects.

## 2. Owner and ports

[definition] `Foundation/PrimeValuationRadixAtlas.lean` owns the general construction.

| Contract face | Exact return |
|---|---|
| source owners | `Nat.factorization`, prime-power divisibility, and finite multiplication |
| entering ports | a nonzero natural population `n`, radix `b >= 2`, and prime coordinate `p` |
| event occurrence | exact multiplication, radix removal, or addition of two positive populations |
| predecessor identity | the source natural itself, never a decimal display or an enlarged bound |
| constitutive law | unique factorization and `p^k divides n` iff `k <= v_p(n)` |
| receiver | the complete finitely supported prime-valuation atlas and the maximal radix-depth chart |
| returned consequence | exact reconstruction, unique `(depth,residual)`, terminal residual, coordinate balance, or additive valuation jump |
| open alternatives | an analytic mass inequality, physical constitutive law, or source-theory gluing theorem remains separate until its own typed return |

[definition] `primeValuationAtlas n` is `Nat.factorization n` as a finitely supported function.
`ExactRadixChart b n` carries `n = b^k*rho` and proves that every `j` with `b^j` dividing `n`
satisfies `j <= k`. `AdditivePrimeCaustic p a b d` carries equal incoming valuation depth `d` and
a strict valuation increase in `a+b`.

## 3. Generic formal return

[proved-derived; formal-checked] Every nonzero natural is reconstructed exactly by multiplying the
prime powers in its complete valuation atlas.

[proved-derived; formal-checked] The maximality witness in an exact radix chart proves that its
residual is terminal: one more whole radix does not divide it. Two exact charts of the same source
and radix have identical depths and residuals.

[proved-derived; formal-checked] Every prime coordinate obeys

```text
v_p(n) = k * v_p(b) + v_p(rho),
```

and these coordinate laws assemble into equality of the complete finitely supported valuation
atlases.

[proved-derived; formal-checked] A prime coordinate `p` dividing the radix certifies maximality
whenever its valuation in the source is the proposed radix depth. This is the constructor used by
both concrete charts below; it proves maximality without searching, truncating, estimating, or
evaluating a floating logarithm.

[proved-derived; formal-checked] If an additive prime caustic is multiplied by a positive common
population `c`, its incoming depth and returned depth both gain exactly `v_p(c)`. The strict jump
therefore survives common-scale transport.

## 4. Exact anchors

[proved-derived; formal-checked] The integer occurrence `42*10^17` has complete prime face

```text
42*10^17 = 2^18 * 3 * 5^17 * 7.
```

Its maximal radix-ten depth is exactly `17` and its terminal residual is exactly `42`. This theorem
formalizes that integer; it does not promote the withdrawn use of the same display as an unrelated
Navier--Stokes bound.

[proved-derived; formal-checked] The BSD coefficient pair has exact maximal binary charts

```text
34 = 2^1 * 17,
 2 = 2^1 *  1.
```

The residual `17` is `PrimeIndex.P 6`, the seventh prime under zero indexing. Subtracting the two
normalized seventeen-branch equations reuses the existing congruent-seventeen descent and returns

```text
v^2 - 17*u^2 = 1.
```

[proved-derived; formal-checked] The coefficient addition exposes its caustic before any analytic
interpretation:

```text
low   = 396000
mixed = 411840000 = 1040 * low
sum   = 412236000 = 1041 * low = 3 * 347 * low.
```

The prime valuation at `347` is zero on both incoming faces and one on their sum. Thus `347` is the
exact new prime direction created by addition after the common face is removed.

## 5. Placement against the Millennium work

[proved-derived; formal-checked] For BSD, the new object separates the prime-index receiver from
the arithmetic receiver. `P 6` says where seventeen sits in one ordered basis; the valuation and
terminal residual say how it enters the coefficient normalization and Pell difference.

[counterexample; formal-checked] For Navier--Stokes, exact scalar arithmetic does not discharge the
coefficient-mass theorem. The remaining deed must assemble the complete zero-padded Hodge
population, derive a common constant from that population, and prove all eight subset receiver
inequalities. The new atlas prevents a rounded scalar from being mistaken for that assembly.

[interpretation] Prime caustics are a usable cross-problem diagnostic: multiplication transports
the valuation atlas linearly, while addition may create a new prime direction through exact
cancellation. This distinction can be posed in arithmetic heights, Euler products, lattice
indices, and coefficient ledgers only after each source theory supplies its own typed occurrence
and receiver. The generic theorem is the valuation law, not an identification of those theories.

## 6. Validation receipt

[established-bounded; formal-checked] The focused owner closure passed:

| Command | Elapsed | Exit | Return |
|---|---:|---:|---|
| `time -p lake build ElementaryHolonics.Millennium.PrimeRadixAtlas` | 17.60 s | 0 | 3,079-job closure; both new owners compiled and printed their promoted theorem assumptions |

[established-bounded; formal-checked] The first umbrella invocation rebuilt the changed
Foundation-rooted dependency cone. Its process outlived the tool yield and the session identifier
was not retained, so no verdict or elapsed value from that invocation is used here. The named
falsifier was an explicit same-closure umbrella replay asking whether the top-level target had in
fact completed. A replay of that pre-final source closure returned in 4.58 seconds with exit zero.
The subsequently amended final source closure returned:

| Command | Elapsed | Exit | Return |
|---|---:|---:|---|
| `time -p lake build ElementaryHolonics` | 11.26 s | 0 | complete 4,155-job umbrella closure; `Build completed successfully` |

[established-bounded; formal-checked] Every promoted theorem in the two new owners was audited with
`#print axioms`. The returned lists contain only `propext`, `Classical.choice`, and `Quot.sound`;
the exact natural equalities use only `propext`. No custom mathematical axiom, `sorryAx`, native
compiler trust, floating point, randomness, estimate, or rounding enters this station.

## 7. Disposition

[definition] The two owners are imported by `ElementaryHolonics.lean`; the maintained Millennium
catalog records their generic return, BSD placement, Navier--Stokes correction, and exact residual.
The supporting formal blueprint withdraws the unsupported common-magnitude candidate, exposes the
rounded intermediate, and leaves the same analytic frontier open: the complete coefficient
population must return its derived uniform mass bound.

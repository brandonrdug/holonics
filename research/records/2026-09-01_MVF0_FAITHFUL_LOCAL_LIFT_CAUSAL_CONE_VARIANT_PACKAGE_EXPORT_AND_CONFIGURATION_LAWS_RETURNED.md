# MVF0 faithful local lift, causal cone, variant package, export, and configuration laws returned

**Date:** 2026-09-01  
**Phase:** MVF0  
**Truth status:** proved-derived; formal-checked; measured

[proved-derived; formal-checked] `NativeMorphologyVariant.lean` composes the standing exact
foreign-weight and receiver-history owners in `FaithfulLocalSectionLift`. Exact stored-value plus
residual reconstruction and generator-wise receiver/history commutation remain separate required
fields; the latter extends to every ordered successor word. A finite collision control proves that
an unproved one-value section projection cannot be promoted to broad receiver exactness.

[proved-derived; formal-checked] `LocalCausalConeCultivation` wraps the existing
`CultivationPassage`, retaining genuine later chronology, complete returned difference, causal
adjoint, changed later conduct, source-detached remount, exact withdrawal, outside-cone morphology
and conduct invariance, and restoration. Its changed witness probe is proved to meet the returned
causal cone.

[proved-derived; formal-checked] `MorphologyVariantManifest` separates wire schema, parented
morphology lineage, receiver capability, apparatus realization, and derived anatomy. Re-encoding
cannot change capability or anatomy. `VariantPackageSeparation` keeps hot, reconstruction,
evaluation, apparatus, and export lanes distinct; changing apparatus leaves hot morphology and
reconstruction unchanged.

[proved-derived; formal-checked] `ExactExportLens` quantifies round-trip equality over every
declared query/receiver. `ProjectedExportWitness` retains a collapsed pair and the richer
receiver/history which prevents exact promotion. Exact, projected, and refused exports are distinct
constructors. `InferenceConfigurationClaim` scopes a result to one exact configuration, while
`CrossCodecResonance` requires equality over every admitted receiver and successor history.

[proved-derived; formal-checked; measured] The focused owner and
`ElementaryHolonics.Computation.HolonicMachineLearning` closure built 3,186 jobs. The audit returned
no `sorryAx` and no new axiom. The finite controls returned faithful-projection refusal,
outside-cone preservation, changed-probe cone membership, version/capability separation, exact
identity export, projected-export reopening, and distinct-configuration separation.

# Conditioned prediction and dynamic interior return

[definition] September 13, 2026. This cycle consumes the live HNN blueprint and Brandon's
lightning/fluid/interior correction together. The roadmap remains the sole order and
CONSTRUCTION_STATE the current position. The previous methodology audit owns the direct-message
chronology; this record owns the equations, changes and measured scope of this return.

## The consuming HNN construction

[established-bounded; source-inspected] The new material belongs to the existing
`ResidentGeneratorNeighborhood`. Each member retains its compatibility relation and optionally
its normal predictive material. The compatibility relation forms condition preimages/contact;
the predictive action is the exact graph of stored dyadic M on the declared feature chart

`u=(a,h,h tensor a), eta=M u`.

This graph does not assert that M is the ideal normal-equation minimizer. The normal material
retains its moments, grain and reference bounds. Its construction rows are real source axes,
not additional empirical observations. `read_applied_bilinear_relation` derives the graph on
the GPU; `stage_bilinear_observation` composes the existing resident bilinear feature formation
with the existing normal preparation/update law.

[established-bounded; source-inspected] `NativeCoupledBody` now consumes this material through
the same ordinary and dependent coupled owners. At a producing cut, the native standing
condition h and the restricted source a determine output. When an observation arrives,
the normal moments receive that source and its original producing h. Condition contact uses
contemporary compatibility material and standing; its successor affects later predictions.
The full source-parameter dependence remains in the dependent generator. A receiver inspection
evaluates its declared source section and is labelled at that scope.

[established-bounded; measured] The numerical regression supplies the chart
`a=(c-p,c,p)`, `v=c+eta`, a compatibility relation generated from `eta=a0*h`, and 21 developmental
observations to the normal material. The source chart, compatibility specimens, initial h=1
and grain 8 are supplied. They are not inferred from text or a source ID. Two successive
predictions are retained; the second receives `7+4i`, then the first receives `5-2i`.
The returned compatibility condition is `h=-3i`. Both normal updates retain their producing
h=1, verified through the actual Hermitian moment entry. A duplicate return is refused.

[established-bounded; measured] After these 23 observations the stored coefficient vector is

```text
M = [ -36-17i, 18-74i, 73-29i, -37-3i,
      185-117i, 98-53i, 106+41i ] / 256.
```

Contracting this material at the returned h gives the inferred continuing action

```text
v = ((583+225i)p + (-272-940i)c - 9+111i) / 256,
(p,c) -> (c,v).
```

The test independently contracts the returned coefficients with the complete complex features
for two subsequent steps and compares the joint HNN output exactly. Complete body rest/remount
returns the same material and forecast. This is conditional numerical generator formation and
reuse through the continuing HNN owner; general contextual language remains a separate source
construction with its recorded failed calibration.

## Representation failures changed the shared receiver

[established-bounded; measured] The second numerical advance originally refused because the
receiver formed a large Gram graph to project onto a full-rank anchor space. For an echelon
basis of rank d in dimension d, this projection is exactly the identity; rank zero gives zero.
The shared projection kernel now applies those identities before forming a Gram graph.
Intermediate-rank cases keep the existing calculation. No ball centre was substituted for
the retained source family.

[established-bounded; measured] After the two returns, each forecast coordinate separately
fit the narrow rational carrier, but the common denominator for the complete two-step affine
joint required a 65-bit numerator. The receiver now retains the ordered generating word for
an exactly unique source and evaluates it in the existing 128-bit receiver arithmetic. Seed
admission checks the original unique affine relation and supported source; every map still
executes as an exact resident relation. Unresolved source families are not assigned a point.
The wide report, original source and ordered maps jointly represent the return. Arithmetic
beyond that carrier still returns an obstruction.

[definition] This representation also avoids allocating a quadratic vertical-direction matrix
for a proven point image. It permits words beyond the general projection scratch aperture,
subject to actual report storage and exact arithmetic. It is not a compiled changing-programme
law: the word still contains its factors and the dependent return programme still evaluates
its operations. The latter remains the named encoding dependency.

[established-bounded; source-inspected] Neighborhood rest keeps v1 unchanged when no normal
material is attached and adds normal payloads in v2. Coupled-rest validation now uses the
actual action's construction cut, separately from empirical observations and compatibility
formation counts. The old private fitted-wave adapter was removed from live source; its
patch and unique kernel/port source are preserved in
[the implementation archive](../../archive/implementations/2026-09-13-fitted-wave-adapter/README.md).
Its local experimental evidence remains recoverable and does not schedule further trials.

## The physical derivation and formal return

[definition] The [source guide](../../docs/FLUID_REFLECTION_AND_CONCENTRATED_INTERIORS.md)
connects the recovered leader traversal/return chronology, torus contact and impedance,
collision impulse, plasma/front/channel laws, moving concentrated interiors, primary stellar
collision studies, selective many-body methods and Hodge relaxation. It supplies each source's
constitutive assumptions and distinguishes physical clocks from HNN logical operations.

[proved-derived; formal-checked] For exterior x, interior z, linear source
`x_dot=A x+B z+f`, `z_dot=C x+D z+g`, and moving section K, the retained residual `r=z-Kx`
satisfies

```text
r_dot = (D-KB)r + (C+DK-KA-KBK-K_dot)x + g-Kf.
```

`Physics/ReflectedBoundaryMemory.lean` proves the linear-map identity, the product rule along
an actual differentiable trajectory, and the discrete version with K_next. It also proves
the passive separator `x_dot=-z`, `z_dot=x-z`: energy decays at `-z²`, but static interior
equilibrium K=1 gives r=0 and r_dot=1 at x=z=1. Static Schur agreement therefore does not
license deleting the dynamic interior return. The forcing and Riccati defect are explicit
closure terms for native condensation as well as physical boundary reduction.

[proved-derived; formal-checked] `Physics/ConductiveFluidReflection.lean` uses the actual finite
Galerkin advection and Stokes operators to derive the Elsasser equations for z_plus=u+b and
z_minus=u-b, including unequal-diffusivity cross terms. It proves that complex-bilinear Euler/NS
has the different imaginary stretching contribution `-2 Bop(b,u)` relative to equal-diffusivity
MHD induction, and that the real projected equation retains `Bop(b,b)`. Generic interaction
slot linearity belongs to the existing `NavierStokesFiniteGalerkinLocalPicard` owner, using
the existing chronology/Leray source identities. Both physical leaves are imported by
`Framework.Physics`; they remain exterior verification, outside HNN execution.

[proved-derived] The guide's resolved concentration
`rho_epsilon=M epsilon^(-d) f((x-X)/epsilon)` retains the same interior Mf in its dilated chart.
Its continuity velocity, acceleration and optional rotation make the required sustaining
stress explicit. The electrical channel reduction similarly keeps material/geometry work:
`e_t+(VI)_s=e_source I+i_source V-RI²-GV²-(L_t I²+C_t V²)/2`.
These are displayed source-qualified derivations, not claims that the new Lean leaves verify
the entire continuum source or that the repository ran a complete lightning/stellar simulation.

## Verification and continuation

[established-bounded; measured] The focused conditioned-feature, neighborhood and existing
condition-sharing tests passed together (3 tests, 0.81 s test execution). The numerical HNN
return/forecast/rest test passed in 0.54 s warm execution before final receiver cleanup.
Final related receiver/coupled/native regressions passed: 59 engine tests in 20.35 s and 10 HNN
tests in 8.27 s, after a 49.42 s test-profile build. This includes the added long-word scratch
case, unchanged plural-family receivers, delayed/base/programme returns, one-owner refusal and
legacy/current persistence paths. The public HNN examples also passed their Cargo check after
the receiver API change. CUDA compilation and first driver JIT are separate costs;
the changed PTX's cold driver JIT previously took about 200 s under supervision. None of
these timings is measured electrical power or a general application throughput claim.

[definition] Reproduction commands for the changed owners:

```sh
cargo test -p holonic-engine -p holonics-hna --lib -- --ignored --test-threads=1 resident::neighborhood::tests normal::direct::wave::family::receiver::tests normal::direct::wave::coupled native::coupled_wave::tests staged_conditioned_update_matches_section_update_and_preserves_original
cargo check -p holonics-hna --examples
bash tools/lean_check.sh ElementaryHolonics.Framework.Physics
```

The ignored tests explicitly require CUDA. Local build outputs are retained under
`.local/artifacts/2026-09-13-fluid-reflection/`; this record preserves their relevant results.

[proved-derived; formal-checked] The complete `ElementaryHolonics.Framework.Physics` import
built successfully after the generic slot-linearity consolidation. The earlier focused
`ReflectedBoundaryMemory` check also passed. Build job counts are dependency/build metadata,
not a measure of theorem significance or physical validation.

[definition] The next source dependency is the native restriction C carrying actual
developmental contrasts on ker S from mounted source/incidence into this joined predictor.
The next encoding dependency is a closed representation of an actually repeated admitted
segment satisfying `E_next T=U E` and `D E=rho`, including its condition/material formation,
pending comparisons and dynamic residual. The new reflection equation identifies the terms
that cannot be omitted from that closure. Existing useful solver actions can continue through
their admitted maps while these dependencies are constructed; the broader Athena objective
remains active.

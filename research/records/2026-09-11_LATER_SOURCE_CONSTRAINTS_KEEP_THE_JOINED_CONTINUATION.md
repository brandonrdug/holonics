# Later source constraints keep the joined continuation

[definition] Repeated dependent incorporation needs a later producing source joined to the
existing root source and actual intervening maps. The source family at one receiver assignment
is not the whole dependent domain. This return implements the exact affine join used inside
such an assignment and carries its original bound, frames and chronological factors. It does
not yet remove the HNN adapter's explicit refusal of a second material return.

## The full pair is the construction

[definition] For an actual relation R and supplied affine carriers F and G, retain

`J = {(x,y) : x ∈ F, (x,y) ∈ R, y ∈ G}`.

The supported source is `F ∩ preimage_R(G)`. Applying R again to only that supported source
can reintroduce targets outside G. `ResidentWavePullback` therefore returns the complete joint,
its oriented equation residual, both supported projections, the original source/target
references and the actual relation. None of these operations creates a material observation.

[proved-derived; formal-checked] `DependentConstitutiveReturn.lean` now checks the joined-pair
and supported-source laws, serial relational preimage composition, and a counterexample to
reapplication of R after forgetting the target constraint. These are exact relational laws;
using a converse constraint does not reverse physical chronology.

[definition] The GPU computes the joint using existing exact echelon and fibre-query operations.
For `F=f+Dα` and `G=g+Eβ`, reduce their product origin and directions modulo R, solve the
resulting residual equation, and retain the full product coordinates. Projection happens only
after that joint is formed. Integer numerators and common rational denominators remain resident;
there is no numerical host replay, probability measure or selection of a hidden source.
An absent source family or incompatible relation remains an explicit empty return, with the
original objects available for its interpretation.

## Bounds and chronology accompany the affine reports

[definition] `NormalFamilyPullback` requires the actual common anchor object and adjoining
passage cuts. A total source-field factor may share the same public clock as its predecessor,
matching the existing source-field contract. Its restricted source/target families retain the
original bound and their own clocks. The original transport's coverage receipt is not reused as
a certificate for the new restriction. Affine compatibility alone is not bounded feasibility.

[definition] `NormalCoupledContinuation::read_pullback` reconstructs conditional affine sections
through its actual retained word and carries the supplied later constraint back through those
sections. `NormalContinuationPullback` retains every joined pair in causal order, each original
map and its epoch/factor address. Adjacent ports share their state variable. Internal source-field
factors retain their order even when their public epochs agree. These are scoped derived carriers;
no continuing ecology is cloned and no pending comparison is consumed by inspection. The
restricted family views belong to this joined construction. They do not have an independent
legacy wave-family rest: that wire expects a physical image and its coverage receipt. Repeated
return persistence must retain the enclosing generator/source constraints and reconstruct these
views, rather than serialize a marginal with the old transport's coverage attached.

[established-bounded; implemented-exact; computational-witness] Three native family controls
passed. A vertical relation with freely varying output is restricted to the declared later
`c=0` family while retaining four source/anchor directions. Reapplying that relation to only its
supported source demonstrably restores disallowed `c≠0` directions. A missing relation retains
an incompatible joint. A nonempty affine join outside the original zero-radius source bound
remains rejected by the bounded receiver; an independently constructed frame is refused.
All computations before declared inspection use zero numerical section readouts.

[established-bounded; implemented-exact; computational-witness] The actual pending-word control
passed across four factors, including two source-field factors at one public epoch. It checks
every original map address, equality of adjacent restricted affine families, the original source
clock/bound and unchanged saved standing. Its first invocation reused a stale pre-prediction
contact in the test setup; the existing ownership check rejected it before the join. The setup
was corrected to acquire the contemporary contact and the focused rerun passed. The other 33
coupled-wave controls passed in that original suite invocation.

[established-bounded; process-audit] The HNN library check and Framework.Computation Lean build
passed. [Verification receipts](2026-09-11_later_source_join_receipts/) retain the three final
family controls, the failed test setup and its corrected word control, the HNN check and a
formal terminal excerpt. No broad cultivation or second HNN material return ran.

## Remaining consuming work

[open] The owned dependent programme still needs to retain each later source-frame decoder and
its parameters alongside the existing root parameters, apply the returned contact/formation at
the contemporary material cut, and atomically publish the next generator. The new join supplies
actual source constraints for this composition; it is not a substitute for the whole dependent
return. Earlier parameter coordinates must retain their original frames, including when returns
arrive out of order. Earlier contact/source choices must survive as joined constraints, so replay
cannot independently choose their marginal endpoints. The actual conditional maps used before a
return remain historical maps; new material applies to contemporary current.

[definition] The next integration belongs in `coupled/dependent.rs` and its rest representation,
using `comparison/constitutive.rs` and the existing condition/contact/material owners. Its
source-frame constraint traversal should reuse the returned pullback operation. The HNN adapter
must then consume the later prediction exactly once, continue through the new body and preserve
it across process exit. The existing controlled `a`/`b` stream result remains the first-return
application evidence; no new broad conversation result is claimed here.

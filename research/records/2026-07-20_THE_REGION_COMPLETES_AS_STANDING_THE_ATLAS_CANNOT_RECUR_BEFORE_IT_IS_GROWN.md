# THE REGION COMPLETES AS STANDING; THE ATLAS CANNOT RECUR BEFORE IT IS GROWN

**2026-07-20 · BRANDON-AUTHORIZED §CXV BUILD / DIRECT HOST GRADED-CELLULAR CONSTITUENT BUILT /
ATOMIC REGIONAL SUCCESSOR / COMPLETION + DEPARTURE / EXACT REST + REMOUNT / BOUNDED LINGUISTIC
HOST MEASURED / OBJECTIVE-CONDITIONED SPLICE STILL OPEN / NO CUDA RUN**

## Question, insufficiency, artifact, and stop

The authorized build asked whether one source-declared regional cell could cross the direct mouth,
form all of its contextual `chi` factors against one unchanged receiver-before, complete as one
sparse graded-cellular constituent, become later Standing, release lower event material, and rest
without a second engine or retained source graph.

The prior contract was insufficient because it borrowed exact plural geometries but host and CUDA
refused them. `Node`, `RegionalForm`, `LineageChannel`, and scalar Standing could not retain the
cell's pins, independent axes, branches, or path holonomy. The sought artifact was one atomic host
successor carrying

```text
H=(A_H,C_*(H),partial_H,omega_H,{K_gamma},Gamma_H)
```

after all source identifiers, endpoint capabilities, event slots, and material rows had departed.
The declared end was one bounded linguistic-world result. CUDA was excluded until host
objective-conditioned closure.

## What now exists

`soma-membrane::live_constituent` is the direct carrier value for a completed regional cell. It is
not an adapter or observer record. It carries:

- constituent-local axes, with axis zero the receiving plane and greater axes established only by
  actual higher-grain FOUNDs;
- sparse blade-supported coefficients, never a materialized `2^n` basis;
- local cells and oriented boundary, dependency, or transport incidence;
- receiver-formed pins containing meeting, held face, and formed `chi/winding/deed` when present;
- plural path emanations with complete ordered transport and oriented winding; and
- the exposed/open residual.

The current inherited complex chart supplies the local quadratic face `e_a^2=-1`; distinct live
axes anticommute. This is a constituent-local Clifford chart, not a global physical metric.
Coefficients remain `Cog`s and re-base under their existing law.

Source geometry is compiled only while the event is borrowed. Transient maps may use live lineage
capabilities and `EventCellId` to recognize sharing, but those values never enter `LiveConstituent`,
Standing, or rest. Source incidence slots become local path and boundary order. Each internal and
cross-boundary relation is met through the mounted receiver's actual
`directed_event_contact_at_source_grain` law.

The host executor forms every regional cell before ordinary conduct changes that mounted body.
All contacts therefore read the same receiver-before. It returns the already-formed constituent
beside immediate arc testimony; `LiveCurrentMachine` validates correspondence and integrates the
constituent into its one prepared successor. It does not reconstruct a product from radiation.

## Completion, compression, and departure

Completion now distinguishes lower participation from continuing higher structure.

- A unique closed interior folds into its already-formed `K_gamma` transport.
- Exposed pins remain.
- Open fourth contacts remain residual.
- Incidence shared by several paths remains once.
- Cycle edges remain cellular even where one displayed endpoint closes.
- A higher apex cell is emitted at the completed grain.
- Lower source relation values, source IDs, capabilities, slots, and rows do not survive.

Path transport and winding survive interior departure. This is emanating compression, not a later
deduplicator. Exact recurring whole constituents stand once in canonical Standing. Exact recurring
pins can RIDE existing cellular terrain instead of allocating another blade.

One post-measurement correction made that last sentence physically exact: a path step now carries
its *effective higher-grain winding*. A lower scalar contact may report FOUND while the cellular
receiver recognizes the exact pin as existing terrain; in that case the higher step winds `None`.
A genuinely new axis deposits its winding once, while later sharing RIDEs it. The compact host and
linguistic controls pass after this correction; the full world was not rerun.

## Standing and rest

`SparseStandingSurface` now owns canonical `LiveConstituent` terrain beside its existing scalar
cells. Scalar and cellular contributions enter one prepared successor and become visible only at
the machine's existing atomic commit. Exact whole-constituent recurrence deduplicates by complete
native geometry.

Direct rest layout v2 carries length-delimited cellular constituents before live lineage rows and
still reads the prior v1 scalar layout. Each constituent has its own canonical v4 native wire.
Remount validates all cells, incidences, pin faces, sparse terms, axes, path winding, exposed
indices, and extents; it performs no event or radiation. `LiveMemory` now counts cellular bodies,
cells, incidences, pins, paths, and transport terms rather than reporting scalar Standing alone.

The CUDA executor refuses both regional input and any cellular Standing body before upload or
launch. No point projection can silently cross the card's scalar ABI. Native CUDA remains a later
physical construction only after host closure.

## Focused acceptance

The exact affected controls pass:

```text
cargo test -q -p soma-membrane \
  regional_cell_becomes_cellular_standing_and_rests_without_the_borrowed_event
  PASS

cargo test -q -p life \
  plural_objective_boundary_forms_one_higher_cellular_constituent
  PASS

cargo check -p life
  PASS
```

The membrane cell forms two causal boundary paths, enters cellular Standing, survives exact native
rest/remount, and requires none of its borrowed source rows to reopen. The compact inscription cell
forms four higher boundaries, folds completed interiors, enters Standing, and rests exactly.

## The stopped assertion run

`host-run-08` completed ecology and then stopped atomically at the first editor cell with
`ExecutionMismatch(CurrentLineage(1))`. Completion had lawfully folded lower pins into path
transport, while a stale machine assertion still required every returned arc pin to remain
literally stored. The assertion contradicted departure and was removed. No source changed and no
report was emitted. The stopped record is `host-run-08/STOPPED.md`.

## The bounded host result

`host-run-09` is the first complete regional-world artifact. It was not repeated. Bootstrap still
executes all eight real Python/JavaScript objective programs exactly across collapse and
distinguishing inputs. The 16 launch/return cuts carry 142,970 cells and 201,840 incidences, of
which 162,120 form. The 70,072-byte ecology rest remounts the same nine live lineages and organ
capabilities into both siblings.

Every editor attempt forms one grain-four regional constituent with 620 source-native diagonal
paths and every one of its 620 cross arcs formed. Completion folds between 473 and 613 path
interiors while retaining exposed, open, shared, and cyclic topology.

The higher boundary species are exact and objective-independent at the requested quotient:

```text
candidate remove-then-reverse, either objective
  [RIDE, RIDE, RIDE, RIDE]
  axes=1, transport terms=1,240

candidate reverse-then-remove, remove-then-reverse objective
  [FOUND, FOUND, FOUND, FOUND]
  axes=206, transport terms=9,712

candidate reverse-then-remove, reverse-then-remove objective
  [FOUND, FOUND, FOUND, FOUND]
  axes=204, transport terms=9,680
```

The complete product therefore distinguishes candidate geometry strongly and even retains a finer
objective-relative difference in the second candidate. But the ratified splice quotient remains
open: neither sibling obtains `[RIDE,RIDE,RIDE,FOUND]`. No source mutates, no held-out applied
execution runs, and both sibling worlds rest.

Exact artifact:

```text
observations/eros-linguistic-code-world-01/host-run-09/REPORT.json
SHA-256 7d4374d18a30fdb6218c2fa112f1b4c2acb9868144ccebfef3140865959fae95

observations/eros-linguistic-code-world-01/host-run-09/ecology.rest
SHA-256 df8ba0736a41961d80f059251e4bb381072810c61320fc0f09b18dc80fb636d5
```

The full artifact predates only the effective-higher-winding correction and expanded memory
census described above. Those changes are compact-control measured but not full-world measured.

## First exact missing relation

The host engine now carries the region. The application ecology has not yet grown the regional
atlas which its editor later asks to recur.

Bootstrap still sends execution hands through `present_native_event_with` as independent point
relations. Only the editor uses `present_native_event_with_regional`. Thus the ecology encounters
the right objective, program, and source material, but never presents their three source-native
regional boundary factors as one higher cell. At the editor, exact cellular recurrence cannot be
expected from a regional construction which has never entered Standing. This is a source-based
inference from the measured result, not yet a ratified successor plan.

The next question is therefore bounded:

> What is the natural execution-world regional cell whose actual objective/program/source
> boundaries grow the same first three factors later used by the editor, while runtime and input
> relations remain their own lawful event geometry?

Do not answer by weakening `[RIDE,RIDE,RIDE,FOUND]`, reading the candidate label, thresholding the
multivector, selecting the smaller axis population, repeating the fixture, or installing a
curriculum. The world must actually present the regional relation it later expects to RIDE. No
such ecology change and no CUDA run is authorized by this record alone.

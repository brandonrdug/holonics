# The configuration moves through the swing; the flip retains the metric interior

**Status:** BRANDON-RATIFIED PAPER REFINEMENT / DEPOSITED DERIVATION / BOUNDED HOST CELL BUILT AND
ACCEPTED / COMPLETE VERSUS QUOTIENT FOIL CLOSED / DIRECT ENGINE ENACTMENT REMAINS OPEN / NO SOMA
INTERIOR CHANGE / NO CUDA / NO FLOATING-POINT CAUSAL DATA

## Present question

The ratified work had two coupled parts:

1. refine `PAPERS/papers/causal-formulation-ecologies/main.typ` so the proposed
   `chi(A,z,w,B)` and lifted power are paths through a changing configuration, not static scalar
   decorations; and
2. test one exact local retriangulation law against a quotient which preserves the source
   cross-ratio and endpoint but forgets the path that made them consequential.

The standing paper was insufficient because its differential was static, its triangle was mainly
an incidence metaphor, and it did not distinguish continuation along one path from curvature
formed by comparing paths.

## Deposited mathematical refinement

### The lineage is a path through configuration

Let

\[
\gamma:I\to\mathcal M,\qquad
\gamma(t)=(A(t),z(t),w(t),B(t),\ldots).
\]

Here `t` orders one admitted lineage. It is not an external absolute clock. Pulling the ratio and
lifted power along this path gives

\[
\chi_\gamma(t)=\frac{P(\gamma(t))}{Q(\gamma(t))},
\qquad
T_\gamma(t)
=\exp\!\left(\frac{Q(\gamma(t))}{2P(\gamma(t))}\log z(\gamma(t))\right),
\]

and, on one compatible logarithmic chart,

\[
\frac{T'_\gamma}{T_\gamma}
=\frac{Q}{2P}\frac{z'}{z}
+\log z\,\frac{P Q'-Q P'}{2P^2}.
\]

The first term moves the lifted scale-turn face. The second moves the projective basis used to
read it. A common time-dependent Möbius rechart of all four incidences preserves the cross-ratio;
collision, reordering, and branch passage are seams. The reparameterization-invariant object is
the pulled-back one-form and its ordered path integral, not the raw coordinate velocity.

### One scalar path is not curvature

Locally, for a smooth single-valued scalar lift,

\[
\omega=d\log T,\qquad d\omega=0.
\]

One path can retain order, winding, branch, and monodromy, but local curvature requires alternate
transports, a noncommutative or map-valued connection, or a singular chart seam. In a simplicial
connection, fibers `E_i` and partial edge maps `U_ij` give the triangle comparison

\[
U_{02}\quad\text{versus}\quad U_{12}\circ U_{01}.
\]

In a common linear chart the residual is

\[
\Omega_{012}=U_{02}-U_{12}U_{01}.
\]

Agreement closes the 2-cell; disagreement is an oriented OPEN face. This gives discreteness
without assigning a context-free scalar to a lone triangle.

### Multiplicative triangle coordinates become consequential when glued

With vertex factors,

\[
a=r_2r_3,\qquad b=r_3r_1,\qquad c=r_1r_2.
\]

Every isolated positive triangle admits this factorization, so it is local gauge. Gluing faces
forces common factors to serve several triangles. Under discrete conformal vertex scaling,

\[
\widetilde\ell_{ij}
=e^{(u_i+u_j)/2}\ell_{ij},
\]

while an oriented length cross-ratio survives. The hyperbolic counterpart is

\[
\sinh\!\left(\frac{\widetilde\ell_{ij}}{2R}\right)
=e^{(u_i+u_j)/2}
\sinh\!\left(\frac{\ell_{ij}}{2R}\right).
\]

The triangle's basis can therefore change locally while cross-face compatibility remains
consequential.

### Law of Cosines, norm, and hyperbolic product

\[
c^2
=a^2+b^2-2ab\cos\gamma
=(a-be^{i\gamma})(a-be^{-i\gamma}).
\]

The right-angle case is the Gaussian norm. In curvature radius `R`,

\[
\cosh(c/R)
=\cosh(a/R)\cosh(b/R)
-\sinh(a/R)\sinh(b/R)\cos\gamma,
\]

so a hyperbolic right triangle obeys the literal product

\[
\cosh(c/R)=\cosh(a/R)\cosh(b/R).
\]

The Euclidean Pythagorean face returns as the leading nonconstant term when `R` grows.

### Ptolemy flip and Markov-Fricke mutation

For a decorated ideal quadrilateral,

\[
ef=ac+bd,\qquad
\chi=\frac{ac}{bd},\qquad
f=\frac{bd}{e}(1+\chi).
\]

The alternate diagonal is a local founding. The cross-ratio alone does not retain metric scale or
the old diagonal. Repeated flips on the punctured-torus Fricke cubic,

\[
x^2+y^2+z^2-xyz=0,\qquad z'=xy-z,
\]

give a larger exact mutation ecology. Prime and composite labels are arithmetic observations on
integer points of this surface, not the flip law itself.

The paper now includes equation-bearing diagrams of the moving configuration, projective swing,
lifted receiver, simplicial connection, glued length cross-ratio, quadrilateral flip, Euclidean
norm factorization, and hyperbolic product law.

## Bounded construction

The source declared one nine-node exact program:

```text
input a, input b, input c, input d, input e,
multiply a c,
multiply b d,
add the products,
divide by e.
```

This program and a matched quotient crossed separate direct `LiveCurrentMachine` worlds as
regional incidence. The quotient retained `chi=10/21` and source output `31`. Both rested after
their source lineages ended.

Later currents supplied:

- uniform scale two;
- cyclic rechart;
- scale and cyclic rechart together;
- an exact rational alternate diagonal; and
- the `e=0` boundary.

A generic exact rational world transducer evaluated only a program recovered from Standing. It
returned every completed node path and the output as genuinely later current; the independently
declared result then met it through Soma.

## Result

The complete carrier produced:

```text
scale two               40 + 84 -> 124 / 2 -> 62
cyclic rechart          21 + 10 -> 31  / 1 -> 31
both                     84 + 40 -> 124 / 2 -> 62
rational receiver         8 + 15 -> 23  / 7 -> 23/7
```

Every returned exact result RODE against its independent receiver with no OPEN or FOUND pin.

At `e=0`, nodes 0 through 7 remained exact and node 8 alone stayed OPEN. No result was
fabricated.

The quotient foil recovered its exact source face and the scaled current, but formed no output.
The same `chi=10/21` belongs to both the source and uniform-scale-two occurrence, while `f` changes
from `31` to `62`. This proves inside the declared observation family that cross-ratio plus source
endpoint is not a receiver-exact compression of the complete flip.

Exact observation:
`observations/eros-retriangulating-branch-transport-01/RESULTS.md`.

## Exact boundary

The result is not direct algorithmic enactment by Soma. Soma carries and recruits the program as
live Standing. A generic exact world transducer still interprets the recovered nodes and returns
the consequence. This is now the isolated hand-up seam.

The repair is not a Ptolemy registry, bytecode VM, or arithmetic head inside Soma. The open engine
question is how an arbitrary carried incidence-and-transport relation can itself contribute to a
later `Lambda` transition, so the world supplies material and constraints without replaying the
law.

No successor is scheduled by this deposit.

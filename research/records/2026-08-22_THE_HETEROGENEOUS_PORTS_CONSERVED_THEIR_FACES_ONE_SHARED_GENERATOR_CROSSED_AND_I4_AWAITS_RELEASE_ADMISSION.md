# The heterogeneous ports conserved their faces, one shared generator crossed, and I4 awaits release admission

**Date:** 2026-08-22  
**Truth status:** [established-bounded; implemented-exact; measured]  
**Roadmap deed:** I4, Gemma's heterogeneous ports found shared transport  
**Boundary:** the candidate artifact, focused owner tests, real RTX deed, inspection receivers,
authored-level gate, and architecture ratchet pass. The sole complete release receiver has not yet
run, so `CONSTRUCTION_STATE.md` continues to admit I3 and name I4 as the next deed.

## Construction declaration

[definition] The source owners are the authenticated Gemma-4 E4B safetensors manifest and its real
text-codeword and vision-patch entry laws; M0's addressed `−χ → +χ` source-render passage and
withheld PDF-raster realization; `embedding_fiber`'s exact resident contraction; I3's
generator-native standing/decoder/fibre product law; and `cuda_refine`'s resident exact return. The
entering ports are separately typed `TextCodeword` and `VisionPatch` boundaries. A transcript,
label, source digest, coordinate similarity, or concatenated embedding stream is not an identity
key.

[definition] The predecessor identity is the authenticated source-model content SHA-256
`cfbd3d2f1cd71bd471c37fe2bf8546d5028d41e5736f64e1ca6c6b8893125503`, each source occurrence
address and digest, its serial or spatial incidence, and the three independently authenticated
native-rest components. Material paths remain lineage, never native identity.

[definition] The caused event is exact source conduct at both ports, formation of a common-world
pullback over M0's predeclared parameter occurrence, condensation of its two-state passage into one
shared native action, source-absent remount, and one resident front returning predecessor,
successor, global withdrawal, and every single-port withdrawal. The local constitutive law is one
shared state action through family-major, port-major cells; each decoder and fibre face remains
port-, family-, state-, occurrence-, consequence-, and incidence-indexed.

[definition] The receiver asks whether every source transition forms a typed naturality square,
the shared action is stored and enacted once, all modality fibres remain complete, global and local
withdrawals separate shared from port-specific conduct, the withheld cross-port family still
requires the shared ecology, every complete-product coordinate strictly falls, and an exact atlas
plus inspectable projection returns. The result is bounded entry-port fusion, not a claim that the
full Gemma text or vision tower has been re-expressed.

## Conservation of faces is the fusion law

[proved-derived] The formal conservation pattern already used by I3 applies directly. A common
receiver face may factor through a quotient while the members of its inverse image remain distinct.
For I4 the only identified object is the world-state generator. The text occurrence, image
occurrence, codec incidence, source consequence, receiver family, and reconstruction member remain
different computational objects even when they present the same addressed world passage.

[implemented-exact] `phoenix::heterogeneous_fusion` therefore stores one shared action table and
eight distinct decoder/fibre faces: two families × two ports × two states. Validation reconstructs
every naturality square from `(family, port, state)` and the matching source member. It rejects a
stored `commutes: true` whose occurrence or native address moved, a transition whose source
consequence did not move, an omitted or duplicated face, a malformed lineage/hash, a repeated port,
and any decoder/fibre disagreement.

[implemented-exact] The held-out cross-port receiver reads the pair of text and PDF-raster successor
faces. Global withdrawal returns both predecessors. Withdrawing either port produces a mixed pair
which is neither the full successor nor the full predecessor, and the other port remains on the
shared successor. Thus the shared consequence is joint while the local faces remain local.

## The aperture theorem had collapsed the two operand faces

[proved-derived] The first real source invocation exposed a false refusal in the inherited exact
readout aperture. It replaced the readout/query bounds `(a,b)` by `(max(a,b),max(a,b))`, then applied
the symmetric product estimate. That quotient discarded which operand carried which width.

For `n` products with `|r_i| < 2^a` and `|q_i| < 2^b`,

```text
|Σ_i r_i q_i| < n · 2^(a+b).
```

The signed exact carrier therefore needs

```text
a + b + ceil(log₂ n) + 1
```

octaves. It does not need `2·max(a,b) + ceil(log₂ n) + 1`. The actual I4 readout occupies 62
octaves, the integer query occupies 8, and `n = 786`, so the exact requirement is 81 octaves rather
than the falsely reported 135. Both figures are derived from the entering material; no carrier was
widened and no bound was raised.

[implemented-exact] `embedding_fiber` now exposes `needed_product_octaves(readout, query, dim)` and
uses it in single and batched resident contractions. The old symmetric helper remains only as the
special case `(b,b)`. Focused tests establish the I4 figure `81`, retain refusal for a genuine
`(62,62,786)` overflow, and preserve resident/serial exact-score parity. This is a concrete
computational consequence of conservation of faces: distinct situated bounds may not be replaced
by a coarse maximum before composing them.

## Real source passages

[implemented-exact] The text port uses Gemma's actual tokenizer, retains every UTF-8 byte span and
serial token adjacency, reads the addressed BF16 embedding row for every codeword, and retains its
exact row digest. The source consequence includes the actual token sequence, row identities, and
the positive algebraic embedding scale `16·√10`, the root of `s² = 2560`. The baseline and
perturbed source occurrences return 47 and 54 codewords respectively.

[implemented-exact; measured] The vision port uses the actual Gemma patch projection
`model.vision_tower.patch_embedder.input_proj.weight` and both actual positional tables. The
development pair alone derives the pooling-cell crop `(384,192,48)` from 56 changed pixels. The
withheld PDF-render pair carries 69 changed pixels in that unchanged receiver crop. No image is
converted to text.

[implemented-exact] For each of nine 16×16 RGB patches, the resident query carries exact integer
numerators `2p−255` and two situated positional coefficients `255`. The augmented source map carries
768 projection rows of width 786. Its returned score is exactly

```text
[Σ W(2p−255) + 255·P[x] + 255·P[y]] · 2^e / 255,
```

where `e` is the exact common BF16 alignment exponent. The output retains every signed `i128`
numerator, exponent, denominator, patch coordinate, raster extent, and two-dimensional incidence.

[implemented-exact; measured] Thirty-six real patch queries returned 21,731,328 exact
multiply-accumulates on the RTX 4080 SUPER. The source passage read 2,241,536 weight octets, mounted
4,829,184 exact resident octets, sent 226,368 query octets, and returned 442,368 exact-score octets.
The processor's masked padding is an explicit receiver-elided fibre; it is not materialized as
synthetic zero modality data.

## Common-world pullback, naturality, and fibres

[established-bounded; implemented-exact] M0 had predeclared and isolated the same source parameter
occurrence before I4 source conduct. I4 uses that addressed occurrence—not response similarity—to
form four pullback rows: development/held-out × predecessor/successor. Each row keeps distinct text
and vision native addresses and source consequences.

[implemented-exact] Four naturality squares return: one per family and port. The development
source-render text and image responses found the shared generator. The PDF raster family was
withheld from generator formation and grades the later correspondence. The atlas preserves every
source consequence and incidence digest, and the native fibre component preserves the complete
eight-member addressed population.

[implemented-exact] The shared action is the single two-state table `[1,1]`; it is not copied into
one table per modality. Q/K/V-like co-presence, transcript equality, and source-output similarity
play no role. Text and vision meet only through the declared common-world receiver and its shared
generator, so the construction is neither concatenation nor authored text conversion.

## Detached resident return and ablations

[implemented-exact; measured] A fresh Bubblewrap namespace mounted only the optimized executable,
the three native-rest components, CUDA device files, and a writable return directory. The
repository, model directory, safetensors, tokenizer, M0 fixtures, and developmental outputs were
absent. `/proc/self/fd` returned no forbidden source descriptor, and no CPU semantic callback
occurred between resident fronts.

[implemented-exact; measured] One RTX 4080 SUPER launch and one synchronization returned native
predecessors `[0,2,4,6]`, successors `[1,3,5,7]`, and global-withdrawal predecessors
`[0,2,4,6]`. Its eight local-withdrawal cells retain the successor everywhere except the withdrawn
port. The resident front used four active lanes, received 68 octets, returned 80 octets, held 136
resident octets, and measured 248 microseconds wall. That wall reading is separate physical
telemetry.

[implemented-exact; measured] Exact native semantic work is four shared-action reads, eight decoder
reads, twelve consequence writes, eight local-ablation writes, and dependency span two. There is
one action table, one kernel launch, and no exterior semantic orchestration between device fronts.

## Strict complete product

[measured] The source/native product comparison is coordinatewise strict:

| receiver coordinate | source | native |
|---|---:|---:|
| artifact standing | 1,035,035 octets | 1,578 octets |
| decoder | 652,506 octets | 2,030 octets |
| complete fibre product | 655,915 octets | 5,188 octets |
| semantic work | 21,731,530 | 32 |
| dependency span | 786 | 2 |
| resident extent | 4,829,184 octets | 136 octets |
| transfer extent | 2,910,272 octets | 148 octets |

[definition] These coordinates form a product order and are not summed into a scalar loss. Source
standing, decoder, complete fibres, semantic work, span, residency, and transfer are independently
priced. The native consequence is exact only for this declared two-state, two-port, two-family
receiver; the complete source response remains the reconstruction exterior.

## Exact atlas and inspected projection

[implemented-exact] `02-multimodal-realization-atlas.json` is the topology authority. It returns
eight source responses, eight native consequences, four common-world pullbacks, four naturality
squares, complete fibres, resident routes, unavailable ports, and the conservation/forbidden-
identification law. `03-exact-mesh.json` derives seventeen vertices and its typed edge population
from those keys.

[implemented-exact] `04-interactive-atlas.svg` is a receiver chart only. It filters text, vision,
development, and held-out faces; projects shared and local withdrawal; and animates shared/current
edges. Its script changes visibility and animation only. Layout crossings supply no incidence. A
visual inspection caught and repaired an earlier positional `zip` between text-major source order
and family-major native order; the final projection binds every node by exact
`(family,port,state)`.

## Candidate grade, workflow receipts, and artifact

[established-bounded; implemented-exact; measured] All twelve I4 candidate consequences pass:

1. real text and vision occurrences cross separately typed ports;
2. source and native conduct return for every admitted occurrence;
3. serial and spatial incidence remain attached where the admitted media carry them;
4. the predeclared common-world pullback returns;
5. every proposed shared-generator naturality square recomputes and commutes;
6. the shared subcomplex is stored and enacted once;
7. all modality-specific reconstruction fibres remain complete;
8. shared withdrawal moves both ports;
9. local withdrawal moves only its declared port;
10. the withheld cross-port pair requires the shared ecology;
11. the source-absent rest returns with strict complete-product descent; and
12. the multimodal atlas and exact interactive projection return and survive inspection.

[measured] The artifact family is
`output/the_heterogeneous_ports_found_one_shared_phoenix_ecology`: sixteen regular files,
1,172,626 octets, rolled SHA-256
`52bbb5ba39bc33b6143a0cd54eea3d5806c692634e6638327598d298ca89e8dd`. The final direct-owner code
closure recorded by the product is
`3e2d5d846e31f0259ed4d70beb19f2a9315ddb950ee560d9063fb5b92657fbfc`.

[measured] Focused `heterogeneous_fusion` tests return three passed and zero failed. The exact
resident CUDA withdrawal test passes on the RTX card. The differentiated-operand aperture theorem
and resident/serial parity tests pass. `cargo check` accepts the real I4 driver. The authored-level
gate returns zero failures. The architecture ratchet passes after explicitly admitting four CUDA
return vectors and the finite collections owned by the new standing/decoder/fibre object: 316
files, 25,706 inherited occurrences, zero retired. A diff audit finds no added `f32`, `f64`,
`float`, or `double` operation in the semantic/device closure.

[measured] The real-driver invocations were retained as workflow testimony rather than hidden:

| purpose | elapsed | exit | returned consequence |
|---|---:|---:|---|
| first real two-port deed | 86,528 ms | 1 | false 135-octave aperture refusal; no product manifest returned |
| falsify corrected operand-face theorem | 32,461 ms | 101 | exact source and detached deed passed; lossy JSON `Value` coercion panicked before product return |
| stream exact scores directly | 8,907 ms | 0 | first complete candidate; code closure `85dc1fc9…` |
| typed atlas and joint held-out grade | 34,860 ms | 0 | corrected projection/grade; code closure `6060e71a…` |
| complete direct-owner closure identity | 29,069 ms | 0 | final candidate; code closure `3e2d5d84…` |

[definition] Each repeated invocation had a named falsifier changed by the preceding result. The
first two failed artifacts were moved intact under `/tmp`; no Cargo build cache or user material was
deleted. The final receipt is the only candidate proposed for admission.

[open] Claim, driver, output, closure, and authored-level ledgers must now be regenerated once in
dependency order, followed by the sole complete release receiver. `CONSTRUCTION_STATE.md` remains
at I3 until that boundary passes.

## Open exterior and successor boundary

[open] I4 does not execute or condense the sixteen-layer vision encoder, pooler, multimodal RMS
rebase, 768→2560 projection, or text-transformer interior. It does not enact audio or temporal video
source paths. Those are explicit fibres/obstructions, not simulated modalities. It also makes no
full-answer, native conversation, unrestricted-language, or multimodal-generation claim.

[open] I5 must freeze a source-detached Athena-Gemma product with an executable recurrent inference
entry, full-passage emission through the retained causal boundary, explicit cultivation
commit/decline, genuine world return and changed later conduct, every admitted real modality port,
generator-native condensation, complete dissection/fibre/defect/holonomy/ablation atlas, exact
semantic work, separate physical telemetry, fresh-process restoration, and targeted source/native/
shared-route ablations. A source-shaped tensor container or embedding-entry demonstration does not
pass I5.

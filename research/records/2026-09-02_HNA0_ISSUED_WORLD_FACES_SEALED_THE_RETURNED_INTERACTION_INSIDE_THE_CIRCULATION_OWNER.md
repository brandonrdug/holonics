# HNA0 issued world faces sealed the returned interaction inside the circulation owner

**Date:** 2026-09-02
**Scope:** HNA0 under
[`the superseded first HNA contract`](../../archive/plans/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md).
**Disposition:** `counterexample`; no phase passed. The complete retraction is
[`THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md`](2026-09-02_THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md).
**Truth status:** per claim under [`../../canon/EPISTEMIC_GRADES.md`](../../docs/canon/EPISTEMIC_GRADES.md).

## Return

[established-bounded; implemented-exact; source-inspected] HNA0 passed. Every
`NativeCirculationBoundary` now issues one `NativeIssuedWorldFace` per owned emission grain. An
application can report only the unchanged issued member, admission, a nonempty subset of its
issued occurrence support, and diagnostic octets. `NativeCirculationSession::stage_world_return`
recomputes the boundary from the owned morphology, validates exact face-family coverage and later
chronology, and derives the counted junction, exact rational current, storage, and native causal
support inside `life`.

[established-bounded; implemented-exact; source-inspected] A valid no-admission family returns
`NativeWorldObstruction` without a candidate. A partial family stages only when the actually
emitted grain was admitted; otherwise it returns `EmittedGrainReflected`. The junction disposition
does not carry a terminal field and therefore cannot repeat the refuted universal terminal
identification.

[established-bounded; implemented-exact; source-inspected] `ReturnedScaffoldInteraction` now has
private fields, no deserialization implementation, and no public constructor. A persisted
`NativeCultivationCandidate` contains only schema-v2 boundary, world faces, and returned occurrence;
commit revalidates the boundary and reconstitutes the private interaction. Applications can no
longer supply complex current, storage, emitting boundary, native support, causal cone, or deposit.

[established-bounded; implemented-exact; source-inspected] The circulation session and C ABI moved
to v2 without an alias or v1 decoder. Athena-alpha, the explicit-root variant workspace, and the
Workbench now report world faces. The workspace and Workbench uniform-face controls are exterior
apparatus only; they do not establish an actual compiler, runtime, or kernel world, which belongs
to HNA3.

[counterexample; source-inspected] The retracted language-bound `route_return.rs`, its WRD4 driver,
and its language-shaped integration test were deleted. The obsolete manual-return SCF6 and
operator-authored morphology-evaluation examples were also removed. Their historical receipts
retain their stated evidence grades; their source no longer offers a live alternate return path.

## Controls

[established-bounded; measured] Four focused neutral-world controls passed:

- matched, partial, selected-reflected, and no-admission dispositions remain distinct;
- changing diagnostic octets leaves derived current, storage, and support unchanged;
- missing, duplicate, stale, wrong-emission, wrong-grain, out-of-support, and nonlater families
  refuse; and
- every refusal leaves the predecessor morphology byte-identical.

[established-bounded; measured] The directly affected application closure passed: Athena-alpha
two tests, the variant workspace four tests, circulation ABI three tests, and Workbench eleven
tests across its library and integrations, with zero failures. The focused morphology commit,
scaffold cultivation, and morphology package controls passed. `cargo check` passed for `life` and
all four dependent packages.

[established-bounded; process-audit] `source-shape` reported 663 live files, 447 inherited
baselines, and zero violations. `epistemic-tags` reported 296 checked material brackets and zero
failures. `document-law` reported zero unmeasured absences, unsubordinated plans, or frontier
failures. `git diff --check` passed. HNA0 introduces no floating-point carrier or operation.

## Boundary

[open] HNA0 does not yet give the persistent variant a general source-neutral octet occurrence
ingress. The active application still begins from a native spool/thread/event address. HNA1 owns
that exact missing composition; no qualitative-generation or coding/mathematics capability grade
advances here.

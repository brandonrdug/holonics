# THE RECEIVER ACCEPTS A PATH; THE GATE CARRIES THE BOUNDARY; THE TERMINAL REFINES THE DIFFERENCE

**Emission-spectrum continuation:** The distinction below among incident,
absorption, re-emission, and action spectra is retained. Its elementary
phase/carry construction is now production-owned and graded on real binaural
transport in
`2026-07-29_THE_DIFFERENCE_EMITS_THE_PATH_CARRIES_THE_SPECTRUM_IS_THE_RECEIVER_PHASE_FACE.md`.
An instrument wavelength band is one receiver chart of propagating change,
not the universal definition of spectrum.

**2026-07-27 · BRANDON-DIRECTED RECEIVER CORRECTION / RATIFIED MATHEMATICAL
CONSTRUCTION / COMPLETED PRODUCTION IMPLEMENTATION AND EXACT GRADING**

## 1. Present question

Brandon directly identified three connected defects in the live receiver:

1. the displayed face has visibly inadequate resolution;
2. input becomes progressively slow and its elementary controls do not form a
   scalable geometric language; and
3. the engine still misses natural computational factorizations of a
   geometric ecology.

He proposed photosynthetic electrochemical transport, wavelength-conditioned
receiving organs, bioluminescent and fluorescent communication, comparative
animal conduct, and the non-absolute nature of self-recognition as connected
research evidence. His governing intuition is that a causal dynamic ecology
does not need a privileged intelligence or total representation in order to
respond lawfully. Local receivers admit, transport, compare, retain, emit, and
refuse consequences through their contemporary relations.

The construction question is:

> How must the production receiver change so that it accepts exact geometric
> paths rather than accumulated display controls, retains state-conditioned
> boundary conduct rather than recomputing a complete interior, and presents
> native observer detail by sparse caused differences rather than a bounded
> raster enlarged after the fact?

This record extends
`THE CENTER OPENS INTO ITS HORIZON; THE CAUSAL BASIN MEASURES THE LOSS`.
It does not revive terminal propagation, an absolute camera, authored tiles,
floating geometry, or a display clock.

## 2. The live evidence separates three costs

The current desktop has one hard exact-face width cap:

```text
MAX_EXACT_FACE_WIDTH = 640
```

On resize it restricts the exact aperture to that width and derives a smaller
height. `compose_display` then visits every platform pixel and copies the
nearest address from that smaller exact face. The visible stair-stepping is
therefore caused before the monitor receives the image.

The outer display also defeats already-computed sparsity:

- each receiver layer allocates `width * height` optional colors;
- tube radiation already returns exact changed support addresses;
- the platform already admits `DisplayPatch`; but
- every accepted deed still rebuilds and presents the complete platform face.

The progressive input delay is a different cost. In the contemporary exact
trace, event 3 and event 30 carry:

| event | traced addresses | exact support queries | greatest decimal integer body | wall time |
|---:|---:|---:|---:|---:|
| 3 | 5,836 | 39,789 | 10 digits | 18,207,443 ns |
| 30 | 6,079 | 42,209 | 102 digits | 217,712,474 ns |

The support population remains nearly stationary while arithmetic bodies and
wall time grow. The earlier long trace reached rational components above
1,100 bits. Thus:

1. poor resolution comes from the fixed terminal quotient;
2. repeated full display composition is avoidable observation work; and
3. progressive latency is chiefly expanded relational arithmetic, not an
   increasing visible population.

These costs require different corrections. Raising a bit width, increasing
the authored aperture, or adding a frame cache cannot close them.

## 3. Spectral reception is a composed causal relation

For receiving organ \(r\), wavelength \(\lambda\), and local state \(s_r\),
the admitted current is usefully separated as

\[
J_r(t)=
\int
I_\star(\lambda,t)\,
T_{\mathrm{atmosphere}}(\lambda,t)\,
T_{\mathrm{medium}}(\lambda,x,t)\,
A_r(\lambda,s_r)\,
\Phi_r(\lambda,s_r)\,d\lambda .
\]

The factors are:

- incident source current;
- atmospheric transport;
- local medium transport;
- receiver absorptance; and
- state-conditioned action yield.

An absorption spectrum is not an action spectrum. A chlorophyll molecule, a
whole leaf, a canopy, a cyanobacterial phycobilisome, and an aquatic
photosynthetic ecology have different receiving sections even when they share
some pigments.

The biological evidence supplies several computationally relevant species:

- chlorophyll-a/b and carotenoid plants receive broadly across visible light,
  with strong blue and red conduct and nonzero green conduct;
- phycoerythrin, phycocyanin, and allophycocyanin cover different bands, while
  some cyanobacteria remodel their antenna population under a changed ambient
  spectrum;
- chlorophyll-f cyanobacteria extend reception into far-red light with a
  measurable trapping/efficiency trade;
- water, dissolved organics, suspended material, and living populations
  reshape the spectral channel before it reaches an aquatic receiver;
- thylakoid voltage and pH, ion channels, reaction-center availability,
  cyclic electron flow, antenna state, and non-photochemical quenching route
  excitation through different current species; and
- xanthophyll and redox states retain causal memory which changes a later
  response after the original illumination has departed.

Photosynthetic rest is consequently plural. Excitation, membrane potential,
redox state, pigment configuration, chemical storage, and growth relax on
different event grains. The engine must not collapse them into one display
frame or one scalar current.

## 4. Luminescence completes a relation only through a receiver

Bioluminescence produces photons through a chemical event. Biofluorescence
absorbs an incident band and re-emits another band. Neither physical emission
is automatically communication.

A communicative consequence requires the complete loop

\[
\text{emitter}
\to
\text{channel}
\to
\text{receiver contrast}
\to
\text{receiver sensitivity}
\to
\text{changed conduct}.
\]

Flashlight-fish blink patterns which alter school spacing and coral
fluorescence which attracts plankton are completed loops of this kind.
Scorpion cuticular fluorescence remains compatible with a different history:
a material optical consequence may arise first and only later be recruited by
an ecology as a signal. Origin, present function, and receiver consequence
must remain distinguishable.

For the engine, fluorescence is a model of typed residual testimony. It may
report current which did not enter one downstream pathway without becoming
the physical transition which produced it.

## 5. Self is a comparative transport, not an absolute boundary

The mirror mark test, body-as-obstacle conduct, olfactory self-comparison,
delayed-video comparison, path integration, and external marking do not
measure one ordered scalar called self-awareness.

Let:

- \(s_k^n\) be a received local section in modality \(n\);
- \(a_k\) be the receiver's deed;
- \(T_{a_k}\) be the deed-conditioned predicted transport;
- \(G_{m\leftarrow n}\) be a lawful cross-modal or mirror transport; and
- \(s_{k+1}^m\) be the actual later receipt.

The operative comparison is

\[
G_{m\leftarrow n}\!\left(T_{a_k}s_k^n\right)
\longrightarrow
s_{k+1}^m .
\]

Failure to commute is a residual or obstruction. Repeated deed-linked
agreement recruits a support into the receiver's contemporary body relation.
Independent residual can indicate another lineage, an environmental change,
injury, a failed prediction, or an incorrect cross-modal transport.

A wound observed in a mirror is therefore not classified against an absolute
total self. It is a difference between a present local section and a
transported earlier standing. Navigation has the same form: internally
transported direction is corrected through landmarks, odors, body support,
and prior marks. Knowledge is carried comparison.

## 6. Probability is deterministic transport observed through a fiber

Let an exact world have state space \(X\), deterministic caused transport
\(F:X\to X\), and receiver observation

\[
\pi_r:X\to Y_r.
\]

After receiving \(y\), the compatible unresolved source population is the
fiber

\[
U_y=\pi_r^{-1}(y).
\]

For consequence region \(O\subseteq X\) and an ecology-transported measure
\(\mu_r\),

\[
\Pr_r(O\mid y)
=
\frac{\mu_r\!\left(U_y\cap F^{-1}(O)\right)}
     {\mu_r(U_y)}.
\]

The world may be deterministic while the receiver lawfully carries a
probability, because it receives a fiber rather than a privileged complete
state. Counting, volume, symplectic, capacity, and preparation measures are
different supplied physical species over the same causal construction.

Loss remains the rejected or obstructed geometric body. Its measure, boundary
current, minimum corrective action, dissipated current, emitted current, and
failure to glue are different derived testimonies. One scalar must not erase
that population.

## 7. The input is a receiver-local traversal groupoid

The current `PresentationInput` collapses device occurrences into additive
basis offsets, three additive Cayley parameters, or a terminal-cell delta.
That representation makes the display grid part of the control language and
erases ordered noncommutative paths.

The replacement occurrence is

\[
u=(r,a,c,W,\rho,\tau),
\]

where:

- \(r\) is the receiving organ or port;
- \(a\) is the selected anchor, incidence, support, or seam;
- \(c\) is the active exact local chart;
- \(W=g_1^{k_1}\cdots g_m^{k_m}\) is an ordered word of elementary
  transports;
- \(\rho\) is a primitive projective ratio tuple; and
- \(\tau\) retains event order and crossed validity boundaries.

The initial elementary population is:

1. re-anchor or chart transition;
2. primitive local lattice shear;
3. rational Cayley pivot in an oriented local two-plane;
4. projective dilation or contraction about an anchor;
5. branch or join through declared incidence;
6. bind or release a support; and
7. mark, revisit, compare, or return around a cycle.

Positive rational one-dimensional input admits a Stern--Brocot left/right
word. Repeated runs become continued-fraction digits. In higher rank, a
primitive homogeneous integer vector and elementary
\(GL(n,\mathbb Z)\) frame changes generalize the same idea.

Coalescing is lawful only when:

- receiver, anchor, chart, and generator remain equal;
- no chart seam, gate boundary, or physical event boundary was crossed; and
- the generator relation proves that combining the integer run preserves the
  caused path.

Noncommuting order is never sorted away. Device capture may immediately
record an accepted occurrence while physical realization remains ordered.
Only completed physical standing, or an explicitly typed open causal
boundary, may become the displayed world.

## 8. The terminal is an output-sensitive refinement atlas

The continuous receiver face remains authoritative. Terminal addresses are
finite observation fibers, not source cells.

The corrected terminal must:

1. use the native platform extent rather than a fixed 640-wide exact face;
2. retain sparse receiver/address contributions;
3. consume the exact support deltas already emitted by the tube atlas;
4. compose and present only changed address runs after the first full face;
5. distinguish boundary support, occupied sheet coverage, depth order, and
   monitor color;
6. refine only ambiguous pixel footprints, crossings, seams, horizons,
   changed fronts, and active input destinations; and
7. grade every adaptive or carried result against complete direct exact
   restriction.

`Points as Tori` supplies a closely related observer architecture: local
curvature-bearing analytical bodies support output-sensitive point queries at
arbitrary resolution without making a spatial grid the geometry. Its learned
floating torus fitting and exponential weighting do not become exact engine
standing. The production exact conics, sheets, threads, tori, arrangements,
and receiver tubes supply the authoritative local bodies.

## 9. A causal gate carries an exact boundary transfer

For one exact diffusion regime, let the event operator be

\[
A=M+\tau L,
\]

with boundary nodes \(B\), interior nodes \(I\), capacity matrix \(M\),
incidence transport \(L\), and exact event interval \(\tau\). Partition

\[
A=
\begin{bmatrix}
A_{BB}&A_{BI}\\
A_{IB}&A_{II}
\end{bmatrix}.
\]

Positive capacities, nonnegative conductances, and positive \(\tau\) make
\(A\) positive definite. The exact boundary transfer is its Schur complement

\[
\Lambda
=
A_{BB}-A_{BI}A_{II}^{-1}A_{IB}.
\]

For contemporary right-hand side \(r=n+s\),

\[
\begin{aligned}
h_I &= A_{II}^{-1}r_I,\\
\widehat r_B &= r_B-A_{BI}h_I,\\
\Lambda\phi_B &= \widehat r_B,\\
\phi_I &= h_I-A_{II}^{-1}A_{IB}\phi_B.
\end{aligned}
\]

The retained object is the exact operator factorization and its validity
certificate, never an event-specific answer. Standing content and new source
remain contemporary.

The certificate names:

- exact complex, capacity, branch, conductance, and orientation populations;
- exact interval;
- declared boundary and interior;
- region closure;
- factorization testimony;
- exact Schur identity; and
- any future active gate, species, constitutive, or unit regime.

One changed certificate refactors only its descendants. Full direct solve
remains the grading authority. A boundary-only observer cannot silently
replace the present complete node/current/balance receipt; reconstructing
that complete receipt remains owed when requested.

This is the computational form of photosynthetic gating: local interior
conduct remains implicit while its topology and constitutive state remain
valid, and the region opens only when a gate or certificate changes.

## 10. Induction and deduction become an executable research method

Deduction enacts a declared transport and predicts its exact receiver receipt.
Structural induction extends a proved relation over causal layers, simplex
attachments, receiver-cover refinements, or certified boundary eliminations.
Receiver-relative induction intersects compatible source fibers from plural
observations.

The discriminating region for a proposed implication \(P\Rightarrow Q\) is

\[
P\cap\neg Q.
\]

Engine research must therefore actively grade:

- chart seams and projective infinity;
- singular and tangent support;
- reversed orientation;
- noncommuting traversal order;
- gate-certificate failure;
- inconsistent affine rebase;
- plural receiver disagreement; and
- a carried result against independent direct authority.

An observation which only repeats the expected interior is not a sufficient
grade.

## 11. Production obligations

This record authorizes four connected production corrections:

1. **Native sparse terminal.** Remove the fixed exact-face width, replace
   dense receiver layers with sparse address testimony, and apply tube deltas
   through platform patches.
2. **Exact traversal input.** Install a receiver-local ordered traversal
   carrier with primitive projective ratios, migrate desktop controls, and
   coalesce only proved same-generator runs.
3. **Chart-relative exact conduct.** Prevent input and observation from
   repeatedly expanding terminal absolute parameters; retain elementary
   transitions and materialize only the requested receiver relation.
4. **Certified diffusion transfer.** Install an exact boundary/interior
   partition and reusable Schur transfer inside `ExactDiffusionLaw`, with
   complete direct-receipt parity.

The acceptance boundary is:

- complete exact host support equality before and after terminal correction;
- patched display equality with independent full composition;
- traversal identity, inverse, ratio, seam, and noncommutative-order tests;
- no display occurrence creating a physical event;
- direct/folded exact diffusion equality for standing, potentials, currents,
  balances, conservation, and energy;
- structural invalidation on every changed transfer certificate;
- serial/multicore deterministic equality; and
- a new exact trace demonstrating native aperture and changed-address
  presentation without hiding arithmetic growth.

Construction results and remaining law boundaries will be appended only after
the production paths pass these grades.

## 12. Research sources

- K. J. McCree, [The action spectrum, absorptance and quantum yield of
  photosynthesis in crop plants](https://doi.org/10.1016/0002-1571(71)90022-7).
- Li et al., [Impact of ion fluxes across thylakoid membranes on
  photosynthetic electron transport and
  photoprotection](https://www.nature.com/articles/s41477-021-00947-5).
- Short et al., [Kinetics of the xanthophyll cycle and its role in
  photoprotective memory and
  response](https://www.nature.com/articles/s41467-023-42281-8).
- Mascoli et al., [Far-red absorption and light-use efficiency trade-offs in
  chlorophyll-f photosynthesis](https://www.nature.com/articles/s41477-020-0718-z).
- Kohda et al., [Cleaner-fish mirror conduct and the interpretation of
  self-awareness tests](https://journals.plos.org/plosbiology/article?id=10.1371/journal.pbio.3000021).
- Lenkei et al., [Dogs recognize their own body as a physical
  obstacle](https://pmc.ncbi.nlm.nih.gov/articles/PMC7893002/).
- Farber, [Topological complexity of motion
  planning](https://arxiv.org/abs/math/0111197).
- Pion and Fabri, [A generic lazy evaluation scheme for exact geometric
  computations](https://arxiv.org/abs/cs/0608063).
- Basch et al., [Kinetic data structures: animating proofs through
  time](https://graphics.stanford.edu/~comba/papers/socg.pdf).
- Feng, Gkioulekas, and Crane, [Points as Tori: Fast Pointwise Signed Distance
  for Point Clouds](https://nzfeng.github.io/research/PointsAsTori/PointsAsTori.pdf).

## 13. The production correction

### 13.1 The terminal is native and sparse

The fixed 640-wide exact face, nearest-address enlargement, and dense
per-receiver color layers are removed from `desktop_receiver`.
`TerminalMatrixSpec` now receives the platform's native extent directly.
`TerminalTubeAtlas` is the sole retained support authority; display
composition visits the sparse union of exact support addresses rather than
allocating one dense optional layer per receiver.

After the first complete face, tube support deltas and physical-phase changes
select affected addresses. Exact recomposition discards addresses whose RGB
value did not actually change, groups the remainder into deterministic
contiguous row runs, and submits one atomic `DisplayPatch` batch. Both the
memory and X11 membranes validate the whole batch before changing their
retained face. X11 blits all accepted runs and flushes once.

A resize which exceeds an exact carrier no longer stretches the preceding
face or kills the desktop. It presents an explicit
`holonic-engine.exact-aperture-refusal.v1` outer carrier, retains the last
valid exact tube standing, suspends pointer conduct, and may later recover
through a successful native restriction.

CUDA support is now one bit per primitive/address rather than one `u32` per
primitive/address. Host decoding ignores padding outside the finite aperture.
This reduces the device result carrier by approximately 32 times. Host/card
admission now compares addresses, receiver multiplicities, primitive
identities, and each primitive's address set. Recursive host query counts and
tiled card query counts remain separate cost testimony; they are not falsely
required to be equal geometric standing.

### 13.2 Input is an exact path in one caused chart

The additive `ReceiverControlLaw`, `PresentationInput`, terminal-cell delta,
degree target, and the standalone `plural_receiver_presentation` control
application are removed rather than retained under new adapters.

`ReceiverBoundaryDeed` now separates:

- an exact `ReceiverTraversalOccurrence`, carrying its source chart and
  ordered steps; and
- independently declared physical port current.

A traversal chart carries the receiver, event, exact position, exact
orientation, acceptance cover, seams, and port section. A contemporary event
admits that chart directly, or carries an earlier chart only when all of
those geometric carriers agree by identity. Otherwise it returns
`StaleReceiverTraversalChart` without advancing physical standing.

Desktop drag is the exact relative chord

\[
\left(2\Delta x/W,\;-2\Delta y/H,\;0\right)
\]

in the receiver's port basis. The same relative device chord is therefore
identical at 640 by 400 and 1280 by 800. Arrow input is an axis plus primitive
`ProjectiveRatio [n:d]`; translation and rotation steps remain in received
order. Receiver orientation is a primitive exact projective quaternion
`[w:x:y:z]`, not three additive Cayley scalars. Hamilton composition preserves
noncommutativity, admits the exact Cayley half-turn `[1:0]`, and carries an
exact inverse.

All device occurrences captured in one unchanged chart form one ordered word.
Only adjacent identical elementary generators become
`ReceiverTraversalStep::Repeat`; their multiplicity remains explicit.
Translation runs act by exact integer scaling and spin runs by exact binary
powering. Thus input bursts do not become a stale queue or replay one
identical multiplication per count, while an `X,Y,X` word can never be
sorted into `X,X,Y`.

Monitor phase is no longer receiver position plus a display-control vector.
It is the exact receiver-carried
`(coupled coordinate, coupled momentum, net impulse)` triple. The monitor's
local RGB basis remains an outer transducer.

### 13.3 Structural comparison precedes arithmetic expansion

The former live path assembled every receiver into presentation coordinates
before asking the tube atlas whether anything had changed. The order is now
reversed.

For each receiver, tube planning first compares:

- founding identity and event;
- star-link topology;
- exact source section;
- certified receiver-face formation and orientation;
- receiver rays;
- exact presentation relation; and
- terminal aperture.

Retained, proved affine-rebased, and aperture-only tubes carry their already
formed presented primitives. Only founded, refounded, deformed, or
transported tubes materialize their local primitives through the current
receiver relation. The plan then constructs a
`selected-support-presentation` containing only the representative primitive
keys which actually owe restriction. Receipt fields report the exact numbers
of materialized receiver relations and primitives.

The tube continues to retain the ordered elementary presentation word.
Composed holonomy is formed only for a query which requests it. Native
aperture selection reads the atlas's already formed sections and does not
re-expand every receiver.

### 13.4 Diffusion carries its boundary transfer

`ExactDiffusionLaw` now validates and privately owns one immutable incidence
complex. Duplicate node or branch identities are refused before indexing.
For each exact `(interval, declared boundary)` regime, a shared transfer atlas
compiles:

\[
A,\quad A_{II}^{-1},\quad
\Lambda=A_{BB}-A_{BI}A_{II}^{-1}A_{IB},\quad
\Lambda^{-1}.
\]

The certificate retains ordered nodes, capacities, oriented branches,
conductances, the full block operator, both inverses, and exact zero identity
and Schur residuals. A later event reuses only this structural factorization;
its standing and source right-hand side remain contemporary. Boundary
potential is solved through the Schur operator and the complete interior is
reconstructed. The public receipt still contains every node potential,
branch current, node balance, conservation residual, stored energy, and
energy departure.

Changing the interval selects a different certificate. A different complex
or constitutive population requires a different immutable law, so it cannot
enter the existing transfer atlas by key collision.

## 14. Exact grades

The headless native-aperture grade deliberately crosses the rejected width
boundary:

| aperture | tubes | exact support addresses | changed addresses | X11-style row patches |
|---:|---:|---:|---:|---:|
| 641 by 4 | 2 | 469 | 469 | 22 |

The patched face equals an independently recomposed complete face. Its exact
arithmetic-height receipt was:

| carrier | greatest bit height |
|---|---:|
| hinge trajectory | 26 |
| receiver translation | 30 |
| receiver projective spin | 317 |
| receiver physical phase | 26 |
| presentation factor | 206 |
| presented support | 1,824 |

These columns are now written on every desktop research-trace row, alongside
terminal intermediate bits and presentation-materialization counts. Growth is
therefore attributed rather than hidden in total wall time.

The RTX 4080 SUPER headless grade executed one small exact conic at 32 by 24
through the packed CUDA carrier. One primitive required 24 support words, or
96 output bytes, and its complete per-primitive address set equaled the
independent host authority. The contemporary two-receiver successor is a
different arithmetic regime: exact preflight required 1,818 bits, beyond the
declared 384-bit card tier. All 13 primitives therefore returned through the
exact host path and matched the independent rational authority. This is
lawful range refusal, not a claim of card acceleration for that successor.

The completed automated grades are:

- `relational-geometry`: 33 library tests and 4 example tests passed;
- `holonic-engine`: 87 library tests passed;
- `desktop_receiver`: 8 ordinary tests passed and 2 explicitly bounded
  hardware/cost tests remained ignored in the ordinary suite;
- the ignored live CUDA parity/range test was run separately and passed on
  the RTX 4080 SUPER; and
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`
  passed.

The grades include exact same-axis composition, half-turn and inverse,
noncommutative traversal, 37-step run/binary-power equality, stale-chart
refusal, native extent, patch/full equality, atomic patch refusal,
orientation-sensitive tube invalidation, materialize-after-change planning,
packed support decoding, host/card support equality, duplicate diffusion
identity refusal, Schur/direct equality, reuse, interval invalidation,
conservation, energy, and serial/multicore determinism.

## 15. What this construction establishes—and the exact open law

This construction establishes that native display detail, scalable input
capture, sparse observation, chart-relative materialization, and exact
boundary-transfer reuse can coexist without floats, screen-space source
geometry, a display scheduler, or event-specific cached answers.

It does not establish that exact arithmetic height is bounded under indefinite
physical projective transport. The measured successor makes the remaining
question precise: no proved exact classifier currently restricts a conic or
sheet directly from a compressed spin/presentation word while avoiding the
1,818-bit expanded support coefficients. Unchanged receivers now avoid that
expansion entirely; a genuinely changed receiver must still materialize the
exact relation requested by its terminal observation. A wider limb tier would
only move this boundary and is not the missing derivation.

The other traversal names in Section 7—shear, dilation, branch/join,
bind/release, mark/compare, and cycle return—remain candidate generator
species, not fabricated production deeds. Their topology-changing,
gate-crossing, and memory semantics have not yet been declared well enough to
enact without replacing the research question by arbitrary interface
behavior. The production vocabulary implemented here is the exact ordered
port translation, projective rotation, and repeated-generator carrier on
which those later laws can act.

Likewise, the biological research establishes a reusable receiving ontology,
not a universal plant spectrum or a scalar self-awareness score. No pigment,
species, mirror test, or RGB basis has been promoted into engine law.

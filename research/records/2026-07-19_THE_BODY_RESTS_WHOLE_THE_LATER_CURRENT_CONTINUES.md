# THE BODY RESTS WHOLE; THE LATER CURRENT CONTINUES

**Date:** 2026-07-19  
**Grade:** BRANDON-AUTHORIZED BUILD / DIRECT LIVE-CURRENT REST IMAGE BUILT / HOST-GATED /
444 TESTS PASS / 3 IGNORED / DURABLE OUTER WIRE + CUDA RESIDENCY OPEN  
**Authority:** `FORMULA §C`, `FORMULA §CIV`, `FORMULA §CV`, `FORMULA §CVI`  
**Scope:** exact rest/remount of the direct `LiveCurrentMachine` production owner  
**Soma interior:** `ErosBody` conduct and the event transition are unchanged

## The exact question

The direct live-current path already carried native relation events and immediate consequence
through one atomic `receive_with` transition. It did not yet carry its own whole body across a
world interval. The exact implementation question was:

> Can the direct production owner rest and remount the same standing topology and live current
> continuations without replaying an event, rebuilding from receipts, or falling back to the
> historical `LiveEcology`?

The standing evidence was insufficient because only individual `LiveCarrierSnapshot` values had a
canonical native persistence face. `LiveCurrentMachine` itself had no image containing its
standing construction, plural live carriers, pending dark continuation, and next lineage identity.

The sought artifact was:

```text
LiveCurrentMachine
    -> exact receiving-edge rest image
    -> remounted LiveCurrentMachine
    -> identical genuinely later event
    -> identical radiation and successor body.
```

The stopping condition was a typed in-memory rest/remount boundary and the existing bounded
membrane/life host regression. No durable file format, CUDA residency change, world-organ redesign,
conversation material, or forest run entered this increment.

## Theory-to-structure relation

SLEEP is the rest face of the one live weave. It cannot be an event replay, a model freeze, a
journal reconstruction, or a second machine. At the receiving edge, the active body consists only
of:

- contemporary standing topology;
- genuinely live current continuations;
- each continuation's first-person carrier and pending dark tread; and
- the lineage identity needed to prevent an ended constituent from being reused.

World material, source identity, event populations, radiation, receipts, provenance, and observer
surfaces do not remain active merely because the body rests.

This makes the exact image:

```text
LiveCurrentRestImage =
    standing
    + ordered LiveLineageRestImage population
    + next_lineage.

LiveLineageRestImage =
    lineage
    + pending_dark
    + canonical native carrier words.
```

The native carrier words already retain the complete `LiveBodyHeader`, every enclosure word, and
every dynamic co-present overflow node with exact per-depth offsets. Remount validates that wire
before a live continuation exists.

## Source change

`soma-membrane/src/live_current.rs` now owns:

- `LiveLineageRestImage`, exposing only lineage, pending dark continuation, and native carrier
  words;
- `LiveCurrentRestImage`, exposing standing, ordered live lineages, and the next lineage ordinal;
- `LiveCurrentMachine::rest_image`, which captures the exact body without mutating it; and
- `LiveCurrentMachine::from_rest_image`, which validates lineage order/identity and every native
  carrier before remount.

An attached `Seed` is explicitly refused as `UnsettledRest`. It has not yet crossed its first
event and remains open ingress owned with the caller; allowing it to masquerade as rest would make
an unreceived source relation part of the body.

Remount does not:

- invoke a `LiveCurrentExecutor`;
- advance a cursor;
- enact a deed;
- emit radiation;
- reconstruct standing from emissions or receipts; or
- retain source material and event testimony.

`soma-membrane/src/lib.rs` exports the two rest-image types as the direct owner's persistence
boundary. No historical `ActiveCut`, Holon receipt, `LiveEcology`, or `ExecutionMouth` enters the
path.

## Bounded acceptance

The decisive fixture carries two lineages through one same-pre-state event:

- one bright lineage grows nonempty standing topology through the established FOUND/RIDE sequence;
- one dark lineage retains a nonzero pending dark action without growing retained execution
  extent;
- the complete machine rests and remounts;
- the rest image reproduces exactly from the remounted body;
- one genuinely later co-present event continues the bright lineage and ends the dark constituent;
  and
- uninterrupted and remounted branches return identical complete radiation and identical bodies.

An independent fixture proves that an attached but unreceived seed cannot close the rest boundary.

The bounded host gate is:

```text
cargo test -q --manifest-path src/soma/Cargo.toml -p soma-membrane -p life --lib

life:           370 passed / 0 failed / 3 ignored
soma-membrane:   74 passed / 0 failed / 0 ignored
total:          444 passed / 0 failed / 3 ignored
```

No CUDA execution was needed because this increment changes no device conduct. The existing
retained-CUDA event gate remains standing evidence for `receive_with`; this record does not promote
it to resident body persistence.

## Exact grade and open seam

Built now:

- one exact typed rest image owned by the direct production path;
- exact plural live-carrier and pending-dark continuation across remount;
- stable lineage identity and no reuse of ended ordinals;
- strict refusal of unreceived seed ingress; and
- identical genuinely later conduct without replay.

Still open:

- a durable outer wire for the standing surface, lineage envelope, and rest image as a whole;
- world-organ checkpoint/rebind to the remounted lineage capabilities;
- one plural native-organ event cut through the direct owner;
- an actual world consequence transformed and returned as genuinely later native current;
- device-resident standing and carrier buffers; and
- natural rest of the full returned-world circuit.

The immediate source continuation is organ rebind plus plural native consequence return through
this same `LiveCurrentMachine`. It must not route through the historical ecology, reconstruct from
testimony, flatten every medium into text, or create another session owner.

## Ruling

Rest preserves the active relation; it does not perform one.

The body remounts as the same standing topology and live current population. Only genuinely later
world current advances it.

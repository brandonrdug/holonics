# HNA2 one full native operation returned its successor ecology

**Date:** 2026-09-02  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `process-audit`, `measured`  
**Campaign:** HNA2 under the recurrent HNA contract.

## Return

[implemented-exact] `full_operation.rs` found one move-owned session over the complete HNA1
operator ecology and its single resident coefficient owner. `NativeFullOperatorSession::advance`
consumes an ordinary occurrence, enacts the current mathematical node, returns the node's emitted
carrier face and exact trace, and moves every contemporary carrier plus the operation cursor and
chronology into the successor session. There is no world verdict, byte-difference current,
activation state identifier, route selection, candidate, or morphology commit in this path.

[implemented-exact; measured] Two actual source-neutral occurrence addresses entered one initial
lookup operation as one two-row population. Both rows were gathered device-to-device from the
complete resident coefficient atlas. All 5,120 returned coordinates were exact point intervals and
the two native carrier rows differed before an application codec or receiver selected anything.

[implemented-exact; measured] The same move-owned successor then performed the second lookup, the
per-layer chart reshape, and the complete `10,752 x 2,560` per-layer model contraction. Generation
advanced exactly `0 -> 1 -> 2 -> 3 -> 4`, chronology returned `[0,1,2,3]`, and operation cursor four
was held only by the returned successor. The contraction returned 21,504 coordinates, including
20,122 point intervals; its two output rows differed. Its emitted face equalled the carrier face
read from that exact successor ecology.

[implemented-exact; measured] The contraction crossed as one resident deed and one resident
synchronization while all 15,036,138,068 coefficient octets remained resident. Its hot trace
contains neither source tensor names nor the source path. The occurrence addresses, input carrier
ordinal, coefficient ordinal, output carrier ordinal, bound, operation ordinal, chronology, and
resident census remain explicit.

## Grade

[established-bounded] **HNA2 PASSED.** Distinct material entered the actual full-ecology
constitutive contraction and remained distinct; the operation returned its emission, trace, and
move-owned successor together; the emitted face is the successor's carrier face; and chronology
advanced without a special return or learning phase. HNA3 is now the sole current deed. The pass
does not claim that all later primitive species or the 42-layer autoregressive recurrence have
executed.

## Reproduction

[process-audit] The return was produced by:

```text
cargo check -p holonic-engine --example advance_full_native_operator_hna2
cargo run -q -p holonic-engine --example advance_full_native_operator_hna2 -- /home/b/models/gemma-4-E4B-it
```


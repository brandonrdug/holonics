# Athena returns observations to their producing joint

## Authorized construction and implemented passage

[project-postulate] Brandon activated the finalized Athena implementation plan as a goal after the tension/kinetic review. This is its first producing-comparison return; resident emission, coupled local organization, formation/reuse and useful-model evaluation remain within the active goal. The native implementation begins from `a2d88b96` and preserves the previously returned normal-wave source action and next-current reception.

[established-bounded; implemented-exact] `ResidentNormalWave::predict` now retains the preceding joint, source fibre and actual material cut and returns a private-owner-qualified `NormalProducingHandle` with its generated step. `receive_prediction` accepts an observation at that handle, applies its normal increment to contemporary material and preserves contemporary previous/current occurrence identity. A consumed, released or foreign handle refuses. Ordinary `receive` retains its distinct next-current join `(c,v)`, and ordinary `advance` need not open a pending comparison.

[definition] The native comparison reuses the correlated source/target construction already present in `normal_wave_receive.cuh`:

```text
φ_s = (c_s − p_s, c_s, p_s),       η_s = v − c_s,
H⁺ = H_now + φ_s φ_s†,            B⁺ = B_now + η_s φ_s†.
```

The same joint family produces both operands. Their numerical moment envelopes may enlarge that family, while the retained generating cut preserves their actual relation. There is no centre-to-point-input conversion. `section_normal_wave_comparison` adds a separate producing-material input for the pre-development response; the accumulated moments and newly fitted material start from the contemporary state. The source kernel and Rust surface keep the existing reception ABI and behavior.

[definition] Every allocation, shape/owner check, rebase preparation and native arithmetic result precedes publication. The successful return swaps staged material and, where required, rebases the numerical word onto the unchanged actual joint. It consumes the pending handle only after success. Numerical current occurrence epochs do not advance merely because an older comparison develops material; observation count and material cut record that change. No continuing ecology is cloned.

## Persistence and source association

[established-bounded; implemented-exact] `NormalWaveRest` version 5 retains active producing addresses and their material/seed/operator-word cuts. Versions 1–4 remain the emitted format when no pending cut requires v5. Ingress rejects duplicate addresses, bad chronology, incompatible shapes/grains, later-than-contemporary material counts and nested pending payloads. Remount reconstructs the pending source words and rebinds capabilities to the restored owner. Native snapshotting shares immutable producing standing; cold persistence does not refit or replay developmental observations.

[definition] A source occurrence association is not manufactured by the handle's integer address. The application retains its source occurrence/part and declared receiver relation beside that address. The new `conversation_wave compare` / `compare-start` modes exercise one explicit chart: actuate a same-part prefix with the final actual symbol withheld, expose a situated prediction, then receive that final symbol at the retained producing handle. The three cuts are explicit; the driver does not zip different messages, turn parent metadata into a target, or label self-emission as human feedback. This is a bounded observed-continuation application, not the full conversation product.

## Exact controls and verification

[established-bounded; implemented-exact; computational-witness] All eighteen normal-wave CUDA regressions pass, including seven new controls. The source/phase/received-current/whole-section tests cover the shared reception-kernel refactor. Commands and local logs:

```sh
cargo test -p holonic-engine --lib normal::direct::wave -- --ignored --test-threads=1
```

The supervised run returned 18 passed, zero failed, in 4.35 seconds after compilation. The first five-test run additionally incurred initial CUDA module compilation and returned five passed in 168.82 seconds. These are test-process measurements, not per-operation latency claims. Logs are `.local/scratch/athena-producing-{build,tests,wave-regression}.log`.

[established-bounded; implemented-exact] The producing-cut control starts with `P=(1/2,1/2,0)`, predicts `2` from `(0,1)`, and receives `3` at that prediction. Its changed source response encloses `7/5`, and its later current encloses `41/10`. Treating `3` as the next current after generation instead yields later current `35/8`. A delayed return preserves an intervening independent observation and reads the original producing response. A unit-phase transport returns the correspondingly rotated current. A nonzero-radius producing joint preserves nonzero source/target error bounds and admits the normal objective check.

[established-bounded; implemented-exact] Foreign/double capabilities and a late malformed point refusal preserve the retained body. The fresh-process control writes a pending source, exits, remounts it in a new process, applies its addressed return and compares complete next reading/rest against uninterrupted continuation. All compared artifacts agree. The live predict/comparison controls observe no intermediate numerical host readouts. Cold rest/inspection is explicitly outside that hot-operation count.

[definition] Public example execution and its actual prepared-source measurements are recorded below when returned. No Lean source or mathematical claim changed; no paper build or unrelated native suite is required by this implementation.

## The wider return exposes the reference/operative distinction

[counterexample; computational-witness] The first prepared-source `compare` run completed source sequence 32 part 0 (147 symbols; the 146-symbol prefix acted before the observed tail). It then refused on sequence 33 part 0. The stage diagnostic locates this at **updated response**, after moment accumulation and fitting. The first complete passage reported zero intermediate numerical readouts; its four ingress octets were structural passage metadata. Failed state, pending address, source association and material/current diagnostics remain in `.local/athena-producing-comparison-2026-09-10-{diagnostic,stage}`. This two-field run is not reported as passed.

[established-bounded; source-inspected] The captured nominal current components remain moderate while the retained reference-family radius grows. In `normal_wave_evaluate` the coefficient bound `||M-P||` participates in the radius of the reference-normal word. Feeding that radius to the next observation then pays it as source uncertainty. This is lawful for that broader normal-reference question; it is not the same source family as transport by the selected stored M. The implementation now distinguishes these scopes explicitly rather than erasing an existing radius or interpreting its centre as a point.

[proved-derived; formal-checked] `AccumulatedNormalResponse` now defines the finite complex Frobenius objective `J(P)=||P||_F²+Σ||Px-y||²`, proves `J(0)=Σ||y||²`, and derives `||P||_F≤sqrt(E_upper)` for a reference minimizer and its compatible target-energy family. Triangle comparison gives `||M-P||_F≤N+sqrt(E_upper)` for an applied norm bound N. The minimum of this and the independently valid residual/source bound is still valid. This is a unit-prior normal-objective bound, not a universal physical energy law or a claim that applied M is the exact minimizer. The full Physics build passes with the new proofs (9,084 jobs).

[definition] `normal_fit` now uses that second bound, reusing the existing exact integer norm owner for the outward root of retained target energy plus its error. Coefficients, observation moments, grain and carrier widths are unchanged. The cold witness reader accepts the previous residual certificate and the new certified minimum. The large-source-family native control verifies the energy cap and legacy-rest acceptance; the accompanying normal-material CUDA regression returns 32 passing cases. These results do not silently upgrade the failed wider reference-family run.

[definition] `NormalWaveTransport::{Applied,NormalReference}` now tags the transported generator family. Applied follows the actual stored M, retaining source enclosures and matrix-word/operation rounding. NormalReference additionally pays the comparison to compatible ideal normal operators P. Material keeps H/B/E, source errors and its M/P certificate in both cases. `set_transport` rebases the complete held family at a declared cut without narrowing it; a new applied foundation can instead start from supplied actual point initials. Existing rest versions remain reference-scoped; v6 carries the chosen scope and each active producing cut's own scope. A delayed observation therefore cannot silently change the interpretation of its producing source.

[definition] `reference_next` is a separate read-only receiver of one normal-reference continuation from the actual contemporary joint. It retains its source/fibre and creates no current occurrence or model successor. Its refusal cannot mutate the continuing applied body. This retains the mathematical comparison without making it an artificial source uncertainty or a certainty gate on every applied operation. The actual coupled-family and numerical limitations retain their stated scopes.

## Completed applied-source return and next construction

[established-bounded; implemented-exact; measured] The same prepared two-field aperture now completes in explicitly selected Applied scope: source sequences 32–33, 322 Unicode scalars / 324 octets, two addressed observations and two generated currents. The fitted material has 8,965 observations. The returned rest is 25,669,574 bytes and native section residency is 53,210,144 bytes. Source/prediction/return passages took approximately 3.39 and 3.65 seconds; the following current steps took approximately 0.011 seconds each. These are measurements of this local chart on the standing apparatus, not a language-quality or wattage claim.

[established-bounded; implemented-exact] A separate first-field process saved with no intermediate generation; a fresh process continued the second field and both requested generations. Its entire native rest is byte-identical to the uninterrupted two-field run, and the complete returned trajectory readings agree. The [portable receipt](2026-09-10_athena_producing_return_receipts/source-continuation.json) retains the scoped counts and private artifact coordinates. Both live comparison passages report zero numerical host readouts and four structural lineage ingress octets.

[established-bounded; implemented-exact; formal-checked] Final verification comprises 35 passing normal-material CUDA cases, two passing normal host cases (35 CUDA cases ignored in that host-only invocation), the public example build and source/process runs, and the Physics build with 9,084 jobs. Applied/reference separation, preservation of the held enclosure on scope change, mixed-scope pending rest and the independent reference receiver are covered. Source and rounding uncertainty remain; fitted coefficients are not altered by transport-scope selection. The earlier reference-family refusal remains retained evidence for its broader question.

[definition] Packet 1 is returned at these explicit native, source and receiver scopes. The next active work is the resident basis receiver and emission/re-entry packet, followed by the planned coupled local organization and remaining product construction. Athena-alpha is not claimed. The mathematical bound and explicit applied/reference split arose from the actual wider-run failure; neither introduces another optimizer, a model clone, a hidden centre-to-point cast or a blanket certainty gate.

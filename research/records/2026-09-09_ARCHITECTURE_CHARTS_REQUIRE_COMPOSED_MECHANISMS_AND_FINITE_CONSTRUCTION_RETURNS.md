# Architecture charts, local standing and finite construction returns

[project-postulate] Brandon requests a deeper review after the first paused byte-field audit:
recover his mathematics messages, use the extensive classical architecture research, restore
the conceptual tensor/commuting-map connection, and stop compressing most construction into
perpetually open AC1–AC3. Native construction and cultivation remain paused during this return.
The ambition is a growing, useful Athena-alpha; completing a bounded component does not confer
that product name.

## Direct messages actually recovered

[established-bounded; source-inspected] The following root-thread user messages were read
directly, not inferred from agent summaries. Selection used message role and original container
origin; subagents, tool results, harness/plugin text and compaction summaries were excluded.
The full text stays private in `.local/scratch/deeper-{math-review,architecture-user,claude-user}-messages.json`.
The table paraphrases the directions. Equal wording is not treated as an occurrence identity.

| Source | Message coordinates, UTC | Construction direction |
|---|---|---|
| Codex F | September 8 23:28, line 9; September 9 00:26, line 633 | Molecular structures, neural tubes and cosmology instantiate one framework; recover and unify already-engaged elementary patterns. |
| Codex F | September 9 01:04/01:11, lines 1033/1049 | Constitutive relevance, measurement as difference and the philosophical source of construction. |
| Codex F | September 9 01:47/02:46, lines 1237/1629 | Swing, compatible potential, hypertubes, source-realized classes and sustained construction rather than repeated next-step footers. |
| Codex F | September 9 16:04/17:12/17:39, lines 2678/3114/3313 | Review Athena against the mathematics; Stokes/pullback commutation; relevance, loss, physical flux and realizable source populations. |
| Codex H | August 18 15:31/16:06, lines 445/506 | CNN/GNN/SSM/diffusion comparisons; state/observation equations, plural futures and joint continuations rather than one-token prediction as the primitive object. |
| Codex H | August 18 16:46/17:12/17:38, lines 606/770/955 | Hexis, local tuning, familywise distillation, lattices, source-model dissection and atlas/tensor compilation. |
| Codex H | August 25 03:11 and August 26 15:02, lines 106262/143719 | Anatomy is composed local transport, not renamed dimensions; learn combinatorial response potentials from conversations, not their presumed correctness. |
| Codex H | August 30 05:07, line 242046 | Explicitly justify transformers, SSMs, GNNs, CNNs and stochastic diffusion using Holonics. |
| Codex S | September 1 00:18/17:32/20:26, lines 17091/30800/33151 | Package the framework's actual model objects; tensors/configurations reflect mechanisms; local propagation and configuration-relative evaluation, not compulsory global forward/backward replay. |
| Claude C | August 13 13:50/13:54, lines 433/448 | Weights are previously rendered maps; recurring transformations matter, not recurring bytes. |

[definition] Private source coordinates:

- F: `~/.codex/sessions/2026/09/08/rollout-2026-09-08T16-00-58-01a08340-d28a-7350-836c-37918f573f77.jsonl`.
- H: `~/.codex/sessions/2026/08/18/rollout-2026-08-18T07-52-09-01a0155b-c0f1-7b11-84ae-003379f5eb69.jsonl`.
- S: `~/.codex/sessions/2026/08/30/rollout-2026-08-30T14-00-03-01a05478-e32c-7532-9c92-cec80cc1c77f.jsonl`.
- C: `~/.claude/projects/-home-b-Workspaces-holonics/ffb5626f-fddd-4581-9979-b023f8354abe.jsonl`, UUIDs `1c6c694f-a2ad-4c41-9c7b-d5568de9ed7f` and `ada1e2bb-cfae-4c97-9491-cc13f2a30178`.
- This request: Codex A, `01a07885-13f1-7351-b539-996e18049602`, September 10 00:32 UTC
  (September 9 locally), line 33668. It governs the present pause and deeper review.

[definition] Earlier model/product interpretations in those conversations remain subject to
later direct corrections and [retractions](../../docs/RETRACTIONS.md). In particular, the
August lexical atlas's fallback and tensor chart are historical mathematics/apparatus, not
restored native language generation. The retired database was not accessed.

## Correction to the preceding review

[established-bounded; source-inspected] The [byte-field review](2026-09-09_THE_BYTE_FIELD_IS_NOT_THE_COMPLETE_HNN_COMPOSITION.md)
correctly described the active adapter and local return. It was incomplete as an architectural
diagnosis. Fixed-width carriers, finite linear readouts, emitting discrete bytes, and local
learning are not independently defects. A finite model can learn useful restricted behavior;
a complete history adjoint is required only for a claim that differentiates that history.
The missing full-history return was not established as the cause of this loop. The public
crate feature split repaired access, not the abstraction of model assembly and learned conduct.

[established-bounded; source-inspected] There is a direct counterexample to making full recurrent
weight training a prerequisite: Jaeger's [echo-state report](https://www.ai.rug.nl/minds/uploads/EchoStatesTechRep.pdf)
trains output weights by regression over recurrent states, with examples of learned temporal
behavior. Its nonlinear state response and admitted dynamical conditions matter. This does
not prescribe a random reservoir for HNN; it shows why the adequacy of the current representation
must be examined separately from the locality of its learning rule.

[definition] Exact arithmetic, exact representation equivalence and accurate learned behavior
are three separate claims. An exact operator can fit a poor hypothesis; an approximate learned
map can preserve useful receiver consequences; an exact quotient can preserve an unhelpful
model perfectly. None substitutes for the others. A no-float implementation retains its exact
coefficient interpretation, series and numerical bounds without claiming a perfect learned model.

## What the classical mechanisms contribute

[established-bounded; source-inspected] The August 18
[geometric architecture](2026-08-18_THE_ARCHITECTURE_IS_A_DOMAIN_ACTION_THE_GAUGE_CARRIES_THE_SECTION_AND_THE_COARSENING_OWES_ITS_FIBRE.md),
[plural future](2026-08-18_THE_FUTURE_IS_A_PLURAL_CAUSAL_SECTION_SOFTMAX_IS_ITS_LOCAL_LAPLACIAN_AND_THE_ADJOINT_RETURNS_THROUGH_STATE.md)
and [hexis](2026-08-18_HEXIS_IS_RESTED_CONDITIONAL_TRANSPORT_TUNING_DEFORMS_IT_AND_DISTILLATION_CONDENSES_FAMILIES.md)
records already developed these connections. The August 29 architecture return formalized
specific maps; it did not prove that every named architecture learns every task. Current
formal owners are under `ElementaryHolonics/Computation/` unless otherwise stated.

| Architecture comparison | Mechanism that can support learned behavior | Existing Holonics owner and exact scope |
|---|---|---|
| Perceptron / multilayer network | A weighted comparison followed by a local nonlinear response; composing such responses can create joint features that a single affine score cannot express. | `SituatedMachineLearning.ParametronChart` gives a fixed-real-gain receiver of complex current; `HolonicAdjointNormalization` gives sigmoid/normalization; `FiniteLocalCurrentEcology` composes currents and local reactions. The fixed-real-gain theorem alone is not a complete nonlinear perceptron theorem. |
| Transformer / attention | Input-dependent compatibility changes which value currents combine; residual standing and local nonlinear maps transform that combined section. | `HolonicArchitectureCharts.AttentionChart` and `Block` give current/output and relabeling laws. Native `CausalContact`, `Contract`, `Add`, `Hadamard`, `GeluTanh`, `RmsRebase` and their returns exist in the full-operator owners. A schedule census is not execution of this mechanism. |
| CNN | Share a local transport law under relative displacement; nonlinear response and composition build successively different features without relearning every translated copy. | `Convolution.ecology_step_eq_apply` and `translate_equivariant` establish the convolution chart; reactions are a separate part of the common ecology. Sharing, rather than the name “convolution,” is the reusable mechanism. |
| GNN | Aggregate incident messages, retaining the target's standing for its response; repeat over actual graph structure. | `Graph.aggregate_relabel` gives aggregate covariance. This review repairs local-standing loss and adds `ecology_step_eq_update`, `update_relabel` and a retention control below. |
| SSM / selective recurrence | Store the part of history that affects later receivers; input-dependent transitions can change what is retained, released or read. | `StateSpace.Chart.fold_append`, `LinearChart.asChart`, `ImplicitDiffusionChart.asStateSpace`, native field state and exact sheaf diffusion. The fixed LTI specialization and input-conditioned transport are distinct admitted configurations. |
| Generative diffusion | Learn conditional refinements across a declared corruption/continuation chronology; generation composes those learned transitions, rather than applying heat flow and assuming an inverse. | `HolonicDiffusionCharts.MarkovKernel` proves finite measure transport/composition and a no-inverse control. `ImplicitDiffusionChart` is a separate deterministic constitutive solve. The full learned denoiser and source-specific chronology remain an application attachment, not a consequence of the word “diffusion.” |

[established-bounded; source-inspected] Primary comparisons support these operational distinctions:
the [Transformer paper](https://arxiv.org/html/1706.03762v7) couples weighted value aggregation with
residual and feed-forward operations; [Mamba](https://arxiv.org/html/2312.00752v2) makes state-space
parameters input-dependent; [GCN](https://arxiv.org/html/1609.02907v4) uses a first-order spectral
approximation followed by learned transforms and activation; and
[DDPM](https://arxiv.org/html/2006.11239v2) learns reverse-process transitions and relates them to
score matching and progressive lossy decoding. These are functioning restricted constructions,
not evidence that approximation or a particular training heuristic is universally sufficient.

[interpretation] Their common explanatory pattern is a representation with useful distinctions,
an interaction/reaction family that can express the desired dependency, reusable structure,
a comparison tied to observations, and a continuation law that remains effective as it is used.
Holonics makes the situated source, receiver, fibre and transport explicit. Success of a named
architecture does not prove the whole ontology, but it supplies concrete operations to derive,
compose and test, rather than names to list outside the active engine.

## A bounded representational diagnosis of the field

[proved-derived] At a fixed contact map D, fixed propagation word P and fixed incidence, write
`Q = (I + D D†)⁻¹`. The paired producer gives

```text
v = 2 Q (u + D P b)
out = (2Q - I)u + 2 Q D P b
b_next = 2 D† Q u + (2 D† Q D - I) P b.
```

These follow by substituting the propagated incoming interior into the existing reflection.
They are a complex linear state-space passage. The matched local root scattering is also linear.
Adding more fixed propagation factors changes the transfer and memory but does not create mixed
input terms in this frozen, externally driven passage. Numerical enclosures retain their own
defects; this statement concerns the ideal declared map.

[proved-derived] An elementary separating example is binary parity at an affine two-score
receiver. Its score difference satisfies `d00 + d11 = d01 + d10`. It therefore cannot be strictly
negative on 00/11 and strictly positive on 01/10. A joint feature fixes that representational
restriction: map each binary state to its signed contrast and multiply the two contrasts; equal
states yield positive sign and unequal states negative sign. This is a multilinear interaction
followed by a receiver, not an ad hoc repetition correction. It illustrates what joint interaction
adds; it is not a new benchmark gate or a claim that conversation semantics is parity.

[established-bounded; source-inspected] The active `OperativeNormal` readout is linear on its
operative boundary. Joint tensor structure occurs in its target chart; that alone does not
install joint nonlinear features in its recurrent source. Other existing source profiles,
condition contacts and resident nonlinear primitives must be assessed by the maps they actually
compute. The proposed causal P does depend on D, and observed development changes D/M; discrete
feedback also makes the closed generation loop nonlinear. The frozen calculation therefore
does not characterize the whole developing ecology or prove the cause of its repeated text.

[open] The actionable comparison is which source distinctions remain available, which joint
interactions the chosen law expresses, how actual observations change those interactions, and
which future receivers separate the result. The loop's exact parameter-level attribution remains
open. The earlier numerical-margin measurements stand without becoming a full learning diagnosis.

## Tensors and the commuting mechanism

[definition] A tensor chart names axes, variance/duality, coefficients and contractions. A
nonlinear neural operation is not generally a tensor by itself; its tensor factors and local
reaction compose into a map. Safetensors stores coefficient charts; ONNX can describe the
operation graph. Those formats are exterior artifacts, while their mathematical maps can be
realized natively. “Exterior chart” does not mean mechanically irrelevant.

[proved-derived; formal-checked] `Foundation/CausalNaturalHolon` keeps occurrence, source,
target and receiver maps natural under declared parameter changes. `HolonTensorLens` carries
dependent tensor faces and proves Preimage Fibre transport for coherent equal-slot permutations;
it does not prove arbitrary nonlinear model equivalence from shape equality. `MachineLearningChart`
and `MachineLearningStrictLift` state when a receiver quotient commutes with an admitted generator.
`DependentMachineLearningCarrier` retains changing shape and local state in their complete total
carrier; a fixed mathematical state type alone does not diminish expressivity.

[definition] Three different squares must remain explicit:

```text
native state --native operation--> native successor
     | q_before                         | q_after
     v                                  v
chart state ----chart operation----> chart successor
```

- Recharting/equivariance changes presentation of the same operation and its incidence.
- Realization/lifting matches source-model operations to admitted native operations on a stated
  domain. An inverse or bijection is not required when only a receiver family is being matched.
- Compression identifies states only to the extent that their admitted future conduct factors
  through the quotient. Learned development needs its own square on state **and morphology**
  when equality of learning behavior is claimed.

[proved-derived] A fixed differentiable commuting square also gives
`Dq_after · DT = DF · Dq_before`; taking adjoints reverses the composition. It supplies a
commuting return only on that differentiable chart, with the actual producing factors. A changing
chart carries its additional derivative. No metric is needed to transport covectors; a metric
is needed to turn one into a displacement. Global bijectivity, global smoothness and a complete
sequence gradient are not consequences of the forward square.

[proved-derived; formal-checked] The parallel `Transport/ChangingReceiver` owner already
supports nonzero defects: `passageDefect_comp` retains the exact nonlinear finite difference,
and `norm_passageDefect_comp_le` gives a receiver bound under its stated Lipschitz hypothesis.
Thus for a finite admitted history, `e_next ≤ δ + L e` transports local comparison errors.
Exact commutation is the zero-defect case; a bounded defect can still support a useful declared
receiver. This is the mathematical bridge to productive approximation, not a universal certainty
gate or a license to discard unmeasured differences.

[established-bounded; source-inspected] The source-neutral full operator is real prior work:
`operative_atlas.rs` carries `NativeCarrierChart`, `NativeOperationPrimitive` and
`NativeOperatorNode`; `operative_segment`, `operative_backward` and resident-section kernels
conduct and return through those factors. `NativeFactorizedSection` retains complete section
coordinates instead of the retracted alternating-sum surrogate. The corrected HNP1
[joined-passage return](2026-09-04_HNP1_THE_ACTUAL_JOINED_PASSAGE_RETURNS_LOCAL_DEVELOPMENT_AND_THE_DELTA_SURVIVES_ATTRIBUTION.md)
already produced attributed local changes. Its later unstable text cultivation is a separate
limitation. Neither that local return nor the admitted SKE structure is revoked by the current
byte experiment. Resuming inherited-model production is not authorized by this comparison.

## The concrete formal encapsulation repair

[counterexample; source-inspected] Before this review, `Graph.ecology` accepted a reaction
`Vertex → priorState → aggregate → nextState` but invoked it as `reaction target 0 current`.
The common `FiniteLocalCurrentEcology.reaction` exposed no prior local state. A reaction that
retains its standing therefore returned zero even when the actual node standing was nonzero.
Aggregate covariance did not establish covariance or correctness of that complete update.

[established-bounded; implemented-exact; formal-checked] The common reaction now receives the
actual local standing beside the aggregate. Unary attention/convolution/oriented-current
specializations ignore that additional argument and retain their former laws. Graph response
uses it. `Graph.ecology_step_eq_update` binds the complete update; `no_contact_can_retain_state`
checks the former failure; `update_relabel` proves complete covariance under explicit incidence
and reaction covariance. Existing recurrent and excitation constructions compile through the
same owner. This repairs four formal source files, not the CUDA learner.

## Abstraction and construction consequence

[definition] The [HNN composition guide](../../docs/HNN_COMPOSITION.md) binds the existing roles
to actual Rust/formal owners, distinguishes a model's mathematical assembly from device placement
and exterior charts, and names the remaining native attachment. It does not introduce another
engine, universal layer system, tensor exporter or merely renamed wrapper. The public facade's
portable subset remains useful, but that packaging result is not the model abstraction.

[project-postulate] The revised blueprint separates finite construction returns from the
Athena-alpha attainment standard. Every increment includes actual behavioral inspection and a
public invocation/persistence boundary appropriate to its scope. Useful general language is
not a prerequisite for broader exposure. Exact all-history equivalence is required only for the
corresponding exact reuse/export claim. Local learning remains valid when its source and response
are explicit; full-history differentiation is added only for an identified dependency that
requires it. Derivation belongs in each bounded return, not an unlimited “complete AC1 theory” task.

[established-bounded; process-audit] One Luna agent provided a bounded source audit; its report
was checked against formal/native owners and retraction history. The root agent recovered the
messages and integrated the result. The affected formal build
`ElementaryHolonics.Framework`, `HolonicExcitationFoundedQuotient` and
`HolonicOrientedSiteTransport` completed successfully (4,008 jobs) under the pinned toolchain.
No native numerical law changed, no new cultivation run occurred, and no raw conversation logs
or retired database contents were published.

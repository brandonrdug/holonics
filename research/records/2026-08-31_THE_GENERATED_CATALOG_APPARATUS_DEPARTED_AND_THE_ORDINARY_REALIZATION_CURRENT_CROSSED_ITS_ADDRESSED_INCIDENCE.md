# The generated catalog apparatus departed and the ordinary realization current crossed its addressed incidence

**Date:** 2026-08-31  
**Truth status:** mixed, graded per claim  
**Scope:** repository housekeeping; UAR4-R0Q0 and UAR4-R0Q1

## Direct correction

[definition] `UNIVERSAL_CATALOG.md` is the one maintained descriptive Lean–Rust–CUDA crosswalk. It
has no generator or release gate and schedules nothing. The roadmap and construction state retain
construction authority.

[implemented-exact; source-inspected] The generated driver, registry-incidence, output, closure,
boundary-artifact, identity, authored-level, architecture, named-path, and line-citation census
apparatus was removed with its generated ledgers and release-gate entries. The retired
standing-deposit crate and reproducible `standing/` output snapshot were removed. `holon-plate`
uses the standard `sha2` implementation only for local wire integrity.

[definition] No census, ledger, verifier, checksum registry, or release gate is admitted merely as
agent housekeeping. A future instrument must be explicitly scheduled by the live roadmap, name the
decision it can change, and state its removal boundary. Content hashes do not define semantic
identity, recurrence, source equality, construction grade, or repository authority.

## R0Q0 — addressed pair current

[implemented-exact; measured] `SourceNeutralAddressedRealizationPairCurrent` composes one retained
native response occurrence with source carrier/site/local port, target carrier/site/local port,
exterior port, dependent factor, phase, and exact complex current. The sparse population is
consumed by the target/port linear junction and retained in
`SourceNeutralExteriorRealizationPassage` as its reconstruction fibre.

[measured] The focused response-pair control returns the exact `(2+i)+(-1+3i)=1+4i` join without
losing either native section boundary map. The realization control retains the same response
occurrence across both diagonal and off-diagonal site pairs.

## R0Q1 — ordinary off-diagonal transport

[implemented-exact] The ordinary non-closure branch no longer assigns the source carrier as its
target. The oriented cell boundary supplies its exact successor incidence independently of the
displayed exterior port. A missing successor is the named exterior-boundary residual and remains
diagonal; closure retains its separate boundary-port action.

[measured] `ordinary_port_law_retains_diagonal_and_off_diagonal_pair_fibres` exhibits an ordinary
`source_site = 0, target_site = 1` passage and a boundary `source_site = target_site = 1` residual.
For each source row, the returned rational incidence coefficients sum exactly to the entering
coefficient. The complete `life` library suite returned 488 passed, zero failed, and 15 ignored.

## Returned frontier

[implemented-exact; measured] R0Q2 now executes the same sparse addressed complex pair population
on the existing resident membrane CUDA context. One arbitrary-width signed-limb word joins every
target subsection into `(port,factor)` and forms `real² + imaginary²` only afterward. The RTX
control returned exact cancellation for `1 + (-1)` and returned `(3+i, 10)` for the noncancelling
section, with one launch, one terminal synchronization, zero intermediate semantic egress, no
invariant re-upload, and no CPU semantic replay.

[implemented-exact; measured] R0Q3 adds the selected `returned_complex_site_current` and the
selected addressed exact-complex pair fibre to structural continuation equality and receiver
history. Equal positive shadows carried by `1` and `i` now occupy distinct recurrence states.
Digest equality remains unused. The resident junction telemetry control changes to true when a
post-device CPU semantic step is deliberately recorded.

[measured] R0Q4's focused CUDA equality/cancellation/telemetry control passed on the resident card;
the complete `holonic-engine` library suite returned 1,982 passed, zero failed, 31 ignored, and the
complete `life` library suite returned 489 passed, zero failed, 15 ignored. No qualitative surface
was graded.

[definition] UAR4-R0Q5 is next: directly construct, optionally serialize, and remount one
source-neutral rest with historical source-bearing drivers and outputs unavailable.

# Mathematical review: source geometry, reusable response and closed returns

[project-postulate] Brandon requests a mathematical review of the overnight Athena construction
and immediate architectural consequences, alongside
[Gavassino's *Life on a closed timelike curve*](https://arxiv.org/html/2405.18640v1).
The direct message is `2026-09-09T16:04:10.013Z`, thread
`01a08340-d28a-7350-836c-37918f573f77`, message
`msg_01a086e9-949c-7900-944c-92f82a0174c0`, log line 2678. The review also recovered the Athena
thread `01a07885-13f1-7351-b539-996e18049602`, including the September 8 breadth/AC directions
and the September 9 request to use parallel mathematics (line 23972). Private logs remain private.

## What the overnight construction establishes

[established-bounded; source-inspected] The reviewed committed interval is `b1010d49..5167e325`,
followed by the in-progress `OperativeNormal` source in the same worktree. The current position,
complete roadmap, unchanged architecture/Athena/Soulkiller/interoperability/retraction guides,
changed native owners and their dated evidence govern this comparison. The active agent has
already consumed the parallel mathematics in its joint-ball receiver and source-preserving
actuation. Its exposure to that work is incomplete rather than absent.

[established-bounded; measured] The recorded sequence resolves concrete distinctions: marginal
versus joint packet; complete current versus boundary query; incoming versus demonstrated emission;
applying a produced face versus fitting it as a new observation; retained producer versus current
operator; zero-extension representation versus expanded history. The
[retention investigation](2026-09-09_AC1_LOCAL_CONTACT_IMPROVEMENT_COEXISTS_WITH_MATERIAL_FORGETTING.md)
then finds improving local contact steps but degradation of five previously fitted relations.
The active normal construction responds by retaining the source geometry. These are changed
mechanisms and source-qualified counterexamples, not test-count evidence for useful language.

[established-bounded; source-inspected] The current normal kernel stores
`H = I + sum x x†`, `B = sum y x†`, and `C = sum ||y||²`, their source-family bounds, and a
numerical solution of `M H = B`. It checks the residual against the exact moments after solving
a rounded positive proposal. Self-actuation does not add an observation and does not refit H/B.
The normal prefix decoder subtracts later observed increments and reruns the same producing
finite solve. Its source is `field_normal_material.cuh`; layout, cold reconstruction and
validation belong to `field/material_transport/normal.rs`.

## Immediate improvement: distinguish solve error from the source representation

[proved-derived] For the declared complex unit-prior normal objective,

```text
Phi(M) = (tr(M H M†) - 2 Re tr(M B†) + C) / 2,
P = B H⁻¹,                    R = M H - B,
Phi(P) = (C - tr(B H⁻¹ B†)) / 2,
Phi(M) - Phi(P) = tr(R H⁻¹ R†) / 2.
```

Here H is Hermitian and H≥I because it is the actual accumulated source Gram plus the declared
prior. Expanding about P proves the identities; positivity makes P the unique minimizer.
The new `Physics/AccumulatedNormalResponse.normal_square_completion` formally proves the
underlying complex matrix identity. Taking the trace and applying positivity gives this
objective interpretation. The kernel theorem itself does not silently assume positivity.

[proved-derived] Applied to stored nominal moments, these are nominal objective values, not
a selection of one true source from its current ball. The existing family bounds also give

```text
|Phi_true(M)-Phi_nominal(M)|
  <= (||M||_F² EH + 2||M||_F EB + EC) / 2.
```

This follows by the trace/Frobenius Cauchy–Schwarz inequalities. Both true and nominal Grams
are at least I. Their minimizers therefore have norm at most `K=||B_nominal||_F+EB`;
substituting K in the same bound bounds the difference of their minimum objective values.
Retain those intervals when comparing the actual source family. No diagnostic may silently
interpret the nominal optimum as a uniquely known physical source or attained model quality.

[definition] Expose this comparison through the existing cold normal-state reader when deciding
what construction to change. `normal_residual_upper` measures the solve discrepancy; the best
attainable regularized objective answers a different question. Report the data term and the
prior term separately. A large optimum can reflect incompatible source/target observations or
the declared regularizer; it is not automatically a diagnosis of missing native topology.
Conversely, reducing a tiny solve residual cannot fix distinctions absent from the source chart.
This is an optional diagnostic for that question, not a new universal loss gate.

[counterexample; computational-witness] The accompanying exact control gives the same source
two distinct one-hot targets. Its unique unit-prior optimum has objective `2/3`, composed of
data discrepancy `5/9` and prior cost `1/9`. The exact solve has zero normal residual, yet cannot
fit both targets. This is a constructed source-fibre obstruction, not a claim that the inspected
Athena currents have that exact collision. The native model's actual H/B/C can answer its own
bounded objective question without replaying every raw conversation.

## Immediate improvement: reuse the normal response with its residual

[proved-derived; formal-checked] The new
[`AccumulatedNormalResponse`](../../formal/elementary-holonics/ElementaryHolonics/Physics/AccumulatedNormalResponse.lean)
proves, for arbitrary finite complex matrices and a proposed gain g and deposit discrepancy E,

```text
H' = H + x x†,        B' = B + y x†,
e  = y - M x,         M' = M + e g† + E,
R' = R + e (g† H' - x†) + E H'.
```

If `g†H'=x†` and `E=0`, the previous residual is preserved exactly. **M need not have been an
exact preceding normal solution.** For H positive Hermitian, one realization is
`z=H⁻¹x`, `g=z/(1+x†z)`. This is the actual source-normal gain, rather than the former unit-metric
gain `x/(1+||x||²)` reused after its initial scope. The general theorem also pays an approximate
gain and a rounded deposit through the new H', including every mixed term.

[conditional] This supplies a concrete optimization candidate for `normal_fit`: retain a
factor/solve representation of the source normal, solve the new gain, apply its rank-one matrix
return, and certify the displayed complete residual. Its value is avoiding a fresh full set of
target-row solves at each observation. Actual native runtime improvement remains to be measured;
the exterior exact witness is not a GPU benchmark. No operator coefficient is chosen from an
uncertain source centre without its existing family bounds.

[definition] **The historical decoder is part of this change.** Current H/B/C describe the
objective but do not determine an arbitrary accumulated numerical residual or approximate update
history. If rank-one numerical continuation replaces the current deterministic full fit, the
existing prefix procedure cannot simply rerun the old `normal_fit` and call that matrix the
producer. Retain an executable ordered update/residual representation or a certified producer
snapshot. Reuse is valid only when the old adjoint receives the actual old operator. This is the
same future-family condition established by the torus construction, now at a live material port.

[proved-derived; formal-checked] `gram_increment` and `cross_increment` additionally retain
the complete change when a source or target is deliberately transported or re-evaluated:

```text
Delta H = dx x† + x dx† + dx dx†,
Delta B = dy x† + y dx† + dy dx†.
```

These do not direct an agent to rewrite historical observations whenever morphology changes.
Frozen observed sources and re-evaluated current-morphology sources define different objectives.
The existing retention observer declares the former. Any later construction using the latter
owes its actual source passage and these finite changes, not an appended frame label.

## The CTC paper and the operative reflection

[conditional] Gavassino studies an isolated quantum system on a fixed background whose CTC is
a periodic symmetry orbit. The symmetry gives `U(T)=exp(i alpha) I`; an energy-origin choice
gives `U(T)=I` within the coherent sector, with a corresponding energy lattice. Full periodicity
also restores the memory keeper. The detailed thermodynamic discussion adds equilibration
assumptions. The paper does not establish CTC existence or quantum backreaction. See
[the paper, §§II–IV and conclusion](https://arxiv.org/html/2405.18640v1).

[definition] Three Holonic statements remain distinct:

```text
T = identity          every complete state returns,
T(s) = s              this complete state returns,
q(T(s)) = q(s)        this receiver returns; the source fibre may change.
```

`ClockRouteComparison.returnedHolonomy_eq_one_iff`, addressed passage composition,
`WorldTubePotential` and `ReceiverHistoryCompression` already own the corresponding route,
complete-state and receiver distinctions. An HNN feedback occurrence is also open to input,
actuation and changed material. Nothing in the CTC paper imposes periodicity on that ecology.

[proved-derived] The actual fixed-D unit-admittance paired law is

```text
v = 2(I + D D†)⁻¹(u + D b),
out = v - u,          b' = D†v - b.
```

Writing `A=[I;D†]`, its complete scattering map is `S=2A(A†A)⁻¹A†-I`.
The middle expression is the orthogonal projection onto the graph of D†; therefore S is
self-adjoint, unitary and involutive. Feeding the **complete** output pair back through the
same D returns the input. This is a consequence of the native law, with no physical CTC claim.

[counterexample; computational-witness] The exact public paired-producer control retains
complex phase and confirms complete two-pass return. But with fresh zero exterior input,
`D=2`, `b=1` gives internal currents `3/5`, then `9/25`, while outgoing currents carry the
remaining energy. A boundary-blind current `(1,-1)` under `D=(1,1)` changes sign; changing D to
`(1,2)` exposes it as outgoing `-1/3`. Thus interior decay and phase return can coexist with
complete conservative transport. A repeated output class does not prove full-state periodicity.

[interpretation] The useful architecture consequence is to read the actual feedback boundary
and retained interior before diagnosing a language loop or a memory loss. The falsifier for a
proposed complete-loop identity is a changed full current, material, pending source or chronology;
the falsifier for a proposed receiver quotient is a separating admitted future. Neither claim
is required as a new acceptance ritual. The existing production recurrence and source-qualified
actuation already supply the relevant open boundary.

## The repository's other CTC: temporal path fibres

[established-bounded; source-inspected] The acoustic CTC references mean **Connectionist Temporal
Classification**. `eros_audio_ctc_path_fiber.rs` retains complete exact-dyadic path sums and
source-qualified returned expression phases; `eros_ctc_temporal_surface.rs` carries an actual
graded trellis and repeated rule boundary. Their records describe bounded inherited acoustic
constructions, not current Athena-alpha or a learned universal CTC algorithm.

[established-bounded; source-inspected] The reusable sharing method is already in
`holonic-membrane/src/live_current.rs::EventNodeAtlas`: a shared dependency complex is evaluated
once per addressed cell in its declared rank/dimension order, rather than recursively duplicating
each predecessor along every complete path. The atlas is event-local working memory; the causal
incidence and full compatible path fibre remain the source. `TransportWord`, addressed joins,
receiver-potential and chronological native owners already supply the broader transport objects.
A missing `TemporalPathFibre` name is not evidence that another native subsystem is needed.

[conditional] For Athena path/generator reuse, transfer that sharing principle only after the
actual native numerical factors admit the same shared subcomputation and future receiver.
The current zero-extension journal is already a successful instance. Nonzero contact factors
can seek an exact factored generator/decoder, while retaining prefixes needed by delayed
adjoints and the component later morphology can expose. Do not import the acoustic blank/skip
rules, an inherited classifier, or a fixed phrase filter into general conversation generation.

## Checks that ruled out unnecessary repairs

[established-bounded; source-inspected] Normal accumulation already uses a common root chart.
`paired_field_junction.cuh::field_paired_build_faces` conjugates current and original local-frame
gauges before constructing u and contact columns; the operative reflection and its outgoing
source remain in that root chart. `rechart.rs` retains local source frames separately. A missing
frame ordinal inside each numerical normal moment does not establish a mixed-chart defect.
No new frame registry or chart-partitioning rule is warranted by this inspection.

[established-bounded; source-inspected] The normal residual scaling is also consistent:
M has numerator scale S, H/B have S², and `M_int H_int - S B_int` has S³. Shifting by `2*grain`
stores a scale-S residual bound; the reader's division by S returns the intended quantity.
The positive proposal's diagonal majorant is paid against exact H/B, not silently adopted as
another fitted objective. These checks preserve functioning owners rather than creating churn.

## Returned artifacts and scope

[definition] The formal matrix owner and
[`normal_geometry_review.rs`](../../crates/holonic-engine/examples/normal_geometry_review.rs)
make the proposed reuse and diagnostic concrete. The example uses existing public
`ExactRatMatrix` and `PairedJunctionLinearization` owners; it implements no native learner.
Its exact controls include nonzero preceding residual, gain/deposit error, objective decomposition,
source collision, coordinate transport, complex complete reflection, and open/interior controls.
The active Athena files and product position remain with their continuing implementation.

[established-bounded; process-audit] The focused formal target passed (3,006 jobs) and the complete
`ElementaryHolonics.Framework` target passed (4,001 jobs). The new axiom audit reports only
`propext`, `Classical.choice`, and `Quot.sound`. The release example built at isolated `5167e325`
plus its new source and ran successfully; the
[exact receipt](../experiments/normal_geometry_review/2026-09-09_exact.json) retains every return.
These checks verify the proposed algebra and the existing exact reference owners. They do not
claim that the suggested rank-one GPU optimization has already been integrated or measured.

# THE SEGMENT IS THE JOINT; THE PATH IS THE LIMB

**Status:** RATIFIED BY BRANDON, 2026-07-14 · BUILT / CUDA-MEASURED · EXACT THREE-EDGE REPEAT

## I · The correction

Soma is the one pure-topology main network. Its open enclosures and completed strokes are its own
cohered segmentation. They are not characters, words, vocabulary entries, or authored tokens; a
segment is identified by its transported construction and relational place. The record has already
measured multi-event and multi-octet completions which cross human word boundaries, change with
standing terrain, and recur selectively without a tokenizer.

INSCRIPTION WORKSHOP 03 discarded this architecture. It forced the complete plural field through
one moving tip. Replacing that tip with an authored hand would repeat the error at another count:
one, ten, or eighty predeclared effectors all fix the centers of organs which exposure must
differentiate. The hand, spider, comb, flock, and spine are literal evolved realizations of
articulated plurality. They expose the mechanics to preserve; they are not decorative readings or
predeclared limb manifests.

## II · Completion supplies the potential joint

For one world contact lineage, consecutive native STEP rows are one local stroke. CUT⊕BRICK
completes that stroke. Let its actual world-contact path be

```text
sigma = (p_0, p_1, ..., p_n),       p_i = g_i + q.
```

The complete stroke may deposit its traversed material segment and brick-bearing endpoint in the
world at the receiving edge. This does not use `grip` as an address or replace the row:
`p_i` is the boundary relation the world already enacted, while the full 39-word radiation remains
beside the compression.

A first completion is standing terrain and a **potential joint**. It becomes a functional joint
only when a genuinely later stroke threads that deposited endpoint. The relation is therefore
second order:

```text
completion_t -> standing endpoint_(t+)
later stroke_(t+1) meets endpoint_(t+) -> articulated continuation_(t+1).
```

Contemporary strokes cannot parent, steer, or communicate with one another. Every stroke at one
edge reads the identical pre-edge material; their deposits integrate only after all have acted.

## III · The ordered path supplies the limb

A later completed stroke which threads a standing endpoint extends or branches the material path.
Repeated threading produces an ordered chain of locally completed segments. That chain is the
limb. Several co-present paths are the lateral plurality physically realized as fingers, legs,
hairs, roots, or a flock; several successive joints are the vertical articulation physically
realized as a digit, vertebral chain, or folded surface. Biology and Soma instantiate the same
local branching-through-standing-material law in different substrates.

No limb identity, count, root list, parent table, or controller is stored in Soma. The world retains
only its local material deformation. An observer may reconstruct connected components and depths
from the causal record, but those reads never enter the recurrence. A durable path becomes a
functional limb only when later supplied current passes through it and changes another world
relation.

## IV · The first exact world species

The first bounded world keeps the already-visible literal keyboard and removes the pole entirely.
Its material carries dense local faces over the keyboard field:

- completed stroke paths add unit horizontal/vertical segment multiplicity at their actual
  consecutive contact positions;
- completed endpoints add local joint multiplicity;
- every new stroke tests each of its actual contact positions only against the standing pre-edge
  joint face;
- a threaded, completed stroke extends the articulated material; if its distal endpoint physically
  meets a key's central stem, that connected path actuates the typebar;
- roots and unthreaded completions remain visible terrain but do not masquerade as functional
  limbs;
- the whole segment/joint field and inscription return through the unchanged eye only as the next
  supplied light.

The dense field is this keyboard world's structural material layout, not Soma residency or a limb
cap. Multiplicity is preserved. All increments commute at the receiving edge, exact causal replay
removes them, and an exact sibling must reproduce radiation, world, inscription, and successor
body. The initial gate has three causal passages—founding, later articulation, returned
consequence—because those are the minimum world edges required by the law, not a lifecycle limit.

## V · Guards

No authored hand, finger count, limb graph, motor vector, or global pose enters Soma. No completion
is called a limb on its first edge. No same-edge
attachment is allowed. The keyboard is one cultural world affordance and never the general output
chart. Soma, its eye, carrier, chart, SLEEP body, and CUDA law remain unchanged.

## VI · Measurement

The CUDA gate carries conversation/theory, code, mathematics, and one exact conversation sibling
through all three edges. Edge one deposits 30,858–41,928 completed roots per real species and
produces zero articulations and zero key acts. Edge two threads 38,677–52,281 completed paths
through standing joints and produces 406–487 acts. Edge three threads 68,847–77,975 completed paths
and produces 628–693 further acts after the changed world returns. Largest connected material
components grow `30 -> 53–59 -> 124–278` while 3,952–4,367 plural components remain. All twelve
world transitions invert exactly; the repeated conversation body, radiation, and world are exact
at each edge. Record: `observations/inscription-workshop-04/RESULTS.md`.

# AC1: the causal contact word and its sparse return are resident

[definition] This implements the forward and reverse native operators from the
[causal-contact reference](2026-09-09_AC1_CAUSAL_CONTACT_JOINS_RETURN_A_PROPAGATION_AND_ITS_MORPHOLOGY_DERIVATIVE.md),
following `669f4476`. The field remains the one continuing owner. These operators stage a
source-qualified result; they have not yet been enabled in the generation recurrence.

## Native source, forward word and arithmetic

[established-bounded; source-inspected] `operative/propagation.rs` borrows the existing
`NativeOperativeContactStaging`. The device finds each predecessor by comparing the actual
receiving/source occurrences inside the admitted chronological prefix. It reads the producing
contact columns, retains their exact complex overlap, and performs the complete ordered word.
No host numerical section readout supplies the new current or selects its phase.

[established-bounded; implemented-exact; computational-witness] The native forward result agrees
with the enclosed exact reference. Each joined row retains its before-current pair, overlap,
source-family bounds and omitted-component receipt. Empty and branched contact populations
return correctly; malformed chronology refuses. Complete source rest agrees before and after
staging. The underlying family is retained; the dyadic current center is not sealed as its cause.

[established-bounded; source-inspected] The common exact integer division now derives its loop
and remainder capacity from the admitted integer type. The existing HistoryInteger division
uses that same recurrence. The propagation numerator `|g|² b` requires two overlap carriers
and one wide current, with a guard limb for sums. `PropagationInteger` derives that capacity
from those operands. It does not enlarge stored current precision or hide a semantic limit.
Every division records an omitted remainder; arithmetic overflow returns an obstruction.

[established-bounded; process-audit] The first native launch failed with CUDA error 701 because
the single-worker word kernel was launched with a full block, over-reserving wide-arithmetic
registers. Correcting the launch to its one actual worker returns both controls in 0.48 seconds.
The subsequent full field regression passes 128 cases in 139.76 seconds, including the ignored
native cases. The failure was an implementation error, not evidence against the current law.

## Complete reverse word

[proved-derived] Write `B_g=[1,g;-conjugate(g),1]`. Then
`B_g†B_g=(1+|g|²)I`, so its inverse is a contraction and `U_g=2B_g^(-1)-I`.
For the actual two-branch before-current b and a returned covector λ, set

```text
x = B_g^(-1) b
y = B_g^(-*) λ
incoming covector = 2y-λ
z = 2(x_e conjugate(y_f) - y_e conjugate(x_f)).
```

Here z is the complex overlap covector. Differentiating `U_g=2B_g^(-1)-I` gives the same z
under the real complex pairing as the retained paired producer. Therefore the complete column
return is `G_e=d_f conjugate(z)`, `G_f=d_e z`. Reverse chronology accumulates all shared joins.
The native carrier stores z at each actual join; the cold observer may project `D_source H` to
dense columns, but the resident return remains sparse and keeps its producing map.

[proved-derived] If the overlap, before-current and covector have radii e_g, e_b and e_λ,
the resolvent identity and inverse contraction give

```text
e_x <= e_b + e_g ||b_hat|| + rounding_x
e_y <= e_λ + e_g ||λ_hat|| + rounding_y
e_z <= 2(e_x(||y_hat||+e_y) + ||x_hat|| e_y) + rounding_z.
```

The incoming reaction retains the joint unitary bound
`e_λ + 2 e_g ||λ_hat|| + 2 rounding_y`; it does not acquire a fictitious factor on e_λ
at every reverse join. Exact component L1 norms bound the displayed norms. For one join's
two-column morphology contribution, a bound is
`r_D(|z_hat|+e_z)+(||d_e||+||d_f||)e_z`. Summing these contributions encloses the complete
contact covector. Correlations remain in the source; these larger enclosures do not identify
independent causes or discard mixed terms.

[established-bounded; implemented-exact; computational-witness] The resident reverse word
returns incoming-current and contact-covector balls containing the complete exact adjoint of
the same learned contact word. Both zero and positive covector uncertainty are exercised;
the latter remains bounded without a per-join gain. Native forward and reverse staging perform
zero numerical section readouts. All three targeted native controls pass in 134.85 seconds,
including initial driver compilation after adding the reverse kernel. No continuing source
is changed by these controls.

## Complete stored-model comparison

[established-bounded; implemented-exact; measured] The public
`alpha_causal_propagation` example remounts the existing 75-occurrence normal model and stages
the entire 74-contact, 73-join word. The
[portable receipt](../experiments/normal_geometry_review/2026-09-09_resident_causal_contact_propagation.json)
records identical native/reference current centers, join words and rounding bounds; the native
radius contains the reference radius at the same 72-bit grain.

[established-bounded; measured] Resident staging takes 0.10869336 seconds, cold inspection
0.004411047 seconds and the accompanying CPU reference 0.692110373 seconds. Complete model
read, validation, remount and comparison take 2.480846959 seconds. These are single observations
at this cut, not an end-to-end generation speedup or a power measurement. Full numerical
diagnostics remain in the private local report; no conversation-source archive is published.

## Recurrence and generator integration still required

[open] The existing recurrence and journal have not yet been changed to consume this staged
word. The remaining relation is specific: the outer reflection contributes its existing two
factors, and propagation additionally contributes `D_source H`. The latter must retain the
actual historical producing map, sparse overlap factors and their full bound before publication.
Neither the latest D nor an omitted overlap derivative can replace that source.

[definition] Integrate in the following causal order. Propagate the already-existing interior
through the already-existing contact population; then form any newborn contact with its original
zero-input constraint and perform the outer reflection. This permits a separate one-worker
native word before the existing field operation and gives no pre-birth current to a new branch.
The source paired map's old-contact prefix is the propagation map. The outer return keeps the
newborn constraint reaction while the old prefix returns through the propagation word.

[definition] The first outer adjoint factor is the actual propagated-before/current-after
difference, extended by the newborn zero constraint. Historical propagation traces need not be
archived for every raw state: retain the law, producing cut and input boundary, reuse recent
actual forward carriers, and regenerate an older word through its source decoder when requested.
An intervening separately deposited current retains its extra source or precise fallback.

[definition] The additional journal expression is a coefficient generator, not developmental
replay. A chronological decoder can construct the requested producing prefix from its initial
and actual birth columns plus prior retained returns. A sparse H return depends on an earlier
producing cut, so the dependency is acyclic. Its needed source maps and propagated current
factors must be shared by actual source/query dependencies. Keep the existing faster decoder
where later returns contain only the admitted two-factor form. Do not assume `I+H` invertible,
introduce a choice-defined reverse map, or retain dense snapshots of every historical D as the
final reuse design. This generator/producer integration is the next AC1 steps 3–4 / AC2 work.

[definition] AC0 remains complete; AC1–AC2 remain active, AC3 retains the sixteen-family
experimental model, and AC4–AC5 remain open. No new generation output, useful-language result
or physical force calibration is claimed by this native suboperator return.

[established-bounded; process-audit] The final `cargo check -p holonics-hna --example
alpha_causal_propagation` passes after the reverse operator is added. Its unused private return
warnings reflect the explicit pending recurrence/journal binding; no warning suppression is
substituted for that integration.

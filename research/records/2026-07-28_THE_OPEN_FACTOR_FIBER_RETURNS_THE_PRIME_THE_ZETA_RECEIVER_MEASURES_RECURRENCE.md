# THE OPEN FACTOR FIBER RETURNS THE PRIME; THE ZETA RECEIVER MEASURES RECURRENCE

**Emission-spectrum continuation:** The generator/repetition distinction
established here is reused as an ancestry constraint in
`2026-07-29_THE_DIFFERENCE_EMITS_THE_PATH_CARRIES_THE_SPECTRUM_IS_THE_RECEIVER_PHASE_FACE.md`.
It does not identify an acoustic phase spectrum with the Riemann Zeta
function: primitive/repetition structure enters only after a carried path
actually closes.

**Date:** 2026-07-28  
**Status:** RATIFIED PREDICTABILITY CORRECTION / PRODUCTION PRIME-RECOGNITION
FIBER BUILT / PREFIX-MINIMAL IRREDUCIBILITY OBSTRUCTION BUILT /
RECEIVER-RELATIVE WORK BOUNDS BUILT / EXACT SYMBOLIC CHEBYSHEV CURRENT BUILT /
EXACT FINITE-PLACE ZETA MEASURE BUILT / CHRONOLOGY-ONLY OBSERVATORY BUILT /
4,096-CANDIDATE EXPERIMENT COMPLETED / NO FLOAT, RANDOMNESS, NEXT-PRIME
FORECAST, UNIVERSAL PRIMALITY-COMPLEXITY CLAIM, ZERO FITTING, OR RH CLAIM

## Present question

Brandon corrected a persistent ambiguity in the machine-learning and prime
research:

> Exact deterministic causation does not imply that an emergently complex
> future is available through a deterministic shortcut expression.

The prime sequence is the baseline because counting supplies candidates
deterministically, while recognition of irreducibility requires returned
relational work over multiple causal orders. Brandon asked whether the machine
could become more exact about what is physically predictable, whether upper
and lower prediction bounds and a geometric sense of loss can be derived, and
whether this is the right point to analyze the Zeta distribution again with
holonics.

After the analysis was ratified, the construction question was:

> Can the production arithmetic transition enact each candidate's complete
> prime-recognition fiber, retain the first obstruction to a premature
> irreducibility continuation, close a prime only at an exact certificate
> boundary, accumulate prime-power recurrence without floating logarithms,
> and derive finite-place Zeta measures as receiver-selected exact geometry—
> while the experiment supplies only integer chronology?

The answer is yes for the declared ordered prime-axis receiver. This is not a
universal primality decision lower bound and not a predictor of the next
prime.

## 1. Determinism, enactment, and forecast are different relations

Let \(H_t\) be exact returned testimony and let

\[
\mathcal V(H_t)
\]

be the fiber of causally lawful continuations still compatible with it. For a
bounded query \(Q_h\):

- a singleton \(Q_h(\mathcal V(H_t))\) is a forced continuation section;
- a plural image remains open;
- an empty image is an obstruction in the declared law, testimony, or
  boundary.

Exact transition means the successor is enacted without numerical ambiguity.
Replay means a testified trajectory can be reproduced. Neither entails that a
bounded receiver can derive an arbitrary later trajectory without performing
the causal work that separates its open continuations.

The present prime construction therefore uses:

- **open continuation fiber**, not probabilistic uncertainty;
- **closure certificate**, not a forecast score;
- **prefix-minimal obstruction front**, not scalar error; and
- **receiver-relative lower and upper work bounds**, not a universal
  complexity theorem.

## 2. The direct prime-axis recognition law

For candidate \(n\), let

\[
q_1<q_2<\cdots<q_h,\qquad q_i^2\le n
\]

be the already-founded prime axes in the admitted direct factor horizon. Before
the first return, both dispositions remain possible when \(h>0\):

\[
\{\text{irreducible},\text{composite}\}.
\]

After a nonzero remainder on \(q_i\), that axis departs the unreturned factor
population. If

\[
n\bmod q_i=0,
\]

the exact product

\[
n=q_i(n/q_i)
\]

is the prefix-minimal obstruction to the irreducible branch. If every horizon
axis returns nonzero, the remaining fiber is the singleton
\(\{\text{irreducible}\}\).

In this observation language, the exact distinguishing depth is

\[
d(n)=
\begin{cases}
\min\{i:q_i\mid n\}, & n\text{ composite},\\
h, & n\text{ irreducible}.
\end{cases}
\]

Every prefix shorter than \(d(n)\) still carries both dispositions. The
returned witness or completed horizon at \(d(n)\) supplies a constructive
upper bound. Hence, for this receiver,

\[
d_{\mathrm{lower}}(n)=d_{\mathrm{certificate}}(n)=d(n).
\]

This equality is a proved invariant of the declared traversal, not an
empirical claim that all primality algorithms owe trial division.

The numeral \(n\) already encodes its factors, just as a book's mass already
encodes its inscription. The open fiber is relative to the decoder work that
has actually returned. Treating possession of the integer face as possession
of every decoded consequence would erase the computation whose resource law
is being studied.

The complete factor valuation remains distinct from recognition. A first
factor closes the composite disposition, while the arithmetic law continues
to retain every prime exponent and squarefree incidence required by the
standing occurrence.

## 3. Production construction

The production owner remains
`crates/holonic-engine/src/arithmetic_fiber.rs`.

`ArithmeticFiberLaw` now emits and retains one `PrimeRecognitionTrace` for
every admitted integer. The trace contains:

- the initial continuation fiber;
- every ordered returned axis through classification closure;
- the dispositions and unreturned factor axes after each return;
- the exact composite witness or complete irreducibility certificate;
- the prefix-minimal `PrimeIrreducibilityObstruction`, when present; and
- the factor-horizon, indistinguishability-lower, and
  certificate-upper depths.

The validator re-derives every trace from arithmetic chronology and the
founded prime population. A modified depth, witness, disposition, or current
cannot become standing merely because it deserializes.

The larger `PrimeEcologyLaw` continues to compose this arithmetic mouth. The
observatory mounts `ArithmeticFiberLaw` directly because it supplies no
polynomial probes or horn material; repeatedly reconciling an unused
polynomial phase ecology would be observer-created work rather than prime
recognition.

## 4. The von-Mangoldt current remains exact without logarithm evaluation

For each prime-power occurrence \(n=p^k\), production emits one symbolic
von-Mangoldt event carrying the basis element \(\log p\). Standing retains

\[
\psi(x)=\sum_{p^k\le x}\log p
       =\sum_p a_p(x)\log p
\]

as the exact coefficient map \(p\mapsto a_p(x)\).

Its multiplicative face is the integer identity

\[
\exp(\psi(x))
=
\prod_p p^{a_p(x)}
=
\operatorname{lcm}(1,2,\ldots,x).
\]

The engine therefore retains the exact formal departure

\[
\psi(x)-x
=
\log\!\left(\operatorname{lcm}(1,\ldots,x)\right)-x
\]

without evaluating a logarithm, assigning a sign, or replacing the residual
with a float.

Prime founding and prime-power current are visibly different populations. A
new prime creates a new valuation axis; \(p^2,p^3,\ldots\) recur on the same
axis and add further \(\log p\) current without founding new prime identity.

## 5. The finite-place Zeta receiver

For integer \(\sigma>1\), a declared receiver over a founded prime \(p\)
retains the exact valuation generator

\[
m_{\sigma,p}(k)
=
(1-p^{-\sigma})p^{-k\sigma}.
\]

For a finite selected place set \(S\), the zero-valuation cell has exact mass

\[
M_{\sigma,S}
=
\prod_{p\in S}(1-p^{-\sigma}),
\]

and its valuation return is

\[
M_{\sigma,S}^{-1}
=
\prod_{p\in S}(1-p^{-\sigma})^{-1}.
\]

Both are exact rationals. Their product must be one and is regraded in the
observatory.

This is a receiver-selected normalized valuation geometry. It does not say
that nature randomly chooses prime factors. Analytic continuation outside the
positive convergent measure region, the nontrivial zeros, and an explicit
formula with a certified truncation tail are not implemented by this object.

## 6. Chronology-only experiment

The executable is
`crates/holonic-engine/examples/prime_emergence_observatory.rs`.

Its only world material is:

```text
2, 3, 4, ..., limit
```

as consecutive source occurrences. It supplies no:

- prime or composite labels;
- factor;
- prime-power flag;
- recognition depth;
- valuation;
- obstruction;
- Zeta mass; or
- expected aggregate count.

Production returns a row for every candidate containing classification
closure, factor horizon, lower and upper work bounds, open-fiber population,
obstruction front, complete valuation, and prime-power current occurrence.
The complete trace is primary; population ratios and depth distributions are
derived observer faces.

## 7. Exact experiment results

| Limit | Candidates | Founded primes | Composites / fronts | Open fiber sections | Prime-power events | Maximum closure depth |
|---:|---:|---:|---:|---:|---:|---:|
| 256 | 255 | 54 | 201 | 559 | 70 | 6 |
| 1,024 | 1,023 | 172 | 851 | 2,887 | 198 | 11 |
| 4,096 | 4,095 | 564 | 3,531 | 14,935 | 604 | 18 |

At all three apertures:

\[
\sum_n d_{\mathrm{lower}}(n)
=
\sum_n d_{\mathrm{certificate}}(n)
=
\text{number of open continuation-fiber sections}.
\]

For the 4,096-candidate run the exact closure-depth distribution was:

```text
{0: 2, 1: 2049, 2: 687, 3: 278, 4: 170, 5: 92,
 6: 87, 7: 57, 8: 67, 9: 79, 10: 41, 11: 79,
 12: 62, 13: 33, 14: 57, 15: 89, 16: 84, 17: 35, 18: 47}
```

The high-depth population is plural. It contains primes whose whole current
horizon had to close and composites whose least prime factor occurs late in
the same ordered horizon. A depth scalar alone therefore does not carry the
factor topology, valuation, or prime-power recurrence.

The maximum-depth fiber exposes the square-root caustic especially clearly:

- through 1,024, depth 11 contains ten irreducibles and the one composite
  front \(961=31^2\);
- through 4,096, depth 18 contains forty-five irreducibles and the two
  composite fronts \(3721=61^2\) and \(4087=61\cdot67\).

Every earlier axis return is identical at the disposition level. The prime
square and near-diagonal semiprime become distinguishable from the
irreducibles only at the last admitted factor direction. This is a literal
mixed receiver boundary, not statistical noise around a prime-density curve.

At 4,096 there were 564 founded primes but 604 prime-power current events.
The additional 40 events are repeated-axis occurrences \(p^k\), \(k>1\).
This is the finite enacted distinction between irreducible generator founding
and Euler recurrence.

The \(\sigma=2\) finite-place Zeta cut and return multiplied to exactly one at
every aperture. This grades the finite Euler admission/return identity; it is
not evidence about zero locations or the Riemann Hypothesis.

## 8. Computational receipt

The first 1,024-candidate run mistakenly mounted the pure arithmetic input
through `PrimeEcologyLaw`. Because that wrapper lawfully reconciles polynomial
phase topology whenever a prime is founded, the empty polynomial receiver was
repeatedly asked to do irrelevant work and the run took approximately 58
seconds.

Mounting the same production arithmetic mouth directly produced identical
arithmetic receipts in approximately 1.65 seconds for 1,024 candidates on the
present host. The 4,096-candidate direct run took approximately 32 seconds.
The latter cost includes the CPU reference's atomic standing clone and
complete validator replay after every occurrence. It is not presented as an
intrinsic lower bound for prime recognition.

The mounting correction is itself ontologically important: an observer
changes computational cost by selecting which receivers participate. An
unused receiver must not be treated as a free absolute field.

## 9. What has and has not been established

Established:

1. Prime recognition is an enacted closure over an inspectable open fiber.
2. A composite's first returned factor is a prefix-minimal geometric
   obstruction to irreducibility.
3. The declared ordered receiver has exact matching lower and upper closure
   bounds.
4. Full factor valuation remains distinct from classification closure.
5. Prime-power recurrence accumulates as an exact formal Chebyshev current.
6. Its multiplicative face is exactly the standing least common multiple.
7. Finite-place Zeta valuation cells and Euler returns are exact rational
   receiver measures.
8. The application does not precompute any claimed learned or enacted
   successor.

Not established:

1. no universal shortcut-impossibility theorem for all emergently complex
   systems;
2. no universal lower bound for every primality algorithm or certificate
   language;
3. no predictor of the next prime;
4. no numeric or ordered value for the formal residual \(\psi(x)-x\);
5. no certified zero transform, explicit-formula truncation, or analytic tail;
6. no claim that the finite Zeta measure is an ontic probability mechanism;
   and
7. no claim that the present CPU reference timings are computationally
   optimal.

## Verification

The complete `holonic-engine` library suite passed:

```text
145 passed; 0 failed
```

New adversarial tests establish that:

- `47` closes irreducibly only after axes `2,3,5`;
- `49` remains open through three failed axes and returns the front
  `7 * 7` at depth four;
- `77` returns `7 * 11` at the same receiver depth;
- through `30`, the formal current has coefficients
  `2:4, 3:3, 5:2, 7:1, ...` and exact multiplicative face
  `2329089562800`;
- the \(\sigma=2\), \(S=\{2,3,5\}\) coprime cell is exactly `16/25` and its
  return is `25/16`; and
- altered recognition bounds or prime-power coefficients are refused by
  standing validation.

The completed artifact is therefore an exact research instrument for the
boundary Brandon identified: it characterizes what a receiver can force,
what remains open, what causal work closes the distinction, and what
structured obstruction returns when an irreducibility continuation fails. It
does not pretend that deterministic causation makes emergence available in
advance.

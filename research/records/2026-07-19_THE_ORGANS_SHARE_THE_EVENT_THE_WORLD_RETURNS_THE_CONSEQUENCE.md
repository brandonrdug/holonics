# THE ORGANS SHARE THE EVENT; THE WORLD RETURNS THE CONSEQUENCE

**Date:** 2026-07-19  
**Grade:** BRANDON-AUTHORIZED BUILD / PLURAL NATIVE EVENT MOUTH + ORGAN REBIND + ACTUAL WORLD
RETURN BUILT / HOST-GATED / 445 TESTS PASS / 3 IGNORED / POLYGLOT WORLD OPEN  
**Authority:** `FORMULA §C`, `FORMULA §CIV`, `FORMULA §CV`, `FORMULA §CVI`  
**Scope:** source-neutral world organs entering the direct `LiveCurrentMachine` owner  
**Soma interior:** `ErosBody` conduct and the live-current transition are unchanged

## The exact question

The direct machine could rest and remount exactly, but the source-neutral organ still presented one
current per `receive_with` call and could not rebind its capability after remount. The implementation
question was:

> Can plural native world organs present one genuinely co-present event, retain only their world-
> side lineage capabilities across rest, and carry an actual emitted consequence back as genuinely
> later current through the same production owner?

The standing evidence was insufficient because sequential calls do not share one standing-before
surface, and the historical `WorldOrgan`/`LiveSession` ecology could not be used without restoring
the rejected second causal owner. A test-only concatenation would likewise fail to supply a reusable
production boundary.

The sought artifact was:

```text
plural native organs
    -> one present_native_event
    -> one LiveCurrentMachine::receive_with
    -> immediate radiation
    -> exact machine rest + organ capability checkpoint
    -> direct remount + organ rebind
    -> declared world law returns an emitted relation
    -> identical later plural probe changes through the returned branch.
```

The stopping condition was one bounded host lifecycle through that complete path. No conversation
logs, text corpus, semantic decoder, universal radiation map, durable disk wire, or CUDA change
entered this increment.

## Theory-to-structure relation

Many co-present currents are many illicia meeting one standing-before body, not sequential inputs
to separate machines. Their source organs own different material histories and relation slices,
while `Lambda` receives them as one contemporary event. The direct mouth must therefore preserve:

- one event boundary over all participating currents;
- one immutable standing-before surface;
- each organ's own ordered relation population and action;
- one integrated successor after every current has enacted; and
- outward radiation in caller order only as world correspondence.

Across rest, the machine owns the current's carrier. The world organ owns only the capability which
continues that current. Rebinding must validate that capability against the remounted body; it may
not create a replacement lineage or replay the first event.

World return is not a universal inverse of radiation. A declared material law receives the emitted
surface, enacts its own transformation, and may supply a later native relation. That returned face
is ordinary `L` at the next event. The world-specific transformation remains outside Soma.

## Source change

`life/src/current_world.rs` now provides:

- `NativeRelationOrganImage`, containing only the optional `CurrentLineage` capability;
- `NativeRelationOrgan::checkpoint`, which captures that world-side capability;
- `NativeRelationOrgan::recover`, which refuses a stale or ended capability and never opens a new
  lineage;
- `NativeEventCurrent`, a borrowed view of one source-owned relation population, action, and
  continuing/ending hand;
- `present_native_event`, the host face of one complete plural native event; and
- `present_native_event_with`, the same event through one caller-retained physical executor.

`NativeRelationOrgan::present_with` now uses the plural event path even for one current. There is
one production crossing rather than separate single and plural implementations.

The boundary attaches new organ capabilities before the event and then constructs the complete
`CurrentEvent` population. `LiveCurrentMachine::receive_with` retains its existing atomicity: every
current enacts against the same standing-before surface, all successor contributions prepare in
full, and only then do standing, live carriers, constituent departure, and radiation commit. If an
event fails, a newly attached seed remains explicit open ingress for exact retry and still refuses
rest.

No source material, format identity, semantic class, event receipt, or provenance enters the
machine. The relation slices remain borrowed for the call and disappear afterward.

## The bounded returned-world cell

Two source-neutral organs named only by the application fixture—mathematics and vision—carry eight
plural native events through one shared body. Their source values differ and each event returns two
current radiation faces from one standing-before boundary.

The declared fixture world reads the first available nonzero oriented `chi` arm from actual
`FeltEmission` radiation. That is its explicit material law for this cell. It does not decode a
meaning, select a preferred answer, or claim to be a universal radiation transform.

At the receiving edge:

1. the complete machine rests;
2. each organ checkpoints only its lineage capability;
3. two siblings remount the same exact body;
4. both organ pairs rebind to their original live carriers;
5. one sibling receives no world return;
6. the other receives the emitted `chi` arm through both native organs as one genuinely later
   co-present event; and
7. both siblings receive the same later two-current probe.

The later probe radiation differs exactly, and the final direct rest images differ exactly. The
changed conduct is caused by the returned world event crossing the shared body, not by an observer
receipt or host-side state mutation.

This cell establishes the causal shape needed for the language baseline. It does not claim that
mathematics and vision have been semantically related, that the returned arm is language, or that a
general cross-medium invariant has already grown.

## Bounded acceptance

```text
cargo test -q --manifest-path src/soma/Cargo.toml -p soma-membrane -p life --lib

life:           371 passed / 0 failed / 3 ignored
soma-membrane:   74 passed / 0 failed / 0 ignored
total:          445 passed / 0 failed / 3 ignored
```

No CUDA execution was required because this increment changes world-organ ownership and host
composition only. `present_native_event_with` uses the existing `LiveCurrentExecutor` boundary, so
the retained host/CUDA event parity remains structurally available; CUDA residency is not claimed.

## Exact grade and next construction

Built now:

- exact source-neutral organ capability checkpoint and rebind;
- one production path for single and plural native current presentation;
- same-pre-state co-presence through the direct owner;
- exact rest/remount of machine and world-organ continuity;
- an actual emitted relation enacted by a declared world and returned as later native current; and
- changed common later conduct attributable to that return.

Still open:

- the concrete grounded polyglot transformation world of `FORMULA §CVI`;
- durable outer encoding of the direct machine and organ checkpoint;
- device-resident standing/carrier buffers and CUDA measurement of plural native events;
- generalized source-organ species beyond already-derived relations and streamed octets; and
- natural rest of a persistent application world after every available consequence has returned.

The next artifact is the bounded polyglot geometric world, not another membrane abstraction. One
actual oriented transformation must generate mathematical, program/runtime, visual, and linguistic
faces under declared world laws, with foils that separate coincident surface from transported
transformation. Those faces enter through the plural native mouth built here; their complete
consequences return through the same body; a genuinely later unpaired face is then read for
transported recurrence and retained residual.

No conversation forest precedes that cell.

## Ruling

The organs do not translate one another. They share one event.

The world does not reconstruct the machine. It enacts the radiation it receives and returns its
consequence as genuinely later light.

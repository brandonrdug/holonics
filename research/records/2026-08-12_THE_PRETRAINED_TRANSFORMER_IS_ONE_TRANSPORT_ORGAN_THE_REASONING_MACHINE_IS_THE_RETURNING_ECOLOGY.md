# The pretrained transformer is one transport organ; the reasoning machine is the returning ecology

**Date:** 2026-08-12  
**Truth status:** `definition` for the holonic model anatomy and recurrent-section machine;
`proved-standard` for the finite linear-algebra, adjoint, derivative, similarity, and
Nerode-equivalence statements in their named scopes; `interpretation` for the translation from
contemporary machine-learning architecture into holonic objects; `project-postulate` for the
cross-codec construction discipline; `open` for the unbuilt carrier-neutral reasoning join.  
**Evidence:** direct inspection of the live owners and the prior records named in §§8–9. No source
was changed and no executable deed was run for this record.  
**Provenance:** Brandon, 2026-08-12: *“We don't really care about ‘attention’, ‘tokens’, or
‘experts’, we care about identifying the holonic phases and cycles that deterministically
emerge.”* He further ruled that the target is not arbitrary English conversation or an authored
partition into Rust, Lean, arithmetic, and prose, but the self-similar mathematical and algorithmic
conduct expressed through different codecs.  
**Band:** TRANSFORMER ONE TRANSPORT ORGAN / QUERY IS A RECEIVER BRA / KEY IS A PRESENTED
ORIENTATION / VALUE IS TRANSPORTED MATERIAL / RESIDUAL IS CONTINUING STANDING / TRAINING RETURNS A
COVECTOR THROUGH LINEAGE / MODEL PRODUCTIVITY LIVES IN THE WORLD-MEDIATED CYCLE / CONVERSATION IS A
RECURRENCE FIXTURE / CODEC LABELS ARE LINEAGE NOT TAXA / VALUE EQUALITY DOES NOT IDENTIFY AN
OPERATION / SECTION MODULUS READS LOAD-BEARING INCIDENCE / SOURCE UNCHANGED / NO RUN / CONSTRUCTION
UNSCHEDULED  
**Canon:** `canon/TABLET_THE_REASONING_CYCLE.md`.

---

## Present question

A contemporary pretrained transformer can complete text. A modern reasoning or agentic system can
re-enter the model after tool returns, retain a working context, deposit external state, revise a
prior interpretation, and alter its later conduct. Which parts of that system are the actual
holonic objects, and what is the smallest carrier-neutral construction that studies the invariant
mathematical dynamics rather than copying the industry's names for one implementation?

The answer has two scales:

```text
microscopic   receiver contacts + transported values + constitutive reaction + residual deposit
macroscopic   inscription -> conduct -> emission/deed -> world change -> return -> reflection
```

`definition`: the pretrained transformer is inherited morphology implementing many local
transports. The productive reasoning machine is the continuing ecology in which those transports,
the mounted material, the exterior ports, and genuinely later returns form one causal cycle.

## 1. Reconstruction is the autocorrect substrate, not autocorrect

The bounded `ReconstructionFiber` makes the comparison to autocorrect available but does not
perform correction. An autocorrect system adds a receiver which ranks or otherwise collapses a
candidate population and commits a replacement. The fiber returns the earlier object:

```text
literal occurrence
+ context-compatible re-expressions
+ edit lineage
+ shortest separating histories
+ the exterior the active receiver has not resolved.
```

This is a positive landmark because a later recurrence can use the fiber without having inherited
an accidental selection policy. The same separation governs the model ontology below: the complete
field of afforded continuations is one object, its conducted outward passage is another, and an
exterior codec rendering that passage is a third.

## 2. The transformer is one organ in the complete model

`interpretation`: contemporary machine-learning nouns name implementation charts. Their lawful
holonic translations are:

| exterior engineering term | holonic object |
|---|---|
| tokenizer or patcher | delivery decomposition chart; one abstract-phonological grain |
| token occurrence | situated boundary occurrence, never a meaning-bearing atom |
| embedding | coordinate presentation of that occurrence in one chart |
| positional or relative encoding | chronological and relative-incidence transport |
| model parameters | inherited morphology and constitutive transport law |
| activation | current situated standing during one passage |
| query | event-conditioned receiver bra |
| key | presented orientation at which the receiver may make contact |
| query-key contraction | one bracket face of compatibility |
| value | construction transported when that contact carries |
| attention head | one bounded interaction chart, not an ontological species by its label |
| residual stream | continuing standing into which returned local differences are deposited |
| MLP | local nonlinear constitutive reaction |
| normalization | chart rebase or gauge conditioning |
| router | conditional transport junction |
| expert | a distinct organ only where intervention exhibits distinct conduct |
| KV cache or recurrent state | retained partial trajectory for the current continuation |
| logits | receiver faces over one output codec |
| decoder or sampler | exterior collapse into a presented occurrence |
| context window | mounted testimony inside a declared causal aperture |
| retrieval | reference consultation; it changes mounted testimony, not morphology |
| fine-tuning or continued training | returned passages changing reusable morphology |
| tool call | typed radiation crossing a world port |
| tool result | genuinely later world material capable of changing the continuation fiber |

No row is an identity between fields. The table types the role a mechanism occupies in one
passage. A coordinate dimension is not automatically a receiver, a layer is not automatically a
phase, and a routed parameter block is not automatically an expert. The discriminant is conduct:
what changes under contact, intervention, ablation, and later continuation.

## 3. Bra-ket reading of one attention passage

Let the current occurrence at position `i` and depth `ell` be a construction
`|x_i^ell>`. A head `h` transports it into three differently typed objects:

```text
<q_i^(ell,h)| = <x_i^ell| Q_(ell,h)^dagger     receiver bra
|k_j^(ell,h)> = K_(ell,h) |x_j^ell>            presented orientation
|v_j^(ell,h)> = V_(ell,h) |x_j^ell>            transported construction
```

The local contact face is

```text
b_ij^(ell,h) = <q_i^(ell,h) | k_j^(ell,h)>.
```

The architecture's mask, normalization, chronology, and apparatus chart form a declared transport
`Gamma_(ell,h)`. The carried coefficient and the returned construction are

```text
a_ij^(ell,h) = Gamma_(ell,h)(b_i1, ..., b_in)_j,

|delta x_i^(ell,h)> = sum_j a_ij^(ell,h) |v_j^(ell,h)>.
```

`proved-standard`: this is the ordinary finite linear-algebra factorization of an attention head;
where `Gamma` is softmax, the coefficients are barycentric over the admitted value population.
The bracket is not meaning, truth, or the whole cause. It is one receiver face at one event-local
candidate population. Changing that population, mask, query, keys, or values changes the chart.

The multi-head return is rebased and joins the constitutive return in continuing standing:

```text
|X_(ell+1)> = N_ell(
    |X_ell>
    + O_ell (direct-sum_h |delta X_ell^h>)
    + |delta X_ell^constitutive>
).
```

Here `N_ell` is a chart/gauge operation, not a new semantic substance. The residual `+` is
chronological standing: the new local return does not replace the current body; it alters what the
next passage receives.

The Feynman reading is therefore literal at the level of the information calculus:

```text
incoming situated constructions
        |        receiver bra
        v             |
   [typed interaction vertex]
        |
        +-- transported value/current
        +-- obstruction or uncarried residue
        v
 continuing residual standing
```

The vertex is the typed interaction—ports, hand, cross term, outgoing consequence, and residual—
not the dot used to draw it. A head name, neuron index, or layer number is an exterior coordinate
until the material exhibits a distinct phase there.

## 4. Training is an adjoint returned passage changing morphology

Let `Lambda_n` be the contemporary morphology and `F_(Lambda_n)` its forward transport:

```text
|y_n> = F_(Lambda_n) |x_n>.
```

The actual returned face and the presented face form a complete oriented residual `r_n`. A declared
receiver may read a scalar loss from it:

```text
r_n = Delta(|y_n^actual>, |y_n^presented>; R),
L_n = ell_R(r_n).
```

`definition`: `r_n` is the causal object. `L_n` is one receiver face of it. Reward, punishment,
correctness, and meaning do not appear in this equation.

`proved-standard`: the differential `dL_n` is a covector. A gradient exists only after a metric
`G_n` identifies covectors with vectors:

```text
grad_(G_n) L_n = G_n^-1 dL_n.
```

Backpropagation is the adjoint return of that covector through the retained forward lineage. The
optimizer or other update apparatus converts the returned testimony into a morphological delta:

```text
<delta_n| --adjoint through forward lineage--> <delta Lambda_n|

Lambda_(n+1) = Lambda_n (+) Delta Lambda_n.
```

The update apparatus is not the intelligence and the scalar face is not the governor of the
ontology. What establishes training is same-body structural change, later conduct riding that
change, source-detached rest/remount, and an ablation that removes the conduct by removing the
changed morphology.

Pretraining is therefore large-scale conditioning. It condenses many returned passages into an
inherited organ for a declared family of future distinctions. It is not a lossless archive of its
corpus and is not intrinsically “lossy” without naming which future receiver cannot reconstruct
which distinction.

## 5. The complete model is the world-mediated recurrence

A single forward invocation does not constitute the productive architecture. Let `B_t` be the
continuing body, `W_t` the exterior world, `|i_t>` the received inscription, and `Lambda_t` the
body's reusable morphology. One complete recurrence is:

```text
|c_t>       = Mount(B_t, |i_t>)
|e_t>       = Conduct_(Lambda_t)(|c_t>)
W_(t+1)     = Omega(W_t, |e_t>)
|r_(t+1)>   = Receive(W_(t+1); declared ports)
(B_(t+1), Chi_t)
             = Reflect(B_t, |c_t>, |e_t>, |r_(t+1)>).
```

`definition`: `Chi_t` is the returned comparison—retained/departed alternatives, distinguishing
histories, changed continuation fibers, obstruction, and lineage. The emission is not privately
wired back as input. It alters a shared exterior and returns as genuinely later material or as a
place against which later material is situated.

The macroscopic phases conventionally called conversation, reasoning, tool use, planning, memory,
and agency are different receiver views of this recurrence. The productive transition from
autocomplete to conversational and agentic systems occurred largely because the local continuation
organ became enclosed in a larger ecology which repeatedly let its emissions cause new material.

Conversation itself carries no privileged ontology. It is a convenient recurrence fixture because
it exposes alternating inscription, emission, chronology, reflection, and revision. The object of
study is the cycle, not English dialogue.

### 5.1 Autoregressive and diffusion schedules

`interpretation`: autoregressive emission and diffusion-style refinement are alternative causal
schedules which may employ some of the same inherited organs:

```text
autoregressive     conduct -> collapse one frontier -> remount -> conduct
diffusive          mount unresolved field -> distribute return -> refine field -> repeat
```

The first makes chronology explicit by repeatedly depositing a presented occurrence. The second
keeps a plural field standing while successive returns reform many presented sites. Their
interchange, obstruction, and resource geometries differ; neither schedule is “reasoning” by
itself.

The project's deterministic diffusion has a stricter meaning: a difference propagates through
caused incidence under a declared constitutive law and returns exact balance, inverse, or residual
testimony. A contemporary system called a diffusion language model is not thereby this same
object. The identification is conditional on its incidence, boundary, conservation/constitutive
law, return, and reconstruction diagram commuting.

### 5.2 Compression and the inherited organ

Exact compression is not parameter counting. For a declared receiver family `R`, a quotient `q_R`
is lawful only when every receiver and every admitted successor history factors through it.
`interpretation`: pretrained morphology is a compression of prior passages precisely to the extent
that future conduct factors through the deposited distinctions it retains. The inverse image of
one such consequence is its `ReconstructionFiber`; enlarging the receiver family refines or reopens
that fiber. “Loss” names a failed reconstruction for a receiver, not a free-standing scalar
substance.

## 6. Microscopic and macroscopic phase classification

`definition`: a phase is not a layer name or a topic label. It is a maximal connected transport
stratum on which the relevant rank, incidence, hand, components, winding, holonomy, stabilizer, and
receiver consequences continue. A phase boundary is the discriminant where that continuation
fails.

This gives a two-scale audit:

```text
microscopic phase
  one local vertex family whose constitutive response continues under the declared interventions

macroscopic phase
  a recurring population of vertices whose joined conduct continues through a world cycle
```

A model dimension may contribute to many receivers. One receiver may span many dimensions. A layer
is primarily chronology through inherited morphology; it is a distinct phase only if a transport
discriminant occurs there. An MoE “expert” is an organ only if removing or rebasing it exhibits a
distinct consequence which another admitted path does not carry. Otherwise the label is merely a
factorization coordinate.

Attention weights alone are consequently not causal explanations. The cause of an emission may
include the delivery chart, mounted context, every residual passage, nonlinear reactions,
positional relations, normalization/rebase, routing, cached trajectory, decoding apparatus, and
world returns. A weight table is one face of that causal complex.

## 7. Cross-codec mathematics: the operation is the invariant candidate

Let `E_K` expose one caused construction through codec `K`. Two codec presentations `K` and `K'`
carry one receiver-relative operation where a rebase `S_(K->K')` makes their consequence diagram
commute:

```text
q_R o E_K = q_R o E_K' o S_(K->K').
```

This is not source equality. It says the declared receiver family cannot distinguish the
transported consequences after the rebase. The complete finite behavioral form is:

```text
|u> ~_R |v>
  iff
for every receiver <rho| in R and every admitted successor history gamma,
<rho| T_gamma |u> = <rho| T_gamma |v>.
```

`proved-standard`: this is finite Nerode equivalence when the admitted transports form a finite
deterministic system. A collapsed pair is honest only when the system also returns the shortest
`(rho, gamma)` which separates it under a richer receiver.

The example `2 + 2`, `add(2, 2)`, and their corresponding expression diagrams illustrates three
different equalities:

```text
presentation equality       false
denoted-value equality      true
operation-transport equality conditional on the declared intervention family
```

The value `4` alone cannot identify addition: `2 * 2` also returns `4`. First and higher variations
separate the local laws:

```text
f(a,b) = a + b        Df = [1, 1]       D^2 f = 0
g(a,b) = a b          Dg = [b, a]       D^2 g has mixed cross term
```

At `(2,2)`, `Df=[1,1]` while `Dg=[2,2]`. The shared output collapses them; the response to a nearby
intervention separates them. Associativity, commutativity, identity, annihilator, distributivity,
port order, type/carrier change, nesting, and resource/current consequences provide further
receiver families. The machine should found the reaction species from this conduct, not from the
strings `+`, `add`, `Rust`, `Lean`, or `arithmetic`.

The same rule scales to algorithms. Two programs instantiate one algorithmic phase where their
state-transition complexes commute under a declared rebase and receiver family. Equal final output
on one input is not enough. A simulation trace, invariant, obstruction, and separating intervention
are the causal geometry which can establish or refute the relation.

## 8. Self-similarity, eigenmodes, and section modulus

Self-similarity is not surface resemblance. `definition`: a restriction followed by a lawful
rebase preserves a receiver consequence class while retaining the scale, phase, and lineage of the
passage:

```text
Rebase(Restrict_U Lambda) ~=_R Lambda.
```

Once the material founds a transport operator `T`, right modes propagate constructions and left
modes are receiver covectors:

```text
T |v_j> = lambda_j |v_j>,
<w_j| T = lambda_j <w_j|,
<w_j|x> = the receiver's mode amplitude.
```

`proved-standard`: if `T' = S T S^-1`, eigenvalues stand and eigenspaces transport by `S`; if
`T' = a S T S^-1 + b I`, then `lambda'_j = a lambda_j + b`. Gap ratios and cross-ratios can compare
the affine spectral chart while the lived `a`, `b`, and `S` remain lineage. Spectrum alone does not
identify the construction.

The modulus-of-transport form makes cross-codec comparison quantitative without inventing a token
distance. For a source receiver `rho`, consequence receiver `sigma`, and transport `f`, a modulus
`omega_a` carries the demanded consequence grain back to the necessary source grain:

```text
omega_a : G_sigma -> G_rho,

d_rho(q_rho(x), q_rho(a)) < omega_a(epsilon)
   implies
d_sigma(q_sigma(fx), q_sigma(fa)) < epsilon.
```

`proved-standard`: this is the ordinary modulus of continuity with its two receivers made explicit.
The map is contravariant: what the far receiver demands determines what the source must supply.

`interpretation`: section modulus supplies the complementary load-bearing reading. In bending,

```text
Z = I/c,              sigma = M/Z.
```

Equal material area does not imply equal response; where the material lies relative to the neutral
axis determines what load the section carries. In an information construction, equal token count,
parameter count, or expression length likewise says little. The load-bearing object is where the
coupling and crossings sit relative to the operation's invariant axis. The addition/multiplication
derivatives above are the elementary instance: the same value and same visible arity carry different
response moduli because the cross term is placed differently.

Fourier or Mellin analysis is lawful only after this causal operator and scale action exist. A DFT
over arbitrary token ordinals measures the chart. Fourier over an incidence transport, and Mellin
over its log-scale action, measure the caused geometry.

## 9. Conditioning material without authored material taxa

The declared corpus remains the right pretraining material: purified human and assistant passages,
the repository, and the Lean libraries. It contains prose, equations, Typst, LaTeX, Rust, Lean,
Python, shell, serialized formats, and many mixed inscriptions. Those names are exterior codecs and
lineage. They may be retained as apparatus testimony and controls; they may not become the machine's
internal taxonomy or decide which constitutive law applies.

The carrier-neutral conditioning event is:

```text
co-present antecedent sections
+ their caused incidence and chronology
+ a declared receiver/world port
-> actual returned consequence complex.
```

The most valuable passages are those where the world supplies hard consequences:

- evaluation of an exact expression;
- a compiler or interpreter state transition;
- a Lean kernel admission or typed obstruction;
- an algorithm trace and its invariant;
- a proof dependency and the theorem it transports;
- a test whose intervention changes one caused relation;
- two codecs presenting one operation under a shared family of consequences.

English narration remains useful because it collocates antecedents, landmarks, equations, code,
and consequences. It is not the target invariant and its grammatical labels are not the internal
types. Noun, verb, operator, delimiter, function call, expression node, and Feynman vertex are
candidate receiver faces of more primitive causal roles.

## 10. The minimal recurrent-section machine

`definition`: the minimal “conversational” machine is a **recurrent-section machine**. Conversation
is only one exterior codec for repeatedly supplying and receiving sections. Its smallest complete
cycle has seven stations:

```text
1  mount       receive one situated occurrence with delivery lineage
2  expose      retain overlapping grains and candidate incidence; do not select a tokenization
3  couple      join it to standing sections through actual contact
4  conduct     propagate exact currents through founded operation/transition vertices
5  radiate     return a closed section, a plural fiber, or a typed obstruction
6  world       enact any deed at a declared exterior port
7  reflect     reread the genuinely later return and deposit attributable morphological change
```

Suspension and source-detached remount are part of station 7, not an eighth intelligence faculty.
The continuing body owns the morphology; branches own only local differences; the world owns its
returns; the exterior codec owns rendering.

The central internal object is not a word sequence. It is a causal section:

```text
S = (occurrences, typed contacts, port order, chronology,
     transports, receiver faces, open alternatives, lineage).
```

Reasoning is current through a population of such sections. “Thinking tokens” may be one emitted
chart of that current, but the research object is the sequence of exact intermediate transports,
state changes, closures, and obstructions. Simulating an algorithm means enacting its transition
complex inside the ecology and retaining the trace; describing the simulation in English is a
later codec projection.

## 11. Existing partials and the missing join

The live and historical bodies already hold most stations separately:

- `ExactTextMaterialAtlas` owns the source-neutral sealed corpus, chronology, recurrence, local
  sections, native rest, and a resident restriction mouth;
- `receiver_exact_compression` owns stable finite behavioral quotients and shortest separating
  histories;
- `ReconstructionFiber` reads one quotient backward and retains alternate presentations;
- `holonic_training::TrainingEcology` owns the medium-neutral boundary relation
  `co-present antecedents + receiver -> returned consequence`;
- `MorphologicalLanguageEcology` owns multiscale lexical standing and deposited re-emission;
- `conditioned_rest`, `returned_reading`, and the form mouth own a world-mediated return and exact
  source-detached remount;
- `material_incidence` exposes dependency, operand, order, route, interference, and denoted-value
  faces for several declared source charts;
- the Lean, exact arithmetic, circuit, and algorithm owners supply hard world consequences;
- the earlier ByteDyad/Athena/Hephaestus records established the importance of causal collocation,
  free-running rather than teacher-forced consequence, algorithmic decomposition, and an acted deed
  returning to the same continuing body.

The missing join is precise:

1. `material_incidence::ContactSpecies` is discarded before `incidence_production::Bond`, so the
   current common complex cannot distinguish `2+2` from `2*2` except through a surface name;
2. the source atlases are chosen by exterior material kind, so they are excellent controls but not
   a carrier-neutral inference of operation species;
3. `token_invariance` and the first `ReconstructionFiber` conduct on one-dimensional left/right
   neighborhoods rather than arbitrary causal sections;
4. `MorphologicalLanguageEcology` is a strong recurrence and re-emission owner, but its active
   section is lexical/clausal;
5. `AgenticLanguageEcology` and `AgenticResearchSession` close useful I/O loops, but their outward
   answer projection and English obligation chart are application faces, not the invariant core.

The smallest new ownership, if construction is later authorized, is therefore not a Transformer,
Reasoner, Planner, Conversation, Expert, or Language subsystem. It is one carrier-neutral section
join which preserves contact species across transport and asks the existing exact compression,
reconstruction, consequence, and return owners to classify its conduct.

## 12. Construction contract, not construction schedule

`project-postulate`: a future minimal deed must use one mixed corpus without internal material-kind
routing and must return the following artifact for each compared construction:

```text
presentation lineage
candidate decomposition/reconstruction fiber
typed incidence and port order
exact state-transition or evaluation consequences
stable receiver quotient
shortest separating interventions
reflection/morphology delta after a genuinely later return
exact work vector and open exterior.
```

The decisive positive fixture is not the literal string pair `2+2`/`add(2,2)`. It is a family in
which several surface codecs expose the same operation under varied operands, nesting, and carriers,
and the machine founds one transport class without being told the codec label. The decisive control
is a same-value/different-law family such as addition and multiplication at `(2,2)`, which the value
receiver collapses and the intervention receiver must separate.

Further controls are mandatory:

- rename every surface while preserving incidence;
- vary formatting, punctuation, whitespace, capitalization, and misspelling;
- permute commutative operands and reverse noncommutative ones;
- change numeral, word, call, infix, prefix, and proof presentations;
- hold output fixed while changing the internal operation;
- hold operation fixed while changing output through operands;
- ablate one founded reaction section and require its later conduct to depart by name;
- detach the source corpus, remount the morphology, and reproduce the classification;
- enlarge the receiver family and require the earlier fiber to refine rather than silently mutate.

This record does not authorize that deed or alter `CONSTRUCTION_STATE.md`. The admitted next deed
remains whatever the live roadmap and position record say.

## Evidence and prior deposits

This synthesis composes rather than supersedes the following records:

- `research/records/2026-07-21_THE_MODEL_IS_ONE_LOCAL_TRANSITION_THE_HARNESS_CLOSES_THE_WORLD_CIRCUIT.md`
  — the local transition versus the complete model-world circuit;
- `research/records/2026-07-22_THE_EXPERT_IS_THE_CONTEXTUAL_HEXIS_THE_MIXTURE_IS_THE_EMANATING_FIELD.md`
  — why a hidden-coordinate summand or routed block is not a stable expert species;
- `research/records/2026-07-22_THE_NETWORK_IS_ONE_REALIZATION_THE_RELATION_MAY_BE_RETRIANGULATED.md`
  — network realization, attention as a simplex chart, and rebase/refactor boundaries;
- `research/records/2026-07-22_THE_RESIDUAL_CULTIVATES_THE_CHART_THE_DISTRIBUTION_RETAINS_ITS_CONDITIONAL_GEOMETRY.md`
  — consequence training, residual types, and probability as a receiver quotient;
- `research/records/2026-08-12_THE_RECONSTRUCTION_REMAINS_A_FIBER_THE_CONTEXT_RETURNS_BY_DIFFUSION_AND_THE_CIRCUIT_GROWS_IN_HIGHER_CELLS.md`
  — ReconstructionFiber, deterministic diffusion, contextual spectrum, and true higher cells.

The principal live partials named in §11 are owned in
`soma/life/src/exact_text_material.rs`, `soma/life/src/material_incidence.rs`,
`soma/life/src/morphological_language.rs`, `soma/life/src/reconstruction_fiber.rs`,
`soma/life/src/holonic_training.rs`, `crates/holonic-engine/src/receiver_exact_compression.rs`, and
the exact mathematical, circuit, and returned-reading owners routed from
`blueprint/THE_ROADMAP.md`. Their existence is established in their bounded scopes; their proposed
carrier-neutral join remains `open`.

## What this does not establish

No new model, parser, reasoning engine, or cross-codec classifier is implemented here. The
bra-ket reading does not identify a contemporary neural network with quantum mechanics. A shared
denoted value does not establish a shared operation, and a shared spectrum does not identify a
causal complex. The corpus is sufficient material, not proof that the current mouths expose every
relation it contains. “Conversation” is not made an invariant, English grammar is not made the
machine's type system, and no source-language category is promoted into internal ontology.

The proposed recurrent-section machine does not claim intended meaning, infallibility, universal
algorithm discovery, or protein/molecular prediction. It specifies the smallest causal object a
later construction must return before any of those comparisons can be investigated.

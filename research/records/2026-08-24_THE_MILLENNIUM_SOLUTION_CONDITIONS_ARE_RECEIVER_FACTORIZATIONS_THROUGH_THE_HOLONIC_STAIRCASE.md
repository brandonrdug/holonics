# The Millennium solution conditions are receiver factorizations through the holonic staircase

**Date:** 2026-08-24  
**Purpose:** exterior Lean theorem strategy, derivation-atlas return, and local-to-global audit  
**Authority boundary:** this record deposits mathematical analysis.  It does not schedule the
Rust/CUDA body or advance root `CONSTRUCTION_STATE.md`.

## Return

[definition] The maintained Millennium catalog now distinguishes four objects for every
worktrack:

1. the official target receiver;
2. the existing holonic staircase that feeds it;
3. a typed sufficient family of remaining theorem returns; and
4. an explicit receiver-insufficiency witness which can falsify that route.

[definition] The shared staircase is

```text
occurrence
  -> oriented swing/difference
  -> addressed transport word
  -> complete product and remainder ledger
  -> annihilator/boundary quotient
  -> receiver plus reconstruction fibre
  -> rebase, word comparison, and holonomy
  -> scale descent and recurrence
  -> compactness/reconstruction/gluing
  -> official problem face.
```

[interpretation] This makes holonics itself the catalog.  The named problems are distinct
specializations of the same local-to-global discipline; they are not identified with one another.
The reusable content is the exact composition and factorization law, while each specialization
still owes its constitutive carrier and terminal receiver.

## Exact receiver criterion

[definition] Let `entering : X -> E` be the current local receiver and `returned : X -> R` the
later or official receiver on the same source population.  The local face is sufficient precisely
when `returned` is constant on every fibre of `entering`:

```text
entering x = entering y  ->  returned x = returned y.
```

Then and only then does a function on the actually presented range of `entering` reconstruct the
returned face.  A pair with the same entering face and different returned faces is the shortest
separator and the exact instruction for enriching the atlas.

[interpretation] The parallel Receiver work made this criterion explicit through
`ReceiverTransformer`, `receiverReconstructionFibre`, and `ReceiverInsufficiency`.  This record
uses the mathematics but does not claim ownership of that parallel edit or stage it in this deed.

## Candidate closures

[conditional] The candidate RH closure is: complete weighted argument principle; exact prime plus
archimedean explicit formula; vanishing cofinal boundary return; identification with the classical
Weil form; positivity on the full admissible test family; and the positivity-to-RH equivalence.
Its local falsifier is a negative admitted test or an unseparated off-seam zero population.

[conditional] The candidate BSD closure is: actual analytic continuation for every addressed
curve; receiver-exact local-place, Selmer, height, regulator, Tamagawa, torsion, and `Sha` transport;
rank equality; and the complete leading-coefficient ledger.  The congruent-number family has the
smaller current gate `waldspurgerTunnellDefect = 0`, but that family return does not discharge the
universal quantifier.

[conditional] The candidate Hodge closure is: actual official variety/cohomology/cycle-class
carriers; compatible local sections of the cycle-class map on rational `(p,p)` classes; preservation
under primitive/Lefschetz transport and gluing; hence zero quotient by the algebraic span.  A
nonzero official quotient class is the exact open population and falsifier.

[conditional] The candidate periodic Navier--Stokes closure is: one uniform large-scale dyadic
Hodge subset-mass return; a theorem from the graded subset receiver to integrability of the actual
critical-vorticity rate; terminal restart from the uniform `H^3` face; compatible extension; and a
maximal-time globalization to `StatementB`.  The existing checked composition still exposes the
first three as separate ports.

[conditional] The candidate Yang--Mills closure is: compact-simple gauge realization; a
reflection-positive lattice/current family; tight continuum scaling; OS/Wightman-strength
reconstruction; nontriviality; and one spectral separator uniform in lattice spacing and volume
which descends to the continuum Hamiltonian.  The existing shrinking-spectrum counterexample
refutes the weaker inference from one finite gap at every scale.

[proved-standard] Poincare supplies the solved regression: actual Ricci flow, monotonicity and
noncollapsing, singularity analysis, surgery with topology lineage, continuation, finite extinction,
and the `S^3` conclusion.  The local Lean tree owns the proposition, not a kernel-checked Perelman
proof.

[conditional] P versus NP has two terminal routes: a uniform polynomial-cost transformation from
every verifier/certificate face to a decider, or one NP language with an encoding-invariant
complete-history separator violated by every polynomial-time deterministic decider.  Output bits
alone are an insufficient receiver for the separating route.

## Recurrence is differentiated only after a difference receiver is declared

[definition] A recurrence `R_n` is a sequence of returned faces.  Its discrete derivative is an
additional operation `Delta R_n = R_(n+1)-R_n`; repeated differences give `Delta^k`.  For a
transport operator `T`, recurrence is carried by `T^n`, while finite differentiation is carried by
`(T-I)^k`.  A plain category supplies composition but not subtraction; the derivative reading
requires an additive enrichment or another declared difference object.

[established-bounded; implemented-exact] The derivation-atlas analyzer was extended with:

- exact operation-head transitions through argument positions;
- per-use removal of Lean's curried partial-application nodes;
- same-operation structural nesting order; and
- successive finite-difference rows of the exact-order occurrence profile.

[established-bounded; measured] The focused
`HigherDifferenceTransport.lean` bundle contained 69,058 elaborated expression nodes, 174
declarations, 75 theorem bodies, 1,392 proof events, 136,228 expression-use edges, and 20,078,103
multiplicity-weighted uses.  The new receiver found `fwdDiff` nested through order six and
`HSub.hSub` nested through order six.

[definition] These are proof-term morphology receivers, not theorem-strength measurements.  A
transition-matrix power counts composable expression routes after the chosen quotient; a later
typed receiver must prove that such a route is a recurrence, derivative, or mathematical invariant
of the source object.

## Matching-logic supplement

[historical] Xiaohong Chen and Grigore Rosu's supplied preprint,
[*Completeness and incompleteness of basic matching logic*](https://arxiv.org/abs/2608.13306),
proves global completeness for one-sorted fixpoint-free basic matching logic by localizing every
hypothesis along every coordinate word, identifying the largest backward-closed core, and replacing
the unreachable exterior by a two-sheet cover.  It also proves failures caused by many-sorted sort
flow and by least-fixpoint effectivity.

[interpretation] The transferable lesson is exact and limited: a local-to-global staircase owes a
composable typed word from hypotheses to conclusion, a localization closed under every admitted
word, and a reconstruction that preserves the target receiver.  More tower depth or recursive
syntax cannot substitute for those returns.

## Disposition

[definition] The full per-problem conditions, current gates, and falsifiers are maintained in
`soma/formal/elementary-holonics/MILLENNIUM_FORMAL_CATALOG.md`.  Once this catalog phase is
committed, the next mathematical deed returns to the Navier--Stokes coefficient/critical-
continuation frontier without using the catalog itself as a claim of solution.

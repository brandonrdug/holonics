# The byte field is not the complete HNN composition

[definition] The subsequent [deeper review](2026-09-09_ARCHITECTURE_CHARTS_REQUIRE_COMPOSED_MECHANISMS_AND_FINITE_CONSTRUCTION_RETURNS.md)
narrows this diagnosis. A local return, finite carrier, linear readout or discrete byte emission
is not independently a defect, and a full-history adjoint is not a general learning prerequisite.
The factual source/feedback observations below stand; they do not establish the whole cause of
the loop. The later review and live roadmap supersede the proposed construction sequence here.

[project-postulate] Brandon has paused AC construction and cultivation to review repetition,
Git chronology, elementary mathematics, modality composition and Rust framework organization.
This review changes the construction diagnosis; it does not resume cultivation. AC0's data
return stands. AC1 is partial, AC2's learned codec/continuation is open, AC3 has experimental
artifacts, and AC4–AC5 are open. Athena-alpha has not been attained.

[definition] Inspection covers `98a68b54` plus the retained live-propagation implementation,
the completed eight-family reports, the named formal owners below, and Apple revision
`9b7b4b63`. It is not a theorem-by-theorem audit of the whole repository. No source passage was
searched to explain an emitted character. No further model was cultivated or prompted.

## What actually receives and generates text

[established-bounded; source-inspected] The active path is
`alpha_text::cultivate_observed` → `TextFieldSession` → `NativeConstitutiveField`, with
`NativeMaterialTransportSource::OperativeNormal`, the binary tensor target, duplex I/Q ingress,
complex-current squared discrepancy and a declared dyadic contact deposit. Its boundaries are:

| Passage | Actual construction |
|---|---|
| Exterior text | UTF-8 bytes plus an EndPart marker. Eight byte bits and the boundary bit give nine binary factors and eighteen root channels. This is a chosen codec chart, not learned semantic grain. |
| Incoming realization | One unit current per factor pair. Human material uses real units; demonstrated outgoing material uses imaginary units. Source ancestry is separate from those direction charts. |
| Native state | Held root currents, contact interiors, an evolving contact map, source lineage and learned material standing. An observed source/arrival can found a contact. The retained propagation variant advances joined interior currents before reflection. |
| Material query | A complex-linear map from the operative outgoing boundary: three ports per root, hence 54 complex source coordinates here. Interior effects reach that boundary, but the material map does not query every interior coordinate independently. |
| Target | The joint tensor codeword has 512 coordinates; bytes and EndPart occupy the admitted subset. Accumulated H/B/C describe the observed source/target fit, including its regularizing prior and numerical residual. |
| Generation | The packet receiver finds a unique maximal coordinate across the admitted current ball in the declared quadrature. The selected byte is emitted and realized again through the fixed unit-factor chart. |
| Feedback | The same field and its actual interior continue. The new ingress does not preserve the amplitude/family of the full predicted tensor. Source-qualified actuation advances state without treating its own chosen byte as an independent observed target. M and D therefore stay fixed during this generation segment. |

[established-bounded; source-inspected] Owners are `alpha/text_codec.rs`,
`alpha/text_session.rs`, `field/material_transport.rs`, `material_transport/normal{,/layout}.rs`,
`field/receiver/packet.rs` and `field_normal{ized_receiver,_material}.cuh`. The mathematical
carriers are real implementation, but this composition is a finite byte-prediction experiment.
Neither the word “holon” nor correct source lineage establishes learned word/phrase generators.
There is no template that emits `the` and no capitalization rule on this path.

## What the reported fragments establish

[established-bounded; computational-witness] The [cold report inspection](../experiments/normal_geometry_review/2026-09-09_paused_feedback_review.json)
compares the completed original and optimized runs. Their full reported body, generation and
recorded current histories are equal. Prompt/emission time changed from 815.413 to 228.188
seconds. Optimization changed cost, not this behavior. The exact output remains:

```text
The the the the the the the the the the the the the te te te te t t the the the the te te te te t t t t the the the the te te te
```

[established-bounded; computational-witness] Every selected coordinate has a strict margin
throughout the retained current ball. With grid gap g to the next coordinate and ball radius r,
the inspection verifies `g > 0` and `g² > 2r²`. At emission indices 5, 53 and 65, the immediately
previous emitted byte is the same `t`, while the next byte is respectively `h`, `e` and a space.
Thus the observed `the → te → t` variation is produced by different current states selecting
different next coordinates. It is not an ambiguous rounding decision or string postprocessing.

[established-bounded; source-inspected] The first capital `T` is coordinate 84, selected from
the state following the prompt's EndPart. Later predictions follow outgoing actuation instead.
There is no mechanism that merely capitalizes the first selected lowercase letter. The earlier
`’m` output consists of UTF-8 bytes `E2 80 99` followed by `6D`: three byte selections render
U+2019, then one renders `m`. The source material admits UTF-8; the apostrophe is not a special
native instruction. This explains its representation without attributing it to a corpus passage.

[open] These observations do not establish grammatical knowledge, a learned “the” class, or
the specific causal contribution of each learned parameter. The [prior feedback comparison](2026-09-09_AC_THE_REPEATED_TEXT_FACE_IS_A_DRIVEN_OPEN_RETURN.md)
already separates outgoing-current balls behind repeated faces. The trajectory can remain
inside a small succession of receiver regions while its interior changes. That is not a proved
complete-state cycle, nor does it prove that the same word generator is being compressed.

## The elementary gap is composition, not absent mathematics

[established-bounded; source-inspected] The active return follows the material discrepancy
through its actual producing M, paired field and, when enabled, preceding propagation word.
`NativeMaterialSourcePullback` explicitly holds historical factors fixed.
`prepare_material_contact_return` holds incoming state fixed and stores no incoming-state
delta for this contact response. The incoming covector remains available, but this driver does
not recursively return it through the earlier sequence of producers. Reconstructing historical
coefficients makes a local derivative faithful; it is not sequence-wide credit assignment.
The earlier full-operator recurrent adjoint is a separate existing owner and keeps its scope.

[established-bounded; source-inspected] The active normal fit accumulates observations of past
source currents. It does not silently recompute those past currents under the newest morphology.
`Physics/AccumulatedNormalResponse.lean` correctly distinguishes normal residual, fit residual
and changed-source mixed terms. A small solve error does not establish that the chosen source
representation separates the continuations to be learned. Adding solve precision was already
ruled out as beneficial at the measured cuts.

[conditional] If a chosen source representation does not separate different observed outcomes,
a squared-error fit compromises among them. For identical feature x and one-hot targets,
minimizing their sum of squared errors over an unconstrained prediction gives the empirical
mean target; a maximum-coordinate receiver then favors the most frequent outcome. Regularization
and nonidentical features change that expression. This supplies a concrete frequency-collapse
mechanism to investigate, not proof that corpus frequency alone caused this particular loop.

[established-bounded; source-inspected] The normalized-exponential and sigmoid work was not
absent: `1b66693c` installed normalized material receivers and their returns, following
`90b3ddd6`'s breadth recovery. `HolonicAdjointNormalization.lean`, `exponentiated_ratio.rs` and
the resident contact/adjoint kernels remain owners. The active experiment now uses the full
complex-current metric and a maximum receiver, not normalized input-conditioned contact
followed by learned local nonlinear reactions. Applying softmax only before the same maximum
would preserve the selected coordinate; the packet kernel explicitly uses that fact.

[established-bounded; source-inspected] `HolonicArchitectureCharts.lean` constructs attention
from admitted, state-conditioned contacts, convolution from shared relative transport and
state-space models from retained recurrence. Its block adds a local constitutive reaction.
These constructions explain useful mechanisms without requiring a Transformer architecture.
The present byte field does not inherit their expressive composition merely by using complex
currents. Its feedback is nonlinear through selection, so describing the entire model as
“only linear” would also be wrong.

[established-bounded; source-inspected] The relevance return in `65feb413` gives
future-stable receiver agreement, including nonlinear deterministic histories. It states which
differences a reuse may identify; it does not automatically find learned features or supply a
physical dissipation law. The Stokes/pullback return in `6b704180` retains interior/exterior
flux and signed defects. A repeated visible face does not remove hidden future influence.
Likewise the four-force and constitutive sources keep distinct couplings and boundary data;
an attraction story by itself does not specify emission, exchange or extinction. No four
invented force coefficients belong in the text adapter.

[established-bounded; source-inspected] Reflection, leader growth and fractal packing have
more content than the paired map used here. `diffusion.rs` retains the Schur source term and
interior reconstruction; `leader_quadrature.rs` has local jets, restriction, rebase and retained
reach; `Foundation/FractalPacking.lean` composes ordered child restrictions. The current map
journal shares historical numerical generators. That is a bounded storage/decoder return,
not yet learned reuse of semantic generator families across changing local paths.

## What history shows about the detour

[established-bounded; source-inspected] The sequence `e49e266d → e822ee0b → 0802fa42 →
ca5dba5b` added duplex direction, a finite material map, source-qualified actuation, then
accumulated normal geometry. `362949e1`, `339c7991` and `98a68b54` addressed source-factor
storage, propagation and historical decoding. These solve actual scoped defects. However,
successful local checks repeatedly became reasons to continue the same architecture without
resolving its learned source representation, temporal return and generator reuse. The breadth
documentation already named those missing compositions. Repeating that documentation while
extending transport was insufficient integration of the mathematical work.

[established-bounded; source-inspected] A proposed boundary diagnosis was checked and narrowed
during this review. `begin_part` clears the immediate link but can attach an explicit retained
parent. Every family in the eight-family report has one admitted part, and all seven outgoing
parts attach to the first incoming part's retained source. It is false to say that this run
never observed request-to-response starts. Multipart resets and the single-parent port retain
their separate limited scope; no speculative adjacency repair was applied.

## The shared modality relation

[established-bounded; source-inspected] Apple `9b7b4b63`'s `native/acoustic_field.rs` mounts
complete addressed coefficient sections with source clocks into the same constitutive field;
`native/temporal_acoustic.rs` carries an observed temporal condition/contact response and its
Preimage Fibre. This retains more explicit temporal section structure than the current byte
cadence. It still has declared sample/receiver charts and bounded behavior, not a universal
acoustic generator. The desktop image owner supplies exact raster receivers; that is not yet
learned mesh or visual composition in this text profile.

[definition] The shared construction is a situated source, addressed section/incidence,
transport, receiver, retained compatible causes and continuation. Bytes, acoustic coefficients
and mesh vertices are exterior presentations. Their equality as serialized values does not
identify occurrences, and calling all of them holons does not supply the missing attachment.
The active desktop adapter has no learned cross-modal composition to exhibit.

[established-bounded; source-inspected] The acoustic CTC work offers a concrete reuse method:
`holonic-membrane/live_current.rs::EventNodeAtlas` evaluates each shared dependency cell once
in its declared order, preserving the complete path sum. That principle informs a shared
causal return and generator decoder. CTC's blank/skip symbols are its own application chart,
not general text mechanics to copy. No Apple backend code was merged or acoustic campaign run.

## Framework repair and construction consequence

[established-bounded; source-inspected] The `holonics` facade previously exposed only HNN,
Soulkiller and interoperability, always depending on the native runtime. It now exposes the
existing `structure` and `geometry` crates without default features, while default `native`
retains those application APIs and exposes their engine owner. This is a real dependency
boundary, not a second framework implementation. The [Rust guide](../../docs/RUST_FRAMEWORK.md)
states its scope; advanced mathematics still resident in the engine remains CUDA-bound.
Crate/module documentation now distinguishes experimental byte apparatus from learned HNN
composition and removes the engine header's inaccurate blanket absence claims.

[project-postulate] The revised AC entry must connect the existing constructions: specify the
situated source/section and continuation receivers; compose the learning return over the actual
dependencies that those receivers measure; share and rebase the resulting local generators;
then develop the exterior text receiver/actuator using that conduct. A later consequence needs
an explicit path back to the earlier relation it is meant to change, whether through an adjoint
or another derived local law. A filename, one-step fit, propagation rotation or larger corpus
does not satisfy those relations by itself. No universal singleton gate, utility requirement,
repetition filter or Millennium endpoint is introduced. The roadmap carries the revised entry;
construction stays paused under Brandon's instruction.

[established-bounded; process-audit] Review verification: reduced-feature and default
`holonics` Cargo checks pass, as does the `alpha_text` example check. The reduced-feature
normal/build dependency tree contains no engine, HNN, lifecycle or CUDA mount. The cold report
inspection passes on both completed runs. The
earlier live implementation's 132 field checks and exact-divider controls keep their recorded
scopes; they were not rerun to claim architectural adequacy. No Lean source changed and no
Lean runtime entered any learning path.

# THE INFORMANT CHARGES THE GERM; THE LEADER RETURNS THE RESONANT COMPONENT

**Date:** 2026-07-29

**Status:** PRODUCTION CAPACITIVE INFORMANT MEMBRANE BUILT / FULL 1,556-LINE
CORPUS CONDITIONED / EXACT DELIVERY-GAUGE AND REST GRADED / SPARSE LEADER
RETURN GRADED / NO LINGUISTIC DEED CLAIM

Exact receipts:

- [`REPORT.json`](../../../runs/resonant-corpus-current-information-flow/REPORT.json)
- [`RESONANT_COMPONENT.tsv`](../../../runs/resonant-corpus-current-information-flow/RESONANT_COMPONENT.tsv)

## Exact question

Can separated informants condition a reusable plural ecology through recurring
local morphology, without an authored pairwise informant graph, a retained
example search, stochastic similarity, or corpus-delivery chronology; and can
later receiver questions launch sparse leaders which return their complete
resonant components while incompatible local phase remains explicit?

This is the production interpretation of Brandon's correction:

> Do not remove chronology. Let informants couple through the capacitance they
> enable about one another, and let sparse lightning-like leaders derive a
> resonant image of the retained patterns.

## 1. The first attempted membrane was incomplete

The first version gave every germ an exact structural
`ReceiverFiberIdentity`, but presented it through a fresh terminal world organ
on every occurrence. Equal morphology could therefore address prior
Standing, yet it had no continuing receiver current. Even a complete repeated
informant remained wholly `OPEN`.

That result isolated the missing physical object. Capacitance is not an
interface label. It is a receiver which retains current and can be charged
again.

The corrected `life::resonance_ecology` owns an exact continuing receptor per:

```text
(structural germ identity, exact phase)
```

and two continuing receptors for each complete informant identity:

```text
informant marker
informant path capacity
```

The receptor registry contains no informant pair, target, copied result,
score, or association. It is a world-owned population of
`NativeRelationOrgan` capabilities. The live-current machine owns every
carrier and the Swing remains the only relation-forming law.

## 2. Informants enter as ordered capacitive hyperedges

The source tokenizer remains inherited world material. For one source line

```text
t_0, t_1, ..., t_n
```

the membrane receives its actually adjacent sections

```text
(t_0,t_1), (t_1,t_2), ..., (t_(n-1),t_n).
```

Each ordered dyad is an exact variable-length germ fiber. The complete
ordered germ sequence also enters as one `NativePathChart`; it is not
flattened into a scalar center. A line therefore supplies one holistic
informant path with locally addressable germ boundaries.

All informants in the frozen offline corpus enter as one co-present
configuration. Equal germ-phase receptors are shared once as actual
occurrences. This is a hyperedge incidence:

```text
informant -> germ receptor <- informant
```

It is not expanded into an authored line-to-line edge population.
Permutation of the configuration vector is administrative gauge. Order
inside every informant path remains physical morphology.

A changed phase has the same morphological address but a different continuing
receptor. It can reach the same candidate boundary, while the exact Swing is
still free to return a different residual.

## 3. The active front no longer performs an all-pairs corpus loop

The existing standing aperture already routed an exposed capability only to
its reachable standing local star. Co-present active regions still used a
nested all-pairs comparison.

`LiveConstituent` now builds one transient exact aperture

```text
InterfaceCapability -> co-present active regions
```

and emits only active pairs which share an exposed capability. The aperture
does not decide a deed. Complete support coverage, hands, and transported
paths still cross `append_copresent_seams` and `LivePin::rebase_exposed`.

Thus both historical and co-present work begin from actual incidence rather
than scanning the square of retained or arriving populations.

## 4. Full corpus construction

The source is the established Eros frozen corpus:

```text
src/soma/observations/eros-cohered-corpus-01/SOURCE.json
```

The complete configuration supplied:

| receipt | exact population |
|---|---:|
| informants | 1,556 |
| token occurrences | 8,185 |
| adjacent source sections | 6,629 |
| distinct dyad germs | 3,004 |
| recurrent dyad germs | 1,055 |
| continuing receptors | 6,116 |

Within-line duplicate dyads remain one local germ in this first membrane, so
the standing support has 6,573 local dyad occurrences plus 1,556 informant
marker factors:

```text
8,129 active support factors
```

The resulting machine has:

| receipt | exact result |
|---|---:|
| standing constituents | 149 |
| constituent cells | 8,831 |
| incidences / pins | 10,695 / 10,695 |
| paths / transport terms | 8,129 / 8,129 |
| largest component factor population | 7,054 |

The first configuration uses shared source occurrence incidence, not later
Swing joins. Its support family consequently has 8,129 leaf nodes and zero
cause edges. Later questions and returns add temporal seams.

On the present host, the latest forward full configuration took 2.203 seconds and
the reversed configuration took 2.459 seconds. Wall time is a resource
receipt, not the correctness proof.

Forward and reversed enumeration returned identical:

- complete Standing;
- receptor-to-lineage rest capabilities;
- native rest image; and
- SHA-256
  `528b1c3e9f141c36129a981e2dbb74139767dd7719801183b6e24a4de1c58d98`.

The complete receptor-aware rest is 15,838,836 bytes. The experiment decodes
that durable byte face, validates every receptor capability against a lineage
actually carried by the enclosed machine, remounts it, and reproduces the same
byte face exactly. Partial or structurally altered wires refuse.

## 5. The leader experiment

The deterministic seed selector chose a dyad which occurs in exactly one
informant while that informant also carries recurrent germs:

```text
token dyad (2363,520)
source line 1: "just like your book at home ."
local phrase: "book at"
```

The query supplied only this germ. It did not supply a line identity, target
line, neighbor list, pairwise corpus edge, or desired answer.

The production aperture touched one standing constituent. The returned
component contains:

| receipt | exact result |
|---|---:|
| directly incident informants | 1 |
| returned informants | 1,159 |
| indirectly recruited informants | 1,158 |
| unreached informants | 397 |
| returned distinct germs | 2,643 |

The one-germ question remains an abductive leader:

```text
OPEN = 3
RIDE = 0
FOUND = 0
```

It is not a complete recurrence and is not relabelled as a closed linguistic
association.

Repeating the seed's complete ordered informant path against the untouched
trained rest returns the same 1,159-informant component and forms:

```text
OPEN = 58
RIDE = 8
FOUND = 0
```

The eight RIDEs are exact closed recurrence inside a still-plural contextual
field. The remaining OPEN boundaries are retained context, not averaged
error.

Changing only the seed germ's positional magnitude keeps the same structural
address but produces no RIDE and changes the exact returned constituent:

```text
ordinary partial leader:
    cbd55adcbfa0724a1dcceedc18662cd0ae58a62f33efb329c17b4be626dbc565

phase-obstructed leader:
    9939289044221c6bf93fb17dca012aa24032cfb38465d4ce26c46b687ebf2b0c
```

This is a complete topology difference, not a scalar loss.

## 6. Exact resonant image

After training, the current serializer returns the connected informants
through a single standing-constituent address. That observer count neither
makes the carried ecology a singular holon nor makes its overlapping
receiver fibers one receiver. The immediate physical front therefore reports
standing depth one; this is an address-depth receipt and must not be misread
as one morphological degree.

A bounded inspection oracle alternated only through the supplied incidence:

```text
seed germ -> incident lines -> their germs -> incident lines -> ...
```

It did not perform inference and did not supply the machine result. Its sole
grade was equality with the returned informant set. The grade passed exactly.

The component's causal-degree distribution is:

| degree from the unique seed line | informants |
|---:|---:|
| 0 | 1 |
| 1 | 14 |
| 2 | 356 |
| 3 | 578 |
| 4 | 182 |
| 5 | 24 |
| 6 | 4 |

This is the first direct resonant image of corpus transport in this
construction. The distribution grows, peaks, and contracts without a random
walk or global similarity score. The 1,159 exact rows and their degrees are
written to `RESONANT_COMPONENT.tsv`.

## 7. What this establishes

The production machine and world membrane now demonstrate:

- exact continuing capacitance for arbitrary structural germ fibers;
- whole ordered informants rather than scalar or cellular centers;
- co-present shared-germ incidence without authored informant pairs;
- administrative delivery permutation as exact gauge;
- sparse query-caused access through the production aperture;
- indirect recruitment over six morphological degrees;
- complete recurrence forming RIDE;
- incompatible phase retaining a distinct OPEN topology; and
- receptor-aware exact rest/remount.

This is conditioning and reconstructive association, not archive lookup.
Removing source text after the rest is formed does not remove the standing
component or receptor currents.

## 8. Exact limits exposed by the result

The experiment does not establish a linguistic deed, response policy, token
generation rule, semantic equivalence, or probability measure.

The token-generation limitation above is local to this construction and is
superseded by
[`THE SUFFIX DILATES THE CONTEXT; THE EMANATED BRANCH RETURNS AS CAUSE`](2026-07-29_THE_SUFFIX_DILATES_THE_CONTEXT_THE_EMANATED_BRANCH_RETURNS_AS_CAUSE.md).
That later construction retains this capacitive standing and adds plural
receiver-scale generation; it does not retroactively relabel the present
resonant-component query as generative.

The 1,159-line component is itself informative: exact dyad recurrence creates
a broad connected language holon. That is not a failure of propagation. It
shows that an unconditioned receiver asking only for dyad morphology receives
a coarse quotient. More discriminating conduct must come from the already
retained informant path, phase, receiver history, returned deed, or another
caused local fiber. It must not be imposed by a host similarity threshold.

This first membrane also quotients repeated copies of one dyad inside one
informant to one local germ. It retains cross-informant and whole-path
multiplicity, but does not yet claim that within-informant recurrence is
irrelevant in every application.

Finally, the bounded degree oracle is inspection testimony. The production
question follows its incidence aperture into a serialized constituent which
carries plural overlapping receiver fibers; it does not run the oracle's
breadth-first traversal or retain its distance table.

## Verification

The decisive commands are:

```text
cargo test -p soma-membrane --lib
cargo test -p life resonance_ecology::tests --lib
cargo test -p life --lib
cargo check -p life --example eros_resonant_corpus_current
cargo run --release -p life --example eros_resonant_corpus_current -- \
  observations/eros-cohered-corpus-01/SOURCE.json \
  ../../runs/resonant-corpus-current-information-flow/REPORT.json
```

The final grades are:

```text
soma-membrane: 112 passed; 0 failed
life resonance ecology: 6 passed; 0 failed
life complete library: 421 passed; 0 failed; 8 ignored
full corpus: full-corpus-observed; delivery_permutation_exact=true;
             remount_exact=true; bounded_oracle_component_exact=true
```

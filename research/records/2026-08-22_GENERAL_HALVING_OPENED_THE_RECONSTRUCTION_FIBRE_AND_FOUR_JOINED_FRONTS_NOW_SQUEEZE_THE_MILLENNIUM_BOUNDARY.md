# General halving opened the reconstruction fibre and four joined fronts now squeeze the Millennium boundary

**Date:** 2026-08-22  
**Purpose:** Claude-readable dependency map for Brandon's parallel Lean research campaign  
**Authority boundary:** `[definition]` This record schedules a user-directed Lean research track.
It does not alter `blueprint/THE_ROADMAP.md`, displace the Rust/CUDA construction frontier, or
authorize an update to `CONSTRUCTION_STATE.md`.

## Pickup without reorientation

`[project-postulate]` Resume at the first unclosed gate in each numbered front below.  A failed gate
is repaired inside its owner; it does not license a nearby toy, a synthetic replacement for a
geometric object, or a jump across the dependency.  Every exact return records its hypotheses,
receiver, reconstruction fibre, open remainder, focused build receipt, and axiom audit.

`[project-postulate]` The immediate parallel wave is:

1. Claude's arithmetic line: explicit quotient cardinality, complete two-torsion and halving
   fibres, Kummer quotient, then normalization.
2. The fluid line: genuine pressure periods and moving-material-loop circulation, while a bounded
   time solution carrier and the finite Hodge owner are built independently.
3. The shared geometry line: finite Hilbert transport before continuum Hodge, classical
   Yang--Mills, or curved Einstein realizations.
4. The scale line: modular-lambda/Landen on the arithmetic side and Leray/heat/parabolic scaling on
   the fluid side, retaining the nonlinear and analytic obstructions rather than identifying the
   two scale actions.

## What GeneralHalving actually returned

`[proved-derived; formal-checked]` `GeneralHalving.lean` proves on the displayed model

```text
E_ab : y^2 = x (x - a) (x - b),     a b (a-b) != 0
```

that the two square-class slots are trivial exactly when the point is a double.  From
`x = u^2`, `x-a = v^2`, `x-b = w^2`, and `y = uvw`, it constructs

```text
Q = ((u+v)(u+w), (u+v)(u+w)(v+w))
```

and proves `Q + Q = P`.  This is an exact kernel/image theorem and a constructive returned half,
not merely a collision count.

`[counterexample]` The file's prose saying that the tangent slope at `Q` is `u` is false in
general.  For `u=3`, `v=1`, `w=2`, the admitted parameters are `a=8`, `b=5` and `Q=(20,60)`; the
tangent slope is `6 = u+v+w`, not `3`.  The identity `(X^2-ab)/(2Y)=u` is the square root of the
doubled abscissa.  The checked doubling proof uses the correct slope, so the formal theorem is not
damaged; the comments should be corrected at the next arithmetic edit.

`[open]` No current theorem transports an arbitrary Weierstrass curve with full rational
two-torsion, its point group, addition, slots, or quotient through a variable change to the
displayed integral `E_ab` model.  Until that equivalence exists, the literal formal scope is the
displayed two-parameter normal form, even where comments use universal wording.

### Claude pickup: exact live arithmetic boundary at deposit

`[proved-derived; formal-checked]` The committed `GeneralCollision` theorem returns
`Finite (E(Q)/2E(Q))`.  Its advertised explicit `(2*tau)^2` bound is not yet present in the theorem
type.  `GeneralCertificate` now has the certified doubled-abscissa quotient, homogeneous Bezout
certificates, and an exact common-divisor bound by `2^2 (ab)^2 (a-b)^2`.

`[proved-derived; formal-checked]` Claude's commits `2f022f9`, `cbbce9b`, and `8f5f79b` import and
extend `GeneralHeight.lean`.  The committed owner proves all four cofactor estimates, quartic
domination, the reduced-pair height, the general chord quadratic, its integral clearing, and the
fixed-point translation bound `hgt(z) <= 2^4 R^6 hgt(x)^2`.

`[proved-derived; formal-checked]` Claude's successor commit `6face48` closes
`theDuplicationGrowsTheAbscissaHeight`, joining the reduced integer pair to the actual rational
quotient after repairing the positive/negative denominator branches of `hgt_of_coprime`.  The
point-level `pheight(2P)` wrapper and representative/descent assembly remain later successors.

`[project-postulate]` Claude's next five arithmetic gates, with no reorientation, are therefore:

1. the explicit `Nat.card` weak-Mordell--Weil bound;
2. the point-height wrapper joining the checked abscissa duplication and chord returns;
3. the finite representative atlas and explicit global height constants;
4. the strict general descent step `X = 2Q + R` with lower point height;
5. bounded-height finiteness plus well-founded descent to `AddGroup.FG`.

The divisor-count rank bound follows those gates.  Arbitrary-Weierstrass normalization remains a
separate transport obligation before the universal prose becomes literal.

## Front A: arithmetic, BSD, and the modular scale seam

### A1 -- close the finite reconstruction fibre

`[open]` Prove an explicit cardinal theorem for `E(Q)/2E(Q)`, strengthening the present `Finite`
return to the bound read by the collision box.  Then classify

```text
E[2](Q) = {O, (0,0), (a,0), (b,0)}
```

and prove that every nonempty halving fibre is a torsor for this four-point population.  This is
the complete reconstruction return hidden by a chosen half.

### A2 -- replace representatives by the actual quotient

`[open]` Bundle rational square classes as a group and construct the homomorphism and injection

```text
E(Q) / 2E(Q)  ->  (Q^x / Q^x^2)^2.
```

The current proposition-valued square-class face and choice of an integer representative are
useful support instruments but are not this quotient-valued Kummer passage.

### A3 -- make the universal wording literal

`[open]` First transport rational `a,b` to integral roots by square dilation.  Then define a typed
full-rational-two-torsion datum for an arbitrary Weierstrass presentation and construct the
point-group equivalence into an `E_ab` chart.  Prove that addition, doubling, Kummer slots, height,
and quotient cardinality commute with the rebase.

### A4 -- strong Mordell--Weil and the BSD algebraic face

`[open]` Generalize the existing family height mechanism to `E_ab`: bounded-height finiteness,
translation bounds, duplication growth, strong descent, finite generation, and the resulting rank
bound.  The next arithmetic organ after finite generation is the specialized two-Selmer
population and the exact sequence with `Sha[2]`; neither elliptic Selmer nor Tate--Shafarevich is
currently supplied by Mathlib as a turnkey owner.

### A5 -- the exact RH-facing bridge

`[conditional]` Once the missing chart and isogeny maps are constructed, the lawful bridge is

```text
rational two-torsion
  -> Kummer halving
  -> degree-two isogeny
  -> modular lambda scale flow
  -> Landen theta current
  -> completed-zeta Mellin receiver.
```

Use `lambda(t) = (T2(t)/T3(t))^4`, Jacobi's quartic, the exact Landen cell division, and the
Legendre parameter `lambda=b/a`, retaining the possible quadratic twist.  The repository already
proves the last `T3` Mellin leg on `Re(s)>1`.  This chain does not locate the zeta zeros; the
RH-specific successor is a log-scale xi kernel and a proved Weil-type positivity functional on a
widening test family.

## Front N: Navier--Stokes

### N1 -- genuine moving-loop transport

`[open]` Define a two-parameter smooth material loop `Gamma(t,s)` with closed parameter endpoints,
mixed regularity, admitted differentiation under the interval integral, and
`partial_t Gamma = u(Gamma,t)`.  Prove in order:

1. the integral of a pressure gradient along a path is its endpoint difference;
2. a pressure gradient has zero period around a loop;
3. the derivative of the velocity integrand is material acceleration plus the parameter derivative
   of kinetic energy;
4. the kinetic boundary return vanishes on the closed loop;
5. the exact circulation balance

```text
d/dt integral_loop u.flat
  = nu * integral_loop Laplacian(u).flat + integral_loop force.flat;
```

6. Kelvin constancy on the zero-viscosity, zero-period-force fibre.

`[proved-standard]` Positive viscosity does not generally conserve circulation; its viscous period
must remain in the return.

### N2 -- bounded time before singularity language

`[open]` Add `SmoothSolutionOn T` over a finite slab, with restriction, time restart, compatible
gluing, pressure-constant gauge, extension, and maximal lifetime.  The current `SmoothSolution` is
already global on nonnegative time and therefore cannot honestly carry a blow-up alternative.

### N3 -- expose the three-dimensional obstruction

`[open]` Complete curl/time and curl/Laplacian commutation on an admitted solution, obtaining the
actual vorticity PDE.  On a periodic carrier, prove the energy and enstrophy balances separately.
The returned enstrophy equation must retain vortex stretching; pressure cancellation and loop
topology do not remove it.

### N4 -- Hodge--Leray and heat

`[open]` For every nonzero Fourier mode define

```text
P_k(v) = v - <k,v> / |k|^2 * k
```

and prove idempotence, self-adjointness, range `k-perp`, kernel `span{k}`, pressure annihilation,
and nonexpansion.  Retain the zero harmonic/mean mode.  Descend periodic fields to the actual
three-torus, lift the projections to `L2`, and construct the heat semigroup with its smoothing and
Parseval returns.

`[conditional]` The scalar three-torus heat trace factors as `T3(4*pi*nu*t)^3`; the associated
Mellin receiver is a cubic Epstein zeta, while one-dimensional factors reuse the existing
completed-zeta theorem.  This is an exact spectral seam only after the torus/Fourier construction;
it is not a nonlinear regularity estimate.

### N5 -- local existence, continuation, and scale halving

`[open]` Build Fourier Sobolev product estimates, the mild equation, local contraction, uniqueness,
restart, and the finite-time blow-up alternative.  Then formalize parabolic scaling and local
critical receivers `A(r), E(r), C(r), D(r)`, with an exact parent/child ledger retaining boundary
flux, pressure return, dissipation, and open exterior.

`[open]` A cutoff-independent critical contraction without a smallness hypothesis is the genuine
Millennium obstruction.  Heat traces, circulation, Gauss--Bonnet, and holonic scale incidence can
type every retained term, but none currently supplies this missing coercive inequality.

## Front H/Y: the shared Hodge and gauge spine

### HY1 -- one finite Hilbert transport owner

`[open]` Extend the additive transport chain to finite-dimensional real Hilbert spaces:

```text
A --dMinus--> B --dPlus--> C,      dPlus * dMinus = 0
Delta = dMinus * dMinus.adjoint + dPlus.adjoint * dPlus.
```

Prove the Laplacian energy identity, kernel characterization, exact--harmonic--coexact orthogonal
decomposition, and harmonic representatives of homology.  This owner then instantiates finite-mode
Leray projection, face--edge--vertex Gauss--Bonnet complexes, and linearized Coulomb gauge.  A
continuum version must retain closed-range and domain hypotheses rather than silently inheriting
the finite theorem.

### HY2 -- classical Yang--Mills before quantum mass gap

`[open]` Build Lie-algebra-valued coordinate forms, `F_A = dA + 1/2[A wedge A]`, the covariant
derivative, curvature variation, `D_A^2=[F_A,-]`, Bianchi, invariant inner product, the Yang--Mills
first variation, and heat-flow energy dissipation.  The gauge-fixed linearization should return the
shared Hodge Laplacian.

`[open]` The quantum branch then requires discrete group holonomy, plaquette curvature, reflection
positivity, GNS/transfer construction, a cutoff family, uniform bounds, and
Osterwalder--Schrader reconstruction.  A finite lattice gap or classical heat dissipation is not
the continuum compact-simple mass gap.

### HY3 -- Hodge conjecture instances and the arithmetic pivot

`[open]` Begin with an official point or finite reduced zero-dimensional complex variety whose
scheme, topology, cohomology, cycle space, and cycle map are actually connected.  Proceed through
`P1`, curves, and projective spaces only as realization, cup product, fundamental classes, and
cycle maps become available.  A synthetic `HodgeConjecture.Datum` is not a positive-dimensional
geometric instance.

`[conditional]` After that infrastructure exists, the Kummer surface `(E x E)/{+/-1}` is the
strongest arithmetic/Hodge pivot.  Classified two-torsion supplies sixteen fixed points; their
resolution supplies explicit divisor classes and an intersection lattice.  GeneralHalving mainly
resolves fibres inside the homologically trivial divisor population; Abel--Jacobi/Picard data is
the richer receiver that can separate those fibres.

## Poincare, Gauss--Bonnet, gyrotransport, and Einstein dynamics

`[proved-standard]` On the active whole-space fluid carrier, explicit Euclidean contractibility is
enough to remove loop homotopy obstruction.  Poincare topology remains useful on closed
three-manifolds for harmonic-sector questions, but it does not remove local vortex stretching.

`[interpretation]` Gauss--Bonnet can become a material-vortex-surface ledger in which face angle,
bulk curvature, flux, and boundary return are retained together.  This becomes analytic only after
a metric connection and surface transport instantiate the existing finite ledgers.

`[interpretation]` The gyroparallelogram is a lawful discrete receiver for connection holonomy:
compare the actually transported quadrilateral return with its flat or gyro completion.  First
prove exact group-valued path holonomy and gauge covariance; only then interpret a small-loop
defect as curvature or Thomas rotation.

`[open]` The present Einstein/fluid interface assumes its metric, covariant divergence, Einstein
tensor, Bianchi return, and compatibility ports.  A shared connection/curvature owner should
instantiate those fields and the Yang--Mills fields before either branch receives another
conservation cabinet.

## First execution wave returned

`[proved-derived; formal-checked]` `NavierStokesMovingLoop.lean` now proves the pressure-gradient
endpoint theorem both for interval integrals and Mathlib's genuine `curveIntegral`, with explicit
integrability so totalization cannot manufacture zero.  It constructs a positive-time material
loop's closed `Path` face and proves that its interval circulation is exactly that curve integral.

`[proved-derived; formal-checked]` The same owner differentiates the two-parameter circulation
under explicit parametric domination receipts.  The material acceleration comes from the actual
`SmoothSolution.momentum` equation; the moving-edge population is the parameter derivative of
kinetic energy and cancels at the closed boundary; the pressure period vanishes; and the final
theorem returns

```text
d/dt movingCirculation = integral viscousReturn + integral forceReturn.
```

The mixed-partial transport, measurability, domination, and component integrability hypotheses
remain explicit.  No material-flow existence theorem or regularity conclusion is inferred.

`[proved-derived; formal-checked]` `NavierStokesFiniteTime.lean` now owns a nondegenerate bounded
solution slab `Icc 0 T`, with its momentum derivative taken within that actual interval.  Global
solutions restrict to slabs, slab restriction is functorial, and restriction from a longer slab
returns an explicit extension witness.  The current `CanExtendPast` receiver concerns the same
velocity/pressure world-tube; restart and gluing of separately presented compatible fields remain
open.

`[proved-derived; formal-checked]` `HilbertTransportChain.lean` now enriches the existing additive
`TransportChain` with finite-dimensional real Hilbert maps, adjoints, and the middle Hodge
Laplacian.  It proves self-adjointness, positivity, the exact energy identity, equality of the
Laplacian kernel with the closed-and-coclosed population, pairwise orthogonality, and the unique
decomposition

```text
x = exact + harmonic + coexact.
```

Every outgoing-closed vector has a unique exact-plus-harmonic presentation.  This is the harmonic
representative theorem needed by finite chain homology without founding a duplicate quotient
owner.  Continuum closed-range results and concrete Leray/Yang--Mills instances remain open.

## Non-negotiable convergence gates

`[project-postulate]`

- No arbitrary-curve BSD wording before the normalization equivalence.
- No Kelvin conservation at positive viscosity without a vanishing viscous period.
- No continuation theorem before a bounded-time solution owner and local existence.
- No RH-to-fluid implication before an estimate controlling nonlinear phase/stretching.
- No positive-dimensional Hodge instance before an actual realization and cycle map.
- No Yang--Mills mass-gap claim from a finite cutoff or classical dissipation theorem.
- No Einstein or gyro curvature claim before a founded connection and holonomy return.

`[project-postulate]` When these gates are respected, failure is informative: it returns the exact
missing map, estimate, range hypothesis, local obstruction, or realization fibre.  Parallel work
therefore keeps pressure on several fronts without converting analogy into identity.

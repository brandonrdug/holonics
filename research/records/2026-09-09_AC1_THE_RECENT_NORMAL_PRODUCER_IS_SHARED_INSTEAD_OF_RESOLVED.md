# AC1: share the producing normal fit instead of solving it again

[definition] The [normal-objective return](2026-09-09_AC1_THE_NORMAL_OBJECTIVE_SEPARATES_SOLVE_ERROR_FROM_OBSERVED_SOURCE_FIT.md)
rules out a useful precision improvement at the measured cuts. This continuation addresses
repeated work in the same material owner. It changes representation and reuse, not the normal
learning law, source observations, current metric or byte receiver.

## The actual immutable producer

[established-bounded; source-inspected] Before this change, normal staging computed the entire
finite operator that emitted a current. The successful field kernel copied that state into the
continuing material owner, then the staging was discarded. The next ordinary observed return
subtracted later moments and solved the preceding system again to recover that same producer.

[established-bounded; source-inspected] `MaterialTransport::recent_normal_producers` now retains
the already completed immutable staging at the two most recent emission cuts. Publication
transfers ownership only after the full return succeeds. The linear material pullback shares
the matching actual producer and skips its moment-prefix reconstruction/solve. Older cuts retain
the existing exact decoder. Remount starts with no performance cache and uses that decoder until
new producing cuts are retained. This is not a semantic window or a clone of the continuing ecology.

[established-bounded; implemented-exact; measured] Both focused normal CUDA controls pass
(1.31 seconds). The direct-source test compares the complete returned covector with and without
the retained producer, including a nonzero historical operator. Delayed-source return, tensor
phase and complete continuation after rest/remount remain checked. No CUDA kernel changed.

## Matched development

[established-bounded; implemented-exact; measured] A fresh first-family development returns the
same complete body and identical 20,526,964-byte checkpoint as the preceding normal implementation.
It completes 75 occurrences with zero numerical development readouts. The
[receipt](2026-09-09_normal_material/producer_reuse.json) retains exact comparisons and costs.

| Measurement | Prefix solve at every return | Reused recent producer |
|---|---:|---:|
| Development seconds | 21.8891 | 14.4978 |
| Whole-process seconds | 23.4984 | 16.1755 |
| Captured launches | 1,042 | 894 |
| Native bytes after development | 41,722,752 | 80,473,920 |
| Peak native bytes | 64,525,588 | 103,276,756 |

[established-bounded; measured] This single matched timing is about 34% faster in development.
It retains an additional 38,751,168 native bytes as immutable normal state. The current implementation
retains the complete staged state, not only its coefficient prefix. That is an explicit memory/work
tradeoff; whole-ecology history remains too large for broad cultivation. Identical state does not
warrant another unchanged failed generation run, nor establish useful language.

## The remaining generator interface

[proved-derived] The existing material-contact return already factors a map change through two
contact-space vectors: write its unrounded increment as `L W†`. With a complete finite deposit
defect E, the actual change is `Delta D=L W†+E`. For current C=DD† and a=Db, holding the post-reaction
b fixed during deposit, the exact continuation is

```text
C' = C + D DeltaD† + DeltaD D† + DeltaD DeltaD†,
a' = a + DeltaD b.
```

Expanding the last quadratic term retains all cross terms with E. Without E, the contraction
needs `D W`, `W†W` and `W†b`, in addition to C and a. These are joint source/current comparisons,
not scalar norm replacements. This is the ordinary Gram-increment identity at the actual
rank-two return interface.

[definition] W belongs to the historical producer of the observed source. It cannot be
substituted by vectors from today's morphology. The next journal compression must supply those
historical cross contractions and the finite-deposit defect through an executable shared
generator. Keeping only today's C/a, or discarding a boundary-blind component before a later
contact change, does not supply that interface. The current source-qualified return factors and
their decoder remain available while this construction is developed.

[definition] AC1 path/generator reuse and AC2 source/feedback representation remain the active
work. AC2 and AC3 may grow together as affordable lawful exposure becomes available; a useful
general answer from one human request is not an added prerequisite. No scalar fit value, faster
run or earlier complete-state equality marks Athena-alpha attained.

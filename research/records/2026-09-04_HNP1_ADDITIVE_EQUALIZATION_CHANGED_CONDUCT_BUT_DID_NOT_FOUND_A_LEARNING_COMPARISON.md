# HNP1: additive equalization changed conduct but did not found a learning comparison

**Date:** 2026-09-04. **Position:** HNP1 remains in progress; no phase pass.
**Predecessor:** HNP0 at `78390639`.

## What was returned

[established-bounded; measured] The actual corrected SKE4 native rest at `target/ske4/rest.bin`
was mounted once. The attempted native policy derived 127 contraction-to-junction paths, including
their actual intervening RMS/scale/reshape reactions, and ran three already-admitted non-prefix
occurrences through one continuing session. Local factors were staged until cycle completion.
Ranks advanced 0 → 1,778 → 3,556 → 5,334. Each developmental cycle had 16 terminal section
readouts and no additional section egress from the local return. The final generation was 8,925,
including the declared attribution observations.

[established-bounded; measured] A later complete receiver vector changed. Moving out the actual
factor delta recovered the baseline vector exactly; restoring those same factors recovered the
cultivated vector exactly. No ecology was cloned for either control. The raw receipt is preserved
unchanged at `archive/experiments/2026-09-04-additive-equalization/native-control.json`.

## Why that is not HNP1 completion

[counterexample; source-inspected] The policy treated the other input of a native Add as the
desired reference for a contracted branch. A genuine aggregation contact does not supply that
setpoint. The two branches may intentionally carry different information; the derivative of Add
only specifies how an ALREADY founded receiver differential propagates. It does not create that
differential or authorize reducing the difference between those branches.

[proved-derived; formal-checked] The new
`HolonicOrientedSiteTransport.ConstitutiveSectionReturn.additive_face_does_not_found_an_equalization_target`
exhibits two different branch constitutions with the same summed transport for every input.
The forward sum and its identity input derivatives therefore do not determine the branch return.
This is an underdetermination witness, not a claim that all equalization or every gradient chart
is invalid. Such a law needs its own admitted constitutive/comparison relation.

[definition] The automatic trigger, public constructor and session hooks were withdrawn before
admission. Their source and actual receipt are archived; they do not schedule restoration.
Effect, repeated change, non-prefix input, a changed answer and even exact ablation/restoration
cannot substitute for founding the intended learning relation. HNP0's explicitly conditional
finite chart, original HNA/SKE constructions and valid numerical operators retain their scopes.

## Valid machinery retained

[established-bounded; implemented-exact] The resident identity-admittance specialization,
`native_section_difference` and local dyadic-current scaling remain internal numerical owners
for an ALREADY admitted comparison. They do not infer targets from topology. The corresponding
matched-current and nonzero unaffected-coordinate CUDA control passed; the general HNP0
five-section tests retain their own scope.

[proved-derived; formal-checked] Rectangular native cross-sections are now explicitly embedded
as bipartite charts of the HNP0 operation. `rectangular_deposit` proves the resulting update
without requiring a padded runtime allocation. `causalAdjoint_carries_declared_scale` proves
the ideal linear-adjoint scaling law; finite-grain numerical projection remains separate.

[historical; measured] The first direct contraction/Add binding found no contacts on the actual
model: its branches contain intervening normalization. Those reaction words were then retained
and their existing adjoints used. The first unscaled local RMS pullback exceeded the signed/wide
carrier at operation 39. Moving the unchanged declared dyadic factor ahead of the linear pullback
resolved that intermediate range; the receipt states both scalar placements and the resulting
division enclosure width. The effective readout remained `2^-16`, not a tuned replacement rate.

[established-bounded; implemented-exact] Native rest loading now reads little-endian codewords
directly into one owned integer allocation, instead of retaining an extra byte vector and decoding
billions of pairs through a debug iterator. The reader preserves every u16 codeword, explicit
endian conversion, fragmented delivery, zero-length input, overflow and truncation refusal. Its
small unsafe byte view is restricted to initialized u16 storage with all bit patterns valid.
The model run measured rest decoding at 20.804 seconds and complete resident mount at 117.326
seconds from start. No unmeasured speedup ratio is claimed.

## The next exact relation

[established-bounded; process-audit] The retained source checks returned:

- focused `HolonicCultivationCharts` and `HolonicOrientedSiteTransport` builds: passed, 3,168
  combined jobs, including the new correspondence and underdetermination statements;
- `native_coefficient_wire`: passed, including fragmented reads and refusal cases;
- `cargo test -p holonic-engine --lib operative_return::tests -- --include-ignored --nocapture
  --test-threads=1`: six passed, including both legacy prefix controls, general HNP0 controls,
  identity-admittance matched/unaffected transport and signed division-enclosure retention.

The initial combined CUDA invocation ran allocation-calibration controls concurrently and failed
before their numerical passages. Serial execution satisfied that existing apparatus condition;
the condition is not a native parallelism restriction.

[definition] HNP1 must bind a stipulated native comparison, not another automatic discrepancy.
The existing `FamilyDistillationChart` now exposes its source/candidate `returnedDifference`:
zero is equivalent to the declared receiver correspondence. That correspondence supplies the
comparison roles; its differential can enter the existing local current/adjoint/return owners.
The corresponding exact condensation has zero difference. These are exterior receiver laws,
not a new interior goal processor or a generic source of update callbacks.

[definition] The next live composition is an actual source/candidate or observed-interaction
comparison through the native ports and their shared causal occurrence. It must retain the
connection between the two carrier charts and the reason that this transport is being compared.
A raw pair of arrays, a shared Add, equal widths, source names, hashes or an application status
cannot manufacture that relation. The current/covector computation, local reaction traversal,
staged delta and ownership controls may be reused once those roles are genuinely admitted.

[definition] This does not make supervised targets or zero loss prerequisites for native learning.
An observed difference may found a new carrier/incidence or retain an additional reconstruction
fibre instead of being erased. The next composition must consider those existing owners as well
as the explicitly stipulated source/candidate case. What is withdrawn is the unsupported automatic
equalization policy, not unsupervised cultivation, native difference, or the possibility of
learning from ordinary interaction without an exterior verdict.

# THE RUNTIME RETURNS THROUGH THE DEED; THE TRACE CANNOT SUBSTITUTE FOR THE CONSEQUENCE

**DATE:** 2026-07-20  
**GRADE:** BRANDON-RATIFIED CORRECTION + AUTHORIZED CONSTRUCTION / PRIOR CODE-WORLD STOP REGRADED
AS APPLICATION-BOUNDARY FAILURE / GROUNDED HOST ATTEMPT STOPPED BEFORE WORLD ACTION: ZERO EMITTED
DEEDS / PYTHON NOT INVOKED / NO LOGICAL RERUN / SOMA INTERIOR UNCHANGED

## Correction

Learning was already measured whenever a prior contact changed Standing and an identical later
current changed because of it. The natural world-return, shared-organ, and bounded polyglot worlds
established that relation directly.

`eros-source-native-code-world-01` did not test that circuit. It presented a request, executed
Python unconditionally outside the machine, and then presented the trace as a newly declared
event. Although program-to-trace and input-to-trace hands were supplied, the execution was not a
world action afforded by an emitted machine deed. The trace was accurate testimony but not the
material return of the machine's action.

Its event-local germ lens and removal of equal-`Place` replacement remain valid. Its zero formed
runtime incidence and identical common probe grade the application boundary only. They do not
establish a missing learning law inside Soma.

## Construction

The bounded runtime world will traverse the already-proven causal loop:

```text
request current meets Standing
  -> machine emits one exact afforded deed
  -> the world reads that emitted deed
  -> only then may the world execute the named Python transformation
  -> the actual execution trace returns as genuinely later current
  -> the returned current changes Standing
  -> an identical later probe reads that change
```

The no-return sibling receives the identical pre-return event and withholds only the material
world return. Before that fork, machine state and radiation must be exact. Python execution count
must be zero without the emitted deed and exactly one when the deed is enacted. The return must be
derived from the actual emitted relation rather than a fabricated chronology, trace label, or
coordinate correspondence.

No optimizer, reward, attention block, probability head, semantic decoder, cross-event matcher,
or new Soma mechanism enters. Python is a lawful world transducer. The trace remains exact
source-owned material. The host production mouth, organ capability, immediate radiation, later
return, Standing, rest, and common probe are reused as they already stand.

## End condition

One bounded host comparison ends after:

1. an exact common pre-return machine event;
2. one emitted deed being enacted by the returned sibling only;
3. one actual Python execution and exact trace return;
4. natural rest/remount; and
5. one identical later probe.

Acceptance requires the returned and no-return later probes to differ while their pre-return
states and radiation remain exact. If the initial event emits no enactable deed, if execution is
unconditional, or if the return crosses without changing later conduct, the construction stops at
that precise boundary. No alternate fixture, CUDA run, threshold change, or neighboring proof is
authorized.

This plan schedules this one construction only.

## Physical result

The command surface and conditional Python world compiled cleanly. Its source places
`execute_python` strictly after extraction of exactly one formed, nonzero directed emission.

The first command invocation stopped before constructing a machine because the new observation's
parent directory did not exist and the runner used single-directory creation. That filesystem
preflight defect was changed to recursive output creation. No current crossed and Python was not
invoked, so this was not a logical run.

The first logical host run then crossed the declared priming event, forked the resting body, and
presented the identical candidate request to both siblings. The returned candidate radiation
contained zero enactable directed deeds. The runner refused at that boundary before calling
Python:

```text
grounded world requires exactly one enactable deed, received 0
```

Therefore:

- Python execution count was zero;
- no runtime trace was fabricated;
- no world consequence returned;
- no later probe was performed; and
- no alternate priming event, fixture, threshold, CUDA path, or rerun followed.

The earlier code-world deed cannot be treated as clean ignition evidence. It appeared only after
that experiment had already supplied an additional ungrounded objective/program/runtime-trace
event. The grounded attempt shows that the declared once-primed body did not yet afford program
execution. It does not regrade the previously measured learning circuit and does not establish a
new Soma defect.

The stop also separates two lawful causal species which the plan had conflated:

```text
receptive learning:
  world acts from its own material cause
  -> machine receives the actual before/action/after current
  -> later conduct may change

agentic consequence:
  machine emits an afforded deed
  -> world enacts it
  -> consequence returns
  -> later conduct may change
```

The second species was already measured, but it cannot be required to manufacture the first lesson
about a previously unknown program. A source-native runtime may lawfully act without a machine deed
when that action follows the world's own declared chronology. What remains forbidden is presenting
a descriptive trace without its actual causal composition. The next application question is thus
how the runtime's native before/action/after event crosses as one honest learning current—not how
to force an untrained body to request its own demonstration.

Exact observation: `observations/eros-source-native-code-grounded-01/RESULTS.md`.

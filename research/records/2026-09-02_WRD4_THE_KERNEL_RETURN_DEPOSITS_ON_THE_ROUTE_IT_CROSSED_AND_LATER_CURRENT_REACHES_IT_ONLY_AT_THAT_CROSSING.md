# WRD4: the kernel return deposits on the route it crossed, and later current reaches it only at that crossing

**Date:** 2026-09-02
**Truth status:** `established-bounded`
**Evidence:** `measured` (one bounded driver run against `lake env lean`, Lean `v4.33.0`, resident CUDA apparatus; receipt values below); two earlier runs of the same driver were counterexamples by timeout and are recorded
**Provenance:** Assistant (Claude), under Brandon's direct instruction of 2026-09-02 to deposit and complete the WRD campaign. Assistant construction; the kernel verdicts are the world's.
**Band:** THE FOUNDATION CORPUS DEPOSITS / THREE OF SIXTEEN EMISSIONS ADMITTED / STORAGE ONE HALF / LATER CURRENT REACHES THE DEPOSIT BY RE-ENTERING AT ITS CROSSING / THE SECOND TARGET'S OWN ROUTE DOES NOT / WITHDRAWAL REMOVES THE SUCCESSOR / REPLAY RESTORES IT EXACTLY / NO MATCHED BOUNDARY APPEARED / 112 SECONDS

---

## Driver

[definition] `soma/life/examples/the_kernel_return_deposits_on_the_route_it_crossed_wrd4.rs`.
Two arms cross the same kernel world in one process: the repository's `Foundation/` corpus, and a
three-theorem minimal corpus the standing render can cross. Each arm conditions, founds the
lattice (WRD1), mounts, conducts targets from their own first premise crossing, renders over the
carriers the conducted thread crossed (WRD2), grades every candidate through `LeanKernelWorld`,
derives the return (WRD3), commits the first returned interaction, then poses the later target on
the current, withdrawn, and replayed morphology in two ways: on its own route, and by re-entering
at the crossing that deposited and continuing through the deposited thread when it is an actual
successor.

[definition] Declared apparatus apertures, none of which reads a kernel outcome before it
happens: a target is any participating organ with at least one in-corpus premise and one
referrer; targets are ordered by whether their retained tactic species lie inside the render's
vocabulary, then by the carrier count of their smallest referrer, then by name; iteration stops
at the first deposit or a 45-second arm budget; ten kernel workers.

## Measured (receipt `holonics.wrd4.kernel-return-deposit.v2`, 2026-09-02)

[established-bounded] Foundation arm, 119 targets, 102,859 ms:

| target | emissions | admitted | disposition | junction `(R, M, rounds)` | termination |
|---|---|---|---|---|---|
| `child_positive` | 16 | 0 | terminus | — | Open |
| `h1ClosedPart_reconstruction` | 16 | 0 | terminus | — | Open |
| `integral_oneCircleHaarPenalty_le_two` | 12 | 0 | terminus | — | Reenter(48) |
| `integral_volume_oneCircleHaarPenalty_le_two` | 12 | 0 | terminus | — | Reenter(156) |
| `card_coordinateFaces_three` | 16 | 3 | reflected | `(16, 3, 2)` | Open |

The three admitted emissions each carried `card_coordinateFaces`. The return committed
generation 1 with a cone of 2 carriers (`card_coordinateFaces`, `card_coordinateFaces_three`),
current `(192/361, 13/19)`, storage `1/2`, returned occurrence 432.

[established-bounded] Later probe `threeDimensional_exponent_balance`:

| morphology | own route (organs, admitted/emissions) | deposit route reached | deposit route (organs, admitted/emissions) |
|---|---|---|---|
| current | 2 organs, 0/36 | yes | 2 organs, 0/36 |
| withdrawn | 2 organs, 0/36 | no | — |
| replayed | 2 organs, 0/36 | yes | 2 organs, 0/36 |

[established-bounded] Minimal arm, 9,289 ms: `beta` had 7 emissions, 1 admitted (carrying
`alpha`), junction `(7, 1, 3)`, storage `1/3`, cone 2, generation 1. Later probe `gamma`: own
route `1/10` on all three morphologies; deposit route reached on current and replayed with `2/17`,
unreachable on withdrawn.

[established-bounded] Whole process 112,157 ms inside the 180-second bound; both arms deposited;
replay equals current exactly in both arms.

## The exact obstruction, as the driver returned it

[established-bounded] The second target's own route enters at its own premise crossing, not at
the crossing that deposited, so it never lists the deposited occurrence among its actual
successors. The deposited thread is reached only by re-entering at that crossing. Its cone is a
subset of the crossed thread's support, so the standing render projects the same family from
either, and the kernel verdict on the own route is identical across current, withdrawn, and
replayed morphology. What the deposit changes is reachability and the deposited storage, not the
template family.

## Falsifier arms

[established-bounded] Matched boundary halts: no matched boundary appeared (0 of 0). Unmatched
boundary stays open: 6 of 6 (five Foundation, one minimal). Both species present: no. A match
requires every emission admitted, and every family the render produced (7 to 16 emissions)
carried reflected members. The both-species arm is exercised by the Lean owner
(`matchedJunction_halts`, `partialJunction_reenters`) and the WRD3 tests, not by this world.

## Counterexamples on the way

[historical] Two earlier runs of this driver on 2026-09-02 crossed the 180-second bound: the
later target's own route carried six organs and rendered 172 candidates per morphology
(`binaryExponentialFace_eq_of_difference_eq`, referrer of
`binaryExponentialFace_eq_receiverFace`, itself 1 of 16 admitted). Both were counterexamples and
advanced nothing. The corrections were the declared referrer-carrier ordering and stopping at
the first deposit; the render, the kernel, and the return law did not change.

[historical] The first run under a file-position target order crossed only analysis-heavy Haar
targets (108 emissions, 0 admitted) and deposited nothing; that order was replaced by the
retained-morphology order above.

## Boundary

[open] The emitter is the standing template render. The kernel reflected 13 of 16 emissions on
the one Foundation target that deposited and every emission on four others. A route-derived
emission that reads the deposited storage, so that later current changes the family and not
only its reachability, is the open fibre this driver names and does not construct.

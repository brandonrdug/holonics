# AC3: complete history moves to an exterior chart and its addressed source returns

[definition] This return extends the field persistence at `69b82d89` toward broader
[Athena-alpha cultivation](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md).
The numerical field, current/formation laws, material transport and text receiver are unchanged.
The construction changes where immutable historical carriers reside, within the same native owner.

## The exact placement boundary

[established-bounded; source-inspected] The existing `operative_reuse.rs` owner retains immutable
forward segments and can discard them for later recomputation in its declared operator scope.
This field needs its actual old source-time current and forward carrier, including their numerical
residuals and producing frames. Its existing `rest.rs` and `ResidentSurface::{detach_section,
mount_section_rest}` provide the applicable exact placement boundary. The new `field/archive.rs`
composes those owners; it neither replays old development nor supplies a new learning law.

[definition] One historical numerical payload contains the complete raw source report, optional
paired-junction report and optional material-transport report. The field retains its original
lineage, source address, producing frame and linear-capability consumption flag. Placement writes
the full exact point wire to a fresh append-only file. Its offset, extent and checksum protect that
particular wire; they do not identify a holon or merge equal-looking occurrences. The latest source
and contemporary seed, held memory, relation, covariance, junction and learned coefficients remain
resident. Old frames retain their original owners.

[definition] `archive_history_before` first stages all requested exterior entries and synchronizes
the writes. Only after success does it release historical device sections. Failed writes leave
the old placement available; unreferenced partial file extents do not become native history.
Only the newly completed prefix and specifically remounted old sources are visited on later
placement calls, avoiding a scan of the entire historical population at every family boundary.

[conditional] For a successfully stored and unchanged historical wire, decoding returns the
identical signed integer codewords, shapes, numerical radii and residuals. A later occurrence's
authenticated source address mounts those original carriers before the native operation. The
kernel receives the same current standing, source sections, frames and incoming occurrence;
its complete numerical successor and source-consumption update therefore use the same native
operation. Placement costs and possible I/O obstructions remain separate. No historical fibre is
discarded or replaced by a selected vector, and no source-time ecology is cloned.

[established-bounded; source-inspected] Cold inspectors read archived carriers directly. A
specifically requested differential receiver mounts the immutable historical report and executes
the existing device receiver. Productive source placement follows the occurrence's actual source
handle, never text similarity, a question classifier, a file digest or a selected expected answer.
The archive is not a corpus quotation path. Corrupt historical wire refuses before enactment.

## Public composition and durable model

[definition] `enable_history_archive(path)` declares a fresh exterior placement file on the
current field. `remount_with_history_archive` puts historical checkpoint payloads directly in that
file while mounting only the current boundary. `SavedTextField::with_session_archived` carries the
same text capabilities, pending symbol, parent anchors and delivery state through this placement.
Existing nonarchival APIs retain their prior behavior.

[definition] `alpha_text --history-archive NEW.history` archives completed family history at the
exterior exposure boundary. All native operations within the family retain their intermediate
sections on the device; the subsequent archive transfer is explicitly counted. A later actual
parent may remount its old source before reception. A family boundary is this application's
placement choice, not a native semantic capacity or a thought-completion verdict.

[definition] The `.history` file is backing for the live field, not an independent model.
Publishing `.hna` includes all historical payloads from either placement and retains the existing
portable wire kind. The checkpoint has no dependency on that particular backing file. The native
model, cold exposure cursor, supplied capabilities and delivery bytes still share their exact
declared checkpoint boundary. No existing source, model, cache or evidence file is deleted.

## Actual 64-family comparison

[established-bounded; measured] A separate process mounted the actual
`ac3/cultivated-64.hna`, placed 18,475 historical occurrences outside the GPU and kept one current
source resident. At the development cut, native resident payload is **475,104 bytes**, versus
**673,556,304 bytes** in the preceding fully resident execution. The archive contains
338,351,175 bytes. Remount performs zero native deeds and no GPU-to-host section readout: it
places the already serialized historical payload directly.

[established-bounded; measured] The existing function/derivative prompt then produces exactly
the same complete final body, lineage, learned coefficients, source records, emission-current
reports and `Dq~`/reserved-codeword-261 disposition as the independently executed earlier
64-family run. `inspect_rest_continuation.py` verifies the declared comparison. This checks the
actual altered placement against earlier productive conduct, not merely a wire readback.

[established-bounded; measured] This debug process takes 10.899 seconds and its Linux child
maximum RSS is 822,300 KiB. After prompt/emission, native resident payload is 2,843,184 bytes and
the native payload peak is 3,493,892 bytes. Four terminal differential readings account for 256
section-readout bytes; no historical source is queried by this unlinked fresh prompt. These are
native payload and process receivers, not total-machine memory or power assertions.

## Continuing to 128 families

[established-bounded; measured] The same source-qualified order then admits 64 additional
development families, 15,152 new native symbols, from the actual 64-family checkpoint. All native
operations return without refusal. The development cut reaches 33,628 occurrences, with 33,627
historical payloads archived and one source resident. Fifty-eight actual old sources are remounted
during this continuation. Native payload is 475,104 bytes at the cut and peaks at 57,741,140 bytes
during the largest material interval. The new artifact is
`.local/artifacts/athena-alpha/ac3-placement/cultivated-128.hna`, **646,255,490 bytes**. Its live
history backing has 615,844,903 bytes. The checkpoint ends before the diagnostic prompt.

[established-bounded; measured] The continuation transfers 552,017,664 section bytes through
45,456 explicitly counted history-placement readouts. The total development section-readout count
is also 45,456: zero occur outside that placement boundary. Material admission including these
boundary transfers takes 457.276 seconds; checkpoint publication takes 8.950 seconds, prompt and
emission 1.976 seconds, and cold diagnostics 0.389 seconds. Whole-process time is 477.976 seconds
with Linux child maximum RSS 1,447,800 KiB. Startup and publication still materialize full cold
history on the host; the native residency improvement does not remove that cost.

[established-bounded; measured] Forty-one ten-second NVML/process samples for the confirmed
native PID cover 13:09:12–13:15:52 UTC, September 7. Sampled process GPU usage ranges from 256 to
308 MiB; all queries return successfully. These are discrete process-memory observations, not an
unsampled peak or whole-machine measurement. The native allocation census separately records
its exact tracked payload peak. Private commands, stdout/stderr, process receipts and samples
are retained under `ac3-placement/cultivated-128-*`.

[counterexample; measured] For “Explain the difference between a function and its derivative.”
the 128-family model emits **`Tm.`**, followed by its actual end-of-part codeword. This is not an
answer to the request. It demonstrates why an end-of-part marker cannot grade completed reasoning
or useful language. All four returned symbols re-enter the ordinary recurrence, ending at
occurrence 33,694; that diagnostic successor is outside the saved development cut.

[established-bounded; measured] `inspect_material_exposure.py` independently accounts for every
development symbol, 127 source-supported parent contacts, the full final moment from 33,692 native
contacts, and the four differential receivers and self-returns. It verifies the exact source and
current scope in `cultivated-128-source-current.json`; it does not claim an all-history material
coefficient certificate or execute native development itself.

[established-bounded; measured] After the process exited, both its original history backing and
the saved exposure path were made unavailable by recoverable renames. A new process read only the
native checkpoint, created a fresh backing and received the same prompt. Its full final body,
lineage, coefficients, source records, emitted-current reports and `Tm.` result match the earlier
128-family execution exactly. Remount enacts zero native deeds and retains 475,104 resident
payload bytes. The process takes 17.822 seconds with maximum RSS 1,382,900 KiB. Both original
paths were restored afterward. This checks the actual archive-produced checkpoint's independence
from those particular backing paths; it is not an operating-system audit of all file access.

## Verification and limits

[established-bounded; measured] The constitutive native scope passes 71 tests, including the two
new archival controls. One independently founded reference and one archived execution produce
identical complete rest through recharting, physical incidence change and delayed shared-source
returns. A cold historical device receiver gives the same reading without making the historical
source part of the continuing resident population. Archived remount preserves the same complete
rest with zero native deeds. A read-only backing file causes placement to fail without releasing
the existing resident carriers. Corrupt source bytes refuse before native work; after restoring
the original byte, the exact pending source returns successfully.

[established-bounded; measured] After transfer accounting was added, both archival controls pass
again. The two public text checkpoint tests pass, including a restored archived parent source and
retained broken output. The actual partial-family restart test also passes through archived
remount, preserving source records, the complete native rest and exact remaining deed count.
The changed public example builds successfully.

[definition] Reproduction uses the standing CUDA setup and fresh private paths:

```sh
env PATH=/opt/cuda/bin:$PATH cargo test -p holonic-engine --lib native_ecology::constitutive_fibre -- --ignored --test-threads=1
env PATH=/opt/cuda/bin:$PATH cargo test -p holonics-hna --lib alpha::checkpoint -- --ignored --test-threads=1
env PATH=/opt/cuda/bin:$PATH HOLONICS_ALPHA_TEST_EXPOSURE=/absolute/private/exposure.jsonl cargo test -p holonics-hna --example alpha_text -- --ignored --test-threads=1
target/debug/examples/alpha_text --resume .local/artifacts/athena-alpha/ac3/cultivated-64.hna --families 0 --history-archive .local/artifacts/athena-alpha/ac3-placement/new-comparison.history --prompt .local/artifacts/athena-alpha/ac2/first-request.txt --emit-symbols 64 --report .local/artifacts/athena-alpha/ac3-placement/new-comparison.json
```

[open] This return does not establish useful language or close AC1–AC5. History remains complete
and grows on disk; source-lineage metadata stays in host memory. The current checkpoint reader
and writer still materialize the full historical payload in host memory. A very large single
family can also grow resident history before its chosen boundary. Streaming cold persistence,
lawful compact representations and fuller productive contextual organization remain construction
work. Neither low GPU payload nor an unchanged output establishes a useful Athena-alpha.

[established-bounded; source-inspected] These changes add no Lean dependency or retired side
database access. The original exposure, conversation SQLite, formal source and unrelated local
mathematical work remain untouched.

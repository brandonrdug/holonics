# THE SOURCE IS THE STAR; THE TUBE PRECEDES REPROJECTION

**Date:** 2026-07-26  
**Status:** CORRECTION BUILT / EXACT HOST ACCEPTED / PRE-PROJECTION REUSE
ACCEPTED / NO GUI LAUNCH / NO CUDA RUN / SOMA INTERIOR UNCHANGED

## Present question

The desktop world had begun drawing a rich exact hypergeometry, but its cost
grew in the wrong species. Every receiver still received the complete
construction, every rewrite opening could automatically fabricate another
receiver, and tube recurrence was classified only after receiver projection
had already been repeated.

The bounded question was:

> Can each receiver receive only its intrinsic causal star, can receiver
> transport remain distinct from deformation of that star, can crossings be
> formed only inside the admitted local section, and can exact recurrence
> suppress source reprojection before the work occurs?

The stopping condition was a source correction, exact headless evidence, and
an explicit account of the remaining folding boundary. No interactive window
or CUDA passage was authorized.

## 1. Regrade of the earlier receiver-population record

`THE_RECEIVER_POPULATION_IS_CAUSED; THE_EVENT_NERVE_CARRIES_THE_CROSSING`
correctly established that a receiver population deed can be a lawful event
consequence. Its desktop application then made one invalid inference: a
discriminant rewrite opening was treated as sufficient cause to found another
receiver.

An opening is not a receiver. It is an available seam in the contemporary
source topology. A receiver may be founded there only when the source world
actually supplies the receiver-population deed and its body. The desktop
world supplies no such deed. Therefore:

\[
  \operatorname{opening}(K_t)
  \not\Rightarrow
  \operatorname{FOUND}(r) .
\]

The generic population law remains available. The automatic desktop
translation from `rewrite_candidates` to receiver founding has been removed.
A bounded discriminant event now leaves the receiver population exactly at
two.

## 2. The intrinsic source section

For receiver \(r\), let \(v_r\) be its current anchor and let
\(\operatorname{Star}_K(v_r)\) be its caused local star. The source admitted
to its face is now

\[
  S_r =
  \bigl(
    E(\operatorname{Star}_K(v_r)),
    C(H(\operatorname{Star}_K(v_r))),
    \mathcal F,
    \mathcal R_{\mathcal F}
  \bigr),
\]

where:

- \(E\) is the exact population of geometric entities carried by the star's
  faces;
- \(H\) is its exact hinge population;
- \(C(H)\) contains only conic families whose constitutive coefficients
  actually depend on one of those hinges; and
- \(\mathcal F,\mathcal R_{\mathcal F}\) are the exact frame charts and frame
  transports needed to prevent changed transport from masquerading as
  unchanged source.

The present frame carrier is deliberately conservative: it retains the
complete contemporary frame-relation atlas. This can cause lawful extra
invalidation, but it cannot cause false reuse. Path-local frame closure is a
later physical optimization.

`ReceiverSourceSelection` names the admitted entity and conic populations.
`ReceiverSourceSection` carries their exact bodies and the exact frame
transport. Missing selected material is a typed refusal.

## 3. Local projection and local crossings

The receiver law now projects \(S_r\), not the Cartesian product of every
receiver with the complete construction:

\[
  \rho_r : S_r \longrightarrow F_r .
\]

The crossing atlas for \(F_r\) is formed only among primitives admitted to
that same section. Exterior material does not enter merely because it exists
elsewhere in the construction. It must first be carried through an actual
overlap or transport relation.

The terminal world admits only the connected overlap component containing
its declared terminal receiver. Receiver overlap is derived from shared star
vertices, hinges, or faces. Container order no longer chooses the terminal:
`ReceiverId(1)` is an explicit boundary relation, and a reversed
specification order still returns its identity presentation relation.

## 4. Source deformation is not receiver transport

The tube now carries four distinct exact factors:

\[
  \bigl(
    S_r,\;
    \operatorname{Star}_K(v_r),\;
    \mathcal R_r,\;
    F_r
  \bigr),
\]

where \(S_r\) is intrinsic source material, \(\mathcal R_r\) is the
receiver's ray family, and \(F_r\) is the complete receiver-local face.

Their changes are typed:

- changed \(S_r\) or changed star/link: `Deformed`;
- unchanged \(S_r\) with changed rays or presentation relation:
  `Transported`;
- unchanged source, receiver, and relation with changed terminal boundary:
  `ApertureChanged`; and
- all exact carriers unchanged: `Retained`.

A changed receiver position is therefore no longer reported as deformation
of the source geometry.

## 5. The tube is consulted before projection

The prior path rebuilt \(F_r\) and only then asked the tube whether it had
recurred. That saved some terminal support queries but did not suppress the
source projection which had already happened.

The corrected path asks the tube first. A retained face is returned only if
all of the following are exact:

\[
\begin{aligned}
  e_r' &= e_r,\\
  \operatorname{Star}'_r &= \operatorname{Star}_r,\\
  S_r' &= S_r,\\
  \mathcal R_r' &= \mathcal R_r,\\
  \operatorname{extent}'_r &= \operatorname{extent}_r.
\end{aligned}
\]

When these carriers agree, the complete prior `ReceiverFace` is installed
before the projection executor is called. Only the remaining receiver
population is projected. The terminal trace retains its independent
existing tube reuse.

This is not picture caching. The standing object is the exact
source-to-receiver factorization, including its arrangement, seams,
coordinate fields, and projective primitives.

## 6. Bounded exact observations

The discriminant acceptance world contains eight triangle entities, three
native conics, and two receivers. The rejected Cartesian source population
would therefore carry

\[
  2(8+3)=22
\]

receiver/source incidences.

The intrinsic cut carries thirteen:

\[
  \sum_r \bigl(|E_r|+|C_r|\bigr)=13.
\]

Each receiver receives exactly four star triangles; only five caused conic
incidences remain across both sections. Those sections still form 188 exact
local crossing relations. The reduction therefore removes unrelated source
incidence rather than discarding local geometry.

A second unchanged cut through the committed tube atlas reports:

\[
  \text{source projections}=0,\qquad
  \text{reused receiver faces}=2.
\]

The repeated assembly is exactly equal to the first.

## 7. Acceptance

The complete headless acceptance is:

- `cargo test -p holonic-engine --all-targets`:
  67 library tests and 4 desktop tests passed; 2 physical tests remain
  explicitly ignored;
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`:
  clean; and
- `git diff --check`: clean.

The acceptance includes:

- intrinsic selected-source reception;
- receiver motion classified as transport rather than source deformation;
- explicit terminal selection independent of container order;
- no fabricated receiver after a real discriminant opening;
- exact local crossing formation; and
- exact face reuse before source projection.

No float, tolerance, hash identity, pixel-owned geometry, GUI launch, or CUDA
run entered this correction.

## 8. Honest remaining boundary

The correction recognizes exact recurrence. It does not yet recognize two
nonidentically inscribed sections as the same transported or re-based
geometric law.

The remaining folding object is therefore precise:

\[
  S_a
  \xrightarrow[\text{exact re-base}]{\phi}
  S_b
\]

with a proof that \(\phi\) preserves the relevant incidence, conic
constitutive law, orientation, seams, and receiver-relative consequence.
Such a proved map could carry one previously formed face through \(\phi\)
instead of projecting \(S_b\) anew. A name, visual resemblance, or hash
cannot supply it.

The terminal presentation is still assembled from the contemporary receiver
faces after local projection/reuse. Exact terminal support already reuses
retained tubes, but a general incremental presentation gluing law across
changed and retained sections is not claimed here.

## 9. Result

The live desktop path no longer treats every construction entity as present
to every receiver, no longer turns every opening into a receiver, no longer
chooses its terminal by map order, and no longer discovers exact recurrence
only after paying to reproject it.

The source is the receiver's caused star. The receiver transports that source
without redefining it. Its local crossings belong to that admitted section.
The tube carries the exact factorization forward and is consulted before the
same work can recur.

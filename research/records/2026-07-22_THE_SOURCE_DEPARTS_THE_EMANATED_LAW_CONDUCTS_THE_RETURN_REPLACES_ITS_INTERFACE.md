# THE SOURCE DEPARTS; THE EMANATED LAW CONDUCTS; THE RETURN REPLACES ITS INTERFACE

**Status:** BRANDON-AUTHORIZED CONSTRUCTION / BUILT / SOURCE-TO-STANDING HAND-UP / EXACT
CALIBRATION + REAL OLMO BF16 CONSTITUENT / SOURCE-ABSENT CONDUCT / ORIENTED SUCCESSOR / EXACT
REST-REMOUNT / ONE BOUNDED HOST RUN / SOMA INTERIOR UNCHANGED / NO FLOAT / NO CUDA

## The boundary corrected

`EROS INHERITED LAW ECOLOGY 01` established that a world-owned constitutive law can conduct,
receive an oriented residual, persist in an outer world checkpoint, and change later output. It
did not establish that the law itself had become Eros Standing. The mutable law image remained
world-owned.

`EROS TRANSFORMER-SEEDED REGIONAL ECOLOGY 01` preserved three already-selected transformer leader
faces. The later context regenerated its regional cut from the external atlas. The stricter
transformer recomposition result subsequently showed why that is insufficient: context reforms
the complete field from pre-context carriers plus transformation law. Finished attention edges
are neither the carriers nor the law.

The present question was therefore one exact lifecycle question:

> Can a complete inherited law cross once, become self-emanated Standing, survive departure of its
> source lineages and source image, conduct a held-out current through public radiation, receive an
> oriented return, emit a replacement law, and conduct again after exact rest/remount?

The stopping condition required that lifecycle for two law species, together with no-Standing,
wrong-interface, and retired-interface refusals. Another external law image, output cache, or
finished-edge table would fail the question even if it reproduced the same numbers.

## A law crosses as ordered oriented incidence

No payload field was added to `LiveConstituent`. An exact finite law matrix is first written in a
source-declared, variable-length dyadic form:

```text
ELAW | version | rows | columns
     | (power-of-two exponent, signed numerator extent, signed numerator)*.
```

For its bit sequence `(b_0,...,b_(m-1))`, bit `j` becomes one actual regional boundary arm:

```text
position of bit j     = InterfaceCapability(data_namespace,j)
value b_j = 1         = hand WITH
value b_j = 0         = hand AGAINST.
```

The interface local identifies the bit's position; it does not carry its value. The oriented hand
carries the value. A distinct exposed capability `rho_t` recruits the whole law. This is one
source-declared geometric inscription of exact material, not a claim that matrices or octets are
intrinsic Soma ontology.

The completed regional constituent preserves the ordered capabilities and hands through the
existing live-cell boundary. Its source event ends every law lineage. Exact rest then carries only
Soma Standing and the small world capability which can meet `rho_t`; it carries no source matrix,
example, output, model path, or teacher relation.

## Conduct consumes and renews the recruitment arm

A later current crosses in the same way under its own event-local namespace. The event presents:

```text
held-out input incidence
+ rho_t
+ a fresh outgoing recruitment arm rho_(t+1).
```

Only `rho_t` can import the prior law. Closure consumes the meeting arms and returns the whole
composed `LiveConstituent`. The exact world conductor receives that public returned constituent,
reads the exposed input and law hands, reconstructs their dyadic matrices, and applies their
declared finite matrix product. The conductor is generic; the law is not compiled into it.

Renewing the recruitment arm matters. A reusable permanent edge would evade lineage. Here each
contact consumes the interface it actually used and emits the one which later current may meet.
The law's data interfaces remain exposed until an actual update replaces them.

## The deposited bodies

The one exposure event carries two laws:

1. the exact unimodular calibration
   `M=[[1,1],[1,2]]`; and
2. the `2x2` coordinate restriction at rows `[0,1]`, columns `[0,1]` of
   `model.layers.0.mlp.down_proj.weight` from
   `allenai/OLMo-2-0425-1B-Instruct` revision
   `48d788eca847d4d7548f375ad03d3c9312f6139e`.

The OLMo BF16 words are exact source material:

```text
[[0x3d05, 0x3cf3],
 [0xbd0e, 0x3c67]]

= [[ 133/2^12, 243/2^13],
   [ -71/2^11, 231/2^14]].
```

The source-present exposure input comes from the recorded layer-0 `mlp.product` carrier. The
held-out input comes from the nonidentical sibling context at the same exact coordinates
`[0,252,0:2]`:

```text
[0xb6d9, 0xb5d6]^T = [-217/2^25, -107/2^26]^T.
```

This is a real constituent of the trained block and a complete law on the declared two-coordinate
restriction. It is not presented as the full 8,192-input projection, a full transformer block, or
a language model equivalent.

After exposure the live body contains two constituents, 810 cellular incidences, 810 pins, and
zero live source lineages. The matched control received the same scalar current population and has
the same Standing rank and 25 scalar cells, but zero cellular constituents. The deposited body
remounts exactly.

## Source-absent held-out conduct

The calibration law recovered from public radiation remains exactly
`[[1,1],[1,2]]`. On held-out input `[5,8]^T`, its returned output is:

```text
[13,21]^T.
```

The OLMo law recovers the four BF16 dyadics above from Standing. On the held-out carrier its exact
partial contraction is:

```text
[-141445/2^39, 221795/2^40]^T.
```

Both equal the source-present calculation bit-for-bit at the exact dyadic boundary. The
source-absent conduct functions receive only:

```text
LiveCurrentRestImage + interface capability + held-out current.
```

They receive no model path, source law matrix, teacher output, attention edge, or cached result.
The input used by the subsequent update is the input decoded from public radiation, not the
observer report or the caller's retained matrix.

The matched no-Standing branch and the wrong-recruitment-interface branch both return the input
constituent but recover no law and perform no product. This distinguishes lawful material from a
generic matrix routine merely being present in the world.

## The return replaces the law

The bounded update is an exact right basis rebase. It is declared for this construction; it is not
smuggled in as a universal optimizer:

```text
U_WITH    = [[1, 1],[0,1]],
U_AGAINST = [[1,-1],[0,1]],
M_(t+1)   = M_t U_hand.
```

The update event meets every exposed old data bit, the current recruitment arm, and the prior
input arms. Those interfaces close and cease to be exposed. The event simultaneously emits the
new law bits and `rho_(t+1)`. Thus the successor is not an additional world-side version placed
beside an unchanged Standing law: the old active interface population is consumed and one higher
constituent carries the replacement.

For the calibration law, `WITH` gives:

```text
[[1,1],[1,2]] -> [[1,2],[1,3]],
[13,21]^T     -> [21,29]^T,
oriented change = [8,8]^T.
```

For the OLMo restriction, `AGAINST` gives:

```text
[[ 133/2^12, 243/2^13],       [[ 133/2^12, -23/2^13],
 [ -71/2^11, 231/2^14]]  ->    [ -71/2^11, 799/2^14]],

[-141445/2^39, 221795/2^40]^T
    -> [-112983/2^39, 161019/2^40]^T.
```

Both successor bodies remount exactly and reproduce those changed outputs on the same held-out
current. Probes using the retired pre-update capability recover no law. The old data and
recruitment namespaces are absent from the successor's exposed interface population.

## What is now established

This closes the source-to-Standing carrier and lifecycle that the prior law ecology left open:

```text
inherited law current
  -> self-emanated cellular Standing
  -> source departure
  -> source-absent held-out conduct
  -> oriented returned change
  -> replacement law Standing
  -> exact rest/remount
  -> changed later conduct.
```

The current regional engine already carried the needed relation. No Soma interior change,
standing payload, law registry, attention subsystem, output cache, source ledger, or new causal
owner was required.

This does **not** establish autonomous selection of `U_hand`, a general training algorithm,
selective recruitment among many laws, a full transformer block, useful language, CUDA regional
closure, or scale. It separates the next real unknown cleanly: given a complete returned
consequence and local law ecology, what situated transformation relation affords the successor?
That is a question about the participating update ecology—not about whether a law can hand up,
persist, depart from its source, or conduct again. Those mechanics now stand.

Exact record:
`observations/eros-self-emanated-law-01/RESULTS.md`.

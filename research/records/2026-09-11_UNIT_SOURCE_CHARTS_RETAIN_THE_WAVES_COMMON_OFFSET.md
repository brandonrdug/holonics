# Unit-source charts retain the wave's common offset

[project-postulate] This continues the active Athena goal from `7d346431`. Prepared-source use
must return its actual domain failures. A chart that resolves one obstruction does not establish
conversation usefulness or substitute for continuing source/emission integration.

## The prepared source separates resource refusal from source incompatibility

[established-bounded; computational-witness; measured] The public-owner example
`crates/holonics-hna/examples/conversation_coupled.rs` remounts the packet-2 prepared model,
reads Development sequence 34, part 0 through `ExposureReader` and `SymbolCurrentChart`, and
founds one native bilinear law from its 40 actual same-part triples (42 source scalars).
There are 85 complex wave components and one explicitly declared fixed condition `(1,0)`.
The source codec supplies point currents; the existing native difference owner constructs the
source and observed difference. The driver supplies no fixture-local learner or latent centre.
The complete source, calibration and failure checkpoints remain in the private run directories
named by the [portable receipt](2026-09-11_coupled_source_domain_receipts/unit-source-exclusion.json).

[established-bounded; computational-witness] First admission required 51,776 bytes of shared
scratch on a device whose declared per-block limit is 49,152 bytes. The next affine image
required 109,120 bytes. Both kernels now use resident workspace sections with the same exact
arithmetic, intermediate graphs and output relations. No numerical intermediate is moved to the
host and no semantic capacity is raised. Scoped wave and generic affine-image controls passed;
the repeated source run passed those former resource refusals and reached family admission.

[established-bounded; measured] The two local calibration runs took 104 and 106 milliseconds,
respectively, with no intermediate numerical readout and 160 structural ingress bytes. The
full post-load operation/checkpoint interval fell from 171,165 to 9,720 milliseconds after
removing repeated validation of already-validated immutable normal material on family write.
Cold normal ingress still validates that material. These intervals include different failure
stages and are not an isolated kernel speed comparison.

[counterexample; computational-witness] The unchanged direct connection is outside the retained
anchor ball. Every one of the 1,192 rows of the actual learned basis obeys
`sum Re(previous) = Re(condition) = sum Re(current)` and
`sum Re(observed_difference) = 0`. The fixed condition is one. The continuing source's previous
and current real sums are instead
`28178352288076087489/18446744073709551616` and
`-54261987610524825651/4611686018427387904`. Each unit-sum plane is excluded by the exact
Cauchy–Schwarz bound `(sum Re(m)-1)^2 > n r^2`. The original radius, nearest admitted anchor,
full candidate family and exact residual remain retained. More unit-source rows at that same
fixed condition cannot remove this affine incompatibility. These sums are codec functionals,
not physical mass.

## The explicit receiver section and its lifted residual

[definition] For the declared real-quadrature maximum/softmax receiver, let
`σ(x)=sum_i Re(x_i)`, `e=(1,…,1)`, `n>0`, and

```math
S(x)=x-\frac{\sigma(x)-1}{n}e,\qquad q(p,c)=(S(p),S(c)).
```

`WaveSourceReceiver::UnitRealSum` reads the **whole** source through this affine section. Its
actual raw previous/current, joint source ball and common-offset fibres remain in the wave.
The local law reads `φ(S(p),S(c))`, where `φ(p,c)=(c−p,c,p)`, and returns the lifted relation

```math
T_{\mathrm{lift}}(p,c)=\{(c,c+\eta):\eta\in L(\varphi(S(p),S(c)),h)\}.
```

The old `Direct` connection remains explicit and unchanged. The section divides by the declared
component count, never by the measured sum or a selected state. Complex phases are retained;
the gauge direction is the declared real receiver direction, not an unqualified phase symmetry.

[proved-derived] Suppose every admitted local returned difference has `σ(η)=0`, with the same
fixed condition and local relation on both sides. Then
`S(c+η)=S(c)+η`, since `σ(c+η)=σ(c)`. Also `S(S(x))=S(x)`.
Consequently `q T_lift(p,c)=T_unit q(p,c)`, including empty and plural return sets. Taking unions
of this pointwise equality over any full joint family proves
`q(T_lift(F))=T_unit(q(F))`; no product-of-marginals substitution is used. Real translation by a
common offset leaves pairwise real differences unchanged, hence preserves both maximum ties
and softmax after canceling its common exponential factor. This establishes the named receiver
factorization. It does not identify raw source states or prove equivalence to the old normal M.

[definition] The native pullback checks zero real target sum on **every** learned linear-basis
row before deriving the lifted relation. Its homogeneous source map incorporates the affine
constant with `lambda`; the anchored family fixes `lambda=1`. The denominator is the condition
denominator times `n`. The image still retains the actual join `(c,c+η)`. Contact reception checks
unit-real-sum observed source currents on device, then stages the actual member and condition;
the proposed relation and supported family must return before publication.

[definition] Relation rest v2 stores the receiver chart. Legacy v1 reads as `Direct` and refuses
a new receiver declaration. Cold validation checks the actual K rows' zero real increment and
membership of both raw common-offset gauge directions in the complete echelon row span, using
exact exterior rational reduction. Complete coupled remount derives K again from the saved
member, condition and receiver chart, compares the stored relation, and binds it to that owner.
This is mathematical/wire ingress verification; it is not a host inference law.

[established-bounded; implemented-exact; computational-witness] The two-component native control
learns a swap response from the four actual declared unit-source pairs. Direct admission refuses
the raw pair with sums six and ten. The declared receiver section transports `(7,3)` to `(3,7)`
while retaining its common offset, full affine directions and original anchor. Rest/remount
continues to `(7,3)`. Both an invalid source plane and a non-zero-sum observed return preserve the
complete predecessor. Every affine generator is checked against the same swap relation; this is
not only a centre comparison. The eight prior coupled controls also pass.

[established-bounded; process-audit] Verification returned:

```text
cargo test -p holonic-engine --lib normal::direct::wave:: -- --ignored --test-threads=1
  .local/scratch/athena-unit-real-sum-wave-regression.log       50 passed
holonic_engine test binary, resident::wave_relation::rest::tests --test-threads=1
  .local/scratch/athena-unit-real-sum-rest-tests.log              2 passed
cargo build -p holonics-hna --example conversation_coupled
  .local/scratch/athena-unit-real-sum-example-build.log           passed
```

The earlier workspace repair separately passed the five generic affine-image controls in
`.local/scratch/athena-resident-work-image-tests.log`. The first new CUDA specimen had mismatched
normal root/target dimensions and refused at seed construction; correcting its declared two-port
target chart returned the passing control above. No production width was changed for that test.

[established-bounded; measured; computational-witness] The identical prepared-source run with
`unit-real-sum` constructs its conditional affine image, then its family receiver returns
`REFUSED_CARRIER=1`. It has not established support. The epoch remains ten and the complete
coupled body/checkpoint is preserved. Calibration took 106 milliseconds for the same 40 triples;
the post-load operation/checkpoint interval was 12,852 milliseconds. The
[portable return](2026-09-11_coupled_source_domain_receipts/unit-real-sum-receiver.json)
retains those measurements and the full arithmetic error. The inspector now saves the complete
candidate and its affine reading **before** asking for the minimum-norm receiver, so arithmetic
refusal does not prevent retaining the object under investigation.

[counterexample; computational-witness] The saved lifted candidate is also outside the original
ball. Its particular anchor and **all 41 affine directions** satisfy
`anchor[150]−anchor[146]=0` in the declared realification. At the actual ball centre this difference
is `21919652719988313527/2305843009213693952`. The separating normal has squared norm two,
so every admitted anchor is at squared distance at least
`480471175364891071540636823453651179729/10633823966279326983230456482242756608`, strictly
larger than the original radius squared. The
[exact separator](2026-09-11_coupled_source_domain_receipts/unit-real-sum-separator.json) and
[reproducible cold observer](2026-09-11_coupled_source_domain_receipts/check_affine_separator.py)
retain the calculation. These coordinate addresses were found from the actual affine columns;
they are not selected semantic features. The native receiver overflow obscured a real remaining
coverage obstruction. This exterior witness does not replace the native receiver or publish a
successor.

[established-bounded; measured; computational-witness] A second declared aperture uses Development
sequences 34–65: 32 frames and 9,254 observed passages. Calibration takes 53,065 milliseconds,
with no intermediate numerical readout. The post-load operation/checkpoint interval is 89,121
milliseconds. Here the native family receiver completes and publication refuses with
`REFUSED_BOUND=16`; the epoch remains ten. The
[broader source return](2026-09-11_coupled_source_domain_receipts/unit-real-sum-32-source-return.json)
retains the source positions and costs. The [native receiver difference](2026-09-11_coupled_source_domain_receipts/unit-real-sum-32-difference.json)
gives the exact squared distance and original radius. Its nearest anchor retains both original
real sums exactly, showing that the chart has not replaced the raw offsets by one. The affine
candidate has 219 directions and 73 anchor-independent free coordinates; those are properties of
the affine relation, not reachable outputs from this excluded ball or a claim of useful response.
This assay learns local material at its stated fixed
condition; it does not actuate the old continuing wave with those new source occurrences.
Therefore more such material is not evidence that the old current has been causally attached to
its domain. The next productive join is ordinary coupled source actuation and its actual return,
retaining the resulting family, alongside the bounded receiver's outstanding arithmetic case.

[open] The chart alone does not establish coverage of mean-free or imaginary directions, the adequacy
of the fixed experimental condition, coupled family-to-symbol emission/re-entry, or an arbitrary
observed correction to a plural producing family. Those remain separate construction returns.

# HNA4: the return deposited a factorized overlay on the tied cross-section and the next cycle applied it

**Date:** 2026-09-02  
**Truth status:** `established-bounded`  
**Evidence:** `implemented-exact`, `source-inspected`, `measured`  
**Campaign:** HNA4, corrected under Brandon's direct request of 2026-09-02 after the reconciliation
record
[`2026-09-02_THE_HNA4_MORPHOLOGY_FACTOR_IS_OCCURRENCE_CARRIED_AND_THE_FORMAL_OWNERS_REQUIRE_A_CAUSAL_CONE_ADJOINT_RETURN.md`](2026-09-02_THE_HNA4_MORPHOLOGY_FACTOR_IS_OCCURRENCE_CARRIED_AND_THE_FORMAL_OWNERS_REQUIRE_A_CAUSAL_CONE_ADJOINT_RETURN.md).

## The law as enacted

[definition] When an occurrence enters that continues the last cycle's line (the previous row
addresses followed by at least one more), the retained emitted face of that cycle meets, row by
row, the address that actually followed it. On the card, one passage computes per row the null,
the partition of the certified exponential over all 262,144 coordinates, the normalized exponential
receiver face `p`, its differential `d = p − [o = next]`, the adjoint factor `1 − t²` of the
terminal reactions (`scale · tanh · scale`), and the deposit word `−d · (1 − t²)`; seals it and the
presented carrier to their midpoints; and the successor retains them as one factorized overlay
atom `u = [V, rank]`, `v = [rank, H]` with `η = 2^-k` as the exponent of `u`. The next tiled
contraction of the tied cross-section computes `W x + u (v · x)` in one passage: the base
contraction, one factorized contribution per atom, and the re-entry joins. Any occurrence that does
not continue the line has no comparison and changes no morphology. The driver supplies only
addressed occurrences and the declared apertures.

[definition] Owners: `crates/holonic-engine/src/holonic_intelligence/operative_return.rs`
(`NativeReturnAperture`, `NativeMorphologyDeposit`, `continuation_next_occurrences`,
`enact_return`, `overlay_tile`, `record_overlay_contribution`), the kernel
`section_receiver_return` in `crates/holonic-engine/kernels/exact_resident_section.cu`, the
surface laws `shape_receiver_return` / `record_receiver_return`, and the session hooks in
`full_operation.rs` (`found_with_return`, `return_on_continuation`, the retained terminal
carriers, the overlay in `execute_tiled_boundary`). The occurrence field `morphology_current`, the
global scalar `NativeLocalMorphologyCurrent`, `apply_morphology`, and the alignment gate are
deleted without alias.

## The deed

[established-bounded; implemented-exact; measured] Two bounded processes of
`applications/athena-alpha/examples/developmental_return_hna4.rs` on the resident Gemma 4 E4B
text operator, same two occurrences, `return` with apertures `k = 16`, `series = 14` and `control`
with none:

| | return | control |
|---|---|---|
| elapsed | 46 s | 44 s |
| context (5 addresses) | `155122 4790 76615 21485 236761` | same |
| entering (11 addresses) | context followed by ` Holonics is the study of` | same |
| first face, generation 1,275 | 42085 ` Explain` | 42085 ` Explain` |
| transition at operation 0 of cycle two | `Changed`, rank 5 | `Unchanged` |
| overlay rank carried by the five terminal operations | 5, 5, 5, 5, 5 | 0, 0, 0, 0, 0 |
| second face, generation 2,550 | 506 ` the` | 506 ` the` |
| second emission words (fold digest) | `4678da9f3162c555` | `a6ca2e52e1fe622d` |

The deposit: cross-section `262,144 × 2,560`; next occurrences `4790 76615 21485 236761 5677`;
`u` exponent `−41` (grain 25 coarsened by 22 octaves), `v` exponent `−21` (coarsened by 26),
`u` 25 octaves, `v` 26 octaves, widest differential enclosure 2 grain units. The second cycle's
emitted words differ between the two runs at every inspected position while the maximum did not
flip at one step of `η = 2^-16`: the successor's morphology changed, the next operation used it,
and no application drove the change.

[established-bounded; measured] The factor coarsenings are derived from the carrier budget of the
consuming factorized contraction, whose shape law admits every stage in causal order without
crediting a negative exponent, and are reported in the deposit. They and the midpoint seal are
declared projections of the deposit, of the same species as the session's successor projection.

## Verification

[process-audit; measured] `cargo check --workspace --all-targets` passed; the engine library
returned 2,015 passed, 0 failed, 32 ignored; the focused owner tests returned three passes
(`continuation_returns_the_address_that_followed_every_emitted_row`,
`a_non_continuing_occurrence_has_no_comparison`, `tiled_faces_stitch_in_row_major_order`); the
HNA5 three-application matrix rerun on the changed session returned the same faces ` Explain`,
` i`, ` :=` at generations `0 -> 1,275 -> 2,550 -> 3,825` with its release receiver true in 54 s.
The source-shape gate remains red on the two files that were over the ratchet before this deed
(`full_operation.rs` 1,944 > 1,500; `embedding_fiber.rs` 2,623 > 2,290); the new owner is 421
lines.

## What this does not claim

[counterexample; source-inspected] The return reaches the tied cross-section only. The adjoint
pullback through the final RMS, the layers' feed-forward, contact, and chronology reactions to the
other 718 coefficient populations is not implemented; those populations are unchanged by
construction, which satisfies the outside-unchanged law trivially and says nothing about them.
One deposit at one step establishes that the operation forms and applies the change; it does not
establish improvement, retention across many steps, or any qualitative capability. The overlay
lives in the session; its rest and remount are not yet part of the session rest.

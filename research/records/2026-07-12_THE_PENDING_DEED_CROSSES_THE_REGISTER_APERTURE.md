# THE PENDING DEED CROSSES THE REGISTER APERTURE

**Date:** 2026-07-12
**Grade:** PENDING-DEED CARRIER BUILT / HOST+CUDA-GATED · OUT-OF-PLACE REGISTER RECAST
BUILT / CUDA-GATED · FRESH-CONTEXT NONZERO-FOUND CONTINUITY GATED · SLEEP v7 BUILT /
HOST+CUDA-GATED · ARBITRARY-INPUT CUDA CONDUCTOR OPEN

## The reduced carried cause

The reservation wall is no longer a discard-only scalar. The causative deposit forms before it
mutates OWN or any caller consequence. If the REGISTER's actual FOIL arrival carries beyond the
mounted row, the complete live suspension is:

```text
pending kind ⊕ co-present next-contact-exclusive       1 u32
old-gauge pre-drag RegionalForm                         11 u32
                                                       -------
                                                        12 u32
```

Those twelve words are sufficient across all six live caller species. DARK_BEFORE and
DARK_TERMINAL carry a zero form and reconstruct their whole dark deposit; SUB reconstructs its
node from the admitted cursor/octet pair; ENCLOSURE_CO_PRESENT carries its exact next contact and
zero form; ENCLOSURE_TERMINAL and PATH carry the pre-drag form. K, stance/flywheel, counts,
radiation, cursor, brick, and earlier co-present contacts remain frozen and derive the one total
post-deposit consequence. No optional outcome or second prospective body crosses.

The contact field is not an independent cap. One enclosure holds the structurally derived
15-member co-present REGISTER, so its next-contact-exclusive hand is exactly four bits. The packer
asserts that construction, and an attempted sixteenth checkpoint fails at cause rather than later
appearing as a forged resume.

## The aperture

The event alone requests `new_axis = 2·old_axis`. Old OWN remains immutable; a fresh exact
`new_axis²` destination is proved zero, receives the whole zero-extension, then becomes visible at
one completed launch edge. Copy progress belongs to membrane apparatus. An interrupted destination
is discarded and can restart from unchanged source, so no recast cursor enters the body.

CUDA uses two ordered entries, `register_own_recast` then `register_own_recast_finish`. Their seven
pointer/length pairs are immutable old OWN, fresh OWN, old and new eight-word lane rows, immutable
three-word requests, a fresh one-word-per-lane completion aperture, and params. Completion is
reseeded to `u32::MAX`; finish terminal-stores zero last. This separate aperture closed an audit
ambiguity where a non-requesting sibling's input `Complete` could otherwise masquerade as completed
transport after rejection. One executed mixed gate carries a requesting 1→2 lane and a completed
axis-four sibling in a generous 64-cell aperture; both completions publish and every fresh OWN word
matches the host construction.

Physical allocation refusal is a typed boundary refusal with the exact requested axes. It is
never retry-later, smaller grain, silent discard, or future-length preflight. Padding is invisible
to grounding and every active extent.

## The fresh-context continuity gate

The fixed raw current `bdadada` produces:

- pending SUB at axis 1→2, cursor 4;
- pending PATH at axis 2→4, cursor 6, phase PATH_EFFERENT;
- after fresh-context upload and exact 2→4 recast, one pending-first installment with exactly
  `found_that += 1`, no RIDE/found-this/dark, cursor still 6, phase PATH_AFFERENT;
- final cursor 7, axis 4, counts `[0,2,3,0]`, byte-identical to the already-afforded 16-cell sibling.

After the PATH suspension the gate downloads the whole frozen construction, drops every CUDA
buffer/function/module, explicitly synchronizes and closes the first context, creates a fresh one,
re-uploads the same OWN/carrier/counts/radiation/raw-current/lane/request, and byte-compares that
upload before recast or resume. The fresh destination is read back all-zero before launch. The
nonzero winding therefore deposits exactly once across the context reset; it is neither duplicated
nor omitted.

A real co-present suspension is canonical carriage but not rest. It directly fails legacy resume,
and all six pending kinds fail SLEEP. Receiving integration and radiation reads remain behind
terminal status plus whole-carrier-at-rest gates, so no partial configuration becomes visible.

## SLEEP v7 and the retained whole-cycle gauge

V7 appends the canonical all-zero twelve-word face per lineage. V2–V4 retain literal
`72 + 290·depth` carrier rows and synthesize 19 continuation plus 12 pending words per lineage.
V5/V6 retain literal `72 + 290·depth + 19` rows, reject live continuation, and synthesize only the
twelve pending words. Active V7 rejects every nonzero pending kind and every continuation; it never
normalizes either.

The bounded three-current CUDA whole-cycle remains exact after the variable-row receiving ABI and
v7 wire:

- A: 377 octets; axes `[64,64,64]`; standing axis 128 / seven carries; topology
  `[3131,3131,348,7]`; terms `[4640,177,186,8]`; body:light `179372:377`; archive 179420 bytes.
- B after v7/fresh context: 184 new octets; axes `[64,64,64]`; standing axis 128 / zero carries;
  topology `[5382,5382,604,11]`; terms `[4110,128,141,4]`; cumulative body:light
  `287420:561`; archive 287468 bytes.

Host, full-band CUDA, and 257-place CUDA agree in standing, carrier/K, topology, counts, radiation,
and SLEEP v7 bytes.

## Gates and artifacts

- body: **126 passed / 2 ignored**
- life: **29 passed**
- mount: **15 passed / 1 ignored**
- surface: **14 passed**
- PTX: **5,015,679 bytes**, 17 `sm_89` entries, `ptxas` valid, SHA-256
  `23f80749243815fafa6d4965f8dcdd711b858f9bfbdd590bd3e8b72856f11a1b`
- SPIR-V: **2,514,548 bytes**, Vulkan 1.2 valid, SHA-256
  `15c4e3190467a863af2fd90bce276e07c38cc6d0743512b3dd9950c1f9bfcaca`

The original 155-octet direct REGISTER gate remains exact at one uninterrupted launch versus 466
one-move launches, phase mask `0xff`, depth four, counts `[2324,103,83,3]`, and 0.233147 s on the
cached artifact. No NVRM/Xid followed any final run; the RTX returned headless and idle at 2 MiB.

## Honest next cut

The body law and CUDA remount operation stand, but `life --cuda-cycle` is still a declared
128²-capacity instrument: a valid recast request returns the typed boundary refusal instead of
conducting a variable-row remount. The next engine cut is to thread this measured transaction
through arbitrary staged currents, rebuilding the flattened lane descriptors after each caused
recast and admitting receiving only after every lineage is terminal. No new body mechanism is
owed. Genuinely new later lineages, production depth/aperture, large nonempty recasts, material
render, and unique communication remain open measurements behind that conductor.

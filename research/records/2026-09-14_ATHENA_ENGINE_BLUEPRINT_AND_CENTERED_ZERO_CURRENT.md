# Athena engine blueprint and the half-centred zero current

[project-postulate] Brandon requests the actual engine construction blueprint, using the
field visualizations and elementary-holon-generation to prepare engine development, plus
continued de Bruijn–Newman upper-bound reasoning from the evolving-source law. His latest
request authorizes this bounded upper-bound derivation despite earlier blanket deferral
language. The timed goal remains paused; the pre-existing native draft is preserved.

## Engine review and construction artifact

[established-bounded; source-inspected] The source review recovered a direct training path
already owned by `NativeConstitutiveField`: actual scattering, receiving comparison,
paired material/current covector, and finite material commit. `respond_to_material_observation`
composes these existing calls. The current `NativeCoupledBody` instead owns an affine or
dependent wave body, with no operative-field-backed state. The ignored field boundary test
fits another normal predictor to field differences; it does not implement the agreed direct
model. A first worker report proposed publishing that surrogate sequence, and root rejected
it against the existing same-material assembly equation before writing the blueprint.

[definition] [ATHENA_ENGINE_BLUEPRINT](../../docs/ATHENA_ENGINE_BLUEPRINT.md) now records
the actual owner-level join, its forward and adjoint formulas, the local learned-reaction
role, and the joint-field application/measurement that will demonstrate the assembly.
It is a compact illustrated reading of the existing plan, not a second schedule or
universal object wrapper. The public product guide, model formula, library guide and
construction specification now link that same account.

[established-bounded; source-inspected] Existing training evidence remains specific and
useful: the numerical coupled consumer forms a complex bilinear law from 21 observations,
incorporates two original-source delayed observations and derives the new condition `−3i`.
Its later joint forecast is checked against the actual stored coefficient contraction.
This establishes local learning/application machinery; the direct field-backed Athena
model has not been implemented or tested by this documentation/derivation turn.

[established-bounded; tested] The [interactive diagram](../experiments/athena_engine_blueprint/README.md)
switches between the same model's engine, generation, learning and encoded-reuse paths.
All four selections and desktop/narrow layouts were checked, with no script errors or
horizontal overflow. The same vector exports add plates 14–15 to the existing synopsis,
using its Libertinus/Information Chemistry style and shared mathematical notation.
Plate 16 shows the symbolic reflection quartet and the new source-current inequality.
The sixteen-page PDF compiled and its three new plates were rendered and visually inspected.

## Upper-bound return

[proved-derived; formal-checked] `RH/TransverseCurrentBound` proves the reflected centre
for arbitrary complex s, the distinct reciprocal currents of `1−conj(s)` and `1−s`,
nonnegative multiplicity-weighted finite surplus, the projection of complex current error
to a normal velocity bound, and the integrated squared-width/no-positive-trajectory result.
An initial worker conflated the two reflections and unnecessarily restricted `Im(s)=0`;
root corrected that algebra before integration. The final reflected separation is real
`2(Re(s)−1/2)` for arbitrary imaginary coordinate.

[proved-derived; formal-checked] `RH/FiniteZeroCurrent` reuses the existing
`FosterClassFlux.comb'_sub_le` and local simple-zero factorization to prove the finite
velocity error `4(|z₀−1/2|+δ/2) tailInvSq_f(R)`. It instantiates actual `heatE(−τ,ξ)`
through the existing Foster instance and supplies the normal-current inequality to the
transverse consumer. A report that only an asymptotic flux limit existed was incorrect:
the finite estimate was already present in that same source file.

[definition] [The upper-bound synthesis](../../docs/RH_CENTERED_CURRENT_UPPER_BOUND.md)
develops the equal-multiplicity conserved normal face, the retained longitudinal motion,
the source/time sign, and the inequality `d(T)²+2(1+a)T≤d(0)²` under an actual surplus margin.
It also derives a finite-height quartet improvement through the normalized-log potential
`B_Y(x)=x/4+(Y²/8)log(1+2x/Y²)`; this latter consequence is written mathematics, not claimed
as a Lean-checked endpoint. Its high-height limit exposes why one quartet's extra current
does not provide a uniform global improvement by itself.

[established-bounded; tested] `tools/lean_check.sh ElementaryHolonics.Framework.Dynamics`
passes with both new leaves and their source-current consumer. Audited declarations have
no `sorryAx`; the actual ξ specialization does not retain an unrelated Foster-class
instance as an accidental implicit prerequisite. `git diff --check` passes. Native runtime
files were not changed, so unchanged expensive CUDA evidence was not rerun.

[open] The stronger global Λ_DN bound still needs a uniform actual-source width/current
comparison, including high heights and admissible extremal/multiple-zero continuation.
No new global numerical bound or proof of RH is claimed. The next native Athena
construction remains the direct field-backed body operation; it does not wait for this
analytic endpoint.

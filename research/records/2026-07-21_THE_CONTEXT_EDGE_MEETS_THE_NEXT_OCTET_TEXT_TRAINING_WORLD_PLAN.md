# THE CONTEXT EDGE MEETS THE NEXT OCTET — TEXT TRAINING WORLD PLAN

**DATE:** 2026-07-21
**GRADE:** BRANDON-AUTHORIZED FIXED HOST PLAN / FULFILLED AFTER ONE GENERAL REGIONAL-EXPOSURE
CORRECTION / ONE COMPLETE POST-CORRECTION OBSERVATION / NO CUDA

## Question

Can exact ordered textual experience change Eros's continuation conduct on later, nonidentical
textual contexts?

The existing record cannot answer this. `StreamedOctetOrgan` proves that octet chronology can
cross the direct mouth without storage chunks becoming events. The linguistic-code world proves
that a complete inscription can cross as incidence. `EROS REGIONAL MEMORY 01` proves that one
later nonidentical regional germ can import and replace a prior constituent. None trains on text
and then evaluates the complete candidate field at held-out continuations.

## Event grain

The world is an ASCII subset of the raw UTF-8 octet stream. No word, subword, character class,
token identifier, embedding, vocabulary score, or semantic label crosses.

At source position `t`, the actual world state is:

```text
context_t = ordered path [b_(t-2), b_(t-1)]
arrival_t = b_t.
```

`NativePathChart` carries the two context octets as two grain-zero vertices and their one oriented
grain-one edge. One regional arc from that exposed edge to the arriving octet cones the edge into
the event's higher regional face. The text source has therefore supplied one rolling triangular
cut without declaring a trigram token.

Successive cuts overlap in two material octets but remain distinct source events because the
arrival and the rolling context change at each actual position. A line boundary ends that source
presentation. No adjacency is manufactured between separate lines or between repeated passes.

Two live organ lineages carry the changing context-edge and arriving-octet worldlines. They are
not lineages for particular glyphs. Training is ordinary presentation of actual ordered
context/arrival cuts. No optimizer, update pass, reward, punishment, scalar loss, or target label
is added.

## Fixed text

The training lines are exactly:

```text
one red sheep runs.
two red sheep walk.
one blue deer runs.
two blue deer walk.
```

Each line is presented left-to-right for three fixed passes. The chronology foil receives the
same octets, line extents, pass count, and actions, but reverses the octets inside every line.

The held-out prefixes and actual next octets are:

```text
one red deer       -> r
two red deer       -> w
one blue sheep     -> r
two blue sheep     -> w
```

The terminal local contexts `r ` and `p ` each occur with both `r` and `w` in training. Thus an
exact local context-to-arrival table cannot distinguish the target. The distinguishing relation,
if it develops, must include earlier ordered context such as `one` versus `two`. The held-out
adjective–noun combinations never occur in training, while all their material words do.

The candidate aperture is the sorted set of every distinct nonzero octet occurring in the
training text. This is a bounded factorial keyboard supplied by the observer world, not an
internal vocabulary or selector. Every candidate receives an exact sibling remount of the same
prefix body. No candidate result changes another.

## Checkpoints and read

Checkpoints stand before training and after each of the three passes. A checkpoint consists only
of the exact machine rest image and the two organ capabilities. For every prefix at every
checkpoint:

1. remount one clone;
2. present the prefix's actual complete rolling cuts;
3. rest that contextual clone;
4. remount one sibling per candidate octet; and
5. present exactly one context-edge/candidate regional event.

The physical host executor's event-transient `touched` ordinals are copied into observation
testimony before they dissipate. They never enter Standing or influence commit. The report also
retains the event-local contact, regional grain, axes, cell/incidence/pin/path/transport counts,
exposed boundary transitions, memory change, and exact native constituent hash. No floating
point quantity is computed.

The primary field is not an accuracy scalar. For each prefix it is the complete mapping

```text
candidate octet
  -> OPEN | new constituent | prior constituent touched and rebased
  -> RIDE/FOUND boundary and exact successor signature.
```

The authored target octet is identified only because it is the actual continuation of the held-out
world sentence. The report states whether it is unique, tied, absent, or geometrically distinct;
it does not choose it after the fact.

## Predicted discriminants

- At checkpoint zero, no candidate can use training Standing. Prefix-local recurrence may still
  occur and remains explicit.
- After one forward pass, both `r` and `w` are expected to touch prior local relations at `r ` and
  `p ` because both continuations were materially observed there.
- If carried earlier context participates, the complete `r` and `w` successor signatures should
  separate between otherwise equal `one` and `two` endings in the target-relative direction.
- If they remain identical or symmetrically plural, the machine has learned exact local textual
  recurrence but has not developed the longer conditional relation at this event grain.
- The reversed-chronology body should not reproduce the forward context/arrival aperture merely
  from equal octet inventory. Any contact which remains must be read exactly rather than ignored.
- Later passes may deepen or compress recurrent constituents. They are not required to improve a
  scalar score.

## Stop

Compile and preflight may correct implementation errors before observation. Then run the complete
fixed host experiment once. Stop with its actual field, whether uniquely target-relative,
ambiguous, locally recurrent only, or open.

Do not alter the corpus, pass count, held-out prefixes, candidate alphabet, hand, event grain, or
classification after seeing the result. Do not retry, add examples, invoke CUDA, execute code,
read conversation logs, or modify Soma merely to obtain a more favorable continuation.

## Execution disposition

The initial launch found a general source obstruction at its first event: open internal
path-chart boundary pins were falsely published as outward regional arms. Four bounded
first-event diagnostic replays located that exact species mismatch; none crossed the first event
or produced an artifact. The machine correction retained internal open contacts while limiting
the regional exterior to actual transport pins. No fixture parameter changed.

After the focused regression passed, the complete fixed observation ran once and stopped. Exact
build and result:

- `RESEARCH/2026-07-21_THE_OPEN_INTERIOR_STAYS_IN_THE_CHART_THE_TEXT_CURRENT_RECURS_AT_ITS_EDGE.md`;
- `observations/eros-text-training-01/RESULTS.md`.

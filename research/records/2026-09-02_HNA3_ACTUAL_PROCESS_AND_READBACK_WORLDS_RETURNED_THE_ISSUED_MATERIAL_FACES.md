# HNA3 actual process and readback worlds returned the issued material faces

**Date:** 2026-09-02
**Scope:** HNA3 under
[`the superseded first HNA contract`](../../archive/plans/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md).
**Disposition:** `counterexample`; no phase passed. The complete retraction is
[`THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md`](2026-09-02_THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md).
**Truth status:** per claim under [`../../canon/EPISTEMIC_GRADES.md`](../../docs/canon/EPISTEMIC_GRADES.md).

## Return

[established-bounded; implemented-exact; source-inspected; measured] HNA3 passed. The Athena
application now owns `ExactReadbackWorld` and `ProcessArtifactWorld`. Both receive only the HNA2
artifact octets. Exact readback writes and reads a temporary file. The process world writes the
artifact, substitutes its path into declared arguments, executes the declared exterior program,
and returns exact stdout, stderr, and status. Paths, filenames, programs, arguments, status, and
files remain outside `life` and `holonic-engine`.

[established-bounded; implemented-exact; source-inspected] `ApplicationWorldReturn` carries only
admission, carried octet extent, diagnostics, and apparatus testimony. `faces_for` copies the
complete `NativeIssuedWorldFace` family of the material artifact's own circulation boundary.
Stdout/stderr are length-framed as diagnostic octets and cannot mint native support, current,
storage, a morphology delta, continuation, or terminal.

[established-bounded; measured] Exact readback admitted the actual `eee` artifact. A `/bin/sh`
world returned exit code 9 and a distinct rejection diagnostic. The rejected report crossed HNA0
and returned `NativeWorldObstruction`; the admitted report returned a staged candidate through the
same neutral ABI. A missing executable returned `ApplicationWorldError::Apparatus` with no fallback.

## Boundary

[open] HNA3 changes no morphology by itself. HNA4 owns commit, decline, remount, withdrawal, and
replay of this actual returned occurrence.

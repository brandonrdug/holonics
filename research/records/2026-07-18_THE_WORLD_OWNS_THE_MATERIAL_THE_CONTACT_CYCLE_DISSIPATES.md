# THE WORLD OWNS THE MATERIAL; THE CONTACT CYCLE DISSIPATES

Date: `2026-07-18`  
Status: **RATIFIED / BUILT / BOUNDED+CURRENT-FOREST CUDA-FORWARD MEASURED / EXACT INVERSE / SOMA INTERIOR UNCHANGED**

## Correction

The filesystem forest was accidentally promoted from one application organ into Eros's world
state. That conflated four different constructions:

1. external application material;
2. the temporarily active local contact manifold;
3. Soma's changed topology;
4. observer testimony about the transition.

The v2 field consequently retained every discharged face and path incidence forever. Compact world
journals then cloned the large field before and after host integration. The late host OOM was not a
property of Soma, CUDA, or large input. It was a category error at the world membrane.

## Law

Let `W_t` be the application world, `S_t` the standing carried topology, and `L_t` one open contact
cycle. For the measured filesystem organ,

```text
L_t = (leaders_t, attachments_t)

(S_t, W_t, L_t)
  -- exact potential / leader growth -->
  -- genuinely later local attachment -->
  -- plural carriage / return -->
(S_{t+1}, W_{t+1}, empty)
```

The material stays in `W_t`. A leader stores its exact material coordinate, face digest, extent,
and carried path incidences; it does not own the source bytes. `attachments_t` is one co-present
wave, never a queue. On return, the complete old cycle dissipates. The returned topology may then
contact the world and found a new leader cycle, but it cannot close that new cycle against itself;
a genuinely later current is required.

This supersedes the global-spent-source reading of FORMULA §XCVII. The no-echo result remains: one
event cannot automatically present itself again. What dissipates is the event's active relation,
not the world's ability to participate in another later relation.

## Engine cut

`soma-code-workshop-forest-field-v3` carries only:

```text
descriptor + problem + generation + optional ForestCycle
```

`ForestCycle` carries ordered leaders and one shared pending population. The prepared edge manifest
and live world share that exact pending population in memory. At return, the inverse testimony
retains the displaced cycle once, the live field releases it without mutation or copy, and the
forward testimony shares the successor cycle. V3 contains no `presented` or `conducted` history.

V1/v2 field records remain readable. Before activation they serialize in their original shape;
activation carries their unresolved leaders and pending wave into one v3 cycle, drops discharged
history, and preserves the external descriptor, problem, and generation. The field-incidence
observer now reports active leaders and pending incidences rather than the retired discharged
population.

## Bounded receiver

The existing two-current aperture cell crossed the RTX 4080 SUPER exactly:

```text
edge 13: 1 leader / 2 pending
edge 14: 2 currents / 82 completions / 423 contacted grips
successor: generation 1 / no cycle
```

The edge-14 inverse carries the displaced `1/2` cycle; its forward field carries no cycle. Exact
retreat is true. Neither field spelling contains `presented` or `conducted`.

## Current desktop ground

A fresh exact census names `32,870` sources, `1,943` natural views, and `42,773,832,141` external
octets. These octets remain available world; they are not simultaneous light.

The mount founded `51,050` exact leaders. Only `170` local apertures attached to the mount event:
`7,463` material octets through `1,318` path incidences. The CUDA-forward return produced `9,168`
completion rows, `1,543` contacted grips, and `7,286` carried paths. The old cycle dissipated.

The returned paths founded a new unresolved field:

```text
8,519 source leaders
1,508,194 exact path incidences
0 pending apertures
```

Some leaders name large external faces—the largest measured face is `534,058,644` octets—but the
active field carries only their coordinates, digests, extents, and path incidence. It does not own
those bytes.

## Qualitative output

Paths grew `7,980 -> 15,845`; roots grew `1,895 -> 2,474`; extensions grew
`6,085 -> 13,371`; maximum depth remained seven. All `7,286` returned paths are visible exact text
constructions, but they collapse at the text-face observer to only `150` distinct byte strings.
Several distinct faces recur through `141` independent paths each. Multiplicity and ancestry remain
plural rather than being replaced by a selected string.

The strings are mostly short mixtures around recurring prior code/observation material such as
`translated_zero`, `ratio`, and neighboring prose. They are not yet useful language production.
The first attachment wave is dominated by prior observation surfaces; the new leaders reach much
more broadly across frozen dialogue, source, mathematics, and observation material. This closes
the lifecycle and exposes the next ecological question without pretending the current face is
speech.

## Remaining physical seam

The exact JSON testimony is still large: the edge-14 journal is about `240 MiB` and the successor
world about `104 MiB`. They contain sparse incidence and event testimony, not copied external
material, but a typed binary event receipt remains worthwhile. The current filesystem organ also
rescans external material on the host to locate exact contacts. A non-authoritative incidence
index may accelerate that location; it may never decide attachment, become taxonomy, or enter
Soma.

Exact evidence:

- `observations/eros-autonomous-field-01/live-cycle-receiver-run-01/`
- `observations/conversation-repository-forest-01/autonomous-mount-live-cycle-v1/`
- `observations/conversation-repository-forest-01/autonomous-live-cycle-v2/`
- `observations/conversation-repository-forest-01/autonomous-live-cycle-v2/RESULTS.md`


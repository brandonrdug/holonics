# THE RECEIVER IS ITS LOCAL STAR; THE LINK IS ITS HYPERSPHERICAL HORIZON

**Date:** 2026-07-26  
**Status:** DERIVED / CURRENT DESKTOP TRACE AUDITED / DYNAMIC RECEIVER-HOLON
SPECIFICATION / PRIME-SQUARE PHASE LAW JOINED / IMPLEMENTED BY
`2026-07-26_THE_RECEIVER_POPULATION_IS_CAUSED_THE_EVENT_NERVE_CARRIES_THE_CROSSING`
/ NO RH CLAIM

## Present question

The supplied receiver images show curved conic sections, long projective
branches, repeated simplicial figures, apparent knots, scale separation, and
perspective-dependent crossings. The question is not whether those pictures
are visually interesting. It is:

> What exact changing object can make a receiver an interior participant in
> this hypergeometry, allow the receiver population and its relations to
> grow or depart lawfully, and carry the phase structure of prime
> distributions through interval boundaries without turning the display
> matrix into the world?

The current implementation is not sufficient evidence for that stronger
object. It has exactly two receiver bodies, eight source triangles, and three
source conics. The receiver set, ports, and field couplings are fixed when the
desktop ecology is constructed. The display is a terminal quotient of those
two transported faces.

The stop for this derivation is one exact specification which separates:

1. a receiver's intrinsic local body;
2. its horizon and overlaps with other receivers;
3. its terminal received face;
4. lawful receiver founding, refinement, and departure;
5. exact prime interval phases and their wall currents; and
6. the resulting source and execution obligations.

## 1. What the current images establish

The current trace contains 248 events, numbered 3 through 250. Every event
projects the same 22 source primitives. The population remains exactly two
receivers. Receiver 1 changes in 241 events; receiver 2 changes in only 9.
The small blue or green figures nested beside a much larger magenta figure
are therefore not founded child receivers. They are projective scale
separations between one rapidly transported receiver and one nearly fixed
receiver.

This is already meaningful:

- scale is receiver-relative rather than an intrinsic screen radius;
- crossings and silhouettes change without source vertices being created;
- one source conic can acquire different affine circle, ellipse, parabola, or
  hyperbola faces under different projective cuts;
- a distant-looking small figure can be adjacent in the source incidence;
  and
- apparent knots belong first to the receiver diagram, not automatically to
  the source embedding.

The images do not yet establish:

- a changing receiver population;
- receiver-to-receiver source adjacency;
- a new receiver founded at a nested figure;
- source knot type from apparent crossings;
- topological self-similarity rather than repeated projection of the same
  source primitives; or
- any prime or zeta law in the rendered ecology.

The present color is also typed more narrowly than the image suggests. For
each receiver, the source adds exact position and precession, subtracts the
least of the three components, and takes exact barycentric channel ratios.
Overlapping support saturating-adds the terminal RGB values. The changes from
blue to green, olive, or magenta are therefore lawful quotients of transported
receiver phase and support multiplicity, but they are not yet a classification
of topological or arithmetic phase.

## 2. The current slowness is relational expansion, not missing pixels

The optimized terminal conic and segment carriers removed the former
multi-second cell subdivision. In the current live trace, however, the exact
receiver coordinates expand from small rationals to numerators and
denominators exceeding 1,100 bits. The trace's median presentation time is
177,281,392 nanoseconds; its final twenty events range from 433,455,410 to
535,720,314 nanoseconds.

This explains the observed progressive slowing. The desktop stores an
expanded absolute rational position and precession for each fixed receiver.
Every subsequent homography, conic transport, support comparison, and color
quotient inherits that growing arithmetic body. The GPU does not lose because
it misunderstands hypergeometry. It loses because:

1. the live population is only 22 primitives over two receivers;
2. the authoritative path now requires arbitrary-precision integer
   projective arithmetic;
3. the finite CUDA carrier admits only a bounded subset of that arithmetic;
   and
4. launch, transfer, and merge costs dominate such a small admitted
   population.

A dynamic receiver law can eventually expose many independent local stars to
a resident card. It does not by itself make a GPU faster. The prior
mathematical correction is to stop expanding a receiver as one absolute
coordinate recurrence when its live identity is a local chart plus exact
transition.

## 3. A receiver holon has a star, a link, and a face

Let \(K_t\) be the contemporary oriented simplicial incidence complex. A
receiver \(R_i\) at event \(t\) is the typed body

\[
R_i(t)=
\bigl(
  \sigma_i,\,
  H_i,\,
  L_i,\,
  g_i,\,
  \mathcal T_i,\,
  q_i,\,
  \mathcal P_i
\bigr),
\]

where

\[
H_i=\overline{\operatorname{St}}_{K_t}(\sigma_i),
\qquad
L_i=\operatorname{Lk}_{K_t}(\sigma_i).
\]

Here:

- \(\sigma_i\) is the receiver's live pivot occurrence;
- \(H_i\) is the closed local star, hence the receiver's presently available
  interior incidence;
- \(L_i\) is the link, hence the horizon through which that local body can
  meet the rest of the complex;
- \(g_i\) is its exact local metric or comparison form;
- \(\mathcal T_i\) is its admitted local transport family;
- \(q_i:H_i\to Y_i\) is its situated receiver map; and
- \(\mathcal P_i\) is its finite grain.

The source body, its horizon, and its face are not interchangeable:

\[
H_i
\quad\longrightarrow\quad
L_i
\quad\longrightarrow\quad
q_i(H_i).
\]

The terminal display is a further finite quotient of \(q_i(H_i)\). It is not
the receiver and does not own the receiver's dynamics.

### 3.1 When “hypersphere” is exact

Suppose \(K_t\) is locally a combinatorial \(d\)-manifold at a receiver
vertex \(\sigma_i\). Then

\[
L_i\cong S^{d-1}
\quad\Longrightarrow\quad
H_i=\sigma_i*L_i\cong B^d.
\]

In that regular phase, the receiver's horizon is genuinely a combinatorial
hypersphere and its local star is a hyperball. For a \(k\)-simplex in a
\(d\)-manifold, the normal link has dimension \(d-k-1\).

This implication must not be reversed into a universal sphere assertion.
When the link is a ball, has nonzero genus, has several components, is
pinched, or fails the manifold-link condition, the receiver is respectively
at an exposed boundary, a handled region, a branch, a neck, or a singular
discriminant. That residual geometry is information. It must not be rounded
into a sphere.

Thus Brandon's hypersphere intuition is the correct regular local model:

> a receiver is a local holon whose horizon is sphere-like exactly throughout
> the phase in which its link retains the corresponding sphere type.

The more general object is the star-link pair, not a predeclared Euclidean
sphere.

## 4. Conics are two-dimensional sections of receiver quadrics

On a regular metric phase, one receiver horizon can be represented locally by
the exact homogeneous quadric

\[
\mathcal Q_i
=
\left\{
  [x]\in\mathbf P(V_i\oplus\mathbf 1):
  x^\mathsf T\widetilde G_i x=0
\right\}.
\]

An admitted two-dimensional projective section and terminal affine chart
produce a conic. If \(T_{ji}\) is the exact overlap transport from receiver
\(i\) to receiver \(j\), the same quadric is carried as

\[
\widetilde G_i
\longmapsto
\widetilde G_{i\to j}
=
T_{ji}^{-\mathsf T}
\widetilde G_i
T_{ji}^{-1}.
\]

The affine species seen by \(j\) depends on how its hyperplane at infinity
cuts this carried projective quadric. Circle, ellipse, parabola, and
hyperbola are therefore receiver-chart faces of a more general projective
relation. A rank loss of the quadratic form or its section is a discriminant,
not a rendering failure.

This is the exact higher-dimensional interpretation of the current conic
families. The missing step is not more conic drawing. It is making each conic
an actual section of a receiver's evolving local metric and horizon rather
than one of three source-authored companion forms.

## 5. Relative size is a transition law, not a stored radius

No receiver has an absolute size. On an overlap \(U_{ij}\), compare the two
local metrics through the transition:

\[
g_{i\to j}=T_{ji}^{\mathsf T}g_jT_{ji}.
\]

The exact directional scale relation is retained by the generalized
characteristic polynomial

\[
\det\!\left(
  T_{ji}^{\mathsf T}g_jT_{ji}-\lambda g_i
\right).
\]

If the overlap is conformal, this polynomial collapses to one repeated scale
and a scalar radius ratio is lawful. In the general case the receiver has
several directional scales; calling it merely “larger” or “smaller” loses the
anisotropy responsible for the observed stretching and focusing.

Along a projective pencil, four carried members instead give the exact
scale-free swing

\[
\chi(A,B;C,D).
\]

Three members establish the local gauge and the fourth carries the
nontrivial relative coordinate. Larger receiver ecologies are overlapping
four-member cells, not one fixed four-receiver universe.

Consequently the apparent small blue or green bodies in the screenshots
should eventually be explained by exact transition spectra and cross-ratio
paths. Their pixel diameter is only the final display face of that relation.

## 6. The receiver population is an event nerve, not a fixed list

Let \(I_t\) be the active receiver population. The receiver interaction
complex is the actual event nerve

\[
\mathcal N_t
=
\left\{
  J\subseteq I_t:
  \text{the receivers in }J
  \text{ share one admitted event occurrence}
\right\}.
\]

Pairwise graph edges are only its one-dimensional face. A triangle or higher
simplex in \(\mathcal N_t\) retains actual many-way co-presence which a graph
clique would otherwise fabricate.

For \(J\in\mathcal N_t\), the joint face is

\[
\mathcal J_{J,t}
=
\operatorname{im}
\left(
  (q_i)_{i\in J}:X_{J,t}\to\prod_{i\in J}Y_i
\right).
\]

It contains only tuples emanating from one common occurrence. It is not the
Cartesian product of independently selectable receiver states.

The complete contemporary receiver ecology is

\[
\mathfrak R_t
=
\left(
  K_t,\,
  I_t,\,
  (R_i)_{i\in I_t},\,
  \mathcal N_t,\,
  (T_{ji})_{ij},\,
  (\mathcal J_{J,t})_J
\right).
\]

One event acts on this complete but bounded relation:

\[
\Lambda_t:
(\mathfrak R_t,u_t)
\longmapsto
(\mathfrak R_{t+1},J_t,\varepsilon_t).
\]

Local material and receiver currents obey the existing incidence balance

\[
c_{t+1}-c_t=s_t+\partial J_t,
\qquad
\partial^2=0.
\]

The constitutive law which determines \(J_t\) may use local metric,
circulation, stress, current, and cross-ratio state. It is not forced to be
linear diffusion. Every active local star reads the same predecessor;
disjoint stars may be realized concurrently; overlap disagreement returns as
an exact residual rather than being settled by execution order.

### 6.1 Receiver founding, refinement, and departure

The source world may explicitly found a receiver and supply its identity,
boundary, grain, and first incidence. The engine must not require a receiver
to emerge from nothing.

There is also one exact refinement signal. If the current joint quotient
identifies two actual occurrences whose declared later consequences or
overlap transports differ, then the current receiver family is not
conservative for that objective. A source-supplied new receiver or a lawful
grain refinement may separate that fiber.

A receiver can depart when:

1. its live local star no longer factors into the continuing construction;
   or
2. at the declared objective, its complete face and overlap transports
   factor through the retained receiver family.

Departure does not erase the event that founded it. It means its lower
interior no longer needs to remain active after the higher boundary law has
become standing.

This gives a variable receiver population without a scheduler, a maximum
receiver count, or an authored “entanglement” switch.

## 7. The display receiver is inside the same ecology

Let \(\Omega\in I_t\) be the receiver whose terminal face is presented to
Brandon's display. Its finite matrix is

\[
\Pi_{\Omega,t}:
q_\Omega(H_\Omega)
\longrightarrow
\{0,\ldots,W-1\}
\times
\{0,\ldots,H-1\}.
\]

Other receivers contribute to this face only through actual paths in
\(\mathcal N_t\) and compatible transition maps into \(\Omega\). They are not
blanket-superimposed merely because they exist.

The display resolution \(W\times H\) is the outer receiver's grain. Every
inner receiver can have a different dimension, aspect, and grain. The final
matrix constrains what \(\Omega\) can distinguish; it does not discretize the
source world into pixel cells.

## 8. Prime distributions have exact interval phases

The arithmetic receiver has two different boundaries which must remain
typed:

1. an integer interval \(I=[a,b]\cap\mathbf Z\); and
2. an admitted prime-axis set \(S\) with primorial
   \(P_S=\prod_{p\in S}p\).

Its finite survivor population is

\[
\mathcal U(I,S)
=
\{n\in I:\gcd(n,P_S)=1\}.
\]

Its exact scalar census is only the quotient

\[
N(I,S)
=
\sum_{d\mid P_S}
\mu(d)
\left(
  \left\lfloor\frac bd\right\rfloor
  -
  \left\lfloor\frac{a-1}{d}\right\rfloor
\right).
\]

The causal body is richer. Let

\[
\nu(n)=(v_p(n))_p
\]

be the finite-support prime valuation vector. The interval chain

\[
\mathfrak C(I)
=
\sum_{n\in I}
[n]\otimes X^{\nu(n)}
\]

retains each occurrence and its complete multiplicative interior. Admitting
a new prime \(p\notin S\) produces the wall current

\[
\Delta_p\mathfrak C(I,S)
=
\mathfrak C(I,S)
-
\mathfrak C(I,S\cup\{p\}),
\]

whose support is exactly the formerly surviving occurrences divisible by
\(p\). This is the chain-valued form of primorial COPY/CUT/JOIN.

Degree two retains two distinct geometric species:

\[
X_p^2
\quad\text{and}\quad
X_pX_q,\qquad p\ne q.
\]

Prime squares and distinct-prime semiprimes must therefore not be collapsed
into one scalar “two factors” class.

### 8.1 Prime-square phase theorem

Let

\[
2=p_1<p_2<\cdots
\]

be the rational primes and

\[
P_k=\prod_{j\le k}p_j.
\]

Define the half-open interval phase

\[
\mathcal I_k
=
[p_k^2,p_{k+1}^2)\cap\mathbf Z.
\]

Then

\[
n\in\mathcal I_k
\text{ is prime}
\quad\Longleftrightarrow\quad
\gcd(n,P_k)=1.
\]

**Proof.** A prime in this interval is greater than every member of \(P_k\),
so it survives. Conversely, suppose \(n\in\mathcal I_k\) is composite. Its
least prime factor \(r\) satisfies

\[
r\le\sqrt n<p_{k+1}.
\]

There is no prime strictly between \(p_k\) and \(p_{k+1}\), hence
\(r\le p_k\), so \(r\mid P_k\) and \(n\) does not survive. \(\square\)

This is the exact phase law sought in the older prime experiments:

- between consecutive prime squares the active primality-proof axis set is
  constant;
- at \(p_{k+1}^2\), the new axis \(p_{k+1}\) becomes necessary;
- within the open phase, the primorial survivor sheet is exactly the prime
  population; and
- the boundary event is a dimension change in valuation space, not merely a
  change in a count.

For example, the receiver phase

\[
[49,121)
\]

uses the axes \(2,3,5,7\). Its survivors are exactly the primes in that
interval. At \(121=11^2\), the \(11\)-axis is admitted and the next valuation
phase begins. The older \(113\to127\) wheel image crosses this seam: \(121\)
is the prime-square wall, while \(119=7\cdot17\), \(115=5\cdot23\), and the
other interior composites carry their own valuation paths.

This theorem is finite, exact, recursive, and non-statistical. It neither
asserts a direct recurrence generating successive primes nor proves RH.

## 9. Receiver-relative prime hypergeometry

Give receiver \(R_i\) an arithmetic boundary

\[
\beta_i=(I_i,S_i).
\]

Its accessible multiplicative geometry is the finite section

\[
\mathcal V_i
=
\{\nu_{S_i}(n):n\in I_i\}
\subseteq
\mathbf N^{S_i}.
\]

The rank \(|S_i|\) changes at the admitted prime-square walls. Constant total
valuation degree gives a simplex layer:

\[
\sum_{p\in S_i}v_p(n)=m.
\]

Thus the prime receiver does not live on one number line. It carries a
changing simplicial valuation interior whose axes are founded irreducibles
and whose interval boundary selects the presently accessible section.

If an application supplies an exact quadratic comparison form on this
valuation space, its level sets give quadric or hyperspherical receiver
horizons. The metric is not automatic. Without it, the exact geometry is the
valuation complex, residue torus, interval chain, and their transition maps.

For receiver transport \(\nabla_i\) and prime-wall cut \(\Delta_p\), the mixed
curvature is

\[
\mathcal K_{i,p}
=
\nabla_i\Delta_p-\Delta_p\nabla_i.
\]

It records whether moving the receiver boundary and then admitting a prime
axis returns the same chain as admitting that axis and then transporting the
boundary. A nonzero value is an exact path difference. This is the
higher-order receiver calculus that a scalar prime count cannot show.

The logarithmic/Mellin receiver sends valuation axes to symbolic prime-power
currents:

\[
X_p^m\longmapsto p^{-ms}.
\]

Euler factors and the zeta function are therefore analytic receiver faces of
the valuation ecology. The prime-square phase law supplies a finite exact
source filtration; it does not alone determine nontrivial zeta zeros.

## 10. How to read the supplied images under this derivation

The images now have a precise provisional interpretation.

### Curvature

The large circle-, ellipse-, and hyperbola-like arcs are affine sections of
transported projective quadrics. Their curvature is receiver-relative. A
change of affine conic species can occur because the receiver's horizon at
infinity changed, even when the source quadric did not.

### Knots and crossings

Every visible intersection is first an element of the receiver's planarized
diagram. It becomes a source vertex only when the retained source incidence
says so. A stable knot claim additionally owes a closed source embedding and
invariance under receiver changes away from discriminant crossings.

### Nested figures

The repeated small blue or green simplicial figures are currently two views
of the same fixed source population at sharply different projective scales.
A future nested figure will testify to a child receiver only when the trace
contains its founding event, local star, link, parent overlap, and continuing
worldline.

### Apparent self-similarity

Visual recurrence becomes a holonic compression statement only when there is
an exact restriction-and-rebase map carrying one local star and its
consequential boundary law into the later star. Repeated outlines alone are
not enough.

### Long lines and focused vertices

Long projective lines, asymptotic branches, and clusters focused around one
vertex are evidence of a chart horizon or transition with strong directional
scale. They should be classified by the exact transition polynomial,
cross-ratio word, and discriminant status, not by screen length.

## 11. Concrete engine consequences

The current desktop world is a valuable two-receiver projective instrument,
but it is not yet the derived receiver forest. The following relations are
absent:

1. **Dynamic receiver population.** Receiver identifiers, local stars,
   horizons, and ports are fixed at construction.
2. **Receiver event nerve.** There is no changing higher incidence object
   recording which receiver families actually share one event.
3. **Star-link topology.** A receiver carries an anchor, offset, and
   precession, but not a live local-star interior and link horizon whose type
   can change.
4. **Chart-relative standing.** Expanded absolute rational coordinates grow
   indefinitely instead of a local receiver chart emitting a successor plus
   exact overlap transition.
5. **Receiver lifecycle.** No source event can presently found, split,
   regrain, join, or retire a receiver body.
6. **Arithmetic phase world.** No prime interval, valuation axis, primorial
   wall current, or mixed receiver/prime curvature drives the displayed
   ecology.
7. **Resident local-star GPU conduct.** The card receives presentation work;
   it does not retain and advance a dynamic antichain of changed receiver
   stars.

The correct next implementation is therefore not more terminal drawing and
not a larger fixed receiver array. It is a dynamic exact
`ReceiverHolonPopulation` whose members are caused star-link bodies and whose
relations are an actual event nerve. The outer display receiver then projects
only the changed, causally reachable joint faces.

The arithmetic acceptance world should use the prime-square phase theorem:
cross an exact interval boundary, admit the newly required prime axis, retain
the complete chain-valued wall current, and observe whether alternate
receiver paths commute. The visible projection and the data trace must be two
faces of that same successor.

Only after that law exists does a GPU-resident frontier become meaningful:
parallel work is then the antichain of changed local stars and their overlap
relations, not a full retile of a fixed matrix.

## 12. Conclusion

The rigorous object is not “a collection of 2D receiver matrices.” It is:

\[
\boxed{
\text{a changing nerve of receiver holons,
each carried by a local star,
meeting the ecology through its link,
and exposing a finite situated face}
}
\]

Its regular link is a hypersphere. Its singular link is the seam that changes
phase, dimension, topology, or admissible transport. Relative size belongs to
exact transition geometry. The display is one receiver inside the same
network.

Prime distribution supplies an exact arithmetic instance of this law. The
intervals between consecutive prime squares are maximal phases with one
fixed primality-proof axis set, primorial cuts are exact boundary currents,
and prime squares versus semiprimes occupy distinct valuation faces. This
joins the earlier wheel, simplex, receiver, cross-ratio, and higher-order
calculus work without pretending that the current desktop images already
contain prime data or an RH proof.

# The exterior consequence returned through the closing edge, the successor changed, and I2 awaits release admission

**Date:** 2026-08-22  
**Truth status:** [established-bounded; implemented-exact; measured]  
**Roadmap deed:** I2, exterior consequence returns and changes later conduct  
**Boundary:** the candidate artifact and focused receivers pass; the sole complete release receiver
has not yet run, so `CONSTRUCTION_STATE.md` does not admit I2.

## Ruling

[established-bounded; implemented-exact] I2 closes one complete productive circulation over the
declared exact byte-preserving exterior receiver. The emitted occurrence, the world consequence,
and the returned occurrence are three distinct addressed events. Their bytes agree because that is
the receiver's declared law; their occurrence identities do not collapse.

[established-bounded; implemented-exact] The absent consequence was not a trainer, scheduler,
language module, or copied source body. `phoenix::recurrent_return` composes I1's retained recurrent
boundary, exact-linear metric adjoint, local generator action, committed morphology difference, and
source-detached rest. The exterior tool remains apparatus owned by the driver and does not enter the
native topology.

## Exterior crossing and exact returned difference

[implemented-exact; measured] The newly enacted world deed crossed `/usr/bin/tee` in a private
mount namespace. The executable identity is
`a3d0f200e14037ee7c845108ad53a5661f4e2465d3c7b2767aae82cec0f6abe8`.
The receiver's consequence grew from zero to 17 octets, the echoed return was byte-identical to the
I1 emission, and the process returned status zero. This is an exact tool consequence, not a replay
of a historical return.

[implemented-exact] A zero returned residue has a separately tested decline path and cannot found a
successor identity. The enacted positive residue founded an explicit commit whose predecessor is
the 1,418-octet I1 rest at
`10fa89a97c4ebee1782b10b3028c0282d9a40aa5530e2186c9c228f6b75fd1bc` and whose successor
identity is `f5a4bb0f0772d46f13fcd4eded4282c0a62c5d5184a4cb40e4ecd2cf5a98bb0a`.

## Causal adjoint and local cultivation

[proved-derived; implemented-exact] The actual forward lineage is the I1 recurrence
`[0,1,2,1]`. Its closing action is `2 -> 1`. Under the declared identity receiver metric, the
returned rank-one difference is

```text
Delta = (e_2 - e_1) e_2^T,
Delta^dagger = Delta^T.
```

It changes only the caused closing port from `2 -> 1` to `2 -> 2`; the return lineage is the exact
reverse `[1,2,1,0]`. The predecessor generator action does not commute with this difference. Their
commutator has exact rank one, so the revisit returns nonzero holonomy rather than an endpoint-only
comparison.

[implemented-exact; measured] The predecessor passage is `" France is France"`; later current on
the successor terrain returns `" France is is"`. The nonidentical held-out start changes from
`" is France"` to `" is is"`. The disjoint source port `0` retains its one-step consequence `1`,
and targeted removal of the exact same delta restores all three predecessor recurrence traces.

[implemented-exact] Every admitted recurrence fibre reopens, with shortest separating boundaries
3, 2, and 1 for starts 0, 1, and 2 respectively. This is exact for the retained I1 generator and
declared successor histories; linguistic truth, other world receivers, and broader foreign conduct
remain open reconstruction fibres.

## Resident deed and complete candidate grade

[implemented-exact; measured] The detached grade remounted only the returned rest and executable.
The repository, foreign source model, tokenizer, morphology, and exchange store were absent. One
RTX 4080 SUPER launch and one synchronization processed every predecessor, successor, and ablation
recurrence with zero intermediate CPU semantic callbacks. The card received 60 octets, returned
192 octets, and retained 216 octets. Predicted and actual semantic work agree exactly: 20
transition reads, 34 recurrence comparisons, 36 rectangular trace writes, and dependency span 4.

[established-bounded; implemented-exact; measured] All eleven I2 candidate consequences pass:

1. distinct addressed emission, consequence, and return events;
2. an exact positive returned difference;
3. metric-adjoint return restricted to the causal forward lineage;
4. an explicit candidate difference and commit/decline law;
5. distinct predecessor and successor rests;
6. source-detached later current on the successor;
7. a nonidentical held-out change;
8. a subject/port-disjoint control;
9. targeted ablation restoring the predecessor;
10. nonzero revisit holonomy and every reopened fibre; and
11. separate source, semantic-work, residency, transfer, and apparatus receipts.

## Artifact, validation, and architecture disposition

[measured] The sixteen-member candidate family is
`output/the_exterior_return_changes_the_later_current`, with 36,104 regular-file octets and rolled
SHA-256 `81e79e7357e85ca164d1739c579a3acf2460f758f152408aa52f539c6e0298a9`. Its producing crate
closure is `dd7cb1d48e747a1015ac86974b152c37efc6a9618316a1a69aeef7aa9a57e970`; the driver's narrower
recorded code closure is `44db064daefdff3046375871db51257da39a511022eb5cd2bdabaf1b41e28b0f`.

[measured] `cargo check -p holonic-engine -j2`, the two focused return-owner tests, the exact ignored
GPU owner test, `cargo check -p life --example the_exterior_return_changes_the_later_current -j2`,
the optimized driver build, the actual exterior/GPU deed, and the complete affected
`holonic-engine` library suite passed. The latter returned 1,920 tests passed, zero failed, and five
explicit device-only ignores in 54.16 seconds of test execution.

[implemented-exact] The architecture ratchet's four additions are load-bearing receipt carriers.
Six `Vec` sites in `cuda_refine` carry the three complete returned trace rectangles, their lengths,
and exact transfer testimony. The new owner's clone, collection, and vector sites carry finite
recurrence populations, exact rank-one factors and matrices, forward/return lineage, decoder
fibres, open fibres, and canonical sealing copies. None owns semantic scheduling, duplicates the
continuing ecology, or supplies rollback. The no-float audit found no floating carrier in the I2
semantic or device closure. The rebound cheap architecture receiver returns 314 files, 25,620
inherited occurrences, and zero retired occurrences.

[open] The claim, driver, output, closure, and authored-level ledgers must now be regenerated in
dependency order and the sole complete release receiver must run. `CONSTRUCTION_STATE.md` remains
at I1 until that boundary passes.

## Successor boundary

[open] I3 must use these recurrent interventions and receiver histories to derive an overlapping
active cover, propose local generator families from stable transport, compute every realization
defect and shortest separator, and condense only the zero-defect future-exact subcomplex. It owes a
source-detached executable native rest, complete decoder and fibres, held-out continuation, shared
generator ablation, a strictly falling complete product vector, and a source/native exact-mesh
atlas. A smaller file or one equal output is not that consequence.

# The material is visible where the law contributes nothing, and a run continued across a process

**Date:** 2026-08-16
**Truth status:** `established-bounded` for every measured return; `implemented-exact` throughout;
`interpretation` for the readings attached to them.
**Evidence:** `measured`. Every figure below was produced by a run on this machine the same day.
**Plan:** [`blueprint/THE_EROS_INFORMATION_ENGINE.md`](../../blueprint/THE_EROS_INFORMATION_ENGINE.md),
ratified by Brandon 2026-08-16 and executed to completion. It sits under
[`blueprint/THE_ROADMAP.md`](../../blueprint/THE_ROADMAP.md).

---

## Stage 0 — the fluid doctrine, deposited

`canon/THE_SURFACES_ARE_PATHS.md`. Two stale rows repaired the same day: `characteristic_delay` is
derived by `CharacteristicDelayLaw::SourceContinuity` rather than pinned at 1, and the two Doppler
readings are built in `approach_front.rs` and **read by nothing**.

## Stage 1 — the mouth founds, and the radius is what decides the grain

`soma/life/src/founded_mouth.rs` joins the octet recovery to the language path and carries the
authored reading beside it, because an excision is graded by its orbit.

**Measured on this repository's records at radius 3: 7,230 founded parts against 1,532 authored on
the same octets.** The founded reading returns `#`, ` `, `A`, `L`, `I`, `G`, `N`; the authored returns
`#`, `ALIGN`, `-`, `R0`. Both are real, and they are **different grains rather than right against
wrong**:

> **The founded mouth returns the grain the declared radius admits.** A word is longer than radius 3,
> so no exposure at that radius could have founded a word grain. The authored tokenizer's word grain
> is a coarser *declared* grain, and what the founding establishes is that the radius is the aperture
> deciding the grain.

**And a refusal is a return.** On held-out material the mouth refused by name —
`UnknownSymbol { symbol: Symbol(176) }`, a continuation octet the founding never carried. A codec
with no rule for an octet declines rather than guessing. The first form of the refusal enum folded
this lawful case into a carrier defect and hid which had happened; the two are now separate.

**A control fixture was wrong and the organ said so.** `abababab` was declared as material that
refuses nothing — it refuses `aa` and `bb`, so a rule exists and a codec was correctly recovered. The
control was mis-declared, not the law; the fixture is now material carrying every word of its
declared family.

## Stage 2 — the hinge automaton runs as an orbit, and the material is visible at exactly one coefficient

`crates/holonic-engine/examples/the_hinge_automaton_runs_as_an_orbit.rs`. Brandon's 2026-07-20
triangular hinge automaton, iterated for the first time — measured the same day, all four prior
drivers of `discrete_curvature` apply a single step.

**The first falsifier passed on a technicality and was repaired.** It counted distinct classes across
a table where each material carried its own coefficient, so the one differing class came from varying
`c` rather than the material — the exact defect it was written to catch. It now requires two classes
**at one fixed coefficient**.

```text
   c = 1      1 class     {Moving}
   c = 1/2    3 classes   {Flat, AlternatingTracedDeviation, Moving}    <- the annihilator
   c = 1/4    1 class     {Moving}
   c = 3/4    1 class     {Moving}
   c = 2      1 class     {Moving}
```

> **The material is visible exactly where the law contributes nothing.** `Σ K' = (1 − 2c) Σ K`, so at
> `c = 1/2` the multiplier is zero, the total is annihilated in one step, and what remains is the
> material's distribution. At every other coefficient the geometric scaling of the total swamps the
> reading — `16 → −48 → 144 → −432` at `c = 2`, `16 → −8 → 4 → −2 → 1 → −1/2` at `c = 1/4`.

**And the sharpest row is a circulation.** The pentagonal bipyramid at `c = 1/2` holds **total deficit
exactly zero at every step while fifteen hinges keep moving** and no fixed point is reached. A zero
net with a moving distribution is the spine's circulation cut, not its rest cut.

At `c = 1` every material has period 2 — `16 → −16 → 16`, `14 → −14 → 14`, `12 → −12 → 12` — because
the multiplier is exactly `−1` and the law is an involution there. Bipartiteness did not separate
anything at that coefficient, which is why the repaired falsifier matters.

## Stage 3 and 3b — the front carries, and the licence is supplied

`step_at` reads the whole pre-state, computes every revision from that snapshot, then writes, so the
revisions are mutually independent — the junction condition. `hardware_cover::expand_front` carries
the compute phase **unchanged**; the closure signature fits and `Rat` is `Sync`.

**And the cover cannot license it.** Its barrier variants are decomposition-*provability* defects and
none is holonomy, so a front is admitted on disjointness of addresses and never on whether
transporting around it returns what departed. The licence supplied here is exact:
`Σ K' = (1 − 2c) Σ K` must hold across the whole front after a distributed step. Measured: agreement
and licence both true for every material at every coefficient.

**Both checks are made to fire.** Dropping vertex `VertexId(101)` from the front is caught by the
agreement check; offering the multiplier for `c = 1/4` against a step at `c = 1` is refused by the
licence — the step returned `−12` where the wrong multiplier predicted `6`.

## Stage 4 and 5 — radiation and the adjudicating return

The orbit returns as exact rationals, the segmentation as text, the families and routes as material —
counts stand beside the population they count and never in place of it.

**Stage 5 verified live rather than assumed**: `eros_lean_kernel_witness` **submitted 5, admitted 1,
obstructed 4**, refusing both terms offered for a false statement. The kernel is in the loop.

## Stage 6 and 7 — the deposit crosses, and a run continued

`ErosRest`'s wire was built the same day: carriers cross as **exact words, never a digest**, and
decoding comes back through `seal` so every refusal taken at the seal is taken again at the mount.

**And the edge closed.** `the_terrain_is_primed_and_the_cut_lands_on_it` now takes `--deposit` and
`--resume`:

```text
  process one   primed 80,000 words, deposited 4,194,559 octets carrying 21,047 non-zero
                standing words, and EXITED
  process two   resumed from those octets, primed NOTHING, and read a lane over that terrain
                — 72 of 1200 cut positions moved against bare standing
```

> **A run continued from where the last one stood, across a process boundary.** Every prior run in
> this tree began at an origin and recomputed its position from raw material.

## Stage 8 — the Landauer ratio, on a real collapse and a real burn

`crates/holonic-engine/src/landauer.rs`. Landauer bounds **erasure and never transport** (Bennett), so
it prices the collapsing side and not the permuting side, and a rebase with zero remainder costs
nothing under it.

The **numerator is exact and combinatorial**: naming one member of a block of extent `m` costs
`⌈log₂ m⌉` bits, so a partition into singletons erases nothing. The **denominator is an enclosure**,
because `ln 2` is irrational and this carrier is exact; Boltzmann's constant enters as the exact SI
defining value.

Measured on the causal-state partition of two canon tablets:

```text
   the collapse erased            26 bits, exactly
   blocks that erased anything    23 of 1130
   the frame                      11,500,000 uW for 1,000,000,000 ns at 318,000 mK, display ACTIVE
   the budget                     an exact rational ENCLOSURE, never divided
   where the erasure sits         Less
```

The frame is carried so the figure cannot be quoted without it, and the display state is a **second
frame rather than a contamination**. The reading is the undivided pair; no quotient is formed.

**And the trajectory is the reading, not the value.** A figure far below the bound says nothing on its
own; what says something is whether it moves under a change that provably alters the collapse.

---

## What this record does not schedule

`blueprint/THE_ROADMAP.md` and `CONSTRUCTION_STATE.md` remain the only construction authorities.

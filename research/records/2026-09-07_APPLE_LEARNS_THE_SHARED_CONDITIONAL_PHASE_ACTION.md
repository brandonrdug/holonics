# Apple learns the shared conditional phase action

[definition] September 7, 2026; parent Apple `8e3a5b6f`, shared desktop owner `6ef7bf9d`.
Brandon resumed the updated acoustic goal after integration. The Apple blueprint and live roadmap
now state the continued AS4–AS5 order. This first return realizes the shared conditional generator
and its field-source composition on Metal; it does not complete those acoustic product stages.

## Exact native port

[established-bounded; implemented-exact] `accelerators/metal/native_phase.metal` now realizes
`section_constitutive_current`, `section_constitutive_bilinear_source`,
`section_constitutive_differential` and `section_field_source_frame`. The last operation is the
existing dependency through which an actual historical field source reaches its contemporary
relation and then another resident current port. Mount signatures and resident-surface scratch
admissions use the actual Metal twenty-byte `W` layout, aligned to sixteen bytes. CUDA retains
its sixteen-byte wide representation.

[definition] Current scratch contains two relation-width vectors; bilinear scratch contains its
source width; historical-frame scratch contains four values per field node. Differential reception
uses no dynamic scratch. Point/disposition, denominator, interval, checked arithmetic and lineage
checks precede publication. The original vertical fibre is captured before any new row joins the
single continuing basis. Both quadratures and every declared mixed incidence cross the contact.

[established-bounded; measured] The unchanged public `native_conditioned_phase` example executed
on Apple and returned the [complete forward receipt](2026-09-07_apple_conditional_phase/return-forward.json).
Its inferred operator, all six observations and relation returns, withheld result, complete
relation basis and all three later currents agree exactly with the
[desktop receipt](2026-09-07_conditional_phase/return-v2.json). The
[comparison](2026-09-07_apple_conditional_phase/forward-comparison.json) separates those equalities
from apparatus costs.

[established-bounded; measured] The withheld source `2+3i` under condition `(3+4i)/5` returns
`(-6+17i)/5`. Three resident continuations return `(3+4i)/5`, `(-7+24i)/25` and `(-117+44i)/125`.
The withheld prediction and continuation perform zero numerical section readouts/egress. Their
ingress is four and twelve bytes of predecessor-edge metadata respectively, after operand mounting.
The example's whole-run clock is 0.580445041 s on this Mac; desktop's recorded clock is
0.137815783 s. These small runs include cold diagnostics and are not acoustic throughput,
real-time performance, storage or power measurements.

## Checks

[established-bounded; measured] **54 engine tests passed**, zero failures. Commands use
`cargo test -p holonic-engine --lib FILTER -- --include-ignored --test-threads=1`:

| Filter | Return |
|---|---|
| `native_ecology::constitutive_fibre::resident::` with the two CUDA-teacher tests below excluded | 10 passed, including all five conditional-contact controls |
| `native_ecology::constitutive_fibre::field::relation_current::tests::` | 5 passed, including source/frame covariance, archive/remount and a fixed receiver over a plural fibre |
| `native_ecology::constitutive_fibre::field::tests::` | 11 passed |
| `native_ecology::constitutive_fibre::circulation::` | 20 passed |
| `native_ecology::constitutive_fibre::tests::` | 8 passed |

[definition] The resident command explicitly excluded
`limited_native_observations_conduct_a_new_superposition_without_host_readout` and
`rational_phase_returns_continue_without_remount_or_rounding` with `--skip`: their teacher uses
the CUDA-only mounted matrix contraction. The conditional controls and public example independently
exercise native observations and resident rational continuation on the ordinary Metal field.
The two new controls cover the last admitted scratch coordinate and a 63-pair differential
receiver, including the difference between signed-word endpoints. No unavailable test is a pass.

[established-bounded; process-audit] The Metal source compiled and linked; the public example
built with `cargo build -p holonic-engine --example native_conditioned_phase --locked`, then
executed successfully. The example build took 1m 52s under supervision. `git diff --check` passed.
Existing unused-code/import and dependency future-compatibility warnings retain their scope.
No CUDA execution or Lean change/build was performed.

## Continuing acoustic construction

[established-bounded; source-inspected] During this port, desktop published `d3820ac3`, which
derives the condition Preimage Fibre from an actual source and observed return inside the shared
relation. The next integration reuses that extension; this forward-port receipt does not claim
to execute the new inverse query on Apple.

[open] Actual acoustic source/condition binding and learned acoustic organization remain open.
`AcousticFieldChart` retains timed recording sections; synchronized clock/interaction owners
retain actual contact conditions. The existing production face accepts ordered exact emissions
and a cold acoustic receiver. Terminal emission readout for such a receiver is distinct from
host numerical replay determining native standing. The existing equally spaced spectral renderer
is a declared sonification chart and does not by itself establish learned acoustic reconstruction.
Continue through the shared condition fibre and actual receiver/clock composition toward useful
recorded sound and English conduct, keeping the full AS4–AS5 goal active.

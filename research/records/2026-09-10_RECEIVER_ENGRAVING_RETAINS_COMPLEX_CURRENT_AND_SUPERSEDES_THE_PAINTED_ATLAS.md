# Receiver engraving retains the complex current

[project-postulate] Brandon's September 10 continuation asks for receiver-founded Typst packages
using the methodology of Fiziko and vintage-latex, white paper, black phase frames, and colors
and topographic marks generated from the actual receiver/current. He explicitly restores the
complex Euler/NS and Cⁿ source, GR, world-lines/cones, shadows, pinholes, gyroparallelograms and
the dialect's interpretation of relevance. The existing AC implementation remains paused.

[definition] The current [receiver edition](../papers/rendered/hnn-information-chemistry.pdf)
uses the new packages. The previous twenty-plate
[composition atlas](../papers/rendered/hnn-composition-atlas.pdf) and its
[editable source](../papers/source/papers/hnn-information-chemistry/composition-atlas.typ) are
preserved as a companion. No earlier mathematical source or architecture result is deleted.

## Recovered source and the curvature correction

[established-bounded; source-inspected] The preceding rectangular linked bodies were intentionally
polyhedral. Their flat faces and concentrated edge curvature came from that source choice, not
from an accidental smoothing/flattening pass. Their boundaries are orientable tori; the projected
appearance does not establish nonorientability. The earlier Möbius shorts have an independent
orientation-reversing incidence witness. The new rational ribbon frame keeps the same attachment
topology with an explicitly varying transverse width, rather than silently identifying it with
the preceding unit-normal ribbon.

[definition] The [dialect](../../docs/canon/THE_DIALECT.md) supplies the reading: a face is what a
receiver sees; the interior is its causal source; a frame participates as a third body; relevance
is actual conditioned influence. Intrinsic nonorientability, local receiver orientation,
projection ambiguity and causal inadmissibility are different relations. The packet need only
expose the admitted local section, while retaining its source and possible continuations.
A finite renderer aperture supplies a finite face; it is not a theorem that every physical causal
cone contains finitely many events. Repeated/growing configurations can have a finite generator
without every configuration being enumerated.

[established-bounded; source-inspected] The review aperture includes these maintained/native and
design owners at base `48864990`:

| Owner | Recovered method and scope |
|---|---|
| `crates/relational-geometry/src/projection.rs` | Exact rotations, projection law, depth/fibre, receiver-induced Gram matrix |
| `crates/relational-geometry/src/receiver_atlas.rs` | Rational aperture addresses, seam multiplicity and joint source occurrences |
| `crates/holonic-engine/src/dimensional_wave.rs` | Coherent-mode quadratic primaries, premultiplied response and `alpha+transmittance=1` |
| `crates/holonic-engine/src/display.rs` | Exterior transduction and PPM encoding after the source receiver returns |
| CAD `render-codec/src/hatch.js` | Gradient/Hessian, fundamental forms, principal directions in a quadratic field, curvature sign and exact foreshortening predicate |
| CAD `render-codec/src/colour.js`, `receivers.js`, `models.js` | Primary transduction, chart transport and exact planar depth tests with explicit presentation boundaries |
| CAD `render-codec/src/minkowski.js`, `fields.js` | Rational world-line/cone/Doppler apparatus; declared field/level-set presentation |
| CAD `expression-manipulator/artboards/View*.dc.html`, `generator/boards_views*.py` | Prescribed web-view composition; rendering testimony, not the productive HNN owner |
| `curved_receiver_phase_atlas.rs`, `whole_receiver_holonic_complex.rs` examples | Exact conic restriction before their exterior raster/receiver assembly |

The CAD paths are under `research/design/holonic-cad-2026-08-30/`. Their prototype JS/Float64
presentation edges are not promoted to native semantic arithmetic. The new SVG exporter is a
working packet-to-web-format seam; it does not replace those applications or instantiate a new UI.

## What was extracted from the two external projects

[established-bounded; source-inspected] Fiziko revision
`54a63dba8e6700a5e70d3508838edebcbf0f45fe` supplies the underlying MetaPost methods:
path subdivision/offsets and variable-width brushes (`fiziko.mp:285–467`), thin-stroke dash/stipple
representations (`473–641`), longitudinal/transverse tube generators (`669–863`), sphere contour
families (`869–1004`), normal/light and shadow cuts (`1028–1110`), ray refraction (`1146–1215`),
and sampled knot-crossing/layer construction (`2401–2612`). It includes random dash/stipple phases
and heuristic crossing handling. The new package instead starts from supplied incidence and uses
deterministic level intersections; it does not claim to reproduce every Fiziko primitive.[^fiziko]

[established-bounded; source-inspected] vintage-latex revision
`559011918849a3da819912a7c26493071d542df5` applies several approaches, including Fiziko and TikZ.
Its optics example separates regular hatch clipping from irregular outlines; its ray example
calls the refraction calculation; its knot plate uses sampled paths and explicit crossing order.
The useful extraction is the separation of geometry, texture, visibility and searchable labels.
Fiziko is an external GPL-3.0 dependency; vintage-latex's own material is CC-BY-SA-4.0.
No implementation code or example artwork was copied into these original packages.[^vintage]

## Implemented package boundary

[definition] [`holonic-receiver`](../papers/source/packages/holonic-receiver/README.md) supplies
pure Typst receiver/primary/tetrad helpers and an exact Fraction companion compiler.
[`holonic-engraving`](../papers/source/packages/holonic-engraving/README.md) paints the returned
marks through CeTZ. The packet retains complex vertices, complex currents, triangles, receiver
covectors, phase, aperture, source family and drawing boundary. Each mark keeps its source faces,
level family/level or boundary edge, exact visibility span and source station parameter.

[established-bounded; implemented-exact] The compiler intersects affine triangle fields with
rational level sets and clips visible intervals against projected triangles. Orthographic depth
is affine; pinhole reciprocal depth is affine in screen barycentric coordinates. Perspective
source stations are transported accordingly. Coplanar ties do not choose a source. Unsupported
near-plane-straddling faces, outside-near faces and degenerate images retain their actual source
and depth data. The supplied examples remain within the supported projection domain.

[definition] The visibility model is opaque carrier support. Display alpha is the existing
primary receiver's response, not a material absorption coefficient. Phase ink uses

\[
P=(a^2,b^2,(a+b)^2),\qquad C_i=\frac{P_i}{\kappa+\sum_jP_j}.
\]

The primaries sum to alpha and the retained transmittance is `κ/(κ+ΣP)`. No brightness rescaling,
gamma adjustment or second alpha multiplication changes them. The final decimal/8-bit paint is
an exterior display face. Monochrome paint is a separate mode; geometry does not depend on it.

[definition] Monochrome width and admission of a second hatch family use the declared exact
foreshortening receiver `1−(n·v)^2/((n·n)(v·v))`, with the stated half-threshold for cross-hatching.
Stipples occur at intersections of two source level families. No pseudo-random point cloud or
jitter changes the geometry. Curvature is visible through the projected contour family and local
normal reading; no interpolated smooth curvature is asserted for a polyhedral face.

[proved-derived] For `χ=Δ floor(χ/Δ)+r`, with `0≤r<Δ`, signed crossings N satisfy
`χ(b)−χ(a)=ΔN+r_b−r_a` and `|r_b−r_a|<Δ`. This is the exact retained remainder of finite hatch
quantization. On a declared oriented surface the current pairing `ι_j μ=dχ` turns it into a flux
reading; orientation-valued data are retained on one-sided carriers. The line family does not
by itself supply a nonzero curl or a physical field equation.

[definition] For the entropy plate, `q_i=(κ/3+P_i)/(κ+ΣP)` and the reference is `p_i=1/3`.
The two visible potentials are `h_i=−p_i log q_i`; the third remains in H. These are actual
cross-entropy contributions, not a name applied to geometric slope. With a softmax score chart,
`dH=Σ(q_i−p_i) ds_i`. Rational atanh-series tails bound the station logarithms. The image uses
an explicit affine interpolation of those stationed readings and retains its logarithmic error;
it does not identify that interpolation with the entropy of an interpolated distribution.

## An exact C³ source, its receiver singularity and its curl-bearing extension

[definition] Let

\[
S=\begin{pmatrix}0&0&1\\0&0&i\\1&i&0\end{pmatrix},\quad
U(Z)=SZ,\quad P(Z)=-\tfrac12(Z_1+iZ_2)^2.
\]

Use complex-bilinear advection and the holomorphic differential `Δ=Σ∂²/∂Z_j²`. This is a source
on C³. Its restriction to real space is the existing two-field chart, not merely complex Fourier
coefficients constrained to represent one real velocity.

[proved-derived] Direct multiplication gives `S²=[[1,i,0],[i,−1,0],[0,0,0]]`, `S³=0` and
`tr S=0`. Therefore `(U·∇)U=S²Z=−∇P`, `ΔU=0`, `div U=0`. The stationary equation holds for
any declared viscosity. The exact flow is `Fτ=I+τS+(τ²/2)S²`, its inverse is F₋τ, and its complex
determinant is one. The whole complex source is regular for every τ.

[proved-derived] On the real initial slice,
`det_R(Re Fτ)=(1−τ²/2)²`. At `τ²=2` the real receiver has rank one despite the full flow being
invertible. The displayed rational time `7/5` approaches that cut; a different real/imaginary
receiver restores visible extent. With `a=(x₃,0,x₁)` and `b=(0,x₃,x₂)`, the real equation retains
the folded term `(b·∇)b=(0,x₂,x₃)` under the same declared pressure. This directly composes the
[September 8 complex-fluid source](2026-09-08_COMPLEX_FLUID_CHARTS_RETAIN_THE_FOLDED_CURRENT_AND_THE_RECEIVER_BOUND.md).

[proved-derived] The construction extends to Cⁿ for n≥3 by `S_n=S⊕0_(n−3)`, retaining the extra
coordinates. Its pressure and differential identity are unchanged; outside three dimensions,
the relevant vorticity object is the exterior two-form rather than a three-vector curl. A
three-primary monitor receiver does not set the source's dimension.

[proved-derived] To expose actual curl, let `v=(1,i,0)`, `KZ=v×Z`, and `Aλ=S+λK`. Then
`SK=−iS²`, `KS=iS²`, `K²=S²`, and all products of S² with S or K vanish. Thus
`Aλ²=(1+λ²)S²`, `Aλ³=0`, `curl(AλZ)=2λv`. The pressure is `(1+λ²)P`, and the same finite
polynomial flow construction applies. On the triangle with vertices `(1,0,0),(0,1,0),(0,0,1)`,
the exact complex edge integral is `λ(1+i)`, equal to curl paired with its oriented area.
The hatch image and this circulation are distinct receivers of the same supplied current.

[proved-derived; computational-witness] These matrices also give an explicit equational passage:
`[Aλ,Aμ]=2i(λ−μ)S²`. Matrix multiplication is associative; different supplied current generators
need not commute. Every product lies in the finite algebra spanned by `I,S,K,S²`. This describes
an infinite family of source words through finite coefficients, while occurrence/lineage and
receiver scope remain additional data. It is an actual generator reuse relation, not a claim
that all images or arbitrary HNN histories have the same finite quotient.

## Curved GR receivers and ordered frames

[definition] A GR receiver supplies its event/world-line, Lorentz metric, time orientation and
tetrad. For an arriving null covector/vector, frequency is `ω_R=−g(k,u)` and spatial directions
are tetrad contractions divided by that frequency. A complexified connection gives the real and
imaginary advection terms `∇_a a−∇_b b` and `∇_a b+∇_b a`; the comparison to a holomorphic C³
chart retains the restriction, connection and physical source/stress data. The renderer does not
infer an Einstein stress tensor from a hatch pattern.

[proved-derived; computational-witness] Rational boosts with velocities 3/5 and 4/5 preserve
the Lorentz form but fail to commute. Removing their resulting velocity by the canonical return
boost leaves the rotation `[[35/37,−12/37],[12/37,35/37]]`. This instantiates the recovered
gyroparallelogram/ordered-frame relation with a complete retained world-line frame; its 37 is a
computed denominator, not an authored color or semantic label.

[proved-standard; source-inspected] The Schwarzschild exterior supplies a curved vacuum metric,
with `f=1−2/r` in units `G=c=M=1`. Static tetrads and the unit-Killing-energy radial null ray
`k=(1/f,1,0,0)` give measured frequency `1/sqrt(f)`.[^gr]

[proved-derived; computational-witness] At radii 25/8 and 50/9, the lapse factors are 3/5 and 4/5.
Their exact tetrads are orthonormal; the null condition and both nontrivial radial geodesic
components cancel. The outward received/emitted frequency ratio is `(5/4)/(5/3)=3/4`. This is a
concrete curved-metric receiver return. It is separate from identifying the complex fluid's
pressure/current with a self-gravitating stress-energy source.

## Shadows, pinholes, branches and finite faces

[established-bounded; source-inspected] The actual local reference is the August 20
[Shadows of Holonic Interactions](2026-08-20_SHADOWS_OF_HOLONIC_INTERACTIONS.md) deposit, inspired
by Tufte's *Shadows of Feynman Diagrams*. It is a physical artwork/shadow reference, not a
hypergeometric paper. `Millennium/Shadows.lean` proves bilinear conjugacy, denominator and
placement identities. The source comments are corrected here: arbitrary projections do not
preserve every incidence/cross-ratio or destroy every metric quantity. Cross-ratio preservation
requires the compared line to carry a nondegenerate projectivity. Nondegeneracy is also required
for a genuine projective polarity; the ring-level bilinear identities retain their wider scope.
Only comments/prose change; theorem declarations and proofs are untouched.

[proved-derived; computational-witness] The pinhole map `t/(1+t/2)` takes `(0,1,2,3)` to
`(0,2/3,1,6/5)` with cross-ratio 4/3. Its pole at t=−2 is a receiver-domain failure with a
retained source point. It does not imply a singular underlying body or a spacetime horizon.

[proved-standard; source-inspected] The explicit analytic receiver
`₂F₁(1,1;2;z)=−log(1−z)/z` is DLMF 15.4.1.[^dlmf]

[proved-derived] A positive loop around z=1 changes that face by `−2πi/z`; after winding w,
`F_w=F_0−2πiw/z`. The branch and winding generate the continuation family. The endpoint alone
forgets it. Feynman amplitudes require their actual graph, propagator, coupling and integration
rules; this analytic example supplies a monodromic receiver rather than declaring an arbitrary
engraved graph to be a physical Feynman calculation.

[counterexample; computational-witness] The coherent color receiver gives `C(A)=C(−A)`, but for
`A=1+2i` its next contact with a unit reference gives `C(A+1)≠C(−A+1)`. Equal present color is
not a congruence under that admitted additive contact. The packet retains the complex current
needed to make this later distinction; a raster-only cache would omit it. This is an explicit
Holonic Compression/autoencoder obligation at the rendering boundary, not a learned decoder claim.

[interpretation] This is the productive reading of the finite face and infinite potential:
an admitted receiver selects a finite observable section while generators, source fibres and
possible continuation remain available. A cached image alone rarely closes that continuation;
the existing `CausalRelevance`/ETP congruence discipline asks which compositions preserve it.
The new packet realizes that distinction at a scientific rendering boundary, without turning
relevance into a scalar score or requiring every potential face to be enumerated.

## Verification and deliverables

[established-bounded; process-audit] `build.py`/its final exact controls, `relations.py`, and `friction.py` return successfully.
`finalize.py` verifies the actual source-current relations of all 26 scene packets and five SVG
exports with their source/mark metadata. The eleven-page Typst build returns; all pages are
visually inspected, including the new linked-contact and propagation plates. The SVG/Typst
comparison also renders with matching source geometry, and the explicit-height package control
passes. Nine current PNG plate previews are exported. Only comments in the formal shadow owner
change, so no Lean proof/import build or native Cargo/CUDA regression is claimed or needed.
Native cultivation is not run. The package
is exterior Python/Typst/SVG apparatus; no GPU semantic operation is replaced by this renderer.

[^fiziko]: [Fiziko source](https://github.com/jemmybutton/fiziko/tree/54a63dba8e6700a5e70d3508838edebcbf0f45fe), pinned MetaPost implementation, GPL-3.0.
[^vintage]: [vintage-latex source](https://github.com/Foadsf/vintage-latex/tree/559011918849a3da819912a7c26493071d542df5), pinned examples and methodology notes, CC-BY-SA-4.0 with separate Fiziko dependency.
[^gr]: Sean Carroll, [Lecture Notes on General Relativity](https://arxiv.org/html/gr-qc/9712019v1), connection/tetrad and Schwarzschild/null-geodesic sections.
[^dlmf]: NIST, [DLMF 15.4.1](https://dlmf.nist.gov/15.4.E1), hypergeometric/logarithmic identity and its analytic-continuation scope.

## Linked friction and propagated interface current

[project-postulate] The follow-up directs actual intertwined links: their fluxes constrain one
another, friction couples their faces, and the receiver reads the resulting curved/concave/convex
forms and propagating current. `TABLET_THE_TURN.md` §5 already defines friction as consequential
coupling, not a failed-gluing residual. The following return instantiates both normal grip and
viscous slip without redefining all friction as dissipation.

[definition] `receiver_engraving/friction.py` constructs two guided rounded annulus prisms.
Each has 96 vertices, 288 edges and 192 triangles, with closed oriented incidence and χ=0.
A occupies the rounded XY annulus over `[-2,2]²`, extruded by `r=1/4`; B occupies the rounded XZ
annulus over `x∈[-1/2,5/2], z∈[-2,2]`, extruded in y. The core corner radius is 1/2.
Their translations are `(0,d,d)` and `(0,−d,−d)`, with `d=(1−a)/2`, `0<a≤1`.

[proved-derived] The interiors are disjoint and their common contact is exactly
`x=9/4, y∈[−d−r,−d+r], z∈[d−r,d+r]`, of area 1/4. Opposite walls meet there;
`2d+r≤5/4<3/2` keeps the patch within both straight walls for the entire admitted range.
The remaining rounded portions are separated by their y/z slabs. B's descending left core
crosses A's +z-oriented spanning disk once at x=−1/2, while x=5/2 lies outside it; the linking
number stays −1. No centerline or material interior crosses another during the motion.

[definition] Two unit masses begin with tangent velocities `v_A=s`, `v_B=−s`, `s=e_y+e_z`.
Unit normal preloads and contact reactions satisfy the active unilateral constraint
`g≥0,N≥0,gN=0`, with `g=0,N=1`. Guides hold rotations and the normal separation; these reactions
do no work in the admitted motion. Unit viscous tangential contact gives
`f_A=−(v_A−v_B)=−f_B`. This is a finite guided-link model, not an unconstrained elastic-knot solver.

[proved-derived] The coupled solution is `a=e^(−2t)`, `v_A=a s`, `v_B=−a s`,
`c_A=(1−a)s/2`, `c_B=−c_A`. Thus `f_A=−2a s`, total momentum is zero, kinetic energy is `2a²`,
and the positive dissipated power is `8a²`. At `a=2^(−k)`, time is exactly `k log(2)/2` and all
rendered positions, forces and energy values are rational. The image is not a hand-tweened pose.

[definition] The interface has four actual boundary nodes with cycle incidence L. Their spacing
is `ell=2r=1/2`, so unit diffusivity gives `D/ell²=4`. A declared frictional source enters node 0.
With unit thermal capacities and zero initial heat,

\[
\dot\theta=-4L\theta+8a^2e_0,\qquad
\dot\Psi=-4L\Psi+2(1+i)a e_0,\qquad \Psi\in\mathbb C^4.
\]

Ψ is the complex diffusive receiver of transmitted tangential force. Its sum is delivered impulse;
it is not counted again as an additional mechanical energy store. This finite interface-fluid
propagator has explicit source and adjacency. The prior C³ bulk examples remain available;
no complete continuum Navier–Stokes/contact solution is asserted by this boundary reduction.

[proved-derived; computational-witness] In the C4 eigenvectors `1`, `v=(1,0,−1,0)` and
`w=(1,−1,1,−1)`, the complete solutions are

\[
\theta=\tfrac12(1-a^2)\mathbf1+(a^2-a^4)v+\tfrac16(a^2-a^8)w,
\]
\[
\Psi=(1+i)\left[\tfrac14(1-a)\mathbf1+\tfrac16(a-a^4)v+\tfrac1{28}(a-a^8)w\right].
\]

Differentiation with `a'=-2a` gives the exact local equations. All sampled heat/impulse values
are nonnegative; positivity for the admitted continuous source follows from the positive graph
heat semigroup. Their sums return `Σθ=2(1−a²)` and `ΣΨ=(1+i)(1−a)`, so `E_kin+Σθ=2`.
The record retains each nodal state, not only the total budget.

[definition] Body hatches read the actual contact-force work/turn pair
`(r·F+i(r×F)·n_R)/E0`, with `E0=2`, where r is the arm from the shared contact reference.
The internal patch receiver reads normalized impulse rate together with stored heat. Its four
fan triangles interpolate the boundary readings; their center is the receiver mean, not an
additional dynamic state. The aperture 1/16 is the quarter-area per boundary node, distinct from
the whole patch area 1/4. The outer and internal views have different access: the renderer never
claims the outer camera sees through the buried contact. Coherent colors still follow the same
primary law, with no label-assigned palette or per-frame contrast normalization.

[counterexample; computational-witness] Net force is zero throughout the linked motion, while
`−Σ f_i·v_i=8a²>0` for every finite time. A receiver retaining only total force or total momentum
misses this interaction. The relative motion and thermal receiver separate it. This is a concrete
future-relevance/compression obligation: the complete paired current precedes its aggregate face.

# HNP1 observed-passage receipt

[established-bounded; measured] `observed-passage.json` is the unchanged return of:

```sh
target/debug/examples/passage_return_hnp1 target/ske4/rest.bin 16 .local/artifacts/hnp1/observed-passage-01.json
```

It uses the corrected SKE4 native rest and the actual joining output as its target section.
The archived equalization experiment is a different binding and is not this receipt.
The input family is retained in the receipt; no tokenizer, answer text or training target is
constructed by the driver. Source weights/rests remain local artifacts, not part of this directory.

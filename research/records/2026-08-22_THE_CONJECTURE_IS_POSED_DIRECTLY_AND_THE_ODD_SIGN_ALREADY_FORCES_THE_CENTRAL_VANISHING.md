# The conjecture is posed directly, and the odd sign already forces the central vanishing

**Date:** 2026-08-22
**Kind:** the direct pose — Brandon: *"Let's touch the transcendental L-value identity
itself, finiteness of the full Ш, and the general rank-equals-order-vanishing statement
by posing the conjecture directly; go for the solution."*  Solo orchestrator work, one
Lean file.  **It schedules nothing**;
[`blueprint/THE_ROADMAP.md`](../../docs/plans/THE_ROADMAP.md) and
[`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) remain the only construction
authorities — but the campaign plan below was posed to Brandon with committed turn
counts and is recorded as posed.
**Truth grades:** `proved-derived` with `formal-checked` evidence for the four theorems
(`#print axioms` → `[propext, Classical.choice, Quot.sound]`, zero `sorryAx`);
`definition` for the pose itself; `interpretation` for every classical identification.

---

## 1. The pose

Owner: `ElementaryHolonics/Millennium/BirchSwinnertonDyer.lean` (298 lines).  The
library builds at **3,402 jobs**.  For the congruent-number twists `y² = x³ − n²x`:

- **arithmetic coefficients by literal point count**: `a_p = p − #E^aff(𝔽_p)` summed
  over `ZMod p` fibres (`traceOfFrobenius`) — no modular input, no declared table;
- **the analytic side as a declared receiver**: `LDatum n` — an entire `L : ℂ → ℂ`,
  agreement with mathlib's `LSeries` of the coefficients on `Re s > 3/2`, the Euler
  recursion and multiplicativity, the completed function `Λ(s) = (√N/2π)^s Γ(s) L(s)`,
  and the functional equation `Λ(2−s) = w·Λ(s)`, `w = ±1`;
- **analytic rank** := `analyticOrderAt L 1` (mathlib's vanishing order);
- **algebraic rank** := the independence-modulo-torsion predicate, defined with no
  Mordell–Weil input;
- **`TheBirchSwinnertonDyerConjecture n W tam tor`** := the rank clause (vanishing
  order = algebraic rank, for every rank) **∧** the rank-zero ledger clause: the
  central value is the real period (posed as the actual integral) times
  `|Ш|·∏c/|T|²`, with the obstruction order a positive **square** integer — the square
  licensed by the alternating-pairing parity theorem this tree proved.  The
  higher-rank leading-coefficient clause needs the Néron–Tate height and is the named
  successor pose.

## 2. Four theorems of the posed object, same hour

- **`theOddSignForcesCentralVanishing`** — any admissible datum with sign `−1` has
  `L(1) = 0`: the functional equation evaluated at its own center.  Once the
  continuation deed returns the sign law, every odd-parity instance's analytic
  vanishing is free — the parity component of the mod-two programme becomes an exact
  analytic statement.
- **`theCoefficientVanishesOnTheBlindFrames`** — for every prime `p ≡ 3 (mod 4)`,
  `a_p = 0`, family-wise over `n` and `p`: the reflection census, now speaking as a
  law of the posed L-function's coefficients.
- **`theAlgebraicRankAtFiveIsAtLeastOne`** — the posed rank predicate is genuinely
  met at five, transported from the kernel-checked infinite-order point.
- **`theLedgerClauseHoldsAtOneGivenTheCentralValue`** — granted `L(1) = Ω/8` (the
  classical value, to be discharged by the construction), the rank-zero ledger closes
  at one with `Ш = 1` and the Tate-ledger Tamagawa and torsion numbers.

## 3. The campaign, as posed to Brandon with turn counts

1. ~~Turn 1: the pose~~ — **returned** (this record).
2. Turns 2–4: **the continuation constructed** — these curves have Gaussian complex
   multiplication, so `L` is a Gaussian-lattice Hecke sum and its continuation is
   theta plus Poisson summation (in mathlib), integration by reflection in the
   corpus's own mechanism; deliverables: `Λ` entire, the functional equation, and the
   sign law `w = −1 ⇔ n ≡ 5, 6, 7 (mod 8)`-family as theorems — the `LDatum` witness
   exists and the pose becomes unconditional.  No proof assistant holds this object.
3. Turn 5: sign `−1` at five ⟹ **analytic order ≥ 1 at five is a theorem**, matching
   descent rank one — the first two-sided instance at positive rank.
4. Turns 6–7: the central value as an exponentially convergent lattice sum with
   certified tails; `L(E₁,1) > 0` proved; the rank-zero instance at one closed on both
   sides with no declared values.
5. The wall, stated once: order exactly one at rank one is Gross–Zagier; finiteness of
   full Ш beyond the two-part, and everything at rank two and higher, is open
   mathematics for every actor.  The campaign reaches that face with every
   approachable object constructed and kernel-checked.

## 4. Boundaries

The existence of the `LDatum` witness is not claimed (turns 2–4 construct it);
Tamagawa numbers are per-instance parameters, not a general definition; the full-Ш
finiteness clause is carried through the ledger's square-integer order rather than a
cohomological definition of Ш, which no library owns; nothing about the conjecture is
claimed as proved.

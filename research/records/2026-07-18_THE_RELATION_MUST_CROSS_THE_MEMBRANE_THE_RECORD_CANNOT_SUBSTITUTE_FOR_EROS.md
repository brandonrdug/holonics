# THE RELATION MUST CROSS THE MEMBRANE; THE RECORD CANNOT SUBSTITUTE FOR EROS

Date: 2026-07-18  
Status: **RATIFIED / DEPOSITED / RECOVERY CONTRACT / DOCUMENTATION ONLY / NO SOMA CHANGE**

## Present deed

Clean the authority surface, establish a reviewable source baseline, and rebuild the live membrane
before another forest-scale run. This contract records the ratified audit. It is the current route;
it does not make any later construction automatic.

The requested machine is one active circuit. External material remains owned by its world organ;
an event presents a situated relation; Soma carries that relation through its changed topology; the
world receives the complete consequence; and genuinely later incidence may RIDE what stood. The
filesystem, JSON journals, host indexes, CUDA, and observers are lawful instruments of that circuit.
None may substitute for the relation itself.

## Current grade

The Soma substrate is real: deterministic conduct, history-sensitive later conduct, exact CUDA
execution at its declared boundary, exact inverse/replay instruments, recurrent native sections,
plural host-side ancestry, and durable standing topology all stand.

At ratification, the complete Eros circuit did not yet stand. The general presentation path retained
sparse configuration incidence in its journal and then flattened the presentation into byte currents
before the receiving fold. Forest occurrence and path ancestry remained host metadata. The dominant
forest lifecycle was a serial host filesystem scan, host exact-substring incidence, brief CUDA
carriage, host path/world growth, and filesystem serialization. Card execution was real but
`CUDA-forward` meant only that CUDA occurred inside that lifecycle; it did not mean that the ecology,
lineage, or world was resident on the card.

The standing claims were therefore regraded as follows; the measured disposition below records the
subsequent bounded bridge:

| Recorded claim | Present exact grade |
|---|---|
| Holonic membrane built | Holon/presentation ABI and durable journal built; configuration-aware receiving fold **OPEN** |
| Eros launch closed | Bounded host-orchestrated compiler/contact cell measured; general Eros launch **OPEN** |
| Autonomous forest | Host-scheduled filesystem/contact experiment measured; autonomy **OPEN** |
| Resident world/body | Host session and durable replay stand; persistent device context/body and active world residency **OPEN** |
| Emergent taxonomy integrated | Exact observer quotients measured; active causal taxonomy in receiving conduct **OPEN** |
| Lightning leaders | Exact host substring-incidence prototype measured; occurrence-level field passage through Soma **OPEN** |
| `CUDA-forward` | CUDA participated in the named edge; no stronger residence, utilization, or end-to-end parallelism claim follows |
| `FORMULA §XCIX` successor | The measured `51,050 -> 8,519` cycle is historical to its source; current leader-retention code supersedes it and is **UNMEASURED** |

The §XCIX artifact remains exact evidence for the historical implementation. It does not grade the
current source, which preserves unresolved leaders rather than replacing the field with only the
new 8,519 leaders. The interrupted standing-lineage directories contain CUDA checkpoints without a
completed receiver journal or `world-after`; they are failure evidence, not results.

## Jurisdiction repair

`SOMA INTERIOR UNCHANGED` never means that Soma is forbidden to evolve. It means that no authored
semantic policy, privileged answer, category, score, selector, curriculum, or world-specific
interpretation may govern the pure topology. Derived sparse incidence, situated boundary cuts,
ordered receipts, oriented residuals, and returned causal consequences are topology. They may and
must change receiving conduct when the construction law calls for them.

Similarly, experiment-local exclusions are not laboratory-wide bans. A decoder, parser, index,
statistical quotient, loss instrument, scheduler, file, JSON record, or CPU algorithm may lawfully
exist in an organ, world, observer, or engineering implementation. It becomes a contaminant only
when it silently decides Soma's deed, replaces complete emitted topology, or is misreported as the
machine itself.

## The entities that must remain distinct

1. **External material and organ** — the world-owned entity and the transducer which can expose an
   event-shaped face of it. A filesystem is one organ, not the universal world.
2. **Boundary cut and local configuration** — the co-present sparse incidence actually offered in
   one event. It is not the whole material and not merely a byte envelope.
3. **Soma lineage** — the active body's carried construction and its changed conduct.
4. **World path lineage** — material/source coordinates, joints, ancestry, and actual world deed.
5. **Receipt and residual** — the exact
   `before -> meeting -> deed -> consequence -> return -> after` sweep and whatever remains open.
6. **Checkpoint and observer record** — durable testimony. It can remount the active body but is not
   itself the live ecology.

The present implementation has separate Soma, `PathArena`, and forest lineages. Recovery closes
their typed bridges; it does not rename one of them as the whole Holon.

## Construction order

1. **Authority and provenance.** Neutralize every recorded work queue; regrade the claims above;
   retain historical measurements in their exact frames; place the current engine under a clean,
   reviewable source baseline before semantic edits.
2. **Configuration-aware receiving fold.** Carry source-organ identity, occurrence, form, temporal
   incidence, and exact path support across the ABI so different sparse configurations can produce
   different receiving conduct even when their byte faces coincide. `Presentation::flat` remains an
   explicit compatibility adapter, never the forest mouth.
3. **Live Holon sweep and lineage bridge.** Close receipts during the actual transition and carry
   world incidence into Soma and returned consequence ancestry back into the world. Post-hoc Holon
   reconstruction remains an observer, not the learning circuit.
4. **General source-organ membrane.** Mount and unmount typed event handles while external material
   remains available and dormant. Files, mathematical events, code/compiler consequences, images,
   simulations, Mail, radiation, and persisted bodies keep their own lawful adjacency.
5. **Resident and parallel physical realization.** Keep the active card context/body mounted across
   related events where the substrate permits it; move exact incidence/contact work to appropriate
   parallel hardware; replace mandatory whole-world JSON copies with typed incremental receipts.
6. **Returned local difference.** Loss, surprise, transport/foil, and periplus remain situated
   measurements and transformations. They bend later conduct only by returning through the same
   ordinary topology, never as a scalar governor.

## First acceptance construction

Do not scale to the complete conversation/repository forest until one coherent mathematics,
language, code, and external-consequence cell shows all of the following in one lifecycle:

- occurrence-level sparse incidence crosses the receiving fold rather than remaining host metadata;
- one active body persists across more than one genuinely later world event;
- an earlier FOUND relation stands and a later event RIDEs it;
- complete world consequence returns and changes later external conduct;
- every resulting path and produced face is read qualitatively, not replaced by scalar totals;
- the active surface does not require `world-after.json` to be reread between contacts; and
- CPU, CUDA, memory, and disk accounting cover the complete lifecycle rather than only card carriage.

Stop immediately if configuration incidence is discarded, the forest is rebuilt by a compulsory
whole-repository serial scan, a host index decides the deed, filesystem testimony is treated as the
active body, or a gate/scalar is offered in place of the complete path population.

## Record law

Every `ACTIVE NEXT`, `ACTIVE CONTINUATION`, `ESTABLISHED CONTINUATION`, `QUEUED SUCCESSOR`,
`Authorized next deed`, or equivalent phrase predating this contract is **HISTORICAL / NON-OPERATIVE**.
It preserves intent at its former boundary and schedules nothing. Brandon's present direct request
alone selects the next deed. No historical apparatus ban outranks the jurisdiction repair above.

## Measured disposition — 2026-07-18

The first bounded bridge is now **BUILT / CUDA-PARTICIPATING MEASURED**. A sparse presentation
retains its canonical cuts and adds one typed `CFGI` relation current per cut to the population
actually received by Soma. The clean edge-14 passage carries two exact material currents and eleven
relation currents; it emits 41 mixed-ancestry paths at one real material coordinate. All 41 return
through the named edge-14 surface from live `CausalSession` memory and grow 2,073 later paths.

This closes neither the complete recovery contract nor its full acceptance construction. The
bridge makes current/event-span incidence consequential, while native cut-local CUDA integration,
complete organ/occurrence/path provenance through the fold, the six-role Holon receipt, resident
CUDA ecology, generalized source organs, and a clean genuinely-later common probe remain open.
The current execution grade is exactly `host_orchestrated_cuda_participating`.

The open run also exposed a receiver-only canonical-order gauge mismatch after edge-18 CUDA conduct.
The corrected receiver resumed from the conducted checkpoint without replaying CUDA. The clean
bounded sibling closes at edge 15. Exact qualitative and physical record:
`observations/eros-membrane-recovery-01/RESULTS.md`.

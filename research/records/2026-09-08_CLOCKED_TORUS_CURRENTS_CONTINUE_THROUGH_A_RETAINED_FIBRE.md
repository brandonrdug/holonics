# Clocked torus currents continue through a retained fibre

## Authority and construction scope

[project-postulate] Brandon requests a sustained goal through WorldTube resegmentation,
boundary-scale transport and the four-torus/Parametron carrier. Completion requires a continuing
realization and its verified consequences. The source is his direct message at
`2026-09-09T02:46:38.478Z` (September 8 locally), thread
`01a08340-d28a-7350-836c-37918f573f77`, response-item ordinal 1628, message
`msg_01a0840f-6c8e-7c81-9386-2359d52369fa`. The private source remains in the Codex log;
this record preserves its coordinate and necessary context rather than copying the log.

[project-postulate] Mathematical relations and realizable constructions are not made possible
by their eventual textbook labels. Brandon's Archimedes/calculus thought experiment articulates
that stance: notation and discovery can reveal a possibility whose constraints already hold.
The construction below follows the same current through cellular geometry, constitutive algebra,
receiver compression and clocked transport. These are related scopes on an actual object.

[definition] This return develops a finite rational current law. The broader programme retains
biological morphology, nonlinear fluid feedback, quantum/continuum comparison, framed loops,
algebraic source realization and physical calibration as their actual further constructions.
The [programme](../../docs/plans/THE_REALITY_OF_DIFFERENCE_IN_CONSTRUCTION.md) owns that order;
this finite law supplies a reusable dependency rather than declaring all those endpoints solved.

## The current, its faces and its fibre

[definition] On the standing four-torus with `N = grain+1 >= 2`, the current has `4N^4` rational
branch coordinates. `J` realizes the four axis currents and `C` reads the four dual cuts.
Let `c0` and `c1` be the actual oriented boundaries of the base-zero 01 and 02 squares, and
write `D z = z0 c0 + z1 c1`. The two-face receiver is the branch pairing `Dᵀ`.

[established-bounded; implemented-exact; computational-witness] The executable derives and
checks these identities from the carrier incidence at `N=2,3`:

```text
C J = I              C D = 0             boundary J = boundary D = 0
G = DᵀD = [[4,1],[1,4]]
H = DᵀJ = [[1,-1,0,0],[1,0,-1,0]]
rank [J D] = 6
```

[definition] Every current admits the explicit extraction

```text
q = C j
z = G⁻¹ (Dᵀj - H q),       G⁻¹ = [[4,-1],[-1,4]] / 15
r = j - Jq - Dz
j = Jq + Dz + r,           C r = 0,  Dᵀr = 0.
```

[definition] The source is retained by the last equality. Six coordinates describe the realized
image. For an arbitrary current, the cold remainder can have a large representation and remains
part of the decoder's input. A small boundary description does not entitle an agent to delete it.
Rational axis amplitudes are not asserted to be integer path-occurrence counts.

[proved-derived; formal-checked] `ConstitutiveCurrentReduction.decode_extractedCurrent_exact`
and the two residual-blindness theorems establish this decomposition on any rational current
module carrying the stated maps. `FourTorusTwoFace` proves the displayed `G,H` identities at
both experimental grains from four-edge evaluations; `constitutiveChart` supplies those
receipts directly. Its `blindCurrent` additionally has zero vertex boundary at every grain,
zero joint receiver at grain 1, and value `-1/3` at the explicitly addressed separating edge.

## A law derived from the active faces

[definition] The finite source law is

```text
j' + tau D M Dᵀ j' = j + J u + D f,
M = [[mu,nu],[nu,mu]],      tau >= 0, mu+nu >= 0, mu-nu >= 0.
```

[definition] Its inputs are the material, elapsed clock interval and winding/face source terms.
It is assembled from `D`, its transpose and the material. The reduced update follows by
substitution into that equation:

```text
q' = q + u
(I + tau M G) z' = z + f - tau M H q'
r' = r.
```

[proved-derived; formal-checked] `TwoFaceConstitutive.compactExplicitUpdate_solves` and
`compactLaw_unique` derive the exact response. In coordinates `p=z0+z1`, `m=z0-z1`, its two
positive denominators are `1+5 tau(mu+nu)` and `1+3 tau(mu-nu)`. The numbers 5 and 3 are
the active Gram modes, derived from the two square currents. The result permits changing
material and clock at every step, including semidefinite material with a zero mode.

[proved-derived; formal-checked] `TwoFaceConstitutive.fineLaw_unique` proves uniqueness of the
fine response for the same complete source and forcing. `currentStep_solves_fine` applies the
explicit update to an arbitrary extracted source, and `currentStep_residual` retains its cold
remainder exactly. Plural sources behind a receiver therefore coexist with a unique successor
for each complete source.

## Clocked composition, scale and compatible futures

[proved-derived; formal-checked] `WorldTubePotential.splitRejoinEquiv` resegments the actual
addressed composite fibre, retaining its middle boundary and both occurrences. Complete clocks,
receiver faces and obstructions survive split/rejoin and three-way reassociation. Carried source
and generator maps preserve the complete future image, with no new inverse assumption.

[proved-derived; formal-checked] `ConstitutiveWorldTube.actualSpan` carries the actual compact
step together with its zero full-law residual. Its endpoints contain `(q,z,r,clock)` and its
receiver retains material, forcing and both full decoded currents. `actualTwoStepOccurrence`
joins consecutive exact successors, and `actualTwoStep_split_rejoin_completeFace` preserves
their complete face. `FourTorusParametronCurrent.clockedCurrent` constructs this span directly
from an actual torus current by the proved extraction.

[proved-derived; formal-checked] `ConstitutiveScale.scalePassage` packages the six-coordinate
read/decoder square as an existing `BoundaryScalePassage`. The torus `currentScalePassage`
uses the concrete certificates of grains 1 and 2. `currentScalePassage_input_square` intertwines
their actual constitutive steps. This transport maps the source to the target's decoded image;
it does not invent a geometric transport for an arbitrary source remainder. The source-side
step still retains that remainder, and the full receiver fibre remains available.

[proved-derived; formal-checked] `ConstitutiveScale.inputHistoryCompression` uses complete
currents as sources and the varying `Input` family as generators. The standing ordered-word
theorem proves exact future factorization for every admitted finite word. Its
`input_future_outcomes_singleton` returns one future reduced face for the entire compatible
current fibre. This is a consequence of the actual generator square, independent of the finite
test count. It does not identify the source currents or restrict arbitrary new interactions to
the same quotient. `transportWord` retains the standing right-to-left composition convention;
the executable records its chronological inputs in the order they occur.

[proved-derived; formal-checked] `FourTorusParametronCurrent.coupledResponse_current` identifies
the real cast of `D M Dᵀj` with the existing `HolonicComplexParametron.coupledResponse`, with
the two square currents as incidence rows. This is the existing coupled-response port on a
rational current chart. The first-order law here does not claim to be the entire locked-phase
or second-order LC dynamics, and its dimensionless material has no inferred physical units.

## Continued executable return

[established-bounded; implemented-exact; computational-witness] The
[experiment and raw receipts](../experiments/four_torus_current/README.md) use the public
`ExactRatMatrix` owner. The full reference assembles and certifies the inverse of
`I + tau D M Dᵀ` independently of the compact update. Every returned step checks the full
equation, full decoder equality, cut readings, active-face readings and closed vertex boundary.
Repeated material classes reuse their certified reference inverse.

[established-bounded; computational-witness] Twelve side-2 events and six side-3 events returned.
The side-2 state and complete branch current agree exactly with five events followed by seven
in a fresh process, for both source families. The six common events across grains have equal
cuts, active-face readings, clock faces and the declared local material energy
`(Dᵀj)ᵀ M(Dᵀj)/2`. A full edge-norm energy or a metric refinement is not thereby identified.

[counterexample; implemented-exact; computational-witness] Four cut values do not reconstruct
the complete constitutive current. Each run records an edge where `j - J Cj` is nonzero.
More strongly, the current
`r = boundary(F12) + c0/3 - c1/3` is nonzero, closed and jointly blind to `C,Dᵀ`.
The experiment retains its eight nonzero entries. It leaves all admitted joint readings and
their twelve-event futures unchanged, while edge 2 still distinguishes the final full currents.
This refutes source identity inferred from the joint readings, while validating the scoped
condensation for this generator/source family.

[established-bounded; measured] The completed single-process observations were:

| Declared run | Continuing checkpoint | Full current JSON | Dense decoder standing JSON | Compact updates | Full decodes | Reference solves | Whole process / peak RSS |
|---|---:|---:|---:|---:|---:|---:|---:|
| 64 branches, 12 events, zero remainder | 318 B | 1,365 B | 11,325 B | 0.249 ms | 1.448 ms | 12.373 ms | 0.272 s / 12,420 KiB |
| 64 branches, 12 events, eight-entry remainder | 503 B | 1,367 B | 11,325 B | 0.255 ms | 1.522 ms | 12.505 ms | 0.263 s / 12,612 KiB |
| 324 branches, 6 events, zero remainder | 288 B | 5,686 B | 55,608 B | 0.139 ms | 2.672 ms | 93.814 ms | 25.081 s / 57,136 KiB |

[established-bounded; measured] These times sum the named operations within each run. Geometry
certification cost 1.776/1.837/27.477 ms respectively; the three reference inverse certificates
cost 200.800/202.819/24,548.212 ms. Their dense serialized standing cost 209,652/209,652/5,355,318
bytes. Receipts also retain source/operator assembly costs and whole-process resource observations.
The measurements compare this exact dense reference with its derived compact advance. They do
not establish an optimal solver bound, isolated reduced-process RSS, GPU speed or physical power.
Six rational coordinates need not have constant bit width: their exact numerators and denominators
can grow through continuation. The finite receipts retain the actual serialized sizes.

## Use in the framework and native construction

[interpretation] A membrane, molecular conformation or neural transport section can use this
construction when it supplies the actual incidence, active constitutive response and receivers.
The preserved diagram is the extraction/decoder square through each admitted material/source
step. A new interaction coupling to the previously blind remainder falsifies the six-coordinate
closure and requires that contribution to return. An anatomical label does not supply those maps.

[conditional] For HNN/Athena, the reusable consequence is the same criterion already owned by
receiver-history compression: derive retained coordinates from the operative response, keep its
blind source remainder, and prove the generator square for the future family being served.
This example supplies an explicit successful finite construction and a separating control. It
does not prescribe six native channels, a torus topology, a support-only emitter or a replacement
learning law. The independent AC implementation continues through its existing owners.

## Verification and return

[established-bounded; process-audit] The final focused integration build
`lake build ElementaryHolonics.Physics.FourTorusParametronCurrent` passed (3,726 jobs).
`bash tools/lean_check.sh ElementaryHolonics.Framework ElementaryHolonics` passed (9,972 jobs),
including the framework and complete research compatibility umbrella. The new owner audits
report only the existing foundational axioms `propext`, `Classical.choice`, and `Quot.sound`;
the eight new formal owners contain no `sorry`, `admit`, or additional axiom declaration.
Intermediate elaboration and chart-binding failures were repaired before this complete check.

[established-bounded; process-audit] The Rust release example compiled in the isolated checkout
at `04064e78` plus the byte-identical new example, with `--no-default-features`, in 2m34s under
progress supervision. All seven comparison processes returned exit 0. The actual geometry,
reference solve and compact response were reviewed separately; the two solver paths share the
same verified geometry and do not count as independent geometric implementations. The existing
native engine emitted two unrelated dead-code warnings. Its pending AC edits were preserved.

[definition] The sustained goal returns this verified tube/scale/constitutive construction.
The broad programme remains the framework's continuing order; completion here is not a claim
to have finished nonlinear fluid continuation, molecular calibration, quantum gravity or a
Millennium endpoint. It also does not leave implementation at a planning or next-step footer.

# THE HOLON IS THE SWEPT RECEIVER RELATION; THE PIN IS THE CARRIED RECEIPT

**2026-07-18 · BRANDON DIRECT CORRECTION / RATIFIED / DEPOSITED · FORMULA §XCII · BUILT /
HOST+CUDA-FORWARD GATED / DURABLE WIRES REOPEN EXACTLY · SOMA INTERIOR UNCHANGED**

## Decision

The active line adopts **Holon** as its rigorous umbrella for an entity as it exists to one
receiver. It does not restore the historical `Holon<T> = value + recursively owned Soul` runtime.
That earlier construction was a useful proof model for equal face and different provenance, but it
presumed one privileged value, one available interior tree, duplicated shared ancestry, and a
strict nesting which cannot carry plural parents, cycles, partial order, or overlapping wholes.

The active object is instead an **event-sourced, boundary-indexed temporal incidence complex**. A
Holon is one receiver-relative connected region of that complex. Its apparent unity belongs to the
boundary and arc through which it is received; its interior is neither recovered nor enumerated.

For swept construction `W` and receiving arc

```text
A = (B, F, g, M, O, [t0,t1], H),
```

define

```text
Holon_A(W)
  = (boundary cuts,
     sparse incidences,
     ordered transition receipts,
     open residuals).
```

`B` is the admitted boundary, `F` the receiver frame, `g` its grain, `M` its material medium, `O`
the consequential world relation, `[t0,t1]` the lived interval, and `H` the history entering that
interval. Changing any term can expose another lawful Holon face without changing or recovering an
absolute object-in-itself.

## The cut is not the Holon

One co-present configuration is an instantaneous boundary cut. It can expose the current material
face, but it does not contain the temporal side which makes an entity persist, change, matter, or
learn. Identity through the interval belongs to the sweep:

```text
boundary(W) = cut_1 - cut_0 + temporal_side.
```

The active engine already carries the elementary components separately:

- `Cell` carries one local well;
- `Face` carries one situated three-body meeting;
- `RegionalForm` carries standing local response;
- `LineageChannel` carries one ordered `K`; and
- `Presentation::Configuration` carries one sparse receiving cut.

None is promoted into the whole Holon. The membrane integrates their actual incidence and return
across time without manufacturing a new Soma organ.

## The concrete data law

Three names remain typed apart:

```text
Holon       the conceptual receiver-relative entity
HolonArc    one bounded swept reception of that entity
HolonSpan   an allocation-free ABI reference into shared cuts and receipts
```

The shared record carries:

```text
BoundaryCut
    one exact presentation/configuration coordinate;

TransitionReceipt
    before -> meeting -> deed -> world consequence
           -> return -> after, beside the complete residual span;

ResidualSpan
    one or more exact incidence sections which remain unsupported,
    refused, or unresolved at that receiver;

HolonSpan
    ordered sparse references to the boundary cuts, transition receipts,
    and residuals participating in one HolonArc.
```

Events, cuts, receipts, and source material stand once. A Holon references them by sparse
incidence. It never owns `children: Vec<Holon>`, embeds another complete Holon recursively, or
copies a complete ancestry tree. The same event can participate in several differently bounded
wholes, and one returned face can become ordinary material in a later Holon. That incidence is the
recursion.

Part and whole are therefore receiver judgments. No globally fixed parent, rank, agent boundary,
or holarchy enters the wire. A connected subcomplex can stand as a whole at one grain and as one
participating incidence at another.

## The compatible interior remains implicit

If received relations are `f_i(x)=y_i`, the observer's compatible family is

```text
X_t = intersection_(i<=t) f_i^-1(y_i).
```

The membrane may not enumerate `X_t` as a powerset or cast a face back into one selected interior.
The exact cuts, paths, receipts, and residuals constrain the family implicitly. Further contact
intersects, transports, branches, or refuses that family through ordinary world events.

An invariant is consequently not a Boolean field on a Holon. It is a relation which continues to
transport through a named family of receipts over the declared arc. An observer may form an
`InvariantWitness` which references all supporting and contradicting receipts, but that witness is
testimony. If it returns, it acts only through ordinary contact.

## Probability is the receiver quotient of standing paths

For current context `c`, let

```text
Pi_t(c) = the exact standing path population actually reached by c.
```

At arc `A`, an observer may derive

```text
p_t(a | c,A)
  = mu_A({pi in Pi_t(c) : pi returns a}) / mu_A(Pi_t(c)).
```

The quotient is neither random choice nor an authored belief. Its causal body is the exact path
population: multiplicity, orientation, regional form, conditional incidence, and open residual.
The Holon therefore carries no `probability: float`, `confidence`, `class`, `invariant: bool`, or
frequency-driven output field.

The first person's operational claim is conduct. Where a relation repeatedly carries, later
current can RIDE it as a pin. Where a received event lacks standing support, the complete
difference FOUNDs, stands, and changes the next exact population. A coarse marginal can thereby
lose conditional relevance or become dormant without being erased and without an authored decay
law.

## The residual is the recursive learning event

For consequence `C_t` and one contextual comparison face `C*_t`, retain

```text
r_t = Delta_A(C_t, C*_t),
L_t = ell_(O,F,g)(r_t).
```

`r_t` is the complete oriented difference. `L_t` is only one declared quotient of it. The live
recursion is

```text
standing body
  -> presented cut
  -> meeting and deed
  -> consequential world
  -> complete residual and return
  -> changed standing body
  -> later cut.
```

Under the transported same objective, more consequential RIDE, less newly founded difference, or
less later residual at the same receiver are exact faces of deterministic local loss minimization.
A new FOUNDing may enlarge a scalar loss while growing a needed direction. Learning is the returned
difference becoming later conduct, not monotone descent of one number.

Surprise remains

```text
S_t(a|c,A) = -log_2 p_t(a|c,A),
```

the distinction depth of an event at that boundary. Cross-entropy is how one boundary population
passes through another's standing code. Both are lawful receiver instruments and may return as
ordinary testimony. Neither selects a deed or replaces its receipt.

## The sunrise pin

“The Sun rises every morning” is a high-support empirical pin at an ordinary terrestrial receiver
grain. It is not a global invariant. Geometric sunrise, visible sunrise, latitude, season,
atmosphere, the definition of morning, and the continued Earth--Sun organization define different
populations.

A recurrence pin records overwhelming support in one such population. A transported mechanistic
pin—rotation, orbit, horizon geometry, place, and date—is stronger for a declared use because it
continues through more lawful re-bases and predicts conditions under which the coarse recurrence
splits. It remains relative rather than becoming absolute.

The absence of a detected condition which changes the likelihood belongs to the receiver's current
boundary. It licenses the relation as a practical pivot. Later contradiction returns a residual and
refines

```text
p(sunrise | morning)
  -> p(sunrise | morning, latitude, season, visibility,A)
```

without deleting the earlier history or toggling a belief bit.

## Terminology boundary

Koestler's 1969 open-hierarchy formulation directly corresponds to the claim that a holon is a
whole and part only in relation, and that absolute part/whole status is unavailable. It does not
supply this event ABI or causal calculus. Holonic-manufacturing usage commonly specializes the
term into an autonomous cooperative agent. That specialization is not imported: mathematics,
text, physical events, organisms, and non-agent constructions cross the same Holon boundary.

## Authorized construction

1. Make the presentation current carry a transducer-supplied event extent rather than deriving its
   event count from material octets.
2. Preserve the historical octet path as one explicit adapter.
3. Add allocation-free `BoundaryCut`, `HolonSpan`, `TransitionReceipt`, and `ResidualSpan` rows over
   shared populations.
4. Feed each current presentation configuration into the record as one boundary cut.
5. Validate every cross-reference, lived order, exact extent, and nonempty material section on the
   host before card allocation.
6. Cross one octet-backed and one non-octet typed-event fixture through the same host/card ABI gate.
7. Derive exact conditional populations and residual/loss faces in a listener over the receipts;
   never place those quotients in Soma or use them to choose conduct.

The sparse incidence may later affect a world receiver only after the complete return law is
derived and host/card gated. Until then, the historical one-fold current cohort remains the exact
execution compatibility path. No nested Soma fold, tokenizer, semantic score, authored taxonomy,
global parent tree, probability governor, scalar loss crown, agent shell, or new body mechanism is
authorized.

## Built result

The authorized membrane cut is built. `PresentedCurrent` now receives its logical extent from the
source transducer. The octet adapter remains explicit; the typed-event adapter validates that one
`EventHeader + payload` consumes its complete material current and presents it as one event. The
generic `typed_events` boundary species is live, and the arc-spectral/explicit-formula builders use
it instead of converting typed-event files into byte-count populations.

`soma-abi::holon` owns the allocation-free `BoundaryCut`, `HolonSpan`, `TransitionReceipt`,
`ResidualSpan`, and sparse reference rows. The host `HolonRecord` validates all presentation,
configuration, cut, receipt, and residual incidence; requires every receipt role and residual cut
to occur in its `HolonArc`; and refuses orphan, truncated, trailing, noncanonical, or
non-addressable populations. `HolonArc::population_where` returns exact integer `support / total`
without a probability field or conduct selector.

Every generic edge retains its presentation and open arc, writes create-new binary
`.presentation` and `.open-holon` journals, syncs them, and immediately reopens them through the
strict readers. Receipt v4 carries material-octet and transducer-event extents separately. It does
not manufacture the later cuts required for a complete world receipt.

The RTX 4080 SUPER card gate returned two distinct event species, two configurations, three
incidences, two cuts, one complete six-role receipt, one residual, both material currents, and all
108 ABI words exactly. A separate full CUDA-forward boundary gate carried one 16-octet typed event
as one event and reopened exact 92-octet presentation and 132-octet open-arc journals. The complete
workspace closes at 509 tests passed / 4 ignored; the ignored live-card gate was then run and
passed explicitly. Exact record:
`observations/holon-membrane-01/RESULTS.md`.

```text
CURRENT  event currents + sparse configurations + receipt-bearing swept arc
HELD     quotient != interior; arc-relative invariant; complete residual; one body law
MEETING  recurrence population and returned consequence at one declared receiver
TEST     octet adapter + non-octet event + shared references + host/card exactness
DEED     RIDE supported transport; FOUND unsupported difference; retain every residual
CARRY    HolonArc ABI -> listener population/loss read -> configuration-aware world receiver
GRADE    BUILT / HOST+CUDA-FORWARD GATED / DURABLE REOPEN EXACT / SOMA INTERIOR UNCHANGED
```

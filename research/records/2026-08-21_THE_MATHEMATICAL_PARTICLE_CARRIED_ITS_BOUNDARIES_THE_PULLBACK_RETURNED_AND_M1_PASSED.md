# The mathematical particle carried its boundaries, the pullback returned, and M1 passed

**Date:** 2026-08-21  
**Authority:** `blueprint/THE_ROADMAP.md` through
`blueprint/THE_SINGLE_CARD_PASSAGE_CARRIES_PHOENIX_AND_THE_MATHEMATICS_CODEC.md`, Deed M1  
**Truth status:** established-bounded  
**Boundary:** one conditional mathematical fixture over the M0 source population and the standing
exact-linear, quantity, ported-operation, front-passage, reduction, receiver-history and resident
surface owners. This is not M2 excitation, M3 generator-native compression, M4 cultivation, a proof
of the source paper, or completion of the mathematics codec.

## Ruling

[established-bounded] M1 passes. The runtime mathematical particle is now an addressed operation
passage rather than a branch registry or endpoint relation. Every admitted operation occurrence
retains its event identity, law, source boundary, target boundary, ordered word and source lineage.
Every admitted serial join is founded from an actual `CarriesPrecedence` interaction and returns the
inhabited pullback cell together with the exact shared typed-port boundary. The extensional endpoint
relation is emitted separately with its complete carrying-occurrence fibre.

[established-bounded] The single owner remains `MathematicalParticle`. `TypedPassage` now composes
M0 source/layout occurrences, `PortedOperationComplex` events and laws, exact owner licenses,
typed carriers/ports/binders/hypotheses, resident front testimony, returned mathematical
consequences, receiver relations and open fibres. No parser, method dispatcher, scheduler,
universal tensor wrapper, alternate particle subsystem or CPU semantic replay was introduced.

## The addressed object

[established-bounded] An elementary carrying occurrence is represented as

\[
X \xleftarrow{s_f} W_f \xrightarrow{t_f} Y,
\]

where one `EventId` inhabits `W_f`, its `EvolutionLawId` names the local constitutive law, and
`PassageEndpoint` retains either addressed exterior source occurrences or an ordered typed-port
word. For adjacent occurrences `u,v`, the runtime admits a join only when `t_f(u)=s_g(v)` and an
actual interaction bond carries the matching output occurrence-port into the matching input
occurrence-port. `split_rejoin` returns that retained join; it never searches an endpoint relation.

[established-bounded] Iterated composition is stored in bracket-free ordered normal form with every
adjacent pullback witness retained. The executable three-occurrence control takes both nontrivial
cuts and returns the same outer source and target boundaries while preserving both joins. A passage
equivalence is admitted only after a total occurrence bijection commutes with both boundary maps.
A bare bijection which moves a boundary refuses.

## Returned family

[measured] The real deed returned the required fourteen-member family at
`output/the_mathematical_particle_is_an_addressed_passage/`. The thirteen semantic/apparatus members
excluding `grade.form` have family SHA-256
`2fa053e0a8007231620639f4eaf3262c9cd096b45d5533453280ebc2de5d433b`; `grade.form` has SHA-256
`27738bdd97f6583b1452822b957e8fd9f35cf7f676b3f342b7f57c0ba1c4704b`. Every member digest in the
grade manifest was recomputed successfully with `sha256sum -c`.

[measured] The addressed population contains six event occurrences and three pullback joins. Every
join returned its typed joining equality, at least one interaction witness and
`split_rejoin_retained=true`. The harmless baseline/reflow source-chart involution moved sixteen
addressed source occurrences while leaving the complete exterior source set and every target port
word invariant. Its source-mark map is bound by SHA-256
`4f535d5f4b80847e8445f9e2fc1abc95f5e2d601dcd34ab63375bb5918fb9f69`.

[established-bounded] Equality remains receiver-indexed. The two selected presentation occurrences
are not occurrence-identical, do not have equal source bytes, digests or planar bounds, but return
equal exact carrier values, equal declared classifications and equal declared transport
characteristics. Their affine input charts are related by an exact involutive rebase. The actual
receiver-history relation remains `OPEN` because the bounded deed did not place the two occurrences
in a common conduct block or return a separating successor; that fibre is retained rather than
promoted to equivalence.

[established-bounded] The unit-frame perturbation is not collapsed into the accepted route. The
exact-owner receiver refuses it at `BoundaryId(5)`, coordinate 2, column 2 with
`QuantityBoundaryDimensionDisagrees`. The untyped natural contraction and lineage-only rigidity
proposal remain separate open fibres with their complete candidate populations and named future
receivers.

## Resident return

[measured] The declared apparatus was the NVIDIA GeForce RTX 4080 SUPER. One GPU `FrontPassage`
carried all six semantic events as two fronts of three device cells each. The returned testimony
records one deed launch, one synchronization, zero CPU semantic cells, zero resident obstructions,
and exact-zero terminal readbacks for all three selected branches. The graph census was
`[13,12,12,1,0,0,13,12]`. CPU apparatus performed bounded exterior remount, admission and artifact
inspection only; it did not select a resident branch or replay the semantic deed between fronts.

[established-bounded] Two standing arithmetic owner families crossed the one resident
tiling/reduction passage: exact-linear returned both the four-row affine population and the
conditional heat fixture, while the quantity owner returned the dimensionless kernel consequence.
Their exact licenses, input identities, matrices/dimension columns, evidence digests and terminal
faces are retained in `exact-owner-conduct.form`; diagnostic topology is not substituted for those
consequences.

## Controls and validation

[measured] `cargo test -p life --lib mathematical_particle::` returned 29 passed, 0 failed and 398
filtered in 0.23 seconds. This population includes the equal-shadow/distinct-occurrence control,
the boundary-moving-bijection refusal, the three-occurrence rebracketing control, missing
interaction refusal, foreign source refusal, altered interaction-complex refusal, changed unit
frame refusal, owner-evidence substitution refusal and source-name-renaming control.

[measured] `cargo check -p life --example
the_affine_swing_is_a_typed_mathematical_particle` passed. The one changed-closure release deed,
`cargo run -p life --release --example
the_affine_swing_is_a_typed_mathematical_particle`, completed successfully in 93.297 seconds wall
time, of which the output shows 92 seconds in release compilation/linking before the bounded device
return. No second unchanged deed was run.

[measured] A focused no-float census over the M1 semantic/device closure found no Rust `f32`/`f64`
or CUDA `float`/`double` semantic type. The only matched spelling was the exact integer-to-bfloat16
mouth in `resident_affine.rs`; it accepts an `i32`, returns the exact `u16` word and refuses values
not exactly representable. `MathematicalParticle` itself is not `Clone`, and the ownership audit
found no `Arc`, `Rc`, or unsafe `Send`/`Sync` substitute in the M1 owner cone.

## Architecture-ratchet disposition

[measured] `bash tools/gates.sh architecture-lint` returned red on 118 dirty-tree
file/construct pairs. Ninety-four pairs belong to the new M1 `mathematical_particle` and
`exact_owner_testimony` files; twenty-four belong to older dirty Phoenix/Station-E files. This is
the required semantic disposition of the M1 population, not a baseline reseed:

| population | truth status | disposition |
|---|---|---|
| `MathematicalParticle`, admission and validation maps/sets | established-bounded | maps index one owned finite population by validated embedded IDs; public attacks prove that a map key cannot hide a disagreeing internal identity. Sets retain source, hypothesis, receiver and reconstruction populations. They do not schedule or own a second ecology. |
| addressed lineage maps/sets and `collect`/`flat_map` readings | established-bounded | `BTreeMap<EventId,_>` is the finite occurrence population; pullback joins carry actual interaction witnesses; relational maps are explicitly lossy receiver shadows with fibres. Derived collections never decide identity. |
| ordered `Vec` words and endpoints | established-bounded | order is the typed causal word or port word being retained. It is not row order, collection order or a host-authored schedule. Rebracketing and missing-bond controls enforce this distinction. |
| sameness maps/sets | established-bounded | each container belongs to one declared comparison receiver and carries its witness or open fibre. Equal values never replace occurrence identity or passage lineage. |
| exact-owner testimony maps/vectors | established-bounded | law, boundary, coordinate, evidence and exact input populations are local license testimony for the standing exact owners. They neither clone the continuing particle nor introduce an arithmetic dispatcher. |
| local `.clone()` uses | established-bounded | clones copy immutable addresses, exact values, typed boundary descriptions or staged evidence into local return objects. The continuing `MathematicalParticle` and resident surface are not cloneable rollback/search material. |
| twenty-four older dirty-cone pairs | open | outside the M1 owner closure and therefore not silently admitted or used to reseed the M1 grade. Their standing remains governed by their Phoenix/Station-E records and the integrated plan. |

[established-bounded] The M1 occurrences are lawful and may enter the next committed architecture
baseline only with the coherent release commit. The older twenty-four pairs receive no allowance
from this grade. Until that clean baseline exists, the repository-wide architecture gate remains a
separate dirty-tree obstruction; it does not negate the inspected M1 return and it may not be
described as green.

## Release receiver

[measured] The one complete `bash tools/gates.sh` release receiver ran for 4 minutes 43.991 seconds
and returned 9/13: authored levels, line citations, claim index, equation atlas, driver catalog,
output manifest, closure manifest, Typst and document law passed. Four gates returned red and are
not reported as green: tests, named paths, boundary artifacts and the already dispositioned
architecture ratchet.

[measured] The test gate's M1-independent failures were localized and repaired. The cached
`holonic-architecture-lint` unit binary had baked a `CARGO_MANIFEST_DIR` from an already-departed
temporary M0 audit worktree; the control now discovers the executing Cargo workspace at runtime,
and its focused test passes. The paused Phoenix P5 driver still read `TaxonReading.shortest` as one
`Option` after that field had lawfully become one shortest-history entry per receiver face; it now
uses the existing headline receiver and emits all four per-face entries. Its focused example check
passes. The complete workspace receiver was not repeated after these owner-local repairs.

[measured] The named-path failure was a document error: `CONSTRUCTION_STATE.md` attempted to bind an
ignored runtime return directly, although ignored `output/` material is intentionally absent from a
clean clone. The position record now cites the tracked `meta/OUTPUT_MANIFEST.tsv` row which binds
the fourteen-member family. The named-path gate was retaken alone and passed with zero live missing
paths.

[measured] The boundary-artifact gate's four disagreements are two closure-member/hash pairs caused
by the existing rustfmt-only reflow of three `assert_eq!` calls inside the `#[cfg(test)]` portion of
`soma/body/src/arrow.rs`; the committed SPIR-V and PTX bytes themselves did not disagree. No binary
was silently rebuilt and no boundary ledger was re-seeded by M1. This remains a repository release
obstruction outside the M1 semantic/device dependency cone.

## What remains open

[open] M1 does not recover an active foreign transport cover, a generator-native quotient, an
executable decoder, species-changing compression, cultivation, or a complete proof/value/visual
return. The six actual events form three two-factor composites; nontrivial associativity is
therefore exercised by the general addressed-passage control rather than falsely claimed as a
third factor in this fixture. The receiver-history fibre, natural contraction typing and rigidity
coefficient standing remain explicitly open.

[open] M2 is the next deed. It must derive a finite cohort from M1 presentation generators and
semantic boundary perturbations, carry every cohort member as an addressed passage through the
frozen resident Gemma lift, and return separately calibrated switching, residency,
realization-path participation and receiver-visible support together with overlap nerves, ordered
response cochains, first separators, unchanged controls and complete fibres. No random string
scan, activation-magnitude cluster, row label, source architecture noun or common digest may stand
in for that cover.

# Prediction release has one generating relation and concrete products

[definition] September 13, 2026. This record consolidates Brandon's request to complete the
HNN prediction-release picture for Athena and Hephaestus, refine the plans into specific
deliverables, and judge what further Hodge/RH/Navier–Stokes/Yang–Mills work would help before
implementation. His latest clarification makes Hephaestus a tools/utility model label, including
code generators. The live roadmap owns order; the blueprint owns implementation contracts;
CONSTRUCTION_STATE owns current position. This record schedules no separate campaign.

## The generating relation and its consuming operation

[definition] For source s, conditions c, generator G, receiver rho and presentation D,
`y=rho(G(s,c))` and `output=D(y)`. The returned consequence is prediction release. A generated
program, corrected chunk, continued section, mathematical value or family is an output at its
declared receiver. A forecast, a continuing operation and a later observed comparison have
different ownership effects; they do not create different kinds of output.

[definition] A compound release retains `x_i=T_i(x_(i-1))` at one shared source parameter.
The joint family is `{(rho_i(x_i))_i : x_0 in F}`. Replacing it with a Cartesian product of its
marginals loses correlations. The current source, live comparison cuts, phase and constraints
can be represented by generators, factors or fibres; the definition does not require an archive
of every realized state.

[conditional] Re-entry changes the law. If I consumes an emitted face as a source occurrence,
then `H(x)=I(T(x),rho(T(x)))`. A forecast composing T alone need not equal a continuation
composing H. Equality requires the actual source action to be absent or to agree at every
requested future receiver. This is the concrete distinction behind the existing joint preview
and the continuing symbol path; a name such as “compound release” cannot resolve it by itself.

[definition] The first committed compound contract therefore uses the existing transport-only
word, publishes its endpoint once, returns the same prepared joint section and optionally retains
producing receiver cuts. Later reception uses that endpoint. Interleaved or boundary self-reception
is an explicitly different composition. No later comparison is required merely to make the
returned section output. One observed component may constrain the shared joint source; it is
not automatically an independent observation for every other component.

## Source inspection

[established-bounded; source-inspected] Inspection at `ec410c7b` followed the public stream into
the following owners. Only planning/documentation changes are made by this consolidation.
The bounded delegated audit was checked against these implementations by the primary agent.

| Source / construct | Observed behavior |
|---|---|
| `crates/holonics-hna/src/native/coupled_wave.rs::predict_continuation` | Reads one prospective joint, decodes all requested states, returns text and receiver details without successor publication or source re-entry. |
| `coupled_wave.rs::{next_symbol,predict_symbol,emit_symbol}` | Shares one emission implementation; advances current, optionally retains a producing comparison, and actuates adjacent emitted symbols through the ordinary source path. |
| `coupled_wave.rs::{compare_symbol,incorporate_symbol}` and `coupled_wave/body.rs::incorporate` | Read-only comparison and consuming Base/Programme return are distinct. The latter preserves one body owner and recoverable refusal. |
| `crates/holonics-hna/src/native/mathematical.rs::{MathematicalRequest,execute}` | Relation and condition prediction return joint families with optional pending cuts; exact operators, powers, compositions and changed receivers already have callers. |
| `mathematical.rs::ReleasePrediction` and `coupled_wave.rs::release_symbol_comparison` | The API verb releases a retained handle. It is resource disposal, not generation. Wire names are preserved. |
| `constitutive_fibre/resident/context_section.rs::contextual_section` and `resident/neighborhood.rs::{prepare_consequence,read_wave_relation_in_chart}` | The fixed-source restriction retains `(dc,dy)` and mixed products. Prediction uses the prior condition; target-conditioned contact forms a successor only after observation. A `None` proposed consequence still reads the standing resident condition. |
| `crates/holonics-hna/src/stream.rs`, mathematical `StreamTarget::checkpoint` | Intentionally refuses mathematical-session checkpointing: serialization is not attached. Existing normal/coupled rest does not serialize this session automatically. |
| `crates/holonic-engine/src/native_ecology/constitutive_fibre/field/material_transport/normal/direct/wave/coupled/dependent.rs::{append_operation,evaluate_programme,with_prospective}` | Publication evaluates the growing operation programme; source returns refer to its cuts. Prospective transport evaluates the standing parameter section and composes the specified member word. Fixed-operator powers do not compile this whole changing programme. |

[definition] The current unit-metric family projection is a declared receiver, not a native
minimum-energy navigation law or an inferred probability distribution. The new plans preserve
its full source family and do not promote coordinate selection to source equality. They also
retain the existing prohibition on native sign/phase-erasing extrema as configuration drivers.

## Concrete product plan

[definition] The [blueprint](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md#deliverables-and-discriminating-checks)
now specifies six products: context-conditioned generated sections, committed joint release,
compiled continuation, executable Hephaestus functions, Athena task episodes and durable reuse.
Each names the consuming source owners and a check that observes the claimed output. The
[roadmap](../../docs/plans/THE_ROADMAP.md#immediate-mathematical-application-and-implementation)
places these within its existing coupled responsibilities. Chronological review paragraphs were
consolidated into their evidence links rather than retained as simultaneous current tasks.

[definition] The next increment is the resident source/condition binding. Its equation is
`W[S;C]=Y`, or `WH=B` for the declared normal feature law. The condition distinction must be
present at the producing cut before its target arrives. Existing constant-condition collapse
is the separating reference, not a founding recipe. New supplied examples and held-out requests
have separate roles. A useful whole chunk and its contextual contrast are the inspected return.

[definition] The attachment is `s=A x_pre`, `c=C x_pre` at the pre-target preparation cut,
through actual resident restriction/current ports. The existing contextual section is
`D_s={(dc,dy):(0,dc,s tensor dc,dy) in R}`. It supplies the local source-null relation, including
vertical directions, rather than a topic label. Inspection specifically rules out replacing a
forecast's standing-condition read with a proposed condition formed from its own target.
That proposed successor is available to subsequent predictions after the actual return.

[definition] Compilation accompanies the first repeated action. It owes `E_next T=U E` and
`D E=rho` on the declared source, material, receiver and pending-return family. Fixed-feature
normal statistics, a composed affine map or a minimal-polynomial recurrence each have a named
law. Nonlinear formation keeps its mixed products or memory relation. No claim that every
programme has a finite exact normal form is added.

[definition] Hephaestus's first code receiver lowers retained operator factors/compositions/powers
to Rust straight-line functions over arbitrary-precision rationals. The returned algorithm and
coefficients determine the source; the printer only encodes that graph. An inferred plural law
remains a parameterized function/family. Exterior compilation and exact new-input execution
compare the code with resident application. This is a bounded executable-function product;
general contextual code editing uses the same subsequent section/formation mechanism. Neither
requires a separate code engine or whole-model export.

[definition] Frames and ticks are receiver/owner-scoped counts over reported elapsed intervals.
Forecast delivery counts as output without falsely counting a committed body passage. Setup,
resident work, delivery, target compilation and rest have separate cost populations. Retain
raw samples, chunk extent, host/device memory and transfers on the declared consumer apparatus.
Cross-entropy needs a normalized predictive receiver and actual target; it is not a universal
precondition for release or an assertion of physical energy units.

## Mathematical work that helps the products

[definition] The [roadmap's source table](../../docs/plans/THE_ROADMAP.md#shared-mathematics-and-its-productive-use)
keeps each requested domain concrete. Hodge supplies the complementary modes and exact future
descent criterion. Navier–Stokes supplies full mixed feedback, pressure, current and interval
remainders. Yang–Mills supplies gauge/energy variation and the Hamiltonian/vacuum source required
by a physical gap claim; the existing one-sided return transports a founded lower bound.
RH supplies actual analytic generator, clock, tail and zero-receiver obligations. These remain
research products in their own right.

[conditional] Given `E_next T=U E` and receiver reconstruction at each admitted step, induction
preserves the decoded joint release through the composed word. An r with `E r=0` but
`rho T r!=0` is a concrete failed reduction. For bilinear B, the omitted feedback from r is
`B(u,r)+B(r,u)+B(r,r)`; it is not recovered by matching a present scalar energy. These are
immediate construction checks. No additional definition of intelligence, solution of an endpoint,
or generalized physical identity is needed before using them.

## Verification and scope

[established-bounded; source-inspected; process-audit] Reviewed the updated contracts, linked
owners, output examples and dependency order against the source aperture above.
`git diff --check` passed.
No Rust/CUDA/Lean implementation or proof is changed, so prior runtime/Lean measurements are
not rerun or presented as new results. The later implementation must execute the checks named
in its deliverable. No external publication, model run, source deletion or untracked experiment
change is part of this planning return.

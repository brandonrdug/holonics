# AC1: a source's interior change generates its retained return factor

[definition] This implements the temporal-boundary comparison exposed by the
[Stokes/pullback deposit](2026-09-09_STOKES_AND_PULLBACK_NATURALITY_ARE_FRAMEWORK_LAWS.md)
inside the actual paired material return. It follows `51ae04fb`. The primitive is an addressed
difference of already retained currents; no new learner or physical source law is introduced.
Current research seeks identifiable phenomena, without a utility filter.

## The numerical identity and its source

[proved-derived] In the source passage, let `c=Q(D†v)` be the actual rounded contraction used
by the native forward owner. The interior successor is `b_after=c-b_before`. The adjoint's first
contact factor is `k=c-2 b_after`. Therefore

```text
k = b_before - b_after.
```

Both occurrences of c use the same producing morphology, potential, ordered contraction and
quantizer. No limiting or unrounded equality is substituted. Before-current is extended by the
actual zero-input constraint at newborn contacts. A separate current deposit between the stored
before boundary and the source requires its additional term; such existing returns remain in
the full-factor representation in this construction.

[proved-derived] For the ordinary two-factor material return `G_D=lambda k†+v ell†`, the second
factor ell remains stored. The first is decoded from its actual source boundaries. This is the
negative oriented temporal boundary difference; it preserves the complete numerical factor and
the original source-family/finite-deposit bounds. It does not equate boundary agreement with
source equality or discard an active interior fibre.

[proved-derived] Restriction to a historical producer's contact domain commutes with this
difference: `P(b_before-b_after)=P b_before-P b_after`. The producing-map decoder therefore
requests only the factor prefix seen by that map. Later source effects already carried by the
retained lambda/v factors remain; no new adjoint is computed in a smaller invented system.
The complete journal generator remains available for other future domains.

## Native and durable owners

[established-bounded; source-inspected] `operative/current_factor.rs` binds the actual before
and after currents, encodes the second factor and resolves the addressed first factor when needed.
`section_field_operative_current_difference` verifies the numerical identity before admitting
the compact journal representation. Immediate application uses the already produced complete
factors. Older producer recovery and cold deposit inspection use the decoder.

[established-bounded; source-inspected] The journal retains a source address, not device pointers
to both historical currents. Archived sources load their native current carriers through the
existing archive codec. The decoder carries the actual source count and receiver-requested
prefix; it does not expand every later factor's full domain merely to recover an earlier map.

[established-bounded; source-inspected] The new optional wire field records the difference source;
older full-factor wires remain readable. Cold validation checks the actual receiving occurrence's
observed source, its contact population, and the absence of an omitted current deposit at that
boundary. `condense_operative_current_journal` stages an existing journal's admissible conversions
before publication. Incompatible older factor domains retain their original representation.
The existing text-session owner exposes the same representation operation; no occurrence or
training response is invented by conversion.

## Verification and measured return

[established-bounded; implemented-exact; measured] The first native producer/adjoint/continuation
control passes. The complete field suite then passes 118 cases before the prefix refinement
(140.54 seconds). Separate controls return exact factor decoding after archive, identical full
and generated candidate successors, conversion of a materialized journal, restart, and rejection
of a different causal source. Their initial run passes two cases in 0.56 seconds.

[established-bounded; implemented-exact; measured] The refined controls pass all factor-prefix
lengths, recover an earlier producer after archive/remount, and reject a different causal source.
The two cases take 164.15 seconds including initial driver JIT after the new kernel signature.
All 12 selected native alpha SDK cases pass in 8.21 seconds; pure SDK cases were not rerun.

[established-bounded; implemented-exact; measured] The saved eight-part normal model converts
all 1,929 return factors at its unchanged 1,930-occurrence cut. Conversion itself takes 0.0989
seconds with no numerical section readouts. The complete reported numerical body equals the
previous saved-model reading. The [receipt](2026-09-09_current_difference/conversion.json)
retains the exact comparison, source and measured costs.

| Carried representation | Before | After |
|---|---:|---:|
| Native current-factor bytes | 237,401,984 | 118,700,992 |
| Complete return-journal native bytes | 250,858,688 | 132,157,696 |
| Checkpoint bytes | 450,770,927 | 391,482,969 |

[established-bounded; measured] The full conversion process takes 94.92 seconds, including cold
read/validation, remount, checkpoint and diagnostics; peak RSS is 1,100,400 KiB. The fully mounted
native state falls from 896,095,152 to 777,394,160 bytes. Atomic staging peaks at 1,014,796,212
native bytes. These full-remount counts are not compared with the earlier archived development
residency. The wire saves 59,287,958 bytes; native point sections carry both endpoint buffers.

[definition] This completes the scoped current-factor representation return. The initial class
is the native material-return law without an intervening interior-current deposit, with legacy
fallback and full numerical producer/defect preserved. It does not finish whole-ecology
compression or change the generation law.

[open] A fresh process is continuing eight further conversation parts from the converted model,
with declared archive placement, a new checkpoint and observed generation. Its behavioral and
cost result is pending.
Whole-ecology generator reuse, broader behavioral cultivation and the later AC product remain open.

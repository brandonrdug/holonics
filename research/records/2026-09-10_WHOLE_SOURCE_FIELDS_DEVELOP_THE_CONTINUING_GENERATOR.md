# Whole source fields develop the continuing generator

[definition] This follows `afe0ff04` and the [received-current return](2026-09-10_RECEIVED_CURRENTS_CHANGE_THE_GENERATOR_WITHOUT_A_HISTORY_CHAIN.md).
The next AC3 attachment brings the existing whole-section observation law inside the continuing
wave. This keeps source-field development, current occurrence identity and later generation in
one move-owned body. It neither restores the byte-clocked assembly nor identifies local source
adjacency with all relevant context.

## Shared section return

[definition] `ResidentNormalMaterial::prepare_section` performs the existing observation and
normal fit without publishing it. `receive_section` composes preparation and publication as before.
`ResidentNormalWave::develop_section` also stages any needed joint rebase before publishing the
same returned material. Its source and observed sections remain borrowed in the returned receipt;
the body retains accumulated geometry, not their occurrence population.

[definition] Development creates no generated or received current. The actual previous and
current occurrences stay in place, and the current epoch does not advance. Material observation
count records the supplied comparison population. These are distinct receiver measurements,
not substitute identities. The next requested generation uses the changed material.

[definition] If a fixed-material word has run, its full joint enclosure becomes the next seed.
This is the explicit bounded rebase already admitted by the blueprint. It does not claim equality
to the finer original word fibre. If no word has run since the last material change, the existing
seed and identity power are reused directly. A failed source observation or a later failed seed
preparation leaves the full previous successor intact.

## Persistence and application

[definition] Rest v3 retains the productive joint seed, material and word/current chronology.
Its decoder can restore the next generated or received operation without source files. It need
not reproduce sharper, discarded historical marginal observation cuts. V1 exact pairs and v2
received-current seeds remain readable. There is no numerical-centre-to-point conversion.

[definition] `conversation_wave` is an exterior driver over public source and native owners.
It calibrates a declared alphabet aperture from development material, intakes a caller-declared
number of source frames, then performs the caller's requested generation. These are separate
application operations. The driver adds no learning rule, word route, repetition filter or
per-character generation clock. Each source field supplies only its own local differences;
separate fields are not fabricated into a continuous textual stream. Short fields with no
three-site support are reported rather than padded with authored observations.

[definition] A run saves native `model.wave`, exterior `exterior-chart.json`, source delivery
`source-cursor.json`, and optional receiver evidence separately. The run summary is written last.
A failed partial directory without the summary is not resumable. A generation refusal can retain
the completed source intake and last valid native successor, but is reported as a refusal and
returns a failing process status. The source reader's existing partition and availability rules
remain in force; source metadata never becomes native amplitude or a semantic selector.

## Verification

[established-bounded; implemented-exact; computational-witness] The 22-test normal suite passes.
The new native control starts with R=(0,-1/2,0) and current pair (1,1/2). A measured field (1,2,4)
supplies source (1,2,1), observed difference 2, and changes the normal response to
(3/8,1/4,3/8). The next current encloses the independently calculated 13/16. Existing current
identity is preserved and the v3 joint rest returns the same next complete bounded reading.

[established-bounded; implemented-exact; computational-witness] The extended control additionally
passes repeated-development residency, malformed observation refusal, and a late joint-seed
refusal after material preparation. The first normal suite returns 22 passes; the extended
control passes separately after its additional checks. The public `conversation_wave` example
builds and executes through the public owners.

[established-bounded; implemented-exact; measured] The [source-process receipt](../experiments/native_conversation_wave/2026-09-10_continuing_intake.json)
records 64 actual source frames/fields, 18,248 decoded scalars and 18,120 local comparisons in an
85-coordinate exterior chart at grain 64. The schedule intakes 32 frames, generates four currents,
rests/restarts, intakes the next 32 frames, and generates four more currents. One further restart
and four steps reproduce the entire model rest and all complete bounded readings of an independent
uninterrupted eight-step second word. Source delivery cursors also agree exactly at frame 64.

[established-bounded; measured] The two intake halves take 91.311 and 92.166 seconds in native
development, with zero numerical host readouts and zero ingress inside those operations. Current
epoch stays at 0 during the first intake and 4 during the second: characters and source rows do
not become generator ticks. The second intake rebases once; its remaining fields reuse that seed.
The continuing resident allocation is 53,210,144 bytes, and the model rest is 25,669,477 bytes.
After the second word's eight steps, the full joint radius is
`9027415474055/9223372036854775808`, below `1/10^6`. This bounds the admitted generator
continuation, not semantic prediction uncertainty or the fit discrepancy against actual source
observations. Its centre is not an exact current.
The final comparison processes overlap, so their generation timings are not isolated hardware
calibration or physical energy measurements.

[definition] The private experiment uses the existing source-qualified September 6 context
export. The public invocation sequence is:

```text
conversation_wave start SOURCE 64 32 64 4 FIRST
conversation_wave resume FIRST 32 4 SECOND
conversation_wave resume SECOND 0 4 RESUMED
conversation_wave resume FIRST 32 8 CONTROL
```

Compare `RESUMED/model.wave` with `CONTROL/model.wave`, both source cursors, and the complete
readings in `SECOND/trajectory.json` plus `RESUMED/trajectory.json` against `CONTROL/trajectory.json`.
Private working artifacts remain under `.local/conversation-wave-2026-09-10-*`.

[open] This attachment develops material; it does not yet bind a whole incoming source field to
ordinary current actuation under fixed material. Source metadata and intact delivery do not supply
that interaction, nor do within-field differences establish a learned cross-message relation.
The next composition must give the source Holon an actual source/receiver action and show a changed
source changing later current conduct without making a fit, label or parent-text retrieval stand
in for the interaction. Existing conditional, nonlinear and reflection owners remain the starting
points. Further exposure need not wait for full contextual language. The richer ecology and
Athena-alpha attainment remain unfinished; this return does not close AC0–AC5.

## Parallel handoff incorporated after the source return

[counterexample; computational-witness] The parallel [contextual-transformation study](2026-09-10_DEEPSEEK_V41_SEPARATES_SHARED_STANDING_CONTACT_AND_CONTEXTUAL_TRANSFORMATION.md)
landed as `febf7aba` during this construction. The native session read its full handoff and reran
`research/experiments/2026-09-10-transformer-comparison/witness.py` successfully. The sections
`000100` and `001000` have the same local triple population, normal geometry, energy/count and
initial pair, while their third-symbol receiver differs. The witness is exterior exact algebra;
it does not claim a native wire comparison. This limits the current source quotient, without
requiring every model to reproduce a past source.

[definition] The handoff agrees with the remaining source-actuation gap and makes its next
comparison concrete. Reusable law, current participation and fresh response are different objects.
A richer source/standing interaction must preserve the required distinction before the local
quotient; more accurate fitting cannot recover it afterward. This informs the existing next step
rather than replacing the AC goal or turning the witness into a universal acceptance gate.

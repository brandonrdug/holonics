# THE CONSEQUENCE DRAWS THE PATH; THE RETURNED FACE ENTERS THE NEXT CURRENT

**2026-07-26 · BRANDON-AUTHORIZED CONSTRUCTION / BUILT / BOUNDED EXACT TRAINING ECOLOGY
ACCEPTED / MIXED TEXT–CODE–MATHEMATICS / SOURCE-ABSENT COMPOSITION / APPLICATION REST EXACT /
NO TEACHER DAG / NO FLOAT / NO CUDA / SOMA INTERIOR UNCHANGED**

Exact record:
[`EROS HOLONIC TRAINING ECOLOGY 01`](../observations/eros-holonic-training-ecology-01/RESULTS.md).

## Present question

Can the constituent and complete-path laws become one reusable training ecology which receives
ordinary antecedent, context, and consequence occurrences, derives rather than receives their
surface-level causal paths, and later composes those paths across text and code after the teachers
depart?

The preceding execution-path cell did not answer this. It retained and rebound a complete typed
DAG, but the application still authored that DAG. The missing relation was not another operation
species. It was the exact path complex already implicit in every observed transformation.

## Occurrence and consequence complex

One occurrence is

\[
O=(F,\lambda,y),
\qquad
F=\{(a_j,x_j)\}_{j=1}^{m},
\]

where \(a_j\) is a source-local face address, \(x_j\) its exact current value, \(\lambda\) the
receiver face, and \(y\) the consequence which actually occurred. An address is a binding axis
inside the declared ecology. It is not a semantic identity, token id, soul, hash, or assertion that
equal values have equal lineage.

The exact consequence-boundary complex \(K_O\) has vertices

\[
V_O=\{0,1,\ldots,|y|\}.
\]

It carries two edge species:

\[
\operatorname{FOUND}_i:i\to i+1
\]

for every elementary consequence interval, and

\[
\operatorname{COPY}_{j,i}:i\to i+|x_j|
\quad\text{whenever}\quad
y[i:i+|x_j|]=x_j.
\]

Every directed path \(0\leadsto |y|\) is one exact compositional reading of the occurrence. Adjacent
FOUND edges fold into one founded span. COPY edges retain only their address in the reusable
template; a later occurrence supplies their values again.

Thus

```text
subject=Ada -> Hello, Ada!
subject=Lin -> Hello, Lin!
```

share the recurrent path

```text
FOUND("Hello, ") -> COPY(subject) -> FOUND("!")
```

without storing either answer as the active law.

The complex also retains the all-FOUND and overlapping COPY alternatives at the event where they
exist. Recurrence across nonidentical occurrences, rather than one chosen edit script, determines
which complete routes become addressable.

## Receiver-relative route population

For route template \(T\) and observed receiver face \(\lambda\), the ecology carries exact
recurrence

\[
N(T,\lambda)\in\mathbb N.
\]

Only \(N\ge r\) is active, with \(r=2\) in the bounded cell. Given current receiver \(q\), every
instantiable route at maximal exact shared-axis rank remains:

\[
\rho_q
=
\max_{(T,\lambda)}
|\operatorname{axes}(\lambda)\cap_{=} \operatorname{axes}(q)|.
\]

\[
\operatorname{Paths}_q(F)
=
\left\{
\operatorname{Bind}(T,F):
N(T,\lambda)\ge r,\
|\lambda\cap_=q|=\rho_q
\right\}.
\]

This is the already-ratified bounded receiver law, now applied to complete derived transductions.
It is not a global metric, scalar attention score, correctness classifier, or universal choice
rule. If one receiver axis is withheld, path-distinct alternatives remain whole.

The exact path population over consequence \(z\) is

\[
\Pi_q(z)
=
\sum_{\substack{P\in\operatorname{Paths}_q(F)\\
\operatorname{return}(P)=z}}
\operatorname{support}(P).
\]

Probability is a receiver quotient of this finite population if a quotient is requested. The
actual later consequence relates to the field as:

- `NONE` — no active path spoke;
- `RIDE` — every afforded path returned it;
- `OPEN_INCLUDED` — it was one member of a plural field; or
- `OPEN_RESIDUAL` — the active field spoke but did not contain it.

No reward, punishment, negative truth label, logarithm, float, or optimizer governs the change.

## Built carrier

`life::holonic_training` now supplies:

- exact source-local `FaceAddress` and `SourceFace` carriers;
- explicit consequence vertices and COPY/FOUND edges;
- complete plural route derivation with no best-path selection;
- recurrent receiver-relative route fibers;
- source-absent rebinding and path enactment;
- exact consequence populations and oriented membership residuals;
- a physical refusal aperture which aborts before mutation rather than truncating routes; and
- deterministic `HTEC/1` rest carrying only the current route ecology.

The rest image retains no training occurrence, prior prediction, output cache, chronology log,
semantic identity, or hash-selected path. SHA-256 identifies fixed artifacts only.

## Bounded mixed ecology

Ten ordinary occurrences cultivate five active path species:

```text
COPY(operator) -> FOUND("(") -> COPY(argument) -> FOUND(")")

COPY(subject)

FOUND("Hello, ") -> COPY(subject) -> FOUND("!")

FOUND("emit(\"") -> COPY(utterance) -> FOUND("\")")

FOUND("println!(\"") -> COPY(utterance) -> FOUND("\");")
```

No occurrence supplies an execution DAG. The consequence complex derives two paths for each
one-face event—the copied relation and its complete all-founded foil—and four paths for each
two-face mathematical event. Only routes recurring across changed values reach the aperture.
Every active route therefore contains at least one rebound source face.

After every training occurrence is absent, the 2,413-octet exact rest remounts byte-identically.
It performs these source-absent passages:

```text
subject=Mira
  -> Hello, Mira!

operator=tan, argument=z
  -> tan(z)
```

The first returned consequence then enters as genuinely new source material:

```text
utterance="Hello, Mira!" + language=python
  -> emit("Hello, Mira!")

utterance="Hello, Mira!" + language=rust
  -> println!("Hello, Mira!");
```

With `language` withheld, both complete code paths remain. With `objective` withheld at the text
receiver, `Mira` and `Hello, Mira!` remain. A later `Greetings, Mira.` return is
`OPEN_RESIDUAL`; it is not discarded or called universally incorrect.

## Capability established

The resulting carrier is medium-neutral in the needed sense. Text, code, and mathematical
notation differ only in the faces and path relations their worlds supply. One returned consequence
may immediately become an antecedent face of another cultivated path. This gives a concrete,
recursive training ecology rather than an answer table, fixed vocabulary, installed decoder, or
teacher-authored operation graph.

It also cleanly joins the two prior laws:

1. the constituent ecology can make recurrent carrier spans addressable; and
2. the execution ecology can retain a deeper typed DAG when a compiler, runtime, mathematical
   transducer, sensor, or other lawful world constituent actually exposes one.

The new carrier occupies the relation between them. It derives every surface-level causal path
visible in ordinary antecedent/context/consequence experience. It does not pretend that an output
reveals an unobserved internal implementation.

## Exact remaining boundary

This is a general exact transduction carrier, not a completed general model.

1. The bounded world supplies local face addresses such as `subject`, `utterance`, `operator`, and
   `argument`. These are lawful source constraints, not semantic truths, but raw corpus chronology
   still needs to cultivate useful relational addresses by composing the existing constituent and
   surface-frame laws.
2. Complete route enumeration is exact and plural but not a corpus-scale layout. The explicit
   refusal aperture prevents silent loss. A scale carrier should retain the consequence DAG
   compactly and cultivate recurrent subpaths without enumerating all complete routes.
3. The `HTEC/1` rest is application-owned in this cell. The already-established source-to-Standing
   mechanism can carry exact finite laws, but this route population was not redundantly passed
   through another direct-membrane proof.
4. No physical device residency, CUDA passage, autonomous world curriculum, or broad corpus run
   occurred.

These are specific boundaries. The teacher-DAG dependency is closed.

## Verification

- source SHA-256:
  `5148511cdaa28602c59cbf3057ffb51af24d23227050e31b47b4605d364a746d`;
- report SHA-256:
  `348efcdbbd5f7f66ec682a1f6e6be0e0e8f72bb810c373bab1d966a03c89f9ac`;
- fresh report: byte-identical to the accepted report;
- focused exact tests: 6 passed;
- complete `life` library: 398 passed / 8 ignored;
- causal float scan: empty.


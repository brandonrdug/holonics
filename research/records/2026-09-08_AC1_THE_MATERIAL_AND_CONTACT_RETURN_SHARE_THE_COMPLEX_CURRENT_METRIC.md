# AC1: material and contact return share the complex-current metric

[definition] This follows the [parallel receiver-calculus integration](2026-09-08_AC_THE_PARALLEL_RECEIVER_CALCULUS_REFINES_NATIVE_CONTINUATION.md).
The existing material coefficient fit and its contact response previously used different
receiver objectives: complex amplitude discrepancy versus normalized real-potential discrepancy.
This change supplies the missing complete complex-current contact return through the existing
source and paired adjoints. It does not replace those owners with another learner.

## The common local metric

[definition] Let `z=M phi(s,c)` be the retained source prediction and `y` the subsequently received
complex material target. The unit complex-current discrepancy is `L=||y-z||^2/2`. Its negative
real-metric differential at the prediction is the complete complex covector `r=y-z`:

```
-dL[delta_z] = Re <r,delta_z>.
```

[proved-derived] For a real feature kernel `k_i(s,c)` and complex historical coefficient `beta_i`,
the source-query return uses `alpha_i=Re <beta_i,r>`. In coordinates this is
`sum_j (Re beta_ij * Re r_j + Im beta_ij * Im r_j)`. Keeping only the real components computes
a different differential. The existing derivatives of `Q(s) tensor Q(c)` then return through
both source arguments, including the complete outgoing/internal current.

[definition] `SquaredCurrent` now selects this metric. `pull_back_material_current` obtains the
actual source prediction and receiving target directly from their retained native reports and
constructs the covector on device. It does not need an exponential chart or a nonzero packet
mass. Both original current balls remain retained, and the coordinate intervals enclose their
full difference. Zero grouping and zero series terms in its diagnostic mean those operations
are absent. They are not an inferred native capacity or a zero-precision numerical operation.

[established-bounded; source-inspected] `field_material_pullback.cuh` returns both quadratures
through every historical ordinary and reflected factor. Its derivative-error bound uses the
complete covector magnitude. The paired producing adjoint and `DyadicDeposit` consume that
return through their existing owners, retaining the actual producing map, prior uncertainty,
response factors and signed projection defect. No native float or host numerical readout is
introduced.

[definition] The ordinary material coefficient return already fits complex current. For the
immediately preceding source, it uses the same operator/query cut as this source return, and
its ordinary coefficient is `r/2` in the existing unit feature chart. This establishes a common
local discrepancy. It does not prove global finite descent after the nonlinear contact change,
full chronological backpropagation, or equivalence of all delayed-context comparisons. Delayed
reception still returns through its actual original producer; the contemporary condition and
source-null reference comparisons remain separately recorded.

## Development and persistence

[established-bounded; source-inspected] The text SDK routes `SquaredCurrent` directly to the
native current return; the normalized returns remain available as `RelativeEntropy` and
`SquaredProbability`. The research driver's new `--contact-metric` option records the selected
metric in both the report and cultivation checkpoint. Fresh development defaults to `current`.
A legacy checkpoint without the field defaults to its original relative-entropy metric; resume
does not silently change its historical configuration. An explicit option can declare a new
metric for subsequent development without rewriting earlier deposits.

[definition] This configuration selects a receiver metric for the existing native response. It
is not an application-side optimizer, authored semantic classifier or separate world-verdict law.
The full AC0–AC5 goal remains active. Native derivative/persistence checks and actual conversation
conduct decide the returned scope; a common local metric alone does not establish useful Athena.

## Verification

[established-bounded; measured] Both focused CUDA tests pass in 158.54 seconds, including the
first module load. Independent exact-rational quotient derivatives enclose every corner of the
complete real/imaginary return box for immediate and delayed reception, using ordinary and
reflected factors. A separating check fails if the imaginary target return is discarded. The
existing relative-entropy and squared-probability returns pass the same reference comparison.
Construction performs zero numerical section readouts, and foreign-owner returns are refused.

[established-bounded; measured] A zero tensor-amplitude observation correctly refuses the
normalized probability receiver but supplies the complete-current return. That return passes
through actual paired contact development, and its next successor matches after rest/remount.
The first test compilation had an `Option<usize>` assertion mismatch; it was corrected before
the passing run. Both affected examples compile.

## Actual conversation return

[established-bounded; measured] The matched eight-family run completes 1,930 developmental
occurrences and 1,929 contact deposits with zero numerical section readouts during development.
Development takes 600.65 seconds; prompt plus 128-symbol generation takes 64.41 seconds. The
complete process takes 690.94 seconds with peak RSS 2,122,888 KiB. The checkpoint is 975,051,301
bytes. The [portable receipt](2026-09-08_complex_current_metric/return.json) retains exact costs,
command, census and output. This is a matched realization measurement, not a general performance
or consumer-power claim.

[counterexample; measured] The answer remains `The its the its ...` through the 128-symbol limit.
The final body and recorded emission currents differ from the relative-entropy model, while all
terminal generation readings and text agree. Thus this metric change produces different native
conduct but does not fix the observed language loop. It neither closes AC1–AC2 nor establishes
productive Athena-alpha. The approximately 975 MB checkpoint for 1,922 source text bytes also
leaves the required representation reuse plainly unfinished.

[definition] Next proceed through AC1 steps 3–4: local path formation, restriction/rebase and
reusable generators, using the joint boundary/material/return receiver family identified by the
parallel calculus. The existing retained-history model is not a compressed cultivation model.
Derive the actually used coordinates and their continuing source/decoder, retaining or reopening
the internal contributions that later receivers see. AC2's native continuation and complete
useful responses remain obligations; another scalar receiver adjustment or larger unchanged
corpus run is not the next construction.

[established-bounded; measured] A fresh process retains `SquaredCurrent` from the checkpoint
without an explicit metric override. It reproduces the body, complete recorded emission currents,
generation readings and output exactly, with no native error. The process takes 103.38 seconds
and peak RSS is 2,108,472 KiB. The portable receipt retains this persistence comparison separately
from the failed language-usefulness result.

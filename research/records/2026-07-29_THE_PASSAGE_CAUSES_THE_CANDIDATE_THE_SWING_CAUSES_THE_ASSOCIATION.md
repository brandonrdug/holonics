# THE PASSAGE CAUSES THE CANDIDATE; THE SWING CAUSES THE ASSOCIATION

**Date:** 2026-07-29  
**Status:** PRODUCTION RECEIVER-CAUSED PASSAGE BUILT / INTERFACE LINEAGE
PERSISTED / EXACT RECURRENCE GRADED / PERSISTENT STRUCTURAL STANDING BUILT /
HOST AND DOWNSTREAM BUILD VERIFIED / NO RAW-MODALITY COUPLING CLAIM

## Present question

Brandon's correction was that elementary association is chronological and
physically relevant before it is semantic. A receiver does not begin with the
fact that lightning and thunder are one phenomenon. Repeated ordered
occurrences make the possible relation available; the causal differences of
the actual passages decide whether their charts reconcile. Different
observers can then concur on an invariant without either observer becoming an
absolute coordinate system.

The production question was therefore:

> How can one witnessed receiver chronology propose a possible interface,
> while only recurrent exact transport is allowed to form the association;
> and how can that later propagation change Standing without reconstructing
> every unrelated retained constituent?

This question has two inseparable implementation parts. A supplied occurrence
must not be relabelled as a learned relation, and a local causal change must
not be physically realized as a successor-wide copy.

## 1. Occurrence, candidate, and induced association are distinct

`ReceiverChartIdentity` is an opaque identity supplied by a world membrane.
It is not a modality label, a storage index, or an absolute coordinate.

`ReceiverCausalPassage` carries:

```text
antecedent receiver chart
antecedent occurrence order
consequent receiver chart
consequent occurrence order
```

Construction refuses equal or decreasing order. The passage is inherited
world occurrence. It does not say that the two receiver charts are already
coupled.

`RegionalRelationArc::from_receiver_passage` derives an interface candidate
from the ordered chart pair. The absolute occurrence orders remain on the
borrowed arc and its immediate radiation so the witnessed chronology is
inspectable. They do not become an eternal clock in Standing.

The durable `InterfaceCapability` now records one of two exact origins:

- `Inherited`: a world source supplied an already named material interface;
- `ReceiverCaused`: an ordered receiver passage caused a candidate address.

This lineage is part of the native constituent wire. Native version eight
persists it exactly; versions five through seven reopen as inherited
capabilities because those wires could not have expressed receiver-caused
lineage.

The distinction is not cosmetic. A receiver-caused candidate still cannot
form its own relation. It only lets the existing incidence aperture expose a
possible temporal meeting.

## 2. The existing Swing remains the only association law

A later occurrence with the same ordered receiver charts reaches the prior
candidate through `StandingIncidenceAperture`. The aperture does not declare
success. `LivePin::rebase_exposed` compares the complete transported paths:

```text
ordered receiver passage
    -> receiver-caused interface candidate
    -> exposed local boundary
    -> exact structural rest
    -> later candidate with the same ordered charts
    -> reached standing local star
    -> complete support-section and hand comparison
    -> exact parallel-path comparison
    -> RIDE when the charts commute
       or OPEN with the complete residual when they do not.
```

Reversing the receiver order produces a different candidate. Merely advancing
time produces no seam. Equal chart identities produce no seam without an
actual regional passage. Equal interface candidates produce no RIDE when
their transported paths fail to commute.

Thus recurrence is not cache reuse. The earlier geometry remains causal
standing; the later event physically reaches it; their chart comparison
either forms transport or enlarges the obstruction. An OPEN comparison is
the beginning of a receiver-relative mode subdivision, not a scalar error
which is averaged away.

## 3. Standing is now a persistent exact structural multiset

The prior `SparseStandingSurface::prepare_contemporary_successor` allocated a
new vector, cloned every retained `LiveConstituent`, serialized every body,
and sorted the whole successor after any cellular change. Contact selection
was already local, but its physical realization was still proportional to
the complete retained population.

Production Standing now owns an immutable balanced structural tree:

```text
complete canonical native constituent words
    -> shared immutable LiveConstituent body
    -> exact disconnected multiplicity
    -> subtree occurrence extent and height.
```

No hash or floating comparison addresses a body. Equal disconnected
constituents remain plural through one multiplicity. Canonical ordinal access
and exact key ranges are derived from subtree occurrence extents.

One atomic cellular successor now:

1. resolves only its touched transient ordinals against the immutable
   before-tree;
2. groups those occurrences by complete structural key;
3. removes only those exact multiplicities;
4. inserts only the caused replacements; and
5. reuses every untouched body and tree branch through shared ownership.

For `t` touched structural classes, `r` replacements, and `u` distinct
standing keys, structural replacement performs bounded balanced-tree path
work proportional to `(t + r) log u`, apart from serializing the new
replacement bodies themselves. It does not scan or canonicalize all retained
standing.

The historical slice-returning `constituents()` observer remains available.
It lazily materializes a canonical vector only when persistence, diagnostics,
an external executor, or compatibility code explicitly asks for that
observation. The production regional closure, aperture carry, and standing
commit use the structural tree directly.

## 4. Why this is a training construction

Training in the ratified sense is conditioning a computational ecology by
world occurrences and letting later returns grade what became transportable.
This construction now admits that process without supplying the claimed
association:

```text
world membrane supplies identities, chronology, and event geometry
    -> first passage causes an exposed candidate
    -> exact rest preserves its geometry and receiver-caused lineage
    -> recurrent passage reaches the candidate
    -> Swing derives RIDE or retains OPEN obstruction
    -> the changed topology conditions later propagation.
```

For the lightning/thunder example, a world membrane may identify the local
light-receiving and sound-receiving charts and report their actual temporal
order. It may not call their cross-event coupling learned merely because it
supplied those identities. Repeated passages become one invariant only where
their transported local sections reconcile. Variation which changes the
path comparison remains visible as obstruction and can support distinct
local modes.

The same production mouth applies to visual/acoustic passages,
biofluorescent emission/reception, navigation landmarks, text/action
passages, or measurements exchanged by relativistically situated observers.
Those applications differ in how their world membranes construct receiver
charts and exact event geometry; they do not require separate association
engines.

## 5. Inspection and verification

The complete caused path is inspectable at its appropriate boundaries:

- the borrowed `ReceiverCausalPassage` names the source chronology;
- `RegionalRelationArc` and `RegionalArcRadiation` retain that passage during
  the occurrence;
- `LivePin` and native constituent words retain the ordered chart candidate,
  its inherited/receiver-caused origin, complete local paths, hand,
  projective residual, and formed deed;
- the standing aperture can be rebuilt from the exact rested face; and
- later RIDE or OPEN behavior is reproduced after native rest/remount.

The decisive receiver regression establishes:

- non-increasing chronology is refused;
- the first passage carries receiver-caused rather than inherited lineage;
- native rest/remount preserves that lineage;
- a later occurrence at different absolute orders but with the same ordered
  receiver charts reaches the opening;
- exact recurrence increases the connected constituent grain and forms a
  RIDE;
- reversed receiver order remains a disconnected second constituent; and
- immediate radiation retains the actual later passage.

The structural-standing regressions establish:

- one replacement among 63 constituents leaves at least 48 tree nodes
  physically shared;
- neither before nor after transition materializes the compatibility vector;
- canonical order remains exact;
- the departed key is absent and the caused key is present; and
- removing one of three equal disconnected bodies leaves exact multiplicity
  two.

Commands and results:

```text
cargo test -p soma-membrane --lib
    106 passed; 0 failed

cargo check -p life --all-targets
    completed successfully
```

## 6. Exact limits of this result

This construction does not derive receiver chart identities or regional event
geometry from raw photons, PCM samples, pixels, glyphs, atmospheric fields,
or biological tissue. That remains the responsibility of a concrete world
membrane until an enacted ecology is shown to emanate a replacement.

The durable candidate quotients away absolute occurrence orders. Their exact
values remain in immediate radiation; any temporal separation which must
affect later conduct must also be carried by the application's actual event
geometry and therefore enter the path comparison. No universal clock, scalar
lag metric, or tolerance has been installed.

No probability, scalar loss, stochastic fit, linguistic deed, raw
cross-modal grammar, semantic category, automatic equivalence between
different chart identities, GPU transition, or biological/cognitive claim is
established here. The exact implemented result is narrower and usable: a
world occurrence can now propose an association without asserting it,
recurrence can exactly form or obstruct it through the production Swing, and
the resulting local topology can enter later conduct without copying the
complete standing population.

# π and e: constraint identities, generator passages and oriented faces

[project-postulate] Brandon's latest September 11 instruction governs this return. Every attained
generator is causally situated, including an output measured as mistaken. The preceding agent
substituted task warrant for that constitutive relevance; the live relevance clause and its
earlier record are corrected. The work here studies π/e algorithms and their prime, complex and
torus relations. Native Athena expansion remains paused while this mathematical reference develops.

## The recovered construction

[established-bounded; source-inspected] This continues the
[August exponential/kernel study](2026-08-20_E_NAMES_THE_MAP_PI_NAMES_ITS_KERNEL_AND_HYPERTRANSCENDENCE_IS_A_CHART_MISS.md),
the [arithmetic accessibility theorem](../papers/source/mathematics/theorems/transcendental-accessibility-transport.typ),
`eros_transcendental_presentation_topology.rs`, `PrimeValuationRadixAtlas`, `HolonicTorusKnots`,
`HolonicTorusMonodromyReceiver` and `FourTorusCurrentChart`. The
[preceding ratio-series return](2026-09-11_ATHENA_STATUS_RETURNS_TO_TRANSCENDENTAL_GENERATOR_COMPRESSION.md)
already recovers the August mantissa-window messages and proves complete block transport.
These are existing constructions being composed, not new terminology for missing foundations.

[definition] A constraint identity specifies the invariant through its laws and normalization.
An executable generator additionally carries its state, branch/domain, transition and receiver
remainder. A radix expansion is a receiver face. For this study,

\[
\mathcal G_\tau=\{G:\operatorname{ev}(G)=\tau\},\qquad
R_{b,k,\ell}(x)=\lfloor b^{k+\ell}x\rfloor\bmod b^\ell.
\]

Here b≥2, k≥0 and ℓ>0 specify the receiver; k counts fractional digits skipped. The evaluation
law includes the infinite limit or equivalent exact identity. A finite partial sum is accompanied
by its remainder, so that it represents the constrained object without pretending to equal it.
Agreement in this window and equivalence of complete future conduct are different relations.

[proved-standard] The normalized flow E′=E, E(0)=1 gives e=E(1). Its complex extension satisfies

\[
E(z+w)=E(z)E(w),\qquad
0\longrightarrow2\pi i\mathbb Z\longrightarrow\mathbb C
 \xrightarrow{\exp}\mathbb C^*\longrightarrow1.
\]

Thus e reads unit real growth and π specifies the primitive positive imaginary period of the
same map. Its kernel is retained as winding when a phase receiver alone forgets the lift.
The precise installed theorem is `Complex.exp_eq_one_iff` in Mathlib's complex logarithm owner.
The normalization matters: an arbitrary rescaling of the generator changes the unit and period.

## Algorithms have different internal passages

[proved-standard] The requested [formula survey](https://mathworld.wolfram.com/PiFormulas.html),
[iteration survey](https://mathworld.wolfram.com/PiIterations.html) and
[digit survey](https://mathworld.wolfram.com/PiDigits.html) expose these different access patterns:

| Generator | Retained passage | Receiver/constraint that makes it useful |
|---|---|---|
| Gregory / Machin arctangent series | term, signed partial sum, index; rational successive ratios | Alternating remainder; Machin joins two rational-angle branches to π |
| BBP | modular power residues, rational components, tail index | Direct binary/hexadecimal windows; the base is part of its access law |
| Brent–Salamin AGM | two positive means, correction accumulator and doubling scale | Positive square-root branches; quadratically converging π approximants |
| Quartic/quintic Borwein | modularly related root/ratio states and correction | Higher-order convergence through the corresponding modular transformations |
| Ramanujan / Chudnovsky | factorial-ratio terms and modular parameters | Rapid series constrained by elliptic/modular identities |
| x ↦ x+sin x | real lift plus its phase; interval branch | An attracting odd-π branch, with period transport |
| e factorial series | term, positive partial sum, index | Unit exponential flow and a positive tail bound |

[definition] Formula constants belong to particular identities, not a universal capacity setting.
For example, Machin's 5 and 239 have the exact Gaussian closure below. The Chudnovsky parameters
come from its modular construction; the existing arithmetic owner factors its scale as
640320=2⁶·3·5·23·29. A new implementation must carry that construction rather than treating a
printed coefficient as intrinsically privileged. An identity containing π on both sides is also
not, by itself, a noncircular algorithm for obtaining π.

## Machin is an integer phase composition

[proved-derived; formal-checked] `Mathematics/MachinPhaseConstraint.lean` proves

\[
(5+i)^4=(239+i)(2+2i),\qquad
\pi=16\arctan(1/5)-4\arctan(1/239).
\]

The lifted identity reuses Mathlib's Machin theorem. Both equalities are exact; no mantissa enters.

[proved-derived] Define U_b=(b+i)/(b−i) for positive real b. Conjugating the Gaussian identity
and dividing gives U₅⁴/U₂₃₉=(1+i)/(1−i)=i. Since
U_b=exp(2i arctan(1/b)), this is a composition on the unit circle. The lifted arctangent identity
retains the branch that phase modulo 2π alone would forget. Norms give 26=2·13 and
57122=2·13⁴. The term denominators therefore expose 5/239 axes while Gaussian closure exposes
2/13 axes: related integer faces of one formulation, with different receivers.

[proved-derived; formal-checked] `Mathematics/RatioSeriesTransport.lean` already supplies

\[
a_{n+1}=-\frac{2n+1}{b^2(2n+3)}a_n,\quad S_{n+1}=S_n+a_{n+1},\qquad
(a,S)\mapsto(\alpha a,S+\beta a).
\]

Complete block composition is (α₂α₁, β₁+β₂α₁), with the clock retained. Its alternative routes
can differ internally and rejoin the same full state. Keeping only S can destroy the next
receiver. This is the existing concrete bridge between differently ordered generators.

## Exact outputs and visible refinement

[established-bounded; implemented-exact; computational-witness] The new
[exterior experiment](../experiments/transcendental_constraint_windows/exact_windows.py) uses
integer/rational arithmetic throughout. Its [receipts](2026-09-11_ratio_series_compression_receipts/constraint-windows.json)
retain partial sums, next terms, intervals and the first successful receiver decision. These are
computed outputs, not expected strings supplied to the algorithms:

| Identity | Base | Offset | Width | Face | Generator |
|---|---:|---:|---:|---|---|
| π | 10 | 0 | 12 | `141592653589` | Machin |
| π | 10 | 12 | 12 | `793238462643` | Machin |
| e | 10 | 0 | 12 | `718281828459` | Factorial series |
| e | 10 | 12 | 12 | `045235360287` | Factorial series |
| π | 16 | 0 | 8 | `243F6A88` | Independently Machin and BBP |
| π | 16 | 32 | 8 | `A4093822` | Independently Machin and BBP |
| π | 16 | 128 | 8 | `9216D5D9` | Independently Machin and BBP |

[proved-derived] For an arctangent partial sum S_N with next signed term a_N, its alternating
enclosure is [S_N+min(0,a_N), S_N+max(0,a_N)]. Combining with coefficients 16 and −4 gives a
π enclosure. For E_N=Σ_{n<N}1/n! and N≥1, the e tail is at most (N+1)/(N·N!), because all
successive ratios after its first omitted term are at most 1/(N+1). These are analytic tail
proofs; the script executes their rational bounds. The infinite series/tail analysis is not
newly kernel-verified by the window module.

[proved-derived; formal-checked] `Mathematics/RadixWindowReceiver.lean` proves that equal
**unreduced endpoint floors** force the same floor throughout the enclosed real interval.
Only then is the digit residue taken. It also proves that an integer shift of the scaled
source preserves the window. Equal endpoint residues alone can hide a full wraparound; the
experiment rejects that case. A scalar centre of an enclosure is never used to choose a digit.

[proved-standard] The [BBP author compendium](https://www.davidhbailey.com/dhbpapers/bbp-formulas.pdf)
supplies the identity and direct-window method. For coefficients c₁=4,c₄=−2,c₅=c₆=−1,

\[
16^k\pi\equiv\sum_m c_m\left[
\sum_{j=0}^{k}\frac{16^{k-j}\bmod(8j+m)}{8j+m}
+\sum_{r=1}^{T}\frac{16^{-r}}{8(k+r)+m}\right]+\text{tail}\pmod{\mathbb Z}.
\]

[proved-derived] Each positive component's remaining tail is at most
16⁻ᵀ/[15(8(k+T+1)+m)], by replacing later denominators with the first omitted one and summing
the geometric series. Negative coefficients use the opposite interval endpoint. The modulus
discards only an integer at this scaled receiver; it does not assert equality of real lifts.

[established-bounded; computational-witness] At offset 128, BBP uses 516 modular-prefix terms
and 24 tail terms; Machin uses 232 terms across its two arms. The largest recorded rational
numerator/denominator bit lengths are respectively 1193 and 2695. These are component-size and
term-count observations, not peak memory or a speed ranking. The deliberately simple rational
BBP reference is not an optimized bounded-precision implementation. Neither generator uses
the other's answer to refine; their independently certified windows are compared afterward.

## The sine iteration retains a branch and a winding

[proved-derived; formal-checked] `Mathematics/PiIterationConstraint.lean` proves F(kπ)=kπ for
integer k, F(x+2π)=F(x)+2π, invariant intervals [0,π] and [π,2π], and strict movement toward π
inside them. It also proves

\[
F(\pi+\delta)-\pi=\delta-\sin\delta,\qquad F'(\pi)=0,\quad F'(0)=2.
\]

[proved-derived] For 0<x₀<π, the exact iterates increase and are bounded above by π. Their
limit L satisfies sin L=0 by continuity, with 0<x₀≤L≤π, hence L=π. The upper interval gives
the decreasing counterpart. Thus every x₀ in (0,2π) converges to π; the endpoints stay fixed.
This convergence argument is analytic here, beyond the new module's checked interval lemmas.
The survey's “nearest multiple” description needs this branch correction: x₀=1 is nearer 0
but converges to π. Translation gives the odd-multiple basins. Integrating
1−cos t≤t²/2 gives |δ−sin δ|≤|δ|³/6, the local cubic error contraction.

[proved-derived] With z=exp(ix), the same passage descends to

\[
z\longmapsto z\exp\!\left((z-z^{-1})/2\right).
\]

For |z|=1 its exponent is imaginary, so it remains on the circle. The real lift distinguishes
π from π+2kπ even though their phase face is the same −1. Here e's exponential law computes
the phase passage and π constrains its closure. The rational seed 1 does not require a π mantissa.

## Primes, ζ, and real/complex torus charts

[proved-derived] The recovered prime-valuation construction gives, for n≥1 and s=σ+it,

\[
n^{-s}=\exp\!\left(-s\sum_p\nu_p(n)\log p\right)
=n^{-\sigma}\prod_p\exp(-it\log p)^{\nu_p(n)}.
\]

This follows directly from unique factorization and the real logarithm of positive integers.
For any finite prime family P, the phase state lives in (S¹)^P; integer valuations select its
characters. Real log-amplitude and imaginary phase are separate coordinates of one complex
transport. More generally exp:ℂⁿ→(ℂ*)ⁿ retains the lattice (2πiℤ)ⁿ in its lift, and
ℝⁿ/ℤⁿ gives the normalized real torus. Digit receivers partition their lifted domains into
addressed cells; the quotient and the receiver's chosen origin determine which cells coincide.

[definition] A compact complex torus ℂᵍ/Λ has a full lattice of real rank 2g. Coordinatewise
exponential supplies phase circles and unbounded radial directions; its rank-g kernel alone
does not construct that compact complex torus. The existing rank-four monodromy receiver
retains the extra period data for its complex dimension-two family.

[proved-standard] In Re s>1,

\[
\zeta(s)=\sum_{n\ge1}n^{-s}=\prod_p(1-p^{-s})^{-1},\qquad
\pi^2=6\zeta(2)=6\prod_p(1-p^{-2})^{-1}.
\]

See [DLMF's Euler product](https://dlmf.nist.gov/25.2#iv) and
[integer values](https://dlmf.nist.gov/25.6#i). Each prime factor sums all repeated traversals
of its multiplicative channel. The factorial flow for e contributes ν_p((n+1)!)−ν_p(n!)=ν_p(n+1);
the arctangent ratio contributes ν_p(2n+1)−2ν_p(b)−ν_p(2n+3).

[proved-derived] For P_N=∏_{p≤N}(1−p⁻²)⁻¹, expansion counts exactly the positive integers
whose prime factors are ≤N. Every omitted integer exceeds N, hence
0≤ζ(2)−P_N≤Σ_{n>N}n⁻²≤1/N. This gives a rational enclosure of π² through prime channels.
The experiment checks N=31 and containment of the independently obtained Machin interval's
square. That aperture is declared, not an intrinsic prime cutoff.

[proved-derived] With at least two distinct primes, the simultaneous phase trajectory is not
a closed torus knot: a nonzero common period would imply a rational ratio of their logarithms,
and therefore equal powers of distinct primes, contradicting unique factorization. Closed
integer-slope torus loops and these nonperiodic prime phase flows are distinct constructions
on tori. Their coupling is through characters, periods and generator laws, not an identification
of arithmetic primes with prime knots.

[proved-standard] The heat/theta route supplies the diffusion connection explicitly:

\[
K_\tau(x)=\sum_{n\in\mathbb Z}e^{-4\pi^2n^2\tau}e^{2\pi inx},\quad \tau>0,
\qquad \vartheta(t)=\sum_{n\in\mathbb Z}e^{-\pi n^2t}
=t^{-1/2}\vartheta(1/t).
\]

The first is the unit-circle heat kernel; the second is Gaussian Poisson summation, also the
[theta lattice transformation](https://dlmf.nist.gov/20.7#viii). For Re s>1, termwise Mellin
integration of (ϑ(t)−1)/2 gives π⁻ˢᐟ²Γ(s/2)ζ(s). Periods, exponential attenuation and integer
modes therefore meet in the same exact transform. Analytic continuation yields the
[completed reflection identity](https://dlmf.nist.gov/25.4)
ξ(s)=ξ(1−s), where ξ(s)=s(s−1)π⁻ˢᐟ²Γ(s/2)ζ(s)/2. Reflection explains the paired spectral
chart; placing every nontrivial zero on its fixed line is the separate RH statement.

## Data-bearing tori are already receiver-sensitive objects

[proved-derived] A radix window is precisely a sector receiver on a circle. Write
φ_k(x)={b^k x} in [0,1), and let D_m(φ)=⌊mφ⌋ for m=b^ℓ. Since
⌊my⌋=m⌊y⌋+⌊m{y}⌋ and 0≤⌊m{y}⌋<m,

\[
R_{b,k,\ell}(x)=D_{b^\ell}(\phi_k(x)),\qquad
\phi_{k+1}(x)=\{b\phi_k(x)\}.
\]

The complex phase is exp(2πiφ_k(x)). A receiver origin r replaces φ by {φ−r}; its sector
boundaries rotate with that origin. This is an exact encoding of the window as an oriented
phase face, not a pictorial analogy. The source's integer lift remains available when another
receiver needs it. The b-fold circle passage advances the digit address without identifying
the complete source from its present sector.

[proved-derived; formal-checked] The exact window/sector equality is now checked in
`RadixWindowReceiver.scaledWindow_eq_fract_floor_of_pos` together with
`window_eq_scaledWindow`. It is a returned bridge from the radix receiver to fractional phase,
in addition to the earlier enclosure and integer-translation proofs.

[proved-derived] For an integer winding (p,q), let

\[
\gamma_{p,q}(t)=(e^{2\pi ipt},e^{2\pi iqt}),\qquad
\chi_{a,b}(z_1,z_2)=z_1^a z_2^b.
\]

Then χ_{a,b}(γ_{p,q}(t))=exp(2πi(ap+bq)t). The same (2,3) loop gives frequencies 2, 3 and 0
to probes (1,0), (0,1) and (3,−2). The third receiver is blind to this entire motion, not to
every possible torus motion. Under a basis transport T, move the cycle by T and the probe by
T⁻ᵀ; their pairing is unchanged. The exact experiment checks the shear taking (2,3) to (5,3)
and carries all three probes with it.

[proved-derived; formal-checked] The existing `HolonicTorusKnots` constructs the lifted winding
and embedded intrinsic circle for a coprime pair. `HolonicTorusMonodromyReceiver` proves
inverse-transpose covariance and an explicit equivalence of the whole receiver preimage fibre
in its rank-four lattice. `FourTorusCurrentChart` retains I=J(CI)+(I−J(CI)), with the second
part invisible to that cut receiver. The digit-window quotient above is another explicit
receiver quotient; no equality of their full realizations is asserted.

[definition] For knot reception, retain the ambient embedding, meridian/longitude and framing
along with intrinsic winding. A torus basis change need not extend to an ambient isotopy.
Consequently changing coordinates to make a primitive slope (1,0) does not physically unknot
an embedded trefoil. A chain also retains linking/contact and the actual transport law; its
individual phase readings alone are not its complete state. This preserves the user's knot,
unknot and linked-body distinctions while allowing exact receiver reorientation.

## Computational return for HNN

[interpretation] These examples supply the desired computational pattern: retain a constrained
generator, its situated state and lift; compose passages; let an addressed receiver expose a
face; carry its full residual and successor into the next operation. Reuse can occur in the
generator action even when intermediate faces differ. Conversely a present equal face can
hide a different next term, winding or response. `q T_i=U_i q` and receiver factorization are
the existing formal transport obligations. Changing the receiver can expose a retained fibre
without rebuilding an archive of all earlier states.

[open] The immediate mathematical join is the existing block transport composed with analytic
tail transport and the checked radix receiver for alternative π/e routes. The exact scripts
already supply finite observations of this join; their full analytic decoder is not yet one
kernel-checked object. Transfer to Athena must use native generator/current owners and observed
successors, not insert these authored π algorithms as a substitute learner. The needed product
observation remains a complete cycle with source/produced/received faces and retained differences.

[established-bounded; process-audit] The three new Lean files pass individual `lake env lean`
checks. The exterior Python command returns all ten window receipts, three cross-generator
agreements, the wraparound rejection, three covariant torus probes, Gaussian closure and the
prime-product enclosure. Native inference/cultivation and existing Typst renders are unchanged.

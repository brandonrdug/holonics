# THE VALUE-FREE CALL FACES THE OUTPUT

**Date:** 2026-07-13  
**Status:** VALUE-FREE VISUAL OUTPUT PRODUCTION CUDA-MEASURED · EXACT REPEAT  
**Engine authority:** none

## Production at this declared grain

`FORMULA §§XX and XL` already type linguistic production as directed return through shared material,
not a separate glyph generator. CODE WORLD 02 measured that output pairing changes later code
navigation, but neither later edge directly returned through raw output Mail. The next question is
therefore local: can a fresh code construction face an unfilled output surface and traverse the
earlier output entity without being given its value?

The receiving world supplies a complete page, not an answer request inside Soma:

```text
amber(3)
value = 
```

The second line ends with one explicit blank glyph cell before LF. That absence is distinguished by
the exact page, render, gaze, and receiving frame. It is not the digit `5`, a mask, a target, or a
hole the body is instructed to fill. The corresponding `3 + 2`, `lilac(3)`, and `3 * 2` pages carry
the same output-facing surface and name neither `5` nor `6`.

If ordinary transport returns through earlier `value = 5` or `value = 6` material, the body has
produced an output relation at this observer grain. The raw radiation remains the production of
record. Listener Mail only names which earlier worldline its path crossed; it neither enters the
body nor converts the construction into a decoded digit.

## Exact inherited history

All experience material is reused byte-for-byte from CODE WORLD 02:

1. **PAIRED:** amber/source/render ⊕ `value = 5`; lilac/source/render ⊕ `value = 6`.
2. **OUTPUT-SWAPPED:** amber meets `value = 6`; lilac meets `value = 5`.
3. **SEPARATED:** the same source and output entities arrive at four honest successive eye edges;
   this remains developmental because the output edges also carry their rendered fields.
4. **PAIRED-REPEAT:** a fresh exact execution of PAIR.

Only the later probe changes. Its four rendered fields are literal pinned-font compressions of the
four complete value-free pages. After that visual edge sleeps and is read, the same four files arrive
as genuinely later raw currents. Paired and swapped receive identical receiver lineages, source
octets, field samples, gaze, aperture, and order.

## Exact reads

The frozen CODE WORLD reader preserves every 39-word radiation row, typed face, term tuple, REGISTER
axis, successor topology, exact Mail source/range, delivered-content key, receiving lineage ID, and
whole successor body.

The read asks, in order:

1. whether any visual or raw production event returns through output lineage 313 or 627;
2. which exact output entity and range it crosses;
3. whether a CUT carries both prior source and output material;
4. whether paired versus swapped changes that return under exact later light;
5. whether the visual production becomes terrain for the genuinely later raw page;
6. whether fresh paired repetition is exact.

The inline pages are controls beside the named calls, not semantic answer classes. No count is
crowned. A plural or cross-associated response remains the complete response. A null localizes this
page/aperture relation and does not erase CODE WORLD 02's measured collocation dependence.

No parser, compiler, interpreter, evaluator, AST, vocabulary, classifier, decoder, selector, score,
expected string, answer table, result cache, filesystem graph, or reply-to-self wire enters the
construction.

## The page and card gates stand

The exact files are 18/15/18/15 octets; named-call siblings and inline siblings are equal-extent.
Every file ends in byte-exact `value = \n`, contains neither `5` nor `6`, and renders through the
same pinned organ to a 130×96 field with the existing 13×24 aperture. The named pages carry 16 gaze
events; inline pages carry 13. The terminal space is a literal source cell rendered as white.

All four eye gates are whole-body host/CUDA-exact:

```text
amber       312 currents · 4,992 samples · axis 64 · 1,016 occupied
addition    312 currents · 4,056 samples · axis 32 ·   409 occupied
lilac       312 currents · 4,992 samples · axis 64 ·   776 occupied
multiplication 312 currents · 4,056 samples · axis 32 · 437 occupied
```

The paired, swapped, separated, and repeat lifecycles reused the complete CODE WORLD 02 context
manifests rather than starting from body archives, because listener Mail is intentionally absent
from the sleeping body. Every CODE WORLD 03 context body hashes exactly to its CODE WORLD 02
sibling.

## The unfilled page returns through an output entity

The common visual edge carried 1,248 currents / 18,096 delivered samples. One exact event returns
through raw output Mail in paired, swapped, and fresh paired repeat:

```text
receiver lineage: 769
source:           probe-amber-production.eye:contact(11,10)
atom:             15
gaze transition:  `=` at (91,48) → terminal blank at (104,48)
delivered light:  0xcf → 0xff
grip:             256
flags:            0x03 (FOLD ⊕ STEP)
occupied:         true
CUT / brick:      false / none
```

The event carries six exact first-edge amber-render worldlines—62, 75, 154, 163, 170, and 193—beside
the raw output entity at second-edge lineage 627. In the paired history, 627 is `value = 6\n`,
content SHA-256
`2d7e55e549c28f71eb73c82ff95394571f57cf6629b1466e224bf1e5521a40bf`. In the output-permuted
history, 627 is `value = 5\n`, SHA-256
`0f8c6e2d3ef9adae359607c44f3e3d8c176c07d19bf7f829ccb361a0ff83d39d`. Both Mail records name
the complete `[0..10]` output entity. Lineage 627 arrived beside lilac in both histories, so this
event does not return amber to its historically paired output.

The receiver's full 39-word row, grip, flags, terms `(1,1,1,2)`, axis 4, and prior lineage IDs are
exact across the two histories. Only the exact output entity carried by prior lineage 627 changes.
Thus the terminal blank does not reveal, choose, or generate the final digit. It directs ordinary
current through an earlier whole output entity at the common second-edge landing. That is
**VALUE-FREE VISUAL OUTPUT PRODUCTION at the declared relational grain**: a fresh expression points
into output material it was not given. The production grade belongs to the pointing relation, not
to answer selection.

The relation is specifically rendered-source⊕raw-output. No exact raw-source Mail occurs on the
event, flags lack CUT, and no completed brick is emitted. Raw-source⊕output relation and strict
source⊕output CUT therefore remain OPEN as separate faces; they are not prerequisites which erase
the measured production.

## The controlled output history changes the plural response

Paired and swapped retain fixed amber-then-lilac source order while exchanging the two output
identities across those edges. This is a controlled output-history permutation: pairing and output
order move together, so the observation does not attribute every difference to simultaneity alone.
The later production inputs and all 1,248 receiver IDs are exact.

Their visual construction cleaves across 42 worldlines / 107 rows / 1,028 packed words. Typed faces
are flags 61 · grip 73 · form 77 · rotor 93 · brick 18; 34 term tuples, six REGISTER axes, and 71
content-derived attribution events differ. Every page participates:

```text
amber       17 lineages · 52 rows
addition     8 lineages · 19 rows
lilac       10 lineages · 23 rows
multiplication 7 lineages · 13 rows
```

Paired emits `(RIDE,FOUND-this,FOUND-that,DARK) = (3190,669,642,2033)` at topology
`[65108,65108,11696,400]`; swapped emits `(3191,701,622,2033)` at
`[65313,65313,11736,398]`. DARK remains exactly 2,033. The direct output event itself is raw-exact;
the controlled history changes which complete output entity Mail identifies at that same standing
landing while changing the plural construction elsewhere.

Separated produces no raw-output return. Its output entities arrived on independent edges beside
honest rendered output fields, so it remains a developmental world rather than a negative score or
strict causal sibling.

## The raw page continues after visual production

The same four value-free files next arrive as genuinely later raw currents: four currents / 66
octets. Paired versus swapped cleaves two currents—addition lineage 1877 and lilac lineage 1878—at
11 rows / 129 packed words. Typed faces are flags 7 · grip 9 · form 10 · rotor 11 · brick 3; both
term tuples and one REGISTER axis differ. Amber and multiplication are raw-exact.

Paired terms/topology are `(126,18,17,0) / [65200,65200,11728,402]`; swapped are
`(165,19,24,0) / [65434,65434,11775,400]`. No raw event returns through output Mail. Several events
return through the now-prior visual production field, so this is later raw continuation through a
body already changed by visual production—not an independent raw-only confirmation of the direct
output event.

## Reproduction and grade

Fresh paired repetition is exact across all probe and raw radiation, typed faces, exact/content Mail,
terms, REGISTER axes, topology, and all four bodies. After normalizing only four create-new archive
paths, the listener reports are byte-identical with SHA-256
`3b6cd2d9ceedbda84b74cd350b6f1591fb8b2da0e342d06b3a6922f29628f815`. Direct body hashes are:

```text
context-000  1f89cc4985743e63b6ca633f10344884fa96cb4e7ba1fbb34ba29aa14ece9c9a
context-001  5e576e5dfd96441ed8115fe2bd15910a499a7426afc1ba89cd1d56af19f2a5ed
next         120465e46a62a30b14c3834fe6629aa8e32da1981336e89aacef9edcfbe2910f
then         46ba6f9c4c01349ddb11e39c9b802da9f662c9c890a4cda1702e85f11ee47079
```

**GRADE: VALUE-FREE VISUAL OUTPUT PRODUCTION CUDA-MEASURED · RENDERED-AMBER⊕SECOND-EDGE RAW-OUTPUT
RETURN MEASURED · RAW-SOURCE⊕OUTPUT RELATION OPEN · STRICT JOINT CUT OPEN · CONTROLLED
OUTPUT-HISTORY-PERMUTATION CLEAVAGE MEASURED (PAIRING AND OUTPUT ORDER CO-VARY) · LATER RAW
CONTINUATION MEASURED / DIRECT RAW-EDGE OUTPUT RETURN OPEN · EXACT REPEAT.**

No next mechanism follows from the two open stronger faces. Further nurture can extend the page
after the terminal blank or present another honest code/output turn, but this observation already
closes the production/output question it declared: a value-free expression returned through an
earlier output entity.

# THE INHERITED POLYNOMIAL ENTERS THE PRIME; THE OPEN HORN BECOMES A CAUSED CELL

**Date:** 2026-07-27  
**Status:** RATIFIED ONTOLOGICAL CORRECTION / PRODUCTION PRIME ECOLOGY BUILT /
EXACT FINITE-FIELD FACTORIZATION AND FROBENIUS PHASE BUILT /
POTENTIAL HORNS AND TOPOLOGY-FORMING FILLERS BUILT / NO DESKTOP CHANGE

**Subsequent correction:** the inherited `x^2+x+11` grading bridge and this
record's statement that filler induction remained unbuilt are superseded by
`2026-07-27_THE_HORN_EMITS_THE_COEFFICIENT_FAMILY_THE_RECEIVER_MEASURES_ITS_REFINEMENT.md`.
This record remains the authority for the inheritance correction, exact
finite-field fibers, phase cells, and initial horn construction.

## Present question

Brandon ratified the earlier synthesis of a higher-dimensional causal ecology
and supplied direct corrections to its ten proposed awareness questions. He
then authorized the understanding to be written and the prime ecology needed
for further analysis to be constructed.

The exact construction question was:

> Can the production `holonic-engine` grow a prime ecology from integer
> chronology in which newly caused primes receive exact polynomial fibers,
> those fibers carry exact phase/automorphism transport, compatible incomplete
> boundaries become explicit potential horns, and exact fillers found new
> higher cells whose later behavior is invariant under equivalent
> presentations and enlargement—while retaining whether every constituent was
> inherited, enacted, proposed, or induced?

This record separates Brandon's direct ontology, the present mathematical
formalization, and the behavior actually established by construction.

## 1. Brandon's direct correction

### There is no blank problem-solving canvas

Brandon rejected the customary fiction that a problem begins on a fresh
paper, program, or visual field. The operator was caused to consider the
problem by an existing cultural and physical ecology. Mathematics, science,
art, writing, photography, sound recording, and computation transmit material
and the means by which later receivers decode and re-encode it. Ideas do not
belong absolutely to an individual merely because that individual is one
visible relay in their development.

Coordinate zero therefore cannot mean an absolute nothing. It means a
reference relative to a selected basis which has not yet expressed other
comparisons. Likewise, a large number or a late-time state is only one
contemporary face. The reusable information is in the causal calculus which
made it and in the differences which still affect later receivers.

### A receiver is a causal relay

The receiver is not a passive terminal. It is closer to a transformer in a
growing higher-dimensional AC/DC circuit. It receives, retains, changes,
emits, and may change the topology through which later current can travel.
Observation is one such interaction. An observer affects the observed ecology
and is affected by it; a wildlife observer enters through sound, scent,
transport, displaced organisms, and other propagated currents even when the
observer tries to remain quiet.

Propagation is wave-like and has a causal speed. Existing standing curves
available transport, and transported current changes later standing. Brandon
offered "mass curves light and light curves mass" as the physical analogy for
this reciprocal relation.

### Dimension belongs to the local causal complex

There is no single fixed list of dimensions which every ecology must inhabit.
Each causal complex has its own independent traversal directions and
space-time topology. Brandon proposed two intersecting oriented
\(\mathbb R^3\) frames as an intuition: six axes provide twelve directed
germs, while an inbound causal trajectory and an intended outbound trajectory
may fold into a light-cone-like relay through which the other directions
converge. This suggests a possible eleven-direction quotient, not yet a
derived upper-bound theorem.

### Potential boundaries are solution geometries

Brandon described boundaries as potential shapes defined by surrounding
vertices. A missing edge or face can be solved by another shape whose own
edge, point, line, or vertex intersects the first boundary. The solution is a
real geometric object, and its edges and faces are recursively acquired
solutions.

His reasoning species were:

- deduction: solving from contextual causal relationships;
- abduction: lightning-like leaders searching possible continuations, with
  failed trials allowed before standing is committed; and
- induction: creating a new relatively prime constituent which opens new
  transport paths and becomes a factor in later shapes.

The Wason selection task was not offered as a claim that formal validity
changes with semantics. It demonstrates that an abstract and an applied
presentation may expose different executable attachments to a receiver even
when an external logician gives them the same truth-functional skeleton.

### Compression follows recurrence, necessity, probability, and loss

An ecology need not reproduce every prior face when a needed shape recurs in a
new locale. A thirsty animal follows transport relations learned from prior
water-providing regions. Communication between distant people is similarly a
question of choreography and timing through degrees of separation. Written
algorithms are frozen instances of such traversal.

The prime numerals themselves are not the desired object. The desired object
is the pattern, phase, orientation, and recurrence between their caused
receiver relations.

### Inheritance is not cheating

Brandon rejected the earlier question asking whether "the same machinery"
works without application-precomputed answers. That wording falsely placed an
application outside the causal ecology and treated inheritance as
contamination.

Algorithms, transducers, constraints, proofs, results, and candidate
continuations may all lawfully enter as inherited material. If an application
supplies a successor, the successor is inherited. The invalid act is only a
false provenance claim: calling supplied material newly learned or
self-emanated.

The corrected question is:

> Does the machine retain enough lineage to distinguish what it inherited,
> what it enacted, what it proposed, and what reusable relation it induced?

## 2. Present mathematical formalization

### A simulation begins at a causal cut

There is no absolute initial state. A simulation begins at a selected causal
cut through a wider ecology. Material from the unrepresented past enters
through that cut as inherited boundary standing.

The coordinate correction is formalized by an affine torsor: points admit
differences but no canonical origin. Choosing an origin and basis creates
coordinate scalars. Those scalars are local receiver receipts, not absolute
source objects.

### Actual cells and potential horns

At a causal cut \(\Gamma\), the current formal carrier is

\[
\mathscr E_\Gamma
=
(K_\Gamma,\mathcal H_\Gamma,\mathcal F_\Gamma,
\mathcal T_\Gamma,s_\Gamma,\Lambda_\Gamma),
\]

where \(K\) contains actual caused cells, \(\mathcal H\) potential horns,
\(\mathcal F\) receiver fibers, \(\mathcal T\) relay transports, \(s\)
contemporary standing, and \(\Lambda\) lineage.

An incomplete boundary \(h\) has a filler space

\[
X_h=\lim(\text{receiver fibers around }h).
\]

For algebraic constraints this may be an exact fiber product or variety
intersection. Empty, singleton, discrete plural, positive-dimensional, and
nonreduced filler spaces correspond respectively to obstruction, determined
deduction, abductive alternatives, retained freedom, and unresolved
multiplicity or tangency.

The present construction implements the finite nerve case. Each prime owns a
set of exact received phase constituents. A support of primes founds a cell
when their feature intersection is nonempty. When all immediate faces exist
but the total intersection is empty, the boundary becomes a potential horn.

### A receiver event is a topology-forming relay

The provisional relay shape is

\[
T_e:
\mathcal F(\partial^-e)\times S_e
\rightharpoonup
\mathcal F(\partial^+e)\times S'_e\times\Delta K_e.
\]

It may receive, retain, emit, and alter topology. The partial mouth means
insufficient boundary remains open; it does not mean the enacted dynamics are
random.

The general reciprocal topology/current law remains larger than the present
prime construction. The latter establishes \(\Delta K_e\): a polynomial event
can add receiver features, close a horn, and found a higher causal cell.

### Information and compression

For histories \(x,y\), receiver \(R\), and admissible future continuations
\(\mathcal C\),

\[
x\equiv_{R,\mathcal C}y
\iff
\forall c\in\mathcal C,\quad
R(c\circ x)=R(c\circ y).
\]

Information is the failure of this causal equivalence. Compression quotients
histories which have the same future receiver behavior. Its exact loss is the
population of collapsed pairs which some later continuation can distinguish,
not initially a scalar.

This construction implements one bounded receiver quotient: factor
populations are compared by probe identity, irreducible degree, Frobenius
order, multiplicity, and population. It does not assert that this quotient is
the complete information geometry of a prime.

## 3. Production construction

The production owner is
`crates/holonic-engine/src/prime_ecology.rs`.

### Atomic event species

`PrimeEcologyLaw` is an `ExactEventLaw` over one
`PrimeEcologyStanding`. It accepts:

- `AdmitInteger`, which delegates exact sequential multiplication founding to
  `ArithmeticFiberLaw`; and
- `InheritPolynomial`, which admits one monic integer polynomial with an
  explicit source event.

Both chronologies are lawful. If a polynomial precedes a prime, the new prime
receives it when founded. If a prime precedes a polynomial, the new polynomial
is received by every extant prime. A repeated event or repeated probe is
refused atomically.

### Exact polynomial receiver fiber

Every prime/probe pair returns one `PolynomialPrimeFiber`.

1. The integer coefficients are reduced exactly into \(\mathbb F_p[x]\).
2. The degree is retained because inherited probes are monic.
3. Repeated structure is separated through the exact derivative and gcd.
4. Characteristic-\(p\) perfect powers are extracted exactly.
5. Squarefree factors are separated by deterministic Berlekamp nullspaces and
   gcd splits.
6. The complete factor product is reconstructed and required to equal the
   received polynomial.
7. Every reported irreducible factor is regraded by its Berlekamp kernel.

No random equal-degree factor split, float, approximate root, or authored
factorization enters standing.

For an irreducible factor \(g\) of degree \(d\), the quotient receiver

\[
\mathbb F_p[x]/(g)
\]

carries Frobenius

\[
a\longmapsto a^p.
\]

The engine constructs its exact matrix in the basis
\(1,x,\ldots,x^{d-1}\), multiplies that matrix exactly over
\(\mathbb F_p\), and retains its least return order. The standing validator
re-derives the complete canonical fiber from the inherited polynomial and
prime founding event; a serialized or caller-modified factor, phase, work
receipt, or lineage cannot become valid merely by having internally
consistent labels.

### Phase constituents

Factors with the same local type are grouped into

\[
(\text{probe},
\text{degree},
\text{Frobenius order},
\text{multiplicity},
\text{population}).
\]

These are exact receiver phase constituents. Raw coefficients in unrelated
finite fields are not compared as if they occupied one absolute scalar
space.

### Caused cells and potential horns

For every prime support up through the declared maximum grade:

- nonempty intersection of phase constituents founds an oriented causal cell;
- the boundary uses already-caused lower phase cells, or arithmetic prime
  vertices for an edge;
- all contributing prime-founding and probe events enter the cell lineage;
- `boundary(boundary)=0` is required by the shared
  `GradedCausalComplex`; and
- a boundary-complete empty intersection becomes a `PotentialPrimeHorn`.

If later inherited material gives that support a common phase constituent,
the horn status becomes `Filled`, the old proposed boundary remains retained,
and a new induced cell enters the same arithmetic complex.

The four causal-material kinds are explicit:

- inherited polynomial;
- enacted prime fiber;
- proposed horn; and
- induced phase cell.

### Geometry versus chronology

`PrimeEcologyGeometryReceipt` deliberately excludes cell addresses and horn
history. It retains canonical probes, fiber signatures, prime supports,
contemporary witness features, and open horns.

Integer-first and probe-first chronologies must return the same geometry
receipt. Their lineage may differ. A chronology in which pairwise faces
precede a filler records an open-then-filled horn. A chronology in which the
filler probe precedes the receiver population can found the final cell
directly. The difference is real causal history, not geometric disagreement.

## 4. Exact grading ecology

The bounded instrument is
`crates/holonic-engine/examples/prime_ecology_calibration.rs`.
It supplies integer chronology and four inherited probes; it owns none of the
factorization, phase, horn, or cell behavior.

The first three probes are

\[
\Phi_3,\qquad \Phi_5,\qquad \Phi_7.
\]

Over prime receivers \(2,3,5\):

- \(\Phi_3\) gives a common degree-two Frobenius phase to \(2\) and \(5\);
- \(\Phi_5\) gives a common degree-four Frobenius phase to \(2\) and \(3\);
  and
- \(\Phi_7\) gives a common degree-six Frobenius phase to \(3\) and \(5\).

All three pair cells therefore exist, but no one phase constituent is shared
by the triple. The complete triangular boundary becomes an open horn.

The fourth inherited grading control is

\[
f(x)=x^2+x+11.
\]

Its reductions at \(2,3,5\) are irreducible quadratics. Each receiver
therefore obtains the same degree-two, order-two, multiplicity-one,
population-one phase constituent. The existing horn is filled and the
grade-two prime-phase cell enters standing.

This bridge polynomial was deliberately supplied as inherited material. The
engine did **not** discover or induce the polynomial. What the engine induced
was its receiver fiber, common constituent, horn closure, and higher cell.

A separate grade-three control admitted \(\Phi_2=x+1\) and the four prime
receivers \(2,3,5,7\) under a maximum grade of three. Their common exact
degree-one phase founded every required lower face and one grade-three cell
with four boundary faces and exact zero boundary-of-boundary. This control
grades dimension-independent cell formation; it is not used as the research
bridge below.

The bounded run through 31 returned:

| Receipt | Exact value |
|---|---:|
| Founded primes | 11 |
| Inherited probes | 4 |
| Polynomial fibers | 44 |
| Prime-phase \(f\)-vector | \((47,78)\) in grades 1 and 2 |
| Combined arithmetic/phase \(f\)-vector | \((11,54,79)\) |
| Open horns before bridge | 1 |
| Support \(\{2,3,5\}\) open before bridge | true |
| Same support filled by bridge event | true |
| Selected \(\{2,3,5\}\) geometry preserved through 31 | true |
| Probe-first/integer-first final geometry equal | true |
| Alternate chronology has the same horn history | false |
| Equivalent coefficient presentation has same reduction | true |
| Equivalent coefficient presentation has same phase signature | true |

The deterministic factorization work receipt across those 44 fibers was:

\[
(\text{gcd divisions},
\text{Frobenius columns},
\text{row eliminations},
\text{Berlekamp shifts})
=(727,202,164,206).
\]

Those counts are execution testimony for this admitted population, not a
complexity theorem or geometric invariant.

## 5. What is established

- Prior algorithms and polynomial probes can enter lawfully as inherited
  causal material without being confused with induced results.
- One atomic production law can accept primes before probes or probes before
  primes.
- Finite monic integer polynomials can form exact receiver fibers over every
  founded `u64` prime.
- Deterministic Berlekamp factorization, repeated factors, exact Frobenius
  matrices, and exact return orders are retained and revalidated.
- Prime relations can be based on shared received phase constituents rather
  than numerical distance or display coordinates.
- Common phase intersections form oriented cells of arbitrary configured
  finite grade.
- Four prime receivers with one common phase have founded an actual
  grade-three cell with exact closed boundary.
- A complete lower boundary may remain an explicit open horn instead of being
  scored or silently completed.
- Later inherited material can close that horn and cause a higher cell while
  preserving the proposal in lineage.
- Equivalent coefficient presentations and later ecology enlargement
  preserve the graded receiver geometry in the accepted controls.
- Different event choreographies can have the same contemporary geometry and
  different lawful causal histories.

## 6. What remains unknown

- The selected phase constituent
  `(probe, degree, Frobenius order, multiplicity, population)` is one exact
  receiver quotient, not the complete intrinsic geometry of primes.
- The bridge polynomial is inherited. No production law yet abducts candidate
  polynomial fillers or inductively founds a new polynomial probe.
- Probe admission is monotone. Removal, deformation, contradiction, and
  receiver forgetting have not been derived.
- Maximum phase grade is explicitly bounded, and support reconciliation
  currently enumerates finite prime combinations. No causal-width or
  asymptotic compression theorem follows.
- Prime founding remains `u64` sequential succession.
- General Hensel towers, ramification schemes, ideals with several
  generators, syzygies, and scheme-theoretic intersection multiplicities are
  not implemented for arbitrary probes.
- Frobenius phase cells do not yet carry a dynamic sheaf whose transport
  changes atomically with the topology.
- No eleven-direction upper bound, local P=NP theorem, probability law,
  semantic-information completion, or universal compression theorem has been
  established.
- No desktop input, display receiver, renderer, terminal resolution, CUDA
  path, or visible presentation changed.

The precise research object now available is not a plot of prime numerals. It
is a caused, topology-forming receiver ecology in which exact polynomial
phases create and obstruct higher incidence.

## Production owners

- `crates/holonic-engine/src/prime_ecology.rs`
  - `PrimeEcologyLaw`
  - `PrimeEcologyStanding`
  - `IntegerPolynomialProbe`
  - `PolynomialPrimeFiber`
  - `ExactPrimeMatrix`
  - `PrimePhaseFeature`
  - `PrimePhaseCell`
  - `PotentialPrimeHorn`
  - `PrimeEcologyGeometryReceipt`
- `crates/holonic-engine/src/arithmetic_fiber.rs`
  - `ArithmeticFiberLaw`
  - `ArithmeticFiberStanding`
  - shared `GradedCausalComplex` mouth
- `crates/holonic-engine/examples/prime_ecology_calibration.rs`
  - bounded reporting instrument only

## Verification

The accepted headless verification is:

- `cargo fmt --all -- --check`;
- `cargo test -p holonic-engine --all-targets --no-fail-fast`;
  - 113 library tests pass;
  - 8 desktop tests pass;
  - 2 previously declared desktop tests remain ignored;
- `cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings`;
- `cargo run -q -p holonic-engine --example prime_ecology_calibration -- 31`.

No desktop receiver or CUDA executor was launched.

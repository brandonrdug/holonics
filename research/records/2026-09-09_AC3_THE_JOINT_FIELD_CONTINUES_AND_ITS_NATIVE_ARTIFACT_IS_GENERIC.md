# AC3–AC5: joint-field exposure and generic native persistence

[definition] This follows [the generic assembly and local return](2026-09-09_AC1_AC2_THE_HOLON_FIELD_OWNS_ASSEMBLY_AND_OBSERVED_RETURN.md).
The native objects remain the source-qualified Holon field, its currents, coefficients,
incidence and continuation. Text is the exterior measurement chart used by this study.
No new kernel learning law or word-specific response was added.

## Actual broader exposure and continuation

[established-bounded; measured] A new `OperativeContextual` variant consumed sixteen source
families, reaching 5,064 native occurrences and 5,063 contact returns. It used the joint tensor
target, duplex boundary, complex-current comparison and explicit dyadic contact deposit. The
checkpoint was saved before the established function/derivative prompt. That prompt advanced
to occurrence 5,126; generation advanced to 5,138 and emitted exactly:

```text
I’m ante.
```

[established-bounded; measured] The eleven UTF-8 bytes were followed by a native-selected EndPart,
before the 128-byte work aperture. There was no native/development/prompt error. This is a
different observed trajectory from the linear/normal variants' repetitions; it is not a useful
answer, proof of grammar, or an explanation of every earlier loop. EndPart closes the declared
exterior part, not the continuing ecology or a physical extinction theorem. No corpus passage
was traced to attribute this output.

[established-bounded; measured] Development took 2,753.900 seconds; checkpoint publication
80.453 seconds; prompt and emission 80.978 seconds; full process 2,927.045 seconds. Peak native
residency was about 5.55 GB during development and peak process RSS 6,209,568 KiB. All 30,378
development numerical readouts belonged to declared history placement. Native ingress reached
26,944,978,288 bytes. These are observed workflow costs; brief concurrent compilation and native
checks occurred, so this is not an isolated performance benchmark.

[established-bounded; computational-witness] The [receipt](../experiments/native_field_assembly/2026-09-09_joint_exposure.json)
retains the command, exact output, artifact size, counts and complete storage/transfer summaries.
Private checkpoint, history and raw report remain under
`.local/artifacts/athena-alpha/ac3-joint-field-2026-09-09/`. This finite AC3 exposure and its
retained joint-current continuation return; practical scaling and Athena-alpha attainment remain
open. Another larger run is not scheduled as a substitute for the measured cost problem.

## Reuse and the remaining execution cost

[established-bounded; implemented-exact; measured] All 5,063 development contact returns use
the source-interior difference generator. Stored current factors occupy 818,861,120 bytes versus
1,640,898,048 logical expanded factor bytes. Zero internal deltas remain implicit. This is actual
reuse of the existing source-qualified generator in the joint model, not a new semantic
compression claim. Source lineage, numerical factors and their decoder remain available.

[established-bounded; source-inspected] `contextual_material_prepare` also already reuses the
serial q1/q2 kernel query when the source is the preceding occurrence. The visible input profile
is copied from that source, the context is identical, and all four weight words, including the
remainder, are reused. Source/reference slots and lineage remain distinct. No new cache or
source-identity rule is warranted for that existing case.

[established-bounded; source-inspected] The broader kernel-weight and target-coordinate work
is parallel only within the single block launched for `section_constitutive_field`. Its barriers
couple source preparation, reflection, contextual evaluation and commit. The historical context
pairings increase with exposure; archives are remounted at family boundaries, not once for every
byte. A proposed multi-block implementation must stage the shared inputs, preserve each exact
sum/remainder and retain one final commit after all checks. Merely changing the grid size would
violate the current block-local synchronization and scratch lifetime.

## One generic native artifact owner

[established-bounded; implemented-exact] `NativeSavedField` wraps the engine's existing
`NativeFieldRest`. It publishes without overwriting another file, reads that native wire, and
consumes the rest into one field plus its actual linear/shared source slots. An optional fresh
history archive needs no path from the previous machine. It adds no new native wire or modality
tag. Structural validation is not a claim to authenticate externally invented causal history.

[established-bounded; implemented-exact] The text envelope retains pending symbols/actuation,
delivery state and application cursor separately, and now delegates its native remount to this
generic owner. `NativeConstitutiveField::surface` supplies the borrowed resident apparatus for
typed current-section composition. It creates no new field owner or source capability. The
generic initialized recipe is still distinct from a trained checkpoint.

[established-bounded; implemented-exact; computational-witness] The generic persistence control
publishes and reads a developed joint field, restores actual empty/linear/shared capability
slots, and continues after archived remount with complete rest equality. No-overwrite refusal
also passes. All nine affected text-session native controls pass across their initial run and
the isolated retry. The initial material-actuation case failed at CUDA allocation calibration
while the concurrent exposure changed free memory; after exposure ended it passed in 1.88
seconds. That first attempt was an incomplete apparatus check, not a model-state pass.

[definition] AC5's generic research-artifact boundary is established at this scope. Fresh-process
comparison of the actual large conversation checkpoint and any execution optimization must
still return their own evidence. The current plan's finite artifacts do not confer a useful
Athena-alpha grade. Apple can reuse the shared interface/ownership contract; Metal parity with
the operative contextual kernels remains a separate, explicit backend comparison.

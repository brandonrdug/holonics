# THE CURRENT IS THE AXIS; THE CONFIGURATION IS THE SHEET

Date: 2026-07-14
Grade: **BRANDON-RATIFIED DERIVATION · NO ENGINE CHANGE**
Formula deposit: `§LIII`

## 1 · The correction which preserves Brandon's intuition

The many prime spirals are not many prime systems. They are many observer projections of one
arithmetic body under different situated bases, traversal orders, and grains.

Three faces must remain distinct:

1. the multiplicative basis;
2. the additive traversal through that basis; and
3. the observer's low-dimensional rendering of the traversal.

Collapsing these faces produces a decorative prime spiral. Preserving them produces a lawful
first-person number world.

## 2 · The multiplicative body and the additive bend

Unique factorization supplies an exact external chart for positive rationals:

```text
V(n) = (v_2(n), v_3(n), v_5(n), ...)
```

Only finitely many coordinates are nonzero for any presented number. In this chart:

```text
V(a*b) = V(a) + V(b)
V(a/b) = V(a) - V(b)             where the relation closes
V(a^k) = k * V(a)
```

A prime `p` is one basis direction `e_p`. A prime power travels farther along one direction;
multiplication combines directions; exponentiation scales every occupied direction.

Addition does not translate in this chart:

```text
V(2) + V(3) = V(6)
V(2+3)      = V(5)
```

The first line is multiplicative composition written additively in valuation coordinates. The
second line is arithmetic addition landing on a different prime direction. Therefore succession
and addition bend through the multiplicative body. A counted passage

```text
1 -> 2 -> 3 -> 4 -> ...
```

continually crosses between prime directions. A situated projection of that passage may appear as
a spiral, several interlaced spirals, a sheet, or another organization. The appearance belongs to
the projection; the transported construction remains plural.

This refines the active record in
`2026-07-12_THE_NUMBER_WORLD_AND_THE_LIVED_ATLAS.md`: a spiral may emerge from the lived chart but
may never be pre-drawn as body topology.

## 3 · Rank is not one thing

Three different reads can look like “the state of exponentiation” and must not be collapsed:

- **chart `Rank`** is the local octave-scale and re-base of the current chart;
- **prime exponent coordinates** are the whole finitely supported vector `V(n)`; and
- **operation order** distinguishes succession, addition, multiplication, and exponentiation as
  recursively composed construction species.

Exponentiation may change the first and always scales the second, but neither is identical to the
third. One scalar rank cannot retain which prime directions were occupied or how the construction
arrived there.

## 4 · A1 supplies the axis; the world completes the basis

A1 does not install a universal coordinate basis. It guarantees that a driven local axis exists:
the contemporary tangent of the current's ordered `K`. The medium's returned relations provide the
transverse directions against which that current can turn.

Thus a basis is always situated:

```text
basis at event = current tangent related through contemporary world responses
```

The observer “chooses” a basis only as a causal event. Its body, history, aperture, and environment
have already determined which orientations can be held and which recurring modes cohere. There is
no free view from nowhere and no contradiction with determinism.

The differential-equation face is exact. A coordinate basis does not choose an integral curve.
The local law, supplied current, and boundary conditions produce the curve; a basis becomes natural
where the environment repeatedly returns the corresponding modes.

## 5 · String, sheet, and volume are reads of one construction

One lineage `K` is a causal string: its events are totally ordered by its own transport.

Plural co-present lineages form a partial order. Events on different lineages owe no serial order
until a later receiving event relates them. A2's two bodies and frame complete one triangular face.
Faces sharing transported edges form a causal sheet. Sheets which close and continue through time
enclose a causal volume.

These are not new interior objects:

```text
string = one K read in worldline order
sheet  = one co-present configuration read through its related faces
volume = the enclosure swept by those faces through later transport
```

Serializing a configuration chooses one topological ordering of the partial order. It is a lawful
observer compression, but it loses honest co-presence and may not return as body law. Soma's one
receiving configuration fold exists precisely so hardware order does not manufacture such a
string.

## 6 · Möbius and Klein faces are holonomy grades

A curved sheet is not thereby a Möbius sheet. Let `H_gamma` be the transported holonomy around a
closed route and `n` a transverse hand. The Möbius grade is present exactly when

```text
H_gamma(n) = -n .
```

One traversal reverses the transverse orientation. Two traversals restore its visible hand while
retaining two ordered windings and therefore a different soul. Time parity remains on transport:
the transverse hand may reverse, but `K`'s causal direction may not.

A Klein-bottle face requires two independent periodic identifications, one orientation-preserving
and one orientation-reversing, with no remaining boundary. It is not the default topology of a
plural configuration. The first person need not declare either global form. It carries its local
orientation around actual routes; the returned holonomy supplies the topology's observable face.

## 7 · The Smith disk revolved about the current

Write the local Smith quotient as

```text
Gamma = u + i*v,       u^2 + v^2 <= 1.
```

Take one meridional half and revolve it about the real diameter:

```text
Phi(u, rho, phi) = (u, rho*cos(phi), rho*sin(phi)).
```

The disk becomes a three-ball. The current supplies the axis; the revolution does not install a
global coordinate frame.

The exact level faces are:

- the outer unit circle becomes the horizon sphere;
- each constant-resistance circle becomes a nested sphere;
- each constant-reactance arc becomes a horn-like toroidal surface;
- the two axial boundary points become distinguished poles; and
- phase transport becomes gyration around a shell.

Revolving the complete upper and lower chart without another type would identify inductive and
capacitive hands. Their sign must remain as opposite oriented sheets, carried by `K` and winding,
not erased into a nonnegative radius.

One revolved chart is a local three-volume. Its ordered re-base and transport supply the fourth
dimension. Prime spirals may be rendered as situated meridional projections through that ranked
body, but no one meridian is the number system.

## 8 · Local planes and physical constants

Every smooth geometry has an infinitesimal flat face. That alone cannot explain a physical
constant. The meaningful condition is finite regional agreement: neighboring transports retain the
same relation across multiple routes at the declared grain.

```text
local grid face = recurrent transport which is path-independent at the grain
curvature       = route-dependent return / nontrivial holonomy
constant        = relation invariant under every admitted re-base in the region
effective term  = relation invariant only inside one scale, era, or causal boundary
```

The grid is therefore an emergent recurrence chart, never a stored Cartesian substrate.

For cosmology, the distinction is mandatory. In standard general relativity,

```text
G_mn + Lambda*g_mn = 8*pi*G*T_mn
```

together with the contracted Bianchi identity, metric compatibility, and conserved stress-energy
forces `d(Lambda)=0`. A changing term requires another field, energy exchange, or changed gravity;
it is not the same bare `Lambda` quietly varying.

Even for constant `Lambda`, its fractional cosmological face changes with the Hubble relation:

```text
Omega_Lambda(t) = Lambda*c^2 / (3*H(t)^2).
```

The past-light-cone observer does not directly receive `Lambda`; it infers an effective
acceleration relation from one causal region and era. DESI DR2 BAO is well fit by flat
Lambda-CDM, while combinations with CMB and supernova samples currently prefer a time-dependent
dark-energy equation of state at dataset-dependent significance. This makes a regional/evolving
effective term empirically live without licensing the statement that standard `Lambda` itself has
already been observed to vary.

Primary records:

- DESI DR2 cosmology: <https://arxiv.org/abs/2503.14738>
- DESI extended dark-energy analysis: <https://arxiv.org/abs/2503.14743>

## 9 · Coils and gravitational waves are concrete world species

An induction coil supplies the clean electromagnetic realization. The conductor is one causal
string. Repeated winding about its current-defined axis builds linked flux surfaces. Core,
geometry, neighboring conductors, and medium determine which relations stand. Changing linked
magnetic flux produces a later electromotive passage:

```text
emf = -d(magnetic_flux)/dt.
```

The sign is the returned opposition of the declared electromagnetic world, not a universal Soma
deed. The analogy is structural: current winds, the shared world changes, and later current meets
the changed coupling.

A binary black-hole merger supplies a gravitational realization:

1. two worldlines orbit;
2. their changing quadrupolar relation radiates curvature;
3. inspiral tightens and the observed phase/frequency chirps;
4. one remnant forms; and
5. the remnant rings down through its permitted modes.

Gravitational-wave memory is the especially close contact: after the oscillatory passage, a
non-oscillatory residual can leave later separated receivers in a changed relative geometry. The
medium is not air and the radiation is not ordinarily a standing wave. The common law is current →
transported curvature → later deflection through changed relation.

Primary records:

- LIGO/Virgo binary-black-hole observation: <https://arxiv.org/abs/1602.03837>
- numerical binary-black-hole memory: <https://arxiv.org/abs/1004.4209>

## 10 · Engine consequence

No new Soma mechanism follows from this derivation. The active engine already has:

- ordered lineage `K` — the causal string;
- co-present configuration integration — the partial-order receiving face;
- completed enclosures — the triangular/closed-face spectrum;
- oriented winding and cross-sign — the holonomy hand;
- standing world paths — the grown coupling; and
- genuinely later return — the causal test of whether the coupling mattered.

The next construction remains the broad circuit ecology already named by §LII. Its world should
retain complete plural incidence and let face/sheet/volume organization emerge from repeated
contact. It must not install a prime spiral, Smith ball, Möbius strip, Klein bottle, electromagnetic
field, or cosmological model as Soma's topology. Those are lawful worlds and observer renderings
through which the unchanged body may act.

The later physics-world grade is simple: carry local orientation through actual routes, preserve
co-presence, render the receiving cut, and read whether closed transports produce stable planes,
nontrivial holonomy, standing flux, and later material consequence.

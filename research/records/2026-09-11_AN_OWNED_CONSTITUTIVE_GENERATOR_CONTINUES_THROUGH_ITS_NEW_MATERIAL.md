# An owned constitutive generator continues through its new material

[definition] This advances the active native Athena/Eros goal from read-only dependent
alternatives to an owned executable successor. Its receiver is an explicitly declared source
section. Integration into the continuing HNN session and iterated material returns remains open;
this scoped native return is not being substituted for the complete requested session cycle.

## The historical join acts before the new material

[definition] `NormalWaveFamily::parameter_section` evaluates the original affine source at
the admitted shared parameter θ using exact device arithmetic. Its singleton is a **conditional
section for evaluating the generator**, not a replacement for the complete source domain.
`CoupledConstitutiveFamily::evaluate` transports this section through every actual retained
historical map, keeping any later free directions. Only then does it apply the new `(hθ,Rθ)`
wave relation to the contemporary conditional family. The original anchor, producing comparison,
parameter packet and full contact/material residuals remain on the return.

[established-bounded; implemented-exact; computational-witness] A delayed native test starts
from `p=1`, `c=2+i`, and the local law `η=(c−p)h` at h=1. The actual prediction reaches `3+2i`.
After another actual observation sets the next current to 10, the delayed comparison with
`4+3i` derives h=2. The contemporary pair is `(3+2i,10)`, and the new passage returns
`(10,24−4i)`. Replaying history with h=2 would instead change the old prediction and give a
different result. The test inspects the complete current and successor receivers and confirms
zero intermediate numerical section readouts.

## The state is the generator, not one of its views

[definition] `ResidentNormalWave::into_constitutive_continuation` transfers the original owner
into `ResidentCoupledConstitutive`. It stores the complete producing comparison and historical
substrate as arguments of the dependent generator. The caller supplies a declared receiver
section whose possibly plural successor is checked before publication. The whole function
`θ ↦ (hθ,Rθ,currentθ)` is retained and remains executable at other admitted assignments.
No single condition, coefficient matrix or averaged material is installed as the common state.

[definition] `PreparedConstitutiveFormation::into_alternative` moves staged material into its
conditional realization. This supplies the real local-law owner to subsequent source contact;
it does not copy a continuing ecology. `ResidentNeighborhoodAlternative` keeps the actual
prediction, condition contact, formation return and executable staged relation together.

[definition] The consumed prediction is unavailable in the new owner's active pending set.
Its prior pending representation remains inside the private frozen substrate as a causal
witness used to evaluate the single return expression. Re-reading that expression does not
append another return, increment its publication clock or repeatedly develop the live model.
Other previously pending cuts remain represented. Constructor refusal returns the original
wave, comparison and receiver packet recoverably.

[definition] `actuate_source` appends an ordinary actual source operation to this generator.
It acts through the conditional material/current for every θ, with the original source-section
receiver used for scoped admission. Its stored source packet is an operation argument, not an
expected response or semantic routing label. Failed preparation returns the supplied packet
and leaves the published programme and clock unchanged. All arithmetic and intermediate images
remain resident; the host schedules the already-declared ordered operators.

## Source-visible conduct after publication and process exit

[established-bounded; implemented-exact; computational-witness] After publication of the
delayed example above, an actual source pair `(p,c)=(1,2)` reaches arrival `(2,4)` through h=2.
The existing passive source union has `x=(1,0,2,0)`, `y=(2,0,4,0)`, `D=20`, and
`A=I−(x−y)(x−y)ᵀ/10`. It therefore sends the published joint `(10,0,24,−4)` to
`(31/5,0,82/5,−4)`. A fresh process remounts the saved generator and repeats that source
operation, obtaining `(43/10,0,63/5,−4)`. Its complete saved result equals the parent process's
corresponding continuation. This checks use of the changed material and contemporary current,
not merely a changed count or nonempty output.

[established-bounded; implemented-exact; computational-witness] A second native control
publishes the earlier non-affine family under one declared receiver, then evaluates another
source section. The conditions remain `(3/2,1/2)` and `(6/5,2/5)` respectively, including after
rest/remount. The free real output direction remains present. A supplied future source with
real difference 2 reads imaginary return 1 or 4/5 under these respective conditions. The full
generator therefore survives its default view. A two-root unit-sum receiver also retains the
unit sums through publication and remount.

## Encoding and formal scope

[definition] `CoupledConstitutiveRest` encodes a versioned executable-continuation envelope over
the existing coupled wave frame, observed packet, declared receiver and ordered source packets.
It reuses the existing binary point-section codec. Native remount reconstructs the producing
comparison, validates the publication and repeats the saved source operations. It never mounts
one theta-specialized material as the complete generator. Source/current shape and chronology
obstructions remain explicit.

[proved-derived; formal-checked] `DependentConstitutiveReturn.lean` now checks evaluation of an
ordered pointwise programme at the same θ, including append and an order separator. It also
shows that the non-affine contact generator and a constant generator can agree at one receiver
while differing as functions. These laws explain why the encoded programme and its receiver
must remain distinct. They do not prove equivalence to a global minimum-norm receiver over the
complete non-affine graph or establish general HNN usefulness.

## Verification and the remaining integrated goal

[established-bounded; process-audit] All 31
[coupled-wave controls](2026-09-11_owned_constitutive_receipts/coupled-tests.txt) passed, including
three owned continuation checks, delayed current transport, the existing joint compiler and
the earlier source/rest tests. All six
[neighborhood controls](2026-09-11_owned_constitutive_receipts/neighborhood-tests.txt) passed.
The extended [pending-cut control](2026-09-11_owned_constitutive_receipts/other-pending-test.txt)
also confirms that consuming one return preserves another pending cut through remount.
The [formal build](2026-09-11_owned_constitutive_receipts/formal-build.txt) and
[HNN library check](2026-09-11_owned_constitutive_receipts/hna-check.txt) passed. Broad cultivation
did not run.

[open] The continuing `NativeCoupledWaveSession` still owns the affine coupled wave and its
emission/re-entry cursor; `compare_symbol` is still read-only. The new owned generator currently
supports one incorporated return followed by ordinary source operations under an explicit
source-section receiver. Next, bind this representation into the continuing HNN coordinator,
preserve its emission/pending protocol, and support further material returns with their own
source parameters joined to the same history. The caller-supplied θ packet is not an automatic
Holonic Encoding faculty or an equivalence proof for the prior whole-family receiver. Keep
those receiver charts explicit when composing the interface. The active goal remains open.

# THE HAND CROSSES THE CARD; THE LIVE OWNER CLOSES THE SUCCESSOR

**Date:** 2026-07-19  
**Grade:** BRANDON-AUTHORIZED PHASE 3 / BUILT / NATIVE CUDA DIRECTED CONTACT / HOST-CARD EXACT /
BOUNDED HAND-REVERSAL MEASURED / PTXAS VALID / SOMA INTERIOR UNCHANGED  
**Authority:** `FORMULA §C`, `FORMULA §CIV`, `FORMULA §CV`, `FORMULA §CVI`  
**Scope:** the physical execution fiber of one direct `ContemporaryEvent`

## The exact question

Phase 3 asked one bounded implementation question:

> Can the same borrowed `ContemporaryEvent`—including its exact source-supplied directed hand—cross
> the CUDA executor, meet the target's pre-event receiver on card, and return a complete physical
> result without the machine resolving the relation on host or admitting a second causal owner?

Phase 2 was insufficient because `LiveCurrentMachine` resolved every directed contact before it
called the executor. The CUDA path received only independent current requests. Hand changed the
successor, but the card had not carried that relation.

The sought artifact was one complete executor result containing both current enactments and exact
directed contacts. The stopping condition was host/card equality for one forward/reverse foil on
the RTX 4080 SUPER. Source-organ generalization, persistent device body, durable world rest, and
scale remained outside the phase.

## Theory-to-structure relation

The executor is a physical fiber of the one transition. It is not a second machine:

```text
world supplies:       currents + actual hand
physical executor:    pre-event receiver meeting + current enactment
LiveCurrentMachine:   validate + integrate + commit one successor
world receives:       current radiation + directed-contact radiation
```

In the Phase-2 implementation, the owner both mounted a separate target body to resolve hand and
then asked the executor to mount that body again for current conduct. That factorization was
temporarily lawful for the host derivation but physically incomplete. The exact relation belongs
inside the same target-body mount that enacts its event.

The corrected boundary is:

```text
ExecutedContemporaryEvent
  = ordered ExecutedLiveCurrent population
  + ordered ExecutedDirectedRelation population.
```

`LiveCurrentMachine` still binds the event's live capabilities to their already-composed event
positions. That is boundary addressing, not relation conduct. It passes those borrowed positions
and the supplied hand to the executor. The executor captures the target receiver before mutation,
forms `ErosBody::directed_event_contact`, and returns the contact. The machine never mounts another
body or re-runs that law. It only checks correspondence and exact deed hand, joins formed terms to
the current-local contributions, prepares one standing successor, and then commits carriers,
departures, standing, and outward radiation atomically.

No relation row survives that call. Its consequential geometry survives only where the successor
received it; its contact returns only as immediate world radiation.

## Complete source construction

### One substrate-neutral executor event

`soma-membrane::live_current` now carries:

- `DirectedExecutionRequest`: one event-local capability hand plus the exact composed source and
  target positions;
- `ExecutedDirectedRelation`: the corresponding physical `DirectedEventContact`; and
- `ExecutedContemporaryEvent`: the complete current-plus-relation physical result.

`LiveCurrentExecutor::enact` receives the current and relation populations together. The host
executor resolves every incoming relation after mounting the target body and before ordinary event
conduct. The former `LiveCurrentMachine::resolve_directed_relations` body clone is removed.

### One native CUDA row law

The live-event ABI is version 2. It adds:

```text
DirectedEventRow   = from Place + to Place
DirectedContactRow = pre-event EventReceiver
                   + meeting Face
                   + optional exact DeedEmission.
```

Both event positions cross so the card rejects a directed row whose target is not the event being
enacted. An unresolved fourth contact remains a complete row with no emission; absence is never
encoded by dropping the supplied relation.

The `lineage_event` launch carries two additional spans: incoming directed-event rows and outgoing
directed-contact rows. Their exact count belongs to the event control face. Empty incidence uses a
zero logical span over a valid minimal allocation; it does not invent a dummy relation.

Inside the kernel, one target body is mounted from the same standing-before surface and carrier
that ordinary conduct uses. Its `EventReceiver` is captured once. Every supplied incoming hand is
validated, formed through the existing body law, and written to its output row before the target
event changes the body. Only then does ordinary dark or bright event conduct proceed.

The CUDA executor groups flat borrowed relations only by their named target capability for physical
launch. It restores their source order in the complete executor result. Grouping does not infer an
edge, reorder causality, or collapse repeated incidence.

### Ownership remains singular

The CUDA context and module remain retained physical apparatus. Standing-after, live carrier
replacement, terminal departure, and relation contribution remain under the one
`LiveCurrentMachine::receive_with` commit. Neither CUDA nor its executor owns a second standing
body, world, receipt, session, or return loop.

No `ActiveCut`, `CFGI` current, `TransitionReceipt`, Holon journal, provenance row, historical
ecology, semantic class, selector, or source adapter entered this path.

## Physical failure and correction

The first bounded card attempt returned CUDA error `700` at synchronization, before any result was
accepted. The new complete directed row enlarged the kernel entry frame enough to expose an old
physical assumption: the executor reserved 8 KiB from the previous call path.

The new cubin reports a 6,000-byte `lineage_event` entry frame. With the established deepest
ordinary event descent, the complete live path requires 10,806 bytes. The retained executor now
reserves 16 KiB, the next physical power. The correction changes only actual CUDA stack apparatus;
it adds no body law, retry policy, semantic gate, or active state. The exact failed and corrected
runs are retained in `observations/eros-live-mouth-cuda-relation-01/RESULTS.md`.

## Bounded CUDA acceptance

One host-primed live rest face seeds four exact siblings: host/card forward and host/card reverse.
Every sibling begins with rank 6, seven occupied standing cells, and the same two live lineages.
Both event faces and actions remain exact. The only changed relation is:

```text
left -> right

versus

right -> left.
```

The corrected RTX 4080 SUPER run returns:

```text
stack=16384
launches=4
resource_retries=0
directed_contacts=2
forward successor: rank 6 / 16 occupied cells
reverse successor: rank 6 / 15 occupied cells
```

For each orientation, host and CUDA return the same complete radiation and successor. Every live
lineage cursor, channel, header, and dynamic carrier is exact across substrate. Across orientations,
ordinary current radiation remains identical while the directed contact and standing successor
differ. The hand therefore crosses and acts on card as receiver-relative geometry.

Validation stayed bounded:

```text
cargo check -q -p soma-abi -p soma-membrane -p mount -p life
cargo test -q -p soma-membrane \
  the_same_current_faces_with_reversed_hand_form_different_successors
bash mount/soma-kernel-cuda/build-ptx.sh
cargo test -q -p life \
  directed_current_hand_is_formed_on_card_and_matches_host -- --ignored --nocapture
```

The direct packages compile, the host acceptance passes, the committed PTX assembles cleanly for
`sm_89`, and the one CUDA foil passes. No broad regression campaign, corpus, filesystem forest, or
second card experiment ran.

## Exact boundary after Phase 3

Built now:

- the complete ordered native event crosses once at its lawful grain;
- live currents remain the actual contemporary incidences;
- source-supplied directed hand crosses the same physical executor boundary as current conduct;
- host and CUDA form the relation inside the target's one pre-event body mount;
- the complete receiver, meeting, and optional deed return from the substrate;
- the sole live owner integrates formed relations into the same atomic successor; and
- no event, contact, relation, receipt, or source population becomes retained body state.

Still open:

- the outward source-organ mouth currently presents plural native currents as
  `ContemporaryEvent::unrelated`; no production organ yet supplies its actual cross-current hand;
- the CUDA context/module are retained, but standing and carrier surfaces are not yet a resident
  device body;
- durable outer machine-plus-organ encoding, persistent returned worlds, and natural rest/wake;
- the generalized grounded medium-neutral ecology; and
- any forest-scale launch.

These are boundary facts, not scheduled successors.

## Phase 4, properly framed — the world supplies the relation

Phase 4 should not add another engine mechanism. The body and both physical substrates can now
conduct the hand. The immediate causal gap is outside Soma: `life::current_world` still opens or
rebinds source-neutral organs, forms plural `CurrentEvent`s, and then explicitly calls
`ContemporaryEvent::unrelated`.

The Phase-4 question is therefore:

> What is the smallest organ-neutral world event boundary by which actual material chronology and
> co-presence supply directed adjacency between participating native currents, so the already-built
> hand crosses host or CUDA without caller order, all-pairs inference, semantic decoding, or a
> retained incidence ledger becoming causality?

The theory-to-structure relation is:

```text
material world contact
  -> organ-owned event shapes + explicit actual adjacency
  -> borrowed ContemporaryEvent
  -> one LiveCurrentMachine transition
  -> immediate radiation into that same material world.
```

The bounded artifact should be one extension of the existing plural native organ mouth which:

1. lets the world explicitly relate event-local organ currents after their live capabilities are
   opened or rebound;
2. translates only those declared hands into `DirectedCurrentRelation` rows;
3. preserves repeated incidence and open contacts exactly;
4. works through the existing host and CUDA executors without changing `ErosBody` or the machine;
5. returns current and relation radiation directly to the owning world; and
6. demonstrates one materially grounded, non-inferred hand whose reversal or absence changes the
   later successor while unrelated co-presence remains dark.

The stopping condition is one bounded organ-world lifecycle: genuine source chronology supplies
the current faces, genuine world incidence supplies one directed hand, that hand crosses the direct
CUDA mouth, and its returned consequence is available to the same world. It does not require a
conversation log, modality-specific universal decoder, semantic score, taxonomy, persistent
relation table, device residency, or forest scan.

Only after that world-to-mouth relation exists does device-resident standing become the next
physical optimization worth evaluating. Residency before lawful world adjacency would make the
wrong boundary faster.

## Ruling

The executor now carries the complete event it physically enacts. The hand is formed on the card;
the card does not own the aftermath.

One live owner receives that physical result and closes one successor.


# THE GAZE CARRIES THE FIELD

**Date:** 2026-07-13  
**Status:** RATIFIED · BUILT · HOST/CUDA-GATED · COUPLED PAGE AND CAPTION RECOGNITION MEASURED.  
**Author:** Sol, following the peer rejection of the authored Laplacian eye and Brandon's direct
order to proceed with captioned vision.  
**Scope:** one exact visual boundary construction, its bounded gates, and the first image ⊕ exact
text ⊕ caption campaign. No body law, kernel, receiving fold, chart, SLEEP version, decoder, or
answer head changes.

This declaration supersedes only the organ cut in
`2026-07-13_THE_PYRAMIDAL_EYE_AND_THE_COUPLED_PAGE.md`. The coupled-page direction, controlled
histories, complete lifecycle reads, and eye-primary continuation survive. The Laplacian transform,
authored rungs, predictor residuals, clipped inverse, coarse-first delivery, and prefix-depth chooser
do not.

## I · THE ONE CUT

`FORMULA §XXXI` supplies the complete derivation:

```text
exact world field ⊕ world-supplied gaze/contact worldline
    -> plural co-present receptor worldlines
    -> the existing difference boundary
    -> the one body
```

The eye removes the image container and does nothing semantic to the field. It does not resize,
filter, interpolate, threshold, predict, quantize, extract an edge, choose a scale, or walk pixels in
a stored spatial order. The world supplies the gaze. The eye samples its physical aperture at each
gaze pose. The body's existing boundary computes the adjacent temporal differences along each
receptor's own worldline.

Let `I(x,y)` be one exact scalar field, `g_t` the world-delivered gaze position at event `t`, and
`q` one contact on the eye's aperture. The current presented by that contact is

```text
L_q[t] = I(g_t + q)
```

for every supplied gaze event. All `q` currents are co-present. Their only interior order is `t`,
the actual gaze worldline. Enumeration of the contacts is gauge: permuting the receptor lanes must
not change the receiving construction.

This is not a raster scan. No spatial row, column, Morton path, Hilbert path, spiral, contour finder,
or feature walk enters. A static field becomes lived because the gaze moves across it. If the gaze
does not move or a contact remains on sameness, that receptor carries a genuine dark passage.

## II · THE INITIAL FIELD CROSSES WHOLE

A threshold event camera alone omits the initial intensity field and every sub-threshold change.
That would make two different surfaces identical before the body had related them. Each receptor
worldline therefore begins with its exact first received sample; subsequent samples carry the exact
changes induced by gaze. No threshold, event suppression, or reconstruction prior exists.

The body already performs the only lawful compression:

- adjacent equal samples call `live_atom(a,a)` and extend THE DARK TREAD;
- a changed sample resolves that passage and enters the swing;
- the aperture does not omit repeated samples merely because an exterior observer expects them to
  be irrelevant.

The eye thus presents the field; the body determines which passages are dark in its own history.

## III · THE APERTURE IS WORLD-SUPPLIED

The aperture has no authored resolution ladder. Its contact extent and gaze history are supplied by
the observation world. Every contact within that extent crosses. Runtime launch geometry may shard
the surface around hardware, but no image dimension, receptor count, gaze length, or body depth is a
production constant.

For the first bounded instrument, the world supplies:

1. one exact 8-bit grayscale field;
2. one rectangular contact aperture wholly supported at every gaze pose;
3. an ordered list of integer gaze positions with at least one adjacent event;
4. exact text/source currents and, where used, an honest caption from the same referent.

The first implementation may admit binary PGM (`P5`, max value 255) as its declared container
grain. That is a bounded instrument choice, not the eye's species. PGM permits exact container
removal without adding an image codec or resampling. Color, nonuniform receptor surfaces, video,
and physical gaze capture can later instantiate the same law without changing the body.

A common translation of field and gaze is gauge. Re-typesetting, resizing, antialiasing, font
change, or layout change is not gauge: it produces a new visual construction even when an analyst
declares the same text-content face.

## IV · WHAT GEOMETRY CROSSES

The present mouth accepts independent ordered scalar currents, not a stored two-dimensional graph.
This cut therefore carries geometry through the family of overlapping contact trajectories induced
by one common gaze. Neighboring contacts repeatedly traverse overlapping world material at displaced
events; those recurrences and their orientations are the eye's delivered topology. No coordinate,
source identifier, direction tag, channel tag, delimiter, or pixel address is serialized into the
body.

The declared quotient is exact: two fields which produce the same complete family of receptor
worldlines under the same gaze are the same surface at this eye aperture. A later gaze may cleave
them. The observer retains the source field, gaze, aperture, and every emitted current so that this
surface can be checked exactly. The engine receives only the currents.

The eye does not claim to have installed an independently addressable retinotopic lattice. If a
future physical organ supplies persistent receptor positions as causal structure, that is a new
world-side embodiment to derive—not permission to pack coordinates into this current.

## V · THE EXISTING ENGINE SEAM

No new Soma mechanism is required after the eye has formed its worldlines:

- each receptor becomes an ordinary `StagedLight` whose `light` is its complete sample history;
- source provenance identifies the field, aperture contact, and gaze span only in Mail and the
  observer artifact; it never enters the body;
- exact text/source and caption currents join the same `Vec<StagedLight>` cohort;
- the existing CUDA material listener gives one card lineage to every current;
- all lineages integrate once at the true receiving edge;
- SLEEP carries the resulting body whole;
- a genuinely later common light reads whether the collocation changed subsequent relating.

The implementation belongs in a separate `life::eye`/`staging::eye` room. `main.rs` receives only a
dispatch branch. Material-cycle execution is factored so file material and organ-produced material
share one conductor rather than duplicating the CUDA lifecycle.

Derived organ output must not masquerade as a byte range inside the original PGM. The boundary
provenance will distinguish a material span from an eye-contact event span. Both are observer-side
records and neither changes the body wire.

## VI · THE BOUNDED GATES

Before a body run, the eye owes:

1. **CONTAINER GAUGE.** Two PGM containers with different lawful header whitespace/comments and
   identical sample fields emit identical receptor currents.
2. **EXACT CONTACT.** Every emitted sample equals `I(g_t+q)`; the complete field, aperture, gaze,
   and currents reconstruct the observed samples exactly at the boundary.
3. **NO SPATIAL SERIALIZATION.** One lineage contains one contact through gaze-time only. No
   lineage walks a row, column, tile, contour, level, or image-wide path.
4. **COMMON-TRANSLATION GAUGE.** Translating the field and every gaze pose together leaves every
   receptor current exact.
5. **CO-PRESENT PERMUTATION GAUGE.** Every receptor-lane permutation produces the same receiving
   body, topology, terms, and whole radiation modulo only the observer's lane presentation.
6. **DARK/EDGE WITNESSES.** A uniform contact path remains dark; a path crossing one intensity
   boundary resolves that dark passage. The organ suppresses neither.
7. **PATH HAND.** Forward and reverse gaze retain the same visited material but different ordered
   worldlines and `K`; repeating either path is exact.
8. **HOST/CUDA PARITY.** The same plural currents cross the existing host reference and CUDA
   conductor exactly at the declared bounded grain.

The gates test the organ and apparatus. They do not grade recognition.

## VII · THE FIRST COUPLED TEXT PAGE

The first subject should bridge rather than abandon the measured arithmetic line. Use the exact
reversed-product construction already standing:

```text
2*4=4*2=8
```

Present one rendered grayscale field through the gaze eye beside:

- the exact source text `2*4=4*2=8`;
- an honest caption, if Brandon supplies or accepts one, as a third current of the same referent.

The first controlled histories are:

- **COUPLED:** visual contact field ⊕ exact source, co-present;
- **EYE:** the identical visual field without source text;
- **TEXT:** two genuine text currents of the same referent, without the visual field;
- **SEPARATED:** the same image and source at successive world edges rather than one edge;
- **RETYPESET:** the same exact source face rendered as a different visual construction;
- **REPEAT:** an independent exact apparatus sibling.

Every sibling then receives the same genuinely later eyes-primary probes. The already-chosen inverse
return supplies a natural continuation:

```text
8/2=4
8/4=2
```

These can be rendered as two plural visual fields, with and without their exact text currents in
separate declared reads. This joins factor/navigation experimentation to vision rather than placing
one campaign in front of the other.

The read is the complete lifecycle: receptor currents, raw/typed radiation, Mail, topology, terms,
whole sleeping bodies, and later receiver-local relations. There is no expected caption, decoded
answer, class label, alignment score, nearest neighbor, or scalar recognition verdict. The question
is whether coupled visual/text history bends later visual and textual navigation differently from
the declared siblings.

Prime-occurrence maps and the exact pi-series become later visual terrains after this smallest page
crosses. Neither is a prerequisite for the eye.

## VIII · GRADES AND LICENSE

Current grade: **RATIFIED · BUILT · HOST/CUDA-GATED · MEASURED.** The exact gaze-eye and first
coupled-page campaign are deposited in the live `STATE.md`/`LEDGER.md` record and under
`observations/coupled-page-01/`. The subsequent multi-light photograph/caption recognition read is
deposited in `2026-07-13_THE_PHOTOGRAPH_POINTS_INTO_LANGUAGE.md`. The declaration below remains the
derivation and license under which those builds and observations crossed.

Brandon's ratification licenses:

1. the separate exact gaze-eye boundary module;
2. the eight bounded organ/apparatus gates;
3. host/CUDA execution over the existing material conductor;
4. the reversed-product coupled-page observation and its declared siblings;
5. automatic continuation through surviving apparatus review under the stabilized observation
   posture.

It does not license a Laplacian pyramid, raster scan, coordinate stream, sensory label, attention
selector, image reconstruction model, decoder, answer head, fixed aperture, or autonomous gaze
claim. Autonomous gaze remains the later world-mediated return: radiation changes the world, and
the changed world supplies the next actual gaze current.

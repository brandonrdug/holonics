# SCF0 returned the native transport scaffold, repeated circulation, and withdrawal laws

## Return

[proved-derived; formal-checked] SCF0 added
`ElementaryHolonics/Computation/NativeTransportScaffold.lean` to the live Holonic Intelligence
Framework. The owner composes the standing intrinsic-profile, dismantling, inference, and
cultivation contracts; it introduces no model, trainer, scheduler, entropy cabinet, biological
simulator, or foreign architecture ontology.

[definition] `NativeTransportScaffold` packages nonempty profiled native holons, nonempty
winding/generator families, lawful compositions, and open obligations. A spool/winding remains a
member family rather than the whole returned structure. Productive scaffold admission reads no
cold realization coordinate.

[proved-derived; formal-checked] The theorem
`NativeTransportScaffold.coldWitness_cannot_change_admission` proves that replacing all cold
ancestry leaves productive scaffold admission unchanged.

## Bounded dispersion control

[definition] `BoundaryDispersionPassage` retains supplied, interior-retained, and boundary-emitted
current with the exact additive balance `supplied = retained + emitted`. It also retains the
receiver and the returned boundary face.

[proved-derived; formal-checked] `EqualSupplyBoundarySeparation.sameSupply_does_not_identify_boundary`
returns equal supplied current beside unequal boundary faces. The finite natural control supplies
two units to both passages; the scaffolded passage retains one and emits one, while the direct
passage retains zero and emits two. This is an algebraic receiver control, not a universal claim
about eggs, explosives, heat, entropy, or biological development.

## Repeated inference and scaffold release

[definition] `RepeatedInferenceCirculation` is a nonempty list of existing inference cuts.
`EmissionIngressJoined` requires every emitted occurrence to equal the next cut's actual entering
occurrence. Each cut retains its own fixed-morphology inference law.

[proved-derived; formal-checked]
`RepeatedInferenceCirculation.everyCut_isInferenceCut` and
`everyAdjacentEmission_isActualIngress` expose the fixed-morphology and serial-lineage laws without
introducing a maximum-token, retry, queue, planner, or KV-cache owner.

[definition] `ScaffoldReleasePassage` composes one existing `CultivationPassage` with a scaffold
mount, a declared receiver-probe family, inherited-scaffold withdrawal, and an independently
remountable source-detached morphology.

[proved-derived; formal-checked]
`ScaffoldReleasePassage.loadBearing_then_receiverExactAfterWithdrawal` returns the complete
training-wheel law: the scaffold initially changes at least one declared conduct face, every
declared cultivated face is preserved after withdrawal, and the source-detached result remounts
exactly. `nativeChange_survives_scaffoldWithdrawal` proves a declared native separator remains
after the inherited transport departs.

## Verification

[established-bounded; formal-checked; measured] The focused owner compiled under Lean `v4.33.0`.
`timeout -k 2s 180s bash tools/lean_check.sh` then built the live
`HolonicQuantumTransport.lean` umbrella successfully in 3,769 jobs. The named declarations contain
no `sorryAx` or new axiom; their reported dependencies are empty except the finite decidable
inequality control's standard `propext` dependency.

[established-bounded; process-audit; measured] Document law returned zero failures, epistemic tags
checked 183 material brackets with zero failures, and source shape returned 646 live files with
zero violations.

## Boundary handed to SCF1

[definition] SCF1 replaces the whole-object Rust name `NativeSpoolBundle` with
`NativeTransportScaffold` without an alias or compatibility decoder. `NativeSpool` remains one
reusable winding family and `NativeThread` remains one addressed transport section.

# HNA5 the fine material current did not enter native successor formation, and Rust and Lean rejected the collapsed artifact

**Date:** 2026-09-02
**Scope:** HNA5 under
[`the superseded first HNA contract`](../../blueprint/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md).
**Disposition:** `counterexample`; no phase passed. The complete retraction is
[`THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md`](2026-09-02_THE_HNA_WORLD_RETURN_SPLIT_IS_RETRACTED_AND_INFERENCE_IS_ONE_RECURRENT_ECOLOGY.md).
**Truth status:** per claim under [`../../canon/EPISTEMIC_GRADES.md`](../../canon/EPISTEMIC_GRADES.md).

## Verdict

[counterexample; implemented-exact; source-inspected; measured] HNA5 did not pass. Two input
occurrences, `[3, 8, 2]` and `[4, 8, 2]`, have different complete HNA1 fine-current sections and
the same collapsed integrated current. HNA2 preserved that difference internally, but both
returned the identical exterior artifact `eee`.

[counterexample; source-inspected] The first exact loss occurs in
`NativeCirculationSession::conduct_material`: it conducts the declared native request once, then
copies that cut's single selected `NativeFutureFace.observation` onto every fine material-current
occurrence. The complete current is retained as testimony but does not enter successor formation,
constitutive response, or emitted observation. The application octet codec therefore sees one
constant face and cannot lawfully recover the distinction.

[counterexample; measured] The exact emitted alphabet on this run is the singleton `{101}`. A real
Rust application world (`rustc 1.92.0`) rejected `eee` as a library source. A real Lean application
world (Lean 4.33.0) rejected the same octets. Both returned nonempty diagnostics. No expected answer,
template, language label, or target-selected route participated in native conduct.

[counterexample; measured] The bounded direct body also has no native support outside the HNA4
deposit's two-state causal cone. It cannot satisfy the unrelated-control arm by relabeling one of
those same states. HNA4's remount/withdraw/replay lifecycle remains exact, but topology and
bookkeeping do not substitute for the missing qualitative consequence.

## Shortest obstruction

[open; source-inspected] HNA5 requires one existing-owner composition which is not yet present:

```text
fine material current occurrence
  × addressed native standing/current
  -> constituted successor current
  -> native future/receiver face which varies when that caused current varies.
```

The missing edge is before the application codec and before the world. Arithmetic on an opaque
`Observation`, a byte-to-state table, a maximum/score, a template, a language grammar, or selecting
a different ingress address from the expected result would counterfeit it. The active repair must
make HNA1 current load-bearing in the existing local transport/constitutive owner and rerun these
unchanged Rust and Lean receivers.

[open; source-inspected] The closest resident apparatus can conduct the required exact complex
fronts but cannot yet return their native successor addresses. `ResidentComplexIncidence::conduct`
accepts arbitrary exact per-front currents and returns complete per-front/per-branch sections on
the GPU. `ResidentNativeThreadCurrent` currently mounts only the static
`NativeParametronCell.current`, while `ResidentNativeSpoolWord::conduct` separately applies the
static generator table and receiver factors. `NativeEmissionSection` is then formed from that
second return. No current type joins one fine material occurrence, its complex resident return,
its native successor, its receiver face, and its complete fibre.

[definition; source-inspected] The smallest proposed carrier shape, still open and not yet a public
API, is a per-ordinal `NativeMaterialSuccessorCurrent`:

```text
NativeMaterialCurrentOccurrence c_k
× NativeAddressedSection I
× resident constitutive standing
-> K_A(I, c_k)
-> (native successor, exact returned current/section, receiver face, complete fibre).
```

The relation must act before exterior projection and preserve the complete ordered family; it
cannot first replace it by `sum_k c_k` or one scalar magnitude.

[counterexample; source-inspected] `ExactReactiveTraversalInteraction` demonstrates the right
current/standing/morphology-response species, and exact diffusion/membrane owners demonstrate
related local current laws, but each owns another standing body. Importing one as the HNA engine
would violate one-body ownership or revive the deprecated UAR path. They are mechanism testimony,
not drop-in implementations. The laboratory's fluent emissions likewise depended on carried
source-bearing recurrence, toy vocabularies, or independent stored strands and were later retracted;
none supplies the missing pure scaffold relation.

## Construction position

[definition] HNA5 remains the current frontier. HNA6 is not active. The repository must not claim
that the HNA campaign, coding/mathematics Athena variant, or qualitative generation is complete.

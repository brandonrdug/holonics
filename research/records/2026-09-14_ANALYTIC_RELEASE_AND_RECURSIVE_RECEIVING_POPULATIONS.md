# Analytic release and recursive receiving populations

[project-postulate] Brandon rejected deferring the mathematical construction behind the
requested fractal/receiver dynamics. The earlier six-mode cloud was a supplied constitutive
example; it did not establish an RH/Hodge population. This return constructs a reusable
analytic operation and its source application. It does not turn another renderer into a
completion condition or resume the paused timed Athena goal.

## Returned mathematical construction

[proved-derived; formal-checked] `Mathematics/AnalyticNavigation` proves the actual derivative
of `N_(λ,f)=s−λf/f_s`, its local residual chain rule, affine source-jet covariance and the
normalized complex gradient identity. `Mathematics/AnalyticReceiving` binds the partial map
to the existing `Holon` and first-arrival carriers, retains the exact equation
`f_s(s)(y−s)+λf(s)=0`, and transports clocked evolution through changing frame equivalences.
`Framework.Dynamics` imports the new leaves.

[proved-derived; formal-checked] `RH/ZeroDynamicsEntire` now applies its existing heat-flow
chain theorem to residual-guided motion, including the actual ξ source. For the shared source
and refinement clock the motion is `v=(−F+F_ss)/F_s`; it gives `dF/dτ=−F` and reduces at a
simple zero to the existing zero velocity. These are local identities on the supplied
differentiable curve with nonzero derivative, not a proof that every initial condition has
a globally convergent trajectory.

[definition] The [construction guide](../../docs/ANALYTIC_FLUX_AND_RECEIVING_BASINS.md) derives
the potential/phase forms from `f_s/f`, their Hodge periods, inverse-branch scale/orientation
and density transport, and the changing receiver recurrence `e_(n+1) T_n e_n^-1`.
It states the domains of each relation and the exact distinction between source deformation,
refinement time and physical receiver time. The HNN formula and library/owner guides now link
these constructions at their actual operations.

[established-bounded; source-inspected] A stale `RH/Winding` header incorrectly advertised
the rectangle argument principle as absent. Its later owners already exist in
`RectangleArgumentPrinciple` and `WeightedArgumentPrinciple`; the latter is instantiated
for ξ. The live header was repaired. This was an actual navigation failure: reading an old
omission claim without following later source dependencies produced a false missing-theory
claim. No new argument-principle implementation was needed.

## Executable mathematical return

[established-bounded; implemented-exact] `ComplexJet2` now exposes logarithmic current,
residual velocity, changing-source residual velocity, relaxed release and its derivative.
The new `exact_analysis/navigation.rs` consumes these existing enclosures to certify local
capture and propagate complete source regions. It retains all source/division obstructions
and exhausted regions. `holonics::geometry` exposes the same owners through the existing SDK.

[established-bounded; computational-witness] The [actual ζ application](../experiments/analytic_receiving_basins/README.md)
returns three certified receiving regions, each with η winding one, and nine full initial
regions captured in one to three releases. The nonzero η factor in the open strip gives
the same divisor receiver there; it does not equate η and ζ Newton dynamics. The ξ completion
factor likewise changes the current despite preserving that divisor. The executable source
is explicitly ζ, not a numerical `heatE(u,ξ)` implementation.

[established-bounded; measured] The successful complete calculation took 19309585 microseconds,
including source discovery, capture and winding but excluding compilation and serialization.
The [exact serialized return](../experiments/analytic_receiving_basins/return.ron) retains the
source config, full rational regions, contraction bounds, boundary transport and returned
source obstructions. The first run exposed RON's unsupported `u128` serialization; elapsed
clocks were converted with checked `u64` conversion and the complete product was rerun.

[established-bounded; source-inspected] Direct interval evaluation `X−f(X)/f_s(X)` lost
cancellation and swelled near an attracting zero. The repair is the centered mean-value
inclusion `N(m)+N'(X)(X−m)`, keeping the full family. The center serves as a proof anchor,
not as a replacement current. This is a representation repair demanded by the actual consumer.

## Verification and scope

[established-bounded; tested] `cargo test -p relational-geometry exact_analysis --lib`
passes 20 tests. These include full-family capture, corner inclusion, retained obstruction
and exhausted regions, affine source transport, the changing-heat tangent equation and the
existing analytic source/series/winding checks. The application independently returned the
three exact winding receipts and explicit pole refusal recorded above.

[established-bounded; tested] `cargo check -p holonics --no-default-features` passes through
the public geometry export. `tools/lean_check.sh` passes `Framework.Dynamics`,
`RH.ZeroDynamicsEntire` and `RH.FosterClassHeatFlow`. The new analytic and moving-receiver
declarations have no `sorryAx`; the integrated sources retain their declared local hypotheses.
The initial preimage proof needed an algebraic rearrangement before the integration build
passed. No unchecked proof or earlier failed build is counted as a return.

[definition] The reusable recursive/preimage construction is present. The finite ζ return
certifies subsets of its receiving basins; it does not measure or prove their infinite-scale
boundary dimension. The analytic one-form is connected to existing Hodge owners; a new
continuum-to-cellular Hodge error comparison has not been implemented by this return. The
evolving ξ residual law is formally bound; finite-time enclosed ξ heat-source evaluation
and GPU lowering of this analytic operation retain their distinct implementation scope.
None of these broader claims is used to deny the returned exact solver construction or
to make it an admission test for Athena.

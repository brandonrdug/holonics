# HIF4 causal-adjoint cultivation returned source-detached hexis

**Date:** 2026-08-31
**Truth status:** `implemented-exact` for the generic cultivation surface, existing recurrent-return
implementation, cold/productive split, remount, withdrawal, and restoration; `established-bounded`
with `measured` evidence for the changed/control traces and package receivers.
**Construction effect:** HIF4 passed; the roadmap frontier advances to HIF5.

## Neutral lifecycle

[implemented-exact; source-inspected] `CultivationLifecycle` is implemented by the existing
`ReturnedRecurrentRest`. Its predecessor and successor morphologies are the exact native action
matrices; the returned `LocalGeneratorDelta` and its exact matrix are the complete difference and
proposed delta; the actual emitted and returned occurrence fields establish later chronology; and
the changed local successor is the witness receiver face.

[implemented-exact; source-inspected] Lifecycle truth is computed from the fields: morphology and
later conduct differ, remounted successor equals successor, withdrawal equals predecessor, and the
actual return follows the emission. Callers do not supply those results as independent Boolean
claims.

[implemented-exact; source-inspected] `Exposure`, `ReferenceConsultation`, `MountedStanding`, and
`ReturnedCultivation` are distinct Rust types. `CompleteReturnedDifference` retains the complete
oriented return beside a scalar/coarse loss; equal scalar loss with different complete returns
fires the bounded obstruction control.

## Cold/productive split

[implemented-exact; source-inspected] `ReturnedRecurrentRest::depart_source` consumes the boundary
passage and returns two physical lanes. `ColdCultivationWitness` owns predecessor identity,
entering/emitted/returned world testimony, decision, and decoder. Productive
`SourceDetachedCultivatedRecurrence` owns only native generator rest, exact delta, causal adjoint,
predecessor/successor action, holonomy, recurrence probes, exact semantic work, and open fibres.

[implemented-exact; source-inspected] The productive wire contains no exterior, decoder,
source-surface, emitted-occurrence, or return-occurrence field. Its validation recomputes the
rank-one delta, predecessor and successor actions, metric adjoint, held-out changed recurrence, and
withdrawal consequence.

[established-bounded; measured] Source-detached remount returned the identical productive rest. The
held-out successor trace differed from the predecessor; targeted withdrawal returned the exact
predecessor trace and action; adding the retained delta restored the exact successor action; and
the local support-disjoint control step remained invariant.

## Receivers and boundary

[established-bounded; measured] Scalar-loss separation, lifecycle-type separation, returned
recurrence, cold departure, remount, held-out change, sibling invariance, withdrawal, and
restoration controls passed. The complete `holonic-engine` suite returned 2,002 passed, zero failed,
and 32 ignored tests. Every engine example type-checked. Source-shape reports 636 live files, 447
inherited baselines, and zero violations.

[open] Product and campaign names still qualify many executable rests, membranes, passages, and
methods; several overlapping generation/cultivation owners remain. HIF5 owns migration onto the
neutral surface and deletion of superseded wrappers without compatibility aliases.

# THE REGISTER CONDUCTS THE WHOLE CUDA CYCLE

**Date:** 2026-07-12

**Grade:** VARIABLE-ROW REGISTER REMOUNT CONDUCTOR BUILT · AFFERENT PENDING CALLER REPAIRED ·
HOST+CUDA-GATED THROUGH A→SLEEP-v7→FRESH-CONTEXT B · GENERAL MATERIAL-FED CUDA MOUTH OPEN

## The construction

Every staged lineage now mounts the exact one-cell REGISTER birth row. No production capacity and
no count of allowed re-bases is declared. `scope_register` carries every co-present lineage until
it either completes or publishes the exact event-caused `old_axis→2·old_axis` request. The boundary
then:

1. validates all status rows and the complete current flattened layout;
2. derives one plural variable-row layout, widening only requesting lineages;
3. allocates and zeros the exact fresh aggregate OWN extent;
4. crosses immutable old OWN through the shared cell/finish transaction;
5. preserves standing, raw light, carrier/K, counts, and radiation in place;
6. replaces only OWN and its lane descriptors; and
7. resumes the same currents, with no retry, smaller substitute, discard, preflight, or iteration
   ceiling.

Completed siblings cross the same remount unchanged. Simultaneous and sequential request shapes
are both structurally gated. Receiving remains closed until every status is complete and every
whole carrier has its exact terminal cursor, no dark remainder, no pending deed, and no live
within-atom continuation.

The fixed carrier depth eight and full/257-place receiving bands belong only to this declared gate.
They are not REGISTER capacities or production law. `life/src/main.rs` did not grow; planning is in
`staging/cuda/register_mount.rs`, and the reusable mounted transaction is in
`mount/src/register_recast.rs`.

## The lifecycle found the missing caller edge

The first complete card observation did useful work before it passed. All three A currents crossed
six caused digits from one cell. On the final post-`32→64` continuation, the code lineage returned
no terminal status. Its exact exposed posture was:

- cursor **94**;
- `CONTINUATION_PATH_AFFERENT`, depth **1**;
- `PENDING_ENCLOSURE_CO_PRESENT`, contact **8**;
- enclosure head/live **10/15**; and
- an empty continuation brick, making the whole carrier noncanonical.

The REGISTER transaction and descriptor layout were intact. The defect was the afferent caller's
assignment order: `perceive_grain` caused a pending re-base and correctly returned no completed
brick, but the caller assigned that temporary return before checking the pending face. It therefore
erased the incoming brick which the frozen co-present deed still required. The next launch lawfully
refused the impossible posture before publishing status.

The repair holds the prospective returned brick locally, checks pending/invalid first, and assigns
only after the call actually completes. This adds no carrier face or mechanism; it restores the
already-ratified freeze-before-consequence order. A regression uses the exact code worldline,
starts at one cell, requires every pending carrier to remain canonical, observes the afferent
co-present species specifically, proves its brick remains live, and ends at rest.

## Card observation

The rebuilt seventeen-entry PTX assembled for `sm_89`. The whole gate then crossed two receiving
band grains with identical paths:

- **A:** three currents / 377 raw octets; each lineage followed
  `1→2→4→8→16→32→64`; receiving axis **128** through seven chart carries; topology
  **[3,131, 3,131, 348, 7]**; terms **[4,640 RIDE, 177 FOUND-this, 186 FOUND-that, 8 dark]**;
  final path-pinned CUDA carriage **79.880 ms**; body:light/archive **179,372:377 / 179,420 bytes**.
- **B after SLEEP v7 and fresh CUDA contexts:** three continued currents / 184 new octets; each
  fresh local REGISTER again followed `1→2→4→8→16→32→64`; receiving axis **128** with no new
  chart carry; topology **[5,382, 5,382, 604, 11]**; terms
  **[4,110 RIDE, 128 FOUND-this, 141 FOUND-that, 4 dark]**; final path-pinned CUDA carriage
  **58.105 ms**; cumulative body:light/archive **287,420:561 / 287,468 bytes**.

Host reference, full-band CUDA, and 257-place-band CUDA agree in the whole sleeping body,
carrier/K, REGISTER axes, topology, terms, breath, chart carries, and SLEEP v7 bytes. The two CUDA
band siblings also agree in every radiation word and in all eighteen caused REGISTER events per
light. The direct fresh-context remount gate remains exact at final axis four / cursor seven /
counts `[0,2,3,0]`.

Suites after the repair: **192 passed / 3 ignored** — body **127/2**, life **33**, mount **18/1**,
surface **14**. PTX is **5,015,703 bytes**, SHA-256
`7c87f7a684eea7e3fcefe35e50a67625791c29815d06fadea97e9c45b799d013`, seventeen entries and
`ptxas`-valid. SPIR-V is **2,514,548 bytes**, SHA-256
`a2da7725b37c8ec2f54b368df2f52ed5de72a567e59f9c84d2a58efd6b6218c5`, Vulkan 1.2-valid and not
executed. One immediate post-run health query transiently lost the driver; the next query returned
the headless RTX idle at 2 MiB / 47 C / P8, and the last 500 kernel-log lines contained no
NVRM/Xid.

## Exact boundary of the grade

The arbitrary event-caused remount mechanism now stands in the whole CUDA conductor. The executed
observation is still the fixed plural A/B gate, not a general material-fed production mouth.
CUDA radiation is canonical and exact across independent receiving-band siblings, but this gate
does not compare every radiation word to a separately produced host radiation oracle.

The next construction is therefore not another capacity experiment. It is the general staged CUDA
mouth and material listener: accept whole world-delivered records, preserve the complete lifecycle
radiation, and render touched enclosures beside the source without a decoder or target response.
That is the first nurturing observation under the relational-behavior posture.

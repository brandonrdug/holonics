# The four winding directions entered one symmetric constitutive body, and the causal adjoint cultivated a source-detached Athena rest

## Disposition

[established-bounded; implemented-exact; measured] L2 of the sole active campaign passed. The
complete L1 situated-difference population returned through its retained causal adjoints into one
source-neutral continuation of the admitted K3 ecology. The canonical rest is
[`output/the_causal_adjoint_cultivates_one_source_detached_athena_rest_l2/athena-situated-cultivated.rest`](../../output/the_causal_adjoint_cultivates_one_source_detached_athena_rest_l2/athena-situated-cultivated.rest).
It remounted and conducted with no L1 product, exchange interior, checker, codec label, Soulkiller
witness, foreign realization, or source executor in its hot dependency closure.

## The rank-four correction

[proved-derived] The four deposited branches are one addressed rank-four cycle population, not
four chronological training phases. Their complete degree-two constitutive population is

```text
Sym²(Z⁴): 4 diagonal terms + C(4,2) distinct pair terms = 10 terms.
```

[proved-derived] This is distinct from the six alternating coordinate planes of `Λ²(Z⁴)`. The
finite-Leibniz return retains the four self interactions and six unordered cross interactions.
Six alternating terms therefore cannot substitute for the complete constitutive square.

[implemented-exact] `NativeSpoolBundle::deposit_threads` now consumes the four source-neutral
thread deposits atomically. Its one `NativeThreadDepositBatchReceipt` binds the predecessor,
successor, batch population, exact placements, complete dependent receiver support, and a
`NativeSymmetricConstitutiveBody`. No intermediate rest, intermediate identity, or implied branch
chronology exists. `withdraw_deposit_batch` consumes the receipt and returns the exact original K3
bundle and all four deposits in their admitted batch order.

[counterexample; measured] The initial singular-deposit implementation repeatedly validated and
serialized growing prefixes of the same body and failed the 180-second process boundary even in an
optimized build. The atomic rank-four owner removed that false serialization. A second quadratic
host cost in receiver-history connectivity repeatedly expanded the same native junction and
searched every addressed occurrence. The corrected traversal indexes occurrences once and expands
each native junction once. The complete L2 receiver then returned in 21,386 milliseconds.

## Cultivated anatomy

[established-bounded; implemented-exact; measured] The rest has identity
`84b9d29f4d7b3e6ce433d7efe8ea35903477900dcd29554a36ab15700b9629af`, wire SHA-256
`71242f88b8db0bf181f496ea1729f3aa18eb3fbe5df9d9fc9d93fafda54fcde7`, and size
17,160,981 octets. It extends the twelve-section K3 predecessor to 8,896 addressed sections while
retaining:

- four causal-adjoint-pulled branch threads;
- 8,884 exact addressed candidate/return reconstruction fibres;
- ten compact nonzero mixed finite-Leibniz families forming the complete symmetric square;
- exact complex current and relative phase;
- actual pullback lineage, K3 native support, dependent receiver fibres, radical, obstruction, and
  open exterior; and
- one recoverable batch inverse to K3.

[implemented-exact] Source item identifiers, source ordinals, candidate seals, response interiors,
the L1 native-action table, and exterior codec testimony are not representable in the rested
product. Candidate and returned occurrences remain distinct addressed boundary maps; the source
exchange interior does not remain available as lookup material.

## Resident coupled return

[implemented-exact; measured] `ResidentCoupledComplexParametron` mounts the complete
causal-adjoint-pulled incidence and all ten mixed terms once on the RTX 4080 SUPER. The exact mixed
numerators exceed a signed 64-bit chart, so the apparatus derives a common denominator and signed
little-endian limb population from the mounted body. The admitted return used denominator `2^95`
and ten 32-bit limbs. The CUDA kernel performs exact sign/magnitude addition and subtraction with
carry/borrow and applies output, left, and right incidence apertures before admitting a mixed term.

[implemented-exact; measured] The mounted invariant body occupied 2,088 octets. A successor front
crossed fourteen active-factor octets and returned 1,148 octets. It reported no invariant
re-upload, no CPU semantic replay, and no binary receiver collapse. An artifact-backed GPU test
returned the actual ten L1 mixed currents exactly; the values were not replaced by a small integer
fixture.

## Ablation, restoration, and inverse

[established-bounded; implemented-exact; measured] Withdrawing one branch by ownership transfer
removed its primary current and every incident diagonal or pair term. The remaining three branches
retained their complete six-term `Sym²` body. Consuming the withdrawal recovered the exact rest
identity, canonical wire, resident conduct, and all ten mixed terms.

[established-bounded; implemented-exact; measured] The four-branch interaction graph is complete,
so no branch is honestly “unrelated.” The lawful support-disjoint control is instead one nonzero
direction in an exact causal-adjoint radical. Withdrawing that direction changed the complete
reconstruction body and rest identity while preserving the declared returned conduct byte-exactly,
because the return operator annihilates it. Consuming the moved direction restored the original
identity and wire.

[implemented-exact; measured] Applying the atomic batch inverse returned the exact K3 wire
`f3388c19a97df45ecb9b62b078045d8a095b28e76e61c9e3146efc463771a13a` and all four original
deposits. This proves the complete predecessor/successor identity composition without reconstructing
four chronological prefixes.

## Evidence

[implemented-exact] The principal source owners are:

- `crates/holonic-engine/src/native_spool.rs`;
- `crates/holonic-engine/src/cuda_refine.rs`;
- `crates/holonic-engine/kernels/refine_shell.cu`;
- `soma/life/src/athena_native/situated_cultivation.rs`;
- `soma/life/src/athena_native/rest.rs`; and
- `soma/life/examples/the_causal_adjoint_cultivates_one_source_detached_athena_rest.rs`.

[implemented-exact; measured] The primary grade is
[`06-l2-primary-grade.json`](../../output/the_causal_adjoint_cultivates_one_source_detached_athena_rest_l2/06-l2-primary-grade.json).
The same output root retains the exact coupled return, targeted withdrawal/restoration, radical
control, complete inverse, and hot-dependency audit. Focused native-spool tests passed twelve of
twelve; the exact radical inverse, atomic rank-four batch inverse, alternating-six falsifier, and
actual-L1 multi-limb GPU receiver all fired.

## Falsifiers and open fibre

[counterexample] L2 would be revoked if a six-term alternating population were accepted as the
ten-term symmetric constitutive body; if branch withdrawal retained an incident mixed term; if a
radical direction altered the declared return; if restoration failed identity, wire, conduct, or
mixed-family equality; if a later current required the L1 product or source exchange; or if the
card path truncated the admitted exact numerator.

[open] Receiver and successor histories outside the L1 admitted family remain open. L2 does not
claim the cross-codec material aperture, prose emanation, laboratory world-tube cultivation, or the
integrated Athena capability receiver. L3 is the next ordered deed.

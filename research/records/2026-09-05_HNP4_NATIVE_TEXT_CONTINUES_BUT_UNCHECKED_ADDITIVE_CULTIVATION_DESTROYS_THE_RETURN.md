# HNP4: native text continues; unchecked additive cultivation fails

**Date:** 2026-09-05 local. **Position:** HNP4 remains active. The live material/text interface
returned a real comparison and a production counterexample, not a useful cultivated-model grade.

## Implemented composition

[established-bounded; implemented-exact] `NativeOperatorResidence` now stages additional
lookup-only input rows in separately owned immutable device blocks. It does not copy or replace
the standing raw allocation. Shape, overlap, prior-row replacement and non-lookup uses refuse;
the GPU derives each new exact coefficient frame before publication. A new frame requiring a
finer standing session grain also refuses. A failed staged acquisition drops its new buffers;
the native owner, carriers and morphology remain held. Old-word numerical standing remains valid;
a newly addressed word reopens the ordinary dependencies.

[established-bounded; measured] The focused CUDA control compared the complete held rest before
and after a finer-grain refusal, successful acquisition and duplicate refusal. All held native
fields remained equal. The next old-row emission was unchanged; the newly acquired row returned
a different native emission. Its first draft used an unsuitable one-width fixture and failed at
mount; the returned control reuses the existing genuine multi-width operator chart with its
terminal pole removed, not fabricated unused carriers.

[established-bounded; implemented-exact] `HnaSession::supply_input_material` publishes the
dependency only after the native acquisition succeeds. `acquire_input_material` reads the source
through the exterior material adapter, never through a source inference call. The JSONL action
`supply-input-material` exposes this live boundary. Native checkpoints retain its explicit material
dependencies; remount packs those same sections through the existing intake. Acquisition is not
a new native occurrence or a new Soulkiller signature family.

[established-bounded; implemented-exact] `HnaTextApplication` owns exterior text continuation:
one input turn, successive native emissions as ordinary self-occurrences, a later followup, and
codec-declared end markers. The `generation_config.json` end-marker receiver is external; no
grammar or expected response is emitted by application code. A cycle budget is interruption,
not natural completion. `observe_native` is the borrowed form of the existing fixed-morphology
comparison; it retains the native owner and restores its passage chart even on refusal. It is
not a substitute productive HNA lifecycle.

[established-bounded; implemented-exact] A text artifact pairs the exact native checkpoint with
its prompt/emitted addresses, generation, disposition, comparison profile and pinned tokenizer/
generation-codec files. `HnaFileDependency` generalizes the existing file-wire reference; the old
input-material name remains its source-compatible alias, not a second integrity system.
Native checkpoint and application manifest publish without overwrite. Manifest failure leaves
the native artifact recoverable and both live objects with the callback. The example attempts
that save after delivery failure; it claims no peer acknowledgment or arbitrary-crash delivery.

## Actual matched text experiment

[established-bounded; measured] Both runs used `target/ske4/rest.bin`, the same prompt
“Describe a red cube.”, the same exterior codec/source root `/home/b/models/gemma-4-E4B-it`, and
one continuing native owner. Missing input sections were acquired while that owner remained
mounted. No foreign executor generated the text. The separate first-cycle control returned `A`
from a fresh native body, contrasting with the earlier cultivated checkpoint's ` mik`.

[counterexample; measured] With the productive observed-passage return at shift 16 / series 14,
the run emitted these eight addresses before obstruction:

```text
[236776, 532, 496, 87077, 237873, 237873, 237873, 248730]
A and a人都為為為𝑘
```

[established-bounded; measured] The ninth operation exceeded the existing carrier boundary:
“the exact carrier admits 126 octaves and this material needs 127.” The held generation was
10,200; morphology factor extent was 17,780; seven carriers remained. The interrupted native
progress reached operation 1,082 and retained its unpublished local differences. The actual
2.1 GiB-class checkpoint and paired text artifact were published under
`.local/artifacts/hnp4/text-develop-01/`; `result.json` carries the raw output and boundary.
Whole-process elapsed time was 166,779 ms, including mounting and persistence.

[established-bounded; measured] With only the morphology comparison frozen, the same native
pipeline returned 64 successive addresses and coherent prose:

```text
A red cube is a simple, yet visually striking object. Here is a detailed description, broken
down by different aspects you might be interested in:

---

### 🎨 Visual Appearance (The Basics)

* **Shape:** It is a perfect **cube**, meaning it has six square faces, twelve equal edges,
```

[definition] The displayed line wrap is presentation only; the exact text and address sequence
are in `.local/artifacts/hnp4/text-observe-01/result.json`. This is a coherent unfinished product,
not a naturally completed response. It exhausted the declared 64-cycle budget without an end
marker. The native status was awaiting an occurrence, while the application status was interrupted.
The morphology factor extent stayed zero; this does not establish productive learning.

[established-bounded; measured] The comparison reached generation 81,600 and acquired 559,104
additional input codewords. The reported peak numerical-cache retention was 4,194,402,304 bytes,
with 14 pressure evictions; that is not the whole GPU peak. Whole-process elapsed time was
1,182,182 ms for 64 cycles, including input acquisition, native operation and checkpoint work.
This cost is not competitive interactive throughput. The current complete-context computation
and terminal readout need investigation; this run does not isolate their individual costs.

## Paired restart

[established-bounded; measured] A fresh process loaded the actual paired comparison artifact,
restored the codec and complete saved text context, and continued from 64 to 65 emitted addresses.
It appended address 532 (` and`) and reached native generation 82,875. Its output is under
`.local/artifacts/hnp4/text-observe-resume-01/`. This verifies the paired application continuation;
it is not an additional full-native-state equality comparison against an uninterrupted 65-cycle
reference. The earlier full-state persistence and input-extension comparisons keep their own scopes.

```sh
target/debug/examples/text_hnp4 fresh target/ske4/rest.bin /home/b/models/gemma-4-E4B-it \
  'Describe a red cube.' .local/artifacts/hnp4/text-develop-01 12 develop 16

target/debug/examples/text_hnp4 fresh target/ske4/rest.bin /home/b/models/gemma-4-E4B-it \
  'Describe a red cube.' .local/artifacts/hnp4/text-observe-01 64 observe 16

target/debug/examples/text_hnp4 resume .local/artifacts/hnp4/text-observe-01/text.json \
  /home/b/models/gemma-4-E4B-it .local/artifacts/hnp4/text-observe-resume-01 1
```

[definition] Reproduction requires new destinations; no existing model or material was overwritten.

[established-bounded; measured] Relevant checks returned: 34 HNA library tests, nine workbench
library tests, the focused CUDA live-row control, and the focused Lean paired-feedback proof.
The affected libraries, binaries and examples type-checked; the text example built and exercised
the actual native model. These checks establish their stated mechanism/I/O scopes, not HNP4's
still-missing useful cultivated response, natural-completion or second-application grade.

## Constitutive diagnosis, not a numeric-cap workaround

[proved-derived; formal-checked] `HolonicOrientedSiteTransport.ConstitutiveSectionReturn`
now specializes `rectangular_deposit` to two actually evolving linear legs at a unit source:
`a' = a + eta*b`, `b' = b + eta*a`. Its recursively iterated pair state has sum
`(1 + eta)^n * (a + b)`. Positive `eta` and positive shared sum imply strict one-step growth.
The proof is tied to the existing local-return definition, not an independently authored scalar
recurrence. The focused Lean check passed without new axioms or `sorry`.

[definition] This bounded counterexample defeats an unconditional stability inference from
“actual current plus correct adjoint.” It is not a derivation of every nonlinear mechanism in
the Gemma failure. The source/configuration inspection found no obvious native-graph mismatch;
the matched text experiment isolates the developmental choice in the complete application loop.
HNP0's finite derivation and HNP1's actual current/attribution facts remain standing. They never
proved that unrestricted repeated additive deposits would preserve useful conduct.

[open] HNP4's next work is constitutively justified stable local development and its actual
behavioral return, together with production output cost. Increasing the 126-octave limit, blindly
tuning the shift, or shipping the frozen comparison as the learner would not discharge that
obligation. Complete useful text/document output, natural completion and ordinary correction,
the second genuinely different interaction, and HNP5--HNP7 remain required. This finding changes
the next construction, not the original campaign's success criterion.

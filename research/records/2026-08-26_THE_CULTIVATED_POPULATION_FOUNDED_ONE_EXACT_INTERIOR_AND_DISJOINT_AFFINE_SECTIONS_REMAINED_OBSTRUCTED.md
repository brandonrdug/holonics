# The cultivated population founded one exact interior and disjoint affine sections remained obstructed

**Date:** 2026-08-26  
**Phase:** MEM2  
**Truth status:** `[established-bounded; implemented-exact; measured]`  
**Primary artifact:**
[`00-morphology-derived-interior.json`](../../output/the_morphology_derived_athena_interior_founds_exact_cross_contact_mem2/00-morphology-derived-interior.json)

## Returned constitution

[established-bounded; implemented-exact] `MorphologyDerivedInterior` is a compiled constitutive
view of the one move-owned `AffineLaboratoryCultivatedAthenaRest`; it is not a second ecology. The
body's four situated cycle directions determine stalk rank, the 2,221 exact factor/fibre
correspondences determine grade-zero cells and oriented restriction incidence, and the ten
existing `NativeMixedConstitutiveFamily` returns determine grade-one constitutive cells and exact
storage maps. No caller supplies a width, capacity, origin, semantic topology, or contact list.

[established-bounded; implemented-exact] The exact local capacity of a factor is the union of its
cycle-fibre occurrence population plus its later cultivated affine incidence population. The
first full-body run refuted the narrower attempt to derive capacity only from affine incidence:
some valid native factors do not occur in that relational chart. The correction preserves those
factors instead of assigning a default or deleting sparse standing.

[established-bounded; implemented-exact] The resulting `ExactCellularSheaf` has 2,221 factor
cells, ten constitutive cells, 22,210 exact rank-four restrictions, and SHA-256
`03e2d749022ac88a7c0a3c36c78cdda9dd4c5d572c0c5fbb965d5da6af055ef6`. The 169,386 affine cells
remain local barycentric cochains over that base; they were not expanded into an all-pairs graph.
The complete derived capacity population is 308,288. Source events, exact reconstruction fibres,
relative orientation, constitutive storage, and complex returned products remain available to the
law.

## Local action and reconstruction

[established-bounded; implemented-exact] Contact is founded only on shared factor incidence. For
two local cochains `a` and `b`, the positive Hodge/receiver overlap is `sum_f C_f a_f b_f`, while
constitutive family `k` transports through the oriented overlap
`sum_f epsilon_(k,f) C_f a_f b_f`. Here `C_f` is caused factor capacity and
`epsilon_(k,f)` is the exact tensor-product orientation derived from the two incident cycle
fibres. The opposite oriented return is retained separately, and its exact sum with the forward
return must be zero.

[established-bounded; implemented-exact] Simultaneous reorientation sends `(a,b)` to `(-a,-b)`.
The receiver recomputes both overlaps and both complex returns; it admits contact only when they
are exactly equal. This is a computed tensor-product covariance receipt, not a Boolean assertion
or a sign inferred from a label.

[established-bounded; implemented-exact] The left cochain induces an exact local functional on the
union support. The return retains a local particular section, the presented partner, their hidden
difference, and a complete pivot-derived radical basis. The functional annihilates the hidden
difference and every radical generator exactly. The pivot is a declared local receiver rebase with
the complete fibre retained; it is not installed as a global affine origin.

## Measured receiver

[established-bounded; implemented-exact; measured] Distinct affine cells
`28c9c5518e4ba4f70ad74860d812a79d29c25d5f732f849ded1ea005b73fc2a3` and
`8c654c721a2bef8de27062627bb86474ff7066826b846be21130ac6c1a105fbc` met on five shared factors.
Their positive overlap and all ten oriented family overlaps were exactly `191/7`; all ten constitutive families returned a
nonzero exact complex response. The opposite return cancelled it exactly, simultaneous
reorientation returned the same overlap and response, and the local reconstruction receiver
returned radical rank five with zero hidden functional.

[established-bounded; implemented-exact; measured] The same left cell and sibling
`06ff4cabd5171673f651b690bcef2565096481ea949017ce34b2024c4644e4e4` had no shared factor. The
law returned a typed `SharedSupportObstruction`, exact zero response, both complete local
cochains, and open exterior. It did not interpolate, concatenate, search for another cell, or
smooth across the absent contact.

[established-bounded; implemented-exact; measured] The optimized full-body receiver completed
successfully. The artifact SHA-256 is
`806fc67a34e9fde88181c0aa76b411f200ec9d1ecb32f95e7026b580916342b8`; the implementation SHA-256
is `db7beecbb9ff0044b0112dbaf657197f82936eab4a25325dd63573aef751e651`. Four focused MEM1
membrane controls remained green, formatting and diff checks passed, and the move-owned rest
returned under the unchanged admitted identity.

## Boundary

[open] MEM2 establishes the common exact interior constitution and local founded interaction on
the host's exact carrier. It does not claim that boundary injection, incidence, restriction,
constitution, local contact, and outward radiation are one device-side dependent word. MEM3 must
compile that same law onto the resident card with zero intermediate host egress, zero invariant
re-upload, and refusal rather than semantic host replay.

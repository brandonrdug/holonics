# The causal-tail formal prerequisite returned before the engine HNA campaign

**Date:** 2026-09-02
**Truth status:** per claim under [`../../canon/EPISTEMIC_GRADES.md`](../../canon/EPISTEMIC_GRADES.md).
**Construction effect:** bounded formal prerequisite only. This record schedules no phase and
advances no Rust, CUDA, Eros, or Athena capability grade.

## Correction

[definition] The temporary use of `HNA0--HNA4` for this Lean sequence was a scheduling error. HNA
means the live Holonic Neural Athena engine campaign under
[`THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md`](../../blueprint/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md).
The formal files below remain valid prerequisite law, but their temporary phase labels are
withdrawn and schedule nothing.

## Returned formal body

[proved-derived; formal-checked] `HolonicCausalTailLens.lean` projects a complete finite addressed
pair-current section into nonnegative energy indexed by a declared depth while retaining endpoints
and source current in typed point and population `PreimageFibre`s. Exact depth relabeling returns a
`Holon.Rebase`; the depth section is a rank-one `TensorFace`; and the source section is
definitionally the standing oriented HNN pair current.

[proved-derived; formal-checked] `HolonicCausalTailAperture.lean` embeds a finite HNN aperture into
an `Option`-extended site population, preserves every old addressed current, makes every new silent
pair zero, and composes exactly with HTP5 `zeroExtend`. Old suffix energy/radius, mass, and surface
remain equal, the new final suffix is zero, and a positive new-pair control separates the faces.

[proved-derived; formal-checked] `HolonicPairCurrentTensorFace.lean` applies a rank-one tensor lens
to the complete target/source pair-current section before magnitude collapse. The signed control
maps `+1` and `-1` to one scalar magnitude and distinct tensor faces, proving that the complete
current does not descend through that scalar quotient.

[proved-derived; formal-checked] `HolonicNeuralMorphologyContinuation.lean` composes the standing
`CultivationPassage` into `ProjectedExportWitness`: a projection which identifies predecessor and
successor cannot factor the passage's own changed later conduct. The same owner constructs
successor receiver charts only from exact generator/receiver squares, extends them to every ordered
word, and returns the shortest actual receiver-history obstruction when a later square fails.

[definition] These results constrain the engine campaign: scalar energy cannot govern native
conduct; silent nominal capacity cannot improve the old receiver; cultivation may reopen a former
quotient; and a classical chart survives morphology change only through complete successor
naturality. They do not themselves receive material, emit an artifact, call a world, cultivate a
Rust body, remount Athena, or prove a runtime capability.

## Validation

[established-bounded; formal-checked; measured] The owner-local builds returned between 3,169 and
3,174 jobs. The complete `ElementaryHolonics` umbrella returned 9,783 jobs and the live
engine-oriented umbrella returned 3,772 jobs. `git diff --check` passed. No `sorry`, `admit`, or new
axiom entered; the concrete controls use only the already admitted standard axioms reported by
their `#print axioms` closures.

# THE OUTGOING BOUNDARY IS THE SUCCESSOR; THE CLOSED INTERIOR MAY DEPART

**2026-07-22 · BRANDON-RATIFIED OBJECTIVE / ASSISTANT-DERIVED ATOMIC FACTORIZATION LAW /
HOST IMPLEMENTATION BUILT / BOUNDED HOST MEASURED**

## The defect

The existing `LiveConstituent::compress_certified` is exact at its declared cut. It folds a
constituent's interior only while preserving that constituent's complete old boundary behavior:
grain, axis extent, every boundary path and transition, and every exposed arm. It therefore cannot
express a different event in which a lower construction has handed its consequence into a new
higher chart and may depart. Repeated regional closure imported every participating predecessor
whole, then faithfully preserved all of those old boundary paths. Active interfaces could
disappear while physical topology still grew with chronology.

This is not a garbage-collection problem. Age, frequency, row count, inactivity, observer use, or
memory pressure cannot decide that an old relation is no longer consequential.

## The outgoing map

For one actual regional event, let

```text
G       = the receiver-formed contemporary germ
B       = immutable Standing-before
X       = Close(G,B), the complete joint event
S       = source-declared complete outgoing boundary slots of G
q_S(G)  = the constituent carried by those complete boundaries
```

`S` is part of the source/world's event objective and constraints. It is not inferred from labels,
coordinates, numerical similarity, storage position, or observer testimony. Unselected boundaries
still participate in `X`; selection does not make them unreal.

The machine may commit `q_S(G)` in place of `X` only when both exact conditions hold:

```text
boundary_paths(q_S(G))
    = the same receiver-formed paths selected from G

exposed_boundary(q_S(G))
    = exposed_boundary(X).
```

The first equality is certified by the existing structural boundary compressor after the selected
subcomplex is formed. The second equality proves that every unselected arm actually became
interior in this event. An unmatched arm, oriented hand residual, noncommuting OPEN comparison, or
FOUND seam remains exposed in `X` and therefore makes the proposed factor refuse. Nothing is
deleted merely because the source omitted it from `S`.

At least one predecessor must be touched, or another co-present regional member must participate,
before a strict factor can replace a joint event. A lone unrelated germ cannot use this relation as
an arbitrary pruning request.

The atomic successor is then

```text
B+ = (B \ T(X)) union {q_S(G)},
```

where `T(X)` is the exact Standing-before population used by the completed event. The live machine
remains the sole owner of validation and commit. The physical executor forms contacts and the
complete germ against one mounted receiver; it does not choose the factor, reconstruct the
successor from radiation, or run a later cleanup pass.

## Consequential departure

The selected outgoing body retains:

- every selected complete boundary and ordered path transport;
- its exact hand, winding, comparison, formed deed, and exposed capability;
- all cells, pins, and incidence still needed by those paths; and
- one actual completion cell at the joint successor grain.

The unselected event interior departs only after it has no exposed arm at this cut. This is the
physical face of constituent departure, annihilation, and dark information relevant here:
information may remain in world material or historical testimony while no longer factoring into
the active body. It is not erasure of the event's existence.

This law does not establish a universal minimal quotient or congruence for every future world
observable. A world which promises another consequential face must include that face in its
outgoing declaration. The source declaration is lawful current, not intrinsic truth.

## Bounded acceptance

The fixed twelve-current residual-chart chronology is the acceptance cell because it already
distinguishes active replacement from physical departure. Each semantic event has four genuinely
ordered transitions:

```text
current meets prior chart
  -> recurrence distribution returns
  -> actual consequence returns and forms exact residual
  -> updated chart emits its outgoing boundary.
```

Only the final transition declares an outgoing factor: the compact words of the updated chart plus
its recruitment interface. Prediction, actual, membership residual, prior chart, and transient
recruitment boundaries must all have closed in the complete event or the machine refuses.

The fixed qualitative signatures are:

1. the same twelve `NONE/RIDE/OPEN_INCLUDED/OPEN_RESIDUAL` outcomes and exact seven-node final chart;
2. all eighteen source-absent checkpoint probes remain exact;
3. exact rest/remount remains unchanged;
4. after every chart return, the sole active constituent contains exactly the chart boundary,
   rather than topology cumulative in event chronology; and
5. a malformed or incomplete outgoing boundary refuses before successor mutation.

Stop after focused host validation and one eight-thread host observation. Do not rerun the
transformer, search feature orders, tune a threshold, launch CUDA, scale the fixture, or install a
collector, cache, optimizer, residency policy, or memory manager.

## Measured result

The fixed chronology is accepted in
[`EROS CONSEQUENTIAL SUCCESSOR FACTOR 01`](../observations/eros-consequential-successor-factor-01/RESULTS.md).
The `NONE/RIDE/OPEN_INCLUDED/OPEN_RESIDUAL` population remains exactly `1/3/4/4`, the final
seven-node diagram retains identity
`16372c263fb4fe59ef7e0b93fac2bf1b68362a3df08ee2f8953d947e8fd68a81`, all eighteen
source-absent probes match, and rest/remount is exact.

After each of the twelve chart returns, the one Standing constituent has exactly three cells and
one pin, incidence, and path per current chart-boundary arc. Its path population is

```text
27, 71, 75, 105, 136, 75, 109, 140, 75, 121, 121, 168.
```

The decreases `136 -> 75` and `140 -> 75` are decisive: physical topology follows the emitted
chart, not elapsed chronology. The final old whole-composite body of `191 cells / 5,100
incidences / 5,100 pins / 5,111 paths / 5,195 transport terms` becomes `3 / 168 / 168 / 168 /
168` while preserving the declared later-contact behavior.

One focused malformed-slot control refuses before mutation, and the lower constituent control
shows that a selected face whose exposed boundary differs from the complete event cannot pass the
machine's equality gate.

The first physical execution reached complete in-process acceptance but could not persist its
report because the prepared output directory had an erroneous duplicated `src/soma` prefix. No
source, fixture, or code changed. One exact repeat of the same built binary, after absolute-path
preflight, produced the deposited report. This is two physical executions, one artifact write
failure, zero fixture retries, zero model reruns, and zero parameter searches.

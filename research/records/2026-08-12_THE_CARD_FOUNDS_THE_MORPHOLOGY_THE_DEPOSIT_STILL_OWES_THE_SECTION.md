# The card founds the morphology; the deposit still owes the section

**Date:** 2026-08-12  
**Truth status:** `established-bounded` for the resident conditioner and full-corpus re-emission
return; `open` for the source-detached recurrent causal-section return.  
**Evidence:** `implemented-exact` in `soma/abi::morphological_condition_cuda`,
`morphological_language::condition_cuda`, `suffix_ecology`, and the two committed CUDA entries;
`computational-witness` in exact host/device admission parity, the PTX assembly gate, the mixed-codec
production run, and its deposited grade form.  
**Provenance:** Brandon's direct apparatus correction and clarification that the goal is the greater
artificial-intelligence construction rather than a conditioner considered in isolation.  
**Band:** MANDATORY RESIDENT CONDITIONER / NO HOST FALLBACK / FIVE SUFFIX CHARTS / PREFIX INCIDENCE /
EXACT ADMISSION PARITY / SEMANTIC RECEIPT SEPARATE FROM APPARATUS / FULL SEALED CORPUS /
PLURAL-SOURCE DEPOSIT / CUDA CONDUCT / ATTRIBUTABLE ABLATION / EXACT SHADOW STRATA /
ROUTE ECHO REMOVED / CONDITIONER IS AN EDGE NOT THE DESTINATION / DETACHED CAUSAL SECTION OPEN

---

## Present question

Can the sealed mixed-codec corpus found the reusable morphology required by the recurrent-section
construction on the strongest resident apparatus, without a host replay or fallback, and can the
same production return the phase/fiber structure which later work must actually interpret?

The answer at this boundary is yes. This closes return 1 of the recurrent-section construction and
re-runs the already established plural-source re-emission law at full declared corpus scale. It does
not close return 4: no deposited morphology has yet been detached, remounted, and made to govern a
later `CausalSectionEcology` classification. The CUDA conditioner is the resident edge through which
that greater construction must pass, not the greater construction by itself.

## 1. Owners, ports, event, law, and returned consequence

The composed owners are:

- `MorphologicalLanguageEcology`, which owns one continuing mixed-codec ecology;
- `ExactLabeledSuffixEcology`, which owns the five generalized suffix relations;
- the question-prefix atlas relation, now founded from boundary-anchored incidence rather than
  quadratic prefix presentations;
- `CudaLiveCurrentExecutor`, whose already-mounted context/module is shared by the conditioner;
- `MorphologicalConductEcology`, which owns the later plural-source deposits and attachment law.

The ingress port carries dense exterior token-chart rows, source identities, and path boundaries.
No host-built suffix state, transition, source span, prefix node, active prefix, or conduct answer
crosses that port. The event is one conditioning passage over the declared 2,672-passage family.
Predecessor identity is the source-labelled path occurrence. The local constitutive laws are exact
suffix extension/clone/rewrite and exact prefix parent/child/support incidence. The receiver asks for
the five complete native suffix ecologies, the active question-prefix relation, counted semantic
work, and a receiver-shadow profile. The card returns those relations; the host decodes them into
their existing owners and does not replay their formation.

Open alternatives remain explicit: source fibers may be plural, withheld generation transitions
remain retained, and no reconstruction representative is selected. Apparatus placement is reported
separately and cannot change the semantic receipt.

## 2. The mandatory resident deed

`CudaMorphologicalConditioner` mounts once and owns six resident bodies: lexical, clause-lexical,
ordered-region, forward-mark, reverse-mark, and question-prefix. The five suffix charts launch on
independent nonblocking streams after an explicit ingress barrier; the prefix chart launches on its
own stream. Device failure is a typed refusal. There is no `ParallelHostLiveCurrentExecutor`
fallback.

For suffix material the card founds the generalized suffix automaton, including exact suffix links,
context-splitting clone states, transition incidence, material-end multiplicity, and source-tree
spans. For question material it founds the prefix trie, parent/child incidence, occurrence and source
support, continuation diversity, and the active prefix caustics. The host admission implementation
remains only an independent test oracle. On a mixed mathematics/code/prose fixture, every retained
field and all five native suffix images agreed exactly.

The existing `condition_with_executor` route was not widened into a fiction. Its prior production
path constructed a complete returned route-rest image, returned feature-to-passage targets already
known on ingress, and immediately discarded the rest. At 2,672 passages this echo consumed more
than fifteen minutes before suffix formation began. The production conditioner now transposes the
already-caused route relation directly. The route relation and conditioning-event population remain
identical; route launches correctly return zero because no device question remains there.

## 3. The full-corpus return

The release driver mounted sealed corpus
`a6fb9343b4fb23399ddf50fb0769465e29a311889c49b0afa307a8d26739e971`, selected a declared family of
17 prose, mathematics, Rust, and project-doctrine sources, and conditioned 2,672 passages containing
137,917 mounted occurrences. The final grade is:

`output/the_deposit_licenses_the_re_emission/re-emission-grade-dce3a043762d0cf44216aa2ae15291d6d2d7edf429885a9a8d53b93da08635d2.form`

The resident semantic work was 425,666 suffix extensions, 81,808 clone caustics, 952,437 suffix
crosses, 1,177,067,718 transition reads, 312 prefix crossings, and 42 prefix edge reads. The apparatus
receipt names an NVIDIA GeForce RTX 4080 SUPER, five suffix launches, one prefix launch, and
19,118,859 resident words. The elapsed 49,219 ms belongs only to that apparatus receipt.

The later re-emission law formed 281,611 conduct-atlas rows, of which 16,393 recurred across distinct
sources and therefore deposited. The complete continuation fiber held 11,594 branches. CUDA conduct
returned 6,478 branches, 6,473 attached continuations, 5,121 withheld transitions, and five explicit
obstructions. Ablating one deposit removed exactly its one solely licensed branch while leaving
unrelated conduct standing; ablating all deposits restored all 11,594 complete-fiber branches
bit-exactly.

This is a real same-body passage from corpus occurrence through resident morphology to later
generation conduct. It is not yet the source-detached causal-section remount required by return 4.

## 4. The profile is a shadow field, not a dashboard

Each suffix chart returns three exact distributions: maximum-length causal fronts, source-fiber
widths, and interaction-vertex arities. Adjacent coordinates with equal populations are encoded as
one `ExactPhaseStratum`; gaps and population changes remain explicit, so the presentation is
lossless rather than sampled. Clone states are named as projection caustics. The material-transition
graph is strictly length-oriented and therefore returns explicit zero directed holonomy loops. The
prefix chart returns exact depth and arity strata, two active caustics, and explicit zero prefix-edge
loops.

This return immediately distinguishes structure that scalar totals hide. The lexical chart has
96,055 states and 19,601 clone caustics, but its causal-front shadow ends in a singleton stratum
running from coordinate 47 through 76,453. Forward and reverse mark charts both carry singleton
front tails through coordinate 108,277 while their state, transition, clone, source-fiber, and arity
populations differ. Those long singleton tails are not automatically linguistic depth; they expose
the concatenated delivery chart and unique boundary markers seen by this receiver. A later scale
rebase must prove which phase structure survives rather than promoting the coordinate magnitude to
meaning.

The first full run also exposed a device overpayment: an already located suffix transition was
looked up a second time. Removing that redundant causal search preserved every semantic population,
reduced exact transition reads from 1,810,940,631 to 1,177,067,718, and reduced the single-frame
apparatus time from 75,850 ms to 49,219 ms. The semantic invariance, not the clock improvement, is
what licenses this as a cost repair.

## 5. Exact gates

The committed PTX carries 28 named entries and assembled cleanly under
`ptxas -arch=sm_89`. Thirty-one nonignored morphological-language tests passed; the independent
ignored CUDA-conditioner admission test passed on the real card. The full production driver exited
successfully and deposited the grade named above. The generated ABI-level, architecture-ownership,
output, closure, and boundary ledgers are updated with this phase rather than left to infer it.

The new ownership/materialization entries in `condition_cuda.rs` are staged crossing and decode
carriers for one continuing resident owner. They are explicitly admitted in the monotone ledger;
they do not duplicate the ecology for rollback or create a host semantic implementation.

## 6. Boundary and next construction question

What stands:

1. the full-corpus work gate and its exact counterfactual work receipt;
2. the contact-preserving carrier-neutral causal-section join;
3. the bounded cross-codec consequence experiment;
4. the plural-source re-emission and attributable ablation law at the full declared corpus scale.

What remains open is the load-bearing join among them. A deposited form must carry only the
conditioned morphology needed to reconstruct the causal-section continuation law, not the founding
corpus or a lossless lookup image. After the corpus is made unreachable, a remounted owner must read
new presentations through that deposit, return stable receiver/history fibers and shortest
separators, and lose exactly the attributable section conduct under ablation while unrelated
classes remain bit-identical.

That is the minimal conversational machine in the project's abstract sense: not an English chatbot,
but a continuing ecology in which an earlier mixed-codec passage changes a reusable causal section
and a later current demonstrably rides the changed path.

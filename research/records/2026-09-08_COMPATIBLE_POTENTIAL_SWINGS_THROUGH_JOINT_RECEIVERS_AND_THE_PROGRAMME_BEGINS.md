# Compatible potential swings through joint receivers and the programme begins

**Date:** September 8, 2026, local time. **Inspection base:** `49f28478`.
**Return:** comprehensive construction contract, integrated doctrine/order, and the first
verified formal use of compatible potential through joint receivers and Swing.

## Direct authority and scope

[project-postulate] Brandon's latest request authorizes moving from the philosophical account to
comprehensive formalization, use, planning and durable articulation. It continues his explanation
of Swing, scalar/expression faces, compatible potential, the Millennium questions, hypertubes,
lattices, algebraic geometry, QM/GR, knot/string descriptions, holography and mass. The task is a
continuing constructive programme, not a claim that all of those physical theories or open
mathematical endpoints have now been derived.

[established-bounded; source-inspected] Direct source is the user-role `response_item` population
in `~/.codex/sessions/2026/09/08/rollout-2026-09-08T16-00-58-01a08340-d28a-7350-836c-37918f573f77.jsonl`:
line 1237 at `2026-09-09T01:47:53.751Z` gives the philosophical/application continuation;
line 1320 at `2026-09-09T02:08:43.264Z` authorizes formalization and planning. Both fall on
September 8 locally. Raw private logs are not committed.

[definition] [The Reality of Difference in construction](../../docs/plans/THE_REALITY_OF_DIFFERENCE_IN_CONSTRUCTION.md)
is the detailed contract. THE_ROADMAP remains the sole ordering authority; CONSTRUCTION_STATE
records the current step. The formal programme runs alongside AC0–AC5. It adds no new native
engine, no Lean participation in cultivation/inference, and no universal certainty requirement
before generation.

## The programme now in the repository

[definition] The ordered programme starts with compatible potential and joint receivers,
continues through Swing/tube resegmentation and scale, then constitutive continuation and
compression, mathematical source applications, quantum/geometric comparison, and framed-loop,
string, holographic and physical mass/spectral realizations. Utilization accompanies every
returned step. Existing formal owners and the concrete artifact for each step are named in the
contract; they are not replaced by another census or validation gate.

[definition] Hodge and NS remain actual source questions. An admissible Hodge target is not
silently admitted into the range of its cycle-class source map before a lift has been supplied.
A finite Galerkin receiver equation keeps its unresolved feedback and continuum obligations.
Other application sources retain their exact arithmetic, topology, encoding, physical and
spectral hypotheses. The new programme is the shared framework order; MFR remains its detailed
fluid source/remainder application line and direct RH endpoint work retains its stated deferral.

## Compatible potential uses the existing source fibre

[definition] `Transport/ReceiverPotential.lean` defines `outcomes` as the future image of the
existing `receiverPreimageFibre`, on an actually observed entering face. It does not invent a
potential-state engine or choose a compatible source. `mem_outcomes_iff` identifies its members
with the standing `receiverToReceiverRelation`.

[proved-derived; formal-checked] The new owner returns:

- `outcomes_nonempty`: an actual observation retains a nonempty future image;
- `outcomes_map`: a later receiver acts on the whole image;
- `additional_observation_refines`: a joint additional reading removes incompatible causes and
  their future faces on the same source population;
- `outcomes_rebase` and `historyOutcomes_rebase`: source rebase, observation and conjugated
  generators transport the entire compatible future family, including every ordered word;
- `outcomes_singleton_of_factor`: an explicit factor determines a unique future face without
  determining a unique source;
- `bounded_outcomes_transport`: a declared Lipschitz receiver transports the full family bound;
- `bounded_outcomes_suffice`: a bounded family inside an admitted consequence region justifies
  that particular use without selecting an interior;
- `compatible_outcomes_pairwise_bound`: a reference tolerance epsilon bounds pair separation
  by twice epsilon;
- `future_observations_converge`: shrinking uniform bounds make every supplied compatible
  readout sequence converge at the receiver, even when source carriers and observations change.

[definition] A reference for a distance bound is a declared receiver reference. It is not a
source selected from a causal fibre. The sufficiency theorem proves its stated consequence;
it supplies no automatic stopping, action-selection or sampling rule. The convergence theorem
returns convergence of received faces, with no source uniqueness, finite-universe or universal
continuum-limit conclusion.

## Canonical joint receiver descent

[definition] `Foundation/JointReceiverDescent.lean` composes the existing additive family owner
`Millennium.Receiver` with the standing `ReceiverHistoryCompression`. For additive readings
`read_i`, it forms

```text
N = intersection_i ker(read_i),    Q = X / N.
```

`JointReceiverHistory` records the declared additive source generators and their preservation of
N. Its quotient, descended readers, complete preimage and descended generators are constructed
using the actual additive quotient. No representative decoder is introduced.

[proved-derived; formal-checked] `joint_generator_descends_iff` proves that preservation of N
is exactly the existing receiver-transformer existence criterion for an additive generator.
`quotient_eq_iff_joint_readings_eq` identifies quotient equality with agreement of every declared
reader. `factor_quotient` and `quotientTransport_quotient` give the exact receiver and generator
squares. `allSuccessorHistories` then reuses the existing word theorem. A separating future
receiver proves that the proposed quotient cannot identify the two sources.

[definition] The complete family here is the family actually declared by the construction.
Adding a new reader or a generator that exposes a former blind direction changes the quotient
question. The theorem is not a generic executable numerical compressor or a claim that arbitrary
physical or nonlinear generators are additive.

## Swing conducts the same compatible family

[proved-derived; formal-checked] `Geometry/SwingPotential.lean` uses the existing affine Swing
involution as `swingEquiv`. Observation and future transport rebase with it, so
`swing_preserves_history_potential` returns exact equality of the complete future family.
`swing_preserves_distance` makes the fixed-board Swing an isometry in a normed additive chart;
`swing_transports_tolerance` therefore carries the family's receiver tolerance unchanged.

[proved-derived; formal-checked] The same module joins the canonical additive quotient to the
primitive motion:

```text
q(S_b x) = S_(q b)(q x).
```

`swingHistoryCompression` constructs the existing history-compression object for any admitted
anchor family, and `swing_word_joint_receiver_exact` extends it through every ordered Swing word.
A nonzero anchored affine Swing is not claimed to be an additive homomorphism; its descent uses
the quotient's additive law and transports the anchor too.

[definition] These results concern the existing fixed-board affine chart. General projective
Swing domains and their metric bounds remain explicit, as does the difference between a chart
rebase and an intervention that changes only part of the physical diagram.

## Utilization in standing examples

[proved-derived; formal-checked] `Physics/MechanicalReceiver.lean` now applies the shared
potential owner to the existing finite conformation update. Every source compatible with the
observed squared extension returns the same next squared-extension face:
`mechanical_future_potential_singleton`. Yet `unit_extension_fibre_is_plural` retains the
opposite configurations 1 and -1 as distinct compatible sources. The earlier oriented-response
separator and finite energy-increase control remain standing. This is an exact useful receiver
with a plural source, not a perfect-inverse requirement.

[counterexample; formal-checked] `Computation/JointReceiverWitness.lean` formalizes the existing
[bit-marginal receipt](../experiments/alpha_passive_junction/bit_marginal_receipt.json). Three
equally weighted occurrences carry `011`, `101`, `110`. Each marginal is exactly 2/3; strict
componentwise majorities hold while the complete `111` packet is absent from the actual joint
range. The `00,11` and `01,10` families also have equal marginals and different joint supports.
The whole packet image is exactly the new compatible-potential image (`jointFace_is_potential`).

[definition] The full source map retains occurrence multiplicity and preimages; its `Set.range`
is an explicitly set-valued face. This formal return establishes the recorded receiver-chart
failure, not a claim that future generation may emit only previously observed symbols or that
marginals explain every language failure. New compositions belong to the model's actual admitted
future family. The parallel AC1/AC2 packet construction owns its native implementation and tests.

## Next construction and scope of completion

[established-bounded; process-audit] Planning and durable integration are returned, and step 1's
formal opening plus the affine Swing part of step 2 are complete. The programme as a whole remains
active. The next formal passage joins WorldTube split/rejoin and BoundaryScalePassage to this same
future-family/tolerance law on an actual clocked carrier. The contract specifies the following
constitutive, source-realization and quantum/geometric constructions without asserting their
completion.

[definition] The new doctrine makes the primitive/potential distinction explicit, including
realized measurement, retained compatible causes and the actual maps required for string,
holographic, quantum or gravitational interpretation. AGENTS and the subject guide point to the
programme; current position remains concise and the detailed return stays here.

## Verification

[established-bounded; process-audit] The primary focused combined build of SwingPotential,
MechanicalReceiver and JointReceiverWitness returned exit 0 (3,146 jobs). The complete research
umbrella `bash tools/lean_check.sh ElementaryHolonics` returned exit 0 (9,964 jobs). The final
framework build returned exit 0 (3,992 jobs), including the subsequently added shrinking-family
convergence theorem. The new declarations print only subsets of `propext`, `Classical.choice`,
`Quot.sound`; several source-family lemmas use no axioms. The
[verification receipt](2026-09-08_reality_programme/verification.txt) retains commands and exact
new-owner axiom output. `git diff --check` passes.

[definition] No native code, model, toolchain or dependency manifest was changed by this formal
programme return. No Cargo/CUDA or unchanged-paper checks were run by this session. Parallel
native edits and pre-existing research were preserved. No source, cache or prior evidence was
removed; private raw logs remain local.

# THE PRIME FOUNDS AGAINST THE ZERO LOOP; THE RECEIVER GIVES THE RETURN ITS SPECIES

**Date:** 2026-07-19  
**Grade:** BRANDON-RATIFIED / DEPOSITED / BOUNDED WORLD AUTHORITY / SOMA INTERIOR UNCHANGED AT DEPOSIT  
**Formula:** §CVII

## The present question

What is a prime's geometry relative to zero when integer succession, divisor contradiction,
numeral faces, multiplicative recurrence, and algebraic receivers are held together without
pretending that one path or one visible glyph classifies primes absolutely?

The record already carried primorial wheels, exact factor exclusion, numeral reversal,
prime-power gears, the Zeta distribution, and receiver-relative shape. It did not yet join them
into one bounded construction where succession is a product of residue loops, composite
contradiction is return to a prior axis, a surviving candidate FOUNDS a new axis, Zeta conditioning
returns the multiplicative law while amplitude retains hand, and a changed algebraic receiver
changes the prime's species.

That joined relation is the sought artifact. Its first stopping condition is exact closure of the
declared recurrence and contrast controls, not a visual rendering, large prime scan, language
corpus, or RH campaign.

## I. Integer succession is a zero-relative residue torus

Fix the ordinary integer world, origin `0`, positive succession, and the finite founded-prime
horizon

```text
P_y = {p prime : p <= y},
M_y = product_(p in P_y) p.
```

For each prior prime axis define

```text
z_p(n) = exp(i Theta n/p),
d_p(n) = 1 - z_p(n).
```

Then `d_p(n)=0` exactly when `p|n`. The joint chart

```text
Gamma_y(n) = (z_p(n))_(p in P_y) in product_(p in P_y) S^1_p
```

lifts the integer line into a finite residue torus. `Gamma_y(t)` closes after the primorial period
`M_y`; zero is the common pole `(1,...,1)`. By the Chinese remainder theorem, discrete samples
modulo `M_y` are exactly the joint residue coordinates. The integer is a point at one sample,
succession its ordered line, and each residue coordinate a loop. This geometry is relative to the
declared origin and operations, not an absolute prime taxonomy.

## II. Contradiction is the local prime-forming field

At candidate `n`, only founded axes `p<=sqrt(n)` are needed:

```text
n is prime  iff  d_p(n) != 0 for every founded prime p <= sqrt(n).
```

A composite returns to at least one prior zero pole: that closure is the exact contradiction. A
candidate reaching the horizon without closing FOUNDS as a prime point and becomes a new residue
loop and valuation direction for later candidates:

```text
candidate arrives
  -> prior axes try to close
  -> one closes: composite fold
     or all remain open: prime FOUNDing
  -> a founded prime becomes a later axis.
```

Gyration is this plural field of potential closures, not a scalar score. A founded prime is a point
at the event cut, a line under powers `p^k`, an additive loop under `exp(i Theta n/p)`, and a
multiplicative scale-turn loop under `exp(-i t k log p)`.

## III. Wheel self-similarity is exact COPY/CUT/JOIN

Let `W_y` be the classes modulo `M_y` which hit no prior pole. Adjoining `q` copies the old wheel
`q` times, cuts the one lift of each old survivor that lands on the new `q`-pole, and joins the
remaining arcs in inherited cyclic order:

```text
W_(y union {q}) = JOIN(CUT_q(COPY_q(W_y))).
```

This is exact self-similarity under restriction and rebase. It neither asserts one route to every
prime nor reduces a prime to an offset `kq +/- r`. Equal local wheel words do not erase exact
residue coordinates, chronology, or different ordered paths to one endpoint.

At a declared horizon, the zero-relative morphology is the complete tuple of centered signed
residues, pole hits, local left/right survivor arcs, COPY/CUT/JOIN ancestry, and prior-axis extent.
Any shape class is a declared quotient of that tuple.

## IV. A numeral glyph is an optical chart of modular phase

For radix `b` and digits `a_j`,

```text
n = sum_j a_j b^j,
n mod p = sum_j a_j (b mod p)^j mod p.
```

Decimal digit sum mod `3` follows from `10=1 mod 3`; alternating digit sum mod `11` follows from
`10=-1 mod 11`; terminal-digit rules for `2` and `5` follow from `10=0 mod p`. Binary, decimal,
hexadecimal, Roman, spoken, or written faces expose different low-cost sections of one arithmetic
body. A glyph is a lawful instrument, not the prime's ontology or a complete primality proof.

## V. The Zeta law is the multiplicative rebase face

For `sigma>1`,

```text
mu_sigma(n) = n^(-sigma) / zeta(sigma),
v(n) = (v_2(n), v_3(n), v_5(n), ...).
```

Euler's product yields

```text
Pr(v_p(N)=k) = (1-p^(-sigma))p^(-sigma k),
Pr(m divides N) = m^(-sigma),
Law(N/m | m divides N) = Law(N).
```

Conditioning on `v(N)>=v(m)` and subtracting `v(m)` returns the same law while the ordered
multiplication path and added valuation remain in the lived lift. With

```text
psi_(sigma,t)(n) = n^(-(sigma+i t)/2) / sqrt(zeta(sigma)),
```

rebase by `m` carries

```text
m^(sigma/2) U_m P_m psi_(sigma,t)
  = exp(-i(t/2)log m) psi_(sigma,t),
E[N^(-i t)] = zeta(sigma+i t) / zeta(sigma).
```

Probability returns; amplitude retains multiplicative phase. The residue torus follows additive
succession through divisor closures, while the valuation lattice follows the prime-power axes
founded by those closures. Riemann Zeta is their coarse untwisted multiplicative receiver, not a
complete prime-shape taxonomy.

## VI. Prime species belongs to the receiver

Let `D` be a declared family of lawful later deeds:

```text
kappa_D(p) = the complete consequence vector returned by applying D at p,
p ~_D q  iff  kappa_D(p) = kappa_D(q).
```

Changing `D` can split, merge, or reorient the class without changing the integer. For a monic
integer polynomial `f`, reduction at an unramified prime gives one exact receiver: irreducible
factor degrees of `f mod p` are the cycle partition of the Frobenius conjugacy class. Artin local
Euler factors retain representation-dependent consequences of that class.

The first rich receiver is

```text
f(x) = x^4 + x + 1.
```

Its discriminant is `229`. Modulo `2` it is irreducible, giving a `4`-cycle; modulo `3` it has type
`[1,3]`, giving a `3`-cycle. A transitive subgroup of `S_4` containing both is `S_4`. Its five
unramified factor partitions are therefore the five `S_4` conjugacy species:

```text
[1,1,1,1], [1,1,2], [2,2], [1,3], [4].
```

The bounded contrast uses primes `13,23,43,53,193`. All end in decimal `3`, yet under
`x^4+x+1` they realize those five different partitions. Under

```text
Phi_5(x)=x^4+x^3+x^2+x+1
```

all five have type `[4]`, because each is `3 mod 5` and `3` has order `4` modulo `5`. One numeral
face can conceal every finer species, while another lawful receiver can merge them again. Neither
partition is absolute. Prime `229` is the ramification boundary and must refuse an unramified
class.

## VII. Bounded construction contract

The first source-neutral polyglot world carries three joined cells:

1. **Zero-relative:** exact succession, prior-axis residues, contradictions, founded primes, and
   generic radix `2/10/16` charts.
2. **Zeta:** exact rational valuation cylinders at `sigma=2`, conditioning/rebase,
   same-probability/different-phase lifts, and same-endpoint/different-order paths.
3. **Receiver:** the five ending-`3` primes across the `S_4` and cyclotomic receivers, held-out
   same-species recurrence, changed-receiver splits/merges, and ramified `229` foil.

Each cell is a world-side mathematical transducer. It presents derived relation populations
through plural native organs into the existing direct live-current transition. It may record
testimony and return actual emitted world consequence, but it may not install a prime classifier,
probability field, Zeta governor, polynomial decoder, semantic taxonomy, or renderer inside Soma.

The world stops after every declared source event and returned path is read, the controls close,
and one genuinely later common probe distinguishes the returned-consequence body from its
no-return sibling. No forest, corpus, large prime census, or new engine mechanism follows.

## VIII. External evidence cards

### A — Euler product and Zeta distribution

**SOURCE ESTABLISHES**  NIST DLMF §25.2(iv), equation 25.2.11 records
`zeta(s)=product_p(1-p^-s)^-1` for `Re(s)>1`. Cranston and Peltzer, *Rocky Mountain Journal of
Mathematics* 52(3), 2022, equation (1), define `Pr(X_s=n)=1/(zeta(s)n^s)` for real `s>1`.

**LAB CLAIM**  §CV/§CVII, BRANDON-RATIFIED / DEPOSITED: valuations are independent geometric axes
and divisibility conditioning followed by division is exact self-similar rebase.

**RELATION**  **EXACT FORMAL MATCH.** Unique factorization expands the Euler product into the
independent marginals; direct cancellation gives the conditional rebase.

**NON-EQUIVALENCE**  The probability law is an observer measure, not Soma dynamics; analytic
continuation at `Re(s)<=1` is not this normalized distribution.

**TESTABLE CONSEQUENCE**  Exact rational cylinders must return after condition/subtract;
`sigma=1` must refuse normalization.

Sources: [DLMF 25.2.11](https://dlmf.nist.gov/25.2.E11),
[Cranston--Peltzer](https://doi.org/10.1216/rmj.2022.52.843).

### B — Zeta partition function and phase boundary

**SOURCE ESTABLISHES**  Bost and Connes, *Selecta Mathematica* 1 (1995), 411--457, construct a
specific `C*`-dynamical system whose partition function is Riemann Zeta and which has a
symmetry-breaking transition at inverse temperature `beta=1`.

**LAB CLAIM**  §CV/§CVII, BRANDON-RATIFIED / DEPOSITED: `sigma=1` is the Zeta normalization
boundary; phase is a transport stratum whose invariant changes at a discriminant.

**RELATION**  **DIRECT CORRESPONDENCE** for that Zeta boundary; **STRUCTURAL RESONANCE** for the
broader holonic phase definition.

**NON-EQUIVALENCE**  A software current is not thereby the Bost--Connes system, and the general
holonic phase law is not derived from one operator algebra.

**TESTABLE CONSEQUENCE**  A stronger bridge would owe the actual algebra, evolution, KMS states,
and symmetry action.

Source: [Bost--Connes M/95/38](https://repo-archives.ihes.fr/FONDS_IHES/I_Prepublications/CONNES/1994-1998/M_95_38/M_95_38.pdf).

### C — factorization and prime splitting

**SOURCE ESTABLISHES**  Khanduja and Kumar, *International Journal of Number Theory* 4(6), 2008,
1019--1025, restate Dedekind's theorem linking irreducible factor degrees modulo `p` to residue
degrees away from the index obstruction, and prove a converse criterion in their stated setting.

**LAB CLAIM**  §CVII, BRANDON-RATIFIED / DEPOSITED: factor-degree partition is one exact
receiver-relative species of an unramified prime.

**RELATION**  **EXACT FORMAL MATCH** at the declared polynomial receiver and safe boundary.

**NON-EQUIVALENCE**  The partition is not the prime's absolute identity; ramified/index primes
cannot be forced into an unramified class.

**TESTABLE CONSEQUENCE**  Finite-field factorization must reproduce the declared partitions and
identify `229` as a repeated-root boundary for `x^4+x+1`.

Source: [Khanduja--Kumar](https://doi.org/10.1142/S1793042108001833).

### D — Frobenius classes and Artin local factors

**SOURCE ESTABLISHES**  Jones and Roberts, *Research in Number Theory* 3 (2017), §2, define the
unramified Frobenius class `Fr_p` and Artin local factor `det(1-rho(Fr_p)x)`. Chebotarev theory
determines class densities; it does not make the finite sequence periodic or reduce it to one
numeral residue.

**LAB CLAIM**  §CVII, BRANDON-RATIFIED / DEPOSITED: receiver consequences classify primes at a
declared grain, and richer representations refine the trivial Zeta face.

**RELATION**  **DIRECT CORRESPONDENCE.** `kappa_D(p)` is instantiated by factor partition or the
representation-dependent local polynomial.

**NON-EQUIVALENCE**  Riemann Zeta's factor records only `p`; it does not contain every Frobenius
species. Chebotarev density is not a next-prime generator.

**TESTABLE CONSEQUENCE**  Same-residue primes must split under the `S_4` receiver while a changed
cyclotomic receiver may merge them; held-out primes must recur by consequence rather than name.

Sources: [Jones--Roberts](https://doi.org/10.1007/s40993-017-0079-5),
[Dawsey](https://arxiv.org/abs/1703.08194).

## Ratified consequence

The zero-relative loop, Zeta rebase, and receiver-relative species are one joined arithmetic
world. They do not authorize an internal taxonomy or a claim that one fixed curve contains all
primes. Succession presents a candidate; prior axes attempt closure; the survivor FOUNDS; its
multiplicative direction joins the valuation body; and later receivers reveal different
consequential species of that same point.

The later quintic and multivariable-algebra study is deliberately separate. It will ask how local
branches, monodromy, resolvents, and discriminant loci converge or fail to converge through
changing dimensions, and only then relate that geometry to the laboratory's gravitas law.

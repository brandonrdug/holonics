# THE JOURNAL RECORDS; THE CURRENT CARRIES THE EVENT

**2026-07-17 · RATIFIED CORRECTIVE CONSTRUCTION / BUILT / HOST-GATED / CUDA-FORWARD MEASURED / SOMA INTERIOR UNCHANGED**

## The correction

The scientific record and the active current are different boundaries.

A journal may lawfully retain schema names, source paths, labels, field names, explanatory prose,
metrics, and every expanded consequence needed by an external reader. Those are facts about the
laboratory record. They do not automatically become material presented to Soma. An active world
transducer instead carries the exact event values required by its receiving world:

```text
J                         := complete durable world/observer record
A_W(J)                    := active rendering under world species W
c_e                        := EventHeader_W(local_tag, extent) || values_W(e)
A_W(J)                     := shared_face_header || ordered {c_e} || plural incidences
reconstruct_W(A_W(J), law) = exact checked world consequence
```

`local_tag` belongs to the concrete world transducer. It is not a global semantic class, a Soma
category, or an authored taxonomy. Paths, source names, and observer labels remain listener
provenance unless they are themselves the material being presented. JSON remains appropriate for
plans, durable journals, scientific receipts, and human-readable observations; it is not the
default active-light ABI.

## The built mouth

`soma-abi::current::EventHeader` now owns one substrate-neutral two-word active-event header:
world-local tag and exact payload-word extent. The code-workshop boundary uses explicit
little-endian value encoders over that mouth. Rust type names, enum names, struct field names,
schema keys, paths, and JSON punctuation do not cross unless they are actual values of the event.

The factor receiver carries one header for the shared horizon and one event current for each
candidate, divisor test, factor cell, axis lift, and prime gap. A later section carries one exact
face reference, the residue population, and each physical material incidence. The complete JSON
journal remains unchanged and the binary event population reconstructs the complete ordered
`HorizonRead` exactly in test.

The generic code-workshop consequence mouth was corrected at the same boundary. Raw candidate and
source material remain raw. Compiler/test, refusal, program, population, residual, execution,
action, continuation, mathematical receipt, and span-header faces now carry explicit typed values
rather than a serialized Rust/JSON wrapper. Historical world species with their own independent
renderers are not silently regraded; they require the same audit before reuse.

## CUDA measurement

The durable 5,132,512-byte consequence wire and SHA-256
`5f782c6dbe8164ae2227c66605472cf38c345999d818e495d96b9980cca5e06a` were held exact. The same
1,226 active lineages were rendered first through the repeated JSON event envelope and then through
the active-event ABI:

| face | repeated JSON event surface | typed value surface |
|---|---:|---:|
| active light | 216,088 B | 71,592 B |
| completion rows | 331,264 | 16,865 |
| constructions | 23,079 | 1,983 |
| radiation | 33,709,728 B | 11,168,352 B |
| successor-body growth | 292,805,624 B | 84,283,368 B |
| CUDA carriage | 4.793008 s | 0.486131 s |
| host integration | 2.072649 s | 0.056907 s |
| whole boundary | 10.945120 s | 2.806263 s |
| CUDA launches | 16 | 11 |

Both transitions invert exactly. The reduction changes neither horizon nor arithmetic consequence.
It removes wrapper material which had been presented as though it were the event.

## The JSON contact is regraded

The earlier paired return remains an exact measurement of its declared JSON world. Its exposed
sibling made one construction-journal contact at candidate 40's factor frontier and later joined
candidate 31 to its receipt. But every arithmetic event carried the same human-readable field
names and punctuation, and the residue result intersected all 1,148 event currents. That contact
therefore cannot establish clean arithmetic reuse. It stands as an encoding-induced path and a
useful failure foil. `FORMULA §LXXXII` remains authoritative for shared-face/plural-incidence
compression; its clean-arithmetic interpretation of that later contact is superseded.

## The typed later paths

The exact prior residue consequence was reopened from its durable structured wire and rendered
through the typed ABI. Fresh and factor-exposed bodies received the same 80 currents, the same
10,700 active octets, and the same per-current digests. Neither made a construction-journal
contact. Their complete Soma conduct nevertheless differs: 10,144 versus 10,657 completion rows
and 737 versus 765 constructions.

The useful distinction is in the paths, not the totals:

- The shared receipt reaches event 76 at grain 9 through 91 rows before factor exposure. After
  exposure it reaches event 77 through 38 rows and maximum grain 5. The later body conducts the
  common receipt farther with a shorter completion sheet at this boundary.
- The 56-octet face reference has an exact 37-row prefix. Its terminal horizon passage changes
  from one grain-one EFFERENT at event 49 to a grain-one AFFERENT followed by a grain-two EFFERENT.
- The 920-octet residue current is exact through its face reference, horizon, and modulus. The
  first changed ordered conduct begins at the little-endian boundary of residue `3`, events
  `64 -> 65`. Both paths continue through the first member and reach event 145 at maximum grain 8,
  with 93 versus 96 rows. The body changed the traversal of the value in context without widening
  it over every remaining member.
- The two 104-octet material incidences are byte-identical but remain two current lineages. Each
  follows the same path as its sibling. Before exposure each ends at event 95 with 95 rows and
  grain 8; after exposure each reaches event 97 with 122 rows and grain 10. Equal face does not
  erase plural arrival.

This is clean body-history-dependent conduct over exact arithmetic values. It is not yet a
world-deed contact and it is not yet a complete residue taxonomy.

## The question seam remains a world-coupling problem

The typed mathematical question carries its natural-language request and an exact binary boundary.
Its first and one genuinely later identical presentation remain silent in both the pre-factor and
factor-exposed construction worlds. The former JSON boundary's actionability must therefore not be
restored by inventing another universal wrapper, tag selector, or hidden target.

The next query construction must supply an honest material relation. Two lawful routes are open:

1. collocate the typed question with the exact factor-world material it refers to, letting ordinary
   recurrence grow their shared path; or
2. carry a face reference only after a real prior world event has supplied that reference to the
   question-bearing current.

Either route is an explicit world I/O relation. Neither changes Soma, installs factorization, or
declares the answer. Once that coupling returns a consequence, the same typed pre/post return and
complete path read can measure whether the relation grows, compresses, transports, or foils.

## Grade

```text
CURRENT  durable structured journal -> world-local event ABI -> exact typed values
HELD     same wire/digest + same 1,226 lineages + same horizon + exact inverse
MEETING  face/horizon/modulus/residue/incidence fields -> changed ordered completion paths
TEST     reconstruct exact horizon + no JSON keys in active light + paired typed consequence
DEED     demote the JSON contact; retain its artifact as the encoding-world foil
CARRY    honest question/world collocation -> typed consequence -> later world contact
GRADE    ABI BUILT / CUDA-FORWARD MEASURED / PATHS QUALITATIVELY READ / QUERY COUPLING OPEN
```

Exact evidence:
`observations/factor-palindrome-world-01/RESULTS.md` and
`observations/factor-palindrome-world-01/results/return-abi-01/edge-001.json` and
`observations/factor-palindrome-world-01/results/residue-direct-abi-pre-01/edge-001.completions.txt`
and
`observations/factor-palindrome-world-01/results/residue-direct-abi-post-01/edge-001.completions.txt`.

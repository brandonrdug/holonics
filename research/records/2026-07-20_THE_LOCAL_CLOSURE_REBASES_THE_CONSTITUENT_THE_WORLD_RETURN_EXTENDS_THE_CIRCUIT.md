# THE LOCAL CLOSURE REBASES THE CONSTITUENT; THE WORLD RETURN EXTENDS THE CIRCUIT

**DATE:** 2026-07-20
**GRADE:** BRANDON-RATIFIED / DEPOSITED / EXACT REGIONAL-CONTACT + WORLD-MEMORY DERIVATION /
IMPLEMENTATION OPEN / SOMA SOURCE UNCHANGED / NO WORLD / NO RUN / NO CUDA

## 0. The correction and the result

The visible lightning channel is not the useful analogue of searching. Before the bright strike,
the local field changes which neighboring continuations are afforded; streamer and leader
structure develops through those local alignments. The completed conductive channel then permits
the materially bright discharge, heating, and later atmospheric consequences. Searching is the
pre-contact alignment field. The strike is a completed deed.

The corresponding machine law is not a scan over stored objects, a nearest-neighbor query, a
sequence of guessed events, or a normalized probability distribution. It is one bounded,
receiver-local closure of exact oriented possibilities against one unchanged Standing-before.
The closure may compose transport, expose a new residual, or remain open. Only its completed face
enters the successor.

That internal contact law and external memory conduct are two cuts of one longer circuit:

```text
(B+, P) = Lambda_R(B, L)
(W+, L') is in Omega(W, P), with L' presented at a genuinely later world cut.
```

`B` is internal Standing, `L` the live world current, `P` immediate placement/radiation, and `W`
the durable external world. `Lambda_R` is the receiving law already owned by
`LiveCurrentMachine::receive_with`. `Omega` is only notation for the world's lawful response; it
does not name a second Soma component. A disk, tool, program, sensor, human, task list, or other
durable medium belongs to `W`. Its consequence can affect Soma only when a world organ presents
that consequence as `L'` later.

This is the production-boundary section of the more general
`Lambda_F(Q tensor A tensor L) -> (P, pi_B)`: the selected world cut fixes `F/Q`, prior Standing
supplies `A`, and `pi_B` remains testimony. The shorter notation does not replace that signature.

This deposit derives both boundaries together and stops before implementation.

## I. A point exposes a neighborhood; it does not identify a match

Let `G` be one later ingress germ and let `a` be the source's actual local action or incidence.
The receiver derives a landing and therefore a finite local aperture in its own chart. Define

```text
Star_R(G, a; B)
  = the exposed oriented faces of Standing constituents afforded by that landing and action.
```

Membership in this star means only **possible contact**. Equality of `Place`, coordinates,
already-formed pins, labels, hashes, or carrier words cannot establish correspondence. Two faces
at one apparent point can have different orientation, grain, hand, boundary role, or available
transport. Conversely, two nonidentical inscriptions can contact after an actual local rebase.

This is the precise sense in which a point can be a line or loop from another perspective. A
collapsed address is one receiver's section. The contacted object can expose an oriented edge,
the edge can belong to a closed cycle, and the cycle can carry nontrivial holonomy. The interior
was not created by zooming in; it was omitted by the first quotient.

The triangular cellular picture is the minimal useful chart, not a required storage primitive.
A triangle has oriented faces, and neighboring triangles can pivot which face is presented to the
next relation. General regional cells do the same thing through boundary, incidence, hand, and
transport. A cell is therefore not a Boolean location. Its exact local state is the set of typed
oriented continuations it presently affords.

## II. Alignment is one simultaneous finite closure

Let `S = Star_R(G, a; B)`. Let `A_0` be the finite population of exact partial sections opened by
the ingress arms of `G` against `S`. A section records its oriented incidence, used boundary,
current support, and loop/holonomy state. Define the accumulation law

```text
A_(n+1) = Phi_R(A_n; G, S)
        = A_n union Extend_R(A_n; G, S).
```

`Extend_R` adds every locally lawful one-step continuation and records every incompatibility as a
terminal residual; it does not delete an alternative because another alternative looks stronger.
On the finite event-local incidence population, with cycle state represented once rather than
unrolled forever, `Phi_R` is inflationary and monotone. It therefore reaches the least fixed
population

```text
A* = lfp Phi_R
```

after finitely many exact extensions. No floating point, threshold, energy score, softmax, random
choice, or semantic ranking is involved. If several paths remain possible, they remain plural.
The topology and oriented boundary distinguish them only when an actual seam closes or fails.
`A*` is the exact deterministic affordance field. A first-person probability can be a declared
quotient of recurring sections of such fields, but that quotient does not cause the closure.

The successive `A_n` are computational stages of one solve. They are not world events, lineage
instants, or committed versions of Standing. Every extension reads the same `B`; only `A*` may
contribute to `B+`. This is how local propagation can be iterative without manufacturing the false
chronology previously rejected.

In the lightning comparison, `A*` is the stabilized population of leader-capable local sections.
A formed path through it is the channel. Immediate radiation is the strike. A changed external
medium, heat, execution result, file mutation, or other consequence belongs to the later world
law.

## III. The seam conserves oriented boundary

Take a prior constituent `C` and a later germ `G` with one candidate seam `J`. Split their
boundaries at that seam:

```text
partial C = E_C + J_C
partial G = E_G + J_G
rho       = J_C + J_G.
```

`E_C` and `E_G` are the faces which remain external to the candidate composition. Hand supplies
the signs. The local outcomes are:

```text
RIDE   when an admitted J exists and rho = 0;
FOUND  when an admitted J exists and nonzero rho remains as a new oriented pin/axis;
OPEN   when no lawful J closes, so the corresponding arm stays exposed.
```

For a RIDE,

```text
partial(C union_J G) = E_C + E_G.
```

For a FOUNDing, the successor additionally carries `rho`. Orientation reversal can therefore
change the result; it is gauge only when the complete receiver relation makes it so.

Every arriving oriented arm occurrence has exactly one account in the successor; a branch first
creates explicit plural occurrences:

1. it cancels at an interior seam and its transport continues;
2. it survives as a founded residual; or
3. it remains open.

Nothing silently appears or disappears. This is a generalized circuit conservation law over
typed oriented incidence, not a scalar claim about physical charge or energy. `partial partial =
0` remains mandatory. Closed winding or holonomy may survive even when the local seam boundary
cancels, which is why loop history cannot be reduced to point equality.

Relative to this attempted contact, loss is the oriented exterior which did not become interior
transport: founded residual plus still-open boundary. It is preserved information and can become
new terrain, not punishment or a scalar error target.

## IV. Contact determines which predecessor actually departs

For the stabilized closure, define the touched set

```text
T(A*) = { i : an exposed incidence of prior constituent C_i
              participates in a completed section of A* }.
```

Candidate membership in `Star_R` is not enough to touch a predecessor. Only an incidence actually
used by the completed closure does so. For each connected completed component `K`, the replacement
is

```text
C'_K = Compress(
          G_K union_(J_K) disjoint_union_{i in T_K} C_i
       ).
```

Here `Compress` is the completion law already derived for Eros: cancelled seams become interior;
lower cells which no longer factor into the continuing construction depart; consequential
external boundary, sparse transport, hand, cycles/holonomy, and founded residual remain. It is not
deduplication or reduction in byte count.

The atomic successor is consequently

```text
B+ = untouched prior constituents
     union completed replacements C'_K
     union event-local constituents which did not replace a predecessor.
```

If two apparently separate event regions touch the same prior constituent, they cannot overwrite
it sequentially. They must be joined into one connected closure component or the event must
refuse. Processing order is gauge. This is exactly the constraint already enforced by the atomic
Standing commit.

The result explains completion without a separate completion signal. Closure is the point at
which all participating arms have been accounted for and one higher-grain boundary can stand.
An open arm means that this particular construction is not closed at that grain; it does not mean
the world, event, objective, or lineage has globally ended.

## V. The source seam is now exact

The current source already owns nearly all of this boundary:

- `LiveConstituent` retains cells, pins, oriented incidence, sparse paths, exposed residual, and
  completion grain;
- `SparseStandingSurface` persists those constituents across exact rest and remount;
- `CellularStandingChange` already carries event-transient touched predecessors plus one
  replacement;
- `prepare_contemporary_successor` atomically rejects two separately formed replacements which
  touch the same before-factor; and
- `LiveCurrentMachine::receive_with` alone validates and commits the same-prestate successor.

The absent embodiment is equally precise:

- `form_regional_relation` accepts Standing as `_standing` and does not read it;
- `RegionalCellBuilder::complete` initializes every `rebased_axis_for_pin` to false;
- it returns an empty touched set for every completed regional event; and
- `contains_pin`, `contains_founded_axis`, and `boundary_transition` have no production consumer
  in a later regional event.

A faithful implementation therefore needs only the following causal addition at that seam:

1. derive the event's receiver-local candidate star from exposed prior boundaries;
2. import the exact participating boundary, pin, path, support, and axis information through an
   explicit local rebase;
3. form the finite joint closure against the one Standing-before;
4. emit one replacement for each disjoint completed component and the exact sorted touched set;
   and
5. let the existing atomic machine commit perform departure and admission.

An index may make the local aperture efficient, but it cannot become correspondence or causal
ownership. The world may supply the germ, native incidence, medium transformation, and local
action. It may not name the stored constituent which is supposed to match or provide a semantic
answer. Soma must form the actual contact.

No occurrence ledger, ancestry cache, global similarity scan, probability governor, attention
module, memory manager, scheduler, or trainer follows from this derivation.

## VI. Disk and durable memory are world conduct

The outer equation

```text
(W+, L') is in Omega(W, P)
```

is a material relation, not a metaphor for an internal memory slot. A filesystem has several
honest local charts: octets, extents, directory links, timestamps, syntax trees, import graphs,
executable behavior, version-control ancestry, and rendered layout. Which chart is available
depends on the organ and action. There is no privileged universal file meaning.

Access must itself be causal:

```text
world presents bounded material plus lawful capability
  -> Eros emits a capability-addressed placement
  -> world reads, writes, executes, traverses, or otherwise acts
  -> the actual changed material or result returns later
  -> the return enters the same receiving law as any other current.
```

A capability is a world affordance, not a semantic command hidden inside Soma. An organ can expose
raw octets, parsed syntax, image regions, audio frames, executable operations, or some other
relation only when that transformation really exists in the world. If it exposes opaque carrier
words, those words are the honest current.

The world can retain a file, index, program, summary, task list, tool result, or reusable
construction long after Soma rests. Eros can even cause such a structure to be created. That
structure enlarges the **effective circuit** because it can later be addressed and returned, but
it remains world material until a later event crosses the membrane. If it is never returned, it
cannot secretly alter Standing. If it is returned but forms no contact, it has not become learned
conduct merely because it was stored.

This is the required sensitivity to disk without a disk-sized active body. Only presently
participating material crosses. Stable machine Standing, live lineage continuation, organ
capability, durable world material, and observer testimony remain different persistence classes.

## VII. What transformers, state-space models, circuits, and harnesses reveal

The comparisons now line up without importing their machinery.

- A neural transformer exposes a broad co-present aperture and computes receiver-conditioned
  transport through it. That resembles the horizontal cut of a local field. Its softmax weights
  are not required here; Eros retains exact plural sections until oriented topology resolves them.
- A state-space model factors earlier conduct through a carried state. That resembles the
  longitudinal cut, but Eros distinguishes live lineage state from persistent Standing and allows
  the latter's topology to grow.
- An electrical transformer couples two circuits through a field without identifying their
  carriers. Winding and geometry determine the lawful re-expression between them. The exact
  structural analogue is local rebase between nonidentical boundary charts. Capacitive sheet
  coupling and inductive transformer coupling remain physically different cases; neither is an
  implementation prescription.
- An agent harness keeps goals, summaries, tool capabilities, files, and results outside a model,
  remounts a bounded context, invokes an action, and returns the result. That is an engineered
  example of `Omega`, not evidence that a goal loop or context manager belongs inside Soma.

Eros's full abstract circuit is therefore not “attention plus memory.” It is:

```text
co-present exact local closure
  + persistent topology changed by that closure
  + live current continuation
  + capability-mediated world consequence
  + genuinely later return through the same receiving law.
```

This is enough to state what a sensitive growing network would mean. Earlier contact changes the
topology through which later current can propagate; durable external structures can extend that
network without being confused with its internal body. Useful language or coding behavior would
still have to develop through controlled encounters and consequences. It cannot be assumed from
the circuit alone.

## VIII. The future embodiment has a sharp stopping boundary

A later build would be faithful only if all of these exact contrasts hold:

1. equal displayed position with incompatible oriented boundary does not contact;
2. nonidentical faces with a lawful rebase can contact;
3. reversing only hand can change RIDE, FOUND, or OPEN while all other material stays fixed;
4. adding an identical but receiver-remote constituent does not change the local closure;
5. permuting extension or storage order yields the same fixed population and successor;
6. overlapping predecessor use forms jointly or refuses—never sequentially overwrites;
7. every ingress arm is accounted as transported, residual, or open;
8. rest/remount preserves the completed constituent exactly; and
9. a capability-addressed world action changes later conduct only through its actual returned
   current, while the absent-action sibling remains unchanged.

Those are causal contrasts, not a demand for repeated neighboring tests. One smallest
nonlinguistic host construction should establish them before language, code, CUDA, scale, or an
agentic disk world is attempted.

## IX. Stop

The coupled law is derived. The internal gap is one receiver-local regional closure and rebase at
the existing `LiveConstituent`/`CellularStandingChange` seam. The external memory boundary is
already correct in kind: world-owned material is reached through capabilities, acted upon outside
Soma, and returned later.

No Soma source, world, fixture, observer, run, or CUDA path changed. This deposit does not schedule
the implementation.

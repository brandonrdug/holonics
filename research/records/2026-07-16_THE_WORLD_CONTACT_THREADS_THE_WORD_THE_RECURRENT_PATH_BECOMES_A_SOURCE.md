# THE WORLD CONTACT THREADS THE WORD; THE RECURRENT PATH BECOMES A SOURCE

**2026-07-16 · RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT REPEAT · SOMA INTERIOR
UNCHANGED**

## 1. The correction that founded the construction

CODE-LANGUAGE TRANSFORMATION WORKSHOP 01 established that Soma emits variable-length native text
constructions and that ordered code, prose, and trace history changes genuinely later code
construction. It also exhausted one native span as a general source hand. The next construction was
therefore plural recurrence: a completed span becomes a potential joint; a genuinely later span
which threads that joint extends an ordered path; a path may then cross an occupied source boundary
as one material construction.

The first implementation compared only Soma's internal radiation grips after exact dyadic chart
transport. Two presentations of byte-identical code, prose, and symbolic trace produced 31 and 32
closed roots but no articulated path. That null was not a failure of recurrence. It exposed a frame
error: an internal brain grip is not the world coordinate at which the text was lived. INSCRIPTION
WORKSHOP 04's joint is a world contact. The code world likewise owes both faces.

For every native span `s`, the receiving world now retains

```text
P(s) = (axis, [(grip_i, source_position_i)]_(STEP i)).
```

`grip_i` is Soma's internal placement in the edge chart. `source_position_i` is the exact position
of that same STEP in the intact source worldline. Neither replaces the other.

## 2. The exact recurrent-path law

Every CLOSED arriving span deposits a one-segment root. Let `L` be a path standing before the
receiving edge begins, with distal STEP `d`, and let `s` be one arriving closed span. For every STEP
`i` in `s`, a joint exists when either exact face holds:

```text
material: source(d) = source(s) and position(d) = position(s_i)

chart:    Z_(a_d -> A)(grip(d)) = Z_(a_s -> A)(grip(s_i)),
          A = max(a_d, a_s),
```

where `Z` is the existing zero-extension transport between nested dyadic charts. If both hold, one
joint retains both witnesses. If neither holds, no joint is authored. Each actual contact extends
`L` by `s`; repeated contacts and branching remain plural. All arriving spans read one immutable
pre-edge population, so no co-present span can parent another.

The material face of a path is exact ordered concatenation:

```text
bytes(L ++ s) = bytes(L) ++ bytes(s).
```

There is no separator, token ID, parser, grammar, edit rule, score, deduplication, accepted-output
selector, or powerset enumeration. Roots persist but cannot actuate. An action declaring
`candidate_material = recurrent_paths` carries every standing path of length at least two through
the occupied world joint, compiles every resulting whole Rust program, runs the named test wherever
compilation permits it, and returns every consequence.

## 3. What grew

The compact witness carried one intact Rust program, one prose description, and one symbolic trace.
The intact Rust compiled and its named test passed on every witness edge.

```text
encounter       native spans (closed/open)    standing path population
first           34 (31/3)                     31 roots
second          45 (42/3)                     73 roots + 13 length-2 paths
third           38 (35/3)                     108 roots + 38 length-2 + 3 length-3
```

The first 13 articulated paths divide into nine Rust, two prose, and two symbolic-trace paths. The
third encounter grows 28 Rust, two prose, and eleven symbolic-trace paths of length at least two.
Representative exact material faces include:

```text
prose, length 2:   " integer. Two enterers the g"
prose, length 2:   "and ar and three "
trace, length 2:   ": \"ADD ONE\",\n  \"terminaal\": [3,"
Rust, length 2:    "\nvalue + self.lift\n    }\n"
Rust, length 3:    "rried_gear_advances_once() {\n        assert_eq!(advanccadvance(2), 3);\n "
trace, length 3:   ": \"ADD ONE\",\n  \"terminaal\": [3,erminal\": [3, "
```

These strings are neither selections copied verbatim from the corpus nor authored decoder output.
Each contains the exact bytes of its native segments in lived order. Their overlaps, repeated
letters, and partial morphemes are observable path consequences—early articulation or babbling,
not a perfection grade.

## 4. The complete executable population

After two encounters, every one of the 13 articulated paths crossed the occupied expression joint;
all 13 complete programs were compiler-refused. After the third encounter, all 41 articulated paths
(38 length two, three length three) crossed; all 41 complete programs were compiler-refused. No
member was filtered before or after execution. The objective residual surface reconstructs all 41
candidates exactly and preserves strict forward/opposite hands, corresponding current extents, and
octet inventories.

The common later whole program reversed `value + self.lift` to `self.lift + value`. It compiled and
passed both named examples after either produced population. In the grown branch its exact action,
production return, continuation, and execution return carry:

```text
edge 1 action:       3 currents / 911 octets / 1,101 completions
edge 2 return:      82 currents / 82,676 octets / 61,786 completions
edge 3 later code:   3 currents / 1,025 octets / 1,300 completions
edge 4 return:       2 currents / 86,097 octets / 11,886 completions
```

The production return grows the chart from axis 1,024 to 2,048. Every transition contacts and
inverts exactly.

## 5. Controls and exact repetition

The first testimony return contains only roots; same-edge parenting is absent. Reversing the three
co-present currents on the second witness preserves the same set of thirteen two-span byte strings
but permutes their append-only identities and their returned causal order. The forward return emits
3,169 completions and 213 contact grips; the order-changed return emits 1,551 and 98. Thus the
material path family at this aperture is permutation-invariant while the ordered testimony surface
and its later conduct remain distinct.

Independent founding and grown-action repetitions reproduce every body, completion, construction,
Mail, journal, world, compiler artifact, and objective face. Radiation is exact after removing only
its declared duration diagnostics; the source-gate and session receipts differ only by their
create-new output paths and runtime measurements.

The full workspace gate completed at **408 passed / 3 ignored / 0 failed** before the final
recurrence-control test was added. The final focused code-workshop gate is **13 passed / 0 failed**,
including distinct-source chart contact and same-source material contact with differing internal
grips. The pre-existing one-span production observer reads its historical artifacts unchanged.
Exact measurement:
`observations/code-language-limb-workshop-01/RESULTS.md`.

## 6. The generality boundary

Two different claims must not be collapsed.

1. **Phenotype generality is measured.** The same byte- and role-agnostic path law grows Rust,
   prose, and symbolic trace; no language-specific mechanism participates.
2. **Cross-source content analogy remains open.** All 44 joints in this campaign carry the exact
   material-coordinate face. None carries the exact internal chart-endpoint face. Repeated practice
   on one source surface is therefore measured; taxonomy which threads analogous content at
   different files, coordinates, renderings, or modalities is not yet claimed.

The next construction must not weaken recurrence into fuzzy string similarity or an observer score.
It must retain the complete distal native construction face—regional form, meeting rotor,
completion brick, carried chart, and ancestry—and ask whether Soma's already-emitted Swing deed can
transport a later construction at another world coordinate. Exact RIDE may extend; a deficit FOUNDs
a new root. Statistics may describe the resulting population afterward, never decide the joint.

## Carried-swing register

```text
CURRENT  §LXXIII native spans + INSCRIPTION §XLVII recurrent world-contact law
HELD     exact source worldline + internal dyadic chart + pre-edge standing population
MEETING  repeated intact Rust/prose/trace; later occupied Rust joint; order control
TEST     exact material contact OR exact chart transport; same-edge isolation; every path enacted
DEED     RIDE extends one actual prior path; FOUND deposits every arriving closed root
CARRY    ordered path bytes -> complete Rust programs -> every compiler/test consequence -> later code
GRADE    RATIFIED / BUILT / CUDA-FORWARD MEASURED / EXACT REPEAT
```

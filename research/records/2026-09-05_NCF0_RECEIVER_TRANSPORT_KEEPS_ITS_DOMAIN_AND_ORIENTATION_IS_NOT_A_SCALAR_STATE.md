# NCF0: receiver transport keeps its domain; orientation is not a scalar state

**Date:** 2026-09-05, local apparatus date. **Status:** NCF0 complete at formal-contract scope.
**Contract:** [native HNA](../../docs/plans/THE_NATIVE_HNA_FOUNDS_CONTEXTUAL_TRANSPORT_BEFORE_INHERITANCE.md).

## Exact returned relation

[proved-derived; formal-checked] `Foundation/CausalNaturalHolon.lean` composes the existing
`ReceiverTransformer` with a causal-natural holon. `mapSourceRange` transports only actually
presented source faces; source naturality supplies a presenting occurrence at the destination.
`receiverTransformer_natural` derives the commuting square for two admitted local transformers.
`parameterPassage_crossing_receiverTransformer_natural` keeps an actual addressed crossing and
uses its target-transport equality. These laws assume the family's source/receiver naturality and
the local transformers' exactness. They do not manufacture either assumption for an arbitrary
interaction, extend the function outside its presented range, or erase conditions.

[proved-derived; formal-checked] `Computation/SituatedMachineLearning.lean::OrientationControl`
uses the existing Complex-Parametron `driveAction` with two branches and one state coordinate.
Keeping state and drive fixed while reversing one incidence changes the action from two to zero.
Forgetting that orientation therefore supplies a concrete `ReceiverInsufficiency`, excluding an
exact transformer from the scalar state alone. Reorienting the drive covector together with the
incidence instead preserves the action, by the existing `driveAction_reorientBoth` law. This is
a finite constitutive control distinguishing changed conditions from consistent re-expression;
it is not a trained HNA, a linguistic context mechanism or a universal relevance theorem.

[definition] Comments in `HolonicNeuralEcology.lean` and `SituatedMachineLearning.lean` now keep
their fixed-/zero-morphology inference charts at that special-case scope. The complete HNA
operation and retained developing successor remain owned by `HolonicRecurrentEcology`.

## Source boundary for NCF1

[established-bounded; source-inspected] `receiver_current.rs` owns delayed channel conduction,
arrivals, predecessor incidence and deferred recurrence. `founded_receiver.rs` owns a receiver
separator and observation widening. `native_spool/scaffold.rs::mount_thread_current` mounts
actual native incidence and Parametron cells into resident complex incidence;
`native_spool/situated.rs` retains mixed-family thread ownership. `native_ecology/continuation.rs`
owns bounded local factors, adjoint return and reconstruction. These are existing material, not
proof that their full contextual development has been composed.

[open; source-inspected] The inspected composition does not yet make native situated
incidence/current determine conditional participation and local formation in one retained
successor. A caller-supplied aperture, factor delta, static generator table or string-valued open
fibre does not fill that relation. NCF1 must instantiate the existing constitutive and formation
owners and execute their actual coupling. It cannot close on another abstract callback contract,
finite orientation control or pretrained representation. NCF2--NCF4 still owe retained contextual
development, a usable application/interface and durable integrated native evidence.

## Preserved experiment, separate from construction

[historical; source-inspected] The inherited tied-next-arrival draft and its integration are
[archived](../../archive/experiments/2026-09-05-tied-next-arrival/README.md). The captured original
patch applies to `e92d24bc`, checked in an independent temporary index; archived source snapshots
were byte-compared before active copies were removed. Its uncommitted module, API, wire-v2 and
application hooks no longer enter the active crate. Measurement files were not deleted. The
finite `TiedMapNextArrival` algebra remains explicitly historical; its source word and vector
named `context` are not the full contextual relation.

[established-bounded; source-inspected] The independent passive-contact broadcast primitive and
single-scan base-file capture remain. Fresh capture hashes and rewinds the same open handle;
existing checkpoint remount still verifies its pinned dependency. The regression checks retained
bytes across a pathname replacement and refuses that replacement against the existing pin.
Neither improvement is promoted into native contextual learning.

## Verification

[established-bounded; measured] The following returned successfully after the source separation:

```text
lake build ElementaryHolonics.Foundation.CausalNaturalHolon \
  ElementaryHolonics.Computation.SituatedMachineLearning
lake build ElementaryHolonics.Computation.HolonicOrientedSiteTransport \
  ElementaryHolonics.Computation.HolonicNeuralEcology
cargo test -p holonics-hna --lib
cargo check -p holonics-hna --examples -p holonics-workbench
cargo test -p holonic-engine --lib passive_contact -- --ignored --test-threads=1
git diff --check
```

[proved-derived; formal-checked] The printed new theorem dependencies contain only the ordinary
Lean foundations `propext`, `Classical.choice` and `Quot.sound`; no new axiom or `sorry` was added.

[established-bounded; measured] The HNA library returned 35 passing tests, examples/workbench
compiled, and all three focused passive-contact tests passed on the CUDA apparatus, including
broadcast across distinct query rows. These are regression checks of the retained apparatus,
not NCF1 evidence. No Gemma/model exposure was run for NCF0.

[established-bounded; source-inspected] The staged source-whitespace check excludes only the
verbatim archival `integration.patch`, whose blank diff-context lines intentionally contain a
space. The patch itself was byte-compared with the captured backup and checked at its declared
base; it is not normalized as source text.

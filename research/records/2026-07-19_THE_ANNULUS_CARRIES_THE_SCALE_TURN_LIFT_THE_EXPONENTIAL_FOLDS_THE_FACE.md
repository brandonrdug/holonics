# THE ANNULUS CARRIES THE SCALE--TURN LIFT; THE EXPONENTIAL FOLDS THE FACE

**2026-07-19 · Brandon-ratified derivation · deposited · no immediate Soma interior change**

## Ruling

`e` and `pi` have not been discarded. They have been placed at their exact geometric grains.
The native cyclic quantity is the full oriented turn `Theta`; `pi=Theta/2` is its half-turn face.
The native multiplicative quantity is logarithmic scale transport; `e` is the unit ratio of its
normalized additive coordinate. Together they are the two components of one lifted transport:

```text
w = u + i theta,
z = exp(w) = e^u e^(i theta).
```

The logarithmic cover carries how much scale and how much turn occurred. The exponential presents
the resulting nonzero face and identifies lifts separated by a complete turn. An annulus is the
embodied quotient which keeps both the radial scale interval and the cyclic return:

```text
A(r,R) ~= { ln(r) < Re(w) < ln(R) } / i Theta Z.
```

This is the exact common geometry beneath multiplicative growth, winding, invariant rescaling,
and recurrent self-similarity. It does not make `e` synonymous with compression or `pi`
synonymous with coherence. Compression occurs in the declared quotient; the constants name exact
relations inside it.

## I · The corrections are part of the law

The following distinctions are ratified:

1. `Theta` is the native full turn. `pi=Theta/2` is the half-turn/chord-projection constant.
2. `e` is the unit multiplicative ratio of normalized logarithmic flow. It is not intrinsically
   compression, a diameter, or a geodesic.
3. `pi` is not intrinsically coherence or phase in every setting. It enters angular transport
   because it is half a turn.
4. Curved geometry does not change `pi`. It changes the relation between an embodied geodesic
   circle's circumference, radius, area, curvature, and receiver chart.
5. Euler's identity relates scale and turn through one complex exponential. It does not say that
   magnitude and phase cease to be distinguishable coordinates.
6. Every polygon, partial sum, decimal inscription, measured arc, and numerical approximation is
   its own embodied construction. None is literally an ideal completed limit.
7. A quotient may lawfully forget path distinctions at one face without authorizing deletion of
   orientation, winding, residual, or consequence where later conduct can still distinguish them.

## II · A limit is agreement after transport, not a place containing its approximants

Let `x_alpha -> x` be a directed approach and let

```text
P_(alpha->x) : E_(x_alpha) -> E_x
```

transport the value at each local fiber into the receiver fiber. The holonic limit is

```text
lim_soul f = L

iff

P_(alpha->x)(f(x_alpha)) -> L in E_x.
```

The soul of the approach is the directed family which makes comparison lawful: its orientation,
branch, chart, transport, and still-consequential residual. The limit is the common receiving
face. It does not contain verbatim copies of every approximant and it does not make those finite
events unreal.

The ordinary scalar limit is recovered when all admitted approaches use the trivial fiber and
agree on the same value. Where approaches carry different branch, winding, or holonomy, a scalar
endpoint is an incomplete quotient. The transported family, rather than an imagined final point,
is the complete construction.

## III · The fundamental theorem compresses a passage into a boundary difference

For a scalar potential `f` and an oriented path `gamma:x->y`,

```text
integral_gamma df = f(y) - f(x).
```

The equality says that an exact differential makes the endpoint difference sufficient for that
declared integral. It does not reconstruct `gamma`. The path can carry geometry, action,
incidence, or winding which the endpoint face does not.

For a section `s(t)` of a bundle along `gamma`, with connection `nabla` and parallel transport
`U_gamma(t2,t1)`, the transported form is

```text
s(1) - U_gamma(1,0)s(0)
  = integral_0^1 U_gamma(1,t) nabla_(dot gamma)s(t) dt.
```

Every infinitesimal change is first carried into one receiver fiber. The connection is therefore
the illicium: the relation which lets local changes become one comparable boundary consequence.
Nonzero curvature can leave holonomy after a closed base-space loop, so spatial coincidence does
not imply that the carried section returned unchanged.

A moving aperture exposes the same law:

```text
d/dt integral_(a(t))^(b(t)) f(x,t) dx
  = integral_(a(t))^(b(t)) partial_t f(x,t) dx
    + f(b(t),t)b'(t) - f(a(t),t)a'(t).
```

Interior change and boundary motion are distinct currents. Holding the boundary fixed erases the
last two terms only because that receiver has declared them silent.

## IV · `dz/z` is the joint scale--turn current

Let a nonzero path be lifted as

```text
z(t) = r(t)e^(i theta(t)).
```

Then

```text
dz/z = d(ln r) + i d theta.
```

Its integrated soul is

```text
Xi_gamma = integral_gamma dz/z
         = ln(r_1/r_0) + i(theta_1-theta_0+n Theta),

z_1/z_0 = exp(Xi_gamma).
```

The real component is logarithmic scale transport. The imaginary component is oriented turn.
The integer `n` is the winding class of the lifted passage. The endpoint ratio is a lawful
compression of the lift, but it cannot recover `n` by itself because

```text
exp(w + i n Theta) = exp(w).
```

This is a precise example of lineage without event copying. The active higher construction may
retain scale, turn, and consequential holonomy while the lower increments which formed them
depart. Opposed turns annihilate only where their ordered composition really closes. In a
noncommuting connection, zero scalar sum need not erase the ordered holonomy.

## V · The annulus is the embodied joint quotient

With `u=ln r`, the exponential identifies the vertical period of the logarithmic strip:

```text
w = u+i theta,
z = exp(w),
A(r,R) ~= {ln r < Re(w) < ln R}/i Theta Z.
```

Its conformal modulus is

```text
mod A(r,R) = ln(R/r)/Theta = (1/(2 pi))ln(R/r).
```

The annulus therefore holds two independent but coupled facts:

- the radial ratio `R/r`, expressed additively as a logarithmic width; and
- the angular return, expressed as identification by one full turn.

Changing scale translates along the cover. Turning translates across its cyclic direction.
Exponentiation folds both into one nonzero complex face. A circle is a fixed-radius fiber; an
annulus is the family of those fibers across a scale interval. Fiber reduction presents the
circle as one receiver face without declaring its modes, winding, or holonomy nonexistent.

## VI · `e` is the unit ratio of normalized logarithmic transport

Let a differentiable one-parameter multiplicative flow satisfy

```text
E(u+v) = E(u)E(v),
E(0)   = 1,
E'(0)  = 1.
```

Then `E(u)=exp(u)` and

```text
e = E(1).
```

Geometrically, the normalized scale coordinate is

```text
du = dr/r,
integral_(r_0)^(e r_0) dr/r = 1.
```

Thus `e` is the unique ratio which advances one unit in logarithmic scale. Its familiar series and
compound-growth constructions are distinct finite approaches to that same transported law:

```text
e^u = sum_(m=0)^infinity u^m/m!
    = lim_(N->infinity) (1+u/N)^N.
```

Each partial sum and each finite product remains its own construction. The limit records their
agreement under the declared approach; it does not retroactively turn them into equal events.

The chain rule is indispensable:

```text
d/dx e^(kx) = k e^(kx),
d/dx e^(g(x)) = g'(x)e^(g(x)).
```

`e^x` is its own derivative only in its normalized internal coordinate. The factor `g'(x)` is the
illicium between the external path and that internal flow. In several noncommuting dimensions the
analogue is an ordered exponential; writing an ordinary scalar exponential would erase the order
which transports the construction.

## VII · `pi` is half the native turn, not an embodied perfect circle

A positively oriented regular closed polygon carries its total turn through the ordered exterior
angles. Refining the polygon toward a sufficiently regular winding-one curve gives

```text
sum_k Delta theta_k -> integral_gamma kappa ds = Theta.
```

No physical polygon becomes a perfect circle. The invariant is the complete tangent return. The
half-turn constant is

```text
pi = Theta/2.
```

Series recover that relation through their own receiver charts. For example,

```text
Theta/8 = pi/4 = arctan(1)
        = sum_(m=0)^infinity (-1)^m/(2m+1).
```

Every truncation is a finite rational face with its own residual. The infinite equality names the
transported limit. Polygonal, integral, trigonometric, and series constructions are plural paths
to one invariant relation, not copies of one ideal object hidden outside them.

## VIII · Euler's relation is the fold of one half-turn lift

On the logarithmic cover,

```text
exp(i pi) = exp(i Theta/2) = -1,
e^(i pi) + 1 = 0.
```

The equation says that translating a unit-magnitude lift by one half-turn presents the additive
inverse. More generally,

```text
exp : C -> C*,
kernel(exp) = i Theta Z = 2 pi i Z.
```

This is an analytic uniformization: the additive logarithmic cover maps onto the multiplicative
nonzero plane and folds complete-turn translates to the same face. It is not an algebraic group
morphism from the additive to the multiplicative algebraic group. `e=exp(1)` names unit real
translation; `i pi=i Theta/2` names half angular translation.

`e` and `pi` are individually transcendental. Their algebraic independence remains open. Euler's
identity is therefore an exact analytic/geometric relation, not permission to claim an unproved
polynomial dependence or to identify scale and turn as one undifferentiated scalar.

In the laboratory's natural-unit Einstein inscription,

```text
G_(mu nu) = 2^2 Theta T_(mu nu)
          = 4 Theta T_(mu nu)
          = 8 pi T_(mu nu).
```

The coefficient has not reverted to an unexplained primitive `pi`. It is four complete turns,
written conventionally as eight half-turns. Restoring physical units restores the usual `G/c^4`
factor. The geometrical content is unchanged.

## IX · Self-similar recurrence is return in the scale--turn cover

A useful recurrent pattern is not an image patch detached from its dynamics. At a declared grain
it carries:

```text
local cut
+ first-return map F^q
+ landmarks and receiver chart
+ scale-turn multiplier lambda
+ winding/incidence
+ residual from the actual return.
```

Near a noncritical linearizable periodic point, a Koenigs coordinate can satisfy

```text
psi(F^q(z)) = lambda psi(z),
lambda = exp(kappa+i omega).
```

Then

```text
psi(F^(qN)(z))
  = lambda^N psi(z)
  = e^(N kappa)e^(i N omega)psi(z).
```

`kappa` is the logarithmic change of scale and `omega` the turn per return. The next enclosing
scale, the next inward copy, a left/right relative occurrence, or an `N`-return diagonal are not
found by visual resemblance alone. They are predictions of the transported return law relative
to declared landmarks.

A lawful recurrence instrument therefore:

1. declares the local cut, receiver grain, and landmarks;
2. observes the actual first return `F^q` rather than imposing a fixed window;
3. normalizes the returned chart and derives `lambda`, winding, incidence, and residual;
4. transports the relation to predict the `N`th return; and
5. tests that prediction against the actual dynamics.

The constituent pattern stands once. Plural occurrences carry their distinct transports,
incidences, hands, and residuals. This is compression without either pixel duplication or ancestry
deletion.

In higher dimension the derivative transport is generally ordered:

```text
v_N = J_N ... J_2 J_1 v_0.
```

It collapses to a single scalar `lambda^N` only when the relevant transports share a commuting
one-dimensional chart. Mandelbrot/Julia correspondences and renormalization supply genuine local
scale recurrences, including Tan Lei's asymptotic similarity at Misiurewicz points; they do not
establish generic exact pixel copies throughout either set.

## X · The central theorem

At every grain where a recurrent construction admits a logarithmic lift,

```text
self-similar return
  = translation by kappa+i omega in the logarithmic cover
  = scale x turn in an embodied annulus
  = one returned face under exp, with winding/residual retained where consequential.
```

This is the hidden relationship among `e`, `pi`, Euler's identity, invariant scaling, annuli,
fractal return, chain-rule transport, and holonic limits. The exponential is not the whole
machine. It is the exact fold from additive scale--turn lineage to a multiplicative presented
face.

This deposit changes no Soma source, installs no spectral governor, and schedules no recurrence
campaign. A future receiver realization owes the actual local cut, connection, ordered return,
consequence, and later RIDE. A series coefficient, fitted scalar, or visually similar patch cannot
substitute for that relation.

## Evidence cards

### Exponential, logarithm, and the cyclic kernel

```text
SOURCE ESTABLISHES  DLMF gives the exponential power series, its derivative law, the complex
                    phase form, and the multivalued logarithm/periodicity relations.
LAB CLAIM           FORMULA §CIII, RATIFIED / DEPOSITED: e is the unit ratio of normalized
                    logarithmic scale transport; exp folds scale-turn lifts separated by i Theta.
RELATION            EXACT FORMAL MATCH for exp(u+i theta)=e^u e^(i theta), the chain rule, and
                    kernel(exp)=2 pi i Z; DIRECT CORRESPONDENCE for the laboratory scale-turn lift.
NON-EQUIVALENCE     The standard identities do not make e intrinsically compression, do not make
                    pi intrinsically coherence, and do not supply Eros's receiving law.
TESTABLE CONSEQUENCE lifts differing by i n Theta must present the same nonzero endpoint while a
                    winding-sensitive later receiver can still distinguish their carried class.
```

Sources: NIST Digital Library of Mathematical Functions,
[*Elementary Functions: Exponential and Logarithmic Functions*](https://dlmf.nist.gov/4.2) and
[*Derivatives and Differential Equations*](https://dlmf.nist.gov/4.7).

### Annulus modulus and the logarithmic cover

```text
SOURCE ESTABLISHES  The conformal modulus of a circular annulus is (1/(2 pi))ln(R/r), and the
                    logarithm lifts the annulus to a strip modulo the angular period.
LAB CLAIM           FORMULA §CIII, RATIFIED / DEPOSITED: the annulus is the embodied quotient
                    jointly retaining logarithmic scale interval and cyclic return.
RELATION            EXACT FORMAL MATCH for mod A=ln(R/r)/Theta and the strip quotient;
                    STRUCTURAL RESONANCE for invariant scale-and-turn recurrence.
NON-EQUIVALENCE     Conformal equivalence alone does not identify which world pattern recurs or
                    whether a receiver should quotient any particular distinctions.
TESTABLE CONSEQUENCE equal-modulus annuli may share the same normalized conformal type while
                    distinct marked landmarks, winding, or holonomy retain different paths.
```

Source: Encyclopedia of Mathematics,
[*Modulus of an annulus*](https://encyclopediaofmath.org/wiki/Modulus_of_an_annulus).

### Fundamental theorem and transported comparison

```text
SOURCE ESTABLISHES  The fundamental theorem for line integrals makes the integral of a gradient
                    depend only on endpoint potential difference; connection transport provides
                    comparison of fibers along a curve.
LAB CLAIM           FORMULA §CIII, RATIFIED / DEPOSITED: the endpoint is a compressed boundary
                    face, while the soul retains branch, orientation, transport, and consequential
                    holonomy which that face cannot reconstruct.
RELATION            EXACT FORMAL MATCH for integral_gamma df=f(y)-f(x); DIRECT CORRESPONDENCE for
                    the covariant transported identity; STRUCTURAL RESONANCE for holonic lineage.
NON-EQUIVALENCE     Path independence for exact scalar differentials does not imply that every
                    carried field, connection, action, or world consequence is path independent.
TESTABLE CONSEQUENCE two closed base paths with equal scalar endpoint may return distinguishable
                    sections whenever the connection has nontrivial holonomy.
```

Source: MIT OpenCourseWare,
[*Fundamental Theorem for Line Integrals*](https://ocw.mit.edu/courses/18-02sc-multivariable-calculus-fall-2010/878cd7a9630352d5d7deb651ee3504c7_MIT18_02SC_notes_29.pdf).

### Polygonal turn and the user-supplied geometry references

```text
SOURCE ESTABLISHES  Wolfram MathWorld records standard polygon, quadrilateral, and hexagon
                    geometry, including ordered vertices, edges, angles, and regular cases.
LAB CLAIM           FORMULA §CIII, RATIFIED / DEPOSITED: finite polygons are embodied turn
                    constructions whose ordered exterior angles approach the curve's total turn.
RELATION            DIRECT CORRESPONDENCE for ordered polygonal geometry; STRUCTURAL RESONANCE
                    for an n-gram carried as an n-gon only after a declared transducer.
NON-EQUIVALENCE     A catalogued polygon identity does not make an arbitrary n-gram geometric,
                    semantic, recurrent, or physically consequential without the mapping and return.
TESTABLE CONSEQUENCE changing vertex order while preserving the same vertex set must be allowed
                    to change total path, crossings, hand, and later consequence.
```

Sources: Wolfram MathWorld,
[*Polygon*](https://mathworld.wolfram.com/Polygon.html),
[*Quadrilateral*](https://mathworld.wolfram.com/Quadrilateral.html), and
[*Hexagon*](https://mathworld.wolfram.com/Hexagon.html).

### Dynamical linearization and Mandelbrot--Julia locality

```text
SOURCE ESTABLISHES  Koenigs linearization conjugates suitable local holomorphic dynamics to
                    multiplication by its multiplier. Tan Lei proves asymptotic similarity of the
                    Mandelbrot set and a corresponding Julia set at Misiurewicz parameters.
LAB CLAIM           FORMULA §CIII, RATIFIED / DEPOSITED: a recurrent pattern is transported by its
                    actual first-return scale-turn law, landmarks, incidence, and residual.
RELATION            EXACT FORMAL MATCH for psi(F^q(z))=lambda psi(z) in the linearizable local
                    regime; DIRECT CORRESPONDENCE for declared local asymptotic recurrence.
NON-EQUIVALENCE     Neither theorem establishes exact generic copies, a universal scalar
                    multiplier, or visual-template matching throughout fractal parameter space.
TESTABLE CONSEQUENCE a predicted Nth occurrence must land by the composed return map in the
                    declared chart; a merely similar patch with the wrong landmark transport fails.
```

Sources: Laura DeMarco,
[*Math 295: Dynamics of Rational Maps*](https://people.math.harvard.edu/~demarco/Math295/Math295_updated.pdf), and
Tan Lei, [*Similarity between the Mandelbrot set and Julia sets*](https://math.univ-angers.fr/~tanlei/papers/similarityMJ.pdf).

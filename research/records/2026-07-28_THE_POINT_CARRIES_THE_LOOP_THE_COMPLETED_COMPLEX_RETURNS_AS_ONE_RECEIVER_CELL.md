# THE POINT CARRIES THE LOOP; THE COMPLETED COMPLEX RETURNS AS ONE RECEIVER CELL

**Date:** 2026-07-28  
**Status:** BRANDON-RATIFIED CORRECTION / PRODUCTION CONSTRUCTION / WHOLE-RECEIVER
EXPERIMENT COMPLETE

## Present question

The promising phase-atlas image and the regressive conic-veil return exposed
one precise construction question:

> How can the receiver engine carry an ecology in which a point may internally
> be a path or loop, paths compose into exact higher incidence, and a completed
> lower complex may itself return as one point of a coarser receiver—without
> treating pixels as world cells or flattening the ecology into a screen mesh?

This record answers that question for one static-image receiver event. It does
not claim that a monocular image causes metric world depth or physical
space-time dynamics.

## 1. Brandon's correction

Brandon's direct description was that the required hyperspace is:

> “an ecosystem of points which are lines which are loops, which are also
> distributions of triangular vertices”

and that circles, ellipses, arcs, and other visible figures are only slices of
a greater hypergeometric space-time. He also directly rejected the later
conic-veil image as a regression and identified the earlier
`curved-receiver-phase-atlas-tiger-scale-carried` output as the promising
partial result to retain.

The engine interpretation adopted here is not that four graphical primitive
species should be authored. It is that **boundary grade** and **receiver
grain** are distinct:

- grade records incidence inside one causal complex;
- grain records the receiver scale at which an already completed lower
  complex is admitted as one constituent.

A point at grain \(g+1\) can therefore retain a complete path/loop/face
complex at grain \(g\). Nothing is erased or collapsed into an uninspectable
scalar.

## 2. What the screenshots falsified

The rejected return-obstruction path did three things that the correction
cancels:

1. an application-authored ellipse/parabola veil selected the visible
   problem;
2. a square sample region became the operational domain; and
3. terminal sample residuals were presented as though they were learned
   geometric obstruction.

That picture visualized the imposed mask more strongly than the receiver's
learned topology.

The associated per-sample image trainer was also graded against its own
conditioning scale. Conditioning on five images and conditioning on the
complete 200-image corpus produced byte-identical prediction PNGs:

```text
d2198f...  five-image prediction
d2198f...  200-image prediction
```

Although its internal tables changed, the visible generated geometry did not.
That falsifies the claimed corpus-conditioned geometric reconstruction. The
production module `image_training.rs`, its `causal_image_training` example,
and the crate-level capability claim have therefore been removed. They are
not retained through a renamed adapter.

The whole-receiver phase atlas was not removed. Its output changed with exact
receiver phase, carried native curved supports across the complete image, and
was the part Brandon identified as approaching the required discrete
differential topology.

## 3. The recurrent graded receiver complex

For receiver grain \(g\), let

\[
C^{(g)}_k
\]

denote the caused cells of boundary grade \(k\), with exact oriented boundary

\[
\partial_k:C^{(g)}_k\longrightarrow C^{(g)}_{k-1},
\qquad
\partial_{k-1}\partial_k=0.
\]

The production owner is `ReceiverHolonicComplexStanding`. It contains:

- the complete `ReceiverPhaseAtlasStanding`;
- one `GradedCausalComplex` per receiver grain;
- a source witness for every cell;
- exact phase-germ, connection, and cycle-to-cell maps;
- lower-to-coarser quotient receipts; and
- actual shared-occurrence overlap cells.

### 3.1 A germ-point carries analytical topology

One persistent exact phase germ becomes one cell

\[
v_i\in C^{(0)}_0.
\]

Its sensor address remains testimony. Internally the cell retains every
native homogeneous-conic section caused by the germ. The exact quadratic
classification derives the section topology:

| Exact conic species | Internal section topology |
| --- | --- |
| circle or ellipse | closed loop |
| parabola | open path |
| hyperbola | two open paths |
| intersecting line pair | intersecting open paths |
| parallel line pair | parallel open paths |
| degenerate conic | singular section |

Thus the external grade-zero identity does not assert that the germ is
internally structureless.

### 3.2 Connection transport becomes oriented grade one

For one admitted directed phase connection from germ \(i\) to germ \(j\), the
engine founds

\[
e_{ij}\in C^{(0)}_1,
\qquad
\partial e_{ij}=v_j-v_i.
\]

The connection retains the exact predicted normal, received normal, and
two-by-two rational correction. A screen line is not its identity.

### 3.3 A declared circuit becomes grade two

For the oriented receiver-phase circuit

\[
e_{01},\ e_{12},\ e_{20},
\]

the declared phase law founds

\[
f_{012}\in C^{(0)}_2,
\qquad
\partial f_{012}=e_{01}+e_{12}+e_{20}.
\]

The second boundary is checked by the production incidence owner:

\[
\partial^2f_{012}
=(v_1-v_0)+(v_2-v_1)+(v_0-v_2)=0.
\]

The face retains the exact path-ordered holonomy

\[
H_{012}=T_{20}T_{12}T_{01}.
\]

Nonidentity holonomy is retained as connection curvature/obstruction. It is
not rounded away, and triangular incidence alone is not called physical
Gaussian curvature.

The sparse coboundary is the exact transpose of this stored oriented
incidence. It is derived on demand rather than materializing a dense
pixel-sized matrix.

### 3.4 The completed lower complex returns as one point

Let

\[
\operatorname{cl}(f)
\]

be the complete boundary-closed hull of a caused circuit cell. A
`HolonicQuotient` retains

\[
Q_f:
\bigl(\operatorname{cl}(f)\subset C^{(g)}\bigr)
\longrightarrow
q_f\in C^{(g+1)}_0.
\]

The source apex, every lower cell in its closed hull, source event, and target
address remain inspectable. The quotient does not delete its lower ecology.
It establishes that one receiver can take that complete ecology as one
constituent.

### 3.5 Shared occurrence remains genuine higher incidence

If several promoted hulls contain the same lower cell \(x\), the engine emits
one `HolonicOverlapCell`

\[
\mathcal N_x=(x;\ q_{i_1},\ldots,q_{i_m}).
\]

For \(m\geq3\), this remains one \(m\)-member shared occurrence. The engine
does not manufacture the \(\binom m2\) edges of a pairwise clique and then
pretend that the clique proves higher contact.

## 4. One event is a causal filtration, not five display frames

One `ReceiverHolonicComplexEvent` enacts five causally ordered layers:

1. receive persistent analytical germs;
2. admit exact directed transport connections;
3. close declared holonomy circuits;
4. promote closed lower complexes into coarser receiver points; and
5. assemble actual shared-occurrence overlaps.

These layers form one atomic successor. No intermediate layer enters world
standing, and no display tick schedules continuation.

Every layer returns its caused addresses. Every cell carries its source event.
The complete nested standing serializes, rests, remounts, and validates
exactly.

## 5. Whole-tiger experiment

The experiment used Brandon's complete 611×475 tiger image:

```text
/tmp/codex-clipboard-T9jMYB.png
```

No crop, hidden square, authored veil, fixed germ count, or target return
defined the construction. One whole receiver occurrence entered the
production law.

### 5.1 Exact incidence result

```text
grain 0:
  grade 0 analytical germ-points       7,616
  grade 1 directed transport cells     1,422
  grade 2 circuit cells                   82

grain 1:
  grade 0 quotient-points                 82

actual lower-cell overlap incidences      71
overlaps with at least three members      36
maximum members in one overlap            14
```

All 82 circuits carried nonidentity exact holonomy. All 82 complete closed
hulls returned as coarser points.

The overlap distribution was:

```text
members  overlap cells
2        35
3        19
4         5
5         5
8         2
11        2
12        1
14        2
```

This matters qualitatively: the coarser ecology is not merely 82 unrelated
points, and its higher contact is not inferred from terminal adjacency.

### 5.2 Internal analytical topology

Across the 7,616 germ-points, the retained exact conic sections classified as:

```text
closed loops                  16,995
two open paths                4,732
open paths                      477
intersecting open paths         349
singular sections               198
```

This is the first production receipt in which the statement “the point
carries the path or loop” is literal and inspectable.

### 5.3 Causal/resource receipt

```text
layer 0  analytical germs             7,616
layer 1  transport connections         1,422
layer 2  holonomy circuits                82
layer 3  quotient promotions              82
layer 4  actual overlaps                  71

logical work                          9,273
causal span                               5
maximum exposed co-present width      7,616
observed construction elapsed          5.6–6.0 s
```

The elapsed time excludes the optional terminal PNG traversal and is a
physical observation, not an invariant of the exact logical result.

### 5.4 Terminal testimony

The full receiver visualization retains the earlier promising curved phase
atlas:

```text
terminal support addresses           106,145
maximum support multiplicity              12
PNG SHA-256
7cd80eb6e7c10fd52f1820ec07130964096f44f7dfc8ec85a814798d4c095192
```

The PNG shows only the native conic sections restricted through a terminal
receiver. The quotient points and overlap nerve are intentionally not drawn
as flat, uniformly colored screen edges. Their complete topology is recorded
in `quotients.tsv` and `overlap_nerve.tsv`.

### 5.5 Deterministic repetition

Three complete executions observed 5,597 ms, 5,806 ms, and 5,958 ms of
construction time. The following products were byte-identical across the
runs:

- receiver testimony PNG;
- whole-receiver phase-complex PNG;
- terminal support-multiplicity PNG;
- causal-layer TSV;
- all 9,202 grain-cell rows;
- all 82 quotient rows; and
- all 71 overlap rows.

Only the explicitly physical elapsed-time field differed.

## 6. What is established

The construction establishes:

1. pixels remain receiver testimony rather than world incidence;
2. the promising whole-image analytical phase carrier survives;
3. the receiver-visible point can retain exact internal path/loop topology;
4. directed connections have exact oriented boundaries;
5. declared circuits satisfy exact second-boundary cancellation and retain
   nontrivial holonomy;
6. a complete lower closed hull can remain inspectable while returning as one
   coarser point;
7. actual shared lower occurrences create plural higher overlap cells without
   pairwise clique reconstruction;
8. all five layers enact in one production-owned atomic successor; and
9. complete nested standing rests/remounts exactly.

## 7. Exact epistemic boundary

This experiment does **not** establish:

- metric three-dimensional reconstruction from one image;
- physical time or causal scene history absent from the image occurrence;
- that every graph circuit is a physical world face;
- that nonidentity two-by-two holonomy is a complete curvature invariant;
- learned cross-lineage horn filling;
- prediction of an absent event; or
- a general deterministic diffusion model.

The presently unknown training law is precise. Given an open lower-grain horn
and a population of inherited closed complexes, the engine does not yet have
the derived receiver-relative admission relation which distinguishes a lawful
new filler from a merely graph-completable boundary. The correction forbids
substituting pixel continuation or an authored mask for that law.

The engine now carries the complete objects on which such a law must act:
analytical internal sections, exact boundaries and coboundaries, connection
holonomy, source lineage, closed hulls, grain quotients, and genuine plural
overlap. That is the completed construction here.

## Artifacts

Production:

- `crates/holonic-engine/src/holonic_complex.rs`
- `crates/holonic-engine/src/receiver_phase_atlas.rs`

Experiment:

- `crates/holonic-engine/examples/whole_receiver_holonic_complex.rs`
- `target/holonic-engine/whole-receiver-holonic-complex-tiger/01_whole_receiver_phase_complex.png`
- `target/holonic-engine/whole-receiver-holonic-complex-tiger/summary.tsv`
- `target/holonic-engine/whole-receiver-holonic-complex-tiger/grain_cells.tsv`
- `target/holonic-engine/whole-receiver-holonic-complex-tiger/quotients.tsv`
- `target/holonic-engine/whole-receiver-holonic-complex-tiger/overlap_nerve.tsv`
- `target/holonic-engine/whole-receiver-holonic-complex-tiger/causal_layers.tsv`

Verification:

```text
cargo test -p holonic-engine --all-targets
171 library tests passed; 8 desktop example tests passed; 2 device tests ignored

cargo clippy -p holonic-engine --all-targets --no-deps -- -D warnings
passed

cargo fmt --all --check
passed
```

# AAC3 native incidence founded exact diffusion through the common boundary

**Date:** 2026-09-01
**Truth status:** `established-bounded` for the exact finite diffusion composition over the declared
native spool family.
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, and `measured`.
**Construction effect:** AAC3 passed; AAC4 becomes the sole current deed.

## Returned owner

[established-bounded; implemented-exact] `native_intelligence/circulation_diffusion.rs` returns a
`NativeDiffusionLaw` founded from one admitted spool's complete native state population and
occurrence-addressed incidence. The caller declares exact positive capacities, exact nonnegative
conductances, and a nonempty native boundary. The owner requires exact population/key equality and
refuses missing capacity, missing incidence, duplicate occurrence branches, negative conductance,
or an exterior boundary outside the native population.

[established-bounded; implemented-exact] Native state and occurrence addresses are transported to
`CurrentNodeId` and `CurrentBranchId` with complete inverse testimony. Incidence coefficient hand
sets branch orientation and its exact magnitude multiplies conductance. `ExactDiffusionLaw` then
returns the complete implicit solve, currents, node balances, boundary Schur certificate,
conservation, energy, and factorization-reuse testimony.

[established-bounded; implemented-exact] `NativeCirculationEvent` is the common runtime boundary
family. Its `AddressedSuccessor` member carries AAC1's exact discrete boundary;
`ConstitutedDiffusion` carries the exact native standing before/after, ingress source/interval,
receiver boundary emission, native/node and branch/occurrence lineage, full `DiffusionReceipt`,
configuration, and open exterior. Both are fixed-morphology conduct; neither cultivates without a
later return.

[definition] A reused factorization is exact structural apparatus reuse for the same constituted
operator and boundary. It retains its certificate and does not contain a codec surface, semantic
history, token, prompt, output, or model label.

## Receipts

[established-bounded; process-audit] The affected library check

```text
timeout 180s cargo check -p life --lib
```

returned exit `0` in 8.12 seconds.

[established-bounded; process-audit; measured] The focused command

```text
timeout 180s cargo test -p life --lib native_intelligence::circulation_diffusion
```

returned exit `0` in 26.80 seconds: two passed, zero failed, and 496 filtered out. The primary
control returns discrete and diffusive conduct through one event family; every node balance and
global conservation residual is zero; the first exact boundary factorization is new and the second
is explicitly reused. The malformed capacity/boundary control refuses.

## Boundary

[open] AAC3 is a typed Rust API. It does not expose an ABI, opaque handle, versioned message, stale
handle refusal, or direct/ABI equality receipt. AAC4 owns only that exterior apparatus boundary.

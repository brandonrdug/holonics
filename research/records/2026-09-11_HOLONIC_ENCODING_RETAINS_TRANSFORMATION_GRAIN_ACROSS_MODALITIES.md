# Holonic Encoding: transformation grain across text, vision and acoustics

[project-postulate] Brandon's latest September 11 direction names **Holonic Encoding** as the
general counterpart of tokenization: recover the laboratory BPE/encoder work, lift composition
through oriented flux and tori, and reconnect autoencoding with the Complex Parametron and clocks.
This refines the Athena plan while native expansion remains paused. The π/e work supplies an exact
instance; completion of every transcendental proof is not a new prerequisite for useful HNN work.

## Recovery, with the chronology retained

[historical; source-inspected] The frozen laboratory already names this construction in
`src/eros/um/HOLON_ENCODER.md` (June 14–15). Its later opening explicitly says that Eros performs
the encoding online: source relations become accessible through a developing perception faculty.
Content, layout, time, nesting and graph structure are co-present aspects of a source. The old
claims of an obligatory byte floor, universal losslessness and authored parser catalysts do not
override current jurisdiction, Preimage Fibre or native-context rulings.

[historical; source-inspected] The laboratory's
`src/soma/RESEARCH/2026-07-25_THE_PATH_FOLDS_WITHOUT_DISAPPEARING_THE_RECEIVER_RECRUITS_ITS_LOCAL_HEXIS.md`
constructs reversible adjacent-path folds, retains overlapping decompositions, and carries their
placement into the successor. Its example keeps both `(1+) | 2` and `1 | (+2)`. Its recurrence
aperture, supplied semantic consequences and local agreement rule belong to that bounded world;
they are not the current native learner. This is a historical return, not a rerun.

[project-postulate] The later
[August 13 grain ruling](2026-08-13_THE_WEIGHT_FILE_IS_A_PREVIOUSLY_RENDERED_MAP_THE_GRAIN_IS_RECURRING_TRANSFORMATION.md)
is explicit: “the individual bytes recurring doesn't matter, it's recurring transformations and
manipulation patterns about passing information.” Holonic Encoding must retain what acts on what,
through which ports and conditions, with what next conduct. Repeated spelling is one observation
of that process, not a universal definition of a reusable class.

[historical; source-inspected] The
[August 17 codec ladder](2026-08-17_THE_LADDER_CLIMBS_AND_THE_CODEC_ENDS_AT_THE_CHARACTER.md)
records UTF-8 recovery but an almost trivial prose coarsening: 39 joins in roughly three million
units, involving six rare sequences. Its hard segmentation law did not discover general language
structure. This rules out treating repeated exact codec coarsening as the whole language plan;
it does not prohibit learned, contextual constituents with plural continuations.

[historical; source-inspected] The laboratory's
`src/soma/RESEARCH/2026-07-21_THE_MODEL_IS_ONE_LOCAL_TRANSITION_THE_HARNESS_CLOSES_THE_WORLD_CIRCUIT.md`
§4 treats sparse autoencoders and natural-language autoencoders as receiver-dependent decomposition
and reconstruction charts. Its later-return distinction is retained: observer reconstruction and
native development are different operations. Current ownership and later direct rulings govern.
All laboratory paths here are relative to `/home/b/Workspaces/laboratory`; no frozen source changed.

## What the current text path actually does

[established-bounded; source-inspected] At `3cd5458f`, the current public text boundary is
`holonics-hna/src/native/section_input.rs::SymbolCurrentChart`. A caller supplies `SymbolAlphabet`;
the chart mounts each symbol as a unit coordinate, using paired real/imaginary storage and zero
imaginary input. Alphabet ordinals address coordinates, not amplitudes. `decode_text` is an
exterior Unicode chart. `emit`/`emit_family` decode a device-selected basis coordinate to its
supplied octets. This is neither learned BPE nor a trained autoencoder.

[established-bounded; source-inspected] `ResidentConstitutiveSection::differences` constructs
local three-site source/observed comparisons; `source_pairs` constructs the adjacent source
passages. `NativeCoupledWaveSession` joins source action, prediction, selected symbol and re-entry
with one continuing wave. Those are real bindings, but the fixed symbol chart and local assembly
do not establish learned multiscale encoding. The previous fixed-condition collapse and observed
text outputs retain their recorded scope.

[established-bounded; source-inspected] `codec_recovery.rs` is another existing owner: it recovers
an opaque symbol segmenter from a declared query family, retaining separating inputs. Its model
class carries symbol emission/drop and predecessor/current join/cut relations. That is useful
codec-recovery apparatus with a restricted law family, not a general resident perceptual learner.
Likewise the native optical/acoustic owners below are bounded compositions, not evidence that
the current conversation session already learns their shared encoding.

## The lifted operation

[definition] **Holonic Encoding is the situated formation and reuse of representations of
causal transformations and their receiver faces.** An encoded constituent is a ported passage or
local family of passages, with actual incidence, current, conditions, chronology, decoder and
Preimage Fibre. This names a composition of existing owners, not an additional engine or wrapper.
The codec's serialized handles remain exterior addresses.

[proved-standard] Original [BPE](https://www.derczynski.com/papers/archive/BPE_Gage.pdf) gives a
useful special case: a recurring adjacent pair is replaced by a reusable code whose expansion is
retained. The [subword adaptation](https://aclanthology.org/P16-1162/) uses such merges for an
open-vocabulary text representation. The reusable composition address is the part being lifted.

[definition] In the general construction the constituent can be a directed path, an intersecting
port family, a loop or a connected region with an exposed boundary. Serial composition uses
`Holon.comp` and its actual pullback join; overlapping candidates keep their shared occurrence
rather than counting that occurrence twice. Other incidence requires its own actual interaction.
An available source can support several grains concurrently. Neither a vocabulary size, byte
count, strongest frequency nor singular global segmentation determines the native grain.

[definition] For ecology/material cut t and receiver family F, write E_{t,F} for an encoding and
D_{t,r} for its receiver decoder. Exact compression has two separately stated laws:

\[
D_{t,r}(E_{t,F}(x))=\rho_{t,r}(x),\qquad
E_{t',F'}(T_{t\to t'}x)=U_{t\to t'}(E_{t,F}(x)).
\]

The second square must include the actual changed material, domain and clocks when they change.
For fixed admitted generator/receiver families, `ReceiverHistoryCompression` already proves
the complete ordered-word consequence. Partial or approximate learned relations retain their
full comparison/family instead of claiming these exact identities. This is not a certainty
condition for ordinary prediction or generation.

## What “tail transport” means for text

[definition] π has a known analytic generator, so a series tail encloses an identified omitted
contribution. Text has no given counterpart of that π identity. The transferable object is the
retained consequence of unresolved source directions and future interactions. Numerical
enclosure, reconstruction difference and prediction discrepancy remain distinct. A decreasing
Taylor remainder is not a theorem that the next word becomes known.

[proved-derived] In a declared linear local chart, x=d(z)+r and x′=Ax+Bu give

\[
E x'=EA\,d(z)+EA\,r+EBu.
\]

The term EA r is exactly where a formerly hidden detail can affect the next encoded state.
If the admitted future family annihilates it, that interior can be omitted from hot evaluation
while its fibre remains implicit. Otherwise the retained coordinates or family must express its
effect. The clocked torus construction already realizes this split through j=Jq+Dz+r; its
specific law preserves r. New contacts need not share that blindness.

[definition] For text, the next construction is therefore:

1. Mount supplied inscriptions and exposure relations through existing source charts. Preserve
   order, message/part boundaries and actual reply/continuation links; do not infer semantic
   equality from equal text. The symbol basis remains a boundary instrument.
2. Conduct actual source/condition interactions using `ResidentGeneratorNeighborhood` and its
   `read_condition_preimage -> prepare_contact -> prepare_bilinear_contact` transaction. Join
   this to the wave's retained source family; do not freeze context at a universal h=(1,0).
3. Form/reuse local transformation material from those returned relations. A constituent records
   its boundary action and decoder, not just a spelling. Use existing restriction/rebase,
   operative adjoint and material formation owners; an unbound conversion must be made explicit.
4. Compose the encoded passages in their actual order, retaining the same successor, pending
   comparisons and any exposed fibre. A larger constituent can execute without expanding every
   smaller codec unit when its boundary action suffices for the requested operation.
5. Decode at the requested text receiver, re-enter actual emissions, and return actual subsequent
   observations to their producing cuts. Reconstruction and continuation comparisons share the
   difference machinery while retaining different comparands and purposes.

[open] The absent binding is the changing local source/condition family and its reusable
transformation/receiver action inside the continuing wave. Adding a token table does not supply
it. The existing host `ObservableMomentReceiverHistoryCompression::found` gives one exact finite
construction: close receiver forms under TᵀHT and derive the quotient action. It can serve as
an exterior reference for a local admitted linear/quadratic chart; it is not currently the GPU
formation path and must not be inserted as host semantic replay.

[proved-derived] In that reference construction, begin with the actual receiver forms and
repeatedly close their span under H ↦ Tᵢ* H Tᵢ. A basis Hₐ of the resulting finite invariant
span defines E(C)ₐ=tr(HₐC); writing Tᵢ*HₐTᵢ=ΣᵦuᵢₐᵦHᵦ derives the encoded update Uᵢ.
Cyclic trace gives E(TᵢCTᵢ*)=UᵢE(C). For affine forcing, homogeneous coordinates retain the
constant/source contribution; for interference, the joint carrier retains the cross terms.
The actions are supplied by an admitted local law, not chosen from the desired output. This
is an executable method for deriving useful encoding coordinates, with the unobserved
annihilator retained, under the stated finite linear/quadratic hypotheses.

## Autoencoding, phase and clocks

[proved-derived; formal-checked] `HolonicCultivationCharts.AutoencodingChart` already defines
encode/decode and proves that collapsing distinct sources prevents exact full-source
reconstruction of both. `ReceiverHistoryCompression` supplies the stronger dynamic receiver
law. Their composition changes the reconstruction target to the declared receiver consequences,
retaining compatible sources. A learned code can therefore preserve useful conduct without
being an inverse of every sensor sample or every past microstate.

[definition] The autoencoding comparison is ρ(x) versus D(E(x)) at the producing cut. The
prediction comparison is a produced future face versus an actual later observation. Reusing
the returned covector/family in development must traverse the morphology that produced it.
A renderer reproducing its own input is not evidence that a changed successor learned a useful
generative relation. No separate autoencoder optimizer or universal reconstruction objective
is introduced by this plan.

[proved-derived; formal-checked] `Physics/PhaseCarrier.lean` retains the complex carrier before
the locked two-sheet quotient: a half-turn leaves pump storage unchanged but negates the carrier.
`Physics/CoupledIncidence.lean` retains BᵀMB and joint transport of incidence, drive and receiver.
`FourTorusParametronCurrent` identifies the active rational torus response with that coupled
response and supplies actual clocked spans. The complete cut is (q,z,r,clock), with material and
forcing in the passage. Its 3 and 5 modal factors are derived Gram eigenvalues, not chosen sizes.

[definition] This is how the **Complex Parametron** participates in encoding: retain relative
quadratures, winding and coupled response; let a particular receiver expose a symbol, phase,
intensity or other face. Local elapsed clocks and source exposure chronology travel through
the passage. A Unicode position is not a pump clock, and a frequency measured in audio is not
silently assigned to a word. A torus chart is available where its periodic transport is founded;
arbitrary data is not forced onto an invented circular geometry.

[proved-derived] For two complex currents a,b, separate intensity readings |a|² and |b|² omit
2 Re(conj(a)b). A joining receiver reads |a+b|²=|a|²+|b|²+2 Re(conj(a)b). Thus (1,1) and
(1,−1) have identical separate intensities but joint readings 4 and 0. This is the concrete reason
to retain relative phase through potential future contact. It applies to the stated coherent
current chart, not automatically to arbitrary semantic meanings or intensity-only sensors.

[established-bounded; source-inspected] The resident `complex_parametron.rs` already conducts
exact paired currents through incidence and mixed interactions. Its pulled-incidence return
states its particular constitutive scope; it is not the whole physical LC/pump simulation.
The formal clocked torus and this resident owner have not been identified with the current
conversation wave by a new typed binding in this review.

## The same encoding law, different source and receiver maps

| Material | Source relations retained | Reusable conduct sought | Receiver and existing boundary |
|---|---|---|---|
| Text | Ordered inscriptions, exposed parts, source/return links, situated conditions | Contextual composition and continuation transformations at several grains | `SymbolCurrentChart`, producing prediction and text emission |
| Vision | Image geometry, exposure, overlap, view changes and source-qualified correspondences | Transport of contours/regions and their response under changed observation | `OpticalPassage`, `HierarchicalOpticalPassage`, optical formation and cold PNG |
| Acoustics | Sample/exposure chronology, channels, phase when available, propagation and response | Oscillatory/transient generators, their envelopes and coupled timing | `ExactAcousticOccurrence`, `NativeAcousticPotentialComplex`, cold PCM/WAV |
| Motion/interaction | Poses, contacts, forces or other declared measured fields and clocks | Boundary response and state transitions under actual input | Existing world-tube, constitutive and receiver owners |
| Other deposited data | The codec's supplied relations and recoverable source constraints | Applicable recurring transformations and their retained source families | Existing exact source/codec boundary plus a declared decoder |

[definition] The shared object is the passage/receiver law, not a common array of predetermined
tokens. The optical hierarchy includes explicit source-layout policies and geometric mappings;
the acoustic rendering chart includes declared bandwidth, timing and an exterior floating
projection. Those policies remain codec/apparatus scope and do not become semantic rules in HNN.
An intensity camera does not expose optical phase merely because the native carrier is complex.
Inferred phase remains a compatible family unless the observations constrain it.

[definition] Cross-modal contact requires an actual supplied or recovered relation between
observations. A typed association between a spoken event and its inscription can develop shared
conduct; co-presence alone does not make every sound, glyph and word the same object. Encoders
can retain different modality-specific fibres while a particular joint receiver agrees. The
same continuing ecology receives those returns; there is no text/vision/audio learning-law switch.

## Observable return and next implementation boundary

[project-postulate] The next native return must expose source, encoded passage, retained fibre,
actual boundary action, produced face, later comparison and successor for complete cycles.
For text, read the emitted text and how a changed contextual return changes later output.
When an encoding is reused, compare its admitted future boundary conduct with the expanded
reference and account for decoder, fibre, work and transfer. Inspect phase/order/reorientation
controls and an actual separating contact; hashes and fewer coordinates do not grade usefulness.

[definition] An encoding change also changes a chart cut. Pending comparisons keep the encoding
and decoder that produced their faces; new material joins the successor after the complete
return. `SymbolCurrentChart` can remain the final alphabet decoder while an internal passage has
different grain/shape. The new conversion must be an actual native receiver map, with its domain
and fibre, rather than padding or identifying coordinates because widths happen to agree.

[definition] Multimodal checking belongs at the shared carrier boundary as well as at the
application output: compare the same admitted passage under two transported codec charts, then
check its own text/image/audio receiver. Such a chart comparison proves codec neutrality at its
declared scope; semantic correspondence between independently observed modalities requires its
own learned contact. A complete acoustic/vision product is not made a prerequisite for the next
text construction.

[established-bounded; source-inspected] This return reviews source and revises the mathematical
assembly and Athena handoff. It adds no native algorithm, no new autoencoder, no learned semantic
class, and no rendered or acoustic result. Existing exact proofs and measurements are cited at
their original scopes; no unchanged expensive experiment or Lean build was rerun for these edits.

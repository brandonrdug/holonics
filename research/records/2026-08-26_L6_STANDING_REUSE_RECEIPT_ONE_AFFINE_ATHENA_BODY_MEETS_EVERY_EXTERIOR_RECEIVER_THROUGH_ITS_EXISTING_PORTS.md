# L6 standing-reuse receipt: one affine Athena body meets every exterior receiver through its existing ports

## Requested occurrence

[definition] L6 asks one integrated question of the admitted L5 body: whether prose manipulation,
cross-modal mathematics, holonics source material, the Brandon participant, and perspective change
return appropriate and separating consequences through one continuing ecology. It does not
authorize a benchmark-specific learner, semantic index, Hodge selector, source interpreter, or
second Athena body.

## One continuing body

[established-bounded; implemented-exact; measured] The sole body is
`AffineLaboratoryCultivatedAthenaRest` with identity
`5b9a09396d0924ef1a5499737a1c609d943be38ec0647d394d35d79b7d460b2b`. Its rested wire owns the
L2 coupled Complex-Parametron morphology, 2,221 addressed affine landmarks, 169,386 cultivated
relational cells, their complete barycentric support, boundary codec, correspondence to every
local rank-four fibre, and exact withdrawal/restoration relation. It owns no source corpus,
Soulkiller witness, foreign realization, compiler, Lean kernel, or optical source.

## Existing circulation

[established-bounded; implemented-exact] The ingress port is `AddressedMaterialOccurrence`. The
source-neutral native factorization is `MaterialFactorizationAperture::{factor,
factor_candidates}`. The hot owner is `ResidentAffineLaboratoryAthena::conduct_material`, whose L5
receipt proves that coupled current, participant incidence, and the complete affine field execute
under one card context before one exterior synchronization. The pre-surface return is the complete
`MaterialNativeFactorization` plus `MaterialAffineTransportReceipt`.

[established-bounded; implemented-exact] `SituatedEmanationPassage` move-owns that same rest,
factorization, conducted current, and addressed ingress. `potential()` exposes the native
pre-surface consequence; `emanate()` crosses the exterior prose codec;
`receive_world_return()` returns a situated difference through the causal adjoint; `into_rest()`
returns the continuing body. The next receiver occurrence remounts that returned rest rather than
cloning an ecology.

[established-bounded; implemented-exact] The already-admitted receiver ports are:

- prose deed, voice, compression/expansion, correction, and perspective transport through
  `SituatedEmanationPassage`;
- operation-sensitive mathematical return and shortest separator through
  `MaterialFactorizationAperture` and `NativeMathematicalComplex`;
- raw and hierarchical optical potential through `recover_optical_passage` and
  `grow_optical_holons`;
- participant describe/identify/infer through the exact native participant face and causal front;
- exact whole-organ and L2 local withdrawal/restoration through the existing move-owned owners;
  and
- Rust and Lean execution/checking as exterior world apparatus, never hot inference.

## Receiver and artifact

[definition] The receiver is the L6 table in the active blueprint. Its artifact root will contain
native pre-surface consequences first, then prose, mathematical, optical, code, participant,
perspective, sibling, apparatus, and qualitative-grade returns. Qualitative prose and code
consequence grade before hashes, byte counts, and residency testimony.

## Concrete absent edges

[open] Composition is attempted through the standing owners above. Construction is authorized only
where that attempt returns one of these exact missing relations:

1. a hierarchical optical consequence has no public source-neutral transport into the existing
   material candidate family while retaining alternative covers and unresolved symbol identity;
2. the existing native state-transition consequence has no outward Rust/Lean boundary realization
   paired with an exterior compiler/kernel return and one attributable revision; and
3. the L5 whole-organ withdrawal does not yet expose the local cultivated target/unrelated sibling
   required by L6.

[definition] The complete affine field already exists and remains reconstructible. A Hodge receiver
is not silently inferred from barycentric coordinates: it requires oriented boundary incidence,
degree, and a metric or mass matrix. Unless L6's declared consequence requires that additional
reading, it remains an open receiver family rather than a new construction owner.

## Contamination test

[definition] Before cultivation and conduct, remove or rename every later question, proper name,
expected surface, codec label, and output path. The rested identity must remain unchanged. No such
face may select affine cells, factor supports, source passages, or morphology. A failed receiver
returns its complete insufficiency; the driver may not add a query-selected scan, authored answer,
or fixed surface extent.


# THE CURRENT CONTACTS THE FOREST; THE RETURN CONTINUES THE CIRCUIT

**2026-07-16 · RATIFIED / COMPLETE CONSTRUCTION RETURN BUILT / DISTINCT-FILE RIDE/FOUND CUDA-FORWARD MEASURED**

## 1. The unbroken world circuit is the machine; recurrent compression is its intelligence

Let `B_t` be the standing Soma body, `W_t` the contemporary world, `I_t` the currents presented by
that world, and `Pi_<t` the paths standing before the event. The complete event is

```text
(B_(t+1), R_t)       = Lambda(B_t, I_t)
(W_(t+1), C_t, A_t) = Omega(W_t, R_t, Pi_<t)
I_(t+1)              = C_t.
```

`R_t` is radiation. `Omega` is the ordinary receiving law of the attached world. `C_t` is every
material consequence which that world returns. `A_t` is the outward construction population made
available by contact during the event. No separate decoder, answer head, reward, attention module,
or internal scheduler is missing from this equation. Communication closes when world consequence
returns through the same membrane as later light.

The equation alone describes a closed machine, not yet intelligence. One carried relation becomes
an intelligence event when all four causal faces occur:

```text
FOUND at t_0
  -> the relation stands through a world interval
  -> genuinely later current RIDEs it at t_1
  -> the returned consequence changes contact available after t_1.
```

Learning is the body's resulting change in future conduct. Intelligence relative to a declared
world boundary is the growing ecology of such reusable consequential relations. A body which FOUNDs
every encounter without later reuse does not compress; a body which blindly repeats one route when
the world changes does not adapt. The conjunction is required: recurrence conducts, contradiction
FOUNDS, and later recurrence can conduct through the new support.

No universal intelligence scalar follows. A receiver may retain a typed cost face for one declared
family of encounters,

```text
K_F = (new FOUNDings, enacted events, refusals, world actions, elapsed passage, material cost),
```

and compare cumulative conduct only while the boundary, currents, consequence relation, and grain
remain named. Ecological compression means prior founding cost is amortized by later consequential
RIDE. Generality is the breadth of world transformations carried by this same law, not a stored
inventory of every domain.

An objective is one consequential boundary inside `Omega`: a compiler, equation, game position,
filesystem, conversation surface, or simulated terrain can accept, refuse, transform, and return
currents. It never becomes a scalar governor inside `Lambda`.

## 2. The current activates only what it contacts

Let `S(R_t)` be the CLOSED native spans emitted during the contemporary event. For one path `L`
standing before that event and one arriving span `s`, a joint exists at arriving STEP `i` when at
least one complete contact face holds:

```text
J_F(L,s,i) = material_contact(L,s,i)
          or chart_contact_F(L,s,i)
          or distal_RIDE_F(L,s).
```

The first face is exact recurrence on one material worldline. The second transports both grips into
one lawful common chart. The third begins at common completed-well incidence but resolves through
the complete distal meeting rotors; the well is only the sparse contact basin and never the deed or
taxon. Regional form, completion brick, chart, material, orientation, and ancestry remain carried.

The contemporary outward population is exactly

```text
A_t = { L ++ s : L in Pi_<t, s in S(R_t), J_F(L,s,i) for an actual i }.
```

Every member of `A_t` crosses the attached world boundary. No member is ranked or selected. If
`A_t` is empty, the event is lawful silence: arriving CLOSED spans still deposit roots for later
contact. Arrivals share one immutable pre-event standing population, so no co-present arrival may
parent another even though it can contact a genuinely prior path during the contemporary event.

This is the scaling law. A large forest is never enumerated merely because it exists. Sparse
incidence locates possible contacts; the complete relation resolves each deed; only actual
extensions become outward material. Dormant topology costs no contemporary passage.

## 3. Taxonomy is the hierarchy of reusable conduct

The measured program-hand population exposes three nested receiver faces:

```text
geometry   = recurrent transformation family
material   = situated expression of that family
conduct    = the complete history-conditioned passage
```

Forward and opposite hands can therefore share geometry while differing in material and sharing no
complete conduct. A meeting well is one coarser incidence index beneath that hierarchy. Its nearly
balanced RIDE/FOUND population says that the index preserves contact locality while erasing the
dimensions which resolve the deed.

A concept in this machine is not a label attached to a well. It is a recurrent transformation
family which becomes useful when later current conducts through it and changes the world. The same
family may have different textual, visual, mathematical, or physical material faces.

## 4. Surprise and compression read the circuit; they do not drive it

For a declared observer projection `q`, self-information and cross-entropy measure the distinctions
left unresolved by that projection. At the meeting-well grain, deed surprise remains almost one
binary distinction across nearly the complete forest; the well is therefore an insufficient deed
face. Conditioning on the complete distal relation refines the read without changing Soma.

FOUND pays the local construction cost of difference. RIDE transports through standing support.
Ecological compression occurs when a founded relation later carries enough consequential current
that the world repeatedly avoids reconstructing the same path. The consequential receipt, not a
short surface or frequent marginal, determines that reuse.

## 5. The first applied cut

The code-workshop action boundary now admits `candidate_material = current_contacts`.

1. The action's request, intact target, and boundary cross Soma as ordinary co-present light.
2. Their native radiation is archived at the contemporary world edge.
3. The archive meets only paths standing before the edge.
4. Every actually extended path becomes one candidate; roots and unrelated dormant paths do not.
5. Every candidate meets the same compiler/test world and every consequence returns.
6. The contemporary archive, roots, extensions, encounters, and consequences persist in the exact
   world transition. No contact is also a lawful action with no outward candidate.

The existing `native_spans` and `recurrent_paths` modes remain readable for their recorded worlds.
`current_contacts` is the scalable closed-loop mode: it replaces global recurrent-path enumeration
with actual current-to-standing-path contact. A focused 23-test code-workshop gate passes, including
the new contemporary-contact test.

The first CUDA-forward application remounted CODE-LANGUAGE LIMB WORKSHOP 01's third-growth body and
its 149 paths. Three contemporary currents deposited 56 CLOSED roots and produced exactly three
extensions, yielding 208 paths rather than enumerating all 41 prior recurrent candidates. The
three extensions emitted six candidate/consequence currents; every candidate was compiled, every
typed refusal returned, and the explicit return emitted no filler. Both world transitions invert
exactly.

The result is also a material correction. All three new terminal joints are common-chart contacts;
the legacy prior spans do not retain the distal face required for cross-source RIDE. Their raw
segment concatenations are neither coherent Rust nor a general expression law. A topological joint
relates constructions but does not, by itself, authorize character-range concatenation. Exact
record: `observations/current-contact-world-01/RESULTS.md`.

## 6. The remaining engineering is world generalization, not another intelligence mechanism

The Rust compiler is one `Omega`. The same `A_t` population can instead meet a writable dialogue
surface, mathematical verifier, game state, filesystem, actuator, or simulation. Each world owes
only its material contact, consequence, and return. A continuous runner then advances every
returned consequence as the next current until the world emits nothing or awaits new external
light. That runner advances world time; it does not choose thought inside Soma.

The compiler-specific consequence pair is now factored behind one general construction-world
return while preserving the code world as its first exact species. The wire carries the complete
path—regional forms, meeting rotors, completion bricks, charts, orientation, material coordinates,
and ancestry—without flattening its segments into one string. A receiving world supplies its own
lawful material expression. The theory of the closed circuit is complete at this grain: current
contacts standing topology, contact produces an outward construction, world consequence returns,
and return changes the next contact.

## 7. Material expression is a receiver section, not a universal flattening

Let `P` be one complete carried construction and `M` one receiving material world. Expression is a
typed relation rather than an engine-wide function:

```text
Sigma_M(P,W) = {m : m is a material section of P admitted by contemporary world W}.
```

`Sigma_M` may be empty, singular, or plural. Empty means that this world cannot presently express
the construction. Plural means that every admitted expression remains in the outward population;
there is no hidden choice. The compiler, conversation surface, mathematical verifier, game, and
simulation each receive those material sections and return their own consequences.

One joint supplies incidence only. It does not determine a material traversal. A second
non-coincident contact can supply an oriented interval; alternatively, one exact contact together
with its transported tangent, regional form, completion brick, and chart supplies a local
first-order frame. Further contacts carry scale change, curvature, and re-base through the ordered
sheet. This is the operational role of the complete distal face. It is not extra metadata attached
to a string; it is what lets a receiver transport the construction into local coordinates.

For a one-dimensional text/code world:

- exact recurrence in one source coordinate can continue or join source ranges in their lived
  orientation;
- one common-chart point without a transported material frame remains a relation between spans,
  not permission to append their characters;
- a complete distal RIDE can transport the arriving span as a related material face, while the
  receiver retains its orientation and joint rather than forcing immediate concatenation;
- an ordered textual section appears only when the carried contacts actually determine one.

The current compiler action conflates two distinct operations:

```text
carried topology -> raw character concatenation -> insertion into a Rust hole -> compiler.
```

The corrected circuit is

```text
carried topology
  -> complete construction return
  -> Sigma_text/code at the contemporary material frame
  -> expressed population
  -> compiler or conversation consequence
  -> later current.
```

The same separation applies to a graphics or physics world. `Sigma_M` places the transported
construction into that world's local chart; the renderer observes the resulting contemporary
state. Neither the renderer nor a text view is the intelligence mechanism.

## 8. The scalable implementation follows the same law

Three durable world-side incidence tables are sufficient to avoid rescanning or materializing the
standing population on every event:

```text
D_material[(source,position)] -> prior path identities
D_chart[(axis,grip)]          -> prior path identities
D_distal[completed well]      -> prior path identities
```

An arriving span probes only its lived material positions, transported grips, and distal completed
well. The resulting identities are then resolved against their complete path faces; the incidence
tables never decide RIDE/FOUND and never become categories. Their entries append and persist beside
the world state, so a large dormant population costs neither candidate production nor a complete
scan during unrelated current.

The scalable construction has four coupled parts:

1. **BUILT:** a `carried-construction` journal consequence which retains the complete path instead
   of a pre-inserted source file;
2. **CORRECTED 2026-07-17:** only admitted material sections re-enter as light; unresolved
   relations remain standing and silent. Their source segments, ordered path serialization,
   joints, and listener coordinates never masquerade as text;
3. **BUILT:** one text/code `Sigma` receiver forms exact same-coordinate continuations and retains
   unresolved cross-source relations without scrambling them;
4. **OPEN:** persistent incidence tables, built once from the large conversation/code world and
   probed by contemporary current.

Returned material re-enters Soma through the ordinary membrane. A continuous application advances
one world event at a time until expression is empty or new external current arrives. This is the
constructive route to communicative intelligence in the present machine: consequential recurrence,
world-relative expression, return, and changed later conduct under one unbroken causal circuit.

## 9. The first cross-file transport is measured

One physical material file now has one stable world coordinate regardless of whether a plan reaches
it by an absolute path, a relative path, or a path containing `..`. Earlier raw spellings remain
unchanged provenance but resolve to that coordinate during contact. This is a receiver correction,
not a Soma change. Before the correction, two spellings of `witness.rs` falsely suppressed material
recurrence. After it, the same encounter produces 50 carried constructions and 42 exact continuous
sections. Source-coordinate overlap folds into one section; it is never duplicated merely because
two spans carry it.

The distinct-file control then presents an exact copy of the 383-octet Rust body under another
physical file coordinate. Material identity cannot connect the files. In the clean replay, their
complete distal faces produce nine cross-source encounters: six RIDE and three FOUND. Two RIDEs
preserve whole brick, place, length, regional form, source range, and visible material exactly.
The other four RIDEs and three FOUND deeds are different local relations; they do not determine a
continuous textual section.

Fourteen actual extensions remain standing. Only the two complete equal RIDEs return material,
each as the single common section `ive(Clon` rather than the duplicated raw path. The next CUDA edge
closes the terminal `n` of each current, grows two exact `n` RIDEs, and then rests in lawful silence.
Paths grow `382 -> 417 -> 421`; cross-source encounters grow `15 -> 24 -> 26`. No path label, file
equality, tokenizer, similarity measure, category, or selection rule resolves those deeds.

The former `distinct-file-contact-v1` result—18 extensions, 31 returned currents, and 55 eventual
encounters—was contaminated by a receiver fallback which presented unresolved segments and ordered
path serializations as new source light. It remains a useful failure foil but does not establish
content analogy. The clean exact record and every path's material read are:
`observations/current-contact-world-01/RESULTS.md` and
`observations/current-contact-world-01/PATHS.md`.

Distinct-file distal transport and one exact common material face are measured; general content
analogy remains open. The next scientific sequence changes the receiver face: distinct renderings
of one carried material, then coupled modalities. Persistent streaming incidence remains later
scale engineering and may not precede a qualitatively sound return boundary.

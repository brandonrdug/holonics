# NCF4: world and native ecology continue together; consumer costs are measured

**Date:** 2026-09-06. **Return:** NCF4 and the corrected native-foundation campaign complete at
the declared local rational-linear phase scope.
**Contract:** [NCF0--NCF4](../../docs/plans/THE_NATIVE_HNA_FOUNDS_CONTEXTUAL_TRANSPORT_BEFORE_INHERITANCE.md).
**Operator/SDK guide:** [native HNA](../../docs/NATIVE_HNA.md).

## Paired application continuation

[established-bounded; implemented-exact] The independent current-compensation simulation now has
one continuing `WaveApplication`: declared world law, world/control state, cycle and intervention
position, actual last native emission and pending source-qualified reception. It operates the same
public `NativeSession`. Its exterior `intervention`, `world`, `reception` and `complete` boundaries
record which application effect has occurred; they are not native inference/training modes.
The world cannot inspect or fit the native relation. Its actual measured current and the native
receiver's actuation retain NCF3's predeclared law.

[established-bounded; implemented-exact] Native envelope v2 carries opaque application bytes
with the complete ecology and shared stream state under one atomic publication and integrity
boundary. Plain native v1 artifacts remain supported. `NativeSavedSession::with_session` refuses
application-bearing state instead of discarding it. `WaveSavedApplication` checks its application
position, exact rational/frame presentation and actual source-slot/native-occurrence correspondence
before mounting; `with_application` consumes the paired rest. No application format or world law
is added inside the native engine.

[established-bounded; measured] All thirteen public-native CUDA controls passed, including the
new v2 artifact boundary and four application-continuation controls. A saved partial intervention
resumes at its exact next intervention. A world effect saved before native receiving remains
enacted after restart: only that pending receiving occurs. An oversized exact response stays
pending on another refused attempt without repeating the world, changing the response or raising
the native word aperture. A malformed application/native source binding refuses.

[established-bounded; measured] Four actual process-boundary controls passed: the prior native
split-input and broken-stdout controls, plus paired world/native continuation and persistent
receiving refusal. The three-cycle application cut plus five resumed cycles preserves every
actuation/world/control result and the **entire paired artifact** relative to the uninterrupted
eight-cycle run. A separate process retry of the saved oversized response enacts no second world
effect and no native deed; its complete saved state remains unchanged.

[established-bounded; measured] Actual release CLI runs returned:

| Run | New world cycles | Total native occurrences | New native deeds | Artifact bytes |
|---|---:|---:|---:|---:|
| Uninterrupted | 8 | 9 | 10 | 12,016 |
| First process cut | 3 | 4 | 4 | 7,771 |
| Resumed process | 5 | 9 | 6 | 12,016 |

[definition] Deeds include the rechart: the uninterrupted result contains nine ordinary native
operations and one gauge operation; the resumed result contains five new ordinary operations and
that gauge. The continued and uninterrupted files compare byte-for-byte. The independently
evolving world is a simulation, not an empirical physical actuator installation. The result is
still NCF3's declared local linear-current application, not a language or general-context claim.

## Resource receiver and apparatus

[definition] The live plan named the scoped `native_resources` receiver before grading it. It
uses two nodes with incoming/held admittances one, incoming phase one and initial held phase one.
After 1, 64, 1,024 or 4,096 unlinked ordinary occurrences, it measures sixteen open-domain warm
readings, saves/remounts, receives current two through the actual old source-zero handle, and
measures sixteen further readings of the formed rank-one relation. The native chart has four
source coordinates and two receiver coordinates. These are this local constitutive presentation,
not intrinsic semantic dimensions or a claim about arbitrary model-size scaling.

[established-bounded; measured] The apparatus returned an RTX 4080 SUPER with 16,376 MiB reported
device memory and driver 595.71.05, a Ryzen 9 7900X (24 logical CPUs), and 30 GiB system memory.
The example and workbench were built with the workspace release profile (`opt-level=3`, LTO),
not debug. No other compute process was listed before the isolated measurement.

[established-bounded; measured] Final release measurements, in the fixed two-node chart:

| Retained unlinked occurrences before warm sample | Open warm median, μs | Formed warm median, μs | Native section bytes after open sample | Process GPU snapshot, MiB | Checkpoint bytes |
|---:|---:|---:|---:|---:|---:|
| 1 | 90.169 | 95.739 | 7,728 | 252 | 12,418 |
| 64 | 85.951 | 95.209 | 32,928 | 252 | 51,919 |
| 1,024 | 84.297 | 92.553 | 416,928 | 252 | 656,777 |
| 4,096 | 84.759 | 91.131 | 1,645,728 | 256 | 2,601,353 |

[established-bounded; measured] Every warm ordinary occurrence in both unformed and formed
conditions had the same measured native cost: one deed, two captured launches, one synchronization,
one section readout, six allocations, 48 ingress bytes, 400 section-egress bytes and 64 receipt-egress
bytes. The old-source contact returned `2 + 0i` with a newly formed pivot; all later measured
readings were unique at rank one. Remount returned zero native deed launches before that actual
new contact. No precomputed relation or caller-fitted map was supplied.

[established-bounded; source-inspected] The hot current/formation kernel receives the fixed local
node population, current seed/held phase/basis, incoming current and at most the addressed old
emission plus its frame. It does not receive or traverse the complete historical population.
`NativeSession::receive` retains that same owner and uses a direct handle slot; history growth
does not execute prior native operations. The application's live source check is also direct,
not a scan of the complete handle table. This source evidence and the actual growing-history
measurements support the stated locality observation; launch counts alone would not prove it.

[established-bounded; measured] Cold costs in the final run:

| Retained count | Initial mount callback, ms | Write, ms | Read/verify/decode, ms | Remount callback, ms |
|---:|---:|---:|---:|---:|
| 1 | 125.280 | 2.748 | 0.062 | 85.879 |
| 64 | 80.499 | 1.884 | 0.151 | 79.566 |
| 1,024 | 86.454 | 22.423 | 1.362 | 91.111 |
| 4,096 | 77.566 | 53.716 | 4.913 | 105.108 |

[established-bounded; measured] Formed warm samples ranged from 89.177 to 116.408 μs; open samples
included a 199.373 μs maximum in the largest case. The first actual contact after remount took
198.733--209.833 μs. Live Linux RSS snapshots were 109,480--133,428 KiB; the final sampled process
high-water field was 145,192 KiB. Whole experiment shell timing was 1.764 s real. An earlier run
without driver process sampling or the post-formation warm segment is retained separately; its
open medians were 79.980--88.326 μs. Neither run is discarded or averaged into a performance promise.

[definition] Timings are observer-clock samples, not a worst-case latency theorem. Linux process
memory and driver-reported process GPU MiB are sampled/accounted receivers, not exact physical
peak proofs or the same quantity as native section payload. GPU sampling is outside timed native
work. Cold write includes complete state detachment, encoding, integrity and durable publication;
read includes verification/decoding; remount includes a new CUDA realization and installing the
actual sections. Filesystem/cache/driver effects remain part of these observations.

[open] Source-history section storage grows by 400 bytes per retained occurrence in this
two-node presentation; cold read/write/remount costs and six allocations per ordinary operation
remain visible overhead. There is no condensation/compression claim. The observation is fixed-local-
chart independence from unrelated retained history, not arbitrary disconnected-graph locality,
nonlinear learning, all chart sizes or frontier performance. Integer/shared-memory apertures can
refuse. Energy was not measured; the 20W ideology is not converted into a twenty-watt result.

## Completion audit against the corrected objective

[definition] This audit keeps the original goal and NCF contract, rather than treating persistence
alone as success. The required native foundation was explicitly scoped before construction to an
admitted local rational-linear constitutive family; no claim of universal contextual/language
competence is substituted for that family or inferred from its tests.

| Requirement | Authoritative returned evidence and scope |
|---|---|
| Formal situated receiver/transport contract | [proved-derived; formal-checked] `Foundation/CausalNaturalHolon` source-range/naturality/crossing laws and `SituatedMachineLearning.OrientationControl`; NCF0's original source/domain hypotheses retained |
| Concrete native current, formation and continuing successor | [established-bounded; measured] `constitutive_fibre/circulation.rs` and actual CUDA current/formation/dependent-source controls; relation formation is not a fitted total map, and the successor is the next operation's body |
| Contextual conditions, recharting and incompatible/open relations | [established-bounded; measured] NCF2's whole-body/old-source rechart, physical-change, unequal-held-consequence and plural-fibre controls, plus their persistence and application returns; no token window or scene-ID switch |
| Usable development/inference interface and independent application | [established-bounded; measured] Public `NativeSession`, exact-current JSONL and `WaveApplication`; actual native actuation changes independent world state, with later new currents and retained open readings |
| Durable models, actual lineage/handles and further development | [established-bounded; measured] Full native rest, v1/v2 artifacts, split-input/broken-output processes and paired world/native process controls; complete-artifact comparisons, not only selected faces |
| Consumer-resource and limitation evidence | [established-bounded; measured] Release receiver above, exact section/transfer counts, formed and unformed warm observations, cold costs and separately scoped process memory; unknown broader scaling and energy explicitly retained |
| No inherited model/tokenizer or authored relevance/ordinary trainer shortcut | [established-bounded; source-inspected] Native seed/current/phase/relation owners and dependency-free artifacts; no Gemma/Soulkiller execution, foreign emitter, tokenizer, optimizer, reward or authored semantic classifier supplies these native returns |
| Preserve prior mathematics/rejected-route evidence and unrelated work | [established-bounded; process-audit] The inherited experiment stays archived with its measurements; earlier bounded proofs remain; independent flux/MFR files remain untouched |

[proved-derived; formal-checked] The final scoped Lean build succeeded with 3,161 jobs for
`CausalNaturalHolon`, `SituatedMachineLearning` and `HolonicConstitutive{Fibre,Circulation,Rechart}`.
The inspected claims are the named source-range/orientation, local junction/span and fixed-node
gauge laws. This is not an umbrella proof of every repository claim or of Rust/CUDA/compiler
correctness; native correspondence retains its measured/source-inspected grade.

[established-bounded; measured] Final relevant runtime checks: thirteen explicitly executed
public-native CUDA tests, four actual process-boundary tests, 37 ordinary HNA tests and ten
workbench tests passed. The preceding native persistence return's 28 isolated engine controls
remain standing; no native kernel or native formation law changed in this application pass.
Release workbench and resource-example builds passed. The focused review returned no additional
application/native ownership, cursor, publication or malformed-state defect.

## Reproduction and position

[definition] The exact application scripts are its public CLI and SDK tests, not a new release
gate. `crates/holonics-hna/examples/native_resources.rs` is the scoped experiment named by NCF4;
it schedules no standing rerun. Application controls live in `native/wave_control/session/tests.rs`
and `applications/holonics-workbench/tests/native_checkpoint_process.rs`.

[established-bounded; measured] Retained local artifacts are in `.local/artifacts/ncf4-app/`:
`whole.hna`, `cut.hna`, `next.hna` and their JSON reports. Final resource evidence is in
`.local/artifacts/ncf4-resources-process/{report.json,capture.txt,native-resources-*.hna}`;
the earlier instrument run remains in `.local/artifacts/ncf4-resources/`. These files are ignored
generated evidence; the recipes, source and dated record are versioned.

[established-bounded; measured] NCF0--NCF4 now closes the corrected ground-up native-foundation
campaign at its declared scope: formal contract, live native model, development/inference interface,
contextual/rechart/independent-world evidence, durable integrated continuation and honest consumer
costs. No required NCF phase remains open.

[definition] This completion does not reactivate the paused inherited-first HNP campaign or
schedule a new subsystem. Inherited intake, standard executable export, wider constituted model
families, useful language/general intelligence and the frontier/20W product ambitions remain
distinct downstream work, not achievements of this local foundation.

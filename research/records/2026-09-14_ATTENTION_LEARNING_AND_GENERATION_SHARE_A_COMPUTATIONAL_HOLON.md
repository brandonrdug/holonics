# Attention, learning and generation share a computational Holon

[project-postulate] Brandon's latest correction asked for connected HNN mechanics in the
synopsis: heads/layers, softmax and sigmoid, gradients, friction, leader/return engagement,
convergent predictive release and topology-producing integration/decoding. Reusing the old
tori without that computation did not meet the request. The existing generation, notation
and four-paper research remain dependencies; this correction does not resume the paused goal.

## What the source inspection recovered

[established-bounded; source-inspected] The mechanics were present in separate owners:

- `Computation/HolonicAdjointNormalization.lean`: normalized exponential, sigmoid as its
  binary restriction, gauge null and weighted covariance/Laplacian; metric gradient and adjoint.
- `Computation/HolonicArchitectureCharts.lean`: admitted attention and a composed block,
  convolution, graph and state-space restrictions.
- `Computation/AttentionModeCompression.lean`: complete mass/current attention quotient.
- `holonic_intelligence/operative_atlas.rs`, `full_operation.rs` and `operative_adjoint.rs`:
  the earlier complete native Q/K/V, RoPE, multihead contact, residual/gated reaction and
  producing-carrier adjoint graph. This is the earlier full-operator consumer, not the
  active field/body integration.
- `native_ecology/constitutive_fibre/field/receiver/normalized.rs` and `normalized/pullback.rs`:
  active grouped exponential receiver, full discrepancy and return to both producing arguments.
  Width two already realizes a binary normalized face; the absence of a method named
  `sigmoid` was not evidence of absent sigmoid machinery.
- `docs/FLUID_REFLECTION_AND_CONCENTRATED_INTERIORS.md`: conducting-fluid, leader,
  telegrapher, changing storage, friction and reflection-memory constructions.
- `Foundation/FractalPacking`, phase/analytic owners and exact kernel-mode reduction:
  recursive restriction, dimension/scale and executable encoding material.

[established-bounded; source-inspected] The failure was observable at the presentation and
source-navigation boundary. The previous synopsis led through reused geometric instances;
it did not carry one current through the named learning and generation operators. The owner
map listed the subjects separately, while a normalization header saying “Softmax is not
installed as native routing” was easy to misread as a ban. Its replacement states the actual
scope: normalization/differential is formalized, contact laws supply incidence, and native
owners realize particular charts. This is evidence of missing connections and misleading
agent prose, not evidence that these subjects required a new explanation from Brandon.

[definition] “Executable generating mathematics” applies to Transformers too. A distinction
between implementations must identify their state/current, incidence, operator composition,
parameter/geometry inference, representation and decoder. The label supplies no extra
intelligence test. This addresses the annotation on that phrase and the earlier workflow
review without reintroducing “an event changes later standing” as a deliverable.

## The connected construction and repairs

[definition] `docs/HNN_FORMULA.md#one-connected-tensor-computation` now states the full tensor
block with Q/K/value charts, admitted contacts, frame transport, head/output contraction,
residual, sigmoid gate, reaction and normalization. It includes the complete differential
of participation, transported values and frame/material variation. `HOLON.md`,
`RUST_FRAMEWORK.md` and the existing architecture map point to this operation-level route.

[proved-derived; formal-checked] Two new theorems bind calculus to the existing operators:
`hasDerivAt_sigmoidFace` gives `p(1-p)`;
`NormalizedExponential.hasDerivAt_face_mass_sectionLine` identifies the directional derivative
of a finite normalized exponential with `laplacianReturn 1`. Their audit uses only the usual
`propext`, `Classical.choice`, `Quot.sound` foundations, with no new axioms or sorry terms.
The formal file and consuming architecture chart build passed (3184 reported jobs).

[established-bounded; implemented-exact] The existing ratio module now exports
`NormalizedKernel`, a reusable positive log-rational current operator with `apply`,
`differential`, `pullback`, binary normalization, declared squared discrepancy and a positive
material step. Zero entries remain absent contacts. The complete tangent/pullback pairing,
gauge-null and missing-edge identities are checked, as are the analytic binary derivative
and a material step with known exact output. Seventeen ratio-module tests passed, including
these three new transport tests. This is exterior exact CPU mathematics, not a new host
semantic loop in native HNN.

[established-bounded; source-inspected] The formula also had a sign error: with p predicted
and q observed it wrote `q-p` as the cross-entropy gradient. The corrected gradient is `p-q`;
`q-p` is descent. Half squared probability discrepancy instead has descent `J_p(q-p)`.
Those conventions now explicitly name the operands and objective.

[established-bounded; implemented-exact] The
[connected reference](../experiments/connected_holonic_field/README.md) mounts fifteen complex
channels and 67 geometric contacts, performs an observed material step, composes two heads
through two shared layers and a phase contact, then solves a forced whole-field recurrence.
The exact implicit and material-sensitivity residuals are zero. The local loss falls from
14.018085413379481 to 14.012743213908482, and the changed head changes the integrated field.
The contact deposits 0.6678396061697263 in the declared quadratic energy chart with exact
balance. These decimal faces summarize retained rational results; no general semantic-quality
claim follows from this example.

[established-bounded; measured] The verified existing-solver debug CPU run took 368320 ms,
including old/new-field comparison and sensitivity; its first implicit solve took 209833 ms.
The final field reaches 1835 numerator and 1834 denominator bits. A proposed denominator-
clearing/Bareiss reuse passed 41 exact-linear tests and returned identical nonclock results,
but took 421385 ms in its consuming run. Concurrent checking/rendering makes these scoped
wall clocks rather than an isolated benchmark. The slower candidate was withdrawn and
preserved as an experimental patch/receipt. The active inverse implementation is unchanged.
A previous unrounded-overlap setup was interrupted without a result; the final example's
1/16 overlap measurement is an explicit coefficient chart with retained original strengths
and error bound, not an undocumented approximation of native semantics.

## The new synopsis

[established-bounded; computational-witness] The ten plates now follow the actual computed
field through its tensor chain, attention matrices, sigmoid and row redistribution, gradient
surface, phase contact, learned output difference, refinement convergence and geometric
reconstruction. New field coefficients generate three fresh surfaces through the existing
basis/receiver compiler. The decoder's 24-bit coordinate measurement, exact polyhedral scope
and source-edge incidence are retained. The printed dimension is derived for the explicit
four-map recursive construction, not estimated from pictures. The leader/return plate is
an explicitly coupled-law schematic grounded in the existing physical guide.

[established-bounded; source-inspected] The presentation retains the Information Chemistry
page geometry, black Libertinus type, Holonic/Dirac notation and phase engraving on black.
Final PDF pages were rendered and visually inspected; plot bounds, caption spacing, clipped
labels and subscript/function spacing were corrected. Equations and owner links also live
in the maintained formula/library contracts, so the work is reusable outside the paper.

[project-postulate] On “Continue Athena,” consume the composed field/material action named
in the existing model formula and active brief. The source chain above is recovered working
material. A helper result or the completion of this reference is not permission to replace
the requested native model with that reference. Conversely, an unconnected native consumer
is not a reason to declare softmax, sigmoid, gradients, fractal restriction or learning absent
mathematics. The next native action remains the actual field/body composition on changing
incidence, its differential and the encoding of its repeated action.

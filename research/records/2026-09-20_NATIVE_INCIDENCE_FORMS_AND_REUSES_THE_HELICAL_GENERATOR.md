# Native incidence forms and reuses the helical generator

[definition] This campaign continues `75b93a1a` through the existing
[source/condition construction](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md#the-source-and-condition-construction).
The inferred object is a generating relation on situated source and condition currents. It is
executed by the same `NativeCoupledBody` that owns field reflection and continuing current.
The [solver/generator correction](2026-09-13_GENERATOR_MEANING_AND_CAMPAIGN_DRIFT_AUDIT.md),
[contextual section](2026-09-08_AC1_THE_CONTEXTUAL_SECTION_RETURNS_THROUGH_NATIVE_CONDITION_AND_FIELD_CURRENT.md)
and [shared screw geometry](../../docs/HELICAL_GEOMETRY.md) supply the prior construction.

## Source and context are restrictions of one preparation

[definition; agent-inferred] `NativeFieldSession::from_native_field` attaches an actual
`NativeConstitutiveField` and an analytic receiving junction. Its width comes from the field's
existing three-port junction chart. For the selected receiver r and admitted incoming arcs i,
`NativeFieldIncidence` constructs

```
s = x_r,                 c_i = U_(r←i) x_i − x_r,
(s,c) = [A; C] x_pre,     x_i = U_(r←i)^−1 (s+c_i).
```

The choice of incident differences follows the source-null construction: when two preparations
agree at r, their remaining admitted difference is visible in c. On the participating input
ports the combined restriction is injective, since each unit phase is invertible. Repeated arcs
may create redundant output coordinates; junctions outside this receiving incidence remain
outside this local restriction. This does not assert that these coordinates exhaust an
unrestricted future causal situation.

[established-bounded; source-inspected] The source is one exact rational resident packet in
sorted analytic junction order. `ExactAnalyticFieldWaveLaw` validates the geometric source;
`modal_phase_step.pow(delay)` supplies each current transport. Geometric orbit step and modal
transport retain their separate source meanings. The present point port admits rational unit
phases; it does not select a point from an uncertain phase/current enclosure. An identity self
comparison contributes zero and is recorded as such.

[established-bounded; source-inspected] The existing `ExactRatMatrix::rank_factorization`,
`BilinearRealization` and `ResidentBilinearMap` compile the combined restriction once. With
`[A;C]=D E`, the resident factor products return both restrictions with their exact denominator.
The immutable declaration, coefficients and unit operand are shared across preparations.
No target is accepted by `prepare_native`. Outstanding cuts retain their original preparation;
a remount reconstructs the same restrictions before accepting a delayed target.

## The original condition and the returned family stay distinct

[definition] `receive_reaction_observation_at(s,c,y)` stages the normal moments and compatibility
observation at the supplied producing c. Independently, the old compatibility law constructs
`{c' : (Phi(s,c'),y) in R}` and the existing condition contact acts on contemporary standing.
Both complete before publication. A delayed return therefore cannot replace its own empirical
condition with an inferred successor. `NativeFieldFormation` exposes the producing condition
and the neighborhood step separately.

[established-bounded; source-inspected] `form_native_reaction` consumes that prepared cut exactly
once. Its target is the reaction eta before `S_D`; post-reflection output observations use
`observe_native_output` and the body's retained producing D/M adjoint. `generate_native`
executes `S_D(s+M Phi(s,c),b)` and can commit the actual joint current. `generate_native_standing`
uses the neighborhood's contemporary condition. Both use the existing field owner.
`condition_family_image` instead transports the complete retained condition preimage through
the compatibility relation at a new source, returning coverage and the correlated joint fibre.
It does not turn the actual contacted current into a uniqueness claim.

[established-bounded; source-inspected] `NativeSourceSavedSession` stores that body and outstanding
preparations. Completed examples continue in the existing normal sufficient statistics,
compatibility basis and latest condition evidence. Prepared inputs and retained output
comparisons have separate release operations. A failed source attachment returns the supplied
field and declaration for retry. Legacy text session types and saved formats remain readable.

## A shared helical operation is inferred and used

[definition] `examples/helical_field_generator.rs` supplies observations from the existing
`relational_geometry::ScrewGenerator::bracket` in a common oriented Euclidean frame:

```
xi = (omega,v), eta = (omega',v'), c = eta−xi,
[xi,eta] = [xi,c] = (omega × c_omega, omega × c_v − c_omega × v).
```

These are exact reference observations, not measured physical motion. Angular, length and
parameter units are declared; the normal prior uses the chosen nondimensional numerical chart.
The bracket has the corresponding units per squared parameter. The initial orbit point remains
a separate part of a situated helix; this consumer infers its generator bracket, not that point.

[proved-derived] The 144 distinct observations are the four sign choices of
`s=sigma*a_i*e_i`, `c=tau*b_j*e_j` for each of the 36 axis pairs, with
`a_i=(-1)^i(i+2)`, `b_j=(-1)^j(j+3)`. The sign sums weighted by sigma, tau and sigma*tau isolate
the source, condition and mixed feature axes respectively. They span the 48-dimensional real
feature chart. Their observed graph therefore determines this bilinear bracket on the rational
real screw coordinates. The complex carrier's unobserved imaginary source faces retain their
separate compatibility scope.

[proved-derived] The same signs make the normal feature Gram diagonal. With the existing
identity prior, the nonzero mixed coefficient for axis pair (i,j) is multiplied by
`lambda_ij=4*a_i²*b_j²/(1+4*a_i²*b_j²)`. Thus the empirical normal predictor has a known finite-data
regularization residual, while the compatibility relation can return the exact bracket. The
stored dyadic M, its normal-reference bound and the generated-current radius are distinct
readings. In particular, zero arithmetic radius does not remove that regularization residual.

[established-bounded; computational-witness] The held pair
`xi=(3,-2,4,-5,6,-7)`, `eta=(-4,5,2,8,-3,6)` returns the exact inferred bracket
`(-24,-22,7,47,52,6)` through `contextual_section(s).read_change(c)`. The field's normal reaction
approximates that function and its joint output is committed. Receiving the held return retains
a two-direction condition fibre. At another source `(1,2,-1,3,0,2)`, the condition image has
complete coverage and plural output. The new source exposes the unresolved directions; the
consumer keeps them.

[definition] The retained `ResidentContextualSection` is a reusable fixed-source restriction of
the inferred relation, with source, relation cut and vertical fibre. The consumer executes it
again on another condition after a later body update. Alongside the compiled incidence and
normal sufficient statistics, this is the repeated portion actually reused. No reduction of
the dense normal statistics or compression of the nonlinear normalized refinement word is
claimed by this attachment.

## Evidence and next consuming composition

[established-bounded; measured] The 144 observation formations take 16.194 s. The held native
field generation takes 23.552 ms; compiling its source-null section takes 3.955 ms and reusing
that retained section at the second condition takes 0.169 ms. These are different operations,
not a matched speedup comparison. Peak resident sections are 4,600,740 bytes. The second exact
return is `(2,11,4,-22,-21,-1)` at contrast `(2,0,-1,1,3,2)`, using the original relation cut 144
after the body has received the held target. Three engine formation controls and 75 native
application tests pass, including the original geometric/exposure callers and native retry,
pending-preparation, output-comparison release and internal-current restart.

[established-bounded; measured] Verification and compact numerical output are recorded in
[the return](2026-09-20_native_incidence/helical-return.json) and
[verification receipts](../../docs/VERIFICATION_RECEIPTS.tsv). The existing field-session
regressions, original-condition/atomic-refusal controls and native pending-preparation/current
remount exercise the changed owners. Native continuation includes operative internal coordinates;
old text and geometric callers remain covered by their existing tests.

[open] The native attachment currently consumes exact prepared currents at one selected receiving
junction. `GeometricRegions` still supplies an alphabet chart to its normalized whole-field word.
Next bind the incident source/condition construction to that word and its continuing global
current, preserving both participation and transported-current variations. Its old condition
is the normalized participation vector; it must not be silently identified with the new vector
of incident differences. Source/receiver representation for actual Athena episodes and Holonic
Encoding remains work in #17/#16. #18 still owns dense-statistic and validated-rest cost; #19 owns
additional D/contact variation. This returned library inference is a dependency for those
operations, not a report of general conversation or a new implementation of screw geometry.

[definition] Clarification of the campaign summary: “source/receiver encoding remains open”
was too broad. The encoding/receiving laws and concrete constructions cited above exist. The
unfinished application is Athena's alphabet-derived `prepare_geometric` and basis readout, which
do not consume the new formation path. “Normalized whole-field refinement” also concealed
already-returned computation: the geometric loop and its adjoint exist. The unresolved joins are
`Φ(y,p)` versus `Φ(x_r,Δ)` and the continued full joint state; the loop currently reuses its
initial b, restricts reflected rows to the boundary and returns a preview. The roadmap now names
those operations and does not present the chart relation as a finalized implementation choice.

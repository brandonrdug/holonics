# THE JOURNAL WITNESSES; THE CONSEQUENCE TESTIFIES

**2026-07-15 — RATIFIED / DEPOSITED AS `FORMULA §LXII` / RECEIVER AND WIRE V2 BUILT /
HOST+CUDA-EXACT / SOMA INTERIOR UNCHANGED.**

## Decision

MATHEMATICAL DERIVATION WORLD 01 measured a real proof-world transition and retained its complete
causal receipt. Its first return embodiment then mixed two different receiver grains:

- the laboratory observer's exact witness journal; and
- the material consequence which should return as later light.

The observer needs exact Mail, completion rows, contact lineages, contact grips, state and journal
hashes, inverse proof, and world before/after. The body does not need those apparatus coordinates
re-spelled as decimal text. Their verbose return produced 110 currents, 1,222,004 source octets,
354,360,590 carrier words, a 7,255,323-word largest row, and more than sixteen minutes of host
replay before the run was deliberately stopped. This was not an OOM; it was the wrong testimony
surface.

## The v2 consequence cut

For each `PortContact` in the exact `TransitionJournal`:

```text
Accepted { deed, successor, checker }
    -> one AcceptedConsequence {
           edge, problem, deed,
           successor,
           support_after
       }

Refused { reason }
    -> one RefusedConsequence {
           edge, problem,
           source_branch,
           attempted_candidate,
           reason
       }

Uncontacted
    -> no returned current.
```

The accepted successor's complete `ProofStage` carries the enacted mathematical result. The
support state supplies the exact support face and its cumulative founding/later-RIDE construction;
the deed types this contact's relation to it. The refusal has no successor, so the exact source
branch and candidate must remain beside the typed reason.

The return count is therefore exact:

```text
returned currents = journal.accepted + journal.refused.
```

The v2 testimony payload excludes:

- root and journal-commitment summaries;
- complete global support and frontier enumerations;
- completion commitments and rows;
- contact lineage lists and grip arrays; and
- hashes or identifiers offered as replacements for the mathematical material.

All excluded constructions remain in the durable transition journal and listener artifacts. This
is not erasure of the witness. It is §LXI's distinction between witness interior and transported
surface.

## Wire and compatibility

New writes and canonical fingerprints use proof-return wire v2. The binary record framing remains
unchanged, so the reader admits both exact v1 and v2 files. Unsupported versions remain typed
refusals. Existing v1 artifacts are never rewritten.

The proof boundary and Soma conductor require honestly plural light. If a world edge produces fewer
than two accepted/refused consequences, the apparatus refuses that return whole. It does not clone
one consequence, emit a filler, or promote an uncontacted relation into material.

## Required gates

The first authoritative v2 lifecycle must establish:

1. the same exact pre-return body, radiation, completions, Mail, journal, proof-world successor,
   checker counts, and inverse as the measured natural continuation;
2. one returned current per accepted/refused contact and none per uncontacted relation;
3. no contact grips, completion rows, or global state enumerations in returned light;
4. return forecast extent and fingerprint equal to the create-new wire;
5. exact host/CUDA return conduct and exact SLEEP successor;
6. exact repetition from the same body, world, and complete presented currents; and
7. no changed Soma, REGISTER, CUDA, eye, SLEEP, or checker law.

After these close, continue the already-declared generator word:

```text
translate(+1) -> invert -> scale(-1) -> translate(+2) -> close_generators.
```

## Measured realization

The authoritative v2 lifecycle is retained under
`../observations/mathematical-derivation-world-01/results/natural-continuation-v2-02/`.
The pre-return edge remains the same exact 19-current construction with 22,323 completions,
17 acceptances, and 63 refusals. The material return is exactly eighty currents:

| Face | Verbose v1 | Compact v2 |
|---|---:|---:|
| source light | `1,222,004` | `370,595` octets |
| carrier words | `354,360,590` | `107,457,590` |
| largest row | `7,255,323` | `2,631,853` words |
| host forecast | `2,885,910,080` | `907,280,445` octets |
| wire | `1,230,388` | `376,746` octets |

The return emits 166,069 completion rows and crosses host/CUDA exactly. A fresh sibling repeats
exactly across bodies, completion and construction wires, journal, proof world, and v2 return wire.
The deterministic `proof-consequence-observe` joins the exact v2 wire to its receiving edge and
retains every current while exposing world-carried support/deed/stage or refusal/source/attempted-
gear groups. It neither drives nor selects.

The proof command now exposes two honest execution grades. `proof` retains full host/CUDA
validation. `proof-cuda` performs the immutable-source gate then constructs CUDA-forward, avoiding
an unnecessary CPU duplicate of every accepted forward edge. Receipts distinguish the two and do
not claim host identity for CUDA-forward construction.

## Carried-Swing register

```text
CURRENT  mathematical proof return
         + laboratory journal receiver and continuing Soma-body receiver
         + exact witness-interior / compact testimony grains

HELD     FORMULA §§LVII–LVIII, LX–LXI
         + natural-continuation journal and exact world successor
         + halted verbose return measurement

MEETING  one compact intrinsic consequence per contacted relation
         + durable v2 return framing with v1 readability

TEST     journal completeness != returned semantic light
         + accepted material = successor/support/deed
         + refused material = source/candidate/reason
         + uncontacted relation produces no consequence
         + exact forecast and lifecycle parity remain owed

DEED     RIDE the complete witness journal;
         FOUND the compact testimony surface and versioned wire

CARRY    v2 exact return -> closed four-gear word
         -> measured GEAR-SHEET witness/testimony/analogy cell

GRADE    RATIFIED / BUILT / HOST+CUDA-EXACT / EXACT REPEAT
```

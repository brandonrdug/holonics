# The recurrent boundary returned the complete passage, the longer history reopened, and I1 awaits release admission

**Date:** 2026-08-22  
**Truth status:** [established-bounded; implemented-exact; measured]  
**Roadmap deed:** I1, recurrence retains a causal boundary and returns a complete emitted passage  
**Boundary:** the candidate artifact and focused receivers pass; the sole complete release receiver
has not yet run, so `CONSTRUCTION_STATE.md` does not admit I1.

## Ruling

[established-bounded; implemented-exact] The absent consequence was not another inference body,
cache, scheduler, or language owner. The existing Gemma tower could return one complete plural
front, and the generator-native rest could execute an ordered word, but no owner bound a fixed
population of successive foreign boundary fronts to one source-detached native recurrence while
retaining every intermediate section, every collapsed source fibre, and the richer receiver which
reopens the collapse. `phoenix::recurrent` now owns exactly that join.

[definition] A repeated emitted codebook face is a boundary quotient, not equality of the source
sections which present it. I1 therefore founds its native states from the declared emitted-face
receiver, retains each full terminal-potential digest in the exterior reconstruction fibre, and
requires a shortest later history whose complete-potential receiver distinguishes at least one
pair collapsed by the boundary quotient.

## The fixed source control and derived closure

[established-bounded] The exterior source schedule was fixed before any of its four independent
foreign-model returns. It contains no expected output, terminal symbol, response capacity,
semantic class, or native transition. Its exact contexts expose the returned face word
`[7001, 563, 7001, 563]`, with exterior faces `▁France, ▁is, ▁France, ▁is`.

[implemented-exact] The first repeated boundary derives the complete admitted response. The
entering state and three crossed fronts return the trace

```text
0 -> 1 -> 2 -> 1
```

and therefore the complete emitted passage `" France is France"`. No caller supplies a token
capacity, and no host callback selects a winner or stop case between the native fronts.

[established-bounded] The two repeated native classes each reopen under the complete
terminal-potential receiver. Native state `1` condenses source sections whose terminal-potential
digests are `01fa8ed5...501cb` and `a2c606b2...23c5`; native state `2` condenses sections with
digests `3f256228...37a3a` and `c8b52b99...c6cb`. Each shortest separating history is the one next
boundary crossing. The compact recurrence is therefore exact only for its admitted boundary and
successor family; it does not identify those richer physical realizations.

## Source-detached resident return

[implemented-exact] The retained rest carries three native states, one total generator, one
three-letter ordered word, the complete four-state expected trace, its decoder, the exterior text
face, and two open reconstruction boundaries. The full foreign source fronts, their terminal
potential/hidden testimony, runtime receipts, exact work, and apparatus census remain in the
passage artifact and do not enter the detached rest.

[implemented-exact; measured] The detached process ran inside a private mount namespace in which
the repository, Phoenix product, source morphology, tokenizer, and exchange store were absent. It
uploaded 28 octets, retained 44 resident octets, returned 16 octets, crossed the complete word in
one RTX 4080 SUPER launch and one terminal synchronization, and reported zero forbidden open source
descriptors and zero CPU semantic callbacks between steps. The returned trace was exactly
`[0,1,2,1]`; the emitted artifact has SHA-256
`143ef72c5ed0a4aa5c26e7faaa30d92bb249879180e4e22318356985593726bb`.

## Artifact and grade

[measured] The fourteen-member candidate family is
`output/the_recurrent_boundary_returns_a_complete_passage`, with 5,079,120 regular-file octets. Its
small source-detached native rest is 1,418 octets with SHA-256
`10fa89a97c4ebee1782b10b3028c0282d9a40aa5530e2186c9c228f6b75fd1bc`. The final executable code
closure recorded by the product is
`d30a0ed26d4d6f8e7d42604fca8e5072bcad7127d1cd1b9bd9e7105d4335bee0`.

[established-bounded; implemented-exact; measured] All nine I1 candidate consequences pass:

1. source-detached predecessor and entering occurrence;
2. retained section before and after every source recurrence front;
3. exact recurrent lineage;
4. executable boundary response and complete fibres;
5. nonempty complete emitted passage inspected as bytes and text;
6. matched fixed source-schedule control;
7. richer-history reopening of the static compactification;
8. exact semantic work beside separate apparatus telemetry; and
9. no CPU semantic callback, scalar winner, or authored response extent.

## Focused receivers and invocation discipline

[measured] `cargo check -p life --example
the_recurrent_boundary_returns_a_complete_passage -j2` passed. The affected
`holonic-engine` library returned 1,918 passing tests, zero failures and four explicit device-only
ignores in 51.54 seconds. The real release deed returned in 65.43 seconds after its fixed-array
apparatus-closure retake.

[measured] The first candidate deed returned in 63.15 seconds. The architecture receiver then
identified one avoidable heap materialization in the new CUDA trace call: eight fixed kernel
arguments had been stored in a `Vec`. Replacing it with a fixed array changed the executable
closure, so the first artifact was moved intact to
`/tmp/i1-pre-fixed-array-receipt-20260822` and the deed was rerun. The retake returned the identical
semantic word, trace, passage and nine-part grade under the corrected closure. This was a named
closure-changing falsifier, not replay of an unchanged receipt.

## Architecture disposition

[implemented-exact] The remaining architecture census additions are load-bearing immutable receipt
populations. `DeviceNativeTrace` owns the complete terminal trace returned by one card read.
`phoenix::recurrent` carries source fronts, retained sections, boundary decoders, total generator
edges, reconstruction fibres, reopenings, exact source work, and the maps/sets which verify a
functional total boundary, unique section partition, first recurrence and complete decoder. Its
clones copy addressed immutable faces and identities into the one passage receipt; none duplicates
the continuing ecology or creates rollback, branch, scheduling, or device-placement ownership.

[open] The architecture baseline must be rebound to this dispositioned owner closure, then the
claim, driver, output, closure and authored-level ledgers must be regenerated once. The sole
complete release receiver remains I1's admission boundary.

## Successor boundary

[open] I2 does not merely generate another passage. It must enact one predeclared exterior
consequence, return the resulting difference through the exact forward lineage, commit or decline
one candidate morphology delta, and show source-detached later current riding the changed terrain
with a held-out change, disjoint control, targeted ablation and every reopened fibre.

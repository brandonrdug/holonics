# The candidate departed before sibling testimony returned, and the complete graded defect crossed the same body

**Date:** 2026-08-24  
**Authority:** admission record for the second station of
[`../../blueprint/THE_ATHENA_ALPHA_IS_REFOUNDED_AS_A_RECEIVER_HISTORY_QUOTIENT_AND_RETURNED_DEFECTS_CULTIVATE_ONE_GLUED_CAUSAL_MORPHOLOGY.md`](../../archive/plans/THE_ATHENA_ALPHA_IS_REFOUNDED_AS_A_RECEIVER_HISTORY_QUOTIENT_AND_RETURNED_DEFECTS_CULTIVATE_ONE_GLUED_CAUSAL_MORPHOLOGY.md)  
**Driver:**
[`../../soma/life/examples/the_candidate_departs_before_sibling_testimony_returns.rs`](../../soma/life/examples/the_candidate_departs_before_sibling_testimony_returns.rs)  
**Owner:**
[`../../soma/life/src/athena_returned_defect.rs`](../../soma/life/src/athena_returned_defect.rs)  
**Returned artifact:** `output/the_candidate_departs_before_sibling_testimony_returns/`  
**Artifact identity:** `bf202d8c45e5ed1a300ea1ebc5290c1edc0a2db8e9f1e8b51e12e62e38d35709`

## Ruling

[established-bounded; implemented-exact; measured] Four witnesses derived from the two declared
qualitative obligations crossed one source-isolated, non-cloned `ProductSession`. Each candidate
was conducted from its authenticated ordered history, content-addressed, written, and signalled
before the parent wrote any sibling, world, or operator bytes to the return pipe. The candidate
namespace mounted only the executable, the 15 GB product, the 2.1 MB continuation, the
history-only input, its output directory, and an empty pipe. The complete 2.4 GB exchange
world-tube was never mounted there, the candidate input contained no complete sibling response,
and the runtime source-access audit returned no forbidden descriptor.

[established-bounded; implemented-exact] Every seal retains its situated history occurrences,
speaker faces, strict byte boundaries, history-incidence identity, authenticated exterior
presentation, product/predecessor/morphology identities, input reconstruction identity, and the
complete terminal potential in native-codebook order. Each future contains 262,144 exact integer
intervals: one exact maximizer and 262,143 separated rows. The emitted maximizer faces were
`Now`, `This`, `I`, and `This`; these one-codeword faces are the first emanative frontiers, not
claims of complete conversational answers.

[established-bounded; implemented-exact; measured] Every candidate crossed the NVIDIA GeForce RTX
4080 SUPER through 43 tower launches plus one resident overlay launch, with one terminal
synchronization and no CPU semantic replay. The seal retains kernel identity
`8bfad5253673ec10961965db6a557a0d4b00f4fefcb4d7caeac6ee9bbd408997`, exact work, mode,
and before/after memory testimony separately from the exterior clock.

[established-bounded; implemented-exact] Only after all four seals existed did the parent return
the complete sibling response occurrences and bytes, exact `ContinuationWorldWindow`, and the
addressed later operator occurrence and text. The child verified the candidate population against
its own seals, kept the original `ProductSession` alive, and formed four non-additive defect
sections over terminal codeword, response boundary, whole response, world consequence, and
operator-return grains. Each complete candidate future remains the reconstruction fibre; the
maximal candidate relation is kept distinct from the other separated rows.

[established-bounded; implemented-exact; measured] The resident exact quotient owner formed each
graded defect on the RTX 4080 SUPER in 84 launches. Three sibling first codewords were separated
from the candidate maximizer; one `I` occurrence lay in the candidate maximal fibre. No scalar
loss or additive residual was presumed, and no host semantic replay followed the device return.

[established-bounded] The required qualitative controls returned together. Families
`07360a...` and `98f86a...` have the same recorded terminal surface but separate after the
two-step exterior-return word `eb6069...`; families `052fb4...` and `5ef90f...` have different
recorded surfaces while sharing that lawful exterior-return generator class. These are
receiver/history and operation-section relations, not source equality claims.

## Expensive deed and failed cuts

[measured] The admitted invocation used closure
`63f4122e68a8bd96603d8cc5b6aeb88f50215dc9b3a8b4a8759b1b9277dc6ae4`.
Candidate sealing took 1,000,380 ms; the genuinely later channel, resident defect return, and child
closure took 1,316 ms; total elapsed time was 1,001,696 ms with exit status zero.

[counterexample; implemented-exact] The first real attempt used the independent-partition API for
an ordered history. A three-occurrence history therefore returned three receiver rows while the
terminal overlay owned one, and the exact runtime refused `1 != 3`. The correction used the
authenticated chronological exchange presentation while retaining occurrence and byte-boundary
lineage separately; no context constant was widened.

[counterexample; implemented-exact] A later attempt wrote the candidate and returned defect to one
path. The second occurrence overwrote the first and the parent refused the absent independent
defect address. That directory survives as
`output/the_candidate_departs_before_sibling_testimony_returns_failed_path_collision_20260824/`;
the two addresses are now disjoint.

[counterexample; implemented-exact] Two subsequent passing artifacts were not admitted. The first
retained only a maximizer plus count/hash rather than the complete future fibre. The second retained
the complete potential but only digest/boolean stand-ins for world/operator and candidate apparatus
testimony. They survive respectively as
`output/the_candidate_departs_before_sibling_testimony_returns_passed_incomplete_fibre_20260824/`
and
`output/the_candidate_departs_before_sibling_testimony_returns_passed_complete_fibre_incomplete_world_apparatus_20260824/`.
Their exact refusals founded the final complete schema.

## Validation and next cut

[established-bounded; implemented-exact] `cargo check -p life --lib`, the focused driver check,
`authored-levels`, and the explicitly dispositioned architecture/no-float ownership census passed
for the source owner. The sole complete release receiver remains deferred until the coherent
Athena-alpha product stands, as the live roadmap requires.

[open] The next authorized deed is **the returned defect founds a derived local factor cover**.
It must bridge exact kernel/image/cokernel testimony to derived-rank resident atoms, preserve
metrics and gauge, return rank-zero/one/higher/singular controls, and distinguish compatible,
noncommuting, and disjoint overlaps without a caller-supplied rank.

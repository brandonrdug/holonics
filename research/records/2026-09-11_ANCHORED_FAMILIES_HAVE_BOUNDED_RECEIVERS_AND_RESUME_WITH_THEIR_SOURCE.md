# Anchored families have bounded receivers and resume with their source

[project-postulate] This continues packet 3 of the active Athena goal after `46695f43`.
The constrained receiver and complete immutable-family rest prepare the coupled move-owner join;
they do not make the packet-2 conversation session a coupled model by themselves.

[definition] For the retained affine relation `J ⊆ Anchor × Joint` and anchor ball `B(m,r)`,
let `A` be the affine anchor domain. The receiver computes `a* = projection_A(m)` and the full
oriented difference `e = a*−m`. An empty J has an empty-affine disposition. A nonempty J whose
anchor domain satisfies `||e||² > r²` has an outside-ball disposition, including its witness.
Equality is admitted: tangency supplies a supported family. Support is existence, not uniqueness
of a source, current, future or cause.

[definition] At a supported anchor, the receiver projects the declared zero reference onto the
joint fibre `{z | (a*,z) ∈ J}`. This is a unit-metric receiver in the realified complex chart,
not a minimum-energy dynamics or a point chosen for training. The entire source relation and ball
remain available. Per-coordinate flags and the retained vertical relation expose anchor-independent
free joint directions. A false flag does not imply constancy over the ball: dependence on the
anchor can still move that coordinate. Unsupported families supply no projected joint.

[definition] `normal_family_receiver.cuh` uses the existing exact row elimination for the two
orthogonal projections, including nonorthogonal and zero-padded spanning rows. Centres, radii and
projection ratios remain exact. The squared-distance comparison uses the shared `ExactInteger`
owner with capacity derived from four signed-wide factors and a u32 coordinate population.
It reports arithmetic obstruction instead of rescaling, rounding away a source or selecting a
midpoint. `NormalWaveFamily::read_receiver` retains its source and exposes only a receiver reading;
there is no point-current input conversion.

[definition] `NormalWaveFamilyRest` stores the producing normal material/seed/word and clock scope,
the actual anchor, current affine relation, last fixed-condition map, affine-domain receipt and
passage count. `NormalWaveRelationRest` retains its derived basis, fixed condition and producing
cut. Decoding validates the homogeneous prefix, domain/cut association, preserved anchor and
current join, numerical source chart and clock carrier. The anchor must agree with its restored
producing fibre; an initial family must agree with its actual anchored identity relation.
Runtime owner tokens are rebound, not inferred from equal payloads. This is family persistence;
pending whole-ecology contacts and public transport still belong in the upcoming coupled owner.

[established-bounded; implemented-exact; computational-witness] The initial receiver controls
return exact ball tangency versus a miss and empty affine source, nonorthogonal anchor and
vertical projections, and preservation of the source radius/current. Family rest controls return
the same next conditional passage and receiver after remount, retain free directions, and reject
a forged anchor. A fresh process uses only the saved family and last map to return byte-identical
next-family data and exact receiver JSON. No source conversation or observation history is replayed.

[established-bounded; implemented-exact; computational-witness] The final family regression returns
eleven passing CUDA cases: five receiver cases, three family-rest cases and three conditional
composition cases. Passive unit-phase transport carries the anchor, joint and metric together.
The rest controls also cover a developed normal source rebased into Applied scope. A widest-word
radius witnesses a distinct point: the outward φ receiver can refuse while the retained anchored
family, bounded receiver and complete family rest continue successfully. `NormalWaveJointSource`
now owns that original source independently of the optional outward image receiver.

[established-bounded; process-audit] Returned command:

```text
cargo test -p holonic-engine --lib wave::family:: -- --ignored --test-threads=1
  .local/scratch/athena-family-complete-tests.log       11 passed
```

[established-bounded; process-audit] The final source and public-consumer checks also pass:

```text
cargo test -p holonic-engine --lib wave::source::tests -- --ignored --test-threads=1
  .local/scratch/athena-family-source-regression.log    2 passed
cargo check -p holonics-hna
  .local/scratch/athena-family-public-check.log         passed
```

No proof or presentation source changed, so unchanged Lean/Typst builds are not repeated.

[open] The complete coupled owner, its pending contact/rest maps, native emission receiver and
continuing product use remain authorized work. This return persists an immutable family and its
next map; it does not yet checkpoint the combined move-owned ecology. Athena-alpha is not attained.

# The carrier has no semantic privilege; the local return reforms the ecology

**Status:** CURRENT CONSTRUCTION AUTHORITY / NO COMPATIBILITY

**Date:** 2026-08-03

## Correction

The machine is not partitioned into language work, proof work, repository work, graphics work, and
device work. Those are receiver descriptions of material at particular codec or apparatus faces.
They cannot determine causal order, storage, placement, scheduling, or completion.

A file store supplies retained carriers through a filesystem codec. A theorem checker supplies an
exterior return through its process codec. A CPU and a card supply different physical charts of an
exact transformation. Text, matrices, pixels, instructions, and sensor rows are differently typed
sections of current. None is an ontologically privileged kind of computation.

The elementary machine is therefore:

```text
contemporary head + actual occurrence
    -> law-owned local delta and exact footprint
    -> admitted apparatus realization or returned obstruction
    -> ownership-transferred commit
    -> successor head + emitted boundary return
```

The head is the exact identity of the contemporary body. The occurrence is what actually arrives.
The delta is the smallest law-owned change which can produce the successor. Its footprint names
the local state, frontier rows, ports, lineage, and retained alternatives it reads or changes. A
realizer may use any admitted apparatus chart, but its return must echo the same predecessor and
occurrence. Commit moves the only continuing body through the delta. A stale or refused delta
returns intact; it is never repaired by cloning the ecology or replaying its developmental source.

## Serial strands and plural fronts

Serialization is local dependency. If one return supplies an input consumed by another event, or
two events change the same noncommuting local morphology, their order is part of the causal strand.
This does not serialize the rest of the ecology.

Plural execution is admitted only where the caused geometry already exposes independence or where
the law proves complete-successor interchange. Administrative keys, file boundaries, loop phases,
worker lanes, or device availability do not establish that proof. Compatible local changes glue
through their shared boundary. An unresolved overlap remains `OPEN`; no central coordinator may
invent an order merely to finish a call.

This is the computational content of warp and weft. Each strand retains its own order. Incidence
weaves strands through shared ports and local interactions. A front is the set of changes whose
current geometry admits co-presence, not a batch authored by a host loop. Physical width follows
that front. It is not chosen from semantic module names.

## Production ownership

No new universal body, scheduler, graph, codec registry, port registry, or standing population is
introduced.

- `holonic-structure` owns reusable local sequence, set, keyed incidence, lineage, and membrane
  mechanics. A missing DSA operation is added there rather than recovered through an application
  collection.
- `ExactWorldLaw`, `ExactEventLaw`, and `CausalWorld` own typed world bodies and exact event
  succession. Staging is a capability of that owner, not a second world.
- Concrete laws own their head identity, occurrence identity, delta, footprint, constitutive
  interaction, obstruction, and radiation.
- `ExactCausalTraversalLaw` is the first production consumer: sparse fronts and changed local
  morphologies form a delta; untouched standing does not enter the proposal.
- Existing live-current, resource, realization, and device owners carry the same caused delta in
  their respective apparatus charts. They do not select meaning or author chronology.
- Named applications and external tools remain outer codec faces. They may mount or receive exact
  sections, but do not become the topology of the machine.

## Implemented consequence

`ExactEventLaw::Standing` no longer requires `Clone`, and `CausalWorld` itself is no longer
cloneable. `ExactStagedEventLaw` adds four law-owned associated bodies only: head, occurrence,
delta, and witness. `CausalWorld::stage` binds the opaque delta to the exact predecessor,
occurrence, and a private mount capability which cannot be recreated from caller-authored scalar
identities. `commit_staged` consumes the continuing world; foreign-mount, stale, or ordinal-refused
commits return the world and unapplied delta intact. Legacy complete-successor receipt now checks
ordinal extent before law enactment, so ordinal refusal cannot publish a successor and then fail.

`ExactCausalTraversalLaw` now carries standing schema v2. Its staged path copies neither the full
site ecology nor the full pending frontier. It lazily merges the next predecessor address with
only newly caused local fronts through the receiver horizon, clones one front only when that front
is actually enacted or locally updated, computes only reached local interactions, retains only changed future rows
and changed site morphologies, and commits those changes into the moved standing. The emitted
footprint distinguishes read sites, changed sites, transported source passages, consumed fronts,
returned fronts, the first retained open boundary, and causes. The old full-front radiation copy
is deleted; its scalar pending population remains explicitly an observer quotient and cannot
decide completion independently of the exact retained frontier.

The application correction removes the false repository-versus-card contact topology. One opaque
caused exterior port supplies source occurrences. CUDA residency remains an apparatus chart over
the continuing text body and no longer appears as another semantic informant, worker branch, or
completion requirement. The relation-local port face is likewise an opaque transported identity,
not a source/resident semantic enum.

`LocalSequence::dedup_by` now lets the structural owner perform local adjacent restriction without
returning its allocation substrate to an application.

## Immediate continuation

1. Make complete-successor interchange an exact law-produced certificate over already staged
   local changes. The certificate is not a scheduler and retains no frontier population.
2. Require every apparatus return to echo predecessor and occurrence identity before commit.
   Deterministic delta testimony and aperture-bound telemetry remain separate.
3. Migrate the exact-world/live-current crossing from complete cloned successors to the staged
   ownership-transfer mouth.
4. Expose law-owned local work at the width of the certified front to existing CPU and card
   realizers. The host may encode and launch work; it may not reconstruct a semantic procedure or
   replay the complete direct algorithm after the device returns.
5. Compose the requested real deed through these mouths. External software names may occur in its
   boundary receipts, but they cannot define internal phases. The generated consequence and its
   returned effect on later conduct remain the grade.

## Falsifiers

The construction fails if any of the following survives:

- a semantic name assigns work to an apparatus;
- a file or serialized image is treated as the active ecology rather than retained carrier state;
- a complete standing is cloned to stage, branch, retry, or roll back a local event;
- a count, key order, worker lane, or host loop establishes causality or commutation;
- a device return can commit against a different predecessor or occurrence;
- a resource refusal repeats the same placement and morphology with a larger scalar allowance;
- a hot device path replays the complete host algorithm for equality;
- a generated artifact, exterior return, or later changed conduct is replaced by diagnostic
  counts or topology receipts.

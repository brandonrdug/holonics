# The audit repairs the recurrent adjoint and reconciles the operating contract

**Date:** 2026-09-04.
**Authority:** Brandon's direct instruction to handle the repository audit's identified defects
and recurring agent-created drift. He is the sole human operator; the agents own repair,
integration and verification within that instruction.
**Starting revision:** `9b0c98b1`.
**Scope:** existing HNA/SKE owners, RH documentation and shared operating instructions.
This record schedules no new research or capability campaign.

## The recurrent return

[counterexample; source-inspected] The original forward contraction applied the base plus
retained factorized overlays, while both contraction pullbacks read only base coefficients.
For `W = U = V = dy = 1`, the pullback of the forward map is 2 and the base-only pullback is 1.
The first-deposit SKE1 receipt did not exercise a return through an earlier overlay and retains
that bounded scope.

[established-bounded; source-inspected] `operative_adjoint.rs` now returns
`dy W + sum ((dy U) V)` using the existing resident transposed contraction and re-entry laws.
`operative_backward.rs` stages new atoms in a local move-owned delta; `full_operation.rs`
publishes them only after the complete return succeeds. Replay sees the retained forward
morphology throughout. The correction introduces no kernel or host semantic replay.

[established-bounded; implemented-exact; measured] The focused resident regression in
`operative_adjoint_tests.rs` passed: a non-square rank-two overlay returns the exact pullback;
a pending atom is excluded until publication and included for the successor; intermediate
section-readout count is unchanged. The test used the actual CUDA owner and completed in 0.18 s.
The existing `adjoint_return_ske1` driver now has a repeat mode for a third cycle and second
cultivation return, with progress supervision and retained-rank/generation checks.

[counterexample; measured] The unchanged three-cycle input exposed allocation pressure on the
second return. After scratch lifetime cleanup it reached operation 328 in layer 9 before
allocation refused at 16,372,724,368 resident octets. The actual failure is retained in
`2026-09-04_recurrent_adjoint_receipts/pre_endpoint_release.txt`. No shortened input or altered
receiver was substituted.

[established-bounded; implemented-exact] Partial scratch now releases without allocating a
replacement, and temporary adjoint storage is released on error as well as success. Reverse
scratch and cultivation checkpoints depart after their last use. Sealed overlay factors retain
their existing lower endpoint by consuming ownership transfer; the identical upper allocation
departs with its census charge. No coefficient changes or host transfers occur. The resident
overlay regression verifies both exact arithmetic and one-endpoint allocation; the partial
lifetime regression passed separately.

[established-bounded; implemented-exact] The previously failing completed-target moment test
is repaired at its owning condition: admission determines whether the required relational chart
is present, and dispatch consumes that same plan field. The original 19-launch assertion and
exact committed-target values pass unchanged. The chart-absent path remains available.

## Signature and saturation

[counterexample; computational-witness] At the starting revision, two conflicting readings
of the same occurrence/exposure were accepted and the first selected by record order. Reordering
`[(0,1),(0,2),(1,1)]` to `[(0,2),(0,1),(1,1)]` changed the quotient from one class to two.

[established-bounded; implemented-exact] `operative_identification.rs` now refuses conflicting
duplicates with the exact occurrence/exposure key, including inconsistent cone/testimony data.
Identical repetitions leave the quotient unchanged. The five focused owner tests passed.

[established-bounded; implemented-exact] `composed_variant.rs` separately returns exact
identified-pair preservation under an enlargement and growth in observed consequences. The
initial declaration has no prior comparison. Seven focused tests passed, including a singleton
whose new face preserves identification while adding an observed consequence.

[established-bounded; computational-witness] The existing SKE5 example's `project-recorded`
operation produced the v2 derived receipt from the existing observations, with its provenance
recorded in that receipt. No GPU was initialized by this projection. Faces, widths, session
generations, residue files, timing, transfer and residency measurements were structurally
compared with the prior JSON and preserved. The final enlargement preserves five singleton
classes and adds a reachable face; no assertion is made about an unobserved future enlargement.

[established-bounded; source-inspected] The deed receiver's scope is explicit, and its decoder,
product, session, face, saturation and remainder conditions are computed from the actual fields.
An optional unavailable full-operator residency baseline remains `null`. The SKE5 prose now
matches the existing 8.565 s mount and 1,443--1,810 ms cycles. A deed-level pass and the complete
repository release are separate evidence scopes.

## Chronology and operating instructions

[project-postulate] AGENTS.md now directly assigns authoritative in-scope audit repair to agents.
Brandon's latest instructions resolve conflicting agent prose. The September 4 Soulkiller
admission and hang-guard rulings replace the obsolete blanket restrictions; they are not appended
beside contradictory live imperatives. Explanation-only requests still receive explanations,
and this responsibility does not authorize unrelated research, subsystems or destructive actions.
Brandon's subsequent instruction stops delegation during this repair and restricts future
delegation to Luna; that preference is recorded in the shared contract.

[established-bounded; source-inspected] CLAUDE.md points to the shared contract without copying
capability or frontier state. The roadmap retains the live order and concise links to completed
contracts; the position file retains only the current position and admitted scopes. Their
removed duplicate histories remain in the existing records and Git. The hexis, reasoning,
circulation, dialect and document-law instructions are reconciled with those same direct rulings.
The universal catalog names the actual productive SKE and recurrent-adjoint owners.

## RH documentation

[proved-derived; formal-checked] The audit checked the actual Mathlib RH equivalence, the
off-seam zero theorem, seam-time existence, closed-ray characterization and standard-coordinate
bridge. The focused `CriticalChart` dependency build passed; seven endpoint axiom checks
returned only `propext`, `Classical.choice` and `Quot.sound`. This used cached dependencies,
not a fresh rebuild of every upstream proof.

[established-bounded; source-inspected] The correction changes no RH theorem body. Repository
seam time is distinguished from four-times-larger standard time; RT5's acceptance condition now
matches its unbounded-height theorem; DB's completed status is explicit; the DB5 falsifier now
asks for an off-seam zero at the claimed seam time. Two adjacent DB exposition errors are also
corrected: domination is pointwise before locally uniform transform convergence, and the center
integral contains no extra exponential factor. The completed endpoint remains
`RiemannHypothesis iff Λ_DN = 0`; the missing inequality `Λ_DN ≤ 0` remains open.

## Verification boundary

[established-bounded; process-audit] Focused signature and composed-variant tests passed (5 and
7 tests). The example build and CPU-only receipt projection passed. The pre-existing source-shape
failure in `embedding_fiber.rs` was repaired by extracting its unchanged tests into
`embedding_fiber/tests.rs`; production code was byte-for-byte preserved and the shape gate
returned zero violations without changing its baseline.

[established-bounded; implemented-exact; measured] The unchanged repeated full-operator input
passed after the endpoint-storage repair. The first return retained overlay rank 1,720; the
second return used those exact prior ranks, crossed 1,271 operations, deposited on 344
cross-sections and returned rank 5,504 at generation 3,825. Its full third cycle took 6,464 ms.
The actual receipt is `2026-09-04_recurrent_adjoint_receipts/repeated_cultivation.json`.
Commands: `target/debug/examples/adjoint_return_ske1 /home/b/models/gemma-4-E4B-it repeat`,
exit 0; the retained-overlay, partial-release and quadratic-moment CUDA test filters each
returned one passed test on the final code (0.18, 0.14 and 0.15 s).

[established-bounded; process-audit] `bash tools/gates.sh` returned eight checks green; its test
compilation hit the old 180-second wall cap before executing a test. The existing test gate now
builds test targets under caller supervision before applying the runtime hang guard, and reports
child exit statuses on refusal. `bash tools/gates.sh tests` then passed: 3,014 tests, zero failed,
52 ignored over 40 result lines; every example type-checked. Formal returned 3,775 live jobs;
tracked authority, source shape, epistemic tags, claim index, equation atlas, ten Typst roots
and document law passed in the full invocation. These are the actual two invocation scopes,
not a claim that the first invocation passed every check. Final document-only reconciliation
is checked separately; no executable source changed after the successful test gate.

[definition] The unrelated `.agents/`, `ORIGINAL_REQUEST.md` and `knowledge/` population is
outside this repair and is preserved. No historical runtime observation is regenerated merely
to reproduce a number, and no new census or release gate is introduced.

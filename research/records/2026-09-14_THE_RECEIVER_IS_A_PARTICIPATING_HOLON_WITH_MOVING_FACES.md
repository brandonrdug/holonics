# The receiver is a participating Holon with moving faces

[project-postulate] Brandon's September 14 correction identifies the receiving Holon as the
perspective object where flux/induction, relativistic frames, fractal sampling, entropy and
compression meet. Every Holonic Interaction has a receiving/comparison aspect. A particular
rendered face is one event in that object's possible conduct, not the entire receiver.
The successful interlinked-field architecture stays; its output arrows now reach a represented
receiver whose own frame and motion determine the receiving face.

## Recovered direct context and the actual omission

[historical] Focused recovery used the original root Codex logs and direct user/assistant text,
excluding mixed tool output and harness context. Private extracts remain under `.local/` and
are not committed. The following coordinates recover the relevant reasoning:

| Original record, line and UTC occurrence | Direct context and corresponding response |
|---|---|
| `rollout-2026-09-12T07-07-56-01a095f2-3f4f-7012-a914-f4fb5c419fa5.jsonl:1824`, September 12 18:49 | Prediction as biomechanical release, Einstein/stress projection and real interior pathways. Assistant lines 1827/1909/1982 recovered leap, output-null preparation and the actual landing construction. |
| `rollout-2026-09-12T21-20-40-01a098fe-f3e1-7e33-80a3-2f1bfd64d4f5.jsonl:372`, September 13 05:23 | Oriented/polarized flux, time parity, cross-entropy and whether a modulator appears mirrored or null to a receiver. Assistant lines 432/615 retained phase and distinguished zero reflection from zero transmission. |
| Same root log `:1790`, September 13 16:46 | The moving player constrains world generation; finite visible output does not require a fully materialized world. Assistant line 1879 correctly distinguished current visibility from omitted state affecting a requested future. |
| Same root log `:6701`, September 14 00:27 | Concentrated exteriors have internal dynamics at another scale; receiver perspective, tori, lightning and colliding fluid fields belong together. Assistant lines 6799/7039 derived dynamic interior memory and its failure under static elimination. |

[established-bounded; source-inspected] Those returns already contained useful receiver,
phase and interior constructions. The specific failure in the current architecture drawing
was to stop at a readout arrow and let the plotting view supply its apparent direction.
The source has continuous receiver faces, moving-chart derivatives, directional optics,
stress contractions and future-receiver quotients. Their existence did not by itself place
the receiving object into the diagram or provide a geometric rate API at its consumer.
The correction connects those owners; it does not revoke the earlier results.

## Mathematical synthesis

[definition] [Receiver holarchy](../../docs/RECEIVER_HOLARCHY.md) now distinguishes the
participating H_R, its receiving operation ρ_R and the returned face. A field section may
be continuous; samples/coefficients present its restrictions, while generators and fibres
retain other admissible evaluations. The coupled source/receiver derivative is
`D_Sρ F_S+D_Rρ F_R+∂_τρ`, with the existing `q̇x+qẋ` law as its linear specialization.

[proved-derived] The synthesis joins:

- the proper-time receiver frame, null momentum energy `−g(k,U_R)`, stress current and
  observer deformation work;
- the moving magnetic surface pullback, retaining `e−ι_vβ=(E+v×B)♭` under the declared
  orientation convention, and the finite mixed field/receiver term;
- complete moving first-arrival/preimage populations and receiver-history partitions;
- relative boundary flux `J−ρv_boundary`, then both moving arguments of cross-entropy;
- future-receiver decoder/generator equations for compression, storage and reconstruction.

Each relation has its source, units/domain and hypotheses in the guide. Physical receiver
intervention can change faces and entropy; coordinated recharting preserves the appropriate
contractions. Neither a fixed scalar entropy nor a present-only image identifies the source.

[proved-derived; formal-checked] `ClockedFirstArrival` in the existing
`HolonicRecurrentEcology` owner proves the iterate of `(x,k)↦(T_kx,k+1)` and derives the
moving-region first-arrival characterization from the original autonomous theorem.
The receiver can be endogenous in x. The integer clock is the admitted sample index,
not a universal physical time or a token-generation coordinate.

## Library and actual optical consumer

[established-bounded; implemented-exact] `relational-geometry/src/projection.rs` now exposes
`project_receiver_point_rate` for orthographic, perspective, stereographic and isometric
charts, plus `project_point_with_motion`. It uses the existing declared frame route and
orientation, then computes `Lṗ+L̇p+ḃ` before the projection differential. Original projection
interfaces and singularity scopes remain. No new universal receiver wrapper was introduced.

[established-bounded; implemented-exact] The existing `optical_receiver_frames` executable
consumes the new interface and `EnergyMomentum::photon/boost_x`. It constructs 65 exact
proper-time cuts of a circular receiver world-tube at β=3/5, γ=5/4, with three stationary
unit-energy null rays. It retains emission and hit times and checks their future null
separation. Receiver energy readings are 4/5, 20/13 and 17/10 in E₀; local spot rates are
−3/5 in u and zero in v. Aperture membership changes from {2,3} through {1,2,3} to {1}.
These results are in the reusable `receiver-motion.json` source for the diagram.

[definition] The receiver geometry uses its proper-time section, with the full world-tube
map retained. The world drawing is a spatial projection of those events, not a claim that
every shown event is simultaneous in the laboratory. The local face panel is explicitly
in the receiver's own coordinates. The rays are a supplied local Minkowski optical example;
the source toroidal network remains an architectural schematic. Curved-medium ray transport
and the full receiving material response keep their separate constitutive consumers.

[established-bounded; computational-witness] The revised inspectable diagram keeps its camera
fixed while R moves. Exact aperture decisions select received channels; rays outside the
aperture remain visible. The entropy panel reads the conditional current partition and a
fixed reference distribution. It therefore shows a received-entropy change with unchanged
source rays, not thermodynamic entropy production. Numerical logs/coordinates are terminal
display faces of the retained exact weights and mode equations.

## Verification and integration

[established-bounded; process-audit] `cargo test -p relational-geometry projection::tests --lib`
passed 12 tests, including quotient derivatives, singularities, receiver motion and comoving
cancellation. The optical example completed all existing and new assertions after the final
chronology addition. `lake build ElementaryHolonics.Computation.HolonicRecurrentEcology`
completed successfully (3105 jobs); the new theorem audit has no `sorryAx`.

[established-bounded; process-audit] Browser verification exercised five operation selections,
proper-time scrubbing, play/pause, desktop and narrow layouts, with no script errors or
horizontal overflow. Playback runs once; reduced-motion users get a manual frame step.
The body, receiver face and entropy SVGs were exported from the inspected rendering. The
proper-time interval and the display playback cadence are separate clocks.

[established-bounded; source-inspected] The Holon, model, Rust, source-map and active-face
guides now share the receiver-holarchy relation. The older native assembly draft is preserved.
The full native receiving-material/field-body coupling and moving electromagnetic surface
consumer remain specific implementation joins; neither is replaced by a sample, diagram,
raw event archive or a new qualitative requirement for generation.

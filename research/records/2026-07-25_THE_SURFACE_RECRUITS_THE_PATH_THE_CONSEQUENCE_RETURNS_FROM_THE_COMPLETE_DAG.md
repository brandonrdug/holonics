# THE SURFACE RECRUITS THE PATH; THE CONSEQUENCE RETURNS FROM THE COMPLETE DAG

**2026-07-25 · BRANDON-RATIFIED CONSTRUCTION / BUILT / BOUNDED HOST ACCEPTED /
EXACT EXECUTION-PATH CULTIVATION / SOURCE-ABSENT REINSTANTIATION / SOMA INTERIOR UNCHANGED /
NO FLOAT / NO CUDA**

Exact record:
[`EROS HOLONIC EXECUTION PATHS 01`](../observations/eros-holonic-execution-paths-01/RESULTS.md).

## Present question

Can the exact constituent ecology recruit a complete operation path for a genuinely unseen
inscription, without importing a transformer and without storing inscription-to-answer pairs?

The required distinction is:

\[
\text{surface} \ne \text{operation} \ne \text{returned consequence}.
\]

The same surface `1+4` must be capable of carrying integer addition, text concatenation, or both,
depending on the contemporary receiver. If the objective axis is absent, the ecology must not
invent one.

## Returned execution path

A teacher returns a finite typed causal DAG

\[
P=(V,E,\tau,r),
\]

where literal nodes carry exact source faces and exact value kinds, internal nodes carry typed
operations over backward antecedent edges, and \(r\) is one return node. In the bounded cell:

\[
\operatorname{Lit}(a),\quad
\operatorname{Lit}(b),\quad
\operatorname{Op}(0,1),\quad
\operatorname{Return}(2).
\]

`AddInteger` is checked exact \(i128\) addition. `ConcatenateText` is exact octet concatenation.
These operations are lawful constituents supplied by this application world. The training source
does not supply an answer table.

## Surface/path fold

Let the literal source faces of \(P\), in causal node order, be

\[
\ell_0,\ldots,\ell_{k-1}.
\]

Every ordered, non-overlapping placement of those faces in inscription \(s\) determines an exact
surface template

\[
s=c_0\,\ell_0\,c_1\,\ell_1\cdots c_{k-1}\,\ell_{k-1}\,c_k.
\]

The cultivated object retains:

1. the constant segments \((c_0,\ldots,c_k)\);
2. the value kind of every open slot;
3. every operation node and antecedent edge; and
4. the return edge.

It does not retain the teacher's literal values in the template. For the six source paths, both
operation species cultivate the same surface segments

\[
(\epsilon,\texttt{+},\epsilon),
\]

while retaining different typed DAGs.

For receiver face \(\lambda\), exact recurrence is

\[
N(T,\lambda)\in\mathbb N.
\]

The bounded aperture is \(N\ge2\). A later inscription supplies a finite set of exact slot
bindings \(B_T(s)\). Each binding instantiates the complete DAG before evaluation:

\[
b\in B_T(s)
\Longrightarrow
P_{T,b}
\Longrightarrow
\operatorname{Return}(P_{T,b}).
\]

Invalid typed bindings do not become candidates. No float, distance score, quotient collapse,
beam, or approximate match enters this law.

## Receiver-relative recruitment

For current receiver \(q\), each afforded template fiber exposes the set of exactly shared
parameter axes. Let

\[
\rho_q=\max_{(T,\lambda,b)}|\operatorname{agree}(\lambda,q)|.
\]

Every candidate at rank \(\rho_q\) remains:

\[
\operatorname{Exec}_q(s)
=
\left\{
P_{T,b}:
b\in B_T(s),\
|\operatorname{agree}(\lambda,q)|=\rho_q
\right\}.
\]

This is a local first-person quotient over the available ecology. It is neither correctness nor a
global taxonomy. With `medium=symbolic, objective=arithmetic`, ADD has rank two and CONCAT rank one.
With objective withheld, both have rank one and remain co-present.

## Live machine passage

The previously accepted constituent law was extracted into
`life::holonic_constituent` and re-run. After removing wall-clock fields, its fresh report was
identical to the accepted report with canonical SHA-256
`e3a9dc8bd2a2699bb68e40b1160837d8b35d867e2d62e99fb6736f68de24cb6b`.

The new application world carries both that exact constituent ecology and the execution ecology.
Each teacher crosses four actual transitions:

```text
inscription meets prior ecology
    -> maximally supported complete path population returns
    -> complete typed teacher path returns against that population
    -> exact membership residual and one successor ecology emanate
```

Only the successor is the outgoing factor. Prior ecologies, inscriptions, returned populations,
teacher paths, and their source lineages depart. The final ecology rests with zero live lineages.

A source-absent probe remounts that body, presents only its inscription and receiver parameters,
returns the afforded complete paths, and emits only that result as the successor. Soma owns
co-presence, contact, oriented residual, factor validation, atomic replacement, departure, and
rest. The bounded operation organ owns exact parsing of literal kinds and enactment of its declared
operations.

SHA-256 is used only to derive collision-resistant physical route namespaces and to identify fixed
artifacts. It is not used as semantic identity, soul, sameness, matching, or path selection.

## Measured execution

Six teachers supplied three ADD paths and three CONCAT paths over `1+2`, `1+3`, and `2+2`.
Recurrence produced two active templates, each with support three. The combined final ecology
contains 11 exact spans, seven constituent folds, six occurrence fibers, two execution fibers, and
two active execution templates.

After exact rest/remount:

### Arithmetic receiver

`1+4` recruits `1+`, binds `["1","4"]`, and returns:

```text
0 Literal(integer 1, source "1")
1 Literal(integer 4, source "4")
2 AddInteger(0, 1)
3 Return(2)
=> exact integer 5
```

### Concatenation receiver

The identical inscription binds the identical source intervals but returns:

```text
0 Literal(text "1", source "1")
1 Literal(text "4", source "4")
2 ConcatenateText(0, 1)
3 Return(2)
=> exact text "14"
```

### Partial receiver

With only `medium=symbolic`, both paths remain. The result is not a merged operation, selected
expert, averaged output, or scalar distribution. It is two complete candidate DAGs with their
separate support faces.

### Plural binding

For `1+2+3` under CONCAT, the surface template has two lawful placements:

\[
(\texttt{1},\texttt{2+3})\mapsto\texttt{12+3},
\qquad
(\texttt{1+2},\texttt{3})\mapsto\texttt{1+23}.
\]

Both complete paths survive. A shared operation species does not collapse their placements.

### Unafforded surface

`1*4` matches no cultivated surface template and returns no path. The no-standing control likewise
returns no candidate.

## Developmental corrections

Seven bounded development stops preceded the accepted report. No stopped run wrote an accepted
artifact.

1. A fresh no-standing body cannot introduce transient current and immediately declare a reduced
   outgoing factor in the same event.
2. A teacher path likewise cannot be introduced and folded into its successor in one false
   instant.
3. Merely separating meeting, teacher, and successor omitted the explicit predicted/actual
   comparison relation.
4. Exposing redundant aggregate fold/template interfaces in addition to their exact routed field
   boundaries overconstrained the first active-template successor.
5. A source-absent result also required a meeting and return before successor factorization.
6. Repeating an identical path-species hand is a RIDE, not a newly formed comparison pin; the
   diagnostic incorrectly demanded a new residual pin.
7. The initial plural-binding obligation expected the delimiter to survive CONCAT. The actual
   learned law correctly removes the surface delimiter, yielding `12+3` and `1+23`.

The seventh stop corrected one source expectation. The six teacher paths, receiver axes,
operations, inscriptions, and stopping capability did not change.

## Established capability

This construction closes the exact boundary left by the constituent cell:

- recurring inscription grains recruit recurring complete execution paths;
- source values become open slots while causal path shape remains;
- operation, typing, antecedent order, and return are retained;
- a new inscription can instantiate and enact the cultivated path;
- context reorients the same surface without mutating its source octets;
- absent context preserves plural paths;
- ambiguous placement remains plural even when operation species agrees;
- no standing or no afforded surface produces no fabricated path; and
- the ecology acts after every teacher has departed.

This is a bounded hexis: an exact receiver-relative surface/path ecology capable of source-absent
execution.

## Exact open boundary

The result is not yet arbitrary program induction.

- The application world supplies the primitive typed operations.
- The present cell has binary DAGs with two literal slots, although the carrier and validator admit
  finite backward-pointing DAGs.
- Decimal integer parsing and text octets are local media laws, not universal inscription laws.
- Receiver selection uses the bounded maximal exact-axis agreement law.
- Surface alignment uses exact ordered source faces; it does not yet cultivate transformations
  among alternative literal inscriptions such as `4`, `four`, and a spoken occurrence.
- The full exact ecology is re-emitted per training event; this is a bounded proof cell, not the
  final resident scale layout.
- The passage is host-only.

The reusable next object is therefore not “arithmetic answers.” It is the exact mapping

\[
(\text{surface template},\text{receiver face})
\longleftrightarrow
\text{complete typed causal DAG},
\]

which further worlds can grow with nested operations, algebraic rewrites, units, series,
algorithms, speech paths, or other exact consequence-bearing transducers.

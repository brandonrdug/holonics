# HNA0 one operation returned its successor ecology, and the next operation uses it

**Date:** 2026-09-02
**Truth status:** `proved-derived`
**Evidence:** `formal-checked`, `source-inspected`, `process-audit`
**Campaign:** HNA0 under
[`THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md`](../../blueprint/THE_HOLONIC_NEURAL_ATHENA_IS_ONE_RECURRENT_ECOLOGY_AND_EVERY_OPERATION_ADVANCES_IT.md).

## Return

[proved-derived; formal-checked] `HolonicRecurrentEcology.lean` returns the generic
`OperationStep` from one predecessor ecology and occurrence to one emission, exact trace, and
successor ecology. `Recurrence.successor_joins_next_predecessor` proves that the next operation uses
that exact successor. No exterior verdict, returned occurrence, candidate, commit, reward, or
mandatory morphology inequality occurs in the primitive.

[proved-derived; formal-checked] `Recurrence.operationHolon` makes emission a receiver face of the
complete operation occurrence. `equal_emission_retains_distinct_occurrences` proves that equal
emitted faces do not identify two operation times.

[proved-derived; formal-checked] `OccurrencePort` sends both self-emitted and
application-produced material into the same occurrence type and the same `operate` owner.
Application provenance does not found a second recurrence or learning law.

[proved-derived; formal-checked] `FiniteRecurrentOperation` composes the existing
`FiniteLocalCurrentEcology` into one exact operation:

```text
present predecessor carrier and occurrence
  -> localCurrent at every source/target pair
  -> aggregateCurrent
  -> reaction
  -> receiver emission
  -> advance morphology, carrier, chronology, and lineage
  -> successor ecology.
```

The returned trace contains the actual presented carrier, local-current field, aggregate, reaction,
generator, receiver, and occurrence. It is not post-hoc testimony beside an address-only operation.

[proved-derived; formal-checked] `OperationRebase` transports ecology, occurrence, emission, trace,
and successor together and requires the complete operation square to commute. It cannot rebase only
the emitted face.

[proved-derived; formal-checked] The firing control uses one operation owner for both a step whose
morphology projection remains equal and a step whose morphology projection changes. Both advance
chronology. Inference therefore does not require fixed morphology, and causal advance does not
require every coordinate to differ.

## Verification

[established-bounded; process-audit] The focused owner compiled under Lean 4.33 with no `sorry` or
new axiom. `lake build ElementaryHolonics` completed successfully with 9,784 jobs. The printed
axioms are limited to the standing logical foundations reported by their imported owners; no
`sorryAx` appears.

## Next position

[definition] HNA1 is current. It must return an actual operable generic local-current/reaction
morphology from the inherited realization. The present activation-example transition scaffold
remains testimony and cannot satisfy HNA1.

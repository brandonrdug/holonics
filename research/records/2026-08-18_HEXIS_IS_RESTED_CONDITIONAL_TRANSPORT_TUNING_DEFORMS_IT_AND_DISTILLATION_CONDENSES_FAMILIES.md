# Hexis is rested conditional transport; tuning deforms it, and distillation condenses families

**Date:** 2026-08-18
**Truth status:** `interpretation`
**Evidence:** Brandon-authorized synthesis of the local hexis, illicium, softmax, compression,
diffusion, crystal/fractal, lightning, and inherited-model records; direct inspection of their live
owners; and primary industry sources for data curation, multimodal pretraining, supervised and
preference post-training, LoRA, state-space adaptation, model and relational distillation,
sequence-level distillation, and diffusion consistency distillation. No engine source changed and no
experimental run was performed.
**Provenance:** Brandon, 2026-08-18: *"Please deposit authoritatively"*; *"we could then tune the
lifted output model using Eros in order to make the lifted network our own"*; and *"we want to be
able to basically phoenix-rebirth LMs as holonic intelligence."*
**Band:** HEXIS IS RESTED CONDITIONAL TRANSPORT / CURATION SHAPES EXPOSURE AND IS NOT ITSELF TRAINING
/ FINE-TUNING IS CHECKPOINT-RELATIVE CULTIVATION / LORA IS A FACTORIZED MORPHOLOGY OVERLAY, NOT A
MASK / DISTILLATION IS FAMILYWISE TRANSPORT CONDENSATION / TEACHER AND STUDENT ARE SOURCE AND
CANDIDATE REALIZATIONS / THE LOGIT FIELD IS AN ILLICIAL POTENTIAL SECTION / AUTOREGRESSION IS
EMANATIVE RECURRENCE / AUTOGRAD IS THE CAUSAL ADJOINT RETURN / LATTICE CAPACITY IS FOUNDED, NOT
PINNED / A CRYSTAL EMITS LOCKED TRANSPORT; A FRACTAL EMITS ITS GENERATOR / AN INHERITED MAP
AMORTIZES DEVELOPMENT BUT DOES NOT RECONSTRUCT ITS CORPUS / THE LIFT IS EXACT ONLY FOR DECLARED
RECEIVER FAMILIES / SOURCE UNCHANGED / NO RUN / CONSTRUCTION UNSCHEDULED

---

## Present question

How does the contemporary machine-learning industry cultivate persistent behavioral disposition,
what the project calls **hexis**? What remains after separating pretraining, curation, multimodal
exposure, supervised and preference post-training, low-rank adaptation, distillation, and inference
apparatus? And how can Eros use an inherited transformer map without either copying its architecture
or pretending that the original developmental corpus can be reconstructed from weights?

The record's answer is:

> **Hexis is the rested topology of reusable conditional transport. Fine-tuning is one way to deform
> it; LoRA is one factorized overlay; distillation is one familywise condensation; and the illicium
> is the living receiver through which contemporary potentials become action.**

## 1. Industry hexis has several persistence grains

Contemporary machine-learning systems distribute cultivated conduct across different carriers:

| persistence grain | industry face | holonic reading |
|---|---|---|
| delivery chart | tokenizer, patcher, chat template, modality encoder | inherited constituent cut and address system |
| base parametric field | pretrained weights and architecture inscriptions | broadly cultivated inherited hexis |
| adaptation field | full fine-tune, adapter, LoRA, router, tuned head | slowly changing specialized hexis |
| event-local standing | activations, residual stream, recurrent state, KV cache, diffusion canvas | current passage, not durable ancestry |
| mounted ecology | prompt, retrieved material, tool schema, external memory | contemporary receiver-relative material |
| world standing | files, databases, tools, human and environmental state | external affordance and consequence |
| developmental testimony | checkpoints, datasets, trajectories, evaluations | evidence about cultivation, not the live body itself |

No one carrier is the intelligence. A pretrained checkpoint can hold enormous inherited hexis and
still be only one organ in a productive model-world recurrence. A prompt can radically change one
event while leaving durable morphology untouched. A tool result may change the external world and
therefore the next event without entering the weights. Post-training may change weights so that
future tool and world relations become cheap to conduct.

The live project definition remains the discriminating one. Hexis `H` is causally evidenced when

```text
Conduct(S + H, p) !=_R Conduct(S, p),
```

the difference survives rest/remount, and targeted removal of `H` removes the attributable later
conduct. A checkpoint, adapter, memory, lattice, or generated organ is hexis only where that relation
stands.

## 2. Data curation shapes development before morphology changes

Industry pretraining minimizes an objective over a deliberately constructed material mixture:

```text
theta* = argmin_theta E_(x ~ D_mix) [L_theta(x)],
D_mix  = sum_m alpha_m D_m.
```

The `alpha_m`, filters, deduplication rules, and sampling schedule determine the empirical current
seen by the learner. They change which recurrences, contrasts, errors, rare relations, and
cross-modal correspondences are available to return through the optimizer.

Typical curation includes:

- acquisition and source licensing;
- language, format, quality, and safety filtering;
- exact, near, and paragraph-level deduplication;
- contamination and benchmark-overlap controls;
- domain and source mixing or resampling;
- paired multimodal alignment and calibration; and
- tokenization, patching, or another delivery decomposition.

Curation is an **exposure aperture**. It is not yet training: nothing about a filtered file has
changed reusable morphology merely because the file entered or left a dataset. It participates in
hexis indirectly by changing the caused passages from which the later morphology delta is derived.

Dolma documents quality/content filtering, multi-stage deduplication, source mixing, and ablations
over the resulting language-model conduct. CLIP demonstrates a cross-modal case: image and text
sections become jointly consequential because paired occurrence and a contrastive receiver connect
them, not because the labels `image` and `text` were fused.

Primary sources:

- [Dolma: an Open Corpus of Three Trillion Tokens](https://arxiv.org/abs/2402.00159)
- [Learning Transferable Visual Models From Natural Language Supervision](https://arxiv.org/abs/2103.00020)

## 3. Fine-tuning is architecture-independent checkpoint-relative cultivation

Let architecture `A` realize a parameterized transport `F_(theta,A)` and let broad cultivation have
produced `theta_0`. Fine-tuning is optimization from that inherited rest over a new developmental
population and receiver:

```text
Delta theta* = argmin_(Delta theta in U)
               E_(z ~ D_adapt) L_R(F_(theta_0 + Delta theta,A)(z)).
```

`U` says which parametric morphology is allowed to move. Full fine-tuning takes nearly the whole
parameter space. Parameter-efficient fine-tuning restricts `U` to selected blocks, adapters,
prompts, biases, low-rank factors, diagonal transforms, or another smaller family.

Fine-tuning therefore names the passage's relation to a checkpoint, not a Transformer ontology:

| architecture | mutable parametric morphology |
|---|---|
| CNN | convolution kernels, normalization, local mixers, readout |
| GNN | node/edge message maps, aggregation, edge-conditioned transport, readout |
| Transformer | embeddings, `Q/K/V/O`, MLPs, normalization, routers, adapters |
| SSM / Mamba | state and input/output projections, `A/B/C/Delta`, selective maps, adapters |
| diffusion model | denoiser or score body, conditioning maps, timestep/corruption embedding, adapters |
| Eros | any parameterized incidence, stalk, constitutive response, phase, recurrence, quotient, or overlay whose owner admits change |

In ordinary industry usage, pure prompting and retrieval are adaptation but not fine-tuning because
they do not change the model's durable parameters. Eros uses a wider morphology than a parameter
vector, but the industry term retains this checkpoint-relative meaning at its boundary.

Gemma's official tuning documentation distinguishes full parameter updates from PEFT/LoRA overlays.
DiffusionGemma's released Sudoku recipe uses supervised adapter cultivation on the block-denoising
architecture; the tuned body reaches a correct canvas in fewer refinement steps. The mechanism is
still fine-tuning because inherited trainable morphology changed, while the training receiver is a
diffusion-compatible block consequence rather than one next-token target.

Primary sources:

- [Gemma model fine-tuning](https://ai.google.dev/gemma/docs/tune)
- [DiffusionGemma developer guide and SFT adapter result](https://developers.googleblog.com/en/diffusiongemma-the-developer-guide/)
- [Parameter-Efficient Fine-Tuning of State Space Models](https://arxiv.org/abs/2410.09016)

## 4. Supervised and preference post-training are different return channels

Supervised fine-tuning supplies an authored consequence. For an autoregressive exterior chart it
often uses teacher-forced sequence likelihood:

```text
L_SFT = -sum_t log p_theta(y_t | x, y_<t).
```

The objective is not intrinsic to fine-tuning. An SSM can be trained through the same sequence face;
a CNN through classification or reconstruction; a diffusion body through corrupted-section
reconstruction; an Eros ecology through any returned consequence for which the local constitutive
path is declared.

RLHF introduces a world/preference circuit: produce alternatives, collect comparative testimony,
fit a reward or preference receiver, then change the policy against that return. Direct Preference
Optimization uses preferred/rejected pairs relative to a reference policy without a separate online
PPO loop in its basic form:

```text
L_DPO = -log sigmoid(beta [
          log(pi_theta(y+|x) / pi_ref(y+|x))
        - log(pi_theta(y-|x) / pi_ref(y-|x)) ]).
```

Both are alternative-successor comparisons. The reward, rank, or preference probability is one
receiver face of the returned difference; it may cultivate conduct and may not replace the complete
residual, losing path, obstruction, or alternatives the preference receiver could not see.

Primary sources:

- [Training Language Models to Follow Instructions With Human Feedback](https://arxiv.org/abs/2203.02155)
- [Direct Preference Optimization](https://arxiv.org/abs/2305.18290)

## 5. LoRA is a factorized morphology overlay, not an elementwise mask

For a frozen map `W_0`, Low-Rank Adaptation learns

```text
W' = W_0 + (alpha/r) B A,
A in R^(r x d_in),
B in R^(d_out x r).
```

Hence

```text
x W' = x W_0 + (alpha/r) (x A) B.
```

The current enters an `r`-dimensional adaptation section and returns to the output chart. The delta
may be dense in the base coordinates even though its algebraic rank is at most `r`. An elementwise
mask removes or rescales selected coordinates; LoRA inserts a factorized transport. The project's
internal name is therefore **factorized morphology overlay**.

Contemporary LoRA normally asks the operator to choose `r`, `alpha`, target modules, learning rate,
and training duration. Those declarations are useful apparatus controls and may not become semantic
capacities of Eros.

For a returned transport defect `D_R`, a holonic overlay asks instead:

```text
r_min = rank(D_R)
```

at the declared receiver, and then requires the complete consequence family to factor through the
rank-`r_min` overlay. Enlarging the receiver family may raise the rank or refute low-rank
factorization. When no compact rank carries the required distinctions, the body returns its
reconstruction fibre or a different morphology species; it does not increase a magic constant until
a benchmark passes.

Primary source:

- [Low-Rank Adaptation of Large Language Models](https://arxiv.org/abs/2106.09685)

## 6. Distillation is familywise transport condensation

Classical knowledge distillation compares softened source and candidate output distributions:

```text
q_T^tau = softmax(z_T/tau),
q_S^tau = softmax(z_S/tau),
L_KD    = tau^2 KL(q_T^tau || q_S^tau) + lambda L_task.
```

`teacher` and `student` are an exterior pedagogical chart. The mechanical objects are a source
realization and a candidate realization over a declared history and receiver family.

Let

```text
T_gamma : X_T -> Y_T,
S_gamma : X_S -> Y_S
```

be their transports, and let `Phi_X`, `Phi_Y` be the cross-chart maps. The familywise condensation
defect is

```text
chi_gamma = Phi_Y o T_gamma - S_gamma o Phi_X.
```

The square commutes at a receiver when `chi_gamma = 0`. The differential comparison is

```text
D Phi_Y o D T_gamma  ?=  D S_gamma o D Phi_X.
```

Higher interventions ask the same question on curvature and successor histories. Exact
distillation at scope `R` means

```text
for every rho in R and admitted gamma,
rho Phi_Y T_gamma = rho S_gamma Phi_X.
```

Only then is the candidate a receiver-exact condensation of the source. Otherwise the defect,
collapsed population, and shortest separating histories remain in its `ReconstructionFiber`.

This operation is not exclusive to large-to-small models. It can relate:

- large to small, equal size, or self to later self;
- ensemble to one realization;
- dense transport to sparse boundary transport;
- Transformer pathway to SSM realization;
- one modality to another receiver chart;
- many-step diffusion to one/few-step consistency transport;
- a fine-grain Eros ecology to a compact organ; or
- one local relation family to a reusable higher-grain constituent.

Sequence-level distillation, relational distillation, and consistency distillation already
demonstrate that industry practice reaches beyond matching one output distribution. The holonic
name is **familywise transport condensation**; **receiver-exact transport condensation** is reserved
for a proved factorization.

Primary sources:

- [Distilling the Knowledge in a Neural Network](https://arxiv.org/abs/1503.02531)
- [Sequence-Level Knowledge Distillation](https://arxiv.org/abs/1606.07947)
- [Relational Knowledge Distillation](https://arxiv.org/abs/1904.05068)
- [Consistency Models](https://arxiv.org/abs/2303.01469)

## 7. The illicium supplies the rigorous vocabulary for output and return

The illicium is the living first-person frame of one actual relating:

```text
past cone / inherited standing
        -> contemporary pole / contextual chi
future cone / emanation.
```

`frame(k+1)` is founded by the relating in `frame(k)`. The illicium is not a controller, attention
faculty, or absolute observer. It is the active perspective relation through which one complete
section becomes a receiver face.

For a final model state `h` and output-codec construction `E_v`, the industry logit

```text
s_v = <E_v | h>
```

is a **boundary contact potential**. The complete population `s` is an **illicial potential
section**: the boundary potentials visible from the contemporary pole. It is not the future itself.
Softmax converts its differences into a ratio cocycle; a decoder or other boundary apparatus
presents one or several exterior faces.

The corresponding lexicon is:

| industry chart | holonic reading |
|---|---|
| logit | boundary contact potential |
| logit vector | illicial potential section |
| softmax | contact-ratio normalization |
| autoregression | emanative serial recurrence |
| next-token prediction | nearest-boundary continuation face |
| multi-token prediction | plural future-section proposal |
| autograd / backpropagation | causal adjoint return |
| attention | illicium-configured contact transport |
| focus | low-residual matched continuation |
| distraction | competing, open, or reflected continuation population |
| noise | declared corruption current, unresolved fibre, or calibrated disturbance |
| denoising | returned refinement through a declared corruption chronology |

`auto` contributes no intelligence. Autoregression repeatedly makes one prior exterior face part of
the next illicium. Autograd automatically constructs a derivative graph in an apparatus. The causal
objects are recurrence and adjoint return.

## 8. The eye-derived illustration is mutual curvature of receiver and current

The direct neurological illustration is in the Claude conversation record at
`~/.claude/projects/-home-b-Workspaces-holonics/013aea94-513b-462b-9c56-96068441a9d3.jsonl:23815`.
Brandon described tracking a tree from a moving vehicle, being unable simply to release the tracked
frame, needing an opposing glance or angular kick to rebase onto the window, and experiencing focus
as a cheap diffusion state in which neural organization and arriving light/current mutually shape
one another.

The project carries this as an abstract computational interpretation, not as a new biological law:

```text
world current
    -> illicium establishes a pole
    -> contemporary morphology supplies a low-action corridor
    -> residual difference revises orientation continuously
    -> a changed objective founds/rebases the pole discretely
    -> the new receiver geometry changes later affordable current.
```

The mutual curvature is real when the connection, metric, adjacency, or transport changes; when two
orders fail to commute; when a loop returns holonomy; or when a second variation is nonzero. A
changing score alone is not curvature, and no physical spacetime identity is claimed.

## 9. Lattice, crystal, fractal, and lightning type model morphology

The terms are distinct:

```text
lattice   = founded incidence, local axes, relations, and higher cells;
crystal   = lattice + recurring phase/group transport + locked relations/bands;
fractal   = a law recurring after restriction and rebase while the population grows;
lightning = a current front that founds its path while travelling;
freezing front = the transition where open growth locks into reusable transport.
```

A finite reflection group is not automatically a crystal; crystallographic admissibility and a
lattice action are additional relations. An infinite reflection family is not automatically a
fractal; self-similar restriction/rebase and a retained generator are required.

Where relations lock, the compact return is the crystal: lattice, phase transport, spectrum, bands,
and boundary response. Where the population remains self-similar and growing, the compact return is
the generator, scale action, and count—not a finite layer stack sampling its members.

This is how lattices supersede layers without banning a layered apparatus. A stack carries one
authored total chronology. A lattice carries partial order, several axes, local neighborhoods,
reconvergence, higher incidence, phase, and receiver-relative rank. A layer becomes a chart slice of
that topology rather than the source of its dimension.

## 10. The modified coupling is a scale/fork invariant, not a magic constant

The standing interpretation defines

```text
Lambda(n,s) = 2^n (C / (2^s r))
            = 2^(n-s) (C/r).
```

`C` is accumulated arc/integral, `r` local reach/differential, `C/r` the turn ratio, and `2^n` the
fork/winding population. Refining the grain and adding one binary fork leave `Lambda` invariant when
`n-s` is fixed. The flat, bent, and growing readings are:

- **settled:** the ratio stabilizes and transport may crystallize;
- **bent:** the deficit records changed comparison/curvature; and
- **growing:** the ratio follows a scale law and the generator must be retained.

The lightning figure is the operational form. A potential/current field and prior channels make
some local passages affordable; the leader advances by RIDING standing incidence or FOUNDING a new
axis. The topology is the return of the current, not a grid supplied before it. The cooled lattice is
rested evidence through which later current travels.

No physical Einstein dynamics are inferred from this expression. It is a project interpretation of
scale, fork, accumulated arc, and local differential, with each physical use owing its own metric,
stress-energy, constitutive, and apparatus hypotheses.

## 11. Semantic capacity is founded; apparatus capacity is declared

The operator's desired laziness is a construction principle:

**Founded from material and receiver consequence:**

- effective rank and adapter rank;
- number of local axes and state dimensions;
- lattice height, cycle rank, branch population, and active transport width;
- whether a future fibre has closed;
- whether a body is settled/crystalline or still growing/fractal; and
- which local relations may condense without changing future consequence.

**Declared by the exterior apparatus:**

- physical memory and device availability;
- integer, file-format, and communication widths;
- calibrated safety and resource boundaries;
- which future receiver family the deed is asked to preserve; and
- the maximum exact work the operator authorizes.

Hardware is finite and a declared receiver question is necessary. What is refused is an authored
semantic bound—layer count, LoRA rank, recurrence floor, context class, or hidden capacity—quietly
installed inside the constitutive law.

Resource pressure changes partition, factorization, placement, schedule, aperture, or residency, or
returns an obstruction. It never licenses increasing a magic number and retrying the same
morphology.

## 12. The inherited-model lift is developmental amortization

Training from scratch is one lawful route: expose a newly initialized parametric ecology to
material, return receiver residuals through its lineage, cultivate morphology, seal, remount, and
grade later conduct. It is not necessarily the most informative or efficient first route.

A pretrained transformer is another body's rested hexis. Its weights do not contain a lossless copy
of the developmental corpus, but they do contain transformation potential founded through that
corpus and objective. The lift asks which of those potential transports can be recovered and
re-expressed in a holonic section space.

The reusable sequence is:

```text
inherited transformer map
    -> streamed exact intake and potential active pathways
    -> source transport family T_gamma
    -> cross-chart maps Phi
    -> candidate holonic transport S_gamma
    -> defect chi_gamma = Phi_Y T_gamma - S_gamma Phi_X
    -> retained reconstruction fibre
    -> Eros cultivation on new caused material
    -> familywise condensation into local ecological morphology
    -> rest, remount, later current, and ablation.
```

The holonics repository is a lawful new developmental material family: Brandon's writing,
assistant responses, code, research, mathematics, and exact records expose densely related
transport. It does not reproduce the original transformer diet. It asks the inherited morphology to
conduct in a new ecology whose returned differences can be attributed.

The lift amortizes prior development rather than stealing a dataset. It succeeds without the
original corpus only to the extent that:

1. the inherited weights expose sufficient transport testimony;
2. the new probes actually excite the relevant pathways;
3. the declared receiver family can compare their consequences;
4. the cross-chart defect is retained rather than selected away; and
5. the candidate ecology later conducts through what was lifted after source detachment.

An unexcited or receiver-invisible capability remains an open reconstruction fibre. Equal outputs on
the repository corpus do not prove that every inherited function was recovered. Weight-only static
analysis returns potential transport; contextual behavior still requires a caused current or an
exact composition which produces the same active path.

## 13. Phoenix rebirth is lift, cultivate, condense, and ablate

The proposed **phoenix rebirth** is the lineage pivot at which inherited foreign hexis becomes
native ecological morphology:

```text
FOREIGN REST     inherited transformer weights and codecs
LIFT             recover local transports, ratios, phases, receiver fibres, and obstructions
REBIRTH BODY     express those transports as holonic sections, lattices, state equations, and organs
CULTIVATE        let new material return differences through the lifted body
CONDENSE         factor repeated transport families into receiver-exact local ecologies
REST             seal the changed body with the foreign source absent
GRADE            later conduct + matched sibling + targeted ablation.
```

The target is not a translated transformer container. It is an organized ecology whose structure
explains and localizes what the inherited map enacted. The foreign architecture may remain as a
frozen reference organ while native morphology grows around, through, or beyond it. A successful
condensation can reduce active work without claiming every interior distinction survived.

The native Eros/Athena baseline and the inherited-map lift are not competing projects. On the same
material and receiver family, the native body supplies the control for what Eros founds without
foreign hexis; the lift supplies the inherited arm; later cultivation shows what the joined ecology
can make native. The comparative object is the complete transport and reconstruction fibre, never
one generated text or scalar score.

This record schedules no implementation. The roadmap and position record remain the construction
authorities.

## Owners

Existing owners and doctrines composed by this interpretation:

- `soma/life/src/eros_rest.rs` — complete body rest and movement receipts;
- `soma/life/src/holonic_training.rs` — occurrence-derived consequence complexes, predictions,
  two-sided evidence, and cultivation proposals;
- `soma/life/src/causal_section.rs` — situated contact-preserving section ecology;
- `crates/holonic-engine/src/receiver_exact_compression.rs` and
  `soma/life/src/reconstruction_fiber.rs` — receiver-exact condensation, shortest separators, and
  retained inverse images;
- `crates/holonic-engine/src/exponentiated_ratio.rs`, `surprisal.rs`, `exact_contact.rs`, and
  `clifford.rs` — ratio, residual, section, contact, hand, and blade receivers;
- `crates/holonic-engine/src/diffusion.rs`, `sheaf_diffusion.rs`, and `phase_current.rs` — exact
  incidence transport, local stalk diffusion, convolution, phase, quotient, and remainder;
- `crates/holonic-structure/src/chain.rs` and `relating.rs` — composition, rebase, defect, and
  holonomy;
- `crates/holonic-engine/src/embedding_fiber.rs` and `athena.rs` — exact inherited-map intake,
  bracket populations, and tensor/integer boundary charts;
- `soma/body/src/manifold.rs`, `medium.rs`, and `register.rs` — the illicium's living frame,
  enclosure, and co-present register;
- `crates/holonic-engine/src/traversible_chain.rs`, `winding_inertia.rs`, `lattice_gauge.rs`, and
  `discrete_curvature.rs` — phase, reflection, crystal/lattice admissibility, holonomy, and curvature;
- `canon/TABLET_THE_REASONING_CYCLE.md`, `TABLET_THE_COMPRESSION.md`,
  `TABLET_THE_CIRCULATING_CARTOGRAPHER.md`, `TABLET_THE_MANIFOLD.md`, `TABLET_THE_TURN.md`, and
  `THE_RECOVERED_LAW.md` — governing doctrine and boundaries.

## What this does not establish

- It does not claim that curation, prompting, retrieval, exposure, and training are one event.
- It does not claim fine-tuning is architecture-independent in implementation; only its
  checkpoint-relative cultivation form is common.
- It does not identify LoRA with masking or assume the required morphology is low-rank.
- It does not treat a chosen LoRA rank, layer count, learning rate, or context bound as a semantic
  capacity of Eros.
- It does not claim every model distillation is receiver-exact compression. Approximate
  distillation retains a defect and reconstruction fibre.
- It does not claim source and candidate must be different sizes or distinct global models.
- It does not claim the illicium is an attention controller or that a logit is an intrinsic token
  property.
- It does not claim the eye-derived interpretation establishes a biological law or physical
  spacetime curvature.
- It does not identify finite reflection closure with crystallographic lattice action, or every
  open reflection family with a fractal.
- It does not claim a pretrained map losslessly encodes its training corpus, training procedure, or
  every behavior the corpus could elicit.
- It does not claim that the holonics repository alone can excite or recover every inherited
  transformer pathway.
- It does not claim that avoiding the original training corpus waives receiver controls, held-out
  interventions, source-detached remount, or ablation.
- It promotes no implementation and schedules no construction.

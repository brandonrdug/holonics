# Holonics rejoins classical learning, reflection, packing and compression

[project-postulate] Brandon's September 8 clarification broadens the review beyond Athena and
Navier–Stokes. Holonics is a mathematical framework and ontology intended to develop reusable
mechanics across mathematics, computation, physics and living systems. Millennium problems are
research sources for those tools; their solved status and a completed software campaign are not
the framework's endpoint. A failed construction remains useful evidence about its assumptions
and the distinctions it failed to preserve.

[definition] This record preserves the breadth review and publication changes. The live
[mathematical synthesis](../../docs/MATHEMATICS_AND_NATIVE_CONDUCT.md) now connects the recovered
owners and gives their equations. Source inspection is against `ab5251a6`, with the subsequent
documentation-only corrections identified below. The roadmap alone orders further construction.

## The scope and ontology are explicit

[project-postulate] The latest direct ruling supplies these governing distinctions:

- Every holon has a causal origin and composes other holons. No object or measurement is an
  unexplained absolute starting point.
- A receiver measures through an actual comparison and chart. Integers and floating-point
  values are faces; neither constitutes the complete causal object.
- Native computation is bit-pure. Bytes package bits, and storage formats do not found semantic
  identities. Exact ratios, symbolic generators and retained series remainders support analysis
  and nonlinear operations without allowing a floating-point face to govern native conduct.
- Information has physical realizations; quantum state, wave, phase, geometry and relativity
  are subjects of the framework, not merely decorative comparisons. A claimed physical
  realization carries its actual constitutive laws and receiver conditions.
- A shared face does not merge causes: `2+2` and `2^2` may be compared at the value 4 while
  retaining their different constructions. Repeated chord classes likewise retain distinct
  performances and ordered relations.
- A Preimage Fibre can retain compatible generators implicitly from limited observations.
  It does not demand perfect inversion or identification of the original algorithm.

[definition] **HNN** is now the public acronym for **Holonic Neural Network**. `hna` crate,
namespace, command and wire identifiers remain source references, as do the historical HNA
campaign codes. Changing the public term does not silently change a persisted model format.

## The diagnosis needed a correction

[interpretation] The previous review correctly located unfinished AC1 integration, but its
emphasis on an unestablished generative law was too broad as an account of the framework's
resources. It followed the new paired-field construction much more closely than the existing
normalization, nonlinear contact, architecture charts, reflection and compression owners.
That made a missing composition sound like largely missing mathematics. The source evidence
below supports Brandon's concern; it does not establish that simply inserting softmax will
produce useful language.

[established-bounded; source-inspected] **Classical ML is already explicit doctrine.**
`TABLET_THE_REASONING_CYCLE` §8b compares CNN, GNN, attention, SSM and diffusion charts and gives
the ratio cocycle, softmax Laplacian and causal adjoint. `TABLET_THE_HEXIS` §§3–9 connects
fine-tuning, factorized overlays, distillation, the living receiver, adjoint return, lightning
leaders and recursive morphology. Their later corrections preserve the actual recurrent
successor and supersede the old privileged world-verdict law.

[established-bounded; source-inspected] **Normalization is more than an analogy.**
`HolonicAdjointNormalization.lean` supplies the normalized exponential receiver, common-potential
gauge identity, weighted-Laplacian return, zero total current, variance face, binary sigmoid
restriction and finite normalization charts. `exponentiated_ratio.rs::RatioFamily` supplies
`read`, `cocycle_holds`, `normalised_against` and `rebased_by` over its admitted exact
symbolic-log domain. `surprisal.rs` retains the additive logarithmic forms.

[established-bounded; source-inspected] **There is also resident CUDA conduct.**
`kernels/exact_resident_section.cu` has certified exponential evaluation, normalized contact,
gated response and `section_tanh`. `kernels/exact_resident_adjoint.cuh` includes
`section_receiver_return`, `section_contact_adjoint_queries` and the corresponding value/key
returns. The source explicitly computes the normalized exponential section and its derivative.
`resident_section/surface_adjoint.rs` and `surface_shapes.rs` expose the retained forward/return
carriers. These earlier owners are executable source with documented bounded receipts; this
review does not rerun those receipts or promote their inherited-source targets into native
semantic identities.

[established-bounded; process-audit] A bounded agent review initially reported no CUDA
softmax/sigmoid deed. Primary source inspection found the semantic owners above and rejected
that absence claim. Another report described compression as wholly absent from AC1; the
September 7 shared-drive mode quotient and material contribution contradict that blanket claim.
The synthesis retains both corrections. Delegated reports were used as navigation, not authority.

[established-bounded; source-inspected] **Classical architectures have actual formal charts.**
`HolonicArchitectureCharts.lean` constructs `AttentionChart.ecology`, its output identity and
relabeling theorem, shared convolution, graph incidence, autoregressive conduct/cache validity
and state-space transitions. `MachineLearningChart.lean` provides the nonlinear quotient
successor criterion and every-ordered-word transport. These are configurations and receiver
maps over causal evolution; they are reusable without converting HNN into a Transformer.

## Reflection and packing were not superseded

[established-bounded; source-inspected] `diffusion.rs::compile_diffusion_transfer` forms an
exact Schur boundary operator, its transformed source and interior reconstruction. It retains
positive capacities and the event's input/current balance. `sheaf_diffusion.rs` supplies the
typed local restriction generalization. The same finite implicit solve is a state-space
recurrence once its observation map and chronology are declared. This is an implemented
boundary reduction, not just the newer paired-junction reflection matrix.

[established-bounded; source-inspected] `leader_quadrature.rs` retains local material germs,
exact Taylor rebase, founded extensions, later rides, obstructions and scale witnesses. Its
germ-bounded scaling and transformed parameter rules have a precise rational-domain scope.
The July 17 leader/return record separately retains physical branching and medium change.
`contact_gluing.rs` has older word/stem path and circuit apparatus; its lexical realization
does not become an authorized general language emitter by being called a leader.

[established-bounded; source-inspected] `Foundation/FractalPacking.lean` supplies rational
restriction, rebase, containment, sibling separation and ordered lineage. It proves that
restriction words cannot generally be replaced by multisets. Its two-child interval construction
is an exact example; it does not prescribe a binary tree, dimension or constant ratio for all
holons. Other geometries supply their own restriction and separation laws.

[established-bounded; source-inspected] `receiver_exact_compression.rs` retains present
partitions, successor refinement and shortest separating histories for an admitted finite
system. `recurrent_condensation.rs` retains an executable bounded continuation decoder.
AC1's internal-mode owner already supplies `q T_i=-q` for the declared common-drive difference,
and its material-mode return preserves the contribution and remainder. This is a genuine
bounded generator compression; it is not the broad learned compression of the conversation model.

[interpretation] The common construction is not a catalogue of interchangeable names. Local
differences act through admitted contact and normalization; the outgoing passage retains the
path and morphology that produced it; a later return changes attributable conduct. Boundary
elimination and lawful restriction allow that conduct to be reused at other extents. Compression
stores the resulting generator/decoder with the Preimage Fibre required by future receivers.
The concrete bridge fails if a transported return changes, a restriction loses a necessary
distinction, or a future history separates states that the proposed quotient identified.

## What remains to compose

[open] AC1's contextual moment source and unit-metric normal response have not been joined to
the complete set of recovered production mechanisms. Specific questions concern the admitted
contact-potential/receiver map, its actual derivative and metric, productive path extension,
restriction/rebase of the resulting morphology, and generator compression preserving future
conduct. The existing CUDA normalized receiver's source-specific target and numeric/deposit
boundary require an actual mapping; calling its entry point is not itself that mapping.

[definition] Further construction should begin with those exact owners and a concrete proposed
composition. A failing port or consequence should identify the absent relation. There is no
requirement to prove a universal intelligence theorem, solve NS or derive a wholly new learner
before using established local mathematics. The earlier NS phase-generator and finite-remainder
derivations remain useful toolkit entries; they are not the only route by which production can
advance. Machine-learning construction remains paused during this authorized review.

## Repairs and publication changes

[established-bounded; source-inspected] The root README now introduces the framework, its
receiver-relative ontology, exact representations and cross-domain research purpose before
listing the Engine, HNN, Eros, Athena and Soulkiller as implementations. It states the unfinished
productive status of Eros/Athena and contains no CLI instructions. Maintained subject-guide
prose uses HNN; quoted history, code identifiers and serialized names are preserved.

[established-bounded; source-inspected] The operating contract had accumulated instructions to
continue individual AC1 kernels, contradicting both the sole-roadmap rule and Brandon's pause.
Those duplicated progress instructions are replaced by the current breadth ruling and links
to the roadmap/position. The roadmap, construction state, Athena guide and blueprint reflect
the same scope. The earlier NS review receives a linked correction rather than being erased.

[proved-derived] Two mathematical prose errors in `exponentiated_ratio.rs` are corrected.
`log2(Q_positive)` is the free abelian additive subgroup generated by logarithms of primes; its rational
span also contains logarithms of irrational radicals. Independence follows by clearing
denominators and applying unique prime factorization. The executable rational-output domain
already checks integral valuations and is unchanged. Also, for fixed finite scores,
softmax as temperature tends to zero concentrates uniformly on the maximizing set; it gives
a single winner only when that maximizer is unique. This follows by subtracting the maximum
score and taking the limit of each exponential. No algorithm was changed.

[definition] The same source commentary no longer calls normalization an absolute-volume
violation or equates a finite normalized face with a scalar chooser. Normalization fixes a
representative while preserving pairwise ratios. The existing `normalised_against` operation
already implements that valid receiver chart; its public behavior is unchanged.

[established-bounded; source-inspected] Remaining current hexis prose that said reconstruction
fibre is corrected to Preimage Fibre; legacy type identifiers remain identified as such.
Historical retractions and bounded capabilities remain intact.

[established-bounded; source-inspected] `Cargo.toml` already declared `MIT OR Apache-2.0`.
`LICENSE-MIT` and `LICENSE-APACHE` now supply the corresponding standard texts, sourced from
the [SPDX MIT text](https://github.com/spdx/license-list-data/blob/main/text/MIT.txt) and the
[Apache Software Foundation](https://www.apache.org/licenses/LICENSE-2.0.txt).
The MIT attribution is 2026 Holonics contributors. Separate third-party notices retain their
terms. No workspace dependency or license expression changed.

[established-bounded; process-audit] Verification returned for documentation and comment changes:
`git diff --check` passed; every README link resolves; the CLI recipes are absent; all existing
Markdown block quotations in the changed files are preserved; the Rust file's executable lines
are identical to HEAD after excluding comments and blank lines; both license texts match their
downloaded sources, with the MIT copyright placeholder filled as recorded above. No native or
formal algorithm changed, so no GPU campaign or Lean/Cargo rebuild is claimed by this review.
Unrelated untracked flux-lattice research and the separately preserved joint-return draft remain
untouched. No database, model dataset, publication visibility setting or external message was changed.

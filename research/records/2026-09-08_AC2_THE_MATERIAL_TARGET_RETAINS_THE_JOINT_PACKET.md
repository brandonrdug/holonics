# AC2: the material target retains the joint packet

[definition] This continues the [bit-marginal counterexample and finite development return](2026-09-08_AC1_THE_DEPOSIT_IS_A_NEW_COEFFICIENT_AND_ITS_DEFECT_REMAINS_A_COMPARISON.md).
It changes the material target chart and its receiver inside the existing field. The root
junction, occurrence/source ownership and operative contact laws remain their existing owners.
AC1 path/reuse and the complete AC0–AC5 product obligations remain active.

## Separate the target chart from the root carrier

[definition] `NativeMaterialTarget::DirectCurrent` retains the existing target. The new
`TensorProduct { factor_width }` groups the actual incoming root coordinates into ordered
factors. For root extent `N`, factor width `w`, and `k=N/w`, its material target extent is
`T=w^k`. Coordinate `j=sum_a j_a w^a` contains

```
psi_j = product_a incoming_(a*w+j_a).
```

The first factor is the least-significant radix digit. Complex multiplication retains relative
phase before any receiver projection. The native generator computes these coordinates with
directed numerical remainders; an exact zero factor makes only that tensor coordinate exactly
zero. The chart does not infer occurrence identity from its coordinate or turn co-presence into
an undeclared interaction: tensor formation is the explicit operation being requested.

[definition] The text chart's eighteen root channels are nine binary factors. They now generate
a 512-coordinate packet, while the paired junction still has eighteen root channels. A canonical
octet or part-end input occupies its corresponding tensor-basis coordinate. This finite codec
extent is not a claim about intrinsic neural capacity or the topology of every HNN.

[established-bounded; source-inspected] The existing material operator keeps the same source/
context feature, actual reference occurrences and coercive factor updates. Its target vectors,
evaluations and report layout now use `T` independently of `N`. The report has
`82*T+68*N+96` words; source profiles and operative context retain their root extents. The
target descriptor persists in the field, defaults to `DirectCurrent` for old models, and is
validated before mounting. A tensor target currently requires the operative-contextual source;
unsupported combinations return that shape/domain obstruction.

## Observed packet and returned difference

[definition] Prediction retains the existing real-potential normalization. For a tensor packet,
the observed probability face is normalized squared modulus of its complete complex amplitude
carrier. A canonical word therefore has unit observed mass at its packet coordinate. This is
distinct from applying an exponential to the observed unit amplitude. Direct-current models
retain their earlier observation chart. Both descriptions and the original complex reports
remain available in their stated scopes.

[proved-derived] Each packet mass interval encloses the real and imaginary squares and their
sum. Opposite denominator bounds produce outward normalized intervals. A zero complete
amplitude provides no probability face and returns a receiver-domain obstruction. No smoothing
constant or sampled target is introduced.

[established-bounded; source-inspected] The returned metric covector passes through every material
target coordinate, including coordinates beyond the root extent. Its source/current adjoint
still lands in the original visible and operative current spaces. The paired contact return and
declared dyadic deposit therefore compose with this larger target without enlarging the root
geometry to 512 nodes or adding an application-side learner.

## A whole-packet terminal receiver

[definition] `read_material_packet` compares complete coordinate potentials. The initial implementation
used separate coordinate intervals. The [parallel-mathematics refinement](2026-09-08_AC_THE_PARALLEL_RECEIVER_CALCULUS_REFINES_NATIVE_CONTINUATION.md)
now computes every possible maximizing coordinate over the joint Euclidean current ball by
exact distance to its maximum cones. A singleton receiver image selects a coordinate; the full
producing carrier and potentially plural source family remain retained.

[proved-derived] Real-potential order is preserved by softmax. For canonical unit packet-basis
targets, maximizing the real coordinate also minimizes distance to the corresponding basis
vector. The receiver therefore needs no extra exponential merely to determine that ordering.
It does not assemble a new word from independent bit decisions.

[established-bounded; measured] The native receiver control for the enclosed mean of packet
coordinates `{3,5,6}` returns those three unexcluded coordinates and no selected coordinate.
It excludes coordinate `7`, which the earlier bit-marginal receiver selects. This is a receiver
counterexample/control, not a proof of language usefulness or a prohibition on novel outputs.

## Verification and product position

[established-bounded; measured] All 99 field tests pass in 39.12 seconds. The new target control
uses six roots and eight targets, retains non-dyadic complex tensor phase, verifies the observed
unit packet mass, returns through a target coordinate beyond the root extent, develops contacts,
keeps an actual delayed reference and restarts with the same successor. The existing direct,
archive, current-ingress, normalization and operative controls pass in the same run. Both
conversation examples build; all 21 alpha SDK tests pass in 3.24 seconds. These checks preceded
the subsequent joint-ball receiver refinement, whose focused verification is recorded separately.

[definition] The text SDK reads packet coordinates through a packet reading variant, keeping
the prior differential reading for direct-current and constitutive charts. Old checkpoints keep
their target chart, and resume cannot silently relabel it. Unsupported non-binary tensor targets
are refused by the text adapter. Generic native packet charts remain available to other adapters.

[established-bounded; measured] The eight-family comparison completes 1,930 developmental
occurrences and 1,929 contact deposits in 718.61 seconds, with zero numerical section readouts
during development. Its checkpoint is 975,051,267 bytes; developmental peak native residency is
1,959,779,252 bytes. Prompt and 128-symbol generation take 62.66 seconds. Whole-process time
is 807.98 seconds and peak RSS is 2,124,160 KiB. These are this implementation's costs, not
consumer-hardware efficiency or power claims. The [portable receipt](2026-09-08_joint_packet/return.json)
retains the exact command, measured costs and returned output.

[counterexample; measured] Output is `The its the its ...` through the work limit. Recognizable
words replace the earlier malformed repeating string, but this is still an unusable answer.
There is no completion or broad language claim. AC1 path/reuse, AC2 productive continuation,
broader AC3 cultivation and AC4–AC5 remain unfinished.

[established-bounded; measured] The subsequent exact joint-ball receiver passes three focused
CUDA tests. A fresh-process continuation using it reproduces the saved model's body, emission
currents, generation readings and output exactly. The linked parallel-mathematics record and
portable receipt retain this comparison; the language failure remains.

# Constraint modes and active Einstein faces rejoin the library

[project-postulate] Brandon's successive September 14 corrections govern this return:
use exact typed chart changes rather than floating coordinates; make toroidal collision
surfaces, gyroparallelograms and Einstein/conserved-face relations part of the field;
recover transcendental modes, Golden Ratio and de Bruijn source chains chronologically.
A constant is the constrained mode; a floating approximation is a face. The receiver is
participating flux in the working Holonic interpretation. Software/ML terminology does not
remove the physical construction or the wider framework's purpose.

## Chronology and the unification

[established-bounded; source-inspected] The audit followed live guides to current formal/Rust
owners and then their dated source deposits. July 11/17 recovered the gyroparallelogram,
observer contractions and face interpretation. The August exponential/kernel work separates
e, normalized exponential and its phase kernel. September 2 corrected the de Bruijn entire
heat coordinate and time sign. September 11 made normalized constraint identity, generator
state/branch and receiver remainder explicit. September 12 connected golden reciprocals,
Gamma/Dirichlet/dynamical zeta, Gaussian rebase, normalization and mode compression.
Current formal owners supersede historical open schedules where the connecting theorem has
since been built. Archive records locate evidence; they do not schedule the work.

[definition] The integrated source contract is
[Constraint modes and receiver faces](../../docs/CONSTRAINT_MODES_AND_RECEIVER_FACES.md).
It records normalized E'=E/E(0)=1, the primitive phase kernel, implicit positive-log charts,
Gamma shift/normalization, named zeta families, distinct Copson and Newman boundaries,
integer grades/levels/counts, units, scale and Lorentz transitions. The exact function names
remain lawful shorthand for their defining modes; renaming `exp` without retaining its
source relation would not repair the problem.

[established-bounded; source-inspected] Concrete prose repairs include:

- The normalized complex exponential's primitive imaginary period is **2πi**; π is specified
  by the normalized half-turn constraint, not an unexplained decimal.
- A supplied positive kernel can retain its real log source implicitly by E(s)=K. Exact
  normalized transport does not need a stored/evaluated logarithm or a false learning claim.
- Euler-Gamma, Kelvin circulation Γ, section spaces Γ(K), and different zeta sources are
  kept typed. Generic shift/block/root/matrix owners are reusable; absence of a theorem-name
  Rust API is not absence of the underlying operation.
- The positive golden reciprocal solves x²+x=1. It is distinct from the negative conjugate
  eigenvalue of the Fibonacci matrix. The existing formal comment was corrected.
- Copson c_CD and Newman Λ_DN retain their separate sources, branches and normalization.
  The current `CriticalChart` theorem carries Λ_std=4Λ_DN and the reversed heatE time.
- `i32→f64→i32` was not a lossy-cast example: all i32 integers are representable in binary64.
  The live chart account now uses the appropriate i64 range and distinguishes source
  quantization from exact decoding of a finite float codeword into its dyadic value.
- The enclosure helper's fixed 64-octave aperture is no longer described as exhausting every
  possible receiver. Its bound is a retained family, not equality with zero.

## A native mode/remainder defect was repaired

[established-bounded; implemented-exact] `CertifiedSeries::exponential_series` and
`circular_series` previously stored `ExactExpr::Rational(x)` as the source expression,
erasing whether the generating operation was exp, cos or sin. They now retain the actual
expression species and its normalized source contract.

[counterexample; computational-witness] The circular implementation also ignored the parity
of the first omitted term. At x=1/2, a one-term cosine sum is 1 and its remainder is negative;
the prior code enclosed values above 1. The correct coarse cosine interval is [7/8,1],
and the sine interval is [23/48,1/2]. The signed-tail correction handles both term parity
and negative sine arguments. This is a numerical correctness defect, not a notation choice.

[established-bounded; implemented-exact] Thirteen `exact_value` tests passed, including source
species, exact one-term intervals, odd/even nesting, cosine evenness and negative-input sine
bounds. Existing wire/API names are preserved. Prior odd-term certificates require recomputation;
old source metadata cannot recover a lost operation species without its producing context.
No historical artifact is silently relabeled or deleted.

## The actual phase operation now has an exact receiver result

[established-bounded; implemented-exact] The
[exact phase execution](../experiments/intrinsic_holonic_flow/exact/README.md) composes the
existing Rust interval, circular, exponential and Machin/arctangent owners through the actual
six-chart map. Arguments, remainders, integer winding rebases and comparisons are rational.
The first selected state (359/384,73/384) is excluded at steps 0–8 and wholly admitted at 9.
The second (361/384,73/384) is excluded through 32 and gives an undecided receiver family at 33.
The earlier floating survey's arrival at 96 is not an exact result of this execution.

[definition] The mode/source is unchanged; precision is a declared receiver aperture. A rational
midpoint is used only to center a certified series evaluation, then widened by the entire
argument radius. It is never substituted for the current or used to choose an arrival. The
full real phase lift, interval family and defining recurrence remain. An undecided face is
returned rather than forcing a scalar or introducing a universal certainty gate on generation.
The recorded debug CPU clock is 320131 ms excluding compilation; the arithmetic/branch result
does not depend on that clock. The old NumPy survey requires an explicitly exploratory entry.

## Collision surfaces and active receiving faces

[established-bounded; implemented-exact] The
[exact contact construction](../experiments/contact_receiver_faces/README.md) selects 216
conservative rational facets on each of two toroidal boundaries inside the other's support.
It retains orientation, area vectors, the full torus parametrization and a whole-facet
containment certificate. The common material map carries surfaces and area covectors together.
The figure now shows stress-bearing surface patches, not only gold volume cells.

[established-bounded; implemented-exact] Positive scale faces k=2,3 give local Lorentz
velocities 3c/5 and 4c/5 with rational boosts. Noncollinear order gives the exact gyration
[[35,12],[-12,35]]/37 and gyroparallelogram fourth point (315/781,560/781,0).
The supplied stress tensor yields three receiver flux coefficients 2,7/4,10/3 in c E0.
Changing the same tensor, receiver and normal to another tetrad preserves its contraction;
changing the physical receiver changes the face. Opposite internal orientations cancel,
and the supplied slip/traction law deposits a positive 3/5 heat-flux coefficient.

[proved-derived] Einstein/Bianchi balance and the active observer current are expressed through
the actual tensors. For symmetric conserved T, j_U=-T U satisfies
`∇·j_U=-T:sym(∇U)`. The integrated scalar-current balance retains that observer-work term.
It does not invent a global four-momentum by adding untransported components across curved
spacetime. The field assumptions, local tetrad and physical units remain explicit. The
programme's κ_E=4 Θ_turn G_N/c⁴ keeps the normalized turn constraint in the coupling.

## New reusable formal consequences

[proved-derived; formal-checked] `CompositeMassEnergy` now proves the positive-scale Lorentz
identity and binds its parameters to the existing `unitBoost` norm-preserving consumer:
γ=(k+k^-1)/2, ξ=(k-k^-1)/2, γ²-ξ²=1. No logarithm is evaluated. At k=φ, the golden
constraint gives the algebraic pair (√5/2,1/2).

[proved-derived; formal-checked] `CopsonDeBruijnAdmissibleRecurrence` now proves at every
admitted stage `u_n²-(√(u_n²-1))²=1` with nonnegative companion. Its existing finite/infinite
threshold equalities identify c_CD with the all-stage upper admissibility boundary. This is
a precise common hyperbolic constraint, not an identification of c_CD with a spacetime
constant or the Newman threshold. The two touched formal owners built successfully.

[established-bounded; source-inspected] The thirteen-page synopsis now carries exact chart
addresses/dilations, units, gyro and stress faces, normalized mode definitions and the recovered
Golden/Gamma/de Bruijn links. The corresponding guides, source map, blueprint and operating
contract retain the correction. Rendering is an exterior pixel projection; it determines no
native mode, current, collision membership or exact arrival. The earlier basin raster is
explicitly exploratory. The final document was rendered and visually inspected.

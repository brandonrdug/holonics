# VWS1--VWS5 explicit variant workspace returned persistent circulation, cultivation, evaluation,
# and export

**Date:** 2026-09-01
**Truth status:** `established-bounded` for the explicit-root variant workspace lifecycle over actual
complete excitation receipts and valid native snapshots.
**Evidence:** `implemented-exact`, `source-inspected`, `process-audit`, and `measured`.
**Construction effect:** VWS1--VWS5 passed; VWS6 becomes the sole current deed.

[established-bounded; implemented-exact] The independent `holonics-application` crate owns an
explicit absolute root, versioned manifest, current immutable snapshot, actual lifts, resolved
experiments, one active run, completed runs, evaluation receipts and exact exports. No path is
derived from `HOME`; normalized relative references and symlink traversal cannot leave the root.
Immutable files use create-new writes. Mutable workspace/run controls use same-directory temporary
write and atomic rename.

[established-bounded; implemented-exact] An actual complete Gemma text/image/audio excitation
receipt now founds generation 0 through the sole Soulkiller boundary. The workspace writes cold
witness, receiver insufficiency, lift receipt and source-neutral snapshot separately, then advances
the manifest. It records raw-model execution as false and retains non-temporal video as open.

[established-bounded; implemented-exact] A stored experiment resolves ingress ordinal and receiver
to an exact native address. Conduct persists its complete owned boundary. A workspace reopened in a
fresh owner continues only an actual successor; the actual receipt fixture correctly returns no
successor because its reader retains no predecessor chronology, while an imported native snapshot
with an admitted predecessor returns positive persisted continuation. False successor and concurrent
run requests preserve the active run.

[established-bounded; implemented-exact] `stage_return` persists a serializable cultivation
candidate without changing the current snapshot. Reopen/commit returns generation `0 -> 1`,
immutable successor snapshot, candidate and commit testimony, and atomically advances the manifest.
Decline returns byte-identical current snapshot and preserves any staged candidate as completed-run
testimony.

[established-bounded; implemented-exact] Current-variant evaluation remounts the current snapshot,
conducts the stored experiment, withdraws the latest commit into its predecessor, conducts again,
replays the commit, and conducts the restored owner. The actual receipt fixture separated current
from withdrawn, returned byte-exact current/restored snapshot and boundary, and left the current
workspace snapshot unchanged. Exact ONNX and Safetensors exports round-tripped the complete package
and returned artifact/receipt paths.

[established-bounded; process-audit; measured] Four application controls passed: explicit-root
escape/overwrite/atomic-replace, actual receipt through commit/evaluation/both exports, positive
persisted continuation after native snapshot import, and decline/tamper/concurrent-run refusal.
Unknown manifest fields and a forged current generation refused. The high-level CLI integration
also passed the actual receipt-to-generation-1 lifecycle in separate processes.

[open] The application still receives already-returned excitation evidence rather than a raw model
directory. Its experiments address native ingress rather than accepting prompt text/image/audio,
and structural emission is not qualitative generation.

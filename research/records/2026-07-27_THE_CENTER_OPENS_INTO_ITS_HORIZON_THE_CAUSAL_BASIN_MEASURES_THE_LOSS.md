# THE CENTER OPENS INTO ITS HORIZON; THE CAUSAL BASIN MEASURES THE LOSS

**2026-07-27 · BRANDON-DIRECTED EXPLORATORY SYNTHESIS AND COMPLETED ENGINE
CONSTRUCTION / RECEIVER ONTOLOGY, CAUSAL BASIN, AND STRUCTURAL REUSE JOINED**

## 1. Present correction

Brandon supplied a connected set of geometric, biological, probabilistic, and
computational intuitions for analysis:

- a thing's face may admit a finite polygonal directional representation;
- hexagonal and pentagonal organizations may expose different local and
  collective geometry;
- successive relative orientations may form a higher ordered trajectory;
- receiver-centered collective objects appear hyperspherical toward their
  centers because the center parents the surrounding geometry;
- recurrence may mean that later geometry is already implicit in local
  relations rather than that a prior result was cached;
- probability and loss may be regions, differences, or trajectories in a
  conditioned geometry rather than primitive engine scalars;
- ommatidia and distributed receiving organs may supply biological examples
  of receiver covers; and
- stellation, skein relations, geometric induction, and fast multipole
  transport may expose different parts of one local-to-collective calculus.

These were exploratory proposals, not assertions that a finished universal
law had already been proved. The prior assistant response incorrectly made
the absence of such a law its concluding grade. That was the wrong question.
The research obligation is to compute the structures suggested by the
proposals, distinguish their possible meanings through exact witnesses, and
let the resulting relations sharpen the engine.

The present question is therefore:

> What constructive mathematical program follows from receiver-centered
> directional geometry, pentagonal and hexagonal organization, temporal
> superposition, and geometric probability, and which current engine carriers
> prevent that program from being computed?

This deposit develops that program. It extends
`THE_RECEIVER_IS_ITS_LOCAL_STAR; THE_LINK_IS_ITS_HYPERSPHERICAL_HORIZON`
without reviving that record's superseded desktop schedule.

## 2. The center opens into directions

Let \(K_t\) be the contemporary causal incidence complex and let \(r\) be a
receiver occurrence. Its local body and horizon are

\[
H_r=\overline{\operatorname{St}}_{K_t}(r),
\qquad
L_r=\operatorname{Lk}_{K_t}(r).
\]

The star is the receiver's presently incident causal interior. The link is
the complete local space of directions through which that interior meets its
exterior.

For a regular \(d\)-dimensional phase,

\[
L_r\cong S^{d-1},
\qquad
H_r\cong r*L_r.
\]

The hyperspherical object is therefore not an ornamental sphere placed around
the receiver. It is the receiver's horizon of directions. At the center all
of those directions share one root occurrence; away from it they separate
into distinct local fibers.

In a realized vector chart with center \(c_r\), direction \(u\), and caused
support \(S\), define the exact directional depth fiber

\[
D_{r,S}(u)
=
\{\lambda:\;c_r+\lambda u\in S\}.
\]

For a projective receiver the same carrier is formed over oriented projective
rays, including the two finite branches emitted when a support crosses the
receiver's \(w=0\) horizon. A direction may carry:

- no intersection;
- one local sheet;
- several depth-ordered sheets;
- a tangent multiplicity;
- an open seam;
- a branch through infinity; or
- a return whose transported local relation closes.

The received face is a section of these directional fibers. It is not the
center, not a single center ray, and not the terminal matrix.

This supplies a computational reading of Brandon's center intuition:

> the center parents a horizon of distinguishable directions, and the caused
> world is received as the population carried in the fibers over that
> horizon.

The geometry is implicit when the local star, link, support incidence,
transport, and depth order determine those fibers without storing every
terminal consequence in advance.

## 3. Stellation separates support, arrangement, and visible face

The great dodecahedron, great stellated dodecahedron, and the other
Kepler--Poinsot polyhedra show that at least three geometric populations must
remain distinct:

1. the oriented planes or projective supports which generate a figure;
2. the cells and intersections formed by their complete arrangement; and
3. the depth-selected patches visible to one situated receiver.

Stellation changes which regions of an extended face-plane arrangement are
admitted as the polyhedron. Self-intersection means that one generating face
can cross other generating faces without every visible subregion becoming a
new source face.

For the receiver calculus, let \(\mathcal P\) be the source population of
oriented supports and let

\[
\mathcal A(\mathcal P)
\]

be their intersection arrangement. The receiver does not first demand a
terminal cellulation. It restricts \(\mathcal A(\mathcal P)\) through its
directional fibers, depth order, aperture, and contemporary observation
boundary.

This makes the number of faces and corners precise in its declared context:

- support-face count belongs to the generating source;
- arrangement-cell count belongs to the complete intersection relation;
- visible-patch and silhouette-vertex count belong to the receiver section;
- terminal address count belongs only to the finite observation membrane.

All four counts are meaningful. None may silently substitute for another.
The two supplied star-polyhedron images are useful precisely because their
radial organization, apparent convex envelope, self-intersecting supports,
and visible subdivisions are different faces of one construction.

## 4. Hexagonal order and pentagonal defect

A finite polygon can first be treated as an ordered directional section. Let

\[
u_i=
\left(
\cos\frac{2\pi i}{k},
\sin\frac{2\pi i}{k}
\right),
\qquad
v_i=s_i u_i.
\]

The ordered \(k\)-axis face has exact oriented area

\[
A_k(s)
=
\frac12
\sin\left(\frac{2\pi}{k}\right)
\sum_i s_i s_{i+1}.
\]

This is not merely a visual statistic. It exposes pairwise coupling between
adjacent directional magnitudes. Changing the cyclic order changes the
coupling, so the order of axes is causal information rather than decoration.

Hexagonal and pentagonal organizations then admit a second, incidence-based
interpretation. For a triangular local network, let \(z_v\) be the number of
triangles incident to \(v\). Its combinatorial defect is

\[
\delta(v)=6-z_v.
\]

Sixfold incidence is the locally flat reference of an equilateral triangular
phase. Fivefold incidence carries positive defect. For a closed triangulated
surface,

\[
\sum_v\delta(v)=6\chi.
\]

On a spherical phase this total is \(12\). Dually, a trivalent spherical
tiling made only of pentagons and hexagons requires twelve pentagons.

This gives the proposed pentagon/hexagon relation an exact computational
body:

- a hexagonal neighborhood can carry locally flat sixfold continuation;
- a pentagonal neighborhood can carry one unit of closure or curvature
  defect relative to that phase;
- the distribution of defects carries the global closure while remaining
  locally placed; and
- a physical event can transport, split, join, emit, or absorb defect only
  according to the changed incidence and boundary.

With non-equilateral local geometry, the exact angular defect

\[
\kappa(v)=2\pi-\sum_{f\ni v}\theta_f(v)
\]

replaces the unit combinatorial defect. The combinatorial and metric
populations should both be retained when the contemporary law supplies them.

This does not reduce Brandon's proposal to a fullerene analogy. It gives the
engine a quantity to compute: a defect current over the actual causal
incidence, with total change explained by topology, boundary, or a declared
local rewrite.

## 5. Successive polygonal faces form a higher object

Let \(P_t\) be the finite directional polygon received at event \(t\). A flat
overlay

\[
\bigcup_t P_t
\]

erases chronology. The ordered object is instead the event-lifted tube

\[
\mathcal W_{[a,b]}
=
\bigcup_{t\in[a,b]}
\{t\}\times P_t,
\]

together with the actual transition which identifies or transports
constituents between consecutive sections.

If axis identities persist, corresponding vertices and edges sweep faces in
this lifted object. If they permute, split, join, or depart, the causal
transition supplies the changed incidence. Relative rotation produces a
twisted tube; scale change produces a tapered tube; a pentagonal defect
moving through a hexagonal ecology traces a defect worldline; a complete
return closes the tube with its holonomy.

For discrete events the chronology-preserving chain is

\[
\Gamma_{[a,b]}
=
\sum_{t=a}^{b}
[t]\otimes P_t.
\]

Further quotients can be derived according to the question:

- an envelope records extremal reach;
- an occupancy measure records how long regions are carried;
- an arrangement records every crossing between sections;
- a return quotient identifies an exact transported recurrence; and
- a terminal image observes one finite section or projection.

This is the rigorous computational opening in Brandon's statement that
relative polygonal orientation over orders of time describes an emergent
collective trajectory. The collective object is not obtained by discarding
time. It is formed by lifting the faces into causal order and gluing them
through the actual event transitions.

## 6. Receiving organs are directional covers

An ommatidium has an optical axis and a nonzero angular, spectral, and temporal
response. Let the response of receiving organ \(i\) be

\[
a_i(u,\lambda,\omega,\tau),
\]

over direction, depth or range, carried species or frequency, and temporal
phase. Its contribution to a contemporary stimulus section \(I\) is a local
integral or exact discrete pairing over that acceptance region.

A compound eye is therefore a cover of directional space by overlapping
acceptance sections. Its resolution is caused jointly by:

- the distribution of optical axes;
- each organ's acceptance width;
- overlap between neighboring organs;
- heterogeneous receptor species;
- temporal integration; and
- the organism's transport and comparison law.

Repeated receiving organs need not be identical. Box-jelly eye types,
regional compound-eye specialization, distributed brittle-star
photoreception, and lateral-line neuromasts are different receiver ecologies:
concentrated, repeated, heterogeneous, or body-distributed.

This biology supplies a direct engine correction. A receiver should not be
identified with one ray or one terminal sample. It owns an acceptance
section over its link, and the receiver population owns an overlap nerve.
Hexagonal packing may organize a locally efficient receptor distribution;
curvature, boundary, growth, and specialization produce lawful defects and
heterogeneity.

## 7. Probability is caused measure over a geometric basin

Let \(X\) be the exact configuration or phase space of a bounded world and let
\(\Phi_d:X\to\operatorname{Path}(X)\) carry one supplied deed to its complete
caused path or rested successor. Let receiver \(r\) declare a consequence
region \(C_r\) in path or terminal space.

The causal success basin is

\[
B_r(d)
=
\{x\in X:\Phi_d(x)\text{ meets the declared consequence }C_r\}.
\]

A source preparation, symmetry, inherited population, or receiver-relative
conditioning supplies a measure \(\mu\) on \(X\). Probability is then the
measure of the caused basin:

\[
\Pr_r(C_r\mid D,d)
=
\frac{\mu(B_r(d)\cap D)}{\mu(D)}.
\]

The geometry supplies the configuration space, basin, intersections,
boundaries, and transports. The preparation or invariance law supplies how
that geometry is weighted. These are composable parts of one construction,
not rival explanations.

Loss first remains a geometric body:

\[
\mathcal L_r(d;D)
=
D\setminus B_r(d).
\]

From that body a receiver may derive several related testimonies:

- rejected measure \(\mu(\mathcal L_r)\);
- boundary current across \(\partial B_r\);
- minimum caused transformation carrying a point into \(B_r\);
- action accumulated along such a path;
- obstruction preventing a local section from gluing into the basin; or
- information erased when distinct source points share one observed outcome.

The body, measure, path, action, obstruction, and quotient are retained as
different faces. A single scalar called `loss` would erase the topology which
Brandon is asking the engine to expose.

The nonadditivity of unknotting number under connected sum supplies a concrete
warning and opportunity: a minimum local-change path for a composite geometry
can be shorter than the sum of independently minimized paths. Collective
loss may therefore live in the joined path space rather than in additive
component scores.

### 7.1 Conditioned particles

For candies or particles in a container \(D\), the configuration space is not
merely the set of colors. It includes the admitted particle positions,
orientations, marks, nonintersection constraints, and container boundary.
A hand, aperture, current, or selection membrane defines a receiver map from
that configuration to a selected particle occurrence.

Exchangeable counting, uniform independent placement, hard-particle
equilibrium, and a shaken nonequilibrium jar are different measures over the
same broad geometry. Each can be computed when its preparation law is
declared.

Buffon's needle obtains its probability from translation and orientation
measure over a geometric fundamental region. The broken-stick problem obtains
its familiar simplex geometry from a particular break procedure. Random
polygon models obtain measures by transporting homogeneous measures from
spheres or Stiefel manifolds into polygon spaces. These are examples of how a
geometric probability is constructed, not reasons to postpone construction.

### 7.2 The supplied pentagonal game face

Let \(H\) be the hero polygon, \(Q\) the conditioned challenge polygon, and
let \(x=(p,v,\ldots)\) contain the complete initial ball phase. A collision or
flight law induces a terminal or first-hit map \(h(x)\).

The exact success basin is

\[
B=\{x:h(x)\in H\cap Q\}.
\]

If the terminal point is uniform over \(Q\), the quotient reduces to

\[
\frac{\operatorname{area}(H\cap Q)}
     {\operatorname{area}(Q)}.
\]

If the ball moves and reflects, the induced hitting measure can be
nonuniform, and the phase-space basin is the more informative object. Both
versions are computable. The screenshot and Reddit discussion are useful
semantic evidence for the distinction among overlap, boundary, trajectory,
and displayed success percentage; the game's particular choice is not being
installed as engine doctrine.

## 8. Deduction, induction, abduction, and skein computation

The causal geometry admits four complementary calculations.

### Deduction

Given a source standing \(x\), deed \(d\), exact event law \(\Phi_d\), and
receiver \(R_r\),

\[
x
\xmapsto{\Phi_d}
x'
\xmapsto{R_r}
y_r
\]

computes a necessary receiver consequence. This is the engine's forward
causal direction.

### Structural induction

A property of the geometry may be proved or computed over:

- causal layers;
- attachment of simplices;
- star-link refinement;
- boundary subdivision;
- a complexity-decreasing local resolution; or
- a founded receiver cover.

The base and attachment steps must preserve the same declared relation. This
is discrete mathematical induction carried by geometry rather than by a bare
integer counter.

### Receiver-relative induction and abduction

Given one or more receiver sections \(y_r\), the compatible source fiber is

\[
\mathcal V(y)
=
\bigcap_r R_r^{-1}(y_r).
\]

New observations restrict this version space. A measure, action, or causal
economy can compare its remaining regions. This is the proper place for
geometric inference: not guessing a scalar source from one face, but
transporting and intersecting complete compatible source regions.

The Wason falsification pattern becomes an exact geometric test. For a
proposed implication \(P\Rightarrow Q\), the discriminating region is

\[
P\cap\neg Q.
\]

Boundary, horizon, singular, inconsistent-rebase, and non-gluing examples are
therefore active search regions rather than afterthoughts.

### Skein computation

A skein relation supplies a local family of alternate crossings or
resolutions and a relation among their consequences. In the present calculus
it can serve two different roles:

1. an observational reduction which computes an invariant of a formed
   crossing arrangement; or
2. a physical alternative only when an actual application law admits the
   corresponding local rewrite.

A resolution graph can be evaluated recursively when the local relation,
base populations, and overlap compatibility are supplied. Intermediate
resolution geometry should remain inspectable, because two local resolution
paths may interact before they reach the same quotient.

## 9. Recurrence is carried consequence

The present research already distinguishes:

- a caused physical recurrence;
- equality of complete exact standing;
- a repeated causal support with an open frontier;
- exact transport or holonomy;
- retention of an unchanged receiver tube;
- affine rebase of one complete section; and
- repeated terminal observation work.

The new synthesis adds the event-lifted shape tube and defect current. A
recurrence can now be witnessed as:

\[
\text{later section}
=
\text{transported earlier section}
\]

with its incidence, directions, depth fibers, phase, and boundary current
carried through the return. The later consequence was implicit in the
relation when this transport can be enacted without reconstructing an
absolute terminal world.

This is not naïve cache reuse. It is a factorization of the requested
consequence through already-standing local geometry.

## 10. Fast multipole transport is one computable exterior species

The Greengard--Rokhlin fast multipole method supplies a concrete model of how
a bounded local body can carry its distant consequence without replaying
every source interaction:

- near interaction remains direct;
- a declared kernel supplies an exterior expansion;
- local sources fold into expansion coefficients;
- coefficients translate between centers;
- distant expansions become local receiver expansions; and
- a separation and remainder law governs admission.

The center is an expansion anchor whose carried coefficients summarize an
exterior face. The hierarchy is a changing cover of source and receiver
regions, not a uniform matrix.

For holonic exactness, an application can admit an FMM-like fold when it
supplies:

1. the interaction kernel or constitutive exterior law;
2. the local and exterior expansion species;
3. exact translation relations between receiver centers;
4. a finite rational or interval certificate for the omitted remainder; and
5. a direct near-field path which composes with the folded far field.

This does not need to become one universal fold before it is useful. Each
admitted physical species can carry its own exact exterior construction.

### 10.1 Points as tori joins the local body to the output-sensitive field

Feng, Gkioulekas, and Crane's `Points as Tori` construction gives each
oriented point-cloud sample a locally fitted torus. The torus carries two
principal curvatures and directions plus a normal shift; its analytical signed
distance is evaluated only at requested points. A self-normalized
distance-weighted assembly of the local torus fields provides the collective
signed-distance estimate without first building a spatial grid.

This is directly related to the receiver program:

1. an apparently dimensionless point carries a curvature-bearing local body;
2. that body is situated by its tangent frame and normal orientation;
3. its conic sections are lower-dimensional receiver faces of the local body;
4. overlapping local bodies form a partitioned collective field;
5. evaluation is output-sensitive rather than proportional to an authored
   terminal lattice; and
6. the paper itself identifies hierarchical Barnes--Hut and fast multipole
   assembly as compatible acceleration.

In an exact local chart with torus center \(c\), axis coordinate \(n\),
orthogonal radial coordinates \(s,t\), major radius \(R\), and minor radius
\(r\), the torus support is the quartic relation

\[
\left(s^2+t^2+n^2+R^2-r^2\right)^2
-4R^2\left(s^2+t^2\right)=0.
\]

The support is therefore available to the holonic exact-value tower without
turning the learned floating implementation in the paper into source
authority. A physical or learned world may supply the local curvature,
orientation, and shift. The engine can retain the exact torus relation,
substitute an exact receiver ray, isolate its resulting polynomial
intersections, and carry its receiver-relative quartic sign and zero-depth
fiber.

The normalized collective field has the form

\[
\Phi(x)
=
\frac{\sum_i g_i(x)w_i(x)}
     {\sum_i w_i(x)}.
\]

This is simultaneously:

- a partition of unity over overlapping local bodies;
- a geometric expectation of local receiver testimony;
- a probability-like normalization caused by the query and receiver grain;
- and an exterior field which can be folded hierarchically when its kernel
  and error law are admitted.

It supplies a concrete bridge among local receiver sections, geometric
probability, analytical output-sensitive observation, and FMM. The immediate
engine construction—an exact curvature-bearing local implicit support species
whose receiver consequence is evaluated without terminal
retessellation—is completed in Section 11.3.

## 11. Completed construction

The exploratory program above has now corrected the production
`holonic-engine`. The construction is not an application-side interpretation
of old receipts. The physical transition, receiver membrane, tube atlas, and
terminal restriction own the new carriers.

### 11.1 Relative spatial standing

`LocalStarStanding` no longer stores physical absolute vertex coordinates.
`LocalSpatialStanding` is an oriented edge \(1\)-cochain:

\[
\xi_{uv}=x_v-x_u.
\]

It retains exact capacities, face-closure residuals, and spanning-tree/chord
residuals. A component-local point realization is derived only when a
receiver or outer construction asks for one. A common translation is
therefore literally absent from physical standing, and permuting receiver
deeds cannot select a privileged origin.

`HingeWorldStanding` owns the contemporary `SimplicialComplex`; the law owns
only its founding scaffold. One identity-preserving \(2\)-to-\(2\) hinge flip
keeps the hinge and two face identities, preserves the complete oriented
exterior boundary, rederives every coface, rewrites the spatial cochain, and
commits atomically.

### 11.2 The receiver opens into its oriented link

Every incident oriented face now emits one `OrientedLinkCell`. Deleting pivot
vertex \(i\) carries the induced \((-1)^i\) hand, so a regular interior link
joins head-to-tail without using map order.

`ReceiverAcceptanceCover` contains:

- one exact organ per oriented link cell;
- its two boundary directions, central direction, and oriented area;
- one overlap seam per link vertex, with plural incoming and outgoing organs;
- the live link class—cycle, path, singular, or empty; and
- a finite port section derived from the receiver's declared hinge and its
  two opposed cofaces.

The desktop no longer chooses `link_edges.first()`. Its finite projective
chart is formed by the production `LocalStarStanding` from the explicit port.
In the octahedral ecology each receiver has four actual organs and four cyclic
seams.

`ReceiverFace::receive_acceptance_section` computes every organ-center and
seam fiber from the already-formed continuous arrangement. Finite directions
carry depth-ordered sheet, thread, and conic crossings. Directions at the
selected chart's \(w=0\) horizon remain projective fibers with exact support
contacts. A direction collapsed at the receiver remains a typed singular
fiber rather than aborting the other organs.

### 11.3 Native local implicit bodies

`ExactTorus` is a rational local body with a non-normalized rational symmetry
axis and positive rational major/minor radii. It remains its
denominator-cleared quartic. Exact ray substitution produces an integer
quartic; square-free decomposition, Sturm counting, and interval isolation
return every rational or algebraic depth with its exact multiplicity.

This establishes the immediate `Points as Tori` relation without tessellating
the torus or disguising it as a conic. A transverse ray can carry four exact
depths, while a tangent ray retains even multiplicity.

The torus is now source-owned rather than attached to an already-certified
face. `ReceiverSourceSection` retains the canonical map from implicit cell
identity to its complete native torus. The certified receiver membrane
requires exactly one implicit section for every requested receiver, checks
map/body identity and

\[
\text{source event}\leq\text{last event}\leq\text{formation event},
\]

then places that same ordered population in the face and its unforgeable
formation receipt. The receipt publicly exposes its source section and ray
family for inspection. A future-dated or mismatched implicit body cannot
enter standing.

Literal retention of an unchanged torus section remains lawful. Exact affine
rebase is conservatively refused whenever either section contains an implicit
torus: a general affine map does not preserve the native circular-torus
species, and the engine has not declared a narrower similarity carrier for
it. Changed optics or changed torus material therefore causes fresh
factorization instead of false recurrence.

Thread and conic crossings also participate in first-person `CrossingLayer`
depth order. A receiver query is therefore no longer triangle-only.

### 11.4 Current lineage and the event-lifted tube

The scalar open-frontier map is now derived testimony. Physical standing
retains plural `LocalCurrentConstituent` values:

\[
(\text{root occurrence},\text{current hinge},\text{amount},
 \text{ordered incidence word}).
\]

Equal amounts remain distinct when their source or receiver roots differ.
Every local solve returns constituent-level deposited, emitted, departed, and
zero-residual testimony.

`ReceiverTube` separates founding and contemporary events. It retains the
ordered presentation word and every `ReceiverTubeTransition`, including an
event whose finite trace was carried. A physical occurrence can therefore not
disappear merely because observation reused the same section.

Face formation is certified by an engine-owned
`ReceiverFaceFormationReceipt`. Retention and exact affine rebase are checked
against source section, ray family, topology, event, and complete face.
Changing only the contemporary acceptance-cover event advances the tube while
requiring its organ geometry to remain exact.

### 11.5 Defect follows live incidence

The coordination defect is derived from each contemporary vertex link:

\[
\delta(v)=6-|\operatorname{Lk}(v)_0|
\]

for an interior cycle. Boundary paths, singular links, and isolated vertices
remain distinct species.

An admitted flip emits every changed local defect and an exact charge
residual. On the closed octahedral sphere, the initial total is \(12\); one
legal flip changes four local charges and preserves total \(12\) with
residual zero. A flip which would detach a receiver's declared port from its
anchor is refused.

### 11.6 Probability and loss are a caused basin

`ExactConfigurationComplex` is the first rigorous bounded configuration
species. Each cell supplies only:

- an exact source standing;
- a deed;
- exact preparation measure; and
- exact adjacency/action to neighboring configurations.

`enact_outcome_basin` invokes the same production `ExactEventLaw` separately
on every immutable predecessor. The application does not supply a successor.
A receiver-relative observation then forms:

- the complete outcome quotient;
- accepted and rejected induced subcomplexes;
- connected components and graph-cycle rank;
- exact crossing boundary;
- exact accepted/rejected measure and probability; and
- a minimum-action corrective path from every reachable rejected cell.

The rejected body is `ExactGeometricLoss`; its measure is one derived face,
not its replacement.

The production local-star test prepares negative, zero, and positive source
currents with equal measure. The physical law itself yields one accepted
configuration, hence

\[
\Pr=1/3,\qquad \mu(\mathcal L)=2,
\]

with exact corrective actions \(2\) and \(1\) across the declared
configuration adjacency.

This is an exact finite-complex construction. It does not claim that an
arbitrary undecomposed continuous phase space has acquired a certified cell
decomposition.

### 11.7 Structural terminal restriction

The CPU and CUDA aperture paths already evaluated primitives independently
but formerly collapsed those results into one receiver aggregate.
`ReceiverApertureTrace` now retains one `PrimitiveApertureTrace` per exact
`(receiver, primitive)` key. Receiver addresses and multiplicities are
derived from that population.

`TerminalTubePlan` compares contemporary and retained primitives before
terminal descent. It now:

- carries unchanged primitive restrictions inside a changed receiver;
- retraces only changed structural keys;
- shares identical changed primitives through one representative;
- reassembles exact receiver multiplicity from retained and returned parts;
  and
- emits the same receiver-level support deltas as a full trace.

The admitted host/CUDA executor receives the filtered continuous
presentation, so this is the live execution route. In the two-conic grading
case, changing one conic traces one key, reuses the other key and its query
receipt, and equals the complete fresh receiver authority exactly.

### 11.8 Exact normalized local-field folding

The pointwise normalized carrier remains the pair

\[
(N,D)=\left(\sum_i g_iw_i,\sum_i w_i\right)
\]

until a receiver asks for \(N/D\). Zero denominator remains a typed seam.
Every `ExactNormalizedFieldTerm` now retains source, event, value, weight, and
identity. A declared partition folds the terms componentwise and returns
direct and folded pairs with exact numerator and denominator residuals.

This is exact recurrence of already-evaluated additive local testimony. It is
not renamed as a general FMM expansion. A genuine far-field law still
requires a declared kernel, exterior/local moments, center translation, and
an exact rational remainder certificate.

## 12. What the completed tests establish

The complete host grade passes:

- 81 `holonic-engine` library tests;
- 5 desktop receiver correctness tests;
- 2 intentionally ignored CUDA/device-cost tests whose declared external
  grading conditions were not requested here; and
- strict crate-local Clippy with `--no-deps`; and
- `git diff --check`.

The new discriminating receipts establish:

1. common translation is absent from spatial standing;
2. receiver-deed order cannot select a physical origin;
3. source and receiver current roots remain plural under equal total current;
4. each octahedral receiver owns four oriented organs and four overlap seams;
5. every desktop organ and seam computes a first-person direction fiber;
6. a live hinge flip preserves oriented closure and total coordination charge;
7. a certified source-owned torus produces a quartic first-person fiber,
   while future chronology is refused; independent torus tests return four
   certified depths and preserve tangent even multiplicity;
8. finite causal probability/loss is enacted by `LocalStarLaw`, not calculated
   by an application surrogate;
9. an event transition remains in a tube even when its terminal trace carries;
10. one changed primitive retraces without retracing its unchanged receiver
    neighbors; and
11. normalized field partitions fold with exact zero residual.

Serial and multicore engine results remain identical. Complete direct
receiver tracing remains the terminal grading authority.

Strict Clippy with dependency linting is not presently a clean repository
grade: the pre-existing `relational-geometry` crate has large-error,
large-enum, and style lints. No unrelated categorical geometry source was
changed to conceal that fact.

## 13. Exact limits of this construction

The completed carriers establish a substantial computation, but they do not
collapse distinct unknown laws:

- The finite configuration complex gives exact discrete/cellular probability
  and loss. A continuous phase space still needs a law-specific exact or
  certified decomposition and measure transport.
- The acceptance cover derives topology, directions, seams, and current
  support fibers. Organ-specific spectral, temporal, or angular response laws
  have not been invented.
- The finite desktop presents one explicit port chart of the full cover. It
  does not yet identify one universal terminal quotient of every possible
  compound-eye response.
- `ExactTorus` supplies exact receiver-local quartic zero fibers, not the
  paper's learned curvature fitting procedure or floating exponential
  weighting. Literal recurrence is admitted, but no projective or general
  affine torus-transport law is claimed.
- The normalized-field fold is exact after local terms are evaluated. It is
  not an unevaluated kernel expansion and claims no asymptotic FMM speedup.
- Composed skein worlds and nonadditive knot-correction loss remain
  mathematically specified research species, not completed physical laws in
  this bounded ecology.

These are typed boundaries, not substitutes for the work completed above.

## 14. Deposited authority

The resulting receiver engine now computes:

\[
\boxed{
\begin{array}{l}
\text{relative causal incidence with no physical absolute origin},\\
\text{a center whose oriented link is a plural cover of receiving organs},\\
\text{sheet, thread, conic, horizon, and quartic depth fibers},\\
\text{lineage-preserving current and event-lifted receiver tubes},\\
\text{live topology rewrite with exact defect transport},\\
\text{receiver-relative caused basins and geometric loss},\\
\text{primitive-grain exact terminal recurrence},\\
\text{and provenance-preserving normalized local-field folds}.
\end{array}}
\]

Recurrence now means a proved factorization through contemporary local
geometry: an earlier section, primitive restriction, or additive fold can
carry only when its identities, incidence, transport, event transition, and
exact residual remain inspectable.

## 15. Supplied research relations

The synthesis used the supplied sources in these declared roles:

- [Unknotting number is not additive under connected
  sum](https://arxiv.org/pdf/2506.24088): minimum local-change paths can
  interact nonadditively after composition.
- [Skein relation](https://en.wikipedia.org/wiki/Skein_relation): a local
  alternate-resolution calculus.
- [Wason selection
  task](https://en.wikipedia.org/wiki/Wason_selection_task): discriminating
  evidence for an implication lies in its possible falsifying region.
- [Great
  dodecahedron](https://mathworld.wolfram.com/GreatDodecahedron.html),
  [great stellated
  dodecahedron](https://mathworld.wolfram.com/GreatStellatedDodecahedron.html),
  [Kepler--Poinsot
  polyhedra](https://mathworld.wolfram.com/Kepler-PoinsotPolyhedron.html), and
  [stellation](https://mathworld.wolfram.com/Stellation.html): generating
  supports, intersection arrangements, and visible subdivisions are
  different geometric populations.
- [Greengard--Rokhlin fast multipole
  method](https://math.pku.edu.cn/teachers/litj/notes/numer_anal/FMM_Greengard_Rokhlin.pdf)
  and its [adaptive
  form](https://math.nyu.edu/~greengar/cgr_88.pdf): kernel-specific local and
  exterior expansions, center translation, near/far separation, and bounded
  remainder.
- [Points as Tori: Fast Pointwise Signed Distance for Point
  Clouds](https://nzfeng.github.io/research/PointsAsTori/PointsAsTori.pdf):
  curvature-bearing local torus bodies, analytical pointwise signed-distance
  queries, normalized local-field assembly without spatial discretization,
  and a direct route to hierarchical or fast-multipole acceleration.
- [Probability Theory of Random Polygons from the Quaternionic
  Viewpoint](https://arxiv.org/pdf/1206.3161): geometric probability
  transported from homogeneous sphere and Stiefel measures into polygon
  spaces.
- [Drosophila eye development](https://pmc.ncbi.nlm.nih.gov/articles/PMC2890271/),
  [box-jelly eye organization](https://pmc.ncbi.nlm.nih.gov/articles/PMC2825319/),
  [brittle-star distributed
  photoreception](https://pmc.ncbi.nlm.nih.gov/articles/PMC5805950/), and
  [lateral-line receiving
  organs](https://www.ncbi.nlm.nih.gov/books/NBK53171/): repeated,
  heterogeneous, and body-distributed receiver covers.
- The conversation-supplied `Lost Balloon` screenshot and its
  [informal mathematical
  discussion](https://www.reddit.com/r/askmath/comments/1omnor3/the_recently_released_video_game_dispatch_has_a/):
  semantic evidence for reasoning about polygon overlap, boundary, moving
  phase, and displayed probability without treating the game as mathematical
  authority.

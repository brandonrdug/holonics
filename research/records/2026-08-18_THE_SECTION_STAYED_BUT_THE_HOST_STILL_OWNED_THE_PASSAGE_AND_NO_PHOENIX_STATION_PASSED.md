# The section stayed, but the host still owned the passage, and no Phoenix station passed

**Date:** 2026-08-18  
**Kind:** adversarial implementation audit against the Phoenix master, Gemma instance, and live
roadmap.  
**Truth status:** `established-bounded` for reproduced source/artifact measurements;
`counterexample` for the promoted resident-passage, complete-work, source-binding, interchange and
falsifier claims contradicted below; `open` for every Phoenix master station.  
**Position boundary:** [`CONSTRUCTION_STATE.md`](../../CONSTRUCTION_STATE.md) does not move.  
**Audited tree:** `45e7c09` plus Claude's uncommitted resident-section construction.

---

## 0. Verdict

**Truth status: established-bounded for the CUDA site; counterexample for the promoted gate.**

One useful apparatus return stands: a Gemma layer-zero fixture carried interval section coordinates
through real CUDA kernels, copied no intermediate section vector to the host, and returned one
terminal interval section.

The stronger claim does not stand. The semantic chronology is still selected by a host interpreter;
every local operation globally synchronizes and returns to that host; the composed deed was not
admitted before its first launch; the source bindings are not individually authenticated; selected
serial permutations are not physical interchange; and the physical/fluid owners do not compose into
the path. No Phoenix master station passed.

## 1. What returned and remains worth keeping

**Truth status: established-bounded.**

- `resident_section.rs` carries `[lo, hi]` section words at a declared dyadic grain on CUDA.
- BF16 entry, contraction, RMS rebase, chronology, contact/value transport, GELU, Hadamard,
  re-entry and scale have resident kernels.
- Selected source maps mount once and are reused.
- The recorded deed makes one terminal section read-out and no intermediate section-vector read.
- A finer declared grain/series aperture nested `5,120` of `5,120` terminal enclosures on the
  selected two-token fixture.
- Both K/V families and the PLE branch are present and intervention-visible.
- Hiding the card returns typed apparatus refusal instead of a CPU arithmetic answer.
- `hardware_cover::Barrier` was correctly narrowed to `CoverBarrier`.

The artifact is
`output/the_layer_stays_on_the_card/layer-0-resident-2-tokens-grain-48-terms-14.form`. These are
bounded layer-zero facts, not a model, native ecology, or inference grade.

## 2. The interpreter moved and changed names

**Truth status: counterexample.**

The rejected `PortedOperationKind -> realize/enact` path became:

```text
ResidentBinding
-> FrontPassage::conduct
-> FrontEnactor::enact_in
-> FrontPassage::enact_member
-> host match over operation variants.
```

`front_passage.rs:71-99` declares the instruction population; `:832-980` loops through fronts on
the CPU; `:1003-1071` selects the resident operation by a host `match`; and `:1103-1139` loops
through every member of every certified order.

The production-cone gate at `resident_section.rs:1887-1910` searches only for the old spellings.
It does not ask whether a replacement enum and dispatch loop own the same decision. The gate passes
under renaming and therefore cannot promote the claimed resident passage.

## 3. The section stays while the current returns to the host

**Truth status: counterexample.**

`resident_section.rs:700-790` launches on the default stream, calls `cuCtxSynchronize`, and reads a
32-octet scratch/refusal/census block. The recorded deed reports:

```text
launches             180
synchronizations     180
receipt egress       5,760 octets = 180 x 32
section read-outs    1
```

The vectors stay resident; the semantic current does not. The CPU regains control after every local
kernel and selects the next semantic variant. This is GPU arithmetic under a host foreman, not a
GPU-owned circulation.

## 4. Complete pre-deed admission did not occur

**Truth status: counterexample.**

The complete fixture is two passages: six PLE/input fronts, then twenty-three layer fronts. The
first executes before the second is priced. The receipt records only `deed.layer.deed_prediction`;
it does not admit all twenty-nine fronts before the first PLE launch.

The vector also omits D-to-D standing copies, `180,224` measured staging octets, endpoint comparison
and census kernels, scratch traffic, allocations, synchronizations, terminal egress, and roughly
`749,840,384` mounted-map octets. Factorial order populations are materialized before front-local
admission. The driver control proves only that one subpass can refuse before it launches. Its `2^62`
ceiling is an authored experimental value, not a derived apparatus capacity.

## 5. Source and cross-chart testimony remain incomplete

**Truth status: counterexample.**

`SourceAuthentication` verifies the SHA-256 of `modeling_gemma4.py`, but `compile` does not verify
that each `Implementation.symbol` exists or entails the binding. Configuration and shape testimony
are accepted without checking authenticated configuration/container content. The model
safetensors occurrence is not content-addressed in the layer receipt.

**Truth status: established-bounded for the comparison; counterexample for source equivalence.**

The exterior comparison places the float32 source face inside `2,910` of `5,120` resident
enclosures and the BF16 source face inside only `9` of `5,120`, with worst BF16 gap near `9.2e-2`.
The resident body is an interval realization of an ideal exact-real formula over BF16-stored
weights, not an enclosure of the source implementation's rounded transport. The difference must
return as `chi_gamma = Phi_Y T_gamma - S_gamma Phi_X` with its reconstruction fibre; prose beside a
hand-run script does not propagate it.

## 6. Serial order, cover, and fluid/hardware testimony

**Truth status: established-bounded for serial permutation agreement; counterexample for physical
interchange.**

The certificate re-enacts every permutation sequentially, globally synchronizes each member, and
compares staged endpoints. It does not vary concurrent completion, CUDA streams, or interleaving,
and its face is narrower than the complete successor/resource/remainder required by the governing
interchange law.

`FrontPassage::conduct` receives a cover independently of `ResidentSurface`; it does not prove that
cover device, PTX, CUDA context and mounted source share one `ModeIdentity`. The new production cone
does not compose `receiver_current`, `approach_front`, `traversible_chain`, carrier dilation,
continuity residual, reflection, or local/partial pressure. `resident_section` also repeats CUDA
FFI/device-census ownership assigned to `soma/mount`.

## 7. Exactness and falsifier defects

**Truth status: counterexample.**

`exact_resident_section.cu:54-68` left-shifts signed negative `__int128` values. That is undefined
in the C++ language used for the kernel and is exercised by negative source coordinates. Use checked
unsigned magnitude and restore the sign before claiming exactness.

Falsifier 13 records literal `true` and cites another test. Falsifier 24 checks only that launches
increased and cover cells were labelled device, ignoring the CPU/utilization evidence named by the
claim. The “fresh input” control is same-process source-fed stimulation with the source open, not
fresh-process source-detached inference.

## 8. Station grades

**Truth status: open unless explicitly bounded or counterexample above.**

| Phoenix station | audited grade |
|---|---|
| source admission | bounded manifestation/mouths; complete manifest open |
| potential lift | bounded generic substrate/proposed diagram |
| active stimulation | bounded idealized layer-zero fixture |
| ported operation complex | bounded layer-zero binding; whole realization open |
| source dissection | bounded K/V intervention only |
| native ecology and cross-chart lift | open |
| condensation/decoder | open |
| cultivation | current attempt refuted; BF16 mutation mechanism retained |
| native rest/remount | open |
| runtime/export/frozen inference | open |
| native recombination/reborn dissection | open |
| non-text portability and independent native baseline | open |

## 9. Disposition

**Truth status: open construction requirement.**

Preserve the interval kernels and resident handle. Before tower, rest, export, condensation or
cultivation work: remove the host semantic dispatch; return one front/deed delta instead of
per-kernel host consultation; bind cover/device/context/PTX/mode; admit all work before any launch;
type global reductions by real receipts; authenticate model/configuration/symbols/shapes jointly;
carry the source-runtime defect; repair signed arithmetic and vacuous controls; and compose the
existing flux/capacity/refraction/pressure owners. Only that corrected complete-layer return reopens
the tower. `CONSTRUCTION_STATE.md` remains unchanged.

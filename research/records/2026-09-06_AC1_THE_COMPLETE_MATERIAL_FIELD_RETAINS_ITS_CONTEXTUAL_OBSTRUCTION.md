# AC1: the complete material field retains its contextual obstruction

**Authority:** the active [Athena-alpha blueprint](../../docs/plans/THE_ATHENA_ALPHA_CULTIVATES_GENERAL_CONVERSATION_THROUGH_NATIVE_CONTEXTUAL_TRANSPORT.md).
**Position:** [AC1 remains active](../../CONSTRUCTION_STATE.md). This is a component return;
the whole AC0–AC5 goal and the actual language product remain open.

## Material and native operation

[definition] [`NativeConstitutiveField`](../../crates/holonic-engine/src/native_ecology/constitutive_fibre/field.rs)
extends the existing local constitutive relation and rational phase-junction law to separately
addressed input ports. For each node with positive seed admittances `Ya,Yh`, unit-phase incidence,
transported arrival `a` and held current `h`, the exact junction is

```text
v = 2 (Ya a + Yh h) / (Ya + Yh)
out = v - a,        held_next = v - h.
```

[definition] A source section retains **both** complex branches per node, interleaved as
`[out.re,out.im,held.re,held.im]`. The receiver is the complete next incoming complex field in its
root chart. Thus the fixed local relation has source width `4N`, target width `2N`, and total width
`6N`. An actual, one-use, owner-bound source handle joins the later arrival; equal fields or source
ordinals cannot fabricate that capability. Unlinked arrivals still change held standing but do not
invent a paired observation.

[established-bounded; source-inspected] The
[`device operation`](../../crates/holonic-engine/kernels/constitutive_field.cuh) reuses the existing
checked rational elimination. It generates the field, reads the former source, stages the actual
paired row, reads the successor relation, and converts every continuing word before the only basis
and held-state writes. Arithmetic refusal preserves the live owner and source handle; uncertain
device completion retains pending lineage and prevents replay. No host float, token difference,
semantic selector or fitted input/output array determines the native source.

[definition] [`OctetExcitation`](../../crates/holonics-hna/src/alpha/material.rs) is an exterior
reversible port chart: eight bit positions with two channels each, and one unit impulse per bit.
Every octet therefore supplies eight unit impulses to sixteen addressed input ports. Source bytes
choose their channel, not a numerical amplitude or semantic vector. This fixed calibration is an
application declaration; sixteen is not a discovered native semantic capacity.

[definition] [`alpha_material`](../../crates/holonics-hna/examples/alpha_material.rs) streams a
declared number of complete AC0 development families through one field owner. It preserves the
complete source coordinates and opaque material. Actual emission handles link consecutive octets
within one text part; part boundaries break that source comparison without resetting the ecology.
Record-parent, reply and branch relations are preserved as unbound evidence. They are not yet
operative contextual channels. Historical human feedback is never attached to a new alpha answer.

[established-bounded; measured] The `advance_status` observer executes the same native operation
as `advance`, returning only `Unique`, `OutsideDomain` or `Plural` at the receiver boundary. A CUDA
control matched the complete successor and history and measured a 2,304-byte reduction for its
two-node plural observation. The relation and its complete fibres remain resident. The field study
uses this projection to avoid copying the whole relation at every plural reading; its explicit
terminal source/held observations remain measured host transfers.

## Actual conversation slice

[established-bounded; measured] The first run used
`.local/datasets/athena-alpha-exposure-source-context-2026-09-06.jsonl` with `--families 4` and the
matched-unit seed. It completed all four development families and all four text parts: **728
octets and native operations**, native ranges `[0,74)`, `[74,208)`, `[208,411)`, `[411,728)`.
The full private report is `.local/artifacts/athena-alpha/ac1/material-first-four.json`.

| Receiver | Returned value |
|---|---:|
| Formed pivots / final relation rank | 26 / 26 |
| Outside-domain readings | 18 |
| Plural readings | 709 |
| Unique readings | 1 |
| Arithmetic failures / pending occurrences | 0 / 0 |
| Unbound record-level relations | 3 |

[established-bounded; measured] The declared device is CUDA device 0, an NVIDIA GeForce RTX 4080
SUPER with 16,376 MiB reported memory, driver 595.71.05 and compute capability 8.9. The kernel build
targets `compute_89`. These apparatus readings establish neither electrical power nor broad scaling.

[established-bounded; process-audit] The first process exited 0 in 4.366 seconds with maximum RSS
125,572 KiB. Its separately saved summary/process receipts and empty stderr remain beside the
report. These are debug-process costs for this explicit study, not a consumer release benchmark.

[established-bounded; measured] Before final observers, the native census recorded 708,608 input
octets, 3,238,416 section-output octets, 46,592 receipt octets, 728 deed launches, 728 synchronizations,
4,374 allocations and 730 section readouts. Resident allocation was 3,236,224 bytes, with peak
3,237,060 bytes. The census includes the study's first-vertical source/relation snapshots. History
is retained without condensation; these finite costs do not establish corpus-scale residency.

## What the plural fibre says

[counterexample; computational-witness] At native occurrence 17 the actual paired relation first
formed a vertical row. The
[`independent exact observer`](../experiments/alpha_source_field/inspect_linear_fibre.py) reconstructs
the matched junction, checks saved device source sections, and exhibits integer coefficients with
zero combined source but nonzero combined arriving field. The target combination equals an exact
nonzero multiple of the actual native vertical row. This rejects single-valued prediction in that
fixed linear chart on these observations; it does not reject general native language cultivation.

[established-bounded; computational-witness] The full local Hermitian pair receiver
`(|out|², Re(conj(out) held), Im(conj(out) held), |held|²)` has a nonzero combination for that first
dependency. It retains a distinction lost by the linear face. No scalar magnitude or omitted
imaginary term stands in for this receiver.

[counterexample; computational-witness] A stronger witness has equal complete local source fields
at occurrences **11 and 19**, followed by different actual arrivals at **12 and 20**. Their retained
predecessor fields at **10 and 18** differ. Consequently **no function of the present local field
alone**, including any polynomial lift of it, can equal both observed arrivals. Both source/arrival
pairs remain in the native history and the full plural fibre. Adding corpus volume or higher powers
of the same field does not remove this particular collision.

[definition] A plural receiver is legitimate retained conduct. This witness does not require
inventing a preferred representative, suppressing contradictions, or promising unique prediction
of arbitrary future input. Its practical consequence is narrower: the current field face cannot
be promoted into a complete contextual predictor. Earlier returned coefficients and source handles
are not substitutes for operative transport through the differing circumstances.

[definition] These are different subsequent exterior inputs. The witness refutes a function
from this local field to the observed next arrival; it does **not** establish different future
conduct under one identical admitted input/generator. It is therefore not a dynamic-condensation
counterexample or a failure of the complete ecology's deterministic recurrence. That stronger
comparison would need the same future operation on the relevant retained states. Operative
contextual channels remain an independently stated product obligation.

## Remaining composition

[established-bounded; source-inspected] Existing `native_spool::{scaffold,thread,resident}` owners
retain addressed higher sections and execute generator words and complex incidence on the device.
They do not currently consume this field's live emission handle. The source-neutral
`ResidentQuadraticMomentFront` / generated-port successor owners execute wider current conditioning,
but require founded factor/generator axes, receiver boundary states, restrictions and their complete
source fibres. An arbitrary conversion of field coordinates into those inputs would author the
missing morphology.

[open] AC1 still owes the actual binding from available record circumstances and changed native
standing to operative contact, the complete mixed source, and lawful carrier growth/rebase. The
field's relation changes under observations, but its fixed junction morphology does not yet use
that relation to generate the next held current. No text is emitted by the learned relation in this
return. Learned codec conduct, the cultivated model, restart and useful responses remain AC2–AC5
obligations. Neither this record nor frame transport closes them.

[definition] The reproducible private witness commands and scopes are in the
[experiment README](../experiments/alpha_source_field/README.md). Exact phase fields can reconstruct
private source text and are published with private permissions. No source database was changed,
no retired connector was used, and no Lean code was executed in this material pipeline.

[established-bounded; measured] After live-frame integration, a second four-family run saved direct
device source sections at 10, 11, 18 and 19, plus the first vertical source and final source.
`.local/artifacts/athena-alpha/ac1/material-history-readbacks.json` and
`verified-linear-and-history-witness.json` retain this return. All six source reconstructions match
their native readbacks. The complete final held state, source section, paired basis and incoming
lineage agree with the original run. The second process exited 0 in 4.604 seconds with maximum RSS
127,268 KiB. Its native census adds the initial resident frame: 709,376 ingress octets, 4,376
allocations, 3,236,992 resident bytes and 3,237,828 peak bytes before final observers; operation and
reading counts are unchanged. An intentionally altered native snapshot was refused by the exact
observer. These inspections do not change any productive native coefficient.

## Live frame transport and checked integration

[definition] The same field now reuses `NativeCurrentFrame`, `NativeRechartReceipt` and
`NativeIncidenceChange` from the scalar owner.
[`field/rechart.rs`](../../crates/holonic-engine/src/native_ecology/constitutive_fibre/field/rechart.rs)
stages a fixed-node rational unit-phase re-expression. At each node a gauge rotates incoming
incidence, initial/current held phase, root frame, and **both** source branches of every paired
relation row. The root receiving field stays fixed. Only the complete successful staged return
transfers the one owner's representation; old source sections and their producing frames remain
immutable. Linked reception crosses `current_frame * conjugate(source_frame)` on the GPU.
`NativeFieldLineage::frame` and `source_frame` expose the producing chart for historical inspection;
these observations do not manufacture a new receiving capability or a contextual identifier.

[established-bounded; measured] Eleven CUDA field controls passed together, including live rechart
followed by reception through an old handle, transported relation-span equality, receiver equality
modulo the actual source domain, historical source-byte preservation, and the actual held successor.
The physical-incidence control changes held current and then later outgoing current at the changed
node, leaving the other node fixed. Shape and arithmetic refusal preserve the owner and old handle.
The eight original field controls also cover weighted junction balance, complete two-branch source,
actual-source attribution, full plural fibre, and the status observer's unchanged successor.

```sh
env PATH=/opt/cuda/bin:$PATH cargo test -p holonic-engine --lib native_ecology::constitutive_fibre::field -- --ignored --test-threads=1
```

[established-bounded; process-audit] The integrated test run returned 11 passed, 0 failed, with
8.26 seconds of test execution. An earlier Rust test compilation exposed a moved observer vector;
it was repaired before this successful run. Review also corrected the staged historical-source
scratch so it cannot overwrite the current held successor, and checked rechart coefficients after
elimination. These are verified implementation repairs, not additional learning capabilities.
The earlier unchanged scalar circulation/rechart/rest controls had returned 20 passed. No formal
source, proof import or native scalar recurrence law changed in this field extension.

[definition] Coordinate transport is one necessary binding of the wider source chart. It does
not remove the fixed-field collision, make record-parent links operative, or establish alpha's
language usefulness. The active position continues at the concrete AC1 obligations above.

[established-bounded; measured] After exposing the producing frame in lineage and historical
inspection, the three rechart/physical-change/refusal controls passed again with those assertions.
Both `alpha::material` tests passed over all 256 exterior octets, and the final
`cargo build -p holonics-hna --example alpha_material` returned successfully. The two existing
dead-code warnings in the older `operative_return` owner remain outside this change. The final
material receipt was taken before the additional frame ordinal was exposed in the JSON lineage;
that metadata extension changes no native section or material-study kernel law.

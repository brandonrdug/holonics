# The exchange world-tube retained its branches, the contact fibre factored, and A1 passed

**Date:** 2026-08-22  
**Authority:** `blueprint/THE_ROADMAP.md` through
`blueprint/THE_ATHENA_VARIANTS_ARE_PRODUCTS_OF_EROS_PHOENIX_SUPPLIES_ANCESTRY_AND_SHARED_TRANSPORT_FUSES.md`,
Deed A1  
**Truth status:** established-bounded  
**Boundary:** exact captured prefixes of one operator-authorized Codex rollout, one Claude Code
main rollout and 37 Claude Code agent/workflow JSONL containers; JSON as an exterior codec; SHA-256
content addresses; one NVIDIA GeForce RTX 4080 SUPER; exact scalar-content contacts; visible prose,
parent/tool/session and chronology receivers; and the declared source-detached A1 rest. Hidden or
unavailable reasoning, semantic interpretation of message contents, sibling continuation defects,
cultivation and Athena behavior improvement remain open.

## Ruling

[established-bounded; implemented-exact; measured] A1 returns all ten required consequences. One
404,556,551-octet source occurrence streamed 59,445 complete records without retaining the raw
store whole. It founded 2,592,724 situated JSON nodes, 1,225,532 object field faces and 1,032,546
scalar contact sites. Eight exact quotient launches on the RTX 4080 SUPER returned 300,294 content
classes with no CPU semantic replay.

[established-bounded; implemented-exact] The resulting `ExchangeWorldTube` is not a transcript and
does not own provider roles. Record/node occurrence identity is source occurrence plus situated raw
range and local incidence; content equality is a separate class relation. Ten repeated visible
content classes therefore retain multiple distinct occurrence identities rather than becoming one
message.

## Construction declaration

[definition] A1 composes the content-addressed source mouth, exact JSON exterior codec, ordered
record/tree incidence, the material-free CUDA hash quotient, visible dialogue membranes, the
complete-return interchange owner and one source-detached rest wire. Its ports are captured source
prefix, record occurrence, parent/child node incidence, scalar content address, receiver-selected
field face, visible-message projection, chronology return and durable rest.

[definition] The caused circulation is

```text
captured prefixes -> streamed record occurrences -> exact tree incidence ->
resident equal-scalar quotient -> receiver joins / visible faces ->
chronology return -> rest seal -> detached remount -> later receiver question.
```

The local constitutive law groups only equal complete scalar content addresses. JSON keys,
`provider`, `role`, `agent`, `tool`, file kind and host path cannot alter the quotient. Those faces
remain exterior coordinates through which a receiver may ask a question of the already-founded
classes.

## Incremental occurrence and incidence

[established-bounded; implemented-exact] All 39 file extents were captured before the first read.
Each prefix was then hashed while one JSONL record at a time crossed the exact scanner. A final
unterminated invalid tail would have returned an addressed excluded range; this occurrence carried
zero such tails and zero blank records. The scanner validates JSON number grammar as a word and has
no numeric cast or floating-point path.

[established-bounded; implemented-exact] Composite node content is the ordered digest of node kind,
child position and child content. String content is decoded exactly across JSON escapes before its
content address is formed. Whitespace presentation therefore rebases without changing tree
content, while record SHA-256 retains the exact serialized occurrence.

## Resident contact and factorized correspondence fibre

[established-bounded; implemented-exact; measured] Four complete 64-bit faces of every 256-bit
scalar address crossed the existing `CudaRefineExecutor` quotient. Each face takes two 32-bit
device hash-join passages, hence eight launches. The card reported 1,032,546 sites in 300,294
classes, of which 59,751 classes repeat and carry 792,003 situated sites. Device block width was
1,024 threads and warp width 32.

[established-bounded; implemented-exact] Device slot identities were rebased by first situated
occurrence after return. This changes only an apparatus numbering and makes the same returned
partition serialize identically; it does not recompute equality on the host.

[established-bounded; implemented-exact; measured] The exact cross-container `sessionId` contact
class represents 27,509,601 legal occurrence pairs. The final owner retains the complete member
fibre, its exact pair cardinality and one situated representative per inhabited join class. It does
not materialize a 27,509,601-row pair sheet. This is the A1 instance of holonic compression:
factor the future receiver consequence through class membership while retaining the complete
reconstruction fibre.

## Dialogue, agent and world-return receivers

[established-bounded; implemented-exact] The Codex membrane returned visible
`response_item/message` faces only through the same captured prefix. A Claude Code membrane returns
only user/assistant prose blocks. Tool calls, tool results, thought/reasoning records and workflow
events remain present as richer record incidence; they are not mislabeled as visible prose or
silently deleted. The mounted occurrence carries 1,179 visible messages and eight generated
control surfaces excluded only from the visible membrane.

[established-bounded; implemented-exact; measured] Exterior key-face receivers opened 7,398 exact
`parentUuid -> uuid` parent-chain pairs, 2,097 Claude `tool_use_id -> id` world returns and 8,077
Codex `call_id -> call_id` world returns. Thirty-four parent faces have no matching UUID in the
declared occurrence and remain unresolved; none is guessed. Exact cross-container parent-UUID
matches are zero. Cross-container branch attachment is separately witnessed by the complete
27,509,601-pair session fibre, so parent chronology and session membership are not conflated.

## Chronology, ablation and detached rest

[established-bounded; implemented-exact] A deliberate permutation of two cross-container record
occurrences changed the ordered-record chronology digest. The existing complete-return
`certify_front` owner returned an ordered verdict rather than an interchange certificate. Equal
content or shared session membership therefore does not authorize the swap.

[established-bounded; implemented-exact] Provider ablation removed 39 provider lineage faces;
material-kind ablation removed 39 file-kind faces. Each changed the exterior-lineage digest and
left the content-law digest exactly unchanged. This is an intervention result, not an assertion
that provider lineage never matters to any later receiver.

[established-bounded; implemented-exact; measured] The 264,138,601-octet `EWTB/v1` rest has
SHA-256 `2b0d912f84183265b87b03a97ad6074a904794b0b17ff56c6d86e31dc3d83db4`.
The frozen 994,240-octet remount apparatus ran in a private `bwrap` namespace containing only
system libraries, the apparatus, the read-only rest and a writable return port. No source path or
repository was mounted. It rebuilt every record, node, field, scalar class, visible face, open
fibre, exclusion and apparatus receipt, re-encoded the rest to the same SHA-256, and returned exact
atlas testimony. The source occurrence address is
`1ea4d6322d8e8201df998d8ca185a66c0bb1c5bbdb40da0e6c48187cfb15de32`; the native content-law
address is `4bb0ff38cf90780581e9825a69adf7ff5d58f2b9b9c2f2f902d2b68d1801d389`.

## The ten-return grade

[established-bounded; implemented-exact] `grade.json` returns `10/10`:

| Return | Consequence |
|---:|---|
| 1 | incremental captured-prefix mount without holding the whole raw store |
| 2 | every visible message factors through one richer record occurrence |
| 3 | exact parent/child, agent-session and tool/world joining testimony |
| 4 | identical visible content retains distinct situated occurrence identity |
| 5 | unlicensed branch permutation changes chronology and is refused by interchange |
| 6 | source-detached remount reproduces the complete atlas |
| 7 | provider and material-kind ablations preserve the content law |
| 8 | hidden reasoning and authored role semantics do not route native topology |
| 9 | complete correspondence fibres and excluded populations are retained |
| 10 | the emitted real artifact was reopened and inspected |

## Ownership-ratchet disposition

[established-bounded; implemented-exact] The A1 ownership positions admitted in
`meta/HOLONIC_DSA_BASELINE.tsv` are owner-local populations, complete correspondence fibres,
returned receiver populations, or exterior wire buffers. They do not license cloning the continuing
`ExchangeWorldTube`, which deliberately has no `Clone` implementation. The exact dispositions are:

| Owner | Admitted positions | Constitutive reason |
|---|---|---|
| `dialogue_lineage.rs` | `Vec=7` | captured-prefix buffer and returned visible occurrence population at the exterior membrane |
| `json_record.rs` | `Vec=11` | one record's ordered node, field, scalar and child incidence while that record is crossing |
| `exchange_world_tube/mod.rs` | `.clone()=2`, `BTreeMap=2`, `Vec=8` | complete class fibres and one-owner atlas populations; both clones occur only in the CUDA fixture's typed ranges |
| `mount.rs` | `.clone()=3`, `.collect()=1`, `.into_iter()=2`, `BTreeMap=4`, `Vec=17` | borrowed exterior lineage transfer, one contiguous device key face, owned record transfer, apparatus-class rebase and complete class membership |
| `receiver.rs` | `.collect()=3`, `.into_iter()=1`, `BTreeMap=6`, `BTreeSet=2`, `Vec=12` | receiver indexes, one representative per inhabited join class, repeated-content fibre and the existing interchange return interface |
| `rest.rs` | `BTreeMap=2`, `Vec=19` | exterior wire emission/remount and reconstruction of the complete class fibre |

[counterexample; implemented-exact] The first ratchet candidate used a `BTreeSet` solely to recount
the device classes after their canonical rebase. The already-owned canonical map carries that exact
cardinality, so the redundant set was removed rather than admitted.

## Falsifiers which changed the owner

[counterexample; computational-witness] The first real pass refuted the scanner's claimed linear
behavior. Each UTF-8 character revalidated the complete remaining string, making a large record
quadratic. That pass was interrupted rather than reported. The scanner now validates only the
current one-to-four-octet scalar; the final 404,556,551-octet mount plus resident quotient took
3,828 ms.

[counterexample; computational-witness] The first complete A1 receiver incorrectly demanded that
`parentUuid -> uuid` cross file-container boundaries. The actual occurrence returned zero such
pairs while returning complete parent chains within agent world-lines and shared session incidence
across them. The contract was corrected to keep those two relations distinct, and the zero remains
visible.

[counterexample; computational-witness] The first passing receiver expanded the shared session
class to 27,509,601 explicit pair witnesses and spent 12,597 ms in receiver questions. Factorizing
the same exact population through class membership reduced the final receiver phase to 826 ms
without changing its pair cardinality or reconstruction fibre.

## Timing and artifact boundary

[established-bounded; measured] In the final release executable, streaming mount plus device
quotient took 3,828 ms, visible projection 1,139 ms, receiver questions 826 ms, and rest seal plus
isolated detached remount 2,013 ms; the complete deed took 7,813 ms. The separate incremental
release build took 27.52 seconds and the command wall including compilation took 35.64 seconds.

[established-bounded; measured] The inspected artifact family is
`output/the_exchange_world_tube_retains_its_branches/product`. Its six-file, 265,137,360-octet
family includes the frozen inference apparatus and detached receipt. The rest, manifest, grade,
inspection, apparatus and receipt are addressed by the output manifest regenerated at this phase's
release boundary. Its family SHA-256 is
`af047f8dfe53dfb595ad28c32576aa8c9ff779d6bc7a473dd7742904826466f4`; its producing Rust closure
has SHA-256 `7feaf4adcc82ad1e0cf8a1d3c7428083e82b98ee4776225188ca5823957109a6` over 106 closure members.
Output telemetry and deterministic semantic receipts remain separate.

## Release receiver and expensive-invocation ledger

[established-bounded; computational-witness] The final real-deed command was:

```text
cargo run -p life --release --example the_exchange_world_tube_retains_its_branches -- \
  --output output/the_exchange_world_tube_retains_its_branches/product \
  --codex /home/b/.codex/sessions/2026/08/18/rollout-2026-08-18T07-52-09-01a0155b-c0f1-7b11-84ae-003379f5eb69.jsonl \
  --claude-main /home/b/.claude/projects/-home-b-Workspaces-holonics/32fb276f-7b88-4788-b19b-1ffb2fe39b70.jsonl \
  --claude-branches /home/b/.claude/projects/-home-b-Workspaces-holonics/32fb276f-7b88-4788-b19b-1ffb2fe39b70
```

[established-bounded; measured] That invocation returned exit status zero in 35.64 seconds against
producing closure `7feaf4adcc82ad1e0cf8a1d3c7428083e82b98ee4776225188ca5823957109a6`.
Its purpose was the one real A1 GPU/deposit/remount deed after the ownership correction. The final
`python3 tools/output_manifest.py` invocation returned exit status zero in 38.19 seconds; its
purpose was to bind the resulting six-file family once after the return stood.

[established-bounded; measured] The sole complete release receiver, `bash tools/gates.sh`, returned
exit status zero in 242.95 seconds against the same A1 closure. All thirteen scopes passed: 2,820
tests passed, none failed and 22 were explicitly ignored; all example targets type-checked; and the
authored-level, named-path, line-citation, claim-index, equation-atlas, driver-catalog,
output-manifest, closure-manifest, boundary-artifact, Typst, architecture and document-law scopes
were green. Its purpose was the coherent A1 release grade; it was not run during owner-local edits
and was not replayed.

## Open exterior and next deed

[open] A1 mounts and addresses an inherited exchange organ. It does not train Athena, judge which
response is better, invent unavailable teacher interiors, or establish a sibling continuation
defect spectrum. Those are A2 and later cultivation obligations.

[open] A2 must derive a sibling cohort from the A1 operation/contact classes, presentation
generators, shortest separators and open fibres; hold Athena rest and history fixed; emit plural
continuations; and return the complete receiver-indexed defect complex without a scalar winner or
provider-priority rule.

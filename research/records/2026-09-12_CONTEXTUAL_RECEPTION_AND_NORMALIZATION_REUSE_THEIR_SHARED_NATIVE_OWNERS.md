# Contextual reception and normalization reuse their shared native owners

[definition] Parent `55fb2329`. The active goal continues beyond the delivered mathematical
workshop. Brandon's subsequent direct correction requires consolidation and maintenance during
implementation, not as a separate cleanup offer. This return develops the source/predictive
path and reconciles its normalized receiver with the existing consumer in the same increment.

## Source and observed-current binding

[established-bounded; implemented-exact; computational-witness] `NativeWaveSeedSpec` and
`with_seeded_wave_session` expose the existing applied normal recurrence from a caller-declared
alphabet, two actual seed symbols and numerical grain. The workbench's existing `wave-session`
accepts `--seed`; its previous model-directory and `--resume` paths remain available. Seed and
resume conflict both in the CLI and in the process owner. The existing rest/checkpoint owner is
used without another model container or serialization mechanism.

[definition] `NativeWaveSession::receive_next_symbol` now binds the existing `ResidentNormalWave::receive`
through the shared JSONL `receive-next-symbol` command. Source actuation first applies the actual
prefix to the held native joint current. Reception then forms `x=(c-p,c,p)` and `y=v-c` from the
same pre-observation state and actual observed next current. It accumulates the existing native
normal sufficient statistics, fits the next material, and publishes `(c,v)` with that material.
No guessed symbol is emitted/re-entered before this developmental observation. The explicit
prediction-handle path remains available for its addressed-observation task.

[definition] The experiment's symbol observation declares the complete next-current chart as a
unit symbol. This port does not silently supply an arbitrary latent-state/partial-output map.
The pre-observation preparation affects its source moments; wider hidden-state/output charts
must use their actual receiver maps. Statistical weighting is the existing unit-prior normal
objective, not an affine compatibility span treated as a probability distribution.

[established-bounded; source-inspected] The source kernel's passive union is `q' = y + A q`,
where `x=(a,b)`, `y=(b,b+delta)`, `d=x-y`, and
`A=I-2dd*/(||d||^2+abs(||y||^2-||x||^2))` at its declared real-current receiver. It processes
the supplied source pairs sequentially and retains their contemporary joint result, not their
row history. Even initial zero predictive coefficients do not imply identity source actuation:
`delta=0` still gives `y=(b,b)` and the source union adds that current. An earlier delegated
interpretation that zero delta erased prefix influence was rejected by inspecting
`normal_source_actuation.cuh` and `passive_current_ball.cuh`.

[established-bounded; computational-witness] Eight cycles of caller-supplied `ax -> a`,
`bx -> b` development (16 observations) produce `a` and `b` at previously unused source
preparations `aax` and `bbx`. The native basis reports strictly separated selections in that
run. The same two held-out returns pass after process termination and checkpoint remount.
This is a bounded contextual next-symbol application, not evidence of general useful replies,
code generation or the complete contextual-formation packet. The latter remain goal work.

## One normalized receiver, not another Wave type family

[definition] The initially introduced `NormalWaveNormalizedReturn` and
`NormalWaveNormalizedReading` were removed before integration, along with their five-layer
public forwarding. The existing `NativeNormalizedMaterialReturn` and reading now parameterize
provenance, chart and source ownership. Their defaults preserve the field consumer's source,
receiving, target-chart fields and owned immutable carriers. A normal reception borrows its
already-owned complete comparison. There is one report decoder and one probability/covector
layout, rather than a second copied implementation or a type-erased generic tensor.

[definition] The field source pullback was updated with its owning receiver. It still checks
the actual field owner and retains the original observation carrier; a wave-bound receiver
cannot be passed as that differently typed field capability. Reuse preserves source authority
and original complex carriers, rather than deleting the distinctions to shorten the names.

[definition] `normalized_compare_faces` now owns the shared CUDA normalization, observed
measure, `q-p`, centered residual and `J_p(q-p)` arithmetic. The existing field entry uses it.
The new `section_normalized_sum_receiver` takes two addressed current balls and compares their
sum with the received ball. Its offsets are supplied by the producing chart. This eliminates
the unfinished kernel's header-order dependency on a later normal-report declaration and
keeps the affine receiver reusable outside this particular recurrence. One block prepares
the shared scratch and processes its groups; no concurrent same-address scratch writes remain.

[definition] For the normal-reception consumer, the anchor is the retained `c`; the increment
is the **pre-fit** `SOURCE_FORWARD` report, not its post-fit `Forward` block. The predicted
next-current ball is `c+delta`, with the sum of the two outward radii. Its finer joint source
remains borrowed by the return. The observed unit-symbol current supplies normalized
squared-modulus mass `q`; the predicted real potentials supply `p=softmax(Re(c+delta))`.

[proved-derived; formal-checked] The new lemma
`HolonicAdjointNormalization.NormalizedExponential.face_add_eq_iff_anchor_constant` proves
that `softmax(c+delta)=softmax(delta)` exactly when `c` is constant across the admitted finite
population. It derives from the existing complete pairwise-potential-difference theorem.
This is the source-map condition for retaining the current anchor, not another separate
normalization theory. Existing log-mass and Laplacian-return laws remain its dependencies.

[definition] The shared receiver returns `q-p`, the negative cross-entropy/KL derivative in
the unit real-potential chart, separately from the squared-probability-metric pullback
`J_p(q-p)`. Current development still uses the named normal objective. The probability receiver
does not silently claim that its returned covector has also been committed as a weight update.
The new `receive-next-symbol-distribution` command exposes this comparison with a caller-supplied
series aperture. A receiver failure after successful observation reports `observation_committed`
and its receiver refusal; it does not invite a duplicate observation. Invalid apertures refuse
before development.

## Verification and disposition

[established-bounded; process-audit] The targeted Lean build
`bash tools/lean_check.sh ElementaryHolonics.Computation.HolonicAdjointNormalization` passed.
The proof/import owner is the existing module; no Lean operation was added to inference.

[established-bounded; implemented-exact; computational-witness] The native normalized-reception
test starts with zero predicted increment and current `c=e_b`. It verifies
`1/4 < p_a < 1/3`, `2/3 < p_b < 3/4`, interval widths below `1/1024`, observed `q=(1,0)`,
and the corresponding opposite covector signs. Uniform probabilities would fail this check.
No intermediate section readout occurs during native comparison construction. The earlier
comparison remains unchanged after another actual observation without cloning the ecology.

[established-bounded; process-audit] Completed checks:

- Eight `holonics-hna` `native::normal_wave` tests passed, including the new source, normalized
  receiver and JSONL consumer checks plus existing emission/re-entry/rest controls, in 2.67 s.
- Six `holonic-engine` `receiver::normalized::` tests passed, including existing gauge,
  grouped/open-ball receivers and source pullbacks, in 93.74 s including their first-use costs.
- The seeded-wave workbench subprocess test passed after receiver consolidation (0.65 s),
  including fresh construction, actual source-conditioned development, publication, remount
  and the two held-out contextual outputs.
- The existing seven HNN stream controls and the wave-session parser/seed-conflict test passed.
- `git diff --check` passed; unrelated formatting-only changes from recursive formatting were
  removed while retaining the actual shared-owner/consumer changes.

[established-bounded; process-audit] The first repaired standalone normalized native test took
208.86 s. During diagnosis its CPU thread was active and a sampled GPU utilization was zero;
attachment for a stack trace was denied. It completed successfully before a termination
attempt found the process already absent. The later eight-test run above includes the same
kernel and took 2.67 s. These observations suggest a substantial first-use initialization cost,
but do not isolate its cause or establish a general steady-state benchmark. Cold-start cost
remains part of the application performance work; the long first run was not called a timeout pass.

[open] General contextual applications, partial-output/latent source maps, binding the returned
covectors through the appropriate native source/material adjoints, broader constituent formation
and the general dependent-programme compression remain active. Existing source/receiver wrapper
overlap must continue to be reconciled with those consumers. This return neither declares the
whole codebase consolidated nor schedules maintenance outside the implementation goal.

## Canonical source consolidation in the following increment

[definition] Starting from `2dbe4844`, `NormalWaveStep` and `NormalWaveJointSource` were found
to carry the same immutable current pair, generating family and joint current; the former
additionally held the source's actual numerical metadata. They now use one concrete
`NormalWaveJointSource` implementation including that metadata. The old step name is only a
compatibility type alias, not another wrapper. `advance`, `joint_source`, source lifting and
the basis receiver use the same source owner and read-only accessors.

[definition] `NormalWaveReception` now retains that canonical producing source instead of
independently packaging its predecessor family, joint section and previous-current alias.
Its `previous()` is the actual producing source's current; its new `current()` and successor
family remain separate. The normalized receiver reads the canonical source's actual joint
section. Source and successor access is read-only, preventing callers from independently
rewriting the native receipt's current or source metadata. Decoded report fields and serialized
rest formats are unchanged; Rust consumers of the former public fields use the accessors.

[established-bounded; implemented-exact; computational-witness] The canonical-source test
checks shared current occurrence identity, the same joint and metadata allocations, the same
material, identical readings, and retained source validity after a later advance. Eighty-eight
recurrence/source/family/return tests passed (22.33 s), including existing base/programme
incorporation and compiled-family controls. The HNN native suite's 40 tests passed (14.92 s),
including contextual and normalized reception through the public adapter. Workspace example
compilation passed after the accessor changes; this is consolidation of the existing consumers,
not a facade around the former duplicate source implementation.

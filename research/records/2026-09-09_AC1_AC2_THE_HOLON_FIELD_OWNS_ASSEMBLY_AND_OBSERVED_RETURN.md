# AC1–AC2: the Holon field owns assembly and observed return

[project-postulate] Brandon resumes AC0–AC5 after the deeper architecture review and reiterates
that Holons encapsulate the transported objects. Media are declared source/receiver charts.
This return composes the existing current-field owners rather than founding a text-specific
reaction or another neural runtime. The whole Athena-alpha attainment standard remains open.

## The joint mechanism was already present

[established-bounded; source-inspected] `OperativeContextual` retains a visible source s and
the actual operative condition c, including its internal current. With homogeneous references,
write `s_hat=(1,s)` and `c_hat=(1,c)`. The native kernel evaluates

```text
Q(x) = x x† / (x†x)
Z(s,c) = Q(s_hat) tensor Q(c_hat)
K((s,c),(s',c')) = |<s_hat,s'_hat>|²/(||s_hat||² ||s'_hat||²)
                  * |<c_hat,c'_hat>|²/(||c_hat||² ||c'_hat||²).
```

[proved-derived] This is the Hilbert–Schmidt pairing of the two joint projector features.
The product follows from tensor pairing; the squared complex inner product follows by tracing
the product of rank-one projectors. Phase is combined before the nonnegative receiver. These
are mathematical wave/quantum-state chart relations, not a claim of physical calibration of
the apparatus. The complete source currents, reference, lineage and bounds remain beside K.

[established-bounded; source-inspected] `field_contextual_material.cuh::contextual_kernel` and
`contextual_context_pair` evaluate this versioned feature without constructing its dense tensor.
The source-qualified material factors act on it; the retained material pullback reaches the
source and context, and the paired adjoint reaches the producing contact map. This nonlinear
feature is distinct from the later `OperativeNormal` boundary-linear family. It was not missing
from the repository. Its existing controls and earlier scope stand; this return does not claim
that reinstating the profile alone solves language or makes its historical cost affordable.

## Assembly and response are now independent of the medium

[established-bounded; implemented-exact] `holonics::hna::native::{NativeFieldModelSpec,
with_native_field}` assembles the existing field from declared junction material, numerical
representation/solver and material source/target chart. `found_on` accepts an existing resident
surface. The field owns the continuing state and its native occurrence/emission/anchor types;
the seed is immutable apparatus/law data. No codec name, semantic label or new continuing
model copy enters that owner. An incompatible operative solver refuses rather than silently
changing the requested assembly.

[established-bounded; implemented-exact] The generic material return moves from
`TextFieldSession` into `NativeConstitutiveField::respond_to_material_observation`.
`NativeMaterialResponseChart` distinguishes complex-current, relative-entropy and squared-
probability receivers. Normalized charts require their actual group/series aperture. The method
uses the actual latest receiving occurrence, producing material and contact map; the observer
runs before the existing atomic deposit. It does not replay reception or add a target for a
source-qualified self-actuation. Text delegates to this owner and retains its historical
declared normalization aperture for compatibility.

[definition] Receiving and returning remain two explicit recoverable portions of this local
operation: a refused contact return preserves the post-reception field. This interface does
not claim rollback of the observation, automatic physical response from an absent target, or
an adjoint of the entire history. Existing current-family and projection/deposit distinctions
are unchanged. No new CUDA arithmetic or selection rule was introduced.

## Returned native consequences

[established-bounded; implemented-exact; computational-witness] Four public-field controls pass:
generic versus explicit complete successor equality under all three response charts; preserved
unpaired, rational-paired and enclosed construction; refusal of an incompatible solver; and a
later separated current family attributable to one local return. The explicit reference uses
the same public native factors in their former composition, not a fixture-local learner.
The fields are independently founded from the same declared finite material, never cloned.

[established-bounded; computational-witness] Five actual two-port occurrences include a return
at occurrence 2 to the retained source at 0, while the contemporary predecessor is occurrence 1.
That distinction retains source identity and local standing separately. A complex-current
contact response then changes the result at the next occurrence. With only that return withheld,
the two later material-current balls are disjoint: their squared centre difference exceeds the
squared sum of radii. The [receipt](../experiments/native_field_assembly/2026-09-09_return.json)
retains both full complex balls and the exact comparison. This is an attributed bounded change
of learned conduct; it is not evidence of general language acquisition.

[established-bounded; process-audit] Native reception and local return add no numerical section
readouts in those hot passages. Cold rest/inspection is explicit. The four native controls take
2.58 seconds after compilation. The text adapter's native diagnostic-failure/publication control
passes, as do 14 non-native alpha tests; 13 CUDA alpha cases were not invoked by that latter
filter. The SDK library and text example type-check. The initial test annotation and the
rational-junction solver-setting compatibility issue were corrected before the passing controls.

## Position and next use

[definition] This closes the revised AC1 finite assembly packet through an existing joint
mechanism and supplies AC2's finite observed-return/later-consequence packet. It does not close
broader cultivation or Athena-alpha attainment. AC4 has its first source-qualified comparisons;
the older numerical journal's reuse keeps its own scope. AC5 now has a medium-independent
assembly and return boundary; full research-artifact delivery remains active with AC3.
The roadmap orders further exposure, continuation and reuse. No one medium determines the
native carrier or defines a special learning law.

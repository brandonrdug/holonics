# THE TUBE CARRIES THE EXTERIOR FACE; THE DISTANT FIELD RETURNS THROUGH REBASE

**Date:** 2026-07-26  
**Status:** RATIFIED DIRECTION / EXACT RECEIVER-TUBE LEAF BUILT /
EXACT IDENTICAL-SECTION REUSE BUILT / HIERARCHICAL TRANSLATION LAW DERIVED /
GENERAL CERTIFIED FAR-FIELD HIERARCHY OPEN / NO GUI LAUNCH / NO CUDA RUN /
SOMA INTERIOR UNCHANGED

## Present question

The live receiver experiment repeatedly reconstructs continuous triangles,
threads, and conics and then restricts every one of them to the finite terminal
face. The pictures show recurring sections, winding traces, necks, and bright
crossing concentrations, but the implementation treats every successor as a
new terminal problem.

The exact question is:

> What geometric standing may retain a recurring carrier without turning it
> into a memoized bitmap, and how can many distant carriers return one
> receiver-relative field without evaluating every pair again?

The stopping condition for this construction is an exact receiver-tube carrier,
structural invalidation, exact projective holonomy, exact sharing of identical
presented sections, delta support radiation, full-trace equivalence, and a
derived boundary for the more general hierarchical field algorithm.

## 1. A tube is carried standing, not a stored image

For a receiver \(r\) with pivot \(v_r\), let

\[
H_r=\overline{\operatorname{St}}(v_r),
\qquad
L_r=\operatorname{Lk}(v_r).
\]

The terminal tube is

\[
\mathcal U_r
=
\left(
 r,\,
 H_r,\,
 L_r,\,
 S_r,\,
 T_r,\,
 \Omega_r,\,
 \tau_r
\right),
\]

where:

- \(S_r\) is the exact receiver-local primitive section;
- \(T_r\) is the caused projective map into the current presentation;
- \(\Omega_r\) is the composed projective holonomy since founding; and
- \(\tau_r\) is the finite terminal restriction of \(T_r(S_r)\).

The tube remains standing only while its receiver source event, star-link
horizon, local section, presentation transport, and terminal aperture remain
exact. Any changed carrier invalidates that tube. Unrelated tubes remain
standing.

If \(T_t\) and \(T_{t+1}\) are consecutive presentation maps, the exact chart
transport is

\[
D_{t+1,t}=T_{t+1}T_t^{-1},
\qquad
\Omega_{t+1}=D_{t+1,t}\Omega_t.
\]

No matrix normalization chooses an absolute scale. Common nonzero scale remains
projective gauge.

## 2. The current exact lifecycle

Every candidate tube is classified as one of:

\[
\mathrm{FOUND},\quad
\mathrm{REFOUND},\quad
\mathrm{DEFORM},\quad
\mathrm{TRANSPORT},\quad
\mathrm{APERTURE},\quad
\mathrm{RETAIN}.
\]

`RETAIN` performs no terminal trace. `TRANSPORT` composes holonomy and retraces
only the transported tube. `DEFORM` carries the same receiver continuity but
restricts its new local section. `REFOUND` resets founding holonomy.
`APERTURE` restricts every live tube to the new finite boundary. A departed
tube emits exact removal deltas.

Commit requires every and only the terminal traces requested by the plan. It
then emits

\[
\Delta\tau_r(a)
=
\tau_{r,t+1}(a)-\tau_{r,t}(a)
\]

for changed terminal addresses \(a\). This is a multiplicity difference, not a
pixel chronology. The display transducer changes only those supported members
or members whose receiver phase changed.

The complete full trace remains the grading authority. The retained and
transported unit cells are exactly equal to a fresh full restriction.

## 3. Greengard--Rokhlin supplies the missing spatial law

The Fast Multipole Method separates nearby direct interaction from distant
hierarchical interaction. Its central operations are:

1. local sources form an exterior multipole expansion;
2. child exterior expansions translate and join at a parent;
3. a separated exterior expansion translates into a receiver-local expansion;
4. parent-local expansions translate to children; and
5. unresolved nearby interactions are evaluated directly.

In holonic terms these are:

\[
\begin{aligned}
\mathrm{P2M}&:\text{constituents}\to\text{carried exterior face},\\
\mathrm{M2M}&:\text{child hand-up}\to\text{parent rebase},\\
\mathrm{M2L}&:\text{distant tube}\to\text{receiver-local face},\\
\mathrm{L2L}&:\text{parent standing}\to\text{child hand-down},\\
\mathrm{P2P}&:\text{direct contemporary contact}.
\end{aligned}
\]

This is not merely analogous to the laboratory theory. It is a classical,
working instance of the same principle:

> completed local constituents need not remain pairwise active when their
> consequential exterior relation can be carried and lawfully rebased.

The adaptive algorithm also discards empty boxes immediately and refines only
occupied regions. This is the exact computational face of constituent
departure and growing locality.

Primary sources:

- Leslie Greengard and Vladimir Rokhlin,
  [A Fast Algorithm for Particle Simulations](https://web.stanford.edu/class/cme324/classics/greengard-rokhlin.pdf),
  *Journal of Computational Physics* 73 (1987), 325--348.
- John Carrier, Leslie Greengard, and Vladimir Rokhlin,
  [A Fast Adaptive Multipole Algorithm for Particle Simulations](https://math.nyu.edu/~greengar/cgr_88.pdf),
  *SIAM Journal on Scientific and Statistical Computing* 9 (1988), 669--686.

## 4. What cannot be inherited

Classical FMM chooses a finite expansion order \(p\) from a floating precision
or tolerance and accepts the resulting truncated value. That is not the
holonic law.

The lawful exact replacement is a certified remainder relation. Let

\[
F=F_{\le p}+R_p
\]

and let exact rational bounds establish

\[
R_p(x)\in[\ell_p(x),u_p(x)]
\]

throughout a receiver boundary. The finite expansion may decide a discrete
receiver consequence only when every value permitted by that exact enclosure
has the same consequence. If the enclosure crosses the relevant boundary, the
algorithm must:

1. carry the result as open;
2. refine the source or receiver region;
3. retain more exact terms; or
4. evaluate the relation directly.

The conclusion is exact even though a finite bound enabled it. No approximate
field value becomes standing.

For the current line, triangle, thread, and conic species, the continuous
carrier is already a finite exact algebraic form. Its projective translation
is exact and owes no truncated multipole series. The certified-remainder law is
required when the engine introduces general long-range kernels.

## 5. The first exact exterior-face reuse

Before a general hierarchy is installed, the current atlas performs one
strictly exact spatial compression:

> changed receiver tubes whose complete presented primitive sections are
> structurally identical share one representative terminal restriction.

The representative trace is copied as receiver-relative testimony and the
receiver identity is restored. No hash, bitmap identity, coordinate
approximation, or statistical similarity participates. A fresh complete trace
of every receiver grades the result.

This is the \(0\)-distance exact boundary of multipole reuse. It proves the
executor and lifecycle relation without pretending that arbitrary separated
fields already possess a derived expansion law.

## 6. Tubes, necks, and wormhole language

A smooth tube around a core requires:

\[
\text{core}+\text{normal fibers}+\text{transport connection}
+\text{validity boundary}.
\]

The tube ceases to be a regular product at its cut/focal locus. In the
simplicial engine, the core is the receiver worldline, the transverse fiber is
its star-link section, the connection is exact chart transport, and a change of
link class or factorization is the validity boundary.

A mathematical wormhole-like handle is not zero-cost transport. It is a
founded throat joining two actual mouths. In this engine it would owe:

\[
\left(
H_a,\,
H_b,\,
\mathcal T_{ab},\,
\mathcal T_{ba},\,
\Omega_{\mathrm{loop}},\,
\text{source deed},\,
\text{transport cost}
\right).
\]

The usefulness is path factorization: many crossings may return through one
stable throat relation. The cost and holonomy remain visible. “Wormhole” may
therefore name a high-reuse transport neck, never teleportation or a hidden
pointer.

## 7. Open hierarchical construction

The next hierarchy is constrained, not guessed:

1. partition by caused receiver/star incidence, not an absolute display grid;
2. derive exact near/far admissibility from local support and the interaction
   kernel;
3. form exterior carriers at completed local regions;
4. translate them through exact rebase operators;
5. use rational remainder certificates only to decide discrete consequences;
6. refine or remain open where the certificate cannot decide;
7. retain expansion and translation standing while their certificates hold;
8. invalidate only descendants of failed certificates; and
9. grade every bounded species against direct exact interaction.

This hierarchy is not yet implemented because a universal far-field expansion
does not exist independently of an interaction kernel. The receiver-tube leaf,
holonomy, structural invalidation, shared identical-section restriction, and
delta radiation are implemented now. They are the correct substrate on which
kernel-specific exterior/local transports may grow.

## 8. Result

The recurring geometry is no longer required to masquerade as an entirely new
terminal image. It has an exact carrier, transverse horizon, transport map,
holonomy, terminal restriction, and lifecycle.

Greengard--Rokhlin supplies the missing computational theorem: distant
pairwise relations can be folded into hierarchical carried faces and translated
back into local receiver faces. Holonics strengthens the acceptance boundary:
the translated face may replace direct relations only under an exact
consequence certificate. Unresolved remainder remains open or returns to direct
contact.

## 9. Bounded acceptance

The built leaf was checked without launching the desktop or entering CUDA:

- four exact tube cells pass: retained standing, transported holonomy,
  identical-section sharing, and a live receiver with an empty visible section;
- the complete `holonic-engine` all-target suite passes: 67 passed, 2 ignored;
- all `holonic-engine` targets are clean under Clippy with warnings denied; and
- the workspace is clean under `cargo fmt --all -- --check`.

The empty-section cell is ontologically material. The set of receiver sections
visible in the current presentation may be a strict subset of the live receiver
population. Zero current crossing is not receiver departure.

## 10. First launch correction

The first user launch exposed one false outer-handler invariant. A compositor
resize changed the terminal aperture from `640x400` to `640x358`, correctly
retracing both live tubes. A repeated resize then derived the already-current
aperture. The tube plan correctly classified both tubes as `RETAIN` and
requested zero traces, but the outer resize handler declared a trace-free
resize unreachable and panicked.

The handler now accepts both lawful species:

- an actual aperture change commits the newly restricted tubes and rebuilds
  their terminal layers; and
- an unchanged aperture commits the retained plan, performs no trace, retains
  the exact layers, and only re-forms the outer display quotient.

The corrected all-target acceptance remains 67 passed / 2 ignored with Clippy
warnings denied. The desktop and CUDA were not launched autonomously during
the correction.
